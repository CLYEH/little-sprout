// 複製本檔為 Secrets.xcconfig（同目錄）——該檔名已被 .gitignore 排除，填了真實值也不會進 repo。
// 取得真實值：supabase MCP 的 get_project_url／get_publishable_keys（唯讀工具）。
//
// 切勿：①把這份 .example 檔改名成 Secrets.xcconfig 後原樣 commit ②把下面的值換成真實金鑰後
// 移除 .gitignore 的排除規則。Secrets 永不進 repo（CLAUDE.md 全域規約）。

// 網址中間用 $() 拆開兩個斜線：xcconfig 的 `//` 是註解起點，寫成一般的 `https://` 會從那裡
// 被截斷成 `https:`（LS-49 實測踩到，Config/Base.xcconfig 有同樣註記）。填真實值時務必保留這個寫法。
SUPABASE_URL = https:$()/$()/your-project-ref.supabase.co
SUPABASE_ANON_KEY = your-anon-or-publishable-key
