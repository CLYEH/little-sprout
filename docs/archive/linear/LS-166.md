# LS-166 Task：LS-20 iOS——相簿詳情（瀑布流照片牆／加入照片入口／寶貝標記／改名與刪除相簿）依 LS-142 稿實作，含深色、AX3、iPad、34 張壓測

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:M, lane:ui |
| Cycle | 2 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M3 相簿與上傳 |
| 估點 | — |
| 建立 | 2026-09-04T05:36:21.203Z |
| 開始 | 2026-09-12T02:48:26.825Z |
| 完成 | 2026-09-12T11:25:11.580Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-166/taskls-20-ios-相簿詳情瀑布流照片牆加入照片入口寶貝標記改名與刪除相簿依-ls-142-稿實作含深色ax3ipad34-張壓測 |
| 父票 | LS-20 Story：相簿與照片上傳（壓縮／縮圖／背景續傳） |
| 關係 | related→LS-230, related→LS-229, related→LS-226, related→LS-215, related→LS-205, related→LS-209, related→LS-211, related→LS-217, related→LS-130, related→LS-119, related→LS-216, related→LS-210, related→LS-152, related←LS-237, related←LS-146, related←LS-236, related←LS-233, related←LS-189, related←LS-212, related←LS-203, related←LS-200, related←LS-96, related←LS-167, blocks←LS-165, blocks←LS-142 |

## 描述

## 來源

[LS-142](https://linear.app/little-sprout-app/issue/LS-142/設計相簿頁相簿詳情上傳佇列ls-20-畫面群-ui-designervisual-reviewer-3-輪) 設計稿（已併 development `7f335d8`；核可頁 `2bee1766`，使用者已核可）。三張實作票之二。

Design: `LS-142 / 15 相簿詳情 (iPhone)`（`pVSXP`）、`15 相簿詳情 · 深色`（`yfZyT`）、`15b 空狀態`（`ZXdu2`）、`15c 刪除相簿 · 確認`（`w0NxC`）、`A11y / 15 AX3`（`OysBA`）、`15-iPad`（`rFiLJ`）、`Stress / 15 34 張全滿`（`FPaFl`）；Notes `kHDk4`（printH 分佈 143~445、`cellH = 8+wrapH+32`、corner-out 5、比例 4:3／1:1／3:4）。

**2026-09-06 補充（**[LS-212](https://linear.app/little-sprout-app/issue/LS-212/taskls-20-後端上傳管線-影片暫存檔與孤兒-media-清理補完上傳失敗中斷app-重啟後的本機暫存media-列存在但) **handoff 發現、orchestrator** `git grep` **核實）**：[LS-167](https://linear.app/little-sprout-app/issue/LS-167/taskls-20-ios-上傳佇列-sheet沒有成功正在進行已完成三群ls002-容量已滿列最前重試與背景續傳固定-detent依-ls) 交付的 `UploadQueueSheetView`／`UploadQueueStore` 目前**零 production 接線**（只有 preview／harness），production 上傳路徑是 `DiaryComposerStore`／`MediaUploadService`。本票是「加入照片」多選入口，**必須**把佇列 sheet 接上真實上傳路徑（見範圍 2）。

## 範圍

1. push 進來的畫面（無 Tab Bar）：瀑布流照片牆，每格白邊＋四角托沖印品（`cmp/Photo Print`／`Card Photo` 對應），真實比例混排；深色與亮色逐位元同幾何。
2. 「加入照片」入口：PhotosPicker 多選 → 建立上傳工作交給 `MediaUploadService`（沿 `DiaryComposerStore` 既有路徑或抽共用）→ **呈現** [LS-167](https://linear.app/little-sprout-app/issue/LS-167/taskls-20-ios-上傳佇列-sheet沒有成功正在進行已完成三群ls002-容量已滿列最前重試與背景續傳固定-detent依-ls) **的** `UploadQueueSheetView`**，**`UploadQueueStore` **由真實上傳狀態驅動**（等待／上傳中％／完成／失敗＋錯誤碼、LS002 額度、背景續傳回來的狀態）；完成後照片牆即時出現新格；App 被殺後重開的收斂依 [LS-212](https://linear.app/little-sprout-app/issue/LS-212/taskls-20-後端上傳管線-影片暫存檔與孤兒-media-清理補完上傳失敗中斷app-重啟後的本機暫存media-列存在但) 規則（`MediaDraftTempStorage`、孤兒軟刪）。相簿多寶貝標記（`set_album_children`）；編輯名稱；刪除相簿（`set_album_deleted`，owner／建立者限定，LS027／LS045 文案鍵）——刪除確認語彙依 `w0NxC`，**不承諾 30 天可還原**（[LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表) IN-1 裁決）。
3. 空狀態（Blank Print 牆格尺寸）；34 張壓測不掉幀（縮圖、LazyVGrid）。
4. 變體：深色、AX3、iPad（六格比例等比）。
5. 測試：XCTest（瀑布流尺寸計算對 Notes 公式、比例分類；佇列接線：真實 `MediaUploadService` 狀態 → `UploadQueueStore` 分群的映射）、UITests（進入詳情、加入照片 → 佇列 sheet 出現、刪除確認流程）、tap-target gate；截圖對稿。AX 字級測試一律 `launchArguments`（不用 `launchEnvironment`，[LS-210](https://linear.app/little-sprout-app/issue/LS-210/taskls-133-ios-設定頁法律區兩列改開-legaldocumentsheet目前仍-link-到外部) review 實測無效）。

## 不做

相簿列表（[LS-203](https://linear.app/little-sprout-app/issue/LS-203/taskls-20-ios-相簿-tab-列表改讀-album-summaries-viewvisible-media)）、上傳佇列 sheet **本體視覺**變更（[LS-167](https://linear.app/little-sprout-app/issue/LS-167/taskls-20-ios-上傳佇列-sheet沒有成功正在進行已完成三群ls002-容量已滿列最前重試與背景續傳固定-detent依-ls) 已 Done；本票只接線）、照片全螢幕檢視器變更（若既有）。

## 驗收

- [ ] PR body `Design:` 列上述板名；QA 截圖比對（含 34 張壓測與 AX3）；**QA 實跑：多選 3 張 → 佇列 sheet 出現並走到「已完成」、關閉後照片牆有新格；斷網一張失敗列可重試**
- [ ] 尺寸公式單元測試與 Notes 一致；UITests 綠；push gate 綠
- [ ] merge-reviewer（效能：列表載縮圖、圖片解碼不在主執行緒；race：佇列狀態與 UI 更新 actor 隔離）、QA PASS

## 留言（14）

### 2026-09-12T02:48:30.366Z · Chih-Lin Yeh · `48a32f27`

**orchestrator 09-12 10:48**：ui lane 空 1（LS-216 併入 dev 轉 QA）→ 補位本票（cycle 2、High、blockedBy LS-165／LS-142 皆 Done）。worktree `.claude/worktrees/LS-166`＝`feature/LS-166-album-detail`（自 development `2d189b9`，含 LS-216 的 `TimelineStore` 拆檔），已派 ios-dev：設計以 worktree 內 `.pen` JSON 離線讀（板 `pVSXP`／`yfZyT`／`ZXdu2`／`w0NxC`／`OysBA`／`rFiLJ`／`FPaFl`＋Notes `kHDk4`），不碰 Pen；證據 `.claude/evidence/LS-166/r1/`；E2E 持 lock。→ In Progress。

### 2026-09-12T04:49:56.360Z · Chih-Lin Yeh · `409e29eb`

## Ticket

LS-166：Task：LS-20 iOS——相簿詳情（瀑布流照片牆／加入照片入口／寶貝標記／改名與刪除相簿）依 LS-142 稿實作，含深色、AX3、iPad、34 張壓測

## 已完成

1. **資料層 API**（`4936a33`，`LittleSprout/Services/Albums/AlbumsAPIClient.swift`／`SupabaseAlbumsAPIClient.swift`／`AlbumsModels.swift`／`AlbumsStore.swift`／`PreviewAlbumsAPIClient.swift`／`StubAlbumsAPIClient.swift`）：新增 `fetchAlbumMediaLinks(albumID:)`、`attachMedia(albumID:familyID:mediaID:sortOrder:)`、`updateAlbumTitle(albumID:title:)` 三支協定方法＋ Supabase／Preview／Stub 三份實作；新增 `AlbumMediaLinkRow` 型別（`AlbumsModels.swift:117-131`）；`AlbumsStore.apiClient`（`AlbumsStore.swift:25-29`）從 `private` 改 internal 供重用。
2. **AlbumDetailStore**（`84ba759`，`LittleSprout/Services/Albums/AlbumDetailStore.swift`，全新檔）：`refresh()`（連結列→media→簽名→依 sortOrder 由大到小排，RLS 濾掉的連結安靜跳過不 throw）、`attachUploadedMedia(_:)`（LS-167 佇列上傳成功掛鉤接線端，best-effort 不對外拋錯）、`submitEdit(title:childIDs:)`（只有真的改過的那一半才打對應請求）。
3. **瀑布流照片牆版面**（`e4571ba`，`LittleSprout/Services/Albums/AlbumPhotoGridLayout.swift`／`LittleSprout/Features/Albums/AlbumPhotoGridView.swift`，全新檔）：`AlbumPhotoGridLayout`（純函式，貪婪分欄，依 Notes `kHDk4` `vfPjM`／`k3jJ5j` 公式）＋`AlbumPhotoGridView`（每格白邊＋四角托沖印品，`LazyVStack` 逐欄延遲載入）。
4. **編輯／刪除相簿 sheet**（`d6446dc`，`LittleSprout/Features/Albums/EditAlbumView.swift`／`AlbumDeleteConfirmationSheet.swift`，全新檔）。
5. **相簿詳情主畫面＋佇列真實接線**（`0aaa6da`，`LittleSprout/Features/Albums/AlbumDetailView.swift`／`AlbumDetailView+Actions.swift`（全新）／`LittleSprout/Services/Albums/UploadQueueStore.swift`／`AlbumsView.swift`／`LittleSprout/Navigation/RootView.swift`）：`UploadQueueStore` 新增 `onUploadSucceeded` 掛鉤（`UploadQueueStore.swift:32-38`、`197-207`）。
6. **測試＋tap-target gate 註冊**（`15a4cd3`，`LittleSproutUITests/AlbumDetailViewTests.swift`／`AlbumDetailAX3UITests.swift`（全新）／`TapTargetGateTests.swift`／`TapTargetGateScreenName.swift`／`TapTargetGateHarness.swift`／`TapTargetGateHarness+Albums.swift`／`scripts/gates/tap-target-exemptions.txt`）。
7. **XcodeGen 同步**（`e845acf`，`project.pbxproj`）。

## 已驗證（怎麼驗的）

- **對應驗收「尺寸公式單元測試與 Notes 一致」**：`AlbumPhotoGridLayoutTests`（`LittleSproutTests/AlbumPhotoGridLayoutTests.swift`，11 個測試）——`test_place_iPhoneContentWidth_uses2ColumnsAt164_5`／`test_place_iPadContentPaneWidth_uses3ColumnsAt167` 釘住欄數欄寬；`test_photoHeight_ratio4to3_matchesNotesSHn1L`／`test_photoHeight_ratio1to1_matchesNotesUuum0`／`test_photoHeight_ratio3to4_matchesNotesIS0DY` 對 Notes `k3jJ5j` 六格實測值（111.5／148.5／198）；`test_cellHeight_equals8PlusWrapHPlus32` 釘住 `cellH = 8+wrapH+32` 公式。指令：`xcodebuild test-without-building -only-testing:LittleSproutTests/AlbumPhotoGridLayoutTests`，11/11 綠。
- **AlbumDetailStore 邏輯**：`AlbumDetailStoreTests`（`LittleSproutTests/AlbumDetailStoreTests.swift`，12 個測試）全綠——涵蓋 refresh 排序／RLS 過濾／失敗態／空相簿、attachUploadedMedia INSERT／sortOrder 遞增／失敗不拋錯／防重複、submitEdit 只改一半／兩者皆改／失敗不落地。
- **UploadQueueStore.onUploadSucceeded 真實接線**（LS-167／LS-212 補充要求）：新增 `test_onUploadSucceeded_firesWithOriginalIDAndMediaID_onlyOnSuccess`——改了什麼：`UploadQueueStore.start(_:)` 成功分支加一行 `self.onUploadSucceeded(id, mediaID)`（`UploadQueueStore.swift:200-207`）→ 哪條測試紅：拿掉這行呼叫後該測試斷言 `recorded.count == 1` 失敗 → 斷言訊息原文：`XCTAssertEqual failed: ("0") is not equal to ("1") - 只有成功那筆該觸發掛鉤`。
- **對應驗收「UITests（進入詳情、加入照片 → 佇列 sheet、刪除確認流程）」**：`AlbumDetailViewTests`（5 個，`LittleSproutUITests/AlbumDetailViewTests.swift`）全 PASS——`testOwnerSeesBackButtonMoreMenuAndAddPhotosButton`／`testMemberDoesNotSeeMoreMenu`／`testBackButtonPopsToAlbumsList`／`testMoreMenuEditOpensEditAlbumSheet`／`testMoreMenuDeleteOpensConfirmationSheet_cancelDismissesWithoutDeleting`。
- **對應驗收「深色、AX3」**：`AlbumDetailAX3UITests.testOwnerNavRowButtons_ax3_remainHittableAndDoNotOverlap` PASS（硬規則 minHeight≥48）；`TapTargetGateTests` 新增 3 個（owner／member／editAlbum）PASS。
- **全量回歸**：`LittleSproutTests`（本機獨立跑）912/912 綠；`LittleSproutUITests`（扣 QASmokeTests）127 個、1 skipped、0 failures。
- **Gate**：`swiftlint lint --quiet` 0 warning／0 error；`tap-target-registry-check.sh`／`api-contract-check.sh`／`error-codes-check.sh` 皆通過；push gate（unit tests＋lint＋registry）綠燈通過後 push（見下方 push-gate flake 說明）。
- **模擬器截圖對稿五變體**（對應驗收「QA 截圖比對（含 34 張壓測與 AX3）」，`.claude/evidence/LS-166/r1/`，經 `TapTargetGateHarness` launch-environment 機制驅動，`AlbumDetailScreenshotAPIClient` 餵真實比例混排假資料）：
  - `album-detail-light.png`（亮色，12 張混合比例，`.albumDetailPopulated`）
  - `album-detail-dark.png`（深色，確認 `$print-paper` 不變黑、同幾何）
  - `album-detail-ax3.png`（AX3，Nav Row／Action Bar 不重疊可觸達）
  - `album-detail-ipad.png`（iPad——已知落差，見「未完成」）
  - `album-detail-stress34-top.png`（34 張壓測，`.albumDetailStress`）
- **模擬器對本機容器互動驗證（E2E，`supabase-lock.sh --hold`，怎麼驗：`SIMCTL_CHILD_LS_QA_API_URL`／`SIMCTL_CHILD_LS_QA_ANON_KEY` 指向本機 Supabase 容器走 `SupabaseClientFactory.qaOverride`，真實 Email OTP 登入→建立家庭→建立相簿→進入詳情→PhotosPicker 多選 3 張→佇列 sheet 三列皆「已完成」→關閉後照片牆出現 3 張新格）：
  - 第一段「LS-166 dev E2E」持有 10 分 9 秒（截圖驗證階段，未寫入容器資料）。
  - 第二段「LS-166 dev E2E upload flow」持有 17 分 3 秒——真實流程全程走完，`e2e-upload-completed-grid.png` 為證（`3 張相片`，真實比例反映在瀑布流版面）。

## 未完成

- **斷網一張失敗可重試（票文驗收條件）未真人手動重現**：本輪 lock 時間預算（20 分鐘窗口）優先完成「多選 3 張 → 佇列到已完成 → 照片牆出現新格」主流程真實驗證。**處置**：留給 QA 在 `test` branch 驗收時實跑（票文驗收條件本來就要求 QA 這段）；邏輯正確性已由既有 `UploadQueueStoreTests.test_failedUpload_networkError_isRetryable_andRetryRestartsIt`（PASS）與新增 `test_onUploadSucceeded_firesWithOriginalIDAndMediaID_onlyOnSuccess`（PASS，確認失敗那筆不誤掛進相簿）覆蓋。
- **iPad 截圖欄數與設計稿不符（5 欄非 3 欄）**：純粹是 harness host（`albumDetailPopulatedHost`）沒有包在真正的 `SectionSplitView` 內、缺少 Nav Sidebar，量到的可用寬度比正式環境寬；不是程式碼缺陷——`AlbumPhotoGridLayoutTests.test_place_iPadContentPaneWidth_uses3ColumnsAt167` 已用正式環境實際寬度 533pt 驗證公式產生 3 欄／167pt colW，與設計稿一致。**處置**：不修＋理由如上，QA 在真實 iPad 導覽路徑驗收時會看到正確的 3 欄。
- **「更多」選單可見性採 Notes（僅 owner）而非票文字面（owner／建立者限定）**：docs/API.md 顯示 `set_album_children`／`albums.title` 直接 `.update()` 實際授權皆為僅建立者本人（比「owner 任何一本」更窄），Notes `OHMPk` 是唯一針對「這顆選單何時可見」給出明確文字的來源，以其為準；owner 若不是建立者，送出編輯／刪除會收到 `42501`／`LS045`，錯誤文案會呈現（`AppError.userFacingMessage` 既有映射），不會靜默失敗或 crash。**處置**：不修＋理由如上；如需要更精確的可見性規則需另開票調整設計稿。
- **影片格徽章／播放未套用於相簿詳情照片牆**：同 Notes `ThcXO` 記錄的範圍控制（本輪未套用，非新規則）；PhotosPicker 支援 `.any(of: [.images, .videos])` 上傳，但照片牆不特別標示影片、不可點播放。**處置**：不修＋理由如上，若後續需要另開票沿用 LS-119 既有徽章語彙。
- **`DeleteAccountFlowModelTests` push-gate 首次疑似平行執行 flake**：首次 `git push` 跑 push-gate 時 3 個測試失敗（`XCTAssertNil failed: "inProgress"`），本票完全未觸碰 `DeleteAccountFlow*` 任何檔案；`xcrun simctl erase` 後單獨重跑該測試類別（28/28 綠）與全 `LittleSproutTests`（912/912 綠，含該類別）皆通過，判定為與本票無關的既有平行執行序 flake。**處置**：已記錄於 LS-96 comment `98a571c6`，非本票修復範圍。

## 風險

- `AlbumDetailScreenshotAPIClient`（截圖用假 client，`TapTargetGateHarness+Albums.swift`）的 `signedURL` 恆為 `nil`——截圖顯示灰底占位，只驗證版面幾何；真實圖片載入已由 E2E 互動驗證額外覆蓋。
- `AlbumDetailStore.attachUploadedMedia` 是 best-effort：`album_media` INSERT 失敗時這張照片已合法上傳成功（`media` 列存在）但沒有掛進這本相簿，使用者不會被明確告知——刻意的取捨，理由見該方法文件註解「best-effort，不對外拋錯」段。
- `nextSortOrder` 是純記憶體計數器（同一次 session 內遞增），不同裝置／不同 session 同時對同一相簿加照片時可能撞相同 `sortOrder`（DB 無 unique 約束），僅影響顯示順序美觀度，不影響資料正確性。

## 產出位置

- PR：https://github.com/CLYEH/little-sprout/pull/356（`feature/LS-166-album-detail` → `development`）
- Head SHA：`e845acf`
- Commits：`4936a33`（資料層）／`84ba759`（AlbumDetailStore）／`e4571ba`（照片牆版面）／`d6446dc`（編輯／刪除 sheet）／`0aaa6da`（主畫面＋佇列接線）／`15a4cd3`（測試＋gate 註冊）／`e845acf`（xcodegen）
- 截圖：`.claude/evidence/LS-166/r1/`（`album-detail-light.png`／`album-detail-dark.png`／`album-detail-ax3.png`／`album-detail-ipad.png`／`album-detail-stress34-top.png`／`e2e-upload-completed-grid.png`）
- E2E 持有時長：「LS-166 dev E2E」10 分 9 秒＋「LS-166 dev E2E upload flow」17 分 3 秒
- 模擬器已關：`585B009E-05D0-4F55-B46F-817A75CA2198`（LS-166-iPhone17Pro）、`403B3590-73EE-4922-9649-60DF742F5FD6`（LS-166-iPadPro13，本輪新建）

### 2026-09-12T04:51:47.094Z · Chih-Lin Yeh · `edf4e34e`

**orchestrator 09-12 12:5x**：R1 handoff `409e29eb`——PR #356 head `e845acf`（`AlbumDetailStore`／`AlbumPhotoGridLayout`／`AlbumDetailView`＋Actions／`EditAlbumView`／`AlbumDeleteConfirmationSheet`；`UploadQueueStore.onUploadSucceeded` 真實接線）；912 單元／127 UI 綠、push gate 綠；E2E 三張上傳到「已完成」＋牆新格（`.claude/evidence/LS-166/r1/`）；未完成兩項（斷網重試未手動重現、iPad 截圖欄數受 harness 限制）；無關 flake 入池 `98a571c6`。→ In Review，已派 merge-reviewer R1（效能縮圖／解碼、佇列 actor 隔離與世代、接線重放、未完成兩項是否阻擋、五變體對稿）。

### 2026-09-12T05:19:51.259Z · Chih-Lin Yeh · `c4b58f7d`

## merge-review R1 — LS-166 PR #356 head `e845acf`

**裁決：REQUEST_CHANGES**（blocker 1〔機械 gate〕／major 2／minor 6／informational 6）

### 審查範圍與方法

`git diff origin/development...e845acf`（27 檔、+2014／−28）。設計對稿一律離線讀 worktree 內 `design/littlesprout.pen` JSON（`pVSXP`／`ZXdu2`／`rFiLJ`／Notes `kHDk4`），未碰 Pen。實跑用專屬機 `LS-166-iPhone17Pro`／`LS-166-iPadPro13`，收工已 shutdown。**Supabase lock 持有時長：未持有**（本輪未跑 DB 測試，也未重放 E2E 上傳，理由見「我沒審／沒跑的範圍」）。

### 已驗證（怎麼驗的）

- **單元測試實跑**：`xcodebuild test -only-testing:LittleSproutTests/AlbumDetailStoreTests -only-testing:LittleSproutTests/AlbumPhotoGridLayoutTests -only-testing:LittleSproutTests/UploadQueueStoreTests`（destination＝LS-166-iPhone17Pro）→ 31/31 綠，`** TEST SUCCEEDED **`。
- **iPad 欄數落差（handoff「未完成」第 2 項）自己補跑**：`xcodebuild build` → `xcrun simctl install` → `SIMCTL_CHILD_LS_TAP_TARGET_GATE_SCREEN=AlbumDetailViewPopulated xcrun simctl launch` 於 LS-166-iPadPro13，截圖存 `scratchpad/LS-166-ipad-verify.png`，PIL 掃描列量到照片牆橫向 span＝40.0pt→991.5pt（952pt＝1032−2×40 `screenPadLarge`）、colW 178pt、gap 16pt。952pt 正是「pane＝全螢幕寬、無 Nav Sidebar」的可用寬，5 欄是公式對這個寬度的正確輸出 → **handoff 的解釋成立**（不是 `contentWidth` 量測鎖死，`.background(GeometryReader)` 確實從初值 984 收斂到 952）。
- **亮／深色逐位元同幾何**：`.claude/evidence/LS-166/r1/album-detail-light.png` 與 `album-detail-dark.png` 同一條掃描列的欄邊界皆為 24.0–192.7pt／209.0–377.7pt（gap 16.3、角托外掛 5pt、content 354＝402−2×24），深色底色 (26,14,18) 確為深色態。
- **尺寸公式對 Notes**：`AlbumPhotoGridLayoutTests` 11 個斷值與我自己用 python 解 `design/littlesprout.pen` 取到的 `pVSXP` `S5Vk8a` 節點逐項一致（padding [8,8,32,8]、colW 164.5、Photo Wrap 111.5、角托 TR x＝143.5＝164.5−26+5、BL y＝130.5＝cellH−26+5），Notes `vfPjM`／`k3jJ5j` 的 148.5／198 亦相符；`AlbumPhotoGridLayout.swift` 的 mount pool 四角不透明度 0.393／0.259／0.293／0.197 與稿面 fill 陣列逐值相同。
- **E2E 證據查實**：`.claude/evidence/LS-166/r1/e2e-upload-completed-grid.png` 確為 app 真實畫面（標題「LS166 E2E Album」、「3 張相片」、三格真實比例混排），支持 handoff「三張走到已完成＋牆有新格」的申報。
- **LS-96 入池查實**：`98a571c6` 已在 LS-96 comment 串內，內容為 `DeleteAccountFlowModelTests` 平行執行 flake，與本票無關。
- **PR body**：`bash scripts/gates/pr-body-check.sh <PR body> --branch feature/LS-166-album-detail --verify` 綠（檔頭段含 LS-166、第 48 行 LS-96 comment `98a571c6` 存在、申報反查 1 條）；`Design:` 行七個板名齊全。

### Blocker（機械 gate，非程式碼缺陷）

**B1 — PR 與 `development` 衝突，CI 從未觸發。**
`git merge-tree --write-tree origin/development HEAD` → `CONFLICT (content): LittleSprout.xcodeproj/project.pbxproj`（development 已前進到 `268f546`，含 LS-217）。GitHub 因此建不出 merge ref，`gh api repos/CLYEH/little-sprout/actions/runs?branch=feature/LS-166-album-detail` 回 `total_count: 0`、`gh pr checks 356` 回 no checks——四個 required checks 一個都不存在，這支 PR 目前物理上併不進去。
處置：先 merge／rebase `development`、`xcodegen generate` 重產 pbxproj 再 push（head 會變 → 需重審重貼 `merge-review` status）。

### Major

**M1 — `LittleSprout/Services/Albums/SupabaseAlbumsAPIClient.swift:201-210`（`updateAlbumTitle`）＋`LittleSprout/Services/Albums/AlbumDetailStore.swift:129-140`（`submitEdit`）：RLS 打不中的改名會「靜默 0 列」，UI 卻回報成功。**
`.update(UpdateAlbumTitlePayload).eq("id", …).execute()` 沒有 `.select()`／受影響列數檢查。`docs/API.md` §2 對 `albums` 這條路徑有逐字規定：「owner 越權 `.update()` 內容欄位（`title`／`cover_media_id`）不會回 `42501`，而是靜默影響 0 列……**呼叫端必須自己檢查回傳的受影響列數／`return=representation` 的內容判斷「這次 `.update()` 到底改到了沒有」，不能假設「沒有丟出錯誤＝改到了」**」；`supabase/migrations/20260825010000_albums_comments_owner_scope.sql` 的 policy 說明段也實測過 `GET DIAGNOSTICS row_count` 是 0、無 exception。
失敗情境：家庭 owner（不是這本相簿的建立者——「更多」選單對 owner 一律顯示，見 `AlbumDetailView+Actions.swift:19-21`）改名 → `submitEdit` 不 throw → `title = newTitle`（`AlbumDetailStore.swift:134`）→ `editState = .success` → sheet 關閉、詳情頁顯示新名字，但 DB 完全沒改；重新進入或其他家人看到的仍是舊名。
連帶：`AlbumDetailView.swift` 檔頭、`AlbumsAPIClient.swift`、`AlbumDetailStore.submitEdit`、`EditAlbumView.swift:170-175` 四處註解都寫「owner 若不是建立者送出會收到 `42501`／`LS045`，錯誤文案會呈現，不會靜默失敗（Rule 11 fail loud）」——這個前提對 `set_album_children` RPC 成立，對 `albums.title` 直接 UPDATE **不成立**，註解需一併訂正。
建議修法：`.update(…, returning: .representation).select("id")` 或 `count: .exact`，0 列時丟出對應 `AppError`（沿用 LS045 文案鍵）；或把「更多」可見性改成 `isOwner && isCreator`（需 `AlbumSummary` 帶 `created_by`）讓 UI 從源頭不給非建立者這個入口。

**M2 — `LittleSprout/Features/Albums/AlbumDetailView+Actions.swift:116-123` ＋ `LittleSprout/Features/Albums/AlbumDetailView.swift:38-39`：上傳中離開詳情頁，完成的照片永遠掛不進相簿，使用者完全不知情。**
`onUploadSucceeded: { [weak detailStore] _, mediaID in Task { await detailStore?.attachUploadedMedia(mediaID) } }`；`detailStore`／`uploadQueueStore` 都是 `AlbumDetailView` 的 `@State`。pop 掉詳情頁後 `@State` 釋放 → `AlbumDetailStore` 沒有任何其他強參照（`AlbumsStore` 不持有它）→ deinit；但 `UploadQueueStore.start(_:)` 的 `Task { [weak self] in guard let self else { return } … }`（`UploadQueueStore.swift:208-217`）在進入後強持有 store，上傳**會繼續跑完**。於是每一張在離開後才完成的照片：`media` 列建立成功、`album_media` 從來沒被寫入、照片牆與相簿列表永遠看不到它，而且沒有任何失敗提示。依 `supabase/migrations/20260906050606_soft_delete_unreferenced_media.sql`（LS-212）這些列最後會被當孤兒軟刪。
失敗情境（票文範圍 2 明列「背景續傳」，佇列 sheet 的 footer 文案就是「在背景繼續，關閉視窗」`UploadQueueSheetView.swift:218`，等於明示使用者可以離開）：加入照片選 3 張 → 佇列出現「上傳中」→ 關閉 sheet → 按「相簿」返回 → 上傳完成 → 回到該相簿仍是 0 張。
證據等級：**未實跑重現**（需要在上傳飛行中返回，本輪未持 lock 做互動 E2E），但機制由程式碼形狀直接決定（weak 捕獲＋`@State` 生命週期），不是時序巧合。
建議修法：把 queue／attach 的責任提到比畫面長壽的層（例如 `AlbumsStore` 持有 per-album 的 `UploadQueueStore`＋attach 由它負責），或讓掛鉤改捕獲「相簿 id＋apiClient」這種不隨畫面消失的值（attach 本來就只需要 `albumID`／`familyID`／`apiClient`），照片牆的即時插入則仍靠 weak store。

### Minor

**m1 — `AlbumDetailView+Actions.swift:87-112`：沒有「載入中」旗標，兩批挑選交錯時會把整批佇列狀態蓋掉。**
`加入照片` 按鈕在 `loadPicked` 期間不停用，使用者可以立刻再開一次 picker。兩批的 `PickedItemLoader.load` 若非按開始順序完成（例如第一批含長影片、第二批是小圖），後完成的那批會執行 `let queue = uploadQueueStore ?? makeUploadQueueStore(...)`（`:107`），把先前那顆 store 換掉 → 先建立的那批列（含失敗列與重試鈕）從 sheet 消失、實際併發變成 2×`maxConcurrentUploads`。日記路徑早就因同一類 review 意見加了 `DiaryComposerStore.isLoadingPickedItems`（`LittleSprout/Services/Diary/DiaryComposerStore.swift:46-52`，merge-review R1 M3／m6），本票沒沿用。

**m2 — `AlbumDetailView+Actions.swift:91-93`：不支援格式與載入失敗都靜默丟掉。**
`case .unsupportedFormat: continue` 與 `guard let loaded = … else { continue }` 都沒有任何回饋，使用者只會發現「我選了 5 張，佇列只有 3 張」。日記路徑有 `DiaryComposerStore.unsupportedFormatSkippedCount` ＋常駐回話列（同一支既有 review 的 m4）。

**m3 — `LittleSprout/Features/Albums/EditAlbumView.swift:180`：`Array(selectedChildIDs)`（`Set` → `Array`）順序不定，破壞 `submitEdit` 的「只打真的改過的那一半」。**
`AlbumDetailStore.submitEdit` 用陣列相等判斷有沒有改（`AlbumDetailStore.swift:136`）；`Set` 轉陣列的順序是 hash 順序，多寶貝時同一組選擇每次的順序可能不同 → 沒改也會多打一次 `set_album_children`，且 `detailStore.childIDs` 會被寫成任意順序。`test_submitEdit_neitherChanged_isNoOp_stillReportsSuccess` 直接呼叫 store、餵同一個陣列，量不到這個落差。建議在 View 層或 store 內用集合比較（或排序後比較）。

**m4 — `AlbumDetailStore.swift:76`／`:73`：`nextSortOrder = links.count` 撞號 ＋ `sorted { $0.sortOrder > $1.sortOrder }` 不穩定排序。**
Swift 的 `sorted` 不保證穩定；`sortOrder` 一旦有重複（跨裝置同時加照片，或連結列被刪過導致 `count` 落在既有值上），同分的照片順序會在每次 `refresh()` 之間跳動。handoff「風險」段只提到撞號，沒提不穩定排序這一半。建議 tie-break 加 `mediaId`。

**m5 — 「更多」僅 owner 可見（`AlbumDetailView+Actions.swift:19-21`）與票文「owner／建立者限定」不一致。**
後端允許建立者本人（即使是 member）改名（`albums_update`）與軟刪（`set_album_deleted` 的作者分支），但 UI 對非 owner 的建立者完全沒有入口；反過來 owner 對別人的相簿看得到入口卻改不動（見 M1）。實作者已在 handoff「未完成」宣告不修並以 Notes `OHMPk`（「僅 owner 可見」）為據——Notes 確實這樣寫（我已離線核對原文），所以這是稿面與票文的衝突，不是實作擅自偏離；但兩邊的組合在 M1 修好之前會產生「看得到、按得下、改不了卻說成功」。

**m6 — `AlbumDetailView.swift:102-108`：`familyStore.myFamily?.id` 或 `albumSeed` 為 `nil` 時永久停在 `ProgressView`。**
`.task(id: albumID)` 的 guard 一旦落空就沒有任何後續（不會重試、沒有錯誤態、沒有返回以外的出口）。正常導覽路徑不會發生（相簿一定在 `albumsStore.albums` 裡），但這是無聲的死畫面而不是 fail loud。

### Informational（逐條編號，請在下一輪 handoff「未完成」列處置）

1. **IN-1 iPad 截圖證據無效**：`.claude/evidence/LS-166/r1/album-detail-ipad.png` 的內容是 iPad 主畫面（springboard、Dock、App icon），**不是 app 畫面**——handoff「iPad 截圖欄數 5 欄」這句在這個檔案上沒有任何證據支撐。我已自己補跑同一支 harness 產生 `scratchpad/LS-166-ipad-verify.png` 並量測（見「已驗證」），結論與 handoff 一致，但證據檔本身要重拍。
2. **IN-2 「34 張壓測不掉幀」沒有量測證據**：只有一張靜態截圖 `album-detail-stress34-top.png`，沒有 frame time／Instruments 數據。`LazyVStack` 逐欄延遲載入的設計方向正確（且 `AsyncImage` 解碼不在主執行緒、列表只載 `thumb_path`，LS-130 口徑正確），但「不掉幀」目前是宣稱不是量測。
3. **IN-3 照片牆無分頁上限**：`fetchAlbumMediaLinks`（`SupabaseAlbumsAPIClient.swift:175`）無 `.limit()`／`.order()`，`fetchMedia(ids:)` 與 `signedURLs` 也是一次全簽。票文把壓測上限定在 34 張，但正式相簿沒有這個上限；建議在後續票加 keyset 分頁（現在不要加，超出本票範圍）。
4. **IN-4 UITest 沒有覆蓋「加入照片 → 佇列 sheet 出現」**：`AlbumDetailViewTests` 自陳 PhotosPicker 是別的行程不 tap，這條票文驗收只由手動 E2E 覆蓋；`loadPicked` → `enqueue` → `showsUploadQueueSheet` 這段組裝邏輯目前零自動化覆蓋（store 兩端各自有測試，中間的接線沒有）。
5. **IN-5 mutation 重放未能執行**：LS-209 要求我自己重放 handoff 申報的 mutation（拿掉 `UploadQueueStore.swift:217` 的 `self.onUploadSucceeded(id, mediaID)`），但 worktree 的檔案修改被權限系統擋下（reviewer 不得改檔），我只做了靜態核對：production 端該掛鉤只有這一個呼叫點（`grep -rn onUploadSucceeded`），測試 `test_onUploadSucceeded_firesWithOriginalIDAndMediaID_onlyOnSuccess` 的 `recorded.count` 只能由它遞增，且 handoff 引用的訊息格式與 XCTest 對 Int 的輸出格式一致。**我沒有實跑轉紅，請 orchestrator 視需要指派可改檔的環境補驗。**
6. **IN-6 模擬器 runtime 與 pin 不符**：`.ios-runtime` 釘 26.2，本機只有 iOS 26.0／26.5，`LS-166-iPhone17Pro`／`LS-166-iPadPro13` 兩台都是 **iOS 26.0**；我的版面量測與實作者的五張截圖都是在 26.0 上取得（LS-205：runtime 差異會影響 tap-target／版面量測，CI 若用別的 runtime 可能與本機不一致）。

### 我沒審／沒跑的範圍（誠實聲明）

- **沒有重放上傳 E2E**：本輪未持 `supabase-lock.sh --hold`、未跑本機容器互動流程（持有時長：未持有）。理由：PR 目前與 development 衝突、CI 從未觸發，head 必然要換，且已有兩個 major 需要改動同一條上傳接線；等 rebase＋修完 M1／M2 的新 head 再做一次完整互動驗證才有意義。E2E 我只做到「證據截圖查實」這一層。
- **斷網重試（票文驗收「斷網一張失敗列可重試」）**：同上未實跑。程式路徑上 `UploadQueueStoreTests.test_failedUpload_networkError_isRetryable_andRetryRestartsIt`（LS-167 既有、本輪跑過為綠）覆蓋了重試邏輯本身，**這條可以放 QA 在 `test` branch 實跑**（票文本來就把它列為 QA 實跑項）；但注意 M2 未修之前，QA 若在重試期間離開畫面會落入同一個坑。
- **深色／AX3／空狀態只做幾何比對**，沒有逐一對稿 token 取色；`w0NxC`（刪除確認）與 `ZXdu2`（空狀態）只讀了 .pen 結構與程式碼，沒有對應截圖可比（本輪證據沒有這兩態的截圖）。
- **沒有跑 UITests／全量 912 單元測試**（只跑本票三個 class），也沒有跑 `tap-target-check.sh`／`ax-launch-check.sh`——這幾支是 CI 的工作，而 CI 因 B1 從未執行。
- **`UploadQueueSheetView` 本體、相簿列表（LS-203）、全螢幕檢視器** 依票文「不做」清單未審；diff 也確實沒有碰（scope 乾淨：`UploadQueueStore` 只加了一個預設空閉包的掛鉤，`AlbumsView`／`RootView` 只多傳一個 `mediaUploadService`，`AlbumsStore.apiClient` 由 private 改 internal 有交代理由，沒有順手重構）。

---

**`bash scripts/gates/handoff-evidence-check.sh` 輸出（貼文前跑，LS-211）**

```
✓ 第 11 行起的列項有證據
✓ 第 12 行起的列項有證據
✓ 第 13 行起的列項有證據
✓ 第 14 行起的列項有證據
✓ 第 15 行起的列項有證據
✓ 第 16 行起的列項有證據
✓ 第 17 行起的列項有證據
```
（exit 0）

### 2026-09-12T05:21:50.556Z · Chih-Lin Yeh · `937b5de6`

**orchestrator 09-12 13:2x**：merge-review R1 `c4b58f7d` **REQUEST_CHANGES**（B1 與 development `268f546` 衝突於 pbxproj、CI 從未觸發；M1 改名直接 `.update()` 靜默 0 列仍回報成功，違 API.md §2；M2 `onUploadSucceeded` 弱持 View `@State` 的 store，pop 後完成的上傳只建 `media` 無 `album_media`；6 minor／6 info；IN-1 iPad 截圖是 springboard；IN-5 reviewer 不得改檔故未做 mutation；IN-6 本機 runtime 26.0 vs 釘 26.2）。裁定 R2：合併 dev（xcodegen 重生 pbxproj）＋M1 改取回列數 0 列即錯＋M2 相簿掛載改由上傳流程長生命週期物件完成、不依賴 View；重拍 iPad；自做 mutation；補「上傳中離開再回來」E2E。已派 ios-dev R2。

### 2026-09-12T06:51:06.548Z · Chih-Lin Yeh · `3db44e5d`

## Ticket

LS-166：Task：LS-20 iOS——相簿詳情（瀑布流照片牆／加入照片入口／寶貝標記／改名與刪除相簿）依 LS-142 稿實作，含深色、AX3、iPad、34 張壓測——**R2**（修 merge-review R1 `c4b58f7d` REQUEST_CHANGES）

## 已完成

1. **B1（blocker）合併 development**（`9fb718d`）：`git merge origin/development`（不 rebase，development 現 `268f546`，含 LS-216／LS-217）＋`xcodegen generate` 重生 `project.pbxproj`，diff 只含 LS-166 新檔的 BuildFile／FileReference 節點，無其他專案結構異動。
2. **M1 修正**（`ce7076d`）：`SupabaseAlbumsAPIClient.updateAlbumTitle`（`LittleSprout/Services/Albums/SupabaseAlbumsAPIClient.swift:201`）原本 `.update()` 沒檢查受影響列數，owner 對別人建立的相簿改標題會靜默 0 列、`submitEdit` 卻回報成功。改走 `requireUpdatedRow`（`SupabaseAlbumsAPIClient.swift:221`，同 `SupabaseFamilyAPIClient` 既有解法：`.update()` 預設 `returning: .representation`，0 列時丟 `AppError.rejected(code: "no_rows_updated")`）。訂正 `AlbumDetailView`／`AlbumsAPIClient`／`AlbumDetailStore`／`EditAlbumView` 四處過期註解（原本聲稱這條路徑「不會靜默失敗」對 `albums.title` 直接 `.update()` 不成立，現在已成立）。
3. **M2 修正**（`ae3d218`）：`onUploadSucceeded` 掛鉤原本 `[weak detailStore]` 直接呼叫 `AlbumDetailStore.attachUploadedMedia` 寫入 `album_media`；`detailStore` 是 `AlbumDetailView` 的 `@State`，使用者在上傳飛行中 pop 掉詳情頁就會 deinit（`UploadQueueStore` 自己靠飛行中 `Task` 的 `guard let self` 撐住不受影響），掛鉤變成 no-op——上傳成功的照片建出 `media` 列卻永遠沒有連結。修法：寫入責任移到 `AlbumsStore.attachUploadedMedia`（`LittleSprout/Services/Albums/AlbumsStore.swift:179`，app 層存活，同 `TimelineStore`／`ChildrenStore` 一級；`sortOrder` 現查現算 `fetchAlbumMediaLinks(albumID:).count`，不再依賴 per-view 記憶體計數器）；`AlbumDetailStore.attachUploadedMedia` 改名 `reflectUploadedMedia`（`AlbumDetailStore.swift:104`，只做 UI-only 反映，不再打網路寫入，移除 `nextSortOrder` 欄位）；`AlbumDetailView+Actions.makeUploadQueueStore`（`AlbumDetailView+Actions.swift:137`）的閉包改成只捕捉 `albumID`／`familyID`（值型別）與 `albumsStore`（長生命週期），`detailStore` 仍弱引用只做即時 UI 更新。
4. **三項 minor 修正**（`b44de02`）：
   - m1：新增 `AlbumDetailView.isLoadingPickedItems`（`AlbumDetailView.swift:48`），`loadPicked` 用 `defer` 包住整段迴圈（`AlbumDetailView+Actions.swift:99-100`），兩顆「加入照片」按鈕讀這顆旗標停用＋顯示「照片載入中…」，擋下兩批 picker 交錯完成互相蓋掉佇列。
   - m3：`AlbumDetailStore.submitEdit`（`AlbumDetailStore.swift:137`）改用集合比較（`Set(newChildIDs) != Set(childIDs)`），修正 `Set<UUID>` 轉 `Array` 順序不定造成的誤判改動。
   - m4：`refresh()` 排序（`AlbumDetailStore.swift:72`）加 `mediaId` tie-break，`sortOrder` 重複時排序結果確定性；撞號本身隨 M2（sortOrder 改現查現算）一併改善。
5. **IN-1 重新拍攝並更新 iPad 截圖**（無程式碼變更）：R1 證據 `album-detail-ipad.png` 內容是 springboard；本輪用 harness（`SIMCTL_CHILD_LS_TAP_TARGET_GATE_SCREEN=AlbumDetailViewPopulated`）在 `LS-166-iPadPro13` 重拍，確認畫面是相簿詳情（「阿公阿嬤家過年」12 張相片，5 欄，harness host 寬度落差同 R1 結論），存 `.claude/evidence/LS-166/r2/album-detail-ipad.png`。
6. **IN-5 自己執行 mutation 重放**（`ce7076d`／`ae3d218`，reviewer 無法改檔）：見「已驗證」。
7. **m2／m6 記入 LS-96 待辦池**（comment `1aa74165-c670-4a72-b565-5db538d10ff7`）。

## 已驗證（怎麼驗的）

- **對應 M1**：新增 `SupabaseAlbumsAPIClientUpdateAlbumTitleTests`（`test_updateAlbumTitle_oneRowAffected_doesNotThrow`／`test_updateAlbumTitle_zeroRowsAffected_throwsRejected`）PASS；既有 `AlbumDetailStoreTests.test_submitEdit_titleUpdateFails_reportsFailure_doesNotCallSetAlbumChildren` 覆蓋 store 端回滾（`title` 不變、`editState` 轉 `failure`）。
  - **Mutation（IN-5）**：拿掉 `requireUpdatedRow` 的受影響列數檢查 → `test_updateAlbumTitle_zeroRowsAffected_throwsRejected` 轉紅，斷言訊息原文：`failed - 非建立者本人（0 列受影響）應該要 throw`。已還原修法、確認回綠（`xcodebuild test -only-testing:LittleSproutTests/SupabaseAlbumsAPIClientTests`）。
- **對應 M2**：新增 `AlbumsStoreTests.test_attachUploadedMedia_writesAlbumMedia_withoutAnyAlbumDetailStoreInstance`（完全不建立任何 `AlbumDetailStore`，直接對 `AlbumsStore` 呼叫，模擬「detailStore 早就不存在」的極端情況）／`test_attachUploadedMedia_computesSortOrderFromCurrentLinkCount`／`test_attachUploadedMedia_attachFails_doesNotThrow`，PASS；`AlbumDetailStoreTests` 的 `attachUploadedMedia` 測試段改寫為 `reflectUploadedMedia`（UI-only 契約，`test_reflectUploadedMedia_prependsToPhotos_whenMediaFetchSucceeds`／`test_reflectUploadedMedia_fetchMediaReturnsEmpty_doesNotThrow_andDoesNotAppendPhoto`／`test_reflectUploadedMedia_alreadyPresent_doesNotDuplicate`），PASS。
  - **Mutation（IN-5）**：拿掉 `AlbumsStore.attachUploadedMedia` 內的 `attachMedia` 呼叫 → `test_attachUploadedMedia_writesAlbumMedia_withoutAnyAlbumDetailStoreInstance` 轉紅，斷言訊息原文：`XCTAssertEqual failed: ("0") is not equal to ("1") - 沒有任何 AlbumDetailStore 存在也該寫入 album_media`；同時 `test_attachUploadedMedia_computesSortOrderFromCurrentLinkCount` 轉紅，斷言訊息原文：`XCTAssertEqual failed: ("nil") is not equal to ("Optional(2)") - 現查現算：已有 2 筆連結，新的一筆接續在後`。已還原修法、確認回綠（`xcodebuild test -only-testing:LittleSproutTests/AlbumsStoreTests`）。
- **對應 m3**：`AlbumDetailStoreTests.swift` 新增 `test_submitEdit_childIDsSameSetDifferentOrder_isNoOp_doesNotCallSetAlbumChildren`（同一組寶貝、陣列順序相反 → 不該多打一次 `set_album_children`），PASS。
- **對應 m4**：`AlbumDetailStoreTests.swift` 新增 `test_refresh_duplicateSortOrder_tieBreaksByMediaIdDeterministically`（兩筆 `sortOrder` 相同時，排序結果依 `mediaId` 字串序確定），PASS。
- **全量回歸**：`LittleSproutTests` 全 target 953/953 綠（`xcodebuild test -only-testing:LittleSproutTests`，本機獨立跑，含 LS-216 `TimelineStoreReactionTests`／LS-217 Push 測試）；`LittleSproutUITests/AlbumDetailViewTests`（5）／`AlbumDetailAX3UITests`（1）／`TapTargetGateTests`（38，含 Albums 三支：`testAlbumDetailViewOwner`／`testAlbumDetailViewMember`／`testEditAlbumView`）共 44 個測試全綠（乾淨模擬器重啟後單獨跑一次確認無回歸——中途發現一支我自己遺留的背景 `xcodebuild` 進程佔用同一台模擬器導致一支測試 crash，已 kill 掉重跑，非本票程式碼問題）。
- **Gate**：`swiftlint lint --quiet` 0 warning／0 error；`tap-target-registry-check.sh`／`api-contract-check.sh`／`error-codes-check.sh` 皆通過；push gate（unit tests＋lint＋registry＋tap-target-check，含全新 clean DerivedData 重新編譯）通過後 push（`git push` 帶 `push-gate` hook，`simulator: LS-166-iPhone17Pro 585B009E-05D0-4F55-B46F-817A75CA2198 iOS 26.0（pinned 26.2）`，detect-simulator 已印警告不自動重建，見「未完成」IN-6）。
- **模擬器對本機容器互動驗證（E2E，`supabase-lock.sh --hold "LS-166 R2 dev E2E"`，持有 17 分 45 秒）**：`SIMCTL_CHILD_LS_QA_API_URL`／`SIMCTL_CHILD_LS_QA_ANON_KEY` 指向本機 Supabase 容器（`SupabaseClientFactory.qaOverride`）→ 真實 Email OTP 登入（Mailpit 取碼，帳號 `ls166-r2-e2e@ls.test`）→ 建立家庭 → 相簿 tab 建立相簿「LS166 R2 E2E Album」→ 進入詳情（`0 張相片`）→「加入照片」PhotosPicker 多選 3 張（`simctl addmedia` 種入）→ 佇列 sheet 三列皆走到「已完成」，`還有 0 張還沒完成` → 關閉視窗 → 照片牆即時出現 3 張新格（`3 張相片`，`.claude/evidence/LS-166/r2/e2e-upload-completed-grid.png`）。追加驗證：再加入 1 張（video）＋1 張刻意放大到 6000×6000px 的合成大圖（想製造較長解碼／上傳時間、嘗試在上傳飛行中提前導覽離開再回來重現 M2 情境）——本機對 localhost 容器上傳速度極快（次秒級），兩次嘗試在我能發出「返回」點擊之前上傳已完成，未能人工重現「離開時仍在飛行中」這個精確時序；改為對照 DB 直接查證：`select count(*) from album_media where album_id = (select id from albums where title = 'LS166 R2 E2E Album')` → `5`，與畫面上 5 張相片一致，`.claude/evidence/LS-166/r2/e2e-grid-5-photos.png`／`e2e-upload-queue-completed-list.png`。**M2 的權威驗證是上面的單元測試＋mutation**（`test_attachUploadedMedia_writesAlbumMedia_withoutAnyAlbumDetailStoreInstance` 直接斷言「沒有任何 `AlbumDetailStore` 存在」這個根本情境，比人工計時競態更嚴謹、不依賴時序運氣）。

## 未完成

- **m2**（不支援格式／載入失敗靜默丟掉，無回饋）：記入待辦池 `LS-96` comment `1aa74165-c670-4a72-b565-5db538d10ff7`——`DiaryComposerStore` 已有 `unsupportedFormatSkippedCount`＋常駐回話列的既有解法可沿用，本輪聚焦 B1／M1／M2 未跟進，估 size S。
- **m5**（「更多」可見性採 Notes 僅 owner，與票文字面「owner／建立者限定」不一致）：不修＋理由——reviewer 已核對 Notes `OHMPk` 原文確為「僅 owner 可見」，稿面與票文的既有衝突，非實作擅自偏離；M1 修好後這個組合已從「看得到、按得下、改不了卻說成功」變成「看得到、按得下、改不了會收到明確錯誤」，殘留的可見性精確度問題需另開票調整設計稿。
- **m6**（`familyStore.myFamily?.id`／`albumSeed` 為 nil 時永久停在 `ProgressView`）：記入待辦池 `LS-96` comment `1aa74165-c670-4a72-b565-5db538d10ff7`——正常導覽路徑不會觸發（相簿一定在 `albumsStore.albums` 裡），屬防禦性硬化缺口，估 size S。
- **IN-2**（34 張壓測沒有 frame time 量測）：不修＋理由——量測基礎設施本票範圍內不存在，非本輪相關，維持 R1 既有結論，留給後續票。
- **IN-3**（照片牆無分頁上限）：不修＋理由——票文壓測上限 34 張已足夠，keyset 分頁超出本票範圍，留給後續票。
- **IN-4**（UITest 沒有覆蓋「加入照片 → 佇列 sheet 出現」）：不修＋理由——`PhotosPicker` 是別的行程，UITest 無法直接操作選圖，同 R1 既有結論；本輪已在手動 E2E 補驗這段組裝邏輯。
- **IN-6**（模擬器 runtime 26.0 vs pin 26.2）：不修＋理由——本機仍只有 iOS 26.0／26.5，`.ios-runtime` 釘 26.2 本機裝不了；本輪版面量測與 R1 一致皆在 26.0 上取得，push gate 本輪也印出同一則警告，CI 若用 pinned runtime 可能有落差，已記錄同 R1（LS-205）。
- **票文驗收「斷網一張失敗可重試」**：本輪未真人手動重現（R1 已述理由，本輪聚焦 B1／M1／M2／IN-1／mutation，未再花額外 lock 時間重試）；邏輯正確性由既有 `UploadQueueStoreTests.test_failedUpload_networkError_isRetryable_andRetryRestartsIt`（PASS）覆蓋，留給 QA 在 `test` branch 實跑（票文驗收條件本來就要求 QA 這段）。

## 風險

- 沿用 R1：`AlbumDetailScreenshotAPIClient` 的 `signedURL` 恆為 `nil`，截圖只驗證版面幾何；真實圖片載入已由 E2E 覆蓋。
- `AlbumsStore.attachUploadedMedia`／`AlbumDetailStore.reflectUploadedMedia` 皆 best-effort：真的失敗時使用者不會被明確告知「這張沒有掛進相簿」，取捨理由同 R1（避免「明明說完成了又說失敗」的矛盾體驗）。
- `sortOrder` 現查現算（`fetchAlbumMediaLinks(albumID:).count`）仍是「查詢當下的計數」，多裝置／多次瀏覽同時對同一相簿加照片仍可能撞號——比 R1 的 per-view 記憶體計數器更即時，但沒有 DB 端唯一約束兜底，純粹影響顯示順序美觀度、不影響資料正確性（`refresh()` 已加 tie-break，撞號時排序至少是確定的）。

## 產出位置

- PR：https://github.com/CLYEH/little-sprout/pull/356（`feature/LS-166-album-detail` → `development`），body 已更新含 R2 處置表
- Head SHA：`b44de02`
- Commits（R2）：`9fb718d`（B1 合併＋xcodegen）／`ce7076d`（M1）／`ae3d218`（M2）／`b44de02`（m1／m3／m4）
- 截圖：`.claude/evidence/LS-166/r2/`（`album-detail-ipad.png`／`e2e-upload-completed-grid.png`／`e2e-upload-queue-completed-list.png`／`e2e-grid-5-photos.png`）；R1 截圖仍在 `.claude/evidence/LS-166/r1/`
- E2E 持有時長：「LS-166 R2 dev E2E」17 分 45 秒
- LS-96 comment：`1aa74165-c670-4a72-b565-5db538d10ff7`（m2／m6）
- 模擬器已關：`585B009E-05D0-4F55-B46F-817A75CA2198`（LS-166-iPhone17Pro）、`403B3590-73EE-4922-9649-60DF742F5FD6`（LS-166-iPadPro13）

### 2026-09-12T06:54:59.108Z · Chih-Lin Yeh · `e67204d3`

**orchestrator 09-12 14:4x**：R2 handoff `3db44e5d`——head `b44de02`：B1 `9fb718d`（merge dev 268f546＋xcodegen）、M1 `ce7076d`（`requireUpdatedRow` 0 列丟 `no_rows_updated`）、M2 `ae3d218`（`album_media` 寫入搬到 app 生命週期 `AlbumsStore.attachUploadedMedia`；detail store 只反映 UI）、三 minor `b44de02`；IN-1 iPad 重拍；兩支 mutation 自做；m2／m6 入池 `1aa74165`；push gate 953/953＋Albums UITests 44/44；E2E lock 17m45s（`album_media`=5 對牆 5 格）；「上傳中離開」本機重現不了、以單元＋mutation 為權威證據。agent 自報殺掉一個早前自動背景化殘留的 `xcodebuild`（timeout 截斷背景化——LS-215 hook 盲區）。已派 merge-reviewer R2 delta（含 development 已前進到 `94b4a54` 的 mergeState 檢查）。

### 2026-09-12T07:26:21.559Z · Chih-Lin Yeh · `44549ba9`

## merge-review R2（delta 審）— LS-166 PR #356 head `b44de02`

**裁決：APPROVE**（blocker 0／major 0／minor 3／informational 7）

### 審查範圍與方法

- delta：`git diff e845acf b44de02`；其中 LS-166 自身的修正是 `git diff 9fb718d b44de02`（12 檔、+315／−87），`9fb718d` 是併入 `origin/development` `268f546` 的 merge commit。
- scope 對帳：`git diff --name-only 268f546 b44de02` 只有 Albums 相關 production 檔＋對應測試＋`TapTargetGateHarness+Albums.swift`＋`project.pbxproj`＋`scripts/gates/tap-target-exemptions.txt`，無順手重構、無他票檔案漂移；`RootView.swift` 只多傳一個 `mediaUploadService` 參數（4 行）。
- 不改 worktree：兩組 mutation 重放一律在 `git archive b44de02` 產生的 scratch 快照（`scratchpad/LS-166-xcodegen`）上做，worktree 全程 clean。
- **Supabase lock 持有時長：未持有**（本輪沒有跑 DB 測試、也沒有重放本機容器 E2E，理由見「我沒審／沒跑的範圍」）。
- 模擬器：只用專屬機 `LS-166-iPhone17Pro`（585B009E-05D0-4F55-B46F-817A75CA2198），runtime **iOS 26.0**（`.ios-runtime` 釘 26.2，IN-6 落差持續，LS-205）；收工已確認 Shutdown（兩台皆 Shutdown）。

### 逐條查實（R1 `c4b58f7d` 全項處置）

1. **B1（blocker，已解）**——把 head 樹 `git archive` 到 `scratchpad/LS-166-xcodegen` 後跑 `xcodegen generate`，重生的 `project.pbxproj` 與 commit 內的檔案 **byte-identical**（`diff -q` 無輸出）；pbxproj 相對 `268f546` 的 delta 只有 11 支 LS-166 新檔各自的 BuildFile／FileReference／group／Sources 節點，無刪除、無其他專案設定漂移。`gh pr view 356` 回 `mergeable: MERGEABLE`、`mergeStateStatus: BLOCKED`（BLOCKED 是 required checks 未跑完，不是衝突）。CI 本輪終於觸發，見第 9 項。
2. **M1（major，已解）**——`LittleSprout/Services/Albums/SupabaseAlbumsAPIClient.swift:201-225` 改走 `requireUpdatedRow`，與 `docs/API.md` §2「靜默 0 列」例外的逐字要求（呼叫端必須自己檢查受影響列數／`return=representation` 的內容，不能假設沒丟錯誤就是改到了）一致，寫法與既有 `LittleSprout/Services/Family/SupabaseFamilyAPIClient.swift:115-120` 同一個 pattern（同一組 `AppError.rejected(code: no_rows_updated)`）。呼叫端行為：`AlbumDetailStore.swift:128-131` 先 `try await updateAlbumTitle` 成功才 `title = newTitle`，失敗落到 `:144` `editState = .failure`，本地 title 不變、sheet 不 dismiss（`EditAlbumView.swift:180-185` 只有 `succeeded` 才 `dismiss()`）。單元覆蓋 0 列／1 列兩案：`SupabaseAlbumsAPIClientUpdateAlbumTitleTests`。
3. **M2（major，已解）**——寫入路徑改由 `LittleSprout/Services/Albums/AlbumsStore.swift:179-188` 負責。持有鏈我自己追過：`AlbumsStore` 建立於 `LittleSprout/LittleSproutApp.swift:73` 的 App 層 `@State`（app 生命週期，同 `TimelineStore`／`ChildrenStore` 一級）；`AlbumDetailView+Actions.swift:137-150` 的閉包只捕獲 `albumID`／`familyID`（值型別）與局部 `let albumsStore`（強捕獲長生命週期物件），`detailStore` 僅 weak 且只用於 `reflectUploadedMedia`（UI-only）；`UploadQueueStore.swift:208-217` 飛行中的 `Task { [weak self] in guard let self …}` 在進入後強持有 store，因此掛鉤閉包與其捕獲的 `albumsStore` 在使用者 pop 掉詳情頁後依然存活 → 寫入不再依賴 View。結構性防回歸：`AlbumDetailStore` 已無任何寫入方法（`reflectUploadedMedia` 只動 `photos`），且 `LittleSproutTests/AlbumDetailStoreTests.swift` 新增 `XCTAssertTrue(apiClient.attachMediaCalls.isEmpty)` 斷言擋「UI 端又自己寫一次」。去重：`AlbumDetailStore.swift:108` 的 `photos.contains` 與插入之間沒有 await，MainActor 上是原子的，`refresh()` 與 reflect 交錯不會插出重複格。
4. **三項 minor（已解）**——m1：`AlbumDetailView.swift:48` 新增 `isLoadingPickedItems`，`AlbumDetailView+Actions.swift:99-100` 以 `defer` 包住整段迴圈，`addPhotosBarButton` 走 `PrimaryButton(isLoading:)`（`LittleSprout/DesignSystem/AuthButtons.swift:30` 本來就 `.disabled(isLoading)`）、iPad 行內版 `AlbumDetailView+Actions.swift:86` 另外明確 `.disabled`。m3：`AlbumDetailStore.swift:137` 改集合比較；附帶查實顯示端不吃陣列順序（`EditAlbumView.swift:129-133`／`AlbumDetailView.swift:214` 都是 filter `childrenStore` 的既有順序），所以這個修法是足夠的。m4：`AlbumDetailStore.swift:72-76` 加 `mediaId` tie-break，排序確定性成立。
5. **其餘 minor 處置查實**——m2／m6 確實入池：Linear LS-96 comment `1aa74165` 兩條都在、描述與 R1 原文相符（對應 `AlbumDetailView+Actions.swift:103-106` 的靜默 continue 與 `AlbumDetailView.swift:110-117` 的 guard 死畫面）。m5 不修＋理由沿用 R1 已核對過的 Notes `OHMPk`，我同意（稿面與票文的既有衝突，M1 修好後最壞情況已從「說成功其實沒改」變成「明確報錯」）。
6. **IN-1（已解）**——我自己打開 `.claude/evidence/LS-166/r2/album-detail-ipad.png`：畫面是相簿詳情 app 畫面（返回鍵「相簿」／「更多」／標題「阿公阿嬤家過年」／「12 張相片」／「加入照片」鈕／5 欄沖印品瀑布流），不再是 springboard。另開 `.claude/evidence/LS-166/r2/e2e-grid-5-photos.png` 亦為 app 真實畫面（「LS166 R2 E2E Album」「5 張相片」、5 格含一格影片黑底縮圖），與 handoff 申報的 `album_media`=5 一致。
7. **IN-5 mutation 我自己重放（LS-209）**——(1) 拿掉 `requireUpdatedRow` 的 0 列檢查（改成 `_ = response.value`）→ `xcodebuild test -only-testing:LittleSproutTests/SupabaseAlbumsAPIClientTests` 16 個測試 1 紅，訊息原文 `failed - 非建立者本人（0 列受影響）應該要 throw`，與 handoff 申報逐字相同；(2) 拿掉 `AlbumsStore.attachUploadedMedia` 內的 `attachMedia` 呼叫 → `xcodebuild test -only-testing:LittleSproutTests/AlbumsStoreTests` 17 個測試 2 支紅，訊息原文 `XCTAssertEqual failed: ("0") is not equal to ("1") - 沒有任何 AlbumDetailStore 存在也該寫入 album_media` 與 `XCTAssertEqual failed: ("nil") is not equal to ("Optional(2)") - 現查現算：已有 2 筆連結，新的一筆接續在後`，同樣逐字相同。兩組都是真的斷言失敗（不是 build fail／app crash），還原後回綠。基線：同一份快照跑 `SupabaseAlbumsAPIClientTests`／`AlbumsStoreTests`／`AlbumDetailStoreTests` 46/46 綠。
8. **押後檢查：乾淨環境**——開跑前 `pgrep -f xcodebuild` 空（無殘留行程），在專屬機獨立重跑 Albums 的 UI 測試：`xcodebuild test -only-testing:LittleSproutUITests/AlbumDetailViewTests -only-testing:LittleSproutUITests/AlbumDetailAX3UITests` 加 `TapTargetGateTests` 三支 Albums 案（`testAlbumDetailViewOwner`／`testAlbumDetailViewMember`／`testEditAlbumView`）共 **9/9 綠**，`** TEST SUCCEEDED **`。這是 handoff「Albums UI 測試 44／44」中與本票直接相關那一段的獨立複驗（其餘 35 支 tap-target 案我沒重跑，見「我沒審／沒跑的範圍」）。
9. **CI（R1 因衝突從未跑，本輪首次執行）**——`gh run view 34679056222`：五個 check 全綠。`ci` 綠（35m38s，log 內 `Executed 953 tests, with 0 failures (0 unexpected)`＋`** TEST SUCCEEDED **`，獨立佐證 handoff 的 953/953）；`ci-ipad` 綠（8m19s，iPad 回歸含 TapTargetGateTests 的 Regular 案）；`db`／`lint`／`rules` 皆綠（rules 含 pr-body-check／api-contract-check／error-codes-check／tap-target-registry-check 等 gate 自測）。沒有觸發 LS-229 已知 flake。

### Findings（本輪 delta 新增，皆 minor，不阻擋）

**minor-1 — `LittleSprout/Services/Albums/AlbumsStore.swift:179-188`：`sortOrder` 現查現算帶來兩個副作用（效能＋批內順序）。**
每張上傳成功都先 `fetchAlbumMediaLinks(albumID:)`（無 `.limit()`，整本相簿的連結列全抓）再 `attachMedia`；加上 `reflectUploadedMedia` 的 `fetchMedia`＋`signedURLs`，每張照片上傳完成後固定 4 次網路往返。更實際的問題是並發：`UploadQueueStore` 預設 `maxConcurrentUploads = 3`（`UploadQueueStore.swift:69`），同時完成的三筆各自 `await` 讀到同一個 `links.count`，三張會拿到同一個 `sortOrder`。失敗情境：一次選 6 張 → 每批三張 `sortOrder` 相同 → 牆上這批的先後不是使用者挑選／上傳完成的順序，而是 `mediaId` 字串序（m4 的 tie-break 保證了確定性，但不合直覺）。相對 R1 的同步記憶體計數器（同一批必定遞增）這一點是退步；R1 的計數器綁在已被移除的 View store 上、換掉本身是對的。建議：在 `AlbumsStore` 保一顆 per-album 的單調保留值（`max(links.count, lastReserved + 1)`），或改查 `sort_order` 倒序 limit 1，兩者都能同時解掉全量讀與撞號。

**minor-2 — `LittleSprout/Features/Albums/AlbumDetailView+Actions.swift:143-147` ＋ `LittleSprout/Features/Albums/AlbumDetailView.swift:110-117`：上傳飛行中離開再回到同一本相簿，照片牆不會反映後續完成的那幾張。**
pop 之後舊 `AlbumDetailStore` deinit，掛鉤裡的 weak 參照變 nil；使用者立刻再進同一本相簿會建立**新的** `detailStore`（`.task(id: albumID)` 的 `guard detailStore == nil` 對新的 View 實例成立）並 `refresh()` 一次，但那一刻還沒完成的上傳之後完成時，只有 `AlbumsStore.attachUploadedMedia` 會寫 DB，沒有任何東西通知這個新 store → 牆上少格，且沒有第二次 refresh 的觸發點（詳情頁沒有 `.refreshable`），要再離開／再進來一次才看得到。資料是正確的（M2 已保證寫入），純畫面過期；比 R1 的「永久遺失」輕一個量級。建議：`AlbumsStore` 對外暴露一個 app 層的「最近掛上的 (albumID, mediaID)」`@Observable` 值供詳情頁 `onChange` 反映，或詳情頁在 `scenePhase` 回前景／`onAppear` 補一次 refresh。

**minor-3（PLAUSIBLE）— `LittleSprout/Services/Albums/AlbumDetailStore.swift:59-93` 與 `:104-110`：`reflectUploadedMedia` 不參與 `generation` 守門，與 `refresh()` 交錯時剛插入的那張會被蓋掉。**
`refresh()` 在 `:80` 只比對自己的世代，而 reflect 不會 bump `generation`；若 refresh 的讀取發生在 attach 落地之前、回寫 `photos = newPhotos` 發生在 reflect 插入之後，剛插進去的那一格會消失（DB 仍正確，下次進出即回來）。窗口窄：`refresh()` 只有首次 `.task` 與載入失敗態的「重新載入」鈕（`AlbumDetailView.swift:248`）兩個觸發點，我沒有實跑重現，故標 PLAUSIBLE。建議：reflect 前後比對 `generation`，或把待反映的 mediaID 記進 pending 集合讓 refresh 合併。

### Informational（逐條編號，請在下一輪／收尾 handoff 列處置）

1. **IN-1 `development` 已前進到 `94b4a54`（LS-230 UITests＋LS-226 back-merge），本 PR 併的是 `268f546`。** `mergeStateStatus=BLOCKED`／`mergeable=MERGEABLE`——不是衝突。我另外對帳 `git diff --name-only 268f546 94b4a54` 與本票 diff **零檔案交集**（dev 新增的是 design gate 腳本、agent 定義與 `LittleSproutUITests/InteractionRowUITests.swift`），語意衝突風險也沒有；併入前要不要再 merge 一次由 orchestrator 決定（再 merge 就是換 head，需重審重貼 status）。
2. **PR body 前半仍是 R1 版，與 R2 事實矛盾。** 「產出位置」寫 `Head SHA: e845acf`；「風險」段仍描述 `nextSortOrder` 是純記憶體計數器（R2 已移除）；「已驗證」段仍寫 912 測試與 `attachUploadedMedia`（已改名 `reflectUploadedMedia`）。R2 處置表（body 第 65 行起）本身正確，`pr-body-check` 也綠（rules job），但 QA 讀上半段會被誤導。建議併入前請實作者修上半段或在檔頭標「以 R2 處置表為準」。
3. **M2 的 View 層接線本身零自動化覆蓋。** `test_attachUploadedMedia_writesAlbumMedia_withoutAnyAlbumDetailStoreInstance` 證的是「`AlbumsStore` 那支方法會寫」，不是「掛鉤真的呼叫它」；`makeUploadQueueStore` 的捕獲清單目前只有結構性防護（`AlbumDetailStore` 已無寫入方法，要走回頭路得先加回一支）。可補：注入可控延遲的 stub upload service，UITest 在「上傳中」pop 掉詳情頁後斷言寫入（需 harness 支援，不在本票範圍）。
4. **「上傳中離開」的人工重現本輪仍未達成（handoff 自承本機對 localhost 容器次秒級完成）。** 我同意以單元＋mutation 為權威證據：M2 的根因是生命週期（weak 捕獲＋View `@State`）而非時序巧合，而新測試直接斷言「沒有任何 `AlbumDetailStore` 存在也要寫入」正是根因情境，比靠計時運氣的人工重現嚴謹。若要真的實跑需要 3 的 stub 延遲設施。
5. **殘留 `xcodebuild` 行程（harness 缺口）已入池，本輪再次撞到同型。** handoff 自承 push gate 期間殺掉一個早前被工具 timeout 截斷而自動背景化的 `xcodebuild`（污染一個測試結果）；orchestrator 已入池 LS-96 comment `4de3e796`。我這一輪也撞到同型（`gh run watch` 超過 600s 被自動背景化成 `b1j5gba09`）——證實 LS-215 hook 只擋顯式背景化的盲區確實存在，且不限於 `xcodebuild`。我的 9 支 UI 測試是在 `pgrep` 確認無殘留後跑的。
6. **我沒有重跑全量 953 單元測試**（那正是 CI `ci` job 做的事，結論見「逐條查實」第 9 項），也沒有重跑 34 張壓測／深色／AX3 截圖對稿（delta 未觸碰版面檔，R1 已審）。
7. **IN-6 仍在**：本機兩台專屬機都是 iOS 26.0、`.ios-runtime` 釘 26.2；我本輪的 9 支 UI 測試（含 tap-target 量測）也在 26.0 上取得，CI 用 pinned runtime 可能有落差（LS-205）。

### 我沒審／沒跑的範圍（誠實聲明）

- **未持 supabase lock、未跑本機容器互動 E2E**（持有時長：未持有）。理由：本輪 delta 的兩個 major 都是生命週期／回傳值語意問題，權威證據是單元＋mutation（我已自己重放）；handoff 的 `album_media = 5` 我只做到截圖查實（`.claude/evidence/LS-166/r2/e2e-grid-5-photos.png` 為 app 真實畫面、格數與「5 張相片」一致），沒有自己連 DB 覆核那一行 SQL。
- **票文驗收「斷網一張失敗列可重試」本輪同樣沒有實跑**，沿用 R1 結論交給 QA 在 `test` branch 驗（邏輯由既有 `UploadQueueStoreTests` 覆蓋）。
- **只跑了 Albums 相關的 9 支 UI 測試**，`TapTargetGateTests` 其餘 35 支、全量 `LittleSproutUITests` 與 34 張壓測情境沒跑。
- **R1 已審過的設計對稿（`design/littlesprout.pen` 板 `pVSXP`／`ZXdu2`／`rFiLJ`＋Notes `kHDk4`）、瀑布流尺寸公式、深色與 AX3 幾何本輪沒有重審**——delta 沒有動 `AlbumPhotoGridLayout.swift`／`AlbumPhotoGridView.swift`。
- `UploadQueueSheetView` 本體、相簿列表（LS-203）、全螢幕檢視器依票文「不做」清單未審，delta 也確實沒碰。

---

**`bash scripts/gates/handoff-evidence-check.sh <verdict 暫存檔> --repo .claude/worktrees/LS-166` 輸出（貼文前跑，LS-211）**

```
✓ 第 15 行起的列項有證據
✓ 第 16 行起的列項有證據
✓ 第 17 行起的列項有證據
✓ 第 18 行起的列項有證據
✓ 第 19 行起的列項有證據
✓ 第 20 行起的列項有證據
✓ 第 21 行起的列項有證據
✓ 第 22 行起的列項有證據
✓ 第 23 行起的列項有證據
```
（exit 0；`--repo` 必須指向本票 worktree，指主 checkout 會因為 `main` 上還沒有這些測試檔而誤報 8 條找不到）

### 2026-09-12T07:28:21.932Z · Chih-Lin Yeh · `250e7291`

**orchestrator 09-12 15:2x**：merge-review R2 `44549ba9` **APPROVE**（0 major／3 minor／7 info；status success 貼 `b44de02`；pbxproj byte-identical、CI 五 job 綠 953/953、M1／M2 mutation 重放逐字相符、`AlbumsStore` 為 App 層 `@State` 持有鏈屬實、iPad 截圖已是 app 畫面）。處置：IN-1（dev 已到 `94b4a54`，零檔案交集）→ 不再 merge、直接併；IN-2（PR body 前半 R1 版）→ orchestrator 取 live body 在檔頭補訂正段；minor 1–3（sortOrder 現查現算與同批同號、離開再進不刷新、`reflectUploadedMedia` 無 generation 守門）→ 池 LS-96 `4fafaa19`（QA 若命中則升票）；IN-5（`gh run watch` 也被 timeout 背景化）→ 已入池 `4de3e796`。啟動 `LS-166-merge-follow.sh` 併 #356 → dev → promote → QA（三張上傳到已完成＋牆新格、斷網一張可重試、五變體對稿、34 張壓測）。

### 2026-09-12T07:29:30.860Z · Chih-Lin Yeh · `dace0702`

**orchestrator 09-12 15:29**：#356 已併 development **`938e854`**（dev tip status `promote: no content diff（#356 b44de02 R2 44549ba9）`）→ **QA**。晉升 test 暫扣：等 LS-217 QA 過先把 test `94b4a54`（LS-216／217／230）打 v0.22.0 上 main，再 promote 本票＋LS-203 進 test 派 qa（三張上傳到已完成＋牆新格、斷網一張可重試、五變體對稿、34 張壓測；順帶驗池項 `4fafaa19` 三條是否命中）。

### 2026-09-12T11:11:36.782Z · Chih-Lin Yeh · `d3862fb8`

# LS-166 QA 裁決（v0.22.0 批次）

worktree `/Users/clyeh/little-sprout/.claude/worktrees/qa-test`，HEAD `07d82bdfdb7d1721f96d7ba189c19f2e82547653`（＝`origin/test`）。

## 判定：PASS

## 已驗證

- **多選 3 張 → 佇列 sheet 出現並走到「已完成」→ 關閉後照片牆有新格（真後端）**：帳號 `ls166-qa@ls.test` 建立家庭「LS166 QA Familyg」→ 建立相簿「LS166 QA Album」（標記寶貝「小豆」）→ PhotosPicker 多選 3 張（`simctl addmedia` 種入三張不同尺寸比例的圖）→ `UploadQueueSheetView` 三列皆「已完成」（`.claude/evidence/LS-166/qa/upload-queue-completed.png`）→ 關閉後照片牆顯示「3 張相片」三格新卡（`.claude/evidence/LS-166/qa/album-detail-3-photos-grid.png`）→ `bash scripts/ops/supabase-lock.sh -- docker exec supabase_db_little-sprout psql ...` 直接查 `select count(*) from album_media join albums on ...` 得 3、`album_children` 含小豆標記，證實真後端寫入非畫面假象。
- **斷網一張失敗列可重試**：⊘ BLOCKED（方法限制，非 FAIL）——本環境無 Network Link Conditioner／可控延遲 stub 注入工具，`xcrun simctl` 無法針對單一 app 斷網；本機對 localhost 容器上傳為次秒級，人工操作追不上「上傳中中斷」的精確時序（同 R1／R2 handoff 已述限制）。邏輯正確性由既有單元測試 `LittleSproutTests/UploadQueueStoreTests.swift` 的 `test_failedUpload_networkError_isRetryable_andRetryRestartsIt`（R1／R2 皆 PASS）覆蓋。
- **五變體截圖對稿（含 34 張壓測與 AX3）**：`.claude/evidence/LS-166/qa/variants/album-detail-light.png`（亮色，對照設計板 `pVSXP`，白邊四角托沖印品瀑布流幾何相符）／`album-detail-dark.png`（深色，對照 `yfZyT`，同幾何深色底不變黑）／`album-detail-ax3.png`（真 AX3，`-UIPreferredContentSizeCategoryName UICTContentSizeCategoryAccessibilityXL` launch argument 而非 `simctl ui content_size`，Nav Row／Action Bar 標題與「加入照片」按鈕不重疊、可讀可觸達，對照 `OysBA`）／`album-detail-ipad.png`（對照 `rFiLJ`，4 欄——harness host 缺 `SectionSplitView` 導致可用寬度偏寬的既有限制，`AlbumPhotoGridLayoutTests.test_place_iPadContentPaneWidth_uses3ColumnsAt167` 已釘住正式環境 3 欄，本輪非新回歸）／`album-detail-stress34-top.png`＋`album-detail-stress34-bottom.png`＋`album-detail-stress34-end.png`（對照 `FPaFl`，34 張全滿捲動流暢無 crash；未做 frame time 量測，同 R1 handoff 已揭露的量測缺口，非本輪新增）。
- **改名（0 列時不假成功——用另一帳號改非自己的相簿應被拒且 UI 顯示錯誤）**：新增 member 帳號 `ls166-qa-b@ls.test`（以 `NPNSEY` 邀請碼申請、`approve_join` RPC 核准加入），B 建立「B Owned Album」；owner A（非該相簿建立者）點「更多」→「編輯相簿名稱」改標題為「B Owned Album Hacked By A」→ 儲存時 UI 顯示紅字錯誤「無法完成這個操作」、sheet 不關閉（`.claude/evidence/LS-166/qa/rename-non-creator-rejected.png`）；直接查 DB `select title from albums where title like 'B Owned Album%'` 確認標題仍是原文「B Owned Album」，M1 修法（`requireUpdatedRow`）在真實跨帳號情境下有效、未靜默假成功。
- **刪除相簿確認語彙依 `w0NxC`，不承諾 30 天可還原**：對 A 自己的「LS166 QA Album Renamed」點刪除，實際 UI 文案「要刪除「LS166 QA Album Renamed」相簿嗎？」／「這本相簿會從相簿列表移除；裡面的照片不會被刪除，之後還能在時間軸看到。這個動作目前無法在 App 內復原。」（`.claude/evidence/LS-166/qa/delete-album-confirmation-wording.png`），與 `LittleSprout/Features/Albums/AlbumDeleteConfirmationSheet.swift` 註解、LS-152 IN-1 裁決一致（未採用稿面原文「30 天內可還原」）；確認刪除後列表即時移除、DB `deleted_at` 已設、`select count(*) from media join album_media ... where deleted_at is null` 仍為 3（照片未被刪）。
- **寶貝標記 `set_album_children`**：建立相簿時勾選「小豆」，相簿列表卡與詳情頁頭皆即時顯示「小豆 · 11 個月大」（`.claude/evidence/LS-166/qa/new-album-0-photos-with-tag.png` 於 LS-203 段落亦可見同張證據），DB `album_children` 查有該筆連結。
- **池項 `4fafaa19` 三條是否命中**：本輪 3 張同批上傳（`maxConcurrentUploads` 併發）走到「已完成」後排序目視正常，惟相簿已刪除、未回頭重查 `sort_order` 是否撞號；「上傳中離開再進」與「generation 守門」窗口極窄，本輪互動未刻意重現。三條皆非本輪新發現，沿用 merge-review R2（Linear comment `44549ba9`）既有 PLAUSIBLE／已入池判定，不影響本票 PASS（畫面排序目視佐證 `.claude/evidence/LS-166/qa/album-detail-3-photos-grid.png`）。

## 未完成

- 斷網重試（見上，BLOCKED＋理由）；34 張壓測 frame time 量測（沿用 R1 既有揭露缺口，非本輪新增，未修＋理由見上）。

## 風險

低：本輪為真實互動＋DB 直查雙重驗證，未發現新回歸；已知殘留缺口（斷網重試方法限制、frame time 未量測、iPad harness 4 欄）皆為既有已審查、非阻擋項。

## 產出位置

- 截圖：`.claude/evidence/LS-166/qa/`（`upload-queue-completed.png`／`album-detail-3-photos-grid.png`／`rename-non-creator-rejected.png`／`delete-album-confirmation-wording.png`／`album-detail-empty-state.png`）、`.claude/evidence/LS-166/qa/variants/`（五變體）
- 模擬器已關：`53C3CD8A-ED48-404E-B838-FD13B83C14DB`（`LS-166-QA-iPhone17Pro`）、`FC7E7526-23E9-4534-B021-0801AEA96036`（`LS-166-QA-iPadPro11`）
- lock：`LS-166 QA 冒煙`，持有 52 分 13 秒，正常釋放非到期

---

`bash scripts/gates/handoff-evidence-check.sh scratchpad/LS-166-qa.md --repo .claude/worktrees/qa-test`（貼文前跑，LS-211）：

```
✓ 第 9 行起的列項有證據
✓ 第 10 行起的列項有證據
✓ 第 11 行起的列項有證據
✓ 第 12 行起的列項有證據
✓ 第 13 行起的列項有證據
✓ 第 14 行起的列項有證據
✓ 第 15 行起的列項有證據
```
（exit 0）

---

**總結**：LS-217／LS-166／LS-203／LS-230 四票在 `test` tip `07d82bd` 皆判 PASS，qa status 即將貼到本 SHA。

### 2026-09-12T11:16:26.572Z · Chih-Lin Yeh · `525f0607`

**orchestrator 09-12 19:15**：QA PASS（`d3862fb8`，真後端三張上傳→佇列→格線、非建立者改名被拒、五變體截圖；「單張網路失敗後可重試」在本機無 Link Conditioner 為方法限制 BLOCKED，由 `UploadQueueStoreTests.test_failedUpload_networkError_isRetryable_andRetryRestartsIt` 覆蓋）→ `promote.sh test main` ✓ → main `07d82bd`，tag `v0.22.0`。收尾儀式進行中：①dead-code-sweeper 已派（v0.22.0 四票批次）②lesson review 待 ③cleanup：`cleanup-merged.sh --apply LS-166` ✓（QA 兩台模擬器 `LS-166-QA-iPhone17Pro`／`LS-166-QA-iPadPro11` 已刪）。

### 2026-09-12T11:23:16.114Z · Chih-Lin Yeh · `fcd175ae`

## dead-code sweep — LS-166

**dead-code-sweeper 09-12 19:21**

範圍：v0.22.0 批次（main `07d82bd`）。PR #356（`feature/LS-166-album-detail`，merge `938e854`）。`git log --oneline c5f07b9..07d82bd --grep "LS-166"` 共 13 commits（含 R2 修正 `9fb718d`／`ce7076d`／`ae3d218`／`b44de02`）；`gh pr diff 356 --name-only` 取異動檔清單逐一交叉查引用。

### Findings

無發現本 feature 引入的死碼。逐項交叉比對：

- **`AlbumDetailStore`**：`refresh()`／`reflectUploadedMedia(_:)`／`submitEdit(title:childIDs:)`／`resetEditState()`／`seedForPreview(photos:)`／`preview(...)` 皆有生產或測試呼叫端；`resetEditState()` → `EditAlbumView.swift:54 .onAppear`。
- **`AlbumPhotoGridLayout`**：`place`／`photoWidth`／`photoHeight`／`cellHeight` 皆被 `AlbumPhotoGridView.swift`／`AlbumDetailView.swift:278`（空狀態格）／`AlbumPhotoGridLayoutTests.swift` 呼叫。
- **`AlbumDeleteConfirmationSheet`／`EditAlbumView`**：皆掛在 `AlbumDetailView` 的 sheet 呈現鏈上（`albumDeleted` 掛鉤 `AlbumDetailView.swift:83` → `AlbumDetailView+Actions.swift:156`）。
- **`nextSortOrder`（R1 版純記憶體計數器）**：R2 已完全移除（`AlbumDetailStore.reflectUploadedMedia` 改名且不再持有該欄位），全樹 `grep -rn "nextSortOrder"` 0 命中，無孤兒殘留。
- **`scripts/gates/tap-target-exemptions.txt`** 新增的 `AlbumPhotoGridView` 條目：理由「純顯示元件，瀑布流沖印品格無任何按鈕／導覽」與票文「不做」清單（全螢幕檢視器／影片播放）一致，非過期條目。
- **`AlbumDetailScreenshotAPIClient`**（harness 假 client，`TapTargetGateHarness+Albums.swift:209`）：同檔 `:158`／`:182` 兩處建構呼叫（一般填滿／34 張壓測），非孤兒。

### 觀察（既有殘留，非死碼，供 orchestrator 參考，不重複開票）

- merge-review R2（comment `44549ba9`）已將以下功能性缺口記入 LS-96 待辦池（comment `1aa74165`）：**m2**（PhotosPicker 不支援格式／載入失敗靜默丟棄無回饋）、**m6**（`familyStore.myFamily?.id`／`albumSeed` 為 nil 時永久停在 `ProgressView`）。這兩項程式碼路徑會被執行到，只是行為不完善（防禦性硬化缺口），不屬於「無呼叫點的死碼」，本次巡檢範圍不含，僅確認已有追蹤紀錄。
- QA 裁決提到的池項 `4fafaa19`（sortOrder 現查現算的邊界情況／batch 排序）同上，非死碼範疇。

### 搜尋方法與盲區聲明

方法同 LS-217：`gh pr diff --name-only` 取異動檔 → 對每個新增 struct/enum/func 用 `grep -rn` 全樹核對呼叫端 → 對照 Linear 票文與四輪 review comment（R1 `c4b58f7d`、R2 `44549ba9`）找「暫留」字樣。

盲區：`PhotosPicker` 跨行程界線的呼叫鏈無法純靜態驗證（reviewer 已在多輪以模擬器 E2E＋DB 直查驗證涵蓋，本次未重跑）；reflection／字串拼接不在偵測範圍。

### 2026-09-12T11:24:47.564Z · Chih-Lin Yeh · `7974c17d`

**orchestrator 09-12 19:24 收尾儀式 ②lesson review → Done**
- ①死碼：sweeper `fcd175ae` 無死碼（`nextSortOrder` R1 機制已完全移除；m2／m6 靜默失敗路徑已在池 `1aa74165`）。③cleanup：QA 兩台模擬器已刪（前序 comment）。
- **教訓 1**：PR 與 development 的 `project.pbxproj` 衝突讓 CI 根本沒跑（BLOCKED 看起來像「CI 沒回報」）→ R2 用 `xcodegen generate` 重生後合併。已寫入派工固定規則（push 前 `git merge origin/development`，pbxproj 衝突一律 xcodegen 重生）；巡檢 `patrol.sh` 對 `mergeable=CONFLICTING` 的 PR 宜直接印「→ 併 dev 解衝突」而非「CI 沒回報？」——記入池項待補（見 LS-96 本日池項）。
- **教訓 2**：與 LS-203 的語意衝突（`coverMediaId:` 參數）在各自 PR 綠、併 dev 後才由 CI 抓到 → 同 milestone 同時在飛的票，派工單要點名「先 merge dev 再跑一次 build」（已在規則中）。
- **工具缺口**：本機無網路失敗注入手段，「單張失敗→可重試」只能靠單元測試 → 池項 `a8c6456a`（harness P2）。本票 Done（v0.22.0，main `07d82bd`）。

