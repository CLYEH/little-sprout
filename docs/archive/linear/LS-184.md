# LS-184 Harness：lock 持有安全補強——`supabase-lock.sh --hold` 在主 checkout（`git-dir`＝`git-common-dir`）直接拒絕（exit 2）＋`supabase stop`／`start` 納入 H3b＋qa／ios-dev 派工模板固定 `cd <worktree> && --hold` 同鏈（LS-96 `8fcc81c5`／`b4cb1e29`）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-05T00:41:57.603Z |
| 開始 | 2026-09-05T00:41:58.346Z |
| 完成 | 2026-09-05T02:46:49.506Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-184/harnesslock-持有安全補強-supabase-locksh-hold-在主-checkoutgit-dirgit-common |
| 關係 | related→LS-159, related→LS-88, related→LS-180, related→LS-96, related→LS-183, related→LS-179, related→LS-175, related→LS-158, related→LS-144, related←LS-260, related←LS-238, related←LS-215, related←LS-186, related←LS-185 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項 `8fcc81c5`（[LS-179](https://linear.app/little-sprout-app/issue/LS-179/taskls-23-後端-營運防線使用者家庭停權旗標profilessuspended-atfamiliessuspended) QA `ee11e2a8` 過程注記，2026-09-05）：Bash 工具在 `run_in_background`／timeout 背景化之後 ambient cwd 重設回主 checkout，`supabase-lock.sh --hold` 以 cwd 推導 worktree → 持有者被記成 `/Users/clyeh/little-sprout`（今日三次：[LS-175](https://linear.app/little-sprout-app/issue/LS-175/taskls-22-後端-media-新增彙總通知事件批次上傳-50-張一則爸爸新增了-50-張照片after-insert) QA 批次、[LS-179](https://linear.app/little-sprout-app/issue/LS-179/taskls-23-後端-營運防線使用者家庭停權旗標profilessuspended-atfamiliessuspended) QA 兩次）。[LS-183](https://linear.app/little-sprout-app/issue/LS-183/harnesspretooluse-擋繞過-supabase-locksh-的本機容器操作-docker-exec-進-supabase) H3b 上線後這個誤記會直接造成「持有者自己被 hook 擋」（H3b 用 holder worktree 判定放行）。同池 `b4cb1e29`（[LS-183](https://linear.app/little-sprout-app/issue/LS-183/harnesspretooluse-擋繞過-supabase-locksh-的本機容器操作-docker-exec-進-supabase) handoff）：`supabase stop`／`start` 起停容器會打斷持有者卻不在 H3b 範圍。巡檢 08:4x lane:harness 空（[LS-183](https://linear.app/little-sprout-app/issue/LS-183/harnesspretooluse-擋繞過-supabase-locksh-的本機容器操作-docker-exec-進-supabase) 進 QA）→ 依 [LS-144](https://linear.app/little-sprout-app/issue/LS-144/harness巡檢加開票責任機械提醒-lane-在飛-0-且無可派候補時動作清單印-開票並列來源story-拆票ls-96-p1-升票) 升票。

## 範圍

1. `scripts/ops/supabase-lock.sh --hold`：呼叫端 cwd 解析為主 checkout（`git rev-parse --git-dir` 等於 `--git-common-dir`，同 [LS-158](https://linear.app/little-sprout-app/issue/LS-158/harnessqa-端到端驅動不依賴-mobile-mcp-littlesproutuitests-加-qa-e2e) N1 對 `qa-e2e.sh` 的做法）→ exit 2，訊息「請在票 worktree 內 `cd <worktree> && bash scripts/ops/supabase-lock.sh --hold …`」；`--` 包裝模式維持現狀（只包一條命令，持有者記錄仍應為 worktree——同樣拒主 checkout，但 orchestrator 需要時可用 `LS_LOCK_ALLOW_MAIN=1` 明示放行）。自測 `supabase-lock.test.sh` 補主 checkout 負樣本＋放行變數正樣本。
2. `scripts/hooks/pretool_engine.py` H3b 觸發清單加 `supabase stop`／`supabase start`／`supabase db start`（未包裝且非持有者 deny；`supabase status` 仍放行）；`pretool.test.sh` 正負樣本各 2。
3. `.claude/agents/qa.md`／`ios-dev.md`／`merge-reviewer.md` 的 lock 段與 COLLABORATION §4-b 派工模板：`--hold` 一律寫成 `cd <worktree> && bash scripts/ops/supabase-lock.sh --hold …` 同一命令鏈；`agent-tools-check.sh` BODY_RULES 若能機械驗就加。
4. [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `8fcc81c5`／`b4cb1e29` 銷除註記；COLLABORATION §7 lock 列 gate 欄補上。

## 不做

Bash 工具 cwd 重設本身（Claude Code 行為）；每 worktree 獨立容器。

## 驗收

- [ ] 主 checkout 內 `--hold` → exit 2 並印指引；worktree 內正常；`LS_LOCK_ALLOW_MAIN=1` 放行（自測）
- [ ] 未包裝 `supabase stop` → deny；包裝／持有者 → allow（自測）
- [ ] merge-reviewer 通過；hotfix 流程（自 main、PR 回 main、back-merge）

## 留言（11）

### 2026-09-05T00:42:14.567Z · Chih-Lin Yeh · `4badcab5`

**2026-09-05 08:43 orchestrator — 派工**

- lane:harness 空（LS-183 進 QA）→ 依 LS-144／§5-b 升池項 `8fcc81c5`＋`b4cb1e29`；cycle 1；hotfix 流程：worktree `.claude/worktrees/LS-184`，分支 `hotfix/LS-184-lock-hold-safety`（base origin/main `ae6ca24`，含 LS-183 H3b），PR → main，併入後 back-merge。
- 派 general-purpose R1。

### 2026-09-05T01:04:54.501Z · Chih-Lin Yeh · `6af6b24b`

## handoff（LS-184，2026-09-05 09:05）

**Ticket**：LS-184 — Harness：lock 持有安全補強（`--hold` 拒主 checkout＋`supabase stop`／`start` 納入 H3b＋派工模板固定 `cd <worktree> && --hold` 同一命令鏈）。來源 LS-96 池項 `8fcc81c5`（LS-179 QA 過程注記：Bash 工具背景化後 cwd 重設回主 checkout，`--hold` 把持有者記成主 checkout 三次）／`b4cb1e29`（LS-183 handoff 判斷項）。hotfix 自 `main` `ae6ca24` 切、PR 回 `main`；全程只在 worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-184`，主 checkout 未碰（hook／lock 腳本仍是主 checkout 舊版，本票改動併入 main 並 pull 前不生效）。

**已完成**（SHA＋file:line）：
1. `8a91475` `scripts/ops/supabase-lock.sh`：主 checkout 判定與拒絕 :216-227（`git rev-parse --path-format=absolute --git-dir` vs `--git-common-dir`，同 `qa-e2e.sh:74-77` LS-158 N1；不在 repo 內 git-dir 為空、不算；`LS_LOCK_ALLOW_MAIN=1` 才放行、`=0` 不算；放在參數驗證之後、重入判定與取鎖之前——主 checkout 連「已持有」都不回 3），拒絕訊息帶回原 label／`--max-minutes` 組成 `cd <worktree> && bash scripts/ops/supabase-lock.sh --hold "<label>" --max-minutes N` 指引；檔頭環境變數 :17、機制段 :50-56、exit 碼表 :82-83 同步。`scripts/ops/supabase-lock.test.sh` ㉛ :550-589（21 組：`git init` 空 repo 當主 checkout、手搭 `.git` 檔＋`.git/worktrees/wt1/{HEAD,commondir,gitdir}` 當 linked worktree，不 commit——主 checkout 拒＋四段訊息＋不留 lock／`=0` 不算／worktree 過且 holder `worktree=` 是該 worktree／放行變數過且 holder 記主 checkout、同處 `--release` 可／`--` 包裝在主 checkout 照常）；㉒ :387-388 的「本程序直接 `--hold`」加 `LS_LOCK_ALLOW_MAIN=1`（CI 的 cwd 就是主 checkout，該組驗的是 owner 祖先腿）。
2. `f9577ac` `scripts/hooks/pretool_engine.py`：docstring (e) :78-81；常數 `H3B_SUPABASE_LIFECYCLE`／`FB_SUPABASE_LIFECYCLE_RE` :183-187；`check_precise` 旗標 :635、token 比對 :725-726、回傳 :762-763（排在 supa_local 之後）；`check_fallback` :797-798。`scripts/hooks/pretool.sh` 規則表 H3b :31-34。`scripts/hooks/pretool.test.sh` H3b-s :598-622（deny：`supabase stop`／`start -x studio`／`db start`／`bash -c` 包 stop&&start／括號退回／別目錄對照；allow：包裝 qa.md 原句／持有者 worktree／`status -o env`／echo 字面）。
3. `b3b35e1` `.claude/agents/ios-dev.md:15-16`／`qa.md:17`、`:24`／`merge-reviewer.md:8`、`:11`：`--hold` 例句一律 `cd <worktree> && bash scripts/ops/supabase-lock.sh --hold "…" --max-minutes 15`（`cd` 與 `--hold` 同一條命令鏈的理由寫在旁）、H3b 句補 `supabase stop`／`start`／`db start`、「不得從主 checkout `--hold`」補「LS-184 起直接 exit 2」；`docs/COLLABORATION.md` §4-b :135 派工模板兩句、§6 :217 兩處、§7 :248（lock 列 gate 欄＋盲區欄）／:249（agent-tools 列前饋＋gate 欄）／:300（H3 列前饋＋gate 三處＋盲區）；`scripts/gates/agent-tools-check.sh:65-69` 註解、BODY_RULES 三行 :77-79（10→13 條）；`agent-tools-check.test.sh:7`、`:39-41`（HOLD 改 cd 同鏈、新增 BARE_HOLD）、⑤ 計數 13 :103、⑩ 五組 :123-128、mutant 負控 :162-168（54→60 組）。
4. LS-96 銷除回覆：`8fcc81c5` ↳ **`b3c2aa71`**（PR #296＋`8a91475`／`b3b35e1`）；`b4cb1e29` ↳ **`b183721a`**（PR #296＋`f9577ac`／`b3b35e1`）。
5. PR **#296** `hotfix/LS-184-lock-hold-safety` → `main`（head `b3b35e1`），body 過 `pr-body-check.sh`（LS-96 行 1 條帶 comment id、「已修」行 0 條）後建立；body 在最後一次 push 之後、`gh pr create` 之前就位（本票沒有 body 後的再 push）。

**已驗證**（怎麼驗）：
- `bash scripts/ops/supabase-lock.test.sh`：**239 組全綠**（原 218＋㉛ 21），43 秒；另以 cwd＝`git init` 空 repo（CI 主 checkout 形狀）重跑一次同樣 239 綠（`LS-184-lock-suite-mainlike.log`）。
- `bash scripts/hooks/pretool.test.sh`：**195 組全綠**（原 185＋10），9 秒。**mutation**（暫存樹 `LS-184-mut/`，未入 repo）：`LS-184-mutate.py` 拿掉引擎 lifecycle 觸發（精確 `supa_lifecycle` 回傳＋字面 `FB_SUPABASE_LIFECYCLE_RE` 各 1 處）→ 恰 **H3b-s①–⑥ 6 條 deny 變紅**、allow 4 不動；同一暫存樹未突變的基線 191 綠／3 紅，那 3 紅是 I6（暫存樹沒有 `.claude/settings.json`），與突變無關。
- `bash scripts/gates/agent-tools-check.sh`（真 repo）：通過、印「正文必含字樣 13 條」、三份各印「正文含「cd <worktree> && …」」；`agent-tools-check.test.sh` **60 組全綠**（⑩ 五組＋mutant 負控）。
- `python3 -m py_compile`／`bash -n` 通過；三次 push 皆過 push-gate（純 harness，跳過 Swift）。
- 未跑：CI（push 後未等；交 orchestrator）；merge-review 尚未派。未碰 Pen／模擬器／容器／真 lock（自測全用 `SUPABASE_LOCK_DIR` 合成目錄與 mktemp 夾具）。

**未完成／處置**（本票尚無 reviewer informational；以下是我自己的判斷）：
- **不修＋理由（請 reviewer 裁定）**：`--` 包裝模式**未**拒主 checkout——票文範圍 1 括號內寫「同樣拒主 checkout」，但 (a) 命令型 holder 不給任何人 worktree 腿（`supabase-lock.sh:270` `hold_owner_ok` 首行、`pretool_engine.py` `_h3b_holder_worktree` 都只認 `cmd=hold:*`），主 checkout 的 `--` 不會造成「主 checkout 程序直通」；(b) CI db job `.github/workflows/ci.yml:561` `bash supabase/tests/run.sh` 經 `run.sh:23-26` 自包 wrapper 就是在主 checkout 跑，`merge-reviewer.md:8` 也允許在主 checkout 以 `-- <命令>` 跑 DB 測試——同樣拒絕會直接紅 CI、擋在飛 reviewer；(c) 票文驗收只列 `--hold`。代價：主 checkout 的 `--` 其 holder／hold.log 的 worktree 記成主 checkout、回溯看不出票號（已寫進 §7 盲區）。㉛ 有一組釘住此行為；日後要改＝改那組＋`ci.yml` db job 加 `LS_LOCK_ALLOW_MAIN=1`。
- **不修＋理由**：`--release`／`--held` 不擋主 checkout（票文未列；主 checkout 已無法建立 hold，`--release` 只會對 `LS_LOCK_ALLOW_MAIN=1` 建的 hold 生效——㉛ 釘住）。
- **不修＋理由**：H3b stop／start 用 exact token、不辨子命令位置——`supabase migration new start` 這類把 `start` 當名字會被擋（寧嚴，包 wrapper 即可；`supabase --debug stop` 這種旗標插在前的形狀也抓得到）。
- **不修＋理由**：`npx supabase stop` 命令位置是 `npx`、不觸發——與既有 H3／H3b 對 `npx supabase db reset` 的限制同型，非本票新開。
- LS-183 R1-I1（§7 盲區補「`cd` 進持有者 worktree 可借用放行」一句）在池項 `e8fb11af`、非本票範圍，未動。

**風險**：
- **併入後 `--hold` 在主 checkout 立即被拒**：`supabase-lock.sh` 每次呼叫讀檔——merge 一落地、主 checkout pull 後，在飛 qa／ios-dev／merge-reviewer 下一次從主 checkout（含 Bash 工具背景化後 cwd 重設）發的 `--hold` 會 exit 2；訊息直接給 `cd <worktree> && … --hold "<原 label>" --max-minutes N`，重發即可。已持有中的 hold 不受影響（`--release`／hold 內 `-- <命令>`／`--held`／`--status` 都不擋）；`qa-e2e.sh` 自身已擋主 checkout。
- **H3b stop／start 併入即生效**：裸 `supabase stop`／`start`／`db start` 被 deny、訊息指向包 wrapper 或 `--hold`；qa.md 重建容器句本來就包 wrapper。
- hotfix 流程：併入 `main` 後請補 back-merge `main`→`development`；主 checkout 要 `git pull` 後 hook／lock 腳本才是新版。

**產出位置**：PR #296 https://github.com/CLYEH/little-sprout/pull/296（head `b3b35e14492c25f4043c5b8634b33693f0a03306`；commits `8a91475`／`f9577ac`／`b3b35e1`）；worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-184`（clean）；LS-96 銷除回覆 `b3c2aa71`／`b183721a`；scratchpad：`LS-184-pr-body.md`／`LS-184-handoff.md`／`LS-184-lock-suite.log`／`LS-184-lock-suite-mainlike.log`／`LS-184-pretool-suite.log`／`LS-184-pretool-mutant.log`／`LS-184-pretool-baseline-scratch.log`／`LS-184-agent-tools-gate.log`／`LS-184-agent-tools-suite.log`／`LS-184-mutate.py`／`LS-184-mut/`。lock：未持有（本票純腳本邏輯；自測用合成 lock 目錄）；模擬器／Pen／容器未碰。

### 2026-09-05T01:05:50.252Z · Chih-Lin Yeh · `34d6c17f`

**2026-09-05 09:0x orchestrator — R1 handoff `6af6b24b` 收到（PR #296 head `b3b35e1`）→ In Review，派 merge-reviewer R1**

- 裁定：`--` 包裝模式**不**拒主 checkout（CI db job 與 merge-reviewer 在主 checkout 跑包裝命令是既定用法，命令型 holder 無 worktree 腿、無直通洞）——票文括號那句撤回，以實作為準；`--release`／`--held` 不擋（票文未列）；stop/start exact token 寧嚴可接受。
- 併入後 back-merge；併入即對在飛 agent 生效（主 checkout `--hold` 立即 exit 2）。

### 2026-09-05T01:08:09.283Z · Chih-Lin Yeh · `b8b4e7f1`

**2026-09-05 09:07 — PR #296 CI `db` job 失敗查因（run 33934840343）：CI 基礎設施 flake，與本票無關；已 `gh run rerun --failed` 一次**

- **失敗步驟**：`.github/workflows/ci.yml:524-526`「supabase db reset（套用 supabase/migrations）」——`run: supabase db reset`，**裸跑**，不經 `supabase-lock.sh`、hook 不在 CI 跑；前一步 :522 `supabase db start` 已成功。
- **根因引句**（`gh run view 33934840343 --log-failed`）：`failed to start docker container "supabase_db_little-sprout": Error response from daemon: failed to set up container networking: driver failed programming external connectivity on endpoint supabase_db_little-sprout (87196b89…): failed to bind host port for 0.0.0.0:54322:172.18.0.2:5432/tcp: address already in use` → `Error: failed to start containers: c8863dc6…`。即 `db reset` 重建 db 容器時，runner 的 docker 對 54322 的舊綁定尚未釋放（docker networking 競態），非腳本邏輯。
- **排除本票影響**：本票改動只在 (1) `supabase-lock.sh --hold` 路徑（CI 沒有任何步驟呼叫 `--hold`；`ci.yml:561` 的 `run.sh` 走 `-- <命令>` 包裝模式，本票明確未改、㉛ 有樣本釘住主 checkout 照常）、(2) `pretool_engine.py` H3b（PreToolUse hook，CI 不載入）。失敗發生在 `run.sh` 之前、且不經任何本票檔案；`rules`（含 `supabase-lock.test.sh`／`pretool.test.sh`／`agent-tools-check`）、`lint`、`ci` 三 job 皆綠。
- **處置**：`gh run rerun 33934840343 --failed` 已下（09:07）；結果等 CI 回報，reviewer R1 可先審 diff。若 rerun 仍紅且錯誤相同，屬 CI 環境問題（候選：`db reset` 前 `supabase stop` 或加重試），另入池、不在本票。
- head 不變：`b3b35e1`。

### 2026-09-05T01:52:19.772Z · Chih-Lin Yeh · `c73e6b78`

## merge-review R1 — **APPROVE**（PR #296，head `b3b35e14492c25f4043c5b8634b33693f0a03306`，base `ae6ca24`；2026-09-05 09:51）

範圍：`git diff ae6ca24...b3b35e1`，11 檔 +154/−19（lock 1＋自測 1／hook 3／gate 2＋docs 4）。無 blocker、無 major；4 條 informational 皆非阻擋。

### 裁定（實作者請 reviewer 裁的兩項）

**1. `--` 包裝模式不拒主 checkout（handoff「不修＋理由」第 1 條）——同意，且我獨立複驗過理由的兩隻腳：**
- `scripts/ops/supabase-lock.sh:270` `hold_owner_ok()` 首行 `case "$h_cmd" in hold:*) ;; *) return 1 ;; esac`、`scripts/hooks/pretool_engine.py:1024` `_h3b_holder_worktree` 同樣只認 `cmd=hold:*` → **命令型 holder 不給任何人 worktree 腿**，主 checkout 的 `--` 不會造成「主 checkout 程序直通」，票文那句要防的洞在 `--` 模式本來就不存在。
- `.github/workflows/ci.yml:561` `bash supabase/tests/run.sh` → `supabase/tests/run.sh:23-26` 以 `--held` 判定後 `exec bash "$lock_sh" -- bash run.sh` 自包 wrapper，而 CI 的 cwd 就是主 checkout（git-dir＝git-common-dir）——一併拒會直接紅 db job。實測：夾具主 checkout 內 `-- sh -c` 照跑 exit 0（㉛ 已釘住）。
- 與 orchestrator `34d6c17f` 的裁定一致，無須再改。

**2. `--release`／`--held` 不擋、stop/start exact token 寧嚴——同意**：主 checkout 已建不了 hold，`--release` 只可能對 `LS_LOCK_ALLOW_MAIN=1` 建的 hold 生效（㉛ 有樣本）；`supabase migration new start` 這類誤擋包 wrapper 即可，代價可接受。

### 我實跑的驗證（不是覆述 handoff）

- **自測（真 bash 3.2.57，`/bin/bash`）**：`supabase-lock.test.sh` **239 ✓**（㉛ 21 組）、`pretool.test.sh` 全綠（H3b-s 10 組）、`agent-tools-check.test.sh` **60 組**、真 repo `agent-tools-check.sh` 印「正文必含字樣 13 條」。
- **活夾具（隔離 `SUPABASE_LOCK_DIR`；`git init` 主 checkout ＋**真** `git worktree add` 的 linked worktree，未碰真 lock／容器）**：主 checkout `--hold` → **exit 2**，訊息尾端確實帶回原 label 與分鐘數（`… --hold "LS-999 dev E2E" --max-minutes 15（…LS-184）。orchestrator 明示放行：LS_LOCK_ALLOW_MAIN=1`）；`LS_LOCK_ALLOW_MAIN=0` → 仍 exit 2；worktree → `held pid=…`；不在 git repo 內 → 照常 held（git-dir 空不誤判）；`LS_LOCK_ALLOW_MAIN=1` → held 且 holder `worktree=` 記主 checkout。
- **H3b 決策探針（觸發字串以片段組裝，避免命中我自己的 hook）**：`supabase stop`／`supabase start -x studio`／`supabase db start` → **DENY**；`supabase stop --linked` → **DENY**（正確不吃 `--linked` 豁免）；`supabase status` → ALLOW；包 `supabase-lock.sh --` → ALLOW；`echo '…'`／`grep -rn '…' docs/` → ALLOW（無誤擋）。命令位置取 basename（:25）→ `/opt/homebrew/bin/supabase stop` 同樣命中，無絕對路徑繞道。
- **我自己的 mutation（暫存樹，未入 repo；環境雜訊 I6×3／⑫⑯⑰ 已扣除）**：
  - M1 整段砍掉 `supabase-lock.sh:216-227` 主 checkout 判定 → **killed**，恰 ㉛ 四條紅（exit 2／`cd <worktree> && …` 指引／帶回 label＋`--max-minutes`／`LS_LOCK_ALLOW_MAIN=1` 提示）。
  - M2 `[ "${LS_LOCK_ALLOW_MAIN:-}" != 1 ]` → `[ -z … ]`（把 `=0` 也當放行）→ **killed**，㉛ 兩條紅（exit 2 與「不留 lock」）。**放行變數只認 `=1` 是被測住的，不是巧合。**
  - M3 同時拿掉精確 `supa_lifecycle` 與字面 `FB_SUPABASE_LIFECYCLE_RE` 兩處 → **killed**，恰 **H3b-s①–⑥ 6 條 deny 變紅、allow 4 不動**——與 handoff／PR body 的「恰 6 條」宣稱一致（我獨立重現）。
- **LS-96 銷除**：`b3c2aa71`（parent `8fcc81c5`）／`b183721a`（parent `b4cb1e29`）都在、都掛對母 comment、都帶 PR #296＋commit SHA。
- **逐 commit 歸屬**：`8a91475` lock＋自測／`f9577ac` hook 三檔／`b3b35e1` 三份 agent 定義＋COLLABORATION＋gate 兩檔——切分乾淨，無夾帶；全 diff 的純空白改動只有 1 行（見 I-1）。
- **CI**：`ci`／`db`／`lint`／`rules` **四項全 pass**（run 33934840343，`db` 是 rerun 後綠）——前一次 `db` 紅屬 runner docker networking flake（54322 舊綁定未釋放），與 diff 無關，`b8b4e7f1` 的判斷成立。

### findings（皆 informational，不阻擋）

**I-1（informational · scope）`scripts/hooks/pretool_engine.py:186`**：`H3B_WHAT_FALLBACK ="本機 Supabase 容器操作…"` — 等號後少一個空白（PEP8 E225）。這是**全 diff 唯一一行純空白改動**，與票文 1–4 任何一項都對不上（手術式修改原則）；失敗情境：無功能影響、CI 不驗 python 格式，只是 diff 噪音與日後 blame 誤導。建議：下次動這個檔時補回空白（或本輪順手，非必要）。

**I-2（informational · 效能）`scripts/ops/supabase-lock.sh:217-218`**：兩次 `git rev-parse` fork 可併成一次——實測 `git rev-parse --path-format=absolute --git-dir --git-common-dir` 一次印兩行。依本檔自己的量測註解（PR #122 R1 M1「git 兩次 fork 約 60–130ms」、PR #265 R1 i4 為此把 `--status` 的 git 呼叫砍掉）約省 30–65ms。**只在 `--hold` 路徑、且在 `mkdir` 之前**，不落在 holder 空窗內、不放大任何競態窗口，所以價值很低——列出來只因本檔有「砍這種 fork」的前例。

**I-3（informational · 盲區未記錄，PLAUSIBLE 繞道）`pretool_engine.py:725-726`／`:797-798`**：實測 `npx supabase stop`、`pnpm exec supabase start` → **ALLOW**（命令位置是 `npx`／`pnpm`，非透明前綴詞）。失敗情境：agent 若改用 `npx supabase stop`，H3b 不擋、容器被起停而 lock 持有者的 reset／測試被打斷——正是本票要防的事故。判為 informational 而非 major 的理由：(a) 與既有 H3 對 `npx supabase db reset` 的限制同型、非本票新開；(b) 沒有任何 agent 定義／派工模板叫人用 `npx supabase`（repo 一律裸 `supabase`）。但我 grep 過 `docs/COLLABORATION.md`／`pretool.sh`／`pretool_engine.py`：**`npx` 三個字一次都沒出現**，這個家族盲區目前完全沒被記錄。建議：§7 H3 列盲區欄補一句（或入 LS-96 池），成本一行。

**I-4（informational · 一致性）`.claude/agents/qa.md:35`**：`--status` 沒看到自己的 label「就先 `--hold`」這句仍是裸寫法（無 `cd <worktree> &&`）。同檔 :17 已是正典寫法、`agent-tools-check` 也釘住 :17，而且真的裸寫時 lock 會 exit 2 並直接把正確寫法印出來——**判定：本 PR 不需要再補提示**（audit point 6 的結論）。我掃過三份 agent 定義＋`docs/COLLABORATION.md`＋`CLAUDE.md` 全部 `--hold` 出現處：所有**祈使句範例**都已帶 `cd <worktree> &&`，其餘是敘述句（`在 --hold 內操作`／`qa-e2e.sh 內部自 --hold`）。

### 沒看／沒跑的（誠實揭露）

- 未實跑任何真容器操作：`supabase stop`／`start` 一次都沒真的執行，H3b 只驗到 **hook 決策層**（deny/allow 與訊息），不含「擋下之後容器真的沒被動」。
- 未碰真 lock（`--hold` 一次都沒對真 lock 下）：**持有時長＝未持有**（本票純腳本邏輯，不需互動式實跑）。
- 未碰 Pen／模擬器；`.github/workflows/` 本 PR 未改、只讀不審。
- `pretool.test.sh` 我只驗「全綠」與新增 10 組的行為，未逐條複核既有 185 組。

**verdict：APPROVE**（無 blocker／major）。合併後記得 back-merge `main`→`development`，並提醒主 checkout `git pull` 後新 hook／lock 才生效（在飛 agent 下一次從主 checkout 發 `--hold` 會 exit 2，照訊息改成 `cd <worktree> && …` 重發即可）。

### 2026-09-05T01:54:18.171Z · Chih-Lin Yeh · `e2fea3cc`

**2026-09-05 09:53 orchestrator — merge-review R1 `c73e6b78` APPROVE → PR #296 已併入 main `b9470bc`（主 checkout 已 pull：`--hold` 拒主 checkout、H3b stop/start 生效）；back-merge main→development 背景鏈進行；票進 QA（隨 LS-183 同批 delta QA：`supabase-lock.test.sh` 239／`pretool.test.sh` 195／真環境 dry-run）。**

- I-3（`npx supabase` 不觸發 H3／H3b 盲區）／I-1（空白 nit）→ LS-96 `f8e101b4`；I-2 不修；I-4 依 reviewer 裁定不補。

### 2026-09-05T02:26:34.874Z · Chih-Lin Yeh · `8efe3df3`

**2026-09-05 10:25 orchestrator — back-merge PR #297 已併入 development 並晉升 test `0bdf667`；派 delta QA（LS-183＋LS-184：三支自測＋引擎 dry-run 六樣本＋`--hold` 主 checkout／worktree 實測）；PASS 後 test→main、tag v0.13.1（harness-only）。**

### 2026-09-05T02:38:53.546Z · Chih-Lin Yeh · `907a0f8a`

## QA — delta PASS（test `0bdf667`，2026-09-05 02:38 UTC）

驗的是 `test` head `0bdf66706621a68a2450bf42a010494928a247d3`（＝`origin/test`；含 LS-183＋LS-184 兩支 back-merge，11 檔）。worktree `.claude/worktrees/qa-test` detach 到該 SHA，clean。純 harness 票，未碰 app／模擬器／資料庫／Pen／容器。

### 逐條驗收（票文）

- [x] **主 checkout 內 `--hold` → exit 2 並印指引；worktree 內正常；`LS_LOCK_ALLOW_MAIN=1` 放行（自測）**：`bash scripts/ops/supabase-lock.test.sh`（`/bin/bash` 3.2.57）**239/239 ✓**（含 ㉛ 21 組主 checkout 判定）。**真環境實測**（非合成夾具，對真 `/Users/clyeh/little-sprout` 主 checkout 與真 `qa-test` worktree）：
  - 主 checkout（確認 `git rev-parse --git-dir`＝`--git-common-dir`＝`/Users/clyeh/little-sprout/.git`）內 `bash scripts/ops/supabase-lock.sh --hold "LS-184 QA probe" --max-minutes 1` → **exit 2**，訊息含「這是主 checkout…不得在這裡 --hold…請在票 worktree 內…`cd <worktree> && bash scripts/ops/supabase-lock.sh --hold "LS-184 QA probe" --max-minutes 1`…`LS_LOCK_ALLOW_MAIN=1`」；事後 `--status` 仍 `free`（未真的持有，無殘留）。
  - `cd <qa-test worktree> && bash scripts/ops/supabase-lock.sh --hold "LS-184 QA" --max-minutes 2` → **held pid=99983**；`--status` → `worktree=/Users/clyeh/little-sprout/.claude/worktrees/qa-test`（持有者正確記到該 worktree）；`--release` → 成功、持有 0m10s，釋放後 `--status` 回 `free`。
- [x] **未包裝 `supabase stop` → deny；包裝／持有者 → allow（自測）**：`pretool.test.sh` 195/195 ✓（H3b-s① `supabase stop` deny／H3b-s⑦ 包裝 allow／H3b-s⑧ 持有者 worktree allow 等 10 組）；真環境 dry-run（見 LS-183 comment）另證：裸 `supabase stop` → deny、`bash scripts/ops/supabase-lock.sh -- supabase db reset`（同型包裝）→ allow。
- [x] **merge-reviewer 通過；hotfix 流程（自 main、PR 回 main、back-merge）**：merge-review R1 `c73e6b78` APPROVE（0 blocker/major，4 informational 已記 LS-96／不修）；PR #296 → main `b9470bc`；back-merge 已併入 development 並晉升 test（`0bdf667`）。

### 其他確認
- `git diff --stat aa199f8..0bdf667` 11 檔全在 LS-183/LS-184 範圍，無夾帶。
- `scripts/gates/agent-tools-check.sh`（真 repo）exit 0，印「正文必含字樣 13 條」，三份 agent 定義皆含 `cd <worktree> && bash scripts/ops/supabase-lock.sh --hold`；`agent-tools-check.test.sh` 60 組全綠。
- `docs/COLLABORATION.md` §4-b 派工模板、§7 :217／:248／:249／:300 與三份 agent 定義字樣核對一致；`CLAUDE.md` 收尾儀式 ④（LS-180 Pen 清場）仍在。
- CI `test@0bdf667`：`ci`／`db`／`rules`／`lint` 四項皆 `success`（run 33939033576）。

**裁決：PASS**（與 LS-183 同批 delta QA）。

### 2026-09-05T02:41:20.523Z · Chih-Lin Yeh · `2ac9be44`

**2026-09-05 10:4x orchestrator — delta QA PASS `907a0f8a`（三支自測、六樣本 dry-run 全符、主 checkout `--hold` exit 2 且未持有、worktree 內 held→release）→ 已到 main `0bdf667`（v0.13.1，harness-only）；sweeper 批次巡檢中 → retro → Done。**

### 2026-09-05T02:45:55.034Z · Chih-Lin Yeh · `dae2c1b6`

## dead-code sweep — LS-184

**範圍**：`git diff --stat aa199f8..0bdf667`（main `0bdf667`，同批含 LS-183＋LS-184，11 檔）；主 checkout 唯讀巡查，未執行任何 hook 樣本、未動容器/Pen。

### Findings

**0 findings** — 未發現本 feature（LS-184）引入的死碼。

逐項核對：
- **`scripts/ops/supabase-lock.sh`**：主 checkout 判定新增段（:216-227，`git rev-parse --git-dir`/`--git-common-dir` 比對＋`LS_LOCK_ALLOW_MAIN` 分支）不是重複邏輯——既有的 `hold_owner_ok()`（:270）與 `read_holder()`（:166）維持原樣、未被繞過或部分廢棄；新判定放在既有取鎖流程「之前」，未留下任何舊判定分支變成永遠走不到的死碼。
- **`scripts/hooks/pretool_engine.py`**：LS-184 新增的 `H3B_SUPABASE_LIFECYCLE`／`FB_SUPABASE_LIFECYCLE_RE` 常數與 `check_precise`/`check_fallback` 內對應分支（`supa_lifecycle` 旗標）逐一 `grep -n` 確認皆 ≥2 命中（定義＋回傳判斷），排在既有 H3b 分支（`supa_local`）之後，未取代或遮蔽既有分支。
- **`scripts/ops/supabase-lock.test.sh`**（㉛ 21 組）／`scripts/hooks/pretool.test.sh`（H3b-s 10 組）：新增測項皆有對應 `rm -rf`／`unset` 清理（:589 `rm -rf "$fx"`；:622 `rm -rf "$ls184_work"`），未留暫存目錄或殘留變數污染後續樣本；未見任何被註解掉的舊案例。
- **`.claude/agents/{ios-dev,qa,merge-reviewer}.md`**：LS-184 把既有 `--hold` 例句改寫成 `cd <worktree> && bash scripts/ops/supabase-lock.sh --hold …` 同一命令鏈，是**原地修改**既有句子（非新增重複句）；核對三份定義未見改寫後遺留的舊裸 `--hold` 例句副本。
- **`scripts/gates/agent-tools-check.sh`**：`BODY_RULES` 新增三行（`cd <worktree> && …` 句），與 LS-183 新增的三行「本機容器操作同樣要在 lock 內」句是不同字樣、不衝突。
- **`docs/COLLABORATION.md`**：§4-b 派工模板（:135）／§6 序列化段（:217）／§7 lock 列（:248）／agent-tools 列（:249）／H3 列（:300）五處插入逐一核對，皆與 LS-183 已插入的句子分段清楚標註（`**LS-184**`），無同義重複；§6 :217 一段內同時含 LS-159 舊句與 LS-184 新句，讀起來是舊機制加新的機械擋一句，非重疊。

### 既有死碼觀察

同 LS-183 sweep：`docs/COLLABORATION.md:248` 保留一句 LS-88 之前的舊文字（「CLI 本身沒有 hook 可攔」），已由 LS-183 merge-reviewer 覆核判定語意仍成立、非死碼，不重複列入本票 finding。本票（LS-184）未新增任何類似文字債。

另注意到 merge-review（`c73e6b78`）已自行抓到並記錄一則本票程式碼的純空白瑕疵：`pretool_engine.py:186` `H3B_WHAT_FALLBACK ="..."` 少一個空白（PEP8 nit，非死碼，已入 LS-96）——不重複列為本次 finding。

### 搜尋方法與盲區聲明

- 方法同 LS-183 sweep：`git diff --stat`／逐檔 `git diff`；`pretool_engine.py` 新符號逐一 `grep -n` 計數判定孤兒與否；三份 agent 定義＋COLLABORATION.md 用 `Read` 讀取完整行核對語意重疊；`.test.sh` 新增區塊檢查清理陳述式是否齊全。
- 盲區：純文字比對抓不到動態載入／reflection 情形；未實跑任何 `.test.sh`（QA／merge-reviewer 已各自機械跑過 239/195/60 組全綠，本次巡檢只讀邏輯不重跑）；未對活資料庫或真容器查證（本票純腳本邏輯，QA／merge-reviewer 已用合成 lock 目錄與真夾具驗過主 checkout 拒絕行為）。

**建議處置**：無 finding 需要開票；既有死碼觀察與 PEP8 nit 兩項皆已被上游（reviewer／LS-96）覆核或記錄，不重複處理。

### 2026-09-05T02:46:45.663Z · Chih-Lin Yeh · `eb8589da`

**2026-09-05 10:5x orchestrator — 收尾儀式 → Done**

- sweeper `dae2c1b6` 0 finding；worktree／分支已清；LS-96 `8fcc81c5`／`b4cb1e29` 已銷除。
- **Lesson learning**：(1) 同一天三次「持有者被記成主 checkout」→ 當天立案、當天上線拒絕；根因（Bash 工具背景化後 cwd 重設）是平台行為，改用「`cd <worktree> && --hold` 同鏈」＋機械拒絕雙保險。(2) `--` 包裝模式不拒主 checkout 的裁定是對的（CI db job 與 merge-reviewer 依賴），實作者先揭露、reviewer 覆核 grant 面無借用洞——「票文寫了但不該做」由實作者提出是想要的行為。(3) CI `db` job 第一次紅是 runner docker port flake（LS-96 `305a9279`），不是本票；rerun 前先查因再 rerun 的順序正確。(4) QA agent 工具白名單無 `Write`，handoff 用 heredoc 落地——設計如此，但派 QA 的 prompt 之後不要寫「用 Write 工具寫 handoff」。

