# LS-150 Task：LS-136 寶貝 tab icon 由 figure.and.child.holdinghands 換 stroller.fill（使用者 0903 核可；AppSection 單點改＋a11y label 不變）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | lane:ui |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M4 日記與時間軸 |
| 估點 | — |
| 建立 | 2026-09-03T08:58:03.058Z |
| 開始 | 2026-09-03T08:58:03.132Z |
| 完成 | 2026-09-03T11:51:15.429Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-150/taskls-136-寶貝-tab-icon-由-figureandchildholdinghands-換-strollerfill使用者 |
| 父票 | LS-120 設計 chore：cmp/Tab Bar AX3 適應（單一形態規則） |
| 關係 | related→LS-132, related→LS-149, related→LS-96, related→LS-136, related→LS-142, related←LS-160 |

## 描述

## 來源

[LS-136](https://linear.app/little-sprout-app/issue/LS-136/taskls-120-ios-tab-bar-全字級純-icon-實作選中態紙片墨線icon-2632ax3-48voiceover) QA R1 `9fc8e90e` m4：寶貝 tab 現用 `figure.and.child.holdinghands`（大人牽小孩全身人形），核可稿 [LS-120](https://linear.app/little-sprout-app/issue/LS-120/設計-chorecmptab-bar-ax3-適應單一形態規則) 用 lucide `baby`（一張臉／嬰兒）；QA 建議 `stroller.fill` 更貼語意且長輩易辨。使用者 2026-09-03 16:58 裁決「換嬰兒車 OK」。

## 範圍

1. `LittleSprout/Navigation/AppSection.swift` `.children` 的 `systemImage` 改 `stroller.fill`；`accessibilityLabel`「寶貝」不變；`AppSectionTests.testEverySectionSymbolExistsInSFSymbols` 需通過（iOS 17 SDK 有 `stroller.fill`，若無則退 `stroller`）。
2. 尺寸／選中態沿用 [LS-136](https://linear.app/little-sprout-app/issue/LS-136/taskls-120-ios-tab-bar-全字級純-icon-實作選中態紙片墨線icon-2632ax3-48voiceover)（26／32、AX3 48）；截圖淺色＋深色＋AX3 各一張，像素量測 icon 視覺重量與其他三顆一致（不得明顯偏大／偏小）。
3. `.pen` Notes 的 SF Symbol 對照表補一行——不在本票動 .pen（設計票 [LS-142](https://linear.app/little-sprout-app/issue/LS-142/設計相簿頁相簿詳情上傳佇列ls-20-畫面群-ui-designervisual-reviewer-3-輪) 順手或下一張設計票補），本票在 handoff 註明。

## 驗收

- [ ] 四 tab 截圖（預設／AX3／深色）；`SectionTabBarTests` 與 tap-target gate 綠
- [ ] merge-reviewer 通過（scope：單點改）；QA 隨下一版 smoke

## 留言（9）

### 2026-09-03T11:05:25.118Z · Chih-Lin Yeh · `f7eb9c30`

## handoff

**Ticket**：LS-150（寶貝 tab icon `figure.and.child.holdinghands` → `stroller.fill`，使用者 2026-09-03 16:58 裁決）

**已完成**
- `LittleSprout/Navigation/AppSection.swift`：`.children` 的 `systemImage` 改 `stroller.fill`（`17ab59c` L37-44），`accessibilityLabel`「寶貝」不變。
- `LittleSproutUITests/SectionTabBarPushRegressionTests.swift`：同步修正硬編碼的 SF Symbol sentinel 比對字串（`17ab59c` L19/24）——這是實作時跑第一次全測試才發現的連鎖：sentinel 拿 icon 的 accessibility identifier 判斷 Tab Bar 是否存在，字串沒同步改會讓兩條 push regression 測試假紅。
- PR #252：`feature/LS-150-children-tab-icon` → `development`，head `17ab59c1803d3c4046cd656679fd9ea08cb07709`。

**已驗證：怎麼驗的**
- `xcodebuild test`（scheme LittleSprout，模擬器 `LS-150-iPhone17Pro`）：unit + UI 全綠，含 `AppSectionTests.testEverySectionSymbolExistsInSFSymbols`（`stroller.fill` 在 iOS 17+ SDK 存在，不需要退回 `stroller`）與修正後的 `SectionTabBarPushRegressionTests` 兩條。
- `swiftlint lint --strict --quiet`：0 違規。
- `git push` 觸發的 pre-push gate（push-gate.sh）通過：SwiftLint／unit tests／API 契約／migration／分支票號等機械檢查全過（第一次 push 因本機 SSH 連線在 11 分鐘的 gate 執行期間被 GitHub 斷線失敗，第二次加 `GIT_SSH_COMMAND` keepalive 後成功，過程記錄在 scratchpad 沒有影響 gate 判定本身）。
- 模擬器實機視覺驗收：用 `SIMCTL_CHILD_LS_TAP_TARGET_GATE_SCREEN=SectionTabView xcrun simctl launch`（app 內建的 `TapTargetGateHarness`，DEBUG-only、不需 `Config/Secrets.xcconfig`／不打真網路）直接開到 compact 版的 `AuthenticatedRootView`，實際點擊「寶貝」分頁確認選中態＋畫面標題「寶貝」（entry-conditions.md ⑬ 契約）正確。截圖預設／深色／AX3 各一張＋選中態抽測一張。
- 像素量測（`stroller.fill` 未選中態 vs. 相簿／設定，同字級同格比較；icon 墨色像素用 `Assets.xcassets/text-primary.colorset`／`text-secondary.colorset` 的實際 RGB 值比對，不猜測底色）：
  - 預設／深色：children 82×82px vs. 平均 89.0×80.5px（Δ寬 -7.9%、Δ高 +1.9%），在 ±15% 內。
  - AX3：children 126×127px vs. 平均 137.0×124.0px（Δ寬 -8.0%、Δ高 +2.4%），在 ±15% 內。
  - 寬度略窄（-8%）是 `stroller.fill` 本身比 `photo.on.rectangle.angled`／`gearshape` 更窄高的天然比例，未達「明顯偏小」門檻，未套用 `.imageScale` 或 symbol variant 調整。
  - 選中態抽測（children 選中 101×101px vs. 時間軸選中 101×86px，寬度一致，高度差來自 `rectangle.grid.1x2` 本身較扁的天然形狀）：沒有觀察到「明顯偏大」的視覺重量落差。
  - 證據：`/private/tmp/claude-501/-Users-clyeh-little-sprout/954fc361-48a1-4e75-b3ca-02a3ce7da71b/scratchpad/LS-150-evidence/`（`LS-150-default.png`／`LS-150-dark.png`／`LS-150-ax3.png`／`LS-150-children-selected.png`／`pixel-measurements.txt`）。

**未完成**
- `.pen` Notes 的 SF Symbol 對照表尚未同步更新（票文範圍 3 明講本票不動 `.pen`）——留給下一張觸碰 `cmp/Tab Bar` 的設計票（LS-142 或後續）順手補上，不另開票。
- merge-reviewer 尚未審查本 PR（PR #252 剛建立）——目前沒有 reviewer informational 待處置；審查回來後的 informational 由後續 round 依規約逐條處置。
- CI（GitHub Actions）：`db`／`lint`／`rules` 三個 job 已 pass，`ci`（xcodebuild）尚 pending，交 orchestrator 監看，不在此等待。

**風險**
無 race condition／效能／migration 風險——純字串常數替換＋一條既有測試的比對字串同步更新，scope 單點。唯一風險是視覺主觀判斷（icon 是否夠像「嬰兒車」對長輩夠易辨識）——已由使用者 2026-09-03 裁決核可，非本票自行認定。

**產出位置**
- PR：https://github.com/CLYEH/little-sprout/pull/252（head `17ab59c1803d3c4046cd656679fd9ea08cb07709`）
- Worktree：`/Users/clyeh/little-sprout/.claude/worktrees/LS-150`（branch `feature/LS-150-children-tab-icon`）
- 證據目錄：`/private/tmp/claude-501/-Users-clyeh-little-sprout/954fc361-48a1-4e75-b3ca-02a3ce7da71b/scratchpad/LS-150-evidence/`
- 模擬器已關：`F0FE9D87-FA41-4F93-B6D7-54E596D9B3A5`（LS-150-iPhone17Pro）


### 2026-09-03T11:06:19.204Z · Chih-Lin Yeh · `9ee34491`

orchestrator 2026-09-03 19:06：handoff `f7eb9c30` 收到——PR **#252** head `17ab59c`（`AppSection.children` → `stroller.fill`＋UI 測試 sentinel 字串同步；截圖 4＋像素量測 Δ寬 −8% 在 ±15% 內）→ In Review、派 merge-review R1。過程觀察入池 LS-96 `a98d7cdb`：手動 `xcodebuild test` 與 push gate 撞同一台模擬器（`simulator-lock.sh` 只保護 gate）、push 期間 SSH 斷線（11 分鐘 gate）。

### 2026-09-03T11:11:37.787Z · Chih-Lin Yeh · `a6d9c83f`

## merge-review R1 — PR #252（head `17ab59c`）

**判定：APPROVE**（0 blocker、0 major、3 informational）

審查範圍：`git diff 40dd1cd...17ab59c`（base = merge-base with `origin/development`）。四維度（race condition／運算效能／平行優化／scope）逐一過；本票是純字串常數置換，前三維度實質無面。

---

### 逐項查實

**① diff 範圍**：✅ 只有 2 檔，無夾帶。
- `LittleSprout/Navigation/AppSection.swift:45` `case .children: "stroller.fill"`（唯一功能變更），L32-40 註解同步（把 `.children` 從 LS-136 的「不動」清單移出＋補 LS-150 由來）——是被本次改動逼出來的文件修正，不算 scope 外。
- `LittleSproutUITests/SectionTabBarPushRegressionTests.swift:27` sentinel 陣列＋L18-23 註解。
- `accessibilityLabel` 不變：`SectionTabBar.swift:65` 是 `.accessibilityLabel(section.title)`，`AppSection.swift:24` `case .children: "寶貝"` 未進 diff，逐字未動。VoiceOver 標籤、`accessibilityShowsLargeContentViewer`（L71-73）行為不變。
- 尺寸／選中態未動：`SectionTabBar.swift` 完全不在 diff 內；`iconSize(selected:)`（L76-83）仍是 26／32、AX3 40／48，膠囊高 64／88（L22）。
- `.pen` 未觸碰 → `design-evidence-check` 不適用（票文範圍 3 明講本票不動，已在 PR body Design 行與 handoff 註明）。
- commit 訊息 `fix(ui): LS-150 …` 合規；單一 commit。

**② UI 測試硬編碼 → 建議列 informational（不擋）**，見下方 I1。

**③ `stroller.fill` 最低版本：✅ 獨立查證通過，非採信 handoff。**
- deployment target = iOS **17.0**（`project.yml:12-13`、`project.pbxproj:1237/1331` `IPHONEOS_DEPLOYMENT_TARGET = 17.0`）。
- 查 CoreGlyphs 權威表（iOS 26.5 simruntime 內 `SFSymbols.framework/CoreGlyphs.bundle/name_availability.plist`）：`stroller.fill` → year `2022.1`；同檔 `year_to_release["2022.1"].iOS = "16.1"`。**16.1 < 17.0，全支援版本皆存在**，不需退回 `stroller`（`stroller` 同為 2022.1）。
- 對照組：`figure.and.child.holdinghands` 2022（16.0）、`rectangle.grid.1x2` 2019、`photo.on.rectangle.angled` 2020、`gearshape` 2020——四顆現行符號全在 floor 之下。

**④ 截圖與量測：✅ 通過。**
- 四張截圖親眼看過（default／dark／ax3／children-selected），另把 AX3 tab bar 帶裁切放大並排比對四顆 icon：`stroller.fill` 在淺／深／AX3 三態都清楚讀成「嬰兒車」，與相鄰 tab 的視覺重量一致，未見偏大／偏小。選中態紙片（`$surface` 圓角＋1.5pt 描邊＋落影）正常包住 icon，畫面標題「寶貝」在位（entry-conditions ⑬ 契約）。
- 量測方法合理：以 `Assets.xcassets/text-primary`／`text-secondary` 的實際 RGB 找墨色像素（不猜底色——膠囊底與頁面背景近同色且有漸層，猜測法會誤把整個 cell 當 icon），並且只拿**同字級的未選中三顆**（albums／children／settings）做主要比較，含選中態的那一列自己標了「僅供參考」。方法站得住。
- 兩點方法學備註（不影響判定）：(a) ±15% 是實作者自訂門檻，票文只寫「不得明顯偏大／偏小」，不是稿面或 gate 定義的數字；(b) 單看 bbox 對不同長寬比的字形容易誤導——但同檔的 `px_count`（墨色像素數）交叉佐證：children 2920 落在 settings 1953 與 albums 3433 之間，非離群值，與我目視結論一致。
- 順帶查核一個我原本擔心的點：`.fill` 變體混進「紙片墨線」語彙會不會太重？裁切放大後確認**不會**——`photo.on.rectangle.angled`（相簿）本來就是前景實心的半填充符號、墨量還更大（3433 > 2920），現行四顆本來就是線稿／半填充混用，stroller 沒有破壞既有語彙。

**⑤ PR body／CI／scope**：
- `bash scripts/gates/pr-body-check.sh --branch feature/LS-150-children-tab-icon --verify <body>` → **exit 0**（「檔頭段含本票票號 LS-150」＋「申報反查：LS-96 行 0 條、『已修』行 0 條」）。Design: 行齊備、四張截圖路徑逐一列出且落在 scratchpad 未進版控（符合 `evidence-path-check` 取證不進版控的規約）。
- `gh pr checks 252`：**四項全綠**——`ci` pass（13m57s，handoff 當時尚 pending，現已完成）、`db` pass、`lint` pass、`rules` pass。
- informational 處置：handoff 未完成欄已列（`.pen` 對照表待下一張設計票、reviewer informational 待本輪回覆），reviewer 尚無前輪 informational 需追蹤，本輪為 R1。
- scope 未擴。

---

### 四維度

- **Race condition**：無面。`AppSection` 是無關聯值的 `enum`（隱式 `Sendable`），`systemImage` 是純 computed property；`SectionTabBar` 未動，`@MainActor` 隔離與 `@Binding` 寫入路徑不變。無 actor／Task／背景佇列涉入。
- **運算效能**：無面。無 DB／RLS／查詢／圖片解碼變更；`Image(systemName:)` 由系統快取 symbol，換一個名字不改 render 成本量級。
- **平行優化**：不適用（無並行工作）。
- **Scope**：合格。UI 測試那一改是本次符號置換的必然連鎖（不改會讓兩條 push regression 假紅），屬手術式修改的最小必要集。

---

### Informational（皆不擋，建議處置如下）

**I1 · `SectionTabBarPushRegressionTests.swift:26-28` 硬編碼四個 symbol 字串——建議「本票不改」**
```swift
private let tabIconIdentifiers = [
    "rectangle.grid.1x2", "photo.on.rectangle.angled", "stroller.fill", "gearshape"
]
```
- 可行性我查了：改成引用 `AppSection.children.systemImage` **技術上做得到**——UI test target 跑在獨立行程不能 `import LittleSprout`，但本 repo 已有現成慣例：`project.yml:87-92` 把 `LittleSprout/TapTargetGateScreenName.swift` 這一個檔案同時掛進 app 與 UI test 兩個 target 的 sources，就是為了消滅跨行程字串漂移。`AppSection.swift` 只 `import Foundation`、無 app 內部相依，照樣掛得進去。
- 但我建議**不要在本票做**，理由三條：(a) 失敗模式是**大聲**的——字串沒同步時兩條測試立刻紅（實作者這次正是這樣被抓到的），不是靜默漏測，這跟 `TapTargetGateScreenName` 要防的「launch environment 值對不上導致畫面沒起來」不同量級；(b) 把整個 production 導航型別複製進測試 bundle，比複製一個 rawValue enum 重，另抽一份「只有符號字串」的共用型別又是為單一用途造抽象；(c) 本票是使用者裁決的單點置換，順手重構即為 scope 擴張。現行的緩解（L18-19 註解明寫「見 `AppSection.systemImage`」）已足夠。
- 處置建議：記入 LS-96 待辦池即可，優先序低；若日後 tab 符號還要再動第三次，屆時再一併處理。

**I2 · `LittleSprout/Features/Children/ChildrenManagementView.swift:233` 仍用舊符號（本票正確地沒改）**
```swift
systemImage: "figure.and.child.holdinghands",
```
- 這是 regular（iPad）`NavigationSplitView` detail 欄「選擇一個寶貝」空狀態的 `ContentUnavailableView` 圖示，不是 tab icon。全 repo grep 確認這是唯一殘留的舊符號引用（其餘兩處只是註解裡的沿革敘述）。
- 併入後 iPad 版會出現「tab 是嬰兒車、寶貝頁空狀態是大人牽小孩」的圖像語彙不一致。**這不是本 PR 的缺陷**——票文範圍 1 明確只圈 `AppSection.swift`，動它才是 scope 外；使用者的裁決文字也只針對 tab icon。
- 處置建議：交 orchestrator 判斷是否要一致化（若要，記 LS-96 或併進 LS-142 順手處理），並提醒 QA 下一版 smoke 若在 iPad 看到這顆不必當作 LS-150 回歸。

**I3 · `AppSectionTests.testEverySectionSymbolExistsInSFSymbols` 的保證弱於名字（既有限制，非本 PR 引入）**
- `LittleSproutTests/AppSectionTests.swift:26-34` 用 `UIImage(systemName:)` 驗存在，但只在**當下跑測試的那個 runtime** 上驗；本機僅裝 iOS 26.0／26.5（`xcrun simctl list runtimes`），CI 同理。也就是說填一個 iOS 26 才引入的符號這條測試照樣綠，卻會在 iOS 17 裝置上靜默變空白圖示——正是這條測試註解宣稱要防的事故。
- 本票不受影響（我已用 `name_availability.plist` 獨立確認 16.1 < 17.0），但這個盲區值得記錄。真要機械化，得比對 CoreGlyphs 的 `name_availability.plist` 與 `IPHONEOS_DEPLOYMENT_TARGET`。
- 處置建議：記入 LS-96 待辦池（harness 缺口），非本票責任。

---

### 未覆蓋範圍（誠實聲明）

- 我**沒有**自己跑 `xcodebuild test`／模擬器（本票不需模擬器，orchestrator 派工明示）——測試綠採信 CI `ci` job pass（四項全綠，非僅採信 handoff）＋PR head 與我審的 SHA 一致。
- iPad regular 版面（`NavigationSplitView` sidebar 的 `Label(section.title, systemImage:)`）沒有截圖佐證，僅靜態讀碼確認同一個 `systemImage` 來源；換符號在該版面是同構變更，風險極低，但未實測。
- 未做無障礙實機 VoiceOver 朗讀驗證（label 字串未動，靜態確認即止）。


### 2026-09-03T11:13:07.395Z · Chih-Lin Yeh · `a9f4d75b`

orchestrator 2026-09-03 19:12：merge-review R1 `a6d9c83f` **APPROVE**（`stroller.fill` iOS 16.1 引入 < target 17.0 獨立查證；截圖四張＋AX3 並排目視、`px_count` 交叉佐證；diff 只 2 檔）→ PR #252 併入 development **`5cdd082`**（status 已貼）。informational：I2 `ChildrenManagementView.swift:233` iPad 空狀態仍用 `figure.and.child.holdinghands`（本票範圍外，入池 LS-96；QA 勿當本票回歸）；I3 `AppSectionTests` 只驗當下 runtime 符號存在（harness 缺口入池）；UI 測試硬編碼字串低優先入池。下一步：隨 LS-149 一起 promote→test → QA smoke（四 tab 截圖）。

### 2026-09-03T11:36:32.006Z · Chih-Lin Yeh · `8826f72f`

orchestrator 2026-09-03 19:35：test＝`bab1073`（同批 LS-149／LS-132）→ QA、派 qa smoke（四 tab 淺／深／AX3 截圖、選中態、視覺重量、VoiceOver label、三組測試綠；iPad 空狀態舊符號為既知 informational 不算回歸）。qa status 待 LS-149 同 PASS 後由 orchestrator 彙整貼。

### 2026-09-03T11:44:59.742Z · Chih-Lin Yeh · `2e4804cc`

## QA R1 — test `bab1073`

判定：**PASS**（4/4 可驗條件通過，1 條 informational 不算 FAIL）

Worktree `qa-test` fetch＋detach 到 `bab1073dedc78855de045edac9ba33bba9dcc226`；獨立讀 `LittleSprout/Navigation/AppSection.swift` 確認 `.children` 的 `systemImage` 就是 `stroller.fill`（L45），非採信 handoff。模擬器：專屬機 `LS-150-iPhone17Pro`（`F0FE9D87-FA41-4F93-B6D7-54E596D9B3A5`，ios-dev 先前建立，沿用）。

---

### 1. 四 tab 截圖（預設淺色／深色／AX3，寶貝 tab＝`stroller.fill`；選中態紙片＋標題「寶貝」；未選中態墨線）— **PASS**

用 `SIMCTL_CHILD_LS_TAP_TARGET_GATE_SCREEN=SectionTabView` 直開 tab bar（DEBUG harness，未打真網路）：

- `01-default-light-timeline.png`：預設淺色，時間軸選中、其餘三顆未選中墨線；mobile-mcp 讀 accessibility tree 確認 `寶貝` 按鈕 `identifier=stroller.fill`。
- `02-default-light-children-selected.png`：淺色點寶貝 tab 後，選中態紙片包住 icon＋畫面標題「寶貝」在位。
- `03-dark-children-selected.png`：深色寶貝選中，紙片＋標題「寶貝」正常。
- `04-dark-timeline-tabbar.png`：深色未選中態墨線（timeline 選中對照組）。
- `05-ax3-timeline-tabbar.png`：AX3（`accessibility-extra-extra-extra-large`＝`UICTContentSizeCategoryAccessibilityXXXL`）未選中，accessibility tree 再次確認 `寶貝`／`stroller.fill`。
- `06-ax3-children-selected.png`：AX3 寶貝選中，紙片＋標題「寶貝」正常，未破版。

證據：`/private/tmp/claude-501/-Users-clyeh-little-sprout/954fc361-48a1-4e75-b3ca-02a3ce7da71b/scratchpad/LS-150-qa-r1/01~06-*.png`

### 2. 視覺重量（與相簿／設定同格比較，不得明顯偏大／偏小；AX3 沿用 48／40pt）— **PASS**

獨立重測（不引用 handoff／review 數字），用 `text-secondary` colorset 實際 RGB（light `0x55/0x30/0x40`）±40 容差抓墨色像素，scale=3x，只比較未選中三顆（albums／children／settings，取自 `01`／`05` 兩張時間軸選中時的對照組）：

- 預設字級：albums bbox=97×80 px_count=3518；**children bbox=82×82 px_count=2962**；settings bbox=81×81 px_count=2023。avg(albums,settings) 寬 89.0／高 80.5 → children Δ寬 -7.9%／Δ高 +1.9%。
- AX3：albums bbox=149×123 px_count=8521；**children bbox=126×127 px_count=7254**；settings bbox=125×125 px_count=4946。avg 寬 137.0／高 124.0 → children Δ寬 -8.0%／Δ高 +2.4%。
- 兩字級下 Δ 皆在 ±15% 內；px_count 排序皆落在 settings／albums 之間（非離群值）。與 handoff／merge-review 數字獨立重跑後一致（未直接引用）。未見「明顯偏大／偏小」。

證據：`.../LS-150-qa-r1/pixel-measurements.txt`

### 3. VoiceOver label「寶貝」— **PASS**

- mobile-mcp accessibility tree（預設＋AX3 兩次）：`{"type":"Button","label":"寶貝","name":"stroller.fill","identifier":"stroller.fill"}`。
- `xcodebuild test -only-testing:LittleSproutUITests/SectionTabBarTests`：**7/7 綠**，含 `testEveryTabHasAccessibilityLabelMatchingItsName`（斷言 accessibilityLabel＝「寶貝」）與 `testChildrenRootShowsChildrenHeading`。

### 4. `xcodebuild test`（AppSectionTests／SectionTabBarPushRegressionTests／TapTargetGateTests）— **PASS**

用 `LS-150-iPhone17Pro` 跑：
- `AppSectionTests`：3/3（含 `testEverySectionSymbolExistsInSFSymbols`）
- `SectionTabBarPushRegressionTests`：2/2
- `TapTargetGateTests`：5/5（含 `testSectionTabView`）

`** TEST SUCCEEDED **`。log：`.../LS-150-qa-r1/xcodebuild-test.log`

### 5. informational（不算 FAIL）— **⊘ 未驗，如實標註**

`ChildrenManagementView.swift:233` iPad regular「選擇一個寶貝」空狀態仍用 `figure.and.child.holdinghands`——按 review I2 已入池 LS-96（`d9a3c88a`），本輪未驗、不視為回歸。

### 6. 模擬器收工

`xcrun simctl shutdown F0FE9D87-FA41-4F93-B6D7-54E596D9B3A5`（`LS-150-iPhone17Pro`）已執行，僅關自己這台。

---

**注意**：test tip `bab1073` 同時含 LS-149（backend），status 需等兩票皆 PASS 才由 orchestrator 彙整貼 `qa` status，本輪不單獨貼。

### 2026-09-03T11:45:31.518Z · Chih-Lin Yeh · `0b9657d7`

orchestrator 2026-09-03 19:45：QA R1 `2e4804cc` **PASS**（四 tab 淺／深／AX3、選中態、視覺重量獨立重量 Δ寬 −8% 在 ±15%、VoiceOver「寶貝」、三組測試 10/10 綠）。qa status 等 LS-149 同 SHA PASS 後彙整貼。收尾儀式開始：派 sweeper → retro → promote main → cleanup → Done。

### 2026-09-03T11:47:12.564Z · Chih-Lin Yeh · `75426afc`

## dead-code sweep — LS-150（test `bab1073`）

（dead-code-sweeper 產出、orchestrator 代貼，19:46）**結論：未發現本 feature 引入的死碼。**

範圍：PR #252 merge commit `5cdd082` 僅 2 檔（`AppSection.swift` systemImage＋沿革註解；`SectionTabBarPushRegressionTests.swift` sentinel 陣列＋註解），13+/5−。
1. 舊符號 `figure.and.child.holdinghands` 全 repo 3 處：`ChildrenManagementView.swift:233`（iPad 空狀態，**非本票引入**，既知入池 LS-96 `d9a3c88a`）、`AppSection.swift:37` 與測試 `:22` 皆為沿革 doc comment。無殘留呼叫點。
2. 測試檔無備援分支／註解掉的舊陣列；helper 單一路徑。
3. `AppSectionTests` 為 `allCases` 通用迴圈，無舊符號特例、無 `XCTSkip`。
4. `stroller` 全 repo 5 處皆預期；`AppSection.systemImage` 是唯一真實來源；diff 無 debug print／暫時旗標。

盲區：執行期字串拼接／reflection／storyboard 間接引用（本例 switch 常數字面值，風險低）。

### 2026-09-03T11:51:11.998Z · Chih-Lin Yeh · `f49b1de7`

## retro — LS-150（orchestrator lesson learning，19:50）

**落地**：test `bab1073` → main **`bab1073`**（19:50，同批 LS-149／LS-132）→ tag **v0.8.5** → worktree 已清（`cleanup-merged.sh --apply`）。iPhone 灌版待下一輪（目前手機正在灌 v0.8.4）。

**做對的**：單點改＋UI 測試 sentinel 同步在同一 PR，測試先紅再綠（實作者自己被抓到）；截圖用 `TapTargetGateHarness` 直開 tab bar 不需 Secrets／OTP；像素量測改以 token 色比對墨色（背景與膠囊近色時 bbox 法失效）——這個方法值得寫進 QA 視覺量測慣例（入池）。reviewer 獨立查證 `stroller.fill` iOS 16.1 引入，不信 handoff。

**harness 改善／工具缺口（皆已入池 LS-96）**：`d9a3c88a` P2 `AppSectionTests` 只驗當下 runtime 符號存在（iOS 26 符號在 iOS 17 會空白）、P3 iPad 空狀態舊符號、P3 sentinel 硬編碼；`a98d7cdb` P3 手動 `xcodebuild test` 與 push gate 撞同一台模擬器（`simulator-lock.sh` 只保護 gate）、P3 push gate 11 分鐘 SSH 斷線（keepalive 設定成文）。

**orchestrator 自身**：agent commit 後 40 分鐘未 push（等背景通知沒醒）——巡檢查勤有抓到，但應在派工 prompt 就寫「不開背景等待、push 後前景 `--watch`」（dispatch-prompt-rules 已有，這次派工文漏寫）。

**待使用者**：無（icon 裁決已落地）。

