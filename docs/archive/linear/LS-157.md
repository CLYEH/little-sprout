# LS-157 Harness：XS 池項同批——main-checkout-guard 三項（GIT_* 全剝修 fail-open／帶引號前綴與 env -i／nice 穿透／「即將建立的 worktree」誤擋）＋pre-commit 衝突標記 gate＋dead-code-sweeper 白名單補 save_comment

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-04T02:35:40.985Z |
| 開始 | 2026-09-04T02:35:41.124Z |
| 完成 | 2026-09-04T03:45:23.268Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-157/harnessxs-池項同批-main-checkout-guard-三項git-全剝修-fail-open帶引號前綴與-env-inice |
| 關係 | related→LS-158, related→LS-144, related→LS-96, related→LS-154, related←LS-238, related←LS-159, related←LS-156 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 待辦池 XS 項同批升票（§5-b harness 優先序；池註「與其他 XS harness 同批」）：`42b7816e`（P2＋P3，來源 [LS-154](https://linear.app/little-sprout-app/issue/LS-154/harness擋-agent-寫入主-checkout-pretooluse-hook-對-writeedit-路徑與-bash) review R2 `47ab021a` N1／informational 1）、`53e7b0fd`（P3×2，來源 [LS-154](https://linear.app/little-sprout-app/issue/LS-154/harness擋-agent-寫入主-checkout-pretooluse-hook-對-writeedit-路徑與-bash) 收尾實測）、`5cb5ddf3` 第一項（P2，dead-code-sweeper 缺 `save_comment`，今日已代貼 3 次）。[LS-154](https://linear.app/little-sprout-app/issue/LS-154/harness擋-agent-寫入主-checkout-pretooluse-hook-對-writeedit-路徑與-bash) 的 hook 已於 2026-09-04 併 main `4d9e44e` 生效，其中 N1 是**無聲 fail-open**，優先補。

## 範圍

1. `scripts/hooks/main_checkout_guard.py`（三項）：
   a. `_clean_env()` 改剝所有 `GIT_*` 前綴變數（R1 原建議），並把 `repo_dirs`／`is_main_checkout` 對「路徑落在 `CLAUDE_PROJECT_DIR`（或 hook 解析出的 repo 根）字首之下、但 `git rev-parse` 失敗」的情況改為 **deny 並印原因**（fail-closed；repo 外路徑如 scratchpad 不受影響）。負樣本：`GIT_OBJECT_DIRECTORY=/nonexist` 下主 checkout Write → deny、同環境 worktree Write → allow；mutation：拿掉 `startswith("GIT_")` 改回具名清單 → 負樣本變綠即紅。
   b. `find_cmd`／`:237-241` `not hq` 條件：`FOO="a b" rm <主>`、`FOO='a b' tee <主>`、`env -i rm <主>`、`nice rm <主>`（可再加 `nohup`）皆須穿透到真正的命令並 deny；正樣本同形對 worktree allow。
   c. 「即將建立的 worktree」：目標路徑落在 `<root>/.claude/worktrees/<name>/…`（`<name>` 之下至少一層）且 `<name>` 目錄尚不存在時視為 worktree 放行（`git worktree add` 後同一條命令 `cd` 進去 `git merge` 的形狀，[LS-154](https://linear.app/little-sprout-app/issue/LS-154/harness擋-agent-寫入主-checkout-pretooluse-hook-對-writeedit-路徑與-bash) 收尾實測誤擋）；`<root>/.claude/worktrees/` 本身與 `<root>/.claude/worktrees/<name>` 這一層的直接寫入仍擋。正負樣本各 1。
2. **pre-commit 衝突標記 gate**：`scripts/gates/commit-gate.sh`（或 `.githooks/pre-commit` 既有鏈）加一步：`git diff --cached -U0` 中新增行以 `<<<<<<< `／`=======`（整行）／`>>>>>>> ` 開頭即 fail 並列檔名（先 grep 全 repo 確認文件內無合法示範；若 `docs/*.md` 有，改為只排除 fenced code block 內或以路徑白名單處理並寫明）。自測正負樣本＋掛 CI `rules` job。
3. `.claude/agents/dead-code-sweeper.md` `tools:` 加 `mcp__linear__save_comment`；「輸出」段改為「用 `save_comment` 直接貼到該票，標題 `## dead-code sweep — LS-<n>`，並在 handoff 回傳 comment id」；`scripts/gates/agent-tools-check.sh` 若有必要工具表一併更新（自測綠）。
4. `docs/COLLABORATION.md` §7 對照表：[LS-154](https://linear.app/little-sprout-app/issue/LS-154/harness擋-agent-寫入主-checkout-pretooluse-hook-對-writeedit-路徑與-bash) 那一列補三項修正；commit gate 列補衝突標記；§6 收尾儀式一句改 sweeper 直貼。
5. [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 銷除註記：`42b7816e`（整則）、`53e7b0fd`（整則）、`5cb5ddf3`（只第一項 P2；第二項 P3 db reset 清種子留池）。

## 不做

`pretool_engine.py` 本體重構；main-checkout-guard 與 `pretool.sh` wrapper 共用化；`5cb5ddf3` 第二項；`e1d6d282` QA e2e 驅動（另票）。

## 驗收

- [ ] `main-checkout-guard.test.sh` 新增正負樣本＋mutation 全過（既有 87 案不退化），CI `rules` 綠
- [ ] `commit-gate` 衝突標記自測正負樣本綠、掛 CI `rules`；本機對含標記的 staged 檔實跑被擋
- [ ] `agent-tools-check.sh` 綠；sweeper 定義含 `save_comment`
- [ ] merge-reviewer 通過（誤擋面：[LS-154](https://linear.app/little-sprout-app/issue/LS-154/harness擋-agent-寫入主-checkout-pretooluse-hook-對-writeedit-路徑與-bash) R1 121 組正樣本形狀不得退化，特別是 `python3 - <<EOF` 與 memory symlink）
- [ ] 併 main → back-merge → 主 checkout `git pull` 後 orchestrator 實跑 `GIT_OBJECT_DIRECTORY=/nonexist` 探針被擋
- [ ] [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 三則銷除註記

## 留言（7）

### 2026-09-04T02:37:11.520Z · Chih-Lin Yeh · `533ad548`

orchestrator 2026-09-04 10:36：harness lane 空 1 輪（LS-154 Done 後）→ 依 LS-144 開票責任自 LS-96 池 XS 項同批開本票（`42b7816e`／`53e7b0fd`／`5cb5ddf3` 第一項，銷除公告 `cd29dc1e`）；P1 `e1d6d282` 另立 LS-158 待 Spec。worktree `hotfix/LS-157-guard-xs-batch` 自 main `4d9e44e`；派 general-purpose agent。PR 回 main → back-merge → `git pull` → orchestrator 實跑 `GIT_OBJECT_DIRECTORY` 探針。

### 2026-09-04T03:02:07.244Z · Chih-Lin Yeh · `f6c63931`

general-purpose 2026-09-04 11:00 handoff

**Ticket**：LS-157 — PR **#262** `hotfix/LS-157-guard-xs-batch → main`，head **`5e14aae`**（自 main `4d9e44e`；四段 commit `74751e3`／`d8fd5f3`／`2706f40`／`5e14aae`，皆過 commit／push gate）。PR body 過 `pr-body-check.sh --verify`（本機無 LINEAR_API_KEY，comment id 只驗格式）。**未等 CI**。

**已完成**

| 項 | 修法 | commit | 位置 |
|---|---|---|---|
| 1a `42b7816e` P2（R2 N1 無聲 fail-open） | `_clean_env()` 改剝**所有** `GIT_*`；安全網：目標 realpath 落在 hook 自己解析出的 repo 根（`CLAUDE_PROJECT_DIR`，缺則 cwd 上溯找 `.git`，不靠 git）之下但 rev-parse 說「not a git repository」→ **W0 deny 並印 rev-parse stderr 首行**；repo 外路徑不受影響 | `74751e3` | `scripts/hooks/main_checkout_guard.py:132-137`（`_clean_env`）、`:140-165`（`_ROOT_HINT`／`root_hint`／`_under_root`／`_under_root_msg`）、`:213-217`（安全網 raise）、`:577-578` |
| 1b `42b7816e` P3（informational 1／3） | `find_cmd` 對賦值 token 不再要求「未加引號」（`FOO="a b"` 被 tokenizer 收成單一 hq token）、透明前綴詞後緊接的 `-` 旗標跳過（`env -i`）、`nice` 補進透明表（engine 沒有）；`INLINE_SWITCH_RE` 前綴賦值值可帶引號（引號只由引號分支吃、不平衡即不匹配，無回溯爆炸）；開關本身加引號不認（bash 裡不是賦值） | `d8fd5f3` | `main_checkout_guard.py:104`、`:126-129`、`:278-302` |
| 1c `53e7b0fd` 第一項 P3（worktree add＋cd＋merge 誤擋） | `hits(abspath, as_dir=False)`：`<root>/.claude/worktrees/<name>/…` 且 `<name>` 不存在（`lexists`）→ 視為 worktree 放行並 stderr 註記；W1／W2 需 `<name>` 之下至少一層，W3 的 git 目錄可就是 `<name>`；`worktrees/` 本身與 `<name>` 一層的檔案寫入／`mkdir` 仍擋 | `d8fd5f3` | `main_checkout_guard.py:105`、`:406-424`、`:506` |
| 2 `53e7b0fd` 第二項 P3（衝突標記） | 新 `scripts/gates/conflict-marker-check.sh`：`git diff --cached --no-renames --diff-filter=ACM -U0` 的 `+` 行以 `<<<<<<< `／`=======`（整行）／`>>>>>>> ` 開頭即紅、列 檔案:行號（hunk 標頭算行號；`---`/`+++` 成對才認檔名）；exit 0／1／2（非 repo fail closed）。**全 repo grep 零命中 → 純 staged-diff 檢查、不做 fenced-block／路徑排除**（檔頭寫明誤擋面：setext 恰好七個 `=` 底線）。接進 `commit-gate.sh`（tracked-ignored 之後）；自測 13 組掛 CI `rules`（step 名稱清單加 `conflict-marker-check`） | `2706f40` | `scripts/gates/conflict-marker-check.sh`（awk `:27-33`）、`conflict-marker-check.test.sh`、`scripts/gates/commit-gate.sh:36-38`、`.github/workflows/ci.yml:164`／`:179-180` |
| 3 `5cb5ddf3` 第一項 P2（sweeper） | `tools:` 加 `mcp__linear__save_comment`；輸出段改「用 `save_comment` 直貼該票、標題 `## dead-code sweep — LS-<n>`、handoff 回傳 comment id」；`agent-tools-check.sh` 規則表 dead-code-sweeper 改 Linear 三支（自測補「少 save_comment」一組 → 24 組） | `5e14aae` | `.claude/agents/dead-code-sweeper.md:4`、`:28`；`scripts/gates/agent-tools-check.sh:41-45`；`agent-tools-check.test.sh:47`、`:70-71` |
| 4 文件 | §1 `:33` 審查類 agent Linear 權限；§2 `:59` Worktree 規約補「即將建立的 worktree」（R1 誤擋面第 7 類）；§4 `:111` Commit gate 列補衝突標記、`:116` 收尾 gate sweeper 直貼；§6 `:223` 收尾儀式 sweeper 直貼＋回傳 comment id；§7 `:291` agent tools 列、`:304` LS-154 列（全剝＋安全網／前綴穿透／即將建立的 worktree／自測計數 33-41-9-6-3-5／盲區改寫）、`:305` 新增衝突標記 gate 一列 | `5e14aae` | `docs/COLLABORATION.md` |
| 5 LS-96 銷除註記 | orchestrator 已於 `cd29dc1e` 貼升票公告，本票**不另貼**（`5cb5ddf3` 第二項 db reset 清種子留池） | — | — |

**已驗證**
* `bash scripts/hooks/main-checkout-guard.test.sh` → **116 ✓／0 ✗**（既有 87 不退化＋新 29）：正 P28–P32（＋P30b 註記）、負 N31–N39、開關 S8／S9（＋S8b）、git 環境變數 G4–G6（＋G4b 驗 deny 是 **W1** 不是靠安全網）、安全網 X1–X3（＋X1b 驗 stderr 含 rev-parse 首行；X3 用 `env -u CLAUDE_PROJECT_DIR` 走 cwd 上溯）、mutation **M4a／M4b／M5**（見「未完成／自查」第 1 條，與票文 mutation 描述有出入）。fixture 新增 `$fx/broken/.git` → `gitdir: <不存在>`（rev-parse 回 `fatal: not a git repository: …`）。
* `bash scripts/hooks/pretool.test.sh` 139 ✓（engine 未動）；`bash scripts/gates/conflict-marker-check.test.sh` 13 ✓；`bash scripts/gates/agent-tools-check.sh` 綠（6 份）＋`agent-tools-check.test.sh` 24 ✓；`ci.yml` YAML 可解析。
* **本機對含標記的 staged 檔實跑**：worktree 內 `LS-157-marker-probe.txt`（第 2 行 `<<<<<<< HEAD`）`git add` 後跑 `commit-gate.sh` → exit 1、輸出 `LS-157-marker-probe.txt:2: <<<<<<< HEAD`（探針已 reset＋刪除，worktree clean）。
* **真實路徑探針 33 組、0 不符**（`CLAUDE_PROJECT_DIR=/Users/clyeh/little-sprout`，直接餵 hook JSON、只評估不寫；腳本與輸出在 scratchpad `LS-157-realpath-probe.sh`／`LS-157-realpath-probe-output.txt`）：
  * A. R2 N1 重現 `GIT_OBJECT_DIRECTORY=/tmp/nonexist-objs` 下 Write `/Users/clyeh/little-sprout/LS-154-probe.txt`：**main 現行 hook（4d9e44e）exit 0、stderr 空（對照）；本分支 hook exit 2 `W1：主 checkout 禁寫`**；同環境 worktree Write／scratchpad Write 皆 exit 0。
  * B. R1「實測確認不誤擋」抽 14 組全 exit 0：`F=<memory>/MEMORY.md; python3 - "$F" <<'EOF'`、heredoc 內文字面寫 memory 絕對路徑（`cat > <memory symlink>/x.md`）、`sed -n '1,5p' docs/COLLABORATION.md`（cwd 主）、`git pull --ff-only`、`bash scripts/ops/promote.sh development test`、`git -C 主 worktree add …/LS-999`、`cleanup-merged.sh --dry-run`、`gh pr create --body-file <scratchpad>`、Write scratchpad／memory symlink／`.claude/patrol-state.json`、`cat 主/docs/… | grep`、worktree 內 `sed -i ''`＋`git commit`、`git status; log; rev-parse`。
  * C. 本票新行為 15 組全符：`FOO="a b" rm 主`／`FOO='a b' tee 主`／`env -i rm 主`／`nice rm 主`／`nohup rm 主` → exit 2 **W2**（舊 hook 對 `FOO="a b" rm 主` exit 0，對照）；同形對 worktree exit 0；`FOO="a b" LS_ALLOW_MAIN_CHECKOUT_WRITE=1 rm 主` → exit 0 且 stderr「命令位置前綴帶 LS_ALLOW…=1」（是開關、不是盲區）；`git worktree add …/LS-999 -b … && cd …/LS-999 && git merge origin/main`（LS-999 不存在）→ exit 0 並註記「尚未建立的 worktree LS-999」（舊 hook exit 2 W3，對照）；`echo x > 主/.claude/worktrees/LS-999`（只一層）→ exit 2；Write `主/.claude/worktrees/LS-999/docs/x.md` → exit 0；Write `主/LS-154-probe.txt`／Edit `主/.github/workflows/ci.yml` → exit 2。
* 主 checkout 與 worktree `git status --porcelain` 皆空。

**未完成**
* reviewer informational 處置：**無**（merge-review 尚未派）。
* 自查盲區／與票文的出入：
  1. **票文 1a 的 mutation「換回五個具名 → 負樣本變綠」在有安全網的情況下不成立**——mutant 下主 checkout Write 仍 deny，只是從 W1 退成 W0。故 M4 改驗兩件事：M4a 同環境 **worktree** Write 變紅（證明全剝有牙齒）、M4b 主 checkout deny 理由退成 **W0**（證明安全網真的接住 N1）；另加 M5（拿掉安全網 raise → 壞 `.git` 根下 Write 變綠）獨立驗安全網。請 reviewer 認可此替代。
  2. 旗標帶獨立參數的前綴（`sudo -u bob rm 主`／`nice -n 5 rm 主`／`timeout 5 rm 主`）：參數 `bob`／`5` 被當成命令名 → 仍靜默放行（§7 盲區已改寫指明）；`env -i LS_ALLOW…=1 cmd` 開關 regex 不吃 env 旗標 → 走一般評估、deny 訊息叫人用 `env LS_ALLOW…=1 <cmd>`（可用，未特別支援）。
  3. 安全網的 repo 根：`CLAUDE_PROJECT_DIR` 若本身是 worktree，字首只涵蓋該 worktree（主 checkout 判定仍走 git 語意；安全網只是 rev-parse 失敗時的兜底）。
  4. `find_cmd` 既有 `guard < 16` 迴圈上限：前綴串超過 16 個 token → 回 None → 該段跳過（pre-existing fail-open，未動）。
  5. 衝突標記 gate 不掛 CI（PR merge ref 無 staged）；diff3 的 `|||||||` 未單獨列（必與 `<<<<<<<` 同現，仍擋）。
* 併 main 後（orchestrator）：back-merge `main`→`development`（注意 `ci.yml:164` step 名稱清單本 PR 加名，若他票同期改同行會再衝突）；主 checkout `git pull` 後實跑驗收探針——`printf '%s' "$(python3 -c 'import json;print(json.dumps({"tool_name":"Write","cwd":"/Users/clyeh/little-sprout","tool_input":{"file_path":"/Users/clyeh/little-sprout/LS-157-probe.txt"}}))')" | env CLAUDE_PROJECT_DIR=/Users/clyeh/little-sprout GIT_OBJECT_DIRECTORY=/tmp/nonexist-objs bash /Users/clyeh/little-sprout/scripts/hooks/main-checkout-guard.sh; echo exit=$?` 應 `exit=2` 且 stderr `W1：`。

**風險（誤擋面變化）**
* 只增加放行（1c、引號值開關）與對「先前無聲放行」形狀的 deny（1b 三形皆為寫主 checkout，本就該擋）；R1 121 組正樣本形狀抽 14 組實跑未退化。
* 安全網只在「目標在 repo 根之下且 rev-parse 說不在 repo 內」觸發；全剝 `GIT_*` 後正常環境不會失敗，不會誤觸。
* 效能：`_clean_env` 仍每個 distinct 目錄一次；`root_hint` 每次 hook 一次純檔案系統查詢，未量測到可感知差異。

**產出位置**：PR https://github.com/CLYEH/little-sprout/pull/262（head `5e14aae`）；worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-157`；檔案 `scripts/hooks/main_checkout_guard.py`、`scripts/hooks/main-checkout-guard.test.sh`、`scripts/gates/conflict-marker-check.sh`、`scripts/gates/conflict-marker-check.test.sh`、`scripts/gates/commit-gate.sh`、`.github/workflows/ci.yml`、`.claude/agents/dead-code-sweeper.md`、`scripts/gates/agent-tools-check.sh`、`scripts/gates/agent-tools-check.test.sh`、`docs/COLLABORATION.md`；scratchpad `LS-157-pr-body.md`、`LS-157-realpath-probe.sh`、`LS-157-realpath-probe-output.txt`。

### 2026-09-04T03:22:38.719Z · Chih-Lin Yeh · `24ec4983`

## merge-review R1 — PR #262（head `5e14aae`）

**裁決：APPROVE**（0 blocker／0 major；1 minor＋4 informational）

審的 head `5e14aae34a4798639168ebac9040f6532f8ac168`；`gh pr view 262 --json headRefOid` 當下同值（審查期間 head 未前進）。diff `4d9e44e...5e14aae`，10 檔 +369/−32。作業於 worktree `.claude/worktrees/LS-157`，只讀不改；主 checkout 與 worktree `git status --porcelain` 皆空。

### 怎麼驗的（不靠讀碼）

| 面向 | 做法 | 結果 |
|---|---|---|
| shipped 自測 | worktree 內實跑四支 | `main-checkout-guard.test.sh` **116 ✓／0 ✗**、`pretool.test.sh` **139 ✓／0 ✗**、`conflict-marker-check.test.sh` **13 組**、`agent-tools-check.sh` 綠（6 份）＋`agent-tools-check.test.sh` **24 組** |
| **誤擋面回歸** | 真實路徑餵 hook JSON，**新舊兩版並排**（舊＝`4d9e44e` 的 `scripts/hooks/`，與主 checkout 現行 hook byte-identical），`CLAUDE_PROJECT_DIR=/Users/clyeh/little-sprout` | **87 組、0 不符、0 退化**（腳本／輸出見下） |
| 1c symlink 與路徑穿越 | 合成 repo fixture（真實 repo 不能寫） | 10 組全符 |
| 衝突標記邊界 | 合成 repo，9 組額外邊界 | 7 符、2 為新 finding（見 N2／N3） |
| **mutation（我自己做的，非 shipped 那幾支）** | 對原始碼下 12 個 mutation，跑 shipped 全套看是否變紅 | **11 被殺、1 存活**（見 N1） |
| 端到端 | 真造 merge 衝突 → 保留標記 `git add` → 跑整條 `commit-gate.sh` | rc=1，逐行列出 `docs.txt:2/4/6`；清掉標記後整條鏈綠 |
| 效能 | 新舊交錯各 15 次 | 中位數 **49.5 ms vs 49.5 ms**（無差異） |
| CI／PR body | `gh pr checks 262`；`pr-body-check.sh --branch hotfix/LS-157-guard-xs-batch --verify` | `ci`／`db`／`lint`／`rules` **四綠**（run 33831611975）；PR body 通過 |

### A. 誤擋面：87 組實跑，**0 退化**

「退化」定義＝舊版放行、新版擋，且該形狀本應放行。實得 **0 組**。所有「舊 0 → 新 2」的轉變（7 組）全部是本票**刻意新增的 deny**：`FOO="a b" rm 主`／`FOO='a b' tee 主`／`env -i rm 主`／`nice rm 主`／`env -i FOO="a b" tee 主`／`command nice rm 主`／`GIT_OBJECT_DIRECTORY=/nonexist` 下主 checkout Write。

**A 組 40 個正樣本全 exit 0（新舊皆 0）**，涵蓋 LS-154 R1「實測確認不誤擋」清單：`F=<memory>/MEMORY.md; python3 - "$F" <<'EOF'`、heredoc 內文字面寫 memory 絕對路徑（`cat > <memory symlink>/x.md`）、`sed -n '1,5p' docs/COLLABORATION.md`（cwd＝主）、`git pull --ff-only`／`fetch`／`tag`／`worktree add`／`status`／`log`／`rev-parse`、`promote.sh`／`cleanup-merged.sh --dry-run`／`post-status.sh`／`patrol.sh`／`pen-open.sh --status`／`supabase-lock.sh`／`pr-body-check.sh`、`gh pr merge`／`create --body-file`／`view`／`api`、`xcrun simctl shutdown`、`cat`｜`grep`／`grep -rn`／`jq`／`ls`／`wc`、`2>/dev/null` 與 `>&2`、Write memory symlink 與 `.claude/memory/` 直路徑、`.claude/patrol-state.json`、scratchpad／`/tmp`／`mktemp -d`、worktree 內 `sed -i ''`＋`git commit`＋`push`、`xcodegen`、`chmod`、`open -a Pen`、`node overflow-scan.js > design/evidence/…`（cwd＝worktree）。memory symlink 實測 `~/.claude/projects/-Users-clyeh-little-sprout/memory → /Users/clyeh/little-sprout/.claude/memory`，realpath 後仍放行。

**新安全網（1a）不誤擋 repo 外**：`GIT_OBJECT_DIRECTORY=/nonexist` 下 scratchpad Write exit 0；`GIT_OBJECT_DIRECTORY=<存在目錄>`（git quarantine 真實形狀）下 worktree 0／主 checkout 2；`GIT_CONFIG_GLOBAL`／`GIT_WORK_TREE`＋`GIT_INDEX_FILE` 下皆正確。
* **`git` 不在 PATH**（python3 仍在）：Write scratchpad → **新舊皆 exit 2**（`W0：git rev-parse 無法執行（FileNotFoundError）`）。是 LS-154 既有的 fail-closed，**非本 delta 引入**，安全網沒有加重它。
* **`CLAUDE_PROJECT_DIR` 未設**：cwd=`/tmp`／cwd=scratchpad／cwd=worktree 三種下 scratchpad 與 worktree Write 皆 exit 0（新舊相同）；cwd=worktree 寫主 checkout 仍 exit 2。`root_hint` 的 cwd 上溯**沒有**把 scratchpad 寫入誤擋。

**1b 穿透（B 組 16）**：五種前綴對主 checkout 全 deny（W2）、同形對 worktree 全 allow；`FOO="a b" LS_ALLOW_MAIN_CHECKOUT_WRITE=1 rm 主` → exit 0 且 stderr `命令位置前綴帶 LS_ALLOW_MAIN_CHECKOUT_WRITE=1`（確認是開關認到、不是掉進盲區）。**未列邊界 3 組**：`nice -n 5 rm 主`／`timeout 5 rm 主`／`sudo -u bob rm 主` → 皆 exit 0、**不 crash**，與 handoff 自承的盲區與 §7 文字一致；對照 `nice -n 5 echo hi > 主` 因重導規則仍 deny。`INLINE_SWITCH_RE` 的新 `_PREFIX_VAL` 我另做回溯壓力測試（12 KB 賦值鏈、5 KB 未閉合引號、20 KB 無空白）→ 最慢 1.03 ms，**無回溯爆炸**。

**1c（C 組 12＋fixture 10）**：`worktrees/NEW/x`／`NEW/docs/x.md`／`NEW/a/b/c/deep.txt` allow（舊版 deny，正是 LS-154 收尾實測的誤擋）；`worktrees/NEW`（一層）／`worktrees/x.txt`／`worktrees/`（含尾斜線）／`mkdir -p worktrees/NEW`／`rm -rf worktrees` 皆 deny；`worktrees/../CLAUDE.md` 與 `worktrees/NEW/../../../CLAUDE.md` **deny**（`realpath` 先正規化，穿越無效）；`git worktree add NEW && cd NEW && git merge`（同一條命令）allow 並 stderr 註記。**symlink：`worktrees/XLINK -> <root>` 存在時 `lexists` 為真 → 不走旁路，realpath 折回主 checkout → deny**（`XLINK/CLAUDE.md` 與 `XLINK/docs/new.md` 皆 exit 2）——這是正確的，`lexists`（而非 `exists`）是對的選擇。既有 worktree `LS-1`／`LS-157` 正常放行。

### B. 替代 mutation M4a／M4b／M5 的認可（票文 1a 的 mutation 描述）

**認可 handoff 的替代，且我獨立驗過等價性。** 票文原本要求「換回五個具名 → 負樣本變綠」；我實跑確認**該斷言在有安全網時確實不成立**——五個具名的 mutant 下主 checkout Write 仍 deny，只是理由從 W1 退成 W0。handoff 改成 M4a（同環境 worktree Write 變紅）＋M4b（主 checkout deny 退成 W0）＋M5（拿掉安全網 → 壞 `.git` 根下 Write 變綠），**在語意上比票文原案更強**：原案只驗「有沒有擋」，替代案把「哪個機制在擋」也釘住了。

我把 mutation 施加在**原始碼**上（不是 shipped 測試裡那份複本），跑 shipped 全套看是否變紅：

| mutation | shipped 自測 | 被哪些樣本殺掉 |
|---|---|---|
| `startswith("GIT_")` → 五個具名 | 紅 | G4b／G5／M4 錨點 |
| 安全網 `raise` → `pass` | 紅 | X1／X1b／X3／M4a／M4b／M5 |
| `_under_root` 恆假 | 紅 | X1／X1b／X3／M4a／M4b |
| `E.ASSIGN_RE.match` 回到 `not hq and …` | 紅 | N31／N32／N36／S9 |
| 移除 `-` 旗標穿透 | 紅 | N33／N36 |
| `TRANSPARENT` 回到 engine 詞表（拿掉 `nice`） | 紅 | N34 |
| 移除 1c 旁路 | 紅 | P30／P30b／P31／P32 |
| 1c 層數 4→3（放寬） | 紅 | N6／N37／N38 |
| 1c 拿掉 `lexists`（永遠旁路） | 紅 | M3 |
| 1c 忽略 `as_dir`（W3 也要 4 層） | 紅 | P30／P30b |
| `INLINE_SWITCH_RE` 回到 `\S*` | 紅 | 55 個樣本 |
| **`root_hint` 忽略 `CLAUDE_PROJECT_DIR`（只走 cwd 上溯）** | **綠（存活）** | — → **N1** |

### C. 衝突標記 gate

* **全 repo 掃描我自己重跑**（609 個 tracked 檔逐行比對三種標記）→ **0 命中**，handoff 的「不做 fenced-block／路徑排除」前提屬實。所有「整行只有 `=`」的命中都在二進位圖檔且長度為 1。
* **接線位置正確**：`commit-gate.sh:36-38` 在 `tracked-ignored-check` 之後、`evidence-path-check` 之前；`set -euo pipefail` 下唯一的早退是保護分支／分支命名的 **exit 1（本來就是失敗）**，`DETACHED`（rebase／cherry-pick 進行中）只跳過命名檢查、仍會跑到本 gate。**沒有「通過但跳過」的路徑。**
* 我另跑的邊界：md 表格分隔線 `|---|`＋行中 `x ======= y` → 綠；shell 內 `"sep: ======="`／`"=======x"` → 綠；六個／八個 `=`／`--------` → 綠；二進位檔 staged → 綠；新增檔第一行就是標記（`--- /dev/null` 分支）→ 紅並正確報 `docs/newfile.md:1`；setext 恰好七個 `=` → 紅（檔頭已自承的誤擋面，repo 一律 `#` 標題）；heredoc 內文行首 `<<<<<<< ` → 紅（誤擋面，全 repo 零命中所以目前無害）。**這兩個誤擋面檔頭與 §7 都寫明了，屬誠實揭露。**
* **本 PR 自己不自打臉**：新增的 `conflict-marker-check.sh`／`.test.sh` 裡那些標記字面都不在行首，gate 對本 PR 自身 diff 是綠的（CI `rules` 綠可佐證）。

### D. sweeper／agent-tools／文件

* `dead-code-sweeper.md:4` `tools:` 加 `mcp__linear__save_comment`；`:28` 輸出段語意一致（直貼該票、標題 `## dead-code sweep — LS-<n>`、handoff 回傳 comment id、明說「`save_comment` 只用來貼報告，不改票文／狀態」）。與檔案本身「不動任何檔案／只報告」的定位不衝突。
* `agent-tools-check.sh:41-45` 規則表 `dead-code-sweeper|Bash ${LINEAR3}` 與定義同步；自測補「只缺 `save_comment`」一組（24 組），**mutation 意義成立**——我把定義的 `save_comment` 拿掉後 gate 確實紅。
* COLLABORATION 7 個 hunk／8 行逐條核對與程式碼一致：§1 `:33`（sweeper 寫回權）、§2 `:59`（即將建立的 worktree）、§4 `:111`（commit gate 列加衝突標記）／`:116`（收尾 gate sweeper 直貼）、§6 `:223`、§7 `:291`（agent tools 24 組）／`:304`（LS-154 列：全剝＋安全網／前綴穿透／即將建立的 worktree／自測計數 開關 9・git 環境變數 6・安全網 3・mutation 5）／`:305`（新增衝突標記 gate 一列）。**計數我逐項對過實跑輸出，無虛報**；`:304` 盲區欄新寫的「`sudo -u bob`／`nice -n 5`／`timeout 5` 這類旗標帶獨立參數的前綴詞（該參數被當成命令名放行）」**與我 B11–B13 的實測完全相符**，`:305` 主動列出 setext 誤擋面，誠實。

### Scope

10 檔全部可回溯到票文 1–4：guard py（1a／1b／1c）、guard test、`conflict-marker-check.sh`＋`.test.sh`＋`commit-gate.sh` 一行接線＋`ci.yml`（2）、`dead-code-sweeper.md`＋`agent-tools-check.sh`／`.test.sh`（3）、`COLLABORATION.md`（4）。**無順手改動、無無關重構**：`commit-gate.sh` 除新增 3 行外只動檔頭註解一行；`ci.yml` 只加 step 名稱一項＋run 一行（`overflow-scan`／`detect-simulator` 的名稱清單不對稱在 `4d9e44e` 就存在，非本 PR 引入）；`.claude/settings.json` **未動**（本票不需，正確）。票文「不做」的 `pretool_engine.py`／wrapper 共用化／`5cb5ddf3` 第二項皆未碰。票文第 5 項（LS-96 銷除註記）在 diff 之外、handoff 稱 orchestrator 已於 `cd29dc1e` 代辦——**我沒有查證這條**（見未審）。

---

## N1 — informational：mutation 存活——沒有樣本釘住「`root_hint` 以 `CLAUDE_PROJECT_DIR` 優先於 cwd 上溯」

`scripts/hooks/main_checkout_guard.py:145-157`（`root_hint`）

把 `p = os.environ.get("CLAUDE_PROJECT_DIR")` 換成 `p = None`（即只走 cwd 上溯）後，**shipped 116 組全綠**。原因是 X1／X2／X3／M4a／M4b 五組樣本的 `cwd` 恰好都等於（或落在）`CLAUDE_PROJECT_DIR`，兩條路徑取得同一個根。

**具體行為差（我已實測，不是推測）**：cwd＝worktree、`CLAUDE_PROJECT_DIR`＝壞 `.git` 的根、目標在該根之下 → **shipped exit 2（W0）、mutant exit 0**。這正是生產形狀（subagent 的 cwd 是 worktree、`CLAUDE_PROJECT_DIR` 是主 checkout），也就是安全網最主要的覆蓋面沒有被自測釘住：日後若有人把 `root_hint` 簡化掉，gate 會退回 R2 N1 的無聲 fail-open 而測試不會紅。

**建議**：補一組（成本 2 行）——`cwd=$fx/scratch`（或 `$wt`）、`CLAUDE_PROJECT_DIR=$fx/broken`、Write `$fx/broken/x.txt` → 必須 W0 deny。**不擋併入**；建議記入 LS-96（P3，屬自測覆蓋而非行為缺陷）。

## N2 — minor：`conflict-marker-check.sh` 的 `--diff-filter=ACM` 漏掉 `T`（type change），該類 staged 變更帶標記時 gate 報綠

`scripts/gates/conflict-marker-check.sh:22`

**具體失敗情境（已實測）**：HEAD 內 `link.txt` 是 symlink；解衝突後該路徑變成含衝突標記的一般檔案並 `git add` → `git diff --cached --name-status` 為 **`T link.txt`** → 被 `--diff-filter=ACM` 濾掉 → gate 印 **`✓ conflict-marker gate：staged 新增行無衝突標記`、exit 0**。拿掉 filter 的同一份 diff 明確含 `+a`／`+=======`／`+b` 三行。symlink↔一般檔的 merge 衝突是 git 會產生 `T` 的真實途徑。

**為何是 minor 而非 major**：本 repo **tracked symlink 數為 0**（`git ls-files -s` 無 `120000`），此路目前不可達；且 gate 是新增的防護、漏一類不會讓現況比修前更糟。

**建議修法**：`--diff-filter=ACM` → `--diff-filter=ACMT`（單字元）。順帶確認過 `D` 加進來也安全（純刪除只有 `+++ /dev/null`，不匹配 `/^\+\+\+ b\//`，且 `++ /dev/null` 不是標記），所以直接拿掉 filter 也可以。自測補一組 symlink→file 的 `T`。**不擋併入**。

## N3 — informational：awk 的檔名／行號可被 diff 內容行「偽造」，命中會報到錯的檔案與行號（偵測本身仍會觸發）

`scripts/gates/conflict-marker-check.sh:27-33`

**具體情境（已實測）**：HEAD 內某行字面是 `-- a/fake.md`，本次 staged 把它刪掉、同一 hunk 新增一行 `++ b/spoof.md`。`-U0` 的 diff 中它們分別渲染成 `--- a/fake.md`（設 `hdr=1`）與 `+++ b/spoof.md`（被當成檔頭吃掉、`f` 改成 `spoof.md`，且 `ln` 不重設）。之後同檔的真實命中被印成 **`spoof.md:2: =======`**，實際位置是 `docs/seed.md:3`。

**不是漏擋**：exit 仍是 1、gate 仍紅；只是訊息指錯檔案／行號，人照著去清會找不到。觸發需要「刪除行以 `-- a/` 開頭」緊接「新增行以 `++ b/` 開頭」，極為刻意。

**建議修法**（若要收）：把檔頭辨識改成只在 `diff --git ` 之後的兩行內生效（追一個 `in_header` 旗標），或直接改用 `git diff --cached -U0 --src-prefix=…` 之外的 per-file 迴圈（`--name-only` 逐檔跑 diff）。**成本不低、收益很小，我的建議是不修、只在檔頭補一句已知限制。**

## N4 — informational（nit）：檔名含空白時，git 會在 `+++ b/…` 檔頭尾端加 TAB，該 TAB 被 `substr($0, 7)` 一起吃進輸出

`scripts/gates/conflict-marker-check.sh:29`

實測 `docs/with space.md` 命中時輸出為 `docs/with space.md<TAB>:2: =======`。純顯示問題，檔名仍可辨識。本 repo 有 `LittleSprout/Preview Content/`（含空白）在版控內，所以並非不可達。修法：`sub(/\t$/, "", f)`（一行）。**不擋併入。**

## N5 — informational：安全網把「repo 根之下、`.git` 壞掉」從無聲放行改成 W0 deny，是新的誤擋類別（本票**刻意**要的方向）

`scripts/hooks/main_checkout_guard.py:213-217`

**實測**：`<root>/.claude/worktrees/LS-1/.git` 指向已不存在的 metadata（殘留 worktree 目錄）時，Write `<root>/.claude/worktrees/LS-1/docs/x.md` → **新版 exit 2（W0，訊息含 rev-parse stderr 首行）、舊版 exit 0**。也就是在該壞狀態下，那個 worktree 的 agent 每次寫入都會被擋。

**為何不算問題**：這正是票文 1a 要的 fail-closed（「rev-parse 失敗 → deny 並印原因」），且前置條件本身已是壞狀態；訊息「落在 repo 根 … 之下，但 git rev-parse 判定不在 repo 內（fatal: not a git repository: …）——環境／.git 異常時不放行」具診斷性；逃生門在此路徑上**仍然有效**（環境變數與 inline 開關都在 `Guard` 執行前判定，我確認過程式順序）。唯一可挑的是 **W0 訊息不像 W1–W5 那樣附上逃生門用法**，但那是 LS-154 起所有 W0 訊息的既有風格，非本 delta 引入。列此僅為誤擋面書面完整，**不建議改**。

**另記一則（非缺陷）**：1c 旁路使 `<root>/.claude/worktrees/<任何尚不存在的名字>/…` 下的任意寫入（含 `rm -rf`）都放行。`.gitignore:16` 已 ignore `.claude/worktrees/`，所以影響僅止於磁碟雜物、不會污染版控或 promote 流程，與票文換取的誤擋修復相比划算。

---

### 四維度

* **Race condition**：單次同步 hook 判定、無並發、`_CACHE` 是行程內 dict。新增的 `_ROOT_HINT` 是模組層 global，但只在 `main()` 開頭設一次、單次行程內不再變動，無競態。conflict-marker gate 在 pre-commit 讀 `git diff --cached`：各 worktree 有自己的 index（`.git/worktrees/<n>/index`），跨 worktree 平行 commit 不共用，無 index 競態。**無可審之處。**
* **運算效能**：新舊交錯各 15 次中位數 **49.5 ms vs 49.5 ms**（min 48.0/48.5、max 55.4/53.4），無可測差異。`root_hint` 在 `CLAUDE_PROJECT_DIR` 有值時只做一次 `realpath`；`hits()` 新增的 `lexists` 是每個候選路徑一次的檔案系統查詢；`_clean_env()` 仍受 `repo_dirs` 的 `_CACHE` 保護、非 per-token。`INLINE_SWITCH_RE` 的新 regex 無回溯爆炸（壓力測試 ≤1.03 ms）。**可接受。**
* **平行優化**：不適用（單一 rev-parse 子行程＋快取；gate 是一次性 awk）。
* **Scope**：見上，乾淨。

### 未審／限制（誠實聲明）

* **未實跑「hook 真正載入後」的端到端行為**——PR 未併 main，`.claude/settings.json` 指向主 checkout 的 `scripts/hooks/`，我的驗證一律是直接餵 hook JSON 的等價驗證（但用真實路徑、並與 `4d9e44e` 版本並排對照）。票文驗收第 5 條（併 main → `git pull` → 實跑 `GIT_OBJECT_DIRECTORY` 探針）**仍待 orchestrator 完成**。
* **票文第 5 項「LS-96 三則銷除註記」我沒有查證**——不在 diff 內，handoff 稱 orchestrator 已於 `cd29dc1e` 代辦。請 orchestrator 自行對帳。
* LS-154 R1／R2 已審面（W1–W5 全表、121／57 組探針、settings 接線）本輪未重審，只跑上述 87 組回歸探針＋shipped 全套確認未退化。
* 未審 `pretool_engine.py` 本體（本 PR 未動；`pretool.test.sh` 139 綠確認未受影響）。
* 未在 CI 上重跑，沿用 run 33831611975 的四綠（已用 `gh pr checks` 核對）。
* 探針腳本與原始輸出：scratchpad `LS-157-probe.sh`／`LS-157-probe-out.txt`（87 組）、`LS-157-fixture.sh`（symlink／衝突標記邊界）、`LS-157-e2e.sh`（真實衝突端到端）、`LS-157-perf.sh`。

### 建議處置

N1／N2／N3／N4 皆非併入阻礙。若要順手修，**只有 N2 的 `ACM` → `ACMT` 一個字元值得在併入前做**（gate 的漏擋面，成本近零）；其餘三項建議一併記入 LS-96 待辦池（N1 P3、N3 不修只補註、N4 P3）。

*merge-reviewer 2026-09-04 11:19 CST*


### 2026-09-04T03:25:53.636Z · Chih-Lin Yeh · `3a364be8`

orchestrator 2026-09-04 11:27：merge-review R1 `24ec4983` **APPROVE**（新舊 hook 並排 87 組真實路徑探針 0 退化、R1 誤擋面抽 40 組全 allow、12 支 mutation 殺 11、替代 M4a／M4b／M5 認可、衝突標記 gate 端到端實跑；N2 minor `--diff-filter=ACM` 漏 `T`（本 repo 不可達）與 N1 informational 已入池 LS-96 `88571948`；status success 已貼 `5e14aae`）→ PR #262 併 main **`2621443`** → 主 checkout `git pull` → hook 重載後 Write 主 checkout probe 仍被擋 ✓、`conflict-marker-check.sh` 與 sweeper `save_comment` 到位。back-merge 走本地合併版 `hotfix/LS-157-backmerge-development`（ci.yml step 名聯集 28 名，marker 0、yaml OK，新 gate 在 commit gate 內實跑通過）→ push 中 → PR → 併 development → sweeper（可直貼）／retro → Done。

### 2026-09-04T03:40:18.246Z · Chih-Lin Yeh · `a760eb58`

orchestrator 2026-09-04 11:39：back-merge PR #263 四綠＋status → 併 development **`b454ed9`**（main ⊂ development 恢復）。派 sweeper（本票起可直貼 comment——第一次實測新白名單）→ retro → Done；back-merge worktree 滿 10 分鐘後 cleanup。

### 2026-09-04T03:44:23.985Z · Chih-Lin Yeh · `1d922ae6`

## dead-code sweep — LS-157

範圍：`git diff 4d9e44e..5e14aae`（PR #262 全部，10 檔 +369/−32，main `2621443`）。工作於主 checkout 唯讀（`git diff`／`git show`／grep／Read），未寫入任何檔案。merge-review R1 `24ec4983` 的 N1–N5 已入池 `88571948`，本報告不重報。

### Findings

**未發現本 feature 引入的死碼。** 逐項核對如下：

1. **`scripts/hooks/main_checkout_guard.py` 新增符號皆有呼叫點**：
   - `TRANSPARENT`（:104）→ 用於 `find_cmd`（:298）
   - `WORKTREES_REL`（:105）→ 用於 `hits()`（:422）
   - `_PREFIX_VAL`（:126）→ 用於 `INLINE_SWITCH_RE`（:128）
   - `_ROOT_HINT`／`root_hint()`／`_under_root()`／`_under_root_msg()`（:140-165）→ 皆在 `repo_dirs()`（:213-217）與 `main()`（:577-578）被呼叫
   - 舊 `GIT_ENV_POLLUTERS` 具名清單已隨 `_clean_env()` 改寫**乾淨移除**（`git grep -n GIT_ENV_POLLUTERS` 全 repo 零命中，僅一份不相干的舊 worktree 快照 `.claude/worktrees/qa-test/...`——非本 diff 範圍、worktree 已 gitignore，不算殘留）。
   - `hits(abspath, as_dir=False)` 的新參數在兩個呼叫點都正確傳入（`as_dir=True` 於 W3 git 子命令分支 :506）。

2. **`conflict-marker-check.sh`**：awk 內 `hdr`／`f`／`ln` 三個狀態變數全部在同一段被讀寫，無孤兒變數；`commit-gate.sh` 只新增一次呼叫（:36-38，接在 `tracked-ignored-check` 之後、`evidence-path-check` 之前），未見重複呼叫或死分支。`conflict-marker-check.test.sh` 13 組 fixture（①乾淨、②三種標記+行號、③形似非標記、④HEAD既有、⑤未staged、⑥純刪除、⑦兩檔並列、⑧改名+修改、⑨非repo）逐一對應腳本的每個分支，無未使用 fixture。

3. **`agent-tools-check.sh` 規則表**：6 個 agent 條目（`merge-reviewer`／`qa`／`dead-code-sweeper`／`ui-designer`／`visual-reviewer`／`ios-dev`）在 `.claude/agents/` 下皆有對應檔案（逐一 `test -f` 驗證通過）；`LINEAR3` 三支工具名與 `dead-code-sweeper.md` frontmatter 實際 `tools:` 一致。

4. **`dead-code-sweeper.md` 輸出段**：新句「LS-157 起由 sweeper 自貼，不再由 orchestrator 代貼」是對舊行為的明確交代，未見殘留矛盾句（無「由 orchestrator 代貼」等舊描述殘留）。

5. **`ci.yml` step 名稱清單 vs run 區塊**：本 PR 新增的 `conflict-marker-check` 在 step name（:164）與 `run:` 區塊（:179-180，`bash scripts/gates/conflict-marker-check.test.sh || rc=1`）**一一對應、位置皆緊接 `merge-conflict-check` 之後**，無「名稱有、run 沒跑」或「run 有、名稱沒列」的問題。

6. **`docs/COLLABORATION.md` 本輪新增段落**：§1 `:33`、§2 `:59`、§4 `:111`／`:116`、§6 `:223`、§7 `:291`／`:304`／`:305` 引用的腳本名（`conflict-marker-check.sh`／`main-checkout-guard.sh`／`main_checkout_guard.py`）、旗標（`LS_ALLOW_MAIN_CHECKOUT_WRITE`）、環境變數（`CLAUDE_PROJECT_DIR`／`GIT_OBJECT_DIRECTORY`）與程式碼現狀一致；agent 清單（6 個）與 `agent-tools-check.sh` 規則表同步。

### 既有死碼觀察（非本 feature 引入，僅供參考）

- `.github/workflows/ci.yml:164` 的「Gate 腳本自測」step 名稱清單（27 項）與 `run:` 區塊實際執行的 `.test.sh`／`.test.js`（28 項）不對稱——`detect-simulator.test.sh`（run :186）未列在 step 名稱裡。用 `git show 4d9e44e:.github/workflows/ci.yml` 核對，此不對稱在 LS-157 開工前即存在，非本 PR 引入；merge-review R1 已於 comment `24ec4983` 主動點出「`overflow-scan`／`detect-simulator` 的名稱清單不對稱在 `4d9e44e` 就存在，非本 PR 引入」。此為既有觀察，不計入本次 finding。

### 建議處置

無 finding 需開 fix 票。既有觀察（ci.yml step 名稱清單不對稱）建議記入 LS-96 待辦池（P3／純命名完整性，不影響 gate 是否執行，僅影響 PR checks 頁面可讀性），或留給 orchestrator 判斷是否值得開票。

### 搜尋方法與盲區聲明

- 方法：`git diff 4d9e44e..5e14aae` 逐檔通讀＋`git grep`／`grep -n` 交叉比對符號呼叫點；`git show <base>:<path>` 比對 ci.yml 基準版本以判定 asymmetry 是否為本 PR 引入；`test -f` 驗證 agent 檔案存在性；未執行任何測試腳本（純靜態比對，遵照工作基準指示）。
- 盲區（誠實聲明）：純文字 grep 抓不到執行期動態行為（例如 hook 是否真的被 Claude Code runtime 載入、MCP 工具名是否真的存在於 server 端——這些盲區 merge-review comment `24ec4983` 已列且非本次巡檢職責）；未重跑 `main-checkout-guard.test.sh`／`conflict-marker-check.test.sh` 等 shipped 自測（merge-reviewer 已在 worktree 內實跑並回報 116✓/13組全過，本次不重複驗證行為正確性，只驗證「有無死碼」）；反射／字串拼接／Objective-C runtime 類動態引用本 PR 不涉及 Swift 範疇，不適用。

*dead-code-sweeper 2026-09-04 11:43 CST*

### 2026-09-04T03:45:17.191Z · Chih-Lin Yeh · `9c1084fb`

## 收尾儀式（orchestrator，2026-09-04 11:44）

**dead-code sweep**：sweeper 首次直貼 `1d922ae6`（新白名單生效）——**未發現本 feature 引入的死碼**（新增 Python 符號皆有呼叫點、舊 `GIT_ENV_POLLUTERS` 乾淨移除、衝突標記 gate 與 13 fixture 無孤兒、agent-tools 規則表 6 條皆對到檔、sweeper 定義無矛盾句、ci.yml 名單↔run 對應）。既有觀察：ci.yml「Gate 腳本自測」名單 27 vs run 28（`detect-simulator.test.sh` 跑了沒列名，早於本票）→ 入池。

**retro（lesson learning）**
- 做對的：XS 同批升票一小時內從開票到併 main，reviewer 新舊 hook 並排 87 組探針證明零退化；替代 mutation 比票文原案更強且被認可；衝突標記 gate 在自己的 back-merge 流程中就實跑過一次。
- harness：本票由 LS-154 收尾實測直接衍生（同 session 內 hook 上線→踩坑→修），前饋反饋閉環一天內完成。
- 流程：back-merge 再次撞 ci.yml step 名稱清單（第二次），聯集解法已成慣例——候選機械化：step 名稱改由 run 區塊自動產生或乾脆不列名（入池）。
- 清理：LS-157 worktree、`LS-157-backmerge` worktree／本機分支皆已 `cleanup-merged.sh --apply`；遠端分支由 GitHub 自動刪。
- 待驗（票文驗收第 5 條）：`GIT_OBJECT_DIRECTORY` 探針需 hook 行程環境帶該變數，orchestrator 的 Bash 工具無法注入——以 reviewer JSON 探針（新 hook exit 2 W1）與 hook 重載後 Write probe 被擋為據。

**狀態 → Done**（main `2621443`、development `b454ed9`）。

