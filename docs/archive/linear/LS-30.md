# LS-30 Harness：little-sprout-brand 專案 skill（設計語言定案後）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-22T16:00:10.509Z |
| 開始 | 2026-08-25T06:10:14.772Z |
| 完成 | 2026-08-25T07:15:44.921Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-30/harnesslittle-sprout-brand-專案-skill設計語言定案後 |
| 關係 | related→LS-90, related→LS-33, related→LS-72, related→LS-37, related→LS-68, blocks→LS-67, related→LS-70, related→LS-44, related→LS-46, related→LS-38, related→LS-17, related←LS-198, related←LS-187, related←LS-89, related←LS-84, related←LS-88, related←LS-87, related←LS-86, related←LS-81, related←LS-57, related←LS-77 |

## 描述

M1 設計語言通過三輪迭代＋使用者核可後，用 skill-creator 把定案內容做成專案 skill：暖色 tokens（含實測對比值）、相簿紙母題（紙紋＋photo corner）、構圖規則（一畫面一 accent、字級跳階、間距節奏）、長輩硬約束、slop 禁例（十條清單的專案化版本）。ui-designer 與 visual-reviewer 定義同步改為必載此 skill——品質從「靠 review 撈」變成「起點就對」。

依賴：[LS-17](https://linear.app/little-sprout-app/issue/LS-17/storysign-in-with-appleemail-otp-登入)/18 design gate 完成（末輪 APPROVE＋使用者核可）。

## 定案後的來源（2026-08-25，[LS-46](https://linear.app/little-sprout-app/issue/LS-46/設計定稿合併c-基準使用者指定調整-designlittlesproutpen) Done）

* Tokens 板（`design/littlesprout.pen`）：色票（含深色錨點 #E3A9C4/#FCA4B5）、字級跳階、間距 8/16/24/44、`$print-paper`/`$print-ink`/`$print-ink-secondary`（不掛 theme）、`$print-edge`。
* 字標：蝠筆四字等大 B 版式（標題列 190pt）；「LITTLE SPROUT」小字只在歡迎頁相片白邊；深色字標＋tagline 坐紙條。
* 母題：沖印品白邊 8/8/8/8、四角角托壓紙緣 5pt、相片出血；一畫面一顆實心主鈕；44pt 章節斷點（碼輸入家族豁免）。
* 長輩硬約束：對比 ≥4.5（內文目標 7:1）、≥44pt 點擊、Dynamic Type/AX3 板必備、a11y metadata。
* Slop 禁例：[LS-38](https://linear.app/little-sprout-app/issue/LS-38/實驗第二輪軌-c-pen-軌-d-canvas-粉色調漸層各-5-輪對抗)/[LS-46](https://linear.app/little-sprout-app/issue/LS-46/設計定稿合併c-基準使用者指定調整-designlittlesproutpen) 11 輪審查累積的專案版清單（濟鏡式重皮、紫藍漸層、假截斷、倒推餘數、反轉的紙上墨…）。
* 進場條件 12 項：[LS-46](https://linear.app/little-sprout-app/issue/LS-46/設計定稿合併c-基準使用者指定調整-designlittlesproutpen) comment `ba6dbabb`。
* 不包含 Pencil 工具限制（那是 [LS-44](https://linear.app/little-sprout-app/issue/LS-44/harnessui-designervisual-reviewer-定義更新批次pencil-缺陷清單4收工程序內容證明) 的 agent 定義）。

## 順序

在 [LS-67](https://linear.app/little-sprout-app/issue/LS-67/設計多寶貝-profile-三畫面建檔管理切換-ui-designervisual-reviewer-3-輪) 開工前落地（[LS-67](https://linear.app/little-sprout-app/issue/LS-67/設計多寶貝-profile-三畫面建檔管理切換-ui-designervisual-reviewer-3-輪) blockedBy 本票）；harness lane 現由 [LS-70](https://linear.app/little-sprout-app/issue/LS-70/harness並行後端票搶同一個本機-supabase-容器db-reset-互踩-隔離或序列化) 佔位，完成即接。skill-creator 產出 `.claude/skills/little-sprout-brand/`；ui-designer/visual-reviewer 定義改為必載。

## 留言（5）

### 2026-08-25T06:35:43.171Z · Chih-Lin Yeh · `bb05d56c`

## Handoff（orchestrator 代貼——實作 agent 在 push-gate 完成後失聯，分支與 PR body 已備妥）

**PR**：https://github.com/CLYEH/little-sprout/pull/136（`hotfix/LS-30-brand-skill` → main，head `adf7588`，3 commits：skill 本體 → agent 定義必載 → gate＋CI＋規約）。

**已完成**
- `.claude/skills/little-sprout-brand/SKILL.md`＋`references/`（tokens／wordmark-brand／motifs／elder-constraints／slop-forbidden 20 條／entry-conditions 12 項）。缺值標「待補（需 Pen）」：`fs-code` AX3（Tokens 板 72 vs tokens.json 76）、六個 token 的板上對比欄。
- `ui-designer.md`／`visual-reviewer.md`：開工必載 `little-sprout-brand`，載入失敗不得靜默；ui-designer 色彩定案以 skill 為準；visual-reviewer 逐條對照專案版 slop 禁例。
- 反饋 gate `scripts/gates/brand-skill-check.sh`（SKILL.md 存在／frontmatter／references 齊／兩份 agent 定義含字樣）＋`.test.sh` 19 組；CI `rules` job 兩個 step；COLLABORATION §1 一句＋§7 一列。

**已驗證（依 PR body）**：自測 19 組綠；push-gate 全過。**orchestrator 未複驗**——交 merge-reviewer 親跑。

**未完成**：reviewer informational 待審後處置；「待補（需 Pen）」值由下一張開 Pen 的設計票（LS-81 或 LS-67）順手補進 `tokens.md`。

**風險**：skill 與 Tokens 板之間無機械對帳（§7 盲區已寫）；agent 失聯屬第四次「等背景 push 沒醒」型停滯——LS-88 H9（Stop hook 巡檢）與巡檢催醒是既有對策，本次由 orchestrator 接手收尾。

### 2026-08-25T06:54:40.697Z · Chih-Lin Yeh · `c4e98d43`

## Merge gate review — PR #136（LS-30 little-sprout-brand skill）

**Verdict：ITERATE**（0 blocker／**1 major**／5 minor／8 informational）

Base `origin/main`（`c54a80a`，已含 #133）… Head `hotfix/LS-30-brand-skill`（`adf7588`，merge-base `3aa1dfa`）。13 檔、+690/−4。

**審查方式**：`git diff 3aa1dfa...adf7588` 逐檔＋**對稿實測**——不開 Pencil MCP（LS-81 佔用），改用 `git show origin/main:design/littlesprout.pen` 做**唯讀 python 結構查詢**（只取 `variables`／text `content`／id 集合；此法即 ui-designer.md 明文豁免的「落地檢查腳本用 python 只讀結構統計」，也是 `design-landing-check.sh` 在 CI 做的事）。另以 WCAG 2.1 相對亮度公式獨立複算全部對比欄、以 CIE Lab 複算 accent／danger 的 L*C*h。

---

### 四維結論

| 維度 | 結論 |
|---|---|
| Race condition | **N/A** — diff 內 0 行 Swift／SQL。無 actor／Task／@MainActor／Sendable、無上傳佇列、無 Supabase 寫入、無快取。 |
| 運算效能 | **N/A** — 無 RLS policy、無查詢、無分頁、無圖片解碼／壓縮。唯一可執行物是 bash gate：讀 3 個檔＋1 次 `grep -oE`，CI 上 <50ms。 |
| 平行優化 | **N/A** — 無可平行化路徑；gate 的三項檢查彼此有輸出順序語意（一次列全部違規），序列化是正確選擇。 |
| Scope | **無 finding** — 13 檔恰好＝skill 7 檔＋2 份 agent 定義＋gate 2 檔＋ci.yml＋COLLABORATION。兩份 agent 定義是純新增（`ui-designer.md` 一行工作方式＋一行 handoff 欄改寫、`visual-reviewer.md` 一行工作方式＋一行輸出欄），**無刪除既有規則、無順手重構**。`merge-tree --write-tree` 對 `origin/main`／`origin/development`／`origin/test` 三方皆 CLEAN。3 條 commit message 逐條過 `commit-msg-gate.sh`；PR body 過 `pr-body-check.sh`。 |

---

### 內容正確性：實測結果（這是本票的重點，先講通過的部分）

**① `references/tokens.md` vs `.pen` 的 72 個變數——逐項比對，零差異。**
所有色票（light／dark 兩欄）、`fs-*`（default／ax3）、`sp-*`、`ctl-pad-*`、`radius-*`、`icon-*`、`corner-out` 5、`print-edge`／`print-edge-bottom` 8、`kbd-h` 336、`kbd-accessory` 60、`font` Noto Sans TC／`font-num` Courier Prime **全部逐值吻合**。另確認：
- `print-ink`／`print-ink-secondary`／`on-photo` 在 `.pen` 裡確實是**無 theme 陣列的單值**——skill 的第 2 條核心規則有機械證據，不是宣稱。
- 「已除役」清單正確：`accent-text`／`mount-edge`／`scrim-ink`／`scrim-fade`／`inset-mount`／`sp-inline` **全部不在 variables 內**。
- `#E3A9C4` **不是變數**（只是 Color Table 一列）——skill 寫「無生產角色、對照錨點」與 LS-46 R2 覆核原文（「D 軌原本就是把它放在同一格位試色…兩條規則使它沒有合法空位」）逐意吻合。

**② 對比欄 19 對——18 對逐位重現。**
`print-ink` 14.92／12.55、`print-ink-secondary` 9.66／8.13、`text-primary` 12.21／15.31、`text-secondary` 7.91／9.41、`on-accent` 7.30／9.99、surface-2 上 11.02／11.45＋7.14／7.03、`border` 1.86／2.01、`control-line` 3.60／4.72、`danger` 8.46／9.85、`success` 7.44／9.54、`accent-soft` 13.41／12.02、surface vs bg 1.22／1.17、bg-lit vs bg 1.13／1.07、google 16.48／14.47＋4.53／5.83、apple 21.00 — **全部逐位吻合**（唯一例外見 i1）。Lab 值也對：accent 淺 L\*32.85／C\*46.84／h 6.36°、深 76.43／35.02／7.94°；danger↔accent 色相分離 33.9／30.5（板寫 34.0／30.6，白點實作差異）；「淺色兩者 C\* 相等」實測 46.84 vs 46.87 ✓、「深色不宣稱相等」35.02 vs 40.05 ✓。中性階「每階 B ≥ G、色相 334–356°」16 個值全數成立。Lab Imprint 暗角 3.47:1 我用 α=0.36 合成複算得 3.47 ✓。

**③ `entry-conditions.md` ①–⑫ 與 `ba6dbabb` 逐字 diff：12/12 完全相同**（我用 SequenceMatcher 跑，不是目視）。底下「值得保留的清單」亦忠實。

**④ 「待補（需 Pen）」是誠實的，而且 `fs-code` 那一條是真的抓到東西。**
我原本懷疑「Tokens 板 72」是猜的（LS-46 comment 0 命中、repo 只有 76）——**實測推翻我的懷疑**：`.pen` 節點 `F7Swz6`（`Type Table > r5 > c`）內容確實是 `'72'`，而變數 `$fs-code` size=ax3 ＝ **76**；板上註記 `h7EnT` ④ 更寫明「六位 AX3 76 會溢出 8.6，**已降 72**」。所以不一致為真、方向也查明了（**變數才是落後的一方**）。這條是本票的加分項。
其餘 6 個 token 的對比欄標「待補」也屬實——板上 Color Table 該欄確實空著。

**⑤ 其他抽查全數屬實**：白邊 `["$print-edge",…,"$print-edge-bottom",…]`＝8/8/8/8 且下緣視覺 32 的組成（8＋7＋17）✓；`corner-out` 5＋LS-81 對帳中的標注 ✓；44pt 章節斷點與 **7 張具名豁免**（06/06b/06c/06d ＋ 03/03b/Stress-03）與板上 `JkWvW`／`t4ZGwX` **逐字一致**，實建 18/25 ✓；38pt Legal／Status 槽 ✓；錯誤文法四層與 `k2Mw4` 原文四層逐層對得上（含「只有 03b 時限句用 `$text-primary`」這個具名例外）✓；`S1FuR` 眉標退場規則 ✓;字標 190×90.6／iPad **247×118**（1.3×、長寬比 2.093）——`tokens.json` 與 LS-46 R9-E 覆核雙重佐證 ✓。
**節點 id 衛生**：skill 引用的 22 個 id 在現行 HEAD **全部存在**；查無的 3 個（`DW9PE`／`G3kSy`／`bPhGg`）都是刻意引用的歷史節點，上下文正確。
`slop-forbidden.md` **20 列、每列 5 欄皆非空**——「為什麼在本專案是錯的」與「判準／處方」逐條齊備，非空話。

**⑥ SKILL.md frontmatter**：`name: little-sprout-brand` ＝目錄名 ✓；`description` 329 字元（上限 1024）、含「新畫面／改版面／design token／mockup／.pen 視覺審查／SwiftUI 對照」等觸發詞，可觸發性足 ✓；本體 60 行 3.8 KB，細節確實都在 references ✓。

**⑦ gate 親跑**：`/bin/bash` **3.2.57** 下 `brand-skill-check.test.sh` 全綠（20 個 ✓，rc=0）。負向案例我逐一確認**真的紅**：frontmatter 未閉合、第一行不是 `---`、缺 name、name≠目錄名、description 空白／缺、name/description 寫在本文、references 缺檔、任一 agent 少字樣、agent 檔缺、三處違規一次列全。exit code 約定（0/1/2）與 fail-closed 路徑實測正確。ci.yml 兩個 step 位置正確（自測併進既有「Gate 腳本自測」step 並收各自 rc、gate 本體獨立 step），另抽跑 3 支既有 gate 自測未受影響。

---

## Findings

### MAJOR

**M1 — `references/motifs.md:67`（連帶 `references/tokens.md:82`）：邀請碼段把「已上線且有測試的後端契約」寫反了**

skill 寫：
> 6 位英數混合（**對齊後端 40-bit**；字母集排除易混字元 Q/O/0/I/L/1 的方案因 29.4 bit 不足被擱置）；輸入 **3+3 分組**

三處與事實相反：

1. **算術不成立**：6 位 × 36 符號 ＝ 31.0 bit，不可能「對齊 40-bit」。設計稿自己的註記 `h7EnT` 就寫得很白：「⚠ 若 LS-33 需維持 40 bit，**6 位英數不可能達成**（需 ~100 個符號），要嘛加到 8 位…**設計端立場：寧可 7-8 位也不要讓長輩把 O 唸成零**」。
2. **後端實際是 8 位、32 字元**：`supabase/migrations/20260823010000_join_approval.sql:233,290-295` — `c_alphabet = '23456789ABCDEFGHJKLMNPQRSTUVWXYZ'`（32 字元），迴圈 `for k in 1..8`，8 × 5 bit ＝ 40 bit（該檔 302 行註解自述）。測試 `supabase/tests/80_join_approval.sql:1386` 斷言 `v_code !~ '^[23456789ABCDEFGHJKLMNPQRSTUVWXYZ]{8}$'` ——**8 位是已合併、已被測試釘死的契約**。
3. **「排除易混字元被擱置」也反了**：後端採用的正是排除 0/1/I/O 的 Crockford 式 32 字元集；被擱置的是「30 字元 × 6 位」這個**組合**，不是排除原則。

**具體失敗情境**：LS-67 之後任何邀請碼畫面票，ui-designer 開工載入本 skill → 依「6 位、3+3」畫六格 → ios-dev 照做 → `create_invite` 回傳 8 碼（如 `K7M2FD9X`）→ 使用者第 7 個字無處可打、貼上被截斷 → **加入家庭整條流程斷**。而稿上早就備好解法，skill 卻整段沒收：`h7EnT`【R6 · 八格應變規格】已給出 4+4 分組、gap `$sp-tight` 6＋中間 `$sp-item` 16、格寬 31.63、**`fs-otp` AX3 必須由 52 降到 42（否則 31.2 > 27.6 爆格）**、**`fs-code` 由 60 降到 52**、以及「整列是單一 tap target、不得把每格做成獨立按鈕」這條前提。這正是 skill 自己第 ⑤ 節「值得保留的清單／勿倒洗澡水」該裝的東西。

**建議修法**（不需開 Pen，皆為文字）：`motifs.md:67` 改寫為——後端 `create_invite` 產 **8 位 × 32 字元**（`23456789ABCDEFGHJKLMNPQRSTUVWXYZ`，排除 0/1/I/O）＝ 40 bit（LS-33／LS-37 已上線並有測試）；稿上 06 家族現為 6 格 3+3，屬**設計早於後端定案的既有落差**，實作前必須依 `h7EnT`【R6 八格應變規格】轉 4+4（含 `fs-otp` AX3 52→42、`fs-code` 60→52、單一 tap target 前提）。`tokens.md:82` 的 `fs-code` 用途欄同步（現寫「3+3 分組」）。另建議 orchestrator 另開一張 design chore 票把 06 家族實際改成八格——但**本 PR 只要不再宣稱「對齊後端」即可**。

---

### MINOR

**m2 — `SKILL.md:8`／`SKILL.md:48`／`references/motifs.md:10`：節點數與「已知溢出 51 項」已被 LS-72 推進，但 skill 寫成現況**
SKILL.md:8 寫「2654 節點」、:48 寫「已知溢出 51 項…**自動化版面檢查以此為白名單基線**」。實測現行 `origin/main` 的 `design-landing-check.sh` ＝ **nodes 2676**；LS-72 handoff 與 QA 記錄 FLAGGED ＝ **50**（−1，LS-72 自述「不可歸因」、其 merge-review I3 標為未複驗）。
**失敗情境**：LS-68／LS-88 之後有人照 skill 寫死 `--expect-flagged 51`，第一次跑就紅，然後去「修」一個不存在的迴歸。
**建議**：51／2654 標明是 **R11 基線**，並註「LS-72 後實測 2676／FLAGGED 50（−1 不可歸因、尚未獨立複驗）」。entry-conditions ⑫ 是逐字引文、**不要動**。

**m3 — `references/elder-constraints.md:9`：1.46／1.11 的配對順序會被讀反**
現寫「（`$print-ink` 12.55、`$print-ink-secondary` 8.13 對 #E8D9D4）——綁 `$text-*` 會掉到 **1.46／1.11**:1」。前半是 primary→secondary 的順序，後半照抄過來就變成 primary=1.46、secondary=1.11。實際（LS-46 R9-A 原文＋我複算）：**`$text-primary` #F6E3E5 on #E8D9D4 ＝ 1.113（Family Caption）／`$text-secondary` #D3AEB2 ＝ 1.462（Lab Imprint）**，即 primary 1.11、secondary 1.46——**顛倒**。
`slop-forbidden.md:10` 與 `wordmark-brand.md` 寫成範圍（1.11–1.46），沒問題；只有這一行有配對暗示。
**建議**：改成「會掉到 1.11–1.46:1（primary 1.11／secondary 1.46，R9-A）」。

**m4 — `references/tokens.md:84`／`slop-forbidden.md:17`：13pt 成員資格的第三類與稿上不符**
稿上 `NHDwj` 明列三類：①法務行 ②微標籤 ③**設計註記（游標位置）**。skill 改成「①法務行 ②欄位微標籤 ③**純時間戳**」——「時間戳」在稿上**沒有任何出處**。
**失敗情境**：日記時間軸／相簿列表的「2 天前」依 skill 授權可用 13pt `$text-secondary`；但在這個 app 裡時間是主要資訊，而且 skill 自己的規則是「13pt 只給不讀也能完成任務的內容」——兩者互打，而 visual-reviewer 被要求「逐條對照」時會放行。
**建議**：回到稿上三類，或把「時間戳」升為具名新增並註明是本 skill 的擴充（誰裁決的）。

**m5 — `scripts/gates/brand-skill-check.sh:77-84`：`[ -r ]` 擋不住 0-byte reference，與腳本自己的註解不符；自測也沒有這個負向案例**
第 12 行註解寫「progressive disclosure 的指標**指到空檔**＝載入後仍拿不到內容」，但實作是 `[ ! -r "${skill_dir}/${ref}" ]`。實測：把 `references/tokens.md` 清成 0 bytes → **gate 綠**。
**失敗情境**：日後有人 `: > references/tokens.md`（或 merge 衝突解成空檔、或 LFS pointer 沒還原），CI 綠、ui-designer 載入後拿到空白 tokens、色票全靠猜——正是這支 gate 存在的理由。
**建議**：`[ ! -s ... ]`（一個字元），並在 `.test.sh` 補一組「references 為 0 bytes → 紅」。

**m6 — `scripts/gates/brand-skill-check.sh:89`：`for a in $agents` 未加引號分詞**
`agents` 是空白分隔的路徑字串。實測 repo 路徑含空白時（`/…/repo with space`）輸出三行亂掉的假路徑。**是 fail-closed（rc=1）不會假綠**，但錯誤訊息完全誤導，且自測沒有這個樣本（`mktemp -d` 不含空白）。
**建議**：改成陣列或直接兩次呼叫一個 `check_agent()` 函式。

---

### INFORMATIONAL（不擋合併）

**i1 — `references/tokens.md:37` `photo-corner` 的 2.11／2.22 換了量測基準。** 我對 `$bg` 複算是 **1.73／2.60**；2.11／2.22 只有在對 **`$surface`** 量時才成立。查稿確認：板上節點 `WEjnC`（`Color Table > Row 15`）內容就是 `'2.11／2.22'`——**skill 是忠實轉錄，不是算錯**，是板上這一列用了跟同表其他列（全部對 `bg`，我逐列驗過）不同的底。同表 §① 又寫「所有文字對比都以 bg 計」。結論不變（裝飾、<3:1、申報豁免），但建議在該格註明基準，或列入下一張動 Tokens 板的票。

**i2 — `references/tokens.md:44`／`elder-constraints.md:6` `on-photo` 的「印相罩最不利點 7.36／8.71」標籤已過期。** 同樣是忠實轉錄（板上 `sQsj1` 原文一字不差），但**同一份稿**的 `jJONK`／`pX9pq` 已宣告「印相罩 print-scrim（已除役）…R4 起全 app 再沒有任何 chrome 壓在照片上」，而 skill 自己的「已除役」段也照抄了這一句——**同一份檔案裡一個數字掛在一個宣告不存在的機制上**。我另驗了數值本身：以 `$photo-dim`（#190E12 @ α 0.18）套在「淺色 7.36 對應的最不利點」上複算得 **8.65**（板寫 8.71，差 0.7%，可由取樣色非純灰解釋）——**數字是真的，過期的是標籤**。建議改標「深色套 `$photo-dim` 後的最不利點」。（順帶一提：這正好中了本 skill slop 第 18 條「過期的 Notes 比沒有更糟」。）

**i3 — agent 接線用全檔子字串 grep，強度低於 §7 的措辭。** 實測：把 `ui-designer.md` 內容換成 `<!-- 舊版曾要求 little-sprout-brand，已移除 -->` → **gate 綠**。COLLABORATION §7 寫「必載指示被拿掉即紅」略強於實作（實作是「所有提及都被拿掉才紅」）。若要收緊，成本很低：`grep -qE '載入.*little-sprout-brand'`。

**i4 — `scripts/gates/brand-skill-check.test.sh:138` 自報「19 組樣本」，實跑輸出 20 個 ✓。** PR body 也寫 19。

**i5 — `references/wordmark-brand.md:14` 的 18.2:1／15.3:1 換了主體。** 我複算：**#000000** on `$print-paper` ＝ 18.20／15.31，而 `$print-ink` #2B141C ＝ 14.92／12.55。所以這對數字量的是**字標 PNG 的實際墨色（近純黑）**，不是 `$print-ink`——與 LS-46 R6-N6 原文一致，沒錯，但同一份 skill 裡兩組「紙上的墨對比」相差 3.3，建議加半句「（量的是 v3 蠟筆素材的最深像素，非 `$print-ink`）」。

**i6 — `references/entry-conditions.md:5` 的「不另出**淺色**字標資產（`mengya-diary-crayon-v3-dark.png` 不需要）」語意疑似有誤（該檔是深色資產）。** 但這是 `ba6dbabb` 原文、逐字收錄是對的做法；`wordmark-brand.md:14` 已用正確語意（「不另出**深色**資產」）表述同一件事。留記錄，勿為此破壞逐字性。

**i7 — 合併順序：#136 併入 main 後請立刻 back-merge main → development → test。** `rules` job 對 `[development, test, main]` 的**所有** PR 都跑，且 GitHub 用 merge commit；PR **#117**（`feature/LS-17-sign-in-ui` → development）在 back-merge 落地前重跑 checks 會紅在新 gate 上（base 端沒有 skill 檔）。這是新增檔案存在型 gate 的固有成本，不是缺陷，但需要排序。

**i8 — `references/tokens.md:82` 的 fs-code 待補措辭可再精確。** 我已查明方向：板上 Type Table 格子 ＝ 72、板上註記 `h7EnT`④ ＝「六位 AX3 76 會溢出 8.6，已降 72」、變數 `$fs-code` ax3 ＝ **76**。所以**落後的是變數**，回填動作是「把變數改成 72」而不是「去查是幾」。順帶：`h7EnT`④ 同時指出八位時 `fs-code` 要降到 52——與 M1 同一批。

---

### 已看／未看

**已看**：13 檔完整 diff；`.pen` 全部 72 個變數逐值比對；1075 個 text 節點針對 12 個關鍵字串的定位（含祖先鏈確認欄位歸屬）；skill 引用的 25 個節點 id 對現行 HEAD 的存在性；19 對對比 ＋ 4 組 Lab 值 ＋ 暗角合成 ＋ photo-dim ΔL\* 的獨立複算；`entry-conditions.md` ①–⑫ 對 `ba6dbabb` 的字元級 diff；gate 在 bash 3.2 下親跑＋5 個額外邊界探測（0-byte references／含空白路徑／註解假接線／0-byte SKILL.md／孤兒 reference）；3 條 commit message gate；PR body gate；三方 merge-tree；後端 `create_invite` 與其測試的實際契約。

**未看／未驗**：
- **LS-38 出處的數字**（icon 5 輪、軌 C 7 輪／軌 D 12 輪、ΔE 81.7／14.5、unique RGB ≥20k、grain ≥1.5、覆蓋率極差 7.63pp、筆寬比 0.886–0.973、縱拉 1.456×）——我沒讀 LS-38 的 comment，一律標為**採信**。
- **51 vs 50 的溢出重掃**：需要版面引擎，我只能引用 LS-72 的記錄（見 m2），沒有自己重跑。
- **染料池 lit() 公式的座標系**：我試著用 `tokens.json` 的角托座標反推 TL/TR 的 .494/.288，兩顆解出的 y 不一致（≈80 vs ≈150），但這**很可能是我不知道 Print Stage 的 viewport 偏移**，不是公式錯——標 PLAUSIBLE，未列 finding。
- **模擬器／視覺驗收**：本票零 Swift，N/A。

---

**結論**：skill 的工程品質很高——tokens 逐值、對比逐位、進場條件逐字，「待補（需 Pen）」的標記是誠實的而且還順手抓到一個真的變數漂移；gate 與自測是我近期看過最紮實的一支。**擋在 M1**：這份檔案的定位是「之後所有設計票的起點」，而它對一條已上線、已被測試釘死的後端契約做了方向相反的斷言，並且漏掉了稿上早就寫好的 8 位應變規格。M1 是純文字修正，改完（順手帶 m2–m6）即可 APPROVE，不需再開一輪。


### 2026-08-25T07:05:55.411Z · Chih-Lin Yeh · `612d696a`

## R2 Handoff（PR #136 `hotfix/LS-30-brand-skill` → main，head `8cfe1c7`）

**Ticket**：LS-30 第 2 輪，回應 merge-review `c4e98d43`（ITERATE：1 major／5 minor／8 informational）。接手失聯 agent；只在 worktree `.claude/worktrees/LS-30` 作業，未開 Pencil MCP。

**已完成**（4 commits，`adf7588` → `8cfe1c7`，已 push、push-gate 全過）
- `f38239f` docs(skill) M1 邀請碼 8 位對齊後端（依原派工）
- `b8cc90f` docs(skill) m2–m4（＋i1／i2／i5 skill 端加註）
- `9af6fe9` chore(harness) m5／m6 gate（＋i3／i4）
- `8cfe1c7` docs(skill) M1 改依 **LS-89 使用者裁決 6 碼**（coordinator 中途更新；history 保留裁決變更軌跡）

逐條處置：
- **M1 已修**：`references/motifs.md` 邀請碼段最終版＝**6 碼、3+3 分組、32 字元表 `23456789ABCDEFGHJKLMNPQRSTUVWXYZ`（排除 0/O/1/I）、30 bit、核准必開；後端 LS-90 對齊中（現行部署 `create_invite` 仍 8 碼／40 bit，LS-90 併入前設計稿不得改 8 格）**。`h7EnT`【R6 八格應變規格】完整收錄為「已擱置備案」（4+4、格寬 31.63、`fs-otp` AX3 52→42、`fs-code` 60→52、單一 tap target 前提、切換成本）。`tokens.md` `fs-otp`／`fs-code` 列、`elder-constraints.md`:40、`SKILL.md` 數字速查同步；原「對齊後端 40-bit／29.4 bit 被擱置」的反向敘述已刪。
  - ⚠ 給 orchestrator：LS-46 定案文字「6 碼」對現行部署 8 碼的偏差**已由使用者裁決**（LS-89→LS-90），不再需要回報；但 LS-90 併入前 `create_invite` 仍回 8 碼——邀請碼畫面票（LS-67 等）的 QA 須排在 LS-90 之後，否則會在「6 格對 8 碼」上假失敗。
- **m2 已修**：`SKILL.md`:8／:48、`motifs.md`:10 改為 LS-72 後現況 **2676 節點／FLAGGED 50**（R11 2654／51 保留為基線出處；−1 標「LS-72 自述不可歸因、未獨立複驗」），註明「基線隨最新 landing 更新，見 COLLABORATION §7 design-landing 列」，不要寫死 51。`entry-conditions.md` ⑫ 逐字引文未動。
- **m3 已修**：`elder-constraints.md`:9 → 「1.11–1.46:1（primary 1.11／secondary 1.46，R9-A）」。
- **m4 已修**：`tokens.md` fs-meta 列與 `slop-forbidden.md` #11 回到 `NHDwj` 原文三類（①法務行 ②微標籤 ③設計註記），刪無出處的「時間戳」。
- **m5 已修**：`brand-skill-check.sh` references 檢查 `[ ! -r ]` → `[ ! -s ]`，訊息「不存在或為 0 bytes」，腳本註解／規則行／COLLABORATION §7 對齊；自測補 ③b「references 0 bytes → 紅並點名」。
- **m6 已修**：`agents` 改陣列 `"${agents[@]}"` 逐檔；自測補 ⑤b 含空白路徑正向（綠）＋負向（只點名缺的 visual-reviewer.md、ui-designer.md 不被分詞誤報）。

informational（i1–i8）：
- **i1 已修（skill 端）**：`tokens.md` photo-corner 對比格加註「板上 `WEjnC` 原文；量測底 `$surface`，對 `$bg` 為 1.73／2.60，與同表其他列基準不同」。板端 → 建議**併 LS-81 順修**（同是 Tokens 板對帳），不另開票。
- **i2 已修（skill 端）**：`tokens.md` on-photo 與 `elder-constraints.md`:8 標「印相罩」標籤已過期（R4 除役）、數字為深色 `$photo-dim` 最不利點（複算 8.65）。板上 `sQsj1` → 併 LS-81 順修。
- **i3 已修**：接線判準收緊為同一行 `grep -qE '載入.*little-sprout-brand'`（只剩註解／歷史提及即紅）；自測補 ④b；COLLABORATION §7 措辭同步。兩份 agent 定義現行第 11 行皆符合。
- **i4 已修**：自測樣本數改由 `ok()` 計數（現 24 組），末行不再手寫；PR body／COLLABORATION 同步 24。
- **i5 已修**：`wordmark-brand.md`:14 加註「量的是 v3 蠟筆素材最深像素≈#000 對 `$print-paper`，非 `$print-ink` 14.92／12.55」。
- **i6 不修**：`entry-conditions.md` ① 是 `ba6dbabb` 逐字引文，保留逐字性；`wordmark-brand.md`:14 已用正確語意。
- **i7 由 orchestrator 執行**：#136 併入 main 後立刻 back-merge main → development → test（PR #117 在 back-merge 落地前重跑 checks 會紅在新 gate）。
- **i8 已修**：`tokens.md` fs-code 待補改為「落後的是變數（`$fs-code` ax3 76 vs 板上 `F7Swz6` 72，`h7EnT`④）；回填動作＝把變數改成板上值」。

**已驗證（怎麼驗）**
- `/bin/bash` 3.2.57：`brand-skill-check.test.sh` **24/24 ✓ rc=0**；`brand-skill-check.sh` 對本 repo綠；`bash -n` 兩支語法 OK。
- 4 commits 逐條過 commit-gate／commit-msg-gate；push-gate 全過（unit tests、api-contract、error-codes、migration 版本、分支票號 7 commits、與 origin/main `c54a80a` 可乾淨合併）。
- 稿內容查證同 reviewer 手法：`git show origin/main:design/littlesprout.pen` 用 python 唯讀取 `h7EnT`／`NHDwj`／`WEjnC`／`sQsj1` content 與 `fs-otp`／`fs-code` 變數（52／76）；節點數 2676 親算。未開 Pencil MCP。
- 後端契約親讀：`supabase/migrations/20260823010000_join_approval.sql`:233,293（32 字元表、`for k in 1..8`）、`supabase/tests/80_join_approval.sql`:1386（`{8}`）。
- PR body 已加 R2 段（先過 `pr-body-check.sh`，再 `gh pr edit 136 --body-file`）。
- **未等 CI**（依派工）。

**未完成／需 orchestrator**
- i7 back-merge（上）。
- 板端 i1／i2（`WEjnC` 基準註記、`sQsj1` 標籤）與 `$fs-code` ax3 變數 76→72 回填，建議併 LS-81 或下一張開 Pen 的票。
- 若 LS-89 裁決再翻（升 8 碼），只需把 motifs.md「已擱置備案」升為現行，不必重開設計輪。

**風險**
- `f38239f` 先寫 8 位、`8cfe1c7` 改 6 碼——同段兩次改寫，squash 與否由 orchestrator 決定（history 忠實記錄裁決變更）。
- LS-90 併入前後端仍回 8 碼：此期間任何邀請碼畫面 QA 失敗都不是設計 bug。
- skill 與 Tokens 板仍無機械對帳（§7 盲區未變）。

**產出位置**：worktree `.claude/worktrees/LS-30`；分支 `hotfix/LS-30-brand-skill` head `8cfe1c7`；PR https://github.com/CLYEH/little-sprout/pull/136。

### 2026-08-25T07:13:27.747Z · Chih-Lin Yeh · `139b9baa`

## Merge gate review R2 — PR #136（head `8cfe1c7`）

**Verdict：APPROVE**（0 blocker／0 major／0 minor／6 informational）

Base `origin/main` `c54a80a`；R1 head `adf7588` → R2 head `8cfe1c7`（4 commits）。R2 增量 9 檔 +70/−33；base…head 合計 13 檔 +727/−4。未開 Pencil MCP——稿內容同 R1 手法：`git show origin/main:design/littlesprout.pen` 以 python 唯讀取節點 content／variables。

---

### 核銷：R1 findings 逐條

| # | 結論 | 我怎麼驗的 |
|---|---|---|
| **M1** | **已修，且方向正確** | `motifs.md:67-69` 最終版＝6 碼／3+3／32 字元表 `23456789ABCDEFGHJKLMNPQRSTUVWXYZ`／30 bit／核准必開，與 LS-89 使用者裁決（`7683b00c`：A—後端改 6 碼、32 字元表維持、30 bit）逐項吻合。反向敘述**已清乾淨**：全 skill grep `對齊後端` 0 命中、`29.4` 0 命中；`40 bit` 僅剩 2 處，皆是「現行部署仍 8 碼／40 bit、LS-90 對齊中」的正確陳述。後端引用親驗：`origin/main` 的 `20260823010000_join_approval.sql:233`（32 字元表）、`:293`（`for k in 1..8`）、`80_join_approval.sql:1386`（`{8}`）——skill 寫的三個位置全對。LS-90 存在、In Progress、lane:backend、scope 與 skill 敘述一致。 |
| M1（八格備案） | **已收錄且轉錄忠實** | 對 `h7EnT` 原文逐項比對：格寬 44.17／31.63、`fs-otp` AX3 52→42（31.2 > 27.6；42＝25.2）、`fs-code` 八位 60→52（297.6，餘 7.4）、單一 tap target、切換成本——**五項數字與原文一致**。我另獨立算過寬度模型 `n×(0.6·fs+4)+16`：六位 76 ＝ 313.6（溢出 8.6 ✓）、六位 72 ＝ 299.2（不溢出 ✓）、八位 60 ＝ 336 ✓、八位 52 ＝ 297.6 ✓——skill 引用的每個數字都能從公式重現。 |
| **m2** | 已修 | 我用 `design-landing-check.sh` 的同一套遍歷法對 `origin/main` 的 `.pen` 親算：**nodes ＝ 2676** ✓。SKILL.md:8／:48、`motifs.md:10` 改為「R11 基線 51／LS-72 後現況 2676・FLAGGED 50（−1 未獨立複驗）／不要寫死 51」——措辭誠實。`entry-conditions.md` ⑫ 逐字引文未動 ✓（R2 diff 未含該檔）。 |
| **m3** | 已修，配對正確 | 獨立複算 WCAG：`$text-primary` #F6E3E5 on #E8D9D4 ＝ **1.113**、`$text-secondary` #D3AEB2 ＝ **1.462**。新寫法「1.11–1.46:1（primary 1.11／secondary 1.46，R9-A）」正確。 |
| **m4** | 已修，回到原文 | `NHDwj` 原文三類＝①法務行（登入即表示…）②微標籤（邀請碼／加入申請這種放在 60pt 主體上方的眉標）③設計註記（游標位置）——`tokens.md` fs-meta 列**逐字吻合**；`slop-forbidden.md:11` 同步；全 skill grep `時間戳` **0 命中**。 |
| **m5** | 已修 | `[ ! -s ]` 已上；註解／錯誤訊息／COLLABORATION §7 三處措辭同步。親跑自測 ③b（0 bytes tokens.md → 紅並點名「0 bytes」）✓。我另獨立探測 0-byte SKILL.md → 紅（走 frontmatter 分支）✓。 |
| **m6** | 已修 | `agents=(...)` 陣列＋`"${agents[@]}"`；自測 ⑤b 兩案（含空白路徑接線齊 → 綠；缺 visual-reviewer.md → 只點名它、ui-designer.md 不被誤報）親跑通過 ✓。 |
| i1／i2／i5／i8 | 已修（skill 端） | i1 的基準說明我複算過：`photo-corner` 對 `$surface` ＝ **2.114／2.219**（＝板上 `WEjnC` 的 2.11／2.22）、對 `$bg` ＝ **1.73／2.603**——新加的註記完全正確。i2／i5／i8 的加註與 `sQsj1`／`F7Swz6`（板上格子＝`'72'`）原文一致。 |
| **i3** | 已修，且真的收緊了 | 我獨立探測：`<!-- 舊版曾要求 little-sprout-brand，已移除 -->` → **紅**（R1 時是綠）✓。兩份 agent 定義第 11 行皆含「開工先用 Skill 工具載入專案 skill \`little-sprout-brand\`」，符合新判準。 |
| **i4** | 已修 | 樣本數由 `ok()` 計數；親跑輸出「24 組樣本」，實際 ✓ 行數 ＝ 24，與 PR body 的分項（3+8+2+4+2+2+2+1）加總一致。 |
| i6 | 不修，同意 | `ba6dbabb` 逐字引文，保留逐字性正確；`wordmark-brand.md:14` 已用正確語意表述同一件事。 |
| i7 | orchestrator | 併入後 back-merge，PR 外動作，本輪不評。 |

### 親跑結果（`/bin/bash` 3.2.57，arm64）

- `brand-skill-check.sh` → 綠 rc=0
- `brand-skill-check.test.sh` → **24/24 ✓ rc=0**（與 handoff 宣稱一致）
- 4 條 commit message 逐條過 `commit-msg-gate.sh` ✓
- `git merge-tree --write-tree`：對 `origin/main`／`origin/development`／`origin/test` **三方皆 CLEAN**
- PR #136 CI：`ci`／`db`／`lint`／`rules` **四 job 全綠**（run 32819817113）

### 四維

| 維度 | 結論 |
|---|---|
| Race condition | **N/A** — R2 增量 0 行 Swift／SQL（9 檔皆 .md／.sh）。 |
| 運算效能 | **N/A** — 唯一可執行物是 bash gate；`-r`→`-s` 同成本，陣列迴圈同成本，CI 實測整個 rules job 55s（含既有全部 gate）。 |
| 平行優化 | **N/A** — 無可平行路徑；一次列全部違規需序列累積，正確。 |
| **Scope** | **無 finding** — R2 的 9 檔逐檔可回溯到 R1 的 M1／m2–m6／i1–i5／i8，無無關重構、無順手改動。COLLABORATION §7 的單列改寫也只同步 `-s`／同一行判準／24 組三處。兩次改寫同段（`f38239f` 8 位 → `8cfe1c7` 6 碼）我只驗最終樹，內容一致，不要求 squash。 |

---

## Findings（全部 informational，不擋合併）

**R2-i1 — `scripts/gates/brand-skill-check.sh:80`：`-s` 擋 0 bytes，但擋不住只含一個換行的 reference。**
實測 `printf '\n' > references/tokens.md`（1 byte）→ gate **綠**。m5 想擋的現實情境（merge 解成空檔、LFS pointer 沒還原）產出的都是 0 bytes，所以這是可接受的收斂；只是「指到空檔」的字面承諾比實作寬一點。要補的話成本是一個 `grep -q '[^[:space:]]'`。**不建議在本 PR 動**——已達 m5 目的。

**R2-i2 — 同檔 `:93`：`grep -qE '載入.*little-sprout-brand'` 要求兩詞同一行且順序固定，會有 false red。**
實測兩種寫法會誤紅：①必載指示折行（「…載入專案 skill\n  \`little-sprout-brand\`」）②語序倒置（「\`little-sprout-brand\` 開工必先載入」）。兩者都是 **fail closed**（rc=1）且錯誤訊息直接寫出要求的字串，改回去成本 10 秒，不是缺陷；但日後有人重寫 agent 定義段落時會撞到。若要放寬，`grep -q 'little-sprout-brand'` 配「同段落內有 載入」的兩段式判定即可。（另：`不需要載入 little-sprout-brand` 仍綠——grep 型 gate 的固有極限，不列缺陷。）

**R2-i3 — `brand-skill-check.test.sh:125-139`：⑤b 把 `$work/repo` 移走並刪除，其後 ⑥ 的「多餘參數」樣本仍以 `$work/repo` 為路徑。**
目前**仍是有效斷言**（gate 的參數個數檢查先於路徑檢查；若有人把順序調換，輸出會變「找不到目錄」而該樣本會紅）。風險在未來：若在 ⑥ 之後追加任何用 `expect()` 的樣本而忘了 `reset`，它會對不存在的目錄跑、拿到 exit 2。建議 ⑤b 結尾補一行 `reset`（一行，非必要）。

**R2-i4 — `motifs.md:68`／`SKILL.md:48` 的「後端 LS-90 對齊中——現行部署仍 8 碼／40 bit」是有到期日的敘述，需要指定清除者。**
LS-90 一併入，這兩句就變成本 skill 自己 slop 第 18 條「過期的 Notes 比沒有更糟」的實例，而且是最會被信的那一段（ios-dev 對接契約）。建議把「刪除 skill 內的『對齊中』註記」寫進 **LS-90 的驗收清單**（不是本 PR 的事），順帶也是 §7「skill 與 Tokens 板無機械對帳」盲區的一個具體個案。

**R2-i5 — `tokens.md:81-82` 的交叉索引編號與 `motifs.md` 的清單編號不一致。**
`motifs.md` 把原文 `h7EnT` R6 的 ③（分組）併進 ①，所以其清單 ①②③④⑤ 對應原文 ①②④⑤⑥；`tokens.md` 引用時寫的是**原文編號**（`h7EnT` R6 ②／④）。兩邊各自正確，但讀者順著 `tokens.md` 的「R6 ④」去 `motifs.md` 找 ④ 會看到 tap target 而不是 fs-code。建議 `motifs.md` 的清單標成原文編號（①②④⑤⑥）或在句尾標「＝原文 ④」。

**R2-i6 — 稿上 `h7EnT` R2 的 Q 排除註記在裁決後與採用的字母表相衝，板端未修。**
原文寫「排除易混字元 Q O 0 I L 1…示例碼已由『K7M 2QD』改為『K7M 2FD』，06/06b/06c 輸入格第 5 格 Q → F」，但 LS-89 裁決採用的 32 字元表**含 Q**。skill 已用「稿上 30 字元集未採用，字母表以後端為準」正確標示，所以 ui-designer 載 skill 不會走偏；殘留風險只在**只讀稿不讀 skill** 的路徑。建議併 LS-81／LS-90 順修 `h7EnT`（與 R1 的板端 i1 `WEjnC`、i2 `sQsj1`、`$fs-code` ax3 76→72 同一批）。

---

### 已看／未看

**已看**：R2 增量 9 檔完整 diff；全 skill 對邀請碼／40-bit／對齊後端／時間戳／29.4 的殘留 grep；`h7EnT`／`NHDwj`／`WEjnC`／`sQsj1`／`F7Swz6` 板上原文對 skill 轉錄的逐項比對；`.pen` 節點數親算（2676）；4 組對比值與 photo-corner 兩種量測底的獨立複算；八格／六格寬度模型的獨立重算；後端 `create_invite`／測試正則的實際契約；gate 與自測在 bash 3.2 親跑（24/24）＋5 組我自己設計的邊界探測（0-byte SKILL.md、折行必載指示、1-byte reference、語序倒置、否定句）；4 條 commit message gate；三方 merge-tree；PR 四 job 狀態。

**未看／未驗**：
- **FLAGGED 50 vs 51**：需版面引擎，無法獨立複驗，沿用 LS-72 記錄（skill 已標「未獨立複驗」，屬誠實標記）。
- **LS-38 出處的數字**（ΔE 81.7／14.5、筆寬比 0.886–0.973、覆蓋率極差 7.63pp 等）：R1 已標採信，本輪未再查，維持採信。
- **R1 已逐值驗過而 R2 未觸碰的內容**（72 個變數全表、19 對對比、entry-conditions 12 項逐字 diff）：本輪只重驗 R2 動過的欄位＋抽查，未整表重跑。
- 未開 Pencil MCP（依派工），板端修正一律留給 LS-81／LS-90。

**結論**：M1 的實質問題（對已上線契約做反向斷言）已由 LS-89 裁決＋本次改寫徹底解決，且解法比 R1 的建議更好——把裁決、現況、擱置備案三層講清楚，還把 `h7EnT` R6 的規格完整保住，未來升位不必重開設計輪。m2–m6 逐條可驗、gate 的三個新負向樣本我親自確認會紅。無 blocker／major／minor，**APPROVE**。六條 informational 皆為未來維護提示，其中 R2-i4（LS-90 併入時清除「對齊中」註記）建議寫進 LS-90 驗收，R2-i6 併入板端修正批次。

### 2026-08-25T07:15:41.400Z · Chih-Lin Yeh · `7f503489`

## 收尾儀式（orchestrator，2026-08-25）

**① dead-code 巡檢**：純 harness 票免；R1／R2 reviewer 逐檔審（72 變數逐值、19 對對比、12 項逐字），無殘留。

**② Lesson learning review**
- **最大收穫**：reviewer 抓到 skill 把邀請碼寫成「6 碼對齊 40-bit」——**設計定案（6 碼）與已部署後端（8 碼）衝突兩天沒人發現**，直到有人把兩邊寫在同一份文件。教訓：**skill／規格文件裡每個涉及後端契約的數字都要標來源檔（migration／API.md 行）**，reviewer 才能機械對帳；已在 skill 內落實（M1 段引用 migration:233/293、tests:1386）。衍生 LS-89（使用者裁決 A）→ LS-90。
- **Review 2 輪**：R1 1 major＋5 minor（三個是數字／配對轉錄錯、兩個是 gate 邊界）；R2 APPROVE。轉錄類錯誤的根因是「不開 Pen 只讀 evidence」——正確的取捨（Pen 被 LS-81 佔用），代價已由 reviewer 的獨立複算吸收。
- **Agent 失聯**：實作 agent 在 push-gate 完成後失聯（今日第四例：LS-70、LS-17×2、LS-30），orchestrator 用其備妥的 PR body 開 PR 接手。對策已在 LS-88 H9（Stop hook 巡檢）；另觀察：失聯的都是 >350k token 的長任務——**派工時把大票拆成「一個 PR 一個 agent」**，寫進 COLLABORATION §3（LS-88 一併）。
- **informational 處置**：R2-i1（1-byte reference 不紅）／i2（同行 grep 語序）不修，fail-closed 且訊息明確；i3（自測 ⑥ 依賴 ⑤b 刪掉的路徑）→ 併 LS-88 順修；i4 → LS-90 驗收已加；i5（tokens.md 交叉索引編號）→ LS-90 刪「對齊中」敘述時一併對齊；i6（板端「排除 Q」註記）→ LS-81 順修。
- **待補（需 Pen）**：`fs-code` ax3 變數 76→72 回填、六個 token 板上對比欄 → LS-81 最後一群順做。

**③ 部署**：harness，無雲端。back-merge main→development→test 鏈跑中（新 gate `brand-skill-check` 需先回流，否則 #117 會紅）。→ Done。LS-67 的 blockedBy LS-30 解除。

