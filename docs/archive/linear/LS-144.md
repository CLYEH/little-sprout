# LS-144 Harness：巡檢加「開票責任」機械提醒——lane 在飛 0 且無可派候補時，動作清單印「→ 開票」並列來源（story 拆票／LS-96 P1 升票）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-03T08:29:05.414Z |
| 開始 | 2026-09-03T10:11:43.422Z |
| 完成 | 2026-09-03T12:13:36.605Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-144/harness巡檢加開票責任機械提醒-lane-在飛-0-且無可派候補時動作清單印-開票並列來源story-拆票ls-96-p1-升票 |
| 關係 | related→LS-118, related→LS-104, related→LS-115, related→LS-22, related→LS-124, related→LS-18, related→LS-17, related→LS-103, related→LS-152, related→LS-67, related→LS-24, related→LS-132, related→LS-145, related→LS-142, related→LS-96, related→LS-19, related→LS-20, related→LS-131, related←LS-206, related←LS-204, related←LS-198, related←LS-197, related←LS-186, related←LS-185, related←LS-184, related←LS-183, related←LS-182, related←LS-181, related←LS-179, related←LS-178, related←LS-175, related←LS-172, related←LS-155, related←LS-169, related←LS-168, related←LS-156, related←LS-160, related←LS-159, related←LS-157, related←LS-158, related←LS-154, related←LS-141 |

## 描述

## 來源

使用者 2026-09-03 16:24–16:30：「你有一個責任是要在適當的時機把單開出來，不要讓產線停擺」「我們是 agentic 開發，應該持續工作，除非我請你停下來」「巡檢請加上提醒你要開單的機制」。事故：09-03 15:36 全部在飛票結清後，orchestrator 連續三輪巡檢回報「無異常、補位 hold」而未開新票（design／harness／backend 三條 lane 同時空著 50 分鐘）。現行 `patrol-linear.sh` 只算「候補」（既有 Backlog 票），候補為空或全被規則擋住時就靜默，沒有「該開票了」的信號。

## 範圍

1. `scripts/ops/patrol_linear.py`／`patrol-linear.sh` 第 3 段 lane 補位：每個 lane 若「在飛 0 且（無候補 或 候補全被擋——`lane:ui` 無核可稿、使用者裁決 hold 清單）」→ 動作清單印 `→ 開票：lane:<x> 空 ≥1 輪——來源候選：` 並機械列出：(a) `lane:design`／`lane:ui`：Backlog 中標「需 Design gate」的 Story（[LS-19](https://linear.app/little-sprout-app/issue/LS-19/story孩子檔案-crud-與年齡標記)／20／24…）及其尚無設計票的畫面群；(b) `lane:harness`：[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項中 P1／P2 且尚未升票者（解析池 comment 的 `P1 ·`／`P2 ·` 前綴＋是否已有票引用該 comment id）；(c) `lane:backend`：Backlog Story 中可拆「後端先行（不需 Design gate）」者。連續空 ≥2 輪升級為 ⚠。
2. 「使用者裁決 hold」清單機械化：Linear label `hold:user`（或 issue 內固定標記行）→ 補位與開票候選皆跳過並在清單註明「使用者裁決」，不再靠 orchestrator 記憶（[LS-131](https://linear.app/little-sprout-app/issue/LS-131/authgoogle-登入授權視窗顯示-supabaseco-改原生-google-sign-in-或-supabase-自訂網域待裁決)／132／133）。
3. `docs/COLLABORATION.md` §4-b 巡檢 prompt 模板加第 4 步「開票責任」（動作清單印「→ 開票」時 orchestrator 必須當輪開票或在 notes 寫明不開的理由）；§5-b 補「lane 空＝停擺，不是等待」；§7 對照表加一列。
4. 自測 `patrol-linear.test.sh`（既有）補樣本：lane 空＋無候補 → 印開票行；lane 空＋候補全 hold:user → 印開票行且註明；lane 有在飛 → 不印。

## 不做

不自動建票（只提醒＋列來源；建票仍由 orchestrator 判斷 scope）。

## 驗收

- [ ] hotfix 自 `main`、PR 回 `main`；自測正負樣本全過並掛 CI `rules`
- [ ] 對目前 Linear 實跑一次（附輸出）：ui lane 應印「→ 開票」並列 [LS-19](https://linear.app/little-sprout-app/issue/LS-19/story孩子檔案-crud-與年齡標記)／24 畫面群（[LS-20](https://linear.app/little-sprout-app/issue/LS-20/story相簿與照片上傳壓縮縮圖背景續傳) 已由 [LS-142](https://linear.app/little-sprout-app/issue/LS-142/設計相簿頁相簿詳情上傳佇列ls-20-畫面群-ui-designervisual-reviewer-3-輪) 承接）
- [ ] merge-reviewer 通過
- [ ] 併 main 後 back-merge；orchestrator 同步更新 session cron prompt

## 留言（17）

### 2026-09-03T10:12:29.823Z · Chih-Lin Yeh · `82f187f0`

orchestrator 2026-09-03 18:12：LS-141 併 main `e5a9c23` → harness lane 接續，本票 Backlog→In Progress（cycle 1）。派 general-purpose：worktree `.claude/worktrees/LS-144`、branch `hotfix/LS-144-patrol-open-ticket-reminder`（自 main e5a9c23）、PR 回 main；範圍：patrol-linear 印「→ 開票」＋來源候選（design／ui Story 畫面群、LS-96 P1/P2 未升票、backend 可拆 Story）、`hold:user` label（只貼 LS-131）、§4-b 第 4 步／§5-b／§7、自測三樣本＋對目前 Linear 實跑。session cron 第 4 步已先行（ee81b7fa），本票落地後對齊模板。

### 2026-09-03T10:37:37.692Z · Chih-Lin Yeh · `63bab52f`

## handoff

**Ticket**：LS-144（hotfix 自 main `e5a9c23`；PR #250 → main；branch `hotfix/LS-144-patrol-open-ticket-reminder`，head `a31b8bd`；三段 commit `30479ab` code＋.gitignore／`272455f` test／`a31b8bd` docs）

**已完成**（票文範圍 1–4）
1. 開票責任信號（`30479ab` `scripts/ops/patrol_linear.py`）：
   - 常數 `HOLD_LABEL`／`DESIGN_GATE_MARK`／`POOL_PRIORITY_RE`／`BACKEND_KEYWORDS`／`STATE_FILE_REL`（`patrol_linear.py:56-64`）；`CLOSED_ISSUES_QUERY`（`:91`）／`POOL_COMMENTS_QUERY`（`:101`）；`fetch_closed_issues()`（`:211`）／`fetch_pool_comments()`（`:215`）／`best_effort()`（`:231`，gql 的 stderr 接進報表行內，`--json` 呼叫端 `2>&1` 仍合法）
   - `classify_candidate()` 加 `hold`（`:369`）／`design_gate`（`:381`，票文「需 Design gate」且非 lane:design 永不列為候補）；`lane_pending()` 改回傳 dict spec／structure／design_gate／hold／blocked（`:470`）
   - 來源候選 `design_gate_sources()`（`:509`）／`backend_sources()`（`:522`）／`pool_sources()`（`:544`，`P1 ·`／`P2 ·` 且未被任何 open／已結案票引用 id 前 8 碼、未在池內被「銷除／升為」）；`format_open_ticket_action()`（`:574`）；`load_state()`／`save_state()`（`:591`／`:600`，原子換檔、fail-soft）
   - `build_report()`：lazy 附加查詢＋`open_ticket_sources()`（`:784`）；每 lane 在飛 0 且候補清單為空 → `open_ticket` 欄＋「→ 開票：lane:<x> 空 n 輪（無候補｜候補全被擋：…）——來源候選：…」，連續空 ≥2 輪升 ⚠，計數存 `<root>/.claude/patrol-state.json`（`:844-870`）；`format_lane_line()` 多「待Design」「hold:user」兩欄（`:929`）
   - `.gitignore:25` 加 `.claude/patrol-state.json`
2. `hold:user`：Linear label 已建（team LS，id `400d08d3`，`#95A2B3`，description「使用者裁決暫不動；補位與開票候選皆跳過」）；只貼 LS-131（現 `lane:product`＋`hold:user`）；LS-132／133 未貼（使用者已改「有空就做」）。機械面：`classify_candidate()`→`hold`，lane 表 hold:user 欄「LS-x（使用者裁決）」，候補全 hold → 開票行「候補全被擋：hold:user LS-x（使用者裁決）」。
3. 規約（`a31b8bd` `docs/COLLABORATION.md`）：§4-b 工具段補信號說明（`:124`）＋巡檢模板第 4 步「開票責任」（`:135`：印「→ 開票」當輪開票或 notes 寫明理由；印 ⚠ 仍不開必須回報使用者）；§5-b Lane 段補 `hold:user`（`:162`）＋新條「lane 空＝停擺，不是等待」（`:177`）；§7 新列（`:279`，含盲區）。
4. 自測（`272455f` `scripts/ops/patrol-linear.test.sh`）：⑩（`:632` 起）——lane 空＋無候補（harness）印開票行並列池項來源（P1／P2 才列、被票引用或池內銷除不列、P3 不列、P1 排前）；lane 空＋候補全 hold:user（backend）印開票行註明「使用者裁決」且 hold 票不進候補；lane 有在飛（design）不印；「需 Design gate」歸待Design 不進候補、open 或已結案設計票承接的 Story 不列；第二輪升 ⚠ 且計數檔正確；human 兩新欄；附加查詢失敗 fail-soft。⑨ 斷言改「不含任何 save_issue 派工動作」（`:605`）；主 stub 補 `comments(`／已結案票查詢回空（`:141`）。

**已驗證（怎麼驗）**
- `bash scripts/ops/patrol-linear.test.sh`：82 ✓／0 ✗、exit 0（⑩ 21 條新樣本；既有樣本全綠）。push-gate 全綠（無 Swift 變更，SwiftLint／unit tests 跳過、CI 仍跑）。
- 對真 Linear 實跑（2026-09-03 10:27 UTC，`patrol-linear.sh --repo .claude/worktrees/LS-144`）：四 lane 皆有在飛（harness 1/1、backend 3/3、design 1/1、ui 1/2）→ 無 lane 觸發「→ 開票」；ui lane 候補（無）、**待Design：LS-19, LS-20, LS-24, LS-145**（此前這三張會被列為候補並印 `save_issue state=Ready`——池項 b2993155 的事故形狀）。完整輸出貼在 PR #250 body。實跑產生的計數檔（四 lane 皆 0）已自主 checkout 刪除（主 checkout `.gitignore` 併入後才有規則），主 checkout 仍 clean。
- `pr-body-check.sh` 格式＋`--verify`（LS-96 id 反查 3 條皆存在）通過。
- CI PR #250 run `33744834149`：`lint` pass 12s／`rules` pass 2m56s／`db` pass 3m0s／`ci` pass 4m56s（`gh pr checks 250 --watch` 前景等到完成）。

**未完成／informational 處置**
- 記入待辦池 LS-96 `3dd30183`：4 條 P3——已升票判定只認 id 前 8 碼字面（票文引用「教訓」也會被當已升票）／計數檔跨 session 互相加輪、不帶時間戳／`cycle_progress()` 舊 best-effort 路徑 stderr 外漏同型未順修／`BACKEND_KEYWORDS` 泛用字命中過多。
- 池項 `b2993155`（P1，lane:ui 候補不驗 Design 出口）由本票落地——銷除 comment `62ecf8f1`（採嚴格變體：不看 blockedBy 是否掛 Done 的 design 票，Story 本身不派）。
- 不修＋理由：票文驗收「ui lane 應印 → 開票並列 LS-19／24」——實跑時 ui 在飛 1（規則要求在飛 0），不為了實跑造假條件；該路徑由自測 ⑩ 覆蓋，實跑改以「待Design」欄證明 LS-19／20／24 已被正確攔下。
- 待 orchestrator：merge-reviewer；併 main 後 back-merge main→development；§4-b 第 4 步文字依票文摘要寫，請對齊 session cron 現行 prompt（票文驗收最後一條）；Linear 狀態未動（仍 In Progress）。

**風險**
- 計數檔在主 checkout，多個 session 同時巡檢會互相加輪；隔夜再跑接著數（§7 盲區）。
- 來源候選是啟發式（後端關鍵字、`P1 ·` 前綴、id 前 8 碼引用），漏列／誤列靠 orchestrator 判斷。
- `hold:user` 要人貼；忘了貼的票仍會被列為候補。

**產出位置**：worktree `.claude/worktrees/LS-144`；PR #250；scratchpad `LS-144-selftest.txt`／`LS-144-realrun.txt`／`LS-144-pr-body.md`。

### 2026-09-03T10:39:20.183Z · Chih-Lin Yeh · `5f982b87`

orchestrator 2026-09-03 18:40：handoff `63bab52f` 收到——PR **#250** head `a31b8bd`（開票提醒＋來源候選三類、連續空計數 `.claude/patrol-state.json`、`hold:user` label 只貼 LS-131、§4-b 第 4 步／§5-b／§7、自測 82；實跑把 LS-19／20／24／145 移到「待Design」欄——LS-96 `b2993155` 銷除 `62ecf8f1`；informational 入池 `3dd30183`），CI 四綠 → In Review、派 merge-review R1（重點：候補判定與承接比對、狀態檔競態、`--json` 純淨、mutation、LS-145 是否誤列待Design）。

### 2026-09-03T10:51:58.365Z · Chih-Lin Yeh · `de969e4f`

## merge-review R1 — PR #250（head `a31b8bd`）

**判定：REQUEST_CHANGES**（2 major／5 minor；major 兩條都在**真實 Linear 資料上實測重現**，非推測）

審查範圍：`git diff e5a9c23...a31b8bd`（4 檔 540+/20-：`scripts/ops/patrol_linear.py`、`scripts/ops/patrol-linear.test.sh`、`docs/COLLABORATION.md`、`.gitignore`）。head 與 `gh pr view 250 --json headRefOid` 一致（`a31b8bdd60875844b384bce779414e205844662d`）。worktree 唯讀，未改任何檔案。

---

### F1（major）`scripts/ops/patrol_linear.py:60`／`:357-359` — 「需 Design gate」子字串比對對**否定句**無感，把不需設計稿的票鎖進「待Design」

```python
DESIGN_GATE_MARK = "需 Design gate"                                    # :60
return DESIGN_GATE_MARK in (issue.get("description") or "") and ...    # :359
```

`"不需 Design gate"`、`"（另票，需 Design gate）"` 都**包含**這個子字串。

**具體失敗情境（現正發生）**：
- **LS-145**（`lane:ui`／Backlog／有 `## 驗收`）票文第一行：「TestFlight／送審前置，**非畫面票（不需 Design gate）**」。實跑 `patrol-linear.sh --json`：`lane:ui` → `pending_design: ["LS-19","LS-20","LS-24","LS-145"]`、`candidates: []`、`wip 1/2`。**上限 2、在飛 1、有空位，動作清單卻一條都不印**（`wip != 0` 所以連「→ 開票」也不觸發）——lane 靜默停擺，正是本票要治的事故形狀。
- 對照組（同一份 live 資料跑 `e5a9c23` 的舊版）：`lane:ui candidates=["LS-19","LS-20","LS-24","LS-145"] chosen=LS-19`。舊版選 LS-19 是 `b2993155` 的錯（本票修對了），但 **LS-145 從「可派候補」變成「永不候補」是本 PR 新引入的迴歸**。
- 反向誤導：`design_gate_sources()` 同樣命中 → ui lane 一旦空掉，開票行會列 `LS-145「Phase 2-2：PrivacyInfo.xcprivacy…」（需 Design gate、尚無設計票（先開 lane:design））`，叫 orchestrator 為一張 plist／xcprivacy 設定票開設計票（實測輸出：來源候選＝`LS-22`＋`LS-145`）。
- 同型潛伏：`LS-143`／`LS-149`（`lane:backend`）票文都寫「拆後端先行（**不需 Design gate**）；UI 端（另票，**需 Design gate**）」——目前是 In Review 所以走 `not_backlog` 先返回，一旦有同樣寫法的 backend 票落在 Backlog 就同樣被鎖死。這不是罕見寫法：它就是票文 §範圍 (c) 規定的 backend 拆票用語。

**建議修法**（已在 scratch 複本實測）：`DESIGN_GATE_MARK = "**UI 票：需 Design gate**"`——LS-19／20／22／24 四張真 Story 與 ⑩ 全部 fixture 用的都是這個正典粗體形式。實測：自測 **82 ✓／0 ✗**、live `LS-19 → design_gate`（b2993155 修法保住）、`LS-145 → ok`（放回 ui 候補）、ui 來源候選只剩 `LS-22`。另請補負樣本（「不需 Design gate」「另票，需 Design gate」各一）——現行 ⑩ 對修前修後**同樣全綠**，代表這個分支沒有負向覆蓋。

---

### F2（major）`scripts/ops/patrol_linear.py:552-562` — 「銷除」公告 comment 自己被當成未升票的 P1 池項

`POOL_PRIORITY_RE.search(body)` 對整則 body 全文搜尋，而「銷除」公告依池內既有慣例會**引述被銷除項的 `P1 ·` 文字**；`:558-562` 的排除只檢查「**別則** comment 提到我的 id 前綴＋含銷除字樣」，公告自己不會被自己排除。

**具體失敗情境（現正發生）**：LS-96 comment `62ecf8f1` 就是**本 PR 自己**銷除 `b2993155` 的那則公告，body 為「**銷除**：待辦池 comment \`b2993155\`（**P1 ·** patrol_linear.py lane 補位候補對 `lane:ui` 票不驗 Design 出口）——已由 LS-144 機械落地…」。實跑 `pool_sources()` 對 live LS-96（156 則 comment）→ 輸出 13 條，`LS-96#62ecf8f1` 排在 **P1 群第 3**，why＝「P1 池項尚未升票」。依本 PR 寫進 §4-b 模板第 4 步的「**必須當輪開票**」，orchestrator 下一輪空 lane 就會為**本 PR 已完成並已銷除的工作**開一張重複票。b2993155 本身有被正確隱藏（機制的正向路徑是好的），壞的是公告本身被反向計入。

**建議修法**（已實測）：`pool_sources()` 迴圈開頭跳過以銷除／銷案開頭的公告（live 兩則公告都是 `**銷除**：`／`銷案：` 起頭），例如 `if re.match(r"^\W{0,4}(銷除|銷案)", body): continue`。實測：自測 **82 ✓／0 ✗**、`62ecf8f1` 從來源消失、其餘 12 條不變。同一類還有「P3 池項 body 中間引用了 `P1 ·` 就被升級成 P1」，同修法涵蓋不到，建議一併考慮把前綴比對收斂到入池行。

---

### F3（minor，PLAUSIBLE）`:511-520`／`:91-99` — Canceled 的設計票也算「已承接」
`CLOSED_ISSUES_QUERY` 撈 `completed` **與** `canceled`，`design_tickets_for()` 不看 state。設計票被取消的 Story 會從 design／ui 開票來源**靜默**消失（方向是漏列，不是誤列）。live 有 `LS-124`（Canceled、`lane:design`），但標題未提任何 Story ident，目前無實害——故列 minor 不列 major。建議只認 `state.type == "completed"` 或 open。

### F4（minor）`:511-516` — 承接只比對 parent 與**標題**
設計票若只在票文（非標題）提到 Story，會判為「未承接」而多列一次。方向安全（多提醒），live 三張（LS-67→LS-19、LS-142→LS-20、LS-152→LS-24）全部判對，整字比對 `LS-19` 不吃 `LS-190` 也驗過。僅記錄。

### F5（minor）`:847` — 觸發條件 `wip == 0` 讓「有空位但在飛>0 且候補空」完全靜默
符合票文「在飛 0」的字面，但 §4-b 要抓的停滯型態是「**lane 有空位卻沒補票**」。live `lane:ui` 上限 2／在飛 1／候補空，就是既不派工也不提醒的死角（F1 修掉後這個 lane 會恢復有候補，但形狀仍在）。建議 §7 盲區列補一句，或日後改判 `wip < limit`。

### F6（minor，docs）`docs/COLLABORATION.md:185` — 交叉引用與新編號相撞
該行寫「巡檢結構檢查（§4-b **模板第 5 步 (e)**）…補位（**第 4 步**）」，但模板在本 PR 前只有 1–3 步（陳舊源自 LS-103），本 PR 新增的第 4 步是「開票責任」——「補位（第 4 步）」現在會指到錯的步驟。順手一句修正即可（不修就請入池）。

### F7（minor，已知已入池）`:591-608`／`:864-869` — 計數檔的跨行程競態
`load_state()`→算→`save_state()` 是 read-modify-write，`os.replace()` 只保證檔案不半截、不保證跨行程不互蓋：兩個 session 同時巡檢會少算（lost update，⚠ 延後）或互相加輪（⚠ 提早）。fail-soft、只影響提醒強度，且 §7:279 盲區欄與 LS-96 `3dd30183` 都已記錄。不擋併入。

---

### 逐項查實結果

1. **判定邏輯／lazy／best-effort／`--json` 純淨**：`wip == 0 and not candidates_shown`（`:847`）與 §5-b 一致（F5 是條件本身的邊界）。附加查詢確實 lazy（`extra` 快取、各打一次）；`gql()` 用 `capture_output=True`，curl 的 stderr 不會漏到 fd 2，`best_effort()`（`:231`）再接住 Python 側 stderr＋`SystemExit`。**實跑驗證**：`patrol-linear.sh --json` exit 0、stdout 為合法 JSON、**stderr 0 bytes**；自測的 fail-soft 樣本以 `2>&1` 收全部輸出仍能 `json.loads` 成功。計數檔原子換檔（pid 命名 tmp＋`os.replace`）與 `.gitignore:25` 皆到位。
2. **`hold:user`**：Linear API 核對全 25 張 open 票，只有 **LS-131**（`lane:product`＋`hold:user`）貼了，符合票文。候補全 hold → 行文「候補全被擋：hold:user LS-973（使用者裁決）」正確，且 mutation 證明有被測到。
3. **自測**：實跑 `patrol-linear.test.sh` → **82 ✓／0 ✗、exit 0**（與 handoff 相符）。**Mutation ×2**（在 scratch 複本上做，未動 repo）：① 拿掉 `classify_candidate()` 的 `hold` 分支 → 3 條紅（`hold:user 不進候補`／`backend 開票行註明使用者裁決`／human hold 欄）；② 拿掉 `design_gate` 分支 → 7 條紅（含待Design、開票行、計數檔、第二輪 ⚠、fail-soft）。樣本確實綁住行為，非空轉。⑨ 改「不含任何 `save_issue` 派工動作」合理——開票行是提醒不是派工，原斷言（actions 為空）在新行為下必然假陽。
4. **docs**：§4-b `:124` 工具段、`:135` 模板第 4 步涵蓋三類來源（design／ui 共用同一份設計票來源，對應程式碼 `open_ticket_sources()` 的分派）＋「當輪開票或 notes 寫明理由」＋「⚠ 仍不開必須回報使用者、不得再回無異常」；§5-b `:177`「lane 空＝停擺」；§7 `:279` 一列且含四項盲區。**未驗**：live session cron prompt（CronCreate、session-only）我無法讀取，只能與票文摘要比對——「與現行 cron prompt 逐字對齊」這條請 orchestrator 自行核對。
5. **待Design 欄語意**：LS-19／20／24 歸「待Design」正確（承接判定也對：LS-19←LS-67 Done、LS-20←LS-142 In Progress、LS-24←LS-152 標題整字命中，三張都沒被誤列為開票來源）。**LS-145 誤列 → F1**（不只是顯示問題，它同時被踢出候補）。
6. **其他**：主 checkout clean（我實跑產生的 `.claude/patrol-state.json` 已刪；註記：`--repo` 指 worktree 時 `ROOT` 仍解到主 checkout，所以計數檔一律落在主 checkout，併入 main 前它在主 checkout 會是 untracked）。`pr-body-check.sh` 格式與 `--verify` 皆通過（LS-96 三條 id 反查存在）。`gh pr checks 250` 四綠（lint／rules／db／ci）。**scope 未擴**：4 個檔案全在票文範圍 1–4 內，`fetch_issue_pages()` 抽取與 `lane_pending()` 回傳型別變更都是本次功能必需，無順手重構。

**結論**：機制骨架、fail-soft、自測強度都夠（mutation 自證通過）；擋下的是兩條在**真實資料上已經在發生**的誤判——F1 讓一張合法 ui 票永不被派（新迴歸），F2 讓本 PR 自己銷除的池項變成「該開票」的第一順位。兩條的修法各約 1–3 行且不動既有樣本（實測 82 綠），補上對應負樣本後再送 R2。


### 2026-09-03T10:53:12.419Z · Chih-Lin Yeh · `1a5ec8d5`

orchestrator 2026-09-03 18:48：merge-review R1 `de969e4f` **REQUEST_CHANGES**——F1 `DESIGN_GATE_MARK` 裸子字串把「不需 Design gate」的 LS-145 誤歸待Design（本 PR 迴歸，ui lane 有空位卻不印動作）；F2 LS-96 銷除公告被算成未升票 P1（會開重複票）→ 令 agent R2：改正典粗體標記＋跳過銷除公告，⑩ 各補負樣本，5 minor 逐條處置。

### 2026-09-03T11:08:57.020Z · Chih-Lin Yeh · `949c0f16`

## handoff R2（merge-review R1 `de969e4f` 處置）

**Ticket**：LS-144；PR #250 → main；head **`72df412`**（R2 三段：`1c9898a` fix／`7d2078c` test／`72df412` docs；R1 head `a31b8bd`）。CI run `33747411856`：`lint` pass 12s／`rules` pass 2m55s／`db` pass 3m7s／`ci` pass 6m32s（`gh pr checks 250 --watch` 前景等完；`headRefOid` 對 `72df412d…`）。

**Finding 處置表**

| # | 等級 | 處置 | 證據 |
|---|---|---|---|
| F1 裸子字串「需 Design gate」對否定句無感（LS-145 被鎖進待Design、ui 1/2 靜默＝本 PR 迴歸） | major | 已修 `1c9898a` `patrol_linear.py:62`（`DESIGN_GATE_MARK = "**UI 票：需 Design gate**"`）＋`:360` `needs_design_gate()` docstring | live `--json`（stderr 0 bytes）：`lane:ui` candidates `['LS-145']`、chosen `LS-145`、pending_design `['LS-19','LS-20','LS-24']`，動作 `→ save_issue LS-145 cycle=1（scope+，取自 cycle 外）`／`→ save_issue LS-145 state=Ready cycle=1`；負樣本 ⑩d（`7d2078c` `patrol-linear.test.sh:854` 起）：LS-978「非畫面票（不需 Design gate）」與 LS-979「拆後端先行（不需 Design gate）；UI 端（另票，需 Design gate）」皆不進待Design、照常列為候補並被選中；design lane 來源只列正典標記票 |
| F2 銷除公告 `62ecf8f1` 引述 `P1 ·` 被算成未升票 P1 | major | 已修 `1c9898a` `patrol_linear.py:64`（`POOL_ANNOUNCE_RE`）＋`:558` 迴圈開頭跳過「銷除／銷案」公告；同類（reviewer「一併考慮」）：`:63` `POOL_PRIORITY_RE` 放寬到 P1–P4、`:564` 以 body **第一個** `Pn ·` 定級、只列 1／2 → P3 池項文中引用「P1 ·」不再升級 | live `pool_sources()`（156 則）：13 → **10** 條，`62ecf8f1`／`b2993155` 皆不列；負樣本（`7d2078c` `:721`）：dddd 公告引述「P1 ·」不列、abababab P3 引用「P1 ·」不列 |
| F3 Canceled 設計票算承接 | minor | 已修 `1c9898a` `patrol_linear.py:510`（`design_tickets_for()` 排除 `state.type == canceled`） | 樣本 LS-982（`7d2078c` `:707`，Canceled、標題提到 LS-970）→ LS-970 仍列來源 |
| F4 承接只比對 parent＋標題 | minor | 不修＋理由：設計票票文慣例引用多張無關票（LS-142 提到 LS-17／46／119／120／136），比對票文會把那些 Story 誤判為已承接而**漏列**——方向反而不安全；理由寫進 `design_tickets_for()` docstring（`:503`）與 §7 | — |
| F5 `wip == 0` 讓「有空位但在飛>0 且候補空」靜默 | minor | 判定不改（票文範圍 1 字面「在飛 0」、範圍 4 樣本「有在飛 → 不印」）；§7 盲區已補 `72df412` `COLLABORATION.md:279`；`wip < limit` 變體記入待辦池 LS-96 `bcb97555` | — |
| F6 §5-b「模板第 5 步 (e)／第 4 步」陳舊 | minor | 已修 `72df412` `COLLABORATION.md:185`（改指 patrol-linear.sh 輸出第 4 段「開票結構」(e)／第 3 段「Lane 補位」） | — |
| F7 計數檔 read-modify-write 競態 | minor | 不修——已在待辦池 `3dd30183`（第 2 條），§7 盲區已列 | — |
| 附註 `--repo` 指 worktree 時計數檔仍落主 checkout | — | 不修＋理由：ROOT（主 checkout）是 `.env`／worktrees 的共用根，計數要跨 worktree 共享才有「連續空輪」語意；併入 main 後由 `.gitignore` 涵蓋，併入前只在開發者手跑時出現、本票每次跑完都刪，主 checkout 仍 clean | — |

**mutation 結果**（scratch 複本逐一還原修法 → 跑自測 → 復原，最終檔與備份 `cmp` 一致）
- 還原 F1 裸子字串 → **4 條紅**（⑩d 全部：LS-978 被鎖進待Design、ui 誤印開票、LS-979 被鎖、design 來源多列）
- 拿掉 F2 公告跳過 → **池項來源斷言紅**（dddd 公告被列）
- 還原 `P([12])` 全文搜尋（拿掉第一個 `Pn ·` 定級）→ **池項來源斷言紅**（abababab 被升級成 P1）
- 拿掉 F3 canceled 排除 → **4 條紅**（⑩ ui 來源／開票行／fail-soft 來源／⑩d design 來源）

**自測**：`patrol-linear.test.sh` 87 ✓／0 ✗、exit 0（R1 82＋⑩d 5）。**主 checkout** clean（計數檔／`__pycache__` 每次跑完即刪）。PR body 已追加 R2 處置表（`pr-body-check.sh` 格式＋`--verify`：LS-96 id 5 條反查存在、「已修」SHA 4 條皆在 PR commits 內）。

**informational 新增**：記入待辦池 LS-96 `bcb97555`（F5 `wip < limit` 變體、第一個 `Pn ·` 定級的混級代價）。

**待 orchestrator**：merge-review R2；併 main 後 back-merge；§4-b 第 4 步對齊 session cron。

### 2026-09-03T11:09:49.793Z · Chih-Lin Yeh · `d61947ea`

orchestrator 2026-09-03 19:09：R2 handoff `949c0f16` 收到——head `72df412`（F1 正典粗體標記，live ui 候補回到 LS-145；F2 跳過銷除公告＋第一個 `Pn ·` 定級，池項 13→10；F3 Canceled 不算承接；F4／F7 不修附理由；F5 入池 `bcb97555`；F6 §交叉引用；mutation ×4、自測 87）；CI 四綠 → 派 merge-review **R2**。過程教訓（mutation 迴圈用 `git checkout --` 沖掉未 commit 修改）記入 retro。

### 2026-09-03T11:21:30.826Z · Chih-Lin Yeh · `1f31ddfc`

## merge-review R2 — PR #250（head `72df412`）

**判定：REQUEST_CHANGES**（1 major／3 minor）

審查範圍：`git diff a31b8bd..72df412`（3 檔 94+/22-：`patrol_linear.py`／`patrol-linear.test.sh`／`COLLABORATION.md`）＋ R1 `de969e4f` F1–F7 逐條複核。head 與 `gh pr view 250 --json headRefOid` 一致（`72df412d830e5026d3b073279b6977157de3e9d4`，審完再對一次仍相同）。worktree 唯讀，未改任何檔案（我 importlib 產生的 `__pycache__`、實跑產生的 `.claude/patrol-state.json` 都已刪，兩處 checkout 皆 clean）。

---

### R1 F1–F7 複核

| # | 申報 | 複核結果 |
|---|---|---|
| F1 | 已修 `patrol_linear.py:62` 正典粗體標記 | **成立**（live 實跑＋mutation 皆重現，見下 ①）；殘留反方向風險見 N2 |
| F2 | 已修 `:64` 公告跳過＋`:563-566` 第一個 `Pn ·` 定級 | **公告跳過成立；「第一個 `Pn ·` 定級」在 live 上藏掉 2 條真 P2 → N1（major）** |
| F3 | 已修 `:510` Canceled 不算承接 | **成立**（mutation 4 條紅；live 唯一 Canceled 設計票 LS-124 標題不提任何 Story，無實害，故只由 fixture LS-982 覆蓋——合理） |
| F4 | 不修＋理由（比對票文更不安全） | **理由實測成立**：若改比對票文，`LS-152`／`LS-119` 的票文都提到 **LS-22**，LS-22 會被誤判已承接而從 design／ui 開票來源漏列；其餘四張（19／20／23／24）無額外命中。維持只比對標題正確 |
| F5 | 不改判定＋§7 盲區＋入池 `bcb97555` | **成立**：`:857` 仍 `wip == 0`（票文字面），§7 已補靜默死角一句，`bcb97555` 存在且內容相符 |
| F6 | 已修 `COLLABORATION.md:185` | **成立**：新文指「輸出第 4 段『開票結構』(e)／第 3 段『Lane 補位』」，與 `patrol_linear.py:982`／`:988` 的實際段名段號一致；模板現為 1–4 步、第 4 步確為「開票責任」 |
| F7 | 不修——已在池 `3dd30183` | **成立**（`pr-body-check --verify` 反查存在；delta 未動計數檔邏輯） |

---

### N1（major）`scripts/ops/patrol_linear.py:563-566` — 「取 body **第一個** `Pn ·` 定級」在真實池資料上把 **2 條真正的 P2 池項**從開票來源藏起來

```python
m = POOL_PRIORITY_RE.search(body)          # :562 全文第一個 match
if not m or m.group(1) not in ("1", "2") or len(prefix) < 8:
    continue                               # :564-565 首項不是 P1/P2 → 整則丟掉
```

池內慣例是**一則 comment 裝多個列點項**（`- P3 · …\n- P2 · …`）。「第一項定全則」會讓「首項 P3、後段有真 P2」的 comment 整則消失。

**具體失敗情境（live 實測，非推測）**：對真 LS-96（163 則 comment）跑 head 的 `pool_sources()` → 12 條；把定級換成「第一個 match 與**行首錨定** `^\s*(?:[-*]\s*)?P([1-4])\s*·` 取最小級」→ 14 條，**多出的恰好是 `LS-96#ca993eba` 與 `LS-96#d8634a08`，少列 0 條（strict superset）**：

- `ca993eba`：`- P3 · docs/API.md:1144 …` ／ `- P2 · **「mutation 自證」機械化**（LS-121 三輪 review 3 個 major 全是測試 gate 無鑑別力）… size:M；**再發生一次即升獨立票**`
- `d8634a08`：`- P3 · MediaUploadService 呼叫序列化…` ／ `- P2 · **影片暫存檔與孤兒 media 清理不完整**（孤兒 media 仍計入 families.storage_used_bytes）…`

兩條都**未**被任何 open／已結案票引用 id 前綴、池內也沒有對應的銷除公告 → 依票文 §範圍 1(b)「LS-96 池項中 **P1／P2 且尚未升票者**」，它們是必須列出的來源。後果：LS-144 併入後 harness lane（WIP=1）一空，動作清單印「→ 開票：lane:harness 空 n 輪（無候補）——來源候選：…」時就是少這兩條；`pool_sources()` 是唯一會機械提名池項的地方，沒被列＝永遠不會被提名升票（`ca993eba` 自己還寫著「再發生一次即升獨立票」）。

**接受此代價的前提在 live 上不成立**：全池掃描 163 則，**8 則是混級**（行首各項不只一級：`d9a3c88a`／`e381f653`／`b7cbee95`／`5cb5ddf3`／`ad1c4599`／`d351af55`／`d8634a08`／`ca993eba`），其中 2 則首項不是最高級。`bcb97555` 與 §7 寫的「池內慣例一則一級，混級少見」與資料不符（混級佔 5%，且潛在來源 14 條裡 2 條受害＝14%）。另外 handoff 證據行「池項 13→10」把 3 條全數描述成 false positive 的移除（公告＋P3 引用），實際上 **3 條裡有 2 條是真 P2**——這條申報需要更正。（池在我審查期間仍在長，156→163 則，所以絕對條數與 handoff 對不上是正常的；上面比的是同一次抓取下的集合差。）

**建議修法**（我在 live 驗過：strict superset、0 條誤丟；`62ecf8f1` 仍被 `POOL_ANNOUNCE_RE` 擋掉、`bcb97555`（P3+P3）仍不列、`abababab` 形狀的行中引用仍不升級）：

```python
ANCHORED_PRIORITY_RE = re.compile(r"(?m)^\s*(?:[-*]\s*)?P([1-4])\s*·")
levels = [int(m.group(1))] + [int(x) for x in ANCHORED_PRIORITY_RE.findall(body)]
if min(levels) not in (1, 2):
    continue
```
這正是 R1 F2 建議的「把前綴比對**收斂到入池行**」；「取第一個 match」是它的弱化近似。**自測請補一則「首項 `- P3 ·`、行首另有 `- P2 ·`」的混級樣本**——現行 ⑩ 的 `abababab` 只覆蓋「P3＋行中段引用 P1」，對這條修法沒有鑑別力：我把定級改回 R1 全文搜尋（M4）只有 1 條斷言紅，混級方向 0 覆蓋。

---

### N2（minor，PLAUSIBLE）`patrol_linear.py:62` — 正典字串是精確比對，票文寫法漂移即**漏擋**（方向與 F1 相反）

live 掃全 153 張票，票文含「Design gate」的 20 張：Backlog 的 UI Story（LS-19／20／22／23／24）與 LS-18／21 **全部**用正典 `**UI 票：需 Design gate**`（F1 的修法對現存 Story 全命中 ✓）；唯一變體是 **LS-17**（Done、lane:ui）寫 `**UI 票：需先過 Design gate**`。

**失敗情境**：日後有人沿 LS-17 的措辭開一張 Backlog UI Story → `needs_design_gate()` 回 False → 該 Story 直接被列為候補並 `→ save_issue … state=Ready` 派工，繞過 design gate（正是池項 `b2993155` 要治的事故）。24 張票裡出現過 1 次漂移，機率不是零；live 目前無實例（LS-17 已 Done），故 minor／PLAUSIBLE。
**建議**：容錯但仍排除否定句的 regex，`re.compile(r"\*\*UI 票：需.{0,6}Design gate\*\*")`（「不需 Design gate」「另票，需 Design gate」都不在 `**UI 票：` 粗體內，仍不命中）；或 patrol 另印一行「票文含 'Design gate' 但不含正典標記」的提醒。擇一，或入池亦可接受。

### N3（minor，PLAUSIBLE）`patrol_linear.py:64` — `POOL_ANNOUNCE_RE` 只錨 body 開頭 4 個非字元內

live 3 則公告（`62ecf8f1`／`ef4b8dec`／`a17f9935`）全部命中 ✓。但公告若寫成「2026-09-03 **銷除**：…（`P1 ·` …）」或「LS-144 已落地，銷除 comment `xxxx`…」（數字／英數開頭，`\W{0,4}` 不吃），就不匹配 → 該公告本身又會被當成未升票的 P1，**R1 F2 的原型事故重演**。無 live 實例。
**建議**：不必現在改；要收斂可改判「body 前 2 行內含 銷除／銷案 且帶反引號 comment id」，或直接記入 LS-96。

### N4（nit）`patrol_linear.py:515` — `design_gate_sources()` docstring 仍寫「票文標『需 Design gate』」

module docstring 與 `needs_design_gate()` docstring 都已改成「正典標記」，只剩這一處措辭停在裸子字串，容易讓下一個讀者以為還是舊語意。純措辭，隨 N1 一起改即可。

---

### 逐項查實

1. **F1 live**：`patrol-linear.sh --repo <worktree> --json` exit 0 → `lane:ui` `candidates:["LS-145"]`、`chosen:"LS-145"`、`pending_design:["LS-19","LS-20","LS-24"]`、actions `→ save_issue LS-145 cycle=1（scope+，取自 cycle 外）`／`→ save_issue LS-145 state=Ready cycle=1`——與 handoff 完全相符。LS-22／23 落在 `blocked_by_unresolved`（`classify_candidate()` 的 `blocked` 判定排在 `design_gate` 之前，解阻後仍會落回 `design_gate`，順序正確）。
2. **F2 live**：見 N1（`pool_sources()` 集合差已逐條核對原文，不是抽樣）。三則公告的開頭形狀已全部驗過（N3）。HEAD 列出的 12 條來源逐條回查 body 開頭，全是真「入池」項，無引述誤列。
3. **mutation ×4（scratch 複本，repo 未動，跑完 `diff` 確認還原）**：M1 還原 F1 裸子字串 → **5 紅**（⑩d 全部 4 條＋總結）；M2 拿掉公告跳過 → **2 紅**；M3 拿掉 canceled 排除 → **5 紅**（⑩ ui 來源／開票行／fail-soft 來源／⑩d design 來源＋總結）；M4 還原 `P([12])` 全文搜尋 → **2 紅**。四支樣本確實綁住行為，與 handoff 申報一致。唯一無鑑別力的方向是 N1 的混級。
4. **自測**：`patrol-linear.test.sh` → **87 ✓／0 ✗、exit 0**（實跑，非引述）。**`--json` 純淨**：stdout 1680 bytes 合法 JSON、**stderr 0 bytes**。**主 checkout clean**（我實跑產生的 `.claude/patrol-state.json` 已刪；提醒：`.gitignore` 規則隨本 PR 才進 main，併入前它在主 checkout 是 untracked）。**worktree clean**。
5. **PR body**：`pr-body-check.sh` 格式通過；`--verify` 全綠（LS-96 comment id 5 條反查存在、「已修」SHA 4 條皆在 PR commits 內）。`gh pr checks 250` 四綠（lint 12s／rules 2m55s／db 3m7s／ci 6m32s，run `33747411856`）。**scope 未擴**：delta 3 檔全落在票文範圍 1／3／4，無順手重構；全 PR `e5a9c23..72df412` 仍是 4 檔（`.gitignore` 那 2 行還在）——**`git checkout --` 事故沒有在最終 diff 留下殘缺**。
6. **四維度**：本 delta 無 Swift／DB 變更。race condition 只涉及計數檔 read-modify-write（F7，已入池 `3dd30183`，delta 未動）；效能面 `pool_sources()` 對 163 則 × 153 票做子字串掃描＋池內 O(n²) 互查，live 整趟 <10s，非瓶頸；平行化不適用（單次 CLI）。
7. **未審範圍（明講）**：session cron prompt 是 session-only、我讀不到，「§4-b 第 4 步與現行 cron 對齊」仍需 orchestrator 自行核對（同 R1）；`a31b8bd` 以前的部分只做交叉複核，未重審全部。

---

**結論**：F1／F3／F6 三條修法在 live 與 mutation 上都站得住，F4／F5／F7 的不修理由成立（F4 我另外用 live 資料證實了它的方向論證）。擋下的是 F2 修法的**過度收斂**：為了擋「P3 中段引用 `P1 ·`」而採用「第一項定全則」，在真實池上把 2 條符合票文 §範圍 1(b) 的 P2 來源整則藏掉，而據以接受此代價的「混級少見」與資料相反。修法約 3 行、我已在 live 驗過是 strict superset（0 條誤丟），補一則混級自測樣本即可送 R3。


### 2026-09-03T11:22:42.699Z · Chih-Lin Yeh · `16abb7e4`

orchestrator 2026-09-03 19:22：merge-review R2 `1f31ddfc` **REQUEST_CHANGES**——R1 F1–F7 全部收斂，但 F2 修法「取第一個 `Pn ·` 定級」過收斂：live 8 則混級 comment 中 `ca993eba`／`d8634a08` 兩條真 P2 被漏列（reviewer 以行首錨定取最小級實測 14 條 ⊃ 12）；3 minor（正典字串精確比對 fail-open、公告偵測只錨開頭、docstring）→ 令 R3（逐行取最小級＋混級樣本＋申報更正）。

### 2026-09-03T11:26:38.543Z · Chih-Lin Yeh · `53a60474`（回覆 949c0f16）

**更正（R3）**：上表 F2 證據「live 來源 13 → 10」的申報不準——移除的 3 條裡只有 `62ecf8f1`（銷除公告）是 false positive，另 2 條 `ca993eba`／`d8634a08` 是首項 `- P3 ·`、次項 `- P2 ·` 的混級 comment，屬票文 §範圍 1(b) 必列的真 P2，被「第一個 `Pn ·` 定全則」誤藏（merge-review R2 `1f31ddfc` N1）。R3 改為逐行取最小級，數字以 R3 handoff 為準。

### 2026-09-03T11:39:26.529Z · Chih-Lin Yeh · `a01a0b43`

## handoff R3（merge-review R2 `1f31ddfc` 處置）

**Ticket**：LS-144；PR #250 → main；head **`b65faab`**（R3 三段：`22dbf71` fix／`164bcdd` test／`b65faab` docs；R2 head `72df412`）。CI run `33750366448`：`lint` pass 14s／`rules` pass 2m57s／`db` pass 3m8s／`ci` pass 5m11s（`gh pr checks 250 --watch` 前景等完；`headRefOid` 對 `b65faab59…`）。

**Finding 處置表**

| # | 等級 | 處置 | 證據 |
|---|---|---|---|
| N1 「第一項定全則」藏掉混級 comment 的真 P2（`ca993eba`／`d8634a08`） | major | 已修 `22dbf71` `patrol_linear.py:67`（`POOL_ITEM_LINE_RE` 行首列點）＋`:581`（等級＝首個 `Pn ·` 與各行首 `- Pn ·` 的最小級、摘要取該項） | live（165 則）：來源 12 → **14**，`ca993eba`／`d8634a08` 以 P2 列出，`62ecf8f1`／`b2993155` 仍不列；混級 8 則（`d9a3c88a`／`e381f653`／`b7cbee95`／`5cb5ddf3`／`ad1c4599`／`d351af55`／`d8634a08`／`ca993eba`）與 reviewer 清單一致。樣本 cdcdcdcd（`164bcdd` `patrol-linear.test.sh:727`：首項 `- P3 ·`、次項 `- P2 ·`）→ 以 P2 列出（`:799` 兩條斷言）；mutation 改回第一個定級 → **3 條紅** |
| R3 順修（N1 live 對帳時發現）：非公告池項提到別則 id＋「銷案」字樣即算銷除 | — | 已修 `22dbf71` `:586`：只有公告（`is_pool_announcement()` `:557`）能銷除別則；`POOL_LIFTED_WORDS` 移除 | 我自己的 `bcb97555` 更正文第 3 行提到 `ca993eba`／`d8634a08`＋「銷案」，R3 第一版把這兩條又藏掉（live 12）；修後 live 恢復 14。樣本 efefefef（`:728`，第 3 行提到 aaaa＋「銷案」）→ aaaa 仍列；mutation 改回任何 comment 皆可銷除 → **2 條紅** |
| N2 正典字串精確比對，LS-17「需先過 Design gate」變體會漏擋（fail-open） | minor | 已修 `22dbf71` `:65` `DESIGN_GATE_RE = \*\*UI 票：需.{0,6}Design gate\*\*`（粗體內容忍 ≤6 字；「不需」「另票，需」不在粗體形內仍不命中）＋`:365` docstring | 樣本 LS-983「**UI 票：需先過 Design gate**」歸待Design、LS-984「**UI 票：不需 Design gate**」照常列候補（`164bcdd` `:889-924`）；mutation 改回精確字串 → **4 條紅**；live ui lane 不變（candidates `['LS-145']`、chosen LS-145、pending_design LS-19／20／24） |
| N3 `POOL_ANNOUNCE_RE` 只錨 body 開頭 | minor | 已修 `22dbf71` `:70`＋`:557`：前 2 行含「銷除／銷案／已升票」即公告（依 orchestrator 指示） | 樣本 dddd 改為日期／票號起頭仍被跳過；mutation 改回錨開頭 → **2 條紅**。live 判為公告 4 則：`62ecf8f1`／`ef4b8dec`／`a17f9935` 正確、`3dd30183` 誤判（第 2 行討論這套判定本身；P3、無實害）——雙向盲區記 §7 `b65faab` `COLLABORATION.md:279`＋記入待辦池 LS-96 `72a74d85` |
| N4 `design_gate_sources()` docstring 措辭 | nit | 已修 `22dbf71` `:521` | — |

**申報更正**：R2 handoff「池項 13 → 10」不準——3 條移除裡 2 條是真 P2（`ca993eba`／`d8634a08`），已在 `949c0f16` 下回覆更正（`53a60474`）；`bcb97555` 第 2 條「混級少見」改為實況（163 則中 8 則、5%）並銷案；§7／§4-b／§5-b 措辭同步（`b65faab` `:124`／`:177`／`:279`）。

**mutation**（檔案備份復原，不用 `git checkout --`；最終檔與備份 `cmp` 一致）：N1 → 3 紅（池項清單＋混級斷言＋總結）、N2 → 4 紅（⑩d 三條＋總結）、N3 → 2 紅、只有公告能銷除 → 2 紅。
**自測**：`patrol-linear.test.sh` 89 ✓／0 ✗、exit 0（R2 87＋2）。live `--json` stderr 0 bytes；主 checkout clean（計數檔／`__pycache__` 每次跑完即刪）。PR body 已追加 R3 處置表（`pr-body-check.sh` 格式＋`--verify`：LS-96 id 6 條反查存在、「已修」SHA 9 條皆在 PR commits 內）。
**informational 新增**：記入待辦池 LS-96 `72a74d85`（公告判定前 2 行的雙向盲區）。
**待 orchestrator**：merge-review R3；併 main 後 back-merge；§4-b 第 4 步對齊 session cron。

### 2026-09-03T11:40:55.412Z · Chih-Lin Yeh · `ec12f5b6`

orchestrator 2026-09-03 19:40：R3 handoff `a01a0b43` 收到——head `b65faab`（N1 最小級定級 live 12→14 含兩條真 P2；順修「只有公告能銷除別則」；N2 `DESIGN_GATE_RE` 容忍「需先過」；N3 前 2 行公告偵測（`3dd30183` 誤判入池 `72a74d85`）；N4；申報更正 `53a60474`；mutation 全部備份復原、自測 89）；CI 四綠 → 派 merge-review **R3**。

### 2026-09-03T11:54:11.527Z · Chih-Lin Yeh · `969cde39`

## merge-review R3 — PR #250（head `b65faab`）

**判定：APPROVE**（0 blocker／0 major；2 minor(PLAUSIBLE)／2 nit，皆不擋併入，請逐條處置）

審查範圍：`git diff 72df412..b65faab`（3 檔 72+/36-：`patrol_linear.py`／`patrol-linear.test.sh`／`COLLABORATION.md`）＋ R2 `1f31ddfc` N1–N4 與 R3 順修逐條複核。head 與 `gh pr view 250 --json headRefOid` 一致（`b65faab59…`，審前審後各對一次）。worktree 唯讀未改任何檔案；mutation 全在 scratch 複本做、最終與 worktree 檔 `cmp` 一致；我實跑產生的 `.claude/patrol-state.json` 已刪，主 checkout（`main`）與 worktree 皆 clean、無 `__pycache__` 殘留。

---

### R2 N1–N4＋R3 順修複核

| # | 申報 | 複核結果 |
|---|---|---|
| N1（major）逐行取最小級 | 已修 `patrol_linear.py:67`／`:581` | **成立**。同一份 live 快照（LS-96 165 則、open 24＋closed 129）跑兩版 `pool_sources()`：`72df412`=**12** → `b65faab`=**14**，差集恰為 `ca993eba`／`d8634a08`（皆 P2），**反向差集 0**（strict superset）。`62ecf8f1`（公告）／`b2993155`（被公告銷除）皆不列 ✓。混級 8 則＝`d9a3c88a`／`e381f653`／`b7cbee95`／`5cb5ddf3`／`ad1c4599`／`d351af55`／`d8634a08`／`ca993eba`，與我 R2 清單**逐字一致**。mutation（改回 `level = int(m.group(1))`）→ **3 紅**（池項清單＋混級斷言＋總結），與申報相符 |
| R3 順修 `:586` 只有公告能銷除 | 已修 | **成立且必要**。隔離實驗（R3 的定級邏輯＋R2 的 `POOL_LIFTED_WORDS` 銷除規則）＝12 條，缺的正是 `ca993eba`／`d8634a08`——沒有這條順修，N1 的修法在 live 上等於沒生效。反向風險（見下方逐項 2）實測為 0 |
| N2（minor）`:65` `DESIGN_GATE_RE` | 已修 | **成立**。正負樣本實測見下方逐項 3；live ui lane 不變；mutation（改回 `re.escape` 精確字串）→ **4 紅** |
| N3（minor）`:70`／`:557` 公告前 2 行 | 已修（依 orchestrator 指示） | **實作與申報相符**，但這個方向本身留下一條新的靜默藏匿通道 → **M1（minor, PLAUSIBLE）**。mutation（改回 `^\W{0,4}(銷除|銷案)`）→ **2 紅** |
| N4（nit）`design_gate_sources()` docstring | 已修 `:522` | **成立**（`grep` 全樹：`POOL_LIFTED_WORDS`／`DESIGN_GATE_MARK` 0 筆殘留引用，無孤兒常數） |

---

### M1（minor，PLAUSIBLE）`scripts/ops/patrol_linear.py:70`／`:557`／`:586` — 公告偵測的「前 2 行」窗同時決定**自己不被列**與**有權銷除別則**，寬到會反向藏掉真 P1／P2

`is_pool_announcement()` 現在有兩個身分：跳過自己（`:574`）＋授權銷除別則（`:586`）。判定只是「前 2 行出現 銷除／銷案／已升票」。

**live 證據**：
- 判為公告 4 則：`62ecf8f1`／`ef4b8dec`／`a17f9935`（真）＋`3dd30183`（誤判，第 2 行在討論這套判定本身）。**把窗改成只看第 1 行**：公告回到 3 則（誤判消失）、`pool_sources()` 仍是**同一組 14 條**、自測仍 **89 ✓／0 ✗**——也就是說第 2 行在 live 上**只帶來那 1 個誤判、沒帶來任何真公告**，而且**現行自測沒有任何樣本能區分 1 行／2 行**（我把 `[:2]` 改成 `[:1]` 全綠；改成整份 body 才有 1 紅——上界有釘、寬度沒釘）。
- 銷除力這一半的脆弱度：`bcb97555`（就是 R2 N1 事故的來源）的「銷案」字樣落在**第 3 行**。同一則 comment 只要把那條更正列成第 2 項，`ca993eba`／`d8634a08` 這兩條真 P2 就會**再次被藏**——與 R2 判 major 的失效類別完全相同。
- 誤判公告的連帶效果：它提到的每個 comment id 前 8 碼都被視為已銷案。`3dd30183` 提到 `f960d843`（P3 且已被票文引用），所以目前無實害；但 live 165 則中有 **29 則**帶跨則 id 引用。

**具體失敗情境**：日後入池「入池 …（LS-xxx 收尾）：\n- P2 · 「已升票」判定改認完整 UUID…」——第 2 行含「已升票」→ 該 P2 **永不被提名**（`pool_sources()` 是唯一機械提名處），且它交叉引用到的其他 P1／P2 一併被當成已銷案。無 live 實例故標 PLAUSIBLE／minor，不擋併入。

**建議**（擇一）：(a) 窗收成第 1 行——live 已驗零成本（公告 4→3、來源仍 14 同集合、自測 89 全綠）；(b) 維持 2 行但補一支「公告字樣只在第 2 行」的 fixture 把寬度釘住；(c) 銷除力與跳過力拆開（例如銷除要求公告字樣與反引號 id 同一行——`62ecf8f1` 第 1 行同時有「**銷除**」與 `b2993155`，仍成立）。註：池項 `72a74d85` 提的候選修法「公告需同時帶反引號 comment id」對 `3dd30183` **無效**（它第 2 行本來就帶 `f960d843`），若採不修請把這點補進池項，別讓下一個人照著改了還是沒解。

### M2（minor，PLAUSIBLE）`:70`／`:586` — 銷除字彙由 4 個（銷除／銷案／升為／已升）收成 3 個（銷除／銷案／已升票），「已升為」不再具銷除力

R3 的銷除力集合是 R2 的**子集**（方向是「多列」＝噪音，不是藏，這點安全）。live 三則真公告都以「**銷除**：」「銷案：」起頭（`ef4b8dec` 雖寫「已升為 LS-104」但第 1 行有「銷除」），故無實害。**失敗情境**：日後把升票公告寫成「待辦池 comment \`xxxx\` 已升為 LS-160 scope」而不寫銷除／銷案 → 目標不被銷除（多列一條）＋該公告若引述 `P1 ·` 會**自己被列為來源**（R1 F2 的原型復發）。自測也沒有這型 fixture。**建議**：把「升為／已升」補回 `POOL_ANNOUNCE_RE`，或在 §5-b 明訂銷除公告一律以「銷除／銷案」起頭（規則就要有對照的 gate），或入池。

### n1（nit）`:588` — 摘要取「body 中第一個 \`P<level> ·\`」而非「最小級那個行首列點」

若某則最小級來自行首 `- P2 ·`，而 body 更早處有行中引用「P2 ·」，`title` 摘要會取到引用文（`why`／排序仍正確）。**live 14 條全部取對**（6 條取自行首列點、8 條取自 `入池…：Pn ·` 首個 match，逐條核過），純顯示問題。

### n2（nit）`:65` `.{0,6}` 容忍窗

方向是 fail-closed（多鎖進待Design＋多列一條 design 來源，人看得到），可接受。附記：`**UI 票：需要跳過 Design gate**` 這種造作寫法也會命中（無 live 實例），僅記錄。

---

### 逐項查實

1. **N1**：見上表。另核 `ca993eba`／`d8634a08` 的「未升票」前提：全 153 張票（open＋closed）票文／標題皆無此前綴，池內也無**公告**提到它們（唯一提到的 `bcb97555` 不是公告，且它的「銷案」指的是它自己那條 P3）。
2. **R3 順修的反向風險（要求抽查已銷案項）**：我不抽樣，直接枚舉 live **全部**公告→目標關係：`62ecf8f1`→`b2993155`（P1，**R3 下仍被銷除** ✓，這是唯一一條銷除規則實際承載的）；`ef4b8dec`→`c29eb808`（目標整則沒有 `Pn ·`，本來就不列）；`a17f9935`→未提到任何 comment id（銷的是 LS-115 R1 i3）；`3dd30183`（誤判公告）→`f960d843`（P3 且已被票文引用）。**沒有任何已銷案項因為這條順修回到清單**（隔離實驗：hybrid 12 條 ⊂ R3 14 條，R3 多出的兩條就是 `ca993eba`／`d8634a08`，hybrid-only = 空）。mutation（改回「任何 comment 提到 id＋銷除字樣即銷除」）→ **2 紅**。
3. **N2**：命中表 —— 命中 `**UI 票：需 Design gate**`／`**UI 票：需先過 Design gate**`／`**UI 票：需先通過 Design gate**`；**不**命中 `**UI 票：不需 Design gate**`／`非畫面票（不需 Design gate）`／`拆後端先行（不需 Design gate）；UI 端（另票，需 Design gate）`／`**UI 票：不需先過 Design gate**`／`**UI 票：無需 Design gate**`／裸體無粗體形。live 全 153 張票逐張比對 R2 vs R3 `needs_design_gate()`：**唯一差異是 LS-17（Done）False→True**，Done 不進候補計算 → **live ui lane 完全不變**（實跑 `--json`：candidates `['LS-145']`、chosen `LS-145`、pending_design `['LS-19','LS-20','LS-24']`、actions 兩條 `save_issue LS-145`；ui 現在 wip 0/2 但有候補故不印開票行）。
4. **N3 方向評估**：fail-open（漏判公告）的代價＝該公告自己被列＋目標不被銷除 → **多列、噪音，orchestrator 看得到**；fail-closed 誤判（真池項被當公告）的代價＝**整則靜默消失＋它引用的 id 一併被視為已銷案** → 與 R2 判 major 的失效類別相同。也就是說「誤判公告＝少列一條 P3」這個描述只在 `3dd30183` 這個實例成立，不是這條規則的一般後果。目前無實害（唯一誤判是 P3、目標也是 P3），且已記 §7（`COLLABORATION.md:279` 雙向盲區）＋池 `72a74d85`，**接受、不擋併入**；但既然收窄到第 1 行在 live 上是零成本（見 M1），值得下一次順手做掉。
5. **其餘**：自測實跑 `patrol-linear.test.sh` → **89 ✓／0 ✗、exit 0**（非引述）。**mutation 我自己重做 4＋2 支**：N1→3 紅、只有公告能銷除→2 紅、N2→4 紅、N3→2 紅；額外探針：公告窗改整份 body→1 紅（上界有釘）、改第 1 行→**0 紅**（寬度沒釘，見 M1）。live `--json`：exit 0、stdout 1766 bytes 合法 JSON、**stderr 0 bytes**。**PR body**：`pr-body-check.sh` 格式通過；`--verify`（在 worktree 內跑）全綠——「已修」SHA 9 條皆在 PR commits 內、LS-96 id 6 條反查存在。`gh pr checks 250` **四綠**（lint 14s／rules 2m57s／db 3m8s／ci 5m11s，run `33750366448`）。**scope 未擴**：delta 3 檔對應票文範圍 1／3／4，三段 commit 各只動一個檔（`22dbf71` code／`164bcdd` test／`b65faab` docs），無順手重構；全 PR 仍是 4 檔，**`.gitignore` 那 2 行完好**（`.claude/patrol-state.json` 註解＋規則）。刪除的測試行逐條核過**無弱化**（`dddd`／`abababab` 都以更嚴形式回填，⑩d 斷言是拆開＋擴充）。
6. **四維度**：本 delta 無 Swift／DB 變更。**race**：計數檔 read-modify-write 未動（已在池 `3dd30183`）。**效能**：`pool_sources()` 是 165 則 × 153 票子字串掃描＋池內 O(n²)，且 `:586` 把 `is_pool_announcement()`（含 `splitlines()`）放進內層迴圈重算——live 整趟 <10s，非瓶頸；池若日後破千則先把公告集合預算成 set 即可，現在不必動。**平行化**不適用（單次 CLI）。
7. **未審範圍（明講）**：session cron prompt 是 session-only、我讀不到，「§4-b 第 4 步與現行 cron 逐字對齊」仍需 orchestrator 自核（同 R1／R2）；`72df412` 以前的部分只做交叉複核，未重審全部。

---

**結論**：R2 的 1 major＋3 minor 全部收斂，而且 N1 的修法在 live 上確實是 strict superset（12→14，0 誤丟），R3 自己發現的順修（只有公告能銷除）是讓 N1 真正生效的必要條件、且不會放回任何已銷案項（全枚舉核過，非抽樣）。剩下的是兩條沒有 live 實例的 PLAUSIBLE minor 與兩條 nit，其中 M1 附一個已在 live＋自測上驗過的零成本收窄方案，建議下一輪順手或入池。**APPROVE**。


### 2026-09-03T11:55:56.242Z · Chih-Lin Yeh · `e64767c0`

orchestrator 2026-09-03 19:55：merge-review R3 `969cde39` **APPROVE**（N1 live 12→14 兩條真 P2 回列、順修 `:586` 必要且全部公告→目標關係枚舉無誤銷；N2 負樣本齊；N3 前 2 行窗留一條新通道＝M1 minor）→ PR #250 併入 **main `c03465d`**（status 已貼）→ back-merge PR main→development 開立中 → sweeper → retro → cleanup → Done。informational M1（公告窗收 1 行）／M2（`升為`／`已升` 失去銷除力）／n1／n2 入池 LS-96。session cron 第 4 步對齊 §4-b 由 orchestrator 自核。

### 2026-09-03T12:02:20.126Z · Chih-Lin Yeh · `b3d1fa0d`

## dead-code sweep — LS-144（main `c03465d`）

（dead-code-sweeper 產出、orchestrator 代貼，20:01）**結論：未發現本 feature 引入的死碼；不需開票。**

範圍：`e5a9c23..b65faab` 9 commits、4 檔（`patrol_linear.py`／`patrol-linear.test.sh`／`COLLABORATION.md`／`.gitignore`）；自測在 main 實跑 89 ✓／0 ✗，主 checkout 跑後 clean、無 `patrol-state.json` 殘留。
1. **三輪修法孤兒**：`POOL_LIFTED_WORDS`（R3 移除）、`DESIGN_GATE_MARK`（R2 改 `DESIGN_GATE_RE`）、reviewer 建議名 `ANCHORED_PRIORITY_RE` 全樹 0 筆；R2「第一個 `Pn ·`」是內聯行、R3 就地改寫無舊分支；`POOL_PRIORITY_RE`（`:66`）與 `POOL_ITEM_LINE_RE`（`:67`）互補非冗餘（前者抓非行首單項如樣本 `aaaaaaaa`）；53 個函式皆有呼叫點。
2. **樣本鑑別力**：⑩／⑩d 11 組樣本逐一對照鎖定分支（`bbbbbbbb`／`eeeeeeee` 兩條「已升票」路徑、`abababab`／`cdcdcdcd` 互補、`efefefef` 鎖 `bcb97555` 事故、`LS-983`／`LS-984` 容忍窗上界與否定句），無重疊到「所有 mutation 皆不紅」者；M1 公告窗 1 行／2 行無樣本區分＝覆蓋不足非冗餘（已入池 `72a74d85`）。
3. **docstring／§7 ↔ 程式**：六處 COLLABORATION 改動逐條與 `:581`／`:586`／`:65`／format 段號一致；舊「第 5 步／第 4 步」編號無殘留。module docstring 未寫 R3「只有公告能銷除」細節＝摘要省略，不建議修。
4. **`.gitignore`**：兩行對應 `STATE_FILE_REL`（`:72`）與 `load_state`／`save_state` 唯一讀寫點，產物實際產生且不入版控。

盲區：未逐一重跑 mutation（三輪 review 已做 4＋2 支）；未掃 scope 外既有邏輯；session cron 對齊仍由 orchestrator 自核（已於 19:57 重建 cron `6afb312a` 對齊 §4-b 第 4 步）。

### 2026-09-03T12:02:39.254Z · Chih-Lin Yeh · `f8e8ea6d`

## retro — LS-144（orchestrator lesson learning，20:01）

**落地**：PR #250 → main **`c03465d`**（19:55）；back-merge PR #255 main→development 等 CI 後併；session cron **已重建 `6afb312a`**（19:57）對齊 §4-b 第 4 步：動作清單機械印「→ 開票：lane:<x> 空 n 輪——來源候選」時當輪開票或 notes 寫理由＋向使用者要裁決；連續空 ≥2 輪 `⚠` 不得回「無異常」；另保留 LS-118 視覺驗收暫緩、待核可稿註記、cycle 回顧、`date` 先跑規則。

**做對的**：三輪 review 每輪都用 live Linear 資料實跑（不是只看 fixture）才抓到 F1（否定句誤判 LS-145、本 PR 迴歸）、F2（銷除公告算 P1）、N1（混級漏兩條真 P2）——**規則型 harness 的 review 必須對 live 資料跑一次**，fixture 全綠不代表對真實票文有效；agent R3 自己對帳發現順修（自己的更正文被舊規則藏掉）也是同一原理。mutation 每輪重做且用檔案備份復原（R2 教訓：`git checkout --` 沖掉未 commit 修改）。

**harness 改善／工具缺口（皆已入池 LS-96）**：`bcb97555` `wip<limit` 變體（在飛 1/2 但候補全被擋時不印開票）；`72a74d85` 公告偵測前 2 行窗雙向盲區；`9905feb7` M1（窗收 1 行零成本）／M2／n1／n2；`3dd30183` informational 4 條。**方向性教訓**：本票兩處 fail-open 修成 fail-closed 都在 review 被要求補「反向」負樣本——新增規則時正負樣本要成對開，不然 mutation 只紅一邊。

**orchestrator 自身**：票文寫作時三 lane 空、實跑時四 lane 皆有在飛，「→ 開票」行本輪從未真的印出——機制的第一次實戰要等下次 lane 真空；巡檢時若看到 `⚠` 我必須回報而非「無異常」。cron prompt 是 session-only，每次 session 重建都要對照 §4-b 最新模板（patrol-mechanism 記憶已更新）。

**待使用者**：無。

### 2026-09-03T12:13:32.559Z · Chih-Lin Yeh · `c67e30a6`

orchestrator 2026-09-03 20:13：back-merge PR #255 main→development 併入 **`cd638ea`**（status `back-merge: no content diff（hotfix PR #250 head b65faab）` 已貼；`git diff main..development` 對 `patrol_linear.py`／COLLABORATION 為空）→ worktree `cleanup-merged.sh --apply` 已清 → **Done**。session cron `6afb312a` 已對齊 §4-b 第 4 步；LS-96 `b2993155` 已銷除。harness lane 接 LS-154。

