# LS-34 Harness：run.sh 全形括號在 bash 3.2 的 unbound variable bug ×3

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Low |
| 標籤 | — |
| Cycle | — |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-22T17:02:46.833Z |
| 開始 | 2026-08-22T20:06:56.205Z |
| 完成 | 2026-08-22T21:26:01.760Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-34/harnessrunsh-全形括號在-bash-32-的-unbound-variable-bug-3 |
| 關係 | related→LS-15, related→LS-6, related→LS-33, related←LS-40, related←LS-37, related←LS-36 |

## 描述

來源：[LS-33](https://linear.app/little-sprout-app/issue/LS-33/taskls-18-%E5%BE%8C%E7%AB%AF-%E9%82%80%E8%AB%8B%E5%8A%A0%E5%85%A5%E7%94%B3%E8%AB%8B%E8%88%87%E5%AF%A9%E6%A0%B8%E7%9A%84-schemarpcrls) handoff（依手術式修改原則未順手修）。`supabase/tests/run.sh` 第 58、133、134 行：macOS bash 3.2 會把全形括號吃進變數名，`set -u` 下**失敗路徑**以 `unbound variable` 中止並蓋掉真正的錯誤訊息（gate 仍非 0 結束、不漏放，但診斷消失）。修法比照 [LS-33](https://linear.app/little-sprout-app/issue/LS-33/taskls-18-%E5%BE%8C%E7%AB%AF-%E9%82%80%E8%AB%8B%E5%8A%A0%E5%85%A5%E7%94%B3%E8%AB%8B%E8%88%87%E5%AF%A9%E6%A0%B8%E7%9A%84-schemarpcrls) 已修的同類位置。升級為修復項（PR #36 review F6：「用中文迴避」是前饋無 gate 的形態）：CI rules 破壞性偵測器於 grep 前先剥除 `--` 註解行——此項屬 .github/ harness，走 hotfix；原記錄：偵測器誤判（[LS-33](https://linear.app/little-sprout-app/issue/LS-33/taskls-18-%E5%BE%8C%E7%AB%AF-%E9%82%80%E8%AB%8B%E5%8A%A0%E5%85%A5%E7%94%B3%E8%AB%8B%E8%88%87%E5%AF%A9%E6%A0%B8%E7%9A%84-schemarpcrls) 實測踩到，以中文措辭避開）——偵測器不分辨註解為已知限制，migration 註解避免出現英文破壞性關鍵字，寫進 COLLABORATION §6 一行。走 hotfix 流程，低優先可共車。

追加（[LS-33](https://linear.app/little-sprout-app/issue/LS-33/taskls-18-%E5%BE%8C%E7%AB%AF-%E9%82%80%E8%AB%8B%E5%8A%A0%E5%85%A5%E7%94%B3%E8%AB%8B%E8%88%87%E5%AF%A9%E6%A0%B8%E7%9A%84-schemarpcrls) **糾正 orchestrator 前提）**：`60_default_privileges.sql` 只列舉 private schema 函式，**不會逐支登記 public 的 SECURITY DEFINER RPC**——漏 grant 時呼叫端會炸但無測試指出原因。補一段列舉式檢查：public 的 definer 函式必須逐支登記授權（現有 7 支起始清單在 80_ §11）。
**流程更正**：本票所有檔案（[run.sh](<http://run.sh>)、60_）屬 supabase/tests/——**不在 harness 例外清單，走正常 feature 流程**（前述「走 hotfix」作廃）。
**追加（**[LS-33](https://linear.app/little-sprout-app/issue/LS-33/taskls-18-後端-邀請加入申請與審核的-schemarpcrls) **修復輪建議）**：外鍵 RI 索引缺漏做成 60_ 式列舉檢查（掃全 schema 每個 FK 的反向索引）——取代單點同義反覆式斷言；附帶知識：PLAN §5 禁令的實際危險形狀是 aggregate 子查詢（PG17 會上拉等值 EXISTS，M20 誠實非發現），50_ 偵測器註解可同步。

## 留言（6）

### 2026-08-22T20:16:00.003Z · Chih-Lin Yeh · `4d85d8e4`

**Hotfix 部分（PR #37）review：APPROVE**（merge-reviewer, opus）

核心宣稱獨立重現通過（LS-33 誤判形狀修復、真指令零漏抓、YAML/quoting 無展開時機問題）。2 minor 已處理：F1 引號內 `--` 後同行關鍵字會漏掉（人造輸入；採文件補洞——§7 已知限制擴寫，保留行尾註解剝除）；F2 PR body 誤稱 CI 是 pipefail 模式（實為 `bash -e {0}`；reviewer 兩模式重跑 12 案例 byte-identical，僅措辭更正）。

**F4 follow-up 記入本票**：`supabase/migrations/20260823010000_join_approval.sql` 內留有 LS-33 時代的偵測器 workaround 註解（六行、宣稱偵測器不分辨註解——PR #37 後已不成立）。不可在 hotfix 動 migrations；本票 feature 分支若順路清理，新註解措辭仍須避開英文關鍵字（該分支 head 的 ci.yml 尚未含剝註解修復，workflow 跟 head 走）；否則留收尾時開小票。

另（F3, informational, 前存）：偵測器未濾 `+++` diff 頭，migration 檔名含關鍵字會自觸發；`|| true` 會把 git diff 本身的失敗吞成 0。皆前存、不在本票範圍。

### 2026-08-22T20:27:42.658Z · Chih-Lin Yeh · `5c942763`

**Feature 部分 handoff（ios-dev）**：四項全完成，PR #41 → development（commit e14ec9a）。

已驗證：`supabase db reset` → `run.sh` 全綠（含新增兩段）；四個負向路徑各演練一次（註解 create_invite grant→60_ FAIL、註解 resolved_by 索引→65_ FAIL 指名 constraint、docker exec begin/rollback 驗白名單雙向、`/bin/bash` 3.2.57 實機重現→修復→弄壞 owner_guard 觸發 133-134 分支見 rc=3 非 unbound variable），每次演練後還原重 reset 確認回全綠。

兩個發現：
1. **票面認知落差**：「取代現有單點 FK 索引斷言」——全 repo 無此既有斷言（LS-33 review 即記「F5 沒有對應 mutation」），故為新增非取代。
2. **65_ 掃出 11 個 LS-6～LS-15 遺留 FK 缺反向索引**（與本票無關的既有債）：本票不動 migrations，列為附反向對照的暫置白名單 → 已開 **LS-36** 補索引（priority Medium）。
3. 60_ 第 8 段白名單實為 8 支：LS-33 的 7 支＋先前漏列的 `register_device_token`。

→ In Review，merge-reviewer 審 #41 中。（hotfix 部分 PR #37 已於稍早併入 main 並 back-merge 完成，見前一則 comment。）

### 2026-08-22T20:43:08.227Z · Chih-Lin Yeh · `11e98428`

**PR #41 review：REQUEST_CHANGES**（merge-reviewer, opus；亮點：無 supabase stack 仍以 CI db 綠燈日誌＋拋棄式 PG 對 14 種索引形狀實測完成審查）

**Major ×2**：F1 PLAN §5 措辭修改實質把規則「必須」降成「建議」且「等值形不是實害」說死了——reviewer 實測 RLS qual 的等值 IN 在 work_mem 不足時退化 `(SubPlan 1)`＋loops>1、會被 50_ 判 FAIL（文件祝福的寫法 gate 會擋）；修法＝恢復祈使句規則本體、細緻度進理由子句、hashing 說成規劃器選擇非保證。F2 60_ §8 與 80_ §11 兩個登記點並存會漂移、80_ :1059-1062 註解已成假話，且新登記點**比舊的弱**（缺 `prosecdef＋search_path=''` 斷言；register_device_token 的 search_path 至今零覆蓋）；修法＝斷言併入 60_ §8、80_ §11 縮成指路註解——正好回應票面「併入單點斷言」原意。

**Minor ×4 一併吸收**：F3 60_ 雙陣列白名單有 fail-open 縫（text loop 漏加無人發現）→單一來源導出；F4 65_ 缺第三向對照（殭屍白名單條目會無聲豁免同名重建 FK）；F5 判定式三個潛伏誤判（INCLUDE 非鍵欄、invalid index、AM 不設限）；F6 判定式複製兩份→抽單一查詢。F7 可選（run.sh 證據檔頭措辭同步）。

**Reviewer 正面確認**：run.sh 全 repo `\$var＋非 ASCII` 掃描零殘留、bash 3.2.57 實機複現＋修復正確；65_ 前綴涵蓋／欄序不敏感／partial／expression／unique 底層索引判定全對（14 形狀實測）；register_device_token「先前漏列」宣稱屬實（8 支不多不少）；禁區零變更。

已派回原 ios-dev 修 F1-F6（F7 順手），含三個新負向演練要求。

### 2026-08-22T21:01:32.232Z · Chih-Lin Yeh · `8a2cc8e8`

**PR #41 定點複驗：APPROVE**（merge-reviewer, opus）

F1/F2 兩 major 實質修復確認：PLAN §5 規則本體回到祈使句、零新語義鬆動（逐句比對）；60_ §8 斷言 ⊇ 舊 80_ §11（逐字相同的 prosecdef＋search_path 條件、覆蓋 8 支 > 舊 7 支）、80_ 殘留文字與事實矛盾句全數消失、NULL proconfig 三值邏輯實測無漏接。F3-F7 以實測確認：17 形狀矩陣重跑（3 誤判全翻正、`(x,y) include (z)` 證明 guard 無 over-reject、其餘 12 形狀零回歸）；65_ 三向負向演練全部指名 FAIL（漏登記／過期／殭屍）；CI 全綠且真實 schema 上 8 支 RPC search_path 硬化＋11 項白名單仍精確。

3 nit 不阻擋：①80_ §11 段落標題與內容脫節（一行）②LS-33 mutation 觀察隨舊段刪除未搬遷（一行）③65_ 零外鍵極端態三段靜默通過（純資訊，preflight 會先紅）。①②已派回 ios-dev 順手清，commit 後即併。

票面「取代單點斷言」語義覆核（reviewer 無 Linear 工具，由 orchestrator 覆核）：票面原句「取代單點**同義反覆式**斷言」出自 LS-33 修復輪建議脈絡，F2 已將 80_ §11 併入 60_ §8——無論原指哪項，「找到它、刪掉或併入」皆已滿足。

### 2026-08-22T21:19:14.281Z · Chih-Lin Yeh · `59b177b6`

**QA（test branch, promote #42）：PASS**——`/bin/bash` 3.2.57 下 run.sh 全綠；負向演練：暫時移除 60_ §8 白名單一條 → 可讀 FAIL 訊息（指名 withdraw_join(uuid)＋登記指引，非 unbound variable）→ 還原重跑全綠。→ 收尾儀式後 Done。

### 2026-08-22T21:25:46.049Z · Chih-Lin Yeh · `a24f9a83`

**收尾儀式**：dead-code-sweeper 無發現（run.sh 原地替換、65_ 單一判定式無舊版殘留、80_ §11 搬遷乾淨無死變數、glob 掃描自動撿起新測試檔）。Lesson：
1. 「宣稱＝實測」要含**宣稱的措辭本身**——PR body 誤稱 CI 為 pipefail 模式被 review 抓到（F2），驗證聲明也是可被審的產物。
2. 列舉式白名單 check 上線即產出（掃出 11 個真實 FK 索引債→LS-36）——這類 gate 的價值在「第一次跑」就兌現。
3. merge-reviewer 無 supabase stack 仍以拋棄式 PG container＋形狀矩陣完成實測級審查——資源受限下的替代證據鏈技術，值得沿用。
4. 票面認知落差（「取代單點斷言」實為 80_ §11 而非 FK 斷言）——開票時引用來源 handoff 的原句要附檔案位置，降低轉述漂移。
→ Done。

