# LS-65 Harness：push-gate 便宜檢查（契約對帳／錯誤碼／分級／票號／衝突預檢）前移到 xcodebuild 之前

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Low |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-24T23:03:35.348Z |
| 開始 | 2026-09-01T12:45:28.315Z |
| 完成 | 2026-09-01T14:08:23.999Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-65/harnesspush-gate-便宜檢查契約對帳錯誤碼分級票號衝突預檢前移到-xcodebuild-之前 |
| 關係 | related→LS-59, related→LS-83, related→LS-70, related→LS-100, related→LS-86, related→LS-95, related→LS-50, related←LS-74, related←LS-68, related←LS-44, related←LS-69, related←LS-106, related←LS-96, related←LS-87, related←LS-76 |

## 描述

## 來源

[LS-50](https://linear.app/little-sprout-app/issue/LS-50/harness分支起點乾淨度-gate-feature-分支不得夾帶其他票號的-commit) PR #90 review I6（2026-08-25）。

## 現象

`scripts/gates/push-gate.sh` 步驟順序 1 SwiftLint → 2 xcodebuild test（數分鐘）→ 3 api-contract → 4 error-codes → 5 migration 分級 → 6 票號乾淨度 → 7 merge-tree 衝突預檢。3–7 皆秒級；有衝突／票號錯要等測試跑完才被擋；第 7 步的 `ls-remote` 等值檢查具時效——測試期間 development 前進就整個重跑。

## 修法

順序改 1 → 3 → 4 → 5 → 6 → 7 → 2（xcodebuild 最後）。步驟編號與 COLLABORATION §4 敘述同步；不改任何步驟內容。

## 驗收

`bash -n`；人造衝突分支 push 在 10 秒內被擋（不等 xcodebuild）；乾淨分支全綠。

## 留言（6）

### 2026-09-01T13:12:27.415Z · Chih-Lin Yeh · `2041f2a4`

**PR #202** https://github.com/CLYEH/little-sprout/pull/202（`hotfix/LS-65-pushgate-reorder` → `main`）已 push，push gate 全綠。

順序改為 1(SwiftLint) → 1a(XcodeGen 漂移，LS-106 已前移，本票未動) → 3(API 契約) → 3b(點擊目標畫面覆蓋) → 4(錯誤碼三方對帳) → 5(migration 版本號撞號/分級) → 6(分支起點乾淨度) → 7(合併衝突預檢) → 2(xcodebuild unit tests，含 1b/LS-95)，不改任何步驟內容。

實測：人造衝突分支（真實 git merge-tree 衝突）在新順序下 0.19 秒被擋、xcodebuild 全程未呼叫；套用重排前的舊腳本跑同一場景需等 xcodebuild test（stub sleep）跑完（60+ 秒）才擋下——對照確認前移生效。push-gate.test.sh 37/37 綠。COLLABORATION.md §4 同步（程式化重建驗證為純搬移＋新增註記，無內容變更）。

詳見 PR body 與 handoff。

### 2026-09-01T13:29:43.918Z · Chih-Lin Yeh · `304cf178`

## merge-review R1 — PR #202（head `c4f3703`，base `main` 9803b0d）

**Verdict：REQUEST_CHANGES**（1 major；「純搬移」宣稱本身通過機械驗證，問題出在這個 deliverable 沒有任何 gate 釘住）

CI 四項全綠（rules／db／lint／ci），head 未前進。

---

### 「純搬移」宣稱：驗證通過

1. **`push-gate.sh` 逐行**：`diff <(sort base) <(sort head)` → 差異只有 **6 行新增註解**＋檔頭註解 2→3 行重排，**零可執行行變動**（沒有任何一行判斷邏輯／訊息／早退行為被新增、刪除或修改）。
2. **`COLLABORATION.md` §4**：把 Push gate 那個 cell 依「；」切段做多重集合比對 → base 44 段 vs head 46 段；唯一新增是 LS-65 註記段＋cell 結尾切分，其餘段落逐字元相同；base 第 36–43 段整組移到 head 第 11–18 段。無 LS-86／LS-95／LS-106 內容回退。
3. **隱性依賴掃描（雙向）**：前移的六支腳本沒有一支用到 `xcodebuild`／`DEVELOPER_DIR`／`xcrun`／模擬器／活 DB（`api-contract-check` 本機走文字模式，psql 只在 CI 的 `--catalog`），也不吃 step 2 產出的 SPM 產物；反向 grep 過 step 2 全段，沒有讀 `branch`／`base_ref`／`target_ref`／`base_sha`／`findings`，沒有變數遮蔽。
4. **清理／鎖配對**：1a 的 `xg_work` EXIT trap 在成功路徑 `trap - EXIT` + `rm -rf` 之後才進到前移區塊；LS-83 模擬器鎖與 LS-100 shutdown trap 都在 step 2 內取得／設定 —— 前移的檢查早退時鎖與 trap 根本還沒建立，**沒有殘留，比舊順序更乾淨**（舊順序是先開模擬器跑完測試、再讓第 3～7 步擋下）。push-gate 不涉及 LS-70 supabase lock。
5. **早退實測（重演 handoff 的 0.19s）**：合成 repo ＋ 真的 `merge-conflict-check.sh`／`branch-ticket-check.sh` ＋ 會 `sleep 20` 的 xcodebuild 假身，同一條真衝突分支 —— head **0.48s 被擋、xcodebuild 呼叫紀錄為空**；base **21.26s 才擋，且 `-version`／`-resolvePackageDependencies`／`test` 全跑過**。驗收條件（10 秒內、不等 xcodebuild）成立。
6. **自測**：`push-gate.test.sh` 重跑 37/37 綠、rc=0、21.6s，穩定。

---

### M1（major）`scripts/gates/push-gate.test.sh` 全檔 —— 本票唯一的 deliverable（順序）沒有機械 gate

**失敗情境（已實測，不是推測）**：把 head 的 `scripts/gates/push-gate.sh` 換回 base 9803b0d 的版本（＝完整回退 LS-65 的順序改動），拿**本 PR 的** `push-gate.test.sh` 跑 → **37/37 全綠、rc=0**。也就是說：日後任何人把 xcodebuild 那段搬回前面、或在 merge 解衝突時解錯邊、或新增一個昂貴步驟插在便宜檢查之前，自測**不會有任何訊號**，只會靜靜地把本票的效益吃掉。

這在本 repo 是硬規則（CLAUDE.md「新增重要規則＝同時新增它的 gate，否則視為未完成」），而且**同一個檔案已經有現成 idiom**：case ④（`push-gate.test.sh:223-245`）就是用 awk 跳過註解行、斷言「trap 設定行號 < xcodebuild test 行號」的靜態接線斷言。手動跑一次 0.19s 是一次性取證，不是 gate。

**建議修法（兩選一即可，皆 ≤15 行）**：
- (a) 靜態（照 ④ 的模式，最省）：對 `$gate_src` 取第一個**非註解**的 `error-codes-check.sh`／`merge-conflict-check.sh` 行號，斷言小於第一個非註解 `xcodebuild test` 行號。實測 head 為 171／230 < 352；base 為 318／377 > 265 —— 回退即紅。
- (b) 動態：把 `$R/scripts/gates/error-codes-check.sh` 暫時換成 `exit 7`，`run_gate` 後斷言 rc≠0、輸出**不含**「執行 unit tests」、`$SHUTDOWN_LOG` 為空（＝根本沒進 xcodebuild 分支），跑完還原成預設成功假身（沿用 ⑦ 已有的還原寫法）。

---

### m1（minor）`scripts/gates/push-gate.sh:295` —— 註解的事實前提已與新順序矛盾

> `EXIT trap 要等本腳本剩下的第 3～7 步（真環境好幾秒）跑完才觸發，那時鎖早已釋放`

重排後第 3～7 步跑在 step 2 之前，trap 幾乎在鎖釋放的同一瞬間觸發（**實務上是好事**：共用機被誤關的時間窗從「好幾秒」縮到接近 0）。但這段是 PR #164 R1 F1「共用機不設 trap」那道防線的*理由*；理由的前提沒了，未來讀的人可能據此判斷「窗口已消失，可以拿掉防線」——不行（共用機不設 trap 與第二道 `[ -d "$sim_lock_dir" ]` 都仍必要，只是命中機率降低）。建議補一行 LS-65 註記說明窗口縮小、防線保留。

### m2（minor）`docs/COLLABORATION.md` §4 —— LS-65 註記漏了步驟 3b，且該項文中位置與程式不一致

新註記列了六項，但程式實際前移的是**七項**：`3b) tap-target-registry-check.sh`（文中「**Features 畫面覆蓋對帳**」）也一起搬到 step 2 之前了（`push-gate.sh:165-167`）。而文件裡「Features 畫面覆蓋對帳」仍留在 unit tests／點擊目標 gate 那段之後（segment 比對：head 第 38 段，仍在 unit tests 區塊內）。票的驗收明列「步驟編號與 COLLABORATION §4 敘述同步」，這項沒同步 —— 讀文件的人會以為它仍要等 xcodebuild。
另（純外觀）：cell 尾端多出一個全形「；」→ `…一致→略過，LS-106）；| 自動（…）`。

### m3（minor，PLAUSIBLE）合併衝突預檢的時效性被反向拉長 —— 這是唯一沒有伺服器端兜底的一關

`merge-conflict-check.sh` 的設計前提是「本機 ref＝遠端當前 sha」才不是假綠（該檔用法 §2、LS-50／PR #77）。舊順序它是 push 前的最後一步（結論到實際 push 幾乎零延遲）；新順序它跑完之後還要等整段 xcodebuild test（分鐘級）才真的 push。

**失敗情境**：T0 通過衝突預檢 → T0+3min 另一個 worktree 把衝突內容併進 `origin/development` → T0+5min 你的 push 成功 → PR 不可合併 → GitHub 不觸發 pull_request workflow → **CI 零紀錄**，正是這關要防的 PR #77 事件。其他前移的檢查（migration 撞號／分級／票號）CI 都有伺服器端兜底，只有這關沒有（§4 明寫「這一關只有本機 push 前做得到」）。

本票明確指定了這個順序，所以**不當作要改的缺陷**；建議記入待辦池（LS-96）：step 2 之後補跑一次 `merge-conflict-check.sh`（實測 <0.5s），fail-fast 與時效兩者兼得。

---

### Informational

- **i1**：`push-gate.test.sh:280`（⑦ 標頭）與 `:354`（⑧ 的 `sleep 3` 假身，註解寫「模擬 push-gate 第 3～7 步耗時」）描述的都是舊順序才有的「step 2 之後的耗時窗」，重排後該窗不存在。**但我實測這兩個 case 的牙齒沒退化**：把 F1 防線（`sim_name` 專屬機判斷）mutate 成 `if true` → head 與 base 一樣是 ⑤⑥⑧ 轉紅；把第二道防線 `[ -d "$sim_lock_dir" ]` mutate 成 `if false` → head 與 base 一樣 ⑦ 兩條轉紅。純註解／時序模型過時，非覆蓋弱化。
- **i2（回答派工的 stub 疑慮）**：三處 error-codes-check.sh 假身**沒有遮蔽任何既有測試意圖** —— 合成 repo 從來就沒有這支腳本，舊版是「無條件失敗、靠 trap 已設好而放著不管」，沒有任何 case 在真跑 error-codes 三方對帳。反而 ⑨（shutdown 失敗不影響整體 exit code）從「兩次都因缺腳本而非 0」變成「兩次都是 0」，斷言比改前更有意義。殘留風險也不存在：stub 全部寫在 `mktemp -d` 出來的 `$R`／`$hf_root`／`$hf2_root` 底下，檔頭 `trap 'rm -rf "$work"' EXIT` ＋ `set -uo pipefail`（無 `-e`）→ case 中途失敗照樣跑完並清乾淨，不可能污染真 repo。⑦ 的 sleep-4 假身還原點在 `wait "$a7_pid"` 之後，位置正確。
- **i3（平行優化維度）**：不建議在本票把 xcodebuild 丟背景與便宜檢查併跑 —— 便宜檢查失敗時模擬器鎖已被佔住，與 fail-fast 目的相衝，也拉長 LS-83 鎖持有時間。現行序列化是對的取捨，這維度無 finding。
- **i4（scope）**：diff 僅三檔，每一行都追得回本票（順序、步驟編號註記、§4 同步、為新順序補的測試假身）。無夾帶重構。

### 未看範圍

`.github/workflows/ci.yml` 不在 diff 內（已確認未被動）；`push-gate.sh` step 2 內部的 LS-95／LS-106 既有邏輯只做了變數與依賴掃描，未逐行重審。

### 2026-09-01T13:38:22.187Z · Chih-Lin Yeh · `e8f2c2ca`

**R2 已 push**（head `129e555`，PR #202）。回應 merge-review R1（`304cf178-ff84-4ef3-bdb1-6ce76677bb7c`）：

**M1（唯一阻擋，已修）**：`push-gate.test.sh` 新增案例 ㉗——照案例④同款 awk 靜態接線斷言 idiom，斷言 `error-codes-check.sh`／`merge-conflict-check.sh` 的呼叫行號都在 `xcodebuild test` 之前。**回退 mutation 驗證**：把 push-gate.sh 換回 base `9803b0d` 版本（＝完整回退 LS-65）跑本檔案，㉗ 轉紅、其餘 36 條不受影響；用現行版本跑則 38/38 全綠。這條 gate 現在真的釘得住「順序被回退」這個失敗情境。

**m1（已修）**：`push-gate.sh:295` 一帶註解——EXIT trap 距鎖釋放的窗口在 LS-65 之後大幅縮小（第 3～7 步已前移，trap 觸發前只剩視 diff 而定的 LS-95 量測），但不是歸零；明寫共用機不設 trap、shutdown 前查鎖目錄兩道防線仍必要，不能因窗口縮小就拿掉。

**m2（已修）**：COLLABORATION.md §4 LS-65 註記補上第 7 項（3b Features 畫面覆蓋對帳，六項改七項）；描述段落搬到與其餘前移項目同一組（API 契約對帳之後、錯誤碼三方對帳之前，對齊程式實際順序）；順手清掉 cell 尾端多餘全形「；」。程式化重建驗證：R2 對 R1 的差異只有這三處，其餘文字逐字元相同。

**m3（PLAUSIBLE，不修＋理由）**：merge-conflict-check 的時效性窗口被反向拉長——這是本票明確指定的順序取捨（順序本身就是這張票的 deliverable），且其他前移項目（migration 撞號／分級／票號）皆有 CI 端伺服器兜底，只有這一關沒有。**記入 LS-96 待辦池**：建議另開子項「step 2（xcodebuild）之後補跑一次 merge-conflict-check.sh（實測 <0.5s）」，本票不處理。

**i1（已修）**：`push-gate.test.sh` 案例⑦標頭（:279-287）與案例⑧的 sleep 3 假身註解（:356-358），更新為反映 LS-65 之後第 3～7 步跑在 xcodebuild test 之前（而非之後）的時序模型；reviewer 已用 mutation 驗證這兩個案例的把關能力沒有因此弱化，純屬敘述過時，本次順手訂正。

**i2／i3／i4**：reviewer 已驗證通過（stub 無遮蔽、序列化取捨正確、diff scope 乾淨），無需處置。

驗證：`bash -n` 兩支腳本皆過；`push-gate.test.sh` 38/38 綠（新增 ㉗，原 37 不回退）；push-gate.sh 對 base 的 mutation 回退測試如上；本分支 push 兩次 push gate 皆綠。

### 2026-09-01T13:45:16.428Z · Chih-Lin Yeh · `9979f328`

## merge-review R2 — PR #202（head `129e555`，前一輪 head `c4f3703`）

**Verdict：APPROVE**（無 blocker／major；1 minor＋3 informational，皆不阻擋）

CI 四項全綠（rules／db／lint／ci）於 `129e555`。R1 的 M1／m1／m2／i1 逐條覆核通過，m3 的「不修＋理由」接受。

---

### R1 findings 覆核結果

**M1（major）→ 已修，且我獨立重跑過，不沿用申報**
- R2 全套：**38/38 綠、rc=0、21.7s**（與申報一致，穩定）。
- **回退 mutation（我自己跑的）**：把 `push-gate.sh` 換成 base `9803b0d`（＝完整回退 LS-65），拿 R2 的 `push-gate.test.sh` 跑 → **rc=1，36 綠、㉗ 紅**，訊息正是「LS-65 的便宜檢查前移順序被回退了」。R1 那個「整包回退卻 37/37 全綠」的失敗情境現在真的被釘住了。
- ㉗ 的 fail-closed 也對：把 `error-codes-check.sh` 呼叫整行刪掉 → ㉗ 紅（三個 pattern 缺一即不 print `yes`）。

**m1（minor）→ 已修，內容核對正確**：`push-gate.sh:292-301` 新註解說「第 3～7 步已前移，trap 觸發前只剩下面視 diff 而定的 LS-95 量測、窗口大幅縮小但不是歸零，共用機不設 trap 與 shutdown 前查鎖目錄兩道防線仍必要」——與程式實況一致（`xcodebuild test` 釋鎖後確實還有可能觸發的 tap-target 區塊才到 trap）。

**m2（minor）→ 已修，機械驗證通過**：對 base `9803b0d` 重跑段落多重集合比對 → base 44 段 vs **R2 45 段**，唯一新增就是 LS-65 註記段（已改「七項」、已含「Features 畫面覆蓋對帳」）；R1 那個冗餘全形分號造成的第 46 段消失了。R2 段落順序 11–21 為 API 契約 → **Features 畫面覆蓋對帳**(12–14) → 錯誤碼 → 版本號撞號 → 分級 → 票號 → 衝突預檢，與程式 3→3b→4→5→6→7 **完全對齊**。R2 對 R1 的 §4 差異就是申報的三處，無其他變動。

**m3（PLAUSIBLE）→ 接受「不修＋理由」**：順序本身就是本票 deliverable，且其他前移項目都有 CI 伺服器端兜底，理由成立。**惟 LS-96 目前尚未出現對應池項**（最新一則是 12:29 的 LS-59）——依既有慣例池項由 orchestrator 寫入（「來源 PR #… —— orchestrator 處置確認」），請 orchestrator 補一則：「來源 PR #202 R1 m3 · step 2 之後補跑一次 `merge-conflict-check.sh`（<0.5s）· 估 size:XS」。這不影響本輪裁決。

**i1 → 已修**：⑦ 標頭與 ⑧ 的 `sleep 3` 註解都改成「LS-65 之後這段跑在 xcodebuild test 之前」的正確時序模型，並各自說明為何不影響該案例要驗的東西。與我 R1 mutation 實測的結論相符。

**純搬移性質未被 R2 破壞**（機械驗證）：`push-gate.sh` R1→R2 的**可執行行 231 行完全相同**（純註解變更）；R2 對 base 的可執行行**多重集合仍完全相同**（＝依然是零內容變動的純搬移）。

---

### m1（minor，本輪唯一 finding）`scripts/gates/push-gate.test.sh:907-927` —— ㉗ 只釘住 7 項前移檢查中的 2 項

我對 R2 的 `push-gate.sh` 做了 5 個**部分回退** mutant（每個都只把一個步驟區塊搬回 step 2 之後，`bash -n` 皆過），跑同一段 awk：

| mutation（只搬這一項回 xcodebuild 之後） | ㉗ |
|---|---|
| 整包回退成 base 9803b0d | **紅 ✅** |
| step 4 `error-codes-check` | **紅 ✅** |
| step 6+7 區塊（`branch-ticket` + `merge-conflict`）＝派工點名的「error-codes 留前面、merge-conflict 搬後面」 | **紅 ✅** |
| 刪掉 `error-codes-check` 呼叫（fail-closed 驗證） | **紅 ✅** |
| step 3 `api-contract-check` | 綠 ❌ 漏抓 |
| step 3b `tap-target-registry-check` | 綠 ❌ 漏抓 |
| step 5 migration 三支（version／immutable／breaking） | 綠 ❌ 漏抓 |
| 拆開 6/7 只搬 `branch-ticket-check`（merge-conflict 留前面） | 綠 ❌ 漏抓 |

**先回答派工的疑問**：「只把 error-codes 前移、merge-conflict 留在後面」**不會漏抓** —— 兩個錨點是各自獨立比對的（`a<c && b<c`），任一支落到 xcodebuild 之後就紅。

**殘留缺口**：§4 宣告前移的是**七項**，㉗ 只釘住其中 2 項（error-codes、merge-conflict）。上表後四列的失敗情境是：日後有人只把 api-contract／tap-target-registry／migration 那一組搬回 xcodebuild 之後（例如某張票覺得「這支要吃 migrations 目錄、放後面比較順」），38/38 照樣全綠，該項就靜靜地退回「要等數分鐘才被擋」。另外 ㉗ 只認 `xcodebuild test` 這個字面，若有人在便宜檢查之前插入新的 `xcodebuild build` 之類昂貴步驟，也不會紅（實測 mutant 亦為綠；此點屬 M1 原文提到的第二種回退方式）。

**不阻擋的理由**：最可能發生的回退路徑（整包回退／merge 解衝突解錯邊）已被釘死並經實測；缺口需要有人**外科式地只搬其中一支**，機率低，且損失只有時間、無正確性影響。

**建議修法（約 4 行，下次動這支自測時一併補）**：awk 內再加 4 個 `saw_` 變數（`api-contract-check.sh`／`tap-target-registry-check.sh`／`migration-version-check.sh`／`branch-ticket-check.sh`），END 條件一起比；順手把 `/xcodebuild test/` 放寬成 `/xcodebuild (test|build)/`。→ 7/7 全釘。若不修，建議記入 LS-96。

---

### Informational

- **i1**：㉗ 註解引用的「head 171／230 < 352」是我 R1 在 `c4f3703` 上量的；R2 因 step 2 區塊多了 3 行註解，實際是 **171／230 < 355**。那是歷史引用、不是斷言輸入（awk 每次重算），不值得為它單獨改一版。
- **i2**：㉗ 放在 ㉖ 之後、summary 之前，位置與 ④ 同款靜態斷言的慣例一致；對 `$gate_src`（真檔）而非合成 repo 的副本比對，正確。
- **i3（scope）**：R2 三檔、+45/-12 與 +9/-3 與 +1/-1，每一行都追得回 R1 的 M1／m1／m2／i1，無夾帶重構。

### 未看範圍

`.github/workflows/ci.yml`（不在 diff）；`push-gate.sh` step 2 內部 LS-95／LS-106 既有邏輯本輪未再逐行重審（R1 已確認 R2 未動其可執行行）。R1 已做過的早退實測（0.48s vs 21.26s）本輪未重跑——R2 對 base 的可執行行完全相同，行為不可能改變。

### 2026-09-01T13:47:05.499Z · Chih-Lin Yeh · `d39f67a5`

## Orchestrator retro（收尾儀式；純 harness 票，dead-code-sweeper 免跑）

**過程**：R1 實作（純搬移＋0.19s vs 60.4s 對照實測）→ merge-review R1 REQUEST_CHANGES（M1：deliverable 自身無 gate——回退仍全綠，最高原則違反）→ R2 補案例㉗＋回退 mutation 自證 → R2 APPROVE（reviewer 加打 5 個部分回退 mutant 定界斷言範圍）→ 併 main `cda6d42`、back-merge PR #203。

**Lesson learning**：
1. **「順序」也是需要 gate 的規則**——M1 是最高原則（新增重要規則＝同時新增它的 gate）在「重排」型變更上的實例：變更本身沒有斷言釘住，回退即無聲。此後凡 deliverable 是「結構／順序／配置」的票，驗收條件應內建「回退 mutation 會紅」一條。
2. **Reviewer 的 mutant 定界法**（5 個部分回退變體逐一打斷言）給出了斷言範圍的精確邊界，缺口記池（92b97543 #2）而非擋票——「最可能路徑已釘死、缺口需外科式操作」的分級判斷合理。
3. 對照組實測（新舊版本同場景計時）是順序型變更最有力的驗證形態，值得成為此類票的慣例。

informational 處置：m3＋㉗ 範圍缺口記 LS-96（comment `92b97543`）；i 級全數經 reviewer mutation 排除。

### 2026-09-01T14:08:27.522Z · Chih-Lin Yeh · `027d7b2e`

結案：PR #202 併 main `cda6d42`（merge-review=success on `129e555`，linear:9979f328）；back-merge PR #203 併 development `e85e6c5`（§2 驗證：merge-tree 無內容差、status 貼 `cda6d42` id 53301239970；期間一次 lint runner 卡死 cancelled flake，`gh run rerun --failed` 後全綠）。收尾儀式完成——純 harness 免 sweeper、retro（comment `d39f67a5`）、informational 記 LS-96（comment `92b97543`）、清理：`cleanup-merged.sh --apply LS-65`。

