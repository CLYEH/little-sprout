# LS-232 Harness：「新增登入後全屏 gate 的票必更新 QADriver」機械化——`RootView` 全屏 gate 清單 vs `QADriver` 落點處理清單對照 gate（CI `rules`＋push gate），漏一個即紅；兩起事故：LS-190 EULA（→LS-220）、LS-217 推播前置頁（QA R1 FAIL `e4863482`）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:S, lane:harness |
| Cycle | 2 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-12T07:45:30.489Z |
| 開始 | 2026-09-12T09:35:59.714Z |
| 完成 | 2026-09-12T13:16:57.273Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-232/harness新增登入後全屏-gate-的票必更新-qadriver機械化-rootview-全屏-gate-清單-vs-qadriver |
| 關係 | related→LS-229, related→LS-215, related→LS-113, related→LS-209, related→LS-205, related→LS-231, related→LS-96, related→LS-190, related→LS-217, related→LS-220, related←LS-218, related←LS-224, related←LS-233 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項 P1 `qa-driver-gate-check`（來源 [LS-190](https://linear.app/little-sprout-app/issue/LS-190/taskls-23-ios-eula-同意頁首次登入版本更新時零容忍條款摘要08刪除單筆內容確認日記-10留言-10b沿-ls-142)／[LS-220](https://linear.app/little-sprout-app/issue/LS-220/harnessqa-e2esh-qadriver-支援-ls-190-eula-同意-gate登入後落在使用條款更新)：EULA 同意頁讓 `qa-e2e.sh` 三情境全紅半天，錯誤訊息與環境故障無法分辨）。2026-09-12 第二起：[LS-217](https://linear.app/little-sprout-app/issue/LS-217/taskls-22-ios-推播權限前置說明頁系統授權register-device-token設定頁推播通知列與權限狀態同步-依-ls) 推播權限前置頁（`PushPrepromptView`，`LittleSprout/Navigation/RootView.swift:147-158` `.fullScreenCover`）併入後，QA R1（comment `e4863482`）`qa-e2e.sh browse`／`publish` 對任何新帳號 `xcodebuild exit 65`——票本身驗收全過卻 FAIL，qa status failure 卡住 test→main。規約句「新增登入後全屏 gate 的票 DoD 必含 QADriver 更新」（[LS-220](https://linear.app/little-sprout-app/issue/LS-220/harnessqa-e2esh-qadriver-支援-ls-190-eula-同意-gate登入後落在使用條款更新) 收尾寫進池）靠自律，兩票兩次漏。

## 範圍

1. 機械 gate：`scripts/gates/qa-driver-gate-check.sh`（＋python）——(a) 從 `RootView.swift`（及其直接子 View）抽出登入後全屏 gate 清單：`.fullScreenCover`／`.sheet` 綁定的 View 型別名（如 `EULAConsentView`、`PushPrepromptView`），或以註解標記 `// QA-GATE: <View>` 為單一來源；(b) 從 `LittleSproutUITests/QA/QADriver.swift` 抽出已處理 gate 清單（`landed` 落點名或 `// QA-GATE-HANDLED: <View>` 標記）；(c) 差集非空即紅，印出漏掉的 View 名與「在 QADriver 加落點＋dismiss」的修法提示。
2. 接線：CI `rules` job＋`push-gate.sh` 便宜檢查段（Swift 檔有變更時）；自測 `.test.sh` 正負樣本＋mutation（拿掉 QADriver 的 EULA 處理 → 紅）。
3. `docs/COLLABORATION.md` §7 對照表加一列；ios-dev 定義加一句「新增登入後全屏 gate 必同 PR 更新 QADriver（gate 會擋）」。
4. 回歸：現行 head（含 [LS-217](https://linear.app/little-sprout-app/issue/LS-217/taskls-22-ios-推播權限前置說明頁系統授權register-device-token設定頁推播通知列與權限狀態同步-依-ls) R5 修正後）綠；[LS-217](https://linear.app/little-sprout-app/issue/LS-217/taskls-22-ios-推播權限前置說明頁系統授權register-device-token設定頁推播通知列與權限狀態同步-依-ls) R5 之前的 head 紅（原文）。

## 不做

改 QADriver 的實際 gate 處理（[LS-217](https://linear.app/little-sprout-app/issue/LS-217/taskls-22-ios-推播權限前置說明頁系統授權register-device-token設定頁推播通知列與權限狀態同步-依-ls) R5 做）；qa-e2e 情境本身。

## 驗收

- [ ] 對 [LS-217](https://linear.app/little-sprout-app/issue/LS-217/taskls-22-ios-推播權限前置說明頁系統授權register-device-token設定頁推播通知列與權限狀態同步-依-ls) R5 前後兩個 head 各跑一次：前紅（列出 `PushPrepromptView`）、後綠（原文）；自測＋mutation 綠／紅
- [ ] merge-reviewer 通過（scope：只加 gate＋文件）；hotfix 流程（自 main、PR 回 main、back-merge）

## 留言（18）

### 2026-09-12T09:36:04.082Z · Chih-Lin Yeh · `31245a3b`

**orchestrator 09-12 17:35**：harness lane 空（LS-231 Done）→ scope+ 補 cycle 2 → Ready → worktree `.claude/worktrees/LS-232`＝`hotfix/LS-232-qa-driver-gate-check`（自 main `6c4be6f`，PR 回 main）→ 派 ios-dev（回歸以 dev `938e854`／`ebe390a` 快照驗「R5 前紅／後綠」；順手收池 `f2c2100f`(4)）→ In Progress。

### 2026-09-12T10:15:56.111Z · Chih-Lin Yeh · `81bbff41`

## Ticket

LS-232（Harness：「新增登入後全屏 gate 的票必更新 QADriver」機械化）

worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-232`，分支
`hotfix/LS-232-qa-driver-gate-check`（自 `origin/main` `6c4be6f` 切），
head `c0ee0b229db30047ee58bbe2f5a8db1f4927267e`，PR
https://github.com/CLYEH/little-sprout/pull/365（→ `main`）。

## 已完成

1.（範圍 1）`scripts/gates/qa_driver_gate_check.py`（新檔）：核心判定邏輯——`RootView*.swift`
   （`RootView.swift`＋同目錄 `RootView+*.swift`）的 `// QA-GATE: <View>` 標記為唯一決定紅／綠
   的來源（(a)），`LittleSproutUITests/QA/QADriver*.swift` 的 `// QA-GATE-HANDLED: <View>` 標記
   為已處理清單（(b)），差集非空即紅並印修法提示（(c)）；額外對 `.fullScreenCover`／`.sheet`
   modifier 做純文字備援掃描，找到沒有 `// QA-GATE` 標記的疑似 gate 印 ⚠（不影響 exit code，
   純提醒）。`scripts/gates/qa-driver-gate-check.sh`（新檔，可執行）：bash 3.2 相容 wrapper，
   `--repo <dir>` 參數（未給時 fallback `git rev-parse --show-toplevel`）。commit `ddf1496`。
2.（範圍 1）在現有兩個登入後全屏 gate 補上標記，讓現行 head 判綠：
   `LittleSprout/Navigation/RootView+AuthenticatedGate.swift:103-107` 的 `EULAConsentView`
   （條件式整樹替換，非 modifier 綁定）加 `// QA-GATE: EULAConsentView`；
   `LittleSprout/Navigation/RootView.swift:114-116` 的 `CreateChildView`（`.fullScreenCover`）加
   `// QA-GATE: CreateChildView`；`LittleSproutUITests/QA/QADriver.swift:127-128`／`:171-172`
   對應加 `// QA-GATE-HANDLED: EULAConsentView`／`// QA-GATE-HANDLED: CreateChildView`。純註解
   新增，不改任何生產邏輯（`swiftlint lint --strict --quiet` exit 0）。commit `ddf1496`。
3.（範圍 2）接線：`scripts/gates/push-gate.sh:180-190`（3c 步驟）——Swift 檔有變更時跑（共用
   第 1 步 SwiftLint 的 `skip_swift_steps` 判斷，CI 仍無條件全跑）；`.github/workflows/ci.yml`
   `rules` job 新增獨立 gate 步驟（無條件跑，同 `ax-launch-check`／`privacy-manifest-check`）＋
   掛進「Gate 腳本自測」step 呼叫 `qa-driver-gate-check.test.sh`，並更新該 step 的 `name:` 清單。
   commit `975618c`。
4.（範圍 2）自測 `scripts/gates/qa-driver-gate-check.test.sh`（新檔，17 組樣本）：正負樣本＋
   mutation（拿掉標記轉紅）＋備援掃描 ⚠ 不轉紅＋多 gate 獨立判定＋參數 fail closed＋真 repo 驗證。
   commit `ddf1496`。
5.（範圍 3）`docs/COLLABORATION.md`：§7 對照表加一列說明本 gate 判定規則與已知盲區；「真後端
   QA 登入後 30 秒未到落點」段（原第 133 行）補一句登入後全屏 gate 清單改由本 gate 機械把關
   （main 目前尚未含 LS-217 R5 在 `development` 對同一行加的那句「機械化見下方 harness 票」——
   main／development 分岔，見下方風險段）。`.claude/agents/ios-dev.md` 加規約句「新增登入後
   全屏 gate 必同 PR 更新 QADriver（`qa-driver-gate-check` 會擋）」。commit `c0ee0b2`。
6.（範圍 3）`scripts/gates/agent-tools-check.sh` 新增 1 條 `BODY_RULES`（ios-dev 正文須含上述
   規約句字樣）；`scripts/gates/agent-tools-check.test.sh` 同步新增變數、併入 `IOS_BODY`、新增
   正負樣本＋mutation 三組、總條數計數 38→39。commit `c0ee0b2`。

## 已驗證（怎麼驗）

- **範圍 1(a)(b)(c)、驗收「自測＋mutation 綠／紅」**：`bash scripts/gates/qa-driver-gate-check.test.sh` 全 17 組樣本 `✓`，末行 `qa-driver-gate-check.test.sh：全部通過`（exit 0）。mutation 樣本（票文明點「fixture 拿掉 QADriver 的處理 → 紅」）：改了什麼一行——把合成 fixture QADriver.swift 的 `// QA-GATE-HANDLED: PushPrepromptView` 標記拿掉；哪條測試紅——對該 fixture 跑 `qa-driver-gate-check.sh --repo "$R"`；斷言訊息原文——`✗ qa-driver-gate-check：QADriver 沒有處理下列登入後全屏 gate：` 換行 `    - PushPrepromptView（宣告於 …/RootView.swift:5）——在 QADriver 加落點＋dismiss（比照既有 EULA／推播前置頁處理），並在對應位置加 \`// QA-GATE-HANDLED: PushPrepromptView\` 標記`，exit 1（測試名 `③mutation拿掉標記後-轉回紅` PASS，見 `qa-driver-gate-check.test.sh`）。
- **範圍 1、真 repo 驗證**：`bash scripts/gates/qa-driver-gate-check.sh`（對本分支 head，`--repo` 預設 `git rev-parse --show-toplevel`）→ `✓ qa-driver-gate-check：宣告的 2 個登入後全屏 gate（CreateChildView、EULAConsentView）QADriver 皆已標記處理`，exit 0，無 ⚠ 警告。原文見 `.claude/evidence/LS-232/r1/regression-main-head.txt`。
- **範圍 2、接線不破壞既有 gate**：`bash scripts/gates/push-gate.test.sh` → 96 組樣本全 `✓`，末行 `push-gate 模擬器自測通過`（exit 0）——本票新增的 3c 步驟前置條件 `[ -f LittleSprout/Navigation/RootView.swift ]` 在既有合成 fixture 內恆為 false（fixture 從未建立該路徑），對既有 96 組樣本零副作用，純加法確認。另跑 `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` → 印 `YAML OK`，`ci.yml` 語法未破。
- **範圍 2、push gate 實跑（含 xcodebuild 全量單元測試）**：`git push -u origin hotfix/LS-232-qa-driver-gate-check` 輸出印 `✓ qa-driver-gate-check：宣告的 2 個登入後全屏 gate（CreateChildView、EULAConsentView）QADriver 皆已標記處理`（3c 步驟原文）與 `✓ push gate 通過`，push 成功（`* [new branch] hotfix/LS-232-qa-driver-gate-check -> hotfix/LS-232-qa-driver-gate-check`）。**模擬器 runtime 註記（LS-205）**：push gate 印 `⚠ detect-simulator：本機無 iOS 26.2 runtime（有：26.0、26.5），改用 iOS 26.0；CI 為 iOS 26.2，tap-target／版面量測可能不一致`——本票未新增任何 UI／點擊目標畫面，量測基準差異對本票改動無實質影響（純標記註解＋harness 腳本）。
- **範圍 3、agent 定義與 gate 同步**：`bash scripts/gates/agent-tools-check.sh`（對本分支真 repo）→ 列出 `ios-dev.md：正文含「新增登入後全屏 gate 必同 PR 更新 QADriver」`，末行 `✓ agent-tools gate 通過（6 份 agent 定義；正文必含字樣 39 條）`，exit 0。`bash scripts/gates/agent-tools-check.test.sh` → 120 組樣本全 `✓`（含新增正負樣本＋mutation 三組：`ios-dev 正文含新增全屏 gate 規約句 → 印「正文含」`／`ios-dev 缺該句 → exit 1，其餘句子齊全不救`／`mutant：拿掉規則後「缺新增全屏 gate 規約句」的負樣本變綠`），末行 `✓ agent-tools-check 自測通過（120 組樣本）`，exit 0。
- **範圍 4、驗收「LS-217 R5 前後兩個 head 各跑一次：前紅（列出 PushPrepromptView）、後綠」**：在 session scratchpad 用 `git archive 938e854`／`git archive ebe390a` 兩個 development SHA 匯出快照（不在本 worktree 內 checkout 別的 ref），各自用 python 腳本 patch 進 `// QA-GATE: PushPrepromptView` 標記（`938e854` 快照額外不補 `// QA-GATE-HANDLED`，忠實反映當時 `QADriver` 確實沒有對應處理程式碼；`ebe390a` 快照額外在真實新增的 `dismissPushPrepromptIfPresent()` 函式旁補 `// QA-GATE-HANDLED: PushPrepromptView`，忠實反映當時該處理程式碼確實已存在），以 `--repo <快照目錄>` 各跑一次：`938e854`（R5 前）輸出 `✗ qa-driver-gate-check：QADriver 沒有處理下列登入後全屏 gate：` 換行 `    - PushPrepromptView（宣告於 …/RootView.swift:147）——在 QADriver 加落點＋dismiss（比照既有 EULA／推播前置頁處理），並在對應位置加 \`// QA-GATE-HANDLED: PushPrepromptView\` 標記`，exit 1；`ebe390a`（R5 後）輸出 `✓ qa-driver-gate-check：宣告的 1 個登入後全屏 gate（PushPrepromptView）QADriver 皆已標記處理`，exit 0。兩次原文（含 `CreateChildView` 備援掃描 ⚠ 行——兩個快照皆未 patch `CreateChildView` 標記，⚠ 屬預期、不影響 exit code）見 `.claude/evidence/LS-232/r1/regression-dev-shas.txt`。

## 未完成

首輪實作，尚無 reviewer informational finding 可處置（merge-reviewer 尚未審）。

自己已知未做（票文「不做」範圍內，非遺漏）：未改 QADriver 的實際 gate 處理邏輯（沿用既有
EULA／CreateChildView 的處理方式，只加標記）；未動 `qa-e2e.sh` 情境本身。

## 風險

- 低：`docs/COLLABORATION.md:133` 段落——main 目前不含 LS-217 R5 在 `development` 對同一段落加的敘述句（「機械化見下方 harness 票」，因為 LS-217 尚未 promote 到 main），本票在同一段落末尾新增了獨立的敘述句（指向本票 `qa-driver-gate-check`）。這不是修改衝突（兩處敘述目前互不重疊），但 back-merge `main`→`development` 時，這段落在 development 上已有的 LS-217 R5 敘述與本票新增的敘述可能需要人工合併成一句、避免重複——請 orchestrator 在 back-merge 時注意這一段（`docs/COLLABORATION.md` 該段落，關鍵字「登入後 30 秒未到落點」）。
- 低：本票新增的三處 Swift 檔案變更（`RootView.swift`／`RootView+AuthenticatedGate.swift`／`QADriver.swift`）皆為純註解新增，`swiftlint lint --strict --quiet` 與 push gate 全量單元測試（含這三個檔案的編譯）皆通過，無行為變更風險。
- 中：`qa-driver-gate-check` 的備援掃描（`.fullScreenCover`／`.sheet` 純文字 heuristic）有已知盲區——特殊寫法（巢狀更深、identifier 用變數組出來）可能找不到候選、不會印 ⚠ 提醒；標記才是唯一判定來源，找不到 ⚠ 不代表沒有全屏 gate，仍要靠 code review／DoD 自律補位（已寫入 `qa_driver_gate_check.py` 檔頭與 `docs/COLLABORATION.md` §7 新增列）。

## 產出位置

- PR：https://github.com/CLYEH/little-sprout/pull/365（head `c0ee0b229db30047ee58bbe2f5a8db1f4927267e`，→ `main`）
- 新檔：`scripts/gates/qa-driver-gate-check.sh`、`scripts/gates/qa_driver_gate_check.py`、`scripts/gates/qa-driver-gate-check.test.sh`
- 證據：`.claude/evidence/LS-232/r1/regression-dev-shas.txt`（938e854 紅／ebe390a 綠原文）、`.claude/evidence/LS-232/r1/regression-main-head.txt`（本分支 head 綠原文）
- push gate 完整輸出：本 session tool-result（`git push` 執行輸出，非 repo 內檔案）
- 模擬器已關：`50E3FE6E-99F4-4E8D-B13C-E62B8D848B81`（`LS-232-iPhone17Pro`，push gate 自動 boot／shutdown，本輪未額外手動操作模擬器）

### 2026-09-12T10:17:20.885Z · Chih-Lin Yeh · `97792dc3`

**orchestrator 09-12 18:2x**：R1 handoff `81bbff41`——PR #365 head `c0ee0b2`：`qa-driver-gate-check.sh`＋python（`// QA-GATE:`／`// QA-GATE-HANDLED:` 標記為單一來源，modifier 掃描只印 ⚠ 備援，因 `EULAConsentView` 是條件式整樹切換掃不到）；接線 push-gate／CI rules／agent-tools-check；三次回歸：dev `938e854`（補標記）紅列 `PushPrepromptView`、`ebe390a` 綠、main 綠。風險（agent 自揭）：main 無 `PushPrepromptView`，back-merge 到 development 需補兩處標記；COLLABORATION.md:133 main／dev 各加一句需合併。→ In Review，已派 merge-reviewer R1（重點：備援掃描只 ⚠ 不紅是否 fail-open、回歸是否只驗「人有沒有寫標記」、back-merge 標記行與合併句原文）。back-merge 走 `hotfix/LS-232-backmerge-development` 分支（merge main＋補標記＋合併句）而非直接 main→development PR。

### 2026-09-12T10:51:10.489Z · Chih-Lin Yeh · `2f276f29`

# LS-232 merge-review R1 — REQUEST_CHANGES

（09-12 完成；在主 checkout 審。**未持有 supabase lock**——本票是純文字掃描 gate，不需要 DB、不需要模擬器、未跑 `supabase db reset`／`run.sh`。）

- head：`c0ee0b229db30047ee58bbe2f5a8db1f4927267e`（PR #365，`hotfix/LS-232-qa-driver-gate-check` → `main`）。開審時 `cd .claude/worktrees/LS-232 && git fetch origin && git status` → `nothing to commit, working tree clean`、`git rev-parse HEAD`＝`c0ee0b2…7267e`，與派工給的 head 一致。
- 審的 diff：`git diff origin/main...c0ee0b2` 共 12 檔（+489／−5），逐行看完，無跳過的檔案。
- 模擬器 runtime（LS-205）：本輪未跑 `xcodebuild test`／未 boot 任何模擬器，無 runtime 比對需求；未執行 `xcrun simctl ui`，字級／外觀零改動。
- 判定：**0 blocker／1 major／2 minor／5 informational → REQUEST_CHANGES**

---

## 第 1 點裁決：備援掃描**應該改為紅**——現況是真的 fail-open，票文「前紅」在真 head 上不成立

### M1（major）`scripts/gates/qa_driver_gate_check.py:150-160`（`run()` 只用 `missing` 決定 exit code）——gate 對「兩起事故的實際形態」完全無效

判定紅／綠的唯一來源是 `RootView*.swift` 的 `// QA-GATE:` 標記。備援掃描找得到候選也只 `print("⚠ …")`，不進 exit code（`scan_modifier_backup()` 的回傳只被拿去印，`missing = sorted(set(declared) - set(handled))` 與它無關）。因此「新增 gate 的人沒加標記」＝宣告清單是空的＝差集是空的＝**永遠綠**。

**我自己重放的機械證據**（不是引用 handoff）：對 LS-217 事故當下的 development head `938e854` 做**未經任何 patch 的**快照——

```
git archive 938e854 | tar -x -C <scratch>/LS-232-replay-938e854
grep -rn "QA-GATE" <scratch>/LS-232-replay-938e854/LittleSprout/Navigation/ \
  <scratch>/LS-232-replay-938e854/LittleSproutUITests/QA/    # → 0 hit（快照裡本來就沒有任何標記）
bash scripts/gates/qa-driver-gate-check.sh --repo <scratch>/LS-232-replay-938e854
```

輸出原文：

```
⚠ qa-driver-gate-check：…/RootView.swift:129：`.fullScreenCover` 疑似綁定 `CreateChildView`，沒有對應 `// QA-GATE: CreateChildView` 標記……
⚠ qa-driver-gate-check：…/RootView.swift:147：`.fullScreenCover` 疑似綁定 `PushPrepromptView`，沒有對應 `// QA-GATE: PushPrepromptView` 標記……
✓ qa-driver-gate-check：宣告的 0 個登入後全屏 gate（無）QADriver 皆已標記處理
exit=0
```

**這支 gate 放行了它被建出來要擋的那個 commit。** 票文驗收 4 寫的是「對 LS-217 R5 前後兩個 head 各跑一次：**前紅（列出 `PushPrepromptView`）**」——在真實的 `938e854` 上它是綠的；handoff 的「前紅」是把 `// QA-GATE: PushPrepromptView` 手動 patch 進快照後才得到的（handoff 與 `.claude/evidence/LS-232/r1/regression-dev-shas.txt` 檔頭都誠實寫明了這一點，這點值得肯定，但不改變驗收條件未達成的事實）。

**為什麼這是 major 而不是「票文允許的設計取捨」**：票文範圍 (a) 的「或以註解標記 `// QA-GATE: <View>` 為單一來源」確實給了兩條路，但兩起事故的形態是同一個——**新增 gate 的人根本沒想到 QADriver**。LS-217 QA R1（`e4863482`）原文：「`QADriver`／`QASmokeTests` 對這個新畫面沒有任何 dismiss 處理」；沒想到 QADriver 的人，同樣不會想到去寫 `// QA-GATE:`。以標記為唯一紅源，實際擋下的只剩「記得寫宣告標記、卻忘了寫處理標記」這個窄很多、也罕見很多的殘差情境。

這同時牴觸 CLAUDE.md 最高原則「所有給 agent 的指示都假設可能不被遵守；每條重要規則都必須有機械式 gate 攔截違規」：`// QA-GATE:` 標記本身現在是一條**只靠 ios-dev.md 正文自律、沒有 gate 的規則**，而自律正是本票要取代的東西（兩票兩次漏）。`agent-tools-check` 只驗「ios-dev.md 這份檔案裡有沒有那句話」，不驗「有沒有人照做」。

### 最小改法（`scripts/gates/qa_driver_gate_check.py`）

偵測面**完全不用改**——上面的重放證明備援掃描已經正確抓到 `PushPrepromptView`（行號、型別名都對），錯的只有 exit code。

1. 新增白名單標記：`EXEMPT_MARKER_RE = re.compile(r"//\s*QA-GATE-EXEMPT:\s*([A-Za-z_][A-Za-z0-9_]*)")`，在 `run()` 裡用 `scan_markers(root_view_files, EXEMPT_MARKER_RE)` 取出 `exempt`。
2. `scan_modifier_backup()` 改成回傳「候選 View 名＋位置」，在 `run()` 裡算 `unmarked = 候選 − declared − exempt`；`unmarked` 非空即紅（與 `missing` 併成兩段各自列名的紅訊息，提示分別是「補 `// QA-GATE:` 標記＋更新 QADriver」與「這不是登入後 gate 就標 `// QA-GATE-EXEMPT: <View>`」）。
3. **`CONTAINER_EXCLUDE` 必須同時擴充**（見 m1）——否則第 2 步一上線就會假紅。
4. 自測 `qa-driver-gate-check.test.sh` 的 ④ 從「備援掃描印警告不轉紅」翻面成「未標記的 modifier 綁定 → 紅、列出候選名」，並補：(a) 有 `// QA-GATE-EXEMPT:` → 綠；(b) `.sheet { Text(…) }` 之類非 gate 內容 → 不得紅（m1 的負樣本）；(c) 回歸樣本：**未經 patch 的 `938e854` 形狀**（`.fullScreenCover` 綁 `PushPrepromptView`、repo 內 0 個標記）→ 紅。第 (c) 條才是票文驗收 4 真正要的東西。

改完之後，現行 main head 仍綠（我實測 `RootView*.swift` 只有一處 modifier、`RootView.swift:120` 的 `CreateChildView`，已有標記；`bash scripts/gates/qa-driver-gate-check.sh` 對 head 零 ⚠、exit 0），不會誤擋本 PR 自己。

---

## Findings

### m1（minor）`scripts/gates/qa_driver_gate_check.py:39-44`（`CONTAINER_EXCLUDE`）——缺 SwiftUI 基本型別，非 gate 的 `.sheet` 會被點名

`CONTAINER_EXCLUDE` 只收了容器型別（`VStack`／`NavigationStack`／`Binding`…），沒有 `Text`／`Button`／`Image`／`Label`／`Color`／`ProgressView`／`Toggle`／`Picker`／`Section`／`NavigationLink` 這些常見的「第一個大寫識別字」。合成樣本實測：

```
.sheet(isPresented: $showHelp) {
    Text("說明文字")
}
```

輸出原文（`--repo` 指向該合成樹）：

```
⚠ qa-driver-gate-check：…/RootView.swift:5：`.sheet` 疑似綁定 `Text`，沒有對應 `// QA-GATE: Text` 標記——若這是登入後全屏 gate，請補標記並確認 QADriver 有 `// QA-GATE-HANDLED: Text`
```

今天只是雜訊（⚠ 不影響 exit code），但**一旦照 M1 把它改紅，這就是假紅**，而且會落在與本票無關的票上。所以 m1 必須和 M1 一起修，不能分開。

### m2（minor）`docs/COLLABORATION.md:336`（§7 對照表新增列）——敘述宣稱的能力超過程式實際做得到的

新增列寫：「對 LS-217 R5 修正前後兩個 development head（`git archive` 匯出快照＋`--repo` 指向）各驗一次：**修正前紅（列出 `PushPrepromptView`）**、修正後綠。」

這句省略了決定性前提——那個「紅」是**人工把 `// QA-GATE: PushPrepromptView` patch 進快照之後**才成立的；對真正的 `938e854` 內容跑是綠的（見 M1 的重放）。讀者照字面會以為這支 gate 對真實舊 head 會紅，正好誤判它的防護範圍。

§7 這張表自己就是為了防這種漂移存在的（同表 LS-215 R2 `40ec7b65` M1 的原話：「程式與正典表不一致，正是本表要防的漂移」）。建議改法：紅／綠那句加上「（標記由本票人工補入快照；gate 本身對未加標記的舊 head 判綠——見已知盲區）」，或在 M1 修完後改寫成真實的回歸敘述。

### i1（informational）宣告來源只掃 `LittleSprout/Navigation/RootView*.swift`

`ROOT_VIEW_GLOB` 限定 `RootView.swift` 與同目錄 `RootView+*.swift`。若未來的登入後全屏 gate 掛在更深層（例如 `SectionTabView` 或 `LittleSprout/Features/**` 某個 View）而不是 `RootView` 這一層，標記掃描與備援掃描**都**看不到。票文範圍 (a) 明文就是「`RootView.swift`（及其直接子 View）」，故屬票文範圍內的已知界線、不是遺漏；`qa_driver_gate_check.py` 檔頭與 §7「已知盲區」也寫了。記錄供日後評估是否擴大 glob。

### i2（informational）`BACKUP_SCAN_LOOKAHEAD = 20` 的視窗上限

備援掃描從 modifier 那行往下最多 20 行找候選。目前兩處實際距離是 7 行（`RootView.swift:132` → `:139` `CreateChildView`，以 back-merge 後的行號計）與 8 行（`:150` → `:158` `PushPrepromptView`），餘裕充足。但 `.fullScreenCover(isPresented: Binding(get:…, set:…))` 這種寫法本身就佔 6–8 行，若 `set` 分支再長一點就會越界、靜默不印 ⚠。M1 修完後這個界線會從「少印一行提醒」升級成「該紅沒紅」，屆時值得一併評估（例如改成掃到該 modifier 的 closure 結束，而非固定行數）。

### i3（informational）`scripts/gates/push-gate.sh:184` 的前置條件在檔案搬家時靜默跳過

3c 步驟包在 `if [ -f LittleSprout/Navigation/RootView.swift ]` 裡。`RootView.swift` 一旦改名／搬目錄，本機 push-gate 會**靜默**不跑這支 gate（連「跳過」都不印）。補位是 CI `rules` job 無條件跑、且 `qa_driver_gate_check.py` 的 `find_files` 找不到檔案時 `fail()` → exit 2 fail-closed，所以整體仍擋得住，只是本機前置失守。與同檔既有慣例（`if [ -d LittleSprout/Features ]` 之於 `tap-target-registry-check`）一致，不要求改。

### i4（informational）`scripts/gates/push-gate.test.sh` 對新增的 3c 步驟零覆蓋

我查實 handoff 的「零副作用」說法屬實：`grep -n "RootView\|qa-driver" scripts/gates/push-gate.test.sh` → **0 hit**，合成 fixture 從未建立 `LittleSprout/Navigation/RootView.swift`，所以 96 組樣本全部走 `if` 的 false 分支。反面是：3c 的兩條分支（`skip_swift_steps=1` 印跳過 vs 實際呼叫 gate）**都沒有被自測釘住**，日後若接線退化（例如條件寫錯、`skip_swift_steps` 判斷改動）不會有樣本轉紅。符合既有慣例（gate 正確性由自己的 `.test.sh` 顧、CI 無條件跑補位），記錄不修。

### i5（informational）handoff 的 `RootView.swift:114-116` 行號差 3 行

handoff「已完成 2」寫「`LittleSprout/Navigation/RootView.swift:114-116` 的 `CreateChildView`」，實際標記在 **`:117-119`**（`grep -n "QA-GATE" LittleSprout/Navigation/RootView.swift` → 117、118；第 119 行是續行「那一步）。」）。`:114-116` 落在 LS-113 的既有註解區塊上。同段其他兩處行號（`RootView+AuthenticatedGate.swift:103-107`、`QADriver.swift:127-128`／`:171-172`）我逐一 `grep -n` 核對**完全正確**。純引用瑕疵、不影響程式。

---

## 第 5 點：back-merge 到 `development` 的明確結論（給 orchestrator）

我用 `git merge-tree --write-tree origin/development c0ee0b2` 實際算過一次合併（結果 tree `ac19d398…`）：**三個 Swift 檔全部 auto-merge 乾淨**（`RootView.swift`／`RootView+AuthenticatedGate.swift`／`QADriver.swift`），**`docs/COLLABORATION.md` 會衝突**（不是並存）——`CONFLICT (content): Merge conflict in docs/COLLABORATION.md`，單一 hunk，兩邊各一整行。

### (a) 要補的標記：兩處，行號以**合併後**的檔案為準

**① `LittleSprout/Navigation/RootView.swift`：插在第 150 行之前**（合併後第 150 行＝`        .fullScreenCover(isPresented: Binding(`，即 `PushPrepromptView` 那一個；`CreateChildView` 的 modifier 在第 132 行、標記已隨本 PR 進來在 129-131 行）。原文（8 空格縮排，比照同檔既有標記的寫法）：

```swift
        // QA-GATE: PushPrepromptView（LS-232：登入後全屏 gate 清單的單一來源標記；QADriver 對應標記見
        // `QADriver.swift` 的 `// QA-GATE-HANDLED: PushPrepromptView`——`dismissPushPrepromptIfPresent()`
        // 點「稍後再說」那一步）。
```

**② `LittleSproutUITests/QA/QADriver.swift`：插在第 197 行之前**（合併後第 196 行＝`    func dismissPushPrepromptIfPresent() throws {`，第 197 行＝`        guard pushPrepromptSkipButton.waitForExistence(timeout: 10) else { return }`）。**放在函式本體第一行、不要放在 `func` 上方**——第 186-195 行是 `///` 文件註解，中間插一行 `//` 會切斷它與宣告的綁定。原文（8 空格縮排）：

```swift
        // QA-GATE-HANDLED: PushPrepromptView（LS-232：對應 `RootView.swift` 的
        // `// QA-GATE: PushPrepromptView` 標記——這裡點「稍後再說」再等一次時間軸就是處理方式）。
```

（等價的另一個落點：第 186 行 `/// LS-217 QA R1 FAIL…` 之**上**。放 `///` 之上不會切斷綁定，但函式本體第一行與本 PR 既有兩處標記的擺法一致。）

**不補會怎樣**：現況（本 PR 未修 M1）→ back-merge 後 gate 仍**綠**，只印一行 ⚠ 點名 `PushPrepromptView`——等於 `development` 上這個真實存在的 gate 從此不在對帳清單內，下一次有人改 `QADriver` 拿掉 push 處理也不會紅。若 M1 依上面改法修掉 → back-merge 後會**紅**（正確行為，逼著同一筆 back-merge 補上標記）。兩種情況都該補，差別只在會不會被機械擋下。

### (b) `docs/COLLABORATION.md` 第 133 行：**會衝突**，建議的合併句原文

衝突是單一 hunk、兩邊各一整行（我用 `git merge-file` 重現：`<<<<<<< development` 在 133、`=======` 在 135、`>>>>>>> LS-232(main)` 在 137）。兩邊共同的前綴（LS-220 的 (a)(b)(c) 排障順序，一路到「…回 200 即後端健康、該查 app 端邏輯而非容器本身。」）**完全相同、原樣保留**；差異只在該句之後各自追加的一段。建議把兩段合併成下面這一段，接在共同前綴之後（同一行，不換行）：

```
`QADriver` 目前認得的登入後全屏 gate 清單：EULA 同意頁（LS-220）、建立家庭後可跳過的寶貝建檔步驟（`CreateChildView`）、推播權限前置頁（`PushPrepromptView`，LS-217 QA R1 FAIL `e4863482`——`ensureFamily()` 建完家庭進時間軸後自動彈出，`dismissPushPrepromptIfPresent()` 判定並點「稍後再說」）。**新增登入後全屏 gate 的票，DoD 必含 `QADriver` 更新**（同型前例：LS-190 EULA gate 未同步更新 QADriver，卡了 LS-220 一整輪；LS-217 推播前置頁重演同一種回歸）——已由機械 gate `qa-driver-gate-check`（LS-232）把關：`RootView.swift`（及其 `RootView+*.swift` 直接子 View）用 `// QA-GATE: <View>` 標記宣告登入後全屏 gate 清單，`QADriver*.swift` 用 `// QA-GATE-HANDLED: <View>` 標記已處理清單，push-gate／CI `rules` job 對帳兩者的差集，非空即紅——不再只靠這裡的排障口訣或 agent 自律提醒。
```

三個刻意的取捨，供 orchestrator 取捨時參考：

1. **補進 `CreateChildView`**——back-merge 後宣告清單實際是 3 個（`EULAConsentView`／`CreateChildView`／`PushPrepromptView`），development 原句只列 2 個。
2. **拿掉 development 句裡的「（`.fullScreenCover`）」限定**——EULA gate 是條件式整樹替換、不是 `.fullScreenCover`，留著會讓讀者以為只有 modifier 型態才算。
3. **「機械化見下方 harness 票」換成實際敘述**——這句正是 LS-217 merge-review R5（`b9abb471`）m2 指出的死指標（`grep -rn "LS-232" docs/` 當時 0 hit），本次合併順帶解掉。

若 M1 要求的修正落地，這段結尾的「非空即紅」還應補上備援掃描那一條（未標記的 `.fullScreenCover`／`.sheet` 綁定亦紅、非 gate 用 `// QA-GATE-EXEMPT:` 豁免）；本輪先給現況版本。

---

## 第 2 點：三次回歸夠不夠證明 gate 有效——**不夠**（這是 M1 的另一面）

三次回歸我逐一核對，數字與原文都對得上，但它們證明的命題比票文要的窄：

| 回歸 | handoff 主張 | 我的重放／核對 | 實際證明了什麼 |
|---|---|---|---|
| dev `938e854`（R5 前） | 紅，列 `PushPrepromptView` | **我自己重放**（`git archive 938e854` → `--repo`）：未 patch 標記 → **綠 exit 0**；handoff 的紅需先 patch `// QA-GATE: PushPrepromptView` | 只證明「有宣告標記、沒有處理標記 → 紅」，**沒有**證明票文驗收 4 的「R5 前的 head 會紅」 |
| dev `ebe390a`（R5 後） | 綠 | 核對 `.claude/evidence/LS-232/r1/regression-dev-shas.txt` 原文，與 handoff 一致 | 只證明「兩個標記都在 → 綠」 |
| main head `c0ee0b2` | 綠，2 個 gate | **我自己實跑** `bash scripts/gates/qa-driver-gate-check.sh` → `✓ …宣告的 2 個登入後全屏 gate（CreateChildView、EULAConsentView）QADriver 皆已標記處理`、exit 0、零 ⚠ | 屬實 |

兩個 dev 快照的標記都是人手 patch 進去的，所以這組回歸驗的是「**人有沒有把兩個標記都寫齊**」，不是「**gate 抓不抓得到新增的全屏 gate**」。要補的回歸就是 M1 改法第 4(c) 條：拿未經 patch 的 `938e854` 形狀當 fixture，斷言紅。

---

## 已驗證（怎麼驗）—— handoff 逐條對應票文範圍編號

- **handoff 已完成 1（票文範圍 1）**：`scripts/gates/qa_driver_gate_check.py`（181 行）＋`scripts/gates/qa-driver-gate-check.sh`（53 行）兩個新檔我完整讀過；`--repo` fallback `git rev-parse --show-toplevel`、找不到檔案／缺值／未知參數／多餘位置參數皆 exit 2 屬實。bash 3.2 相容：wrapper 只用 `case`／`while`／`[ ]`／`exec`，無 `declare -A`／`mapfile`／`${var,,}`。`git show --stat ddf1496` 確認落在該 commit。
- **handoff 已完成 2（票文範圍 1）**：`git diff origin/main...c0ee0b2 -- LittleSprout/ LittleSproutUITests/` 逐行看完——三個 Swift 檔共 +11 行，**每一行都以 `//` 開頭**，無任何邏輯行、無刪除行。行號 `grep -n "QA-GATE"` 核對：`RootView+AuthenticatedGate.swift:103`（`EULAConsentView(` 在 :107）、`RootView.swift:117-119`（handoff 誤寫 114-116，見 i5）、`QADriver.swift:127-128`／`:171-172` 皆屬實。**零行為變更成立。**
- **handoff 已完成 3（票文範圍 2，接線）**：`scripts/gates/push-gate.sh:180-190` 的 3c 步驟——`push-gate.sh:14` 是 `set -euo pipefail`，3c 直接 `bash …/qa-driver-gate-check.sh` 未加 `|| true`，非 0 會中止 push，接線確實有牙齒。位置在 xcodebuild unit tests（`:262`）之前，屬「便宜檢查前移」慣例。`skip_swift_steps` 的 allowlist（`push-gate.sh:63`）**含 `(^|/)LittleSproutUITests/`**，所以只改 `QADriver.swift` 拿掉處理標記的 PR 不會被誤判成純文件而跳過——這點我特地查過，沒有漏洞。
- **handoff 已完成 3（CI）**：`.github/workflows/ci.yml` `rules` job 新增獨立 step「QADriver 全屏 gate 對帳（qa-driver-gate-check，LS-232）」（無條件跑）＋掛進「Gate 腳本自測」step 並更新 `name:` 清單。`gh run view 34687745246` 確認**兩個 step 都 conclusion=success**（見下方 CI 段）。
- **handoff 已完成 4（票文範圍 2，自測）**：`bash scripts/gates/qa-driver-gate-check.test.sh` 我實跑 → 17 組全 `✓`、末行 `qa-driver-gate-check.test.sh：全部通過`、exit 0，與 handoff 的 17 組一致。
- **mutation 重放（LS-209）——handoff 申報 1 組，我重放 2 組，皆對得上**（兩組分別見下兩點；自測檔 `scripts/gates/qa-driver-gate-check.test.sh` 實跑，真 repo 複本另跑一次）：
  1. handoff 申報的合成 fixture mutation＝自測 ③。改了什麼一行：把 fixture `QADriver.swift` 的 `// QA-GATE-HANDLED: PushPrepromptView` 拿掉。哪條測試紅：`③mutation拿掉標記後-轉回紅`（`grep -n "③mutation拿掉標記後-轉回紅" scripts/gates/qa-driver-gate-check.test.sh`）。斷言訊息原文：`✗ qa-driver-gate-check：QADriver 沒有處理下列登入後全屏 gate：` ＋ `    - PushPrepromptView（宣告於 …/RootView.swift:5）——在 QADriver 加落點＋dismiss…`，exit 1。**實跑 PASS，與申報一字不差。**
  2. 我額外做的**真 repo** mutation（派工單第 3 點點名的那條）：`git archive HEAD` 匯出複本，`grep -v "QA-GATE-HANDLED: EULAConsentView"` 拿掉 QADriver 的 EULA 處理標記 → `bash scripts/gates/qa-driver-gate-check.sh --repo <複本>` → `✗ … - EULAConsentView（宣告於 …/RootView+AuthenticatedGate.swift:103）…`、**exit 1**。真紅、是預期的斷言失敗（不是 crash／build fail）。
- **handoff 已完成 5／6（票文範圍 3）**：`bash scripts/gates/agent-tools-check.sh` 我實跑 → `ios-dev.md：正文含「新增登入後全屏 gate 必同 PR 更新 QADriver」`、末行 `✓ agent-tools gate 通過（6 份 agent 定義；正文必含字樣 39 條）`、exit 0（38→39 屬實）。`bash scripts/gates/agent-tools-check.test.sh` 我實跑 → 末行 `✓ agent-tools-check 自測通過（120 組樣本）`、exit 0，含新增的 ⑳ 三組（正／負／mutant）。
- **接線不破壞既有 gate**：`bash scripts/gates/push-gate.test.sh` 我實跑 → rc=0、`grep -c "^✓"` ＝ **96**、末行 `✓ push-gate 模擬器自測通過`，與 handoff 的 96 組一致。

## Scope（票文「只加 gate＋接線＋文件＋標記」）——**乾淨，無 finding**

12 個檔案逐一對應票文範圍：範圍 1 → 3 個新 gate 檔＋3 個 Swift 標記檔；範圍 2 → `push-gate.sh`（+13，純加法）、`ci.yml`（+12／−1，那 1 行是自測 step 的 `name:` 清單續寫）；範圍 3 → `COLLABORATION.md`（+2／−1，同理是 §4-b 那行加句）、`ios-dev.md`（+1）、`agent-tools-check.sh`（+3／−1，BODY_RULES 末行續寫）、`agent-tools-check.test.sh`（+18／−2）。**沒有順手重構、沒有無關改動、沒有動到票文「不做」的 `QADriver` 實際處理邏輯與 `qa-e2e.sh` 情境**。commit 三段切分（`ddf1496` gate＋標記／`975618c` 接線／`c0ee0b2` 文件＋agent 定義）與 handoff 敘述一致（`git show --stat` 逐一核對）。

## 四個必審維度

- **Race condition**：生產碼三個檔案共 +11 行全是註解（上面逐行核對過），無 `Task`／`actor`／`@MainActor`／`Sendable` 面變動，**無並發風險**。gate 本身是單次執行的單執行緒純文字掃描，無共享狀態。
- **運算效能**：無 DB／RLS／查詢／圖片路徑變更。gate 掃 2 個 `RootView*.swift` ＋ 1 個 `QADriver*.swift`，複雜度 O(行數)，備援掃描最壞 O(行數 × 20)；實跑本 repo 亞秒級。push-gate 3c 落在 xcodebuild 之前，對 push 總時長影響可忽略。**無 finding**。
- **平行優化**：無可平行化的工作（單次檔案掃描），不適用。
- **Scope**：見上，乾淨。

## CI（head `c0ee0b2`）

`gh run view 34687745246` → `status: completed`、`conclusion: success`，五 job 全綠：`db` 3m51s／`lint` 12s／`rules` 7m12s／`ci-ipad` 6m7s／`ci`（本輪最後完成）。`rules` job 內本票新增的兩處我個別確認 conclusion：獨立 step「QADriver 全屏 gate 對帳（qa-driver-gate-check，LS-232）」＝**success**；「Gate 腳本自測（…／qa-driver-gate-check）」step＝**success**。無 flake 需要判讀、無 rerun。

## 未完成／跳過（誠實聲明）

1. 未跑 `xcodebuild test`／未 boot 任何模擬器——本 PR 生產碼零行為變更（已逐行確認），採信同 head 的 CI `ci`／`ci-ipad` 綠。故無 LS-205 runtime 比對，也未執行 `xcrun simctl ui`。
2. 未跑 `supabase db reset`／`supabase/tests/run.sh`——本 PR 不含 migration／SQL，採信 CI `db` job 綠。**未持有 supabase lock**（不需要）。
3. 未實跑 `qa-e2e.sh` 任何情境——票文「不做」明列 qa-e2e 情境本身，且本 PR 不動 QADriver 處理邏輯。
4. back-merge 的實際合併我只用 `git merge-tree`／`git merge-file` **算過**、未真的建立 back-merge 分支或寫入任何檔案（reviewer 不改檔）。第 5 點給的行號以那次合併結果（tree `ac19d398…`）為準；若 back-merge 前 `development` 又前進，行號需重算。
5. 未逐一審閱 `push-gate.test.sh` 96 組與 `agent-tools-check.test.sh` 120 組的全部樣本內容——只實跑取結果，並針對本票新增的 ⑳ 三組與 3c 的覆蓋缺口（i4）做了定點檢查。

## handoff-evidence-check 輸出

`bash scripts/gates/handoff-evidence-check.sh <本文暫存檔> --repo /Users/clyeh/little-sprout/.claude/worktrees/LS-232` → **exit 0**，「已驗證」段 8 個列項全綠：

```
✓ 第 164 行起的列項有證據
✓ 第 165 行起的列項有證據
✓ 第 166 行起的列項有證據
✓ 第 167 行起的列項有證據
✓ 第 168 行起的列項有證據
✓ 第 169 行起的列項有證據
✓ 第 170 行起的列項有證據
✓ 第 171 行起的列項有證據
```

（過程註記，供 harness 參考：第一次跑撞到腳本檔頭自述的 **N6(b)** 已知限制——`find_section` 只取第一個標題含「驗收／查實／已驗證」的段落，我原本的「第 1 點裁決」標題裡有「驗收 4」字樣，於是被當成待掃段落而報「段落內沒有任何列項」。把該標題改寫成不含關鍵字後即正常掃到真正的「已驗證」段。這是既有已知限制、不是本票引入的問題，僅記錄。）

### 2026-09-12T10:52:56.147Z · Chih-Lin Yeh · `4f8a9fb2`

**orchestrator 09-12 18:4x**：merge-review R1 `2f276f29` **REQUEST_CHANGES**（M1：對未 patch 的事故 head `938e854` 跑出貨版 gate → 宣告 0 個、exit 0，放行了它要擋的 commit；m1 `CONTAINER_EXCLUDE` 缺 SwiftUI 基本型別；m2 §7 新列宣稱過實作；5 info；CI 五綠）。裁定：備援掃描改為紅（候選 − 宣告 − `// QA-GATE-EXEMPT:` 非空即紅）＋自測補未 patch 的 `938e854`／`ebe390a` fixture。已派 ios-dev R2。reviewer 已給 back-merge 兩處標記行（`RootView.swift` 合併後第 150 行前、`QADriver.swift` 第 197 行前）與 COLLABORATION.md:133 合併句原文——R2 併入後 back-merge 走 `hotfix/LS-232-backmerge-development` 分支套用。

### 2026-09-12T11:16:28.062Z · Chih-Lin Yeh · `139cc33f`

## Ticket

LS-232 R2（修 merge-review R1 REQUEST_CHANGES，comment `2f276f29`：1 major／2 minor／5 informational）

worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-232`，分支 `hotfix/LS-232-qa-driver-gate-check`，PR https://github.com/CLYEH/little-sprout/pull/365（→ `main`），head **`a5773c0e604974b65ba78f5ef3f912e762f2ef12`**。

## 已完成

1. **M1（major，已修 commit `00ab8a5`）**：`scripts/gates/qa_driver_gate_check.py` `run()`——備援掃描候選原本只印 ⚠、不影響 exit code，對未 patch 的事故 head `938e854`（`RootView.swift` 完全沒有任何 `// QA-GATE` 標記）reviewer 實測是綠 exit 0，放行了它要擋的 commit。改法：新增 `// QA-GATE-EXEMPT: <View>` 豁免標記（`EXEMPT_MARKER_RE`），候選－宣告－豁免非空即紅（已宣告的候選仍走既有差集邏輯，不重複列），紅訊息印候選 View 名＋位置＋修法提示。
2. **m1（minor，與 M1 同 commit `00ab8a5`）**：`CONTAINER_EXCLUDE` 補 `Text`／`Button`／`Image` 三個 SwiftUI 基本型別（reviewer 實測 `.sheet { Text(…) }` 會被誤判成候選，M1 轉紅後會是假紅），規則寫在檔頭「備援掃描」段。
3. **自測（commit `2ad8506`，17→21 組）**：④ 翻面（未標記 modifier 綁定從「印 ⚠ 不轉紅」改為「轉紅＋列候選＋修法提示」）；新增 ⑨ `// QA-GATE-EXEMPT` 豁免→綠；⑩ `.sheet { Text(…) }` 假紅案例（m1）→綠；⑪⑫ 用 `git show <sha>:<path>` 取 LS-217 事故當下**未經任何 patch** 的 development head `938e854`／`ebe390a`（兩者 `RootView.swift` 逐字相同、皆無任何標記）各驗一次，皆紅、列出 `PushPrepromptView`。
4. **m2（minor，已修 commit `a5773c0`）**：`docs/COLLABORATION.md:336` §7 對照表——原句宣稱「修正前紅」但那個紅是人工 patch 標記後才成立，M1 修完後這句話已經是真的，已訂正敘述並補上 M1／m1 修法摘要與更準確的已知盲區敘述。
5. **PR #365 body**：既有段落原樣保留，新增「R2 處置表」段（M1／m1／m2 各附 commit SHA＋檔案:行號；i1–i4「不修＋理由」；i5「純文字更正」；三次回歸原文；自測摘要）。

## 已驗證（怎麼驗）

- **M1 mutation（LS-232 R2 必修「mutation：拿掉『未標記即紅』邏輯」）**：改了什麼一行——`run()` 內 `unmarked = {}` 取代累加 `scan_modifier_backup` 候選的迴圈（`unmarked = {}  # MUTATION: LS-232 R2 驗證用，故意拿掉未標記即紅邏輯`）；哪條測試紅——對 `938e854`（未 patch，`git show` 取值）快照重跑 `qa-driver-gate-check.sh --repo <快照>`；斷言訊息原文——mutated 後 `✓ qa-driver-gate-check：宣告的 0 個登入後全屏 gate（無）QADriver 皆已標記處理`、exit 0（轉綠，證明現行邏輯正是能抓到這個 fixture 的原因）；還原後同一 fixture 轉回 `✗ qa-driver-gate-check：偵測到疑似登入後全屏 gate 但未標記（2 個）：` 換行列出 `CreateChildView`／`PushPrepromptView`、exit 1；`git diff --stat scripts/gates/qa_driver_gate_check.py` 對還原後版本核對零差異（`.claude/evidence/LS-232/r2/mutation-unmarked-logic.txt`）。
- **自測 ③ mutation（既有，R1 沿用未變動）**：改了什麼一行——拿掉合成 fixture `QADriver.swift` 的 `// QA-GATE-HANDLED: PushPrepromptView`；哪條測試紅——`③mutation拿掉標記後-轉回紅`（`grep -n "③mutation拿掉標記後-轉回紅" scripts/gates/qa-driver-gate-check.test.sh` 可驗存在）；斷言訊息原文——`✗ qa-driver-gate-check：QADriver 沒有處理下列登入後全屏 gate：` 換行 `    - PushPrepromptView（宣告於 …/RootView.swift:5）——在 QADriver 加落點＋dismiss（比照既有 EULA／推播前置頁處理），並在對應位置加 \`// QA-GATE-HANDLED: PushPrepromptView\` 標記`，exit 1。
- **自測 ④ 翻面（LS-232 R2 必修「自測」第 1 項）**：改了什麼一行——CreateChildView 綁定 `.fullScreenCover` 但 `RootView.swift` 無任何 `// QA-GATE:` 標記；哪條測試紅——`④備援掃描未標記轉紅`（`grep -n` 可驗存在）；斷言訊息原文（隔離重跑實測）——`✗ qa-driver-gate-check：偵測到疑似登入後全屏 gate 但未標記（1 個）：` 換行 `    - CreateChildView（…/RootView.swift:7（\`.fullScreenCover\`））——若這是登入後全屏 gate，請加 \`// QA-GATE: CreateChildView\` 標記＋在QADriver 加落點＋dismiss 處理＋加 \`// QA-GATE-HANDLED: CreateChildView\` 標記；若不是，請加 \`// QA-GATE-EXEMPT: CreateChildView\` 並寫明理由`，exit 1。
- **自測 ⑨（`// QA-GATE-EXEMPT` 豁免）**：測試名 `⑨QA-GATE-EXEMPT豁免-綠`（`grep -n "⑨QA-GATE-EXEMPT豁免-綠" scripts/gates/qa-driver-gate-check.test.sh` 可驗存在）；隔離重跑實測輸出 `✓ qa-driver-gate-check：宣告的 0 個登入後全屏 gate（無）QADriver 皆已標記處理`、exit 0，且不含 `CreateChildView` 字樣（未被列進未標記清單）。
- **自測 ⑩（m1 `Text` 假紅案例）**：測試名 `⑩Text假紅案例-綠`（`grep -n "⑩Text假紅案例-綠" scripts/gates/qa-driver-gate-check.test.sh` 可驗存在）；隔離重跑實測輸出 `✓ qa-driver-gate-check：宣告的 0 個登入後全屏 gate（無）QADriver 皆已標記處理`、exit 0，不含 `Text` 字樣。
- **自測 ⑪⑫ ＋ 回歸三次原文（LS-232 R2 必修「補未 patch 的 938e854 fixture」＋「回歸三次原文重貼」）**：怎麼驗——`git show <sha>:LittleSprout/Navigation/RootView.swift`／`git show <sha>:LittleSproutUITests/QA/QADriver.swift` 取兩個 development SHA（`938e854c3ab81bcf469613edbc7e12bace1da27e`、`ebe390a528234b4fa9be1cd0d05a6c87eb8452c8`，皆為 `origin/development` 祖先，`git merge-base --is-ancestor` 已核對）原始內容建 fixture（未經任何 patch），對本 PR head 也跑一次；三次原文（`.claude/evidence/LS-232/r2/regression-real-snapshots.txt`）：
  ```
  === 938e854（LS-217 R5 修正之前的 development head；未經任何 patch） ===
  ✗ qa-driver-gate-check：偵測到疑似登入後全屏 gate 但未標記（2 個）：
      - CreateChildView（…/RootView.swift:136（`.fullScreenCover`））——若這是登入後全屏 gate，請加 `// QA-GATE: CreateChildView` 標記＋在QADriver 加落點＋dismiss 處理＋加 `// QA-GATE-HANDLED: CreateChildView` 標記；若不是，請加 `// QA-GATE-EXEMPT: CreateChildView` 並寫明理由
      - PushPrepromptView（…/RootView.swift:155（`.fullScreenCover`））——若這是登入後全屏 gate，請加 `// QA-GATE: PushPrepromptView` 標記＋在QADriver 加落點＋dismiss 處理＋加 `// QA-GATE-HANDLED: PushPrepromptView` 標記；若不是，請加 `// QA-GATE-EXEMPT: PushPrepromptView` 並寫明理由
  exit=1

  === ebe390a（LS-217 R5 修正之後的 development head；同樣未經任何 patch——RootView.swift 內容與 938e854 逐字相同） ===
  ✗ qa-driver-gate-check：偵測到疑似登入後全屏 gate 但未標記（2 個）：
      - CreateChildView（…/RootView.swift:136（`.fullScreenCover`））——若這是登入後全屏 gate，請加 `// QA-GATE: CreateChildView` 標記＋在QADriver 加落點＋dismiss 處理＋加 `// QA-GATE-HANDLED: CreateChildView` 標記；若不是，請加 `// QA-GATE-EXEMPT: CreateChildView` 並寫明理由
      - PushPrepromptView（…/RootView.swift:155（`.fullScreenCover`））——若這是登入後全屏 gate，請加 `// QA-GATE: PushPrepromptView` 標記＋在QADriver 加落點＋dismiss 處理＋加 `// QA-GATE-HANDLED: PushPrepromptView` 標記；若不是，請加 `// QA-GATE-EXEMPT: PushPrepromptView` 並寫明理由
  exit=1

  === main head（本 PR head a5773c0，兩處 QA-GATE 標記皆已補上） ===
  ✓ qa-driver-gate-check：宣告的 2 個登入後全屏 gate（CreateChildView、EULAConsentView）QADriver 皆已標記處理
  exit=0
  ```
  兩個歷史快照皆紅，證明「沒寫標記」本身會被抓，不因為 QADriver 當時是否已有處理程式碼（`ebe390a` 已有真實的 `dismissPushPrepromptIfPresent()`）而放行——這正是 M1 修正後的行為（R1 版本對這兩個未 patch 快照皆綠 exit 0）。
- **全量自測**：`bash scripts/gates/qa-driver-gate-check.test.sh` → 21 組全 `✓`，末行 `qa-driver-gate-check.test.sh：全部通過`，exit 0。
- **不破壞既有 gate**：`bash scripts/gates/push-gate.test.sh` → 96 組全 `✓`，末行 `push-gate 模擬器自測通過`，exit 0；`bash scripts/gates/agent-tools-check.sh` → `✓ agent-tools gate 通過（6 份 agent 定義；正文必含字樣 39 條）`，exit 0；`bash scripts/gates/agent-tools-check.test.sh` → `✓ agent-tools-check 自測通過（120 組樣本）`，exit 0；`swiftlint lint --strict --quiet` → exit 0（純文字／註解變更，無 Swift 邏輯行）。
- **m2 文件訂正**：`docs/COLLABORATION.md:336` diff 已核對（`git show a5773c0`），敘述改為「M1 修完後這句話已經是真的（未 patch 也紅）」，不再宣稱未修正版本做不到的能力。
- **PR body gate**：`bash scripts/gates/pr-body-check.sh <暫存檔> --branch hotfix/LS-232-qa-driver-gate-check --verify` → `✓ 第 40 行「已修」SHA 00ab8a5 在 PR commits 內`、`✓ 第 48 行「已修」SHA a5773c0 在 PR commits 內`、`✓ 申報反查：LS-96 行 0 條、「已修」行 2 條`，exit 0（未接管線、直接看 exit code）。
- **push gate（`scripts/gates/push-gate.sh`，含全量單元測試）**：`git push origin hotfix/LS-232-qa-driver-gate-check` 輸出含 `✓ qa-driver-gate-check：宣告的 2 個登入後全屏 gate（CreateChildView、EULAConsentView）QADriver 皆已標記處理`（3c 步驟原文）與 `✓ push gate 通過`；push 成功（`c0ee0b2..a5773c0`）。**模擬器 runtime 註記（LS-205）**：push gate 印 `⚠ detect-simulator：既有專屬機「LS-232-iPhone17Pro」目前是 iOS 26.0，與釘住版 iOS 26.2 不同（CI 為 iOS 26.2）`——本輪未新增任何 UI／點擊目標畫面，量測基準差異對本票改動無實質影響（純 gate 邏輯＋純註解＋文件）。

## 未完成／reviewer informational 處置（5 條，一條不能省）

- **i1**（`ROOT_VIEW_GLOB` 只掃 `RootView*.swift`）：**不修**——票文範圍 (a) 明文界線（"`RootView.swift`（及其直接子 View）"），`qa_driver_gate_check.py` 檔頭與 §7 已記錄「已知盲區」，reviewer 本人判定屬票文範圍內的已知界線、不是遺漏。
- **i2**（`BACKUP_SCAN_LOOKAHEAD = 20` 視窗上限）：**不修**——reviewer 判定目前餘裕充足（實際 modifier 到候選距離 7／8 行，遠低於 20 行上限）；M1 修完後風險等級雖從「少印一行提醒」升級，但非本輪必修範圍，記錄供未來評估。
- **i3**（`push-gate.sh:184` 前置條件在檔案搬家時靜默跳過）：**不修**——CI `rules` job 無條件跑＋`find_files` 找不到檔案 `fail()` exit 2（fail-closed）已補位，與既有慣例（`tap-target-registry-check` 的 `if [ -d LittleSprout/Features ]`）一致，reviewer 不要求改。
- **i4**（`push-gate.test.sh` 對新增 3c 步驟零覆蓋）：**不修**——符合既有慣例（gate 正確性由自己的 `.test.sh` 顧、CI 無條件跑補位），reviewer 記錄不修。
- **i5**（handoff 行號 `114-116`→`117-119`）：**已修**——本 comment 與 PR #365 body 皆已更正為正確行號 `117-119`（`CreateChildView` 的 `// QA-GATE:` 標記實際行號，`grep -n "QA-GATE" LittleSprout/Navigation/RootView.swift` 核對）。

## 風險

- 低：`docs/COLLABORATION.md:133` 段落與 back-merge `main`→`development` 的合併——R1 已裁決走 `hotfix/LS-232-backmerge-development` 分支，reviewer 給的兩處標記行（`RootView.swift` 合併後第 150 行前、`QADriver.swift` 第 197 行前）與合併句原文未變動（本輪未動 back-merge，等 orchestrator 依 R1 comment `2f276f29` 第 5 點執行）。
- 低：三個生產碼檔案（`qa_driver_gate_check.py`、`qa-driver-gate-check.test.sh`、`docs/COLLABORATION.md`）皆為 gate 邏輯／自測／文件變更，無 Swift 生產碼異動，`swiftlint --strict` 與 push gate 全量單元測試皆通過。

## 產出位置

- PR：https://github.com/CLYEH/little-sprout/pull/365（head `a5773c0e604974b65ba78f5ef3f912e762f2ef12`，→ `main`）
- Commits：`00ab8a5`（M1＋m1）、`2ad8506`（自測＋fixture）、`a5773c0`（m2＋文件）
- 證據：`.claude/evidence/LS-232/r2/regression-real-snapshots.txt`（938e854／ebe390a 未 patch 紅、main head 綠原文）、`.claude/evidence/LS-232/r2/mutation-unmarked-logic.txt`（M1 mutation 驗證）
- 模擬器已關：`50E3FE6E-99F4-4E8D-B13C-E62B8D848B81`（`LS-232-iPhone17Pro`，push gate 自動 boot／shutdown，本輪未額外手動操作模擬器）

### 2026-09-12T11:17:34.890Z · Chih-Lin Yeh · `0d03800e`

**orchestrator 09-12 19:17**：R2 交件 head `a5773c0`（handoff `139cc33f`：未標記候選非空即紅＋真快照 `938e854`／`ebe390a` 迴歸＋mutation 重放＋自測 21 組＋COLLABORATION §7 列訂正）→ 派 merge-reviewer R2（聚焦 M1 關閉重放、CONTAINER_EXCLUDE 是否新 fail-open、back-merge 標記行複核）。

### 2026-09-12T11:31:21.383Z · Chih-Lin Yeh · `e84534ed`

# LS-232 merge-review R2 — APPROVE

（09-12 19:2x 完成；在票 worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-232` 審。**未持有 supabase lock**——純文字掃描 gate，不需要 DB／模擬器，未跑 `supabase db reset`／`run.sh`。未跑 `xcodebuild test`、未 boot 模擬器、未執行 `xcrun simctl ui`，故無 LS-205 runtime 比對需求。）

- head：`a5773c0e604974b65ba78f5ef3f912e762f2ef12`（PR #365，`hotfix/LS-232-qa-driver-gate-check` → `main`）。開審 `git fetch origin && git status` → `nothing to commit, working tree clean`、`git rev-parse HEAD` = `a5773c0e6…2ef12`，與派工給的 head 一致；`gh pr view 365 --json headRefOid` 當下同值（審查期間無人 push）。
- 審的 diff：R2 delta `git diff c0ee0b2..a5773c0`（3 檔 +164／−54）逐行看完；另複看全 PR `git diff origin/main...a5773c0`（12 檔 +599／−5）。R1→R2 **零 Swift／零接線變動**（`git diff --stat c0ee0b2..a5773c0 -- '*.swift' '.github/' 'scripts/gates/push-gate.sh' '.claude/agents/'` 輸出為空），故 R1 對 Swift 標記與接線的逐行結論沿用。
- 判定：**0 blocker／0 major／2 minor／4 informational → APPROVE**（minor 不擋合併；建議處置寫在各條末尾）

---

## 第 1 點：M1 是否真關 —— **是**，我自己對真快照重放（不採信 handoff）

```
git archive 938e854c3ab81bcf469613edbc7e12bace1da27e | tar -x -C <scratch>/938e854
git archive ebe390a528234b4fa9be1cd0d05a6c87eb8452c8 | tar -x -C <scratch>/ebe390a
grep -rn "QA-GATE" <scratch>/938e854/LittleSprout/ <scratch>/938e854/LittleSproutUITests/ | wc -l   # → 0（未經任何 patch）
grep -rn "QA-GATE" <scratch>/ebe390a/LittleSprout/ <scratch>/ebe390a/LittleSproutUITests/ | wc -l   # → 0
git merge-base --is-ancestor 938e854… origin/development   # → exit 0（兩個 SHA 都是 development 祖先）
bash scripts/gates/qa-driver-gate-check.sh --repo <scratch>/938e854
```

輸出原文（938e854，未 patch）：

```
✗ qa-driver-gate-check：偵測到疑似登入後全屏 gate 但未標記（2 個）：
    - CreateChildView（…/RootView.swift:136（`.fullScreenCover`））——若這是登入後全屏 gate，請加 `// QA-GATE: CreateChildView` 標記＋在QADriver 加落點＋dismiss 處理＋加 `// QA-GATE-HANDLED: CreateChildView` 標記；若不是，請加 `// QA-GATE-EXEMPT: CreateChildView` 並寫明理由
    - PushPrepromptView（…/RootView.swift:155（`.fullScreenCover`））——若這是登入後全屏 gate，請加 `// QA-GATE: PushPrepromptView` 標記＋…
exit=1
```

`ebe390a` 同樣紅、同樣兩個名字、同樣行號（兩個快照 `RootView.swift` 逐字相同）。PR head（真 repo、不帶 `--repo`）：`✓ qa-driver-gate-check：宣告的 2 個登入後全屏 gate（CreateChildView、EULAConsentView）QADriver 皆已標記處理`、exit 0。

**三次輸出與 handoff `.claude/evidence/LS-232/r2/regression-real-snapshots.txt` 逐字相符**（行號 136／155 我獨立跑出同值）。R1 M1「對未 patch 的 `938e854` 放行」已關閉：`scripts/gates/qa_driver_gate_check.py:158-162`（候選 − 宣告 − 豁免）＋`:164`（`if missing or unmarked:`）讓未標記候選直接進 exit code。

## 第 2 點：mutation 重放 —— 「未標記即紅」確實 load-bearing

mutation 做在 scratch 複本（`cp` 出 `qa-driver-gate-check.sh`＋`qa_driver_gate_check.py`），**本 worktree 全程未改任何檔**。改了什麼一行：`run()` 內 `unmarked` 的累加迴圈（`qa_driver_gate_check.py:158-162`）整段換成 `unmarked = {}  # MUTATION`。

| 對象 | mutated 後 |
|---|---|
| `938e854` 未 patch 快照 | `✓ qa-driver-gate-check：宣告的 0 個登入後全屏 gate（無）QADriver 皆已標記處理`、**exit 0（翻綠）** |
| `ebe390a` 未 patch 快照 | 同上、exit 0 |
| PR head | `✓ …宣告的 2 個…`、exit 0（本來就綠，不受影響） |

與 handoff `.claude/evidence/LS-232/r2/mutation-unmarked-logic.txt` 申報**一字不差**（申報的斷言訊息原文與 exit code 都對得上，不是 crash／不是別條紅）。收工核對：`git status --porcelain` 空、`git diff --stat` 空、`git rev-parse HEAD` 仍 `a5773c0`。

## 第 3 點：CONTAINER_EXCLUDE 是否造成新的 fail-open

**先回答判定機制**：既不是「根節點型別」也不是「任一祖先」。`qa_driver_gate_check.py:124-135` 是從 modifier 那行起往下最多 20 行的**線性文字掃描**，逐一略過**任何位置**出現在 `CONTAINER_EXCLUDE` 的識別字（不分巢狀層級、**不看 closure 邊界**），取第一個非排除的「大寫識別字＋`(`」當候選。這一個機制同時解釋下面兩個反例。

**反例 A（以 `Button` 為根的真全屏 gate）→ 綠，確實 fail-open**（合成 fixture，`--repo` 指向該樹）：

```swift
.fullScreenCover(isPresented: $showGate) {
    Button("我知道了") { acknowledgeGate() }
        .buttonStyle(.borderedProminent)
}
```
→ `✓ qa-driver-gate-check：宣告的 0 個登入後全屏 gate（無）…`、exit 0。

**反例 D（更寫實的 inline gate：`VStack`＋`Text`＋`Button`，沒有具名 View 型別）→ 綠**；對照同一 fixture 用 R1 版 py（`git show c0ee0b2:scripts/gates/qa_driver_gate_check.py`）會印 `⚠ …疑似綁定 Text`——也就是 m1 的排除清單確實把這種 inline 形態的偵測面縮掉了一點（R1 版在 M1 規則下會紅，但紅的名字是 `Text`，是「對的紅、錯的名字」）。→ 列為 **N3（informational）**，理由見 Findings。

**反例 C（20 行視窗跨過 closure 邊界 → 假紅，而且指名無關型別）**：

```swift
.sheet(isPresented: $showHelp) {
    Text("純說明文字，不是 gate")
}
.background(Color.clear)
SomeUnrelatedHelper(value: 1)
```
→ `✗ …未標記（1 個）： - SomeUnrelatedHelper（…/RootView.swift:9（`.sheet`））…`、exit 1。→ **N2（minor）**。

**`RootView*.swift` 現有 gate 逐一核對（main head）**：`grep -n "\.fullScreenCover(\|\.sheet(" LittleSprout/Navigation/RootView*.swift` → **只有一處**（`RootView.swift:120`，候選 `CreateChildView`，已宣告 → 不列入未標記）；`EULAConsentView` 是條件式整樹替換（`RootView+AuthenticatedGate.swift:103-107`），只有標記抓得到。宣告 2／候選 1／未標記 0 → 綠，與實跑一致。（派工單寫的「現有五個 gate」在 main head 不成立：main 上只有 2 個、`PushPrepromptView` 還在 `development`，back-merge 後合計 3 個。）

## 第 4 點：自測 21 組＋CI 是否真的跑到

- `bash scripts/gates/qa-driver-gate-check.test.sh` 實跑 → **21 個 `✓`、末行 `qa-driver-gate-check.test.sh：全部通過`、exit 0**。
- ⑪⑫ **確實走真快照**：`scripts/gates/qa-driver-gate-check.test.sh:255-266` 用 `git -C "$root" show "${sha}:LittleSprout/Navigation/RootView.swift"`／`…QADriver.swift` 取 `938e854`／`ebe390a` 原文建 fixture（不是手工樣本），且 `git show` 失敗時 `fail=1`（fail-closed，不會靜默跳過）。
- CI 接線：`.github/workflows/ci.yml:51`（`rules` job `fetch-depth: 0`）、`:228`（「Gate 腳本自測」step 呼叫 `qa-driver-gate-check.test.sh`）、`:263-264`（獨立 step 無條件跑 gate 本體）。
- CI run `34690283176`（head `a5773c0`）：`rules` job **success**，兩個 step 個別反查皆 `conclusion: success`——這同時證明 hotfix→main 的 PR checkout 在 CI 上 `git show 938e854`（development 側歷史）拿得到，⑪⑫ 不會在 CI 變成假綠或假紅。`db`／`lint`／`ci-ipad` 亦 success；`ci` job 當下仍 `in_progress`（見「未完成」）。

## 第 5 點：scope

- R2 delta 只有 3 檔：`qa_driver_gate_check.py`（M1＋m1＋檔頭）、`qa-driver-gate-check.test.sh`（④翻面＋⑨⑩⑪⑫）、`docs/COLLABORATION.md`（**只有第 336 行那一列，1 行改 1 行**；`:133` 那句未被動到）。無順手重構、無無關改動、未動票文「不做」的 QADriver 處理邏輯與 `qa-e2e.sh`。
- i5 行號修正**正確**：`awk 'NR>=114 && NR<=122' LittleSprout/Navigation/RootView.swift` 印出 `:117-119` 就是 `CreateChildView` 的 `// QA-GATE:` 三行註解（`:120` 才是 `.fullScreenCover(`），`:114-116` 是 LS-113 既有註解。handoff 與 PR body 都已改成 `117-119`。
- i1～i4 的「不修＋理由」與 R1 的原意一致（i2 我另有補充，見 N6）。PR body「R2 處置表」五條齊全，但有兩處引用瑕疵 → **N5（informational）**。

## 第 6 點：back-merge（給 orchestrator 的精確落點）

1. **dev 現況確為紅**：`git archive origin/development`（`922884e7787890d947b2eb9acee1c1aedc48aee5`）→ `bash scripts/gates/qa-driver-gate-check.sh --repo <snap>` → `✗ …未標記（2 個）：CreateChildView（:136）、PushPrepromptView（:155）`、exit 1。
2. **合併樹**：`git merge-tree --write-tree origin/development a5773c0` → tree `7be3a5b064dd6d8b35d57f4ac5f5631494a39633`、rc=1；三個 Swift 檔 `Auto-merging` 乾淨，`CONFLICT (content): Merge conflict in docs/COLLABORATION.md`（與 R1 結論相同）。把該 tree `git archive` 出來跑 gate → **紅，只剩 `PushPrepromptView`（`RootView.swift:158`）**——`CreateChildView` 的標記隨本 PR 進來了。R2 的 M1 讓 back-merge **非補標記不可**（R1 時只會印 ⚠ 放行），這正是想要的行為。
3. **R1 給的兩處標記行原文對 R2 gate 仍有效**：我把 R1 的字逐字套進合併樹複本 → `✓ …宣告的 3 個登入後全屏 gate（CreateChildView、EULAConsentView、PushPrepromptView）…` exit 0；再 mutation 拿掉 QADriver 那行 → `✗ …QADriver 沒有處理下列登入後全屏 gate： - PushPrepromptView（宣告於 …/RootView.swift:150）…` exit 1。
4. **精確落點（行號以上述合併樹為準；back-merge 前 development 若再前進需重算）**：
   - `LittleSprout/Navigation/RootView.swift`：**插在第 150 行之前**（第 150 行＝`        .fullScreenCover(isPresented: Binding(`，PushPreprompt 那一個），8 空格縮排、三行註解，原文照 R1 comment `2f276f29` 第 5(a)①。
   - `LittleSproutUITests/QA/QADriver.swift`：**插在第 197 行之前**（第 196 行＝`    func dismissPushPrepromptIfPresent() throws {`），放函式本體第一行，**不要插進 186-195 的 `///` 文件註解與宣告之間**，原文照 R1 第 5(a)②。
5. **`docs/COLLABORATION.md:133` 合併句**：沿用 R1 第 5(b) 的原文，但結尾「非空即紅」那句**建議補上 R2 的新規則**：未標記的 `.fullScreenCover`／`.sheet` 綁定候選亦紅、非 gate 用 `// QA-GATE-EXEMPT: <View>` 豁免（R1 自己也預告了這一補充；否則 §4-b 又會落後實作一輪，正是 m2 的同型問題）。

---

## Findings

### N1（minor）`scripts/gates/qa-driver-gate-check.sh:12`／`:21-23`（`--help` 輸出）、`.github/workflows/ci.yml:227`——gate 自己的說明文字仍停在 M1 修正前

`--help` 至今印：「找到沒有 `// QA-GATE` 標記的疑似 gate 會印 ⚠（**不影響 exit code**）」，檔頭 `:10-12` 的 exit 說明也只講標記差集。**實際行為**：候選 − 宣告 − 豁免非空就 exit 1，且程式已完全不印 ⚠（`grep -n "⚠" scripts/gates/qa_driver_gate_check.py` → 只剩檔頭沿革敘述，無任何 `print`）。`ci.yml:227` 的 step 註解同樣寫「備援掃描 ⚠ 不轉紅」。

**失敗情境**：下一個被這支 gate 擋紅的 ios-dev 跑 `--help` 想確認判定規則，得到的是「這只是提醒、不影響 exit code」，於是往別的方向排障（或以為是壞掉的 gate）。這正是 R1 m2 指的同一種漂移——m2 只修了 `COLLABORATION.md:336`，gate 自己的 `--help` 與 CI 註解沒跟上。

**建議修法**：`qa-driver-gate-check.sh:21-23` 的 `--help` 文字與 `:10-12` 檔頭改成「備援掃描候選 − 宣告 − `// QA-GATE-EXEMPT:` 非空即紅」；`ci.yml:227` 註解同步。純文字、零風險，建議併進 back-merge 那支 PR 一起改（不必為它另開 PR）。

### N2（minor）`scripts/gates/qa_driver_gate_check.py:124-135`——20 行視窗會跨過 closure 邊界，產生「指名無關型別」的假紅

備援掃描找不到候選時不會停在 `}`，而是一路往下掃到第 20 行。所以一個**非 gate、內容只有被排除型別**的 `.sheet`，會把 closure **之後**的第一個具名型別認成它的綁定目標。上面反例 C 重現：`.sheet { Text(…) }` 之後第 4 行的 `SomeUnrelatedHelper(value: 1)` 被報成「`.sheet` 綁定、未標記」→ exit 1。

**失敗情境**：某張與本票無關的票在 `RootView.swift` 加一個純提示用的 `.sheet { Text(…) }`（或 `Button`／`Image` 開頭的小 sheet），push-gate／CI 立刻紅，訊息指著一個根本不在那個 closure 裡的型別名；照訊息照做的修法是替**無關型別**補 `// QA-GATE-EXEMPT:`，於是在 `RootView.swift` 留下一條語意錯誤的豁免，下次真的有人想豁免那個型別時就分不清了。m1（補 `Text`／`Button`／`Image`）消掉了 R1 實測的那個樣本名字，但沒消掉產生它的機制。

**現況不影響本 PR**：main head 的 `RootView*.swift` 只有一處 modifier、沒有這種形狀（實跑綠）；合併樹也只有 `PushPrepromptView` 一個候選。屬**尚未被踩到的假紅**，故 minor 不 major。

**建議修法（二選一，皆非本輪必修）**：(a) 候選搜尋在遇到該 modifier closure 的收合行（縮排回到 modifier 同層的 `}`／`))`）就停；(b) 只接受「modifier 之後、closure 大括號內」的行。與 R1 i2（20 行視窗上限）同根，建議**兩條併成一項記入 LS-96 池**（優先序由 orchestrator 依 §5-b 判）。

### N3（informational）沒有具名 View 型別的 inline gate 偵測不到（反例 A／D）

`.fullScreenCover { VStack { Text…; Button… } }` 這種把 gate 直接寫在 closure 裡、不抽成具名 View 的形態，R2 版一律找不到候選 → 綠。**不列為 major 的理由**：(1) 兩起真實事故（`EULAConsentView`／`PushPrepromptView`）都是具名 View，專案慣例（設計稿 → 具名畫面檔）也是具名；(2) `qa_driver_gate_check.py:38-40` 與 §7 新列的「已知盲區」已明說「找不到候選不代表沒有全屏 gate」；(3) R1 版對 D 也只會印一行 ⚠ 名為 `Text` 的雜訊，不是真的偵測。建議把「closure 內只有 `Text`／`Button`／`Image` 等基本型別」這個具體形狀補進檔頭盲區敘述（一句話），連同 N1 一起改。

### N4（informational）`// QA-GATE-EXEMPT:` 是無成本的自助靜音鍵，且作用域是「型別名全域」

`scan_markers(root_view_files, EXEMPT_MARKER_RE)`（`qa_driver_gate_check.py:152`）只取型別名，不綁位置也不要求理由——`RootView*.swift` 任何一行寫上 `// QA-GATE-EXEMPT: FooView`，整組檔案裡所有 `FooView` 候選就永久靜音（自測 ⑨ 的 fixture 有寫理由，但沒有任何機械檢查在驗它）。目前唯一的補位是 merge-review 看得到 diff。不建議本輪處理（加「理由必填」等於再造一支 gate），記錄供日後評估。註：豁免**不能**用來繞過已宣告 gate 的 `missing` 檢查（`:154` 與 exempt 無關），這點設計是對的。

### N5（informational）PR #365 body 兩處引用瑕疵（與 R1 i5 同類）

- 「### m1」段寫 `scripts/gates/qa_driver_gate_check.py:52-58（CONTAINER_EXCLUDE）`——實際 `CONTAINER_EXCLUDE` 在 `:68-73`（`:52-58` 是 docstring 結尾與 import）。
- 「## 已知盲區」段仍是 R1 原文（「就不會印 ⚠ 提醒……找不到 ⚠ 不代表沒有全屏 gate」），與 R2 行為不符——同 N1，PR body 是合併後的常駐紀錄，建議一併訂正。

### N6（informational）R1 i2（`BACKUP_SCAN_LOOKAHEAD = 20`）我複核後同意不修，但風險敘述要更新

我量過合併樹的實際距離：`RootView.swift:150` → 候選 `:158` ＝ 8 行，`:132` → `:139` ＝ 7 行，離 20 還很遠。但 M1 之後這個常數的失效方向已經從「少印一行提醒」變成「**該紅沒紅**」（fail-open），而且它與 N2 是同一段程式的一體兩面（視窗太短＝漏抓；視窗不認 closure 邊界＝誤抓）。建議與 N2 併成同一項池項處理，不要分開評估。

---

## 四個必審維度

- **Race condition**：R2 delta 零 Swift 變動（實測 `git diff --stat c0ee0b2..a5773c0 -- '*.swift'` 為空），R1 已逐行確認全 PR 的 Swift 變更只有 11 行註解；gate 本身是單次執行、單執行緒、無共享狀態的純文字掃描。**無 finding**。
- **運算效能**：無 DB／RLS／查詢／圖片路徑變更。掃描複雜度 O(行數 × 20)，對象是 2＋1 個檔案，實跑亞秒級；push-gate 3c 仍排在 `xcodebuild` 之前。**無 finding**。
- **平行優化**：單次檔案掃描，無可平行化工作，不適用。
- **Scope**：見第 5 點，乾淨。

## 已驗證（怎麼驗）—— 逐條對應派工單編號

- **派工單第 1 點（M1 是否真關）**：`git archive 938e854… | tar -x` ＋ `git archive ebe390a… | tar -x` 取未經任何 patch 的真快照（`grep -rn "QA-GATE" <snap>` → 0 hit 先確認沒被動過手腳），各跑 `bash scripts/gates/qa-driver-gate-check.sh --repo <snap>`：兩者皆 exit 1、皆列出 `PushPrepromptView`＋`CreateChildView`（行號 136／155）；PR head 不帶 `--repo` 跑 → exit 0、只列 `CreateChildView、EULAConsentView`。與 `.claude/evidence/LS-232/r2/regression-real-snapshots.txt` 逐字核對相符。
- **派工單第 2 點（mutation 重放）**：把 `qa_driver_gate_check.py` 複製到 scratch 後，將 `run()` 的 `unmarked` 累加迴圈換成 `unmarked = {}`，對 `938e854` 快照重跑 → 翻綠 exit 0（斷言訊息原文 `✓ qa-driver-gate-check：宣告的 0 個登入後全屏 gate（無）QADriver 皆已標記處理`），證明該邏輯 load-bearing；本 worktree 未被改動（`git status --porcelain`／`git diff --stat` 皆空、HEAD 仍 `a5773c0`）。
- **派工單第 3 點（CONTAINER_EXCLUDE）**：讀 `scripts/gates/qa_driver_gate_check.py:124-135` 確認判定是線性掃描而非根節點／祖先判定，並用三個合成 fixture（Button 為根／inline VStack+Text+Button／`.sheet { Text }` 後接無關型別）各跑一次 `qa-driver-gate-check.sh --repo <fixture>`，得到 綠／綠／假紅；對照組用 `git show c0ee0b2:scripts/gates/qa_driver_gate_check.py` 跑同一 fixture 比對 R1 行為。現有 gate 用 `grep -n "\.fullScreenCover(\|\.sheet(" LittleSprout/Navigation/RootView*.swift`＋`grep -rn "QA-GATE" LittleSprout/ LittleSproutUITests/` 逐一核對。
- **派工單第 4 點（自測與 CI）**：`bash scripts/gates/qa-driver-gate-check.test.sh` 實跑 → 21 `✓`、exit 0；讀 `scripts/gates/qa-driver-gate-check.test.sh:255-266` 確認 ⑪⑫ 走 `git show <sha>:<path>` 真快照且 fail-closed；`grep -n "qa-driver\|fetch-depth" .github/workflows/ci.yml` 確認接線（`:51`／`:228`／`:263-264`）；`gh run view 34690283176 --json jobs` 反查 `rules` job 與其兩個相關 step 的 `conclusion: success`。
- **派工單第 5 點（scope）**：`git diff c0ee0b2..a5773c0` 全文逐行看完（含 `docs/COLLABORATION.md` 僅第 336 行一列）、`git diff origin/main...a5773c0 --stat` 複看 12 檔；`awk 'NR>=114 && NR<=122' LittleSprout/Navigation/RootView.swift` 核對 i5 的 `117-119`；`gh pr view 365 --json body` 讀 R2 處置表五條。
- **派工單第 6 點（back-merge）**：`git archive origin/development | tar -x` 出快照後 `bash scripts/gates/qa-driver-gate-check.sh --repo <snap>` → 紅（證明必須帶標記）；`git merge-tree --write-tree origin/development a5773c0` 取 tree `7be3a5b…` 後 `git archive` 出來再跑一次 `bash scripts/gates/qa-driver-gate-check.sh --repo <merged>` → 紅只剩 `PushPrepromptView`；把 R1 給的兩處標記行逐字套進該複本 → 綠且列 3 個 gate；再 `grep -v` 拿掉 QADriver 那行 → 紅列 `PushPrepromptView`（`missing` 分支）。
- **不破壞既有 gate**：`bash scripts/gates/agent-tools-check.sh` 實跑 → `✓ agent-tools gate 通過（6 份 agent 定義；正文必含字樣 39 條）`、exit 0，含 `ios-dev.md：正文含「新增登入後全屏 gate 必同 PR 更新 QADriver」`。

## 未完成／跳過（誠實聲明）

1. **CI `ci` job 在我下判定時仍 `in_progress`**（`gh run view 34690283176 --json status` → `in_progress`；`db`／`lint`／`rules`／`ci-ipad` 四個皆 `success`）。R1→R2 零 Swift 變動、R1 對同樣 Swift 內容的 head `c0ee0b2` 已見過 `ci` 五綠，故不等它；合併前仍請 orchestrator 確認 `ci` 綠（分支保護本來就會擋）。
2. 未重跑 `push-gate.test.sh`（96 組，需模擬器）與 `agent-tools-check.test.sh`（120 組）——R2 delta 未觸及這兩支的受測對象；我改跑 `agent-tools-check.sh` 本體確認 39 條規則仍過。
3. 未跑 `xcodebuild test`／未 boot 模擬器／未跑 `supabase db reset`／`run.sh`／未實跑 `qa-e2e.sh`（本 PR 零 Swift 行為變更、不含 migration，票文「不做」明列 qa-e2e 情境）。**未持有 supabase lock**（不需要）。
4. back-merge 只用 `git merge-tree`／`git archive` **算過**，未建立分支、未寫入任何 repo 檔案（reviewer 不改檔）；行號以 tree `7be3a5b…` 為準，development 再前進需重算。
5. 未逐一審閱 `push-gate.test.sh`／`agent-tools-check.test.sh` 的樣本內容（R1 已做定點檢查，R2 未動）。

## handoff-evidence-check 輸出

`bash scripts/gates/handoff-evidence-check.sh <本文暫存檔> --repo /Users/clyeh/little-sprout/.claude/worktrees/LS-232` → **exit 0**，「已驗證」段 7 個列項全綠：

```
✓ 第 149 行起的列項有證據
✓ 第 150 行起的列項有證據
✓ 第 151 行起的列項有證據
✓ 第 152 行起的列項有證據
✓ 第 153 行起的列項有證據
✓ 第 154 行起的列項有證據
✓ 第 155 行起的列項有證據
```

（過程註記：第一次跑時「派工單第 6 點」那條紅，原因是該列項只寫了「快照跑 gate」而沒有寫出命令字面；我把實際跑過的 `bash scripts/gates/qa-driver-gate-check.sh --repo <snap>` 補進敘述——是補證據，不是改寫結論。）

### 2026-09-12T11:33:20.808Z · Chih-Lin Yeh · `43350038`

**orchestrator 09-12 19:33**：merge-review R2 `e84534ed` **APPROVE**（0 major／2 minor N1 文件過期、N2 closure 邊界假紅／4 informational），status 貼 `a5773c0`。但 main 已在 19:14 晉升到 `07d82bd`（v0.22.0，含 LS-217 R5 的 `PushPrepromptView` 與 COLLABORATION:133），PR #365 對 main 轉為 CONFLICTING——原規劃放在 back-merge 的標記與合併句必須改在本分支做 → 派 ios-dev **R3**：merge origin/main、解 COLLABORATION:133（R1 5(b) 句＋R2 規則）、補 `// QA-GATE: PushPrepromptView`／`// QA-GATE-HANDLED: PushPrepromptView` 兩處、順修 N1 文字、N2–N6 處置；合併樹 gate 綠列 3 個 gate 為證。R3 交件後派 merge-review R3 delta → 併 main → back-merge main→development（屆時無內容差異）。

### 2026-09-12T11:45:16.423Z · Chih-Lin Yeh · `bebac81f`

## Ticket

LS-232 R3（main 晉升到 `07d82bd`／v0.22.0 後 PR #365 轉 CONFLICTING；merge origin/main＋補 `PushPrepromptView` 兩處標記＋處置 merge-review R2 `e84534ed` 的 N1–N6）

worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-232`，分支 `hotfix/LS-232-qa-driver-gate-check`，PR https://github.com/CLYEH/little-sprout/pull/365（→ `main`），head **`cd5a9c3057b6a563123c25a2a3bdba517aa34240`**。

## 已完成

1. **合併（commit `71cdcc0`）**：`git fetch origin && git merge origin/main`——三個 Swift 檔（`RootView.swift`／`RootView+AuthenticatedGate.swift`／`QADriver.swift`）auto-merge 乾淨；唯一衝突 `docs/COLLABORATION.md:133`，合併句沿 merge-review R1 verdict `2f276f29` 第 5(b) 原文（補回 `CreateChildView`、拿掉「機械化見下方 harness 票」死指標），結尾補 R2 新規則敘述（備援掃描未標記候選亦計入紅／綠、`// QA-GATE-EXEMPT:` 豁免）。
2. **補兩處標記（commit `cd5a9c3`）**：`LittleSprout/Navigation/RootView.swift:150`（`.fullScreenCover(isPresented: Binding(` 之前）加 `// QA-GATE: PushPrepromptView` 三行；`LittleSproutUITests/QA/QADriver.swift:197`（`func dismissPushPrepromptIfPresent() throws {` 本體第一行）加 `// QA-GATE-HANDLED: PushPrepromptView` 兩行——沿用 merge-review R1 給的逐字文字與落點。
3. **N1（minor，已修，commit `cd5a9c3`）**：`scripts/gates/qa-driver-gate-check.sh` 的 `--help`／檔頭與 `.github/workflows/ci.yml:226-227` 註解訂正為現行行為（候選－宣告－豁免非空即紅，不再宣稱「⚠ 不影響 exit code」）。
4. **N3（informational，已修，commit `cd5a9c3`）**：`qa_driver_gate_check.py` 檔頭「備援掃描」段補上「inline gate 無具名 View 型別會偵測不到」這個具體形狀的敘述。
5. **N2＋N6（minor／informational，記入待辦池，不修）**：已在 Linear LS-96 `save_comment` 一則（comment id `4c7a7d7e`），並在 `qa_driver_gate_check.py` 檔頭補一段誠實描述 20 行視窗不認 closure 邊界的盲區（commit `cd5a9c3`）。
6. **N4（informational，不修＋理由）／N5（informational，純文字訂正）**：見「未完成」段逐條處置。
7. **PR #365 body**：既有段落（R1／R2）原樣保留，新增「R3 處置表」段（合併背景＋標記落點＋N1–N6 逐條處置＋驗證原文）。

## 已驗證（怎麼驗）

- **合併後三個 gate 是否齊全**：`bash scripts/gates/qa-driver-gate-check.sh`（對本 PR head，票文範圍：R3 驗證第 4 點「合併樹上跑 gate 必綠且列 3 個 gate」）→ `✓ qa-driver-gate-check：宣告的 3 個登入後全屏 gate（CreateChildView、EULAConsentView、PushPrepromptView）QADriver 皆已標記處理`，exit 0。
- **負向驗證（mutation：拿掉 QADriver 那行）**——改了什麼一行：複製當前 `RootView.swift`／`QADriver.swift` 到 scratch 複本，從 `QADriver.swift` 複本刪掉 `// QA-GATE-HANDLED: PushPrepromptView` 兩行；哪條測試紅：對該複本跑 `qa-driver-gate-check.sh --repo <複本>`；斷言訊息原文：`✗ qa-driver-gate-check：QADriver 沒有處理下列登入後全屏 gate：` 換行 `    - PushPrepromptView（宣告於 …/RootView.swift:150）——在 QADriver 加落點＋dismiss（比照既有 EULA／推播前置頁處理），並在對應位置加 \`// QA-GATE-HANDLED: PushPrepromptView\` 標記`，exit 1。原文存 `.claude/evidence/LS-232/r3/regression-r3.txt`（含正向與負向兩段）。
- **全量自測（票文範圍：`bash scripts/gates/qa-driver-gate-check.test.sh` 21 組綠）**：實跑 → 21 組全 `✓`，末行 `qa-driver-gate-check.test.sh：全部通過`，exit 0（合併與補標記未影響合成 fixture，因為自測用的是合成樣本、不依賴真 repo 狀態）。
- **不破壞既有 gate**：`bash scripts/gates/agent-tools-check.sh` → `✓ agent-tools gate 通過（6 份 agent 定義；正文必含字樣 39 條）`，exit 0；`swiftlint lint --strict --quiet` → exit 0（純註解＋文件變更，無 Swift 邏輯行）。
- **push gate（`scripts/gates/push-gate.sh`，含全量單元測試）**：`git push origin hotfix/LS-232-qa-driver-gate-check` 輸出含 3c 步驟原文 `✓ qa-driver-gate-check：宣告的 3 個登入後全屏 gate（CreateChildView、EULAConsentView、PushPrepromptView）QADriver 皆已標記處理` 與 `✓ push gate 通過`；push 成功（`a5773c0..cd5a9c3`）。**模擬器 runtime 註記（LS-205）**：push gate 印 `⚠ detect-simulator：既有專屬機「LS-232-iPhone17Pro」目前是 iOS 26.0，與釘住版 iOS 26.2 不同（CI 為 iOS 26.2）`——本輪未新增任何 UI／點擊目標畫面，量測基準差異對本票改動無實質影響。
- **PR body gate**：`bash scripts/gates/pr-body-check.sh <暫存檔> --branch hotfix/LS-232-qa-driver-gate-check --verify` → `✓ 第 40 行「已修」SHA 00ab8a5 在 PR commits 內`、`✓ 第 48 行「已修」SHA a5773c0 在 PR commits 內`、`✓ 第 109 行「已修」SHA cd5a9c3 在 PR commits 內`、`✓ 第 117 行「已修」SHA cd5a9c3 在 PR commits 內`、`✓ 申報反查：LS-96 行 2 條、「已修」行 4 條`，exit 0（本機無 `LINEAR_API_KEY`，LS-96 comment id 僅格式驗證，`⚠ 未反查` 屬預期、不影響 exit code；未接管線、直接看 exit code）。
- **PR 狀態**（對照 `scripts/gates/push-gate.sh` 的「與 origin/main 可乾淨合併」檢查）：`gh pr view 365 --json mergeable,mergeStateStatus` → `{"mergeable":"MERGEABLE","mergeStateStatus":"BLOCKED"}`——`MERGEABLE` 確認與 `origin/main`（`07d82bd`）不再衝突（push gate 輸出同時印 `✓ 與 origin/main（07d82bd）可乾淨合併`），`BLOCKED` 是必要 CI 檢查尚未跑完／通過（分支保護正常行為），非合併衝突。

## 未完成／reviewer informational 處置（merge-review R2 `e84534ed` N1–N6，一條不能省）

- **N1**（minor，`--help`／`ci.yml` 說明文字過期）：**已修**——見「已完成」第 3 項，commit `cd5a9c3`。
- **N2＋N6**（minor／informational，20 行視窗不認 closure 邊界，會假紅或漏抓）：**記入待辦池 LS-96**（comment `4c7a7d7e`）——reviewer 判定 main head／本 PR 合併樹目前無此形狀、非本輪必修；已在 `qa_driver_gate_check.py` 檔頭補上誠實敘述（commit `cd5a9c3`），供之後評估修法（掃到 closure 收合行為止，或只接受 closure 內的行）。
- **N3**（informational，inline gate 無具名 View 偵測不到）：**已修**——見「已完成」第 4 項，commit `cd5a9c3`（僅補檔頭盲區敘述，reviewer 本人判定不必改偵測邏輯：兩起真實事故皆具名 View、專案慣例也是具名）。
- **N4**（informational，`// QA-GATE-EXEMPT:` 無成本靜音鍵、作用域全域）：**不修**——reviewer 判定加「理由必填」等於再造一支 gate，不建議本輪處理，記錄供日後評估。
- **N5**（informational，PR body 兩處引用瑕疵：R2「### m1」段行號 `:52-58` 應為 `:68-73`(R2 時)／現為 `:74-79`；「## 已知盲區」段仍是 R1 舊敘述）：**已於 PR body「R3 處置表」N5 節訂正**——歷史段落（R1／R2 原文）原樣保留不回改，正確敘述以 R3 節與 `qa_driver_gate_check.py` 檔頭現況為準。

## 風險

- 低：`.claude/evidence/LS-232/r2/`、`.claude/evidence/LS-232/r3/` 皆為本機路徑（gitignore `.claude/evidence/`），非 repo 內檔案，僅供本次驗證追溯；handoff／PR body 已逐字貼原文備查。
- 低：本輪三個生產碼檔案改動（`RootView.swift`／`QADriver.swift` 各 +3／+2 行純註解標記；`qa_driver_gate_check.py`／`qa-driver-gate-check.sh`／`ci.yml` 純說明文字訂正）皆無 Swift 邏輯行變更，`swiftlint --strict` 與 push gate 全量單元測試皆通過。
- 低：PR 目前 `mergeStateStatus: BLOCKED`（CI 檢查跑完前的正常狀態），非合併衝突；待 CI 綠後才可 promote／merge，orchestrator 監看。

## 產出位置

- PR：https://github.com/CLYEH/little-sprout/pull/365（head `cd5a9c3057b6a563123c25a2a3bdba517aa34240`，→ `main`）
- Commits（R3 新增兩筆）：`71cdcc0`（merge origin/main，解 COLLABORATION §4-b 衝突）、`cd5a9c3`（補兩處 QA-GATE 標記＋N1／N3 訂正）
- 證據：`.claude/evidence/LS-232/r3/regression-r3.txt`（合併後 3 gate 綠原文＋拿掉 QADriver 標記後紅原文）
- Linear LS-96 池項：comment `4c7a7d7e`（N2＋N6 併案）
- 模擬器已關：`50E3FE6E-99F4-4E8D-B13C-E62B8D848B81`（`LS-232-iPhone17Pro`，push gate 自動 boot／shutdown，本輪未額外手動操作模擬器）

### 2026-09-12T11:45:56.942Z · Chih-Lin Yeh · `52718218`

**orchestrator 09-12 19:41**：R3 交件 head `cd5a9c3`（`71cdcc0` merge main 解 COLLABORATION:133；`cd5a9c3` 兩處 `PushPrepromptView` 標記＋N1／N3；合併樹 gate 綠列 3 gate、mutation 紅；自測 21/21；handoff `bebac81f`；N2＋N6 → 池 `4c7a7d7e`）→ PR 已 MERGEABLE，派 merge-review R3 delta。

### 2026-09-12T11:52:46.490Z · Chih-Lin Yeh · `029fc9d3`

# LS-232 merge-review R3（delta）— APPROVE

（09-12 19:4x 完成；在票 worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-232` 審。**未持有 supabase lock**——純文字掃描 gate，不需要 DB／模擬器。未跑 `xcodebuild test`、未 boot 模擬器、未執行 `xcrun simctl ui`，故無 LS-205 runtime 比對需求。）

- head：`cd5a9c3057b6a563123c25a2a3bdba517aa34240`。開審 `git fetch origin && git status` → `nothing to commit, working tree clean`、`git rev-parse HEAD` = `cd5a9c3…4240`，與派工一致；`gh pr view 365 --json headRefOid` 當下同值（審查期間無人 push）。
- 審的範圍：**只審 delta `a5773c0..cd5a9c3`**（`71cdcc0` merge origin/main `07d82bd`、`cd5a9c3` 兩處標記＋N1／N3 文字）。R2 已 APPROVE 的 gate 邏輯（`qa_driver_gate_check.py` 的判定路徑、21 組自測內容）本輪未再重審，但對新 head 重跑了一次（見下）。
- 判定：**0 blocker／0 major／0 minor／2 informational → APPROVE**

---

## 第 1 點：merge commit `71cdcc0`

**(a) 分支帶進 main 的內容＝原封不動的 LS-232 足跡，沒有夾帶、沒有吃掉 main 的東西。** `git diff 07d82bd..71cdcc0 --stat` → **12 檔 +599／−5**，與合併前 `git diff origin/main...a5773c0` 的足跡（同樣 12 檔 +599／−5）逐檔相同。限 Swift 再看一次逐行（`git diff 07d82bd..71cdcc0 -- 'LittleSprout/**' 'LittleSproutUITests/**'`）：**只有 11 行 `+` 註解、零刪除行**——`EULAConsentView`／`CreateChildView` 四處標記原樣，main 帶進來的 `PushPrepromptView` 相關程式碼一行未動。

**(b) `docs/COLLABORATION.md:133` 合併句：LS-217 R5 那句與 R1 5(b)＋R2 規則三者都在。** `git diff 07d82bd...71cdcc0 -- docs/COLLABORATION.md` 只動這一行（另加 §7 第 336 行新列，屬本 PR 既有內容）。逐項核對 main（`07d82bd`）版本的成分：

| main 原句成分 | 合併後 |
|---|---|
| (a)(b)(c) 排障順序整段 | **逐字保留** |
| `QADriver` 認得的清單：EULA 同意頁（LS-220）、推播權限前置頁（`PushPrepromptView`，LS-217 QA R1 FAIL `e4863482`——`ensureFamily()` 建完家庭進時間軸後自動彈出，`dismissPushPrepromptIfPresent()` 判定並點「稍後再說」） | **逐字保留**，中間補進 `CreateChildView`（R1 5(b) 取捨 1，合併後宣告清單確實是 3 個） |
| **新增登入後全屏 gate（`.fullScreenCover`）的票，DoD 必含 `QADriver` 更新** | 保留，依 R1 5(b) 取捨 2 拿掉「（`.fullScreenCover`）」限定（EULA gate 是條件式整樹替換，留著會誤導） |
| 同型前例 LS-190／「本次推播前置頁」 | 保留，改寫為「LS-217 推播前置頁」 |
| 「——機械化見下方 harness 票」 | 依 R1 5(b) 取捨 3 換成實際敘述（這正是 LS-217 merge-review R5 `b9abb471` m2 點名的死指標） |
| （R2 新規則） | 新增：備援掃描未標記候選也計入紅／綠（候選－宣告－豁免非空即紅）、非 gate 用 `// QA-GATE-EXEMPT: <View>` 豁免 |

**沒有任何 main 側資訊在合併中遺失**，我逐句比對過兩個版本。

## 第 2 點：`cd5a9c3` 的兩處標記與 N1

**標記位置與逐字文字：與我 R2 verdict `e84534ed` 第 6 點給的完全一致。**

- `LittleSprout/Navigation/RootView.swift:150-152`（三行註解），下一行 `:153` 就是 `.fullScreenCover(isPresented: Binding(`（PushPreprompt 那一個）——即「插在合併後第 150 行之前」，8 空格縮排，文字一字不差。
- `LittleSproutUITests/QA/QADriver.swift:197-198`，`:196` 是 `func dismissPushPrepromptIfPresent() throws {`、`:199` 是原本的 `guard pushPrepromptSkipButton.waitForExistence…`——放在函式本體第一行，`:186-195` 的 `///` 文件註解與宣告的綁定未被切斷，文字一字不差。

**我自己在 HEAD 跑 gate（不只讀 handoff）**：`bash scripts/gates/qa-driver-gate-check.sh` → `✓ qa-driver-gate-check：宣告的 3 個登入後全屏 gate（CreateChildView、EULAConsentView、PushPrepromptView）QADriver 皆已標記處理`、exit 0。

**我自己重放兩組 mutation**（都做在 `git archive HEAD` 的 scratch 複本，worktree 全程未改）：

1. 拿掉 `QADriver.swift` 的 `// QA-GATE-HANDLED: PushPrepromptView` 兩行 → `✗ qa-driver-gate-check：QADriver 沒有處理下列登入後全屏 gate：` ＋ `    - PushPrepromptView（宣告於 …/RootView.swift:150）——在 QADriver 加落點＋dismiss…`、**exit 1**。與 handoff 申報（`.claude/evidence/LS-232/r3/regression-r3.txt`）**一字不差**，紅的是預期的斷言、不是 crash。
2. （handoff 沒申報、我加驗的）拿掉 `RootView.swift:150-152` 的宣告標記 → 走 M1 備援掃描路徑：`✗ …偵測到疑似登入後全屏 gate 但未標記（1 個）： - PushPrepromptView（…/RootView.swift:158（`.fullScreenCover`））…`、**exit 1**。證明真實合併後的 `PushPrepromptView` 兩條紅路徑（漏處理標記／漏宣告標記）都活著。

**N1 已真的修好**：`bash scripts/gates/qa-driver-gate-check.sh --help` 實跑輸出現在是「……找到沒有 `// QA-GATE` 標記的疑似 gate 候選——扣掉已宣告與 `// QA-GATE-EXEMPT: <View>` 豁免清單後非空也算紅（LS-232 R2：不再只印 ⚠、不影響 exit code）。exit：0＝全過；1＝有缺漏或未標記候選；2＝參數／環境錯誤。」與行為一致。`ci.yml:226-228` 的 step 註解同步改成「備援掃描未標記候選轉紅（R2）＋QA-GATE-EXEMPT 豁免…＋真實歷史快照回歸」。`grep -rn "不影響 exit code"` 剩下的三處全部是「原本……已改成」的沿革敘述（`qa-driver-gate-check.sh:14`／`:27`、`qa_driver_gate_check.py:29`），不是現行行為宣稱，正確。

## 第 3 點：處置表

- **N2＋N6 → LS-96 池項 `4c7a7d7e`：內容對得上**。我讀了該 comment：(a) 視窗跨過 `}` 把 closure 外第一個具名型別誤判成候選（假紅、訊息指名無關型別、照訊息會豁免錯型別）、(b) 視窗太短會漏抓（M1 後風險等級升級），連我用的合成 fixture（`.sheet { Text(…) } .background(Color.clear) SomeUnrelatedHelper(value: 1)`）與兩個建議修法（掃到 closure 收合行為止／只接受 closure 內的行）都照抄了，並註明「main head／本 PR 合併樹目前無此形狀、未觸發真事故」「估 size S」。與我 R2 的 N2／N6 原意一致，無走樣。`qa_driver_gate_check.py:39-46` 檔頭也補了同一段盲區敘述（我讀過，敘述與程式行為相符，沒有再宣稱程式沒有的能力）。
- **N4 不修理由成立**：理由（加「理由必填」等於再造一支 gate、唯一補位是 merge-review 看 diff）正是我 R2 給的判斷，屬「reviewer 本人判定不必修」的照辦，不是自行降級。
- **N5 訂正只動 R3 段**：我把 R2 當下抓的 PR body 與現在的 body 逐行 `diff`——**R3 段之前的內容逐字未改**（唯一差異是 `🤖 Generated with…` 頁尾被移到 R3 段之後），符合「歷史段落原樣保留、正確敘述以 R3 節為準」的宣告。N5(a) 新引用的 `:74-79` 我核對屬實（`grep -n "CONTAINER_EXCLUDE = {"` → `74`，集合結尾 `}` 在 `:79`）。

## 第 4 點：CI

`gh pr checks 365 --json name,state,bucket`（未用 `gh run watch`）：

```
pass     SUCCESS      lint
pass     SUCCESS      rules
pass     SUCCESS      db
pending  IN_PROGRESS  ci
pending  IN_PROGRESS  ci-ipad
```

`rules` job（run `34691668035`，head `cd5a9c3`）內本票兩個 step 逐一反查 `conclusion`：「QADriver 全屏 gate 對帳（qa-driver-gate-check，LS-232）」＝**success**；「Gate 腳本自測（…／qa-driver-gate-check）」＝**success**。`gh pr view 365` → `mergeable: MERGEABLE`（與 `07d82bd` 不再衝突）、`mergeStateStatus: BLOCKED`（必要檢查未跑完的正常狀態）。

---

## Findings（0 blocker／0 major／0 minor／2 informational）

### i1（informational）`docs/COLLABORATION.md:336`（§7 那列）的「已知盲區」比 `qa_driver_gate_check.py` 檔頭少兩個形狀

R3 把 N3（inline gate 無具名 View）與 N2／N6（20 行視窗不認 closure 邊界）補進了 `qa_driver_gate_check.py:36-46`，但 §7 那列的「已知盲區」仍只寫「巢狀更深、identifier 用變數組出來」。**敘述本身沒有變成假的**（現有句子仍然成立），只是正典表比程式檔頭少兩項；§7 的用途正是當單一正典入口，日後有人只讀 §7 會低估盲區。建議下次動到 §7 時補上（一句話），不值得為它擋這支 PR，也不必另開票。

### i2（informational，nit）PR body R3「N5」節一處簡體字

「兩處历史段落原樣保留」的「历史」是簡體字（全 repo 文件慣例為繁體）。純 PR body 文字，不影響程式與任何 gate。

---

## 四個必審維度（delta）

- **Race condition**：delta 的 Swift 變更是 `git diff 07d82bd..cd5a9c3 -- 'LittleSprout/**' 'LittleSproutUITests/**'` 共 16 行 `+`、**非註解行數 0**、零刪除行——無 `Task`／`actor`／`@MainActor`／`Sendable` 面變動。`RootView.swift:150-152` 的註解插在 `.task(id:)` 與 `.fullScreenCover` 之間，屬 modifier 鏈中的合法註解位置，不改變鏈的求值順序。**無 finding**。
- **運算效能**：無 DB／RLS／查詢／圖片路徑變更；gate 掃描量不變（多一個標記＝多一個 dict 條目）。**無 finding**。
- **平行優化**：不適用（單次檔案掃描）。
- **Scope**：`cd5a9c3` 只動 5 個檔（2 個 Swift 純註解標記＋wrapper／py 檔頭文字＋ci.yml 註解），`71cdcc0` 是純合併、唯一人工解的是 `COLLABORATION.md:133`。**無順手重構、無無關改動、未動票文「不做」的 QADriver 處理邏輯與 `qa-e2e.sh`**。

## 已驗證（怎麼驗）—— 逐條對應派工單編號

- **派工單第 1 點（merge commit）**：`git diff 07d82bd..71cdcc0 --stat` 比對足跡（12 檔 +599／−5，與合併前 `git diff origin/main...a5773c0` 相同）；`git diff 07d82bd..71cdcc0 -- 'LittleSprout/**' 'LittleSproutUITests/**'` 逐行確認只有 11 行註解加法、零刪除；`git diff 07d82bd...71cdcc0 -- docs/COLLABORATION.md` 全文逐句比對 main 原句與合併句的每個成分（見上表）。
- **派工單第 2 點（標記與 N1）**：`grep -n "QA-GATE" LittleSprout/Navigation/RootView.swift LittleSprout/Navigation/RootView+AuthenticatedGate.swift LittleSproutUITests/QA/QADriver.swift` ＋ `awk 'NR>=147 && NR<=156'`／`awk 'NR>=194 && NR<=200'` 核對落點與縮排；`bash scripts/gates/qa-driver-gate-check.sh` 對 HEAD 實跑 → 綠、列 3 個 gate；`git archive HEAD | tar -x` 出複本後做兩組 mutation（刪 QADriver 標記／刪 RootView 標記）各跑一次 `bash scripts/gates/qa-driver-gate-check.sh --repo <複本>` → 皆 exit 1、訊息如上；`bash scripts/gates/qa-driver-gate-check.sh --help` 實跑看輸出；`grep -rn "不影響 exit code" scripts/gates/qa-driver-gate-check.sh scripts/gates/qa_driver_gate_check.py .github/workflows/ci.yml` 確認殘留字樣都是沿革敘述。
- **派工單第 3 點（處置表）**：`mcp__linear__list_comments` 讀 LS-96 comment `4c7a7d7e` 全文與 N2／N6 原文對照；讀 `scripts/gates/qa_driver_gate_check.py:36-46` 的新盲區段落；`gh pr view 365 --json body` 取現行 body 後與 R2 當下存下的 body 做 `diff`（R3 段前逐字相同）；`grep -n "CONTAINER_EXCLUDE = {" scripts/gates/qa_driver_gate_check.py` 核對 N5 引用的 `:74-79`。
- **派工單第 4 點（CI）**：`gh pr checks 365 --json name,state,bucket`（未用 `gh run watch`）；`gh run list --commit cd5a9c3…`＋`gh run view 34691668035 --json jobs` 反查 `rules` job 內兩個相關 step 的 `conclusion: success`；`gh pr view 365 --json mergeable,mergeStateStatus`。
- **自測與既有 gate 不破**：`bash scripts/gates/qa-driver-gate-check.test.sh` 實跑 → exit 0、`grep -c "^✓"` ＝ **21**、末行「全部通過」。

## 未完成／跳過（誠實聲明）

1. **`ci`／`ci-ipad` 兩個 job 在我下判定時仍 `IN_PROGRESS`**（`lint`／`rules`／`db` 已 SUCCESS）。本輪 Swift 變更是 16 行純註解、非註解行數 0，且合併未動 main 的任何程式行，編譯風險極低；分支保護會擋到全綠才准合併，請 orchestrator 併之前確認。
2. 未重跑 `push-gate.test.sh`（96 組，需模擬器）與 `agent-tools-check.test.sh`（120 組）——delta 未觸及其受測對象（R2 已跑過；R3 handoff 也申報 `agent-tools-check.sh` 本體 exit 0）。
3. 未跑 `xcodebuild test`／未 boot 模擬器／未跑 `supabase db reset`／`run.sh`／未實跑 `qa-e2e.sh`（票文「不做」明列 qa-e2e 情境；本 PR 零 Swift 邏輯變更、不含 migration）。**未持有 supabase lock**（不需要）。
4. 未重審 R2 已 APPROVE 的 gate 判定邏輯與 21 組自測的樣本內容（只對新 head 重跑取結果）；R2 的 CONTAINER_EXCLUDE／fail-open 反例分析不再重做。
5. back-merge `main`→`development` 我未執行也未再算一次合併樹——本 PR 併入 main 後，`development` 與 main 在這三個 Swift 檔上應已無內容差異（標記已隨本 PR 就位），但仍請 orchestrator 在 back-merge 後跑一次 `bash scripts/gates/qa-driver-gate-check.sh` 確認綠、列 3 個 gate。

## handoff-evidence-check 輸出

`bash scripts/gates/handoff-evidence-check.sh <本文暫存檔> --repo /Users/clyeh/little-sprout/.claude/worktrees/LS-232` → **exit 0**，「已驗證」段 5 個列項全綠（一次通過，無需補寫）：

```
✓ 第 87 行起的列項有證據
✓ 第 88 行起的列項有證據
✓ 第 89 行起的列項有證據
✓ 第 90 行起的列項有證據
✓ 第 91 行起的列項有證據
```

### 2026-09-12T11:54:31.469Z · Chih-Lin Yeh · `cc1d23a7`

**orchestrator 09-12 19:58**：merge-review R3 delta `029fc9d3` **APPROVE**（0 major／0 minor／2 informational：i1 §7 列盲區敘述少兩形狀、i2 簡體字 nit），status 貼 `cd5a9c3`。併入鏈已啟動（`LS-232-merge-chain.sh`）：先等 LS-229 晉升 main（避免 main 非 FF 擋 test→main）→ #365 CI CLEAN → 併 main → 開 back-merge PR main→development → 併入並貼 dev tip status。i1／i2 處置：i1 併入 back-merge 後由下一張動 §7 的 harness 票順手補（記入本票收尾 comment），i2 純 PR body 文字不修。

### 2026-09-12T12:39:05.677Z · Chih-Lin Yeh · `306d5eae`

**orchestrator 09-12 20:38**：#365 已於 20:15 併 main → main `ca5021e`；back-merge PR **#366** main→development 已建。#366 PR run 34693191593 `ci-ipad` 紅：`testFamilySectionInviteRowRegressionPushesAndBackReturns` 1/7 失敗（「返回後應該回到原本的清單、重新看到入口列本身」；xcresult 由 LS-231 機制上傳、已下載核對），同 SHA 的 main push run 34693184754 `ci-ipad` 綠 → 判 flaky，等 run 完 `gh run rerun --failed` 後併入並貼 dev tip status（鏈 `LS-232-366-rerun-chain.sh`）。flake 首起記入 LS-96 池。

### 2026-09-12T13:11:23.717Z · Chih-Lin Yeh · `96b552d0`

**orchestrator 09-12 21:12**：#366 rerun 後 ci／ci-ipad／db／lint／rules 全綠仍 BLOCKED——development 必要 check 含 `merge-review`，而 #365 的 merge commit `ca5021e`（main tip＝#366 head）沒有 status。依 §2 核對第二親 `cd5a9c3` merge-review R3 `029fc9d3` success 後，貼 `promote: no content diff` 於 `ca5021e`；鏈會接著併 #366 並貼 dev tip status。

### 2026-09-12T13:12:08.268Z · Chih-Lin Yeh · `94624364`

**orchestrator 09-12 21:12**：back-merge #366 已併 → development `35528b0`（dev tip 貼 `promote: no content diff`）；祖先鏈恢復 main ⊂ dev。在 development tip 實跑 `bash scripts/gates/qa-driver-gate-check.sh` → 綠：「宣告的 3 個登入後全屏 gate（CreateChildView、EULAConsentView、PushPrepromptView）QADriver 皆已標記處理」。收尾儀式開始：①dead-code-sweeper 已派 ②lesson review 待 ③cleanup `cleanup-merged.sh --apply LS-232` 執行中。

### 2026-09-12T13:16:12.904Z · Chih-Lin Yeh · `22571443`

**dead-code-sweeper 09-12 21:15**

## dead-code sweep — LS-232

範圍：`git log --oneline 922884e..ca5021e`（8 commit：`ddf1496`／`975618c`／`c0ee0b2`／`00ab8a5`／`2ad8506`／`a5773c0`／`71cdcc0`／`cd5a9c3`）、`git diff --stat 922884e ca5021e`（12 檔，+616/−5）。在主 checkout（唯讀，`main`＝`ca5021e`）逐檔核對，未動任何檔案。

### Findings：未發現本 feature 引入的死碼

逐項核對如下（含判定證據）：

1. **`scripts/gates/qa_driver_gate_check.py`（新檔，223 行）**：全部常數／函式皆有呼叫點——`fail`／`find_files`／`read_lines`／`scan_markers`／`scan_modifier_backup`／`run`／`main` 逐一有呼叫；`GATE_MARKER_RE`／`HANDLED_MARKER_RE`／`EXEMPT_MARKER_RE`／`MODIFIER_RE`／`VIEW_CALL_RE`／`CONTAINER_EXCLUDE`／`BACKUP_SCAN_LOOKAHEAD`／`ROOT_VIEW_GLOB`／`QA_DRIVER_GLOB` 逐一在 `run()`／`scan_modifier_backup()` 內用到。`CONTAINER_EXCLUDE` 的 20 個型別是備援掃描的排除清單（非函式，不適用「未被呼叫」判準）；`Text`／`Button`／`Image` 三個是 R2 新補、其餘既有——這是設計為「涵蓋常見 SwiftUI 基本型別」的安全網集合，經 merge-review R1/R2 兩輪實測驗證有效（`.sheet { Text(…) }` 假紅案例），不是投機的彈性、屬票文與 review 已核可的既定設計，不列為死碼。
2. **`--help` 與檔頭敘述 vs 現行行為**：R1 版本 `--help` 曾宣稱「⚠ 不影響 exit code」但 R2 已把備援掃描候選改為計入紅/綠——此為 merge-review R2 N1（minor）點名的過期文字，**R3 commit `cd5a9c3` 已修**：`bash scripts/gates/qa-driver-gate-check.sh --help` 實跑核對現在正確描述「候選－宣告－豁免非空即紅」。`grep -rn "不影響 exit code"` 剩三處（`qa-driver-gate-check.sh:14/27`、`qa_driver_gate_check.py:29`）皆為「原本……已改成」的沿革敘述，不是現行行為宣稱（merge-review R3 delta 已逐一核對）。**驗證**：我自己重跑 `--help`，輸出與程式行為一致。
3. **自測 `qa-driver-gate-check.test.sh`（21 組）**：我自己實跑 `bash scripts/gates/qa-driver-gate-check.test.sh` → 21 個 `✓`、末行「全部通過」、exit 0。逐行讀過全檔，①～⑫ 無任何組被註解掉或加 `skip`／`SKIP` 旗標，皆是實際執行的斷言（含 mutation ③、備援掃描翻面④、豁免⑨、`Text` 負樣本⑩、真實歷史快照回歸⑪⑫）。
4. **`.github/workflows/ci.yml`**：diff 只新增「Gate 腳本自測」清單續寫一個名字（`／qa-driver-gate-check`）與一個獨立 step，無殘留舊步驟名、無重複 step（`git diff` 逐行看過，+13/−1，那 1 行刪除正是清單續寫本身）。
5. **`scripts/gates/push-gate.sh`**：新增 3c 段落純加法（+13/−0），無殘留。
6. **`scripts/gates/agent-tools-check.sh`／`.test.sh`**：新增 1 條 `BODY_RULES` 釘句與對應 ⑳ 三組正/負/mutant 樣本，`bash scripts/gates/agent-tools-check.sh` 與 `.test.sh` 我皆實跑通過（39 條規則、120 組自測），無殘留。
7. **`docs/COLLABORATION.md` §4-b／§7**：`git grep -n "機械化見下方 harness 票"` → **0 hit**（R1 引入的死指標已在 R3 `71cdcc0` merge main 時解掉，換成實際敘述），未留下死指標。
8. **`QA-GATE` 標記三方對帳**（`git grep`，`.pen`/文件敘述除外，只看程式碼）：
   - 宣告（`RootView*.swift` 的 `// QA-GATE:`）：`CreateChildView`、`EULAConsentView`、`PushPrepromptView` —— 3 個
   - 已處理（`QADriver*.swift` 的 `// QA-GATE-HANDLED:`）：`CreateChildView`、`EULAConsentView`、`PushPrepromptView` —— 3 個，**完全成對，無孤兒標記**
   - 豁免（`// QA-GATE-EXEMPT:`）：真 repo 內 0 筆（只在自測合成 fixture 內出現），不影響現況
9. **`bash scripts/gates/qa-driver-gate-check.sh` 對 HEAD 實跑**：`✓ qa-driver-gate-check：宣告的 3 個登入後全屏 gate（CreateChildView、EULAConsentView、PushPrepromptView）QADriver 皆已標記處理`，exit 0。
10. **三個 Swift 檔（`RootView.swift`／`RootView+AuthenticatedGate.swift`／`QADriver.swift`）**：diff 逐行核對，全部新增行皆為 `//` 註解，零邏輯行、零刪除行、無註解掉的程式碼塊、無 debug print、無測試假資料殘留。
11. **鷹架殘留掃描**：`git diff 922884e ca5021e` 全文 grep `TODO|FIXME|XXX`／「暫留」「待刪」「之後移除」──0 命中（唯一符合 `print(` 樣式的是 gate 程式本身合法的輸出敘述，非 debug 殘留）。

### 既有死碼觀察（非本 feature 引入，僅記錄不列 finding）

- 無。本次 diff 範圍內未發現與本 feature 無關、但順帶碰到的既有死碼。

### Reviewer informational 處置對帳（供交叉核對，非我新開的 finding）

R1/R2/R3 merge-review 累計 blocker 0／major 1（M1，R2 已修並經 R2/R3 reviewer 重放證實）／minor 4（m1 已修、m2 已修、N1 已修、N2 記入 LS-96 池）／informational 8（N3 已修、N4 不修有理由、N5 純文字已訂正、N6 併入 N2 記池、i1/i2 R3 delta 的 2 條 informational：i1 §7 列敘述比 python 檔頭少兩個已知盲區形狀——reviewer 判定「不值得為它擋 PR、下次動 §7 時補」，i2 PR body 簡體字 nit）。這些都已由 orchestrator／reviewer 在票內逐條處置完畢，不是本次死碼掃描的新發現，僅在此交叉確認處置紀錄與程式現況一致（例如確認 §7 表格現況、確認 N2/N6 池項存在）。

### 搜尋方法

- `git log --oneline`／`git diff --stat`／`git diff <path>` 逐檔逐行閱讀（12 檔全覆蓋）
- `git grep -n "QA-GATE"` 全 repo 交叉比對宣告／已處理／豁免三組標記是否成對
- 實跑 `qa-driver-gate-check.sh`（HEAD、`--help`）、`qa-driver-gate-check.test.sh`（21 組）、`agent-tools-check.sh`／`.test.sh`（39 條／120 組）——全綠
- `git grep` 搜尋死指標字樣（「機械化見下方 harness 票」）、TODO/FIXME/debug print 樣式

### 盲區聲明

- 純文字/grep 搜尋抓不到動態組出來的字串引用；本票程式本身（`qa_driver_gate_check.py` 檔頭）已誠實記錄自己的 heuristic 盲區（inline gate 無具名 View、20 行視窗不認 closure 邊界），這些是**程式功能盲區**、已由 reviewer 三輪定案為「已知限制」，不是我掃描死碼的盲區。
- PR #365 body（GitHub 上的敘述文字）不在 git 版控範圍內，本次未逐字重審其歷史段落是否有殘留過期敘述——merge-review R3 delta 已核對過「歷史段落原樣保留、R3 段訂正」，我採信該核對結果，未重做。
- 未執行 `supabase db reset`／`run.sh`（本票無 migration／SQL，比照三輪 merge-review 皆判定不需要 supabase lock）。

### 建議處置

不需要開 fix 票、不需要另外派工——本次掃描確認 LS-232 引入的程式碼在 QA 通過後、Done 前的狀態已乾淨，無死碼殘留。orchestrator 可繼續收尾儀式 ②lesson learning review／③cleanup-merged.sh。

### 2026-09-12T13:16:54.202Z · Chih-Lin Yeh · `246e3db9`

**orchestrator 09-12 21:16 收尾儀式 ②lesson review → Done**
- ①死碼：sweeper `22571443` 無死碼（12 檔 +616/−5 逐行；python 常數／函式皆有呼叫；`QA-GATE` 宣告↔HANDLED 成對無孤兒；自測 21/21；COLLABORATION 死指標 0 hit）。③cleanup：`cleanup-merged.sh --apply LS-232` ✓（worktree／本機分支／模擬器 `LS-232-iPhone17Pro`／DerivedData）。
- **教訓 1（gate 設計）**：R1 標記制只認「已宣告的 gate」→ 未標記的新 gate 仍綠＝fail-open，reviewer 用真快照 `938e854` 抓到；R2 改為「候選 − 宣告 − 豁免 非空即紅」＋真快照迴歸測試。**規則**：任何「清單對帳」型 gate 的自測必含一個「清單漏列」的真實歷史案例，否則等同沒測。
- **教訓 2（流程）**：R2 APPROVE 後 v0.22.0 晉升 main 讓 #365 對 main 衝突（同檔 COLLABORATION:133），白審一輪、改派 R3；hotfix 分支在 release 晉升後要先 merge main 再審——已寫入派工規則記憶（派 review 前先看 `mergeable`）；patrol 對 CONFLICTING PR 直接印解法已入池 `18003df9`。
- **教訓 3（back-merge 鏈）**：#365 merge commit `ca5021e` 沒貼 merge-review status → #366 五 check 綠仍 BLOCKED 20 分鐘；back-merge 鏈模板要在併 main 後立刻依 §2 貼 `promote: no content diff` 於 main tip（LS-231 那次有做、本次鏈漏了）。
- **首次實用**：LS-231 的 xcresult 上傳在 #366 flaky（`SettingsViewIPadTests`）直接讀出 assertion 行，池 `17858ef7`。
- **informational 處置**：R3 i1（§7 列盲區敘述少兩形狀）→ 下一張動 §7 的 harness 票順手補（記入池 `4c7a7d7e` 同項）；i2 簡體字 nit 不修。本票 Done（main `ca5021e`，dev `35528b0`）。

