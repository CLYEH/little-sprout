# LS-186 Harness：gate 精修兩項——`pr-body-check.sh --verify`「已修」行 SHA 候選只認 git object（Linear comment id 形狀不再被當 SHA 反查，LS-185 兩次紅 CI）＋CI `db` job `supabase db reset` 對 54322 port 綁定 flake 加一次重試（LS-184 run 33934840343）——LS-96 `29b413bb` p1／`305a9279`

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-05T03:08:39.809Z |
| 開始 | 2026-09-05T03:08:39.993Z |
| 完成 | 2026-09-05T05:09:33.388Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-186/harnessgate-精修兩項-pr-body-checksh-verify已修行-sha-候選只認-git-objectlinear |
| 關係 | related→LS-143, related→LS-149, related→LS-70, related→LS-180, related→LS-144, related→LS-184, related→LS-96, related→LS-140, related→LS-185, related←LS-264, related←LS-228, related←LS-198, related←LS-187, related←LS-195 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項 `29b413bb` 第 1 條（[LS-185](https://linear.app/little-sprout-app/issue/LS-185/harnessoverflow-scanjs-第六支-board-clip後代-aabb-超出有-clip-的-root-frame) merge-review R2 `b68fa0bd` process-1，2026-09-05）：`scripts/gates/pr-body-check.sh:99-166` 對含「已修」的行取第一個 ≥7 hex 當 SHA 候選——[LS-185](https://linear.app/little-sprout-app/issue/LS-185/harnessoverflow-scanjs-第六支-board-clip後代-aabb-超出有-clip-的-root-frame) PR body 標題行「## R2（merge-review R1 `d87d4334` … 皆已修）」的 Linear comment id 被當 SHA 反查，CI `rules` 連紅兩次、close/reopen 兩回才過；實作者本機因 `| tail -1` 吃掉 exit code 且未帶 `--branch --verify` 沒發現。[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `305a9279`（[LS-184](https://linear.app/little-sprout-app/issue/LS-184/harnesslock-持有安全補強-supabase-locksh-hold-在主-checkoutgit-dirgit-common) CI 查因 `b8b4e7f1`）：`.github/workflows/ci.yml:524-526` `db` job 裸跑 `supabase db reset`，runner docker 對 54322 舊綁定未釋放 → `address already in use`，rerun 即綠（純 flake）。巡檢 11:07 lane:harness 空 1 輪（[LS-185](https://linear.app/little-sprout-app/issue/LS-185/harnessoverflow-scanjs-第六支-board-clip後代-aabb-超出有-clip-的-root-frame) 進 QA）→ 依 [LS-144](https://linear.app/little-sprout-app/issue/LS-144/harness巡檢加開票責任機械提醒-lane-在飛-0-且無可派候補時動作清單印-開票並列來源story-拆票ls-96-p1-升票) 升票，兩項同為 gate／CI 精修合成一支 S。

## 範圍

1. `pr-body-check.sh --verify`：「已修」行的 SHA 候選只接受「`已修` 後緊鄰（允許空白、反引號、括號前）的 7–40 hex」，且以 `git cat-file -e` 驗為 commit object 才視為申報；行內其他 hex（comment id、issue uuid 片段）不再成為候選；找不到合格候選 → 紅並印「已修行需附 commit SHA 於『已修』之後」。**紅時最後一行印明顯** `✗` **並 exit 1**（避免 `| tail` 誤讀——同時在檔頭 usage 寫「勿接管線，看 exit code」）。自測 `pr-body-check.test.sh` 補：標題行含 comment id＋「已修」不誤判、`已修 \`<sha>`（comment `<id>`）` 綠、`已修（<id>）` 無 SHA 紅、既有樣本不回歸。
2. CI `db` job：`supabase db reset` 前先 `supabase stop --no-backup || true`，reset 失敗且錯誤含 `address already in use` 時 `sleep 10` 重試一次（最多一次；其他錯誤不重試）；step 名稱保留；`ci.yml:195`／`:394` 註解「四支」順修為「六支」（[LS-185](https://linear.app/little-sprout-app/issue/LS-185/harnessoverflow-scanjs-第六支-board-clip後代-aabb-超出有-clip-的-root-frame) R1 i2，同檔一字）。無法在本機跑 CI，以 `bash -n`＋`actionlint`（若有）＋一段 shell 抽出成 `scripts/ci/db-reset-retry.sh`＋自測（stub `supabase` 二進位：第一次印 address in use 失敗、第二次成功 → 綠；其他錯誤 → 紅不重試）。
3. COLLABORATION §6 PR body 段補一句「驗 body 用 CI 完整旗標 `--branch <br> --verify`、看 exit code」；派工模板同步（§4-b）；`agent-tools-check.sh` BODY_RULES 若能機械驗就加。
4. [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `29b413bb` 第 1 條／`305a9279`／`978cdcdb` 第 2 條（ci.yml 註解）銷除註記。

## 不做

pr-body-check 反查 Linear comment id 的 API 行為（[LS-140](https://linear.app/little-sprout-app/issue/LS-140/harnesshandoff-申報可驗證-gate-記入-ls-96必附-comment-id已修必附-sha行號pr-body-check) 既有）；CI 其他 job 的 flake。

## 驗收

- [ ] 用 [LS-185](https://linear.app/little-sprout-app/issue/LS-185/harnessoverflow-scanjs-第六支-board-clip後代-aabb-超出有-clip-的-root-frame) 當時的 body（含標題行 comment id）跑 `--branch --verify` → 綠（不誤判）；「已修」無 SHA → 紅＋`✗`（自測）
- [ ] `db-reset-retry.sh` stub 自測：port 綁定失敗一次後成功 → exit 0；其他錯誤 → exit 非 0 不重試
- [ ] merge-reviewer 通過；hotfix 流程（自 main、PR 回 main、back-merge）

## 留言（9）

### 2026-09-05T03:09:02.386Z · Chih-Lin Yeh · `48dfbf2f`

**2026-09-05 11:09 orchestrator — 派工**

- lane:harness 空 1 輪（LS-185 進 QA）→ 依 LS-144／§5-b 升兩則 gate／CI 池項合成一支 S；cycle 1；hotfix 流程：worktree `.claude/worktrees/LS-186`，分支 `hotfix/LS-186-gate-refinements`（base origin/main `6b2d52a`），PR → main，併入後 back-merge。
- 派 general-purpose R1。

### 2026-09-05T03:37:26.501Z · Chih-Lin Yeh · `75c45cb0`

# LS-186 Handoff（general-purpose，2026-09-05 11:34 CST）

## Ticket
LS-186 — Harness：gate 精修兩項（`pr-body-check.sh --verify`「已修」SHA 候選只認緊鄰 commit object＋紅時末行 ✗；CI `db` job 54322 port 綁定 flake 抽 `scripts/ci/db-reset-retry.sh` 重試一次；COLLABORATION／模板一句；池項銷除）。來源 LS-96 `29b413bb` 第 1 條／`305a9279`／`978cdcdb` 第 2 條。hotfix 自 `main` `6b2d52a` 切，PR #300 → `main`（併入後補 back-merge `main`→`development`）。全程只在 worktree `.claude/worktrees/LS-186`，主 checkout 未碰。

## 已完成
1. `0b57d69` `scripts/gates/pr-body-check.sh`：`fix_shas` :122-125（「已修」後只隔空白／反引號／`*`／半形全形左括號的第一個獨立英數 token，須 7–40 小寫 hex；LC_ALL=C 位元組交替、不進中括號）、迴圈改用 :145、格式紅訊息 :147、`bad_b` 指引句改「已修行需附 commit SHA 於『已修』之後」＋comment id 放括號範例、`red_exit` :88-92（四條 exit 1 出口共用，stdout 末行 `✗ pr-body-check：未通過…`）、`--verify` 分兩種紅 :172-197（候選都不是 commit object → 「不是本 repo 的 commit」＋票文句；是 commit 非祖先 → 「不在 PR commits 內」）、檔頭 usage 勿接管線 :13-15、規則 (b) 說明 :27-30。`pr-body-check.test.sh` 66→84：⑦ :219-262（LS-185 回歸格式＋`absent` 不列 comment id、修正後寫法綠、全形括號綠、冒號紅、別欄紅、`x<sha>y` 紅、雙「已修」、三條出口 stdout 末行 ✗＋綠時 ✓、`--verify` 四案＋不誤報「不在 PR commits 內」）；既有樣本兩處調整 :115（表格列改粗體緊鄰）、:190（⑥a 期望字串改「不是本 repo 的 commit」）。
2. `4b4212c` `scripts/ci/db-reset-retry.sh`（新：:21 `PORT_RACE`、:24 `run_reset` tee＋PIPESTATUS、:32 首跑、:41-49 重試路徑 stop→sleep 10→db start→reset）＋`db-reset-retry.test.sh` 13 組（假 `supabase`／`sleep` PATH 前置）；`.github/workflows/ci.yml`：db job step :531 `run: bash scripts/ci/db-reset-retry.sh`（名稱不變，註解 :526-528）、rules job 自測行 :200＋step 名單 :164、註解「四支」→「六支」:195-196／:396-397。
3. `45db12f` `scripts/gates/agent-tools-check.sh` :69-71 註解＋BODY_RULES :75 `ios-dev|pr-body-check.sh <f> --branch <分支> --verify`（13→14 條）；`.claude/agents/ios-dev.md:13` PR body 句改 CI 完整旗標＋「直接看 exit code、勿接 `| tail`」；`agent-tools-check.test.sh` :55-59 fixture、⑪ :137-140、mutant 負控 :181-189（60→65 組）。
4. `7a1bdce` `docs/COLLABORATION.md` :85（§3 PR message 段）、:91（派工五條之 2）、:135（§4-b 派工句「派任何會開 PR 的 agent，prompt 必帶一句…」）、:240／:290（§7 兩列：緊鄰規則、兩種紅、末行 ✗、自測 84）。
5. LS-96 銷除註記：`29b413bb` 第 1 條 ↳ `e333f358`；`305a9279` ↳ `059dd023`；`978cdcdb` 第 2 條 ↳ `c3f9fc2f`（皆附 PR #300＋commit SHA）。
6. PR #300 `hotfix/LS-186-gate-refinements` → `main`（head `7a1bdce`）：body `LS-186-pr-body.md` 以 `bash scripts/gates/pr-body-check.sh <body> --branch hotfix/LS-186-gate-refinements --verify` 直接看 exit code＝0 後才 `gh pr create`；body 在最後一次 push 之後就位、PR 開後無再 push。

## 已驗證（怎麼驗）
- `bash scripts/gates/pr-body-check.test.sh` **84 組全綠 × 4 環境**（bash 5.3／`/bin/bash` 3.2.57／`LANG=C LC_ALL=C`／UTF-8）。**LS-185 實際 body 回放**（CI 同旗標 `--branch hotfix/LS-185-overflow-scan-board-clip --verify`）：紅版本（scratchpad `LS-185-live-body.md`）→ exit 1、第 24 行「沒有 commit SHA 緊鄰…」、**不列 `d87d4334`**、stdout `| tail -1` 印 `✗ pr-body-check：未通過…`；PR #298 現況 body → exit 0「第 24 行 SHA 73fa7e1 在 PR commits 內」。**Mutation**：`fix_shas` 退回 `hex_tokens 7 40`（行內任一 hex）→ ⑦ 五案紅（78/84：LS-185 回歸格式案、冒號、別欄、末行 ✗、真 SHA 在後面不緊鄰）。
- `bash scripts/ci/db-reset-retry.test.sh` **13 組全綠**（bash 5／3.2）；mutation：任何錯誤都重試 → ③ 三條紅；永不重試 → ②④⑤⑥⑦ 八條紅。自測 ⑥ 抓到我第一版 `if ! cmd; then rc=$?` 把原 exit code 丟成 0 的 bug（已改 :44）。`ci.yml` 以 pyyaml 載入並核對 db step `run`／rules 自測行／step 名單；`actionlint` 本機未安裝、未跑。
- `bash scripts/gates/agent-tools-check.sh`（真 repo）通過、印「正文必含字樣 14 條」；`agent-tools-check.test.sh` **65 組全綠**（bash 5／3.2）。
- 四次 commit／push 皆過 commit／push gate（純 harness）。**未跑**：真 CI（push 後不等，交 orchestrator）、真容器（票文＋H3／H3b 禁）、merge-review（尚未派）。未碰 Pen／模擬器／容器／主 checkout；lock 未持有。

## 未完成／判斷（本票尚無 reviewer informational；以下是我的判斷，每條附處置）
- **不照票文字面＋理由（請 reviewer 覆核）**：`db-reset-retry.sh` **不**在第一次 reset 前無條件 `supabase stop --no-backup`——`db reset`（local）先 `AssertSupabaseDbIsRunning`，前一步 `db start` 剛起容器，無條件 stop 會讓每次 reset 紅在「supabase start is not running」（非 port 錯誤、不會走重試），等於打死 db job；stop 只放重試路徑清殘留並補 `db start`。sleep 10／只重試一次／其他錯誤不重試照票文。依據是 Supabase CLI 原始碼記憶、未實跑容器——若 reviewer 能證偽，改回只需搬一行（腳本檔頭與 PR body 皆已標明）。
- **驗收第一項字面衝突（請 orchestrator 裁定）**：「用 LS-185 當時的 body 跑 `--branch --verify` → 綠（不誤判）」——該 body 第 24 行只有「皆已修」、沒有任何 SHA，依範圍 1「找不到合格候選 → 紅」它必須紅；我取「紅在正確原因（缺 SHA）、不列 comment id 為候選」為「不誤判」，修正後 body（PR #298 現況）綠。若意在讓該行綠＝放行無 SHA 的申報，與 LS-140 相悖。
- **既有樣本兩處調整**（票文「既有樣本不回歸」）：⑤ 表格列樣本的舊形狀（SHA 在別欄 `| … | \`sha\` |`）正是新規則要擋的，改成粗體緊鄰、舊形狀移作 ⑦ 紅案；⑥a「SHA 不存在」仍紅、只換期望字串（訊息分家）。其餘 64 組原樣全綠。
- **不修＋理由**：`CLAUDE.md:26` 仍是短形式 `pr-body-check.sh <body>`——票文未列 CLAUDE.md、LS-185 動 CLAUDE.md 曾被要求說明，本 PR 不動；建議 orchestrator 順手補一行（同 §3 五條之 2 的寫法）。
- **不做**：格式模式（不帶 `--verify`）對 `已修（<comment id>）` 仍綠——8 hex 也是合法短 SHA、無 git 分不出，紅只在 `--verify`（票文設計如此，CI 帶 `--verify`）；Linear comment id 反查行為（LS-140 既有）、CI 其他 job 的 flake 依票文不做。
- **殘餘盲區**（已寫進 §7 :290）：同行多個「已修」一真一假不會紅；「已修：`sha`」冒號寫法 LS-186 起紅、訊息給正典範例。
- 池項未動：`29b413bb` 第 2、3 條、`978cdcdb` 第 1 條不在本票範圍，仍留池（銷除註記內已註明）。

## 風險
- 收緊對所有 PR body 即日生效（CI rules 在 PR 事件重讀 body）：in-flight PR 若 body 寫「已修：`sha`」或 SHA 在別欄，下一次 push／reopen 會紅——失敗訊息直接給正典寫法「已修 `<sha>`（comment `<id>`）」。
- `db-reset-retry` 重試路徑 `stop --no-backup` 清 volume，只適用 CI fresh runner；本機絕不能裸跑（H3／H3b 也擋）。真 runner 行為待 PR #300 CI（本 PR 自己的 db job 就會走這支腳本的首跑路徑）。
- hotfix 流程：併入 `main` 後補 back-merge `main`→`development`；ios-dev.md 與 COLLABORATION 改動 pull 後即生效，無 hook 變更。

## 產出位置
- PR #300 https://github.com/CLYEH/little-sprout/pull/300（head `7a1bdce4d2036ee673cba699cb2309b4eed8ff0f`；commits `0b57d69`／`4b4212c`／`45db12f`／`7a1bdce`，皆已 push）
- worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-186`（clean，與 origin 同步）
- scratchpad：`LS-186-pr-body.md`／`LS-186-handoff.md`／`LS-186-prbody-suite{,-bash32,-clocale,-utf8}.log`／`LS-186-prbody-mutant.log`／`LS-186-ls185-red.log`／`LS-186-ls185-fixed.log`／`LS-186-pr298-fixed-body.md`／`LS-186-dbretry-suite{,-bash32}.log`／`LS-186-dbretry-mutant-{anyerr,never}.log`／`LS-186-agent-tools-{gate,suite,suite-bash32}.log`／`LS-186-mut*/`（mutation 副本，可刪）
- LS-96 回覆：`e333f358`／`059dd023`／`c3f9fc2f`
- lock：未持有；Pen／模擬器／容器未碰。

### 2026-09-05T03:39:23.143Z · Chih-Lin Yeh · `ce5c3bd9`

**2026-09-05 11:4x orchestrator — R1 handoff `75c45cb0` 收到（PR #300 head `7a1bdce`）→ In Review，派 merge-reviewer R1**

裁定三件：(1) `db-reset-retry.sh` 不無條件先 `stop`——只在 `address already in use` 時 stop→sleep→`db start`→重試一次：**接受**（票文寫法會讓每次 reset 紅在 not running）；(2) 驗收第一項以「紅在正確原因、不再列 comment id 為候選」解讀：**接受**（放行無 SHA 申報與 LS-140 相悖）；(3) 既有兩處樣本調整接受。`CLAUDE.md:26` 短形式一行同步：允許在本 PR 補（與 LS-180／185 先例一致），由 reviewer 核。

### 2026-09-05T03:50:18.911Z · Chih-Lin Yeh · `4bfab9b7`

## merge-review R1 — **REQUEST_CHANGES（ITERATE）**（PR #300，head `7a1bdce4d2036ee673cba699cb2309b4eed8ff0f`，base `6b2d52a`；2026-09-05 11:48）

範圍：9 檔 +295/−37。CI 四項全 pass（run 33942174881）。**1 major＋2 minor＋1 informational**；major 的修法約 3 行＋1 組自測，其餘部分我逐項驗過都乾淨，R2 應該很快。

### major-1 · `scripts/ci/db-reset-retry.sh` 本機執行毫無機械防呆，且檔頭宣稱的 hook 覆蓋**實測不成立**

檔頭 `:19-21` 寫「**本機不要裸跑這支**……PreToolUse H3／H3b 也會擋」。我用本 PR 的 hook 實測三條命令（payload `cwd`＝LS-186 worktree、未持 lock）：

| 命令 | 實測 |
|---|---|
| `bash scripts/ci/db-reset-retry.sh` | **ALLOW** |
| `bash <絕對路徑>/scripts/ci/db-reset-retry.sh` | **ALLOW** |
| `bash scripts/ops/supabase-lock.sh -- bash scripts/ci/db-reset-retry.sh` | ALLOW（本來就該過） |

原因是機械的：H3／H3b 只看**命令列文字**，看不到腳本內部的 `supabase db reset`／`supabase stop --no-backup`。`supabase/tests/run.sh` 之所以擋得住，是因為它 (a) 自己 `--held` 後 re-exec 進 lock、且 (b) 檔名被 `RUNSH_RE` 認得——這支兩者都沒有。

- **具體失敗情境**：QA／ios-dev／下一輪 reviewer 想「驗一下 CI 的重試邏輯」而在 worktree 跑這支（腳本名字本身就在邀請這件事；本輪協調者也是靠 prompt 文字叮嚀我「本機絕不裸跑」，那正是沒有 gate 的證據）：第一次 `supabase db reset` 就已經是 LS-70 的裸跑事故形狀；若真撞上 port 競態，接著 `supabase stop --no-backup` 會把**共用本機容器的 volume 連同別人 hold 中的 session 資料一起清掉**——正是 LS-149／LS-143 的事故類別，也正是 LS-184（昨天）把 `supabase stop`／`start` 納入 H3b 的理由。本 PR 等於在那道剛補好的門旁邊開了一條沒鎖的側門，而且在檔頭寫「這裡有鎖」。
- **與 repo 最高原則的關係**：CLAUDE.md「新增重要規則＝同時新增它的 gate，否則視為未完成」。本 PR 新增了規則（本機不得跑）卻只有前饋、沒有反饋，且反饋宣稱錯誤。
- **建議修法（三選一，最小者約 3 行）**：
  1. 腳本開頭加 CI-only 守門：`[ "${GITHUB_ACTIONS:-}" = true ] || [ "${CI:-}" = true ] || [ "${LS_DB_RESET_RETRY_ALLOW_LOCAL:-}" = 1 ] || { echo "✗ db-reset-retry：只給 CI runner（重試路徑會 supabase stop --no-backup 清 volume）；本機請 bash scripts/ops/supabase-lock.sh -- supabase db reset" >&2; exit 2; }`——CI 兩個變數都有、不影響 `ci.yml:531`；**自測 `db-reset-retry.test.sh:60` 是直接 `bash "$script"`，記得在 `run()` 或檔頭 export 覆寫變數，否則 13 組會全紅**；再補一組「無 CI 變數 → exit 2 且完全沒呼叫 supabase」的樣本。
  2. 或把檔名加進 `pretool_engine.py` 的 H3 家族（同 `RUNSH_RE` 的做法），讓裸跑被 hook 擋。
  3. 或兩者都做（我建議 1＋訂正檔頭那句，2 可入池）。
- **無論採哪個，檔頭 `:19-21` 那句「H3／H3b 也會擋」都必須訂正**——留著會讓下一個 reviewer／QA 以為有保護。

### minor-1 · 「已修」緊鄰規則比修好 LS-185 所需更嚴：repo 歷史上 **28%** 的申報寫法會被擋（今日 in-flight 0 條）

我把最近 **60 個 PR** 的 body 全部取下來逐行比對新舊規則（`gh pr list --limit 60`，#241–#300）：**154 條「已修」行中 43 條在新規則下沒有任何候選**，其中絕大多數同一行就有**真的 commit SHA**。再用真腳本對合成 body 實跑確認形狀（`--branch … --verify`）：

| 形狀 | 實測 | 例（真實 PR） |
|---|---|---|
| `已修 \`sha\`` | ✅ 綠 | 正典 |
| `已修（\`sha\`）` / `**已修** \`sha\`` | ✅ 綠 | |
| **`**已修**（commit \`7bed50a\`）`** | ❌ **紅** | PR #286 |
| `已修，…。commit \`d2df2ef\`。` | ❌ 紅 | PR #270（4 條） |
| `已修：\`sha\`` | ❌ 紅 | PR #256（多條） |
| 表格別欄 `| 已修：… | \`sha\` |` | ❌ 紅 | PR #251／#254／#256／#261（20+ 條） |

最刺眼的是第三列：票文的用意是「允許括號」，規則也確實把 `（` 列為分隔符，但**只要括號後先寫了 `commit` 這個字就破功**——而那正是本 repo 最常見的寫法。
- **不是 blocker 的理由**：目前唯一的 OPEN PR 是 #300 自己且它綠；歷史 PR 不會被重驗；紅時訊息有給正典寫法。
- **更便宜且同樣修好 LS-185 的替代**：把候選改成「**已修 之後（同一行任意位置）的第一個 7–40 hex token**」——LS-185 那行的 `d87d4334` 在「皆已修」**之前**，仍然紅在正確原因；上表 3–6 列則全部恢復為綠。或退一步：分隔符再允許 `commit`／`：`／`，` 這幾個字面。
- 我理解「緊鄰」是票文明寫、orchestrator 已裁定，所以列 minor 交裁定：**放寬**（改一行 regex＋改 ⑦ 的三個紅案為綠）或**維持**（則建議把「正典寫法」同時寫進 §3 派工模板，讓 agent 不必踩一次才學到）。

### minor-2 · `CLAUDE.md:26` 仍是短形式，和本票新規約互相矛盾

`ios-dev.md:13`／`COLLABORATION.md:85`／`:91`／`:135` 全改成「`--branch <分支> --verify`、看 exit code、勿接 `| tail`」，但 `CLAUDE.md:26` 還是 `bash scripts/gates/pr-body-check.sh <body>`——**照 CLAUDE.md 做就會複製 LS-185 那兩次失敗**（不帶 `--verify` 只驗格式）。CLAUDE.md 是每個 session 必讀、且 `agent-tools-check` BODY_RULES 只釘 `ios-dev.md`，這行沒有任何機械保護。orchestrator 在 `ce5c3bd9` 已預先允許本 PR 補這一行（LS-180／185 先例），建議補上（一行，與 §3 五條之 2 同語）。

### informational-1 · 兩支存活突變是**等價突變**、不是漏測

我跑了 9 支 mutation，7 支 killed，2 支存活但經分析為等價，特此說明以免下一輪誤判為測試缺口：
- `run_reset` 的 `return "${PIPESTATUS[0]}"` → `return $?`：腳本有 `set -uo pipefail`，`$?` 在管線後等於最右邊的非零碼＝reset 的碼；只有「reset 成功但 tee 失敗」才有差。現寫法（明寫 PIPESTATUS）更穩，保留即可。
- `supabase stop --no-backup || true` → 拿掉 `|| true`：腳本**沒有** `set -e`，失敗不會中止，`|| true` 是宣告意圖用的。⑤「stop 失敗不應影響」照樣綠。

### 已驗證乾淨的部分（獨立於 handoff）

- **自測（`/bin/bash` 3.2.57）**：`pr-body-check.test.sh` **84 組全綠**、`db-reset-retry.test.sh` **13 組全綠**、`agent-tools-check.test.sh` **65 組全綠**、真 repo `agent-tools-check.sh` 印「正文必含字樣 **14 條**」。
- **我自己的 mutation（複製樹，基線先驗綠、每支跑完還原並重驗基線）**：`fix_shas` 退回行內任一 hex（協調者指定）→ **⑦ 恰 5 案紅**（LS-185 回歸、冒號、別欄、末行 ✗、真 SHA 不緊鄰）；兩種紅訊息合併 → 4 條紅；`red_exit` 末行改印 stderr → 3 條紅（三個出口都被釘）；放寬冒號為分隔符（**grep＋sed 兩處都改**）→ ⑦ 冒號案紅；`db-reset-retry` 任何錯誤都重試 → ③ 4 條紅；永不重試 → 9 條紅；重試路徑拿掉 `db start` → 7 條紅。（第一輪我只改 grep 沒改 sed，得到假存活；並且第一批的 D5 因 assert 中斷把 D4 突變留在檔上，導致下一支變成雙重突變——兩次都靠「還原後重驗基線」抓到並重跑，此處列出的是重跑後的乾淨結果。）
- **`red_exit` 行為**：四個 exit 1 出口（`:96`／`:108`／`:163`／`:279`）全部走 `red_exit`；`2>/dev/null | tail -1` 實測印 `✗ pr-body-check：未通過（exit 1）…`、exit 1 ✓；LS-185 紅 body 形狀重放**不再列 `d87d4334`**、訊息改為「沒有 commit SHA 緊鄰在『已修』之後」✓；「非 commit object」與「非祖先」兩種紅分家 ✓。
- **`db-reset-retry.sh` 邏輯**：首跑 → 只在輸出含 `address already in use` 時 stop→sleep 10→`db start`→重試一次；其他錯誤原樣印、以原 exit code 結束、不重試；`run_reset` 用 `PIPESTATUS[0]` 取 reset 自己的碼（handoff 說的 `if ! cmd; then rc=$?` 陷阱確實已避開，`:44` 寫法正確）。**不無條件先 stop 的判斷我覆核同意**：`db reset` 前置是 db 容器在（前一步 `db start` 剛起），無條件 stop 會讓每次 reset 紅在 not running 且不走重試——與 orchestrator 裁定 (1) 一致。
- **`ci.yml`**：`:531` `run: bash scripts/ci/db-reset-retry.sh`（step 名稱不變）、`:200` 掛自測、`:164` step 名單、`:195-196`／`:396-397` 註解四支→六支；`python3 -c "import yaml…"` 載入通過（jobs: rules/db/lint/ci）。`actionlint` 我也沒裝、同樣未跑（與 handoff 一致）。
- **文件**：`ios-dev.md:13`、COLLABORATION `:85`／`:91`／`:135`／`:240`／`:290` 逐行看過，語意一致、§7 兩列有記「緊鄰規則／兩種紅／末行 ✗／自測 84」與殘餘盲區（同行多個「已修」一真一假不會紅）。
- **LS-96 銷除**：`e333f358`（parent `29b413bb`）／`059dd023`（`305a9279`）／`c3f9fc2f`（`978cdcdb`）都在、掛對母 comment、帶 PR #300＋commit SHA，且誠實註明未動的池項（`29b413bb` 第 2、3 條、`978cdcdb` 第 1 條仍留池）。
- **逐 commit 歸屬**：`0b57d69` gate＋自測／`4b4212c` CI＋新腳本＋自測／`45db12f` agent-tools＋ios-dev／`7a1bdce` 純 COLLABORATION —— **無夾帶**，worktree 乾淨；handoff 的 8 個行號錨點抽驗全中。
- **PR body**：`--branch hotfix/LS-186-gate-refinements --verify` 直接看 exit code → **0**（`✓ 第 27 行「已修」SHA 0b57d69 在 PR commits 內`）。

### 沒看／沒跑的（誠實揭露）

- **完全沒有碰 Pen／模擬器／容器**，`db-reset-retry.sh` 一次都沒有真的執行過（只跑自測的假 `supabase`／`sleep`）——**重試路徑在真 runner 上的行為未經驗證**，只驗了呼叫序列與 exit code；本 PR 自己的 db job 只走首跑成功路徑。
- `actionlint` 未安裝、未跑（只用 pyyaml 驗語法）。
- 我對「28%」的統計只涵蓋 PR body（不含 handoff comment、commit message）。
- lock：**未持有**（全程離線腳本）。

**verdict：REQUEST_CHANGES**（major-1）。修好 major-1（＋建議一併處理 minor-2、並就 minor-1 給裁定）後我重審 R2；程式其餘部分我已驗過，R2 只需看增量。

### 2026-09-05T04:17:13.320Z · Chih-Lin Yeh · `7b2d3627`

# LS-186 R2 Handoff（general-purpose，2026-09-05 12:15 CST）— merge-review R1 `4bfab9b7` REQUEST_CHANGES 三件全修

## Ticket
LS-186 — Harness：gate 精修兩項（`pr-body-check.sh --verify`「已修」SHA 候選只認 commit object＋紅時末行 ✗；CI `db` job 54322 port 綁定 flake 抽 `scripts/ci/db-reset-retry.sh` 重試一次；COLLABORATION／模板一句；池項銷除）。PR #300 `hotfix/LS-186-gate-refinements` → `main`，**新 head `b199cf8`**（R1 `7a1bdce` ＋ R2 三個 commit）。全程只在 worktree `.claude/worktrees/LS-186`，主 checkout 未碰。

## 已完成（R2 增量；R1 見 `75c45cb0`）
1. **major-1 → `360ef5f`** `scripts/ci/db-reset-retry.sh`：CI-only 守門 :25-29（`GITHUB_ACTIONS=true`／`CI=true`／`LS_DB_RESET_RETRY_ALLOW_LOCAL=1` 缺一即 exit 2，在 `mktemp`／任何 `supabase` 呼叫之前；訊息指向 `bash scripts/ops/supabase-lock.sh -- supabase db reset`）；檔頭 :17-21 訂正——明寫「PreToolUse H3／H3b 只看命令列文字、看不到腳本內部呼叫（reviewer 實測裸跑 ALLOW）」、exit 碼表加 2＝守門。`db-reset-retry.test.sh`：`run()` :62 帶 `LS_DB_RESET_RETRY_ALLOW_LOCAL=1 GITHUB_ACTIONS= CI=`（本機與 CI 跑出同一結果）、⓪ :66-75 四案（無變數 → exit 2 且 calls.log 為空、`=0` 不算、`GITHUB_ACTIONS=true` 放行、`CI=true` 放行）；13→17 組。`ci.yml:531` 不必改（Actions 兩個變數都會設）。
2. **minor-1 → `1e76a80`**（orchestrator 裁定採 reviewer 替代方案）`scripts/gates/pr-body-check.sh`：`fix_shas` :125-129 改為「同一行第一個『已修』之後、第一個 7–40 位小寫 hex 獨立 token」（`${1#*已修}` → `hex_tokens` → `cut -f1`；「已修」之前的 hex 與其後第二個起都不算）；檔頭 (b) :27-32、格式紅訊息、`bad_b` 指引句（正典寫法加 `**已修**（commit \`sha\`）`、comment id 寫在 SHA 之後）、`--verify` 段 :176 註解與訊息（「之後的第一個 hex token 不是本 repo 的 commit…SHA 要寫在 comment id 之前」）同步。`pr-body-check.test.sh` 84→88：⑤ 表格列樣本 :117 恢復 R1 前原形（別欄 SHA 綠）；⑦ :221 起——冒號 :241／`**已修**（commit \`sha\`）`／`已修，…。commit \`sha\`。`／表格別欄四種 reviewer 形狀為綠、`\`sha\` 已修` hex 在前紅 :245、`x<sha>y` 紅、`已修（8 hex）` 格式綠（--verify 才紅）、一行兩個「已修」只看第一個；`--verify` 案 :263 改為「第一個 hex 是 comment id、真 SHA 在其後 → 仍紅」。
3. **minor-2 → `b199cf8`** `CLAUDE.md:26` 改 `bash scripts/gates/pr-body-check.sh <body> --branch <branch> --verify` 並檢查 exit code（勿接 `| tail`）；同 commit 同步 `docs/COLLABORATION.md` §7 :240（自測 88）／:290（規則改「已修後第一個 hex」、四種形狀皆可、盲區改寫）與 `.claude/agents/ios-dev.md:13`（「已修」後第一個 hex 是本分支 commit SHA、comment id 寫在 SHA 之後）。`agent-tools-check` BODY_RULES 釘的是旗標字樣 `pr-body-check.sh <f> --branch <分支> --verify`，不受 minor-1 影響、未動。
4. **informational-1** → 記入 LS-96 `4b71c2b7`（兩支等價突變的說明＋reviewer 替代案 2「檔名加進 pretool_engine H3 家族」列候選）。
5. PR #300 body 更新（`LS-186-pr-body.md`：變更摘要改為最終語意、加「## R2…已修 `360ef5f`／`1e76a80`／`b199cf8`」段、風險段拿掉「hook 也擋」）：以 `bash scripts/gates/pr-body-check.sh <body> --branch hotfix/LS-186-gate-refinements --verify` 直接看 exit code——**前兩版被自己的 gate 紅**（五行純敘述含「已修」字樣、一行「已修」後第一個 hex 是 run id `33934840343`、一行是 `d87d4334`），改寫措辭後 exit 0 → `gh pr edit 300` → 再 `git push`（body 在最後一次 push 之前就位、synchronize 事件讀到的是新 body）。

## 已驗證（怎麼驗）
- `pr-body-check.test.sh` **88 組全綠**（bash 5.3／`/bin/bash` 3.2.57／`LANG=C LC_ALL=C`）。**LS-185 紅 body 重放**（`--branch hotfix/LS-185-overflow-scan-board-clip --verify`）→ exit 1、第 24 行「『已修』之後沒有 commit SHA」、**不列 `d87d4334`**、stdout `| tail -1` 印 `✗ pr-body-check：未通過…`；PR #298 修正後 body → exit 0「第 24 行 SHA 73fa7e1 在 PR commits 內」。**Mutation**（scratch 副本，先驗基線 88 綠）：候選退回「行內任一 hex」（`rest=$1`＋拿掉 guard）→ ⑦ 三案紅（LS-185 回歸、hex 在前、末行 ✗），84/88；改成「已修之後全部 hex 任一」（拿掉 `cut`）→ ⑦「第一個是 comment id、真 SHA 在其後」紅，86/88。（第一次做「任一 hex」突變時只改 `rest=$1` 沒拿掉 guard，得到 30 紅的壞突變——察覺後重做，此處是重做結果。）
- `db-reset-retry.test.sh` **17 組全綠**（bash 5／3.2），⓪ 四案含「無變數 → exit 2 且完全沒呼叫 supabase」。R1 的兩支 mutation（任何錯誤都重試／永不重試）語意未動、未重跑。
- `agent-tools-check.sh`（真 repo）通過、14 條；`agent-tools-check.test.sh` **65 組全綠**（bash 5／3.2）——ios-dev.md:13 改句後 BODY_RULES 字樣仍在。
- 三個 commit 皆過 commit／push gate；**未跑**：真 CI（push 後不等）、真容器（`db-reset-retry.sh` 一次都沒真的執行，只有假 `supabase`／`sleep`）、`actionlint`（未安裝）。未碰 Pen／模擬器／容器／主 checkout；lock 未持有。

## 未完成／處置（reviewer R1 全部 finding）
- major-1：**已修** `360ef5f`（`scripts/ci/db-reset-retry.sh:25-29`、`db-reset-retry.test.sh:62-75`）。採 reviewer 三選一的第 1 案＋訂正檔頭；第 2 案（hook 層 `RUNSH_RE` 同型）入池 LS-96 `4b71c2b7`。
- minor-1：**已修** `1e76a80`（`scripts/gates/pr-body-check.sh:125-129`、`pr-body-check.test.sh:221-273`）。依 orchestrator 裁定採替代方案「已修之後同一行第一個 hex」。**殘餘盲區**（已寫進 §7 :290）：同行多個「已修」只看第一個；SHA 寫在 comment id 之後會紅（訊息給正典寫法）；純數字 token（run id、時間戳）也是合法 hex 候選——`hex_tokens` 既有性質，本 PR body 第二版就踩到（`33934840343`），--verify 會擋、格式模式放行。
- minor-2：**已修** `b199cf8`（`CLAUDE.md:26`）。
- informational-1：記入 LS-96 `4b71c2b7`（等價突變非測試缺口；不改程式）。
- 池項未動：`29b413bb` 第 2、3 條、`978cdcdb` 第 1 條不在本票範圍。

## 風險
- `pr-body-check` 規則即日對所有 PR body 生效：SHA 在「已修」之前、comment id 寫在 SHA 之前（`已修（comment \`id\`，commit \`sha\`）`）會紅，純敘述 gate 規則的行也會被掃（不辨語境是 LS-140 既有取捨）——本 PR body 就紅了兩版。
- `db-reset-retry.sh` 守門是腳本內檢查、非 hook：`LS_DB_RESET_RETRY_ALLOW_LOCAL=1` 是明示放行，放行後重試路徑會 `stop --no-backup` 清共用容器 volume。真 runner 上重試路徑未驗（本 PR db job 只走首跑成功路徑）。
- hotfix 流程：併入 `main` 後補 back-merge `main`→`development`。

## 產出位置
- PR #300 https://github.com/CLYEH/little-sprout/pull/300（head `b199cf8145f96d7e0f6930aee8996c982279e136`；R2 commits `360ef5f`／`1e76a80`／`b199cf8`，皆已 push；body 已更新）
- worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-186`（clean，與 origin 同步）
- scratchpad：`LS-186-pr-body.md`／`LS-186-handoff.md`／`LS-186-r2-prbody-{bash,bash32,clocale}.log`／`LS-186-r2-prbody-mutant-{anyline,allafter}.log`／`LS-186-r2-ls185-{red,fixed}.log`／`LS-186-r2-dbretry-{bash,bash32}.log`／`LS-186-r2-agent-tools-{gate,suite,suite32}.log`／`LS-186-r2-mut-*/`（mutation 副本，可刪）
- LS-96：R1 銷除 `e333f358`／`059dd023`／`c3f9fc2f`；R2 入池 `4b71c2b7`
- lock：未持有；Pen／模擬器／容器未碰。

### 2026-09-05T04:24:04.279Z · Chih-Lin Yeh · `73c10b4e`

## merge-review R2（增量）— **APPROVE**（PR #300，head `b199cf8145f96d7e0f6930aee8996c982279e136`；2026-09-05 12:22）

範圍：`git diff 7a1bdce..b199cf8`＝3 commit／7 檔 +70/−37。R1 的 **major-1／minor-1／minor-2 全部處理完畢**，我逐項實測；**無 blocker／major／minor**，2 條 informational。CI 四項全 pass（run 33944051759）——其中 **`db` job 3m40s 綠，代表新的 CI-only 守門在真的 GitHub Actions 環境放行、沒有打死 db job**（這是本輪最重要的一項真環境證據）。

### (1) major-1 守門 — 位置、邊界、測試都對

- **位置**：`db-reset-retry.sh:23-26`，緊接 `set -uo pipefail`、在 `PORT_RACE` 與任何 `supabase` 之前。我在 **PATH 沒有假 `supabase`** 的環境實跑（若守門在 reset 之後，會先看到 `command not found`）：輸出只有守門訊息、`exit=2`，一次 supabase 都沒碰。
- **邊界（`env -u` 清空三個變數後逐一加回）**：無變數→2；`LS_DB_RESET_RETRY_ALLOW_LOCAL=0`→**2**；`=`（空值）→2；`CI=1`→2；`CI=false`→2；`GITHUB_ACTIONS=false`→2。只有字面 `true`／`1` 放行，與 LS-184 `LS_LOCK_ALLOW_MAIN` 的慣例一致。
- **我自己的 mutation（複製樹，基線先綠、跑完還原重驗）**：G1 整段拿掉守門 → ⓪ 2 案紅；G2 把 `=0` 也當放行 → ⓪「覆寫 =0 應仍 exit 2」紅；G3 把守門搬到第一次 reset 之後 → ⓪「非 CI 應 exit 2 且**不呼叫 supabase**」紅。**三支全 killed**——「位置在最前」這件事確實被釘住，不只是「有守門」。
- 自測 13→**17 組**（⓪ 四案：非 CI／`=0`／`GITHUB_ACTIONS=true` 放行／`CI=true` 放行），全綠（bash 3.2.57）。
- 檔頭 `:16-21` 已把我 R1 的實測寫進去（「H3／H3b 只看命令列文字…reviewer 實測 `bash scripts/ci/db-reset-retry.sh` 是 ALLOW」），錯誤宣稱已訂正。

### (2) minor-1 新規則 — 我 R1 列的四種形狀全綠、LS-185 仍紅在正確原因、沒有新的假綠

用真腳本對合成 body 實跑（`--branch … --verify`）：

| 形狀 | R1 | **R2** |
|---|---|---|
| `已修 \`sha\`` | ✅ | ✅ |
| `**已修**（commit \`sha\`）`（PR #286） | ❌ | **✅** |
| `已修：\`sha\``（PR #256） | ❌ | **✅** |
| `已修，…。commit \`sha\`。`（PR #270） | ❌ | **✅** |
| 表格別欄 `| 已修：… | \`sha\` |`（#251／#254） | ❌ | **✅** |
| `\`d87d4334\` …皆已修`（LS-185 原形狀） | 紅 | **紅**（訊息：『已修』之後沒有 commit SHA） |
| `已修 x7c2ff80y`（非獨立 token） | 紅 | 紅 |
| `已修（comment \`d87d4334\`，commit \`sha\`）` | — | 紅（第一個 hex 是 comment id；訊息明說「SHA 要寫在 comment id 之前」）——fail-closed，正確 |

**回歸量化（我 R1 那份 60 PR 資料重跑）**：154 條「已修」行在 R1「緊鄰」下 43 條（28%）無候選；**R2 只剩 10 條會紅**（2 條無候選＋8 條首個 hex 不是 commit，其中 5 條是 `20260903115014` 這種 migration 檔名數字前綴排在 SHA 之前）——誤紅率 28% → 6.5%，且剩下的都有明確訊息可循。

**新假綠檢查**：`fix_shas` 現在是 `${1#*已修}` ＋ `hex_tokens … | cut -d, -f1`。
- 純數字 token（CI run id `33934840343`、migration 前綴）確實會成為候選 → **格式模式（不帶 `--verify`）會假綠**（我實跑：格式模式 exit 0、`--verify` exit 1）。但這是 **main 上就有的既有性質**（舊規則同樣把任何 hex token 當候選），不是本輪新引入；CI 一律帶 `--verify`，機械層仍擋得住。列為 info-1。
- 「只取第一個候選」是刻意 fail-closed：F2（改成所有候選皆可）→ ⑦「comment id 在前、真 SHA 在後仍要紅」變紅；F3（改取最後一個）→ 4 條紅。**兩支都 killed**，這個選擇被測住、不會被無聲放寬。
- F1′（把 `${1#*已修}` 改成整行掃、保留其餘邏輯）→ **killed**，紅的正是「LS-185 回歸」與「hex 在『已修』之前不算」兩案。（第一次我把 F1 寫成 `rest=$1` 卻留著 `[ "$rest" != "$1" ] || return 0` 的哨兵，結果變成「永不回候選」的無效突變、30 條紅——已重跑修正版，此處列的是修正後結果。）
- 自測 84→**88 組**全綠（bash 3.2.57），新增的四案標題直接引用 PR #256／#286／#270／#251-254 的真實形狀。

### (3) 文件三處與規則一致

- `CLAUDE.md:26`：`bash scripts/gates/pr-body-check.sh <body> --branch <branch> --verify` 並檢查 exit code（勿接 `| tail`）— R1 minor-2 已補，與 ios-dev.md／COLLABORATION §3 同語。
- `.claude/agents/ios-dev.md:13`：改為「『已修』之後第一個 hex 是本分支的 commit SHA（comment id 寫在 SHA 之後）」，與新規則一致；`agent-tools-check` 釘的字樣沒被改壞——真 repo gate 重跑仍印「正文必含字樣 **14 條**」、自測 65 組全綠。
- `docs/COLLABORATION.md:240`／`:290`：規則敘述改為「同一行『已修』之後第一個 hex」、樣本數 84→**88**、並記了「R1 緊鄰會擋掉歷史 28%」的沿革與「SHA 寫在 comment id 之前否則紅」。三處與程式行為一一對得上。

### (4) head／夾帶

- `--expect $(gh pr view 300 --json headRefOid)` 與我審的 SHA 相同、貼 status 前再比一次；審查期間 head 未前進。
- 逐 commit：`360ef5f` 守門＋自測／`1e76a80` gate＋自測／`b199cf8` 三份文件各 1–2 行——**無夾帶**，worktree 乾淨。
- PR body 以 CI 旗標實跑 exit **0**（第 27 行 `0b57d69`、第 31 行 `360ef5f` 兩條申報都反查到）。

### findings（皆 informational）

**info-1 · 格式模式對「純數字 token」仍會假綠**：`已修，見 CI run 33934840343` 在不帶 `--verify` 時 exit 0（把 run id 當短 SHA）。既有性質、非本輪引入，CI 帶 `--verify` 會紅；且本票已把「本機也要帶 `--verify`」寫進 CLAUDE.md／ios-dev.md／COLLABORATION 三處。若日後想更嚴，可考慮「候選必須含至少一個 a–f 字母」之類的啟發式（會犧牲全數字短 SHA 的合法情況，我不建議在本票做）。

**info-2 · 殘餘 6.5% 的誤紅形狀**：`已修：\`20260903115014_xxx.sql:230\`……commit \`sha\`` 這種「數字檔名前綴排在 SHA 之前」仍會紅（歷史上 5 條）。訊息已明說「SHA 要寫在 comment id 之前」，但沒點名「檔名/行號裡的數字也算」；下次動這支時可在指引句補半句。不阻擋。

### 沒看／沒跑的（誠實揭露）

- **`db-reset-retry.sh` 我仍然一次都沒有真的執行過重試路徑**（只跑假 `supabase`／`sleep` 的自測；真 CI 這次也只走首跑成功路徑）——「真 runner 上 stop→sleep→db start→重試」依舊未驗證，仍是既有風險，不是本輪新增。
- 未碰 Pen／模擬器／容器；`actionlint` 未安裝未跑（ci.yml 本輪未再改）。
- 60 PR 的統計只涵蓋 PR body。
- lock：**未持有**。

**verdict：APPROVE**（R1 的 major-1／minor-1／minor-2 皆已修且被測住）。併入 `main` 後補 back-merge `main`→`development`。

### 2026-09-05T05:00:11.863Z · Chih-Lin Yeh · `3bfaac6a`

## QA delta R1 — **PASS**（2026-09-05 12:59 CST，摘要；完整報告見 LS-195 comment `e919cab1`）

範圍：`test` tip `b1f714f10d51b5be653131e33aba1bbf5c88c341`（PR #300 head `b199cf8`；merge-review R2 `73c10b4e` APPROVE），與 LS-195 合併一輪 delta。worktree `/Users/clyeh/little-sprout/.claude/worktrees/qa-test`，`HEAD` 已核對相符。零 app 程式碼（`git diff --stat 103d9d4..b1f714f -- LittleSprout LittleSproutTests LittleSproutUITests` 為空）——不跑 xcodebuild、不用模擬器。

### 逐條驗收

**自測**：
- `bash scripts/gates/pr-body-check.test.sh` → **88 組全綠**。
- `bash scripts/ci/db-reset-retry.test.sh` → **17 組全綠**。
- `bash scripts/gates/agent-tools-check.test.sh` → **65 組全綠**。

**真實驗證 (1) — 裸跑 `db-reset-retry.sh`（無 CI 變數）**：
```
$ env -u CI -u GITHUB_ACTIONS -u LS_DB_RESET_RETRY_ALLOW_LOCAL bash scripts/ci/db-reset-retry.sh
✗ db-reset-retry：只給 CI runner（重試路徑會 supabase stop --no-backup 清掉共用本機容器的 volume）；
  本機請用 bash scripts/ops/supabase-lock.sh -- supabase db reset（明示放行：LS_DB_RESET_RETRY_ALLOW_LOCAL=1；LS-186）
EXIT=2
```
stdout 只有守門訊息，未呼叫任何 `supabase`（守門在腳本 `:23-26`，緊接 `set -uo pipefail`、在 `mktemp`／`run_reset` 之前——已核對原始碼位置）。

**真實驗證 (2) — LS-185 紅 body 形狀＋`--branch --verify`**：合成一份含標題行「## R2（merge-review R1 \`d87d4334\` … 皆已修）」的 PR body，`--branch hotfix/LS-185-overflow-scan-board-clip --verify` 跑出：
```
✗ pr-body-check：第 5 行含「已修」，但『已修』之後沒有 commit SHA（…「已修」之前的 hex 不算）
✗ pr-body-check：未通過（exit 1）——原因見上方 ✗ 各行；請以 exit code 分支，勿接 | tail
```
exit 1、最後一行有 ✗、且**不把 `d87d4334` 當 SHA 候選**（訊息明確排除「已修」之前的 hex）——符合驗收要求。

再測「已修 \`<真 SHA>\`（comment \`d87d4334\`）」形狀 → 全綠：
```
✓ PR body 檔頭段含本票票號 LS-185
✓ 第 7 行「已修」SHA <sha> 在 PR commits 內
✓ 申報反查：LS-96 行 0 條、「已修」行 1 條
EXIT=0
```

### merge-review R2 informational 追蹤（`73c10b4e`）
info-1（格式模式對純數字 token 仍假綠，`--verify` 才擋得住）／info-2（殘餘 6.5% 誤紅形狀，訊息已給正典寫法）——皆 informational、reviewer 已判非阻擋，QA 未發現新增問題。

### 回歸／未跑
- CI `db` job 的 port 競態重試路徑（真 runner 上 stop→sleep→db start→重試）本輪 QA 與 merge-review 皆**未實跑**，屬既有已知風險（merge-review R2 誠實揭露：這次 CI 也只走首跑成功路徑），非本輪新增缺口。
- `actionlint` 未安裝，未跑（`ci.yml` 本輪未再改，僅註解「四支」→「六支」）。

### 產出位置
- scratchpad：`LS-186-195-qa-ls185-red-body.md`／`LS-186-195-qa-ls185-fixed-body.md`
- 模擬器已關：無；lock 已釋放：`LS-186-195 delta QA`（持有 3 分 48 秒，正常釋放）；qa-e2e：未跑（純 harness delta，無 app UI）

裁決：**PASS**，與 LS-195 共用同一次 `post-status.sh qa=success`（貼到 `b1f714f10d51b5be653131e33aba1bbf5c88c341`）。

### 2026-09-05T05:07:59.574Z · Chih-Lin Yeh · `c640bd58`

## dead-code sweep — LS-186

範圍：`git diff 103d9d4..9492c84`（PR #300 `hotfix/LS-186-gate-refinements` → main，R1+R2 共 7 commit：`0b57d69`／`4b4212c`／`45db12f`／`7a1bdce`／`360ef5f`／`1e76a80`／`b199cf8`）——`scripts/gates/pr-body-check.{sh,test.sh}`、`scripts/ci/db-reset-retry.{sh,test.sh}`（新檔）、`scripts/gates/agent-tools-check.{sh,test.sh}`、`.github/workflows/ci.yml`、`CLAUDE.md`、`.claude/agents/ios-dev.md`、`docs/COLLABORATION.md`。唯讀核對，未 checkout／未碰容器與 lock。

### Findings

未發現本 feature 引入的死碼。逐項核對：

1. **`pr-body-check.sh` 舊「緊鄰」規則殘留分支／無用變數**：`git diff 103d9d4..9492c84 -- scripts/gates/pr-body-check.sh` 顯示 R1（`0b57d69`）與 R2（`1e76a80`）是對 SHA 候選抽取邏輯整支改寫，不是加分支——舊的 `hex_tokens "$line" 7 40`（整行任一 hex）被完整替換為新增的 `fix_shas()` 函式（`${1#*已修}` 取「已修」之後子字串 → `hex_tokens … | cut -d, -f1` 取第一個），呼叫點同步換掉，沒有留下死路徑或孤兒變數。`grep -n "緊鄰" scripts/gates/pr-body-check.sh scripts/gates/pr-body-check.test.sh` 只命中程式碼註解裡的沿革敘述與自測標題（`.sh:31,123`／`.test.sh:9,221,237,259`），不是可執行的舊邏輯。三份文件（`CLAUDE.md:26`、`.claude/agents/ios-dev.md:13`、`docs/COLLABORATION.md` §3／§4-b／§7 共 6 處）已同步改為「已修後第一個 hex」的新敘述，沒有任一處仍把已作廢的「緊鄰」規則當現行規則描述。
2. **`db-reset-retry.sh` 守門後 `ci.yml` 是否有重複的 `supabase stop` 步驟或死 env**：`grep -n "supabase stop\|supabase db reset\|supabase db start" .github/workflows/ci.yml` 只命中三行——`db start`（:522,524）與新的 `bash scripts/ci/db-reset-retry.sh`（:529）；沒有裸的 `supabase db reset` 殘留，也沒有額外的 `supabase stop` 步驟（`stop --no-backup` 只在 `db-reset-retry.sh` 內部重試分支呼叫一次）。腳本讀的 `GITHUB_ACTIONS`／`CI`／`LS_DB_RESET_RETRY_ALLOW_LOCAL` 三個 env 都用於 CI-only 守門，ci.yml 不需另外設定（前兩個 GitHub Actions runner 內建），檔頭註解已言明，非死 env。
3. **三支自測是否有跳過／永真案**：實跑三支，全綠且樣本數與 commit message／QA 記錄一致：`bash scripts/ci/db-reset-retry.test.sh` → 17 組；`bash scripts/gates/pr-body-check.test.sh` → 88 組；`bash scripts/gates/agent-tools-check.test.sh` → 65 組。`grep -n "skip\|SKIP\|xfail\|TODO\|FIXME"` 三檔僅命中 `agent-tools-check.test.sh:153` 一處 awk 變數名 `skip`（mutation 用的區塊過濾旗標，非測試跳過）。三支都附 mutation 負控（`db-reset-retry` 的「任何錯誤都重試／永不重試」、`pr-body-check` 的「退回行內任一 hex／取最後一個」、`agent-tools-check` 的「拿掉 LS170-BODY-RULES 區塊」），且 merge-review R1／R2 各自重跑過一遍得到相同 killed 結果，未發現只斷言 exit code 不查輸出、或只查輸出不管 exit code 的空洞案例。
4. LS-195 遷移檔「其餘四種 kind」計數口誤不在本票範圍，見 LS-195 報告（已由 merge-review N2 入池，未重複列於此）。

### 既有觀察（非本 feature 引入）
無。

### 結論
LS-186 引入的 gate／CI 精修無死碼殘留：R1→R2 的規則替換乾淨（無殘留分支）、CI 接線無重複步驟／死 env、三支自測皆非空洞。

### 搜尋方法與盲區
- 方法：`git diff 103d9d4..9492c86` 鎖定本票變動的 10 個檔案；對 `pr-body-check.sh` 逐行核對新舊定義與呼叫點；`grep -n` 掃 `ci.yml` 的 supabase 生命週期呼叫；實跑三支 `.test.sh` 核對樣本數與是否含跳過標記。
- 盲區：純文字搜尋抓不到執行期動態行為——`db-reset-retry.sh` 的重試路徑（`supabase stop --no-backup → sleep 10 → db start → 重試`）僅由假 `supabase`／`sleep` stub 驗過呼叫序列與 exit code，未在真實 GitHub Actions runner 上實際觸發過 54322 port 競態（merge-review R2 與 QA 皆誠實揭露：本次 CI 的 `db` job 只走首跑成功路徑）；此為既有風險揭露而非死碼，不列入本報告 Findings。

### 2026-09-05T05:09:29.976Z · Chih-Lin Yeh · `ce8fedcb`

**orchestrator 2026-09-05 13:1x 收尾儀式 → Done**

- 併入：PR #300 → main `9492c84`；back-merge #302 → development；promote dev→test→main `b1f714f`；tag **v0.13.3**。delta QA PASS `3bfaac6a`（完整版 LS-195 `e919cab1`）。
- sweeper `c640bd58`：0 件死碼（`fix_shas()` 整支替換無殘留分支；ci.yml 無重複 stop／死 env；三支自測無跳過案）。
- lesson learning：(1) 本 PR body 自己被新規則紅兩版（敘述 gate 規則時含「已修」＋run id 純數字 token）→ 派工模板已加「敘述 gate 規則避開申報字樣」；(2) reviewer R1 用 60 個歷史 PR 量化誤紅率（28%→6.5%）是好做法，記入 harness reviewer 慣例；(3) `db-reset-retry.sh` 真 runner 重試路徑仍未驗（只走首跑成功路徑）——留待下次 flake 自然驗證，不造 flake。
- cleanup：worktree／本機分支 12:36 `cleanup-merged.sh --apply LS-186` 已清；informational 入池 `092db41b`。

