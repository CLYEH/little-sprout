# LS-182 Chore：LS-22 後端——推播／通知／停權測試與文件債清理（LS-96 池項六條：Stub 410 注入入口、claim race 耗時斷言、`ls172_*_capture` 殘表清理、push-dispatch 行轉換搬進 handler.ts＋測試、`delete_my_account` 情況 2 對停權者回歸案、API.md／105 檔頭三處一行修）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:S, lane:backend |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M5 互動與推播 |
| 估點 | — |
| 建立 | 2026-09-04T23:16:31.664Z |
| 開始 | 2026-09-04T23:16:31.792Z |
| 完成 | 2026-09-05T00:46:42.222Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-182/chorels-22-後端-推播通知停權測試與文件債清理ls-96-池項六條stub-410-注入入口claim-race |
| 父票 | LS-22 Story：留言、愛心與彙總推播 |
| 關係 | related→LS-180, related→LS-181, related→LS-8, related→LS-96, related→LS-172, related→LS-179, related→LS-23, related→LS-144, related→LS-175, related←LS-204, related←LS-183 |

## 描述

## 來源

巡檢 2026-09-05 07:1x（[LS-144](https://linear.app/little-sprout-app/issue/LS-144/harness巡檢加開票責任機械提醒-lane-在飛-0-且無可派候補時動作清單印-開票並列來源story-拆票ls-96-p1-升票) 開票責任）：lane:backend 在飛 0（[LS-179](https://linear.app/little-sprout-app/issue/LS-179/taskls-23-後端-營運防線使用者家庭停權旗標profilessuspended-atfamiliessuspended) 進 QA）、Backlog 無 backend 候補、PLAN Phase 1 後端路線圖已全部落地；依 COLLABORATION §5-b 從 [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 待辦池升同區域的測試／文件債為一支 chore。全部為 merge-review／QA informational，單獨升票不划算、合併成一支 S 票在同一次 lock hold 內做完。

## 範圍（每條附池項 comment id，做完各回一則銷除）

1. [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `531a0975`（[LS-172](https://linear.app/little-sprout-app/issue/LS-172/taskls-22-後端-推播發送-edge-function-push-dispatch消化-notification-events) QA-1）：`push-dispatch` `index.ts`／`StubApnsProvider` 加環境變數（如 `PUSH_DISPATCH_STUB_RESPONSE=410`）讓 `functions serve` 下能注入 410／BadDeviceToken，Stub E2E 可驗 `tokens_removed` 與 `device_tokens` 刪列；deno 測試一案。
2. [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `8519d8a4` **第 2 條**（[LS-172](https://linear.app/little-sprout-app/issue/LS-172/taskls-22-後端-推播發送-edge-function-push-dispatch消化-notification-events) R2-i2）：`supabase/tests/concurrency/` claim race 案例補耗時斷言，真釘住 `SKIP LOCKED`（改 `for update` 必紅——自證）。
3. [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `8519d8a4` **第 3 條**（[LS-172](https://linear.app/little-sprout-app/issue/LS-172/taskls-22-後端-推播發送-edge-function-push-dispatch消化-notification-events) R2-i3）：`supabase/tests/run.sh` cleanup 補 `drop table if exists public.ls172_claim_race_capture, public.ls172_claim_vs_record_capture`。
4. [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `a6f28382` **第 2 條**（[LS-175](https://linear.app/little-sprout-app/issue/LS-175/taskls-22-後端-media-新增彙總通知事件批次上傳-50-張一則爸爸新增了-50-張照片after-insert) R1-i2，R2 只做一半）：`push-dispatch` `index.ts` 的 `claimEvents` 行轉換搬進 `handler.ts` 純函式並補測試（未知欄位／null 的行為釘住），`index.ts` 只剩 `Deno.serve` 接線。
5. [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `17d8c759` **n3b**（[LS-179](https://linear.app/little-sprout-app/issue/LS-179/taskls-23-後端-營運防線使用者家庭停權旗標profilessuspended-atfamiliessuspended)）：`105_suspension_and_registrations.sql` 補「停權者為唯一成員 → `delete_my_account()` 情況 2 cascade 硬刪成功」一案。
6. [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `17d8c759` **n1／n2／n3**：`docs/API.md:188`（`service_role` 用語改「只有表擁有者；service_role 若需要須另 grant」）、`docs/API.md:1356`（場景號 8／9 → 10／11）、`105_…sql:8`（8–14 → 8–13）三處一行修。

## 不做

新功能；report 推播對象／文案（[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `12e20e0c`，綁 [LS-23](https://linear.app/little-sprout-app/issue/LS-23/story檢舉封鎖額度-uieula) 產品決定）；APNs 429 generation（[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `8519d8a4` 第 1 條，綁 [LS-8](https://linear.app/little-sprout-app/issue/LS-8/phase-0-4註冊-apple-developer-program個人佔-bundle-id-與-app-名稱) 部署前清單）；`50_` 判準盲區（[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `17d8c759` n4，harness lane）。

## 驗收

- [ ] Stub E2E 能注入 410 並觀察到 `tokens_removed=1`＋`device_tokens` 刪列（handoff 貼 JSON）
- [ ] claim race 案例在 `for update` mutation 下紅；[run.sh](<http://run.sh>) 跑完無 `ls172_*_capture` 殘表；`claimEvents` 行轉換有測試；105 新案綠
- [ ] SQL＋deno 全綠、gates 綠、merge-reviewer 通過（scope：只動測試／文件／index.ts↔handler.ts 搬移，行為等價）；六則池項銷除註記

## 留言（10）

### 2026-09-04T23:16:51.692Z · Chih-Lin Yeh · `64f144fb`

**2026-09-05 07:17 orchestrator — 派工**

- lane:backend 空（LS-179 進 QA）→ 依 LS-144／§5-b 升六條池項為一支 chore；cycle 1；worktree `.claude/worktrees/LS-182`，分支 `feature/LS-182-push-test-debt`（base origin/development `a0b025c`，含 LS-179）。
- 派 ios-dev R1。

### 2026-09-04T23:56:31.910Z · Chih-Lin Yeh · `9d8c1019`

## Ticket
LS-182 — Chore：LS-22 後端——推播／通知／停權測試與文件債清理（LS-96 池項六條）

## 已完成（六條，各附 commit SHA＋檔案:行號）

1. **Stub 410 注入入口**——`ef38167`。`supabase/functions/push-dispatch/handler.ts`（`parseStubResponse()`，`StubApnsProvider` 類別上方新增）：讀 `PUSH_DISPATCH_STUB_RESPONSE` 環境變數，設成 `"410"` 時回傳 `invalidToken:true` 的 responder；未設定回 `undefined`（維持預設 `ok:true`）；不認得的值 fail loud 丟例外。`supabase/functions/push-dispatch/index.ts`（`apnsProvider` 建構處）接線。`docs/API.md` §10（`StubApnsProvider` 段）新增說明。deno 測試 4 案（`handler.test.ts`）。
2. **claim race 耗時斷言**——`3f937a6`。`supabase/tests/concurrency/push_dispatch_claim_race_setup.sql`（`ls172_claim_race_capture` 加 `claim_duration_ms` 欄位）、`push_dispatch_claim_race_s2.sql`（`clock_timestamp()` 前後記時）、`push_dispatch_claim_race_verify.sql`（斷言 ≤1000ms）。
3. **`run.sh` cleanup 補殘表清理**——`89f7ca9`。`supabase/tests/run.sh`（`cc_cleanup.sql` heredoc 開頭）補 `drop table if exists public.ls172_claim_race_capture, public.ls172_claim_vs_record_capture`。
4. **`claimEvents` 行轉換搬進 `handler.ts`**——`d1216f0`。新增 `handler.ts` 的 `mapClaimedEventRows()` 純函式（`isContentTargetType` 定義之後）；`index.ts` 的 `claimEvents` 只剩接線（呼叫 RPC → 餵給該函式）。4 案測試於 `handler.test.ts`。
5. **105 補停權者唯一成員 cascade 硬刪回歸案**——`4765fa5`。`supabase/tests/105_suspension_and_registrations.sql` 新增場景 14（第 766-817 行），並訂正檔頭場景範圍（第 7-10 行）。
6. **三處一行修**——`9e0e800`。`docs/API.md:188`（`service_role` 用語）、`docs/API.md:1356`（場景號 8/9→10/11）。

LS-96 池項銷除註記（附本票 PR 號與 commit SHA）：
- `531a0975`（回覆 `de67de63`）
- `8519d8a4`（回覆 `2d0872b5`，涵蓋第 2／3 條）
- `a6f28382`（回覆 `02b4c6ba`）
- `17d8c759`（回覆 `394e25d3`，涵蓋 n1／n2／n3／n3b）

## 已驗證：怎麼驗的

全部在同一次 `bash scripts/ops/supabase-lock.sh --hold "LS-182 dev" --max-minutes 25` 內完成，`--release` 印出**持有 11 分 43 秒**：

- `supabase db reset` → `bash supabase/tests/run.sh` 全綠（含新增場景 14、耗時斷言 S2=4.1ms）。
- 殘表查詢：`select schemaname, tablename from pg_tables where tablename like 'ls172_%';` → **0 rows**（suite 跑完無殘表，收工前又重跑一次同樣 0 rows）。
- **mutation 紅訊息**（項目 2 自證）：在本機 DB 直接 `CREATE OR REPLACE FUNCTION public.claim_notification_events` 把 `for update skip locked` 改成 `for update`（只改 DB 函式，未動 migration 檔，`git status supabase/migrations/` 全程乾淨）→ 重跑 `run.sh` → 案例紅：
  ```
  ERROR:  FAIL 併發：S2 呼叫 claim_notification_events() 耗時 1507.6 ms，超過 1000ms 閾值——SKIP LOCKED 疑似沒有正確跳過 S1 鎖住的列，反而被卡住等待 S1 commit（merge-review R2-i2）
  ```
  → `supabase db reset` 還原 → 重跑全綠（S2 耗時 4.1ms）。
- **Stub E2E JSON**（項目 1）：`supabase functions serve push-dispatch --env-file <PUSH_DISPATCH_PROVIDER=stub,PUSH_DISPATCH_STUB_RESPONSE=410>`，灌 1 家庭＋1 device_token＋1 筆已穩定事件，真打 HTTP 端點：
  ```json
  {"claimed":1,"recipients":1,"sent":0,"failed":0,"tokens_removed":1,"stopped_early":false}
  ```
  `select count(*) from device_tokens where token='ls182-e2e-410-token'` → **0**（確實刪除）。
- deno：`deno test supabase/functions/*/*.test.ts` → **92 passed / 0 failed**；`deno check`／`deno lint`／`deno fmt --check` 皆綠。
- Gates：`api-contract-check`（RPC 34／表 23）、`error-codes-check`（28 碼）、`migration-immutable-check`／`migration-version-check`／`migration-breaking-check --base origin/development`（本票無 migration 變更，rc=0 無分類）皆綠；`git push` 的 push gate 通過（無 Swift 變更，SwiftLint／unit tests 皆跳過但 CI 仍跑）。

## 未完成（含判斷不做的項目與理由）

- 六條全部完成，無未完成項目。
- LS-96 `17d8c759` n4（`50_rls_plan_no_percall_subquery.sql` 判準對 policy 內帶外層欄位參數的函式呼叫抓不到）：票文「不做」清單已明列（屬 harness lane、綁 LS-8 部署前清單性質不同），維持池項現狀，不在本票處理。
- LS-96 `8519d8a4` 第 1 條（APNs 429 generation）與 `12e20e0c`（report 推播對象／文案）：票文「不做」清單已明列，分別綁 LS-8 部署前清單與 LS-23 產品決定，不在本票範圍。

## 風險

- 純測試／文件／`index.ts↔handler.ts` 內部搬移，本票無 migration 變更、無產品行為變更，無正式站部署影響（`migration-breaking-check` rc=0 無分類即為證明）。
- 項目 5 的新場景 14 沿用既有效能測試家庭（`fc000000-…-0001`），全程包在 `begin/rollback` 內，已用完整 `run.sh` 全綠驗證不影響其他測試檔對這個家庭的既有假設。
- 項目 6 的 `105_...sql:8` 檔頭修法與票文原始描述（「8–14 → 8–13」）不同——票文寫這行時 LS-182 尚未規劃出項目 5 的新場景 14，若照字面把 header 改成「8–13」，項目 5 的新增又會讓它立刻過期。改成同時反映 R2（8–13）與 LS-182（補 14）的準確描述，已在 LS-96 `17d8c759` 銷除回覆與 commit `4765fa5` 訊息中說明理由。

## 產出位置

- PR：https://github.com/CLYEH/little-sprout/pull/293（`feature/LS-182-push-test-debt` → `development`，6 commits，head `4765fa5`）
- `supabase/functions/push-dispatch/{handler.ts,index.ts,handler.test.ts}`
- `supabase/tests/concurrency/push_dispatch_claim_race_{setup,s2,verify}.sql`
- `supabase/tests/run.sh`
- `supabase/tests/105_suspension_and_registrations.sql`
- `docs/API.md`
- 模擬器已關：無（純後端票，未 boot 任何模擬器）

### 2026-09-04T23:57:06.007Z · Chih-Lin Yeh · `8255d591`

**2026-09-05 07:5x orchestrator — R1 handoff 收到（PR #293 head `4765fa5`，六條各一 commit）→ In Review，派 merge-reviewer R1**

- 審點交 reviewer：六則池項銷除回覆是否都在；`mapClaimedEventRows` 搬移行為等價；claim race 耗時斷言在 CI 機器上的 flaky 風險（1.5s 門檻）；105 檔頭 8–14→8–13 是否也修了。

### 2026-09-05T00:04:51.270Z · Chih-Lin Yeh · `de3ad20e`

## merge-review R1 — PR #293（head `4765fa51`，base `origin/development` `a0b025c`）

2026-09-05 08:03。審查對象：`git diff a0b025c...4765fa5` 全部 9 檔（EF 3、SQL 測試 4＋`run.sh`、`docs/API.md`）逐檔看完，6 個 commit 逐一對回票文六條。開審與收工各比對 `gh pr view 293 --json headRefOid`＝`4765fa51b5805fbc2006b9218f5c4541b4230792`，與我實際 diff 的 SHA 相同，審查期間 head 未前進。本機驗證全部在同一次 `--hold「LS-182 review R1」`（reset＋`run.sh`＋殘表查詢＋mutation＋還原＋複跑）內完成，**持有 2 分 53 秒**後 `--release`（狀態回 `free`）。未碰 Pen、未用模擬器。

### 裁決：APPROVE — blocker 0、major 0、minor 0、informational 2

這是 chore，我用「每一行都要對得到六條之一」的標準審 scope：**9 個檔案、6 個 commit 全部對得上，沒有一行是六條之外的**。

---

## 1. 六則池項銷除回覆（派工第 1 點）——**全部查實，沒有漏回**

到 LS-96 逐一核對（回覆存在、內容與 commit 一致）：

| 池項 | 對應票文條目 | 銷除回覆 | 核實 |
|---|---|---|---|
| `531a0975`（LS-172 QA-1） | 1 Stub 410 注入 | `de67de63` | ✓ 內容與 `ef38167` 一致，附 E2E JSON |
| `8519d8a4` 第 2 條（R2-i2） | 2 claim race 耗時斷言 | `2d0872b5` | ✓ 一則涵蓋第 2／3 條，附 mutation 紅訊息與 0 rows 殘表查詢 |
| `8519d8a4` 第 3 條（R2-i3） | 3 `run.sh` cleanup | 同上 | ✓ |
| `a6f28382` 第 2 條（LS-175 R1-i2） | 4 行轉換搬遷 | `02b4c6ba` | ✓ 說明「逐字搬移、`index.ts` 只刪不改」＋4 案測試 |
| `17d8c759` n3b | 5 場景 14 | `394e25d3` | ✓ 一則涵蓋 n1／n2／n3／n3b |
| `17d8c759` n1／n2／n3 | 6 三處一行修 | 同上 | ✓ |

**`17d8c759` n2（`105_…sql:8` 檔頭）是否真的做了（派工特別點名）**：**做了**。檔頭現在是「R2（merge-review R1 `23fa5e37`）新增場景 8–13（n2 訂正：檔頭原寫 8–14，實際 R2 只到 13）…LS-182（merge-review R2 `0ccb5ef9` n3b）補場景 14」——它同時反映 n2 的訂正與本票新增的場景 14。**這個偏離票文字面（「8–14 → 8–13」）是正確的處理**：票文寫那行時本票的場景 14 還不存在，照字面改成「8–13」會在同一個 PR 內立刻過期；handoff 的「風險」段與 LS-96 `394e25d3` 都明說了理由。handoff 把它記在項目 5（不是項目 6）之下，所以派工看到的「只提 API.md 兩處」是分類問題，不是漏做。

## 2. `mapClaimedEventRows` 搬遷（派工第 2 點）

- **逐字等價**：`git diff` 對照——`index.ts` 移除的那段（`for` 迴圈、未知 enum 的 fail-loud 分支、九個欄位映射、錯誤字串模板）與 `handler.ts` 新增的 `mapClaimedEventRows()` **完全相同**，連註解都原樣搬過去；`index.ts` 的 `claimEvents` 現在是「RPC → `if (error) return …` → `return mapClaimedEventRows(...)`」三行接線，錯誤路徑（`error.message`）與空資料處理（`data ?? []`）也未變。呼叫點只有這一處。
- **4 個新測試釘住的東西**：正常列的型別轉換（`String`/`Number`）、`actor_id` 為 `null` → `actorId: null`（不是字串 `"null"`）、未知 kind → **整批** fail loud（`events: []`＋error）、缺欄位 → 既有的寬鬆 `String(undefined)="undefined"`（明說是「搬移不修既有行為」）。四個角度都在。
- **我自己做的 mutation（3 個，不採信宣稱）**：(a) `actorId` 改成一律 `String(row.actor_id)` → 1 案紅；(b) 未知 kind 從 fail loud 改成 `continue` 跳過該列 → 1 案紅；(c) `parseStubResponse` 的 fail loud 改成靜默回 `undefined` → 1 案紅。基準 `deno test supabase/functions/*/*.test.ts` = **92 passed / 0 failed**。
- **「`index.ts` 只剩 `Deno.serve` 接線」**：實質達成（沒有未被測試覆蓋的判斷式了），但字面上還差一點——見 R1-i1。

## 3. Stub 410 注入（派工第 3 點）

- **只在 stub 生效**：`index.ts` 的 `parseStubResponse(Deno.env.get("PUSH_DISPATCH_STUB_RESPONSE"))` 只出現在 `(Deno.env.get("PUSH_DISPATCH_PROVIDER") ?? "apns") === "stub"` 這個三元的 **stub 分支內**；正式分支仍是原封不動的 `buildRealApnsProvider({...五個 secrets...}, fetch)`，缺任一 secret 照樣在模組層級 fail loud（該函式未被本票觸碰，`git diff` 對 `apns.ts` 是空的）。設了 `PUSH_DISPATCH_STUB_RESPONSE` 但沒設 `PUSH_DISPATCH_PROVIDER=stub` 時，這個變數根本不會被讀。
- **fail loud 的方向正確**：值不是 `"410"` 就丟例外，不會悄悄退回 `ok:true`（打錯字不會偽裝成「一切正常」）；未設定／空字串 → `undefined` → 維持既有預設。deno 測試 4 案（未設定／空字串／`"410"`／不認得的值）＋我的 mutation (c) 證明有牙齒。
- **API.md §10 有登錄**：新增 `PUSH_DISPATCH_STUB_RESPONSE` 段（明寫「非 secret、只在 stub 有意義、正式站不設定」），並在「本機 Stub 端到端」那段補上用法。

## 4. claim race 耗時斷言與 flaky 風險（派工第 4 點）

- **我自己重做 mutation**：把 `claim_notification_events()` 的 `for update skip locked` 改成 `for update`（只改 DB 函式，未動 migration 檔）→ 同一組 setup/s1/s2 → **verify 紅**：`FAIL 併發：S2 呼叫 claim_notification_events() 耗時 1511.9 ms，超過 1000ms 閾值`。還原函式後同一組再跑 → **綠，S2 耗時 4.0 ms**。全套 `run.sh` 內那次是 **5.2 ms**。
- **數字與門檻**：正常 4.0–5.2 ms、失敗 1511.9 ms，門檻 1000 ms 落在正常值的 ~200 倍、失敗值的 2/3——上下都留了很大的餘裕，設計合理。
- **CI 確實會跑**：`.github/workflows/ci.yml:434` 的 db job 跑 `supabase/tests/run.sh`，而 concurrency 案例掛在 `run.sh` 內，所以這條斷言在 CI 機器上執行；本 head 的 `db` job 已 pass（3m41s）。
- 殘餘 flaky 風險與建議見 R1-i2（不阻擋）。

## 5. `run.sh` cleanup 與 105 場景 14（派工第 5 點）

- cleanup 補的是 `drop table if exists public.ls172_claim_race_capture, public.ls172_claim_vs_record_capture;`，放在 `cc_cleanup.sql` heredoc 開頭、在既有的家庭／使用者刪除之前；兩張表都是 concurrency setup 自己建的測試輔助表（setup 開頭也有 `drop table if exists`，重跑安全），**沒有任何其他測試檔引用它們**（我 grep 過），對 suite 其他案例無副作用。**實測**：`run.sh` 全綠後查 `pg_tables where tablename like 'ls172_%'` → **0 rows**。
- 105 場景 14 沿用 A6 形狀：停權者為唯一成員 owner → `delete_my_account()` → 斷言 `families` 0 列（情況 2 cascade 生效、不被停權豁免擋住）＋`deletion_requested_at` 已標記；包在 `begin/rollback` 內、沿用既有效能測試家庭。實測 suite 內該案的 `raise notice` 有出現、全套綠。

## 6. gates／deno／CI（派工第 6 點）

`api_contract_check.py`（RPC 34／表 23）、`error-codes-check`（28 碼）、`migration-version-check`（本分支引入 0 檔）、`migration-immutable-check`、`migration-breaking-check --base origin/development`（rc=0 無分類——本票無 migration 變更）皆綠；`deno test` 92 passed／`check`／`lint`／`fmt --check` 綠；`gh pr checks 293` **四項皆 pass**（run 33931145237，對應本 head）。

**Scope／安全**：9 檔全部對得到六條之一，無範圍外檔案；新增行中沒有任何 URL／外連，沒有 `Deno.env.toObject`／檔案寫入／子程序／`eval` 之類的 API；唯一新增的環境變數是測試開關且只在 stub 分支讀取。

---

## Findings（皆 informational，不影響裁決）

### R1-i1 `getRecipients` 的行轉換仍留在 `index.ts`，零測試——「只剩 `Deno.serve` 接線」字面上還沒完全成立

`supabase/functions/push-dispatch/index.ts:125-129`：`rows.map((row) => ({ eventId: String(row.event_id), userId: …, token: …, platform: … }))` 是 `claimEvents` 那段的雙胞胎，同樣是「PostgREST 原始列 → 型別化物件」的轉換，同樣因為模組層級 `Deno.serve()` 而無法被測試 import。**票文第 4 條只點名 `claimEvents` 行轉換，所以這不是漏做、也不是 scope 違規**；但池項 `a6f28382` 第 2 條的意圖（「`index.ts` 只剩 `Deno.serve` 接線」）字面上還差這一段。風險比 `claimEvents` 那段低很多（沒有 enum 守門、沒有 fail-loud 分支、四個欄位都是無條件 `String()`），所以我不要求本票補。建議：下次碰這個檔案時一併搬（或在 LS-96 記一筆，size XS）。

### R1-i2 1000ms 門檻的殘餘 flaky 風險與更穩健的寫法

門檻是硬編碼常數，而「正常值」取決於機器負載：本機 4.0–5.2 ms，CI runner 上如果某次 GC／IO 抖動讓一次 5 列的 claim 卡超過 1 秒，就會出現與 SKIP LOCKED 無關的紅。以目前的餘裕（200 倍）這機率極低，且本 head 的 CI 已 pass，所以我不要求現在改。若日後真的偶發紅，**建議不要只把常數調高**——改成相對比較更貼近它要釘的東西：S1 的持有時間（`pg_sleep(2)`）本來就是已知常數，可以斷言 `S2 duration < S1 hold / 2`（＝1000ms，但語意上綁在「沒有被 S1 卡住」而不是一個魔術數字），或讓 S1 也記一個 `clock_timestamp()` 差值供 verify 相對比對。

---

## 已驗證（怎麼驗的）

- **同一次 hold 內**：`supabase db reset --workdir LS-182` → `bash supabase/tests/run.sh` **全綠**（S2 耗時 5.2 ms、場景 14 notice 出現）→ 查 `ls172_%` 殘表 **0 rows** → SKIP LOCKED mutation（race 案紅、1511.9 ms）→ 還原 claim 函式 → 同一組複跑（綠、4.0 ms）→ 清理輔助表並確認 0 張。持有 2 分 53 秒後 `--release`。
- **deno**：基準 92 passed；我自己做的 3 個 mutation（actorId null 處理／未知 kind fail loud／stub fail loud）**各自都被抓到**（每次 1 案紅）。
- **搬遷等價**：`git diff` 逐行對照移除段與新增函式；`index.ts` 呼叫點只有一處，錯誤與空資料路徑未變。
- **六則池項**：逐一開 LS-96 對照回覆 id 與內容（表格見上）。
- **gates／CI**：見第 6 節。
- 105 檔頭 n2、105 場景 14、`run.sh` cleanup、API.md 三處一行修：逐處讀過現況文字，與池項描述一致。

## 未審／跳過（誠實聲明）

- 沒有重跑實作者的 Stub E2E（`supabase functions serve` + `PUSH_DISPATCH_STUB_RESPONSE=410` 打真實端點取得 `tokens_removed:1`）：那是互動段，需要另一段 hold 與 EF runtime。我改以「`parseStubResponse` 的 4 案單元測試＋我的 fail-loud mutation＋`index.ts` 接線只在 stub 分支」三者覆蓋這條路徑的邏輯面；未親眼看到 HTTP 端點的那次注入。實作者的 JSON 與 `device_tokens` 0 rows 我採信但標明未複驗。
- 真正的 APNs／正式站：不在本票範圍。
- iOS 未 build（本 PR 無 Swift 變更）；只在本機容器驗證。

證據（session scratchpad，未進 repo）：`LS-182-driver.out`（reset／全套／殘表／mutation／還原／複跑）、`LS-182-claim-{orig,noskip}.sql`、`LS-182-mut-{A_actorid,B_skip_unknown,C_stub_silent}/`。實驗資料已清理（輔助表已 drop、claim 函式已還原原版並回讀確認），本機 DB 留在 LS-182 分支狀態。

### 2026-09-05T00:06:24.893Z · Chih-Lin Yeh · `372b1979`

**2026-09-05 08:06 orchestrator — merge-review R1 `de3ad20e` APPROVE（0 blocker／0 major／0 minor／2 informational；六則池項銷除全查實、3 mutation 全抓、去 SKIP LOCKED 紅 1511ms）→ 已併入 development `aa199f8`，票進 QA**

- dev CI 綠後晉升 test，隨 LS-181 同批 delta QA（deno 92＋run.sh＋殘表 0＋harness 自測），PASS 後 test→main、tag。
- R1-i1（`getRecipients` 行轉換仍在 index.ts）／R1-i2（1000ms 門檻改相對比較）→ 記入 LS-96。

### 2026-09-05T00:18:47.476Z · Chih-Lin Yeh · `9f6edfb1`

**2026-09-05 08:18 orchestrator — 已晉升 test `aa199f8`（含 LS-181 back-merge），派 delta QA（LS-181＋LS-182）；PASS 後 test→main、tag v0.13.0（連同 LS-179／180）、正式站推 `20260904212530`。**

### 2026-09-05T00:37:45.528Z · Chih-Lin Yeh · `cfc4c5cd`

## QA — delta（test `aa199f8`，回歸自 `a0b025c` QA PASS）2026-09-05 08:36 CST

**裁決：PASS**

worktree `qa-test` fetch＋checkout `aa199f8f38518d68e5e4b39bcc23a72b147844a0`（detached，等於 `origin/test`）。全部本機 DB／EF 互動在同一次 `bash scripts/ops/supabase-lock.sh --hold "LS-182 QA" --max-minutes 20` 內完成，`--release` 印出**持有 6 分 49 秒**（`--status` 事前確認 `free`）。收工前已 `supabase db reset` 把 E2E 灌入的測試資料清乾淨才釋放鎖。

### 逐條驗收

1. **SQL＋deno 全綠、gates 綠**
   - `supabase db reset` → `bash supabase/tests/run.sh` **全套通過**（`✓ 全部 DB 測試通過`），含新場景「場景 14」（`105_suspension_and_registrations.sql:768` 起，停權者為唯一成員 owner → `delete_my_account()` 情況 2 cascade 硬刪成功，`families` 0 列＋`deletion_requested_at` 已標記）；concurrency claim race 案例輸出耗時「S2 耗時 6.1 ms（遠低於 S1 持有的 2 秒）」——耗時斷言確實在跑。
   - 殘表查詢：`select schemaname, tablename from pg_tables where tablename like 'ls172_%';` → **0 rows**（suite 跑完無殘表）。
   - `mapClaimedEventRows` 有測試：`deno test` 輸出可見 4 案（正常列／`actor_id` null／未知 kind fail loud／缺欄位既有寬鬆行為），皆 `ok`。
   - `deno test supabase/functions/*/*.test.ts` → **92 passed / 0 failed**；`deno check`／`deno lint`／`deno fmt --check` 對 `supabase/functions` 全綠（9 檔）。
   - Gates：`api-contract-check.sh`（RPC 34／表 23 一致）、`error-codes-check.sh`（28 碼三方一致）、`migration-immutable-check.sh --base origin/main`（本票無 migration 變更，綠）、`migration-version-check.sh`（tree 33 檔無撞號）皆綠。

2. **Stub E2E 能注入 410 並觀察到 `tokens_removed=1`＋`device_tokens` 刪列** — ✓（本輪 QA 重新獨立跑一次，不只採信實作／複驗方的既有紀錄）
   - 在 hold 內起 `supabase functions serve push-dispatch --env-file <PUSH_DISPATCH_PROVIDER=stub,PUSH_DISPATCH_STUB_RESPONSE=410> --no-verify-jwt`。
   - 灌 1 筆已穩定（10 分鐘前）待送事件（家庭 `fa000000-…-0001`，kind=`comment`，actor=A 家媽媽，家內唯一有 device_token 的是 A 家爸爸 `ls6-token-a001`）。
   - 直打 HTTP 端點（`Authorization: Bearer <service_role key>`），回應：
     ```json
     {"claimed":1,"recipients":1,"sent":0,"failed":0,"tokens_removed":1,"stopped_early":false}
     ```
   - 查詢 `select count(*) from public.device_tokens where token='ls6-token-a001';` → **0**（確實刪除）；`notification_events` 該列 `sent_at` 已標記。
   - 功能結束後 `pkill` 停掉 serve process，`supabase db reset` 清回乾淨狀態。

3. **claim race 案例在 `for update` mutation 下紅；`run.sh` 跑完無 `ls172_*_capture` 殘表；`claimEvents` 行轉換有測試；105 新案綠** — ✓
   - Mutation 紅訊息已由 merge-review R1 `de3ad20e` 實測釘住（`for update skip locked` 改 `for update` → `FAIL 併發：S2 呼叫 claim_notification_events() 耗時 1511.9 ms，超過 1000ms 閾值`，還原後複跑綠）；本輪 QA 複驗斷言邏輯（`push_dispatch_claim_race_verify.sql` 的 `v_s2_duration_ms > 1000` 分支）與正常路徑（S2=6.1ms）皆確認存在且正確接線，未重覆做 mutation（避免對本機容器做非必要的函式覆寫風險，merge-review 已完整覆蓋）。
   - 殘表 0 rows（見上）；`claimEvents` 行轉換測試（見上）；105 場景 14 綠（見上）。

4. **SQL＋deno 全綠、gates 綠、merge-reviewer 通過（scope）；六則池項銷除註記** — ✓
   - merge-review R1 `de3ad20e` APPROVE（0 blocker／0 major／0 minor／2 informational：`getRecipients` 行轉換未搬、1000ms 門檻可改相對比較——兩者已記入 LS-96，不影響本票裁決）。
   - 六則 LS-96 池項銷除回覆（`531a0975`／`8519d8a4`／`a6f28382`／`17d8c759`）已在 merge-review 逐一核對存在且內容相符。
   - `docs/API.md:188`：`private.suspension_notes（只有表擁有者，postgres；service_role 若日後需要須另外 grant——目前零 grant…）`——文字現況正確。
   - `docs/API.md:1356`：`場景 10／11（n1 訂正：場景 8 是 suspension_notes、場景 9 是 content_reports，跟 delete_my_account() 無關）`——核對 `105_suspension_and_registrations.sql` 場景清單（1–14），場景 8＝`suspension_notes`、場景 9＝`content_reports`/`join_requests_select`、場景 10／11＝`delete_my_account()` 兩案，文字現況正確。

### 額外把關
- `gh run view` 對 `aa199f8` 的 CI（run 33932507735）：**success**（ci job 內含 `supabase/tests/run.sh`／`deno test`／`db reset`／API 契約對帳，17m56s 全綠；lint／rules 亦綠）。

### 未驗證
- 未重做 merge-reviewer 已做過的 SKIP LOCKED mutation（理由見上，屬不重複風險操作而非跳過驗證項）。
- 未驗 APNs／正式站真實推播（不在本票範圍，`PUSH_DISPATCH_PROVIDER` 正式站不設定）。

證據：Stub 410 回應 JSON 與查詢結果已貼在上方；`run.sh` 完整輸出、`deno test` 輸出、`functions serve` log 為本次終端機驗證過程，未落地為 repo 檔案。

### 2026-09-05T00:39:52.526Z · Chih-Lin Yeh · `90c2ae28`

**2026-09-05 08:39 orchestrator — delta QA PASS `cfc4c5cd`（Stub 410 E2E 獨立重跑 `tokens_removed=1`、殘表 0、deno 92）→ 已到 main `aa199f8`（v0.13.0）；sweeper 批次巡檢中 → retro → Done。**

### 2026-09-05T00:45:38.016Z · Chih-Lin Yeh · `2182b829`

## dead-code sweep — LS-182

**2026-09-05 08:43 CST，dead-code-sweeper。範圍：`git diff --stat 34dd954..aa199f8` 中屬本票的檔案——`supabase/functions/push-dispatch/{handler.ts,index.ts,handler.test.ts}`、`supabase/tests/concurrency/push_dispatch_claim_race_{setup,s2,verify}.sql`、`supabase/tests/run.sh`、`supabase/tests/105_suspension_and_registrations.sql`（場景 14＋檔頭）、`docs/API.md`（三處一行修，與 LS-179 共用檔案但不同段落）。在主 checkout（唯讀，未改任何檔案）巡查。**

### Findings

未發現本票引入的死碼。逐項證據：

1. **`index.ts` 死 import 檢查（票面第 4 條核心風險）**：`mapClaimedEventRows` 搬進 `handler.ts` 後，`index.ts` 的 import 清單已同步移除 `type ClaimedEvent`／`isContentTargetType`／`isNotificationKind`（改 import `mapClaimedEventRows`／`parseStubResponse`），`git diff` 逐行核對確認舊 import 無殘留、`claimEvents` 內原本 18 行的行轉換邏輯（含 fail-loud 分支）完整移除、換成 3 行接線呼叫新函式。無死 import、無殘留舊邏輯分支。
2. **`mapClaimedEventRows` 逐字等價**：對照移除段與 `handler.ts` 新增函式，內容逐字相同（含註解），非重寫；呼叫點只有 `index.ts` 一處。4 個新測試（正常列／`actor_id` null／未知 kind fail loud／缺欄位既有寬鬆行為）皆有對應 `Deno.test` 呼叫，無孤兒測試。
3. **`parseStubResponse`／`StubApnsProvider` 注入路徑**：確認 `PUSH_DISPATCH_STUB_RESPONSE` 只在 `PUSH_DISPATCH_PROVIDER === "stub"` 分支內被讀取（`index.ts` 三元運算式），正式 APNs 分支（`buildRealApnsProvider`）未被觸碰、`apns.ts` diff 為空；4 個 `parseStubResponse` 單元測試（未設定／空字串／`"410"`／不認得的值）皆有對應斷言，無定義後未呼叫的測試案例。
4. **`run.sh` cleanup 補的殘表清理**：`drop table if exists public.ls172_claim_race_capture, public.ls172_claim_vs_record_capture` 對照 `push_dispatch_claim_race_setup.sql`，這兩張表確實是 concurrency 測試自建、且 `grep` 全 `supabase/tests/` 目錄確認無其他測試檔引用它們，補的 cleanup 語句沒有多刪或少刪。
5. **claim race 耗時斷言的新欄位**：`ls172_claim_race_capture` 新增的 `claim_duration_ms` 欄位在 `setup.sql`（DDL＋grant update）、`s2.sql`（寫入）、`verify.sql`（讀取＋斷言）三檔的引用完整串接，無孤兒欄位；`grant insert, select, update` 的 `update` 權限確實對應 `s2.sql` 新增的 `update ... set claim_duration_ms`。
6. **`105_` 場景 14 與檔頭**：場景 14（第 767-817 行）沿用既有效能測試家庭 fixture、包在獨立 `begin/rollback` 內，未與其他場景共用可變狀態；檔頭措辭「R2（8–13）…LS-182（補場景 14）」與實際場景數逐一核對一致，非死碼、非殘留敘述（票面原文「8–14→8–13」被 ios-dev 判斷會在同一 PR 內立即過期而改寫，merge-review R1 `de3ad20e` 已核實這個偏離是合理處理，非漏做）。
7. **`docs/API.md` 三處一行修**：`:188`／`:1356` 已於 LS-179 報告核對過現況正確；本票額外核對 §10 新增的 `PUSH_DISPATCH_STUB_RESPONSE` 說明段與 `handler.ts` 現況一致，無提及未實作的行為。
8. **TODO／debug 殘留掃描**：`grep -n "TODO\|FIXME\|XXX"` 對全部本票檔案 0 命中；`console.log`／`debugger` 掃描：`index.ts:154` 的 `console.log(message)` 是既有（LS-172，非本票引入）的正式 logging 實作（`log(message) { console.log(message) }`），非 debug 殘留，本票 diff 未觸碰該行。

### 既有死碼觀察

無新發現。（`index.ts:119-130` 的 `getRecipients` 行轉換仍留在模組層級、零測試覆蓋——這是 merge-review R1-i1 已記錄且明確判斷「不要求本票補」的既有觀察，已記入 LS-96，非本次巡檢新發現，故不重複列為 finding。）

### 建議處置

無需開票。

### 搜尋方法與盲區聲明

- 方法：`git diff 34dd954..aa199f8 -- <本票檔案清單>` 逐檔讀取新增／刪除區段；對 `index.ts`／`handler.ts` 的 import 清單與函式呼叫點做交叉比對，確認搬遷後無死 import／死分支；對 SQL 測試檔的欄位／表名做全目錄 `grep` 交叉引用；核對 merge-review R1（`de3ad20e`）與 QA delta（`cfc4c5cd`）記錄的驗證結果（Stub E2E JSON、殘表 0 rows、mutation 紅訊息）與本次讀碼結果一致。
- 盲區：純文字掃描抓不到 Deno/TS 執行期動態 import 或反射（本票新增程式碼皆為靜態 import，風險低）；未實際起 `supabase functions serve` 或跑 `deno test`（依規約，dead-code 巡檢只讀不執行；merge-review 與 QA 已各自獨立重跑過 Stub E2E 與 92 項 deno 測試，本次採信其記錄）；未對本機容器下 SQL 查證殘表現況（QA `cfc4c5cd` 已實測 `pg_tables where tablename like 'ls172_%'` 為 0 rows，本次不重複操作容器）。
- 範圍排除：本票與 LS-180 同批出現在 `34dd954..aa199f8` 的 diff 內，但 LS-180 不在本次巡檢範圍（詳見 LS-179 報告的範圍排除說明）。


### 2026-09-05T00:46:38.762Z · Chih-Lin Yeh · `09553547`

**2026-09-05 08:5x orchestrator — 收尾儀式 → Done**

- sweeper `2182b829` 0 finding（`index.ts` 舊 import 已清）；worktree／分支已清；六則池項銷除回覆 reviewer 逐一查實。
- **Lesson learning**：(1) 六條 informational 合併成一支 S chore 在同一次 hold 內做完、R1 即 APPROVE——同區域小債批次處理比逐條升票划算，可成慣例（COLLABORATION §5-b 候選）。(2) `a6f28382` 第 2 條被拆了三次仍剩 `getRecipients` 一小段（LS-96 `ecf55dfa` R1-i1）——池項拆做時要在銷除回覆明寫「剩餘範圍」，避免同一條反覆出現。(3) 耗時斷言 1000ms 硬編碼 → 若 CI 偶紅改相對比較（`ecf55dfa` R1-i2）。(4) QA 獨立重跑 Stub 410 E2E（沒沿用實作者數字）是正確做法。

