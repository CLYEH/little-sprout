# LS-113 Task：LS-47 iOS——多寶貝 profile 三畫面實作（建檔／管理／切換），依 LS-67 核可稿

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | lane:ui |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M2 孩子檔案 |
| 估點 | — |
| 建立 | 2026-08-31T01:38:29.367Z |
| 開始 | 2026-08-31T07:12:26.642Z |
| 完成 | 2026-08-31T11:05:44.076Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-113/taskls-47-ios-多寶貝-profile-三畫面實作建檔管理切換依-ls-67-核可稿 |
| 父票 | LS-47 寶貝 profile：建立家庭後可新增多個寶貝（建立/管理/切換） |
| 關係 | related→LS-107, related→LS-85, related→LS-109, related→LS-96, related→LS-67, related→LS-95, related→LS-66, related→LS-111, related←LS-232, related←LS-190, related←LS-169, related←LS-126, related←LS-19, related←LS-119, related←LS-117, related←LS-108, related←LS-103 |

## 描述

來源：使用者 2026-08-31 核可 [LS-67](https://linear.app/little-sprout-app/issue/LS-67/設計多寶貝-profile-三畫面建檔管理切換-ui-designervisual-reviewer-3-輪) 多寶貝三畫面設計稿 → 開實作票。父票 [LS-47](https://linear.app/little-sprout-app/issue/LS-47/寶貝-profile建立家庭後可新增多個寶貝建立管理切換)（story）2026-08-25 已提前結案，本 Task 完成該 story 的 UI 半段；後端半段 [LS-66](https://linear.app/little-sprout-app/issue/LS-66/taskls-47-後端-children-多寶貝-crud-rpc軟刪-30-天可還原角色矩陣測試)（children CRUD RPC、軟刪 30 天可還原、角色矩陣）已 Done。

## Scope

1. 依 `design/littlesprout.pen` [LS-67](https://linear.app/little-sprout-app/issue/LS-67/設計多寶貝-profile-三畫面建檔管理切換-ui-designervisual-reviewer-3-輪) 核可 frame 實作三畫面：寶貝建檔、寶貝管理（含編輯／軟刪）、寶貝切換。互動與版面以核可稿為準，不自行發明。
2. 資料層走 [LS-66](https://linear.app/little-sprout-app/issue/LS-66/taskls-47-後端-children-多寶貝-crud-rpc軟刪-30-天可還原角色矩陣測試) 既有 RPC（見 `docs/API.md` children 段）；不新增 migration。
3. 建立家庭 onboarding 的接續行為、管理入口位置、切換方式——皆依核可稿呈現的結構實作；稿上沒有的行為不做。
4. 刪除寶貝走後端軟刪（30 天可還原），UI 文案照稿；不做硬刪。

## 驗收條件

- [ ] 三畫面與核可稿逐 frame 對照一致（QA 模擬器視覺驗收，含深色與 AX3）
- [ ] 建檔→列表→編輯→軟刪→（還原路徑若稿內有）全流程在模擬器可完成
- [ ] children RPC 呼叫皆有 wire 層測試；狀態管理有單元測試；既有測試全綠
- [ ] 所有可點元件 ≥44pt（[LS-95](https://linear.app/little-sprout-app/issue/LS-95/harness44pt-點擊目標機械-gate-ui-票-qa-前以-accessibility-frame-量測所有) 未落地前人工量測記錄於 QA）
- [ ] PR body 含 `Design: <frame 名稱>` 行（CI Design gate）

## 派工前置（orchestrator 注意）

設計稿讀取需 Pen——[LS-111](https://linear.app/little-sprout-app/issue/LS-111/設計-chore07-邀請家人-角色選擇列一般成員只能看白話說明選中態ls-46-定稿只有單一產生鈕ls-107-以既有-token-暫拼) visual-review R3 在飛期間 Pen 被占用，待其收工後再派（Ready 不停留的例外＝§5-b Pen 共用條款）。

## 留言（5）

### 2026-08-31T09:01:53.970Z · Chih-Lin Yeh · `d0d24c9b`

## Handoff — LS-113 多寶貝 profile 三畫面實作

**Ticket**：LS-113（依 LS-67 核可稿：寶貝建檔／管理／切換三畫面）

**已完成**：
- 資料層：`ChildModels`／`ChildAPIClient`／`SupabaseChildAPIClient`（對接 LS-66 的 `list_children`／`create_child`／`update_child`／`set_child_deleted` 四支 RPC＋`family_members` 角色查詢）／`ChildrenStore`（`@Observable` 狀態機，隨 app 存活）。
- `BirthdayFormat`：處理 `children.birthday`（Postgres `date`）與 `DatePicker`（裝置本地時區）之間的轉換，避免跨時區位移一天。
- `ChildFilterLayout`：寶貝切換器分段／下拉的**寬度**判斷純函式（依 LS-67 R3 F9 定案，非數量判斷），AX3 強制下拉。
- 四個畫面：`CreateChildView`（08／08-iPad）、`ChildrenManagementView`（09／09 空狀態／09-iPad 簡化 master-detail）、`EditChildView`＋`DeleteChildSheet`（09b／09c）、`ChildFilterBar`（10／10b，掛在 `TimelineView` 頂部）。
- 串接：建立家庭成功後 `.fullScreenCover` 接上 08（可跳過，`FamilyStore.showsChildOnboarding`）；`ChildrenStore` 登出一併 reset。

**已驗證（怎麼驗的）**：
- 單元測試 302 個全綠（本票新增／擴充約 90 個：`ChildrenStoreTests`／`SupabaseChildAPIClientTests`／`BirthdayFormatTests`／`ChildFilterLayoutTests`／`ChildAvatarInitialTests`／`FamilyStoreTests` 新增 4 個）；SwiftLint `--strict` 乾淨；push gate 通過。
- 模擬器手動全流程（本機 `supabase start`，dedicated 機 `LS-113-iPhone17Pro`）：Email OTP 登入→建立家庭→onboarding 建立寶貝（含日期選擇器）→09 列表→09b 編輯→09c 軟刪確認→09 已移除揭露展開→點列還原（全綠）；10 時間軸切換器分段／下拉／AX3 強制下拉皆驗證；深色模式驗證印相罩白邊未隨深色反轉。過程中用模擬器手動量測抓到並修正兩個真實 bug（見下）。合併 `origin/development`（LS-108 加入路徑）後重跑一次建立家庭＋onboarding 跳過／不跳過兩條路徑，確認合併沒有破壞既有流程。

**未完成（informational 處置）**：
本票尚未經過 merge-reviewer／QA，以下是我（ios-dev）自己在實作與手動驗證過程中發現、且已在 PR body「風險」欄位詳細記錄的範圍取捨與已知簡化，供 merge-reviewer 複核：
1. 09b compact 版稿面本身沒有「移除這個寶貝」按鈕（R3/R4 修版後的實際 frame 只有 Save/Cancel）——已依 09-iPad 既有語彙加回，見 PR 風險欄第一條。
2. 大頭貼上傳非本票範圍（LS-67 設計註記 E3 明講），09-iPad「最近的照片」區塊同理未實作。
3. 10c（發佈時選擇歸屬寶貝）不在票文 Scope 內，未實作。
4. 09-iPad 為簡化版 master-detail（重用 EditChildView，含它自己的取消鈕），非逐像素比照稿面。
5. ChildFilterBar 下拉選單為自訂 popover，`TimelineView` 本身仍是 placeholder，視覺細節未逐像素核對。

**風險**：無 migration；無新增 race condition 面（各動作各自 in-flight guard）。詳見 PR body。

**產出位置**：
- Head SHA：`9975ca9`（feature/LS-113-multi-baby，4 commits：3 個 feat + 1 個 merge origin/development）
- PR：https://github.com/CLYEH/little-sprout/pull/189（feature/LS-113-multi-baby → development）
- 模擬器已關：`B2C4455B-5AA8-44B5-B344-9236BDFEE432`（LS-113-iPhone17Pro，本次任務期間開關多次，最終已 shutdown）

之後 orchestrator 派 merge-reviewer。push 後不等 CI，如上。

### 2026-08-31T09:08:12.053Z · Chih-Lin Yeh · `c3cb4a34`

## merge-review R1 — APPROVE

審的 head：`9975ca9ff4b5e2875382608c308c6a2835fdf0a2`（= PR #189 當下 headRefOid，已比對一致）。base：`origin/development`（merge-base `1a12762`，含 LS-108）。

### 1. 合併衝突解決（最高優先）— 乾淨
`git show --cc 9975ca9` 只看手解 hunk，四個衝突檔全部是 LS-108＋LS-113 的**正確聯集**，無吞功能／無漏接／無重複：
- `LittleSproutApp.swift`：`childrenStore`（LS-113）＋`pendingInviteCode`／`.onOpenURL`（LS-108）都在；`RootView(...)` 同時傳兩者。
- `RootView.swift`：`childrenStore` 一路串到 `AuthenticatedRootView`，`pendingInviteCode` 一路串到 `ForkView`，兩條互不干擾。
- `FamilyStore.swift`：新增 `showsChildOnboarding`（LS-113）與 join-request 狀態塊（LS-108）並存；`reset()` 同時含 `showsChildOnboarding = false` 與 `resetJoinRequestsState()`。
- `project.pbxproj`：兩邊 file list 聯集；兩個刪除都保留（LS-113 刪 `ChildrenView.swift`、LS-108 刪 `JoinFamilyPlaceholderView.swift`）。
- 全樹無 conflict marker 殘留。**CI `ci` job 的「XcodeGen 漂移檢查」green** → 提交的 pbxproj 與 `project.yml` 逐字一致（孤兒／重複 entry 會讓此檢查失敗），機械證實 pbxproj 手解正確。

### 2. Race / Scope
- `ChildrenStore` `@MainActor @Observable`，四動作各有 in-flight guard，登出 `reset()`（SettingsView 已接線，mirror `familyStore.reset()`）。`refresh()` 用 `async let` 併發 `listChildren`＋`fetchMyRole`（平行優化到位）。TabView 下 Timeline／Management 同 `.task(id:)` 併發呼叫時，第二次被 `guard !listState.isSubmitting` 早退，無雙查／無競態。`reloadChildrenList()` 與 `refresh()` 同在 MainActor 串行、最後寫者勝，最差是短暫舊清單自我校正——非 blocker。
- Scope 精準：children 資料層＋四畫面＋Support＋`ChildrenRoute`＋`ChildAvatarView`＋`accent-soft` token＋onboarding 串接（FamilyStore/RootView/LittleSproutApp/SettingsView/TimelineView/刪 ChildrenView placeholder）＋測試。無夾帶無關重構；`PreviewChildAPIClient` `#if DEBUG` 隔離。

### 3. 兩個 bug 修法 — 確認達標
- avatarURL nil encode：`CreateChildParams`／`UpdateChildParams` 手寫 `encode(to:)` 用 `encode`（非 `encodeIfPresent`），nil 送明確 JSON `null`。測試斷言**同時**驗 `keys.contains("p_avatar_url")` 與 `is NSNull`，並註解說明為何只斷言 `as? String == nil` 測不到（key 缺席也會過）。create/update 兩路都有。修法對症、測試驗到位（Rule 8）。
- 44pt：文字鈕 `controlPaddingTap`(9.5)→`controlPaddingMedium`(15.5)，body ~20pt＋15.5×2 ≈ 51pt ≥ 44；與 AuthButtons/LabeledTextField/OTPCodeField/InviteCodeField 既有慣例一致。ChildFilterBar segment 另修「`.frame(maxWidth:.infinity)` 綁 Button 而非 label 內部」的縮寬 bug，理由正確。

### 4. 範圍取捨判定 — 合理
- 09b compact 加回「移除這個寶貝」（僅 owner）：稿面 R3/R4 只剩 Save/Cancel，但無此鈕則 iPhone 完全沒有刪除路徑、無法滿足驗收「建檔→…→軟刪→還原全流程」；沿用 09-iPad 既有 danger 文字鈕語彙。合理。
- 09-iPad 簡化 master-detail 重用 EditChildView：合理簡化，已記風險欄。
- 兩者稿面逐像素忠實度屬 QA。

### 5/6. 品質閘 — 全綠於 9975ca9
`gh run 33375597265`（headSha 確認 = 9975ca9）：`ci` pass（含 XcodeGen 漂移檢查＋Build & Test）、`db` pass、`lint`(SwiftLint) pass、`rules` pass。既有測試綠／swiftlint 淨／xcodegen 0 diff 皆機械證實。

### Informational（入 LS-96 待辦池，非阻擋）
- `ChildFilterBar.removedMenuRow`：下拉內點「已移除」列還原後未關 popover（還原列會即時從區段消失，有回饋，非 bug，純 UX 一致性）。
- `reloadChildrenList()` 以 `try?` 吞錯：意圖明確且有註解（動作已成功，僅刷新清單），可接受。

### Verdict：**APPROVE**（無 blocker／major）
即將對 `9975ca9` 貼 `merge-review=success` commit status。

### 2026-08-31T09:09:56.309Z · Chih-Lin Yeh · `50bc5897`

**R1 APPROVE → 已併 development `afca107`（2026-08-31）**：merge-review=success 貼於 head `9975ca9`（R1 comment `c3cb4a34`）；LS-108 合併衝突解決核對乾淨（XcodeGen 漂移綠證實 pbxproj 正確）、race/scope/兩 bug 修法全過。i1/i2 入 LS-96（comment `cf669f7f`）。→ QA。

**晉升暫緩（orchestrator 記錄）**：LS-108 的 qa 正在 test tip `1a12762` 驗收中，現在 promote dev→test 會讓其 qa status SHA 錯位。序列：LS-108 QA 裁決 →（PASS 則 promote test→main 釋出 LS-108）→ promote dev→test 帶入 LS-113＋LS-109 → 派 qa（LS-113 三畫面視覺驗收對照 LS-67 核可稿＋LS-109 鍵盤留白＋09b/09-iPad/切換器逐 frame）。稿面逐 frame 忠實度是本票 QA 主體。worktree LS-113 待併後清。

### 2026-08-31T10:59:57.061Z · Chih-Lin Yeh · `07f4af97`

## QA R1 首驗 — PASS

**環境**：worktree `qa-test` @ `73814e4`（= origin/test tip）；模擬器 `qa-LS108-109-113-iPhone17Pro`（iOS 26.5）。**Pen 路徑**：`get_app_state` 初次回報 active 為主 checkout（`/Users/clyeh/little-sprout/design/littlesprout.pen`），與 worktree 不符 → 用 `pen-open.sh` 強制切到 `.claude/worktrees/qa-test/design/littlesprout.pen`，`get_app_state` 確認節點數＝**3644**（符合預期）後才開始比對。

### 驗收條件逐條

**✓ 三畫面全流程在模擬器可完成**：建立家庭 → onboarding 建檔（Xiao An，含跳過選項「之後再說」存在）→ 09 管理列表（4 個孩子）→ 09b 編輯（改名／改生日）→ 09c 軟刪確認 → 已移除的寶貝（1）展開 → 點擊還原 → 回到列表。全程操作順暢無卡點。

**✓ 逐 frame 視覺對照 LS-67 核可稿**（含深色＋AX3）：
| 畫面 | 一般/淺 | 深色 | AX3 |
|---|---|---|---|
| 08 建檔 | ✓ 一致 | ✓ 一致 | — |
| 09 管理列表 | ✓ 一致（含空狀態按鈕/文案結構） | ✓ 一致 | ✓ 一致，長名字換行不裁切 |
| 09b 編輯 | ✓ 一致 | — | ✓ 一致，捲動後底部按鈕不重疊 |
| 09c 軟刪確認 | ✓ 一致 | — | ✓ 一致（文案於 AX3 截斷但按鈕仍可辨識可點） |
| 10 切換器（≤3，segmented） | ✓ 一致 | ✓ 一致 | — |
| 10b 下拉（>3） | ✓ 一致（含已移除項標記） | — | ✓ 一致（10 AX3 強制下拉，長名字換行不裁切） |

**✓ 可點元件 ≥44pt 抽測**（LS-95 落地前人工量測）：09 列表 row 94pt／編輯儲存 57pt／取消 51pt／移除這個寶貝 51pt／已移除展開列 51pt／還原列 59pt／09c 移除按鈕 50pt／取消 49pt——全數 ≥44pt，未見設計稿原有的 39pt 問題複現。

**✓ children RPC 抽測（SQL 唯讀路徑確認軟刪/還原）**：
```sql
-- 軟刪：set_child_deleted(id, true) → deleted_at/deleted_by 正確寫入
-- 還原：set_child_deleted(id, false) → deleted_at/deleted_by 正確清空
```
兩次呼叫皆用 owner JWT context 驗證，結果與 UI 操作一致。

### Informational（不影響本輪裁決，供後續處置）
1. **09b 編輯畫面多了設計稿沒有的「移除這個寶貝」按鈕**——核可稿 `BqxHJ`（09b）Footer 只定義 Save/Cancel 兩個按鈕，無刪除入口；`gpSsR`（09 列表）的 Row 也只有 Identity+Chevron，無 swipe 或圖示。推測稿上沒畫出「軟刪從哪裡觸發」，實作者在編輯頁補了必要入口才能讓 09c（稿內確實存在）可被觸發，屬功能上必要、且未破壞既有版面。建議記入 LS-96 待辦池或另開設計票補齊 09/09b 的刪除入口稿面，避免「稿上沒有的行為」爭議。
2. **10c 發佈·選擇歸屬寶貝 未能驗證**——目前時間軸／相簿頁尚無「新增日記/照片」的 compose 入口（無 FAB／發佈流程 UI），屬 LS-85/86/87 範圍尚未接上 UI。10c 是 LS-67 稿內 frame，但不在本票（LS-113：建檔／管理／切換）驗收條件明列的三畫面內，故標記 **⊘ 需其他票銜接後才能驗**，不計入本輪 FAIL。已將 10c 設計稿單獨截圖存證，供該功能實作時對照。

### 證據
`.claude/evidence/LS-113/qa1/`：`09-list-light.png`／`09-list-dark.png`／`09-list-ax3-light.png`／`09b-edit-light.png`／`09b-edit-ax3-light-top.png`／`09b-edit-ax3-light-bottom.png`／`09c-confirm-light.png`／`09c-confirm-ax3-light.png`／`09-removed-expanded-light.png`／`10-switcher-dark.png`／`10b-dropdown-4children-light.png`／`10-dropdown-ax3-light.png`

**裁決：PASS**

### 2026-08-31T11:05:40.883Z · Chih-Lin Yeh · `fe3de920`

**收尾完成 → Done(2026-08-31,v0.6.0)**:QA PASS(三畫面全流程＋逐 frame 對照 LS-67 核可稿含深色/AX3＋44pt 抽測＋軟刪還原 SQL 驗證,comment `07f4af97`);批次 sweeper CLEAN(ChildrenView 刪除零殘留、全部新符號有呼叫端)。QA informational(q2 09b 移除鈕稿面回填)已入 LS-96。

**Retro**:模擬器實測抓到兩個靜態審查抓不到的真 bug(nil avatar encode 省 key→404、39pt tap target)——與 LS-107 同一教訓,實跑驗證是 ui 票不可省的環節;與 LS-108 的合併衝突手解經 merge-review 用 `git show --cc` 逐 hunk 核對＋XcodeGen 漂移綠雙重驗證,「大衝突手解必須被機械+人工雙驗」的做法值得沿用。使用者 2026-08-31 核可設計到功能上 main,單日完成。

