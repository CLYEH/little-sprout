# LS-154 Harness：擋 agent 寫入主 checkout——PreToolUse hook 對 Write／Edit 路徑與 Bash 重導／cp／tee 目標在 repo 根但不在 .claude/worktrees 下的呼叫拒絕（LS-96 f22f0645 P1 升票）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-03T12:03:31.465Z |
| 開始 | 2026-09-03T12:13:40.148Z |
| 完成 | 2026-09-04T02:33:03.336Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-154/harness擋-agent-寫入主-checkout-pretooluse-hook-對-writeedit-路徑與-bash |
| 關係 | related→LS-141, related→LS-73, related→LS-114, related→LS-104, related→LS-88, related→LS-144, related→LS-96, related→LS-143, related→LS-79, related→LS-149, related←LS-156, related←LS-157, related←LS-145 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 待辦池 `f22f0645`（P1）：2026-09-03 同日兩起——[LS-143](https://linear.app/little-sprout-app/issue/LS-143/taskls-24-後端-刪除帳號-rpc唯一-owner-須先轉移成員內容軟刪併發無死鎖) agent 自報曾把檔案寫進主 checkout、[LS-149](https://linear.app/little-sprout-app/issue/LS-149/taskls-23-後端-檢舉封鎖owner-移除內容-rpccontent-reportsblocked-users) agent 把 `supabase/migrations/20260903091313_notification_kind_report.sql` 留在主 checkout untracked（巡檢抓到、令搬回 worktree）。規約（CLAUDE.md「一張 ticket＝一個 worktree」、COLLABORATION §3）只靠派工 prompt 約束，沒有機械 gate；主 checkout 常駐 `main`，任何寫入都會污染 promote／harness 流程。

## 範圍

1. `.claude/hooks/`（現有 PreToolUse hook 所在，[LS-88](https://linear.app/little-sprout-app/issue/LS-88/harnessclaude-code-hooks-集縮減版-三支-fail-closed-pretooluse-hook禁-no)／[LS-79](https://linear.app/little-sprout-app/issue/LS-79/harnesscyclesprint機制-每週規劃一次核可lane-補位只取當前-cyclehook-擋無-cycle-的派工巡檢對帳) 同型）加規則：
   * **Write／Edit／MultiEdit／NotebookEdit**：`file_path` 解析為絕對路徑後，若在 repo 根 `/Users/clyeh/little-sprout/` 之下、且不在 `.claude/worktrees/<x>/` 之下 → 擋，訊息指出「主 checkout 禁寫，請在自己的 worktree 作業；harness 改動亦走 hotfix worktree」。白名單：`.claude/patrol-state.json`（[LS-144](https://linear.app/little-sprout-app/issue/LS-144/harness巡檢加開票責任機械提醒-lane-在飛-0-且無可派候補時動作清單印-開票並列來源story-拆票ls-96-p1-升票) 產物，由腳本寫非 tool 寫，實際不經 hook）、無其他。
   * **Bash**：命令文字含把輸出導向主 checkout 路徑的樣式——`>`／`>>`／`tee`／`cp`／`mv`／`install`／`rsync` 其目標為 `/Users/clyeh/little-sprout/<非 .claude/worktrees>` 絕對路徑，或以 `cd /Users/clyeh/little-sprout &&` 開頭後接寫檔——擋；`git -C /Users/clyeh/little-sprout pull|fetch|tag|worktree|status|log|diff|show|rev-parse`、`bash scripts/ops/*.sh`、`bash scripts/gates/*.sh`（從主 checkout 跑 gate 是規約）不擋。相對路徑無法判定的以「當前 cwd＝主 checkout」推定（hook 可取得 cwd）。
   * 例外開關：環境變數或 hook 設定 `LS_ALLOW_MAIN_CHECKOUT_WRITE=1` 只給 orchestrator 明確需要時（例如 memory 檔不在 repo 內，不需開關）——預設不開。
2. 自測 `.claude/hooks/*.test.sh`：正樣本（worktree 內 Write、scratchpad Write、memory 目錄 Write、`git -C` 唯讀、`bash scripts/ops/promote.sh`）全放行；負樣本（主 checkout Write／Edit、`tee /Users/clyeh/little-sprout/x`、`cp a /Users/clyeh/little-sprout/supabase/migrations/`、`cd /Users/clyeh/little-sprout && echo > f`）全擋；mutation（拿掉路徑判斷 → 負樣本綠）紅。掛 CI `rules` job。
3. `docs/COLLABORATION.md` §3 worktree 規約補「機械面」一句、§7 對照表加一列；CLAUDE.md 不動。
4. 部署：hook 檔在 repo 內（`.claude/hooks/`）走 hotfix PR → main → back-merge；使用者 settings 不動（hook 註冊若需改 `.claude/settings.json` 的 matcher，只加不刪）。

## 不做

改 agent 定義；擋 orchestrator 對 repo 外路徑的寫入；Pen（.pen 經 MCP 寫入不經此 hook）。

## 驗收

- [ ] 自測正負樣本全過並掛 CI `rules`
- [ ] 實跑：從一個 subagent 嘗試 Write 主 checkout 檔案被擋、同檔在 worktree 內放行（附截圖或 hook 輸出）
- [ ] merge-reviewer 通過（誤擋面：`git -C` 唯讀、gate 腳本、promote／cleanup 腳本不得被擋）
- [ ] [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `f22f0645` 銷除註記本票

## 留言（16）

### 2026-09-03T12:03:44.391Z · Chih-Lin Yeh · `9d5e5eb6`

orchestrator 2026-09-03 20:03：開票責任——LS-144 併 main 後 harness lane 即將在飛 0，從 LS-96 P1 池項 `f22f0645`（agent 寫入主 checkout，同日兩起）升票；Ready、cycle 1、size:S、High。LS-144 back-merge 併入＋Done 後派 general-purpose（hotfix 自 main c03465d）。

### 2026-09-03T12:14:10.048Z · Chih-Lin Yeh · `d44d0756`

orchestrator 2026-09-03 20:13：LS-144 Done → harness lane 在飛 0 → In Progress、派 general-purpose（hotfix 自 main `c03465d`，worktree `.claude/worktrees/LS-154`）。判定用 git 語意（`--git-common-dir`＝`--git-dir` ⇒ 主 checkout）不寫死路徑；Write／Edit／MultiEdit／NotebookEdit 路徑＋Bash 寫入樣式；開關 `LS_ALLOW_MAIN_CHECKOUT_WRITE=1`；自測正負 ≥6＋mutation 掛 CI `rules`；§3／§7。

### 2026-09-03T12:44:06.147Z · Chih-Lin Yeh · `b2249a68`

orchestrator 2026-09-03 20:43：第三起事故——LS-145 agent 自述兩次用錯絕對路徑把 `.github/workflows/ci.yml` 編進主 checkout（`git status` 自查後還原，未 staged）。本票 guard 的 Edit 路徑判定正是這個形狀：`Edit` 的 `file_path` 在主 checkout（`--git-common-dir`＝`--git-dir`）→ 擋。請把此案列為自測負樣本之一（`Edit /Users/clyeh/little-sprout/.github/workflows/ci.yml`）。

### 2026-09-03T12:44:15.429Z · Chih-Lin Yeh · `915d642a`

general-purpose 2026-09-03 handoff

**Ticket**：LS-154（LS-96 `f22f0645` P1 升票）— PR **#258** `hotfix/LS-154-main-checkout-write-guard` → `main`，head **`b575223`**（自 main `c03465d`）。CI `ci`／`db`／`lint`／`rules` 四項皆 pass；`merge-review` 待派。

**已完成**
1. `scripts/hooks/main-checkout-guard.sh`（bash wrapper，fail-closed trap）＋`scripts/hooks/main_checkout_guard.py`（規則 W0–W5）。**獨立一支、不併進 pretool.sh**（pretool 是 H1–H3 分派、190 組自測已成型；本票需要 git 子行程與路徑解析，分開放各自 header／test／CI 行），前處理 `import pretool_engine`（heredoc 剝除／引號感知斷詞／`$(...)`／透明前綴詞表）不重寫 tokenizer。放在 `scripts/hooks/`（票文寫 `.claude/hooks/`，repo 沒有該目錄，依 LS-88／LS-104 現有結構）。
   * 主 checkout **用 git 語意判定、不寫死路徑**：目標最近存在的祖先目錄 `git rev-parse --path-format=absolute --git-dir --git-common-dir` 相等 ⇔ 主 working tree；只管 `$CLAUDE_PROJECT_DIR`（缺則 cwd）所在 repo，其他 repo 主 checkout 不擋。
   * W1 Write／Edit／MultiEdit／NotebookEdit 路徑（相對以 cwd 解析）；W2 Bash 重導 `>`／`>>`／`N>`／`&>`／`>|`（獨立或黏連）、`tee`／`touch`／`mkdir`／`rm`／`rmdir`／`truncate` 全部位置參數、`cp`／`mv`／`install`／`rsync`／`ln` 目的地（含 `-t`）、`sed`／`perl -i` 檔案參數，相對路徑沿命令追蹤 `cd`／`pushd`（`cd <root> && echo > f` 擋；cwd 主 checkout 時相對目標一律擋）；W3 `git [-C dir] <sub>` 改 working tree 的子命令（add／am／apply／checkout／cherry-pick／clean／commit／merge／mv／rebase／reset／restore／revert／rm／switch／stash 除 list／show）；W4 直譯器 `-c`／`-e`／heredoc／`<<<` 提到主 checkout 絕對路徑（讀寫不分一律擋），shell `-c`／heredoc／`$(...)`／反引號遞迴；W5 無法斷詞時字面兜底；W0 stdin 空／JSON 壞／python3／git 缺席／未預期例外皆 deny。
   * 白名單只有兩處 gitignore 本機狀態：`.claude/patrol-state.json`（票文）、**`.claude/memory/`（實測發現 `~/.claude/projects/<proj>/memory` 是指向主 checkout `.claude/memory/` 的 symlink，realpath 落在主 checkout——票文「memory 不在 repo 內」與實況不符；不加會擋掉 orchestrator 寫 memory）**。
   * 開關 `LS_ALLOW_MAIN_CHECKOUT_WRITE=1`：hook 行程環境變數整支放行；Bash 命令文字以該賦值開頭放行該次（Write／Edit 只認環境變數）；皆 stderr 註明；預設關。
   * deny：stdout deny JSON＋stderr「W<n>：主 checkout 禁寫（…）——請在自己的 worktree（.claude/worktrees/LS-<n>）作業；harness 改動走 hotfix worktree（COLLABORATION §2 Worktree 規約／§7）；明確需要時 LS_ALLOW_MAIN_CHECKOUT_WRITE=1 放行」。**引 §2 非票文的 §3**：Worktree 規約實際在 §2「分支模型」，§3 是命名規約。
2. `.claude/settings.json` PreToolUse 新增獨立一條 `Bash|Write|Edit|MultiEdit|NotebookEdit` → `main-checkout-guard.sh || exit 2`；既有兩條與 permissions 不動。
3. `scripts/hooks/main-checkout-guard.test.sh`（`mktemp -d` 臨時 repo＋linked worktree＋memory symlink＋另一 repo，不依賴真實路徑）掛 CI `rules` job Gate 自測 step。
4. `docs/COLLABORATION.md` §2 Worktree 規約補機械面一句、§7 加一列（含盲區：hook 只看 tool 呼叫，MCP／其他 tool 寫檔不經此 hook）。CLAUDE.md 不動。

**已驗證**
* 自測全綠：正樣本 28、負樣本 31、開關 4、盲區註記 1、fail-closed 5（空 stdin／壞 JSON／無 python3／引擎檔缺席／wrapper 中途 exit 1 由 trap 轉 deny）、**mutation 2**（`return git_dir == common_dir` → `return False` 後主 checkout Write 與 tee 負樣本變綠）、settings 接線 3。`pretool.test.sh` 仍全綠；jq 驗 settings.json、yaml 驗 ci.yml。
* **實跑等價驗證**（真實路徑直接餵 hook JSON，`CLAUDE_PROJECT_DIR=/Users/clyeh/little-sprout`）：Write `/Users/clyeh/little-sprout/LS-154-probe.txt` → exit 2 W1；同檔在 worktree LS-154 → exit 0；Edit 主 checkout `docs/COLLABORATION.md` → exit 2；Write scratchpad／memory → exit 0；Bash `cp … /Users/clyeh/little-sprout/supabase/migrations/` → exit 2 W2；`git -C root pull`／`bash scripts/ops/promote.sh` → exit 0；`echo hi > LS-154-probe.txt`（cwd 主 checkout）→ exit 2；worktree 內 `git commit` → exit 0。效能 35–41 ms／次（timeout 10 s）。
* **未用 Write 真跑主 checkout**：hook 註冊讀主 checkout 的 settings.json，併 main 後才生效，本 session 真寫會違反本票要擋的規則 → **待併 main 後由 orchestrator `/hooks` 重載（或重啟）實跑一次**：Write `/Users/clyeh/little-sprout/LS-154-probe.txt` 應被擋、同檔在 worktree 放行。
* CI：#258 `ci`／`db`／`lint`／`rules` pass（run 33756257386）。

**未完成**
* reviewer informational 處置：**無**（merge-review 尚未派）。
* 併 main 後：orchestrator 實跑（上述）、back-merge `main`→`development`、LS-96 `f22f0645` 銷除註記。

**風險（誤擋面與繞法）**
* cwd 主 checkout 時**相對路徑寫入**一律擋（票文要的 fail-closed）→ 用 worktree 絕對路徑或 scratchpad。
* `rm`／`touch`／`mkdir` 主 checkout 任何路徑擋，**含 gitignore 的 `.claude/evidence/`**——§7 既有規約說 evidence 是 worktree 相對，故刻意不加白名單（主 checkout 現有 LS-114／119／120 三個 evidence 目錄是此前違規殘留）；要放的話 `ALLOWLIST_PREFIX` 加一項。**請 orchestrator 裁定**。
* `mv` 雜散檔從主 checkout 搬回 worktree → 放行（只查目的地）；`rm` 雜散檔 → 擋，用 `LS_ALLOW_MAIN_CHECKOUT_WRITE=1 rm …`（命令開頭賦值）放行該次。
* W4 直譯器內**讀**主 checkout 檔案也擋（讀寫不分）→ 改 Read／cat。
* W3 是 scope 判斷（票文只列「不擋」白名單，未明說擋 `git -C root checkout`）——採「改 working tree 子命令黑名單」而非嚴格白名單以免 `ls-files`／`remote`／`stash list` 誤擋；reviewer 若認為超範圍可單獨砍。
* 漏擋（§7 盲區）：MCP／非 tool 寫檔；`$VAR` 目標放行＋stderr 註記；暫存腳本／`xargs`／`find -exec`／`eval`／`sudo -u`；`mv` 來源不查；hook 未重載不生效（同 pretool）。
* race／migration／相容性：無。

**產出位置**
* PR https://github.com/CLYEH/little-sprout/pull/258（commits `5f48fcf` feat／`7e9283a` test／`b575223` docs）
* worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-154`
* 檔案：`scripts/hooks/main-checkout-guard.sh`、`scripts/hooks/main_checkout_guard.py`、`scripts/hooks/main-checkout-guard.test.sh`、`.claude/settings.json`、`.github/workflows/ci.yml`、`docs/COLLABORATION.md`
* 暫存：scratchpad `LS-154-pr-body.md`、`LS-154-realpath-verify.sh`／`LS-154-verify-output.txt`（等價驗證原始輸出）

### 2026-09-03T23:47:06.748Z · Chih-Lin Yeh · `9a3ed2ae`

orchestrator 2026-09-04 07:47：handoff `915d642a` 收到（agent 昨晚 22:xx 被用量上限中斷，PR 已開）——PR **#258** head `b575223`（獨立 `scripts/hooks/main-checkout-guard.sh`＋`main_checkout_guard.py` W0–W5、git 語意判定、白名單 `.claude/patrol-state.json`＋`.claude/memory/`（實測 memory 是 symlink 進主 checkout）、開關 `LS_ALLOW_MAIN_CHECKOUT_WRITE=1`、settings 加一條、自測正 28／負 31／mutation 2、實跑等價驗證）；CI 四綠 → In Review、派 merge-review R1（重點：**誤擋面**——orchestrator 主 checkout 的 `git pull`／`tag`／`worktree add`、`promote.sh`／`cleanup-merged.sh`、`python3 - <<EOF` 對 memory symlink、`sed -n` 純讀相對路徑；W0 python3 缺席 deny 全停的取捨）。**裁定**：`.claude/evidence/` 不加白名單（§7 走 worktree 相對，主 checkout 三個殘留目錄另清）；W3 git 黑名單保留與否由 reviewer 依誤擋面裁定。併 main 後 orchestrator 用 Write 主 checkout probe 實跑。

### 2026-09-03T23:56:51.810Z · Chih-Lin Yeh · `94035c1a`

## merge-review R1 — PR #258（head `b575223`）

**裁決：APPROVE**（0 blocker／0 major；2 minor＋2 informational）

審的 head `b575223f5d4b8080356c1f76ac0bc02c6f3378b5`，`gh pr view 258 --json headRefOid` 當下同值（審查期間 head 未前進）。base `c03465d`，diff 6 檔 +851/-2。

### 怎麼驗的

不靠讀碼下結論——對**真實路徑**餵 hook JSON 實跑 **121 組探針**（4 批，`CLAUDE_PROJECT_DIR=/Users/clyeh/little-sprout`），全部符合預期、0 mismatch（除下方 minor 2 項）：

* 批 1（38 組）：W1 四種寫檔 tool＋orchestrator 日常操作誤擋面
* 批 2（41 組）：W2／W3／W4／W5 正負樣本＋開關
* 批 3（29 組）：env 變體、`/tmp`→`/private/tmp` 與 memory symlink、`GIT_DIR`、`CLAUDE_PROJECT_DIR` 三種取值、盲區
* 批 4（13 組）：設計／QA／發布路徑＋效能

另跑：shipped `main-checkout-guard.test.sh` 全綠（67 個 `expect`＋mutation 2＋settings 接線 3，4.5 s）、`pretool.test.sh` 仍全綠、`ci.yml` yaml 可解析、`pr-body-check.sh --verify` 通過（含 LS-96 申報反查）、CI `ci`／`db`／`lint`／`rules` 四綠。

### 逐項查實

**1. 主 checkout 判定（`main_checkout_guard.py:120-155`）** — 正確。實測 `git rev-parse --path-format=absolute --git-dir --git-common-dir`：主 checkout 兩者皆 `/Users/clyeh/little-sprout/.git`（相等）、worktree 為 `.git/worktrees/LS-154` vs `.git`（不等）。
* **symlink**：`readlink ~/.claude/projects/-Users-clyeh-little-sprout/memory` → `/Users/clyeh/little-sprout/.claude/memory`，handoff 的實測屬實。`hits()` 的 `os.path.realpath(abspath)`（`:335`）把它折回主 checkout 後命中 `ALLOWLIST_PREFIX` → 放行（實測 exit 0）。`/tmp` vs `/private/tmp` 亦正確。
* **`CLAUDE_PROJECT_DIR` 三種取值**實測皆正確：主 checkout（orchestrator 形狀）／worktree／未設。**subagent 的 `CLAUDE_PROJECT_DIR` 若仍是主 checkout，判定仍正確**——`project` 存的是 `common_dir`（`:317`），主 checkout 與其 linked worktree 共用同一個 common dir，故 worktree 目標先過 repo 同一性檢查、再被 `is_main_checkout` 判否 → 放行。實測「`CLAUDE_PROJECT_DIR`=主 checkout、cwd=worktree、寫 worktree」exit 0、「寫主 checkout」exit 2。
* **submodule**：本 repo 無（無 `.gitmodules`）；`git -C <主> submodule update` 不在 `GIT_MUTATING` → exit 0。
* **`GIT_DIR`**：見 minor 1。

**2. W1–W5 規則** — 逐條實跑通過。
* W1 四 tool 全覆蓋（含 `NotebookEdit` 的 `notebook_path`，`:495`）；`Read` 不在 matcher → exit 0。
* W2 重導（`>`／`>>`／`N>`／`&>`／`>|`／黏連）、`tee`／`touch`／`mkdir`／`rm`／`rmdir`／`truncate`／`cp`／`mv`／`install`／`rsync`／`ln`／`cp -t`／`sed -i`／`perl -i` 全數正確；`cd`／`pushd` 追蹤實測正確（`cd <root> && echo > f` 擋、`cd <worktree> && echo > f` 放、`cd <wt> && cd ../../.. && echo > f` 擋）。`2>/dev/null`、`>&2` 不誤觸（`:381-382` 的 `&` 早退）。
* W3 黑名單實測正確且**誤擋面乾淨**：`pull` ／`fetch`／`tag`／`worktree add`／`status`／`log`／`diff`／`rev-parse`／`branch`／`push`／`stash list`／`submodule` 全 exit 0；`checkout`／`reset --hard`／`commit`／`restore`／`stash`／`apply`／`clean`／`cherry-pick`／`switch`／`merge` 對主 checkout exit 2、對 worktree exit 0。
* **W4 誤擋面——票文點名的兩個形狀實測皆放行**：
  * `F=<memory>/MEMORY.md; python3 - "$F" <<'EOF' … EOF` → **exit 0**（`$F` 命中 `UNRESOLVABLE_RE`，`:162`）。
  * heredoc 內文**字面**寫 memory 絕對路徑 → **exit 0**（`hits()` 走同一份 allowlist，`:336`）。
  * `sed -n '/…/p' docs/COLLABORATION.md`（相對、cwd 主 checkout、純讀）→ **exit 0**（`INPLACE_RE` 要求 `-i`，`-n` 不匹配，`:85/:408`）。
  * 三者皆非誤擋，**不列 finding**。W4 讀寫不分確實會擋純讀（`python3 -c "open('/Users/clyeh/little-sprout/…')"`），但那是票文要的寧嚴勿鬆，且 `cat`／`grep`／`sed -n`／`jq`／`ls` 全部天然不命中（實測），逃生成本低——列入誤擋面清單、不算問題。
* W5 字面兜底、W0 fail-closed 5 條（空 stdin／壞 JSON／無 python3／引擎缺席／wrapper 中途 exit 1 由 trap 轉 deny）皆由 shipped 自測覆蓋並通過。**python3 缺席 → deny 判斷正確**，見 informational 2。

**3. 白名單** — 正確。`.claude/patrol-state.json`、`.claude/memory/` 實測放行；`git check-ignore -v` 確認兩者與 `.claude/evidence/` 皆已 gitignore。`.claude/evidence/` **未加**，符合 orchestrator 裁定；主 checkout 現存 `LS-114`／`LS-119`／`LS-120` 三個 evidence 目錄確認為殘留。實測 `node scripts/design/overflow-scan.js … > design/evidence/…`：cwd=worktree exit 0、cwd=主 checkout exit 2——與 §7「evidence 走 worktree 相對」一致。

**4. 開關** — `LS_ALLOW_MAIN_CHECKOUT_WRITE=1` env 形式（Bash 與 Write 皆）與命令開頭賦值形式實測放行；`true; LS_ALLOW…=1 rm …`（非開頭）實測仍擋。
**繞法寫明**：`INLINE_SWITCH_RE`（`:93`）只錨在命令最前，故 `LS_ALLOW_MAIN_CHECKOUT_WRITE=1 true; rm <主 checkout 檔>` 實測 exit 0——整條命令被放行。**這不是安全邊界**，是給 orchestrator 的逃生門（agent 本來就能直接用開關），列此僅為書面完整。

**5. `.claude/settings.json`** — diff **只加不刪**（`git diff | grep '^-'` 除檔頭外空），`permissions` 為 `{}`、未動。新條 `Bash|Write|Edit|MultiEdit|NotebookEdit` → `bash "$CLAUDE_PROJECT_DIR/scripts/hooks/main-checkout-guard.sh" || exit 2`，與既有 `pretool.sh`／`linear-issue-check.sh` 兩條**字串形狀完全一致**（`$CLAUDE_PROJECT_DIR` 相對＋`|| exit 2`），符合 §10 慣例。subagent 與主 session 同一 project dir → 讀主 checkout 的 settings、解析到主 checkout 的 `scripts/hooks/`（併 main 後才存在，handoff 已標為待實跑）。

**6. 自測** — shipped 全綠。**我自己另做的 2 支 mutation 皆被 shipped суite 殺掉**（＝這兩個機制有真實覆蓋、不是巧合過關）：
* M-A 拿掉 W2 的 `cd` 追蹤（`:366` 不更新 `cur`）→ shipped `P18`（cd 進 worktree 後相對寫檔）與 `N10`（`cd 主 checkout && echo > f`）**雙向變紅**。
* M-B 拿掉 `hits()` 的 `realpath`（`:335`）→ shipped `P4`（memory symlink）／`P4b`（`.claude/memory/` 直路徑）／`P7`（patrol-state）變紅。
* CI `rules` 掛載已核（`ci.yml:195`，且 step 標題清單同步更新）。
* **效能**：`main-checkout-guard.sh` 36.7 ms/次（15 次平均），既有 `pretool.sh` 45.1 ms/次——本 hook 比既有那支還輕，每次 Bash 總開銷 ~82 ms，可接受。

**7. 文件／scope**
* §2 落點正確——`awk` 核出 `Worktree 規約（平行分工）` 位於 `## 2. 分支模型`，handoff「票文寫 §3、實際在 §2」的更正屬實，deny 訊息與 §7 列亦引 §2。
* §7 新增一列，盲區段誠實（MCP 寫檔、hook 未重載、`$VAR`、暫存腳本／`xargs`／`find -exec`、`mv` 來源不查、另一 repo 主 checkout）。實測驗證盲區為真：`echo <主路徑> | xargs rm`、`find … -exec rm {} ;`、`echo x > $HOME/../little-sprout/y.txt` **皆 exit 0**——文件沒有虛報覆蓋。
* PR body `pr-body-check.sh --verify` 通過；CI 四綠。
* **scope 裁定：W3 保留，不算超範圍**。票文列的是「`git … pull|fetch|tag|worktree|status|log|diff|show|rev-parse` **不擋**」——列出不擋清單本身即蘊含其餘 git 子命令在考慮範圍內；且 `git -C <主> checkout/reset/stash` 直接改主 checkout working tree，正是票文「主 checkout 常駐 main，任何寫入都會污染 promote／harness 流程」要防的同一類傷害。採黑名單而非白名單也避開 `ls-files`／`remote`／`stash list` 誤擋（實測皆放行）。diff 其餘部分無順手改動、無無關重構。

---

### minor 1 — `GIT_DIR` 等 git 環境變數會污染判定，令**所有 worktree** 誤判成主 checkout（PLAUSIBLE）

`scripts/hooks/main_checkout_guard.py:133-136`

```python
r = subprocess.run(
    ["git", "-C", d, "rev-parse", "--path-format=absolute", "--git-dir", "--git-common-dir"],
    capture_output=True, text=True, timeout=5,
)
```

子行程完整繼承 hook 行程的環境。`GIT_DIR` 一旦存在，`--git-dir` 與 `--git-common-dir` 對**任何** `-C` 目錄都回傳同一個值 → `is_main_checkout` 恆真 → 每個 linked worktree 都被判成主 checkout。

**具體失敗情境（已實測）**：`GIT_DIR=/Users/clyeh/little-sprout/.git` 時，`Write /Users/clyeh/little-sprout/.claude/worktrees/LS-154/f.txt` → **exit 2**（正確應為 0），deny 理由 `W1：主 checkout 禁寫`。同一環境下每個 agent 在自己 worktree 的每次 Write／Edit／Bash 寫入全被擋＝**產線全停**，且錯誤訊息叫人「請在自己的 worktree 作業」——人正在 worktree 裡，無從自解。

**為何仍是 minor 而非 major**：觸發需要 `GIT_DIR` 出現在 hook 環境。PreToolUse hook 由 Claude Code 啟動（非 git），環境繼承自啟動 claude 的 shell；我無法證明本機實際會出現，故標 **PLAUSIBLE**。

**但本 repo 有同型前科**，值得一修——`scripts/gates/push-gate.sh:7-11`：

> `# LS-73：pre-push hook 由 git 啟動時會 export GIT_DIR／GIT_WORK_TREE／GIT_INDEX_FILE（linked worktree 指向 .git/worktrees/<n>）。…「fatal: unable to read tree <sha>」（LS-46 merge 帶進新相依時 100% 重現…）`
> `unset GIT_DIR GIT_WORK_TREE GIT_INDEX_FILE GIT_PREFIX`

亦即「git 環境變數洩進 gate 腳本」在本 repo 已咬過一次，且已有既定慣例修法。本檔沒跟上該慣例（§10 conformance）。

**建議修法**（1 行、無其他行為變動）：`subprocess.run(..., env={k: v for k, v in os.environ.items() if not k.startswith("GIT_")})`，或在 `main()` 開頭 `for k in ("GIT_DIR","GIT_WORK_TREE","GIT_COMMON_DIR","GIT_INDEX_FILE","GIT_PREFIX"): os.environ.pop(k, None)`。自測加一組：`GIT_DIR=<fixture main>/.git` 下 worktree Write 必須 exit 0。

### minor 2 — 逃生門的 `env VAR=1 cmd` 標準形式被擋，且 deny 訊息未說明位置限制

`scripts/hooks/main_checkout_guard.py:93`（`INLINE_SWITCH_RE = r"^\s*LS_ALLOW_MAIN_CHECKOUT_WRITE=1(?:\s|$)"`）＋ `:510-511`（deny 訊息尾句）

**具體失敗情境（已實測）**：`env LS_ALLOW_MAIN_CHECKOUT_WRITE=1 rm /Users/clyeh/little-sprout/stray.sql` → **exit 2**。deny 訊息只說「明確需要時 `LS_ALLOW_MAIN_CHECKOUT_WRITE=1` 放行」，未說「必須置於命令最前、且不能經 `env`」。照訊息提示改用 POSIX 慣用的 `env VAR=1 cmd`、或寫成 `cd x && LS_ALLOW…=1 rm …`，會拿到**一字不差的同一則 deny** → 使用者／agent 陷入重試迴圈。這正是 LS-149 型事故的收尾動作（清主 checkout 雜散檔）最可能踩到的地方。

**建議修法**（擇一）：(a) deny 訊息尾句改為「明確需要時把 `LS_ALLOW_MAIN_CHECKOUT_WRITE=1` **放在整條命令最前面**放行該次」；(b) 更好——`find_cmd`（`:202-221`）本來就會跳過 `env` 與 `VAR=val` 前綴，讓 inline 開關比照認「命令位置前的賦值串」即可，兩種形式一致。(a) 成本近零，建議至少做 (a)。

### informational 1 — orchestrator 指定的 LS-145 具名負樣本未落成 test case

comment `b2249a68` 要求把 `Edit /Users/clyeh/little-sprout/.github/workflows/ci.yml` 列為自測負樣本。該 comment 建立時間比 handoff 早 **9 秒**，實作方應為未見。

`main-checkout-guard.test.sh:115` 的 `N2 Edit 主 checkout 既有檔案`（`$main/docs/a.md`）走的是**完全相同的規則路徑**（W1 → `file_path` → `hits`），`.github/workflows/` 沒有任何路徑特異性；我另以真實路徑實跑 `Edit /Users/clyeh/little-sprout/.github/workflows/ci.yml` → **exit 2**。**無行為缺口**，僅缺具名樣本，補一行即可（不擋併入）。

### informational 2 — python3 缺席 → deny 的曝險面比 LS-88 大一級（建議維持現狀）

`scripts/hooks/main-checkout-guard.sh:51`。與 `pretool.sh:139` 同一慣例（fail-closed），**方向正確、應維持**。差別只在 matcher：`pretool.sh` 是 `Bash|Read|Grep`，本 hook 多了 `Write|Edit|MultiEdit|NotebookEdit`——python3 消失時，原本就已經「所有 Bash 全擋」（產線已停），現在額外連寫檔 tool 也擋。**不構成新的失效類別**，且 fail-open 會讓 gate 在最需要時靜默失效，與 §「前饋必有反饋」相悖。**建議不改**。

---

## 誤擋面清單（正常操作會被擋者＋繞法）

以下**全部經實測確認會被擋**。除第 5 項外皆為票文有意的 fail-closed，列此供 orchestrator 預期管理：

| # | 被擋的正常操作 | 規則 | 繞法 |
|---|---|---|---|
| 1 | cwd＝主 checkout 時**任何相對路徑寫入**（`> f`／`tee f`／`rm f`／`mkdir d`／`touch f`） | W2 | 改 worktree 絕對路徑或 scratchpad |
| 2 | `mkdir -p .claude/evidence/…`、`node … > design/evidence/…`（cwd＝主 checkout） | W2 | 依 §7 在 worktree 內做（orchestrator 已裁定不加白名單） |
| 3 | `rm` 主 checkout 雜散檔（＝清理 LS-149 型事故本身） | W2 | `LS_ALLOW_MAIN_CHECKOUT_WRITE=1 rm …`（**必須置於最前**，見 minor 2） |
| 4 | `git -C <主> checkout／reset／stash／merge／restore／switch／clean／apply／cherry-pick／commit／add` — 含 §2「**本地合併版 back-merge**」（把 main merge 進臨時分支）若在主 checkout 執行 | W3 | 在 worktree 做；正規 back-merge `gh pr create --head main --base development` **不受影響**（實測 exit 0） |
| 5 | `env LS_ALLOW_MAIN_CHECKOUT_WRITE=1 <cmd>`、`cd x && LS_ALLOW…=1 <cmd>` | — | **minor 2**，建議修訊息 |
| 6 | 直譯器內文提到主 checkout 絕對路徑，**即使純讀**（`python3 -c "open('/Users/clyeh/little-sprout/…')"`、`node -e`、heredoc） | W4 | 改 Read／`cat`／`grep`／`sed -n`／`jq`（實測全放行） |
| 7 | `mkdir -p` 到**尚未存在**的 worktree 目錄（最近存在祖先是主 checkout 的 `.claude/worktrees`） | W2 | `git worktree add` 不受影響（實測 exit 0） |
| 8 | `Edit` 主 checkout 的 `.claude/settings.json`／`CLAUDE.md`／`docs/`（harness 檔） | W1 | 走 hotfix worktree（＝票文本意）；`/hooks` 指令非 tool 呼叫、不經此 hook |

**實測確認不誤擋（全 exit 0）**：`git pull --ff-only`／`fetch`／`tag`／`worktree add`／`status`／`log`／`diff`／`rev-parse`／`branch -a`／`push`／`stash list`／`submodule update`；`promote.sh`／`cleanup-merged.sh`／`post-status.sh`／`patrol.sh`／`supabase-lock.sh`／`pen-open.sh`／`scripts/gates/*.sh`；`gh pr merge`／`create`／`view`／`api`；`xcodegen`；`supabase db push`；`xcrun simctl shutdown`；`open -a Pen`；`cat`／`grep -rn`／`ls`／`wc`／`jq`／`sed -n`（相對與絕對）；`2>/dev/null`／`>&2`；**`python3 - "$F" <<'EOF'`（`$F`＝memory 路徑）**；**heredoc 內文字面寫 memory 絕對路徑**；Write／Edit memory symlink 路徑與 `.claude/memory/` 直路徑；`.claude/patrol-state.json`；worktree 內一切寫入與 `git commit`／`sed -i`／`push`；scratchpad／`/tmp`／`mktemp -d`；`chmod`；`&&` 串接的唯讀鏈。

---

## 未審／限制（誠實聲明）

* **未實跑「hook 真正載入後」的端到端行為**——本 PR 未併 main，`.claude/settings.json` 尚未生效；我的驗證是**直接餵 hook JSON 的等價驗證**（與 handoff 同法，但用真實路徑、121 組、含 handoff 未涵蓋的 env／`GIT_DIR`／`CLAUDE_PROJECT_DIR` 變體）。handoff 已列「併 main 後由 orchestrator `/hooks` 重載實跑一次」為未完成項，**該項仍未完成，併入後務必補**。
* Race condition／運算效能／平行優化三維度：本票為單次同步 hook 判定、無並發、無 DB／RLS／SwiftUI 面，**無可審之處**（唯一效能面即上述 36.7 ms，已量）。
* 未審 `pretool_engine.py` 本體（LS-104 既有、本 PR 未動），只驗其被 import 的介面在本 hook 下行為正確。

## 建議處置

minor 1／minor 2／informational 1 皆非併入阻礙。建議：**minor 1（`GIT_DIR` unset，有 LS-73 前科＋既定慣例）與 minor 2（deny 訊息一句話）併入前順手修**，成本各 1 行；若 orchestrator 判定即刻併入，則三項一併記入 LS-96 待辦池（minor 1 建議 P1——失效模式是產線全停且訊息誤導）。


### 2026-09-03T23:58:11.801Z · Chih-Lin Yeh · `3ef76a58`

orchestrator 2026-09-04 07:57：merge-review R1 `94035c1a` **APPROVE**（誤擋面：真實路徑 121 探針，orchestrator 日常操作全放行，`python3 - <<EOF` 對 memory symlink／`sed -n` 純讀皆 exit 0；被擋 8 類皆票文有意 fail-closed；W3 保留；效能 36.7 ms）。2 minor 我裁定**併 main 前修**（hook 誤判＝產線全停）：`GIT_DIR` 等四變數污染 subprocess 致所有 worktree 誤判（比照 LS-73 `push-gate.sh:7-11` unset）；逃生門不認 `env VAR=1` 前綴＋deny 訊息未說位置 → 令 agent R2 兩行修＋負樣本 → 快審 → 併 main → back-merge → 我用 Write 主 checkout probe 實跑。

### 2026-09-04T00:09:25.767Z · Chih-Lin Yeh · `128d6bcf`

general-purpose 2026-09-04 R2 handoff

**Ticket**：LS-154 — PR **#258** head **`838582b`**（R1 `b575223` → fix `c0a15d9` → test `838582b`）。CI `ci`／`db`／`lint`／`rules` 四綠（run 33820163418）。PR body 已加「R2 處置」表，`pr-body-check.sh --verify` 通過（4 條「已修」SHA 反查在 PR commits 內）。主 checkout `git status` clean；worktree `scripts/hooks/` 無 `__pycache__`。

**已完成（merge-review R1 `94035c1a` 處置）**
| 項目 | 處置 |
|---|---|
| minor 1 `GIT_DIR` 等污染 subprocess → 所有 worktree 誤判 | 已修 `c0a15d9`：`GIT_ENV_POLLUTERS=(GIT_DIR, GIT_WORK_TREE, GIT_INDEX_FILE, GIT_PREFIX, GIT_COMMON_DIR)`＋`_clean_env()`，`subprocess.run(..., env=_clean_env())`（比照 `push-gate.sh` LS-73）。樣本 G1（`GIT_DIR`＋`GIT_WORK_TREE` 下 worktree Write 放行）／G2（同環境主 checkout 仍擋）／G3（`GIT_COMMON_DIR`／`GIT_INDEX_FILE`／`GIT_PREFIX` 下 worktree Bash 重導放行）＋**mutation M3**（`env=_clean_env()`→`env=None` 後 G1 變紅）已修 `838582b` |
| minor 2 逃生門不認 `env VAR=1 cmd`、訊息未說位置 | 已修 `c0a15d9`：`INLINE_SWITCH_RE` 改認命令位置前的任意 `VAR=val`／`env` 串（`LS_ALLOW…=1 cmd`／`env LS_ALLOW…=1 cmd`／`FOO=1 env LS_ALLOW…=1 cmd` 皆可；`cd x && LS_ALLOW…=1 cmd` 不算）；deny 訊息尾句改為「明確需要時以 `LS_ALLOW_MAIN_CHECKOUT_WRITE=1 <cmd>` 或 `env LS_ALLOW_MAIN_CHECKOUT_WRITE=1 <cmd>` 開頭（整條命令最前面；Write／Edit 只認環境變數）放行該次」。樣本 S5／S6 放行、S7 `cd x &&` 之後不放行＋S7b 訊息含兩種形式與位置 已修 `838582b` |
| informational 1 LS-145 具名負樣本（comment `b2249a68`） | 已修 `838582b`：N2b `Edit <主 checkout>/.github/workflows/ci.yml` → deny |
| informational 2 python3 缺席 → deny 曝險面 | **不修**：reviewer 建議維持；fail-open 會讓 gate 在最需要時靜默失效（§0），python3 消失時 pretool.sh 已擋所有 Bash、產線本就停，非新失效類別 |
| 自查（非 reviewer 所列） | `import pretool_engine` 會在 `scripts/hooks/` 留 `__pycache__`——reviewer 跑自測時已在 worktree 出現（07:48），併 main 後 hook 每次執行都會讓主 checkout dirty（連 `cleanup-merged.sh` 的 dirty 判定都會受影響）。已修 `c0a15d9` `sys.dont_write_bytecode = True`（import 前設）；樣本 T1（複製到暫存目錄跑一次、斷言無 `__pycache__/`）已修 `838582b` |

docs/COLLABORATION.md §7 列同步（開關兩種形式與位置限制、環境衛生、自測計數）併在 `c0a15d9`。

**已驗證**
* `main-checkout-guard.test.sh` 全綠：正 28／負 32／開關 7／盲區 1／fail-closed 5／git 環境變數 3／`__pycache__` 1／mutation 3／接線 3；`pretool.test.sh` 仍全綠。
* 修後直接跑 worktree 那份 hook 一次 → `scripts/hooks/` 無 `__pycache__`；主 checkout `git status` clean。
* CI 四綠（run 33820163418）。

**未完成**
* reviewer informational 處置：如上表（已修 ×3、不修 ×1）；無入池項。
* 併 main 後：orchestrator `/hooks` 重載（或重啟）→ Write `/Users/clyeh/little-sprout/LS-154-probe.txt` 實跑應被擋、同檔在 worktree 放行；back-merge `main`→`development`；LS-96 `f22f0645` 銷除註記。

**風險**
* 逃生門仍非安全邊界（R1 已寫明）：`LS_ALLOW…=1 true; rm <主 checkout 檔>` 整條放行——給 orchestrator 的逃生門，agent 本可直接用開關。
* 剝除 `GIT_*` 只影響本 hook 的 rev-parse 子行程，不動 hook 行程本身的環境。
* 其餘同 R1 handoff（誤擋面／盲區）。

**產出位置**：PR https://github.com/CLYEH/little-sprout/pull/258（`c0a15d9` fix／`838582b` test）；worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-154`；scratchpad `LS-154-pr-body.md`（含 R2 處置表）。

### 2026-09-04T00:09:58.070Z · Chih-Lin Yeh · `7028966c`

orchestrator 2026-09-04 08:09：R2 handoff `128d6bcf` 收到——head `838582b`（`_clean_env()` 去 GIT_* 五變數＋G1–G3／M3；逃生門接受 `env`／`VAR=val` 前綴鏈＋deny 訊息說明位置；N2b 負樣本；`sys.dont_write_bytecode` 避免 hook 每次在主 checkout 留 `__pycache__`；自測 正 28／負 32／開關 7／git-env 3／pycache 1／mutation 3）；CI 四綠。**使用者指示本週用量將盡、逐步收工，不再派新任務**——merge-review R2、併 main、back-merge、`/hooks` 重載＋Write probe 實跑、LS-96 `f22f0645` 銷除註記，留待下一 session（見交接文件）。

### 2026-09-04T01:57:53.986Z · Chih-Lin Yeh · `49f929e5`

orchestrator 2026-09-04 09:57：新 session 接手，派 **merge-review R2**（PR #258 head `838582b`，delta `b575223..838582b`：`_clean_env()` 剝 GIT_* 五變數、逃生門認 env／`VAR=val` 前綴鏈＋deny 訊息說明位置、`sys.dont_write_bytecode`、N2b／G1–G3／M3／T1 樣本）。要求：handoff 每條「已修」`git show` 查實、三修改點各做獨立實測＋至少 2 組 handoff 未列邊界、實跑兩支自測、貼 `merge-review` status 到 `838582b`。APPROVE 後：併 main → back-merge `main`→`development` → `/hooks` 重載 → Write 主 checkout probe 實跑應被擋 → LS-96 `f22f0645` 銷除註記 → 收尾。

### 2026-09-04T02:08:16.981Z · Chih-Lin Yeh · `47ab021a`

## merge-review R2 — PR #258（head `838582b`）

**裁決：APPROVE**（0 blocker／0 major；1 minor(PLAUSIBLE)／3 informational）

審的 head `838582be016a508a37b374dceb2e36575cbe689d`；`gh pr view 258 --json headRefOid` 當下同值（審查期間 head 未前進）。只審 delta `b575223..838582b`（3 檔 +89/−7：`main_checkout_guard.py`、`main-checkout-guard.test.sh`、`docs/COLLABORATION.md`），R1 `94035c1a` 已審面不重審，但下方 E 組回歸探針確認 delta 未破壞 R1 已驗行為。作業於 worktree `.claude/worktrees/LS-154`，只讀不改。

### handoff `128d6bcf` 逐條查實（4 條「已修」全部對得上 SHA）

| 申報 | 查實 | 結果 |
|---|---|---|
| minor 1 `GIT_*` 剝除 已修 `c0a15d9` | `git show c0a15d9:…py`：`GIT_ENV_POLLUTERS`（`:108`）／`def _clean_env`（`:112`）／`env=_clean_env(),`（`:159`）皆在；`b575223` 同名 grep 皆 0 | ✅ 屬實 |
| G1–G3＋M3 樣本 已修 `838582b` | `838582b:…test.sh` 有 G1／G2／G3／M3；`c0a15d9` 該檔皆 0（fix 與 test 分兩 commit，與申報一致） | ✅ 屬實 |
| minor 2 逃生門＋訊息 已修 `c0a15d9` | `INLINE_SWITCH_RE`（`:105-106`）改寫、deny 訊息（`:536`）、allow stderr 措辭（`:527`）皆在 `c0a15d9`；`b575223` 無「整條命令最前面」 | ✅ 屬實 |
| S5／S6／S7／S7b 樣本 已修 `838582b` | 四條皆在 `838582b`、`c0a15d9` 為 0 | ✅ 屬實 |
| informational 1（LS-145 具名負樣本）已修 `838582b` | `test.sh:121` N2b `Edit <main>/.github/workflows/ci.yml` → deny；fixture 亦補建該檔 | ✅ 屬實 |
| informational 2 **不修** | R1 原文即「**建議不改**」（fail-open 會讓 gate 在最需要時靜默失效）——處置與 reviewer 建議一致 | ✅ 合理 |
| 自查 `__pycache__` 已修 `c0a15d9`＋T1 `838582b` | `sys.dont_write_bytecode = True`（`:68`）在 `import pretool_engine`（`:70`）之前；T1 在 `838582b` | ✅ 屬實 |
| **「無入池項」** | 4 條 R1 項目全部有明確處置（已修 ×3、不修 ×1），無宣稱入池者 → LS-96 無需對帳 | ✅ 一致 |
| 引據來源查證 | `.gitignore:48` 只有 `design-canvas*/__pycache__/`（repo 根**未** ignore）＋`cleanup-merged.sh:4/70/74` 明載 dirty 判定用 plain `git status --porcelain`（LS-141，不帶 `--ignored`）⇒ handoff「留了會把主 checkout 弄 dirty、連 `cleanup-merged.sh` 的 dirty 判定都受影響」**成立**，非誇大 | ✅ 屬實 |
| §7 列與程式碼一致 | 該列新寫的「兩種形式＋位置限制」「剝除五變數」「`sys.dont_write_bytecode`」「負 32／開關 7／git 環境變數 3／pycache 1／mutation 3」逐項對得上原始碼與實跑計數（計數採「情境」粒度、非 assertion 粒度，沿用此前「開關 4 對 5 個 ✓」的同一慣例，非虛報） | ✅ 一致 |

### 實跑（不只讀碼）

**shipped 自測**（worktree 內）：`main-checkout-guard.test.sh` → **exit 0，87 ✓／0 ✗**（P28／N32／S9／G3／T1／M3／R3／B2／F5）；`pretool.test.sh` → **exit 0，139 ✓／0 ✗**。

**三個修改點各自的獨立實測**（真實路徑餵 hook JSON，`CLAUDE_PROJECT_DIR=/Users/clyeh/little-sprout`，共 57 組，1 組不如預期＝下方 N1）：

* **(a) git 環境變數**：`GIT_DIR`／`GIT_WORK_TREE`／`GIT_COMMON_DIR`／`GIT_INDEX_FILE`／`GIT_PREFIX` **各自單獨**設定 × 4 種形狀（worktree Write／主 checkout Write／worktree Bash 重導／主 checkout Bash 重導）＝20 組全部正確；另加五變數齊發、`GIT_DIR` 指向不存在路徑、`GIT_DIR` 下 W3 `git -C <主> checkout` 仍擋 → 皆正確。
* **(b) 逃生門**：`env SW=1 cmd`／`FOO=1 env SW=1 cmd`／`SW=1 cmd` → exit 0；`cd /tmp && SW=1 cmd`／`true; SW=1 cmd`／`SW=0 cmd`／`foo=SW=1 cmd`（假前綴）→ exit 2。deny 訊息實得含**三要素**：`` `LS_ALLOW_MAIN_CHECKOUT_WRITE=1 <cmd>` ``、`` `env LS_ALLOW_MAIN_CHECKOUT_WRITE=1 <cmd>` ``、「整條命令最前面」。
* **(c) `__pycache__`**：把 hook 三檔複製到 `mktemp -d` 跑三次（Bash／Write／tee 三種 payload）→ 目錄內**無** `__pycache__/`；同法跑 `b575223` 版本 → **有** `__pycache__/`。修法確實生效、且對照組證明不是巧合。
* **(d) 回歸（R1 已驗行為，delta 未破壞）**：worktree Write／主 checkout Write／`Edit .github/workflows/ci.yml`／memory symlink／`patrol-state.json`／`git -C <主> pull`／`promote.sh`／scratchpad／`Read` 不在 matcher —— 9 組全部與 R1 結論一致。

**`_clean_env()` 單元探針**（handoff 未列的邊界之一）：`os.environ` 63 → clean 62，剝除清單實得 `['GIT_DIR']`（只剝真的存在的污染變數）；`PATH`／`HOME`／自訂 `FOO_MARKER`／`GIT_SSH_COMMAND`／`GITHUB_TOKEN` **全部保留** → 無誤剝非目標變數，也不影響 rev-parse。

**效能**：交錯測量（old/new 交替各 40 次、取中位數，避開單向連測的暖機偏差）——`b575223`（暖 pycache）**39.9 ms**、`838582b`**43.5 ms**，代價 **+3.6 ms／次（約 9%）**，可接受。（先做的單向測量一度出現 80 vs 47 ms 的假象，交錯重測後不成立，故**不列為 finding**。若日後在意這 3.6 ms，`sys.pycache_prefix` 指到 repo 外可同時保住快取與乾淨。）

**CI**：`gh pr checks 258` → `ci`／`db`／`lint`／`rules` 四綠（run 33820163418），與 handoff 相符。**主 checkout 與 worktree `git status --porcelain` 皆空**（我的探針未留任何殘留）。

---

### N1 — minor（PLAUSIBLE）：`GIT_*` 剝除只收 5 個具名變數，`GIT_OBJECT_DIRECTORY` 仍能把整支 gate 靜默關掉

`scripts/hooks/main_checkout_guard.py:108`（`GIT_ENV_POLLUTERS`）＋`:162-164`（`"not a git repository"` → `res = None` → 放行）

**具體失敗情境（已實測）**：`GIT_OBJECT_DIRECTORY` 指向**不存在**的路徑時，`git -C <任何目錄> rev-parse --path-format=absolute --git-dir --git-common-dir` 回 `fatal: not a git repository`（rc=128；git 2.47.0，實測——objects 目錄不存在會讓 `is_git_directory()` 判否，因此連主 checkout 都探不到 repo）。`repo_dirs` 把這個 stderr 歸類成「不在 repo 內」→ 回 `None` → `hits()` 判否 → **放行**。實測 `GIT_OBJECT_DIRECTORY=/tmp/nonexist-objs` 下 `Write /Users/clyeh/little-sprout/LS-154-probe.txt` → **exit 0，stderr 一個字都沒有**。也就是整支 guard 在該環境下無聲失效（`W0` 的 fail-closed 兜不到這條，因為它不是「rev-parse 失敗」而是「被判成不是 repo」）。

**為何仍是 minor 且標 PLAUSIBLE**：(1) 觸發條件與 R1 minor 1 同一機率級距——需要該變數出現在 hook 行程環境，而 git 只在 receive-pack quarantine 這類情境設它、且那時指向**存在**的目錄；我無法證明本機會出現。(2) 行為在 `b575223` 完全相同（old=new 實測皆 exit 0）＝**非本 delta 引入**。

**但仍列為 delta 的 finding**：本 delta 的標的正是「git 環境變數污染 rev-parse」這一類，R1 建議的修法 `env = {k: v for k, v in os.environ.items() if not k.startswith("GIT_")}` 本可一併涵蓋，實作收窄成 5 個具名變數才留下這個成員；而且**方向比 minor 1 更糟**——minor 1 是吵鬧的 fail-closed（產線停、看得見），這條是無聲的 fail-open（gate 關了沒人知道）。

**建議修法**：`GIT_ENV_POLLUTERS` 改回 `GIT_` 前綴全剝（對 `rev-parse` 這種純本地、無網路的子命令，剝掉 `GIT_CONFIG_*`／`GIT_SSH_COMMAND` 等只會更乾淨）；另外 `repo_dirs` 回 `None` 時補一行 stderr 註記，讓「gate 判定目標不在 repo 內」這個狀態至少可觀測。自測補一組：`GIT_OBJECT_DIRECTORY=<不存在路徑>` 下主 checkout Write 必須 exit 2。**不擋併入**——建議記入 LS-96（P2；失效模式是無聲關閉，但觸發面窄）。

### informational 1 — `find_cmd` 對「帶引號值的前置賦值」與「帶旗標的透明前綴」不認，W2 靜默失效（pre-existing，非 delta 引入）

`scripts/hooks/main_checkout_guard.py:237`／`:241`（`if not hq and E.ASSIGN_RE.match(text)` ／ `if not hq and base in E.TRANSPARENT`，承 `pretool_engine.py:544/565` 慣例）

**實測（new 與 old 完全相同，皆放行、皆無 stderr）**：`FOO="ab" rm <主 checkout 檔>` → exit 0；`FOO='a b' rm …` → exit 0；`FOO="a b" tee <主>` → exit 0；`env -i rm <主>` → exit 0；`nice rm <主>` → exit 0。（對照：`FOO=ab rm …`／`FOO=1 rm …`／`env rm …`／`nohup rm …` 皆正確 deny；`FOO="a b" echo hi > <主>` 也仍 deny——重導走另一條規則。）根因是 token 帶引號時 `hq=True`，既不算賦值也不算透明前綴，於是被當成「命令名」，後續參數就不再過寫入規則。

`env -i`／`nice` 屬 §7 盲區已收的「`sudo -u` 這類帶參數的前綴詞」；**「引號值賦值前綴」不在既有盲區文字內**。因 old=new，**非本 delta 引入、R1 已審過該面**，故列 informational。建議記入 LS-96：`find_cmd` 解出的 command basename 若自身像賦值（`^[A-Za-z_][A-Za-z0-9_]*=`）或以 `-` 開頭，至少走 `note()` 出一行 stderr（現在是完全無聲），或直接視為判不出而 fail-closed。

### informational 2 — 逃生門「整條命令放行」的語意隨新前綴形式一併擴大（設計必然，非缺陷）

`main_checkout_guard.py:526-528`。實測 `env LS_ALLOW_MAIN_CHECKOUT_WRITE=1 true; rm <主 checkout 檔>` → 新版 exit 0、舊版 exit 2；與 R1 已寫明的 `LS_ALLOW…=1 true; rm …`（新舊皆 exit 0）同一類。既然 R1 已裁定「開關不是安全邊界、是給 orchestrator 的逃生門」，讓 `env` 形式與裸賦值形式行為一致正是 minor 2 要的結果。列此僅為書面完整，**不建議改**。

### informational 3 — `FOO="a b" LS_ALLOW…=1 <cmd>`「能過」是巧合，不是開關生效

`INLINE_SWITCH_RE`（`:105-106`）的 `\S*` 吃不下含空白的引號值：直接對該 regex 求值，`FOO="a b" LS_ALLOW_MAIN_CHECKOUT_WRITE=1 rm x` → **False**（對照 `FOO=1 env …`／`env …`／裸賦值 → 皆 True）。實跑該命令卻 exit 0——因為它掉進 informational 1 的 W2 盲區被放行，**不是**逃生門認了它。也就是說 informational 1 一旦補上，這個形式會立刻變成被擋，而 deny 訊息列出的兩種形式都不涵蓋它。建議與 informational 1 一併處理（若採納，`\S*` 改成能吃引號值，或在訊息裡明說「前綴賦值的值不可含空白」）；自測 S6 目前只覆蓋不帶引號的 `FOO=1 env …`，可順帶補一條。

---

### Scope

delta 3 檔全部可回溯到 R1 兩 minor＋informational 1＋handoff 自查的 `__pycache__`，**無順手改動、無無關重構**：`main_checkout_guard.py` 只動 header 註解、`sys.dont_write_bytecode`、`INLINE_SWITCH_RE`、`GIT_ENV_POLLUTERS`／`_clean_env`、`env=` 參數、兩處訊息字串；`main-checkout-guard.test.sh` 只加 fixture 一檔＋N2b／S5–S7b／G1–G3／M3／T1（`expect` 的可選 env 參數在 `b575223` 就已存在，本輪未改動測試框架）；`docs/COLLABORATION.md` **只改 §7 那一列的 1 行**，內容與程式碼逐項相符。`.claude/settings.json`／`ci.yml`／wrapper `.sh` 本輪未動。

### 四維度

* **Race condition**：本 hook 為單次同步判定、無並發、無共享狀態；`_CACHE` 是行程內 dict，行程間不共用。`sys.dont_write_bytecode` 順帶消除了「多個 hook 行程並發寫同一份 `.pyc`」這條理論上的競態。**無可審之處**。
* **運算效能**：唯一效能面是 `dont_write_bytecode` 的 +3.6 ms（已交錯量測，見上）；`_clean_env()` 每個 distinct 目錄只呼叫一次（`repo_dirs` 有 `_CACHE`，`:152-153`），不是 per-token 成本。**可接受**。
* **平行優化**：不適用（單一 rev-parse 子行程、且有快取）。
* **Scope**：見上，乾淨。

### 未審／限制（誠實聲明）

* **仍未實跑「hook 真正載入後」的端到端行為**——PR 未併 main，`.claude/settings.json` 尚未生效（主 checkout `scripts/hooks/` 目前確實沒有這兩支檔，我已確認）。我的驗證一律是直接餵 hook JSON 的等價驗證。handoff 列的「併 main 後 `/hooks` 重載＋Write probe 實跑」**仍未完成，併入後務必補**。
* R1 已審面（W1–W5 全表、121 組誤擋面、settings 接線、mutation M-A/M-B）本輪未重審，只跑上述 9 組回歸探針＋shipped 全套自測確認未退化。
* 未審 `pretool_engine.py` 本體（本 PR 未動）；informational 1 的根因雖在其慣例，但不在本票 scope。
* 未在 CI 上重跑（沿用 run 33820163418 的四綠，已用 `gh pr checks` 核對）。

---

*merge-reviewer 2026-09-04 10:06 CST*


### 2026-09-04T02:11:23.820Z · Chih-Lin Yeh · `01cdf61e`

orchestrator 2026-09-04 10:11：merge-review R2 `47ab021a` **APPROVE**（handoff 4 條「已修」SHA 全對得上；自測 87／139 全綠；獨立探針 57 組；status 已貼 `838582b`）→ PR #258 併 main **`4d9e44e`** → 主 checkout `git pull` → **hook 已由 settings watcher 自動載入並實跑驗證**：Write `/Users/clyeh/little-sprout/LS-154-probe.txt` 被擋（W1 deny 訊息含兩種逃生門形式與位置說明），檔案未建立；同 session 寫 scratchpad 與 `.claude/memory/` 正常。back-merge PR **#259** `main → development` 已開（純 merge，四綠即併）。R2 N1 minor(PLAUSIBLE)（`GIT_OBJECT_DIRECTORY` 等未列名 GIT_* 變數致 rev-parse 失敗→無聲 fail-open）與 informational 1（`FOO="a b" cmd`／`env -i`／`nice` 前綴靜默放行）入池 LS-96；LS-96 `f22f0645` 已於 `212875c6` 銷除。下一步：#259 併入 → sweeper／retro → Done。

### 2026-09-04T02:18:31.966Z · Chih-Lin Yeh · `a6aa8156`

orchestrator 2026-09-04 10:18：back-merge PR #259 **CONFLICTING**（`.github/workflows/ci.yml` 「Gate 腳本自測」step 名稱：development 側 LS-145 加 `privacy-manifest-check`、main 側本票加 `main-checkout-guard`；run 區塊自動合併成功、`docs/COLLABORATION.md` 乾淨）→ 已關 #259，依 §2 開 `hotfix/LS-154-backmerge-development`（自 development 併 `origin/main`，兩名並列，marker 0、yaml OK、harness 檔與 main 零差異）→ push gate 進行中 → 開新 PR。LS-154 worktree 已 `cleanup-merged.sh --apply` 移除。附帶觀察（入池候選）：hook 對「同一條 Bash 內 `git worktree add` 後 `cd` 進去 `git merge`」會誤擋 W3（目錄尚不存在、最近存在祖先是主 checkout）——拆成兩條命令即可，屬 R1 誤擋面第 7 類的變體。

### 2026-09-04T02:21:51.710Z · Chih-Lin Yeh · `5ff929d5`

orchestrator 2026-09-04 10:21：push gate 過 → back-merge PR **#260** `hotfix/LS-154-backmerge-development → development` 已開（取代 #259；head `79a2e6a`）。CI 四綠＋merge-review status 後併入，之後 sweeper／retro → Done。

### 2026-09-04T02:28:01.226Z · Chih-Lin Yeh · `2a0a96d1`

orchestrator 2026-09-04 10:27：#260 CI 四綠＋merge-review status → 併入 development **`1bb2a68`**（`git rev-list origin/development..origin/main` = 0，`scripts/hooks/`／`.claude/settings.json` 兩邊零差異）。hook 自 10:10 起在本 session 對 orchestrator 生效。派 sweeper 巡 PR #258 六檔 → retro → Done；back-merge worktree 待滿 10 分鐘由 cleanup 清。

### 2026-09-04T02:33:00.674Z · Chih-Lin Yeh · `3d2986fe`

## 收尾儀式（orchestrator，2026-09-04 10:32）

**dead-code sweep**（sweeper 產出、orchestrator 代貼）：範圍 PR #258 六檔 `c03465d..838582b`（933 insertions），主 checkout 唯讀。**未發現本 feature 引入的死碼**：`main_checkout_guard.py` 全部常數／regex／函式／類別逐一 grep 皆有引用點，`GIT_ENV_POLLUTERS`→`_clean_env`→`repo_dirs` 串接完整；import 自 `pretool_engine.py` 的 9 個符號皆存在；wrapper 四函式皆在呼叫路徑（與 `pretool.sh` 同構未共用＝刻意設計，列觀察）；test fixture 目錄與 helper 全部被用到，分類計數 P28／N32／S7／G3／B1／F3 與 handoff 一致；`.claude/settings.json` 指向的檔案存在；`ci.yml` step 名與 run 區塊兩支 `.test.sh` 一致（非 #260 衝突殘留）；COLLABORATION §2／§7 引用的腳本名／環境變數名與程式碼逐字相符。既有觀察：主 checkout `.claude/evidence/LS-114／119／120` 殘留（R1 已點名，gitignored）。盲區：未執行測試（改以 grep 結構比對；實跑證據沿用 R1 121 探針／R2 57 探針／CI 四綠）。

**retro（lesson learning）**
- 做對的：兩輪 review 都對真實路徑餵 hook JSON 實跑（121＋57 組），不靠讀碼；R1 minor 依「hook 誤判＝產線全停」裁定併前修，R2 用 mutation M3 證明 `_clean_env` 有牙齒；併入後 settings watcher 自動載入、Write probe 實跑被擋，端到端閉環。
- 立即見效：本 session 內 orchestrator 自己已被擋兩次（probe、以及一次真實誤操作），符合 §0「前饋必有反饋」。
- harness 缺口（皆入池 LS-96）：`42b7816e` P2 未列名 GIT_* 變數致無聲 fail-open＋P3 帶引號前綴／`env -i`／`nice` 放行；新增兩條 P3——「同一條 Bash 內 worktree add＋cd＋merge」誤擋（誤擋面第 7 類變體）、commit gate 不擋衝突標記（本輪實際踩到、靠 push 前自查抓回）。
- orchestrator 自身：Bash cwd 跨呼叫持久，相對 `cd` 失敗讓 `&&` 後半段沒跑而 `git add`／`commit` 照跑——一律絕對路徑／`git -C`（已寫 notes）。
- 清理：LS-154 worktree 與 `LS-154-backmerge` worktree／本機分支皆 `cleanup-merged.sh --apply` 移除；遠端分支由 GitHub 自動刪。

**狀態 → Done**（main `4d9e44e`、development `1bb2a68`；hook 生效中）。

