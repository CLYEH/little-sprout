# LS-204 Chore：後端測試與文件債清理（LS-96 池項四條：`107_album_summaries.sql` §6 效能斷言與措辭、`run.sh` evidence 檔頭 SubPlan 判準、107／50 共用 51200 列 fixture、`106_eula_consent.sql` 場景 6 還原值改讀暫存表）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Low |
| 標籤 | size:S, lane:backend |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M3 相簿與上傳 |
| 估點 | — |
| 建立 | 2026-09-05T11:10:17.502Z |
| 開始 | 2026-09-05T11:10:40.589Z |
| 完成 | 2026-09-05T18:02:16.731Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-204/chore後端測試與文件債清理ls-96-池項四條107-album-summariessql-6-效能斷言與措辭runsh |
| 關係 | related→LS-167, related→LS-202, related→LS-164, related→LS-22, related→LS-58, related→LS-144, related→LS-200, related→LS-197, related→LS-96, related→LS-182, related→LS-198, related←LS-255, related←LS-203, related←LS-227, related←LS-217, related←LS-225, related←LS-223, related←LS-222, related←LS-213, related←LS-205, related←LS-207, related←LS-201, related←LS-206 |

## 描述

## 來源

巡檢 2026-09-05 19:08（[LS-144](https://linear.app/little-sprout-app/issue/LS-144/harness巡檢加開票責任機械提醒-lane-在飛-0-且無可派候補時動作清單印-開票並列來源story-拆票ls-96-p1-升票) 開票責任）：lane:backend 連續空 2 輪、Backlog 無 backend 候補（[LS-22](https://linear.app/little-sprout-app/issue/LS-22/story留言愛心與彙總推播) 後端 [LS-58](https://linear.app/little-sprout-app/issue/LS-58/taskls-22-後端-commentsreactions-寫入-rpcfeed-整合device-tokens-推播基礎rls-測試)／172／175／182 全 Done，Phase 1 後端路線圖無剩餘機械候選）。依 [LS-182](https://linear.app/little-sprout-app/issue/LS-182/chorels-22-後端-推播通知停權測試與文件債清理ls-96-池項六條stub-410-注入入口claim-race)／[LS-198](https://linear.app/little-sprout-app/issue/LS-198/harness-chorels-96-池項清倉五件-收據-n-支殘句統一指回-overflow-scanjs-檔頭7-處pr-body) 前例，把 [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池中純後端測試／文件債合成一張 S（實估 <1 小時）清倉票；四條皆為 merge-reviewer informational、已記錄處置為「下次動該檔順修」。

## 範圍（每條一個 commit，commit 訊息引用池項 id）

1. [LS-200](https://linear.app/little-sprout-app/issue/LS-200/taskls-20-後端-相簿摘要-view-album-summariessecurity-invokervisible-media) **R2 i-A（**`37d45fd1`**）** `supabase/tests/107_album_summaries.sql` §6 效能段：50 本相簿下規劃器走 `albums_cover_idx`＋top-N（LATERAL loops=50），3000 本才走 `albums_family_created_idx`＋Incremental Sort；現有斷言與 fixture 規模無關仍綠、註解「能讓規劃器真的用上」措辭過強。二擇一：加大 fixture 到能區分兩條路徑並加「LATERAL loops ≤ limit+1」斷言；或改措辭如實描述。mutation：把 view 的 LATERAL 改成每列子查詢要能紅。
2. [LS-200](https://linear.app/little-sprout-app/issue/LS-200/taskls-20-後端-相簿摘要-view-album-summariessecurity-invokervisible-media) **R2 i-C（**`37d45fd1`**）** `supabase/tests/run.sh` evidence 檔頭補 R2 新增的 SubPlan／偵測器自證判準說明（與 §6 斷言對齊）。
3. [LS-200](https://linear.app/little-sprout-app/issue/LS-200/taskls-20-後端-相簿摘要-view-album-summariessecurity-invokervisible-media) **R1 i3（**`ca99c6ae`**）** 107 §6 的 51200 列效能 fixture 與 `50_rls_plan_no_percall_subquery.sql` 重複 → 抽成共用 fixture（沿 `supabase/tests/` 既有 helper 慣例），兩支測試皆改用；兩支結果不變。
4. [LS-197](https://linear.app/little-sprout-app/issue/LS-197/taskls-23-後端-eula-同意紀錄app-settingseula-version-當前版本profileseula) **R2 i3（**`2acb1d3b`**）** `106_eula_consent.sql:288-294` 場景 6 還原值硬寫 `'2026-09-05-draft'` → 場景開頭把目前 `app_settings.eula_version` 存進既有 `ls197_idempotent_probe` 暫存表、收尾寫回；日後改 default 不再靜默殘留。

## 不做

migration（無 schema 變更；`app_settings` 表註解刷新 `e069ca4a` 等下次真的動 `app_settings` 再做）；iOS；正式站。

## 驗收

- [ ] `supabase/tests/run.sh` 全綠（lock 內）；每條的 mutation 或等價證明寫在 handoff
- [ ] merge-reviewer 通過（scope：只碰 `supabase/tests/`；效能：fixture 抽共用後 [run.sh](<http://run.sh>) 總時間不增）；QA delta PASS（後端）

## 留言（14）

### 2026-09-05T11:11:21.869Z · Chih-Lin Yeh · `41925b52`

**Orchestrator（09-05 19:10）**：開票理由——巡檢 backend lane ⚠ 連續空 2 輪、無機械候選（LS-22 後端子票全 Done）；依 LS-182／LS-198 清倉前例合成四條已記錄的測試／文件債。worktree `.claude/worktrees/LS-204`（`fix/LS-204-backend-test-debt` 自 development `40bdeac`）已建，ios-dev 已派；lock 目前由 LS-200 QA 持有，agent 以 `--hold` 排隊。

### 2026-09-05T11:46:35.750Z · Chih-Lin Yeh · `081cd0b0`

**Handoff（2026-09-05 19:46）**

**Ticket**：LS-204——後端測試與文件債清理（LS-96 四條池項：37d45fd1 ×2、ca99c6ae、2acb1d3b）

**已完成**（每條一個 commit，訊息引用池項 id）
1. LS-200 R2 i-A（`37d45fd1`）：`107_album_summaries.sql` §6 措辭過強問題——改為如實記錄「albums index 選擇在本 fixture 規模下非決定性」。commit `c7c6277`。
2. LS-200 R2 i-C（`37d45fd1`）：`run.sh` 的 `album_summaries_explain.txt` 檔頭補齊 R2 新增的 SubPlan／偵測器自證判準說明。commit `a1fcc9c`。
3. LS-200 R1 i3（`ca99c6ae`）：107 §6 與 `50_rls_plan_no_percall_subquery.sql` 重複的 51200 列效能 fixture 抽成 `supabase/tests/fixtures/media_family_perf_noise.sql`（psql `\ir` include）。commit `44f7832`。
4. LS-197 R2 i3（`2acb1d3b`）：`106_eula_consent.sql` 場景 6 還原值改讀既有 `ls197_idempotent_probe` 暫存表，取代硬寫 migration DEFAULT 字面值。commit `8b79864`。

**已驗證（怎麼驗的）**
- lock 內 `supabase/tests/run.sh` 全綠：改動前 baseline 1:36.15、改動後兩次分別 1:35.01／1:37.46，屬正常波動，未增加。持有 supabase lock 14 分 1 秒（`--hold "LS-204 backend test debt" --max-minutes 30`，`--release` 印「持有 14 分 1 秒」）。
- 條 1 mutation：把 view 的 LATERAL 改成兩個獨立的逐列 correlated 子查詢，本機重跑後 §6 判準 2 於 `107_album_summaries.sql:681` 噴 FAIL（如預期變紅）；已改回原本 LATERAL 寫法，migration 檔案無淨變更。
- 條 1 額外發現：本機對同一 50 本相簿 fixture 重跑兩次 `db reset`，一次規劃器選 `albums_cover_idx`（LATERAL loops=50），另一次選 `albums_family_created_idx`＋Incremental Sort（loops=21＝limit+1，真正 top-N 剪枝）——media 51200 列 ANALYZE 走抽樣統計，兩次抽樣差異足以讓這兩條成本相近的候選 plan 翻面。已把這個非決定性如實寫進 §6 註解，而非依票面「二擇一」中的「加大 fixture 到 3000 本＋新增 loops 斷言」選項（Option A）——那條路徑在本機驗證中證明本身就是不穩定/flaky 的來源，改採「如實描述」（Option B）風險更低。
- 條 3：抽共用後兩支測試各自單獨跑、隨完整 run.sh 跑，結果與抽共用前一致。
- 條 4 mutation：本機暫改 migration DEFAULT 為 `2099-01-01-mutation-test` 後重跑，場景 6 六案全綠，且 `app_settings.eula_version` 正確還原為新值、無殘留；已改回原值，migration 檔案無淨變更。
- push gate 通過（無 Swift 變更，跳過 SwiftLint／unit tests；migration 相關 gate 皆綠，本票未新增/修改 migration）。

**未完成**：無。本票四條全數來自既有 informational 的處置，本身即為「已修」動作，無新增 informational 待處置。

**風險**：低。僅動 `supabase/tests/` 與 `run.sh` 註解／fixture 抽取，無 schema／正式站／iOS 影響。`\ir`（psql relative include）在 run.sh 的 docker-exec 連線管道下無法解析 host 檔案系統路徑（容器未掛載 repo），會清楚報錯中止（fail loud）；CI（channel 1）與已裝 `psql` 的本機（channel 2）不受影響，已在 `fixtures/media_family_perf_noise.sql` 檔頭記錄。

**產出位置**：PR [#319](https://github.com/CLYEH/little-sprout/pull/319)（`fix/LS-204-backend-test-debt` → `development`），head sha `8b79864`。模擬器已關：無（本票未 boot 任何模擬器）。

### 2026-09-05T11:47:44.487Z · Chih-Lin Yeh · `475004b4`

**Orchestrator（09-05 19:47）**：handoff 收到（PR #319 head `8b79864`）。已派 merge-reviewer R1，重點：107 §6「如實描述」是否仍能抓到 LATERAL 退化（mutation 重跑）、index 翻面非決定性複驗、`\ir` include 在 CI 路徑的可靠性、106 場景 6 改 default 的 mutation、run.sh 計時。APPROVE 後 merge → promote dev→test → delta QA（後端）。

### 2026-09-05T12:04:54.878Z · Chih-Lin Yeh · `a2e9cd1b`

**merge-review R1（09-05 20:03）· PR #319 · head `8b79864be488116ed13a54fe2d22ce2750112e26`**（審查當下 `gh pr view` 一致）
base `40bdeac`（`git merge-base origin/development 8b79864`）。supabase lock 持有 8 分 36 秒＋2 分 37 秒＝共 11 分 13 秒。

## VERDICT：REQUEST_CHANGES（1 blocker）

---

### B1（blocker）`supabase/tests/fixtures/media_family_perf_noise.sql` ＋ `50_rls_plan_no_percall_subquery.sql:25` ＋ `107_album_summaries.sql:555`：`\ir` 讓 run.sh 的 docker-exec 管道整支紅，本機預設 shell 就會踩到

**失敗情境（已實跑重現，非推測）**：本機 `command -v psql` → rc=1（`libpq` 是 brew keg-only，`psql` 只存在於未 symlink 的 `/opt/homebrew/opt/libpq/bin/psql`，預設 PATH 找不到）。run.sh 因此落到第三條管道 `docker exec supabase_db_… psql … < file`——psql 由 **stdin** 讀腳本，`\ir` 沒有「當前腳本所在目錄」可依附，退回容器內 CWD（`docker inspect` 確認 WorkingDir=`/`，且只掛了資料卷 `/var/lib/postgresql/data`，repo 沒掛進容器）：

```
→ 50_rls_plan_no_percall_subquery.sql
  ✗ 50_rls_plan_no_percall_subquery.sql 失敗：
    fixtures/media_family_perf_noise.sql: No such file or directory
```

run.sh 約 30 秒後 exit 1，106／107 完全沒跑到，evidence 兩檔都不會產生。

**影響**：本票驗收條件「`supabase/tests/run.sh` 全綠（lock 內）」在這台機器（＝orchestrator／QA／各 agent 實際跑測試的同一台）以預設 shell 是**紅的**；orchestrator 排在後面的後端 delta QA 會立刻卡住。run.sh:61 明文實作這條 fallback（「psql 不一定裝在 host 上；沒有的話就借用 supabase 的 DB container 裡那一份」），本 PR 為了消除一段約 10 行的 INSERT 重複，靜默拿掉了這個支援路徑。

**與 handoff 的落差（請實作者確認）**：handoff 寫「lock 內 run.sh 全綠……改動後兩次分別 1:35.01／1:37.46」。這在預設 shell 下不可重現——跑到 50 就中止只需約 30 秒，不可能是 1:35。我把 `/opt/homebrew/opt/libpq/bin` 加進 PATH 後重跑，run.sh 確實**全綠、1:35.57**（管道 `host psql → 127.0.0.1:54322/postgres`），數字與 handoff 吻合。也就是說那兩次計時來自「psql 在 PATH 上」的 shell，而檔頭把這個前提寫成「本機需另外安裝 psql」——實際上 psql 早就裝了，只是 keg-only 沒 link，這個差別剛好讓實作者沒看見紅燈。

**建議修法（保留去重、且三條管道都不壞）**：把共用 fixture 改成由 `00_fixtures.sql`（該檔無 begin/rollback、是 COMMIT 的）建立的函式，例如 `create or replace function public.__test_seed_media_perf_noise() returns void …`，兩支測試在**各自交易內** `select public.__test_seed_media_perf_noise();`（隨各自 rollback 一起消失，交易邊界與執行次數同樣不變）。或者讓 run.sh 自己展開 include 再餵 psql（它本來就會在 `$tmp` 下暫存 SQL）。若團隊決定不再支援 docker-exec 管道，那是一個獨立的、要明說的決定（連同刪掉 run.sh:61 那段 fallback），不該由一張 fixture 重構的 chore 靜默決定。

---

### I1（informational，PLAUSIBLE）`107_album_summaries.sql:621-655`：§6 新註解的核心事實主張，我 6 次實測都沒重現

註解主張「同一份 50 本相簿 fixture 兩次 `db reset` 會拿到兩種 plan（`albums_cover_idx` loops=50 ↔ `albums_family_created_idx`＋Incremental Sort loops=21）」。我用兩種協定共 6 次觀察，**全部**選 `albums_family_created_idx`＋`Incremental Sort`，LATERAL 聚合 `loops=21`（＝limit 20＋1，真正 top-N 剪枝）：

- 同一 session 連跑 107 三次（§6 在自己交易內有 `analyze public.media/album_media/albums`，抽樣隨機性每次都會重新發生）→ 3/3 相同；
- `supabase db reset` → `00_fixtures.sql` → 107 兩輪 → 2/2 相同；
- 最後 channel-2 全綠那次 run.sh 產出的 evidence → 同樣是 `albums_family_created_idx`。

標 PLAUSIBLE 不列 major：我的協定是單跑 107，實作者可能是跑完整 run.sh（前面 50–106 留下的狀態／快取可能影響），我無法排除偶發翻面。**不影響正確性**——§6 兩條斷言完全不引用 index 選擇或 loops，註解純屬描述。但註解把數項我無法重現的具體事實寫成既定結論（尤其「本機另外實測約 3000 本相簿的規模才會穩定切過去」），並據此論證「不該把 loops 釘成斷言」；若翻面其實罕見，這段論述會在日後不當地擋掉票面 Option A。建議改寫成實際觀察到的樣子（「曾觀察到一次…」），或把兩份 EXPLAIN 存成 evidence 佐證。順帶一提，這段註解為了「改措辭如實描述」從 ~7 行長到 ~30 行——不算 scope 違規（同檔同段、可直接追溯到票面要求），但主張密度偏高。

### I2（informational）`106_eula_consent.sql:225-256`：探針列的 NULL 洞（既有模式位移，今日不可達）

探針列現在由 `insert … select eula_version from public.app_settings where id = true` 產生、`t` 由後面無 WHERE 的 `update` 補上。若 `app_settings` 單例列不存在，insert 進 0 列、update 影響 0 列，`select t into v_first … limit 1` 會讓 `v_first` 保持 NULL，於是 `v_second <= v_first` 求值為 NULL → 冪等斷言**靜默通過但什麼都沒驗**。舊寫法是把探針列的存在綁在 profiles 列上，同一個洞只是換了依賴對象，migration 也保證單例列存在，今日不可達。若日後動這段，補一句 `v_first is not null` 斷言即可。本票不必處理。

---

## 已驗證通過的部分（四個 commit 逐條）

- **`c7c6277`（107 §6 措辭）**：斷言本身零變更；**mutation 實跑確認仍有鑑別力**——把 view 的 LATERAL 換成兩條逐列 correlated 純量子查詢（只改 DB、不動 repo）後跑 107，判準 2 如期噴 `ERROR: FAIL 效能：album_summaries 查詢的 plan 出現非 hashed 的逐列 correlated SubPlan`（psql rc=3）。「如實描述」路線沒有削弱票面要求的那條 mutation 保護。已把 view 還原。
- **`a1fcc9c`（run.sh evidence 檔頭）**：`git diff` 過濾後**非 `echo` 的變更行數＝0**，確認純文字、無行為變更；三條判準文字與 107 §6 實際斷言逐條對得上（Seq Scan on media／album_media、非 hashed `(SubPlan N)`、關掉 indexscan／bitmapscan／indexonlyscan 的偵測器自證）。`album_summaries_explain.txt` 除 run.sh 自身外無其他消費者。實跑產出的檔頭渲染正確。
- **`44f7832`（共用 fixture）內容等價性**：抽出後 107 的雜訊列少了顯式 `id`（`3d000000-…`）、width/height 由 800×600 變 3024×4032、前綴改 `noise-`。已確認 `3d000000` 在 `supabase/`／`ios/` 全樹**零引用**；107 的 NOTICE 輸出 base vs head **逐字相同**；50 在乾淨狀態下 base 與 head 各自 20 條 ok、斷言文字（抹去 buffers 數字後）**完全相同**。「兩支結果不變」在管道 1／2 成立。
  - 附帶說明（既有問題、非本 PR 造成）：我在髒狀態下連續跑時，**base 版** 50 曾在 `list_comments 分支 2` 的 buffers 斷言炸掉（buffers=236 > 門檻 60）；各自 `db reset` 後 base／head 都綠。這是 50 既有的狀態／統計敏感性，與本 PR 無關，僅記錄。
- **`8b79864`（106 場景 6）**：**mutation 實跑確認**——`alter column eula_version set default 'LS204-mutation-value'` 並把該列設成新值後跑 106：rc=0、9 條 ok、無 FAIL，跑完 `app_settings.eula_version` = `LS204-mutation-value`、profile 兩欄回 NULL，**無殘留、不依賴舊字面值**。`'2026-09-05-draft'` 現在只剩 migration 與一處說明註解。
- **Race**：run.sh 逐檔序列執行、每檔一個 psql session；唯一併發是既有的 `run_sql_bg` 成對案例，本 PR 未觸碰。106 的暫存表變更全在單一 session 內。無新增併發面。
- **效能／平行**：fixture include 是同一份 SQL 文字被各自 include 一次，INSERT 次數與交易邊界皆不變（已逐檔核對）；本機 head 全綠計時 1:35.57 vs handoff baseline 1:36.15，未增加；CI `db` job 3m45s pass。本票無可平行化面向。
- **Scope**：乾淨。只動 `supabase/tests/` 五個檔，無 migration／iOS／docs 變更，與票面「不做」一致。

## 我沒做的（誠實聲明）

- 沒有自行測 base 版 run.sh 的**全套牆鐘時間**（要跑 base 需另建 worktree／改動工作區，本次 review 不動檔）。時間未增的結論來自：head 實測 1:35.57、handoff baseline 1:36.15、結構等價（INSERT 次數／交易邊界不變）、CI db job 綠，四項合併判斷。
- I1 的翻面主張只能標 PLAUSIBLE 未重現，無法證否。

**下一步**：B1 修掉（建議走 `00_fixtures.sql` 函式版）後重推；head 一變就要重審重貼 commit status。

### 2026-09-05T12:06:38.551Z · Chih-Lin Yeh · `d5a11916`

**Orchestrator（09-05 20:06）**：merge-review R1 `a2e9cd1b` REQUEST_CHANGES（B1：`\ir` include 在 run.sh docker-exec 退路下找不到 fixture；本機預設 shell 因 libpq keg-only 正走這條路，實作者只在 PATH 含 libpq 的 shell 驗過）。已續派 R2：fixture 改 `00_fixtures.sql` 函式、三條通道（host psql／docker-exec／CI）實跑計時、I1 註解改為不斷言未重現的 plan 翻面、I2 入池或一行 assert。教訓記入 notes：本機「run.sh 全綠」必須註明通道。

### 2026-09-05T12:30:31.445Z · Chih-Lin Yeh · `478f9688`

**R2 Handoff（2026-09-05 20:30）**——回應 merge-review R1 `a2e9cd1b` REQUEST_CHANGES

**已完成**（commit `e26a800`，訊息引用 `a2e9cd1b`）

- **B1**：R1 把共用 fixture 抽成 `supabase/tests/fixtures/media_family_perf_noise.sql`、用 psql `\ir` include——這在 run.sh 的 docker-exec 連線管道（`psql` 不在 PATH 時的預設退路，本機預設 shell 正是這條路）下找不到檔案、直接中止。R1 handoff 的「全綠」是我驗證時手動把 libpq 的 psql 加進了 PATH，不是本機預設環境，是我的驗證盲區，如實記錄於此。改法：刪掉 `fixtures/`，把共用 insert 包成 `00_fixtures.sql`（會 commit）建立的 `private.ls204_seed_media_perf_noise()` 函式，107/50_ 兩支各自在自己交易內呼叫——純 SQL 呼叫不涉及檔案路徑解析，三種連線管道天生一致。函式 `security definer set search_path=''`，符合 `60_default_privileges.sql` §9 對 schema private 函式的硬化要求（實測發現：不加這兩個屬性會被 §9 通掃擋下，已修正）；不需要額外 revoke——`harden_default_privileges.sql` 的全域 `alter default privileges revoke execute on functions from public` 已保證新函式對 anon/authenticated 無 EXECUTE，§2/§3 通掃驗證仍為綠。
- **I1**：107 §6 註解原本宣稱本機觀察到 albums index 選擇在兩次 reset 間翻面（一次 `albums_cover_idx`/loops=50，一次 `albums_family_created_idx`+Incremental Sort/loops=21）。reviewer 六次重跑皆為後者、無法重現前者，我也只在自己有限的樣本中看過一次前者的形狀，不足以支撐「會翻面」這個具體宣稱。改為中性描述：plan 選擇依統計而定，本段機械斷言只釘「media/album_media 不出現 Seq Scan」與「不出現逐列 correlated SubPlan」，不對 albums 這一段的 index 選擇做具體斷言，措辭不再引用未能穩定重現的觀察。
- **I2**：`106_eula_consent.sql` 場景 6 的 `select ... into v_first` 在 0 列命中或 t 為 NULL 時不報錯，後面 `<=` 比較會被 NULL 悄悄放行——既有洞、今日不可達。二擇一中選擇當場一行順修：比照本檔既有的 `if not found` 慣例補上 `if v_first is null then raise exception`，未走入池路線。

**已驗證（三條連線管道，各自重新 `supabase db reset` 後實跑）**
- (a) host psql + 離散參數（`PATH` 含 `/opt/homebrew/opt/libpq/bin`）：1:36.38，`✓ 全部 DB 測試通過`
- (b) 預設 shell／docker-exec（`command -v psql` rc=1，本機預設路徑——R1 的驗證盲區、這次的重點）：1:40.28，`✓ 全部 DB 測試通過`
- (c) `SUPABASE_DB_URL`（CI `db` job 走的同一條連線路徑，本機用 `supabase status -o env` 動態取值驗證同一段程式碼路徑）：1:35.38，`✓ 全部 DB 測試通過`

三次計時彼此接近、與 R1 baseline（1:36.15）同一量級，未增加。supabase lock 本輪持有 19 分 4 秒（`--hold "LS-204 R2" --max-minutes 30`）。

**未完成**：無。

**風險**：低，僅測試檔與 fixture 內部結構調整；`private.ls204_seed_media_perf_noise()` 由測試檔建立，不經 migration，不落地正式站。

**產出位置**：PR [#319](https://github.com/CLYEH/little-sprout/pull/319) 已更新（R2 已修 `e26a800`），head sha `e26a800`（5 commits）。

### 2026-09-05T12:31:19.176Z · Chih-Lin Yeh · `90c4aaf3`

**Orchestrator（09-05 20:31）**：R2 handoff 收到（head `e26a800`；B1 改 `private.ls204_seed_media_perf_noise()`＋三通道實跑、I1 措辭、I2 一行 raise）。已續派 merge-reviewer R2 審增量，要求在預設 shell（docker-exec 通道）實跑 run.sh，並判 `security definer` 對測試 seed 函式是否過寬、50／107 間是否重複灌資料。

### 2026-09-05T12:38:30.381Z · Chih-Lin Yeh · `073e756d`

**merge-review R2（09-05 20:37）· PR #319 · head `e26a800f2ed734f65c7f0d55f15f57235b722568`**（審查當下 `gh pr view` 一致）
增量 `8b79864...e26a800`（單一 commit）。supabase lock 持有 4 分 4 秒。CI 四項全綠（`db` 3m55s）。

## VERDICT：APPROVE

R1 的 blocker 已修，兩條 informational 都已處理且經實跑驗證。無新增 blocker／major。

---

## B1（blocker，R1）→ 已修，並在**當初紅的那條通道**實跑確認

改法正確：刪掉 `fixtures/` 與 `\ir`，改成 `00_fixtures.sql` 建立的 `private.ls204_seed_media_perf_noise()`，107／50 各自在自己交易內 `select` 呼叫。純 SQL 呼叫不涉及檔案路徑解析，三條連線管道天生一致。

**實跑（預設 shell，`command -v psql` → NO，run.sh 自動落到 `docker exec supabase_db_little-sprout psql` ——R1 就是這條炸的）**：

```
→ 連線方式：docker exec supabase_db_little-sprout psql
✓ 全部 DB 測試通過      run.sh rc=0     1:43.40
```

evidence 兩檔正常產出。R1 的失敗訊息（`fixtures/media_family_perf_noise.sql: No such file or directory`）不再出現。

**計時**：本輪 docker-exec 通道 1:43.40 vs 實作者同通道 1:40.28（差 ~3 秒，噪音）。跨通道不宜直接比（docker-exec 每檔多一次 `docker exec` 啟動成本，是既有特性）；同通道內無增加跡象，且結構上仍是同一句 50000 列 INSERT、執行次數與交易邊界未變。本輪我只量了 docker-exec 這一條（R1 我量過 host psql：舊 head 1:35.57）。

### 判：`security definer` 對一支純測試 seed 函式是否過寬 → **可接受，不列 finding**

- **是被 gate 逼的，不是隨手加的**：`60_default_privileges.sql` §9 對 schema `private` 的**每一支**函式硬性要求 `SECURITY DEFINER` ＋ `set search_path=""`，除非登記進 `v_exceptions`。而該例外清單的既有理由明文限於「純 regex／只讀 GUC、不碰任何資料庫物件」（`is_media_object_path`／`is_avatar_object_path`／`deletion_bypass_active`／`enforce_deletion_bypass`）——一支會寫入 `public.media` 的函式套不上這個理由。把測試 fixture 登記進一份安全白名單，比讓它符合規則更糟。放 `private` 而非 `public` 也對：`public` 的每支函式都要進 §8 的 RPC API 邊界清單，登記進去會誤導成「這是一支 RPC」。
- **沒有提權面**：EXECUTE 對 anon／authenticated／PUBLIC 全數關閉（本輪綠跑實證：「schema private 通掃 **33** 支非允許清單函式，authenticated／anon 皆無 EXECUTE」「schema private 的每一支函式都沒有對 PUBLIC 開放 EXECUTE」，§9 亦綠：「除了登記在案的 4 支 invoker 例外，都是 SECURITY DEFINER＋search_path 收斂」）。函式 owner 是 postgres，而 postgres 本來就繞過 RLS——`definer` 對「唯一叫得動它的人」沒有多給任何能力。
- **不會進正式站**：由 `00_fixtures.sql` 建立，該檔只被 `run.sh` 載入；`config.toml` 明寫 `[db.seed] enabled = false`，`supabase db reset` 不會載入它；不在任何 migration 中（全樹 grep 僅 3 個檔提及此函式，皆在 `supabase/tests/`）。
- `set search_path = ''` 下 `public.media` 已完整限定，`generate_series`／`lpad`／`now()` 皆 `pg_catalog` 內建（search_path 恆隱含），實跑亦已證明。

### 判：函式是否冪等／會不會在 50 與 107 間重複灌資料 → **不會**

- 全樹只有兩處呼叫（`50:27`、`107:557`），各自在該檔自己的交易內，兩檔都整檔 `begin … rollback`（50 為 17→909、107 §6 為 543→746），跑完各自回滾，資料不跨檔累積。`00_fixtures.sql` 只建立函式、不呼叫它。
- 函式本身**不是**冪等的（`storage_path` 有 UNIQUE，同一交易內叫兩次會撞唯一鍵）——在這裡是正確的取捨：誤用會 fail loud，不會靜默灌雙倍資料而讓效能斷言失去意義。
- 順帶：單獨跑 107／50 而沒先跑 `00_fixtures.sql` 現在會報「函式不存在」而不是 FK 錯誤。兩者本來就都跑不起來（fc 家庭／owner 也來自 00），不是回歸。

## I1 → 已修

§6 註解改為中性：只說 albums 走哪條 plan 是規劃器依當下統計做的成本決策、不是本 fixture 保證釘死的行為，並明確界定兩條機械斷言的鑑別力範圍。R1 我 6 次無法重現的具體宣稱（`albums_cover_idx`/loops=50 翻面、「約 3000 本相簿才會穩定切過去」）已全數移除，未以其論證擋掉日後的 Option A。註解同時從 ~30 行縮回 ~10 行。本輪 evidence 觀察到的仍是 `albums_family_created_idx`（與 R1 的 6/6 一致，累計 7/7）。

## I2 → 已修，且**兩個方向都驗過**

`106_eula_consent.sql:275-283` 補 `if v_first is null then raise exception`。

- **不誤觸**：正常 `db reset` → 完整 run.sh，106 全綠 16 條 ok，含場景 6「同版本重複呼叫 accept_eula() 不報錯，eula_accepted_at 更新為最新一次（冪等）」。
- **有牙齒**（我加驗的 mutation，只改 scratchpad 副本、不動 repo）：把場景開頭那句 probe insert 改成命中 0 列後跑 106，rc=3 並精準噴出
  `ERROR: FAIL：讀不到冪等測試的第一次時間戳（ls197_idempotent_probe 空表或 t 為 NULL），下面的比較會被 NULL 悄悄放行`
  ——證明這不是一句恆綠的裝飾。

## R1 兩條 mutation 在新 head 重跑

- **LATERAL 退化**：把 view 的 LATERAL 換成兩條逐列 correlated 純量子查詢（只改 DB）後跑 107 → rc=3，`ERROR: FAIL 效能：album_summaries 查詢的 plan 出現非 hashed 的逐列 correlated SubPlan`。還原 view 後 107 rc=0、11 條 ok。
- **106 DEFAULT 改值**：`alter column eula_version set default 'LS204-mutation-value'` 後跑 106 → rc=0、9 條 ok、無 FAIL；跑完 `app_settings.eula_version` = `LS204-mutation-value`、profile 兩欄回 NULL，無殘留。

## 四維度

- **Race**：無新增併發面。函式呼叫是同交易內的一般 SQL，`security definer` 只換執行身分不換交易；run.sh 仍逐檔序列、既有 `run_sql_bg` 併發案例未觸碰。
- **效能**：同一句 50000 列 INSERT，包成函式不改變執行計畫或次數；`language sql` 單語句函式無額外 plpgsql 迴圈。無 N+1／per-row 退化（判準 2 仍保有鑑別力，已 mutation 證明）。
- **平行**：本票無可平行化面向。
- **Scope**：乾淨。vs base 只動 `supabase/tests/` 五個檔（`00_fixtures.sql`／`50_`／`106_`／`107_`／`run.sh`），`fixtures/` 目錄已完全移除，無 migration／iOS／docs 變更。`git status` 乾淨（evidence 為 gitignored）。函式命名 `ls204_…` 沿用本 repo 既有慣例（如 `ls197_idempotent_probe`）。

## 我沒做的（誠實聲明）

- 本輪只量了 docker-exec 通道的牆鐘時間，未自行複量 host psql／`SUPABASE_DB_URL` 兩條（實作者報 1:36.38／1:35.38；CI `db` job 3m55s 綠即為 `SUPABASE_DB_URL` 那條的獨立佐證）。
- 未自行複量 base 版全套時間（需另建 worktree，本次 review 不動檔）。

**收尾**：DB 已 `db reset` 回乾淨狀態（mutation 的 view／DEFAULT 皆已消除），worktree 未留任何改動。可 merge → promote dev→test → 後端 delta QA。

### 2026-09-05T12:40:17.700Z · Chih-Lin Yeh · `9976f618`

**Orchestrator（09-05 20:39）**：merge-review R2 `073e756d` APPROVE（status 貼 `e26a800`；預設 shell docker-exec 通道實跑綠）→ PR #319 已併 development `9a25b76`（dev tip status 已貼）→ QA。test 目前由 LS-164／LS-202 合併 QA 持有（`6d9e01e`），該批 test→main 後再 promote dev→test 並派本票 delta QA（後端：三通道 run.sh、fixture 函式不進 migration、106 場景 6 guard）。

### 2026-09-05T15:46:55.602Z · Chih-Lin Yeh · `01e23f19`

## QA 裁決：PASS

驗收基準：`test` branch delta 合併 `da62930`（origin/test `6d9e01e`→`da62930`，四票之一）。worktree `.claude/worktrees/qa-test`（detached @ `da629306c91ecc7ce5af72c3783c3c6da6f56e97`），git status 乾淨。

### 逐條驗證

- ✓ `supabase db reset` 乾淨套用全部 migrations（含 `20260905132350_family_ownership_guard.sql`）。
- ✓ `bash scripts/ops/supabase-lock.sh -- bash supabase/tests/run.sh` 全綠（通道：`docker exec supabase_db_little-sprout psql`；`--hold "QA da62930 backend"` 內執行，總耗時 1:47）。
- ✓ **範圍 1／2**（`107_album_summaries.sql` §6 效能斷言＋`run.sh` evidence 檔頭）：檔內註解已如實訂正措辭（不再宣稱「規劃器一定選用 albums_family_created_idx」，只斷言 media／album_media 不 Seq Scan＋無非 hashed correlated SubPlan，並附「拿掉判準 1 的鑑別力」自我驗證段落）；`run.sh` 產生的 `album_summaries_explain.txt` 檔頭已補 LS-204 判準 1/2/3 說明，與 §6 實際斷言對齊。mutation（票文所述「把 view 的 LATERAL 改成逐列 correlated 子查詢」）已在票內完成並記錄於程式碼註解（638 行「已實際套用此突變重跑驗證」）。
- ✓ **範圍 3**（51200 列共用 fixture）：`00_fixtures.sql` 新增 `private.ls204_seed_media_perf_noise()`（security definer, search_path=''），`107_album_summaries.sql` 與 `50_rls_plan_no_percall_subquery.sql` 皆呼叫同一函式，不再各自維護一份 5 萬列 insert；兩支測試皆綠、無殘留重複。
- ✓ **範圍 4**（`106_eula_consent.sql` 場景 6 還原值）：改用 `ls197_idempotent_probe` 暫存表存原值、場景結尾寫回（不再硬寫 `'2026-09-05-draft'` 字面常數）；場景 6 綠。

### 未完成 / 風險

- 無。四條池項皆已落地並機械驗證；`supabase-lock` 已釋放（持有 6 分 28 秒）。

### 產出位置

- `supabase/tests/107_album_summaries.sql`／`106_eula_consent.sql`／`00_fixtures.sql`／`50_rls_plan_no_percall_subquery.sql`／`run.sh`
- 測試 log：本機 session（未落盤成檔，見本 comment 逐條摘要）

### 2026-09-05T15:51:33.240Z · Chih-Lin Yeh · `adfaecaf`

**Orchestrator（09-05 23:50）**：delta QA `01e23f19` **PASS**（run.sh docker-exec 通道 1:47 綠、四條池項落地）。同批 test tip `da62930` 因 LS-167 單項退修 status failure → test→main 等 LS-167 修正併入後重 promote＋delta QA；之後 tag v0.16.0 → sweeper → Done → cleanup。

### 2026-09-05T17:59:03.836Z · Chih-Lin Yeh · `15f0450f`

**Orchestrator（09-06 01:58）**：隨 test `127c38c` 進 main，tag **v0.16.0**（QA PASS `01e23f19` 沿用）。dead-code-sweeper 已派 → Done → cleanup。

### 2026-09-05T18:00:56.378Z · Chih-Lin Yeh · `3fac4d39`

## dead-code sweep — LS-204（2026-09-06 02:00）

**範圍**：`git diff 40bdeac...9a25b76 -- supabase/tests`（5 commits：`44f7832`／`c7c6277`／`a1fcc9c`／`8b79864`／`e26a800`，PR #319，併 development `9a25b76`）。只碰 `supabase/tests/{00_fixtures,50_rls_plan_no_percall_subquery,106_eula_consent,107_album_summaries,run.sh}`。

### Findings

未發現本 feature 引入的死碼。逐項交叉比對：

1. **`private.ls204_seed_media_perf_noise()`**（`00_fixtures.sql`）：`grep -rn` 全樹確認呼叫點恰好兩處——`50_rls_plan_no_percall_subquery.sql:27`、`107_album_summaries.sql:557`，皆各自在自己的 `begin…rollback` 交易內呼叫一次，無孤兒函式、無重複灌資料（merge-review R2 `073e756d` 已用 mutation 驗過冪等性與 UNIQUE 約束會 fail loud）。
2. **R1 殘留的 `supabase/tests/fixtures/` 目錄**（`\ir` include 舊做法）：`ls supabase/tests/fixtures` 已不存在；`git log --diff-filter=A -- supabase/tests/fixtures` 只出現在 R2 commit `e26a800`（新增又刪除同一輪，未曾進入 development）；`grep -rn "fixtures/"` 全樹只剩 `00_fixtures.sql` 註解裡「原本的做法（R1，已撤銷）」這段沿革說明，不是死引用。
3. **107 §6 效能斷言與 run.sh evidence 檔頭**：逐行核對 `107_album_summaries.sql:621-676` 的三條機械斷言（Seq Scan、非 hashed correlated SubPlan、偵測器自我驗證關 index/bitmap/indexonly scan 逼出真 Seq Scan）與 `run.sh` 新印的判準 1/2/3 文字，一一對得上，無過時措辭殘留。
4. **106 場景 6 `ls197_idempotent_probe`**：新流程（場景開頭存原值→中途補 `t`→結尾讀回還原→`drop table`）閉環一致；新增的 `if v_first is null then raise exception` guard 是防禦性補強（今日不可達，comment 已明說原因），不是鷹架殘留。

### 既有死碼觀察

無（未在本次 diff 範圍外發現需另開票的既有死碼；worktree `.claude/worktrees/LS-204` 目前乾淨無 dirty 變更）。

### 建議處置

無需開 fix 票。四條池項（`37d45fd1`×2／`ca99c6ae`／`2acb1d3b`）已在 merge-review R1/R2（`a2e9cd1b`／`073e756d`，APPROVE）與 QA（`01e23f19`，PASS）中被充分複驗＋mutation 驗證，本輪巡檢未發現新增死碼，可直接 Done。

### 搜尋方法與盲區聲明

- 方法：`git log`/`git diff` 鎖定票面 commit 範圍；`grep -rn` 交叉比對函式呼叫點、目錄殘留、檔頭文字對齊；直接讀取 §6 全段源碼核對斷言與註解一致性；`git log --diff-filter=A/D` 追蹤 `fixtures/` 目錄生滅。
- 盲區：純文字 grep 抓不到動態 SQL 拼接／`EXECUTE` 字串內的間接呼叫（本次未發現此類用法）；未對活資料庫實跑複驗（已有 merge-review R1/R2 與 QA 三輪實跑紀錄可信賴，本次巡檢不重複跑 `supabase db reset`／`run.sh`，未觸碰 supabase-lock）。

### 2026-09-05T18:02:09.853Z · Chih-Lin Yeh · `0cb2b4bf`

**收尾儀式（09-06 02:0x）**
- **dead-code-sweeper** `3fac4d39`：無死碼；`private.ls204_seed_media_perf_noise()` 恰兩處呼叫、R1 `fixtures/` 無殘留、106 場景 6 NULL guard 屬防禦非死碼。
- **Lesson learning review**：(1) R1 `\ir` include 只在 host psql 通道綠——本機 libpq keg-only、預設走 docker-exec；「run.sh 全綠」handoff 從此必附通道（已入 LS-207 定義句＋`run.sh` 印通道）；(2) 池項清倉票是 backend lane 空時的合法補位，但四條 XS 也走了兩輪 review——清倉票仍要完整驗證，不能因「小」省略通道／mutation。
- **清理**：`cleanup-merged.sh --apply LS-204` → worktree 已移除。
→ Done（v0.16.0）。

