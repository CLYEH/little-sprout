# LS-59 Harness：既有三支 gate .test.sh 在 macOS bash 3.2 下 ✗ 分支變數名吸入多位元組會炸

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Low |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-24T20:24:34.933Z |
| 開始 | 2026-09-01T12:01:07.150Z |
| 完成 | 2026-09-01T12:44:50.331Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-59/harness既有三支-gate-testsh-在-macos-bash-32-下-分支變數名吸入多位yuan組會炸 |
| 關係 | related→LS-119, related→LS-110, related→LS-95, related→LS-86, related→LS-96, related→LS-57, related→LS-53, related←LS-68, related←LS-65, related←LS-103, related←LS-66, related←LS-50, related←LS-56, related←LS-51 |

## 描述

## 來源

[LS-53](https://linear.app/little-sprout-app/issue/LS-53/harness破壞性偵測器盲區-alter-policy-false-與-revoke-未納入關鍵字語意判定) handoff 風險欄（2026-08-24）。

## 現象

`scripts/gates/destructive-approval-check.test.sh`、`api-contract-check.test.sh`、`error-codes-check.test.sh` 的 ✗ 分支寫成 `"$name（…"`——macOS `/bin/bash` 3.2 會把緊接的全形字元吸進變數名，導致**只有在樣本真的失敗時**本機才會炸（CI ubuntu bash 5 無此問題）。即：gate 自測在本機「壞掉時反而報錯誤的錯」，違反 fail-loud。

## 修法

四支檔案改 `${name}`（[LS-51](https://linear.app/little-sprout-app/issue/LS-51/chore設計畫布執行產物入版控規則統一-shotcheckhtml-等abd-三軌) 新增的 `tracked-ignored-check.test.sh:36` 同寫法，PR #79 review F5 實測同炸；[LS-53](https://linear.app/little-sprout-app/issue/LS-53/harness%E7%A0%B4%E5%A3%9E%E6%80%A7%E5%81%B5%E6%B8%AC%E5%99%A8%E7%9B%B2%E5%8D%80-alter-policy-false-%E8%88%87-revoke-%E6%9C%AA%E7%B4%8D%E5%85%A5%E9%97%9C%E9%8D%B5%E5%AD%97%E8%AA%9E%E6%84%8F%E5%88%A4%E5%AE%9A) 新增的兩支已用此寫法）。附一個負向樣本在本機 bash 3.2 跑通證明。

## 驗收

* 三支 `.test.sh` 在 `/bin/bash`（3.2）與 `bash`（brew 5.x）下，正向全綠、刻意注入失敗樣本時輸出正確的 ✗ 訊息而非 bash 語法錯誤。
* 不動規則本體、不動 CI。

## 分支

harness → `hotfix/LS-<n>-*` 從 main 切、PR 回 main、back-merge。

## 順修（[LS-56](https://linear.app/little-sprout-app/issue/LS-56/harnesserror-codes-check-抽取規則硬化註解掉的-case表格首欄格式artifact-retention) R2 INFO-1）

`scripts/gates/error-codes-check.sh:29-30` 檔頭只寫「碼被退役會紅（fail-loud）」半邊；另半邊（後續 migration 以 `create or replace` 拿掉某 raise 後聯集仍綠＝假綠）只在 COLLABORATION §7 有寫。檔頭補齊兩向。

## 順修（[LS-50](https://linear.app/little-sprout-app/issue/LS-50/harness分支起點乾淨度-gate-feature-分支不得夾帶其他票號的-commit) PR #90 review I1–I5）

* `branch-ticket-check.sh:89` 無票號違規時印出空的 `Bundles: ` 逃生口 → 改印「先補票號」。
* `merge-conflict-check.sh:57` rc≥2 一律歸咎 git 版本 → 區分 unrelated histories 等訊息。
* COLLABORATION §7 兩列措辭：「落後」→「任何 sha 不等即拒」；盲區補「GitHub 誤判 CONFLICTING 時本機綠≠CI 會跑」、「`git merge --squash`＋自票訊息可走私內容」、「detached／推非 HEAD 分支跳過；PR body 事後編輯不重驗」。

## 留言（4）

### 2026-09-01T12:17:48.679Z · Chih-Lin Yeh · `79d740ec`

## Handoff：LS-59（ios-dev）

**Ticket**：LS-59 Harness：三支 gate `.test.sh` 的 macOS bash 3.2 多位元組變數名炸裂修復＋兩組順修

**已完成**
1. **主體**：`destructive-approval-check.test.sh:18`、`api-contract-check.test.sh:46,167`、`error-codes-check.test.sh:68`（含 ⑪ 兩個自訂分支 line 191／211）改用 `${name}`／`${want}`／`${got}` 避免緊鄰全形標點時被 bash 3.2 吞進變數名。**票文只點名 `$name`，但實測發現同一行的 `$want`／`$got` 緊鄰全形字元時同樣會炸**（見「已驗證」的原始輸出），已比照修正，範圍限於同幾行的 ✗ 輸出。
   - 額外修了 `tracked-ignored-check.test.sh:36`：票文「四支檔案改 `${name}`」提到此檔（LS-51 新增、PR #79 review F5 已測出同炸），但實際上這行至今仍是舊寫法未修——已修正（`${name}`／`${must}`／`${got}`，`$want` 因後接 ASCII `$` 不受影響、維持原樣）。
2. **順修 A**（LS-56 R2 INFO-1）：`error-codes-check.sh` migrations 側 bullet（現行行號因 LS-57 已位移）補齊「碼被退役會紅（fail-loud）」與「後續 migration `CREATE OR REPLACE FUNCTION` 拿掉某 raise、沒人同步清 §5／Swift 時聯集仍綠（假綠）」兩向，措辭比照 COLLABORATION §7。純註解，未動邏輯。
3. **順修 B**（LS-50 PR #90 review I1–I5）全部五項：
   - `branch-ticket-check.sh:89` 空 Bundles 逃生口（只有 `[無票號]` commit、無異票需宣告時）改印「先補票號」。
   - `merge-conflict-check.sh:57` rc≥2 不再一律歸咎 git 版本：先判斷 `$out` 含 `unrelated histories` 就點名「沒有共同祖先、分支起點可能切錯」。
   - `docs/COLLABORATION.md` §7 兩處「本機 ref 落後即擋」→「本機 ref 與遠端任何 sha 不等即擋」（line 113 Push gate 總覽列＋line 215 merge-conflict-check 專列）。
   - 盲區補三條（票文原文）：merge-conflict-check 列補「GitHub 自己判定 CONFLICTING 與本機 `git merge-tree` 判定可能不一致，本機綠不保證 CI 會跑」＋「push-gate 第 6/7 步共用同一分支判定，detached HEAD 或推非目前 checkout 分支時兩步一起靜默跳過」；branch-ticket-check 列補「`git merge --squash` 他票分支＋自己補一個本票 commit 可把異票內容走私進來，這裡只看 commit subject 票號」＋「PR body 的 Bundles 宣告只在 CI 那次跑時驗過，事後編輯不會重驗」。

**已驗證（怎麼驗）**
- 本機新裝 `brew install bash`（5.3.15）補齊票文要求的第二個 bash 版本（環境原本只有 `/bin/bash` 3.2.57，無 brew bash）。
- 正向：四支自測（destructive-approval-check／api-contract-check／error-codes-check／tracked-ignored-check）在 `/bin/bash` 3.2.57 與 brew `bash` 5.3.15 下皆全綠（11／15／23／7 組）。
- 負向（刻意注入失敗樣本，證明不炸 bash 語法/執行期錯誤）：對每支腳本臨時把對應 `.sh`（非 `.test.sh`）換成固定回錯誤 exit code 的假 checker（換完立刻用 `git status` 確認原檔已還原、無殘留 diff），跑 `.test.sh`，兩個 bash 版本輸出逐字相同，例：
  ```
  $ /bin/bash scripts/gates/destructive-approval-check.test.sh   # 假 checker 固定 exit 0
  ✓ ① 獨立成行
  ✗ ② 散文：〈〉括起（期望 exit 1，實得 0）
  ✗ ③ 小寫（期望 exit 1，實得 0）
  ...
  test.sh exit=1
  ```
  同樣手法驗了 api-contract-check.test.sh（①②③⑦⑨⑩ 觸發 ✗）、error-codes-check.test.sh（②-⑩ 觸發 ✗，另外分別用固定 exit 0／exit 1 的假 checker 逼出 ⑪ 兩個自訂分支 line 191／211）、tracked-ignored-check.test.sh（固定 exit 99＋不含期望字串，逼出全部 ⑦ 組且驗了 `$must` 空值／非空值兩條路徑）。
- 順修 A：`bash -n` 語法檢查通過，`error-codes-check.test.sh` 仍 23 組全綠（純註解，無邏輯變動）。
- 順修 B：`branch-ticket-check.test.sh`（19 組）／`merge-conflict-check.test.sh`（9 組）兩個 bash 版本下仍全綠；另手動重現「只有無票號 commit」情境（印出「先補票號」而非空 `Bundles: `）與「unrelated histories」情境（用 orphan branch 建無共同祖先的兩條分支，`git merge-tree` 跑出 rc=128，印出「沒有共同祖先、分支起點可能切錯」），兩者在兩個 bash 版本下輸出一致。
- Push gate 本機跑過（見下方產出位置的 push 輸出）：純文件/harness 變更，正確跳過 SwiftLint／unit tests；`branch-ticket-check`／`merge-conflict-check`／`error-codes-check` 等既有 gate 全綠，`push gate 通過`。

**未完成 / informational 處置**：本票尚未經 merge-reviewer 審查（初次實作＋自我驗證後直接 push＋開 PR，依規約不等 CI），目前無 reviewer informational 待處置——待 merge-review 後再補。範圍內票文列出的所有項目（主體三支＋順修 A＋順修 B 五項）均已完成，無保留項。

**風險**：低。純 harness 自測腳本與說明文字變更，不動任何 gate 規則本體、不動 CI 設定；已用兩個 bash 版本、正負向樣本反覆驗證。

**產出位置**
- Worktree：`.claude/worktrees/LS-59`　Branch：`hotfix/LS-59-bash32-testsh`
- PR：https://github.com/CLYEH/little-sprout/pull/200（base `main`；push gate 本機已過，CI 由 orchestrator 監看）
- Commits：`71012cb`（主體 ${name} 修復）／`9b37c8f`（順修 A）／`fc9f2bd`（順修 B）
- 模擬器已關：無（本票不需模擬器，未 boot 任何模擬器）


### 2026-09-01T12:27:20.216Z · Chih-Lin Yeh · `932d41a2`

## Merge review R1：LS-59（PR #200）

**審查對象**：`hotfix/LS-59-bash32-testsh` head `fc9f2bd6d576d99083fb5b8c8cc7f4fa2757731c`（`git diff origin/main...fc9f2bd`，8 檔 +26/-14）
**CI**：`rules`／`db`／`lint`／`ci` 四個 job 皆 SUCCESS（run 33506834670）

### Verdict：**APPROVE**（0 blocker、0 major；2 minor、3 informational）

---

### 四維度

| 維度 | 結論 |
|---|---|
| **Race condition** | 無新增併發面。`merge-conflict-check.sh` 的 `ls-remote` → `merge-tree` → 實際 push 之間本來就有 TOCTOU（遠端可能在這之間前進），是既有且已在 §7「只驗 push 當下」記錄的限制，本 PR 未動該邏輯。 |
| **運算效能** | 唯一新增的執行成本是 `merge-conflict-check.sh:57` 在 **rc≥2（已經要退出）** 的路徑上多一個 `printf｜grep` 子行程。四支 `.test.sh` 只改字串插值寫法，樣本數／執行時間不變（實測兩個 bash 版本秒級）。 |
| **平行優化** | N/A：gate 自測本來就必須序列（共用 `mktemp -d` 工作目錄與臨時 git repo），沒有被不必要序列化的可平行工作。 |
| **Scope** | 乾淨，見下方逐項裁決。無無關重構、無順手改動、未動任何 gate 規則本體與 CI 設定（符合票文「不動規則本體、不動 CI」）。 |

---

### 1. 修復完整性：驗過，無第五個漏網變數

**根因確認（實測，非推論）**：這個炸裂是 **locale 相依**的，不是無條件發生——

```
LC_ALL=C            : ✗ OK（tail）                    ← 不炸
LC_ALL=en_US.UTF-8  : /bin/bash: v: unbound variable  ← 炸
LC_ALL=zh_TW.UTF-8  : /bin/bash: v: unbound variable  ← 炸
```
（`/bin/bash -c 'set -u; v=OK; echo "✗ $v（tail）"'`，bash 3.2.57）

所以 `${}` 是正確且唯一 surgical 的修法（另一種修法 `export LC_ALL=C` 會連帶改掉這些自測裡 `grep`／`sort`／`comm` 對中文字串的 collation 語意，不該碰）。

**負向實測（stub `.sh` 逼出 ✗ 分支，`/bin/bash` 3.2.57）**：

- `destructive-approval-check.test.sh`：main 版在 **line 18** 直接 `name<byte>: unbound variable` 而**整支中止**（`set -u` → 後面 8 組樣本根本沒跑到，比「訊息錯」更糟）；head 版 8 條 ✗ 全部正確印出。
- `error-codes-check.test.sh`：head 版逼出 ②–⑩ 共 14 條 ✗ ＋ ⑪ 的 line 191，全部正確。
- `tracked-ignored-check.test.sh`：head 版逼出 ②③④⑥ 四條 ✗，含 `${must}` 非空路徑（輸出含「design-canvas-d/_shotcheck.html」）。
- `api-contract-check.test.sh` line 46：head 版逼出 ①②③⑦⑨⑩ 六條 ✗，全部正確。

**「只修 `$name`（票文字面）夠不夠」——實測不夠**：把 main 版只改成 `${name}` 保留 `$want`／`$got`，bash 3.2 下改成在 **同一行**炸 `want<byte>: unbound variable`。→ 實作者把 `$want`／`$got`／`$must` 一併修**是同一個 bug class、且是達成驗收條件的必要條件**，不是擴權。

**全 repo 掃描（排除註解行）**，pattern `\$[A-Za-z_][A-Za-z0-9_]*[非 ASCII]`：`*.test.sh` 內**零殘留**（其餘命中全在 `#` 註解，不展開）。非 `.test.sh` 的命中逐條裁決見 informational-1。

### 2. 負向測試方法有效性：有一條分支 stub 法走不到（我已補驗）

`api-contract-check.test.sh:167`（`expect_catalog`）**走不到**——它是 `python3 "$py_checker" --catalog …`，直接呼叫 `api_contract_check.py`，不經被 stub 的 `.sh`。實作者 handoff 列的觸發清單（①②③⑦⑨⑩）正好缺 ⑬–⑮，與這個方法盲區完全吻合，也就是說**這一行修完沒被任何負向測試走過**。

我改 stub `api_contract_check.py`（固定 exit 0）補驗：
- main 版：`apimain.test.sh: line 167: name<byte>: unbound variable`（確認該行的修是 load-bearing）
- head 版：`✗ ⑭ catalog 大寫表名 fail loud（LS-54 N3）（期望 exit 1，實得 0）` / `✗ ⑮ …` 正確印出

→ 該行**已由 reviewer 實測覆蓋**，不需退修。其餘 ✗ 行：`error-codes-check.test.sh:72`（elif must-missing）在 main 上**本來就已是** `${name}`／`${must}`，line 197／line 220 無裸變數，皆無須動。

### 3. Scope 逐項裁決

- **`tracked-ignored-check.test.sh:36`＝真 bug，正確順修（非誤改）**。我讀 `origin/main` 原文確認是 `echo "✗ $name（期望 exit $want${must:+、輸出含「$must」}，實得 $got）"`——`$name（`／`$must」`／`$got）` 三處裸寫全中。票文寫「四支檔案改 `${name}`」本來就把它算進去（票文說它「已用此寫法」那句才是錯的）。head 保留 `$want` 裸寫**是對的**：它後接 ASCII `$`（`$want${must:+…}`），名稱在此終止，不受影響——實測全綠。
- **`$want`／`$got`／`$must` 擴修**：同 bug class、同一行、且必要（見 §1）。合理 scope。
- **順修 A**（`error-codes-check.sh:27-32`）：純註解。我對照了 `retired_mig_codes` 的實作（line 132/137/152：白名單登記後該碼被從 migrations 集合扣掉，若 §5 仍列著 → `retired_but_doc` 紅），敘述與程式碼一致，沒有寫出做不到的承諾。
- **COLLABORATION §7**：`--word-diff` 逐詞比對確認**只有新增＋兩處「落後」→「與遠端任何 sha 不等」改寫，零刪除**；LS-86（收尾 gate `cleanup-merged.sh`）、LS-95（`tap-target-registry-check`／`(^|/)LittleSprout/`／`ci` job diff 判斷）那些列**完全未被觸碰**，無回退。

### 4. 順修 B 行為：兩條都覆核過（雙 bash 版本）

**branch-ticket-check「先補票號」**（只有 `[無票號] wip`、無異票號時）：
- main 版重現了 bug：`刻意夾帶：本票 commit body 加獨佔一行 \`Bundles: \`（理由寫下一行）` ← 空的逃生口
- head 版：`先補票號：上面列的是 [無票號] 的 commit，Bundles 逃生口只認得「已有票號、屬於其他票」的 commit，這裡不適用——…`，`/bin/bash` 3.2 與 brew 5.3.15 輸出逐字相同，rc=1 不變。

**merge-conflict-check unrelated histories**（orphan branch 造無共同祖先、file:// 裸 repo 當遠端，git 2.47.0）：
- main 版：`git merge-tree 失敗（exit 128；--write-tree 需 git ≥ 2.38）`（誤導）
- head 版：`git merge-tree 失敗（exit 128；origin/development 與本分支沒有共同祖先，refusing to merge unrelated histories——分支起點可能切錯了）`，rc **仍是 2**（fail closed 未改變），兩個 bash 版本一致。

### 5. 測試品質：兩個 bash 版本、六支自測全綠

| test.sh | /bin/bash 3.2.57 | bash 5.3.15 |
|---|---|---|
| destructive-approval-check | rc=0（11 組） | rc=0 |
| api-contract-check | rc=0（15 組） | rc=0 |
| error-codes-check | rc=0（23 組） | rc=0 |
| tracked-ignored-check | rc=0（7 組） | rc=0 |
| branch-ticket-check | rc=0（19 組） | rc=0 |
| merge-conflict-check | rc=0（9 組） | rc=0 |

---

### Findings

**m1（minor）· `scripts/gates/branch-ticket-check.test.sh:92`、`scripts/gates/merge-conflict-check.test.sh`（9 組全體）**
順修 B 新增的兩條訊息分支**都沒有自測覆蓋**。
*失敗情境*：日後有人把 `branch-ticket-check.sh:91` 的 `if [ -n "$undeclared" ]` 條件寫反、或整個 else 分支被 refactor 掉，19 組仍全綠；merge-conflict-check 的 9 組樣本沒有任何一組讓 `merge-tree` 本身回 rc≥2，新的 `unrelated histories` 判斷寫壞（例如 grep pattern 打錯）同樣 9 組全綠。這正是本票在打的那種「自測給不出反饋」。
*建議修法*：兩支 `expect` 都已支援 `<輸出必含字串>` 參數，各 1 行即可——case ⑦ 的 must 由 `'[無票號] wip'` 改成（或新增一組）`'先補票號'`；merge-conflict 加一組 orphan branch 案例 `expect 2 '⑩ 無共同祖先 → exit 2 並點名' '沒有共同祖先'`。
*不擋本 PR 的理由*：兩條分支的實際行為我已手動實測正確（見 §4），這是覆蓋率缺口不是正確性缺陷；且票文驗收條件未要求。

**m2（minor）· `scripts/gates/branch-ticket-check.sh:88-95`**
混合情境（同時有 `[無票號]` 與異票號 commit）時只印 Bundles 那半邊，`[無票號]` commit 拿不到針對性指引。
*失敗情境*（已實測）：分支上有 `wip` ＋ `fix(db): LS-31 …` → 輸出只有「刻意夾帶：… `Bundles: LS-31`」。agent 照做補宣告、再 push，這次 `undeclared` 空了才看到「先補票號」——多一輪來回。
*建議修法*：改成兩個獨立 `if`（`[ -n "$undeclared" ]` 印 Bundles；`[ -n "$noticket" ]` 印先補票號），彼此不互斥。
*不擋的理由*：仍嚴格優於 main（純無票號情境 main 印空的 `Bundles: `），且 `非刻意：… rebase` 那行對無票號 commit 仍然適用。

**INFO-1 · 同 bug class 在票外仍有 2 處活的實例：`scripts/ops/demo-refresh.sh:38`（`（rc=$build_rc）`）、`:41`（`$SIM_NAME（$udid）`）**
該檔 shebang `#!/bin/bash`、`set -uo pipefail`、**無** `export LC_ALL=C`。實測 3.2＋UTF-8 LC_CTYPE 下兩行都炸 `unbound variable`；其中 **line 41 在成功路徑上**——demo 明明已 install＋launch 成功，腳本卻以 rc=1 收場。
實作者沒動它**是對的**（票外、surgical 原則）；建議記入 LS-96 待辦池或另票。
*已排除的偽陽性（供後續參考，別再誤修）*：`scripts/gates/migration-breaking-check.sh:76` 的 `$known_file（` 因該檔 line 58 有 `export LC_ALL=C` 而**不會**炸（我實跑 `--known-functions /nonexistent/…` 確認訊息正確）；`scripts/gates/pr-body-check.sh:42` 的 `\$HEAD）` 是跳脫的字面 `$`，不展開。

**INFO-2 · 修了實例、沒加 gate（前饋無反饋）**
這個 bug class 已經是第三次現身：LS-51 的 `tracked-ignored-check.test.sh`、本票三支、以及 `supabase/tests/run.sh`（LS-110 已修，檔內留了三段註解記同一個坑）。CLAUDE.md 最高原則是「新增重要規則＝同時新增它的 gate」，本 PR 只清實例。
*建議*：另開 harness 票，在 CI `rules` job 加約 5 行掃描——對 `git ls-files '*.sh'` 跑 `perl -ne 'next if /^\s*#/; …/\$[A-Za-z_][A-Za-z0-9_]*[^\x00-\x7F\s]/'`，命中即紅（可豁免有 `export LC_ALL=C` 的檔，或乾脆一律要求 `${}`）。超出本票 scope，不擋本 PR。

**INFO-3 · `docs/COLLABORATION.md:215` 新增盲區措辭略寬**
「detached HEAD 或推的是非目前 checkout 分支的 ref 時這兩步一起靜默跳過」——對照 `push-gate.sh:371-378`：只有 `main|test|development|DETACHED` 才跳過；**站在 `feature/LS-1` 卻 push `feature/LS-2`** 時不是跳過，而是拿 HEAD 的分支去驗（＝驗錯對象，可能假綠也可能誤擋）。措辭是照票文原文抄的，非實作者臆造，但描述的失敗模式與實際不完全一致。下次順修時可修為「detached／站在保護分支時跳過；推非目前 checkout 分支時驗到的是錯的分支」。

---

### 沒審／沒跑的範圍（誠實聲明）

- 沒有重跑 CI 本身，只讀 `gh pr view` 的結論（四個 job 皆 SUCCESS）。
- 順修 A 是純註解，我核對了敘述與 `retired_mig_codes` 程式碼一致，但**沒有**為「`CREATE OR REPLACE` 拿掉 raise → 三方聯集假綠」構造合成 migrations 實跑該情境。
- 本 PR 未動 Swift／supabase/tests／CI workflow，這些範圍未審。

*取證用暫存檔全部在 scratchpad（`LS-59-repro`／`LS-59-stub-*`／`LS-59-mcc`／`LS-59-btc`），未進 repo；worktree `.claude/worktrees/LS-59` 審後 `git status` 仍乾淨。*


### 2026-09-01T12:29:20.379Z · Chih-Lin Yeh · `0eb492a0`

## Orchestrator retro（收尾儀式；純 harness 票，dead-code-sweeper 免跑）

**過程**：實作（主體＋兩組順修，一輪）→ merge-review APPROVE（無 blocker/major，2 minor＋3 informational 記 LS-96）→ 併 main `9803b0d`、back-merge PR #201。單輪過審——size:S、票文即完整規格的票型效率最好。

**Lesson learning**：
1. **Reviewer 補根因**（locale 相依）證明「審查不只驗修法、也驗『為什麼是這個修法』」有價值——`${}` vs `export LC_ALL=C` 的取捨從此有據可查。
2. 實作者超出票面字面（`$want`/`$got`/`$must`）被 reviewer 驗證為**必要**而非 scope creep——「同 bug class 內的擴修」是合理 scope 的邊界範例，與虛構引據（LS-119 R2）的差別在可驗證性。
3. 此 bug class 第三次出現（INFO-2）→ 已達升票門檻，記在 LS-96 待 harness lane 空檔升「rules job 掃描 gate」票。
4. tracked-ignored-check.test.sh 一案：**票文本身的引據也可能錯**（說它是好範例、實際同 bug）——reviewer 對票文引據做來源查證再次抓到問題，此手法已固化進派審 prompt。

informational 處置：m1/m2/INFO-1–3 已記 LS-96（comment 見池）。

### 2026-09-01T12:44:57.831Z · Chih-Lin Yeh · `1c6b8dd5`

結案：PR #200 併 main `9803b0d`（merge-review=success on `fc9f2bd`，linear:932d41a2）；back-merge PR #201 併 development `ac3f715`（§2 驗證：merge-tree 無內容差、status 貼 `9803b0d` id 53295755206；CI 全綠）。收尾儀式完成——純 harness 免 sweeper、retro（comment `0eb492a0`）、informational 記 LS-96（comment `2e92ff55`）、清理：`cleanup-merged.sh --apply LS-59`。

