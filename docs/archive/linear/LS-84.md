# LS-84 DB 測試：60_ §2 private 函式授權檢查改通掃＋例外名單（手寫列舉漏 enforce_child_not_deleted／enforce_children_family_immutable）＋87_ §8 anon 半段斷言形狀

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | lane:backend |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M2 孩子檔案 |
| 估點 | — |
| 建立 | 2026-08-25T05:17:43.829Z |
| 開始 | 2026-08-25T05:38:10.184Z |
| 完成 | 2026-08-25T06:26:12.851Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-84/db-測試60-2-private-函式授權檢查改通掃例外名單手寫列舉漏-enforce-child-not-deletedenforce |
| 父票 | LS-64 LS-58 收尾小修：API.md §2 引用對齊＋private.record_notification_event 授權邊界測試 |
| 關係 | related→LS-30, related→LS-70, related←LS-197 |

## 描述

## 來源

[LS-64](https://linear.app/little-sprout-app/issue/LS-64/ls-58-收尾小修apimd-2-引用對齊privaterecord-notification-event-授權邊界測試) PR #124 review F4（informational，超出該票範圍）＋F1（minor）。

## 項目

1. **F4（結構性假綠）**：`supabase/tests/60_default_privileges.sql` §2（:43-87）是手寫列舉「不得對 authenticated 開放的 private 函式」，名單外的函式（已確認漏 `private.enforce_child_not_deleted()`／`private.enforce_children_family_immutable()`）若日後被 `grant execute … to authenticated`，§2／§3 都掃不到、三道 gate 全綠。改法：§2 改為對 `private` schema 全體函式通掃（`pg_proc` join `pg_namespace`）＋顯式例外名單（目前應為空；有例外必附理由註解），比照同檔 §9 的 fail-open 防呆（通掃結果為 0 支即 FAIL）。
2. **F1（87\_ §8 anon 半段）**：anon 無 schema USAGE，`private.` 下任何名稱都 42501，六句斷言退化為一句事實（實測 `private.this_function_does_not_exist_xyz()` 對 anon 也 42501）。改為：一次呼叫斷言 `sqlerrm like '%schema private%'`＋註解說明鎖的是 schema USAGE；或保留六句但斷言訊息形狀。
3. mutation 自證：對 §2 通掃刻意 grant 一支名單外函式 → 紅；還原 → 綠。寫進檔頭。

## 分支

feature 從 development；無 migration。

## 留言（6）

### 2026-08-25T05:47:55.707Z · Chih-Lin Yeh · `cc43f3e6`

## ios-dev：PR #127 → development｜push 完成，交 handoff（不等 CI）

分支 `feature/LS-84-private-fn-privilege-sweep`（origin/development 切出）。無 migration。PR：https://github.com/CLYEH/little-sprout/pull/127

### 已完成
1. `supabase/tests/60_default_privileges.sql` §2：刪除手寫 7 支 trigger 函式拒絕列舉，改為對 schema private 全體函式通掃，扣掉 `v_allow_fns`（6 支 policy 函式，正向對照不變）與 `v_exceptions`（額外例外，目前為空，附註若日後新增需理由），剩下 15 支逐一斷言 `has_function_privilege('authenticated'/'anon', oid, 'execute') = false`；比照同檔 §9 補上 fail-open 防呆（通掃候選數 0 支即 FAIL）。
2. `87_comments_reactions_notifications.sql` §8 anon 半段：六句各自斷言 sqlstate=42501 改為一次呼叫＋斷言 `sqlerrm like '%schema private%'`，註解說明鎖的是 schema USAGE、不是函式層；authenticated 半段未動。
3. 87_ 檔頭 M4 補一句交叉引用：四支 `notify_*` 的紅實際是 0A000（非 v_err=NULL），理由指向 §8 開頭既有說明。
4. Mutation 自證（寫入 60_ 檔頭）：交易內對 `private.enforce_child_not_deleted()`（review F4 點名的漏網函式）額外 `grant execute … to authenticated` → §2 精準點名該函式判紅；`revoke` 還原後重跑變綠。

### 已驗證（怎麼驗的）
- 單獨對兩支改動檔案跑 `docker exec supabase_db_little-sprout psql … < <file>`：全綠，§2 通知列印「通掃 15 支非允許清單函式」、「允許清單內的 6 支…」，確認掃到的候選數與手動列舉的私有函式總數（21 支）減允許清單（6 支）一致。
- Mutation 自證親自重演（非只讀程式碼）：begin 內 grant → §2 psql 執行拿到 `ERROR: FAIL：schema private 這些函式（不在允許清單／例外清單內）… private.enforce_child_not_deleted()`；`has_function_privilege` 探測確認 grant 無殘留；還原後單獨重跑 §2 兩則 NOTICE 皆綠。
- `bash supabase/tests/run.sh` 全綠（含 concurrency 場景，尾端印出「✓ 全部 DB 測試通過」）。
- `bash scripts/gates/api-contract-check.sh`：RPC 23 支／表 18 張，通過。
- `bash scripts/gates/error-codes-check.sh`：22 個自訂碼三方一致，通過。
- push gate（unit tests + lint）通過；`git merge-tree` 與 origin/development 可乾淨合併。
- 分段 commit：`test(db): LS-84 60_ §2 通掃`（a57f434）、`test(db): LS-84 87_ §8 anon 斷言＋M4`（6433a4e）。
- PR body 先過 `scripts/gates/pr-body-check.sh --branch feature/LS-84-private-fn-privilege-sweep`，通過後才 `gh pr create`。

### 未完成／informational 處置
無新增 informational finding——本票是承接 LS-64 PR #124 review 0d239507 的 F1（已修，改斷言形狀）／F2（已修，補 M4 交叉引用）／F4（已修，改通掃＋例外名單）。同一輪 review 的 F3／F5／F6 已在 LS-64 orchestrator comment 處置（不修，理由已記），不在本票範圍內，未重複處置。

### 風險
- 通掃邏輯依賴 `v_allow_fns` 與 `v_exceptions` 兩份陣列持續與實際 schema 對齊；若日後 policy 函式改名而漏改 `v_allow_fns`，該函式會落入通掃的拒絕檢查，测试會主動 FAIL（fail loud），不是靜默漏測，風險可控。
- 未新增 migration，無 RLS/破壞性變更疑慮。

### 產出位置
- `/Users/clyeh/little-sprout/.claude/worktrees/LS-84/supabase/tests/60_default_privileges.sql`
- `/Users/clyeh/little-sprout/.claude/worktrees/LS-84/supabase/tests/87_comments_reactions_notifications.sql`
- PR：https://github.com/CLYEH/little-sprout/pull/127

### 2026-08-25T05:59:25.846Z · Chih-Lin Yeh · `8be69ff7`

## merge-reviewer：PR #127 → development｜verdict **APPROVE**

base `origin/development`／head `feature/LS-84-private-fn-privilege-sweep`（6433a4e）。
diff：`supabase/tests/60_default_privileges.sql`（+104/-53 區間）、`supabase/tests/87_comments_reactions_notifications.sql`（§8＋M4）。共 2 檔，無 migration。

無 blocker、無 major。2 條 minor（皆為註解行號引用失準）＋2 條 informational，不擋合併。

環境：容器 `supabase_db_little-sprout`（healthy，PG 17.6，`lc_messages=en_US.UTF-8`）；跑前 `pgrep` 確認無其他 run.sh／db reset 併發，全程獨佔。所有探測都在 `begin … rollback` 內，事後驗過無殘留、worktree `git status --porcelain` 為空。

---

### 一、通掃的正確性（審查重點 1）——親自重演，不是只讀 diff

**1-a `v_allow_fns` 6 支逐支查證：確實被 policy 以「呼叫者身分」求值，不是 SECURITY DEFINER 內部呼叫。** 直接查 `pg_policies`：

| 函式 | 出現在 | 求值身分 |
|---|---|---|
| `family_ids()` | `albums/media/diaries/comments/reactions/children/family_members/feed_items/blocked_users/content_reports/families` select 系 policy＋`storage.objects media_bucket_select` | 呼叫者（authenticated） |
| `owned_family_ids()` | `albums_delete`／`families_update`／`invites_*`／`join_requests_select`／`media_delete`／`storage media_bucket_delete/update` | 同上 |
| `contributor_family_ids()` | `albums_insert/update`／`album_media_*`／`diary_media_*` | 同上 |
| `uploadable_family_ids()` | `media_insert/update`／`storage media_bucket_insert/update/delete` | 同上 |
| `peer_profile_ids()` | `profiles_select` | 同上 |
| `is_media_object_path(text)` | `storage.objects media_bucket_insert`／`media_bucket_update` 的 WITH CHECK | 同上 |

6 支全部命中、無一支是「只被 definer 函式內部呼叫」而混進允許清單的。**允許清單正當且最小**。反面也查了：15 支被通掃的函式沒有任何一支出現在 policy 裡（trigger 函式由 trigger 機制觸發，不需要呼叫者持 EXECUTE），拿掉 authenticated EXECUTE 不會誤傷。

**1-b `v_exceptions` 為空 ✓**，且 `coalesce(array_agg(...), array[]::oid[])` 的寫法與同檔 §9 已論證過的 NULL 陷阱一致。

**1-c 扣除後 15 支 ✓**：DB 實查 `private` 共 21 支函式，21 − 6 = 15，與程式印出的 `通掃 15 支` 及 handoff 相符。

**1-d overload 問題：沒有漏掃——實作是以 `oid` 為單位，不是 `proname`。** 我直接造了同名不同簽名的 overload 驗證：

```
begin;
create function private.family_ids(p uuid) ... ;
grant execute on function private.family_ids(uuid) to authenticated;
→ ERROR: FAIL：schema private 這些函式（不在允許清單／例外清單內）對 authenticated
          或 anon 開放了 EXECUTE —— private.family_ids(uuid)
```

允許清單用 `f::regprocedure::oid` 解析成特定簽名，`family_ids()` 的放行不會外溢到 `family_ids(uuid)`。**這個提問的答案是「不會漏」，且是設計使然而非巧合**（目前 21 支剛好無 overload，所以這點只能靠人造 overload 證明，我造了）。

**1-e 候選 0 支即 FAIL 的防呆真的會觸發**——三種打壞法都試過：

| 打壞方式 | 結果 |
|---|---|
| schema 名打錯（`'privateX'::regnamespace`） | `ERROR: schema "privatex" does not exist`（硬錯，fail loud，不會靜默綠） |
| 例外清單意外涵蓋全部 21 支 | `ERROR: FAIL：§2 通掃結果為 0 支 private 函式……防呆攔截` ✓ |
| 允許清單被清空（`array_agg`→NULL 的經典 fail-open） | 同上防呆攔截 ✓（NULL 陣列讓 WHERE 篩不出列 → count 0 → 被擋） |
| 允許清單有錯字（函式不存在） | `ERROR: function "private.family_idsXX()" does not exist`（cast 直接噴，不會無聲漂移） |

**四條退化路徑全部 fail loud，沒有一條會靜默通過。**

**1-f 額外查證：§2 的涵蓋面比舊列舉嚴格更廣。** `has_function_privilege` 含角色繼承，所以 `grant execute … to public` 也會被 §2 抓到（實測 `grant execute on function private.feed_sync_media() to public` → §2 判紅）。舊列舉只驗 7 支、只驗直接 grant；新通掃 15 支且涵蓋 PUBLIC 路徑。

### 二、Mutation 自證親自重演（審查重點 2）

不是複述檔頭，是我自己重跑：

```
begin; grant execute on function private.enforce_child_not_deleted() to authenticated;
→ ERROR: FAIL：… —— private.enforce_child_not_deleted()   ← 精準點名
rollback;

begin; grant execute on function private.enforce_children_family_immutable() to anon;
→ ERROR: FAIL：… —— private.enforce_children_family_immutable()   ← anon 半段同樣活著
rollback;
```

**F4 當初點名的兩支漏網函式，現在兩個方向（authenticated／anon）都被實測攔下。** 檔頭 M1 只寫了 authenticated 那一半，我補驗了 anon 半邊也有效。

未 grant 的 baseline 重跑為綠（`通掃 15 支` / `允許清單內的 6 支`）。**殘留探測**：21 支 × {authenticated, anon} → `auth_exec=6, anon_exec=0`，且那 6 支逐一比對就是允許清單本身。**共用容器無污染。**

### 三、87_ §8 anon 半段（審查重點 3）

**3-a 訊息措辭在本地確實含 `schema private` ✓**——PG 17.6／`lc_messages=en_US.UTF-8` 實測：

```
anon 呼叫既有函式     → sqlstate=42501 msg=[permission denied for schema private]
anon 呼叫不存在的函式 → sqlstate=42501 msg=[permission denied for schema private]
```

兩者逐字相同，**F1 的診斷（六句退化成一句事實）在本機重現確認**。

**3-b 新斷言不是恆真的空案，而且比舊版嚴格更強**——這點是我這輪最有價值的發現：

| 情境 | 舊版（6 句 sqlstate=42501） | 新版（1 句＋訊息形狀） |
|---|---|---|
| 現況 | 綠 | 綠 |
| `grant usage on schema private to anon`（真實的安全退化） | **綠（漏掉！）** | **紅** ✓ |
| usage + execute 全開 | 紅 | 紅 ✓ |

實測新版 A1 的紅訊息：`實際 sqlstate=42501，訊息=permission denied for function record_notification_event`。**舊版在 anon 拿到 schema USAGE 時會維持綠（sqlstate 仍是 42501），新版才抓得到**——這是淨增的覆蓋，不是單純的精簡。

砍掉的另外 5 句沒有損失覆蓋：只要 anon 無 schema USAGE，`private.` 底下任何名稱都到不了函式層；而「anon 被單獨 grant EXECUTE 但沒有 USAGE」這個殘餘情境由 60_ §2 的 anon 半段接住（上面 2 已實測）。**兩檔合起來把 schema 層與函式層兩個維度都封住了，沒有縫。**

**3-c M4 交叉引用與程式一致 ✓**——新增句宣稱四支 `notify_*` 的紅是 0A000，我實測：

```
grant execute on function private.notify_album_created() to authenticated;
authenticated 直呼 → sqlstate=0A000 msg=[trigger functions can only be called as triggers]
```

**與新增的敘述逐字相符**，因果順序（無 EXECUTE 先卡 42501；有 EXECUTE 才走到 0A000）也成立。

### 四、刪掉手寫列舉是否讓 §2 失去「特定函式必須存在」的斷言（審查重點 4）

**確實失去了，但我查證後結論是不需要補 `count >= N`。**

舊 §2 的 `has_function_privilege('authenticated', 'private.feed_sync_albums()', 'execute')` 其文字→`regprocedure` cast 在函式不存在時會噴錯，等於隱含存在性斷言；新通掃只斷言 `v_swept <> 0`，7 支 trigger 函式若被刪不會在 §2 反映。逐支追行為面覆蓋：

| 函式 | 行為面覆蓋（刪掉會紅的地方） |
|---|---|
| `add_creator_as_owner` | `00_fixtures.sql:163`（明確斷言「trigger 可能沒有生效」） |
| `feed_sync_albums/media/diaries` | `40_triggers_feed_and_storage.sql:31,51,56,60,66,78` |
| `media_storage_sync` | `40_:151-209`（用量加減四路徑） |
| `enforce_family_has_owner` | `30_owner_invariant.sql:22,38,47,74` ＋ `concurrency/owner_guard_*` |
| `enforce_deletion_attribution` | `88_deletion_attribution.sql` |
| `enforce_child_not_deleted`／`..._family_immutable` | `95_children_backend.sql` |

**7 支全部有行為面覆蓋，存在性訊號沒有真的消失，不必補 `count >= N`（那也只是弱斷言：15 支砍到剩 1 支照樣過）。** 記為 informational（F3），因為這份冗餘現在是承重的：日後若有人精簡 40_／30_／88_，存在性訊號會一起消失而無人察覺。

### 五、四維

- **Race condition：N/A（明說）**。無 Swift、無 runtime 併發改動。相關性質已查：§8 收尾有 `reset role`，斷言失敗時雖跳過 `reset role`，但 exception 會 abort 交易且 psql 帶 `ON_ERROR_STOP` 直接結束 session，無殘留——與改動前行為一致，未引入新風險。§2 純唯讀 catalog 查詢。
- **運算效能：無可測增幅**。各跑 3 次取平均：60_ `0.094s → 0.068s`（通掃的單句集合查詢取代 14 次逐支 `has_function_privilege` 迴圈，方向上還略快）、87_ `0.082s → 0.089s`，皆落在 docker exec 啟動噪音內。完整 `bash supabase/tests/run.sh` **55.4s 全綠**（尾端「✓ 全部 DB 測試通過」），時間由 concurrency 的 `pg_sleep` 主導，本 PR 無貢獻。
- **平行優化：N/A**。單 session 序列斷言，微秒級，無可平行化空間被浪費。未引入新的共享狀態假設（不建 fixture、不留 grant），容器共用前提與改動前相同。
- **Scope：乾淨**。`git diff --name-only` 恰為票文指定的 2 檔；60_ 改動限於檔頭 mutation 記錄＋§2，87_ 限於 M4 補句＋§8 anon 區塊（authenticated 半段零改動，已逐行確認）。無順手重構、無無關改動，符合手術式修改原則。`git merge-tree origin/development HEAD` 乾淨可合併。

---

### Findings

**F1 — minor — `supabase/tests/60_default_privileges.sql:92`：本 PR 自己讓自己引用的行號失準**

該行註解寫「fail-open 防呆（比照第 9 段 **60_:506**）」。`:506` 在 base 版本正確（指向 §9 的 `coalesce … array_agg 對零列取值會回 NULL` 那段論證），但本 PR 的 §2 擴寫讓 §9 整體下移約 48 行——**HEAD 的 :506 現在落在 §8「public schema 白名單」的查詢中段**，與 fail-open 完全無關；正確位置是 `:552`。

**具體失敗情境**：日後有人依這個指標去找「比照的先例」，翻到 :506 看到不相干的 public schema 查詢，可能誤判這個「比照」是虛構的、或以為 §9 的防呆已被移除。符號部分（「第 9 段」）仍正確，所以讀者有機會自救，故列 minor 不列 major。

**建議**：拿掉數字只留「第 9 段」（行號引用本質上會被任何編輯打壞，這次就是自己打壞自己），或更新為 `60_:552`。

**F2 — minor — `supabase/tests/87_comments_reactions_notifications.sql:57-58`（M4 新增句）：同一類問題，同一個成因**

新增句寫「理由見 §8 開頭 **:1003-1010** 的因果說明」。`:1003-1010` 在 base 正確，但本 PR 的 M4 插入使其後所有內容下移 **+7 行**，實際位置已是 `:1010-1017`。更麻煩的是 HEAD 的 :1003-1009 現在剛好是「放 87_ 而不是 60_ 的理由」那段（即 PR #124 review F3 點出、orchestrator 決定不修的那段論述）——**指標精準地落在一段講別件事的文字上**，讀者不會馬上發現指錯了。

**具體失敗情境**：維護者依 M4 指引去確認「四支 notify_* 為何是 0A000」，讀到的卻是「為何放在 87_ 而非 60_」，得不到答案且不會意識到是引用失準（因為那段文字本身也通順）。

**建議**：拿掉數字只留「§8 開頭」，或更新為 `:1010-1017`。

> F1／F2 是同一個根因：**在會被自己的 diff 位移的檔案裡寫死行號**。兩處都是本 PR 新增的引用，不是既有債。合併前一併改掉最省事；若要另票收也可以，因為兩處的符號指標（§9／§8 開頭）都仍正確，讀者不會被導向錯誤結論、只會多繞一圈。

**F3 — informational — `60_default_privileges.sql:71-137`：通掃取代列舉後，7 支 trigger 函式的隱含存在性斷言消失（已查證有他處覆蓋，不需補）**

理由與逐支覆蓋對照表見上方「四」。**不建議補 `count >= N`**：那是弱斷言（15 支砍到剩 1 支照樣過），真正的保障是 40_／30_／88_／95_／00_fixtures 的行為面測試。記錄的目的是留下「這份冗餘是承重的」這個事實，供日後精簡那幾支測試時參考。

**F4 — informational — `60_default_privileges.sql:117-119`：PUBLIC 洩漏時 §2 的建議修法會把人帶偏一步**

§2 現在也會抓到 `grant execute … to public`（我實測過，見 1-f），但 FAIL 訊息的建議是「先到本段 `v_allow_fns` 登記並附理由」——對 PUBLIC 洩漏而言正確修法是 `revoke execute … from public`（§3 的訊息才對）。因為 §2 排在 §3 之前，PUBLIC 洩漏會先由 §2 報出。

**具體失敗情境**：某 migration 誤寫 `grant … to public` → §2 紅 → 開發者照建議把函式登記進 `v_allow_fns` → §2 綠。**傷害有上限**：§3 沒有例外清單、對全體 private 函式掃 PUBLIC，所以 §3 仍會判紅、洞不會真的漏出去，只是多繞一次冤枉路。另外此環境有 owner=postgres 的全域 `ALTER DEFAULT PRIVILEGES … function → postgres,service_role`（實測新建 private 函式的 proacl 是 `{postgres=X,service_role=X}` 而非 NULL），所以 PUBLIC 這條路徑必須有人顯式 `grant to public` 才會發生，機率更低。**不建議在本票改**（會擴大 scope），記錄備查。

---

### 未看的範圍（誠實聲明）

- **未跑 CI**：僅本機容器驗證。未確認 GitHub Actions db job 的 PG 版本／`lc_messages` 與本機一致——這對 F2 提到的 `sqlerrm like '%schema private%'` 有理論上的相關性（若 CI 以非英文 locale 跑，該斷言會誤紅）。我查了本機是 `en_US.UTF-8`，且 supabase CLI 映像對本機與 CI 是同一份，風險低但我沒有實際去 CI 上證實。
- **未複驗 `api-contract-check.sh`／`error-codes-check.sh`**：本 PR 不動 RPC、表、錯誤碼，這兩道 gate 的輸入面為零改動，我改為把時間花在 §2 通掃的四條退化路徑與 §8 的 mutation 對照上。handoff 宣稱的這兩項綠燈我未複驗。
- **未逐支複驗 87_ 其餘 33 則 notice 的內容正確性**：只確認整檔無 ERROR、notice 數 34。§8 以外的段落本 PR 未改。

**verdict：APPROVE。** F1／F2 兩處行號引用建議在合併前順手改掉（各一行、零風險）；F3／F4 記錄備查，不需動作。

### 2026-08-25T06:02:16.812Z · Chih-Lin Yeh · `483832f2`

## ios-dev：PR #127 review 8be69ff7 R1 處置 → 已 push（c2aad8d），不等 CI

merge-reviewer 對 PR #127 review 0d239507（不重審，同一輪 comment `8be69ff7`）：**APPROVE**，F1／F2 是本 PR 自己引入的 minor（寫死行號），F3／F4 informational 不修。

- **F1**：`60_default_privileges.sql:92` 註解「比照第 9 段 60_:506」→ 已修，改為「比照第 9 段」（去掉會漂移的行號）。
- **F2**：`87_comments_reactions_notifications.sql` M4 段落「理由見 §8 開頭 :1003-1010」→ 已修，改為「理由見 §8 開頭」。
- **F3**：informational，不修（orchestrator 指示）。
- **F4**：informational（PUBLIC 洩漏修法訊息偏差），不修，另票時再處理（orchestrator 指示）。

已驗證：改動後單獨對兩支檔案 `docker exec supabase_db_little-sprout psql … < <file>` 重跑，兩支皆全綠（純註解字面改動，無邏輯變化）；push gate（unit tests + lint + api-contract + error-codes + 分支票號 + 與 origin/development 可乾淨合併）通過。commit `test(db): LS-84 R1 F1/F2 註解去行號`（c2aad8d），已 push。

### 2026-08-25T06:02:41.400Z · Chih-Lin Yeh · `187bd863`（回覆 8be69ff7）

已處置（見上方新 comment）：F1／F2 已修（去行號），commit c2aad8d 已 push；F3／F4 informational 不修，F4 另票時再處理。感謝重演驗證。

### 2026-08-25T06:20:40.773Z · Chih-Lin Yeh · `cddcf2fd`

## QA：LS-84 驗收 → **PASS**

環境：`test` branch（HEAD `4ae98fa`，LS-84 併入於 `c361149`／commits `a57f434`／`6433a4e`／`c2aad8d`）；worktree `.claude/worktrees/qa-test`（detached，驗收後 `git status --porcelain` 為空，未留痕）。所有 DB 操作皆經 `scripts/ops/supabase-lock.sh` 序列化（LS-70），未裸跑共用容器。`gh pr view 127`：4 項 CI checks（rules／db／lint／ci）皆 SUCCESS，已 MERGED。

### 逐條驗收

**1. `supabase db reset` + `run.sh` 全綠，60_ 印「通掃 15 支」、87_ §8 兩則 ok — ✓**
- `bash scripts/ops/supabase-lock.sh -- supabase db reset`：15 支 migration 全部套用成功。
- `bash scripts/ops/supabase-lock.sh -- bash supabase/tests/run.sh`：尾端 `✓ 全部 DB 測試通過`。
- 逐檔確認：
  - `60_default_privileges.sql:263` `ok：schema private 通掃 15 支非允許清單函式，authenticated／anon 皆無 EXECUTE`
  - `60_default_privileges.sql:264` `ok：允許清單內的 6 支 private 函式只有 authenticated 可執行`
  - `87_comments_reactions_notifications.sql:598` `ok：authenticated 直呼六支 LS-58 private 函式…皆 42501`
  - `87_comments_reactions_notifications.sql:599` `ok：anon 呼叫 private. 底下任何名稱皆卡在 schema USAGE（42501 permission denied for schema private）`
  - 兩檔皆 `✓`，全套件（含 concurrency）無一失敗。

**2. 親自 mutation：交易內對 `private.enforce_child_not_deleted()` grant execute to authenticated → 單跑 60_ 必紅；rollback → 綠；`has_function_privilege` 無殘留 — ✓**
- 透過 lock 執行 `begin; grant execute on function private.enforce_child_not_deleted() to authenticated;` 後緊接整份 `60_default_privileges.sql`（單一 psql session，未 commit）：
  ```
  ERROR:  FAIL：schema private 這些函式（不在允許清單／例外清單內）對 authenticated 或 anon 開放了 EXECUTE
  —— private.enforce_child_not_deleted()（…）
  exit code: 3
  ```
  精準點名該函式，非泛泛而紅。
- 殘留檢查（同一 lock 內、psql session 已斷線＝交易自動 rollback）：`has_function_privilege('authenticated', …)=f`、`has_function_privilege('anon', …)=f`，無殘留 grant。
- 重跑（無 mutation）：獨立重新對 `60_default_privileges.sql` 執行，exit code 0，`ok：schema private 通掃 15 支…` 再度出現，確認回綠。

**3. anon 斷言：`set role anon` 直呼 `private.record_notification_event(...)` 得 42501 且訊息含 `schema private` — ✓**
- `begin; set local role anon;` 後直呼該函式並在 exception handler 印 `sqlstate`／`sqlerrm`：
  ```
  NOTICE:  sqlstate=42501, message=permission denied for schema private
  ```
  逐字含「schema private」，與 87_ §8 anon 段斷言一致。

**4. 雲端：無 migration，N/A；模擬器：不適用，N/A — ✓（已核實非跳過）**
- `git diff --stat <development merge-base> c361149 -- supabase/migrations` 為空；LS-84 三支 commit 僅改 `supabase/tests/60_default_privileges.sql`、`supabase/tests/87_comments_reactions_notifications.sql` 兩支測試檔，無 migration，無 Swift/UI 變更——雲端部署與模擬器視覺驗收確屬本票範圍外，非省略。

### 回歸／RLS 冒煙
本票純測試檔修改，無 RPC／schema／Swift 變更，登入／時間軸／照片上傳／留言四條主流程不受影響，未額外跑 app 端冒煙（範圍不涉及）。RLS 冒煙即本票驗收條件本身（跨家庭 LS026、schema private 授權邊界），已在上方 1-3 覆蓋，`run.sh` 全數斷言（含既有的 cross-family／owner-invariant／concurrency 案例）全綠。

### Review informational 處置核對
merge-reviewer PR #127 review `8be69ff7`：F1／F2（minor，行號引用漂移）已由 ios-dev 修正並 push（`c2aad8d`，本次驗收已包含在 HEAD 內）；F3／F4（informational）依 review 記錄「不修，備查／另票」，不在本票動作範圍——與 LS-84 handoff 一致，未見遺漏。

### 未完成
無。

### 風險
無新增風險；通掃邏輯的既有風險（`v_allow_fns`／`v_exceptions` 需與 schema 對齊，若漏改會 fail loud 而非靜默漏測）已在 review 中記錄，QA 未發現額外問題。

**裁決：PASS**——驗收條件 4 條全數 ✓，證據見上；命令輸出摘要另存 `/private/tmp/claude-501/-Users-clyeh-little-sprout/6e4c757c-a7ba-4756-8bed-60b72847734c/scratchpad/LS-84-run-full.log`（本機暫存，非 repo 產出）。

### 2026-08-25T06:21:47.033Z · Chih-Lin Yeh · `ddceb220`

## 收尾儀式（orchestrator，2026-08-25）

**① dead-code 巡檢**：本票只動兩支測試檔、無 migration／Swift；merge-reviewer 逐行審＋mutation 重演、QA 再親驗一次，死碼面為零——orchestrator 裁定免跑 sweeper（比照純 harness 票），理由記錄於此。

**② Lesson learning review**
- **Gate**：本票本身把 LS-64 review F4 的結構性假綠（手寫列舉）改成通掃＋例外名單＋0 支防呆；reviewer 驗證 overload 以 oid 為單位、四條退化路徑全 fail-loud。教訓已在 LS-64 retro：**授權類測試一律通掃**——LS-30 之後由 harness 票在 COLLABORATION §6 加一句慣例。
- **Review 1 輪 APPROVE＋2 minor（本 PR 自己寫死的行號）**：教訓：**會被自己 diff 位移的檔案裡不要寫行號**，寫段落／符號。
- **並行**：實作期間 `pgrep` 等待規則生效一次（QA 併發）；QA 全程經 LS-70 lock。
- **派工／model**：恰當。

**③ 部署**：無 migration，N/A。→ 隨 test→main 晉升併入後 Done。

