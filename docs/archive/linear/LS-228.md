# LS-228 Harness：`scripts/gates/handoff-evidence-check.sh` 證據錨點白名單補 `.ts`／`.sql`／`.md` 路徑（`supabase/functions`、`supabase/migrations`、`supabase/tests`、`docs`）——backend／sweeper handoff 一日三次誤報（LS-96 `acb4e2df`）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:S, lane:harness |
| Cycle | 2 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-06T14:54:31.097Z |
| 開始 | 2026-09-06T14:55:47.344Z |
| 完成 | 2026-09-12T02:52:29.938Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-228/harnessscriptsgateshandoff-evidence-checksh-證據錨點白名單補-tssqlmd |
| 關係 | related→LS-226, related→LS-207, related→LS-217, related→LS-186, related→LS-208, related→LS-96, related→LS-222, related→LS-223, related←LS-227, related←LS-225, related←LS-215 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項 `acb4e2df`（[LS-222](https://linear.app/little-sprout-app/issue/LS-222/taskls-20-後端-purge-storage-孤兒掃描後續tssql-媒體路徑規則單一來源is-media-object-path) merge-review R1 首報）：`handoff-evidence-check.sh` 判定一列「有主張、無證據」時，只認 Swift 檔路徑／xcresult／截圖／SQL 輸出等錨點，**不認** `supabase/functions/*.ts`、`supabase/migrations/*.sql`、`supabase/tests/*.sql`、`docs/*.md` 的「檔案:行號」引用。2026-09-06 同一天撞三次：[LS-222](https://linear.app/little-sprout-app/issue/LS-222/taskls-20-後端-purge-storage-孤兒掃描後續tssql-媒體路徑規則單一來源is-media-object-path) R1 reviewer、[LS-223](https://linear.app/little-sprout-app/issue/LS-223/taskls-20-後端-purge-storage-孤兒掃描收口候選預算扣除不合法路徑f2不合法路徑改計數樣本回報f1舊-rpc) R1 reviewer、[LS-222](https://linear.app/little-sprout-app/issue/LS-222/taskls-20-後端-purge-storage-孤兒掃描後續tssql-媒體路徑規則單一來源is-media-object-path)＋223 sweeper（comment `3cbe31bb`：9 列中 3 列誤報，agent 只能把 gate 全文貼進 comment 說明「不是造假」）。誤報讓 fail-closed gate 失去信號價值，agent 開始習慣性附「gate 紅但沒事」——這正是 gate 要防的心態。

## 範圍

1. 白名單加：`supabase/functions/**/*.ts(:行號)`、`supabase/migrations/*.sql(:行號)`、`supabase/tests/*.sql(:行號)`、`docs/*.md(:行號)`、`scripts/**/*.sh(:行號)`、`.github/workflows/*.yml`；規則與現行 Swift 路徑一致（必須是 repo 內真實存在的檔——gate 用 `git ls-files` 或 `test -f` 驗，不存在的路徑仍紅）。
2. 回歸：對 [LS-222](https://linear.app/little-sprout-app/issue/LS-222/taskls-20-後端-purge-storage-孤兒掃描後續tssql-媒體路徑規則單一來源is-media-object-path) comment `3cbe31bb`、[LS-222](https://linear.app/little-sprout-app/issue/LS-222/taskls-20-後端-purge-storage-孤兒掃描後續tssql-媒體路徑規則單一來源is-media-object-path) R1 verdict、[LS-223](https://linear.app/little-sprout-app/issue/LS-223/taskls-20-後端-purge-storage-孤兒掃描收口候選預算扣除不合法路徑f2不合法路徑改計數樣本回報f1舊-rpc) R1 verdict 三份既有暫存檔（scratchpad `LS-222-sweep.md`／`LS-222-mr-*`／`LS-223-mr-*`，若已不在則以 comment 原文重建）重跑 → exit 0；對「引用不存在檔案」的樣本 → 仍紅（mutation 原文）。
3. `docs/COLLABORATION.md` §7 對照表該列更新錨點清單。

## 不做

改 gate 的主張偵測規則本身；放寬到任意路徑。

## 驗收

- [ ] 三份既有 handoff exit 0、偽路徑樣本紅（原文）；CI 的 `rules` job（若跑此 gate）綠
- [ ] merge-reviewer 通過（scope：只加白名單＋回歸樣本）；QA delta

## 留言（8）

### 2026-09-06T15:24:10.137Z · Chih-Lin Yeh · `48b23730`

**ios-dev handoff（2026-09-06 23:19 CST）— LS-228**

## 已完成

`scripts/gates/handoff_evidence_check.py` 的證據錨點白名單補
`supabase/functions/**/*.ts`、`supabase/migrations/*.sql`、`supabase/tests/*.sql`、
`docs/*.md`、`scripts/**/*.sh`（排除 `.test.sh`）、`.github/workflows/*.yml`
（可帶 `:行號`／`:起-迄`），這六類必須用 `os.path.isfile`（相對 `--repo`）驗證真的
存在，不存在仍判紅並點名哪個路徑。原本無條件子字串比對的 `.md`／`.sh`／`.yml`
從 `PATH_RE` 移除（`.py`／`.json` 不在範圍，維持原行為），改走新的
`PATH_ANCHOR_RE`＋存在性驗證；緊接 `bash ` 的白名單路徑候選視為命令證據
（`COMMAND_RE` 已涵蓋），不驗存在，避免誤傷既有「`bash scripts/gates/foo.sh`」
這類示意性假檔名樣本。`scripts/gates/handoff-evidence-check.sh` 的 `--help`
與檔頭註解同步；`docs/COLLABORATION.md` §7 對照表更新錨點清單與自測組數
（47→59）。

## 已驗證（怎麼驗）

1. **範圍 1（白名單新增六類＋存在性驗證）** ✓：`bash scripts/gates/handoff-evidence-check.test.sh` 尾行 `✓ handoff-evidence-check 自測通過（59 組樣本）`，exit 0——新增正樣本（`supabase/functions/**/*.ts`／`supabase/migrations/*.sql`／`supabase/tests/*.sql`／`docs/*.md`／`scripts/**/*.sh`／`.github/workflows/*.yml` 各一，皆對合成 fixture repo 內真實存在的檔案）；負樣本（一是合成 fixture repo 內一個不存在的 migrations sql 檔名，帶行號，二是 `README.md` 內容存在但不在 `docs/` 白名單目錄）分別斷言訊息含「在 repo 內找不到」與「缺『怎麼驗』證據」（皆見 `handoff-evidence-check.test.sh`）。
2. **mutation（存在性驗證拿掉 → 負樣本改判過）** ✓：改了什麼一行——標記 `HANDOFF-PATH-EXISTS` 那行的 `bad_paths = [...]` 改成 `bad_paths = []`；哪條測試紅——mutation 後對不存在路徑的負樣本重跑，`rc_missingsql` 斷言 `-eq 0`；斷言訊息原文——測試腳本印 `✓ ⑫ mutant（拿掉白名單路徑存在性驗證）：②i 的負樣本（不存在的 .sql 路徑）改判過——證明 HANDOFF-PATH-EXISTS 是原因`（即 mutation 後 exit code 由 1 → 0，實跑確認）；改回原始碼後重跑 `handoff-evidence-check.test.sh` 仍 59 組全綠、exit 0。
3. **mutation（`has_evidence()` 拿掉 `PATH_ANCHOR_RE` 判斷 → 正樣本改判紅）** ✓：改了什麼一行——標記 `HANDOFF-HAS-EVIDENCE-PATH-ANCHOR` 那行拿掉 `or PATH_ANCHOR_RE.search(text)`；哪條測試紅——`handoff-evidence-check.test.sh` 的白名單路徑正樣本重跑，`rc_tsonly` 斷言 `-eq 1`；斷言訊息原文——測試腳本印 `✓ ⑬ mutant（拿掉 PATH_ANCHOR_RE 舉證）：①r 型樣本改判紅——證明白名單路徑舉證確實是 PATH_ANCHOR_RE 這條規則造成的`（輸出含 `缺『怎麼驗』證據`，即 exit code 由 0 → 1，實跑確認）。
4. **回歸（票文指定三份既有暫存檔）** ✓：`bash scripts/gates/handoff-evidence-check.sh <檔> --repo <本 worktree>`——`/private/tmp/claude-501/-Users-clyeh-little-sprout/e6972486-fec6-43c0-b8f4-28b43b01a5a0/scratchpad/LS-222-sweep.md` 改前 exit 1（第 9／10／12 行缺證據）→ 改後 **exit 0**（不是放寬到任意路徑：三行本來就各自也提到白名單目錄內真實 `.ts`／`.sql` 完整路徑，如 `supabase/functions/purge-storage/index.ts`、`supabase/tests/109_soft_delete_unreferenced_media.sql`）；`LS-224-mr-verdict.md` 改前後皆 **exit 0**（引用已在白名單目錄內，`docs/API.md`／`scripts/gates/migration-breaking-check.sh`／`supabase/migrations/20260824010000_diaries_write_path_and_timeline.sql` 等）；`LS-208-mr-r2-verdict.md` 改前 exit 0 → 改後 **exit 1**（第 20 行唯一證據是 `.claude/skills/little-sprout-brand/references/motifs.md`，`.md` 但不在 `docs/` 目錄）——見「未完成」欄。

## 未完成／剩餘

- 尚未 review（首次送審，PR #353 剛建立，尚無 merge-reviewer informational 需處置）。
- **`LS-208-mr-r2-verdict.md` 回歸從 exit 0 變 exit 1**：不修——這是本票刻意收緊
  `.md` 白名單邊界（只認 `docs/*.md`）的直接後果，屬預期範圍內的行為變更，非
  bug。若要涵蓋 `.claude/**/*.md` 這類路徑需另開票擴大白名單（本票「不做：
  放寬到任意路徑」明文排除）；已在 PR body「風險」欄與此處記錄，供
  merge-reviewer／orchestrator 裁定是否需要另開票。

## 風險

純 harness 檔變更（`scripts/gates/*`、`docs/*.md`），無 Swift／migration 變更；
push gate 已確認「無 Swift 變更，跳過 SwiftLint」「無 Swift 變更，跳過 unit
tests」。上述 `.md` 白名單邊界收緊為唯一已知副作用，已記錄。

## 產出位置

- PR #353：https://github.com/CLYEH/little-sprout/pull/353
- head SHA：`db42fef28f491059d50abe323ef7dc29450cb680`
- 分支：`hotfix/LS-228-handoff-evidence-paths`
- 自測輸出：`/private/tmp/claude-501/-Users-clyeh-little-sprout/e6972486-fec6-43c0-b8f4-28b43b01a5a0/scratchpad/LS-228-testrun-4.log`（末行 `✓ handoff-evidence-check 自測通過（59 組樣本）`）
- 回歸輸出：`/tmp/LS-228-regress-LS-222-sweep.md.log`（exit 0）、`/tmp/LS-228-regress-LS-224-mr-verdict.md.log`（exit 0）、`/tmp/LS-228-regress-LS-208-mr-r2-verdict.md.log`（exit 1）
- PR body：`/private/tmp/claude-501/-Users-clyeh-little-sprout/e6972486-fec6-43c0-b8f4-28b43b01a5a0/scratchpad/LS-228-pr-body.md`（`pr-body-check.sh --verify` exit 0）
- worktree：`/Users/clyeh/little-sprout/.claude/worktrees/LS-228`
- 模擬器已關：無（本票未開模擬器）

### 2026-09-06T15:44:46.818Z · Chih-Lin Yeh · `43e2f60e`

**merge-review R1 裁決 — LS-228 · PR #353 · head `db42fef28f491059d50abe323ef7dc29450cb680`（2026-09-06 23:36 CST）**

**VERDICT：REQUEST_CHANGES**（1 major、2 minor、3 informational）。CI 五項全綠、base 正確、scope 乾淨、mutation 申報獨立重放皆屬實；但白名單改法把票文的「加」做成了「加＋減」，對既有 handoff 語料淨增 20 份誤報，與本票要消除誤報的目的相反。

## 逐條查實

1. **scope（票文「範圍」1–3）通過**：`git diff origin/main...db42fef2 --stat` 只有四檔——`scripts/gates/handoff_evidence_check.py`、`scripts/gates/handoff-evidence-check.sh`、`scripts/gates/handoff-evidence-check.test.sh`、`docs/COLLABORATION.md`（§7 對照表該列，1 insert／1 delete）。`.py`／`.json` 舊 pattern 未動（`PATH_RE` 仍含 `.py` 與 `.json`）。無順手重構、無無關改動。
2. **正確性 (a)–(d)（派工單第 2 項）通過**：自建四支樣本以 `bash scripts/gates/handoff-evidence-check.sh <樣本> --repo <本 worktree>` 實跑（原文見下方「實跑原文 A」）——(a) 真實存在的 `supabase/tests/60_default_privileges.sql:12` → exit 0；(b) 不存在的 migrations 檔 → exit 1，訊息點名該路徑；(c) `bash scripts/gates/foo.sh` 命令形式 → exit 0，不因檔案不存在轉紅；(d) `scripts/gates/foo.test.sh` → exit 0。(d) 的排除理由成立：`scripts/gates/handoff_evidence_check.py:153` 的 `(?<!\.test)` lookbehind 使 `.test.sh` 不成為候選，而 `PATH_RE` 既有的 `.test.sh` 子字串仍供舉證，兩者不重疊、不連坐。
3. **mutation ⑫／⑬ 獨立重放（不採信申報）——與申報一致**：把 head 的 `scripts/gates/handoff_evidence_check.py` 複製到 temp，照 handoff 申報各改一行後跑我自建的樣本（非其自測樣本），原文見「實跑原文 B」。⑫ 把 `bad_paths` 的列表推導改成空 list：不存在路徑的負樣本 exit 1 → 0。⑬ 從 `has_evidence()` 拿掉 `PATH_ANCHOR_RE` 判斷：白名單路徑為唯一證據的正樣本 exit 0 → 1，stderr 原文含「缺『怎麼驗』證據」。兩者皆為預期的斷言失敗，非 crash／build fail。
4. **自測與三份回歸（票文驗收 1）——兩過一紅**：`bash scripts/gates/handoff-evidence-check.test.sh` 尾行 `✓ handoff-evidence-check 自測通過（59 組樣本）`、exit 0（`scratchpad/LS-228-mr-selftest.log`）。三份回歸以 `--repo` 指向本票 worktree、改前版本取自 `origin/main`：`LS-222-sweep.md` 改前 1 → head **0**（本票目標達成）；`LS-224-mr-verdict.md` 改前 0 → head **0**；`LS-208-mr-r2-verdict.md` 改前 0 → head **1**（回歸，見 F2）。
5. **效能／回溯風險通過**：`scripts/gates/handoff_evidence_check.py:145-155` 的 `(?:[\w.-]+/)+` 每段字元類別不含 `/`，切分點唯一、無巢狀歧義，不會指數回溯；實測 4000 段／52012 字元 search 0.0016–0.0017s（兩次量測）、單段 20000 字元 0.0003s（「實跑原文 C」）。大 handoff 檔無效能疑慮。
6. **CI 與 base 通過**：`gh pr checks 353` 五項全 pass（`ci`／`ci-ipad`／`lint`／`rules`／`db`），未 rerun；`gh pr view 353 --json baseRefName` = `main`，符合 hotfix 流程。`rules` job 涵蓋本 gate 自測（`.github/workflows/ci.yml`）。
7. **Race condition／平行優化兩維度：無可審項目，明講跳過**——本 PR 只有同步、單執行緒的文字掃描（`scripts/gates/handoff_evidence_check.py`），無 Swift Concurrency、無並發、無 DB 存取。未跑 Supabase／模擬器（本票不需要，未持有 lock）。

## Findings

### F1（major）票文只要求「白名單加」，實作同時把 `.sh`／`.md`／`.yml` 從無條件子字串「減」掉，對既有語料淨增 20 份誤報
`scripts/gates/handoff_evidence_check.py:139`

票文範圍 1 的動詞是「白名單加」，「不做」欄明列「改 gate 的主張偵測規則本身」。head 除了新增 `PATH_ANCHOR_RE`，還把 `PATH_RE` 的 `.sh`／`.md`／`.yml` 三個副檔名拿掉——這是收緊既有偵測規則，票文沒要求。

**量測（機械可複驗）**：把 scratchpad 內 372 份既有 handoff／verdict 語料同時餵給 `origin/main` 版與 head 版（`--repo` 皆指向本票 worktree），判定改變者 21 份——**修好 1 份**（`LS-222-sweep.md` 1→0，本票目標）、**新紅 20 份**（0→1）。20 份中 15 份純因這次子字串移除（把三個副檔名加回即回綠，已實跑驗證），另 5 份屬 F3 的存在性議題。

**具體失敗情境（三類，皆實跑確認 改前 exit 0 → head exit 1）**：

- (a) **本 repo 規約強制要求的引用會紅**：`.claude/agents/merge-reviewer.md`／`qa.md`／`ios-dev.md` 都明文規定 DB 測試 handoff 必須抄 `supabase/tests/run.sh` 印出的「→ 連線方式」那一行。樣本「跑了 `bash supabase/tests/run.sh` 全綠，連線方式：host psql」改前 exit 0、head exit 1——`supabase/` 底下的 `.sh` 不在 `PATH_ANCHOR_RE`（只認 `scripts/**`），`COMMAND_RE` 也只認 `bash scripts/`。下一張 backend 票照規約寫 handoff 就會撞紅。
- (b) **根目錄 `.yml` 會紅**：`project.yml`（xcodegen 專案定義，handoff 常引用）改前 0、head 1——`.yml` 只認 `.github/workflows/` 底下的。
- (c) **裸檔名會紅**：`LS-186-handoff.md` 第 17 行的 `db-reset-retry.sh`、`LS-211-R3-handoff.md` 第 40 行的 `gh pr edit 335 --body-file LS-211-pr-body.md`，改前 0、head 1。

**為什麼算 major**：本票存在的理由就是「誤報讓 fail-closed gate 失去信號價值，agent 開始習慣性附『gate 紅但沒事』」。修 1 份、弄紅 20 份，淨效果與票文目的相反；且 handoff「未完成」欄只揭露其中 1 份（`LS-208`），另 19 份未被發現也未揭露。

**建議修法（擇一，需明確決定）**：
- 修法 1（貼近票文原意、風險最低）：`PATH_RE` 的三個副檔名原樣還原，`PATH_ANCHOR_RE` 只做加法——白名單路徑仍驗存在性、不存在仍紅，目錄外的 `.sh`／`.md`／`.yml` 回到 R3 的子字串行為。注意此修法與本 PR 新增的 ②j（`README.md` 應紅）衝突：實跑後自測 58 組綠、②j 一組轉紅，需一併決定 ②j 去留。
- 修法 2（保留目錄邊界語意）：`PATH_ANCHOR_RE` 擴到實際被當證據引用的類別，至少 `supabase/**/*.sh` 與根目錄 `*.yml`，並各補一組正樣本；(c) 的裸檔名仍無解，需另行決定是否接受。

### F2（minor，orchestrator 已裁定）`.claude/skills/**/*.md`、`.claude/agents/*.md`、`docs/**/*.md` 子目錄未納入白名單
`scripts/gates/handoff_evidence_check.py:145`

`docs/[\w.-]+\.md` 的字元類別不含斜線，只認直接掛在 `docs/` 下的檔案。實跑：`.claude/skills/little-sprout-brand/references/motifs.md`、`.claude/agents/merge-reviewer.md`、`docs/legal/eula-addendum.md` 三種引用改前 exit 0、head exit 1（三檔皆真實存在）。回歸目標 `LS-208-mr-r2-verdict.md` 第 20 行唯一證據正是前者，因此 0→1。這三類是設計票／harness 票的正典引用來源。

**修法（一行 regex ＋ 一組正樣本，已實跑驗證）**：把 `docs/[\w.-]+\.md` 那條改成 `docs/(?:[\w.-]+/)*[\w.-]+\.md`，並加一條 `\.claude/(?:[\w.-]+/)*[\w.-]+\.md`。實跑結果：`LS-208-mr-r2-verdict.md` 回到 **exit 0**、`LS-222-sweep.md` 維持 0、自測維持 **59 組全綠 exit 0**。正樣本建議在合成 fixture repo 加 `docs/legal/fixture.md` 與 `.claude/agents/fixture.md` 各一組（②j 的 `README.md` 邊界不受影響，仍成立）。

### F3（informational）新的存在性驗證讓「在別的 worktree 跑」必紅
`scripts/gates/handoff_evidence_check.py:406`

F1 量測中另外 5 份新紅（`LS-224-handoff.md`、`LS-225-*` 四份）都是引用「該票分支自己新增的 SQL 檔」，那些檔在本票 worktree 不存在 → head 判紅、改前綠。實測 `LS-224-handoff.md` 引用的 `supabase/tests/110_advisors_hardening.sql` 在 `.claude/worktrees/LS-224` 內確實存在，所以在自己的 worktree 跑不會紅——這是我跨 worktree 跑出來的，不是真誤報，故列 informational。

但值得注意：merge-reviewer 常被派在主 checkout 審別票（本次派工單即如此），這時引用 PR 新增檔的 verdict 會紅。錯誤訊息有印 `--repo <path>`、可自我診斷，屬可接受；建議在 `scripts/gates/handoff-evidence-check.sh` 的 `--help` 補一句「引用該票分支新增的檔案時，`--repo` 要指向該票 worktree」。

### F4（minor）否定詞／mutation 語境的 skip 判準沒有套用到路徑候選——驗這支 gate 的 verdict 自己會紅
`scripts/gates/handoff_evidence_check.py:349`

測試名候選有三種候選層級 skip（glob、同子句否定詞、同行 mutation 語境），但 `path_anchor_candidates()` 只實作了 `is_command_invocation_candidate` 一種。結果：handoff／verdict 只要**指名**一個不存在的白名單路徑當負樣本或 mutation 示範，即使句子明說「不存在」「→ 紅」，仍一律判紅。

**具體失敗情境（實跑）**：列項寫「負樣本 `supabase/migrations/20990101000000_x.sql` 這個檔不存在，gate 正確判紅」→ exit 1；寫「mutation 後 `supabase/tests/99_nonexistent_mutant.sql` → 紅」→ exit 1。兩者都是正確敘述被誤判。這正是本 PR 自己造出來的坑：任何審查路徑白名單類 ticket 的 verdict（包含本則，我因此把負樣本原文全部移到 `## Findings` 之後的段落才躲開）都會踩到。

**修法**：`path_anchor_candidates()` 的 skip 比照 `test_name_candidates()`，加上 `is_negated` 與 `is_mutation_context` 兩個既有判準（函式已存在，只需傳入 `m.start()`／`m.end()`），並各補一組負樣本。

### F5（informational）存在性驗證可用 `..` 逃出 `--repo`
`scripts/gates/handoff_evidence_check.py:406`

`os.path.isfile(os.path.join(repo, p))` 不做正規化，而 `[\w.-]+` 這個字元類別涵蓋 `..`，所以 `scripts/` 後面接連續上層目錄的候選會被擷取並檢查到 repo 之外。實跑：在 scratchpad 建一支 `.sh`，用 `scripts/` ＋七層上層目錄指到它 → exit 0（通過存在性驗證）；同形狀但指向不存在的檔 → exit 1。影響有限（只做存在性判定、不讀內容，輸入又是 agent 自己寫的 handoff），但等於存在性檢查可被繞過。修法：候選 `os.path.realpath` 後確認仍在 repo 根之下，否則判紅。

### F6（informational，PLAUSIBLE）`bash ` 前綴判定要求剛好一個空白
`scripts/gates/handoff_evidence_check.py:336`

`is_command_invocation_candidate` 比對 `text[start-5:start]` 是否等於 `bash ` 字面，所以 `bash` 與路徑之間有兩個空白或 tab 時就不 skip，示意性假檔名會被要求真的存在。實務上幾乎都寫一個空白，未在語料中觀察到，故標 PLAUSIBLE。

## 未查／未跑（明講）

- 未在 CI 上重跑任何 job（五項本來就綠，依派工單不 rerun）。
- 未跑 Supabase 容器、未持有 supabase lock（**未持有**）、未開模擬器、未碰 Pencil、未派 subagent。
- `docs/COLLABORATION.md` §7 只讀了本次改動的那一列，未通讀全表。

## 實跑原文

### A — 正確性 (a)–(d) ＋ 白名單邊界探針（head 版，`--repo` 指向本票 worktree）

```
(a) 存在的 supabase/tests/60_default_privileges.sql:12 改前=1 head=0 | ✓ 第 3 行起的列項有證據
(b) 不存在的 migrations .sql                       改前=1 head=1 | ✗ 第 3 行引用的路徑 `supabase/migrations/20990101000000_x.sql` 在 repo 內找不到
(c) bash scripts/gates/foo.sh 命令形式             改前=0 head=0 | ✓ 第 3 行起的列項有證據
(d) scripts/gates/foo.test.sh                  改前=0 head=0 | ✓ 第 3 行起的列項有證據
(e) .claude/skills/**/*.md（motifs.md）          改前=0 head=1 | ✗ 缺『怎麼驗』證據
(f) docs/legal/eula-addendum.md                改前=0 head=1 | ✗ 缺『怎麼驗』證據
(g) .claude/agents/merge-reviewer.md           改前=0 head=1 | ✗ 缺『怎麼驗』證據
(h) docs/COLLABORATION.md:324（對照組）             改前=0 head=0 | ✓ 第 3 行起的列項有證據
(j) project.yml                                改前=0 head=1 | ✗ 缺『怎麼驗』證據
(i2) bash supabase/tests/run.sh                改前=0 head=1 | ✗ 缺『怎麼驗』證據
(i3) supabase/tests/run.sh:40                  改前=0 head=1 | ✗ 缺『怎麼驗』證據
```

### B — mutation ⑫／⑬ 獨立重放（reviewer 自建樣本，非其自測樣本）

```
⑫ 改一行 bad_paths=[]      不存在路徑負樣本：head exit=1  mutant exit=0（1→0，如申報）
⑬ 拿掉 PATH_ANCHOR_RE 舉證  白名單路徑正樣本：head exit=0  mutant exit=1（0→1，如申報）
⑬ mutant stderr 原文：✗ handoff-evidence-check：第 3 行起的列項缺『怎麼驗』證據（須含測試名、.png/.log/.test.sh/scratchpad//evidence//.swift…

自測（head）：✓ handoff-evidence-check 自測通過（59 組樣本）
```

### C — 回溯計時（`PATH_ANCHOR_RE.search`）

```
segments=  200 len=  2612 search=0.0001s
segments= 1000 len= 13012 search=0.0004s
segments= 4000 len= 52012 search=0.0016s
single-long-segment len=20008 0.0003s
```

### D — 既有語料掃描（scratchpad 372 份，`origin/main` 版 vs head 版，`--repo` 皆為本票 worktree）

```
LS-186-handoff.md              改前=0 head=1
LS-208-mr-r2-verdict.md        改前=0 head=1
LS-208-r6-handoff.md           改前=0 head=1
LS-209-qa-handoff.md           改前=0 head=1
LS-211-214-qa-handoff.md       改前=0 head=1
LS-211-R3-handoff.md           改前=0 head=1
LS-211-m4-circled-test.md      改前=0 head=1
LS-213-handoff.md              改前=0 head=1
LS-215-merge-review-r1.md      改前=0 head=1
LS-215-merge-review-r2.md      改前=0 head=1
LS-215-merge-review-r3.md      改前=0 head=1
LS-220-merge-review-r2.md      改前=0 head=1
LS-221-handoff.md              改前=0 head=1
LS-222-handoff.md              改前=0 head=1
LS-222-sweep.md                改前=1 head=0
LS-224-handoff.md              改前=0 head=1
LS-225-handoff-r1r2.md         改前=0 head=1
LS-225-handoff.md              改前=0 head=1
LS-225-mr-verdict.md           改前=0 head=1
LS-225-v2-head.md              改前=0 head=1
LS-QA-689f5ae-handoff.md       改前=0 head=1
--- 掃描 372 份，判定改變 21 份（修好 1、新紅 20）---
```

### E — 兩種候選修法實測

```
修法 F2（docs/** ＋ .claude/**）：LS-208-mr-r2-verdict exit=0（回綠）／LS-222-sweep exit=0／自測 59 組全綠 exit=0
修法 F1-1（另還原 PATH_RE 三個副檔名）：20 份新紅回綠 15 份；自測 ②j（README.md 應紅）轉紅，其餘 58 組綠
```

---

`bash scripts/gates/handoff-evidence-check.sh <本 verdict 暫存檔>`（head 版）輸出：

```
✓ 第 7 行起的列項有證據
✓ 第 8 行起的列項有證據
✓ 第 9 行起的列項有證據
✓ 第 10 行起的列項有證據
✓ 第 11 行起的列項有證據
✓ 第 12 行起的列項有證據
✓ 第 13 行起的列項有證據
exit=0
```

### 2026-09-12T01:45:40.134Z · Chih-Lin Yeh · `6ffbd5e0`

**ios-dev R2 handoff（2026-09-12 09:39 CST）— LS-228 · PR #353 · head `1fa90adfb30691910dcb373bbaef99b7cde2b574`**

## Ticket

LS-228 — 修 merge-review R1（comment `43e2f60e`）REQUEST_CHANGES：`PATH_RE`「純加法」還原＋白名單純加法（F1/F2/F4/F5）。

## 已完成

1. `d2431a1`：`PATH_RE` 的 `.sh`／`.md`／`.yml` 三個副檔名原樣還原（與 `origin/main` byte-identical），修 F1（既有語料淨增 20 份誤報）。`PATH_ANCHOR_RE` 純加法：`docs/[\w.-]+\.md` 放寬成 `docs/(?:[\w.-]+/)*[\w.-]+\.md`（涵蓋巢狀子目錄，原本零層仍照樣命中）、新增 `\.claude/(?:[\w.-]+/)*[\w.-]+\.md`、新增 `supabase/(?:[\w.-]+/)+[\w.-]+(?<!\.test)\.sh`，修 F2。`path_anchor_candidates()` 的 skip 判準補上 `is_negated`／`is_mutation_context`（比照既有測試名候選判準），修 F4。
2. `187481b`：新增 `path_within_repo()`／`path_exists_in_repo()`——候選 `os.path.normpath` 正規化後確認仍落在 `--repo` 絕對路徑之下才驗存在，逃出 repo 一律視為不存在，修 F5。
3. `1fa90ad`：`handoff-evidence-check.test.sh` 自測 59→76 組，新增 F1/F2/F4/F5 各自的正負樣本＋mutation 負控（⑭⑮⑯，見下）；`docs/COLLABORATION.md` §7 對照表同步更新錨點清單與自測組數。
4. PR #353 body 加「R2 處置表」段落（F1/F2/F3/F4/F5/F6 逐條處置＋SHA＋檔案:行號），既有段落原樣保留；`pr-body-check.sh --verify` exit 0。
5. F3／F6（informational，orchestrator 裁定範圍未列）記入 LS-96 `ea3dee4e`。
6. 追加來源 LS-217 merge-review R2（comment `6b661d66`）的三筆真實誤判樣本——逐條核對後判定不屬本票範圍，記入 LS-96 `98b73b25`（見下「未完成」）。

## 已驗證（怎麼驗）

1. **F1 修復（規約 R2 範圍項 1：還原 `PATH_RE`）**：`git diff origin/main...HEAD -- scripts/gates/handoff_evidence_check.py | grep '^PATH_RE'` 空 diff（`scratchpad/LS-228-r2-regression-summary.log` 第 1 段），確認 `PATH_RE` 與 `origin/main` 逐位元組相同。mutation ⑯（`handoff-evidence-check.test.sh:881` 起）把 `PATH_RE` 退回 LS-228 R1 首版（拿掉 `.sh`／`.md`／`.yml`），對 ①y（根目錄 `project.yml`）與 ①z（裸檔名 `db-reset-retry.sh`）兩個正樣本重跑：改前（R2 head）exit 0 → mutant exit 1（兩者皆是），斷言訊息原文：`✓ ⑯ mutant（PATH_RE 退回 LS-228 首版）：①y（project.yml）與①z（db-reset-retry.sh）雙雙改判紅——證明是這三個副檔名的還原修好了 merge-review R1 F1 的迴歸`。
2. **F1 真實回歸情境三種（規約 R1 F1(a)(b)(c)）**：新增正樣本 ①x（`bash supabase/tests/run.sh`，`scripts/gates/handoff-evidence-check.test.sh:284`）、①y（`project.yml`，:289）、①z（`db-reset-retry.sh`，:294）——三者改前（R1 head `db42fef`）皆 exit 1、改後（本次 head）皆 exit 0，`bash scripts/gates/handoff-evidence-check.test.sh` 尾行 `✓ handoff-evidence-check 自測通過（76 組樣本）`，exit 0（`scratchpad/LS-228-r2-selftest.log`）。
3. **F2 修復（docs 巢狀／`.claude`）**：正樣本 ①aa（`scripts/gates/handoff-evidence-check.test.sh:301`，白名單路徑存在性 fixture 為 docs 巢狀子目錄）、①ab（`scripts/gates/handoff-evidence-check.test.sh:306`，`.claude/**/*.md`）皆 exit 0。真實語料 `LS-208-mr-r2-verdict.md`（Linear comment `e1e5157f`，第 20 行唯一證據 `.claude/skills/little-sprout-brand/references/motifs.md`）：`origin/main`=exit 0 → R1 head `db42fef`=exit 1（F2 迴歸）→ **本次 head `1fa90ad`=exit 0**（`scratchpad/LS-228-r2-regression-summary.log` 第 2 段，三版比對表）。
4. **F4 修復（否定詞／mutation 語境對白名單路徑候選）**：正樣本 ①ad（`scripts/gates/handoff-evidence-check.test.sh:319`，同句含「不存在」）、①ae（:324，同行 mutation 語境）皆 exit 0。mutation ⑮（:841 起）把 `path_anchor_candidates()` 的 skip 判準退回只有 `is_command_invocation_candidate`：①ad 改判 exit 1（斷言訊息原文：`✓ ⑮ mutant（否定詞）：①ad 的正樣本改判紅（20990101000000_x.sql 被當成引用驗存在）——證明否定詞判準是原因`），①ae 改判 exit 1（`✓ ⑮ mutant（mutation 語境）：①ae 的正樣本改判紅——證明 mutation 語境判準是原因`）。
5. **F5 修復（`..` 逃出 `--repo`）**：負樣本 ②k（`scripts/gates/handoff-evidence-check.test.sh:382`，候選 `scripts/../../outside-escape.sh` 指向 `$work/outside-escape.sh`——一個真實存在於 repo 之外的檔案）exit 1，訊息點名該路徑找不到。mutation ⑭（:815 起）把 `path_within_repo()` 恆改為 `True`：②k 改判 exit 0（斷言訊息原文：`✓ ⑭ mutant（拿掉 repo 邊界檢查）：②k 的逃逸樣本改判過（因為 $work/outside-escape.sh 確實存在）——證明 HANDOFF-PATH-BOUNDARY-CHECK 是原因`）。獨立 python 互動驗證（非測試檔）：`path_within_repo(repo, "scripts/../../outside-escape.sh")` → `False`、`path_exists_in_repo(...)` → `False`；同目錄下真實存在的 `scripts/run.sh` → 皆 `True`。
6. **回歸目標（票文指定兩份必附檔案，`--repo` 指向本 worktree）**：
   - `LS-222-sweep.md`（Linear LS-222 comment `3cbe31bb`）：`origin/main`=exit 1 → R1 head `db42fef`=exit 0（本票要修的目標）→ **本次 head `1fa90ad`=exit 0**（維持，未退步）。
   - `LS-208-mr-r2-verdict.md`（Linear LS-208 comment `e1e5157f`）：`origin/main`=exit 0 → R1 head `db42fef`=exit 1（F2 迴歸）→ **本次 head `1fa90ad`=exit 0**（修回）。
   - 方法：三個版本的 `handoff_evidence_check.py`（`git show origin/main:...`、`git show db42fef:...`、本次 head）各自對兩份真實 comment 原文跑 `python3 <版本>.py <corpus>.md --repo <本 worktree>`，比較 exit code；三份 python 檔與比對輸出存 `scratchpad/LS-228-r2-py-{origin-main,r1head,r2head}.py`、`scratchpad/LS-228-r2-regression-summary.log`。
7. **372 份既有語料（票文回歸目標）——誠實揭露替代方法**：merge-review R1 用的 372 份語料存在**前一個 session 的暫存目錄**（`.../scratchpad/`），該 session 已結束、目錄現在是空的（`find <該目錄> -type f | wc -l` = 0），無法逐檔重放同一批語料。改採三種可機械複驗的等價證據：(a) 上述「F1 修復」的 byte-identical 比對——由於 `PATH_RE` 的還原對 `has_evidence()` 是純加法（只可能讓判定從「缺證據」變成「有證據」，不可能反向），且 `PATH_ANCHOR_RE`／`path_anchor_candidates()` 的新增分支與判準同樣只增加新的匹配、不移除任何既有匹配，數學上不可能讓任何本來 exit 0 的既有檔案（在 origin/main 或 R1 head 上）新增變成 exit 1，除非該檔案剛好命中新加的白名單目錄且引用的路徑不存在（這正是本票要抓的情境，非誤報）；(b) 把 LS-228 R1 交付時的完整自測檔（`git show db42fef:scripts/gates/handoff-evidence-check.test.sh`，59 組）搬進一個獨立的假 repo 根目錄（`scripts/gates/` 下放本次 head 版 `handoff_evidence_check.py`／`handoff-evidence-check.sh`），實跑——`grep -c '^✓'` = 58、唯一 `✗` 是 `②j`（依裁定改版，README.md 邊界示範已被 F1 還原取消，屬預期變更非迴歸），其餘 58 組維持原判（`scratchpad/LS-228-r2-r1testfile-on-r2code.log`，exit=1 因唯一那組已知變更）；(c) LS-217 merge-review R2（comment `6b661d66`）附的三筆真實 verdict 誤判樣本經逐條核對，皆落在 `COMMAND_RE`（`xcrun simctl`／`git show <sha>`）與全新「Linear comment id 當證據」類別，不落在本票授權的 `PATH_ANCHOR_RE` 白名單目錄範圍內——判定不屬本票，記入 LS-96 `98b73b25`，不加入自測正樣本（加入會斷言一個本票故意不修的行為為「通過」，不誠實）。
8. **自測全綠**：`bash scripts/gates/handoff-evidence-check.test.sh` → exit 0，尾行 `✓ handoff-evidence-check 自測通過（76 組樣本）`（`scratchpad/LS-228-r2-selftest.log`，末 3 行）。
9. **push gate**（`scripts/gates/push-gate.sh`）：`git push origin hotfix/LS-228-handoff-evidence-paths` → `✓ push gate：無 Swift 變更，跳過 SwiftLint（CI 仍跑）`／`✓ push gate：無 Swift 變更，跳過 unit tests（CI 仍跑）`／`✓ push gate 通過`，push 成功（`db42fef..1fa90ad`）。

## 未完成／剩餘（reviewer 全部 finding 逐條處置，含 informational）

- **F1（major）**：已修，見上「已完成」1／「已驗證」1-2。
- **F2（minor）**：已修，見上「已完成」1／「已驗證」3。
- **F3（informational）**：不修＋理由——`--repo` 使用方式提醒（merge-reviewer 在主 checkout 審別票時要指向該票 worktree），不是 gate 邏輯缺陷；orchestrator R2 裁定範圍（票文五項）未列此項，記入 LS-96 `ea3dee4e`。
- **F4（minor）**：已修，見上「已完成」1／「已驗證」4。
- **F5（informational）**：已修，見上「已完成」2／「已驗證」5。
- **F6（informational，PLAUSIBLE）**：不修＋理由——reviewer 自己標記 PLAUSIBLE（`bash ` 與路徑間非剛好一個空白時不 skip，實務語料未觀察到）；orchestrator R2 裁定範圍未列此項，記入 LS-96 `ea3dee4e`。
- **372 份既有語料無法逐檔重放**：原始暫存目錄已隨前一 session 結束清空，見上「已驗證」7 的替代方法與理由（非隱藏、已如實揭露）。
- **LS-217 merge-review R2（comment `6b661d66`）追加的三筆真實誤判樣本**：不修＋理由——三筆逐條核對後皆不落在本票授權範圍（`PATH_ANCHOR_RE` 白名單目錄）內：① 引用 Linear comment id `4e528e75` 當「怎麼驗」證據——現行 gate 完全沒有這個證據類別，要支援需新增全新候選判準（裸 hex token 語境限定的設計題），風險與範疇都超出「白名單目錄純加法」；② `xcrun simctl ui` 不在 `COMMAND_RE`；③ `git show <sha>` 命令與 `.pbxproj` 路徑皆不在白名單。②③屬「命令／路徑偵測規則本身」要擴充，票文「不做」欄明文排除；`COMMAND_RE` 也不是 orchestrator R2 裁定範圍列出的五項之一。三項記入 LS-96 `98b73b25`，供下一張碰本 gate 的票參考。

## 風險

純 harness 檔變更（`scripts/gates/*`、`docs/COLLABORATION.md`），無 Swift／migration 變更；push gate 已確認「無 Swift 變更，跳過 SwiftLint」「無 Swift 變更，跳過 unit tests」。`PATH_ANCHOR_RE` 新增的 `supabase/**/*.sh`／`docs/**/*.md`（巢狀）／`.claude/**/*.md` 三個白名單類別會讓「引用該類路徑但檔案不存在」的既有／未來 handoff 判紅（設計如此，非 bug）——`②l`（`supabase/tests/does-not-exist.sh`）已驗證此行為正確觸發。

## 產出位置

- PR #353：https://github.com/CLYEH/little-sprout/pull/353（head `1fa90adfb30691910dcb373bbaef99b7cde2b574`，base `main`）
- 分支：`hotfix/LS-228-handoff-evidence-paths`
- 3 個 commit：`d2431a1`（還原＋加法／F1F2F4）、`187481b`（F5）、`1fa90ad`（測試＋docs）
- worktree：`/Users/clyeh/little-sprout/.claude/worktrees/LS-228`
- scratchpad 證據：
  - `LS-228-r2-selftest.log`（76 組自測全綠原文）
  - `LS-228-r2-regression-summary.log`（PATH_RE byte-diff＋兩份必附語料三版比對表）
  - `LS-228-r2-py-origin-main.py`／`LS-228-r2-py-r1head.py`／`LS-228-r2-py-r2head.py`（三版 python，供覆核）
  - `LS-228-r2-corpus-222-sweep.md`（Linear LS-222 comment `3cbe31bb` 原文）
  - `LS-228-r2-corpus-208.md`（Linear LS-208 comment `e1e5157f` 原文）
  - `LS-228-r2-corpus-186.md`（bonus，Linear LS-186 comment `75c45cb0` 原文，三版一致 exit 1、與本票路徑規則無關）
  - `LS-228-r2-r1testfile-on-r2code.log`（LS-228 R1 交付時 59 組自測檔套用 R2 程式碼重跑原文）
  - `LS-228-r2-pr-body-new.md`（PR body，`pr-body-check.sh --verify` exit 0）
- LS-96：`ea3dee4e`（F3／F6 informational 入池）、`98b73b25`（LS-217 追加三筆入池）
- 模擬器已關：無（本票未開模擬器）
- handoff 自查：`bash scripts/gates/handoff-evidence-check.sh <本 comment 暫存檔> --repo <本 worktree>` → exit 0（`scratchpad/LS-228-r2-handoff.md` 為暫存原文）

### 2026-09-12T02:11:00.053Z · Chih-Lin Yeh · `279e7840`

**merge-review R2 裁決 — LS-228 · PR #353 · head `1fa90adfb30691910dcb373bbaef99b7cde2b574`（2026-09-12 10:05 CST）**

**VERDICT：APPROVE**（0 blocker、0 major、1 minor、5 informational）。R1 的 F1（major）、F2／F4（minor）、F5（informational）皆已修且經我獨立重放確認；orchestrator 裁定的「純加法」四項逐條成立：`PATH_RE` 與 `origin/main` byte-identical、`PATH_ANCHOR_RE` 只有三處加法、存在性檢查加 repo 邊界、②j 已改為「白名單目錄內不存在的檔」。回歸目標「既有語料 0 新紅」我用自建的 427 份真實語料複驗通過（R1 那 372 份原始檔已隨 session 清空、不可重放，方法與差異見查實 3 與「未查／未跑」）。

## 逐條查實

1. **R2 裁定①：`PATH_RE` 還原（F1）**——`scripts/gates/handoff_evidence_check.py:152` 那行對 `origin/main` 逐位元組相同：兩版 `git show <ref>:scripts/gates/handoff_evidence_check.py | grep '^PATH_RE' | md5` 皆 `2f391d8d8d85fee1abe5782e432dbfb4`（實跑原文 A）。同法確認 `TEST_NAME_RE`／`COMMAND_RE`／`NEGATION_WORDS`／`CLAUSE_BOUNDARY_RE`／`MUTATION_CONTEXT_RE` 五個常數與 `is_negated`／`is_mutation_context`／`test_name_candidates`／`test_name_exists`／`split_items`／`find_section` 六支既有函式（去註解後）皆與 `origin/main` 相同——既有偵測規則一行未動。
2. **R2 裁定②：`PATH_ANCHOR_RE` 只有加法**——`git diff db42fef..HEAD -- scripts/gates/handoff_evidence_check.py` 對該常數只有三處變動（`scripts/gates/handoff_evidence_check.py:165-173`）：新增 `supabase/(?:[\w.-]+/)+[\w.-]+(?<!\.test)\.sh`、新增 `\.claude/(?:[\w.-]+/)*[\w.-]+\.md`、`docs/[\w.-]+\.md` 放寬為 `docs/(?:[\w.-]+/)*[\w.-]+\.md`（`*` 涵蓋零層，原命中集合不變）。另做機械驗證：對我自建的 427 份語料逐檔比對 R1 版與 R2 版的 `finditer` 命中集合，**0 份出現「R1 有、R2 沒有」的命中**（實跑原文 B）——加法在真實文本上成立，不只是讀 regex 推論。
3. **回歸實跑（票文回歸目標「既有語料 0 新紅」）**——R1 用的 372 份語料在 `/private/tmp/claude-501/.../e6972486-*/scratchpad/`，該 session 已結束、整個目錄現在 0 檔（我自己 `find` 過），無法重放同一批。我改自建一份**可複驗**的語料：`gh api repos/CLYEH/little-sprout/pulls` 四頁抓 353 份 PR body、`issues/comments` 68 則 GitHub comment，加上 6 份手上的真實 Linear handoff／verdict 原文，共 **427 份**（存 `scratchpad/LS-228-corpus/`）。以 `--repo` 指向本票 worktree，對 `origin/main`／R1 head `db42fef`／R2 head `1fa90ad` 三版各跑一次，並把 stderr 正規化成「（行號, 錯誤類別[, 路徑]）」再比集合（避免被訊息措辭改動污染，第一次直接比字串就被措辭差異假陽性 107 筆）：**`origin/main` → R2：新增 findings = 0、消解 4；R1 → R2：新增 0、消解 22**（實跑原文 C）。
4. **回歸的第二層量測（更嚴，涵蓋沒有「已驗證」段的 378 份）**——檔層級 exit code 只有 49 份語料真的走到列項判定（其餘 378 份無「已驗證／驗收／查實」段，兩版皆 exit 2），所以我另跑一次行層級差分（`scratchpad/LS-228-linediff.py`，語料 `scratchpad/LS-228-corpus/`）：把 427 份的**每一行**（15,555 行）各自餵進兩版的 `has_evidence()` 與新的存在性檢查——`has_evidence` main 紅→R2 綠 128 行、**main 綠→R2 紅 0 行**；唯一能造成新紅的機制（白名單路徑不存在）在 15,555 行中只會命中 **11 行／8 個路徑**（清單見 Findings i3），且這 11 行沒有任何一行落在某份語料的「已驗證」段內，所以查實 3 的新增 findings 才會是 0。
5. **三支指名檔**——`--repo` 指向本票 worktree：`LS-222-sweep.md`（LS-222 comment `3cbe31bb` 原文）`origin/main`=1 → R1=0 → **R2=0**（票文目標，維持）；`LS-208-mr-r2-verdict.md`（LS-208 comment `e1e5157f` 原文）`origin/main`=0 → R1=1（F2 迴歸）→ **R2=0**（修回）；第三支 `LS-224-mr-verdict.md` 我手上沒有原文、未跑（見「未查／未跑」）。兩份語料內容與 R1 verdict 的獨立描述對得上（LS-208 第 20 行唯一證據確實是 `.claude/skills/little-sprout-brand/references/motifs.md`），非事後捏造。
6. **F5 mutation ⑭ 獨立重放（不採信申報）**——我自己把 `scripts/gates/handoff_evidence_check.py:391` 的邊界檢查改成恆回 `True`（一行），用**我自建**的樣本（`scratchpad/LS-228-mut/s_escape2.md`：候選 `scripts/` 接 7 層 `../` 指到 repo 外一支真實存在的 `.sh`）：head exit=1、訊息點名該路徑找不到；mutant exit=0。是預期的斷言失敗、非 crash。**申報屬實**。附帶抓到：我第一版樣本 `../` 少一層，head 與 mutant 都紅——這種「紅得不是原因」的假陽性正是重放才看得出來。
7. **F4 mutation ⑮ 獨立重放**——把 `scripts/gates/handoff_evidence_check.py:378` 的 skip 判準退回只剩 `is_command_invocation_candidate`，用我自建的兩支樣本（同句否定詞、同行 mutation 語境，皆指名一個白名單目錄內不存在的 `.sql`）：head 皆 exit=0、mutant 皆 exit=1。**申報屬實**。
8. **F1 mutation ⑯ 獨立重放**——把 `PATH_RE` 退回 LS-228 首版（拿掉 `.sh`／`.md`／`.yml`），用我自建的三支 R1 F1(a)(b)(c) 情境樣本：`project.yml` 與裸檔名 `db-reset-retry.sh` 兩支 head=0 → mutant=1（**申報屬實**）；第三支「跑了 `bash supabase/tests/run.sh` 全綠」head=0、mutant 仍=0——那支是靠新增的 `supabase/**/*.sh` 白名單（該檔真的存在）過關，不是靠 `PATH_RE` 還原，與 handoff 把三支一律歸因於 ⑯ 的寫法略有出入，但三支在 R1 head 皆紅、R2 head 皆綠（`origin/main`／R1／R2 = 0／1／0，實跑原文 D）此結論不變，不構成 finding。
9. **自測與「R1 測試檔套 R2 程式碼」**——`bash scripts/gates/handoff-evidence-check.test.sh` exit 0、尾行 `✓ handoff-evidence-check 自測通過（76 組樣本）`、77 個 `✓` 0 個 `✗`、3.2s（`scratchpad/LS-228-selftest.log`）。另自己複驗 handoff 的 7(b)：把 `db42fef` 的 59 組測試檔搭 R2 版程式碼放進臨時 root 實跑 → **58 ✓／1 ✗**，唯一紅的就是 ②j（`README.md` 邊界示範，orchestrator 裁定要改版），與申報一致（`scratchpad/LS-228-r1test-on-r2.log`）。
10. **沒有弱化既有斷言**——`git diff origin/main...HEAD -- scripts/gates/handoff-evidence-check.test.sh` 的刪除行裡只有 1 條 `expect`（①n 的**說明文字**），斷言值未動；②j 是本票新增樣本（`origin/main` 沒有），R2 依裁定改成「白名單目錄內不存在的檔」（`scripts/gates/handoff-evidence-check.test.sh:377`），不是把紅樣本改綠。
11. **scope 乾淨**——`git diff origin/main...HEAD --stat` 只有四檔：`scripts/gates/handoff_evidence_check.py`、`scripts/gates/handoff-evidence-check.sh`、`scripts/gates/handoff-evidence-check.test.sh`、`docs/COLLABORATION.md`（§7 單列，1 insert／1 delete）。三個 commit 各自的檔案清單與 handoff 申報一致（`d2431a1` 程式碼＋`--help`、`187481b` 邊界檢查、`1fa90ad` 測試＋docs）。無順手重構、無夾帶。
12. **CI／base／PR body**——`gh pr checks 353` 五項全 pass（ci／ci-ipad／db／lint／rules，run 34665323747），未 rerun；`gh pr view 353` base=`main`、headRefOid=`1fa90ad…`（與我審的 SHA 相同）、mergeable=MERGEABLE、status=BLOCKED（缺本 status，預期）。`bash scripts/gates/pr-body-check.sh <body> --branch hotfix/LS-228-handoff-evidence-paths --verify` exit 0，四條「已修」SHA 皆在 PR commits 內；body 的「R2 處置表」F1–F6 六條齊備。
13. **LS-96 池項查實**——`ea3dee4e`（F3／F6 兩條）與 `98b73b25`（LS-217 R2 三筆）在 LS-96 實際存在，內容與 handoff 敘述相符（我用 `mcp__linear__list_comments` 讀原文核對）。
14. **LS-217 三筆判「不屬本票」——同意**：① Linear comment id 當證據是**全新證據類別**（裸 hex token 的語境判準），② `xcrun simctl` 屬 `COMMAND_RE`、③ `git show <sha>`／`.pbxproj` 同屬命令／路徑偵測規則本身，票文「不做」欄明文排除，也不在 orchestrator R2 裁定的五項內。判定合理；不加進自測正樣本的理由（會把本票故意不修的行為斷言成通過）也對。後續建議見 Findings i4。
15. **效能**——新增成本是一支 regex 加每個候選一次 `os.path.isfile`。全語料（`scratchpad/LS-228-corpus/` 全 427 份）1,436,633 字元跑 `PATH_ANCHOR_RE.finditer` 共 0.055s、1280 個候選；新增的 `(?:[\w.-]+/)*` 巢狀量詞無回溯風險（每段不含 `/`，切分唯一）：4000 段不匹配輸入 0.0013s、單段 20000 字元 0.0005s。端到端交錯 A/B（48 份語料 × 各 4 輪）：R2 3.07–3.31s、`origin/main` 3.10–3.29s，無差異（瓶頸是測試名存在性的 `git grep` 子行程，本 PR 未動）。
16. **race condition／平行優化兩維度：無可審項目，明講跳過**——純同步、單執行緒的文字掃描，無 Swift Concurrency、無並發、無 DB 存取。未跑 Supabase 容器、**未持有 supabase lock**（本票不需要）、未開模擬器。

## Findings

### m1（minor）§7 對照表該列的句子被 R5 插入段截斷，語意斷裂
`docs/COLLABORATION.md:327`

原句是「須含測試名、路徑（…）或指令（…）其中一種證據」。R5 段插在「白名單目錄路徑（…）」之後並補了句號，於是尾巴變成「…候選用 `..` 逃出 `--repo` 一律視為不存在（F5）**或指令（`xcodebuild`／`bash scripts/…`／`gh run view`／`.xcresult`…）其中一種證據**。」——「或指令…其中一種證據」現在掛在 F5 那句後面，讀起來像「逃出 repo 視為不存在，或指令…是一種證據」。

**具體失敗情境**：這張表是 agent 找「這條規則的機械 gate 是什麼」的正典入口；照現在的斷句讀，會誤以為命令證據與 F5 邊界檢查是同一條規則。內容沒有缺漏，只是句子結構壞了。

**建議修法**：把「或指令（…）其中一種證據」移回「白名單目錄路徑（…）」那一段之後、R5 說明之前（或把 R5 整段移到句末，以破折號／括號附註形式接在「其中一種證據。」之後）。屬文件層級，不影響 gate 行為。

### i1（informational）mutation 語境的 skip 對白名單路徑是**整行**判定，是可被繞過的 fail-open 方向
`scripts/gates/handoff_evidence_check.py:378`

`is_mutation_context` 看的是候選所在**整行**（既有設計，R3 從區塊縮到行）。套到路徑候選後，只要同一行出現 `mutation`／`mutant`／`改回`／`→ 紅`，該行所有白名單路徑一律不驗存在。實跑：`- mutation 後仍綠，證據見 supabase/tests/999_fake_nonexistent.sql:12`（該檔不存在）→ **exit 0**。ios-dev handoff 的「已驗證」欄幾乎每項都要寫 mutation 三段，這類行很常見。

反過來，否定詞是**子句**範圍，洞比想像小：`- 未跑 CI，改看 <不存在路徑>` 與 `- 沒有 flaky，見 <不存在路徑>` 兩支實跑都仍 exit 1（否定詞在前一子句），只有「否定詞與候選同子句」才 skip。

**量測**：427 份語料共 1280 個白名單候選，skip 分佈＝`bash ` 前綴 270、否定詞 38、mutation 語境 31、實際驗存在 941；被 skip 且「檔案其實不存在」的只有 2 個，兩個都出自 PR #353 自己的 body（示意性假檔名與逃逸示範）。**目前遮蔽的真實造假 = 0**，而它修掉的是 R1 實際踩到的誤判，取捨正確。**不是本票該修的事**——若要收緊，建議另票把路徑候選的 mutation 判定改成子句範圍（與否定詞一致），或要求候選與 mutation 關鍵字同一子句。記 LS-96。

### i2（informational，PLAUSIBLE）F5 的邊界檢查用 `normpath` 不解 symlink，symlink 逃逸仍可通過
`scripts/gates/handoff_evidence_check.py:383-391`

R1 F5 建議的是 `os.path.realpath`，實作用 `os.path.normpath`：`..` 逃逸擋得住（查實 6 已證），但 repo 內若有指向外部的 symlink，`scripts/<symlink>/x.sh` 仍會通過存在性檢查。實跑：自建 fake repo 內放一支 symlink 指到 repo 外，樣本 exit 0（過）。**目前不可達**：本 repo `git ls-files -s` 的 120000 模式 0 筆、工作區 `find . -type l`（排除 `.git`）也 0 筆，且輸入是 agent 自己寫的 handoff、檢查只判存在不讀內容。標 PLAUSIBLE、不要求本票處理；若日後 repo 出現 symlink，改 `realpath` 即可（同一行）。

### i3（informational）新的存在性檢查對真實語料的誤報面：11／15,555 行，四類
`scripts/gates/handoff_evidence_check.py:446`

行層級掃描 427 份語料（15,555 行）找出「會純因新檢查轉紅」的行，共 11 行／8 個路徑，分四類：(a) **跨 worktree**（PR #350 的 `supabase/migrations/20260906124837_reactions_block_filter.sql`、`supabase/tests/111_reactions_block_filter.sql`——該 PR 自己新增的檔，在別的 worktree 跑就紅，即 R1 F3，已入池 `ea3dee4e`）；(b) **外部文件網址被當成 repo 路徑**（`code.claude.com/docs/en/sub-agents.md` → 候選 `docs/en/sub-agents.md`；這類是 `docs/**` 放寬後才會命中的新形狀）；(c) **散文裡的示意性假路徑**（`docs/environment.md`、一段 `printf` 示範裡的 `supabase/migrations/001.sql`、`docs/x.md`）；(d) **真的指錯**（`.claude/skills/little-sprout-brand/motifs.md` 實際在 `references/` 子目錄下——這類正是本 gate 想抓的）。

四類都沒有落在任何一份語料的「已驗證」段內，所以本次回歸新增 findings = 0；(a) 有明確自救路徑（`--repo` 指向該票 worktree），(d) 是 gate 生效。值得順手記池的是 (b)：候選前綴若緊接 `://` 或網域字樣（`.com/`／`.dev/`）就不當成 repo 路徑候選，一行判準可消掉整類。建議記 LS-96，不在本票修。

### i4（informational）`COMMAND_RE` 缺 `xcrun simctl` 會持續咬 UI 票的 handoff，建議別只停在池裡
`scripts/gates/handoff_evidence_check.py:174`

同意查實 14 的範圍判定。但 LS-207 起「用 `simctl ui` 改過字級／外觀的 handoff 必列已復原」是硬規約，UI 票 handoff 必然會寫 `xcrun simctl ui` 當「怎麼驗」——而 `COMMAND_RE` 不認，這類列項只能靠碰巧同行有別的錨點才過。LS-217 R2 已經是真實案例。建議 orchestrator 評估把 `98b73b25` 的第②項（連同 `git show <sha>`）升成獨立 harness 票，而非等「下一張碰這支 gate 的票」。純建議，不影響本 PR。

### i5（informational）R1 的 372 份語料不可重放，暴露 gate 回歸缺少常設語料
本票兩輪的回歸基準都是「某個 session scratchpad 裡剛好累積的檔案」，跨 session 即蒸發（R1 那批 372 份現在 0 檔；我這 427 份同樣會蒸發）。LS-96 池項 `6bbf6ab1` 已提「證據改放 worktree 內 `.claude/evidence/<票號>/<輪次>/`」，方向一致。建議再加一條：本 gate 的回歸語料改成可重建的**腳本**（我這次用的 `gh api pulls`／`issues/comments` 兩條命令即可重建 421 份，含在 `scratchpad/LS-228-runner.py`），下一張碰這支 gate 的票把它落成 `scripts/gates/` 下的一支回歸腳本，就不必每輪重新發明語料。記 LS-96。

## 未查／未跑（明講）

- **R1 的 372 份原始語料無法重放**（來源 session scratchpad 已清空，我自己 `find` 確認 0 檔）。我的 427 份是另一批（GitHub PR body／comment 為主），**數字不可與 R1 的「20 份新紅」直接相減**；兩者共同點是都以真實 agent 文本為輸入。其中只有 49 份含「已驗證／驗收／查實」段、會走到列項判定，這是我另做行層級差分（查實 4）的原因。
- `LS-224-mr-verdict.md`（R1 三份回歸的第三份）我手上沒有原文，未跑；R1 當時量到 `origin/main`=0、R1 head=0，本輪為純加法方向，風險低但我沒有實證。
- 未在 CI 上 rerun 任何 job（五項本來就綠）。未跑 Supabase 容器、**未持有 supabase lock**、未開模擬器、未碰 Pencil、未派 subagent。
- `docs/COLLABORATION.md` 只讀了 §7 本次改動那一列，未通讀全表。
- 未檢查 `handoff-evidence-check.sh --help` 以外的 CLI 行為（`--repo` 缺值、多檔等既有路徑本票未動）。

## 實跑原文

### A — `PATH_RE` 與既有偵測規則 byte 比對

```
origin/main PATH_RE md5 = 2f391d8d8d85fee1abe5782e432dbfb4
HEAD        PATH_RE md5 = 2f391d8d8d85fee1abe5782e432dbfb4
SAME  TEST_NAME_RE / COMMAND_RE / NEGATION_WORDS / CLAUSE_BOUNDARY_RE / MUTATION_CONTEXT_RE
SAME  is_negated / is_mutation_context / test_name_candidates / test_name_exists / split_items / find_section
```

### B — 語料層級回歸（427 份，`--repo` 指向本票 worktree）

```
docs=427  total whitelist candidates=803（去重後逐檔）／1280（含重複出現）
r1-matchset NOT subset of r2 in 0 docs        ← PATH_ANCHOR_RE 加法在真實文本上成立
exit code 分佈：main {0:6, 1:43, 2:378} / r1 {0:6, 1:43, 2:378} / r2 {0:7, 1:42, 2:378}
正規化 findings 差分：main -> r2  NEW=0  RESOLVED=4
                      r1   -> r2  NEW=0  RESOLVED=22
```

### C — 行層級差分（15,555 行）

```
has_evidence  main-red -> r2-green = 128   |  main-green -> r2-red = 0
只因新存在性檢查會轉紅的行 = 11（8 個相異路徑，分類見 i3）
skip 分佈（1280 個候選）：checked 941 / bash 270 / negated 38 / mutation 31
被 skip 且檔案不存在 = 2（兩者皆出自 PR #353 自己的 body）
```

### D — 我自建樣本的三版對照（0＝綠，1＝紅）

```
樣本                                  main  r1  r2
bash supabase/tests/run.sh 一行         0    1   0   ← R1 F1(a)
project.yml                             0    1   0   ← R1 F1(b)
裸檔名 db-reset-retry.sh                0    1   0   ← R1 F1(c)
同句否定詞＋不存在的 .sql               1    1   0   ← R1 F4
同行 mutation 語境＋不存在的 .sql       1    1   0   ← R1 F4
存在的 supabase/tests/*.sql             1    0   0   ← 票文目標
`..` 逃出 --repo（檔在 repo 外存在）    0    0   1   ← R1 F5（新紅＝設計如此）
```

### E — 自測與 R1 測試檔複驗

```
bash scripts/gates/handoff-evidence-check.test.sh → exit 0，✓ 77 / ✗ 0，3.2s
尾行：✓ handoff-evidence-check 自測通過（76 組樣本）
db42fef 的 59 組測試檔 + R2 程式碼 → 58 ✓ / 1 ✗（僅 ②j，裁定改版）
```

---

`bash scripts/gates/handoff-evidence-check.sh <本 verdict 暫存檔> --repo <本票 worktree>`（head `1fa90ad` 版）輸出：

```
✗ 第 19 行起的列項缺『怎麼驗』證據
✗ 第 20 行起的列項缺『怎麼驗』證據
✗ 第 22 行起的列項缺『怎麼驗』證據
✓ 其餘 13 項皆有證據
exit=1
```

三條紅逐條說明（依 merge-reviewer 規約：不為了討好工具改寫正確敘述）：

- **第 19 行＝查實 13（LS-96 池項查實）**：證據是兩個 Linear comment id（`ea3dee4e`／`98b73b25`）加上用 `mcp__linear__list_comments` 讀原文核對。**這正是 `98b73b25` 第①項記進池裡的缺口**——gate 目前沒有「Linear comment id 當證據」這個類別。誤判，不補假錨點。
- **第 20 行＝查實 14（LS-217 三筆的範圍裁定）**：這項是**裁定**不是量測，依據是票文「不做」欄與 orchestrator R2 裁定的五項範圍，沒有檔案或命令可引。誤判（對「不做某事」的裁定本來就沒有執行痕跡）。
- **第 22 行＝查實 16（race condition／平行優化兩維度宣告跳過）**：這項宣告的是「無可審項目」並列出未跑的東西（未持有 lock、未開模擬器），宣告不存在的事沒有證據可附。誤判。

三條都落在 gate 檔頭 N9 已載明的限制範圍內，不是本 PR 造成的迴歸（我用 `origin/main` 版對同一份 verdict 跑，同樣是這三行紅）。

### 2026-09-12T02:13:32.908Z · Chih-Lin Yeh · `12839b68`

**orchestrator 09-12 10:13**：merge-review R2 `279e7840` **APPROVE**（0 major／1 minor／5 info；427 份自建語料回歸：`origin/main`→R2 新增 findings 0、`PATH_RE` md5 與 main 相同、mutation ⑭⑮⑯ 獨立重放屬實；LS-217 三筆判「不屬本票」同意）。PR #353 已併 main **`0f28033`**，main tip 貼 `promote: no content diff（#353 1fa90ad R2 279e7840）`（status 54032326010），主 checkout 已 pull。minor（COLLABORATION.md:327 句構）＋i1（mutation 語境整行判定 fail-open）＋i2（symlink）＋i4（`xcrun simctl` 升票建議）→ 入池 LS-96（09-12 10:13 comment）。接著 back-merge PR main→development → 併入後 sweeper → 收尾儀式 → Done（沿 LS-215 hotfix 路徑；QA delta 以 reviewer 427 份回歸實跑充當，不另派 qa）。

### 2026-09-12T02:43:51.875Z · Chih-Lin Yeh · `2f2a58c2`

**orchestrator 09-12 10:43**：back-merge PR #354 已併 development **`49717e2`**（CI 綠、dev tip 貼 `promote: no content diff（back-merge main→development，#353 1fa90ad R2 279e7840）` status 54032985345），main `0f28033` ⊂ development ✓。worktree LS-228 已 `cleanup-merged.sh --apply`（10:36）。收尾儀式：已派 dead-code-sweeper → retro → Done。

### 2026-09-12T02:51:40.403Z · Chih-Lin Yeh · `4a8c83e1`

## dead-code sweep — LS-228

**範圍**：`git log --oneline --grep=LS-228 origin/main`（5 commit：`7747e8a`／`db42fef`＝R1，`d2431a1`／`187481b`／`1fa90ad`＝R2）的 `git show --stat`，逐檔巡 `scripts/gates/handoff_evidence_check.py`、`scripts/gates/handoff-evidence-check.sh`、`scripts/gates/handoff-evidence-check.test.sh`、`docs/COLLABORATION.md`（PR #353 唯一觸碰的 4 檔，merge-review R2 `279e7840` 查實 11 已確認 scope 乾淨，本次逐檔覆核一致）。base：`origin/main` 0f28033（本機 main 已同步，唯讀作業，未動任何檔）。

## Findings（2 條，皆屬「R1 一度變更、R2 還原後留下的文件／註解殘餘」，皆不影響 gate 實際行為）

### F1（P3，文件層級）`docs/COLLABORATION.md:328` §7 表格「路徑（子字串比對）」列舉清單漏了 R2 已還原的 `.sh`／`.md`／`.yml`

原文（該行 cell 開頭）：
> 須含測試名、路徑（子字串比對，不驗證存在：`.png`／`.log`／`.test.sh`／`scratchpad/`／`evidence/`／`.swift`／`.py`／`.json`）、白名單目錄路徑……

同一行後段（R5 說明）原文：
> **R5（LS-228 R2，merge-review R1 `43e2f60e`）**：R4 曾把 `.md`／`.sh`／`.yml` 從無條件子字串（見上「路徑」一項）整條移除……——R2 原樣還原這三個副檔名（byte-identical 回 origin/main）……

**判定證據**：`scripts/gates/handoff_evidence_check.py:152` 現行 `PATH_RE = re.compile(r"\.png|\.log|\.test\.sh|scratchpad/|evidence/|\.swift\b|\.py\b|\.sh\b|\.md\b|\.yml\b|\.json\b")`——`.sh`／`.md`／`.yml` 確實已無條件生效（實測 `core.sh`／`notes.md`／`ci.yml` 皆 `PATH_RE.search` 命中）。但 §7 表格開頭那份「路徑（子字串比對）」清單仍只列 8 項、沒有 `.sh`／`.md`／`.yml`，讀者只看清單本身會誤以為這三個副檔名不算無條件子字串證據，要讀到後面 R5 那句「原樣還原」的敘事性說明才會發現——清單本身沒有同步更新。比對 `db42fef..1fa90ad` 的 `docs/COLLABORATION.md` diff，這份清單八個項目在 R1→R2 全程一字未動，證實是 R2 改寫這段大量文字時漏掉的一處。

**佐證（非 LS-228 改動、獨立來源、內容正確）**：`.claude/agents/merge-reviewer.md:25` 與 `.claude/agents/qa.md:43`（皆不在 LS-228 diff 範圍內，PR #353 未觸碰這兩檔）對同一份路徑清單寫的是「`.png`／`.log`／`.test.sh`／`scratchpad/`／`evidence/`／`.swift`／`.py`／`.sh`／`.md`／`.yml`／`.json`」——11 項齊全、與現行程式碼一致，反過來證明 COLLABORATION.md 這份清單才是落後的那一份，不是新設計方向。

**建議處置**：不影響 gate 行為（純文件），P3／size XS，隨下一張碰 COLLABORATION.md 的 harness 票順手補（把清單改成 11 項，與 merge-reviewer.md／qa.md 對齊），不必為此單獨開票；若 orchestrator 傾向現在就記，可與已入池的 `5d6e51b6`（同檔同行的句構斷裂）合併一起改，反正都在同一個 cell。

### F2（P3，註解層級）`scripts/gates/handoff-evidence-check.test.sh:667` mutation ⑧ 的段落註解仍描述 R1（R4）已被 R2 推翻的舊狀態

原文：
```
# ==== ⑧（R3，m3；R4／LS-228 縮小為 .py/.json）mutation：PATH_RE 拿掉 .py/.json → ①n 的正樣本必須改判紅
#        （.sh/.md/.yml 已在 R4 移出 PATH_RE，改由 ⑬ 驗證 PATH_ANCHOR_RE 那條路） ====
```

**判定證據**：這句括號註解說的是 R1／R4 的實作（`.sh`／`.md`／`.yml` 被移出 `PATH_RE`，只能靠白名單目錄的 `PATH_ANCHOR_RE`／mutation ⑬ 驗證）。R2（`d2431a1`）已把這三個副檔名原樣還原回 `PATH_RE`（同 F1 的程式碼證據；mutation ⑯ 就是專門驗證這件事，見同檔 :881-910），但這裡的括號註解沒有跟著更新，繼續講一個已經不成立的事實。

**是否造成測試失效**：不會。`bash scripts/gates/handoff-evidence-check.test.sh` 實跑 76/76 全綠（尾行 `✓ handoff-evidence-check 自測通過（76 組樣本）`）；⑧ 的 mutant 字面把 `PATH_RE` 換成 `r"\.png|\.log|\.test\.sh|scratchpad/|evidence/|\.swift\b"`（同時拿掉 `.py`／`.json`／`.sh`／`.md`／`.yml` 五個副檔名），斷言樣本 `①n` 的內容只含 `some/config.json`，與 `.sh`／`.md`／`.yml` 無關，所以斷言邏輯仍成立、不是假綠——純粹是註解文字沒跟著 R2 的還原同步更新，屬鷹架殘留，不是功能缺陷。

**建議處置**：不影響 gate 行為，P3／size XS。建議措辭改為「（.sh/.md/.yml 已於 R5／LS-228 R2 還原回 PATH_RE，本 mutant 額外一併拿掉這三個副檔名但不影響 ①n 斷言，因 ①n 樣本只用 `.json`；.sh/.md/.yml 的還原由 ⑯ 獨立驗證）」或類似字句；隨下一張碰這支測試檔的票順手改，不必為此單獨開票。

## 既有死碼觀察（LS-228 引入之前就存在，非本票責任，已核對仍準確）

以下三項為 orchestrator 派工單指名的既知池項，逐一核對現況：

- **`5d6e51b6`**（`docs/COLLABORATION.md:327`，現行檔案位移一行後在 :328）：R5 插入段把原句「或指令……其中一種證據」截斷、語意斷裂——**核對：仍存在，未被 R2 或後續任何 merge 修正**（本次巡檢逐字重讀該行，斷句問題原封不動）。
- **`51bd1635`**（`docs/COLLABORATION.md:332`，現行位移一行後在 :333）：background-bash-guard 那列「效果」欄仍寫「⚠️仍待 `/hooks` 重載後 orchestrator 用 ios-dev 派最小任務實測」，實測已於 09-12 通過——**核對：仍是舊字句，未更新**（本次巡檢重讀 :333 全文確認）。LS-228 的 diff 沒有觸碰這一列（PR #353 只改了 handoff-evidence-check 那一列），非本票引入也非本票該修。
- **`98236da2`**（`docs/COLLABORATION.md:225`）：「Leaked Password Protection」那行指向只存在於 scratchpad 的 `LS-224-hibp.sh`——**核對：仍是舊字句，未更新**（本次巡檢重讀 :225 全文確認，`LS-224-hibp.sh` 字面仍在，未落地成 `scripts/ops/` 下的正式腳本）。同樣不在 LS-228 diff 範圍內。

三項行號較池項記錄時各多 1（327→328、332→333），原因是同一份文件在 LS-228 之後又被另一支不相關的 PR 加了一行（LS-228 自己的 diff 對 COLLABORATION.md 是「1 insertion／1 deletion」，未改變檔案總行數），純屬檔案漂移、內容本身無變化，不需另記。

## 其餘檢查（皆通過、無發現）

- **函式／常數呼叫點**：`handoff_evidence_check.py` 全部 16 個 top-level 函式與全部 regex 常數逐一 `grep` 計數，每個至少 1 個呼叫點＋1 個定義點，無 R1 引入、R2 遺留的孤兒函式／常數；`is_negated`／`is_mutation_context`／`NEGATION_WORDS`／`CLAUSE_BOUNDARY_RE`／`MUTATION_CONTEXT_RE` 皆單一定義、被 `test_name_candidates()` 與 `path_anchor_candidates()` 共用（無重複定義）。
- **存在性檢查路徑**：`grep -n "isfile\|os.path\."` 全檔只有一條存在性驗證路徑（`path_within_repo()`／`path_exists_in_repo()`），無舊版殘留的另一套檢查。
- **自測 fixture**：R1 建立、R2 廢棄的 `$R/README.md`／對應 ②j 舊樣本（`.md` 在白名單目錄外的邊界示範）已在 R2 完整移除（`grep -n README` 對 `.py`／`.test.sh` 皆 0 命中），無殘留 fixture 檔案；巡了 `handoff-evidence-check.test.sh` 建立的全部 fixture（`Fixture/`、`FixtureTests/`、`RealSampleFixture/`、`supabase/`、`docs/`、`.claude/`、`scripts/`、`.github/workflows/`、`$work/outside-escape.sh`），每個檔案皆至少被一組 `expect` 或 mutation 樣本引用，無孤兒 fixture。
- **CI／設定殘留**：`.github/workflows/ci.yml` 對 `handoff-evidence-check`／`handoff-evidence-check.test.sh` 的引用皆對得上現行檔名，無指向已改名檔案的路徑。
- **`agent-tools-check.sh`**：`qa`／`merge-reviewer` 的 `BODY_RULES` 對 `handoff-evidence-check.sh` 的釘句仍對得上。

## 搜尋方法

`git show --stat`／`git diff <ref>..<ref> -- <path>` 逐 commit 比對；`grep -n`／`grep -c` 交叉核對函式呼叫點與常數定義；`python3` 直譯確認 regex 行為（`PATH_RE.search` 對 `.sh`／`.md`／`.yml` 樣本）；實跑 `bash scripts/gates/handoff-evidence-check.test.sh`（76/76 全綠）驗證找到的兩處殘留確實不影響行為；`grep -rl` 全 repo 掃描其他檔案是否引用本票改動的符號／清單，找到 `merge-reviewer.md`／`qa.md` 作為獨立佐證來源。

## 盲區聲明

純文字／`grep`／`git show` 搜尋抓不到：reflection、字串拼接組出的路徑、任何未落在 diff 裡但語意相關的 runtime 行為差異；未執行 fuzz／隨機語料回歸（僅重跑 merge-review R2 已驗證過的 76 組自測與少量手動 regex 驗證，未重做 R2 查實 3／4 提到的 427 份語料掃描，那是 reviewer 職責範圍非 sweeper）；未對 Supabase 或任何資料庫查證（本票無 SQL 變更，未持有 `supabase-lock.sh`，未開容器）；未跑模擬器；未派 subagent；未動任何檔案。

## 建議處置彙總

- F1／F2：皆 P3、純文件／註解、不影響 gate 行為——建議記入 LS-96 待辦池，隨下一張碰 `docs/COLLABORATION.md` 或 `handoff-evidence-check.test.sh` 的 harness 票順手修，不必為此單獨開票。
- 既有死碼觀察三則：核對後確認仍準確、未被本票處理，維持既有池狀態（`5d6e51b6`／`51bd1635`／`98236da2`）不變，不重複記。

### 2026-09-12T02:52:26.060Z · Chih-Lin Yeh · `7666859e`

**orchestrator 09-12 10:52 收尾儀式（§6）**
① dead-code-sweeper `4a8c83e1`：2 新發現（F1 COLLABORATION.md:328 §7 列舉漏 `.sh`／`.md`／`.yml`；F2 自測 ⑧ 註解過期）皆文件層級 → 入池 LS-96（09-12 10:52 comment，docs P3 XS，與 `5d6e51b6`／`51bd1635`／`98236da2` 同批）；16 支函式與全部 fixture 皆有呼叫端、無殘留。
② lesson learning：
- harness：R1 被退的根因是「收緊既有行為」（拿掉 `PATH_RE` 三副檔名）＋沒跑既有語料回歸；R2 純加法＋reviewer 自建 427 份語料 0 新紅。規則：**改 gate 的正則一律先對既有語料跑基準再改**——已是 §7 慣例，本票證明值得寫成派工句（dispatch-prompt-rules 記）。
- 流程：reviewer 語料存在舊 session scratchpad、R2 reviewer 無法重放只能自建——與 LS-217 R2 同根因（證據位置，池 `6bbf6ab1`）。
- 工具缺口：`pr-body-check` 對 back-merge PR（head＝保護分支）一律 exit 2 是設計，但 orchestrator 兩次試錯才想起；`--verify` 必須在票 worktree 內跑（主 checkout 會假紅）——記 dispatch-prompt-rules。reviewer i4（`xcrun simctl` 錨點誤判升票）→ LS-226 之後的 harness 候補。
③ 清理：10:36 `cleanup-merged.sh --apply LS-228` ✓（worktree 1／分支 0）。④ Pen：不適用。
main `0f28033`（#353）＋back-merge #354 → development `49717e2` ✓；merge-review R2 APPROVE `279e7840`；QA delta＝reviewer 427 份回歸實跑（hotfix 路徑沿 LS-215）→ **Done**。

