# LS-51 chore：設計畫布執行產物入版控規則統一（_shotcheck.html 等，A/B/D 三軌）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Low |
| 標籤 | — |
| Cycle | — |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-24T15:03:55.245Z |
| 開始 | 2026-08-24T20:21:01.646Z |
| 完成 | 2026-08-24T21:42:42.985Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-51/chore設計畫布執行產物入版控規則統一-shotcheckhtml-等abd-三軌 |
| 關係 | related→LS-31, related→LS-59, related→LS-38, related←LS-202, related←LS-63, related←LS-60, related←LS-61 |

## 描述

[LS-38](https://linear.app/little-sprout-app/issue/LS-38/實驗第二輪軌-c-pen-軌-d-canvas-粉色調漸層各-5-輪對抗) 死碼巡檢 F2：`design-canvas*/_shotcheck.html` 是 `_shot.mjs` 每次重寫的執行期產物，卻未列入 `.gitignore`（同類 `shots/`、`measured.json`、`verified.json`、`shots.json`、`selftest.json` 已 ignore）。三軌（design-canvas／design-canvas-d 及任何後續）同一慣例，需一次統一：要嘛全部 ignore＋從版控移除，要嘛明文說明為何保留。順帶盤點各軌 .gitignore 是否一致。低優先，可併入任一次 harness 維護批次。

## 留言（3）

### 2026-08-24T20:46:34.760Z · Chih-Lin Yeh · `6f9bacb8`

## merge-reviewer R1 — PR #79 → ITERATE（1 應修、4 informational；orchestrator 代貼＋裁決）

**F1（應修）**：`design-canvas-d/.gitignore` 加一行 `!_shotcheck.html` → `check-ignore` rc=1、gate rc=0 綠，而檔案仍被追蹤——本 PR 明文倚賴「各軌 .gitignore 是根規則子集」卻無機制強制；§7 卻標無保留 ✅；解法訊息尾句「確實該入版控者改 .gitignore」未區分收窄根規則（可見）與子目錄否定（不可見）。
→ **裁決：(c)＋(b)**——`tracked-ignored-check.sh` 加第二檢查：任何 `design-canvas*/.gitignore` 內出現 `^!` 行即紅（正負對照樣本各一）；解法訊息改為「確實該入版控者：只准收窄根 `.gitignore` 規則，子目錄否定規則會被本 gate 擋」。§7 該列維持 ✅，列內註明「含否定行偵測」。（依「新增重要規則＝同時新增它的 gate」，不採純文件方案 (a)。）

**Informational → 處置**
- F2 封鎖點更早：CI checkout merge ref，development 含本 PR 後 LS-31/LS-38 一開 PR 即紅（LS-38 2 檔、LS-31 1 檔）→ handoff 改寫「下次開 PR 或 commit 前 `git rm --cached`」；LS-31/LS-38 票各記一筆。
- F3 三條產品名 pattern 反方向（該追蹤卻被 ignore）`git add -A` 靜默略過 → 接受、記錄。
- F4 自測驗腳本行為不驗規則內容 → 接受、記錄。
- F5 `.test.sh:36` bash 3.2 全形字元吸進變數名（既有慣例）→ 併入 LS-59（四支一起改 `${name}`）。

**已實測通過**：自測 6/6＋4 mutant 全殺；fail-closed 5/5；全域 excludes 4 條注入無效；效能 5k 檔 37ms／15k 檔 55ms；pre-commit E2E；back-merge 自身不被卡；與 #78 正反序合併同 tree；`--exclude-per-directory` 非 deprecated 且 ubuntu step 已綠；盤點與 PR body 一致；scope 純加法 147/0。

**R2 進場**：F1 機械補強＋樣本＋訊息＋§7 註記；只驗此項。

### 2026-08-24T20:56:35.446Z · Chih-Lin Yeh · `b5456aae`

## merge-reviewer R2 — PR #79 @ `7a4fdaa`：APPROVE（orchestrator 代貼）

- 裁決 (c)＋(b) 落地親複現：第二檢查讀 index（`git show :path`）、`^[[:space:]]*!` 偵測；子目錄否定行 `git add` 不需 `-f` 即成功、gate 紅並點名 `:2:`；只收窄＋註解＋空行綠；同時觸發①②兩違規時兩段診斷都印出才 exit 1。
- `.test.sh` 8/8 綠（系統 bash 3.2）；mutant rc=1 訊號成立（診斷路徑 `:36` 既有 bash 3.2 缺陷 → LS-59 已含）。
- 解法訊息收窄；§7 維持 ✅ 並註明否定行偵測；`merge-tree` 對 main（含 #81）無衝突；scope 3 檔對應 F1。

→ 併入 main（順序 #79 → #78 → #80）。

### 2026-08-24T21:42:56.846Z · Chih-Lin Yeh · `2e3fdb10`

back-merge #86（development）／#87（test）落地，三分支對齊 → **Done**。Lesson：gate 倚賴的資料檔（子目錄 .gitignore）也是攻擊面——「規則是根規則的子集」這種不變量若無機械強制，一行否定就繞過；R1 補了否定行偵測。LS-31／LS-38 下次開 PR 前需 `git rm --cached`（已記各票）。

