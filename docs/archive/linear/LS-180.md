# LS-180 Harness：`pen-open.sh --force-reload` 以 SIGKILL 清場會讓 Pencil MCP 斷線且 session 內不重連——VR 切檔後五支掃描／截圖全部不可用；改為不殺行程的可信切檔（切檔＋磁碟 tree_hash 新鮮度驗證），殺行程只做最後手段並印重連指引；巡檢／派工前偵測 Pencil 連線

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-04T21:34:59.933Z |
| 開始 | 2026-09-04T21:35:00.074Z |
| 完成 | 2026-09-05T03:45:17.098Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-180/harnesspen-opensh-force-reload-以-sigkill-清場會讓-pencil-mcp-斷線且-session |
| 關係 | related→LS-141, related→LS-185, related→LS-175, related→LS-91, related→LS-118, related→LS-177, related→LS-96, related→LS-176, related→LS-163, related←LS-247, related←LS-238, related←LS-234, related←LS-226, related←LS-208, related←LS-201, related←LS-194, related←LS-186, related←LS-184, related←LS-182, related←LS-183, related←LS-179, related←LS-181 |

## 描述

## 來源

[LS-177](https://linear.app/little-sprout-app/issue/LS-177/設計留言愛心互動列與留言-sheetls-22-畫面群推播通知權限提示與設定頁推播通知列-ui-designervisual) visual-review R2 `b017cbd1`（2026-09-05 05:3x）：VR 依派工單 `bash scripts/ops/pen-open.sh <LS-177 worktree> --force-reload` 切檔成功（exit 0，[LS-176](https://linear.app/little-sprout-app/issue/LS-176/harnesspen-openpen-read-對pen-記得的舊-worktree-路徑已不存在視為可安全捨棄設計票新鮮度保證恢復) 舊路徑放行有效），但 `--force-reload` 一律走「安全判定＋kill＋重開」（`scripts/ops/pen-open.sh` 檔頭 :27-28「`--force-reload` 略過『目前已經 active 就直接算成功』的捷徑，一律走清場流程」）——Pen 主行程被 SIGKILL 後 **Pencil MCP 連線中斷且本 session 不重連**（`mcp__pencil__*` 五個工具全部不可用，orchestrator session 亦同），結果：五支溢出掃描與截圖本輪全數無法執行，VR 只能離線驗 tree_hash／結構，R3 被迫等使用者手動重連 MCP。同一機制早在 [LS-163](https://linear.app/little-sprout-app/issue/LS-163/設計歡迎頁以帳號密碼登入小字連結三顆登入鈕下方非大按鈕帳號密碼登入畫面審核帳號用方案-b-ui-designervisual) R1 卡過一次（[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `bed3ca3e`「設計輪進行中被 pen-read 換掉 active document 無護欄」同族）。「照指示切檔」與「照指示重掃」目前互斥。

## 範圍

1. `pen-open.sh`：**優先不殺行程的切檔路徑**——`open -a Pen <file>`／`pen` CLI 切檔後，用「磁碟 .pen 的 `design_tree_hash.py` 結果 vs Pencil 端 `get_app_state()`／`execute` 回讀的 tree_hash 相符」作為新鮮度證明（[LS-118](https://linear.app/little-sprout-app/issue/LS-118/harnesspencil-mcp-execute-filepath-回傳陳舊版本目標檔非-active-doc-時吐-pen) 引入 `--force-reload` 是為了避免 renderer 停在舊磁碟內容；tree_hash 相符即等價證明），相符就不清場；只有不相符且安全判定通過才 kill＋重開，並在 stdout 印「Pencil MCP 需重連：請在 Claude Code 執行 /mcp 重連 pencil」。自測：切檔後 hash 相符不 kill／不相符才 kill 兩案（stub `pen`＋假 hash）。
2. Pencil 連線偵測：`pen` CLI 或 `get_app_state` 等價的 shell 探針（若無 CLI 途徑，至少用 Pen 行程存活＋MCP port 探測）——`patrol.sh` 設計票在飛時印「Pencil MCP：連線／斷線」一行；派工模板（COLLABORATION §4-b）加「設計票派工前先確認 Pencil 連線，斷線先請使用者 /mcp 重連」。
3. VR／ui-designer agent 定義：切檔一律用不殺行程路徑；`--force-reload` 只在 orchestrator 明示時使用，且使用後必回報「需重連」。
4. [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `bed3ca3e` 銷除或改註；COLLABORATION §7 對照表一列。

## 不做

修 Pencil 本身；Claude Code MCP 自動重連（客戶端行為，無法從腳本控制）。

## 驗收

- [ ] 模擬「Pen 停在 A 檔、目標 B 檔、磁碟 B 的 hash 與 Pencil 回讀相符」→ 不 kill 即成功；「不相符」→ 走清場並印重連指引（自測）
- [ ] patrol 印 Pencil 連線狀態；派工模板更新；merge-reviewer 通過；hotfix 流程（自 main、PR 回 main、back-merge）

## 留言（17）

### 2026-09-04T21:35:31.074Z · Chih-Lin Yeh · `14e1ac08`

**2026-09-05 05:36 orchestrator — 派工**

- lane:harness 空 1 輪＋LS-177 VR R2 直接受害 → 立案；cycle 1；hotfix 流程：worktree `.claude/worktrees/LS-180`，分支 `hotfix/LS-180-pen-open-no-kill`（base origin/main `8e00758`，含 LS-176），PR → main，併入後 back-merge。
- 限制：Pen 目前開著 LS-177 worktree 檔且 Pencil MCP 已斷線（等使用者重連）——本票自測一律 stub，**不得對活的 Pen 執行 pen-open／pen-read／kill**。

### 2026-09-04T22:12:05.927Z · Chih-Lin Yeh · `5a4900a5`

# LS-180 Handoff（2026-09-05 06:10）

## Ticket
LS-180 — `pen-open.sh --force-reload` 以 SIGKILL 清場讓 Pencil MCP 斷線；改為不殺行程的可信切檔（tree_hash 新鮮度）＋Pencil 連線偵測＋規約。hotfix 自 `main` 8e00758，PR #289 回 `main`，併後需補 back-merge `main`→`development`。

## 已完成
1. **`pen-open.sh` 不殺行程的可信路徑**（70d51c2）
   - `scripts/ops/pen-open.sh:99`（檔頭 LS-180 段，含實機限制與已知盲區）、`:198`（`--kill` 旗標）、`:234` `read_pen_hash()`（`pen interactive --app desktop` 餵 `execute({ input: overflow-scan.js 全文＋"SCAN_HASH_ONLY = true;" })`，JSON 編碼成 JS 字串字面值；擷取 `SUMMARY-HASH … tree_hash=<16 hex>`；看門狗 `PEN_OPEN_HASH_TIMEOUT` 預設 60s、重試 `PEN_OPEN_HASH_ATTEMPTS` 預設 2）、`:318`（`--kill` 路徑）、`:323-339`（磁碟 `design_tree_hash.py` vs 回讀：相符 exit 0 不清場／讀不到 exit 3 印期望值／不符才落入既有清場）、`:503` `pen_restarted=1`、`:508` `reconnect_notice()`（任何真的結束主行程的路徑都印「⚠ Pencil MCP 需重連：請在 Claude Code 執行 /mcp 重連 pencil」，含預設模式的 R2 自動清場）。
   - `scripts/ops/pen-read.sh:17`（檔頭契約改寫，含 exit 3）。
   - 自測：`scripts/ops/pen-open.test.sh:151`（`set_hash` stub 控制檔；`pen` stub 讀 stdin 分 `execute(`／get_app_state 兩路）、`:627` ⑮a–⑮i；`scripts/ops/pen-read.test.sh:158`／`:171`。
2. **Pencil 連線探針＋巡檢**（2994830）
   - `scripts/ops/pen-status.sh`（新）：`:51-53` `lsof -F dn`＋`pgrep 'Pen\.app/.*mcp-server-[^ ]* --app desktop'`；`:87` 一行輸出 `Pencil：行程 ✓（pid N）／✗ · 路徑 <path>／✗ · MCP 探針 ✓／✗／不可測`；exit 0／1／2。判定＝mcp-server 各 fd 的 `->0x` peer 與 Pen 行程 unix socket 位址集合交集（無 mcp-server ✗、缺 lsof 或讀不到 Pen socket 不可測、多支需全連才 ✓）。
   - `scripts/ops/patrol.sh:224`（分支名含 design → `design_wt=1`）、`:311-327`（探針段、非 0 `add_flag "[Pencil] …"`）、`:502`（`--json` 多 `pencil` 物件）；human 段內附「→ 設計票派工前先請使用者在 Claude Code 執行 /mcp 重連 pencil」、brief 印探針行＋旗標。
   - `scripts/ops/pen-status.test.sh`（新，①–⑩）、`scripts/ops/patrol.test.sh:36`（全檔預設假身隔離真 Pen）／`:642` ㉒、`.github/workflows/ci.yml:275`（rules job 掛 pen-status.test.sh）。
3. **agent 定義＋gate**（d5fc707）
   - `.claude/agents/visual-reviewer.md:11`（pen-read exit 0／3／1／2 處置；輸出含「需重連」即停下回報；`--kill` 只在 orchestrator 明示時）、`.claude/agents/ui-designer.md:15`（切檔只用不帶旗標 `pen-open.sh`；`--force-reload`／`--kill` 只在 orchestrator 明示時，用後回報需重連）／`:64`（步驟 5 若印需重連必附）、`.claude/agents/qa.md:10`（exit 3 與需重連句處置——qa 也是 `pen-read.sh` 呼叫端，契約多了 exit 3 所以一併同步，超出票文點名的兩份定義）。
   - `scripts/gates/agent-tools-check.sh:66-67` BODY_RULES 加 ui-designer／visual-reviewer 正文必含「--kill 只在 orchestrator 明示時」；`scripts/gates/agent-tools-check.test.sh:97` ⑦ 三案＋fixture。
4. **COLLABORATION**（f3e318d）：`docs/COLLABORATION.md:61`（§2 pen-read 契約改寫）、`:135`（§4-b 巡檢模板：段清單加「Pencil 連線」；設計票派工前先確認 Pencil 連線——patrol `Pencil：` 行／`pen-status.sh`，斷線先請使用者 `/mcp` 重連再派；派工單切檔句一律普通 `pen-open.sh <worktree>`，旗標只在明示清場時給；agent 回報需重連就再請使用者重連）、`:305`（§7 「切檔不殺行程」列，含盲區）。
5. **LS-96 `bed3ca3e` 改註**：回覆 `3422dc1d`（PR #289；面 1「切檔即殺／斷線」已消、面 2「持有者 lock」保留 P2，附「乾淨文件無 backup」實測對 lock 設計的含意）。

## 已驗證
- `bash scripts/ops/pen-open.test.sh` → **40 組全綠**（⑮a 相符不殺／⑮b 不符＋dirty 不殺 exit 1／⑮c 讀不到 exit 3 印期望值／⑮d 回讀掛住看門狗 1s 收工 exit 3／⑮e `--kill` execute 零次＋清場＋需重連／⑮f `--kill` dirty 仍擋／⑮g 預設模式 execute 零次／⑮h 旗標互斥與拼錯 exit 2／⑮i 壞 .pen exit 2；⑪a／⑬a 加驗需重連句、⑬d 沒殺不印；⑬a–⑬c 改為雜湊不符才清場）。
- `bash scripts/ops/pen-read.test.sh` → **10 組全綠**。
- `bash scripts/ops/pen-status.test.sh` → **11 組全綠**（stub pgrep／lsof／pen；含 ⑨ mutation 防退化：有額外 unix fd 但 peer 非 Pen 仍 ✗）。
- `bash scripts/ops/patrol.test.sh` → **全綠**（㉒：無 design worktree 略過且不呼叫探針／探針 ✗ human 段內指示＋brief `[Pencil]`＋json `pencil.ran/rc/line`／探針 0 不標／探針腳本不存在標 `[Pencil]`）。
- `bash scripts/gates/agent-tools-check.sh` → 通過（6 份；正文必含字樣 6 條）；`agent-tools-check.test.sh` → **44 組全綠**；`brand-skill-check.sh` 通過；`pr-body-check.sh` 通過。
- **真環境只做唯讀觀察**（硬限制遵守：未對活的 Pen 跑 pen-open／pen-read／`--status`／open／kill）：
  - `pgrep -fl Pen.app`：Pen 主行程 pid 3185、renderer 3213（`--init-params` fileURI＝LS-177 worktree，isDirty:false）、Pencil mcp-server pid 24097（`…/mcp-server-darwin-arm64 --app desktop --agent claudeCodeCLI`）。
  - `lsof -nP -a -U -p 3185 -F dn`：`d0x1b7c…`＋`n/Users/clyeh/.pencil/socket/pencil-desktop.sock`（listen）與一條已接受連線；`lsof … -p 24097 -F dn`：只有 f0／f1／f2，peer 皆不在 Pen 集合 → 這就是「斷線」的機械訊號，探針交集邏輯依此形狀寫（連線正常的樣本本票沒有）。
  - `pen interactive --help`：shell 工具只有 browser／execute／get_app_state／get_style／read_skill（**無**切檔／關檔工具）；`execute({ input })` 可用，「file path is automatically injected」。
  - `~/.pencil/backup/`：LS-177 路徑（sha1 `c1890f74…`）與主 checkout 路徑皆**無** backup——Pen 只對 dirty 文件寫 autosave，所以「用 backup 檔當 renderer 記憶體代理算雜湊」不可行（原本考慮的替代設計，已否決）；磁碟 `design_tree_hash.py` LS-177 ＝ `80e2bae6e897e950`（與 VR R2 收據一致）。

## 未完成
- **Pencil 端雜湊回讀的 CLI 實跑格式未實機驗**（硬限制）：`read_pen_hash()` 的 REPL 輸入（`execute({ input: "<JSON 字串>" })` 單行 ~45 KB）與 `SUMMARY-HASH` 行擷取只以 stub 自測。失敗方向是 exit 3（不殺、不放行、agent 複算）。**請 orchestrator 在使用者 `/mcp` 重連後對 LS-177 worktree 跑一次** `bash scripts/ops/pen-open.sh /Users/clyeh/little-sprout/.claude/worktrees/LS-177 --force-reload`：期望 exit 0 印「tree_hash=80e2bae6e897e950 與磁碟一致」（renderer 3213 是 05:22 重開後讀的現稿、磁碟 05:12 未再變）；若 exit 3 就是 CLI 路徑要調（REPL 參數格式／大稿 interrupted），屆時已可直接觀察 CLI 輸出。
- **替代設計說明**（票文允許）：Pencil 端唯一 shell 途徑是 `pen interactive` 的 `execute`，它在 9000 節點級的稿會機率性 `InternalError: interrupted`（LS-177 R1／R2 實測，VR 靠分段累加繞過）——所以雜湊回讀只當快路徑，讀不到即「印期望值＋exit 3 交 agent 複算」（`pen-open.sh:99` 檔頭第 3 點明說）。沒有做 CLI 端分段累加（需改 overflow-scan.js 加範圍旗標、多次 execute，本票不做）。
- **需裁決（規約，未動）**：`open -a Pen` 對已在背景視窗開著的路徑不會奪回 active（pen-open.sh 檔頭既有已知坑，LS-177 R1 VR 實證仍成立）。ui-designer 步驟 5「handoff 前切回主 checkout」會把票檔留在背景視窗 → 下一輪 VR `pen-read.sh` 回到票檔仍走清場（會斷線）；本 PR 的不殺路徑只救「目標已是 active」（例：VR R2→R3 同稿連讀、orchestrator 已切檔後派 VR）。建議：設計票期間 ui-designer **不切回主 checkout**（票檔保持 active，designer↔VR 交替零 kill；下一張設計票 `open -a Pen <新路徑>` 不需 kill），改由 orchestrator 在票結案／清理時 `pen-open.sh <主 checkout> --kill` 一次清場並請使用者重連。影響 ui-designer.md 步驟 5、COLLABORATION §2「設計票收工條件」、LS-176 的「移除 worktree 前切回主 checkout」（已因 LS-176 舊路徑放行而非必要）。未在本票動，因為是使用者核可過的規約（R2）。
- **reviewer informational**：本票尚未進 merge-review，無 informational 可申報；PR #289 待 merge-reviewer。
- 不做（票文）：修 Pencil 本身、Claude Code MCP 自動重連。

## 風險
- MCP 探針只有斷線樣本：連線正常時 mcp-server 是否真以 unix socket 連 `pencil-desktop.sock` 待第一次重連後對照（`bash scripts/ops/pen-status.sh` 應印 ✓）；誤報方向是 ✗（fail loud）。多支 mcp-server（多 session）分不出哪支是本 session，部分斷線一律 ✗。
- `patrol.sh` 判設計票在飛只看分支名含 `design`；不符慣例的分支不觸發探針。
- exit 3 是新回傳值：呼叫端只有 VR／qa 定義（已同步）與 `patrol-linear.sh`（只用 `--status`，不受影響）。
- 硬限制下本票沒有跑過 `pen-open.sh --status`／`pen-status.sh` 的真環境輸出；pen-status 的路徑段複用既有 `--status` 邏輯。

## 產出位置
- PR **#289** `hotfix/LS-180-pen-open-no-kill` → `main`，head `f3e318d903370ceaad28c557b95b27af58163f70`；commits 70d51c2／2994830／d5fc707／f3e318d（每項各一，皆已 push；CI 未等）。
- Linear：LS-96 `bed3ca3e` 回覆 `3422dc1d`。
- 暫存：scratchpad `LS-180-pr-body.md`、`LS-180-handoff.md`。
- 模擬器／supabase 容器：未碰。

### 2026-09-04T22:12:53.566Z · Chih-Lin Yeh · `4025d19c`

**2026-09-05 06:1x orchestrator — R1 handoff `5a4900a5` 收到（PR #289 head `f3e318d`）；規約裁決後補一輪再派 review**

- 裁定「規約待裁決」：**採納**——設計票進行期間 Pen 一律停在該票 worktree 檔（designer 收工不切回主 checkout；VR 直接讀同一檔），票結案（PR 併入 dev 後）由 orchestrator 用 `--kill` 清場一次並請使用者重連 MCP。`ui-designer.md` 步驟 5 與 COLLABORATION §4-b 派工模板隨本 PR 改；VR 定義補「若 Pen 已在票檔且 hash 相符，不切檔」。
- 之後 In Review → merge-reviewer R1。

### 2026-09-04T22:18:15.469Z · Chih-Lin Yeh · `c3c3fd62`

## LS-180 Handoff 補記（2026-09-05 06:2x）——規約裁決「設計票期間 Pen 停在票檔」已落地

orchestrator 裁決採納 handoff「需裁決」項：設計票進行期間 Pen 一律停在該票 worktree 檔；ui-designer 收工不切回主 checkout；VR 讀同一檔、收工也不切檔；票結案（設計 PR 併入 development 後）由 orchestrator `pen-open.sh <主 checkout> --kill` 清場一次並請使用者 `/mcp` 重連。

**PR #289 新 head `371dbfcbade4aca69c49fdb0818371206ecfad56`**（commit `371dbfc` `docs(agents): LS-180 裁決……`，已 push；PR body 已增第 5 項＋風險更新，在 push 前經 `pr-body-check.sh` 通過並 `gh pr edit`）。

### 變更檔案:行號
- `.claude/agents/ui-designer.md:64`（步驟 5 整段改寫：**不**切回主 checkout、不跑 `pen-open.sh`，收工只 `get_app_state()` 確認路徑仍＝票檔並在 handoff 註明；票結案清場由 orchestrator 做）；`:15`（切檔由 orchestrator 派工前執行，你收工不切檔）；`:83`（handoff「Pen 路徑」欄加「收工 Pen 停在票檔的路徑」與需重連原句）。
- `.claude/agents/visual-reviewer.md:11`（LS-180 裁決：Pen 停在票檔，`pen-read.sh` 路徑立即一致、只比雜湊、不殺；你收工也不切檔）；`:54`（handoff「Pen 路徑」欄加新鮮度證明方式）。
- `docs/COLLABORATION.md:61`（§2「設計票收工條件」改為 LS-180 裁決，取代 R2「Pen 只開主 checkout」；移除 worktree 前不必再切回，LS-176）；`:135`（§4-b：派工單不要再寫「收工切回主 checkout」、同票下一輪 `pen-status.sh` 已在票檔就不切檔、設計 PR 併入 development 後執行 §6 ④）；`:223`（§6 收尾儀式加 **④ Pen 清場（設計票限定）**：`pen-open.sh <主 checkout> --kill` → 使用者 `/mcp` 重連 → `pen-status.sh` ✓ 才算完成、記 ticket comment；一票只清一次，不在 designer↔VR 每輪之間清）；`:305`（§7 列盲區改為裁決後的兩種會殺情況，狀態欄加「收工不切回⚠️巡檢事後」）。
- `scripts/gates/agent-tools-check.sh:70` BODY_RULES 加 `ui-designer|收工 Pen 停在票檔`（只驗新句在；「切回主 checkout」字樣本身不擋，步驟 5 需要用它說明為什麼不切）；`scripts/gates/agent-tools-check.test.sh:105` ⑧ 四案（含句通過／只有 --kill 句缺收工句紅／改回「切回主 checkout」而無收工句紅／visual-reviewer 不要求）。
- `scripts/ops/pen-read.sh:37`、`scripts/ops/pen-open.sh:123`（檔頭「ui-designer 收工切回主 checkout」的舊敘述改為裁決後語意）。

### 已驗證
- `bash scripts/gates/agent-tools-check.sh` → 通過（6 份；正文必含字樣 7 條）；`agent-tools-check.test.sh` → **48 組全綠**；`brand-skill-check.sh` 通過；`bash -n` pen-open／pen-read／agent-tools-check 通過；`pr-body-check.sh` 通過。
- grep 三份 agent 定義＋pen-read.sh 的「切回主 checkout」：只剩否定／說明句（ui-designer:15／64／83、VR:11、pen-read:37），無殘留指令句。
- 硬限制照舊：未碰活的 Pen。

### 未做／待 orchestrator
- `CLAUDE.md` 的「Feature 收尾儀式」摘要句仍列三項＋指向 §6，未加 ④——是否寫進 CLAUDE.md 由 orchestrator 決定（本 PR 只改 COLLABORATION §6）。
- 「收工不切回」本身無機械 gate（BODY_RULES 只驗 ui-designer.md 有那句）；agent 有沒有真的跑 `pen-open.sh <主 checkout>` 只有巡檢的 Pencil 路徑行事後看得到——已寫進 §7 盲區。
- LS-177 現況正好符合新規約（Pen 停在 LS-177 檔、renderer 為 05:22 重開的現稿）：使用者 `/mcp` 重連後，VR R3／designer R3 派工前跑 `bash scripts/ops/pen-status.sh`（期望路徑＝LS-177 檔、MCP ✓），VR 用 `pen-read.sh` 應走「雜湊相符不殺」或 exit 3 複算——這也是 `read_pen_hash()` CLI 路徑的第一次實機驗證點。

### 2026-09-04T22:19:19.855Z · Chih-Lin Yeh · `9fc7a82b`

**2026-09-05 06:2x orchestrator — 規約補記 `c3c3fd62` 收到（head `371dbfc`）→ In Review，派 merge-reviewer R1**

- 併入 main 後：back-merge main→development；MCP 重連後實機驗 `pen-status.sh`＋`pen-open.sh <LS-177 wt> --force-reload`（期望 exit 0 印 hash 一致或 exit 3）作為 QA 條件。
- CLAUDE.md 收尾儀式摘要句加「④ Pen 清場（設計票限定）」由 orchestrator 隨 back-merge 補（一行文件）。

### 2026-09-04T22:32:39.525Z · Chih-Lin Yeh · `9dfec264`

## merge-review R1 — PR #289（head 371dbfc）

裁決：**APPROVE**（無 blocker／無 major；**1 條 minor（N1）＋4 條 informational**）。審的 head＝`371dbfcbade4aca69c49fdb0818371206ecfad56`，與 `gh pr view 289 --json headRefOid` 當下值相同；base `8e00758`，15 檔 +742/−84；`gh pr checks 289` **四項全 pass**（ci 6m3s／db 3m38s／lint 13s／rules 3m27s，run 33924898461——開審時三項在跑，等收工才判讀）。2026-09-05 06:30 CST。

**硬限制遵守**：對活的 Pen **只跑了授權的那一次 `pen-status.sh`**（結果見下）。**先揭露一件呼叫鏈事實**：`pen-status.sh:41` 內部會呼叫 `pen-open.sh --status`（即 `pen interactive --app desktop` 的 `get_app_state()`）——純唯讀、不切檔不殺行程，屬授權的探針行為，但值得寫明以免日後有人以為「pen-status 完全不碰 CLI」。其餘一律 stub；未跑 `--force-reload`／`--kill`；未碰模擬器與容器。跑自測前我先確認 `pen-open.test.sh` 以 PATH stub 換掉 `open`／`pgrep`／`osascript`／`ps`／`pen` 五個外部命令、kill 只送給測試自己起的假行程，才敢執行。

### （1）`--force-reload` 新語意 — 決策鏈正確，三個 mutation 全殺

判定順序（`pen-open.sh:319-345`）：`--kill` → 跳過比對直接清場（有 stderr 註記）；否則路徑一致時先算磁碟 hash（算不出 **exit 2 fail closed**）→ `read_pen_hash()` 回讀 → **相符 exit 0 不清場**／**讀不到 exit 3**（印期望值與複算指引，不殺不放行）／**不符**才落入既有清場流程。

| Mutant | 結果 |
|---|---|
| M1 把「相符」判斷反過來（`=` → `!=`） | **殺**——⑮a 紅（相符卻清場）**且** ⑬a／⑬b／⑬c 同時紅（不符卻不清場）：兩個方向都被釘住 |
| M2 「讀不到 → exit 3」改成 `exit 0` 放行 | **殺**——⑮c／⑮d 紅（必須 exit 3 且不 kill） |
| M3 清場成功那條 `exit 0` 拿掉 `reconnect_notice` | **殺**——⑪a／⑬a／⑮e 三案同時紅（重連訊息在三條清場路徑上都被釘） |

**「任何真的結束 Pen 主行程的路徑都印重連訊息」我逐分支核對過**：`pen_restarted=1`（`:505`）之後的三個出口（`:520` exit 0／`:524` exit 1／`:528` exit 2）全部呼叫 `reconnect_notice()`。唯一不印的出口是 `:501-503`「SIGKILL 後仍存活——需人工介入」——**那條路徑上行程確認還活著（`kill -0` 成功）、主行程沒有結束，MCP 也就沒斷**，不印是對的。基準線：`pen-open.test.sh` **40 組**、`pen-read.test.sh` **10 組**全綠。

### （2）`pen-status.sh` — 交集判定穩健，我在真機跑到了正確答案

授權的那一次實跑（本機當下狀態＝Pen 開著 LS-177 票檔、MCP 已斷）：

```
Pencil：行程 ✓（pid 3185） · 路徑 …/worktrees/LS-177/design/littlesprout.pen · MCP 探針 ✗（mcp-server 1 支皆無 Pen socket 連線——…執行 /mcp 重連 pencil）   exit=1
```

三段全部答對，**而且順帶證明一件對審點 5 很關鍵的事：`pen interactive` 這條 CLI 通道現在是活的**（路徑就是透過它讀出來的）。

判定邏輯：`lsof -nP -a -U -p <pen_pid> -F dn` 取 `d…` 位址集合，對每支 mcp-server 取 `n->…` peer 做交集，**全部連上才 ✓、部分連上 ✗、零連上 ✗**。自測 ⑨ 專門釘住「有多餘的 unix fd 但 peer 不在 Pen 集合 → 仍 ✗」，也就是它驗的是交集、不是「有 socket 就算」——這正是這種探針最容易寫錯的地方。fail 方向分析見 I-2／I-3。`patrol.sh` 只在有 `design` 分支 worktree 時才跑探針（`design_wt=1`），非 0 才 `add_flag "[Pencil] …"`，`PATROL_PEN_STATUS_SH` 可換假身；`patrol.test.sh` ㉒ 對 human（段內指示）／brief（`[Pencil]` 旗標）／json（`pencil.ran/rc/line`）三面都有斷言，外加「無 design worktree 不呼叫探針」與「探針腳本不存在也標旗標」。`pen-status.test.sh` 11 組全綠。

### （3）規約落地

- `BODY_RULES` 現有 **7 條**，含本票新增的 `ui-designer|--kill 只在 orchestrator 明示時`／`ui-designer|收工 Pen 停在票檔`／`visual-reviewer|--kill 只在 orchestrator 明示時`；真 repo gate 綠並逐條印出；`agent-tools-check.test.sh` **48 組**全綠（含 ⑧「改回『切回主 checkout』而無收工句 → 紅」這個正反樣本）。
- `ui-designer.md:64` 步驟 5、`visual-reviewer.md:11`、`qa.md:10`（exit 3 契約，超出票文點名的兩份但屬同一契約的呼叫端，合理）、COLLABORATION §2:61／§4-b:135／§6:223 ④／§7:305 都改到位。
- **「切回主 checkout」全 repo grep：多數是否定句／說明句／測試 fixture，但有一處是活的舊規則 → 見 N1。**

### （4）申報查實／相容／範圍

- 15 個檔全部落在票文 1–4（三份 agent 定義、COLLABORATION、agent-tools-check＋自測、patrol＋自測、pen-open／pen-read／pen-status＋三支自測、ci.yml 掛 pen-status.test.sh），**無他票夾帶**。
- **LS-96 `bed3ca3e` 的改註 `3422dc1d` 我到 LS-96 抓全部 comment 核對：存在**，而且是**改註不是銷除**——明白分成兩面，只宣稱面 1（切檔即殺／斷線）已消、面 2（持有者 lock）保留 P2。這個自我約束是對的。
- **bash 3.2**：macOS `/bin/bash` 3.2.57 實跑六支自測（pen-open／pen-read／pen-status／patrol／agent-tools-check／cleanup-merged）**全部 OK**。
- `pr-body-check.sh --verify` 綠。

### （5）裁定：CLI 回讀格式未實機驗 — 風險可接受，但列為併入後 QA 硬條件

**我的判斷：這個未知比 handoff 自陳的更小，因為 transport 已經被證明。** `read_pen_hash()` 用的呼叫形狀與既有、天天在跑的 `poll_once()` **完全相同**——同樣是 `"$PEN_BIN" interactive --app desktop < <暫存檔> > <暫存檔> 2>&1 &` ＋背景 kill 看門狗，暫存檔內容都是「`<一行指令>`＋`exit()`」，差別只在指令本體（`execute({ input: … })` vs `get_app_state()`）與擷取用的 grep。而 `pen --help` 也確認 `pen interactive` 就是官方給 agent 用的模式（"Agents should use this mode to design with pen.dev"），我這次 `pen-status.sh` 實跑更是當場用同一條通道讀到了正確路徑。

剩下的未知只有三項，**而且全部 fail 向 exit 3（不殺、不放行、交 agent 複算）**：(a) 單行 ~45 KB 的 `execute({ input: "…" })` REPL 是否吃得下；(b) `Print` 的輸出是否原樣回顯到 stdout 讓 `SUMMARY-HASH …` 抓得到；(c) 9000 節點級的 execute 是否在 60s 內完成而不 `InternalError: interrupted`。三者任一失敗都只會退化成「多一次 agent 複算」，不會誤殺行程、也不會把陳舊 renderer 當成新鮮。

**併入後 QA 條件（請 orchestrator 在使用者 `/mcp` 重連後依序做，全部唯讀）**：
1. `bash scripts/ops/pen-status.sh` → 期望三段皆 ✓（路徑＝LS-177 票檔、**MCP 探針 ✓**）。**這同時是探針「連線正常樣本」的第一次驗證**——本票只有斷線樣本，✓ 那條分支至今沒被真實資料走過。
2. `bash scripts/ops/pen-open.sh /Users/clyeh/little-sprout/.claude/worktrees/LS-177 --force-reload` → 期望 **exit 0** 並印「tree_hash=`80e2bae6e897e950` 與磁碟一致（未清場）」。
3. 若得到 **exit 3**：把 `read_pen_hash()` 那次的 CLI 原始輸出貼回 LS-180（此時 MCP 已連上、可直接觀察），據以判斷是 (a)(b)(c) 哪一項——**exit 3 不是回歸，是設計好的安全退路**，但要開 follow-up 修 CLI 路徑。
4. 若得到 **exit 2/1**：屬既有清場路徑，必須看到「⚠ Pencil MCP 需重連」那行，並回報。
5. 驗完把 1–4 的實際輸出貼進 LS-180 comment，作為本票 acceptance 的最後一格。

### Finding

**N1（minor）`docs/COLLABORATION.md:262` 的舊 §7 列還寫著「設計票收工前須切回主 checkout」，與本票的裁決正面衝突。**
本 PR 已改 §2（`:61`）、加 §6 ④（`:223`）、加新 §7 列（`:305`）、改 `ui-designer.md:64`（「**不**切回主 checkout」，且被 BODY_RULES 釘住），但 LS-91 那一列（`:262`）的規則欄仍原樣保留「orchestrator 派工前先切檔、**設計票收工前須切回主 checkout**（§1／§2，LS-91…）」。同一張 §7 對照表出現兩條互相矛盾的規則；照 `:262` 做（收工切回）正好會把票檔留在背景視窗、讓下一輪 VR 只能清場 → 重現本票要修的斷線事故。
handoff 與補記都寫「grep …無殘留指令句」——**這一處不符**（其餘命中確實都是否定句／說明句／fixture，`cleanup-merged.sh:291` 的「切回主 checkout」是結案清理的處置提示、與新規約一致，不算殘留）。
修法：把 `:262` 規則欄那一小句改成「設計票期間 Pen 停在票檔、結案時由 orchestrator 依 §6 ④ 清場（LS-180，見下方『切檔不殺行程』列）」。一句話的事，可在 back-merge 時順手改，**不必為它另開一輪**——因為 agent 真正讀的三份定義都正確且有 gate，orchestrator 讀的 §4-b 派工模板也已更新。

### Informational

**I-1 patrol brief 模式的三個看門狗會疊加，逼近 SessionStart 30s 預算** — fetch 10s ＋（低水位時）du 8s ＋（有 design worktree 時）Pencil 探針上界 8s（`PEN_OPEN_ATTEMPT_TIMEOUT` 預設 8）≈ 26s ＋ 其他工作。今天的機器狀態正好三個條件都成立（設計票在飛＋磁碟曾低於門檻）。我這次實跑探針是秒級返回（CLI 有回應時很快），8s 只是上界；但建議 brief 模式給探針一個更短的 attempt timeout（例如 patrol 呼叫時帶 `PEN_OPEN_ATTEMPT_TIMEOUT=4`），或在 brief 模式只讀行程／mcp-server 存在性、跳過會打 CLI 的路徑段。

**I-2 `不可測` 的 fail 方向不對稱** — lsof 讀不到 **Pen** 端 socket → 「不可測」且 **exit 0**（fail-open，patrol 不標旗標）；但 lsof 讀不到 **mcp-server** 端 peer → 被算成「沒連上」→ ✗ 且 exit 1（fail-loud）。同一個工具、同一種讀不到，兩邊方向相反。實務上兩個行程同用戶、lsof 不需 sudo，所以這個不對稱幾乎不會發生；記一筆是為了日後真的踩到時知道往哪看。

**I-3 多支 mcp-server 一律「全連才 ✓」** — 機器上若留著別的 Claude session 的殘留 mcp-server，探針會永久 ✗ → 巡檢每輪都標 `[Pencil]`，久了會 alarm fatigue。現在的方向是 fail-loud（安全），且 handoff 已列為已知風險；若日後噪音太大，可改成「≥1 支連上即 ✓、其餘支數列為附註」。

**I-4 `patrol` 判「設計票在飛」只看分支名含 `design`** — 不符命名慣例的設計分支（例如 `feature/LS-<n>-legal-viewer-design` 會中，但 `feature/LS-<n>-pen-*` 不會）不會觸發探針。handoff 已自陳；記一筆。

### 四維度

race：本票的核心風險是「切檔會不會殺掉 Pen」，判定鏈與三條清場路徑的重連訊息我都用 mutation 驗過；`read_pen_hash` 的看門狗＋重試沿用既有 `poll_once` 模式（背景程序＋背景 sleep kill，`$!` 指向 pen 本身而非管線），不會留孤兒。效能：`--force-reload` 快路徑最多多付 `HASH_TIMEOUT`（60s）×`HASH_ATTEMPTS`（2）的**上界**，但那是「回讀失敗」的最壞情況；成功時省下整個 kill＋重開＋重新輪詢（實務上更快，且省掉一次 MCP 重連的人工成本）。巡檢面的成本見 I-1。平行：無並發寫入；探針純唯讀。scope：15 檔全在票文 1–4，`qa.md` 那一處是 exit 3 契約的呼叫端同步（合理外溢），無他票夾帶。


### 2026-09-04T22:34:35.235Z · Chih-Lin Yeh · `77f0de2d`

**2026-09-05 06:35 orchestrator — merge-review R1 `9dfec264` APPROVE 帶 N1 → 派實作者補一行後 reviewer 補 status 再併**

- N1：`docs/COLLABORATION.md:262` 舊 §7 列與裁決衝突 → 本 PR 修（不另開票）；順帶 `CLAUDE.md` 收尾儀式句加 ④ Pen 清場；併 main `34dd954`。
- I-1～I-4 入池 LS-96 `b3db8c07`。
- 併入 main 後 back-merge；MCP 重連後實機驗 `pen-status.sh` MCP ✓ 分支與 `--force-reload` 回讀（reviewer 列的五步 QA 條件）。

### 2026-09-04T22:41:32.657Z · Chih-Lin Yeh · `2c11f082`

## LS-180 merge-review R1 N1 補輪＋main 併入（2026-09-05 06:4x）

**PR #289 新 head `a9266d720769d6a7d7bd79975f57f7ae29e2c724`**（commits `9662f42` fix(ops)／`63f1971` docs(collab)／`a9266d7` merge origin/main 34dd954；皆已 push。PR body 已增第 6 項、驗證段註明併入後重跑，push 前 `pr-body-check.sh` 通過並 `gh pr edit`）。

### N1（`9dfec264`）處置——變更檔案:行號
- `docs/COLLABORATION.md:262`（舊 §7 LS-91 列）兩處：前饋欄「設計票收工前須切回主 checkout」→「設計票期間 Pen 停在票檔、結案由 orchestrator `--kill` 清場（§1／§2／§6 ④；LS-91 原訂『設計票收工前須切回主 checkout』，**LS-180 裁決撤銷**——切回會把票檔留在背景視窗、下一輪只能清場斷線，見 §7『切檔不殺行程』列）」；機械欄「ui-designer 收工程序最後一步、任何動過 .pen 的 agent 結束前都須確認 `pen-open.sh <repo 根目錄>` exit 0」→ 沿革括號（LS-91～LS-176 期間如此，LS-180 裁決改為停在票檔、結案 `--kill`，§6 ④）。
- **全 repo 再 grep**（`切回主 checkout|只開主 checkout|切回 repo 根|pen-open\.sh <repo 根目錄>|pen-open\.sh "$(git worktree list`，排除 .git／evidence／其他 worktree）：reviewer 指出的宣稱確實不成立——除 :262 外**另抓到 `scripts/ops/cleanup-merged.sh:285`／`:291`** pen_guard 處置句仍建議「先 `pen-open.sh <ROOT>` 把 Pen 切回主 checkout」，已改為 `pen-open.sh <ROOT> --kill 清場切回主 checkout（LS-180 §6 ④——不帶 --kill 只會把票檔留在背景視窗、幽靈 renderer 照舊；清場後請使用者 /mcp 重連 pencil）`（commit `9662f42`）。其餘命中（agents:11／15／64／83、COLLAB:61／135／223／305、pen-open.sh:124、pen-read.sh:37、agent-tools-check.sh:60-61、其 test:43／44／104／107）皆為否定／說明／測試負樣本句；再以更寬的 `切回|只開主|收工.*主 checkout` 掃 docs／CLAUDE.md／agents／skills／scripts，剩餘命中全是 `supabase-lock` 的「收工 `--release`」誤中。`cleanup-merged.test.sh:545` ⑰ 只斷言處置句含 `pen-open.sh`，仍綠。
- `CLAUDE.md:14` 收尾儀式句加「＋**④ Pen 清場（設計票限定**：設計 PR 併入 development 後 orchestrator `bash scripts/ops/pen-open.sh <主 checkout> --kill` 並請使用者 `/mcp` 重連 pencil——設計票期間 Pen 一律停在票檔、agent 收工不切回，LS-180）」，一行內（commit `63f1971`）。

### main 併入
`git fetch origin main && git merge origin/main`（34dd954，v0.12.0：LS-175 media 通知事件——migrations／push-dispatch／API.md／tests）：**無衝突、無檔案重疊**（本票只動 scripts/ops、scripts/gates、.claude/agents、docs/COLLABORATION.md、CLAUDE.md、ci.yml）。

### 併入後全套自測（全綠）
pen-open 40 組／pen-read 10 組／pen-status 11 組／patrol 全綠／agent-tools-check 真 repo 通過（7 條）＋自測 48 組／brand-skill 通過／cleanup-merged.test.sh 198 ✓ 全綠／`bash -n` cleanup-merged.sh 通過。硬限制照舊：未碰活的 Pen。

### 未完成
無新增；前輪待 orchestrator 項不變（重連後對 LS-177 跑 `pen-open.sh <wt> --force-reload` 實機驗 CLI 回讀；「收工不切回」無機械 gate 已列 §7 盲區）。PR #289 待併；併後補 back-merge `main`→`development`。

### 2026-09-04T22:49:24.061Z · Chih-Lin Yeh · `eae45ef3`

## merge-review R2 — PR #289（head a9266d7，增量 371dbfc..a9266d7）

裁決：**APPROVE**（無 blocker／無 major）。R1 的 **N1 已修對**（沿革寫法，不是刪句），實作者自己再 grep 補修的 `cleanup-merged.sh` 兩行處置句也對；CLAUDE.md ④ 一行到位。新增 1 條 informational（R2-I1，我 mutation 跑出來的）。審的 head＝`a9266d720769d6a7d7bd79975f57f7ae29e2c724`，與 `gh pr view 289 --json headRefOid` 當下值相同；`gh pr checks 289` **四項全 pass**（ci 6m18s／db 3m26s／lint 15s／rules 3m39s，run 33926520172——開審時三項在跑，等收工才判讀）。2026-09-05 06:48 CST。**硬限制**：本輪完全未碰活的 Pen（R1 已用掉授權的那一次探針，R2 無需再跑）。

### （1）N1 — 修對了，而且用的是沿革寫法

`docs/COLLABORATION.md:262` 現在讀作：

> …orchestrator 派工前先切檔、**設計票期間 Pen 停在票檔、結案由 orchestrator `--kill` 清場**（§1／§2／§6 ④；**LS-91 原訂「設計票收工前須切回主 checkout」，LS-180 裁決撤銷**——切回會把票檔留在背景視窗、下一輪只能清場斷線，見 §7「切檔不殺行程」列…）

同一列的 gate 欄也補了對應沿革（「LS-176 期間…LS-180 裁決改為設計票期間 Pen 停在票檔、agent 收工不切檔，結案由 orchestrator `pen-open.sh <repo 根目錄> --kill` 清場一次，§6 ④」）。**保留原文＋標記撤銷**比直接刪句好：後面的人查 LS-91 的歷史時看得到規則變過、為什麼變。

**實作者自己再 grep 補的兩行也對**（`9662f42`）：`cleanup-merged.sh:285`／`:291` 的處置句由 `pen-open.sh ${ROOT} 把 Pen 切回主 checkout` 改為 `pen-open.sh ${ROOT} --kill 清場切回主 checkout（LS-180 §6 ④…之後請使用者在 Claude Code 執行 /mcp 重連 pencil）`，`:291` 還多一句「不帶 `--kill` 只會把票檔留在背景視窗、幽靈 renderer 照舊」。這是 R1 我判定「屬結案清理提示、與新規約一致」的那兩行——實作者主動把它們升級成帶 `--kill` 的精確版，比我要求的更進一步。

**我自己跑的全 repo grep（你指定的四個 pattern `切回|只開主|收工.*主 checkout|pen-open.sh <ROOT>`）逐條分類**：

| 命中 | 判定 |
|---|---|
| `ui-designer.md:15/64/83`、`visual-reviewer.md:11`、`pen-read.sh:36-38`、`pen-open.sh:124`、`agent-tools-check.sh:60-61` | **否定句／說明句**（「**不**切回主 checkout」「LS-180 裁決改為…」） |
| `agent-tools-check.test.sh:43/44/104/107` | 測試 fixture 與負樣本標題（⑧ 就是釘「改回切回主 checkout 而無收工句 → 紅」） |
| `cleanup-merged.sh:285/291` | 結案清理處置句，**已帶 `--kill`**、指向 §6 ④ |
| `COLLABORATION.md:61/135/223/262/305`、`CLAUDE.md:14` | 新規約正文（含 :262 的沿革標記） |
| `patrol.sh:184`「主 checkout 不在 main…派工前切回 main 並 pull」 | 講 **git 分支**，與 Pen 無關 |
| `ios-dev.md:16`／`qa.md:17,32`／`merge-reviewer.md:11` | 講 supabase-lock／模擬器收工，與 Pen 無關 |

**結論：零殘留「要 agent 收工把 Pen 切回主 checkout」的指令句。** 唯一還在的「切回」動作是 orchestrator 結案清場（帶 `--kill`），那正是新規則。

### （2）`CLAUDE.md:14` ④

只動了那一行（diff 是同一行的 ±，其他字未改），內容：「＋**④ Pen 清場（設計票限定**：設計 PR 併入 development 後 orchestrator `bash scripts/ops/pen-open.sh <主 checkout> --kill` 並請使用者 `/mcp` 重連 pencil——設計票期間 Pen 一律停在票檔、agent 收工不切回，LS-180）」。與 COLLABORATION §6 ④ 的用語一致（設計票限定／併入 development 後／`--kill`／請使用者 `/mcp` 重連／agent 收工不切回），且維持 CLAUDE.md「摘要＋指向 §6」的體例。小到不值得列 finding 的排版瑕疵：粗體結束在「（設計票限定**」括號中間，渲染沒問題、語意也清楚，下次順手調即可。

### （3）merge commit `a9266d7`

`git diff 34dd954...a9266d7`（對 main 的差集）＝**17 檔，全部是本票的**（R1 的 15 檔＋`CLAUDE.md`＋`cleanup-merged.sh`）——**零夾帶**；merge 帶進來的 LS-175 push-dispatch／db 變更只出現在「對舊 head 的 diff」那一側，屬 main 既有內容。

自測（**用 macOS `/bin/bash` 3.2.57 一次跑完六支**）：`pen-open` **40 組**／`pen-read` **10 組**／`pen-status` **11 組**／`patrol` 全綠／`agent-tools-check` **48 組**／`cleanup-merged` 全綠（**198 ✓**）。

### R2-I1（informational，我 mutation 跑出來的）

**`cleanup-merged.test.sh` 沒有釘住那兩行處置句的 `--kill`。** ⑰ 的斷言是 `has '⑰ 印出處置（切回主 checkout）' "$out17" 'pen-open.sh'`——只 grep `pen-open.sh` 這個子字串。我把 `:285`／`:291` 兩處的 `--kill` 拿掉（退回 R1 前的寫法）再跑，**198 ✓ 全綠、抓不到**。也就是說這次的修正只有人眼守著，日後有人改回去不會紅。
影響有界（是給操作者看的處置字串、不是行為），但這正是本票在意的那條規則：不帶 `--kill` 的提示會讓人留下背景幽靈 renderer。修法一行：把針改成 `pen-open.sh ${ROOT} --kill`（或至少 `--kill`），順便把案例標題裡還寫著的「切回主 checkout」更新成新語意。可在 back-merge 或下次動 `cleanup-merged.test.sh` 時順手做，不必為它另開一輪。

### R1 其餘 informational 的狀態

I-1（patrol brief 三看門狗疊加逼近 30s）／I-2（`不可測` 的 fail 方向不對稱）／I-3（多支 mcp-server 全連才 ✓）／I-4（只看分支名含 `design`）本輪未動、維持原判，不擋併入。**R1 列的併入後 QA 五步條件仍然有效且未執行**（MCP 尚未重連）——特別是第 1 步「`pen-status.sh` 三段皆 ✓」是探針**連線正常分支的第一次真實驗證**，第 2 步是 `read_pen_hash()` CLI 路徑的第一次實機驗證。

### 四維度（增量）

race：本輪只動文件與兩行提示字串，未碰任何並發／訊號路徑。效能：無變化。平行：無。scope：對 main 的差集 17 檔全屬本票；`CLAUDE.md` 只動一行、`cleanup-merged.sh` 只動兩行字串。


### 2026-09-04T22:50:43.529Z · Chih-Lin Yeh · `e40c4467`

**2026-09-05 06:50 orchestrator — merge-review R2 `eae45ef3` APPROVE → PR #289 已併入 main `0c6b58b`；back-merge main→development 背景進行；票進 QA**

- QA 條件（reviewer 五步）：使用者 `/mcp` 重連 pencil 後，orchestrator 跑 `bash scripts/ops/pen-status.sh`（期望三段 ✓，探針「連線正常」分支首個真實樣本）與 `bash scripts/ops/pen-open.sh <LS-177 worktree> --force-reload`（期望 exit 0 印 `tree_hash=80e2bae6e897e950 與磁碟一致`；exit 3 則貼 CLI 原始輸出）。
- R2-I1（cleanup-merged.test.sh 針未釘 `--kill`）入池 LS-96 `6b0eb939`。

### 2026-09-04T23:04:06.934Z · Chih-Lin Yeh · `5e73749b`

**2026-09-05 07:03 orchestrator — back-merge PR #290 已併入 development `ca2da12`（main ⊂ dev 恢復）；dev CI 綠後晉升 test。QA 實機驗證仍待使用者 `/mcp` 重連 pencil。**

### 2026-09-04T23:28:39.236Z · Chih-Lin Yeh · `1ebc51e7`

**2026-09-05 07:27 orchestrator — 已晉升 test `a0b025c`；隨 LS-179 同批 QA（本票只驗機械部分：六支自測＋`pen-status.sh` 唯讀＋patrol Pencil 段＋殘留 grep）；需 Pencil MCP 的實機驗證仍待使用者 `/mcp` 重連後由 orchestrator 執行。**

### 2026-09-05T00:02:52.449Z · Chih-Lin Yeh · `71bae29e`

## QA 裁決：**PASS（機械部分）／MCP 實機驗證待使用者 `/mcp` 重連**（test head `a0b025c15b90224844cf735d4041cf6a75eda389`）

worktree `qa-test`（detached `a0b025c`）；硬限制遵守：本輪**未執行** `pen-open.sh`／`pen-read.sh`（含 `--status`／`--force-reload`／`--kill`），僅跑唯讀探針 `pen-status.sh` 一次與 `patrol.sh` 一次。

### 逐條驗收（機械可驗部分）

1. **六支自測全綠**：
   - `pen-open.test.sh` **40 組全綠**
   - `pen-read.test.sh` **10 組全綠**
   - `pen-status.test.sh` **11 組全綠**
   - `patrol.test.sh` 全綠（含 ㉒ Pencil 探針全部子案例）
   - `cleanup-merged.test.sh` **198 ✓ 全綠**
   - `agent-tools-check.test.sh` **48 組全綠**
   （皆與 merge-review R1／R2 記錄的組數一致）
2. **`pen-status.sh` 唯讀探針實跑一次**：
   ```
   Pencil：行程 ✓（pid 3185） · 路徑 .../worktrees/LS-177/design/littlesprout.pen · MCP 探針 ✗（mcp-server 1 支皆無 Pen socket 連線——…在 Claude Code 執行 /mcp 重連 pencil）   exit=1
   ```
   三段判讀皆符合預期（行程存活、路徑＝LS-177 票檔、MCP 斷線待重連），與 orchestrator 派工時描述的現況一致。
3. **`patrol.sh 40` 實跑一次**：輸出含獨立的「Pencil 連線」段，內容與上述 `pen-status.sh` 探針一致，並印「→ 設計票派工前先請使用者在 Claude Code 執行 /mcp 重連 pencil，重連後再派（LS-180）」。
4. **grep 全 repo 無「收工切回主 checkout」指令句殘留**：`grep -rn "收工切回主 checkout"` 全 repo 僅 1 命中（`COLLABORATION.md:135`），且是**否定句**「派工單不要再寫『收工切回主 checkout』」，不是殘留指令。另以較寬的 pattern（`切回主 checkout|只開主 checkout|切回 repo 根`）逐一分類全部命中：agent 定義／COLLABORATION 內的命中皆為否定句／說明句／沿革標記／測試 fixture；`cleanup-merged.sh:285`／`:291` 的處置句**已帶 `--kill`**（`pen-open.sh ${ROOT} --kill 清場切回主 checkout`），符合 R1 merge-review N1 修正後的狀態；`cleanup-merged.test.sh:545` 的斷言僅 grep `pen-open.sh` 子字串（R2-I1 已知 informational，入池 LS-96 `6b0eb939`，非本輪新發現）。**零殘留**。

### 未驗證（需 Pencil MCP，本輪明確不做）
- `pen-status.sh` **MCP ✓ 分支**的第一次真實驗證（目前只有斷線樣本）。
- `pen-open.sh <LS-177 worktree> --force-reload` 的實機回讀（期望 exit 0 印 `tree_hash=80e2bae6e897e950 與磁碟一致`，或 exit 3 印期望值）——這是 `read_pen_hash()` CLI 路徑的第一次實機驗證，reviewer R1 五步 QA 條件的第 1／2 步。
- 上述兩項**待使用者在 Claude Code 執行 `/mcp` 重連 pencil 後，由 orchestrator 執行**（非 QA 職責範圍內能做，本輪工具白名單下 Pencil MCP 本就斷線不可用）。

### 風險
- 無新增風險。I-1～I-4（patrol brief 預算、`不可測` fail 方向不對稱、多 mcp-server 全連判準、分支名判斷）與 R2-I1（`cleanup-merged.test.sh` 未釘 `--kill`）皆為 merge-review 已記錄的 informational，非阻塞。

### 證據
- 六支自測輸出：本次 session 逐一實跑（未落地為檔案，皆為 stdout 觀察，數字與 merge-review R1/R2 comment 記錄的組數逐一核對一致）
- `pen-status.sh`／`patrol.sh` 輸出：見上方逐字貼出

### 2026-09-05T00:04:54.140Z · Chih-Lin Yeh · `eac6359f`

**2026-09-05 08:04 orchestrator — QA 機械部分 PASS `71bae29e`（六支自測、pen-status 唯讀、patrol Pencil 段、殘留 grep 零）；票留 QA，待使用者 `/mcp` 重連後由 orchestrator 做 `pen-status.sh` MCP ✓ 與 `--force-reload` 回讀實機驗證，PASS 即進收尾。**

### 2026-09-05T03:38:21.161Z · Chih-Lin Yeh · `c8953139`

**2026-09-05 11:37 orchestrator — 實機驗證 PASS（使用者 `/mcp` 重連後）**

- `bash scripts/ops/pen-status.sh` → 「行程 ✓（pid 3185）· 路徑 LS-177 票檔 · MCP 探針 ✓（mcp-server 1 支皆有 unix socket 連到 Pen pid 3185）」——探針「連線正常」分支首個真實樣本。
- `bash scripts/ops/pen-open.sh <LS-177 worktree> --force-reload` → 「磁碟 tree_hash=80e2bae6e897e950，向 Pencil 端回讀比對……✓ tree_hash 與磁碟一致（renderer 內容＝磁碟，未清場、Pencil MCP 連線保留）」；事後 `pen-status.sh` 仍 MCP ✓——CLI 回讀路徑（`read_pen_hash()`）實機首驗通過，reviewer 五步 QA 條件全部滿足。
- 收尾：sweeper（本票先前未隨批次掃）→ retro → Done。

### 2026-09-05T03:44:06.097Z · Chih-Lin Yeh · `6b3b9755`

## dead-code sweep — LS-180

**範圍**：`git diff 34dd954..0c6b58b`（PR #289 併入 main 的 17 檔，此為其中 15 檔本體＋兩份既有函式的字串變更）：`scripts/ops/pen-open.sh`／`pen-status.sh`（新）／`pen-status.test.sh`（新）／`pen-open.test.sh`／`pen-read.sh`／`patrol.sh`／`patrol.test.sh`／`cleanup-merged.sh`／`.claude/agents/{ui-designer,visual-reviewer,qa}.md`／`scripts/gates/agent-tools-check.sh`／`agent-tools-check.test.sh`／`docs/COLLABORATION.md`／`CLAUDE.md`。主 checkout 唯讀巡查，**全程未碰活的 Pen**（未跑 pen-open／pen-read／pen-status，Pen 開著 LS-177 票檔）。

### Findings

**0 個**——未發現本 feature（LS-180）引入的死碼。逐項排查如下（含派工單點名的四個懷疑點，皆排除）：

1. **`pen-open.sh --force-reload` 舊清場路徑是否有無法到達的分支** —— 排除。逐分支追蹤：`poll_rc=0 && kill_mode=1`（:319）／`poll_rc=0`（force_reload 分支，:321，內含 hash 相符 exit 0、讀不到 exit 3、不符落入清場三條路徑）／`poll_rc!=0`（:344 else）皆可達且皆被 `pen-open.test.sh` 40 組樣本覆蓋（含 merge-review R1 的 3 個 mutation：判斷反向、exit 3 改 exit 0、拿掉 reconnect_notice，三者皆被自測釘死）；`--force-reload`／`--kill`／`--no-quit` 由 `usage()` 的 elif 鏈保證互斥（:194-205），不存在三旗標並存的死分支。本機重跑 `bash scripts/ops/pen-open.test.sh` 40/40 綠。

2. **`pen-status.sh` 與 `pen-open.sh --status` 是否重複邏輯** —— 排除，這是誤判前提。`pen-status.sh:41` 直接呼叫 `bash pen-open.sh --status`（見檔頭 :4 明載），不是另寫一份 `poll_once` 邏輯；`pen-status.sh` 只新增行程 `pgrep` 與 MCP `lsof` 交集兩段。組合（compose）而非複製，無重複可刪。

3. **agent 定義「切回主 checkout」殘句** —— 複核 R1/R2 已做過的 grep，結果一致：全 repo `切回主 checkout|只開主 checkout|切回 repo 根` 命中分佈於 `ui-designer.md`（:15/67/86，皆為「**不**切回主 checkout」的否定句或步驟 5 的裁決說明）、`visual-reviewer.md:11`（同否定句）、`COLLABORATION.md`（:61/135/223/262/305，正文或 :262 的沿革標記「LS-91 原訂…**LS-180 裁決撤銷**」）、`pen-open.sh:124`／`pen-read.sh`（LS-180 段落敘述，非指令）、`agent-tools-check.sh:60-61`／`.test.sh:52/112/115`（gate 規則說明與測試 fixture／負樣本標題）、`cleanup-merged.sh:285/291`（**已帶 `--kill`** 的正確清場處置句，指向 §6 ④）、`cleanup-merged.test.sh:545`（斷言字串）。**零殘留指令句**——沒有任何一處要求 agent「收工切回主 checkout」而未反映 LS-180 新語意。

4. **`.test.sh` 註解掉的案例** —— 排除。`pen-open.test.sh`／`pen-status.test.sh`／`patrol.test.sh`／`agent-tools-check.test.sh` 皆無 `^#`／`: '...'` 形式的停用測試區塊（`grep -n "TODO\|FIXME\|XXX"` 只命中 `mktemp ...XXXXXX` 模板字面與 python `print(`，非真殘留）。

### 補充排查（非派工單點名，但屬同類巡檢範圍）

- `pen-open.sh` 內部變數（`HASH_TIMEOUT`／`HASH_ATTEMPTS`／`kill_mode`／`force_reload`／`no_quit`／`mode`）逐一 grep 確認皆有多處實際引用，無孤兒變數。
- `patrol.sh` 的 `design_wt`／`pencil_ran`／`PENCIL_LINE`／`pencil_rc` 四個新變數在 human／brief／json 三種輸出模式（:502 JSON `pencil` 物件、:507 brief、:534-539 human）皆有對應消費，無死路徑。
- `cleanup-merged.sh` 的 `pen_guard`／`pen_probe` 是 LS-141 既有函式，LS-180 只改了兩行字串內容（`git diff` 確認），行為邏輯本身不在本票範圍——這兩行的測試斷言未釘住新增的 `--kill` 字面值（R2-I1，已入池 LS-96 `6b0eb939`），非本輪新發現，不重複列。
- `agent-tools-check.sh` 新增的 3 條 `BODY_RULES`（`ui-designer|--kill 只在 orchestrator 明示時`／`ui-designer|收工 Pen 停在票檔`／`visual-reviewer|--kill 只在 orchestrator 明示時`）逐條 grep 對應的 agent .md 正文，皆有命中——無指向已刪文字的死 gate 規則。
- `.github/workflows/ci.yml:275` 確認掛了 `pen-status.test.sh`，CI 接線無孤兒。
- `qa.md`／`ui-designer.md`／`visual-reviewer.md` 對「步驟 5」等內部引用的編號未因本次改寫而錯位。

### 既有死碼觀察

無新增觀察。既有唯一相關項（R2-I1／`cleanup-merged.test.sh:545` 斷言未釘 `--kill`）已由 merge-reviewer 記錄並入池 LS-96（`6b0eb939`），沿用原判，不重複開票。

### 建議處置

不需開票、不需併入下一張票順手改——本輪為 0 finding。R2-I1 維持原處置（LS-96 待辦池，下次動 `cleanup-merged.test.sh` 時順手修）。

### 搜尋方法與盲區聲明

- 方法：`git diff 34dd954..0c6b58b`（指定檔案）取本票變更範圍；對每支 shell script 做 `bash -n` 語法檢查、逐一 grep 函式／變數引用計數、追蹤旗標分支可達性；6 支自測（`pen-open`／`pen-status`／`pen-read`／`patrol`／`agent-tools-check`）本機重跑確認全綠（40／11／10／全綠／60 組，與 QA `71bae29e`、merge-review R1/R2 記錄的組數一致，`agent-tools-check` 因 main 已含 LS-185 故為 60 非 48，屬預期）；全 repo grep 四種「切回主 checkout」相關 pattern 逐條分類。
- 盲區：純文字搜尋抓不到動態引用（reflection／字串拼接組出的函式名／間接呼叫鏈）；未對活的 Pen 執行任何指令，`read_pen_hash()` 的 CLI 實跑格式是否有殘留分支未驗（但這是 LS-180 票文自陳的已知未完成項，非死碼範疇）；未跑 shellcheck（環境無此工具），僅靠人工追蹤分支與 grep 引用計數，可能漏掉極隱晦的邏輯死路（例如恆真／恆假的複合條件）。

Comment id：（見本則留言本身）

### 2026-09-05T03:45:13.426Z · Chih-Lin Yeh · `3e4592d8`

**2026-09-05 11:5x orchestrator — 收尾儀式 → Done**

- 實機驗證 PASS（`c8953139`）、sweeper `6b3b9755` 0 finding、worktree／分支已清、LS-96 `bed3ca3e` 改註。
- **Lesson learning**：(1) 「照指示切檔」與「照指示重掃」互斥是 harness 自己造成的事故——修法把「新鮮度」從「殺行程」改成「雜湊相符」，設計原則：任何會殺外部行程的自動化都要先問「有沒有等價的唯讀證明」。(2) MCP 斷線只能靠使用者 `/mcp` 重連（斷了約 6 小時，LS-177 R3 因此停擺）——巡檢現在有 Pencil 段直接印指引，並在派工模板禁用 `--force-reload`／`--kill`。(3) 規約「設計票期間 Pen 停在票檔、結案由 orchestrator `--kill` 一次清場」是把切檔次數降到每票一次，配 LS-176 舊路徑放行才成立。(4) 第一次真實 MCP ✓ 樣本與 CLI 回讀路徑今天才驗到——harness 票的「實機驗證」若依賴外部連線，要在票面標成 QA 條件而不是併入前條件（本票這樣做了，對）。

