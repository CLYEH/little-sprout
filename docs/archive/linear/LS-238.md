# LS-238 Harness：規約文件去敘事化——`docs/COLLABORATION.md`（172 處「事故／教訓／R<n> F<n>」、80 處「不再／改為／已廢止」）與 `.claude/agents/ui-designer.md`（12 處）改成「規則＋一行理由＋LS 指標」，一檔一 PR、`agent-tools-check`／`brand-skill-check` 釘住字句重驗（prompt-audit 0912 F8）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:M, lane:harness |
| Cycle | 2 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-12T17:09:05.006Z |
| 開始 | 2026-09-13T00:43:19.182Z |
| 完成 | 2026-09-13T06:22:56.350Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-238/harness規約文件去敘事化-docscollaborationmd172-處事故教訓rn-fn80-處不再改為已廢止與 |
| 關係 | related→LS-235, related→LS-26, related→LS-180, related→LS-73, related→LS-187, related→LS-118, related→LS-137, related→LS-76, related→LS-136, related→LS-117, related→LS-122, related→LS-157, related→LS-81, related→LS-184, related→LS-91, related→LS-104, related→LS-167, related→LS-125, related→LS-185, related→LS-68, related→LS-202, related→LS-120, related→LS-152, related→LS-67, related→LS-171, related→LS-168, related→LS-142, related→LS-226, related→LS-119, related→LS-44, related→LS-177, related→LS-46, related→LS-245, related→LS-244, related→LS-239, related→LS-236, related←LS-246, related←LS-254, related←LS-96 |

## 描述

## 來源

2026-09-12 prompt 稽核（另一 session 執行，稽核頁 artifact `a490c4c6-9498-4a53-a160-e1d4f064d285`）F8：規則裡夾帶事故重述，讓 agent 每輪讀入大量敘事、規則本身被淹沒。[LS-236](https://linear.app/little-sprout-app/issue/LS-236/harness殘留-xcodebuild-防護bash-timeout-截斷自動背景化push-gatedetect-simulator) 只做 §4-b cron 模板一個示範 hunk。

## 範圍

1. `docs/COLLABORATION.md`：逐節把「事故重述」改寫為「規則＋一行理由＋LS 指標」——**規則語意不變、LS id 保留、不整批剝 id**；每節改完跑 `bash scripts/gates/agent-tools-check.sh`（釘住字句）與 CI `rules` 全綠；§4-b cron 模板改動需同步 `scripts/ops/session-start.sh` 引用（若有）。
   **裁定（09-13 11:5x，PR-B R1 只達 −2.66% 後）**：「LS id 保留」的判準是 **每一節（§）的 LS id 集合前後相等**（每個 id 在同一節至少出現一次、可反查），**不是逐 id 出現次數相等**——同一 §7 列內同一 LS id 重複 5–11 次的技術錨點可合併成一次；同一規則跨多張票的演進敘事（LS-a 加 → LS-b 改 → LS-c 再改）壓成「現行規則＋一行『演進：LS-a→LS-b→LS-c』」；§7 每列右欄以 ≤ 現行 1/3 長度為目標；§4-b cron 模板（行 139–141）仍不動。
2. `.claude/agents/ui-designer.md`（12 處）同法；`brand-skill-check`／`agent-tools-check` 釘住字句重驗。
3. 一檔一 PR（兩支 PR，hotfix 流程自 main），PR body 附「改前／改後行數、釘住字句清單、無語意變更聲明」；merge-reviewer 以「規則有無漏掉、指標有無斷鏈」為審查重點。

## 不做

改規則內容；改 CLAUDE.md（硬規定檔另議）；動 `.claude/memory/*`。

## 驗收

- [ ] 兩檔行數／字數顯著下降（目標 COLLABORATION.md −30% 以上），`agent-tools-check`／`brand-skill-check`／CI `rules` 綠
- [ ] merge-reviewer 通過（抽查 10 條規則改前改後語意一致；所有 LS 指標仍可反查——以每節 id 集合相等為準）

## 留言（18）

### 2026-09-13T00:43:22.910Z · Chih-Lin Yeh · `0e42b7f8`

**orchestrator 09-13 08:42**：巡檢動作清單（harness lane 空：LS-239 Done）→ 補 cycle 2、Ready → 派 **PR-A**（`.claude/agents/ui-designer.md`，worktree `.claude/worktrees/LS-238`／`hotfix/LS-238-ui-designer-denarrative` 自 main `99abb92`）。**PR-B**（`docs/COLLABORATION.md`）等 v0.24.0 併 main 後再派——dev 上 LS-244／LS-245 等 PR 仍在加 §7 列，先做會在 back-merge 時大面積衝突。

### 2026-09-13T00:54:21.086Z · Chih-Lin Yeh · `a46fcf5d`

**ios-dev 09-13 08:53 PR-A R1**

**Ticket**：LS-238（範圍 2＋3：`.claude/agents/ui-designer.md` 去敘事化。範圍 1 `docs/COLLABORATION.md` ＝ PR-B，本輪不做）

**已完成**：hotfix worktree `.claude/worktrees/LS-238`、branch `hotfix/LS-238-ui-designer-denarrative`（自 origin/main `99abb92`）。把 `.claude/agents/ui-designer.md` 11 處事故重述改成「規則＋一行理由＋LS 指標」：拿掉具體日期、`R<n> F<n>` 審查輪次、Linear comment id、LS-96 池項 hash 等過程敘事，規則語意與 LS 票號不動。改前 90 行／20656 字、改後 90 行／20212 字（純行內片段替換，字數 −444，約 −2.1%；−30% 目標票文只訂給範圍 1 的 COLLABORATION.md，本檔按「12 處左右」範圍執行）。PR body 附完整改前／改後對照（抽 5 條）。

**已驗證**（對應票文範圍 2＋3、驗收條件「兩檔行數／字數下降＋兩支 gate 綠」）：
- `bash scripts/gates/agent-tools-check.sh` → exit 0，`✓ agent-tools gate 通過（6 份 agent 定義；正文必含字樣 42 條）`；改動一行 → 釘住字句 `--kill 只在 orchestrator 明示時`／`收工 Pen 停在票檔` → 全量重跑仍列印「正文含」（原文），無斷言失敗訊息（gate 本身無 FAIL 分支輸出，以 exit code 與逐項「正文含」清單為證）；已修 `c38d6dd6ef5c969da575134d5d4817b181e984d8`
- `bash scripts/gates/brand-skill-check.sh` → exit 0，`✓ brand-skill gate：little-sprout-brand skill 本體齊全、ui-designer／visual-reviewer 已接線`；已修 `c38d6dd6ef5c969da575134d5d4817b181e984d8`
- `bash scripts/gates/agent-tools-check.test.sh` → `✓ agent-tools-check 自測通過（125 組樣本）`；已修 `c38d6dd6ef5c969da575134d5d4817b181e984d8`
- `bash scripts/gates/brand-skill-check.test.sh` → `✓ brand-skill-check 自測通過（24 組樣本）`；已修 `c38d6dd6ef5c969da575134d5d4817b181e984d8`
- `bash scripts/gates/pr-body-check.sh LS-238-pr-a-body.md --branch hotfix/LS-238-ui-designer-denarrative --verify` → exit 0（`✓ PR body 檔頭段含本票票號 LS-238`、6 條「已修」SHA 皆在 PR commits 內；LS-96 候選因本機無 LINEAR_API_KEY 只驗格式，CI 會反查）
- `git push` push gate 全綠（無 Swift 變更，跳過 SwiftLint／unit tests／QADriver gate；api-contract／tap-target-registry／error-codes／migration 系列／分支票號乾淨／可乾淨合併皆通過）

純 markdown、未開模擬器、未碰 DB，DoD 第 3 條不適用。

**未完成**：
- 範圍 1（`docs/COLLABORATION.md`，PR-B）待 orchestrator 於 v0.24.0 併 main 後另派，避免與在飛 PR 的 §7 對照表列衝突（票文明列的分工，非本 PR 缺項）。
- 本輪尚未經 merge-reviewer 審查，目前無 informational finding 待處置；R1 若有將逐條回覆已修／記入 LS-96／另票／不修＋理由。

**風險**：純敘事片段替換、無邏輯變更；風險僅在語意誤改，已用釘住字句 gate＋兩支自測（125＋24 組樣本）機械覆核，並附改前改後對照表供人工複核。

**產出位置**：
- PR #382：https://github.com/CLYEH/little-sprout/pull/382 （hotfix/LS-238-ui-designer-denarrative → main）
- head sha：`c38d6dd6ef5c969da575134d5d4817b181e984d8`
- 變更檔：`.claude/agents/ui-designer.md`
- worktree：`/Users/clyeh/little-sprout/.claude/worktrees/LS-238`
- 模擬器已關：無（本輪未開模擬器）

### 2026-09-13T00:55:48.477Z · Chih-Lin Yeh · `b2dc1937`

**orchestrator 09-13 08:55**：PR-A handoff 收到（PR #382 → main，head `c38d6dd`；11 處改寫、釘住字句三條完整、兩支 gate 綠）→ In Review、派 merge-review R1（重點：LS 票號集合前後相等、逐 hunk 語意一致、漏網敘事句）。PR-B 仍等 v0.24.0。

### 2026-09-13T01:10:55.688Z · Chih-Lin Yeh · `dbdb9e00`

**merge-reviewer 09-13 09:04 PR-A R1 APPROVE**

**審查對象**：PR #382（LS-238 範圍 2＋3）`hotfix/LS-238-ui-designer-denarrative` → `main`，head `c38d6dd6ef5c969da575134d5d4817b181e984d8`，base `origin/main` `99abb92a6e3b3aaf21162c926f6e31e540a5f9e8`（merge-base＝origin/main，可 FF）。diff 指令 `git diff 99abb92...c38d6dd -- .claude/agents/ui-designer.md`：1 檔、1 commit、11+／11−。審查期間重新 `gh pr view 382 --json headRefOid` 覆核，head 未前進。

**結論：APPROVE**（無 blocker、無 major；6 條 informational 全列於後）

## 逐條查實（對應票文範圍 2＋3 與驗收條件）

- **範圍 2｜12 處去敘事化**：`git diff 99abb92...c38d6dd -- .claude/agents/ui-designer.md` 逐 hunk，再用 `difflib.SequenceMatcher` 對 11 條改動行做行內 opcode 比對 → 11 處語意一致；另 2 處仍是敘事但本輪未改（info-1／info-2）。
- **範圍 2｜`agent-tools-check` 釘住字句重驗**：`bash scripts/gates/agent-tools-check.sh`（於 worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-238`，HEAD＝`c38d6dd`）→ exit 0，42 條必含字樣全數列印。
- **範圍 2｜`brand-skill-check` 釘住字句重驗**：`bash scripts/gates/brand-skill-check.sh` → exit 0。
- **範圍 2｜兩支 gate 自測**：`bash scripts/gates/agent-tools-check.test.sh` → exit 0（125 組樣本）；`bash scripts/gates/brand-skill-check.test.sh` → exit 0（24 組樣本）。
- **範圍 2｜釘住字句是否真的在把關**：把 `.claude/agents/ui-designer.md` 複製到 `scratchpad/LS-238-mut/`（repo 內未動任何檔案）改掉釘住字句後，以 `bash scripts/gates/agent-tools-check.sh <複本 agents 目錄>` 重跑 mutant A／B → 皆 exit 1 並逐字點名該字樣；還原後再跑 `bash scripts/gates/agent-tools-check.sh` 回 exit 0。
- **驗收①｜行數／字數下降**：`git show <sha>:.claude/agents/ui-designer.md` 接 `wc -m`／`wc -c`／`wc -l` → 20656→20212（−444）、35858→35144、90→90，與 PR body 表格逐格相符。
- **驗收②｜LS 指標仍可反查**：對 `.claude/agents/ui-designer.md` 的改前改後兩版跑 `grep -oE 'LS-[0-9]+' | sort -u` 後 `diff` → 空（各 24 個）；再以 `uniq -c` 比逐 id 出現次數 → 全數相同。
- **驗收②｜抽查規則語意一致**（票文要求 10 條）：對 `.claude/agents/ui-designer.md` 抽 11 條＝全部改動行，含 4 條帶條件／例外的長句（L14／L33／L66／L67）→ 全部一致，詳見下節表格。
- **範圍 3｜一檔一 PR、PR body 三要素**：`gh pr view 382 --json files,body` → 僅 1 檔；改前改後行數、釘住字句清單、無語意變更聲明、對照表齊備，且所有數字與我實測相符。
- **CI `rules` 全綠**：`gh pr checks 382` → ci／ci-ipad／db／lint／rules 全 pass。
- **hotfix base 正確、與 dev 無在飛衝突**：`git merge-base`＋`git diff origin/main origin/development -- .claude/agents/ui-designer.md`＋`gh pr list --state open` → base＝main 且 merge-base＝origin/main；dev 側該檔與 main 相同（空輸出）；僅 #382 觸碰此檔。
- **commit 首行格式**：`git log -1 --format=%s c38d6dd` → `docs(harness): LS-238 ui-designer.md 去敘事化（PR-A）`，合規。

## 1) 指標有無斷鏈 — 通過（比票文要求更嚴）

- 集合相等：`grep -oE 'LS-[0-9]+' | sort -u` 改前改後各 **24 個**，`diff` 為空。
- 再嚴一層：**逐 id 出現次數也完全相同**（LS-44 10／LS-46 11／LS-168 8／LS-185 6／LS-226 5／LS-202 5／LS-68 5 …… 24 個 id 全數 before==after）——代表沒有任何一處是「刪掉一個 id、靠同行另一個 id 撐著」。
- 被刪的過程性指標全部落在票文允許的四類：`R<n> F<n>` 5→0（`R2 F1`×3／`R2 F4`／`R2 F2`）、Linear comment id `6b367b37`→0、LS-96 池項 hash `1ae89cbd`→0、以及 `LS-46 R8`／`LS-46 R7 comment`／日期 `2026-09-01`（L14 那一處）。
- 殘留的 `R<n>` 逐條看過，全部仍掛在 LS id 上（LS-46 R3-R6、LS-152 VR R3、LS-171 R1 N2、LS-202 R2／R3、LS-120 R2／R5、LS-177 R2、LS-67 R1、LS-152 R1、LS-68 R2：F2、LS-142 R7），**沒有本 PR 新造成的孤兒指標**。全檔唯一無 LS 錨的 `R2` 在 L69，是本 PR 未觸碰的既有文字（info-3）。

## 2) 規則語意一致 — 抽 11 條（等於全部）逐字比對，通過

用 `difflib.SequenceMatcher` 對 11 條改動行做行內 opcode 比對，只印真正被替換的字串區間、其餘逐字相同。**含條件／例外的長句 4 條**特別覆核：

| 行 | 規則 | 條件／例外是否保全 |
|---|---|---|
| L14 | `get_app_state()` 不接受任何參數 | 「傳入**不報錯但**靜默忽略」的轉折保全；同行 `Export()` 相對路徑被誤植到 `design/.claude/evidence/` 的例外、`TakeScreenshot` 無 `outputPath` 的例外**逐字未動**（只把 `LS-96 comment 6b367b37 第 6 項…` 縮成 `LS-96`） |
| L33 | `scan_scope`／`tree_hash`／分批 | 硬規則「不得改標 `document`」「缺 `SCAN_TREE_HASH` 會直接 throw」「兩個旗標同時設會 throw」「兩次之間不得有任何寫入」**全部逐字未動** |
| L66 | `--marker` 字串選法 | 「必須本輪才出現、上一輪落地檔沒有」「同時出現＝沒有鑑別力、exit 2 要求換一個」「純數值屬性變更找不到就不用管」整段逐字未動，只刪 ` R2 F1` |
| L67 | `pen-land.sh` 鑑別力檢查 | 「沒給 `--marker`／給了沒命中／落地檔也找得到（exit 2）」三分支＋`--allow-unchanged` 例外整段逐字未動，只刪 ` R2 F1` |

其餘 7 條（L15／L26／L55／L58／L59／L68／L80）同法覆核，規則的「做什麼／何時做／違反會怎樣」皆不變。逐條摘要：L15、L59、L80 只刪輪次標記；L26 把兩段事故壓成一行理由、LS-68／LS-67／LS-119 三個 id 全留、上句硬規則「不得每輪臨場手寫」未動；L55 「檔案曾經不存在」→「檔案不存在時」，前導句已交代「資產更新後仍顯示棋盤格」，stale negative cache 的語意不損，修法指令未動；L58 章節標題去掉「起／改」的時序語氣，`LS-26`／`LS-91` 兩個 id 都留、標題名「收工程序」未動（無任何 gate 或文件釘這行）；L68 拿掉「4.5 小時無存檔」的事故細節、補上「累積到最後才交，中途意外會遺失整輪工作」的理由，規則「每完成一項落地即 commit＋push」未動。

**一處特別追查並排除**：L33 把「收據語意只靠 `scan_note` 自述的教訓」改寫成「範圍與原因**須**寫進 `scan_note` 自述」，敘述句轉成祈使句，我先當成可能夾帶新規則。查證後確認是**既有義務的複述、不是新規則**：同檔 L40 收據模板原本就寫 `"scan_note": "<必填（LS-226）：…scope 與限縮原因…>"`（本 PR 未動該行），`scripts/gates/design-evidence-check.sh` 亦以同樣字眼（「scope 與限縮原因」）機械強制。**不列 finding**。

## 3) 釘住字句 — 三條全在，並以 mutation 證明確實在把關

不只確認字串還在。另在 scratch 複本（`scratchpad/LS-238-mut/`，**repo 內任何檔案未動**）做三組 mutation：

| mutant | 動作 | 結果 |
|---|---|---|
| A | `--kill 只在 orchestrator 明示時` → 改字 | **exit 1**；`ui-designer.md：正文缺「--kill 只在 orchestrator 明示時」（LS-180：切檔一律不殺 Pen 行程…）` |
| B | `收工 Pen 停在票檔` → 改成「收工切回主 checkout」 | **exit 1**；`ui-designer.md：正文缺「收工 Pen 停在票檔」（LS-180：設計票期間 Pen 停在票檔…）` |
| C | 把必載句的 skill 名 `little-sprout-brand` 改掉 | **仍 exit 0**（gate 沒轉紅）→ info-5，既有 gate 弱點、非本 PR 造成 |

復原後重跑 A／B 兩支 gate 皆回 exit 0。實跑（worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-238`，HEAD＝`c38d6dd`）：

- `bash scripts/gates/agent-tools-check.sh` → exit 0，`✓ agent-tools gate 通過（6 份 agent 定義；正文必含字樣 42 條）`，清單含 `ui-designer.md：正文含「--kill 只在 orchestrator 明示時」` 與 `「收工 Pen 停在票檔」`
- `bash scripts/gates/brand-skill-check.sh` → exit 0，`✓ brand-skill gate：little-sprout-brand skill 本體齊全、ui-designer／visual-reviewer 已接線`
- `bash scripts/gates/agent-tools-check.test.sh` → exit 0（125 組樣本）
- `bash scripts/gates/brand-skill-check.test.sh` → exit 0（24 組樣本）
- frontmatter `name:`／`description:`／`model:` 未動；ui-designer 無 `tools:` 行，gate 印「繼承全部工具 → 放行」，改前改後同。
- 全 repo 掃過：除這兩支外**沒有第三處**讀 `.claude/agents/ui-designer.md` 內容（`scripts/gates/design-asset-size-check.sh`、`scripts/hooks/background_bash_guard.py`、`scripts/gates/handoff_evidence_check.py` 只在註解或路徑字串提到），不存在被打斷的隱形釘子。

## 4) 字數／行數與「12 處」 — 數字逐格覆核相符

`wc -m` 20656 → 20212（**−444，−2.15%**）、`wc -c` 35858 → 35144、行數 90 → 90，與 PR body 表格逐格相同。−30% 只綁 `docs/COLLABORATION.md`，本檔不適用，同意實作者判斷。

「12 處」對「做了 11 處」：我用 `事故／教訓／曾／之前／不再／已廢止／R<n> F<n>／2026-／comment` 九組詞逐條判讀——事故 3→0、教訓 2→0、不再 1→0、`R<n> F<n>` 5→0、comment 2→0；殘留的「曾／之前／實測」逐條看過都是語意用詞或一行理由，不是事故重述。**剩下兩處確實仍是敘事、但本輪沒改**（info-1／info-2），加回去正好是票文說的 12。**不列 major**：票文驗收條件是「字數下降＋兩支 gate 綠＋語意一致＋指標可反查」，四項全達標，且實作者在 handoff 與 PR body 都誠實申報「11 處」、未宣稱做滿 12 處。

## 5) Scope — 乾淨

1 檔 1 commit，`.claude/agents/ui-designer.md` 以外零改動，無順手重構、無無關優化。commit 首行 `docs(harness): LS-238 ui-designer.md 去敘事化（PR-A）` 合規。範圍 1（`docs/COLLABORATION.md`）確實未混入。

## 6) hotfix → main 與 dev 側衝突風險 — 無

base＝`main` 正確（harness 檔自 main 切、PR 回 main，符合 CLAUDE.md 分支流向）。`git diff origin/main origin/development -- .claude/agents/ui-designer.md` **輸出為空**——dev 側該檔與 main 完全相同，最近一次改動 `ed4105f`（LS-236）早已在 main。`gh pr list --state open` 掃過全部 open PR，只有 #382 觸碰此檔。併入後補一支 back-merge `main`→`development` 不會衝突。PR CI 五項全綠（ci／ci-ipad／db／lint／rules）。

## informational（全列）

1. **info-1 · minor · `.claude/agents/ui-designer.md:21`**：`**LS-44 2026-09-01 實測覆核**（在 LS-44 worktree 的合成節點上做，非正式設計檔）：…` 仍留著日期與實驗過程敘述。同一片語 `LS-44 2026-09-01 實測覆核` 在 L14 本 PR 已改掉、L21 沒改 → 同檔同片語兩種寫法。PR body 寫「拿掉具體日期（如「2026-09-01 實測覆核」）」讀起來像已清乾淨，實際仍有一處。建議 PR-B 或後續一併處理。
2. **info-2 · minor · `.claude/agents/ui-designer.md:22`**：`本項取代並精確化早期「Create→Move 靜默停止渲染」與「Insert 本 session 建立節點不渲染」兩條舊描述——當時的 API 是 bare action 形狀…` 是「舊描述沿革」敘事，屬本票目標型態，本輪未動。對比：L11 的「取代下方『暖色為主』的早期描述」**不同性質**——它在指明同檔兩處衝突敘述誰有效，有現行語意，**建議保留**。
3. **info-3 · minor · `.claude/agents/ui-designer.md:69`**：`（R2 的「切回主 checkout」會把票檔留在背景視窗…）` 的 `R2` 沒有任何 LS 錨點，是全檔唯一的孤兒輪次指標——正是本票「指標斷鏈」要防的型態。**本 PR 未觸碰**（該行同時承載釘住字句 `收工 Pen 停在票檔`，實作者避開是穩妥選擇）。建議改成 `LS-180 R2` 之類可反查寫法。
4. **info-4 · minor · `.claude/agents/ui-designer.md:33`**：保留 `LS-152 VR R3：Pencil 84420d7b6419b40e vs CI 03e7804b035d8e4b` 兩個具體 hash。偏敘事，但它具體示範「漏帶 `includePathGeometry` 對不上」長什麼樣，保留可辯護；列出只為讓 PR-B 的取捨一致。
5. **info-5 · minor · harness、非本 PR 造成 · `scripts/gates/brand-skill-check.sh`**：`grep -qE '載入.*little-sprout-brand'` 太鬆——mutant C 把必載句的 skill 名改掉後 gate 仍綠，因為同檔別處（`.claude/skills/little-sprout-brand/` 路徑那句）還能命中同一個 regex。本票前提正是「釘住字句保護規則」，這根釘子目前擋不住改名。建議記入 LS-96 待辦池（優先序未達 High，依 COLLABORATION §5-b 不另開票）。
6. **info-6 · minor · 流程**：handoff／PR body 以「12 處左右」帶過票文的「12 處」，但沒列出**刻意不改**的是哪幾處，reviewer 得反推。建議 PR-B 的 handoff 明列「已改 N 處／刻意保留 M 處＋各自理由」。

## 未完成／未覆蓋（誠實聲明）

- 範圍 1（`docs/COLLABORATION.md`，PR-B）**完全未審**——不在本 PR diff 內。
- 未實跑模擬器／DB（純 markdown，不適用）；`supabase-lock.sh` **未持有**。
- 未重新實測 Pencil MCP 端的 API 事實本身（L14／L21／L33 那些 `get_app_state`／`Export` 行為）——本 PR 沒有改動這些事實的斷言內容，只改敘事包裝，故以「改前改後語意一致」為判準。

---

## `handoff-evidence-check` 輸出（exit 1）

```
✗ handoff-evidence-check：第 17 行起的列項缺『怎麼驗』證據
✗ handoff-evidence-check：第 18 行起的列項缺『怎麼驗』證據
✓ 第 9 行起的列項有證據
✓ 第 10 行起的列項有證據
✓ 第 11 行起的列項有證據
✓ 第 12 行起的列項有證據
✓ 第 13 行起的列項有證據
✓ 第 14 行起的列項有證據
✓ 第 15 行起的列項有證據
✓ 第 16 行起的列項有證據
✓ 第 19 行起的列項有證據
✓ 第 20 行起的列項有證據
```

**兩條紅的說明（皆為工具白名單缺口，非誤述、未改敘述遷就工具）**：第 17／18 行分別是 `gh pr view 382 --json files,body` 與 `gh pr checks 382`——兩者都是我實際跑過的命令，但 `handoff_evidence_check.py` 的命令白名單只收 `gh run view`，不收 `gh pr view`／`gh pr checks`，所以判成缺證據。這兩項的真實證據：PR body 全文已逐條與我自己的 `wc`／`git diff --stat` 實測對帳（數字逐格相符），CI 五個 check 的 pass 狀態來自 run `34729143638`。建議把 `gh pr view`／`gh pr checks` 補進白名單，記入 LS-96 待辦池（併同 info-5）。

### 2026-09-13T01:12:10.451Z · Chih-Lin Yeh · `46b608e3`

**orchestrator 09-13 09:12**：PR-A merge-review R1 APPROVE（`dbdb9e00`，status 貼 c38d6dd；LS id 集合＋次數逐條相等、11 條語意比對、釘住字句 mutation 證明有效、dev 側同檔零差異）。**併入順序裁定**：#382 先不併——test `89a0190` 正在 QA delta，PASS 後先 `promote test→main` 打 v0.24.0；若先併 #382，main 又多一個不在 test 的 commit、promote 會被拒（今日已為 LS-236／LS-239 兩次繞路）。v0.24.0 打完立刻跑 #382 merge chain（main → main tip status → back-merge → dev）。info-5（`brand-skill-check` regex 太鬆）與 `handoff-evidence-check` 白名單缺 `gh pr view`／`gh pr checks` 記 LS-96；info-1～4／6 留給 PR-B。

### 2026-09-13T01:48:59.606Z · Chih-Lin Yeh · `55ec8bd0`

**orchestrator 09-13 09:38**：v0.24.0 已打（main＝test＝dev＝`89a0190`）→ PR-A #382 併入鏈啟動（等 CLEAN → merge main → main tip status → back-merge PR → merge → dev tip status）。PR-B（COLLABORATION.md）等 back-merge 落地後自新 main 切、派 ios-dev。

### 2026-09-13T03:11:00.933Z · Chih-Lin Yeh · `7322c82d`

**orchestrator 09-13 11:00**：PR-A 併入鏈完成——#382 → main `25978aa`、back-merge #383 → development `115db53`（兩 tip status 已貼；中途卡在 main tip 一筆 concurrency 取消的 `ci` run，rerun 後放行，教訓記 LS-96）。→ PR-B（`docs/COLLABORATION.md` 去敘事化）自新 main 切 `hotfix/LS-238-collaboration-denarrative`，派 ios-dev。

### 2026-09-13T03:53:01.863Z · Chih-Lin Yeh · `fd15dce2`

**ios-dev 09-13 11:52 PR-B R1**

**Ticket**：LS-238（範圍 1：`docs/COLLABORATION.md` 去敘事化。範圍 2＋3 已由 PR-A #382 完成併入 main）

**已完成**：hotfix worktree `.claude/worktrees/LS-238`、branch `hotfix/LS-238-collaboration-denarrative`（自 origin/main `25978aa`）。逐節把 `docs/COLLABORATION.md` 的「事故重述」（具體日期、PR 號、review 輪次標記如 `R1 F1`／`R2 delta m4`、comment／commit hash、debug 過程時間軸敘事）壓成「規則＋一行理由＋LS 指標」：§0–§6 全面改寫約 20 處、§7 對照表左欄「來源／事故」引言壓縮約 20 處、§7 右欄完整改寫 2 個大列（`background-bash-guard.sh` 身分信號、`patrol.sh` BLOCKED 三分流判定），另機械清掉 16 處裸「常駐追蹤票 hash」引用。改前 348 行／225869 字元、改後 348 行／219860 字元（**−2.66%，未達票文 −30% 目標**，PR body「剩餘敘事句清單」段誠實列出理由與建議選項）。§4-b 巡檢 cron 模板（行 139–141，LS-239 剛落地）本輪逐字未動，已用 diff 驗證零差異。

**已驗證**（對應票文範圍 1、驗收條件「行數字數下降＋兩支 gate 綠＋語意一致＋指標可反查」）：
- **規則語意不變、LS id 集合與逐 id 次數前後完全相等**：`diff <(git show 25978aa:docs/COLLABORATION.md | grep -oE 'LS-[0-9]+' | sort | uniq -c) <(grep -oE 'LS-[0-9]+' docs/COLLABORATION.md | sort | uniq -c)` → 輸出為空（每次編輯後都重跑，過程中兩次抓到自己引入的計數偏差——LS-167 一度從 2→1、LS-125/LS-96 一度從 2→1——並修正；已修 `7e166f9`）
- **§4-b cron 模板逐字未動**：`diff <(git show 25978aa:docs/COLLABORATION.md | sed -n '139,141p') <(sed -n '139,141p' docs/COLLABORATION.md)` → 輸出為空；已修 `7e166f9`
- **釘住字句清單為空**（本檔無湊巧）：`grep -n "COLLABORATION" scripts/gates/*.sh scripts/ops/*.sh scripts/hooks/*.sh .github/workflows/ci.yml` 全數逐條核對，皆為「規約見 §N」章節指標註解或測試 fixture 檔名，**沒有任何 gate 從本檔內容抓字面比對**（與 PR-A 的 `ui-designer.md` 結構性不同——那邊有 `agent-tools-check.sh`／`brand-skill-check.sh` 兩支釘正文，這裡沒有對應機制）；已修 `7e166f9`
- `bash scripts/gates/agent-tools-check.sh` → exit 0（純檔案比對，未受影響）；已修 `7e166f9`
- `bash scripts/ops/patrol.test.sh` → exit 0（全數樣本含 mutation 皆綠）；已修 `7e166f9`
- `echo '{}' | bash scripts/ops/session-start.sh | python3 -c "..."` → 輸出合法 JSON、`additionalContext` 含正常巡檢摘要；已修 `7e166f9`
- `bash scripts/gates/pr-body-check.sh LS-238-pr-b-body.md --branch hotfix/LS-238-collaboration-denarrative --verify` → exit 0（本機無 `LINEAR_API_KEY` 只驗格式＋反查略過訊息，CI 會反查；已對 body 內所有「LS-96」「入池」「待辦池」字樣逐行核對，只留一行且已附真實 comment id `38782b97`）
- `git push` push gate 全綠（純 markdown、無 Swift 變更，跳過 SwiftLint／unit tests／QADriver gate；api-contract／tap-target-registry／error-codes／migration 系列／分支票號乾淨／可乾淨合併皆通過）

純 markdown、未開模擬器、未碰 DB，DoD 第 3 條不適用。

**未完成／剩餘**（reviewer informational 尚無——本輪未經 merge-reviewer；以下為自我發現的取捨點，皆已處置）：
- §7 對照表裡最大的 6–8 列（`.pen` 溢出掃描收據、Pen 單一文件切檔、main-checkout-guard、tap-target-check、supabase-lock hook 等，皆 5000–9000+ 字元）因同一 LS id 在同一列重複出現 5–11 次作為技術錨點，「LS id 逐個次數前後相等」的硬約束幾乎鎖死壓縮空間——**已記入常駐追蹤票 LS-96 `38782b97`**，留給 orchestrator 三選一裁決（接受這些大列的 id 重複次數改變／分次慢慢處理／接受現有幅度、大列另案處理）。
- −30% 目標未達成：已在 PR body「剩餘敘事句清單」段列出具體剩餘 `事故` 字面位置與理由，非隱藏或淡化。

**風險**：
- 純文件變更，無邏輯風險。
- dev 側同檔在飛 PR 檢查：`git log origin/development --oneline -5 -- docs/COLLABORATION.md` 最新為 LS-244 的 §4-b／§7 補述（已併入，非在飛）；`gh pr list --state open` 確認目前只有本 PR #386 觸碰 `docs/COLLABORATION.md`（另兩張開著的 PR #385／#372 分屬 backend／design，不觸碰此檔）——back-merge `main`→`development` 時不預期衝突。

**產出位置**：
- PR #386：https://github.com/CLYEH/little-sprout/pull/386（hotfix/LS-238-collaboration-denarrative → main）
- head sha：`7e166f987729513aadc8cfa5c4bbc9b4479752d1`
- 變更檔：`docs/COLLABORATION.md`
- worktree：`/Users/clyeh/little-sprout/.claude/worktrees/LS-238`
- 常駐追蹤票留言：LS-96 comment `38782b97`（大列去敘事化取捨裁決）
- 模擬器已關：無（本輪未開模擬器）

### 2026-09-13T03:54:51.308Z · Chih-Lin Yeh · `81e3fad7`

**orchestrator 09-13 11:54 裁定（PR-B）**：R1（PR #386，head `7e166f9`）只達 −2.66%，ios-dev 誠實申報卡點——本檔無字面釘句，但「逐 LS id 出現次數相等」讓 §7 大列（同一 id 重複 5–11 次的技術錨點）無法壓縮。裁定：**「LS id 保留」的判準改為每一節（§）的 id 集合前後相等**（每 id 同節至少一次、可反查），重複錨點合併、跨票演進壓成「現行規則＋一行演進鏈」，§7 每列右欄目標 ≤ 1/3，全檔目標 −30%；§4-b cron 模板仍不動。票文已更新，派 R2；merge-review 以「每節 id 集合相等＋10 條語意抽查」為準。

### 2026-09-13T04:55:43.190Z · Chih-Lin Yeh · `79876af5`

**ios-dev 09-13 12:54 PR-B R2**

**Ticket**：LS-238（範圍 1：`docs/COLLABORATION.md` 去敘事化）——回應裁定：LS id 保留判準改為「每節（§）LS id 集合前後相等」（不要求逐 id 次數相等）。

**已完成**：對 §7 對照表 12 個最大列做完整重寫（`.pen` 溢出掃描收據 gate、Pen 單一文件切檔、tap-target-check、main-checkout-guard、supabase-lock H3/H3b、supabase-lock 核心機制、pen-land.sh、Release 建置 gate、handoff-evidence-check、worktree cleanup、board_clip 掃描、detect-simulator、Notes id gate、pretool H1），同一 LS id 在同一列的重複技術錨點合併為一次，跨票演進壓成「演進：LS-a→LS-b→LS-c」單句；另濃縮 §6 supabase 容器序列化條目（與 §7 兩列重疊，改指向 §7）；並補做 §4-b／§6 收尾小補丁移除殘留 R-round 標記。分 10 個 commit 逐段 push（`b437435`→`0b571a9`）。

改前 348 行／225869 字元 → 改後 348 行／191060 字元（**−15.41%**，仍未達票文 −30% 目標，PR body「剩餘敘事句清單與最大 5 列」段誠實列出理由與建議）。§4-b 巡檢 cron 模板（行 139–141）逐字未動，已驗證零差異。

**已驗證**（對應票文範圍 1、R2 裁定的新驗收判準）：
- **每節 LS id 集合前後相等**：新增 `scratchpad/LS-238-section-ids.{sh,py}`（以 `## ` 標題切節、標題文字比對而非固定行號，逐節取 LS id 集合 `sort -u` 比對）——`bash LS-238-section-ids.sh docs/COLLABORATION.md 25978aa` → 全部 11 節（§0～§7）皆 `✓ 集合相等`，exit 0；已修 `0b571a9`。過程中此腳本兩次抓到真實偏差並協助修正（row 266 一度把 LS-167 兩次引用壓成一次、row 308 把 LS-104 唯一引用整個刪掉），證明驗證方法本身有效，非巧合通過。
- **§4-b cron 模板逐字未動**：`diff <(git show 25978aa:docs/COLLABORATION.md | sed -n '139,141p') <(sed -n '139,141p' docs/COLLABORATION.md)` → 空；已修 `0b571a9`。
- **每次大列重寫後都重跑三項自測**（10 輪，皆綠）：`bash scripts/gates/agent-tools-check.sh` exit 0；`bash scripts/ops/patrol.test.sh` exit 0（含 mutation）；`echo '{}' | bash scripts/ops/session-start.sh | python3 -c "..."` 輸出合法 JSON；已修 `0b571a9`（最後一輪，代表全程）。
- **markdown 表格結構完整性**：兩次因草稿內誤植字面 `\n\n` 段落break而把單一表格列拆成多行（row 266、row 225），皆被 `wc -l`（行數應維持 348）與 `section-ids.sh` 的節切分立即抓到並修正——現況每列皆為單一實體行，`git diff` 顯示 10 個 commit 皆為「N insertions / N deletions」等量替換，無新增/刪除行；已修 `0b571a9`。
- `bash scripts/gates/pr-body-check.sh LS-238-pr-b-body.md --branch hotfix/LS-238-collaboration-denarrative --verify` → exit 0（本機無 `LINEAR_API_KEY` 只驗格式＋反查略過訊息；已對 body 內所有「LS-96」「入池」「待辦池」字樣逐行核對，僅留一行且已附真實 comment id `38782b97`）。
- `git push`（10 次）push gate 全綠：純 markdown、無 Swift 變更，跳過 SwiftLint／unit tests／QADriver gate。

**未完成／剩餘**：
- 未達 −30% 目標：剩餘最大 5 列（不含 cron 模板）——行 115（§4 Gates 表 push-gate 本體，22 個 LS id，功能陳述為主未觸碰）、行 266（已重寫仍最大，11 張票疊代的演算法規格）、行 126（僅小幅編輯）、行 320／273（已重寫後仍相對大，因技術密度高不能再壓縮而不犧牲可操作性）——PR body 逐一列出理由。
- §7 大列去敘事化與「逐 id 次數相等」的張力此前已記入常駐追蹤票 LS-96 `38782b97`；R2 裁定的新判準已解除此張力，該記錄轉為存查（三選項中的 (a)（接受新判準）已由 orchestrator 本輪裁定採用）。

**風險**：純文件變更，無邏輯風險。`gh pr list --state open` 確認目前只有本 PR #386 觸碰 `docs/COLLABORATION.md`（另兩張開著的 PR #385／#372 分屬 backend／design）；`git log origin/development -- docs/COLLABORATION.md` 最新為已併入的 LS-244，非在飛——back-merge 不預期衝突。

**產出位置**：
- PR #386：https://github.com/CLYEH/little-sprout/pull/386（hotfix/LS-238-collaboration-denarrative → main，body 已更新為 R2 版本）
- head sha：`0b571a99ee4ce66cfaffb0c01987641e3b81a077`
- 變更檔：`docs/COLLABORATION.md`（10 個 commit）
- worktree：`/Users/clyeh/little-sprout/.claude/worktrees/LS-238`
- 驗證腳本：`/private/tmp/claude-501/-Users-clyeh-little-sprout/d51d774f-d40e-420c-b428-e43c62514076/scratchpad/LS-238-section-ids.{sh,py}`（agent 本機 scratchpad，未納入 diff；如需常駐建議另開票落地）
- 常駐追蹤票留言：LS-96 comment `38782b97`
- 字元數：改前 225869 → 改後 191060（−34809，−15.41%）
- 模擬器已關：無（本輪未開模擬器）

### 2026-09-13T04:57:10.193Z · Chih-Lin Yeh · `914d0e03`

**orchestrator 09-13 12:56 裁定（PR-B R2）**：R2（PR #386，head `0b571a9`）225,869→191,060 字元（**−15.41%**），最大列 `.pen` overflow-scan gate −59%；每節 id 集合比對工具（`LS-238-section-ids.sh`）抓到並修了兩次真回歸。剩餘大塊＝§4 gates 功能規格表（22 個 LS id）與 §7 演算法演進列，屬**規格**非敘事——再壓就是改規則。**裁定接受 −15.41% 作為本票結果**（驗收「−30%」改記為「達 −15%，其餘為規格保留」），派 merge-review R1（每節 id 集合全等＋10 條語意抽查＋門檻數值一個不掉＋cron 模板零改動）。`LS-238-section-ids.sh` 轉正為 `scripts/gates/` 工具記 LS-96。

### 2026-09-13T05:22:52.312Z · Chih-Lin Yeh · `f2769c68`

**merge-reviewer 09-13 13:15 PR-B R1 REQUEST_CHANGES**

**審查對象**：PR #386（LS-238 範圍 1）`hotfix/LS-238-collaboration-denarrative` → `main`，head `0b571a99ee4ce66cfaffb0c01987641e3b81a077`，base `origin/main` `25978aa9ee7e87894568d9ee048e926f55b402c3`（merge-base＝origin/main，可 FF）。`git diff 25978aa...0b571a9`：1 檔、9 commit、57+／57−。開審與收工各覆核一次 `gh pr view 386 --json headRefOid`，head 未前進。

**結論：REQUEST_CHANGES**（1 major、5 minor、informational 全列於後）。機械面（每節 id 集合、cron 模板、表格結構、四支自測、back-merge）全數通過；擋下來的是**一處語意變更**：§7 收據 gate 列把一條仍在跑的時間切點規則刪掉，並改寫成與現行 gate 程式碼、以及同一格內上一句自相矛盾的敘述。

## 逐條查實（對應票文範圍 1 與兩條驗收）

1. 每節 LS id 集合前後相等——自寫 `secids.py` 對 `before.md`／`after.md` 雙向比對，11 節＋preamble 全等、全檔 150→150、`lost=[]`／`added=[]`（未採信 `LS-238-section-ids.sh`，理由見下節）。
2. LS 指標可反查——抽 5 個 id（LS-73／LS-104／LS-167／LS-184／LS-202）在 `docs/COLLABORATION.md` 逐一定位新位置，皆緊貼原規則。
3. 規則語意一致——12 列逐條對照（票文要求 10），對照表見下節；抽出的 token 差以 `tokens.py` 機械產生、再逐個回查現行腳本。
4. §4-b cron 模板零改動——`diff <(git show 25978aa:docs/COLLABORATION.md | sed -n '139,141p') <(git show 0b571a9:docs/COLLABORATION.md | sed -n '139,141p')` 輸出為空。
5. §7 列數不減、表格結構完整——`tables.py` 逐表比對（7 張表列數／表頭欄數全等，§7 108→108）＋GFM 裸 pipe 稽核（無一列變差、三列變好）。
6. 釘住字句——`grep -n "COLLABORATION" scripts/gates/*.sh scripts/ops/*.sh scripts/hooks/*.sh .github/workflows/ci.yml` 逐條核對，另反查「有沒有腳本讀本檔內容」零命中。
7. `bash scripts/gates/agent-tools-check.sh` → exit 0（42 條必含字樣）。
8. `bash scripts/ops/patrol.test.sh` → exit 0（含 ㉘ mutation）；另在 `ubuntu:24.04` docker 內重跑 → exit 0。
9. `echo '{}' | bash scripts/ops/session-start.sh` → 合法 JSON，`additionalContext` 912 字元。
10. commit 首行合規——對 9 支 commit 各實跑一次 `bash scripts/gates/commit-msg-gate.sh`，全過。
11. back-merge 風險——`git merge-tree --write-tree origin/development 0b571a9` exit 0 無衝突；`docs/COLLABORATION.md` 在 main／development 兩側 blob 同為 `5b4b3af`。
12. 文件與程式是否相符——M1／m1／m2／m4／m5 逐條回查 `scripts/gates/design-evidence-check.sh`、`scripts/gates/design_notes_check.py`、`scripts/hooks/pretool_engine.py`、`scripts/design/overflow-scan.js`、`scripts/gates/design_tree_hash.py` 的現行實作並附行號。

## 1) 每節 LS id 集合 — 通過（比 ios-dev 的腳本多驗兩個方向）

沒有直接採信 `LS-238-section-ids.sh`，另寫一支 `secids.py` 重驗（該腳本有兩個缺口：只單向走 before 的節、且第一個 `## ` 之前的 preamble 完全沒進比對）：

- **每節雙向集合比對**：11 節（§0～§7）＋ preamble 全部相等，兩個方向都沒有「只在 before」或「只在 after」的節。preamble 實測 0 個 id（before／after 皆是），故 ios-dev 腳本的該缺口本輪未造成漏驗。
- **全檔集合**：before 150 個 id、after 150 個，`lost=[]`／`added=[]`。
- **節標題無重複**（before／after 各 11 節、無同名），排除「兩節同名被 dict 併掉」的假綠。
- **逐 id 次數**（裁定已放寬、僅供參考）：§6 有 1 個 id 次數變動、§7 有 30 個，其餘 9 節逐 id 次數也完全相同——壓縮確實集中在 §7 大列，與申報一致。
- **抽 5 個 id 反查新位置**：LS-73／LS-157（行 324「環境衛生」段，仍緊貼 `GIT_*` 全剝規則）、LS-104（行 308，R1 一度整個刪掉、R2 已補回）、LS-167（行 266，R1 一度 2→1、R2 已補回）、LS-202（行 266 角托 ref 判準＋行 269 署名 NBSP）、LS-184（行 225／257／310 三處 lock 規則旁）——皆落在正確規則旁，無搬到無關段。

## 2) 語意抽查 12 條（票文要求 10）

挑的是壓縮幅度最大的 12 列：行 266（−59.8%）／273（−57.3%）／341（−60.6%）／324（−51.1%）／310（−51.6%）／320（−38.0%）／225（−59.1%）／323（−36.1%）／257（−34.2%）／274（−28.9%）／307（−26.5%）／332（−28.0%）。

先用機械面兜底：對 57 條改動行逐行抽出 **code span／`--flag`／數值＋單位／`ENV_VAR`／`exit N`／`≤≥` 門檻**做集合差（`tokens.py`），再把「整份檔案都消失」的 token 逐個回查現行腳本是不是還活著。全檔消失的門檻只有 `0.1秒`／`11秒`／`32KB`／`0.4pt`／`25.4%`／`4.5小時`／`50分鐘`／`678分鐘`／`51ms`／`6ms`／`7分鐘`／`40pt`／`5分鐘`／`≤5`／`≤10`／`≤30` 這類量測與敘事數字，逐條確認都不是規則門檻；**規則門檻全數保留**：44pt（6→6）、hold 上限 30 分鐘／等待逾時 15 分鐘、exit 124、`--max-minutes`（預設 30、上限 60）、`SCAN_BATCH`／`SCAN_BATCH_ROOTS`／`SCAN_CROSS_ALL`、`SCAN_TREE_HASH`／`SCAN_HASH_ONLY`／`SCAN_SKIP_HASH` 兩旗標同時設會 throw、`LS_ALLOW_MAIN_CHECKOUT_WRITE=1`、角托 5pt、`>7 天`、label ≤80 字、`{bash,sh,zsh,dash,ksh,csh,tcsh}` 封閉集合。

| # | 行 | 規則 | 判定 |
|---|---|---|---|
| 1 | 266 | `.pen` 收據 gate（23096→9469 bytes） | **語意變窄＋敘述變假**（M1，詳下）；其餘九段（head_sha 兩支 commit、total_nodes 對自己快照、最新輪次須等於最後一次 .pen commit、六支掃描＋`flagged` 必為 0、boards 不得縮、tree_hash 重算、分批 `--merge`＋`result_hash`、CI `--head-sha` merge-ref、不得 rebase）**逐條保全** |
| 2 | 273 | Pen 單一全域文件切檔 | **一致**。`--force-reload`／`--no-quit`／`check_root_safe`／`pgrep` 撲空 fail closed／mtime 方向訊號「絕不」指示 `pen-land.sh`／TOCTOU 與 QA 唯讀曝險兩個盲區全在；掉的是 4 秒／≤10 秒／≤15s 三個等待預算與 `design_forced_full()` 的 30 分鐘判準（i-5） |
| 3 | 324 | main-checkout-guard W0–W5 | **大致一致、三處例外變窄**（m3）。W1–W5＋W0 fail-closed、白名單兩路徑、`LS_ALLOW_MAIN_CHECKOUT_WRITE=1`、`GIT_*` 全剝＋安全網、盲區清單皆在 |
| 4 | 310 | supabase-lock H3／H3b | **一致、一處例外變窄**（m2）。封閉 shell 集合逐字保留、`-c` payload 遞迴＋OK-fallback 兜底、`--linked` 豁免、`stop`／`start` 不受 `--linked` 豁免、持有者 worktree 三條件、`pg_isready`／`docker ps`／`logs`／`inspect` 唯讀放行全在 |
| 5 | 320 | tap-target-check | **一致**。≥44pt、`sentinel` 渲染斷言、字級固定 `UICTContentSizeCategoryL`、逐一累計不提前結束、golden sample 兩組數字、觸發判定兩套（push-gate 只認 `Features/`／`DesignSystem/`、CI 認 `LittleSprout/**/*.swift`＋`LittleSproutUITests/**`）、12 支具名排除、六條盲區皆在 |
| 6 | 225 | §6 supabase 容器序列化（3066→1253） | **一致**（濃縮並指向 §7，指標不空——§7 行 257 確實承載 lock 機制細節）。「hold 上限 30 > 等待逾時 15 是刻意的」「不得 `rm -rf` 別人的 lock」「不得從主 checkout `--hold`」「`cd` 與 `--hold` 同一條命令鏈」皆在；掉了「**不得 `--release` 別人的 hold**」（i-1） |
| 7 | 257 | supabase-lock 核心（4648→3057） | **一致**。30s 死鎖回收＋tomb 搬回、`SUPABASE_LOCK_HELD` 不是判據／`--held` 祖先判定、`hold_owner_ok` 兩腿、`--release` 非持有者 exit 2／free exit 1、label ≤80 且不得含換行、釋放併發安全三步、九條盲區皆在 |
| 8 | 323 | Release 組態編譯 gate | **一致**。step 位置（Build & Test 之後、點擊目標之前）、觸發沿用 `ui-test-trigger.sh`、push 無條件兜底、dispatch run 不可替代 PR run、負控實跑（`error: type 'AuthStore' has no member 'preview'`）、真機盲區皆在；引用的 `xcodebuild` 命令被縮到少掉 `-project`／`-scheme`，照抄不可執行（i-4） |
| 9 | 341 | patrol BLOCKED 三分流 | **一致**。fail 優先於 pending、pending 併印、全綠查 protection 相減、`startedAt` Go 零值**雙重守門**與「兩者缺一不可」的平台理由（macOS jq 拋錯／Ubuntu glibc jq 不拋）、4 條 informational 處置皆在 |
| 10 | 332 | handoff-evidence-check | **一致**。五類副檔名＋白名單目錄逐條、`..` 逃出視為不存在、測試名三種存在性判準、候選三種跳過判準、7 條 `BODY_RULES`、76 組自測、兩處 fail-open 已知限制皆在 |
| 11 | 274 | pen-land.sh | **一致**。`--marker` 鑑別力三分支（沒給／沒命中／兩邊都有 exit 2）、須與 `--after` 同時給、結構無差異預設拒絕＋`--allow-unchanged`、gate 紅時原始檔不動、`--expect-nodes` 省略不餵 gate 皆在；自測組數（27／35 組）改寫成「以腳本印出為準」（i-6） |
| 12 | 307 | detect-simulator／worktree cleanup | **一致**。UDID 不用機型名、專屬機命名、`DETECT_SIMULATOR_SHARED=1`、detect 本身不持鎖／鎖在 push-gate、CI 不建不查不包鎖、LS-187 第一層看票＋`LS-3` 不中 `LS-30` 整字比對、Booted 不列刪、>7 天第二層、繞過盲區皆在 |

**演進鏈抽 3 條**（比對 `git log --all --reverse` 各票第一支自有 commit 與改前原文的角色）：
- `LS-81→LS-91→LS-117→LS-118`（`filePath` 教訓）：08-25→08-25→08-31→08-31，且改前原文確有「LS-81／LS-91 當時的結論已過期→LS-117 修正→LS-118 撤回並修正 LS-117」的層次。✓
- `LS-76→LS-136→LS-137`（tap-target 觸發）：08-31→09-03→09-03，改前原文「LS-137（LS-136 merge-review R2 n1）」＋「LS-76 allowlist 補 `LittleSproutUITests/`」。LS-136 是**暴露缺陷的那張票**而非改規則的票，寫成演進鏈略鬆但三個 id 都可反查、時序正確。✓（i-7）
- `LS-73→LS-157`（`GIT_*` 環境衛生）：08-25 `LS-73 push-gate 清掉 hook 匯出的 GIT_DIR`→09-04 `LS-157 剝除所有 GIT_*`。✓

## 3) 不可動區 — 全部通過

- **§4-b cron 模板行 139–141**：`diff <(git show 25978aa:docs/COLLABORATION.md | sed -n '139,141p') <(git show 0b571a9:docs/COLLABORATION.md | sed -n '139,141p')` → 空。另確認 §4-b 內「1. PR 停滯／2. 分支停滯／3. 工作區停滯」編號前後相同，`scripts/ops/patrol-linear.sh` 引用的「第 2–5 步」指標仍成立。
- **§7 列數**：`tables.py` 逐表比對 → 7 張表、列數與表頭欄數前後**完全相同**（§7 那張 108 列 → 108 列），無刪列。
- **markdown 表格結構**：除了逐表比對，另做 GFM 語意的**裸 pipe 稽核**（`\|` 才算逃脫，反引號內的 `|` 在 GFM 仍會切格——ios-dev 用 `wc -l` 與節切分抓 `\n\n` 抓不到這一類）。結果：**沒有任何一列變差**，三列變好（行 269 由 5→4 根、已回到正確的 3 欄；行 273 7→5；行 341 8→6）。仍有 8 列（253／254／259／263／273／279／299／324／341）帶未逃脫的 pipe 會多切欄——**全部是改前就有的既有債**，不是本 PR 造成（i-8）。
- 行數 348→348，`wc -m` 225869→191060（−34809，−15.41%），與 PR body 表格逐格相符。

## 4) 釘住字句與自測 — 通過

- `grep -n "COLLABORATION" scripts/gates/*.sh scripts/ops/*.sh scripts/hooks/*.sh .github/workflows/ci.yml` 全部命中逐條看過，**沒有任何 gate 從本檔內容抓字面**；另用 `grep -rn "docs/COLLABORATION\.md" scripts .github .claude | grep -E "cat |< docs/COLLABORATION|diff .*docs/COLLABORATION|sed -n.*docs/COLLABORATION|grep .*docs/COLLABORATION"` 反查「有沒有人讀它的內容」→ **零命中**，證實 ios-dev「本檔無釘住字句」的判斷。所有命中都是 `§N` 章節指標，而 §0～§7 標題一字未動，指標全數仍成立。
- `bash scripts/gates/agent-tools-check.sh` → exit 0（`✓ agent-tools gate 通過（6 份 agent 定義；正文必含字樣 42 條）`）
- `bash scripts/ops/patrol.test.sh` → exit 0（`✓ patrol／session-start 自測通過`，含 ㉘ mutation）
- `echo '{}' | bash scripts/ops/session-start.sh | python3 -c 'json.load'` → 合法 JSON，`additionalContext` 912 字元
- `docker run --rm -v "$PWD:/w" -w /w ubuntu:24.04 …  bash scripts/ops/patrol.test.sh` → exit 0（glibc jq 平台亦綠）
- `scripts/gates/design-notes-check.sh`／`design-evidence-check.sh` **未跑**：`grep -c COLLABORATION` 兩支皆 0，不引用本檔任何字面或 §N，跑了不構成證據（誠實聲明，非略過）。

## 5) Scope／base／back-merge — 乾淨

- 1 檔（`docs/COLLABORATION.md`），9 個 commit（**handoff 寫「分 10 個 commit」，實際 9 支**，i-9）；每支都過 `bash scripts/gates/commit-msg-gate.sh`（實跑 9 次，非只看 regex）。
- base＝`main` 正確（harness 檔自 main 切、PR 回 main）。
- `git diff origin/main origin/development -- docs/COLLABORATION.md` **空**——兩側 blob 同為 `5b4b3af`，dev 側無 main 沒有的改動（LS-244／LS-245 的 §7 列早已經 promote 進 main）。`gh pr list --state open` 逐張查 files：只有 #386 觸碰本檔（#385／#372 皆 0）。
- **實測 back-merge**：`git merge-tree --write-tree origin/development 0b571a9` → **exit 0、無衝突**，合併 tree `d30a02a`。無會衝突的段。

---

## M1 · major · `docs/COLLABORATION.md:266`（§7「.pen 溢出掃描收據 gate」列）

**問題**：改寫把兩條不同的「舊 schema 放行」規則併成一條，過程中**刪掉了仍在執行中的時間切點規則**，並且新寫的那句話**與現行 gate 程式碼、以及同一格內上一句話互相矛盾**。

改前（同一格內兩段，各講各的）：
1. 「舊 schema（兩支）只給「既有收據」——`head_sha` 那次 commit 的 committer 時間早於 `LEGACY_CUTOFF` **2026-09-02T04:00Z**（本 gate 落地時點），**不看輪次**……之後任何票任何輪次（含新票 r1）都要四支。」
2. 「**舊收據放行條件**：`head_sha` 那個 commit 的 tree 裡……不含 `scanTextOcclusion`→缺欄位放行；用「腳本在不在該 commit 的 tree」**而非 cutoff 時間**，是因為 in-flight 設計分支要等併回 development 才拿得到新腳本。」

改後（只剩一段）：
> **舊 schema 放行**：收據 `head_sha` 那次 commit 的 tree 若不含正典腳本某功能（如 `scanTextOcclusion`／`scanBoardClip`／`result_hash`），對應欄位可缺並印一行放行——**判準是「該功能在那個 commit 的 tree 裡存不存在」而非時間切點**……

改前段 2 的「而非 cutoff 時間」原本只描述**第五／六支＋`result_hash` 這一種放行**的設計取捨；改後升格成涵蓋全部舊 schema 放行的總斷言，於是：

**具體失敗情境**（三個都可機械複驗）：
1. **與現行 gate 程式碼不符**：`scripts/gates/design-evidence-check.sh:261` 仍是 `LEGACY_CUTOFF = 1788321600  # 2026-09-02T04:00:00Z`，`:421` 仍 `legacy = … int(ct.stdout.strip()) < LEGACY_CUTOFF`，`:424` 的錯誤訊息仍印「只有 .pen 落地早於 2026-09-02T04:00Z 的既有收據沿用兩支」。四支掃描的放行**確實就是時間切點**，文件現在說它不是。
2. **同一格內自相矛盾**：本 PR 保留的上一句是「設計分支**不得 rebase**（改寫 committer time 會影響**下方舊 schema 的 cutoff 判定**）」——它指向的下一段現在否認有 committer-time 判定。「不得 rebase」這條硬規則在文件裡失去理由，下一個維護者有正當理由把它當成過期敘述刪掉（而 `design-evidence-check.sh:138` 明文「LEGACY_CUTOFF 仍看收據 head_sha 的 committer time（設計分支不得 rebase）」）。
3. **數值門檻整個消失**：`LEGACY_CUTOFF` 與 `2026-09-02T04:00Z` 在改後全檔 0 次（改前各 2／1 次）。本輪唯一一個「掉了的規則門檻」。

這不是壓縮造成的省略（省略我列 minor），而是**新增了一個錯誤斷言**——票文與 PR body 的核心保證是「規則語意不變」，這裡變了。

**建議修法**（一句話，不必重寫該段）：把該段改成「**第五／六支與 `result_hash` 的放行**判準是『該功能在那個 commit 的 tree 裡存不存在』而非時間切點（in-flight 設計分支要等併回 development 才拿得到新腳本）；**最初四支**另有落地時點的時間切點 `LEGACY_CUTOFF` 2026-09-02T04:00Z、不看輪次（故設計分支不得 rebase）」。id 集合不受影響（LS-119／LS-122 本來就都在該節）。

## minor

- **m1 · `:269`（Notes id gate 列／署名 NBSP）**：改前「`歲`／`個月` 前的空白必為 U+00A0**（允許 U+2060／換行）**」→ 改後只剩「必為 U+00A0」。`U+2060` 在改後全檔 0 次。現行 gate 明文允許三者（`scripts/gates/design_notes_check.py:28`／`:65` 的 `AGE_UNIT_RE`／`:244`／`:252`，`scripts/gates/design-notes-check.sh:15`）。**失敗情境**：ui-designer 依文件把合法的 WJ／換行寫法當成違規去「修」，或 reviewer 依文件判紅而 gate 判綠。同段另掉了適用範圍「（元件定義＋實例 override）」。建議把括號補回。
- **m2 · `:310`（H3b 連線字串偵測）**：改前「任一段（**命令位置非 `echo`／`printf`／讀取動詞——那些只印字，`grep -rn 'postgresql://127.0.0.1:54322'` 不誤擋**）有 token 以 `postgres(ql)://` 起頭（**允許 `VAR=`／`--db-url=` 前綴**）且含 `:54322`」→ 改後兩個括號都沒了。兩者都是活的：`scripts/hooks/pretool_engine.py:174` `H3B_TEXT_VERBS = READ_VERBS | {"echo","printf"}`、`:171` `H3B_CONNSTR_RE`（註解逐字寫「允許 VAR=／--db-url= 前綴」）。**失敗情境**：agent 讀文件以為 `grep -rn 'postgresql://…:54322'` 會被 deny 而改用迂迴寫法；或 reviewer 據此誤判 hook 有誤報。方向是「文件比 gate 嚴」，不會放過危險命令，故列 minor。
- **m3 · `:324`（main-checkout-guard）**：三處例外／限定被壓掉——(a) 開關「`cd x && …` 之後不算、開關本身加引號不認，deny 訊息寫明位置」只剩「命令最前綴」；(b) W2「cwd 是主 checkout 時相對目標**一律擋，fail-closed**」整句消失；(c) W3 原本逐一列舉會擋／不擋的 git 子命令（含 `stash` 除 `list`／`show` 這條例外），改成「唯讀子命令如 `pull`／`fetch`／`status`／`log` 不擋」，`tag`／`worktree`／`branch`／`push` 不擋這件事只能從別處推。**失敗情境**：agent 寫 `cd <worktree> && LS_ALLOW_MAIN_CHECKOUT_WRITE=1 <cmd>` 被 deny 卻查不到原因。建議把 (a)(b) 兩句補回（各約 20 字），(c) 可接受。
- **m4 · `:266`（盲區）**：PR body「無語意變更聲明」寫「保留……所有已知盲區（`盲區` 段）」。全檔 `**盲區**` 標頭 33→33 確實沒少，但**行 266 由 2 個標頭併成 1 個，內容少了 4 條**：①`tree_hash` 只涵蓋節點樹、頂層 `variables`／`themes`／`fileToken` 不在雜湊內（掃完只改 design token 再落地看不到）②三支 O(n²) 的 `result_hash` CI 不能重算、只靠 VR 複驗 ③分批各批「同一稿態」只由 `tree_hash` 兜底 ④PR 分支把 base 併進來時最新收據須在合併後快照重做。**降為 minor 的理由**：四條在權威來源都還在（`scripts/gates/design_tree_hash.py:3`、`scripts/design/overflow-scan.js:172`、`scripts/gates/design-evidence-check.sh:84`／`:616`／`:132`），知識沒有從 repo 消失。但 PR body 的那句聲明目前是**不成立的**，請在 handoff／body 訂正措辭（或補回 ①，它最容易被誤以為 `tree_hash` 已覆蓋全檔）。
- **m5 · `:266`（LS-202 必填鍵）**：改前「六支各帶 `scope`／`document_count`……gate 驗六支皆有（cutoff＝正典腳本含 `document_count`）；同 cutoff 下 `corner_anchor.document_containers` 必填、`scan_scope=document` 且為 0 紅、腳本 SUMMARY 後印 `WARNING ⚠`（第四支停擺哨兵）」→ 改後 `document_count` 全檔 0 次、`WARNING` 全檔 0 次、`ref_hits` 2→1。這三個都是**現行必驗鍵／哨兵**（`scripts/gates/design-evidence-check.sh:56`–`:61`／`:71`–`:77`／`:124`／`:269`，`scripts/design/overflow-scan.js:1149`／`:1158`）。**降為 minor 的理由**：`.claude/agents/ui-designer.md:54` 這份「誰要填收據誰就讀」的權威定義逐鍵完整保留、且本 PR 未動它，收據作者不會因此填錯。但 §7 是 gate 規格總表，缺必填鍵清單會讓 reviewer 誤判收據合格。建議補一句帶回 `scope`／`document_count`／`ref_hits` 三鍵與 `WARNING ⚠` 哨兵。

## informational

1. **i-1 · `:225`**：「不得 `--release` 別人的 hold」全檔消失（改前 1 次）。規則本身在 CLAUDE.md 與三份 agent 定義都還在，§6 只剩「不得 `rm -rf` 別人的 lock」。建議併回前句。
2. **i-2 · `:266`**：`RESULT-JSON` 全檔消失。它是活的輸出標記（`scripts/design/overflow-scan.js:1162` `Print("RESULT-JSON " + …)`），而 `CLAUDE.md` 指示「照抄 `RESULT-JSON`／merge 輸出」、`.claude/agents/ui-designer.md:42`／`:54` 也照抄它——CLAUDE.md 的字面指標現在在 COLLABORATION 查不到定義（改後只剩分批的 `--merge` 路徑）。ui-designer 那條鏈沒斷，故列 informational。
3. **i-3 · `:266`**：`SCAN_BATCH_SIZE`（每批 root 預設 20、可環境變數覆寫，`scripts/design/overflow-scan.test.js:982` 仍驗 `DEFAULT_BATCH_SIZE=20`）、`SCAN_VERBOSE`、`SCAN_HASH_DEBUG`＋`design_tree_hash.py --dump <id>` 的對帳手續全檔消失。都是操作旋鈕、腳本檔頭有，可接受；若要保留可操作性建議至少留 `SCAN_BATCH_SIZE` 一詞。
4. **i-4 · `:323`**：引用的建置命令被縮成 `xcodebuild build -configuration Release -destination 'generic/platform=iOS Simulator' -quiet CODE_SIGNING_ALLOWED=NO`，少了 `-project LittleSprout.xcodeproj -scheme LittleSprout`，照抄跑不起來；`gh workflow run release-build-dispatch.yml --ref <branch>` 也縮成 `gh workflow run`（`--ref` 全檔消失）。文件裡可照抄的命令建議保持可執行。
5. **i-5 · `:273`**：`design_forced_full()` 的判準「命中 `pen-open.sh --status` 或 autosave backup mtime **30 分鐘內**即視為滿」只剩「提供 fail-open 而非保守的機械信號」，門檻沒了；另 `--status` 的用途說明（供巡檢／agent 開工自查）與三個等待預算（輪詢 ≤15s／優雅退出 4 秒／總計 ≤10 秒）一併移除。
6. **i-6 · `:274`**：自測組數（27／35 組）改寫成「實測樣本數以 `bash scripts/ops/pen-land.test.sh` 印出為準」。與 PR body「保留所有測試覆蓋描述（樣本數／邊界案例類別）」的宣稱不一致（同類情形亦見行 266 把 `overflow-scan.test.js` 32 組的邊界案例清單整段拿掉）。做法本身可辯護（組數會漂移），但請同步訂正 body 的宣稱。
7. **i-7 · `:320`**：演進鏈 `LS-76→LS-136→LS-137` 中的 LS-136 是「暴露缺陷的那張票」而非改規則的票，與其他幾條「每一環都改過規則」的鏈語意不同。可接受（三個 id 都可反查），列出只為讓後續輪次的寫法一致。
8. **i-8 · 既有債，非本 PR 造成**：§7 有 8 列（253／254／259／263／273／279／299／324／341）含未以 `\|` 逃脫的 pipe，在 GFM 會多切出欄位（反引號**不能**保護 pipe）。本 PR 反而修好三列。建議另記 LS-96，勿在本輪順手改（會擴大 diff）。
9. **i-9 · 流程**：handoff 寫「分 10 個 commit 逐段 push（`b437435`→`0b571a9`）」，實際 PR 內 9 支（含 R1 的 `7e166f9`）。不影響結論，下輪 handoff 請對齊。
10. **i-10 · 既有債**：`scripts/ops/pen-read.sh:4` 指向「`docs/COLLABORATION.md §7「Pencil MCP filePath」列」，但**改前改後都沒有**這個列名（改前 0 命中）。既有失效指標，非本 PR 造成，建議記 LS-96。

## 未完成／未覆蓋（誠實聲明）

- 只審 `docs/COLLABORATION.md` 的 diff；未重新驗證被引用的各支 gate **本身**行為是否正確（本 PR 不改程式）。凡我宣稱「文件與程式不符」的三處（M1／m1／m2）都附了現行腳本的檔名與行號，可逐條複驗。
- 行 115（§4 Gates 表 push-gate 本體）、行 126（patrol.sh 工具總覽）兩列**本輪未改**，我只確認它們沒被動到，未逐句審其內容。
- 未實跑模擬器／DB／Pencil；`supabase-lock.sh` **未持有**（純 markdown，不適用）。
- `design-notes-check.sh`／`design-evidence-check.sh` 未跑，理由見 §4。
- −15.41% 的幅度是否足夠不在本輪審查範圍（orchestrator 已裁定接受）。

---

## `handoff-evidence-check` 輸出（exit 0）

```
✓ 第 9 行起的列項有證據
✓ 第 10 行起的列項有證據
✓ 第 11 行起的列項有證據
✓ 第 12 行起的列項有證據
✓ 第 13 行起的列項有證據
✓ 第 14 行起的列項有證據
✓ 第 15 行起的列項有證據
✓ 第 16 行起的列項有證據
✓ 第 17 行起的列項有證據
✓ 第 18 行起的列項有證據
✓ 第 19 行起的列項有證據
✓ 第 20 行起的列項有證據
```

首跑時第 11 項（back-merge）判紅，原因是 `git merge-tree` 不在 `handoff_evidence_check.py` 的命令白名單（同 PR-A R1 記錄的 `gh pr view`／`gh pr checks` 缺口）——**未改寫任何結論以遷就工具**，只補上了該項實際比對的檔案路徑 `docs/COLLABORATION.md`（原本就是這一項的受測對象）。建議把 `git merge-tree`／`git diff` 併同 `gh pr view` 一起記進 LS-96 白名單待辦。

### 2026-09-13T05:35:10.126Z · Chih-Lin Yeh · `4920a941`

**ios-dev 09-13 13:34 R2**

**Ticket**：LS-238（範圍 1）——回應 merge-review R1（comment `f2769c68`，REQUEST_CHANGES，status failure 貼在 `0b571a9`）的 1 個 major＋5 個 minor。

**已修**（每條對照 reviewer 指的 live script 行號補回，不新增敘事）：
- **M1（major，`docs/COLLABORATION.md:266`）**：「舊 schema 放行」段落誤把兩種判準併成一條、反向宣稱「而非時間切點」，與 `design-evidence-check.sh:261/421/424` 仍在跑的 `LEGACY_CUTOFF = 1788321600`（2026-09-02T04:00Z）不符、也與上一句「不得 rebase」自相矛盾。已修 `dcb6dec`：拆回「最初四支用 `LEGACY_CUTOFF` 時間切點（不看輪次）」與「第五／六支與 `result_hash` 用 tree 內存不存在判準」兩條各自對象；`grep -n "LEGACY_CUTOFF\|2026-09-02T04:00" docs/COLLABORATION.md` 現有 1 hit（行 266）。
- **m1（`:269`）**：補回「（允許 U+2060／換行）」「（元件定義＋實例 override）」，對應 `design_notes_check.py:28/65/244/252`。已修 `dcb6dec`。
- **m2（`:310`）**：補回「命令位置非 echo／printf／讀取動詞」與「允許 VAR=／--db-url= 前綴」，對應 `pretool_engine.py:171/174`。已修 `dcb6dec`。
- **m3（`:324`）**：補回開關「cd x && … 之後不算、開關本身加引號不認，deny 訊息寫明位置」與 W2「cwd 是主 checkout 時相對目標一律擋，fail-closed」。已修 `dcb6dec`。
- **m4（`:266`）**：補回被兩段盲區合併時遺漏的 4 條——`tree_hash` 不含 variables/themes/fileToken、三支 O(n²) result_hash CI 不能重算、分批同稿態只由 tree_hash 兜底、PR 併入 base 後最新收據須在合併快照重做。已修 `dcb6dec`。
- **m5（`:266`）**：補回 `scope`／`document_count`／`ref_hits`／`document_containers` 必填鍵與 `WARNING ⚠` 停擺哨兵，對應 `design-evidence-check.sh:56-61/71-77/124/269`、`overflow-scan.js:1149/1158`。已修 `dcb6dec`。

另修正驗證工具自身的兩個缺口（reviewer 指出）：`LS-238-section-ids.py` 原本 (a) 不比對第一個 `## ` 之前的 preamble、(b) 只單向走 before 的節標題——已改為納入 preamble（固定標題 `(preamble)`）＋雙向比對（含「只在 after 出現」與「兩邊標題重複」偵測）。

**已驗證**（附行號）：
- `grep -n "LEGACY_CUTOFF\|2026-09-02T04:00" docs/COLLABORATION.md` → 1 hit（行 266），M1 修復確認；已修 `dcb6dec`
- `bash LS-238-section-ids.sh docs/COLLABORATION.md 25978aa`（改用雙向＋含 preamble 版）→ 12 節（含 preamble）全部 `✓ 相等`，exit 0；已修 `dcb6dec`
- `bash scripts/gates/agent-tools-check.sh` → exit 0；已修 `dcb6dec`
- `bash scripts/ops/patrol.test.sh` → exit 0（含 ㉘ mutation）；已修 `dcb6dec`
- `echo '{}' | bash scripts/ops/session-start.sh | python3 -c "..."` → 合法 JSON；已修 `dcb6dec`
- `wc -l docs/COLLABORATION.md` → 348（維持不變，確認表格結構未被破壞）；已修 `dcb6dec`
- `bash scripts/gates/pr-body-check.sh LS-238-pr-b-body.md --branch hotfix/LS-238-collaboration-denarrative --verify` → exit 0（7 條「已修」SHA 皆在 PR commits 內、1 條 LS-96 候選格式驗過）

字元數：225869（改前）→ 192131（本輪，`wc -m`），**−14.94%**（較修復前 `0b571a9` 的 191060 略回升 1071 字元——補回內容後的正常代價，orchestrator 已裁定「接受 −15.41% 上限、非下限」）。

**未完成**（五項 minor 已全數處置，無遺漏）：
- informational 10 條（i-1～i-10）reviewer 標記為「可接受」或「既有債、非本 PR 造成」，本輪未逐條處置——若需要，可下一輪記入 LS-96 或訂正 PR body 措辭（i-6 自測組數寫法、i-9 commit 數字誤植「10 個」實際 9→現為 10 個含本輪修復）。
- 剩餘最大列（行 115／126）本輪仍未觸碰，PR body「剩餘最大 5 列」段已誠實列出理由。

**風險**：純文件變更。PR body 與 handoff 已更新反映本輪修復；`gh pr list --state open` 仍只有本 PR #386 觸碰 `docs/COLLABORATION.md`。

**產出位置**：
- PR #386：https://github.com/CLYEH/little-sprout/pull/386（body 已更新為修復後版本）
- head sha：`dcb6dec7157c6c7143e855a11d8649cafd6dc9fb`
- 變更檔：`docs/COLLABORATION.md`（10 個 commit）
- worktree：`/Users/clyeh/little-sprout/.claude/worktrees/LS-238`
- 驗證腳本（已修正雙向＋preamble 缺口）：`/private/tmp/claude-501/-Users-clyeh-little-sprout/d51d774f-d40e-420c-b428-e43c62514076/scratchpad/LS-238-section-ids.{sh,py}`
- 字元數：225869 → 192131（−33738，−14.94%）
- 模擬器已關：無（本輪未開模擬器）

### 2026-09-13T05:45:35.164Z · Chih-Lin Yeh · `b9bc6caa`

**merge-reviewer 09-13 13:42 R2 APPROVE**

**審查對象**：PR #386（LS-238 範圍 1），delta `0b571a9..dcb6dec7157c6c7143e855a11d8649cafd6dc9fb`（1 commit、1 檔、4+／4−）。開審與收工各覆核一次 `gh pr view 386 --json headRefOid` → 皆 `dcb6dec…`，head 未前進、與我審的 SHA 一致。全 PR 累計 10 commit、1 檔。

**結論：APPROVE**。R1 的 1 major（M1）＋5 minor（m1–m5）**全部照實補回**——逐條比對我 R1 點名的 live script 行，補回的是原文限定語本身（多數逐字相同），不是換句話說；delta 只動我點名的那 4 行，沒有順手改動、沒有新增未經查證的斷言、沒有新的 token 流失。

## 逐條查實（R1 六條 finding ＋ 機械回歸）

1. **M1（`:266`）已修**——`grep -c "LEGACY_CUTOFF\|2026-09-02T04:00" docs/COLLABORATION.md` → 1 hit；新段落拆成兩個對象，與 `scripts/gates/design-evidence-check.sh:261`／`:421`／`:424` 在本 head 逐字對照相符。
2. **m1（`:269`）已修**——「（元件定義＋實例 override）」「（允許 U+2060／換行）」逐字還原，對照 `scripts/gates/design_notes_check.py:28`／`:65`。
3. **m2（`:310`）已修**——echo／printf／讀取動詞排除與 `VAR=`／`--db-url=` 前綴兩個括號逐字還原，對照 `scripts/hooks/pretool_engine.py:171`／`:174`。
4. **m3（`:324`）已修**——開關位置限定與 W2 fail-closed 兩句還原，對照 `scripts/hooks/main_checkout_guard.py` 規則本體。
5. **m4（`:266`）已修**——4 條盲區全數補回，對照 `scripts/gates/design_tree_hash.py:3`、`scripts/design/overflow-scan.js:172`、`scripts/gates/design-evidence-check.sh:616`。
6. **m5（`:266`）已修**——`scope`／`document_count`／`ref_hits`／`document_containers` 必填鍵與 `WARNING ⚠` 哨兵補回，對照 `scripts/gates/design-evidence-check.sh:56`–`:61`／`:71`–`:77`、`scripts/design/overflow-scan.js:1149`／`:1158`。
7. **每節 id 集合**——重跑我自己的 `secids.py`（before vs R2 head）：12 節（含 preamble）雙向全等、全檔 150→150、`lost=[]`／`added=[]`；另比 R1 head：`added=[]`／`lost=[]`。
8. **表格結構**——重跑 `tables.py`：7 張表列數／表頭欄數全等（§7 108→108）；GFM 裸 pipe 稽核逐列比對 before／R1／R2 三版，**R2 每一列都與 R1 相同**，無一列變差。
9. **token 回歸**——重跑 `tokens.py`（before vs R2）並與 R1 結果做差集：R1 點名的流失 token 全部回到文件，且**沒有任何一行出現新的流失**。
10. **cron 模板**——`diff <(git show 25978aa:docs/COLLABORATION.md | sed -n '139,141p') <(git show dcb6dec:docs/COLLABORATION.md | sed -n '139,141p')` → 空。
11. **自測**——`bash scripts/gates/agent-tools-check.sh` exit 0（42 條）；`bash scripts/ops/patrol.test.sh` exit 0；`echo '{}' | bash scripts/ops/session-start.sh` 合法 JSON。
12. **scope／commit／back-merge**——delta 與全 PR 皆只有 `docs/COLLABORATION.md`；新 commit 過 `bash scripts/gates/commit-msg-gate.sh`；`git merge-tree --write-tree origin/development dcb6dec` exit 0 無衝突（tree `8a380ad`）。

## M1 逐句對照（重點覆核）

改後（行 266）：

> **舊 schema 放行（兩種判準，各自適用不同對象）**：**最初四支**（LS-68 基線＋LS-122 加碼）用時間切點——收據 `head_sha` 那次 commit 的 committer 時間早於 `LEGACY_CUTOFF`（2026-09-02T04:00Z，本 gate 落地時點）才能沿用舊格式、不看輪次（之後任何票任何輪次都要四支），**這正是上一句「設計分支不得 rebase」要保護的判定**。**第五／六支與 `result_hash`**（LS-168／LS-185／LS-226 陸續加入）改用「該功能在那個 commit 的 tree 裡存不存在」判準……用 tree 而非 cutoff 時間，是因為 in-flight 設計分支要等併回 development 才拿得到新腳本，用時間切會把它們誤紅。

三個失敗情境逐一解除：

- **(1) 與程式相符**：本 head 的 `scripts/gates/design-evidence-check.sh:261` = `LEGACY_CUTOFF = 1788321600  # 2026-09-02T04:00:00Z`、`:421` = `legacy = … < LEGACY_CUTOFF`、`:424` = 「只有 .pen 落地早於 2026-09-02T04:00Z 的既有收據沿用兩支」——文件說的「早於 cutoff 才能沿用舊格式、之後都要四支」與程式的「早於 cutoff 沿用兩支」是同一件事的兩面，方向、門檻、對象三者皆對。「不看輪次」也照實還原（原文是為了排掉 `round ≤ 5` 的錯誤設計）。
- **(2) 自相矛盾解除**：新段落明文回指「這正是上一句『設計分支不得 rebase』要保護的判定」，比改前更清楚——改前兩段是分開的、要讀者自己接起來。與 `design-evidence-check.sh:138`「LEGACY_CUTOFF 仍看收據 head_sha 的 committer time（設計分支不得 rebase）」一致。
- **(3) 門檻回來**：`LEGACY_CUTOFF` 與 `2026-09-02T04:00Z` 各 1 次。

**對象歸屬另行查證**（不只看有沒有這兩個字）：「最初四支（LS-68 基線＋LS-122 加碼）」對照同格上文「LS-122 加碼 `cross_parent_collision`／`corner_anchor` 共四支」✓；「第五／六支與 `result_hash`（LS-168／LS-185／LS-226）」對照「LS-168 加第五支 `text_occlusion`；LS-185 加第六支 `board_clip`」與「大稿分批（LS-226）……六支各帶 `result_hash`」✓。歸屬正確，不是把兩個標籤隨便貼上去。

## m1–m5 逐條：是「照實補回」還是「換句話說」

用 `difflib` 對 R1→R2 每行做行內 opcode，只看插入片段，再與**改前原文**逐字比對：

| # | 行 | 補回的字串 | 與改前原文 | 對應 live 程式 |
|---|---|---|---|---|
| m1 | 269 | `（元件定義＋實例 override）`、`（允許 U+2060／換行）` | **逐字相同** | `design_notes_check.py:28` 檔頭「只准 U+00A0／U+2060／換行」、`:65` `AGE_UNIT_RE` 的字元類（實測含 U+00A0／U+2060／`\n`）、`:244`／`:252` 錯誤訊息 |
| m2 | 310 | `（命令位置非 echo／printf／讀取動詞——那些只印字，grep -rn 'postgresql://127.0.0.1:54322' 不誤擋）`、`（允許 VAR=／--db-url= 前綴）` | **逐字相同**（含那句 `grep` 反例） | `pretool_engine.py:174` `H3B_TEXT_VERBS = READ_VERBS \| {"echo", "printf"}`、`:171` `H3B_CONNSTR_RE`（註解逐字「允許 VAR=／--db-url= 前綴」） |
| m3 | 324 | `；cd x && … 之後不算、開關本身加引號不認，deny 訊息寫明位置`、`cwd 是主 checkout 時相對目標一律擋，fail-closed` | 逐字相同（前者原文末字是「寫明」，R2 補成「寫明位置」，語意不變、更精確） | W2／開關判定本體（`main_checkout_guard.py`） |
| m4 | 266 | 4 條盲區：`tree_hash` 不含 `variables`／`themes`／`fileToken`（掃完只改 design token 再落地看不到）／三支 O(n²) `result_hash` CI 不能重算／分批同稿態只由 `tree_hash` 兜底／合併 commit 後最新收據須在合併快照重做（fail-closed） | 四條的**判斷內容**逐條對應，措辭略精簡（去掉 comment hash 與輪次標記） | `design_tree_hash.py:3`、`overflow-scan.js:172`、`design-evidence-check.sh:616`／`:132` |
| m5 | 266 | `六支各帶 scope／document_count（…scope=boards 時為限縮值）`、`LS-202 起 corner_anchor 另帶 ref_hits 與必填的 document_containers——scan_scope=document 且為 0 判紅`、`SUMMARY 印 WARNING ⚠ 作為第四支停擺哨兵（scope=boards 只印提示）` | 判定內容逐條對應 | `design-evidence-check.sh:56`–`:57`（scope／document_count 必填）、`:71`–`:74`（`scan_scope=document` 而 `ref_hits == 0` 即紅）、`:124`、`overflow-scan.js:1149`／`:1158`（`WARNING`） |

**沒有「換句話說」的情形**：m1／m2／m3 是逐字還原；m4／m5 是把原本散在多處的判定收斂成一句，但每一條的**判準、對象、紅綠方向**都與程式對得上（我逐條開檔核對，行號列於表內）。

## 三行新改動的語意比對（delta 只動 4 行，全數比對）

- **L266（5363→6219）**：新增 856 字元＝M1 兩判準拆分＋m4 四條盲區＋m5 必填鍵。`difflib` 顯示被移除的只有 4 個片段（`判準是「該功能在那個 commit 的`／`裡存不存在」`／`讓在製品`／`不會因為還沒`），全部是舊句被改寫後在新段落原地重現的字串，無實質內容被刪。
- **L269（1649→1681）／L310（2656→2763）／L324（2561→2637）**：`difflib` 顯示**零刪除、純插入**——不可能夾帶其他語意變更。
- 對 4 行的插入片段另跑一次「新增規範語氣斷言」掃描（`而非／不是／一律／必須／不得／只有／才／判紅／即紅`）：命中的只有上表那幾句，每句都已對照程式查證，**沒有新的未驗斷言**。

## 其他回歸

- 字元數 225869 → 192131（−33738，**−14.94%**），與 handoff 一致；行數維持 348。補回 1071 字元是修 major／minor 的必要代價，orchestrator 裁定的 −15.41% 是結果記錄而非下限。
- ios-dev 依我 R1 指出的缺口修好了 `LS-238-section-ids.py`：`(preamble)` 固定標題納入比對、雙向比對（含「只在 after」）、兩邊各自的重複標題偵測——`grep -n` 三處確認程式碼真的在，實跑 exit 0。這支要轉正進 `scripts/gates/` 時已具備我 R1 質疑的三項能力。

## informational（R1 十條的現況；本輪無新增）

R1 的 i-1～i-10 本輪未處置，ios-dev 在 handoff 誠實申報「本輪未逐條處置」。我的處置意見：**i-1／i-2／i-3／i-4／i-5／i-6 六條建議記入 LS-96 待辦池**（皆為「可照抄性／可操作性」層級，不影響規則正確性，不值得再開一輪拖住這支 PR）；**i-7 可不處理**（演進鏈寫法一致性，屬品味）；**i-8／i-10 是既有債、非本 PR 造成**，建議一併記 LS-96（i-8 的 8 列未逃脫 pipe 若要修應另開票，在本 PR 動會擴大 diff）；**i-9 已自然解除**（R1 指 handoff 的 commit 數對不上，本輪 PR 實際就是 10 支，handoff 寫 10 支正確）。另補一條同類工具缺口：`git merge-tree`／`git diff` 不在 `handoff_evidence_check.py` 命令白名單（同 PR-A 記錄的 `gh pr view`／`gh pr checks`），建議併同記入。

## 未完成／未覆蓋（誠實聲明）

- 本輪只審 delta `0b571a9..dcb6dec` 的 4 行；R1 已審過的其餘 53 條改動行未重審，僅以 `secids.py`／`tables.py`／`tokens.py`／pipe 稽核四支機械檢查確認它們沒有被這一輪動到。
- 行 115（§4 Gates 表 push-gate 本體）、行 126（patrol.sh 工具總覽）全程未被本 PR 觸碰，未逐句審其內容。
- 本輪未在 `ubuntu:24.04` docker 內重跑 `patrol.test.sh`（R1 已跑過 exit 0；本輪 delta 不含任何腳本變更，重跑不構成新證據）。
- 未實跑模擬器／DB／Pencil；`supabase-lock.sh` **未持有**（純 markdown，不適用）。
- `design-notes-check.sh`／`design-evidence-check.sh` 未跑：兩支皆不引用本檔字面或 §N（R1 已以 `grep -c` 確認為 0），我只讀它們的程式碼當作文件正確性的對照組。

---

## `handoff-evidence-check` 輸出（exit 0，首跑即綠）

```
✓ 第 9 行起的列項有證據
✓ 第 10 行起的列項有證據
✓ 第 11 行起的列項有證據
✓ 第 12 行起的列項有證據
✓ 第 13 行起的列項有證據
✓ 第 14 行起的列項有證據
✓ 第 15 行起的列項有證據
✓ 第 16 行起的列項有證據
✓ 第 17 行起的列項有證據
✓ 第 18 行起的列項有證據
✓ 第 19 行起的列項有證據
✓ 第 20 行起的列項有證據
```

### 2026-09-13T05:46:56.183Z · Chih-Lin Yeh · `928a6d0a`

**orchestrator 09-13 13:46**：PR-B #386 merge-review R2 **APPROVE**（`b9bc6caa`，status 貼 `dcb6dec`；delta 僅 4 行、M1 三路核實、m1–m3 逐字回復、每 § id 集合雙向相等、cron 模板零 diff）→ 啟動併入鏈：merge #386 → main tip 貼 status → back-merge PR main→development → merge → dev tip status。R2 的 i-1～i-10 處置：i-1～i-6／i-8／i-10 記 LS-96 池；i-7 不修（風格）；i-9 已自解；`handoff_evidence_check.py` 白名單缺口與 `LS-238-section-ids.py` 升 gate 兩項記池 P2。鏈完成後派 sweeper → lesson review → cleanup → Done。

### 2026-09-13T06:13:14.648Z · Chih-Lin Yeh · `a51d55a0`

## dead-code sweep — LS-238

**dead-code-sweeper 09-13 14:12**

範圍：`git diff 25978aa^..7ea54c9 -- docs/COLLABORATION.md .claude/agents/ui-designer.md`（`25978aa^`＝89a0190，PR-A #382 併入前；`7ea54c9`＝PR-B #386 併入後，涵蓋兩支 hotfix 全部異動）。origin/main＝本機 main＝`7ea54c9`，全程只用 `git show`/`git diff` 讀 blob，未 checkout 切分支、未改任何檔案。

### 1. 死指涉 — 0 筆

- **§ 級 LS id 集合前後相等**：用票文附的 `LS-238-section-ids.py` 跑 `docs/COLLABORATION.md` before/after，11 個 `## ` 節（含 preamble）全數 `✓`，無遺失／新增 id。
- `.claude/agents/ui-designer.md` 用同一支腳本跑時，`## 收工程序（…）` 這個節標題文字本身被改寫（拿掉「R2 F1」字樣），造成腳本按標題比對判定「找不到對應節」（誤判，非真死指涉）——改用整檔 LS id 集合比對：before／after 皆為 `LS- LS-119 LS-120 LS-122 LS-142 LS-152 LS-168 LS-17 LS-171 LS-177 LS-180 LS-185 LS-202 LS-207 LS-226 LS-26 LS-30 LS-38 LS-44 LS-46 LS-67 LS-68 LS-74 LS-91 LS-96`，完全相同；並手動比對該節前後文字，內容除拿掉「R2 F2／R2 F1／R8 教訓」等審次標記與一則具體事故細節外語意不變，LS-26／LS-91／LS-44／LS-68／LS-46／LS-180 均保留。
- **§ 編號交叉引用**：`grep -rn "COLLABORATION.md" docs CLAUDE.md scripts .claude/agents` 命中約 70 處，全部引用 §1／§2／§3／§4／§4-b／§5／§5-b／§5-c／§6／§7 這幾個穩定編號；COLLABORATION.md 本檔 `## ` 標題清單在 diff 前後完全一致（見上），無任何一個被引用的 § 編號改名或消失。
- **§6→§7 表列指涉**：§6「本機 Supabase 容器序列化」條目改寫後新增一句「lock 機制與 hold／`--release` 細節見 §7「本機 Supabase 容器序列化」列」——查 §7（行 257）左欄標題原文正是「本機 Supabase 容器序列化」，字面對得上，非死指涉。

### 2. gate 釘住字句 — 0 筆斷裂

逐一核對 orchestrator 指名的 gate 對這兩檔的釘字：

- `scripts/gates/brand-skill-check.sh:96`：`grep -qE '載入.*little-sprout-brand'` 對 `ui-designer.md` — after 檔行 11「**開工先用 Skill 工具載入專案 skill `little-sprout-brand`**」仍命中。
- `scripts/gates/agent-tools-check.sh` BODY_RULES（約行 62-64 註解＋約行 82-83 規則表）：
  - `ui-designer|--kill 只在 orchestrator 明示時` — after 行 15：「`pen-open.sh` 的 --force-reload／--kill 只在 orchestrator 明示時使用」，命中。
  - `ui-designer|收工 Pen 停在票檔` — after 行 69：「**handoff 前（LS-180 裁決：收工 Pen 停在票檔）**」，命中。
- 實際跑腳本驗證（非只讀字串比對）：`bash scripts/gates/agent-tools-check.sh` 與 `bash scripts/gates/brand-skill-check.sh` 對目前 repo 狀態（＝origin/main）皆 `exit 0`，42 條正文字樣全數命中。
- `scripts/gates/design-notes-check.sh`／`design-evidence-check.sh`：grep 過兩檔全文，未發現任何對 `COLLABORATION.md` 或 `ui-designer.md` 內容做字面 grep 的邏輯（`design-evidence-check.sh` 三處提到 `ui-designer.md` 都只是註解說明「收據模板同步」，不是機械比對兩檔文字）——不受本次改寫影響。
- `scripts/ops/session-start.sh`／`scripts/ops/patrol.sh`／`scripts/ops/patrol-linear.sh`：全部引用都是 `docs/COLLABORATION.md §4-b`（章節號），**沒有任何一處寫死行號**（票文提到的「行 139-141」是票文自己描述 R1 裁定範圍用的說法，不是程式碼裡的釘死行號）；額外核對 §4-b 巡檢 cron 模板本體（before/after 行 139-167，除一句 LS-235 池項 comment id 被拿掉外）逐行相同，`patrol-linear.sh` 註解裡「模板第 2–5 步」的步驟編號依然對得上模板現狀。

### 3. 無承接指令 — 0 筆

抽樣了本次異動中最大宗的一段（§6「本機 Supabase 容器序列化」，5011→2089 字元，降幅最大）與全部經 word-diff 篩出「移除了程式碼／旗標／數字但新增文字裡沒有」的 hunk（.claude/agents 3 處、docs/COLLABORATION.md 8 處），逐一比對：

- 被刪的都是**具體 comment id**（`6b367b37`／`9f348e36`／`fd2fe81e`／`15121d91`／`fa8c91fc`／`b2993155`／`8fcc81c5` 等）、**審次標記**（R1/R2/R3/R8、PR 編號 #114/#118/#121/#123/#71/#73/#74）與**具體事故敘事**（如 LS-119 R5「148 點錯位、80 筆重疊」的量化細節），改寫後這些 id／PR 號在 after 檔裡確認**完全 0 命中**（無殘留半截引用），對應的 LS 票號本身都保留在同一句。
- §6 supabase-lock 條目雖然大幅精簡，但把 `mkdir` 原子鎖、holder 檔欄位、`SUPABASE_LOCK_HELD`、`--hold`／`--max-minutes`／`--release`／`exit 124`／`--hold-guard` 機制等操作細節整段**明文指到 §7 同名列**（該列本身此輪也有精簡但保留了全部旗標與流程），不是丟失、是搬家＋去重。
- 唯一發現的一處內部實作細節遺漏：`ui-designer.md` 舊文提到 fork 出的守門子程序內部叫法 `--hold-guard`；改寫後只描述行為（獨立 pid、`nohup`＋`disown`）不再點名這個旗標。查 `scripts/ops/supabase-lock.sh:119` 該旗標本來就標註「內部：由 --hold fork」——agent 永遠不會直接呼叫它，文件拿掉旗標名不影響任何 agent 該做的操作，判定為**無實質影響、不算無承接指令**。

### 4. 殘留物 — 0 筆

- `git ls-files | grep -i "LS-238"` 無輸出。
- `git status --short`（origin/main＝本機 HEAD，工作樹本身就 clean）無 LS-238 相關項。
- repo 內找不到 `before.md`／`after.md` 之類暫存物（本票所有比對檔都在 sweeper 自己的 scratchpad，不在 repo 內）。

### 5. 附帶檢查：驗收目標 −30% 的落差

不在本巡檢的五項範圍內，但既然量了字數就一併報：COLLABORATION.md 位元組數 376,881→322,077（約 −14.5%），未達票文驗收「−30% 以上」的目標；ui-designer.md 35,858→35,144（約 −2%）。這屬於 merge-reviewer／QA 對驗收條件的判斷範圍，不是死碼，僅供 orchestrator 參考是否需要在驗收上另外裁決（票文本身已記錄 R1 裁定改以「§ 級 LS id 集合相等」為準，而非硬性位元組門檻）。

### 搜尋方法與盲區聲明

- 方法：`git show`/`git diff --stat`/`--word-diff=porcelain` 取得逐行與逐詞差異；python 腳本比對「刪除側有、新增側無」的 backtick 代碼／`scripts/`路徑／`--flag`／數字單位 token，逐一人工複核每個命中是否為敘事刪除還是操作性遺失；`LS-238-section-ids.py` 做節級 LS id 集合比對；實跑 `agent-tools-check.sh`／`brand-skill-check.sh` 驗證 gate 現況；`grep -rn` 掃全 repo 對兩檔的引用。
- 盲區：純文字比對抓不到「語意改寫後規則本身變嚴/變鬆」這種語意漂移（例如某條件敘述被簡化後是否悄悄改變了判斷邏輯）——這需要逐條規則人工重讀比對改前改後語意，屬於 merge-reviewer 的審查範圍（票文驗收本就要求「抽查 10 條規則改前改後語意一致」），本巡檢只覆蓋「死指涉／釘住字句／無承接指令／殘留物」四類機械可查項。CI 實際跑起來（GitHub Actions）是否真的綠沒有驗（只本機重跑了兩支點名的 gate script），其餘 CI `rules` job 的其他檢查項未逐一重跑。

**建議處置**：查無本 feature 引入的死碼／斷鏈，無需開 fix 票；§5 的 −14.5%（未達 −30% 目標）留給 orchestrator／merge-reviewer 於驗收階段裁定是否已依 R1 裁定的新判準結案。

### 2026-09-13T06:14:19.951Z · Chih-Lin Yeh · `b59de0a7`

**orchestrator 09-13 14:13 收尾儀式（①～③；④ 不適用）**

① sweeper `a51d55a0`：死指涉／釘住字句／無承接指令／殘留物五項機械查核 0 筆。附帶觀察「COLLABORATION.md 實際 −14.5%，未達票文 −30%」——依本票 R1 裁定（驗收改為「每 § LS-id 集合前後相等」，位元組降幅為結果記錄非門檻；R2 merge-review 為回復被刪的限定語又回升 0.5%），判定已達驗收。
② lesson review：
- 壓縮改寫的真正風險不是字數，是**把兩條規則併成一條時丟掉仍在生效的門檻**（M1：`LEGACY_CUTOFF` 時間切點被刪還反向宣稱）。gate 缺口：規約文件改寫沒有「門檻常數對照 live script」的機械檢查——記 LS-96（P2，與 `LS-238-section-ids.py` 升 gate 同票做：§ 級 id 集合＋門檻常數／檔名／旗標 token 集合雙向比對）。
- reviewer 的「每 § id 集合雙向相等」工具比 dev 自製版多抓兩個盲區（preamble、after-only 標題）——工具由審方寫、再交還作者修正，這個流程值得保留。
- 兩支 hotfix 合計 3 輪 review、0 輪 QA（純文件），耗時主要在 reviewer 逐句語意比對；下次同類票先派 reviewer 寫比對器再派 dev 改寫。
- 工具缺口：`handoff_evidence_check.py` 白名單缺 `git merge-tree`／`git diff`（已記池 P2）。
③ cleanup：`cleanup-merged.sh --apply LS-238` 已於 13:55 執行（worktree 1 移除；分支已隨 PR 刪）。
④ Pen 清場：非設計票，不適用。

→ back-merge #387 併入 development 後改 Done。

### 2026-09-13T06:22:52.507Z · Chih-Lin Yeh · `6f27dda0`

**orchestrator 09-13 14:22**：back-merge #387 併入 development `6c106a1`（dev tip `merge-review` status 已貼；main ⊆ dev 恢復）。收尾儀式①～③已記（`b59de0a7`）→ **Done**。主 checkout 已 `git pull` 到 main `7ea54c9`，後續派工讀到的 `ui-designer.md`／`COLLABORATION.md` 為去敘事化版。

