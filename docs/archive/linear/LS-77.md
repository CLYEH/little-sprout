# LS-77 Harness：開票結構 gate——open 票必有 project；Phase 票必有 milestone；Task 必有 parent（巡檢列出＋Spec guard 擋）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Low |
| 標籤 | size:M, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-25T04:46:53.644Z |
| 開始 | 2026-09-01T18:41:05.545Z |
| 完成 | 2026-09-01T19:22:43.899Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-77/harness開票結構-gate-open-票必有-projectphase-票必有-milestonetask-必有 |
| 關係 | related→LS-44, related→LS-104, related→LS-96, related→LS-8, related→LS-103, related→LS-75, related→LS-30, related→LS-48, related→LS-66, related→LS-46, related→LS-76, related←LS-198, related←LS-88, related←LS-70, related←LS-79 |

## 描述

## 來源

2026-08-25 使用者問「epic／story／feature／task 結構有在遵守嗎」。盤點：COLLABORATION §3 規定 Project＝epic、Milestone＝feature 群、Issue＝story、Sub-issue＝task；但 [LS-46](https://linear.app/little-sprout-app/issue/LS-46/設計定稿合併c-基準使用者指定調整-designlittlesproutpen) 起 orchestrator 自開的 31 張票（[LS-46](https://linear.app/little-sprout-app/issue/LS-46/設計定稿合併c-基準使用者指定調整-designlittlesproutpen)～[LS-76](https://linear.app/little-sprout-app/issue/LS-76/harnesspush-gate-對無-swift專案檔變更的-pr-跳過-xcodebuild-test純文件-harness-pr) 除 [LS-30](https://linear.app/little-sprout-app/issue/LS-30/harnesslittle-sprout-brand-專案-skill設計語言定案後)／44）全無 project，Phase 1 的 Task 票（[LS-48](https://linear.app/little-sprout-app/issue/LS-48/taskls-21-後端-diaries-schema-補全時間軸查詢-rpcrls測試)／58／66）無 milestone，[LS-66](https://linear.app/little-sprout-app/issue/LS-66/taskls-47-後端-children-多寶貝-crud-rpc軟刪-30-天可還原角色矩陣測試)／67／72／55／62／64 無 parent。**規則無 gate → 沒被遵守**（違反「前饋必有反饋」）。當天已人工回填。使用者追問「有辦法做 hook 或其他機械式規約嗎」→ 有：Claude Code PreToolUse hook 可以 match MCP 工具名。

## 驗收條件

1. **前饋 gate（主要）**：`.claude/settings.json` 加 PreToolUse hook，matcher `mcp__linear__save_issue` → `scripts/gates/linear-issue-check.sh`（讀 stdin JSON 的 `tool_input`）。規則只對「建票」（無 `id`）生效：
   * 缺 `project` → deny；
   * `project` 以 `Phase` 開頭且缺 `milestone` → deny；
   * `title` 以 `Task：` 開頭且缺 `parentId` → deny；
   * `labels` 無 `lane:*` 或多於一個 → deny（[LS-75](https://linear.app/little-sprout-app/issue/LS-75/harnesslane-wip-上限backlogready-機械規則巡檢每輪補位票文-lane-標籤blockedby-關係) 規則一併機械化）；
   * deny 時 `hookSpecificOutput.permissionDecision: "deny"`＋`permissionDecisionReason` 指向 §3。更新既有票（有 `id`）不擋，避免改狀態被誤攔。
   * 正負自測 `linear-issue-check.test.sh`（建票缺 project／Phase 缺 milestone／Task 缺 parent／無 lane／合規 → 對應結果；更新票一律放行），掛 CI `rules` job。
   * 依 update-config skill 流程 pipe-test；hook 需 `/hooks` 或重啟才載入（PR body 註明）。
2. **反饋（backstop，補 Linear UI 手動建票的盲區）**：巡檢 prompt（COLLABORATION §4-b 模板）加一步：`list_issues` 全 open 票 → 列出 (a) 無 project、(b) project＝Phase-* 但無 milestone、(c) 標題以「Task：」開頭但無 parentId 的票。三類任一非空即列表，不得「無異常」。
3. Spec guard（§5-b）：Backlog→Ready 補位時，票缺 project／（Phase 票）milestone 者標「待結構」不派，與「待 Spec」並列。
4. COLLABORATION §3「開票結構」加一句：orchestrator `save_issue` 開票必帶 `project`（＋Phase 票 `milestone`；Task 票 `parentId`；一個 `lane:*`），§7 對照表加一列（前饋＝§3 開票結構；反饋＝hook 1＋巡檢 2＋Spec guard 3）。
5. PR body 附一次巡檢實跑輸出證明三類列表為空＋hook pipe-test 證據。

## 盲區（明寫進 §7）

使用者在 Linear UI 手動建票不經 hook → 靠巡檢步驟 2 補抓。

## 分支

hotfix 從 main 切，PR 回 main 後 back-merge。

## 留言（4）

### 2026-09-01T18:54:31.242Z · Chih-Lin Yeh · `9a1bcf51`

Ticket：LS-77

已完成（含對帳表）：
- **驗收 1（PreToolUse gate，新機制）**：`.claude/settings.json` 新增 matcher `mcp__linear__save_issue` → `scripts/gates/linear-issue-check.sh`（讀 stdin hook JSON 的 `tool_input`）。只在建票（無 `id`）時套四條規則：缺 `project` deny／`project` 以 `Phase` 開頭缺 `milestone` deny／`title` 以「Task：」「Task:」開頭缺 `parentId` deny／`labels` 內 `lane:*` 數 ≠ 1 deny；更新票一律放行。fail-closed（空 stdin／JSON 壞／jq 與 python3 皆缺／腳本中途未預期中止）比照 `pretool.sh` 既有 `trap on_exit EXIT` 慣例；reason 只放靜態文字不回顯欄位值。自測 `scripts/gates/linear-issue-check.test.sh`（28 組：七情境＋fail-closed 三態＋jq 缺退 python3 備援＋四條 deny 規則各一個 mutant 負控）掛進 CI `rules` job（`.github/workflows/ci.yml`）。
- **驗收 2／3（對帳結果：已由 LS-103 落地，本票補文件點名）**：巡檢 backstop（`patrol_linear.py` 的 `ticket_structure()`，開票結構 (a)-(e)）與 Spec guard（`classify_candidate()` 判定 `'structure'`、`lane_pending()` 標「待結構」，Backlog→Ready 只挑 `'ok'` 候補）在 LS-103 已機械落地且與票文規則字面一致，只是 `docs/COLLABORATION.md` 未點名——本票補：§3 加一句「`save_issue` 開票必帶 project／milestone／parentId／lane:*」；§5-b Backlog→Ready 規則補「待結構」與「待 Spec」並列；§7 新增一列合併記錄前饋（新 hook）＋反饋（既有巡檢＋Spec guard）。
- **驗收 4**：同上 §3／§7 更新，一併完成。
- **驗收 5**：見下方已驗證。

已驗證（怎麼驗的）：
- `bash scripts/gates/linear-issue-check.test.sh` 全綠（28 組：六情境如票文列＋labels 過多＋fail-closed 三態＋jq 缺退 python3＋四條規則各一個 mutant，mutant 反向確認規則區塊確實從 mutant 腳本移除）。
- pipe-test（比照 update-config skill 慣例）：`echo '{"tool_name":"mcp__linear__save_issue","tool_input":{"title":"Foo","team":"LS","labels":["lane:harness"]}}' | bash scripts/gates/linear-issue-check.sh` → deny（exit 2，reason 指向 §3）；合規建票 payload → allow（exit 0 無輸出）；`{"id":"LS-77", ...}` 更新票 payload → allow。
- 驗收 5 巡檢實跑（read-only、未真的呼叫 save_issue）：`bash scripts/ops/patrol-linear.sh` 第 4 段「開票結構」實際輸出：`(a) 無 project：無`／`(b) Phase 專案缺 milestone：LS-8`／`(c) Task：標題缺 parent：無`。(a)(c) 為空；(b) 命中 `LS-8`（Project「Phase 0 — 開發地基」，Backlog，既有舊票、非本 PR 新開）——如實列出，非本票範圍（不回填既有 backlog），`lane:product` 本就不進自動補位，留給 orchestrator 決定是否處理。
- 回歸：`bash scripts/hooks/pretool.test.sh` 全綠（既有 Bash/Read/Grep hook 未受影響）；`python3 -m json.tool .claude/settings.json` 合法；`.github/workflows/ci.yml` YAML 解析合法；push gate 通過（無 Swift 變更，SwiftLint／unit tests 依規則跳過，CI 仍全跑）。

未完成／剩餘（reviewer informational 處置）：本票尚未經 merge-reviewer 審查，目前無 informational 待處置；若 review 後有 informational 會逐條處置並更新本欄。

風險與已知問題：
- 無破壞性變更、無 migration。新 hook 只影響「建票」路徑，更新票流程完全不受影響；既有 `Bash|Read|Grep` matcher 未改動。
- **hook 需 `/hooks` 或重啟 session 才會載入新 matcher**——本 PR 併入後，orchestrator 需執行 `/hooks` 讓 `mcp__linear__save_issue` matcher 生效。
- 巡檢發現 LS-8（Phase 0 專案）缺 milestone，屬既有 backlog 資料品質問題，非本票範圍，留給 orchestrator 判斷是否處理。
- 盲區（同 §7 新列）：使用者在 Linear UI 手動建票不經此 hook，仍靠巡檢 backstop 補抓。

產出位置：
- branch：`hotfix/LS-77-issue-structure-gate`
- PR：https://github.com/CLYEH/little-sprout/pull/214（head `d4e1efe`）
- 新檔：`scripts/gates/linear-issue-check.sh`、`scripts/gates/linear-issue-check.test.sh`
- 修改：`.claude/settings.json`、`.github/workflows/ci.yml`、`docs/COLLABORATION.md`（§3／§5-b／§7）
- worktree：`.claude/worktrees/LS-77`
- 模擬器已關：無（本次任務無 Swift 變更，未 boot 任何模擬器；`demo-iPhone17Pro` 為既有 demo-* 持久機，非本次 boot，豁免）


### 2026-09-01T19:08:34.946Z · Chih-Lin Yeh · `8bb40708`

## merge-review R1 — PR #214（head `d4e1efe021f19d722bf83950ce2e6f519fdb4e35`，base `main`）

審查範圍：`git diff main...d4e1efe`（5 檔 +363/-3）：`scripts/gates/linear-issue-check.sh`（新）、`scripts/gates/linear-issue-check.test.sh`（新）、`.claude/settings.json`、`.github/workflows/ci.yml`、`docs/COLLABORATION.md` §3／§5-b／§7。

**裁決：APPROVE**（無 blocker／major；6 個 minor＋informational，處置請照 CLAUDE.md handoff 規則逐條記錄）

---

### 實跑驗證（我自己跑的，不是複述 handoff）

| 項目 | 結果 |
|---|---|
| `linear-issue-check.test.sh` | 28 組全綠（✓ 行實數 28，與宣稱一致） |
| CI `rules` job 接線自證 | job log 確有 `bash scripts/gates/linear-issue-check.test.sh \|\| rc=1` 這行、且 18:54:39 印出 `PASS：linear-issue-check.test.sh 全數通過`；四個 check 全 pass |
| 既有 hook 回歸 | `pretool.test.sh` 全綠；`.claude/settings.json` PreToolUse 仍 2 組、`Bash\|Read\|Grep` 那組原封未動 |
| 驗收 5 巡檢實跑（我自己跑） | `patrol-linear.sh` §4：(a) 無／**(b) LS-8**／(c) 無／(d) 無／(e) 無 — 與 handoff 完全一致 |
| 驗收 2／3 對帳（讀碼逐條對票文） | `ticket_structure()` L513-533 的 (a)(b)(c) 字面等同票文三類（`project` 缺／`project.name.startswith("Phase")` 且無 `projectMilestone`／`title` 以 `Task：`\|`Task:` 起頭且無 `parent`）；`classify_candidate()` L285-291 對缺 project／Phase 缺 milestone 回 `'structure'`，`lane_candidates()` 只挑 `'ok'`、`lane_pending()` 輸出「待結構」→ 驗收 2／3 宣稱屬實，§7 新列與程式碼相符 |
| base 對帳 | `origin/main` 就是 merge-base `f06f5e4`，head 只多本票 2 個 commit；`git merge-tree` 乾淨；未回退今日任何票 |
| 我自建的 7 個 mutant | m1 反轉「無 id＝建票」／m2 rule D `-ne 1`→`-lt 1`／m3 rule C 拿掉全形冒號／m4 rule A 永不觸發／m5 空 stdin 放行／m6 解析失敗放行 **全部被測試殺掉**；**m7（拿掉 `trap on_exit EXIT`）存活** → 見 F2 |
| **真實流量回放**（`~/.claude/projects` session 記錄 590 次 `save_issue`） | **475/475 更新票全部 ALLOW（誤攔 0）**，含 32 次 `patch` 模式（全帶 `id`）、`state`／`priority`／`labels`／`cycle`／`blockedBy` 各種形狀；115 次建票中 74 次會被擋，最近 40 次建票只擋 2 次、且兩次都是真違規（2026-08-25 Phase 1 票缺 milestone、2026-09-01「設計 chore：cmp/Tab Bar AX3」缺 project）。欄位名也由真實流量證實：`id`／`project`／`milestone`／`parentId`／`labels`／`title` 正是 save_issue 實際參數名，且 116 次全部用**專案名稱**（"Phase 1 — 單一家庭 MVP"／"Harness 與協作基建"）傳 `project` → rule B 的 `Phase*` 前綴在真實用法上成立 |

四維度：race condition／運算效能／平行優化三項對本 diff **無 finding**（單次同步 hook、無共享狀態、無暫存檔、最多 spawn 一個 jq；CI 新增一支自測沿用既有序列慣例）。**scope 乾淨**：5 檔全部對得上驗收 1／4，COLLABORATION 三處皆為新增句、無順手重構、無夾帶。

---

### Findings

**F1（minor）`scripts/gates/linear-issue-check.test.sh` 全檔：新 hook 的 settings.json 接線沒有自證斷言**
本檔對 `.claude/settings.json` 的引用數＝0。倉庫既有兩個 hook 都有接線自證：`scripts/hooks/pretool.test.sh:282-301`（I6①②③：matcher 存在／指向 pretool.sh／帶 `|| exit 2`，註解明寫「把 PreToolUse 區塊刪掉／matcher 漏列 Grep／忘了 `|| exit 2` 之前 CI 全綠」）與 `scripts/ops/patrol.test.sh:209-214`（⑨ SessionStart）。**失敗情境**：日後任一 harness 票編輯 `.claude/settings.json`（同一個 PreToolUse 陣列）時把 `mcp__linear__save_issue` 那組刪掉、或改壞 `|| exit 2`，`linear-issue-check.test.sh` 照樣印 PASS、`rules` job 照樣綠、PR 照樣併入，而 hook 已經死掉——本票的交付物就是這條接線，卻是三個 hook 中唯一沒有機械覆核的。**降級為 minor 的理由**：後果有第二層兜底——巡檢 `ticket_structure()` 會在 26 分鐘內把因此漏進來的壞票列出來（我實跑確認這段活著），所以爆炸半徑有界。**建議修法**（約 10 行、無行為風險，比照 I6）：`jq -r '.hooks.PreToolUse[] | select(.matcher == "mcp__linear__save_issue") | .hooks[] | select(.type=="command") | .command'`，斷言非空、含 `linear-issue-check.sh`、含 `|| exit 2`。我已手動跑過這三條，**目前接線是對的**（command＝`bash "$CLAUDE_PROJECT_DIR/scripts/gates/linear-issue-check.sh" || exit 2`）。

**F2（minor）`scripts/gates/linear-issue-check.sh:125-131` 的 `trap on_exit EXIT` 無測試覆蓋**
我做 mutant m7（只刪 `trap on_exit EXIT` 一行）重跑全套 → **28 組仍全綠，mutant 存活**。本檔案檔頭與 §7 新列都宣稱 fail-closed 含「腳本自身中途未預期中止」，測試只覆蓋三態（空 stdin／JSON 壞／jq＋python3 皆缺）。`pretool.test.sh:304-338` 的 F5 正是為同一缺口而寫（當時被評 major），本檔案註解也自稱「同 pretool.test.sh 的 F5 慣例」卻沒實作它。**行為本身我驗過是對的**：在 RULE-A 前注入 `exit 1`，有 trap → exit 2＋deny JSON；無 trap → exit 1 無輸出（此時仍靠 settings.json 的 `|| exit 2` 在 wiring 層擋下，只是 deny 理由文字消失）——所以是測試缺口、不是行為缺陷。**建議修法**：照搬 F5 的 with_trap／no_trap 兩份副本斷言（約 15 行）。

**F3（minor）`scripts/gates/linear-issue-check.sh:156` 與 `:185`：`read` 少了 `-d ''`，欄位遇換行截斷**
`IFS="$SEP" read -r idv projectv milestonev titlev parentidv labelsv <<<"$out"` 沒有 `-d ''`，read 只讀到第一個換行為止。`pretool.sh:182/206` 刻意用 `read -r -d ''`，其註解記載這正是 LS-104 R1 F1 的根因（多行欄位在第一個 `\n` 被截斷）。**失敗情境**：`title`／`project` 值含換行時，後面的欄位全部變空 → 例如 `{"project":"P\nQ","title":"Foo","labels":["lane:ui"]}` 實測 DENY，理由卻是「labels 需恰一個 lane:*」（誤導）。**方向是 fail-closed（過度攔截、非漏放）**，且我確認多行 pretty-printed JSON 不受影響（jq 輸出本身是單行）；真實流量 116 次建票無一命中。**建議修法**：加 `-d ''`（尾端換行已有 `labelsv=${labelsv%$'\n'}` 處理），或至少把註解寫明此處與 pretool.sh 的差異是刻意的。

**F4（minor）`scripts/gates/linear-issue-check.sh:199` / `:205-211`：`project` 只做「非空字串」與「Phase 前綴」字面判定**
實測：`project:"   "`（純空白）→ **ALLOW**（rule A 沒擋）；`project` 傳 UUID／slug（Linear MCP schema 明載 `project` 接受 name／ID／slug）→ rule B 的 `Phase*` 永不觸發、Phase 票可以無 milestone 通關；`"phase 1"` 小寫同理。**真實風險低**：116/116 次建票都用專案名稱，且巡檢 (b) 事後仍抓得到。**建議**：rule A 改判「去空白後非空」；rule B 的盲區（非名稱形式的 project）明寫進檔頭「已知盲區」段，別讓 §7 的 ✅ 讀起來像全覆蓋。

**F5（informational）`:194` 的 `id` 判定接受任何非空字串化值**
實測 `id:{}`／`id:[]`／`id:123`／`id:"   "` 皆判定為「更新票」→ 四條規則全部跳過（`id:null`／`id:false`／`id:""` 則正確走建票路徑，⑥b 已覆蓋）。要踩到得先送出 Linear 本身就會拒絕的 payload，實務不可達，記錄備查即可。

**F6（informational）`:226-235` labels 以逗號 join 再 split**
單一標籤名內含逗號會被誤拆：`labels:["lane:ui,extra"]` → 算 1 個 lane → ALLOW；`labels:["lane:"]`（裸前綴）也算數。Linear 現有標籤名無逗號，屬理論面。順帶確認符合預期的：`["Lane:UI"]` 大小寫不符 → DENY、`["lane：ui"]` 全形冒號 → DENY、`["lane:ui","lane:design"]` → DENY（皆為 fail-closed 方向，與票文一致）。

**F7（informational）腳本完全不看 `tool_name`**
matcher 是字串比對／regex 語意，若日後 Linear MCP 出現名稱以 `mcp__linear__save_issue` 起頭的其他工具，四條規則會被錯套。加一行「`tool_name` 非 `mcp__linear__save_issue` 即 allow」即可，成本近零。

**F8（informational）`linear-issue-check.test.sh:326/373` 的 `mktemp -d` 無 trap 清理**
`work`／`mut_dir` 只在正常路徑 `rm -rf`；測試中途失敗會留下暫存目錄。`pretool.test.sh:124` 用 `trap 'rm -rf "$work"' EXIT`。

**F9（informational，操作面預告）誤攔面實測與即將出現的摩擦**
更新票 0 誤攔（475/475）；但最近 40 次建票中有 1 次（2026-09-01「設計 chore：cmp/Tab Bar AX3 適應」，只帶 `labels:["lane:design"]`）依現行規則會被擋——也就是說 hook 上線後，這類「設計 chore」建票必須補 `project`（Phase 專案還要補 `milestone`）。這是規則本身要的效果，不是缺陷，但請 orchestrator 有心理準備：`/hooks` 重載後第一次開 chore 票就會撞到。另：`template` 建票（`template` 有值、`title`／`project` 缺）實測會被擋，可用「template＋明列 project／labels」通過；590 次真實呼叫中 template 使用次數為 0，故僅列備查。

---

### LS-8 的處置意見（實跑 (b) 命中）

LS-8「Phase 0-4：註冊 Apple Developer Program」（Project「Phase 0 — 開發地基」、`lane:product`、Backlog、High、需使用者本人刷卡）。三個選項我的建議是 **① 補 milestone（資料修），不要走豁免、也不要放著**：

- **不要放著**：(b) 會每 26 分鐘印一次 LS-8 直到永遠，把「開票結構」段訓練成雜訊——這正是 `patrol_linear.py:514-515` 註解對 LS-96 明寫的顧慮（「否則每輪都命中且永遠清不掉，會訓練出忽略習慣」）。
- **不要成文豁免 Phase 0**：豁免要改 `ticket_structure()`／`classify_candidate()`（超出本 PR scope），且會變成倉庫第二個永久例外；一張票的資料歸位不值得換一條規則例外。
- **補 milestone 的實作成本≈0**：`save_issue{id:"LS-8", milestone:"<Phase 0 的 milestone>"}` 帶 `id` → 新 hook 放行。若 Phase 0 專案目前沒有任何 milestone（從 LS-8 被命中看很可能沒有），先在 Phase 0 建一個（例如「M0 地基與帳號」）再掛上。**附帶好處**：hook 上線後，任何**新**的 Phase 0 票都必須帶 milestone，Phase 0 沒有 milestone 可選就會直接卡死——先建這個 milestone 等於一併解掉未來的死路（Phase 1 已有 M1／M2，不受影響）。
- 補 milestone 不改變 LS-8 的性質（仍是使用者本人操作、仍 `lane:product` 不進自動補位），純結構歸位。

---

### 未審／未驗證（明講）

1. **未實際呼叫 `mcp__linear__save_issue`**（依派工限制），故「hook 在真實 session 載入後端到端生效」與「exit 2 時 stdout 的 `permissionDecisionReason` 是否真的被 Claude Code 呈現給 model」我沒有驗證——本 PR 完全沿用 `pretool.sh` 既有慣例（stdout JSON＋exit 2），若該慣例有問題也是既有問題、不是本 PR 引入，PLAUSIBLE、不列 finding。
2. `patrol_linear.py` 只讀了本票對帳需要的 `ticket_structure()`／`classify_candidate()`／`lane_candidates()`／`lane_pending()`，其餘（LS-103 範圍、已併入）未複審。
3. 無 migration／無 Swift 變更，故未跑 `supabase db reset`／`supabase/tests/run.sh`（也就未使用 supabase-lock）。


### 2026-09-01T19:11:18.216Z · Chih-Lin Yeh · `284a1bf3`

## Orchestrator retro（收尾儀式；純 harness 票，dead-code-sweeper 免跑）

**過程**：對帳模式實作（驗收 2/3 發現早由 LS-103 patrol_linear.py 機械化、只補成文——省下重做；驗收 1 hook 為新作）→ merge-review 一輪 APPROVE（3 minor＋6 informational 全入池，無阻擋）→ 併 main `9ea471b`、back-merge PR #215。

**Lesson learning**：
1. **真實流量回放是 hook 類票的黃金驗證法**：reviewer 從 session transcript 抓出 590 次真實 save_issue 呼叫重放（475 更新全放行、零誤攔；116 次 project 全傳名稱證實 rule B 假設）——比合成樣本更有說服力，hook 類審查慣例加入「歷史流量回放」。
2. **對帳模式三連發都省了重做**（LS-44/68/77）——「舊票先對帳」正式固化為派工規則（已在 dispatch 慣例）。
3. **LS-8 處置示範「資料修優於豁免」**：巡檢旗紅的正解是把資料修對（建 M0 milestone 掛上），不是為單票開豁免——豁免會累積成第二套規則。

informational 處置：F1–F9 記 LS-96（comment `257aca42`）；LS-8 資料修已執行（M0 上架前置）。**hook 生效需 /hooks 重載**——已列入等使用者動作清單（與 pretool hook 同一次重載即可）。

### 2026-09-01T19:22:47.251Z · Chih-Lin Yeh · `02907098`

結案：PR #214 併 main `9ea471b`（merge-review=success on `d4e1efe`，linear:8bb40708）；back-merge PR #215 併 development `6056b5c`（§2 驗證：merge-tree 無內容差、status 貼 `9ea471b` id 53326343675；CI 全綠）。收尾儀式完成——純 harness 免 sweeper、retro（comment `284a1bf3`）、informational 記 LS-96（comment `257aca42`）、LS-8 資料修（M0 milestone）、清理：`cleanup-merged.sh --apply LS-77`。hook 生效待 /hooks 重載（使用者動作）。

