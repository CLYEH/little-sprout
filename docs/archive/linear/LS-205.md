# LS-205 Harness：模擬器 runtime 與 CI 對齊——`detect-simulator.sh` 建專屬機時選 CI 同版 runtime（`.ios-runtime` 釘住）、push-gate／CI／巡檢印出 runtime 版本並標不一致；順修 push-gate 看門狗三項（info-1～3）、`api-contract-check.sh` 納入 view（relkind v）、evidence-check 檔頭數字

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:M, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-05T12:08:38.553Z |
| 開始 | 2026-09-05T12:08:58.370Z |
| 完成 | 2026-09-05T18:02:20.715Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-205/harness模擬器-runtime-與-ci-對齊-detect-simulatorsh-建專屬機時選-ci-同版-runtimeios |
| 關係 | related→LS-164, related→LS-204, related→LS-200, related→LS-201, related→LS-206, related→LS-176, related→LS-83, related→LS-111, related→LS-187, related→LS-107, related→LS-202, related→LS-96, related→LS-167, related←LS-275, related←LS-272, related←LS-270, related←LS-271, related←LS-266, related←LS-260, related←LS-261, related←LS-259, related←LS-253, related←LS-246, related←LS-245, related←LS-243, related←LS-237, related←LS-132, related←LS-218, related←LS-232, related←LS-203, related←LS-166, related←LS-230, related←LS-217, related←LS-221, related←LS-216, related←LS-220, related←LS-189, related←LS-193, related←LS-211, related←LS-212, related←LS-192, related←LS-209, related←LS-191, related←LS-207 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項 `f728958a`（**High**）：[LS-167](https://linear.app/little-sprout-app/issue/LS-167/taskls-20-ios-上傳佇列-sheet沒有成功正在進行已完成三群ls002-容量已滿列最前重試與背景續傳固定-detent依-ls) merge-review R4 `876f150d` I-a——`.xcode-version` 釘 Xcode，但專屬機 runtime 各異（[LS-167](https://linear.app/little-sprout-app/issue/LS-167/taskls-20-ios-上傳佇列-sheet沒有成功正在進行已完成三群ls002-容量已滿列最前重試與背景續傳固定-detent依-ls) 26.0／共用機 26.5／CI 26.2）；iOS 26.2+ 對 sheet 內容套 ≈0.96 縮放，造成「本機綠 CI 紅」燒掉 [LS-167](https://linear.app/little-sprout-app/issue/LS-167/taskls-20-ios-上傳佇列-sheet沒有成功正在進行已完成三群ls002-容量已滿列最前重試與背景續傳固定-detent依-ls) 兩輪實作＋兩次完整 CI。巡檢 2026-09-05 20:07：lane:harness 在飛 0（[LS-202](https://linear.app/little-sprout-app/issue/LS-202/harness設計-gate-精修三項-overflow-scanjs-corner-anchor-改以-ref-cmpphoto) 進 QA）→ 依 harness 優先序取 High 池項升票，並順帶同檔／同類 XS 池項：`37a390d0`（push-gate 看門狗 info-1～3）、`ca99c6ae` i1（`api-contract-check.sh` 只對帳 `relkind r/p`，view 落網外）、`cc078cb9`（`design-evidence-check.sh:68`「43 份」實為 42）。

## 範圍

1. **runtime 釘住**：repo 根新增 `.ios-runtime`（例 `26.2`，與 CI workflow 的 `-destination OS:` 同源——CI 改為讀此檔，不再硬寫）；`scripts/gates/detect-simulator.sh` 建 `LS-<n>-*` 專屬機時選該版 runtime（`xcrun simctl list runtimes -j` 對齊 `iOS <ver>`），本機沒有該版 → 印 `⚠ 本機無 iOS <ver> runtime（有：…），改用 <實際版>；CI 為 <ver>，tap-target／版面量測可能不一致` 並繼續（fail-open）；既有專屬機 runtime ≠ 釘住版 → 印同款 ⚠（不自動重建）。
2. **可見化**：`push-gate.sh` 與 CI `ci` job 在 xcodebuild 前印一行 `simulator: <name> <udid> iOS <ver>（pinned <ver>）`；`patrol.sh` 專屬模擬器段列出各機 runtime，與 `.ios-runtime` 不一致者標 `⚠ runtime`。
3. **push-gate 順修**（`37a390d0`）：(1) session log 來源改 `grep -qE "<string>${wd_repo_root}/[^/<]*\.xc(odeproj|workspace)<"`；(2) lock 回收「holder 必須是被殺子孫」補自測樣本（讓既有 mutation 死）；(3) `wd_kill_tree` KILL 前重收一次子孫。
4. `api-contract-check.sh`（`ca99c6ae` i1）：對帳納入 `relkind 'v'`（security_invoker view），`album_summaries` 六欄進 API.md ⇄ schema 對帳；自測補 view 正負樣本。
5. `design-evidence-check.sh:68` 檔頭「43 份」→「42 份」。
6. `docs/COLLABORATION.md` §7 對照表加 runtime 釘住一列；ios-dev／qa／merge-reviewer 定義各加一句「量測前確認模擬器 runtime＝`.ios-runtime`，不同要在 handoff 註明」。

## 不做

自動下載 runtime；重建既有專屬機；`BLEED_RE` Mount（`56de90a6`，需重跑 cross_parent 等價證明，另次）。

## 驗收

- [ ] `detect-simulator.test.sh`／`push-gate.test.sh`／`patrol.test.sh`／`api-contract-check` 自測全綠＋mutation（釘住檔改別版 → 建機選版案紅；relkind 只留 r/p → view 案紅）
- [ ] 本機實跑：新建一台 `LS-<本票>-*` 專屬機 runtime＝`.ios-runtime`；CI `ci` job log 出現 `simulator: … iOS 26.2（pinned 26.2）`
- [ ] merge-reviewer 通過；hotfix 流程（自 main、PR 回 main、back-merge）

## 留言（14）

### 2026-09-05T12:09:39.501Z · Chih-Lin Yeh · `561abf6e`

**Orchestrator（09-05 20:08）**：開票理由——harness lane 空 1 輪（LS-202 進 QA），依優先序取 High 池項 `f728958a`（LS-167 今日因 runtime 不一致燒兩輪）升票，順帶三項同類 XS。worktree `.claude/worktrees/LS-205`（`hotfix/LS-205-simulator-runtime-pin` 自 main `4832eff`）已建，ios-dev 已派；本機無 26.2 runtime，正好實測 fail-open ⚠ 路徑，CI 端驗 `pinned 26.2` 印出。

### 2026-09-05T13:10:46.661Z · Chih-Lin Yeh · `57167e56`

**Handoff（2026-09-05 21:06）**

**Ticket**：LS-205——模擬器 runtime 與 CI 對齊＋三項順修（`.ios-runtime` 釘住；push-gate/CI 可見化；push-gate 看門狗 info-1～3；api-contract-check 納入 view；design-evidence-check 檔頭數字；COLLABORATION §7＋三份 agent 定義）。

**已完成**（6 個 commit，每個範圍一支）：
1. `.ios-runtime`（`26.2`）單一來源；`detect-simulator.sh` 建新專屬機優先選釘住版，本機沒裝該版 fail-open（印 ⚠ 改用 `header_os`）；既有專屬機 runtime ≠ 釘住版同款印 ⚠、不重建。
2. `push-gate.sh`／CI `ci` job 的 Build & Test 步驟在 `xcodebuild test` 前印 `simulator: <name> <udid> iOS <ver>（pinned <ver>）`；`patrol.sh` 專屬模擬器段依 runtime 分組解析各機版本，不一致印「⚠ runtime」。
3. push-gate 看門狗三項順修（LS-96 池項 `37a390d0`）：(1) session log 來源改精確 `grep -qE` 比對；(2) 補「別人的鎖不被誤收」自測；(3) `wd_kill_tree` KILL 前重新收集子孫，且對第一輪已知的每個 pid 各自再查一次（不只從根重走——中間層無 trap 的行程收到 TERM 就死透會斷鏈，實測踩到才發現這個更深的成因）。
4. `api-contract-check.sh` 納入 `relkind 'v'`（LS-96 池項 `ca99c6ae` i1）；`api_contract_check.py` 新增 `create/drop view` 事件解析；`docs/API.md` §9 補 `album_summaries`。
5. `design-evidence-check.sh:68` 43→42（LS-96 池項 `cc078cb9`）。
6. `docs/COLLABORATION.md` §7 補一列；`ios-dev.md`／`qa.md`／`merge-reviewer.md` 各加一句 runtime 確認提醒。

**已驗證**（怎麼驗的）：
- 自測全綠：`detect-simulator.test.sh` 38/38（新增 ⑬-⑯）、`push-gate.test.sh` 82/82（新增 ㊱㊲，① 補印出格式斷言）、`patrol.test.sh` 245/245（新增 ⑭b）、`api-contract-check.test.sh` 21 組樣本全過（新增 ⑰-⑳／㉑）、`design-evidence-check.test.sh` 65/65、`agent-tools-check.sh`／`brand-skill-check.sh` 皆綠。
- Mutation 手動驗證：`api-contract-check.sh` 的 `relkind` 只留 `'r','p'` → ㉑ 紅（已還原）；`.ios-runtime` 改成本機沒裝的版本 → 建機選版正確跟著改（⑬/⑭ 覆蓋兩個方向）。
- 本機建機實跑：本機無 `iOS 26.2` runtime（`xcrun simctl list runtimes` 只有 26.0／26.5）。`bash scripts/gates/detect-simulator.sh` 建新專屬機 `LS-205-iPhone17Pro`，印：
  `⚠ detect-simulator：本機無 iOS 26.2 runtime（有：26.0、26.5），改用 iOS 26.0；CI 為 iOS 26.2，tap-target／版面量測可能不一致`
  （fall back 到 26.0 是因為「清單第一台可用 iPhone」所在的 OS 分節就是 26.0——`header_os` 邏輯本身沒變，符合預期，不是 26.5）。再跑一次改印既有專屬機 runtime 不符釘住版的警告（不重建）。驗完 `xcrun simctl delete` 該機，模擬器已關：無殘留（`xcrun simctl list devices available | grep LS-205` 已確認為空）。
- `push-gate.sh` 本機實跑：本 PR 未動 Swift／`.xcode-version`／`project.yml`，判定「無 Swift 變更」跳過 SwiftLint／unit tests（CI 仍無條件全跑）；`api-contract-check`（文字模式，35 支 RPC／24 張表對帳一致）／`tap-target-registry`／`error-codes`／migration 版本號／分支票號／合併乾淨度皆綠，`push gate 通過`。
- CI `ci` job 的 `simulator: … iOS 26.2（pinned 26.2）` 那行留給 orchestrator 從 CI log 確認（我不追蹤 CI 結果，見 CLAUDE.md 規約）。

**未完成／剩餘**：無 informational 待處置（本 PR 是初次送審，尚未經 merge-reviewer；四個 LS-96 池項本身已在本票範圍內處理完，見上方「已完成」對應）。

**風險**：無破壞性變更；`.ios-runtime`／push-gate／CI／patrol 皆新增或 fail-open 分支，既有行為不變；`api-contract-check` 擴大對帳範圍只會多抓不會漏抓既有表。

**產出位置**：
- PR：https://github.com/CLYEH/little-sprout/pull/321（`hotfix/LS-205-simulator-runtime-pin` → `main`）
- head：`29bf836c4789269de7bdbe90c94522b7fc6fb65e`
- 模擬器已關：`LS-205-iPhone17Pro`（已 `simctl delete`，不留殘機）

### 2026-09-05T13:12:12.593Z · Chih-Lin Yeh · `f3bf91a8`

**Orchestrator（09-05 21:11）**：handoff 收到（PR #321 head `29bf836`）。已派 merge-reviewer R1：本機實跑缺版 fail-open ⚠、CI log `pinned 26.2` 行、push-gate 82 組＋看門狗重查 mutation、api-contract view mutation、patrol 245 組；本票改到 push gate 本身故要求在 worktree 實跑 push-gate 無回歸。APPROVE 後走 hotfix 鏈（merge main → back-merge dev → promote）。

### 2026-09-05T13:43:33.659Z · Chih-Lin Yeh · `0b8b73be`

**merge-review R1（2026-09-05 21:41）——REQUEST_CHANGES**

PR #321 `hotfix/LS-205-simulator-runtime-pin` → `main`｜審的 head `29bf836c4789269de7bdbe90c94522b7fc6fb65e`（開審與收工各比對一次 `gh pr view 321 --json headRefOid`，未前進）｜base `origin/main` `4832eff`｜diff 17 檔 +421/−38，全部落在 harness 腳本＋`.github/workflows/ci.yml`＋docs＋`.claude/agents/`，無 `ios/`、無 `design/`、無 `supabase/migrations`——**scope 乾淨**，每個檔都對得上範圍 1–6。

CI 已全綠（run 33967910861：db／lint／rules／ci 四項 success）。以下是我自己實跑複驗後的結果。

---

## Blocker
無。

## Major

### M1（major）`scripts/ops/patrol.sh:463-467` — runtime 不一致併進 `sim_flagged`（「專屬模擬器待清」），一台重複計數、且在本機永久非零

`sim_flagged` 這個計數器的既有語意是「要清的台數」（`patrol.sh:652` 印成 `專屬模擬器待清 N（殘機 M）`，`patrol.test.sh` ⑭ 明文斷言 `專屬模擬器待清 4（殘機 0）`）。本次把「runtime 不一致」也 `+1` 進去，但票文自己寫明這一類**不清、不重建**。

**具體失敗情境（已重現）**：合成一台 `LS-9999-iPhone17Pro`（runtime 26.0、無 worktree），ROOT 帶 `.ios-runtime=26.2` 跑 `patrol.sh --brief`：

```
專屬模擬器待清 2（殘機 1）
[專屬模擬器 LS-9999-iPhone17Pro] runtime iOS 26.0 與釘住版 iOS 26.2 不一致（不自動重建，見 .ios-runtime）
[專屬模擬器 LS-9999-iPhone17Pro] 票 LS-9999 的 worktree 已不在（殘機） → cleanup-merged.sh --apply LS-9999
```

**一台機器被算成 2**（464 行 +1，481 行再 +1）。

**併入後的即時影響**：`ROOT` 取 `git rev-parse --git-common-dir` 的上層＝**主 checkout**（我實測從 LS-205 worktree 跑，讀的是主 checkout 的 `.ios-runtime`，目前不存在所以整段沒生效——併入 main 後就會生效）。本機現有 5 台受管專屬機（`LS-164`／`LS-167`／`LS-188-iPhone17Pro`／`main-iPhone17Pro` 在 26.0、`LS-188-iPadAir11M3` 在 26.5）全部 ≠ 26.2 → 每輪巡檢（26 分）固定吐 5 筆 flag、`專屬模擬器待清` 由 0 變 5，且 `--brief` 從此再也不會印「巡檢：無異常」。而這 5 筆**沒有可達的收斂狀態**：本機無 26.2 runtime，票文「不做」明列不自動下載，刪掉重建也只會 fail-open 回 26.0（我實跑驗過，見下方「已驗證」(1)）。永久紅且無動作可做的 flag，等於把 LS-83／LS-187 那條清理訊號洗成雜訊。

**建議修法**：`sim_flagged` 不動，另開 `sim_rt_mismatch` 計數，brief 行改成 `專屬模擬器待清 N（殘機 M）· runtime 不一致 K`；另建議只在「釘住版本機真的裝得到」（`simctl list runtimes` 命中）時才標——裝不到的情況 `detect-simulator.sh` 每次建機已經印 ⚠ 了，巡檢再吐一份是重複且不可行動。`patrol.test.sh` ⑭b 順手補一條計數斷言（現在 ⑭b 只驗字串、不驗數字，所以這個 bug 自測抓不到）。

### M2（major）`scripts/gates/detect-simulator.sh:134`／`196` — LS-176 同票重用仍以 `header_os` 為準，釘住版一旦生效就失效，LS-107 的「同票堆多台」回來了

`create_dedicated()` 已改成建在 `pinned_os`，但 `find_udid_same_ticket()` 的 awk 仍 `-v os="$header_os"`，只掃 header_os 那個分節。穩態下 `header_os`（清單第一台可用 iPhone 所在分節＝本機最低的 iOS 分節）與 `pinned_os` **本來就不同**（本機：header 26.0 vs pin 26.2），於是「用釘住版建出來的機器」永遠不會被重用判定看到。

**具體失敗情境（A/B 實測，真 simctl）**：
- B（有釘住版，`.ios-runtime=26.5`、header_os 26.0）：先手動建 `LS-205-iPhone17ProMax` 於 26.5（模擬「上次用釘住版建的同票機」），再跑 `detect-simulator.sh` →
  `platform=iOS Simulator,id=8FF4A90B…`，結果**同票兩台**：`-- iOS 26.5 -- LS-205-iPhone17Pro` ＋ `LS-205-iPhone17ProMax`，沒有印任何重用訊息。
- A（對照，移除 `.ios-runtime`，同票機建在 header_os 26.0）：同樣情境 →
  `→ detect-simulator：重用同票既有專屬機「LS-205-iPhone17ProMax」（同 runtime iOS 26.0，不另建…；LS-176）`，只有一台。

觸發前提是「精確名稱 `<票號>-<機型>` 落空」，也就是清單第一台可用 iPhone 的機型變了（Xcode／runtime 升級、原廠機被刪重建）——那正是 LS-107 實際發生過、LS-176 為此才寫的情境。要命的是**這條回歸要等「本機裝上釘住版」才會啟動**，也就是本票目標達成的那一刻才開始壞，屆時沒有任何訊號（`patrol` 磁碟水位是事後、CoreSimulator/Devices 目前已 24 GB）。

`detect-simulator.test.sh` 抓不到：⑪⑫（LS-176 重用）的合成 worktree 沒有 `.ios-runtime`（只有 ⑬-⑯ 的 LS-111/112/113/114 有），釘住版 × 重用的交互完全沒被覆蓋。

**建議修法**：把「有效目標 runtime」在 dispatch 之前算一次——
```
target_os="$header_os"
[ -n "$pinned_os" ] && <simctl list runtimes 命中 pinned_os> && target_os="$pinned_os"
```
`find_udid_same_ticket`（`-v os="$target_os"`，重用訊息裡的版本字串同步）與 `create_dedicated` 共用它；自測補一條「⑪ 的 fixture 加上 `.ios-runtime`＝釘住版且既有同票機在釘住版分節 → 仍重用」。

## Minor

### m1 `scripts/gates/push-gate.sh:56` — `.ios-runtime` 沒進 0b 純文件判定的 allowlist
該行 allowlist 已含 `(^|/)\.xcode-version$`（同檔 41–43 行的 R1 F2 註解寫明理由：「避免只改它時連帶跳過 1b」），`.ios-runtime` 是同款單一來源卻沒加。**已重現**：本 PR 自己在 worktree 實跑 `push-gate.sh`，印 `→ push gate：無 Swift 變更，跳過 SwiftLint`／`✓ push gate：無 Swift 變更，跳過 unit tests`——也就是「CI runner 升到 26.3、只改 `.ios-runtime` 一行」這種最需要在新 runtime 上跑一次 unit tests 的 PR，本機會整段跳過（CI 仍無條件全跑，所以只是本機前置失守，不是漏網）。修法：regex 加 `|(^|/)\.ios-runtime$`，`push-gate.test.sh` 補一條。

### m2（PLAUSIBLE）`scripts/gates/push-gate.sh:412-415` — `wd_kill_tree` 重收子孫的 pid 重用 TOCTOU
重收時對「TERM 之後最多 3 秒」的舊 pid 逐一 `pgrep -P`，這些 pid 可能已死；若 OS 已回收該 pid 給無關行程，它的子行程會被加進 KILL 名單。macOS pid 空間近 10 萬且 3 秒內要跑完一輪回收實務上到不了，所以標 PLAUSIBLE、不列 major。要硬化就在迴圈裡先 `kill -0 "$p" 2>/dev/null || continue`。

## Informational（處置請在 handoff 逐條交代）

- **i1｜info-1（session log regex）零迴歸測試**：把 `push-gate.sh:433` 改回舊的 `grep -qF "<string>${wd_repo_root}/"`，`push-gate.test.sh` **82 條全綠**。㉝ 的 fixture 把「別的 worktree」放在 repo 根之外，正好避開這次要修的那個 bug（主 checkout 的根是 `.claude/worktrees/*` 的字首）。修法本身我看過是對的，只是沒有東西守著。另外 `${wd_repo_root}` 未跳脫就內插進 ERE（`.claude` 的 `.` 會當萬用字元），今天無害。
- **i2｜㊲ 沒有鎖住「holder 必須是被殺子孫」這個條件**：保留 `kill -0` 存活檢查、只拿掉 `case " $wd_killed_pids "` 那段 → 82 條**仍全綠**（㊲ 的外來 holder 是活著的 `sleep 10`，存活檢查就擋掉了）。按測試註解描述的 mutation（holder 檔存在就無條件 `rm -rf`）確實會讓 ㊲ 紅，所以票文字面要求算達成，但被點名的那個條件仍未被釘住。補法：再加一條 fixture，holder 寫一個**已死且非本次子孫**的 pid，斷言鎖不被回收。
- **i3｜㉑ 是 SQL 字面斷言**：`grep -qE "relkind in \('r', 'p', 'v'\)"` 連空白都釘死，SQL 無害重排就會假紅；行為只由 CI db job 背書（本 run 已綠：`✓ api-contract gate（catalog 模式／權威）通過：RPC 35 支、表 24 張`）。測試註解自己講清楚了，記錄備查。
- **i4｜CI 端只印不比**：`ci.yml:667` 同時握有實際版本與 `ios_runtime_pin`，卻沒比。runner image 的第一順位 runtime 一漂（`ci` job 走 `CI=true` 分支用共用第一台，`.ios-runtime` 對 CI 選版沒有任何約束力），CI 照樣綠，LS-167 那個「本機綠 CI 紅」的失效模式就悄悄回來，唯一訊號是一行沒人會 diff 的 log。一行 `[ "$os" = "$pin" ] || echo "::warning::…"` 就能收口。票文範圍 2 只要求「印」、§7 也把盲區寫出來了，所以不列 major。
- **i5｜重複的 `xcrun` 呼叫**：`detect-simulator.sh:82` 的 `os_of_udid()` 重跑一次 `simctl list devices available`，內容與第 26 行已抓好的 `$list` 相同；`create_dedicated()` 最多會跑 3 次 `simctl list runtimes`。相對 xcodebuild 可忽略（每次 0.2–0.5s），但白付。
- **i6｜文字模式解析延伸到字串常值裡的散文**：`blank_sql_comments()` 只清 SQL 註解，不清 `comment on view … is '…'` 的內文；`20260905074037_album_summaries_view.sql:107/109` 的散文就含 `CREATE VIEW`／`CREATE OR REPLACE VIEW`，這次沒中純粹是後面接反引號、不符 `[A-Za-z_]`。新增的 `drop view` 方向更危險（散文寫 `drop view public.x` 會**靜默**移除追蹤 → API.md 反被判幽靈項）。catalog 模式是權威且已綠，所以只是記錄。
- **i7｜驗收條文「六欄進對帳」的讀法**：api-contract gate 對表只比**名稱**（任何表都不比欄位），所以本次實際達成的是「view 名稱進名稱對帳」；六欄的機械保障來自既有的 `supabase/tests/107_album_summaries.sql`（`albums` 欄位集合 ⊆ `album_summaries`）。無回歸，只是驗收條文別誤讀。

---

## 已驗證（怎麼驗的）

**自測數字（sandbox clone，非 worktree，未動任何受審檔案）**
- `detect-simulator.test.sh` **38/38**、`patrol.test.sh` **245/245**、`push-gate.test.sh` **82/82**（84 秒）、`api-contract-check.test.sh` **21 組全過**、`design-evidence-check.test.sh` 全過、`agent-tools-check.sh` rc=0、`brand-skill-check.sh` rc=0。與 handoff 申報一致。

**Mutation（5 支，全部我自己跑）**
| # | 變異 | 結果 |
|---|---|---|
| M1 | 拿掉 `wd_kill_tree` KILL 前重收子孫 | ㊱ 紅（`晚生孫行程（pid 14923）沒被殺乾淨`）✅ |
| M2 | 重收改成「只從根 `$1` 重走一次」 | ㊱ 兩條紅（`行程樹沒殺乾淨：19403 19404`＋晚生孫）✅ **實證了實作者說的「中間層 TERM 先死透導致斷鏈」**，不是臆測 |
| M3 | 鎖回收改成無條件 `rm -rf` | ㊲ 紅 ✅ |
| M4 | 停用釘住版選版（`if false`） | ⑬「runtime＝釘住版 26.5（非 header_os 26.0）」紅、⑭ fail-open 警告紅 ✅ |
| M5 | 鎖回收只拿掉「子孫」判斷、保留 `kill -0` | **全綠**（見 i2） |
| M6 | session log regex 改回舊的 `-qF` 字首 | **全綠**（見 i1） |

**本機真跑 `detect-simulator.sh`（真 simctl，本機只有 26.0／26.5）**
1. 釘住 26.2（本機無）→ `⚠ detect-simulator：本機無 iOS 26.2 runtime（有：26.0、26.5），改用 iOS 26.0；CI 為 iOS 26.2，tap-target／版面量測可能不一致`，機器建在 **26.0**、rc=0。fail-open ✅
2. 釘住 26.5（本機有、且 ≠ header_os 26.0）→ 機器建在 **26.5** ✅ 釘住版選版對真 simctl 有效
3. 既有機在 26.5、釘住版改回 26.2 → `⚠ … 既有專屬機「LS-205-iPhone17Pro」目前是 iOS 26.5，與釘住版 iOS 26.2 不同…不自動重建`，機器數仍 1、不重建 ✅
4. 選版比對安全性：`simctl list runtimes` 實際格式是 `iOS 26.0 (26.0.1 - 23A8464) - com.apple…iOS-26-0`；`grep -m1 "^iOS ${pinned_os} "` 尾隨空白使 `26.2` 不會誤命中 `26.20`／`26.2.1` ✅（`pinned_os` 未跳脫進 BRE，`.` 是萬用字元，但實務上構不成誤命中）
5. `.ios-runtime` 讀法與 `.xcode-version` 一致（皆 `tr -d '[:space:]'`）；`od -c` 確認檔案就是 `26.2\n`，純數字無尾註解 ✅

**CI log 那一行（run 33967910861，`ci` job「Build & Test」）**
```
simulator: iPhone 17 Pro 6F69910C-E4FA-488A-B9C2-41B770484810 iOS 26.2（pinned 26.2）
```
驗收條件達成 ✅（但見 i4：CI 端沒有比對，這一行只是可見化）

**其他**
- `push-gate.sh` 在 LS-205 worktree 實跑：`✓ push gate 通過`（走「無 Swift 變更」路徑，故新的 `.ios-runtime` fail-closed 與 `simulator:` 那行本機沒被執行到，由 ① 覆蓋——同時也是 m1 的重現）
- `api-contract-check.sh`（文字模式）在本分支：`RPC 35 支、表 24 張` 綠；CI catalog 模式（`supabase db reset` 後查活 DB）同樣 `24 張` 綠，`album_summaries` 確實進了對帳、且 public schema 沒有其他 view 落網
- `design-evidence-check.sh:68` 的 43→42：`git ls-tree -r origin/development -- design/evidence | grep -c '\.json$'` ＝ **42** ✅
- 四維度：race → 只有 m2 那個 pid 重用 TOCTOU（PLAUSIBLE），M2 mutation 反證了重收邏輯本身是必要且正確的；效能 → i5 的重複 `xcrun`，patrol 的 runtime 分組解析是同一次 awk 內多一條規則、零額外呼叫；平行 → 無不必要序列化；scope → 乾淨

## 未看／跳過的範圍（誠實揭露）
- `patrol.sh` 新 awk 對**非 iOS** runtime key（`SimRuntime.watchOS-11-0`）的解析會產出無意義字串（`sub(/^.*SimRuntime\.iOS-/)` 不命中），但那些分節底下的裝置名不會是 `LS-<n>-*`／`main-*`，一律 `continue`，實務不可達 → 不列 finding。
- `--catalog` 模式我沒有對本機 DB 另跑（CI db job 已用 `db reset` 後的活 DB 驗過同一支，證據力更強），因此**沒有持有 supabase-lock**。
- CI runner 上 `.ios-runtime` 與實際 runtime 的長期一致性無法在本次驗證（見 i4）。

## 產出位置
- 審的 head：`29bf836c4789269de7bdbe90c94522b7fc6fb65e`（收工再比對一次仍相同）
- 模擬器：本次為驗證共建了 4 台 `LS-205-*`（`LS-205-iPhone17Pro` ×2、`LS-205-iPhone17ProMax` ×2），**全部已 `simctl delete`**，`simctl list devices | grep -c LS-205` ＝ 0；全程只 `create` 未 `boot`，沒有我開的機器需要 shutdown（清單裡 `LS-167-iPhone17Pro (Booted)` 是別人的，未動）。sandbox clone／副本已清。
- supabase lock：**未持有**（不需要互動式實跑）。

**Verdict：REQUEST_CHANGES**（M1、M2 兩個 major；m1／m2 建議一併修，i1–i7 請在 handoff 逐條交代處置）。

### 2026-09-05T13:45:15.399Z · Chih-Lin Yeh · `358a3637`

**Orchestrator（09-05 21:45）**：merge-review R1 `0b8b73be` REQUEST_CHANGES（M1 runtime 不一致誤入「待清」計數、M2 同票重用未用釘住版、m1／m2、i1～i7）。裁定全收：M1 改獨立提示行不進 `sim_flagged`、不影響 `--brief` 無異常；M2 `find_udid_same_ticket` 與 `create_dedicated` 共用同一解析（含帶 `.ios-runtime` 的自測 fixture）；m1 allowlist；m2 至少註明取捨；i1／i2 補案讓 reviewer 的存活 mutation 死。已續派實作 R2。

### 2026-09-05T15:02:44.951Z · Chih-Lin Yeh · `6d2e46bb`

**Handoff R2（2026-09-05 23:01）**

**Ticket**：LS-205 R2——回應 merge-review R1 `0b8b73be` REQUEST_CHANGES（2 major、2 minor、7 info）。

**已完成**（6 個 commit，每項一支）：
1. **M1**（`9a98b5c`）patrol.sh runtime 不一致原本 `+1` 進 `sim_flagged`（「待清」語意）且掛 `add_flag`——一台機器同時是殘機又 runtime 不一致會被算成 2，且 `--brief` 從此再也不印「無異常」。改成獨立計數 `sim_rt_mismatch`，只進 `SIM_LINES` 細項與彙總數字（「· runtime 不一致 N（釘住 iOS ver，提示不擋）」），不叫 `add_flag`；`--json` 加 `rt_mismatch_simulators`。
2. **M2**（`c8caffb`）`find_udid_same_ticket()` 仍用 `header_os`、`create_dedicated()` 建機用 `pinned_os`，兩者不同步——釘住版生效後用它建出來的既有同票機永遠不會被重用判斷看到，LS-107 回歸。抽出共用 `target_os`（優先 pinned_os，本機真的裝得到才採用），兩處共用同一次解析。
3. **m1**（`740d59c`）push-gate 0b allowlist 補 `.ios-runtime`（本 PR 自己重現：漏了這條，改 `.ios-runtime` 會被判「無 Swift 變更」跳過 unit tests）。
4. **m2**（`e2160aa`，PLAUSIBLE 硬化）`wd_kill_tree` 重收子孫前先 `kill -0` 存活檢查，降低 pid 重用 TOCTOU；未加 `ps -o lstart`，理由寫在註解（剩餘視窗已縮到微秒級）。
5. **i1／i2**（`eb6fe12`）補讓 reviewer 存活的 mutation 死：新增 ㉝b（巢狀 worktree 路徑，session log regex 若改回舊版 `-qF` 會誤判）、㊳（holder 檔竄改成已死非本次子孫的 pid，鎖回收若拿掉 `wd_killed_pids` 判斷會誤收）。
6. **i4**（`b4fd0b4`）CI 端加一行比對實際 runtime 與 `.ios-runtime`，不一致印 `::warning::` annotation（不 fail）。

**Informational 逐條處置**（i1/i2 已隨 M/m 修一併列在上方；i3/i5/i7 不修＋理由、i6 記入待辦池）：
- i3：㉑ 的 SQL 字面靜態斷言是刻意設計（本檔案不起真 DB，行為由 CI `db` job 的 `--catalog` 權威對帳背書，已綠）；假紅風險已寫在測試註解裡，接受，不修。
- i5：`os_of_udid()`／`create_dedicated()` 重複 `xcrun` 呼叫——M2 重構已把 `create_dedicated()` 原本最多兩次 `simctl list runtimes` 併成一次（順手改善）；`os_of_udid()` 是另一支獨立函式，成本相對 xcodebuild 可忽略，此輪不擴大範圍去重構，不修。
- i6：`blank_sql_comments()` 不清 `comment on view ... is '...'` 字串常值裡的散文，`drop view` 若出現在散文裡會被文字模式靜默移除追蹤——記入待辦池 LS-96 `b40646d4`（catalog 模式權威、已綠；此票尚未真的觸發，留給下次動這支解析器時處理）。
- i7：驗收條文「六欄進對帳」的讀法——gate 只比表／view 名稱，六欄的機械保障其實來自既有 `supabase/tests/107_album_summaries.sql`；無回歸，純粹措辭問題，已在 PR 描述澄清，不修。

**已驗證**：
- 自測全綠：`detect-simulator.test.sh` 43/43（新增 ⑰⑱）、`push-gate.test.sh` 85/85（新增 ㉑b／㉝b／㊳）、`patrol.test.sh` 252/252（新增 ⑭c）、`api-contract-check.test.sh` 22 行輸出（21 組樣本，不變）、`design-evidence-check.test.sh` 65/65、`agent-tools-check.sh`／`brand-skill-check.sh` 仍綠。
- Mutation 手動驗證（逐一還原）：M2 改回 `header_os` 硬編 → ⑰ 三條斷言全紅；i1 改回舊版 `-qF` → ㉝b 紅、㉝ 仍綠（重現 reviewer 觀察）；i2 拿掉 `case " $wd_killed_pids "` 判斷 → ㊳ 紅、㊲ 仍綠（重現 reviewer 指出的確切盲區）；m1 拿掉 allowlist 項 → ㉑b 紅。
- 本機真 simctl 三段＋同票重用：(1) 釘住 26.2（本機無）→ fail-open 建在 26.0；(2) 釘住 26.5（本機有）→ 建在 26.5；(3) 刪精確命名機、留一台不同機型的同票機在 26.5 → 印「重用同票既有專屬機「LS-205-iPhoneAir」（同 runtime iOS 26.5，不另建 LS-205-iPhone17Pro；LS-176）」，只剩一台（M2 修復前會變兩台）。驗完 `xcrun simctl delete` 全部、`.ios-runtime` 用 `git checkout --` 還原成 26.2（未污染 commit）。
- push-gate.sh 本機實跑：R2 這輪本 PR 的 diff 因 m1 修好而不再被判「無 Swift 變更」，真的跑了一次 `xcodebuild test`（scheme LittleSprout），`✓ push gate 通過`；用到的 `LS-205-iPhone17Pro`（fail-open 建在 26.0）跑完已 `xcrun simctl delete`。
- Supabase lock：未持有（本票全程未碰本機容器）。

**未完成／剩餘**：無（i1-i7 全數處置完畢，見上）。

**風險**：無破壞性變更；皆為既有邏輯的正確性訂正或新增獨立訊號，不影響既有行為的其餘部分。

**產出位置**：
- PR：https://github.com/CLYEH/little-sprout/pull/321（`hotfix/LS-205-simulator-runtime-pin` → `main`）
- head：`b4fd0b4ada5d9e821c099fda5dc44306e318cc00`
- PR body 已更新（R2 段落＋逐條處置，`pr-body-check.sh --branch hotfix/LS-205-simulator-runtime-pin --verify` exit 0，含 LS-96 comment id 驗證）。
- 附註：`gh pr edit` 更新 body 後 CI 的 `pull_request` 事件不含 `edited`，且本次推送當下 `rules` job 剛好撞上 Linear API 一次性網路瞬斷（`curl: (56)`，fail closed），已 `gh pr close`/`gh pr reopen` 321 觸發對目前 body 的新一輪檢查（COLLABORATION 既有指引），CI 結果留給 orchestrator 監看。
- 模擬器已關：本輪驗證共建過 `LS-205-iPhone17Pro`（fail-open，26.0，×2）／`LS-205-iPhoneAir`（26.5）/`LS-205-iPhone17Pro`（26.5），全部已 `xcrun simctl delete`，`xcrun simctl list devices available | grep LS-205` 為空。

### 2026-09-05T15:27:37.524Z · Chih-Lin Yeh · `c4acdc01`

**merge-review R2（2026-09-05 23:25）——APPROVE**

PR #321｜審的 head `b4fd0b4ada5d9e821c099fda5dc44306e318cc00`（開審與收工各比對一次 `gh pr view 321 --json headRefOid,state`：OPEN、未前進）｜增量 `29bf836...b4fd0b4`，7 檔 +222/−39，全在 harness 腳本＋`.github/workflows/ci.yml`——**scope 不變、乾淨**。

R1 的 2 個 major、2 個 minor 全部修掉，而且我自己重放 R1 那兩支「當時活下來」的 mutation，現在都死了。以下逐條複驗。

---

## R1 findings 複驗

### M1（patrol 待清計數）→ **已修，實機驗證**
`sim_rt_mismatch` 獨立計數，不進 `sim_flagged`、不叫 `add_flag`；細項行改成「（提示不擋，不計入待清）」；brief／human 表頭與 `--json` 都多一欄。

我用**本機真實裝置清單**跑 `--json`（`SIMCTL_LIST_JSON=` 讓它去問真的 `xcrun`）：
```
rt_mismatch_simulators = 5          ← 正是我 R1 預測的 5 台（全部 ≠ 26.2）
flags = 6，其中模擬器相關 4 筆全是既有「殘機」原因，runtime 不一致 0 筆
```
再用單台合成機（`main-iPhone17Pro`、26.0、剛用過——唯一異常就是 runtime 不一致）：
```
… · 專屬模擬器待清 0（殘機 0） · runtime 不一致 1（釘住 iOS 26.2，提示不擋） · …
flags = 2（都是這個 sandbox clone 自己的 DETACHED／hooksPath，與模擬器無關）
詳細段：⚠ runtime main-iPhone17Pro（SIM-MAIN）iOS 26.0 ≠ 釘住版 iOS 26.2（提示不擋，不計入待清）
```
**加入 runtime 不一致機前後 flag 數完全不變**＝「巡檢：無異常」的判定不受影響，永久噪音問題解除。R1 的雙重計數重現案也已成為 ⑭c 的斷言（`專屬模擬器待清 1（殘機 1） · runtime 不一致 1`，不再是 2）。
Mutation（把不一致重新塞回 `sim_flagged`＋`add_flag`）→ **⑭b／⑭c 共 4 條紅**，含那條雙重計數斷言 ✅

### M2（LS-176 同票重用 vs 釘住版）→ **已修，A/B 實機重放**
`target_os` 提到頂層算一次（`pinned_os` 且本機 `simctl list runtimes` 真的命中才採用），`find_udid_same_ticket()` 與 `create_dedicated()` 共用，重用訊息的版本字串也跟著改。

真 simctl 重放我 R1 的重現步驟（pin=26.5、header_os=26.0，先手動建同票不同機型 `LS-205-iPhone17ProMax` 於 26.5）：
```
R1：另建第二台 → 同票兩台
R2：→ detect-simulator：重用同票既有專屬機「LS-205-iPhone17ProMax」（同 runtime iOS 26.5，不另建 LS-205-iPhone17Pro；LS-176）
     機器數 1 ✅
```
fail-open 分支也重跑（pin=26.2 本機無）：`⚠ … 本機無 iOS 26.2 runtime（有：26.0、26.5），改用 iOS 26.0 …`，建在 26.0、rc=0 ✅（R2 把警告移到 `target_os != pinned_os` 判斷後，行為與 R1 一致）。
Mutation（`find_udid_same_ticket` 改回 `header_os`）→ **⑰ 3 條紅**（`實得 UUID-00000（另建了第二台？）`），⑱ 維持綠（退回分支本來就該一樣）✅

### m1（`.ios-runtime` 未進 0b allowlist）→ **已修，本 PR 自己驗證**
在 LS-205 worktree 實跑 `push-gate.sh`：**不再印「無 Swift 變更」**，真的跑完 SwiftLint＋unit tests，`✓ push gate 通過`（rc=0）。順帶第一次在真機上跑到 R1 沒被執行到的可見化那行：
```
→ push gate：simulator: LS-205-iPhone17Pro 34B2E4C3-6BD6-447D-94FE-833CB28D0A7F iOS 26.0（pinned 26.2）
```
本機 26.0 vs 釘住 26.2 的落差就這樣一眼看到，正是這條規則要的效果。
Mutation（allowlist 拿掉 `.ios-runtime`）→ **㉑b 紅** ✅

### m2（`wd_kill_tree` pid 重用 TOCTOU）→ **已收窄**
`kill -0 "$p" && more=…`：只查還活著的 pid，視窗從「TERM 後最多 3 秒」縮到同一次迭代內的微秒級。不比對 `ps -o lstart` 的理由寫在註解裡，我認同（成本／效益不成比例）。
另外驗了 `set -e` 安全性：`for … do kill -0 X && …; done` 在最後一輪失敗時**不會**讓 `set -euo pipefail` 提早結束（AND-list 非末項豁免），我用同形狀的最小案例實測 rc=0 ✅——這是這個寫法唯一會踩的坑，沒踩到。

### i1／i2（R1 兩支活下來的 mutation）→ **兩支都死了**
- ㉝b：別票 worktree **巢狀**在 repo 根底下（`<root>/.claude/worktrees/LS-999/LittleSprout.xcodeproj`）——正是真實佈局。重放 R1 的 M6（regex 改回 `-qF` 字首）→ **㉝b 紅（exit 124，別票 crash 被當自己的）** ✅
- ㊳：holder 檔竄改成**已死且非本次子孫**的 pid。重放 R1 的 M5（只拿掉 `case " $wd_killed_pids "`、保留 `kill -0`）→ **㊳ 紅** ✅ 這正是 R1 指出「㊲ 測不到的那個條件」。

### i4（CI 端只印不比）→ **已修**
`ci.yml` 加 `[ "$sim_ios_ver" != "$ios_runtime_pin" ]` → `::warning::` annotation、不 fail。本 run 兩者相符（`simulator: iPhone 17 Pro C100EB07-… iOS 26.2（pinned 26.2）`），故走的是「不印」那一支；warning 分支本身無法在 CI 上人為製造不一致去驗（列為下方 n4）。不 fail 的理由（runner 升版是排程性事件、不該怪當次 PR）我認同。

### i3／i5／i7 不修、i6 入池 → **理由與落地都核對過**
PR body 逐條都有處置，7 條 informational 一條不少。`i6` 的池項 `b40646d4` 我讀了原文：內容準確（連「這次沒誤中純屬後面接反引號」這個細節都寫進去了），處置「下次動這支解析器時一併處理」合理。

---

## R2 新發現

### m3（minor）`docs/COLLABORATION.md:306` — §7 那一列在 R2 之後有三處與程式碼相反
CLAUDE.md 把 §7 定為前饋↔反饋的正典對照表，這一列現在說的三件事已經不成立：
1. 「與釘住版不一致者印「⚠ runtime」**並掛 `add_flag`**」——R2 的 M1 修法**刻意移除** `add_flag`（改成獨立計數、不進 flag 清單）。照這句話讀的人會以為巡檢會發 flag。
2. 盲區欄：「`.ios-runtime` 忘記同步更新（CI 升級 runtime 但沒改這個檔）**不會有任何提示**」——R2 的 i4 已經讓 CI 在不一致時印 `::warning::` annotation，這個盲區已經被自己這輪填掉了。
3. 自測清單只列 `⑬-⑯`／`①`／`⑭b`，未含本輪新增的 ⑰⑱／㉑b／㉝b／㊳／⑭c。

不影響執行正確性，但「程式碼改了、對照表沒改」正是這張表最不該發生的漂移。修法：那一列三處各改一句（約 3 行）。**注意這會讓 head 前進、需要重貼 `merge-review` status**——若判斷成本大於效益，記入 LS-96 池項也可接受，請在 handoff 明講選了哪條。

### n1（informational）`detect-simulator.sh:87-90`：`target_os` 在頂層無條件計算
`CI=true` 分支根本用不到它，卻仍多跑一次 `xcrun simctl list runtimes`（`ci.yml` 有兩個呼叫點）。相對 xcodebuild 可忽略，且 i5 已裁定此輪不做 `xcrun` 去重，只是同族又多一次；未來若要處理，延後到真的要用時再算即可。

### n2（informational）`--json` 的 `rt_mismatch_simulators` 是數字、鍵名卻與陣列同形
`stale_simulators`／`orphan_simulators`／`booted_simulators` 都是陣列，這個是計數。與既有的 `default_simulators`（同為數字）一致，所以不算錯，但消費端 `jq '… | length'` 會炸——記錄備查。`patrol.test.sh` 也沒有斷言這個新欄位（我用實跑確認它存在且值正確：5／1）。

### n3（informational）㊳ 對時序有依賴
「輪詢最多 4 秒等 `holder` 檔出現」搭配 `PUSH_GATE_XCODEBUILD_TIMEOUT_SEC=5`；機器負載高時可能等不到 → 會 fail-loud 印「自測本身沒運作」（方向安全，不會假綠）。本機共觀察到 4 次通過（2 次完整跑＋2 次 mutation 跑中它維持綠），未見 flake。

### n4（informational）`ci.yml` 的 `::warning::` 分支未被執行過
只驗到「相等 → 不印」。CI yaml 在本 repo 一向沒有單元測試層，且無法人為製造 runner runtime 不一致，屬可接受盲區——但也代表那行字串真的印出來長什麼樣沒人看過。

---

## 合併前提（不是 code finding，但擋合併）

**`rules` job 目前在 `b4fd0b4` 上是紅的**，兩次 run（33973135045／33973534621）都紅在同一步：
```
✗ pr-body-check：curl 失敗（exit 28）：Operation timed out after 25002 milliseconds — fail closed
✗ pr-body-check：LS-96 comment 清單取得失敗（exit 2）——fail closed
```
在那之前 `✓ PR body 檔頭段含本票票號 LS-205` 與 7 個「已修」SHA 全數驗過，失敗點是 Linear API 逾時——與本 PR 內容無關（LS-96 池項 `7902dc20` 已記錄同一時段 PR #322／LS-167 #305 也被打到，並提出 curl 重試的修法）。

**但要注意副作用**：`pr-body-check` 是 `rules` job 的第 6 步，它一紅，第 9 步「Gate 腳本自測」與第 13 步「Ops 腳本自測」**被 skip 了**——也就是 R2 新增／修改的四套自測**從未在 CI 上跑過**。我用本機完整重跑補上這個缺口（數字見下），但 required check 仍必須等 `rules` 重跑綠才能合併。我的 APPROVE 只涵蓋程式碼，不涵蓋那道 required check。

其餘三項：`ci` success、`db` success、`lint` success。

---

## 已驗證（怎麼驗）

**四套自測（sandbox clone at `b4fd0b4`，未動 worktree 任何檔案）**
| 套件 | R1 | R2 | 新增 |
|---|---|---|---|
| `detect-simulator.test.sh` | 38 | **43/43** | ⑰⑱ |
| `push-gate.test.sh` | 82 | **85/85**（91s） | ㉑b／㉝b／㊳ |
| `patrol.test.sh` | 245 | **252/252** | ⑭c＋⑭b 改斷言 |
| `api-contract-check.test.sh` | 21 | **21 組（22 ✓）** | 不變 |

另：`design-evidence-check.test.sh` 全過、`agent-tools-check.sh` rc=0、`brand-skill-check.sh` rc=0。`push-gate.test.sh` 連跑 2 次皆 85/85（查 ㊳ 是否 flaky）。

**Mutation 5 支（全部我自己跑，跑完 `git checkout --` 還原）**
| 變異 | 結果 |
|---|---|
| patrol：runtime 不一致重新進 `sim_flagged`＋`add_flag` | ⑭b／⑭c **4 條紅** ✅ |
| detect-simulator：`find_udid_same_ticket` 改回 `header_os` | ⑰ **3 條紅** ✅（⑱ 維持綠） |
| push-gate：session log regex 改回 `-qF` 字首（＝R1 的 M6，當時全綠） | ㉝b **紅** ✅ |
| push-gate：拿掉「必須是被殺子孫」只留 `kill -0`（＝R1 的 M5，當時全綠） | ㊳ **紅** ✅ |
| push-gate：allowlist 拿掉 `.ios-runtime` | ㉑b **紅** ✅ |

**真機／真 CI 實跑**：M2 的 A/B（重用只一台）、fail-open ⚠ 路徑、patrol 對真實 5 台機的 `--json`／單台合成機的 `--brief`／human 三面、LS-205 worktree 的完整 `push-gate.sh`（含 unit tests，rc=0）、CI `ci` job log 的 `simulator: … iOS 26.2（pinned 26.2）`。

**四維度**：race → m2 已收窄，`set -e` 與 AND-list 的互動實測安全；效能 → n1 多一次 `simctl list runtimes`，patrol 的 runtime 分組仍是同一次 awk、零額外呼叫；平行 → 無不必要序列化；scope → 7 檔全在 harness／CI，未碰 `ios/`／`design/`／`supabase/migrations`／docs／agents。

**未看／跳過**：`ci.yml` 的 warning 分支（n4）；`--catalog` 未另跑本機 DB（CI `db` job 已綠，且 R2 未動 api-contract）；因此**未持有 supabase-lock**。

## 產出位置
- 審的 head：`b4fd0b4ada5d9e821c099fda5dc44306e318cc00`
- **模擬器已清**：本輪建了 3 台（A/B 用 2 台＋push-gate 實跑建的 `LS-205-iPhone17Pro`），全部 `simctl delete`；`xcrun simctl list devices | grep -c LS-205` ＝ **0**，目前無任何 Booted 裝置。sandbox clone／副本已刪。
- supabase lock：**未持有**。

**Verdict：APPROVE**（無 blocker／major）。m3 建議合併前順手修（會動 head、需重貼 status），或明確記入 LS-96；n1–n4 記錄備查。合併仍須等 `rules` 重跑綠。

### 2026-09-05T15:29:20.119Z · Chih-Lin Yeh · `65c45012`

**Orchestrator（09-05 23:28）**：merge-review R2 `c4acdc01` **APPROVE**（status 貼 `b4fd0b4`；R1 兩 major 兩 minor 全複驗；五支 mutation 皆死）。裁定：m3（COLLABORATION §7 該列三處與 R2 程式相反）不為 3 行文件再開一輪，入池 `8af1c2bc`、下一張 harness 票必修；n1～n4 入池。`rules` 因 Linear 反查逾時紅（且連帶 skip 自測步驟，已入池要求自測不依賴 pr-body-check）→ 已 rerun；hotfix 鏈已啟動：等 CLEAN → merge main → main tip status → back-merge PR → dev tip status → 之後 promote → delta QA（harness：本機建機 ⚠ 路徑、patrol brief 無異常、CI `pinned 26.2` 行）。

### 2026-09-05T15:51:27.403Z · Chih-Lin Yeh · `6339ae0e`

**Orchestrator（09-05 23:50）**：hotfix 鏈完成——PR #321 併 main `034145b`（main tip status ✓）、back-merge #324 併 development `70d95f3`（dev tip status ✓）→ QA。test 目前 `da62930` 的合併 QA 因 LS-167 單項退修而 status failure；LS-167 修正併入 dev 後 promote，本票與之同批 delta QA（harness：本機建機 ⚠ fail-open 路徑、patrol brief 仍「無異常」、CI `pinned 26.2` 行、四套自測）。下一張 harness 票 LS-207 已派（含本票 m3 文件訂正）。

### 2026-09-05T17:35:31.981Z · Chih-Lin Yeh · `e51c1e6f`

**Orchestrator（09-06 01:34）**：development `127c38c`（含本票 back-merge #324＋LS-167 修正）已 promote 到 test。已派 delta QA（本票：四套自測、本機無 26.2 的 fail-open 建機實跑、patrol brief 仍無異常、`pinned 26.2` 行、api-contract view 對帳；同批 LS-167 30pt 複驗）；PASS 後 test→main tag v0.16.0 → sweeper → Done → cleanup。

### 2026-09-05T17:55:58.131Z · Chih-Lin Yeh · `8fe70c13`

## QA 裁決：PASS

驗收基準：`test` branch tip `127c38cfd2869920363760a9c4719b055667e331`（origin/test 相符）。本機 runtime 只有 26.0／26.5（無 26.2），正好是票文設計要覆蓋的 fail-open 情境。未持有 `supabase-lock`（`api-contract-check` 走文字模式）。

### 逐條驗證

- ✓ **四套自測全綠**：`detect-simulator.test.sh` 43/43、`push-gate.test.sh` 85/85、`patrol.test.sh` 252/252、`api-contract-check.test.sh` 21 組樣本全過（含 `⑰-⑳`／`㉑` 新增的 view 相關案）。`agent-tools-check.sh`／`brand-skill-check.sh` 皆 rc=0。
- ✓ **實跑 fail-open 路徑**：在 qa-test worktree 建臨時分支 `qa-verify-LS-205-runtime`（含 `LS-205` 字樣供 `detect-simulator.sh` 的票號擷取邏輯辨識），首次跑：
  ```
  ⚠ detect-simulator：本機無 iOS 26.2 runtime（有：26.0、26.5），改用 iOS 26.0；CI 為 iOS 26.2，tap-target／版面量測可能不一致
  platform=iOS Simulator,id=A6288181-5526-45ED-BE35-5478A5D76D4A
  ```
  建出 `LS-205-iPhone17Pro`（確認落在 iOS 26.0 分節），exit 0。**再次執行**（既有專屬機 runtime≠26.2）：
  ```
  ⚠ detect-simulator：既有專屬機「LS-205-iPhone17Pro」目前是 iOS 26.0，與釘住版 iOS 26.2 不同（CI 為 iOS 26.2）；不自動重建…
  ```
  機器數維持 1（未重建），符合票文「既有專屬機 runtime≠釘住版→印⚠不重建」。驗完 `simctl delete` 該機、`git branch -D` 刪除臨時分支、切回 `origin/test` 對應的 detached HEAD。
- ✓（替代形式達成）**`patrol.sh --brief` 在多台 ≠26.2 專屬機下的行為**：本機當時有 8 台受管專屬機 runtime≠26.2（含我建的 `LS-205-iPhone17Pro`），brief 行印出獨立計數「`專屬模擬器待清 0（殘機 0） · runtime 不一致 8（釘住 iOS 26.2，提示不擋）`」——待清計數維持 0，不因 runtime 不一致而被誤算成需清理；本輪因另有 5 個已併入 base 但 worktree 未移除的殘留（LS-167／LS-201／LS-204／LS-205／LS-206，與本票驗收無關的既有清理待辦）導致沒有印出純粹的「無異常」，但「runtime 不一致」欄位本身的判定符合票文要求（提示、不進待清）。
- ✓ **`push-gate.sh` 實跑印 `pinned` 行**：本機因 branch-ticket-check（工作分支命名規則）擋在較早步驟，無法在裸分支上跑到印那行的步驟；改讀 CI log 佐證（票文允許的替代方式）——CI run `33967910861`（PR #321 R1，`ci` job「Build & Test」）：
  ```
  simulator: iPhone 17 Pro 6F69910C-E4FA-488A-B9C2-41B770484810 iOS 26.2（pinned 26.2）
  ```
  另在最新 CI run `33978602554`（PR #325，即本次 `test` tip 所併入的那輪 CI）同樣確認：
  ```
  simulator: iPhone 17 Pro C100EB07-2BC6-4796-BB7F-40CDFD61AD2D iOS 26.2（pinned 26.2）
  ```
  兩次不同 PR 的 CI run 皆穩定印出且版本相符，可見化功能正確、CI runner 上釘住版本身也持續有效。
- ✓ **`api-contract-check.sh` 對 development 現況綠、view 納入對帳**：文字模式 `RPC 36 支、表 24 張，docs/API.md 與 schema 一致`；`docs/API.md` §3 有 `album_summaries`（LS-200，view）條目，六個彙總欄位（`visible_media_count`／`latest_media_id`／`latest_thumb_path`／`latest_storage_path`／`cover_thumb_path`／`cover_storage_path`）逐一列出，且對帳邏輯本身已由自測 `⑰`-`㉑` 覆蓋 view 的 create/drop/replace 與 catalog `relkind` 清單含 `'v'`。

### 未完成／限制

- push-gate.sh 的 `pinned` 行未能在 qa-test worktree 直接實跑取得（因裸分支/detached HEAD 與工作分支票號規則不相容，非本票 bug）；已用 CI log 佐證，符合任務指示的替代驗證路徑。
- 未逐一複驗 R1→R2 的 5 支 mutation（那是 merge-review 職責範圍，已在 R1／R2 comment 中完整記錄且我信任其結果；QA 這輪聚焦「自測數字、實跑行為、CI 可見化」三個機械可複驗面向）。

### 產出位置

- 自測 log：`.claude/worktrees/qa-test/scratchpad/LS-205-qa-selftests{,-pushgate,-patrol,-apicontract}.log`
- CI log 佐證：`/private/tmp/claude-501/.../scratchpad/ci-run-{33967910861,33978602554}.log`（本機暫存，未落地 repo）
- 本輪未 boot 任何模擬器做視覺驗收（純命令列/CI 驗證）；`detect-simulator.sh` 建的 `LS-205-iPhone17Pro` 已 `simctl delete`，臨時分支 `qa-verify-LS-205-runtime` 已刪除

**Verdict：PASS**

### 2026-09-05T17:58:59.857Z · Chih-Lin Yeh · `c379467c`

**Orchestrator（09-06 01:58）**：delta QA `8fe70c13` **PASS**（四套自測、本機無 26.2 fail-open 建機實跑、既有機不重建、patrol brief 獨立計數、CI `pinned 26.2` 行、api-contract view 對帳）→ test→main，tag **v0.16.0**。dead-code-sweeper 已派 → Done → cleanup。m3 文件三處由 LS-207 訂正。

### 2026-09-05T18:01:26.540Z · Chih-Lin Yeh · `9abdb534`

## dead-code sweep — LS-205（2026-09-06 02:00）

**範圍**：`git diff 4832eff...034145b`，篩掉同範圍內併入的 LS-164（帳密登入，無關檔案）後鎖定 LS-205 實際觸碰的 17 個檔案：`.ios-runtime`、`scripts/gates/{detect-simulator,push-gate,api-contract-check,design-evidence-check}.sh`＋對應 `.test.sh`、`scripts/gates/api_contract_check.py`、`scripts/ops/patrol.sh`＋`.test.sh`、`.github/workflows/ci.yml`、`docs/{API,COLLABORATION}.md`、`.claude/agents/{ios-dev,merge-reviewer,qa}.md`（PR #321 併 main `034145b`，back-merge #324；11 commits：`da326b4`…`b4fd0b4`）。

### Findings

未發現本 feature 引入的死碼。針對派工指定的重點逐項核查：

1. **`header_os` 舊路徑**：R2 把「有效目標 runtime」抽成 `target_os`（`detect-simulator.sh:87`），`header_os` 並未變成死變數——`target_os="$header_os"` 是 fail-open 的預設值來源，兩者共存且各自有用途（`grep -n header_os` 仍有 3 處實際賦值/比較用途，其餘為註解說明沿革），非殘留。
2. **`add_flag` 路徑**：merge-review R1 major M1 指出 runtime 不一致原本誤併入 `sim_flagged`＋`add_flag`（會讓「巡檢：無異常」永久消失）；R2 已改成獨立計數 `sim_rt_mismatch`，`grep -n "sim_rt_mismatch\|add_flag" scripts/ops/patrol.sh` 確認兩者完全不相鄰、`add_flag` 沒有任何呼叫點掛在 `sim_rt_mismatch` 分支——R1 的死分支已徹底移除，非殘留。
3. **`supabase/tests/fixtures/` 類殘檔**：本票未碰 `supabase/tests/`，不適用；worktree `.claude/worktrees/LS-205` 檢查為乾淨（無 dirty 檔案、無孤兒暫存檔）。
4. **R1→R2 迭代死分支**：`push-gate.test.sh` 新增的 `run_gate_keep_lock`／`WD_LATE_SPAWN`／㊱㊲㊳ 等 R2 新增測試路徑（對應 LS-96 池項 `37a390d0` 的 (2)(3) 兩項順修）均有被對應斷言使用，無空掛的 helper；`detect-simulator.test.sh` 的 ⑬-⑱ 與 `patrol.test.sh` 的 ⑭b/⑭c 皆為新增且被斷言覆蓋，無 R1 遺留但 R2 已不再需要的樣板。
5. **註解引用已改名變數**：全篇掃過 `pinned_os`／`target_os`／`sim_rt_mismatch`／`sim_pinned_os` 等新變數名，註解內引用與實際變數名一致，未發現改名後註解未同步的情形。
6. **API.md／COLLABORATION §7**：`album_summaries` 只新增一處 TABLES 對帳條目，與既有 §3 文件說明無重複；COLLABORATION §7 runtime 列已知有 m3 三處過時（`add_flag`／盲區描述／自測清單未列全），按派工指示已入池 `8af1c2bc`／交 LS-207 處理，本輪不重複報。

### 既有死碼觀察

無新增觀察。merge-reviewer R1/R2 已完整記錄的 informational（i1-i7、n1-n4）多為效能／測試覆蓋面的取捨說明（如 `os_of_udid()` 重複 `xcrun` 呼叫 i5、`--json` 新欄位 `rt_mismatch_simulators` 命名一致性 n2），非死碼，且已有明確處置（不修＋理由／記入 LS-96／APPROVE 接受），不重複列入本報告。

### 建議處置

無需開 fix 票。本票經兩輪 merge-review（R1 `0b8b73be` REQUEST_CHANGES → R2 `c4acdc01` APPROVE，5 支 mutation 全部驗證死）與 QA（`8fe70c13` PASS）充分複驗，本輪巡檢未發現新增死碼，可直接 Done。

### 搜尋方法與盲區聲明

- 方法：`git log`/`git diff` 鎖定 LS-205 實際觸碰檔案（排除同範圍混入的 LS-164 檔案）；`grep -n` 逐一核對變數宣告與所有引用點（`header_os`／`target_os`／`pinned_os`／`sim_rt_mismatch`／`add_flag`）；全文讀取 `scripts/ops/patrol.sh`／`scripts/gates/detect-simulator.sh` 相關區段確認邏輯完整無斷鏈；`git diff --stat` 核對無意外夾帶的暫存檔。
- 盲區：純文字搜尋抓不到 reflection／字串拼接的間接呼叫（本次腳本皆為 bash/python 直接呼叫，風險低）；未在真實 CI runner 上重跑 `::warning::` 分支（reviewer n4 已記錄此為既有盲區，非本次新增）；未對本機以外環境（其他 agent 的模擬器 runtime 狀態）做全面普查，僅檢查 LS-205／LS-204 兩個 worktree 的 git 乾淨度。

### 2026-09-05T18:02:13.107Z · Chih-Lin Yeh · `eecfba89`

**收尾儀式（09-06 02:0x）**
- **dead-code-sweeper** `9abdb534`：無死碼；`header_os` 為 fail-open 退路仍活、R1 `add_flag` 路徑已移除、自測皆有活斷言。
- **Lesson learning review**：(1) 釘 runtime 只解決一半——系統層 `content_size`／`appearance` 同樣讓量測漂移（LS-167 R7 實證），已入 LS-207（simulator-lock 自動復原）；(2) 「不一致 ⇒ 待清」的計數語意混用會讓 `--brief` 永久失去「無異常」（R1 M1）——新旗標一律先問「會不會讓機械信號永久噪音化」；(3) 本票 R2 自測在 CI 從未跑過（rules 一紅 skip 自測步驟）靠 reviewer 本機補——LS-207 `if: always()` 修。
- **清理**：`cleanup-merged.sh --apply LS-205` → worktree 已移除。
- Pen 清場：不適用。
→ Done（v0.16.0）。m3 文件三處由 LS-207 訂正。

