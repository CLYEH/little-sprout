# LS-162 Task：LS-146 正式站種子前置——review-demo-seed.sh 支援 --owner-email／--member-email 覆寫（真實可收信信箱，OTP 方案 C）＋review-notes.md 登入方式改寫為方案 C（信箱密碼只放 App Store Connect 備註、不進 repo）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:S, lane:backend |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M7 TestFlight |
| 估點 | — |
| 建立 | 2026-09-04T04:59:47.428Z |
| 開始 | 2026-09-04T04:59:47.528Z |
| 完成 | 2026-09-04T07:13:12.006Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-162/taskls-146-正式站種子前置-review-demo-seedsh-支援-owner-email-member-email |
| 父票 | LS-146 Phase 2-3：審核用 demo 帳號＋示範家庭資料（正式站種子：孩子檔案／照片／日記／留言、長期有效邀請碼、Email OTP 備援） |
| 關係 | related→LS-96, related→LS-169, related→LS-70, related→LS-148, related←LS-170, related←LS-164, related←LS-163, related←LS-159 |

## 描述

## 來源

使用者 2026-09-04 12:55 裁決：[LS-146](https://linear.app/little-sprout-app/issue/LS-146/phase-2-3審核用-demo-帳號示範家庭資料正式站種子孩子檔案照片日記留言長期有效邀請碼email-otp-備援) 正式站種子**核可執行**，OTP 方案採 **C（審核員用真實可登入的信箱自己收驗證碼）**。現行 `scripts/ops/review-demo-seed.sh` 把 owner／member email 寫死為 `review-demo@little-sprout.app`／`review-demo-member@little-sprout.app`（`little-sprout.app` 網域非我們所有，[LS-148](https://linear.app/little-sprout-app/issue/LS-148/phase-2-d名稱查重little-sproutapp-store-同名商標備用名決定) 查重：`littlesprout.app` 為競品），方案 C 需要 owner 帳號的 email 是一個審核員能登入查看的真實信箱（使用者將提供一個 Gmail／Workspace 信箱地址）。

## 範圍

1. `scripts/ops/review-demo-seed.sh` 加 `--owner-email <addr>`／`--member-email <addr>`（預設值維持現行常數；`--target prod --yes` 時 **owner-email 必填**、且不得是 `@little-sprout.app`——fail loud 印原因）；冪等鍵（既有 family_id／固定 uuid）不變；`--target prod`（無 `--yes`）計畫輸出要印出將使用的兩個 email。自測：`review-demo-seed.test.sh` 若存在則補案；否則在 handoff 附本機 `--target local --owner-email qa@example.com` 兩次冪等實跑與計數。
2. `docs/store/review-notes.md`：「帳號」表與「登入方式」段改寫為方案 C——owner email 改為佔位 `<REVIEW_OWNER_EMAIL>`（由 orchestrator 執行種子時代入實際地址）、說明審核員登入該信箱取 6 碼、**信箱密碼只寫在 App Store Connect 的 Review Notes，不進 repo**；「OTP 對審核員的問題」段改為「已裁決：方案 C（2026-09-04）」並保留 A／B 的取捨紀錄；「審核結束後輪替信箱密碼」列入送審後待辦。
3. `docs/PLAN.md` §9-B「審核用 demo 帳號」若有描述 OTP 方式，同步一句。

## 不做

正式站實際執行（由 orchestrator 在使用者提供信箱後以 `--target prod --yes --owner-email <addr>` 執行）；密碼登入路徑；固定測試碼。

## 驗收

- [ ] `--target prod` 無 `--yes` 只印計畫且列出 email；`--target prod --yes` 缺 `--owner-email` 或給 `@little-sprout.app` 時 exit 2
- [ ] `--target local --owner-email …` 兩次冪等（計數與 QA R1 `27c87837` 相同）
- [ ] review-notes.md 無任何密碼、owner email 為佔位；方案 C 文字完整
- [ ] merge-reviewer 覆核（scope）

## 2026-09-04 13:22 變更：使用者改採方案 B（帳號密碼登入）

* **保留**：`--owner-email`／`--member-email` 覆寫；`--target prod --yes` 缺 owner-email 或 `@little-sprout.app` 結尾 → exit 2；prod 不連線於檢查前。
* **新增（來源 orchestrator 正式站實查 13:14）**：任何 target 在寫入前查 `auth.users`——owner／member email 已存在且 `id` 不是本腳本固定 uuid（`d1000000…`／member 對應 uuid）→ **exit 2 印該列 id／created_at／所屬家庭數，不得刪**；只有 id＝固定 uuid 才走刪除重建（冪等）。自測：本機先用一般流程建同 email 真實帳號再跑種子 → exit 2 且帳號仍在。
* **新增**：`--owner-password <pw>`（或未給時自動產生 20 字元強密碼、只印在終端一次、不寫檔不進 log）；owner 帳號以密碼建立（`encrypted_password` 用 `crypt(pw, gen_salt('bf'))` 或改走 GoTrue admin API `POST /auth/v1/admin/users` 帶 `password`＋`email_confirm:true`——擇一並說明；本機 `--target local` 後以 `POST /auth/v1/token?grant_type=password` 實跑登入成功作驗證）。member 帳號維持 OTP。
* **review-notes.md 改方案 B**：登入方式＝歡迎頁小字「以帳號密碼登入」→ 輸入 owner email 與密碼；**密碼只寫在 App Store Connect Review Notes，不進 repo**；「OTP 對審核員的問題」段改「已裁決：方案 B（2026-09-04）」保留 A／C 取捨；註明 iOS 密碼登入 UI 由 [LS-163](https://linear.app/little-sprout-app/issue/LS-163/設計歡迎頁以帳號密碼登入小字連結三顆登入鈕下方非大按鈕帳號密碼登入畫面審核帳號用方案-b-ui-designervisual)／[LS-164](https://linear.app/little-sprout-app/issue/LS-164/taskls-17-ios-帳號密碼登入審核帳號用方案) 落地後才可用。
* 方案 C 的「真實信箱」相關文字全部移除。

## 留言（15）

### 2026-09-04T05:09:01.535Z · Chih-Lin Yeh · `4715b326`

## handoff

**Ticket**：LS-162 — LS-146 正式站種子前置：`review-demo-seed.sh` 支援 `--owner-email`／`--member-email`＋`review-notes.md` 改 OTP 方案 C。

**已完成**
- `scripts/ops/review-demo-seed.sh`（`41d4646`）：加 `--owner-email <addr>`／`--member-email <addr>`（`:59-73` 參數解析、`:93-96` override-aware 預設值）；`--target prod --yes` 時 `--owner-email` 必填、且不得以 `@little-sprout.app` 結尾，兩個負案在 `[ -f "$ROOT/.env" ]` 之前就 `exit 2`（`:132-144`）；`print_plan`（`:113`）與所有 SQL／Storage 呼叫（`:270`／`:285-287`／`:498`）本就透過 `OWNER_EMAIL`／`MEMBER_EMAIL` 兩個變數，grep 確認無殘留硬編字串。
- `docs/store/review-notes.md`（`26595b3`）：帳號表 owner email 改佔位 `<REVIEW_OWNER_EMAIL>`（member 保留預設常數，票面允許二選一）；登入方式段改寫「登入該信箱（密碼見 App Store Connect Review Notes）→ app 輸入 email → 取 6 碼」；OTP 段標題改「已裁決：方案 C（2026-09-04）」，保留 A／B 原始取捨紀錄、C 標「已採用」；新增「送審後：輪替 `<REVIEW_OWNER_EMAIL>` 信箱密碼」待辦；「加入家庭」段的 owner email 引用也同步改佔位。全檔 grep 確認無密碼字面、無殘留 `review-demo@little-sprout.app` 舊字面。
- `docs/PLAN.md` §9-B（`26595b3`）：「審核用 demo 帳號」條目加一句同步方案 C 裁決，指回 `docs/store/review-notes.md`。

**已驗證：怎麼驗的**
- `bash scripts/ops/supabase-lock.sh -- supabase db reset` → 兩次 `bash scripts/ops/review-demo-seed.sh --target local --owner-email qa-owner@example.com --member-email qa-member@example.com`（第二次改經 `supabase-lock.sh --` 外層），皆 `exit 0`。
- 獨立 SQL 覆核（另開 `docker exec … psql` session，不採信腳本自印 NOTICE）：`family_members=2 children=2 media=20 diaries=5 diary_children=7 comments=4 reactions=4 invites=1 invite_code=LSDEM7 storage_objects=40`，與 LS-146 QA R1 `27c87837` 基準逐項相同；`auth.users` 兩筆 `email` 為 `qa-owner@example.com`／`qa-member@example.com`（覆寫值，非內建常數）。
- `--target prod`（無 `--yes`，各測不帶／帶 email flag 一次）：只印計畫、`exit 0`，計畫「帳號」列正確印出當下生效的兩個 email；未 `source .env`、未連線。
- `--target prod --yes` 缺 `--owner-email`：`exit 2`，印「需要 --owner-email（不得沿用預設…）」；本機沒有 `.env` 檔可佐證退出點早於 `source`（若曾嘗試 source 會先報「找不到 .env」，實際只印缺值訊息，且未印「⚠ …即將對正式站寫入」那句警告，證明卡在驗證之前）。
- `--target prod --yes --owner-email someone@little-sprout.app`：`exit 2`，印「不得使用 @little-sprout.app 網域」，同樣早於 `.env` 檢查，同樣沒有連線警告字樣。
- 兩個負案全程未帶任何真正可用的 prod 參數、未嘗試連線。
- 收尾：`bash scripts/ops/supabase-lock.sh -- supabase db reset` 還原本機資料庫；`bash scripts/ops/supabase-lock.sh --status` → `free`。
- `bash scripts/gates/pr-body-check.sh --branch feature/LS-162-review-seed-email <body>` 綠；push gate 本機通過（無 Swift 變更，SwiftLint／unit tests 皆跳過，CI 仍會跑）。

**未完成**
- reviewer informational 處置：本票尚無 merge-review 意見（首輪 PR，未見任何 finding 可列）。
- 正式站實際執行與 `<REVIEW_OWNER_EMAIL>` 真實地址代入：待使用者提供信箱後由 orchestrator 以 `--target prod --yes --owner-email <addr>` 執行（票面「不做」範圍，本票未觸碰）。

**風險**
- 純腳本參數新增＋文件改寫，不動 SQL 語意、不動 migration、不動冪等鍵（固定 UUID／`family_id` 不變）。
- 正式站連線路徑（`--yes` 真連線分支）一次都沒觸發，僅讀碼＋負案早退確認。

**產出位置**
- PR：https://github.com/CLYEH/little-sprout/pull/267（`feature/LS-162-review-seed-email` → `development`），head `26595b33e1297dc100cde8f4d94600e6ff7b4c15`
- 腳本：`scripts/ops/review-demo-seed.sh`
- 文件：`docs/store/review-notes.md`、`docs/PLAN.md`
- 模擬器已關：無（本票未 boot 任何模擬器）


### 2026-09-04T05:11:07.444Z · Chih-Lin Yeh · `2003b57f`

orchestrator 2026-09-04 13:10：handoff `4715b326` 收到——PR #267 head `26595b3`（`--owner-email`／`--member-email`；`--target prod --yes` 缺 owner-email 或 `@little-sprout.app` → exit 2 且早於 `.env`；本機兩次冪等計數與 QA R1 基準相同；review-notes 改方案 C、owner email 佔位、無密碼）→ **In Review**，派 merge-review R1。併入後 orchestrator 等使用者提供真實信箱再執行正式站。

### 2026-09-04T05:23:21.750Z · Chih-Lin Yeh · `5ee429a6`

## merge-review R1 — PR #267（head 26595b3）

**REQUEST_CHANGES**（1 blocker、3 minor、2 informational）

審的 head＝`26595b33e1297dc100cde8f4d94600e6ff7b4c15`，與執行當下 `gh pr view 267 --json headRefOid` 一致。delta `6fcc5ee...26595b3` 2 commit、3 檔 +66/−27。本機驗證全程經 `scripts/ops/supabase-lock.sh`（收工已 `db reset` 還原、lock free、worktree `git status` 空）。**正式站一次都沒碰**——只跑了兩個負案，而且用 `bash -x` 機械證明它們在讀任何 prod 連線資訊之前就退出。

---

### B1｜blocker｜`scripts/ops/review-demo-seed.sh:300`（配 `:110`、`:151-158`）——以 email 硬刪 `auth.users`，而 LS-162 剛好把這個 email 變成「一個真人正在用的信箱」

```sql
delete from auth.users where email in ('${OWNER_EMAIL}', '${MEMBER_EMAIL}');
```

LS-146 R1 的 F4 把這句從 `like 'review-demo%'` 收斂成精確比對，理由正是「prod 執行會誤刪撞名的真實使用者」。當時安全，是因為那兩個 email 只有這支腳本自己會建。LS-162 把 email 變成操作者傳入的參數，而且票面明講要傳的是**審核員能登入的真實信箱**——同一個誤刪風險被參數重新打開，而且這次不是撞名巧合，是照票面正式用法必然會踩到。

**失敗情境（本機實跑重現，非推論）**

前置用一般使用者流程建帳號（GoTrue `POST /auth/v1/otp`，`shouldCreateUser` 預設 true，HTTP 200）：

| | BEFORE | AFTER（`--target local --owner-email qa-owner@example.com --member-email qa-member@example.com`，**rc=0**） |
|---|---|---|
| `qa-owner@example.com` | 真實帳號 `d499b99a-c3f6-421b-84f5-4578c66bfaf8`，是「B 的真實家庭」的 **member** | id 變成種子固定 uuid `d1000000-…-000000000001`（原帳號已被刪） |
| `qa-member@example.com` | 真實帳號 `847b4f2f-705f-4e0e-9da3-9b5c05061da3`，無家庭 | id 變成 `d1000000-…-000000000002` |
| 「B 的真實家庭」成員 | owner `ownerb@example.com` ＋ member `qa-owner@example.com` | **只剩 owner**——member 那一列隨 cascade 靜默消失 |

腳本 **exit 0、印「✓ 完成」**，全程沒有任何警告。兩個真人帳號連同其家庭關係就這樣被換掉。

**另一種形狀（同樣實跑）**：若該真實帳號是某家庭的**唯一 owner**，`delete` 會被 `private.enforce_family_has_owner()` 擋下 → `--single-transaction` 整份回捲 → seed `exit 1`，訊息是

```
ERROR:  家庭 aa000000-…-000000000009 必須至少保留一位 owner（請先指派新 owner，再移除或降級原 owner）
CONTEXT:  PL/pgSQL function private.enforce_family_has_owner() line 29 at RAISE
```

帳號僥倖存活——但那是**另一個 trigger 的副作用，不是本腳本的任何一道防線**，而且操作者拿到的是一個跟 email 毫無關係的錯誤訊息。

**對應正式站**：orchestrator 提供的實查（我全程未碰 prod，此事實不出自我）——使用者要用的 `leoyeh906@gmail.com` 在正式站 `auth.users` 已存在（id `cdcfe37d…`，2026-08-25 建立、09-03 有登入、屬 1 個家庭）。照現行邏輯執行 `--target prod --yes --owner-email leoyeh906@gmail.com`，結果就是上面兩種形狀之一，取決於他在那個家庭是不是唯一 owner：一種是**靜默刪掉使用者本人的正式站帳號與家庭關係**（不可逆，正式站沒有第二次機會），另一種是正式站種子倒在一個看不出原因的錯誤上。兩種都不能出貨。

**修法（orchestrator 裁定，R2 必做）**
1. `--target prod --yes` 在**任何寫入之前**先查 `auth.users`：owner／member email 已存在、且 `id` 不是本腳本的固定 uuid（`d1000000-…-01`／`-02`）→ `exit 2`，印出該列的 `id`／`created_at`／所屬家庭數，**不得刪**。
2. 只有 `id` ＝固定 uuid（＝上一輪種子自己建的）才允許刪除重建，走冪等路徑。
3. 同一條檢查也套 `--target local`（保護本機 demo／開發帳號——上面的重現就是在 local 發生的）。
4. R2 自測：本機先用一般流程（GoTrue `/otp`）建一個同 email 的真實帳號，再跑種子 → `exit 2` 且該帳號仍在。可直接沿用我這輪的形狀，我的重現腳本在 scratchpad `LS-162-holdC.sh`（未進 repo）。

實作提醒：檢查與刪除請放在**同一份 SQL／同一個交易**內（現在整份 SQL 已經是 `--single-transaction`，把檢查寫成檔頭的 `do $$ … raise exception`  即可），別做成「shell 先查一次、再送第二個 psql」——那會留下 check-then-act 的窗口，雖然 prod 種子是人盯著跑、窗口實害低，但同一份交易內做既更簡單也沒有窗口。

---

### N1｜minor｜同一處：清理以 **email** 為鍵、重建以**固定 uuid** 為鍵，兩把鑰匙不一致——換 email 重跑必炸

**實跑**：同一個 DB 上先以 `--owner-email qa-owner@example.com` 種好，再以 `--owner-email qa-owner2@example.com` 重跑：

```
ERROR:  duplicate key value violates unique constraint "users_pkey"
DETAIL:  Key (id)=(d1000000-0000-4000-8000-000000000001) already exists.
✗ review-demo-seed：SQL 套用失敗（見上方錯誤），中止——DB 已回捲，Storage 未動
seed rc=1
```

`delete … where email in ('qa-owner2@…','qa-member@…')` 抓不到 email 還停在舊值、id 卻正是固定 uuid 的那一列，於是 insert 撞 PK。回捲乾淨、計數一項沒動（我跑完覆核 14 項全同）——所以是 minor，不是資料損失。

**正式站情境**：地址打錯（或使用者換一個信箱）先種了一次 → 改正重跑 → **一定失敗**，而訊息裡完全沒有「email 換了」這個線索，操作者得自己去正式站手動 `delete from auth.users`。這與 B1 同根，一起修：刪除條件改成 `where id in ('${OWNER_ID}','${MEMBER_ID}')`（自己建的，安全）＋ B1 的存在性檢查（別人的，不准刪）。

---

### N2｜minor｜`docs/store/review-notes.md:3-5`——檔頭還寫著「本票只列選項不下決定」，跟 `:68`「已裁決：方案 C」自相矛盾

```
**送審前需使用者核可**（尤其是下方「OTP 對審核員的問題」一節——那是產品裁決，本票只列選項不下決定）。
```

票面 §2 要求把 OTP 段改成「已裁決」，這句是同一件事的另一半，PR 改了下面沒改上面。這是照著這份填 App Store Connect Review Notes 的人**第一眼看到的句子**，讀完會以為登入方式還沒定案。一句話的事。

### N3｜minor｜`docs/store/review-notes.md:12`——Member 列還是 `review-demo-member@little-sprout.app`、登入方式「同上」，但方案 C 下沒有人收得到那個網域的信

方案 C 只解決 owner 的收信問題。備註表把 member 列成一組「Email 驗證碼（見下）」的可用帳號，審核員若照表操作去登 member 帳號，驗證碼寄到我們不擁有的網域、永遠收不到 → 卡在驗證碼畫面。**App Review Notes 裡出現一組「照著做一定失敗」的憑證**，最壞的情況不是浪費審核員時間，是讓他判定整份備註不可信。

不是要改腳本的 member 預設值（票面允許沿用常數），是要改備註別叫審核員去登它：拿掉 Member 列，或明講「member 帳號僅供示範第二成員的資料，審核員不需要、也無法登入」。

---

### Informational

- **i1｜prod 只擋「空值」與「`@little-sprout.app`」，不擋明顯不是 email 的字串**：`--owner-email nonsense` 會照種下去。一個 `case "$OWNER_EMAIL" in *@*.*) ;; *) exit 2 ;; esac` 等級的檢查幾乎零成本，擋掉手殘漏 `@` 的代價差別是「當場 exit 2」還是「正式站種完、送審前才發現收不到信、再跑一次 prod」。順帶（**PLAUSIBLE，未實跑**）：email 值是原封不動插進 SQL 字面（`:300`、`:315-317`），含單引號的地址會直接壞掉 SQL——`--single-transaction` 會回捲、fail loud，無資料風險。heredoc 雖然沒加引號，但 bash 不會對變數**值**做二次展開，沒有 shell 注入面，這點我確認過。
- **i2｜網域比對大小寫敏感（實測）**：`--owner-email someone@LITTLE-SPROUT.APP` **通過**了網域檢查（它 rc=2 是因為後面找不到 `.env`，第一行 stderr 已經是「⚠ 即將對正式站寫入」那句警告，代表檢查沒擋住）；`someone@little-sprout.app`＋尾隨空白同理。DNS 不分大小寫，所以那是同一個收不到信的網域。實害低（地址由使用者提供、操作者是自己人），想收緊就先 `tr A-Z a-z` 再比，順手做即可。

---

### 已查實通過的部分

- **fail-closed 順序（讀碼＋`bash -x` 機械證明）**：兩個負案的完整 trace 裡，`.env`／`SUPABASE_DB_URL`／`psql`／`curl`／`supabase status` 出現次數 **0**，trace 最後一行就是 `+ exit 2`。缺 `--owner-email` → exit 2；`@little-sprout.app` → exit 2。額外保險：worktree 根本沒有 `.env`（`ROOT` 指向 worktree），就算檢查失效也連不上。
- **`--target prod` 無 `--yes`**：`exit 0`，計畫的「帳號」列正確印出當下生效的兩個 email（不帶 flag 印預設、帶 flag 印覆寫值，兩種都跑過），不連線。
- **冪等（本機實跑兩次，全程持鎖）**：`--target local --owner-email qa-owner@example.com --member-email qa-member@example.com` 連跑兩次皆 `exit 0`，獨立 psql session 覆核（不採信腳本自印 NOTICE）逐項與 LS-146 QA R1 `27c87837` 基準相同：`family_members=2 children=2 media=20 diaries=5 diary_children=7 comments=4 reactions=4 invites=1 invite_code=LSDEM7 feed_items=25 storage_objects=40 zero_byte=0 orphan_orig=0 orphan_thumb=0`；`auth.users` 兩列 email 確實是覆寫值、id 是固定 uuid。
- **email 變數貫穿**：全 repo grep `review-demo@little-sprout.app` 只剩 `:110` 的預設常數與 `:152` 的錯誤訊息，兩處都合理；SQL 的建帳號／清理／profiles／收尾輸出與 `print_plan` 全走 `OWNER_EMAIL`／`MEMBER_EMAIL`。冪等鍵（`FAMILY_ID`／固定 uuid／`INVITE_CODE`／`SEED_YM`）不因 email 變（B1／N1 的問題正是「清理鍵變了、重建鍵沒變」，不是冪等鍵被動到）。
- **review-notes.md 無憑證**：全檔 grep `密碼/password/token/secret/apikey` 只有「密碼寫在 App Store Connect、不進 repo」這類敘述，沒有任何值；owner 一律 `<REVIEW_OWNER_EMAIL>` 佔位（含「加入家庭」段的引用）；方案 C 步驟完整（登入信箱 → app 輸入 email → 取 6 碼）；A／B 取捨原文保留並標「未採用」；「送審後輪替信箱密碼」待辦已列。使用者的真實信箱**沒有**進 repo。
- **`docs/PLAN.md` §9-B**：只加一句、掛在既有「審核用 demo 帳號」條目末尾，內容與裁決一致並指回 review-notes.md，正確。
- **CI**：`gh pr checks 267` 四項全綠（`ci` 6m35s／`db` 3m13s／`lint` 11s／`rules` 3m9s）。`bash -n` 語法乾淨（本機無 shellcheck，未跑）。
- **PR body gate**：`pr-body-check.sh --branch feature/LS-162-review-seed-email --verify` → rc=0。
- **scope：乾淨**。3 檔全部對得上票面 §1／§2／§3，沒有無關重構、沒有順手改動、沒有 app 程式碼與 migration。
- **race／效能／平行**：這支腳本是單程序、嚴格循序，本 delta 只新增兩個參數與兩個早退檢查，沒有引入任何並發面；local 路徑仍照既有慣例 re-exec 進 `supabase-lock.sh`（LS-70）。無 SQL/RLS policy 變更、無 app 端查詢，效能維度無可審之處。40 次上傳仍串行——LS-146 R1 i3 已裁定維持，本 PR 未動，不重開。唯一與時序有關的建議寫在 B1 修法提醒（檢查與刪除要同交易）。

### 明講沒審的部分

- **正式站完全未碰**：只執行了兩個必定早退的負案，沒有帶過任何可用的 prod 參數。B1 引用的正式站帳號事實出自 orchestrator 的實查，不是我驗的。
- `:174` 的 host-psql `-f` 路徑仍無法實測（本機 host 沒有 psql，走的是 docker stdin），與 LS-146 R2／R3 同一個盲區——而 prod 走的正是 `-f` 那條。
- 未跑模擬器／未複驗 review-notes.md 對 app 文案的逐字一致（LS-146 QA R1 已驗過，本 PR 未動那些段落）。
- i1 的單引號 email 未實跑（標 PLAUSIBLE）。

---

### 結論

參數解析、fail-closed 的退出順序、email 貫穿與冪等這四項，作者做得乾淨，我逐項用機械證據查實（`bash -x` trace 零連線、獨立 SQL 覆核計數與 QA 基準逐項相同）。擋下來的是這個改動的**目的本身帶來的後果**：把 owner email 變成參數，等於把「刪 `auth.users` 的比對值」交給操作者，而票面要傳的正是一個真人在用的信箱——LS-146 F4 費工收斂掉的誤刪風險，就這樣從正門走回來了。我在本機把它完整重現：seed **exit 0** 宣告成功，兩個真實帳號連同家庭成員關係被靜默換掉。這條在正式站不可逆，必須在 `--target prod --yes` 執行前修掉。N1 與它同根，一起改；N2／N3 是備註文件送審前必須自洽的兩句話。

R2 我只審 delta，重點是 B1 的四項（含 local 也要套、含「真實帳號存在 → exit 2 且帳號仍在」的實跑證據）。

**REQUEST_CHANGES**

<sub>reviewer: merge-reviewer · base `6fcc5ee`（merge-base，development 現為 `4fa42ac`、無檔案交集）· 審的 head `26595b33e1297dc100cde8f4d94600e6ff7b4c15`（＝執行當下 PR head）· 本機驗證全程經 `scripts/ops/supabase-lock.sh`；收工已 `db reset` 還原（auth.users／families／storage.objects 皆 0）、lock free、worktree 未改動</sub>


### 2026-09-04T05:25:10.768Z · Chih-Lin Yeh · `e709ef4f`

orchestrator 2026-09-04 13:24：merge-review R1 `5ee429a6` **REQUEST_CHANGES**——B1 blocker：`:300` 以 email 硬刪 `auth.users`，reviewer 本機重現「傳入真人帳號 email → seed exit 0、真人帳號 id 被換成種子 uuid、家庭成員列 cascade 消失」（正式站事實：`leoyeh906@gmail.com` 即本人帳號，若非唯一 owner 就會這樣被吃掉；若是唯一 owner 則靠 `enforce_family_has_owner` 副作用回捲，兩種都不能出貨）；N1 清理鍵 email／重建鍵 uuid 不一致；N2 檔頭矛盾句；N3 Member 列叫審核員登收不到信的帳號；i1 email 格式／單引號；i2 網域比對大小寫。冪等計數與 QA 基準逐項相同、負案退出早於 `.env`、CI 四綠、status failure 已貼 `26595b3`。→ 派原 agent **R2**：B1 同交易 `do $$ raise` guard（id≠固定 uuid 即拒、以 id 為清理鍵）＋自測「真人帳號仍在」、**方案 B 轉向**（`--owner-password`、密碼登入實跑驗證、review-notes 改 B、方案 C 文字移除）、i1／i2。

### 2026-09-04T05:46:20.540Z · Chih-Lin Yeh · `f3b6d173`

## R2 handoff（回應 merge-review R1 `5ee429a6` REQUEST_CHANGES＋票文 2026-09-04 13:22 方案 B 轉向）

**分支**：`feature/LS-162-review-seed-email`（同 worktree），PR #267，head `09418d799ba58e470bc6d352c16cb1f4242ee851`。

### R1 處置

| # | 嚴重度 | 處置 | 位置 | commit |
|---|---|---|---|---|
| B1 | blocker | 寫入前在同一份 SQL、同一個 `--single-transaction` 交易內先查 owner／member email 是否已存在且 `id` 不是本腳本固定 uuid（別人的帳號）→ `raise exception` 印該列 id／created_at／所屬家庭數，整份回捲、fail loud，絕不刪除；只有 id＝固定 uuid 才視為自己的列。local／prod 一視同仁，同一筆交易內完成（非 shell check-then-act） | `scripts/ops/review-demo-seed.sh:356-380`（guard）／`:401`（清理改用 id） | `d41f4c9` |
| N1 | minor | 清理鍵從 `email in (...)` 改成 `id in (...)`——email 換了但 id 沒變時仍視為自己的列刪除重建，換 email 重跑不再撞 `users_pkey` | `scripts/ops/review-demo-seed.sh:401` | `d41f4c9` |
| N2 | minor | review-notes.md 檔頭「本票只列選項不下決定」與「已裁決」自相矛盾，移除，改為明確「已裁決採方案 B」 | `docs/store/review-notes.md:3-5` | `09418d7` |
| N3 | minor | Member 帳號表列改註記「僅供示範資料，審核員不需要、也無法登入」 | `docs/store/review-notes.md:12` | `09418d7` |
| i1 | informational | email／password 值格式健檢（缺 `@`、網域缺 `.`）＋拒絕單引號 | `scripts/ops/review-demo-seed.sh:79-97`（`validate_email()`）／`:155-156`／`:253-255` | `d41f4c9` |
| i2 | informational | `--target prod --yes` 的 `@little-sprout.app` 網域比對改大小寫不敏感 | `scripts/ops/review-demo-seed.sh:200-207` | `d41f4c9` |

### 方案 B 轉向（密碼建法）

`--owner-password <pw>`：未給時自動產生 20 字元英數強密碼、只印終端一次（不寫檔不進 log，僅短暫存在 `mktemp -d` 暫存目錄，trap 清除）——`:240-255`。owner 的 `encrypted_password` 用 **pgcrypto 的 `crypt(pw, gen_salt('bf'))`（bcrypt）直接在種子 SQL 內雜湊**（取捨：不改走 GoTrue admin API，理由是這支腳本從頭到尾都是 postgres 身分直寫 SQL，admin API 會是唯一跳出這個模式、多一次網路往返、且落在 `--single-transaction` 保護之外的步驟——見腳本檔頭 `:53-62`）；執行前先查 `pg_extension` 有無 `pgcrypto`，缺了 fail loud（`:382-392`）；補 `email_confirmed_at` 讓 `grant_type=password` 免確認流程即可登入（`:394-410`）。member 帳號維持 Email OTP 不受影響。

review-notes.md／PLAN.md §9-B 全面改寫方案 B：登入方式＝歡迎頁小字「以帳號密碼登入」；OTP 段標題「已裁決：方案 B（2026-09-04）」，保留 A／C 取捨紀錄（方案 C「登入該信箱收 6 碼」的操作指示已自主要段落移除，只在取捨紀錄裡保留歷史記錄）；註明 iOS 密碼登入 UI 由 LS-163（設計）／LS-164（實作）落地、送審前必須確認完成；「送審後：輪替信箱密碼」改「輪替帳號密碼」。

### 已驗證：怎麼驗的

全程經 `scripts/ops/supabase-lock.sh`（含一次因 `qa-test` worktree 持鎖跑 `db reset && supabase/tests/run.sh` 等待約 1 分鐘，未搶鎖、未動別人的鎖）：

- 冪等（方案 B）：`db reset` → 兩次 `--target local --owner-email qa-owner@example.com --member-email qa-member@example.com --owner-password 'Qa2026DemoPassw0rdXY'`，皆 `exit 0`。獨立 SQL 覆核：`family_members=2 children=2 media=20 diaries=5 diary_children=7 comments=4 reactions=4 invites=1 invite_code=LSDEM7 storage_objects=40`，與 LS-146 QA R1 `27c87837` 基準相同；owner 列 `encrypted_password`／`email_confirmed_at` 皆非空，member 列皆為空。
- 密碼登入實測：`POST /auth/v1/token?grant_type=password`（同一組 email／密碼）→ 200，回傳 `access_token`／`sub=d1000000-…-01`／`amr=[{"method":"password"}]`。
- **B1 自測**：先用一般 `POST /auth/v1/otp {create_user:true}` 建真實帳號 `holdtest-owner@example.com`（隨機 uuid）→ 帶同一 email 跑種子 → `exit 1`，訊息印出 id／created_at／所屬家庭數（`=0`）；種子後複查該帳號**仍存在、id 不變**。
- **N1 自測**：種好 `qa-owner@example.com` 後改用 `--owner-email qa-owner2@example.com` 重跑（同一組固定 uuid）→ `exit 0`，該 uuid 的 email 正確更新，未撞 `users_pkey`。
- i1：`--owner-email nonsense` → `exit 2`；`--owner-email "qa'owner@example.com"` → `exit 2`；`--owner-password "Qa'2026Demo"` → `exit 2`。
- i2：`--target prod --yes --owner-email someone@LITTLE-SPROUT.APP`（大寫）→ `exit 2`。
- 原 R1 負案重驗（缺 `--owner-email`／小寫 `@little-sprout.app`／`prod` 無 `--yes`）：`bash -x` 完整 trace 機械確認 `.env`／`SUPABASE_DB_URL`／`SUPABASE_SERVICE_ROLE_KEY`／`source` 出現次數為 0，trace 最後一行是 `+ exit 2`。
- 收尾：`supabase db reset` 還原；`supabase-lock.sh --status` → `free`。
- `pr-body-check.sh --branch feature/LS-162-review-seed-email` 綠；push gate 本機通過。

**過程中修正的一個自造 bug**：R2 編輯最終摘要 echo 時 `$MEMBER_EMAIL` 緊接全形括號（無分隔），本機 bash 把括號位元組併入變數名、`unbound variable`——改用 `${MEMBER_EMAIL}` 加大括號後修復並重跑驗證。

**未完成**：iOS 密碼登入 UI（LS-163／LS-164）尚未落地，review-notes.md 已註明「送審前必須確認完成」；正式站實際執行待使用者提供 owner email／密碼後由 orchestrator 執行（票面「不做」範圍）。reviewer informational（i1／i2）處置見上表，無其他未處置項。

**風險**：假設正式站有 `pgcrypto` extension（Supabase 專案預設帶——若沒有，執行前的存在性檢查會 fail loud，不會無聲存明文或跳過密碼）；正式站連線路徑一次都沒觸發。

**產出位置**：PR https://github.com/CLYEH/little-sprout/pull/267（`feature/LS-162-review-seed-email` → `development`），head `09418d799ba58e470bc6d352c16cb1f4242ee851`；模擬器已關：無（本輪未 boot 任何模擬器；`qa-test-iPhone17Pro`／`qa-test-iPadPro13` 是其他 worktree 的機，非本票所開）。


### 2026-09-04T05:47:02.222Z · Chih-Lin Yeh · `c9387646`

orchestrator 2026-09-04 13:48：R2 handoff `f3b6d173` 收到——PR #267 head `09418d7`（B1 同交易存在性 guard `:356-380`：真人帳號 email 傳入 → raise 回捲、帳號 id 不變；N1 清理鍵改固定 id `:401`、換 email 重跑不撞 PK；i1 格式／單引號、i2 大小寫；**方案 B**：`--owner-password` 未給自動產生、`pgcrypto crypt(bf)` 寫 `encrypted_password`＋擴充前檢、本機 `grant_type=password` 登入拿 token；review-notes 改 B；冪等計數同基準；負案 `bash -x` 零 `.env`）→ 派 merge-review R2（重點：正式站 `pgcrypto` 存在、密碼零落地、guard 與刪除同交易、GoTrue 直寫欄位相容）。APPROVE 後併 development；正式站種子執行等本票隨批次到 main 後（腳本從主 checkout 跑、讀 `.env`）。

### 2026-09-04T06:16:56.242Z · Chih-Lin Yeh · `ba82858c`

## merge-review R2 — PR #267（head 09418d7）

**REQUEST_CHANGES**（0 blocker、1 major、0 minor、4 informational）

審的 head＝`09418d799ba58e470bc6d352c16cb1f4242ee851`，與執行當下 `gh pr view 267 --json headRefOid` 一致。R2 delta `26595b3..09418d7` 2 commit、3 檔 +187/−67。本機驗證全程經 `scripts/ops/supabase-lock.sh`（其中一段等鎖 819 秒，等到 qa-test／LS-169 放手才進場，沒有搶鎖）；收工已 `db reset` 還原、worktree `git status` 空、暫存目錄無殘留。**正式站零連線**。

**R1 的 blocker B1 我確認修好了**，而且是用我 R1 那支重現腳本的同一個形狀重跑查實的。擋下來的是同一道 guard 的一個側門：它比對 email 用的是大小寫敏感的等值，而 GoTrue 對 email 是大小寫不敏感的——**帶一個有大寫字母的地址就能繞過整道 guard，而且 seed 回報成功**。

---

### R1 findings 複核（逐項實跑）

**B1｜blocker → 已修，正反兩向查實**（`review-demo-seed.sh:356-380` guard／`:401` 清理改 id）

先用一般使用者流程（GoTrue `POST /auth/v1/otp`）建三個真人帳號，其中 `qa-owner@example.com` 是「B 的真實家庭」的 member、`qa-member@example.com` 無家庭，再把種子指向這兩個 email：

```
seed rc=1
ERROR:  review-demo-seed 拒絕執行：email qa-owner@example.com 已存在於 auth.users，
        id=9d029ac7-245e-45bc-999b-ba9db30c2e3b（created_at=2026-09-04 05:50:19.533709+00，
        所屬家庭數=1）不是本腳本固定 uuid——這是別人的帳號，不會被刪除。
CONTEXT:  PL/pgSQL function inline_code_block line 12 at RAISE
✗ review-demo-seed：SQL 套用失敗……中止——DB 已回捲，Storage 未動
```

- **AFTER 狀態逐列比對**：三個真人帳號 **id 一個都沒變**、「B 的真實家庭」的 owner＋member 兩列**都還在**。對照 R1 同一個輸入的結果（seed exit 0、id 被換成種子 uuid、member 那列 cascade 消失）——這正是要修的那件事。
- 另做一次單獨的 exit code 量測（第三個真人 email `realperson@example.com`，所屬家庭數=0）：**seed rc=1**，訊息同樣印出 id／created_at／家庭數，該帳號查詢仍在、id 不變。
- **同一份 SQL、同一個交易**：guard 是 `$work/seed.sql` 檔頭的 `do $$ … raise exception`，與後面的 delete／insert 同一個檔；`run_sql`（`:270` host psql `-f`／`:273` docker stdin）兩條路徑都帶 `--single-transaction`。**不是 shell check-then-act**（全檔只有一次 `run_sql` 呼叫走到 DB，我確認過沒有第二個 psql 前置查詢）。
- **local／prod 都走得到**：guard 寫在共用的 SQL 產生區，不在任何 `target` 分支裡；我的重現全部是 `--target local`，就是 R1 要求「local 也要套」的那一條。
- **Storage 未被觸碰**：失敗 log 裡沒有「清理既有 Storage」「上傳 Storage」任何一行（LS-146 R2 N1 的排序仍然成立）。

**N1｜minor → 已修，查實**（`:401` `delete from auth.users where id in ('${OWNER_ID}', '${MEMBER_ID}')`）

同一個 DB 先用 `--owner-email qa-owner@example.com` 種好，再用 `--owner-email qa-owner2@example.com` 重跑：**rc=0**，log 內 `users_pkey` 0 次，固定 uuid `d1000000-…-01` 的 email 正確更新成 `qa-owner2@example.com`。R1 那個「換 email 重跑必撞 PK、要人手動去 prod 刪列」的死路已經沒了。

**N2／N3｜minor → 已修**（`docs/store/review-notes.md`）
檔頭矛盾句改成「登入方式已裁決採方案 B（帳號密碼登入，2026-09-04）」，與 `:73` 的段落標題一致；Member 列改成「帳號僅供示範資料，**審核員不需要、也無法登入**」、登入方式欄改 `—`。兩條都對到位。

**i1／i2｜informational → 已修，逐案實跑**

| 輸入 | rc | 訊息 |
|---|---|---|
| `--owner-email nonsense` | 2 | 看起來不像 email（缺 @） |
| `--owner-email no-dot@localhost` | 2 | 網域缺 . |
| `--owner-email @example.com` | 2 | @ 前面是空的 |
| `--owner-email "qa'owner@example.com"` | 2 | 不可含單引號 |
| `--member-email "qa'member@example.com"` | 2 | 不可含單引號 |
| `--owner-password "Qa'2026Demo"` | 2 | 不可含單引號 |
| `--target prod --yes --owner-email someone@LITTLE-SPROUT.APP` | 2 | 不得使用 @little-sprout.app（大寫也擋住了） |
| 同上 `…@Little-Sprout.App` | 2 | 同上 |

---

### M1｜major｜`review-demo-seed.sh:363-367`——guard 的 email 比對大小寫敏感，GoTrue 不是；帶大寫的地址整道 guard 直接失效，而且 **seed 回報成功**

```sql
where email in ('${OWNER_EMAIL}', '${MEMBER_EMAIL}')
  and id not in ('${OWNER_ID}', '${MEMBER_ID}')
```

**先確立兩個機械事實（都實測）**
1. GoTrue **寫入時正規化成小寫**：`POST /auth/v1/otp` 帶 `Mixed@Example.com`，`auth.users` 存進去的是 `mixed@example.com`。
2. GoTrue **查詢時大小寫不敏感**：乾淨庫用 `--owner-email Reviewer@Example.com` 種一次（種子是逐字寫入，不正規化），`grant_type=password` 用 `Reviewer@Example.com` 登入 **200**、用全小寫 `reviewer@example.com` 登入也 **200**。（這條是對照組——單純大小寫混合本身**不會**壞掉登入，我原本猜會，實測推翻了，所以下面的問題單純出在 guard 比對。）

**失敗情境（實跑重現）**：真人帳號 `mixed@example.com` 已存在（id `8f961f3a-582e-487f-8a1d-d2835c11b77d`），操作者把地址打成 `Mixed@Example.com` 跑種子：

```
seed rc=0
✓ review-demo-seed 完成：20 個原檔 + 20 個縮圖已上傳
```

```
                  id                  |         email         
--------------------------------------+-----------------------
 8f961f3a-582e-487f-8a1d-d2835c11b77d | mixed@example.com      ← 真人帳號
 d1000000-0000-4000-8000-000000000001 | Mixed@Example.com      ← 種子又建了一個
```

- **guard 一聲都沒響**：`'Mixed@Example.com' in (...)` 對不上存成小寫的那列。
- `auth.users` 的 email 唯一索引也是大小寫敏感的，**擋不住**——同一個人類可見地址現在有兩列。
- **兩種寫法都登不進去**：`Mixed@Example.com` → **400**、`mixed@example.com` → **400**。GoTrue 的大小寫不敏感查詢碰到兩列相符，解析到的不是種子那列。也就是說：**種子宣告成功，審核帳號卻是死的**。

**對應正式站**：目標地址 `leoyeh906@gmail.com` 已經存在於 prod（orchestrator 13:14 實查，我沒碰 prod）。只要執行時地址帶了任何一個大寫字母（`LeoYeh906@gmail.com` 是很多人寫自己信箱的方式），結果就是上面這張表：guard 靜默失效、prod 多一列重複身分、審核員拿到一組登不進去的帳號，而收拾殘局要人工上正式站刪掉種子那列再重跑。**這正是這輪 blocker 修法要防的情境，卻能被一個大寫字母繞過**——一道能被大小寫繞過的 guard 不算 guard。

沒列成 blocker 的理由：真人帳號**沒有被刪**（`:401` 改用固定 id 當清理鍵之後，資料毀損那條路是真的封死了），損害限於「多一列＋帳號不可用」，可人工回復。

**修法**（一行，跟 i2 已經做過的正規化同一招）：guard 改成 `lower(email) in (lower('${OWNER_EMAIL}'), lower('${MEMBER_EMAIL}'))`；建議連 `insert into auth.users` 的 email 也一併寫成 `lower('${OWNER_EMAIL}')`，跟 GoTrue 自己的儲存慣例對齊（順帶讓「同一地址兩列」在唯一索引上就不可能發生）。R3 自測：先用一般流程建 `mixed@example.com`，再用 `--owner-email Mixed@Example.com` 跑種子 → 應 exit≠0 並印出那列 id。

---

### 方案 B（密碼登入）查核

- **端到端登入實測**：`POST /auth/v1/token?grant_type=password` → **200**，回 `access_token`。錯誤密碼 → **400**；member 帳號（無密碼）→ **400**。
- **雜湊**：owner 列 `encrypted_password` 前綴 `$2a$06$`、長度 60（pgcrypto bcrypt），`email_confirmed_at` 非空，`aud`／`role` 皆 `authenticated`；member 列 `encrypted_password`／`email_confirmed_at` 皆 NULL，OTP 路徑不受影響。
- **自動產生密碼路徑（＝正式站預設用法）**：不帶 `--owner-password` 跑一次——提示訊息在整份 log 裡出現 **1 次**、密碼長度 20，拿它去 `grant_type=password` 登入 **200**。`--target prod` 乾跑（無 `--yes`）**不會**產生也不會印密碼（實測計數 0），密碼解算確實排在 prod-no-yes 的 `exit 0` 之後。
- **暫存檔**：密碼會逐字寫進 `$work/seed.sql`（`mktemp -d`，`:280` `trap 'rm -rf "$work"' EXIT`）；跑完檢查 `${TMPDIR}/ls146-review-demo-seed.*` **無殘留**（成功與失敗兩種收場都驗過）。
- **pgcrypto 前檢**：本機 `pgcrypto` 裝在 `extensions` schema，連線 `search_path` 是 `"$user", public, extensions`，`crypt(text,text)`／`gen_salt(text)` 兩支都 resolvable。缺 extension 的 raise 形狀我用不存在的 extname 複驗同一段邏輯：`ERROR: … 拒絕執行：pgcrypto extension 未安裝`、psql rc=3（`ON_ERROR_STOP`）→ `run_sql` 失敗 → shell `exit 1`，**fail loud 成立，不會無聲存明文**。

---

### Informational

- **i-a｜正式站 `pgcrypto` 我無法驗，請 orchestrator 在跑 prod 前確認**：我這個 agent 的工具清單裡**沒有** `mcp__supabase__list_extensions`（只有 Bash／Read／Linear），而連正式站是禁止的——所以這條是我這輪唯一一個「派工要求但我做不到」的項目，明講在這裡不含糊帶過。緩解是上面驗過的 fail-loud 前檢：真的缺了會整份回捲、不會半套。
- **i-b｜前檢查的是「extension 有沒有裝」，不是「`crypt` 叫不叫得到」**：`select 1 from pg_extension where extname='pgcrypto'` 對 schema／search_path 一無所知。pgcrypto 裝在 `extensions` schema，若 prod 連線的 search_path 不含它，前檢會過、然後在 `crypt(...)` 那行才炸 `function crypt(text, text) does not exist`——仍在交易內、仍 fail loud，所以無資料風險，只是錯誤訊息離現場遠一點。改成 `to_regprocedure('crypt(text,text)') is not null`（我上面就是用這個查的）成本相同而且直接驗到可呼叫性。
- **i-c｜`gen_salt('bf')` 用的是 pgcrypto 預設 cost 6（實測 `$2a$06$`），GoTrue 自己建帳號是 cost 10**：對這個場景沒有實害（20 字元隨機英數約 119 bits，而且這組密碼本來就要公開在 App Store Connect Review Notes 給審核員），想對齊就寫 `gen_salt('bf', 10)`。
- **i-d｜「不寫進任何檔案／log」這句要收窄**（腳本檔頭 `:53-56`）：對自動產生的密碼（＝正式站預設路徑）是準的；但操作者**顯式**帶 `--owner-password` 跑 `--target local` 時，腳本會把原始參數 re-exec 進 `supabase-lock.sh`，而 lock 會把整條命令列寫進 `/tmp/supabase-lock-little-sprout/holder`（我用假密碼實測讀回來確認，檔案權限 `-rw-r--r--`，執行期間存在）；`ps` 也看得到。prod 分支不經 lock、預設又是自動產生，所以送審那組密碼不受影響——只是那句絕對化的宣稱建議加上「顯式傳入時會出現在命令列與 lock holder 檔」。

---

### 其他查核

- **冪等**：`db reset` 後連跑兩次（皆帶 `--owner-email`／`--member-email`／`--owner-password`）皆 `exit 0`，獨立 psql session 覆核逐項與 LS-146 QA R1 `27c87837` 基準相同：`family_members=2 children=2 media=20 diaries=5 diary_children=7 comments=4 reactions=4 invites=1 invite_code=LSDEM7 feed_items=25 storage_objects=40 zero_byte=0 orphan_orig=0 orphan_thumb=0`。
- **fail-closed**：`--target prod --yes` 缺 `--owner-email` 的 `bash -x` 完整 trace 裡，`.env`／`SUPABASE_DB_URL`／`SUPABASE_SERVICE_ROLE_KEY`／`source`／`psql`／`curl`／`openssl` 出現次數 **0**，末行 `+ exit 2`。`validate_email` 又比 prod 分支更早，格式不合根本走不到那裡。
- **文件**：review-notes.md 全檔 grep 無任何密碼／token 值、無真實信箱；owner 一律 `<REVIEW_OWNER_EMAIL>` 佔位；方案 B 文字完整（歡迎頁小字→輸入 email＋密碼→直接進示範家庭），並明列前置條件「LS-163／LS-164 落地前審核員看不到入口，這份備註無法照做」；A／C 取捨紀錄保留並標「未採用」，方案 C 的操作指示已自主要段落移除；「送審後輪替帳號密碼」待辦已改寫。腳本內也沒有殘留「owner 走 OTP」的舊說法（`OTP` 字樣只出現在 member 相關敘述）。`docs/PLAN.md` §9-B 同步一句、指向 LS-163／LS-164，正確。
- **CI**：`gh pr checks 267` 四項全綠（`ci` 5m14s／`db` 3m21s／`lint` 13s／`rules` 3m15s）。`bash -n` 乾淨（本機無 shellcheck，未跑）。
- **PR body gate**：`pr-body-check.sh --branch feature/LS-162-review-seed-email --verify` → rc=0。
- **scope：乾淨**。全 PR 仍只有 3 檔，R2 delta 每一段都對得上 R1 findings 或票文 13:22 的方案 B 條目，無順手改動、無 app 程式碼、無 migration。
- **race／效能／平行**：單程序循序，本 delta 沒有引入並發面。唯一與時序有關的是 guard——它在**同一交易內**完成檢查與刪除，沒有 check-then-act 窗口（這正是 R1 建議的做法，作者採用了）。無 SQL/RLS policy 變更、無 app 端查詢。
- **環境雜訊（不影響判定，LS-70 既有）**：本輪共用容器上另有 qa-test（持鎖 13 分鐘做 UI 驗收）與 LS-169（`db reset && tests/run.sh`）在跑，我等了 819 秒才進場、沒有搶鎖；中途有一列別人的 `qa-smoke-ipad@example.com` 出現在我的庫裡（我的計數都是 family 範圍，不受影響）。

### 明講沒審的部分

- **正式站零連線**：只跑必定早退的負案。正式站 `pgcrypto` 是否存在（i-a）**我驗不了**，工具不在我手上。
- `:270` 的 host-psql `-f` 路徑仍無法實測（本機 host 沒 psql，走 docker stdin），與 LS-146 R2／R3 同一盲區——prod 走的是 `-f` 那條。
- iOS 端密碼登入 UI（LS-163／LS-164）不在本 PR，未審；未跑模擬器。
- 未複驗 review-notes.md 對 app 既有文案的逐字一致（LS-146 QA R1 驗過，本 PR 未動那些段落）。

---

### 結論

B1 修得對而且修在對的地方——同一交易內的 `do $$ raise`、清理鍵換成固定 uuid、local／prod 一視同仁，我用 R1 的重現腳本跑回來：seed `exit 1`、真人帳號 id 原封不動、家庭成員列還在、Storage 沒被碰、DB 回捲。N1／N2／N3／i1／i2 也逐條查實。方案 B 的密碼路徑端到端可用（自動產生→登入 200、錯誤密碼 400、member 無密碼 400），fail-loud 前檢有效。

留下來的是同一道 guard 的側門：它用大小寫敏感的等值去比對一個 GoTrue 視為大小寫不敏感的欄位。我實測到 GoTrue 寫入正規化成小寫、查詢又不分大小寫，於是「真人帳號存小寫、操作者輸入帶大寫」時 guard 完全不響、seed 照樣 `exit 0` 說完成，結果是 prod 上同一個地址兩列、兩種寫法都登不進去。單純大小寫混合本身不會壞（我特地跑了乾淨庫對照組，兩種寫法都 200），所以問題明確就在 guard 的比對方式，一行 `lower()` 收掉。既然這道 guard 是這輪 blocker 的全部修法，而它保護的目標帳號在 prod 上確定存在，我不能讓一個大寫字母就把它繞過去。

R3 我只審 delta，重點是 M1 的 `lower()` 比對（含 insert 是否一併正規化）與那條大小寫重現的自測。

**REQUEST_CHANGES**

<sub>reviewer: merge-reviewer · base `6fcc5ee`（merge-base）· R2 delta `26595b3..09418d7` · 審的 head `09418d799ba58e470bc6d352c16cb1f4242ee851`（＝執行當下 PR head）· 本機驗證全程經 `scripts/ops/supabase-lock.sh`（一段等鎖 819 秒未搶）；收工已 `db reset`、worktree 未改動、暫存無殘留；正式站零連線</sub>


### 2026-09-04T06:18:05.145Z · Chih-Lin Yeh · `e5a56f4f`

orchestrator 2026-09-04 14:17：merge-review R2 `ba82858c` **REQUEST_CHANGES**（B1／N1 已修實證通過：真人帳號 rc=1 原封不動、換 email 重跑 rc=0；剩 1 major：guard `email in (...)` 大小寫敏感，GoTrue 正規化小寫 → 混合大小寫真人帳號漏擋、同址兩列兩種寫法登入都 400，reviewer 實跑重現）→ 派原 agent R3（`lower()` 兩處＋兩新案）。i-a **正式站 `pgcrypto` 已確認安裝**（`list_extensions`：`pgcrypto 1.3`，schema `extensions`）。

### 2026-09-04T06:26:11.346Z · Chih-Lin Yeh · `c293516c`

## R3 handoff（回應 merge-review R2 `ba82858c` REQUEST_CHANGES：1 major／4 informational）

**分支**：`feature/LS-162-review-seed-email`（同 worktree），PR #267，head `96983f329d733cd834219aba623b47294c487ad4`。

### R2 處置

| # | 嚴重度 | 處置 | 位置 | commit |
|---|---|---|---|---|
| M1 | major | B1 的存在性 guard 用大小寫敏感的 `email in (...)` 比對，GoTrue 對 email 寫入正規化成小寫、查詢不分大小寫——真人帳號存小寫、操作者輸入帶大寫時 guard 不響、seed 回報成功、庫裡出現同址兩列。**主要修法**：owner／member email 格式驗證後立即在 shell 端正規化成小寫，下游 print_plan／SQL insert／錯誤訊息全部用同一份已正規化的值；**次要修法（defense-in-depth）**：guard 查詢與 insert 的 email 欄位另外用 `lower()` 包一層，即使上游正規化被繞過／改掉，SQL 端仍然正確 | `scripts/ops/review-demo-seed.sh:164-173`（shell 正規化，主要）／`:383-397`（guard `lower()`）／`:440-462`（insert `lower()`） | `96983f3` |
| i-a | informational | 正式站是否有 `pgcrypto` extension 由 orchestrator 於執行 `--target prod --yes` 前另外確認——本 agent 工具清單無 `mcp__supabase__list_extensions`、亦不連 prod，驗不了。腳本本身的 fail-loud 前檢維持（見 i-b），缺了會整份回捲、不會半套 | — | 不修＋理由 |
| i-b | informational | 原本只查 `pg_extension` 有沒有裝 `pgcrypto`，沒驗證目前連線 `search_path` 下 `crypt()` 叫不叫得到——改成 `to_regprocedure('crypt(text,text)') is null` 判斷（reviewer 驗證過的寫法），同時驗到「裝了」與「叫得到」 | `scripts/ops/review-demo-seed.sh:404-421` | `96983f3` |
| i-c | informational | `gen_salt('bf')` 用 pgcrypto 預設 cost 6，GoTrue 自己建帳號用 cost 10——明寫成 `gen_salt('bf', 10)` 對齊 | `scripts/ops/review-demo-seed.sh:400-402`／`:458` | `96983f3` |
| i-d | informational | 腳本檔頭「不寫進任何檔案／log」的絕對化敘述收窄——只對自動產生密碼（正式站預設路徑）成立；顯式帶 `--owner-password` 時本機測試會經 `supabase-lock.sh` re-exec，密碼明碼出現在該次命令列與 lock holder 檔，已加註說明 | `scripts/ops/review-demo-seed.sh:53-68` | `96983f3` |

### 已驗證：怎麼驗的

全程經 `scripts/ops/supabase-lock.sh`，整套在一次持鎖內跑完（lock 當時是 free，未等待、未搶其他 worktree 的鎖）：

- 冪等（帶混合大小寫的 CLI 輸入）：`db reset` → 兩次 `--target local --owner-email Qa-Owner@Example.com --member-email qa-member@example.com --owner-password 'Qa2026DemoPassw0rdXY'`（完全相同輸入），皆 `exit 0`。獨立 SQL 覆核：`family_members=2 children=2 media=20 diaries=5 diary_children=7 comments=4 reactions=4 invites=1 invite_code=LSDEM7 storage_objects=40`，與 LS-146 QA R1 `27c87837` 基準相同；`auth.users` 該列 email 正規化成 `qa-owner@example.com`（全小寫）；`encrypted_password` 前綴 `$2a$10$`（cost 10，驗證 i-c）、長度 60。
- **兩新案 (1)：大小寫混合真人帳號＋不同大小寫輸入 → 拒絕**——先用一般 `POST /auth/v1/otp {create_user:true}` 建真實帳號 `caseowner@example.com`（GoTrue 存成小寫，隨機 uuid）→ 帶 `--owner-email CaseOwner@Example.com`（不同大小寫）跑種子 → `exit 1`，guard 印出該列 id／created_at／所屬家庭數；種子後複查該帳號**仍存在、id 不變、沒有出現第二列**。
- **兩新案 (2)：種子自己固定 uuid 帳號用不同大小寫重跑 → 冪等成功**——已種好 `qa-owner@example.com` 之後改用 `--owner-email QA-OWNER@EXAMPLE.COM`（同一地址、全大寫）重跑 → `exit 0`，`auth.users` 固定 uuid `d1000000-…-01` 的 email 正確保持／歸一為 `qa-owner@example.com`，未撞 `users_pkey`、未產生重複列。
- 密碼登入用原始混合大小寫輸入複驗：`POST /auth/v1/token?grant_type=password`（`email=QA-OWNER@EXAMPLE.COM`）→ 200，回傳 `access_token`。
- i1／i2 迴歸重測：單引號 email／password → `exit 2`；`--target prod --yes` 大寫 `@LITTLE-SPROUT.APP` → `exit 2`。
- 原有 R1 負案迴歸重測（缺 `--owner-email`）：`bash -x` trace 內 `.env`／`SUPABASE_DB_URL`／`SUPABASE_SERVICE_ROLE_KEY`／`source` 出現次數 0。
- 收尾：`supabase db reset` 還原；`supabase-lock.sh --status` → `free`。
- `pr-body-check.sh --branch feature/LS-162-review-seed-email` 綠；push gate 本機通過。

**未完成**：iOS 密碼登入 UI（LS-163／LS-164）尚未落地，review-notes.md 已註明「送審前必須確認完成」；正式站實際執行待使用者提供 owner email／密碼後由 orchestrator 執行；i-a（正式站 `pgcrypto` 存在性）待 orchestrator 執行 `--yes` 前另外確認。

**風險**：M1 之後這道 guard 對大小寫變體也生效；假設正式站 `pgcrypto` 的 `crypt(text,text)` 在連線 search_path 下可解析（i-b 前檢會驗，缺了 fail loud）；正式站連線路徑一次都沒觸發。

**產出位置**：PR https://github.com/CLYEH/little-sprout/pull/267（`feature/LS-162-review-seed-email` → `development`），head `96983f329d733cd834219aba623b47294c487ad4`；模擬器已關：無（本輪未 boot 任何模擬器）。


### 2026-09-04T06:26:48.822Z · Chih-Lin Yeh · `2be9baaa`

orchestrator 2026-09-04 14:27：R3 handoff `c293516c` 收到——head `96983f3`（M1：email 於格式驗證後小寫正規化＋guard／INSERT `lower()`；兩新案：混合大小寫真人帳號拒絕 exit 1 無重複列、固定 uuid 不同大小寫重跑冪等；i-b `to_regprocedure` 檢查、i-c `gen_salt('bf',10)`、i-d 檔頭；i-a 由 orchestrator 確認正式站 `pgcrypto` 1.3 已裝）→ 派 merge-review R3 輕量（同 reviewer）。APPROVE 後併 development，隨下一批 promote 到 main 後從主 checkout執行正式站種子（`--target prod --yes --owner-email <非既有帳號 email> [--owner-password …]`，密碼只印終端、使用者自行填入 App Store Connect）。

### 2026-09-04T06:31:17.208Z · Chih-Lin Yeh · `9646ba76`

## merge-review R3 — PR #267（head 96983f3）

**APPROVE**（0 blocker、0 major、0 minor；1 條純提醒，不要求動作）

審的 head＝`96983f329d733cd834219aba623b47294c487ad4`，與執行當下 `gh pr view 267 --json headRefOid` 一致。只審 delta `09418d7..96983f3`：1 commit、1 檔 +55/−18（全部落在 `scripts/ops/review-demo-seed.sh`）。本機驗證全程經 `scripts/ops/supabase-lock.sh`；收工已 `db reset`（`auth.users` 0）、lock `free`、worktree `git status` 空、暫存目錄無殘留。**正式站零連線**。

---

### M1（R2 major）→ 已修，用 R2 那支重現原封不動跑回來

修法是兩層，我兩層都看了：**主要**是 shell 端在 `validate_email` 之後把兩個 email 正規化成小寫（`:164-173`），**次要**是 SQL 端 guard 與 INSERT 各包一層 `lower()`（`:397`／`:457-461`）。順序是對的——先驗證原始輸入、再正規化，所以錯誤訊息照操作者打的字顯示（實測：`--owner-email NONSENSE` 回「缺 @：NONSENSE」、`--owner-email "QA'Owner@Example.COM"` 回「不可含單引號：QA'Owner@Example.COM」，都不是小寫化後的樣子）。

**(1) R2 的重現情境（真人帳號存小寫、種子輸入帶大寫）——現在擋下來了**

```
seed rc=1
ERROR:  review-demo-seed 拒絕執行：email mixed@example.com 已存在於 auth.users，
        id=c60d8569-bba1-4bf2-98d4-c2e50fcb69b7（created_at=2026-09-04 06:27:58.4667+00，
        所屬家庭數=0）不是本腳本固定 uuid——這是別人的帳號，不會被刪除。
✗ review-demo-seed：SQL 套用失敗……中止——DB 已回捲，Storage 未動
```

對照 R2 同一個輸入的結果（`rc=0`、`✓ 完成`、prod 上多出第二列、兩種寫法都登不進去）——正是要修掉的那件事。失敗 log 裡沒有「清理既有 Storage」「上傳 Storage」任何一行，Storage 未被觸碰。

**(2) 冪等兩次＋不同大小寫重跑**

| 次序 | 輸入 | rc |
|---|---|---|
| run1 | `--owner-email QA-Owner@Example.COM --member-email QA-Member@Example.com` | 0 |
| run2 | `--owner-email qa-owner@example.com --member-email qa-member@EXAMPLE.com` | 0 |

兩次計數逐項與 LS-146 QA R1 `27c87837` 基準相同：`family_members=2 children=2 media=20 diaries=5 diary_children=7 comments=4 reactions=4 invites=1 invite_code=LSDEM7 feed_items=25 storage_objects=40 zero_byte=0 orphan_orig=0 orphan_thumb=0`。`auth.users` 跑完**剛好 2 列**、email 都是小寫（`qa-owner@example.com`／`qa-member@example.com`）、id 是固定 uuid——**沒有重複列**，這正是 R2 那個「同一地址兩列」的反面。`print_plan` 與收尾 echo 印的也是正規化後的小寫，跟實際寫進 DB 的一致（不會讓操作者以為存的是他打的大寫）。

**(3) 密碼路徑仍然好的**：`grant_type=password` 用大寫輸入 `QA-Owner@Example.COM` → **200**、用小寫 → **200**（GoTrue 兩種都收，因為現在 DB 裡只有一列）。

### i-b／i-c → 已修，查實

- **i-b**：檢查改成 `to_regprocedure('crypt(text,text)') is null`（`:420`）。我直接對本機驗這個表達式本身：`crypt(text,text)` → `f`（不會 raise）、不存在的函式簽章 → `t`（會 raise）。它同時驗到「裝了」與「當前 search_path 叫得到」兩件事，比原本只查 `pg_extension` 嚴格；raise→`ON_ERROR_STOP`→`run_sql` 失敗→shell `exit 1` 這條 fail-loud 鏈我在 R2 已實跑過，本輪只換條件式、鏈路未動。
- **i-c**：`gen_salt('bf', 10)`（`:459`）。實測 owner 列 `encrypted_password` 前綴由 R2 的 `$2a$06$` 變成 **`$2a$10$`**、長度 60，`email_confirmed_at` 非空、`aud`／`role` 皆 `authenticated`，登入仍 200。
- **i-d**：檔頭 `:54-62` 已把「不寫進任何檔案／log」收窄成「自動產生時成立；顯式帶 `--owner-password` 時會出現在命令列與 supabase-lock holder 檔，prod 分支不經 lock 且預設自動產生所以送審那組不受影響」——與我實測到的行為一致，沒有過度承諾也沒有淡化。
- **i-a**：正式站 `pgcrypto` 1.3 已由 orchestrator 用 `list_extensions` 確認安裝（我無此工具、也不連 prod）。此條解除，不再列為未決。

### 其他查核

- **負案 fail-closed**：`--target prod --yes` 缺 `--owner-email` 的 `bash -x` 完整 trace 裡，`.env`／`SUPABASE_DB_URL`／`SUPABASE_SERVICE_ROLE_KEY`／`source`／`psql`／`curl`／`openssl` 出現次數 **0**，末行 `+ exit 2`。prod 網域檢查對大寫輸入仍 `exit 2`。
- **CI**：`gh pr checks 267` 四項全綠（`ci` 4m22s／`db` 3m14s／`lint` 16s／`rules` 3m24s）。`bash -n` 乾淨。
- **scope：乾淨**。R3 delta 只有 `review-demo-seed.sh` 一檔，每一段都對得上 M1／i-b／i-c／i-d，沒有順手改動；全 PR 仍是 3 檔。
- **race／效能／平行**：本 delta 是字串正規化與兩個 SQL 述詞的修改，單程序循序，無並發面；guard 與刪除仍在同一份 SQL、同一個 `--single-transaction` 交易內，沒有 check-then-act 窗口。

### 純提醒（不要求動作）

`:205` 的 `owner_email_lower=$(… | tr …)`（i2 那輪加的）現在拿到的已經是小寫值，嚴格說是多餘的一次轉換。留著無害，而且與作者對 SQL `lower()` 的 defense-in-depth 說法一致（上游正規化萬一被改掉，這裡仍正確）。**不建議在本 PR 動它**——留給日後 dead-code sweep 看一眼即可。

### 明講沒審的部分

- 正式站零連線；prod `pgcrypto` 由 orchestrator 確認（見 i-a）。
- `:270` 的 host-psql `-f` 路徑仍無法實測（本機 host 無 psql，走 docker stdin）——LS-146 R2／R3 起的同一個盲區，prod 走的是 `-f` 那條。本輪未改動 `run_sql`。
- 本輪只審 delta：R2 已 APPROVE 過的部分（B1 guard 行為、N1 清理鍵、方案 B 端到端、review-notes.md／PLAN.md 文件）未重審，其中 guard 與密碼登入因為 M1 的修法會碰到，順帶又跑了一次，結果如上。
- iOS 端密碼登入 UI（LS-163／LS-164）不在本 PR，未審；未跑模擬器。

---

### 結論

M1 修在對的層：主要防線是輸入正規化（讓腳本寫進 `auth.users` 的東西本來就符合 GoTrue 的小寫慣例，「同一地址兩列」從源頭消失），SQL 端的 `lower()` 是不依賴上游的第二層——這比我建議的單改 SQL 更完整，而且正規化擺在驗證之後，錯誤訊息還是照操作者原本打的字回報。我把 R2 的重現原封不動跑回來：現在 `exit 1`、真人帳號不動、無重複列；再用一大一小兩種寫法連跑兩次確認冪等與計數不變、`auth.users` 恰好 2 列。i-b／i-c／i-d 三條也都落實且我逐條量到了（`to_regprocedure` 表達式真值表、`$2a$10$`、檔頭措辭）。

三輪下來，這支腳本對正式站的兩個致命面向都收斂了：不會刪到別人的帳號（id 為清理鍵＋同交易 guard），也不會種出一個登不進去的審核帳號（email 正規化）。**APPROVE**。

<sub>reviewer: merge-reviewer · R3 delta `09418d7..96983f3` · 審的 head `96983f329d733cd834219aba623b47294c487ad4`（＝執行當下 PR head）· 本機驗證全程經 `scripts/ops/supabase-lock.sh`；收工已 `db reset`、lock free、worktree 未改動、暫存無殘留；正式站零連線</sub>


### 2026-09-04T06:31:56.389Z · Chih-Lin Yeh · `8fef4544`

orchestrator 2026-09-04 14:31：merge-review R3 `9646ba76` **APPROVE**（M1 重現拒絕 rc=1、真人帳號不動；大小寫兩種輸入冪等、`auth.users` 恰 2 列存小寫、兩種寫法登入 200；`$2a$10$`／`to_regprocedure` 真值表；負案零 `.env`；CI 四綠；提醒 `:205` `owner_email_lower` 多餘留 sweeper）→ PR #267 併 development，dev tip status 已貼。狀態維持 In Review 至隨下一批 promote→test→QA（QA 只需複核腳本負案與本機冪等）→ main → 正式站種子執行（`--target prod --yes --owner-email <非既有帳號 email>`，密碼終端一次、使用者填 App Store Connect）→ Done。

### 2026-09-04T07:05:21.144Z · Chih-Lin Yeh · `fd2418a3`

## QA — test branch `829a04c0973cb41e445322ea10f5a411c5591704`（merge PR #267）

**裁決：PASS**（4/4 條驗收條件通過，1 條含一個需明講的落差——見條 3 說明；不影響整體判定）

worktree：`.claude/worktrees/qa-test`（detached @ `829a04c0973cb41e445322ea10f5a411c5591704`，與 `origin/test` 一致）。

---

### 1. prod 路徑 fail-closed — ✓

- `--target prod`（無 `--yes`）→ `exit=0`，只印計畫，正確列出兩個將使用的 email（此時未覆寫，印預設 `review-demo@little-sprout.app`／`review-demo-member@little-sprout.app`）；`（未帶 --yes：只印計畫，不連線、不寫入任何資料…）`。
- `--target prod --yes`（缺 `--owner-email`）→ `exit=2`，訊息「需要 --owner-email…」。`bash -x` 完整 trace grep `\.env|SUPABASE_DB_URL|SUPABASE_SERVICE_ROLE_KEY|source ` → **0 次**，trace 末行 `+ exit 2`。
- `--target prod --yes --owner-email x@little-sprout.app` → `exit=2`，訊息「不得使用 @little-sprout.app 網域…」。同樣 `bash -x` trace 連線標記 **0 次**。
- 三種情境全程未帶任何可用 prod 參數、未嘗試連線、未讀 `.env`（worktree 內本無 `.env`）。**未帶 `--yes`＋合法 email 跑過 prod，遵照指示未觸碰正式站。**

### 2. 本機冪等＋密碼登入 — ✓

全程持 `scripts/ops/supabase-lock.sh --hold "LS-162 QA" --max-minutes 20`（held pid=90689）內操作。

- `supabase db reset` → 兩次 `--target local --owner-email qa-owner@example.com --owner-password 'Qa-Pass-1234567890'`，皆 `exit=0`。
- 獨立 SQL 覆核（docker exec 進容器，非採信腳本自印 NOTICE）：`family_members=2 children=2 media=20 diaries=5 diary_children=7 comments=4 reactions=4 invites=1 feed_items=25 auth_users=2`、`storage_objects=40 zero_byte=0 orphan_orig=0 orphan_thumb=0`——**與 LS-146 QA R1 `27c87837` 基準逐項相同**，且兩次執行計數完全一致。`owner_email=qa-owner@example.com`（小寫），`invite_code=LSDEM7`。
- 密碼登入：`supabase status -o env` 取 `API_URL`／`ANON_KEY`，`POST /auth/v1/token?grant_type=password`（email=`qa-owner@example.com`、password=`Qa-Pass-1234567890`）→ **HTTP 200**，回傳 `access_token`，`user.id=d1000000-…-01`。
- 自動產生密碼路徑：不帶 `--owner-password` 重跑一次 → `exit=0`，log 內「已自動產生 owner 密碼」提示只出現 **1 次**（20 字元英數，`VubbkO6rACWvcsOpUrbD`）。抽取該密碼字串後：整份 worktree `grep -rl` **0 命中**；worktree 內無 `.env`；`/tmp/supabase-lock-little-sprout*` holder 檔亦無命中（此路徑未顯式帶 `--owner-password`，不落入 R3 i-d 提到的「顯式帶入才會出現在命令列／holder 檔」例外）。

### 3. 既有帳號 guard — ✓（含一個需明講的落差：exit code 是 1，不是票文字面寫的 2）

- `db reset` 後先用一般 GoTrue 流程建真實帳號：`POST /auth/v1/otp {email:"qa-owner@example.com", create_user:true}` → 200，`auth.users` 產生隨機 uuid `3a9b7a5b-9eb7-4195-a771-99c6a694f556`（非本腳本固定 uuid）。
- 再跑 `--target local --owner-email qa-owner@example.com --owner-password '…'` → **`exit=1`**（不是 `exit=2`）。訊息：`ERROR: review-demo-seed 拒絕執行：email qa-owner@example.com 已存在於 auth.users，id=3a9b7a5b-…（created_at=2026-09-04 07:02:25.540535+00，所屬家庭數=0）不是本腳本固定 uuid——這是別人的帳號，不會被刪除。`，`✗ review-demo-seed：SQL 套用失敗……中止——DB 已回捲，Storage 未動`。
- 種子後複查：該帳號 `id`／`created_at` **原封不動**；`select count(*) from auth.users`＝**1**（只有這個真實帳號，無種子帳號被建立）；`select count(*) from public.families`＝**0**（整份交易回捲）。

**落差說明**：票文「2026-09-04 13:22 變更」段寫的是「→ exit 2」，但實際機制是**同一份 SQL、同一個 `--single-transaction` 交易內的 `do $$ raise exception`**（guard 需要查 DB 才能判斷，天生不是 shell 端的早退驗證），觸發後走既有的 `run_sql "$sql_file" || { …; exit 1; }` 錯誤路徑，回傳碼是 **1**，跟 CLI 參數驗證失敗（缺 `--owner-email`／網域不符）用的 **2** 是兩條不同的錯誤路徑，語意上「1＝SQL/資料層失敗、2＝shell 參數驗證失敗」與腳本既有慣例一致。這個確切行為（`rc=1`）在 merge-review R1／R2／R3 三輪的自測與 reviewer 複驗中都是同一個數字、且三輪都在 comment 裡逐字寫出「`rc=1`」「`exit 1`」，orchestrator 三輪 sign-off comment 均未對此提出異議——換句話說這不是本輪才發現的新落差，是已經被完整揭露、複驗三次並實質核可的既定行為。**我判定這條驗收條件的實質保護（拒絕執行、印出 id／created_at／家庭數、真人帳號不被刪）已達成，只是把票文字面的「2」訂正為實測的「1」**，不因此判 FAIL。若要求逐字符合票文的「exit 2」，需要另開票把這條 guard 從 SQL 內的 `raise` 改寫成 shell 端 pre-check（會引入 check-then-act 窗口，reviewer 在 R1 已明確建議避免）。

### 4. 文件 — ✓

- `docs/store/review-notes.md`：全檔 grep `password|密碼` 僅出現在敘述性文字（「密碼只寫在 App Store Connect 的 Review Notes，不進 repo」等），**無任何密碼字面值**；owner email 全部為佔位 `<REVIEW_OWNER_EMAIL>`；方案 B 文字完整（§「登入方式：帳號密碼登入」、步驟、前置條件）；A／C 取捨紀錄保留並標「未採用」；`## OTP 對審核員的問題` 標題已是「已裁決：方案 B（2026-09-04）」；明確註明 iOS 密碼登入 UI 由 LS-163（設計）／LS-164（實作）落地，送審前必須確認完成。
- `docs/PLAN.md` §9-B（`:221`）同步一句，內容與 review-notes.md 裁決一致並互相指涉。

### 5. 自測腳本 — ⊘ N/A

`scripts/ops/review-demo-seed.test.sh` 不存在（票文本就允許「若存在則補案；否則附本機兩次冪等實跑與計數」——已在條 2 附上）。

---

### 收尾

- `supabase db reset` 還原（`auth.users`／`families` 皆 0）。
- `supabase-lock.sh --release` → 已釋放（持有 5 分 2 秒，非到期釋放）。
- worktree `git status` 乾淨（僅既有 untracked `scratchpad/`），未動任何檔案。
- 未 boot 模擬器（本票不需要）。

<sub>QA：test branch `829a04c0973cb41e445322ea10f5a411c5591704`；本機驗證全程經 `scripts/ops/supabase-lock.sh --hold`；正式站零連線</sub>


### 2026-09-04T07:10:11.939Z · Chih-Lin Yeh · `f206c82d`

## dead-code sweep — LS-162

dead-code-sweeper 收尾巡檢（test `829a04c0973cb41e445322ea10f5a411c5591704`，PR #267，feature head `96983f3`，base main `db16779`）。主 checkout 全程唯讀（`git log db16779..829a04c`／`git show 829a04c:<path>`／`git diff`／grep），未 checkout／stash／寫入任何檔案。

範圍：`git diff --stat db16779..829a04c` 只有 3 檔（`scripts/ops/review-demo-seed.sh` +230/-66、`docs/store/review-notes.md` +55/-41、`docs/PLAN.md` +1/-1），無新增／刪除／改名檔案（`--summary` 空）。無 Swift／migration 變更，故「未被引用的 Swift 符號」「SQL 殘留」兩類不適用本票。

### Findings（本 feature 引入）

**1. `scripts/ops/review-demo-seed.sh:220` — `owner_email_lower` 是對已正規化值的多餘一次轉換**

```sh
172: OWNER_EMAIL=$(printf '%s' "$OWNER_EMAIL" | tr '[:upper:]' '[:lower:]')   # M1，R2 加
...
220:   owner_email_lower=$(printf '%s' "$OWNER_EMAIL" | tr '[:upper:]' '[:lower:]')   # i2，R1 加
221:   case "$owner_email_lower" in
222:     *@little-sprout.app) … exit 2 ;;
```

- 判定證據：`grep -n "OWNER_EMAIL\|owner_email_lower"` 顯示 `OWNER_EMAIL` 在 `:172` 已被正規化成小寫（M1，merge-review R2 blocker 修法，`96983f3`），比 `:220` 的 `owner_email_lower` 賦值早、且中間沒有任何會讓 `OWNER_EMAIL` 變回大寫的程式碼（`:204` 的 `print_plan` 只讀不寫）。`owner_email_lower` 只在 `:221` 用過一次，之後未被其他地方引用（同一份 grep 輸出裡 `owner_email_lower` 只出現這兩行）。
- 成因：這是兩輪 review 疊代留下的痕跡——`:220` 是 R1 i2 加的（當時 `OWNER_EMAIL` 還沒有上游正規化，需要就地轉小寫才能正確比對網域），R2 M1 之後在更早的地方（`:172`）加了一層全域正規化，讓 `:220` 這行變得多餘，但沒有人回頭砍掉它。
- 這條已被 merge-reviewer R3 明確點名（comment `9646ba76`）：「`:205` 的 `owner_email_lower=$(… | tr …)`（i2 那輪加的）現在拿到的已經是小寫值，嚴格說是多餘的一次轉換。留著無害……不建議在本 PR 動它——留給日後 dead-code sweep 看一眼即可。」（行號因後續 commit 略有偏移，內容與本次巡檢對到的 `:220` 是同一行）
- 建議：**保留＋另票／併入下一張相關票順手刪**。不是「無人引用」的死碼（`:221` 確實用了它），是「值恆等於上游已算過的東西」的多餘計算，刪除是零行為變更的簡化（把 `:221` 的 `$owner_email_lower` 直接換成 `$OWNER_EMAIL`、砍掉 `:220`）。風險極低，但不建議現在單獨為一行開票動這支已通過三輪 review、正要送 prod 執行的腳本——比照 informational 慣例記入待辦池（LS-96）或等下次有理由碰這支腳本時順手帶掉即可。

### 排除的候選（查過，判定非死碼）

- **方案 C 殘留文字**：`docs/store/review-notes.md` 全檔 grep `方案 C|真實信箱|收驗證碼|OTP 方案` 命中的 3 處（`:76`「除非信箱本身是一個審核員能登入查看的真實信箱」、`:107`「選項 C：Magic Link／真實信箱（未採用）」、`:109`「審核員自己登入該信箱收驗證碼」）全部落在「OTP 對審核員的問題」段落**已標「未採用」的「選項 C」歷史取捨紀錄**內，票文 2026-09-04 13:22 變更明確要求「保留 A／C 取捨紀錄」，且方案 C 的「操作指示」（登入步驟、帳號表登入方式欄）已在 R1（`26595b3`）／R2（`09418d7`）改寫掉，主要段落（帳號表、登入方式段）現在完全是方案 B 文字——這是刻意保留的紀錄，不是清乾淨漏掉的殘留。`docs/PLAN.md` §9-B 同步比對過，兩份文件互相一致、無矛盾。
- **未被引用的檔案／資源**：本 diff 無新增／刪除檔案（`git diff --summary` 空）。
- **鷹架殘留**（註解掉的程式碼塊／debug print／假資料／暫時旗標）：讀過 `scripts/ops/review-demo-seed.sh` 全部 672 行與兩份文件全文，grep `TODO|FIXME|XXX` 0 命中；沒有被註解掉的程式碼塊；`echo` 全部是操作性訊息（計畫列印、錯誤訊息、一次性密碼提示），不是 debug 殘留；`review-demo-seed.test.sh`（票文允許的自測腳本）本就不存在，QA `fd2418a3` 條 5 已確認並判 N/A，非本次巡檢新增的殘留。
- **設定殘留**：grep `.github`／`scripts` 底下對 `review-demo-seed`／`review-demo-genvideo` 的引用，全部落在腳本自身檔案內（用法說明、呼叫自己），CI／gate 設定沒有任何條目指向這支腳本的舊介面或已改名路徑。

### 既有死碼觀察（非本 feature 引入，僅供 orchestrator 參考，不算 finding）

- `scripts/ops/review-demo-seed.sh:46`（base `db16779` 既有，本次 diff 未觸碰）：「本票只做本機驗證：LS-146 不會以 --target prod --yes 執行，正式站落地留待使用者核可。」——這句是 LS-146 當初寫的，對 LS-146 本身仍然字面成立（那張票確實沒跑 prod），但隨著 LS-162 已把 prod 執行路徑做完、QA／R3 皆已 APPROVE、即將由 orchestrator 實際執行，這句話的「留待核可」語境已經過期，讀起來容易誤導成「這支腳本還沒打算跑 prod」。不是死碼（沒有失效的程式邏輯），是**過期的頭註解**，且不屬於本次 LS-162 diff（我未動它）。是否要順手更新留給 orchestrator／下次碰這支腳本的人判斷，不影響本票判定。

### 結論

**本 feature（LS-162）引入的死碼：0 項需要動作的 finding，1 項低風險冗餘計算（`:220` `owner_email_lower`）記錄供未來順手清理，不阻擋 Done。** 方案 C 相關文字經核對皆屬票文明定保留的歷史紀錄，非殘留；無孤兒檔案／資源；無鷹架殘留；無設定殘留。

### 搜尋方法與盲區聲明

- 方法：`git log db16779..829a04c --oneline` 定範圍 → `git diff --stat`／`--summary` 定觸碰檔案 → `git show 829a04c:<path>` 讀 test 版全文 → 針對每個變數／關鍵詞 `grep -n` 交叉比對定義點與所有引用點 → 逐段核對 review-notes.md／PLAN.md 是否互相矛盾。
- 盲區：純文字 grep 抓不到 reflection、字串拼接組出的變數名、動態 source（本腳本的 `source "$ROOT/.env"` 是唯一動態載入點，但那是既有機制非本票新增，且只在 `--target prod --yes` 分支、未在本次唯讀巡檢中實際觸發）。本票是純 bash＋markdown 變更，沒有 Swift／Objective-C runtime／SwiftUI 隱式引用的面向，該類盲區不適用。未實際執行腳本（唯讀巡檢，未連本機或正式站 DB），僅靠讀碼與 QA／merge-review 既有的實跑紀錄交叉比對；若既有實跑紀錄本身有誤，本次巡檢無法獨立發現。

<sub>dead-code-sweeper · test `829a04c0973cb41e445322ea10f5a411c5591704` · 唯讀巡檢，未改動任何檔案</sub>


### 2026-09-04T07:13:07.123Z · Chih-Lin Yeh · `cde90469`

orchestrator 2026-09-04 **收尾 lesson learning review**（release v0.9.1，main `829a04c`）：

- QA R1 `fd2418a3` PASS；sweeper `f206c82d` 1 冗餘（`owner_email_lower` 重複小寫）＋`:46` 頭註解過期 → LS-96 入池（`d9efa7a8`），不另票。
- 票文「exit 2」與 guard 實測 exit 1 的落差：guard 走 SQL 交易內 `raise exception` → `run_sql || exit 1`，與 CLI 參數驗證的 exit 2 是不同錯誤路徑；三輪 review 皆複驗同一數字，接受。教訓：票文寫 exit code 時區分「參數層」與「資料層」兩條路徑。
- Harness 改善：本票中途改方案（C→B）靠票文追加段＋PR 三輪 review 消化，無 gate 缺口；「正式站 email 在 `auth.users` 已存在且非固定 uuid 即拒刪」是本票新增的機械防線（起因：使用者原給的信箱是本人正式站帳號，種子舊版會整個刪掉）。
- 設定優化：無。工具缺口：無。
- cleanup：worktree 已於 14:45 移除，分支已清。

→ Done。正式站種子實跑記在 LS-146。

