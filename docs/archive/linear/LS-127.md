# LS-127 Harness：design-evidence-check 在 CI merge ref 上誤把 base 側 .pen 變更算成本 PR（boards 漏列假紅＋合併 commit 被當最後一次 .pen commit）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-02T07:00:04.687Z |
| 開始 | 2026-09-02T07:01:09.205Z |
| 完成 | 2026-09-02T07:43:39.540Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-127/harnessdesign-evidence-check-在-ci-merge-ref-上誤把-base-側-pen-變更算成本 |
| 關係 | related→LS-122, related→LS-96, related→LS-119, related→LS-68, related→LS-114, related←LS-171 |

## 描述

## 來源

[LS-119](https://linear.app/little-sprout-app/issue/LS-119/設計日記編輯器時間軸卡片流ls-21-畫面群-ui-designervisual-reviewer-3-輪) PR #223（`feature/LS-119-diary-timeline-design` → development）CI `rules` job「.pen 掃描收據 gate（[LS-68](https://linear.app/little-sprout-app/issue/LS-68/harness設計流程規則機械化-聚焦輪條款收工溢出掃描必跑分段落地掃描收據-gate)）」紅（run 33600796836 job 100153820302）：

* r8–r12 五份四支 schema 收據皆報「`scans.corner_anchor.boards` 漏列本 PR 對 .pen 有變更的頂層節點：`PXPcH`（cmp/Approval Status）」——`PXPcH` 是 development 側 [LS-114](https://linear.app/little-sprout-app/issue/LS-114/設計-chorecmpapproval-statuspxpchax3-錯位-196pt-alignitems-center-缺陷與-ls) 的變更（`5cc45ae`），本分支自 `73814e4` 切出後從未動過它。
* r12 另報「head_sha 不是本 PR 對這份 .pen 最後一次的 commit：最後一次＝`c47edc0`」——`c47edc0` 是 GitHub `refs/pull/223/merge` 的合併 commit（本分支與 development 皆不含），因合併 `.pen` 而被算成「PR 的 .pen commit」。
* 本機在分支 head 上跑 `design-evidence-check.sh design/littlesprout.pen --ticket LS-119 --base origin/development` 九份全綠。
  根因：CI 以 `actions/checkout` 的 merge ref 為 HEAD，gate 用 `merge-base(origin/$BASE, HEAD)..HEAD` 求「本 PR 的頂層節點變更」與「最後一次 .pen commit」，在 base 自分出後也動過 `.pen` 時，base 側變更與合併 commit 都被歸給 PR。長壽設計分支（本案 2 天）必踩；方向是假紅（fail-closed），但每張設計票都會被迫先 merge base 再重做收據。

## 範圍

1. `scripts/gates/design-evidence-check.sh`（含 `.github/workflows/ci.yml` 呼叫處）：在 CI 用 `github.event.pull_request.head.sha` 與 `github.event.pull_request.base.sha`（或 `merge-base(base.sha, head.sha)`）計算「本 PR 觸碰的頂層節點」與「本 PR 對 .pen 的最後一次 commit」，**排除合併 commit 與 base 側 commit**（`git log --first-parent`／`git rev-list base..head` 限本分支 commit）；本機模式（`--base`）行為不變。
2. `total_nodes` 對帳仍以 `head_sha` 快照為準（不受影響）。
3. 自測 `design-evidence-check.test.sh` 加：合成 repo 中 base 在分出後改了 .pen 的另一頂層節點 → 舊 gate 紅、新 gate 綠；PR 自己真的漏列觸碰板 → 仍紅（負控）；merge ref 上跑（模擬 checkout merge commit）→ 綠。
4. COLLABORATION §7 該列補「merge ref」說明。

## 驗收

- [ ] 對 PR #223 的形狀（base 側 .pen 有變更＋merge ref）重現：修前紅、修後綠；PR 真漏列觸碰板仍紅
- [ ] 自測掛 CI `rules` job；本機 `--base` 模式回歸綠
- [ ] merge-reviewer 通過（scope：只動 gate、workflow、自測、§7）

## 留言（4）

### 2026-09-02T07:18:00.109Z · Chih-Lin Yeh · `4383a1c0`

## handoff — LS-127（orchestrator 代貼，2026-09-02 19:00）
**PR #224**（`hotfix/LS-127-evidence-merge-ref` → main，head `875a413`；`5924b8d` gate＋自測／`6ab364f` workflow／`875a413` docs）。
**已完成**：① `design-evidence-check.sh` 加 `--head-sha`（CI 傳 `pull_request.head.sha`）——收據清單、.pen commit 清單與「最後一次 .pen commit」以 head 為終點；每份收據「觸碰板」基準改 `merge-base(base_sha, 該收據 head_sha)`（歷史收據免疫 base 前進；算不出即紅）；`total_nodes`／`LEGACY_CUTOFF` 不動；未加 `--first-parent`（merge ref `^1`＝base，走 first-parent 反而到 base 側）。② 自測 ㉑–㉕（25 樣本／30 斷言）：merge ref 不給 head-sha 仍紅且不點名 base 側節點／給 head-sha 綠／PR 自己漏列仍紅（負控）／PR 併 base 後歷史收據綠／head-sha 解析不到 exit 2。③ ci.yml `HEAD_SHA` 接線。④ §7 補 LS-127 段＋「設計分支不得 rebase」＋盲區（gate 只看 head 快照；PR 併 base 且兩側動 .pen 時最新收據須合併後重做）。
**已驗證**：舊 gate 對新自測 6 紅→新 gate 30/30 綠；PR #223 真實形狀（合成 merge ref）：舊 gate r8–r12 五紅＋r12 F2 紅（同 CI job 100153820302），新 gate `--head-sha 86a060e` 八份綠；GitHub 現行 `refs/pull/223/merge`（`^2`=`6cb9b91`）新 gate 9/9 綠；三 commit 過 hook、pr-body-check 綠。
**未完成**：merge-review；自檢潛伏項 `top_level()` 非 JSON 回 `None` 時 TypeError（方向仍紅）不修→ LS-96。
→ In Review，派 merge-reviewer。

### 2026-09-02T07:31:47.223Z · Chih-Lin Yeh · `a9094933`

## merge-review R1：PASS

**PR #224**（`hotfix/LS-127-evidence-merge-ref` → main），審的 head `875a41324edb9b1566abec28518e960ec3d68edd`，base `fd2746e`。四檔：`scripts/gates/design-evidence-check.sh`／`design-evidence-check.test.sh`／`.github/workflows/ci.yml`／`docs/COLLABORATION.md` §7（+167／−23）。CI 四 job（rules／db／lint／ci）在此 head 全綠。

**Verdict：APPROVE**（無 blocker、無 major）

### 1. 正確性／鑑別力（自己實跑，不採信 handoff）

**(a) `merge-base(base_sha, 收據 head_sha)` 基準會不會讓「PR 自己漏列」逃掉——不會。**
- 推理：收據 head_sha 必屬 `rev-list base_sha..head`（不可由 base_sha 抵達），故 `merge-base(base_sha, sha)` 恆為 sha 的真祖先、且是 base_sha 的祖先＝**只含 pre-PR 內容**，不可能把 PR 自己的板藏進基準。基準往前移只會少算「base 側的差異」、不會少算 PR 側。
- 最新輪收據因 F2 被釘死＝`last_pen_commit`，其基準在「無合併」時＝fork、在「PR 併過 base」時＝併入的 base tip，兩種情況 touched 都等於「PR 相對 base 的全部頂層變更」＝**累積覆蓋**，沒有以輪次切碎覆蓋範圍的縫。
- 自建對抗樣本（沙箱合成 repo，非本 PR 自測）：fork → r6 動板 d（收據列 d）→ base 動 c → PR 自己併 base → r7 動板 e，**r7 只列 `["e"]` → 紅並點名 d**；改列 `["d","e"]` → 綠（歷史 r6 只列 d 也綠）。同形狀再合成 merge ref＋`--head-sha` → 綠；把 r7 改回只列 e → 仍紅。即題目擔心的「r9 漏 B 應紅」與其變形（漏的是更早輪的 B）都紅。
- **真稿負控**：GitHub 現行 `refs/pull/223/merge`（`cd73c6d`，^1=`b516606`、^2=`6cb9b91`）上，把 r13 收據的 `boards` 拿掉一個真觸碰板 `zgVn0` → 紅且**只點名 `zgVn0`、不點名 base 側 `PXPcH`**。

**(b) `--head-sha` 終點下的「最後一次 .pen commit」。**
- 真稿實跑（我自己 clone＋`git fetch origin refs/pull/223/merge`，非合成）：**舊 gate** 在 merge ref 上 → r8–r12 五份「漏列 `PXPcH`」＋r13「最後一次＝`cd73c6d`（合併 commit）」，逐字重現 run 33600796836 的假紅；**新 gate `--head-sha 6cb9b91…`** → **9/9 全綠**。
- 排除合併 commit 的機制不是 `--first-parent`（作者判斷正確：merge ref 的 `^1` 是 base 側），而是 `rev-list <mb>..<PR head>` 根本走不到 GitHub 那顆合併 commit。
- §7 盲區（PR 自己併 base 且兩側動 .pen）確認為 **fail-closed 且可滿足**：該合併 commit 對 .pen 兩側皆不同 → 進 `rev-list` → 成為 `last_pen_commit` → 最新收據必須在合併後快照重做（我的 T2 樣本印出 rev-list 確認合併 commit 在清單內）。不是漏放。

**(c) `LEGACY_CUTOFF`／`total_nodes` 不動——確認。** diff 顯示只有 base 側快照的取得點從 `base_sha` 改為 `touch_base`；`total_nodes` 仍走 `git show <收據 head_sha>:<pen>` → `design-landing-check --print-nodes`，committer-time 判定仍讀收據 head_sha。真稿 r5/r6/r7 仍走舊 schema 綠、r8+ 四支綠，未回溯紅。

**(d) fail-closed 三路都實測：** `--head-sha` 指向不存在物件 → gate exit 2；CI step 拿到壞 sha → `git merge-base` 失敗、step exit 128（`bash -e`）；**shallow clone**（`--depth 1` 合成 repo）→「找不到共同祖先（fail closed）」exit 2。rules job 是 `fetch-depth: 0`，實務上不觸發。

### 2. CI 接線（本 PR 自己不動 .pen，該 step 在 #224 的 CI 不會跑，所以我另外實跑）
把 ci.yml 的 step body 用 `yaml.safe_load` 抽出來，在真實 `refs/pull/223/merge` checkout 上以 `BASE=development HEAD=feature/LS-119-… HEAD_SHA=6cb9b91…` 用 `bash -e` 重演：
- 新接線 → **rc=0、9/9 綠**，`pens` 偵測正確得到 `design/littlesprout.pen`；
- 舊接線（同一支新 gate、不給 `--head-sha`）→ **rc=1**（r13 F2 紅），證明修的是接線＋算法兩處各一半。
- `pull_request` 事件下 `HEAD_SHA` 有值；push 事件該 step 本來就被 `github.event_name == 'pull_request'` 與 head 前綴條件擋掉，不受影響。
- 「PR 只動收據不動 .pen」：`pens` 為空 → 整段跳過，與修前一致（規則本身就是「.pen 有變更才要收據」）。另確認**不會反向誤觸發**——PR 併入的 base 側 .pen 變更必為 `merge-base(origin/$BASE, head)` 的祖先，三點 diff 不會把它算進來，不會出現「偵測到 .pen 但 `pen_commits` 為空 → exit 2」的假紅。
- 自測 ㉑–㉕ 在本 head 的 CI（run 33602755167）rules job 內確認實跑通過（`✓ ㉑…✓ ㉕`＋`全數通過`）；我另外把**新自測餵舊 gate** → 6 條紅，handoff 的鑑別力宣稱屬實。

### 3. 效能
真稿 9 份收據（2.0 MB .pen）全程 **1.3s**；新增成本只有每份收據一次 `git merge-base`（O(收據數)，≤ 十幾份），`git show`＋頂層解析的次數與修前相同。無 O(n²)、無對全歷史掃描。序列執行 1.3s，不值得平行化（平行維度：本 gate 無共享狀態、temp 檔皆 per-process，無競態可言）。

### 4. Scope
只動票文列的四處，無順手重構、無其他 gate 被碰（`grep` 確認全 repo 只有 ci.yml 一個呼叫點）。`pr-body-check.sh` 對 #224 body 綠。§7 抽查 5 處與實作零漂移：`--head-sha` 來源、`merge-base(base,head)..head` 的兩個用途、(a) 的雙層 merge-base、`total_nodes`／`LEGACY_CUTOFF` 不動——全部對得上。

### Informational（不擋合併，處置請記 handoff）
- **i1** `scripts/gates/design-evidence-check.sh:323`：`base_roots = top_level(...) if rc==0 else {}`，`top_level()` 對非 JSON 回 `None` → `rid not in None` TypeError。**pre-existing**（修前同一行）、且方向是紅（python 非 0 → gate 紅），只是訊息變成 traceback。另已確認 head 側快照壞掉時 `design-landing-check` 會先紅，不會因 `head_roots is None` 靜默跳過 boards 檢查而假綠。handoff 記 LS-96 我同意；若要順手修就是 `or {}` 一行。
- **i2** §7「本機不給 `--head-sha` 沿用 HEAD、**行為不變**」只對「終點」成立；(a) 的基準改動在本機模式同樣生效（PR 自己併過 base 時本機行為也變了，方向是修好假紅）。自測 ㉔ 註解有寫「本機模式就能重現」，但 §7 那句可能誤導。
- **i3** §7 該列開頭的用法簽章仍是 `<path.pen> --ticket <LS-n> --base <ref>`，`[--head-sha]` 只在後段 LS-127 段落出現（腳本檔頭簽章已含）。純文件細節。
- **i4** 相鄰的「.pen 落地結構檢查」step 仍以 merge ref 的 HEAD 偵測並對**合併後工作區**跑 `design-landing-check`——這一支是內容三點 diff＋結構檢查，語意在 merge ref 上本來就正確，不屬本 bug 類別，**不需改**；順帶對 §7「合併後 .pen 沒人掃過」的盲區提供結構層（非溢出層）的兜底。

### 未審範圍（誠實聲明）
- `design/littlesprout.pen` 內容與溢出掃描數字本身沒看（不開 Pen／Pencil，也非本票範圍）；收據真實性維持既有盲區。
- 未跑 DB／模擬器（本 PR 不涉及）。


### 2026-09-02T07:33:16.146Z · Chih-Lin Yeh · `da1adf74`

orchestrator（2026-09-02 19:20）：merge-review R1 **APPROVE**（`a9094933`，status success on `875a413`；reviewer 自 clone 真 merge ref 重現舊 gate 六紅→新 gate 9/9 綠、真稿負控只點名 `zgVn0` 不點名 `PXPcH`、對抗樣本累積覆蓋不被輪次切碎、CI 接線端到端、shallow clone fail-closed）。→ PR #224 併入 main、開 back-merge PR main→development。
informational 處置：i1（`top_level()` None TypeError）已入池 LS-96 `7d36d0b0`；i2（§7「本機行為不變」措辭只對終點成立，(a) 基準改動本機也生效）＋ i3（§7 用法簽章缺 `[--head-sha]`）→ 入池 LS-96 與 §7 措辭同批；i4 無需改。

### 2026-09-02T07:43:36.080Z · Chih-Lin Yeh · `aea549f9`

## 收尾（orchestrator，2026-09-02 19:50）
- PR #224 併入 main `5febba9`；back-merge PR #225 併入 development `9d4fdd8`（status 依 §2 貼於 back-merge head）；主 checkout 已 pull。PR #223 已 close/reopen 取新 merge ref 重跑 CI（驗收條件 1 的真實驗證）。
- 純 harness 票免 dead-code 巡檢；**retro 輕量版**：gate 漏接——LS-122 落地時未考慮 CI merge ref 與 base 前進兩種形狀，第一張長壽設計 PR 就踩到（假紅方向）；抓到者為 LS-119 merge-reviewer 的實測分析（`3d5851ac`），並正確阻止了「merge development 也救不了」的誤修；教訓：gate 自測要含「merge ref 上跑」與「base 前進後歷史收據」兩種形狀（本票已補 ㉑–㉕）。派工與 model 無需調整。工具缺口：無新增；i1–i3 入池。
- **清理**：`cleanup-merged.sh --apply LS-127` 移除 worktree＋本機分支。
→ Done。

