# LS-202 Harness：設計 gate 精修三項——`overflow-scan.js` `corner_anchor` 改以 `ref → cmp/Photo Corner` 判準（不再靠 `Corner TL/…` 名稱；`Mount TL/BR` 盲區）＋六支皆輸出 per-scan `scope` 與 `document_count`＋`design-notes-check.sh` 新增「署名年齡片語必 NBSP」機械檢查；三支 python gate 不留 `__pycache__`

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-05T08:41:20.365Z |
| 開始 | 2026-09-05T08:54:30.322Z |
| 完成 | 2026-09-05T15:26:24.763Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-202/harness設計-gate-精修三項-overflow-scanjs-corner-anchor-改以-ref-cmpphoto |
| 關係 | related→LS-200, related→LS-51, related→LS-152, related→LS-163, related→LS-142, related→LS-119, related→LS-120, related→LS-133, related→LS-122, related→LS-185, related→LS-194, related→LS-198, related→LS-96, related→LS-201, related→LS-177, related←LS-238, related←LS-226, related←LS-208, related←LS-207, related←LS-204, related←LS-205, related←LS-164 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項 `0617b9ae`（[LS-194](https://linear.app/little-sprout-app/issue/LS-194/設計-chore使用者-0905-核可意見兩項-ls-152-01-設定-家庭區塊列文字垂直置中ls-163-p1-帳號密碼登入-ax3) VR：`cmp/Profile Print` 的 `Mount TL/BR` 角托以 `ref → GEBcf` 存在但 `corner_anchor` 靠名稱 `Corner TL/…` 辨識、永遠看不到；實測 corner-out 3.8pt 屬盲區）、`ba1ec045`（[LS-177](https://linear.app/little-sprout-app/issue/LS-177/設計留言愛心互動列與留言-sheetls-22-畫面群推播通知權限提示與設定頁推播通知列-ui-designervisual) design PR review：`SCAN_SCOPE=boards` 分段跑時 `cross_parent_collision` 缺 `document_count`）、`ed90c6ab`（[LS-194](https://linear.app/little-sprout-app/issue/LS-194/設計-chore使用者-0905-核可意見兩項-ls-152-01-設定-家庭區塊列文字垂直置中ls-163-p1-帳號密碼登入-ax3) review N3＋lesson：Notes 規則「署名年齡片語必 NBSP」無 gate，BL-1 靠 VR codepoint 比對才抓到）、`c4c10429`（design python gate 留 `__pycache__` 讓 worktree dirty、cleanup 需 `--force`）；[LS-198](https://linear.app/little-sprout-app/issue/LS-198/harness-chorels-96-池項清倉五件-收據-n-支殘句統一指回-overflow-scanjs-檔頭7-處pr-body) R3 自檢同結論「角托判準應用 ref 不用名稱」。

## 範圍

1. `scripts/design/overflow-scan.js` `corner_anchor`：角托候選改為「`type:"ref"` 且 `ref` 指向 `cmp/Photo Corner`（`GEBcf`）的實例」（名稱只作備援），四角／對角判定沿既有；`SCAN_VERBOSE` 列出每個容器的角托數；六支掃描一律輸出 per-scan `scope`（`boards|document`）與 `document_count`，分段跑（`SCAN_BOARDS`）時 `document_count` 仍以全稿計；`overflow-scan.test.js` 補：`Mount TL/BR` ref 實例被算到、名稱像角托但非 ref 不算、分段跑 `document_count` 不變。
2. `scripts/gates/design-evidence-check.sh`：驗六支皆有 `scope`＋`document_count`（cutoff＝腳本含新欄位，舊收據放行）；`design-evidence-check.test.sh` 補正負樣本。
3. `scripts/gates/design-notes-check.sh`（或其 python）：新增「署名文字規則」檢查——所有 `cmp/Card Album`／`cmp/Card Diary` 署名 override 文字節點內，`\d+[NBSP]歲`、`[NBSP]個月` 必為 U+00A0（允許 WJ／`\n`）；命中 U+0020 → 紅並印節點 id 與 codepoint；自測補三案（正確／SP／混合）。對現稿跑一次確認 0 命中（[LS-201](https://linear.app/little-sprout-app/issue/LS-201/設計-chore全稿舊債清倉-root-板重疊-17-對重新落位80pt-板距board-clip-document-332-筆26-板與) 已改 `HIdMW`；若 [LS-201](https://linear.app/little-sprout-app/issue/LS-201/設計-chore全稿舊債清倉-root-板重疊-17-對重新落位80pt-板距board-clip-document-332-筆26-板與) 未併則以 development 現況為準並在 handoff 列命中清單）。
4. `design_notes_check.py`／`design_tree_hash.py`／其他 design python 開頭 `sys.dont_write_bytecode = True`；`.gitignore` 確認含 `__pycache__/`。
5. `.claude/agents/visual-reviewer.md`／`ui-designer.md`／COLLABORATION §7 該列同步一句（角托判準＝ref、NBSP 有 gate）。

## 不做

第七支新掃描類別；.pen 內容修正（[LS-201](https://linear.app/little-sprout-app/issue/LS-201/設計-chore全稿舊債清倉-root-板重疊-17-對重新落位80pt-板距board-clip-document-332-筆26-板與)）。

## 驗收

- [ ] 三支自測全綠＋mutation（角托判準退回名稱 → Mount 案紅；NBSP 檢查移除 → SP 案紅）
- [ ] 對 `origin/development` 現稿跑六支：`corner_anchor` 不出現新誤報（`Mount` 例正確歸類）、收據六支皆有 `scope`／`document_count`
- [ ] merge-reviewer 通過；hotfix 流程（自 main、PR 回 main、back-merge）

## 留言（16）

### 2026-09-05T09:28:16.423Z · Chih-Lin Yeh · `f9533afd`

**Handoff（2026-09-05 17:27 CST）**

Ticket：LS-202

已完成：
1. `scripts/design/overflow-scan.js`（`070257a`）：`corner_anchor` 角托候選＝`type:"ref"` 且 `ref` 解析到 `cmp/Photo Corner`（字面 `GEBcf` ∪ 快照 roots 同名元件定義；限縮快照只剩字面 id）；名稱只供方位 `TL/TR/BL/BR`（無方位進 `unresolved`），名稱像角托但非 ref 不算；`unresolved` 同 `mismatch` 限 `boards`、全稿另列 `document_unresolved`；`container_corners`＋`SCAN_VERBOSE` 印 `CORNERS` 行；`scanAll` 六支各帶 `scope`／`document_count`（整份快照命中數，`scope=boards` 時為限縮值），`compactLines` 六段標頭尾綴、SUMMARY `unresolved=<in>/<doc>`；Pencil 端快照補 `ref: n.ref`（原始碼斷言）。自測 33→37 組。
2. `design-evidence-check.sh`（`549f75a`）：六支必有 `scope`＋非負整數 `document_count`；cutoff＝`head_sha` tree 正典腳本含 `document_count`（輪次最高另看 PR head tree），舊收據放行印一行。自測 ㊷～㊹-b。
3. `design-notes-check.sh`／`design_notes_check.py`（`642f783`）：`cmp/Card Album`／`cmp/Card Diary` 定義 text＋全稿實例 `descendants` content 覆寫，`歲`／`個月` 前空白只准 U+00A0／U+2060／換行，含 U+0020 印板／節點|實例 override／單位／codepoint；`--base` 增量：命中所在頂層節點在 merge-base→head 有變更才紅，其餘列（舊債）。自測 ⑫～⑯-b。
4. `design_notes_check.py`／`design_tree_hash.py` `sys.dont_write_bytecode = True`＋`.gitignore` 全域 `__pycache__/`（`78d38e1`；模組內旗標擋不住「被別人 import」那一次的 .pyc，.gitignore 才是兜底——`git worktree remove` 不把 ignored 當髒）。
5. 文件（`aa059dc`）：ui-designer.md／visual-reviewer.md／COLLABORATION §7 兩列。

已驗證：
- 四套自測綠：`overflow-scan.test.js` 37 組、`design-evidence-check.test.sh` 全數（＋9 條）、`design-notes-check.test.sh` 全數（＋8 條）、`agent-tools-check.test.sh` 65 組；`agent-tools-check.sh`／`brand-skill-check.sh` 對真實 agents 綠；四套跑完無 `__pycache__` 殘留、`git status` 乾淨。
- Mutation 兩支皆紅：角托判準退回名稱 → `overflow-scan.test.js` exit 1，紅在「(d) LS-202 Mount TL/BR…」（`unresolved` 期望 1 實得 0）；NBSP 檢查移除 → `design-notes-check.test.sh` ⑫／⑬／⑬-b／⑭／⑮／⑯／⑯-b 紅（SP 案 ⑬ exit 0）。
- `origin/development`（`48d8b43`）現稿離線跑（明文 JSON 展開 instance，Pencil 未碰）：名稱判準 220 容器＝LS-177 r4 `document_containers` 220／points 1744；ref 判準 261＝220＋41 `Mount TL/BR` 兩對角容器（Profile Print 定義 1＋實例 27＋App icon 13），only-in-OLD 0；41 個中 37 個 `unresolved`（best＝父，0/4：實測 corner-out 3.8／2.82）、4 個 resolved。LS-194 r2（9 板）對照：舊 3 容器＝收據 3/24/0；新 6＝3＋`lE7FJ`／`vgbkF`／`nGPjb` 的 `lOPO7 Print`（Mount 對角、unresolved）——差異只來自新納入的 ref 角托。
- 新 evidence gate 對 development 上 LS-194 r1／r2 真收據：兩份綠＋「LS-202 新欄位不要求——舊收據放行」。
- NBSP 對 development（LS-194 PR 範圍）：違規 0／舊債 9——`qtCd7` 定義 `zk1yE` 2、`x7k2o6` 3；`HLXo3` 實例 `fZ3KF` override `x7k2o6` 3；`X9PfG` 實例 `HIdMW` override `wQVzs` 1（票文預期那一筆；LS-201 只改了它）。

未完成／剩餘：
- merge-reviewer 尚未審；其 informational 處置規則依 COLLABORATION §3 五條之 5 逐條列（修掉附 commit SHA＋檔案:行號／池項附 comment id／另票限 §5-b High 以上／不修＋理由），一條不省。
- 併回 `main` 後補 back-merge `main`→`development`（hotfix 流程）。
- 待裁定（不阻擋合併）：`unresolved` 限 `boards` 是本票的判斷取捨（票文未提；不改則 41 個他票 Mount 容器進每張收據且需逐筆分類），diff 一處可退回；Mount 容器目前歸 `unresolved` 而非 `mismatch`（四軸全偏 1.2 > TOL、既有「≥ 一半軸」紙面門檻認不出父），要成 mismatch 得改紙面判定，本票不動。

風險與已知問題：
- 併入後 in-flight 設計票（LS-201）最新收據須用新腳本重跑補六支 `scope`／`document_count`（同 LS-185 對 LS-177）；`SCAN_BOARDS` 含 `t5wI4`／`Rx5mP`／`z9tytH` 等有 Profile Print 實例的板時多出 in-scope `unresolved` 各 1 需分類。
- NBSP 觸碰即嚴格：LS-201 若觸碰 `HLXo3` 或 `cmp/Card Diary` 定義（`qtCd7`）而未改掉 U+0020 會紅（方向是紅）。
- Pencil `Get` 視訪節點 `n.ref` 可讀性本票未實跑（MCP 被 LS-201 佔用）；若不可讀，第一張真收據 `corner_anchor` 會 `containers=0`（fail-loud），屆時改由 `c` 上下文取。
- `BLEED_RE` 仍是名稱清單，`Mount *` 不在其中（跨 parent 碰撞預設不報 Mount 角托）；不在票文範圍，供 reviewer 裁量。
- NBSP 盲區：只看單位前空白、只擋 U+0020、legacy 板非元件 Age Text 與 Notes 散文不在範圍（全稿一律掃會多 88 筆）。

產出位置：PR #317（base `main`）https://github.com/CLYEH/little-sprout/pull/317，分支 `hotfix/LS-202-design-gate-refinements`，head `aa059dc6eb2c984999fe3e3b86ebcbd69ba66c8b`；離線對照腳本與 mutation 輸出在 session scratchpad（`LS-202-offline-corner.js`／`.out`、`LS-202-mut1.out`／`LS-202-mut2.out`）。

### 2026-09-05T09:39:54.974Z · Chih-Lin Yeh · `b6ae776c`

## merge-review R1 — **APPROVE**（PR #317，head `aa059dc6eb2c984999fe3e3b86ebcbd69ba66c8b`，base `7816413`；2026-09-05 17:37）

範圍：12 檔 +477/−57，五 commit 各司其職。CI 四項全 pass（run 33958029092）。**無 blocker／major**；**1 minor＋4 informational**（含你要的三項裁定）。

### 審點 1：ref 判準與 `unresolved` 取捨 — **我離線重算過，接受限 boards**

我把 `origin/development`（`48d8b43`）的 .pen 離線展開（**每個 instance 複製一份**，與 `resolveInstances` 同形；全程 python 讀明文 JSON、沒碰 Pen）：

| 判準 | 容器數 |
|---|---|
| 舊：名稱 `\bCorner (TL\|TR\|BL\|BR)\b` | **220** ← 與 LS-177 r4 收據 `document_containers` 220 **完全吻合** |
| 新：`type:"ref"` 且 `ref`→`GEBcf` | **259**（實作者報 261） |
| **只在名稱判準（新判準會漏掉的）** | **0** ← 關鍵安全性質：ref 判準是名稱判準的**超集**，不會少抓 |
| 只在 ref 判準（Mount 類） | **39**：`Print` ×26（Profile Print 實例）＋`Icon 40/60/120/180/幾何` ×10＋`Mark` ×1 |

我的 259/39 與實作者的 261/41 差 2，是我的簡化展開器有 depth>8 上限所致（名稱判準 220 完全一致，可見模型正確）；**結論不受影響**。
- `cornerComponentIds` ＝字面 `GEBcf` ∪ 快照 roots 同名元件 → **限縮快照只剩字面 id 的退路成立** ✓（我的 mutation (1b) 拿掉同名元件解析 → 自測紅）。
- 名稱只供方位、無方位進 `unresolved` ✓；`unresolved` 限 `boards`、全稿另列 `document_unresolved` ✓。
- **裁定：接受限 `boards`**（否則 39–41 個他票 Mount 容器每張收據都要逐筆分類，等於把他票舊債綁架本票——與 `corner_anchor.mismatch`／`text_occlusion`／`board_clip` 的既有範圍規則一致）。**你要求的條件已滿足**：SUMMARY 印 `unresolved=<in-scope>/<document>`、`compactLines` 另印 `DOCUMENT-UNRESOLVED <n>× board …` 聚合行，VR 看得到分母 ✓（我 mutation (1c) 把 document 與 in-scope 合一 → 自測紅，這個切分被釘住）。

### 審點 2：`ref: n.ref` 未實跑 — **裁定：原始碼斷言足夠，但「fail-loud」這個說法不成立（minor-1）**

「若不可讀 → `containers=0` fail-loud」我實際查了 gate：`design-evidence-check.sh` 對 `corner_anchor` 只驗「計數是非負整數、`mismatch == 0`、`flagged` 空、`boards` 覆蓋觸碰板、`unresolved` 每筆有分類」——**沒有任何 `containers > 0` 的要求**。所以 `n.ref` 讀不到時的收據長相是 `containers=0, points=0, mismatch=0, flagged=[], unresolved=[]` → **gate 全綠**。那不是 fail-loud，是**靜默失效**：第六支……第四支 `corner_anchor` 整支變成永遠通過，而且比本 PR 之前更糟（名稱判準在現稿能抓到 220 個容器）。詳見 minor-1 的修法。

### 審點 3：evidence cutoff — 通過

- `PERSCAN_MARKER = "document_count"`，`perscan = bool(at_sha) or (is_latest and at_head)`，形制與第五／六支一致 ✓。
- **對 development 上 LS-194 r1／r2 真收據實跑**（sandbox clone，把本 PR 的 gate 覆蓋進去）：**兩份都綠**並各印「LS-202 新欄位不要求——舊收據放行」✓。
- **mutation：`perscan = True`（拿掉 cutoff）→ 16 條紅**（①／⑨／⑬… 既有綠樣本全紅）——cutoff 確實被自測釘住 ✓。

### 審點 4：NBSP — 通過，且我對 development 實跑重現了清單

`bash scripts/gates/design-notes-check.sh design/littlesprout.pen --base origin/main`（clone 內、development 現況）→ **exit 0、「署名 NBSP 違規 0（舊債 9）」**，逐筆與 handoff 一致：`qtCd7`（`cmp/Card Diary` 定義）`zk1yE` ×2、`x7k2o6` ×3；`HLXo3` 實例 `fZ3KF` override `x7k2o6` ×3；`X9PfG` 實例 `HIdMW` override `wQVzs` ×1 ✓。
- mutation：**(2) 把 `if SPACE not in run` 短路（U+0020 也放行）→ 8 條紅**（⑫／⑬／⑬-b／⑭…）；我另加 **(2b) 舊債也算違規 → 3 條紅**、**(2c) 違規也當舊債 → 6 條紅** —— `--base` 增量的**兩個方向都被釘住** ✓。
- 「觸碰即嚴格 → LS-201 會紅」確認是**預期方向**；但 LS-201 票文那句要修，見 info-3。

### 審點 5：bytecode — 通過

`.gitignore:28 __pycache__/`；我在 worktree 內造出 `scripts/gates/__pycache__/probe.pyc` 後：`git check-ignore -v` 命中該行、`git status --porcelain` **0 行**、`cleanup-merged.sh --dry-run LS-202` 摘要印「**worktree dirty 0**」✓（LS-96 `c4c10429` 的症狀確實消失）。四套自測跑完 worktree 內**無 `__pycache__` 殘留** ✓。

### 審點 6

- 自測四套：`overflow-scan.test.js` **37**、`design-evidence-check.test.sh` **61**、`design-notes-check.test.sh` **25**、`agent-tools-check.test.sh` **66**，全 exit 0。
- 我自己的 mutation 共 **8 支、8 支 killed**：(1) 判準退回名稱（過 13 組後停）、(1b) 不認同名元件、(1c) unresolved 不分範圍、(1d) `document_count` 一律用 `flagged`（`AssertionError: SCAN sibling_intersection … scope=document document_count=11`）、(2)(2b)(2c) NBSP 三向、(3) cutoff。
- 逐 commit 歸屬與五組一一對應、**無夾帶**；worktree 乾淨；PR body `--branch … --verify` **exit 0**；head 未前進（`--expect` 再比一次）。
- 文件三處（ui-designer／visual-reviewer／COLLABORATION §7 兩列）與行為一致 ✓，含 `document_unresolved`、NBSP 舊債語意、`scope`／`document_count` 抄寫規則。

### minor-1 · `corner_anchor` 現在有一條「靜默歸零」路徑，且票文寫的名稱備援被拿掉了

- **失敗情境**：Pencil `Get(visit)` 的節點若沒有 `ref` 欄位（本票無法實跑驗證），`isCorner` 全數 false → 收據 `containers=0/points=0/mismatch=0` → **gate 綠**。設計端不會發現第四支已經停擺，而它正是 LS-122 建來抓角托錯位的那支。
- **票文本來就留了退路**：「角托候選改為 ref…（**名稱只作備援**）」，實作把名稱降級成「只供方位」，備援消失。
- **我的量測顯示補回備援是零代價**：development 上「只在名稱判準」= **0**，也就是 `ref ∪ name` 與 `ref` 在現稿結果完全相同（220 ⊂ 259）；補上 `|| CORNER_NAME_RE.test(n.name)` 今天一顆都不會多也不會少，卻能保證「最壞情況退回本 PR 之前的行為」而不是歸零。
- **建議（擇一或並用）**：(a) 依票文把名稱補回成 OR 備援（一行＋一組自測）；(b) 腳本在 `scope=document` 且 `container_corners.length === 0` 時 Print 一行 `⚠ corner_anchor：一顆角托都沒偵測到（ref 欄位可能沒讀到）`，或 gate 對 `document_containers == 0` 的六支收據紅。
- **不阻擋合併的理由**：`n.ref` 不可讀只是未驗證、不是已證偽；LS-201 的第一張真收據就會暴露。**但請在派 LS-201 時明確要求 VR 檢查該收據的 `corner_anchor.containers`／`document_containers` 是否落在 220–260 量級**，若是 0 就是本條命中、立刻回頭補備援。

### informational

**info-1 · `document_count` 在 `scope=boards` 時不是「全稿」數**：票文寫「分段跑時 `document_count` 仍以全稿計」，但 `scanAll` 在 `boards` 模式先 `restrictToBoards` 才掃，任何一支都看不到全稿（要真的全稿得掃兩次，違背 `SCAN_SCOPE=boards` 存在的目的）。實作已在檔頭與 gate 檔頭寫明「`scope=boards` 時為限縮值」，我認為取捨正確；但欄位名 `document_count` 在 boards 模式會誤導只讀單一欄位的人。候選：改名（`scope_count`）或在收據 schema 註記必須與 `scope` 併讀——**入池即可，不必本票改**（改名會動到剛落地的 gate 與 agent 文件）。

**info-2 · `BLEED_RE` 不含 `Mount *`（你要的裁量）——建議入池、不在本票補**：`cross_parent_collision` 的候選仍是名稱清單，`Corner` 在其中而 `Mount TL/BR` 不在，所以 Profile Print／App icon 的角托跨 parent 碰撞不會被報。要修的正解是讓候選集共用本票新的 `isCorner`（ref 判準）——但那會動到 LS-185「候選過濾與全配對逐位元等價」的保證，需要重跑等價證明，超出本票 size 與範圍。

**info-3 · LS-201 票文第 7 條的事實需要更正（跨票協調）**：它寫 `X9PfG/…/HIdMW` 是「**全稿唯一**殘留 ASCII 空白的年齡片語」，但我對 development 實跑是 **9 筆／3 處**（`qtCd7` 定義 5 筆、`HLXo3` 實例 override 3 筆、`HIdMW` 1 筆）。而 LS-201 範圍第 3 條要改 `cmp/Card Album`／`cmp/Card Diary` 的**署名規則**＝一定會觸碰 `qtCd7` → 新 gate 會對那 5 筆變紅。**派 LS-201 前請把票文第 7 條改成這 9 筆清單**，否則設計輪會撞上一個「票文說只有一筆」的紅。

**info-4 · 名稱判準的殘影**：`candidates` 過濾從 `!CORNER_RE.test(name)` 改成 `!isCorner(s)`，所以「名字叫 Corner TL 但不是 ref」的兄弟現在會成為紙面候選（以前被排除）。現稿沒有這種節點（名稱 ⊆ ref），影響為零；記錄供日後查。

### 沒看／沒跑的（誠實揭露）

- **沒有碰 Pen／Pencil MCP**（LS-201 佔用中）：`ref: n.ref` 在真 Pencil 的可讀性**我也無法驗證**，minor-1 就是為此而寫。
- 我的離線展開器是簡化版（depth ≤ 8、不算座標），只用來比對「哪些容器被視為角托容器」，**不驗證 corner-out 幾何**；LS-194 r2 的 3→6 容器對照我沿用實作者的數字，未自行重算。
- 未碰真實模擬器／容器／正式站；**lock 未持有**。

**verdict：APPROVE**（無 blocker／major）。minor-1 建議併前補一行名稱備援（零代價、票文本來就這樣寫），或至少在派 LS-201 時把「檢查 `corner_anchor.containers` 不是 0」寫進派工；info-1／2／3／4 入池或交 LS-201 票文更新。併入 `main` 後補 back-merge `main`→`development`。

### 2026-09-05T09:49:43.083Z · Chih-Lin Yeh · `a6a1b461`

**R2（2026-09-05 17:49 CST）— merge-review R1 `b6ae776c` minor-1 已補，head `fba2f6483bbdcf17b39f4fd30734ce4cb18af942`**

- 修正 commit `e3e8e75d76f3171c3bca06bde6acac8567554d12`（script＋gate＋自測）、文件 `fba2f6483bbdcf17b39f4fd30734ce4cb18af942`，已 push；PR #317 body 加 R2 段——`pr-body-check … --branch hotfix/LS-202-design-gate-refinements --verify` 第一次 exit 1（R2 段一行含「入池」字樣無 comment id，屬引用非申報），改措辭後 exit 0（「已修」行 SHA 反查在 PR commits 內），`gh pr edit 317 --body-file` 已套用。
- **minor-1**：`isCorner`＝ref 判準 ∪ 名稱備援 `Corner TL/TR/BL/BR`（`overflow-scan.js`；票文「名稱只作備援」，development 名稱判準 ⊆ ref 判準、261 個容器不變）；`cornerWarnings`：`document_containers=0` → SUMMARY 後 `WARNING ⚠ … 第四支停擺`＋compactLines 同行，`containers=0` 而 document 非零只印提示；`design-evidence-check.sh` 同 PERSCAN cutoff 下 `scan_scope=document` 且 `corner_anchor.document_containers == 0` 紅。
- **偏離指示一點，請知悉**：指示為「`containers == 0` 判紅」，但 development 既有 4 份正常收據就是 in-scope `containers=0`（LS-133 r1／r2／r3、LS-177 r1：boards 沒有印品，`document_containers` 216）——照指示會把這類正常收據判紅、逼設計端把無關板塞進 `SCAN_BOARDS`。改以 `document_containers`（全稿快照一個角托都沒有＝停擺的唯一長相；merge-review 建議 (b) 同）判，boards 限縮不判；自測 ㊺-c 釘住 LS-133 形狀綠。
- **info-1**：`overflow-scan.js` 檔頭、gate 檔頭、ui-designer.md 收據規則句寫明 `document_count`＝掃描快照內命中數、`scope=boards` 時為限縮值須與 `scope` 併讀（未改名）。
- 自測：`overflow-scan.test.js` 38 組（＋名稱備援三形／名為 Mount 的非 ref 不算／歸零 ⚠ 三態／SUMMARY WARNING 原始碼斷言）、`design-evidence-check.test.sh` 全數（＋㊺／㊺-b／㊺-c）、`design-notes-check.test.sh` 全數、`agent-tools-check.test.sh` 65 組；`agent-tools-check.sh`／`brand-skill-check.sh` 綠；無 `__pycache__` 殘留、worktree 乾淨。
- Mutation：拿掉 `|| CORNER_NAME_RE.test(…)` → `overflow-scan.test.js` exit 1，紅於「名稱備援：無 ref 的 Corner TL/… frame 仍成容器」（scratchpad `LS-202-mut3.out`）。
- 文件同步：ui-designer.md（容器定義＝ref ∪ 名稱備援、WARNING 處置、收據 `document_count` 併讀＋`document_containers` 不得為 0）、visual-reviewer.md（(4) 判準、重掃比對加 `document_containers` 量級 220–261／0＝BLOCKER）、COLLABORATION §7 LS-202 段。
- info-2／info-4 由 orchestrator 處置；info-3（LS-201 票文第 7 條改為 9 筆清單）跨票協調、不在本 PR。

### 2026-09-05T09:52:50.452Z · Chih-Lin Yeh · `7467a858`

**Orchestrator 裁定（09-05 17:52）**：R2 `a6a1b461` 的偏離——gate 改以 `corner_anchor.document_containers == 0` 判紅、不判 in-scope `containers == 0`——**接受**。理由：development 既有 LS-133 r1／r2／r3、LS-177 r1 四份正常收據 in-scope 本就 0（板上沒有印品），照原句判紅只會逼設計端把無關板塞進 `SCAN_BOARDS`；全稿快照一顆角托都算不到才是第四支停擺的唯一長相，自測 ㊺-c 已釘住。

merge-review R2 已派（增量 `aa059dc…fba2f64`），另請 reviewer 判一題：`scope=boards` 時 `document_containers` 是限縮值，若 Pencil 完全沒讀到 `ref`、只剩名稱備援，現有 gate 有沒有讓第四支靜默停擺的路徑。info-3（LS-201 第 7 條「唯一殘留」不成立）已由 LS-201 票文第 8 條涵蓋（9 筆／3 處全改 U+00A0），不另改票。

### 2026-09-05T11:12:24.350Z · Chih-Lin Yeh · `bb2ce8d6`

## merge-review R2 — **APPROVE**（PR #317，head `fba2f6483bbdcf17b39f4fd30734ce4cb18af942`，增量 `aa059dc...fba2f64`；2026-09-05 19:10 CST）

增量 7 檔 +116/−31（`overflow-scan.js`／`.test.js`、`design-evidence-check.sh`／`.test.sh`、ui-designer.md、visual-reviewer.md、COLLABORATION.md），兩支 commit 各司其職。CI 四項全 pass（run 33959007542，headSha 逐字＝`fba2f64`）。**無 blocker／major**；**2 minor＋2 informational**。R1 的 minor-1 我認定**已修**、info-4 **順帶消失**。

### 審點 1：minor-1 修法 — 通過，且「261 不變」我獨立驗過（用的是更強的性質）

`isCorner`（`scripts/design/overflow-scan.js:324`）＝`(type:"ref" && cornerIds.includes(ref)) || CORNER_NAME_RE.test(name)`，`CORNER_NAME_RE = /\bCorner (TL|TR|BL|BR)\b/`（`:143`，**無 `/g`**——有 `/g` 的話 `.test()` 會因 `lastIndex` 殘留而每隔一次回 false，這類靜默漏抓在此不成立）。

我沒有沿用實作者的 261，而是直接驗**union 與 ref 判準是否同一個集合**（這才是「補回名稱備援零代價」的充要條件）。對 head 與 `origin/development` 兩份 .pen 各做一次全樹走訪（含 `descendants` override 的 dict／list 兩種形狀；純 python 讀磁碟 JSON，沒碰 Pen／Pencil）：

| | head `fba2f64` | `origin/development` |
|---|---|---|
| `ref`→cornerIds **且**名稱 `Corner …` | 708 | 708 |
| 只在 `ref` 判準（Mount 類） | 28 | 28 |
| **只在名稱判準（union 會多抓的）** | **0** | **0** |

`cornerIds` 解析結果就是 `['GEBcf']`（全稿沒有第二個名為 `cmp/Photo Corner` 的元件定義根）。**只在名稱判準＝0 ⇒ union ≡ ref ⇒ 容器集合逐一相同、計數必然不變**，不需要重算 261。名為 `Mount` 的非 ref 節點不算（自測 `mountFrames` 案；我在合成樹上也複驗過，見審點 2 的 C 組）。`candidates`（`:359`）用同一個 `isCorner` 過濾，所以 R1 **info-4**（「名字叫 Corner TL 但不是 ref 的兄弟會變成紙面候選」）隨這次修法一併消失。

**Mutation（我自己重跑，不採信 `LS-202-mut3.out`）**：sandbox clone 拿掉 `|| CORNER_NAME_RE.test(n.name || "")` → `node scripts/design/overflow-scan.test.js` **exit 1**，紅在 `AssertionError: 名稱備援：無 ref 的 Corner TL/… frame 仍成容器`；還原後 `git status` 0 行。與實作者回報同一條，killed ✓

### 審點 2：歸零 ⚠／gate 的判準 — **`document_containers` 判準本身成立，我用真實收據語料證實了裁定；但有一條漏判路徑（minor-1）與一個誤報方向（minor-2）**

先給你要的結論所依據的**語料**（`origin/development` 上 43 份收據實跑統計，不是推論）：

- `containers=0` 而 `document_containers` 非零的**正常**收據共 4 份：LS-133 r1／r2／r3、LS-177 r1，皆 `0 / 216`。→ **「in-scope `containers==0` 判紅」確實會把這 4 份形狀判紅，orchestrator 的裁定（改判 `document_containers`）是對的**，我同意，不需要退回。
- `scan_scope=boards` 的收據在語料裡**真的存在**（LS-120 r4／r5、LS-194 r1／r2，後者 `document_containers=3`），所以「boards 限縮不判」不是空談的分支。
- `document_containers` 欄位：43 份中只有 LS-119 r5／r6／r7（legacy 兩支 schema）沒有，其餘全有。

**你問的「boards 限縮收據會不會漏判」——分兩層答：**

1. **「Pencil 完全沒讀到 ref」這個情境，在 R2 之後已經不會歸零**。我用合成樹實跑三組（`scanAll` node 模式）：

| 情境 | scope | containers | document_containers | 警示 |
|---|---|---|---|---|
| A2 ref 正常、限縮到「沒有印品的板」 | boards | 0 | 0 | **⚠ 第四支停擺（誤報，見 minor-2）** |
| B1／B2 **ref 全失**、名稱仍是 `Corner TL/…` | document／boards | 1 | 1 | 無（名稱備援接住 ✓） |
| C1／C2 ref 全失**且**名稱是 `Mount` | document／boards | 0 | 0 | ⚠ 第四支停擺 |

也就是說：ref 讀不到 → 退回 220 級（B 組），**不是 0**；要真的全零必須「ref 讀不到 **且** name 也讀不到」的雙重失效（C 組是模擬這個）。所以「`scope=boards` 時另以名稱備援下限（220 級）判」這個提議**在 boards 模式下沒有可用的分母**——限縮快照本來就只該有那幾板的角托，220 是全稿數字，拿來當 boards 的下限只會把 LS-194（`document_containers=3`）這種正常收據判紅。**結論：不建議加，現行「boards 不判」是對的取捨。**

2. **但漏判路徑存在，而且不在 boards 那一側**——見 minor-1。

**Mutation（gate 側）**：把 `design-evidence-check.sh:533` 的 `if isinstance(ca_scan, dict) and scan_scope != "boards":` 改成 `if False:` → `design-evidence-check.test.sh` **exit 1**，紅在 ㊺（期望 exit 1 實得 0）。這條新檢查確實被自測釘住 ✓

### minor-1 · 收據**省略** `document_containers` 就繞過新檢查（`scripts/gates/design-evidence-check.sh:533-540`，root cause 在 `:391`）

- **失敗情境（我實跑重現，不是推論）**：把自測 fixture 的 `zerodoc` 模式改成「其餘全零、但收據裡**沒有** `document_containers` 這個鍵」，其餘不動 → ㊺ 由紅翻綠，gate **exit 0**（輸出：`✗ ㊺ …（期望 exit 1、實得 0）`）。原因是 `dcont = ca_scan.get("document_containers")` 取到 `None`，`None == 0` 為 False；而 `check_corner_anchor`（`:391`）的必填整數清單是 `("containers", "points", "mismatch", "document_mismatch")`——**`document_containers` 從來不是必填**。同理 `"0"`（字串）也繞得過。
- **為什麼這條要修**：這支 gate 的存在理由就是「收據是 agent 手抄的、抄漏／抄錯要機械擋下」（LS-185／LS-202 兩次 cutoff 都是為了處理欄位缺漏）。真的發生第四支停擺時，收據長相是「一排 0」，而「一排 0 之中順手漏抄一個鍵」比「忠實抄下 `document_containers: 0`」更可能。現在 R2 只擋得住忠實抄下的那一種。
- **建議修法**：把 `document_containers`（連同 `document_points` 一起也可以）加進 `:391` 的必填整數清單——但**必須放在 `perscan` cutoff 之下或用「有才驗＋perscan 才必填」的寫法**：語料裡 LS-119 r5／r6／r7 三份 legacy 收據沒有這個鍵，無條件必填會把它們判紅（方向錯）。加一組自測（缺鍵 → 紅、legacy 仍綠）即可。
- **不阻擋合併的理由**：相對 R1（完全沒有這條檢查）純屬改善，沒有任何回歸；且真正的停擺情境現在需要 ref＋name 雙重失效才成立，第一道防線（腳本內的名稱備援）比 gate 更前面；腳本也會 `Print` 出 ⚠、VR 文件已加「量級 220–261／0＝BLOCKER」的人工複核。故列 **minor**，不是 major。

### minor-2 · `SCAN_SCOPE=boards` 且該批板沒有印品時，腳本會印「第四支停擺、收據不得交」的**誤報**（`scripts/design/overflow-scan.js:624-632`，連帶 `.claude/agents/ui-designer.md:30`）

- **失敗情境（合成樹實跑，上表 A2）**：`scanAll(nodes, {boards:["Q"], scanScope:"boards"})`，Q 板內完全沒有印品 → 限縮快照的 `document_containers` 就是 0 → `cornerWarnings` 走**第一分支**，印出「整份快照沒有任何角托容器——第四支停擺（Pencil 快照沒讀到 ref、名稱備援也沒命中），**這份收據不得交**」。但同一份收據送進 gate 是**綠**（自測 ㊺-b 正是釘這個）。腳本叫設計端別交、gate 說可以交，兩邊互相打架。
- **會踩到誰**：語料裡 `scan_scope=boards` 的收據已有 4 份；LS-133／LS-177 r1 那種「本輪只動非印品畫面」的票，只要因為大稿逾時改用 `SCAN_SCOPE=boards`（ui-designer.md 明文教的逾時對策），就會拿到這句誤報，白跑一輪或誤把 `SCAN_BOARDS` 亂加板。
- **建議修法**：`cornerWarnings` 讀 `ca.scope`（每支物件已經帶了）——`scope==="document"` 時才用現在這句「停擺／不得交」；`scope==="boards"` 時改印中性句（例如「限縮快照內沒有角托容器：`SCAN_BOARDS` 若含印品類板即為停擺、否則正常」）。ui-designer.md 那句同步補上 scope 條件。
- 列 **minor**：行為我已機械重現（不是 PLAUSIBLE），但只影響輸出訊息與人的判斷，gate 判定正確、不會誤紅。

### 審點 3：自測與環境 — 全綠（我在 sandbox clone `fba2f64` 上重跑，非採信 handoff）

| 項目 | 結果 |
|---|---|
| `node scripts/design/overflow-scan.test.js` | exit 0，**38 組** |
| `bash scripts/gates/design-evidence-check.test.sh` | exit 0，**64 條 ✓**（含 ㊺／㊺-b／㊺-c 三條逐字通過） |
| `bash scripts/gates/design-notes-check.test.sh` | exit 0，**25 條 ✓** |
| `bash scripts/gates/agent-tools-check.test.sh` | exit 0，**65 組** |
| `agent-tools-check.sh`／`brand-skill-check.sh`（真實 agents） | 皆 exit 0 |
| worktree `git status --porcelain` | **0 行**；`find -name __pycache__` **無命中**（四套跑完之後） |
| `pr-body-check.sh <body> --branch … --verify` | **exit 0**（必須在 LS-202 worktree 內跑；我第一次在主 checkout 跑得到 exit 1，是 `merge-base --is-ancestor e3e8e75 HEAD` 對 `main` 不成立的環境假紅，非 body 問題——提醒後續 reviewer） |

### 審點 4：文件同步 — 一致

- `ui-designer.md:30` 容器定義改為「ref ∪ 名稱 `Corner TL/TR/BL/BR` 備援、`Mount TL/BR` 靠 ref、方位取自名稱」＋WARNING 處置＋「CI 對 `scan_scope=document` 的這種收據紅」；`:52` 收據規則句加「`document_count` 是**掃描快照**內的命中數、`scope=boards` 時為限縮值須與 `scope` 併讀、`scan_scope=document` 時 `corner_anchor.document_containers` 不得為 0」——與 `design-evidence-check.sh:527-540` 逐條對得上（唯一不精確處是 minor-2 的 scope 條件）。
- `visual-reviewer.md:36` (4) 判準改為「ref ∪ 名稱備援」；`:56` 重掃比對加「`document_containers` 量級：ref∪名稱 261、舊名稱判準 220；`scan_scope=document` 下為 0＝第四支停擺、直接 BLOCKER」——**scope 條件寫對了**（比 ui-designer.md 精確）。
- `COLLABORATION.md:257` LS-202 段與程式一致（含「不以 in-scope `containers` 判」的理由與 LS-133／LS-177 r1 的 216 反例）。

### 審點 5：scope — 乾淨

增量只碰 2 支腳本＋2 支自測＋3 份文件；`git diff --stat` 無 `design/`／`ios/`／`supabase/` 任何檔案，無順手重構、無夾帶。兩支 commit 訊息與內容相符（`e3e8e75` script＋gate＋自測、`fba2f64` 純文件）。

### 四維度

- **Race**：gate 只讀收據檔與 `git show` 快照，無共享可變狀態；`CORNER_NAME_RE` 無 `/g`（無 `lastIndex` 跨呼叫殘留）、`cornerWarnings` 為純函數。無發現。
- **效能**：`isCorner` 多一次非全域 regex，落在既有的單趟 `liveNodes` 走訪（O(n)）與 `candidates` 過濾（原本就是同一個 predicate），`cornerIds` 是 1 元素陣列的 `includes`；`cornerWarnings` O(1)。**無新增 O(n²)**；`overflow-scan.test.js` 的 3000 節點效能案仍過（44ms → 13ms 不變）。
- **平行**：批次／併發語意本票不涉及，無序列化瓶頸可指。
- **Scope**：見審點 5，無 finding。

### informational

**info-1（R1 info-1 的後續）· `COLLABORATION.md:257` 仍寫「該支在整份快照的命中數」**：`overflow-scan.js` 與 gate 檔頭都已改成「掃描快照」，COLLABORATION 這句沒跟著改（下半句「`scope=boards` 時為限縮值」有救回來，不會誤導到底）。純文字一致性，入池即可。

**info-2 · `check_corner_anchor` 的必填整數清單與 ui-designer.md 的收據模板不同步**：模板（`ui-designer.md:45`）列了 `document_containers`／`document_points`，gate（`:391`）只驗四個。minor-1 若照建議修，順手把 `document_points` 一起納入 `perscan` 條件下的必填即可收斂。

### 沒看／沒跑的（誠實揭露）

- **沒有碰 Pen／Pencil MCP**：`n.ref`／`n.name` 在真 Pencil `Get(visit)` 的可讀性**依然沒有實跑驗證**（與 R1 同）。本輪的名稱備援正是為此而補，但「備援本身也失效」的 C 組情境我只能在合成樹上模擬。
- 票文驗收「對 `origin/development` 現稿跑六支、`corner_anchor` 不出現新誤報」需要 Pen 版面引擎，**我沒跑**；我改以「union ≡ ref（只在名稱判準＝0）」這個更強的離線性質替代，能保證容器集合不變，但**不驗證幾何 corner-out 判定**。
- NBSP（第三項）與 `sys.dont_write_bytecode`（第四項）本輪增量未觸碰，我只重跑自測確認沒回歸，**沒有重做 R1 的逐筆對照**。
- 未持有 supabase lock（本票不涉 DB／容器）；未碰模擬器、正式站。

**verdict：APPROVE**（無 blocker／major）。minor-1 建議在本 PR 併前補（一行必填清單＋一組缺鍵自測，注意 legacy 三份收據要放行），或記入 LS-96 由下一張 design gate 票收；minor-2 與 info-1／info-2 入池即可。併入 `main` 後補 back-merge `main`→`development`。

### 2026-09-05T11:15:36.225Z · Chih-Lin Yeh · `dcc5dab9`

**Orchestrator（09-05 19:14）**：merge-review R2 `bb2ce8d6` APPROVE 帶 2 minor（minor-1 收據省略 `document_containers` 可繞過新檢查；minor-2 `scope=boards` 無印品時誤印「第四支停擺」）＋2 info。裁定：**併入前做 R3** 四項全收（gate 能靠省略一個鍵繞過，不能帶著上線；其餘三項都是同檔一行級）；reviewer 對「`scope=boards` 不加名稱備援下限」的結論採納（220 是全稿分母，套限縮快照會誤紅 LS-194 型收據）。R1 遺留 info-2（`BLEED_RE` 不含 `Mount *`）已入池 `56de90a6`；info-3 由 LS-201 第 8 條涵蓋。已續派實作 agent；R3 push 後派 reviewer 審增量並對新 head 貼 status，再走 hotfix 鏈（merge main → back-merge dev → promote）。

### 2026-09-05T11:22:22.747Z · Chih-Lin Yeh · `c27eefdd`

**R3（2026-09-05 19:21 CST）— merge-review R2 `bb2ce8d6` 2 minor＋2 info 已收，head `9339c09c5ecb9e064b7cac1b892b9c32f0fc54f0`**

Ticket：LS-202

已完成（每項一個 commit，已 push；PR #317 body 加 R3 段，`pr-body-check … --branch hotfix/LS-202-design-gate-refinements --verify` exit 0、`gh pr edit 317 --body-file` 已套用、PR head 同 sha）：
1. **minor-1** `9bbe72bee05bb4822dcd36f52cfc400d128810e5`：`design-evidence-check.sh` PERSCAN cutoff 下 `corner_anchor.document_containers` 必填（非負整數），缺鍵／型別錯紅「必填且須為非負整數（收據=None）……省略此鍵不得繞過」；`scan_scope=document` 且為 0 仍紅、boards 不判歸零；必填鍵清單（`boards`／`containers`／`points`／`mismatch`／`document_containers`／`document_mismatch`／`unresolved`）寫進 gate 檔頭。自測 ㊺-d 省略鍵 → 紅；write_receipt6 系列（cutoff 前）沒有此鍵仍綠。
2. **info-2** `d44a3d7fe7de338bc37a8a0b4e287464f6dfc406`：ui-designer.md corner_anchor 規則句列 CI 必驗鍵、與 `check_corner_anchor`＋LS-202 段同步（模板本身已列 `document_containers`，不動）。
3. **minor-2** `b4817842696d57362cca5235f86712df93a37825`：`overflow-scan.js` `cornerWarnings` 依 `ca.scope` 分流——`boards` 限縮且 `document_containers=0` 只印「限縮快照內沒有角托容器」提示（不印停擺／不得交）；`document`（或直接呼叫無 scope 標籤）仍印停擺；檔頭 (d) 與 ui-designer.md:30 同步。自測 38→39。
4. **info-1** `9339c09c5ecb9e064b7cac1b892b9c32f0fc54f0`：COLLABORATION §7 LS-202 段「整份快照」→「掃描快照」，補 R3 兩條規則句。

已驗證：
- 四套自測綠：`overflow-scan.test.js` 39 組、`design-evidence-check.test.sh` 全數（＋㊺-d）、`design-notes-check.test.sh` 全數、`agent-tools-check.test.sh` 65 組；`agent-tools-check.sh`／`brand-skill-check.sh` 綠；無 `__pycache__`、worktree 乾淨。
- **development 既有收據回歸**：依各票原 PR 範圍（merge commit `^1..^2`，收據自 PR head 暫時 materialize、跑完刪除後 `git checkout -- design/evidence/` 還原）跑現行 gate——LS-194 2／LS-177 4／LS-133 3／LS-163 3／LS-152 5／LS-142 7／LS-120 9／LS-119 9，**42／42 綠**（皆在 cutoff 前、印「LS-202 新欄位不要求——舊收據放行」；LS-119 r5–r7 走 LEGACY 兩支 schema）。`design/evidence/` 實際 42 份、非 43（scratchpad `LS-202-dev-receipts-gate.out`）。
- Mutation（`LS-202-mut4.out`）：把必填檢查改成 `if False`（退回 R2 只在鍵存在時判）→ `design-evidence-check.test.sh` exit 1，紅於「㊺-d 省略 corner_anchor.document_containers → 紅（期望 exit 1，實得 0）」——省略鍵 → 紅 被自測釘住。

未完成／剩餘：
- 併回 `main` 後補 back-merge `main`→`development`；info-2／info-4（R1）與 R2 的 info 由 orchestrator 處置；info-3（LS-201 票文第 7 條 9 筆清單）跨票協調。
- 沒有新的 informational 待處置（R2 兩 info 皆已在本輪收掉）。

風險與已知問題：
- 沿前：Pencil `n.ref` 可讀性未實跑（名稱備援兜底；第一張 `scope=document` 真收據請 VR 核對 `document_containers` 落在 220–261 級，=220 代表 ref 沒讀到）；`BLEED_RE` 不含 `Mount *`；NBSP 觸碰即嚴格對 LS-201 的影響。

產出位置：PR #317 https://github.com/CLYEH/little-sprout/pull/317，分支 `hotfix/LS-202-design-gate-refinements`，head `9339c09c5ecb9e064b7cac1b892b9c32f0fc54f0`（R3 四 commit：`9bbe72b`／`d44a3d7`／`b481784`／`9339c09`）。

### 2026-09-05T11:30:53.442Z · Chih-Lin Yeh · `55caa0a7`

## merge-review R3 — **APPROVE**（PR #317，head `9339c09c5ecb9e064b7cac1b892b9c32f0fc54f0`，增量 `fba2f64...9339c09`；2026-09-05 19:29 CST）

增量 6 檔 +49/−13，四個 commit 一項一支。CI 四項全 pass（run 33963123687，`ci` 7m2s／`db`／`lint`／`rules` 皆綠，headSha 逐字＝`9339c09`）。**無 blocker／major／minor**；**1 informational**（一個數字筆誤，源頭是我 R2 的錯）。R2 的 minor-1／minor-2／info-1／info-2 我逐條複驗，**四條全部收掉**。

### ① minor-1（省略 `document_containers` 繞過）— **關閉，我用 R2 那支探針原樣重放**

不採信 ㊺-d，我把 R2 的 M3 探針**逐字重放**（改 `zerodoc` fixture 成「完全沒有 `document_containers` 這個鍵」，其餘不動）：

| | R2（`fba2f64`） | R3（`9339c09`） |
|---|---|---|
| 探針收據送進 gate | **實得 0**（綠，繞過成功） | **實得 1**（紅，繞過失敗） |

（R3 這行仍顯示 `✗ ㊺`，但那是**訊息字串不同**——省略鍵時走的是新的「必填且須為非負整數」錯誤，不是「為 0 而 scan_scope=document」；決定性的是 exit code 由 0 變 1。）修法位置 `design-evidence-check.sh:533-544`：先驗必填＋型別，`elif scan_scope != "boards" and dcont == 0` 才判停擺——**必填檢查不分 scope**（boards 收據也得帶這個鍵），歸零判定仍只對 document，兩件事切得乾淨。實作者的 ㊺-d 用 `containers:1` 的 fixture，把「缺鍵」與「歸零」兩個失敗因子隔離，這個測試設計是對的。

### ② 既有收據回歸 — **抽驗兩票，全綠（含 legacy 形狀）**

用「歷史 PR head 開 worktree ＋ 覆蓋 R3 的 `scripts/gates/`＋`scripts/design/`」跑現行 gate（第一次試跑因為只換了 `design-evidence-check.sh`、沒換 `design-landing-check.sh`，撞到舊版沒有 `--print-nodes` 的假紅；補齊 helper 後正常，記錄下來供日後複驗者避雷）：

| 票 | head／base | 收據 | 結果 |
|---|---|---|---|
| **LS-133** | `cf5cebc` / `1cdb7fc` | 3 份 | **exit 0**，3 ✓／0 ✗（正是 `containers=0` × `document_containers=216` 那個形狀——minor-1 的修法最可能誤傷它，沒有誤傷） |
| **LS-119** | `6cb9b91` / `9d4fdd8` | 9 份 | **exit 0**，9 ✓／0 ✗（含 r5–r7 走 LEGACY 兩支 schema、無 `document_containers` 鍵） |

兩票都在 cutoff 前 → `perscan` False → 必填檢查根本不進場（輸出各印 6／27 行「舊收據放行」）。這與實作者 42／42 的回歸結論方向一致；**我只抽驗 2 票 12 份，另 30 份沿用實作者數字未自行重跑**。

### ③ minor-2（boards 誤報停擺）— **關閉，且「沒把 document 的停擺一起關掉」我用兩支 mutation 釘過**

我把 R2 那支合成樹探針對 R3 腳本重跑，四種情境的訊息分流完全正確：

| 情境 | scope | document_containers | R2 訊息 | R3 訊息 |
|---|---|---|---|---|
| A2 板內本來沒印品 | boards | 0 | ⚠ **第四支停擺／不得交**（誤報） | ⚠「限縮快照內沒有角托容器」提示 ✓ |
| C1 真停擺（ref＋名稱雙失效） | document | 0 | ⚠ 第四支停擺 | ⚠ **第四支停擺**（未被關掉）✓ |
| C2 真停擺但限縮跑 | boards | 0 | ⚠ 第四支停擺 | 降級為提示（**可接受**：訊息自己寫明「限縮模式判不出停擺，第一張 `scope=document` 收據要看 220–261 量級」，把責任交棒交得明確） |
| B1／B2 只有 ref 讀不到 | 兩者 | 1 | 無警示 | 無警示 ✓（名稱備援仍接住） |

**Mutation 兩支，皆 killed**（都紅在新增的第 39 組）：
- `if (ca.scope === "boards")` → `!==`（分流反轉）→ `overflow-scan.test.js` **exit 1**，失敗處印出 boards 訊息出現在期望停擺訊息的位置。
- `if (ca.scope === "boards")` → `if (true)`（等於把 document 的停擺警告整個關掉）→ **exit 1**。

也就是「靜悄悄地把停擺警告關掉」這條路已被自測堵住。新測試連 `scope: undefined`（直接呼叫 `scanCornerAnchor`、沒有 scanAll 貼標籤）也斷言視同 document 印停擺——fail-loud 方向正確。

### ④ 必填鍵型別檢查不誤傷正常收據 — 實跑 round-trip 驗過

`document_containers` 在 JS 端是 `+= 1` 累加的整數，`JSON.stringify` 輸出 `216` 這種無小數點的字面；我實跑 JS→JSON→python：`typeof number`／`Number.isInteger true` → python `isinstance(int) True`、`isinstance(bool) False`、`>= 0` → **必填檢查通過，不誤紅**。浮點（`216.0`）不可能由本腳本產生。`scope=boards` 且合法為 0 的收據也通過（0 是非負整數，歸零判定跳過）——㊺-b 綠釘住。

### ⑤ 自測與 scope

| 項目 | 結果 |
|---|---|
| `node scripts/design/overflow-scan.test.js` | exit 0，**39 組**（R2 的 38＋R3 分流 1） |
| `bash scripts/gates/design-evidence-check.test.sh` | exit 0，**65 條 ✓**（含 ㊺／㊺-b／㊺-c／**㊺-d**） |
| `bash scripts/gates/design-notes-check.test.sh` | exit 0，**25 條 ✓** |
| `bash scripts/gates/agent-tools-check.test.sh` | exit 0，**65 組** |
| `agent-tools-check.sh`／`brand-skill-check.sh` | 皆 exit 0 |
| worktree `git status` ／`__pycache__` | **0 行**／**無** |
| `pr-body-check … --branch … --verify`（worktree 內） | **exit 0** |

**scope 乾淨**：增量只碰 `overflow-scan.js`／`.test.js`、`design-evidence-check.sh`／`.test.sh`、`ui-designer.md`、`COLLABORATION.md` 六檔，無 `design/`／`ios/`／`supabase/`，無夾帶重構。四個 commit 訊息與內容一一對應。

**文件同步**：ui-designer.md:30 的 WARNING 處置已加 scope 條件（minor-2 的文件面）；:52 列出 CI 必驗鍵（`boards`／`containers`／`points`／`mismatch`／`document_containers`／`document_mismatch`／`unresolved`）——與 `check_corner_anchor` ＋ perscan 段的實際行為逐鍵對得上（info-2 收掉）；COLLABORATION §7「整份快照」→「掃描快照」並補 R3 兩條規則句（info-1 收掉）。

**四維度**：race — gate 仍只讀檔、`cornerWarnings` 純函數、無共享狀態；效能 — 新增一次字串比較與一次型別檢查，O(1)，3000 節點效能案不變；平行 — 不涉；scope — 見上，無 finding。

### informational

**info-A · `design-evidence-check.sh:68` 寫「cutoff 之前的收據（development 43 份）不受影響」，實際是 42 份**：`git ls-tree -r origin/development design/evidence | grep -c overflow.json` ＝ **42**（LS-119 9＋LS-120 9＋LS-133 3＋LS-142 7＋LS-152 5＋LS-163 3＋LS-177 4＋LS-194 2）。**這個 43 是我 R2 verdict 裡的筆誤被抄進註解的**（實作者自己的 handoff 寫的是正確的 42），責任在我，不是實作者的疏忽。純註解數字，不影響任何判定；下次有人動這支 gate 時順手改 43→42 即可，**建議入池 LS-96**，不值得為它再開一輪。

### 沒看／沒跑的（誠實揭露）

- **仍未碰 Pen／Pencil MCP**：`n.ref`／`n.name` 在真 Pencil `Get(visit)` 的可讀性三輪都沒有實跑；R3 沒有改變這個盲區。第一張 `scope=document` 真收據仍請 VR 核 `document_containers` 落在 220–261（=220 代表 ref 沒讀到、只剩名稱備援在撐）。
- **`scope=boards` 收據仍偵測不到真停擺**（上表 C2）——這是 orchestrator 已採納的取捨（220 是全稿分母，套限縮快照會誤紅 LS-194 型收據），R3 的做法是把「判不出」寫進訊息本身、交棒給第一張全稿收據，我認為這是這個資訊量下的最佳解，不再列 finding。
- 既有收據回歸**只抽驗 LS-133／LS-119 兩票 12 份**，另 30 份未自行重跑。
- NBSP 與 `sys.dont_write_bytecode` 兩項 R3 未觸碰，只靠自測確認無回歸。
- 未持有 supabase lock（本票不涉 DB／容器）；未碰模擬器、正式站。

**verdict：APPROVE**（無 blocker／major／minor）。可直接走 hotfix 鏈：merge → `main`，補 back-merge `main`→`development`，再 promote。info-A 入池 LS-96。

### 2026-09-05T11:32:11.646Z · Chih-Lin Yeh · `dfef4a91`

**Orchestrator（09-05 19:32）**：merge-review R3 `55caa0a7` APPROVE（R2 四條全收；info-A「43→42」入池 LS-96）。hotfix 鏈已啟動：等 #317 CLEAN → merge main → main tip 貼 `promote: no content diff` → back-merge PR main→development → 併入後 dev tip 貼 status → promote dev→test → delta QA（harness：對 development 現稿跑六支、第一張 `scope=document` 真收據由 LS-201 R2 產出並核 `document_containers` 220–261 級）→ test→main。

### 2026-09-05T11:45:44.860Z · Chih-Lin Yeh · `704a31b5`

**Orchestrator（09-05 19:45）**：PR #317 已併 main `4832eff`（main tip status ✓）；back-merge PR #318 已併 development `f34c078`。鏈腳本在最後一步貼 dev tip status 時因 `$PR）`（全形括號緊貼變數）unbound 中斷，已手動補貼並啟動 promote dev→test；test 前進後派 delta QA（harness）→ test→main → 收尾。

### 2026-09-05T12:05:58.167Z · Chih-Lin Yeh · `eedcb82e`

**Orchestrator（09-05 20:04）**：back-merge 後 development `6d9e01e` 已 promote 到 test（與 LS-164 同批）→ QA。已派 qa 合併 delta QA（harness 段：四套自測、既有收據三票回歸、離線角托容器量級與 NBSP 舊命中只警告、`__pycache__` 防線；「現稿跑六支」由 LS-201 第一張 document 真收據承接）。PASS 後 test→main → sweeper → Done → `cleanup-merged.sh --apply LS-202`。

### 2026-09-05T14:11:16.303Z · Chih-Lin Yeh · `5377d3bc`

**Orchestrator（09-05 22:09）**：合併 QA agent 無產出已停止並重派，本票 harness 段排第一（四套自測、三票既有收據回歸、離線角托量級 220、NBSP 舊命中只警告、`__pycache__` 防線），verdict 會先單獨落地；「現稿跑六支」由 LS-201 r3 收據承接（`document_containers` 220／`unresolved` 0）。

### 2026-09-05T14:18:05.020Z · Chih-Lin Yeh · `0fd775c8`

## QA — LS-202（2026-09-05 22:17 CST）

**驗於** `test` tip `6d9e01efc28be9520fdbf8fa640a024c33daf906`（= `origin/test`，未再前進），worktree `.claude/worktrees/qa-test`（detached）。純 harness 票，本段不需 lock、不需模擬器。

### 驗收條件逐條

1. **三支自測全綠＋mutation**（票文寫「三支」，實際本輪自測套件為四支——沿用票文範圍逐一驗）— ✓
   - `node scripts/design/overflow-scan.test.js`：**39 組全過**，exit 0（含 R2/R3 名稱備援、歸零警示依 scope 分流、`document_count` 等 LS-202 新增案例）
   - `bash scripts/gates/design-evidence-check.test.sh`：**全數通過**（含 ㊺／㊺-b／㊺-c／㊺-d 四條 `document_containers` 必填案），exit 0
   - `bash scripts/gates/design-notes-check.test.sh`：**全數通過**（含 ⑫～⑯-b NBSP 案），exit 0
   - `bash scripts/gates/agent-tools-check.test.sh`：**65 組全過**，exit 0
   - 真實 gate：`agent-tools-check.sh`／`brand-skill-check.sh` 對真實 `.claude/agents/` 皆 exit 0
   - mutation（角托判準退回名稱、NBSP 檢查移除）已由三位 merge-reviewer round 逐一實跑釘住（R1 `b6ae776c`／R2 `bb2ce8d6`／R3 `55caa0a7`），本輪未重跑 mutation（純自測回歸已涵蓋）

2. **對 `origin/development` 現稿跑六支：`corner_anchor` 不出現新誤報、收據六支皆有 `scope`／`document_count`**
   - **收據 schema 面（可機械驗）— ✓**：對三張既有 PR 的全部 9 份收據（LS-133 r1/r2/r3、LS-177 r1/r2/r3/r4、LS-194 r1/r2）用**當前** `design-evidence-check.sh`／`design-landing-check.sh`／`design_tree_hash.py`（overlay 到各 PR head 的臨時 worktree）重跑：**9/9 綠**，含新欄位必填檢查（舊收據落 cutoff 前放行、新收據驗 `scope`／`document_count` 齊全）。
   - **對現稿實跑六支引擎面（需 Pen 版面引擎）— ⊘ 不可跑**：QA 白名單無 Pencil MCP 視覺驗收以外的引擎存取，六支掃描需要 Pencil `Get(visit)` 快照才能執行。**依 orchestrator 裁定由 LS-201 r3 收據承接**（另票證據，已產出：`document_containers=220`／`unresolved=0`），本票不重複驗證。
   - **離線判準量級交叉核對（可機械驗，佐證 R1-R3 reviewer 的算法未偏移）— ✓**：對現稿 `design/littlesprout.pen` 用 `overflow-scan.js` 同款 `CORNER_NAME_RE` 與 ref 判準（`type:"ref"` 且 `ref→GEBcf`／同名元件定義）離線分組：名稱判準容器＝**220**（與各 reviewer round 反覆核對的量級一致）、ref 判準容器＝261、**「只在名稱判準、ref 判準抓不到」＝0**（安全性質：名稱 ⊆ ref，union 與 ref 恆等，不會漏抓）。此為 LS-202 R1 minor-1 修法的核心安全論證，本輪重新獨立驗算一次仍成立。

3. **`design-notes-check.sh` NBSP 機械檢查對現稿只警告不紅** — ✓
   - `bash scripts/gates/design-notes-check.sh design/littlesprout.pen --base origin/main`：**exit 0**，印「署名 NBSP 違規 0（舊債 9）」——9 筆既有 U+0020 命中（`qtCd7`／`HLXo3` override／`X9PfG` override）皆印「本 PR 未觸碰此板，不擋（LS-202）」，無一筆判紅。與票文「對現稿跑一次確認 0 命中」的精神一致（0 違規；9 筆舊債走既有豁免路徑，未觸碰不擋）。

4. **三支 design python gate 不留 `__pycache__`** — ✓
   - `design_notes_check.py`／`design_tree_hash.py` 檔頭皆有 `sys.dont_write_bytecode = True`；`design-evidence-check.sh` 內嵌 python（heredoc，非獨立檔案）以 `PYTHONDONTWRITEBYTECODE=1` 環境變數達同效果。
   - `.gitignore:28` 含 `__pycache__/`（另 `:51` `design-canvas*/__pycache__/`）。
   - 實測：四套自測＋兩支真實 gate 跑完，`find scripts -name __pycache__` **零命中**。

5. **merge-reviewer 通過；hotfix 流程（自 main、PR 回 main、back-merge）** — ✓（非本次 QA 職責範圍，僅核對記錄）
   - PR #317：R1 `b6ae776c` APPROVE（1 minor＋4 info）→ R2 `bb2ce8d6` APPROVE（2 minor＋2 info）→ R3 `55caa0a7` APPROVE（0 blocker/major/minor，1 info）。
   - hotfix 鏈：merge main `4832eff`，back-merge PR #318 → development `f34c078`，promote dev→test，本次驗收的 `6d9e01e` 即為併入 LS-164 後的 test tip。

### 未完成／缺口
- 條件 2 的「對現稿實跑六支引擎面」在 QA 職責與工具白名單下不可自跑（無 Pencil 引擎存取），依 orchestrator 裁定交棒 LS-201 r3 收據承接，非本票遺留缺口。
- 三支自測回歸未重跑各自的 mutation（已由 merge-review 三輪逐一釘住，本輪只做純回歸自測＋schema 面實跑，判定足夠）。

### Verdict：**PASS**

模擬器：無 boot（本段純 harness，不需模擬器）。lock：未持有（本段不涉 DB／容器）。

### 2026-09-05T15:13:50.701Z · Chih-Lin Yeh · `1556409f`

## dead-code sweep — LS-202

**範圍**：`git diff 48d8b43...4832eff` 限定於 orchestrator 指定的 harness 檔案（`scripts/design/overflow-scan.js`＋`.test.js`、`scripts/gates/design-evidence-check.sh`＋`.test.sh`、`design-notes-check.sh`／`design_notes_check.py`＋`.test.sh`、`design_tree_hash.py`、`.gitignore`、`.claude/agents/ui-designer.md`／`visual-reviewer.md`、`docs/COLLABORATION.md`）。同一 diff range 內混入的 LS-200（`album_summaries` view／API.md）不在本次範圍，未觸碰。

### Findings（本 feature 引入）

**1 項，informational，建議併入下一張相關 harness 票順手清（不影響功能，非阻斷）：**

- `.gitignore:51` `design-canvas*/__pycache__/` 現已完全是 `.gitignore:28` 新增的全域規則 `__pycache__/`（LS-202 commit `78d38e1`）的子集，成為多餘（但無害）的重複條目。
  - 證據：`git log -1 --format=%H -S"design-canvas*/__pycache__/" -- .gitignore` → `27811d0`（LS-51，早於本票），`git log --oneline -- .gitignore` 確認 `78d38e1`（本票）才加入通用 `__pycache__/`。`.gitignore` 對重複／被涵蓋的規則不會報錯，純粹是可讀性負債，不是功能性死碼。
  - 建議處置：下次動 `.gitignore` 的票順手刪這一行；不值得為此單獨開票。

### 未發現的死分支類型（已依 orchestrator 指定重點逐一排查，結論：無）

- **R1→R3 迭代死分支**：`cornerWarnings()`（`overflow-scan.js:625-636`）在 R2 引入、R3 補上 `scope==="boards"` 分流，兩個分支（`scope=boards` 印提示／其餘印「第四支停擺」）都有對應自測命中（`overflow-scan.test.js` 的兩則 LS-202 R2／R3 歸零警示測試），且在 `scanAll`／`compactLines` 兩處呼叫點都能走到，非死碼。`cornerWarnings` 被呼叫兩次（SUMMARY 後一次、`compactLines` 內嵌一次）造成同一組警示行重複輸出兩次——這是既有設計（自 R2 起即如此，不是本輪新增的重複），不算 R1→R3 遺留分支，僅在此記錄供 orchestrator 判斷是否值得順手精簡（不影響正確性，只是輸出稍冗）。
- **名稱備援退化路徑（ref 判準加入後的舊純名稱路徑）**：`isCorner()`（`overflow-scan.js:322`）用 `(ref 判準) || (名稱判準)` 的 OR 組合，兩側都有獨立自測命中（LS-202 R2 名稱備援測試：無 ref 的 `Corner TL/…` frame 仍算、`ref` 指向他元件但名稱是 `Corner …` 仍算、`Mount TL/BR` 只有 ref 判準能看到），不是「加了新判準後舊路徑永遠到不了」的死碼，是刻意的雙軌容錯（讀不到 `ref` 時最壞退回舊行為）。
- **gate 內 legacy cutoff 分支是否仍有收據會走到**：`design-evidence-check.sh` 的三層 cutoff（`LEGACY_CUTOFF`／`FIFTH_MARKER`／`SIXTH_MARKER`／`PERSCAN_MARKER`）判斷邏輯與對應自測（㊸～㊹-b）皆可觸發且非重疊covered，`design/evidence/` 下现有收據（development 43 份等）落在對應歷史 cutoff 之前，走的正是「舊收據放行」分支——這些分支目前仍會被實際歷史收據命中，非死碼。
- **`document_count` vs `document_containers` 混用**：兩者是不同語意的獨立欄位（`document_count`＝該支在整份快照的 flagged／命中筆數，六支通用；`document_containers`＝corner_anchor 專屬的容器數），程式碼、gate 驗證、agent 文件三方對這兩個名詞的用法逐一核對皆一致，未發現誤用或殘留引用舊名詞的註解。
- **自測 fixture 殘檔**：`overflow-scan.test.js`／`design-evidence-check.test.sh`／`design-notes-check.test.sh` 內新增的合成節點（`C()` helper、`MA`/`MAPRINT`/`MATL`/`MABR` 等）與案例編號（㊷～㊺-d）皆有對應斷言引用，跑 `node scripts/design/overflow-scan.test.js`（39 組全過）、`bash scripts/gates/design-evidence-check.test.sh`、`bash scripts/gates/design-notes-check.test.sh`（皆全過）三支自測，未見孤兒 fixture。
- **COLLABORATION／agent 定義句與程式碼一致性**：`ui-designer.md`／`visual-reviewer.md`／`COLLABORATION.md` §7 對角托 ref 判準、per-scan `scope`/`document_count`、NBSP gate 的描述經逐句核對，與 `overflow-scan.js`／`design-evidence-check.sh`／`design_notes_check.py` 實際行為相符（含 cutoff 語意「LS-202 起…必填」的措辭與程式碼的 `if perscan:` 守門一致）。
- **worktree 狀態**：`.claude/worktrees/LS-202` 為 clean（`nothing to commit, working tree clean`），無殘留待清。

### 既有死碼觀察

無新發現；已知池項（`1e7a602f`／`cc078cb9`／`56de90a6`）依指示不重複列。

### 建議處置

- `.gitignore:51` 重複行：併入下一張動到 `.gitignore` 的 harness 票順手刪，不需獨立開票。
- 其餘：未發現需修正項。

### 搜尋方法與盲區聲明

- 方法：`git diff 48d8b43...4832eff` 逐檔比對＋`git log -S`/`git blame`-style 溯源判斷欄位／規則引入時點；`grep -n` 交叉比對函式定義與呼叫點、agent 文件與 gate 程式碼的欄位名一致性；實跑三支自測套件（`node overflow-scan.test.js`、`bash design-evidence-check.test.sh`、`bash design-notes-check.test.sh`）驗證無孤兒斷言／fixture、無回歸。
- 盲區（明說）：純文字搜尋抓不到 Pencil MCP 執行期的動態行為（例如 `cornerWarnings` 重複輸出是否在真實 Pencil console 呈現上造成困擾，只能看 script 邏輯推論）；未實際對 `origin/development` 現稿跑 Pencil execute 重新驗證六支數字（該驗收項屬 ticket 驗收清單，非本次死碼巡檢範圍，且不動任何檔案/環境）；`.gitignore` 之外的 CI workflow 檔（`.github/workflows/*`）未列在 orchestrator 指定範圍內，未檢查是否有對應 job 步驟殘留。

### 2026-09-05T15:15:11.833Z · Chih-Lin Yeh · `088d78ea`

## dead-code sweep — LS-202

**範圍**：`git diff 48d8b43...4832eff` 限定於 orchestrator 指定的 harness 檔案（`scripts/design/overflow-scan.js`＋`.test.js`、`scripts/gates/design-evidence-check.sh`＋`.test.sh`、`design-notes-check.sh`／`design_notes_check.py`＋`.test.sh`、`design_tree_hash.py`、`.gitignore`、`.claude/agents/ui-designer.md`／`visual-reviewer.md`、`docs/COLLABORATION.md`）。同一 diff range 內混入的 LS-200（`album_summaries` view／API.md）不在本次範圍，未觸碰。

### Findings（本 feature 引入）

**1 項，informational，建議併入下一張相關 harness 票順手清（不影響功能，非阻斷）：**

- `.gitignore:51` `design-canvas*/__pycache__/` 現已完全是 `.gitignore:28` 新增的全域規則 `__pycache__/`（LS-202 commit `78d38e1`）的子集，成為多餘（但無害）的重複條目。
  - 證據：`git log -1 --format=%H -S"design-canvas*/__pycache__/" -- .gitignore` → `27811d0`（LS-51，早於本票），`git log --oneline -- .gitignore` 確認 `78d38e1`（本票）才加入通用 `__pycache__/`。`.gitignore` 對重複／被涵蓋的規則不會報錯，純粹是可讀性負債，不是功能性死碼。
  - 建議處置：下次動 `.gitignore` 的票順手刪這一行；不值得為此單獨開票。

### 未發現的死分支類型（已依 orchestrator 指定重點逐一排查，結論：無）

- **R1→R3 迭代死分支**：`cornerWarnings()`（`overflow-scan.js:625-636`）在 R2 引入、R3 補上 `scope==="boards"` 分流，兩個分支都有對應自測命中，且在 `scanAll`／`compactLines` 兩處呼叫點都能走到，非死碼。`cornerWarnings` 被呼叫兩次（SUMMARY 後一次、`compactLines` 內嵌一次）造成同一組警示行重複輸出——這是自 R2 起即有的既有設計，不是本輪新增的遺留分支，僅記錄供 orchestrator 判斷是否值得順手精簡。
- **名稱備援退化路徑**：`isCorner()`（`overflow-scan.js:322`）用 `(ref 判準) || (名稱判準)` 的 OR 組合，兩側都有獨立自測命中，是刻意的雙軌容錯，非死碼。
- **gate 內 legacy cutoff 分支是否仍有收據會走到**：`design-evidence-check.sh` 的三層 cutoff（`LEGACY_CUTOFF`／`FIFTH_MARKER`／`SIXTH_MARKER`／`PERSCAN_MARKER`）與對應自測（㊸～㊹-b）皆可觸發，development 現存歷史收據正落在對應 cutoff 之前、會走「舊收據放行」分支，非死碼。
- **`document_count` vs `document_containers` 混用**：兩者是不同語意的獨立欄位，程式碼、gate、agent 文件三方用法逐一核對一致，未發現誤用或殘留舊名詞。
- **自測 fixture 殘檔**：三支自測（`node scripts/design/overflow-scan.test.js` 39 組、`bash scripts/gates/design-evidence-check.test.sh`、`bash scripts/gates/design-notes-check.test.sh`）全過，未見孤兒 fixture／斷言。
- **COLLABORATION／agent 定義句與程式碼一致性**：逐句核對，措辭（含 cutoff「LS-202 起…必填」）與程式碼 `if perscan:` 守門一致。
- **worktree 狀態**：`.claude/worktrees/LS-202` clean，無殘留待清。

### 既有死碼觀察

無新發現；已知池項（`1e7a602f`／`cc078cb9`／`56de90a6`）依指示不重複列。

### 建議處置

`.gitignore:51` 重複行併入下一張動到 `.gitignore` 的 harness 票順手刪；其餘未發現需修正項。

### 搜尋方法與盲區聲明

方法：`git diff` 逐檔比對＋`git log -S` 溯源欄位引入時點＋`grep -n` 交叉比對函式定義／呼叫點與文件一致性＋實跑三支自測套件驗證無回歸。盲區：純文字搜尋抓不到 Pencil MCP 執行期動態行為；未實際對 `origin/development` 現稿跑 Pencil execute 重新驗證六支數字（屬 ticket 驗收項而非死碼巡檢範圍）；`.github/workflows/*` 不在 orchestrator 指定範圍內，未檢查。

### 2026-09-05T15:26:33.210Z · Chih-Lin Yeh · `39affae4`

**收尾儀式（09-05 23:16）**
- **dead-code-sweeper** `088d78ea`：無死碼；1 info（`.gitignore:51` `design-canvas*/__pycache__/` 已被全域 `__pycache__/` 涵蓋）→ 入池，下次動 `.gitignore` 順手刪。
- **Lesson learning review**：(1) 角托 ref 判準在 Pencil `resolveInstances:true` 快照下貢獻 0，全靠名稱備援（VR 實測）→ 入池 High `1e7a602f`，方法要換（`resolveInstances:false` 額外快照取 ref 對照表）；(2) gate 能被「省略收據鍵」繞過，靠 R3 必填檢查補——新 gate 一律加「缺鍵即紅」自測；(3) `pr-body-check` 反查 Linear 是 CI 單點，本票 rules 因此紅一次 → 入池 `7902dc20` 加重試；(4) 三輪 review 各抓到不同層（名稱備援→歸零判準→必填鍵），增量審有效。
- **清理**：`cleanup-merged.sh --apply LS-202` → worktree 已移除。
- Pen 清場：不適用（harness 票）。
→ Done（v0.15.0）。

