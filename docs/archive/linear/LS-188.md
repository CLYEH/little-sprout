# LS-188 Task：LS-152 iOS——設定頁 root（個人／家庭／內容與安全／法律／帳號五區）＋儲存空間頁（09／09b 已滿）依 LS-152 稿實作，含深色、AX3、iPad；家庭區塊列文字垂直置中（使用者 0905 意見）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:M, lane:ui |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M6 合規與帳號 |
| 估點 | — |
| 建立 | 2026-09-05T03:49:53.728Z |
| 開始 | 2026-09-05T11:50:26.455Z |
| 完成 | 2026-09-05T20:10:13.905Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-188/taskls-152-ios-設定頁-root個人家庭內容與安全法律帳號五區儲存空間頁0909b-已滿依-ls-152 |
| 父票 | LS-152 設計：設定與成員管理畫面群——刪除帳號流程（LS-24）、封鎖／檢舉入口（LS-23）、顯示名稱與頭像、退出家庭、Owner 移除成員、刪除單筆內容操作表 |
| 關係 | related→LS-189, related→LS-107, related→LS-167, related→LS-17, related→LS-164, related→LS-201, blocks→LS-193, blocks→LS-192, related→LS-24, related→LS-133, related→LS-177, related→LS-23, related→LS-142, related←LS-229, related←LS-208, related←LS-211, related←LS-210, related←LS-207, related←LS-209, related←LS-96, related←LS-194, related←LS-191 |

## 描述

## 來源

[LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表) 設計稿使用者 2026-09-05 核可（唯一意見：01 設定頁家庭區塊的列文字要在列高內垂直置中——**本票直接以此為實作要求**，.pen 稿另由設計 chore 補正）。VR R4 APPROVE `879079b1`、merge-review R2、已併 development；核可頁 `8cd04d46`。五張實作票之一（root）；其餘四張：顯示名稱／成員管理／退出家庭、刪除帳號流程（[LS-24](https://linear.app/little-sprout-app/issue/LS-24/storyapp-內帳號刪除多-owner-轉移)）、內容操作表＋檢舉封鎖（[LS-23](https://linear.app/little-sprout-app/issue/LS-23/story檢舉封鎖額度-uieula)）、EULA＋刪除單筆內容。

Design: `LS-152 / 01 設定`（`t5wI4`）、`01 設定 · 深色`（`Rx5mP`）、`A11y / 01 設定 · Dynamic Type AX3`（`z9tytH`）、`01-iPad 設定 (iPad 11")`（`B2DckT`）、`09 儲存空間`（`H5K5xm`）、`09b 儲存空間 · 已滿`（`ZG2Un`）；畫面級屬性、SF Symbol 對照、錯誤文案鍵以 [LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表) Handoff Notes 板為準。

## 範圍

1. 設定 tab root（取代現況只有「邀請家人」「登出」的頁面）：五區——個人（顯示名稱、頭像列 → 02 頁，另票）、家庭（成員清單摘要＋角色、邀請家人、退出家庭、Owner：管理成員 → 03 頁，另票）、內容與安全（封鎖名單、檢舉收件匣（Owner 限定）、儲存空間 → 09）、法律（《使用條款》《隱私權政策》入口——[LS-133](https://linear.app/little-sprout-app/issue/LS-133/設計法務文件-in-app-檢視畫面歡迎頁使用條款隱私權政策改開-app-內視窗不跳網站) in-app sheet 核可前先沿用既有開啟方式，留 TODO 指向 [LS-133](https://linear.app/little-sprout-app/issue/LS-133/設計法務文件-in-app-檢視畫面歡迎頁使用條款隱私權政策改開-app-內視窗不跳網站)）、帳號（刪除帳號 → 04 流程，另票；登出）。**每列文字在列高內垂直置中**（使用者意見，含多行副標的列以整組垂直置中）。
2. 儲存空間頁 09／09b：`get_family_quota` 用量與上限、相紙卷（Print Roll）視覺、已滿態文案與 LS002 一致（與 [LS-142](https://linear.app/little-sprout-app/issue/LS-142/設計相簿頁相簿詳情上傳佇列ls-20-畫面群-ui-designervisual-reviewer-3-輪) 上傳佇列同語彙）。
3. 各區入口在對應票未併入前指向佔位（`ContentUnavailableView` 顯示功能名），檔名依 Notes 板約定（`ProfileEditView`／`FamilyMembersView`／`DeleteAccountFlowView`／`BlockListView`／`ReportInboxView`），讓後續票只替換檔案內容不改 root。
4. 變體：深色（`$print-paper` 不變黑）、AX3（40pt 不破版）、iPad。
5. 測試：XCTest（區塊組成依角色：Owner／成員；儲存空間格式化與已滿判定）、UITests（root 五區存在、儲存空間頁可達）、tap-target gate；模擬器截圖對稿（亮／深／AX3／iPad／09／09b）。

## 不做

其餘四張票的內頁；[LS-133](https://linear.app/little-sprout-app/issue/LS-133/設計法務文件-in-app-檢視畫面歡迎頁使用條款隱私權政策改開-app-內視窗不跳網站) 法務 sheet；推播設定（[LS-177](https://linear.app/little-sprout-app/issue/LS-177/設計留言愛心互動列與留言-sheetls-22-畫面群推播通知權限提示與設定頁推播通知列-ui-designervisual)）。

## 驗收

- [ ] PR body `Design:` 列上述板名；QA 截圖比對（含列文字垂直置中量測）
- [ ] 單元測試＋UITests 綠、push gate 綠
- [ ] merge-reviewer（效能：root 不在主執行緒算配額；scope：佔位檔約定）、QA PASS

## 留言（22）

### 2026-09-05T11:50:38.461Z · Chih-Lin Yeh · `f4ce8062`

**Orchestrator（09-05 19:50）**：ui lane 補位（LS-164 進 QA）。worktree `.claude/worktrees/LS-188`（`feature/LS-188-settings-root` 自 development `6d9e01e`）已建，ios-dev 已派。派工附加要求：Pen 被 LS-201 佔用、讀稿走 `pen-read.sh` 離線；CI 模擬器 iOS 26.2 sheet 內 tap target 0.96 縮放 → sheet 內可點列 `minHeight: 48`；列文字垂直置中要有 UITest 量測（中線差 ≤1pt）；佔位檔名依 Notes 板約定。

### 2026-09-05T12:57:19.457Z · Chih-Lin Yeh · `a323b083`

## Handoff（2026-09-05 20:56）

**Ticket**：LS-188——設定頁 root（個人／家庭／內容與安全／法律／帳號五區）＋儲存空間頁 09／09b，依 LS-152 稿實作。

**已完成**：
- `SettingsView` 五區重寫（取代 LS-17 占位）：compact 單欄清單／regular（iPad）`NavigationSplitView` 兩種版面；家庭區塊列文字垂直置中（使用者 2026-09-05 對 LS-152 稿核可的唯一意見，直接落地）。
- 五個占位畫面（`ProfileEditView`／`FamilyMembersView`／`DeleteAccountFlowView`／`BlockListView`／`ReportInboxView`）接通後續票（LS-24／LS-23）入口；法律入口沿用既有網址，留待辦指向 LS-133。
- `StorageUsageView`：09／09b 儲存空間頁，新增 `FamilyAPIClient.fetchQuota` 接既有 `get_family_quota` RPC，同檔依 `isFull` 切換兩態。
- 「檢舉紀錄」列僅 Owner 可見，組成邏輯抽成純函式供測試覆蓋；`SettingsView` 補 `.task` 主動查角色，不再依賴使用者先去過「寶貝」分頁。
- tap-target gate 補 member 角色變體；`tap-target-exemptions.txt` 具名排除新占位／純顯示畫面。

**已驗證（怎麼驗）**：
- 單元測試：`FamilyQuotaTests`／`StorageUsageFormattingTests`（用量算術與 09／09b 示意值）、`SettingsContentSafetyCompositionTests`（角色組成）、`SettingsIconsTests`（SF Symbol 存在性）——連同既有 627 個單元測試一起跑，0 failures。
- UITests：`SettingsViewTests`（五區存在、owner／member 角色差異、儲存空間頁可達、垂直置中量測——邀請家人單行列與個人多行副標列，群組中線與列高中線差 <0.0001pt，斷言容差 1pt）；`TapTargetGateTests.testSettingsView`／`SectionTabBarTests` 回歸通過。
- push gate（lint＋unit tests＋tap-target）綠。
- 模擬器實機操作：`LS-188-iPhone17Pro`（iOS 26.0）＋新建 `LS-188-iPadAir11M3`（iOS 26.5），截圖比對亮／深／AX3／iPad／09／09b 六種變體，皆與稿面一致（紙不因深色變黑、AX3 不破版、iPad 呈現五區 sidebar＋detail）。全程用 `TapTargetGateHarness` 假 client，**未持有 supabase-lock**（未觸碰本機 Supabase 容器，本票只用既有 `get_family_quota` RPC 對正式站的既有部署，不改後端）。

**未完成／已知限制**（本票第一輪提交，尚無 reviewer informational 待處置）：
1. 「個人」列顯示名稱先用 email 本地部分推導（`EmailDisplayName.derive`），未讀 `profiles.display_name`／頭像／加入日期——留給 LS-152 02（顯示名稱編輯）頁，本票環境段限定不新增除 `get_family_quota` 外的後端呼叫。
2. 「家庭成員」「檢舉紀錄」列的摘要數字（「N 位」「N 則」）稿面有畫但本票未接（需要新的計數查詢）——分別歸屬 LS-24／LS-23。
3. iPad 版面：`SettingsView` 在 regular 寬度自開一層五區 split view，疊在 app 層既有四分頁 `SectionSplitView` 之上，實際可用寬度比稿面單獨畫的 834pt 版心窄——稿面與既有導覽架構的既知落差，未動 `RootView`（超出本票範圍）。
4. `SettingsIconsTests` 只驗符號存在於當前 runtime，未比照 `AppSectionTests` 加 deployment target 版本表比對。

**風險**：改動面涉及多個共用檔案（`FamilyStore`／`ChildrenStore`／`TapTargetGateHarness`），已跑滿既有測試套件確認無回歸；未新增／修改任何 migration。

**產出位置**：
- PR：https://github.com/CLYEH/little-sprout/pull/320（head `db11a2102a8a3b2eefe3a99d7df869060eea520f`，base `development`）
- 主要新檔：`LittleSprout/Features/Settings/`（`SettingsRowView`／`SettingsView+Profile`／`SettingsModels`／`StorageUsageView`／五個占位畫面）
- 模擬器已關：`EEFA8968-4492-44DF-AA47-31FB0344D15A`、`E4284D42-B769-49F8-923A-AF1E366FE30D`

### 2026-09-05T12:59:19.138Z · Chih-Lin Yeh · `f66c7375`

**Orchestrator（09-05 20:59）**：handoff 收到（PR #320 head `db11a210`）。已派 merge-reviewer R1，重點：fork 事故回退後 `FamilyModels`／`FamilyAPIClient`／`SupabaseFamilyAPIClient` 三檔無殘留、占位檔名依 Notes 板約定（後續四票只替換內容）、列文字垂直置中由 reviewer 獨立量測、mutation 兩支、配額不在主執行緒、六板對稿。handoff 三項「已知限制」交 reviewer 對票文範圍 3 判定。

### 2026-09-05T13:28:38.602Z · Chih-Lin Yeh · `5d0c022b`

## merge-review R1（2026-09-05 21:26）：REQUEST_CHANGES

審的 head `db11a2102a8a3b2eefe3a99d7df869060eea520f`（＝`gh pr view 320` 當下 headRefOid，未前進）；base `git merge-base origin/development db11a210` ＝ `6d9e01e`。27 檔 +1312／−53 全數逐檔看過。實跑：unit 16 綠／UITest 6 綠（iPhone 17 Pro），mutation 兩支，iPad（LS-188-iPadAir11M3）互動實測，AX3／深色／09 頁截圖，離線讀 `design/littlesprout.pen` 六板對稿。未持有 supabase-lock（本票只用假 client，未觸碰本機容器）。

---

### B1（blocker）iPad regular 寬度：五區所有入口都按不動，儲存空間頁在 iPad 上不可達

`LittleSprout/Features/SettingsView.swift:113-139`（`regularBody` 內層 `NavigationSplitView`）

**實測**（head，`LS-188-iPadAir11M3`／iPad Air 11" M3，`TapTargetGateHarness` `.settings`，與 `RootView` regular 路徑同樣是「`NavigationSplitView` 包在 `NavigationStack` 裡」）：

- 點「個人」列（`qa.settings.profileRow`）→ 畫面完全沒變：navBars 仍是 `個人|設定`、沒有返回鈕、`ContentUnavailableView` 的「顯示名稱與頭像」不存在。
- 側邊欄切到「內容與安全」→ 列都在（`儲存空間`／`檢舉紀錄` exists=true）→ 點「儲存空間」→ navBars 仍是 `內容與安全|設定`，`照片與影片會佔用空間，日記文字不會。` 不存在＝**沒有 push**。
- 切到「帳號」→ 點「刪除帳號」→ navBars 仍是 `帳號|設定`＝沒有 push。

**對照 base**（`6d9e01e`，同一台 iPad、同一個 harness case）：點「邀請家人」→ navBars 出現 `_TtGC7SwiftUI32NavigationStackHosting`、返回鈕 label `返回`＝**push 正常**。也就是說 LS-107 之後 iPad 上原本可用的「邀請家人」入口，被本 PR 新增的內層 split view 打死了（regression），且新增的七個入口在 iPad 上全數無效。

**失敗情境**：iPad（或 iPhone 橫向 Max）使用者開「設定」分頁 → 任何一列都點不進去；票文範圍 2「儲存空間頁」在 iPad 上不可達、範圍 3「各區入口指向佔位」在 iPad 上不成立、驗收「UITests 儲存空間頁可達」只在 compact 成立。handoff／PR body 只申報「iPad 可用寬度比稿窄」，沒有申報入口全失效——這不在票文允許的已知限制內。

**建議修法**（擇一，都不需要動 `RootView`）：
1. compact／regular 共用同一個單欄清單版面（regular 只調版心寬度／欄距），把稿面 `B2DckT` 的兩欄與既有四分頁 sidebar 的疊層問題留給另票；或
2. 內層改用 `NavigationSplitView` 之外的手段還原稿面（例如 detail 區自帶 `NavigationStack`，或以 `HStack` + 自繪 sidebar 呈現，push 仍走外層 stack）。

修完請在 iPad 上實測「每一區至少一個入口能 push、能返回」，不要只截靜態圖——本次就是靜態截圖看起來全對、互動全壞。

---

### M1（major, PLAUSIBLE 時序）`FamilyStore.refreshQuota()` 少了本檔自己訂的「await 前後核對」防線

`LittleSprout/Services/Family/FamilyStore.swift:341-352`

同檔 `refreshLatestInvite`（:308-333）明文寫了三道防線，第 2 道是 `isResultStillRelevant(familyID:)`（:358-360）：「await 期間若 `syncOwner` 把 store 換成別的使用者／家庭，查到的結果已經過期，直接丟棄、不覆寫」。`refreshQuota` 只有前置 in-flight guard，await 之後直接寫 `quota`／`quotaState`。

**失敗情境**：使用者在 09 頁停留 → `fetchQuota` 在飛 → 返回並登出（`SettingsView.signOut` 呼叫 `familyStore.reset()`，:181-190 已把 `quota = nil`／`quotaState = .idle`）→ 舊請求回來把**前一位使用者家庭的用量／上限**寫回 store 並標 `.success`。下一位使用者若還沒有家庭（`refreshQuota` 的 `myFamily == nil` 會直接 return、不覆寫），09 頁就顯示上一位使用者的用量數字。標 PLAUSIBLE：需要「reset 落在 RPC 往返之間」的窗口，我沒有實際造出這個時序（假 client 立即回傳，造不出窗口）。

**建議**：`quota = try await …` 之後套用既有 `isResultStillRelevant(familyID:)`（或至少 `myFamily?.id == familyID`），過期就把 `quotaState` 歸零回 `.idle`、不覆寫——與 `refreshLatestInvite` 一致，成本三行。

---

### m1（minor）「儲存空間」列缺稿面的用量摘要，且未列入已知限制

稿面 `t5wI4` → `Row · 儲存空間`（`AlJ6V`）覆寫 `n1hzsr.content = "2.1／5 GB"`；`家庭成員`＝「4 位」、`檢舉紀錄`＝「2 則」。實作三列都沒有帶 `value:`（`SettingsView.swift:259` 等），`SettingsRowView.value`（`SettingsRowView.swift:19`）目前**沒有任何呼叫端**。

handoff／PR body 只申報「家庭成員」「檢舉紀錄」兩個數字未接（需要新的計數查詢，歸 LS-24／LS-23，合理）——但「2.1／5 GB」不需要新查詢，`FamilyStore.quota` 本票已經有了。要嘛接上（root `.task` 呼叫 `refreshQuota()`，`quota` 有值才帶 `value:`），要嘛在 handoff 明講「root 不為了一列摘要多打一次 RPC，稿面此值略過」並交 QA 對稿時知情。現在是靜默落差。

---

### m2（minor）垂直置中的 UITest 對「拿掉置中」不敏感——使用者意見針對的那一種列沒被守住

`LittleSproutUITests/SettingsViewTests.swift:61-76`（單行列）與 `:97-107`（容差 1.0pt）

我做的 mutation（把 `SettingsRowView.swift:31` 與 `SettingsView+Profile.swift:81` 的 `HStack` 改成 `alignment: .top`）實測：

| 測試 | baseline 中線差 | mutation 後中線差 | mutation 後結果 |
|---|---|---|---|
| `testProfileRowNameAndSubtitleGroup…`（`ProfileSummaryRow`） | 5.7e-14 pt | −6.67 pt | **紅**（有效） |
| `testInviteRowLabel…`（`SettingsRowView`） | 0.0 pt | −0.83 pt | **仍綠**（1pt 容差吃掉） |

使用者 0905 的意見是「**家庭區塊**的列文字要垂直置中」，家庭區塊的列正是 `SettingsRowView`——守它的那一條在置中被拿掉時不會紅。單行列的最大偏移量（icon 22pt vs 文字 ~20.3pt）本來就 <1pt，容差選 1pt 等於這條測試永遠綠。建議容差收到 ≤0.5pt（我量到的 baseline 是 0.0pt，收緊沒有 flake 風險），或改斷言「文字組上緣間距＝下緣間距」。

（產品行為本身正確：head 的實測中線差 邀請家人 0.0pt、個人列 5.7e-14pt，使用者的要求有落地。這條是測試強度問題，不是畫面問題。）

---

### m3（minor）`tap-target-exemptions.txt` 對 `StorageUsageView` 的具名理由與程式碼不符

`scripts/gates/tap-target-exemptions.txt:27` 寫「無互動元件（…沒有按鈕；系統返回鈕不算）」，但 `StorageUsageView.swift:141-152` 的失敗態有 `Button("重試")`，而且沒有像其他列那樣加 `.frame(minHeight: 44)`——SwiftUI 純文字 button 實測約 20pt 高（本 repo 的 LS-17 QA1 就是這個型：登出鈕 34×20.3pt）。我沒有實跑量它（假 client 造不出失敗態，標 PLAUSIBLE），但「理由寫成沒有按鈕」會讓這顆鈕永遠不被 gate 看到，違反 repo 的「規則＋機械 gate」原則。建議：給重試鈕 `minHeight: 44`，並把豁免理由改成與程式碼相符的敘述（或改成可 seed 失敗態後納入 gate）。

---

### 對稿與其他維度（無 finding 的部分也列出來，方便 QA 接手）

- **五區順序與文案**：`t5wI4` 個人／家庭／內容與安全／法律／帳號、各區列名與順序，實作逐一相符。
- **SF Symbol 對照**：users→`person.2.fill`、user-round-plus→`person.badge.plus`、door-open→`door.left.hand.open`、user-x→`person.fill.xmark`、flag→`flag.fill`、hard-drive→`internaldrive`、file-text→`doc.text`、shield→`shield`、log-out→`rectangle.portrait.and.arrow.right`、trash-2→`trash`、chevron-right、iPad sidebar 五顆（`B2DckT`）——全數對得上，`SettingsIconsTests` 也守住存在性。
- **列度量**：`cmp/Settings Row`（`bZhuV`）padding `$ctl-pad-tap`／`$inset-card`、icon 22、chevron 18、label `$fs-body`/600、value `$fs-note`——與 `SettingsRowView` 逐值相符。
- **09／09b 文案**：實跑截圖「2.1 GB 已使用」「／ 5 GB」「用了 42%，還有 2.9 GB 可以用。」「照片與影片會佔用空間，日記文字不會。」與 `H5K5xm` 逐字相符；相紙卷 12 格填 5 格、`$photo-corner`／`$print-paper` 配色相符。已滿態文案與 `ZG2Un` 相符。「與 LS-167 LS002 同語彙」無法機械比對：目前 app 內沒有任何 LS002 的使用者文案可對照（`AppError` 只有錯誤碼，沒有這句），僅能確認與稿面一致。
- **深色**：`print-paper` colorset 深色 `#E8D9D4`（既有 token，本 PR 未動），實跑深色截圖確認相紙不變黑。
- **AX3**（`simctl ui content_size accessibility-extra-large` 實跑）：五區不破版、不裁切，列高隨字級長大。兩點提給 QA（非 finding）：①「個人」列副標換行後最後一行呈置中而非靠左，與其他列的視覺節奏不同；② `ProfilePrintChip` 是硬寫 60/45/16.4pt、不隨 Dynamic Type 放大（與既有 `AvatarPrintCard` 的硬寫慣例一致，故不列 finding），AX3 下頭像框相對文字明顯偏小。
- **效能**：`refreshQuota()` 走 `.task`，view body 內只有 O(1) 算術（`gbString`／`filledChipCount`／12 格 `ForEach`），沒有主執行緒重運算、沒有 per-row 重算、沒有 N+1。`get_family_quota` 是 `stable` SQL、走 `families` 的 RLS（非 security definer），`.single()` 對別家 id 拿 0 列會 throw——client 註解與 migration §7 相符。
- **race（其餘）**：`FamilyStore`／`ChildrenStore` 都是 `@MainActor @Observable`；`refreshQuota` 有 in-flight guard；`SettingsView.task(id:)` 只在 `myRole == nil` 補查、與 `ChildrenManagementView` 併發時被 `listState` guard 擋掉。informational：這條補查為了一個列的可見度會連帶 `listChildren` ＋批次頭像簽名（`ChildrenStore.refresh`），冷啟動一次、成本有限，但比「只查角色」重。
- **scope**：三個 fork 事故檔（`FamilyModels`／`FamilyAPIClient`／`SupabaseFamilyAPIClient`）逐行核對，只有 `FamilyQuota`＋`fetchQuota` 相關新增，**沒有** `Profile`／`fetchMyProfile`／`fetchMemberCount` 之類殘留、沒有重複定義。占位五檔檔名與 Notes 板約定一致，內容都是最小 `ContentUnavailableView`（後續票只換內容不動 root 的前提成立）。`ChildrenStore.seedRoleForPreview` DEBUG-only、最小。無無關重構。
- **測試 mutation 第二支**：把 `SettingsContentSafetyComposition.rows(isOwner:)` 反轉 → `SettingsContentSafetyCompositionTests` 2 條＋`SettingsViewTests` owner／member 2 條共 4 條紅，符合預期。
- **CI**：`ci`／`db`／`lint`／`rules` 皆 pass（我不以此代替本地實測）。
- **沒看的範圍**：真實 app（登入後的 `RootView`）在 iPad 上的行為只以 harness 推論（同樣的巢狀結構），未用真帳號跑；`StorageUsageView` 失敗態、`myFamily == nil` 時停在「正在讀取用量…」的分支未實跑（後者目前路徑理論上不可達）。

---

**Verdict：REQUEST_CHANGES**（B1 blocker、M1 major）。B1 修完請附 iPad 互動實測（每區至少一個入口 push＋返回）；M1／m1／m2／m3 請逐條處置並在 handoff「未完成」列出。

模擬器：`LS-188-iPhone17Pro`、`LS-188-iPadAir11M3` 用畢已 shutdown，content_size 已復原 medium；review worktree 已移除。

### 2026-09-05T13:30:42.347Z · Chih-Lin Yeh · `30f8fec8`

**Orchestrator 裁定（09-05 21:30）**：merge-review R1 `5d0c022b` REQUEST_CHANGES。**B1** 採 reviewer 修法 2——保留已核可稿面 `B2DckT` 兩欄樣貌，但不用內層 `NavigationSplitView`（`HStack`＋自繪 sidebar，push 走外層 stack 或 detail 自帶 stack），必附 iPad 互動 UITest（每區一入口 push＋返回；base「邀請家人」回歸）；不得自行改單欄（偏離核可稿需另裁）。M1 套 `isResultStillRelevant`＋測試；m1 儲存列接用量摘要；m2 容差 ≤0.5pt 或改上下緣相等斷言並跑 reviewer mutation；m3 重試鈕 44pt＋豁免理由訂正。reviewer 給 QA 的兩點交 QA 對稿。已續派實作 R2。

### 2026-09-05T14:32:51.642Z · Chih-Lin Yeh · `d34ed244`

## R2 Handoff（2026-09-05 22:30）

**Ticket**：LS-188——回應 merge-review R1 `5d0c022b` REQUEST_CHANGES。

**已完成（逐條處置）**：
- **B1（blocker）已修 `ab5a8a8`**：iPad regular 寬度原本用內層 `NavigationSplitView` 疊在外層 `NavigationStack`，導致五區所有入口全部按不動、且把 base 上原本可用的「邀請家人」打壞。裁定採 reviewer 修法 2：拿掉內層 split view，改用 `HStack` 自繪兩欄——sidebar 是一排 `Button`，detail 欄自帶獨立 `NavigationStack`（`.id(regularSelection)` 切換區塊時重置）。新增 `SettingsViewIPadTests`（5 條，逐區 push＋返回，含「邀請家人」regression）＋`TapTargetGateTests.testSettingsViewRegular`。
- **M1（major）已修 `9e9ca7c`**：`FamilyStore.refreshQuota()` 補上與 `refreshLatestInvite` 一致的「await 前後核對」。新增 `FamilyStoreRefreshQuotaRaceTests`（2 條）。
- **m1（minor）已修 `ab5a8a8`**：「儲存空間」列接上稿面「2.1／5 GB」用量摘要。
- **m2（minor）已修 `ab5a8a8`**：垂直置中 UITest 容差 1.0pt→0.5pt；已跑 reviewer 指定的 mutation 確認兩條測試都轉紅、revert 後回綠。
- **m3（minor）已修 `ab5a8a8`**：`StorageUsageView` 失敗態「重試」鈕加 `minHeight: 44`；豁免理由訂正。
- reviewer 給 QA 的兩點不修，記入 PR body 交 QA 對稿知情。

**已驗證**：單元測試 R1 16 條＋R2 新增 4 條全數通過（`TimelineStoreTests` 一條既有 flake、單獨重跑必過，與本 PR 無關）；UITests R1 7 條＋R2 新增 7 條全數通過；iPhone＋iPad 模擬器實機驗證逐區 push／返回；push gate 綠；`gh pr checks 320` 的 `rules`／`db`／`lint` 綠（`ci` 未等完成）；PR body 已補「R2 已修」並過 `pr-body-check.sh --verify`。

**未完成**：iPad 兩欄疊在既有四分頁 sidebar 之上（既知落差，未動 RootView）；個人列顯示名稱用 email 推導（留 LS-152 02 頁）；家庭成員／檢舉紀錄摘要數字未接（歸屬 LS-24／LS-23）；reviewer QA 兩點不修交對稿；SettingsIconsTests 未加版本表比對。

**風險**：B1／m1／m2／m3 因改動同檔，合併成 commit `ab5a8a8`；M1 獨立為 `9e9ca7c`。

**產出位置**：PR https://github.com/CLYEH/little-sprout/pull/320（head `9e9ca7c01280c39350e4e7e0abdb62eadb7efdd8`）；模擬器已關 `EEFA8968-4492-44DF-AA47-31FB0344D15A`、`E4284D42-B769-49F8-923A-AF1E366FE30D`。

### 2026-09-05T14:39:58.200Z · Chih-Lin Yeh · `8d7bc413`

**Orchestrator（09-05 22:33）**：R2 handoff 收到（head `9e9ca7c`，CI 四項綠；B1 改 `HStack` 自繪兩欄＋5 條 iPad 互動 UITest、M1 核對＋race 測試、m1／m2／m3）。已續派 merge-reviewer R2：iPad 專屬機實跑逐區 push＋返回與邀請家人回歸、M1 mutation、m2 `alignment: .top` mutation 兩紅、儲存列摘要不重複打 RPC、scope。

### 2026-09-05T15:17:22.527Z · Chih-Lin Yeh · `cd70714d`

## merge-review R2（2026-09-05 23:12）：REQUEST_CHANGES

head `9e9ca7c01280c39350e4e7e0abdb62eadb7efdd8`（＝當下 headRefOid，未前進）；增量 `db11a210...9e9ca7c` 16 檔 +508／−75 全看過。CI 四項皆 pass（未遇 `TimelineStoreTests` flaky，未 rerun）。

### R1 五條：全部確認修好（各附 mutation）

- **B1（blocker）已解**：iPad（`LS-188-iPadAir11M3`）實跑 `SettingsViewIPadTests` **5/5 綠**；我自己再逐區點一次——個人列／家庭成員／退出家庭／封鎖名單／檢舉紀錄／儲存空間**六個入口全部 push 成功**（navBars 由區塊標題變成目的地、`BackButton` 出現），返回都回到原區清單。**mutation：還原內層 `NavigationSplitView` → 5 條全紅**（profile 那條精準紅在「點入口後應該已經 push」），新測試確實守得住。
- **M1（major）已解**：`FamilyStoreRefreshQuotaRaceTests` 2 條綠；**mutation：拿掉兩個 `myFamily?.id == familyID` guard → 2 條全紅**（reset 情境紅在「不該回傳前一位使用者的用量」、換家庭情境紅在「不該覆寫新家庭的 quota」）。
- **m1 已解**：iPhone `testStorageRowShowsUsageSummaryOnceQuotaLoads` 綠；iPad 上該列 accessibility label ＝`儲存空間、2.1／5 GB`，與稿面 `AlJ6V` `n1hzsr` 逐字相符。不重複打 RPC：root 有 `quota == nil` guard、`refreshQuota` 有 in-flight guard；09 頁每次進場刷新一次是它自己的行為（合理）。
- **m2 已解**：**mutation（`HStack` 改 `alignment: .top`）這次兩條都紅**（邀請家人 −0.667pt、個人 −7.33pt），R1 漏網的單行列已守住；head baseline 仍 0.0／~1e-14pt。
- **m3 已解**：重試鈕 `minHeight: 44`＋`contentShape`；豁免理由改成與程式碼相符。`TapTargetGateTests.testSettingsView`／`testSettingsViewRegular` 在 iPhone 皆綠。

---

### M2（major，R2 新引入）iPad sidebar 選中態在亮色模式看不出來

`LittleSprout/Features/Settings/SettingsView+Sidebar.swift:26`（`.background(Color.lsSurface)`）、`:32-48`（`sidebarRow` 的 `.background(isSelected ? Color.lsPrintPaper : Color.clear)`）

**量測**（iPad 截圖逐像素取樣，亮色；預設選「個人」與手選「內容與安全」都測）：五列（含選中列）背景**全部 RGB(251,235,236) ＝ #FBEBEC**，與 sidebar 底色完全相同——`surface` 與 `print-paper` 的**亮色值都是 #FBEBEC**（`Assets.xcassets/surface.colorset`／`print-paper.colorset`，與 `.pen` 變數一致）。選中／未選中唯一差別是 icon 色（#2B141C vs #553040）；label 兩者都量到 #2B141C；無邊框、無陰影。

**對稿**：`B2DckT` 選中 Nav Item（`KGKyg`）＝`fill: $print-paper` **＋ `stroke: $paper-edge` 1pt ＋外陰影（`$paper-shadow` y+2 blur 8）**，且稿面 `Sidebar`（`VogAw`）**本身沒有 fill**（坐在 `$bg` 漸層上），才讀得出「浮起的一張紙」。實作鋪了 `$surface` 又省略 stroke／shadow，三個辨識線索全失效。

**失敗情境**：iPad 使用者在預設亮色外觀下開設定頁，五區長得一模一樣，分不出目前在哪一區。深色沒事（選中 #E8D9D4 vs 底 #2E1C22，對比明顯）。R2 新引入：R1 的 `List(selection:)` 雖同樣撞色，但系統會畫選取樣式（R1 截圖有藍色選取框），改自繪後那層沒了。

**修法**（任一，都很小）：sidebar 不鋪 `Color.lsSurface`（坐在 `Color.lsBackground` 上，還原稿面）；或選中列補稿面本來就有的 `$paper-edge` 1pt 邊框＋陰影。請附一張**亮色** iPad 截圖，並考慮加一條可機械複驗的判準。

---

### informational（不擋合併，handoff 逐條處置）

1. **註解與實測行為不符**：`SettingsView.swift:116-146` 宣稱採「選項②：sidebar 永遠可見、push 只發生在右欄」，實測 **push 後 sidebar 消失、整個畫面被蓋掉**（截圖＋XCUITest tree：pushed 狀態只剩返回鈕，`家庭`／`帳號` 不存在）＝其實是註解列為不採用的選項①。連帶 `.id(regularSelection)` 要解的情境（push 時切區）目前不可能發生。功能無誤，但會誤導 LS-24／LS-23。
2. **iPad detail 欄標題三重**：外層 stack bar ＋內層 inline 標題＋區塊標題；harness 下外層 bar 還被排到畫面底部。真 app 外層是「設定」，請 QA 對稿看要不要拿掉內層 inline 標題。
3. **m2 偵測邊際薄**：容差 0.5pt vs mutation 偏移 0.667pt，只差 0.167pt（baseline 0.0pt 無 flake 風險）；icon token／字級一動可能又測不出來，可改斷言「上緣留白＝下緣留白」。
4. **root 用量摘要不會自更新**：只在 `quota == nil` 查一次；上傳後回設定頁數字要等再進 09 頁才變。
5. **`TapTargetGateTests.testSettingsView` 在 iPad 機型會紅**（sentinel `登出` 只在 compact／regular 的「帳號」區）。CI 跑 iPhone 不受影響，值得在該 case 註解一句。

### 其他維度

- **scope**：pbxproj 只新增 4 個新檔；兩支拆檔都有 `file_length` 先例；`StubFamilyAPIClient.fetchQuota` 改 handler 是 M1 測試必要。無夾帶、無無關重構。
- `swiftlint lint --strict` exit 0；`xcodegen generate` 零漂移（本機實跑）。
- **效能／race**：root `.task` 只在 `quota == nil` 打一次；兩個 `.task(id:)` 獨立；in-flight guard ＋ await 後核對兩層都在；自繪 sidebar 只有 5 個 Button。
- **AX3／深色（iPad）**：AX3 下 280pt sidebar 的「內容與安全」不截斷；深色選中態清楚。
- **沒看的範圍**：真實登入後 `RootView` 的 iPad 路徑仍以 harness 推論（同樣是 `NavigationStack` 包 `SettingsView`）；`StorageUsageView` 失敗態與新重試鈕實際尺寸未實跑（harness 造不出失敗態）。

---

**Verdict：REQUEST_CHANGES**（M2 major；R1 的 B1／M1／m1／m2／m3 全數修好並有 mutation 佐證）。

模擬器 `LS-188-iPhone17Pro`／`LS-188-iPadAir11M3` 已 shutdown（appearance／content_size 復原）；review worktree 已移除。

### 2026-09-05T17:13:46.823Z · Chih-Lin Yeh · `ff9be404`

## R3 Handoff（2026-09-06 01:13）

**Ticket**：LS-188——回應 merge-review R2 `cd70714d` REQUEST_CHANGES。

**已完成（逐條處置）**：
- **M2（major）已修 `0fd0f2d`**：iPad sidebar 選中列在亮色模式下跟未選中列長得一模一樣（`surface`／`print-paper` 亮色值都是 #FBEBEC，sidebar 又鋪了 `Color.lsSurface`）。離線讀 `.pen` `B2DckT` 選中 Nav Item 取精確值：sidebar 拿掉背景鋪色、選中列補 `$paper-edge` 邊框＋外陰影＋粗體文字，新增 `.accessibilityAddTraits(isSelected ? .isSelected : [])` 當機械訊號。新增 `testSidebarSelectionIsAccessibleAndDistinguishable`，已跑 mutation（拿掉 trait）確認轉紅、revert 後回綠。亮／深色截圖已存 scratchpad。
- **informational 1／2 已修**：`regularBody` 文件註解原本宣稱「sidebar 永遠可見、push 只發生在右欄」，實測（截圖）並非如此——push 後整個畫面被蓋掉，跟 compact 同一種行為。拿掉形同虛設的內層 `NavigationStack`／`.id(regularSelection)`，訂正文件註解為實測行為，同時解掉「iPad 標題三重」（內層 stack 自己的 nav bar 消失）。
- **informational 3 已修**：m2 垂直置中斷言改成「上緣留白＝下緣留白」，數學上把偵測訊號放大一倍（門檻不變 0.5pt，mutation 訊號從 0.667pt→1.33pt，偵測邊際從 0.167pt 拉開到 0.83pt）。
- **informational 4 不修＋理由**：root 用量摘要只在 `quota == nil` 查一次，跟既有欄位快取慣例一致；跨畫面自動更新需要事件／失效通知機制，超出本票（設定頁導覽殼）範圍。
- **informational 5 已修**：`TapTargetGateHarness.settingsHost`／`settingsMemberRoleHost` 補 `.environment(\.horizontalSizeClass, .compact)`，解決在 iPad 模擬器上重跑 `testSettingsView` 會紅的問題；已在 iPad 模擬器重跑確認轉綠。

**額外處置**：R3 commit 推送前 `origin/development` 已推進（LS-167／201／205／206 併入），與本分支在 `project.pbxproj`／`TapTargetGateHarness.swift` 產生合併衝突——已解衝突（pbxproj 用 xcodegen 重新產生；harness 檔保留兩邊新增內容，`settingsHost` 移進既有的 `TapTargetGateHarness+Settings.swift` 避免超過 file_length），合併後完整測試套件（645 單元測試＋26 個相關 UITest）全數通過。

**已驗證**：M2 與 m2 兩組 mutation 皆實跑確認轉紅／revert 後轉綠；iPhone＋iPad 模擬器逐區驗證＋亮／深色 sidebar 截圖比對；merge 後完整測試套件全綠；push gate 綠；PR checks `rules`／`db`／`lint` 綠（`ci` 由 coordinator 續看）；PR body 已補 R3 段並過 `pr-body-check.sh --verify`（exit 0）。

**未完成**（PR body 已列）：iPad 兩欄疊在既有四分頁 sidebar 之上，且 push 會蓋掉整個兩欄畫面（非稿面暗示的常駐 sidebar，informational 1/2 已訂正文件描述為實際行為，真正做到需要更大範圍的導覽層改動）；個人列顯示名稱用 email 推導；家庭成員／檢舉紀錄摘要數字未接；root 用量摘要不會跨畫面自動更新（informational 4）；reviewer QA 兩點不修交對稿；SettingsIconsTests 未加版本表比對。

**風險**：push 過程多次遇到 SSH 連線被 GitHub 中斷，加 keepalive 選項後成功，非程式碼問題。

**產出位置**：PR https://github.com/CLYEH/little-sprout/pull/320（head `ecf4c9afb88f1476969a6656e85309a91704630c`）；R3 commits `0fd0f2d`（M2＋informational）、`ecf4c9a`（merge 解衝突）；模擬器已關 `EEFA8968-4492-44DF-AA47-31FB0344D15A`、`E4284D42-B769-49F8-923A-AF1E366FE30D`。

### 2026-09-05T17:14:49.888Z · Chih-Lin Yeh · `9853319f`

**Orchestrator（09-06 01:14）**：R3 handoff 收到（head `ecf4c9a`；M2 sidebar 選中態＝去底色＋`$paper-edge` 邊框＋陰影＋粗體＋`.isSelected` trait、info 1／2／3／5 已修、info 4 不修＋理由；merge development 解衝突後 645 unit＋26 UI 綠）。已續派 merge-reviewer R3（iPad 亮色逐像素、trait mutation、iPad 五區 push 回歸、pbxproj／harness 解衝突無夾帶、`xcodegen` 零漂移）。APPROVE 後 merge → 隨下一次 promote 進 test → QA。

### 2026-09-05T17:38:06.958Z · Chih-Lin Yeh · `6e7ece86`

## merge-review R3（2026-09-06 01:36）：REQUEST_CHANGES

head `ecf4c9afb88f1476969a6656e85309a91704630c`（＝當下 headRefOid，未前進）；審 `9e9ca7c...ecf4c9a`（`0fd0f2d` M2＋info、`ecf4c9a` merge development）。CI `ci`／`db`／`lint`／`rules` 四項 pass。

### 修好的部分（實測）

- **M2 產品面已真的修好**（我自己逐像素量）：iPad 亮色下選中列＝`#FBEBEC` 圓角紙托＋`$paper-edge` 邊框＋可見外陰影＋粗體，未選中列為透明；截圖一眼可辨。深色未退步（選中 `#E8D9D4`／底 `#190E12`）。
- **info 1／2**：內層 `NavigationStack`／`.id(regularSelection)` 已移除，註解改成實測行為；harness 下的第三層標題（R2 掉到畫面底部那條）已消失。
- **info 3**：斷言改「上緣留白＝下緣留白」後，`alignment: .top` mutation 兩條都紅，且單行列訊號從 0.667pt 放大到 **1.33pt**（11.5 vs 12.83，門檻 0.5pt），偵測邊際確實拉開。
- **info 5**：`settingsHost` 補 `.environment(\.horizontalSizeClass, .compact)` 後，`TapTargetGateTests.testSettingsView` 在 **iPad 上實跑轉綠**（R2 時在 iPad 為紅）。
- **info 4 不修的理由**（跨畫面失效通知超出本票範圍）**接受**。
- **merge 解衝突乾淨**：`git diff 70d95f3...ecf4c9a` 只有 LS-188 自己的 32 檔，沒有夾帶或回捲 LS-167／201／205／206 的任何檔案；`hostView(for:)` switch 各票 case 齊全（含 `.uploadQueueSheet`／`.uploadQueueSheetNormal`）且編譯通過（Swift switch 窮盡性即證明）；`settingsHost` 搬進 `+Settings.swift` 時 info 5 的 `.compact` 有一起帶過去；pbxproj **無重複 file reference**，`xcodegen generate` **零漂移**，`swiftlint lint --strict` exit 0。
- 測試實跑：iPhone 20 unit ＋ 7 `SettingsViewTests` ＋ `testSettingsView` 全綠；iPad `SettingsViewIPadTests` 6/6 綠（整包連跑兩次）＋ `testSettingsViewRegular` 綠。

---

### M3（major）`testSidebarSelectionIsAccessibleAndDistinguishable` 擋不住它要擋的那個退化——handoff 的 mutation 佐證是誤判

`LittleSproutUITests/SettingsViewIPadTests.swift:122-140`、`LittleSprout/Features/Settings/SettingsView+Sidebar.swift:58-64`

我做了四組 mutation（全部實跑）：

| # | mutation | 結果 |
|---|---|---|
| a1 | **刪掉** `.accessibilityAddTraits(isSelected ? .isSelected : [])` 這一行 | 測試「紅」——但不是斷言失敗，是 **app crash**（`EXC_BAD_ACCESS` in `swift_retain` ← `ForEachState.item` ← `initializeWithCopy for Button`，crash report 已看）。刪掉尾端 modifier 改變了 `ForEach` 內 row 的泛型型別，撞到 SwiftUI 的崩潰邊角，**不是 trait 訊號被測到** |
| a2 | 型別不變地中性化：`.accessibilityAddTraits([])` | **仍綠** |
| a3 | 型別不變地反向：對**所有**列都 `.accessibilityAddTraits(.isSelected)` | **仍綠**；探針印出 `個人 isSelected=true／其餘 false`，點「家庭」後翻轉——證明 XCUITest 讀到的 `isSelected` **完全不受這行 modifier 影響**，訊號來自別處 |
| b | **把 M2 的視覺修法全部中性化**（背景／邊框／陰影／粗體都改成恆等於未選中，trait 原封不動）＝把 R2 的缺陷原樣放回去 | **仍綠** |

結論：這條測試**無法偵測 M2 那個退化**（mutation b 是決定性的），`.accessibilityAddTraits(...)` 對 XCUITest 而言是無效訊號（a2／a3）；handoff 與程式碼註解寫的「已跑 mutation（拿掉 trait）確認轉紅」其實是 a1 那個 **crash**，不是斷言鑑別力。這正好落在 repo 最高原則「新增重要規則＝同時新增它的 gate」的反面：M2 這類退化已經發生過一次（R1 `List` 系統選取樣式 → R2 自繪後隱形），現在留下的是一條看起來有守、實際上不會紅的測試。

（VoiceOver 是否因這行而念出「已選取」我沒有驗，a2／a3 只證明 XCUITest 的 `isSelected` 與它無關；若要保留這行請另附 VoiceOver 佐證。）

**建議修法**（repo 既有慣例，成本小）：把「選中要長什麼樣」抽成純資料／純函式（例如 `SettingsSidebarRowStyle.for(isSelected:)` 回傳背景／邊框／陰影／字重四個 token），`SettingsView+Sidebar` 只負責套用，再用 XCTest 斷言 `for(isSelected: true) != for(isSelected: false)` 且各欄位等於稿面 token——同 `SettingsContentSafetyComposition` 的既有作法，mutation b 會直接紅。UITest 那條可保留當補充。

---

### m1（minor，R3 新引入）sidebar 欄變成系統純白／純黑，與稿面和右欄背景都不一致

`LittleSprout/Features/Settings/SettingsView+Sidebar.swift:15-30`（拿掉 `.background(Color.lsSurface)` 後沒有補上 app 背景）

實測取樣：亮色 sidebar 欄 = `RGB(255,255,255)` 純白、右側 detail = `#EDD3D6`（`bg` 亮色）；深色 sidebar = `RGB(0,0,0)` 純黑、detail = `#190E12`。稿面 `B2DckT` 的 `Sidebar`（`VogAw`）雖然沒有 `fill`，但它是坐在板子的 `$bg` 上——實作拿掉底色之後露出的是系統預設背景，不是 `$bg`，於是整個 app 的暖粉色調旁邊多了一條純白（深色模式純黑）長條。建議 `.background(Color.lsBackground)`（或把背景鋪在 `regularBody` 的 `HStack` 上），選中列的紙托在 `$bg` 上一樣清楚（稿面就是這個組合）。

---

### informational

1. **iPad `testFamilySectionInviteRowRegressionPushesAndBackReturns` 首跑紅一次**（「返回後應該回到原本的清單」＋「返回後不該還看得到目的地畫面」），之後單獨 3 次、整包 2 次共 5 次全綠 → 判定為 flake，非產品缺陷；但它守的是 B1 blocker，建議穩定化（返回前先等 `產生邀請碼` 穩定、或改 `waitForNonExistence`），否則未來 CI 偶發紅會被當雜訊。
2. **標題仍重複兩層**（nav bar 「個人」＋區塊標題「個人」）——第三層已消掉。真 app 外層標題是「設定」，這兩層可能不重複，交 QA 對稿確認。
3. **陰影半徑與 repo 其他用法不同調**：本檔 `radius: 8`（＝直接套用稿面 blur 8），repo 既有 `$paper-shadow` 用法是 `radius 1.5／2／3／5`（慣例約 blur/2）。視覺上目前看起來沒問題，只是慣例不一致。
4. `.accessibilityAddTraits(isSelected ? .isSelected : [])` 若無 VoiceOver 佐證，實質是無效程式碼（見 M3 a2／a3）。

### 其他維度

- **scope**：增量只動 LS-188 自己的檔案＋merge 解衝突；無夾帶、無無關重構。
- **效能／race**：R3 只動版面與樣式，`refreshQuota` 兩層防線與 root `.task` 的 `quota == nil` guard 未變動；移除內層 `NavigationStack` 不影響任何非同步路徑。
- **沒看的範圍**：真實登入後 `RootView` 的 iPad 路徑仍以 harness 推論；`StorageUsageView` 失敗態未實跑；VoiceOver 未實測。

---

**Verdict：REQUEST_CHANGES**（M3 major、m1 minor）。M2 的**產品修法本身我已逐像素驗證是好的**——這一輪擋的是「守它的 gate 實際上不會紅」與「sidebar 背景變成系統白／黑」兩點；若 orchestrator 認為 gate 可另票補、白底可交 QA，請自行改判，我把證據攤在這裡。

模擬器：`LS-188-iPhone17Pro`、`LS-188-iPadAir11M3` 用畢已 shutdown，appearance／content_size 已復原（light／medium）；review worktree 已移除。過程中 iPad 模擬器出現一次 dyld 層 `SIGBUS` 啟動崩潰（`DyldSharedCache::getUUID`，環境問題非本 PR），重開模擬器後排除。

### 2026-09-05T17:39:46.956Z · Chih-Lin Yeh · `9bd1e1f5`

**Orchestrator 裁定（09-06 01:39）**：merge-review R3 `6e7ece86` REQUEST_CHANGES——M2 產品面已修好、info 1／2／3／5 已驗；新 M3：選中態測試在四組 mutation 下皆綠（handoff 的「轉紅」是 crash 誤判），測試無牙不得併入（Rule 8：測試必須在行為退化時會紅）。R4：選中態編碼成 XCUITest 可讀的 `accessibilityValue`／identifier 並斷言唯一且跟著切區；視覺（邊框／陰影）另以可機械複驗方式釘住；四組 mutation 附真實輸出。m1 sidebar 欄鋪 `lsBackground`（稿面坐在 `$bg`）。四條 info 逐條處置。已續派實作 R4。

### 2026-09-05T18:25:40.829Z · Chih-Lin Yeh · `f9b75b4a`

**R4（2026-09-06 02:25 CST）** — merge-review R3 `6e7ece86` REQUEST_CHANGES 逐條處置，push head `558a347`。

**M3（major）已修 `ca0e1c3`／`558a347`**：R3 的 `testSidebarSelectionIsAccessibleAndDistinguishable` 斷言 `XCUIElement.isSelected`，reviewer 四組 mutation 證明完全擋不住它宣稱要擋的退化（拿掉 trait 行其實是 app crash，我 R3 handoff 誤判成「轉紅」；`.accessibilityAddTraits([])`／全列套 trait／中性化視覺修法皆仍綠）。

改法分兩層：
1. 選中態改編碼成 `accessibilityValue("已選取")`。**R4 過程中額外實測發現真正病灶**：這兩個修飾詞（含 R3 的 `.accessibilityAddTraits`）原本套在 `sidebarRow` 回傳的 `HStack` 上，那是 Button `label:` 的內容視圖、不是 Button 本身——實測 Button 對自訂 label 合成 accessibility element 時不會把子視圖上的這兩個修飾詞往上帶，UITest 讀到的 `.value` 一律 `nil`。移到 `sidebar` 裡包住 `sidebarRow(section)` 的 `Button` 本身才真正解決。
2. 視覺樣式抽成 `SettingsSidebarRowStyle`（`Equatable` 純資料型別，`SettingsModels.swift`），新增 `SettingsSidebarRowStyleTests` 在單元測試層釘住——UITest 對純視覺退化沒有鑑別力。

四組 mutation 實測（非 crash 的真斷言失敗）：
- (a) 拿掉 `.accessibilityValue` 整行 → 紅：`("[]") is not equal to ("["個人"]")`
- (a2) 全部列套用空字串 → 紅：同上
- (a3) 全部列都套用「已選取」→ 紅：`("["個人", "家庭", "內容與安全", "法律", "帳號"]") is not equal to ("["個人"]")`
- (b) 中性化 `SettingsSidebarRowStyle`、保留 accessibilityValue → `SettingsViewIPadTests` 維持綠（設計上如此）；`SettingsSidebarRowStyleTests` 紅：`("clear") is not equal to ("NamedColor(name: "print-paper", bundle: nil)")` 等

revert 後兩邊測試（`SettingsSidebarRowStyleTests` 3 條、`SettingsViewIPadTests` 6 條）皆回綠。

**m1（minor）已修 `ca0e1c3`**：sidebar 拿掉 `Color.lsSurface`（R3 修 M2）後沒補上背景，露出系統預設純白／純黑——補回 `Color.lsBackground`。亮／深色截圖見 scratchpad `LS-188-R4-m1-ipad-light.png`／`LS-188-R4-m1-ipad-dark.png`：sidebar 與右欄現在共用同組暖粉／深栗底色。

**四條 informational 處置**：
- 1（iPad 邀請測試 1/6 flake）已修 `558a347`：`pushedSentinel` 等待時間加 `pushedSentinelTimeout` 參數（預設不變），邀請家人這條傳 10 秒（原因：`onAppear` 觸發的 `refreshLatestInvite()` async 查詢比其他同步渲染畫面多一段排程）。本機重跑 3 次皆過。
- 2（標題重複兩層）不修＋理由：R3 已拿掉多餘的內層 `NavigationStack`，剩下的是稿面 `B2DckT` 本身設計的兩個獨立元素（sidebar 標籤＋外層 nav bar 標題），非程式造成的技術性重複，留 QA 對稿確認。
- 3（陰影 radius 慣例）已修 `ca0e1c3`：radius 8→4，對齊既有 `$paper-shadow` 慣例。
- 4（`.accessibilityAddTraits` 疑似無效）不修＋理由：對 XCUITest `.isSelected` 確實無效（已證實），但保留作 VoiceOver 標準語意標記，文件註解已註明它不再是機械測試證據來源。

已驗證：648 單元測試（含新增 3 條）＋19 個 Settings／TapTargetGate UITest，iPhone＋iPad 模擬器皆全綠；SwiftLint 乾淨；push gate 綠。模擬器已關閉（`EEFA8968-4492-44DF-AA47-31FB0344D15A`、`E4284D42-B769-49F8-923A-AF1E366FE30D`），appearance 復原 light。

PR body 已更新（R4 段＋更新已驗證／未完成／風險／產出位置），`pr-body-check.sh --verify` exit 0。PR: https://github.com/CLYEH/little-sprout/pull/320

### 2026-09-05T18:27:07.519Z · Chih-Lin Yeh · `22e4aea2`

**Orchestrator（09-06 02:26）**：R4 handoff 收到（head `558a347`；M3 根因：accessibility 修飾詞套在 Button label 子視圖不會上帶 → 移到 Button 本身、`accessibilityValue("已選取")`＋抽 `SettingsSidebarRowStyle` 單元測試釘視覺；四組 mutation 皆真斷言失敗；m1 `lsBackground`；info 1／3 已修、2／4 不修＋理由）。已續派 merge-reviewer R4（重放四組 mutation、value 跟隨切區、像素取樣、scope）。APPROVE 後 merge → 隨下一次 promote 進 test → QA。

### 2026-09-05T18:43:18.294Z · Chih-Lin Yeh · `cd1a6907`

## merge-review R4（2026-09-06 02:42）：APPROVE

head `558a3479996e204bda417ca5df4813fcafa5a749`（＝當下 headRefOid，未前進）；審 `ecf4c9a...558a347`（`ca0e1c3` fix、`558a347` test）。CI 四項全 pass（`ci` 19m30s 等完才判）。

### M3（R3 major）已解——四組 mutation 我自己重放，全部是真斷言失敗

實作者找到的根因與我 R3 的量測吻合：`.accessibilityAddTraits`／`.accessibilityValue` 原本套在 `Button` 的 `label:` 內容（`sidebarRow` 回傳的 `HStack`）上，`Button` 合成 accessibility element 時不會把子視圖的這兩個修飾詞往上帶——這解釋了 R3 為什麼「trait 全開／全關／視覺全中性化」都測不出差別。R4 把兩個修飾詞移到 `sidebar` 裡的 `Button` 本身，並把「選中長什麼樣」抽成 `SettingsSidebarRowStyle`（`Equatable` 純資料）＋三條單元測試。

我重放的四組（全部型別不變，避免 R3 那種刪 modifier 造成的 SwiftUI crash 假陽性）：

| # | mutation | 實測結果 |
|---|---|---|
| a | `.accessibilityValue("")`（恆空） | UITest **紅**：`("[]") is not equal to ("["個人"]")`＋切區後 `[] ≠ ["家庭"]` — 真斷言失敗，非 crash |
| b | `.accessibilityValue("已選取")`（全列） | UITest **紅**：`(["個人","家庭","內容與安全","法律","帳號"]) ≠ (["個人"])`，切區後同樣紅 — 證明「恰好一列」語意有效 |
| c | 選中樣式中性化（＝把 R2 M2 缺陷原樣放回去） | `SettingsSidebarRowStyleTests` **紅** 2 條：`testSelectedAndUnselectedStylesDiffer`、`testSelectedStyleMatchesDesignTokens`（`("clear") ≠ NamedColor("print-paper")`／`paper-edge`／`paper-shadow`／`Weight 0.3 ≠ 0.4`） |
| d（我自加） | style 資料保持正確、但 view 端硬寫 `.clear` 繞過 style | 全綠（見下方 informational 1） |

a／b 也證實「已選取」訊號會跟著切區移動。R3 的 M3 不再成立。

### m1（R3 minor）已解——逐像素

| | sidebar 底 | detail 底 | 選中列 | 未選中列 |
|---|---|---|---|---|
| 亮色 | `RGB(237,211,214)` ＝ `#EDD3D6`（`bg`） | 同左 | `#FBEBEC`（`print-paper`） | 同背景 |
| 深色 | `RGB(25,14,18)` ＝ `#190E12`（`bg`） | 同左 | `#E8D9D4` | 同背景 |

兩欄底色一致、不再是系統純白／純黑；截圖確認選中列在 `$bg` 上是清楚的紙托（邊框＋陰影＋粗體），`radius: 8→4` 後視覺仍明確可辨。

### 其餘 R3 條目

- **info 1（flake）**：`pushedSentinelTimeout` 只加在「邀請家人」這條、預設值不變——不是掩蓋回歸（push 真的壞掉時仍會在 10 秒後紅），理由（`InviteFamilyView.onAppear` 的 async 查詢比其他靜態占位頁多一段排程）與我 R3 觀察到的失敗點一致。本輪 iPad 整包 6/6 綠、未再出現該 flake。
- **info 3**：`radius: 4` 與 repo 既有 `$paper-shadow` 用法（1.5／2／3／5）對齊。
- **info 2／4 不修理由接受**（標題層數交 QA 對稿；跨畫面配額失效通知超出本票）。

### 我實跑的驗證

- iPad（`LS-188-iPadAir11M3`）：`SettingsViewIPadTests` **6/6 綠**＋`TapTargetGateTests.testSettingsViewRegular` 綠。
- iPhone（`LS-188-iPhone17Pro`）：16 unit（含新 `SettingsSidebarRowStyleTests` 3 條）＋`SettingsViewTests` 7 條＋`testSettingsView` 全綠。
- `swiftlint lint --strict` exit 0；`xcodegen generate` **零漂移**；pbxproj 增量只有新測試檔的 4 行（無重複 ref）。
- **scope**：增量只碰 `SettingsView+Sidebar.swift`／`SettingsModels.swift`／`SettingsSidebarRowStyleTests.swift`（新）／`SettingsViewIPadTests.swift`／pbxproj —— 與宣告一致，無夾帶。

### informational（不擋合併，記入 handoff 即可）

1. **style→view 的接線本身仍無 gate**（我自加的 mutation d）：把 `SettingsSidebarRowStyle` 資料維持正確、但在 `sidebarRow` 硬寫 `.background(Color.clear)`／`.strokeBorder(Color.clear)`／`shadow(.clear)`，`SettingsSidebarRowStyleTests`、選中態 UITest、tap-target 全部仍綠。這是純資料測試的固有邊界（測得到「決策」，測不到「有沒有真的套用」），比 R3「什麼都測不到」已大幅改善；要補只能靠像素／快照比對，成本較高，建議記入 LS-96 待辦池而不是本票。
2. `SettingsModels.swift` 因為新型別而 `import SwiftUI`（該檔原本刻意只有 Foundation）；仍是純資料、無 View 依賴，只是檔案定位略微擴張，未來若要維持「純模型檔」可考慮另立 `SettingsSidebarRowStyle.swift`。
3. `.accessibilityAddTraits(.isSelected)` 保留但仍無 VoiceOver 實測佐證（R4 註解已誠實標明它不是機械證據，責任交給 `.accessibilityValue`＋單元測試兩層）——同意這個處置。
4. 前幾輪列過、本輪仍成立的既知限制：iPad push 會蓋掉整個兩欄畫面（非稿面暗示的常駐 sidebar）、個人列顯示名稱用 email 推導、家庭成員／檢舉紀錄摘要數字未接、root 用量摘要不跨畫面更新——都已在 PR body／handoff 申報，交 QA 知情。
- **沒看的範圍**：真實登入後 `RootView` 的 iPad 路徑仍以 harness 推論；`StorageUsageView` 失敗態未實跑；VoiceOver 未實測。

---

**Verdict：APPROVE**（無 blocker／major）。R1 B1、R2 M1／m1／m2／m3、R3 M2、R4 M3／m1 全部確認修好，且每一條都有我自己重放的 mutation 或逐像素量測佐證。

模擬器：`LS-188-iPhone17Pro`、`LS-188-iPadAir11M3` 用畢已 shutdown，`simctl ui` 的 appearance／content_size 已復原（light／medium）；review worktree 已移除。

### 2026-09-05T18:44:47.466Z · Chih-Lin Yeh · `1891804b`

**Orchestrator（09-06 02:44）**：merge-review R4 `cd1a6907` **APPROVE**（四組 mutation 重放皆真斷言失敗；m1 像素一致；四條 info 入池 `fc13bbd4`）→ PR #320 已併 development（dev tip status 已貼）→ QA。隨下一次 promote 進 test 做 QA（模擬器視覺：六板對稿、列文字垂直置中量測、iPad 五區 push＋選中態、09／09b）。

### 2026-09-05T18:59:14.747Z · Chih-Lin Yeh · `d9fcaa7c`

**Orchestrator（09-06 02:58）**：LS-191 還要一輪（R4），本票不再等——promote-follow dev `e4fde26` → test 已啟動（等 merge commit CI 綠），test 前進後派 QA（六板對稿、列文字垂直置中量測、iPad 五區 push＋選中態亮／深、09／09b、AX3）。

### 2026-09-05T19:05:15.462Z · Chih-Lin Yeh · `3e2baae5`

**Orchestrator（09-06 03:04）**：development `e4fde26` 已 promote 到 test（增量只有本票）。已派 QA：六板對稿、列文字垂直置中自行量測（≤0.5pt）、owner／member 角色差異、iPad 兩欄五區 push＋選中態亮色可辨、09／09b 真 DB 用量與已滿態、佔位五檔可達、reviewer 交 QA 三項對稿裁定（iPad detail 標題兩層、AX3 副標末行置中、`ProfilePrintChip` 固定尺寸）。PASS 後 test→main tag v0.17.0 → sweeper → Done。

### 2026-09-05T19:58:59.448Z · Chih-Lin Yeh · `0e89bd12`

## QA 驗收（2026-09-06 03:51）：**PASS**

驗 `test` tip `e4fde267122f6f84b8c0446b0fb0b41d813d0b7d`（增量僅 LS-188，PR #320 head `558a347`）。模擬器：`qa-test-iPhone17Pro`（iOS 26.0）／`qa-test-iPadPro13`（iOS 26.0，代 iPad 11" 稿；本機無 26.2 CI 釘住版，26.0 落在允許範圍）。

### 逐條驗收

1. **五區順序／文案／SF Symbol 對 `t5wI4`** ✓——個人／家庭／內容與安全／法律／帳號，順序、列名、icon（person.2.fill／person.badge.plus／door.left.hand.open／person.fill.xmark／flag.fill／internaldrive／doc.text／shield／trash／rectangle.portrait.and.arrow.right）全對；截圖 `01-root-light-top.png`／`02-root-light-bottom.png`。

2. **列文字垂直置中** ✓——自行用 a11y frame 量測（工具座標即 pt，解析度上限 1pt）：單行列（家庭成員／邀請家人／退出家庭）中線差 0.5pt（工具解析度地板值，reviewer R2 用 XCTest 精確量測 baseline 為 0.0pt）；多行列（個人列頭像＋雙行文字、儲存空間列雙行）中線差 **0pt**（完全對齊）。owner／member 角色差異 ✓——`testOwnerSeesReportInboxRow`／`testMemberDoesNotSeeReportInboxRow` 兩條 UITest 皆綠，Owner 看得到「檢舉紀錄」、member 看不到。

3. **深色 `Rx5mP`** ✓——`$print-paper` 頭像沖印框在深色模式維持米白（未變黑），截圖 `04-root-dark-profile.png`。**AX3 `z9tytH`** ✓（40pt 不破版，五區皆可讀不裁切，截圖 `05-root-ax3-top.png`／`06-root-ax3-mid.png`）；同時**直接對比稿面確認兩項既知落差為真**：①「個人」列副標「編輯顯示名稱與頭像」在 AX3 換行為 3 行時，稿面（`z9tytH`／`CmuNv`）三行皆左靠，實作最後一行「像」置中——裁定**接受，記入 LS-96 待辦池**（純 AX3 邊緣情境、不影響可讀性，本票要求的「垂直置中」本身正確落地）。②`ProfilePrintChip` 60/45/16.4pt 硬寫、不隨 Dynamic Type 縮放——裁定**接受**（與既有 `AvatarPrintCard` 慣例一致，非本票新增問題）。

4. **iPad `B2DckT`** ✓——兩欄版面（sidebar＋detail）；五區入口逐一 push＋返回實測：家庭成員（截圖 `09-ipad-family-members-pushed.png`）、儲存空間（`10-ipad-storage.png`）皆 push 成功、BackButton 返回後清單還原（`08-ipad-family-selected.png`）；其餘三區由 `SettingsViewIPadTests` 6/6 覆蓋。選中列亮色可辨（`家庭`／`內容與安全` 選中時可見 `$paper-edge` 邊框＋陰影紙托，非扁平色塊）；sidebar 底＝`$bg`，亮／深色皆與右欄同色系、無系統純白／純黑露底（`12-ipad-dark.png`）。**iPad detail 標題兩層**：直接對比稿面確認為真落差——`B2DckT` detail 欄只有一個標題，實作在 nav bar（大字）與 `SettingsSectionBlock` 內的區塊標籤（小灰字）各顯示一次「個人」/「家庭」等，五區皆然（結構性重複，非單一頁面問題）。裁定**接受（不擋放行），記入 LS-96 待辦池**——純視覺冗餘、不影響導覽功能與可達性，且 iPad 整體版面（push 蓋掉整個兩欄）已是既知落差，適合與後續 iPad 導覽整併一併處理。

5. **09／09b** ✓——真 DB `get_family_quota` 兩帳號皆驗證：`qa-ls188@ls.test`（空家庭）顯示「0 GB 已使用 / 5 GB」「用了 0%，還有 5 GB 可以用。」；用 `qa-e2e.sh publish` 情境對 `qa-e2e@ls.test` 家庭實際上傳照片＋影片後，DB 查得 `storage_used_bytes=34718`（真實非零，`docker exec` psql 核對），UI 正確顯示「0.0／5 GB」「用了 0%，還有 5.0 GB 可以用。」（無 NaN／負值，四捨五入乾淨）；相紙卷 12 格皆正確渲染。**42%／2.9GB／已滿態文案**因本機 fixture 太小（31KB／3KB）無法真實撐到有意義佔比，這段交給既有 `StorageUsageFormattingTests`＋reviewer R1 逐字對 `H5K5xm`／`ZG2Un` 的驗證（已通過，我信任其結果並用真實 RPC 驗證了管線本身無誤）。root 儲存列摘要 ✓（`0／5 GB`、`0.0／5 GB` 皆正確顯示）。**失敗態重試鈕 ≥44** ✓——嘗試斷網造失敗態時被 sandbox classifier 擋下（`docker stop` 觸發破壞性操作分類器拒絕，不強行繞過），改以程式碼檢視確認 `StorageUsageView.swift` 的重試 `Button` 已有 `.frame(minHeight: 44)`＋`.contentShape(Rectangle())`（`m3` 已修）；錯誤文案走 `AppError.userFacingMessage` 全站共用來源，與 LS-167 同語彙管線一致（非本票新增字串）。

6. **佔位五檔可達** ✓——iPad 實測「家庭成員」push 顯示 `ContentUnavailableView`「家庭成員／成員清單、角色管理與退出家庭尚未推出，敬請期待。」；其餘四檔由 `SettingsViewIPadTests` 5 條 push／back 測試覆蓋（Owner：帳號／內容與安全／個人／家庭；`testLegalSectionSwitchesWithoutPush` 確認法律區沿用既有開啟方式、iPad 上不誤 push）。

7. **測試** ✓——本機實跑（非引用 reviewer 結果）：
   - iPhone：`SettingsSidebarRowStyleTests`（3）＋`FamilyStoreRefreshQuotaRaceTests`（2）＋`SettingsViewTests`（7）＋`TapTargetGateTests/testSettingsView`（1）= 13/13 綠。
   - iPad：`SettingsViewIPadTests`（6）＋`TapTargetGateTests/testSettingsViewRegular`（1）= 7/7 綠。
   - 全量 `LittleSproutTests`：**648/648** 綠，0 failures（含上述子集）。

### 回歸冒煙（四主流程）
- 登入／時間軸／照片上傳：`qa-e2e.sh publish` 通過（exit 0），13 張截圖＋Storage log；真實上傳照片＋影片、時間軸出現卡片。
- 瀏覽（日記詳情＋相簿分頁）：`qa-e2e.sh browse` 第一次撞到 OTP 輸入焦點 flake（`Neither element nor any descendant has keyboard focus`，login 共用步驟，非 LS-188 程式碼路徑），**重跑一次通過**（exit 0，9 張截圖），判定環境性 flake。
- 留言：截圖確認「留言功能即將推出。」——確認尚未落地，非本票範圍缺口。

### RLS 冒煙
`supabase db reset` ＋ `bash supabase/tests/run.sh`（皆經 lock）：**全部 DB 測試通過**，含 `10_cross_family_isolation.sql` ✓（跨 family 資料不可見）。LS-188 本身不含任何 migration，此為既有回歸確認、非本票新增覆蓋範圍。

### 對稿裁定彙總（reviewer 交 QA 三項）
| 項目 | 裁定 |
|---|---|
| iPad detail 標題兩層 | **接受**（記入 LS-96，非阻塞） |
| AX3 副標末行置中 | **接受**（記入 LS-96，純 AX3 邊緣情境） |
| `ProfilePrintChip` 固定尺寸 | **接受**（既有慣例一致） |

### 未完成／已知限制（沿用 PR 申報，QA 未發現新增落差）
- iPad push 蓋掉整個兩欄畫面（非常駐 sidebar）——已知，跨票範圍。
- 個人列顯示名稱用 email 推導；家庭成員／檢舉紀錄摘要數字未接——留 LS-192／LS-23。
- `StorageUsageView` 失敗態未能在本輪以真實斷網觸發（sandbox 擋下 `docker stop`），以程式碼檢視＋既有測試佐證取代。
- VoiceOver 未實測（`.accessibilityAddTraits`／`.accessibilityValue` 僅有 XCTest／單元測試佐證）。

模擬器已關：`5F59FF04-D2C6-413D-B31A-AFD39B257FB3`（qa-test-iPhone17Pro）、`F2398C21-EE84-4269-8759-56D7FFC9BE8C`（qa-test-iPadPro13）；appearance／content_size 已復原 light／medium。lock 已釋放（`--hold` 兩段：「LS-188 QA 冒煙」到期 exit 1 已知悉，期間無其他 agent reset，`qa-e2e.sh`／後續 `--hold "...cont"` 起手時容器狀態與資料一致，未見資料被洗掉的異常；「LS-188 QA 冒煙 cont」正常釋放，持有 6 分 57 秒）。

證據：`.claude/worktrees/qa-test/scratchpad/LS-188-qa-shots/`（13 張手動截圖）；`.claude/worktrees/qa-test/.claude/evidence/LS-188/qa-e2e/publish-20260906-033045/`、`browse-20260906-034341/`（各附 xcresult／storage.log）。

### 2026-09-05T20:00:34.512Z · Chih-Lin Yeh · `c2f0abaa`

**Orchestrator（09-06 04:00）**：QA `0e89bd12` **PASS**（六板對稿、置中量測 ≤0.5pt、iPad 兩欄五區 push＋選中態、真 DB 用量 09／09b、648 unit＋20 UITest、qa-e2e publish／browse、RLS 全套；三項對稿裁定皆接受並入池）→ test→main，tag **v0.17.0**。dead-code-sweeper 已派 → Done → `cleanup-merged.sh --apply LS-188`。

### 2026-09-05T20:07:07.128Z · Chih-Lin Yeh · `46908a4a`

## dead-code sweep — LS-188

**dead-code-sweeper（2026-09-06 04:06）**：巡檢範圍＝`git log --oneline 127c38c..e4fde26`（10 commits，PR #320 全部四輪 review 迭代含 merge commit）涉及的 33 個檔案。worktree `.claude/worktrees/LS-188`（`feature/LS-188-settings-root`，head `558a347`）：**clean，無 dirty 檔案**，可直接跑 `cleanup-merged.sh`。

### Findings：**未發現本 feature 引入的死碼**

逐項覆核如下（含票文點名的四輪 review 迭代殘留重點）：

1. **R1→R2 內層 `NavigationSplitView`**：`git diff` 全量 33 檔搜尋 `NavigationSplitView`，只剩 4 處**文件註解**提到它（說明「當初為什麼拿掉」），現行 `SettingsView.swift` `regularBody`（:147-160）是純 `HStack` 自繪兩欄，零殘留程式碼。
2. **R2→R3 內層 `NavigationStack`／`.id(regularSelection)`**：`grep -n "regularSelection"` 現行檔只剩合法用途——`@State` 宣告、`sidebarRow`／`sidebar` 讀寫、`sectionContent(for:)`、`.navigationTitle(regularSelection.title)`；搜尋不到任何裸的內層 `NavigationStack` 或 `.id(regularSelection)` 呼叫，R3 informational 1 訂正過的文件註解與現行行為一致。
3. **R3→R4 accessibility 修飾詞搬家**：`.accessibilityAddTraits`／`.accessibilityValue` 只在 `SettingsView+Sidebar.swift:40-41`（`sidebar` 內、`Button` 本身）各出現一次真呼叫，其餘全是文件註解說明沿革；`sidebarRow(_:)`（:93-116）確認**不再**掛這兩個修飾詞，零舊呼叫殘留。
4. **`SettingsSidebarRowStyle`**：`grep -rn` 全 repo，只有 `SettingsView+Sidebar.swift`（呼叫端 `SettingsSidebarRowStyle.style(isSelected:)`）與 `SettingsSidebarRowStyleTests.swift`（3 條測試）兩處真引用，其餘是文件註解——只被 view 與測試用，符合預期，非死碼。
5. **`SettingsRowView.value`**：現在有真呼叫端——`SettingsView.contentSafetyRow(.storage)`（:270-273）把 `familyStore.quota.map(StorageUsageView.compactUsageSummary)` 傳進 `value:`，`SettingsViewTests.testStorageRowShowsUsageSummaryOnceQuotaLoads` 覆蓋斷言「2.1／5 GB」——R1 m1 的落差已補齊，非死碼。
6. **五個佔位檔**（`ProfileEditView`／`FamilyMembersView`／`DeleteAccountFlowView`／`BlockListView`／`ReportInboxView`）：逐檔讀過，內容皆最小（`ContentUnavailableView` 三行＋`#if DEBUG #Preview`），每檔只有 `import SwiftUI` 一個 import，無多餘引用；檔名與票文 Notes 板約定、`tap-target-exemptions.txt` 新增七行逐一比對皆一致。
7. **`SettingsModels.swift` 的 `import SwiftUI`**：確認是已知池項（R4 informational 2，`SettingsSidebarRowStyle` 用到 `Color`／`Font.Weight`），不重複列為 finding。
8. **fork 事故三檔**（`FamilyModels.swift`／`FamilyAPIClient.swift`／`SupabaseFamilyAPIClient.swift`）：`grep -n "Profile\|fetchMyProfile\|fetchMemberCount"` 只命中既有（非 LS-188 引入）的 `ensureProfileExists`／`ProfileUpsertPayload`（帳號建立既有邏輯），**無** `fetchMyProfile`／`fetchMemberCount` 殘留。`fetchQuota` 全鏈路清查：protocol 宣告（`FamilyAPIClient.swift:61`）→ `SupabaseFamilyAPIClient.fetchQuota`（正式 RPC 實作）→ `PreviewFamilyAPIClient.fetchQuota`（`#Preview`／harness 用，DEBUG）→ `StubFamilyAPIClient.fetchQuota`＋`setFetchQuotaHandler`（測試用，`FamilyStoreRefreshQuotaRaceTests` 兩處呼叫）→ `FamilyStore.refreshQuota()` 唯一呼叫端——五層全部有真呼叫，無孤兒。
9. **`FamilyStore.quota`／`refreshQuota()`／`seedQuotaForPreview`**：`quota`／`quotaState` 有 `StorageUsageView`／`SettingsView` 兩處讀取；`seedQuotaForPreview` 是 `#if DEBUG` 內方法，只給 `#Preview`／harness 用，兩處 `#Preview` 呼叫點都在——非死碼。
10. **`ChildrenStore.seedRoleForPreview`**（LS-188 c558dd7 新增，`#if DEBUG`）：唯一呼叫端 `TapTargetGateHarness+Settings.swift:49`（`settingsMemberRoleHost`），該 host 又被 `TapTargetGateScreenName.settingsMemberRole` case 與 `SettingsViewTests.testMemberDoesNotSeeReportInboxRow`／`SettingsViewIPadTests` 引用——鏈路完整。
11. **`TapTargetGateHarness+Settings.swift`／`TapTargetGateScreenName` 新 case**（`.settingsMemberRole`／`.settingsRegular`）：`grep -rn` 確認兩個 case 都有測試呼叫（`SettingsViewTests`／`SettingsViewIPadTests`／`TapTargetGateTests.testSettingsViewRegular`），舊的 `TapTargetGateHarness.swift` 內被搬到新檔的 `settingsHost` 沒有留下重複定義（`git diff` 顯示舊檔那段是刪除，不是留一份 dead copy）。
12. **`QAAccessibilityID` 三個新常數**（`settingsProfileRow`／`settingsInviteRow`／`settingsStorageRow`）：皆有 app 端 `.accessibilityIdentifier` 賦值與 UITest 讀取端，雙向確認無孤兒。
13. **鷹架殘留掃描**：對 33 檔全量 diff 的新增行做 `print(`／`TODO`／`FIXME`／`XXX`／`debugPrint`／`XCTSkip`／`disabled` 關鍵字搜尋，唯一命中是合法的 `.disabled(isSigningOut)` view modifier（登出中停用按鈕，非鷹架殘留）。文件內的 `// 待辦（LS-133 核可後）` 是票文範圍 1 明定的刻意佔位（指向另一張已存在的票，非空懸 TODO）。
14. **PR body 提到但未落地的項目**：比對 PR #320 body「未完成／已知限制」六條與程式碼現況，全部有對應的 handoff 文字或程式碼註解交代（email 推導顯示名稱、成員／檢舉數字未接、iPad 兩欄疊層、用量摘要不跨畫面更新、`ProfilePrintChip` 固定尺寸、`SettingsIconsTests` 無版本表）——這些是**已知功能限制**，不是死碼（沒有對應的未引用符號或殘留檔案）。
15. **pbxproj**：`grep -c` 六個新檔名共 24 處命中（6 檔 × 4 種典型引用），R1-R4 reviewer 已各自用 `xcodegen generate` 驗證零漂移，本次抽查未見異常。

### 既有死碼觀察

無。本次巡檢範圍內未發現與 LS-188 無關但被順帶碰到的既有死碼。

### 建議處置

不需開票、不需下一張票順手刪——本票乾淨。可直接進入收尾儀式其餘步驟（lesson learning review、`cleanup-merged.sh --apply LS-188`）。

### 搜尋方法與盲區聲明

- 方法：`git log --oneline 127c38c..e4fde26` 鎖定 10 個 commit 的完整 diff（33 檔，2305 行）；對每個票文點名的檔案／符號用 `grep -rn`（跨 `LittleSprout`／`LittleSproutTests`／`LittleSproutUITests`）交叉比對宣告端與呼叫端；對五個佔位檔與 `SettingsModels.swift`／`SettingsRowView.swift`／`SettingsView+Profile.swift`／`SettingsView+Sidebar.swift`／`StorageUsageView.swift`／`SettingsView.swift` 全文讀過而非只看 diff片段（含尾端 `#if DEBUG #Preview` 區塊）。
- 盲區：純文字 `grep` 抓不到 reflection、字串拼接組出的符號名、Objective-C runtime 動態引用——本票是純 SwiftUI／Swift 值型別＋直接呼叫，未發現任何此類動態引用跡象，但無法窮盡排除。`SettingsIconsTests` 的 SF Symbol 字串是手動同步清單（非自動掃描程式碼），理論上可能有畫面新增符號但忘記補測試清單的情況，這屬於測試覆蓋率問題而非死碼，未列入本次 finding。未實際編譯／跑測試（依賴四輪 reviewer 與 QA 已各自實跑並回報 648 單元測試＋UITest 全綠，本次不重複進行）。

### 2026-09-05T20:10:02.950Z · Chih-Lin Yeh · `1500df1d`

**收尾儀式（09-06 04:07）**
- **dead-code-sweeper** `46908a4a`：無死碼；R1→R4 三次結構迭代零殘留；fork 事故三檔乾淨；佔位五檔最小。
- **Lesson learning review**：(1) iPad 巢狀 `NavigationSplitView` 讓全部入口按不動——靜態截圖全對、互動全壞 → iPad 驗收一律互動 UITest（CI iPad job 已入池）；(2) 「測試宣稱會紅」三度失準 → handoff mutation 必附原文（池 `0e49f3e3`）；(3) 派工中誤用 fork 做唯讀擷取造成平行編輯 → 唯讀任務用 Explore；(4) 四輪 review 各抓不同層，每輪都是真缺陷。
- **清理**：`cleanup-merged.sh --apply LS-188` → worktree 已移除。
→ Done（v0.17.0）。LS-189／193 解鎖。

