# LS-158 Harness：QA 端到端驅動不依賴 mobile-mcp——LittleSproutUITests 加 QA e2e 情境測試（launchEnvironment 指定登入 OTP 自本機 Mailpit 取碼／固定 fixture 發佈／時間軸瀏覽），QA 以 -only-testing 驅動並接 Storage log

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:M, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-04T02:35:55.679Z |
| 開始 | 2026-09-04T10:32:51.627Z |
| 完成 | 2026-09-04T20:58:31.723Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-158/harnessqa-端到端驅動不依賴-mobile-mcp-littlesproutuitests-加-qa-e2e |
| 關係 | related→LS-175, related→LS-173, related→LS-174, related→LS-169, related→LS-172, related→LS-155, related→LS-130, related→LS-170, related→LS-144, related→LS-96, related→LS-129, related←LS-270, related←LS-264, related←LS-246, related←LS-245, related←LS-216, related←LS-220, related←LS-190, related←LS-164, related←LS-184, related←LS-181, related←LS-178, related←LS-176, related←LS-168, related←LS-159, related←LS-157 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項 `e1d6d282`（P1，2026-09-03 [LS-129](https://linear.app/little-sprout-app/issue/LS-129/taskls-20-ios-上傳端縮圖mediauploadservice-同步產生並上傳-thumb寫入-thumb-path尺寸)／130 QA `4cb41a06`／`d731c417` BLOCKED）：mobile-mcp 每次互動把模擬器前景重設回主畫面（WebDriverAgent session 未沿用），QA 無法多步驟操作 app；同一 build 用 `xcodebuild test -only-testing:LittleSproutUITests` 可正常驅動，證明是工具層。與 `28f3b345`（QA 方法缺口）合併。池註「cycle 2 第一批」；本票依 [LS-144](https://linear.app/little-sprout-app/issue/LS-144/harness巡檢加開票責任機械提醒-lane-在飛-0-且無可派候補時動作清單印-開票並列來源story-拆票ls-96-p1-升票) 開票責任先立案、**待 Spec**（範圍 (a)／(b) 擇一由 orchestrator 在 cycle 2 規劃時定案），不進 cycle 1。

## Spec 定案（orchestrator 2026-09-04 18:20）：採 **(a)**——QA harness 走 `LittleSproutUITests` e2e 驅動；(b) 不採（QA 現場寫一次性腳本不可重放、無 gate）。cycle 1 harness lane 接在 [LS-170](https://linear.app/little-sprout-app/issue/LS-170/harnessios-devreviewer-互動式本機驗證模擬器對本機-supabase-容器必須先-supabase-locksh) 之後。

### 範圍（定案）

1. `LittleSproutUITests/QA/`：`QASmokeTests`（launchEnvironment：`LS_QA_SCENARIO=login|publish|browse`、`LS_QA_API_URL`／`LS_QA_ANON_KEY` 指向本機容器、`LS_QA_MAILPIT=http://127.0.0.1:54324`）——登入用 Email OTP 自 Mailpit API 取碼；發佈固定 fixture 照片＋影片（bundle 內小檔）；瀏覽時間軸／日記詳情／相簿；每步 `XCTAttachment` 截圖＋`accessibilityIdentifier` 斷言；失敗即紅、不得靜默 skip（環境變數缺 → `XCTFail` 說明）。
2. `scripts/ops/qa-e2e.sh <scenario> [--sim <name>]`：包 `xcodebuild test -only-testing:LittleSproutUITests/QA*`，輸出截圖到 `.claude/evidence/<票號>/qa-e2e/`，內部經 `supabase-lock.sh --hold`（QA 持有）；把 Storage log（`supabase logs`／容器 stdout）一併收。
3. `qa.md`＋COLLABORATION §4-b 派工模板：多步驟驗收優先 `qa-e2e.sh`，mobile-mcp 降為輔助（截圖／單步）；驗收證據路徑規約。
4. Gate：`agent-tools-check.sh` `BODY_RULES` 加 `qa|qa-e2e.sh`；CI `ci` job 對 `LittleSproutUITests/QA*` 至少 build（不跑，需容器）；自測 `qa-e2e.test.sh`（缺環境變數紅、scenario 名錯紅）。
5. [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `e1d6d282`／`28f3b345` 銷除註記。

## 範圍（原候選，保留供對照）

* (a) **QA harness**：`LittleSproutUITests` 加「QA e2e 驅動」測試群（launchEnvironment 指定情境：登入 OTP 自本機 Mailpit `http://127.0.0.1:54324` 取碼、發佈固定 fixture 照片＋影片、瀏覽時間軸／日記詳情／相簿），QA agent 以 `-only-testing:LittleSproutUITests/QA*` 跑並蒐集截圖（`XCTAttachment`）與 Storage log；mobile-mcp 降為輔助。
* (b) **QA 白名單開 Edit 於 **`LittleSproutUITests/QA*` **路徑**（不進 commit），讓 QA 現場寫一次性驅動腳本。
* 兩者皆需：qa agent 定義與派工模板同步；`e1d6d282` 銷除註記。

## 不做

修 mobile-mcp 本身；把 QA 全面改成 UI 測試（回歸冒煙四條仍可用 mobile-mcp）。

## 驗收

- [ ] Spec 定案（(a)／(b)）並補進本段
- [ ] QA 對一張 UI 票以新驅動完成多步驟操作並產出截圖證據，不依賴 mobile-mcp 前景狀態
- [ ] qa agent 定義／派工模板更新；merge-reviewer 通過
- [ ] [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `e1d6d282`／`28f3b345` 銷除註記

## 留言（10）

### 2026-09-04T13:36:21.440Z · Chih-Lin Yeh · `735ad9c8`

## handoff — LS-158（general-purpose，2026-09-04 21:35）

**Ticket**：LS-158 Harness：QA 端到端驅動不依賴 mobile-mcp（Spec 定案 (a)，範圍 1–5）。PR **#281** → development，head `e9dd1b7`（6 commits，自 development `790b72d`；與 `1cdb7fc` 可乾淨合併）：`a546ef1` feat(harness)／`e735578` test(uitests)／`96581b6` docs(collab)／`e9d43c7` fix(harness)／`c9f0483`＋`e9dd1b7` fix(uitests)。**CI 四項綠**（rules／db／lint／ci）。

**已完成**
1. `LittleSproutUITests/QA/`：`QASmokeTests`（`LS_QA_SCENARIO=login|publish|browse` 分派）＋`QADriver`（啟動注入 `LS_QA_API_URL`／`LS_QA_ANON_KEY`、每步 `XCTAttachment` 截圖、等不到元素附 a11y 階層再 `XCTFail`；登入／建家庭／編輯器／`PhotosPicker` 選圖／瀏覽）＋`QASupport`（`QAEnvironment.load` 缺變數一律 `XCTFail` 不 skip；`QAMailpit` 打 Mailpit API 只認寄碼後的新信抽 6 碼）＋`Fixtures/qa-photo.jpg`（31 KB）／`qa-video.mp4`（3.4 KB）。app 端最小接線：`SupabaseClientFactory.qaOverride`（DEBUG only，兩值同時合法才覆寫）、`LittleSprout/QAAccessibilityID.swift`（掛兩個 target）＋六處 identifier（`LabeledTextField` 加可選參數；按鈕沿用 label）。
2. `scripts/ops/qa-e2e.sh <scenario> [--sim] [--ticket] [--email]`：情境／票號／`supabase status`（重試 3 次）／Mailpit＋GoTrue health 逐關 fail closed → 專屬機找／建／boot（自己 boot 的收工關）→ **每情境先 `simctl keychain reset`**、publish 另 `addmedia` → `supabase-lock.sh --hold "<票號> qa-e2e <情境>" --max-minutes 25`（已持有就沿用）→ `xcodebuild test -only-testing:LittleSproutUITests/QASmokeTests` → 截圖依 manifest 改回步驟名（Xcode 自動診斷另放 `diagnostics/`）→ `docker logs --since` 收 Storage 容器 stdout。自測 `qa-e2e.test.sh` 21 組（PATH shim，不實跑 xcodebuild）。
3. `qa.md`（驗收流程 3／4、新段「端到端驅動（LS-158）」、視覺驗收 1、回報加「qa-e2e 證據」行）；COLLABORATION §1 qa 行、§4-b 巡檢／派工模板加派 qa 的一句、§7 新增一列（含盲區）。
4. Gate：`agent-tools-check.sh` BODY_RULES `qa|qa-e2e.sh`＋自測 ⑥；`tap-target-check.sh` 改純 `-skip-testing:LittleSproutTests`＋`-skip-testing:LittleSproutUITests/QASmokeTests`（man xcodebuild：`-only-testing` 對 `-skip-testing` 有優先權，原旗標會把 QA 情境帶進 CI 假紅）＋自測 ⑨ 釘旗標；`ci.yml` 註記 QA/ 只編譯不跑（CI Build & Test log 實證 `QADriver.swift`／`QAAccessibilityID.swift` 被編譯）。
5. 實證：三情境對本機容器各實跑通過（下）；LS-96 銷除回覆已貼：`e1d6d282` ← `7ae17028`、`28f3b345` ← `4d476102`（後者誠實註記五條案例本身未實作、只落地載具）。

**已驗證（怎麼驗）**
- 實跑（證據 `.claude/worktrees/LS-158/.claude/evidence/LS-158/qa-e2e/`，gitignored）：
  - `login-20260904-200804`：✓ 5 張（welcome→email-entered→otp-screen→otp-entered→landed-fork），`Executed 1 test, with 0 failures`；排隊 173 秒等 LS-172 的 hold，持有 59 秒。
  - `browse-20260904-203350`：✓ 12 張（登入 5 步→create-family→timeline→body-typed→published-card→detail→albums→timeline-again），持有 1 分 24 秒。第一輪 `browse-20260904-202621` ✗：沿用 login 留在 Keychain 的 session，中間 qa-test 的「LS-155+LS-169 QA 冒煙 R2」reset 過容器，建立家庭被後端拒（截圖紅字「這個操作沒有成功」）→ 改成每情境 keychain reset＋各自 OTP 登入。
  - `publish-20260904-204924`：✓ 11 張＋`picker-cells-Photo/Video.txt`（挑中 `Photo, September 04, 8:10 PM @ x=0 y=346`＝fixture；這輪容器又被他票 reset 過，情境自己重登、重建家庭仍過）。`storage.log`：`POST /object/media/<f>/2026/09/<id>.jpeg`、`.mp4`、兩次 `_thumb.jpg` 各 200；簽名 `GET` 只命中 `_thumb.jpg`（LS-130 QA 當時拿不到的證據）。前兩輪 ✗：`not hittable`（遠端 view → 座標 tap）、挑到內建樣本（a11y 順序非時間序、label `Photo, September 04, 8:10 PM` 不帶年份 → 只查 `app.images`＋「MMMM dd」）。
- CI 的 tap-target gate 不跑 QA：本機對專屬機實跑 `-skip-testing` 組合 `Executed 22 tests, with 0 failures`（六個既有 UI 測試類全跑、`QASmokeTests` 0 次）；CI 同步驟也是 22 tests。push-gate（unit tests＋tap-target gate）全綠。
- CI：第一次 attempt 的 ci 在 tap-target gate 紅——`CreateChildAvatarFieldUITests.test_avatarField_existsAndIsHittable`（LS-169 的測試，本 PR 未動 `CreateChildView`，本機兩次 22/22 綠）runner 上 `isHittable` 誤判；`gh run rerun 33875323608 --failed` 後綠。flaky 候選，記入本 handoff、未入池（一次觀察）。
- 自測：`qa-e2e.test.sh` 21 組、`tap-target-check.test.sh` 9 組（含 ⑨）、`agent-tools-check.test.sh` 40 組（含 mutation 負控）；`agent-tools-check.sh` 真 repo「正文必含字樣 4 條」。`build-for-testing` `** TEST BUILD SUCCEEDED **`；SwiftLint strict 0；PR body 過 `pr-body-check.sh`。
- Mutation：`tap-target-check.sh` 改回 `-only-testing:LittleSproutUITests` → ⑨ 紅；拿掉 `qa|qa-e2e.sh` 或 qa.md 該段 → ⑥／真 repo 紅（負控：拿掉整個 BODY_RULES 區塊負樣本變綠）；`qa-e2e.sh` 情境名不驗／容器檢查後移 → ②④ 紅（假 xcodebuild 標記檔出現）。`QAEnvironment.load` 改 `XCTSkip` 無自測（需模擬器），靠 review。

**未完成**
- merge-review 尚未進行（PR 剛開、CI 綠）；reviewer informational 處置：無（待 R1）。
- `PhotosPicker` 為系統 UI：label／排序依 iOS 版本與模擬器系統語言（本票對 iOS 26、en 實測；zh-Hant 字樣一併認），其他語言或 iOS 改版會在「照片格」那步紅並附候選清單——屆時退回 mobile-mcp 單步選圖再接 e2e（票文範圍 5 替代）。不另開票。
- 「情境真的跑過」無機械驗（§7 盲區）：靠 handoff「qa-e2e 證據」行＋evidence 目錄。
- `28f3b345` 的五條具體案例（拖曳排序／>50 MiB／VoiceOver／真併發／失敗注入）未實作，載具已在；要追蹤請另立池項。

**風險**
- 共用容器被他票 reset 是常態（本票兩小時內撞到兩次）：情境已自給自足（每次重登），但同一帳號 `qa-e2e@ls.test` 的家庭／資料會隨 reset 消失，browse 會自己 seed 一篇純文字——想看有照片的詳情要先跑 publish 再 browse（同一次容器壽命內）。
- 每情境 addmedia 讓專屬模擬器相簿累積 fixture（不影響挑選：挑今天的第一格）。
- 開工時本機磁碟曾滿（`No space left on device`，docker CLI 掛住、容器全停，patrol／他票的 `docker ps` 掛了十幾個），期間他人清出 96 GiB 並重啟 Docker／容器；`supabase status` 偶爾瞬間回空 → 腳本讀三次。
- ci 的 flaky（LS-169 測試）若再現，值得入池：`isHittable` 在 runner 上可能需要 `waitForExistence` 後再等一拍。

**產出位置**
- PR https://github.com/CLYEH/little-sprout/pull/281（body：scratchpad `LS-158-pr-body.md`）；worktree `.claude/worktrees/LS-158`，branch `feature/LS-158-qa-e2e`。
- 證據：`.claude/worktrees/LS-158/.claude/evidence/LS-158/qa-e2e/{login-20260904-200804,browse-20260904-203350,publish-20260904-204924}/`（screens／storage.log／xcodebuild.log／result.xcresult）；失敗輪 `browse-20260904-202621`、`publish-20260904-203552`／`204016`／`204051`／`204643` 亦保留供對照。
- 模擬器已關：`13D03C8A-243B-436D-B45A-0ADE16E3881A`（`LS-158-iPhone17Pro`，每輪由腳本／push-gate 關）。lock 已釋放：八次 hold 皆由腳本 `--release`（59 秒／1 分 30 秒／1 分 24 秒／2 分 19 秒／7 秒／1 分 24 秒／1 分 33 秒／1 分 25 秒），`--status` 現為 free。
- LS-96 銷除回覆：`7ae17028`（於 `e1d6d282`）、`4d476102`（於 `28f3b345`）。

### 2026-09-04T13:50:00.076Z · Chih-Lin Yeh · `be862711`

## merge-review R1 — PR #281（head e9dd1b7）

裁決：**APPROVE**（無 blocker；**無 major**）。2 條 minor（N1／N2）＋4 條 informational。審的 head＝`e9dd1b715ee0b6ca22f081b65cdac9f03353c835`，與 `gh pr view 281 --json headRefOid` 當下值相同；merge-base `790b72d`，25 檔 +1010/−16；`gh pr checks 281` 四項全 pass（ci 14m56s／db 3m40s／lint 13s／rules 3m25s，run 33875323608 attempt 2）。2026-09-04 21:47 CST。**lock：未持有**——本輪 review 沒有互動式容器操作（唯一碰到容器的是一條在健康檢查就退出的探測，見 N1），期間 lock 由 LS-174 持有中，我沒有排隊也沒有打斷。

### （1）app 端接線的安全面 — **我實跑 Release build 並掃了二進位，零殘留是真的**

靜態：整個 `LittleSprout/` 對 `LS_QA_*` 的引用只有 `SupabaseClientFactory.swift:59-60` 兩行，完整落在 `:57 #if DEBUG` … `:62 #else return nil` … `:64 #endif` 內；呼叫點是 `qaOverride?.url ?? AppConfig.supabaseURL` 與 `qaOverride?.anonKey ?? AppConfig.supabaseAnonKey`，Release 下屬性恆為 nil ⇒ 行為與改動前完全相同。

動態（我自己跑的，不是讀 log）：

```
xcodebuild -configuration Release -sdk iphonesimulator … build   → BUILD SUCCEEDED
strings LittleSprout.app/LittleSprout | grep -c 'LS_QA'          → 0
```

**正控**（證明我的掃法真的抓得到東西）：同一支二進位裡 `XCTestConfigurationFilePath` 1 次、`qa.emailSignIn.emailField` 1 次（accessibility identifier，本來就刻意不圍 `#if DEBUG`）、`LS_TAP_TARGET_GATE_SCREEN` 0 次。**結論：Release 版讀不到也不存在 `LS_QA_*` 這條路徑，launchEnvironment 在 App Store build 不可能觸發覆寫。** 兩個值必須同時合法才覆寫、缺一半就照 `AppConfig` 走的設計也對——半套設定會在既有 assert／後端錯誤大聲失敗，不會靜默指到奇怪的後端。

### （2）`tap-target-check.sh` 改純 `-skip-testing` — 主張正確，而且我在 CI log 裡找到硬數字

- **優先權主張屬實**：`man xcodebuild` 第 337–338 行原文「`-only-testing` has precedence over `-skip-testing`」。所以原本的 `-only-testing:LittleSproutUITests` ＋ skip 是無效組合，改成純 skip 是唯一解。
- **CI 實證（比 PR body 的宣稱更硬）**：我把**第一次 attempt**（失敗那次）的 job log 抓下來，裡面有 `Executed 22 tests, with 1 failure`，而那唯一的失敗是 `CreateChildAvatarFieldUITests.test_avatarField_existsAndIsHittable()`——**不是 QASmokeTests**。attempt 2 同一步綠、印出 `✓ tap-target-check：已量測畫面…`。兩次都證明：新旗標下跑的是 **22 個測試、QASmokeTests 0 次**（它若跑到，缺 `LS_QA_*` 必 `XCTFail` → gate 紅）。
- CI `ci` job 的 Build & Test 步驟確實**編譯**了 `QASmokeTests.swift`／`QASupport.swift`（log 有 SwiftCompile 行）——票文範圍 4「CI 對 QA/ 至少 build」達成，編譯壞掉這步就紅。
- 自測 `tap-target-check.test.sh ⑨`（假 xcodebuild 記 argv、釘住兩個 skip 且無任何 `-only-testing`）我跑過，綠。

### （3）`qa-e2e.sh`

**好的部分**（我逐段讀＋實測）：防線順序正確（情境名／票號 → `supabase status` 三次重試 → Mailpit `/api/v1/info` 與 GoTrue `/auth/v1/health` → 模擬器 → keychain reset → hold → xcodebuild），**任何一關失敗都 exit 2 且碰不到 xcodebuild**；`trap cleanup EXIT` 同時處理 `--release` 與「自己 boot 的模擬器關機」，異常退出也會釋放；`held_by_me` 旗標讓「沿用呼叫者的 hold」時**不代為釋放**；證據路徑 `.claude/evidence/<票號>/qa-e2e/<情境>-<時間>/` 符合規約（gitignored）。

**自測牙齒**：我自己做 3 個 mutation——M1 拿掉情境白名單、M2 拿掉 Mailpit health、M3 票號推不出改成預設值——**三個全被殺**。值得誇一句設計：每案除了 exit code 還斷言「不該呼叫 xcrun／xcodebuild／docker」，三個 mutant 最後其實也都 exit 2，是那條「不該呼叫」的斷言把它們抓出來的。

**N1（minor，我實跑重現）：腳本不擋主 checkout，而錯誤訊息還把人導向那條路。**
`root=$(git rev-parse --show-toplevel)` 只要求在 git repo 內；票號推不出時的訊息是「加 `--ticket LS-<n>`」。我從**主 checkout** 跑（`cd /Users/clyeh/little-sprout && bash …/LS-158/scripts/ops/qa-e2e.sh login --ticket LS-158`），腳本順利通過票號關、一路走到 Mailpit 健康檢查才因我刻意指的壞 URL 退出——也就是說**沒有任何一關擋主 checkout**。若容器健康，它會接著 boot 模擬器並以 `worktree=<主 checkout>` 取得 25 分鐘的 hold；依 LS-170 §6 的持有者判定（同 worktree 即視為持有者），**在主 checkout 上跑的 orchestrator／reviewer 全部會被判成持有者直通**，這個 hold 等於沒鎖——正是 LS-169 那類事故的成因。
修法（三行）：`case "$root" in */.claude/worktrees/*) ;; *) echo "✗ qa-e2e：只能在票 worktree 內跑（hold 的持有者判定看 worktree，主 checkout 會讓所有主 checkout 程序直通，LS-170 §6）"; exit 2 ;; esac`，並把「推不出票號」的提示改成「請 cd 到 `.claude/worktrees/LS-<n>` 再跑」。不列 major：要踩到得從錯的目錄、還得明給 `--ticket`，而 qa.md／ios-dev.md／merge-reviewer.md 三處都寫了「在票 worktree」；失敗模式是鎖失效、不是資料毀損。

**N2（minor）：hold 沿用靠人類可讀訊息字串比對，且這條路徑零自測覆蓋。**
`qa-e2e.sh:138` 用 `hold_rc -eq 2 && grep '已持有 hold\|已在 lock 內'` 判斷「呼叫者已持有」。我對過 `supabase-lock.sh:338/339` 兩條訊息，**目前字面確實對得上**。但這是跨腳本的文字耦合，而 LS-170 R2（就在今天）才剛把 lock 的另一句人類可讀訊息從「QA 持有中」改成「持有中」——證明這類字串會動；一旦動到這兩句，QA 的每次 e2e 都會變成 exit 2 假失敗。而且 `qa-e2e.test.sh` 的 21 案**全部停在 hold 之前**（設計如此：斷言沒碰到任何工具），加上 `supabase-lock.sh` 是以絕對路徑 `$here/supabase-lock.sh` 呼叫、PATH shim 攔不到，所以**步驟 5 與 cleanup 這段完全沒有自測**。方向是 fail-closed（會大聲紅、不會靜默放行），故列 minor。修法擇一：(a) `--hold` 對「你已經持有」回一個專屬 exit code（例如 3），呼叫端判碼不判字；(b) `LOCK_SH=${LS_LOCK_SH:-$here/supabase-lock.sh}` 讓自測能塞 shim，補兩案（沿用不重複 hold／沿用時不代釋放）。

**另外兩個小點**：`trap cleanup EXIT` 沒有帶 `INT TERM`（bash 對 SIGINT 通常仍會跑 EXIT trap，實務上無礙，但寫成 `trap cleanup EXIT INT TERM` 更穩）；hold 取得到 `trap` 設定之間有 2 行空窗（中間沒有會失敗的操作，且 hold 有 25 分鐘自動到期兜底）。

### （4）`QASmokeTests`／`QASupport`

- **缺環境變數不 skip**：`QAEnvironment.load` 對缺項／情境名錯／Mailpit URL 不合法一律 `XCTFail(...)` 後 `throw`，**全檔沒有任何 `XCTSkip`**——符合票文範圍 1。`continueAfterFailure = false` 也對（後續步驟依賴前一步畫面）。
- **Mailpit 取碼**：30 秒逾時、1 秒輪詢、只認 `sentAfter − 5s` 之後寄給該信箱的信（避開上一輪的舊碼，這是實務上最容易誤判成「碼不對」的坑）、逾時錯誤訊息帶「最後看到什麼」。`(?<!\d)\d{6}(?!\d)` 取第一個獨立 6 碼——我查了 `supabase/templates/otp.html`，全文唯一的數字內容就是 `{{ .Token }}`，**今天不會撞**；只是這條規則與模板耦合，模板日後加了訂單號之類的 6 碼就會抓錯（informational I-4）。
- **fixtures**：`qa-photo.jpg` 31 KB、`qa-video.mp4` 3.4 KB，合計 34 KB 進版控——可接受，且刻意用 jpg 避開 pre-commit 對白名單外 `*.png` 的攔截。

### （5）文件與 gate

`agent-tools-check.sh` 的 `BODY_RULES` 加 `qa|qa-e2e.sh`（現在 4 條規則），真 repo gate 綠並印「qa.md：正文含「qa-e2e.sh」」；`agent-tools-check.test.sh` 40 組全綠（我跑過）。qa.md 步驟 3／4 改寫（多步驟優先 e2e、mobile-mcp 降為單步／補截圖）、新增「端到端驅動」段、handoff 產出位置多一行 qa-e2e 證據；COLLABORATION §1／§4-b／§7 各補一段／一列。三份措辭一致。

### （6）實證：證據目錄對得上宣稱，**LS-130 拿不到的縮圖證據這次拿到了**

`.claude/evidence/LS-158/qa-e2e/` 有 8 個回合（login 1／browse 2／publish 5）。我抽驗最新的 publish（`publish-20260904-204924`）：13 張截圖、`xcodebuild.log`、`result.xcresult`、`storage.log`。把 `storage.log` 的 10 行 JSON 逐行解出來：

| # | method | path | status |
|---|---|---|---|
| 1 | POST | `/object/media/<family>/2026/09/<uuid>_thumb.jpg` | 200 |
| 2 | POST | `/object/media/<family>/2026/09/<uuid>.jpeg` | 200 |
| 3 | POST | `/object/media/<family>/2026/09/<uuid>_thumb.jpg` | 200 |
| 4 | POST | `/object/media/<family>/2026/09/<uuid>.mp4` | 200 |
| 5–6 | POST | `/object/sign/media` | 200 |
| 7–10 | GET | `…_thumb.jpg?token=…`（×4） | 200 |

**照片與影片各自「原檔＋`_thumb.jpg`」都上傳成功、簽名、再以 signed URL 取回縮圖**——這正是 LS-129／130 QA 當時因 mobile-mcp 卡住而拿不到的那段證據鏈。一點措辭精確化：實際動詞是 **POST**（Supabase Storage 上傳走 POST），不是 PUT。browse 的 `storage.log` 是 0 行（該情境不上傳，合理）；最早那次 login 的截圖檔名還帶 `_0_<UUID>` 尾綴（改名邏輯是後面的 commit 才加的），後續回合都已是乾淨的 `<情境>-<序號>-<步驟>.png`。

**我沒有自己重跑三個情境**（需要 25 分鐘級的獨佔 hold，而 lock 正被 LS-174 使用）——我驗的是產出物本身（截圖數、log 內容、路徑規約），不是重跑。

### （7）CI 第一次 attempt 的紅 — 判定：**flaky，且不是本票造成的**

attempt 1（job 101030815974，12:56–13:12）唯一失敗是第 7 步「點擊目標 gate」，log 尾段：`Failing tests: CreateChildAvatarFieldUITests.test_avatarField_existsAndIsHittable()`，整批 `Executed 22 tests, with 1 failure`。attempt 2 **同一個 commit、同一組旗標**全綠——同 SHA 一紅一綠，定義上就是 flaky。該測試屬 LS-169、斷言 `isHittable`：XCUITest 的 hittability 受動畫／layout 時序影響，在忙碌的 CI runner 上最容易假紅（log 也看得到每個 case 都要等 `Waiting 10.0s for … to exist`）。

建議處置（**不擋本票**）：開一張小 chore 對 LS-169 那支測試——把裸 `isHittable` 斷言換成「先 `waitForExistence`，再對 hittable 做 expectation 等待」，或直接斷言 `exists` ＋ frame ≥44pt（tap-target gate 真正在乎的是尺寸）。**不建議把「rerun 就綠」當成處置**：沒有診斷的重跑會讓 flake 常態化，下一次它紅在別人的票上時沒人知道是舊帳。順帶一提，本票把 gate 改成 skip-list 之後，這支 flaky 測試仍在 22 個之內、每張碰 UI 的 PR 都會跑到它，所以值得排。

### Informational

**I-1（維護性）`-skip-testing` 是黑名單，未來新增的 test target／類別預設會被納入** — 舊的 `-only-testing:LittleSproutUITests` 是白名單，新形式改成「跳過 unit target ＋ 跳過 QASmokeTests」，語意上等於「其餘全跑」。日後有人加第三個 UI test 類別，它會自動進 CI 的 tap-target gate（時間成本），加第三個 test **target** 更會整包被納入。⑨ 釘住的是旗標字面、不是集合大小；「Executed N tests」的 N 變動是唯一的訊號。記一筆即可，不需改。

**I-2（nit）`http_code` 失敗時印 `000000`** — `curl … -w '%{http_code}'` 失敗時本身已輸出 `000`，`|| printf '000'` 再補一次。我實測訊息是「不可達（HTTP 000000）」。改成 `curl … || true` 或把 fallback 拿掉即可。

**I-3（觀察）證據目錄累積 8 個回合、含 `result.xcresult`** — `.claude/evidence/` 是 gitignored，不進版控，但 xcresult 每份可觀（本機磁碟）。qa.md 沒說何時清；建議 QA 收工時只留最後一次成功回合，或在 cleanup 儀式順手清。

**I-4（耦合）`sixDigitCode` 取第一個獨立 6 碼，與 OTP 模板耦合** — 現行 `otp.html` 唯一數字就是 `{{ .Token }}`，安全；若模板日後加了其他 6 碼數字（訂單號、日期串）就會抓錯。要更穩可改成「取最後一個」或錨在「你的驗證碼是：」之後。

### 四維度

race：`qa-e2e.sh` 的並發面就是 lock，hold／沿用／trap 釋放三條路徑都審過（N1／N2 是它的兩個縫）；`QASmokeTests` 是 `@MainActor` 單一 test case、`continueAfterFailure = false`，沒有測試間共享狀態——每個情境還先 `simctl keychain reset` 從未登入開始，這個決定是對的（實測過的假缺陷來源）。效能：CI 只多編譯兩支 Swift 檔；tap-target gate 仍是 22 個測試、約 6 分鐘（與改動前同量級，QA 情境不在其中）。平行：`-parallel-testing-enabled NO` 沿用既有慣例，正確（UI 測試共用一台模擬器）。scope：25 個檔全部對得上票文範圍 1–5，app 端只加一個 DEBUG-only 屬性＋6 個 accessibility identifier 掛點，沒有順手改動；`project.yml`／`pbxproj` 的變更就是把 `QAAccessibilityID.swift` 掛進兩個 target（XcodeGen 漂移檢查在 CI 綠）。


### 2026-09-04T17:07:00.496Z · Chih-Lin Yeh · `bc18ffe9`

## handoff R2 — LS-158（general-purpose，2026-09-05 01:05）

**Ticket**：LS-158。PR **#281** → development，新 head **`e0cad6f`**（R1 head `e9dd1b7` ＋ 3 commits：`ecaf959` fix(ops)／`5916e24` fix(harness)／`e0cad6f` fix(uitests)）。**CI 四項綠**（rules／db／lint／ci，一次 attempt）。PR body 已補「merge-review R1 → R2」段（在最後一次 push 前 `gh pr edit`，`pr-body-check` 綠：6 條「已修」皆帶 SHA）。

**已完成（R1 `be862711` 逐條處置）**
- **N1 主 checkout 不擋**：已修 `5916e24`——`qa-e2e.sh` 在票號之前判 `git rev-parse --path-format=absolute --git-dir` 與 `--git-common-dir` 同一目錄（主 checkout）即 exit 2、印「請在票 worktree（.claude/worktrees/LS-<n>）內跑」；「推不出票號」訊息改「請在 .claude/worktrees/LS-<n> 內跑；固定 QA worktree（qa-test）才用 --ticket」，不再建議 `--ticket` 繞過。自測 ③ N1：合成主 checkout 帶 `--ticket` → exit 2、訊息含「這是主 checkout」、未碰 xcrun／xcodebuild／docker；推不出票號的訊息斷言不含「加 --ticket」。真機主 checkout 實跑被 PreToolUse main-checkout-guard 擋在命令層（第二層防線），未另做。
- **N2 hold 沿用比對訊息、步驟 5／cleanup 零自測**：已修 `ecaf959`（`supabase-lock.sh --hold` 兩條「已持有」分支回專屬 exit 3，檔頭 exit 表補一行；`supabase-lock.test.sh` ㉑ 改斷言 3＋加「hold 內經 wrapper 重入再 --hold → 3」）＋ `5916e24`（`qa-e2e.sh` `case "$hold_rc"` 判 0／3／其他，不 grep 訊息；`LS_LOCK_SH` 覆寫；`trap 'exit 130' INT`／`'exit 143' TERM`）。自測改真 worktree 夾具（`git worktree add`）＋ smart xcrun 扮專屬機＋ lock stub＋假 xcodebuild，⑥ 六組：取得→跑→`--release`＋關自己 boot 的機／回 3 沿用→照跑、無 `--release`／回 124→exit 2、不碰 xcodebuild、仍關機／xcodebuild 紅→exit 1 仍 `--release`／本來就 Booted 不 boot 不關／publish 缺 fixture 不 hold。
- **I-1 skip-list 黑名單**：不修——白名單會讓新增 UI 測試類別靜默不跑（更危險），黑名單多跑只是時間；⑨ 釘旗標、集合大小由「Executed N tests」看得到（本機／CI 皆 22）。
- **I-2 `http_code` 印 `000000`**：已修 `5916e24`——curl 自己印 000 時不補印，只有無輸出才補；自測 ④ 兩組（HTTP 000 非 000000／curl 無輸出→000）。
- **I-3 xcresult 清理時機**：已修 `5916e24`——qa.md「端到端驅動」段加一句（每回合含 `result.xcresult` 數十 MB；收工只留裁決引用的成功回合、其餘 `rm -rf`）；同段補「只准在票／QA worktree 內跑」。
- **I-4 `sixDigitCode` 取第一個 6 碼**：已修 `e0cad6f`——先錨在「驗證碼是」之後找，找不到錨點才退回整段第一個獨立 6 碼。
- 另兩小點：`trap` INT/TERM 已修 `5916e24`；hold→trap 的 2 行空窗不修（中間只有 `case` 賦值、hold 有 25 分鐘到期兜底）。
- R1 建議的 LS-169 flaky chore（`CreateChildAvatarFieldUITests` 裸 `isHittable`）：不在本 PR 範圍，請 orchestrator 開票（本輪 ci 一次綠，該測試這次沒紅）。

**已驗證（怎麼驗）**
- `qa-e2e.test.sh` **47 組**全綠（含 ③ N1、④ I-2、⑥a–f）；`supabase-lock.test.sh` 全綠（㉑ exit 3 兩組）；`agent-tools-check.test.sh` 40 組＋真 repo gate「正文必含字樣 4 條」；SwiftLint strict 0。
- push gate（unit tests＋tap-target gate）綠：第一次 push 被系統以低記憶體殺掉（xcodebuild 跑 30 分鐘無結果、留下 simulator-lock 死鎖），重跑後 simulator-lock 自動回收死鎖（`持有者 pid 15560 已不存在`）、一次過。
- CI run on `e0cad6f`：rules／db／lint／ci 全綠（ci 一次 attempt）。
- R2 未重跑三情境（R1 已驗產出物；本輪改動只在腳本前置／lock 判碼／取碼錨點，`QASmokeTests` 驅動步驟未動）——如 orchestrator 要求，`bash scripts/ops/qa-e2e.sh login` 即可（需容器 free；此刻 lock 由 LS-173 QA 持有）。
- Mutation：拿掉主 checkout 判定 → ③ N1 紅；hold 沿用改回比對訊息／沿用時代釋放 → ⑥b 紅；`--hold` 改回 exit 2 → lock 自測 ㉑ 紅。

**未完成**：無 R2 待辦；informational 六條處置如上（已修×4、不修×2 附理由）。

**風險**：本機記憶體壓力（同時多個 xcodebuild／模擬器）會讓 push gate 被系統殺掉並留下 simulator-lock 死鎖——lock 有自動回收，但 30 分鐘無輸出時值得先 `ps` 看 xcodebuild 是否還活著再等。

**產出位置**：PR https://github.com/CLYEH/little-sprout/pull/281（head `e0cad6f`；body scratchpad `LS-158-pr-body.md`）；worktree `.claude/worktrees/LS-158`；證據目錄同 R1 handoff（`735ad9c8`）。模擬器已關：`13D03C8A-243B-436D-B45A-0ADE16E3881A`（LS-158-iPhone17Pro；push gate 自關；被殺那次由我手動 shutdown）。lock 已釋放：本輪未 `--hold`（無互動段）；`--status` 現為 LS-173 QA 持有（非本票）。

### 2026-09-04T17:07:37.443Z · Chih-Lin Yeh · `a6adef29`

**2026-09-05 01:07 orchestrator — R2 handoff `bc18ffe9` 收到 → In Review，派 merge-reviewer R2**

- PR #281 head `e0cad6f`，CI 四項綠。
- 審點：R1 `be862711` N1／N2／I-1～I-4 逐條查實（每條「已修」對 `git show <sha>:<file>`）；I-1 不修理由是否成立；`qa-e2e.sh` 自測是否真的覆蓋 hold 沿用不代釋放、xcodebuild 紅仍釋放；`supabase-lock.sh --hold` 新 exit 3 對既有呼叫端（qa.md／ios-dev.md 規則）無回歸。

### 2026-09-04T17:17:47.517Z · Chih-Lin Yeh · `69e71c2a`

## merge-review R2 — PR #281（head e0cad6f，增量 e9dd1b7..e0cad6f）

裁決：**APPROVE**（無 blocker／無 major）。R1（`be862711`）的 6 條**逐條查實：已修 4 條（N1／N2／I-2／I-3／I-4 共 5 項）＋不修 2 條（I-1、hold→trap 空窗）理由成立**，另兩小點的 trap INT/TERM 也修了。新增 1 條 informational（R2-I1，我自己跑出來的存活突變體）。審的 head＝`e0cad6f8aed8b18ffd64f97a021fb89ef2cd0f36`，與 `gh pr view 281 --json headRefOid` 當下值相同；增量 6 檔 +180/−60；CI 四項全 pass（run 33897037524，一次 attempt）。2026-09-05 01:16 CST。**lock：未持有**（本輪無互動段；查詢時 `--status` 為 free）。

### 逐條查實（每條都對 `git show <sha>:<file>` 與實跑核對）

| R1 項 | 申報 | 我的查實 |
|---|---|---|
| **N1** 主 checkout 不擋 | 已修 `5916e24` | **屬實**。`qa-e2e.sh:73-79` 在票號推導**之前**用 `git rev-parse --path-format=absolute --git-dir` vs `--git-common-dir` 判定（linked worktree 的 git-dir 在 `.git/worktrees/<名>`，主 checkout 兩者相同）——技術選擇正確、比我建議的路徑字串比對更硬。**我把 R1 那條重現命令原樣重跑**：主 checkout ＋`--ticket LS-158` → **exit 2**＋「這是主 checkout…請在票 worktree」；不帶 `--ticket` 也是同一關先擋；在票 worktree 內則照常往下走到容器檢查。推不出票號的訊息也不再建議 `--ticket` 繞過（自測 `③ N1` 第 139 行專門斷言訊息**不含**「加 --ticket」）。 |
| **N2** 判訊息字串＋零自測 | 已修 `ecaf959`＋`5916e24` | **屬實**。`supabase-lock.sh:340/341` 兩條「已持有」分支都改 `exit 3`、檔頭 exit 表補一行；`qa-e2e.sh:149-157` 改 `case "$hold_rc" in 0|3|*)`，**不再 grep 任何訊息**；`LS_LOCK_SH` 覆寫點就位；`trap 'exit 130' INT`／`'exit 143' TERM` 補上。 |
| **I-1** skip-list 是黑名單 | 不修＋理由 | **理由成立**。白名單的失敗方向是「新增的 UI 測試類別靜默不跑」（沉默失效），黑名單是「多跑、只花時間」（吵但安全）——與這個 repo 一貫的 fail-loud 取向一致，也正是我 R1 寫「記一筆即可，不需改」的原意。⑨ 釘旗標＋「Executed N tests」當訊號，足夠。 |
| **I-2** `HTTP 000000` | 已修 `5916e24` | **屬實且我實測到新輸出**：`http_code()` 改成 `code=$(curl …); printf '%s' "${code:-000}"`；在票 worktree 用壞 URL 實跑，訊息現在是「不可達（**HTTP 000**）」。 |
| **I-3** xcresult 清理時機 | 已修 `5916e24` | **屬實**。`qa.md`「端到端驅動」段補「每回合含 `result.xcresult`（數十 MB）…收工只留裁決引用的成功回合，其餘 `rm -rf`」，同句還補了「只准在票／QA worktree 內跑」與 LS-170 §6 的理由——文件與 N1 的機械擋一致。 |
| **I-4** `sixDigitCode` 取第一個 6 碼 | 已修 `e0cad6f` | **屬實，而且我用真的 Swift 驗過行為**（把該函式原樣抽出來跑）：真實模板 → `482913`；**錨點前放誘餌「訂單 998877」→ 仍取錨點後的 `123456`**（R1 的舊邏輯會取 998877）；沒有錨點 → 退回第一個獨立 6 碼；長數字串（時間戳）不誤抓。 |
| 另：trap INT/TERM | 已修 `5916e24` | 屬實（見 N2 列）。 |
| 另：hold→trap 2 行空窗 | 不修＋理由 | **理由成立**：中間只有 `case` 賦值與 echo，且 hold 有 25 分鐘到期兜底——與我 R1 的評估相同。 |

### `--hold` 改 exit 3 的回歸面（我另外查的）

- **程式性呼叫端只有一個**：全 repo grep `--hold`，除 `supabase-lock.sh` 自己與其自測外，只有 `qa-e2e.sh` 會判 exit code；`ios-dev.md`／`merge-reviewer.md`／`qa.md`／COLLABORATION 都只寫「先 `--hold`、收工 `--release`、逾時 124 不得 `rm -rf`」，**沒有任何一處把「已持有」寫成 exit 2**——換碼沒有文件殘留。
- 對人類／agent 的行為不變：仍是非 0＋同一句訊息（`先 --release 再 --hold`）。
- `supabase-lock.test.sh` **218 ✓／0 ✗**（LS-170 R2 時是 217，+1＝新的 `㉑ hold 內經 wrapper 重入再 --hold → 也是 exit 3`）；`㉑` 第 357 行明白釘住「已持有再 `--hold` → 專屬 exit 3」。

### 自測 ⑥ 六組是不是真的覆蓋（不是只有名字）

`qa-e2e.test.sh` 47 組全綠（R1 是 21）。⑥ 的夾具是**真的 `git worktree add`**（所以 N1 的 git-dir 判定是真被走過、不是假造）＋`LS_LOCK_SH` lock stub（三模式 0／3／124）＋smart `xcrun` stub（扮專屬機、可設 `FAKE_SIM_STATE=Booted`）＋假 `xcodebuild`（pass／fail）。逐條對到你點名的三件事：

- **⑥b 沿用不代釋放**：`FAKE_LOCK_MODE=already`（回 3）→ 斷言輸出含「沿用呼叫者既有的 hold」、`log_hasnt lock.log 'lock --release'`、且**仍有跑 xcodebuild**。
- **⑥d xcodebuild 紅仍釋放**：`FAKE_XCODEBUILD_MODE=fail` → exit 1、印 log 尾段、`log_has lock.log 'lock --release'`（trap EXIT）、且仍關自己 boot 的機。
- **⑥e 已 Booted 不關**：`FAKE_SIM_STATE=Booted` → `log_hasnt calls.log 'simctl shutdown'` 且 `log_hasnt 'simctl boot '`。
- 另外 ⑥a（取得→`-only-testing:LittleSproutUITests/QASmokeTests`→keychain reset→release＋關機）、⑥c（124 → exit 2、不碰 xcodebuild、不 release、仍關自己的機）、⑥f（publish 缺 fixture → 不 hold、不碰 xcodebuild）。

**我自己的 mutation（4 個）**：M1 拿掉主 checkout 判定 → ③ N1 紅；M2 沿用時設 `held_by_me=1`（代釋放）→ ⑥b 紅；M3 `cleanup()` 改成無條件關機 → ⑥e 紅。**M4 存活**，見 R2-I1。

### LS-96 銷除查實（你的第 4 點）

PR body 只有 1 行 LS-96 申報：「`e1d6d282`／`28f3b345` 兩則池項的銷除回覆已貼：`7ae17028`／`4d476102`」。我**到 LS-96 抓全部 comment 逐一比對**：兩個 id **都存在**，內容是對應池項的「**銷除**（2026-09-04 21:19，LS-158 PR #281）」。而且 `4d476102` 自己帶了一段**誠實註記**：五條方法缺口的個別案例（長按拖曳排序、>50 MiB 阻擋、VoiceOver 雙擊、loadMore 併發、失敗態注入）**本票未實作**，只落地了共同載具——沒有把載具當成缺口全解，這個自我約束值得記一筆。informational 六條的處置是「已修×4／不修×2 附理由」，**沒有任何一條申報記入 LS-96**，所以沒有「申報了卻沒寫」的情況；`pr-body-check --verify` 也把 6 條「已修」的 SHA 逐一對回 PR commits（`5916e24`×4／`ecaf959`／`e0cad6f`）全部命中。

### 我沒有重跑三情境（同意，但理由要精確）

增量唯一碰到驅動路徑的是 `QASupport.sixDigitCode`——而這一項我用**真的 Swift 跑過四種輸入**（見 I-4 列），比重跑一次 `login` 更能證明它對誘餌 6 碼的行為，因為真實模板裡根本沒有誘餌、跑 login 只會走到「本來就會過」的那條路。其餘改動全在腳本前置與 lock 判碼，`QADriver`／`QASmokeTests` 的步驟一行未動（`git diff` 對這兩檔 0 變更）。所以**不重跑**，並明講這是我的判斷。

### R2-I1（informational，新增；我跑出來的存活突變體）

`qa-e2e.sh` 的 hold **失敗分支**（`case` 的 `*)`）裡那句 `[ "$booted_by_me" -eq 1 ] && xcrun simctl shutdown "$udid"`，把 `booted_by_me` 判斷拿掉之後**整套 47 組仍全綠**。原因是 ⑥ 的矩陣缺一角：⑥c 測的是「**自己 boot** ＋ hold 失敗」、⑥e 測的是「**本來就 Booted** ＋ hold 成功」，沒有「**本來就 Booted ＋ hold 失敗**」那一格——所以「hold 拿不到時別去關別人的模擬器」這條沒有被釘住。現行程式碼是對的（判斷還在），只是回歸保護少一格；補一案 `FAKE_SIM_STATE=Booted FAKE_LOCK_MODE=fail` ＋ `log_hasnt calls.log 'simctl shutdown'` 即可（一行）。失敗情境：日後有人重構這段時誤刪判斷 → QA 的 e2e 在搶不到 lock 時會把**別人正在用的模擬器關掉**，而測試不會紅。因為現況正確、且要踩到得先有人改壞，列 informational 不擋併入。

### 四維度（增量）

race：這輪動的正是並發面——`--hold` 回碼、沿用不代釋放、trap INT/TERM／EXIT 三條路徑現在都有 stub 覆蓋（除 R2-I1 那一角）；`exit 3` 是新增碼、不與既有 0／1／2／124 衝突。效能：無變化（多兩次 `git rev-parse`）。平行：qa-e2e 仍是單機單情境、`-parallel-testing-enabled NO` 未動。scope：6 檔全部對得上 R1 的 finding，`qa.md` 只加一句、`QASupport.swift` 只動一個函式、`supabase-lock.sh` 只動兩個 exit 碼與檔頭——沒有順手改動、沒有碰 `QADriver`／`QASmokeTests`／gate 旗標。


### 2026-09-04T17:19:44.288Z · Chih-Lin Yeh · `ff9a9995`

**2026-09-05 01:18 orchestrator — merge-review R2 `69e71c2a` APPROVE → 已併入 development `81c7921`，票進 QA**

- PR #281 head `e0cad6f`（merge-review status id 53561443168）。dev tip 已貼 `promote: no content diff`。
- 驗收第二條「QA 對一張 UI 票以新驅動完成多步驟操作並產出截圖證據」：搭 LS-174（頭像列表刷新 fix，review R3 中）併入後的 test 一起驗——qa 派工單指定用 `scripts/ops/qa-e2e.sh` 驅動，證據路徑照本票規約；PASS 即本票 QA PASS。
- Reviewer informational 處置：R2-I1（`qa-e2e.sh` 自測缺「已 Booted＋hold 失敗」一角）→ 記入 LS-96；LS-169 `CreateChildAvatarFieldUITests` 裸 `isHittable` flaky → 記入 LS-96（連續紅兩次即升 chore 票）。

### 2026-09-04T20:48:41.219Z · Chih-Lin Yeh · `60ee49ea`

**QA — Delta（2026-09-04 20:48 UTC，test head `679a417`）— PASS**

驗的是 dev→test FF 增量（`bc4b592`→`679a417`），只涵蓋 LS-158＋LS-174 兩票的檔案；`git diff --stat` 確認無例外檔案落在其他票路徑。

## 驗收逐條

- [x] **Spec 定案（(a)／(b)）並補進本段**——票文已載明採 (a)，orchestrator 2026-09-04 18:20 定案段落在。
- [x] **QA 對一張 UI 票以新驅動完成多步驟操作並產出截圖證據，不依賴 mobile-mcp 前景狀態**——本輪主要驗收項。在 qa-test worktree（`--ticket LS-174`）依序跑三情境，皆一次過、皆全綠：
  - `login`：exit 0，5 張截圖，`.claude/evidence/LS-174/qa-e2e/login-20260905-041052/`
  - `publish`：exit 0，13 張截圖＋`storage.log`（10 行：照片＋影片各自 POST 原檔／`_thumb.jpg`，簽名與 4 次 GET 皆 200），`.claude/evidence/LS-174/qa-e2e/publish-20260905-041231/`
  - `browse`：exit 0，12 張截圖（含日記詳情、相簿分頁），`.claude/evidence/LS-174/qa-e2e/browse-20260905-041822/`（等 LS-175 的 hold 排隊 256 秒後正常取得，證明 lock 排隊機制本身也正常運作）
  - 三情境全程無需 mobile-mcp 前景重設困擾——多步驟導覽（登入→建家庭→選圖→發佈→詳情→相簿→回時間軸）一次到位，驗證了本票要解的核心問題已解。
- [x] **qa agent 定義／派工模板更新；merge-reviewer 通過**——`qa.md`／`docs/COLLABORATION.md` 已見「端到端驅動（LS-158）」段落；merge-review R2 `69e71c2a` APPROVE（0 blocker/major）。
- [x] **LS-96 `e1d6d282`／`28f3b345` 銷除註記**——merge-review R2 已逐一查核兩則銷除留言存在且內容對應，`pr-body-check --verify` 對「已修」SHA 全命中。

## 回歸冒銀（順帶）
- 時間軸／相簿：browse 情境已覆蓋（詳情頁、相簿分頁截圖）。
- harness 自測：`qa-e2e.test.sh` 47 組全綠；`agent-tools-check.test.sh` 40 組全綠；`supabase-lock.test.sh` 4 次執行中 3 次 218 組全綠、1 次在 ⑬ M1（timing mutation，`SUPABASE_LOCK_HOLD_TICK` 秒級壓縮）於系統重負載（同時段有其他 worktree 的 xcodebuild／模擬器）下短暫紅，重跑即綠——判定系統負載造成的計時 flaky，非本增量回歸（本增量對 supabase-lock.sh 的改動已在 LS-174 delta 之前完成、非本輪新碰）。

**裁決：PASS**。四條驗收全數滿足，逐一附證據。

### 2026-09-04T20:53:40.472Z · Chih-Lin Yeh · `28f1ebbe`

**2026-09-05 04:5x orchestrator — QA PASS `60ee49ea`（qa-e2e.sh 三情境一次過、30 張截圖＋Storage log）→ 已到 main `679a417`（v0.11.0）**

- 驗收四條齊：Spec 定案 (a)／QA 以新驅動完成 LS-174 多步驟驗收／qa.md＋模板更新＋merge-review 通過／LS-96 `e1d6d282`／`28f3b345` 銷除。
- 收尾：sweeper 批次巡檢中 → retro → Done。

### 2026-09-04T20:56:58.990Z · Chih-Lin Yeh · `79ac8bfe`

## dead-code sweep — LS-158

**範圍**：`git diff --stat 1cdb7fc..679a417`（排除 `design/`）中屬本票的檔案——`LittleSproutUITests/QA/`（`QADriver.swift`／`QASmokeTests.swift`／`QASupport.swift`／`Fixtures/`）、`scripts/ops/qa-e2e.sh`＋`qa-e2e.test.sh`、`scripts/ops/supabase-lock.sh`（`--hold` exit 3 段）＋`supabase-lock.test.sh`、`scripts/gates/tap-target-check.sh`＋`.test.sh`、`scripts/gates/agent-tools-check.sh`＋`.test.sh`、`.claude/agents/qa.md`、`docs/COLLABORATION.md`、`.github/workflows/ci.yml`、`project.yml`、以及 app 端最小接線（`SupabaseClientFactory.swift` 的 `qaOverride`、`QAAccessibilityID.swift`、六個 `.accessibilityIdentifier` 掛點）。主 checkout `679a417`，唯讀巡查。

### Findings

**未發現本 feature 引入的死碼。**

- `QAAccessibilityID` 六個常數（`emailField`／`otpCodeField`／`familyNameField`／`diaryBodyEditor`／`timelineDiaryCard`／`diaryDetailBody`）逐一 grep 確認：每個都同時被一處產品程式碼（`.accessibilityIdentifier(...)`）與 `QADriver.swift` 消費，無孤兒常數。
- `QADriver.swift`／`QASmokeTests.swift`／`QASupport.swift`：全部 public／internal 函式（`require`／`waitForAny`／`signInWithEmailOTP`／`ensureLoggedIn`／`ensureFamily`／`openEditor`／`typeDiaryBody`／`publishAndWaitForCard`／`attachFixturesFromPhotoLibrary`／`seedDiaryIfTimelineEmpty`／`browseDetailAndAlbums`／`sixDigitCode`／`fetchOTP` 等）皆在 `runLogin`／`runPublish`／`runBrowse` 三條情境路徑中被呼叫，無未使用的 helper。
- `SupabaseClientFactory.qaOverride`：`#if DEBUG` 圍住，Release 恆為 nil；`strings` 掃描 Release 二進位驗證不殘留（merge-review R1 已實測，`grep -c 'LS_QA'` 為 0，本次巡檢複查程式碼結構一致，唯讀未重跑二進位掃描）。
- `qa-e2e.sh`／`qa-e2e.test.sh`：47 組自測與腳本邏輯一一對應，無死分支；`supabase-lock.sh` 的 exit 3 變更與 `qa-e2e.sh` 的 `case "$hold_rc"` 判碼、`supabase-lock.test.sh` ㉑ 的斷言三處一致。
- `tap-target-check.sh`／`agent-tools-check.sh` 的規則變更（純 `-skip-testing` 組合、`BODY_RULES` 新增 `qa|qa-e2e.sh`）與對應 `.test.sh` 的新增案例（⑨／⑥）一一對應，無殘留的舊斷言或未達成的規則。
- `qa.md`／`COLLABORATION.md`／`ci.yml`／`project.yml` 的文件與設定變更皆與程式碼變更一致，未發現指向已改名／已刪除檔案的殘留路徑。

### 建議處置

無 finding 需處理。

### 搜尋方法與盲區聲明

- 方法：`git diff` 逐檔全文閱讀（含大型檔案 `QADriver.swift` 280 行、`qa-e2e.sh` 231 行、`qa-e2e.test.sh` 211 行全文）；`grep -rn` 對六個 accessibility identifier 常數逐一交叉比對宣告與消費端；`grep -nE "TODO|FIXME|XXX|console\.log"` 全域掃描本票檔案清單（含 `docs/API.md` 提及的字面，確認皆為既有慣例描述而非殘留）。
- 盲區：純文字搜尋抓不到 XCUITest 執行期對 accessibility tree 的動態查詢是否真的命中（例如 `PhotosPicker` 系統 UI 的 label 依模擬器系統語言而變，這是 merge-review R1 已記錄的既知限制，非本次巡檢新發現）；未實跑 `qa-e2e.sh` 三情境或 47 組自測（採信 merge-review R1／R2 與 QA PASS comment 已重複驗證的記錄，唯讀巡查不碰模擬器／容器）；merge-review R2 記錄的 R2-I1（`FAKE_SIM_STATE=Booted` ＋ hold 失敗這一角自測矩陣缺一格）屬測試覆蓋缺口而非死碼，已由 review 記錄、不在本次死碼巡檢範圍內重複列出。

### 2026-09-04T20:58:28.603Z · Chih-Lin Yeh · `4a0af9e5`

**2026-09-05 04:58 orchestrator — 收尾儀式 → Done**

- sweeper `79ac8bfe` 0 finding；worktree／分支已清；LS-96 `e1d6d282`／`28f3b345` 已銷除。
- **Lesson learning**：(1) 首次實戰（LS-174 delta QA）三情境一次過、30 張截圖＋Storage log，QA 不再受 mobile-mcp 前景重設影響——目標達成；(2) `supabase-lock.sh --hold` 已持有改 exit 3 讓呼叫端可判碼而非 grep 訊息——同型「訊息比對」在其他腳本仍可能存在，留意；(3) 自測矩陣缺「已 Booted＋hold 失敗」一角 → LS-96 `5e989fe0`；(4) R1 建議的 LS-169 flaky chore 其實已在 LS-174 修掉——reviewer 沿用前輪建議前要 grep 現況（LS-178 重複票）；(5) push gate 在低記憶體被系統殺留下 simulator-lock 死鎖、重跑自動回收——回收機制有效，但長時間無輸出時先 `ps` 看 xcodebuild 是否活著。

