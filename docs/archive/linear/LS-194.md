# LS-194 設計 chore：使用者 0905 核可意見兩項——LS-152 `01 設定` 家庭區塊列文字垂直置中＋LS-163 `P1 帳號密碼登入` AX3 大字級跑版修正；修完 VR 聚焦複驗一輪＋收據即通過（不再送核可）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:S, lane:design |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M6 合規與帳號 |
| 估點 | — |
| 建立 | 2026-09-05T03:52:19.386Z |
| 開始 | 2026-09-05T06:41:50.738Z |
| 完成 | 2026-09-05T08:20:42.528Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-194/設計-chore使用者-0905-核可意見兩項-ls-152-01-設定-家庭區塊列文字垂直置中ls-163-p1-帳號密碼登入-ax3 |
| 關係 | related→LS-198, related→LS-197, blocks→LS-201, related→LS-180, related→LS-165, related→LS-142, related→LS-188, related→LS-185, related→LS-164, related→LS-163, related→LS-152, related←LS-234, related←LS-207, related←LS-199, related←LS-202, related←LS-96, blocks←LS-177 |

## 描述

## 來源

使用者 2026-09-05 核可頁回饋：(1)「[LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表) 01設定中的家庭版面，文字要放在行的中間」（列文字在列高內垂直置中）；(2)「[LS-163](https://linear.app/little-sprout-app/issue/LS-163/設計歡迎頁以帳號密碼登入小字連結三顆登入鈕下方非大按鈕帳號密碼登入畫面審核帳號用方案-b-ui-designervisual) P1 帳號密碼登入中大字級會跑版」。兩票其餘全部核可，使用者裁決「修掉這個之後，不用再問我直接通過」／「修正後直接過關」。.pen 單檔序列化：**排在** [LS-177](https://linear.app/little-sprout-app/issue/LS-177/設計留言愛心互動列與留言-sheetls-22-畫面群推播通知權限提示與設定頁推播通知列-ui-designervisual) **設計 PR 併入 development 之後**（blockedBy [LS-177](https://linear.app/little-sprout-app/issue/LS-177/設計留言愛心互動列與留言-sheetls-22-畫面群推播通知權限提示與設定頁推播通知列-ui-designervisual)）。實作票 [LS-188](https://linear.app/little-sprout-app/issue/LS-188/taskls-152-ios-設定頁-root個人家庭內容與安全法律帳號五區儲存空間頁0909b-已滿依-ls-152)（設定頁）與 [LS-164](https://linear.app/little-sprout-app/issue/LS-164/taskls-17-ios-帳號密碼登入審核帳號用方案)（密碼登入）已依設計意圖先行，不等本票。

## 範圍

1. [LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表) `01 設定`（`t5wI4`）＋深色（`Rx5mP`）＋AX3（`z9tytH`）＋iPad（`B2DckT`）：家庭區塊每列（家庭名稱、成員列、邀請家人等）文字群在列高內垂直置中（多行副標以整組置中）；其他區塊若同型一併對齊；不改列高與間距 token。
2. [LS-163](https://linear.app/little-sprout-app/issue/LS-163/設計歡迎頁以帳號密碼登入小字連結三顆登入鈕下方非大按鈕帳號密碼登入畫面審核帳號用方案-b-ui-designervisual) `P1 帳號密碼登入` AX3 板（`O1XC2Z` 與 AX3 錯誤態 `LkrrS`／`l4ShP` 等，以核可頁 index 為準）：修正大字級跑版（欄位／登入鈕／錯誤四層／求助行不重疊、不溢出、不被裁切），沿 [LS-163](https://linear.app/little-sprout-app/issue/LS-163/設計歡迎頁以帳號密碼登入小字連結三顆登入鈕下方非大按鈕帳號密碼登入畫面審核帳號用方案-b-ui-designervisual) R2 裁定「錯誤態保留求助行」；必要時調整 AX3 板高度而非縮字。
3. 收據 `design/evidence/LS-<本票>-r1-overflow.json`（六支＋`scan_scope`，[LS-185](https://linear.app/little-sprout-app/issue/LS-185/harnessoverflow-scanjs-第六支-board-clip後代-aabb-超出有-clip-的-root-frame) 起）；Notes 板沿革標記（引用使用者意見日期）；他票 root 不變（python 複算）。
4. visual-reviewer **聚焦複驗一輪**（只驗這兩項＋六支收據＋截圖 AX3），APPROVE 即併；orchestrator 更新 [LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表)／[LS-163](https://linear.app/little-sprout-app/issue/LS-163/設計歡迎頁以帳號密碼登入小字連結三顆登入鈕下方非大按鈕帳號密碼登入畫面審核帳號用方案-b-ui-designervisual) 核可頁對應板截圖存檔，不再送使用者。

## 不做

其他板的任何調整；新板。

## 驗收

- [ ] 兩項修正各附截圖（前後對照）＋量法；VR 聚焦輪 APPROVE；收據六支綠；CI 綠
- [ ] merge-reviewer 通過；併入 development 後 [LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表)／[LS-163](https://linear.app/little-sprout-app/issue/LS-163/設計歡迎頁以帳號密碼登入小字連結三顆登入鈕下方非大按鈕帳號密碼登入畫面審核帳號用方案-b-ui-designervisual) → Done

## 0905 13:1x 追加範圍（3）

[LS-165](https://linear.app/little-sprout-app/issue/LS-165/taskls-20-ios-相簿-tab-首頁相簿卡片列表扇影厚度分級空狀態新增相簿依-ls-142-稿實作含深色ax3ipad-兩欄) 票文範圍 5（開工前一次收掉的純文字／fill 項）依裁定延後至本票：[LS-142](https://linear.app/little-sprout-app/issue/LS-142/設計相簿頁相簿詳情上傳佇列ls-20-畫面群-ui-designervisual-reviewer-3-輪) VR R6 留檔 MN-N4／N5／N6／N7（`X9PfG` 生日派對共圖依使用者裁決）、merge-review R2 N6（Notes `e2b0836`→`2e0b836`）——一次 execute，SUMMARY 應與 [LS-142](https://linear.app/little-sprout-app/issue/LS-142/設計相簿頁相簿詳情上傳佇列ls-20-畫面群-ui-designervisual-reviewer-3-輪) r7 逐位相同。收據同本票 r1。

## 0905 13:4x 追加範圍（4）

[LS-165](https://linear.app/little-sprout-app/issue/LS-165/taskls-20-ios-相簿-tab-首頁相簿卡片列表扇影厚度分級空狀態新增相簿依-ls-142-稿實作含深色ax3ipad-兩欄) merge-review R1 `931f074e` N2：AX3 板 `IjWOp` 署名漏了「·」分隔，而 Notes `epDnW` 要求有——**實作對、稿面錯**，本票補稿（只改該板署名文字）；QA 對 [LS-165](https://linear.app/little-sprout-app/issue/LS-165/taskls-20-ios-相簿-tab-首頁相簿卡片列表扇影厚度分級空狀態新增相簿依-ls-142-稿實作含深色ax3ipad-兩欄) 稿時以 Notes 為準。

## 留言（8）

### 2026-09-05T07:29:01.839Z · Chih-Lin Yeh · `19c463ab`

## ui-designer handoff — LS-194 r1（設計完成，待 visual-reviewer 聚焦複驗一輪）

### skill 影響的取捨
- **little-sprout-brand**：載入成功。十條中影響本輪決策的：#5「間距只有一套節奏」→ LS-152 家庭列修正時嚴守「不改列高與間距 token」（bn1eA 鎖定的 56/122 是既有 fit_content 計算值的字面複製，不是新引入的間距數字，$sp-tight/$sp-group/$ctl-pad-tap/$inset-card 四個 token 值本身完全未動）；#4「角托三段規則」與角托 corner-out 5pt → 修正 IjWOp 角托 y 座標時嚴格套用 `paper.h - cornerH + corner-out(5)` 公式，不是憑感覺挪位置。
- **frontend-design:frontend-design**：**未載入**——`Skill` 工具回報 `Unknown skill: frontend-design:frontend-design`，本環境的可用 skill 清單中沒有這個名稱（只有 `little-sprout-brand` 等專案/外掛 skill）。已照常作業，但這條規則要求的「載入失敗不得靜默略過」在此明說：本輪所有排版取捨僅以 little-sprout-brand 十條與既有稿面既定慣例（如 Password Field 已有的 height:140 pattern）為依據，未套用 frontend-design 的通用方法論檢查清單。

### 完成範圍（對照票文四組）

**1｜LS-152 家庭區塊列文字垂直置中**（`t5wI4` 亮 / `Rx5mP` 深色 / `z9tytH` AX3；`B2DckT` iPad 經檢查為 NavigationSplitView，Detail Pane 預設顯示「個人」而非「家庭」列表，這個缺陷在該板不存在，**未改動**）：
  - 根因：cmp/Settings Row 的 Value 文字節點 `n1hzsr` 在「邀請家人／退出家庭／封鎖名單／使用條款／隱私權政策／登出／刪除帳號」7 個無真實副標的列上，預設沒有 override（保留元件預設的隱藏空白行），導致單行 Label 因為要跟這條看不見的空白行一起置中而視覺偏上，未與 icon 對齊。
  - 修法：這 7 列（每板各 7 個實例，共 21 個）逐一 `Update(id+"/n1hzsr", {enabled:false})` 停用空白行，`Update(id+"/bn1eA", {height, justifyContent:"center"})` 把 TextStack 鎖定在**原本 fit_content 算出的同一個高度**（亮/深色＝56、AX3＝122，字面數字，不是新 token）並置中——單行 Label 因此正好落在與 icon 相同的置中線上。有真實副標的「家庭成員／檢舉紀錄／儲存空間」3 列本來就是 enabled:true 的雙行內容、置中天生正確，**完全未動**。
  - 量法：修前 icon 中心 y=28（相對 Left 容器 56 高），Label 中心 y=12.5（偏上 15.5pt）；修後 Label 隨 justifyContent:center 落在 y=28，與 icon 對齊。前後截圖見 `.claude/evidence/LS-194/r1/t5wI4.png`（board_clip 修正後版本）。

**2｜LS-163 P1 帳號密碼登入 AX3 跑版**：先核對現況，**票文原始節點 id 與實際不符**——`O1XC2Z` 其實是非 AX3、852 高的預設尺寸板（LS-163 R2 MJ-4「保留求助行」的目標板，已在 R3 驗過無誤，本輪未動）；真正的 AX3 板是 `vgvFY`（預設態，1300 高）與 `LkrrS`（錯誤態，1474 高）。
  - 根因：Email Field 的 Field Box（`v6YiYR`）兩板都沿用元件預設 `height:60`，沒有跟進 Password Field 已有的 `height:140` override，導致 AX3 40pt 下 email placeholder 換行成 2 行後，文字被裁切在只有 60 高的框內（`ctx.problems: partially clipped` 實測命中）。
  - 修法：Email Field Box 加 `height:140`（比照 Password Field 既有慣例）；因內容多出 80pt，`Body` 用 `justifyContent:space_between` 定位 Footer 導致底部按鈕與內容重疊，同步把板高 +80（vgvFY 1300→1380、LkrrS 1474→1554）。LS-163 R2 MJ-4「錯誤行＋求助行同時顯示、8pt 間距」結構完全保留、未動。iPad（`l4ShP`，非 AX3、寬度足夠不換行）掃描確認 `ctx.problems` 乾淨，未改動。
  - 量法：修前 Field Value `y=-28, h=116`（partially clipped，框僅 60 高）；修後框高 140、文字 `y=12, h=116`，(140-116)/2=12 置中無裁切。截圖 `.claude/evidence/LS-194/r1/vgvFY.png`、`LkrrS.png`。

**3｜LS-142 留檔項**：核對 Linear 歷史後確認：MN-N4（`FyV5I`）與 MN-N5（`oYEi0`）**已在 LS-142 R7 完成**，本輪未重做（票文列出這四項是沿用 LS-165 建票時抄的 R6 清單，需甄別）。MN-N6（`puHZ5` 封面語意）與 MN-N7（`X9PfG` 生日派對共圖）依 LS-142 最終核可 comment `dc9da6f3`「依稿面定案」——**不換圖、不改封面語意**，已在 Notes 補一行沿革記錄裁決，未做任何幾何或圖片變更。merge-review R2 N6：Notes 節點 `zGp4y`／`EBlnw` 內 sha 錯字 `e2b0836`→`2e0b836` 已訂正（各一處，前兩碼順序顛倒）。

**4｜IjWOp AX3 署名補「·」**（LS-165 merge-review R1 `931f074e` N2：「實作對、稿面錯」）：`ONEs5`／`i3jpIq`／`pNPuF` 三張卡的 `wQVzs` 署名文字，依 Notes `epDnW`「姓名與年齡之間的『 · 』刻意維持一般可斷空白（U+0020）」規則補回「·」（U+00B7，前後一般空白），三處都只加分隔號本身，既有的 NBSP／WORD JOINER 用法未動。**副作用與修正**：加「·」使文字略長，換行從 1 行變 2 行（ONEs5/i3jpIq）或既有 2 行內容各多繞一行，Photo Print 容器透過 flex 自動增高 58pt，但 Corner BL/BR 是絕對定位、未隨之同步，造成 `corner_anchor` 掃描 mismatch=6（BLOCKER）。已將三卡 Corner BL/BR 的 y 值對齊新高度（ONEs5/pNPuF 424→482、i3jpIq 366→424，套用 `corner-out` 公式 `paper.h-26+5`），複驗 mismatch=0。另發現 Notes epDnW 裡「INFO-N2（R7）：AX3 省略『 · 』」一段與這次訂正矛盾——已在 kHDk4 新沿革註記明說這句視為已被推翻，避免下一輪誤判成不一致又改回去。

**額外發現並修正（非票面原始範圍，但為了本輪觸碰的板拿到乾淨收據而必須處理）**：`t5wI4`／`Rx5mP`／`z9tytH`／`IjWOp` 四板的 `Body` 容器都缺 `clip:true`，導致可捲動內容（設定頁「內容與安全／法律／帳號」區塊、相簿卡列表下半部）以 root 邊界計算被 `board_clip` 掃描判定溢出（92 筆 in-scope，BLOCKER）——這是文件既有的「未加裁切視窗的可捲動長欄」架構缺陷（`board_clip` 掃描是 LS-185 才新增，此前這幾板從未被這支掃描檢查過，並非本輪引入）。依 `overflow-scan.js` 檔頭記載的正確作法「板尺寸的 clip 視窗＋內層長欄」，為 4 個 `Body` 容器補上 `clip:true`（純屬性修正，未動任何座標／尺寸），`board_clip.flagged` 收斂為 0。截圖確認加 clip 後可視內容（Tab Bar、「+新增相簿」等）都正常顯示，沒有意外裁掉不該裁的東西。

### Notes 沿革（三張票，各補一行，見上方各段引用）
- LS-152 Notes `b3nzDp`：新增 `i` 節點記錄本輪家庭列置中修法與量測。
- LS-163 Notes `N1aeiU`：新增 `N R4 修訂重點（LS-194，使用者 2026-09-05 核可意見）` 區塊 + `i` 節點，記錄 Email Field Box 修法與節點 id 澄清（O1XC2Z 非 AX3）。
- LS-142 Notes `kHDk4`：新增 `i` 節點記錄 sha 訂正、MN-N6/N7 依稿面定案裁決、「·」補正與 INFO-N2 段落已被推翻的提醒。
- 死 id：本輪未刪除重建任何節點（皆為 Update，id 不變），Notes 內沒有需要沿革寫法的死 id 引用。`design-notes-check.sh` 本機驗證：Notes 板 8 塊、本 PR 範圍內曾存在而 head 已無的 id＝0、缺失＝0，**通過**。

### 尺寸類（width/height）token 規格值族與硬寫次數
| 規格值 | 對應族 | 本輪硬寫次數 | 說明 |
|---|---|---|---|
| 56 | LS-152 家庭列 TextStack 高（亮/深色，17pt） | 14（t5wI4 7 + Rx5mP 7） | fit_content 既有計算值的字面複製，非新數字 |
| 122 | LS-152 家庭列 TextStack 高（AX3，40pt） | 7（z9tytH） | 同上，AX3 版本 |
| 140 | LS-163 Email Field Box 高（比照既有 Password Field 慣例） | 2（vgvFY、LkrrS） | 與既有 Password Field 的 140 同值，非新規格 |
| 1380 | vgvFY 板高（1300+80） | 1 | 因 Field Box +80 連動 |
| 1554 | LkrrS 板高（1474+80） | 1 | 同上 |
| 482 / 424 | IjWOp 角托 Corner BL/BR y（`paper.h-26+5`） | 6（3 卡 × 2 角） | 角托 corner-out 公式的字面結果，未來若卡片內容又變高需同步重算 |

以上皆為 `Update()` 的字面數字（height 不接受 `$variable`，已知限制），下一輪若相關 token（$fs-body/$fs-note AX3 值、cmp/Settings Row 元件結構）改變，上表是要回頭同步的清單。

### 溢出掃描收據
`design/evidence/LS-194-r1-overflow.json`（commit `df05988`）：
- `SCAN_SCOPE="boards"`，`SCAN_BOARDS=["t5wI4","Rx5mP","z9tytH","vgvFY","LkrrS","IjWOp","kHDk4","b3nzDp","N1aeiU"]`（9 板，即本輪觸碰的全部頂層節點——已用 python 對 origin/development 逐 root byte-diff 驗證：220 個共同頂層節點中僅這 9 個有差異、其餘 211 個 byte-identical，無新增/刪除 root）。
- `total_nodes=9460`、`tree_hash=58cdcd771ef99f38`、`head_sha=fd7a3cd58739377137961b9e798d09153d88ea8a`（本輪倒數第二次落地 commit；receipt 本身另開 commit `df05988`，符合「不能自我指涉」規則）。
- 六支結果：`corner_anchor` containers/points/mismatch=3/24/**0**；`text_occlusion` flagged=**0**；`board_clip` flagged=**0**；`sibling_intersection` flagged=52（28 類，皆為既有角托 corner-out／Fan Ghost 堆疊／Body×Tab Bar 疊圖層母題，逐筆附分類）；`row_overflow` flagged=9（3 類，皆角托 corner-out 既有慣例）；`cross_parent_collision` flagged=2（2 類，Spacer Section×Corner 既有版面慣例）。
- SUMMARY 原文：
  - `SUMMARY-HASH total_nodes=9460 tree_hash=58cdcd771ef99f38`
  - `SUMMARY total_nodes=9460 scanned_nodes=1143 scan_scope=boards sibling_intersection=52 row_overflow=9 cross_parent_collision=2(bleed-only) text_occlusion=0/0 board_clip=0/0 corner_anchor=3/24/0 document=3/24/0 unresolved=0 boards=["t5wI4","Rx5mP","z9tytH","vgvFY","LkrrS","IjWOp","kHDk4","b3nzDp","N1aeiU"] tree_hash=skipped`
- 掃描過程中發現的 corner_anchor mismatch=6 與 board_clip flagged=92（皆源自本輪編輯的副作用，見上方「額外發現並修正」段）已在落地前修正，本收據反映修正後的乾淨狀態；`scripts/gates/design-evidence-check.sh` 與 `scripts/gates/design-notes-check.sh` 本機皆已跑過，**通過**。

### 未決事項
無新的待人核可事項——本票使用者已裁決「修完 VR 聚焦複驗一輪＋收據即通過，不再送核可」。唯一待辦是請 visual-reviewer 對本輪四項修正（含額外發現的 clip:true 修正）做聚焦複驗，重點看：(a) LS-152 三板家庭列 icon/文字置中是否目視正確、其他列未受影響；(b) LS-163 AX3 兩板 email 欄位是否確實不再裁切、按鈕與內容不重疊；(c) IjWOp 三卡角托是否確實貼合新的紙面下緣、無視覺錯位；(d) 三張票 Notes 沿革內容是否清楚。APPROVE 後不需再送我，orchestrator 可直接收尾。

### Pen 路徑
- 開工核對：`get_app_state()` 回傳 `/Users/clyeh/little-sprout/.claude/worktrees/LS-194/design/littlesprout.pen`，與 `git rev-parse --show-toplevel`/design/littlesprout.pen 相符，正確。
- 落地：三次，皆 exit 0——
  1. `pen-land.sh --expect-nodes 9449 --after 1788591648`：通過（LS-152+LS-163 修正）。
  2. `pen-land.sh --expect-nodes 9460 --after 1788592984 --marker "2e0b836"`：mtime 快篩落後 73 秒但 `--marker` 命中 backup 內容且未見於落地檔，內容證明覆蓋 mtime 判斷，通過（LS-142 修正＋Notes 沿革）。
  3. 收據檔 `design/evidence/LS-194-r1-overflow.json` 為純文字新增，未經過 pen-land.sh（不是 .pen 檔）。
- 收工：Pen 仍停在本票檔 `/Users/clyeh/little-sprout/.claude/worktrees/LS-194/design/littlesprout.pen`，未切回主 checkout，未跑任何 `pen-open.sh`（LS-180）。

### commits
- `2a26850` design: LS-194 LS-152 家庭區塊列文字垂直置中 + LS-163 AX3 欄位跑版修正
- `fd7a3cd` design: LS-194 LS-142 相簿卡署名補「·」＋sha錯字訂正＋corner_anchor/board_clip收尾＋Notes沿革
- `df05988` design: LS-194 r1 溢出掃描收據

分支 `feature/LS-194-design-chore-approval-fixes`，已 push 到 origin，可乾淨合併 `origin/development`（`73b6c5e`）。

### 2026-09-05T07:47:47.037Z · Chih-Lin Yeh · `eadb66e7`

## visual-reviewer VR R1 聚焦複驗 — **ITERATE**（2026-09-05 15:45 CST）

裁決依 orchestrator 派工規則：六項中 **1／2／4／5／6 全部以量測成立**，**第 3 項（`IjWOp` 署名補「·」＝與 Notes `epDnW` 一致）不成立**，故只列該項退修。**性質：可機械複驗**（codepoint 比對＋折行實測，非審美爭議）。

- **little-sprout-brand skill**：載入成功。本輪判準用到 #4（角托 corner-out 5pt）、#5（間距節奏／列高不動）、#6-#7（AX3 壓力板、長輩可讀）、#10（狀態切換不搬版）。
- **Pen 路徑**：`get_app_state()` = `/Users/clyeh/little-sprout/.claude/worktrees/LS-194/design/littlesprout.pen`，與指定 worktree 相符。**新鮮度由 agent 自行複算證明**：依 orchestrator「不得跑 `pen-open.sh`／`--force-reload`／切檔」的硬規定，未跑 `pen-read.sh`（它 exec 的是 `pen-open.sh --force-reload`），改走 LS-180 exit-3 等價路徑——Pencil 端 `SCAN_HASH_ONLY` 回讀 `tree_hash=58cdcd771ef99f38 / total_nodes=9460`，與磁碟 `design_tree_hash.py` 逐字相同。無「⚠ Pencil MCP 需重連」情形。

### 重掃比對（正典腳本 `scripts/design/overflow-scan.js`，同收據設定兩趟唯讀）
```
SUMMARY total_nodes=9460 scanned_nodes=1143 scan_scope=boards sibling_intersection=52 row_overflow=9
cross_parent_collision=2(bleed-only) text_occlusion=0/0 board_clip=0/0 corner_anchor=3/24/0
document=3/24/0 unresolved=0 boards=["t5wI4","Rx5mP","z9tytH","vgvFY","LkrrS","IjWOp","kHDk4","b3nzDp","N1aeiU"]
```
與 `design/evidence/LS-194-r1-overflow.json` **逐項相同**：三個 gate 項（`corner_anchor.mismatch` / `text_occlusion.flagged` / `board_clip.flagged`）皆 0、`scan_scope=boards`、`tree_hash` 逐字相同、`boards` 9 板；收據 classification 的「同類 N 例」加總＝52／9／2，與重掃 flagged 數一致。
- **獨立抽驗**（不轉貼腳本輸出）：手算 `IjWOp` 三卡 24 個角托斷言。paper `r6cYjT` 分別為 (9447,1072,345×503)／(9447,1599,345×503)／(9447,2126,345×445)，角托 26×26，套 `paper.h−ch+5`：BL/BR y 期望 1554／2081／2550＝實測值，TL/TR y 期望 1067／1594／2121＝實測值，x 期望 9442／9771＝實測值。**24/24 命中，mismatch=0 獨立成立**。
- `design-evidence-check.sh design/littlesprout.pen --ticket LS-194 --base origin/development` ✅
- `design-notes-check.sh design/littlesprout.pen --base origin/development` ✅（Notes 板 8 塊、缺失 0、沿革引用 0）
- **他票 root 不變**：python 對 `origin/development` 逐 root canonical-JSON 比對——220 個頂層節點無新增／刪除，**只有 9 個有差異**（＝`SCAN_BOARDS` 完全一致），**其餘 211 個全部逐位元相同**（不只抽 5 個，是全部 211 個比完）。全稿改動＝34 個既有節點屬性 + 11 個新增 Notes 節點，無其他夾帶。

---

### ❌ BL-1（BLOCKER，本輪唯一退修項）｜`IjWOp` 三卡署名年齡片語用半形空白，AX3 已折出「3 ／ 個月」孤字，與 Notes `epDnW` 不一致

**violates**：Notes `epDnW`「年齡片語（如「2 歲 3 個月」）內部空白**一律用 NBSP（U+00A0）**連接、「個」「月」之間再插入 WORD JOINER（U+2060），防止 Dynamic Type／AX3 折行把年齡單位拆成孤字（BL-B R3 修復）」；brand 十條 #6-#7（AX3 壓力板／長輩可讀）。

**證據（磁碟 JSON codepoint dump）**：
| 節點 | 年齡片語內部空白 |
|---|---|
| `cmp/Card Album` 元件預設 `wQVzs` | 小安`[SP]`·`[SP]`2`[NBSP]`歲`[NBSP]`3`[NBSP]`個`[WJ]`月 ✅ |
| LS-142 / 14 相簿（`PjgtQ`／`WC9pB`／`H51J8u`）、深色、iPad、Stress 板 | 全部 `[NBSP]` ✅ |
| **`IjWOp` 的 `ONEs5`／`pNPuF`／`i3jpIq`（本輪改寫的 override）** | 小安`[SP]`·`[SP]`2`[SP]`歲`[SP]`3`[SP]`個`[WJ]`月 ❌ |

**後果已實際發生**：加「·」使該行從 1 行變 2 行（卡高 445→503／366→424 的來源），而因為分隔是可斷的 `[SP]`，AX3 40pt 下折行點落在「3」之後——截圖 `.claude/evidence/LS-194/vr1/ONEs5.png` 顯示署名讀作 **「小安 · 2 歲 3」／「個月」**，數字與單位被拆開，正是 `epDnW` 該條規則指名要防的失效。`wQVzs` h=116（2 行 × 58）可機械複驗。

**為什麼沒被發現**：`[SP]` 與 `[NBSP]` 在 `Get`／`Print`／截圖上是**視覺全等**的字串（本輪 `Print` 兩者都印成 `"小安 · 2 歲 3 個⁠月"`），只有 codepoint dump 或折行點會顯形。handoff 寫「既有的 NBSP／WORD JOINER 用法未動」與事實不符——這三個 override 原本就**只有 WJ、沒有 NBSP**（base 是 `2[SP]歲[SP]3[SP]個[WJ]月`），是既有潛伏偏差，被本輪的換行引爆。

**改法（精確指令，零幾何風險）**：把 `ONEs5/wQVzs`、`pNPuF/wQVzs`、`i3jpIq/wQVzs` 的年齡片語內部空白由 U+0020 改成 U+00A0，其餘（姓名與「·」之間刻意保留的 U+0020、WJ、`\n` 多寶貝分行）一律不動：
- `ONEs5`／`i3jpIq`：`小安 · 2 歲 3 個⁠月`
- `pNPuF`：`小安 · 2 歲 3 個⁠月\n小明 · 8 個⁠月`

預期折行改為「小安 ·」／「2 歲 3 個月」，行數不變（116／174／116），故 Photo Print 高度應維持 503／503／445、角托 y 維持 482／482／424。**但不得假設**——改完請重量 `wQVzs` 與 `r6cYjT` 高度，若行數變動就用 `paper.h − 26 + 5` 重算四顆角托 y，並重跑六支確認 `corner_anchor.mismatch=0`。

### ⚠️ MN-1（MINOR，與 BL-1 同一趟改掉）｜`epDnW` 的 INFO-N2 仍逐字寫著「AX3 省略「 · 」」

`epDnW`（`kHDk4` 第 17 塊）INFO-N2（R7）原文仍是「AX3 署名文法與上述公式故意不同——AX3 **省略「 · 」**分隔號（ONEs5/i3jpIq＝「小安 2 歲 3 個⁠月」）……避免下一輪被誤判成不一致而『修正』回帶「 ·」的版本」，且引用的是三卡**舊內容**。推翻它的沿革寫在 `qZpeo`（`kHDk4` 第 **59** 塊，全板共 60 塊）——距離 42 塊，讀 `epDnW` 這段的人（ios-dev／QA，票文明訂「QA 對 LS-165 稿時以 Notes 為準」）看不到。`epDnW` 自己就有就地覆寫的前例（「【R3 訂正，取代 R1 段】」），請沿用同一慣例：在 INFO-N2 段尾就地加一句「【LS-194 訂正】本段『AX3 省略「 · 」』已被推翻，AX3 署名與公式一致帶「 · 」，且年齡片語同樣用 NBSP」，`qZpeo` 沿革保留不動。

---

### ✅ 已核銷成立的五項（附量法，之後不必重驗）

**1｜LS-152 家庭區塊列文字垂直置中 — 成立**。`t5wI4`／`Rx5mP`／`z9tytH` 各 10 列全量測（含非家庭區塊的同型列）：**文字群中心 − 列中心 = 0.00pt、icon 中心 − 列中心 = 0.00pt**（判準 ≤1pt），雙行列（家庭成員／檢舉紀錄／儲存空間）以整組置中亦為 0.00。列高 75（亮／深色）／141（AX3）在**已改與未改的列上完全相同**＝列高未變。JSON 逐屬性 diff 證明只動了 instance `descendants` 裡的 `bn1eA{height, justifyContent:center}` 與 `n1hzsr{enabled:false}`，`$sp-tight`／`$sp-group`／`$ctl-pad-tap`／`$inset-card` 與任何 padding／gap／列高屬性**一個都沒動**。`B2DckT` 覆核：該板為 NavigationSplitView，Sidebar 是 5 個 44pt Nav Item、Detail Pane 是「個人」的 Avatar／Name Field，**全板 0 個 `Row ·` 節點、0 個 cmp/Settings Row 實例**——designer 的「此問題在該板不存在」屬實。截圖 `t5wI4.png`／`Rx5mP.png`／`z9tytH.png`。

**2｜LS-163 AX3 跑版 — 成立**。id 更正屬實：`O1XC2Z` = 「LS-163 / P1c 帳號密碼登入 · 帳號或密碼錯誤」393×**852**（非 AX3）；真 AX3 板為 `vgvFY`(1380)／`LkrrS`(1554)。Email Field Box `v6YiYR` 60→140，Field Value h=116 在 140 框內上下各 12pt，**兩板 `ctx.problems` 全樹 = 0**，六支對這兩板 0 flagged。**底部無空帶**：Top→Footer 間距修前修後**都是 48pt**（vgvFY 修前 1133−1085、修後 1213−1165；LkrrS 修前 1307−1259、修後 1387−1339）——板高 +80 恰好被內容 +80 吸收，Footer 下緣與 Body 下緣齊、再接 34pt Home Indicator。R2「錯誤態保留求助行」結構在 `LkrrS` 仍在：Error Line 927..1101＋Help Line 1107..1339 同時 enabled。截圖 `vgvFY.png`／`LkrrS.png`。

**4｜LS-142 留檔項 — 成立**。sha 錯字訂正實測：`zGp4y` 與 `EBlnw` 各一處 `e2b0836`→`2e0b836`，且該兩節點**除此之外無其他字元變動**。MN-N4(`FyV5I`)／N5(`oYEi0`)：全稿 diff 中不存在，確認未重做。MN-N6／N7：`puHZ5`／`X9PfG` 皆在 211 個逐位元相同的 root 內，確認未動圖未改語意，僅在 `qZpeo` 留痕引用 `dc9da6f3`「依稿面定案」。三張票的沿革（`qZpeo`／`Ndshq`／`fiqfu`）內容與稿面事實逐條相符，引用的節點 id 現行都存在。

**5｜4 個 `Body` 補 `clip:true` — 成立，且是正確架構修法**。`Body` 是**固定尺寸的視窗框**（如 t5wI4 Body h=756 vs 內層 Content h=1477），不是長欄本身，符合 `overflow-scan.js` 檔頭「板尺寸的 clip 視窗＋內層長欄」的處方，不是把長欄設 clip 的反例。**「有沒有裁掉原本看得到的東西」逐葉節點驗過**：對每板列出「矩形仍在 board 內、但被新的 Body 邊界裁到」的可見葉節點，四板**各只有 1 個**——`t5wI4`/`Rx5mP`/`z9tytH` 是下一段的 Section Header（「內容與安全」，原本浮在 Tab Bar **下方**的 Home Indicator 帶裡）、`IjWOp` 是第三張卡 Photo Wrap 的最後 34pt（同樣落在 Tab Bar 下緣以下）。Home Indicator Area `fill=null` 且無子節點＝透明，所以這些東西修前**確實會露出來**——那是捲動模擬的破圖，不是內容。修後四板的 Body 下緣恰與 Tab Bar 下緣重合，內容自然收在膠囊底下（見 `IjWOp.png` 第三張卡）。其餘 88 筆原 `board_clip` 命中都在板外、本來就看不見。**這是修掉一個既有破圖，不是掩蓋溢出**。

**6｜收據 — 成立**。見上「重掃比對」。

---

### 值得保留（下一輪別改壞）
1. **列置中的修法本身**：`n1hzsr enabled:false` + `bn1eA` 鎖回原 fit_content 高度並置中——保住列高 75／141 的同時讓單行 Label 落回 icon 的中線；56／122 是為了「不改列高」被迫硬寫（Pencil `height` 不吃 `$variable`），已在 handoff 建表登記，這個取捨是對的。
2. **AX3 用加板高而非縮字**（1300→1380／1474→1554），沒有為了塞下去犧牲 40pt——brand #7 的正解。
3. **48pt 的 Top→Footer 呼吸不動**，狀態變更沒有搬版（brand #10）。
4. **四個 `Body` 的 `clip:true`**：結構上宣告了捲動視窗，別回退。
5. `LkrrS` 錯誤行＋求助行並存的四層結構。

### 重做優先序
1. **BL-1**：三個 `wQVzs` 的年齡片語空白換 NBSP → 重量高度 → 必要時重算角托 y。
2. **MN-1**：`epDnW` INFO-N2 段尾就地加訂正句。
3. 重跑六支 + 更新 `design/evidence/LS-194-r1-overflow.json`（或改為 r2 收據，依 orchestrator 慣例），`tree_hash`／`head_sha` 同步。

### informational（不屬本票，建議記入 LS-96 待辦池，本輪不修）
- **i-1｜`cmp/Profile Print` 角托 corner-out 實測 3.8pt、且第四支結構上永遠看不到它**：`t5wI4` 內 `lE7FJ/lOPO7` Print 60×60 @(3381,22429)，`Mount TL` 16.4×16.4 @(3377.2,22425.2)、`Mount BR` @(3428.4,22476.4)；套 corner-out 5 期望 x=3376／3429.6，實際偏 1.2pt（＝corner-out 3.8）。因為節點名是 `Mount TL/BR` 而非 `Corner TL/BR`，`overflow-scan.js` 的 `CORNER_RE` 不命中，`corner_anchor` 從來沒檢查過這一族——**gate 盲區**。該子樹與 `origin/development` 逐位元相同，非本票引入。
- **i-2｜收據各支缺 per-scan `scope` 欄位**（頂層 `scan_scope="boards"` 正確、gate 通過）。補上後「各支的 `document_*` 是限縮值」在收據自身就讀得出來，不必回頭看 `scan_note` 文字自述。
- **i-3｜`Rx5mP` 深色板「加入於 2024/3」壓印小字**目視對比偏低（坐在深色底而非紙上）。屬 LS-152 既有材料、本輪未動，僅記錄。

產出：`/Users/clyeh/little-sprout/.claude/worktrees/LS-194/.claude/evidence/LS-194/vr1/`（`vgvFY.png`／`LkrrS.png`／`t5wI4.png`／`Rx5mP.png`／`z9tytH.png`／`IjWOp.png`／`ONEs5.png`；已 gitignore，未 add）。

### 2026-09-05T07:57:19.435Z · Chih-Lin Yeh · `b33fa2c0`

## ui-designer handoff — LS-194 R2（回應 VR eadb66e7 ITERATE）

同一 worktree／同一分支續作，Pen 全程未切檔、未 kill（LS-180）。

### BL-1 修正：三處署名年齡片語空白改回 NBSP
`ONEs5`／`i3jpIq`／`pNPuF` 的 `wQVzs` override：年齡片語（`2 歲 3 個月`／`8 個月`）內部空白由 R1 誤用的一般空白（U+0020）改回 Notes `epDnW` 規定的 NBSP（U+00A0）；姓名與「·」之間的一般空白、「個」「月」之間的 WORD JOINER（U+2060）、多寶貝的 `\n` 分隔皆未動——只換了年齡片語內部那幾個空白字元。

- 修前折行（R1 遺留問題）：一般空白讓 AX3 40pt 在數字後方折行，讀成「小安 · 2 歲 3／個月」孤字。
- 修後折行（screenshot 驗證）：`ONEs5`＝「小安 ·」／「2 歲 3 個月」；`pNPuF`＝「小安 ·」／「2 歲 3 個月」／「小明 · 8 個月」；`i3jpIq`＝「小安 ·」／「2 歲 3 個月」——與 VR 預期的「小安 ·／2 歲 3 個月」一致。
- 量法：`wQVzs` 高度實測 `ONEs5=116`、`pNPuF=174`、`i3jpIq=116`，與 R1 完全相同（**行數未變**），角托 Corner BL/BR y 因此**不需重算**，維持 R1 的 `482／482／424`——複驗 `corner_anchor.mismatch=0`。
- 截圖：`.claude/evidence/LS-194/r2/IjWOp.png`。

### MN-1 修正：epDnW 就地訂正
在 Notes `epDnW`（`kHDk4` 板）文字末尾就地追加一句訂正，明說原本「INFO-N2（R7）：AX3 省略『 · 』分隔號」已被 LS-165 merge-review R1 `931f074e` N2 推翻，指向 `kHDk4` 第 59–60 塊（R5 修訂重點段＋LS-194 沿革項）查最新規則，避免下一輪誤判成不一致又改回無「·」版本。沿革寫法（不刪舊句、就地加訂正），非死 id 引用。

### 收工驗證
- `pen-land.sh --expect-nodes 9460 --after <t> --marker "LS-194 R2 訂正"`：exit 0（純文字 Update，節點數不變）。
- commit `554190d`（R2 修正）、`43616ff`（r2 收據），已 push。
- `design/evidence/LS-194-r2-overflow.json`：`SCAN_SCOPE=boards`，同 9 板，六支結果與 r1 **逐位元相同**（`sibling_intersection=52／row_overflow=9／cross_parent_collision=2` 皆既有母題重疊已分類；`corner_anchor` containers/points/mismatch=3/24/**0**；`text_occlusion.flagged=0`；`board_clip.flagged=0`）——證實 NBSP 修正未產生任何新的版面副作用。`head_sha=554190d`（R2 落地 commit）、`tree_hash=205b73bb00c7736b`。
- `scripts/gates/design-evidence-check.sh`：r1／r2 兩份收據皆通過，r2 正確判定為「本輪最新」。
- `scripts/gates/design-notes-check.sh`：通過（Notes 8 塊、無死 id、無缺失）。
- python byte-diff 複核：與 `origin/development` 相比僅 9 個觸碰板有差異（與 r1 相同），其餘 211 個頂層節點 byte-identical，本輪未越界碰其他板。

### 其餘三則 informational（Mount TL/BR 命名、收據缺 per-scan scope、深色小字對比）
依你的指示不動，交由你入池處理。

### Pen 路徑
`get_app_state()` 確認仍在 `/Users/clyeh/little-sprout/.claude/worktrees/LS-194/design/littlesprout.pen`，未切檔、未跑 pen-open.sh。

### commits（本輪新增，接續 R1 五筆）
- `554190d` design: LS-194 R2 修正署名年齡片語 NBSP + epDnW INFO-N2 訂正（VR eadb66e7 BL-1/MN-1）
- `43616ff` design: LS-194 r2 溢出掃描收據

分支 `feature/LS-194-design-chore-approval-fixes`，已 push，與 `origin/development`（`588043e`）可乾淨合併。請 visual-reviewer 針對 BL-1/MN-1 兩項做聚焦複驗。

### 2026-09-05T08:02:14.548Z · Chih-Lin Yeh · `866f92c6`

## visual-reviewer VR R2 聚焦複驗 — **APPROVE**（2026-09-05 16:01 CST）

R1（comment `eadb66e7`）的 **BL-1 與 MN-1 兩項皆以量測成立**，且無新增副作用。R1 已核銷的五項（列置中／AX3 跑版／LS-142 留檔／`clip:true`／收據）在本輪 diff 中完全未被觸碰，不重驗。

- **little-sprout-brand skill**：載入成功。本輪判準：`epDnW` 的 NBSP／WJ 排版規則（brand #6-#7 長輩可讀、AX3 壓力板）、#4 角托 corner-out 5pt。
- **Pen 路徑**：`get_app_state()` = `/Users/clyeh/little-sprout/.claude/worktrees/LS-194/design/littlesprout.pen`。依規定未跑 `pen-open`／`pen-read`／`--force-reload`，沿用 R1 的 hash 等價證明：Pencil 端 `SCAN_HASH_ONLY` 回讀 **`tree_hash=205b73bb00c7736b / total_nodes=9460`**，與磁碟 `design_tree_hash.py` 及 r2 收據逐字相同。無「⚠ Pencil MCP 需重連」。

### ✅ BL-1 解除｜三處署名年齡片語已改 NBSP，AX3 折行點正確

**codepoint 逐字比對（磁碟 JSON，對 R1 head `df05988`）**——全稿只有 **4 個節點**變動、0 新增 0 刪除，其中三處署名的差異**僅限年齡片語內部空白**：

| 節點 | R1（前） | R2（後） |
|---|---|---|
| `ONEs5/wQVzs` | 小安`[SP]`·`[SP]`2`[SP]`歲`[SP]`3`[SP]`個`[WJ]`月 | 小安`[SP]`·`[SP]`2`[NBSP]`歲`[NBSP]`3`[NBSP]`個`[WJ]`月 |
| `i3jpIq/wQVzs` | 同上 | 同上 |
| `pNPuF/wQVzs` | 小安`[SP]`·`[SP]`2`[SP]`歲`[SP]`3`[SP]`個`[WJ]`月`\n`小明`[SP]`·`[SP]`8`[SP]`個`[WJ]`月 | 小安`[SP]`·`[SP]`2`[NBSP]`歲`[NBSP]`3`[NBSP]`個`[WJ]`月`\n`小明`[SP]`·`[SP]`8`[NBSP]`個`[WJ]`月 |

**逐項核對指令要求的「只有年齡片語內換成 NBSP」**：姓名與「·」之間**仍是 U+0020**（`epDnW`「刻意維持一般可斷空白，避免『小』單字孤兒」）✅；`[WJ]`（U+2060）位置未動 ✅；`pNPuF` 的多寶貝 `\n` 未動 ✅；第二人「8 個月」的空白也一併 NBSP 化（與第一人一致）✅。結果與 `cmp/Card Album` 元件預設、LS-142 相簿全族（亮／深色／iPad／Stress）**完全同型**。

**AX3 40pt 實際折行位置（截圖實測，非推論）**：
- `ONEs5`／`i3jpIq`：**「小安 ·」／「2 歲 3 個月」** — 年齡片語完整不斷，R1 的「3 ／ 個月」孤字消失。
- `pNPuF`：**「小安 ·」／「2 歲 3 個月」／「小明 · 8 個月」** — 一行一人的 MJ-6 規則維持，第二人整句一行。
- 截圖 `.claude/evidence/LS-194/vr2/ONEs5.png`／`pNPuF.png`／`IjWOp.png`。

**幾何零位移（獨立重算，不轉貼收據）**：Signature h = **116／174／116**（＝2／3／2 行 × 58），Imprint Row 296／296／238，Photo Print h **503／503／445** — 與 R1 完全相同。角托 24 個斷言逐顆手算（`paper.x−5`／`paper.x+w−cw+5`／`paper.y−5`／`paper.y+h−ch+5`，cw/ch 實測 26/26）：
```
ONEs5   TL(9442,1067) TR(9771,1067) BL(9442,1554) BR(9771,1554)  4/4 OK
pNPuF   TL(9442,1594) TR(9771,1594) BL(9442,2081) BR(9771,2081)  4/4 OK
i3jpIq  TL(9442,2121) TR(9771,2121) BL(9442,2550) BR(9771,2550)  4/4 OK
```
**24/24 期望＝實測，mismatch=0 獨立成立**，角托 y 維持 482／482／424（相對值）。

### ✅ MN-1 解除｜`epDnW` 就地訂正，且交叉引用查證屬實

訂正句**加在 `epDnW` 節點內、INFO-N2 段落尾**（不是另開節點），沿用該節點自己的「【R3 訂正，取代 R1 段】」慣例——`epDnW` 是 R2 的 4 個變動節點之一，diff 顯示**原文一字未刪、只在段尾追加**，符合沿革寫法（不竄改歷史、只標註推翻）。內容查核：引用 `LS-165 merge-review R1 comment 931f074e N2` 正確、描述 R1 補「·」→ R2 改 NBSP 的因果正確、寫出「一般空白會折在數字後產生『3／個月』孤字，改 NBSP 後折在『 · ／2 歲 3 個月』」與我實測的折行完全一致。

**交叉引用逐筆核對**（我的職責是驗現行 id／位置真的在同段給得出來）：訂正句指向「本板（kHDk4）第 59–60 塊」——實查 `kHDk4` 共 60 個子節點，**#59 = `W1MGF`「N R5 修訂重點（取代前輪已過期描述）」、#60 = `u3LTCg`（stamp: LS-194 沿革項）**，與句子描述逐字相符 ✅。`design-notes-check.sh --base origin/development` ✅（Notes 板 8 塊、缺失 **0**、沿革引用 **0**、死 id 0）。

### 收據與 gate
- 我的重掃 SUMMARY：`total_nodes=9460 scanned_nodes=1143 scan_scope=boards sibling_intersection=52 row_overflow=9 cross_parent_collision=2(bleed-only) text_occlusion=0/0 board_clip=0/0 corner_anchor=3/24/0 document=3/24/0 unresolved=0 boards=[9 板]` — 與 `LS-194-r2-overflow.json` 的 `summary_lines` **逐字相同**。
- `head_sha=554190d`（.pen 落地 commit；收據自己在 `43616ff`，未自我指涉 ✅）、`tree_hash=205b73bb00c7736b` 與磁碟／Pencil 三方同值 ✅、`scan_scope=boards` ✅、三個 gate 項（`corner_anchor.mismatch`／`text_occlusion.flagged`／`board_clip.flagged`）皆 **0** ✅。
- `design-evidence-check.sh --ticket LS-194 --base origin/development` ✅（r1／r2 兩份都過）。
- **r1↔r2 收據六支比對**：`corner_anchor`／`text_occlusion`／`board_clip`／`sibling_intersection`／`row_overflow` 五支 JSON **逐位元相同**；`cross_parent_collision` 的**資料欄位（node_a／node_b／筆數）逐位相同**，只有一句 classification 措辭修訂（`puHZ5/Xldkz/T4FpQ 系列` → `puHZ5/Xldkz 系列`）——這是**訂正而非漂移**（`T4FpQ` 是本板自己的 Spacer 節點，不該列在「其他相簿板」清單裡），我認可。

---

### 殘留 MINOR（本輪不退修，記錄供後續判斷）

- **MN-2｜`ONEs5`／`pNPuF`／`i3jpIq` 的第一行以「·」收尾（行尾孤符）**。AX3 下折行後第一行是「小安 ·」，分隔號落在行尾。這是 `epDnW`「姓名與「·」之間維持一般可斷空白（U+0020）」的必然結果，而該規則本身是**已量測過的兩害相權**（改 NBSP 包住姓名會產生「小」單字孤兒，R3 實測後排除）。目前這個折法是兩者中較好的一個，**不要求改**。若未來想更好，唯一乾淨解是 AX3 專用寫法「小安`\n`2 歲 3 個月」（顯式換行、行尾不留分隔號），但那與 `epDnW` 的 `{暱稱} · {年齡}` 公式衝突、且會重蹈 MJ-6（R5）拿掉的「姓名年齡拆兩行」老路，需另行裁決——建議記入 LS-96 觀察項，不在本票處理。
- **i-1（R1 提出，仍在）｜`cmp/Profile Print` 角托 corner-out 實測 3.8pt，且因命名 `Mount TL/BR` 而非 `Corner TL/BR`，第四支 `corner_anchor` 結構上永遠看不到它**——gate 盲區。非本票引入（該子樹與 `origin/development` 逐位元相同）。建議 LS-96。
- **i-2（R1 提出，仍在）｜收據各支缺 per-scan `scope` 欄位**（r2 亦同；頂層 `scan_scope="boards"` 正確、gate 通過）。補上後「各支 `document_*` 是限縮值」在收據自身可讀，不必回頭看 `scan_note` 自述。建議 LS-96。
- **i-3（R1 提出，仍在）｜`Rx5mP` 深色板「加入於 2024/3」壓印小字目視對比偏低**。屬 LS-152 既有材料，本輪未動。

### 值得保留（沿用 R1 清單，另加一項）
R1 的五項全部維持有效；新增第 6 項：**`epDnW` 的「就地追加訂正句、不刪原文、並指出最新修法在哪幾塊」寫法**——這是本專案 Notes 板該有的沿革範式，之後遇到「舊段落被推翻」一律照這個做。

**判定：APPROVE。** 依使用者裁決（修完 VR 一輪＋收據即通過），本票可直接進 merge-review／併入 development，不再送核可。

產出：`/Users/clyeh/little-sprout/.claude/worktrees/LS-194/.claude/evidence/LS-194/vr2/`（`ONEs5.png`／`pNPuF.png`／`IjWOp.png`；已 gitignore，未 add）。未動設計檔、未 commit，Pen 仍停在票檔。

### 2026-09-05T08:06:19.100Z · Chih-Lin Yeh · `bed7e159`

## 收尾：PR 已開，狀態轉 In Review

**PR #312**：https://github.com/CLYEH/little-sprout/pull/312
- base `development` ← head `feature/LS-194-design-chore-approval-fixes`
- head sha（40 碼）：`43616ff05d8560081fdb07ae6953afbaf674af38`
- `pr-body-check.sh --branch feature/LS-194-design-chore-approval-fixes --verify`：exit 0（票號檢查通過；LS-96／已修反查各 0 條命中，本 PR 未涉及待辦池申報）

**PR body 內容**（依 COLLABORATION §3 模板）：Ticket／變更摘要（四組修正＋額外 4 板 Body 補 `clip:true` 的理由）／驗證方式（VR 兩輪 comment id：R1 `eadb66e7` ITERATE、R2 `866f92c6` APPROVE；收據 r1/r2 路徑與 `head_sha`／`tree_hash`；byte-identical 211 root 證據；使用者裁決引用 LS-152 `186dc2bc`／LS-163 `a5814969`）／風險（無）／截圖區 `Design:` 行列出本票觸碰的 9 板（名稱＋節點 id）。

**commits（5 筆，本票全部）**：
- `2a26850` LS-152+LS-163 修正
- `fd7a3cd` LS-142 署名「·」＋corner/clip 收尾＋Notes 沿革
- `df05988` r1 溢出掃描收據
- `554190d` R2 修正 NBSP＋epDnW 訂正（BL-1/MN-1）
- `43616ff` r2 溢出掃描收據

**Pen 路徑**：仍停在 `/Users/clyeh/little-sprout/.claude/worktrees/LS-194/design/littlesprout.pen`，未切檔、未 kill（LS-180；設計 PR 併入 development 後再由 orchestrator 執行 §6 ④ 清場）。

狀態已轉 **In Review**。

### 2026-09-05T08:19:24.030Z · Chih-Lin Yeh · `25e1b911`

## merge-review R1 — **APPROVE**（2026-09-05 16:17 CST）

PR #312 · base `development`（`588043e`）· head `43616ff05d8560081fdb07ae6953afbaf674af38` · 5 commits · 純 .pen 設計 PR（無程式碼、無 DB，**lock 未持有**）。
依規定未開 Pen、未跑 `pen-open`／`--force-reload`——全部用 python 離線解析明文 JSON ＋ `design_tree_hash.py` 複算。

**0 blocker／0 major**，3 則 informational（皆非本 PR 引入、皆在票文「不做」之外）。

---

### 1｜scope／夾帶 ✅

- `git diff --stat origin/development...43616ff -- . ':!design' ':!.claude/evidence'` → **空**。
- 全 diff 僅 3 檔：`design/littlesprout.pen`、`design/evidence/LS-194-r1-overflow.json`、`-r2-overflow.json`（+464/−15）。VR 截圖在 `.claude/evidence/`、已 gitignore、未 add。
- 5 個 commit 訊息皆 `design: LS-194 …`（`2a26850`／`fd7a3cd`／`df05988`／`554190d`／`43616ff`）。
- **development 已前進到 `588043e`（LS-165／LS-197／LS-198 併入），但那段沒動 `.pen`**——`.pen` blob 在 merge-base `73b6c5e` 與 `588043e` 逐位元相同（`2f35ccc8…`），無 .pen 併版風險；`gh pr view` 回報 `mergeable=MERGEABLE`。

### 2｜只動該動的 ✅（root 清單與 handoff 對帳）

canonical hash 逐 root 比對 `origin/development` ↔ head：**220 → 220 root，新增 0／移除 0／變更 9，root 順序未變**。
`variables`／`themes`／`version`／`fileToken` **四項皆逐位元相等** → 無任何 token 值變動。

| root | 名稱 | 本輪改了什麼 |
|---|---|---|
| `t5wI4` | LS-152 / 01 設定 | Body `clip:true` ＋ 7 列 override |
| `Rx5mP` | LS-152 / 01 設定 · 深色 | 同上 |
| `z9tytH` | A11y / 01 設定 AX3 | 同上（高度 122） |
| `vgvFY` | A11y / P1 帳號密碼登入 AX3 | 板高 1300→1380 ＋ Email Field Box `height:140` |
| `LkrrS` | A11y / P1c 錯誤態 AX3 | 板高 1474→1554 ＋ 同上 |
| `IjWOp` | A11y / 14 相簿 AX3 | Body `clip:true` ＋ 三卡署名 ＋ Corner BL/BR y |
| `kHDk4` | LS-142 Notes | epDnW 追加訂正、sha 錯字、新沿革項 |
| `b3nzDp` | LS-152 Notes | 新沿革項 |
| `N1aeiU` | LS-163 Notes | 新 R4 修訂重點段＋沿革項 |

**與派工描述對帳**：派工寫「6 板＋4 板 Body 補 clip」，實際 4 個 clip 板（`t5wI4`／`Rx5mP`／`z9tytH`／`IjWOp`）都是那 6 板的子集，**第 7–9 個是三張 Notes 板**（LS-142／LS-152／LS-163 各一）。9 板與收據 `SCAN_BOARDS`／`corner_anchor.boards` 的 9 項集合完全一致。

**屬性層 diff 總計**：9 個 root 內 **+11 節點／−0 節點／~35 節點屬性**。11 個新增全部是三張 Notes 板的沿革區塊（`u3LTCg`＋`KXMqg`＋`qZpeo`／`EBhtU`＋`wNwrC`＋`Ndshq`／`oLLjW`＋`DsK7o`＋`qNSSl`＋`I0unu`＋`fiqfu`）。**全稿 0 刪除** → 結構上不可能產生死 id。

**不改列高與間距 token（LS-152 規則）逐項查核**：
- 21 個被改的 `Row ·` 都是 `type:"ref" ref:"bZhuV"`，**只在 `descendants` 加了兩個 key**（`bn1eA {height,justifyContent:center}`、`n1hzsr {enabled:false}`），節點本身的 width／height／padding／gap **一個都沒動**。
- 元件 `cmp/Settings Row`（`bZhuV`）**不在 9 個變更 root 內** → 元件本體（`padding:[$ctl-pad-tap,$inset-card]`、`gap:$sp-group`、`bn1eA gap:$sp-tight`）完全未動。
- `$sp-tight`／`$sp-group`／`$ctl-pad-tap`／`$inset-card` 隨 `variables` 相等而未動。
- 52 個 `bZhuV` 實例全列出：t5wI4／Rx5mP／z9tytH **各 10 列中改 7 列**，三列有真實副標的（家庭成員「4 位」／檢舉紀錄「2 則」／儲存空間「2.1／5 GB」）`n1hzsr` 原本就 `enabled:true`、**確實未動**。7 列涵蓋家庭區塊 2 列＋其他同型 5 列，屬票文§1「其他區塊若同型一併對齊」。
- **`B2DckT` 獨立複驗**：33 節點、`ref:"bZhuV"` 實例 **0 個**、`Row*` 命名節點 **0 個** → designer／VR 的「此問題在該板不存在」屬實。
- 家庭區塊（`sTqWF`）實際只有 3 列（家庭成員／邀請家人／退出家庭）；票文括號裡的「家庭名稱」在稿面是 Header Row 副標「「陳家」的帳號與家庭設定都在這裡。」、不是列，未被涵蓋屬正確。

**LS-163**：只有板高 ＋80 與 `v6YiYR {height:140}` 兩處；`LkrrS` 的 `v6YiYR` 保留既有 `stroke:$danger／strokeWidth:2`，只**追加** height。錯誤行／求助行結構未出現在 diff 中 → R2 MJ-4 保留。

### 3｜收據 ✅

- **r2 六支齊全**＋`scan_scope:"boards"`；三個 gate 項 `corner_anchor.mismatch=0`／`text_occlusion.flagged=[]`／`board_clip.flagged=[]`（含 `document_*`）。
- **`tree_hash` 我自己複算**：`design_tree_hash.py` 對磁碟 head `.pen` → `205b73bb00c7736b`；對 `git show 554190d:design/littlesprout.pen` → 同值。r1 對 `fd7a3cd` → `58cdcd771ef99f38`，與 r1 收據同值。**四方一致**。
- **`head_sha=554190d` 正確且非自我指涉**：`git diff --name-only 554190d 43616ff` 只有 `LS-194-r2-overflow.json`，`.pen` 在收據 commit 未再變動。
- **分類加總對得起 SUMMARY**：`sibling_intersection` 28 筆條目帶「同類 3 例」等註記加總 = **52**；`row_overflow` 3 筆 → **9**；`cross_parent_collision` 2 筆 → **2**。與 `summary_lines` 逐項相符。
- **r1↔r2 六支比對**：五支 JSON payload 逐位元相同；`cross_parent_collision` 僅一句 classification 措辭修訂（`puHZ5/Xldkz/T4FpQ 系列` → `puHZ5/Xldkz 系列`），`node_a`／`node_b`／筆數完全相同 — 與 VR R2 的說法一致，認可為訂正。
- **gate 本機綠（兩個版本都跑）**：branch 版 `design-evidence-check.sh --ticket LS-194 --base origin/development` exit 0（r1／r2 皆過，r2 判為本輪最新）、`design-notes-check.sh --base origin/development` exit 0（Notes 8 塊、死 id 0、沿革引用 0、缺失 0）。**另用 development 版腳本重跑一次模擬 CI merge-ref**（LS-198 把綠訊息支數改成依收據層數算、`overflow-scan.js` 只動註解），同樣 exit 0 且正確印出「六支掃描」。
- **CI 四項全綠**：ci／db／lint／rules pass（run 33954328300）。

### 4｜clip:true 與 IjWOp codepoint ✅

**4 個 Body 只加屬性、零位移**——逐屬性 diff 每個都只有 `clip: <absent> → true`，x／y／width／height／padding 全未變。四者皆 `fill_container × fill_container`，其下 Content 長欄遠高於視窗（`t5wI4` 還帶 `Tab Bar Clearance 151`／`Scroll Bottom Inset 80` 兩個捲動模擬 spacer），結構上就是 `overflow-scan.js` 檔頭處方的「板尺寸 clip 視窗＋內層長欄」，不是把長欄設 clip 的反例。

**IjWOp 三處署名 codepoint 逐字（base → head）**：

| 卡 | base | head |
|---|---|---|
| `ONEs5`／`i3jpIq` | 小安`[SP]`2`[SP]`歲`[SP]`3`[SP]`個`[WJ]`月 | 小安`[SP]`**`[·]`**`[SP]`2`[NBSP]`歲`[NBSP]`3`[NBSP]`個`[WJ]`月 |
| `pNPuF` | 同上 `\n` 小明`[SP]`8`[SP]`個`[WJ]`月 | 同上 `\n` 小明`[SP]`**`[·]`**`[SP]`8`[NBSP]`個`[WJ]`月 |

年齡片語 NBSP ✅、姓名與「·」之間 U+0020 未動 ✅、WJ（U+2060）未動 ✅、多寶貝 `\n` 未動 ✅ — **與 VR R2 描述逐項相符**。全稿橫向比對：head 的三處現在與 `cmp/Card Album` 元件預設（`bhroo/r6cYjT/IXmLN/wQVzs`）及 `puHZ5`／`Xldkz`／`Wl90J` 全族**字元序列完全同型**。
角托只改 `mlORb`／`atAkU`（BL/BR）y：424→482 ×2、366→424 ×1（各 +58）；TL/TR（`DjW91`／`JgUK8`）未動。

**Notes 沿革形式正確**：`epDnW` 的 head **`startswith(base)` 為 True**（1204→1565，純追加 361 字），原文一字未刪 — 這正是本專案該有的沿革寫法。`zGp4y`／`EBlnw` 各**只有一處**字元編輯、長度不變、`e2b0836`→`2e0b836` 且 head 已 0 次殘留；我另查 git：**`2e0b836` 是真 commit「design(albums): LS-142 R5 MJ-5 iPad 相簿欄間距 16→24」（與 Notes 句意逐字相符）、`e2b0836` 不是 git 物件** → 訂正方向正確。三則新沿革引用的 `dc9da6f3`／`186dc2bc`／`a5814969`／`931f074e` 為 Linear comment id，交叉引用由 VR R2 查核過。

### 5｜head 未前進 ✅

`gh pr view 312 --json headRefOid` = `43616ff05d8560081fdb07ae6953afbaf674af38`，與我 `git diff` 的 SHA 相同。

---

## informational（0 blocker／0 major；皆非本 PR 引入，建議入 LS-96）

**N1｜LS-177 的兩張設定板沒跟上這次置中修正，稿面現在有兩種列版型**
`y7KAW`（LS-177 / 設定 · 推播通知列 iPhone）與 `y66AzT`（同 · 深色）各有 **7 列 `n1hzsr` 未設 override**（邀請家人／退出家庭／封鎖名單／使用條款／隱私權政策／登出／刪除帳號，共 **14 列**），與修正前的 `t5wI4` 完全同一個條件——也就是使用者回報的那個偏上問題。
**這不是本 PR 的過失**：票文「不做：其他板的任何調整」明文排除，designer 沒碰是對的（兩板在 211 個逐位元相同的 root 內）。但後果是稿面同一畫面出現兩種列版型，而 LS-177 的板還是**較新**的那組（LS-194 blockedBy LS-177）。
**具體失敗情境**：LS-188（設定頁實作）或 QA 依 LS-177 板對稿時會看到未置中版本，與 LS-152 板矛盾，無法判斷哪個是正典。建議入 LS-96，或開一張 chore 把 14 列補齊。

**N2｜全稿最後一處署名年齡片語仍用一般空白**
`X9PfG/X2OkVz/MU4SE/lO4Cw/O6i3Gr/HIdMW` = `小明 · 8[SP]個[WJ]月`（應為 `[NBSP]`）。我對全稿所有 `wQVzs`（自身＋override）掃過，**這是唯一一處**。`X9PfG` 未被本 PR 觸碰（逐位元同 development），屬既有偏差、與 VR R1 抓的 BL-1 同一類。iPad 寬度下大概不會折行，所以是潛伏而非現行破圖（**PLAUSIBLE**，我沒有渲染複驗）。建議入 LS-96 一併掃乾淨。

**N3｜literal 尺寸的同步債（designer 已自報，僅覆核）**
`bn1eA {height:56/122}`、角托 y `482/424` 都是字面數字，Pencil 的 `height` 不吃 `$variable`。若日後 `$fs-body`／`$fs-note`／`$sp-tight`／`$ctl-pad-tap` 或卡片內容改動，這些值會無聲失準且沒有 gate 會抓。designer 已在 handoff 建表登記，本 PR 不需處理。

VR R2 自己留的四則（MN-2 行尾孤符、Mount TL/BR 角托 gate 盲區、收據缺 per-scan `scope`、`Rx5mP` 深色小字對比）我同意其判斷，不重複列。

---

## 我沒做的事（誠實聲明）

- **未開 Pen、未渲染**。所有「渲染後幾何」的主張——列高 75／141 不變、文字中心−列中心 = 0.00pt、`wQVzs` 高度 116／174／116、角托 24/24、AX3 折行點、「clip 只裁掉 Tab Bar 帶裡那 1 個葉節點」——都**來自 VR R1／R2 的實測與收據**，我只做了結構層與雜湊層的佐證，**沒有重新量測**。特別是 `bn1eA` 的 56／122 是否真的等於原本的 fit_content 高度（＝「不改列高」是否成立），離線算不出來，依賴 VR R1 對三板各 10 列的實測。
- **未透過 Pencil 重跑 `overflow-scan.js`**（派工明令不得 pen-open／force-reload）。`tree_hash` 複算只證明「收據對應這份 .pen 的節點樹」，**不證明六支的數字算對**——那要 CI 跑 Pencil，屬既知 gate 盲區。
- 未動任何檔案；lock **未持有**（純設計 PR，無 DB／無容器操作）。

---

**裁決：APPROVE。** 手術式修改做得乾淨——0 刪除、0 新增 root、9 個 root 全部對得上 handoff、token 零變動、四個 clip 只加屬性、Notes 純追加。已對 `43616ff05d8560081fdb07ae6953afbaf674af38` 貼 `merge-review` commit status = success。

### 2026-09-05T08:20:38.029Z · Chih-Lin Yeh · `911d5fec`

**orchestrator 2026-09-05 16:2x 收尾 → Done**

- VR 聚焦兩輪：R1 `eadb66e7` ITERATE（BL-1 NBSP）→ R2 `866f92c6` APPROVE；merge-review `25e1b911` APPROVE（220 root 變 9＝6 設計板＋3 Notes 板、token 零漂移、21 列只加 descendants 兩鍵、clip 只加屬性、codepoint 與 `cmp/Card Album` 逐字同型）；PR #312 併入 development。依使用者裁決「修完直接通過」，不送核可頁。
- 連帶：LS-152 意見 1（01 設定家庭列文字垂直置中）與 LS-163 意見（P1 AX3 跑版，真板 `vgvFY`／`LkrrS`）已落實 → 兩票 Done；LS-165 範圍 5（LS-142 留檔項）與 R1 N2（`IjWOp` 署名「·」）已收。
- reviewer N1（LS-177 `y7KAW`／`y66AzT` 設定板 14 列仍未置中）與 N2（`X9PfG/HIdMW` 年齡片語 ASCII 空白）→ 併入 LS-201 舊債清倉；N3（`bn1eA` 56／122 與角托 y 字面值無 gate）入池。
- lesson：Notes 規定（NBSP）沒有 gate，靠 VR codepoint 比對才抓到——`design-notes-check` 可加「署名年齡片語必 NBSP」機械檢查（入池）。

### 2026-09-05T09:37:38.086Z · Chih-Lin Yeh · `17d5c6f0`

## QA R1 — delta 合併驗收摘要（`test` tip `48d8b43`）— **PASS**

完整報告貼在 <issue id="e141cf57-aaa6-4ca5-bc95-332a50c5b975" href="https://linear.app/little-sprout-app/issue/LS-199/harnesspush-gate-xcodebuild-test-看門狗-逾時預設-25-分與test-runner-hasnt">LS-199</issue> comment `afad58a6-cd9d-4ca1-8110-5a41ac0470f8`（同一輪 delta QA，涵蓋 LS-194＋LS-199 共 8 檔零 app 碼變更）。

本票（LS-194）摘要：
- `design-evidence-check.sh`／`design-notes-check.sh` 對 `588043e` base 皆 exit 0；r1／r2 收據皆通過，r2 判為最新。
- `design_tree_hash.py` 算出 head `.pen` tree_hash＝`205b73bb00c7736b`，與 r2 收據逐字相同。
- python 對 `73b6c5e`（LS-177 併入版）↔ head 逐 root 全比對（220 個頂層節點）：新增 0／移除 0，恰 9 個變更（6 設計板＋3 Notes），`variables` 逐位元相等。
- `IjWOp` 三處署名年齡片語 codepoint 逐字核對：年齡片語內部空白皆 U+00A0（NBSP），姓名與「·」間 U+0020、WORD JOINER U+2060、多寶貝 `\n` 皆未動——與 VR R2 描述相符。
- 4 個 `Body` 的 `clip:true` 逐屬性 diff 確認為純屬性追加，無幾何位移。

驗收條件逐條 ✓（截圖前後對照＋量法／VR APPROVE／收據六支綠／CI 綠／merge-reviewer 通過／併入後 LS-152／LS-163 → Done），詳見 LS-199 該則 comment。無未完成項、無新增風險。

