# LS-138 iOS：Release 組態編譯失敗（41 error，#Preview 引用 DEBUG-only mock）——無法 Archive／TestFlight

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | lane:ui, Bug |
| Cycle | 1 |
| Project | Phase 0 — 開發地基 |
| Milestone | M0 上架前置 |
| 估點 | — |
| 建立 | 2026-09-03T04:07:34.122Z |
| 開始 | 2026-09-03T04:08:04.640Z |
| 完成 | 2026-09-03T05:40:09.167Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-138/iosrelease-組態編譯失敗41-errorpreview-引用-debug-only-mock-無法 |
| 關係 | related→LS-96, related→LS-136, blocks→LS-139, related→LS-130, related←LS-216, related←LS-212, related←LS-140 |

## 描述

## 來源

[LS-130](https://linear.app/little-sprout-app/issue/LS-130/taskls-20-ios-列表詳情改讀-thumb-pathnull-退回原圖全尺寸只在放大播放時簽) PR #237 merge-review R2（comment `b7d27c74-8a10-4eb7-8156-09e5882bb05b`，scope 外 pre-existing）：reviewer 為驗證 `#if DEBUG` 掛勾不進正式版而跑 `xcodebuild -configuration Release build` → **BUILD FAILED，41 個 error**，全部是 `#Preview` 區塊引用 DEBUG-only 的 mock／preview helper（例：`type 'AuthStore' has no member 'preview'`），散在 15 個檔；`development` 上同形，加 `ENABLE_PREVIEWS=NO` 仍失敗。**目前 app 無法 Archive，TestFlight 出不去**（M0 上架前置）。本機 Debug／模擬器與 CI 皆為 Debug 組態，所以從未暴露。

## 範圍

1. 在 development 上重現：`xcodebuild -project LittleSprout.xcodeproj -scheme LittleSprout -configuration Release -destination 'generic/platform=iOS Simulator' build`，列出 41 個 error 的檔案清單貼 handoff。
2. 修法擇一（以最小、可持續為準，handoff 說明取捨）：(a) 所有引用 DEBUG-only helper 的 `#Preview`／`PreviewProvider` 一律包 `#if DEBUG … #endif`；(b) 把 preview helper 從 `#if DEBUG` 移到 `#if DEBUG || targetEnvironment(simulator)`——不採（會把 mock 帶進模擬器 Release）；(c) preview helper 改用獨立 target——超出本票。預期採 (a)。
3. **機械 gate**：CI `ci` job 加一步 `xcodebuild -configuration Release build`（只 build 不跑測試），使 Release 編譯紅＝PR 紅；`docs/COLLABORATION.md` §7 對照表加一列。ci.yml 為 harness 檔——本票 PR 只改 Swift；gate 另開 harness hotfix 票（從 main）並 blockedBy 本票（orchestrator 開）。
4. 不改任何 preview 內容、不動正式邏輯。

## 驗收

- [ ] `xcodebuild -configuration Release build` 綠（handoff 附輸出末段）
- [ ] Debug 全測試（unit＋UI）仍綠；`#Preview` 在 Debug 仍可編譯（`xcodebuild -configuration Debug build` 綠即可）
- [ ] merge-reviewer 通過（scope：只包 `#if DEBUG`，零邏輯變更）
- [ ] Release 編譯 gate harness 票已開並 blockedBy 本票

## 留言（10）

### 2026-09-03T04:08:34.807Z · Chih-Lin Yeh · `1d0bd17a`

orchestrator 2026-09-03 12:08：建票即 Ready→In Progress（cycle 1；ui lane 在飛 1→2；High：TestFlight 出不去）。派 ios-dev：worktree `.claude/worktrees/LS-138`、branch `fix/LS-138-release-build-previews`（自 development `c5f8ed3`）；修法 (a) `#if DEBUG` 包 preview 區塊、零邏輯變更；驗 Release／Debug build＋全測試。CI gate 另票 **LS-139**（harness，Backlog，blockedBy 本票）——本票併入後從 main 開 hotfix。

### 2026-09-03T04:25:42.796Z · Chih-Lin Yeh · `706bc621`

## handoff

**Ticket**：LS-138 — iOS：Release 組態編譯失敗（41→49 error，`#Preview` 引用 DEBUG-only mock）——無法 Archive／TestFlight

**已完成**：
19 個檔的 `#Preview`／`PreviewProvider` 區塊（引用 `AuthStore`／`ChildrenStore`／`FamilyStore`／`TimelineStore.preview()` 或 `PreviewDiaryAPIClient`／`PreviewMediaUploadService`）全部包進 `#if DEBUG ... #endif`，零邏輯變更、`#Preview` 內容逐字未動：

| 檔案 | SHA | `#if DEBUG` 位置 |
|---|---|---|
| `LittleSprout/Features/Auth/EmailSignInView.swift` | 80febea | L76 |
| `LittleSprout/Features/Auth/OTPVerificationView.swift` | 80febea | L176 |
| `LittleSprout/Features/Auth/WelcomeView.swift` | 80febea | L372 |
| `LittleSprout/Features/Children/AttributionSheet.swift` | e61855f | L141 |
| `LittleSprout/Features/Children/ChildFilterBar.swift` | e61855f | L235（見下方額外說明） |
| `LittleSprout/Features/Children/ChildrenManagementView.swift` | e61855f | L340 |
| `LittleSprout/Features/Children/CreateChildView.swift` | e61855f | L381 |
| `LittleSprout/Features/Children/EditChildView.swift` | e61855f | L311 |
| `LittleSprout/Features/Diary/DiaryEditorView.swift` | c15353a | L255 |
| `LittleSprout/Features/Family/CreateFamilyView.swift` | c15353a | L153 |
| `LittleSprout/Features/Family/ForkView.swift` | c15353a | L262 |
| `LittleSprout/Features/Family/InviteFamilyView.swift` | c15353a | L315 |
| `LittleSprout/Features/Family/JoinCodeView.swift` | c15353a | L274 |
| `LittleSprout/Features/Family/JoinWaitingView.swift` | c15353a | L188 |
| `LittleSprout/Features/SettingsView.swift` | d007d59 | L104 |
| `LittleSprout/Features/Timeline/DiaryCardView.swift` | d007d59 | L204 |
| `LittleSprout/Features/Timeline/PhotoCardView.swift` | d007d59 | L56 |
| `LittleSprout/Features/Timeline/TimelineView.swift` | d007d59 | L367 |
| `LittleSprout/Navigation/RootView.swift` | d007d59 | L312 |

重現時 development @ `c5f8ed3` 實際是 **49 個 distinct error**（原始輸出 98 行，universal simulator build 每支診斷印兩次；票面「41」是開票當下的數字，development 之後又併入新畫面／新 `#Preview`，同一種成因，數字隨開發進度變動，不影響修法）。

額外發現：`ChildFilterBar.swift` 有一個只給 `#Preview` 用的 `private struct ChildFilterBarPreview`，定義在 `#Preview` 區塊**之前**、自己直接呼叫 `.preview()`——只包 `#Preview` 本身仍會紅在這個 host struct，改把 `#if DEBUG` 往前移到涵蓋它，Release 才真的綠。其餘 18 檔的 `#Preview` 都在檔尾、彼此相鄰，沒這個問題。

**已驗證**（怎麼驗的）：
1. Release build（修復前）：development @ `c5f8ed3` 跑 `xcodebuild -configuration Release build` → `EXIT:65`，49 個 distinct error，佐證重現。
2. Release build（修復後）：`** BUILD SUCCEEDED **`
3. Debug build（修復後，回歸）：`** BUILD SUCCEEDED **`
4. Mutation test：把 `WelcomeView.swift` 的 `#if DEBUG`／`#endif` 拿掉重跑 Release build，原封不動重現同一組 3 個 error（`WelcomeView.swift:373/377/382 error: type 'AuthStore' has no member 'preview'`），驗完立刻 `git diff` 確認乾淨還原。
5. `xcodebuild test`（Debug，dedicated simulator `LS-138-iPhone17Pro` UDID `B36C9511-BA3B-491E-BA67-BA6D67F0FD72`）：
   - `LittleSproutTests.xctest`：Executed 463 tests, 0 failures
   - `LittleSproutUITests.xctest`：Executed 20 tests, 0 failures
   - `** TEST SUCCEEDED **`
6. SwiftLint：`swiftlint lint --strict` → `Done linting! Found 0 violations, 0 serious in 184 files.`
7. push gate（`git push` 觸發）：`✓ push gate 通過`（含 tap-target gate）。

**未完成**（reviewer informational 處置）：
merge-reviewer 尚未審這張 PR（本次是 ios-dev handoff，審查是下一步）——目前沒有 reviewer 給出的 informational finding 可處置。票文第 3 點「機械 gate：CI 加 `xcodebuild -configuration Release build`」**未做**——照票文指示不動 `ci.yml`（本票 PR 只改 Swift），需要 orchestrator 另開 harness hotfix 票、blockedBy 本票（已在 PR body「風險」段落標註，非我省略）。

**風險**：
無邏輯變更風險——19 個檔 diff 皆恰好 +2 行（`#if DEBUG`／`#endif`），已用 mutation test 證明是必要防線（拿掉即紅）。唯一風險是 development 後續新畫面若未比照此 pattern 包 `#if DEBUG`，Release 會再紅——這正是待開的 harness gate 票要解決的問題。

**產出位置**：
- worktree：`/Users/clyeh/little-sprout/.claude/worktrees/LS-138`（branch `fix/LS-138-release-build-previews`，head `d007d590425d72e5c38923f764f6718284bae95f`）
- PR：https://github.com/CLYEH/little-sprout/pull/239 （development ← fix/LS-138-release-build-previews）
- 建置產物：`/private/tmp/claude-501/-Users-clyeh-little-sprout/954fc361-48a1-4e75-b3ca-02a3ce7da71b/scratchpad/LS-138-dd-*`（Release／Debug／test derivedData，均在 scratchpad，非 repo）
- 模擬器已關：`B36C9511-BA3B-491E-BA67-BA6D67F0FD72`（`LS-138-iPhone17Pro`，測試跑完後自動 shutdown，已確認 Shutdown 狀態）


### 2026-09-03T04:26:44.363Z · Chih-Lin Yeh · `5d1ef829`

orchestrator 2026-09-03 12:26：handoff `706bc621` 收到——PR **#239** head `d007d59`（19 檔，只包 `#if DEBUG`；development 上實測 49 error 非票文 41，因 dev 前進；`ChildFilterBar.swift` 的 preview 專用 struct 一併納入 guard；Release／Debug build 綠、463＋20 測試綠、mutation 驗過）→ In Review、派 merge-review R1（重點：零邏輯變更逐檔核、Release 實跑、遺漏掃描、CI 綠）。APPROVE 後併 development；LS-139 gate 隨後從 main 開。

### 2026-09-03T04:45:01.018Z · Chih-Lin Yeh · `95fa2e12`

## merge-review R1 — PR #239（head `d007d59`）

**VERDICT: APPROVE** — 無 blocker／major／minor。四維度（race／效能／平行／scope）全過，逐項獨立複驗，無一項僅採信 handoff。

審查範圍：`git diff c5f8ed3...d007d59`，19 檔、**38 insertions / 0 deletions**。全部實跑複驗（Release／Debug／base 重現／mutation 各一次自建，derivedData 全在 scratchpad）。

---

### 1. 零邏輯變更 — 機械證實

過濾掉 guard 行後的變更集合為**空**：

```
git diff -U0 c5f8ed3...d007d59 | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' \
  | grep -vE '^[+-][[:space:]]*(#if DEBUG|#endif)[[:space:]]*$'
→ (empty)
```

新增行逐字去重後只有兩種，各 19 次：`+#if DEBUG`、`+#endif`（無尾空白、無縮排差異）。0 deletions ⇒ `#Preview` 內容不可能被動到。

**guard 邊界逐檔看過**（19/19）：每個 `#if DEBUG` 都在檔尾段、`#endif` 都落在最後一行，`#endif` 之後無任何非空白內容；包進去的只有 `#Preview` 與兩個 preview 專用 host：
- `LittleSprout/Features/Children/ChildFilterBar.swift:235-248` — guard 起點正確落在 `private struct ChildFilterBarPreview`（L236）**之前**、`#Preview`（L245）之後收尾。handoff 指出的這個特例屬實且處理正確。
- `LittleSprout/Features/Children/AttributionSheet.swift:141-156` — 同型（`AttributionSheetPreviewHost` L146 在 `#Preview` 之後），一併納入。

兩者皆 `private`，file-scope，不可能被正式碼引用。無正式碼被誤包：
- `LittleSprout/Features/Timeline/DiaryCardView.swift:91-97` 那個 view body 內的 `#if DEBUG` 是 LS-130 既有掛勾，**不在本 diff**，未受影響。

### 2. Release build 綠 — 自建複驗

```
xcodebuild -configuration Release -destination 'generic/platform=iOS Simulator'
→ ** BUILD SUCCEEDED **   errors: 0
```

額外加驗**真機平台**（Archive／TestFlight 實際走的路徑，票文只要求 simulator）：

```
-configuration Release -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO
→ ** BUILD SUCCEEDED **   errors: 0
```

⇒ 票文「無法 Archive／TestFlight」的阻塞確實解除，不只是 simulator slice 綠。

**mock 確實沒進 Release**（比對未 strip 的 .o，非 strip 過的 binary，避免誤判）：`PreviewAuthService.o` Release 17,272 B ↔ Debug 54,088 B（空殼）；`ChildFilterBar.o` 內 `ChildFilterBarPreview` 符號 Release 0 ↔ Debug 47。

### 3. 「41 vs 49」差異 — 自行重現 base，解釋成立

`git archive c5f8ed3` 到 scratchpad 自建：

```
EXIT:65 · 49 distinct errors · 98 raw error lines · 19 檔
```

98 = 49×2 屬實（universal simulator build，arm64＋x86_64 各印一次）。成因分佈全屬同一類：
`FamilyStore` 13、`ChildrenStore` 11、`AuthStore` 11、`TimelineStore` 6 無 `preview`；`PreviewMediaUploadService` 4、`PreviewDiaryAPIClient` 4 找不到 = 49。

票面「41」是 LS-130 PR #237 併入前在該分支量到的數字，`c5f8ed3` 上是 49——差異是 base 不同，非重現有誤。handoff 的數字才是對應本 PR base 的正確值。

### 4. 完整性 — 集合相等（最強證據）

base 報錯檔集合 vs 本 PR 變更檔集合，雙向差集皆為**空**：

```
comm -23 err-files diff-files → (empty)   # 沒有漏修
comm -13 err-files diff-files → (empty)   # 沒有多改（零 scope creep）
19 == 19
```

修的檔＝壞的檔，一個不多一個不少。

### 5. mutation — 獨立重現，guard 是承重牆

scratchpad 複本（`rsync --exclude=.git`，worktree 全程未動、`git status` 乾淨）拿掉 `WelcomeView.swift` 的兩行 guard：

```
EXIT:65 · ** BUILD FAILED **
WelcomeView.swift:373:29 / :377:29 / :382:29
  error: type 'AuthStore' has no member 'preview'
```

與 handoff 宣稱的 3 個 error 位置、訊息完全一致。此 guard 非防禦性冗餘。

### 6. 遺漏掃描 — 數字如下

repo 內 `#Preview` 巨集共 **41** 個；`PreviewProvider` **0** 個（全面已遷移至巨集，見 n2）。其中 **26 個已在 `#if DEBUG` 內**（本 PR 的 19 檔），**15 個未包**，分佈於 13 檔（`DesignSystem/*` 9、`AlbumsView`、`AlbumCardView`、`DayDividerView`、`SectionTabBar`）。

逐檔 grep `\.preview\(|Preview[A-Z]*APIClient|PreviewAuthService|PreviewMediaUploadService|seedMyFamilyForPreview` → **13/13 全無命中**，皆只用正式型別，Release 本來就能編譯。⇒ 沒有遺漏，且未包它們正是修法 (a)「只包引用 DEBUG-only helper 者」的最小解，不是疏漏。

`Support/Preview*.swift` 之外的 preview helper 也查了，全部已在 guard 內：`TapTargetGateHarness.swift`、`TapTargetGateScreenName.swift` 整檔 `#if DEBUG`；`FamilyStore.seedMyFamilyForPreview` 位於 `FamilyStore.swift:125-140` 的 `#if DEBUG` 內（我原本疑其為未包的 preview-only 正式碼，查證後不成立，撤回）。

### 7. Debug 不變 — 且 preview 沒有被靜默殺掉

```
-configuration Debug → ** BUILD SUCCEEDED **   errors: 0
```

Debug build 綠**不足以**證明 preview 仍會編譯（若 `DEBUG` 沒定義，preview 被整段排除也會綠）。補驗條件旗標與符號：

- `SWIFT_ACTIVE_COMPILATION_CONDITIONS`：Debug = `DEBUG`、Release = 空。
- `WelcomeView.o` 內 `PreviewRegistry` 符號：Debug **94** ↔ Release **0**；`Pill.o`（未包的對照組）Debug 41 ↔ Release 0。

⇒ Xcode Previews 在 Debug 照常運作，Release 完全排除。

**CI 全綠**（run 33714947812）：`ci` pass 9m14s、`db` pass、`lint` pass、`rules` pass。
`ui-test-trigger` 判定 **要跑**（我本機以同一份變更清單獨立跑 `--files -` 亦得 exit 0、命中 `LittleSprout/**/*.swift`，與 CI 輸出一致）；`點擊目標 gate` 實際執行並通過（OTPVerificationView／SettingsView／DiaryEditorView／SectionTabView ≥44pt）；unit 463 tests / 0 failures。

### 8. 四維度

- **Race condition**：無。本 PR 未變更任何 runtime 程式碼，`#if DEBUG` 純編譯期；actor 隔離／`@MainActor`／`Sendable`／Task 生命週期／上傳佇列／快取一致性等面向皆未觸及（0 deletions＋僅 guard 行，已於 §1 機械證明）。
- **運算效能**：無 runtime 路徑變動。副作用為正向：Release binary 不再帶入 preview mock（§2 .o 佐證）。無 RLS／查詢／分頁／圖片解碼相關變更。
- **平行優化**：不適用（無並發或批次工作）。
- **Scope**：精準。19 檔全為 `LittleSprout/**/*.swift`；`.github/`、`docs/`、`scripts/`、`supabase/` 零觸碰。票文第 3 點 CI gate 依指示不做，已開 **LS-139** 且 `blocks` 關係存在 ⇒ 驗收第 4 項滿足。commit 訊息 4 支皆合 `fix(ios): LS-138 …` 規約、分段落地。PR body 具 Ticket／驗證／風險／`Design:` 行，`rules` job 已驗過。

### 驗收對照

| 票文驗收 | 結果 |
|---|---|
| Release build 綠 | ✅ 自建 simulator＋真機平台雙綠 |
| Debug 全測試綠、`#Preview` 仍可編譯 | ✅ build 綠＋符號證明 preview 真的有編（94 ↔ 0） |
| merge-reviewer 通過（只包 `#if DEBUG`、零邏輯變更） | ✅ 本則，機械證明 |
| Release gate harness 票已開並 blockedBy | ✅ LS-139 |

---

### informational（不需修改，供 handoff 處置）

- **n1**：15 個未包 guard 的 `#Preview` 仍會在 Release 被型別檢查。目前無害（皆不引用 DEBUG-only 符號，且 Release 不產出 `PreviewRegistry` 符號），且符合修法 (a) 的最小範圍。真正的持續防線是 LS-139 的 Release 編譯 gate——它正好攔截「新 preview 引用 DEBUG-only helper」這唯一會出事的情況。**建議：不修**，本票不擴 scope。
- **n2**：票文與 PR body 都提到 `PreviewProvider`，但 repo 現存 0 個（已全面 `#Preview` 巨集化）。純文字精確度，無程式影響。**建議：不修**。

無 PLAUSIBLE 項；無跳過的範圍。


### 2026-09-03T04:46:30.451Z · Chih-Lin Yeh · `81171365`

orchestrator 2026-09-03 12:46：merge-review R1 `95fa2e12` **APPROVE**（零邏輯變更 38+/0−；simulator＋真機平台 Release 皆綠；修的檔集合＝壞的檔集合 19==19；mutation 重現；Debug preview 符號仍在）→ PR #239 併入 development **`5e9b01b`**（status 已貼 no content diff）。**promote 暫緩**：test `c5f8ed3` 正在 QA（LS-130 R4＋LS-136 R1）→ v0.8.1 出貨後再 promote 本票到 test → QA（Release build 綠＋smoke）→ v0.8.2 → LS-139 從 main 開 hotfix 加 Release 編譯 gate。R1 informational n1（15 個未引 DEBUG helper 的 `#Preview` 不包）／n2（repo 無 `PreviewProvider`）：不修、記錄即可。

### 2026-09-03T05:16:38.388Z · Chih-Lin Yeh · `8f66c995`

orchestrator 2026-09-03 13:16：v0.8.1 出貨後 `promote.sh development test` → **test＝`5e9b01b`**（含本票 #239）→ QA、派 qa R1（Release build simulator＋真機平台綠、Debug build 綠、`xcodebuild test` 全綠、smoke 登入／時間軸／發佈／Tab Bar）。PASS → 收尾 → promote main → v0.8.2 → 派 LS-139。

### 2026-09-03T05:35:02.264Z · Chih-Lin Yeh · `094a31f9`

## QA R1 — test branch HEAD `5e9b01b`（PR #239 mergeCommit 核對一致）

worktree：`.claude/worktrees/qa-test`（detached at `origin/test`）；derivedData／證據皆在 scratchpad，不入 repo。

### 驗收逐項

**1. Release build 綠** ✓ PASS
- `-destination 'generic/platform=iOS Simulator'`：`** BUILD SUCCEEDED **`，`error:` 命中數 0。
- `-destination 'generic/platform=iOS'`（真機平台，`CODE_SIGNING_ALLOWED=NO`）：`** BUILD SUCCEEDED **`，`error:` 命中數 0。
- log：`/private/tmp/.../scratchpad/LS-138-qa-r1/release-sim-build.log`、`release-device-build.log`

**2. Debug 不受影響** ✓ PASS
- `xcodebuild -configuration Debug build`：`** BUILD SUCCEEDED **`，0 error。log：`debug-build.log`
- 抽驗 3 檔，`#Preview` 皆在 `#if DEBUG ... #endif` 內：
  - `WelcomeView.swift`：L372 `#if DEBUG` → L373/377/382 三個 `#Preview` → L386 `#endif`
  - `ChildFilterBar.swift`：L235 `#if DEBUG`（涵蓋 preview 專用 `ChildFilterBarPreview` L236）→ L245 `#Preview` → L248 `#endif`
  - `DiaryCardView.swift`：L204 `#if DEBUG` → L205 `#Preview` → L234 `#endif`（另有 L91-97 一個既有、無關本票的測試掛勾 `#if DEBUG`，未受影響）
  - Debug build 綠 ⇒ preview 在 Debug 仍可編譯。

**3. smoke（Debug 模擬器 `qa-test-iPhone17Pro`）** ✓ PASS
- 安裝 Debug build → 登出既有 session → 用 Email 登入 `qa-ls138@example.com`，OTP 經 Mailpit 取得 `660216` 輸入 → 登入成功 → 建立家庭／寶貝檔案 → 時間軸（空狀態）→ 新增回憶（文字＋1 張照片）→ 發佈 → 時間軸顯示卡片 → 點卡片進詳情（日期／內文／照片／留言區）→ 返回時間軸 → Tab Bar 四顆（時間軸／相簿／寶貝／設定）逐一切換皆正常渲染。
- 截圖：
  - `smoke/1-timeline.png`（空時間軸）
  - `smoke/2-timeline-with-memory.png`（發佈後時間軸卡片）
  - `smoke/3-detail.png`（日記詳情）
  - `smoke/4-tabbar-back-to-timeline.png`（Tab Bar 切一輪回時間軸）

**4. 回歸 `xcodebuild test`（Debug，同一台 `qa-test-iPhone17Pro`）** ✓ PASS
- `** TEST SUCCEEDED **`
- Unit（`LittleSproutTests`）：Executed 463 tests, 0 failures
- UI（`LittleSproutUITests`）：Executed 20 tests, 0 failures
- 與 merge-review R1／handoff 宣稱數字一致（463／20）。log：`test-run.log`

### 裁決：PASS

四項驗收全過，證據齊備（build log＋test log＋4 張截圖）。merge-review R1 的 informational n1／n2（15 個未包 guard 的 `#Preview` 無害；`PreviewProvider` 文字精確度）orchestrator 已裁定不修，QA 不重複挑戰。

模擬器已關：`5F59FF04-D2C6-413D-B31A-AFD39B257FB3`（`qa-test-iPhone17Pro`，收工前 shutdown，已確認 Shutdown 狀態）。

### 2026-09-03T05:36:39.838Z · Chih-Lin Yeh · `e7d74040`

orchestrator 2026-09-03 13:35：QA R1 `094a31f9` **PASS**（Release simulator／真機平台、Debug build 皆綠；463＋20 測試綠；smoke 登入／發佈／詳情／Tab Bar；qa success 貼 `5e9b01b`）→ `promote.sh test main` → **main＝`5e9b01b`** → tag **v0.8.2**（可 Archive）。派 sweeper → retro → cleanup → Done。LS-139（Release 編譯 gate）blockedBy 解除、已從 main 開 hotfix 派工。

### 2026-09-03T05:39:42.501Z · Chih-Lin Yeh · `1255845d`

## dead-code-sweeper 報告（orchestrator 代貼，LS-96 `5cb5ddf3`）

範圍 PR #239（`c5f8ed3...d007d59`，19 檔 38+/0−）。**未發現本票引入的死碼**：(1) `AttributionSheetPreviewHost`（`AttributionSheet.swift:141-156`）／`ChildFilterBarPreview`（`ChildFilterBar.swift:235-248`）與其 `#Preview` 同在 guard 內、無孤兒；`Support/Preview*.swift`／`TapTargetGate*` 本 PR 未觸碰且仍被 DEBUG 消費端引用；`CreateFamilyView.FamilyPreviewCard` 是正式邏輯不受影響。(2) 19 檔 `#if DEBUG`／`#endif` 1:1 配對、無巢狀、無空 `#Preview`（`DiaryCardView.swift` 兩組 L91-97／L204-234 相鄰不重疊）。(3) 未包的 15 個 `#Preview`（13 檔）獨立 grep DEBUG-only 符號 0 命中。(4) 無 `LS-138-*` 進 repo。(5) 文件無過時「Release 無法建置」敘述（§7「push-gate 無 Release archive gate」為另一 gate 既有盲區敘述，LS-96 已記）。盲區：純 grep、未重跑 Release build（採信 merge-review R1 符號比對證據）。

### 2026-09-03T05:40:03.892Z · Chih-Lin Yeh · `244e5f88`

## 收尾（orchestrator 2026-09-03 13:40）

- **sweeper**：`1255845d`，無死碼。
- **Retro**：(1) 這個 pre-existing 缺陷（自 `#Preview` 引 DEBUG-only mock 起，跨 15→19 檔）存在多個版本都沒信號——CI 只跑 Debug、TestFlight 尚未開始，直到 merge-reviewer 為了驗 `#if DEBUG` 掛勾順手跑 Release 才發現。**前饋必有反饋**的缺口＝LS-139（CI Release 編譯 gate），已派工。(2) 修法 (a) 純 guard、零邏輯，reviewer 用「修的檔集合＝壞的檔集合 19==19」＋`.o` 符號比對證明無遺漏、無 scope creep——這種集合相等證據可作為同型修復的固定查實法。(3) 票文寫 41 error、實測 49（development 前進），差異已解釋；未來票文對「數字」標注 base SHA。(4) 補一條更早的反饋：SwiftLint custom rule 擋「`#Preview` 不在 `#if DEBUG` 內卻引用 `.preview()`／`Preview*` 符號」，比編譯 gate 更快（入池 LS-96，下一則）。(5) 時程：開票 12:07 → PR 12:26 → APPROVE 12:46 → 併 dev → v0.8.1 後 promote 13:15 → QA PASS 13:34 → v0.8.2 13:35 → Done 13:40。
- **清理**：`cleanup-merged.sh --apply LS-138` 移除 worktree 與本機分支；遠端無殘留。
- 狀態 → **Done**（隨 **v0.8.2** 出貨，main `5e9b01b`，可 Archive）。

