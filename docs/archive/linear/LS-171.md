# LS-171 Hotfix：LS-168 `tree_hash` Pencil 端與 js／py 不一致——Pencil `Get` 把 `geometry` 省略成 `"..."`，design-evidence gate 對含 path 節點的稿一律 fail-closed（擋住所有設計票收據）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Urgent |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-04T08:01:06.276Z |
| 開始 | 2026-09-04T08:01:06.612Z |
| 完成 | 2026-09-04T09:09:10.141Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-171/hotfixls-168-tree-hash-pencil-端與-jspy-不一致-pencil-get-把-geometry-省略成 |
| 關係 | related→LS-127, related→LS-122, blocks→LS-152, related→LS-168, related→LS-142, related←LS-238, related←LS-236, related←LS-226, related←LS-208, related←LS-201, related←LS-198, related←LS-177, related←LS-133, related←LS-163, related←LS-96 |

## 描述

## 來源

[LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表) visual-review R3（2026-09-04）附加任務：[LS-168](https://linear.app/little-sprout-app/issue/LS-168/harnessdesign-evidence-gate-補強三項-notes-板節點-id-存在性檢查設計稿五度notes) R1-N2 三方 `tree_hash` 比對，結果貼 [LS-168](https://linear.app/little-sprout-app/issue/LS-168/harnessdesign-evidence-gate-補強三項-notes-板節點-id-存在性檢查設計稿五度notes) comment `6383b2fa-b1f8-4515-b89a-0e82b2d274f6`。對同一份 .pen：`design_tree_hash.py` = `03e7804b035d8e4b`、node `treeHash(treeHashLines)` = `03e7804b035d8e4b`、**Pencil execute =** `84420d7b6419b40e`。

**根因（已決定性證明）**：Pencil `Get` 回傳的節點把 `geometry` 省略成字面字串 `"..."`（實測 `String(n.geometry).length === 3`）。把磁碟 JSON 中 8 個帶 `geometry` 的節點該值改成 `"..."`、其餘不動 → 得到 `84420d7b6419b40e`，與 Pencil 完全吻合。8 個節點含 `cmp/Photo Corner`（全專案共用），因此 `design-evidence-check.sh` **的 tree_hash 比對目前不可能通過**——[LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表) 併 development 後的最新收據會被打紅（[LS-168](https://linear.app/little-sprout-app/issue/LS-168/harnessdesign-evidence-gate-補強三項-notes-板節點-id-存在性檢查設計稿五度notes) R2 N1 收緊的必填條件）。

## 範圍

1. **查 Pencil 是否能回傳完整** `geometry`（`Get` 的 `includePathGeometry` 或等價選項；`mcp__pencil__read_skill` 查 schema）。能 → Pencil 端片段改用該選項，三方同值，`geometry` 仍納入雜湊；不能 → 三方**一致地**把 `geometry` 排除在正規化之外，並在 `overflow-scan.js` 檔頭／COLLABORATION §7 明列「path 幾何變更不在 tree_hash 偵測範圍」盲區。擇一並在 handoff 說明。
2. **交叉測試**：`overflow-scan.test.js`／`design-evidence-check.test.sh` 加帶 `geometry` 的 path 節點 fixture，js／py 同值；再加一個「geometry 被替換成 `"..."`」的負樣本，證明現行三方比對會抓到這種差異（或在排除方案下證明不影響）。
3. **Pencil 端實證**：對 [LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表) worktree 的 .pen（Pen 目前開著的檔）三方重算必須同值——**Pen 單檔獨佔、不得切檔**；只做唯讀 `execute`（Get）；若 [LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表) designer R4 尚未落地（該 worktree `git status` 不乾淨）就等落地後再比對，或把比對留給 [LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表) VR R4 並在 handoff 明列。
4. 文件：`overflow-scan.js` 檔頭、`docs/COLLABORATION.md` §7 收據列、`.claude/agents/ui-designer.md`／`visual-reviewer.md`（順帶記：五支掃描一次 `execute` 會 `InternalError: interrupted`，拆兩次跑）。
5. hotfix 流程：自 `main` 切 `hotfix/LS-171-tree-hash-geometry`（worktree 已建）、PR 回 main、back-merge development。

## 不做

改變收據其他欄位語意；碰 [LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表) 設計內容。

## 驗收

- [ ] 三方 `tree_hash` 對含 path 節點的稿同值（handoff 附三個數字）
- [ ] 自測含 path fixture 與 `"..."` 負樣本；CI `rules` 綠
- [ ] [LS-142](https://linear.app/little-sprout-app/issue/LS-142/設計相簿頁相簿詳情上傳佇列ls-20-畫面群-ui-designervisual-reviewer-3-輪) r1–r7／[LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表) r1–r3 既有收據在新 gate 下仍綠（舊收據放行條款）
- [ ] merge-reviewer 通過

## 留言（7）

### 2026-09-04T08:24:51.856Z · Chih-Lin Yeh · `acc247b3`

## handoff — LS-171（2026-09-04 16:23 CST）

**Ticket**：LS-171 Hotfix：LS-168 `tree_hash` Pencil 端與 js／py 不一致（Pencil `Get` 把 path `geometry` 省略成 `"..."`）。PR **#273** `hotfix/LS-171-tree-hash-geometry` → main，head **`ecb84c0`**（2 commits，自 `origin/main` `df95691`）。CI 四項綠：ci 5m47s／db 3m07s／lint 21s／rules 3m02s（run 33852687808）。PR body 在建 PR 時就位（pr-body-check ✓），rules job 讀到的是最終 body。

### 已完成（票文範圍 1–5）

1. **範圍 1 擇 (c)** — `d794e3f` `scripts/design/overflow-scan.js`：`read_skill execute.md` 確認 `GetOptions.includePathGeometry?: boolean` 存在且 `Get(visit, options)` 接受；雜湊走訪改 `Get(visit, { includePathGeometry: true })`。唯讀 `Get` 實測：不帶選項 8 個 path 節點 `geometry` 長度皆 3（`"..."`）；帶選項回傳完整值——`WiqyL` 484／`sD5Ma` 35／`DhOiK` 12／`GuOM0` 110／`aQq5T` 139／`SieME` 127／`j7iqM` 153／`mzo0K` 0（磁碟亦為空字串 `""`），節點數 8242 兩種走訪皆同，`WiqyL`／`sD5Ma` 鍵集與磁碟相同。`geometry` 仍納入雜湊，**零新盲區**；py／js 演算法不變（`design_tree_hash.py` 只補 docstring）。
2. **範圍 2 自測** — `d794e3f`：`overflow-scan.test.js` 22 組（＋3）：合成 doc 加 `type:"path"` 帶 geometry（Corner Shape 形狀）與空 geometry（`mzo0K` 形狀）兩個 fixture、js／py 交叉一致含 `--dump Pp5kQ` 逐字相同、`"..."` 負樣本（geometry 省略 → hash 必變；改一字元也變；空字串以 `""` 參與）、**原始碼斷言**（雜湊走訪的 `Get` 必帶 `includePathGeometry: true`，且 `SCAN_HASH_ONLY`／`SCAN_SKIP_HASH`／`SUMMARY-HASH` 存在）。`design-evidence-check.test.sh` 41 案（＋1）：㉖ 的合成 .pen 頂層 `d` 改為帶 geometry 的 path（touched 板不變、節點數不變）；㉖-b 收據 `tree_hash` 取自 geometry 改 `"..."` 的走訪 → 紅「tree_hash 不符」。
3. **範圍 3 Pencil 端實證**（LS-152 worktree `git status` 乾淨時做；只用唯讀 `execute`／`Get`，未切檔、未寫節點、未落地）：

   | .pen 狀態 | py `design_tree_hash.py` | node `treeHash(treeHashLines)` | Pencil `execute` |
   |---|---|---|---|
   | LS-152 `7197b8d`（.pen `92e0669`） | `03e7804b035d8e4b` | `03e7804b035d8e4b`（8242 行） | 帶選項 **`03e7804b035d8e4b`**；不帶選項 `84420d7b6419b40e`（＝VR 數字＝磁碟 JSON 8 個 geometry 改 `"..."` 的值，本票重算確認） |
   | LS-152 `b0b42ad`（designer 落地 R4 後，worktree 乾淨） | `1d66044f7af83f8a` | `1d66044f7af83f8a` | **本 PR 正典腳本原文（去註解）＋`SCAN_HASH_ONLY = true` → `1d66044f7af83f8a`**，`total_nodes` 8242 |

   兩個稿態三方皆同值。第二次是「designer 在兩次 execute 之間落地 R4」被我當場撞到（第一份 SUMMARY-HASH 與 `7197b8d` 不符 → 查 LS-152 log 發現 `b0b42ad` → 對新磁碟重算 py／js 即相符），順帶證明雜湊對 property-only 變更（板高調整、節點數不變）有偵測力。
4. **範圍 4 文件** — `ecb84c0`：`ui-designer.md`（雜湊走訪必帶選項、正典腳本內建不得自行改寫；`InternalError: interrupted` 拆兩次：第一次 `SCAN_HASH_ONLY = true` 只印 `SUMMARY-HASH … tree_hash=`、第二次 `SCAN_SKIP_HASH = true` 跑五支（SUMMARY `tree_hash=skipped`）、之間不得寫入、`tree_hash` 抄第一次視為同一次掃描）、`visual-reviewer.md`（三方比對先確認此點再指控拼接）、COLLABORATION §7 收據列 LS-171 段（根因、三方數字、自測、盲區）、`overflow-scan.js` 檔頭。
5. **範圍 5 既有收據** — LS-142 r1–r7 @`4b8e97d` 七份綠、LS-152 r1–r3 @`7197b8d` 三份綠（皆舊收據放行；LS-152 分支 head tree 尚無第五支，實測 `grep -c scanTextOcclusion`＝0）。gate 腳本本票未改。

### 已驗證（怎麼驗）

- `overflow-scan.test.js` 22 組全綠；**mutation**：從雜湊走訪拿掉 `{ includePathGeometry: true }` → 原始碼斷言紅（`AssertionError: 雜湊走訪的 Get 必須帶 includePathGeometry: true…`），改回全綠。
- `design-evidence-check.test.sh` 41 案全綠（首跑 4 案紅是我把 path 掛在頂層 `a` 下、讓 `a` 成為 touched 板而 `boards:["d"]` 未列——改掛到 `d` 後全綠，非 gate 問題）；`design-notes-check.test.sh` 全綠；三支跑後 `scripts/gates/` 無 `__pycache__`。
- Pencil 端另以本 PR 正典腳本原文＋`SCAN_SKIP_HASH = true`＋`SCAN_BOARDS=["y6MxMV"]` 跑五支：SUMMARY `total_nodes=8242 scanned_nodes=13143 … text_occlusion=0/9 corner_anchor=0/0/0 document=212/1696/0 tree_hash=skipped`，`else {}` 包裹後五支與彙整段輸出正常。
- `node require`／`bash -n`／`ast.parse -W error` 全過；`git diff origin/main..HEAD` 7 檔 +81/−11、無刪除；worktree 乾淨。

### 未完成／盲區

- **票文範圍 4「五支一次 execute 會 interrupted」我沒有重現**（只跑了 HASH_ONLY 與 SKIP_HASH 兩種拆法，沒跑不帶旗標的全量以免佔用 Pen 太久）；`InternalError: interrupted` 的實測來源是 LS-152 VR R3 comment。旗標是為了讓拆跑不必手改正典腳本；若日後全量一次能跑完，旗標不用也無妨。
- 盲區：Pencil `Get` 對 `geometry` 以外的鍵若另有省略形，本票沒有旗標能補，只能靠 VR 三方比對抓（實測 8242 節點鍵集逐一相同，目前無此情況）。`variables`／`themes`／`fileToken` 不在雜湊內（LS-168 R1 N4 既有盲區不變）。
- `SCAN_HASH_ONLY`／`SCAN_SKIP_HASH` 是票文「順帶記」之外的小幅程式變更（走訪包進條件、`if (skipHash) return;`、`hashOnly` 分支與 `else {}`），五支演算法零變更——請 merge-reviewer 留意 scope。
- reviewer informational：本票尚無 review。

### 風險

- LS-152 R4 收據：designer 用本 PR 併入後的正典腳本重跑，`tree_hash` 才會與 CI 相符；本 PR 併 main → back-merge development → LS-152 併 development 之前，LS-152 分支的腳本仍是舊版（`fifth` 未生效、舊收據放行），順序不對不會誤紅但也不會驗新鮮度。
- 驗證既有收據時曾以 `git archive` 把 LS-142 r1–r7 放進 worktree、跑完誤刪 main 已追蹤的那七份——已 `git checkout` 還原，兩支 commit 皆指名 `git add`，range 內無刪除（PR body 備註 3）。
- Pen 期間全程唯讀（`Get`／`Print`），未切檔；designer 在我兩次 execute 之間落地 `b0b42ad`，未受影響。

### 產出位置

- PR #273 `https://github.com/CLYEH/little-sprout/pull/273`，head **`ecb84c0`**（`d794e3f` 程式＋自測 → `ecb84c0` 文件），建 PR 時間 2026-09-04 08:16:58Z（16:16 CST），四項 CI 綠。
- worktree `.claude/worktrees/LS-171`（乾淨）。
- scratchpad：`LS-171-pr-body.md`、`LS-171-handoff.md`、`LS-171-jstest.txt`／`-mut.txt`、`LS-171-evtest.txt`、`LS-171-real-{142,152}.txt`、`LS-171-pen-dots.pen`（geometry 改 `"..."` 的 LS-152 `7197b8d` 快照）、`LS-171-scan-nocomment.js`（餵 Pencil 的去註解正典腳本）。

### 2026-09-04T08:32:54.380Z · Chih-Lin Yeh · `6d71d7a5`

## merge-review R1 — PR #273（head ecb84c0）

裁決：**APPROVE**（無 blocker／無 major）。根因修對、方案 (c) 選得對、`geometry` 仍入雜湊＝零新盲區。1 條 minor（N1：新增的原始碼斷言有存活突變體）＋4 條 informational。審的 head＝`ecb84c0dc129680e2cc24efc57a3c738cfbbd8ec`，與 `gh pr view 273 --json headRefOid` 當下值相同；base `df95691`（LS-168 併入點），diff 7 檔 +81/−11，CI 四項 pass（run 33852687808），`pr-body-check --verify` 綠。2026-09-04 16:31 CST。

### （1）`includePathGeometry` 覆蓋面／有無其他被省略的欄位 — **離線就能決定性回答，我不必開 Pen**

我沒有 Pencil 工具（本 agent 的 tool 清單沒有 `mcp__pencil__*`），所以**唯讀 `Get` 抽樣我做不到、明講跳過**。但這題有一個比抽樣更強的驗法，我做了：

**把磁碟 JSON 的 8 個 `geometry` 值改成 `"..."`、其餘一個位元都不動 → 我算出 `84420d7b6419b40e`，與 VR 實測的「Pencil 不帶選項」值逐位元相同。**

`tree_hash` 覆蓋 8242 個節點的**每一個鍵**（canon 是全鍵排序序列化）。既然「只改 geometry」就能精確重現 Pencil 的數字，就等於證明：**Pencil `Get` 回傳的 payload 與磁碟 JSON 的差異只有 `geometry` 一項**——`viewBox`／`fill`／`content`／`icon`／`library`／`effect`／`metadata`／`descendants` 全都沒有被省略或改寫，連 `c.index`、鍵序、數字格式也一併被這個等式驗掉（那本來是 LS-168 R1-N2 的另一半疑慮，順帶收掉）。任何第二個漂移欄位都會讓這個等式對不上。

覆蓋面盤點（`7197b8d` 的 .pen，我自己走）：

| 檢查 | 結果 |
|---|---|
| 帶 `geometry` 的節點 | **8 個**，型別**全部是 `path`**（`WiqyL` Apple Icon 484／`sD5Ma` Corner Shape 35／`DhOiK` Corner Edge 12／`GuOM0`＋`aQq5T`＋`SieME`＋`j7iqM` G arc 110/139/127/153／`mzo0K` 0） |
| 全檔 `type:"path"` 的節點數 | **8**——與上一列 1:1，`includePathGeometry` 涵蓋率 100% |
| VR 點名的 `cmp/Photo Corner` | `sD5Ma`／`DhOiK` 兩個都在名單內（全專案共用，確認是「所有稿都會中」） |
| 帶 `viewBox` 的節點 | 4 個、全是 path；**沒有被省略**（由上面的等式證明） |
| 稿內是否已存在字面值 `"..."` | 0 次——省略模型無歧義 |
| 空字串 geometry（`mzo0K`） | 只改 7 個非空的 → `ffc6832e20f1625c`（≠ Pencil 值）；改滿 8 個才對上 → **空字串也會被省略成 `"..."`**，與實作者的模型一致 |

兩個稿態的 py／js 我自己重算，與 handoff 逐字相同：`7197b8d`＝`03e7804b035d8e4b`、`b0b42ad`＝`1d66044f7af83f8a`（皆 8242 行，py＝js）。**Pencil 端那三個數字我無法自己跑**——但實作者回報的「帶選項後 8 個 geometry 長度 484/35/12/110/139/127/153/0」與磁碟長度**完全相同**，加上上面的等式，我認為證據鏈已足夠；剩下的殘餘不確定性只有「Pencil 帶選項時是否回傳與磁碟逐字元相同的 geometry 字串（而非等長但不同）」，而這一點 VR R4 對真實收據跑一次就會自然驗證（不符即紅、fail closed）。

### （2）`SCAN_HASH_ONLY`／`SCAN_SKIP_HASH` — scope 可接受，`tree_hash=skipped` 不會被誤放行

**誤放行實測**（合成 repo，三種情境）：

| 收據 | gate |
|---|---|
| 最新輪次、`"tree_hash": "skipped"`（fifth 生效） | **紅**「tree_hash 必須是 16 碼小寫 hex（收據='skipped'）」 |
| 舊收據放行情境、`"tree_hash": "skipped"` | **紅**（LS-168 ㉛-b 的「欄位在就驗」語意接住了它） |
| 拆兩次的正確用法（`tree_hash` 抄第一次 HASH_ONLY） | **綠** |

**scope 判定：可接受。** 票文 §4 只要求「記載拆兩次跑」，實作者多加了兩個旗標——但 LS-122 明文規定收據一律由**正典腳本**產出、不得每輪臨場手寫；沒有旗標，agent 要拆兩次就只能手改正典腳本，等於違反那條規約。旗標是讓票文要求的做法**合法可執行**的最小改動（走訪包進條件、`if (skipHash) return;`、`hashOnly` 分支、`else {}` 包裹），五支演算法零變更、`total_nodes` 在 skipHash 下仍正確累計（`total++` 在 return 之前）。

### （3）自測牙齒 — 5 個 mutation，**4 死 1 活**

| Mutant | 結果 |
|---|---|
| M1 拿掉 `{ includePathGeometry: true }` | **殺**（原始碼斷言紅） |
| M2 選項改成 `false` | **殺** |
| M3 js `canon` 把 `geometry` 排除 | **殺**（「geometry 省略成 "..." 必須改變 tree_hash」） |
| M4 py 端把 `geometry` 排除 | **殺**（js／py 交叉案） |
| **M5 把選項從雜湊走訪移到快照走訪** | **存活——22 組全綠**（見 N1） |

### Finding

**N1（minor，可重現）新增的原始碼斷言有存活突變體** — `scripts/design/overflow-scan.test.js`「LS-171 原始碼斷言」的 regex：

```
/Get\(\(n, c\) => \{[^]*?addLimbs\(hashAcc, fnv1a64\(line\)\);[^]*?\n  \}, (\{[^}]*\})\);/
```

`addLimbs` 之後的 `[^]*?` 會跨過雜湊走訪自己的 `});`、一路吃到**快照走訪**的 `}, { resolveInstances: true });`。失敗情境（我實跑）：把 `{ includePathGeometry: true }` 從雜湊走訪移到快照走訪 → regex 捕到的 options 變成 `"{ resolveInstances: true, includePathGeometry: true }"`、斷言照樣通過、**22 組全綠**，但這正是 LS-171 的 bug 原狀（雜湊走訪又看到 `"..."`，所有含 path 的稿 `tree_hash` 回到 `84420d7b6419b40e`、每張設計 PR 都紅）。

不列 major 的理由：現行程式碼是對的、CI 綠、而且萬一日後真的被改掉，失敗模式是**fail closed 且吵**（收據一律紅），不是靜默放行。

驗證過的一行修法——把斷言改成對雜湊走訪那段**視窗**做檢查：

```js
const win = src.slice(src.indexOf("const hashAcc"), src.indexOf("const abs = {}"));
assert.ok(/includePathGeometry:\s*true/.test(win), "…");
```

我對正本與 M5 各跑一次：正本**通過**、M5 **紅**——確認這個修法擋得住。

**N2（informational）拆兩次讓「同一次掃描」的保證少一小塊** — 現在 `tree_hash` 證明的是**第一次（HASH_ONLY）那一趟的稿態**＝`head_sha` 的 .pen；五支數字來自第二趟。殘餘漏洞只剩一種形狀：**兩趟之間在 Pencil 內做了「不改節點數的 property-only 編輯、而且沒有落地」**——落地了會讓 head_sha 快照的雜湊對不上（紅）、改到節點數會被既有的 `total_nodes` 比對接住（紅），只有「property-only ＋ 不落地」這一格是機械上看不見的，目前靠 ui-designer.md 的「兩次之間不得有任何寫入」約束。建議把 §7 的話術從概括的「同一次 execute」改寫成這個精確的殘餘描述。

**N3（informational）§7 措辭自相矛盾** — 同一列先粗體宣告「**整份收據只能來自末次落地後的同一次 execute**」，緊接著就寫「拆兩次」。建議改成「同一稿態的單次掃描（必要時拆成中間無寫入的連續兩次唯讀 execute）」。`overflow-scan.js` 檔頭與 ui-designer.md 的寫法已經是對的，只有 §7 這一句沒跟上。

**N4（informational）`SCAN_HASH_ONLY` 與 `SCAN_SKIP_HASH` 同時為 true** → 走 hashOnly 分支印出 `SUMMARY-HASH total_nodes=… tree_hash=skipped`（既不算雜湊也不跑五支，純無用輸出）。抄進收據會被 gate 打紅，所以無害；若要防呆，一行 `if (hashOnly && skipHash) Print("WARNING 兩個旗標互斥…")` 即可。

**N5（informational）覆蓋面的邊界要記得** — 本次的「零漂移」結論綁在**這份稿的 8242 個節點**上：若未來 Pencil 對非 `path` 型別也回傳 `geometry`、或對別的欄位啟用同類省略，`includePathGeometry` 就蓋不到。失敗是 fail-closed（收據紅），偵測路徑是 VR 的三方比對——handoff 的盲區段已寫明，不需要動作。

### （5）誤擋面 — 十份既有收據我自己跑過

LS-142 r1–r7 @`4b8e97d` **7/7 綠**；LS-152 r1–r3 @`7197b8d` **3/3 綠**（r3 head_sha=`92e0669`、total_nodes=8242、本輪最新，走舊收據放行）。本票沒有動任何 gate 腳本（`git diff --name-only df95691..ecb84c0 -- scripts/gates/design-evidence-check.sh …` ＝ 0 檔），所以這是回歸確認：**沒有任何一份被新程式碼波及**。三支自測全綠：js 22 組、evidence 41 案、notes 17 案。

### 四維度

race：無並發；兩趟 execute 之間的「不得寫入」是流程約束、不是程式競態（見 N2）。效能：雜湊走訪的成本不變（我在 LS-168 R1 量過 7746 節點 43ms；本票 8242 節點同級），拆兩次是為了繞開 Pencil 的 `InternalError: interrupted`，不是效能優化。平行：無。scope：7 檔全部對應票文 1–4，`design_tree_hash.py` 只補 docstring、gate 腳本零變更；兩個旗標的擴張如上判定可接受，實作者主動自報，處理方式正確。


### 2026-09-04T08:49:17.153Z · Chih-Lin Yeh · `646d33df`

## handoff — LS-171 R2（merge-review R1 `6d71d7a5` 修正輪，2026-09-04 16:48 CST）

**Ticket**：LS-171。PR **#273**，head **`77cc0e9`**（R1 head `ecb84c0` 之後 2 支 commit：`5a9584a` N1＋N3＋N4、`77cc0e9` N3 補一句）。CI 四項綠：ci 8m44s／db 3m08s／lint 20s／rules 3m26s（run 33854452845）。PR body 於 08:38:47Z 更新（pr-body-check ✓：LS-96 行 3 條皆帶 comment id、「已修」行 4 條皆帶 SHA），**在最後一支 push（16:38:55 CST）之前**就位。

### 已完成（R1 finding 逐條）

| # | 處置 | 位置 |
|---|---|---|
| **N1**（minor）原始碼斷言的 `[^]*?` 跨過雜湊走訪、M5（把選項搬到快照走訪）存活 | **已修** `5a9584a` | `overflow-scan.test.js`：改看視窗 `src.slice(indexOf("const hashAcc"), indexOf("const abs = {}"))`，斷言視窗內有 `addLimbs(hashAcc, fnv1a64(line))`、有 `includePathGeometry:\s*true`、**無** `resolveInstances`；另加「`hashOnly && skipHash … throw new Error`」斷言 |
| **N2**（informational）拆兩次後殘餘：兩趟之間 property-only 且未落地的編輯看不見 | **記入 LS-96 待辦池 `05be01f4`** | 池文含機械補法（第二趟輕量重算 `tree_hash_check`、收據兩值須相等）與未做理由（第二趟多一次雜湊走訪正是 interrupted 成因，需 Pencil 實測）；§7／ui-designer.md 已把這個精確殘餘寫進去 |
| **N3**（informational）§7 與 ui-designer 「同一次 execute」和拆兩次矛盾 | **已修** `5a9584a`＋`77cc0e9` | ui-designer.md:32「整份收據只能來自末次落地後、同一稿態的單次掃描（一次 execute；必要時拆成中間無任何寫入的連續兩次唯讀 execute）」＋寫明 `tree_hash` 證明第一趟稿態＝`head_sha` 快照、五支來自第二趟；ui-designer.md:85 handoff 清單同句同步（拆兩次時 `SUMMARY-HASH` 與 `SUMMARY` 兩行都貼）；COLLABORATION §7 LS-171 段加「單次掃描的定義」。`grep "同一次 execute"` 兩檔皆 0 |
| **N4**（informational）兩旗標同時 true 印無用的 `tree_hash=skipped` | **已修** `5a9584a` | `overflow-scan.js`：`if (hashOnly && skipHash) throw new Error("…互斥…")`（execute 失敗、不印任何 SUMMARY）；檔頭同步 |
| **N5**（informational）覆蓋面綁在當前稿 8242 節點 | **記入 LS-96 待辦池 `05be01f4`** | 池文含預防性偵測做法（雜湊走訪對字面 `"..."` 值印 WARNING）與未做理由（目前無此情況） |

### 已驗證（怎麼驗）

- `overflow-scan.test.js` 22 組全綠。**mutation 三支全殺**：M5（`{ includePathGeometry: true }` 從雜湊走訪搬到快照走訪 `{ resolveInstances: true, includePathGeometry: true }`）→ 紅「雜湊走訪的 Get 必須帶 includePathGeometry: true…」（R1 前存活）；M1（拿掉選項）→ 同訊息紅；M6（拿掉 throw 那行）→ 紅「SCAN_HASH_ONLY 與 SCAN_SKIP_HASH 同時為 true 必須 throw（R1 N4）」。改回後全綠。
- R2 未動雜湊走訪與五支（`overflow-scan.js` 只加 throw 一行＋檔頭一句），Pencil 端三方數字（`7197b8d` `03e7804b035d8e4b`、`b0b42ad` `1d66044f7af83f8a`）不變；`node require` 過；evidence／notes 自測未受影響（未改）。
- `git diff origin/main..HEAD`：9 檔（含 R1）+95/−18，無刪除；worktree 乾淨。

### 未完成／盲區

- N2／N5 只入池未修（`05be01f4`）。
- N4 的 throw 只有原始碼斷言釘住，沒有在 Pencil 實跑「兩旗標同時 true」（read-only 但會讓 execute 失敗一次，無必要）。
- 其餘同 R1 handoff（未重現 interrupted 本體；`variables`／`themes` 不在雜湊內）。

### 風險

- 同 R1：LS-152 R4 收據要在本 PR 併 main → back-merge development → LS-152 併 development 之後用新腳本重跑才會驗新鮮度；順序不對不誤紅。

### 產出位置

- PR #273 `https://github.com/CLYEH/little-sprout/pull/273`，head **`77cc0e9`**（`d794e3f` → `ecb84c0` → `5a9584a` → `77cc0e9`），body 更新 2026-09-04 08:38:47Z，四項 CI 綠（run 33854452845）。
- worktree `.claude/worktrees/LS-171`（乾淨）；scratchpad `LS-171-pr-body.md`（R2 版）、`LS-171-r2-jstest.txt`／`-M5.txt`／`-M1.txt`／`-M6.txt`。
- LS-96 池文 comment `05be01f4`（N2／N5）。

### 2026-09-04T08:53:13.835Z · Chih-Lin Yeh · `95bd0076`

## merge-review R2 — PR #273（head 77cc0e9，增量 ecb84c0..77cc0e9）

裁決：**APPROVE**（無 blocker／無 major）。R1 的 **N1／N3／N4 三條全部修對且我自己複驗過**，N2／N5 入池 `05be01f4` 同意——而且 N2 不只入池，§7 還把殘餘寫成正文（比我建議的更完整）。新增 1 條 informational（R2-I1）。審的 head＝`77cc0e9ffb8a073e2b27dd39fd85934406828d32`，與 `gh pr view 273 --json headRefOid` 當下值相同；增量 4 檔 +15/−8，CI 四項 pass（run 33854452845）。2026-09-04 16:52 CST。只審增量：`overflow-scan.js` 的非註解變更**只有那一行 `throw`**（我用 `git diff | grep -vE '^[+-]//'` 機械確認），雜湊走訪與五支演算法零變更 → R1 對 `includePathGeometry`／geometry 覆蓋面的結論原封不動有效。

### （1）我 R1 的存活突變體 M5 — 已被殺，另外三個方向也殺

正本自測 22 組全綠。四個 mutant 各跑一次（乾淨副本、逐次還原）：

| Mutant | 新斷言的反應 |
|---|---|
| **M5**（R1 存活者）選項從雜湊走訪搬到快照走訪 | **紅**「雜湊走訪的 Get 必須帶 includePathGeometry: true…」 |
| M1 直接拿掉選項 | **紅**（同上） |
| M6 拿掉雙旗標 `throw` | **紅**「SCAN_HASH_ONLY 與 SCAN_SKIP_HASH 同時為 true 必須 throw（R1 N4）」 |
| **M7（我這輪新加）**把 `resolveInstances: true` 加進雜湊走訪 | **紅**「雜湊走訪不得展開 instance（total_nodes 語意）」 |

M7 值得特別講：實作者多加的 `assert.ok(!/resolveInstances/.test(win))` 不只是把 M5 換個方向堵住，它連帶釘住了「雜湊走訪必須是未展開 instance」這個 `total_nodes` 語意前提——那是我 R1 沒想到要求的。視窗法本身的誤擋方向也是安全的：若日後有人改掉 `const hashAcc`／`const abs = {}` 這兩個錨點，`assert.ok(a > 0 && b > a)` 與 `win.includes("addLimbs(...)")` 會先紅並指名「找不到雜湊走訪的視窗」，不會靜默放過。

### （2）N3 措辭 — 兩份文件＋§7 已一致，但 gate 的錯誤訊息還留著舊句

- 「**整份收據只能來自末次落地後的同一次 execute**」這句絕對宣告在 `docs/COLLABORATION.md` 已 **0 次**（我 grep 過）。
- `ui-designer.md:32`：「整份收據只能來自末次落地後、**同一稿態的單次掃描**（一次 execute；必要時拆成中間無任何寫入的連續兩次唯讀 execute，見下）」——定義與例外寫在同一句，不再自相矛盾。
- `ui-designer.md:85` handoff 清單也跟上了：「拆兩次時 `SUMMARY-HASH` 與 `SUMMARY` 兩行都貼」。
- `COLLABORATION.md` §7 新增「**單次掃描」的定義（R1 N3）**」整段，而且把我 R1-N2 的殘餘**精確寫進正文**：「收據 `tree_hash` 抄第一趟：它證明的是第一趟稿態＝`head_sha` 快照，五支數字來自第二趟；兩趟之間 property-only 且未落地的編輯機械上看不見」。這比單純入池好——讀規約的人當場就看得到保證的邊界。

**R2-I1（informational）唯一殘留的舊措辭在 gate 的使用者面訊息** — `scripts/gates/design-evidence-check.sh:424`（本票沒動 gate 腳本，屬 scope 外的殘留）：`tree_hash` 不符時印的字串仍是「…末次落地後重跑正典腳本、**整份收據只能來自同一次 execute**（LS-168…）」。這正是設計端**失敗當下唯一會讀到的一句話**，而它與現行核可的拆兩次流程字面衝突——照著它做的人會以為自己拆兩次跑錯了。建議下次動 gate 時把該字串改成 §7 的新定義（「同一稿態的單次掃描；拆兩次時 `tree_hash` 抄第一趟」）。純文字、無行為影響，不必為它另起一輪。

### （3）`throw` 會不會誤傷正常單次跑 — 用 stub `Get`／`Print` 實跑四種組合

我寫了一支模擬器餵進 `overflow-scan.js` 的 Pencil 分支（合成 3 節點稿，含一個帶 `geometry` 的 path），逐一記錄每次 `Get` 收到的 options：

| 模式 | throw | `Get` 收到的 options | Print |
|---|---|---|---|
| **兩旗標皆未設（正常單次跑）** | **無** | `[{"includePathGeometry":true}, {"resolveInstances":true}]` | `SUMMARY …` ＋五段彙整，正常 |
| 只設 `SCAN_HASH_ONLY` | 無 | `[{"includePathGeometry":true}]`（只走一次） | `SUMMARY-HASH total_nodes=3 tree_hash=4e98e54243fb4af4` |
| 只設 `SCAN_SKIP_HASH` | 無 | 兩次走訪 | `SUMMARY … tree_hash=skipped` ＋五段彙整 |
| 兩個都設 | **throw**（訊息指名互斥與正確用法） | `[]`（在任何 `Get` 之前就擋下） |

**正常單次跑完全不受影響**（`hashOnly && skipHash` 兩者皆 false，短路）。附帶收穫：這次模擬是**執行期**證據——雜湊走訪在 runtime 真的收到 `{includePathGeometry:true}`、快照走訪收到 `{resolveInstances:true}`，比原始碼斷言更貼近真實行為（R1 我只驗到原始碼層）。

### 其他

- 自測全綠：js 22 組、evidence 41 案、notes 17 案。
- 誤擋面不需重驗：本增量沒有動任何 gate 腳本與掃描邏輯（唯一非註解變更是 throw 那行），R1 已驗的十份既有收據不受影響。
- 四維度：race／平行無可審之處；效能——`throw` 是啟動時的兩個布林短路，零成本；scope——4 檔全部對應 N1／N3／N4，無順手改動。


### 2026-09-04T08:57:16.095Z · Chih-Lin Yeh · `9ba74797`

## dead-code sweep — LS-171

裁決：**未發現本 feature 引入的死碼**。2026-09-04 16:56 CST，於主 checkout `8ab54b1`（merge commit）核對 `git diff df95691..77cc0e9`（7 檔 +89/−12）。

### 逐檔核對

1. **`scripts/design/overflow-scan.js`**（+21/−3）——新旗標 `SCAN_HASH_ONLY`／`SCAN_SKIP_HASH`：
   - 兩者都在 `overflow-scan.test.js` 的「LS-171 原始碼斷言」案被 grep 進去驗存在（`src.includes("SCAN_HASH_ONLY") && ...`），且 `hashOnly && skipHash` 互斥 throw 分支被同一案的 regex 斷言釘住（`/hashOnly && skipHash[^]*?throw new Error/`）。
   - 兩旗標的**執行路徑**（`hashOnly` 印 `SUMMARY-HASH`、`skipHash` 跳過雜湊走訪）只在 Pencil `execute` 內觸發（`typeof Get === "function"` 分支），純函數自測跑不到——這是設計上如此（人工在 Pencil snippet 手動設旗標，見 `ui-designer.md:32`），不是遺留旗標找不到呼叫點。merge-review R2 已用 stub `Get`/`Print` 實跑四種旗標組合驗證過執行期行為（正常單次跑不受影響、hashOnly-only／skipHash-only／兩者皆設分別對應正確輸出），不重複驗證。
   - 檔頭新增段落（19–24 行、69–72 行）與 `docs/COLLABORATION.md` §7 LS-171 段內容重疊，但這是本專案既有慣例（`overflow-scan.js` 檔頭面向讀腳本的人、COLLABORATION §7 面向要掌握全貌的 reviewer/orchestrator，LS-168／LS-122／LS-127 等段都是同一模式重複記載）——不算新引入的文件矛盾或殘留，經比對兩處措辭一致（`grep -n "SCAN_HASH_ONLY\|SCAN_SKIP_HASH"` 兩處字面相容）。

2. **`scripts/design/overflow-scan.test.js`**（+34/−1）——新 fixture `Pp5kQ`（帶 geometry 的 path）與 `Sq9uR`（空 geometry 的 path）：
   - `Pp5kQ`＝`doc.children[2]`：被「改一個字元」負樣本（`e.children[2].geometry += "z"`）與 py／js 交叉一致案（`--dump Pp5kQ`／`canonNode(doc.children[2], null, 2)`）兩處引用。
   - `Sq9uR`＝`doc.children[3]`：被空字串 geometry 斷言（`canonNode(doc.children[3], null, 3)` 含 `"geometry":""`）引用。
   - 兩個 fixture 都有至少一個斷言消費，非孤兒資料。

3. **`scripts/gates/design-evidence-check.test.sh`**（+36/−4）——新函數 `dots_hash_of`：只在 ㉖-b 一處呼叫（`dots26="$(dots_hash_of "$sha26")"`），單次呼叫但目的明確（模擬 Pencil 端漏帶 `includePathGeometry` 的雜湊，與既有 `hash_of` 併存、職責不同），非重複造輪也非孤兒函數。`pen4` fixture 改成帶 `geometry` 的 path 節點，同一批既有引用點（㉗／㉝ 的落地內容）同步更新，未見遺漏。

4. **`docs/COLLABORATION.md`／`.claude/agents/ui-designer.md`／`.claude/agents/visual-reviewer.md`**——三處新增措辭彼此一致（「同一稿態的單次掃描」定義、拆兩次流程、`includePathGeometry` 要求），`grep "同一次 execute"` 在 ui-designer.md／COLLABORATION.md 兩檔皆 0 次（R2 已修正 R1-N3）。

5. **`scripts/gates/design_tree_hash.py`**（+2/−0）——只補一句 docstring 說明 Pencil 端須帶 `includePathGeometry`，對應既有 `canon`／`tree_hash` 函數簽名核對無誤，無新增未用符號。

### 自測與殘留檢查

- `node scripts/design/overflow-scan.test.js`：22 組全綠。
- `bash scripts/gates/design-evidence-check.test.sh`：41 案全綠，含新增 ㉖-b。
- `git status --short`（唯讀執行後）：乾淨，無 `__pycache__`／`.pyc` 殘留（`find scripts -name "__pycache__" -o -name "*.pyc"` 皆空）。

### 已知殘留（不重報）

`scripts/gates/design-evidence-check.sh:424` 的錯誤訊息仍寫「整份收據只能來自同一次 execute」，與新定義「同一稿態的單次掃描（可拆兩次）」字面不一致——**這是 R2-I1 已知項，已入池，本次不重複列為 finding**。

### 既有死碼觀察

無（本次巡檢範圍內未發現與本 feature 無關的既有死碼線索；未做全庫掃描，僅比對本 diff 觸及檔案）。

### 建議處置

無需開票。R2-I1 已按既有流程處理（入池，等下次動 gate 腳本時順手改）。

### 搜尋方法與盲區聲明

方法：`git diff df95691..77cc0e9 --stat`／逐檔 diff、`grep -n` 交叉比對旗標與 fixture 引用點、跑兩支自測套件驗證所有新增分支/斷言確實被執行到綠燈、`find` 掃 `__pycache__`／`.pyc`。全程唯讀（未 checkout／stash／寫入任何檔案）。

盲區：純文字搜尋抓不到 Pencil `execute` 內動態組出的字串或 reflection；`SCAN_HASH_ONLY`／`SCAN_SKIP_HASH` 的真實 Pencil 執行路徑本次未重新在 Pen 內實跑（僅信賴 merge-review R2 的 stub 模擬與原始碼斷言），若 Pencil `Get` 對這兩個旗標的實際行為與模擬不符，本次巡檢無法發現。

### 2026-09-04T08:57:52.936Z · Chih-Lin Yeh · `2a409af0`

orchestrator 2026-09-04 16:57 **收尾 lesson learning review**（PR #273 → main `8ab54b1`；back-merge PR #274 CI 中）：

- merge-review R1 `6d71d7a5` APPROVE（minor N1 斷言視窗）→ R2 修 N1／N3／N4、N2／N5 入池 `05be01f4` → R2 `95bd0076` APPROVE（R2-I1 舊錯誤訊息入池 `24f7c599`）。sweeper `9ba74797`：0 finding。
- Harness 教訓：(1) **Pencil 端演算法只能由持 Pen 的 agent 驗**，LS-168 兩輪 review 都無法自跑 Pencil，靠 VR R3 才發現 `Get` 省略 `geometry`——harness 票凡含 Pencil 端片段，票文要明列「VR 三方比對」為驗收項並排進最近的設計票 VR 輪（本票已做）；(2) 「原始碼斷言」型自測要用**視窗**而非全文 regex，否則等同還原 bug 的重構會通過（N1／M5）；(3) reviewer 以 stub 模擬 Pencil 分支取得 runtime 選項傳遞證據，是無 Pencil 工具時可行的替代法，記入 merge-reviewer 慣例候選。
- 設定優化：無。工具缺口：merge-reviewer 定義無 `mcp__pencil__*`（刻意，避免寫入）；維持。
- cleanup：worktree／本機分支已清（`cleanup-merged.sh --apply LS-171`）。

→ PR #274 併入 development 後改 Done；同時通知 LS-152 designer 併 development 重出 r5 收據。

### 2026-09-04T09:09:13.287Z · Chih-Lin Yeh · `a4981eb2`

orchestrator 2026-09-04 17:08：back-merge PR #274 併入 development（merge commit `5fbdc4b`，`git diff origin/main origin/development` 為空；head `8ab54b1` 貼 `merge-review=success`「promote: no content diff」status `53531058917`）。收尾儀式完成 → **Done**。LS-152 designer 接著併 development 重出 r5 收據（VR R4 附帶條件）。

