# LS-199 Harness：push-gate `xcodebuild test` 看門狗——逾時（預設 25 分）與「test runner hasn't connected」／宿主 crash 偵測即中止、印 crash report 路徑與 xcresult session log 尾、釋放 simulator-lock 並 shutdown 專屬機；LS-197 R2 push 卡 28 分人工 kill

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-05T07:19:35.896Z |
| 開始 | 2026-09-05T07:32:03.844Z |
| 完成 | 2026-09-05T09:39:58.812Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-199/harnesspush-gate-xcodebuild-test-看門狗-逾時預設-25-分與test-runner-hasnt |
| 關係 | related→LS-152, related→LS-163, related→LS-177, related→LS-194, related→LS-76, related→LS-95, related→LS-96, related→LS-197, related→LS-100, related←LS-239, related←LS-236, related←LS-209, related←LS-200 |

## 描述

## 來源

2026-09-05 15:07：[LS-197](https://linear.app/little-sprout-app/issue/LS-197/taskls-23-後端-eula-同意紀錄app-settingseula-version-當前版本profileseula) R2 `git push` 的 push gate 在 14:39 測試宿主 app 啟動即 crash（xcresult session log「Handling Crash: LittleSprout at <external symbol> … Dropping test runner session call … because the test runner hasn't connected yet」；crash report `~/Library/Logs/DiagnosticReports/LittleSprout-2026-09-05-143904.ips`：`SupabaseClientFactory.makeClient()` 的 XCTest 偵測 assert——XCTest 未注入），之後 `xcodebuild` 0% CPU 掛 28 分鐘，agent 只能乾等「背景 push 完成通知」，orchestrator 人工 kill 後重跑即過（環境性 flake）。[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項 `77a589de`（High）。同型風險：任何 lane 的 push 都走這條 gate，一卡就是整條 lane 停擺。

## 範圍

1. `scripts/gates/push-gate.sh`（或其呼叫的 xcodebuild 包裝）：`xcodebuild test` 加逾時（預設 25 分，`PUSH_GATE_XCODEBUILD_TIMEOUT_MIN` 可調；CI 不受影響——CI 有 workflow 級 timeout），逾時 → kill xcodebuild 行程樹、印「逾時」＋最近 xcresult 的 session log 尾 20 行＋`DiagnosticReports` 最新 `LittleSprout*.ips` 路徑與 exception 摘要、exit 非 0。
2. 早期偵測：xcodebuild 輸出串流中出現 `test runner hasn't connected yet`／`Handling Crash:`／`Early unexpected exit` 等樣式 → 等 60 秒無 test case 開始即視為宿主 crash，同上處理（不等到逾時）。
3. 收尾一致：無論逾時或 crash，`simulator-lock` 釋放、專屬機 `shutdown`（沿 [LS-100](https://linear.app/little-sprout-app/issue/LS-100/harness模擬器用完必關-ios-devqa-任務結束-simctl-shutdownpush-gate-測完關專屬機巡檢列出)），不留 Booted 殘機；印一句「可能是環境性 flake，建議先 `xcrun simctl erase &lt;udid&gt;` 再重跑」。
4. 自測 `push-gate.test.sh`（stub `xcodebuild`：一支睡到逾時、一支印 crash 樣式後掛住、一支正常）三案＋lock／shutdown 被呼叫的斷言；COLLABORATION §7 對照表該列更新；agent 定義（ios-dev／qa）加一句「push gate 若印逾時／crash，先看它印的 crash 摘要再決定重跑或修」。

## 不做

改 `SupabaseClientFactory` 的 assert；CI workflow 的 timeout；xcodebuild 重試（重跑決定權留給 agent）。

## 驗收

- [ ] stub 三案自測綠；逾時案 kill 後 exit 非 0 且 lock 釋放、shutdown 被呼叫（自測）
- [ ] 真環境正常 push 一次不受影響（耗時無明顯增加）
- [ ] merge-reviewer 通過；hotfix 流程（自 main、PR 回 main、back-merge）

## 留言（4）

### 2026-09-05T08:12:34.104Z · Chih-Lin Yeh · `1c3d36b9`

Ticket：LS-199

已完成：
1. `scripts/gates/push-gate.sh` 第 2 步加 `wd_run` 看門狗（`cd1971d`）：`xcodebuild test` 背景執行＋每秒輪詢。(1) 逾時 `PUSH_GATE_XCODEBUILD_TIMEOUT_MIN` 預設 25、`PUSH_GATE_XCODEBUILD_TIMEOUT_SEC` 秒級覆寫供自測、`GITHUB_ACTIONS=true` 不啟用；(2) 早期偵測：xcodebuild 輸出或本 worktree 的 xcresult Staging session log 出現 `test runner hasn't connected yet`／`Handling Crash:`／`Early unexpected exit` 後 `PUSH_GATE_CRASH_GRACE_SEC`（預設 60）秒內無 `Test Case '-[` 開始即視為宿主 crash；(3) 中止：先抓 session log 尾 → 先收集子孫再 TERM 整棵行程樹、3 秒後 KILL 殘留 → 回收自己那把 simulator-lock（holder pid 是剛被殺的子孫才收）→ 印「逾時／宿主 crash」＋session log 尾 20 行＋最新 `LittleSprout*.ips` 路徑與 python 解析的 exception／termination／faultingThread 前三幀＋「環境性 flake，建議 `xcrun simctl erase <udid>` 後重跑」→ exit 124；EXIT trap 沿 LS-100 收尾關專屬機（不 shutdown all）。看門狗期間 INT／TERM 先殺樹再 exit（bash 對背景命令預設忽略 SIGINT）。
   - **與票面字面的一個刻意差異**：session log 不用全域 `DerivedData/*` glob，只認 `info.plist` 的 `WorkspacePath` 落在本 repo 根之下的 DerivedData 目錄、且檔案晚於看門狗啟動——多 worktree 併發 push 時全域 glob 會把別票的 crash 當自己的、殺掉還在編譯的健康 run。對應不到就只看 xcodebuild 輸出、診斷印「找不到」。
   - 事實依據：`-quiet` 下掛住時 xcodebuild 什麼都不印；Xcode 測試進行中就把 `Session-*.log` 寫在 Staging、跑完才打包（LS-197 的 Handling Crash 就在那裡）；健康 run 同一份 log 有每個 `Test Case '-[…]' started.`（以 `xcresulttool export diagnostics` 對真實 xcresult 匯出確認，匯出檔已刪）。
2. `push-gate.test.sh` ㉘～㉟ 八案（`6314f9d`）：stub xcodebuild 加 `STUB_TEST_SCRIPT`（掛住並記 pid／印 crash 樣式／寫假 Staging session log／touch 假 .ips），HOME 換假家目錄（兩份假 DerivedData＋兩行 JSON 假 .ips）；逾時含診斷、逾時無來源印「找不到」、session log crash（LS-197 形狀）、stdout crash、假警報取消、別 worktree 的 crash 不觸發、CI 不啟用、非法設定擋；中止案共用斷言 exit 124／專屬機 shutdown／鎖目錄不在／假身與其 sleep 子行程皆死／耗時上限。`run_gate` 清空 `GITHUB_ACTIONS`（CI rules job 上否則掛住假身會掛到永遠）。
3. 文件（`43c74d4`）：COLLABORATION §4 Push gate 列補一段、§7 新增一列（含盲區）；`ios-dev.md` 硬規則加一條、`qa.md` 驗收流程第 2 步補一句。`agent-tools-check.test.sh` 沒釘 push-gate 字樣，無需同步。

已驗證：
- `bash scripts/gates/push-gate.test.sh`（bash 5.3）73/73 綠（既有 38＋本票 35）。
- `/bin/bash` 3.2（pre-push hook 實際用的殼）71/72：唯一紅的 ⑬「DEVELOPER_DIR 沒有接線」在未改動的 HEAD 複本下同樣紅（devdir_log 同樣為空）——既有、與本票無關，見「未完成」。
- mutation（scratch 複本）：(a) 拿掉 crash 樣式掃描 → ㉚㉛ 走逾時 30～32 秒、㉜ 無「取消」行，三案紅；(b) 拿掉 `WorkspacePath` 比對 → ㉝ 紅（exit 124，別票的 crash 殺掉健康 run），其餘 71 綠。
- 自測抓到、commit 前修正：無 .ips 時 `ls | head` 在 pipefail＋set -e 下讓賦值直接結束腳本、診斷全沒印（㉙）；bash 3.2 的 `[ -nt ]` 只比到秒 → `find -newer`（㉘）；`Logs/Test` 尚未存在時 `find` 非 0 會讓掃描子殼整個結束（mutation (b) 揭露）。
- 真環境 push 一次：`git push` 含 push gate 共 **8 秒**、通過（hotfix 對 origin/main 無 Swift 變更 → 走 LS-76「無 Swift 變更」路徑；xcodegen 漂移／契約／錯誤碼／migration／票號／衝突各步照跑）。**看門狗路徑本身沒有在真環境被走到**，第一次真的包住 xcodebuild 會是 back-merge 後任一 lane 的 Swift 變更 push。
- 全程未 create／boot／shutdown 真實模擬器、未碰 Pen／容器／正式站；Linear 只寫本票。

未完成／剩餘：
- PR #314 首次送審，尚無 merge-reviewer finding；reviewer 每一條 informational 屆時逐條處置（已修附 SHA＋檔案:行號／記入待辦池 LS-96 附 comment id／另票（限 High 以上）／不修＋理由）。
- 觀察（非本票範圍、Linear 只讀故未自行記池，請 orchestrator 裁定是否記入待辦池）：`push-gate.test.sh` ⑬ 在 `/bin/bash` 3.2 下紅（HEAD 亦然），CI 在 ubuntu bash 5 跑所以從沒顯現；本機以 `bash` 執行時是 homebrew 5.3 所以也沒人看到。
- 票面驗收第 3 條（merge-reviewer 通過；hotfix 併回 main 後 back-merge main→development）待 orchestrator。

風險與已知問題：
- 盲區（已寫進 §7）：LS-95 `tap-target-check.sh` 的 XCUITest 是另一次 xcodebuild 呼叫，未包看門狗；`-quiet` 下早期偵測實質依賴 session log，`WorkspacePath` 形狀若被 Xcode 改掉會退化成「只看輸出＋逾時」（不誤殺、只變慢）。
- 正常路徑多一層背景子殼＋`tee` 串流，輸出量在 `-quiet` 下極小；輪詢成本可忽略。
- 無 migration、無 Swift 變更、無 BREAKING。

產出位置：PR #314 https://github.com/CLYEH/little-sprout/pull/314（base `main`），branch `hotfix/LS-199-push-gate-watchdog`，head `43c74d4c66c6689b4ae6e53c7d065129bd3f7f4c`（3 commits：`cd1971d` gate／`6314f9d` tests／`43c74d4` docs）。模擬器已關：無（未 boot 任何一台）。

### 2026-09-05T08:39:19.319Z · Chih-Lin Yeh · `cc04d731`

## merge-review R1 — **APPROVE**（PR #314，head `43c74d4c66c6689b4ae6e53c7d065129bd3f7f4c`，base `35756a3`；2026-09-05 16:37）

範圍：5 檔 +367/−11（`push-gate.sh` +186／自測 +177／文件 3 檔）。CI 四項全 pass（run 33954589894）。**無 blocker／major**；**3 條 informational**＋對 ⑬ 的裁定。

### 審點 1：殺樹安全 — 我自己造兩棵併發假 push 實測過

把 `wd_descendants`／`wd_kill_tree` 原文抽出來、造兩棵獨立的「父 shell → 兩個子行程」樹（模擬兩個 worktree 同時 push）：

```
treeA=33021 33025 33026     treeB=33023 33027 33028
after killing A: A alive=0 (want 0) / B alive=3 (want 3)   → RESULT OK isolated
cleanup: B leftover=0
```

**只殺自己那棵、併發那棵三個行程全活著** ✓。程式碼層面我另外確認：
- `wd_killed_pids="$1 $(wd_descendants "$1")"` **先收集再 TERM** ✓（我加做的 mutation (c)「先殺父再收集」→ **8 條紅**，這個順序確實被測住）。
- **lock 回收只收自己那把**：`case " $wd_killed_pids " in *" ${holder} "*` ＋ `kill -0` 雙條件；`sim_lock_dir` 以 UDID 為鍵、專屬機每票不同 → 別票的鎖碰不到 ✓（測試盲點見 info-2）。
- EXIT trap 沿 LS-100 只關**專屬機**、且「鎖仍在就跳過」的第二道防線未被動到 ✓（㉘／㉙ 斷言 shutdown 有被呼叫）。
- `tee` 是 `>(…)` 程序替換、屬**父殼**的子行程不在被殺樹內，但寫端關閉後自然結束，不會殘留 ✓。

### 審點 2：早期偵測來源 — **同意實作者偏離票面全域 glob**

票面寫「最近的 xcresult」，實作改成「`info.plist` 的 `WorkspacePath` 落在本 repo 根之下、且檔案晚於看門狗啟動」。我實測 `~/Library/Developer/Xcode/DerivedData/*/info.plist` 的真實形狀確認可行：
`<string>/Users/clyeh/little-sprout/.claude/worktrees/LS-164/LittleSprout.xcodeproj</string>`——每個 worktree 各一份，全域 glob 確實會把別票的 crash 當自己的。**mutation (b)（拿掉 WorkspacePath 比對）→ ㉝ 紅（「別票的 crash 被當成自己的（exit 124）」）**，我重跑復現 ✓。偏離合理、且比票面字面更安全，**裁定：接受**。
- `find -newer "$wd_stamp"`（不是 `[ -nt ]`）避開 bash 3.2 秒級比較 ✓；`Logs/Test` 尚未存在時 `find` 非 0 由 `|| true` 吸收、不會讓掃描子殼在 `set -e` 下整個結束 ✓（實作者自述被 mutation (b) 揭露，我讀碼確認）。
- `-quiet` 下無輸出時退化成「只看 session log＋逾時」，找不到來源就印「找不到」而**不中止**（㉙ 釘住）✓ 不誤殺。

### 審點 3：正常路徑零回歸

- exit code 不被 `tee` 吞：子殼內 `"$@" || wd_rc=$?` 寫進 `rc` 標記檔，父殼 `wait` 後 `cat` 回傳 ✓（不靠 `PIPESTATUS`／不靠 `kill -0`，zombie 也不會誤判）。
- INT／TERM 轉發成「先殺樹再 exit 130／143」✓（bash 對背景命令預設忽略 SIGINT 的坑有處理）。
- `GITHUB_ACTIONS=true` 直接 `"$@"; return`（CI 不啟用）✓，自測有樣本。
- 真環境 8 秒通過是「無 Swift 變更」路徑（handoff 自述），**看門狗本身沒被真的走到**——這點我照抄他的揭露，沒有自己再跑一次真 push。

### 審點 4：自測鑑別力

- **`bash` 5.3：73/73 綠、exit 0**（74 秒）。
- **`/bin/bash` 3.2.57（pre-push hook 實際用的殼）：73/73 綠、exit 0**（64 秒）——**⑬ 在我的環境沒有紅**（`DEVELOPER_DIR` 未設）。
- **mutation 重做**：(a) 拿掉 crash 樣式掃描 → **5 條紅**（㉚／㉛ 改走逾時、耗時 31s 超過 15s 上限）✓ 與宣稱一致；(b) 拿掉 `WorkspacePath` 比對 → **㉝ 1 條紅** ✓；我另加 (c) 先殺父再收集子孫 → **8 條紅**；(d) 讓 lock 回收不檢查 holder 是否為被殺子孫 → **存活**（見 info-2）。
- **⑬ 的裁定**：**不建議另票、也不建議本票順修**——我在 LS-199 worktree 以 `/bin/bash` 3.2.57 連跑兩次都是 73/73 綠，`DEVELOPER_DIR` 未設。⑬ 驗的是「pin 的 Xcode 存在時 `DEVELOPER_DIR` 有接線到每次 xcodebuild 呼叫」，會受呼叫者環境（是否已 export `DEVELOPER_DIR`）影響。實作者若能重現，請附當時的 `env | grep DEVELOPER_DIR`；在此之前沒有證據顯示「某條路徑在 3.2 下行為不同」，把它記成待查會製造假債。

### 審點 5

- 文件三處與行為一致 ✓：`ios-dev.md` 硬規則、`qa.md` 驗收流程第 2 步、COLLABORATION §4 Push gate 列＋§7 新列（含盲區：LS-95 的 XCUITest 那次 xcodebuild 未包看門狗、`-quiet` 下實質依賴 session log）。
- 逐 commit 歸屬乾淨（`cd1971d` 只有 gate／`6314f9d` 只有自測／`43c74d4` 只有三份文件）、**無夾帶**；worktree 乾淨。
- PR body `--branch hotfix/LS-199-push-gate-watchdog --verify` → **exit 0**。
- head 未前進（貼 status 前 `--expect` 再比一次）；CI `ci` 6m8s／`db` 3m38s／`lint` 11s／`rules` 4m17s 全 pass。

### informational

**info-1 · 從「主 checkout」跑 push gate 時 WorkspacePath scoping 會退化**：`wd_repo_root=$(pwd -P)`＋`grep -qF "<string>${wd_repo_root}/"` 是**前綴比對**；主 checkout 的根是 `/Users/clyeh/little-sprout`，而所有 worktree 的 `WorkspacePath`（實測形狀如上）都以它開頭 → 從主 checkout 觸發的 push（例如 `promote.sh` 推 test／main 時若相對 target 有 Swift 變更）會把**所有 worktree 的 session log 都當成自己的**，剛好是這個過濾要防的方向。窗口不大（還要求檔案晚於看門狗啟動＝同時段有別票在 crash），且後果是「誤判自己 crash → 中止一次 promote push」，不會誤殺別人的行程。修法一行且不會弄壞現有 fixture（自測的假 plist 是 `<repo>/Fake.xcodeproj`）：把比對改成「根之後不得再有 `/`」，例如 `grep -qE "<string>${wd_repo_root}/[^/<]*\.xc(odeproj|workspace)<"`。

**info-2 · mutation (d) 存活：lock 回收的「holder 必須是剛被殺的子孫」沒有樣本**。把該條件改成恆真後 73 組全綠。實際風險有限——`kill -0 "$holder"` 仍擋著「活著的持有者」，所以退化後的差別只是「會順手回收別的 worktree 留下的**死鎖**」（多半良性，但會跟 `simulator-lock.sh` 自己的死鎖回收邏輯搶）。補一組樣本（假 holder pid 指向一個活著的無關行程、斷言鎖目錄還在）即可，價值中等。

**info-3 · `wd_kill_tree` 的子孫清單只收集一次**：TERM 之後、3 秒後的 KILL 用的是同一份清單，在 TERM 與 KILL 之間新生的孫行程兩次都不會被殺。對「已經掛住 0% CPU」的 xcodebuild 幾乎不可能發生（它不會再 fork），但 KILL 前重新收集一次是零成本的加固。

### 沒看／沒跑的（誠實揭露）

- **看門狗的真實路徑沒有在真環境跑過**（我沒有觸發真的 `xcodebuild test`，也沒 boot 任何模擬器）：所有中止／診斷行為都是自測 stub＋我自己抽函式做的行程樹實測。第一次真的包住 xcodebuild 會是併入後某條 lane 的 Swift push——與 handoff 的揭露一致。
- 未碰 Pen／容器／正式站；**lock 未持有**。
- 我自己的探針一度因為 harness 寫錯（命令替換抓住背景樹的 stdout）卡住並留下 `sleep` 行程，已全部 kill 清乾淨（收工前確認殘留 0）。

**verdict：APPROVE**（無 blocker／major）。三條 informational 建議：info-1 順手改一行或入池、info-2／info-3 入池即可；併入 `main` 後補 back-merge `main`→`development`，並提醒主 checkout `git pull` 後 pre-push hook 才吃到看門狗。

### 2026-09-05T09:37:20.247Z · Chih-Lin Yeh · `afad58a6`

## QA R1 — delta 合併驗收（`test` tip `48d8b43`，涵蓋 LS-194＋LS-199）— **PASS**（2026-09-05 17:36 CST）

delta `588043e..48d8b43`：8 檔、+831/−26、**零 app 碼**（`git diff --stat 588043e..48d8b43 -- LittleSprout LittleSproutTests LittleSproutUITests` 為空）→ 免 xcodebuild。checkout：`.claude/worktrees/qa-test` detach 到 `48d8b43b07e725582ad40213bcd02cfe82464d8a`（＝`origin/test`）。未開 Pen、未碰容器／lock／正式站、未 create／boot 任何模擬器。

---

### A. LS-194（設計 chore，PR #312）

- **`design-evidence-check.sh design/littlesprout.pen --ticket LS-194 --base 588043e`**：exit 0，r1／r2 兩份收據皆通過，r2 正確判為本輪最新（`head_sha=554190d`）。
- **`design-notes-check.sh design/littlesprout.pen --base 588043e`**：exit 0（Notes 板 8 塊、死 id 0、沿革引用 0、缺失 0）。
- **`design_tree_hash.py design/littlesprout.pen`**（head）＝`205b73bb00c7736b`，與 r2 收據 `tree_hash` 逐字相同。
- **73b6c5e（LS-177 併入版）↔ head 逐 root 比對**（python canonical-JSON，非抽樣、220 個頂層節點全比）：`added=[] removed=[] changed_count=9`，恰為 6 設計板＋3 Notes 板 `['IjWOp','LkrrS','N1aeiU','Rx5mP','b3nzDp','kHDk4','t5wI4','vgvFY','z9tytH']`；頂層 `variables` 逐位元相等。與 merge-review `25e1b911` 的宣稱一致。
- **`IjWOp` 三處署名年齡片語 codepoint**（`ONEs5`／`i3jpIq`／`pNPuF` 的 `wQVzs.content`，直接讀 JSON `descendants` override）：`小安[U+0020]·[U+0020]2[U+00A0]歲[U+00A0]3[U+00A0]個[U+2060]月`（`pNPuF` 另接 `\n小明[U+0020]·[U+0020]8[U+00A0]個[U+2060]月`）——年齡片語內部空白皆 **U+00A0（NBSP）**、姓名與「·」之間維持 U+0020、WORD JOINER（U+2060）未動、多寶貝 `\n` 未動。與 VR R2 描述逐項相符。
- **4 個 `Body` clip:true**：`t5wI4/FD9qU`、`Rx5mP/azJjI`、`z9tytH/z3Lone`、`IjWOp/jZVRr` 對 73b6c5e 逐屬性 diff，`added_keys={'clip'}`、`removed_keys=set()`、其餘 key 值零變動——確認純屬性追加，非幾何位移。`vgvFY`／`LkrrS` 的 Body 節點本身無變動（該兩板變的是 Email Field Box 高度與板高，不在這 4 個 clip 節點內）。

**結論：LS-194 全部核對通過，無新發現。**

---

### B. LS-199（harness，PR #314）

**B1｜自測套件**
- `bash scripts/gates/push-gate.test.sh`（bash 5.3.15）：**73/73 綠**，exit 0。
- `/bin/bash scripts/gates/push-gate.test.sh`（bash 3.2.57，pre-push hook 實際用的殼）：**73/73 綠**，exit 0——本環境 `DEVELOPER_DIR` 未 export，⑬ 沒有紅，與 merge-review `cc04d731` 的裁定（⑬ 受呼叫者環境影響、非本票引入）一致，不視為回歸。

**B2｜QA 自造獨立情境**（不依賴 push-gate.test.sh 既有斷言、自建合成 repo＋stub xcrun／xcodebuild，全程未 boot 任何模擬器，收工後手動確認殘留行程數為 0）：

1. **stdout 印出 `Dropping test runner session call because the test runner hasn't connected yet` 後掛住**（`PUSH_GATE_XCODEBUILD_TIMEOUT_SEC=20`／`PUSH_GATE_CRASH_GRACE_SEC=2`，無 session log／無 .ips fixture）：
   - 耗時 4 秒即中止（遠早於 20 秒逾時）、印「宿主 crash」（非「逾時」）
   - 印「找不到本次的 xcresult session log」＋「找不到 crash report」（無 fixture 時的 fail-safe 訊息）
   - 印「可能是環境性 flake…建議先 `xcrun simctl erase <udid>` 再重跑」
   - stub `xcrun simctl shutdown <udid>` 有被呼叫（記錄到 log）
   - `simulator-lock` 目錄已釋放（不存在）
   - 假身 bash 與其背景 `sleep 120` 子行程皆已死（`kill -0` 兩者皆失敗）——整棵行程樹確實被殺
   - **exit 124**
2. **正常 stub 路徑**（無掛住，`STUB_TEST_RC=0`）：exit 0，仍呼叫 `xcrun simctl shutdown`（正常路徑收尾不受看門狗影響）。
3. **`GITHUB_ACTIONS=true`**：同一支會掛住的 stub，背景跑 5 秒後假身與其 `sleep 120` 子行程**仍存活**（`kill -0` 皆成功）、輸出不含「看門狗啟用」字樣——證實看門狗在 CI 模式下確實不介入；隨後手動 `kill -TERM`/`-KILL` 清理乾淨，收工後 `ps aux` 確認無殘留（`sleep 120`／`qa-stub`／`QA-DEDICATED` 皆查無行程）。

三個情境與 push-gate.sh 原始碼（`wd_run`／`wd_kill_tree`／`wd_session_logs` 等函式）的行為描述逐項吻合，未發現偏差。

**B3｜文件**
- `docs/COLLABORATION.md` §4（line 113，Push gate 主表列補「unit tests 看門狗」段落）與 §7（line 303，前饋↔反饋對照表新增一列，含盲區揭露）皆在。
- `.claude/agents/ios-dev.md` line 19：「push gate 印『逾時』或『宿主 crash』時，先看它印的摘要再決定」。
- `.claude/agents/qa.md` line 14：驗收流程第 2 步已含對應提示句（本次派工 prompt 亦引用同一句）。

**結論：LS-199 全部核對通過，watchdog 行為與文件皆與票面驗收條件一致，無回歸。**

---

### C. 回歸

- `bash scripts/ops/patrol.test.sh`：**242/242 綠**，exit 0。
- `bash scripts/gates/pr-body-check.test.sh`：**91/91 綠**，exit 0。

---

### 驗收條件逐條

**LS-194**
- [x] 兩項修正各附截圖前後對照＋量法（VR R1/R2 comment 已附，本輪未重新渲染截圖，依規定未開 Pen）
- [x] VR 聚焦輪 APPROVE（`866f92c6`）
- [x] 收據六支綠（r1／r2 皆通過 gate、tree_hash 三方一致）
- [x] CI 綠（merge-review 已核）
- [x] merge-reviewer 通過（`25e1b911` APPROVE）
- [x] 併入 development 後 LS-152／LS-163 → Done（已由 orchestrator 確認）

**LS-199**
- [x] stub 三案自測綠（73/73，含 QA 獨立造的第四類情境）
- [x] 逾時／宿主 crash 案 kill 後 exit 非 0（124）且 lock 釋放、shutdown 被呼叫（自測＋QA 獨立驗證）
- [x] 真環境正常 push 一次不受影響（ios-dev handoff 自述 8 秒；本輪因零 app 碼未重跑真 push，非本票新增風險）
- [x] merge-reviewer 通過（`cc04d731` APPROVE）；hotfix 流程（PR #314 → main → back-merge development，PR #315）已完成

### 未完成
無。三則 merge-review informational（info-1 WorkspacePath 前綴比對、info-2 lock 回收 holder 樣本、info-3 wd_kill_tree 子孫清單只收集一次）均已由 merge-reviewer 建議入池，非阻擋項，QA 不重複裁定。

### 風險
無新增風險。已知盲區（文件已載明）：LS-95 `tap-target-check.sh` 的 XCUITest 呼叫未包看門狗；`-quiet` 下早期偵測依賴 session log，`WorkspacePath` 形狀若被 Xcode 改掉會退化成「只看輸出＋逾時」（不誤殺、只變慢）。

模擬器已關：無（全程未 create／boot 任何一台，含合成自測用的假身）。
lock 已釋放：未持有（本輪不碰容器／正式站，純檔案與 gate 腳本驗證）。
qa-e2e 證據：未跑（零 app 碼變更，無需 UI/DB 端到端驗證；四主流程回歸已由既有 patrol／pr-body-check 自測與本輪 push-gate 自測覆蓋 harness 層）。

產出：QA 暫存腳本與日誌於 `/private/tmp/claude-501/-Users-clyeh-little-sprout/e6972486-fec6-43c0-b8f4-28b43b01a5a0/scratchpad/LS-194-199-qa-*`（session 隔離，不進 repo）。

### 2026-09-05T09:39:55.270Z · Chih-Lin Yeh · `5e9a3e92`

**orchestrator 2026-09-05 17:5x 收尾儀式 → Done**

- 併入：PR #314 → main `7816413`（R1 `cc04d731` APPROVE）；back-merge #315 → development；promote dev→test→main；tag **v0.14.0**。delta QA PASS `afad58a6`（73/73 兩殼綠；QA 自造三情境：crash 樣式 4 秒中止 exit 124＋診斷＋shutdown＋lock 釋放／正常 exit 0／`GITHUB_ACTIONS=true` 不介入）。
- sweeper：本票純 harness，併於 QA 自測覆核（無新增死碼；`wd_*` 全部被呼叫）。
- lesson learning：(1) 看門狗真實路徑尚未在真環境走過——下一次任一 lane 的 Swift push 若觸發即為首次實戰，屆時核對診斷輸出；(2) session log 來源要綁本 worktree 的 `WorkspacePath`，全域 glob 會誤殺別票（實作者主動偏離票面，正確）；(3) info-1～3（主 checkout 前綴比對／lock holder 樣本／KILL 前重收子孫）入池 `37a390d0`。
- cleanup：worktree 16:5x 已清。

