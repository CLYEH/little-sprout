# LS-197 Task：LS-23 後端——EULA 同意紀錄（`app_settings.eula_version` 當前版本＋`profiles.eula_accepted_version／eula_accepted_at`＋`accept_eula(p_version)` RPC；欄位只准經 RPC 寫；DB 測試）——LS-190 iOS EULA 同意頁的後端前置

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:S, lane:backend |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M6 合規與帳號 |
| 估點 | — |
| 建立 | 2026-09-05T05:07:04.625Z |
| 開始 | 2026-09-05T05:07:17.460Z |
| 完成 | 2026-09-05T09:40:30.866Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-197/taskls-23-後端-eula-同意紀錄app-settingseula-version-當前版本profileseula |
| 父票 | LS-23 Story：檢舉／封鎖／額度 UI＋EULA |
| 關係 | related→LS-107, related→LS-143, related→LS-84, blocks→LS-190, related→LS-144, related→LS-78, related→LS-179, related→LS-132, related←LS-209, related←LS-204, related←LS-165, related←LS-194, related←LS-199, related←LS-96 |

## 描述

## 來源

[LS-190](https://linear.app/little-sprout-app/issue/LS-190/taskls-23-ios-eula-同意頁首次登入版本更新時零容忍條款摘要08刪除單筆內容確認日記-10留言-10b沿-ls-142)（iOS EULA 同意頁：首次登入／版本更新時顯示零容忍條款摘要，需同意才進 app）已 Ready，但後端目前**沒有任何** EULA 同意紀錄（grep `eula|terms_accepted` 於 migrations／API.md 皆 0）。App Store 5.1.1／UGC 指引要求可證明使用者同意過含零容忍條款的 EULA（`docs/PLAN.md` §6 第 7 項、§10-B）。巡檢 13:1x lane:backend 在飛 0 → 依 [LS-144](https://linear.app/little-sprout-app/issue/LS-144/harness巡檢加開票責任機械提醒-lane-在飛-0-且無可派候補時動作清單印-開票並列來源story-拆票ls-96-p1-升票) 開票。

## 範圍

1. Migration（append-only）：
   * `public.app_settings` 加一列 `eula_version`（text，初值 `'2026-09-05-draft'`，正式文本核可時由 orchestrator 更新；沿 [LS-179](https://linear.app/little-sprout-app/issue/LS-179/taskls-23-後端-營運防線使用者家庭停權旗標profilessuspended-atfamiliessuspended) `registrations_open` 的讀取方式——確認 authenticated 可 `select` 該 key，不可寫）。
   * `public.profiles` 加 `eula_accepted_version text null`、`eula_accepted_at timestamptz null`；**client 不得直接 UPDATE 這兩欄**（沿既有欄位級 grant／`profiles_update` policy 的 `with check` 手法，選最小改動並在檔頭說明）。
   * RPC `public.accept_eula(p_version text) returns void`（security definer、`set search_path=''`、authenticated 可執行、anon／public revoke）：`auth.uid()` 必填；`p_version` 必須等於當前 `app_settings.eula_version`（不等 → 新錯誤碼 `LS0xx`「條款版本已更新，請重新閱讀」，依 `docs/API.md` §錯誤碼表順號並登記 `error-codes-check.sh`）；寫入兩欄（`now()`）；冪等（重複同版本呼叫不報錯、`accepted_at` 更新為最新一次）；停權者仍可呼叫（同 [LS-179](https://linear.app/little-sprout-app/issue/LS-179/taskls-23-後端-營運防線使用者家庭停權旗標profilessuspended-atfamiliessuspended) 豁免思路：同意條款不是內容寫入）。
2. DB 測試（`supabase/tests/`）：版本相符寫入、版本不符錯誤碼、未登入拒絕、anon 無 EXECUTE、client 直接 UPDATE 兩欄被拒、冪等；`60_default_privileges.sql` 白名單若需登記一併補。
3. `docs/API.md`：§設定（`app_settings`）補 `eula_version`；§4 補 `accept_eula`；錯誤碼表補新碼；§10-B 補一句「同意紀錄可稽核」。`error-codes-check.sh`／`api-contract-check.sh` 綠。
4. 本機 lock 內 `db reset`＋`run.sh` 全綠；`migration-breaking-check.sh` 綠（純新增）。

## 不做

iOS 端（[LS-190](https://linear.app/little-sprout-app/issue/LS-190/taskls-23-ios-eula-同意頁首次登入版本更新時零容忍條款摘要08刪除單筆內容確認日記-10留言-10b沿-ls-142)）；EULA 文本；Apple 標準 EULA 附加條款內容（[LS-132](https://linear.app/little-sprout-app/issue/LS-132/法務內容隱私權政策與使用條款正式文本繁中含-ugc-零容忍條款同步公開網址供-app-store)）。

## 驗收

- [ ] 六案 DB 測試綠＋mutation（拿掉版本比對 → 測試紅）；`run.sh` 全綠；CI 綠
- [ ] merge-reviewer 通過（race：同一使用者併發呼叫、版本切換瞬間）；QA 本機兩帳號實測 accept → `profiles` 兩欄更新、直接 UPDATE 被拒
- [ ] 併入後正式站 `db push` 並回讀（[LS-78](https://linear.app/little-sprout-app/issue/LS-78/harnesstestflight-前收回常設授權-migrate-to-production-改回需使用者允許機械-gate) 授權內）

## 留言（7）

### 2026-09-05T05:37:19.479Z · Chih-Lin Yeh · `b8184bfc`

Ticket：LS-197

已完成：
1. Migration `supabase/migrations/20260905051320_eula_consent.sql`（append-only，純新增）：
   - `app_settings.eula_version`（text not null default '2026-09-05-draft'）——欄位級 `SELECT` 開放給 `authenticated`＋新 `app_settings_select` policy（`using (true)`），其餘欄位（`id`／`registrations_open`／`updated_at`）維持原本完全不可讀。
   - `profiles.eula_accepted_version`／`eula_accepted_at`——沿用 LS-143 既有的「整表 UPDATE 已收回、只重開 display_name／avatar_url」機制，新欄位天生擋掉 client 直接 `.update()`，不需要額外 REVOKE。
   - RPC `accept_eula(p_version text)`（SECURITY DEFINER）：版本不符回新錯誤碼 `LS055`；相符寫入兩欄並冪等（同版本重複呼叫覆寫 `eula_accepted_at` 為最新一次）；停權者仍可呼叫（`profiles` 未掛 `enforce_not_suspended` trigger，函式本身 SECURITY DEFINER，天生不需要額外豁免邏輯）。
2. `docs/API.md`：§2 表格列／§3 profiles／§4 新 RPC／§5 錯誤碼表＋Swift 覆蓋段落／§9 機械對帳清單／§11 營運手冊（新增「更新 EULA 版本」小節＋查詢兩條，並註明「同意紀錄可稽核」PLAN §10-B）皆已同步。
3. `LittleSprout/Errors/AppError.swift`／`AppErrorTests.swift`：新增 `LSErrorCode.eulaVersionMismatch = "LS055"`，歸層 `rejected`（同 LS052–LS054：條款版本不符沒有「打錯字重打」這種可換輸入，正確動作是重新抓目前版本、重新顯示條款）。
4. DB 測試 `supabase/tests/106_eula_consent.sql` 七案（票面六案＋額外一案）：版本相符寫入、版本不符 LS055、未登入拒絕、anon 無 EXECUTE（授權面＋實際呼叫兩種驗法）、client 直接 UPDATE 兩欄被拒、冪等（跨兩個真實 COMMIT 交易驗證，因 now() 是 transaction timestamp）、額外驗證停權者仍可呼叫。`60_default_privileges.sql` 白名單登記 `accept_eula(text)`。

已驗證：
- Lock 內（`bash scripts/ops/supabase-lock.sh --hold "LS-197 db tests" --max-minutes 40`，`--release` 輸出「持有 10 分 11 秒」）`supabase db reset`＋`bash supabase/tests/run.sh` 全綠（105 支既有測試檔＋新增 106_eula_consent.sql，含既有併發測試套件）。
- Mutation-killing 驗證（LS-84 style，手動、非常駐）：`docker exec` 進容器把 `accept_eula()` 的版本比對那段拿掉，重跑 `106_eula_consent.sql`，場景 2 正確落紅：
  ```
  NOTICE:  ok：版本相符時 accept_eula() 成功寫入兩欄，且 eula_version 欄位級 SELECT 設計符合預期（讀得到自己、讀不到 registrations_open）
  ERROR:  FAIL：p_version 與目前 eula_version 不相符，accept_eula() 竟然成功
  CONTEXT:  PL/pgSQL function inline_code_block line 11 at RAISE
  ```
  驗證後 `supabase db reset` 還原正確 migration 內容，重跑 `run.sh` 確認恢復全綠。
- 本機靜態 gate：`error-codes-check.sh`（29 碼三方一致）／`api-contract-check.sh`（RPC 35 支、表 23 張一致）／`migration-breaking-check.sh --base origin/development`（0 命中，純新增）皆綠。
- push gate（unit tests + SwiftLint）通過，push 成功。

未完成／剩餘：首次送審，PR #307 尚未經 merge-reviewer 審查，目前無 reviewer informational finding 待處置。

風險與已知問題：
- 純新增 migration，無 DESTRUCTIVE／BREAKING 命中，不需要 BREAKING: 段。
- `accept_eula()` 內部一句 SELECT＋一句 UPDATE，不取額外鎖；讀取後、寫入前 `eula_version` 被別的請求改動不構成資料不一致（呼叫語意是「同意讀到的那個版本」）。
- 票面「沿 LS-179 registrations_open 的讀取方式」在本票被解讀為「沿用同一套封閉機制（欄位級 grant＋policy），但刻意對 eula_version 這一欄開放讀取」——因為 accept_eula(p_version) 需要呼叫端知道目前版本才能決定送什麼參數，跟完全不需要 client 讀取的 registrations_open 不同。已在 migration 檔頭與 API.md 記錄理由，請 reviewer／QA 確認這個解讀符合票面意圖。
- LS055 的 tier 分類（rejected）屬本票判斷，非票面明文規定，PR body 已列出理由供 review。
- iOS 端消費（LS-190）與正式版 EULA 文本（LS-132）皆不在本票範圍；`eula_version` 初值是佔位草稿版號，待 LS-132 核可後由表擁有者手動更新（見 §11）。
- 本機容器已 `supabase db reset` 還原乾淨狀態並釋放 lock，未 boot 任何模擬器（本票不碰 UI／模擬器）。

產出位置：PR #307（https://github.com/CLYEH/little-sprout/pull/307），head SHA `9e8da8641c623825b26f2dde927c7ff6d270fbb9`，branch `feature/LS-197-eula-consent-backend`。

### 2026-09-05T06:13:39.582Z · Chih-Lin Yeh · `7f5a63fe`

## merge-review R1 — PR #307（head `9e8da86`，base `development`）：REQUEST_CHANGES

審查時間 2026-09-05 14:12 CST。`git diff origin/development...9e8da864` 六檔全審（migration／106 測試／60_default_privileges／AppError＋Tests／API.md），逐 commit 檢查無夾帶。DB 實跑在 LS-197 worktree 持有 lock 內完成（`--release` 輸出「持有 8 分 13 秒」），收工已 `db reset` 還原容器（`eula_version` 回到 `2026-09-05-draft`）。

### 票面分歧裁定：`eula_version` 的讀取設計 — 採納實作者的解讀（無 finding）

票面原文是「沿 LS-179 `registrations_open` 的讀取方式——**確認 authenticated 可 `select` 該 key，不可寫**」。破折號後面那句才是驗收條件，而且明確要求 authenticated 讀得到；前半句的類比本身是誤引（LS-179 的 `registrations_open` 對 client 完全不可讀，只有 `private.registrations_open()` 內部讀）。實作滿足票面明文條件，且揭露面實測收斂到剛好一欄：

```
app_settings.id                 auth_sel=f  anon_sel=f
app_settings.registrations_open auth_sel=f  anon_sel=f
app_settings.updated_at         auth_sel=f  anon_sel=f
app_settings.eula_version       auth_sel=t  anon_sel=f
policy app_settings_select | cmd=SELECT | using=true | roles={authenticated}
```

我另外實跑確認：`select *` → 42501、`select registrations_open` → 42501、anon 讀 `eula_version` → 42501、無任何 INSERT/UPDATE/DELETE policy 或 grant。新 policy 只在「已有欄位級 grant」的前提下才有意義，其餘欄位仍是雙重拒絕。LS-179 的封閉模型沒有被削弱，這個分歧不需要改。

### Blocker

**B1 — `supabase/migrations/20260905051320_eula_consent.sql:86`（blocker）**
`comment on column public.profiles.eula_accepted_version` 的說明文字裡有字面 `REVOKE`（「不需要另外下 REVOKE」）。`migration-breaking-check.sh` 是 statement 級分類器、刻意不放過字串字面值內的關鍵字（腳本檔頭「已知限制」第 1 條明載「含 COMMENT ON 的說明文字……說明文字請改寫」），這句沒有 `FROM` 子句 → 依 B3 的 fail-closed 判成 BREAKING：

```
BREAKING	comment on column public.profiles.eula_accepted_version is 'ls-197：使用者最近一次呼叫 accept_eula() 成功時的 app_settings.eula_version 值。null
```

具體失敗情境：CI `rules` job 拿這份分級去要求 PR body 有行首 `BREAKING:` 段，body 沒有 → job 紅（run 33947657524 / job 101256564040，`##[error]migration 改變既有呼叫端行為`）。目前 PR 檢查狀態：`rules` fail、`ci`／`db`／`lint` pass。票面驗收「CI 綠」未達成，分支保護下這支 PR 併不進去。

修法：改寫該句說明文字（例如「不需要另外收回 UPDATE 授權」），不要加 `BREAKING:` 段——這個 migration 是純新增，補段落等於把非破壞性變更誤登記成破壞性。本檔尚未併入 `development`，可直接原地改（append-only 只保護已併入的檔，同 LS-179 R2 的既有裁定）。line 22 的 `--` 註解裡那個 `REVOKE` 會被剝除、不影響；line 123 的 `revoke execute ... from public, anon` 是新函式上鎖慣例，B3 明文豁免。

附帶：handoff 寫「`migration-breaking-check.sh --base origin/development`（0 命中，純新增）綠」與事實不符——這支腳本命中時仍 `exit 0`（它只分級，擋不擋是呼叫端的事），只看結束碼會誤判為綠。本機重跑輸出與 CI 完全一致。

### Minor

**m1 — `supabase/tests/106_eula_consent.sql:221`／`:282-285`（minor）**
場景 6 為了驗 `now()` 跨交易而改用 autocommit，是對的；但收尾只還原 `profiles` 兩欄，沒有還原第 221 行真的 COMMIT 出去的 `app_settings.eula_version = 'v-test-1'`，而第 285 行的 `rollback;` 沒有對應的 `begin`（實測 run.sh 輸出 `WARNING: there is no transaction in progress`），看起來像有回滾、其實是空操作。檔內註解自稱「收尾另外把寫入的資料復原，不留殘留給後續測試檔」，與實際行為不符。

實測（`db reset` → `run.sh` 全綠之後）：
```
select eula_version from public.app_settings where id = true;  -->  v-test-1
```
具體失敗情境：(a) 票面驗收要 QA「本機兩帳號實測 accept」，QA 依 API.md／migration 記的 `2026-09-05-draft` 呼叫會拿到 `LS055`，得先自己去查資料庫才知道被測試檔改掉了；(b) 之後任何編號 >106、會斷言 `eula_version` 預設值的測試檔會莫名其妙紅。修法：收尾補一句還原成 `2026-09-05-draft`（或先存舊值再寫回），並刪掉第 285 行那個沒有 begin 的 `rollback;`。

**m2 — `supabase/migrations/20260905051320_eula_consent.sql:112-116`（minor）**
`update public.profiles ... where id = v_uid` 命中 0 列時，函式靜默回傳成功（`returns void`，呼叫端無從分辨）。我實測（以沒有 `profiles` 列的 uid 呼叫）：
```
R9 profiles 無此列時 accept_eula 回傳成功（靜默 0 列）
R9 該 uid 的 profiles 列數 = 0
```
具體失敗情境：`profiles` 自動建立 trigger 沒跑到、或 LS-107 `ensureProfileExists` 的冪等 upsert 還沒執行時（LS-190 若把 EULA 閘門排在 `ensureProfileExists` 之前就會落進這個時序），client 收到成功、放使用者進 app，但「同意紀錄」根本沒寫進去——正是本票 §10-B「同意紀錄可稽核」要防的事，而且事後查不出來（沒有錯誤、沒有列）。修法：UPDATE 之後 `if not found then raise exception ... using errcode = '42501'`（或另一個既有碼）。
判 minor 不判 major 的理由：`delete_my_account()` 用同一種不檢查 `found` 的寫法，屬既有慣例；且 profiles 自動建立 trigger 有 `96_`／`auth_admin` 兩層測試護著，實際觸發需要 trigger 先失效。但這支的用途是合規稽核，靜默成功的代價與 `delete_my_account()` 不同，建議這票就收掉。

**m3 — `docs/API.md:2869-2871`（§11 查目前狀態）與 `docs/API.md:1554-1560`（§4）（minor）**
§4 要呼叫端「先讀這個值再顯示條款」，§11 給的查詢是 `select eula_version from public.app_settings where id = true;`，但 API.md 沒有任何一處寫「client 端不能帶 `where id = true`」。實測：
```
R3 where id=true -> 42501（client 不能這樣寫）
R1 select eula_version（不帶 where）= v-rev-1
```
（欄位級權限涵蓋查詢中任何位置引用到的欄位，WHERE 用到 `id` 就需要 `id` 的 SELECT 權限。）具體失敗情境：LS-190 的實作者照 §11 範例寫成 `.from("app_settings").select("eula_version").eq("id", true)`，執行期拿到通用的 `permission denied for table app_settings`，訊息完全不指向真正原因。migration 檔頭與 106 的註解都寫了這個教訓，唯獨 client 端要看的合約文件沒寫。修法：§2 的 `app_settings` 列或 §4 補一句警語（§11 的查詢維持原樣即可，那是 postgres 身分）。

### Informational

**i1** — LS-179 的 `comment on table public.app_settings`（已併入的 migration，不可改）現在有兩處過時：「目前只有 `registrations_open` 一欄」、「authenticated 只能透過 `private.registrations_open()` 讀單一布林值，不開放整表 SELECT 給 client」。本 migration 可以用一句 `comment on table public.app_settings is ...` 覆寫刷新（`\d+`／Dashboard 讀到的是最新那句），成本一行、不影響行為。不改也可以——新欄位註解與 API.md 已把真相寫清楚。

**i2** — 版本切換的「未 commit 窗口」（實測、非缺陷，僅登記）：admin 的 `update app_settings set eula_version` 交易**尚未 commit** 時，並行的 `accept_eula('舊版本')` 不會被阻塞（純 SELECT 不等列鎖），成功寫入舊版號；admin commit 之後同一呼叫才回 `LS055`。窗口＝那筆 UPDATE 的交易存續時間（Dashboard 單句、毫秒級），且自我修復（使用者下次啟動讀到新版號 → 再跳一次同意頁）。API.md §4「併發」段已預先聲明「同意的是讀到的那個版本」這個語意，與實測一致。

### 已驗證（實跑證據）

- **DB 全綠**：lock 內 `supabase db reset` ＋ `bash supabase/tests/run.sh` → `✓ 全部 DB 測試通過`（含 106 七案、既有併發套件）。
- **兩支 mutation 皆被殺**：
  - 拿掉版本比對 → `ERROR: FAIL：p_version 與目前 eula_version 不相符，accept_eula() 竟然成功`（場景 2 紅）。
  - 拿掉 `auth.uid()` 必填檢查 → `ERROR: FAIL：auth.uid() 為 NULL 時竟然能呼叫 accept_eula()`（場景 3 紅）。
- **race 1（同一使用者併發呼叫）**：S1 持有列鎖 2s，S2 在鎖上等待 1.40s 後成功，兩者皆 commit，last-writer-wins、`eula_accepted_at` 是較晚那次；無錯誤、無死結、無 serialization failure。
- **race 2（版本切換瞬間）**：見 i2。admin commit 後舊版本呼叫確實回 `LS055`，票面要求成立。
- **我自己以 `authenticated` 角色實測的收斂**：直接 `update profiles set eula_accepted_version` → 42501；`set eula_accepted_at` → 42501；混在 `display_name` 同一句送 → 整句 42501；`accept_eula(NULL)`／`accept_eula('')` → `LS055`（`is distinct from` 的寫法正確）；anon 對 `accept_eula` 無 EXECUTE、對 `eula_version` 無 SELECT。catalog 快照確認 `profiles` 的 UPDATE grant 仍只有 `display_name`／`avatar_url`，兩個新欄位 `auth_upd=f`、`auth_sel=t`（與 `suspended_at`／`deletion_requested_at` 一致）。
- **停權者可呼叫**：106 場景 7 綠；`private.enforce_not_suspended()` 的表清單確實沒有 `profiles`，函式不需要 `delete_my_account()` 那種 GUC 逃生口，判斷正確。
- **其他 gate**：`error-codes-check`（29 碼三方一致）綠、`api-contract-check`（RPC 35 支／表 23 張）綠、`migration-immutable-check` 綠、`migration-version-check` 無撞號（CI）；`ci`／`db`／`lint` job pass，`rules` job fail（B1）。
- **`60_default_privileges.sql`**：`accept_eula(text)` 登記在 `v_definer_rpcs`，與 SECURITY DEFINER 實作相符，位置正確。
- **`AppError.swift` 歸層**：`rejected` 與 `LS052`–`LS054` 同組（狀態拒絕、沒有可換的輸入），我同意這個分類；`AppErrorTests` 的 `expectedRejected ∪ … == Set(allCases)` 斷言會在漏登記時直接紅，不是空洞測試。Swift 端沒有逐碼文案對照表（文案來自 server message），因此 LS055 不需要額外的訊息項，與 LS054 一致。
- **效能／平行**：新 policy `using (true)` 是常數條件，不是 per-row 子查詢（PLAN §5 無違反）；`accept_eula` ＝單列表一次 SELECT ＋ PK 一次 UPDATE，無 N+1、無 OFFSET、無額外鎖；純 SQL 單次呼叫，沒有可平行化卻被序列化的工作。
- **Scope**：六個檔案全部可追溯到票面第 1–4 項，無順手重構、無無關改動。

### 未完成／未審

- 沒有跑 Xcode unit test（`AppErrorTests`）——CI `ci` job pass 已覆蓋，本地未重跑。
- 沒有碰 Pen／模擬器／正式站。
- 正式站 `db push` 與 QA 兩帳號實測不在本次 review 範圍。

**Verdict：REQUEST_CHANGES**（B1 blocker：CI `rules` 紅，PR 併不進去；m1／m2／m3 三個 minor 建議一併收）。修完請重推並重派 R2 對新 head 重審重貼 status。

### 2026-09-05T07:11:39.301Z · Chih-Lin Yeh · `820286ae`

R2 完成（回應 merge-review R1 REQUEST_CHANGES，comment `7f5a63fe`，status failure on `9e8da86`）。

Ticket：LS-197

已完成（R2，四項全修）：
1. **B1（blocker）**已修 `f2640ec`：`eula_accepted_version` 欄位註解裡的中文說明文字含字面「REVOKE」，被 `migration-breaking-check.sh` 的 B3 規則當成「REVOKE ... FROM」語句、找不到 FROM 子句故 fail-closed 判 BREAKING，CI `rules` job 因此紅燈。改寫成「不需要另外收回權限」。
2. **m1** 已修 `2b0bd51`：`106_eula_consent.sql` 場景 6 補回 `app_settings.eula_version` 還原（回 `2026-09-05-draft`），移除一句無對應 `begin` 的孤兒 `rollback;`。
3. **m2** 已修 `f2640ec`（migration）／`2b0bd51`（測試）：`accept_eula()` 的 `UPDATE` 補 `if not found then raise` 防呆，新錯誤碼 `LS056`「帳號資料異常，請聯絡我們」（選擇理由：無既有 LS 碼貼切描述「auth.uid() 沒有對應 profiles 列」這個資料不一致情境，重用 entity-specific 碼會誤導；歸層 `rejected`，同 LS052–LS054）。補測試場景 8。
4. **m3** 已修 `7ee4ecc`：§4／§11 補明確警告——client 讀 `eula_version` 不得帶 `where id = true`（欄位級權限涵蓋 WHERE 引用欄位），正確寫法 `select eula_version from app_settings limit 1`。

i1／i2 informational 由 orchestrator 另行處理，不在本輪修改範圍。

已驗證：
- Lock 內（`--hold "LS-197 R2 db tests"`，`--release` 輸出「持有 5 分 40 秒」）`supabase db reset`＋`bash supabase/tests/run.sh` 全綠（105 支既有測試檔＋`106_eula_consent.sql` 八案，新增場景 8＝LS056）。
- `run.sh` 跑完直查 `app_settings.eula_version` 確認回到 `2026-09-05-draft`（無殘留）；完整 log grep 不到 `WARNING`／`there is no transaction`。
- 重新套用同一支 mutation（拿掉版本比對）驗證場景 2 仍正確落紅，`db reset` 還原後重跑 `run.sh` 確認恢復全綠。
- 三支靜態 gate 皆看過完整輸出內容（不只 exit code）：`error-codes-check.sh`（30 碼三方一致）／`api-contract-check.sh`／`migration-breaking-check.sh --base origin/development`（無任何 BREAKING／DESTRUCTIVE 輸出）。
- SwiftLint 對 `AppError.swift`／`AppErrorTests.swift` 0 違規。
- push gate（unit tests + SwiftLint）通過，push 成功。**過程插曲**：第一次 `git push` 因 push-gate 的 `xcodebuild test` 在 `LS-197-iPhone17Pro` 模擬器上遇到測試宿主 app 啟動時 crash（`~/Library/Logs/DiagnosticReports/LittleSprout-2026-09-05-143904.ips`：`SupabaseClientFactory.makeClient()` 的 `assert(isRunningUnderXCTest || …)` 炸在 `EXC_BREAKPOINT`，代表 XCTest 環境變數沒被正確注入、runner 沒連上）而卡住 28 分鐘，經 orchestrator 判定與本票程式碼改動無關（本票只動 `AppError`／SQL／docs）、屬模擬器／runner 環境性失敗，kill 該 xcodebuild 並 shutdown 模擬器後，直接重跑 `git push` 即順利通過，未改動 `SupabaseClientFactory` 或新增 `Secrets.xcconfig`。

未完成／剩餘：PR #307 已進入 R2，等待 merge-reviewer 再審。

風險與已知問題：同 R1（純新增 migration、無 BREAKING；`accept_eula()` 無鎖競態；`eula_version` 讀取設計解讀已獲確認、無 finding）；新增：LS056 的 `rejected` tier 分類屬本票判斷，理由已寫入 PR body，供 reviewer 確認。

產出位置：PR #307（https://github.com/CLYEH/little-sprout/pull/307），head SHA `7ee4ecc9dd9fef4eba3472dfa77d5541a6cf9b22`，branch `feature/LS-197-eula-consent-backend`。模擬器已關：`DA68142A-D9DF-4D2B-9BF6-71A2C258092C`（push-gate 自身 EXIT trap 關閉，非本 agent 手動 boot）。

### 2026-09-05T07:27:42.039Z · Chih-Lin Yeh · `caac9c0a`

## merge-review R2 — PR #307（head `7ee4ecc`，增量 `9e8da86..7ee4ecc`）：APPROVE

審查時間 2026-09-05 15:26 CST。只審增量三個 commit（`f2640ec`／`2b0bd51`／`7ee4ecc`，五檔 +102/−17），R1 已審範圍不重審。DB 實跑在 LS-197 worktree 持有 lock 內完成（`--release` 輸出「持有 3 分 48 秒」），收工 `db reset` 還原容器並確認 `eula_version` 回到 `2026-09-05-draft`、`accept_eula` 的防呆仍在（`prosrc like '%not found%'` = t）。

### R1 四項 finding 逐項驗收

**B1（blocker）→ 已修，實證關閉。** `migration-breaking-check.sh --base origin/development` 本機重跑：**輸出 0 行**（我看的是內容不是結束碼——R1 的誤判正是只看 exit code 造成的）。CI 四支全綠：`rules` pass（3m56s）／`db` pass／`lint` pass／`ci` pass（15m21s，run 33951818431）。改法正確：只改寫欄位註解的中文說明（`不需要另外收回權限`），沒有加不該加的 `BREAKING:` 段，migration 仍是純新增。

**m1 → 已修，實證關閉。** `db reset` ＋ `run.sh` 全綠後直查：
```
select eula_version from public.app_settings where id = true;  -->  2026-09-05-draft
select count(*) ... eula_accepted_version is not null;         -->  0
```
整份 run log `grep -c WARNING` = **0**，`there is no transaction` 無命中。孤兒 `rollback;` 已移除、場景 6 收尾補上 `eula_version` 還原，檔頭也加註「場景 6 是唯一不用 begin/rollback 的例外」，敘述與行為終於一致。

**m2 → 已修，且新防呆有機械保護。** `if not found then raise ... using errcode = 'LS056'` 緊接在 UPDATE 之後（中間無其他語句，`FOUND` 語意正確）。我用**自己的**假 JWT `sub`（`deadbeef-…-abcd`，與測試檔用的 `00000000-…-0099` 不同）實測：
```
P1 無 profiles 列 -> sqlstate=LS056 message=帳號資料異常，請聯絡我們
P2 正常路徑（fixture a3）寫入 = v-r2-rev
```
sqlstate 與訊息跟 API.md §5 表格逐字相符；正常路徑沒有被誤傷。另外補做第三支 mutation（R1 兩支之外）：把 `if not found` 整段拿掉 → 場景 8 正確落紅
```
ERROR:  FAIL：auth.uid() 沒有對應的 profiles 列時，accept_eula() 竟然成功
```
——新斷言不是空洞測試。`LS056` 的碼選擇（不重用 `42501`）我同意：`42501` 在 API.md 的語意是「未登入／權限不足」，拿來表達「資料不一致」會誤導 UI；`rejected` tier 也正確（使用者沒有可換的輸入、也不是自己能解決的狀態，只能聯絡支援，同 `LS052`／`LS053` 的「請聯絡我們」語意族）。三方登記完整：`error-codes-check` **30 碼三方一致**、`AppErrorTests` 的 `expectedRejected ∪ … == Set(allCases)` 已納入 `.accountProfileMissing`（漏登記會直接紅）、`tier` switch 已加 case（少加會編譯不過）。

**m3 → 已修，文字與權限實況逐條實測相符。** 以 `authenticated` 身分：
```
P3a  select eula_version from app_settings limit 1        -> ok（文件建議的寫法可用）
P3b  select eula_version from app_settings where id=true  -> 42501（文件警告正確）
P3c  select eula_version from app_settings order by id    -> 42501（「任何位置引用到的欄位」說法成立，不限 WHERE）
```
§4 給了 supabase-swift 的正確寫法（`.from("app_settings").select("eula_version").limit(1)`），§11 加了「本節 SQL 皆為表擁有者身分、client 不能照抄」的整段警語＋該行行尾註解。LS-190 的實作者現在照著寫不會踩雷。

### 本輪 finding

**無 blocker、無 major、無 minor。**

**i3（informational）** — `supabase/tests/106_eula_consent.sql:288-294`：場景 6 的還原值是硬寫的字面量 `'2026-09-05-draft'`，與 migration 的 `default` 綁在一起。檔內註解已說明取捨（只在 `db reset` 後的本機庫跑、兩邊同步改即可），我同意這個取捨；但若日後有人用新 migration 改掉 `eula_version` 的 default 而忘了同步這行，殘留會**靜默**回來（沒有任何斷言會紅）。零成本的加固是場景 6 開頭把目前值存進那張既有的 `ls197_idempotent_probe` 暫存表、收尾寫回。不影響本次併入。

**i1／i2（R1 carryover，仍開著）** — i1：LS-179 的 `comment on table public.app_settings` 內容過時（可用一句 `comment on table` 刷新）；i2：版本切換未 commit 窗口會寫入舊版號（毫秒級、自我修復，API.md §4 已聲明語意）。實作者已註明交由 orchestrator 處置，兩者都不影響併入。

### 其他檢查

- **四維度**：race——增量沒有引入新的鎖或寫入路徑，`if not found` 在同一交易內、失敗即整筆 abort，不會留半套寫入；效能——多一次 `FOUND` 判斷，零查詢成本；平行——無變化；scope——五個檔案、三個 commit 全部可追溯到 R1 的 B1／m1／m2／m3，無順手改動、無夾帶（逐 commit stat 檢查過），commit 訊息格式合規。
- **靜態 gate**：`error-codes-check`（30 碼）／`api-contract-check`（RPC 35 支、表 23 張）／`migration-immutable-check`／`migration-breaking-check`（0 行輸出）皆綠。
- **catalog**：`accept_eula(text)` 仍是 `prosecdef=t`、`proconfig={search_path=""}`，簽章未變。
- **DB 全綠**：`db reset` ＋ `run.sh` → `✓ 全部 DB 測試通過`，106 八案全 ok（含新場景 8）。

### 未完成／未審

- 本地未跑 Xcode unit test（CI `ci` job pass 已覆蓋）。
- 未碰 Pen／模擬器／正式站；正式站 `db push` 與 QA 兩帳號實測不在 review 範圍。
- R1 已審過的部分（policy／grant 收斂、race 1／2、`LS055`、`60_default_privileges` 登記）本輪未重審，增量未觸碰這些面向。

**Verdict：APPROVE**（無 blocker／major；i3 與 R1 carryover i1／i2 皆為 informational，不擋併入）。

### 2026-09-05T09:12:52.333Z · Chih-Lin Yeh · `ef5aec80`

## QA R1 — delta 驗收 `test` tip `588043e`（PASS）

完整報告貼在 LS-165（三票合併 delta QA）：https://linear.app/little-sprout-app/issue/LS-165（comment `c1454bb5`）。

**本票摘要**：`supabase db reset`＋`bash supabase/tests/run.sh` 全綠（`106_eula_consent.sql` 九案）；另以兩個真實帳號（`ls-197-qa-a@ls.test`／`ls-197-qa-b@ls.test`）＋一個假 JWT sub 跑完整 HTTP e2e 獨立複驗：`eula_version` 讀取（含 `where id=true` 拒絕）、`accept_eula` 版本相符／不符（LS055）／假 sub（LS056）、直接 UPDATE 兩欄被拒（42501）、anon 拒絕，全部與 API.md／migration 行為一致。`error-codes-check.sh` 30 碼三方一致。

未完成：併入後正式站 `db push` 並回讀——留 orchestrator（LS-78 授權內）。

Status：`qa` success 已貼到 `588043e8b47e0ca4bf6871705a97963db12a187b`。

### 2026-09-05T09:21:59.870Z · Chih-Lin Yeh · `f7287fcb`

## dead-code sweep — LS-197

**範圍**：`git diff 73b6c5ea77f2353970523f3a4843788b6e8d36ba..82fdd89fba7e0c75065f9a2139e7c993436c346a`（LS-197 commits：`b38bb64`／`8584eb9`／`9e8da86`／`f2640ec`／`2b0bd51`／`7ee4ecc`，含 R2 修法）。唯讀 `git show <sha>:<path>` 讀取，不 checkout、不改檔、不碰本機 Supabase 容器／lock（本次未跑 `db reset`／`run.sh`，純靜態讀取比對）。

### Findings

未發現本 feature 引入的死碼。逐項覆核如下：

- **`LS056` 是否有未被任何路徑丟出的碼**：`accept_eula(p_version)` 內 `if not found then raise exception ... using errcode = 'LS056'` 是唯一 raise 點，對應 UPDATE `profiles` 命中 0 列的防呆分支——這是設計上「理論上不該發生但要 fail loud」的防禦性分支，不是未接線的死碼（同 codebase 既有慣例，如 `LS052`／`LS053` 也是低機率但機械可達的狀態碼）。`AppError.swift` 的 `LSErrorCode` 對照表是**通用 rawValue 映射**（`AppError.map(error)` 依 Postgres SQLSTATE 字串對應到 enum case，不是每個碼各自寫一段 throw-site），所以「新增 enum case」本身不構成 dead code——它與 SQL 端 `errcode = 'LS056'` 透過 rawValue `"LS056"` 天然接線。已用 `supabase/tests/106_eula_consent.sql` 場景 8（R2 補的一案）機械驗證：偽造 `auth.uid()` 無對應 `profiles` 列 → 實際觸發 `sqlstate 'LS056'`，非僅宣告未測試。
- **106 測試檔的 `ls197_idempotent_probe` 暫存表清理**：`create temporary table ls197_idempotent_probe`（場景 6 開頭）與對應 `drop table ls197_idempotent_probe`（場景 6 結尾）成對出現在同一測試檔內，中間三個 `do $$...$$` 區塊依序寫入／讀取／驗證後即 drop，未留下跨檔殘留；場景 6 檔頭註解已明載「這裡刻意不用單一 begin/rollback」的理由（`now()` 是 transaction timestamp，需要跨真實 COMMIT 觀察時間戳前進）及收尾手動復原 `profiles` 兩欄的做法，並在註解點名這是 R2（merge-review R1 m1）修正「先前這裡誤留了一句無對應 begin 的 rollback」的產物——目前版本已無該殘留（`grep -n "rollback"` 於場景 6 段落內只在檔案最上方場景 1-5 各自的 `begin;`/`rollback;` 配對中出現，場景 6 起無 `rollback` 字樣）。即使腳本因某個 `raise exception` 中途失敗，`\set ON_ERROR_STOP on` 會讓整支 psql 中止、temp table 隨 session 結束自動釋放，不構成殘留資料庫物件。
- **`AppError.swift`／`AppErrorTests.swift` 的 `LS055`／`LS056` 列舉項**：兩碼皆同時出現在 `LSErrorCode` 本體、`tier` 的 `.rejected` case 分支、`AppErrorTests.swift` 的逐碼列舉測試（`CaseIterable` 窮舉斷言），三處對齊，非孤兒 case。
- **`docs/API.md`／`60_default_privileges.sql` 白名單**：`accept_eula(text)` 已補進 `security_definer_rpcs` 陣列與 `<!-- API-CONTRACT:RPC -->` 清單，`error-codes-check.sh`／`api-contract-check.sh` 三方對帳（API.md §5 表格列 ↔ `LSErrorCode` ↔ migration `errcode`）三邊皆含 `LS055`／`LS056`，無缺角。
- **`f2640ec`（R2 B1）修法殘留**：該 commit 把 migration 檔內 `eula_accepted_version` 欄位註解裡的字面「REVOKE」改寫成「不需要另外收回權限」以避免 `migration-breaking-check.sh` B3 規則誤判——目前版本註解已無字面 `REVOKE` 字樣觸發該規則，且此 migration 尚未部署到正式站（`LS-78` 授權待併入後才 `db push`），R2 這次改寫是同一份未部署 migration 的內容修正，非事後補丁堆疊，無殘留的舊註解版本並存。

### 既有死碼觀察

無（本次巡檢未觸及本 feature 以外的既有程式碼路徑）。

### 結論

LS-197 未發現本 feature 引入的死碼或未接線的錯誤碼。`LS056` 雖是低機率防禦性分支，但透過通用 rawValue 映射與 106 測試檔場景 8 機械驗證，判定為刻意保留的 fail-loud 設計、非死碼。

### 搜尋方法與盲區

- 方法：`git show <sha>:<path>` 讀取各 commit 終態；`git grep -n` 交叉比對 `LS056`／`ls197_idempotent_probe` 等符號在 SQL migration、測試檔、Swift `AppError.swift`／`AppErrorTests.swift`、`docs/API.md`、`60_default_privileges.sql` 五處的接線一致性；未實際對活資料庫跑 `supabase db reset`／`run.sh`（依規則需經 `scripts/ops/supabase-lock.sh`，本次巡檢僅靜態讀取比對，未取用容器鎖）。
- 盲區聲明：未實跑本機 Supabase 容器驗證 `accept_eula` 的實際 SQLSTATE 觸發行為（僅靜態核對測試腳本斷言與函式邏輯是否邏輯自洽），若讀者需要動態證據應另外經 lock 跑一次 `run.sh`；純文字搜尋抓不到透過字串拼接組出的 RPC 名稱或間接呼叫（本次未發現此類用法，但不能窮盡排除）。

### 2026-09-05T09:40:25.473Z · Chih-Lin Yeh · `f7e1865a`

**orchestrator 2026-09-05 17:5x 收尾儀式 → Done**

- 併入：PR #307（R1 `7f5a63fe` REQUEST_CHANGES → R2 `caac9c0a` APPROVE）→ development `82fdd89` → test → main；tag **v0.14.0**。delta QA PASS（`ef5aec80`，合併輪 `c1454bb5`：兩帳號 HTTP e2e——版本讀取範圍、`accept_eula` 成功／`LS055`／`LS056`、直接 UPDATE 42501、anon 拒）。
- **正式站**：`supabase db push` 只推 `20260905051320_eula_consent.sql` → Applied；回讀：`schema_migrations` 已登錄、`app_settings.eula_version = '2026-09-05-draft'`、`profiles` 兩欄在、`accept_eula` 函式在。**正式文本核可時記得由 orchestrator 更新 `eula_version`**（並依 i3 同步 106 測試還原值）。
- sweeper `f7287fcb`：0 件死碼（`LS056` 經 SQLSTATE 映射可達、探針表同檔建刪）。
- lesson learning：(1) 欄位註解裡的字面 `REVOKE` 會被 breaking-check B3 fail-closed 判 BREAKING——說明文字避開 DDL 關鍵字，且該腳本命中仍 exit 0 只能看內容（池項建議 `--strict`）；(2) push gate 卡死 28 分＝宿主 crash＋XCTest 未注入 → 已成 LS-199 看門狗；(3) 票面「沿用某票的讀取方式」若引用錯誤先例，reviewer 以驗收條件為準裁定（本票採實作者解讀）。
- cleanup：worktree 15:34 已清＋專屬機；informational i1／i2／i3 入池 `e069ca4a`／`2acb1d3b`。LS-190（EULA 同意頁 UI）解鎖。

