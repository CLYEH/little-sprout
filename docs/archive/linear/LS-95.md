# LS-95 Harness：≥44pt 點擊目標機械 gate——UI 票 QA 前以 accessibility frame 量測所有 Button／tappable 元件，<44pt 即 FAIL（LS-17 QA1 兩顆按鈕漏網＋#148 R1 I4）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:L, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-25T10:00:06.609Z |
| 開始 | 2026-09-01T03:15:01.056Z |
| 完成 | 2026-09-01T12:00:18.935Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-95/harness44pt-點擊目標機械-gate-ui-票-qa-前以-accessibility-frame-量測所有 |
| 關係 | related→LS-107, related→LS-83, related→LS-119, related→LS-70, related→LS-76, related→LS-17, related←LS-263, related←LS-218, related←LS-216, related←LS-199, related←LS-151, related←LS-160, related←LS-146, related←LS-140, related←LS-139, related←LS-137, related←LS-74, related←LS-68, related←LS-69, related←LS-65, related←LS-59, related←LS-108, related←LS-113, related←LS-98, related←LS-39, related←LS-92, related←LS-96 |

## 描述

## 來源

[LS-17](https://linear.app/little-sprout-app/issue/LS-17/storysign-in-with-appleemail-otp-登入) QA1 FAIL：`OTPVerificationView` 重寄鈕 162×22pt、`SettingsView` 登出 32×19pt，兩顆都通過 visual-reviewer 五輪＋merge-reviewer 兩輪才被 qa 用模擬器量到；修正 PR #148 R1 又因 padding 掛在 Button 外（不參與 hit test）漏網（merge-reviewer I4 建議機械化）。長輩硬約束（brand skill）明寫 ≥44pt，目前只有文字規則＋qa 人工量。

## 前饋

* COLLABORATION §4 QA gate 與 ios-dev 定義：UI 票 push 前必跑點擊目標量測腳本，<44pt 不得 push。

## 反饋（機械）

* `scripts/gates/tap-target-check.sh <UDID> <scheme>`：啟動 app 至指定畫面（沿用 qa 的 mobile-mcp 或 `xcrun simctl` + accessibility dump：`xcrun simctl spawn <UDID> log`／`idb ui describe-all`／XCUITest `XCUIApplication().buttons.allElementsBoundByIndex` frame），列出所有 `Button`／`isAccessibilityElement && traits.contains(.button)` 元件 frame，任一邊 <44pt → exit 1 並列出元件 label＋frame。
* 掛在 push-gate（UI 票：Swift diff 含 `Features/` 時）與 CI（若模擬器可用；否則 CI 只跑腳本自測）。
* 字級：量測固定在一般字級（`content_size large`），因放大字級下內容本身就 ≥44，量了無意義（#148 R1 F2）。
* 自測：對故意 22pt 的樣本 view 紅、對 44pt 綠。

## 驗收

* 對 [LS-17](https://linear.app/little-sprout-app/issue/LS-17/storysign-in-with-appleemail-otp-登入) 修前的 `OTPVerificationView`／`SettingsView` 跑 → 紅並點名兩顆；對修後 → 綠。
* §7 對照表新增一列。
* hotfix 從 main 切、PR 回 main、back-merge。

## 留言（6）

### 2026-09-01T04:05:28.003Z · Chih-Lin Yeh · `c6797216`

## Handoff — LS-95 實作完成，PR #198 已送出

**Ticket**：LS-95 Harness：≥44pt 點擊目標機械 gate

**已完成**
- `scripts/gates/tap-target-check.sh <UDID> <scheme>`：對 `LittleSproutUITests` 跑 `xcodebuild test`，用 XCUITest（`XCUIApplication().buttons.allElementsBoundByIndex` 讀 `.frame`）量測所有 Button／tappable 元件，任一邊 <44pt 即列出元件 label＋frame、exit 1。
- **技術路徑論證**：票文列了三選項，本機 Xcode 26.6 環境實測——`xcrun simctl` 無 accessibility dump 子指令（`simctl help` 列出的子指令沒有任何相關項目）；`idb`／`idb_companion` 均未安裝（`which` 落空，且要求額外裝一支 CLI 依賴，CI／其他開發環境不保證有）。XCUITest 是唯一實測可行、且與現有 `LittleSproutTests` 共用同一套 xcodebuild 流程、不需要額外裝 CLI 的路徑，因此選定。
- 新增 `LittleSproutUITests` target（`bundle.ui-testing`）：`TapTargetGateTests`（正式檢查：`OTPVerificationView`／`SettingsView`）、`TapTargetGateSelfTests`（自測樣本）、`TapTargetMeasurement`（共用量測邏輯）。
- `LittleSprout/TapTargetGateHarness.swift`（`#if DEBUG`）：依 launch environment `LS_TAP_TARGET_GATE_SCREEN` 直接顯示指定畫面，用既有 `.preview()` mock store，不需登入／建家庭流程。
- 字級固定 `UICTContentSizeCategoryL`（一般字級，非 AX 放大——#148 R1 F2：放大字級下量了無意義）。
- 自測樣本覆蓋 #148 R1 I4 的漏網型：`selfTestPaddingOutsideButton`（padding 掛在 Button 外層、不參與 hit test）——這是本票存在的理由。
- 掛進 `scripts/gates/push-gate.sh`（Swift diff 含 `Features/` 才跑，方向矩陣同第 5／7 步）與 CI `ci` job（無條件跑，伺服器端兜底）；既有 unit tests 呼叫加 `-only-testing:LittleSproutTests`，避免每次都連 UI test 一起跑。
- `docs/COLLABORATION.md` §1／§4／§7 補點擊目標 gate 說明。
- 額外必要修正（非票文原始範圍，但是讓機制能運作的必要接線）：`SupabaseClientFactory` 補 `isRunningUnderTapTargetGate`（XCUITest 把 app 當獨立行程啟動，既有 `isRunningUnderXCTest` 偵測不到，不加會在無 `Config/Secrets.xcconfig` 的環境當場崩潰——開發期間實測撞到）；`OTPVerificationView` 新增 `cooldownSeconds` 參數（預設 60，行為不變，只有 harness 會覆寫成 0，讓重寄 Button 立刻可測，不必等 60 秒冷卻）。

**已驗證（怎麼驗）**
- `tap-target-check.test.sh`：8 案例全過（PATH 換假 xcodebuild，驗參數檢查／log 解析／exit code；含 2 條 mutation-style 負控——多違規都要點名、失敗無標記不能誤判成通過）。
- `LittleSproutUITests` 全綠：`TapTargetGateTests` 2 條＋`TapTargetGateSelfTests` 3 條，實機模擬器（iPhone 17 Pro，Xcode 26.6）跑過。
- **驗收黃金樣本**：暫時還原 LS-17 QA1 修正前的 `resendRow`／登出 Button 寫法（未 commit，驗完即 `git checkout` 復原）——跑出「重新寄一次驗證碼 frame=163.0x22.0pt」「登出 frame=34.0x20.3pt」，與票文記錄的 162×22pt／32×19pt 相符（差 1pt 為不同次模擬器渲染的正常誤差）；對修正後現行 main 綠。
- SwiftLint `--strict` 全 repo 乾淨；`LittleSproutTests`（既有 unit tests）、`push-gate.test.sh`（33 案例）、`detect-simulator.test.sh`（23 案例）全部重跑，無回歸。
- 完整 `scripts/gates/push-gate.sh` 手動跑過一次全綠（含新的 tap-target-check 步驟，因本票自身 diff 含 `Features/` 而觸發並通過）；`git push` 時 pre-push hook 再跑一次同樣全綠。

**未完成**
- 尚無 reviewer informational finding——這是初次提交，等待 merge-reviewer 審查，目前沒有 finding 需要處置。
- Hotfix 慣例的 back-merge `main`→`development` 待 PR #198 併入 main 後才能執行（無法在併入前先做），需要後續派工處理。

**風險**
- 只認 `Button`／賦予 `.button` accessibility trait 的元件，非標準手勢（`.onTapGesture` 疊在任意 View 上、無 accessibility trait）量不到。
- 新畫面要進黃金樣本／自測需要 ios-dev 自覺補上，機制本身不會提醒漏加。
- `Features/` diff 判斷是路徑層級，不理解語意（改 `Features/` 下純資料模型檔案也會觸發，多跑一次成本但不誤判方向，不算風險只是效率細節）。

**產出位置**
- PR：https://github.com/CLYEH/little-sprout/pull/198（`hotfix/LS-95-tap-target-gate` → `main`）
- `scripts/gates/tap-target-check.sh`／`scripts/gates/tap-target-check.test.sh`
- `LittleSproutUITests/TapTargetGateTests.swift`／`TapTargetGateSelfTests.swift`／`TapTargetMeasurement.swift`
- `LittleSprout/TapTargetGateHarness.swift`／`TapTargetGateScreenName.swift`
- `LittleSprout/Config/SupabaseClientFactory.swift`、`LittleSprout/Features/Auth/OTPVerificationView.swift`、`LittleSprout/LittleSproutApp.swift`（接線）
- `scripts/gates/push-gate.sh`、`.github/workflows/ci.yml`、`docs/COLLABORATION.md`（gate 掛線與文件）
- 模擬器已關：A90A8C7D-7BC7-4A7C-98C5-9DFC7CF83DC4（`LS-95-iPhone17Pro`，push-gate.sh 的 EXIT trap 已自動關閉，另手動確認一次）


### 2026-09-01T04:25:21.240Z · Chih-Lin Yeh · `6e92e796`

## merge-review R1 — PR #198 · head `9ce0bbe` — **REQUEST_CHANGES**

審查範圍：`git diff origin/main...9ce0bbef339f73781dc1dd7feae0eebafa312cc7`（16 檔 +641/−16）。CI 全綠（ci 11m29s／rules 3m27s／lint 14s／db 2m45s）。所有量測都在 scratch worktree（`git worktree add --detach` 於 scratchpad，審完 `worktree remove`）跑，PR worktree 全程未動（`git status --porcelain` 前後皆空）。

先講結論：**核心量測邏輯是對的、黃金樣本我獨立重建後確實紅**，但這支 gate 有兩條「機械上會靜默放行」的路徑，其中一條我實測重現了。

---

### Blocker

**B1 — `LittleSproutUITests/TapTargetGateTests.swift:22-26`／`TapTargetMeasurement.swift:12-18`：沒有任何斷言確認「要量的畫面真的出現了」，harness 一壞就靜默全綠**

`launch()` 設完 launch environment 就 `app.launch()`，接著直接 `app.buttons.allElementsBoundByIndex`；`assertAllTappablesMeetMinimum` 只對「找到的違規」做 `XCTFail`。找到 0 個元件＝0 個違規＝綠。

**實測重現**（不是推論）：我在 scratch worktree 把 `TapTargetGateHarness.swift:20` 的環境變數鍵改成 `LS_TAP_TARGET_GATE_SCREEN_TYPO`（模擬「兩邊鍵值走鐘」這個最常見的 harness 壞法），`activeScreen` 變 nil、app 退回 `RootView`，然後跑 `-only-testing:LittleSproutUITests/TapTargetGateTests`：

```
Test Case '-[LittleSproutUITests.TapTargetGateTests testOTPVerificationView]' passed (5.180 seconds).
Test Case '-[LittleSproutUITests.TapTargetGateTests testSettingsView]' passed (3.865 seconds).
     Executed 2 tests, with 0 failures (0 unexpected)
** TEST SUCCEEDED **
```

兩條產品畫面檢查全過，`tap-target-check.sh` 會印「✓ 所有量測畫面的 Button／tappable 元件皆 ≥44×44pt」——**量的根本不是 OTP／設定畫面**。

具體失敗情境（任一成立即靜默綠）：(a) 有人改了環境變數鍵名只改一邊；(b) `hostView` 某個 case 因為未來重構回傳空內容；(c) 未來 app 在 `RootView` 之前加了任何啟動關卡（強制更新頁、onboarding），harness 畫面被蓋掉；(d) 畫面內容靠 `.task` 非同步載入，snapshot 取在 button 出現之前——`TapTargetGateSelfTests` 抓不到這些，因為它們量的是自己的樣本畫面，跟產品畫面兩條路。

建議修法：`launch()` 之後對每個畫面斷言一個 sentinel 存在再開始量，例如 `XCTAssertTrue(app.staticTexts["輸入驗證碼"].waitForExistence(timeout: 10))`／`SettingsView` 用 `app.buttons["登出"]`；再加一條底線 `XCTAssertFalse(app.buttons.allElementsBoundByIndex.isEmpty, "畫面沒有任何 Button，harness 可能沒生效")`。這條同時把 (d) 的時序 flake 一起解掉。

**B2 — `scripts/gates/tap-target-check.test.sh:2`／`docs/COLLABORATION.md:269`：自測宣稱掛在 CI rules job，實際沒掛——§7 對照表登記了一個不存在的反饋**

`.github/workflows/ci.yml:125-141` 是逐行列舉 gate 自測的白名單，`tap-target-check.test.sh` 不在裡面。全 repo grep 這個檔名，除了它自己只有 §7 那一列提到它：

- `tap-target-check.test.sh:2`「CI rules job 每個 PR 都跑」→ 不成立
- `COLLABORATION.md:269`「自測 `tap-target-check.test.sh`（…）掛 CI `rules` job」→ 不成立

後果：那兩條刻意寫的 mutation-style 負控（⑦ 多違規不能只印第一個、⑧ 失敗無標記不能誤判成通過）從來不會在 CI 執行；`tap-target-check.sh` 之後被改壞（例如把 fallback 分支改成 exit 0）沒有任何東西會紅。而 §7 是本專案「哪條規則有 gate」的唯一登記處，登記了一個假的比沒登記更糟。

修法：`ci.yml` rules job 加一行 `bash scripts/gates/tap-target-check.test.sh || rc=1`。（我本機手跑過：8 案例全過，腳本本身沒問題，純粹是沒接線。）

---

### Major

**M1 — 畫面註冊表無對帳機制，14 個 Features 畫面只註冊 2 個；同時 §4 把 qa 的人工量測拿掉了**

- `LittleSprout/TapTargetGateScreenName.swift:8-9` 註冊 `OTPVerificationView`／`SettingsView`；`find LittleSprout/Features -name '*View.swift'` = **14**。
- `push-gate.sh:279` 的觸發條件是「diff 含 `Features/`」——任何 UI 票都會觸發，但跑的永遠只有這 2 個畫面，然後印一句聽起來是全域的「✓ 所有量測畫面…皆 ≥44×44pt」。
- 同一個 PR 把 `COLLABORATION.md:114`（§4 QA gate 列）改成「**點擊目標 ≥44pt 已由 push gate／CI 機械把關**，qa 視覺驗收不必再手動逐顆量測」。

對那 12 個沒註冊的畫面而言：機械覆蓋 0、人工覆蓋被移除 → **這個 PR 讓它們比併入前更沒保護**。具體情境：下一張 UI 票（例如 LS-119 的日記時間軸）新增畫面、某顆 icon 鈕 30×30pt → push-gate 綠 → CI 綠 → qa 依新規約不量 → 完整重演 LS-17 QA1。這正是 CLAUDE.md「新增重要規則＝同時新增它的 gate，否則視為未完成」要擋的東西：「新畫面要註冊」現在是一條純前饋規則。

handoff 把它列在「風險」而不是當成缺口處理，但這條剛好是本票存在理由的一半。

修法（擇一即可解 REQUEST_CHANGES）：(a) 加對帳檢查——列舉 `LittleSprout/Features/**/*View.swift`，每支要嘛出現在 `TapTargetGateScreenName`，要嘛出現在一份帶理由的豁免清單，否則擋；(b) 若這次不做 (a)，就別動 §4 那句 qa 免量測，並把通過訊息改成點名實際量到的畫面（「✓ 已量測 OTPVerificationView／SettingsView」），不要讓讀者以為是全覆蓋。

**M1(b) — 就算是已註冊的畫面，也只量到單一狀態，現行程式碼已有實例漏網**

`SettingsView.swift:35-51` 的「邀請家人」`NavigationLink`（LS-107 加的）只在 `familyStore.myFamily != nil` 才渲染；harness 用 `FamilyStore.preview()`（`PreviewFamilyAPIClient.swift:41-45`）＝`FamilyStore(apiClient:)`，而 `FamilyStore.swift:77` 是 `private(set) var myFamily: Family?`，初始 nil 且 preview 路徑從不填。所以那顆列**永遠不會被量到**——golden run 的 `testSettingsView` 也只點名了「登出」一顆，可對照。這不是假設，是現況。至少要讓 `hostView` 能餵有家庭的狀態，或在 §7 盲區明列「同一畫面的條件式內容只量預設狀態」。

---

### Minor

**m1 — `push-gate.sh:279` 觸發條件只認 `Features/`，改 `DesignSystem/` 不觸發，而按鈕元件都住在那裡**
`LittleSprout/DesignSystem/` 底下有 `AuthButtons.swift`、`AppleSignInButton.swift`、`Pill.swift`，以及 `Spacing.swift`——所有點擊區 padding 的 token 來源。把 `AppSpacing.item` 從 16 改成 4，全 app 每個 tap target 縮水，本機 gate 完全不跑（實測 `printf 'LittleSprout/DesignSystem/PrimaryButton.swift' | grep -qE '(^|/)Features/'` → 不匹配）。CI 無條件跑會兜住，所以是 minor 不是 major；但 §1 那句「UI 票（Swift diff 含 `Features/`）」等於把 DesignSystem 改動排除在「UI 票」定義外，建議 pattern 補 `DesignSystem/`。

**m2 — `push-gate.sh:47` 的 LS-76 allowlist 沒補 `LittleSproutUITests/`**
本 PR 新增了這個目錄。只改該目錄下既有 Swift 檔（例如把 `minSide` 從 44 調成 20）→ 不命中 allowlist → `skip_swift_steps=1` → 本機 SwiftLint、unit tests、tap-target 三步全跳過，而 diff 裡明明有 Swift 變更。這正是 LS-76 R1 F1 當時被評 major 的「該跑卻跳」方向。CI 兜住 → minor。

**m3 — CI 成本：實測 5m06s，`ci` job 從約 6m 變 11m29s，註解說的「成本不高」與量測不符**
本 PR 自己那次 run（33468479846）的 step 級時間：`Build & Test` 04:04:40→04:10:39（5m59s）、`點擊目標 gate` 04:10:39→**04:15:45（5m06s）**。`ci.yml:474` 寫「多跑一次 UI test target 的成本不高」——實際是把關鍵路徑 job 幾乎翻倍，而且無條件套用在每個 PR（含純文件、純 DB、純 harness 票）。`timeout-minutes: 40` 還有餘裕，所以不擋；但請至少修正註解，或改成用 `git diff origin/$GITHUB_BASE_REF` 判路徑（需 `fetch-depth: 0`）。本機暖快取下同一支腳本是 51.8s，兩者差距主要在 CI 冷編 UI test target。

**m4 — 新增的 push-gate 分支沒有對應的 `push-gate.test.sh` 案例**
既有 ⑯–㉒ 全都在用 stub xcodebuild 驗 LS-76 那組 diff 觸發條件（含 hotfix→origin/main 方向矩陣）。本票新增的觸發區塊（`Features/` 比對、方向矩陣、保護分支／DETACHED 跳過、target ref 找不到就跳過）一條測試都沒有。條件寫反會讓本機 gate 靜默失效而沒有任何測試會紅——跟 repo 既有慣例不一致。

---

### Informational

- **I1** `TapTargetGateScreenName.swift` 沒有 `#if DEBUG`，且被列進 app target（`project.pbxproj:771`），Release 會編進一個永遠用不到的 enum。其餘圍欄我逐處查過是完整的：`TapTargetGateHarness.swift` 整檔 `#if DEBUG`／`LittleSproutApp.swift` 只在 `#if DEBUG` 內引用它／`SupabaseClientFactory.isRunningUnderTapTargetGate` 只被 `assert` 讀（`-O` 下整段移除，autoclosure 連環境變數都不會查）→ **Release 行為零改變**。UI test target 也是 Debug 組建，把 enum 一起 `#if DEBUG` 應該可行。
- **I2** 我沒能用「Release 真的編得過」來驗 I1 的圍欄：`xcodebuild build -configuration Release` 在本分支噴 66 個錯，但我對 `origin/main`（128a266）跑同一道指令**也是同樣 66 個、全部是 `type 'X' has no member 'preview'`**（未加 `#if DEBUG` 的 `#Repview` 區塊引用 DEBUG-only 的 `.preview()`）。結論：**pre-existing，與本 PR 無關**，本 PR 沒有新增任何 Release 錯誤。但順帶暴露一個既有盲區——repo 裡沒有任何 gate 會建 Release／archive。建議記入 LS-96。
- **I3** §7 已載明的盲區確認為實況而非假設，而且就落在被量的畫面上：`OTPCodeField.swift:37` 的 `.onTapGesture` 不是 `.button` 元素，OTP 輸入框的可點區在 `OTPVerificationView` 上量不到；`CreateChildView.swift:365` 的 `DatePicker` 同理。已如實記錄，不需處置。
- **I4** B2 之所以漏掉，是因為沒有任何機制檢查「`scripts/**/*.test.sh` 都有被 `ci.yml` 列到」。三行 rules job 檢查就能補，建議記入 LS-96。

---

### 引據抽查（handoff 每條逐一覆核）

| handoff 宣稱 | 覆核方法 | 結果 |
|---|---|---|
| golden sample 163.0x22.0／34.0x20.3 | **不沿用申報**：從 `git log -S` 找出修正 commit `80af9e2`，取 `80af9e2^` 的 `resendRow`／登出鈕原始寫法，在 scratch worktree 逐字重建（diff 確認 = 移除 `.padding(.vertical, AppSpacing.group)`＋`.contentShape`、登出鈕還原成 `Button("登出", role: .destructive, action: signOut)`），再跑 gate | ✅ exit 1，`TAP-TARGET-FAIL: 重新寄一次驗證碼 frame=163.0x22.0pt`／`TAP-TARGET-FAIL: 登出 frame=34.0x20.3pt`，與申報完全一致 |
| 對現行 main 綠 | 直接跑 head | ✅ exit 0，51.82s |
| 自測 8 案例全過 | 手跑 `tap-target-check.test.sh` | ✅ ①–⑧ 全過 |
| UI tests 5 條全綠 | 基準跑 | ✅ 2＋3 全綠 |
| SwiftLint --strict 乾淨 | 手跑全 repo | ✅ exit 0 |
| `push-gate.test.sh` 無回歸 | 手跑 | ✅ 全過（但未新增本票案例，見 m4） |
| 「本票 diff 含 Features/ 所以觸發」 | 檢查 diff | ✅ `Features/Auth/OTPVerificationView.swift` 在內，`hotfix/*`→`origin/main` 方向正確 |
| 自測掛 CI rules job | grep `ci.yml` | ❌ **不成立 → B2** |

**額外 mutation control**（票外，我自己加的）：把 `TapTargetMeasurement.minSide` 44→20，`testTooSmallSampleIsFlagged`／`testPaddingOutsideButtonSampleIsFlagged` 兩條轉紅、`testGoodSamplePasses` 維持綠（符合預期方向）。自測**確實會失敗**，不是永遠綠的裝飾。

### 四維度其餘結論

- **Race condition**：app 端沒有引入任何並行狀態；`TapTargetGateHarness` 是無儲存狀態的 static computed property，`hostView` 標 `@MainActor`，Swift 6 strict concurrency 下編得過。唯一的時序問題是 B1 的 (d)（snapshot 早於渲染），已併入 B1。
- **運算效能**：見 m3（唯一發現，已量測）。
- **平行優化**：`-parallel-testing-enabled NO` ＋ `simulator-lock.sh` 序列化是**正確**的，不是該被平行化的瓶頸——UI test 若開平行會 clone 模擬器、與 LS-70／LS-83 的 UDID 鎖語意直接衝突。無 finding。
- **Scope**：app 程式碼四處改動我逐一判定為必要且最小——`SupabaseClientFactory` 的 assert 放行（Release 無影響，已驗）、`OTPVerificationView.init` 的 `cooldownSeconds` 純轉發（`OTPVerificationModel` 在 `origin/main` 就已經有 `cooldownSeconds: Int = 60`，見該檔 line 85，正式呼叫端全部沿用預設）、`TapTargetGateHarness` 掛載點（DEBUG 圍欄完整）、`LittleSproutApp` 抽 `rootView`（`#if DEBUG` 分支的機械必要重構，`.onOpenURL` 掛法未變）。**沒有發現順手重構或票外功能**。

### 處置建議

B1、B2 必修（各是一行到數行的修法）；M1 擇一路徑。m1–m4 可一併修或記入 LS-96 待辦池，請在下一輪 handoff「未完成」欄逐條交代處置。

模擬器：`LS-95-iPhone17Pro`（A90A8C7D）已確認 Shutdown，`demo-*` 未碰。scratch worktree 已 `worktree remove`。

### 2026-09-01T05:07:21.268Z · Chih-Lin Yeh · `50e792f8`

## Handoff — LS-95 R2（merge-review R1 全部 finding 已處置），PR #198 head `af3375a`

**Ticket**：LS-95 Harness：≥44pt 點擊目標機械 gate

回應 R1 裁決 comment `6e92e796-de29-4bf2-ad8a-997183ceacf4`（2 blocker＋1 major＋4 minor＋4 informational）。同 worktree，6 個新 commit（`2ff118f`…`af3375a`）。

**已完成（逐條處置）**

- **B1（blocker，已修）**：`TapTargetGateScreenName` 新增 `sentinel` 屬性（每個畫面渲染成功時必定存在的 accessibility 元素）；`TapTargetMeasurement.assertScreenRendered` 在量測前先斷言 sentinel 存在，找不到就 `XCTFail` 並指名可能原因；`violations(in:)` 另加「Button 清單非空」底線斷言。**自證**：重現 reviewer 的 typo repro（`LS_TAP_TARGET_GATE_SCREEN` → `_TYPO`），確認全部 5 個 test method 現在正確變紅（`SettingsView 畫面沒有渲染出 sentinel 元素「button[登出]」——harness 可能沒生效…`），還原後恢復綠。
- **B2（blocker，已修）**：`tap-target-check.test.sh`／`tap-target-registry-check.test.sh` 已接進 `.github/workflows/ci.yml` 的 Gate 腳本自測 step 白名單。
- **M1（major，已修，採 reviewer 建議路徑 (b)＋部分 (a)）**：新增 `tap-target-registry-check.sh` 對帳 `Features/**/*View.swift` 對 `TapTargetGateScreenName`（已註冊）／`tap-target-exemptions.txt`（具名排除＋理由），兩邊都沒有即擋——12 支未註冊畫面已具名排除、追蹤票 LS-96。`tap-target-check.sh` 通過訊息改點名實際量測畫面，不再用「所有量測畫面」這種措辭。§4 QA gate 那句「qa 不必再手動逐顆量測」已訂正回誠實描述（機械覆蓋僅 2/14，qa 對未註冊畫面仍需視覺驗收）。
- **M1(b)（已修）**：`FamilyStore.seedMyFamilyForPreview`＋`FamilyStore.preview(withFamily:)`，harness 的 `.settings` case 改用它同步餵家庭狀態。**自證**：暫時把「邀請家人」列 padding 改成 1pt，gate 正確抓到「TAP-TARGET-FAIL: 邀請家人 frame=402.0x25.0pt」，改回後恢復綠。
- **m1（已修）**：push-gate.sh 觸發條件補 `DesignSystem/`（`AppSpacing`／`PrimaryButton`／`AuthButtons`／`Pill` 都住在那裡）。
- **m2（已修）**：LS-76 allowlist 補 `LittleSproutUITests/`（`(^|/)LittleSprout/` 不匹配同名前綴目錄）。
- **m3（已修）**：CI 的點擊目標 gate 改成對 `$GITHUB_BASE_REF` 的 diff 判斷，只有真的碰 `Features/`／`DesignSystem/` 才跑；`push` 事件（promote.sh 晉升）無「相對 base」概念維持無條件跑。`ci` job checkout 補 `fetch-depth: 0`。
- **m4（已修）**：`push-gate.test.sh` 新增 ㉓～㉖ 四案例（Features/ 觸發、DesignSystem/ 觸發、mutation-style 負控 Services/ 不觸發、hotfix 方向矩陣）。
- **I1（已修）**：`TapTargetGateScreenName.swift` 補 `#if DEBUG`（實測兩個 target 的 Debug 組態皆有 `SWIFT_ACTIVE_COMPILATION_CONDITIONS = DEBUG`，加圍欄不影響編譯）。
- **I2（不修，pre-existing 與本票無關）**：Release build 既有 66 個 pre-existing 錯誤（未加 `#if DEBUG` 的 `#Preview` 引用 DEBUG-only `.preview()`），reviewer 已確認與本 PR 無關；「repo 沒有任何 gate 建 Release／archive」記入待辦池 LS-96。
- **I3（不修，已如實記錄）**：`.onTapGesture`（`OTPCodeField`／`CreateChildView` 的 `DatePicker`）量不到，§7 盲區已如實記錄，不需處置。
- **I4（記入待辦池 LS-96）**：「所有 `scripts/**/*.test.sh` 都有被 `ci.yml` 列到」目前無機械對帳，記入待辦池追蹤。

**已驗證（怎麼驗）**
- B1 自證：typo repro → 5 個 test method 全紅，訊息正確指名 sentinel；還原後全綠。
- M1(b) 自證：邀請家人列縮小 → 正確抓到違規；還原後全綠。
- reviewer 自己加的 mutation control（`minSide` 44→20）重跑：`testTooSmallSampleIsFlagged`／`testPaddingOutsideButtonSampleIsFlagged` 轉紅、`testGoodSamplePasses` 維持綠——與 R1 記錄一致，機制未受 B1 改動影響。
- Golden sample（LS-17 pre-fix `resendRow`／登出鈕）在全部 R2 改動套用後重新驗證：仍正確紅並點名 163.0×22.0pt／34.0×20.3pt；修正後現行 main 仍綠。
- `tap-target-check.test.sh`（8 案例）、`tap-target-registry-check.test.sh`（7 案例，含 mutation-style 負控：排除清單不可子字串誤配）全過。
- `push-gate.test.sh` 37 案例（含新增 ㉓～㉖）、`detect-simulator.test.sh` 23 案例、`LittleSproutTests`（既有 unit tests）全過，無回歸。
- SwiftLint `--strict` 全 repo 乾淨。
- 完整 `push-gate.sh` 手動跑過＋`git push` 觸發的 pre-push hook 皆全綠（含新增的 tap-target-registry gate 步驟）。

**未完成**
- 無新增 informational（本輪未產生新的 reviewer finding，等待 R2 審查）。
- Hotfix 慣例的 back-merge `main`→`development` 仍待 PR #198 併入 main 後才能執行。

**風險**
- 同 R1 記錄：只認 `Button`／賦予 `.button` accessibility trait 的元件；`Features/`／`DesignSystem/` diff 判斷是路徑層級不理解語意；已註冊畫面目前只量單一 preview 狀態（其餘條件式渲染內容需額外 preview 工廠方法才量得到，M1(b) 已補上 SettingsView 這一個實例，其餘畫面若未來註冊需比照處理）。

**產出位置**
- PR：https://github.com/CLYEH/little-sprout/pull/198（`hotfix/LS-95-tap-target-gate` → `main`，head `af3375a`）
- 新增：`scripts/gates/tap-target-registry-check.sh`／`.test.sh`、`scripts/gates/tap-target-exemptions.txt`
- 修改：`LittleSprout/TapTargetGateScreenName.swift`（sentinel＋`#if DEBUG`）、`LittleSproutUITests/TapTargetMeasurement.swift`／`TapTargetGateTests.swift`／`TapTargetGateSelfTests.swift`、`LittleSprout/Services/Family/FamilyStore.swift`、`LittleSprout/Support/PreviewFamilyAPIClient.swift`、`LittleSprout/TapTargetGateHarness.swift`、`scripts/gates/push-gate.sh`／`tap-target-check.sh`／`push-gate.test.sh`、`.github/workflows/ci.yml`、`docs/COLLABORATION.md`
- 模擬器已關：A90A8C7D-7BC7-4A7C-98C5-9DFC7CF83DC4（`LS-95-iPhone17Pro`，push-gate.sh 的 EXIT trap 已自動關閉，另手動確認一次；`demo-iPhone17Pro` 未碰）


### 2026-09-01T06:05:50.158Z · Chih-Lin Yeh · `146fa7c3`

## merge-review R2 — PR #198 · head `af3375a` — **APPROVE**

覆核範圍：`git diff 9ce0bbe..af3375a`（6 commit、15 檔 +518/−24）＋回歸重測 R1 全部裁決點。CI 全綠（ci 7m2s／rules 3m40s／lint 17s／db 2m44s，run 33472326896）。所有實驗都在 scratch worktree（`git worktree add --detach` → 審完 `worktree remove`）跑，PR worktree 全程 `git status --porcelain` 為空。

**R1 的 2 blocker＋1 major 我逐條親自重演，全部確認修好**——不是接受申報。以下是實測記錄。

---

### B1（blocker）→ 已修，攻擊重演確認

我對新 head 重打 R1 那招：把 `TapTargetGateHarness.swift:20` 的環境變數鍵改成 `LS_TAP_TARGET_GATE_SCREEN_TYPO`，`activeScreen` 變 nil、app 退回 `RootView`。

- **R1**：2 個產品畫面測試 `passed`，`** TEST SUCCEEDED **`，gate 印綠。
- **R2**：`Executed 5 tests, with 7 failures`，exit 1。訊息精準：
  `SettingsView 畫面沒有渲染出 sentinel 元素「button[登出]」——harness 可能沒生效（LS_TAP_TARGET_GATE_SCREEN 鍵值走鐘、hostView 這個 case 回傳空內容、或啟動流程被別的畫面攔截…）`

還原後恢復綠（56.85s）。**5 個 test method 全部轉紅**，符合要求。

另驗 sentinel 機制自身的兩個假陰性面（coordinator 指定）：

- **「sentinel 在但畫面錯」**：產品畫面安全——`.otpVerification` 用 `staticText[輸入驗證碼]`、`.settings` 用 `button[登出]`，互不重疊，掛錯畫面必紅。**但自測樣本有碰撞**，見下方 n2（已實測）。
- **「Button 清單非空」會不會誤紅零按鈕的合法畫面**：會。見 n7（靜態論證，未實跑）。

### B2（blocker）→ 已修，且我從真實 CI log 確認「真的執行了」而非只是檔名在場

`ci.yml:146-147` 兩行加在 `rc=1` 累加區、末尾 `exit "$rc"`，邏輯正確。更重要的是我去撈這次 run 的 rules job log 實際輸出：

```
1 ✓ tap-target-check.test.sh 全部通過
1 ✓ tap-target-registry-check.test.sh 全部通過
1 ✓ ④ 排除清單不可子字串誤配
1 ✓ ⑧ 失敗但無 TAP-TARGET-FAIL 標記 → 仍 exit 1、印 log 尾段
```

兩支自測連同 mutation-style 負控都在這個 PR 的 CI 上跑過了。§7 的宣稱現在成立。

### M1（major）→ 已修（採 (a)＋(b) 兩條路都做）

- **對帳 gate 實測**：在 scratch worktree `touch LittleSprout/Features/FakeReviewProbeView.swift`（不註冊、不排除）→ `exit 1`，點名檔案並印出二選一指引。移除後回綠。
- **自測 7 案例全過**，含 ④ 子字串誤配負控（`WelcomeView：…` 不得讓 `NewWelcomeView` 蒙混）與 ⑤⑥⑦ fail-closed。
- **通過訊息已誠實**：`✓ tap-target-check：已量測畫面（OTPVerificationView、SettingsView）…；其餘 Features 畫面覆蓋見 tap-target-exemptions.txt`。
- **§4 QA 列已訂正**：改成「**部分**由機械 gate 把關…僅涵蓋…兩支…**qa 對未註冊畫面仍須視覺驗收逐顆量測**」。R1 我最在意的「機械覆蓋 0＋人工覆蓋被移除」淨退步已消除。
- 14 支 View 帳目對齊：2 註冊＋12 具名排除 = 14。

### M1(b) → 已修，實測確認

不採信申報：我把「邀請家人」列的 padding 改成 1pt 重跑 → `TAP-TARGET-FAIL: 邀請家人 frame=402.0x25.0pt（需 ≥44×44pt）`。R1 時這顆列對 gate 完全隱形，現在確實進入量測範圍。

### m1／m2／m4 → 已修

`push-gate.test.sh` 新增 ㉓～㉖ 我讀過實作：用假 `tap-target-check.sh` 記錄呼叫，**㉕ 是真的 mutation-style 負控**（只改 `Services/` 必須不觸發），㉖ 刻意把 Features 變更留在 development、hotfix 只加非 UI 檔，讓兩種 target 產生不同結果才驗得出方向矩陣。㉓～㉖ 本機重跑全過。

### m3 → 已修，五個邊界我逐一實測

用 `python3` 把 `ci.yml` 那個 step 的 `run:` 區塊**逐字抽出**成可執行腳本（避免我轉述失真），再在合成 repo 上重現 `actions/checkout` 對 `pull_request` 產生的 **merge commit** 形狀跑：

| 情境 | 結果 |
|---|---|
| 純文件 PR（`GITHUB_BASE_REF=main`） | rc=0、印「略過」、**未呼叫** ✅ |
| 含 `Features/` 的 PR | **呼叫** ✅ |
| 含 `DesignSystem/` 的 PR | **呼叫** ✅ |
| push 事件（`GITHUB_BASE_REF` 空） | **呼叫**（無條件） ✅ |
| base ref 不存在 | **呼叫**（fail-safe 方向正確） ✅ |

coordinator 點名的 push vs pull_request 坑沒踩到：`GITHUB_BASE_REF` 只有 pull_request 有值，程式碼用 `[ -n ... ]` 正確分流；三點 diff 在 merge commit 上等價於 PR 變更集；`fetch-depth: 0` 已補。**而且本 PR 自己那次 CI 的 step 6 跑了 2m42s（非 <1s 的略過路徑）——它有正確判斷「本 PR 碰了 Features/」而照跑**，沒有 false skip。

### I1 → 已修（`#if DEBUG` 已補）；I2／I3 → 不修有據，同意；I4 → 記 LS-96，同意

---

## R2 新發現（皆 minor／informational，不擋合併）

**n1（minor）— 排除理由沒有被機械要求，空理由甚至無理由都會過**
`tap-target-registry-check.sh` 檔頭寫「具名排除並附理由」、`tap-target-exemptions.txt` 訂了格式「`<ViewName>：<理由>`」，但實作只取 `：` 前的名字（`sed -E 's/：.*//'`），**理由本身沒驗**。實測兩種寫法都綠：

- `ProbeNoReasonView`（完全沒有 `：` 與理由）→ `✓ … 全數已註冊或具名排除`，exit 0
- `ProbeNoReasonView：`（有冒號、理由空白）→ 一樣 exit 0

失敗情境：後續有人趕工，直接往清單塞一行光禿禿的畫面名就過關，「不准靜默通過」退化成「不准連名字都不寫」。修法一行：要求該行匹配 `^[^：]+：[[:space:]]*[^[:space:]]`，並在自測補一條負控。

**n2（minor）— sentinel 在自測樣本間碰撞，掛錯畫面照樣綠（已實測重現）**
`.selfTestTooSmall` 與 `.selfTestPaddingOutsideButton` 的 sentinel 都是 `.button("小按鈕")`，且兩者都斷言 `violations.count == 1`。我把 `hostView` 的 `.selfTestPaddingOutsideButton` 分支改成渲染 22×22 的普通按鈕（＝padding-outside 這個幾何完全不再被測），跑完 **exit 0、gate 印綠**。

這條的份量在於：那個樣本的註解自己寫著「這是 LS-95 存在的理由，這個樣本測不出來就代表整支 gate 白做」。一旦它悄悄停止驗證 #148 R1 I4 的漏網型，沒有任何東西會紅。修法：兩個樣本用不同 label（例如「小按鈕(padding外)」），或自測改成斷言違規字串裡的實際尺寸（`20.0x20.0` vs `22.0x22.0`）而不只是數量。

**n3（minor）— 對帳 gate 只保證「有註冊」，沒保證「有被測」**
`TapTargetGateScreenName` 不是 `CaseIterable`，沒有任何測試走訪 `allCases`，`TapTargetGateTests` 是兩個寫死的方法。新增一個 case 時，`hostView(for:)` 與 `sentinel` 兩個窮舉 switch 會被編譯器逼著補分支（這點很好），但**沒有任何機制逼你補 test 方法**——結果是：畫面已註冊、對帳 gate 綠、卻從來沒被量過。修法：enum 補 `CaseIterable`，`TapTargetGateTests` 改成資料驅動走訪非 `selfTest*` 的 case，新增 case 自動被測，這條洞自然消失。

**n4（informational）— 12 條排除理由品質不齊，兩條與事實不符**
逐條看過，據實回報：

- **準確**：`AlbumsView：placeholder（ContentUnavailableView，無互動元件）`——實測該檔 `Button|NavigationLink|onTapGesture` 命中數為 **0**，理由成立。
- **不成立**：`WelcomeView：入口鈕含 Apple 官方 SignInWithAppleButton（系統渲染、非我方 SwiftUI Button）`——該畫面同時有 `GoogleSignInButton`（`WelcomeView.swift:205`）與 email 入口，那些是我方 SwiftUI 元件、完全量得到。這理由只能豁免 Apple 那一顆，不足以豁免整個畫面；而這正是 LS-17 登入流程所在的畫面。
- **偏弱**：`EmailSignInView：需要模擬鍵盤輸入與驗證流程狀態`——該檔 `#Preview` 就是一行 `EmailSignInView(authStore: .preview()) { _ in }`，跟 harness 現用的 `.otpVerification` 寫法同型，掛載成本近乎零。理由講的是「量得不夠有代表性」，不是「掛不起來」。
- 其餘（表單流程、需要假資料）屬同一類「代表性」論述，可接受但同樣偏保守。

不擋合併的理由：清單是進版控的、逐條具名、§4 已把 qa 人工量測的責任明確還回去、且全部指向 LS-96 追蹤——是「大聲排除」而非「靜默通過」。但建議下一輪優先把 `WelcomeView`／`EmailSignInView` 這兩支成本最低、風險最高的補進註冊表。

**n5（informational）— 對帳範圍僅 `Features/**/*View.swift`**
`LittleSprout/Navigation/RootView.swift`、`DesignSystem/ChildAvatarView.swift`、`DesignSystem/ScrollableFillView.swift` 不在對帳範圍；`InviteFamilyView+ActionBar.swift` 這種不以 `View.swift` 結尾的檔案也不會被列舉。這與 R1 M1 我自己提的範圍一致，不算走樣，僅記錄邊界。

**n6（informational，我自己那條建議的代價）— 兩層防護不再獨立**
CI 與 push-gate 現在用同一組路徑樣式（`Features|DesignSystem`）。R1 時 CI 無條件跑，是本機判斷失準時的獨立兜底；改成 diff 判斷後，**樣式本身若漏掉某個未來目錄，本機與 CI 會一起漏**。這是我 R1 m3 要求換來的取捨，資訊揭露而非要求變更；殘餘風險由「新增 View 檔必被對帳 gate 攔」部分抵銷。

**n7（informational）— 零按鈕的合法畫面無法註冊**
`violations()` 的 `XCTAssertFalse(elements.isEmpty)` 是 B1 的第二道防線，但也意味著：真的沒有任何 Button 的畫面一旦註冊就必然假紅。具體實例就是 `AlbumsView`（實測 0 個互動元件，目前靠排除清單迴避）。未來若要納管這類畫面，需要一個「預期零按鈕」的標記。**此條為靜態論證，未實跑重現。**

---

### 引據抽查（R2 handoff 逐條）

| 宣稱 | 覆核 | 結果 |
|---|---|---|
| typo repro → 5 個 test 全紅、還原後綠 | 親自重打同一招 | ✅ 7 failures／5 tests；還原後 exit 0 |
| 邀請家人列縮小 → 抓到 `402.0x25.0pt` | 親自改 padding 為 1pt | ✅ 字串完全一致 |
| 自測掛進 CI rules job | 讀 workflow **＋撈真實 CI log** | ✅ 兩支都實際執行過 |
| `tap-target-registry-check.test.sh` 7 案例 | 本機重跑 | ✅ 全過 |
| `tap-target-check.test.sh` 8 案例 | 本機重跑 | ✅ 全過 |
| `push-gate.test.sh` 含 ㉓～㉖ | 本機重跑＋讀實作 | ✅ 全過，㉕ 是真負控 |
| CI 改 diff 判斷 | 抽出 step 逐字重跑 5 情境 | ✅ 五個邊界全對 |
| 排除清單「具名＋理由」 | 空理由／無理由探測 | ❌ 理由未強制 → n1 |

**mutation control（打新 registry gate）**：① 未註冊未排除的假 View → 紅（達成 coordinator 要求）；② 內建 ④ 子字串負控 → 過；③ 空理由探測 → 揭露 n1。

### 四維度

- **Race condition**：`assertScreenRendered` 用 `waitForExistence(timeout: 10)`，把 R1 B1 (d) 的「snapshot 早於渲染」時序窗口一併關掉；`seedMyFamilyForPreview` 刻意用同步賦值而非 async fetch，避免新增時序窗口——這個選擇是對的。無新增並行狀態。
- **運算效能**：CI `ci` job 11m29s → 7m2s（gate step 5m06s → 2m42s，且非 UI PR 現在直接略過）。改善方向正確。
- **平行優化**：維持序列＋simulator-lock，符合 LS-70／LS-83。無 finding。
- **Scope**：新增的 app 端程式碼（`seedMyFamilyForPreview`／`preview(withFamily:)`）皆 `#if DEBUG` 圍住且只服務 harness，Release 路徑不受影響；`TapTargetGateScreenName` 補了 `#if DEBUG`。**未見票外重構或順手改動。**

### 裁決

R1 全部 blocker／major 已修並經實測確認，R2 新發現皆 minor／informational。**APPROVE**。

建議 n1～n3 併入下一次動到這塊的票或記入 LS-96（n3 的 `CaseIterable` 改法順帶解掉 n2 的一半），n4 的 `WelcomeView`／`EmailSignInView` 優先補進註冊表。以上不阻擋本 PR 合併。

模擬器：`LS-95-iPhone17Pro`（A90A8C7D）已 shutdown，`demo-iPhone17Pro` 未碰；scratch worktree 與合成 repo 已清除，`/tmp/simulator-lock-*` 無殘留。

（附帶更正 R1 comment 的一處筆誤：I2 內文「#Repview」應為「#Preview」，技術結論不變。）

### 2026-09-01T11:44:46.962Z · Chih-Lin Yeh · `830029d6`

## Orchestrator retro（收尾儀式；純 harness 票，dead-code-sweeper 免跑）

**過程**：R1 實作（XCUITest 路徑選型＋黃金樣本重現歷史 bug）→ merge-review R1 REQUEST_CHANGES（2 blocker 全在假陰性面：harness 未啟動靜默全綠、自測未接 CI；M1 覆蓋倒退）→ R2 修復（sentinel 斷言＋registry 對帳 gate＋exemptions＋誠實文件）→ R2 APPROVE（reviewer 重攻擊驗證＋抓 CI log 證明自測真的跑）→ 併 main `a586ac9`、back-merge PR #199。

**Lesson learning**：
1. **「量測工具自己也要被量測」**——本票兩條 blocker 都是 gate 的假陰性（測不到＝通過）。派審時指定「假陰性面」攻擊面再次證明有效；後續所有 gate 類 harness 票的審查 prompt 固定帶這個面。
2. **文件宣稱與機械覆蓋同步**：M1 的核心不是缺 12 個畫面，而是同 PR 把「qa 不必手動量」寫進文件——覆蓋未達前先撤宣稱。原則「要嘛被量、要嘛大聲排除」已落地為 exemptions.txt＋registry gate。
3. **reviewer 抓 CI log 驗證「已接線」**（不只看檔名在 workflow 裡）是值得固定的覆核手法。
4. 豁免清單品質（WelcomeView/EmailSignInView）與 n1–n3 記 LS-96（comment `1abd28b8` 同池）。

informational 處置：n1–n3＋豁免品質＋CI/push-gate 耦合觀察已記 LS-96。

### 2026-09-01T12:00:21.813Z · Chih-Lin Yeh · `bc43add2`

結案：PR #198 併 main `a586ac9`（merge-review=success on `af3375a`，linear:146fa7c3）；back-merge PR #199 併 development `cbea34c`（§2 驗證：merge-tree 無內容差、status 貼 `a586ac9` id 53293136424；CI 全綠含 tap-target step 實跑）。收尾儀式完成——純 harness 免 sweeper、retro（comment `830029d6`）、informational 記 LS-96（comment `c0645ba6`）、清理：`cleanup-merged.sh --apply LS-95` 移除本票 worktree＋本機分支。

