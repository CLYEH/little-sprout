# LS-55 iOS 基礎層收尾 minors（PR #63 R2 N1-N9）：initialSession 離線抹 nil、快取非同步測試、LS999 測試、死碼 userFacingMessage

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | — |
| Cycle | — |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M1 登入與家庭 |
| 估點 | — |
| 建立 | 2026-08-24T17:10:30.480Z |
| 開始 | 2026-08-24T19:59:19.634Z |
| 完成 | 2026-08-24T21:04:16.194Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-55/ios-基礎層收尾-minorspr-63-r2-n1-n9initialsession-離線抹-nil快取非同步測試ls999-測試死碼 |
| 父票 | LS-49 iOS 基礎層：Supabase SDK 整合＋環境設定＋Auth session 服務（無 UI） |
| 關係 | related→LS-58, related→LS-49, related→LS-54, related→LS-17, related←LS-56, related←LS-62, related←LS-60, related←LS-50, related←LS-52 |

## 描述

PR #63 第 2 輪 APPROVE 附帶 minor，須在 [LS-17](https://linear.app/little-sprout-app/issue/LS-17/storysign-in-with-appleemail-otp-登入)**（登入 UI）開工前**處理 N1/N7（root routing 會踩到）：

* **N1** `SupabaseClientFactory` 未設 `emitLocalSessionAsInitialSession: true` → 離線開 app 時 SDK refresh 失敗 emit `.initialSession(nil)`，`SupabaseAuthService` 快取被抹成 nil（但底層 session 仍在）→ 離線回訪被誤判未登入。修：加該 option（SDK 已警告舊行為將移除）或監聽器忽略 `.initialSession(nil)`。
* **N2** 快取的非同步更新路徑（`authStateChanges`）零測試——補「觸發底層 refreshSession → expectation 等快取更新」。
* **N3** `AppError.userFacingMessage` 零呼叫者零測試——留則補「不外洩後端訊息」測試，否則刪。
* **N5** `AppErrorTests.swift:89-93` 測試名/訊息仍寫「LS0xx→validationRetryable」舊前提，改名。
* **N6** `setRequireApproval` 補 happy path 測試。
* **N7** `AuthService.currentSession` 非 Observable——[LS-17](https://linear.app/little-sprout-app/issue/LS-17/storysign-in-with-appleemail-otp-登入) 需 observable 包裝層，文件勿寫「可安全用於 SwiftUI body」。
* **N9** LS016（邀請碼撞碼）歸 validationRetryable 但使用者無可確認之物——UI 票特判，或四層文法補「可重試非使用者之過」層（設計面）。
  （N4/N8 屬 harness，已併入 [LS-54](https://linear.app/little-sprout-app/issue/LS-54/harness-小修批次api-contract-check-n1-n5approve-join-註解矛盾pr-58-r2-minors)。）

## 留言（4）

### 2026-08-24T20:29:45.349Z · Chih-Lin Yeh · `93a9412f`

## merge-reviewer R1 — PR #77 → ITERATE（審 head `02f226f`；orchestrator 代貼＋裁決）

**Blocker**
- **B1**：PR 與 `development` 衝突、GitHub 建不出 merge ref → CI 從未執行（`check-runs total_count: 0`）。衝突檔 `AppError.swift`／`AppErrorTests.swift`。→ **已解**：merge commit `dfee777`，CI 四 job 綠、MERGEABLE CLEAN。
- **B2(a)**：合併後 `LSErrorCode` 多出 LS020–024，三分集聯集斷言必紅——證明測試設計正確。→ **已解**（`dfee777` 更新三分集，66 tests 綠）。
- **B2(b)**：**LS022 `timelineCursorIncomplete` 在 N9 新文法下無處可歸**——游標是 app 自己組的，使用者沒有輸入可換，重試同一呼叫也不會成功；留在 `.validationRetryable` 違反本 PR 剛寫下的定義。→ **Orchestrator 裁決：改 `.rejected`**，註解寫明理由（呼叫端該修 code／回上一頁），`docs/API.md` §5 該列歸層註記同步。LS020/021/023/024 維持 `.rejected`。

**應修**
- **M1**：`TestSupabaseClient.swift:22-30` 自己也設 `emitLocalSessionAsInitialSession: true`，刪掉 `SupabaseClientFactory.swift:31-40` 的正式設定後 N1 測試仍全綠——正式路徑零攔截。→ **修**：抽成 `LittleSprout` 內 internal 常數，正式 factory 與 test helper 共用同一值（`@testable import`），使 N1 測試釘住正式設定。

**Informational（handoff 逐項寫處置）**
- I1 N1 的 SDK 行為逐行核對成立（`AuthClient.swift:1403-1438`／`SessionManager.swift:59-103`／`APIClient.swift:117-123`，v2.49.0）；只有該旗標變了，Keychain／PKCE／autoRefresh 預設未動。→ 證據收進 handoff。
- I2 無 data race（`OSAllocatedUnfairLock` 全路徑 `withLock`；`true` 分支同步讀 storage 再 emit，少一個 stale-overwrite 窗口）。
- I3 N2 確實驗到背景監聽路徑；attach 時序造成低機率 flake（PLAUSIBLE）。→ 登記；CI 若間歇紅先查此處。
- I4 `rejectJoin`／`withdrawJoin` 兩測試越出 N1–N9。→ **保留，本行即登記「已知越界、經 review 同意保留」**。
- I5 離線時 `currentSession` 改為回傳過期 session（以前 nil），LS-17 routing 必須查 `expiresAt`。→ **修**：`AuthService.currentSession` 文件補一句；LS-17 票另記。
- I6 效能無疑慮；I7 測試隔離不受影響；I8 單一 commit 符合 §3。

**未審範圍**：reviewer 無 Linear 工具，票文與 PR #63 R2 原文未讀，N4/N8 對應由 git 史（LS-54 `651ffb1`）間接佐證——orchestrator 補驗：LS-54 D3/N4′/N8 即 PR #63 R2 的 N4（parallel NO）與 N8（序列測試），對應成立。

**R2 進場**：LS022 改層＋M1＋I5 落地後，只驗這三項＋CI 綠即可 APPROVE。

### 2026-08-24T20:43:24.980Z · Chih-Lin Yeh · `9cc1eea2`

## merge-reviewer R2 — PR #77 @ `add6263`：APPROVE（orchestrator 代貼）

四項全 PASS，證據親自複現：
- **B2b LS022 → `.rejected`**：switch／三分集／API.md §5 列＋摘要四處同步；`AppErrorTests` 24/24；`error-codes-check.sh` RC=0（15 碼）。
- **M1**：全 repo 僅 `SupabaseClientFactory.swift:22` 一處字面 `true`；常數改 `false` → N1 測試 `TEST FAILED (2.631s)`；還原 → `passed`，`shasum` byte-identical、`git status` 乾淨。
- **I5**：`AuthService.swift:12-16` 與 SDK `AuthClient.swift:1404-1416` 語意一致，`AuthSession.expiresAt` 存在。
- **N1 sleep 2.5s**：v2.49.0 `defaultRetryLimit=2`、恰好重試一次、退避整 1s（固定 `Task.sleep`，慢機不放大）；實測事件 ~1.05s 到達、餘裕 1.45s；綠燈斷言也落在背景刷新失敗之後（多驗到「失敗刷新不清快取」）。
- Scope 乾淨（7 檔全可追溯）。

**Informational → 處置**
- I-A 2.5s 寫死 SDK 內部常數，SDK 升 retryLimit 即無聲假綠 → **開 backlog 票**：改計數離線 stub 呼叫次數＋短 settle。
- I-B 勿改用 `authStateChanges` 計數（token-scoped，無序保證）→ 記入該票。
- I-C `AppError.swift:86`「這三碼」註解失準 → 已併 LS-58 順修。

**未涵蓋**：未重審 R1 已過項；未跑完整 66 測試（ios-dev 宣稱；CI 四 job 綠為佐證）。

→ 併入 development；LS-55 → QA（等 promote 批次）。

### 2026-08-24T20:56:28.781Z · Chih-Lin Yeh · `768cb8a0`

## QA — `test` branch @ `7bd64ed`（含 `add6263`／PR #77 merge PR #82）— **PASS**

worktree `.claude/worktrees/qa-test`，`git fetch && git checkout test && git pull` → fast-forward `a1c18ac..7bd64ed`，`git log` 確認含 `add6263`。

### Build/Gate
- `xcodegen generate`：0 diff（`git status`/`git diff` 乾淨）
- `swiftlint --strict`：0 violations，30 files
- `xcodebuild test -scheme LittleSprout -destination "platform=iOS Simulator,name=iPhone 17 Pro,OS=26.0.1"`：**66/66 passed, 0 failures**
- `scripts/gates/api-contract-check.sh`：PASS（RPC 14／表 17，與 schema 一致）
- `scripts/gates/error-codes-check.sh`：PASS（15 自訂碼，docs/API.md §5 與 `LSErrorCode` 一致）

### 逐條 N1–N9

- **N1 ✓** `grep -rn emitLocalSessionAsInitialSession` 全 repo：字面 `true` 僅 `SupabaseClientFactory.swift:22` 一處；`TestSupabaseClient.swift:31` 透過 `@testable import` 引用同一常數，未另寫字面值。親自紅→綠：改 `true`→`false` 跑單測 `test_initialSession_offlineWithExpiredLocalSession_doesNotClearCache`（`SupabaseAuthServiceTests.swift:214`）→ `XCTAssertEqual failed: ("nil") is not equal to ("Optional(...)")`（RED，2.663s）；改回 `true` → `TEST SUCCEEDED`（2.522s）。還原後 `shasum -a 256` 與改動前 byte-identical（`1a8c936…`），`git status`/`git diff` 全乾淨。
- **N2 ✓** `test_authStateChanges_backgroundRefresh_updatesCacheAsynchronously`（`SupabaseAuthServiceTests.swift:176`，全測跑 0.041s 綠）：直接呼叫 `client.auth.refreshSession()`（非 `service.refreshSession()`），繞過 `SupabaseAuthService` 自己的方法，靠 `authStateChanges` 背景監聽把快取同步更新，poll `service.currentSession` 驗證。
- **N3 ✓** `test_userFacingMessage_neverLeaksBackendMessage`（`AppErrorTests.swift:249`）逐 case（network／validationRetryable／retryableSystem／rejected／server）灌入含 HTML／狀態碼／SQLSTATE 的假訊息，斷言 `userFacingMessage` 不等於原文也不含任一洩漏片段。
- **N5 ✓** 原 `AppErrorTests.swift:89-93` 已改名為 `test_map_postgrestError_inviteCodeExhausted_isValidationRetryable`（`AppErrorTests.swift:89`），comment 明講改名理由（舊名暗示「所有 LS0xx→validationRetryable」與 LS001/LS013 等 LS0xx 實為 `.rejected` 矛盾）。
- **N6 ✓** `test_setRequireApproval_success_doesNotThrow`（`SupabaseFamilyAPIClientTests.swift:152`）happy path：斷言 PATCH `/rest/v1/families` body `require_approval:false`，stub 200 回應並解出 `require_approval` 為 false。
- **N7 ✓** `AuthService.swift:12-24` `currentSession` doc 明文「這個屬性本身不是 Observable」＋離線時「可能是一份已過期但非 nil 的 session」，呼叫端須查 `expiresAt`，明白警告 LS-17 root routing 不可直接拿它驅動畫面。
- **N9 ✓** `AppError.retryableSystem` 已加（`AppError.swift:23`），`LSErrorCode.Tier` 三案例。`test_lsErrorCode_tierClassification_isExhaustiveAndCorrect`（`AppErrorTests.swift:169`）斷言三集合聯集＝`Set(LSErrorCode.allCases)`＋兩兩 `isDisjoint`。LS016 → `.retryableSystem`（另有專測 `test_map_postgrestError_inviteCodeGenerationCollision_isRetryableSystem`，`AppErrorTests.swift:217`，綠）；LS022 → `.rejected`，與 PR #77 R1 comment B2(b) 的 orchestrator 裁決一致（票文本身未提 LS022 歸層，已核對 R1 comment 為準，非票文遺漏）。
- **N4/N8**：依 brief 不重驗，已於 LS-54 確認存在；spot-check `.github/workflows/ci.yml:300` 仍為 `-parallel-testing-enabled NO`。

### 冒煙
- **UI 冒煙（登入／時間軸／上傳／留言）：BLOCKED** — app 目前無登入畫面（LS-17 尚未實作），非本票範圍，未執行、不計入 PASS。
- **RLS 冒煙：N/A** — `git diff a1c18ac..7bd64ed -- supabase/` 為空，本票零 SQL/RLS 變更，無跨 family 資料路徑可驗。

### 裁決：**PASS**

所有 N1–N9 條目逐條驗證通過（含 N1 親自紅→綠複現），全測試 66/66 綠，四道 gate 全過，worktree 乾淨無殘留變更。

### 2026-08-24T21:01:20.668Z · Chih-Lin Yeh · `2cf2bc2f`

## 收尾儀式（QA PASS 後、Done 前）

### 死碼巡檢（dead-code-sweeper）
11 個 diff 檔逐檔；`retryableSystem`／`userFacingMessage` 零生產呼叫者屬「後端先行」既定模式（LS-17 為呼叫端）；`Tier` 三 case 全用到；N1 支架 `SessionFixture.expiresAt`／`TestSupabaseClient.storage` 各有唯一對應呼叫；`dfee777` 解衝突無重複 case／測試名；舊測試名零殘留；TODO/DEBUG/print 零命中；API.md §5 與程式一致；swiftlint strict 零違規。**零 finding**。I-C「這三碼」註解確認仍失準且未被誤動（LS-58 worktree 正在改）。

### Lesson learning（orchestrator）
1. **PR 開在過舊 base 上＝CI 永遠不跑且無訊號**：靠讀 `mergeStateStatus` 才發現。→ LS-50 加 push 前 `merge-tree` 衝突檢查。
2. **測試守錯層**：N1 迴歸測試原本守的是 test helper 自己寫死的旗標——刪掉正式設定仍綠。修成共用常數後，「改 false 必紅」成為驗收標準（QA 也親自複現）。原則：凡「正式設定」類修正，測試必須能因正式碼退化而紅，handoff 附紅→綠證據。
3. **sleep 等 SDK 重試是無聲假綠的溫床**：300ms 等不到 1s 退避、2.5s 又寫死 SDK 常數 → LS-62。
4. **工具缺口**：merge-reviewer 讀不到票文（LS-60）；本票 R1/R2 兩輪 comment 皆 orchestrator 代貼。

### Done 前置
QA PASS（66/66、gate 全綠）；promote test→main 進行中；併入即 Done。

