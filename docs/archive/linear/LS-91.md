# LS-91 Harness：Pen 開檔路徑對帳＋機械落地——pen-open.sh（open -a 切檔＋對帳）／pen-land.sh（autosave 結構 diff → 落地 → landing gate），根治 Pencil 單一文件與多 worktree 衝突

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Urgent |
| 標籤 | size:M, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-25T07:17:18.755Z |
| 開始 | 2026-08-25T10:15:24.698Z |
| 完成 | 2026-08-25T13:52:30.070Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-91/harnesspen-開檔路徑對帳機械落地-pen-openshopen-a-切檔對帳pen-landshautosave-結構-diff |
| 關係 | related→LS-82, related→LS-96, related→LS-17, related→LS-26, related→LS-68, blocks→LS-67, related→LS-44, related→LS-72, related→LS-87, related→LS-86, related→LS-81, related→LS-88, related→LS-85, related←LS-238, related←LS-208, related←LS-180, related←LS-142, related←LS-119, related←LS-69, related←LS-108, related←LS-117, related←LS-111, related←LS-112, related←LS-116, related←LS-107, related←LS-103, related←LS-39, related←LS-101, related←LS-92, related←LS-79, related←LS-94 |

## 描述

## 實作範圍（B 段，2026-08-25 以 comment「B 段改寫」與「落地行為確認」為準；本段取代下方原計畫）

* `scripts/ops/pen-open.sh <root>`：`open -a Pen <root>/design/littlesprout.pen` → 輪詢讀 Pen 目前文件路徑 → 一致 exit 0／不一致 exit 1／取不到 exit 2；`--status` 只印路徑。orchestrator 派設計票前跑；移除設計 worktree 前先切回主 checkout。已知盲區：目標檔已在背景視窗開著時 `open -a` 不切換（需人工關閉殘留視窗）；Pen 全域單一實例、對帳為 TOCTOU。
* `scripts/ops/pen-land.sh <root> [--expect-nodes N] [--after <epoch>] [--dry-run] [--allow-unchanged]`：sha1(file URI) 找 `~/.pencil/backup` → python 結構 diff（節點數／id 集合／variables／themes／fileToken 不變）→ 新鮮度（backup mtime ≥ --after）→ cp 到 tmp → landing gate → mv。ui-designer 每輪落地呼叫。
* 三份 agent 定義：開工先 `get_app_state` 對路徑、不符即停；handoff「Pen 路徑」欄。
* 規約 §2／§4-b（巡檢對路徑）／§7 兩列。
* 夾帶 [LS-94](https://linear.app/little-sprout-app/issue/LS-94/harnessagent-tools-check-補釘-qa-的-mcp-pencil-executeios-dev-規則列visual)（agent-tools-check：qa 釘 `mcp__pencil__execute`、ios-dev 列；visual-reviewer.md `export_nodes` 校正；§7 qa 盲區）。
* **不做**固定 design worktree（切檔已機械化）。

## 驗收

* 自測全綠並掛 CI；merge-reviewer 突變測試通過；`pen-open.sh` 對主 checkout 對帳 exit 0；[LS-67](https://linear.app/little-sprout-app/issue/LS-67/設計多寶貝-profile-三畫面建檔管理切換-ui-designervisual-reviewer-3-輪) 開工時用 pen-open 切到其 worktree 一次通過。

---

## 原計畫（A 段前，保留供追溯）

來源：[LS-81](https://linear.app/little-sprout-app/issue/LS-81/設計-choreaw57e-角托壓紙緣量與-tokens-板5pt慣例不一致ls-72-visual-reviewer-範圍外觀察)（2026-08-25）Pencil MCP `filePath` 無效、所有編輯落在 Pen 當下開著的文件、autosave 只寫 `~/.pencil/backup/`、MCP 無 open／switch／save API。原提案「固定 design worktree `.claude/worktrees/design`」＋F1–F4／G1–G4，經 A 段實測（pen CLI 0.3.4：`open -a Pen` 可切檔；headless 對本檔因相對 URL 載入失敗）改為上方 B 段。

## 留言（10）

### 2026-08-25T07:24:43.871Z · Chih-Lin Yeh · `73f67417`

## 根治方案升級：pen CLI（`@pen.dev/cli` 0.3.4，本機已裝 `/opt/homebrew/bin/pen`，尚未登入）

使用者 2026-08-25 提供 https://docs.pencil.dev/for-developers/pen-cli 。評估：**比「固定 worktree」更根本**，三個能力直接對應今天的三個症狀：

1. **headless 每次指定檔案**：`pen interactive --in <worktree>/design/littlesprout.pen --out <同路徑>` 開一個無 GUI 的 shell，`execute()`／`get_app_state()` 同 MCP 語意，`save()` 寫回指定檔——沒有「全域單一文件」、沒有「無 save API」、沒有「切檔靠人」。每個 worktree 各自一個 shell。
2. **連線模式可指定檔案**：`pen interactive --app desktop --in <file>` 連到正在跑的 Pen app 並開該檔——即使仍用 GUI，也能由 orchestrator 機械切檔（待實測是否真的切換 app 的開檔）。
3. **CI 可渲染**：headless 下 `execute` 支援 `TakeScreenshot()`／`Export()`，`--enable-preview` 每次變更存 PNG——LS-68 的溢出掃描與截圖可以真的在 CI 跑（需 `PEN_CLI_KEY` Developer Key 進 CI secrets），收據 gate 可升級為 CI 實跑。

**前提／限制**：需後端登入（`pen login` 互動式，只有使用者能做；CI 用 `PEN_CLI_KEY`，key 存 `.env`／GitHub secrets，保管等級同 PAT）；CLI session 與 desktop app 分開（`~/.pencil/session-cli.json`）；無離線模式；0.3.4 早期版本；互動 shell 的每次工具呼叫是否計費待查（agent 模式才用模型，我們不用）。

**本票改為兩段**：
- A（使用者登入後，orchestrator 實測，1 小時內）：(a) `pen interactive --app desktop --in <LS-81 路徑>` 是否切換 app 開檔；(b) headless `-i/-o`＋`save()` 對本檔（2676 節點）可行、landing gate 綠；(c) headless `TakeScreenshot` 輸出可用於溢出掃描。結論寫進本票 comment。
- B（依 A 結果）：ui-designer／visual-reviewer／qa 改用 headless shell（Bash 驅動，`filePath` 由 CLI 注入）或連線模式切檔；`.mcp.json` 的 pencil MCP 保留給人看圖；LS-68 收據 gate 升級為 CI 實跑；固定 worktree 方案降為備案。§7 兩列同步。

### 2026-08-25T07:50:11.796Z · Chih-Lin Yeh · `06be135d`

## A 段實測結果（2026-08-25 16:50，pen CLI 0.3.4 已登入：leoyeh906@gmail.com，session Active）

| 測項 | 結果 |
|---|---|
| (a) `pen interactive --app desktop --in <LS-81 檔>` 能否切換 app 開檔 | **否**——`get_app_state` 仍回 LS-72 的 stale 路徑；`--in` 在連線模式無效。 |
| (a′) **`open -a Pen <worktree>/design/littlesprout.pen`** | **可以**——app 立即切到該檔，`--app desktop` 的 `get_app_state` 回 `.claude/worktrees/LS-81/design/littlesprout.pen`。**這就是機械切檔的方法**，不需 MCP API。 |
| (b) headless `pen interactive -i <本稿> -o <out>`＋`save()` | **失敗**：`Error loading scene graph: Base URI must be absolute!` → 文件載入為空（0 節點），`save()` 寫出 15 KB 空稿。根因：本稿有 26 個相對路徑的圖片 `url`（`hero-grandma.png` 等），headless 載入器沒有 base URI；絕對路徑、`file://`（被當相對路徑拼接）、切到 `design/` cwd 都無效；空畫布 headless 正常。屬 CLI 0.3.4 限制，應回報 pen.dev。 |
| (c) headless `TakeScreenshot`／`Export` 進 CI | **未測**——被 (b) 擋住；待 (b) 修復或改用 app 連線模式（需 GUI，CI 不可）。 |
| 登入 | `pen login` 是 inquirer 互動提示；expect 六版都卡在 email 輸入不吃字，改 Python `pty` 驅動成功；OTP 由使用者提供。CI 用 `PEN_CLI_KEY` 另建。 |

## B 段改寫（依 A）

- **主方案：`open -a Pen` 機械切檔**。orchestrator 派設計票前執行 `open -a Pen <該票 worktree>/design/littlesprout.pen`，並以 `pen interactive --app desktop` 跑 `get_app_state()` 對路徑（可寫成 `scripts/ops/pen-open.sh <worktree>`：open → 等 5s → 對帳 → 不符 exit 1）。agent 定義：開工第一步 `get_app_state` 路徑必須＝自己的 worktree，否則停下回報。移除設計 worktree 前先切回主 checkout 的檔（同一支腳本 `pen-open.sh /Users/clyeh/little-sprout`）。
- 固定 design worktree 降為選配（切檔已機械化就不需要）。
- headless／CI 渲染：留給 LS-68，先向 pen.dev 回報相對 URL 載入 bug；修復後再評估。
- §7 兩列：Pen 路徑一致（前饋＝派工前 pen-open.sh；反饋＝agent 開工對帳＋巡檢 `--app desktop get_app_state` 對帳）。

### 2026-08-25T07:59:41.130Z · Chih-Lin Yeh · `0fb5f855`

**落地行為確認（LS-81 R2 收尾 agent 實測，2026-08-25 17:12）**：即使用 `open -a Pen` 把 worktree 檔開成 active document，Pen **仍只 autosave 到 `~/.pencil/backup/<sha1(file URI)>`**，不寫 worktree 檔（backup mtime 15:50:47、worktree 檔 15:52:35 且內容＝orchestrator cp 後的 bytes）。因此「落地」＝`cp backup → worktree`（LS-26 程序），而 auto-mode 分類器會擋 agent 對 `design/littlesprout.pen` 的覆蓋。

**本票 B 段追加**：
- `scripts/ops/pen-land.sh <worktree>`：算 `sha1("file://<abs path>")` 找 backup → python 結構 diff 印變更清單（節點數／id 集合／meta 不變）→ cp → landing gate；由 ui-designer 呼叫。若仍被分類器擋，請使用者在 `.claude/settings.local.json` 加 `Bash(bash scripts/ops/pen-land.sh:*)` 白名單（腳本本身含驗證，是安全的落地路徑）。
- `open -a Pen` 切檔 ＋ `pen-land.sh` 落地 ＝ 完整機械流程；agent 定義兩步都寫進去。

### 2026-08-25T12:21:11.887Z · Chih-Lin Yeh · `477c232f`

**Handoff（重派接手，B 段落地）**

**Ticket**：LS-91

**已完成**：
1. `scripts/ops/pen-open.sh <worktree-or-repo-root>`／`--status`：前手已寫好（open -a Pen 切檔 → pen interactive --app desktop 輪詢 get_app_state() 對帳，≤15s 看門狗；一致 exit 0／不一致 exit 1／Pen 未開或 CLI 未登入 exit 2）。我接手後補了自測與檔頭修正（見下）。
2. `scripts/ops/pen-land.sh <worktree-or-repo-root> [--expect-nodes N] [--dry-run]`（新寫）：取代 LS-26 手動 SOP。sha1("file://<絕對路徑>") 找 `~/.pencil/backup/<sha1>`（對主 checkout 實測確認目錄與命名）→ python3 結構 diff（節點總數／id 集合／variables·themes·fileToken 不變／逐 (id,prop) 差異排除 children）→ meta 變更或 diff 失敗即不 cp、exit 1 → 否則 cp＋跑 design-landing-check.sh --expect-nodes；--dry-run 零副作用。`$PEN_BACKUP_DIR` 供自測覆寫，不碰真正的 backup。
3. `pen-open.test.sh`（12 組）／`pen-land.test.sh`（16 組），皆用 stub／合成 fixture，掛進 CI「Ops 腳本自測」step。
4. `ui-designer.md`／`visual-reviewer.md`／`qa.md`：開工第一步 get_app_state 對路徑，不等於自己 worktree 即停下回報；ui-designer 每輪落地改呼叫 pen-land.sh；三份 handoff 皆加「Pen 路徑」欄。
5. `docs/COLLABORATION.md`：§2 加派工前 pen-open.sh／移除設計 worktree 前切回主 checkout；§4-b 巡檢模板第 2 步加 `pen-open.sh --status` 對帳；§7 加兩列（Pen 路徑一致／.pen 落地機械化）。
6. **夾帶 LS-94**（`Bundles: LS-94`，commit body／PR body 皆已宣告）：(a) `agent-tools-check.sh` 補釘 qa 的 `mcp__pencil__execute`＋補 `ios-dev` 列＋各一組自測；(b) `visual-reviewer.md` 的 `export_nodes`（本 repo pencil MCP 不存在）改成 `execute` 的 TakeScreenshot／Export 正確寫法；(c) §7 agent-tools-check 列盲區欄補「qa 持 execute 等於可寫 .pen，只有文字約束；pen-open 對帳只在開錯檔時擋一層」。

**已驗證**：
- 自測全綠：`pen-land.test.sh`（16 組）、`pen-open.test.sh`（12 組）、`agent-tools-check.test.sh`（23 組，六份 agent）。另重跑全部 `scripts/gates/*.test.sh`（14 支）與 `scripts/ops/*.test.sh`（6 支）確認無退化；`tracked-ignored-check.sh`／`error-codes-check.sh`／`brand-skill-check.sh` 對本 repo 實跑皆綠。
- push gate（xcodebuild test + SwiftLint，本票無 Swift 變更）通過。
- **pen-open.sh 對真正的 Pen app 實機復驗**（非真檔 save/cp）：開工時 Pen 開著 LS-91 worktree；嘗試切回主 checkout 失敗（30 秒輪詢仍不一致）→ 發現重要限制：`open -a Pen <path>` 只在該路徑**尚未於背景視窗開著**時可靠——本機累積 5 個過去票的背景 renderer（LS-17-impl／LS-72／主 checkout／LS-81／LS-91），對已開著的路徑重開不會重新奪回 active。確認全部視窗 `isDirty:false` 後 `kill -TERM` Pen 主行程乾淨結束、`open -a Pen` 重開，pen-open.sh 對主 checkout 路徑核對 exit 0——**結束前已切回主 checkout 並確認**。此限制與復原程序已寫進 `pen-open.sh` 檔頭與 §7 盲區欄。
- 復驗過程也發現 A 段記錄的「`--in` 在連線模式無效」需要修正：`pen interactive --app desktop --in <file>` 確實會讓**那一次**呼叫回報該路徑，但只是 session-local 覆寫，不影響 app 真正的 active document、不影響其後不帶 --in 的呼叫（含 Pencil MCP）——不是真正的切檔手段，已在檔頭更正措辭。

**未完成／剩餘**：這是本票的初次送審 PR（#152），尚無 merge-reviewer 審查回合，因此沒有 informational finding 需要處置。

**風險與已知問題**：
- `pen-open.sh` 對「已開著背景視窗」的路徑切換不可靠是本票最大的殘留風險——已記錄復原程序（安全 quit＋重開）但**未自動化**：自動 quit 使用者的 GUI app 有誤殺未儲存編輯的風險，兩害相權留給人工判斷（腳本逾時就 exit 1，不擅自處理）。
- qa 持有 `mcp__pencil__execute` 技術上可寫入 .pen，唯讀仍只是 qa.md 文字約束，無機械擋（§7 已記盲區）。
- headless CI 渲染仍不可行（pen CLI 0.3.4 對含相對路徑圖片 url 的檔案載入空場景），留給 LS-68 評估，已回報進待辦。

**產出位置**：
- PR：https://github.com/CLYEH/little-sprout/pull/152（`hotfix/LS-91-pen-path` → `main`，head SHA `17433ed5e187cab824806b3f69b32f83f4cf04ca`，remote 已核對一致）
- Pen 路徑：結束時已核對＝`/Users/clyeh/little-sprout/design/littlesprout.pen`（主 checkout，`pen-open.sh` exit 0 確認）
- 檔案：`scripts/ops/pen-open.sh`、`scripts/ops/pen-open.test.sh`、`scripts/ops/pen-land.sh`、`scripts/ops/pen-land.test.sh`、`.github/workflows/ci.yml`、`.claude/agents/{ui-designer,visual-reviewer,qa}.md`、`scripts/gates/agent-tools-check.sh`、`scripts/gates/agent-tools-check.test.sh`、`docs/COLLABORATION.md`

不等 CI；PR 已推送，CI 由 orchestrator 監看。

### 2026-08-25T12:36:33.007Z · Chih-Lin Yeh · `84222d06`

**merge-review R1：REQUEST_CHANGES**（PR #152，head `17433ed5`；全文因自審 422 落在 PR comment：https://github.com/CLYEH/little-sprout/pull/152#issuecomment-5410456612 ）

**F1（major）落地新鮮度檢查被刪、無替代** — `ui-designer.md:23-26` 刪掉 LS-26 收工程序步驟 2（backup mtime 晚於最後一次 execute），`pen-land.sh` 沒有任何 mtime 比對。`--expect-nodes` 依定義抓不到「只改屬性」的輪次（改色／字級／間距——正是迭代輪最常見形態）。實跑重現：只改屬性 + autosave 未追上 → `pen-land.sh --expect-nodes N` 印「（結構無差異）」＋「✓ design-landing gate 通過…節點數與畫布一致」→ **exit 0**，agent 依 SOP 判定「已落地」，但本輪改動零位元組進 repo，commit-gate／CI 同樣綠。修法：`pen-land.sh` 加 `--after <epoch>`（backup mtime 早於該值即拒），或至少印兩邊 mtime＋「結構無差異」時 exit 非 0。連帶 §7 LS-26 列與 `design-landing-check.sh:42,61` 的「收工程序步驟 2/4」引用已懸空，一起對齊。

**minor 4**：F2 cp 先於 landing gate、紅了不回滾也沒教怎麼收拾（建議 cp→tmp→gate→mv，順帶取得原子性）；F3 省略 `--expect-nodes` 時退回 backup 自身節點數，gate 卻印「與畫布一致」誤導綠字；F4「自己 worktree」對無專屬 worktree 的 qa 未定義（建議三份統一寫 `$(git rev-parse --show-toplevel)/design/littlesprout.pen`）；F5 Pen 全域單例無互斥、對帳是 TOCTOU，§7 盲區欄未記（可比照 `supabase-lock.sh`）。

**informational 6**（I1 建議隨 F1 一起修，其餘依 §5-b 記入待辦池 LS-96）：I1 §7／gate 訊息懸空引用；I2 backup-not-found 訊息漏「路徑編碼」原因、舊 SOP 的空白路徑 caveat 被刪；I3 `pen-land.test.sh` 註解宣稱測了「want 壞 JSON」但實際沒有；I4 §2「移除 worktree 前切回主 checkout」無機械反饋（待 LS-86 `cleanup-merged.sh`）；I5 本票 description 仍是 A 段前舊計畫、與 B 段實作不符，建議更新或註明驗收以 B 段 comment 為準；I6 `pen-open.sh` 對 pen CLI 輸出格式的耦合只有 stub 覆蓋（fail-closed，方向安全）。

**Scope 乾淨**：恰為預期 11 檔，`Bundles: LS-94` 宣告與夾帶內容一致。**CI 四項全 pass**。我實跑：三支自測全綠（16／12／23）、`agent-tools-check.sh` 對 head 六份定義 exit 0、**9 組突變全被自測抓紅**（含 sha1 改 sha256、cp 提前、dry-run 仍 cp、路徑比對退化成 basename）。未驗：真實 Pen app／`~/.pencil/backup`／pen CLI 未碰，handoff 的實機記錄採信未複驗。

### 2026-08-25T13:04:12.329Z · Chih-Lin Yeh · `366b86d5`

**Handoff（R2，回應 merge-reviewer R1）**

**Ticket**：LS-91

**已完成**（本輪 R2）：
1. **F1（major）**：`pen-land.sh` 加 `--after EPOCH`（backup mtime 早於它即拒、印兩邊 epoch——LS-26 步驟 2 的機械版）；「結構完全無差異」現在預設也拒絕（本輪零變更或 autosave 未追上分不出來），`--allow-unchanged` 供刻意放行。ui-designer.md 收工程序改為 `t=$(date +%s)` → `pen-land.sh --after "$t"`。自測 `⑨a` 直接重現 review 的假落地情境，驗證預設會擋。
2. **F2**：`pen-land.sh` 改走 `cp → tmp → design-landing-check.sh → mv` 原子流程；gate 紅時刪 tmp、印「原始檔未動」。
3. **F3**：省略 `--expect-nodes` 時不再塞 backup 自身節點數給 gate（避免「與畫布一致」誤導訊息），改印明確警告。
4. **F4**：`qa.md`／`ui-designer.md`／`visual-reviewer.md` 統一改用 `$(git rev-parse --show-toplevel)/design/littlesprout.pen`（機械可求值，qa 自動解析到它 checkout test 的那份）。
5. **F5**：§7 Pen 路徑一致列補盲區——無互斥、TOCTOU，靠 lane WIP 序列化，機械版記入待辦池 LS-96。
6. **I1**：`docs/COLLABORATION.md:229`／`design-landing-check.sh:42,61` 的懸空「收工程序步驟 2/4」指涉改指向 `pen-land.sh --after`／`--expect-nodes`。
7. **I2**：backup 找不到訊息補回路徑編碼提示。
8. **I3**：`pen-land.test.sh` 補「落地檔壞 JSON」案例。
9. **I6**：`pen-open.sh` 檔頭補 pen CLI 0.3.4 輸出格式耦合說明。
10. **R2 追加（使用者核可）**：`pen-open.sh` 自動清場——殘留視窗時對它的 worktree 跑 `pen-land.sh --dry-run` 驗安全（沒有未落地變更）才 `osascript` 優雅退出（4 秒）→ 還在就 `kill -TERM`（不用 SIGKILL）→ ≤10 秒 → 重開＋重試；`--no-quit` 關掉。ui-designer 收工程序加最後一步：`pen-open.sh <主 checkout>`（`git worktree list --porcelain` 第一行機械取得）須 exit 0。

**已驗證**：
- 自測全綠：`pen-land.test.sh` 23 組（16→23，新增 F1/F2/F3/I2/I3 覆蓋）、`pen-open.test.sh` 16 組（12→16，新增 R2 自動清場四案例：殘留＋安全＋osascript 成功、殘留＋安全＋osascript 無反應 fall back kill -TERM、殘留＋有未落地變更不清場、`--no-quit` 不清場）、`agent-tools-check.test.sh` 23 組不變。
- 重跑全部 `scripts/gates/*.test.sh`（14 支）與 `scripts/ops/*.test.sh`（6 支）確認無退化；`tracked-ignored-check.sh`／`error-codes-check.sh`／`brand-skill-check.sh`／`agent-tools-check.sh` 對本 repo 實跑皆綠。
- push gate（xcodebuild test + SwiftLint，本票無 Swift 變更）通過。
- **實機復驗**（非真檔 save/cp）：`pen-open.sh --status` 開工前確認在 LS-91 worktree；push 前後皆再次確認 `pen-open.sh --status` 回報主 checkout 路徑（`/Users/clyeh/little-sprout/design/littlesprout.pen`），結束時已核對一致。R2 的自動清場邏輯本身透過 `pen-open.test.sh` 的 stub（`open`／`pen`／`pgrep`／`osascript`，`kill` 用真指令作用在測試自己起的背景 `sleep` 上）驗證，未對真正的 Pen app 觸發自動清場（沒有真實殘留場景需要驗）。

**未完成／剩餘**（reviewer informational 逐條處置）：
- **I4**：記入待辦池 LS-96，由 LS-86（`cleanup-merged.sh`）落地時把「移除設計 worktree 前切回主 checkout」接上機械檢查；目前仍是純規約。
- **I5**：不修——orchestrator 已將票文更新為 B 段（含本輪 R2）方案，無需我方再改。

**風險與已知問題**：
- 自動清場的「安全」判定只驗「backup 是否已落地到磁碟檔」，不是「Pen 內部是否真的沒有 dirty 狀態」（`get_app_state()` 無結構化 isDirty）——保守但非完備的代理指標。
- `osascript` 在本機／本 session 沙盒對 Pen 無效果（Automation 權限被擋），已 fall back `kill -TERM`（已實機驗證安全）；若目標環境兩者皆失效會 fail closed（exit 2），不會誤殺。
- Pen 全域單一實例、無互斥鎖，仍是 TOCTOU（F5，已記§7；機械版 pen-lock.sh 記入 LS-96）。
- qa 持有 `mcp__pencil__execute` 技術上可寫入 .pen，唯讀仍只是文字約束。

**產出位置**：
- PR：https://github.com/CLYEH/little-sprout/pull/152（`hotfix/LS-91-pen-path` → `main`，head SHA `464209bbda34e6da680f72a6ef990949e225a899`，remote 已核對一致；PR body 已補「R1 處置」段）
- Pen 路徑：結束時已核對＝`/Users/clyeh/little-sprout/design/littlesprout.pen`（主 checkout，`pen-open.sh --status` 確認）
- 本輪變更檔案：`scripts/ops/pen-land.sh`／`pen-land.test.sh`、`scripts/ops/pen-open.sh`／`pen-open.test.sh`、`scripts/gates/design-landing-check.sh`、`.claude/agents/{ui-designer,visual-reviewer,qa}.md`、`docs/COLLABORATION.md`

不等 CI；PR 已推送，CI 由 orchestrator 監看。

### 2026-08-25T13:18:52.526Z · Chih-Lin Yeh · `3cb6a524`

**merge-review R2：REQUEST_CHANGES**（PR #152，head `464209bb`；全文因自審 422 落在 PR comment：https://github.com/CLYEH/little-sprout/pull/152#issuecomment-5410956728 ）

**R1 五個 finding 全部確實修好**，且我逐項用突變測試確認自測鎖得住：F1 `--after`＋「結構無差異」預設拒絕（拿 R1 的假落地 fixture 重跑，不帶 `--after` 與帶 `--after` 兩條路都擋下）／F2 cp→tmp→gate→mv（gate 紅時原始檔 byte 不變、無殘留 tmp）／F3 省略 N 印警告且不再宣稱「與畫布一致」／F4 三份定義統一 `$(git rev-parse --show-toplevel)`／F5＋I1／I2／I3／I6 皆到位。自動清場的「有未落地變更絕不 quit」判定寫法正確（`land_rc -eq 1` 且訊息比對，是反向解讀；naive 的「exit 0 即安全」會把危險當安全——我拿它當突變，⑪a⑪b⑪c 三組紅）。突變 5 組全被抓紅。CI 四項全 pass。

**F1（major）自動清場只驗「目前 active 文件」，卻 kill 掉整個 Pen** — `pen-open.sh:186-214`：安全判定只對 `LAST_SEEN` 推回的 worktree 跑一次 `pen-land.sh --dry-run`，然後 `kill -TERM` 主行程＝**所有視窗一起結束**。而這條路徑的觸發條件就是「目標路徑已開在另一個背景視窗」——我們明知 `$want` 那個視窗開著，卻完全不驗它就殺掉。R1 檔頭原本的前提是「確認**全部視窗** isDirty:false 後 kill」，R2 窄化成「目前 active 文件」且未在檔頭／§2／§7 說明（§7 記的是信號性質，不是覆蓋範圍）。已用同款 stub 重現：目標 worktree 有未落地變更、active 是另一份「安全」文件 → 照樣清場成功。損害界定：磁碟上的 backup 不會消失，永久損失的是**任何視窗中最後一次 autosave 之後的記憶體變更**（含使用者本人手上的視窗）；且 ui-designer 收工程序第 5 步每輪都會跑 pen-open，這是常態路徑。連帶：清場會收掉另一個 agent 的 Pen session，重開後它的 execute 寫進新目標＝LS-81 事故同型再現（建議一併記進 §7）。**修法（我唯讀實機驗過資料來源可用）**：`pgrep -f 'Pen\.app/Contents/MacOS/Pen$'` 本機回單一 pid 18626（樣式正確）、`ps -Ao command | grep 'Pen Helper' | grep -oE 'file://[^"]*/design/littlesprout\.pen'` 列得出目前開著的文件 → 最小修法約 5 行（連目標 `$root` 也跑同一判定），完整修法是逐一驗全部視窗。功能方向我不反對（osascript 先禮後兵、不用 SIGKILL、`--no-quit` 逃生口都好），擋的只是覆蓋範圍。

**F2（minor）**：§7 與 handoff 說 F5／I4「記入待辦池 LS-96」，但 LS-96 目前只有 3 則 comment、沒有這兩則——與 R1 I1 同型的懸空引用，建議 orchestrator 補。

**informational 4**（記 LS-96）：I1 `pgrep` 空時已印「嘗試安全結束 Pen」卻整段跳過，訊息與行為不符；I2 `pen-land.sh:241` 暫存檔無 `trap`，異常中斷會在 `design/` 留未追蹤 tmp；I3 `--allow-unchanged` 放行時沒有顯著標記，handoff／review 看不出逃生口被用掉；I4 `pen-open.test.sh` 背景 `sleep` 被殺時的 job-control 訊息會進 CI log。另：R1 I5（票文與實作不符）**已修**，票文已改寫成 B 段＋保留原計畫追溯段，處置正確。

**Scope 乾淨**：R1→R2 delta 9 檔全落在「R1 findings 修正」或「使用者核可的自動清場追加」；base 前進帶進來的是別票 Swift 檔、零交集。

### 2026-08-25T13:38:59.570Z · Chih-Lin Yeh · `f772a9ca`

**Handoff（R3，回應 merge-reviewer R2）**

**Ticket**：LS-91

**已完成**（本輪 R3）：R1 的五個 finding 全數被 reviewer 用突變測試核銷確認（不是嘴上修）。R2 唯一擋下的是自動清場的安全判定覆蓋範圍：

1. **F1（major）**：`pen-open.sh` 的自動清場只對「目前 active 文件」跑一次 `pen-land.sh --dry-run`，接著 `kill -TERM` Pen 主行程＝**所有視窗一起結束**——目標路徑（觸發清場的前提就是它已開在另一個背景視窗）從未被驗過就被殺。改採完整修法：`ps -Ao command | grep 'Pen Helper' | grep -oE 'file://.../design/littlesprout\.pen'` 唯讀枚舉目前所有 renderer 行程命令列帶的檔案路徑，聯集上目前 active 路徑與目標路徑、去重，逐一對各自 worktree 根跑安全判定，**全部**安全才 quit；任一不安全就整體不清場、印「先 pen-land <root>」。
2. **I1**：`pgrep` 找不到主行程時，訊息與行為對齊（不再印「嘗試安全結束」卻整段跳過）。
3. **I2**：`pen-land.sh` 暫存檔加 `trap 'rm -f "$tmp"' EXIT`；`mv` 失敗時先 `trap - EXIT` 讓暫存檔真的留得住供人工檢查。
4. **I3**：`--allow-unchanged` 放行時印顯著標記「⚠ allow-unchanged：本輪零變更，刻意放行」。
5. **I4**：`pen-open.test.sh` 假 Pen 行程 `disown`，消除 CI log 的 job-control 噪音。

**已驗證**：
- 自測全綠：`pen-open.test.sh` 16→17 組（新增 `⑪e`：ps 枚舉揪出 get_app_state／want 都看不到的隱藏第三視窗不安全時，即使 active 與目標本身都安全仍整體不清場——直接對照 reviewer 描述的反例重現；並修正既有「不一致」案例配合 F1 完整修法後 `$want` 本身也在候選清單裡的行為）；`pen-land.test.sh` 23 組不變（既有斷言更新以涵蓋 I2/I3 新增的標記與行為）；`agent-tools-check.test.sh` 23 組不變。
- 測試環境對 `ps` 也做了 stub（否則會撈到開發機上真正在跑的 Pen 汙染候選清單——這是我在實作過程中第一次跑測試時就撞到的真實坑，已修正並記入測試檔頭）。
- 重跑全部 `scripts/gates/*.test.sh`（14 支）與 `scripts/ops/*.test.sh`（6 支）確認無退化；`tracked-ignored-check.sh`／`error-codes-check.sh`／`brand-skill-check.sh`／`agent-tools-check.sh` 對本 repo 實跑皆綠。
- push gate（xcodebuild test + SwiftLint，本票無 Swift 變更）通過；`git fetch && git merge --no-edit origin/main` 乾淨合併 LS-82／LS-17 的 Swift 檔（零重疊）。
- **實機唯讀複驗**：`pgrep -f 'Pen\.app/Contents/MacOS/Pen$'`／`ps -Ao command | grep 'Pen Helper' | grep -oE ...` 兩條查詢對真實行程表跑過（未觸發任何清場／quit／kill，只讀），確認格式在本機正確、抓得到目前開著的檔案路徑。未再對真正的 Pen app 觸發自動清場本體。

**未完成／剩餘**（reviewer informational 逐條處置）：
- **F2**：orchestrator 已表示會直接補 LS-96 的 F5／I4（R1）待辦池記錄，不需要我方再改文件措辭，本輪未動。
- **I5**（承 R1）：reviewer 已覆核 R1 I4／I5 的處置正確，本輪無新動作。

**風險與已知問題**：
- ps 枚舉靠 Electron renderer 行程**啟動時**的命令列參數反推目前開著哪些檔；若某個視窗能在不重啟 renderer 的情況下切換內部顯示的文件（本機觀察到的形狀是每個檔各自一個獨立 renderer，尚未見過這種切換），命令列會是舊的——屬未驗證的邊界情況，已記入 §7。
- 安全判定仍只驗「backup 是否已落地到磁碟檔」，不是「Pen 內部是否真的沒有 dirty 狀態」——保守但非完備的代理指標（R2 已記，本輪未變）。
- Pen 全域單一實例、無互斥鎖：即使安全判定現在涵蓋全部目前看得到的視窗，清場那一刻之後、重開之前，另一個 agent 仍可能開新視窗或被這次清場一併收掉 session（TOCTOU，LS-81 同型再現）；機械版 `pen-lock.sh` 記入待辦池 LS-96。

**產出位置**：
- PR：https://github.com/CLYEH/little-sprout/pull/152（`hotfix/LS-91-pen-path` → `main`，head SHA `610adf819877992e82698d3649bee604e179a11d`，remote 已核對一致；PR body 已補「R2 處置」段）
- Pen 路徑：結束時已核對＝`/Users/clyeh/little-sprout/design/littlesprout.pen`（主 checkout，`pen-open.sh --status` 確認）
- 本輪變更檔案：`scripts/ops/pen-land.sh`／`pen-land.test.sh`、`scripts/ops/pen-open.sh`／`pen-open.test.sh`、`docs/COLLABORATION.md`

不等 CI；PR 已推送，CI 由 orchestrator 監看。

### 2026-08-25T13:48:23.965Z · Chih-Lin Yeh · `c17586dd`

**merge-review R3：APPROVE**（PR #152，head `610adf81`；全文因自審 422 落在 PR comment：https://github.com/CLYEH/little-sprout/pull/152#issuecomment-5411339130 ）

**R2 F1（major）已修，且我用 R2 的同一個反例實跑驗證**：候選清單改成 `LAST_SEEN` ∪ `$want` ∪ `ps` 枚舉、去重、逐一 `check_root_safe`、全部安全才 quit；形狀不符／root 不存在／dry-run 無安全訊號一律視為不安全（fail closed）。反例重跑（目標 worktree 有未落地變更、active 是安全的別份、`ps` 故意撈不到額外視窗＝最保守情境）→ 印出兩份候選、點名目標那份不安全、`✗ 不自動 quit`、假行程仍活著、`open` 只被呼叫 1 次。實作三個關鍵細節都做對：逐一檢查用 here-doc 而非 `echo | while`（後者迴圈在 subshell、`all_safe=0` 會蒸發，整段把關靜默失效）、未知形狀與不存在的 root 都 fail closed、失敗訊息逐一列出並附各自的 pen-land 指令。突變 5 組：拿掉 ps 枚舉／「任一不安全即拒」改永遠安全／root 不存在改判安全／拿掉 LAST_SEEN 皆紅。

**R2 F2 已補**：LS-96 comment `1c6a4f67` 已寫入 F5（pen-lock.sh）與 I4（cleanup-merged 前對帳），§7 引用不再懸空。**I1–I4 全修**：pgrep 空時訊息與行為對齊／`trap 'rm -f "$tmp"' EXIT` 掛在 cp 之前且 mv 失敗分支先 `trap - EXIT`（兩個方向都想到）／`⚠ allow-unchanged` 標記（⑨b 已加斷言，突變拿掉即紅）／`disown` 後 job-control 噪音實測消失。

**N1（minor，不擋合併）自測沒鎖住「目標路徑在候選清單裡」**：我刪掉候選清單裡 `printf '%s\n' "$want"` 一行（diff 確認只少這行），**17 組自測全綠**——R2 那個 major 的核心性質目前沒有反饋守著。現有案例都抓不到的原因：⑪a／⑪b 幫目標備了安全 backup、⑪c 是 active 先不安全、⑪e 是第三視窗先不安全、不一致案例是 root 不存在先擋。建議補 ⑪f（照 ⑪e 形狀約 10 行）：active 安全＋ps 清空＋只有目標不安全 → 期望 exit 1、行程存活、`open_calls -eq 1`。本輪補會讓 head 前進、需依 LS-87 重貼 status；不補則建議記 LS-96。

**informational 2**：(a) `pen-open.sh:237` 迴圈裡 `root=` 覆寫外層目標 `root`（`want` 已算完、之後沒人再讀，目前無 bug，但日後在該區塊後用到 `$root` 會拿到錯的值，建議改名）；(b) I2 的 trap 兩分支無自測覆蓋（拿掉 trap 自測仍綠），難自測、我以讀 code 確認順序正確，只是別把「23 組全綠」讀成 I2 有機械保障。

**四維度**：race 的殘留（無互斥、清場後到重開之間的空窗、會收掉別的 agent 的 session、ps 靠 renderer 啟動時參數）已完整寫進 §7 盲區並記 LS-96，記錄誠實；效能／平行無 finding；**scope 乾淨**（`0c161c1` 只動兩支腳本＋兩支自測＋§7 一列，merge commit 只為保持可併，淨 diff 無 Swift）。CI 四項全 pass。

### 2026-08-25T13:52:26.206Z · Chih-Lin Yeh · `b6c77a5e`

## 落地與 retro（orchestrator）

**併入**：PR #152 R3 APPROVE（comment `c17586dd`）→ main `1d65e8a` → back-merge #153 → development。夾帶 LS-94 一併落地。worktree／分支清除。

**驗收**：自測 pen-open 17／pen-land 23／agent-tools 23 掛 CI；merge-reviewer 三輪突變測試（R1 9 組、R2 5 組、R3 4 組）皆紅；ios-dev 實機：`pen-open.sh` 對主 checkout exit 0、`open -a` 切檔＋殘留視窗清場（kill -TERM 路徑）實測；LS-67 開工時做第一次真實使用。

**Retro（lesson learning）**
- 機械化 SOP 時把「兩步缺一不可」的新鮮度檢查壓掉（R1 F1）——文字 SOP 轉腳本必須逐條對照原 SOP 的每一步，reviewer 的假落地 repro 是這次的救命繩。
- 「自動清場」第一版只驗 active 文件卻 kill 全部視窗（R2 F1）——任何「為了方便而終止程序」的自動化，安全判定的範圍必須等於副作用的範圍。
- 本票第一個 agent 失聯（97 分鐘 0 commit）→ 重派後 25 分鐘就有 PR；派工 prompt 加「20 分鐘內首筆 commit」有效，寫進派工規則記憶。
- 兩個真 bug 都是 reviewer 用突變／反例抓到，自測本身沒抓到（N1 仍是缺口，LS-96）——merge-reviewer 的突變測試是 harness 票的必要環節，維持 opus。
- Pen 盲區：`open -a` 對背景視窗不切換、osascript Automation 在沙箱被擋、TOCTOU 無互斥——皆記 §7／LS-96。

**Informational 處置**：R1 F5／I4 → LS-96（`1c6a4f67`）；R3 N1／(a)／(b) → LS-96（`bc320daa`）；其餘已修。→ Done。

