# LS-69 Harness：evidence-path-check 加 --base 模式讓 CI 對 PR diff 重跑（目前 CI 只跑自測）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Low |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-25T01:34:05.584Z |
| 開始 | 2026-09-01T14:08:44.876Z |
| 完成 | 2026-09-01T14:46:51.530Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-69/harnessevidence-path-check-加-base-模式讓-ci-對-pr-diff-重跑目前-ci-只跑自測 |
| 關係 | related→LS-65, related→LS-95, related→LS-96, related→LS-119, related→LS-85, related→LS-91, related→LS-61, related←LS-74, related←LS-68, related←LS-88 |

## 描述

## 來源

[LS-61](https://linear.app/little-sprout-app/issue/LS-61/harness審查取證目錄截圖掃描輸出定固定位置ignore-規則改-agent-存放指示) PR #94 review I10／N1–N3（2026-08-25）。

## 項目

1. `scripts/gates/evidence-path-check.sh` 加 `--base <ref>`：對 `base...HEAD` 的新增／改名路徑套同一規則；CI `rules` job 對 PR merge ref 跑（`--no-verify` 繞過 pre-commit 時 CI 兜底）。正負自測；§7 列狀態改 ✅ hook＋CI。
2. N1：非 git 目錄時先 `git rev-parse --git-dir` 分流，避免 ~100 行 usage 噴出（不可 `2>/dev/null`）。
3. N2：`.claude/agents/qa.md` 截圖路徑改寫絕對路徑（mobile-mcp `saveTo` 由 server 進程解析）。
4. N3：`scripts/gates/commit-gate.sh:2` 檔頭摘要補三支。

## 留言（4）

### 2026-09-01T14:20:05.974Z · Chih-Lin Yeh · `9bcdf1ea`

## Handoff — LS-69

**Ticket**：Harness：evidence-path-check 加 `--base` 模式讓 CI 對 PR diff 重跑＋三條順修 N1–N3。

**已完成**
1. `scripts/gates/evidence-path-check.sh` 新增 `--base <ref>` 模式：比對 `<ref>...HEAD`（三點語法）新增／改名路徑，套用與 `--cached`（staged）相同的判斷迴圈；不帶 `--base` 行為不變。`.github/workflows/ci.yml` `rules` job 新增一步（`if: github.event_name == 'pull_request'`）跑 `evidence-path-check.sh --base "origin/$BASE"`。`docs/COLLABORATION.md` §7 對應列狀態改 ✅ hook＋CI。
2. N1：非 git 目錄先 `git rev-parse --git-dir` 探測分流（stderr 不吞），避免後面 `git diff` 把路徑誤判成 `--no-index <path> <path>` 噴 ~100 行 usage。
3. N2：`.claude/agents/qa.md:20` `mobile_save_screenshot` 的 `saveTo` 由「worktree 相對」改為要求絕對路徑（server 進程解析、非呼叫端 cwd）。
4. N3：`scripts/gates/commit-gate.sh:2` 檔頭摘要補上 `tracked-ignored-check`／`evidence-path-check`／staged `.pen` 落地檢查三支。

**已驗證（怎麼驗）**
- `bash scripts/gates/evidence-path-check.test.sh`：28 組全綠（原 24 組 + --base 正負 2 組、--base 缺參數 1 組、無效 ref 1 組、N1 回歸控併入 ⑳）。
- 兩組 mutation 負控實測（改腳本→跑自測→還原，逐一驗證）：
  - 拔掉 N1 的 `--git-dir` 探測 → ⑳ 紅（噴出完整 usage，被抓到）。
  - 讓 `--base` 悄悄退回 `--cached`（模擬接線斷掉）→ ㉒／㉓ 紅（committed-not-staged 違規檔沒被抓到，被抓到）。
  - 兩次都確認還原後 28 組回綠。
- 本機重演 CI 新步驟的確切指令：`BASE=main bash -c 'bash scripts/gates/evidence-path-check.sh --base "origin/$BASE"'`（等同 ci.yml 新 step 的 shape）對本分支跑出 `✓ evidence-path gate（origin/main...HEAD）：無審查取證形狀`——證明 ref 語法／訊息真的接上而非空接線。
- 嘗試在本 repo 注入違規檔重演紅路徑：`git add` 後 `git commit` 被既有 pre-commit（`--cached` 模式）自己擋下（fail loud，commit 沒發生）——證實「pre-commit 未被繞過」的情境本來就擋得住；`--base` 模式要防的是「pre-commit 被繞過」那個 pre-commit 本身看不到的情境，這個情境已用不掛 hooks 的合成 repo（自測 ㉓）覆蓋並用 mutation 驗證過。
- `bash scripts/gates/brand-skill-check.test.sh`、`bash scripts/gates/agent-tools-check.sh` 等未觸及的 gate 仍綠（確認未動到其他 gate）。
- push gate 本機跑過：無 Swift 變更跳過 SwiftLint／unit tests（CI 仍跑）；secrets／branch-ticket／merge-conflict／migration 撞號／immutable 皆綠；`git push` 成功。

**未完成（informational 處置）**
- 本票四個項目（主體＋N1／N2／N3）皆已實作完成，無 reviewer informational 待處置（本票尚未經 merge-reviewer 審查）。
- 自己實作中注意到一項可能同型問題但**不修**（超出本票 N2 範圍——票文明列僅 `qa.md` 的 mobile-mcp `saveTo`）：`.claude/agents/ui-designer.md:14`、`visual-reviewer.md:10` 也寫「worktree 相對」描述 Pencil MCP 截圖／匯出路徑；Pencil MCP 是否有同樣的 server 進程 cwd 解析問題未經查證。建議 merge-reviewer 或後續票查證後視需要另開票（不預先開票，因未確認是否真的同型）。

**風險**：無已知風險。`--base` 模式與既有 `--cached` 模式共用同一判斷迴圈，只是 diff 來源不同，行為變更面小；CI 新步驟只在 `pull_request` 事件跑，不影響 push 事件與既有四個 required checks 的通過邏輯。

**產出位置**
- Worktree：`/Users/clyeh/little-sprout/.claude/worktrees/LS-69`（branch `hotfix/LS-69-evidence-ci`）
- PR：https://github.com/CLYEH/little-sprout/pull/204（hotfix/LS-69-evidence-ci → main）
- Commits：`ded4682`（主體＋N1）、`b3997ed`（N2）、`8b08915`（N3）
- 變更檔案：`scripts/gates/evidence-path-check.sh`、`scripts/gates/evidence-path-check.test.sh`、`.github/workflows/ci.yml`、`docs/COLLABORATION.md`、`.claude/agents/qa.md`、`scripts/gates/commit-gate.sh`
- 模擬器已關：無（本次任務未 boot 任何模擬器；`demo-iPhone17Pro` 為既有 demo-* 持久機，未動）

### 2026-09-01T14:33:48.443Z · Chih-Lin Yeh · `84245669`

## Merge review R1 — LS-69（PR #204，head `8b08915`，base `main`）

**VERDICT：APPROVE**（無 blocker／major；6 條 informational／minor，處置見下）

審的範圍：`git diff cda6d42...8b08915`（6 檔／+113 −14）。Race condition／運算效能／平行優化三個維度對本票（純 shell gate＋CI 接線＋文件）不適用，已跳過不硬湊；主要打「gate 假陰性」與 scope。

---

### 自證（我實跑的，不是複述 handoff）

**1. CI 接線自證（LS-95 B2 教訓）**
- 本 PR 自己的 rules job（run 33518762792 / job 99892375130）step 7「審查取證不進版控（PR diff 對帳…）」存在且 `conclusion=success`；log 實際輸出 `✓ evidence-path gate（origin/main...HEAD）：無審查取證形狀`——**mode-specific 字樣**，不是空接線也不是跑成 staged 模式。
- checkout 是 `refs/remotes/pull/204/merge`（`HEAD is now at 26ff47a Merge 8b08915 into cda6d42`）＋`fetch-depth: 0`，所以 `origin/main` 存在、三點語法的 merge-base 正好是 base tip → diff 等於 PR 變更。fork PR 同樣走 base repo 的 checkout，且既有 step（`git merge-base "origin/$BASE"`）早就依賴同一條件。
- step 沒有 `|| true`，`run:` 的 shell 是 `/usr/bin/bash -e`（log 可見）→ 非 0 會讓 job 紅。`if: github.event_name == 'pull_request'` 是必要的（push 事件 `github.base_ref` 為空，`origin/` 會 fatal）；step-level skip 不影響 job conclusion，不重蹈 LS-85 的整 job skipped 問題。
- 四個 check 現在全綠（ci／db／lint／rules）。

**2. `--base` 假陰性攻擊（合成 repo 實跑）**
| 情境 | 結果 |
|---|---|
| rename 進違規目錄（`design/home.png` → `ls99r1/home.png`） | 紅、點名目的地。`--name-only` 對 R 只輸出目的地、`--diff-filter=d`（小寫＝只排除刪除）保留 R——沒有 rename 盲區 |
| 路徑含空白（`ls99 r2 review/my shot.png`） | 紅（`-z` 路徑來源與 `--cached` 同構） |
| symlink（`qa notes/link-review`） | 不擋——但那是**檔名層**，與既有設計一致（`visual-reviewer.md` 要放行），非退化 |
| 範圍內含 merge commit | 紅、兩個違規檔都點名（tree-vs-tree，無 merge diff 詭異行為） |
| 刪除後原樣加回（淨零變更） | 綠——符合設計（最終樹沒有該檔） |
| tag／含斜線的 ref 當 base | 正常運作 |
| 無效 ref | exit 2，且 git 原生 fatal **不吞**（fail loud），gate 再補一行 fail closed |
| `--base` 缺參數／`--base=ref` 等號式 | 皆 exit 2 fail closed |

兩模式路徑來源皆為 `git diff --name-only -z --diff-filter=d`，格式完全同構，沒有只在單邊被正規化的路徑形狀。

**3. N1 fail-closed**：非 git 目錄（帶路徑參數）→ exit 2、一行訊息、無 usage 噴版；`git rev-parse --git-dir` 的 stderr 完整帶進訊息、沒有 `2>/dev/null`（讀碼＋實跑雙確認）。

**4. mutation 覆核（複製到 scratchpad 改，repo 未動；每次還原後回綠）**
- M1 拔掉 N1 `--git-dir` 探測 → ⑳ 紅（usage 噴版被抓）✅ 覆現
- M2 `--base` 悄悄退回 `--cached` → ㉒／㉓ 紅 ✅ 覆現
- **M6（我自選）** `--base` 分支拿掉 `-z` → ㉓ 紅（`read -d ''` 讀不到 NUL、整個判斷迴圈空轉的假綠當場被抓）✅ 殺死
- M3／M4／M5／M7 存活 → 見 finding 2、3（M5＝三點改兩點，在 CI 的 merge-ref 用法下語意等價，無害）

**5. 穩定性**：`evidence-path-check.test.sh` 連跑 4 次，28/28 全綠、無 flake（含 mktemp 合成 repo、`GIT_CONFIG_GLOBAL=/dev/null` 隔離）。

**6. scope**：6 檔全部落在票內四項，無夾帶、無順手重構。`commit-gate.sh` 檔頭新列的 7 項與腳本實際執行順序逐項對得上（保護分支／branch 命名／secrets／tracked-ignored／evidence-path／staged `.pen` 落地／Swift lint）；COLLABORATION §7 只動該一列且「✅ hook＋CI」屬實；`qa.md` 只動 N2 那一句。`--cached` 模式的成功訊息措辭有變（`（staged）：無審查取證形狀`），全 repo 沒有任何地方比對舊字串，無連帶破壞。

---

### Findings

**F1（minor，不擋合併）— `scripts/gates/evidence-path-check.sh:27-35` 參數順序寫反會靜默假綠**
`bash scripts/gates/evidence-path-check.sh <repo> --base <ref>`（旗標放後面）→ `--base` 被當多餘參數默默忽略，跑成 staged 模式並輸出 `✓ evidence-path gate（staged）：無審查取證形狀`、exit 0（實測）。失敗情境：日後有人把這行複製到別的 workflow／腳本時把參數順序寫反，gate 變成永遠綠的 no-op，而這正是本票要防的假陰性類型。建議：參數解析完後對殘留的未知參數 fail closed（或讓旗標可出現在任意位置）。現況不擋：CI 與自測都用文件記載的順序。

**F2（minor）— `--base` 模式的自測只有兩個功能案例，rename 未覆蓋**
把 `--base` 分支的 `--diff-filter=d` 改成 `A`（丟掉 rename 目的地）→ 28 組**全綠**（mutant M3 存活）。shipped code 用 `d`、我實測 rename 在 `--base` 模式確實會紅，所以這是測試覆蓋缺口而非行為缺陷。建議補一個「`--base` 模式 rename 到違規目錄」案例（成本 3 行）。

**F3（informational，pre-existing、非本 PR 引入）— 共用規則迴圈的巢狀樣式有存活 mutant**
刪掉 `*/ls[0-9]*/*` 或刪掉 `*/review*/*` 兩個 mutant 都 28 組全綠（M4／M7）——巢狀目錄只有 `*-review*` 那一支被 ⑤ 覆蓋。即 `docs/ls46r1/x.html`、`docs/reviewshots/x.html` 兩型退化偵測不到（首層 `ls<n>` 有 ⑱ 守住）。測試檔頭第 3 行宣稱「若檢查退化成只看第一層目錄…這裡會紅」對這兩型不成立。該迴圈本 PR 未動（LS-61 範圍）。→ 建議記入 LS-96。

**F4（informational）— `--base` 模式的固有盲區沒寫進 §7 盲區清單**
`base...HEAD` 只看差異：**base 上已存在的違規檔永遠不會被點名**。repo 現況就有一個實例：`design-canvas-d/mengya-crayon-alpha.png`（main／test／development 三支皆有的、白名單外 tracked png）。不是本 PR 造成、也不是新的假陽性（pre-commit `--cached` 對「修改它」本來就會紅），但 §7 那列讀起來容易讓人以為 CI 兜底＝全樹掃描。→ 建議 LS-96 補一行盲區文字（或把 `design-canvas-d/` 納入白名單裁決）。

**F5（informational）— 無參數且 cwd 非 git repo 時，N1 的「一行訊息」不適用**
`repo="${1:-$(git rev-parse --show-toplevel)}"` 在 N1 探測**之前**就炸：exit 128＋git 原生 fatal（實測），不是 gate 的 exit 2＋一行訊息。仍是 fail closed（非 0），只影響訊息一致性。

**F6（informational，PLAUSIBLE）— `GIT_DIR` 已匯出時 N1 探測會被環境變數滿足**
若在 `GIT_DIR` 已設定的情況下把非 git 路徑當參數傳，`git rev-parse --git-dir` 會成功、探測放行，接著對洩漏的那個 repo 跑 diff 並印綠（實測）。目前無呼叫端會踩到（hook／CI 都不帶路徑參數、自測獨立跑），且 `--cached` 版本本來就有同樣性質。不擋。

---

### 對 handoff「不修＋標記」的裁決（ui-designer.md:14／visual-reviewer.md:10）

**裁決：接受「不修」，但處置方式要改成「記入 LS-96」。**
- scope 上：票文 N2 只點名 `qa.md` 的 mobile-mcp `saveTo`，動另外兩支 agent 定義屬超範圍，不修符合手術式修改原則——這點實作者判斷正確。
- 我快速查證的結果：**同型問題有相當可能存在，但不跑 Pencil 無法確證**。兩條佐證：(a) `ui-designer.md:15` 對 .pen 路徑已經被迫改成 `$(git rev-parse --show-toplevel)/design/littlesprout.pen` 絕對路徑（LS-91 就踩過同一支 MCP 的路徑語意）；(b) 現場有吻合症狀——主 checkout 存在**空的** `.claude/evidence/LS-119/`（今天 12:42 建立），而 LS-119 的 r3-review PNG 全在 worktree 的 `.claude/evidence/LS-119/r3-review/`：同一票的取證路徑在兩個 root 各出現一次。足以立案、不足以判定，故標 PLAUSIBLE、不列 finding 等級。
- 但 handoff 寫的是「不預先開票、建議後續查證」——依 COLLABORATION §5-b，informational 的預設處置是**記入待辦池 LS-96**（另票只限 High 以上）。請 orchestrator 補一行進 LS-96：「查證 Pencil MCP 截圖／匯出的相對路徑解析 root；若與 mobile-mcp 同型，`ui-designer.md:14`／`visual-reviewer.md:10` 一併改絕對路徑」。

### 建議處置彙整
- F1／F2：可本票修（各 3–5 行），也可記 LS-96；我不因此擋合併。
- F3／F4／F5／F6＋Pencil 查證：記入 LS-96。

已跑但未涵蓋的範圍（誠實揭露）：沒有實跑 push 事件下的 rules job（本 PR 尚未合併，無此 SHA 的 push run）——step-level skip 不影響 job conclusion 是依 GitHub Actions 語意與 log 佐證推斷，非實測。DB／Swift 相關維度本票無變更，未審。

### 2026-09-01T14:36:01.070Z · Chih-Lin Yeh · `c169f00d`

## Orchestrator retro（收尾儀式；純 harness 票，dead-code-sweeper 免跑）

**過程**：單輪實作（--base＋CI 兜底＋N1–N3，帶著 LS-95「CI 自證」與 LS-65「回退 mutation」兩教訓進場）→ merge-review 一輪 APPROVE（2 minor＋4 informational 全records，無阻擋）→ 併 main `2525522`、back-merge PR #205。

**Lesson learning**：
1. **教訓前饋見效**：派工 prompt 帶入前兩票教訓後，實作者主動做了 CI 命令本機重演＋兩條 mutation 負控，reviewer 首輪即 APPROVE——「審查發現→派工規則化」的迴圈第一次完整閉環，單輪過審省一整輪成本。
2. **Reviewer 的存活 mutant 清單**（4 個）成為精確的池項——mutation 定界法連兩票證明價值，正式納入 gate 類票審查慣例。
3. 實作者自揭 scope 外同型問題（Pencil 路徑）→ reviewer 補旁證後轉池：「自揭＋查證＋池」的處置鏈比「沉默」或「順手擴修」都好，值得成為慣例敘述。

informational 處置：F1–F6＋Pencil 路徑疑慮記 LS-96（comment `6b367b37`）。

### 2026-09-01T14:46:54.655Z · Chih-Lin Yeh · `8e0d94eb`

結案：PR #204 併 main `2525522`（merge-review=success on `8b08915`，linear:84245669）；back-merge PR #205 併 development `3c6fdbe`（§2 驗證：merge-tree 無內容差、status 貼 `2525522` id 53305123303；CI 全綠）。收尾儀式完成——純 harness 免 sweeper、retro（comment `c169f00d`）、informational 記 LS-96（comment `6b367b37`）、清理：`cleanup-merged.sh --apply LS-69`。

