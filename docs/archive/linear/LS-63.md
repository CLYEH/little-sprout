# LS-63 Harness：scratchpad 暫存檔名必帶票號（平行 agent 撞檔導致 PR body 誤貼）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | — |
| Cycle | — |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-24T20:53:19.902Z |
| 開始 | 2026-08-24T23:09:22.319Z |
| 完成 | 2026-08-25T01:42:38.699Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-63/harnessscratchpad-暫存檔名必帶票號平行-agent-撞檔導致-pr-body-誤貼 |
| 關係 | related→LS-61, related→LS-6, related→LS-54, related→LS-45, related→LS-51, related→LS-37, related→LS-50, related→LS-56, related→LS-53, related←LS-140, related←LS-88 |
| 附件 | [PR #93 hotfix/LS-63-scratchpad-ticket-naming → main](https://github.com/CLYEH/little-sprout/pull/93) |

## 描述

## 事故

2026-08-24：[LS-53](https://linear.app/little-sprout-app/issue/LS-53/harness破壞性偵測器盲區-alter-policy-false-與-revoke-未納入關鍵字語意判定) 與 [LS-56](https://linear.app/little-sprout-app/issue/LS-56/harnesserror-codes-check-抽取規則硬化註解掉的-case表格首欄格式artifact-retention) 兩個 agent 同時用 `scratchpad/pr-body.md` 暫存 PR body；[LS-56](https://linear.app/little-sprout-app/issue/LS-56/harnesserror-codes-check-抽取規則硬化註解掉的-case表格首欄格式artifact-retention) 覆寫後，[LS-53](https://linear.app/little-sprout-app/issue/LS-53/harness破壞性偵測器盲區-alter-policy-false-與-revoke-未納入關鍵字語意判定) 的 `gh pr edit 78` 把 [LS-56](https://linear.app/little-sprout-app/issue/LS-56/harnesserror-codes-check-抽取規則硬化註解掉的-case表格首欄格式artifact-retention) 的 body 貼到 PR #78 數分鐘（已還原）。若當時含核可標記或 `BREAKING:` 段落字樣，會誤觸 gate。

## 修法

1. `.claude/agents/{ios-dev,general-purpose 派工模板}` 與 COLLABORATION §3 加規則：scratchpad 暫存檔一律 `<LS-n>-<用途>.<ext>`，或用 `mktemp -d` 子目錄。
2. Gate（可選）：`gh pr create/edit` 前檢查 body 首段含本票號（`LS-<n>`），否則擋——可做成 `scripts/gates/pr-body-check.sh` 給 agent 呼叫；CI `rules` job 亦可驗 PR body 含分支名中的票號（正負對照）。

## 驗收

* CI：PR body 不含 head 分支的 `LS-<n>` → 紅；含 → 綠。
* COLLABORATION §3 規則＋§7 列。

## 留言（5）

### 2026-08-25T00:49:10.584Z · Chih-Lin Yeh · `65d786d3`

## Handoff（LS-63，PR #93 → main）

**Ticket**：LS-63

**對 LS-50 的判定**：`branch-ticket-check.sh --pr-body` 只驗「commit body 宣告的 `Bundles:` 在 PR body 也有獨佔一行」，從未斷言 PR body 含分支自己的 `LS-<n>` → 票號半邊確實缺，本票補這半；`Bundles:` 半邊不動。

**已完成**
- `scripts/gates/pr-body-check.sh`（新）：`[--branch <name>] <body-file>`，票號取自工作分支名；驗「檔頭段」含 `LS-<n>`（檔頭段＝到第一個內容段落結束，開頭空白行與 `#` 標題行一併納入——模板「## Ticket／空行／LS-n」、「Ticket: LS-n」（PR #90）、「## Ticket／LS-n」（PR #78）三種實際形狀皆同一段，不誤擋模板）。整字比對（`LS-630` 不滿足 `LS-63`）、CRLF 認得；紅時印檔頭段並點名他票「疑似貼錯 body」；非工作分支／缺檔 exit 2 fail closed。純 bash 3.2。
- `scripts/gates/pr-body-check.test.sh`（新，21 組）：正向 7／負向 7／參數錯 4／臨時 repo 推票號＋detached 3；失敗分支用 `${name}`。
- `ci.yml` rules：新 step「PR body 檔頭段含本票票號（pr-body-check，LS-63）」限 feature|fix|hotfix head（同 LS-50 step），以 `$HEAD` 驗 `github.event.pull_request.body`；自測 step 加 `pr-body-check.test.sh || rc=1`。
- COLLABORATION §3：PR message 加「檔頭段必須含本票＋`gh pr create/edit` 前先跑 gate」；新增「暫存檔（scratchpad）」段（`LS-<n>-<用途>.<ext>` 或 `mktemp -d`，禁無票號通名）。§7 新增一列（✅ CI；檔名⚠️規約）。
- `.claude/agents/ios-dev.md` 硬規則加一條。其他 agent 定義都不開 PR，未動。

**已驗證**
- `/bin/bash scripts/gates/pr-body-check.test.sh`（bash 3.2）21/21 綠。
- 自測有牙（Rule 8）：gate 改成全文 grep → 「事故原形」「票號只在第二段」兩組紅；拿掉右側整字邊界 → 「LS-630」組紅；改完已還原（diff 為空）。
- CI 自測 step 8 支 `.test.sh` 本機依序全綠；`ruby -ryaml` 解析 ci.yml OK（rules 11 steps，新 step 緊接 LS-50 step）；`bash -n` 兩支 OK。
- 本 PR body 先過 `pr-body-check.sh LS-63-pr-body.md` ✓；反向用 LS-56 檔頭對本分支 → 紅並點名 LS-56 ✓。
- commit gate／commit-msg gate／push gate 皆過（`0b7cc2c`）。CI 結果由 orchestrator 監看。

**未完成／剩餘**
- 票文「general-purpose 派工模板」不是 repo 內檔案（orchestrator 的派工 prompt）。本 PR 未動 CLAUDE.md（不在指定範圍）；規則落在 §3。**建議 orchestrator 派工模板固定帶一句**：「暫存檔一律 `LS-<n>-<用途>.<ext>`；`gh pr create/edit` 前先 `bash scripts/gates/pr-body-check.sh <body-file>`」——或核可後補一行到 CLAUDE.md「每個 agent 都要遵守」。
- 合併後 back-merge test／development（harness 檔慣例）。

**風險與已知問題**
- push-gate 不掛此檢查（push 時 PR body 尚不存在）；本機半邊靠 agent 規約呼叫，CI 是強制層。
- 盲區（已寫進 §7）：檔頭段之後貼錯內容看不出來（靠 merge-reviewer scope）；暫存檔名本身在 repo 外無機械 gate。
- 無 migration、無 Swift 變更。

**產出位置**：branch `hotfix/LS-63-scratchpad-ticket-naming`（`0b7cc2c`）；PR https://github.com/CLYEH/little-sprout/pull/93

### 2026-08-25T00:58:01.752Z · Chih-Lin Yeh · `093d9827`

## merge-reviewer R1（PR #93 `hotfix/LS-63-scratchpad-ticket-naming` → main）

**ITERATE**（1 應修；無 blocker）。gate 邏輯本身正確、有牙、注入安全，親跑全綠；卡的是「紅了之後轉不回綠」這條路徑沒交代。

### 親跑證據

- `/bin/bash pr-body-check.test.sh`（GNU bash 3.2.57 arm64）→ **21/21 綠**，組數與 §7 宣稱一致（① 7／② 7／③ 4／④ 3）。
- 自建對抗樣本（不用票內附的 test 檔）：(a) 模板 `## Ticket`／空行／`LS-63` → 綠；(b) `Ticket: LS-63` 單行 → 綠；(c) 票號只在第二段 → **紅**；(d) `LS-630`／`LS-6`（對 LS-63 分支）／`LS-63`（對 LS-6 分支）／`ls-63` → **全紅**；(e) CRLF＋前導空行 → 綠；(f) 見 I3；(g) 事故原形（LS-56 檔頭貼到 `hotfix/LS-53-breaking-migration-detector`）→ **紅並點名 LS-56**、輸出「疑似貼錯 body：暫存檔被平行 agent 覆寫？」。整字邊界兩側都真的成立。
- **歷史回歸（最強的誤擋測試）**：抓全部 42 個工作分支 PR 的真實 body 逐一過 gate → 30 綠／12 紅。12 紅逐一看過檔頭，全部是 body 真的沒把票號寫進檔頭段（`## Summary`／`## 摘要` 開頭），**不是 gate 判錯**。→ 見 F1。
- **注入安全（親看 yaml）**：`ci.yml:26` 用 job-level `env: PR_BODY: ${{ … }}`，`ci.yml:100-101` 只用 `$PR_BODY`／`$HEAD` shell 變數＋`printf '%s'`；全檔 `${{ }}` 只出現在 L14/24/25/26，**沒有任何 `run:` 內插**。實測 body 含 `` `touch /tmp/PWNED_BQ` `` 與 `$(touch /tmp/PWNED_CS)` → 兩個檔案都沒被建立，gate 正常回綠。
- **空／null body**：`printf '%s' ""` → gate exit 1「PR body 是空的」＝ **fail closed**。
- **step 條件與 LS-50 一致**：`ci.yml:98` 與 `ci.yml:88` 逐字同一條 `startsWith(feature/|fix/|hotfix/)`；promote／back-merge／`revert-*` 一律跳過，腳本自身對保護分支另有 exit 2 雙保險。
- **與 LS-50 分工**：`branch-ticket-check.sh:57,99-109` 只在 commit body 宣告夾帶時驗 PR body 有獨佔一行 `Bundles:`，**從未斷言自身票號** → 不重複、不衝突，判定成立。
- `git merge-tree --write-tree origin/main origin/hotfix/LS-63-…` → rc=0、單一 tree oid，**無衝突**。PR #93 CI 四項全綠，`rules` job 12 steps 全 success，含新 step（gate 對自己的 body 實彈跑過）。
- 自測成本：pr-body-check.test.sh 0.24s（現有 7 支合計 5.7s），可忽略。

### F1（應修）`scripts/gates/pr-body-check.sh:70-76`＋`docs/COLLABORATION.md:72,145`：紅了以後，改 body 無法讓 required check 轉綠，而錯誤訊息與新規約都沒說

- `rules` 是 main／development 的 **required status check**（`gh api …/protection` 實查：`["ci","lint","rules","db"]`）→ 這個 gate 紅＝硬擋合併。
- `ci.yml:7-9` 的 `on: pull_request` 沒有 `types:`，預設 `opened, synchronize, reopened`，**不含 `edited`**。複現：agent 用 `## Summary` 開頭的 body 開 PR → rules 紅 → `gh pr edit --body-file` 修好 → **check 仍紅、不會重跑**；`gh run rerun` 也沒用（re-run 重放原始 event payload，body 還是舊的）→ 只能 close/reopen 或再推一個 commit。
- 命中率不是理論值：歷史 42 個工作分支 PR 有 12 個（29%）檔頭段沒本票票號，且**最近 8 個 feature PR 裡有 5 個**（#60／#63／#70／#77／#85）都是 `## Summary` 開頭 —— 下一張 feature 票很可能第一次就踩。
- 這個坑 repo 早被咬過並寫進 `docs/COLLABORATION.md:123`（「加標記後 CI 不會自動重跑（`pull_request` 觸發不含 `edited`），需 close/reopen 或再 push（LS-37 實測）」），但那句掛在 DESTRUCTIVE-APPROVED 段；本票新增的 §3（L72）與 §7 新列（L145）都沒把它帶過來，而本 gate 是**全票通用**、觸發面比 migration 票大得多。
- 建議（二選一或都做，皆 1–2 行）：(a) 最高槓桿——在失敗輸出（L73／L75 之後）加一句「改完 body 後 CI 不會自動重跑：close/reopen PR 或再 push 一個 commit」；(b) §7 L145 盲區欄／§3 L72 補同一句並指向 L123。

### I1（informational）`scripts/gates/pr-body-check.sh:73`：檔頭段合理提及他票時，訊息會誤指「疑似貼錯 body」

真實案例 PR #85 檔頭段是 `BREAKING: …（取代 LS-52 的 hybrid 模式）`；我的樣本 (c) 亦然 → 輸出「檔頭段提到的是 LS-50——疑似貼錯 body：暫存檔被平行 agent 覆寫？」。措辭是問句、不影響判定，但會把「忘了寫本票票號」誤導成「撞檔事故」。可考慮改成「若這不是你的票，疑似…；否則請補上 ${ticket}」。**處置：orchestrator 裁量，可不動。**

### I2（informational）`docs/COLLABORATION.md:145`：新列的 `feature|fix|hotfix` 未跳脫，GFM 會把狀態欄 ✅ 擠掉

§7 表頭是 3 欄（`|---|---|---|`），新列含 2 個未跳脫 `|` → 切成 5 cell，GFM 丟棄超出表頭的 cell，**`✅ CI（agent 本機同腳本；檔名⚠️規約）` 這格渲染時不見**。舊列 149／150／153 有同樣問題（既有債），但最近兩列（LS-50 L143／LS-51 L144）都避開了。修法一字：`feature\|fix\|hotfix`（反引號在 GFM 表格內不保護 `|`）。**處置：本 PR 順手改新列即可，舊三列不要動（非本票範圍）。**

### I3（informational）`scripts/gates/pr-body-check.sh:36-57`：HTML 註解也算檔頭段內容

實測 (f1) body＝`<!-- LS-63 -->`＋空行＋別票內容 → **綠**（票號藏在 rendered body 看不見的地方也滿足 gate）；反向 (f2) body 以 `<!-- 請填寫下列欄位 -->` 開頭、票號在下一段 → **紅**（本 repo 模板不是這形狀，目前無誤擋）。這與 LS-45 已記錄的 DESTRUCTIVE-APPROVED 盲區同型（§5 L123：「藏在 HTML 註解…仍會放行」）。**處置：建議在 §7 L145 盲區欄補半句，不必改腳本。**

### I4（informational）規則放的位置打不到肇事者——handoff 那條建議我同意

事故是 orchestrator 派的 general-purpose agent 造成的，不是 ios-dev。head 分支上 grep 過六個 agent 定義：只有 `.claude/agents/ios-dev.md:13` 提到開 PR／`--body-file`，其餘五個（qa／ui-designer／merge-reviewer／visual-reviewer／dead-code-sweeper）確實不開 PR ——「未動其他 agent」的 scope 判定成立。但 repo 內唯一**所有 agent（含 general-purpose）都會自動載入**的面是 `CLAUDE.md`「每個 agent 都要遵守」。CI 兜底涵蓋所有 PR（不管誰開），所以這不是洞、不擋本 PR；只是前饋沒打到人。**處置：orchestrator 核可後補一行到 CLAUDE.md「每個 agent 都要遵守」（暫存檔一律 `LS-<n>-<用途>.<ext>`；`gh pr create/edit` 前先跑 `pr-body-check.sh`），並同步進派工模板。本 PR 不必動 CLAUDE.md。**

### Scope

乾淨。5 檔／+187−6，每一行都追得回票文：新腳本＋自測、ci.yml 一個新 step＋自測清單加一支（旁邊三行註解改動只是把 `／LS-63` 與新子句插進既有列舉，屬必要）、§3 兩段＋§7 一列、ios-dev.md 只加 1 行 0 刪。無順手重構、無無關改動。無 Swift／無 migration → race condition 與 DB 效能兩個維度本 PR 無可審之處；`$RUNNER_TEMP/pr-body-check.txt` 與 LS-50 的 `pr-body.txt` 刻意不同名（同一 job 內不互撞，且切題）。CI 自測 8 支串行是 LS-54 N5 的既定設計（各收 rc 才看得到全部紅點），不建議改平行。

**未審範圍**：`ci`／`db`／`lint` 三個 job 本 PR 未動，未細看。

**Verdict：ITERATE** —— 修掉 F1（1–2 行）即可 APPROVE；I2 建議一併帶上，I1／I3／I4 由 orchestrator 裁量。

### 2026-08-25T01:05:38.687Z · Chih-Lin Yeh · `7b35b407`

## R1 處置（PR #93，commit `6a87803`；回 merge-reviewer R1 `093d9827`）

- **F1（應修）→ 已修**：`pr-body-check.sh` 兩條失敗路徑（空 body／票號不在檔頭段）都印「修好 body 後 CI 不會自動重跑（`pull_request` 觸發不含 `edited`；`gh run rerun` 也只重放舊 body）——請 close/reopen PR 或再 push 一個 commit（LS-37 實測，COLLABORATION §6）」；檔頭註解同記。§3 PR message 段與 §7 新列同句並指向 §6；§6 L123 LS-37 那句反向加「§3 的 PR body 票號檢查 LS-63 同坑」。自測新增第 22 組 pin 住 `close/reopen` 字樣（丟掉這句就紅）。
- **I2 → 已改**：§7 新列 `feature\|fix\|hotfix` 跳脫；`sed 's/\\|//g' | awk -F'|'` 數得 4 個未跳脫 pipe＝3 欄，狀態欄 ✅ 回來了。舊三列未動。
- **I1 → 已改**：他票訊息改「檔頭段含其他票號（LS-xx）：若為刻意引用可忽略此句、只需補上 LS-63；若非，請確認 body 檔是否被平行 agent 覆寫」。判定不變。
- **I3 → §7 盲區補句**：「逐行比對不做跨行狀態機——HTML 註解 `<!-- LS-<n> -->` 內的票號也算檔頭段內容，rendered body 看不見仍放行（與 LS-45 同型，審查看 raw body；反向：以 HTML 註解開頭的 body 會把註解當內容段落，本 repo 模板不是這形狀）」。不改腳本。
- **I4 → 留給 orchestrator**：CLAUDE.md「每個 agent 都要遵守」一行＋派工模板同步，本 PR 不動。

**驗證**：`/bin/bash pr-body-check.test.sh` 22/22 綠；`bash -n` 兩支 OK；`ruby -ryaml` ci.yml OK；`pr-body-check.sh` 對 LS-56 檔頭 → 紅並同時印 I1 新措辭＋F1 止血句；PR body 更新前先過 gate ✓，`gh pr edit 93` 完成（首行 `Ticket: LS-63`、末段「R1 修正」）。commit／push gate 皆過。

**diff --stat（R1）**：`docs/COLLABORATION.md | 6 +++---`、`scripts/gates/pr-body-check.sh | 8 +++++++-`、`scripts/gates/pr-body-check.test.sh | 5 +++--`（3 files, +13 −6）。

### 2026-08-25T01:08:16.567Z · Chih-Lin Yeh · `a8ef9f4e`

## Orchestrator 核銷 R1 修補（`6a87803`＋`4cb75a3`）
- F1：`pr-body-check.sh:61` 失敗輸出含「close/reopen 或再 push」止血指示；檔頭 :20-21 與 §7 列同句並引用 LS-37 同坑 ✓。
- I2：§7 新列 `feature\|fix\|hotfix` 已跳脫 ✓；I1 措辭改「若為刻意引用可忽略」✓；I3 HTML 註解盲區已寫入 §7 ✓。
- I4：採納，`CLAUDE.md`「每個 agent 都要遵守」加一條（`4cb75a3`，+1 行）。
diff 合計 4 檔 +14/−6，皆文件／訊息層，reviewer R1 已驗 gate 邏輯與注入安全，不再開 R2。CI 綠即併入 main；與 #94（LS-61）同批 back-merge 後 Done。

### 2026-08-25T01:14:32.829Z · Chih-Lin Yeh · `526c47d3`

PR #93 已併入 main（`4fdd099`，CI 四 job 綠）。back-merge development／test 與 #94（LS-61）同批；落地即 Done。

