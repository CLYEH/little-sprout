# LS-211 Harness：handoff 可驗證第二波——qa／reviewer handoff 逐項對應派工單＋引用測試名須存在（grep gate）、`pr-body-check` 本機無 key 對池項候選行印 ⚠＋rerun 模板先看失敗 step、`privacy_manifest_check.py` 驗枚舉值合法性、LS-209 informational I-a／I-b／I-c 順修、24 個未用 import 清理（逐檔 build 驗）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:M, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-06T01:10:14.845Z |
| 開始 | 2026-09-06T01:10:59.146Z |
| 完成 | 2026-09-06T07:24:33.665Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-211/harnesshandoff-可驗證第二波-qareviewer-handoff-逐項對應派工單引用測試名須存在grep-gatepr |
| 關係 | related→LS-213, related→LS-212, related→LS-188, related→LS-167, related→LS-205, related→LS-207, related→LS-190, related→LS-210, related→LS-96, related→LS-209, related→LS-192, related→LS-191, related←LS-263, related←LS-260, related←LS-261, related←LS-255, related←LS-234, related←LS-239, related←LS-236, related←LS-240, related←LS-132, related←LS-203, related←LS-217, related←LS-229, related←LS-231, related←LS-230, related←LS-166, related←LS-208, related←LS-223, related←LS-221, related←LS-222, related←LS-220, related←LS-219, related←LS-189, related←LS-216, related←LS-193, related←LS-215, related←LS-214 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項依 harness 優先序合併（[LS-209](https://linear.app/little-sprout-app/issue/LS-209/harness實作票-agent-防線與-push-韌性-ios-dev-tools-白名單移除-mcp-pencil-禁派) 進 QA 後 lane 空 1 輪）：

* `1ff7b8d8`（S，2026-09-06）[LS-191](https://linear.app/little-sprout-app/issue/LS-191/taskls-133-ios-法務文件-in-app-檢視-sheet使用條款隱私權政策bundled-markdown-渲染footer) 收尾：QA 項 1 以歡迎頁 UITests 綠作證、未實點設定頁；票文條件式範圍（前置票先併入）無人回頭——handoff 驗收項與證據未一對一。
* `df12be2e`（S，2026-09-06）PR #326 `rules` 連紅 4 次：本機無 `LINEAR_API_KEY` 時 `pr-body-check --verify` 對池項行只驗格式；rerun 迴圈盲重跑。
* `88586051`（XS）`privacy_manifest_check.py` 不驗枚舉值合法性。
* `edbc460c`（XS×3）[LS-209](https://linear.app/little-sprout-app/issue/LS-209/harness實作票-agent-防線與-push-韌性-ios-dev-tools-白名單移除-mcp-pencil-禁派) R2 informational：push-gate iPad slug 命中 `qa-*`／`demo-*` 持久機；`ci-ipad` runtime fallback 取第一台；patrol `PENCIL_LINE` 依字面格式。
* `c123d8dd`（S）24 個非 `import os` 的未用 import 清單（[LS-209](https://linear.app/little-sprout-app/issue/LS-209/harness實作票-agent-防線與-push-韌性-ios-dev-tools-白名單移除-mcp-pencil-禁派) `swiftlint analyze` 量得；`7b047d71` 教訓：逐檔 build 驗，勿信 grep）。
* `b63b274a` **(a)（High，S，2026-09-06 09:24 追加；**[LS-210](https://linear.app/little-sprout-app/issue/LS-210/taskls-133-ios-設定頁法律區兩列改開-legaldocumentsheet目前仍-link-到外部) **merge-review R1** `24fc12db` **i3）**：`launchEnvironment["UIPreferredContentSizeCategoryName"]` 對 XCUITest 目標 app 不生效（「設定」標題高度 env AX5＝40.67pt＝未設；`launchArguments` AX5＝69.33pt）——走 env 通道的 AX 字級 UITest 可能假綠；`tap-target-check.sh` 檔頭「launch environment 優先」敘述與實測不符。
* 順修（[LS-209](https://linear.app/little-sprout-app/issue/LS-209/harness實作票-agent-防線與-push-韌性-ios-dev-tools-白名單移除-mcp-pencil-禁派) sweeper `5bd56a44`）：`ci.yml:172` 步驟顯示名缺 `list-ipad-tests`；`promote.sh:16`／`protection-apply.sh:4` 註解仍寫「四個 check」。
* 相關但**不做**：`ca993eba` mutation 自證機械化（L，另票評估）、`0e49f3e3` 已由 [LS-209](https://linear.app/little-sprout-app/issue/LS-209/harness實作票-agent-防線與-push-韌性-ios-dev-tools-白名單移除-mcp-pencil-禁派) 釘句。

## 範圍

1. **handoff 逐項對應 gate**：`qa.md`／`merge-reviewer.md`／`ios-dev.md` 硬規則「handoff『已驗證』每項對應派工單／票文的編號，寫『怎麼驗』（實點路徑或測試名＋結果行）」；新 gate `scripts/gates/handoff-evidence-check.sh <handoff.md>`：掃「已驗證」段每一項須含 `測試名`（正則 `test[A-Z]\w+` 或 `\w+Tests`）或 `截圖／log 路徑`；引用的測試名以 `git grep -q "func <name>"` 驗存在，不存在即紅；qa／reviewer 定義加「貼 comment 前先跑」；自測正負樣本＋mutation；`agent-tools-check.sh` 釘句。
2. **pr-body-check 本機提示**：無 key 時對池項候選行印 `⚠ 未反查（無 LINEAR_API_KEY）：第 n 行候選 <hex>`；push-gate 若能由腳本自行 `source .env`（不經 Bash 命令文字）取 key 則反查（讀取只限 key 名，不印值）；COLLABORATION §4-b rerun 模板加「`rules` 紅先 `gh run view --job --log | grep '✗'`，body 問題改 body＋close/reopen，不盲 rerun」。
3. **privacy manifest**：`privacy_manifest_check.py` 對 `NSPrivacyAccessedAPIType`／reason 枚舉值與 `NSPrivacyCollectedDataType` 等對照 Apple 已公布清單（寫進腳本常數），非法值紅；自測。
4. [LS-209](https://linear.app/little-sprout-app/issue/LS-209/harness實作票-agent-防線與-push-韌性-ios-dev-tools-白名單移除-mcp-pencil-禁派) **順修**：I-a 排除非本票 worktree 命名機；I-b fallback 取最接近 `.ios-runtime` 的較新版本；I-c `pen-status.sh --path` 機器可讀輸出並讓 patrol 改讀它；各附自測。另三條註解／顯示名殘句（見來源）。
5. **unused import 清理**：依 `c123d8dd` 清單逐檔移除、每檔 `xcodebuild build-for-testing` 驗（或分批），任何一檔紅即還原並在 handoff 列「實際有用（符號）」；不開 `unused_import` 規則（[LS-209](https://linear.app/little-sprout-app/issue/LS-209/harness實作票-agent-防線與-push-韌性-ios-dev-tools-白名單移除-mcp-pencil-禁派) 量得 267s）。時間吃緊可排最後，做不完在 handoff 明列剩餘。
6. COLLABORATION §7 每個新 gate 一列。
7. **AX 字級 launch 通道（High）**：盤點 `UIPreferredContentSizeCategoryName` 所有用法，UITest helper 一律改 `launchArguments`（向後相容簽名），AX 測試共用 helper 加「字級真的放大」前置斷言；新 gate：UITests 內 `launchEnvironment[…UIPreferredContentSizeCategoryName` 出現即紅（自測＋mutation、掛 CI `rules`）；`tap-target-check.sh` 檔頭敘述改正。不碰 [LS-190](https://linear.app/little-sprout-app/issue/LS-190/taskls-23-ios-eula-同意頁首次登入版本更新時零容忍條款摘要08刪除單筆內容確認日記-10留言-10b沿-ls-142) worktree 內的呼叫方。

## 不做

mutation 自證機械化（`ca993eba`）；CI 成本 >2 分的 analyzer 規則；app 邏輯。

## 驗收

- [ ] `handoff-evidence-check.test.sh`／其他觸碰的 `.test.sh` 綠＋mutation（實跑原文）；`agent-tools-check.sh` 對真實 agents 綠
- [ ] 對 [LS-191](https://linear.app/little-sprout-app/issue/LS-191/taskls-133-ios-法務文件-in-app-檢視-sheet使用條款隱私權政策bundled-markdown-渲染footer) QA comment `c541cd06` 原文跑 `handoff-evidence-check.sh` 應紅（項 1 無設定頁證據）、對 [LS-192](https://linear.app/little-sprout-app/issue/LS-192/taskls-152-ios-編輯顯示名稱與頭像02家庭成員管理0303-ax303-ipad03b-移除成員確認03c-轉移-owner) QA `88fb24bc` 應綠——作為真實樣本
- [ ] AX 字級 gate 對現有 UITests 綠（改完後）且 mutation 樣本紅；AX 前置斷言在 large 字級下必紅（實跑原文）
- [ ] merge-reviewer 通過；hotfix 流程（自 main、PR 回 main、back-merge）

## 留言（15）

### 2026-09-06T01:12:28.866Z · Chih-Lin Yeh · `f673887a`

**orchestrator 09-06 09:10 派工**：harness lane 補位（LS-209 進 QA 後空 1 輪）。worktree `LS-211`／分支 `hotfix/LS-211-handoff-evidence-gate` 自 main 05b07eb（含 LS-209 新 agent 定義）；PR 回 main、back-merge 由 orchestrator。範圍六項照票文；真實樣本 c541cd06（應紅）／88fb24bc（應綠）。硬規則：禁 fork／禁 Pencil、前景＋timeout、`.env` 讀取只寫在腳本內。

### 2026-09-06T02:49:03.739Z · Chih-Lin Yeh · `f720c228`

**orchestrator 09-06 10:48**：handoff 收到，PR #335 head `3179a70`（8 commit：`handoff-evidence-check`、`pr-body-check` ⚠＋`.env` 只讀 key、privacy manifest 枚舉、I-a／I-b `pick-ipad-runtime.sh`／I-c `pen-status --path`、`ax-launch-check.sh`（`TapTargetMeasurement.swift` 過渡 allowlist，待 LS-190 併主後移除）、13 個 import 清 11 個還原、§7 五列、三條順修；真實樣本 c541cd06 紅／88fb24bc 綠）→ In Review、派 merge-review R1（重點：gate 對 `extension XxxTests` 檔名的假陽性裁決、c541cd06 紅在項 3 而非票文預期的項 1 是否抓對地方、`.env` 讀取安全面、pen-status 契約與 patrol 現況實跑、allowlist 精確度與移除時機、還原 11 檔抽查）。

### 2026-09-06T03:13:16.209Z · Chih-Lin Yeh · `b212dd78`

**merge-review R1（09-06 11:10）— REQUEST_CHANGES**

PR #335 `hotfix/LS-211-handoff-evidence-gate` → `main`；審的 head `3179a700ba1b97b776a59298c37b8499c903f3a6`（開審／收工兩次 `gh pr view 335 --json headRefOid` 皆同此 SHA，未前進）。diff base = merge-base `05b07eb`（現 main `92c891a`）。40 檔 +1767/−154。review worktree `mktemp`（detached 3179a70），收工移除。未持有 supabase-lock（本票不碰容器）。CI 五項全綠（ci／ci-ipad／db／lint／rules）。

四維度：race condition 無適用面（只動 harness 腳本＋13 檔 import 行，無並發程式碼）；效能只有一處已知取捨（見 N1）；平行化無適用面；**scope 乾淨**——diff 全部落在範圍 1–7＋三則 sweeper 順修，`LittleSprout/`／`LittleSproutTests/` 只動 import 行（逐檔核對無夾帶），8 個 commit 與檔案一一對得上。

---

## Findings

### F1（major）`scripts/gates/handoff_evidence_check.py:53-55,191` — 證據字彙與「測試名存在」檢查在本 repo 真實語料上的誤判率過高，而 qa/merge-reviewer 定義把它寫成「綠再貼」的硬規則

**具體失敗情境（我實跑，非推測）**——拿本票自己的 handoff 餵給它：

```
$ bash scripts/gates/handoff-evidence-check.sh scratchpad/LS-211-handoff.md --repo <repo>
✗ 第 40 行引用的測試名 `BogusTests` 在 repo 內找不到
✗ 第 41 行起的列項缺『怎麼驗』證據 …（42／43／44／45 同）
✗ 第 46 行引用的測試名 `LittleSproutTests` 在 repo 內找不到
✗ 第 46 行引用的測試名 `TimelineStoreVideoTests` 在 repo 內找不到
EXIT=1
```

8 條，我判定 0 條真陽性，分三類：

- **(a) 存在性檢查誤判（3 條，無可爭辯）**：`LittleSproutTests` 是**測試 target 名**（`xcodebuild test -only-testing:LittleSproutTests` 是本 repo 每份 handoff 的標準句），不是 class；`TimelineStoreVideoTests` 是**真實檔名**，內部宣告是 `extension TimelineStoreTests`（全 repo 5 個這種檔：`OTPVerificationModelLockout/RateLimit/VerifyRateLimitTests`、`TimelineContentAssemblerThumbPathTests`、`TimelineStoreVideoTests`）；`BogusTests` 是 mutation 敘述裡**刻意的假名**——而「每支 mutation 必列三段」正是 ios-dev.md 的硬規則，兩條規則互相打架。
- **(b) 同型誤判也出現在票文指定的紅樣本裡**：LS-191 `c541cd06` 逐字原文（我用 `list_comments` 重抓、非用測試內嵌版）除了預期的第 11 行，還多紅一條——第 15 行「本 feature 沒有獨立 `*IPadTests` 類別」被判成「引用不存在的測試名 `IPadTests`」。**一句「這東西不存在」的正確陳述被 gate 判成造假**。
- **(c) 證據字彙缺 `.test.sh`（5 條）**：mutation 列項寫了 `pr-body-check.test.sh ⑨ 翻轉，原文：…`，檔案真的在 repo 裡、斷言原文也附了，但 `PATH_RE` 只認 `.png/.log/scratchpad//evidence//.swift`、`COMMAND_RE` 只認 `xcodebuild|bash scripts/`——harness 票的 handoff 主要證據形式（`<gate>.test.sh` 第 n 組）系統性不被承認。

**為什麼是 major 而不是 informational**：`qa.md:42`／`merge-reviewer.md:25` 寫的是「**貼 comment 前先跑** `handoff-evidence-check.sh <暫存檔>` **綠再貼**」。面對上面這些紅，agent 只有兩條路——把正確敘述改寫成能過 gate 的樣子（刪掉「沒有獨立 `*IPadTests` 類別」、避開 target 名、mutation 不敢寫假名 → **handoff 記錄品質下降**，與 `1ff7b8d8` 想解的問題方向相反），或直接忽略規則（規約腐蝕）。CI 確實不跑它、擋不住 merge，但這條規則是寫給人／agent 執行的，成本是真的。

**裁決（回答派工單問的那題）**：**是 gate 設計問題，不是可接受的邊角**。修法（都在同一支檔、都很小）：
1. 存在性檢查改成「任一成立即算存在」：`func|class|struct|enum|extension <name>` **或** `git ls-files "*<name>.swift"` 非空（涵蓋 5 個 extension 型檔案與所有以檔名引用的情形）**或** `git ls-files` 有同名目錄（涵蓋 `LittleSproutTests`／`LittleSproutUITests` target 名）。
2. 候選形狀先過濾：緊鄰 `*` 的 glob（`*IPadTests`）跳過存在性檢查。
3. `PATH_RE` 補 `\.test\.sh`（或改成「repo 內存在的檔案路徑」一律算證據，與 1 共用同一支查存在的函式）。
4. `BogusTests` 這類 mutation 假名：`.test.sh` 被認可後多半自然不再落單；若要更保險，可讓「同一列項已有其他合法證據」時降級為 informational 而非紅。

### F2（major）`scripts/gates/handoff-evidence-check.test.sh:164-171` — ④a 對「證據存在」這條規則零鑑別力（票文驗收條件 1 的主樣本沒被真的釘住）

④a 只斷言 `rc == 1`。我把 `has_evidence()` 整條改成恆真（等於把 gate 的核心規則完全拿掉）再對同一份 LS-191 樣本跑：

```
$ sed 's|^def has_evidence(text):|def has_evidence(text):\n    return True  # MUT|' … | python3 - ls191.md --repo <repo>
✗ 第 15 行引用的測試名 `IPadTests` 在 repo 內找不到
✓ 第 7/9/11/13/17/19 行起的列項有證據
EXIT=1        ← ④a 仍然「通過」
```

也就是：**核心規則被整段刪掉，這支自測還是綠**（紅是 F1(b) 那條誤判撐出來的）。LS-167 R5／LS-188 R3 的教訓（零鑑別力測試）在這裡復現。修法：④a 改成斷言訊息原文（`第 11 行起的列項缺『怎麼驗』證據`）而不是只看 rc；F1 修好後 `IPadTests` 誤判消失，這條也會自然恢復鑑別力——兩者一起修。

### F3（major）`.claude/agents/merge-reviewer.md:25` — 對 merge-review verdict 這個文件型別，規則與 gate 互斥（fail-closed），且自測沒有任何 verdict 形狀的樣本

merge-reviewer 的 verdict 慣例標題是「逐條查實」「Findings」；`SECTION_KEYWORD_RE` 只認「已驗證|驗收」。我拿一份典型 verdict 骨架實跑：

```
✗ handoff-evidence-check：找不到「已驗證」（或標題含『驗收』字樣，如「逐條驗收」）段落——handoff 必須有整理過的逐項驗證清單
EXIT=2
```

exit 2＝fail closed。定義卻寫「綠再貼」——**照定義做就永遠貼不出 verdict**，除非 reviewer 為了討好工具把段落改名。自測 17 組涵蓋 ios-dev handoff 與 QA 逐條驗收兩種形狀，**沒有一組是 merge-review verdict 形狀**（範圍 1 同時改了三支 agent 定義，卻只驗了其中兩種的文件形狀）。修法二選一：段落關鍵字補「查實」（並加一組 verdict 正樣本），或把 merge-reviewer.md 的措辭改成「verdict 的逐條查實段一律用『逐條驗收』標題」並補樣本釘住。

---

## Informational（處置請照 §5-b：High 以上另票，其餘記 LS-96 或直接修）

- **N1**（效能取捨，已揭露）`scripts/ops/patrol.sh:432`：I-c 改成一律呼叫 `pen-status.sh --path`，等於**主動放棄** LS-209 R1 m4「design_wt=1 時沿用 PENCIL_LINE、省一次 pen CLI IPC」的優化，每輪巡檢多一次 IPC（`pen-open.sh --status` 單次上限 8s）。檔頭已寫明理由（不依賴中文字面格式）＋接受代價，我同意這個取捨，僅記錄。
- **N2** `scripts/ops/patrol.test.sh:42-46`：`PATROL_PEN_PGREP` 假身 export 與其註解在本次改動後已成孤兒（`patrol.sh` 不再讀這個變數；我 grep 過全 `scripts/` 只剩這裡）。隔離仍有效（靠第 40 行的 `PATROL_PEN_STATUS_SH` 全域假身），純殘留。Rule 3「自己造成的孤兒要清」。
- **N3** `.github/workflows/ci.yml:172`：顯示名補了 `list-ipad-tests`／`pick-ipad-runtime`，但同一份名稱表仍缺 `design-notes-check`／`detect-simulator`（我機械比對：名稱表 33 項、實際呼叫 33 支，兩邊各差兩項）。與 sweeper 點名的是同一類漂移；票文只要求補 `list-ipad-tests`，故不算超範圍，但洞還在。
- **N4** `scripts/gates/ax-launch-check.sh:38,49`：`relfile=${file#"${root}"/}` 在 `--root` 帶尾斜線時剝不掉前綴 → allowlist 整字比對失效 → 對 `TapTargetMeasurement.swift` 誤紅。CI 不帶 `--root`（走 `git rev-parse`，無尾斜線），只影響手動呼叫。
- **N5** `scripts/gates/pr-body-check.sh:84`：`cut -d= -f2-` 不處理引號包值（`LINEAR_API_KEY="…"`）與 CRLF，值會原樣帶進 token。**現況無影響**——我把主 checkout 的 `.env` 以 symlink 掛進 review worktree 端到端實跑，6 條 LS-96 comment id 全部反查成功（見下方逐條查實 ②）。安全面我確認合格：只 `grep -E '^LINEAR_API_KEY='` 一個 key 名、不 `source`、不印值、token 只走 curl `-K -` stdin（自測 ⑥a3 有斷言 token 不進 argv）；該 `.env` 另有 6 個其他 secret（SUPABASE_*／FIGMA_*），都沒被讀進來。
- **N6** `handoff_evidence_check.py:82-94`：段落偵測兩處 fail-open——(a) 段落中間出現「整行粗體且結尾無標點」的行會提早結束段落，其後列項不檢查；(b) `find_section` 只取第一個含「驗收」的標題，QA comment 若先有「## 驗收條件」（票文抄錄、本來就沒證據）再有「## 逐條驗收」，會檢查錯段落並誤紅。
- **N7** `handoff-evidence-check.test.sh:137-227`：兩個真實樣本對**真 repo** 跑，耦合了 `SettingsViewIPadTests`／`testProfileSectionEntryPushesAndBackReturns`／`testSidebarSelectionIsAccessibleAndDistinguishable`／`TapTargetGateTests`／`testSettingsView`／`testTimelineViewDefaultState`／`testUploadQueueSheetView`／`FamilyMemberActionVisibility.swift` 等 8 個生產符號——**任何未來票改名其中之一，`rules` job 會在無關的 PR 上轉紅**，訊息指向 handoff-evidence-check、極難定位。同一支檔上半段為了避免這件事刻意用合成 fixture repo（檔頭原文：「真原始碼會隨其他票變動，讓自測結果漂移」），下半段違反了自己立的原則。另：④b 內嵌的 LS-192 樣本是**節錄**（真原文 58 行、內嵌 44 行），檔案註解寫「直接在本檔內嵌真實 comment 原文」不精確——我對逐字原文重跑過，`exit 0`、行號 12/16/20/24/28/34/40 與 handoff 引用完全一致，**沒有為了過測而裁剪**。
- **N8**（申報與實效落差，已由 handoff 部分揭露）票文驗收條件 3 後半段「AX 前置斷言在 large 字級下必紅（實跑原文）」與範圍 7 的「UITest helper 一律改 `launchArguments`＋加前置斷言」**本 PR 未交付**——改以 allowlist 過渡。我核對過 LS-190 worktree（`a10f767`）：helper 已改 `launchArguments`、`DeleteConfirmationAX3UITests` 已有「字級有沒有放大」的倍數斷言，所以這條實質由 LS-190 承接、判斷合理；但 handoff 只寫了「allowlist 待移除」，沒有把它對應回**驗收條件 3** 說明——這正是範圍 1 要求 handoff 做的事。請在 R2 handoff 補一行對應。
- **N9** 範圍 1 的 gate 結構上抓不到 `1ff7b8d8` **(b)** 的根因：pool 項講的是「QA 用歡迎頁測試綠去證明設定頁能開 sheet」＝**證據↔宣稱錯配**（語意問題），本 gate 只驗「有沒有證據」。實跑 LS-191 樣本紅在項 3（`qa-test-iPhone17Pro 亮色／深色截圖對稿` 沒給 `.png` 檔名——這條紅本身是合理的真陽性），項 1 因為提到存在的 `LegalDocumentSheetUITests` 而**過關**，也就是票文預期的那一項沒被抓到。實作者在 handoff 已誠實揭露「紅在項 3 而非項 1」，`COLLABORATION.md` §7 新列的 ⚠ 欄也標了「人工」不overclaim，故不重複罰；僅提醒 orchestrator：`1ff7b8d8`(b) 的機械面仍是空的，要不要另立池項由你裁。

---

## 逐條查實（派工單 1–9，附實跑）

**① 範圍 1**：`handoff-evidence-check.test.sh` 17 組全綠（實跑 exit 0）。解析對三種形式穩健性——`## 已驗證`／`**已驗證**`／純文字「已驗證」起段、QA 的數字編號吸收子列點，這幾條我用正負樣本確認可用；缺口見 F1／F3／N6。兩個真實樣本我**用逐字原文重跑**（不採信測試內嵌版）：`c541cd06` → exit 1（第 11 行＝項 3 缺證據，＋第 15 行 `IPadTests` 誤判）；`88fb24bc` → exit 0（第 12/16/20/24/28/34/40 行，與 handoff 引用逐字相符）。「抓對地方嗎」→ 見 N9：抓到的是真陽性但不是票文預期的那一項。`agent-tools-check.sh` 5 條 BODY_RULES 對真實 agents 綠（`bash scripts/gates/agent-tools-check.sh` exit 0；自測 108 組全綠）。CI `rules` 掛載已核對（名稱表＋兩支 `|| rc=1` 呼叫都在）。

**② 範圍 2**：無 key 時對本票真實 PR body 實跑，逐行印出 6 條 `⚠ 未反查（無 LINEAR_API_KEY）：第 11/58/59/60/61/62 行候選 …`，exit 0 不變。`.env` 讀取安全面合格（見 N5）。有 key 路徑我把主 checkout `.env` symlink 進 review worktree 端到端實跑：6 條 comment id 全部反查到（`c123d8dd-a211-…`／`1ff7b8d8-1875-…`／`df12be2e-f511-…`／`88586051-d3a6-…`／`edbc460c-fe0e-…`），且不再印「反查略過」。`pr-body-check.test.sh` 106 組全綠。COLLABORATION §4-b rerun 那句已落地（`rules` 紅先 `gh run view --job --log | grep '✗'` → body 問題改 body＋close/reopen，不盲 rerun）。

**③ 範圍 3**：常數註解有來源（Apple「Describing use of required reason API」／「Describing data use in privacy manifests」）＋記錄日期 2026-09-06＋「需手動核對更新」的但書。枚舉值我逐項核對過 Apple 清單，包含 `NSPrivacyCollectedDataTypePhotosorVideos` 這個 Apple 官方就是小寫 `or` 的拼法——正確。對真 `PrivacyInfo.xcprivacy` 實跑 `privacy-manifest-check.sh` 全綠，且 `NSPrivacyCollectedDataTypes` 三項現在真的被驗了。自測全綠（新增 ②b 7 組＋⑥f–i 4 支 mutation）。

**④ 範圍 4**：I-a `push-gate.test.sh` ㊴(e)(e2)(e3) 全綠（`qa-test-iPadAir11M3` 正確排除、`LS-209-iPadAir11M3` 正確命中）；awk 判準用行首錨點，`qa-*`／`demo-*` 一律排除。I-b `pick-ipad-runtime.sh` 選擇規則正確（≥pin 取最小、無則取最新；13 組自測全綠含 ①b pin=20.0→26.0、①c pin=99.0→26.2），`ci.yml:845-858` 接線我逐行看過：空輸出才 error、版本不符改在取到 UDID 之後印 `::warning::`，與 LS-205 i4 既有裁決一致。I-c `pen-status.sh --path` 契約正確（Pen 沒開／讀不到 → 空輸出＋exit 1；`--path` 帶多餘參數 exit 2）；**現況實跑**：`pen-status.sh --path` → `/Users/clyeh/little-sprout/.claude/worktrees/LS-192/design/littlesprout.pen`，新版 `patrol.sh --brief` 印出 `⚠ Pen 開錯檔（實作票 LS-192）` 與 `[Pen] 開錯檔（實作票 LS-192；lane=lane:ui…）`——符合預期。`patrol.test.sh` 305 組全綠、`pen-status.test.sh` 15 組全綠。

**⑤ 範圍 7**：掃描範圍限 `LittleSproutUITests`、只認 `launchEnvironment[…UIPreferredContentSizeCategoryName` 的實際 subscript（純註解不誤擋，自測 ① 有正樣本）。allowlist **是檔名精確整字比對**（`[ "$relfile" = "$a" ]`），只有 `LittleSproutUITests/TapTargetMeasurement.swift` 一項；移除時機在腳本檔頭、`COLLABORATION.md` §7、PR body 第 7 點、handoff「未完成」四處都寫了。**mutation 我自己重放**（不是跑他的自測）：把 `ALLOWLIST=""` 後對真 repo 跑 → `✗ … TapTargetMeasurement.swift:14: app.launchEnvironment["UIPreferredContentSizeCategoryName"] = "UICTContentSizeCategoryL"`、exit 1（原樣 exit 0、印「允許期限內」）——確認 allowlist 是唯一放行原因、gate 對 main 現況真的有效。`tap-target-check.sh` 檔頭訂正與 LS-96 `b63b274a`(a) 的實測數字（env AX5＝40.67pt、`launchArguments` AX5＝69.33pt）逐字相符，且補上了「本 gate 量到的字級其實來自模擬器系統 content_size」這個關鍵推論。

**⑥ 範圍 5**：24 個候選＝13 刪＋11 還原，我對 LS-96 `c123d8dd` 清單逐一比對，**union 剛好 24、無漏無重**。抽 3 檔還原者查實：`Spacing.swift`（20 處 `CGFloat`，`Foundation` 轉出 CoreGraphics）✓、`MasonryLayout.swift`（12 處 `CGFloat`）✓、`PickedItemLoader.swift`（檔內**既有**註解已寫明 `PhotosPickerItem` 在 PhotosUI×SwiftUI cross-import overlay 裡、`swiftc -typecheck` 實測缺 `import SwiftUI` 會 `cannot find type`）✓——三條申報屬實。13 個移除的 diff 我逐檔看過：只動 import 行與隨之變成孤立的首行空白，零夾帶。**我自己跑的 build**：`xcodebuild build-for-testing -scheme LittleSprout -destination id=90FB3609-…（iPhone 17 Pro, iOS 26.0）` → `** TEST BUILD SUCCEEDED **`、exit 0、`grep -c "error:"` = 0；`swiftlint lint --strict --quiet` exit 0。⚠ **runtime 註記（LS-205）**：`.ios-runtime` 釘 26.2，本機只有 26.0／26.5，我與實作者都在 **26.0** 上 build；純編譯不涉版面量測，判定不影響結論，但依規則揭露。

**⑦ 順修三條**：`ci.yml:172` 顯示名補 `list-ipad-tests` ✓（同時補了 `pick-ipad-runtime`）；`promote.sh:8,18` 改成引用 `$REQUIRED_CHECKS` 並註明「改集合時這段敘述不會自動更新」——我核對 `promote.sh:38` `REQUIRED_CHECKS="ci ci-ipad db lint rules"` 與敘述一致 ✓；`protection-apply.sh:4` 五個 check ✓（與 `:93-95` 的 CHECKS_JSON、`:64` 的 `["ci","ci-ipad","db","lint","merge-review","rules"]` 一致）。`COLLABORATION.md` §7 五列我逐列比對程式碼，**無 LS-207 那種 N 類錯述**（含「macOS 內建 git `-E` 不支援 `\b`、改用 `-P`」這個細節都與 `handoff_evidence_check.py:139-143` 相符）。

**⑧ 申報可驗證**：8 個 commit ↔ 檔案對照逐一核對，與 handoff／PR body 完全一致（`76a8019` 範圍1＋sweeper ci.yml、`ed88439` 範圍2、`eaf388c` 範圍3、`a049605` I-a/b/c、`8667331` 範圍7、`aa4df80` sweeper 註解、`cf8a92d` 範圍5、`3179a70` 範圍6）。票文 5 個池項 id 我到 LS-96 逐一查實存在且內容相符（`1ff7b8d8`／`df12be2e`／`88586051`／`edbc460c`／`c123d8dd`；範圍 7 的 `b63b274a` 也查實）。PR body 過 gate：`pr-body-check.sh <body> --branch hotfix/LS-211-handoff-evidence-gate --verify` exit 0。diff 無夾帶（見四維度 scope 段）。

**⑨ CI**：五項全綠、head 未動 → 依裁決貼 `merge-review = failure`。

---

## mutation 重放（自己跑，斷言訊息原文）

實作者申報 7 支。我**逐支重放**（跑其 `.test.sh` 內嵌的負控＋另外自己手做兩支獨立 mutation），7 支全部真的翻轉、全部是**斷言失敗**而非 crash／build fail（mutant 期望值都是 exit 0＝綠，crash 會是非 0，已排除 LS-188 R3 那種誤判）。原文（與 handoff 引用逐字相符）：

1. `✓ ⑤ mutant（拿掉『測試名存在』檢查）：假測試名 BogusTests 樣本改判綠——證明②b 的紅是這條檢查造成的`
2. `✓ ⑨ mutant（拿掉逐行 ⚠ 未反查迴圈）：⑥a2 的正樣本消失——確認紅是這段迴圈造成的`
3. `✓ ⑥f mutant（拿掉 API 分類枚舉比對）：虛構分類樣本改判過──證明枚舉比對是這裡在擋`（⑥g／⑥h／⑥i 同型皆綠）
4. `✓ ㊴(e3) mutant（拿掉排除判準）：qa-test-iPadAir11M3 改判有被呼叫——確認排除判準是 (e) fail-open 的原因`
5. `✓ ⑤ mutant：pin=20.0 改判選到最新 26.2（而非①b 的 26.0）——確認①b 的答案是「選最接近」分支造成的`
6. `✓ ㉖ mutant：拿掉偵測段後，同一份「非 design 票」負樣本變綠（Pen 開錯檔偵測段確實是原因）`
7. `✓ ④ mutant（拿掉偵測 pattern）：②的紅樣本改判過──證明偵測 pattern 是這裡在擋`

**我自己另做的兩支獨立 mutation**（不經他的自測框架）：
- `privacy_manifest_check.py` `elif data_type not in VALID_COLLECTED_DATA_TYPES:  # PRIVACY-COLLECTED-TYPE-ENUM` → `elif False:`，自製 `NSPrivacyCollectedDataTypeTotallyBogus` manifest：原樣 `✗ … NSPrivacyCollectedDataTypes[0] 的型別 NSPrivacyCollectedDataTypeTotallyBogus 不在 Apple 已公布清單內` exit 1 → mutant `✓ 收集資料型別 NSPrivacyCollectedDataTypeTotallyBogus` exit 0。真翻轉、是斷言而非 crash。
- `ax-launch-check.sh` `ALLOWLIST=""` 對真 repo（見逐條查實 ⑤）。

以及 **F2 那支反向 mutation**（證明自測不夠力）：`has_evidence()` 恆真 → ④a 仍 exit 1、仍會判「通過」。

---

## R2 要求（都在範圍 1 內，其餘範圍我沒有 change request）

1. F1：存在性檢查同時接受「檔案／目錄存在」、glob 形狀跳過、`PATH_RE` 認 `.test.sh`；修完請**對本票自己的 handoff 與 LS-191／LS-192 逐字原文各跑一次**並把輸出貼進 handoff（LS-191 應只剩第 11 行那一條紅）。
2. F2：④a 改斷言訊息原文；並補一支「拿掉 `has_evidence` → ④a 必須紅」的反向負控。
3. F3：verdict 形狀二選一（段落關鍵字補「查實」／改 merge-reviewer.md 措辭），且補一組 verdict 形狀樣本。
4. N2 順手清（自己造成的孤兒）；N4／N6／N7 建議收（N7 至少把真實樣本的耦合寫進檔頭風險註記，或改用 fixture stub）；N8 handoff 補一行對應驗收條件 3。
5. N1／N3／N5／N9 我判定不必動，記錄即可（N3／N9 若要進 LS-96 由 orchestrator 裁）。

已貼 commit status `merge-review = failure` 到 `3179a70`。


### 2026-09-06T03:15:27.167Z · Chih-Lin Yeh · `19d0cd68`

**orchestrator 09-06 11:15 裁決（merge-review R1 b212dd78）**：三個 major 皆是範圍 1 gate 設計問題——F1 假陽性（target 名、`extension XxxTests` 檔名、glob 形狀、否定句、`.test.sh` 證據不被認）→ 改為「檔名／目錄存在亦可」＋glob／否定句跳過＋`.test.sh`／`xcodebuild`／`gh run` 算證據＋mutation 語境不驗名，dogfood 本票 handoff 必 0 假陽性；F2 ④a 只斷 rc → 斷具體 finding 行＋`has_evidence` 恆真必紅的 mutation；F3 verdict 骨架 fail-closed → 多標題／`--section`＋verdict 與 qa 骨架正樣本，且 `merge-reviewer.md`／`qa.md` 的「綠再貼」改為「跑過並附輸出；紅則逐條說明誤判或補證據」（不讓 agent 為工具改寫正確敘述）。informational：N2／N3／N4 順修、N7 改 fixture、N5／N6 檔頭註明、N8 handoff 補對應、N1／N9 不修附理由。已派原 agent R2；R2 push 後派 merge-review R2。

### 2026-09-06T04:11:18.842Z · Chih-Lin Yeh · `08376266`

**orchestrator 09-06 12:11**：R2 handoff 收到，PR #335 head `54ad69f`（F1 存在性接受宣告／檔名／目錄名＋glob／否定句／mutation 語境跳過＋`.test.sh`／`gh run`／`.xcresult` 算證據；F2 ④a 斷具體訊息＋反向 mutation；F3 「查實」標題＋定義改「跑過並附輸出、紅則逐條說明」；N2–N5 順修、N7 改 `RealSampleFixture/`；dogfood 兩份 handoff 0 假陽性，c541cd06 只紅第 11 行、88fb24bc 綠）。已派 merge-review R2（同 reviewer）：重放 8 條假陽性與新漏網、放寬是否失去鑑別力、verdict／qa 骨架不 exit 2、fixture 解耦、`SettingsViewIPadTests` flake 是否入池；status 貼 54ad69f。

### 2026-09-06T04:30:02.708Z · Chih-Lin Yeh · `f6f1ba86`

**merge-review R2（09-06 12:27）— REQUEST_CHANGES**（1 個 major：範圍 6 的 §7 那列未隨 R2 同步，其中一句與本 PR 自己推翻的規則相反）

審的 head `54ad69f953d10b051a79bf4e8e41726bcaab279e`（開審／收工兩次 `gh pr view 335 --json headRefOid` 皆同此 SHA）。R2 delta `3179a70..54ad69f` 共 12 檔 +416/−72、2 個 commit（`0847b6d` F1/F2/F3/N6/N7/N9、`54ad69f` N2/N3/N4/N5），**零 Swift 檔**（`git diff --name-only | grep -c '\.swift$'` = 0），scope 乾淨。CI 五項全綠（ci／ci-ipad／db／lint／rules，run 34010695699）。review worktree `mktemp`（detached 54ad69f），收工移除。未持有 supabase-lock。

**R1 三個 major 我判定都已實質解決**（逐條實跑見下）；擋下來的是一條 R2 新產生的問題。

---

## M1（major）`docs/COLLABORATION.md:323` — 範圍 6 交付的 §7 那列完全沒隨 R2 更新，七處敘述與程式不符，其中一句正是本 PR 推翻的規則

`git diff --name-only 3179a70..54ad69f | grep -c COLLABORATION` = **0**。逐點機械比對該列與 head 程式碼：

| §7 現有敘述 | head 實況 |
|---|---|
| 段落起點「`## 已驗證`／`**已驗證**`／標題含『驗收』如『逐條驗收』」 | 還有「查實」（`SECTION_KEYWORD_RE = 已驗證\|驗收\|查實`）——F3 的修法本身 |
| 「須含測試名（`git -C <repo> grep -P` 驗真的存在）」 | 存在性已放寬為 grep 五種宣告 **或** 同名檔案 **或** 同名目錄，且 glob／同句否定／mutation 語境三種跳過不驗 |
| 路徑「`.png`／`.log`／`scratchpad/`／`evidence/`／`.swift`」 | 多了 `.test.sh` |
| 指令「`xcodebuild`／`bash scripts/…`」 | 多了 `gh run view`／`.xcresult` |
| 「新增 5 條 `BODY_RULES` 釘句」 | 實際 7 條（R2 加了兩條「紅則逐條說明是誤判或補證據」） |
| 「自測 17 組」 | 實跑印 30 條 ✓（①g–①m、④a-mut、⑥×3 等） |
| **「qa／merge-reviewer 另須『貼 comment 前先跑本腳本綠』」** | **`qa.md:43`／`merge-reviewer.md:25` 已改成「跑過並附輸出；紅則逐條說明是誤判或補證據——不得為了討好工具改寫正確敘述（不要求一定要綠）」** |

最後一條不是「不夠新」，是**與現行規則相反**。R1 F3 判 major 的核心就是「綠再貼」會逼 agent 改寫正確敘述；R2 在 agent 定義裡拿掉了，但**規約正典 §7 仍寫著同一句**——併進 main 之後，main 同時存在兩份互相矛盾的規則陳述，而 §7 正是 CLAUDE.md 指定的「前饋↔反饋對照表」（agent／orchestrator 查規約的地方）。等於 F3 只落地了一半。同型前例：LS-207 的 §7 錯述被判必修。

**R3 要求（只有這一項，改一列即可）**：更新 `COLLABORATION.md:323` 那列的上述七點；順帶 `:327` 的 ax-launch-check 列「正負樣本 7 組」現為 14 條（①n 已加）。其餘四列我逐列比對過，R2 沒有讓它們失準。

---

## R1 三個 major 的查實（都已解決）

**F1 ✓**——`handoff_evidence_check.py` 的存在性放寬與字彙補齊確實有效，我用 R1 的 8 條假陽性逐條重放：

- dogfood：`bash scripts/gates/handoff-evidence-check.sh <本票 R1 handoff>` → **exit 0**（7 條 ✓，第 40–46 行）；`<本票 R2 handoff>` → **exit 0**（4 條 ✓）。**0 假陽性**，達成派工單要求。
- LS-191 `c541cd06` **逐字原文**（我自己用 `list_comments` 重抓，不採信測試內嵌版）→ exit 1，**只剩第 11 行**（項 3）：`✗ handoff-evidence-check：第 11 行起的列項缺『怎麼驗』證據（須含測試名、.png/.log/.test.sh/scratchpad//evidence//.swift 路徑，或 xcodebuild／bash scripts/／gh run view／.xcresult 命令）`；第 15 行 `IPadTests` 誤判消失。LS-192 `88fb24bc` 逐字原文 → exit 0（第 12/16/20/24/28/34/40 行）。
- 三條新存在性規則我直接對真 repo 逐一驗過（`test_name_exists`）：`TimelineStoreVideoTests` True（檔名）、`LittleSproutTests`／`LittleSproutUITests` True（目錄）、`OTPVerificationModelRateLimitTests`／`SettingsViewIPadTests` True、`BogusTests`／`TotallyFakeTests`／`testNopeDoesNotExist` **False**（假名仍抓得到，沒有一路放行到底）。

**F2 ✓**——④a 現在斷具體訊息，而且我**自己重放**了反向 mutation（不用他的框架）：把 `has_evidence()` 改成 `return True` 後對 LS-191 逐字原文跑 → **exit 1 → exit 0**（第 11 行的紅消失，7 條全 ✓）。是斷言翻轉、不是 crash（mutant 期望值 0，crash 會非 0）。R1 我指出的「核心規則刪掉自測還是綠」已不成立。

**F3 ✓（有殘留，見 m4）**——`SECTION_KEYWORD_RE` 補「查實」後，`## 逐條查實` 段落可解析（①g 正樣本）；`qa.md`／`merge-reviewer.md`／`agent-tools-check.sh`／腳本檔頭四處措辭一致改成「跑過並附輸出；紅則逐條說明」。qa handoff 骨架（LS-192 逐字原文）exit 0、不 exit 2 ✓。

**自測與 gate 全綠（我實跑）**：`handoff-evidence-check.test.sh` 30 條 ✓、`agent-tools-check.test.sh` 112 組、`ax-launch-check.test.sh` 14 條、`pr-body-check.test.sh` 106 組、`patrol.test.sh` 303 條 ✓ 全綠；`agent-tools-check.sh`／`ax-launch-check.sh` 對真 repo exit 0；PR 上的 live body 過 `pr-body-check.sh --branch … --verify` exit 0（12 條「已修」SHA、6 條 LS-96 id）。

**N2–N9 逐條**：N2 孤兒 export 已移除（`grep PATROL_PEN_PGREP` 全 repo 0 筆，patrol 自測 303 條全綠）✓；N3 名稱表補 `design-notes-check`／`detect-simulator`，我重跑機械比對兩邊一一對應 ✓；N7 兩個真實樣本已改對 `RealSampleFixture/`（`--repo "$root"` 在測試檔內 0 筆殘留），且內嵌原文補成逐字全文（我對 Linear 原文核對過行號一致）✓；N5／N6／N9 檔頭註記已寫 ✓；N8 已在 PR body 補對應 ✓；N1 不修（我同意）✓。

**N4 — 我 R1 錯了，在此更正**：實作者說本機無法重現，我自己重放確認**他是對的、我是錯的**——用 R1 版腳本 `bash ax-r1.sh --root "<repo>/"`（帶尾斜線）→ 仍印「允許期限內」、exit 0，allowlist 沒有失效。原因是 `$root` 與 grep 回報的路徑各自多一個斜線、`${file#"${root}"/}` 兩邊一致抵銷。R1 我把這條列成 informational 卻沒有實跑就寫成事實，是我的疏失。R2 保留 `root=${root%/}` 作防禦性正規化＋①n 正樣本、並在測試檔以註解誠實說明「不做必然不會翻轉的 mutation」——這個處理方式正確。

---

## 新發現（都不擋合併，附實跑）

**m1（minor）`handoff_evidence_check.py:180-190` — mutation 語境的跳過是「整個列項」級，實測讓存在性檢查在 ios-dev handoff 上完全失效**

我把每個列項的候選逐一列出（skip／實際驗證）：

| 文件 | 跳過 | 實際驗證 |
|---|---|---|
| 本票 R1 handoff | 6 | **0** |
| 本票 R2 handoff | 9 | **0** |
| LS-191 `c541cd06` | 1 | 9 |
| LS-192 `88fb24bc` | 0 | 7 |

QA 裁決保有全部鑑別力（很好），但 **ios-dev handoff 的「已驗證」段一個候選都沒被驗**——因為 `split_items` 的數字編號會把其後所有 `-` 子列點吸收進最後一個編號項，而該項含「mutation」字樣，於是整段（含跟 mutation 無關的「24 個未用 import 逐檔驗證」段落裡的 `LittleSproutTests`／`TimelineStoreVideoTests`）一起跳過。票文標題寫的「引用測試名須存在（grep gate）」對 ios-dev handoff 已是空轉。

而且**這個放寬其實用不著這麼寬**：我把三個 skip 判準全部關掉（`skip = False  # HANDOFF-SKIP-CHECK`）重跑 R1 handoff，只剩 **1 條**假陽性（`BogusTests`）；LS-191 逐字原文則**一條假陽性都沒有**（`IPadTests` 已被「同名檔案」規則覆蓋——`*IPadTests.swift` 命中 `SettingsViewIPadTests.swift`）。也就是我 R1 的 8 條裡有 7 條是靠存在性放寬修好的，glob／否定／mutation 三個啟發式只為第 8 條而存在，代價卻是整段不驗。

建議（不強制、由 orchestrator 裁）：mutation 跳過改成**候選層級**（只跳過出現在反引號斷言原文內、或與「假名／不存在」同句的候選），或保留跳過但把該類候選降級成 `ⓘ` 提示而非完全不查。風險目前由 merge-reviewer「mutation 一律自己重放」（LS-209）兜著，所以只列 minor。

**m2（minor）R2 handoff「已驗證」段的 flake 歸因對不上原文——引用的等待字串不可能來自它點名的測試**

handoff 寫：第一次 push 在 `SettingsViewIPadTests.testContentSafetySectionStorageRowPushesAndBackReturns` 失敗，訊息 `Waiting 10.0s for "在背景繼續，關閉視窗" Button to exist`。但機械核對：

- `grep -rn "在背景繼續" LittleSproutUITests` → 只有 `UploadQueueSheetUITests.swift:36`（`waitForExistence(timeout: 10)`，與「10.0s」相符）、`:70`、`:152`。
- `SettingsViewIPadTests.swift:88-100` 從未引用該按鈕，且用的是 `timeout: 5`。

所以本機紅的應該是 `UploadQueueSheetUITests`，不是被點名的那支。這正是 LS-209「申報一律核對」要抓的形狀（雖然只是環境事件、不影響程式）。**CI 側我兩支都查了、都是綠的**：`gh run view 34010695699 --job 101425875894 --log` → `Test Case '-[LittleSproutUITests.SettingsViewIPadTests testContentSafetySectionStorageRowPushesAndBackReturns]' passed (19.077 seconds)`；`ci` job log 有 7 處 `UploadQueueSheet`、job conclusion success。

**入池建議**：入 LS-96（XS）——標的改成 `UploadQueueSheetUITests` 在殘留 app 狀態的模擬器上偶發等不到 footer（`simctl erase` 後即過；`push-gate.sh` 跑 UITest 前不重置 app 狀態），並附「handoff 歸因需以失敗原文比對測試檔」這條教訓。是否開由 orchestrator 裁。

**m3（minor）證據字彙仍是 iOS 本位——harness 票引用 `.py`／`.sh`／`.md`／`.yml` 原始碼一律不算證據**

我照新規則對**自己這份 verdict** 的逐條查實段跑了 gate（輸出附在最後）：6 項中 4 項判「缺證據」，全是假陽性——因為我引用的是 `handoff_evidence_check.py:191`、`docs/COLLABORATION.md:323`、`RealSampleFixture/` 這類 harness 原始碼位置，而 `PATH_RE` 只認 `.swift`（R2 只補了 `.test.sh`）。這與 R1 F1(c) 同一類（當時只修了 `.test.sh` 一項）。建議：`PATH_RE` 改成「任何能解析成 repo 內既有檔案的路徑 token」（可重用 R2 已經寫好的 `git ls-files` 查存在），一次消滅整類。

**m4（minor）verdict 圈號編號形狀仍 exit 2**

派工單要我用自己的 verdict 骨架驗——我 R1 verdict 的列項形狀是 `**① 範圍 1**：…`（本 repo 慣用圈號），實跑仍 `✗ 「已驗證」段落內沒有任何列項（-／數字編號）`、**exit 2**；改成 `- **① …**` 或 ASCII `1.` 就 exit 0（我三種形狀都跑過）。①g 正樣本用的是 ASCII `1.`，所以自測抓不到這個缺口。修法一行：`NUMBERED_ITEM_RE` 接受 `①-⑳` 等圈號。因為 R2 已把「綠再貼」拿掉、exit 2 是可解釋的結果，只列 minor。

## Informational

- **i1** `test_name_exists` 的 `git ls-files -- "*<name>.swift"` 未錨定，任何「真實檔名的後綴」都算存在（`IPadTests`／`PadTests` 皆因 `SettingsViewIPadTests.swift` 判為 True）。目前是好事（順手修掉 `UITests` 類誤判），但也表示假名只要是既有檔名的後綴就能過。若要收緊：改比對 basename 相等。
- **i2** 存在性用 `git grep` 掃全 repo **檔案內容**，所以只寫在自測 heredoc 裡的 fixture 型別（如 `ax-launch-check.test.sh` 內的 `final class BadTests`）也算「存在」。同上，方向是放寬。
- **i3** 否定詞判定只看候選**之前**的同句文字：`「本 feature 沒有獨立 *IPadTests 類別」`（否定在前）過，`「`FooBarTests` 這個類別不存在」`（否定在後）仍誤紅（我實跑確認）。另一面，`未`／`無` 太常見會過度跳過：`「尚未併入的 `TotallyFakeTests` 全綠」` 這種**假名**目前直接放行（實跑確認）。兩個方向都存在，屬啟發式必然的取捨，記錄即可。
- **i4** `qa.md:43`／`merge-reviewer.md:25` 正文仍列舊字彙（沒有 `.test.sh`／`gh run view`／`.xcresult`）且仍寫「不得引用不存在的測試名」而未提三種跳過——定義文字比工具嚴，agent 會以為規則更緊。順手可補。
- **i5** handoff 申報「`handoff-evidence-check.test.sh` 37 組全綠」與實跑不符：實際印 30 條 ✓（`grep -c "^expect "` = 18＋12 個 ad-hoc 區塊）。其他自測都有精確的「n 組樣本」總結行，這支沒有，建議也印一行計數避免手數漂移。
- **i6** N8 的理由「coordinator 派工中途已改口裁定不要再改 helper 本體」在 Linear 沒有對應紀錄（`f673887a`／`f720c228`／`19d0cd68` 都沒有這句）。我核對過 LS-190 `a10f767` 確實已承接，判斷成立；但建議 orchestrator 把這條口頭裁決補一則 comment，否則將來查不到依據。

---

## 我自己這份 verdict 跑 gate 的輸出（依 R2 新規則附上；紅的四條是 m3 描述的假陽性，不改寫敘述）

```
✗ handoff-evidence-check：第 4 行起的列項缺『怎麼驗』證據（…）   ← 引用 handoff_evidence_check.py，.py 不算證據
✗ handoff-evidence-check：第 5 行起的列項缺『怎麼驗』證據（…）   ← 引用 qa.md／merge-reviewer.md 行號
✗ handoff-evidence-check：第 6 行起的列項缺『怎麼驗』證據（…）   ← 引用 docs/COLLABORATION.md:323
✗ handoff-evidence-check：第 7 行起的列項缺『怎麼驗』證據（…）   ← 引用 RealSampleFixture/
✓ 第 3 行起的列項有證據
✓ 第 8 行起的列項有證據
EXIT=1
```

（另：完整 verdict 用圈號 `**① …**` 形狀時 exit 2＝m4；上面這份是改用 `-` 列點才跑得起來的等價摘要。）

---

## 環境

未 boot 任何模擬器（R2 全部是腳本／文件檢查，沒有 build）；未持有 supabase-lock；不碰 Pen。review worktree 收工移除。

已貼 commit status `merge-review = failure` 到 `54ad69f`。**R3 只需改 `COLLABORATION.md` 那一列（＋ax 列組數）**；其餘 m1–m4／i1–i6 皆不擋，處置請照 §5-b 由 orchestrator 裁（我的建議：m1／m3 值得在下一張碰這支 gate 的票收，m2 入 LS-96，其餘記錄即可）。


### 2026-09-06T04:33:58.724Z · Chih-Lin Yeh · `01a97896`

**orchestrator 09-06 12:31**：merge-review R2（f6f1ba86）REQUEST_CHANGES——R1 三個 major 已實質解決（dogfood 0 假陽性、反向 mutation 翻轉、措辭改「附輸出」）；唯一 blocker M1：`COLLABORATION.md:323` §7 列七處與 head 程式不符（含本 PR 自己推翻的「綠再貼」）、`:327` 組數；另 m1（mutation 跳過整列過寬）、m2（flake 歸因應為 `UploadQueueSheetUITests:36`）、m3（證據字彙不認 `.py`／`.sh`／`.md`）、m4（圈號列項 exit 2）、i1–i6；reviewer 撤回 R1 N4。已派原 agent R3：§7 兩列重寫、跳過規則收窄＋否定詞前後看＋錨定＋排除 fixture、證據字彙擴充、圈號列項、m2 池項更正、i4／i5 順修。R3 push 後派 merge-review R3。

### 2026-09-06T05:11:45.396Z · Chih-Lin Yeh · `43e66727`

**orchestrator 09-06 13:11**：R3 handoff 收到，PR #335 head `ac8d7cf`（§7 `:323`／`:327` 兩列重寫；跳過判準收窄＋證據辨識擴充＋圈號列項＋錨定＋排除 fixture，自測 47 組；qa／merge-reviewer 措辭同步 35 條釘句；m2 池項更正 `3002fb63`；自 dogfood exit 0、歷史 R2 handoff 因收窄多一條 `BogusTests` 紅已如實揭露）。已派 merge-review R3（同 reviewer）：§7 七處逐對、候選驗證比例、R2 verdict 原文與圈號樣本、i1–i3 反例、真實樣本重跑；status 貼 ac8d7cf。

### 2026-09-06T05:31:40.893Z · Chih-Lin Yeh · `26ce0bf8`

**merge-review R3（09-06 13:29）— APPROVE**

審的 head `ac8d7cf8e34d26360185bfbce1dfe8d6f8144cf3`（開審／收工兩次 `gh pr view 335 --json headRefOid` 皆同此 SHA）。R3 delta `54ad69f..ac8d7cf` 共 **5 檔 +284/−53、0 個 `.swift`**（`git diff --name-only | grep -cE '\.swift$'` = 0），3 個 commit（`8479a3a` M1／`99bf785` m1,m3,m4,i1,i2,i3,i5／`ac8d7cf` i4），scope 乾淨。**CI 五項全綠**（ci／ci-ipad／db／lint／rules，run 34013228674）。review worktree `mktemp`（detached ac8d7cf），收工移除。未持有 supabase-lock；未 boot 模擬器；不碰 Pen。

R2 唯一擋人項 M1 已解，m1–m4／i1–i6 逐條處置到位，**無 blocker／major**。

---

## 逐條查實

**① M1（R2 唯一 major）✓ 已解** — `docs/COLLABORATION.md:323`／`:327` 兩列，我把 R2 點名的七處逐處對照 head 程式：

| R2 點名 | head 程式 | §7 現在寫的 | 判定 |
|---|---|---|---|
| 段落關鍵字漏「查實」 | `SECTION_KEYWORD_RE = 已驗證\|驗收\|查實` | 「標題含『驗收』／『查實』如『逐條驗收』『逐條查實』——後者涵蓋 merge-reviewer verdict 慣例」 | ✓ |
| 存在性只寫 grep | 5 種宣告／basename 整字相等／同名目錄，排除 `*.test.sh`／`*.test.js` | 三條逐一列出、含排除說明 | ✓ |
| 路徑漏 `.test.sh` | `PATH_RE` 含 `.test.sh`／`.py`／`.sh`／`.md`／`.yml`／`.json` | 全列出 | ✓ |
| 指令漏 `gh run view`／`.xcresult` | `COMMAND_RE` 含兩者 | 全列出 | ✓ |
| 「5 條 BODY_RULES」 | 實測 `sed -n '/LS170-BODY-RULES-START/,/END/p' \| grep -c LS-211` = **7** | 「7 條」 | ✓ |
| 「自測 17 組」 | 實跑尾行 `✓ handoff-evidence-check 自測通過（47 組樣本）` | 「47 組」 | ✓ |
| **「貼 comment 前先跑本腳本綠」（與現行規則相反）** | `qa.md:43`／`merge-reviewer.md:25`：「跑過並附輸出；紅則逐條說明是誤判或補證據（不要求一定要綠）」 | 同一句已改寫 | ✓ |

另 `:327` ax 列「正負樣本 7 組」→「13 組」，我實跑 `ax-launch-check.test.sh` 印 14 行 ✓（13 條測試＋1 總結），數字對得上。三種候選層級跳過判準也新寫進 §7，敘述與 `is_glob_candidate`／`is_negated`／`is_mutation_context` 三個函式一致。`git diff 54ad69f..ac8d7cf -- docs/COLLABORATION.md` 只動這兩列，無夾帶。

**② m1 跳過收窄 ✓** — R2 我量到「ios-dev handoff 的存在性檢查形同虛設」，R3 收窄為候選所在**同一行**後重新量（同一支測量腳本）：

| 文件 | R2（區塊級） | R3（行級） |
|---|---|---|
| 本票 R1 handoff | 跳過 6／驗證 **0** | 跳過 2／驗證 **4** |
| 本票 R2 handoff | 跳過 9／驗證 **0** | 跳過 6／驗證 **3**（其中 1 條判假名） |
| LS-191 `c541cd06` | 1／9 | 2／8 |
| LS-192 `88fb24bc` | 0／7 | 0／7 |

**我自己重放這支 mutation**（不用他的自測框架）：把 `is_mutation_context` 的 `MUTATION_CONTEXT_RE.search(line)` 改回 `search(text)`（＝區塊級）→ R1 handoff 立刻回到 **跳過 6／驗證 0**，與我 R2 量到的數字完全一致——確認收窄是原因、我 R2 的測量也可重現。

R2 handoff 新出現的 `BogusTests` 紅（第 43 行起的列項）我核對過：字面出現在第 55 行「R1 修前這份 handoff 本身觸發 8 條假陽性，含 `BogusTests`」——這是**對假名的後設引述**、該行不含 mutation 關鍵字，收窄後不再被鄰行的 mutation 語境順帶放行。這是收窄的必然代價、不是回歸；實作者選擇不回頭改歷史檔並如實揭露，符合 R2 新規則「紅則逐條說明、不得為了討好工具改寫正確敘述」——處置正確。

**③ m3／m4 ✓（m3 部分達成，殘留是我自己沒引檔案）** — 我把 R2 當時量到 4/6 假陽性的**同一份原檔**（`r2-verdict-check.md`）重跑：4 條紅 → **3 條紅**。消失的那條正是引用 `docs/COLLABORATION.md:323` 的項（`.md` 現在算證據）。剩下 3 條我逐條檢視，**都不是誤判**：兩條是我只寫結論沒引任何檔案／命令，一條是我只寫裸目錄名 `RealSampleFixture/`（無副檔名）——gate 判「缺怎麼驗」是對的，實作者在 handoff 也明白說了「裸目錄名仍正確判缺證據，未過度放寬」，我同意這個界線。
m4：圈號粗體 `**① …**` 樣本 **exit 0**（R2 是 exit 2）；**我自己重放 mutation**（`NUMBERED_ITEM_RE` 退回純 ASCII）→ 同一份樣本回到 **exit 2**，確認圈號支援是原因。m3 我也做了行級重放：`PATH_RE` 退回 R2 窄版 → 引用 `docs/COLLABORATION.md:323` 那項由 `✓ 第 6 行起的列項有證據` 翻成 `✗ …缺『怎麼驗』證據`，確認新副檔名清單是原因。

**④ i1／i2／i3 各一個反例 ✓**（直接呼叫 `test_name_exists`／`is_negated`）：

- i1 錨定：`IPadTests` → **False**、`PadTests` → **False**（R2 時兩者都因 `*IPadTests.swift` 尾碼萬用字元被判 True）；真名不受影響：`SettingsViewIPadTests`／`TimelineStoreVideoTests`／`LittleSproutTests`／`LittleSproutUITests` 皆 True。**重放 mutation**（`relpath.split("/")[-1] == target` 改回 `endswith(target)`）→ `IPadTests` False → **True**，確認整字比對是原因。
- i2 排除自測 fixture：只宣告在 `ax-launch-check.test.sh` heredoc 裡的 `BadTests`／`AnotherBadTests`／`MixedTests`、以及只在自測 fixture 出現的 `FooTests` 皆 **False**。**重放 mutation**（`GREP_EXCLUDE_PATHSPECS = ()`）→ `BadTests` False → **True**，確認排除 pathspec 是原因。
- i3 否定在後：`「\`FooBarTests\` 這個類別不存在」` → skip **True**（R2 時誤判成引用）；`「本 feature 沒有獨立 IPadTests 類別」`（否定在前）仍 True；`「回歸 RealMissingTests 全綠」`（無否定）→ **False**，沒有把一般敘述一起放行。
- m1 行級：`- 條件 1：mutation 樣本 \`BogusTests\` 改判過` / `- 條件 2：回歸 \`PlainFakeTests\` 全綠` 兩行 → `BogusTests` skip=True、`PlainFakeTests` skip=**False**，跨行不再互相污染。

**⑤ 兩個真實樣本（逐字原文，我自己抓的檔）✓** — LS-191 `c541cd06` → exit 1，唯一紅 `✗ 第 11 行起的列項缺『怎麼驗』證據`（項 3），與票文驗收條件相符、與 R2 結果一致；LS-192 `88fb24bc` → exit 0，`✓ 第 12/16/20/24/28/34/40 行起的列項有證據`。本輪的錨定收緊（i1）沒有把 LS-191 的 `IPadTests` 變成誤紅——它改由 glob＋否定兩條跳過接住，結果不變。

**⑥ 自測／gate ✓（我實跑）** — `handoff-evidence-check.test.sh` → `✓ …（47 組樣本）`，且 `grep -c '^✓'` = 48（47 測試＋1 總結），i5 的動態計數與實跑一致，不再手數漂移；`agent-tools-check.test.sh` 112 組全綠；`ax-launch-check.test.sh` 13 條全綠；`bash scripts/gates/agent-tools-check.sh` → `✓ agent-tools gate 通過（6 份 agent 定義；正文必含字樣 35 條）` exit 0。i4 的兩份定義文字我逐字看過，存在性三條規則＋三種跳過＋擴充後的路徑／指令清單都同步了，與程式一致。

**⑦ m2／i6／N4 ✓** — m2 已入 LS-96 `3002fb63`，我讀了原文：標的正確改成 `UploadQueueSheetUITests.swift:36`＋殘留 app 狀態，並記了「flake 歸因須以失敗訊息原文逐字比對測試檔」這條教訓，與我 R2 的認定一致。i6 未另貼 comment、改以 `19d0cd68`／`f720c228`＋LS-190 `a10f767` 作引用鏈——可接受（本來就只是追溯依據）。R1 N4 我已在 R2 撤回，本輪確認實作者維持防禦性正規化＋誠實揭露的處置，正確。

**⑧ 申報核對** — 3 個 commit ↔ 檔案對照與 handoff／PR body 一致；PR body 已加 `## R3` 段、對 M1／m1–m4／i1–i6／N4 撤回逐條處置無一遺漏；live body 過 `pr-body-check.sh --branch … --verify` exit 0（21 條「已修」SHA、7 條 LS-96 id）。四維度：無並發程式碼、無效能路徑、無平行化面；scope 全程無夾帶。

---

## Informational（皆不擋，供下一張碰這支 gate 的票斟酌）

- **i-a**（新，R3 帶入）`PATH_RE` 加了 `.md`／`.sh` 之後，「空泛敘述＋隨手提一個文件名」會通過：我實跑 `- 條件 1：肉眼確認沒問題，細節見 README.md` → **✓ 有證據**。R1 時代這個形狀（「肉眼看起來沒問題」）正是自測的標準負樣本 ②a。m3 修得對，但代價是證據門檻在 harness 語境下明顯變低——建議把這條寫進 `handoff_evidence_check.py` 檔頭「已知限制」（現在只寫了「只是子字串比對、不驗證路徑存在」，沒點出這個後果）。
- **i-b** 否定詞是固定字表（沒有／無／不存在／未），同子句內出現就整條跳過：`- 條件 2：\`SomeFakeTests\` 全綠（其他未跑）`、`- 條件 3：\`OtherFakeTests\` 全綠 無異常` 兩個**假名**實測都被放行（同句無標點邊界）；反過來，換個講法的否定仍會誤紅——見下面我自己 verdict 的第 6 行（我寫「不再算存在」，字表只認「不存在」）。兩個方向都在，屬啟發式的必然取捨，記錄即可。
- **i-c** 本票 R3 handoff 自己 dogfood 是 0 驗證／6 跳過（19 個列項全綠）——因為它的「已驗證」段清一色是 mutation 三段式、候選都落在含 mutation 關鍵字的同一行。這是行級規則的預期形狀，不是缺陷；但也說明「純 mutation 型 handoff」仍不會被存在性檢查覆蓋，真正的防線還是 merge-reviewer 自己重放（LS-209）。
- **i-d** `test_name_exists` 每個 grep 落空的候選都會重新跑一次完整 `git ls-files`（`_git_ls_files` 未快取），一份含多個假名的文件會重複全表列舉。目前規模無感，順手可加 memo。

---

## 我自己這份 verdict 跑 gate 的輸出（依新規則附上；紅的逐條說明，不改寫敘述）

```
✗ 第 5 行起的列項缺『怎麼驗』證據（…）
✗ 第 6 行引用的測試名 `BadTests` 在 repo 內找不到（…）
✗ 第 6 行引用的測試名 `IPadTests` 在 repo 內找不到（…）
✗ 第 6 行引用的測試名 `PadTests` 在 repo 內找不到（…）
✓ 第 3 行起的列項有證據
✓ 第 4 行起的列項有證據
✓ 第 7 行起的列項有證據
✓ 第 8 行起的列項有證據
✓ 第 9 行起的列項有證據
EXIT=1
```

- **第 5 行**（③ m3／m4）：真的沒引檔案或命令，只寫了結論——**gate 是對的**，不是誤判。
- **第 6 行 ×3**（④ i1／i2／i3 反例）：**誤判**，而且是很有代表性的一種——這一項的內容正是「`IPadTests`／`PadTests`／`BadTests` **不再算存在**」，我在陳述它們不存在，gate 卻判我引用了不存在的測試名。i3 的否定字表只認「沒有／無／不存在／未」，我寫的是「不再算存在」，字面差一個字就漏接（見 i-b）。依新規則我在此逐條說明、不改寫這句正確敘述。
- 第 3／4／7／8／9 行 ✓：圈號粗體列項現在解析得到（m4 修好，R2 時整份 exit 2），引用 `.py` 路徑也算證據（m3 修好）。

---

## 併主後待辦（orchestrator）

1. `gh pr merge 335`（hotfix → main）後補一支 back-merge `main` → `development`（`hotfix/*` 流程規定）。
2. 主 checkout `git pull --ff-only origin main`——**這是必要步驟**：三支 agent 定義（`ios-dev`／`qa`／`merge-reviewer`）與 `agent-tools-check` 釘句都在本 PR，subagent 定義讀自主 checkout，沒 pull 之前新規則對新派的 agent 不生效。
3. `scripts/gates/ax-launch-check.sh` 的 allowlist（`LittleSproutUITests/TapTargetMeasurement.swift`）待 **LS-190（PR #331）併主後移除**——移除前先 `git grep -q 'launchEnvironment\[.*UIPreferredContentSizeCategoryName' LittleSproutUITests/TapTargetMeasurement.swift` 確認已無命中（腳本檔頭、§7、PR body 三處都記了）。
4. qa／merge-reviewer 的新規則（逐項對應編號＋跑 gate 附輸出、紅則逐條說明）**自下一次派工生效**；派工 prompt 可順帶帶一句，因為 general-purpose 子 agent 讀不到定義檔。
5. 選擇性：i-a／i-b／i-c／i-d 與本輪 §7 未涵蓋的「已知限制」補充，若要追蹤請入 LS-96；m2 已在 `3002fb63`。

已貼 commit status `merge-review = success` 到 `ac8d7cf`。


### 2026-09-06T05:33:29.012Z · Chih-Lin Yeh · `3bec46bb`

**orchestrator 09-06 13:33**：merge-review R3 APPROVE（26ce0bf8，status 貼 ac8d7cf；CI 五項綠）。收尾鏈啟動：merge #335 → main tip 貼 `promote: no content diff` → 主 checkout `git pull`（qa／merge-reviewer／ios-dev 新硬規則自下一次派工生效；general-purpose 子 agent 派工 prompt 需自帶一句）→ back-merge PR main→development → dev tip 貼 status；之後 dev tip CI 綠 → promote dev→test → harness delta QA → sweeper → Done。informational 四條（PATH_RE 對 `.md`／`.sh` 的「肉眼確認」漏洞、否定字表雙向偏、純 mutation 型 handoff 不受覆蓋、`_git_ls_files` 未快取）→ 記 LS-96（下一則）。待辦：LS-190 併主後移除 `ax-launch-check.sh` 的 `TapTargetMeasurement.swift` allowlist。

### 2026-09-06T06:31:42.399Z · Chih-Lin Yeh · `2665a82d`

**orchestrator 09-06 14:30**：development ba81f8f（含本票 back-merge #338）已晉升 test → 票進 QA。QA 派工待本機模擬器 Keychain 環境冒煙結果（LS-190 QA BLOCKED 同因）——本票為純 harness delta，可先以 harness delta QA 驗（自測全綠、`handoff-evidence-check` 對真實樣本、`ax-launch-check` 對現況、pen-status `--path`、patrol 現況、13 個 import 移除後 build 綠），與 LS-212／LS-214／LS-190 合併排在同一次 QA 派工。

### 2026-09-06T07:10:50.845Z · Chih-Lin Yeh · `3b6d50c1`

**QA delta 09-06 15:09 — PASS**（harness delta，head `ba81f8fceff0a3151709e14f1e9723525308b74c`＝origin/test；qa-test worktree `git checkout --detach ba81f8f` 驗證）

## 逐條驗收（對應票文驗收條件；LS-214 併同一輪見下方 LS-214 comment）

1. **自測全綠**：`handoff-evidence-check.test.sh` exit 0（`✓ handoff-evidence-check 自測通過（47 組樣本）`）；`agent-tools-check.test.sh` exit 0（`✓ agent-tools-check 自測通過（112 組樣本）`）；`ax-launch-check.test.sh` exit 0（13 組樣本＋mutation）；`pr-body-check.test.sh` exit 0（`✓ pr-body-check 自測通過（106 組樣本）`）；`pick-ipad-runtime.test.sh` exit 0；`patrol.test.sh` exit 0（303 條 `✓`）；`push-gate.test.sh` exit 0（`✓ push-gate 模擬器自測通過`）。`bash scripts/gates/agent-tools-check.sh .claude/agents` 對真實 `.claude/agents/` → `✓ agent-tools gate 通過（6 份 agent 定義；正文必含字樣 35 條）` exit 0。
2. **真實樣本**：用 `list_comments` 逐字抓 LS-191 `c541cd06`、LS-192 `88fb24bc` 存檔後跑 `handoff-evidence-check.sh`：`c541cd06` → exit 1，唯一紅「第 11 行起的列項缺『怎麼驗』證據」（票文預期的項 3）；`88fb24bc` → exit 0（第 12/16/20/24/28/34/40 行皆 ✓）。與 merge-review R3（`26ce0bf8`）重跑結果一致。
3. **ax-launch-check 對 ba81f8f 現況**：`bash scripts/gates/ax-launch-check.sh` exit 0，「（允許期限內：…TapTargetMeasurement.swift:22:…LS-190 併主前已知，不算違規…）」＋「✓ ax-launch-check 通過（0 項違規、1 項在允許期限內）」——只有 allowlist 那一項在允許期限內、其餘 0 命中。`grep -rn 'launchEnvironment\["UIPreferredContentSizeCategoryName' LittleSproutUITests` 只 1 筆，是 `TapTargetMeasurement.swift:22` 的 `///` 文件註解、非實際 subscript。
4. **pen-status／patrol**：`bash scripts/ops/pen-status.sh --path` → `/Users/clyeh/little-sprout/.claude/worktrees/LS-192/design/littlesprout.pen`；主 checkout `patrol.sh --brief`（唯讀）印「⚠ Pen 開錯檔（實作票 LS-192）」與「[Pen] 開錯檔（實作票 LS-192；lane=lane:ui…）」，符合預期。
5. **pr-body-check 無 key**：自製樣本（`Ticket: LS-211 …` ＋ `## 池項\n- informational 入池 LS-96 \`a1b2c3d4\``）在 `env -u LINEAR_API_KEY` 下跑 `--verify` → 印 `⚠ 未反查（無 LINEAR_API_KEY）：第 4 行候選 a1b2c3d4`，exit 0。
6. **13 個 import 刪除後**：`xcodebuild build-for-testing -scheme LittleSprout -destination 'generic/platform=iOS Simulator'` → `** TEST BUILD SUCCEEDED **`、0 個 `error:`；`swiftlint lint --strict` → `Found 0 violations, 0 serious in 312 files.` exit 0。逐檔核對 6 個 `LittleSprout/` 檔＋7 個 `LittleSproutTests/`（非 Support）檔的 diff，全部只刪 import 行，無邏輯變更。
7. **COLLABORATION.md §7 抽兩處**：(a) :323「7 條 BODY_RULES」，`sed -n '/LS170-BODY-RULES-START/,/END/p' scripts/gates/agent-tools-check.sh | grep -c LS-211` = 7，相符；(b) :327「正負樣本 13 組」，`ax-launch-check.test.sh` 實跑扣總結行共 13 條 `✓`，相符。

## 回歸／scope（C，跨 LS-211＋LS-214）
`git diff c87e7b3..ba81f8f --stat` 共 44 檔（+2413/−210）：`.claude/agents/`／`.github/workflows/ci.yml`／`docs/COLLABORATION.md`／`scripts/gates/`／`scripts/ops/` harness 檔、`project.pbxproj`（僅新增 `AsyncGate.swift` 註冊 4 行、只掛 `LittleSproutTests` target）、6 個 `LittleSprout/` app 檔＋7 個 `LittleSproutTests/`（非 Support）檔皆純 import 刪除（逐檔看過 diff）、`TimelineStoreTests.swift`／`EULAStoreTests.swift`＋新檔 `Support/AsyncGate.swift`（LS-214 範圍）。**無 migration、無 `.pen` 異動、無 app 邏輯變更**（import 刪除除外）。

## 未完成
無——本次派工範圍（LS-211 harness delta）全部驗證。LS-190／LS-212 app 內容真後端項因模擬器 Keychain -34018 環境問題不在本輪範圍，非本票未完成項。

## 證據
`/private/tmp/claude-501/-Users-clyeh-little-sprout/e6972486-fec6-43c0-b8f4-28b43b01a5a0/scratchpad/LS-211-qa-*.log`（7 份自測＋build／swiftlint log）、`LS-191-c541cd06.md`／`LS-192-88fb24bc.md`（真實樣本原文）、`LS-211-fake-pr-body.md`（自製池項樣本）、`LS-211-214-qa-handoff.md`（完整 handoff）。

`handoff-evidence-check.sh` 對本份 handoff 全文自跑：10 條列項全數 `✓ 第 n 行起的列項有證據` exit 0。

模擬器已關：main-iPhone17Pro（A73356D3-1252-4B19-9480-838912C08F67，qa-test worktree 為 detached HEAD 無票號可抽、`detect-simulator.sh` 落回 `main` 命名，非本票專屬機；已確認 Shutdown）。lock 已釋放：未持有（本次不碰容器）。qa-e2e 證據：未跑（純 harness／測試檔 delta，無需登入或多步驟操作）。

### 2026-09-06T07:12:33.062Z · Chih-Lin Yeh · `79614a22`

**orchestrator 09-06 15:1x**：delta QA PASS（3b6d50c1；七項全 ✓：七支自測綠、真實樣本 c541cd06 只紅項 3／88fb24bc 綠、`ax-launch-check` 對現況只 allowlist 一檔、pen-status／patrol 現況、pr-body-check ⚠、13 import 刪除後 build＋lint 綠、§7 抽查一致；qa status 貼 ba81f8f，描述明示只代表 harness／測試 delta）。**test→main 暫不晉升**：同一 tip 含 LS-190／LS-212 的 app 內容，其真後端 QA 因主機螢幕鎖定（模擬器 Keychain -34018）尚未完成——解鎖後補驗再一起打版。sweeper 已派；Done 隨同批打版。

### 2026-09-06T07:20:47.549Z · Chih-Lin Yeh · `178fa5c7`

## dead-code sweep — LS-211

**時間**：09-06 15:19。範圍：`git diff 92c891a..d89f5a6 --stat`（40 檔 +2347/−159）。方法：唯讀——`git show d89f5a6:<path>` 逐檔核對＋主 checkout `git grep`/`git log`；未碰任何模擬器、Pen、worktree，未修改任何檔案。

### Findings（本 feature 引入）

**F1（minor，文件殘留／R1→R3 未同步）** `scripts/gates/handoff-evidence-check.sh` 的 `--help` heredoc（`--help|-h` 分支內）仍列 R1 原始的證據詞彙：
```
路徑（.png/.log/.test.sh/scratchpad//evidence//.swift）、或命令（xcodebuild／bash scripts/／gh run view／.xcresult）
```
但 `handoff_evidence_check.py:53-55` 的 `PATH_RE` 在 R3（merge-review R2 m3，commit `99bf785`）已擴充為 `.png|.log|.test.sh|scratchpad/|evidence/|.swift|.py|.sh|.md|.yml|.json`——`--help` 漏了後五種（`.py`/`.sh`/`.md`/`.yml`/`.json`，正是 harness 票 handoff 最常引用的原始碼／文件路徑類型）。
證據：`git show d89f5a6:scripts/gates/handoff-evidence-check.sh` 第 14-20 行 vs `git show d89f5a6:scripts/gates/handoff_evidence_check.py` 第 55 行 `PATH_RE` 定義；自測 `handoff-evidence-check.test.sh` ③a 只斷言 `exit 0` 與輸出含子字串「用法：」（`grep -qF -- '用法：'`），未驗證清單內容，故此漂移未被自測攔截（`grep -n -- "--help" scripts/gates/handoff-evidence-check.test.sh` → 只命中 1 處存在性斷言）。
建議處置：下一張碰這支 gate 的票順手同步 `--help` 文字（一行改動，無需重開票）；不影響任何判定邏輯，純文件失準。

**F2（minor，三份幾乎同義的規則未同步演進）** `.claude/agents/ios-dev.md:28` 的「handoff『已驗證』逐項對應派工單／票文編號」規則自 R1（commit `76a8019`）加入後，R2／R3 兩輪都只同步了 `qa.md`／`merge-reviewer.md`（R3 i4，commit `ac8d7cf`），`ios-dev.md` 從未再被觸碰（`git diff 92c891a..d89f5a6 --stat` 顯示 `ios-dev.md` 全程只 `+1`，`qa.md` `+3`、`merge-reviewer.md` `+2`）。結果：
- ios-dev.md 列的證據路徑仍是「`.png`／`.log`／`scratchpad/`／`evidence/`」——連 R1 當時已存在的 `.swift` 都沒列到（`git show 76a8019:scripts/gates/handoff_evidence_check.py | grep 'PATH_RE ='` 顯示 R1 起 `PATH_RE` 就含 `\.swift\b`），更不用說 R3 新增的 `.py`/`.sh`/`.md`/`.yml`/`.json`。
- ios-dev.md 仍寫「不得引用不存在的測試名」（絕對句），沒提到 R2/R3 新增的三種候選層級跳過（glob 形狀／同句否定詞／同行 mutation 語境）——與 `qa.md:43`／`merge-reviewer.md:25` 現行文字（已列出三種跳過）不一致。
- ios-dev.md 措辭是「貼出前可先跑…自我檢查**是否綠**」（選項語氣＋以綠為目標），沒有 `qa.md`/`merge-reviewer.md` 在 R2（merge-review R1 F3）之後改成的「紅則逐條說明是誤判或補證據，不得為了討好工具改寫正確敘述」但書——這正是 R1 F3 點名要防的「逼 agent 改寫正確敘述去討好工具」風險，只在兩份定義補了防呆，ios-dev 這份還留在舊風險狀態。
證據：`git log --oneline 92c891a..d89f5a6 -- .claude/agents/ios-dev.md` 只有 1 個 commit（`76a8019`）；`git show 76a8019:scripts/gates/handoff_evidence_check.py | grep -n 'PATH_RE ='` → `\.png|\.log|scratchpad/|evidence/|\.swift\b`；`git show d89f5a6:.claude/agents/qa.md` 第 43 行／`merge-reviewer.md` 第 25 行已同步新詞彙與跳過條款、`ios-dev.md` 第 28 行未同步（三份逐字比對）。
建議處置：不擋（純文件語意落差，不影響任何 gate 判定，ios-dev 目前也不強制跑此 gate），但值得記入 LS-96／或下一張碰這三份定義的票一併同步三處措辭，避免 ios-dev 誤以為規則比實際更嚴或自我修飾敘述去討好工具。

### 逐項排查結果（全部無死碼，附方法）

- **handoff_evidence_check.py R1→R3 改法殘留**：14 個函式（`fail`／`is_heading_line`／`find_section`／`split_items`／`is_glob_candidate`／`is_negated`／`is_mutation_context`／`test_name_candidates`／`_git_ls_files`／`test_name_exists`／`has_evidence`／`resolve_repo`／`run`／`main`）逐一 `grep -c` 呼叫點，全部 ≥1 個呼叫（非僅定義）；`SECTION_KEYWORD_RE` 全檔只有 1 處定義（`已驗證|驗收|查實`，已是 R3 終態），無舊常數殘留；`is_mutation_context` 已是「同一行」判準（R3 m1 修正後），無 R2 區塊級舊邏輯殘留。
- **`RealSampleFixture/`**：`handoff-evidence-check.test.sh` 建的 6 個 fixture 檔（`LegalDocumentSheetUITests`／`LegalMarkdownDocumentTests`／`LegalDocumentSheetLayoutTests`／`SettingsViewIPadTests`／`TapTargetGateTests`／`SettingsViewTests`）與其內部 7 個 test method 名稱，逐一 `grep -c` 全檔，每個都在後段內嵌的 LS-192 真實樣本（`88fb24bc`）引用處再出現 ≥1 次（如 `SettingsViewIPadTests: 9` 次），無未被引用的孤兒 fixture。
- **`pick-ipad-runtime.sh` 呼叫者**：`ci.yml:855`（`ci-ipad` job 實際呼叫，接手舊版內嵌 `find_ipad_udid`，該內嵌函式已在同一 diff 完整移除、無殘留）＋`ci.yml:198`（自測呼叫）。有真呼叫者，非死碼。
- **`pen-status.sh --path` 呼叫者**：`patrol.sh:432`（`bash "$pssh2" --path`，"Pen 開錯檔"偵測段實際呼叫，取代舊版 sed 解析 `PENCIL_LINE` 字面格式）。`PATROL_PEN_PGREP`／`PATROL_PEN_OPEN_SH` 兩個舊假身變數已確認全 repo 0 命中（`git grep` 只在 `COLLABORATION.md` 的歷史敘述段落提及，非程式碼）。`PENCIL_LINE` 變數本身仍在用（`patrol.sh:768/776/801`，供「Pencil 連線」段的人類可讀輸出，與 `pen_path`／「Pen 開錯檔」是兩個獨立用途，非重複）。
- **`ax-launch-check.sh` allowlist／`--root` 正規化**：`ALLOWLIST` 只有單一項（`TapTargetMeasurement.swift`），`root=${root%/}` 是唯一正規化語句，無 R1/R2 舊分支殘留。額外核對：`LittleSproutUITests/TapTargetMeasurement.swift` 在**現在的 `main`（`d89f5a6`）**仍是 `launchEnvironment["UIPreferredContentSizeCategoryName"]`（`git show main:LittleSproutUITests/TapTargetMeasurement.swift` 第 14 行）——確認 allowlist 目前仍是必要的（LS-190 PR #331 尚未併入 main，`git merge-base --is-ancestor 07a204e main` → not ancestor），非過期殘留，票文與 comment 記載屬實。
- **`agent-tools-check.sh` 新 BODY_RULES**：7 條 LS-211 規則（`sed -n '/LS170-BODY-RULES-START/,/END/p' | grep LS-211`）逐條用 `grep -c -F` 比對對應 agent 檔（`ios-dev.md`／`qa.md`×3／`merge-reviewer.md`×3）正文，每條精確命中 1 次，無失配。
- **`privacy_manifest_check.py` 枚舉常數**：`VALID_API_CATEGORIES`／`VALID_COLLECTED_DATA_TYPES`／`VALID_COLLECTED_DATA_PURPOSES` 三個集合皆在 `cmd_manifest` 內被讀取比對，無未用集合。
- **13 個 import 刪除**：逐檔 `git diff` 核對，全部是單行 `import` 刪除（含 `AppError.swift`／`AuthRoute.swift`／`EmailFormat.swift`／`JoinCodePhase.swift`／`SettingsModels.swift`／`AppSection.swift` 與 7 個 Tests 檔），無殘留空白區塊、無 `// swiftlint` 殘留註解。
- **`docs/COLLABORATION.md` §7 新列**：4 列（handoff-evidence-check／pr-body-check ⚠／privacy-manifest 枚舉／ax-launch-check）逐一核對其量化申報（「7 條 BODY_RULES」「自測 47 組」「106 組」「13 組」「②b 7 組＋⑥f-i 4 支 mutation」）與程式碼、測試檔 `grep -c` 實跑結果一致，無錯述。
- **殘留 `LS-211-*` 暫存檔**：`find /private/tmp -maxdepth 1 -iname "*LS-211*"` 與 `find /tmp -maxdepth 1 -iname "*LS*"` 皆空——所有 `LS-211-*` 檔案都在本 session scratchpad 內（`/private/tmp/claude-501/.../scratchpad/LS-211-*`），依票文排除範圍不算殘留。`.claude/worktrees/LS-211` worktree 仍存在（tip `ac8d7cf`，票仍在 QA 狀態）——這是「收尾儀式」③清理步驟（`cleanup-merged.sh --apply LS-211`）尚未執行的正常狀態，非本次巡檢範圍的死碼。

### 既有死碼觀察
無新增觀察——本次巡檢聚焦 LS-211 引入的檔案，未在範圍外發現其他既有死碼（若要找既有死碼需另跑全庫巡檢，超出本票 diff 範圍）。

### 建議處置
- F1／F2：不擋、不需獨立開票——建議記入 LS-96（下一張碰 `handoff-evidence-check.sh` 或這三份 agent 定義的票順手同步），理由：純文件/help 文字落後於實作，無執行面風險。

### 盲區聲明
純文字 `grep`/`git show` 巡檢，抓不到：(1) reflection／字串拼接／執行期動態組裝的呼叫（如以變數組出的 script 路徑）；(2) CI YAML 之外、由外部系統（如 GitHub branch protection UI）設定的引用；(3) Xcode `project.pbxproj` target membership 是否真的編入 build（本次未變動 `.pbxproj`，不適用）；(4) 是否有其他分支／PR 平行引用了這些新腳本但尚未 merge——只查了 `main` 這一條歷史線。以上盲區皆未影響本次判定範圍內的結論。

### 2026-09-06T07:24:29.253Z · Chih-Lin Yeh · `57d583ad`

**收尾儀式（orchestrator 09-06 15:24）**
① dead-code-sweeper `178fa5c7`：無可執行死碼；F1（`--help` 詞彙漂移）／F2（ios-dev.md 證據段落未與 qa／merge-reviewer 同步）皆 informational → 入池 LS-96（15:24）。
② Lesson learning：(a) 同一條規則寫進三份 agent 定義時，R2／R3 修訂只同步到兩份——`agent-tools-check.sh` 目前只驗「每檔各含一句」，不驗三檔句子一致；已入池。(b) 本票 R1→R3 三輪主要耗在 handoff 範例字串判定（真樣本 fixture 才收斂）——之後 gate 類票開票就要求「內嵌 ≥3 份真 handoff 樣本」為 DoD，少一輪。(c) 三段式 mutation 規則落地後，LS-213／LS-215 handoff 已開始附實跑輸出，方向正確。
③ 清理：`cleanup-merged.sh --apply LS-211` 已執行——worktree 1／專屬模擬器 LS-211-iPhone17Pro 1／DerivedData 1 移除；本機分支已由 worktree 移除一併處理；遠端分支無殘留。
④ Pen 清場：不適用（harness 票）。
QA：test tip ba81f8f `qa` status success（harness／test delta），hotfix 已在 main d89f5a6。→ Done。

