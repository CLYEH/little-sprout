# LS-90 Task：LS-18 後端——邀請碼改 6 碼（32 字元表、30 bit；LS-89 裁決 A）：create_invite／驗證正則／測試／API.md

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | lane:backend |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M1 登入與家庭 |
| 估點 | — |
| 建立 | 2026-08-25T07:00:47.296Z |
| 開始 | 2026-08-25T07:00:47.429Z |
| 完成 | 2026-08-25T08:26:40.959Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-90/taskls-18-後端-邀請碼改-6-碼32-字yuan表30-bitls-89-裁決-acreate-invite驗證正則測試apimd |
| 父票 | LS-18 Story：建立家庭與邀請加入 |
| 關係 | related→LS-17, related→LS-82, related→LS-81, blocks→LS-18, related→LS-89, related→LS-46, related→LS-33, related→LS-53, related←LS-107, related←LS-67, related←LS-85, related←LS-30 |

## 描述

## 來源

[LS-89](https://linear.app/little-sprout-app/issue/LS-89/產品決策邀請碼長度-定案-6-碼ls-46vs-後端已上線-8-碼40-bitls-33衝突擇一) 使用者裁決（2026-08-25）：**後端改 6 碼**。定案（[LS-46](https://linear.app/little-sprout-app/issue/LS-46/設計定稿合併c-基準使用者指定調整-designlittlesproutpen)）：邀請碼英數 6 碼、3+3 分組、核准必開。現況（[LS-33](https://linear.app/little-sprout-app/issue/LS-33/taskls-18-後端-邀請加入申請與審核的-schemarpcrls)）：`create_invite` 產 8 碼、32 字元表（排除 0/O/1/I）、40 bit；`80_join_approval.sql` 釘死 `{8}`；正式站已部署。

## 範圍

1. 新 migration（`date -u +%Y%m%d%H%M%S` 取號，不改已併 main 的檔）：`create or replace function public.create_invite(...)`——長度 8→**6**，**字母表維持 32 字元（排除易混字）**，30 bit；`request_join(code)` 若有長度／格式檢查同步改 6；既有 8 碼 invites 資料處置：正式站無真實家庭資料，**既有未過期 invite 一律失效**（`update invites set expires_at = now()` 或標記）——寫進 migration 註解與 API.md。
2. 撞碼處理：6 碼碰撞機率上升，`create_invite` 的唯一性重試迴圈（若無則加，最多 N 次後 LS016）確認存在；併發測試覆蓋。
3. 測試：`80_join_approval.sql` 正則改 `{6}`；碼只含 32 字元表；撞碼重試；`request_join` 對 8 碼舊格式回拒（錯誤碼沿用格式錯誤碼）。mutation 自證。
4. `docs/API.md`：§ create_invite／request_join 契約（長度、字母表、熵、失效策略）；錯誤碼表不變則 error-codes gate 綠。
5. PR body `BREAKING:` 段（呼叫端可見契約變更；[LS-53](https://linear.app/little-sprout-app/issue/LS-53/harness破壞性偵測器盲區-alter-policy-false-與-revoke-未納入關鍵字語意判定) 分級）；push-gate `migration-breaking-check` 會要求 API.md 有動。

## 驗收

CI 四 job 綠；merge-reviewer；QA；併 main 後 orchestrator 依常設授權 `db push`＋唯讀對帳（`create_invite` 實跑一次回 6 碼）。

## 分支

feature 從 development。

## 留言（6）

### 2026-08-25T07:14:58.961Z · Chih-Lin Yeh · `6b986900`

驗收追加（LS-30 PR #136 R2-i4）：本票併入 main 後，`.claude/skills/little-sprout-brand/` 的 `motifs.md:68`／`SKILL.md:48`「後端 LS-90 對齊中（現行部署仍 8 碼）」敘述須一併刪除／改為現況（有到期日的敘述留著就成 slop）；同 PR 處理或 orchestrator 收尾時做。另：`h7EnT` R2「排除 Q」註記與 32 字元表（含 Q）相衝——以後端字母表為準（含 Q），板端註記由 LS-81 順修。

### 2026-08-25T07:32:35.509Z · Chih-Lin Yeh · `95dc6866`

## merge-reviewer：PR #139（`feature/LS-90-invite-code-6` → `development`）

範圍：`git diff f4f9ade...HEAD`（merge-base，8 檔／+333−16）。四維＋票面重點逐項實跑複驗，DB 操作全程經 `scripts/ops/supabase-lock.sh`（跑前 `--status`＝free）。

### Verdict：**APPROVE**（0 blocker／0 major；2 minor、2 informational）

---

### Findings

**F1（minor，已實測證實）`docs/API.md:164-166`、`supabase/migrations/20260825070627_invite_code_6.sql:19-20`、PR body「破壞性變更」第 2 點**
三處都寫「8 碼舊格式碼**查不到任何列**，回既有的 `LS010`」。這句話對「正式站既有 8 碼邀請」——也就是這支 migration 唯一針對的情境——是錯的：第 3 段刻意用 `UPDATE expires_at`（而非 DELETE，為了留稽核紀錄），列**還在**，`request_join` 的 `select … where i.code = v_code for no key update` 查得到，接著撞上 `expires_at <= now()` → **`LS011`（邀請碼已過期）**，不是 `LS010`。

失敗情境（我在本機重演，非推測）：`db reset` 後插一支 `code='ABCDEFGH'`、`expires_at = now()+7d` 的 invite → 執行 migration 第 3 段那一句 `update public.invites set expires_at = now() where expires_at > now();` → 該列 `is_expired=t`、`row_still_exists=1` → 以另一位 authenticated 呼叫 `request_join('ABCDEFGH')` → `SQLSTATE = LS011（邀請碼已過期）`。`LS010` 只在「這支碼從來不存在」時成立（＝測試 2b 實際打的那條路徑）。

行為本身沒問題（兩邊都是拒絕，且 `LS011` 的文案其實更貼近事實，`AppError` 兩碼都已有對應），所以只是 minor；但這是本 PR 新寫進契約文件的一句話，LS-18 UI 會照它決定文案。建議把三處改成：**既有（已被本 migration 失效）的 8 碼列 → `LS011`；從未存在過的碼 → `LS010`**。

**F2（minor）`supabase/tests/80_join_approval.sql:256-274`（新增的 2b「8 碼舊格式一律被拒」）**
這條斷言與同檔 2 段既有的 `request_join('0000-0000') → LS010` 是同一件事，`'ABCDEFGH'` 只是「一支從未被 insert 的碼」。把 `create_invite` 改回 8 碼，它仍然會綠（除非 `create_invite` 剛好抽中 `ABCDEFGH`）——即「業務邏輯改了測試不會紅」，沒有編碼它自稱要驗的意圖。建議：要嘛拿掉（既有 2 段已覆蓋「碼不存在→LS010」），要嘛改成真的有意義的版本——插一支 8 碼、未過期的 invite，跑一次 migration 第 3 段那句 UPDATE，再斷言 `request_join` 的錯誤碼；那個版本會同時把 F1 抓出來。

**F3（informational）`supabase/tests/concurrency/invite_create_race_*.sql`**
場景是真的兩個並行 psql（`run_sql_bg` + `wait`），但兩邊沒有任何時序對齊（無 `pg_sleep`／advisory lock），而 `create_invite` 之間也不互相持鎖，所以兩筆 INSERT 是否真的重疊只是機率問題；verify 的三條斷言（2 列、碼互異、`{6}` 格式）在 80_ 段單 session 下已全部覆蓋。也就是說：它幾乎不可能因為「併發特有的錯誤」而紅。這不是缺陷（不會 flaky、也確實驗到「兩條連線同時打這支 RPC 不會噴非預期錯誤或卡死」），只是覆蓋力有限——agent 在 handoff 與 1d 段註解對「無法用真隨機製造撞碼」的自承誠實且判斷正確，我同意不硬湊 flaky 撞碼測試。標為 informational，不要求動。

**F4（informational）** 本票 comment 追加的驗收（brand skill `motifs.md:68`／`SKILL.md:48` 的「後端 LS-90 對齊中——現行部署仍 8 碼／40 bit」）在本 PR 未處理——這是合理的：本 branch 從 #137 之前的 development 切出，`.claude/skills/` 不在 branch 內。留給 orchestrator 併 main 後收尾，仍是 Done 的前置條件。

---

### 已驗證（實跑，非閱讀）

1. **migration 正確性**：只新增 `20260825070627_invite_code_6.sql`，未動任何已併 main 的檔（`git diff --stat` 確認）。函式本體與 `20260823010000` 版逐行 diff：**只有 `c_random_bytes` 8→6 個 byte 與 `for k in 1..8`→`1..6` 兩處實質改動**，簽章／`security definer`／`set search_path = ''`／owner 檢查／LS017 邊界／5 次重試／LS016 全部原樣。
2. **取樣均勻性**（票面重點）：本機跑 30,000 次 `gen_random_uuid()`，byte 0-5 各自 `% 32` 皆 `distinct=32`、每格計數 min 863／max 1011（期望 937.5，σ≈30，落在 ±3σ 內）＝無取模偏差（256 = 8×32）。對照組：byte 6 只有 **16** 種值（版本 nibble）——確認 6 碼只取 byte 0-5 是對的，也確認 PR #36 那個坑不會回來。
3. **`create or replace` 的權限保留**：reset 後查 `pg_proc` → `proacl={postgres=X,service_role=X,authenticated=X}`、`prosecdef=t`、`proconfig={search_path=""}`；`has_function_privilege('anon', …)=f`。migration 註解「OR REPLACE 不需重新 grant/revoke」屬實。
4. **`update invites … where expires_at > now()` 的副作用**：`invites` 上沒有任何 trigger／rule，沒有 `FORCE ROW LEVEL SECURITY`（migration 以表 owner `postgres` 執行，不過 RLS），`invites_uses_within_max` 與 `char_length(code) between 6 and 64`（`init_schema.sql:86`，行號引用正確）都不受影響；實測該句對既有列成功執行。`where expires_at > now()` 不動已過期列，語意乾淨。部署當下若有 in-flight `request_join`（持 `for no key update`）只會互相短暫阻塞、不會死鎖（request_join 不再回頭要第二把 invites 鎖），阻塞解除後該申請拿到 `LS011`＝fail-closed，正確。
5. **mutation 自證（親自重演）**：`supabase db reset` + `run.sh` 全綠 → 用 psql 把 `create_invite` 就地改回 8 碼（`array[0,1,2,3,4,5,7,9]`＋`1..8`，**沒有動 repo 任何檔案**）→ `run.sh` rc=1，紅在 `80_join_approval.sql`：`ERROR: FAIL：邀請碼「GBXVFES5」不符字元集規約（6 碼、大寫、不含 0/O/1/I）` → 再 `db reset` 還原（本機 DB 已回到 branch migrations 的狀態，lock 已釋放）。
6. **gates**：`api-contract-check.sh` ✓（RPC 23／表 18 一致）、`error-codes-check.sh` ✓（三方 22 碼一致，未新增碼）、`migration-version-check.sh --target origin/development` ✓、`migration-breaking-check.sh --base <merge-base>` 命中且僅命中 `create or replace function public.create_invite(...)`、`breaking-section-check.sh < PR body` rc=0（`BREAKING:` 行首＋同行有內容）、`pr-body-check.sh` ✓。GitHub CI 四 job 全綠（ci／db／lint／rules）。`git merge-tree --write-tree origin/development HEAD`（現含 #137）rc=0＝可乾淨合併。
7. **client 端**：`SupabaseFamilyAPIClient.swift:71-111` 的 `createInvite` 回 `String`、`requestJoin(code:)` 直接透傳，全 repo `--include=*.swift` 搜不到任何長度／`{8}` 假設；`AppError` 的 LS010/011/012/016 對應不變。無需改動屬實。
8. **四維**：race——併發 create 走 UNIQUE＋subtransaction 重抽（結構未變），既有邀請失效與 in-flight join 的互動見第 4 點，無新競態；效能——重試迴圈 O(1)，無 RLS policy 改動、無新增 per-row 子查詢，一次性 UPDATE 是小表全掃；平行——重試本質必須序列，無被誤序列化的工作，DB 操作有走 lock；scope——8 檔全部落在票面 1-5 項內，沒有順手改動或無關重構。

### 未涵蓋

- 正式站（remote）資料：未連線、未 push，「無真實家庭在使用邀請碼」沿用票面認定，未獨立查證。orchestrator `db push` 後的唯讀對帳（實跑 `create_invite` 確認回 6 碼）仍必要——若 remote 真有未過期邀請，它們會被失效並在使用者端呈現為 `LS011`（見 F1）。
- 未跑 iOS 端測試（本 PR 無 Swift 變更；push-gate 的 Xcode job 由 CI `ci` 綠覆蓋）。


### 2026-08-25T07:42:29.256Z · Chih-Lin Yeh · `9aae446c`

R1 F1/F2 已修（commit `fix(db): LS-90 R1 F1 契約文字 LS011／F2 失效邀請測試`，已 push，push-gate 全綠）：

- **F1**：`docs/API.md:164-166`、`20260825070627_invite_code_6.sql` 第 2 段註解、PR body 破壞性第 2 點三處都改為「既有（已被本 migration UPDATE 標記失效）的 8 碼列 → `LS011`；從未存在過的碼 → `LS010`」。
- **F2**：`80_join_approval.sql` 2b 段改為插入未過期 8 碼邀請 → 執行 migration 那句失效 UPDATE（加 id 限定詞避免波及本檔其他段落仍在用的碼）→ 斷言 `request_join` 回 `LS011`；保留 `'ABCDEFGH'`（從未存在過）作為 `LS010` 對照組。Mutation 自證：拿掉那句限定範圍的 UPDATE，`80_join_approval.sql` 在 `LEGACY8C` 斷言處 FAIL（request_join 直接成功）；還原後全綠。
- **F3／F4**：如 review 所述，不修。F3 informational（併發場景無時序對齊，覆蓋力有限但非缺陷）維持原狀；F4（brand skill `motifs.md`／`SKILL.md` 的「後端 LS-90 對齊中」敘述）留給 orchestrator 併 main 後於 feature 收尾儀式一併處理，仍是 Done 前置條件。

已經 `scripts/ops/supabase-lock.sh -- supabase db reset` ＋ `bash supabase/tests/run.sh` 全綠複驗；PR #139 body 已同步更新（`pr-body-check.sh`／`breaking-section-check.sh` 皆綠）。不等 CI。

### 2026-08-25T08:05:43.508Z · Chih-Lin Yeh · `d1141923`

## QA：PASS（test branch，PR #142／#139 已併入）

環境：`.claude/worktrees/qa-test` detach 於 `origin/test`（03a2f40，含 `Merge pull request #139 from CLYEH/feature/LS-90-invite-code-6`）。全程經 `scripts/ops/supabase-lock.sh`，未動任何 repo 檔案（純 DB 內操作＋rollback／db reset 還原）。

### 逐條

**1. Migration 套用＋全測試綠** ✓
`bash scripts/ops/supabase-lock.sh -- supabase db reset` 乾淨套用全部 15 支 migration（含 `20260825070627_invite_code_6.sql`）。`bash supabase/tests/run.sh` 全綠，含 `80_join_approval.sql`（含 2b 段 `LEGACY8C`→LS011／`ABCDEFGH`→LS010 對照）與併發測試 `兩人同搶邀請碼最後一個名額`／`兩連線同時 create_invite`。

**2. 親自驗證（不依賴既有測試檔）** ✓
- Owner 身分（`a0000000-…-001`／family `fa0…001`）`select create_invite(...)` 連跑 3 次（另補跑 20 次做統計）：回傳 `NAVLG2`／`EKRP85`／`T385NH` 等，全部 6 碼、字元只落在 `23456789ABCDEFGHJKLMNPQRSTUVWXYZ`（regex 斷言 20/20 過）。
- `request_join('QA9990')`（不存在的 6 碼）→ `LS010`。
- 插入未過期 8 碼 invite（`QA8LEGCY`）→ 手動執行 migration 第 3 段那句 `update invites set expires_at = now() where expires_at > now() and id = ...` → `request_join('QA8LEGCY')` → `LS011`（列還在、只是過期，符合 R1 F1 訂正後的契約）。

**3. Mutation 自證** ✓
在 DB 內把 `create_invite` 就地 `create or replace` 回 8 碼版本（`c_random_bytes` 含 byte 7/9、`1..8` 迴圈；**未動 repo 任何檔案**，`git status --short` 全程乾淨）→ 單跑 `80_join_approval.sql`：`ERROR: FAIL：邀請碼「8YQ9JK72」不符字元集規約（6 碼、大寫、不含 0/O/1/I）`，rc=3，紅。`supabase db reset` 還原後 `run.sh` 重新全綠。

**4. Gates／文件** ✓
`api-contract-check.sh` ✓（RPC 23／表 18 一致）、`error-codes-check.sh` ✓（22 碼三方一致）。`docs/API.md`：§3 `invites`（150-169 行）與 §4 `create_invite`/`request_join`（498-524 行）皆已寫明 6 碼／字元集／30 bit 熵／既有未過期邀請一律失效的策略／`LS011`（曾存在、已被標記失效）vs `LS010`（從未存在）的區分，與 R1 F1 訂正一致。

**5. Client 端** ✓
`LittleSprout/Services/Family/*.swift`（`FamilyModels.swift`／`SupabaseFamilyAPIClient.swift`／`FamilyAPIClient.swift`）grep `{8}`／`count == 8` 均無命中；`AppError.swift` 的 `inviteCode*` 錯誤碼對應（LS010/011/012/016）未變、不含長度假設。

**6. 雲端／UI** ⊘（如票面預期，非缺陷）
`mcp__supabase__list_migrations`（唯讀）確認雲端基線**尚未**含 `20260825070627`（最新為 `20260825040000_deletion_attribution`）——雲端面 **BLOCKED**，待 orchestrator 併 main 後依常設授權 `db push`＋唯讀對帳（`create_invite` 實跑一次回 6 碼）。本票無 UI 變更，模擬器視覺驗收 **N/A**。

### 附帶確認
PR #139 四個 CI job（rules／db／lint／ci）皆 `SUCCESS`，`mergedAt` 已確認；merge-reviewer 已 APPROVE（0 blocker／0 major）。R1 comment 中 F1／F2 已修並經上述第 2、3 點獨立複驗；F3（併發測試覆蓋力 informational）、F4（brand skill 敘述留待收尾儀式）維持原裁決不變，非本輪 QA 範圍。

### 裁決：**PASS**（DB／契約／文件／client 端）；雲端部署與模擬器視覺為預期中的 BLOCKED／N/A，非阻塞項，待 orchestrator 後續步驟處理。


### 2026-08-25T08:24:14.119Z · Chih-Lin Yeh · `c5c482ff`

## 雲端部署對帳（orchestrator，2026-08-25 18:00，常設授權）

- 首次 `promote.sh` FF 晉升後 main `8830621` 含 `20260825070627_invite_code_6.sql` → 從已 link 的 qa-test worktree `supabase db push`：dry-run 只列該支 → 實推 Applying 成功。
- 唯讀 MCP 對帳：`list_migrations` 尾端 `…040000, 070627`（14 支）；`pg_get_functiondef(create_invite)` 含 `1..6`、不含 `1..8`；`invites` 未過期列數＝**0**（既有邀請失效語意生效；正式站原本就無有效邀請）。
- QA 的「雲端面 BLOCKED」解除。餘下：sweeper（跑中）＋retro → Done；skill 過期敘述（motifs.md:68／SKILL.md:48／tokens.md:82）由 orchestrator 以 hotfix 順修。

### 2026-08-25T08:26:36.938Z · Chih-Lin Yeh · `650b9542`

## 收尾儀式（orchestrator，2026-08-25）

**① dead-code-sweeper**：本 PR 範圍零死碼（migration 單一 UPDATE、`{8}` 零殘留、race 四檔已註冊、API.md 三處一致、Swift 無 8 碼假設、QA 暫存未入 repo）。兩個既有過期敘述（skill `SKILL.md:48`／`motifs.md:67-68`「後端 LS-90 對齊中」、`tokens.md:82`「fs-code 待補」）→ orchestrator 以 `hotfix/LS-90-skill-cleanup` 順修（本 comment 之後）。

**② Lesson learning review**
- **Gate**：無漏接；`migration-breaking-check` 正確命中 BREAKING、`breaking-section-check` 驗 PR body。reviewer F1 抓到契約文件寫錯錯誤碼（LS010 vs LS011）——教訓：**契約文件裡的錯誤碼陳述要有測試背書**（F2 把它變成測試），已在票內落實。
- **Review 1 輪 APPROVE＋2 minor**；QA 一次 PASS（含 mutation 紅綠）。
- **流程首例**：本票是第一張走 `promote.sh` FF 晉升（零 PR）＋常設授權直接部署的票，從 QA PASS 到正式站 <30 分鐘。
- **產品面**：熵 40→30 bit 由 LS-89 裁決承擔；正式站 `invites` 無有效列，失效語意無實際影響。
- **派工／model**：恰當；agent 對「無法製造真隨機碰撞」誠實聲明，reviewer 接受。

**③ 部署**：已部署＋對帳（前一則 comment）。→ Done。LS-18 的 blockedBy 少一項（剩 LS-17／LS-82）。

