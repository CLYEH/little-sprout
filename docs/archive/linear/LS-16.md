# LS-16 Harness：feature 收尾儀式——dead-code-sweeper agent＋lesson learning review

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | — |
| Cycle | — |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-22T08:45:26.248Z |
| 開始 | 2026-08-22T08:45:26.358Z |
| 完成 | 2026-08-22T09:07:59.696Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | Chih-Lin Yeh |
| URL | https://linear.app/little-sprout-app/issue/LS-16/harnessfeature-收尾儀式-dead-code-sweeper-agentlesson-learning-review |
| 關係 | related→LS-9, related→LS-12, related→LS-10, related→LS-8, related→LS-13, related→LS-7, related→LS-15, related→LS-11, related→LS-6, related→LS-5 |

## 描述

使用者新增的協作規則：每個 feature 結束時 (1) 派專門 agent 巡 dead code；(2) orchestrator 做 lesson learning review（harness 改善、設定優化、工具缺口盤點）。

## Scope

* 新 agent `.claude/agents/dead-code-sweeper.md`（sonnet，唯讀工具，只報告不刪改；限「本 feature 引入」的死碼，既有死碼另列觀察）
* 規約：COLLABORATION §1 角色＋model 表、§4 新增「收尾 gate」、§5 狀態機 QA→Done 前置條件、§6 收尾儀式細則（feature 粒度由 orchestrator 判斷；harness 票免 dead-code、retro 照做）、§7 對照表補列（人工 gate，狀態機承載）
* CLAUDE.md 補一條 session 必讀摘要

## 驗收條件

- [ ] agent 定義與規約文件併入 main 並 back-merge
- [ ] 首次實跑：Phase 0 批次（[LS-5](https://linear.app/little-sprout-app/issue/LS-5/phase-0-1建立-xcode-專案骨架swiftui-ios-17-swiftlint)/6/7）QA 過後執行 dead-code 巡檢＋retro，記錄於各票 comment

依 §2 例外走 hotfix 流程。

## 留言（2）

### 2026-08-22T08:58:03.247Z · Chih-Lin Yeh · `ace802cd`

**Lesson Learning Review — Phase 0 批次（LS-5/6/7＋harness LS-9/13）**（收尾 gate 首次實跑）

## Gate 攔截／漏接記錄
**真實攔截**（gate 證明有效）：commit gate 擋 main 直接 commit（bootstrap 紅燈驗證）；commit-msg gate 擋壞格式；secrets scan 在 LS-6 攔到 run.sh 的 postgres 連線字串（agent 依規約改寫，未繞過）；XcodeGen 漂移 gate 在 LS-7 紅燈操作中攔到「漏同步 pbxproj」（首次實戰攔截，非演習）；CI 紅綠驗證證明測試失敗真的會紅。
**Gate 自身的 bug 被 review 抓到**：secrets pattern 誤判文件敘述（→素材式 pattern）；純刪除 commit 靜默炸 gate（B1，違反 fail loud）；design gate 誤擋 promote PR（→LS-10.6）；hooks 隨分支走＝三個 gate 對舊分支不生效（→CI 兜底層，M1）。
**證據紀律教訓**：我引用「CI 時長」推斷 gate 有跑被 reviewer 打回——此後 gate 證據一律 job step 層級，不用綠燈或時長推斷。

## Review 輪數與返工
LS-9 兩輪（6 major＋複驗 B1）、LS-5 兩輪（1 major→LS-13＋3 minor）、LS-6 兩輪（2 blocker＋4 major；修復輪反向糾正 reviewer 處方兩處，複驗又抓到新死鎖邊）、LS-7 一輪零 finding。共通返工根因：**安全結論靠註解宣稱、測試只蓋單一路徑**（INSERT/UPDATE 面、單/雙 session）——已內化為「修復必附同方向測試＋mutation test」慣例，LS-6 修復輪執行後複驗一次過。

## Model 政策評估
opus reviewer 的實測型 review（自建環境重現、mutation test）是本批最高價值環節——B1 併發磚化這類問題非輕量 review 可穩定抓到，成本值得。sonnet 用於處方式修復與小 diff review 表現足夠。qa sonnet 自我糾錯（點擊座標誤中相鄰鈕）展現了可靠性。派工時註明「已申報風險請覆核勿重複發現」有效降低 review 噪音。**政策維持現行，無調整。**

## 工具與環境缺口
- **Docker 拉映像卡死（proxy）**：supabase start 正式路徑三輪都 BLOCKED，靠 PG16 stub 備援。修復需重啟 Docker Desktop（會動使用者 projectx 容器）——**待使用者決定**。
- mobile-mcp WDA 在 iPad：前次全數逾時、本次正常——不穩定，觀察中，QA 規約的備援路徑（simctl）保留。
- push gate 全量 xcodebuild test 未來會隨測試成長變慢——觀察，暫不動。

## 改善項票據化（全數已開票，無口頭遺留）
LS-10（gate 強化 6 項）、LS-11（CI 真跑 RLS 測試——**harness 債最高優先**，reviewer 兩輪都點名）、LS-15（DB 硬化：rls_auto_enable＋default privileges 缺口）。衍生票形成率 6 張／5 票——問題被票據化而非流失，健康。

## 待使用者決策
LS-12（Swift 6 語言模式，建議採納）、LS-8（Apple Developer）、Docker Desktop 重啟時機。

### 2026-08-22T09:08:02.951Z · Chih-Lin Yeh · `235fde14`

**Handoff（orchestrator）**：規約與 agent 併入 main（PR #11，sonnet reviewer APPROVE）、back-merge test（#12）／development（#13）完成。**首次實跑完成**：Phase 0 批次的 dead-code 巡檢（零 finding，報告見 LS-5/6/7 comment）＋retro（本票上一則 comment）皆於 release（v0.1.0）前執行並歸檔。reviewer 兩個 minor 觀察（指路精確度、harness 票免巡檢的政策縫隙）記錄備查，暫不動作。

