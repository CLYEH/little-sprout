# LS-145 Phase 2-2：PrivacyInfo.xcprivacy（required-reason API）＋Info.plist 用途字串（相簿／相機／推播）＋出口合規旗標 ITSAppUsesNonExemptEncryption=false

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | lane:ui |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M7 TestFlight |
| 估點 | — |
| 建立 | 2026-09-03T08:34:53.496Z |
| 開始 | 2026-09-03T12:07:46.191Z |
| 完成 | 2026-09-04T04:32:05.169Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-145/phase-2-2privacyinfoxcprivacyrequired-reason-apiinfoplist |
| 關係 | related→LS-156, related→LS-70, related→LS-151, related→LS-153, related→LS-146, related→LS-154, related→LS-112, related→LS-49, related→LS-8, related→LS-22, related←LS-159, related←LS-144, related←LS-96, related←LS-132 |

## 描述

PLAN §6 Phase 2-2、§9-B。TestFlight／送審前置，非畫面票（不需 Design gate）。依賴 [LS-8](https://linear.app/little-sprout-app/issue/LS-8/phase-0-4註冊-apple-developer-program個人佔-bundle-id-與-app-名稱)（Apple Developer）以做 Xcode 上傳驗證。

## 範圍

1. `PrivacyInfo.xcprivacy`：列 required-reason API（UserDefaults、file timestamp、system boot time 等，依實際用到的 API 與 SPM 依賴——Supabase SDK 是否自帶 manifest 需查）；資料收集類型（照片／影片、聯絡資訊 email、使用者內容），標明家長自願上傳、僅私密家庭可見。
2. Info.plist：`NSPhotoLibraryUsageDescription`、`NSCameraUsageDescription`（若有拍照入口）、推播（若 [LS-22](https://linear.app/little-sprout-app/issue/LS-22/story留言愛心與彙總推播) 已落地）——文案具體（PLAN §9-B），繁中＋英文。
3. `ITSAppUsesNonExemptEncryption = false`（僅 HTTPS）。
4. 機械 gate：CI `rules` 加檢查——manifest 存在、三個 usage string 非空且非模板字、旗標存在（自測正負樣本）。

## 驗收

- [ ] `xcodebuild -exportArchive`／Xcode Organizer 上傳無 privacy 警告（[LS-8](https://linear.app/little-sprout-app/issue/LS-8/phase-0-4註冊-apple-developer-program個人佔-bundle-id-與-app-名稱) 到位後實測；之前以 `xcodebuild archive` 本機驗）
- [ ] gate 自測綠、merge-reviewer 通過

## 留言（11）

### 2026-09-03T12:08:27.041Z · Chih-Lin Yeh · `99426a38`

orchestrator 2026-09-03 20:08：巡檢動作清單——ui lane 在飛 0、本票為非畫面票（LS-144 起動作清單正確列為候補而非待Design）→ cycle 1 → Ready → In Progress、派 ios-dev（自 development `6c313a0`）。範圍：`PrivacyInfo.xcprivacy`（依實際 required-reason API＋SPM 依賴自帶 manifest 查證）、用途字串繁中 base＋en `InfoPlist.strings`（非模板字）、`ITSAppUsesNonExemptEncryption=false`、gate `privacy-manifest-check.sh` 掛 CI `rules`；本機 `xcodebuild archive`（無簽章）驗；真上傳待 LS-8。

### 2026-09-03T12:42:46.502Z · Chih-Lin Yeh · `ec2ba6b8`

## Handoff — ios-dev

**Ticket**：LS-145 Phase 2-2：PrivacyInfo.xcprivacy＋Info.plist 用途字串＋出口合規旗標＋CI gate（非畫面票，不需 Design gate）

**已完成**：
1. `LittleSprout/PrivacyInfo.xcprivacy`：新增，加進 app target 資源（`project.yml` 未變——XcodeGen folder source path 下自動歸類為 Resources；`xcodegen generate` 後 pbxproj 只新增這個檔案的掛載，`knownRegions` 不變）。`NSPrivacyAccessedAPITypes` 只列一類：File Timestamp APIs（理由碼 `C617.1`）——grep 全專案 Swift 檔＋`DerivedData/…/SourcePackages/checkouts/`（supabase-swift／swift-http-types／swift-crypto／swift-asn1／swift-clocks／swift-concurrency-extras／xctest-dynamic-overlay，LS-49 鎖版 2.49.0 的 transitive deps）確認：唯一用到的 required-reason API 是 `FileManager.attributesOfItem(atPath:)`，來自 app 自身 `LittleSprout/Services/Diary/MediaUploadService.swift:263`（`fileByteSize(at:)`）與 supabase-swift `Sources/Storage/MultipartFormData.swift:253`（`Storage` module 未自帶 `PrivacyInfo.xcprivacy`，故列進 app 側）；`UserDefaults`（App／Auth 皆走 Keychain，非 UserDefaults——`SupabaseClientFactory.swift`／supabase-swift `KeychainLocalStorage`）、system boot time、disk space 三類 API 全專案與全部 SPM checkout 皆無用到，未列。`NSPrivacyCollectedDataTypes` 三類（照片影片／Email／使用者內容，皆 `Linked=true`、`Tracking=false`、purpose `AppFunctionality`）；`NSPrivacyTracking=false`。swift-crypto 各 target 自帶各自 manifest，未重複列。
2. `LittleSprout/Info.plist`：加 `NSPhotoLibraryUsageDescription`（「萌芽日記需要讀取你的相簿，才能挑選寶貝照片上傳到私密家庭相簿。」，31 字元）。**未加**：`NSCameraUsageDescription`（grep 全專案無 `AVCaptureDevice`／`AVCaptureSession`／camera source 的 `UIImagePickerController`；`CreateChildView.swift`／`EditChildView.swift` 的 `Image(systemName: "camera")` 只是頭像編輯鈕圖示）、`NSPhotoLibraryAddUsageDescription`（grep 全專案無 `PHPhotoLibrary.performChanges`／`UIImageWriteToSavedPhotosAlbum` 等存回相簿功能，唯一 `ShareLink` 用途是分享邀請碼文字）、推播用途字串（LS-22 未落地，grep 全專案無 `UNUserNotificationCenter`／`registerForRemoteNotifications`）。三項未加皆有 Info.plist 內註解附證據。
3. `ITSAppUsesNonExemptEncryption = false`（僅 HTTPS）。
4. `scripts/gates/privacy-manifest-check.sh`＋`privacy_manifest_check.py`（python3，跨 macOS／ubuntu-latest，同 `api-contract-check.sh`／`api_contract_check.py` 既有慣例）＋`.test.sh`：驗 (a) manifest 合法 plist＋`NSPrivacyTracking`＋`NSPrivacyAccessedAPITypes` 至少一項且每項有非空理由碼；(b) 用途字串三鍵（存在的那幾個）≥20 字元、不含模板片語黑名單（「此 App」「需要存取」「Your app」「uses the camera」等）；(c) `ITSAppUsesNonExemptEncryption` 為 `false`。掛 `.github/workflows/ci.yml` `rules` job（自測併入既有「Gate 腳本自測」step；正式檢查獨立一個 step，無條件跑，同品牌 skill 接線／agent 工具白名單）。`docs/COLLABORATION.md` §7 對照表加一列。

**刻意的 scope 限縮（與 LS-112 衝突，非遺漏）**：票文原要求繁中 base＋`en.lproj/InfoPlist.strings` 兩語都出貨。但 LS-112（Done，merge-review R1 已用 DerivedData 產物實測驗證）證實這個 app 目前刻意不出貨任何 `.lproj` 在地化資源——一旦有 `en.lproj`（即使只放 `InfoPlist.strings`），`SignInWithAppleButton` 的顯示語系會跟著 app 實際打包的在地化資源集合走，讓偏好英文的裝置上 Apple 官方鈕變回英文、與其餘寫死中文的按鈕再度不一致。因此本票只落地繁中版；gate 對 `en.lproj/InfoPlist.strings` 設計成「存在才驗、不存在不算失敗」——機制已備好，等未來做全 String Catalog 多語系時再出貨內容。PR body 已載明此偏離與理由。

**已驗證（怎麼驗的）**：
- `xcodebuild -scheme LittleSprout -destination 'generic/platform=iOS' -configuration Release archive -archivePath <scratchpad>/LS-145.xcarchive CODE_SIGNING_ALLOWED=NO`：`** ARCHIVE SUCCEEDED **`。`plutil -lint` 對 archive 內 `Products/Applications/LittleSprout.app/PrivacyInfo.xcprivacy`／`Info.plist` 皆 `OK`；`plutil -p` 確認 `ITSAppUsesNonExemptEncryption=false`、`NSPhotoLibraryUsageDescription` 為具體中文文案、`CFBundleDevelopmentRegion` 仍為 `zh-Hant`；`find … -iname "*.lproj"` 確認 app 內無任何 `.lproj`（LS-112 未被動到）。
- `bash scripts/gates/privacy-manifest-check.test.sh`：25 組樣本（①正樣本 5＋②manifest 負樣本 5＋③用途字串負樣本 5＋④出口合規負樣本 3＋⑤參數錯誤 2＋⑥mutation 負控 5）全過，`EXIT=0`。
- `bash scripts/gates/privacy-manifest-check.sh`（對真實 repo 檔案跑）：全過，`EXIT=0`。
- push gate（`swiftlint lint --strict`＋`xcodebuild test -only-testing:LittleSproutTests`）：三次 push 皆 `✓ push gate 通過`。
- PR #257 四個 CI check（`ci`／`db`／`lint`／`rules`）全綠（`gh pr checks 257 --watch` 前景等到）。

**未完成／informational 處置**：本票尚無 reviewer（merge-reviewer 尚未審），無 informational 待處置——一條都沒有可列。

- 待辦（非本票可獨立完成，票文已載明）：`xcodebuild -exportArchive`／Xcode Organizer 上傳無 privacy 警告的真實驗證，待 LS-8（Apple Developer 帳號）到位後才能實測。
- 待辦：`en.lproj/InfoPlist.strings` 英文版用途字串——等未來做全 String Catalog 多語系工作時再出貨（見上方 LS-112 衝突說明）。

**風險**：
- Privacy Manifest 的 required-reason API 清單是靠人工 grep 得出（app 原始碼＋SPM 依賴 checkout），CI 只驗格式合法性、不驗「是否窮盡列齊」——真正的窮盡性要等 LS-8 到位、Xcode Organizer 上傳時的官方靜態分析才能交叉核對（已記在 gate 檔頭與 §7 對照表盲區）。
- 黑名單只擋逐字命中的模板片語，換一種說法的空泛文案（不在黑名單內）仍過得了字數門檻——純機械擋不住語意層面的敷衍。

**產出位置**：
- Branch：`feature/LS-145-privacy-manifest`，head `208c1ca302290bc2957bc3f74e6c5d80a9c4a5ec`
- PR：https://github.com/CLYEH/little-sprout/pull/257（feature/LS-145-privacy-manifest → development，四個 CI check 全綠）
- Worktree：`.claude/worktrees/LS-145`
- 本機 archive 產物：`/private/tmp/claude-501/-Users-clyeh-little-sprout/954fc361-48a1-4e75-b3ca-02a3ce7da71b/scratchpad/LS-145.xcarchive`（39M，scratchpad，未進版控）
- 模擬器已關：`LS-145-iPhone17Pro`（`214B7952-8E71-4436-927E-B493DE05DB24`，push gate 執行完自動關閉，本次任務未另外手動 boot 過其他機器）；`main-iPhone17Pro` 現況 Booted 但非本次任務所開，未動它。

Push 已過 gate（swiftlint＋xcodebuild test）、PR 四個 CI check 已等到全綠，未停在「等 CI」。待 orchestrator 派 merge-reviewer。

### 2026-09-03T12:44:03.251Z · Chih-Lin Yeh · `3168f0fa`

orchestrator 2026-09-03 20:43：handoff `ec2ba6b8` 收到——PR **#257** head `208c1ca`（manifest 只列 `C617.1`（app＋supabase-swift MultipartFormData，該 package 無自帶 manifest）；相簿用途字串；`ITSAppUsesNonExemptEncryption=false`；gate 25 樣本＋5 mutation 掛 CI `rules`；archive 無簽章驗證）；**取捨：不出 `en.lproj/InfoPlist.strings`**（LS-112 零 `.lproj` 原則，避免 Apple 按鈕回英文）→ In Review、派 merge-review R1（重點：required-reason API 自行 grep 含 Auth session storage、manifest 進 bundle、en.lproj 取捨對英文審核員的風險列 informational）。agent 自述誤編主 checkout 兩次已還原（今日第三起）→ LS-154 guard 進行中。

### 2026-09-03T23:56:30.703Z · Chih-Lin Yeh · `095e6769`

## merge-review R1 — PR #257（head `208c1ca`）

**判定：APPROVE**（無 blocker／major；3 minor＋6 informational，皆不阻擋合併）

審查範圍：`git diff 6c313a0...208c1ca`，8 檔（ci.yml／pbxproj／Info.plist／PrivacyInfo.xcprivacy／COLLABORATION.md／gate 三支）。前一位 reviewer 未產出，本輪從頭做，未採信 handoff 任何未經自行複驗的斷言。

---

### 逐項查實

**1. required-reason API 清單（自行 grep，不採信 handoff）**

自行對 186 支 app Swift 檔＋7 個 SPM checkout（`DerivedData/LittleSprout-gwvuerv…/SourcePackages/checkouts`，對照 `Package.resolved`：supabase-swift 2.49.0／swift-asn1 1.7.1／swift-clocks 1.1.0／swift-concurrency-extras 1.4.1／swift-crypto 4.5.1／swift-http-types 1.6.0／xctest-dynamic-overlay 1.11.0）跑四類 required-reason API 全文 grep：

| 類別 | 理由碼 | grep 樣式 | 結果 |
|---|---|---|---|
| File Timestamp | `C617.1` | `attributesOfItem`／`creationDate`／`modificationDate`／`resourceValues`／`getattrlist`／`fstat`／`lstat` | app `LittleSprout/Services/Diary/MediaUploadService.swift:263`＋依賴 `supabase-swift/Sources/Storage/MultipartFormData.swift:253`，**其餘零命中** |
| UserDefaults | `CA92.1` | `UserDefaults`／`NSUserDefaults`／`@AppStorage`／`@SceneStorage`／`NSUbiquitousKeyValueStore` | **app 與全部 SPM Sources 皆零命中** |
| System Boot Time | `35F9.1` | `systemUptime`／`mach_absolute_time`／`mach_continuous_time`／`kern.boottime`／`CACurrentMediaTime` | 零命中 |
| Disk Space | `E174.1` | `volumeAvailableCapacity`／`statfs`／`systemFreeSize`／`volumeTotalCapacity` | 零命中 |

Auth session storage 逐檔查實：`supabase-swift/Sources/Auth/Storage/AuthLocalStorage.swift:11` `static let defaultLocalStorage: any AuthLocalStorage = KeychainLocalStorage()`（`KeychainLocalStorage.swift:4` 註解明載 "This is the default local storage used by the library"），且 `LittleSprout/Config/SupabaseClientFactory.swift:44` 的 `SupabaseClientOptions` 未覆寫 `storage` → 走 Keychain，**`CA92.1` 確實不需列**，handoff 此點成立。

自帶 manifest 的 package 清單（`find … -name PrivacyInfo.xcprivacy`）：僅 swift-crypto 7 個 target（`Crypto`／`CCryptoBoringSSL`／`CCryptoBoringSSLShims`／`CryptoBoringWrapper`／`CryptoExtras`／`CXKCP`／`CXKCPShims`）。**supabase-swift 全 8 個 module 皆無**，故 `Storage` 的用量必須由 app 側 manifest 承擔——本 PR 作法正確。已在 build 產物確認 `swift-crypto_Crypto.bundle/PrivacyInfo.xcprivacy` 隨 app 一起打包。

`NSPrivacyCollectedDataTypes` 三類值對 Apple 枚舉逐字核對：`NSPrivacyCollectedDataTypePhotosorVideos`（Apple 官方字串就是小寫 `or`，未打成 `Or`，正確）／`NSPrivacyCollectedDataTypeEmailAddress`／`NSPrivacyCollectedDataTypeOtherUserContent`；`…Linked=true`／`…Tracking=false`／purpose `…PurposeAppFunctionality` 皆合法。`NSPrivacyTracking=false`＋`NSPrivacyTrackingDomains` 空陣列，一致。`plutil -lint` 對 `PrivacyInfo.xcprivacy` 與 `Info.plist` 皆 `OK`。

**manifest 真的進 bundle**（自行驗，未採信 handoff 的 archive 說法）：對此 worktree、head `208c1ca` 的建置產物 `DerivedData/LittleSprout-gwvuerv…/Build/Products/Debug-iphonesimulator/LittleSprout.app` 檢查——`PrivacyInfo.xcprivacy` 4004 bytes 位於 `.app` 根層（原檔逐字複製、未被轉檔）。`project.pbxproj` 只有單一 `PBXResourcesBuildPhase`（`CA761FF2D874C3B51C94023D /* PrivacyInfo.xcprivacy in Resources */`），該 phase 不分 configuration，故 Release／archive 同樣涵蓋。另 `xcodegen generate` 在乾淨 `git archive` 複本重跑，產出的 `project.pbxproj` 與本 PR 提交版**逐 byte 相同**——pbxproj 非手改、無漂移。

**2. Info.plist／project.yml**

`INFOPLIST_FILE: LittleSprout/Info.plist`（`project.yml:63`）、app target 未設 `GENERATE_INFOPLIST_FILE` → gate 檢查的是真正的 build 來源，不是死檔。`NSPhotoLibraryUsageDescription`＝「萌芽日記需要讀取你的相簿，才能挑選寶貝照片上傳到私密家庭相簿。」（31 字元，點名產品名／動作／去向，非模板字，符合 PLAN §9-B:218「寫具體，敷衍會被退」）。Info.plist 無重複鍵、24 個 top-level key。`ITSAppUsesNonExemptEncryption` 為布林 `false`（bundle 內 `plutil -extract` 複驗）。

未加三鍵的理由自行複驗：相機——全專案零 `AVCaptureDevice`／`AVCaptureSession`／camera-source `UIImagePickerController`（`Image(systemName: "camera")` 確為 SF Symbol 圖示）；存回相簿——零 `PHPhotoLibrary.performChanges`／`UIImageWriteToSavedPhotosAlbum`；推播——零 `UNUserNotificationCenter`／`registerForRemoteNotifications`（LS-22 未落地）。**三項未加皆成立。**

**3. gate 實跑**

`privacy-manifest-check.test.sh` 在此 worktree 實跑：25 組全過、`EXIT=0`、1.9s。5 個 mutation 負控我逐一確認是真的對 `privacy_manifest_check.py` 本體做整行替換（`mutate()` 找不到標記會 `fail=1`，負控不會空轉）。`privacy-manifest-check.sh` 對真檔實跑 `EXIT=0`。CI 接線：`ci.yml` `rules` job 兩處——自測併入既有「Gate 腳本自測」step、正式檢查獨立 step 且**無條件跑**（不依賴 PR body／分支名），與品牌 skill 接線／agent 工具白名單同型。`::error::` 慣例：repo 內 `::error::` 只用於 ci.yml 內嵌檢查，`scripts/gates/*.sh` 一律用 `✗` 前綴＋stderr——本 gate 從眾，無偏離。`docs/COLLABORATION.md:301` 已列。

**4. PR body／CI／scope**

`pr-body-check.sh --verify` 在 PR worktree 內實跑 → `✓ PR body 檔頭段含本票票號 LS-145`／`✓ 申報反查`，`EXIT=0`。`Design: 不需 Design gate（非畫面票，LS-144 正典標記不適用）` 明寫。CI 四 check 全綠（run 33755907326：ci 6m55s／db 3m4s／lint 20s／rules 2m57s）。

**Scope：乾淨。** 8 個檔逐一可回溯票文 §範圍 1–4＋§7 對照表一列；無無關重構、無順手改動。ci.yml 只動 step 名稱字串＋加兩處自己的行。pbxproj 僅 +4 行且經 xcodegen 複現。與票文唯一偏離（不出 `en.lproj/InfoPlist.strings`）已在 PR body、腳本檔頭、§7 三處載明理由——屬有依據的刻意限縮，不列 finding。

**四維度誠實交代**：本 PR 零 Swift 執行期程式碼變更（只有資源檔與 shell/python gate），**race condition／運算效能／平行優化三個維度沒有可審的東西**，我不硬湊 finding。gate 執行成本 3 個 `python3` 行程、CI 上可忽略。

---

### Findings

**N1（minor）· `scripts/gates/privacy_manifest_check.py:191-215`：gate 不驗理由碼與 API 類別是否為 Apple 合法枚舉值——這正是上傳被退的那一類錯**

實跑重現（把真 manifest 複製到 fixture 後改值）：

- `NSPrivacyAccessedAPICategoryUserDefaults` 配 `C617.1`（類別／理由碼錯配）→ `✓ privacy-manifest-check 全過`、`EXIT=0`
- 理由碼打成 `C6171`（漏小數點）→ 同樣全過
- 類別打成 `TotallyBogusCategory` → 同樣全過
- `NSPrivacyCollectedDataTypePhotosorVideos` 改成 `…PhotosOrVideos`（大寫 O，最容易犯的那個 typo）→ **`NSPrivacyCollectedDataTypes` 整段 gate 根本不看**，全過

失敗情境：日後升級 supabase-swift 或新增第二個 API 類別時，複製貼上把 `35F9.1` 填進 FileTimestamp 條目（或理由碼少一個點），gate 綠、CI 四綠、PR 併入，直到 LS-8 到位跑 Xcode Organizer 上傳才被 ITMS-9105x 系列退件——而這個 gate 存在的目的就是在那之前攔住。修法成本很低：Apple 的類別×理由碼是固定四類（`FileTimestamp` C617.1/DDA9.1/C617.1 系列、`SystemBootTime` 35F9.1/8FFB.1/3D61.1、`DiskSpace` E174.1/85F4.1/7D9E.1/B728.1、`UserDefaults` CA92.1/1C8F.1/C56D.1/AC6B.1）＋`NSPrivacyCollectedDataType*` 枚舉，一張約 25 行的靜態表即可。

附帶：`docs/COLLABORATION.md:301` 盲區欄寫「(a) 只驗 manifest 的**格式**是否合法齊全，不驗**內容是否窮盡**」——依上面實測，「格式合法」也只驗到結構非空，值合法性沒驗。若不補表，這句措辭至少要改成「只驗結構非空，不驗值是否為 Apple 合法枚舉、也不驗是否窮盡」，否則下一個人會誤以為 typo 有人守。

**N2（minor）· `scripts/gates/privacy_manifest_check.py:39-48` BLACKLIST：`需要存取` 會誤擋合格文案**

實跑重現（四個樣本走真 gate）：

| 樣本 | 判定 |
|---|---|
| 「萌芽日記需要**存取**你的相簿，才能挑選寶貝照片上傳到私密家庭相簿。」 | **BLOCK** |
| 「為了讓你挑選寶貝的照片上傳到家庭相簿，萌芽日記需要**存取**相片權限。」 | **BLOCK** |
| 「挑選寶貝照片上傳到家庭相簿」（13 字，具體但短） | **BLOCK**（<20） |
| 英文具體版 74 字 | PASS |

第一列跟現行出貨字串只差「讀取／存取」兩個字，語意具體度完全相同。Apple 模板的辨識特徵是「**此 App** 需要存取…」裡的「此 App」，`需要存取` 單獨看只是中文最自然的「需要存取」動詞片語，不是敷衍的標記。失敗情境：日後 LS-22 推播或加拍照入口時，開發者寫出完全合格的 `NSCameraUsageDescription` 卻吃到 CI 紅、訊息說「命中模板片語黑名單」，只能改用比較彆扭的字詞繞過——gate 開始教人寫壞文案。建議把 `需要存取` 收緊成 `此 app 需要存取`／`需要存取你的相片圖庫` 這類真正的樣板全句，或至少在錯誤訊息裡提示「單獨的『需要存取』也會命中」。（≥20 字門檻我認為從嚴合理，不建議動，僅記錄它會擋掉短而具體的文案。）

**N3（minor）· `scripts/gates/privacy_manifest_check.py:117`：`.strings` 以 UTF-8 硬讀，UTF-16 檔會噴 traceback**

`open(path, "r", encoding="utf-8")` 只 catch `OSError`；`.strings` 傳統上是 UTF-16。實跑重現：寫一個 UTF-16LE 的 `en.lproj/InfoPlist.strings`（內容完全合格）→ 未捕捉的 `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xff in position 0`，整段 Python traceback 直接吐到 CI log。exit code 為 1，**行為上仍是 fail-closed（不會誤放行）**，所以不升級嚴重度；問題是診斷訊息完全沒指向真因，而且那是一個內容正確的檔案被判紅。目前 LS-112 定案下這個檔不存在，屬潛伏項；未來做 String Catalog 時會第一個踩到。修法：把 `UnicodeDecodeError` 一併 catch 進 `fail()`，或先試 UTF-8、失敗回退 UTF-16。

---

### Informational（不阻擋，供 orchestrator 決定處置）

**I1 · `NSPrivacyCollectedDataTypes` 未列「Name」（姓名）**
app 確實蒐集姓名：`LittleSprout/Features/Auth/WelcomeView.swift:272` 明確向 Apple 要求 `requestedScopes = [.email, .fullName]`；DB 側 `profiles.display_name`（migration `char_length … between 1 and 50`）與 `children.name`（1–60）都存在 Supabase。依 Apple 定義（資料離開裝置且保存於開發者後端＝collect），這屬 `NSPrivacyCollectedDataTypeName`；`auth.uid` 也可論及 `…UserID`。**不列為 finding 的理由**：票文 §範圍 1 與 PLAN §9-B:216 都只列「照片／影片、聯絡資訊 email、使用者內容」三類，實作與兩份規格一字不差，不是遺漏而是照做。建議與 PLAN §9-B 第 4 項「App Store Connect 資料：App Privacy 標籤」同一張票一起校準，避免 manifest 與 nutrition label 兩邊不一致。

**I2（PLAUSIBLE）· 原圖含 EXIF／GPS 未去除**
`MediaUploadService` 上傳的是原始 `Data`，只有縮圖走 `CGImageSourceCreateThumbnailAtIndex`（`MediaUploadService.swift:279-285`）；全專案無任何 EXIF 剝除邏輯。相機開啟定位時拍的照片帶 GPS，等於精確位置隨原圖進 Supabase Storage。app 從不解析這些 metadata，Apple 對「內容內嵌 metadata 是否要申報 `…PreciseLocation`」的立場屬灰帶——我沒把握下定論，標 PLAUSIBLE。**超出本票範圍**（屬上傳行為不屬 manifest），僅供 I1 那張票一併評估。

**I3 · LS-112「零 `.lproj`」目前沒有任何機械 gate，而本 PR 為它開了一條綠燈路徑**
我 grep 過 `scripts/`／`.github/`／`LittleSproutTests`／`LittleSproutUITests`：**沒有任何測試或 gate 斷言 bundle 內無 `.lproj`**。LS-112 票文的驗收是「模擬器實測附截圖」＋`developmentRegion` 為 zh-Hant，都不是可回歸的機械斷言；不變量現在只靠 `project.yml:18` 一行 `developmentLanguage: zh-Hant` 和「沒人去加 `.lproj`」撐著。本 PR 的 `privacy-manifest-check.sh:76` 把「`en.lproj/InfoPlist.strings` 存在 → 驗內容 → 可以全過」寫成官方認可路徑，等於替那個會回歸 LS-112 的改動預先鋪了綠燈（gate 訊息也是中性的「存在就驗」）。我自行複驗過機制本身成立：head `208c1ca` 的建置產物 `.app` 內 `find -iname "*.lproj"` 零筆、`CFBundleDevelopmentRegion=zh-Hant`、無 `CFBundleLocalizations`——iOS 由 bundle 內 `.lproj` 目錄推導 `Bundle.localizations`，塞進 `en.lproj/InfoPlist.strings` 確實會把 `en` 加回集合，ios-dev 的推論正確。建議（擇一，記 LS-96 即可）：加一條「`LittleSprout/**/*.lproj` 必須為空」的斷言，或把 `en.lproj` 分支的訊息改成明確警示並引 LS-112。

**I4 · 只有繁中用途字串對英文審核員的風險：低；`Base.lproj`／`CFBundleLocalizations` 兩個替代方案我未能驗證，不裁決**
風險評估：權限字串是在 app 內以 app 語系彈出，zh-Hant-only 的 app 配 zh-Hant 字串是 App Store 上的常態，審核端不會因此退件；真正需要英文的是 App Store 商店頁 metadata（另一件事）。兩個替代方案我**沒有實測**，只能給推論並標 PLAUSIBLE：(a) `Base.lproj` — Base Internationalization 不是語言在地化，配 `developmentLanguage: zh-Hant` 時系統應仍解析到中文，理論上不會讓 Apple 鈕變英文，但沒驗；(b) `CFBundleLocalizations` 只列 `zh-Hant` — Apple 文件對這把鍵的語意是「app 自行處理的在地化」，屬**追加**而非**限縮**語系集合，能不能壓過 `.lproj` 推導出來的集合我查不到明確保證，不建議當作可靠手段。若日後真要出英文用途字串，我認為唯一乾淨的路是照 LS-112 票文自己寫的「等未來真的做多語系（全文案進 String Catalog）再加」——把全部文案一起在地化，Apple 鈕跟著英文裝置變英文就不再是不一致而是正確行為。本項依派工要求列 informational、**不裁決**。

**I5 · `PRIVACY-APITYPES-NONEMPTY` 強制 `NSPrivacyAccessedAPITypes` ≥1 項**
一個合法地完全不使用 required-reason API 的 app，其正確 manifest 就該是空陣列，但會被本 gate 判紅。對本專案無影響（有 `C617.1`），僅記錄這條規則的假設。

**I6 · `C617.1` 屬保守申報，不需改**
`FileManager.attributesOfItem(atPath:)` 不在 Apple 具名的 File-timestamp API 清單上（清單列的是 `NSFileCreationDate`／`NSFileModificationDate`／`stat`／`getattrlist` 等），且 `MediaUploadService.swift:263-268` 只讀 `[.size]`（`NSFileSize`，本身不是 required-reason API）。申報比不申報安全、Apple 不會因多報而退件，維持現狀正確。

---

### 未審／未能驗證（誠實交代）

- **二進位符號層複驗失敗、結論不可用**：我試圖用 `nm -u`＋`strings` 掃 `LittleSprout.debug.dylib` 來模擬 Apple 的靜態分析，結果 1810 個 undefined symbol 中連**已知存在**的 `attributesOfItem` 呼叫都掃不到（`10Foundation11FileManager` 前綴 0 筆）——工具路徑本身無效，這次的「零命中」既不能證實也不能證偽窮盡性。**required-reason API 是否列齊，目前唯一證據仍是原始碼層 grep**（我自己重跑過一遍，與 handoff 結論一致），真正的裁決要等 LS-8 到位後 Organizer 上傳——這與 §7 已載明的盲區一致，不是本 PR 的缺失。
- 我**未**自行跑 `xcodebuild archive`（Release／device）。bundle 內含 manifest 是用此 worktree head 的 Debug-iphonesimulator 產物＋pbxproj 只有單一 config-independent `PBXResourcesBuildPhase` 推得；handoff 的 archive 實測我未複驗。
- 未跑 `supabase db reset`／`supabase/tests/run.sh`（本 PR 無 migration／SQL 變更）。未開任何模擬器。
- 票文驗收第一項「Xcode Organizer 上傳無 privacy 警告」依賴 LS-8，本輪無法驗——PR body 與 handoff 皆已載明為既定中間態，同意。

### Verdict：**APPROVE**

N1／N2／N3 三條 minor 依規約可由 ios-dev 選擇「已修／記入 LS-96／不修＋理由」，不阻擋合併。個人建議：N1 的 §7 措辭修正成本近乎零、值得順手做（不補枚舉表也要把盲區寫準），N2／N3 記 LS-96 即可。I3 是我這輪認為最值得後續處理的一條——它是 harness 層的缺口，不是本 PR 的錯。


### 2026-09-03T23:58:43.122Z · Chih-Lin Yeh · `14917eca`

orchestrator 2026-09-04 07:58：merge-review R1 `095e6769` **APPROVE**（required-reason API 自行 grep 186 檔＋7 SPM：只 `C617.1`，Auth 走 Keychain 故 `CA92.1` 不列；manifest 進 bundle 實證；相機／存回／推播未加理由複驗成立）→ PR #257 併入 development **`bfb36c3`**（status 已貼）。3 minor（gate 不驗枚舉值合法性、黑名單「需要存取」誤擋合格文案、`.strings` UTF-16 未捕捉）＋I3（LS-112 零 `.lproj` 無機械 gate）入池 LS-96。下一步：LS-146 QA 完成後 promote→test → QA（`xcodebuild archive` 產物含 manifest＋用途字串實機／模擬器相簿授權對話框文案）。真上傳驗證待 LS-8。

### 2026-09-04T02:03:04.797Z · Chih-Lin Yeh · `ebf56e58`

orchestrator 2026-09-04 10:02：`promote.sh development test` 成功——test `cd638ea`→**`bfb36c3`**（5 commit；ci／db／lint／rules 四綠；merge-review status 由 promote 自動貼 `no content diff`，引 PR #257 head `208c1ca` APPROVE `095e6769`）。狀態 → **QA**，派 qa R1（qa-test worktree；`xcodebuild archive` 產物驗 manifest／用途字串／旗標／零 `.lproj`、模擬器相簿授權對話框截圖、gate 自測、`xcodebuild test`、四條冒煙；Organizer 上傳標「需 LS-8 後實測」）。PASS 後 promote→main、收尾。

### 2026-09-04T02:45:42.087Z · Chih-Lin Yeh · `0a3edf01`

## QA R1 — test `bfb36c3` (2026-09-04 10:44)

**裁決：PASS**（核心票文範圍——manifest／Info.plist／出口合規旗標／gate——全數驗證通過；2 項回歸冒煙子項因共用本機 Supabase 容器同時被其他票（LS-153／LS-151）持續 `db reset` 而 BLOCKED，環境問題、非本票缺陷，逐條列於下）

環境：worktree `qa-test`，`git rev-parse HEAD` = `bfb36c30d4f723eb56775ddb71aeee6c281eb79e`（＝`origin/test`，開工已核對一致）。模擬器 `qa-test-iPhone17Pro` (`5F59FF04-D2C6-413D-B31A-AFD39B257FB3`)。

---

### 逐條驗收

**1. manifest 進 bundle — ✓ PASS**

`xcodebuild archive`（Release，`-destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO`）`** ARCHIVE SUCCEEDED **`。對 `.xcarchive/Products/Applications/LittleSprout.app` 逐項核對：
- `PrivacyInfo.xcprivacy` 4004 bytes 位於 app 根層，`plutil -lint` → `OK`；`plutil -p` 內容含 `NSPrivacyAccessedAPICategoryFileTimestamp`＋理由碼 `C617.1`、三類 `NSPrivacyCollectedDataTypes`（`PhotosorVideos`／`EmailAddress`／`OtherUserContent`，皆 `Linked=true`／`Tracking=false`／purpose `AppFunctionality`）、`NSPrivacyTracking=false`。
- `Info.plist`：`plutil -lint` → `OK`；`NSPhotoLibraryUsageDescription` = 「萌芽日記需要讀取你的相簿，才能挑選寶貝照片上傳到私密家庭相簿。」（具體繁中文案）；`ITSAppUsesNonExemptEncryption` = 布林 `false`（`plutil -extract` 複驗型別）。
- `find "$APP" -iname '*.lproj'` → 0 筆；`CFBundleDevelopmentRegion` = `zh-Hant`（LS-112 不回歸，確認）。

**2. 用途字串實際彈出 — ⊘ 修正認知（非 FAIL）**

模擬器 Debug build & run，`xcrun simctl privacy reset photos <bundle id>` 後登入、建立家庭、進入「寫日記」畫面點「新增照片」——**系統相簿授權對話框並未出現**，`PhotosPicker`（`PHPickerViewController`）直接開啟系統相片選取 UI，無 `NSPhotoLibraryUsageDescription` 彈窗。這與 ios-dev 在 `Info.plist` 註解與 PR body 已載明的說法完全一致（「嚴格來說 `PhotosPicker` 本身不觸發系統相簿授權、不強制要求此鍵；仍加上是防禦性作法」）——**非本票缺陷，是 `PHPickerViewController` 的隱私設計本身**（out-of-process、只把使用者勾選的項目丟給 app，不需要全庫存取授權）。截圖：`photos-picker-no-system-dialog.png`（點「新增照片」後直接看到系統相片格線，無授權對話框）。Dynamic Type 對話框文案截斷檢查因此不適用（沒有對話框可測）。字串本身仍正確、非模板字、gate 已驗——這條「彈跳提示」在目前實作下不會真的被使用者看到，屬防禦性宣告，供未來若改走 `PHPhotoLibrary`／`PHPickerViewController` 進階選項時使用。

**3. gate — ✓ PASS**

`bash scripts/gates/privacy-manifest-check.sh`：全過，`EXIT=0`（含 en.lproj 不存在時的「不算失敗」分支正確觸發）。
`bash scripts/gates/privacy-manifest-check.test.sh`：25 樣本＋5 mutation 負控全過，`EXIT=0`，`✓ privacy-manifest-check 自測通過`。

**4. `xcodebuild test` — ✓ PASS**

`-only-testing:LittleSproutTests`（match CI／push-gate 既有範圍）：`Executed 476 tests, with 0 failures (0 unexpected) in 2.444s`，`** TEST SUCCEEDED **`。

**5. 回歸冒煙四條**

- **登入 Email OTP 本機取碼：✓ PASS**——`/auth/v1/otp` 送出、Mailpit (`127.0.0.1:54324`) 取到明文 6 碼、輸入後成功登入（畫面「歡迎，qalsq1」）。截圖：`smoke-login-otp.png`。
- **時間軸載入：✓ PASS**——建立家庭後導向時間軸，空狀態「還沒有回憶」＋「新增回憶」CTA 正確渲染。截圖：`smoke-timeline.png`。
- **照片上傳：⊘ BLOCKED（環境）**——UI 端到端可用：`PhotosPicker` 選照片→附加到日記草稿（顯示「1／20 張」）皆正常；但按「發佈日記」兩次都收到「伺服器發生問題，請稍後再試一次」（app 端優雅處理，草稿內容保留、給重試按鈕，未當機）。查因：本機 Supabase 容器與 LS-153／LS-151 兩張票的 worktree 共用（LS-70），QA 過程中 `bash scripts/ops/supabase-lock.sh --status` 反覆看到 `held … cmd=bash -c supabase db reset && bash supabase/tests/run.sh` 高頻佔用（多次輪詢間隔 <5s 就換一次 holder），postgres log 同時間戳出現 `relation "auth.users" does not exist`／`insert or update on table "profiles" violates foreign key constraint "profiles_id_fkey"`——我的登入 session 在完成後、發佈前，底層 user 列被其他票的 `db reset` 洗掉。非 LS-145 程式碼缺陷。
- **留言：⊘ BLOCKED（環境，同上）**——日記未能成功發佈，沒有可留言的對象；同一因果鏈未再重試（多輪嘗試已耗用相當時間，詳見下方風險）。

**6. RLS 冒煙：⊘ 標「本票無 SQL 變更」**——`git diff cd638ea..bfb36c3 --stat -- supabase/` 無輸出，確認無 migration／SQL 檔異動，依票文指示跳過。

**7. Organizer 上傳無 privacy 警告：⊘ 標「需 LS-8 後實測」**——依票文指示，非本輪 FAIL 項。

---

### 風險／備註

- **環境觀察（非本票缺陷，供 orchestrator 參考）**：QA 執行的 ~40 分鐘窗口內，本機 Supabase 容器被 LS-153（`feature/LS-153-purge-scheduler`）與 LS-151（`feature/LS-151-delete-account-edge`）近乎不間斷地跑 `supabase db reset && supabase/tests/run.sh`／mutation 測試腳本，lock 平均每 15–60 秒換一次持有者。這使得任何需要「登入→建立家庭→操作→驗證持久化」這種多步驟、跨多次工具呼叫的互動式 QA 在目前的高併發開發時段下不穩定——不是 LS-70 lock 機制失效（lock 本身正確序列化，沒有兩邊同時寫），而是**序列化後的重置頻率高到超過我單步驟 UI 操作的節奏**，導致 session 在完成登入後、還沒來得及發佈內容前，底層資料就被下一輪重置清空。這與 LS-145 程式碼無關，但值得記一筆：往後若要在多票並行 QA，可能需要考慮讓 QA 拿到獨立的本機 Supabase 專案/port，或協調高頻 reset 的票錯開執行窗口。
- 過程中也發現一個純測試操作上的偽陽性：初次用 `mobile_type_keys` 在 Email 欄位打字時，自動完成/預測文字在 `textContentType(.emailAddress)` 欄位偶爾會多插入一個尾隨空白，導致 GoTrue 回 `400 Unable to validate email address: invalid format`——這是自動化打字工具的產物，不是 app 缺陷（真人鍵盤操作不會在確認送出前留下這種尾隨空白；且該欄位本身沒做 `trimmingCharacters`，這點若要根治屬另一張票的範圍，不在此記 finding，僅留意）。

### 產出位置

- Archive：`/private/tmp/…/scratchpad/LS-145-qa1/LS-145.xcarchive`
- 截圖：`smoke-login-otp.png`／`smoke-timeline.png`／`photos-picker-no-system-dialog.png`（同目錄）
- 模擬器已關：`5F59FF04-D2C6-413D-B31A-AFD39B257FB3`（qa-test-iPhone17Pro）


### 2026-09-04T02:47:15.093Z · Chih-Lin Yeh · `747f54ce`

orchestrator 2026-09-04 10:47：QA R1 `0a3edf01` **PASS**（archive 產物 manifest／plist／旗標／零 `.lproj` 全對；gate 25＋5 綠；`xcodebuild test` 476/0；登入 OTP＋時間軸冒煙綠；照片上傳／留言冒煙 BLOCKED 屬環境——共用容器被 LS-153／LS-151 高頻 `db reset` 洗掉 session，本票零 runtime 程式碼變更故不影響判定；`PhotosPicker` 不觸發系統相簿授權對話框，用途字串保留無害；qa status 已貼 test `bfb36c3`）。**promote test→main 暫緩**：main `4d9e44e`（LS-154 hotfix）⊄ test，FF 不成立；待 LS-156（小票）併 development 後批次 `promote.sh development test`（含 LS-154 back-merge，delta 純 harness 6 檔）→ 一次 QA（本票已過項只需複核 build）→ test→main 打 tag → 收尾。QA 環境併發問題入池 LS-96 P2。

### 2026-09-04T04:26:41.500Z · Chih-Lin Yeh · `4e5d5029`

orchestrator 2026-09-04 12:26：QA R1 `0a3edf01` PASS＋批次複核 `568c1900`／`0fc3e44b`（archive 產物 manifest／plist／旗標／零 `.lproj` 無回歸）→ 隨 LS-156 批次 promote：test `b454ed9` → main **`b454ed9`**，tag **v0.8.6**。收尾儀式：派 sweeper（直貼）→ retro → Done；Organizer 上傳驗證仍待 LS-8（記入 Done 後的殘留驗收，非阻擋）。

### 2026-09-04T04:30:46.095Z · Chih-Lin Yeh · `6d164e46`

## dead-code sweep — LS-145

巡檢範圍：`git diff 6c313a0..208c1ca`（PR #257，8 檔：`.github/workflows/ci.yml`／`LittleSprout.xcodeproj/project.pbxproj`／`LittleSprout/Info.plist`／`LittleSprout/PrivacyInfo.xcprivacy`／`docs/COLLABORATION.md`／`scripts/gates/privacy-manifest-check.sh`／`.test.sh`／`privacy_manifest_check.py`）。工作基準：main checkout `b454ed9`（唯讀）。

### Findings（1 條，測試覆蓋缺口——非典型死碼）

**F1（observation，非阻擋）· `scripts/gates/privacy_manifest_check.py:39-48` BLACKLIST 8 個片語中有 4 個沒有任何 `privacy-manifest-check.test.sh` 樣本觸發過**

逐一比對（case-insensitive，比照程式碼實際比對方式 `phrase in text.lower()`）：

| BLACKLIST 片語 | test.sh 內有無對應樣本 |
|---|---|
| `此app`（無空格） | **無**（0 命中） |
| `此 app`（有空格） | 有（`③` 樣本「此 App 需要存取…」，`:192`／`:299`） |
| `需要存取` | 有（同上） |
| `your app` | 有（`③` 樣本「Your App uses the camera…」，`:203`） |
| `uses the camera` | 有（同上） |
| `this app needs` | **無**（0 命中） |
| `we need access` | **無**（0 命中） |
| `為了提供更好的服務` | **無**（0 命中） |

證據指令：
```
$ for phrase in "此app" "此 app" "需要存取" "your app" "uses the camera" \
    "this app needs" "we need access" "為了提供更好的服務"; do
    grep -ci -- "$phrase" scripts/gates/privacy-manifest-check.test.sh
  done
0
3
3
2
1
0
0
0
```

**判定：不是死碼。** 這 4 個片語仍在 `for phrase in BLACKLIST` 迴圈裡逐一比對、每次 gate 執行都會跑到（迴圈機制本身有 `⑥b` mutation 負控釘住：整段迴圈拿掉時 `此 App` 樣本會改判過，見 `.test.sh:294-304`）——它們是「活的」判定邏輯，只是**沒有任何 fixture 內容命中這 4 個字面**。如果日後有人手誤把其中一個片語打錯字（例如 `this app needs` 少一個空格），現有 25 組樣本＋5 組 mutation 都不會發現，因為沒有樣本的文案包含這幾個片語。

**建議處置**：不阻擋、優先度低——`⑥b` mutation 已經證明黑名單迴圈這個機制本身有效，只是 4 個字面內容本身缺個別樣本。可選：(a) 補 4 個對應樣本（每個約 5-8 行，成本低），或 (b) 維持現狀（8 個片語裡目前實際命中出貨文案的只有中英各一種寫法，其餘是預留給未來擴充的常見套話，見程式碼註解「另補幾個常見 AI 生成的空泛套話，供未來鍵補充」——本來就設計成「先放着，之後才會用到」，非本票急需）。交給 orchestrator／ios-dev 裁定，記入 LS-96 待辦池或維持現狀皆可。

### 逐項核對結果（依派工清單）

- **`privacy_manifest_check.py` 未用函式／常數**：`fail`／`load_plist`／`check_description`／`run_description_checks`／`cmd_infoplist`／`parse_strings_file`／`cmd_strings`／`cmd_encryption`／`cmd_manifest`／`main` 十個函式全數經 `dispatch` dict 或直接呼叫鏈觸達，無孤兒函式。`MIN_LEN`／`DESCRIPTION_KEYS`／`BLACKLIST` 三個模組常數皆被讀取；`BLACKLIST` 內容覆蓋缺口見上方 F1。
- **`.test.sh` fixture 是否皆被案例使用**：`mkroot`／`write_manifest`／`write_infoplist`／`write_en_strings`／`expect`／`mutate` 六個 helper 函式逐一 grep 呼叫次數（定義各 1 次＋額外呼叫 `mkroot` 21／`write_manifest` 19／`write_infoplist` 20／`write_en_strings` 4），皆非孤兒；`write_en_strings` 對應的 `strings` 模式（en.lproj 用途字串驗證，LS-112 定案下目前不出貨、但機制先備好）也確實有樣本呼叫，非死碼。
- **`.sh` wrapper 未用變數**：`privacy-manifest-check.sh` 逐行核對 `self_dir`／`py`／`root`／`manifest`／`infoplist`／`en_strings`／`fail`／`rc` 全數在後續分支被讀取，無宣告未用變數。
- **`Info.plist` 新增鍵是否有未被使用的**：本票只新增兩鍵——`NSPhotoLibraryUsageDescription`、`ITSAppUsesNonExemptEncryption`；獨立複驗 `NSCameraUsageDescription` **未加**：
  ```
  $ grep -rn "AVCaptureDevice\|AVCaptureSession\|UIImagePickerController" LittleSprout --include="*.swift"
  （零命中）
  $ grep -rn 'systemName: "camera"' LittleSprout --include="*.swift"
  CreateChildView.swift:300 / EditChildView.swift:86 （只是頭像編輯鈕 SF Symbol 圖示，非相機入口）
  $ grep -n "NSCameraUsageDescription" LittleSprout/Info.plist
  （只出現在檔內註解說明「未加」的理由，非鍵本身）
  ```
  與 review R1（`095e6769`）「相機——全專案零 `AVCaptureDevice`／`AVCaptureSession`」結論一致，獨立核對成立，無孤兒鍵。
- **pbxproj 是否只多 manifest 引用**：diff 僅 3 處新增（`PBXBuildFile`／`PBXFileReference`／群組掛載＋`PBXResourcesBuildPhase` 各一行），皆指向 `PrivacyInfo.xcprivacy`，無其他變更。
- **COLLABORATION §7 引用的腳本名一致**：新增列引用 `privacy-manifest-check.sh`＋`privacy_manifest_check.py`，與 repo 實際檔名（`scripts/gates/privacy-manifest-check.sh`、`scripts/gates/privacy_manifest_check.py`）逐字相符；`ci.yml` 內兩處 step（自測 step 併入清單、正式檢查獨立 step）皆呼叫存在的檔案，無指向已改名/不存在路徑的殘留條目。
- **鷹架殘留**（註解程式碼塊／debug print／TODO／假旗標）：`git diff 6c313a0..208c1ca | grep -niE "TODO|FIXME|debugPrint"` 零命中；`print(...)` 命中的全是 gate 腳本本體的診斷輸出（設計如此，非殘留 debug print）。

### 既有死碼觀察

無——本次巡檢範圍內未發現與本 feature 無關、但順路看到的既有死碼。

### 建議處置彙總

F1（BLACKLIST 4 片語缺樣本）：非阻擋，orchestrator 裁定記入 LS-96 待辦池或維持現狀。

### 搜尋方法與盲區聲明

- 方法：`git diff 6c313a0..208c1ca`（`--stat` 與逐檔全文）＋對 `.py`／`.sh`／`.test.sh` 逐函式／逐變數 grep 呼叫次數交叉比對＋獨立重跑 review 已做過的 `NSCameraUsageDescription` 判定（不採信 handoff/review 結論，自行 grep 複驗）。
- 盲區：純文字搜尋抓不到 App Store Connect 官方靜態分析對 required-reason API 的窮盡性判定（票文與 §7 已明載此為 LS-8 到位後才能實測的已知盲區，非本輪新增）；BLACKLIST 覆蓋缺口是靠列舉每個字面手動核對，未做程式化的「片語×樣本」全交叉矩陣自動化，未來若黑名單再擴充需要重新人工核對；未執行 `xcodebuild archive` 重新驗證（信任已有 QA R1 `0a3edf01`／merge-review R1 `095e6769` 的 build 產物結論，本輪只做原始碼與腳本層級巡檢，未動任何檔案、未觸發建置）。

### 2026-09-04T04:31:55.925Z · Chih-Lin Yeh · `3ad396be`

## 收尾儀式（orchestrator，2026-09-04 12:32）

**dead-code sweep** `6d164e46`（sweeper 直貼）：1 觀察——`privacy_manifest_check.py` BLACKLIST 8 片語有 4 個無自測樣本（非死碼，迴圈有 mutation 守）→ 入池 LS-96 與 `88586051` 同批。其餘乾淨：無 `NSCameraUsageDescription`（獨立核對無相機入口）、pbxproj 只多 manifest 引用、§7 腳本名一致。

**retro**
- 做對的：reviewer 自行 grep 186 檔＋7 SPM 核 required-reason API、驗 Auth 走 Keychain 故不列 `CA92.1`；manifest 進 bundle 用建置產物實證；QA 用 `xcodebuild archive` 產物驗而非只看原始碼；`PhotosPicker` 不彈系統授權對話框這件事在 QA 才確認（用途字串保留無害）。
- 機械 gate 缺口（已入池）：`88586051` P2 枚舉值合法性（正是 Organizer 退件錯型）、零 `.lproj` gate；本輪新增黑名單片語樣本。
- 殘留驗收：票文第一條「Organizer 上傳無 privacy 警告」依賴 LS-8，Done 後由 LS-8 落地時補跑一次 upload validate（記於 LS-8）。
- 清理：worktree／本機分支已清。

**狀態 → Done**（main `b454ed9`，v0.8.6）。

