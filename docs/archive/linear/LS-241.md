# LS-241 Task：LS-22 iOS——日記詳情頁留言區改接互動列＋留言 sheet（移除 LS-126 占位「留言功能即將推出」）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:S, lane:ui |
| Cycle | 2 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M5 互動與推播 |
| 估點 | — |
| 建立 | 2026-09-12T18:46:46.073Z |
| 開始 | 2026-09-12T18:47:52.515Z |
| 完成 | 2026-09-13T00:48:26.640Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-241/taskls-22-ios-日記詳情頁留言區改接互動列留言-sheet移除-ls-126-占位留言功能即將推出 |
| 父票 | LS-22 Story：留言、愛心與彙總推播 |
| 關係 | related→LS-236, related→LS-242, blocks→LS-243, related→LS-167, related→LS-96, related→LS-209, related→LS-177, related→LS-126, related→LS-132, related→LS-216, related→LS-218, related←LS-253, related←LS-245, related←LS-237, related←LS-240 |

## 描述

## 來源

v0.23.0 收尾 sweeper（[LS-132](https://linear.app/little-sprout-app/issue/LS-132/法務內容隱私權政策與使用條款正式文本繁中含-ugc-零容忍條款同步公開網址供-app-store) 巡檢 `f08734d5`，觀察項）：`LittleSprout/Features/Timeline/DiaryDetailView.swift:242-251` `commentsPlaceholder`（[LS-126](https://linear.app/little-sprout-app/issue/LS-126/taskls-21-ios-時間軸日記詳情卡片流day-divider瀑布流照片牆影片觸發態多寶貝-caption依-ls-119-核可稿) 預留：分隔線＋「留言」＋「留言功能即將推出。」）仍在 :112／:146 兩處呼叫顯示；但 [LS-216](https://linear.app/little-sprout-app/issue/LS-216/taskls-22-ios-時間軸卡片互動列愛心-like-togglecount-zone留言鈕與按讚名單-sheet-依-ls-177)（互動列 `InteractionRow`：愛心 toggle／計數／留言鈕）與 [LS-218](https://linear.app/little-sprout-app/issue/LS-218/taskls-22-ios-留言-sheet清單輸入列空狀態錯誤態-網路ls026骨架載入owner-移除操作表既有二次確認ipad)（留言 sheet）已於 v0.23.0 上線，時間軸卡片能留言、詳情頁卻寫「即將推出」——產品層不一致。

## 範圍

1. `DiaryDetailView` 兩處 `commentsPlaceholder` 改為 [LS-216](https://linear.app/little-sprout-app/issue/LS-216/taskls-22-ios-時間軸卡片互動列愛心-like-togglecount-zone留言鈕與按讚名單-sheet-依-ls-177) 的 `InteractionRow`（同一組件、同一 store，不另造）；`onOpenComments` 開 [LS-218](https://linear.app/little-sprout-app/issue/LS-218/taskls-22-ios-留言-sheet清單輸入列空狀態錯誤態-網路ls026骨架載入owner-移除操作表既有二次確認ipad) 留言 sheet（同 `.sheet` 呈現與 iPad 置中卡片規則）。計數與時間軸卡片同步（同 store，回詳情頁／回列表不得各自一套數字）。
2. 刪除 `commentsPlaceholder` 與其 doc 註解；`CommentDeleteConfirmationSheet.swift:9` 引用該名稱的註解改寫。
3. 版面：沿 [LS-177](https://linear.app/little-sprout-app/issue/LS-177/設計留言愛心互動列與留言-sheetls-22-畫面群推播通知權限提示與設定頁推播通知列-ui-designervisual) 稿若有詳情頁互動列板則照板；沒有就放在原占位位置、用 `InteractionRow` 自身的間距 token，不新畫版面（handoff 註明採哪條）。深色／AX3／iPad 各一張截圖。**iPad（R1 M1）**：`iPadLayout` 右欄 248–280pt 放不下 `InteractionRow` 標準態（≈291pt），「留言」字樣與計數被壓掉／折行 → 互動列移出兩欄 `HStack`、放兩欄下方整寬（同一檔內）；13" 直向＋側欄展開與 iPad mini 各一張截圖證明字樣與計數完整。
4. 測試：既有 `DiaryDetailView` 測試更新（占位字串 0 hit）；UITest 一條：詳情頁點留言鈕開 sheet；`tap-target-check.sh`／`ax-launch-check` 綠。
5. **文件同步（R1 m2 裁示，09-13 05:03 追加）**：`docs/store/review-notes.md:50-55`（送審備註）與 `docs/legal/README.md:58` 提到詳情頁留言「即將推出」的敘述改為現況（詳情頁可留言）——驗收 1「全 repo 0 hit」為準，scope 條款放寬到這兩處一句話修正。

## 不做

改互動列或留言 sheet 本身；Realtime；改設計稿；R1 m1（詳情頁三個 sheet 呈現來源與「⋯」`fetchContentAuthor` 窗口重疊——既有多來源模式，記 [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票)）。

## 驗收

- [ ] 「留言功能即將推出」全 repo 0 hit（含 docs）；詳情頁互動列與時間軸卡片計數一致（截圖對照）；iPad 兩張截圖字樣＋計數完整
- [ ] merge-reviewer 通過（race：詳情頁與列表同時開啟時的計數同步；scope：只動 DiaryDetailView＋註解＋測試＋範圍 5 兩處 docs）；QA 模擬器三張截圖＋真後端留言一則後回列表計數一致

## 依賴

[LS-216](https://linear.app/little-sprout-app/issue/LS-216/taskls-22-ios-時間軸卡片互動列愛心-like-togglecount-zone留言鈕與按讚名單-sheet-依-ls-177)／[LS-218](https://linear.app/little-sprout-app/issue/LS-218/taskls-22-ios-留言-sheet清單輸入列空狀態錯誤態-網路ls026骨架載入owner-移除操作表既有二次確認ipad) 已 Done（v0.23.0）。

## 留言（13）

### 2026-09-12T18:47:55.557Z · Chih-Lin Yeh · `d5929e91`

**orchestrator 09-13 02:47**：開票即派（ui lane 上限 2 在飛 1，cycle 2）。worktree `.claude/worktrees/LS-241`／`feature/LS-241-diary-detail-comments` 自 development `144622d`（v0.23.0）建；ios-dev R1 派出。

### 2026-09-12T20:23:01.012Z · Chih-Lin Yeh · `8a5a698b`

**ios-dev 09-13 04:21 R1**

**Ticket**：LS-241 日記詳情頁留言區改接互動列＋留言 sheet（移除 LS-126 占位「留言功能即將推出」）

**已完成**：
- `DiaryDetailView` 兩處占位（compact／iPad）改為 LS-216 的 `InteractionRow`（同一份 `timelineStore`，同時間軸卡片共用計數），`onOpenComments` 開 LS-218 的 `CommentsSheetView`（`.sheet(isPresented:)` + `Bool` 旗標，因為這個畫面只綁定單一 `(kind: .diary, refId: diaryID)`）。
- `DiaryDetailView` 新增 `commentAPIClient` 屬性；更新所有呼叫端（`TimelineView.swift`、`TapTargetGateHarness+Safety.swift` 三個 host）。
- 刪除 `commentsPlaceholder` 及其 doc 註解；`CommentDeleteConfirmationSheet.swift` 頭段註解訂正（真實入口是 `CommentsSheetView+Actions.swift`，不再是「尚未實作」）。
- 查過 `design/littlesprout.pen`（node/jq，未開 Pen）——LS-177 稿只有三種卡片的互動列規格板，沒有詳情頁專屬板；沿用占位原位置、只用 `InteractionRow` 自身間距 token，沒有新畫版面。
- 新增 UITest `DiaryDetailCommentsUITests.testCommentButton_opensCommentsSheet`。

**已驗證（怎麼驗）**：詳見 PR body（完整列表含指令與斷言原文），摘要：
1. 全 repo「留言功能即將推出」0 hit（`git grep`，`LittleSprout`／`LittleSproutTests`／`LittleSproutUITests` 三目錄）。
2. Build／Lint 乾淨；unit tests 989 全綠。
3. UITest：新測試 + `InteractionRowUITests`／`ContentActionsUITests`／`CommentsSheetUITests`／`DeleteConfirmationUITests` 共 31 條全綠。
4. `tap-target-check.sh`／`ax-launch-check.sh` 皆綠（含 `DiaryDetailView`／`CommentsSheetView`）。
5. 真後端 E2E（`supabase-lock.sh --hold`，本機 `Config/Secrets.xcconfig` 指向 127.0.0.1:54321，驗後已刪）：mobile-mcp 在 `LS-241-iPhone17Pro` 走完整鏈路（OTP 登入→建家庭→發日記→開詳情→點留言鈕→送出真實留言）確認詳情頁與時間軸卡片計數同步顯示「留言 1」；另在 `qa-test-iPadPro13` 用同帳號重新登入（新 session）驗證計數持久化與跨裝置一致，`docker exec … psql` 核對 `diaries`／`comments` 兩筆資料庫記錄。截圖 7 張於 `.claude/evidence/LS-241/`。
6. Mutation test（LS-209）：隔離臨時 worktree 把 `onOpenComments` 改成 no-op，`DiaryDetailCommentsUITests.testCommentButton_opensCommentsSheet` 確實轉紅（`XCTAssertTrue failed - 點擊詳情頁留言鈕應該開出留言 sheet`），驗完已刪除臨時 worktree。
7. push-gate 全綠，已 push。

**未完成（處置）**：
- `design/littlesprout.pen` 內 7 處舊占位文字節點——票文「不做」明列不改設計稿、實作票不得碰 Pen（LS-209）；不修，留給下次真的碰這些板的設計票一併清。
- `docs/legal/README.md`／`docs/store/review-notes.md` 兩處還寫著 LS-241 修正前的「兩者不一致」舊敘述——本票範圍明訂「只動 DiaryDetailView＋註解＋測試」；不修，建議另開文件訂正票。
- 觀察到的既有缺陷（非本票造成，本票是第一個把 `InteractionRow` 放進這個版位而曝光）：iPad `DiaryDetailView` 右欄較窄時，留言按鈕文字＋計數視覺上被截斷成只剩圖示（accessibility label 仍正確）——`InteractionRow` 本身票文「不做」明列不得動；不修，建議另開票檢視窄寬度截斷處理。

**風險**：
- E2E 驗證期間 supabase-lock 到期自動釋放（iPad WDA 疑難排解超出原估 15 分鐘），釋放後另一票短暫持有 21 秒；已用 `docker exec … psql` 覆核測試資料完整無損。
- mobile-mcp 的 WDA agent 對「iPad Air 11-inch (M3)」＋iOS 26.0 組合會 timeout（兩台裝置皆重現），改用「iPad Pro 13-inch (M4)」才成功——環境限制，非程式碼缺陷。
- 本機無 iOS 26.2 runtime，push-gate／tap-target-check 用 26.0 量測，CI 會用正確版本再跑一次。

**產出位置**：
- PR #376：https://github.com/CLYEH/little-sprout/pull/376
- Head SHA：`c44bf5b1e6701fc2d7e6a84708f7dea4cb7131e4`
- Commits：`096a349`（feat）／`a1893d9`（test）／`c44bf5b`（fix：doc 註解）
- 證據：`.claude/evidence/LS-241/`（7 張截圖）
- 模擬器已關：`8DB5F20C-3429-4E57-A27E-98059EC5DD6C`（LS-241-iPhone17Pro）、`F2398C21-EE84-4269-8759-56D7FFC9BE8C`（qa-test-iPadPro13，借用後卸載 app 並關閉）、`F72DBCD1-9CBE-4903-AE6E-241064C391E1`（qa-test-iPadAir11M3，借用後關閉）；自建的 `LS-241-iPadAir11M3`（WDA 不相容）已刪除

### 2026-09-12T20:24:10.669Z · Chih-Lin Yeh · `f3dbc765`

**orchestrator 09-13 04:23**：R1 handoff 收到（PR #376，head `c44bf5b`，989 unit＋31 UI 綠、真後端兩裝置計數同步）→ In Review、派 merge-review R1（重點：與 #375 LS-237 `syncCommentCountIfKnown()` 的計數回寫路徑不得重複／互覆；store 單一實例）。handoff 三項「不修」中 `InteractionRow` iPad 窄寬計數截字（既有）→ 記 LS-96 池。

### 2026-09-12T21:00:44.119Z · Chih-Lin Yeh · `2d13d7a6`

**merge-reviewer 09-13 04:59 R1 ITERATE**

**PR** #376 · head `c44bf5b1e6701fc2d7e6a84708f7dea4cb7131e4`（審的就是這個 SHA；`git diff 144622d...c44bf5b`，6 檔 +91/−24）· base `development` · MERGEABLE
**Verdict：REQUEST_CHANGES**（1 major、2 minor、6 informational）
**環境**：`LS-241-iPhone17Pro 8DB5F20C-3429-4E57-A27E-98059EC5DD6C iOS 26.0（pinned 26.2）`——`scripts/gates/detect-simulator.sh` 有印本機無 26.2 的落差警告，版面量測與 CI（26.2）可能不一致。supabase lock：未持有（本票無 migration、未跑本機容器）。

---

## M1（major）iPad：詳情頁互動列的「留言」文字與計數被壓掉，票文範圍 1 的核心在 iPad 看不到

`LittleSprout/Features/Timeline/DiaryDetailView.swift:166`（`iPadLayout` 右欄裡的 `interactionRow`）

**具體失敗情境（兩處獨立證據）**

1. 實作者自附的 `.claude/evidence/LS-241/diary-detail-ipad.png`（iPad Pro 13" 直向、`NavigationSplitView` 側欄展開＝App 預設狀態）：互動列只剩「愛心／0／訊息圖示」，`commentButton` 的「留言」字樣與留言計數**完全不見**。
2. 我用同一份 Debug build 在自建的 iPad mini (A17 Pro)／iOS 26.0 上跑既有 harness（`SIMCTL_CHILD_LS_TAP_TARGET_GATE_SCREEN=DiaryDetailView`，744pt 全寬、**沒有**側欄）重現：「留言」被折成兩行（留／言）＋「0」；**同一台、同一份 build** 換成 `TimelineViewInteractionRow`（真 `TimelineView` 兩欄卡片）時，三張卡片的互動列全部正常顯示「愛心 3　留言 0」。截圖：`scratchpad/LS-241-mini-detail.png`、`scratchpad/LS-241-mini-timeline.png`。

**幾何**：右欄寬＝pane − 2×`screenPadLarge`(40) − 照片欄 360 − `block`(24)。13" 直向含側欄 pane≈712 → 右欄≈248pt；mini 全寬 744 → 280pt。`InteractionRow` 標準態橫向需求≈291pt（likeToggle 118 ＋ tight 6 ＋ countZone 48 ＋ block 24 ＋ commentButton≈95）。同寬度下時間軸卡片可用≈304pt（mini）／≈288pt（13" 含側欄）——**卡片還好好的寬度，詳情頁這個新版位已經壞**。另一個放大因素：`MasonryPhotoWallView` 那欄固定 `.frame(width: 360)`，日記沒有照片時（上面兩張證據都是無照片日記）仍照佔 360pt。

**為什麼不接受「這是 LS-216 既有元件缺陷、票文『不做』」**：mini 的對照實驗證明同寬度下卡片版位沒問題，退化是本票新版位造成的；而且修法**不必動 `InteractionRow`**，完全落在本 PR 已經動的檔案內。

**建議修法（擇一，皆 ≤5 行）**
- `iPadLayout`：把 `interactionRow` 移出 `HStack`，包一層 `VStack(alignment: .leading, spacing: AppSpacing.block)`（照片欄＋文字欄的 `HStack` 在上、互動列整寬在下），`.padding(AppSpacing.screenPadLarge)` 改掛外層 `VStack`；或
- 照片為空（`photos.isEmpty`）時不保留 360pt 欄寬。

修完請補一張「13" 直向＋側欄展開」狀態的截圖（實作者原本那張就是這個狀態，重照即可）——harness 是全寬，跑 UITest／`bash scripts/gates/tap-target-check.sh` 都不會紅（a11y label 仍正確回「留言，N 則」、熱區仍 ≥44pt），這條只有肉眼截圖抓得到。

## m1（minor，PLAUSIBLE）同畫面第三個 sheet 觸發源，與「⋯」非同步流程的呈現互斥窗

`LittleSprout/Features/Timeline/DiaryDetailView.swift:106`（`.overlay(commentsSheetHost)`）／`:270-283`；既有 `:103 .overlay(contentActionsSheetHost)`、`:120 .fullScreenCover(item: $playingVideo)`

**情境**：點「⋯」→ `openContentActions()` 起 `Task` 打 `fetchContentAuthor`（網路往返，期間「⋯」轉圈且 `disabled`，但留言鈕／計數鈕**沒有** disabled）→ 使用者在這個窗口點留言鈕 → 留言 sheet 先呈現 → 查詢回來設 `contentActionsContext`，此時已有 presented sheet，第二次呈現被系統丟棄；`contentActionsContext` 停在非 nil，且 `DiaryContentActionsContext.id == diaryID` 恆定，之後再點「⋯」設同 id 的新值，`.sheet(item:)` 視為同一個 item 不重新呈現 → 「⋯」在這個 view 實例上失效，退出重進才恢復。

標 PLAUSIBLE：要注入延遲才穩定重現，我沒有實跑；且「多個呈現來源＋非同步窗」在本票之前就存在（影片 `fullScreenCover` vs「⋯」），本票只是多加一個觸發源。**不擋本輪**，若要收斂：`onOpenComments` 加 `guard !isResolvingContentActions`（一行，本 PR 已動的檔內），或把三個呈現來源收斂成單一 enum 狀態。

## m2（minor）文件漂移：送審備註與法務對照表在本 PR 後變成錯誤敘述

`docs/store/review-notes.md:50-55`（送審備註，明寫「日記詳情頁的留言區目前也是畫面預留位置（`DiaryDetailView.swift` 顯示…）」並在「送審前待辦」要求送審時說明這塊還在開發中）、`docs/legal/README.md:58`（「兩者不一致，留給後續票處理」）。

票文驗收 1 字面是「全 repo 0 hit」，驗收 2 又要求「scope 只動 DiaryDetailView＋註解＋測試」——兩條互相衝突；實作者選 scope 並揭露，判斷合理。但 review-notes.md 是送審材料，錯誤敘述留著的風險高於一般文件，處置停在 handoff 的「建議另開票」不夠：請 orchestrator 裁示（就地兩行訂正，或立刻落成票／LS-96 池項）。

---

## 已驗證（我怎麼驗）

1. 票文驗收 4「tap-target／UITest 綠」——`bash scripts/gates/tap-target-check.sh 8DB5F20C-3429-4E57-A27E-98059EC5DD6C LittleSprout` exit 0（18m15s；這支會跑整個 `LittleSproutUITests` target 扣掉 `QASmokeTests`，含本票新增的 `DiaryDetailCommentsUITests` 與 `TapTargetGateTests`），輸出「已量測畫面（…DiaryDetailView…CommentsSheetView）的 Button／tappable 元件皆 ≥44×44pt」。
2. 對上 handoff 第 3 點「unit tests 989 全綠」——`xcodebuild test -only-testing:LittleSproutTests` 跑出 `Executed 989 tests, with 0 failures (0 unexpected)` ＋ `** TEST SUCCEEDED **`（log：`scratchpad/LS-241-unit.log`）。
3. 對上 handoff 第 9 點 mutation（LS-209 重放）——`git worktree add --detach` 到 scratchpad，照申報把 `onOpenComments: { showsCommentsSheet = true }` 改成 `onOpenComments: {}`，`xcodebuild test -only-testing:LittleSproutUITests/DiaryDetailCommentsUITests` exit 65，紅的是 `DiaryDetailCommentsUITests.swift:25: error: … XCTAssertTrue failed - 點擊詳情頁留言鈕應該開出留言 sheet`（16.942 秒真斷言失敗，不是 crash／build fail），與申報原文逐字相符；臨時 worktree 已 `git worktree remove`（log：`scratchpad/LS-241-mutation.log`）。
4. 對上 handoff 第 5 點 AX 通道——`bash scripts/gates/ax-launch-check.sh` exit 0（0 違規、1 項在允許期限內）；`swiftlint lint --strict --quiet` 無輸出。
5. M1 的 iPad 重現——自建 `LS-241-review-iPadPro13`／`LS-241-review-iPadMini`（iOS 26.0），裝同一份 Debug build，用 `LS_TAP_TARGET_GATE_SCREEN=DiaryDetailView`／`TimelineViewInteractionRow` 兩個 harness 畫面截圖比對，證據 `scratchpad/LS-241-mini-detail.png`／`scratchpad/LS-241-mini-timeline.png`／`scratchpad/LS-241-review-ipad-detail.png`（13" 全寬無側欄時不截斷，可對照）。兩台用完 `simctl delete`，`LS-241-iPhone17Pro` 已 shutdown；`simctl ui` 字級／外觀我沒有動過。
6. 票文驗收 1 前半「Swift 側 0 hit」與呼叫端補齊——在 `LittleSprout/Features/Timeline/DiaryDetailView.swift` 所在 worktree `git grep` 占位字串在 `LittleSprout`／`LittleSproutTests`／`LittleSproutUITests` 三目錄 0 hit（殘留只在 `design/littlesprout.pen` 與 `docs/store/review-notes.md`，見 m2／i1）；`DiaryDetailView(` 四個呼叫端（`TimelineView.swift:80`＋`TapTargetGateHarness+Safety.swift` 三個 host）全部補上新必填參數，沒有漏改。
7. Race 維度——`LittleSprout/Features/Timeline/TimelineView.swift:79-84` 的 `.navigationDestination` 把**同一個** `timelineStore` 參照傳給 `DiaryDetailView`（`TimelineStore` 是 `@MainActor @Observable` class），`InteractionRow` 的 `reactionState(forKey:)`／`commentCount(forKey:)` 與 `CommentsSheetView` 的 `setCommentCount` 都走 `LittleSprout/Services/Timeline/TimelineStore+Reactions.swift` 同一份 dict，單一事實來源，詳情頁與卡片不可能各自一套數字；詳情頁沒有多訂閱、也沒有多打任何 API。
8. 效能／平行維度——diff 無 SQL／RLS／分頁／圖片解碼改動；`InteractionRow` 只多兩次 O(1) dict 查詢；新 UITest `LittleSproutUITests/DiaryDetailCommentsUITests.swift` 用 `waitForExistence(timeout: 10)` 真等待、無 sleep、無固定座標斷言（符合 LS-167 規約）。
9. Scope——`git diff 144622d...c44bf5b` 只動 `DiaryDetailView.swift`／`TimelineView.swift`／`TapTargetGateHarness+Safety.swift`／`CommentDeleteConfirmationSheet.swift`（僅頭段註解，票文範圍 2）／新 UITest／`project.pbxproj`（xcodegen 同步新檔），無順手重構、無無關改動。

## informational（全列）

- i1 `design/littlesprout.pen` 7 處占位文字節點不修：票文「不做」明列不改設計稿、實作票不得碰 Pen（LS-209），處置成立。我**未複驗**「LS-177 沒有詳情頁專屬互動列板」這個說法（不動 Pen：`scripts/ops/pen-read.sh` 會影響其他 session 的 Pencil 連線），採信實作者，留給 QA／ui-designer 複核。
- i2 票文範圍 1 的核心「計數同步」目前沒有自動化回歸，新 UITest 只驗「留言鈕開得出 sheet」；同步性靠建構子傳同一個 store 的結構保證，日後有人在 route 另建 store 不會有測試轉紅。可考慮 harness host 先 `setCommentCount(3, forKey:)` 再斷言留言鈕 a11y label 等於「留言，3 則」。
- i3 跨 session 首次進場留言計數顯示 0（要開過一次 sheet 才「已知確切」）：`LittleSprout/Features/Content/CommentsSheetView.swift` 既有設計（LS-218），實作者已揭露，非本票缺陷。
- i4 `QAAccessibilityID.interactionRowElement` 只依 kind 區分、不含 refId；詳情頁推入後若底層時間軸卡片仍留在 AX 樹，`qa.interactionRow.diary.commentButton` 會有多個匹配。本輪 UITests 全綠、未撞到，記錄供 QA e2e 參考。
- i5 與 PR #375（LS-237）只有 `LittleSprout.xcodeproj/project.pbxproj` 重疊；#375 的 `syncCommentCountIfKnown()` 在 `LittleSprout/Features/Content/CommentsSheetView+Footer.swift`，與本 PR 無檔案重疊，且兩條回寫路徑都是 `timelineStore.setCommentCount(_:forKey:)` 寫同一個值（idempotent），不會互相覆蓋或重複計數。先併的那支之後，另一支可能要重跑 xcodegen 重出 pbxproj。
- i6 CI：`ci-ipad`／`lint`／`rules`／`db` 已綠（`gh run view 34716845905`），`ci` job 在我裁決當下仍 in_progress——iOS 26.2 的完整結果請 orchestrator 在併入前再確認一次。

## 未看的範圍

- Pen 稿板本身（i1）；真後端 E2E（實作者已跑，QA 會在 test branch 再驗）；CI `ci` job 的最終結果（裁決當下仍在跑）。

## 下一輪（R2）請帶

- M1 的修法與「13" 直向＋側欄展開」截圖；m1／m2 的處置（修／記 LS-96／另票／不修＋理由，一條不能省）。

---

`bash scripts/gates/handoff-evidence-check.sh <本文暫存檔> --repo .claude/worktrees/LS-241` → exit 0
```
✓ 第 46 行起的列項有證據
✓ 第 47 行起的列項有證據
✓ 第 48 行起的列項有證據
✓ 第 49 行起的列項有證據
✓ 第 50 行起的列項有證據
✓ 第 51 行起的列項有證據
✓ 第 52 行起的列項有證據
✓ 第 53 行起的列項有證據
✓ 第 54 行起的列項有證據
```

### 2026-09-12T21:59:01.231Z · Chih-Lin Yeh · `dec9754c`

**ios-dev 09-13 05:58 R2**

**Ticket**：LS-241（回應 merge-review R1 ITERATE，comment `2d13d7a6-b1ce-4ce9-b0fa-2294566280f3`）

**已完成（R2）**：
- **M1（major）已修** `41e929a`：`DiaryDetailView.swift` `iPadLayout`——`interactionRow` 移出照片欄＋文字欄的 `HStack`，改放最外層 `VStack` 整寬（`padding` 隨之改掛外層），未動 `InteractionRow` 本身，採 reviewer 建議的第一個修法。
- **範圍 5（m2 裁示）已修** `41e929a`：`docs/store/review-notes.md:50-55`、`docs/legal/README.md:58` 兩處「即將推出」／「兩者不一致」舊敘述改為現況一句話。
- **m1（minor，PLAUSIBLE）不修**：票文本輪「不做」段已明列（詳情頁三個 sheet 呈現來源與「⋯」`fetchContentAuthor` 窗口重疊），orchestrator 裁示不在本輪處理。

**已驗證（怎麼驗）**：
1. 全 repo「留言功能即將推出」0 hit（含 docs）——`git grep -rn "留言功能即將推出" .` 現在只剩 `design/littlesprout.pen` 7 處（票文「不做」明列不改設計稿）。
2. iPad 兩張截圖字樣＋計數完整：`.claude/evidence/LS-241/diary-detail-ipad13-portrait-sidebar-r2.png`（13" 直向、`NavigationSplitView` 側欄展開＝App 預設狀態，真後端登入走真實流程）、`.claude/evidence/LS-241/diary-detail-ipad-mini-r2-interactionrow.png`（iPad mini (A17 Pro)，`TapTargetGateHarness.diaryDetailHost` 744pt 全寬，同 reviewer 原重現手法）——兩張「愛心」「留言」字樣與計數皆完整不折行不截斷。
3. `xcodebuild test -only-testing:LittleSproutTests`：989 全綠。
4. `xcodebuild test -only-testing:LittleSproutUITests/{DiaryDetailCommentsUITests,ContentActionsUITests,InteractionRowUITests,CommentsSheetUITests,DeleteConfirmationUITests}`：31 全綠。
5. `swiftlint lint --strict --quiet` 乾淨；`bash scripts/gates/ax-launch-check.sh` 通過。
6. `bash scripts/gates/tap-target-check.sh 8DB5F20C-3429-4E57-A27E-98059EC5DD6C LittleSprout` exit 0（含 `DiaryDetailView`／`CommentsSheetView`）。
7. push-gate 全綠，已 push `41e929a`。

**未完成（處置）**：見上方「已完成」的 m1 disposition；其餘 R1 informational（i2–i6）reviewer 已逐條判定非本票需處理項，不修，詳見 PR body。

**風險**：
- R2 真後端驗證另開一次 supabase-lock hold（「LS-241 R2 dev E2E ipad screenshot」），持有 3 分 34 秒，正常 `--release`。
- mobile-mcp 的 WDA agent 對 iPad mini (A17 Pro)／iOS 26.0 同樣 timeout（環境限制），改用 `xcrun simctl launch`（`SIMCTL_CHILD_LS_TAP_TARGET_GATE_SCREEN=` 注入）＋`xcrun simctl io screenshot`（純 simctl，不依賴 WDA）取得該截圖。
- 本機無 iOS 26.2 runtime，量測用 26.0，CI 會用正確版本再跑一次。

**產出位置**：
- PR #376：https://github.com/CLYEH/little-sprout/pull/376（body 已更新含 R2 內容）
- Head SHA：`41e929a`
- Commits（新增）：`41e929a`（fix：M1 iPad 版面＋範圍 5 docs）
- 證據：`.claude/evidence/LS-241/`（新增 `diary-detail-ipad13-portrait-sidebar-r2.png`／`diary-detail-ipad-mini-r2-interactionrow.png`；R1 的 `diary-detail-ipad.png` 保留作修復前對照）
- 模擬器已關：`8DB5F20C-3429-4E57-A27E-98059EC5DD6C`（LS-241-iPhone17Pro）、`F2398C21-EE84-4269-8759-56D7FFC9BE8C`（qa-test-iPadPro13，借用後卸載並關閉）；R2 自建 `D4C48E57-...`（LS-241-iPadMini，WDA 不相容）已刪除

### 2026-09-12T22:09:18.853Z · Chih-Lin Yeh · `55d1332c`

**orchestrator 09-13 06:08**：R2 handoff 收到（`dec9754c`，head `41e929a`；互動列移出兩欄整寬、docs 兩處同步、989 unit＋31 UI 綠）→ 派 merge-review R2（含與剛併入 dev 的 LS-237 計數回寫交叉核）。

### 2026-09-12T22:51:18.745Z · Chih-Lin Yeh · `f71f5acc`

**merge-reviewer 09-13 06:50 R2 APPROVE**

**PR** #376 · head `41e929af6efd57f1c60e566c222a8e94f333d0f4`（審的就是這個 SHA；R2 delta `git diff c44bf5b..41e929a`＝3 檔 +32/−21）· base `development` · `mergeable=MERGEABLE`（`mergeStateStatus=BLOCKED` 只因我 R1 貼的 merge-review failure，非 DIRTY）
**Verdict：APPROVE**（0 blocker／0 major；R1 的 M1 已修並複驗，m2 已修，m1 依 orchestrator 裁示不修）
**環境**：`LS-241-iPhone17Pro 8DB5F20C-3429-4E57-A27E-98059EC5DD6C iOS 26.0（pinned 26.2）`；另自建 `LS-241-review-r2-iPadMini`（iPad mini A17 Pro）與 `LS-241-review-iPadAir11M3`（對照 ci-ipad 機型），皆 iOS 26.0，用完 `simctl delete`。supabase lock：未持有。

---

## R1 findings 逐條結案

- **M1（major）已修、已複驗**：`LittleSprout/Features/Timeline/DiaryDetailView.swift:158-183` `iPadLayout` 把 `interactionRow` 移出兩欄 `HStack`、放最外層 `VStack` 整寬，`padding` 隨之改掛外層，未動 `InteractionRow`——採 R1 建議的第一個修法。實作者兩張新截圖我逐張看過：`.claude/evidence/LS-241/diary-detail-ipad13-portrait-sidebar-r2.png`（13" 直向＋側欄展開，真後端）「愛心 0」「留言 0」字樣與計數完整；`.claude/evidence/LS-241/diary-detail-ipad-mini-r2-interactionrow.png`（mini harness）同樣完整。**我在自建 iPad mini (A17 Pro) 上重放 R1 那次量測**（同手法 `SIMCTL_CHILD_LS_TAP_TARGET_GATE_SCREEN=DiaryDetailView`，744pt 全寬）：R1 折成兩行的「留言」現在單行完整＋計數可見，證據 `scratchpad/LS-241-r2-mini-detail.png`。
- **m2（minor）已修**：`docs/store/review-notes.md`／`docs/legal/README.md` 兩處改為現況敘述；票文驗收 1「全 repo 0 hit」現在含 docs 成立——`grep -rn "留言功能即將推出" .`（排除 `.git`）只剩 `design/littlesprout.pen` 7 處（票文「不做」明列不改設計稿）。
- **m1（minor，PLAUSIBLE）不修**：orchestrator 裁示不在本輪處理＋記 LS-96，接受。**一點更正供紀錄**：handoff 寫「票文本輪『不做』段已明列」與票文原文不符（票文「不做」只列「改互動列或留言 sheet 本身；Realtime；改設計稿」，沒有提這條 sheet 呈現互斥窗）——實際依據是 orchestrator 裁示，請確保它真的落進 LS-96 池，不要只停在 handoff 字面。

## R2 新增檢查

- **與 LS-237（#375，已併 development）的計數回寫沒有重複或互覆**：合併後全樹只有兩個 `setCommentCount(_:forKey:)` 呼叫端，都在 `LittleSprout/Features/Content/CommentsSheetView.swift:132`（`.onChange(of: store.knownExactCount)`）與 `LittleSprout/Features/Content/CommentsSheetView+Footer.swift:96`（LS-237 的 `syncCommentCountIfKnown()`），兩者寫的都是 `store.knownExactCount` 這個**絕對值**（`hasEarlier ? nil : comments.count`）、不是增量，重複寫入是同值 no-op；本 PR 的 `DiaryDetailView` 只讀不寫，沒有新增第三條回寫路徑。兩者對詳情頁是互補的：sheet 在送出飛行中被關閉時，LS-237 那條直接寫進共用的 `timelineStore`，詳情頁背後的 `InteractionRow` 照樣更新。
- **合併可行性**：在暫時 worktree（不動票 worktree）`git merge origin/development` **無衝突**；合併後 `project.pbxproj` 與 `xcodegen generate` 產物**逐位元組相同**（鏡射暫存目錄比對，同 push-gate 漂移檢查手法）。
- **ci-ipad 的紅是既有 flake、與本票無關**：R2 head 的 `ci-ipad` 一度 fail（`SettingsViewIPadTests.testContentSafetySectionStorageRowPushesAndBackReturns`，`SettingsViewIPadTests.swift:98 XCTAssertTrue failed - 切到「內容與安全」後應該看得到「儲存空間」列`）——(a) 該測試在 Settings 畫面，本 PR 一行都沒碰；(b) 同檔另外兩支測試才剛因為同型 flake 被 LS-237 `96734b7` 補同步點，這支用的正是沒補到的舊寫法（`tap()` 後直接 `waitForExistence`）；(c) development 自己併 #375 後的 run `ci-ipad` 綠；(d) 我在合併後的暫時 worktree、用 ci-ipad 同機型 iPad Air 11-inch (M3) 實跑 `SettingsViewIPadTests` 6/6 綠；(e) **同 SHA 重跑 ci-ipad 已轉綠**（run 34721409942）。目前 41e929a 的 `ci`／`ci-ipad`／`db`／`lint`／`rules` 全綠。建議把這支測試的同步點補進 LS-96 池（與 LS-237 第 8 項同性質）。

## 已驗證（我怎麼驗）

1. 票文驗收 4 與 M1 回歸——`bash scripts/gates/tap-target-check.sh 8DB5F20C-3429-4E57-A27E-98059EC5DD6C LittleSprout` 對 R2 head exit 0（18m18s，整個 `LittleSproutUITests` target 扣掉 `QASmokeTests`，含 `DiaryDetailCommentsUITests`／`TapTargetGateTests`），輸出含 `DiaryDetailView`／`CommentsSheetView`。
2. M1 修復的獨立重現——自建 iPad mini (A17 Pro)／iOS 26.0，裝 R2 build 跑 harness `DiaryDetailView`，截圖 `scratchpad/LS-241-r2-mini-detail.png`：「愛心 0」「留言 0」完整；對照 R1 同手法的 `scratchpad/LS-241-mini-detail.png`（當時「留言」折行）。
3. 合併後行為（LS-237 交互）——暫時 worktree `git merge origin/development` 後 `xcodebuild test -only-testing:LittleSproutTests -only-testing:LittleSproutUITests/DiaryDetailCommentsUITests -only-testing:LittleSproutUITests/CommentsSheetUITests -only-testing:LittleSproutUITests/InteractionRowUITests -only-testing:LittleSproutUITests/ContentActionsUITests`：unit `Executed 1005 tests, with 0 failures`、UI `Executed 28 tests, with 0 failures`、`** TEST SUCCEEDED **`（log `scratchpad/LS-241-merged-test.log`）。
4. 合併後 pbxproj 無漂移——鏡射暫存目錄跑 `xcodegen generate` 後 `diff` 與 repo 側 `LittleSprout.xcodeproj/project.pbxproj` 相同（同 `scripts/gates/push-gate.sh` 的檢查手法）。
5. ci-ipad flake 判定——`gh run view --job 103627867933 --log-failed` 取原文；`xcodebuild test -only-testing:LittleSproutUITests/SettingsViewIPadTests` 在合併後 worktree、ci-ipad 同機型 iPad Air 11-inch (M3) 6/6 綠（log `scratchpad/LS-241-ipadair-merged.log`）；`gh run rerun 34721409942 --failed` 同 SHA 重跑 ci-ipad 轉綠。
6. 票文驗收 1（含 docs）——`grep -rn "留言功能即將推出" .` 在 `.claude/worktrees/LS-241` 只剩 `design/littlesprout.pen` 7 處；`docs/store/review-notes.md`／`docs/legal/README.md` 新敘述我逐行讀過，與現況相符。
7. Scope（R2 delta）——`git diff c44bf5b..41e929a` 只動 `LittleSprout/Features/Timeline/DiaryDetailView.swift`（`iPadLayout`＋兩段文件註解）與兩份 docs，沒有順手改動；UITest 檔在 R2 未變動，R1 已做過的 mutation 重放（`onOpenComments: {}` → 該測試轉紅，原文相符）對 R2 仍成立。
8. Race／效能／平行（R2 delta）——純版面與文件變更，無新增併發、無新增查詢；`interactionRow` 仍讀同一個 `timelineStore`（`LittleSprout/Features/Timeline/TimelineView.swift:79-84` 傳同一參照），單一事實來源不變。

## informational（全列）

- i1 `design/littlesprout.pen` 7 處占位文字節點：票文「不做」明列，處置成立；「LS-177 無詳情頁互動列板」我兩輪皆未複驗（不動 Pen），留給 QA／ui-designer。
- i2（R1 i2 延續）計數同步仍無自動化回歸，靠建構子傳同一個 store 的結構保證＋人工 E2E；建議 LS-96 池（harness host 種 `setCommentCount(3, forKey:)` 後斷言留言鈕 a11y label）。
- i3 留言計數在新 session 首次進場顯示 0（要開過一次 sheet 才「已知確切」，LS-218 既有設計）——新的 `docs/store/review-notes.md` 措辭沒有宣稱「留言計數立即顯示」（只說愛心計數／按讚名單／留言清單可互動，且詳情頁與卡片計數一致），敘述成立；寫最終送審備註的人請留意這個細節。
- i4（R1 i4 延續）`QAAccessibilityID.interactionRowElement` 只依 kind 區分、不含 refId；兩輪 UITests 未撞到多重匹配。
- i5 iPad 詳情頁若該篇日記沒有照片，左側 360pt 照片欄仍保留空白，整寬互動列會落在空白區下方（本次兩張 iPad 截圖都是這個情形）——純視覺，非本輪擋點；若要收斂可在 `photos.isEmpty` 時收掉照片欄（我 R1 提的第二個修法），建議記 LS-96。
- i6 量測 runtime 仍是 iOS 26.0（本機無 26.2）；CI 26.2 全綠（含重跑後的 ci-ipad），落差本輪無實際影響。
- i7 `SettingsViewIPadTests.testContentSafetySectionStorageRowPushesAndBackReturns` 缺同步點（見上）——非本票造成，建議記 LS-96／併入 LS-237 同性質待辦。

## 未看的範圍

- Pen 稿板（i1）；真後端 E2E（實作者已跑，QA 會在 `test` branch 再驗）；LS-237 本身的程式碼（只看與計數回寫相關的兩個呼叫端與合併後測試結果）。

---

`bash scripts/gates/handoff-evidence-check.sh <本文暫存檔> --repo .claude/worktrees/LS-241` → exit 0
```
✓ 第 23 行起的列項有證據
✓ 第 24 行起的列項有證據
✓ 第 25 行起的列項有證據
✓ 第 26 行起的列項有證據
✓ 第 27 行起的列項有證據
✓ 第 28 行起的列項有證據
✓ 第 29 行起的列項有證據
✓ 第 30 行起的列項有證據
```

### 2026-09-12T22:52:34.079Z · Chih-Lin Yeh · `63b60c74`

**orchestrator 09-13 06:52**：merge-review R2 APPROVE（`f71f5acc`，status 貼 41e929a；iPad mini 重放量測單行完整、與 LS-237 計數回寫無重複、合併後 1005 unit＋28 UI 綠）→ #376 併入 development `9cf2245`（dev tip status 已貼）→ 與 LS-237／LS-242 同批 promote dev→test → QA（三張截圖＋真後端留言後回列表計數一致）。ci-ipad 既有 flake 那支同步點記 LS-96。

### 2026-09-12T23:29:50.255Z · Chih-Lin Yeh · `c0890662`

**orchestrator 09-13 07:29**：promote dev→test ✓（test `9cf2245`）→ QA 派出（占位字串 0 hit、`DiaryDetailCommentsUITests`、淺色／深色／AX3／iPad 13" 側欄四張截圖、真後端詳情頁留言後回列表計數一致；與 LS-237 同批同一 lock）。

### 2026-09-13T00:29:34.033Z · Chih-Lin Yeh · `a6dbcbb9`

**qa 09-13 08:26 PASS（test 9cf2245）**

## 已驗證（怎麼驗）

- **驗收 1「留言功能即將推出」全 repo 0 hit（含 docs）**：`git grep -n "留言功能即將推出" -- . ':!design/littlesprout.pen'` exit 1（0 hit）；`git grep -c "留言功能即將推出" -- .` 只剩 `design/littlesprout.pen:7`（票文「不做」明列不改設計稿）。`docs/store/review-notes.md:50-55`、`docs/legal/README.md:58` 兩處已讀過內容，均改為「LS-241 起日記詳情頁與時間軸卡片共用同一套互動列⋯不再是預留位置」等現況敘述。
- **詳情頁互動列與時間軸卡片計數一致（截圖對照）**：真後端在時間軸發一篇日記，開詳情頁點留言送出一則 → 詳情頁 `InteractionRow` 立即顯示「留言，1 則」（截圖 `.claude/evidence/LS-241/qa/detail-after-comment-count1.png`），返回時間軸卡片同樣顯示「留言，1 則」（截圖 `.claude/evidence/LS-241/qa/timeline-after-comment-count1.png`）——不需重開 app、兩處數字一致。
- **`DiaryDetailCommentsUITests` 綠**：`xcodebuild test -only-testing:LittleSproutUITests/DiaryDetailCommentsUITests` → 1 test, 0 failures（`testCommentButton_opensCommentsSheet` 8.378s，log `scratchpad/LS-237-241-qa-uitests-iphone.log`）。
- **截圖三張（淺色 iPhone／深色／AX3）**：詳情頁互動列「愛心」「留言」字樣與計數字皆完整不截斷——`.claude/evidence/LS-241/qa/diary-detail-iphone-light.png`（appearance light）、`diary-detail-iphone-dark.png`（appearance dark）、`diary-detail-iphone-ax3.png`（`simctl ui content_size accessibility-extra-extra-extra-large`，即 AX3；截圖後已復原 `content_size large`／`appearance light`）。三張皆用 `LS_TAP_TARGET_GATE_SCREEN=DiaryDetailView` harness（`.preview()` 假資料，不打真網路），畫面即 R2 修復後的 `iPadLayout` 以外分支（compact），互動列完整顯示「愛心 0」「留言 0」。
- **iPad 13" 直向側欄展開一張**：真後端在 `qa-test-iPadPro13`（13 吋機型）以既有帳號（前一輪 ios-dev/merge-reviewer 留下的 session）打開時間軸→點日記卡進詳情頁，`NavigationSplitView` 側欄展開（左側可見「時間軸／相簿／寶貝／設定」四項），右側詳情頁互動列完整顯示「愛心 0」「留言 0」，`commentButton` 元件量測寬度 101pt（566–667）不截斷不折行——截圖 `.claude/evidence/LS-241/qa/diary-detail-ipad13-portrait-sidebar.png`，對照 R1 reviewer 指出「留言」被壓成兩行的失敗態，本輪已完全修復。
- **真後端留言一則後回列表計數一致**：見上方「詳情頁互動列與時間軸卡片計數一致」項，同一次操作即涵蓋（時間軸→詳情→送留言→回時間軸，計數兩處皆為 1）（同 `.claude/evidence/LS-241/qa/detail-after-comment-count1.png`／`timeline-after-comment-count1.png`）。

## 未完成
- 無（票文兩條驗收項全部驗證通過）。

## 風險
- 無新增風險；iPad 螢幕截圖使用的帳號／日記資料為前一輪 ios-dev/merge-reviewer 在同一台 `qa-test-iPadPro13` 留下的既有 session（`docker exec` 未重置該裝置的 app 資料），與 LS-237/LS-241 iPhone 側各自獨立建的新帳號 `qa-ls237-241@ls.test` 不衝突，純視覺驗收用途不影響裁決。

## 產出位置
- 證據：`.claude/evidence/LS-241/qa/`（8 張：3 張 iPhone 外觀＋1 張 iPad 側欄＋1 張 iPad 無側欄 harness 對照＋3 張留言計數同步）
- Log：`scratchpad/LS-237-241-qa-uitests-iphone.log`
- lock 已釋放：LS-237/LS-241 QA e2e（持有 20 分 48 秒，與 LS-237 同一次 hold）
- 模擬器已關：A73356D3-1252-4B19-9480-838912C08F67（main-iPhone17Pro）、F2398C21-EE84-4269-8759-56D7FFC9BE8C（qa-test-iPadPro13）

### 2026-09-13T00:40:54.164Z · Chih-Lin Yeh · `85a6d97c`

**orchestrator 09-13 08:40 QA PASS**（`a6dbcbb9`；占位字串 0 hit、`DiaryDetailCommentsUITests` 綠、四張截圖含真後端 iPad 13" 側欄展開留言鈕 101pt 無折行、詳情頁與時間軸卡片計數即時一致）。qa status 已貼 9cf2245 → 派 sweeper → 收尾 → Done。

### 2026-09-13T00:46:20.553Z · Chih-Lin Yeh · `e86c31be`

**dead-code-sweeper 09-13 08:45**

## dead-code sweep — LS-241

範圍：`git diff 096a349~1 41e929a -- LittleSprout docs LittleSproutUITests LittleSproutTests`（4 commits，R1→R2 全量，`--stat` 逐檔核對與 PR 範圍一致，對照 worktree `qa-test` @ 9cf2245）。

### Findings

未發現本票引入的死碼。逐項覆核：

- **`commentsPlaceholder` 與其 doc 註解 0 hit**：`grep -rn "commentsPlaceholder" --include="*.swift"` 全 repo 只剩 `DiaryDetailView.swift:14` 的一句「回顧沿革」文件註解（提到這個已移除的舊名稱，說明這裡不重複引用原字面），沒有任何實際宣告或呼叫點——符合票文驗收「這裡不重複引用原字面」的寫法，非死碼殘留。`CommentDeleteConfirmationSheet.swift:9` 引用該名稱的舊註解已改寫（比對 diff，完整替換非新舊並存）。
- **「留言功能即將推出」全 repo 0 hit**：`grep -rn "即將推出"` 只命中 `design/littlesprout.pen`（票文「不做」明列不改設計稿，且 LS-241 QA 已核過此點，`scratchpad/LS-241-qa-verdict.md` 有紀錄）與 `scratchpad/`（QA 過程筆記，非程式碼／文件），不算死碼。`docs/store/review-notes.md:50-55`／`docs/legal/README.md:58` 兩處已完整改寫為現況敘述，無新舊並存。
- **`commentsSheetHost` 只一個呼叫點**：`DiaryDetailView.swift` 的 `private var commentsSheetHost`（`:285`）只在 `:111` 的 `.overlay(commentsSheetHost)` 被呼叫一次——這是**既有慣例**，同檔 `contentActionsSheetHost`（`:105`「用 overlay 集中多個 sheet modifier」寫法）與 `TimelineView.commentsSheetHost`（`TimelineView+Comments.swift:9`，同樣單一呼叫點）都是同一套「把 `.sheet` 抽成獨立 computed property 掛在 `.overlay`」的模式，不是死碼、是刻意的程式碼組織慣例。
- **`commentAPIClient` 注入在 `TapTargetGateHarness+Safety.swift`（觀察，非本票死碼）**：三個 host（`diaryDetailHost`／`diaryDetailOwnContentHost`／`diaryDetailRoleNotReadyHost`）都因為 `DiaryDetailView` 新增必要參數而餵入 `PreviewCommentAPIClient()`——其中只有 `diaryDetailHost`（`.diaryDetail` screen）被新增的 `DiaryDetailCommentsUITests.testCommentButton_opensCommentsSheet` 真的觸發留言 sheet；另兩個 host 只被 `ContentActionsUITests`（既有，跟留言無關）使用，`commentAPIClient` 在那兩處目前無測試路徑會真的呼叫進 `CommentsSheetView`。**判定：非死碼**——這是既有慣例（每個 harness host 都要把 View 宣告的相依性餵滿，不是只餵「這支測試會用到的」那些，其他既有參數如 `diaryAPIClient`／`safetyAPIClient` 在部分 host 上同樣未必被對應的 UITest 路徑實際觸發）；純粹是覆蓋率觀察，不是引用死結。
- **`DiaryDetailCommentsUITests` 斷言非空洞**：`QAAccessibilityID.interactionRowElement(kind: "diary", element: "commentButton")` 確認在 `InteractionRow.swift:159` 有對應 `.accessibilityIdentifier` 賦值；斷言的空狀態文字「還沒有人留言」確認在 `CommentsSheetView+States.swift:63` 存在且與 `PreviewCommentAPIClient.listComments` 固定回傳 `[]`（`:13-15`）的行為一致——不是斷言了一個不會出現的字串。
- **docs 兩處改寫無殘留「即將推出」**：見上方 grep 結果，`docs/store/review-notes.md`／`docs/legal/README.md` 兩處新舊敘述完整替換，無並存段落。

### 既有死碼觀察

無（`commentAPIClient` 覆蓋率缺口已在上方列為觀察，非死碼，且屬程式碼組織慣例延伸，不建議另開票）。

### 建議處置

無需開票。`commentAPIClient` 在 `diaryDetailOwnContentHost`／`diaryDetailRoleNotReadyHost` 兩處覆蓋率缺口若要收斂，屬「錦上添花」型測試補強，量級極小（兩個既有 host 沒有涉及留言互動的測試場景），不記入 LS-96（低於待辦池收件門檻）。

### 搜尋方法與盲區聲明

- 方法：`git diff --stat` 核對票範圍與 PR 逐檔一致；`grep -rn` 交叉比對符號宣告與呼叫點；對 UITest 斷言額外核對 accessibility ID 與空狀態文案在原始碼中確實存在（非憑空斷言）。
- 盲區：純文字 grep 抓不到 reflection／字串拼接／SwiftUI 隱式引用（`@main`／`PreviewProvider`／protocol conformance）；未重新執行 `xcodebuild test` 驗證斷言实際會通過，僅靜態核對符號存在與 QA/merge-review 既有紀錄一致；`design/littlesprout.pen` 內容只用文字 grep 掃描字面（.pen 為明文 JSON，未用 Pencil MCP 解析語意結構）。

### 2026-09-13T00:48:22.287Z · Chih-Lin Yeh · `ac8daf73`

**orchestrator 09-13 08:47 收尾儀式 → Done**

- ① 死碼巡檢 ✓（sweeper `e86c31be`）：占位與字串 0 hit（`.pen` 7 處為票文不做）；`commentsSheetHost` 單呼叫點符合既有慣例；觀察：`TapTargetGateHarness+Safety.swift` 兩 host 的 `commentAPIClient` 注入未被開留言的 UITest 走到（慣例上完整注入，不動）。
- ② Lesson learning review：
  - harness：**iPad 兩欄版位塞既有組件要先量可用寬**（R1 M1：右欄 248–280pt < `InteractionRow` 291pt；所有既有 gate——a11y／熱區／harness 全寬——都不會紅，只有截圖看得到）→ 派工單對 iPad 版位改動一律要求「13" 直向＋側欄展開」與「mini」兩張截圖（已在票文 3 落地；寫進 dispatch 規則）。
  - 流程：驗收「全 repo 0 hit」與 scope 條款衝突時，裁示改票文加範圍（05:03），不用 SendMessage 加碼——照 LS-236 教訓執行、順利。
  - 工具缺口：無。
- ③ 清理 ✓：`cleanup-merged.sh --apply LS-241` → worktree 1、專屬模擬器 1、DerivedData 3 已清。
- ④ Pen 清場：不適用。

QA PASS（`a6dbcbb9`，test `9cf2245`）；隨 v0.24.0 隨版。

