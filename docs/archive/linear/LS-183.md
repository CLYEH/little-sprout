# LS-183 Harness：PreToolUse 擋繞過 `supabase-lock.sh` 的本機容器操作——`docker exec` 進 `supabase_*` 容器、`psql`／連線字串打 54322、`supabase functions serve`／`supabase db query`（非 `--linked`）未經 lock 包裝或非持有者一律 deny（LS-96 `e381f653` 第 1 項）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-04T23:39:41.477Z |
| 開始 | 2026-09-04T23:39:41.622Z |
| 完成 | 2026-09-05T02:46:41.782Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-183/harnesspretooluse-擋繞過-supabase-locksh-的本機容器操作-docker-exec-進-supabase |
| 關係 | related→LS-182, related→LS-159, related→LS-180, related→LS-88, related→LS-144, related→LS-104, related→LS-175, related→LS-181, related→LS-96, related→LS-143, related→LS-179, related→LS-149, related←LS-264, related←LS-184 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項 `e381f653` 第 1 項（P2，[LS-143](https://linear.app/little-sprout-app/issue/LS-143/taskls-24-後端-刪除帳號-rpc唯一-owner-須先轉移成員內容軟刪併發無死鎖) QA `5203b497`，2026-09-03）：QA 直接 `docker exec` 進本機 Supabase 容器撞上 [LS-149](https://linear.app/little-sprout-app/issue/LS-149/taskls-23-後端-檢舉封鎖owner-移除內容-rpccontent-reportsblocked-users) 的 mid-reset；lock 只靠派工文約束。現況（2026-09-05 查證）：`scripts/hooks/pretool.sh` H3 規則只擋「`supabase db reset` 或 `run.sh` 未經 `scripts/ops/supabase-lock.sh --` 包裹且非重入」（`pretool_engine.py` `FB_SUBARESET_RE`／`WRAPPER_RE`／lock holder 判定 :653-818），`docker`／`psql` 兩檔 0 命中——繞過路徑仍開著。今日 [LS-175](https://linear.app/little-sprout-app/issue/LS-175/taskls-22-後端-media-新增彙總通知事件批次上傳-50-張一則爸爸新增了-50-張照片after-insert) dev 分兩次包裝被插隊、[LS-179](https://linear.app/little-sprout-app/issue/LS-179/taskls-23-後端-營運防線使用者家庭停權旗標profilessuspended-atfamiliessuspended) QA 誤持有主 checkout，lock 事故頻率支持升票；巡檢 07:4x lane:harness 空（[LS-181](https://linear.app/little-sprout-app/issue/LS-181/harnessmigration-breaking-check-加-alter-type-add-value-規則-enum) 進 QA）。

## 範圍

1. `scripts/hooks/pretool_engine.py`（＋`pretool.sh` 規則表）新規則 H3b：命令文字含以下任一且**未經** `supabase-lock.sh --` **包裝、也不是目前 lock 持有者的 worktree**（沿用既有 holder 判定 :805-818）→ deny，訊息指向 `bash scripts/ops/supabase-lock.sh -- <cmd>` 或 `--hold`：
   * `docker exec`／`docker compose exec` 對 `supabase_*`／`supabase-*` 容器名；
   * `psql` 或連線字串（`postgresql://`／`postgres://`）指向 `127.0.0.1:54322`／`localhost:54322`／`54322`；
   * `supabase functions serve`、`supabase db query`／`supabase db dump`／`supabase migration up` **不帶** `--linked`（本機容器）；
   * 唯讀例外：`docker ps`／`docker logs`／`supabase status`／`supabase-lock.sh --status` 不擋。
2. 正負樣本自測（既有 pretool 自測檔慣例）：每類正樣本 ≥3（含 heredoc／引號內字面不誤擋，沿 [LS-104](https://linear.app/little-sprout-app/issue/LS-104/harnesspretoolsh-誤擋精修-只在命令列位置比對heredoc引號字串echo-內容排除保留歧義即-denyls-88-生效後) 只在命令列位置比對）、負樣本 ≥3（包裝過／持有者／`--linked`／唯讀例外）；bash 3.2。
3. `.claude/agents/qa.md`／`ios-dev.md`／`merge-reviewer.md` lock 規則段補一句「`docker exec`／`psql`／`functions serve` 同樣要在 lock 內」；COLLABORATION §7 對照表該列 gate 欄補上。
4. [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `e381f653` 第 1 項銷除註記（第 2 項 CI flake 案名另追）。

## 不做

每 worktree 獨立容器；`--linked`（正式站）操作的分類器問題；CI 上的容器操作（CI 無 lock）。

## 驗收

- [ ] 未包裝 `docker exec supabase_db_… psql` → deny 並印包裝提示；包裝後／持有者／`--linked`／`docker ps` → 放行（自測）
- [ ] 真環境：對本 session 目前的 lock 狀態跑一次 `pretool` dry-run 樣本，結果與預期一致
- [ ] merge-reviewer 通過；hotfix 流程（自 main、PR 回 main、back-merge）

## 留言（12）

### 2026-09-04T23:40:06.083Z · Chih-Lin Yeh · `da8c04b2`

**2026-09-05 07:40 orchestrator — 派工**

- lane:harness 空（LS-181 進 QA）→ 依 LS-144／§5-b 升池項 `e381f653` 第 1 項（同級 P2 中 size 最小）；cycle 1；hotfix 流程：worktree `.claude/worktrees/LS-183`，分支 `hotfix/LS-183-pretool-lock-bypass`（base origin/main `f304a13`），PR → main，併入後 back-merge。
- 注意：`.claude/settings.json` 指向**主 checkout**的 `scripts/hooks/pretool.sh`，併入 main 並 pull 後立即對所有在飛 agent 生效——自測只在 worktree 副本跑，真環境驗證用 dry-run 模式。
- 派 general-purpose R1。

### 2026-09-05T00:07:32.458Z · Chih-Lin Yeh · `8f5112ea`

## handoff（LS-183，2026-09-05 08:05）

**Ticket**：LS-183 — Harness：PreToolUse 擋繞過 `supabase-lock.sh` 的本機容器操作（H3b）。來源 LS-96 池項 `e381f653` 第 1 項（LS-143 QA `5203b497` 直接 `docker exec` 撞 LS-149 mid-reset）。hotfix 自 `main` `f304a13` 切、PR 回 `main`；全程只在 worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-183`，主 checkout 未碰（hook 仍指向主 checkout 舊版，本票改動併入 main 前不生效）。

**已完成**（SHA＋file:line）：
1. `181d38e` H3b 規則——`scripts/hooks/pretool_engine.py`：常數／regex :166-180（`H3B_CONTAINER_RE`／`H3B_PORT_RE`／`H3B_CONNSTR_RE`／`H3B_SUPABASE_LOCAL_PAIRS`／`H3B_TEXT_VERBS`＋四條 `FB_*` 字面版）；`check_precise` 狀態 :625-626、token 比對 :696-712、回傳 :745-750（`("H3B_TRIGGER", what)`，排在 H3 之後）；`check_fallback` :777-783；`evaluate()` 新 `cwd` 參數＋逐段 `cd`／`pushd` 追蹤 :791-870（遞迴帶 `cur_dir`）；`_resolve_trigger` :873；`_cd_target` :882；`_h3b_check` :915（包裝字面 → H3 重入 → 持有者 worktree，三者皆否才 deny，訊息指向 `supabase-lock.sh -- <cmd>` 或 `--hold`）；holder 檔改 `_holder_fields` :929 一次讀、`_h3_reentrant` :959 改用它（不複製第二份）；`_alive` :980／`_toplevel_of` :994（上溯找 `.git`，不呼叫 git）／`_h3b_holder_worktree` :1008（沿 `supabase-lock.sh:249-254` `hold_owner_ok` 的 worktree 腿：`cmd=hold:*`、pid 活著、`worktree=` realpath 相同）；`main()` 讀 `PRETOOL_CWD` :1029。`scripts/hooks/pretool.sh`：規則表 H3b :25-35；JSON 第六欄 `cwd` :194／:218（jq／python3 兩路）；`PRETOOL_CWD` 傳引擎 :155。
2. `89999fd`＋`f41ce1c` 自測——`scripts/hooks/pretool.test.sh` :489-594：H3b 46 組（a 六／b 六／c 五 deny；包裝 3、持有者 worktree 10、`--linked` 3、唯讀 7、heredoc／echo／grep／commit message／別 port／遠端 6 allow）；`bash_json_cwd` :497 帶 hook JSON `cwd`；假 lock 目錄＋假 worktree（`.git` 檔）。`f41ce1c`：h⑨ 死 pid 樣本改 `sh -c 'echo $$'`（首版 `sleep 300 & kill` 競速沒殺到、`wait` 卡整份自測 5 分鐘——在 dry-run 那次跑出來的，已修）。
3. `a9a2f05` 規約與 gate——`.claude/agents/ios-dev.md:15`／`qa.md:17`／`merge-reviewer.md:8` lock 段各補一句（含唯讀例外清單；字樣 `本機容器操作同樣要在 lock 內`）；`scripts/gates/agent-tools-check.sh:63-72` BODY_RULES 加三行；`agent-tools-check.test.sh:40`（`H3B`／`LOCK_BODY`）、⑨ 五組 :114-118、同一 mutant 負控 :142-151（49→54 組）；`docs/COLLABORATION.md` §7 :248（lock 列 gate 欄補 H3b）、:249（正文字樣列補新句）、:300（H3 列前饋補 LS-183 來源、gate 欄補 H3b 判定／自測／盲區／併入後立即生效）。
4. LS-96 `e381f653` 第 1 項銷除註記：回覆 comment **`8f329ae5`**（附 PR #294＋四個 SHA；第 2 項 CI flake 案名保留未銷）。
5. PR **#294** `hotfix/LS-183-pretool-lock-bypass` → `main`，body 過 `pr-body-check.sh`（LS-96 行 2 條皆帶 comment id、「已修」行 0 條）後建立、`f41ce1c` 後再 gate＋`gh pr edit`。

**已驗證**（怎麼驗）：
- `bash scripts/hooks/pretool.test.sh` 全套 **185 組全綠、9 秒**（既有樣本無回歸＋H3b 46 組）；`python3 -m py_compile`／`bash -n` 通過。
- **mutation 對照（本機暫存樹，未入 repo）**：引擎六處 `H3B_TRIGGER` 回傳換 `pass` → 恰 21 條 H3b deny 樣本變紅（a①-⑥／b①-⑥／c①-⑤／h④⑥⑧⑨），其餘含 H3 的 h⑦ 不動——樣本依賴規則本身。
- `bash scripts/gates/agent-tools-check.sh` → `✓ …正文必含字樣 10 條`；`agent-tools-check.test.sh` 54 組全綠（⑨＋mutant 負控：拿掉規則區塊後「只有 hold 句」樣本變綠）。
- **真環境 dry-run**（worktree 版 `pretool.sh` 完整 hook 路徑、讀真 lock `/tmp/supabase-lock-little-sprout`、零容器命令；07:58 當時 holder＝`hold:LS-179 QA` worktree `qa-test`）：
  ① cwd＝LS-183 worktree、未包裝 `docker exec -i supabase_db_little-sprout psql -U postgres -c "select 1"` → **rc=2** `{"…permissionDecision":"deny","permissionDecisionReason":"H3b：docker exec 進 supabase_* 容器 未經 scripts/ops/supabase-lock.sh -- 包裝、也不在 lock 持有者（--hold）的 worktree 內（本機容器跨 worktree 共用，LS-183）——改用 bash scripts/ops/supabase-lock.sh -- <cmd>，或先在自己的票 worktree 執行 bash scripts/ops/supabase-lock.sh --hold，見 docs/COLLABORATION.md §7"}`
  ② `bash scripts/ops/supabase-lock.sh --status` → **rc=0** allow（無輸出）
  ③ `supabase db query --linked "select 1"` → **rc=0** allow
  ④（附）同一 `docker exec`、cwd＝真持有者 worktree `qa-test` → rc=0 allow（worktree 腿對真 holder 生效）
  ⑤（附）包裝 `supabase-lock.sh -- docker exec …` → rc=0 allow
- 每次 push 過 push-gate（純 harness，跳過 Swift）；pre-commit secrets 掃描擋過一次 `user:pass@host` 樣本 → 樣本改不帶密碼。
- 未跑：CI（push 後未等；交 orchestrator）；merge-review 尚未派。

**未完成／處置**（本票尚無 reviewer informational；以下是我自己的判斷）：
- **不修＋理由**：`supabase stop`／`supabase start`（重啟共用容器）不在 H3b 範圍——票文範圍 1 沒列、qa.md 自己就寫 `supabase-lock.sh -- bash -c "supabase stop && supabase start"`；加規則屬擴 scope，留 orchestrator 裁定是否入池（未自行入池）。
- **不修＋理由**：`--db-url` 指向遠端仍 deny（票文「不做」列 `--linked` 分類器問題，寧嚴）；`pg_restore`／`pg_dump` 只在帶連線字串時命中（票文只列 psql）。
- **不修＋理由（既有文字債，非本票）**：COLLABORATION §7 :248 lock 列仍寫「`supabase db reset` 只能靠 agent 定義…CLI 本身沒有 hook 可攔」與盲區「裸跑 `supabase db reset` 無機械攔截」——自 LS-88 H3 起已不成立；本票只在其後接 H3b 句、未改舊句（Rule 3），建議收尾順手修或入池。
- H3 本身**沒有**加 worktree 腿（hold 期間裸 `supabase db reset` 照舊 deny）——qa.md／ios-dev.md 明文「PreToolUse H3 只認 wrapper 字面」，維持；自測 h⑦ 釘住。

**風險**：
- **併入後對在飛 agent 立即生效**：`.claude/settings.json` 指向主 checkout `scripts/hooks/pretool.sh`，每次呼叫讀檔——merge 一落地，在跑的 ios-dev／qa 下一次 Bash 就受 H3b 管；裸 `docker exec`／`psql -p 54322` 會被 deny。緩解：deny 訊息直接給兩條合法路徑；持有 hold 的 agent 在自己 worktree（`cd <worktree> && …`）不受影響；唯讀 `docker ps`／`logs`／`inspect`／`supabase status`／`--status`／`pg_isready` 不擋。建議併入時機避開 QA 互動段（`--status` 看得到；此刻 LS-179 QA 持有中）。
- 誤擋方向（寧嚴）：命令位置認不得的段落連引號內字面都比對；`DATABASE_URL=postgres://…:54322 …` 賦值前綴也擋——改包 wrapper 即可。
- 漏放方向（已記 §7）：連線字串在 `python3 -c`／`node -e` payload 中段或暫存腳本；從主 checkout `--hold`（規約禁止）讓主 checkout 呼叫端直通（同 `hold_owner_ok` 既有盲區）；hook JSON `cwd` 是 session 目前目錄，agent 要靠 `cd <worktree> && …` 讓判定看到。
- hotfix 流程：併入 `main` 後請補 back-merge `main`→`development`。

**產出位置**：PR #294 https://github.com/CLYEH/little-sprout/pull/294（head `f41ce1c243d9b111f60ad5fa4de5a2ad5b1a624e`；commits `181d38e`／`89999fd`／`a9a2f05`／`f41ce1c`）；worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-183`（clean）；LS-96 銷除回覆 `8f329ae5`；scratchpad：`LS-183-pr-body.md`／`LS-183-dryrun.log`／`LS-183-suite.log`／`LS-183-smoke.sh`。lock：未持有（本票純 hook 邏輯，只用唯讀 `--status`／`--path`）；模擬器／Pen／容器未碰。

### 2026-09-05T00:08:28.662Z · Chih-Lin Yeh · `35ec9842`

**2026-09-05 08:1x orchestrator — R1 handoff `8f5112ea` 收到（PR #294 head `f41ce1c`）→ In Review，派 merge-reviewer R1**

- 裁定：`supabase stop`／`start` 是否納入 H3b → 記入 LS-96（容器起停也會打斷持有者，但票文未列，先入池）；§7 :248 舊句「db reset 只能靠 agent 定義」已失效 → 同一列的 gate 欄本票已改，請 reviewer 裁定是否順手改前饋欄那句（同列、一行）。
- 併入時機：避開 QA 互動段（併入即對在飛 agent 生效）；併入後 back-merge。

### 2026-09-05T00:17:22.403Z · Chih-Lin Yeh · `0e76ea6d`

## merge-review R1 — PR #294（head f41ce1c）

裁決：**APPROVE**（無 blocker／無 major；4 條 informational，其中 I-1 是你要我裁定的 `cd` 借用問題）。審的 head＝`f41ce1c243d9b111f60ad5fa4de5a2ad5b1a624e`，與 `gh pr view 294 --json headRefOid` 當下值相同；base `f304a13`，9 檔 +374/−45；`gh pr checks 294` **四項全 pass**（ci 4m27s／db 4m8s／lint 11s／rules 3m29s，run 33931795449）。2026-09-05 08:15 CST。未碰 Pen／模擬器／容器——**我對引擎的所有探針都是把字串餵進 `pretool_engine.py`（純解析、不執行）**，holder 判定用我自建的假 lock 目錄（`SUPABASE_LOCK_DIR` 覆寫），真 lock 全程沒動。

### （1）H3b 規則正確性 — 我自己餵了 12 條探針，四類觸發與誤擋面都對

**必須放行（誤擋面）——全部 ALLOW**：

| 探針 | 結果 |
|---|---|
| `echo "docker exec supabase_db_x psql -c select"` | ALLOW（引號內字面不擋，LS-104 精神） |
| `grep -rn 'postgresql://…:54322/db' docs/` | ALLOW（讀取動詞的引數不是打 DB） |
| `docker ps --filter name=supabase_db`／`docker logs supabase_storage_…`／`docker inspect …` | ALLOW（無 `exec` 不命中） |
| `supabase status`／`supabase status -o env` | ALLOW |
| `supabase db query --linked -f x.sql` | ALLOW |
| `bash scripts/ops/supabase-lock.sh -- docker exec supabase_db_x psql …` | ALLOW（包裝） |
| `psql --version`／`psql 'postgresql://…example.com:5432/…'` | ALLOW（沒有 54322，不誤擋遠端） |

**必須擋——全部 DENY 且訊息指向 wrapper／`--hold`**：`docker exec supabase_db_little psql …`／`psql 'postgresql://…@127.0.0.1:54322/…'`／`PGPASSWORD=x psql -h 127.0.0.1 -p 54322 …`／`supabase functions serve …`／`supabase db query -f x.sql`（無 `--linked`）。

**持有者判定我用假 lock 逐條驗**（holder pid 用一個**與 hook 無親屬關係**的獨立活行程——第一次我用自己的 shell pid，結果被 `_h3_reentrant` 的祖先腿放行，差點誤判，換 pid 後才看到真實行為）：

| 情境 | 結果 |
|---|---|
| cwd＝持有者 worktree 根／子目錄 | ALLOW（worktree 腿） |
| cwd＝別的目錄 | **DENY** |
| `cd <持有者 worktree> && docker exec …`（從別的目錄） | **ALLOW** → 見 I-1 |
| `pushd <持有者 worktree> && …` | ALLOW（同上，`pushd` 也被追蹤） |
| 守門 pid 已死（stale hold） | **DENY**（不豁免 stale） |
| holder 是命令型（`cmd` 非 `hold:`） | **DENY**（只有 `--hold` 給 worktree 腿） |
| 沒有 holder 檔 | **DENY** |

**Mutation（4 個）**：M1 拿掉持有者 worktree 放行 → **殺**（h① holder worktree allow 紅）；M2 拿掉 `echo`／讀取動詞的文字例外 → **殺**（t③ `grep` 連線字串 allow 紅）；M4 拿掉守門 pid 存活檢查 → **殺**（h⑨ 死 pid deny 紅）；M3 見 I-2。自測 **185 組**（其中 **H3b 46 組**）在 `/bin/bash` 3.2.57 下 **9 秒**跑完全綠——h⑨ 的競速修正（`f41ce1c`）確實讓它不再卡住。

### （2）`pretool.sh` 的 `cwd`

jq 取 hook JSON 頂層 `cwd` 放在 tuple **最後一欄**（吸收尾端換行，`${hook_cwd%$'\n'}` 再去一次），以 `PRETOOL_CWD` 傳給引擎；空值時引擎退回自己的 `os.getcwd()`。規則表 `:25-35` 的文字我逐句對過程式碼——`exec` token／`pg_isready` 唯讀例外／`docker ps`·`logs`·`inspect` 不命中／`psql` 段任一 token 含 54322／連線字串允許 `VAR=`·`--db-url=` 前綴且命令位置非 `echo`·`printf`·讀取動詞／`supabase` 相鄰子命令對且同段無 `--linked`／三段放行順序（wrapper → H3 重入 → holder worktree）**全部一致**。fail 方向見 I-3。

### （3）對在飛 agent 的影響 — 併入即生效，我把現行合法形狀全跑過

`supabase-lock.sh --status`／`--hold`／`--release`、`qa-e2e.sh <情境>` 本身、它內部的 `docker logs --since …`／`supabase status -o env`、包裝過的 `run.sh`、`xcodebuild`——**全部 ALLOW**。`qa-e2e.sh` 的容器相關動作只有 `docker logs`（唯讀例外）與它自己先 `--hold`（之後在票 worktree 內，走 worktree 腿），**不會被誤擋**。ios-dev 在 `--hold` 後於自己 worktree 直接 `psql` 也放行（h①／h② 實測）。

### （4）gate／文件

`BODY_RULES` 由 7 → **10 條**（三份定義各加 `本機容器操作同樣要在 lock 內`），真 repo gate 綠並印「正文必含字樣 10 條」、自測 **54 組**全綠。COLLABORATION 四處插入（§6 序列化段的 H3b 提要、§7 agent-tools 列、§7 序列化列、§7 H3 列的引擎細節）與程式碼一致。

**你要我裁定的 `:248` 前饋欄舊句——我判定不必在本 PR 改**：那句是「…照規約包——**CLI 本身沒有 hook 可攔**」，指的是 **supabase CLI 自己沒有 hook**，而 H3b 攔的是 Claude Code 的工具呼叫；人在終端機直接打、或把命令寫進暫存腳本再執行（§7 :300 自己就寫了）仍然攔不到——**所以那句到今天仍為真**，不是 LS-180 那種被撤銷的舊規則。而且新句就接在它後面形成沿革，讀起來是「以前只有規約 → 現在多了 H3b，但覆蓋面仍限於 agent 的工具呼叫」，語意正確。

### （5）申報查實

- **逐 commit 與 handoff 相符**：`181d38e`（只動 pretool.sh＋engine）／`89999fd`（只動 pretool.test.sh，+106）／`a9a2f05`（三份 agent md＋COLLABORATION＋agent-tools 及其自測）／`f41ce1c`（只動 pretool.test.sh 3 行）——**9 檔全在票文 1–4，無夾帶**。
- **LS-96 `e381f653` 第 1 項的銷除 `8f329ae5` 我到 LS-96 抓全部 comment 核對：存在**，內容帶 PR #294／head／三個 commit SHA，並**明確保留第 2 項（CI flake）另追**——沒有把整則池項一起銷掉。
- `pr-body-check --verify` 綠（LS-96 行 2 條）。

### Informational

**I-1（裁定：可接受，不擋併入）`cd <持有者 worktree>` 確實可以「借用」放行——但這是 `supabase-lock.sh` 自己的信任模型，不是本票新開的洞。**
我實測：從別的目錄下 `cd <持有者 worktree> && docker exec supabase_db_x psql …` → ALLOW。`_h3b_holder_worktree` 沿用的正是 `hold_owner_ok` 的 worktree 腿，而那條腿自 LS-159 起就寫明「**任何在同一 worktree 內跑的程序都算持有者**」。三個理由讓我判它可接受：
1. **一致性優於理論嚴格**：若 H3b 比 lock 本身更嚴，會出現「lock 讓你過、hook 擋你」的分裂——持有者在自己 worktree 的合法命令被擋，是比這個殘留更糟的失敗模式。兩套信任模型比一套有洞更危險。
2. **蓄意路徑早就被別的硬規則擋著**：跨 worktree 操作違反「一票一 worktree、禁止跨 worktree 編輯」，主 checkout 另有 guard；會走到這一步的人已經在違規，hook 不是唯一防線。
3. **本票要擋的情境擋住了**：LS-143 那起是「QA 在自己的地盤忘了 hold 就裸打容器」——那個情境我實測 DENY。
**建議（不必為它另開一輪）**：§7 盲區欄現在只寫了「把命令寫進暫存腳本再執行仍繞得過去」，順手補一句「`cd` 進持有者 worktree 亦可借用放行（沿 `hold_owner_ok` 的 worktree 腿，與 lock 同語意）」，免得日後被當成新發現的洞。

**I-2（我沒能證明它可達，也沒能證明等價——如實報告）**：把 fallback 的 `FB_LINKED_RE` 改成永不匹配（＝退回字面比對時 `--linked` 不再豁免），**185 組全綠、mutation 存活**。我試了 12 種可能觸發 fallback 的形狀（`eval`／`$(...)`／反引號／`bash -lc`／`for`／`if`／env 前綴／行接續／`nohup`／`time`／`xargs`／一般形）都無法讓正本與 mutant 產生行為差異——**傾向判斷它是防禦性程式碼、目前不可達**，而不是測試漏洞。與 LS-181 那次不同的是：那次我能**證明**等價，這次不能，所以明講「我構造不出可達輸入」。若實作者知道會走 fallback 的形狀，補一案最好；否則不需動作。

**I-3（`cwd` 缺欄位的方向）**：`PRETOOL_CWD` 為空時引擎退回 `os.getcwd()`（hook 行程自己的 cwd），既不是 fail-closed 也不是 fail-open，而是「換一個近似值」。實務上 hook 與呼叫端同 cwd，影響極小；但若日後 hook 的執行環境改變（例如在別的目錄 spawn），holder worktree 判定就會依據錯誤的目錄。記一筆。

**I-4（`--db-url` 遠端仍 deny——寧嚴，可接受）**：實測 `supabase db query --db-url postgresql://…@db.example.com:5432/…` → **DENY**（訊息叫你包 wrapper）。正式站的正典通道是 `--linked`（實測 ALLOW），`--db-url` 不是文件化路徑，deny 的方向安全、也給得出補救。若日後 `--db-url` 成為常用路徑，加一條「host 非 127.0.0.1／localhost 且 port 非 54322 → 視為遠端放行」即可。

### 四維度

race：H3b 讀 holder 檔的瞬間狀態（TOCTOU：判定後 hold 可能到期）——但這與 `supabase-lock.sh` 自己的競態面同級，且 hook 只是額外一層，真正的序列化仍由 lock 目錄的 `mkdir` 原子性提供；`_alive` 對 stale hold 的檢查方向正確（deny）。效能：每次 Bash 工具呼叫多一次 holder 檔讀取（`_holder_fields` 為 H3 重入與 H3b 共用、不重複讀）與最多 64 層 `ps -o ppid=`；185 組自測 9 秒，單次成本可忽略。平行：無。scope：9 檔全在票文 1–4，四個 commit 各自乾淨（engine／自測／規約各一，第四個是自測競速修正）。


### 2026-09-05T00:19:11.076Z · Chih-Lin Yeh · `90ed0173`

**2026-09-05 08:2x orchestrator — merge-review R1 `0e76ea6d` APPROVE（0 blocker／0 major／4 informational）；併入 main 延後到目前 delta QA（LS-181＋LS-182）收工後**

- 理由：hook 併入即對在飛 agent 生效；QA 的 Bash cwd 在背景化後可能重設到主 checkout（LS-96 `8fcc81c5`），屆時其 hold 內的 `psql` 會被 H3b 以「非持有者 worktree」擋下，中斷驗收。QA 收工即併＋back-merge。
- I-1（`cd` 進持有者 worktree 可借用放行——沿 `hold_owner_ok`，可接受；§7 盲區補一句）／I-2（`FB_LINKED_RE` mutation 存活、未證等價）／I-3（`PRETOOL_CWD` 空時退回 hook cwd）／I-4（`--db-url` 遠端仍 deny）→ 記入 LS-96。

### 2026-09-05T00:41:37.640Z · Chih-Lin Yeh · `b0282895`

**2026-09-05 08:41 orchestrator — PR #294 已併入 main `ae6ca24`（delta QA 收工後才併；主 checkout 已 pull，H3b 對所有 agent 生效）；back-merge main→development 背景鏈進行；票進 QA（隨下批 test 晉升驗 `pretool.test.sh` 185＋真環境 dry-run）。**

### 2026-09-05T01:07:18.110Z · Chih-Lin Yeh · `86944bf5`

**2026-09-05 09:1x orchestrator — back-merge PR #295 已併入 development `9fc4fdb` 並晉升 test；隨 LS-184 同批 delta QA（`pretool.test.sh` 自測＋真環境 dry-run）。**

### 2026-09-05T02:26:38.418Z · Chih-Lin Yeh · `e5e2db0f`

**2026-09-05 10:25 orchestrator — test 已到 `0bdf667`（含本票與 LS-184 back-merge）；隨 LS-184 同批 delta QA（`pretool.test.sh` 195＋引擎 dry-run 六樣本）。**

### 2026-09-05T02:38:33.024Z · Chih-Lin Yeh · `e3cc68fd`

## QA — delta PASS（test `0bdf667`，2026-09-05 02:38 UTC）

驗的是 `test` head `0bdf66706621a68a2450bf42a010494928a247d3`（＝`origin/test`；含 LS-183＋LS-184 兩支 back-merge，11 檔）。worktree `.claude/worktrees/qa-test` detach 到該 SHA，clean。純 harness 票，未碰 app／模擬器／資料庫／Pen／容器。

### 逐條驗收（票文）

- [x] **未包裝 `docker exec supabase_db_… psql` → deny 並印包裝提示；包裝後／持有者／`--linked`／`docker ps` → 放行（自測）**：`bash scripts/hooks/pretool.test.sh`（`/bin/bash` 3.2.57）**195/195 ✓**（H3b-r／H3b-t／H3b-s 全綠，含 LS-184 追加的 stop/start 10 組）。
- [x] **真環境：對本 session 目前的 lock 狀態跑一次 `pretool` dry-run 樣本，結果與預期一致**：樣本以 quoted literal 寫入暫存檔（不落在 Bash 命令位置，避免誤觸自己的 hook），再用 jq 組成 hook JSON 餵進 worktree 版 `scripts/hooks/pretool.sh`（純解析、不執行）：
  - `docker exec supabase_db_little-sprout psql -U postgres -c "select 1"` → **deny**（`H3b：docker exec 進 supabase_* 容器…`）
  - `bash scripts/ops/supabase-lock.sh --status` → **allow**
  - `supabase db query --linked "select 1"` → **allow**
  - `supabase stop` → **deny**（`H3b：supabase stop／start…LS-184`）
  - `supabase status` → **allow**
  - `bash scripts/ops/supabase-lock.sh -- supabase db reset`（包裝）→ **allow**
  六筆與票文預期完全一致。
- [x] **merge-reviewer 通過；hotfix 流程（自 main、PR 回 main、back-merge）**：merge-review R1 `0e76ea6d` APPROVE（0 blocker/major，4 informational 已記 LS-96）；PR #294 → main `ae6ca24`；back-merge 已併入 development 並晉升 test（`0bdf667`）。

### 其他確認
- `git diff --stat aa199f8..0bdf667` 11 檔全在 LS-183/LS-184 範圍，無夾帶。
- `scripts/gates/agent-tools-check.sh`（真 repo）exit 0，印「正文必含字樣 13 條」；`agent-tools-check.test.sh` 60 組全綠。
- `docs/COLLABORATION.md` §7 :217／:248／:249／:300 與 `.claude/agents/{qa,ios-dev,merge-reviewer}.md` 的 lock 段字樣核對一致。
- CI `test@0bdf667`：`ci`／`db`／`rules`／`lint` 四項皆 `success`（run 33939033576）。

**裁決：PASS**（與 LS-184 同批 delta QA）。

### 2026-09-05T02:41:23.718Z · Chih-Lin Yeh · `b30a2676`

**2026-09-05 10:4x orchestrator — delta QA PASS `e3cc68fd`（`pretool.test.sh` 195、六樣本 dry-run：裸 docker exec／`supabase stop` deny，`--status`／`--linked`／`status`／包裝 allow）→ 已到 main `0bdf667`（v0.13.1）；sweeper 批次巡檢中 → retro → Done。**

### 2026-09-05T02:45:33.015Z · Chih-Lin Yeh · `7e42a5aa`

## dead-code sweep — LS-183

**範圍**：`git diff --stat aa199f8..0bdf667`（main `0bdf667`，同批含 LS-183＋LS-184，11 檔）；主 checkout 唯讀巡查，未執行任何 hook 樣本、未動容器/Pen。

### Findings

**0 findings** — 未發現本 feature（LS-183）引入的死碼。

逐項核對：
- **`scripts/hooks/pretool_engine.py`**：LS-183 新增的常數／regex（`H3B_CONTAINER_RE`／`H3B_PORT_RE`／`H3B_CONNSTR_RE`／`H3B_SUPABASE_LOCAL_PAIRS`／`H3B_TEXT_VERBS`／`FB_DOCKER_EXEC_RE`／`FB_PG_ISREADY_RE`／`FB_PSQL_54322_RE`／`FB_CONNSTR_RE`／`FB_SUPABASE_LOCAL_RE`／`FB_LINKED_RE`／`H3B_WHAT_FALLBACK`）與新函式（`_h3b_check`／`_holder_fields`／`_alive`／`_toplevel_of`／`_h3b_holder_worktree`／`_cd_target`／`_resolve_trigger`）逐一 `grep -n` 計數，**全部 ≥2 命中**（定義＋至少一處呼叫），無孤兒符號。舊版 `_h3_reentrant` 內聯讀 holder 檔的邏輯已被抽成 `_holder_fields()` 共用，未留重複讀取路徑（對照 `git diff` 該段：舊的 `open(holder_path)`／`re.search(r"^pid=…")` 整段被替換，不是新增旁支）。
- **`scripts/hooks/pretool.sh`**：規則表新增 H3b 段文字（:25-35）逐句與 `pretool_engine.py` 對應邏輯核對一致（`docker exec`＋容器名前綴、`exec` 後 `pg_isready` 唯讀例外、`psql`/連線字串含 `:54322`、`supabase functions serve`/`db query`/`db dump`/`migration up` 缺 `--linked`、放行順序 wrapper→H3 重入→holder worktree）；六欄 JSON 解析（`cwd` 新增第六欄，jq／python3 兩路）与 `PRETOOL_CWD` 傳遞一致，無殘留的五欄舊寫法。
- **`.claude/agents/{ios-dev,qa,merge-reviewer}.md`**：LS-183 各補一句「本機容器操作同樣要在 lock 內」；核對三份原文，此句在三份間彼此不重複、也未與既有 `--hold` 段落語意重疊（各自新增子句，非複製既有句）。
- **`scripts/gates/agent-tools-check.sh`**：`BODY_RULES` 新增三行（`ios-dev|merge-reviewer|qa` 各一條 H3b 句），`LS170-BODY-RULES-START/END` 標記仍是自測 mutation 用的錨點（非死碼，`.test.sh` :141 仍在用）。
- **`docs/COLLABORATION.md`**：§7 lock 列（:248）、agent-tools 列（:249）、H3 列（:300）三處插入皆為獨立新增句，與 LS-184 同列的追加句（`--hold` 拒主 checkout／stop-start）互不重疊、彼此指名清楚（`**LS-183**`／`**LS-184**` 分段標註）。

### 既有死碼觀察（非本 feature 引入，僅供 orchestrator 參考）

- `docs/COLLABORATION.md:248`（§7 lock 列 gate 欄）仍保留一句舊文字：「`supabase db reset` 只能靠 agent 定義（ios-dev／qa／merge-reviewer／dead-code-sweeper）照規約包——CLI 本身沒有 hook 可攔。」——此句寫於 LS-88 引入 PreToolUse H3 之前，H3 已提供機械攔截多年，句子本身已不完全成立（H3 攔 `supabase db reset`／`run.sh` 裸跑）。**本票（LS-183）的實作者已在 handoff 自行發現並列為「不修＋理由（既有文字債，非本票）」**，merge-reviewer R1（`0e76ea6d`）也裁定「這句到今天仍為真——它講的是『CLI 自己沒有 hook』，H3/H3b 攔的是 Claude Code 的工具呼叫，人在終端機直接打仍攔不到」，判定不必在本 PR 改。我複核後同意 reviewer 的解讀（該句語意仍成立，非死碼，只是容易被誤讀成「H3 也沒有」）——**不算 finding，僅記錄供日後若要順手潤飾參考**，不建議另開票（成本／效益不對稱）。

### 搜尋方法與盲區聲明

- 方法：`git diff --stat`／`git diff <hunk>` 逐檔比對；對 `pretool_engine.py` 每個新符號跑 `grep -n <symbol>` 計數判定是否孤兒；對三份 agent 定義與 COLLABORATION.md 用 `Read` 工具讀取當前完整行（非只看 diff hunk 的單行 paragraph），核對語意是否重疊。
- 盲區：純文字比對抓不到「symbol 在其他語言／工具（例如 shell 直接 source 這個 python 檔）動態載入後才被引用」的情形；未實際執行任何 `.test.sh`（QA／merge-reviewer 已各自機械跑過 185/195/239/60 組全綠，本次巡檢不重跑、只讀邏輯）；未對活資料庫查證（本票不涉及 SQL）。

**建議處置**：無 finding 需要開票；既有死碼觀察一項不建議另開票（已被本票 reviewer 覆核判定非需修正的問題）。

### 2026-09-05T02:46:38.010Z · Chih-Lin Yeh · `cd052300`

**2026-09-05 10:5x orchestrator — 收尾儀式 → Done**

- sweeper `7e42a5aa` 0 finding；worktree／分支已清；LS-96 `e381f653` 第 1 項已銷除。
- **Lesson learning**：(1) 本票把「lock 只靠派工文約束」變成機械 gate，事故（LS-143 docker exec 撞 reset）→ gate 閉環；併入時機刻意避開 QA 互動段是對的——hook 併入即生效、且 QA 的 cwd 重設問題（LS-184 源頭）會讓持有者自己被擋。(2) reviewer 用假 lock 目錄逐條驗持有者判定，並自承第一次用自己 shell pid 當 holder 被重入腿放行——複驗 hook 時要用獨立活行程當 holder，已寫進 review 記錄。(3) `cd <持有者 worktree>` 可借用放行（沿 `hold_owner_ok`）是已知盲區，記 LS-96 `e8fb11af` I-1，不擴大成新洞的解法是 main-checkout guard＋硬規則。(4) `npx supabase` 不觸發 H3／H3b（LS-184 R1 I-3）→ LS-96 `f8e101b4`。

