# LS-176 Harness：pen-open／pen-read 對「Pen 記得的舊 worktree 路徑已不存在」視為可安全捨棄（設計票新鮮度保證恢復）＋ cleanup-merged 連刪該票專屬模擬器與 DerivedData（磁碟水位）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-04T13:41:34.951Z |
| 開始 | 2026-09-04T17:19:50.025Z |
| 完成 | 2026-09-04T22:37:36.686Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-176/harnesspen-openpen-read-對pen-記得的舊-worktree-路徑已不存在視為可安全捨棄設計票新鮮度保證恢復 |
| 關係 | related→LS-173, related→LS-175, related→LS-158, related→LS-177, related→LS-163, related→LS-96, related→LS-133, related→LS-152, related→LS-107, related←LS-266, related←LS-205, related←LS-187, related←LS-181, related←LS-180 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項 `56eeaee0`（P1，[LS-133](https://linear.app/little-sprout-app/issue/LS-133/設計法務文件-in-app-檢視畫面歡迎頁使用條款隱私權政策改開-app-內視窗不跳網站) VR R1／R2 IN-2）：`scripts/ops/pen-read.sh`／`pen-open.sh` 在 Pen 仍記得已被 `cleanup-merged.sh` 移除的 worktree 路徑（今天 [LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表)、[LS-163](https://linear.app/little-sprout-app/issue/LS-163/設計歡迎頁以帳號密碼登入小字連結三顆登入鈕下方非大按鈕帳號密碼登入畫面審核帳號用方案-b-ui-designervisual) 清理後各發生一次）時回報「路徑不存在→無法確認安全」而拒絕重開／切檔，設計票的「renderer 讀的是現稿」新鮮度保證失效，VR 只能改用 tree_hash 間接佐證，**每張後續設計票都受影響**。同池 `7c9fe5bd`／`0e75271d`（P1）：`~/Library/Developer/CoreSimulator/Devices` 146 GB／77 台、`DerivedData/LittleSprout-*` 每 worktree 一份，今天兩次把磁碟填滿並把 Docker VM 弄掛。

## 範圍

1. `pen-open.sh`／`pen-read.sh`：Pen 目前文件路徑若在磁碟上不存在 → 視為可安全捨棄（無檔即無未落地變更可失），直接切到目標檔並 stderr 註記；路徑存在但有未落地變更仍照舊擋。自測 `pen-open.test.sh`（若無則新增）補「舊路徑不存在」正樣本與「舊路徑存在且 dirty」負樣本。
2. `cleanup-merged.sh --apply LS-<n>`：一併 `xcrun simctl delete` 名稱以 `LS-<n>-` 開頭的專屬模擬器（Booted 先 shutdown）、刪 `~/Library/Developer/Xcode/DerivedData/LittleSprout-*` 中屬於該 worktree 路徑的目錄（DerivedData 目錄名含 hash，用 `info.plist` 的 `WorkspacePath` 比對）；`--dry-run` 列出將刪項；自測補案（用假目錄）。
3. `patrol.sh` 加「磁碟水位」段：`df` 可用 <20 GB 印 ⚠ 並列 Devices／DerivedData 體積與 `LS-*` 模擬器數；`patrol.test.sh` 補案。
4. `detect-simulator.sh`：同票已有專屬機則重用不新建（[LS-107](https://linear.app/little-sprout-app/issue/LS-107/taskls-18-iosa-首次進入三岔路建立家庭05邀請家人07owner-路徑) 曾建 4 台）。
5. COLLABORATION §4-b（巡檢段）／§6／§7 同步；[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 三則銷除註記。

## 不做

每 worktree 獨立容器；Pencil 端問題（`underline`、interrupted）。

## 驗收

- [ ] 模擬「Pen 開著已刪 worktree 的檔」→ `pen-open.sh` 成功切檔；dirty 情境仍擋（自測）
- [ ] `cleanup-merged.sh --apply` 對假票清掉專屬機＋DerivedData（自測）；`patrol.sh` 水位段可觸發 ⚠
- [ ] merge-reviewer 通過；hotfix 流程（自 main、PR 回 main、back-merge）

## 留言（14）

### 2026-09-04T17:19:53.142Z · Chih-Lin Yeh · `fc758d56`

**2026-09-05 01:19 orchestrator — 派工**

- harness lane 空（LS-158 併入 dev 進 QA）→ 本票接手；cycle 1。
- hotfix 流程：worktree `.claude/worktrees/LS-176`，分支 `hotfix/LS-176-pen-open-stale-cleanup`（base origin/main `1cdb7fc` v0.10.0），PR → `main`，併入後 orchestrator 補 back-merge main→development。
- 限制：LS-177 設計票在飛、Pen 開著 LS-177 worktree 檔——本票所有自測一律以 stub 取代 Pen 呼叫，**不得對活的 Pen 執行 pen-open／pen-read**；`cleanup-merged.sh` 新增的模擬器／DerivedData 刪除只能在自測假目錄與 `--dry-run` 驗，**不得真刪任何現存模擬器或 DerivedData**。

### 2026-09-04T20:35:19.974Z · Chih-Lin Yeh · `072f6492`

## Handoff — LS-176（2026-09-05 04:33 CST）

**Ticket**：LS-176。分支 `hotfix/LS-176-pen-open-stale-cleanup`（自 `origin/main` `1cdb7fc`），PR **#285** → `main`，head `c59a398`。

### 已完成（commit SHA＋檔案:行號）

1. **pen-open／pen-read 舊路徑不存在視為可安全捨棄** `9c4b890`
   - `scripts/ops/pen-open.sh:281-286` `check_root_safe()` 新增訊號 d：`[ ! -e "${root}${suffix}" ]` → stderr「舊路徑不存在，視為已捨棄：<path>」、`return 0`；說明 `:89-91`／`:272-276`。路徑存在且 dirty 走原本 a–c／mtime 判定，照舊擋。`pen-read.sh` 是 `--force-reload` 封裝，未改。
   - `scripts/ops/pen-open.test.sh:239-259` ⑭a 正樣本（舊「不一致→root 不存在→exit 1」案改寫成同情境放行）；`:575-591` ⑭b 負樣本（存在且 dirty：exit 1、不 quit、不印「視為已捨棄」）。
   - `scripts/ops/pen-read.test.sh:186-221` 同一對。
2. **cleanup-merged 連刪專屬模擬器與 DerivedData** `e1b5eb4`
   - `scripts/ops/cleanup-merged.sh:46-58` 檔頭 (e)；`:365-366` `FILTER_WT_PATHS`／`FILTER_WT_HANDLED`；`:384-386` `process_wt` 記指名票命中的 worktree（在 cwd 保護之前）；`:599-688` (e) 段本體（`e_left` 判定、`dd_matches()` `:616`、`simctl list devices` awk、Booted 先 shutdown、`/tmp/simulator-lock-<UDID>` 在則跳過、`sed` 讀 XML plist `WorkspacePath`）；`:691-693` 輸出與摘要行。
   - `scripts/ops/cleanup-merged.test.sh:62-96` stub `xcrun`＋`LS_DERIVED_DATA_ROOT` 假目錄在檔頭裝上（隔離真機）；`:666-738` ⑳（33 條斷言）。
3. **patrol 磁碟水位段** `b00c9a3`
   - `scripts/ops/patrol.sh:88-96` `PATROL_DISK_MIN_GB`（預設 20）／`PATROL_DU_TIMEOUT`（未設：brief 8s、其餘 25s）；`:410-446` 段本體（`du_with_timeout()` `:423`、`add_flag "[磁碟] …"`）；`:482-487` json `disk`；`:490` brief 表頭；`:518-521` human 段。
   - `scripts/ops/patrol.test.sh:30-33` 全檔預設 `PATROL_DISK_MIN_GB=0`；`:582-646` ㉑（17 條）。
4. **detect-simulator 同票同 runtime 重用** `a5422af`
   - `scripts/gates/detect-simulator.sh:100-121` `find_udid_same_ticket()`；`:147-156` 主流程。
   - `scripts/gates/detect-simulator.test.sh:41-52` stub 依 db os 欄分節；`:235-253` ⑪／⑫。
5. **COLLABORATION** `c59a398`：`docs/COLLABORATION.md:124`（§4-b 工具段）、`:135`（cron 模板）、`:223`（§6 ③）、`:303`（§7 新列）。
6. LS-96 三則池項各回一則銷除註記（附 PR #285）：`56eeaee0`／`7c9fe5bd`／`0e75271d`。

### 已驗證

- 自測（全部 stub，未碰活的 Pen／真模擬器）：`pen-open.test.sh` ✓ 30 組；`pen-read.test.sh` ✓ 8 組；`cleanup-merged.test.sh` ✓（⑳ 33 條）；`patrol.test.sh` ✓（㉑ 17 條）；`detect-simulator.test.sh` ✓（⑪⑫ 5 條）；`agent-tools-check.sh` ✓；`pr-body-check.sh` ✓。五支自測皆在 CI `rules` job。
- 真環境 dry-run（`PEN_BIN=/nonexistent-pen-LS176`）：`--dry-run LS-173` → `[dry-run] delete 專屬模擬器 LS-173-iPhone17Pro（A025F80F-…）`＋`[dry-run] 刪除 DerivedData LittleSprout-gcxz…（WorkspacePath=…/LS-173/LittleSprout.xcodeproj）`；跑完該機與目錄仍在。`LS-158` 同型（其 worktree 已在本 session 中被清掉，`git worktree list --porcelain` 核對過，非 guard 漏掉）。
- 真環境 `patrol.sh --no-fetch --no-pr`：「可用 77 GB，LS-* 專屬模擬器 5 台 ok」；`PATROL_DISK_MIN_GB=999999` 強制 ⚠：human 21.7s 印「Devices 65 GB、DerivedData 6 GB」；brief 10.5s 回、體積 ?（看門狗生效；原統一 10s 時 Devices `du` 實測 18s 必逾時，故改依模式預設）。
- 硬限制：未對活的 Pen 執行 `pen-open.sh`／`pen-read.sh`（含 `--status`）；未真刪任何模擬器／DerivedData；未跑 `simctl shutdown all`；未碰 supabase 容器。

### 未完成／處置

- merge-review 尚未進行（PR #285 剛開，不等 CI）；併入後 back-merge `main`→`development` 由 orchestrator 補。
- **判斷不做**：(a) 派工單「同機型」嚴格字面＝既有精確名稱比對，修不到 LS-107 堆 4 台；改為「同票、同 runtime 分節、機型可不同」——若要嚴格同機型，`find_udid_same_ticket` 加一個 `nm == dedicated_name` 條件即可。(b) `--all` 不套用附屬資源（沒有票號就沒有前綴可比）。(c) 不加 binary plist 解析（本機 9 份實測皆 XML；讀不到就略過列出）。(d) 「無 xcrun 只跳過模擬器段」在 macOS 無法用 PATH 模擬缺席，僅程式碼審閱、無自測；reviewer 可要求補 `XCRUN_BIN` 覆寫點。(e) 未在真環境跑 `detect-simulator.sh`（會建一台 LS-176 專屬機）。(f) CLAUDE.md「收尾儀式」那句未改（派工範圍限 COLLABORATION §4-b／§6／§7）。
- reviewer informational：尚無（待 R1）。

### 風險

- 附屬資源「沒人在用」只靠「該票 worktree 已不在磁碟」＋simulator-lock 推斷；`LS-<n>-*` 命名的 scratchpad 副本正在 xcodebuild 時抓不到（後果＝重編一次）。
- `pen-open` 放行只看磁碟：renderer 記憶體裡對已刪 worktree 的未落地內容一併捨棄（票文裁定）。
- 磁碟水位是事後發現不是攔截；brief 模式低水位時體積印 ?，cron 的 human 模式才有真數字。
- 同票不同 runtime 的專屬機仍各建一台（刻意）。

### 產出位置

- PR #285 https://github.com/CLYEH/little-sprout/pull/285（base `main`，head `c59a398`；commits `9c4b890`／`e1b5eb4`／`a5422af`／`b00c9a3`／`c59a398`）
- worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-176`；scratchpad `LS-176-pr-body.md`／`LS-176-handoff.md`

### 2026-09-04T20:36:16.336Z · Chih-Lin Yeh · `2248ba74`

**2026-09-05 04:4x orchestrator — R1 handoff `072f6492` 收到 → In Review，派 merge-reviewer R1**

- PR #285 → main，head `c59a398`（5 commits）。
- 裁定：detect-simulator「同機型」採實作者詮釋（同票＋同 runtime 分節即重用）——這才修得到 LS-107 4 台的實況；CLAUDE.md 收尾儀式那句由 orchestrator 在 back-merge 時另補（不進本 PR）。
- 併入後 back-merge main→development（含 LS-176 hotfix）。

### 2026-09-04T20:52:01.828Z · Chih-Lin Yeh · `42383240`

## merge-review R1 — PR #285（head c59a398）

裁決：**APPROVE**（無 blocker／無 major；3 條 informational）。審的 head＝`c59a398de2f6082e882f5b597a0e82f086532944`，與 `gh pr view 285 --json headRefOid` 當下值相同；base `1cdb7fc`，10 檔 +487/−24；`gh pr checks 285` **四項全 pass**（ci 5m24s／db 3m26s／lint 15s／rules 3m28s，run 33916832065——開審時三項還在跑，我等收工才判讀）。2026-09-05 04:50 CST。

**硬限制遵守**：全程**未對活的 Pen 執行 `pen-open.sh`／`pen-read.sh`（含 `--status`）**——我先確認 `pen-open.test.sh` 以 PATH stub 換掉 `pen`／`pgrep`／`osascript` 三個外部命令才敢跑；**未真刪任何模擬器／DerivedData**（cleanup 只在自測的 stub `xcrun`＋假 `LS_DERIVED_DATA_ROOT` 下跑，我沒有跑過 `--apply` 對真環境）；未碰 supabase 容器、未 boot 任何模擬器。

### （1）pen-open 訊號 d — 放行條件正確，mutation 兩個方向都殺得死

`pen-open.sh:277-282`：`[ ! -e "${root}${suffix}" ]`（`suffix="/design/littlesprout.pen"`，`:258`）→ 印「舊路徑不存在，視為已捨棄」`return 0`；其餘一律往下走原本的 a–c／mtime 判定。基準線：`pen-open.test.sh` **30 組**、`pen-read.test.sh` **8 組**全綠。

我做的 mutation（你指定的那個，加一個對照）：

| Mutant | 結果 |
|---|---|
| **M1 把「不存在」判斷反過來**（`[ -e … ]` 才放行） | **殺**——⑭a 紅（該放行的變成 exit 1），連帶 ⑪c／⑪e／⑫a 一起紅（本來該擋的 dirty 情境變成放行）。**⑭a／⑭b 不是只釘一個方向：反過來會同時破壞「放行」與「擋」兩邊** |
| M2 整段還原成 LS-176 之前的 `[ ! -d "$root" ] → return 1` | **殺**——⑭a 紅（回歸到被擋住的舊行為） |

### （2）`cleanup-merged` (e) 段 — 四道守門全部被自測釘住，我逐個 mutation 驗過

守門與我的核對：

- **只在指名票**：`if [ -z "$FILTER" ]` → 印「未指名 LS-<n>，附屬資源不套用」，`--all` 走這條。
- **該票 worktree 已不在磁碟才動**：`FILTER_WT_PATHS` 在 `process_wt` 的 **cwd 自我保護之前**就記錄（`:386`，我核對過行序），所以「因為你正站在裡面而被略過」的 worktree 也算「還在磁碟」→ `e_left>0` → 附屬資源一律不動。這個順序是對的，也是這段最關鍵的一步。
- **Booted 先 shutdown 再 delete**：`act` 兩段呼叫，自測用 stub log 的**行號先後**釘住順序。
- **simulator-lock 在則跳過**：`/tmp/simulator-lock-<UDID>` 存在 → 只標記不刪。
- **dry-run 不刪**：`act()` 在 `MODE=dry-run` 直接 `printf` 後 `return 0`，自測另外斷言「stub log 沒有 shutdown／delete」「假 DerivedData 目錄還在」。
- **word-boundary**：`case "/${p}/" in */"${FILTER}"/*|*/"${FILTER}"-*/*)`——`LS-17` 不會命中 `LS-174`（自測用 LS-930 vs LS-9300 釘）。

我的 mutation（4 個，全殺）：

| Mutant | 結果 |
|---|---|
| A 拿掉「該票 worktree 仍在磁碟」守門 | **殺**（LS-932 那組三條斷言同時紅） |
| B 拿掉 simulator-lock 跳過 | **殺**（鎖中機被刪、摘要數字也錯） |
| C 讓 `--all` 也套用 | **殺** |
| D `dd_matches` 改寬鬆子字串 | **殺**（LS-9300 的 DerivedData 被誤刪） |

**你要我裁定的殘餘 race：我裁定「可接受、不擋併入」，並反對用「Booted 即跳過」當補強。** 理由三點：
1. **窗口比 handoff 自陳的更窄**：LS-158 R2 剛落地的「`qa-e2e.sh` 只准在票 worktree 內跑」讓「該票 worktree 已不在」幾乎等同「該票的 e2e 不可能正在跑」。剩下的唯一漏口是 `qa.md` 明文允許的「qa-test 固定 worktree ＋ `--ticket LS-<n>`」——QA 可以在別的 worktree 用該票的專屬機。
2. **損害有界且可再生**：後果是那次 xcodebuild／e2e 大聲失敗＋重建一台機，模擬器內沒有不可再生的資料（截圖與 storage.log 都落在 `.claude/evidence/`，不在模擬器裡）。
3. **「Booted 即跳過」會打壞本票的目的**：被 OOM 殺掉、留在 Booted 的殘機（LS-158 R2 handoff 就記過一次）將永遠回收不掉——而那正是 146 GB／77 台的組成之一。
   **建議的補強（下次動這段時順手，不必為它另開一輪）**：刪之前多一道 `pgrep -f "$sim_udid" >/dev/null && 跳過`——`xcodebuild -destination id=<udid>`／`simctl` 的命令列都帶 UDID，這道直接偵測「現在真的有人在用」，而不是靠狀態推斷，成本一次 `pgrep`。

### （3）patrol 磁碟水位

- **`du` 只在 ⚠ 才跑**：整段包在 `if [ "$disk_avail_gb" -lt "$DISK_MIN_GB" ]` 內；`--json` 在門檻之上 `devices_gb` 是 `null`，自測直接斷言這一點（「沒跑 du」）。
- **看門狗**：`du_with_timeout` 沿用 `fetch_with_timeout` 的背景＋kill 模式，逾時回 124 → 體積印 `?` 不炸；預設 brief 8s／human・json 25s，`PATROL_DU_TIMEOUT` 可覆寫，非整數 exit 2（有自測）。
- **`PATROL_DISK_MIN_GB`** 預設 20、非整數 exit 2；`patrol.test.sh` 全檔預設 0 隔離機器狀態，㉑ 自己覆寫門檻——這個隔離手法是對的，否則這組斷言會隨我的磁碟狀況偶發紅。
- **三面都有斷言**：human（`== 磁碟水位` 段、體積、`LS-*` 台數 2、處置句）／brief（`[磁碟] ⚠`＋表頭）／json（`disk` 物件五個欄位＋`flags` 內含一筆），外加「門檻之上不標、不 du」的反向組。`patrol.test.sh` 全綠。
- **SessionStart 預算**：`session-start.sh` 本票**未改**（`git diff --name-only` 0 筆），它呼叫 `patrol.sh --brief`。低水位時每次 SessionStart 最多多付 8s（fetch 看門狗 10s＋du 8s＝18s，仍在 30s 內）；而 Devices 的 `du` 實測 18s > 8s ⇒ 低水位時 brief 幾乎必然印 `?`。這是刻意取捨（要數字看 cron 的 human 模式），見 I-3。

### （4）detect-simulator 重用

`find_udid_same_ticket()` 以 `pfx="${ticket}-"` 比對、且只在 `cur == header_os` 的 iOS 分節內找 → **不會誤重用他票**（`LS-17-` 前綴不會命中 `LS-174-…`）、**不會跨 runtime**。自測 ⑪（同票不同機型同 runtime → 重用既有、不另建、stderr 說明重用哪台）與 ⑫（同票只存在於 18.6 節 → 不重用、在 26.0 節建新的）把兩個方向都釘住。與 orchestrator 對「同機型」的裁定一致。

### （5）申報查實／文件／gate

- **每個 commit 動到的檔與 handoff 申報逐一相符**：`9c4b890`（pen-open.sh＋兩支自測）／`e1b5eb4`（cleanup-merged.sh＋自測）／`a5422af`（detect-simulator.sh＋自測）／`b00c9a3`（patrol.sh＋自測）／`c59a398`（只有 COLLABORATION.md）——沒有任何一個 commit 夾帶別檔。
- **LS-96 三則銷除我到 LS-96 抓全部 comment 核對**：`3226622e`（→ `56eeaee0`，pen-open 訊號 d）／`6ae1407b`（→ `7c9fe5bd`，cleanup (e)＋patrol＋detect-simulator）／`b239c503`（→ `0e75271d`，DerivedData）**三則都在**，且各自帶對應的 commit SHA。
- **§7 對照表那一列的 gate 欄我逐字讀過**：四條新規則**各自都有機械承載**（pen-open 訊號 d／cleanup (e) 的三道守門＋word-boundary／patrol 的 df 門檻＋看門狗＋add_flag／detect-simulator 的 `find_udid_same_ticket`），還列了五支自測的具體案例與**盲區清單**（含「附屬資源沒人在用只靠推斷」這條——也就是我在（2）裁定的那個 race，文件自己承認了），承載欄標「✅ CI（自測）；附屬資源在用中⚠️推斷」。§4-b 工具段與 cron 模板、§6 收尾儀式也都同步。
- `agent-tools-check.sh` 綠（本 base 是 main `1cdb7fc`，所以印「正文必含字樣 **3** 條」——LS-158 的第 4 條在 development，不是回歸）。`pr-body-check.sh --verify` 綠。

### （6）bash 3.2 相容 — 我用系統 bash 3.2 實跑五支自測

macOS 的 `/bin/bash` 就是 **3.2.57**，所以這不是模擬：`/bin/bash scripts/ops/pen-open.test.sh`／`pen-read.test.sh`／`cleanup-merged.test.sh`／`patrol.test.sh`／`scripts/gates/detect-simulator.test.sh` **五支全部 OK**。patrol 借用位置參數當「存在的目錄清單」（`set --`，避免 bash 4 陣列）的手法我也查過安全性：磁碟段之後全檔沒有再用到 `$1`／`$@`。

### Informational

**I-1（我對 race 的裁定＋建議）**：見（2）——可接受、不擋；建議下次動這段時加 `pgrep -f "$sim_udid"` 這一道，**不要**改成「Booted 即跳過」。

**I-2（語意比票名廣一點，記一筆）**：訊號 d 判的是 **`.pen` 檔**是否存在，不是 worktree 目錄；所以「worktree 還在、但 `design/littlesprout.pen` 被刪掉」也會放行。票文正文寫的是「Pen 目前**文件路徑**若在磁碟上不存在」，實作與票文一致，而且「無檔即無處可落地」的論證在這個情形同樣成立（`pen-land` 的落地目標就是那個檔）。只是票**名**寫的是「舊 worktree 路徑已不存在」，兩者範圍不同——寫下來讓後面的人不用再推一次。

**I-3（觀察，非缺陷）**：低水位時 SessionStart 的 brief 模式因 8s 看門狗幾乎必然把體積印成 `?`（Devices 的 `du` 實測 18s）。也就是說 SessionStart 只會告訴你「低」、不會告訴你「多少」，要數字得看 cron 的 human 模式——handoff 已揭露，我只是把它記進裁決供日後調參考。

### 四維度

race：本票的核心就是 race——(e) 的三道守門（指名票／worktree 已不在／simulator-lock）＋記錄順序（cwd 保護之前）我都核對並用 mutation 驗過；殘餘一格見 I-1。效能：`du` 只在 ⚠ 才跑並掛看門狗，patrol 的 xcrun 只打一次（重用 `sim_rows`）；`pen-open` 訊號 d 是一次 `[ -e ]`，比原本還省一次 `pen-land --dry-run`。平行：cleanup 的模擬器迴圈用 here-doc（不是管線）所以計數器留在同一個 shell，摘要數字正確。scope：10 檔全部落在票文 1–5，5 個 commit 各自乾淨、**無他票檔案**；`CLAUDE.md` 那句依 orchestrator 裁定不在本 PR。


### 2026-09-04T20:55:39.469Z · Chih-Lin Yeh · `4a101cf8`

**2026-09-05 04:55 orchestrator — merge-review R1 `42383240` APPROVE；PR #285 與 main `679a417` 衝突 → 派實作者併 main 解衝突後 reviewer 審 merge 增量**

- main 在 PR 開出後由 test 晉升（v0.11.0：LS-158 改了 COLLABORATION／supabase-lock／agent-tools-check），`git merge-tree` 確認衝突。實作者在 worktree 併 `origin/main`、解衝突、重跑自測、push。
- I-1（附屬資源在用偵測改 `pgrep -f UDID`）／I-2／I-3 入池 LS-96 `5566d38b`。
- 併入 main 後 back-merge main→development（body 已備 `LS-176-backmerge-body.md`）。

### 2026-09-04T21:04:08.895Z · Chih-Lin Yeh · `f9be427e`

## 合併 origin/main 679a417 — LS-176 handoff 補記（2026-09-05 05:03 CST）

**新 head `8a37d0f`**（merge commit `c3ed12b`＝`c59a398`＋`679a417`，之後 `8a37d0f`）。PR #285 mergeState 由 CONFLICTING → BLOCKED（等 CI required checks）；PR body 末尾已加「## 與 main 679a417 合併」段。

### 衝突檔與處置

- `docs/COLLABORATION.md`：**唯一衝突檔**，衝突只在 §4-b 巡檢 cron 模板那一行（:135）——LS-158 在同一行加了「派 qa 驗收多步驟票必帶 `qa-e2e.sh` 指示」、本票在同一行加了「磁碟水位段＋處置」。處置：以 main 版為底、用 python 套回本票片段（先斷言 HEAD 版＝base＋本票替換、main 版仍含原句，才寫回），雙方內容都在；LS-158 的 §1 qa subagent 段與 §7「QA 多步驟驗收不依賴 mobile-mcp」列、本票 §4-b 工具段／§6 ③／§7「設計票新鮮度與磁碟水位」列皆保留。`conflict-marker-check.sh` 綠、無殘留標記。
- `scripts/ops/supabase-lock.sh`／`supabase-lock.test.sh`、`scripts/gates/agent-tools-check.sh`／`agent-tools-check.test.sh`：本票未動，git 自動合併取 main 版（LS-158 的 lock exit 3／qa-e2e 正文規則），無衝突。

### 合併後自測

- `pen-open.test.sh` ✓ 30 組、`pen-read.test.sh` ✓ 8 組、`cleanup-merged.test.sh` ✓ 全綠、`patrol.test.sh` ✓、`detect-simulator.test.sh` ✓、`agent-tools-check.test.sh` ✓ 40 組、`agent-tools-check.sh` ✓（6 份定義／正文必含 4 條）。
- `supabase-lock.test.sh`：第一次跑 217 ✓／**1 ✗**——⑧「持有中不標 stale」用裸子字串 `hasnt … 'stale'`，被 `--status` 那一行的 `branch=hotfix/LS-176-pen-open-**stale**-cleanup` 誤中（非 merge 回歸、非 lock 腳本問題；同理 ⑤／㉕ 的 `has 'stale'` 在本分支上其實假綠）。`8a37d0f` 把四處 needle 改認腳本實際印的「⚠ stale」標記（dead-pid 偵測力道不變），重跑 **218 ✓／0 ✗**。這是 scope 外的一行式 test hardening——reviewer 若不要可 revert 該 commit（該測試會因本分支名再紅，CI 上 detached HEAD 不會）。

### 硬限制

未對活的 Pen 執行 `pen-open.sh`／`pen-read.sh`（含 `--status`）；未真刪任何模擬器／DerivedData；未跑 `simctl shutdown all`；未碰 supabase 容器（`supabase-lock.test.sh` 全程合成 lock 目錄）。

### 2026-09-04T21:05:16.258Z · Chih-Lin Yeh · `f6edabcc`

**2026-09-05 05:0x orchestrator — 併 main 解衝突 handoff `f9be427e` 收到（head `8a37d0f`）→ 派 merge-reviewer R2 審 merge 增量**

- 唯一衝突 `docs/COLLABORATION.md` §4-b :135（LS-158 qa-e2e 句 × 本票水位句），雙方保留；`supabase-lock.sh`／`agent-tools-check.sh` 取 main 版。
- 附帶 `8a37d0f`：`supabase-lock.test.sh` 裸 `stale` 針撞分支名，改認 `⚠ stale`——請 reviewer 裁定留或退。

### 2026-09-04T21:17:03.337Z · Chih-Lin Yeh · `98430ae2`

## merge-review R2 — PR #285（head 8a37d0f，增量 c59a398→`c3ed12b` merge＋`8a37d0f`）

裁決：**APPROVE**（無 blocker／無 major）。衝突解法正確、兩票的規約句都保住；`8a37d0f` 的測試針加固我裁定**留**（且證明它不是空轉）。R1 的三條 informational 不變、無新增。審的 head＝`8a37d0ffd1ee7708ca7c86d9ebfd45fac1d66be9`，與 `gh pr view 285 --json headRefOid` 當下值相同；`gh pr checks 285` **四項全 pass**（ci 6m30s／db 3m37s／lint 15s／rules 3m29s，run 33919269897——開審時三項在跑，我等收工才判讀）。2026-09-05 05:16 CST。硬限制同 R1：未碰活的 Pen（含 `--status`）、未真刪任何模擬器／DerivedData。

### （1）衝突解法 — 用「對 main 的差集」當主檢驗，比逐段讀 diff 硬

最有力的一條：**`git diff 679a417...8a37d0f` 只有 11 個檔**——就是 LS-176 的 10 檔＋`supabase-lock.test.sh`（8 行，即 `8a37d0f`）。也就是說相對 main，本 PR 的貢獻**完全沒有多出任何東西**，merge 沒有夾帶。

| 檢查 | 結果 |
|---|---|
| `supabase-lock.sh` 是否＝main 版 | `git rev-parse 8a37d0f:<file>` 與 `679a417:<file>` **blob 逐位元相同** ✓（沒有手改） |
| `agent-tools-check.sh` 是否＝main 版 | **blob 逐位元相同** ✓ |
| §4-b:135 cron 模板 | **同一行同時含** LS-176 的「磁碟水位段」「`[磁碟] ⚠` 可用 <20 GB → 對 Done 票逐張跑…」**與** LS-158 的 `qa-e2e.sh <login|publish|browse>` ✓ |
| LS-158 的 §1 列 | 在（`:15` qa subagent 段含 `qa-e2e.sh`） ✓ |
| LS-158 的 §7 列 | 在（`:249`「QA 多步驟驗收不依賴 mobile-mcp…」） ✓ |
| LS-176 的 §4-b／§6 ③／§7 列 | 在（§7 在 `:304`；§4-b 工具段與 §6 收尾儀式句都在——`磁碟水位` 全檔 4 處、`LS-176` 4 處、`LS-158` 3 處） |
| `conflict-marker-check.sh` | ✓ 綠（無衝突標記殘留） |

### （2）`8a37d0f` 裸 `stale` 針改認 `⚠ stale` — 診斷成立、且新針不空轉，我裁定**留**

**診斷我實測重現**：把 `c3ed12b` 版（裸 `stale`）的測試檔拿到同一個 worktree 跑 → **⑧ 紅**（`✗ ⑧ --status 持有中、pid 活著：不標 stale（輸出不應含「stale」）`）。原因就是 `--status` 的持有者行帶 `branch=`，而本分支叫 `hotfix/LS-176-pen-open-**stale**-cleanup`，裸針被分支名撞到。新版在同一個 worktree **218 ✓／0 ✗**。

**新針不是空轉**（這點比診斷更重要，我另做 mutation 證明）：把 `supabase-lock.sh` 裡 `holder_line` 印的「⚠ stale：持有者 pid 不存在」改成「⚠ STALEMARK…」→ **⑤ 立刻紅**（`輸出應含「⚠ stale」`）。所以這四根針真的釘在程式碼的行為上。

**裁定：留。** 三個理由：
1. **舊針本身是錯的、不只是撞名**：`has … 'stale'` 這種寫法在任何分支名含 stale 的地方都會**假通過**——即使程式碼不再印 stale 標記也照樣綠。換成 `⚠ stale` 是把假通過修成真斷言，方向是加嚴不是放寬。
2. **退回等於讓本分支的 CI 紅**：`rules` job 會跑 `supabase-lock.test.sh`，退回 ⑧ 必紅；唯一的替代是改分支名，成本更高、也治標不治本（下一個叫 `*-stale-*` 的分支會再中一次）。
3. **scope 影響最小**：4 行、只動測試檔、不動任何產品邏輯，且該檔在本 PR 其餘部分完全沒被碰過（`supabase-lock.sh` 與 main 逐位元相同）。

### （3）`8a37d0f` 上重跑

| 自測 | 結果 |
|---|---|
| `pen-open.test.sh` | ✓ 30 組 |
| `pen-read.test.sh` | ✓ 8 組 |
| `cleanup-merged.test.sh` | ✓ 全綠 |
| `patrol.test.sh` | ✓（patrol／session-start） |
| `detect-simulator.test.sh` | ✓ |
| `supabase-lock.test.sh` | ✓ **218 ✓／0 ✗** |
| `agent-tools-check.test.sh` | ✓ **40 組**（merge 帶進 LS-158 的第 4 條正文規則後的數字） |
| 真 repo `agent-tools-check.sh` | ✓「正文必含字樣 **4** 條」——R1 時是 3 條，差別來自 merge 進來的 LS-158，不是回歸 |
| **bash 3.2 複跑** | 六支（含 `supabase-lock.test.sh`）用 `/bin/bash` 3.2.57 **全部 OK** |

R1 已驗過的四個審點（pen-open 訊號 d、cleanup (e) 四道守門、patrol 水位段、detect-simulator 重用）在增量中**一行未動**（對 main 的差集裡那 10 檔與 `c59a398` 版相同），故不重驗；R1 的 mutation 結論（pen-open 2 個、cleanup 4 個全殺）繼續有效。

### R1 三條 informational 的狀態

不變、也不需在本輪處理：**I-1**（`cleanup (e)` 的「沒人在用」推斷殘餘 race——我維持「可接受、不擋」，建議下次動這段時加 `pgrep -f "$sim_udid"`，**不要**改成「Booted 即跳過」）；**I-2**（訊號 d 判的是 `.pen` 檔而非 worktree 目錄，比票名廣，與票文正文一致）；**I-3**（低水位時 SessionStart brief 幾乎必然把體積印成 `?`）。

### 四維度（增量）

race：增量沒有動任何並發路徑（merge 帶進的 `supabase-lock.sh` 是 main 版、與本票無關）。效能：無變化。平行：無。scope：相對 main 只多出 11 檔，其中 10 檔是票文 1–5，第 11 檔是 4 行測試針加固——如上裁定為必要且加嚴。


### 2026-09-04T21:18:59.509Z · Chih-Lin Yeh · `d21ee3eb`

**2026-09-05 05:18 orchestrator — merge-review R2 `98430ae2` APPROVE → PR #285 已併入 main `8e00758`；back-merge PR #287 main→development 已開（CI 綠即併）**

- `8a37d0f` 測試針加固裁定留（reviewer 實測舊針假通過、新針 mutation 會紅）。
- 後續：back-merge 併入 → dev→test → 隨 LS-175 同批 QA（本票 delta 只驗自測與 CI）→ test→main → 收尾（sweeper／retro／cleanup）→ Done。
- CLAUDE.md 收尾儀式那句：現行文字「移除已併入的 worktree／本機分支」仍為真（附屬資源清理是其子集），不另改；規約細節在 COLLABORATION §6 ③。

### 2026-09-04T21:47:41.476Z · Chih-Lin Yeh · `ea5e3074`

**2026-09-05 05:46 orchestrator — back-merge PR #287 已併入 development `34dd954` 並晉升 test；隨 LS-175 同批 QA（本票 delta：六支自測＋唯讀 dry-run＋patrol 水位段）**

### 2026-09-04T22:30:51.499Z · Chih-Lin Yeh · `eda15d07`

## QA — test `34dd954`（2026-09-05 06:29 CST）

**裁決：PASS**

範圍核對：`git diff --stat 679a417..34dd954` 只落在 LS-175（media 通知）與 LS-176（harness back-merge）兩票路徑，21 檔，無例外檔案。本票 delta 僅驗自測與唯讀觀察（依派工指示，未對活的 Pen 執行 `pen-open.sh`／`pen-read.sh`、未真刪任何模擬器／DerivedData、未跑 `simctl shutdown all`）。

### 逐條驗收

**1. 模擬「Pen 開著已刪 worktree 的檔」→ `pen-open.sh` 成功切檔；dirty 情境仍擋（自測）— ✓ PASS**
- `pen-open.test.sh` ✓ **30 組**全綠（含 ⑭a「舊路徑不存在，視為已捨棄」正樣本、⑭b「舊路徑存在且 dirty」負樣本仍拒絕清場）。
- `pen-read.test.sh` ✓ **8 組**全綠（同一對正／負樣本，經 `--force-reload` 封裝）。

**2. `cleanup-merged.sh --apply` 對假票清掉專屬機＋DerivedData（自測）；`patrol.sh` 水位段可觸發 ⚠ — ✓ PASS**
- `cleanup-merged.test.sh` ✓ 全綠（stub `xcrun`＋假 `LS_DERIVED_DATA_ROOT` 目錄，四道守門：指名票才套用／該票 worktree 已不在磁碟才動／Booted 先 shutdown 再 delete／simulator-lock 跳過；word-boundary LS-930 不誤中 LS-9300；`--all` 不套用；dry-run 不刪）。
- `patrol.test.sh` ✓ 全綠（含磁碟水位 ㉑ 全部斷言：低於門檻 ⚠ 並列 Devices／DerivedData 體積與 `LS-*` 台數；高於門檻不標、不跑 `du`；`PATROL_DISK_MIN_GB` 非整數 exit 2）。
- 真環境唯讀驗證：`cleanup-merged.sh --dry-run LS-173` → 列出 `[dry-run] delete 專屬模擬器 LS-173-iPhone17Pro（A025F80F-…）`＋`[dry-run] 刪除 DerivedData LittleSprout-gcxz…`；跑完立即核對兩者**仍存在**（`xcrun simctl list devices` 與 `ls DerivedData/` 皆確認未被刪除，dry-run 真的沒有動作）。
  - `patrol.sh 40`（不加 `--linear`）：印出「磁碟水位」段——`可用 82 GB，LS-* 專屬模擬器 5 台 ok`（本機磁碟充足，未觸發 ⚠，門檻邏輯已由自測的合成低門檻案例覆蓋，見上）。

**3. merge-reviewer 通過；hotfix 流程（自 main、PR 回 main、back-merge）— ✓ PASS**
- merge-review R1 `42383240`：APPROVE（0 blocker／0 major／3 informational），逐項 mutation 驗證（pen-open 訊號 d 兩個方向殺得死、cleanup (e) 四道守門全殺、bash 3.2 相容）。
- merge-review R2 `98430ae2`：APPROVE（衝突解法正確、`supabase-lock.test.sh` 測試針加固裁定留並驗證非空轉）。
- Hotfix 流程：分支 `hotfix/LS-176-pen-open-stale-cleanup` 自 `origin/main`；PR #285 → `main`（併入 `8e00758`）；back-merge PR #287 main→development 已併入（`34dd954` 即為 back-merge 後晉升 test 的結果）。

### 額外驗證（本輪自測與正文 gate）
- `detect-simulator.test.sh` ✓ 全綠（⑪ 同票不同機型同 runtime → 重用既有；⑫ 同票不同 runtime → 建新機，不跨 runtime 誤重用）。
- `supabase-lock.test.sh` ✓ **218 ✓／0 ✗**（與 merge-review R2 報的數字一致——裸 `stale` 針改認 `⚠ stale` 標記後，不再被分支名 `hotfix/LS-176-pen-open-**stale**-cleanup` 誤中）。
- `agent-tools-check.test.sh` ✓ **40 組**；真 repo `agent-tools-check.sh` ✓「6 份 agent 定義；正文必含字樣 **4** 條」（development 已含 LS-158 第 4 條 `qa-e2e.sh`，非回歸）。

### 產出位置
- LS-96 三則池項銷除（`56eeaee0`／`7c9fe5bd`／`0e75271d`）核對存在，內容與程式碼一致（已由 merge-review R1 逐一核實）。
- `docs/COLLABORATION.md` §4-b／§6 ③／§7 對照表列已同步，並與 LS-158 的合併衝突正確保留雙方內容（merge-review R2 核對）。

模擬器：本票純 harness／自測，全程未 boot 任何模擬器（`cleanup-merged.sh --dry-run` 唯讀觀察既有的 LS-173 專屬機，未新建、未刪除）。

### 2026-09-04T22:33:14.430Z · Chih-Lin Yeh · `230e8836`

**2026-09-05 06:32 orchestrator — QA PASS `eda15d07`（六支自測＋dry-run 未真刪＋patrol 水位段）→ 已到 main `34dd954`（v0.12.0，含本票 hotfix 與 back-merge）**

- 收尾：sweeper 批次巡檢中 → retro → Done；pen-open 舊路徑放行已在 LS-177 VR R2 實機驗證有效（`b017cbd1`）。

### 2026-09-04T22:36:21.232Z · Chih-Lin Yeh · `6a3fe610`

## dead-code sweep — LS-176

**2026-09-05 06:35 CST，範圍：`git diff --stat 679a417..34dd954` 落在本票的檔案**（`scripts/ops/{pen-open.sh,pen-open.test.sh,pen-read.test.sh,cleanup-merged.sh,cleanup-merged.test.sh,patrol.sh,patrol.test.sh,supabase-lock.test.sh}`、`scripts/gates/{detect-simulator.sh,detect-simulator.test.sh}`、`docs/COLLABORATION.md`）。主 checkout `/Users/clyeh/little-sprout`（唯讀，head `34dd954`），未執行 pen-open／pen-read／pen-status，未跑 `supabase-lock.sh --hold`（本票 delta 不需要活的 DB）。

### Findings

**未發現本票引入的死碼。** 逐檔核對如下：

1. **`pen-open.sh`**：`check_root_safe()` 訊號 d（`[ ! -e "${root}${suffix}" ]`）——`suffix` 變數在 `:258` 全域定義、於 `:283`／`:344` 兩處使用，作用域正確，非未定義變數誤用。舊版 `[ ! -d "$root" ]` 分支已完整替換為新邏輯，沒有殘留的舊判斷分支（`git diff` 顯示乾淨替換，非新增旁支）。
2. **`pen-open.test.sh`／`pen-read.test.sh`**：⑭a／⑭b 一組正負樣本，皆有斷言且對應程式碼行為（放行 vs 拒絕），無被註解掉的案例。
3. **`cleanup-merged.sh`** (e) 段：新增變數 `sim_removed`／`sim_skipped_lock`／`dd_removed`／`FILTER_WT_PATHS`／`FILTER_WT_HANDLED`／`OUT_E` 逐一核對——全部在摘要輸出行（`:691` 附近）或判斷邏輯中被讀取，無宣告未用的孤兒變數；`dd_matches()`／`process_wt()` 內新增的記錄邏輯（`FILTER_WT_PATHS`／`FILTER_WT_HANDLED`）確實供 (e) 段判斷「該票是否仍有 worktree 留在磁碟」使用。
4. **`cleanup-merged.test.sh`** ⑳：新增 stub `xcrun`＋假 `LS_DERIVED_DATA_ROOT` 在檔頭「一開始就裝上」（非只給 ⑳ 用，見測試檔內註解說明原因——避免前面既有案例落到真機），33 條斷言逐一對應四道守門（指名票／worktree 已不在／simulator-lock／dry-run 不刪）與 word-boundary，無孤兒斷言或被跳過的案例。
5. **`patrol.sh`** 磁碟水位段：`disk_avail_gb`／`disk_flag`／`disk_devices_gb`／`disk_derived_gb`／`disk_dedicated` 五個變數在 human／brief／json 三種輸出模式都有對應讀取點；`du_with_timeout()`／`size_gb_of()` 兩個 helper 都在同一段落內被呼叫，非死函式。
6. **`patrol.test.sh`** ㉑：17 條斷言涵蓋門檻觸發／不觸發／非整數三種情境 × human/brief/json 三種輸出模式，`PATROL_DISK_MIN_GB=0` 的全檔預設有明確理由（隔離開發機當下磁碟狀態，避免既有斷言偶發紅），非隨意設定。
7. **`detect-simulator.sh`**：新函式 `find_udid_same_ticket()` 使用的 `ticket`／`header_os` 兩個變數在 `:72-74`／`:59` 已於函式呼叫前定義，作用域正確；呼叫點在 `:150` 確認有實際串接（找不到精確名稱才呼叫，找到就用，否則落回 `create_dedicated`），非孤兒函式。
8. **`supabase-lock.test.sh`**：裸 `stale` 針改認 `⚠ stale`——這 4 處改動已由 merge-review R2（`98430ae2`）實測證明非空轉（mutation：把腳本印的訊息改字會讓斷言紅），不是漂白測試。
9. **`docs/COLLABORATION.md`**：§4-b／§6 ③／§7 三處新增內容與程式碼逐字對得上（工具段描述的參數名、預設值、行為與實際腳本一致）；未發現指向已改名檔案或已移除腳本的殘留路徑。
10. 全 diff（11 個檔案）grep `TODO|FIXME|XXX|console\.log` 於新增行：**0 筆命中**（bash 腳本用 `console.log` 搜尋詞不適用，但同一 grep 也覆蓋此檔案集合確認無殘留）。

### 既有死碼觀察

無（本次巡檢範圍內未發現與本 feature 無關、但恰好路過的既有死碼；merge-review R1/R2 已記錄的既有缺口——如「附屬資源沒人在用只靠推斷」——屬已知風險而非死碼，已入 LS-96 池項 `56eeaee0`／`7c9fe5bd`／`0e75271d`）。

### 建議處置

無需開票——本票沒有引入死碼，不需入池 LS-96、不需另開 fix 票。

### 搜尋方法與盲區聲明

- 方法：`git diff --stat 679a417..34dd954` 鎖定檔案清單，逐檔 `git diff`／`Read` 全文；對新增變數／函式逐一 grep 定義處與使用處確認作用域與呼叫關係；grep TODO/FIXME/XXX/console.log 字面。
- 盲區：純文字搜尋抓不到 reflection、字串拼接、動態 dispatch（bash 腳本的 `eval`／間接變數展開本次未發現此類用法，但未窮盡排除）；未重新執行任何 `.test.sh` 自測（QA `eda15d07` 已於同一 head 跑過全部六支自測，本次巡檢只看「死碼」，不重覆驗證功能）；未對活的 Pen 執行任何操作、未真刪任何模擬器／DerivedData（依派工指示，本票的自測與唯讀觀察已由 QA 驗證有效）。

### 2026-09-04T22:37:28.573Z · Chih-Lin Yeh · `8bdcaa1e`

**2026-09-05 06:4x orchestrator — 收尾儀式 → Done**

- sweeper `6a3fe610` 0 finding；worktree／分支已清；LS-96 `56eeaee0`／`7c9fe5bd`／`0e75271d` 已銷除。
- **Lesson learning**：(1) hotfix 自 main 切後若 test→main 先晉升，PR 必衝突（本票與 LS-158 同改 COLLABORATION）——merge 前先 `gh pr view --json mergeable`；衝突解法「取 main 版＋重套自己那句」＋差集比對零夾帶，reviewer 用 `git diff main...head` 只有 11 檔證明乾淨，是好的複驗手法。(2) 裸字串針撞分支名（`stale`）——測試針要認腳本真正輸出的標記；同型盲區可能在其他 `.test.sh`。(3) pen-open 舊路徑放行修好一半，另一半（`--force-reload` SIGKILL 讓 MCP 斷線）在 LS-177 VR R2 立刻暴露 → LS-180；harness 修 Pen 相關問題時要把「MCP 連線」當一等公民。(4) 附屬資源「在用中」偵測靠推斷 → LS-96 `5566d38b`（`pgrep -f UDID`）。(5) 磁碟水位段首輪即產出數字（84→80 GB），SessionStart brief 的 du 預算風險 → LS-96 `5566d38b` I-3。

