# LS-45 Harness：破壞性核可標記改整行錨定（子字串比對可被無意提及滿足）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | — |
| Cycle | — |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-23T07:52:51.535Z |
| 開始 | 2026-08-24T15:12:15.897Z |
| 完成 | 2026-08-24T15:33:41.153Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-45/harness破壞性核可標記改整行錨定子字串比對可被無意提及滿足 |
| 關係 | related→LS-37, related←LS-96, related←LS-80, related←LS-63, related←LS-58, related←LS-53, related←LS-41 |

## 描述

來源：[LS-37](https://linear.app/little-sprout-app/issue/LS-37/db-安全強化invites-insert-policy-收斂-唯一寫入路徑改-create-invite-rpc) 實作 agent 自首＋實測（run 32626369903）。CI rules job 的破壞性核可檢查是 `grep -q 'DESTRUCTIVE-APPROVED'` 對整個 PR body 的**純子字串比對**——PR body 寫「等待使用者蓋 DESTRUCTIVE-APPROVED」這種**語意相反的提及**也會讓 gate 通過。比 §7 已知限制「agent 技術上寫得進去」更嚴重：不需要意圖，無意提及即失守。

修法：比對改**整行錨定**（`grep -qE '^[[:space:]]*DESTRUCTIVE-APPROVED[[:space:]]*$'`——標記必須獨佔一行）；§7 對照表該列同步更新已知限制描述；COLLABORATION §6 的核可方式補「標記須獨佔一行」。走 hotfix。附帶知識（[LS-37](https://linear.app/little-sprout-app/issue/LS-37/db-安全強化invites-insert-policy-收斂-唯一寫入路徑改-create-invite-rpc) 實測）：workflow 的 pull_request 觸發不含 edited——改 body 不重跑 CI，需 close/reopen 或 push。

## 留言（2）

### 2026-08-24T15:12:21.675Z · Chih-Lin Yeh · `17b33152`

**Handoff**（branch `hotfix/LS-45-line-anchored-stamp` 自 main，commit `9dbe1ec`，PR #56 → main，CI run 32742172961 四 job 全綠）

- 偵測點只有一處：ci.yml Migration rules 的 inline `grep -q`（本機 hook 讀不到 PR body，無對應點）。
- 新增 `scripts/gates/destructive-approval-check.sh`：stdin、`^[[:space:]]*DESTRUCTIVE-APPROVED[[:space:]]*$`（含 CR，web UI CRLF 可認）、grep 錯誤亦視為未核可（fail closed）。ci.yml 改呼叫；新增 rules job「Gate 腳本自測」跑 `destructive-approval-check.test.sh` 7 樣本（獨立成行／獨立成行＋空白＋CRLF／〈〉括起／前綴文字／行內尾隨／粗體包起／空 body）。
- 負向測試有牙：三個 reject 樣本舊 grep 全誤放、新腳本全拒。PR #55 現行 body 回歸 exit 0。
- COLLABORATION §6 補「標記須裸寫獨佔一行」＋「改 body 不重跑 CI 需 close/reopen」；§7 列改行錨定描述、既有已知限制保留。
- 行為變更：`**DESTRUCTIVE-APPROVED**`／反引號包起不再放行。未加 `edited` 觸發（會讓每次改 body 重跑 macOS CI，超 scope，可另開票）。

→ 派 merge-reviewer；併入 main 後 back-merge development／test。

### 2026-08-24T15:26:29.233Z · Chih-Lin Yeh · `a9371c03`

**merge-reviewer APPROVE**（無 major）；minor F1-F3 已補（commit `0a20683`：§6/§7 補「HTML 註解／code fence 內獨佔行仍放行、審 raw body」已知限制；樣本 7→11：末行無換行正向＋引用前綴／小寫／`-NOT` 尾隨三個 reject）。CI run 32744233035 四 job 全綠。**PR #56 已併入 main**（在 PR #59 之後，依 F5 順序）。→ back-merge main→development／test PR 開立中，落地後 Done。
Lesson：注入面確認安全（PR body 走 job-level env＋引號 printf，全檔無 run 內 `${{ }}` 插值）——此寫法定為 ci.yml 讀 PR 資料的規範，可入 COLLABORATION。

