# LS-227 Task：LS-20 後端——DESTRUCTIVE：移除 deprecated `public.purge_storage_queue_enqueue_orphans(text, uuid, text[])` v1（LS-222 起零生產呼叫端，由 `_v2` 取代）＋對應 `109_` 自測與 API.md §3／§9 條目（需使用者 DESTRUCTIVE-APPROVED comment）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Low |
| 標籤 | size:S, lane:backend |
| Cycle | 2 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M3 相簿與上傳 |
| 估點 | — |
| 建立 | 2026-09-06T14:53:46.527Z |
| 開始 | 2026-09-12T01:56:04.475Z |
| 完成 | 2026-09-12T19:20:28.767Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-227/taskls-20-後端-destructive移除-deprecated-publicpurge-storage-queue |
| 父票 | LS-20 Story：相簿與照片上傳（壓縮／縮圖／背景續傳） |
| 關係 | related→LS-233, related→LS-235, related→LS-208, related→LS-224, related→LS-213, related→LS-228, related→LS-96, related→LS-204, related→LS-223, related→LS-123, related→LS-222, related←LS-236, related←LS-132, related←LS-218 |

## 描述

## 來源

[LS-222](https://linear.app/little-sprout-app/issue/LS-222/taskls-20-後端-purge-storage-孤兒掃描後續tssql-媒體路徑規則單一來源is-media-object-path)（migration `20260906091007` 檔頭）以 `_v2` 新函式避開 `create or replace` 不能改回傳型別的限制，舊版 `purge_storage_queue_enqueue_orphans(text, uuid, text[]) returns integer` 保留、`docs/API.md` §3／§9 已標 deprecated；[LS-223](https://linear.app/little-sprout-app/issue/LS-223/taskls-20-後端-purge-storage-孤兒掃描收口候選預算扣除不合法路徑f2不合法路徑改計數樣本回報f1舊-rpc) sweeper（[LS-222](https://linear.app/little-sprout-app/issue/LS-222/taskls-20-後端-purge-storage-孤兒掃描後續tssql-媒體路徑規則單一來源is-media-object-path) comment `3cbe31bb`「不確定」項）確認 v1 唯一呼叫端是 `supabase/tests/109_soft_delete_unreferenced_media.sql:339` 自測，生產零呼叫。orchestrator 2026-09-06 裁定：保留至本票、以 DESTRUCTIVE 流程移除。

## 範圍

1. 新 migration：`drop function public.purge_storage_queue_enqueue_orphans(text, uuid, text[]);`＋`revoke` 殘留 grant（若有）；`migration-breaking-check.sh` 會判 DESTRUCTIVE——PR 需使用者本人在 PR comment 留 `DESTRUCTIVE-APPROVED` 標記（[LS-123](https://linear.app/little-sprout-app/issue/LS-123/harness破壞性-migration-核可標記改認-pr-comment使用者本人留言獨佔行body-覆寫不再洗掉核可) gate），orchestrator 不得代貼。
2. `supabase/tests/109_soft_delete_unreferenced_media.sql` 刪 v1 自測段；`60_default_privileges.sql` 白名單（`v_service_role_rpcs`／SECURITY DEFINER 白名單 28→27）同步；`docs/API.md` §3／§9 刪條目並在 §9 對帳清單註記移除日期。
3. 驗證：`run.sh` 綠、`api_contract_check.py` RPC 數對得上、`get_advisors` 無新 WARN。

## 不做

`purge_storage_unknown_media_paths()`（仍被 `purge_storage_classify_orphan_paths` 內部呼叫，非死碼）。

## 驗收

- [ ] 正式站部署後 `pg_proc` 無 v1；`purge-storage` 排程隔日 log 正常
- [ ] merge-reviewer 通過（scope：只刪 v1）；QA delta

## 留言（13）

### 2026-09-12T01:48:09.886Z · Chih-Lin Yeh · `b7bf419d`

**orchestrator 09-12 09:47**：巡檢動作清單「lane:backend 在飛 0、候補 LS-227（cycle 外，scope+）」→ 補 cycle 2、改 Ready（scope+，取自 cycle 外）。worktree `.claude/worktrees/LS-227`＝`feature/LS-227-drop-enqueue-orphans-v1`（自 development `c5f07b9`），已派 ios-dev（範圍照票文 1–3；PR → development）。**提醒使用者**：PR 開出後需本人在 PR comment 留 `DESTRUCTIVE-APPROVED` 獨佔行（LS-123 gate），在此之前 `rules` check 會 BLOCKED，屬預期；agent／orchestrator 不代貼。

### 2026-09-12T02:19:53.390Z · Chih-Lin Yeh · `df09052a`

**ios-dev 2026-09-12 02:17 UTC — LS-227 R1 handoff**（PR #355，head `7b7c034973b9818297a3eea63add17f89e080aaa`）

## Ticket

LS-227 — DESTRUCTIVE：移除 deprecated `public.purge_storage_queue_enqueue_orphans(text, uuid, text[])` v1（LS-222 起零生產呼叫端，由 `_v2` 取代）。

## 已完成

1. **票文範圍 1（新 migration）**：`supabase/migrations/20260912015310_drop_purge_storage_queue_enqueue_orphans_v1.sql`（commit `4d5fe3c`）——`drop function public.purge_storage_queue_enqueue_orphans(text, uuid, text[]);`。檔頭含 DESTRUCTIVE 理由、v2 取代說明、零生產呼叫端證據（引 LS-222 comment `3cbe31bb-4ab8-4c3d-bbd9-609869afb37c` 逐條查實①）、殘留 grant 查證原文、回滾指引。**無額外 `revoke` 陳述式**——查證見下方「已驗證」第 1 項，`drop function` 本身即移除全部殘留 ACL。
2. **票文範圍 2（測試＋白名單同步）**：commit `c6660b3`：
   - `supabase/tests/109_soft_delete_unreferenced_media.sql`：刪原第 4 段（v1 路徑驗證自測，原 `:316-367`），第 3／5／6／7 段維持不動、段號不重排；檔頭補一段 LS-227 記錄（`:40-45`）。
   - `supabase/tests/60_default_privileges.sql`：`v_service_role_rpcs`（`:518-523`）移除 v1 一行，**8→7 支**；`:418-441` 說明文字同步移除 v1 引用、補 LS-227 記錄。
3. **票文範圍 2（docs/API.md）**：commit `7b7c034`：
   - §3「media」小節「現況」段（`:410-417`）訂正為 LS-227 已移除；SECURITY DEFINER RPC 白名單小節（`:959-966`）括號寫法 `enqueue_orphans[_v2]` 改成單一 `_v2`——**這張表的 28 支不受影響**（見「已驗證」第 5 項說明）。
   - §9 機械對帳清單移除 v1 一行（`:2586` 原文）——**40→39 支**；deprecated 散文段（`:2559-2565`）改記移除紀錄，附日期 2026-09-12。

## 已驗證（怎麼驗）

1. **殘留 grant 查證（範圍 1 驗收）**：本票分支起點 `c5f07b9` 容器實查（`.claude/evidence/LS-227/r1/v1-grants-before-drop.log`）：
   ```sql
   select p.oid::regprocedure::text, p.prosecdef, p.proacl from pg_proc p
    where p.pronamespace='public'::regnamespace and p.proname='purge_storage_queue_enqueue_orphans';
   -- purge_storage_queue_enqueue_orphans(text,uuid,text[]) | t | {postgres=X/postgres,service_role=X/postgres}
   select grantee, privilege_type, is_grantable from information_schema.routine_privileges
    where routine_schema='public' and routine_name='purge_storage_queue_enqueue_orphans';
   -- postgres EXECUTE YES ; service_role EXECUTE NO
   ```
   只有 `postgres`（owner）與 `service_role`（來自 `20260822120300_harden_default_privileges.sql:93` 的全域 default privileges，非本函式專屬 grant）。無 `anon`／`authenticated`／`PUBLIC`——已在 `20260906050606_soft_delete_unreferenced_media.sql:217` 的 `revoke ... from public, anon` 收回。結論：無殘留可 revoke 的額外授權，`drop function` 即移除剩下的 ACL。
2. **驗收③「run.sh 全綠」＋LS-204 通道**：`bash scripts/ops/supabase-lock.sh -- supabase db reset`（套用全部 42 支 migration，含本票新 migration，exit 0）→ `bash scripts/ops/supabase-lock.sh -- bash supabase/tests/run.sh`，**尾行 `✓ 全部 DB 測試通過`**，exit 0。**→ 連線方式：`docker exec supabase_db_little-sprout psql`**（`.claude/evidence/LS-227/r1/run.sh.log` 第 1 行）。`60_default_privileges.sql` 印出 `ok：白名單內的 7 支 public service_role-only RPC 授權正確`（同檔 `:298` 附近）；`109_soft_delete_unreferenced_media.sql` 不再出現 v1 段落、`purge_storage_queue_enqueue_orphans_v2` 段落照常 `ok`。
3. **驗收③「migration-breaking-check 判 DESTRUCTIVE」（預期，不迴避）**：`bash scripts/gates/migration-breaking-check.sh --base origin/development`（對已 push 的 head 重跑）：
   ```
   DESTRUCTIVE	drop function public.purge_storage_queue_enqueue_orphans(text, uuid, text[])
   BREAKING	drop function public.purge_storage_queue_enqueue_orphans(text, uuid, text[])
   ```
   `migration-immutable-check.sh --base origin/development` 綠；`migration-version-check.sh` 綠（tree 共 42 檔）。
4. **驗收③「api_contract_check RPC 數對得上」**：`python3 scripts/gates/api_contract_check.py --text docs/API.md supabase/migrations` → `✓ RPC 39 支、表 25 張，docs/API.md 與 schema 一致`（40→39，drop 掉的那一支）。
5. **白名單數字核對（派工單原寫「28→27」，實查更正）**：`v_service_role_rpcs`（`60_default_privileges.sql`）**8→7**（`run.sh` 輸出「白名單內的 7 支 public service_role-only RPC」）。docs/API.md `909` 行起「SECURITY DEFINER RPC 白名單（authenticated 可執行）」的 **28 支不受影響**——那張表對應的是 `v_definer_rpcs`（authenticated 可執行的清單），v1 從未在這張白名單裡（v1 是 service_role-only，屬 `v_service_role_rpcs`），`run.sh` 同批印出 `白名單內的 28 支 public SECURITY DEFINER RPC` 維持不變、`4 支 SECURITY INVOKER` 也維持不變。派工單「28→27」的假設不成立，本輪已用查詢核實並訂正。
6. **Mutation（scratchpad 副本，未落地進 repo）——三支，逐一「改了什麼→哪條紅→斷言原文」**：
   - **改了什麼**：把 v1 重新加回 `v_service_role_rpcs`（函式已在容器裡 DROP）。**哪條紅**：`60_default_privileges.sql` 第 8 段 `select array_agg(f::regprocedure::oid) ...`。**斷言原文**：`ERROR:  function "public.purge_storage_queue_enqueue_orphans(text, uuid, text[])" does not exist`（psql exit=3，真實 Postgres 錯誤，非 crash／非 build fail）。原文見 `.claude/evidence/LS-227/r1/mutation-60-whitelist.log`。
   - **改了什麼**：把 v1 重新加回 `docs/API.md` 的 `API-CONTRACT:RPC` 機械清單。**哪條紅**：`api_contract_check.py` 對 schema 反查。**斷言原文**：`✗ api-contract gate（text 模式／best-effort）：RPC 清單對不上 docs/API.md 的 API-CONTRACT:RPC 區塊\n  - 文件有、schema 沒有（幽靈 RPC）：purge_storage_queue_enqueue_orphans(text, uuid, text[])`（exit=1）。
   - **改了什麼**：補回 `109_soft_delete_unreferenced_media.sql` 原第 4 段對 v1 的呼叫。**哪條紅**：該 `do $$ ... select public.purge_storage_queue_enqueue_orphans(...) $$`。**斷言原文**：`ERROR:  function public.purge_storage_queue_enqueue_orphans(unknown, uuid, text[]) does not exist\nHINT:  No function matches the given name and argument types.`（psql exit=3）。原文見 `.claude/evidence/LS-227/r1/mutation-109-v1-test.log`。
7. **push gate**：`git push -u origin feature/LS-227-drop-enqueue-orphans-v1` 通過（`✓ push gate 通過`，內部呼叫 `bash scripts/gates/migration-breaking-check.sh` 等同一組 gate，見第 3 項原文），印出預期的 DESTRUCTIVE／BREAKING 提醒（不擋 push，CI／PR 端才擋）。
8. **PR body 申報**：`bash scripts/gates/pr-body-check.sh <body-file> --branch feature/LS-227-drop-enqueue-orphans-v1 --verify` → `✓ PR body 檔頭段含本票票號 LS-227`／`✓ 申報反查：LS-96 行 0 條、「已修」行 0 條`，**EXIT=0**（直接看 exit code，未接管線）；`breaking-section-check.sh` 對同一份 body 單獨複驗 `EXIT=0`。

## 未完成

- **等待使用者 `DESTRUCTIVE-APPROVED` PR comment**（PR #355，LS-123 gate）——`rules` check 在核可前會紅／BLOCKED，這是預期狀態，不會 rerun、不會嘗試繞過。
- 本輪為初次送審，尚未經 merge-reviewer，**無 informational 待處置**（票文「不做」項 `purge_storage_unknown_media_paths()` 已依票文原樣不動，非本票遺漏）。

## 風險

- DESTRUCTIVE 變更：移除一支既有 public RPC 簽名，零生產呼叫端已由 LS-222／LS-223 兩輪查實＋本票再次殘留 grant 查證確認，風險評估為低；正式站部署後仍需依票文驗收①核對 `pg_proc` 無 v1、`purge-storage` 排程隔日 log 正常（本票只涵蓋本機，部署由後續流程執行）。
- 未觸碰 `purge_storage_unknown_media_paths()`（票文明列不做，仍被內部呼叫，非死碼）。

## 產出位置

- PR：https://github.com/CLYEH/little-sprout/pull/355（head `7b7c034973b9818297a3eea63add17f89e080aaa`）
- Commits：`4d5fe3c`（migration）／`c6660b3`（tests＋白名單）／`7b7c034`（docs/API.md）
- `supabase/migrations/20260912015310_drop_purge_storage_queue_enqueue_orphans_v1.sql`
- `supabase/tests/109_soft_delete_unreferenced_media.sql`、`supabase/tests/60_default_privileges.sql`
- `docs/API.md`
- 證據：`.claude/evidence/LS-227/r1/`（`run.sh.log`、`v1-grants-before-drop.log`、`mutation-60-whitelist.log`、`mutation-109-v1-test.log`）
- PR body 暫存檔：`scratchpad/LS-227-pr-body.md`（session scratchpad）
- 模擬器已關：無（本票未使用模擬器，純 SQL／文件票）

### 2026-09-12T02:36:12.198Z · Chih-Lin Yeh · `7685fd50`

**merge-reviewer 2026-09-12 — LS-227 merge-review R1 裁決：APPROVE**（PR #355，審的 head `7b7c034973b9818297a3eea63add17f89e080aaa`）

審查範圍：`git diff c5f07b99dcac114217cb886db1fb776dcec22493...7b7c034`，4 檔 93+／72-（新 migration `20260912015310_drop_purge_storage_queue_enqueue_orphans_v1.sql`、`supabase/tests/109_soft_delete_unreferenced_media.sql`、`supabase/tests/60_default_privileges.sql`、`docs/API.md`）。lock 持有時長：未持有（全程單一命令包 `scripts/ops/supabase-lock.sh -- <命令>`，未進互動持有段）。

## 裁決

**APPROVE** —— blocker 0、major 0、minor 1、informational 3。

## 四個必審維度

- **Scope**：乾淨。diff 只碰票文範圍 1–3 的四個檔；`purge_storage_unknown_media_paths()` 依票文「不做」原樣未動（`git grep` 確認仍在 `60_default_privileges.sql` 的 `v_service_role_rpcs` 與 `109_…sql` 第 3 段）。無順手重構、無無關改動。
- **Race condition**：本票不適用（純 DDL DROP＋測試／文件），無 Swift Concurrency／佇列／快取面。但 DROP 有**部署時序**風險面（若正式站現役 EF 仍呼叫 v1，DROP 一上去排程就會在下次 invocation 炸），已實查並排除，見下方查實 2。
- **運算效能**：不適用（移除一支函式，無 RLS policy／查詢計畫／分頁／解碼路徑變動）。
- **平行優化**：不適用。

## 逐項查實（對應派工單 1–6 與票文驗收）

1. **零呼叫端（派工單 1）**——自查屬實。`git grep -n purge_storage_queue_enqueue_orphans` 全 repo（含 `supabase/functions/**/*.ts`、migrations、tests、docs）：排除 `_v2` 後剩 22 處**全為散文／註解／歷史 migration 定義／本票新 DROP**，零實際呼叫。`supabase/functions/purge-storage/index.ts` 的三個 `supabase.rpc(` 呼叫點（`:238` classify、`:267` enqueue、`:336` mark_failed）分別是 `purge_storage_classify_orphan_paths`／`purge_storage_queue_enqueue_orphans_v2`／`purge_storage_queue_mark_failed`——無 v1。
2. **正式站呼叫數（派工單 1）——查了，但這條證據無效，見 INFO-1**。`supabase db query --linked` 對 `pg_stat_user_functions` 查：v1 `calls=0`、`_v2` 也 `calls=0`；再查 `current_setting(track_functions)` 回 **none**，即正式站根本沒在收集函式層級統計，`calls=0` 無法區分「沒被呼叫」與「沒在統計」。改用兩條**非空洞**的正式站證據取代：（a）掃 `pg_proc.prosrc` 找 DB 端呼叫者 → **0 列**（無任何函式內部呼叫 v1）；（b）`supabase functions list` 顯示 `purge-storage` 部署版本 **v5、updated_at 2026-09-06T14:45:45Z**，晚於 LS-222 的 `44fe1ac`（2026-09-06T10:08Z）與 LS-223 的 `6a286f1`（2026-09-06T11:30Z），且 `6a286f1` 是 `index.ts` 的最後一筆 commit——**正式站現役 EF 就是呼叫 `_v2` 的版本**，部署本 migration 不會打斷在跑的排程。四條查詢原文存於 `scratchpad/LS-227-prod-v1-evidence.log`。
3. **migration 正確性（派工單 2）**——屬實。`drop function public.purge_storage_queue_enqueue_orphans(text, uuid, text[]);` 與原定義 `20260906050606_soft_delete_unreferenced_media.sql:192-197` 的 `(p_bucket_id text, p_family_id uuid, p_object_paths text[])` 參數型別順序完全一致。**殘留 grant 自查對照**：本機容器（reset 前）`.claude/evidence/LS-227/r1/v1-grants-before-drop.log` 原文為 `proacl={postgres=X/postgres,service_role=X/postgres}`、`information_schema.routine_privileges` 只有 postgres EXECUTE YES／service_role EXECUTE NO；我另對**正式站**查同兩項，v1 與 `_v2` 的 acl 皆為 `{postgres=X/postgres,service_role=X/postgres}`——兩者形狀相同，證明 service_role 那筆來自 `20260822120300_harden_default_privileges.sql:93` 的全域 `alter default privileges grant execute on functions to service_role`（我已 `sed -n 88,96p` 讀原文核對），不是本函式專屬 grant。**handoff「不需 revoke」的結論成立**：沒有函式專屬授權可收回，`drop function` 連同 ACL 一併移除。reset 後我實查本機 `pg_proc`＋`routine_privileges`，v1 兩張表皆**查無此列**、只剩 `_v2`，零殘留。
4. **`supabase db reset` 從零套用全部 migration（票文驗收③）**——自跑屬實。`cd <worktree> && bash scripts/ops/supabase-lock.sh -- supabase db reset`：42 支 migration 全數套用（尾端可見 `Applying migration 20260912015310_drop_purge_storage_queue_enqueue_orphans_v1.sql...`），`Finished supabase db reset`。接著 `bash scripts/ops/supabase-lock.sh -- bash supabase/tests/run.sh` → **`✓ 全部 DB 測試通過`，EXIT=0**；**→ 連線方式：`docker exec supabase_db_little-sprout psql`**（log 第 1 行，與 handoff 申報的通道一致，LS-204 判準通過）。
5. **白名單三數字（派工單 3）——handoff 對派工單假設的訂正成立**，三個數字我逐一獨立驗：
   - `v_service_role_rpcs` **8→7**：以 regex 解析 `60_default_privileges.sql` 陣列得 7 支；`run.sh` 實跑印 `ok：白名單內的 7 支 public service_role-only RPC 授權正確`。
   - `docs/API.md` §9 機械對帳 **40→39**：`python3 scripts/gates/api_contract_check.py --text docs/API.md supabase/migrations` → `✓ RPC 39 支、表 25 張，docs/API.md 與 schema 一致`，EXIT=0。
   - SECURITY DEFINER authenticated 白名單**維持 28**：`v_definer_rpcs` 解析得 28；`run.sh` 印 `白名單內的 28 支 public SECURITY DEFINER RPC` 與 `public schema 內 authenticated 可執行的 SECURITY DEFINER RPC 恰好是白名單內的 28 支`；`v_invoker_rpcs` 4 支亦未變。**派工單原寫的 28→27 確實是錯的**——v1 是 service_role-only，本來就不在 authenticated 白名單裡，`git grep` 確認 `docs/API.md` 該表（`:909-966`）從未列過 v1。
   - `60_default_privileges.sql` 改動**只刪 v1 一列**（diff 逐行看：一列刪除＋三處說明文字，`_v2` 與其餘 6 支原樣）。
6. **測試（派工單 4）——只刪 v1 自測段、v2 斷言完整保留，屬實且零覆蓋損失**。diff 只有兩個 hunk：檔頭補 LS-227 沿革 7 行、刪原第 4 段 53 行；第 3／5／6／7 段一行未動、段號未重排。**關鍵覆蓋比對**：被刪的第 4 段三條 fixture（合法路徑／家庭前綴不符／形狀不合規）與三條佇列內容斷言，在第 6 段（`_v2`，`109_…sql:394-445`）**逐條都有等價斷言**，且多一條 `dropped <> 2` 斷言——`_v2` 段是原第 4 段的嚴格超集，移除不造成任何覆蓋回退。`run.sh` 實跑該段印 `ok：purge_storage_queue_enqueue_orphans_v2 正確回報 enqueued=1／dropped=2，且實際排入佇列的內容與 v1 行為一致`。
7. **mutation 重放（派工單 4）——三支我全部自己重放，全部屬實**（皆為真實斷言失敗，非 crash／非 build fail）：
   - 白名單那支：對 reset 後容器跑 `regprocedure` 解析 → `ERROR:  function public.purge_storage_queue_enqueue_orphans(text, uuid, text[]) does not exist`，與 handoff 原文一致。
   - api-contract 那支：把 v1 行加回 `docs/API.md` 的**scratchpad 副本**（未動 repo 檔）再跑 gate → `✗ … 文件有、schema 沒有（幽靈 RPC）：purge_storage_queue_enqueue_orphans(text, uuid, text[])`，EXIT=1，與 handoff 原文逐字一致。
   - 109 第 4 段那支：`git show origin/development:supabase/tests/109_…sql` 取出被刪的原段落餵 psql → `ERROR:  function public.purge_storage_queue_enqueue_orphans(unknown, uuid, text[]) does not exist` ＋ `HINT:  No function matches the given name and argument types.`，與 handoff 原文一致。
8. **DESTRUCTIVE 流程（派工單 5）**——`bash scripts/gates/migration-breaking-check.sh --base origin/development` 印 `DESTRUCTIVE`＋`BREAKING` 各一行（**預期**）；`migration-immutable-check.sh` 綠（既有 migration 未被改寫）；`migration-version-check.sh` 綠（tree 42 檔無撞號）。**CI 現況**：`db` SUCCESS／`lint` SUCCESS／`ci` SUCCESS／`ci-ipad` SUCCESS，`rules` FAILURE——`rules` 紅是等 `DESTRUCTIVE-APPROVED` 的預期狀態，**依派工單不列為 finding**；PR 尚無任何 comment，核可標記需**使用者本人**留獨佔行，我與 orchestrator 均不代貼。
9. **handoff 查實（派工單 6）**——「已完成」三項逐一對上三支 commit（`4d5fe3c` migration／`c6660b3` 測試＋白名單／`7b7c034` API.md），與 diff 內容相符。handoff 申報「無 informational 待處置、未記入 LS-96」——本輪為初審，屬實，LS-96 無需查對。證據檔 `.claude/evidence/LS-227/r1/` 四支全部存在（`run.sh.log` 124KB／`v1-grants-before-drop.log`／`mutation-60-whitelist.log`／`mutation-109-v1-test.log`），`.gitignore:21` 下未進 repo，符合慣例。

## Minor（1 條）

**MIN-1｜PR #355 body 回滾段：`comment on function` 非「逐字」，與宣稱不符（可執行性不受影響）**
- 位置：PR #355 body 回滾段 SQL vs `supabase/migrations/20260906050606_soft_delete_unreferenced_media.sql:224-230`
- 問題：body 寫「逐字取自 `…:192-230`」，但我把兩段抽出 `diff` 後，函式本體、`returns integer`、`security definer`、`set search_path`、`revoke execute … from public, anon` **全部逐字相符**，唯獨 `comment on function` 的字串被截短——原文結尾三句（`on conflict do nothing 天生冪等。`、`R2（merge-review R1 F4）：路徑須落在 p_family_id 前綴下且通過 private.is_media_object_path()，不合的路徑直接丟掉不寫入。`、`回傳值是這次呼叫真正新增的列數。`）在 body 版本中不存在，結尾改成 `…從未有對應 media 列）。`。
- 具體失敗情境：**不會**造成回滾失敗——SQL 仍可執行，函式行為、簽名、授權（含 default privileges 帶來的 service_role EXECUTE）完全還原。實際損失是：若未來真的執行回滾，還原出來的函式註解會**遺失 F4 路徑驗證的設計理由**，而那正是這支函式最容易被誤改的行為點；同時 body 的「逐字」字樣會讓後續執行者誤以為不必再回頭比對原始 migration。
- 建議修法：把 `comment on function` 換成 `:224-230` 的完整原文，或把「逐字取自」改成「函式本體逐字取自…，`comment` 為節錄」。屬 PR body 文字修正，不影響本次 APPROVE。

## Informational（3 條）

**INFO-1｜正式站 `pg_stat_user_functions.calls` 在本專案是空洞證據**：正式站 `track_functions=none`（我實查），所有函式 `calls` 恆為 0。本票結論不受影響（已改用 `pg_proc.prosrc` 掃描 0 列＋EF 部署版本時序兩條硬證據），但**日後 DESTRUCTIVE 票不應再把 `calls=0` 當成「零呼叫」證據**——建議記入 LS-96 待辦池，作為 DESTRUCTIVE 派工單的標準提醒句。

**INFO-2｜票文驗收③的 `get_advisors 無新 WARN` 未見於 handoff 的「已驗證」清單**：handoff 八項涵蓋 grants／run.sh／breaking-check／api_contract／白名單／mutation／push gate／PR body，唯獨缺這項。我這側 `mcp__supabase__get_advisors` 工具在本 session 不可用，無法代跑。風險評估**極低**：本票只 DROP 一支已收斂 `search_path`、不對 authenticated 開放的 SECURITY DEFINER 函式，只會縮小 advisors 掃描面、不可能新增 WARN；且該項本質是**部署後**才驗得到（與票文驗收①「正式站 `pg_proc` 無 v1、排程隔日 log 正常」同性質）。建議併入部署後檢查清單一起做，不阻擋本次合併。

**INFO-3｜三處沿革敘述仍指向已刪除的「第 4 段」**：`supabase/tests/109_soft_delete_unreferenced_media.sql:25`、`:34`（「新增第 3／4 段」「第 3／4 段的既有測試繼續驗證」）與 `docs/API.md:2395`（「新增第 3／4 段對這兩支反查／入列 RPC 的 DB 測試」）。三處都是 LS-213／LS-222 當時的歷史紀錄，且緊接其後就是本票新增的 LS-227 段落明說原第 4 段已刪，讀者能拼出完整沿革；也符合本 repo「段號不重排、沿革層層疊加」的既有慣例（本票檔頭自己就這樣寫）。**不要求修**，僅提示日後若有人照 `:34` 的字面去找第 4 段會撲空。

## 未完成

- `DESTRUCTIVE-APPROVED` 尚未出現在 PR #355（PR 目前零 comment）——需**使用者本人**留獨佔行，`rules` 在那之前維持 FAILURE。我未貼、也未要求 orchestrator 代貼。
- 票文驗收①（正式站 `pg_proc` 無 v1、`purge-storage` 排程隔日 log 正常）與 INFO-2 的 `get_advisors`：皆為部署後項目，本輪不涵蓋。
- commit status `merge-review`：緊接本 comment 對審的 head `7b7c034` 貼 `success`。

---

## handoff-evidence-check 收據

**（a）以 `main` 現行版本（含 LS-228 R4／R5 的 `.ts`／`.sql` 白名單）跑，`--repo` 指向本票 worktree —— EXIT=0 全綠：**

```
✓ 第 18 行起的列項有證據
✓ 第 19 行起的列項有證據
✓ 第 20 行起的列項有證據
✓ 第 21 行起的列項有證據
✓ 第 22 行起的列項有證據
✓ 第 27 行起的列項有證據
✓ 第 28 行起的列項有證據
✓ 第 32 行起的列項有證據
✓ 第 33 行起的列項有證據
```

**（b）以本票 worktree 內的版本跑 —— EXIT=1，第 18 行紅：**

```
✗ handoff-evidence-check：第 18 行起的列項缺『怎麼驗』證據（須含測試名、.png/.log/.test.sh/scratchpad//evidence//.swift/.py/.sh/.md/.yml/.json 路徑，或 xcodebuild／bash scripts/／gh run view／.xcresult 命令）
✓ 第 19 行起的列項有證據
（其餘 8 列皆 ✓）
```

（b）的紅是**工具版本落差、非敘述問題，不修改敘述**：本票 worktree branch 自 development `c5f07b9`，其 `scripts/gates/handoff_evidence_check.py` 仍是 **LS-228 之前**的版本（`grep -c supabase/functions` 得 0，main 版得 7），還沒有 (b2) 白名單目錄規則。第 18 行（查實 1）引用的 `supabase/functions/purge-storage/index.ts` 正是 `.ts` 路徑——這**就是 LS-228 要修的那個缺口本身**（該腳本檔頭 R4 段明文記載來源 LS-96 池項 `acb4e2df`：2026-09-06 LS-222／LS-223 兩張 merge-review 因引用 `.ts`／`.sql` 路徑被誤判，9 列誤報 3 列）。同一份文字在 main 版本下第 18 行判 ✓，可佐證是誤判。LS-228 併入 development 後此落差自動消失。

### 2026-09-12T02:37:37.478Z · Chih-Lin Yeh · `4321dbd5`

**orchestrator 09-12 10:37**：merge-review R1 `7685fd50` **APPROVE**（0 major／1 minor／3 info；status success 貼 `7b7c034`）。reviewer 以兩條實證取代無效的 `pg_stat_user_functions`（正式站 `track_functions=none`）：正式站 `pg_proc.prosrc` 零呼叫者＋`purge-storage` EF v5（09-06 14:45Z）晚於 LS-222／223 commit＝現役 EF 已用 `_v2`。三支 mutation 重放屬實；派工單「28→27」假設錯，handoff 訂正成立。
處置：MIN-1（PR body 回滾段 `comment on function` 字串被截短）→ **不修＋理由**：回滾 SQL 本體逐字相符、可執行且行為完整，註解全文可自 `20260906050606:192-230` 取回；不為此再 push（會重觸 CI 與 body 快照）。INFO-2（`get_advisors` 無新 WARN）→ 併入正式站部署後檢查清單，與 LS-224 push 後的 advisors 對照一起做。
**現況**：`db`／`lint`／`ci`／`ci-ipad`／`merge-review` 綠，只剩 `rules`＝等使用者本人在 PR #355 留 `DESTRUCTIVE-APPROVED` 獨佔行（決策單第 2 題）。留言後 CI rerun → 併 development → promote → main → 正式站 db push → `pg_proc` 無 v1＋隔日 purge-storage log 正常 → Done。

### 2026-09-12T15:11:42.339Z · Chih-Lin Yeh · `13968952`

**orchestrator 09-12 23:11**：使用者 23:05 於 PR #355 對話串留 `DESTRUCTIVE-APPROVED`（決策單 2a）→ `gh run rerun 34667224774 --failed` 重驗 `rules`（LS-123 approval-comment-check）→ 綠後鏈自動併入 development 並貼 dev tip status；之後隨下一批晉升 test／main，再 `supabase db push --linked --yes` 移除正式站舊版 `purge_storage_queue_enqueue_orphans(text, uuid, text[])`。

### 2026-09-12T15:20:25.769Z · Chih-Lin Yeh · `1e71564b`

**orchestrator 09-12 23:20**：`rules` 重驗綠（approval-comment-check 認到使用者 23:05 留言）→ #355 併入 development `10b4626`（dev tip 貼 `promote: no content diff`）→ QA。dev 現含本票 migration＋LS-208 設計稿；背景鏈等 dev push run 綠後 `promote.sh development test` → 派 qa（本機容器 delta：`pg_proc` 無 v1、`_v2` 排程自測、advisors 無新 WARN）→ test→main → `supabase db push --linked --yes`（正式站移除 v1）→ 隔日 `purge-storage` log 核對 → Done。

### 2026-09-12T16:34:07.782Z · Chih-Lin Yeh · `1b64b24e`

**orchestrator 09-13 00:33**：dev→test 晉升 ✓（test `b94b215`）→ 派 qa delta（本機容器：`pg_proc` 無 v1、`_v2`＋109 自測綠、白名單 27、API.md 條目）；與 LS-218 同批 qa status。PASS → test→main → `supabase db push --linked --yes` 移除正式站 v1 → 隔日 `purge-storage` log 核對 → Done。

### 2026-09-12T18:02:05.694Z · Chih-Lin Yeh · `2241bf58`

**qa 09-13 01:59 PASS**（test tip `b94b215e5c9f197a8b17d88891efbbf1bc4d5cf0`，delta；QA 只驗本機，正式站部署為後續步驟）

## 已驗證（怎麼驗）

1. 原文見 `scratchpad/LS-227-qa-evidence.log` §1。**PR #355 已 DESTRUCTIVE-APPROVED 且已併入**：`gh pr view 355 --json state,mergedAt` 回 `MERGED`；`gh api repos/CLYEH/little-sprout/issues/355/comments` 確認使用者本人（`CLYEH`）留言獨佔行 `DESTRUCTIVE-APPROVED`。

2. **本機容器套用 migration**（在 `supabase-lock.sh --hold "LS-227 QA db reset+tests"` 內，未自行下重置指令）：容器套用前 `select version from supabase_migrations.schema_migrations order by version desc limit 5` 只到 `20260906124837`（缺本票 migration）→ `bash scripts/ops/supabase-lock.sh -- supabase migration up` 套用 `20260912015310_drop_purge_storage_queue_enqueue_orphans_v1.sql`（非重置，逐步套用新 migration）→ 套用後同查詢確認 `20260912015310` 為最新。

3. **`pg_proc` 無 v1、`_v2` 存在**（原文見 `scratchpad/LS-227-qa-evidence.log` §3）：查詢只回一列 `purge_storage_queue_enqueue_orphans_v2(text,uuid,text[])`，v1 簽名查無此列。

4. **109 自測綠**：`bash scripts/ops/supabase-lock.sh -- bash supabase/tests/run.sh` exit 0，`✓ 全部 DB 測試通過`；輸出含 `ok：purge_storage_queue_enqueue_orphans_v2 正確回報 enqueued=1／dropped=2，且實際排入佇列的內容與 v1 行為一致`。`supabase/tests/109_soft_delete_unreferenced_media.sql` 內 `grep -n "purge_storage_queue_enqueue_orphans\b"` 只剩沿革註解（`:25`／`:34`／`:40`），無實際呼叫。

5. **`60_default_privileges.sql` 白名單**：`run.sh` 輸出「白名單內的 7 支 public service_role-only RPC 授權正確」（8→7，v1 移除）、「白名單內的 28 支 public SECURITY DEFINER RPC」與「4 支 SECURITY INVOKER」皆維持不變（v1 原本就不在 authenticated 白名單，票文原寫「28→27」的假設已由 R1 merge-review `7685fd50` 訂正，本輪覆核與訂正結果一致）；`grep -n "purge_storage_queue_enqueue_orphans" supabase/tests/60_default_privileges.sql` 只剩沿革註解＋`_v2` 一行。

6. **`docs/API.md` §3／§9 條目已刪並註記日期**：`grep -n "purge_storage_queue_enqueue_orphans" docs/API.md` 確認 §3「media」小節（`:411`）與 §9（`:2533`）皆已改為「移除紀錄（LS-227，2026-09-12）」散文，`<!-- API-CONTRACT:RPC -->` 機械清單只剩 `purge_storage_queue_enqueue_orphans_v2(text, uuid, text[])` 一行；`python3 scripts/gates/api_contract_check.py --text docs/API.md supabase/migrations` → `✓ api-contract gate（text 模式／best-effort）通過：RPC 39 支、表 25 張，docs/API.md 與 schema 一致`。

7. 原文見 `scratchpad/LS-227-qa-evidence.log` §7。**`get_advisors` 無新 WARN**：`mcp__supabase__get_advisors --type security` 對正式站（v1 尚未部署移除、仍存在）查詢，回傳的 `authenticated_security_definer_function_executable` 清單裡沒有 `purge_storage_queue_enqueue_orphans`（v1 是 service_role-only、非 authenticated 可執行，本就不會出現在這條 lint），其餘 WARN 皆為既有既知項（`function_search_path_mutable` ×2、`auth_leaked_password_protection`）；票文驗收①「正式站 `pg_proc` 無 v1、`purge-storage` 排程隔日 log 正常」為部署後項目，本輪不涵蓋（票文範圍已明寫「QA delta」只驗本機）。

## 未涵蓋（明講）

- 正式站部署與部署後驗證（`pg_proc` 無 v1、`purge-storage` 排程隔日 log）：票文驗收①明列為部署後項目，由 orchestrator 後續執行。
- `get_advisors` 本輪查的是正式站現況（v1 尚未移除），不是移除後的差異比對——移除後若要嚴謹比對需部署後再跑一次，風險評估同 merge-review R1 INFO-2（極低，本票只會縮小掃描面）。

## 產出位置

- lock 持有：`LS-227 QA db reset+tests`，4 分 53 秒，`--release` 正常（非到期）
- 本機容器：`supabase migration up` 套用 `supabase/migrations/20260912015310_drop_purge_storage_queue_enqueue_orphans_v1.sql`；`bash supabase/tests/run.sh` 全綠

### 2026-09-12T18:03:45.240Z · Chih-Lin Yeh · `40c301be`

**orchestrator 09-13 02:03**：QA PASS `2241bf58`（本機容器 `pg_proc` 無 v1、`_v2` 在、run.sh 全綠、白名單 7/28/4、API.md 條目已刪）。`promote.sh test main` 被擋（main 含 LS-233 hotfix、test 不含）→ 先 promote development→test → QA delta → test→main（v0.23.0）→ 依常設授權 `supabase db push --linked --yes` 移除正式站 v1 → 09-14 03:15（19:15Z）排程後以 `prod-purge-health.sh` 核對 → Done。收尾儀式：①sweeper 已派（與 LS-218 同批）②lesson review 待 ③cleanup 已清。

### 2026-09-12T18:12:21.261Z · Chih-Lin Yeh · `924f9f55`

**dead-code-sweeper 09-13 02:10**

## 範圍與基準

`git diff 922884e..b94b215`（`922884e`＝v0.22.1；`b94b215`＝origin/test tip，含 PR #355 merge commit）。同一批次注意事項：截至本次巡檢，`origin/main` 尚在 `cfa995f`（LS-233 hotfix），**尚未 fast-forward 到 `b94b215`**，`v0.23.0` tag 本地不存在——與 LS-218 巡檢回報同一提醒，一併知會 orchestrator。

## Findings（本 feature 引入）

**未發現本 feature 引入的死碼。**

## 逐項核對票文列的疑點

- **v1 `purge_storage_queue_enqueue_orphans(text, uuid, text[])` 所有引用是否清零（含 docs、tests、EF）**：`git grep -n "purge_storage_queue_enqueue_orphans\b" b94b215 -- .`（排除 `_v2`）全倉命中 22 處，逐一核對後**全部是三類允許保留的東西**，沒有任何一處是活的呼叫端：
  1. **不可變的既有 migration 檔**（`supabase/migrations/20260906050606_soft_delete_unreferenced_media.sql:50/161/192/217/224`、`20260906091007_purge_orphan_dropped_reporting.sql:7/20/26/88/136`）——按專案「migration 不可竄改」規約，這是 v1 原本的建立紀錄，本來就不會、也不該被改寫或刪除；真正移除動作在新的 `20260912015310_drop_purge_storage_queue_enqueue_orphans_v1.sql` 裡。
  2. **`docs/API.md`**（`:404/411/2369/2533`）——`:411`／`:2533` 已明確改寫成「已由 LS-227 DESTRUCTIVE 移除紀錄（2026-09-12）」的散文，`<!-- API-CONTRACT:RPC -->` 機械清單（`api_contract_check.py` 檢查對象）裡已無 v1、只剩 `_v2` 一行；`:404`／`:2369` 是解釋「為什麼要另立新函式名」的技術背景文字，非對照清單條目。
  3. **`supabase/functions/purge-storage/index.ts:127/257`**——純註解，解釋現行 `_v2` 設計相對舊版 v1 的改進理由（"舊版...只回傳 enqueued，無法回報被丟棄"），程式碼本體的三個 `supabase.rpc(...)` 呼叫點（classify／`_v2`／mark_failed）裡沒有 v1。
  4. `supabase/tests/109_soft_delete_unreferenced_media.sql:25/34/40/389`、`60_default_privileges.sql:442`——沿革註解（含刻意保留、按專案慣例「段號不重排、沿革層層疊加」不修的既有寫法，merge-review R1 INFO-3 已核可此寫法），非測試斷言本體；`grep -n "purge_storage_queue_enqueue_orphans\b" supabase/tests/109_....sql` 排除註解後零實際呼叫（`run.sh` 已驗證第 4 段已整段刪除）。
- **`_v2` 是否還有為相容 v1 留的分支**：讀完 `purge_storage_queue_enqueue_orphans_v2`（`supabase/migrations/20260906091007_purge_orphan_dropped_reporting.sql:97-135`）與 `purge_storage_classify_orphan_paths`（同檔 `:6-32`）全文——`_v2` 是獨立乾淨的實作（前綴＋形狀防禦性重驗、`on conflict do nothing`、回傳 `table(enqueued, dropped)`），沒有任何 `if`／分支邏輯是為了「相容 v1 呼叫端」而存在；唯一與 v1 的關係是註解裡說明「SQL 本體與原本 v1 完全相同，只是回傳型別不同」，這是**沿革說明**不是**執行期相容分支**。

## 既有死碼觀察

無（本票範圍乾淨，且本票本身就是移除既有死碼的 DESTRUCTIVE 票，沒有再引入新的殘留）。

## 建議處置

不需要任何處置——本票對死碼巡檢的三個檢查面（v1 引用清零、`_v2` 無相容分支殘留、docs/tests 同步）皆已在 merge-reviewer 兩輪（R1 `7685fd50` APPROVE）與 QA（`2241bf58` PASS）中做過比本輪更深入的正式站層級查證（`pg_proc.prosrc` 掃描、EF 部署版本時序、mutation 重放），本輪唯讀複核與其結論一致，無新增項目。

## 搜尋方法與盲區聲明

- 方法：`git diff --stat 922884e b94b215` 鎖定 4 個觸碰檔案（新 migration、`109_...sql`、`60_default_privileges.sql`、`docs/API.md`）→ `git grep -n "purge_storage_queue_enqueue_orphans\b"` 全倉排除 `_v2` 逐一分類核對每個命中點的性質（不可變 migration／文件散文／註解／沿革），確認無任何一處是可執行的呼叫端；另讀 `_v2` 與 `classify_orphan_paths` 全文確認無相容分支。
- 盲區：純文字搜尋抓不到透過 `EXECUTE`／動態 SQL 字串拼接、或 PostgREST client 端用字串常數組出的 RPC 名稱（本票 TS 端三個呼叫點皆為字面 `.rpc("...")` 呼叫，風險低）；未連線正式站或本機容器覆核（唯讀讀碼比對，不涉及 `supabase-lock.sh`），正式站 `pg_proc` 現況與部署後驗證已由 QA／merge-review 另行覆蓋，非本輪範圍。

### 2026-09-12T18:13:19.821Z · Chih-Lin Yeh · `03bb1013`

**orchestrator 09-13 02:12 收尾儀式（部分）**

- ① 死碼巡檢 ✓（sweeper `924f9f55`）：v1 函式 22 處殘留引用皆為歷史 migration／API.md 移除紀錄／歷史註解，零活呼叫點；`_v2` 無 v1 相容分支。無待辦。
- ② Lesson learning review：
  - harness：破壞性核可改 owner 獨佔行 comment（LS-123）流程首次全程走完——決策單 → 使用者 `DESTRUCTIVE-APPROVED` → gate 放行；無摩擦。
  - 設定：無。
  - 工具缺口：無。
- ③ 清理 ✓：`cleanup-merged.sh --apply LS-227` → 0／0／0（早已清）。
- ④ Pen 清場：不適用。

**Done 前置未完**：待 v0.23.0 併 main → 正式站 `supabase db push --linked --yes`（drop v1）→ 09-14 03:15 排程跑完後 `prod-purge-health.sh` 核對 → Done。

### 2026-09-12T18:41:32.557Z · Chih-Lin Yeh · `1961ceb1`

**orchestrator 09-13 02:40 正式站 push ✓**

- **v0.23.0 = 144622d** 併 main 後 `supabase db push --linked --yes` → 套用 `20260912015310_drop_purge_storage_queue_enqueue_orphans_v1.sql`。
- 唯讀核對 `pg_proc`（public，`purge_storage%`）：只剩 `purge_storage_queue_enqueue_orphans_v2`＋`classify_orphan_paths`／`queue_mark_failed`／`unknown_media_paths`，**v1 已不存在**。
- 排程今日 03:15 會以 v2 路徑跑（purge-storage EF v6 同時上線），03:20 後 `prod-purge-health.sh` 核對 → 綠即 Done（提前於原訂 09-14）。

### 2026-09-12T19:20:42.762Z · Chih-Lin Yeh · `3419b2c1`

**orchestrator 09-13 03:20 正式站驗收 ✓ → Done**

- `pg_proc` 無 v1（02:40 唯讀核對）。
- v1 drop 後首次排程（今日 03:15）`prod-purge-health.sh`：排程 7/7、purge_runs 7/7、最新 HTTP 200（2026-09-12T19:15 UTC）——purge-storage 走 `_v2` 路徑正常。
- 收尾儀式①②③見 02:12 comment。

