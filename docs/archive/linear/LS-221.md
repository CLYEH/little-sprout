# LS-221 Task：LS-18 iOS——`FamilyStoreJoinRequestsTests.test_requestJoin_whileSubmitting_ignoresDuplicateCall` flake（輪詢到 `.submitting` 時 stub handler 尚未被排程，`("0") != ("1")`）——改用 `AsyncGate.waitForWaiters` 同步（沿 LS-214 修法）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:S, lane:ui |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M1 登入與家庭 |
| 估點 | — |
| 建立 | 2026-09-06T08:17:13.131Z |
| 開始 | 2026-09-06T10:55:47.639Z |
| 完成 | 2026-09-06T14:57:59.349Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-221/taskls-18-ios-familystorejoinrequeststeststest-requestjoin |
| 父票 | LS-18 Story：建立家庭與邀請加入 |
| 關係 | related→LS-205, related→LS-220, related→LS-211, related→LS-96, related→LS-209, related→LS-214, related→LS-213, related←LS-243, related←LS-229, related←LS-193 |

## 描述

## 來源

[LS-213](https://linear.app/little-sprout-app/issue/LS-213/taskls-20-後端-孤兒媒體清理排程補完a-storage-有物件但無-media-列purge-expired-不掃ls-96) merge-review R2 `0e4c0eed`（2026-09-06）：PR #339 `ci` job 紅，diff 零 Swift 檔、同 job 在 `c0cc2d9` 與 development 皆綠——`FamilyStoreJoinRequestsTests.test_requestJoin_whileSubmitting_ignoresDuplicateCall` 斷言 `("0") != ("1")`。reviewer 判根因：測試輪詢到 `.submitting` 後，stub handler 尚未被排程（`LittleSprout/Services/Family/FamilyStore+JoinRequests.swift:14-16`），第二次呼叫落在去重窗口之外。與 [LS-214](https://linear.app/little-sprout-app/issue/LS-214/fixtimelinestoreteststest-refresh-secondcallwithdifferentchildid)（`TimelineStoreTests` 同型 flake，改 `AsyncGate.waitForWaiters(count:1)`）同一類。

## 範圍

1. 測試改用 `LittleSproutTests/Support/AsyncGate.swift` 的 `waitForWaiters(count:)` 等 stub handler 真的進入等待，再發第二次呼叫；不放寬斷言、不加 sleep。
2. 檢視同檔其他「輪詢 `.submitting`」寫法，一併改（列清單）。
3. 本機該測試 `-only-testing` 連跑 20 次綠（原文附 handoff）；mutation：把 `waitForWaiters` 拿掉還原舊寫法 → 多次跑至少一次紅（或以 `Task.yield` 人為放大競態證明）。

## 不做

`FamilyStore` 生產程式碼變更（去重邏輯本身正確）。

## 驗收

- [ ] 20 次連跑綠；mutation 紅；merge-reviewer 通過（scope：只動測試）
- [ ] 併入後 development `ci` 不再因此測試紅

## 留言（5）

### 2026-09-06T11:18:56.238Z · Chih-Lin Yeh · `74c94400`

## merge-review R1 — APPROVE（2026-09-06 19:16 CST）

PR #346 `fix/LS-221-join-request-test-flake` → `development`，審的 head `32eb928b8ea6cc336f4a5ada32c2de435467a14f`（`gh pr view 346 --json headRefOid` 當下同值，審查期間無 push）。

`git diff origin/development...32eb928b`：1 檔、+18/-13，單一 commit `32eb928`，全部在 `LittleSproutTests/FamilyStoreJoinRequestsTests.swift`。無生產程式碼變更。

### 1. Race condition（本票重點）— 通過

新同步點確實消除競態，逐步推導：

- `FamilyStore.requestJoin`（`LittleSprout/Services/Family/FamilyStore+JoinRequests.swift:13-17`）在 `@MainActor` 上**同步**賦 `requestJoinState = .submitting`（:15），之後才 `await apiClient.requestJoin`（:17）。
- `StubFamilyAPIClient`（`LittleSproutTests/Support/StubFamilyAPIClient.swift:8`）是 `final class ... @unchecked Sendable`，`requestJoin(code:)`（:248-252）為 nonisolated async——從 MainActor 呼叫必定跳離 MainActor，`box.withLock { $0.requestJoinCalls.append(code) }`（:249）與 handler 內 `callCount.withLock { $0 += 1 }` 都落在併發 executor。舊寫法輪詢到 `.submitting` 只證明 :15 已執行，與 handler 之間**沒有 happens-before**——票文與 handoff 的根因診斷成立。
- 新寫法 `await gate.waitForWaiters(count: 1)` 只在 `AsyncGate.continuations.count >= 1`（`LittleSproutTests/Support/AsyncGate.swift:42-46`）時返回，而 continuation 由 handler 的 `await gate.wait()`（同檔 :24-27）加入；`callCount` 遞增與 stub 的 `requestJoinCalls.append` 在同一次函式呼叫中位於 `wait()` **之前**（program order），因此返回後兩者必定已完成。`XCTAssertEqual(callCount, 1)` 由此變成決定性斷言。
- 第二次呼叫必定看到 `.submitting`：handler 卡在 `wait()`，`requestJoinState` 不可能推進到 `.success`／`.failure`（`open()` 在斷言之後），故 `XCTAssertNil(secondOutcome)` 同樣決定性。
- 收尾無懸掛 continuation：`await gate.open()`（`AsyncGate.swift:32-36`，一次 resume 全部等待者後清空陣列）→ `_ = await firstCallTask.value`。斷言失敗不會提前 return（`XCTAssert*` 不中斷流程），失敗路徑一樣走到 `open()`；mutation 那次跑出 `failed (0.054 seconds)` 而非掛住，可側面佐證。
- 無死鎖：`AsyncGate` 是獨立 actor，`waitForWaiters` 的 `Task.yield()` 自旋不佔用 MainActor，`firstCallTask` 得以推進（若佔用，20 次迭代與 875 支全套不可能全綠）。

### 2. 運算效能 — 無問題

移除最多 200×5ms 的 `Task.sleep` 輪詢，改成 yield 自旋，單次迭代 0.001–0.022 秒。不涉 RLS／查詢／主執行緒解碼面向。

### 3. 平行優化 — 不適用

測試刻意序列化以驗證去重語意，寫法正確。

### 4. Scope — 通過

只動測試檔，`FamilyStore+JoinRequests.swift` 未變（票文「不做」）；無順手重構、無無關改動。commit 第一行 `test(family): LS-221 修 requestJoin 去重測試的同步點（去 flake）` 合規。

### Handoff「已驗證」逐項對應（LS-211）

1. 驗收「20 次連跑綠」→ 怎麼驗：讀 `LS-221-postrestore-20run.log` 原文，:286-325 為 `Iteration 1 of 20` 至 `Iteration 20 of 20` 全部 `passed`，:326-327 `Test Suite 'FamilyStoreJoinRequestsTests' passed at 2026-09-06 19:05:44.530 / Executed 20 tests, with 0 failures (0 unexpected) in 0.047 (0.060) seconds`，:339 `** TEST SUCCEEDED **`。與 handoff 引文一致。
2. 驗收「mutation 紅」→ 怎麼驗：讀 `LS-221-mutation-run2.log:288` 原文 `.../LittleSproutTests/FamilyStoreJoinRequestsTests.swift:121: error: -[LittleSproutTests.FamilyStoreJoinRequestsTests test_requestJoin_whileSubmitting_ignoresDuplicateCall] : XCTAssertEqual failed: ("0") is not equal to ("1") - 底層 API 只該被呼叫一次`，:289 `failed (0.054 seconds)`、:306 `** TEST FAILED **`；`grep -cE "Restarting after unexpected exit|crash|Fatal error|BUILD FAILED"` = 0 → 是真的斷言轉紅，不是 app crash 或 build fail 被誤當成紅。斷言訊息與 LS-213 report 的 `("0") != ("1")` 對得上。
3. 全套 → 怎麼驗：`LS-221-fullsuite.log:2212` `Executed 875 tests, with 0 failures (0 unexpected) in 5.119 (5.848) seconds`、:2222 `** TEST SUCCEEDED **`，且目標測試在其中 passed。
4. SwiftLint → 怎麼驗：我自己重跑 `swiftlint lint --strict --quiet LittleSproutTests/FamilyStoreJoinRequestsTests.swift`，exit=0 無輸出（實作者只跑非 `--strict`，我補跑 `--strict` 亦乾淨）。
5. push gate／CI → 怎麼驗：`gh pr checks 346` → `ci pass 4m16s`、`ci-ipad pass`、`lint pass`、`db pass`，`rules pending`（等本裁決的 commit status）；`gh run view 34029411140` 的 `headSha` = `32eb928b8ea6cc336f4a5ada32c2de435467a14f`，與我審的 head 相同。
6. 備份檔核對 → 怎麼驗：`LS-221-FamilyStoreJoinRequestsTests.swift.orig` 與 worktree 現行檔逐字相同（`diff` 無輸出），確認 mutation 後已完整還原成 head 版本。

### 未替實作者重放的部分（誠實揭露）

- **mutation 未自行重放**（LS-209 一般要求 reviewer 重放）：本次派工硬規則為「只讀、不 boot 模擬器」，重放 mutation 必須跑 `xcodebuild test` 而必然 boot 模擬器，故改採 log 取證＋時序核對：19:01 備份 → 19:02 純還原舊寫法 20 次仍全綠（`LS-221-mutation-run.log:328` `Executed 20 tests, with 0 failures`、:340 `** TEST SUCCEEDED **`）→ 19:04 加 50ms `Task.sleep` 放大後轉紅 → 19:05 還原後 20 次全綠。四份 log 的 mtime 與內文時間戳一致，無矛盾。
- 一處無法核對的細節（**不列為 finding**）：mutation 報的失敗行號是 `:121`，我用「head 版斷言在 :108 ＋ 還原輪詢迴圈(+8) ＋ iterator 兩行(+1) ＋ sleep(+1)」推得應落在 :118 附近，差約 3 行；mutated 檔未保留無法確認（合理解釋是 mutation 當下另加了註解）。失敗訊息原文、測試名、檔案路徑三者皆對得上且非 crash，故不影響採信。
- CI job 內文未取得：`gh run view --job 101476072090 --log` 在 run 仍 `in_progress` 時無輸出，CI 層級僅採信 `gh pr checks` 的 `ci pass`（未逐測試核對 CI 內的該支測試結果）。
- 未跑本機 DB 測試（本票純單元測試、不碰 Supabase 容器），未持有 supabase lock；**未持有**（時長：未持有）。未 boot 模擬器、未量測版面。

### 範圍項 2 的獨立複核（同檔另一支輪詢）

我自己讀了 `LittleSprout/Services/Family/FamilyStore+JoinRequests.swift:120-132` 的 `performJoinRequestAction`：`processingJoinRequestIDs.insert(requestID)`（:122）在 `await action()`（:125）**之前**、於 MainActor 完成。因此 `test_isProcessingJoinRequest_trueWhileActionInFlight_perRequestID`（`LittleSproutTests/FamilyStoreJoinRequestsTests.swift:245-277`）輪詢到 `isProcessingJoinRequest(A) == true` 之後的三個斷言（`isProcessingJoinRequest(B) == false`、`rejectJoinRequest(B)` 回 true、`await approveTask.value` 後 A 旗標清掉）全都只讀 MainActor 側狀態或 B 自己的呼叫結果，**沒有任何一個讀 approve stub handler 寫入的資料**——與 LS-213 那支的失敗模式不同型。實作者「無同型競態、維持原樣」的判斷成立。另確認全檔只剩這一處輪詢寫法（`grep -n "while \|Task.yield\|Task.sleep\|AsyncStream"` 僅命中 :251/:262/:268）。

### Informational（皆非 blocker／major）

- **I1（informational）** `LittleSproutTests/Support/AsyncGate.swift:42-46`（**非本 PR 引入**，LS-214 既有）：`waitForWaiters` 無逾時上限，等待者若永不出現會無限自旋，測試從「紅」退化成「掛到 CI job timeout」；被移除的輪詢原本有 200 次上限＋`XCTFail("等待 requestJoinState 進入 .submitting 逾時")` 的明確訊息。具體失敗情境：日後生產碼被改成 `requestJoin` 提前 return（不呼叫 `apiClient`），handler 永不進 `wait()`，本測試不會給出斷言訊息而是整條 job 卡住。建議（harness 層、不在本票）：在 `waitForWaiters` 內建迭代／時間上限並 `XCTFail`，全體使用者受惠；建議記入 LS-96 待辦池。
- **I2（informational，既存行為）** `LittleSproutTests/FamilyStoreJoinRequestsTests.swift:89-112`：若生產碼去重 `guard`（`FamilyStore+JoinRequests.swift:14`）被拿掉，第二次呼叫會進入卡住的 handler，測試同樣是「掛」而非「紅」——舊寫法亦然，本 PR 未使其變差，僅記錄。
- **I3（informational）** `LittleSproutTests/FamilyStoreJoinRequestsTests.swift:245-277`：該支可用同一支 `gate.waitForWaiters(count: 1)` 取代 5ms 粒度輪詢（依上述分析，handler 進入 `wait()` ⇒ insert 必已完成），屬純整併、無正確性缺口；本票不做是正確的 scope 判斷。

### Verdict：APPROVE

無 blocker、無 major。驗收條件對照：20 次連跑綠（已核 log 原文）／mutation 紅（放大版，票文明文允許「或以 `Task.yield` 人為放大競態證明」；純還原版本機 20 次未紅已如實揭露）／scope 只動測試（diff 核對）；「併入後 development `ci` 不再因此測試紅」屬併後觀察項，本 PR 於 head `32eb928` 的 `ci` 已 pass。

---

`bash scripts/gates/handoff-evidence-check.sh <本 comment 暫存檔>`（於 `.claude/worktrees/LS-221`）輸出，exit=0：

```
✓ 第 32 行起的列項有證據
✓ 第 33 行起的列項有證據
✓ 第 34 行起的列項有證據
✓ 第 35 行起的列項有證據
✓ 第 36 行起的列項有證據
✓ 第 37 行起的列項有證據
```

### 2026-09-06T11:21:01.845Z · Chih-Lin Yeh · `ecc2bf29`

**orchestrator 09-06 19:2x**：merge-review R1 `74c94400` **APPROVE**（同步決定性、另一支輪詢無同型競態獨立複核、mutation log 真斷言失敗；PR #346 六綠）。I1–I3 → 入池 LS-96（19:2x）。→ 併入 development（`LS-221-merge-follow.sh`）→ QA（CI 綠即證，隨下一次 dev→test 晉升後 delta 註記）。

### 2026-09-06T14:34:36.329Z · Chih-Lin Yeh · `74654350`

**qa 09-06 22:34 — PASS**（test tip `e3f566d`）

## 已驗證（怎麼驗）

1. **驗收①「20 次連跑綠」**：`xcodebuild test -only-testing:LittleSproutTests/FamilyStoreJoinRequestsTests -test-iterations 20 -run-tests-until-failure`（qa-test-iPhone17Pro，iOS 26.0）。原文：`grep -c "test_requestJoin_whileSubmitting_ignoresDuplicateCall.*started"` = 20、`grep -c "...passed"` = 20；整體 `Executed 280 tests, with 0 failures (0 unexpected) in 0.424 (0.607) seconds`、`** TEST SUCCEEDED **`。log：`scratchpad/LS-QA-e3f566d-ls221-20run.log`（session scratchpad）。
2. **驗收②「併入後 development `ci` 不再因此測試紅」**：本輪全套 `xcodebuild test`（unit＋UI，無 `-only-testing` 限制）跑一次，`FamilyStoreJoinRequestsTests` 全數包含在內且皆 passed（xcresult summary：`totalTestCount=986, passedTests=984, failedTests=1`，唯一失敗是 `QASmokeTests.testScenario()`——缺 `LS_QA_*` 環境變數，設計上單跑必紅，見 LS-220 節）。log：`scratchpad/LS-QA-e3f566d-fullsuite.log`。
3. **Scope**：本輪為 delta 複核，未重新審查程式碼（merge-reviewer R1 `74c94400` 已 APPROVE，逐點覆核 race condition 成立）；本次只重放驗收條件本身。

## 未完成

無。merge-review R1 informational I1（`AsyncGate.waitForWaiters` 無逾時上限）／I2／I3 已由 orchestrator 記入 LS-96 待辦池（19:2x comment），非本票範圍，不重複處置。

## 風險

無新增風險。本機模擬器 runtime iOS 26.0（釘住版 26.2 未安裝於本機），同 LS-205 既有落差，不影響本測試（純邏輯測試，不涉版面）。

## 產出位置

- `scratchpad/LS-QA-e3f566d-ls221-20run.log`（20 次連跑原文）
- `scratchpad/LS-QA-e3f566d-fullsuite.log`（全套回歸，含本測試）
- xcresult：`scratchpad/LS-QA-e3f566d-ls221-20run.xcresult`、`scratchpad/LS-QA-e3f566d-fullsuite.xcresult`（皆在 session scratchpad，未進版控）

**裁決：PASS**

### 2026-09-06T14:56:37.985Z · Chih-Lin Yeh · `f64b4739`

收尾儀式① dead-code-sweeper：本票（與 LS-193／LS-220 同批）的巡檢報告貼在 LS-193 comment `bac8e9f7-7b38-4a2e-ab0f-4b8802b1f989`——本票（LS-221）本身無死碼發現。

### 2026-09-06T14:57:44.034Z · Chih-Lin Yeh · `f3ccac81`

（09-06 22:56）收尾儀式完成 → Done（v0.21.0，main e3f566d；QA PASS 74654350——20 次迭代 280/280 綠）。
① dead-code-sweeper：同批報告 LS-193 comment bac8e9f7，本票無 finding（sweeper 獨立複核了 `FamilyStoreJoinRequestsTests.swift:245-278` 不改的決定，正確）。
② lesson learning review：與 LS-214 同型（poll-then-assert）第二例；「flake 修票 DoD 含全 repo 同型掃描」與 grep gate 已記 LS-96 P2（6d4a785e 第 3 筆）。本票由 LS-213 R2 reviewer 在 CI 隨機紅時開票、當日修完，流程無新缺口。
③ 清理：`bash scripts/ops/cleanup-merged.sh --apply LS-221` exit 0 → worktree 1／本機分支 0／遠端分支 0／專屬模擬器 1／DerivedData 1。④ Pen 清場：不適用。

