# LS-98 iPad 歡迎頁視覺還原——`MountPoolOpacity.iPad` 染料池參數接上 `WelcomeView.regularLayout`（01-iPad 稿面；LS-17 sweeper F1）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Low |
| 標籤 | lane:ui |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M1 登入與家庭 |
| 估點 | — |
| 建立 | 2026-08-25T11:08:38.339Z |
| 開始 | 2026-08-25T20:46:10.557Z |
| 完成 | 2026-08-26T01:30:47.058Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-98/ipad-歡迎頁視覺還原-mountpoolopacityipad-染料池參數接上-welcomeviewregularlayout01 |
| 父票 | LS-17 Story：Sign in with Apple＋Email OTP 登入 |
| 關係 | related→LS-106, related→LS-95, related→LS-70, related←LS-130, related←LS-74, related←LS-105, related←LS-101, related←LS-96 |

## 描述

## 來源

[LS-17](https://linear.app/little-sprout-app/issue/LS-17/storysign-in-with-appleemail-otp-%E7%99%BB%E5%85%A5) 收尾 dead-code sweep F1（2026-08-25）：`LittleSprout/DesignSystem/PrintPhotoCard.swift:25-27` 的 `MountPoolOpacity.iPad`（0.418／0.207／0.132／0.03，逐位對上 brand skill `motifs.md` 的 01-iPad 染料池光強度）零呼叫點；`WelcomeView.regularLayout` 只把 iPhone 版重排成雙欄，沒有套 iPad 專屬視覺參數（設計稿 01-iPad：字標框 247×118 vs compact 190×90.6、台紙壓邊兩板皆 8pt——merge-review R1 I2 依 `.pen` 實測修正；原「140pt vs 51pt、20pt vs 34pt」為開票時誤植）。

## Scope

* `WelcomeView.regularLayout`（`horizontalSizeClass == .regular`）改吃 `mountPoolOpacity: .iPad`＋01-iPad 稿面的字標／壓邊尺寸（以 `design/littlesprout.pen` 01-iPad 板為準；開工先 `get_app_state` 對 Pen 路徑）。
* 不動 compact 版；不新增畫面（設計稿已核可，免 design gate）。

## 驗收

* iPad 模擬器（regular）截圖與 01-iPad 板對照：字標、壓邊、染料池四角強度一致（**像素取樣可見**，非只比常數）；iPhone 版版面無變化、但染料池依 01 板變為可見（QA1 FAIL 發現：`PrintPhotoCard.swift:40-45` `.background(lsPrintPaper)` 疊在 `.background(mountPoolGlow)` 之前，opaque 台紙盖住染料池，`.welcome` 也從未渲染——pre-existing，本票一併修）（QA 附兩組截圖）。
* 單元測試不變 110/110；swiftlint strict 綠。
* feature 從 development；ui lane。

## 留言（7）

### 2026-08-25T23:28:38.639Z · Chih-Lin Yeh · `1a262759`

**merge-review R1 — APPROVE**（PR #167，head `e433bd4`）

0 blocker／0 major／1 minor／6 informational。CI 四項全綠（ci／db／lint／rules）。

稿面 8 項我自己用 `python3` 解 `design/littlesprout.pen`（frame `BDrtd` ＋ top-level `variables`）獨立核過，全一致：字標 247×118（節點 `A6DnYR`）、壓邊 `$print-edge`／`$print-edge-bottom` 皆 8、染料池四角 0.418／0.207／0.132／0.03 且四角對應無錯位。compact 板 `svedU`（字標 190×90.6、同一組壓邊 token）確認未受影響。

- **F1（minor, PLAUSIBLE）** `WelcomeView.swift:29/95/320-324`：`.regular` 不等於 iPad——Info.plist 開了 iPhone 橫向，Max 級 iPhone 橫向也是 regular，會一併吃到 iPad 染料池與 247×118 字標，與票文驗收「iPhone 版無變化」有出入，evidence 只有 iPhone 直向一張。純視覺、無版面破壞（右欄 320pt 放得下），故不擋。建議擇一：收斂驗收句成「iPhone 直向無變化」／改 idiom 併判／QA 補一張 Max 橫向截圖。
- **I1** 本次還原只到「染料池強度＋字標」，台紙幾何（稿面 Print 420 寬、照片 404×542、角托 40、池半徑 240 vs 程式碼 190 高／26／156）仍是 compact 尺寸——在 Scope 之外，但驗收句「與 01-iPad 板對照」若整版比對會誤判，建議先收斂。
- **I2** 票文前提數字與 .pen 不符（票文「字標 140 vs 51、壓邊 20 vs 34」；.pen 是 247×118 vs 190×90.6、壓邊兩板同 8）。實作依票文指定的「以 .pen 為準」是對的，票文數字建議修正，否則 QA 照 20／34 驗必不過。
- **I3–I6** `AppSpacing` 收 component 尺寸而 compact 190×91 仍內嵌／`.welcome` 預設值兩處/size class 映射無自動化釘子／`WelcomeView` struct body 有效行數 228（限 250，僅 ~22 行餘裕）。

全文：https://github.com/CLYEH/little-sprout/pull/167#issuecomment-5418354844

未跑：未 boot 模擬器、未跑 xcodebuild／swiftlint（依派工限制，以 CI 綠燈為證）；未做像素級疊圖；iPad 深色模式未實測（.pen 無該板）。

### 2026-08-26T00:10:55.947Z · Chih-Lin Yeh · `6d023094`

**QA1 — FAIL**（test tip `5e4a357d759d606ce31d0d77c4a30491f12a8a47`，PR #167）

## 逐項

**1. 建置＋測試** ✓
`xcodebuild test`（`main-iPhone17Pro`，`scripts/ops/simulator-lock.sh` 序列化）：157 tests, 0 failures，`** TEST SUCCEEDED **`。`swiftlint lint --strict --quiet` exit 0（0 violations）。

**2. iPad 稿面對照** ✗（染料池子項不過，其餘子項過）
用 `python3 -c` 直接解 `design/littlesprout.pen`（明文 JSON）frame `BDrtd` 與 `variables`，不經 Pencil MCP：
- 字標框 247×118（節點 `A6DnYR`）↔ `AppSpacing.wordmarkWidthIPad/HeightIPad` = 247/118 — ✓ 一致（`WelcomeView.swift:320-324`）。
- 台紙壓邊 `$print-edge`／`$print-edge-bottom` 皆 8 ↔ `AppSpacing.printEdge/printEdgeBottom` = 8/8 — ✓ 一致。
- 染料池四角 0.418／0.207／0.132／0.03 ↔ `PrintPhotoCard.MountPoolOpacity.iPad` 常數值 — 程式碼常數本身 ✓ 一致；**但實際渲染完全看不到**：對 `ipad-portrait-normal.png`／`iphone17pro-portrait-normal.png` 兩台裝置的台紙邊緣做密集像素取樣（跳過角托三角形後、卡片中心前的整段），色值與 `$print-paper`（251,235,236）逐 byte 相同、零變化（例：iPad 頂邊 x=200..1100 每 20px 取樣，20 個點全部 `(251,235,236)`；iPhone 底邊 imprint row x=80..1140 同樣全平）。四角強度排序（左上>右上>左下>右下）因此無法從畫面驗證——不是「差異不明顯」，是完全沒有可觀察到的漸層。

**根因**（`LittleSprout/DesignSystem/PrintPhotoCard.swift:40-45`）：
```swift
.padding(.top, AppSpacing.printEdge)
.padding(.horizontal, AppSpacing.printEdge)
.padding(.bottom, AppSpacing.printEdgeBottom)
.background(Color.lsPrintPaper)        // ← 先疊
.background(mountPoolGlow.clipped())   // ← 後疊，落在 lsPrintPaper 底下
.overlay(PhotoCornerOverlay(size: cornerSize))
```
`.background()` 鏈式呼叫時，後呼叫的落在更底層（離觀者更遠）。`Color.lsPrintPaper` alpha=1.0（全不透明）且鋪滿整個卡片（含 padding／spacing 空隙），所以疊在它下面的 `mountPoolGlow` 100% 被完全遮住，任何 opacity 值都畫不出來。這是 pre-existing 問題（`.welcome`／iPhone 也一樣中，不是本票新增的迴歸），本票的 diff 沒有動到這兩行——但驗收句明確要求「染料池四角強度一致」且要用截圖驗證，這一項目前無法通過。

**3. iPhone 直向無變化** ✓
用 `iphone17pro-portrait-normal.png`（`main-iPhone17Pro`，1206×2622，與 baseline 同解析度）對比 `.claude/evidence/LS-101/qa1/01-welcome-light-default.png`：全解析度像素 diff 0.52%（16451/3162132，門檻 |ΔRGB|>30），diff 區域只集中在狀態列時鐘文字（"7:42"→"7:52"，環境差異）與字標／Google logo 邊緣反鋸齒雜訊；相片、按鈕、間距、文案零差異。compact layout 未受影響。

**4. iPhone 17 Pro Max 橫向（R1 F1 順手記錄，不算 PASS/FAIL）**
`qa-LS98-iPhone17ProMax` 橫向：確認會切到 `regularLayout`（Max 機型橫向 horizontalSizeClass 為 `.regular`），套用 iPad 版兩欄版面與較大字標框（版面比例與 iPad 一致）。同樣受第 2 項的染料池不可見問題影響。截圖 `iphone17promax-landscape.png`。

**5. iPad 深色模式**
`ipad-portrait-dark.png`：無溢出、無破版。染料池同樣不可見（同第 2 項根因）。

**6. 回歸冒煙** ✓
Email OTP 全流程（`main-iPhone17Pro`）：輸入 email → `scripts/ops/demo-otp.sh`（本機 stack，未重啟、未重置）取碼 → 輸入驗證碼 → 確認登入 → 成功導向時間軸（`smoke-otp-login-timeline.png`）。時間軸載入正常（含另一次既有session直接載入時間軸的確認）。

## AX3
`ipad-portrait-ax3.png`／`ipad-landscape-ax3.png`（`content_size accessibility-extra-large`）：三顆鈕與文案皆完整換行、無裁切、無溢出。

## 裁決
**FAIL** — 第 2 項驗收句「染料池四角強度一致」無法從畫面驗證通過（渲染完全不可見，非本票新增迴歸，但阻擋本票逐項過關）。其餘 1／3／6 皆過，4／5 已如實記錄。不自行修 code，退回 orchestrator 裁定：是否 (a) 在本票加一行修正 `PrintPhotoCard.swift` 的 `.background()` 順序後重驗，或 (b) 另立 bug 票、本票驗收句改為「常數值與 .pen 一致」（code-level）並註記渲染缺口。

證據：`/Users/clyeh/little-sprout/.claude/evidence/LS-98/qa1/`（ipad-portrait-normal/ax3/dark、ipad-landscape-normal/ax3、iphone17pro-portrait-normal、iphone17promax-landscape、smoke-otp-login-timeline）。

模擬器已關：`main-iPhone17Pro`(26839CAC-C080-45B6-BED4-2E78C2D3CA89)、`qa-LS98-iPadPro13`(464ABEA4-AED4-431E-A37D-3B7C122B91EB)、`qa-LS98-iPhone17ProMax`(2871274F-6A46-4100-88F6-C86F0895D89C)。

### 2026-08-26T00:45:42.112Z · Chih-Lin Yeh · `6c1797ec`

**merge-review R1 — APPROVE**（PR #169 染料池渲染順序修正，head `a12417d`）

0 blocker／0 major／0 minor／4 informational。CI 四項全綠（ci／db／lint／rules）。diff 1 檔 −1/+5（4 行是註解），scope 乾淨。

獨立驗證（不引用 ios-dev 的取樣點）：

1. **疊層 vs 稿面**：`python3` 解 `.pen`，01 板 `XekQz`／01-iPad `eS0jX` 的 `Print` frame `fill` 是陣列 `[$print-paper, radial(0,0), radial(1,0), radial(0,1), radial(1,1)]`，台紙 fill[0] 最底、染料池在其上；`Photo Wrap`／`Imprint Row` 是 children（在 fills 之上）；四個 Corner ref 排在 `Print` 之後（最頂）。改後鏈 `background(glow).background(paper).overlay(corner)` 產生 **paper < glow < photo < corner**，逐層對上。深色 01c 板同構，且稿面 `Wordmark Strip` 是純台紙、不帶染料池——與 `wordmarkPaperStrip` 一致。
2. **像素反解**：對 r2 截圖壓邊帶反解 alpha 對照解析模型 `a_corner×(1−r/78pt)`，iPhone `.welcome` 全線誤差 ≤0.01（16pt 0.381/0.390、32pt 0.286/0.290、100pt 0.000/0.000）；iPad `.iPad` TL 0.333/0.330、TR 0.162/0.163、BL 0.114/0.104、BR 0.029/0.024；iPad 深色 0.332/0.166/0.104/0.024 對 0.330/0.163/0.104/0.024。深色 token（#0D0609／#0D060900／#E8D9D4）與 `.pen` variables 逐值相同。
3. **染料池沒畫到照片上**：QA1（修前）vs R2（修後）逐像素差分，照片內縮 4pt 區域 **max|Δ|＝0**（完全相同），差異只落在四角壓邊帶；角托三角形兩版都是常數 (200,154,163)，確認仍在最頂層。`clipped()` 沒有溢出台紙（卡片列在台紙寬度外 max|Δ|＝0）。Lab Imprint 置中處 alpha=0.000，對比度不受影響。

四維度：race condition 無（純宣告式 view）；效能無退化（疊層數與 blend 次數不變）；平行優化不適用；scope 符合票文「本票一併修」。

Informational（4 條，處置請 orchestrator 裁定）：I1 iPad 台紙幾何仍是 compact（稿面 420×直式、池直徑 240；實際 556×228pt、池 156）——強度對得上、幾何沒有，承接 R1 I1；I2 `.claude/evidence/LS-101/qa1/01-welcome-light-default.png` 等 baseline 需重拍（卡片區 |ΔRGB|>30 佔 0.40% 是合法差異，目前無自動化視覺 gate）；I3 `#Preview` 只蓋預設組合，建議補 `.iPad` 與深色 Preview（SwiftUI 無 ViewInspector，純值測試釘不住 z 序，同意本票不加測試）；I4（PLAUSIBLE，未觸發）`background(Color)` 走 ShapeStyle overload 預設 `ignoresSafeAreaEdges: .all`、`background(glow)` 走 View overload 不延伸，未來全出血版位會露出未染色紙邊。

全文：https://github.com/CLYEH/little-sprout/pull/169#issuecomment-5418989577

未跑：未 boot 模擬器、未跑 xcodebuild／swiftlint（以 CI 綠燈為證）；Max 橫向與 iPad 橫向本輪無 r2 截圖未複驗；`.pen` 只解 JSON 結構、未經 Pencil MCP 渲染疊圖。

### 2026-08-26T01:23:04.308Z · Chih-Lin Yeh · `fd4fa24c`

## QA2 — PASS

驗收 SHA `e4b2a75ddaa0a0a8208afa765985e72e214bda67`（origin/test tip，含 #167＋#169）。

### 1. 建置＋測試
`xcodebuild test`（destination `main-iPhone17Pro` 專屬機，simulator-lock 包住）：**157 tests, 0 failures**。

### 2. 染料池四角像素取樣（Δlum = 取樣點亮度 − print-paper 基準亮度；取樣點避開角托三角，方法同 QA1）

**iPad 直向・一般**（`ipad-normal-light.png`，print card bbox 112,1166–1222,1620 px @2x）

| 角 | RGB | Δlum | opacity token |
|---|---|---|---|
| topLeading | (237,214,217) | −0.1492 | 0.418 |
| topTrailing | (244,225,227) | −0.0749 | 0.207 |
| bottomLeading | (247,228,230) | −0.0490 | 0.132 |
| bottomTrailing | (250,234,235) | −0.0111 | 0.03 |

排序 TL>TR>BL>BR ✓；Δlum 比例 13.4:6.7:4.4:1 對 opacity 比例 13.9:6.9:4.4:1，高度吻合。

**iPad AX3**（`ipad-ax3-light.png`）：card bbox 與四角取樣像素值與一般字級**完全一致**（染料池是固定 pt 幾何，不吃 Dynamic Type）。

**iPad 深色**（`ipad-normal-dark.png`，需先用 gap 偵測分離「字標紙條」與「PrintPhotoCard」兩塊 print-paper 色塊，card 實際 bbox 112,1324–1222,1778）

| 角 | RGB | Δlum |
|---|---|---|
| topLeading | (188,175,171) | −0.2739 |
| topTrailing | (211,196,192) | −0.1434 |
| bottomLeading | (218,204,199) | −0.0947 |
| bottomTrailing | (229,214,209) | −0.0216 |

排序 TL>TR>BL>BR ✓；比例 12.7:6.6:4.4:1 對 13.9:6.9:4.4:1，吻合。

**iPhone 17 Pro 直向・一般 `.welcome`**（`iphone17pro-portrait-normal.png`，card bbox 48,202–1156,882 px@3x）

| 角 | RGB | Δlum | opacity token |
|---|---|---|---|
| topLeading | (234,210,213) | −0.1748 | 0.494 |
| bottomLeading | (239,217,220) | −0.1298 | 0.36 |
| topTrailing | (241,221,223) | −0.1040 | 0.288 |
| bottomTrailing | (243,223,225) | −0.0861 | 0.236 |

排序 TL>BL>TR>BR ✓（與 iPad 排序模式不同，兩個 mountPoolOpacity 常數各自的相對大小本就不同，符合預期）；比例 2.03:1.51:1.21:1 對 2.09:1.53:1.22:1，吻合。

四組取樣點皆以貼近但避開角托三角的座標二次驗證（放大裁圖確認落在乾淨的台紙壓邊帶，無三角/照片污染）。

### 3. 照片區不受影響
QA1 `iphone17pro-portrait-normal.png` vs 本輪同角度同機型screenshot 差分：
- 照片內縮 4pt 區域（1036×546px）**max|Δ|=0**（完全零差異）。
- 角托三角常數色點（topLeading／topTrailing 取樣）A/B 兩輪**完全相同** `(200,154,163)`，證實角托仍在最上層、色值不受本次修正影響。
- 全圖差分（threshold>1）另有 90163px 落在照片＋台紙以外（狀態列時鐘 7:52→9:06 環境差異＋字標圖片抗鋸齒次像素雜訊，經裁圖比對確認視覺一致，非版面/內容變化）；card bbox 內差異則落在四角壓邊帶（染料池從不可見→可見，即本票修正的預期效果）。

### 4. 字標框／壓邊量測
- iPad 字標 accessibility 元素量得 `247×118`（`AppSpacing.wordmarkWidthIPad/HeightIPad`），與稿面一致。
- 壓邊：iPad 螢幕像素探測台紙頂/底邊到照片起點的距離為 16px（@2x）＝8pt，兩板一致，與 QA1 量測無變化（本次修正只動 `.background` 呼叫順序，不動任何 frame/padding）。

### 5. 順手記錄（informational，非 FAIL）
`iphone17promax-landscape.png` 確認：iPhone 17 Pro Max 橫向（`horizontalSizeClass == .regular`）會套用 `regularLayout`（字標移到右欄、`mountPoolOpacity: .iPad`），與 R1 F1 描述一致。

### 6. 回歸冒煙
- Email OTP 登入（`qa-ls98-smoke@example.com`，本機 supabase，`demo-otp.sh` 取碼）→ 成功導向時間軸空狀態。截圖 `smoke-otp-login-timeline.png`。
- 過程中一度因**其他 agent 同時對共用本機 supabase 容器做操作**（`supabase_db` 短暫重啟）導致單次 500/400 瞬斷，等待穩定後重試即通過——與 LS-98 程式碼無關，記錄供巡檢參考。

### 證據
`/Users/clyeh/little-sprout/.claude/evidence/LS-98/qa2/`：`ipad-normal-light.png`、`ipad-ax3-light.png`、`ipad-normal-dark.png`、`iphone17pro-portrait-normal.png`、`iphone17promax-landscape.png`、`smoke-otp-login-timeline.png`。

Pen 路徑：本輪比對用像素取樣＋既有 QA1 截圖差分，未開 Pencil MCP（不需要）。

模擬器已關：`iPhone 17 Pro`(90FB3609)、`qa-LS98-iPhone17ProMax`(2871274F)、`qa-LS98-iPadPro13`(464ABEA4)；`main-iPhone17Pro` 由 xcodebuild test 自動起停，未殘留 Booted。

### 2026-08-26T01:25:01.681Z · Chih-Lin Yeh · `74be86d3`

## Orchestrator — lesson learning review（LS-98）

**流程回顧**：LS-17 sweeper 發現 `MountPoolOpacity.iPad` 零呼叫點 → 開票（Low）→ ui lane 補位派工（05:03 登入逾期中止一次、接手收尾）→ PR #167 R1 APPROVE（reviewer 自己解 `.pen` 對照 8 項；I2 更正票文誤植數字）→ **QA1 FAIL**：染料池四角像素全平——根因 `PrintPhotoCard` 兩個 `.background` 順序反了，opaque 台紙蓋住光暈，**iPhone 歡迎頁自 LS-17 起從未渲染過設計稿的四角染料池**（pre-existing）→ fix PR #169（5 行）R1 APPROVE（reviewer 逐像素差分：照片區 max|Δ|=0）→ QA2 PASS → main v0.4.2 → sweeper（進行中）。開票到 main 約 4.5 小時。

**Harness 改善**
- ✅ QA 派工改「測試機／手動機分離」（LS-105 撞機教訓），本票 QA1／QA2 都沒再撞。
- ✅ QA 像素取樣法（四角相對亮度排序）抓到 reviewer 靜態審查抓不到的渲染 bug——**視覺類驗收必須含像素證據，不能只比常數**；已寫進票文驗收句，建議 qa.md 加通則。
- 記 LS-96：R1 F1（iPhone Max 橫向也 regular）、I1（iPad 台紙幾何仍 compact，需 design 定案）、I3／I4／I5／I6；#169 R1 I2（基準截圖重拍、visual-diff 工具）、I3（Preview 覆蓋）、I4（`ignoresSafeAreaEdges`）；QA2 觀察：**共用本機 supabase 容器被其他 agent `db reset` 時 QA 冒煙短暫 500／400**（LS-70 lock 只序列化 reset，不擋讀端）。

**設定優化**：票文原始數字（140／51、20／34）為開票時誤植，R1 I2 更正——開票時引用稿面數值應由 `.pen` 解析取得，不靠記憶。

**工具缺口**：視覺基準比對（`scripts/gates/visual-diff.sh`）——與 LS-95／UI 量測合併評估。

**Done 前置**：QA2 PASS ✓（status 52909577479）、main v0.4.2（進行中）、sweeper（進行中）、retro ✓ → 完成後 Done。

### 2026-08-26T01:27:40.037Z · Chih-Lin Yeh · `99255d8c`

## dead-code-sweeper — LS-98 收尾巡檢（tip `e4b2a75`，PR #167＋#169，3 檔）

**結論：未發現本 feature 引入的死碼。**
1. `MountPoolOpacity.iPad` 現有呼叫點 `WelcomeView.swift:95`（LS-17 sweeper F1 已解）；`.welcome`／`.iPad` 皆有呼叫點，無第三個孤兒組合。
2. `wordmarkSize` 兩分支共用（唯一呼叫點 `wordmarkImage` 在 compact／regular 都渲染），非死分支。
3. xcodegen 對 `git archive e4b2a75` 樹 → pbxproj 位元級相同；swiftlint strict 3 檔 0；鷹架關鍵字 0；無測試／SQL 變更。

已標註不重報（皆在 LS-96）：#167 R1 I3（compact 190×91 內嵌 vs token 兩份來源）、I4（`.welcome` 預設值兩處）、I1（iPad 台紙幾何仍 compact）；#169 R1 I2（基準截圖過期、無視覺 gate）、I3（Preview 未覆蓋 `.iPad`／深色）、I4（`ignoresSafeAreaEdges`）。

盲區：文字 grep 抓不到 reflection；未重跑 `xcodebuild test`（QA2 157/157）。

### 2026-08-26T01:29:48.536Z · Chih-Lin Yeh · `b294f7b8`

## QA — PASS carried to 170db4c

沿用 QA2 PASS（`e4b2a75`，qa status id 52909577479，comment `fd4fa24c`）。`test`／`development` 已 FF 到 `170db4c6603038153f316c6ba51c9578c74faf18`（PR #171，back-merge hotfix #168 LS-106 push-gate 對齊 CI）。

驗證：
- `git diff --stat e4b2a75 170db4c` → 僅 5 個 harness 檔：`.github/workflows/ci.yml`／`.xcode-version`／`docs/COLLABORATION.md`／`scripts/gates/push-gate.sh`／`scripts/gates/push-gate.test.sh`
- `git diff --stat e4b2a75 170db4c -- LittleSprout LittleSproutTests supabase design` → 空（app 程式碼零變更）
- CI 對 `170db4c` 四項全綠：lint／ci／db／rules（兩次 push 事件皆 success，`gh api .../commits/170db4c.../check-runs`）

裁決：**PASS**（沿用 QA2，未重跑模擬器）。GitHub commit status `qa=success` 已貼到 `170db4c`。

