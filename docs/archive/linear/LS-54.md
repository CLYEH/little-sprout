# LS-54 Harness 小修批次：api-contract-check N1-N5＋approve_join 註解矛盾（PR #58 R2 minors）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Low |
| 標籤 | — |
| Cycle | — |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-24T16:08:24.383Z |
| 開始 | 2026-08-24T18:09:11.525Z |
| 完成 | 2026-08-24T19:11:18.037Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-54/harness-小修批次api-contract-check-n1-n5approve-join-註解矛盾pr-58-r2-minors |
| 關係 | related→LS-17, related→LS-44, related→LS-41, related←LS-106, related←LS-82, related←LS-63, related←LS-50, related←LS-56, related←LS-52, related←LS-48, related←LS-49, related←LS-55 |

## 描述

PR #58 第 2 輪 APPROVE 附帶 minor，集中一張小票（可併入下一次 harness 維護批次）：

* **N1** text 模式與 catalog 模式對顯式 `OUT` 參數處理不一致（text 保留、catalog 排除）→ text 對齊 catalog（out 排除、inout/variadic 保留）；自測補 variadic/out 樣本（`strip_mode_keyword` 現零覆蓋）。
* **N2** F5 錯誤訊息「請把註解移到獨立行」誤導——判定是括號區間內任何 `--`，獨立行也紅；改「移到 create function 那行之前（參數列括號外）」。
* **N3** catalog 模式 `.lower()` 讓 `public.widgets` 與 `public."Widgets"` 塌成一個→靜默逃逸（本 repo 無引號識別字，機率極低）；拿掉 lower 或對 `name != name.lower()` fail loud。
* **N4** API.md §9 補「CI 以 `--catalog`（活 DB）為權威；本機 best-effort；不一致以 CI 為準」。
* **N5** rules job 兩支自測同一 `run:` 下 `bash -e`——第一支紅第二支不跑；收兩個 rc 再一起 exit。
* **N6**（另檔）`20260823010000_join_approval.sql` approve_join 檔頭註解仍寫「不會從錯誤碼差別推敲 request id 存在性」，與 API.md F9 訂正矛盾→ 改註解（同形式 migration 註解可改？依「migration 是歷史紀錄」原則，改用新 migration 的 `comment on function` 或在 API.md 標註即可）。
* **N7** `api_contract_check.py:9` 註解「EXECUTE 動態 DDL 解析不到」偏保守（字面 DDL 會撈到），更正描述。

## 追加（PR #63 R2 harness 項）

* **N4** `docs/API.md` §5 錯誤碼表 ↔ `LSErrorCode.allCases` 對帳 gate（現無機械覆蓋：PR #60 併入後 LS020-022 會落 `.server` 而 CI 全綠）；並補 `LS999 → .server` 測試。
* **N8** 測試序列執行假設無 gate：scheme／CI 明確 `-parallel-testing-enabled NO`（`MockURLProtocol` 全域 handler 在平行測試下會互相汙染）。

## 追加（[LS-48](https://linear.app/little-sprout-app/issue/LS-48/taskls-21-後端-diaries-schema-補全時間軸查詢-rpcrls測試) 收尾）

* **D1** `docs/PLAN.md` §5 補一句：`feed_items` 自 [LS-48](https://linear.app/little-sprout-app/issue/LS-48/taskls-21-後端-diaries-schema-補全時間軸查詢-rpcrls測試) 起新增索引/FK 一律 `CREATE INDEX CONCURRENTLY`＋`NOT VALID` → `VALIDATE`（migration 需拆成非交易檔）。
* **D2** `supabase/tests/run.sh` 會改寫 tracked 的 `supabase/tests/evidence/explain_rls_plan.txt`（時間戳/actual time），本機跑一次就弄髊工作區 → 濾掉 actual time 或 gitignore＋CI artifact。
* **D3** ios-dev agent 定義補一條：「push 後立即交 handoff，CI 由 orchestrator 監看，不得在等 CI 時收工」（[LS-49](https://linear.app/little-sprout-app/issue/LS-49/ios-基礎層supabase-sdk-整合環境設定auth-session-服務無-ui) 連續三次）；可併 [LS-44](https://linear.app/little-sprout-app/issue/LS-44/harnessui-designervisual-reviewer-定義更新批次pencil-缺陷清單4收工程序內容證明) 批次。

## 追加（[LS-41](https://linear.app/little-sprout-app/issue/LS-41/api-契約成品化docsapimddocschema-對帳-gate) 收尾）

* **D4** `docs/API.md` §5（約 L450-454）「目前 repo 尚未有任何網路層 Swift 程式碼…留給 [LS-17](https://linear.app/little-sprout-app/issue/LS-17/storysign-in-with-appleemail-otp-登入)」已被 [LS-49](https://linear.app/little-sprout-app/issue/LS-49/ios-基礎層supabase-sdk-整合環境設定auth-session-服務無-ui) 推翻（`LSErrorCode` 已涵蓋 LS001/002/010-017，尚缺 LS020-022）→ 改寫為現況（已覆蓋碼與尚缺碼）。
* N1 補充：`api-contract-check.test.sh` 補第 11 組樣本 `create function public.foo(in a text, out b uuid)` 覆蓋 `strip_mode_keyword`。

## 留言（2）

### 2026-08-24T18:33:29.211Z · Chih-Lin Yeh · `efea9145`

**Handoff**（`hotfix/LS-54-harness-minors` 7 commits，PR #69 → main）
- N1/N2/N3/N7 解析器；自測 10→15 組（in/out、inout+variadic、合成 catalog 正向、`Widgets`/`Foo` fail loud）。
- **N4′ 錯誤碼對帳 gate** `scripts/gates/error-codes-check.sh`（§5 表 ↔ `LSErrorCode` rawValue 雙向、空集合亦紅）＋6 組自測，掛 push-gate 第 4 步與 CI rules 獨立 step；`LSErrorCode` 補 LS020/021→rejected、LS022→validationRetryable；`LS999→.server` 測試；**負向實證**：對修補前 AppError 跑 gate 紅並列出 LS020-022。xcodebuild 58/58。
- N5 rules job 三支自測各收 rc；N8 `parallelizable: false`＋CI/push-gate `-parallel-testing-enabled NO`（xcodegen 0 diff）；N4/N6/D4 API.md；D1 PLAN §5 CONCURRENTLY 規則；D3 ios-dev 硬規則；§7 兩新列、§6 精確化。
- **D2 改 gitignore＋CI artifact**：遮罩實測四種漂移源（計時／規劃器估計／NOTICE 交錯／buffers hit 23 vs 20）無止境 → `evidence/` gitignore＋`git rm --cached`＋db job `upload-artifact@v4`。
- 小幅擴 scope：push-gate 的 xcodebuild 也加 parallel NO（同一假設一致），供 review 裁量。
- 工具缺口（retro）：migration `errcode` ↔ §5 表仍無機械對帳；text 模式引號識別字靜默略過（catalog 兜底）；§4 Push gate 敘述過時。
→ 派 merge-reviewer；CI 由 orchestrator 監看。

### 2026-08-24T18:42:03.110Z · Chih-Lin Yeh · `b9b924e9`

**merge-reviewer：APPROVE**（自測 6/6＋15/15 親跑；N5 rc 收集模擬確認；scheme 實值 `parallelizable="NO"` 宣稱屬實；D2 附帶正面效果：evidence 變更過去能滿足「migration 必附測試」gate 的誤放行路徑消失；back-merge 對 development/test 乾淨且語意面 doc 13 碼／Swift 13 碼一致）。3 minor → 新票（error-codes 抽取硬化＋retention-days）。**PR #69 已併入 main**；back-merge PR 開立中，落地後 Done。

