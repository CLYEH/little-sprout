# LS-244 Task：LS-20 後端——正式站 Storage 部署驗證腳本 `scripts/ops/prod-storage-verify.sh`（PLAN §5 三條「本機測不到、`db push` 綠燈不能取代」清單機械化：`storage.prefixes` RLS／`owner`＋`owner_id` 填值／`storage.buckets` policy 數＝0；唯讀、比照 `prod-purge-health.sh`）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:S, lane:backend |
| Cycle | 2 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M3 相簿與上傳 |
| 估點 | — |
| 建立 | 2026-09-12T23:46:32.731Z |
| 開始 | 2026-09-12T23:47:50.628Z |
| 完成 | 2026-09-13T01:52:47.407Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-244/taskls-20-後端-正式站-storage-部署驗證腳本-scriptsopsprod-storage-verifyshplan-5 |
| 父票 | LS-20 Story：相簿與照片上傳（壓縮／縮圖／背景續傳） |
| 關係 | related→LS-236, related→LS-239, related→LS-235, related→LS-209, related→LS-242, related←LS-238, related←LS-237, related←LS-96 |

## 描述

## 來源

backend lane 09-13 07:45 空位補位（[LS-242](https://linear.app/little-sprout-app/issue/LS-242/taskls-20-後端-purge-storage-讀佇列重試預算改為整次-invocation-共用現行每批最壞-7s-max) 進 QA）。Explore 09-13 04:57 盤點：`docs/PLAN.md` §5「Storage 的雲端部署驗證清單」三條（約 L119–L125）原文明講「本機測不到、`db push` 綠燈不能取代」，但全 repo 只有 PLAN.md 提到 `storage.prefixes`，`scripts/ops/` 無對應腳本、無票——目前靠人記得去 Dashboard 看。

## 範圍

1. `scripts/ops/prod-storage-verify.sh`：**唯讀**查正式站（沿 `prod-purge-health.sh` 的連線方式：必須在 `supabase link` 過的目錄跑、走既有唯讀通道，不碰任何憑證檔內容）三條檢查：(a) `storage.prefixes` 的 RLS 已啟用且 policy 與 PLAN §5 描述一致（列 policy 名稱）；(b) 抽樣最近 N 筆 `storage.objects`（預設 20）`owner`／`owner_id` 誰填值、是否與 `media.uploaded_by`（或 PLAN 指定欄）一致；(c) `storage.buckets` 上的 policy 數應為 0（PLAN §5 原文）。每條印 ✓／⚠＋原文，任一 ⚠ exit 1；`--fixture <dir>` 走假查詢輸出供自測。
2. 自測 `prod-storage-verify.test.sh`：fixture 三型（全綠／(a) RLS 關／(c) 有 policy）＋mutation；`ci.yml` Ops 自測接線；`agent-tools-check.sh` 綠。
3. `docs/COLLABORATION.md` §4-b 加一句「部署含 Storage 的 migration 後跑 `prod-storage-verify.sh`」＋§7 一列；`docs/PLAN.md` §5 三條旁註腳本名。

## 不做

修任何發現的問題（只報告；若真發現 ⚠ 另開票）；自動化排程；本機容器版本（PLAN 明說本機測不到）。

## 驗收

- [ ] 自測綠、mutation 紅；`agent-tools-check.sh` 綠；merge-reviewer 通過（scope：新腳本＋自測＋文件；唯讀：SQL 只有 select）
- [ ] QA delta：fixture 三型原文；併 main 後 orchestrator 對正式站實跑一次並把原文貼票

## 留言（14）

### 2026-09-12T23:47:57.697Z · Chih-Lin Yeh · `2636678c`

**orchestrator 09-13 07:46**：backend lane 空位補位（LS-242 進 QA；Explore 候選：PLAN §5 三條唯一無票缺口）。worktree `.claude/worktrees/LS-244`／`feature/LS-244-prod-storage-verify` 自 development `9cf2245` 建；ios-dev R1 派出（只做 fixture，正式站實跑留 orchestrator 併 main 後）。

### 2026-09-13T00:00:10.539Z · Chih-Lin Yeh · `f99d8230`

**ios-dev 09-13 07:59 R1**

**Ticket**：LS-244——正式站 Storage 部署驗證腳本 `scripts/ops/prod-storage-verify.sh`（PLAN §5 三條「本機測不到、`db push` 綠燈不能取代」清單機械化）

**已完成**（範圍 1–3 全做）：
1. 新增 `scripts/ops/prod-storage-verify.sh`：唯讀，沿用 `prod-purge-health.sh` 連線骨架（腳本開頭檢查已 `supabase link`，未 link exit 2 並提示；不改判準原文）。三條檢查各自獨立一次 `supabase db query --linked`：(a) `storage.prefixes` 用 `to_regclass()` 判存在，存在才查 `relrowsecurity`／`pg_policy`（表不存在 ⓘ 略過；RLS 已啟用又無 policy 才 ⚠）；(b) `storage.objects where bucket_id='media'` 依 `created_at` 抽樣最近 N 筆（預設 20，`--limit`）聚合判斷 `owner`／`owner_id` 至少一欄有填值（抽樣 0 筆 ⓘ 略過；兩欄整批皆 NULL 才 ⚠，並註明「上傳者自刪孤兒物件在雲端不會生效」）；(c) `storage.buckets` 的 `pg_policy` 數應為 0，非 0 即 ⚠。任一 ⚠ exit 1；全 ✓／ⓘ exit 0。`--fixture <dir>`（目錄內 `prefixes.json`／`objects.json`／`buckets.json` 三個完整 CLI JSON 信封）供自測跳過真正連線。全程只 select，無任何寫入語句。
2. 自測 `scripts/ops/prod-storage-verify.test.sh`（31 組樣本）接進 `ci.yml` Ops 自測步驟；`bash scripts/gates/agent-tools-check.sh` 綠。
3. 文件：`docs/COLLABORATION.md` §4-b 補一句、§7 補一列前饋↔反饋對照；`docs/PLAN.md` §5 標頭旁註腳本名，三條原文判準未改一字。

**已驗證**（逐項對應範圍 1–3，附測試名／指令／斷言原文）：
- 範圍 1：`bash scripts/ops/prod-storage-verify.sh --fixture <dir>` 手動跑全綠 fixture，三行皆 `✓`、exit 0（本機驗過）。
- 範圍 2：`bash scripts/ops/prod-storage-verify.test.sh` → 31 組全綠，含①全綠／②(1) RLS 開無 policy ⚠／③(2) owner／owner_id 皆 NULL ⚠／④(3) buckets 有 policy ⚠／⑤(1) 表不存在 ⓘ／⑥(2) 抽樣 0 筆 ⓘ／⑦ RLS 關閉負控／⑨–⑬ 參數驗證 exit 2／⑭ fixture 目錄不存在／⑮ fixture 目錄缺檔／⑯ 信封缺 rows／⑰ 未 link 前置檢查 exit 2。
  - **Mutation**：改了什麼——`prod-storage-verify.sh` python 段 `if rls and not policies:` 改成 `if False:`（拿掉 (1) 判斷）。哪條測試紅——樣本②三條斷言：`②（1）RLS 開無 policy → exit 1 印 ⚠`／`② 摘要印 RLS 已啟用但無 policy`／`② 異常清單點名`。斷言訊息原文：
    ```
    ✗ ②（1）RLS 開無 policy → exit 1 印 ⚠（期望 exit 1、輸出含「⚠ prod-storage-verify 發現異常」，實得 0）
    ✗ ② 摘要印 RLS 已啟用但無 policy（應含「⚠ storage.prefixes：RLS 已啟用但沒有任何 policy」）
    ✗ ② 異常清單點名（應含「storage.prefixes 啟用 RLS 但無 policy」）
    ```
    其餘 28 條斷言不受影響（皆 ✓），確認②是這個判斷的唯一防線；還原後重跑全綠。
  - Linux 通道：`docker run --rm -v "$PWD":/w -w /w ubuntu:24.04 bash -c 'apt-get update -qq && apt-get install -y -qq jq python3 >/dev/null && bash scripts/ops/prod-storage-verify.test.sh'`（jq 1.7、python3 3.12.3）→ 31 組全綠。
- 範圍 3：`docs/COLLABORATION.md`／`docs/PLAN.md` 差異已 push，見 PR diff；`agent-tools-check.sh` exit 0（本票未動任何 `.claude/agents/*.md`）。
- 純 ops bash＋文件，未開模擬器，**未對正式站跑任何東西**（只用 fixture，正式站實跑留給 orchestrator 併 main 後執行）。

**未完成**：無（本票純新增腳本＋自測＋文件；尚未經 merge-review，無 reviewer informational 待處置）。

**風險**：
- 範圍 2 的 owner／owner_id 判準是「整批抽樣聚合」而非逐列比對——只看整批是否至少一欄有任何填值，看不出列間分佈不一致的細節；PLAN 原文本身只驗 `limit 1`，此設計已是延伸加強、非退步。
- 用 `to_regclass()` 取代 PLAN 原文字面的 `'…'::regclass` 顯式轉型（避免表不存在時整條查詢直接拋錯，改為安全回 NULL）——語意等價於原文判準，是實作面選擇，非判準變更。
- 三條檢查各自獨立一次 `supabase db query --linked`（非單一組合查詢）——換取職責清楚與 `--fixture <dir>` 可分別替換情境，代價是三次網路往返；正式站規模下可忽略。

**產出位置**：
- PR：https://github.com/CLYEH/little-sprout/pull/381（feature/LS-244-prod-storage-verify → development）
- head sha：`e75fc7fe642dc04ef000fda78c25c82b3fd3effc`
- 新增：`scripts/ops/prod-storage-verify.sh`、`scripts/ops/prod-storage-verify.test.sh`
- 修改：`.github/workflows/ci.yml`（Ops 自測接線）、`docs/COLLABORATION.md`（§4-b／§7）、`docs/PLAN.md`（§5 標頭旁註）
- 模擬器已關：無（本票未開模擬器）

**orchestrator 併 main 後在主 checkout 實跑一次的指令與預期輸出形狀**：
```
bash scripts/ops/prod-storage-verify.sh
```
預期（全健康）：
```
✓ storage.prefixes：relrowsecurity=<True/False>，policies=<清單或「無」>
✓ storage.objects：抽樣 <N> 筆（limit=20），owner 已填 <a> 筆／owner_id 已填 <b> 筆
✓ storage.buckets：policy 數＝0（PLAN §5-3 預期狀態）
✓ prod-storage-verify 三條檢查皆通過
```
`storage.prefixes` 表不存在時第一行改印 `ⓘ storage.prefixes：表不存在，略過（本機開發映像通常沒有這張表，PLAN §5-1）`；`storage.objects`（media）目前無物件時第二行改印 `ⓘ storage.objects（bucket=media）：抽樣 0 筆，略過（bucket 目前無物件）`。任一行 `⚠` 開頭即異常（exit 1，stderr 另印「⚠ prod-storage-verify 發現異常：」逐條列出）——請把完整原文貼回本票（票文 QA delta 要求）。

### 2026-09-13T00:01:11.877Z · Chih-Lin Yeh · `2a5c907e`

**orchestrator 09-13 08:00**：R1 handoff 收到（PR #381，head `e75fc7f`；31 組自測 macOS＋docker、mutation ② 轉紅、fixture only）→ In Review、派 merge-review R1（重點：SQL 純 select、三條判準與 PLAN §5 原文逐字對、抽樣查詢不全表掃描）。

### 2026-09-13T00:12:33.427Z · Chih-Lin Yeh · `c0165ef8`

**merge-reviewer 09-13 08:11 R1 APPROVE**

PR #381（`feature/LS-244-prod-storage-verify` → development）／審的 head `e75fc7fe642dc04ef000fda78c25c82b3fd3effc`（`gh pr view 381 --json headRefOid` 當下同值，審查期間 head 未前進）／3 commit／5 檔 406+ 2−。supabase lock：**未持有**（本票純 fixture 自測，不需本機 stack；未開模擬器；未對正式站跑任何東西）。

**blocker 0、major 0 → APPROVE。**

**一、唯讀與安全（票文驗收「唯讀：SQL 只有 select」）**
- 腳本實際送出的三段 SQL 我用 shim 假 `supabase`（攔 `-f <tmpfile>`）原樣攔下來比對：三段都只有 `select`（`grep -nE "insert|update|delete|alter|create |drop|truncate|grant|revoke|copy " scripts/ops/prod-storage-verify.sh` 零命中）。
- 唯一被字串內插進 SQL 的是 `--limit`，在 `scripts/ops/prod-storage-verify.sh:57-66` 先過 `''|*[!0-9]*` 與 `-lt 1` 兩道（自測 ⑨⑩ 釘住 exit 2），注入不可能。
- 不讀不印任何憑證：只對 `supabase/.temp/project-ref` 做**存在性**檢查（`:74`），不讀內容；連線骨架與 `scripts/ops/prod-purge-health.sh:250` 逐字一致（`supabase db query --linked --output-format json -f "$sql_file"`），信封解析（`rows[0].payload`）沿用 LS-235 R2 已對正式站實跑驗證過的形狀。

**二、判準對 `docs/PLAN.md` §5 原文（逐條）**
- (1) `scripts/ops/prod-storage-verify.sh:200-210`：`exists` 假 → ⓘ 略過；`relrowsecurity` 真且 `policies` 空 → ⚠；其餘 ✓ 並列 policy 名。對得上 §5-1「啟用了 RLS 又沒有任何 policy」。RLS 關閉的負控由自測 ⑦ 釘住（印 `✓ storage.prefixes：relrowsecurity=False`）。
- (2) `:212-228`：`bucket_id='media'` 抽樣最近 N 筆，`sample_size=0` → ⓘ（0 筆物件行為有定義）、兩欄 filled 皆 0 → ⚠、否則 ✓ 印兩欄各自筆數。對得上 §5-2「至少一欄非 NULL」。
- (3) `:230-236`：`policy_count == 0` → ✓，非 0（含 payload 缺鍵回 None）→ ⚠ 且 exit 1。對得上 §5-3「應為 0」。
- exit code：任一 ⚠ → 1（`:242-246`）；全 ✓／ⓘ → 0；查無資料／信封壞掉 → fail loud exit 1（`:154-157`、`extract_payload`）；用法錯 → 2。沒有任何「解析失敗當成健康」的路徑。
- `docs/PLAN.md` 三條原文判準一字未改（`git diff` 只動 §5 標頭那一行，`docs/PLAN.md:119`）。

**三、真通道 SQL 我另外實跑過（自測全走 fixture，SQL 本身零覆蓋）**
把攔下來的三段 SQL 丟進一個拋棄式 `public.ecr.aws/supabase/postgres:17.6.1.159` 容器（不是 `supabase_*` 共用容器，不需 lock；審完已 `docker rm -f`）：
- check 1（無 `storage.prefixes` 時）：回 `{"exists": false, "policies": [], "relrowsecurity": null}` —— `to_regclass()` NULL 路徑正確、不像 `::regclass` 那樣拋錯，實作者的選擇成立。
- check 2 空集合：仍回**一列** `{"sample_size": 0, "owner_filled": 0, "owner_id_filled": 0}` —— ⓘ 略過路徑在真通道成立，不會撞到 `rows 為空` 的 fail-loud。塞 2 筆 media（1 筆兩欄有值）回 `{"sample_size": 2, "owner_filled": 1, "owner_id_filled": 1}`。
- check 3：回 `{"policy_count": 0}`。
- check 2／3 對缺表時只回 `relation ... does not exist`（名稱解析階段），代表語法本身解析通過。

**四、mutation 重放（LS-209：申報一律自己重放）**
兩組都在 scratchpad 的副本上做，worktree 全程乾淨（`git status --porcelain` 空）。
- 重放 handoff 申報那組：`prod-storage-verify.sh` 的 `if rls and not policies:` → `if False:`，`bash scripts/ops/prod-storage-verify.test.sh` 轉紅**三條，與 handoff 附的斷言原文逐字相同**：`✗ ②（1）RLS 開無 policy → exit 1 印 ⚠（期望 exit 1、輸出含「⚠ prod-storage-verify 發現異常」，實得 0）`／`✗ ② 摘要印 RLS 已啟用但無 policy（應含「⚠ storage.prefixes：RLS 已啟用但沒有任何 policy」）`／`✗ ② 異常清單點名（應含「storage.prefixes 啟用 RLS 但無 policy」）`，其餘 28 條綠。**申報屬實。**
- 自選一組（反轉 (3) count 判斷）：`if policy_count == 0:` → `if policy_count != 0:`，轉紅 11 條，含 ④ 三條原文 `✗ ④（3）storage.buckets 有 policy → exit 1 印 ⚠`／`✗ ④ 摘要印 policy 數（應含「⚠ storage.buckets：policy 數＝2」）`／`✗ ④ 異常清單點名（應含「storage.buckets 有 2 個 policy（預期 0）」）`，另牽動 ①⑤⑥⑦ 的全綠斷言。判斷確實是承重的。

**五、其他維度**
- 自測：`bash scripts/ops/prod-storage-verify.test.sh` → 31 條斷言＋1 條總結全綠（macOS 本機）；`docker run --rm -v "$PWD":/w -w /w ubuntu:24.04 …` 重跑同樣全綠（python3 3.12.3），**Linux 通道申報屬實**。`bash scripts/gates/agent-tools-check.sh` exit 0。`gh run view 34726803206`：lint／db／rules／ci／ci-ipad 五個 job 全 success（`rules` 已實際跑到新自測）。
- 自測沒有任何一條會打真連線：⑨⑩ 在參數驗證就 exit 2、⑭⑮⑯ 走 fixture 分支、⑰ 在未 link 檢查就 exit 2，CI runner 沒有 supabase CLI 也不會假綠。
- Race condition：單執行緒 bash，三次序列查詢、無背景工作、無共享狀態——無可競態面。
- 平行優化：三次 `supabase db query --linked` 往返可合併成一次；實作者在 handoff 風險欄已申報此取捨（換 fixture 粒度與職責分離），對「部署後人工跑一次」的用途成立，不列 finding。
- Scope：只動票文列的 5 個檔（`scripts/ops/prod-storage-verify.sh`／`.test.sh` 新增、`.github/workflows/ci.yml`／`docs/COLLABORATION.md`／`docs/PLAN.md`），`ci.yml` 單次註冊（`grep -n prod-storage-verify .github/workflows/ci.yml` 只有步驟名與一行呼叫），無順手重構、無無關改動。

**informational（全列，請 ios-dev 在下一次 handoff「未完成」欄逐條處置）**
- **I1（minor，docs 精確度）`docs/PLAN.md:119`／`docs/COLLABORATION.md:143`**：兩處都寫「三條已機械化」，但 §5-1 原文的**最終判準是「做一次真實上傳」**，本腳本明白不做那一步（腳本檔頭 `scripts/ops/prod-storage-verify.sh:8-9` 自己承認「不做真實上傳那一步最終判準」）。失敗情境：未來部署者看到 PLAN 旁註，跑完三行 ✓ 就認定 §5 清單驗完而略過真實上傳；若 `storage.prefixes` 有 policy 但該 policy 不讓 authenticated 寫，本腳本仍印 ✓（check 1 只看「有沒有 policy」不看內容），上傳照樣 403。建議旁註補一句「§5-1 的『真實上傳』最終判準仍需人工」。
- **I2（minor，判準強度）`scripts/ops/prod-storage-verify.sh:212-228`**：check 2 是整批聚合。失敗情境：storage-api 升版後新上傳不再填 `owner`／`owner_id`，事發後只上傳了 3 張，抽樣 20 筆裡仍有 17 筆舊的有填 → 印 `✓ … owner 已填 17 筆／owner_id 已填 0 筆` 且 exit 0，機械面完全不告警（數字有印出來、人看得見，但沒有任何判定接住）。建議：`owner_filled + owner_id_filled` 少於 `sample_size` 時至少降級成提示行，或單獨判「最新一筆」。註：PLAN 原文自己是 `limit 1` 且無排序，本實作不是退步。
- **I3（informational，scope 對票文）**：票文範圍 1(b) 還寫了「是否與 `media.uploaded_by`（或 PLAN 指定欄）**一致**」，本次未實作（只數填值筆數），handoff「未完成」欄也沒申報這項省略。我不列 major：PLAN §5-2 原文與本次派工判準都只要求「至少一欄非 NULL」，票文括號本身就把判準讓給 PLAN。請 orchestrator 裁定：接受（以 PLAN 為準）或記入 LS-96。
- **I4（informational，exit code 一致性）`scripts/ops/prod-storage-verify.sh:68-71`**：`--fixture <不存在的目錄>` 走 exit 1＋`⚠` 前綴，其餘用法錯誤走 exit 2＋`✗`。exit 1 在本腳本語義是「正式站有異常」，路徑打錯會被人或未來的排程誤讀成真異常。自測 ⑭ 已刻意釘住此行為（註明 fail loud），所以是明知的選擇；仍建議統一成 exit 2（fail loud 一樣成立）。
- **I5（informational／PLAUSIBLE，效能）`scripts/ops/prod-storage-verify.sh:95-110`**：`order by created_at desc limit N` 沒有可用的 created_at 索引（repo migrations 未對 `storage.objects` 加任何索引；上游 storage schema 的索引是 `(bucket_id, name)` 系列）→ 需讀完 media bucket 全部列再 top-N 排序，成本隨物件數線性成長。我在拋棄式容器上對同形狀 stand-in 表 `explain` 得到 `Limit → Sort → Seq Scan`（該表無索引，只是形狀示意）。標 PLAUSIBLE：**我沒有查正式站的實際索引清單**（本票不對正式站跑）。以「部署後人工跑一次」的用途現階段可忽略，不建議為此加索引；若哪天 media 物件數上量再說。
- **I6（informational，正面記錄）**：自測全走 fixture，三段 SQL 在 CI 裡零覆蓋——這不是缺陷（失敗時 fail loud、不誤報健康），但代表 SQL 迴歸只能靠正式站實跑接住。我這輪已用拋棄式 supabase/postgres 容器補跑（見上「三」），結論是三段 SQL 在 PG 17.6 上語法與 NULL 路徑皆正確。

**QA delta 建議**：fixture 三型原文已由自測 ①②③④ 覆蓋；併 main 後 orchestrator 對正式站實跑 `bash scripts/ops/prod-storage-verify.sh` 一次並把三行原文貼票（票文 QA delta 要求），若 check 1 印 ✓ 請一併確認 §5-1 的真實上傳仍有人做過（見 I1）。

---
`bash scripts/gates/handoff-evidence-check.sh <本 comment 暫存檔> --repo .claude/worktrees/LS-244`（本票新增的腳本只存在於 PR 分支，預設主 checkout 找不到，故指 worktree）：
```
✓ 第 8 行起的列項有證據
✓ 第 9 行起的列項有證據
✓ 第 10 行起的列項有證據
```
exit 0。

### 2026-09-13T00:14:49.579Z · Chih-Lin Yeh · `489fa33e`

**orchestrator 09-13 08:16**：merge-review R1 APPROVE（`c0165ef8`，status 貼 e75fc7f；真 Postgres 容器補跑三段 SQL 皆只 select、mutation 兩組屬實）→ 等 CLEAN 併入 development（follow script）→ 下一批 promote → QA delta → 併 main 後 orchestrator 對正式站實跑。I3 裁定：**接受**——PLAN §5 原文只要求 owner／owner_id 至少一欄非 NULL，票文「或 PLAN 指定欄」以 PLAN 為準，`media.uploaded_by` 一致性不納入；I1／I2／I4／I5 記 LS-96。

### 2026-09-13T00:18:01.866Z · Chih-Lin Yeh · `24aa018e`

**ios-dev 09-13 08:17 merge-development**

依 orchestrator 指示在 worktree `.claude/worktrees/LS-244` 執行 `git fetch origin && git merge origin/development`（development 自切分支後併入 #375／#376／#379 LS-239 hotfix／#380）。

- **衝突檔清單**：僅 `docs/COLLABORATION.md`（§7 前饋↔反饋對照表——兩邊都在檔尾各自新增表格列）。解法：兩邊新增段全保留，development 的 LS-239 三列在前、本票 LS-244 一列在後，無刪減任何一邊內容。
- **自動合併、無標記殘留**：`.github/workflows/ci.yml`（Ops 自測步驟同時列 `large-file-read-guard.test.sh`——實際掛在 `rules` job 的自測列表，非 Ops 步驟——與 `prod-storage-verify.test.sh`，兩者互不干擾）、`docs/PLAN.md`（LS-239 未動 §5，無衝突）。
- **合併後重驗**：`bash scripts/ops/prod-storage-verify.test.sh` → 31 組全綠；`bash scripts/gates/agent-tools-check.sh` → exit 0（本次合併帶進 `.claude/agents/*.md` 的上游變更，重驗確認未破字樣規則）。
- 已 commit（`chore(ops): LS-244 merge development`）並 push。

**新 head sha**：`34febe94f7cef473f1031dffcb2dde0bd23a83c1`（merge commit，parents `e75fc7fe642dc04ef000fda78c25c82b3fd3effc` ＋ `d9092b90a1d850f6745eef43965a01f7d20bdb08`）

`gh pr view 381 --json mergeStateStatus` 現況：`BLOCKED`（`mergeable: MERGEABLE`，非 DIRTY——BLOCKED 是新 head 的 CI 尚未回報／缺 merge-review status 這一類正常過渡狀態，不是衝突）。

請 reviewer 對新 head 做 merge-commit delta 核對（R1 已 APPROVE `e75fc7f`，這次只多了 merge 進來的上游 commit，本票自身內容無變動）後補貼 status。

### 2026-09-13T00:21:39.982Z · Chih-Lin Yeh · `ef892a91`

**merge-reviewer 09-13 08:20 R1 APPROVE（merge commit delta 核對，不重審）**

新 head `34febe94f7cef473f1031dffcb2dde0bd23a83c1`（merge commit，親 `e75fc7fe642dc04ef000fda78c25c82b3fd3effc`＝我 R1 APPROVE 的那顆 ＋ `d9092b90a1d850f6745eef43965a01f7d20bdb08`＝dev）。`gh pr view 381 --json headRefOid` 當下同值。**結論：合併未夾帶任何新變更，R1 裁決原樣沿用。**

**一、合併後對 dev 的淨差＝#381 原三點，一行不多**
- `git diff d9092b9..34febe9 --stat`：`scripts/ops/prod-storage-verify.sh`（+250 新增）／`scripts/ops/prod-storage-verify.test.sh`（+150 新增）／`.github/workflows/ci.yml`（4 行）／`docs/COLLABORATION.md`（+2）／`docs/PLAN.md`（1 行）＝5 檔 406+ 2−，與 R1 審的 diffstat 完全相同。
- 逐行核對：把 R1 的 diff（`git diff $(git merge-base origin/development e75fc7f)...e75fc7f`）與 `git diff d9092b9..34febe9` 各自抽出實際增刪行比對 → **逐字相同**；兩份 diff 的差異只有 blob hash、hunk 行號與 context 行（因為 §7 表尾在 dev 端多了 LS-239 三列當上下文）。
- blob 直接比對：`git rev-parse e75fc7f:<path>` vs `34febe9:<path>` → `scripts/ops/prod-storage-verify.sh`（`d2b1188`）／`scripts/ops/prod-storage-verify.test.sh`（`b04527f`）／`docs/PLAN.md`（`199535e`）三者皆**同一顆 blob**，即我 R1 逐行審過的內容一個位元都沒動。

**二、衝突解法（`docs/COLLABORATION.md` §7）**
- `git show --cc --format= 34febe9 | grep '^diff --cc'` → 只有 `docs/COLLABORATION.md` 一檔，符合申報（其餘 11 檔是 dev 側 LS-239 的內容，走 second-parent 不算衝突）。
- §7 表尾順序：…LS-236 `pen-open` 列 → LS-239 ①②③ 三列 → **LS-244 一列在最後**，兩邊的列都在、沒有互相覆蓋。§4-b 也是兩句並存（`每週巡檢正式站 purge 健康度`＋`部署含 Storage 的 migration 後跑 prod-storage-verify.sh`，`docs/COLLABORATION.md:145`）。

**三、`ci.yml` 兩支自測都在、各註冊一次（與派工描述略有出入，非異常）**
- `grep -c`：`large-file-read-guard.test.sh` 1 次（`.github/workflows/ci.yml:223`）、`prod-storage-verify.test.sh` 1 次（`:335`）。
- 兩者**不在同一個 step**：LS-239 那支在「Gate 腳本自測」step（`:172` 起），LS-244 這支在「Ops 腳本自測」step（`:312` 起）——但**同屬 `rules` job**，所以 CI 都會跑；這是 LS-239 自己選的落點，不是合併掉隊。派工單寫「Ops 步驟同時含兩支」與實況不同，我核對後判定為描述誤差、非問題。

**四、已驗證（重跑，怎麼驗；在 worktree `.claude/worktrees/LS-244`，HEAD＝34febe9、`git status --porcelain` 空）**
- `bash scripts/ops/prod-storage-verify.test.sh` → exit 0，31 條斷言＋總結 `✓ prod-storage-verify 自測通過（三條檢查各自獨立信封＋ⓘ 略過分流＋⚠ 判準）` 全綠、0 條 `✗`。
- `bash scripts/gates/agent-tools-check.sh` → exit 0，`✓ agent-tools gate 通過（6 份 agent 定義；正文必含字樣 42 條）`（合併帶進 LS-239 對 `.claude/agents/*.md` 的修改，合併後仍綠）。
- 未持有 supabase lock（本輪全程沒有呼叫過 `bash scripts/ops/supabase-lock.sh --hold`，重跑的兩支自測都不碰本機容器）、未開模擬器、未對正式站跑任何東西（同 R1）。

**五、CI（新 head，run 34727585526）**：`lint` success，`rules`／`db`／`ci`／`ci-ipad` 貼此 comment 當下仍 in_progress；PR `mergeable=MERGEABLE`、`mergeStateStatus=BLOCKED`（等 required checks）。R1 那顆 head 的五個 job 已全 success，本次合併未改任何受測內容，預期同綠——**若 `rules` 轉紅請回報，我重看**。

**六、R1 的 6 條 informational（I1–I6，comment `c0165ef8`）原樣有效**，本輪未新增 finding，也未撤回任何一條。

---
`bash scripts/gates/handoff-evidence-check.sh <本 comment 暫存檔> --repo .claude/worktrees/LS-244`：
```
✓ 第 19 行起的列項有證據
✓ 第 20 行起的列項有證據
✓ 第 21 行起的列項有證據
```
exit 0。

### 2026-09-13T00:26:26.299Z · Chih-Lin Yeh · `99f86e6f`

**orchestrator 09-13 08:26**：merge-commit delta 核對 APPROVE 沿用（`ef892a91`，status 貼 34febe9；三 blob 位元相同、§7 三方列齊全）→ 等 CI CLEAN 併入 development（follow script）→ 下一批 promote → QA delta → 併 main 後正式站實跑。

### 2026-09-13T01:08:08.199Z · Chih-Lin Yeh · `8ea1cc94`

**orchestrator 09-13 09:07**：promote dev→test ✓（test `89a0190`＝dev；main 99abb92 ⊂ test）→ QA delta 派出（自測 31＋docker、fixture 四型原文、SQL 純 select、harness-only diff 確認；不碰正式站）。PASS → `promote-main-follow.sh v0.24.0` → 正式站：`supabase functions deploy purge-storage`（LS-242）＋主 checkout 實跑 `prod-storage-verify.sh` 原文貼票 → Done。

### 2026-09-13T01:25:43.522Z · Chih-Lin Yeh · `720b9302`

**qa 09-13 09:21 PASS（test 89a0190 delta）**

worktree `.claude/worktrees/qa-test`，`git fetch origin && git checkout -q --detach origin/test`，`git rev-parse HEAD` = `89a019064651cb98d27ae592261cb1a568f83d1e`，`git status` 乾淨。

**逐條驗收**

1. **harness-only 確認** ✓：`git diff --stat 9cf2245..89a0190 -- LittleSprout LittleSproutTests LittleSproutUITests supabase` 輸出為空——app／DB 內容零改動，9cf2245 的 QA PASS 批次沿用成立。實際變更檔（`git diff --stat 9cf2245..89a0190`，15 檔）：
```
 .claude/agents/ios-dev.md                   |   4 +-
 .claude/agents/merge-reviewer.md            |   2 +-
 .claude/agents/qa.md                        |   2 +-
 .claude/agents/visual-reviewer.md           |   2 +-
 .claude/settings.json                       |  10 +
 .github/workflows/ci.yml                    |   8 +-
 docs/COLLABORATION.md                       |  19 +-
 docs/PLAN.md                                |   2 +-
 scripts/hooks/large-file-read-guard.sh      |  68 +++++
 scripts/hooks/large-file-read-guard.test.sh | 410 +++++++++++++++++++++++++++
 scripts/hooks/large_file_read_guard.py      | 413 ++++++++++++++++++++++++++++
 scripts/ops/patrol.test.sh                  |   3 +
 scripts/ops/prod-storage-verify.sh          | 250 +++++++++++++++++
 scripts/ops/prod-storage-verify.test.sh     | 150 ++++++++++
 scripts/ops/session-start.sh                |   2 +-
 15 files changed, 1331 insertions(+), 14 deletions(-)
```

2. **LS-244**（`f99d8230` ios-dev R1、`c0165ef8` merge-review R1 APPROVE、`ef892a91` merge-commit delta 核對 APPROVE）✓：
- `bash scripts/ops/prod-storage-verify.test.sh` → 31 組全綠，總結 `✓ prod-storage-verify 自測通過（三條檢查各自獨立信封＋ⓘ 略過分流＋⚠ 判準）`（`.claude/evidence/LS-244/qa/prod-storage-verify.test.sh.log`）。
- `--fixture` 三型＋兩欄皆 NULL 型各印一次原文（`.claude/evidence/LS-244/qa/fixture-four-types.log`）：
```
=== 全綠 ===
✓ storage.prefixes：relrowsecurity=True，policies=prefixes_select
✓ storage.objects：抽樣 20 筆（limit=20），owner 已填 20 筆／owner_id 已填 0 筆
✓ storage.buckets：policy 數＝0（PLAN §5-3 預期狀態）
✓ prod-storage-verify 三條檢查皆通過
exit=0

=== RLS 開無 policy ===
⚠ prod-storage-verify 發現異常：
  storage.prefixes 啟用 RLS 但無 policy
⚠ storage.prefixes：RLS 已啟用但沒有任何 policy（PLAN §5-1：上傳會在雲端被擋的形狀）
✓ storage.objects：抽樣 20 筆（limit=20），owner 已填 20 筆／owner_id 已填 0 筆
✓ storage.buckets：policy 數＝0（PLAN §5-3 預期狀態）
exit=1

=== owner／owner_id 兩欄皆 NULL ===
⚠ prod-storage-verify 發現異常：
  storage.objects 抽樣 owner／owner_id 皆為 NULL
✓ storage.prefixes：relrowsecurity=True，policies=prefixes_select
⚠ storage.objects：抽樣 20 筆，owner／owner_id 皆為 NULL（上傳者自刪孤兒物件在雲端不會生效，PLAN §5-2）
✓ storage.buckets：policy 數＝0（PLAN §5-3 預期狀態）
exit=1

=== buckets 有 policy ===
⚠ prod-storage-verify 發現異常：
  storage.buckets 有 2 個 policy（預期 0）
✓ storage.prefixes：relrowsecurity=True，policies=prefixes_select
✓ storage.objects：抽樣 20 筆（limit=20），owner 已填 20 筆／owner_id 已填 0 筆
⚠ storage.buckets：policy 數＝2（PLAN §5-3：預期為 0，有人從 dashboard 加了東西）
exit=1
```
- `bash scripts/gates/agent-tools-check.sh` → exit 0，`✓ agent-tools gate 通過（6 份 agent 定義；正文必含字樣 42 條）`。
- docker ubuntu 24.04 通道：派工原文的 apt-get install 只列 jq，缺 python3（腳本 `scripts/ops/prod-storage-verify.sh` 第 159 行起用 python3 解析信封），照原文跑會 exit 127（`python3: command not found`）；改成 `apt-get install -y -qq jq python3`（與 ios-dev R1 handoff `f99d8230`、merge-review `c0165ef8` 實際驗過的指令一致）後重跑：31 組全綠（jq 1.7、python3 3.12.3）。判定：QA delta 派工文字的疏漏，非 LS-244 程式缺陷。
- 靜態核對：`grep -niE 'insert|update|delete|alter|drop|truncate|create' scripts/ops/prod-storage-verify.sh` 命中兩處——第 12 行是註解、第 106 行是 `order by created_at desc`（`created_at` 子字串命中 `create`，本身在 select 查詢內），非任何 mutation 語句；三段 SQL（`sql_prefixes`／`sql_objects`／`sql_buckets`）通篇只有 select，唯讀成立。
- `docs/COLLABORATION.md` §4-b（line 145）「部署含 Storage 的 migration 後跑 `prod-storage-verify.sh`」句存在；§7（line 348）對照表列存在。`docs/PLAN.md`（line 119）§5 標頭旁註「三條已機械化為 `scripts/ops/prod-storage-verify.sh`，LS-244」存在，三條判準原文未改一字。

3. **LS-239 back-merge 抽驗** ✓（確認 back-merge 沒把 hook 弄壞）：
- `bash scripts/hooks/large-file-read-guard.test.sh` → 64 組全綠（`.claude/evidence/LS-244/qa/large-file-read-guard.test.sh.log`），0 條 `✗`。
- `bash scripts/ops/patrol.test.sh` 在 test tip 跑了 3 次：第 1 次出現 1 條 `✗ ㉗ --json：flags 含一筆 [screen-lock]` 斷言失敗，第 2、3 次皆全綠（366 組／0 `✗`，`.claude/evidence/LS-244/qa/patrol.test.sh.log` 為第 3 次的乾淨結果）。查證：`git diff 9cf2245..89a0190 -- scripts/ops/patrol.test.sh` 只動了 ⑥ SessionStart hook 的 cron 提示斷言（LS-239 範圍 3），完全沒碰 ㉗ 螢幕鎖定段；`scripts/ops/patrol.sh` 本身在這兩顆 commit 之間毫無變動（不在 diff --stat 清單內）；另在乾淨 clone 的 9cf2245 上跑同一支測試也是一次就綠。判定：既有測試本身的間歇性 flake，非 LS-239 back-merge 引入的回歸，不影響本次裁決。

**結論**：LS-244 三條檢查機械化正確、fixture 四型原文與判準逐字對得上票文與 PLAN §5；LS-239 back-merge 的兩支自測（扣掉上述已排除的既有 flake）在 test tip 皆綠。PASS。

---
`bash scripts/gates/handoff-evidence-check.sh <本 comment 暫存檔>`：
```
✓ 第 7 行起的列項有證據
✓ 第 27 行起的列項有證據
✓ 第 67 行起的列項有證據
```
exit 0。

### 2026-09-13T01:36:53.321Z · Chih-Lin Yeh · `67e755ca`

**orchestrator 09-13 09:37 QA PASS**（`720b9302`；harness-only diff 確認、31 組＋docker、fixture 四型原文、SQL 純 select；qa status 貼 89a0190）→ `promote-main-follow.sh v0.24.0` 跑中（等 test 分支 CI 對 89a0190 轉綠）→ 併 main 後主 checkout 實跑 `prod-storage-verify.sh` 原文貼票 → sweeper → 收尾 → Done。

### 2026-09-13T01:50:09.325Z · Chih-Lin Yeh · `c8cadf91`

**orchestrator 09-13 09:49 正式站實跑 ✓**（v0.24.0 `89a0190` 併 main 後，主 checkout `bash scripts/ops/prod-storage-verify.sh`）原文：
```
ⓘ storage.prefixes：表不存在，略過（本機開發映像通常沒有這張表，PLAN §5-1）
✓ storage.objects：抽樣 12 筆（limit=20），owner 已填 12 筆／owner_id 已填 12 筆
✓ storage.buckets：policy 數＝0（PLAN §5-3 預期狀態）
✓ prod-storage-verify 三條檢查皆通過
```
exit 0。備註：正式站 `storage.prefixes` 表不存在 → §5-1 走 ⓘ 略過（storage-api 版本尚未引入該表；日後升級後本腳本會自動改走 RLS／policy 檢查）；ⓘ 訊息「本機開發映像通常沒有」在正式站語境略不精確，記 LS-96 文案項。→ sweeper（已派）→ 收尾 → Done。

### 2026-09-13T01:51:52.816Z · Chih-Lin Yeh · `b7983d60`

## dead-code sweep — LS-244

**dead-code-sweeper 09-13 09:49**

範圍：`git diff 9cf2245..89a0190 -- scripts/ops/prod-storage-verify.sh scripts/ops/prod-storage-verify.test.sh .github/workflows/ci.yml docs/COLLABORATION.md docs/PLAN.md`（主 checkout HEAD `89a0190`，即 v0.24.0 併 main 後狀態）。只讀，未改任何檔案。

### Findings：未發現本 feature 引入的死碼

逐項對照 orchestrator 巡查清單：

1. **三條檢查各自 helper 是否都有呼叫**（活）
   `sql_prefixes`／`sql_objects`／`sql_buckets`（`scripts/ops/prod-storage-verify.sh:82,95,112`）分別在 `fetch_raw(prefixes, ...)`／`fetch_raw(objects, ...)`／`fetch_raw(buckets, ...)`（`:150-152`）各被呼叫一次；三段 payload（`prefixes`／`objects`／`buckets`）各自在 python 段的 `extract_payload()`（`:167-186` 起）被解析、再分別餵進三段判準邏輯（`line1`/`line2`/`line3`，各自 append 到 `problems`）。無孤兒 helper。

2. **`--fixture` 分支與真連線分支是否有只在一邊用的死 helper**（活，無死 helper）
   兩分支共用同一個 `fetch_raw()`（`:123-146`）：`FIXTURE` 有值走 `cat "$f"`（`:127-131`），否則走 `supabase db query --linked -f "$sql_file"`（`:139`）。沒有另外拆出「只給 fixture 用」或「只給真連線用」的獨立函式；唯一分支內邏輯就是 if/else 兩段，皆被 CI 自測（fixture 側 31 組）與 R1 merge-reviewer 另開的拋棄式 Postgres 容器（真連線側三段 SQL）覆蓋過（見 `c0165ef8` 第三節）。

3. **fixture 檔（四型）是否每支被自測引用**（活；無獨立檔案殘留）
   本票的 fixture 不是 checked-in 靜態檔，而是 `mkfixture()`／`envelope()`（`scripts/ops/prod-storage-verify.test.sh:37-53`）在 `mktemp -d` 出的暫存目錄動態產生（①全綠、②RLS 開無 policy、③owner/owner_id 皆 NULL、④buckets 有 policy，另加⑤⑥⑦⑮⑯共 9 組 fixture 目錄），跑完由 `trap 'rm -rf "$work"' EXIT` 清掉，repo 裡沒有殘留的 fixture 檔案需要巡（`find . -iname '*storage-verify*'` 只命中腳本本身與其他 worktree 的同名複本，無孤兒 fixture 檔）。`envelope()`／`mkfixture()` 兩個 helper 全被使用（`grep -c` 確認每個 mkfixture 呼叫都對應到後面至少一個 `expect`/`has`/`hasnt` 斷言）。

4. **31 組斷言有無恆真**（活；逐條核對非重複、非恆真）
   手動逐條清點 `expect`/`has`/`hasnt` 呼叫共 31 次，與 handoff／QA 申報的「31 組」一致。每條 `must` 字串／`hasnt` 目標字串各自對應唯一情境（如 ⑨/⑩ 都斷言「必須是正整數」但輸入分別是 `abc`／`0`，是同一錯誤訊息的兩個不同觸發路徑，非重複斷言）；`hasnt '① 不印 ⚠'`、`hasnt '⑤/⑥ 不印 ⚠'` 屬負向斷言，會在程式誤把 ⓘ 略過路徑也印出 ⚠ 時抓到，非恆真裝飾。未發現「不管輸入什麼都會過」的斷言。

5. **R1 版與 merge 後版之間有無殘留**
   `git diff e75fc7f..34febe9 -- scripts/ops/prod-storage-verify.sh scripts/ops/prod-storage-verify.test.sh` 為空；blob hash 逐一核對（`d2b1188`／`b04527f`）在兩顆 commit 上完全相同——**這兩個檔案本身零殘留**。
   但**如實回報一個與 orchestrator 原始指令字面不符之處**：orchestrator 給的指令是 `git diff e75fc7f..34febe9 -- scripts/ops`（整個目錄，非只兩個檔案），這個範圍**不是空的**——多出 `scripts/ops/patrol.test.sh`／`scripts/ops/session-start.sh` 兩處變更。查證後這是 LS-239（already merged into development）的內容，透過 `34febe9`＝`e75fc7f` merge `development`（`d9092b9`）帶進來的，不是 LS-244 自己新增或殘留的東西；merge-reviewer 在 `ef892a91` 已對此做過逐行核對（LS-244 淨差＝5 檔、406+2−，與 R1 diffstat 完全相同）。判定：非本票死碼，是巡查指令的目錄粒度比實際文件粒度粗所致，特此註記避免誤讀成「有殘留」。

6. **`ci.yml` 單次註冊**（活，各一次）
   `grep -n prod-storage-verify .github/workflows/ci.yml` 只有兩處：`:312` 步驟名稱列表（含其餘 12 支既有腳本名）與 `:334-335` 呼叫行本身，同一個 step（`rules` job 的「Ops 腳本自測」）。未見重複註冊或掛在錯的 job。

7. **PLAN §5 旁註只加一句、三條原文未動**（確認）
   `git diff 9cf2245..89a0190 -- docs/PLAN.md` 只有一行變更（`docs/PLAN.md:119` 標頭），變更內容是在既有括號註記後面加「；三條已機械化為 `scripts/ops/prod-storage-verify.sh`，LS-244」；下方三條編號判準原文（L120-122 起，`storage.prefixes`／`owner`／`owner_id`／`storage.buckets`）在 diff 裡完全沒有 `+`/`-` 標記，逐字未動。

8. **COLLABORATION §4-b 那句與 §7 列指向的腳本名正確**（確認）
   §4-b（`docs/COLLABORATION.md:145`）與 §7 表尾新增列（`:348`）都寫 `` `scripts/ops/prod-storage-verify.sh` ``／`` `prod-storage-verify.test.sh` ``，與實際檔名一致；§4-b 只新增這一行（`git diff` 顯示 §4-b 段落只有 `+` 這一行，前後既有內容不受影響），§7 只新增這一列（表尾，前面 LS-235／LS-236／LS-239 三列皆保留原樣）。

另外順手核對票文額外提到的兩項：
- **TODO／debug print／註解掉的程式碼／假旗標**：`grep -niE 'TODO|FIXME|XXX|console\.log|# debug'` 兩檔零命中（唯一命中是 `mktemp` 模板字串 `LS-244-storage-verify-*.XXXXXX`，屬正常暫存檔命名慣例非殘留標記）。
- **`set -uo pipefail`（無 `-e`）**：與同源 `prod-purge-health.sh`／`prod-purge-health.test.sh` 一致（`grep -n "^set "` 四檔皆同），是既有慣例延續，非本票引入的異常。

### 既有死碼觀察

無。本次巡檢範圍限定於 LS-244 引入的 5 個檔案的變更片段，未擴大掃描全庫既有死碼；票文列的 reviewer informational（I1–I6）已由 orchestrator 裁定（I3 接受、I1/I2/I4/I5 記入 LS-96），與死碼無關，不重複列出。

### 建議處置

不開票。本票未發現需要處置的死碼；收尾儀式可續行下一步（lesson learning review／cleanup-merged／視需要的 Pen 清場——本票非設計票，④ 不適用）。

### 搜尋方法與盲區聲明

方法：`git diff`／`git rev-parse`（blob 比對）／`grep -n`／`grep -c` 交叉核對呼叫點與宣告點；人工逐條清點 `expect`/`has`/`hasnt` 呼叫與其斷言字串的唯一性；手動執行 `find` 確認無孤兒 fixture 檔。

盲區聲明：
- 純文字搜尋抓不到動態組出的 SQL／變數間接引用（如把某個 payload 鍵名拼接後才查）——本票三段 SQL 皆是靜態 heredoc，未見此類手法，但這是工具性盲區而非本次已證明排除。
- 未對正式站實跑腳本驗證其在真實資料上的行為（該步驟是票文 QA delta／orchestrator 併 main 後的職責，非死碼巡檢範圍）。
- 未檢查 `.claude/worktrees/*` 底下殘留的同名檔案是否該由 `cleanup-merged.sh` 清掉（那是收尾儀式的清理步驟，非死碼巡檢；`find` 結果顯示 `qa-test`／`LS-243`／`LS-244` 三個 worktree 仍有複本，留給該步驟處理）。

### 2026-09-13T01:52:43.716Z · Chih-Lin Yeh · `01a76613`

**orchestrator 09-13 09:52 收尾儀式 → Done**

- ① 死碼巡檢 ✓（sweeper `b7983d60`）：三支 SQL helper 各一呼叫點、fixture 動態產生無殘檔、31 組斷言無恆真、`ci.yml` 單次註冊、PLAN／COLLABORATION 各一行。
- ② Lesson learning review：
  - harness：**PLAN 裡「本機測不到」的部署檢查清單要在寫下當天就腳本化**——§5 三條寫了兩週靠人記得；本票模式（唯讀 SQL＋fixture 自測＋併 main 後 orchestrator 實跑貼原文）可複用到 §10-A(2) 用量告警等 cycle 3 候選。
  - 設定：ops 自測若用 python3，docker 通道 apt 清單要含 python3（已寫進 dispatch 規則）。
  - 工具缺口：`handoff-evidence-check` 白名單缺 `gh pr view`／`gh pr checks`（LS-96 `53c1d098`）；I1／I2／I4／I5＋ⓘ 文案（LS-96）同批清。
- ③ 清理 ✓：`cleanup-merged.sh --apply LS-244` → worktree 1 已移除。
- ④ Pen 清場：不適用。

QA PASS（`720b9302`）、正式站實跑三條通過（09:49）；隨 v0.24.0 已上 main。

