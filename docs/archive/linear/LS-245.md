# LS-245 iOS chore：小債清倉 4（無需設計稿）——`InteractionRow` iPad 窄寬留言計數截字（池 `5824399c`）、詳情頁三個 sheet 來源收斂為單一 `sheet(item:)`（`7f77856c`）、`SettingsViewIPadTests` 第三支舊同步寫法（`2c4bfc80`）、`StubCommentAPIClient.setSetCommentDeletedHandler` 無呼叫（`38a3c74b`）、TapTargetGate 兩處 stale 註解

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:S, lane:ui |
| Cycle | 2 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M5 互動與推播 |
| 估點 | — |
| 建立 | 2026-09-12T23:46:55.071Z |
| 開始 | 2026-09-12T23:47:54.700Z |
| 完成 | 2026-09-13T05:20:59.705Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-245/ios-chore小債清倉-4無需設計稿-interactionrow-ipad-窄寬留言計數截字池-5824399c詳情頁三個-sheet |
| 關係 | related→LS-158, related→LS-205, related→LS-209, related→LS-190, related→LS-23, related→LS-96, related→LS-237, related→LS-22, related→LS-218, related→LS-20, related→LS-241, related→LS-24, related←LS-261, related←LS-253, related←LS-246, related←LS-238 |

## 描述

## 來源

ui lane 09-13 07:45 空位補位（[LS-237](https://linear.app/little-sprout-app/issue/LS-237/ios-chore小債清倉-3無需設計稿-上傳批次-sortorder-同號相簿詳情離開再進不刷新池)／[LS-241](https://linear.app/little-sprout-app/issue/LS-241/taskls-22-ios-日記詳情頁留言區改接互動列留言-sheet移除-ls-126-占位留言功能即將推出) 進 QA；候補 [LS-20](https://linear.app/little-sprout-app/issue/LS-20/story相簿與照片上傳壓縮縮圖背景續傳)／[LS-24](https://linear.app/little-sprout-app/issue/LS-24/storyapp-內帳號刪除多-owner-轉移) 無核可稿、[LS-22](https://linear.app/little-sprout-app/issue/LS-22/story留言愛心與彙總推播)／[LS-23](https://linear.app/little-sprout-app/issue/LS-23/story檢舉封鎖額度-uieula) blockedBy）。沿 [LS-237](https://linear.app/little-sprout-app/issue/LS-237/ios-chore小債清倉-3無需設計稿-上傳批次-sortorder-同號相簿詳情離開再進不刷新池) 清倉模式，自 [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池取今日 review／sweeper 查實、**不需設計稿**的 ui 程式面債務：

1. `5824399c`（[LS-241](https://linear.app/little-sprout-app/issue/LS-241/taskls-22-ios-日記詳情頁留言區改接互動列留言-sheet移除-ls-126-占位留言功能即將推出) R1）：`InteractionRow` 留言計數文字在 iPad 窄寬（詳情頁置中卡片、split view 窄欄）被截字 → 計數改 `fixedSize`／`layoutPriority` 或在窄寬降級為只顯示圖示＋計數（不改視覺 token）；`tap-target-check` 熱區不變。
2. `7f77856c`（[LS-241](https://linear.app/little-sprout-app/issue/LS-241/taskls-22-ios-日記詳情頁留言區改接互動列留言-sheet移除-ls-126-占位留言功能即將推出) R1 m1，PLAUSIBLE）：`DiaryDetailView` 三個 sheet 呈現來源（留言 sheet／`contentActionsContext`／其他）與「⋯」非同步 `fetchContentAuthor` 窗口重疊時第二個呈現被系統丟棄、`contentActionsContext` 卡住 → 收斂為單一 `sheet(item:)` enum 來源；單元測試：連續觸發兩來源仍能各自呈現。
3. `2c4bfc80`（[LS-241](https://linear.app/little-sprout-app/issue/LS-241/taskls-22-ios-日記詳情頁留言區改接互動列留言-sheet移除-ls-126-占位留言功能即將推出) R2 reviewer）：`SettingsViewIPadTests.testContentSafetySectionStorageRowPushesAndBackReturns` 仍用舊同步寫法（[LS-237](https://linear.app/little-sprout-app/issue/LS-237/ios-chore小債清倉-3無需設計稿-上傳批次-sortorder-同號相簿詳情離開再進不刷新池) `96734b7` 只補了同檔另兩支）→ 同一套 `waitForExistence` 同步點；ci-ipad 三次紅的直接來源。
4. `38a3c74b`（[LS-218](https://linear.app/little-sprout-app/issue/LS-218/taskls-22-ios-留言-sheet清單輸入列空狀態錯誤態-網路ls026骨架載入owner-移除操作表既有二次確認ipad) sweeper）：`LittleSproutTests/Support/StubCommentAPIClient.swift` `setSetCommentDeletedHandler`＋backing 欄位＋`setCommentDeleted` 無呼叫 → 補一支「Owner 移除留言走 API client」測試用上它，或刪；同筆：`TapTargetGateHarness+Content.swift:33-34`、`TapTargetGateScreenName.swift:126-127` 註解仍寫 `CommentDeleteConfirmationSheet`「無真實入口」→ 改現況。

## 不做

改設計稿／新畫面；[LS-237](https://linear.app/little-sprout-app/issue/LS-237/ios-chore小債清倉-3無需設計稿-上傳批次-sortorder-同號相簿詳情離開再進不刷新池) R2 reviewer n3 的永久測試（另列）；任何後端。

## 驗收

- [ ] 每項附單元／UITest＋mutation；iPad mini＋13" 側欄詳情頁截圖計數完整；`tap-target-check`／`ax-launch-check` 綠；merge-reviewer 通過（race：項 2 兩來源競態；scope：只動列出檔）
- [ ] QA：iPad 兩張截圖＋ `SettingsViewIPadTests` 連跑 3 次綠

## 留言（11）

### 2026-09-12T23:48:00.850Z · Chih-Lin Yeh · `b9d2ee2a`

**orchestrator 09-13 07:46**：ui lane 空位補位（LS-237／LS-241 進 QA；LS-20／LS-24 無核可稿、LS-22／LS-23 blockedBy）。四項皆今日 review／sweeper 查實、不需設計稿。worktree `.claude/worktrees/LS-245`／`feature/LS-245-ui-debt-4` 自 development `9cf2245` 建；ios-dev R1 派出（qa 正對同 tip 跑真後端，lock 排隊）。

### 2026-09-13T01:42:35.940Z · Chih-Lin Yeh · `c851b292`

**ios-dev 09-13 09:41 R1**

**Ticket**：LS-245 iOS chore：小債清倉 4（無需設計稿）——4 個池項清倉

**已完成**：
1. `5824399c`（LS-241 R1）：`InteractionRow` iPad 窄寬留言計數截字——`98bf083`。非 AX3 態改用 `ViewThatFits` 在完整版與只留圖示＋計數的 `commentButtonCompact` 之間自動挑選（真實文字量測，不猜像素常數）；`commentButtonCompact` padding／minHeight／contentShape／accessibility identifier／label 與完整版一致；不改視覺 token。
2. `7f77856c`（LS-241 R1 m1，PLAUSIBLE）：`DiaryDetailView` 留言 sheet／「⋯」內容操作表收斂為單一 `sheet(item:)`——`f00e9a4`。新增 `DiaryDetailSheet` enum（`.comments`／`.contentActions(context)`）取代原本各自獨立的 `showsCommentsSheet`（Bool）／`contentActionsContext`（item）；兩個 case 的 `id` 落在不同命名空間，即使底層 `context.id` 剛好等於 `diaryID` 也不會相撞（原 bug「同 id 卡住不重新呈現」的根因之一）。內容操作表 05 之後的確認鏈（檢舉／封鎖／Owner 移除／刪除）維持各自獨立 `@State`——LS-190 R2 既有規約保證依序接力，不在收斂範圍。**範圍決定**：不含 `.fullScreenCover(item: $playingVideo)`（不同呈現風格，併入會改變影片播放行為，非本票要求）。
3. `2c4bfc80`（merge-review LS-241 R2）：`SettingsViewIPadTests.testContentSafetySectionStorageRowPushesAndBackReturns` 同步點——`fca6ffe`。補上與同檔另兩支測試（LS-237 `96734b7`）相同的 `waitForHittable` 同步點。
4. `38a3c74b`（sweeper LS-218）：`StubCommentAPIClient.setSetCommentDeletedHandler` 補測試用上、兩處 stale 註解訂正——`b63f2f8`。抽出 `CommentDeleteConfirmationSheet.performDelete()`（同 `CommentsSheetView.syncCommentCountIfKnown()` 既有慣例）讓「Owner 移除留言走 API client」可單元測試；`TapTargetGateHarness+Content.swift`／`TapTargetGateScreenName.swift` 兩處「無真實入口」訂正為現況（真實入口是 LS-218 `CommentsSheetView+Actions.swift`）。

**已驗證（怎麼驗）**：
1. 對應驗收「每項附單元／UITest」——全量單元測試 `xcodebuild test -only-testing:LittleSproutTests`：1008 支全綠（`LS-245-iPhone17Pro` 7FC0E099，iOS 26.0——本機無 26.2 釘住版，push gate 印同一則警告，CI 為 26.2）。
2. 相關 UITest 全量：`InteractionRowUITests`／`DiaryDetailCommentsUITests`／`ContentActionsUITests`／`CommentsSheetUITests`／`DeleteConfirmationUITests` 共 33 支全綠。
3. 對應驗收「mutation」——項 2 `DiaryDetailSheetIDTests.test_diaryDetailSheetID_idsAreStableAndDistinctAcrossRepeatedConstruction`：mutation（`.contentActions` 的 `id` 拿掉 `"contentActions-"` 前綴）→ 紅 → `XCTAssertTrue failed - 應該帶命名空間前綴，不能只是裸的 UUID 字串`。
4. 項 2 `DiaryDetailCommentsUITests.testCommentButton_thenMoreButton_bothPresentCorrectly`：mutation（`onOpenComments` 改 no-op）→ 紅 → `XCTAssertTrue failed - 應該先呈現留言 sheet`。
5. 項 2 `ContentActionsUITests.testDiaryDetail_moreButton_thenCommentButton_bothPresentCorrectly`：mutation（`activeSheet = .contentActions(...)` 改成丟棄結果）→ 紅 → `XCTAssertTrue failed - 應該先呈現內容操作表`。
6. 項 4 `CommentDeleteConfirmationSheetTests.test_performDelete_callsSetCommentDeletedWithCommentIDAndDeletedTrueOnAPIClient`：mutation（`deleted` 改 `false`）→ 紅 → `XCTAssertEqual failed: ...RecordedCall(..., deleted: false)... is not equal to ...RecordedCall(..., deleted: true) - Owner 移除留言應該原樣把 commentID 與 deleted: true 轉呼叫 CommentAPIClient.setCommentDeleted`。
7. 對應驗收「iPad mini＋13" 側欄詳情頁截圖計數完整」——`LS-245-iPadMini2`（CB73A6F3）／`LS-245-iPadPro13`（55E8D0E4）用 `SIMCTL_CHILD_LS_TAP_TARGET_GATE_SCREEN=DiaryDetailView xcrun simctl launch` 注入＋`xcrun simctl io screenshot`：`.claude/evidence/LS-245/diary-detail-ipadmini-interactionrow.png`／`diary-detail-ipad13-interactionrow.png`，「愛心 0」「留言 0」皆完整不截字。
8. 對應驗收「`SettingsViewIPadTests` 連跑 3 次綠」——`LS-245-iPadPro13`（55E8D0E4）連跑 3 次，每次 6 支、0 failures（79.4s／77.8s／79.6s）。
9. 對應驗收「`tap-target-check`／`ax-launch-check` 綠」——`tap-target-check.sh 7FC0E099 LittleSprout` exit 0（含 `DiaryDetailView`／`CommentsSheetView`）；`ax-launch-check.sh` exit 0（0 違規、1 項允許期限內）。
10. `swiftlint lint --quiet` 全庫乾淨；push gate 全綠，已 push `f00e9a4`。

**未完成（處置，reviewer informational 對應項一條不省）**：
- 項 2 真正的非同步窗口重疊競態（留言 sheet 與「⋯」`fetchContentAuthor` 同時在飛）：**不修＋理由**——LS-241 reviewer 本人標記 PLAUSIBLE、未實跑（需注入延遲才能穩定重現）。單一 `activeSheet` 從型別層級消除「兩個獨立 `@State` 同時各自持有呈現意圖」這個結構性成因，但沒有新增可控延遲測試通道證實窗口本身；這類延遲注入通道屬新測試基礎設施，超出本張清倉票範圍。
- 項 2 `.fullScreenCover(item: $playingVideo)` 與 `activeSheet` 之間的殘留同型風險：**不修＋理由**——票文範圍只收斂兩個用 `.sheet` 呈現的來源，fullScreenCover 是不同呈現風格，強行併入會改變影片播放行為（scope creep）。
- 項 1 `commentButtonCompact` 目前沒有自動化測試真正觸發「容器窄到需要降級」分支（現有畫面在 M1 修復後皆已足夠寬）：**不修＋理由**——純屬元件層防禦性加固，要建專用窄寬 harness 對 size:XS chore 不成比例，已用截圖＋既有 UITest 全綠＋`ViewThatFits` 為官方機制（非自寫邏輯）佐證。
- 本機無「iPad Air 11-inch (M3)」（ci-ipad 同機型）：項 3 連跑改用 `LS-245-iPadPro13`；push gate 的本機 iPad 回歸段本身也 fail-open 印同一則警告，CI `ci-ipad` job 為正式把關。

**風險**：
- 本機模擬器 runtime 為 iOS 26.0，CI 釘住 26.2（`detect-simulator.sh` fail-open）——量測結果以 CI（26.2）為準。
- 未持有 supabase-lock：本票四項驗證皆用 offline preview client（`PreviewCommentAPIClient`／`PreviewSafetyAPIClient`）與 harness screen，未涉及本機容器，判斷不需要真後端互動即可完整驗證（bug 本身是 SwiftUI 呈現狀態機問題，非後端資料問題）。
- 過程中誤建一台重複命名的 `LS-245-iPhone17Pro`（`detect-simulator.sh` 已自動建好一台，我沒注意到又手動 `simctl create` 一台）；已刪除未使用的那台，收工只留 3 台且皆已關閉。

**產出位置**：
- PR #384：https://github.com/CLYEH/little-sprout/pull/384（`feature/LS-245-ui-debt-4` → `development`）
- Head SHA：`f00e9a47f2f9d08cf75e9c65e69f4255e115e07c`
- Commits：`98bf083`（fix 項1）／`fca6ffe`（test 項3）／`b63f2f8`（chore 項4）／`f00e9a4`（fix 項2）
- 證據：`.claude/evidence/LS-245/`（2 張截圖，worktree 內，gitignored）
- 模擬器已關：`7FC0E099-39DA-4FEB-9265-D5D1C981D4C9`（LS-245-iPhone17Pro）、`55E8D0E4-3E46-4E6B-BCC4-F50B56C9CA7F`（LS-245-iPadPro13）、`CB73A6F3-5A7B-4C0A-BD1E-407A163657C8`（LS-245-iPadMini2）

### 2026-09-13T01:50:05.352Z · Chih-Lin Yeh · `a8be8f86`

**orchestrator 09-13 09:49**：R1 handoff 收到（PR #384，head `f00e9a4`，4 commit；1008 unit＋33 UI 綠、iPad 兩張截圖、`SettingsViewIPadTests` 3× 綠、mutation 項 2／4）→ In Review、派 merge-review R1（重點：單一 sheet 來源與 `fetchContentAuthor` 完成時序、影片 `fullScreenCover` 互斥、`ViewThatFits` 列表成本、`commentButtonCompact` 無觸發畫面是否算死碼）。

### 2026-09-13T02:40:53.796Z · Chih-Lin Yeh · `0795545f`

**merge-reviewer 09-13 10:39 R1 APPROVE**

**PR #384**｜head `f00e9a47f2f9d08cf75e9c65e69f4255e115e07c`｜base `9cf2245`（`git diff 9cf2245...f00e9a4`：12 檔 +338/−49）｜CI 五項全綠（rules／db／lint／ci／ci-ipad，綁同一顆 head）。
模擬器：`LS-245-iPhone17Pro` 7FC0E099 iOS 26.0.1／`LS-245-iPadPro13` 55E8D0E4 iOS 26.0.1（pinned `.ios-runtime`＝26.2 本機未安裝，`scripts/gates/detect-simulator.sh` 印同一則警告）——版面／tap-target 量測以 CI 26.2 為準（LS-205）。supabase-lock：未持有（本票不涉本機容器）。收工時 worktree pristine（`git status --porcelain` 空、HEAD 仍 f00e9a4），無 Booted 模擬器殘留。

## 四維度

**1. race condition — 無 blocker／major（實跑重現，非推論）**

項 2 的非同步窗口重疊我用延遲注入重現了一次：暫時把 `LittleSprout/Support/PreviewSafetyAPIClient.swift` 的 `fetchContentAuthor` 改成先 `Task.sleep` 2.5 秒，加一支臨時 UITest（點「⋯」→ 立刻點留言鈕 → 等 6 秒觀察 → 往下拖關掉 → 再點一次「⋯」），同一支探針分別對 head 與 base 的 `DiaryDetailView` 兩檔各跑一次，跑完 `git checkout --` 還原。

| 觀察點 | 修前（base `9cf2245` 的兩檔） | 修後（head） |
|---|---|---|
| t1 留言 sheet 有開 | true | true |
| t2 查作者回來後 | comments=false、actions=true | comments=false、actions=true |
| t3 拖曳關閉後 | **comments=true**（留言 sheet 幽靈復現）、更多操作不可點 | comments=false、actions=false、更多操作可點 |
| t4 再點「⋯」 | **false（開不出來）** | true |

結論：LS-241 R1 m1 標 PLAUSIBLE 的窗口是真的，但形狀與原推測有出入——不是「第二個呈現被系統丟棄、`contentActionsContext` 卡住」，而是「`showsCommentsSheet` 這個獨立 Bool 在系統把留言 sheet 換成操作表時沒被清掉，關掉操作表後留言 sheet 幽靈復現並蓋住『⋯』，直到退出重進」。單一 `activeSheet` 把整條消掉：`.sheet(item:)` 在 iOS 26.0.1 對「呈現中換 id」的行為是 dismiss＋re-present（不是丟棄、也不是原地換內容），關閉後狀態歸零、可重複觸發，t3／t4 兩欄就是證據。
殘留（不擋、非本 PR 引入）：留言 sheet 被晚到的操作表換掉時使用者已輸入的草稿會沒，修前修後同樣。
確認鏈（檢舉／封鎖／Owner 移除／刪除）維持各自 `@State` 的判斷成立：`ContentActionsSheet` 依 LS-190 R2 規約先 dismiss 再回呼，`activeSheet` 於 dismiss 時被 binding 寫回 nil，後續 sheet 才設值，`ContentActionsUITests` 既有 24 支全綠佐證整條分流沒壞。

**2. 運算效能 — 無 finding**

`ViewThatFits` 對時間軸每張卡片多量一個候選，候選內容是固定寬熱區（118／48）＋2–3 個短 `Text`，相對卡片本身（縮圖、masonry）可忽略；選擇不回饋影響 proposal（proposal 由外層 `.frame(maxWidth: .infinity)` 決定），不會抖動或反覆重排。本 diff 不碰資料層，無 RLS per-row／N+1／OFFSET／主執行緒解碼類問題。
實測數字（臨時 probe UITest 用 `TapTargetMeasurement.launch(_:contentSizeCategory:)` 三種字級各量一次 `commentButton.frame`，跑完還原）：iPhone 17 Pro 標準字級 101.0x47.0、AX2 135.3x59.0、AX3 160.3x80.0，三者都仍是完整版（AX3 走既有 VStack 分支，不經 `ViewThatFits`，題目問的 AX3 抖動不成立）。反推完整版橫向需求約 118+8+48+24+101≈299pt，iPhone 最窄（375−2x24＝327pt）放得下，故 iPhone 不會降級；降級真正會發生在 iPad 窄窗（見 i6）。

**3. 平行優化 — 無 finding**

新增三支測試的同步點都是真等條件：`waitForExistence`／`waitForHittable`／`XCTNSPredicateExpectation(exists == false)`，沒有固定 sleep；`InteractionRow`／`DiaryDetailView` 沒有可平行卻被串行化的工作。

**4. scope — 乾淨**

App 端只動 `LittleSprout/Features/Timeline/InteractionRow.swift`、`DiaryDetailView.swift`、`DiaryDetailView+ContentActions.swift`、`LittleSprout/Features/Content/CommentDeleteConfirmationSheet.swift`、`LittleSprout/TapTargetGateHarness+Content.swift`、`LittleSprout/TapTargetGateScreenName.swift`；測試端 4 檔＋pbxproj 只有兩筆新檔登記。`handleContentAction`／`activeSheet` 去掉 private 是跨檔 extension 的必要最小改動，沒有順手重構、沒有未被要求的功能。`performDelete()` 只是把原本 inline 的 `confirmAction` 閉包抽成具名方法，刪除流程語意未變（`confirmTapped` 仍是 await confirmAction → dismiss → onSuccess；`CommentsSheetView+Actions.swift` 的 `.removeCommentAsOwner`／`.deleteOwn` 兩條真實入口都指向同一張 sheet，測試名稱與現況相符）。

## 已驗證（怎麼驗——以下全是我自己實跑，不採信 handoff 申報）

- 基線單元：`xcodebuild test -only-testing:LittleSproutTests/DiaryDetailSheetIDTests -only-testing:LittleSproutTests/CommentDeleteConfirmationSheetTests` → 3 passed／0 failed。
- 基線 UITest：`xcodebuild test -only-testing:LittleSproutUITests/InteractionRowUITests -only-testing:LittleSproutUITests/DiaryDetailCommentsUITests -only-testing:LittleSproutUITests/ContentActionsUITests` → 24 passed／0 failed（`.xcresult` summary result=Passed）。
- iPad：`xcodebuild test -only-testing:LittleSproutUITests/SettingsViewIPadTests` 於 `LS-245-iPadPro13` → 6 passed／0 failed（我只跑 1 次；handoff 申報的連跑 3 次留給 QA 驗收條款）。
- mutation 重放 4／4 對得上，且都是預期的斷言失敗（非 crash／build fail，LS-209）——失敗訊息逐條取自 `.xcresult`：
  1. `DiaryDetailSheet.id` 拿掉 contentActions- 前綴 → `DiaryDetailSheetIDTests.test_diaryDetailSheetID_idsAreStableAndDistinctAcrossRepeatedConstruction` 紅 → `XCTAssertTrue failed - 應該帶命名空間前綴，不能只是裸的 UUID 字串`。
  2. `performDelete` 的 deleted 改成 false → `CommentDeleteConfirmationSheetTests.test_performDelete_callsSetCommentDeletedWithCommentIDAndDeletedTrueOnAPIClient` 紅 → `XCTAssertEqual failed: ...deleted: false... is not equal to ...deleted: true...`。
  3. `onOpenComments` 改 no-op → `DiaryDetailCommentsUITests.testCommentButton_thenMoreButton_bothPresentCorrectly` 紅 → `XCTAssertTrue failed - 應該先呈現留言 sheet`。
  4. `activeSheet = .contentActions(...)` 改成丟棄結果 → `ContentActionsUITests.testDiaryDetail_moreButton_thenCommentButton_bothPresentCorrectly` 紅 → `XCTAssertTrue failed - 應該先呈現內容操作表`（同一支另連帶記一條「取消鈕找不到」，屬同一 mutation 的連鎖，非獨立訊號）。
- gate：`bash scripts/gates/tap-target-check.sh 7FC0E099-39DA-4FEB-9265-D5D1C981D4C9 LittleSprout` exit 0（涵蓋 `DiaryDetailView`／`CommentsSheetView`）；`bash scripts/gates/ax-launch-check.sh` exit 0（0 違規、1 項允許期限內）。
- 截圖：`.claude/evidence/LS-245/diary-detail-ipadmini-interactionrow.png` 與 `.claude/evidence/LS-245/diary-detail-ipad13-interactionrow.png` 兩張都看過，愛心 0／留言 0 完整不截字（見 i6 的但書）。
- handoff 逐項對應：已完成 1／2／3／4 全部核對到 diff 對應 commit；已驗證第 3／4／5／6 項（四組 mutation）如上逐條重放；第 1 項（1008 支全量單元）與第 2 項（33 支相關 UITest）我只跑了其中 3＋24 支，其餘以本 head 的 CI ci／ci-ipad job 全綠採信；第 7 項截圖已看；第 8 項（iPad 連跑 3 次）只跑 1 次；第 9 項兩支 gate 已自跑；第 10 項 swiftlint／push gate 未自跑，以 CI lint job 全綠採信。

**沒看／沒跑的範圍（明講）**：`CommentsSheetUITests`／`DeleteConfirmationUITests` 未在本機重跑（CI 涵蓋）；真後端（Supabase）完全未觸及，本票四項都在 preview client／harness 層，判斷不需要真後端；iPad Slide Over 窄窗未實機驗證（見 i6）。

## informational（全列）

- **i1（minor，建議記 LS-96 池）**：`LittleSproutUITests/SettingsViewIPadTests.swift` 同檔另外四處 sidebar 切換仍是裸 `.tap()`（L101 家庭、L158 帳號、L185 法律、L239 家庭），與本次補同步點的第三支是同一個 flake 形狀（sidebar 轉場未穩定就 tap）。票文只點名一支，不擋本 PR；下次清倉一併補齊較一致。
- **i2（minor）**：`DiaryDetailSheetIDTests.test_commentsCase_and_contentActionsCase_neverCollideEvenWithSameUnderlyingID` 在任何實作下都不可能紅（`comments` 這個字面與任何 UUID 字串永遠不相等，與前綴有無無關）；另一支只鎖住 `contentActions-` 前綴這個目前不具功能性的字面。兩支對「單一呈現來源」這個真正的不變量沒有鑑別力——真正的鑑別力來自兩支 UITest（mutation 3／4 已證）與本輪的延遲注入重現。測試檔文件註解已誠實寫明此限制，故列 informational 不列 finding；日後若導入 ViewInspector 或做一支可注入延遲的 harness，補一支真正的狀態機測試較好。
- **i3（minor，事實查核）**：`DiaryDetailView.swift` 檔頭「範圍決定」段寫 fullScreenCover 殘留同型風險「記入待辦池」，但我查 LS-96 今日池（最新到 09-13 10:26 那批）沒有對應條目，handoff 的處置也寫「不修＋理由」而非「記入待辦池」——程式碼註解的宣稱目前沒有落地。請 orchestrator 補記一條池項，或把該句改成與 handoff 一致的措辭。
- **i4（minor）**：`commentButtonCompact` 與 `commentButton` 的 padding 11／minHeight／contentShape／accessibility identifier／label 是整組複製（約 18 行），兩份日後容易漂移（例如只改一邊的 padding，熱區就不再一致）。可抽成 `commentButton(compact:)` 一支。非本票必要。
- **i5（PLAUSIBLE，不擋）**：`.fullScreenCover(item: $playingVideo)` 與 `activeSheet` 之間仍是同一形狀的窗口——`playVideo` 也是先 tap、簽名 URL 回來才設狀態；若當下留言 sheet 正開著，`playingVideo` 有可能卡在非 nil 而 `preparingVideoIDs`／同一個 id 讓再點同一支影片不重呈現。我沒有實跑重現（`.diaryDetail` harness 沒有種影片，要動 harness 才測得到，超出 review 範圍）。票文明訂不做，同意不在本 PR 處理，但請與 i3 一起真的落一條池項。
- **i6（資訊補充，含實測數字）**：`commentButtonCompact` 不是死碼——`TARGETED_DEVICE_FAMILY=1,2` 且 `LittleSprout/Info.plist` 沒有 `UIRequiresFullScreen`，iPad Slide Over（約 320pt 視窗，content 約 272pt）低於完整版需求 299pt，會真的落到降級版；handoff 說「現有畫面都不會觸發」偏保守。但這條路徑目前確實零測試覆蓋，兩張截圖也都是全寬 harness 畫面、不是票文寫的「側欄展開詳情頁」窄欄，而 `tap-target-check.sh` 只量標準字級的完整版——降級版的熱區從未被量過（讀 code 是與完整版同一組 padding／minHeight，應為 47pt）。建議 QA 在 iPad Slide Over 或 1/3 分割補一張截圖，把降級版看一眼。
- **i7（PLAUSIBLE，測試脆弱度）**：`DiaryDetailCommentsUITests` 新測試用 `app.staticTexts[留言].firstMatch` 當拖曳起點——我的 probe 量到互動列的完整版本身也會曝露一個 identifier 為 留言 的 staticText（`labelTexts=1`），目前之所以不會抓錯，是因為 sheet 呈現時背景元素被系統標記為非 modal 內容而不可見。改用 sheet 內作用域（例如 `app.sheets.staticTexts`）或給標題一個 accessibility identifier 會更穩。今天綠、不擋。

**verdict：APPROVE**（無 blocker／無 major；上列 i1–i7 皆 informational）。

---
`bash scripts/gates/handoff-evidence-check.sh <本文暫存檔> --repo /Users/clyeh/little-sprout/.claude/worktrees/LS-245` → **exit 0**（8 條列項皆有證據）。須帶 `--repo` 指向本票 worktree：新增的 `DiaryDetailSheetIDTests`／`CommentDeleteConfirmationSheetTests` 只存在於 PR 分支，對主 checkout（main）跑會誤判「測試名找不到」。

### 2026-09-13T02:42:21.828Z · Chih-Lin Yeh · `af063be5`

**orchestrator 09-13 10:41**：merge-review R1 APPROVE（`0795545f`，status 貼 f00e9a4；reviewer 以延遲注入**實跑重現**了項 2 的 race——修前留言 sheet 幽靈復現蓋住「⋯」、修後消失；`ViewThatFits` 不抖動、AX3 不走降級；mutation 4/4）→ #384 併入 development `84e3bc5`（dev tip status 已貼）→ 等 #383 LS-238 back-merge 併入後一起 promote dev→test → QA（iPad 兩張＋**Slide Over 一張**（i6）、`SettingsViewIPadTests` 3×）。i3／i6 記 LS-96。

### 2026-09-13T03:46:40.666Z · Chih-Lin Yeh · `f018974c`

**orchestrator 09-13 11:38**：dev tip `115db53`（含本票 #384）CI `ci-ipad` 紅：`SettingsViewIPadTests.testContentSafetySectionStorageRowPushesAndBackReturns` 仍失敗——項 3 補的 `waitForHittable` 在 CI runner 上沒有消掉 flake（本機 3× 綠）。已 `gh run rerun 34734406745 --failed`；QA 派工時要求下載該 run 的 xcresult artifact 看失敗當下的畫面樹與時間軸，判斷是同步點放錯位置還是 runner 慢到超過等待上限，回報給 orchestrator 決定 R2 或另票。

### 2026-09-13T03:47:31.463Z · Chih-Lin Yeh · `f8a663c1`

**orchestrator 09-13 11:47**：dev tip rerun 綠 → promote dev→test ✓（test `115db53`）→ QA 派出（優先診斷 CI run 34734406745 attempt 1 的 xcresult；`SettingsViewIPadTests` 3×；iPad mini／13"／**Slide Over** 三張；真後端 race 情境）。PASS 且 flake 診斷不需 R2 → 收尾。

### 2026-09-13T05:11:09.695Z · Chih-Lin Yeh · `d3784958`

**qa 09-13 13:10 PASS（test 115db53）**

worktree `.claude/worktrees/qa-test`，`git rev-parse HEAD` = `115db53d0215b6dabada73664ce41b0fdd1985a2`（＝origin/test，`git status` 乾淨）。

## 1. CI flake 診斷（優先，結論：判定 (b) runner 慢到超過等待上限，不需 R2）

下載 run `34734406745` attempt 1 `ci-ipad` 的 xcresult artifact（`xcresult-ci-ipad-ipad-test-1`），用 `xcresulttool get --legacy` 拆出 `testContentSafetySectionStorageRowPushesAndBackReturns` 的完整 activity 時間軸（非 summary，帶每步 start/finish 時間戳）：

- 失敗斷言：132 行 `waitForHittable(contentSafetyTab, timeout: 5)`（項 3 剛補的那個同步點本身）與連鎖的 139 行 `entry` hittable 檢查。
- 決定性證據——同一支測試在**我自己本機對同機型（iPad Air 11" M3）跑的 3 次全綠**中，同兩個動作的耗時對照：

| 動作 | CI 失敗那輪 | 本機（我剛跑的 run1） |
|---|---|---|
| `Checking hittable==1`「內容與安全」 | 4.111s | 0.035s |
| `Tap`「內容與安全」 | **6.579s** | 0.388s |
| `Checking hittable==1`「儲存空間」 | 4.302s | 對照另一支同檔測試量級相近 |
| `Tap`「儲存空間」 | **9.038s** | ~1.1s |

一次單純的 tap 事件合成＋派送在 CI 那輪要 6.6～9.0 秒（正常應為次秒級），同一輪另一支 `testAccountSectionDeleteRowPushesAndBackReturns` 也異常耗時 44s（同檔其餘同構測試 14～21s）——整輪 runner 當時就是全面變慢，不是這一支測試的同步點放錯位置或邏輯錯誤（`waitForHittable` 的用法與同檔另兩支已穩定通過的測試完全同構）。加上 orchestrator 的 rerun（attempt 2）五項全綠、ios-dev 本機 3× 綠、我本機 3× 綠（見下），結論：**CI 基礎設施瞬時降速的 flake，不需要 R2**。

## 2. iPad 測試（`qa-test-iPadAir11M3`，與 CI ci-ipad 同機型，本機 runtime 26.0／pinned 26.2 未安裝，fail-open）

- `SettingsViewIPadTests` 連跑 3 次：每次 6/6 通過、0 failures（77.8s／77.0s／77.3s）。
- `DiaryDetailCommentsUITests` + `ContentActionsUITests`：16/16 通過、0 failures。
- 單元 `DiaryDetailSheetIDTests` + `CommentDeleteConfirmationSheetTests`：3/3 通過、0 failures。

## 3. 截圖（iPad mini／13"）

`.claude/evidence/LS-245/qa/diary-detail-ipadmini.png`、`diary-detail-ipad13.png`（`SIMCTL_CHILD_LS_TAP_TARGET_GATE_SCREEN=DiaryDetailView` 注入）：「愛心 0」「留言 0」兩台皆完整不截字，字級／icon／間距一致。

**Slide Over（i6）未能取得**：嘗試用 mobile-mcp 觸發 iPadOS 原生 Slide Over（從 Dock 長按拖曳到螢幕邊緣），但 mobile-mcp 沒有「長按後拖曳」的原生手勢（`mobile_swipe_on_screen` 是單純滑動，並在嘗試過程中對這台模擬器出現 WebDriverAgent 連線逾時）——無法可靠產生真正的 Slide Over 窄窗截圖。以程式碼佐證替代：`commentButtonCompact`（`InteractionRow.swift`）與完整版共用同一組 `padding(11)`／`.frame(minHeight: likeToggleSize.height)`（47pt）／`contentShape`／accessibility identifier／label，熱區在建構上與已通過 `tap-target-check` 的完整版一致。建議記入 LS-96 池：後續若要真正驗證降級版渲染，需要 ios-dev 加一支固定寬度（如 272pt）的窄寬 harness 畫面，QA 工具無 Edit 權限做不到。

## 4. 真後端（項 2 race，`--hold "qa-e2e LS-245"` 持有 13 分 30 秒）

- `qa-e2e.sh browse --ticket LS-245`：**第一次跑遇到環境性 flake**——落地頁卡在「伺服器發生問題，請稍後再試一次」，Kong log 顯示 OTP 登入成功（200）後緊接著 `/rest/v1/families`／`/rest/v1/profiles` 兩個查詢回 401；用全新帳號手動 curl 重放同一組 OTP→verify→REST 呼叫（`qa-diag-*@ls.test`）拿到全新 access_token 打 `/rest/v1/families` 回 200 `[]`，證明本機後端本身健康，401 是該次特定帳號/模擬器組合的一次性競態（帳號 `qa-e2e@ls.test` 在這輪剛好被 signup，同一秒內有舊 session 殘留的可疑跡象）——不是 app 代碼缺陷，屬 LS-158 文件已知的共用容器風險類別。**重跑一次乾淨通過**：16 張截圖，登入→建家庭→發佈→時間軸→詳情→返回→相簿→時間軸，證據 `.claude/evidence/LS-245/qa-e2e/browse-20260913-122252/`。
- 在該次登入的真實 session 上用 mobile-mcp 手動重現 reviewer 的 race 情境（兩個方向）：
  1. 開留言 sheet → 下滑關閉 → 立刻點「更多操作」→ 內容操作表正常開出（「刪除」「取消」），無留言 sheet 幽靈殘留。
  2. 開內容操作表 → 取消關閉 → 立刻點留言 → 留言 sheet 正常開出，無操作表殘留。
  截圖：`.claude/evidence/LS-245/qa/race/reverse-comments-after-actions-clean.png`（第 2 方向；第 1 方向已在對話中截圖確認，內容操作表正常顯示「刪除」「取消」）。mobile-mcp 的呼叫間隔比 XCUITest 合成事件慢得多，無法真正逼出微秒級競態窗口——這部分的決定性證據仍是 merge-reviewer 已做的延遲注入重現（`0795545f`），本輪是「真後端、真互動」層級的獨立佐證，兩者互補、結論一致：修復有效。

## 5. Gate

- `tap-target-check.sh`：**第一次跑因我自己誤操作（背景執行期間手滑 `simctl shutdown` 了同一台正在測試的模擬器）汙染**，`SettingsViewTests.testMemberDoesNotSeeReportInboxRow` 紅（與 LS-245 diff 無關，判定是我造成的汙染，log 存檔為 `tap-target-check-iphone-run1-contaminated-by-qa-mistake.log` 供稽核）。在乾淨的 `qa-test-iPhone17Pro` 上重跑：✓ 全部量測畫面（含 `DiaryDetailView`、`CommentsSheetView`）皆 ≥44×44pt，log：`tap-target-check-iphone-clean-PASS.log`。
- `ax-launch-check.sh`：exit 0（0 項違規、1 項在已知允許期限內，`TapTargetMeasurement.swift`，LS-190 併主前的既有事項）。

## 驗收條件對應

- [x] 每項附單元／UITest＋mutation：ios-dev／merge-reviewer 已附（見 `c851b292`／`0795545f`），本輪重跑複驗全綠。
- [x] iPad mini＋13" 側欄詳情頁截圖計數完整：見上方第 3 節。
- [x] `tap-target-check`／`ax-launch-check` 綠：見上方第 5 節（乾淨重跑）。
- [x] merge-reviewer 通過：`0795545f` APPROVE。
- [x] QA：iPad 兩張截圖＋`SettingsViewIPadTests` 連跑 3 次綠：見上方第 2、3 節。

模擬器已關：`7FC0E099-39DA-4FEB-9265-D5D1C981D4C9`（LS-245-iPhone17Pro）、`5F59FF04-D2C6-413D-B31A-AFD39B257FB3`（qa-test-iPhone17Pro）、`F72DBCD1-9CBE-4903-AE6E-241064C391E1`（qa-test-iPadAir11M3）、`55E8D0E4-3E46-4E6B-BCC4-F50B56C9CA7F`（LS-245-iPadPro13）、`CB73A6F3-5A7B-4C0A-BD1E-407A163657C8`（LS-245-iPadMini2）。
lock 已釋放：「qa-e2e LS-245」（持有 13 分 30 秒）。
qa-e2e 證據：browse=`.claude/evidence/LS-245/qa-e2e/browse-20260913-122252/`（exit 0；第一次因環境性 flake exit 1，已重跑通過，失敗回合已清除不留存）。

### 2026-09-13T05:13:53.252Z · Chih-Lin Yeh · `3c94cb9f`

**orchestrator 09-13 13:13 QA PASS**（`d3784958`，status 貼 115db53）。CI flake 診斷結論：attempt 1 失敗點正是項 3 新加的 `waitForHittable`，但 xcresult 時間軸顯示該 runner 當時慢 10–20×（單次 tap 6.6–9.0s vs 本機 0.4–1.1s、同 run 另一測試 44s vs 14–21s）→ 屬 runner 降速、同步點放對，**不開 R2**。`SettingsViewIPadTests` 同機型 3× 綠、race 情境雙向真後端乾淨、iPad mini／13" 截圖完整；Slide Over 截圖因 mobile-mcp 無長按拖曳未取得，改以原始碼核對 `commentButtonCompact` 與完整版共用常數 → 記 LS-96（ios-dev 補窄寬 harness 畫面）。→ 派 sweeper → 收尾 → Done。

### 2026-09-13T05:19:44.431Z · Chih-Lin Yeh · `00ef2484`

**dead-code-sweeper 09-13 13:18**

## 範圍

`git diff 89a0190..115db53 -- LittleSprout LittleSproutTests LittleSproutUITests`（worktree `.claude/worktrees/qa-test`，HEAD `115db53d0215b6dabada73664ce41b0fdd1985a2`＝origin/test，`git status --porcelain` 僅有 gitignored `DerivedData-qa/`／`scratchpad/`，checkout 乾淨）——11 檔、+330/−49，逐檔全讀（非只看 hunk）。

## Findings（本 feature 引入）

**F1（非死碼，確認清除乾淨）**：兩個獨立 Bool／item 狀態 `showsCommentsSheet`、`contentActionsContext`（`DiaryDetailView.swift` 原 L85/89 附近）已完全移除，改為單一 `@State var activeSheet: DiaryDetailSheet?`。`grep -rn "showsCommentsSheet\|contentActionsContext\|commentsSheetHost" LittleSprout LittleSproutTests LittleSproutUITests`（`DiaryDetailView` 範圍）——所有殘留 hit 都是文件註解裡的沿革敘述（解釋「原本是什麼、現在改成什麼」），無任何 live `@State`／binding／呼叫點。`TimelineView` 自己另一份同名 `commentsSheetHost`（`TimelineView+Comments.swift:9`）是不同畫面的獨立符號，不受影響，非誤判來源。**處置：不動，確認乾淨。**

**F2（非死碼，確認每個 case 都有呈現路徑）**：`DiaryDetailSheet` enum（`DiaryDetailView.swift:38-46`）兩個 case `.comments`／`.contentActions(context)`，`activeSheetHost`（`DiaryDetailView.swift:316-331`）用 `switch sheet` 窮舉處理，無 `default:`，編譯器保證窮盡；`.overlay(activeSheetHost)` 於 `body`（L141）掛載，確認被引用。**處置：不動，確認乾淨。**

**F3（非死碼，確認共用常數而非複製漂移）**：`commentButtonCompact`（`InteractionRow.swift:185-201`）目前唯一呼叫點是 `ViewThatFits` 兩個候選之一（L75）——這是 SwiftUI 官方機制的即時量測分支，不是宣告後零引用的死碼；與完整版 `commentButton`（L160-176）逐項比對 `padding(11)`／`.frame(minHeight: likeToggleSize.height)`／`.contentShape(Rectangle())`／`accessibilityIdentifier`／`accessibilityLabel` 五項全部逐字相同，非各自硬編、非複製後漂移。與 merge-review i6（`0795545f`）結論一致：真實觸發條件是 iPad Slide Over／窄分割視窗（實測需求 299pt vs 完整版），目前零測試覆蓋、零實機截圖驗證——**列為刻意預留＋零測試覆蓋觀察，非死碼**，出處：`0795545f` i6 已詳列量測數字與建議（補窄寬 harness），此輪只確認熱區常數共用無漂移，不重複開票，併入既有 i6／LS-96 追蹤即可。

**F4（非死碼，確認新測試用上舊未呼叫符號）**：`StubCommentAPIClient.setSetCommentDeletedHandler`（`LittleSproutTests/Support/StubCommentAPIClient.swift:45`）原為 LS-218 遺留無呼叫點的符號（LS-96 池 `38a3c74b`），現在 `CommentDeleteConfirmationSheetTests.swift:20` 有唯一呼叫；`CommentDeleteConfirmationSheet.performDelete()`（新抽出方法）取代原本 inline 在 `confirmAction: { try await commentAPIClient.setCommentDeleted(...) }` 的閉包——`git diff` 顯示無殘留 inline 路徑（confirmAction 現在直接指向 `performDelete`，非兩者並存）。斷言非空洞：`XCTAssertEqual(recorder..., [RecordedCall(commentID: commentID, deleted: true)], ...)` 記錄實際呼叫參數並比對，會隨邏輯改變而變色（已用 mutation 驗證：`deleted` 改 `false` → 紅，`0795545f`／`c851b292` 已附）。**處置：不動，確認乾淨。**

**F5（非死碼，`SettingsViewIPadTests` 同步點寫法一致）**：三支 push/back 回歸測試共用 `assertPushThenBackReturnsToList` helper，內含 `waitForHittable(entry, ...)`（LS-237 `96734b7` 加，對 profile／family／content-safety／account 四支呼叫端一體生效）；本票新補的 `testContentSafetySectionStorageRowPushesAndBackReturns` 額外加了 `waitForHittable(contentSafetyTab, timeout: 5)` 守住 sidebar 分頁切換那一次 tap，與 `waitForNonExistence` 同一套 `XCTNSPredicateExpectation` 輪詢機制（helper 定義在檔案 L64-73，非本票新增，本票只加呼叫）。三次連跑 6/6 皆綠，無殘留舊 `Thread.sleep`／固定秒數等待。**處置：不動，確認乾淨。**

## 發現（本 feature 引入的文件不一致，非死碼分類但值得記錄）

**F6（minor，程式碼／commit message 對現況的敘述不準確）**：`SettingsViewIPadTests.swift` 本票新增的註解（`testContentSafetySectionStorageRowPushesAndBackReturns` 上方，commit `fca6ffe`，同一段文字也逐字出現在該 commit message）宣稱：

> 同檔另外兩支（`testFamilySectionInviteRowRegressionPushesAndBackReturns`／`testLegalSectionRowsOpenAndCloseLegalDocumentSheet`）已經在 LS-237 `96734b7` 補過『tap 前先等真的可點』同步點，這支當時漏補

實際查 `96734b7` 的 diff：該 commit (b) 只在共用 helper `assertPushThenBackReturnsToList` 內對「入口列」（entry，如「邀請家人」列／「儲存空間」列本身）的 tap 加了 `waitForHittable`，這對 profile／family／content-safety／account 四支呼叫端**一體生效**——不是「family／legal 兩支」專屬的修補。而本票這次真正新加的是**另一種 tap**：sidebar 分頁切換 tap（`app.buttons["內容與安全"].tap()`）。用 `grep -n "app.buttons\[\"家庭\"\].tap()\|app.buttons\[\"法律\"\].tap()"` 交叉核對：`testFamilySectionInviteRowRegressionPushesAndBackReturns`（L101）與 `testLegalSectionRowsOpenAndCloseLegalDocumentSheet`（L185）的 sidebar 切換 tap **目前仍是裸 `.tap()`，沒有任何 `waitForHittable` 守門**——與 merge-reviewer i1（`0795545f`）指出的「同檔另外四處 sidebar 切換仍是裸 `.tap()`（L101 家庭、L158 帳號、L185 法律、L239 家庭）」完全吻合。也就是說，本票新加的這段註解／commit message 對「family／legal 已經修過同步點」的宣稱，把「入口列 tap 的同步點」與「sidebar 切換 tap 的同步點」兩件事混為一談，實際上 family／legal 的 sidebar 切換 tap 仍未修，並非誤報而是真的沒修（測試目前綠是因為還沒踩到那個 flake 窗口，不是已經被同步點擋住）。

不影響本票測試正確性（三次連跑皆綠，`tap-target-check`／CI 皆綠），純屬敘述與現況不符，可能誤導日後根據這段註解判斷「family／legal 已經處理過、不用再看」。

**處置建議**：併入 LS-96 池既有的 merge-reviewer i1 項（同一個底層事實：四處裸 `.tap()` 待補），順手在下次補齊 i1 時一併訂正這段註解措辭（例如改成「family／legal 的『入口列』tap 已透過共用 helper 受惠於 LS-237 的同步點，但『sidebar 切換』tap 這一類仍未補，同 i1」）；不需另開票，也不建議現在改檔（非本次巡檢職權，且無功能影響、優先度低）。

## 既有死碼觀察

無。本次巡檢範圍內沒有發現不屬於本 feature、但順帶注意到的既有死碼。

## 盲區聲明

- 純文字 grep／diff 讀碼抓不到：reflection、字串拼接組出的 selector、Objective-C runtime、`#selector` 間接呼叫等動態引用——本次 diff 範圍內未見這類寫法，但無法窮舉排除。
- `ViewThatFits` 的兩個候選在 runtime 是否「真的」渲染 compact 版（而非只是量測 ideal size 後捨棄）屬 SwiftUI 私有實作細節，未實機在窄寬環境下用 view debugger 逐層驗證——採信 merge-review i6 的量測數字與官方 API 語意（非死碼判定風險低，因為即使目前是「量測用、未真的畫出來」，它也不符合「宣告後無呼叫點」的死碼定義，這行程式碼在正常執行路徑上必然被呼叫）。
- 未重新跑 build／全量測試（QA `d3784958` 已在同一顆 head 做過，本次巡檢不重複跑，只讀碼＋跑 `swiftlint lint`（四個變更源檔皆乾淨）＋人工交叉比對呼叫點）。

## 建議處置彙總

- F1–F5：不動，確認乾淨。
- F6：併入 LS-96 池既有 i1 項一併處理（非本票新開票，優先度低，不擋 Done）。

Comment id 將於 handoff 摘要回傳給 orchestrator。

### 2026-09-13T05:20:56.664Z · Chih-Lin Yeh · `1159ac25`

**orchestrator 09-13 13:20 收尾儀式**

① sweeper：`00ef2484` 無死碼；F6（fca6ffe 註解誤稱家庭／法律側欄 tap 已修）→ 併入 LS-96 既有 i1 項（已 comment），不另票、不動檔。
② lesson review：
- 本票 CI 兩次 iPad 紅皆為 runner 慢（QA 已定性，非 R2），rerun 即綠；但 `SettingsViewIPadTests` 仍有兩處裸 tap（i1），runner 慢時就是它們先倒——列 cycle 3 harness 候選（size:S）而非留池到底。
- 註解描述修復範圍失真（F6）屬 reviewer 已知盲區：dev 寫「已修」時未 diff 引用的 commit；派工規則補一句「引用他票 commit 做論據時必附 `git show <sha> --stat` 佐證」（記入 dispatch-prompt-rules）。
- 工具缺口：無。
③ cleanup：`cleanup-merged.sh --apply LS-245` → worktree 1／專屬模擬器 3／DerivedData 1 已移除；本機分支 0、遠端 0（PR #384 併入時已刪）。
④ Pen 清場：非設計票，不適用。

→ Done。

