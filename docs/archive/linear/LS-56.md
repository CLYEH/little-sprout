# LS-56 Harness：error-codes-check 抽取規則硬化（註解掉的 case／表格首欄格式）＋artifact retention-days（PR #69 R1 minors）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Low |
| 標籤 | — |
| Cycle | — |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-24T18:41:58.428Z |
| 開始 | 2026-08-24T20:19:10.854Z |
| 完成 | 2026-08-24T21:42:50.039Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-56/harnesserror-codes-check-抽取規則硬化註解掉的-case表格首欄格式artifact-retention |
| 關係 | related→LS-59, related→LS-55, related→LS-58, related→LS-6, related→LS-54, related←LS-83, related←LS-62, related←LS-73, related←LS-50, related←LS-53, related←LS-63, related←LS-60, related←LS-52 |

## 描述

PR #69 merge review minor：

* **F1** `scripts/gates/error-codes-check.sh` Swift 側 `grep -oE '=[[:space:]]*"LS[0-9]{3}"'` 不分程式碼與註解——`// case x = "LS022"` 仍算存在（實測 gate 綠、enum 實缺）；註解裡的 `code = "LS023"` 會假紅。修：pattern 錨到 `^\s*case\s+\w+\s*=\s*"LS\d{3}"`（或先剝註解）＋自測補「註解掉的 case 必須紅」。
* **F2** doc 側整列 grep 非首欄比對：`| \`LS023``（diary_body_too_long） | `這種既有括號註記格式的列被****靜默略過**`**`（危險方向）；說明欄提到的碼會被多抓成幽靈（假紅）。修：`awk -F'|'`取第 2 欄 anchored 比對、允許括號註記；§5 表內出現`LSnnn`` 卻未被首欄規則捕捉 → fail loud。自測補兩組。
* **F3** ci.yml artifact 加顯式 `retention-days:`（[LS-6](https://linear.app/little-sprout-app/issue/LS-6/phase-0-2supabase-專案schema-migrationrls-policies) 驗收「留存」現押在環境預設 90 天）。
* 順帶（[LS-54](https://linear.app/little-sprout-app/issue/LS-54/harness-小修批次api-contract-check-n1-n5approve-join-註解矛盾pr-58-r2-minors) handoff retro）：migration `errcode` ↔ §5 表機械對帳（可做）；text 模式引號識別字靜默略過；COLLABORATION §4 Push gate 敘述過時（未列 api-contract／error-codes）。

## 追加（back-merge 教訓 2026-08-25）

* GitHub 對 main→development 的 back-merge PR 誤判 CONFLICTING（本地 merge-tree 零衝突），改走本地合併分支時必須命名 `hotfix/LS-<n>-backmerge-<target>`（方向矩陣不收 `back-merge/*`）。→ COLLABORATION §2 補一句「本地合併版 back-merge 分支以 hotfix/* 命名」，或方向矩陣加 `back-merge/*`→development|test。
* push-gate 在 fresh worktree 首次 SPM 解析可能瞬斷失敗（xcodebuild 「Could not resolve package dependencies」），重跑即過 → push-gate 在 xcodebuild 前先 `-resolvePackageDependencies` 一次或失敗重試一次。

## 留言（4）

### 2026-08-24T20:48:59.464Z · Chih-Lin Yeh · `42efbaac`

## merge-reviewer R1 — PR #80 @ `df40c34` → ITERATE（2 應修、9 informational；orchestrator 代貼＋裁決）

**親跑證據**：自測 19/19；真實 repo 三方對帳綠；PR body (a)(b)(c) 修前／修後全複現；fail-closed 8 情境全紅（無 0 列＝綠）；SPM 重試 rc 不吞（stub RC=74）、CI 不引用 push-gate 故退避不進 CI、warm resolve 0.7s；對 development／test／main／LS-52／LS-55／LS-58 全綠不誤擋。

**應修**
- **F1** `error-codes-check.test.sh:72`（本 PR 新增行）`"$name（"`／`"$must」"` 在 macOS bash 3.2 觸發 unbound → 失敗樣本名與 gate 輸出全遺失、後續樣本不跑（違反 LS-54 N5）。→ **本 PR 修**：該行改 `${name}`／`${must}`。第 68 行與其他三支既有同病 → LS-59（已含四支）。
- **F2** §2 新規約 `hotfix/LS-<n>-backmerge-<target>` 對 `<target>=test` 會被 `ci.yml` 方向矩陣擋死（`base=test` 只收 `development|main|revert-*`）；歷史（#73/#74）只在 development 跑過。→ **裁決 (a)**：§2 明寫「本變通只適用 `<target>=development`；`test` 端誤判改 close/reopen 或重推 head=`main`」；§7 :154 同步。不放寬矩陣。

**Informational → 處置**
- I3 「命名 regex 也擋 `back-merge/*`」CI 側不成立（只有方向矩陣擋）→ 修文字：CI 靠方向矩陣、本機靠 `commit-gate.sh` regex。
- I4 行尾註解 `// 參考 "LS099"` 誤報幽靈碼（假紅）→ §7 宣稱加限定語「整行註解」。
- I5 append-only 聯集假綠方向（`create or replace` 拿掉 raise 後仍綠）→ §7 未涵蓋面補一句。
- I6 `/* */` 兩向（Swift 假綠／migration 假紅）→ 已自承，登記。
- I7 `RAISE EXCEPTION SQLSTATE`／變數 errcode → 紅但訊息誤導 → 登記（現況全用字面值）。
- I8 三方合併：`ci.yml`／`push-gate.sh` 三序乾淨；**COLLABORATION 與 #78 必衝突** → **合併順序裁定：#79 → #78 → #80**；本 PR 最後，R1 修完後 `git merge origin/main`（屆時含 #79/#78）逐處合併敘述（§4 Push gate 列同時保留 migration 分級＋SPM 重試；§7 保留 #79/#78 各列＋本票錯誤碼列改寫），不得取單邊。
- I9 `ci.yml:100` step 名、`push-gate.sh:51-52` 註解仍寫「兩方／LS-54」→ 修成三方。
- I10 自測樣本數無機械斷言而 §7 寫「19 組」→ 加 `[ "$count" -eq 19 ]`（或改 §7 不寫數字）——擇前者。
- I11 Scope：migrations 第三腳有舊 §7「可做，待補」依據；push-gate SPM 重試與 §2 back-merge 命名來自 LS-54 retro（orchestrator 派工時列入本票 D-series）→ **orchestrator 確認屬本票範圍**，handoff 註明。

**R2 進場**：F1/F2/I3/I4/I5/I9/I10 落地＋（待 #79/#78 併入後）merge main 解 COLLABORATION 衝突；R2 只驗這些＋衝突解法。

### 2026-08-24T21:32:07.289Z · Chih-Lin Yeh · `7b230bcc`

## merge-reviewer R2（核銷）— PR #80 @ `9684642` → **APPROVE**（0 blocker／0 major；3 informational）

只驗 R1 七項（F1／F2／I3／I4／I5／I9／I10）＋ merge `origin/main` 的衝突解法，R1 已過項不重審。全部證據為本機 `/bin/bash` 3.2.57(1) 親跑。

### R1 七項核銷

**F1 PASS** — `scripts/gates/error-codes-check.test.sh:72` 已改 `${name}`／`${must}`（blame：`830398c`）。
- 病因復現：`/bin/bash -c 'set -u; name=SAMPLE; echo "✗ $name（…）"'` → `bash: name<E3>: unbound variable`，rc=127；`${name}` 形式 rc=0。
- **實跑第 72 行分支**：把樣本①的第 6 參數改成 `ZZZ-NEVER-MATCHES`（exit code 吻合、`must` 不吻合 → 走 elif），輸出完整：
  `✗ ① 三方一致（散文、他節表格、migration 行註解的碼不計入）（exit 對了，但輸出缺少「ZZZ-NEVER-MATCHES」——紅的理由不對）` ＋ gate 原輸出縮排傾印，**且後續 18 組照跑**、結尾 rc=1（不是 127 中斷）。LS-54 N5 精神保住。
- 第 68 行同病仍在（`651ffb1a` 既有），已確認 LS-59 在 Backlog 且描述涵蓋四支，deferral 有歸屬。

**F2 PASS** — `docs/COLLABORATION.md` §2 back-merge 段明寫「**本變通只適用 `<target>=development`**：方向矩陣 `base=test` 只收 `development|main|revert-*`…`test` 端若誤判 CONFLICTING，改 close／reopen 該 PR 或重推一個 head=`main` 的 PR，不放寬矩陣」；§7 `harness 檔 back-merge` 列同步同一限定。
- 與矩陣對帳：`ci.yml` rules job 實際為 `development) feature/*|fix/*|hotfix/*|main|revert-*` ／ `test) development|main|revert-*` ／ `main) test|hotfix/*|revert-*` — `hotfix/LS-n-backmerge-development` 合法、`hotfix/LS-n-backmerge-test` 會被擋，文件與矩陣**一致**。

**I3 PASS** — §2「CI 由 `rules` job 方向矩陣擋（PR #73 被擋、#74 改名後通過）、本機由 `commit-gate.sh` 的 branch 命名 regex 擋」；§7 同步。實地驗證 `commit-gate.sh:20` regex `^(feature|fix|hotfix)/LS-[1-9][0-9]*-[a-z0-9][a-z0-9-]*$` → `back-merge/*` 不匹配（本機擋成立）、`hotfix/LS-54-backmerge-development` 匹配（規約名不會被自家 hook 擋）。

**I4 PASS（含文件屬實驗證）** — §7 已限定「**整行**註解裡的 `"LSnnn"` 不算幽靈碼」，未涵蓋面新增「case 行的**行尾**註解…仍會被抓成幽靈碼（假紅）」。實跑對帳：
- `case a = "LS001" // 參考 "LS099"` → 紅 `LSErrorCode 有、API.md 沒有（幽靈碼）：LS099`（＝§7 所述假紅，屬實）
- 多行 `/* */` 內縮排 case（§7 稱「仍會被算進去」）→ 綠（＝所述假綠，屬實）

**I5 PASS（含文件屬實驗證）** — §7 補「migrations 腳對 append-only 歷史取**聯集**——後續 migration 以 `create or replace` 拿掉某個 raise 後…三方仍綠（假綠方向）；反之…會紅（fail loud，不靜默）」。合成兩檔（`0001` 含 LS001/LS002、`0002` `create or replace` 只留 LS001）實跑 → rc=0 綠，假綠方向屬實，§7 敘述精確。

**I9 PASS** — `ci.yml` step 名 `錯誤碼三方對帳（docs/API.md §5 ↔ LSErrorCode ↔ migrations errcode，LS-54／LS-56）`；`push-gate.sh:51-52` 註解同改三方。

**I10 PASS（含負向）** — `.test.sh` 結尾 `[ "$count" -eq 19 ] || { echo "✗ 樣本數應為 19，實得 ${count}"; exit 1; }`。負向：刪掉「⑨ ERRCODE 大小寫／空白不拘」一組 → `✗ 樣本數應為 19，實得 18`，rc=1。§7 的「19 組」不再無 gate。

**I11 PASS（依據已列，附 informational）** — 三條順帶項依據可追：migrations 第三腳←舊 §7「（可做，待補）」（見淨刪除行）＋PR body「LS-54 retro 工具缺口」；SPM 重試與 back-merge 命名←票文「## 追加（back-merge 教訓 2026-08-25）」。

### 衝突解法核銷（`git show 9684642`，parents `830398c` + `38cc76b`）

- **無 `<<<<<<<`／`>>>>>>>`／`=======` 殘留**（全 repo `.md`/`.yml`/`.sh` 掃描為 0）。
- **§4 Push gate 列**：單一列（count=1），同時含 #78 第 5 步（`migration 分級（migration-breaking-check --base…LS-53）`）＋本票 SPM 重試（`SPM 解析瞬斷隔 10 秒自動重試一次，LS-56`）＋三方對帳（`error-codes-check`：API.md §5 ↔ LSErrorCode ↔ migrations errcode）。無取單邊。
- **§7 逐列**：`設計畫布執行產物不入版控`(#79)／`破壞性 migration 需本人核可`(#78)／`呼叫端破壞性 migration（BREAKING）`(#78) 三列與 `origin/main` **md5 逐列相同**（原文，未被本票改寫）；`migration 必附 RLS 測試` 列採本票版（附 `retention-days: 90` 註）；錯誤碼三方列改寫；back-merge 列補命名機械擋。**每列各出現恰一次**。
- **淨刪除行稽核**：`git diff origin/main HEAD` 的所有 `-` 行逐條追溯，全部是本票刻意改寫的舊敘述（舊 error-codes gate 本體／舊 6 組自測／舊 §4·§7 三列／舊 ci.yml 註解與 step 名），**#78／#79 內容零遺失**。
- **`push-gate.sh`**：`bash -n` OK；步驟序 `1) SwiftLint → 2) Unit tests（內含 SPM resolve＋10 秒退避重試）→ 3) API 契約 → 4) 錯誤碼三方 → 5) Migration 分級(LS-53)`。`set -euo pipefail` 在檔頭 → 重試那次失敗仍會中止，rc 不被吞。
- **`ci.yml`**：`yaml.safe_load` 解析成功；jobs `rules/db/lint/ci`；rules step 序 checkout → 方向矩陣 → 命名/訊息/secrets → **Gate 腳本自測（五支全在：destructive-approval／api-contract／error-codes／migration-breaking／breaking-section，各收各 rc）** → 錯誤碼三方對帳 → tracked-ignored(#79) → Migration rules → Design gate → .pen；`retention-days: 90` 在 `db` job 的 upload-artifact `with` 內。

### 親跑證據（bash 3.2）

| 自測 | rc | 結果 |
|---|---|---|
| destructive-approval-check | 0 | ✓ |
| api-contract-check | 0 | ✓ 15 組 |
| error-codes-check | 0 | ✓ **19 組** |
| migration-breaking-check | 0 | ✓ 69 組 |
| breaking-section-check | 0 | ✓ 13 組 |
| tracked-ignored-check | 0 | ✓ 8 組（①②③③′④⑤⑥⑦） |

- 真實 repo：`error-codes-check.sh` → `✓ docs/API.md §5、LSErrorCode、migrations 三方皆為 15 個自訂碼且一致`，rc=0。
- `git merge-tree --write-tree origin/main origin/hotfix/LS-56-error-codes-gate-hardening` → rc=0、tree `45bb569`，**無衝突**（main=`38cc76b`）。`gh pr view 80` mergeable=MERGEABLE。
- **CI 於 `9684642` 全綠**（run `32779334773`，event=pull_request）：rules／lint／db／ci 皆 pass。
- **F3 端到端實證**：該 run 產出 artifact `rls-plan-evidence`，created `2026-08-24T21:27:14Z` → expires `2026-11-22T21:24:37Z` ＝ **90 天**，`retention-days: 90` 真的生效，不再押環境預設。

### 四維度

- **Race condition**：本 PR 無 Swift concurrency 面。唯一時序面是 push-gate 的 SPM resolve→retry→test 串行，`set -e` 下 rc 不吞，無競態。
- **運算效能**：無 RLS policy／查詢／圖片路徑變更。gate 腳本為 O(檔案行數) 的單 pass grep/awk，CI rules job 8s。
- **平行優化**：不適用（gate 自測序列跑是刻意的——各收各 rc 才看得到全部紅點，LS-54 N5）。
- **Scope**：淨 diff **5 檔**全為 harness（`ci.yml`／`COLLABORATION.md`／`error-codes-check.sh`／`.test.sh`／`push-gate.sh`），無 app 程式碼、無順手重構。R1 修 commit `830398c` 僅 4 檔且逐項對應 R1 條目；merge commit 只手解 COLLABORATION 兩處。**scope 乾淨**。

### Informational（不擋合併）

- **INFO-1** `scripts/gates/error-codes-check.sh:29-30` — 腳本檔頭仍寫「migrations 是 append-only…若日後有碼被後續 migration 退役，這裡會紅…不靜默」，只講了 fail-loud 那半邊；I5 已證實另半邊（`create or replace` 拿掉 raise → 聯集仍綠）是假綠。§7 現在兩向都寫對了，但**先讀腳本檔頭的人會被誤導**。建議下次動這支時補一句（或併入 LS-59），不必為此擋 PR：行為未變、§7 為權威對照表。
- **INFO-2** PR #80 body 停留在 R1 前狀態 — 未提 R1 七項修正（`[ "$count" -eq 19 ]` 斷言、§2 `<target>=development` 限定、三方 step 更名）、未提 merge 與衝突解法。衝突解法在 merge commit message 裡有完整記載（不算遺失），但 PR body 是日後第一眼看到的東西。**建議合併前補一段 R2 增補**。
- **INFO-3** `error-codes-check.test.sh:68` 同一 bash 3.2 病灶仍在（既有行，非本 PR 引入）——R1 已裁決 defer，LS-59 存在且描述涵蓋四支，追蹤到位。此處僅確認 deferral 有歸屬。

### 未審範圍（明講）

- R1 已過項（error-codes-check.sh 抽取規則本體、19 組樣本語意、fail-loud 三情境、PR body (a)(b)(c) 修前修後對照）**未重審**，依 R2 範圍限定。
- 本次未再跑 `xcodebuild test`／SPM 重試的實機時序（R1 已以 stub RC=74 驗過 rc 不吞；本次僅靜態確認 `set -euo pipefail` 與步驟位置）。

---

**VERDICT：APPROVE** — 無 blocker、無 major。三項 informational 建議：INFO-2 合併前補 PR body；INFO-1 併入 LS-59 或下次動該檔時順修；INFO-3 已有歸屬、無需動作。

**收工證明**：worktree `.claude/worktrees/LS-56` — `git status --porcelain` **空（clean）**，`HEAD=9684642d8520ccc7948351d2f90e6a5539b040d7`。本次審查未修改任何檔案；所有合成樣本寫在 session scratchpad，repo 外。

### 2026-08-24T21:33:45.888Z · Chih-Lin Yeh · `bc9f1e06`

PR #80 已併入 main（R2 APPROVE，reviewer 自寫 comment `7b230bcc`；F3 端到端證實 artifact 90 天）。INFO-1（`error-codes-check.sh:29-30` 檔頭只寫 fail-loud 半邊）併入 LS-59 順修；INFO-2 PR body 未補 R2 段——merge commit 有記載，接受。back-merge main→development／test 批次進行中，落地即 Done。

### 2026-08-24T21:42:59.442Z · Chih-Lin Yeh · `5ad9721a`

back-merge #86／#87 落地 → **Done**。Lesson：(1) 自測樣本數要機械釘死（`[ "$count" -eq N ]`），否則 §7 寫的數字是無反饋的前饋；(2) 規約寫進 COLLABORATION 前先對照 CI 方向矩陣實測（§2 back-merge 命名對 test 端本會被擋）；(3) 同批四支 harness PR 改同檔——合併順序由 orchestrator 裁定、最後一支解衝突、reviewer 逐條驗淨刪除行零遺失，這套流程可沿用。

