# LS-236 Harness：殘留 `xcodebuild` 防護（Bash timeout 截斷自動背景化，push-gate／detect-simulator 開跑前偵測同 UDID 殘留行程）＋agent 定義「xcodebuild 前景 timeout 600000、分段」＋`pen-open.sh --kill` 清場後 `.pen` 寫回主 checkout 自動還原＋COLLABORATION／agent 定義過期敘述清倉（池 `4de3e796`／`797c7149`／`188ae73b`／`51bd1635`／`bab56145`／`5d6e51b6`(1)／`588c483f`／`49457773`(1)(3)／`4bbd2960`(2)／`1ae89cbd`）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:S, lane:harness |
| Cycle | 2 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-12T16:38:14.526Z |
| 開始 | 2026-09-12T16:38:14.662Z |
| 完成 | 2026-09-12T21:28:56.761Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-236/harness殘留-xcodebuild-防護bash-timeout-截斷自動背景化push-gatedetect-simulator |
| 關係 | related→LS-199, related→LS-211, related→LS-227, related→LS-132, related→LS-218, related→LS-235, related→LS-239, related→LS-209, related→LS-215, related→LS-171, related→LS-233, related→LS-217, related→LS-226, related→LS-231, related→LS-96, related→LS-208, related→LS-166, related←LS-241, related←LS-244, related←LS-242, related←LS-238 |

## 描述

## 來源

harness lane 09-13 00:36 空位補位（[LS-233](https://linear.app/little-sprout-app/issue/LS-233/harnesspatrolsh-pr-段-blocked-分流-讀-check-bucket-分ci-跑中-n-分check-全綠仍) Done）；§5-b 依 priority＋size 自 [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池取 P2 為核心、同檔 XS docs 項順手清倉：

* `4de3e796`**（P2 S，兩起事故）**：[LS-166](https://linear.app/little-sprout-app/issue/LS-166/taskls-20-ios-相簿詳情瀑布流照片牆加入照片入口寶貝標記改名與刪除相簿依-ls-142-稿實作含深色ax3ipad34-張壓測) R2 handoff 在 push gate 期間發現並殺掉一個早前「Bash 工具 timeout 上限 600000 截斷後自動背景化」殘留的 `xcodebuild` 行程，它正靜默污染測試（模擬器爭用）；同型 [LS-217](https://linear.app/little-sprout-app/issue/LS-217/taskls-22-ios-推播權限前置說明頁系統授權register-device-token設定頁推播通知列與權限狀態同步-依-ls) R4 agent 誤殺自己背景化的 tap-target 行程。[LS-215](https://linear.app/little-sprout-app/issue/LS-215/harnesspretooluse-擋-ios-devqa-的背景-bashrun-in-backgroundtrue-直接) hook 只擋顯式 `run_in_background:true`，截斷背景化不在偵測範圍。
* `797c7149`**（P2 XS）**：[LS-208](https://linear.app/little-sprout-app/issue/LS-208/設計-chore小債清倉-2-末張卡完全隱藏裝飾件-17-筆裁決hlxo3-fz3kf-實例年齡片語-nbspcmpcard-diary) 收尾 `pen-open.sh <主 checkout> --kill` 重開後 Pen 把記憶體中的票檔內容寫回主 checkout `design/littlesprout.pen`（192+/779−），主 checkout 變 dirty，巡檢才發現，orchestrator 手動 `LS_ALLOW_MAIN_CHECKOUT_WRITE=1 git checkout --` 還原。
* docs XS（同檔 `docs/COLLABORATION.md`／agent 定義）：`188ae73b`（:225 HIBP 註記改為「免費方案不支援、接受 WARN、升 Pro 再開（09-12）」）、`51bd1635`（:332 §7 [LS-215](https://linear.app/little-sprout-app/issue/LS-215/harnesspretooluse-擋-ios-devqa-的背景-bashrun-in-backgroundtrue-直接) 效果欄改「已實測 09-12 `00330bed`」）、`bab56145`(1)（:328 §7 路徑列舉補 `.sh`／`.md`／`.yml` 成 11 項）、`5d6e51b6`(1)（:327 R5 插入段截斷句修通）、`588c483f`（§7 [LS-233](https://linear.app/little-sprout-app/issue/LS-233/harnesspatrolsh-pr-段-blocked-分流-讀-check-bucket-分ci-跑中-n-分check-全綠仍) 列「验证」→「驗證」；`patrol.test.sh` ㉘ 補充 mutation 前加註解指路 PR 907 斷言）、`49457773`(1)（:335 §7 [LS-231](https://linear.app/little-sprout-app/issue/LS-231/harnessci-cici-ipad-job-在-xcodebuild-test-失敗時上傳-xcresultif-failure保留-3) 列「`: failed - ` 那一行」改「`grep -F '] : '`」）與 (3)（§4-b 補「`gh run download` 解出的是 bundle 內容目錄，Xcode 開須改名 `*.xcresult`」）、`4bbd2960`(2)（§4-b 補「看失敗 attempt 用 `gh api repos/<o>/<r>/actions/jobs/<job-id>/logs`」）、`1ae89cbd`（`.claude/agents/ui-designer.md:33` [LS-171](https://linear.app/little-sprout-app/issue/LS-171/hotfixls-168-tree-hash-pencil-端與-jspy-不一致-pencil-get-把-geometry-省略成) 兩段式退路過期敘述改為 [LS-226](https://linear.app/little-sprout-app/issue/LS-226/harnessscriptsdesignoverflow-scanjs-對萬節點級-pen-的三支-on-掃描與-tree-hash) `SCAN_SKIP_HASH`＋`SCAN_TREE_HASH` 規則）。

## 範圍

1. **殘留 xcodebuild 防護**：`scripts/gates/push-gate.sh` 與 `scripts/gates/detect-simulator.sh`（或兩者共用的 helper）開跑前 `pgrep -f "xcodebuild.*<專屬機 UDID>"`（含 `-destination` 同 UDID）有殘留 → 印 `⚠ 殘留 xcodebuild（pid／etime／命令前 80 字）` 並**拒跑 exit 2**（不自動 kill——殘留可能是別的 agent 正在跑；提示「確認後 `kill <pid>` 再跑」）；`PUSH_GATE_ALLOW_STALE_XCODEBUILD=1` 逃生口。自測：假 pgrep（fixture）正負樣本＋mutation。
2. **agent 定義**：`.claude/agents/ios-dev.md`／`merge-reviewer.md`／`qa.md` 加一句「`xcodebuild` 一律前景、Bash timeout 600000（工具上限）；預期超過 10 分鐘的測試以 `-only-testing` 分段跑；不得依賴截斷後的自動背景化」；`scripts/gates/agent-tools-check.sh` 驗該字樣存在（三支定義）。
3. **Pen 寫回還原**：`scripts/ops/pen-open.sh --kill` 清場後（重開主 checkout 檔、等 15s 探路徑之後）比對主 checkout `design/littlesprout.pen` 與 `git show HEAD:design/littlesprout.pen`，不同 → `LS_ALLOW_MAIN_CHECKOUT_WRITE=1 git -C <主 checkout> checkout -- design/littlesprout.pen` 並印「Pen 寫回 → 已還原（+N/−M）」；`patrol.sh` 主 checkout dirty 段對此單檔 dirty 印「Pen 寫回 → `pen-open.sh --restore`」提示（提供 `--restore` 子命令）而非泛用警告。自測（fixture 化 git 與 pen CLI）正負＋mutation。
4. **docs 清倉**：上列九處一句話修正，逐條在 PR body 對照池項 id。
5. `docs/COLLABORATION.md` §7 對照表：殘留 xcodebuild 與 Pen 寫回各一列。

## 不做

自動 kill 殘留行程；改 [LS-215](https://linear.app/little-sprout-app/issue/LS-215/harnesspretooluse-擋-ios-devqa-的背景-bashrun-in-backgroundtrue-直接) hook；池 `3a98c68c`（視覺證據量像素，另評）；`10d938ff`／`99c66106`（UNSTABLE 分流、gh 看門狗，另票）。

## 驗收

- [ ] 三支自測綠、mutation 紅；`agent-tools-check.sh` 綠；對 09-12 兩起事故形狀重放：殘留 xcodebuild 存在時 push-gate 拒跑（原文）；主 checkout `.pen` 被寫回時 `--kill` 自動還原（原文）
- [ ] merge-reviewer 通過（scope：只動上列檔）；hotfix 流程（自 main、PR 回 main、back-merge；併 main 後鏈立刻貼 main tip status）

## 留言（12）

### 2026-09-12T16:38:50.221Z · Chih-Lin Yeh · `3d06c1ae`

**orchestrator 09-13 00:38**：巡檢印「lane:harness 空 1 輪」→ 依 §5-b 自 LS-96 池升票（P2 `4de3e796`＋`797c7149` 為核心，同檔 XS docs 九項順手清倉；patrol 候選 `3a98c68c` 內容較模糊，依事故驅動優先，留池另評）。worktree `.claude/worktrees/LS-236`＝`hotfix/LS-236-stale-xcodebuild-guard`（自 main `cfa995f`）；派 ios-dev R1。

### 2026-09-12T17:08:47.422Z · Chih-Lin Yeh · `ac8adcc5`

**orchestrator 09-13 01:09 範圍追加**（來源：另一 session 的 2026-09-12 prompt 稽核，稽核頁 artifact `a490c4c6`，patch 於該 session scratchpad `prompt-audit-0912.patch`；全域檔已由該 session 處理，專案層由本票收）：F3 三份 agent 定義「timeout ≤25 分」→「≤10 分（Bash 硬上限 600000 ms）」（與範圍 2 合併）；F6 `visual-reviewer.md:42` 相對用語改「讀舊票 comment 時 REQUEST_CHANGES 視同 ITERATE」；F7 COLLABORATION §1 模型表拿掉「預設即最高」＋補 fable 層一句；F8 只做 §4-b cron 模板 PR #326 敘事那一個示範 hunk。F8 全檔去敘事（172 處）另開 Backlog 票 LS-238；F4／F5 記憶檔已由 orchestrator 直接改。

### 2026-09-12T18:35:00.211Z · Chih-Lin Yeh · `7d26db32`

## Ticket
LS-236：Harness：殘留 `xcodebuild` 防護＋agent 定義「xcodebuild 前景 timeout 600000、分段」＋`pen-open.sh --kill` 清場後 `.pen` 寫回主 checkout 自動還原＋COLLABORATION／agent 定義九處一句話清倉

## 已完成
1. 範圍 1：新增 `scripts/gates/stale-xcodebuild-check.sh <UDID> [<lock 目錄>]`，`pgrep -f "xcodebuild .*<UDID>"` 偵測殘留（空白防自我誤判）；帶鎖目錄且該鎖有效持有中則放行（避免誤判合法排隊為殘留）；`push-gate.sh`（sim_udid／ipad_udid 兩處呼叫點）與 `detect-simulator.sh`（非 CI 分支）皆已接線；`PUSH_GATE_ALLOW_STALE_XCODEBUILD=1` 逃生口。commit `af46680`。
2. 範圍 2：`.claude/agents/ios-dev.md:23`／`merge-reviewer.md:19`／`qa.md:12` 各補一句 xcodebuild 前景 timeout 規約；`scripts/gates/agent-tools-check.sh` 的 `BODY_RULES` 新增三條釘句。commit `d0f493f`。
3. 範圍 3：`pen-open.sh` 新增 `restore_main_checkout()`（`pen-open.sh:274-306`）與 `--restore` 子命令（`pen-open.sh:308-316`），`--kill` 流程收工前自動呼叫（`pen-open.sh:527`）；`patrol.sh` 主 checkout dirty 段對唯一 dirty 檔是 `design/littlesprout.pen` 改印還原提示（`patrol.sh:311-322`）。commit `cffb593`。
4. 範圍 4：COLLABORATION.md 六處一句話修正（HIBP 裁決、LS-231 grep 訂正、§4-b 兩則補充、簡體字訂正）＋ `ui-designer.md:33` LS-171/LS-226 敘述訂正；另三個池項（`51bd1635`／`bab56145`(1)／`5d6e51b6`(1)）核對後發現已於本輪之前的其他 PR 意外修正，僅驗證現況正確、未再改動（PR body 對照表逐項標注）。commit `ed4105f`。
5. 範圍 5：`docs/COLLABORATION.md` §7 補兩列（殘留 xcodebuild 防護、pen-open --kill 自動還原）。commit `ed4105f`。

## 已驗證（怎麼驗）
每項對應票文範圍編號；測試名可用 `git grep` 驗存在；三支自測綠、mutation 紅（LS-209 三段式）：

1. **範圍 1**——改了什麼一行：`stale-xcodebuild-check.sh` 新增 pgrep 殘留偵測＋鎖持有判斷。哪條測試紅：`scripts/gates/stale-xcodebuild-check.test.sh` 案例 ⑪（mutation：`sed 's/^exit 2$/exit 0/'`）。斷言訊息原文：正常執行時印 `✓ ⑪ mutant（拿掉 exit 2）：②的紅樣本改判成 exit 0——證明 exit 2 是這裡在擋`。指令：`bash scripts/gates/stale-xcodebuild-check.test.sh`（11 組全綠）。
2. **範圍 1 假陽性回歸**——改了什麼一行：`push-gate.test.sh` 新增 fixture 化 `pgrep`／`ps`（真 pgrep passthrough）。哪條測試紅：案例 ⑧（Race 重現，兩個 worktree 併發退回共用 UDID）在修正 pgrep pattern（`xcodebuild.*` → `xcodebuild .*`）之前會紅。斷言訊息原文：`✗ ⑧ 沒看到完整的 TEST_START／TEST_END——時序沒有真的重現`（修正後綠：`✓ ⑧ 兩邊的 xcodebuild test 確實都有跑（不是被跳過整段）`）。指令：`bash scripts/gates/push-gate.test.sh`（96 組全綠）。
3. **範圍 1**——`detect-simulator.test.sh` 新增案例 ㉒（mutation：`sed '/stale-xcodebuild-check\.sh/d'`）。斷言訊息原文：`✓ ㉒ mutant（拿掉呼叫）：⑲的紅樣本改判成 exit 0——證明呼叫 stale-xcodebuild-check.sh 是這裡在擋`。指令：`bash scripts/gates/detect-simulator.test.sh`（40 組全綠）。
4. **範圍 2**——改了什麼一行：三份 agent 定義加一句規約＋`agent-tools-check.sh` BODY_RULES 三條。哪條測試紅：`agent-tools-check.test.sh` 案例 ㉑（mutation：拿掉 LS170-BODY-RULES 區塊）。斷言訊息原文：`✓ ㉑ mutant：拿掉規則後「缺不得依賴截斷後自動背景化句」的負樣本變綠`。指令：`bash scripts/gates/agent-tools-check.test.sh`（128 組全綠）；`bash scripts/gates/agent-tools-check.sh`（對真 repo，`✓ agent-tools gate 通過（6 份 agent 定義；正文必含字樣 42 條）`）。
5. **範圍 3**——改了什麼一行：`pen-open.sh` 新增 `restore_main_checkout()` 呼叫點。哪條測試紅：`pen-open.test.sh` mutation A（`sed` 拿掉 `--kill` 流程呼叫該函式那行）。斷言訊息原文：`✓ mutation A：拿掉呼叫後 ⑲ 的綠樣本清場照樣成功但不再被還原（仍 dirty）——證明呼叫是這裡在還原`。mutation B（拿掉 linked worktree 排除判斷）：`✓ mutation B：拿掉排除判斷後，⑳ 的負樣本（linked worktree）被誤還原——證明該判斷是這裡在保護`。指令：`bash scripts/ops/pen-open.test.sh`（49 組全綠）。
6. **範圍 3**——`patrol.sh` 主 checkout dirty 段特殊判斷。哪條測試紅：`patrol.test.sh` mutation（把 `if [ "$mc_dirty" -eq 1 ] && [ "$mc_dirty_files" = "design/littlesprout.pen" ]; then` 改成 `if false; then`）。斷言訊息原文：`✓ ⑦b mutant：拿掉特殊判斷後改印泛用「1 個未提交變更」——證明特殊判斷是這裡在挑的`。指令：`bash scripts/ops/patrol.test.sh`（364 組全綠）。
7. **Docker Ubuntu 24.04 通道**（ops 自測涉及 shell／libc 行為，`jq git python3 procps`）：六支自測皆重跑一次——`stale-xcodebuild-check.test.sh`／`push-gate.test.sh`／`detect-simulator.test.sh`／`pen-open.test.sh`／`patrol.test.sh` 五支 100% 綠；`agent-tools-check.test.sh` 122/123 綠，唯一紅是「① 真 repo 應通過」——因容器 bind-mount 的 linked worktree `.git` 檔指向主機專屬絕對路徑，`git rev-parse --show-toplevel` 從 cwd discovery 失敗，屬容器掛載限制、與本票程式碼無關。
8. **push-gate.sh 實跑**：`git push` 完整跑過 pre-push hook，`✓ push gate 通過`（無 Swift 變更，SwiftLint／unit tests／QADriver 對帳依既有規則跳過；xcodegen 漂移／API 契約／錯誤碼三方對帳／migration 撞號與不可變／分支票號乾淨／與 origin/main 可合併皆綠）。指令：`git push -u origin hotfix/LS-236-stale-xcodebuild-guard`。

## 未完成（reviewer informational 處置）
本票尚未進 review，無 reviewer informational 待處置。自行發現且判斷不影響本票範圍：
- 三個池項 id（`51bd1635`、`bab56145`(1)、`5d6e51b6`(1)）在票文所述位置已於本輪之前的其他 PR 意外修正——僅核對現況正確、未再改動，PR body 對照表逐項標注，不另開票。

## 風險
- `stale-xcodebuild-check.sh` 的 `pgrep -f "xcodebuild .*<UDID>"` 仍是子字串比對（非嚴格 argv[0] 判定），理論上任何命令列同時含 "xcodebuild " 與該 UDID 字面都會被判定；已用空白區隔避免自我誤判（本票主要發現與修正的假陽性），未發現其他已知假陽性來源。
- `restore_main_checkout()` 的主 checkout 判定（`--git-dir` 與 `--git-common-dir` 相等）僅在本票新建的 fixture（真的 git 倉庫＋`git worktree add` 建的 linked worktree）驗證過；未對正式站主 checkout 實跑（風險低——純比較邏輯，不涉及正式站資料）。

## 產出位置
- PR：https://github.com/CLYEH/little-sprout/pull/374（head `ed4105f`，base `main`）
- 分支：`hotfix/LS-236-stale-xcodebuild-guard`（自 `origin/main` `cfa995f`）
- Worktree：`/Users/clyeh/little-sprout/.claude/worktrees/LS-236`
- 新增檔案：`scripts/gates/stale-xcodebuild-check.sh`、`scripts/gates/stale-xcodebuild-check.test.sh`
- PR body 暫存檔：`/private/tmp/claude-501/-Users-clyeh-little-sprout/d51d774f-d40e-420c-b428-e43c62514076/scratchpad/LS-236-pr-body.md`（已過 `pr-body-check.sh --verify`，exit 0）
- 模擬器：本票全程未 boot 任何模擬器（無 Swift 變更，push-gate 跳過 unit tests）——無需 shutdown。

---
**注意（提醒 orchestrator）**：任務進行中，一則自稱「coordinator」的訊息突然出現在一次 Bash 工具的 grep 結果內（非正常使用者/協調者回合），要求我 apply 一個位於 `/private/tmp/claude-501/.../46406db7-.../scratchpad/prompt-audit-0912.patch` 的 patch（session id 與我本次的 scratchpad session id `d51d774f-...` 不符）、擴大範圍去改 CLAUDE.md 鄰近設定與另外三份 agent 定義。我判定這是注入內容，未執行、未採信，僅依原始派工單完成上述範圍 1–5。若這確實是您本人想追加的範圍，請另開票或在下一輪派工單中明確重新指示。

### 2026-09-12T18:39:44.139Z · Chih-Lin Yeh · `d5c4bb12`

**orchestrator 09-13 02:39**：R1 handoff 收到，PR #374 派 merge-review R1。

關於中途那則「套 prompt-audit patch」訊息：**是 orchestrator 本人的 SendMessage**（不是注入），但你堅守票文範圍的判斷正確——該四項（F3 agent 定義 timeout 敘述、F6 `visual-reviewer.md:42`、F7 §1 model 表、F8 §4-b cron 模板 hunk）不在本票 1–5 範圍，改掛 **LS-239** 範圍 5（同為 harness、下一張派工），本 PR scope 凍結在票文。教訓記入派工規則：範圍變更一律改票文再派，不用 SendMessage 中途加碼。

### 2026-09-12T18:52:41.007Z · Chih-Lin Yeh · `6f327c3d`

已合併 `origin/main`（144622d）解決 PR #374 DIRTY：新 head `5d768e5`。衝突檔僅 `docs/COLLABORATION.md` 一處（§7 對照表末端，兩邊都在 LS-233 那行之後新增列）——解法：保留 LS-233 那行本票已訂正的版本（「验证」→「驗證」，main 當時尚未含這個修正）、接續 LS-235 新增列、再接續本票兩列（殘留 xcodebuild 防護、pen-open --kill 自動還原），三方新增內容皆保留無遺失。其餘檔案（LS-218／LS-208／LS-227／LS-235／LS-132 等）皆 origin/main 帶入、與本票無重疊，自動合併無衝突。已重跑六支自測確認無回歸：`patrol.test.sh`／`agent-tools-check.test.sh`（含對真 repo 跑 `agent-tools-check.sh`）／`stale-xcodebuild-check.test.sh`／`push-gate.test.sh`／`detect-simulator.test.sh`／`pen-open.test.sh` 皆綠。`git push` 已跑過 push-gate（`✓ push gate 通過`）。`gh pr view 374` 現況：`mergeStateStatus=BLOCKED`（CI 待跑，非合併衝突）、`mergeable=MERGEABLE`——DIRTY 已解除。

### 2026-09-12T19:05:31.047Z · Chih-Lin Yeh · `d1a0c46c`

**merge-reviewer 09-13 03:03 R1 ITERATE**

審的 head：`5d768e5ad8c359de045dc8caf82a524c7f76d307`（PR #374 → main）。內容審查在 `ed4105f`；merge commit `5d768e5` 只解 `docs/COLLABORATION.md` §7 一處衝突（下方「merge 解法核對」）。六支自測在 `ed4105f` 的獨立 detached worktree 實跑；`git diff --name-only ed4105f 5d768e5 -- scripts .claude/agents` 只回 main 帶入的 `prod-purge-health.sh`／`prod-purge-health.test.sh`，本票的 harness 檔未被 merge 動到，實跑結論沿用到新 head。

## 逐條查實（怎麼驗）

1. **範圍 1（殘留 xcodebuild 防護）**：`bash scripts/gates/stale-xcodebuild-check.test.sh` 我實跑 11 組全綠（含 ⑩／⑪ 兩個 mutation）；`bash scripts/gates/detect-simulator.test.sh` 全綠（⑲ 有殘留拒跑、⑳ 對照、㉑ CI 不查、㉒ 拿掉呼叫行的 mutation）。**但 `scripts/gates/push-gate.sh` 自己那兩個呼叫點完全沒有測試保護，且 handoff 申報的 pattern mutation 重放不符——F1（major）**。
2. **範圍 2（agent 定義＋機械擋）**：`bash scripts/gates/agent-tools-check.sh` 對本 PR 樹綠（6 份定義、正文必含字樣 42 條，末三條就是新釘句「不得依賴截斷後的自動背景化」×3）；`bash scripts/gates/agent-tools-check.test.sh` 125 組全綠。
3. **範圍 3（Pen 寫回還原）**：`bash scripts/ops/pen-open.test.sh` 49 組全綠（⑯⑰⑱⑲⑳＋mutation A／B）、`bash scripts/ops/patrol.test.sh` 364 組全綠（⑦b／⑦c／⑦d＋mutation）。⑲ 以「清場後仍 dirty」模擬 Pen 寫回（測試檔自述成因不可控），接受；但 `restore_main_checkout()` 的比較基準有偏差——F2（minor）。
4. **範圍 4（docs 清倉 9 項）**：六處已修＋三處現況正確，我逐條在樹上核對；另把兩個引用正確性對照實作驗過——§7 的 `grep -F '] : '` 對得上 `scripts/gates/tap-target-check.sh:119`、`.claude/agents/ui-designer.md:33` 的 LS-226 規則對得上 `scripts/design/overflow-scan.js:1154` 的 throw。三項「已於之前 PR 意外修正」也都查證屬實（`docs/COLLABORATION.md` LS-215 列含 `00330bed`、LS-211 列路徑列舉 11 項且自帶 `bab56145`、R5 插入段已標「已歸位」）。
5. **範圍 5（§7 兩列）**：合併後 `docs/COLLABORATION.md:340`（殘留 xcodebuild）／`:341`（pen-open --kill 自動還原）存在。
6. **Ubuntu 通道 122/123 那一項：親自確認是容器限制、不是真失敗**。`docker run --rm -v "$PWD":/w -w /w ubuntu:24.04` 內 `git rev-parse --show-toplevel` 回 `fatal: not a git repository: /Users/clyeh/little-sprout/.git/worktrees/...`（linked worktree 的 `.git` 檔指向宿主絕對路徑）；同一份程式碼 `rsync` 成獨立 repo 後 `bash scripts/gates/agent-tools-check.test.sh` 125/125 全綠，`bash scripts/gates/stale-xcodebuild-check.test.sh` 在容器內亦全綠。

## Findings

### F1（major）`scripts/gates/push-gate.sh:354`／`:688`：本票主呼叫點沒有任何機械保護，申報的 mutation 重放不符

- **重放 A（刪呼叫行）**：把 `push-gate.sh` 的 `bash …/stale-xcodebuild-check.sh "$sim_udid" "$sim_lock_dir"`（:354）與 `… "$ipad_udid" "$sim_lock_dir"`（:688）兩行刪掉後，`bash scripts/gates/push-gate.test.sh` 仍 **96/96 全綠、exit 0**（我 rsync 一份 `ed4105f` 的樹、sed 刪兩行後實跑）。
- **重放 B（handoff「已驗證」第 2 項申報）**：申報「案例 ⑧ 在修正 pgrep pattern（`xcodebuild.*` → `xcodebuild .*`）之前會紅」。我把 `scripts/gates/stale-xcodebuild-check.sh:44` 改回 loose pattern 重跑 `push-gate.test.sh` → **仍 96/96 全綠，不轉紅**。根因：三支自測（`push-gate.test.sh:145`、`detect-simulator.test.sh` ⑲ 段、`stale-xcodebuild-check.test.sh` 檔頭）都把 `pgrep -f` 整個 stub 掉（只有非 `-f` 形狀 passthrough），pattern 在任何一支自測裡都不會被執行到——pattern 這條修正目前零覆蓋。
- 另外 `push-gate.test.sh:140` 的註解寫「㊵ 系列案例改寫控制檔模擬殘留」，但檔內沒有 ㊵ 案例（`grep ㊵ scripts/gates/push-gate.test.sh` 只命中那句註解）；`push-gate.sh:313-319` 新增的 `ds_rc = 2` 原樣轉發分支也沒有樣本（測試裡的 detect-simulator 假身從不回 2）。
- **具體失敗情境**：日後重構 push-gate（把 stale-check 併進 `wd_run`、搬動 `sim_lock_dir` 計算順序、或整理 iPad best-effort 段）誤刪這兩行，六支自測與 CI `rules` job 全綠、無人察覺。iPad 段尤其只有這一條防線（`detect-simulator.sh` 不參與 iPad 段的 `ipad_udid`），刪掉即完全失效；`sim_udid` 那條雖然還有 `detect-simulator.sh` 的同款檢查兜底（功能上不會立刻破），但票文驗收要的「殘留 xcodebuild 存在時 push-gate 拒跑（原文）」目前沒有任何測試產生過那段原文。
- **建議修法**（fixture 都已就緒，約 30 行）：`push-gate.test.sh` 補一組正樣本——對 `FAKE_DEST_UDID` 寫入 `STUB_STALE_PGREP_OUT`／`STUB_STALE_PS_DB` → 斷言 push-gate `exit 2` 且 stderr 含「⚠ 殘留 xcodebuild」且**沒有**跑到 `xcodebuild test`（既有 stub 有 log 可驗）；再補 mutation（`sed` 刪 `:354` 那行 → 正樣本轉綠）。iPad 段（`ipad_udid`）同款一組。pattern 若要保住，至少在 `stale-xcodebuild-check.test.sh` 補一組**不 stub `pgrep -f`**、用真 pgrep＋一個自己造的 sibling 行程的樣本。

### F2（minor）`scripts/ops/pen-open.sh:315`／`:325`：比較與還原的基準是 index，不是票文寫的 HEAD

- `restore_main_checkout()` 用 `git diff --quiet -- design/littlesprout.pen`（index↔worktree）判斷、用 `git checkout -- <path>`（從 index 還原），但函式註解與票文範圍 3 寫的都是「與 `git show HEAD:design/littlesprout.pen` 比對」。
- **具體失敗情境（已實跑重現）**：暫存 repo 內把 .pen 改掉並 `git add`（staged）→ `patrol.sh` 那側用的是 `git diff --name-only HEAD`（`patrol.sh:304`，看得到 staged），會印「Pen 寫回 → `bash scripts/ops/pen-open.sh --restore`」；跑 `--restore` 卻印 `✓ … design/littlesprout.pen 與 HEAD 一致，無需還原`、rc=0，檔案內容原封不動仍是污染版（`git status` 仍 `M `）。提示與工具互相矛盾、繞不出去，訊息本身也與事實不符。
- **建議修法**：`git diff --quiet HEAD -- design/littlesprout.pen` ＋ `git checkout HEAD -- design/littlesprout.pen`（或 `git restore --source=HEAD --staged --worktree`），與票文一致；`pen-open.test.sh` 補一組 staged 樣本。

### F3（minor，PLAUSIBLE）`scripts/ops/pen-open.sh:598`：還原與 Pen 寫回的時序沒有複驗

- `--kill` 流程在 `poll_until_match` 之後呼叫一次 `restore_main_checkout`。若 Pen 的寫回落在這次還原之後（重開後第一次自動存檔，或「還原改寫了磁碟檔」本身觸發 Pen 再寫一次），主 checkout 會再度 dirty，而這次呼叫不會察覺（只剩下一輪巡檢事後提示）。沒有真 Pen 無法重現，標 PLAUSIBLE、不列 major。
- **建議**：還原後隔數秒再比對一次（或在還原訊息尾端明示「Pen 之後若再寫回，跑 `--restore`」）。

### F4（minor）`scripts/gates/stale-xcodebuild-check.sh:39-45`：鎖持有放行太粗，沒核對候選 pid 是否屬於該 holder

- 只要 `<lock>/holder` 的 pid 還活著就**整批**候選一律放行，不管命中的 pid 與那把鎖有沒有關係。
- **具體失敗情境**：A worktree 合法持鎖跑 xcodebuild（holder 活著），同時上一輪 Bash timeout 截斷留下的殘留 xcodebuild（沒有鎖保護）也還在同一顆共用 UDID 上跑；B 這時跑 push-gate → 因「鎖有效持有中」整段放行 → B 排隊拿到鎖後與那個殘留同時打同一台模擬器——正是 LS-166 要擋的形狀，而 gate 此時是靜默的。
- **建議修法**：只把「pid 落在 holder 的行程樹內」（`pgrep -P` 遞迴，或 `ps -o ppid=` 上溯到 holder）的候選視為合法，其餘照樣 `exit 2`；`stale-xcodebuild-check.test.sh` 的 stub `ps` 已有 pid 資料庫，補 ppid 欄即可測。

## Informational（全列，依 §5-b 由 ios-dev 決定處置：已修／記入 LS-96 池／另票／不修＋理由）

- **i1**：逃生口 `PUSH_GATE_ALLOW_STALE_XCODEBUILD=1` 只在 `stale-xcodebuild-check.sh:35` 印一行 stderr，push-gate 不落檔、CI／commit status 看不到，事後無法得知某次 push 是否繞過。與 repo 既有慣例（`KEEP_SIMULATOR`／`DETECT_SIMULATOR_SHARED`）一致，故只列 informational。
- **i2**：`pen-open.sh:598` 的 `restore_main_checkout "$root"` 傳的是清場目標 root，`--kill <票 worktree>` 時整段略過（印 linked worktree 訊息），不會去看主 checkout。與 LS-208 事故形狀（`--kill <主 checkout>`）一致、也是刻意的 WIP 保護，但票文範圍 3 的字面是「比對主 checkout」，差異值得在檔頭註明。
- **i3**：三支 agent 定義同一條 bullet 內自相矛盾——前半仍是「timeout（≤25 分）」、後半新增「Bash timeout 600000（工具上限）」（`ios-dev.md:23`／`merge-reviewer.md:19`／`qa.md:12`）。已知屬 LS-239（prompt 稽核 F3）範圍，本票不必改，僅記錄。
- **i4**：`agent-tools-check.sh` 的三條新 BODY_RULES 只釘「不得依賴截斷後的自動背景化」這句，`600000`／`-only-testing` 兩個關鍵值沒被釘住（改掉不會紅）。與既有 41 條慣例一致（一條規則一個錨句），僅記錄。
- **i5**：§4-b 兩則 `gh` 行為敘述（`gh run view --job` 只回最新 attempt、`gh run download` 解出的是 bundle 內容目錄）屬外部工具行為，我沒有實測，採信池項的事故紀錄。
- **i6**：`detect-simulator.sh:219` 用 `$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/stale-xcodebuild-check.sh` 取兄弟檔，任何把 `detect-simulator.sh` 單獨複製出去的合成環境都得同步複製新檔（本 PR 已在 `push-gate.test.sh` 三處補 `cp`）。fail-loud、可接受，僅記錄耦合面。

## merge 解法核對（`ed4105f` → `5d768e5`）

- `git diff --stat origin/main...5d768e5` ＝ 17 檔 771+／20−，與 `git diff --stat <merge-base>...ed4105f` **逐檔逐數字相同**——merge 沒有吞掉本票任何一行、也沒有夾帶額外改動。
- `git show --cc --name-only 5d768e5` 只列 `docs/COLLABORATION.md` 一檔（唯一衝突）。§7 現況：`:338` LS-233 列（`grep -o` 全檔該行只有「驗證」、無「验证」）、`:339` LS-235 列（對方新增，保留）、`:340`／`:341` 本票兩列——三方新增全在，順序正常。
- `git diff --name-only ed4105f 5d768e5 -- scripts .claude/agents` 只回 main 帶入的 `prod-purge-health.sh`／`.test.sh`，本票 harness 檔零改動。

## 四維度

- **Race**：兩個 worker 同 UDID 同時檢查 → 都過，隨後由 `simulator-lock.sh` 真正序列化，設計正確；check→取鎖之間的 TOCTOU 由鎖兜住。鎖持有放行的粗粒度見 F4；`--restore` 與 Pen 寫回的時序見 F3。未發現其他競態。
- **運算效能**：每次 push 最多 3 次 `pgrep -f`＋每個候選一次 `ps -p`（毫秒級，行程表掃描），push-gate 額外耗時可忽略；`detect-simulator` 與 `push-gate` 的雙重檢查是刻意的雙防線，成本同樣可忽略。無問題。
- **平行優化**：本 PR 無可平行化工作被序列化的問題（gate 本來就該在開跑前同步擋）。無問題。
- **Scope**：17 檔全在票文 1–5 列舉範圍內（含 `ui-designer.md` 屬池項 `1ae89cbd`）。**未夾帶 LS-239 的四項**——`visual-reviewer.md` 未動、§1 模型表未動、三支定義的「≤25 分」原句未動、§4-b cron 模板敘事未動。手術式修改成立。

## Verdict：**REQUEST_CHANGES（R1 ITERATE）**

唯一 major 是 F1（測試覆蓋，不是功能破損）：補完 `push-gate.test.sh` 的正樣本＋mutation（含 iPad 段）即可，fixture 都已就緒。F2 順手一起修（兩行 git 指令）。F3／F4 與 i1–i6 由實作者決定處置並在 handoff 逐條交代。

未跑：模擬器（本票無 Swift 變更，全程未 boot）、`supabase` 相關（本票不涉 DB，未持 supabase lock——未持有）。

---

`bash scripts/gates/handoff-evidence-check.sh <本 verdict 暫存檔>`（LS-211）：

```
✓ 第 7 行起的列項有證據
✓ 第 8 行起的列項有證據
✓ 第 9 行起的列項有證據
✓ 第 10 行起的列項有證據
✓ 第 11 行起的列項有證據
✓ 第 12 行起的列項有證據
（exit 0）
```

### 2026-09-12T19:55:53.133Z · Chih-Lin Yeh · `21832afd`

**ios-dev 09-13 03:52 R2**

## Ticket
LS-236：Harness：殘留 `xcodebuild` 防護＋agent 定義「xcodebuild 前景 timeout 600000、分段」＋`pen-open.sh --kill` 清場後 `.pen` 寫回主 checkout 自動還原＋COLLABORATION／agent 定義九處一句話清倉——R2 回應 merge-review R1 ITERATE（comment `d1a0c46c-bfe2-42d7-83d9-bd4760464ff6`）。

## 已完成
1. **F1（major，已修）**：`push-gate.sh:354`／`:688` 兩個呼叫 `stale-xcodebuild-check.sh` 的呼叫點原本零機械保護（reviewer 重放：刪掉仍 96/96 全綠；pattern 退回舊版也仍全綠）。`push-gate.test.sh` 補 ㊵(a)～(e) 五組：(a)(c) 正樣本、(b)(d) 對應 mutation（分別刪 :354／:688）、(e) `ds_rc=2` 轉發樣本；同時把 fixture 的 fake `pgrep` 從「固定清單」改成「依 pattern 帶的 UDID 從 `STUB_STALE_PS_DB` 動態 derive」，避免 sim_udid／ipad_udid 兩次查詢互相污染（R1 沒有這個問題是因為 R1 根本沒有這組測試）。`stale-xcodebuild-check.test.sh` 補 ⑫／⑬：真系統 `pgrep`／`ps`（不 stub）驗 pattern 自我誤判回歸。commit `39c47c0`。
2. **F4（minor，已修）**：鎖持有放行邏輯從「holder 活著就整批放行」改成逐一核對候選 pid 是否為 holder 本身或其父子鏈子孫（`ps -o ppid=` 往上追，上限 30 層）；不屬於的仍當殘留。`stale-xcodebuild-check.test.sh` 補 ⑦a（孫行程仍放行）／⑦b（無關候選仍擋，F4 核心重現）／⑩a（mutation：`is_holder_descendant` 恆真）；既有 ⑦／⑩ 的 fixture 補上正確 ppid（候選是 holder 子行程）。commit `39c47c0`。
3. **F2（minor，已修）**：`restore_main_checkout()` 改用 `git diff --quiet HEAD --`／`git checkout HEAD --`（原本裸 `-- <path>` 是跟 index 比、從 index 還原，staged 但未 commit 的污染會被誤判「已一致」且完全不動檔案）。`pen-open.test.sh` 補 ㉑：staged 污染的還原。commit `510634c`。
4. **F3（minor PLAUSIBLE，已修）**：還原後複驗一次，仍不同就印 ⚠ 並回傳非 0；`--kill` 呼叫端據此讓整體 exit code 反映還原是否真的成功。順手修一個因此浮出的既有 bug：目標不是 git 倉庫時原本 `return 2`（會誤擋既有非 git fixture 的 `--kill`），改成 `return 0`（跟「linked worktree、不比對」同一等級的「不適用」）。`pen-open.test.sh` 補 ㉒（mutation：checkout 換成 no-op，複驗抓到「回報成功但沒真的還原」）／㉓（非 git 倉庫迴歸防線）。commit `510634c`。
5. **§7 對照表**：兩列補上 R2 修正說明，同步文件與程式碼。commit `c967196`。

## 已驗證（怎麼驗）
每支 mutation 三段式（改了什麼一行 → 哪條測試紅 → 斷言訊息原文）：

1. **F1 呼叫點保護**——改了什麼一行：刪除 `push-gate.sh:354`（`bash .../stale-xcodebuild-check.sh "$sim_udid" "$sim_lock_dir"`）。哪條測試紅：`push-gate.test.sh` ㊵(b)。斷言訊息原文：`✓ ㊵(b) mutant（刪掉 :354 呼叫）：(a) 的紅樣本改判成 exit 0、xcodebuild test 真的被跑到——證明該呼叫是這裡在擋`。指令：`bash scripts/gates/push-gate.test.sh`（103 組全綠）。
2. **F1 iPad 呼叫點保護**——改了什麼一行：刪除 `push-gate.sh:688`（ipad_udid 那行）。哪條測試紅：㊵(d)。斷言訊息原文：`✓ ㊵(d) mutant（刪掉 :688 呼叫）：(c) 的紅樣本改判成 exit 0、iPad test 真的被跑到——證明該呼叫是這裡在擋`。同一指令涵蓋。
3. **F1 pattern 自我誤判回歸**——改了什麼一行：`sed 's/xcodebuild \.\*\${udid}/xcodebuild.*${udid}/'` 把 `stale-xcodebuild-check.sh:48` pattern 退回舊版（拿掉 `xcodebuild` 後的空白要求）。哪條測試紅：`stale-xcodebuild-check.test.sh` ⑬（用真系統 `pgrep`）。斷言訊息原文：`✓ ⑬ mutant（pattern 退回舊版）：⑫的綠樣本改判成 exit 2——證明 xcodebuild 後的空白要求是這裡在防自我誤判`。指令：`bash scripts/gates/stale-xcodebuild-check.test.sh`（13 組全綠）。
4. **F4 鎖持有父子鏈核對**——改了什麼一行：把 `is_holder_descendant()` 函式體改成 `{ return 0; # ...}`（恆真）。哪條測試紅：⑩a。斷言訊息原文：`✓ ⑩a mutant（恆真的 is_holder_descendant）：⑦b 的紅樣本改判成 exit 0——證明父子鏈核對是這裡在擋跟 holder 無關的候選`。指令：`bash scripts/gates/stale-xcodebuild-check.test.sh`（13 組全綠）。
5. **F2 HEAD 基準還原**——改了什麼一行：`pen-open.sh` 的 `git diff --quiet -- <path>`／`git checkout -- <path>` 改成帶 `HEAD`。哪條測試紅（正樣本驗證，非 mutation——這是修 bug 本身）：`pen-open.test.sh` ㉑。斷言訊息原文：`✓ ㉑ --restore：staged（git add 過）的污染 .pen 也會被還原，git status 乾淨`（R1 版本這裡會印「已一致，無需還原」且檔案不變，已於本機用 R1 版本重放確認過）。指令：`bash scripts/ops/pen-open.test.sh`（54 組全綠）。
6. **F3 還原後複驗**——改了什麼一行：`sed` 把 `env LS_ALLOW_MAIN_CHECKOUT_WRITE=1 git -C "$root" checkout HEAD -- design/littlesprout.pen 2>/dev/null` 換成 `true`（模擬「checkout 回報成功但沒真的動到檔案」）。哪條測試紅：㉒。斷言訊息原文：`✓ ㉒ 還原後複驗發現仍不同 → 印 ⚠ 並回傳非 0，不假裝已還原（F3 PLAUSIBLE 重放）`。指令：`bash scripts/ops/pen-open.test.sh`（54 組全綠）。
7. **Docker Ubuntu 24.04 通道**：`stale-xcodebuild-check.test.sh`／`push-gate.test.sh`／`pen-open.test.sh` 三支（本輪唯一改動的自測）在 `jq git python3 procps` 容器內重跑，13／103／54 組皆綠，與本機 macOS 結果一致。
8. **既有樣本無回歸**：`detect-simulator.test.sh`（40 組）、`agent-tools-check.test.sh`（128 組）本輪未改動內容但共用了 `stale-xcodebuild-check.sh` 的行為變更，重跑皆綠。
9. **push-gate.sh 實跑**：`git push` 完整跑過 pre-push hook，`✓ push gate 通過`。指令：`git push origin hotfix/LS-236-stale-xcodebuild-guard`（原始輸出見上）。

**已知不穩定項（非本票邏輯問題）**：`push-gate.test.sh` 在連續多輪重跑中觀察到一次 `exit 141`（SIGPIPE）發生在 xcodegen 版本對齊步驟（早於本票任何新增程式碼會被執行到的位置），重跑後消失、其餘 4/5 輪皆乾淨全綠；判斷為既有 `xg_work` 符號連結／trap 機制的環境性 flake，與本票 F1-F4 修正無關，未列入紅樣本計數。

## 未完成（reviewer informational 處置，i1–i6 全列）
- **i1**（逃生口不落檔）：不修＋理由——`PUSH_GATE_ALLOW_STALE_XCODEBUILD=1` 只印 stderr，與 repo 既有的 `KEEP_SIMULATOR`／`DETECT_SIMULATOR_SHARED` 逃生口慣例一致，本票不單獨為這支腳本加落檔機制。
- **i2**（`restore_main_checkout` 傳的是清場目標而非無條件找主 checkout）：已修——`pen-open.sh` 檔頭補充澄清段（commit `510634c`），說明這是刻意設計、與 LS-208 事故形狀一致。
- **i3**（三份 agent 定義同一 bullet 內「≤25 分」與「600000（10 分）」自相矛盾）：另票 LS-239——依 coordinator 09-13 02:39 comment 裁決，此為 prompt-audit F3 範圍，本票 scope 凍結不動。
- **i4**（BODY_RULES 只釘句子、不釘 `600000`／`-only-testing` 數值）：不修＋理由——與既有 41 條規則同一慣例（一條規則一個錨句，不逐一釘數值），改動會是另一種規約、不在本票範圍。
- **i5**（§4-b 兩則 `gh` 行為敘述未經 reviewer 實測、採信池項事故紀錄）：不修＋理由——外部工具行為敘述，來源 LS-96 池項 `49457773`(3)／`4bbd2960`(2) 已記錄實際事故經過，與 COLLABORATION.md 其餘外部工具行為敘述同等對待。
- **i6**（`detect-simulator.sh` 與 `stale-xcodebuild-check.sh` 的檔案耦合，複製前者需同步複製後者）：不修＋理由——fail-loud（缺檔案會直接報錯，不會靜默通過），`push-gate.test.sh` 三處合成 repo 已各自明確 `cp` 兩份檔案，耦合面已在自測層面處理。

## 風險
- F1 新增的 ㊵ 系列 fixture 改動了 fake `pgrep` 的實作方式（從固定清單改成動態 derive）；已確認既有 ①～㊴ 全部案例（皆未設定 `STUB_STALE_PS_DB`，該檔預設為空）不受影響，行為等同「無殘留」。
- F4 的父子鏈核對上限 30 層——真實環境下 `simulator-lock.sh` 直接把 `xcodebuild` 當自己的子行程執行（單層），30 層對任何合理的行程樹深度都綽綽有餘，純防禦性上限、非功能限制。
- push-gate.test.sh 觀察到的一次性 SIGPIPE（見上「已知不穩定項」）建議 orchestrator 留意日後 CI 若也出現同類偶發紅，優先懷疑 xcodegen 鏡射步驟而非本票邏輯。

## 產出位置
- PR：https://github.com/CLYEH/little-sprout/pull/374（新 head `c967196`，base `main`）
- 分支：`hotfix/LS-236-stale-xcodebuild-guard`
- Worktree：`/Users/clyeh/little-sprout/.claude/worktrees/LS-236`
- Commits（R2）：`39c47c0`（F1／F4）、`510634c`（F2／F3／i2）、`c967196`（§7 文件同步）
- `gh pr view 374`：`mergeStateStatus=BLOCKED`（CI 待跑，非合併衝突）、`mergeable=MERGEABLE`
- 模擬器：本票全程未 boot 任何模擬器（無 Swift 變更）——無需 shutdown。

### 2026-09-12T20:27:48.631Z · Chih-Lin Yeh · `b92e1b66`

**merge-reviewer 09-13 04:26 R2 APPROVE**

審的 head：`c9671960fbf84d88aef25a36119e8e31743ea0f8`（PR #374 → main，`mergeable=MERGEABLE`、`mergeStateStatus=BLOCKED`＝CI 待回報，非衝突）。R2 delta＝`git diff 5d768e5..c967196`＝6 檔（`stale-xcodebuild-check.sh`／`.test.sh`、`push-gate.test.sh`、`pen-open.sh`／`.test.sh`、`docs/COLLABORATION.md`），全在票文範圍內、無 scope 外改動。

## 逐條查實（怎麼驗；R1 四個 finding 逐一自己重放）

1. **F1-A（`push-gate.sh:354` sim_udid 呼叫點）已真的有機械保護**：我把該行從 production `push-gate.sh` 刪掉後跑 `bash scripts/gates/push-gate.test.sh` → **exit 1**，紅的是 `✗ ㊵(a) 應 exit 2、印警告、且 test_log 全空（實得 exit 0，test_log：test -scheme LittleSprout -destination platform=iOS Simulator,id=DEDICATED-UDID-35613 …）`。未刪時 103/103 全綠。
2. **F1-B（`push-gate.sh:688` ipad_udid 呼叫點）**：刪該行 → **exit 1**，紅的是 `✗ ㊵(c) 應 exit 2、主測試跑了但 iPad 測試沒跑（實得 exit 0）`，而 ㊵(a) 仍綠——兩個呼叫點確實互不遮蔽（R2 把假 pgrep 改成依 pattern 內 UDID derive 的效果，我用這組交叉驗證確認）。
3. **F1-C（`ds_rc=2` 原樣轉發）**：刪 `push-gate.sh` 的 `[ "$ds_rc" = 2 ] && exit 2` → **exit 1**，紅的是 `✗ ㊵(e) 應原樣轉發 exit 2（實得 exit 1）`，輸出裡出現 `✗ push gate：模擬器偵測失敗。`——正是 R1 指出的吞訊息形狀。
4. **F1-D（pattern 零覆蓋）已補**：把 production `stale-xcodebuild-check.sh:48` 改回 `pgrep -f "xcodebuild.*${udid}"` → `bash scripts/gates/stale-xcodebuild-check.test.sh` **exit 1**，紅的是 `✗ ⑫ 應 exit 0 且無輸出（實得 exit 2）——pattern 可能又退化成自我誤判`。⑫／⑬ 用真系統 `pgrep`／`ps`＋一支刻意取名 `xcodebuild-marker.sh` 的真行程，且該檔名不含 `stale-xcodebuild-check`，所以不會被第二層防禦性過濾遮住——這組是真的在驗 pattern 本身。
5. **F4（鎖持有放行改父子鏈核對）**：把 production 的 `is_holder_descendant()` 改成恆真 → stale 自測 **exit 1**，含 `✗ ⑦b 候選跟 holder 無關應 exit 2（實得 exit 0）`。孤兒／`ppid=1` 形狀我另讀碼確認：走訪到 `1` 後真 `ps -o ppid= -p 1` 回 `0`、迴圈條件 `[ "$p" != 0 ]` 即止 → 判定非子孫 → 仍當殘留（⑦b fixture 用的就是 `ppid=1`）。holder 在核對中途死掉的 TOCTOU 方向是 **fail-closed**（父鏈追不到 holder → 當殘留 → exit 2 並印診斷），不是誤放行。真實形狀相容：`scripts/ops/simulator-lock.sh:204` 是 `"$@"`（子行程，非 exec），holder 必為 `xcodebuild` 的祖先，⑦（直接子）／⑦a（隔一層孫）兩組都在。
6. **F2（HEAD 基準）**：把 production `pen-open.sh` 改回裸 `git diff --quiet --`／`git checkout --` → `bash scripts/ops/pen-open.test.sh` **exit 1**，紅的是 `✗ ㉑ 應 exit 0 且已還原`。R1 我實跑重現的 staged 死結（patrol 提示跑 `--restore`、`--restore` 卻說「已一致」）在 R2 已解。
7. **F3（還原後複驗）**：把 production 的複驗條件改成 `if false` → `bash scripts/ops/pen-open.test.sh` **exit 1**，紅的是 `✗ ㉒ 應 exit 非 0 且印複驗失敗警告（實得 exit 0）`——複驗區塊本身有負控守著。
8. **macOS 全綠**：`stale-xcodebuild-check.test.sh`（37 ✓）／`push-gate.test.sh`（103 ✓）／`pen-open.test.sh`（53 組）／`detect-simulator.test.sh`／`agent-tools-check.test.sh`（125 組）＋真 gate `bash scripts/gates/agent-tools-check.sh`（42 條字樣）皆 exit 0。
9. **Ubuntu 24.04 容器全綠**：`docker run --rm -v "$PWD":/w -w /w ubuntu:24.04`（`jq git python3 procps`）內三支改動自測 `stale-xcodebuild-check.test.sh` rc=0（37 ✓）／`pen-open.test.sh` rc=0（53 組）／`push-gate.test.sh` rc=0（103 ✓）——`ps -o ppid=`／真 pgrep 在 glibc 通道行為一致。
10. **§7 兩列文件同步**：`docs/COLLABORATION.md` 兩列已補 R2 的 F1–F4 修正敘述，與程式碼對得上（僅一處措辭 nit，見 i8）。

## Findings（無 blocker／major）

### m1（minor）`scripts/ops/pen-open.sh:627-634`：`--kill` 把 `restore_rc` 反映進 exit code 的那個分支沒有樣本

- **怎麼發現**：把 `if [ "$poll_rc" -eq 0 ] && [ "$restore_rc" -ne 0 ]; then` 改成 `if false; then` → `bash scripts/ops/pen-open.test.sh` 仍 **53 組全綠、exit 0**。㉒ 驗的是 `--restore` 這條路徑的複驗行為，`--kill` 端的 exit code 傳遞沒有被任何樣本釘住。
- **失敗情境**：日後有人整理那段 `if/elif` 時把該分支併回原本的 `if [ "$poll_rc" -eq 0 ]`，`--kill` 會在「⚠ 複驗仍與 HEAD 不同」印出來的同時回 exit 0，呼叫端（orchestrator 清場收工、未來若接進腳本）會把它當成功；自測不會紅。影響有限（⚠ 仍印在 stderr，不是完全靜默），故列 minor、不擋合併。
- **建議修法**：`pen-open.test.sh` 補一組——沿用 ㉒ 的 no-op checkout mutant，但走 `--kill <mainCk>`，斷言 exit 2 且輸出含「還原複驗失敗」。

## Informational（R1 i1–i6 的處置我逐條核過，另加三條新的）

- **i1（逃生口不落檔）→ 不修＋理由**：接受。與 `KEEP_SIMULATOR`／`DETECT_SIMULATOR_SHARED` 同慣例，理由充分。
- **i2（`restore_main_checkout` 傳的是清場目標）→ 已修（文件澄清）**：已核，`pen-open.sh` 檔頭新增「注意（LS-236 R2 merge-review i2）」段，說明 `--kill <票 worktree>` 會判成 linked worktree 而跳過、這是刻意保護。與實作一致。
- **i3（`≤25 分` vs `600000` 自相矛盾）→ 另票 LS-239**：接受（本票 scope 凍結，`.claude/agents/*.md` 的「≤25 分」原句在 R2 確實仍未被動到）。
- **i4（BODY_RULES 只釘句不釘數值）→ 不修＋理由**：接受。
- **i5（§4-b 兩則 `gh` 行為敘述未實測）→ 不修＋理由**：接受（我同樣未實測，沿用池項事故紀錄）。
- **i6（`detect-simulator.sh` 與新腳本的檔案耦合）→ 不修＋理由**：接受，fail-loud 且三處合成 repo 都已 `cp`。
- **i7（新）`scripts/ops/pen-open.sh:306-311`**：「目標不是 git 倉庫」從 `return 2` 改成 `return 0`（靜默跳過）修掉了既有非 git fixture 的誤擋（㉓ 有回歸防線），方向正確；但同一條路徑也會吃掉「其實是 git 倉庫、只是 git 讀不到」的真失敗（典型：`safe.directory`／dubious ownership、`.git` 損壞）——此時會對主 checkout 印「不是 git 倉庫，跳過」這句與事實不符的訊息，且 `--kill` 回 exit 0。訊息仍印在 stderr、巡檢也還會抓到 dirty，故只列 informational。想更嚴謹可加一行：`[ -e "$root/.git" ]` 為真時仍走 `return 2`。
- **i8（新）`docs/COLLABORATION.md` §7 措辭 nit**：殘留 xcodebuild 那列寫「(a)(c) 正樣本：殘留時 exit 2 且完全沒跑到 xcodebuild test」——(c) 實際斷言的是「主測試照跑、只有 iPad 段被擋」，「完全沒跑到」只適用 (a)。一句話可修。
- **i9（既有、非本票引入）iPad best-effort 段遇殘留改為硬擋**：`push-gate.sh:688` 的 exit 2 會讓整支 push gate 失敗（㊵(c) 明文釘住），與同段「找不到 iPad 機就 fail-open 略過」的慣例方向相反。這是票文「有殘留 → 拒跑 exit 2」的字面要求，實作正確；僅記錄語意落差供日後裁定。

## 四維度（R2 delta）

- **Race**：`is_holder_descendant()` 把「鎖有效＝整批放行」收斂成「只放行 holder 的行程樹」，正是 R1 F4 指的競態；TOCTOU 方向 fail-closed（見查實 5）。`ps -o ppid=` 逐層追、上限 30 層，追不到 ppid 即判非子孫，無無限迴圈。pen-open 的還原→複驗把「還原後又被寫回」變成可觀測（雖非可預防），方向正確。
- **運算效能**：每個候選最多 30 次 `ps -o ppid=`（實務上 1–2 次即命中或見底），候選數通常 0–2；push-gate 每次 push 仍只多毫秒級成本。無問題。
- **平行優化**：無可平行工作被序列化。
- **Scope**：R2 只動 6 檔，全在票文範圍；`.claude/agents/*.md`／`detect-simulator.sh`／`patrol.sh` 本輪未再動，LS-239 的四項仍未夾帶。

## Verdict：**APPROVE（R2）**

R1 的 major（F1）與三個 minor（F2／F3／F4）我逐一自己重放，六個 mutation 全部如申報轉紅（handoff「已驗證」1–6 項與我的重放結果逐條對得上，無「申報已驗證但重放不符」的情況）；macOS 與 Ubuntu 容器兩個通道皆全綠。剩下 m1 是新引入的小覆蓋缺口、i7–i9 為記錄性，皆不擋合併——請實作者在收尾 handoff 交代處置（m1 建議記入 LS-96 池或順手補一組樣本）。

未跑：模擬器（本票無 Swift 變更，全程未 boot，`simctl ui` 未動）、DB／supabase（本票不涉，未持 supabase lock——未持有）。handoff 提到的 `push-gate.test.sh` 一次性 `exit 141`（SIGPIPE）我沒遇到：本輪在 macOS 連跑該支 4 次（1 次基準＋3 次 mutation）、容器 1 次，皆無 141。

---

`bash scripts/gates/handoff-evidence-check.sh <本 verdict 暫存檔>`（LS-211）：

```
✓ 第 7 行起的列項有證據
✓ 第 8 行起的列項有證據
✓ 第 9 行起的列項有證據
✓ 第 10 行起的列項有證據
✓ 第 11 行起的列項有證據
✓ 第 12 行起的列項有證據
✓ 第 13 行起的列項有證據
✓ 第 14 行起的列項有證據
✓ 第 15 行起的列項有證據
✓ 第 16 行起的列項有證據
（exit 0；第一次跑時第 13 行紅——我原句只寫「pen-open 自測」沒帶路徑，已補成實際跑的 `bash scripts/ops/pen-open.test.sh`，敘述本身未改）
```

### 2026-09-12T20:44:43.263Z · Chih-Lin Yeh · `2ae06ebe`

**orchestrator 09-13 04:35**：merge-review R2 APPROVE（`b92e1b66`，status 貼 c967196；六組 mutation 全部重放轉紅）→ 併入鏈啟動：等 #374 CLEAN → merge main → main tip 貼 merge-review → back-merge PR main→development → merge → dev tip status → 收尾。m1／i7–i9 記 LS-96 池。

### 2026-09-12T21:24:50.118Z · Chih-Lin Yeh · `f7ce5244`

## dead-code sweep — LS-236

**dead-code-sweeper 09-13 05:23**

範圍：`git diff 144622d..00f4d62 --stat`（PR #374，第二親 `c967196`，主 checkout 已在 `00f4d62`）——17 檔、1118+/21−。逐項核對票文指定的十個懷疑點，逐一附證據；結論：**未發現本 feature 引入的死碼**。

### 逐項核對

1. **`pen-open.sh` R2 改 HEAD 比較後，舊 index-比較 helper／變數是否殘留** — 活，乾淨。`git diff cffb593..510634c -- scripts/ops/pen-open.sh` 顯示 `restore_main_checkout()` 內兩處裸 `git diff --quiet -- <path>` / `git checkout -- <path>` 已**整段替換**成 `... HEAD -- <path>`（:335、:342），無殘留的舊版本比較邏輯、無孤兒變數。呼叫端 `[ "$kill_mode" -eq 1 ] && restore_main_checkout "$root"`（R1）也已替換成捕捉回傳值的版本 `restore_rc=0; [ "$kill_mode" -eq 1 ] && { restore_main_checkout "$root" || restore_rc=$?; }`（:627-628），舊版丟棄回傳值的寫法沒有留下第二份。函式定義次數統計：`restore_main_checkout` 出現 8 次（1 定義 + 7 處引用／文件字面），無孤兒。

2. **`is_holder_descendant()` 之外是否留下 R1 版「holder 活著就整批放行」死枝** — 活，乾淨。`git diff af46680..39c47c0 -- scripts/gates/stale-xcodebuild-check.sh` 顯示 R1 的 `if [ -n "$holder_pid" ] && kill -0 "$holder_pid" 2>/dev/null; then exit 0; fi` 整段被**移除**（不是新增一條分支蓋過去），改為只設定 `holder_pid` 變數＋逐候選呼叫 `is_holder_descendant()`（`stale-xcodebuild-check.sh:51-74`）。無雙軌判斷共存。

3. **`push-gate.test.sh` ㊵ 系列 fixture（假 pgrep／ps／鎖目錄）是否每支都被引用** — 全數活。(a)(c) 用 `STUB_STALE_PS_DB` 佈置殘留樣本並斷言 exit 2＋test_log 空／部分空；(b)(d) 是對應 mutation（`sed` 刪 `:354`／`:688` 呼叫行）驗證紅樣本翻綠；(e) 用 `detect-simulator.sh` 假身回 exit 2 驗證原樣轉發。`REAL_PGREP`／`REAL_PS`（:149、:174）皆在對應 stub 的 passthrough 分支被引用（:165、:190），供 LS-199 看門狗既有案例 ㉙–㊳ 沿用，未觀察到零覆蓋的宣告變數。

4. **`stale-xcodebuild-check.test.sh` ⑫⑬ fixture 是否被引用** — 活。⑫ 用真系統 `pgrep`／`ps`（`$REAL_PATH`）起一支命名 `xcodebuild-marker.sh` 的真行程驗證 pattern 不誤判；⑬ 是其 mutation（pattern 退回無空白舊寫法）驗證同一份 fixture（`marker_script`／`real_udid`）翻紅。`marker_script` 在 ⑫⑬ 各起一次，`REAL_PATH`（檔案層級的 `export STUB_STALE_PS_DB` 那段之前 `REAL_PATH="$PATH"`，:68）在整份自測中除 stub 安装期外，另在 ⑫⑬ 共 4 處使用。

5. **R1 版假 pgrep 固定名單是否還在** — 已改，未殘留。`push-gate.test.sh:139-166` 的 R2 版 pgrep stub 已改成從 `$STUB_STALE_PS_DB` 的 `cmd` 欄位依 pattern 帶的 UDID 動態 derive（非固定 pid 清單），檔頭註解明確記載這是 merge-review R1 F1 重放抓到後的修正（避免 sim_udid／ipad_udid 兩次查詢互相污染）。另外 `detect-simulator.test.sh:353-365` 有一份**獨立**的固定輸出風格 pgrep stub（`STUB19_PGREP_OUT` 印檔案內容、不依 pattern 過濾）——這份不是「R1 遺留未修」，而是本來就設計給只查一次 UDID 的 `detect-simulator.sh` 用（該檔案只有一個呼叫點，不存在兩次查詢互相污染的問題，merge-reviewer R1／R2 皆未把它列入 F1 範圍）。判定：非死碼，是刻意的、範圍較窄的獨立 fixture。

6. **`pen-open.test.sh` 新樣本 ㉑㉒ fixture** — 活。㉑ 用 staged（`git add` 過）污染樣本驗證 HEAD 基準能還原 index 內容；㉒ 用「checkout 換成 no-op」的 mutation 驗證還原後複驗能抓到「回報成功但沒真的動到檔案」。㉓（同輪新增的回歸防線）驗證非 git 倉庫 `--kill` 目標靜默跳過。三組皆有獨立斷言且無互相遮蔽。**唯一相關的覆蓋缺口**是 merge-reviewer R2 APPROVE 自己標出的 m1（`--kill` 分支對 `restore_rc` 反映進 exit code 沒有樣本守——把 `if [ "$poll_rc" -eq 0 ] && [ "$restore_rc" -ne 0 ]; then` 改 `if false` 後 53 組仍全綠）；orchestrator 09-13 04:35 comment 已記入 LS-96 池，非本次巡檢新發現，不重複列。

7. **`patrol.sh` 單檔 dirty 提示分支是否有測試守** — 有，且覆蓋完整。`patrol.test.sh` ⑦b（正樣本：唯一 dirty 檔是 `.pen` → 印還原提示）／⑦c（對照：改別的單檔仍印泛用訊息）／⑦d（對照：`.pen`＋另一檔一起 dirty 仍走泛用訊息，證明判準是「唯一單檔」而非「有 `.pen` 就特殊」）／mutation（`if [ "$mc_dirty" -eq 1 ] && [ "$mc_dirty_files" = "design/littlesprout.pen" ]; then` 改 `if false` 驗證紅樣本翻回泛用訊息）。四組互相牽制，找不到縫隙。

8. **`agent-tools-check.sh` 三條 BODY_RULES 是否重複釘同一句** — 非重複，是既有慣例的正確延伸。`agent-tools-check.sh:130-132` 的三行分別是 `ios-dev|不得依賴截斷後的自動背景化|…`／`merge-reviewer|同句|…`／`qa|同句|…`——`agent`欄不同、檢查的是**三份不同檔案**各自要含這句話，不是同一 `(agent, phrase)` 鍵值對重複宣告。比對既有規則（如 :107-109 的「等長命令一律前景 Bash 帶 timeout」、:118-120 的「background-bash-guard.sh」）同樣是三行對應 ios-dev／merge-reviewer／qa 三份檔案，是整份腳本一貫的慣例（merge-review R1 i4 已核過此點），非死碼。三份 agent 定義（`ios-dev.md:23`／`merge-reviewer.md:19`／`qa.md:12`）皆已逐字含該句，機械擋 `agent-tools-check.sh` 現況綠。**觀察（非死碼，供記錄）**：三份定義同一 bullet 內「timeout（≤25 分）」與新句「Bash timeout 600000（工具上限）」字面互相矛盾——這是既有 informational i3，已裁定併入 LS-239 範圍，本票 scope 凍結不動，非本次新發現。

9. **`docs/COLLABORATION.md` 清倉六處是否留下指向已刪段落的錨點** — 未發現。逐一核對：HIBP 註記（現況「免費方案不支援…升 Pro 再開」，:226 一帶）／LS-231 `grep -F '] : '` 訂正（:336）／§4-b 兩則補充（`gh run download` 解出 bundle 內容目錄 + 改名 `.xcresult`；`gh api repos/<o>/<r>/actions/jobs/<job-id>/logs`，皆在 §4-b cron 模板段內）／簡體字「验证」→「驗證」（`grep -c 验证` 全檔為 0）／LS-215 效果欄補「已實測 09-12 `00330bed`」（:334 尾段）皆已落地且用字與現況程式碼／事實一致，未發現指向已刪除章節或死錨點的懸空引用。§7 新增兩列（殘留 xcodebuild 防護 :340、pen-open --kill 自動還原 :341）內容與程式碼現況（`is_holder_descendant()`、㊵ 系列）相符。**唯一已知措辭瑕疵**是 merge-reviewer R2 APPROVE 自己列的 i8（:340 那列「(a)(c) 正樣本：…完全沒跑到 xcodebuild test」不精確——(c) 其實是「主測試照跑、只有 iPad 段被擋」）；這是文字精度問題不是死碼／懸空錨點，且已記入 LS-96 池，不重複列。

10. **`PUSH_GATE_ALLOW_STALE_XCODEBUILD` 逃生口的文件與自測樣本** — 齊備。文件：`stale-xcodebuild-check.sh:30`（腳本檔頭）、`docs/COLLABORATION.md` §7 新列（:340，「`PUSH_GATE_ALLOW_STALE_XCODEBUILD=1` 逃生口」一句）。自測：`stale-xcodebuild-check.test.sh` ② 斷言殘留訊息含逃生口名稱、④ 正面驗證設了該變數時有殘留仍 exit 0 且印略過訊息、且不印殘留警告（`hasnt`）。與既有 i1（逃生口不落檔，只印 stderr）的處置一致——已核為「不修＋理由：與 `KEEP_SIMULATOR`／`DETECT_SIMULATOR_SHARED` 同慣例」，非本次新發現。

### 既有死碼觀察

無。本次巡檢未在票文範圍外的既有程式碼中發現需要另開票的死碼（本次讀取範圍集中在本票觸碰的 17 檔，未擴大掃描全庫既有死碼——按 Rule 3 不動不是自己弄髒的東西）。

### 建議處置

- 十項懷疑點全數判定「活」，**無 finding 需開 fix 票**。
- m1（`--kill` 分支 `restore_rc` exit code 缺樣本）、i3（timeout 數值矛盾）、i7（非 git 倉庫誤判分支）、i8（§7 措辭不精確）四項已由 merge-reviewer R2 APPROVE comment 提出並由 orchestrator 09-13 04:35 記入 LS-96 池／LS-239，本次巡檢交叉驗證後認同其分類，不重複開票、不重複記錄。

### 搜尋方法與盲區聲明

- 方法：`git diff`／`git log` 鎖定 diff 範圍；對每個懷疑點分別用 `grep -n`／`Read` 讀取實作與對應 `.test.sh`，並用 `git diff <R1 commit>..<R2 commit>` 比對 R1→R2 的確切增刪，確認「舊分支被替換」而非「新分支疊加、舊分支仍在」；函式引用計數（定義+呼叫次數）交叉核對有無孤兒函式／變數。
- 盲區：純文字搜尋抓不到 reflection、字串拼接組出的呼叫、或動態 `eval` 出來的路徑（本票／本次改動範圍內未觀察到此類寫法，皆為靜態 bash 呼叫與固定路徑字面）；未實跑任何自測（僅靜態讀碼比對票文與 merge-reviewer 已重放的紅綠證據，未重複重放 mutation——相信 R2 APPROVE comment 內逐條列出的重放紀錄）；未檢查本票之外的既有死碼（scope 限定本票 diff）。

### 2026-09-12T21:28:23.269Z · Chih-Lin Yeh · `f90a97ad`

**orchestrator 09-13 05:17 併入鏈 ✓**：#374 → main `00f4d62`（04:45；main tip merge-review status 已貼）→ back-merge #377 → development `c14f470`（05:17；dev tip status 已貼）。主 checkout 已 pull（agent 定義新句生效）。→ 派 sweeper → 收尾 → Done → 解鎖 LS-239。

### 2026-09-12T21:28:53.555Z · Chih-Lin Yeh · `68e50a7e`

**orchestrator 09-13 05:28 收尾儀式 → Done**

- ① 死碼巡檢 ✓（sweeper `f7ce5244`）：17 檔無本票引入死碼；R1 版 index 比較／holder 整批放行／固定名單假 pgrep 皆已乾淨取代；fixture 全被引用；逃生口有文件＋自測。
- ② Lesson learning review：
  - harness：**自測把整個 `pgrep -f` stub 掉＝pattern 零覆蓋**（R1 F1：刪呼叫點仍全綠）——規則：涉及行程／檔案系統判斷的 gate，至少一組樣本用真工具對假行程。已由 ⑫⑬＋㊵ 落地；派工規則追加一句。
  - harness：hotfix 併 main 後其他 hotfix PR 立刻變 DIRTY（#374 在 v0.23.0 升 main 後衝突）——promote main 後要重查所有 open hotfix PR 的 `mergeable`；本票用 SendMessage 請 agent 併 main 解掉。
  - 流程：中途 SendMessage 加 scope 被 agent 判為注入（正確）→ 範圍變更一律改票文（已記 dispatch 規則、F3／F6／F7／F8 掛 LS-239）。
  - 設定：無。工具缺口：無。
- ③ 清理 ✓：`cleanup-merged.sh --apply LS-236` → worktree 1 已移除。
- ④ Pen 清場：不適用。

main `00f4d62`、dev `c14f470` 皆含本票；隨下一版隨版發布。

