# LS-81 設計 chore：`aw57e` 角托壓紙緣量與 Tokens 板「5pt」慣例不一致（LS-72 visual-reviewer 範圍外觀察）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Low |
| 標籤 | lane:design |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-25T05:05:32.188Z |
| 開始 | 2026-08-25T05:16:13.816Z |
| 完成 | 2026-08-25T08:24:08.994Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-81/設計-choreaw57e-角托壓紙緣量與-tokens-板5pt慣例不一致ls-72-visual-reviewer-範圍外觀察 |
| 父票 | LS-46 設計定稿合併：C 基準＋使用者指定調整 → design/littlesprout.pen |
| 關係 | related→LS-88, related→LS-68, related→LS-74, related→LS-89, related→LS-26, related→LS-30, related→LS-72, related→LS-67, related←LS-238, related←LS-122, related←LS-118, related←LS-117, related←LS-96, related←LS-111, related←LS-116, related←LS-103, related←LS-87, related←LS-85, related←LS-91, related←LS-90, related←LS-44 |

## 描述

## 來源

[LS-72](https://linear.app/little-sprout-app/issue/LS-72/設計-chorels-46-r10r11-informational-f6-f8print-edge-token-合併tokens) visual-reviewer 核銷輪（2026-08-25）範圍外觀察：節點 `aw57e` 的角托與相片邊緣重疊量與 [LS-46](https://linear.app/little-sprout-app/issue/LS-46/設計定稿合併c-基準使用者指定調整-designlittlesproutpen) 定案「四角角托壓紙緣 5pt」不一致。屬 [LS-46](https://linear.app/little-sprout-app/issue/LS-46/設計定稿合併c-基準使用者指定調整-designlittlesproutpen) 既有幾何，[LS-72](https://linear.app/little-sprout-app/issue/LS-72/設計-chorels-46-r10r11-informational-f6-f8print-edge-token-合併tokens) 未動，依 Rule 3 不順手改。

## 做法

ui-designer 量測全稿所有角托重疊量（絕對座標掃描，比照溢出掃描方法），列表：符合 5pt／不符者逐一修到 5pt；若發現 5pt 慣例本身在某尺寸下不成立（例如小尺寸沖印品），回報改規則而非硬套。visual-reviewer 一輪核銷。取證 `.claude/evidence/LS-<n>/`。

## 驗收

掃描表：不符 0；landing gate 綠；零像素以外的變更不得有。可併入 [LS-67](https://linear.app/little-sprout-app/issue/LS-67/設計多寶貝-profile-三畫面建檔管理切換-ui-designervisual-reviewer-3-輪) 開工前的整理輪或 [LS-30](https://linear.app/little-sprout-app/issue/LS-30/harnesslittle-sprout-brand-專案-skill設計語言定案後) skill 落地後的第一張設計票順做。

## 留言（6）

### 2026-08-25T07:01:05.306Z · Chih-Lin Yeh · `d8c4f575`

## 進度：全稿角托掃描完成（未落地，Pen blocker 待裁決）

**範圍**：`GEBcf`（cmp/Photo Corner）實例全稿共 106 個，其中 80 個是相片/沖印品角托（26 個為裝飾性 app icon 邊角記號，隨 icon 尺寸等比縮放、非固定 5pt 慣例，判定 out of scope）。

**合規 36（9 群組）**：`HqrJw`／`mpeJ1`／`dpxRg`／`v6j7V7`（Print Stage 深層 wrapper）、`yYF9A`／`Cxqc2`／`D9fN9`／`JopMO`（相片本身即父層）、`SzLhU`（深色紙條接縫）— 四角皆與所屬相片/印品邊緣相距 `$corner-out`＝5（Tokens 板既有 token，非新訂）。

**不符 34（3 群組，11 個節點）**，無「建議改規則」情形（所有尺寸下 5pt 皆可行，未見角托會蓋住內容的小尺寸沖印品）：

| 群組 | 節點 | 問題 | 量測 |
|---|---|---|---|
| Email/Family Print 卡片族 | `aw57e`／`bd0jF`／`H0KHI`／`Lm66y`／`yWH0V`／`XrvpC`（6 節點×4 角＝24） | 四角皆偏：TL/TR 應在 (-5,-5)/(x,-5) 卻在 (3,3)/(x,3)；BL/BR 應在 y=154 卻在 y=159 | 含 LS-72 標記的 `aw57e` |
| Print Stage 貼邊族 | `BXbTE`／`Hi9F5`／`Lc5BJ`／`gk1Vi`（4 節點×BL/BR＝8） | 僅下緣兩角短少：y=239，應為 242 | 差 3pt |
| iPad 主印品 | `BDrtd`（BL/BR＝2） | 下緣兩角嚴重偏移，疑似殘留舊值：y=713，應為 820 | 差 107pt |

**Blocker（處置中，等使用者裁決，暫停其他群編輯）**：Group 1（`aw57e`＋`bd0jF`）修正已在 Pencil 記憶體完成並驗證（讀回座標＋截圖確認四角對稱壓紙緣），但 Pencil MCP 為單一全域 in-memory instance，`execute` 的 `filePath` 參數未能重導向——所有編輯實際落在一份標記為已刪除的 `.claude/worktrees/LS-72/design/littlesprout.pen`（該 worktree 已於 LS-72 併入後清理）。已用 node-by-node JSON 結構比對驗證該記憶體內容與 LS-81 檔案基準完全一致（2698 節點、`variables`/`themes`/`fileToken` 皆同），唯一差異即這 12 個屬性值——但落地覆蓋的 `cp` 動作被 auto-mode classifier 擋下。目前依指示：Group 1 修改保留在記憶體、不再嘗試覆蓋檔案、暫停其他群組編輯，等使用者在 Pen app 內操作或授權後再繼續。

### 2026-08-25T07:31:36.165Z · Chih-Lin Yeh · `ff5cb6c2`

## Handoff — LS-81 完成，PR #140 → development

**Ticket**：LS-81 — 全稿角托壓紙緣量與 Tokens 板「5pt」慣例對帳並修正

**已完成**（5 commits，`feature/LS-81-corner-tab-5pt`，PR [#140](https://github.com/CLYEH/little-sprout/pull/140)）

| # | Group | commit | 內容 |
|---|---|---|---|
| 1 | Group 1 | `015765b` | Email 登入（`aw57e`／`bd0jF`）四角托 5pt，8 角 12 屬性值 |
| 2 | Group 2 | `97e2698` | 建立家庭（`H0KHI`／`Lm66y`／`yWH0V`／`XrvpC`）四角托 5pt，16 角 24 屬性值 |
| 3 | Group 3 | `eae8f72` | Print Stage 貼邊族（`BXbTE`／`Hi9F5`／`Lc5BJ`／`gk1Vi`）BL/BR 短少 3pt 修正，8 屬性值 |
| 4 | Group 4 | `0cc17c8` | iPad 主印品 `BDrtd` BL/BR 殘留舊值修正（偏差 107pt，全稿最大者），2 屬性值 |
| 5 | 板端順修 | `8bd51d8` | LS-30 review R1-i1/i2/i6/i8：`h7EnT` 字母表更正（LS-89/LS-90 定案 32 字元含 Q/L）、`$fs-code` ax3 76→72、`WEjnC`/`sQsj1` 量測基準註記 |

**掃描表摘要**：全稿 `cmp/Photo Corner` 106 個實例，80 個為相片/沖印品角托（26 個裝飾性 app icon 邊角記號 out of scope）。合規 36（9 群組）未動；不符 34（11 節點、3 群組）→ 全數修正到 5pt。**無「建議改規則」情形**——所有尺寸下 5pt 皆可行，未見角托覆蓋內容的小尺寸沖印品。完整表見 `.claude/evidence/LS-81/r1/scan.md`（隨 PR 併入前先貼於此，未入版控）。

**已驗證**
- 每群修正皆用 node-by-node JSON 結構 diff 驗證變更範圍精確等於預期（無夾帶）＋ `design-landing-check.sh --expect-nodes 2676` 綠燈
- 收工全樹溢出掃描：**TOTAL_NODES 2676**（與基線一致）／**FLAGGED 56**（基線 50，+6）——逐類核對：A 出血 13 不變、B 角托 40（基線 34，+6＝本票修正的 6 個 Email/Family Print 群組中原本因座標方向錯誤未觸發溢出旗標的 TL 角，修正後正確溢出，**是預期且正確的結果**，角托本來就該壓過相片邊緣）、C `mXQJh` 0（基線 1，LS-46/LS-72 已記錄為母版量測不穩定假訊號）、D AX3 法務 3 不變、E 0 不變；13+40+0+3+0＝56 ✓
- 截圖（修正前後）：`.claude/evidence/LS-81/r1/before/`、`.claude/evidence/LS-81/r1/after/`（`aw57e`、`H0KHI`、`BXbTE`、`BDrtd`）
- push gate（unit tests／api-contract／error-codes／分支票號／合併檢查）5 個 commit 全綠；PR body 過 `pr-body-check.sh`，含 `Design:` 欄

**未完成／informational 處置**：無（本票尚無 reviewer）。

**風險**：Pencil MCP 為單一全域 in-memory instance——本票作業期間發現 `execute` 的 `filePath` 參數無法重導向已載入的文件（原「active editor」停留在已刪除的 LS-72 worktree 路徑），導致標準 LS-26 收工程序（同路徑 sha 比對後 cp）不適用。改為經使用者在 Pen app 內驗證後手動 cp 落地的變通流程，每次落地前皆以完整 node-by-node JSON diff 驗證變更集精確無夾帶。建議另開 harness 票記錄此 Pencil 限制（worktree 檔案清理後前一份文件仍停留記憶體、無 API 可重導向），補進 `.claude/skills/little-sprout-brand` 或 ui-designer 的已知限制清單，避免下一張設計票重蹈。

**產出位置**
- PR：https://github.com/CLYEH/little-sprout/pull/140
- Worktree：`.claude/worktrees/LS-81`（分支 `feature/LS-81-corner-tab-5pt`）
- 證據：`.claude/evidence/LS-81/r1/`（`scan.md`、`before/`、`after/`，worktree 相對，未 commit）

### 2026-08-25T07:46:29.400Z · Chih-Lin Yeh · `0983a58b`

## Merge gate review — PR #140（`feature/LS-81-corner-tab-5pt` → `development`，head `8bd51d8`）

**Verdict：APPROVE**（0 blocker／0 major／3 minor／4 informational）

Base `origin/development`（merge-base `9383575`，已含 #137 的 LS-30 back-merge）。1 檔、+51/−51。**未開 Pencil MCP**——比照 LS-72／LS-30 手法，用 `git show <ref>:design/littlesprout.pen` 做唯讀 python 結構查詢（`design-landing-check.sh` 在 CI 做的同一件事）。

---

### 結構級 diff：逐項對帳，零多餘

| 項目 | 預期 | 實測 |
|---|---|---|
| 檔案 | 只有 `design/littlesprout.pen` | ✓ `--name-status` 僅 1 檔 |
| 節點數 | 2676 | ✓ 2676 → 2676 |
| id 集合 | 不變 | ✓ added 0／removed 0 |
| 樹結構 | 不變 | ✓ (parent, index, type, name) 邊集合**完全相同** |
| themes／fileToken／version | 不變 | ✓ `['mode','size']`／`f75220c0-…`／2.17 皆同 |
| 角托 x/y | 46 屬性值（34 角托） | ✓ 46，無多無少 |
| 文字 content | 4 個（`h7EnT`／`f2Gm3`／`WEjnC`／`sQsj1`） | ✓ 恰為這 4 個 |
| 變數 | 1 個（`fs-code` ax3 76→72） | ✓ 唯一 meta diff＝`variables.fs-code.value[1].value`（`theme.size=ax3`）76→72 |

**逐 commit 增量結構 diff（確認無夾帶、無跨群污染）**：

| commit | 節點屬性 diff | meta diff |
|---|---|---|
| `015765b` Group 1 | **12**（8 節點：`W7Ycw`/`tlJFt`/`jOrlo`/`ny0ec` ＋ `QM8ur`/`NjWHY`/`pAlwq`/`s4Xyo`） | 0 |
| `97e2698` Group 2 | **24**（16 節點，4 群組） | 0 |
| `eae8f72` Group 3 | **8** | 0 |
| `0cc17c8` Group 4 | **2** | 0 |
| `8bd51d8` 板端 | 4（皆 `.content`） | 1（`fs-code`） |

與派工的預期切分（12／24／8／2，Group 1 為 8 節點 12 屬性）**逐格吻合**。

---

### 幾何：我自己重算，不是採信掃描表

慣例出處 `F2IL9w`（本 PR **未動**）：「角托外擴到台紙上的量…**它一定壓過紙的邊緣 5pt**。堆疊順序固定：台紙 → 紙（1pt paper-edge 亮邊）→ 角托。」`$corner-out` = 5。

- **Email/Family Print ×6**：卡片 345×175（393 − 2×`$screen-pad` 24，親驗 `screen-pad`=24）。修正後 TL(−5,−5)／TR(324,−5)／BL(−5,154)／BR(324,154) → 324+26=350=345+5 ✓、154+26=180=175+5 ✓、四角一致。修正前是 (3,3)/(324,3)/(3,159)/(324,159)——**右緣已 5pt、下緣 10pt、左上緣反向內縮 3pt**，三種偏差混在一起，確實是壞的。
- **Print Stage ×4**：Print 361×255 落在 padding `$sp-label` 8 → (8,8)。BL/BR 正解 = 8+255+5−26 = **242** ✓（原 239 只壓 2pt）。TL/TR 的 3／348 本來就對（8−5=3、8+361+5−26=348），未動 ✓。
- **BDrtd（107pt，全稿最大者）——這條我驗最重**：`eS0jX` Print 是 auto-height vertical、gap 7、padding 8/8/8/8，子節點 Photo Wrap 542 ＋ Imprint Row（auto）。Imprint Row `naCpF` 與 `HqrJw` 內已合規的 `pWZET` **結構完全相同**（單一 `$fs-imprint` 文字、justifyContent center），而 `HqrJw` 可反解出 imprint 高 = 17：8+190.1+7+**17**+8 = 230.1，其 BL 角 y=214.1 → 214.1+26 = 240.1 = 5+230.1+5 ✓。同樣代入 `eS0jX`：8+542+7+**17**+8 = **582** → BL/BR y = 273+582+5−40 = **820** ✓ **恰為新值**。舊值 713 反推 Print 高 = 475，但光 Photo Wrap 就 542 ——**舊值可被證明是殘值，新值可被證明正確**。
- **全稿獨立重掃**：106 個 `GEBcf` 實例、33 個群組。我算出的合規集與掃描表**完全一致**（9 群組 36 角），out-of-scope 集也一致（13 個 icon frame × 2 = **26** 個 `Mount TL/BR`，等比縮放非固定 5pt，判定合理）。修正後 80 個相片／沖印品角托**不符 0**——票文驗收條件由我自己的掃描獨立成立。

### FLAGGED 56 vs 50：可重現的部分我重現了

我重寫了角托類的溢出判定（子節點 bounds vs 直接父框），跑 base 與 head：

```
9383575（base）: corner-category flagged = 34
   {aw57e:3, bd0jF:3, H0KHI:3, Lm66y:3, yWH0V:3, XrvpC:3, yYF9A:4, Cxqc2:4, D9fN9:4, JopMO:4}
8bd51d8（head）: corner-category flagged = 40
   {aw57e:4, bd0jF:4, H0KHI:4, Lm66y:4, yWH0V:4, XrvpC:4, yYF9A:4, Cxqc2:4, D9fN9:4, JopMO:4}
```

18+16 = **34** → 24+16 = **40**，**+6 恰為 6 個卡片群組的 TL 角**——與 handoff／scan.md 的宣稱逐格吻合，且 Group 3／Group 4 的修正因仍在父框內故不改變旗標數（我也驗了：242+26=268 < 273、820+40=860 < 1194）。**「B 角托 40」不需要採信，是可重算的。** A／C／D／E 四類我沒重跑（需版面引擎／屬文字出血類），所以「總數 56」我只驗證了它變動的那一段。

### Gates／機械檢查

- `design-landing-check.sh --expect-nodes 2676`：**5 個 commit 的 blob 逐一綠**（＋base 綠）。
- CI `ci`／`db`／`lint`／`rules` 四 job 全綠（run 32821982355）。**註**：`ci.yml` 的「.pen 落地結構檢查」step 是**不帶** `--expect-nodes` 跑的（設計如此，見註解），所以 CI 綠 ≠ 節點數已驗——帶 `--expect-nodes` 的那一版是我自己補跑的。
- 5 條 commit message 逐條過 `commit-msg-gate.sh` ✓。
- PR body 過 `pr-body-check.sh --branch feature/LS-81-corner-tab-5pt` ✓，含 `Design:` 欄 ✓。
- `git merge-tree --write-tree origin/development` → **CLEAN**（exit 0，無 conflict 段）。
- **體積**：5 個新 blob ＋ commit/tree 全範圍 delta-pack ＝ **152,001 bytes ≈ 0.145 MB**，遠低於 LS-74 的 0.5 MB 線 ✓。（loose 物件相加 0.92 MB 是 gc 前的假象。）

### 四維

| 維度 | 結論 |
|---|---|
| Race condition | **N/A** — diff 內 0 行 Swift／SQL。無 actor／Task／`@MainActor`／`Sendable`，無上傳佇列，無 Supabase 寫入，無快取。 |
| 運算效能 | **N/A** — 無 RLS policy、無查詢、無分頁、無主執行緒圖片解碼。唯一成本是 repo 體積（上，0.145 MB）與 CI 一次 `json.load`。 |
| 平行優化 | **N/A** — 無可執行路徑。 |
| **Scope** | **無 finding**。唯一看起來像順手改的 commit 5（板端文字＋變數）**是被授權的**：LS-30 收尾儀式 comment 明文寫「i6（板端「排除 Q」註記）→ **LS-81 順修**」「待補（需 Pen）：`fs-code` ax3 變數 76→72 回填、六個 token 板上對比欄 → **LS-81 最後一群順做**」，R1-i1／i2 亦指名併 LS-81。該指示晚於票文的「零像素以外的變更不得有」，依 Rule 6 取較新且較具體者；且在 commit message／PR body／handoff／scan.md 四處揭露。**Scope 本身沒問題，問題在這批交接的完成度（F2）與其下游殘留（F3）。** |

---

## Findings

### MINOR

**F1 — 取證缺 `before/BDrtd.png`，但 PR body 與 handoff 都宣稱有。**
`/Users/clyeh/little-sprout/.claude/evidence/LS-81/r1/before/` 實際只有 `BXbTE.png`／`H0KHI.png`／`Iqtt4.png`／`aw57e.png`；`after/` 有 7 張含 `BDrtd.png`。PR body 寫「修正前後截圖見 …（aw57e、H0KHI、BXbTE、**BDrtd**）」——**全稿最大的那一筆修正（107pt）沒有 before 圖**；板端三個節點（`lS22L`／`m7iph`／`orS6N`）同樣只有 after。另 `before/Iqtt4.png` 只有 510 bytes，是單顆 26pt 角托節點的匯出，不是表格暗示的畫面級 before shot。
**失敗情境**：visual-reviewer（同時在跑）被要求核銷 BDrtd 的 107pt 位移，手上只有 after 圖，只能靠自己反推或憑單張圖放行——正是「取證要讓下一個人不必重做」的反例。
**修法**：從 `origin/development` 匯出 `before/BDrtd.png`（＋三個板端節點的 before），或在 handoff 誠實寫「BDrtd before 未取證＋原因」。

**F2 — handoff 寫「未完成／informational 處置：無」，但 LS-30 交下來的兩件事只落地一件。**
LS-30 收尾儀式指派給 LS-81 的是**兩項**：①`fs-code` ax3 76→72 回填 ②**六個 token 板上對比欄**。①已完成且正確（我驗過：`variables.fs-code` ax3 = 72，與板上 `F7Swz6` 的 `'72'` 對齊）。②沒做——`/Users/clyeh/little-sprout/.claude/skills/little-sprout-brand/references/tokens.md:51-55` 的 `paper-edge`／`photo-dim`／`disabled-fg`／`knob-edge`+`knob-shadow`／`switch-knob`+`switch-off` 仍是「待補（需 Pen）」，而我在板上查證這些 token **根本沒有 Color Table 列**（只存在於 `variables` 與散文清單 `RYPHw`），所以要補得先量測再加列，是真設計工作、不是轉錄。
**不做是可接受的；不申報不行。** `未完成` 欄本來就是為這種事存在的。
**修法**：handoff 補一條「另票 LS-\<m\>」或「不修＋理由」。

**F3 — 這個 PR 一併入，`little-sprout-brand` skill 的 `references/tokens.md:82` 就會變成錯的，而目前沒人追。**
該行現寫「`$fs-code` size=ax3 變數**仍是 76**——落後的是變數…**待補（需 Pen）**＝下一張開 Pen 的票把變數改成板上值」。#140 併入後變數已是 72，這句話就成為**每張設計票開工都被要求載入**的文件裡的一句假話。這正是 LS-30 自己 R2-i4 立的規矩（有到期日的敘述要指定清除者）與 skill slop 第 18 條（過期的 Notes 比沒有更糟）的實例。
**失敗情境**：下一張設計票的 ui-designer 載入 skill，讀到「變數仍是 76、待補」，於是重做一次已完成的回填，或回報一個不存在的漂移。
**修法**：skill 在 `main`、本 PR 打 `development`，**不能也不該在本 PR 修**——需要一張後續票，或直接併進 LS-30 R2-i4 已建立的「LS-90 併入時清 skill 過期註記」那一批。只要被追蹤即可。

### INFORMATIONAL（不擋合併）

**F4 — `h7EnT` 的更正把稿面待辦推給「下次動這幾張畫面時」，無人承接。**
新增段落自述：「稿內『K7M 2QD → K7M 2FD』轉錄與 06/06b/06c 第 5 格 Q→F 屬舊方案殘留，本票未一併改稿面內容，下次動這幾張畫面時一併核實。」實際風險很低——採用的 32 字元表**同時含 Q 與 F**，所以現有示例碼仍然合法，只是失去理由。更正內容本身我逐項驗過：`23456789ABCDEFGHJKLMNPQRSTUVWXYZ` ＝ 8 數字 ＋ 24 字母（排除 0/O/1/I，含 Q、L）＝ 32 ✓，32^6 = 2^30 = 30 bit ✓。只是「下次」不是一個承接者。

**F5 — 同一段沒帶上 skill 有帶的部署警語。**
skill 的寫法是「LS-90 對齊中——**現行部署 `create_invite` 仍 8 碼／40 bit**」，稿上這段只寫定案結論。只讀稿不讀 skill 的人（visual-reviewer／QA）拿不到「LS-90 併入前用 6 碼做 QA 會假失敗」這個警告。半句話的事，非必要。

**F6 — `WEjnC`／`sQsj1` 從 1 行長到 2–3 行，塞在 280pt 寬的表格欄裡（PLAUSIBLE，交 visual-reviewer）。**
兩者皆 `textGrowth: fixed-width`、`width: fill_container`、13pt Noto Sans TC，父格 `W1HeYg`／`Q804i` 寬 280。**裁切風險我排除了**：從 Row → `r7dBtv` Color Table → `PsZ61` → `XhwG6` 整條祖先鏈 `height` 皆為 null（hug），雖然表格與板框 `clip: true`，但高度會長、不會裁。`h7EnT`（多一整段）在 `jftM0` 底下同理。**但「長高之後這張表還讀不讀得出是表格」是像素判斷，我從 JSON 判不了，明確交給 visual-reviewer。** 同理，6 張卡片的角托現在會外擴 5pt 進入 `z2HagG`（gap `$sp-block`）的上下間隙——這與 `yYF9A`／`Cxqc2`／`D9fN9`／`JopMO` 四個既有合規群組是同一形狀，一致性上說得通，但仍是視覺題。

**F7 — `scan.md` 標題把「角托數」寫成「屬性值數」。**
「不符 34→已修 0（3 群組，11 節點，**34 個角托屬性值**）」——34 是角托數，屬性值是 **46**。handoff 的表格反而是對的（12+24+8+2 = 46 屬性值，8+16+8+2 = 34 角）。純措辭。

---

### 已看／未看

**已看**：base…head 完整結構 diff ＋ 5 段逐 commit 增量結構 diff（節點屬性、meta、id 集合、樹邊集合）；106 個角托實例的全稿獨立重掃與 5pt 反解（含 `$screen-pad`／`$sp-label`／`$print-edge` 的實際變數值）；`eS0jX` 高度用 `HqrJw` 反解 imprint row = 17 的獨立重算；角托類溢出旗標 base/head 重現（34→40）；4 個文字節點的前後全文與祖先鏈 hug/clip 屬性；`fs-code` 變數的 theme 結構與板上 `F7Swz6` 對帳；`F2IL9w` 慣例原文（未變）；Color Table 全 30 列的對比欄實況；skill `tokens.md` 待補項；5 blob × landing gate（帶 `--expect-nodes`）；5 條 commit message gate；PR body gate；merge-tree；delta-pack 體積；CI 四 job 與 `ci.yml` 的 .pen step 實作。

**未看／未驗**：
- **FLAGGED 的 A／C／D／E 四類**（出血 13、`mXQJh` 0、AX3 法務 3、E 0）——需版面引擎，沿用 handoff 記錄，**未獨立複驗**；我只重現了 B 類（角托 34→40）。
- **像素／視覺判定**——依派工交同時在跑的 visual-reviewer：角托壓紙緣的觀感、6 張卡片外擴後與上下元素的關係、`WEjnC`／`sQsj1`／`h7EnT` 長高後的表格與註記板可讀性。**本 review 的 APPROVE 不含視覺結論。**
- 未開 Pencil MCP（依派工）。
- `.claude/evidence/LS-81/r1/` 的 PNG 我只看了檔案清單與大小，未逐張開圖。

---

**結論**：這是我近期看過最乾淨的一次結構級落地——2676 節點、id 集合、樹邊、themes、fileToken 全部不動，51 筆變更逐 commit 可歸因、逐筆可重算，連最可疑的 107pt 位移都能從同稿另一個已合規節點反解出**恰好等於新值**、並證明舊值不可能成立；FLAGGED +6 也不是宣稱而是可重現的。三條 minor 全部落在**取證與交接追蹤**、無一在合併產物內：F1 補一張 before 圖、F2 把 LS-30 交下來沒做完的那半件寫進未完成、F3 開票清 skill 即將過期的那一行。無 blocker／major → **APPROVE**。合併前請 orchestrator 確認 visual-reviewer 的像素結論。

### 2026-08-25T07:46:57.231Z · Chih-Lin Yeh · `3a6b16ed`

## visual-reviewer 核銷輪（第 1 輪，chore 單輪）：ITERATE

`little-sprout-brand` skill **已載入**（SKILL.md ＋ motifs／slop-forbidden／tokens／elder-constraints／entry-conditions／wordmark-brand）。

**方法**：不採信 `scan.md`，直接以 python 唯讀解 `.claude/worktrees/LS-81/design/littlesprout.pen`（純 JSON），與 `git merge-base origin/development HEAD`（`9383575`）逐節點對照。獨立量測存 `/Users/clyeh/little-sprout/.claude/evidence/LS-81/r1-review/independent-scan.txt`。

---

### 一、角托幾何（本票主體）— 通過，且判定凍結，重做不得再動

逐項獨立複驗，全部成立：

- **80 顆相片角托全部 5pt**。我不只驗「四角互相對稱」，而是由 TL/TR/BL 反推 implied reference box，再去比對該群**實際的 Print 子節點座標**。20 群全部吻合到小數位：`HqrJw`/`mpeJ1`/`dpxRg` → Print[16,5,361]、`SzLhU` → Print[16,141.6,361]、`v6j7V7` → Print[16,5,361×336]、`BXbTE`/`Hi9F5`/`Lc5BJ`/`gk1Vi` → Print 361×255＋padding 8、`Cxqc2` → Print Paper[0,0,420×570]、`D9fN9` → [0,0,319×200]、`JopMO` → [0,0,400×244]、`BDrtd` → Print[40,273,420]，高 582（＝ skill 記載的 iPad 閉合值）。不是自洽而已，是對到真正的紙緣。
- **36 顆合規者未被動到**：diff 只碰 11 個節點；未動的 9 群（`HqrJw` `mpeJ1` `dpxRg` `v6j7V7` `SzLhU` `yYF9A` `Cxqc2` `D9fN9` `JopMO`）逐位元相同。
- **26 顆 app icon `Mount TL/BR` 判 out of scope 正確**：實測其偏移對 icon 邊長固定比例 0.078（40/60/80/120/180/512 全一致），走的是母題三段規則第②段，硬套 5pt 反而是錯的。
- **scope 乾淨**：`merge-base..HEAD` 只碰 `design/littlesprout.pen`，51+/51−，零夾帶。`origin/development` 期間未動過 .pen，無衝突。landing gate 綠、nodes 2676 ＝ skill 基線。
- **染料池未失準**（我特別查了，因為角托外移 8pt 而池沒動就會毀掉物理敘事）：`Fade Pools` 獨立圖層只存在於 `Cxqc2`/`D9fN9`/`JopMO`——**全在未動的 36 顆裡**。被改的 6 張 Email/Family Print 的池是 frame fill 上的四道 radial（opacity 0.494/0.36/0.288/0.236，符合 `0.03＋0.50×lit` 的窗光規則），normalized 綁在紙框上，而紙框沒有位移或改尺寸。所以不但沒失準，**修正後角托才真正壓在池心所在的紙角上，套準是變好的**。

---

### 二、FLAGGED 50→56 的 +6：解釋成立，且是算術上的唯一解

我把 before/after 的角托溢出逐顆重算。**新增溢出恰為 6 顆，且全部是那 6 張印品的 `Corner TL`**：`W7Ycw`(aw57e)、`Iqtt4`(H0KHI)、`QM8ur`(bd0jF)、`o7Ap0`(Lm66y)、`QxvAE`(yWH0V)、`FugRL`(XrvpC)。消失 0 顆。

**對「若 5pt 壓紙緣本來就會 clip，為何其他 30 個合規角托不在基線」的正面回答：5pt 壓紙緣本身不會產生 clip 旗標；只有當角托的父層就是印品本身時才會。** 全稿分成兩族：

- **Family A（父層＝印品）**：`aw57e` `bd0jF` `H0KHI` `Lm66y` `yWH0V` `XrvpC`(345×175)、`yYF9A`(345×210)、`Cxqc2`(420×570)、`D9fN9`(319×200)、`JopMO`(400×244)。5pt 外伸必然離開父層 → 必被記。10 群×4＝40。
- **Family B（父層是比印品大的外框）**：`BXbTE` `Hi9F5` `Lc5BJ` `gk1Vi`（Print Stage 393 寬、印品 361 內縮 8）、`HqrJw` `mpeJ1` `dpxRg` `v6j7V7` `SzLhU`（印品在 (16,5)）、`BDrtd`（父層是 834×1194 iPad 畫板）。5pt 外伸仍落在父層內 → 前後都是 0。10 群×4＝40。

那 30 顆「合規卻不在基線」的角托，全部落在 Family B。而且修正前 Family A 前 6 群各只貢獻 **3 of 4**（TL 縮在 (3,3) 內側沒溢出），6×3＝18，加後 4 群 16 ＝ **34 ＝ 基線角托數**；修正後 10×4 ＝ **40**。**那個 3-of-4 的不對稱本身就是 bug 的指紋——一組正確的角托必須貢獻 4 顆。** 所以 +6 不只是「合理」，是被算術逼出來的唯一答案。這條我背書。

---

### 三、退修項（全部落在板端順修與帳目，不含角托幾何）

**F1 — `f2Gm3` 戳記破壞稿內自訂文法（本 PR 引入）**
稿內 `Stamp Rule`（`jQiNh`）明文：戳記是「最後改寫輪次 · 對應 frame」兩欄，且自我執行（「缺戳＝FAIL」「改稿沒回頭校對就不准把輪次往上寫」）。本 PR 把 `"R7 · 全域"` 改成 `"LS-81\n原 R2"`，**把 frame 欄整個拿掉**。全稿 70 個戳記裡，這是唯一第一行不帶任何 frame／scope 的。已有的兩個票號前例都保留：`nlmAI`＝`"LS-46 R3 · 06/07"`、`D5kKcT`＝`"LS-46 R2\n字標 v3"`（scope 在第二行）。
→ 改為 `"LS-81 · 全域\n原 R2"`（沿用原 scope），或點名 `"LS-81 · 06·06b·06c·07·07b\n原 R2"`。若真要把戳記詞彙從 R 輪次改成票號，那是規則變更，寫進 `Stamp Rule` 本文，不要只默默改 70 之 1。

**F2 — `h7EnT` 的更正被追加在過期段落之後，過期敘述仍居首**
`Stamp Rule` 自己的理由就是「過期的 Notes 比沒有 Notes 更糟，因為它看起來還有效」。這段現在開頭是【R2 · 邀請碼字母集（設計對後端的規格請求）】＋約 200 字以規格口吻陳述 30 字元表，讀者要讀完才碰到【LS-81 更正】。ios-dev 是照這段實作邀請碼的。
→ 把【LS-81 更正】移到整段最前面，R2 段降到其下並冠【已作廢 · 保留理由紀錄】；最低限度也要在 R2 段第一行就地加【已作廢，見下方 LS-81 更正】，讓略讀不可能誤認為現行規格。

**F3 — 遞延項未進 Linear，且 handoff「未完成：無」與稿面矛盾**
註記寫「本票未一併改稿面內容，下次動這幾張畫面時一併核實」——這是活在稿面散文裡的未追蹤 TODO，而 handoff 卻寫「未完成／informational 處置：無」。兩者不能並存。
而且**這個遞延多半根本不必要**：我查了稿面，示例碼是 `K7M 2FD`，**F 在 32 字元表內**；06/06b/06c 六格顯示 `K`/`7`/`M`/`2`/`F`/`8` 也全部合法。Q→F 的原始理由作廢了，但結果無害，稿面沒有殘留缺陷。
→ 直接在註記裡結案（「F 在 32 字元表內，示例碼與 06/06b/06c 仍合法，無須改稿」），不要遞延；並補正 handoff 的未完成欄。

**F4 — FLAGGED 基線分項加總不成立（50 vs 51），LS-72 遺留的 −1 被靜默吸收**
handoff 一邊說「基線 50」，一邊列分項 13＋34＋1＋3＋0＝**51**。兩者不能同時為真。skill 已記載這筆帳：「LS-72 後 FLAGGED 50（−1 LS-72 自述不可歸因、未獨立複驗）」。
我直接驗 `mXQJh`：**merge-base 與 HEAD 逐位元相同**（不在 diff 內，同樣是 `cmp/Approval Status` 底下 `fill_container`／auto-height 的 `Status Text`）。LS-81 不可能改變它，所以它的 1→0 是掃描法對 fit_content 節點的不穩定，不是設計變更。
→ C 列應記為「基線不可靠、本票未觸碰、不計入 delta」，而不是「基線 1 → 0」。正確表述：角托 34→40（+6，逐節點可驗），其餘分類不變，殘留的 ±1 是 LS-72 繼承的未結帳，本票不吸收。否則下一張設計票會拿到一個分項加不起來的「56」——51/50 能撐過三張票就是這樣來的。

**F5 — 建議另開 harness 票：溢出掃描沒有 committed 實作，數字不可複現**
`grep -rn "FLAGGED|溢出掃描|overflow" scripts/ docs/ .claude/` **零命中**。`design-landing-check.sh` 只驗 JSON 與節點數，從不算 FLAGGED。也就是說，這個把關每張設計票、且 skill 要大家信任的白名單基線，是由每個 agent 各自臨時重寫的方法產生的。
我實測了它有多敏感：同一份檔案，`fill_container` 解析為 345 時 delta 是 **+6**（正確，由 `$screen-pad`＝24 與 5pt 幾何雙重確認）；不解析該軸時是 **+12**。兩個都「看起來合理」。這正是 CLAUDE.md 最高原則說的「重要規則沒有 gate」。
→ 建議 `scripts/gates/design-overflow-check.sh <pen> --expect-flagged N`，分類白名單以 node id 列名，讓「+6 是預期」變成機器斷言而非散文。我用約 30 行 python 就重現了 34→40，成本很低。

---

### 值得保留、重做時不要動的

1. 角托幾何 34 顆修正——全部正確，且 `BDrtd` 那筆 107pt 是全稿最大的實質視覺修復（修正前 BL/BR 橫切過照片，修正後才真正托住紙腳）。
2. 兩套座標系並存（Family A 相對印品、Family B 相對外框）**不是缺陷**，是父層結構不同的必然；`x:-5` 與 `x:3` 看似矛盾實則同一條 5pt 規則。別「統一」它。
3. `scan.md` 的群組分類與 36/34/26 拆分，與我的獨立列舉逐項吻合——這張表本身是好產出。
4. `$fs-code` ax3 76→72：數學我複算過，76 → 313.6 對內寬 305 溢出 8.6（與註記完全一致），72 → 299.2 餘 5.8。正確。
5. 32 字元表文字正確：`23456789ABCDEFGHJKLMNPQRSTUVWXYZ`＝32 字元、排除 0/O/1/I、含 Q 含 L、32⁶＝2³⁰。與 LS-89／LS-90 及 skill 一致。OTP 仍維持 6 格（3+3），**正確地沒有**在 LS-90 併入前改成 8 格。

### 重做優先序

1. **F1 戳記補回 frame 欄**（最短、且違反的是稿子自己寫下的規則）
2. **F2 更正移到段首**（影響 ios-dev 讀到什麼）
3. **F4 帳目改正**（寫進 `scan.md` 與 handoff，止血，避免下票繼承壞基線）
4. F3 註記結案＋補 handoff 未完成欄
5. F5 另開 harness 票，不擋本票

主體無誤，退修全部是幾分鐘的文字與帳目修正。修完即可 APPROVE，角托幾何不需再審。

### 2026-08-25T07:58:39.000Z · Chih-Lin Yeh · `051a2974`

## Handoff — LS-81 R2 收尾（接手原 ui-designer；設計內容由 orchestrator 落地，本輪不開 Pen）

**Ticket**：LS-81 — 全稿角托壓紙緣量與 Tokens 板「5pt」慣例對帳並修正（R2：review 退修收尾）

**已完成**

| # | commit | 內容 |
|---|---|---|
| 6 | `e732619` | R2：`f2Gm3` 戳記補回 frame 欄（`"LS-81 · 全域\n原 R2"`）；`h7EnT`【LS-81 更正】移到整段最前、舊 R2 段標「已作廢，見上」、示例碼 TODO 句刪除就地結案（`K7M 2FD` 的 F 在 32 字元表內，仍為有效示例） |

- 帳目：`.claude/evidence/LS-81/r1/scan.md` 改「34 顆角托／46 個屬性值」；溢出基線改記 **51**（LS-46 R11 出處；LS-72 自述的 50 是 C 類 `mXQJh` 掃描法不穩定 −1，本票不併入）；HEAD 讀數 **56** ＝ 13+40+0+3+0，可歸因 delta 只有 B 角托 34→40（+6 ＝ Family A 六張印品的 TL 角托正確登記為 clip，visual-reviewer／merge-reviewer 各自重算背書）；C 列改「基線不可靠、未觸碰、不計入 delta」。對帳 51 + 6 − 1 ＝ 56。
- 取證：新增 `.claude/evidence/LS-81/r1/before/BDrtd-coords.txt`——python 唯讀解 `origin/development:design/littlesprout.pen`（merge-base 9383575）取 `BDrtd`/`eS0jX`/四顆角托座標、5pt 反解（BL/BR 713→820）、BDrtd 子樹 37 節點僅 `kSfzp`/`mIStI` 有差。**PNG 未補**：本輪依派工不開 Pencil MCP，無法從 base 渲染截圖。
- PR #140 body 已更新（保留 `Design:` 欄；加「R2 review 處置」與「未完成」段；過 `pr-body-check.sh`）。

**已驗證**
- R2 落地前 python 唯讀 node-by-node 比對 HEAD 8bd51d8 vs 工作樹：差異恰為 `f2Gm3.content`、`h7EnT.content` 兩筆，無夾帶
- `design-landing-check.sh design/littlesprout.pen --expect-nodes 2676` 綠（commit 前手動一次、pre-commit hook 一次）
- push gate 6 commits 全綠（unit tests／api-contract／error-codes／分支票號／與 origin/development `46e2e9e` 可乾淨合併）；remote head `e732619`
- CI 未等（依派工）

**未完成／informational 處置**（逐條）

visual-reviewer（ITERATE）：
- V-F1 戳記缺 frame 欄 → **已修** e732619
- V-F2 `h7EnT` 段序 → **已修** e732619
- V-F3 TODO 句／handoff 未完成欄矛盾 → **已修**（稿面就地結案＋本 handoff 未完成欄補正）
- V-F4 基線 50 vs 51 → **已修**（scan.md 帳目改正，−1 為 LS-72 繼承未結帳、本票不吸收）
- V-F5 溢出掃描無 gate → **另票 LS-68**

merge-reviewer（APPROVE）：
- M-F1 缺 `before/BDrtd.png` → **已修（文字證據 `before/BDrtd-coords.txt`）**；PNG 不補＋理由（本輪不開 Pen）；板端三節點 `lS22L`/`m7iph`/`orS6N` before **不補＋理由**：文字節點，修正前全文可由 merge-base blob 直接取得、merge-reviewer 已逐字比對
- M-F2 LS-30 交辦第②項未申報 → **未完成欄列入**：六個 token 板上對比欄（`paper-edge`／`photo-dim`／`disabled-fg`／`knob-edge`+`knob-shadow`／`switch-knob`+`switch-off`，需量測後新增 Color Table 列）**遞延至下一張開 Pen 的設計票**
- M-F3 skill `tokens.md:82` 併入後過期 → **另票處置**：skill 在 `main`、不在本 PR 修；併入 LS-30 R2-i4 的「LS-90 併入時清 skill 過期註記」批次，**LS-90 收尾時由 orchestrator 改 skill**
- M-F4 `h7EnT` 遞延無承接者 → **已修**（同 V-F3）
- M-F5 `h7EnT` 未帶部署警語 → **不修＋理由**：skill 已載該警語且指定 LS-90 清除；稿面再貼一句有到期日的敘述等於製造下一個過期 Notes
- M-F6 `WEjnC`/`sQsj1`/`h7EnT` 長高後可讀性 → **交 visual-reviewer**：其核銷輪未就此提退修項；R2 Tokens 板 after 圖 `.claude/evidence/LS-81/r2/after/orS6N.png`
- M-F7 scan.md 措辭 → **已修**

**風險**
- Pencil MCP 單一全域 in-memory instance（R1 已記）。R2 觀察 Pen 落地行為：`~/.pencil/backup/5a706d80c6a0…` 的 key ＝ sha1(`file:///Users/clyeh/little-sprout/.claude/worktrees/LS-81/design/littlesprout.pen`)，即 `open -a` 這次開的是 LS-81 worktree 路徑；該 backup mtime **15:50:47**、worktree 檔 mtime **15:52:35**、兩者內容逐位元相同（sha256 `8f808b68…`）。結論：Pen 開 worktree 檔後 autosave 仍只寫 `~/.pencil/backup/<sha1>`，worktree 檔是 1m48s 後被 cp 落地的——**未觀察到 Pen 直接寫 worktree 檔**（若直接寫，檔 mtime 不會晚於 backup 且內容還相同）。另 `8414ffd4…`（key ＝ 已刪除的 LS-72 worktree 路徑）內容 ＝ HEAD 8bd51d8 blob，印證 R1 handoff 所述「前一份文件停留在 LS-72 路徑」。LS-91 的固定 design worktree 方案可根治。
- CI 未等，PR body 改動不觸發 CI 重跑（`pull_request` 不含 edited）；本輪有 push 新 commit，CI 會跑 e732619。

**產出位置**
- PR：https://github.com/CLYEH/little-sprout/pull/140（head `e732619`，6 commits）
- Worktree：`.claude/worktrees/LS-81`（分支 `feature/LS-81-corner-tab-5pt`）
- 證據（gitignored，主 repo 路徑）：`/Users/clyeh/little-sprout/.claude/evidence/LS-81/r1/scan.md`（R2 改正）、`r1/before/BDrtd-coords.txt`（新增）、`r2/after/orS6N.png`

### 2026-08-25T08:23:46.506Z · Chih-Lin Yeh · `e7895d87`

## 收尾儀式（orchestrator，2026-08-25）

**① dead-code 巡檢**：設計 chore、單一 `.pen` 檔；merge-reviewer 已做結構級全量 diff（46 屬性值＋4 content＋1 變數，零多餘）、visual-reviewer 獨立重掃 80 顆角托——無死碼面，免 sweeper（理由記錄於此）。

**② Lesson learning review**
- **本票最大產出不是角托，是 Pencil 的三個事實**：(1) MCP `filePath` 無效、只作用於 app 開著的文件；(2) `open -a Pen <file>` 可機械切檔；(3) autosave 只寫 `~/.pencil/backup/<sha1(uri)>`，落地永遠是一次 cp，而分類器擋 agent 覆蓋設計檔 → **LS-91**（pen-open.sh／pen-land.sh＋agent 開工對帳）。
- **Review**：merge APPROVE（結構 diff 逐項吻合、BDrtd 107pt 修正以幾何反推證明）；visual ITERATE 5 項全是板端文字／帳目（戳記 scope 欄、段序、遞延項未列、基線 51 vs 50）→ R2 一次修完；visual-reviewer 對「+6 溢出」的獨立算術判定（Family A 3-of-4 不對稱＝bug 指紋）值得收進 LS-68 的掃描方法說明。
- **Agent 失聯 ×2**（ui-designer 97 分鐘無 commit被停；R2 後又失聯）→ LS-88／§3「一群一 commit、一輪一 agent」。
- **遞延**：六個 token 板上對比欄（LS-30 交辦）→ 下一張開 Pen 的設計票（LS-67）；skill `tokens.md:82` 過期句 → LS-90 收尾一併改。
- **溢出掃描無 gate**（visual-reviewer V-F5）→ LS-68 已承接。

**③ 部署**：設計檔，N/A；已隨首次 `promote.sh` 進 main `8830621`。→ Done。

