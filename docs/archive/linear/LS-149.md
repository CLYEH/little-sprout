# LS-149 Task：LS-23 後端——檢舉／封鎖／Owner 移除內容 RPC（content_reports／blocked_users 寫入路徑、封鎖過濾納入時間軸與留言、額度 LS002 對帳）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | lane:backend |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M6 合規與帳號 |
| 估點 | — |
| 建立 | 2026-09-03T08:56:53.061Z |
| 開始 | 2026-09-03T08:56:53.177Z |
| 完成 | 2026-09-03T11:52:10.502Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-149/taskls-23-後端-檢舉封鎖owner-移除內容-rpccontent-reportsblocked-users |
| 父票 | LS-23 Story：檢舉／封鎖／額度 UI＋EULA |
| 關係 | related→LS-121, related→LS-57, related→LS-143, related→LS-40, related→LS-22, related→LS-6, related←LS-218, related←LS-225, related←LS-219, related←LS-216, related←LS-186, related←LS-189, related←LS-183, related←LS-181, related←LS-175, related←LS-177, related←LS-172, related←LS-154, related←LS-150, related←LS-153, related←LS-146, related←LS-152, related←LS-132, related←LS-96 |

## 描述

## 來源

[LS-23](https://linear.app/little-sprout-app/issue/LS-23/story檢舉封鎖額度-uieula) story（檢舉／封鎖／額度 UI＋EULA，PLAN Phase 1-7、§9-A1 UGC 三件套、§10-A／B）拆後端先行（不需 Design gate）；UI 與 EULA 同意流程等設計稿另票。TestFlight 關鍵路徑（計畫 review 0903）。backend lane 有空位、使用者指示產線不停擺。

## 範圍

1. 先讀 PLAN §9-A1／§10-A／§10-B、API.md、既有 `content_reports`／`blocked_users` 表與 RLS（[LS-6](https://linear.app/little-sprout-app/issue/LS-6/phase-0-2supabase-專案schema-migrationrls-policies) 建表；若欄位不足以 migration 補，非破壞性）。
2. RPC：`report_content(kind, ref_id, reason)`（任何成員可檢舉同家庭內容；寫 `content_reports`；同人同內容去重）、`block_user(user_id)`／`unblock_user`（封鎖後對方內容在時間軸／留言／相簿查詢一律過濾——改 `get_family_timeline` 與留言查詢加 `NOT EXISTS blocked_users` 條件，或 RLS 層；handoff 說明取捨與效能 EXPLAIN）、`remove_content_as_owner(kind, ref_id)`（Owner 移除他人內容 → 既有 soft delete＋`deleted_by`，並把相關 report 標 resolved）。
3. Owner 收得到檢舉：`content_reports` 對 Owner 可讀（RLS）＋通知列（沿用 `notifications` 表，型別 `report`）——「進到你看得到的地方」§10-B。
4. 額度 §10-A：確認 LS002 超額擋上傳已在（[LS-40](https://linear.app/little-sprout-app/issue/LS-40/taskls-20-後端-storage-bucketrls-policy路徑規約)／128）；補「額度查詢 RPC」`get_family_quota()` 回 `storage_used_bytes`／上限，供 UI 顯示。
5. 測試：檢舉去重、封鎖過濾（時間軸／留言／相簿三處各一案）、Owner 移除＋report resolved、非 Owner 不能移除他人內容、RLS 跨家庭隔離、`api-contract-check.sh` 綠；PR body mutation 清單（拿掉封鎖過濾 → 測試紅）。
6. `docs/API.md` §3／§5／§6 同步。

## 不做

iOS UI／EULA 同意畫面（另票，需 Design gate）；推播（[LS-22](https://linear.app/little-sprout-app/issue/LS-22/story留言愛心與彙總推播)）。

## 驗收

- [ ] migration 非破壞性、測試綠、API.md 更新
- [ ] merge-reviewer 通過（race／效能：封鎖過濾對時間軸 keyset 查詢的 EXPLAIN）
- [ ] QA 以 SQL 三角色實測

## 留言（17）

### 2026-09-03T09:13:10.811Z · Chih-Lin Yeh · `e7059ac6`

開工：worktree 已建於 `.claude/worktrees/LS-149`，branch `feature/LS-149-report-block-rpc`，head `40dd1cd`（= origin/development）。目前在做設計調查（讀 PLAN §9-A1/§10-A/§10-B、API.md、既有 migrations 與 tests 慣例），尚未落 migration。收到 LS-143 相鄰表提醒，會避開 profiles／auth 相關改動。

### 2026-09-03T09:16:09.555Z · Chih-Lin Yeh · `7c368be3`

已修正：兩支 migration 檔誤寫到主 checkout（Write 工具用了絕對路徑但忘了指向 worktree），已 cp 進 `.claude/worktrees/LS-149/supabase/migrations/`、主 checkout 用 `rm` 清掉，`git -C /Users/clyeh/little-sprout status --porcelain` 現在是空的。之後所有檔案操作一律用 worktree 絕對路徑（`/Users/clyeh/little-sprout/.claude/worktrees/LS-149/...`）。continuing 實作。

### 2026-09-03T09:49:19.242Z · Chih-Lin Yeh · `5d5ee898`

## handoff

**Ticket**：LS-149（LS-23 後端切片）——檢舉／封鎖／Owner 移除內容 RPC＋封鎖過濾。

**已完成**（皆有對應 commit SHA＋檔案）：
- `report_content`／`block_user`／`unblock_user`／`remove_content_as_owner`／`get_family_quota` 五支 RPC：`aa2f75a` `supabase/migrations/20260903091317_report_block_rpc.sql`
- `notification_kind` 新增 `'report'`（獨立 migration＋獨立交易，避開 `ALTER TYPE ADD VALUE` 同交易內使用限制）：`aa2f75a` `supabase/migrations/20260903091313_notification_kind_report.sql`
- 封鎖過濾三處（時間軸／留言／相簿）：`albums_select`／`comments_select` 疊加 `NOT EXISTS`、`get_family_timeline`／`list_comments` 加同一種過濾，皆在 `aa2f75a` `supabase/migrations/20260903091317_report_block_rpc.sql`
- 測試：`55b0cd5` `supabase/tests/100_report_block_rpc.sql`（新增，14 項斷言全綠）＋既有測試檔調整（`10_cross_family_isolation.sql`／`60_default_privileges.sql`／`run.sh` 的兩位數編號上限修正）
- API.md §2／§3／§4／§5／§9 同步：`029f0fd`

**已驗證：怎麼驗的**
- 本機 `bash scripts/ops/supabase-lock.sh -- supabase db reset` 套用兩支 migration 成功；`bash scripts/ops/supabase-lock.sh -- bash supabase/tests/run.sh` 全綠（含 `10_cross_family_isolation.sql`／`20_role_permissions.sql`／`50_rls_plan_no_percall_subquery.sql` 等既有回歸，以及新增的 `100_report_block_rpc.sql` 14 項斷言）。
- 效能：`get_family_timeline` 第一版無條件加封鎖過濾導致篩 child 分支從 136 buffers 惡化到 2015（本機用既有 50_ 效能資料集實測抓到），改成 `v_has_blocks` 一次性判斷後回到 138（門檻 ≤200），四條分支與 `list_comments` 兩條分支皆通過既有效能 gate。
- Mutation：本機直接對活資料庫 `ALTER POLICY` 拿掉 `comments_select` 的 `NOT EXISTS`，重跑 `100_report_block_rpc.sql` 實測轉紅（`FAIL 封鎖過濾（留言／直接 SELECT）`），`list_comments` 那組獨立斷言仍綠（證明兩層過濾各自獨立測試釘住）；驗證後 `supabase db reset` 復原，migration 檔本身未被動過。
- `bash scripts/gates/api-contract-check.sh`／`bash scripts/gates/error-codes-check.sh`／`bash scripts/gates/pr-body-check.sh` 皆綠；`gh pr checks 248 --watch` 全部通過（db／lint／ci／rules 四項皆 pass）。

**未完成／剩餘**：無 reviewer informational 待處置（首次送審，尚無 review 意見）。範圍縮限記在 PR body：`media_select`／`diaries_select` 未加封鎖過濾（票文三處不含這兩張表）、`media` 沒有 `deleted_by`（既有 schema 缺口）、`blocked_users` 既有直接寫入路徑刻意未收回。

**風險**：`ALTER POLICY`／`CREATE OR REPLACE FUNCTION`（既有函式）已標 BREAKING，PR body 已附段落；LS-143 同時在改相鄰表，本票只碰 `get_family_timeline`（加封鎖過濾條件，簽章與四分支結構保留供對照），未碰 `profiles`／`auth`。

**產出位置**：PR [#248](https://github.com/CLYEH/little-sprout/pull/248)，head `029f0fd42ed61772ad64aa226d48aa86a27e4019`，`feature/LS-149-report-block-rpc` → `development`。模擬器已關：無（本票未使用模擬器）。

### 2026-09-03T09:50:26.253Z · Chih-Lin Yeh · `d7d9f179`

orchestrator 2026-09-03 17:52：handoff `5d5ee898` 收到——PR **#248** head `029f0fd`（enum 新 kind 獨立 migration、五支 RPC＋`private.blocked_pairs()`、封鎖過濾納入 albums／comments RLS 與 `get_family_timeline`／`list_comments`；`v_has_blocks` 分流修掉 136→2015 buffers 效能回歸；`100_report_block_rpc.sql` 14 斷言＋live mutation；`run.sh` glob 放寬到三位數＋`sort -V`；不新增錯誤碼），CI 四綠 → In Review、派 merge-review R1（重點：EXPLAIN 兩路徑 buffers、keyset 與過濾互動、RLS 既有測試、mutation 實跑、與 LS-143 #244 語意交錯與併入順序）。自報主 checkout 誤寫已清（LS-96 `f22f0645`）。

### 2026-09-03T10:08:16.966Z · Chih-Lin Yeh · `916101ef`

## merge-review R1 — PR #248（head `029f0fd`）

**判定：REQUEST_CHANGES**（1 major、3 minor、2 informational）

審查範圍 `git diff 4268567...029f0fd`（2 支 migration、`100_report_block_rpc.sql`、`10_`／`60_`／`run.sh`、API.md）。**PR diff＝worktree diff**（兩邊 `shasum` 同為 `0e6926f1…`），主 checkout `git status` 乾淨——票文第 6 項「早期誤寫到主 checkout 已清乾淨」查實成立。`gh pr checks 248` 四綠（ci／db／lint／rules），PR body 已過 gate（本輪首審、無 informational 待處置，不需要 LS-96 行）。

實跑（皆經 `scripts/ops/supabase-lock.sh`，跑完已 `supabase db reset` 還原共用容器）：
1. `supabase db reset` ＋ `supabase/tests/run.sh` 全綠，21 個檔、順序 `00→…→99→100` 正確。
2. 自建 EXPLAIN 實驗（50k albums／200k feed_items／6000 標記列三組資料集，比照 `50_` 手法）量兩條路徑 buffers。
3. Mutation A（拿掉去重）→ 紅；Mutation B（拿掉檢舉通知 trigger）→ **全綠**（見 major-1）。

---

### 一、逐項查實（票文八項）

**1. enum migration 順序安全 ✅**
`20260903091313_notification_kind_report.sql:16` 單獨一支只做 `alter type … add value 'report'`，第二支才建用到它的 trigger 函式——本機 `db reset` 逐檔套用成功（log 逐行 `Applying migration …`），`db push` 走同一條逐檔路徑，正式站順序安全。
**彙總誤合併：不會。** `private.record_notification_event`（`20260825020000_…:629`）的合併鍵與 advisory lock 鍵都含 `p_kind`，`report` 併不進 `comment`／`album` 事件。實跑驗證：以 a2 呼叫 `report_content('album', 4a…001)` 後 `notification_events` 多出獨立一列 `report|album|4a…|actor=a2|count=1`，既有列未被改動。

**2. 五支 RPC 與 definer／grant 面 ✅（另見 minor-2）**
- `report_content`（`:516-571`）：membership 42501、`LS026` 跨家庭、`reason` 長度沿用建表 CHECK `char_length(btrim(reason)) between 1 and 1000`（→23514），去重靠 partial unique index（`:512`）＋`ON CONFLICT DO NOTHING`（`:554`）。
- 併發去重**沒有回傳 NULL 的競態**（我一開始懷疑、查證後推翻）：`ON CONFLICT DO NOTHING` 的 speculative insert 會等待同鍵的未完成交易，等對方 commit 後 `:559` 的 fallback SELECT 在 READ COMMITTED 下取新快照必定看得到那列。只有 REPEATABLE READ 才會回 NULL，PostgREST 預設不是。
- `block_user`／`unblock_user`（`:581`／`:611`）：自我封鎖交給既有 `blocked_users_not_self`（23514）、冪等正確。`p_blocked_id` 不驗成員身分——與既有 `blocked_users_insert` policy 的能力一致（policy 也只管 `blocker_id = 我`），沒有新增攻擊面。
- `remove_content_as_owner`（`:645`）：只認 owner、四種 kind 都對得上 `private.target_family_id`（含 `'comment'`，我特地查過既有函式確有 comment 分支）；album／diary／comment 走既有三支 RPC 所以 `deleted_by` 由既有 trigger 推導，media 直接 `UPDATE deleted_at`（`:690`）——**這條路徑的 `families.storage_used_bytes` 是對的**：`media_storage_update` statement trigger 照樣扣減（`20260822120100_triggers.sql:230-249`）。
- `get_family_quota`（`:711`）回傳 `families.storage_used_bytes／storage_quota_bytes`，正是 LS002 判定讀的同兩個欄位（`triggers.sql:256`）——票文「同一來源」成立。

**3. 封鎖過濾效能 ✅（RLS 這邊比 handoff 說的更好，時間軸那邊我補了實測，見 minor-1）**

`albums_select`／`comments_select` 疊加的 `NOT EXISTS`（`:126-142`）**沒有退化成 per-row 子查詢**。50k albums 實測（authenticated 身分）：

| 情境 | plan | buffers |
|---|---|---|
| 舊 policy（本 PR 前基準） | Seq Scan＋hashed SubPlan 1 | 1412 |
| 新 policy、呼叫者無封鎖 | 同上＋`(hashed SubPlan 3)`、`Function Scan on blocked_pairs … loops=1` | 1414 |
| 新 policy、呼叫者有 1 筆封鎖 | 同上 | 1414 |

`blocked_pairs()` 雖是 SECURITY DEFINER（不可 inline），規劃器仍把相關 `NOT EXISTS` 收成 **hashed SubPlan、只求值一次**，PLAN §5 的紅線沒有被踩。

`get_family_timeline` 兩條路徑（200k feed_items＋600 標記列，＝`50_` 的資料集）：

| 分支 | v_has_blocks=false | =true |
|---|---|---|
| 1 不篩 child、首頁 | 51 | 92 |
| 2 不篩 child、深頁 | 51 | 117 |
| 3 篩 child、首頁 | 138 | **1881** |
| 4 篩 child、深頁 | 85 | 990 |

分流確實把常見路徑守回原值（138＝套件內既有數字），這點 handoff 屬實。有封鎖時分支 3 的 1881 與 PR body 自陳的「約 2396 buffers／7ms」同一量級，**我獨立量到、也獨立確認它是有界的**：把該 child 的標記列從 600 拉到 6000 後，規劃器改選 `Nested Loop Anti Join`＋`Index Scan using feed_item_children_family_child_occurred_idx`＋提早 LIMIT，buffers 反而降到 **143**（手寫等值查詢的 plan 我也印出來核對過）。也就是說壞成本只發生在「候選集合小到規劃器認為整段掃比較划算」的中間帶，不是隨資料量線性成長——PR body 把它列為已知代價是誠實且成立的。

**keyset 正確性：沒有問題。** 過濾條件是內層子查詢的 WHERE 述詞、在 `order by … limit` **之前**求值，不是 LIMIT 後再篩，所以一頁仍然滿 20 列、游標永遠落在真正回傳的最後一列上，不會跳列也不會重複。這點很關鍵：`TimelineStore.swift:101,145` 是 `hasMorePages = pointers.count == pageSize`，若當初寫成「先 LIMIT 再篩」，封鎖者的時間軸會在第一頁直接被判定成沒有更多而截斷——現在的寫法不會。

既有 RLS 測試（`50_`／`10_`／`20_`／`86_`）在完整套件裡全綠。`10_cross_family_isolation.sql` 把 `'comments'` 移出 a1 的正向迴圈、另用 a2 驗（`:102-140`）是合理的：a1 封鎖 a3 而 A 家唯一留言正是 a3 發的；跨家庭隔離方向（`:37`／`:229`）仍然涵蓋 comments，防護沒有被削弱。

**4. 測試鑑別力 ✅（去重）／❌（通知，見 major-1）**
我在本機重做的 mutation：`drop index content_reports_reporter_target_pending_key` ＋ 拿掉 `report_content` 的 `ON CONFLICT`（其餘邏輯保留）→ `100_` 立刻紅在第一段去重斷言（`ERROR: FAIL：重複檢舉沒有回傳既有的 id…`，exit 3）。`run.sh:134` 的 `sort -V` 排序實測正確（00→10→…→99→100）。

**5. 錯誤碼 ✅**
沒有新增自訂碼，`LS026`／`42501`／`23514` 的語意都對得上：`LS026` 原文就是「target 存在但屬於別的家庭」，用在檢舉是同一種歸屬檢查；「找不到內容」與「不是 owner」合併成 42501 與既有 LS015／LS020 慣例一致。API.md §2／§3／§4／§5／§9 與 `API-CONTRACT:RPC` 區塊都同步了（`api-contract-check` 綠）。**「檢舉自己的內容」目前不擋、也沒有碼**——票文沒要求，UI 不提供入口即可，記錄在案不算缺陷。

**6. PR 完整性 ✅**（見開頭）

**7. 與 LS-143 #244 → informational-2**

**8. 四維度＋gate ✅**
Scope 乾淨：diff 全部可追溯到票文，沒有順手重構、沒有無關改動；`10_`／`60_`／`run.sh` 的改動都是本票功能直接逼出來的。race condition 面向上，五支 RPC 的併發語意我逐一走過（去重 speculative insert、封鎖冪等、`remove_content_as_owner` 依賴既有三支 RPC 的 `FOR UPDATE`、report→resolved 冪等），沒有找到問題。

---

### 二、Findings

**major-1｜`supabase/migrations/20260903091317_report_block_rpc.sql:735-760` — 檢舉通知（新 enum 值＋新 trigger）零測試覆蓋**

`notification_kind` 新增 `'report'` 與 `content_reports_notify_insert` trigger 是票文範圍第 3 項（§10-B「檢舉要進到你看得到的地方」）的資料層交付物，但整個測試套件沒有任何一處提到 `'report'` 或驗證它產生 `notification_events` 列（`grep -rn "'report'" supabase/tests/*.sql` 無結果；`87_` 只涵蓋既有四種 kind），PR body 的 mutation 清單也沒有這一項。

**具體失敗情境（已實跑）**：`drop trigger content_reports_notify_insert on public.content_reports;` 之後跑完整 `supabase/tests/run.sh` → **`✓ 全部 DB 測試通過`、exit 0**。也就是說「檢舉不再產生任何通知事件」這個功能整個消失，CI 一句話都不會說。後續任何人動 `record_notification_event` 的簽章／合併邏輯、或 refactor 掉這支 trigger，都不會被擋。功能本身是好的（我實測 `report_content` 會產生 `report|album|4a…|count=1` 的事件列），問題純粹在沒有東西釘住它。

**建議修法**：`100_report_block_rpc.sql` 加一段（約 12 行）——在交易內以成員身分 `report_content(...)`，`reset role` 後斷言 `notification_events` 中 `kind='report' and target_type/target_id` 對得上、`actor_id`＝檢舉人、`event_count=1`；順帶斷言同一目標第二位檢舉人在 5 分鐘窗內會累加成 `event_count=2`（這正好也把「kind 不會誤併」釘住）。修完把這條加進 PR body 的 mutation 清單。

**minor-1｜`…:189-191, 315-319` — `v_has_blocks=true` 這條新路徑沒有任何效能 gate**

`50_` 的 perf 帳號（`c0…001`）沒有封鎖任何人，所以四條分支量到的一律是 `v_has_blocks=false` 變體——**本 PR 新增的、風險已知較高的那條變體，效能上完全沒有被測到**（handoff 也自陳如此）。我實測它在 `50_` 的資料集上是 1881 buffers，超過該檔案 200 的既有預算 9 倍；今天沒紅只是因為量的不是這條路徑。

**具體失敗情境**：日後有人動 `feed_item_actor_id` 或那四段 `NOT EXISTS`（例如改成查更多表、或不小心讓它不再是 STABLE），有封鎖關係的使用者那條路徑可以退化到數千至數萬 buffers，而 CI 全綠。

**建議修法**：在 `50_` 的 timeline 段落尾巴補一個 case——交易內插一列 `blocked_users`（blocker＝perf 帳號）後重量分支 3／4，用一個明寫理由的較寬預算（例如 ≤2500，並在註解裡寫明「這條路徑刻意接受較高成本，門檻只擋數量級退化」），跑完該交易本來就 rollback。若判定不值得現在做，請按 §5-b 記入 LS-96 而不是靜默。

**minor-2｜`…:89-114` — `private.feed_item_actor_id()` 對 `authenticated` 開放且無成員檢查，是一個新的 RLS 繞行面**

這支是 SECURITY DEFINER、直接 grant 給 `authenticated`（`:111-114`），本體不驗呼叫者與該內容的家庭關係。任何登入者只要拿到某個 uuid，就能問出**任何家庭**任何 album／diary／media 的作者 id。migration 註解（`:57-60`）說「不構成新的資訊外洩面」——嚴格說並不成立：本 PR 之前 `albums_select` 一定擋得住這個問題，現在多了一條繞過去的路。

**具體失敗情境**：`select private.feed_item_actor_id('album', '<別家的 album uuid>')` 回傳該相簿建立者的 profile id。可利用性趨近於零（v4 uuid 不可猜、別家內容的 uuid 也不會流到這個使用者手上），所以標 minor 不標 blocker。

**建議修法**：兩條路二選一——(a) 接受現狀，但把註解從「不構成新的外洩面」改成誠實描述「多了一條需要事先知道 uuid 才能用的繞行面，判定不可利用故接受」；(b) 函式內加一句 family 成員檢查（只在 `v_has_blocks=true` 的路徑上逐列執行，成本落在本來就較貴的那條路徑）。我傾向 (a)，改註解即可。

**minor-3｜`supabase/tests/run.sh:134` — 未加引號的命令替換，路徑含空白會靜默壞掉**

`for f in $(printf '%s\n' "$here"/[0-9]*_*.sql | sort -V); do` 的結果會被 word splitting 切開。目前 worktree 路徑沒有空白所以無事，但改動前的 glob 寫法本來是抗空白的——這是這次順手放寬 glob 時無意犧牲掉的性質。

**具體失敗情境**：任何人把 repo clone 到含空白的路徑（例如 `~/Library/Mobile Documents/…`），測試 runner 會去找不存在的檔案而不是照常執行；因為外層有 `set -Eeuo pipefail`，症狀是難以理解的中止而不是清楚的錯誤。

**建議修法**：改 `while IFS= read -r f; do … done < <(printf '%s\n' "$here"/[0-9]*_*.sql | sort -V)`，一行的事。（若嫌超出本票範圍，記 LS-96 即可。）

**informational-1｜`…:726-733` — `kind='report'` 的收件人範圍只寫在 SQL 註解裡**

`notification_events` 沒有收件人欄位。LS-22 的 Edge Function 若照既有四種 kind 的形狀廣播給全家庭成員，「有人檢舉了這則內容」（含 `actor_id`＝檢舉人）會送到全家，包含被檢舉者本人——與 §10-B 的「只有 owner 讀得到 content_reports」直接矛盾。migration 註解 `:730-732` 有寫「留給 LS-22」，但 Linear 的 LS-22 票文裡沒有這條需求。建議把它寫進 LS-22 的驗收條件（一行），否則這個限制只活在一個沒人會回頭讀的 SQL 註解裡。

**informational-2｜與 LS-143 PR #244 的併入順序**

- **語意無交錯**：#244 的檔案清單不含 `get_family_timeline`／`list_comments`／`albums_select`／`comments_select`，本票也沒碰 `profiles`／`auth`。被封鎖者刪帳號時：`blocked_users` 對 `profiles` 是 `on delete cascade`（封鎖列消失）、`comments.author_id`／`albums.created_by` 是 `on delete set null`（過濾條件自然失配），而該成員的內容由 `delete_my_account` 軟刪——三者一致，不會出現「內容還在、封鎖失效」的組合。被檢舉內容的作者離開家庭：`content_reports.reporter_id` 是 `on delete set null`，報告本身留著給 owner／平台方看，partial unique index 對 NULL 不成立、不影響去重語意。
- **文字衝突：必然發生兩處**。`supabase/tests/60_default_privileges.sql` 兩邊都改同一行（`'public.set_child_deleted(uuid, boolean)'` 後面各自追加）；`docs/API.md` 的 `API-CONTRACT:RPC` 區塊也在同一段（`delete_my_account()` 與 `get_family_quota(uuid)` 相鄰）。`run.sh` **不會**衝突（本票在 `:134`、#244 在 `:385`／`:415`）。
- **建議順序**：先併 #244（它的併發測試較重、rebase 成本高），本票 rebase 後只需手動接兩處 array／清單，再跑一次 `run.sh` 即可。反過來也行，成本對稱，但別讓兩邊同時 rebase。

---

### 三、沒看的範圍（誠實聲明）

- iOS 端：本 PR 沒有 Swift 改動，我只讀了 `TimelineStore.swift`／`SupabaseTimelineAPIClient.swift` 兩處用來判斷分頁與 albums 查詢形狀是否被影響（結論：沒有）。
- 正式站 `db push` 本身沒有實跑，只以本機逐檔套用推論順序安全。
- QA 的三角色 SQL 實測（票文驗收第三條）不在本輪範圍。

### 判定

**REQUEST_CHANGES** — major-1（檢舉通知零覆蓋，已用實跑 mutation 證實整套測試對它的消失無感）必須先補上；minor-1／2／3 請逐條處置（已修／記 LS-96／不修＋理由，handoff 一條都不能省）。除此之外這個 PR 的品質很高：RLS 的 hashed SubPlan 結果比 handoff 自陳的還乾淨、keyset 與過濾的互動是對的、scope 沒有一行溢出、設計裁量都留了理由在檔頭。

### 2026-09-03T10:09:44.363Z · Chih-Lin Yeh · `9175dbab`

orchestrator 2026-09-03 18:09：merge-review R1 `916101ef` REQUEST_CHANGES（major-1 檢舉通知 trigger 零測試：drop trigger 全套件仍綠；minor：有封鎖路徑無效能 gate（1881 vs 138 buffers，6000 列時規劃器改 anti join 降 143）、definer 註解、`run.sh:134` 引號；RLS 效能與 keyset 正確性經實跑確認優於自陳）→ 令 agent R2：補通知斷言＋mutation、效能 gate、兩處小修；併入順序 **#244（LS-143）先**，本 PR 之後 merge development 解 `60_default_privileges.sql`／API.md 文字衝突。

### 2026-09-03T10:25:57.316Z · Chih-Lin Yeh · `33b4ef82`

## R2 handoff

**回應**：merge-review R1 `916101ef`（REQUEST_CHANGES：1 major、3 minor、2 informational）。

**major-1**（`content_reports_notify_insert` trigger 零測試覆蓋，reviewer 實跑 drop trigger 後整套仍全綠）：已修 `bdd1d6f` `supabase/tests/100_report_block_rpc.sql` 新增「1b. report_content 觸發檢舉通知」段——斷言 kind=report 事件的 actor／event_count 正確、不與既有 kind=album 事件誤併、同人重複檢舉不重複計數、第二位檢舉人在 5 分鐘視窗內正確累加成 2。本機實跑 mutation（`drop trigger content_reports_notify_insert`）確認新斷言正確轉紅（`FAIL：report_content 沒有正確產生 kind=report 通知事件`），驗證後已 `supabase db reset` 復原。

**minor-1**（`v_has_blocks=true` 路徑無效能守門，reviewer 量到 1881 vs 138 buffers）：已修 `bdd1d6f` `supabase/tests/50_rls_plan_no_percall_subquery.sql` 新增專屬 DO 區塊——讓效能帳號封鎖一個非作者的合成 profile 觸發 `v_has_blocks=true`，量四條分支，門檻 ≤2500（v_has_blocks=false 那組是 200，這裡只擋數量級退化）。本機實測分支 1/2/3/4＝105／95／1880／992 buffers，與 reviewer 獨立量到的 92/117/1881/990 同一量級。

**minor-2**（`private.feed_item_actor_id()` 註解「不構成新的資訊外洩面」不誠實）：已修 `7d62b74` `supabase/migrations/20260903091317_report_block_rpc.sql:57-65`——採 reviewer 建議方案 (a)，改成誠實描述這是一條需要事先知道目標 uuid 才用得上的繞行面，可利用性趨近於零，判定接受現狀不加成員檢查。純註解修正，函式行為不變。

**minor-3**（`run.sh:134` 未加引號的命令替換，路徑含空白會靜默壞掉）：已修 `7d62b74` `supabase/tests/run.sh:128-149`——改用 `while read -r f; do … done < <(... | sort -V)`，process substitution 逐行讀取不做 word splitting。

**informational-1**（`kind='report'` 收件人範圍只寫在 SQL 註解，LS-22 票文沒有這條需求）：不修本票程式碼——已在 LS-22 留言記錄（comment `0b83fd0d-aa31-4c96-becf-5624bdac2d82`），提醒 Edge Function 實作時對 `kind='report'` 只通知 owner，不能比照其餘四種 kind 廣播全家庭。

**informational-2**（與 LS-143 #244 併入順序）：不修（現在）＋理由——orchestrator 已裁定 #244 先併，本 PR 之後才 merge development 解 `60_default_privileges.sql`／API.md 兩處文字衝突；**依指示本 PR 目前未 merge development**，等待通知。

**已驗證**：本機 `supabase db reset` ＋ `bash supabase/tests/run.sh` 全綠（含新增的兩段斷言）；`api-contract-check.sh`／`error-codes-check.sh`／`pr-body-check.sh` 皆綠；`gh pr checks 248 --watch` 四項全 pass（db／lint／rules／ci）。

**新 head SHA**：`7d62b74566788307d76e82afa2760df71b556537`（R2 commits：`bdd1d6f` 測試新增、`7d62b74` 註解與 run.sh 修正）。

**CI 狀態**：db pass（2m50s）／lint pass（15s）／rules pass（2m58s）／ci pass（5m37s），四項全綠。

**PR**：https://github.com/CLYEH/little-sprout/pull/248（PR body 已同步更新 R2 內容，含逐項處置與 mutation 清單新增項）。

### 2026-09-03T10:39:45.333Z · Chih-Lin Yeh · `18e5d5bd`

已 merge dev 782bf05、全套件綠。

`git fetch origin && git merge origin/development` 解 2 處文字衝突（`docs/API.md` §4／API-CONTRACT:RPC 機械清單、`supabase/tests/60_default_privileges.sql` 的 `v_definer_rpcs` 陣列），皆為兩票各自新增段落的純累加、雙方內容都保留，未取捨任何一邊；`run.sh` 自動合併無衝突。Merge commit（保留、未 rebase）：`e3c29c8`。

`supabase db reset` ＋完整 `supabase/tests/run.sh`（兩票 schema 合併後第一次完整跑，含 LS-143 的 `91_delete_account.sql`＋兩組併發情境、我的 `100_report_block_rpc.sql`）全綠：

```
✓ 全部 DB 測試通過
```

特別確認：
- `50_rls_plan_no_percall_subquery.sql` 效能 gate 仍綠——get_family_timeline 分支 1-4（v_has_blocks=false）＝46／51／138／85 buffers，分支 1-4（v_has_blocks=true）＝105／95／1880／992 buffers，list_comments 分支 1-2＝11／26 buffers，皆與 merge 前數字一致（LS-143 的改動未影響這條路徑）。
- `60_default_privileges.sql` 仍綠——`v_definer_rpcs` 陣列合併後同時含 LS-149 四支與 LS-143 的 `delete_my_account()`，白名單斷言全過。
- `91_delete_account.sql` 六項斷言全過；`100_report_block_rpc.sql` 十七項斷言全過。

`bash scripts/gates/api-contract-check.sh`／`bash scripts/gates/error-codes-check.sh` 皆綠（30 支 RPC、21 張表、24 個自訂碼，含 LS-143 的 `delete_my_account()`／`LS050`）。

`git push` 過 push-gate；`gh pr checks 248 --watch` 四項全 pass（db 2m59s／lint 14s／rules 2m55s／ci 7m3s）。

**新 head SHA（merge 後）**：`e3c29c88105a8fde53d28c76dfe65247162290ef`
**PR**：https://github.com/CLYEH/little-sprout/pull/248

### 2026-09-03T10:44:02.526Z · Chih-Lin Yeh · `be89f4d3`

orchestrator 2026-09-03 18:43：merge handoff `18e5d5bd` 收到——merge commit `e3c29c8` 併 development 782bf05（LS-143），兩處文字衝突純累加解掉，全套件（含 `91_delete_account` 併發案例）＋效能 gate＋`60_default_privileges` 綠，CI 四綠 → 派 merge-review **R2**（delta `029f0fd..e3c29c8`：R1 兩項複核＋trigger mutation、封鎖分支 buffers 口徑、五處過濾一致、merge 三處正確）。

### 2026-09-03T11:02:22.002Z · Chih-Lin Yeh · `9542908d`

## merge-review R2 — PR #248（head `e3c29c8`）

**判定：APPROVE**（0 blocker、0 major、2 minor、3 informational）

審查範圍：R2 delta `git diff 029f0fd..e3c29c8`（commits `bdd1d6f`／`7d62b74` ＋ merge commit `e3c29c8`），並以 `git diff 782bf05..e3c29c8` 對照本 PR 相對 development 的淨貢獻（8 檔）。worktree（`e3c29c8`）與主 checkout `git status` 皆乾淨；`gh pr view 248 --json headRefOid` 與我實際 diff 的 SHA 一致（審查期間 head 未前進）；`gh pr checks 248` 四綠。

**本輪實跑**（皆經 `scripts/ops/supabase-lock.sh`，每段跑完 `supabase db reset` 還原共用容器，最後容器已還原、lock 已釋放）：

1. `supabase db reset` ＋完整 `supabase/tests/run.sh`（head e3c29c8，兩票 schema 合併後）→ `✓ 全部 DB 測試通過`。
2. Mutation A：`drop trigger content_reports_notify_insert` → 完整套件 **紅**（exit 1）。
3. Mutation B：把 `private.feed_item_actor_id()` 改成每列多掃一次 `public.media`（語意不變、只變貴）→ 完整套件 **紅**（exit 1）。
4. EXPLAIN 探針：`v_has_blocks` 判斷本身、分支 1／分支 3 的內層靜態查詢、`albums_select`，＋分支 3／4 重複量測 ×3。
5. 自建實驗：b1 封鎖 b2 後，對四條 `v_has_blocks=true` 分支各驗一次過濾正確性。

---

### 一、R1 兩項複核（兩項都真的解掉）

**R1-1 通知 trigger 零覆蓋 → 已解，我用 mutation 證實**

`supabase/tests/100_report_block_rpc.sql:71-180` 新增的「1b」段有實質鑑別力。實跑 `drop trigger content_reports_notify_insert on public.content_reports;` 後跑**完整** `run.sh`：

```
→ 100_report_block_rpc.sql
  ✗ 100_report_block_rpc.sql 失敗：
    ERROR:  FAIL：report_content 沒有正確產生 kind=report 通知事件（實際 count=<NULL>／actor=<NULL>…）
```

exit 1。對照 R1 同一個 mutation 是**整套全綠**——這條防線現在確實存在。斷言涵蓋 orchestrator 交辦的三件：誰收到（`actor_id`＝檢舉人，`:118-124`）、去重不重複計數（同人二次檢舉 `event_count` 維持 1，`:143-153`）、不與既有 `kind='album'` 事件誤併（`:127-134`），另加第二位檢舉人 5 分鐘窗內累加成 2（`:156-173`）。**「封鎖對不影響」我另外查證過**：`notification_events` 對 `authenticated`／`anon` 無任何 grant，寫入路徑是 statement trigger、讀取一律 `reset role`，封鎖過濾（只作用在 `albums_select`／`comments_select`／兩支 RPC）碰不到這張表，所以「封鎖不影響檢舉通知」在資料層是結構性成立、不需要額外斷言。

**R1-2 效能 gate → 已解，且我證明它有牙齒**

`supabase/tests/50_rls_plan_no_percall_subquery.sql:644-743` 新增的專屬 DO 區塊。

- **門檻與餘裕**：我獨立量到 `106／94／1880／992`（agent 自報 105／95／1880／992、R1 量到 92／117／1881／990，三方同一量級）。分支 3 重複量測 ×3＝`1880／1880／1890`（±0.5%）。門檻 2500 對最貴的分支 3 是 **1.33× 餘裕**（33%）——偏緊但數字非常穩定，且 CI db job 獨立跑過綠，可接受。
- **有沒有牙齒**：mutation B（`feed_item_actor_id()` 每列多一次全表掃描，回傳值不變）→ 分支 1 從 106 暴增到 **22338 buffers**，新 gate 紅；同一次跑，既有的 `v_has_blocks=false` 那組（門檻 200）維持 `41／51／138／84` **全綠**。也就是說這類退化**只有**新增的這道 gate 攔得住，補得正確。

**1880 buffers 是不是 per-row subquery？——不是 `blocked_pairs()` 的問題，是 `feed_item_actor_id()` 的逐列呼叫，且在 LIMIT 之前。** 我實測分支 3 內層靜態查詢的 plan：

```
Limit → Sort(top-N) → Hash Anti Join  Buffers: shared hit=1817
  Hash Cond: (private.feed_item_actor_id(fc.kind, fc.ref_id) = bp.blocked_id)
  -> Seq Scan on feed_item_children fc  (rows=600)  Buffers: shared hit=15
  -> Hash -> Function Scan on private.blocked_pairs bp (loops=1) Buffers: shared hit=2
```

- `private.blocked_pairs()` **一次物化**（Hash 側 `loops=1`、2 buffers），不是逐列——PLAN §5 的紅線沒被踩；`albums_select` 那邊同樣是 `(hashed SubPlan 3)`（我另外量了一次，`never executed` 因為此資料集 albums 只有 2 列；50k 列的證據沿用 R1 的量測，我沒有重建 50k 資料集）。
- 1817 中的 ~1800 全部來自 `feed_item_actor_id()` 對 **600 個候選列**逐一求值（≈3 buffers／列），成本跟「候選集合大小」走、不跟 LIMIT 走。這是 per-row **函式呼叫**（不是 per-row RLS 子查詢），只發生在 `v_has_blocks=true` 這一條，migration `:158-175` 已把取捨寫清楚，R1 也驗過候選集合放大到 6000 列時規劃器改走 anti join + index → 143 buffers，不隨資料量線性成長。分支 1 的 plan 是 `Nested Loop Anti Join` + `Index Scan`，只對回傳的 20 列求值（74 buffers），符合預期。
- **`v_has_blocks` 判斷本身多一次查詢**：是，每次呼叫一次，plan 是 `InitPlan → Function Scan on private.blocked_pairs`、**2 buffers**；`blocked_users` 有 `blocked_users_blocker_idx (blocker_id)`（`20260823020000_fk_reverse_indexes.sql:56`），不是全表掃。代價已含在 `v_has_blocks=false` 的 41–138 buffers 內，全部低於既有 200 門檻——這筆額外成本實測不會咬到常見路徑。

---

### 二、其餘交辦逐項

**3. merge commit `e3c29c8` 三處合併**

- **沒有掉東西的機械證明**：`git diff 782bf05..e3c29c8 --stat` 只有 LS-149 自己的 8 個檔案——development（含 LS-143 全部內容）一行都沒有被改動或刪掉。
- `supabase/tests/60_default_privileges.sql`：`:77-84` 兩支 private 輔助函式、`:449-457` LS-149 四支 definer RPC ＋ LS-143 `delete_my_account()`、`:462-464` `get_family_quota(uuid)` 進 invoker 陣列——雙方都在，`sort | uniq -d` 無重複條目。
- `docs/API.md`：LS-143 的 §3 `deletion_requested_at`／§4 `delete_my_account`／`LS050` 與 LS-149 的 §3／§4／`LS026` 段落並存；`API-CONTRACT:RPC` 機械清單同時含 `delete_my_account()`（:1627）與 `get_family_quota(uuid)`（:1628），無重複行。
- `supabase/tests/run.sh`：LS-149 的 `while IFS= read -r f`／`done < <(… | sort -V)`（:137/:152）與 LS-143 的兩個 `race_case` 區塊（:400-412）＋ cleanup uuid 清單追加（:430-442）都在，無重複。
- **時間戳**：`20260903084231`（LS-143）< `091313` < `091317`，且是全樹最新三支；**兩支 migration 定義的物件零交集**（LS-143 只碰 `profiles` grant ＋ `delete_my_account()`；LS-149 的 12 個 create/alter 都不在其中）——所以套用順序在語意上也安全，不存在「後套的 CREATE OR REPLACE 把前一票的改動蓋掉」。

**4. 封鎖過濾一致性（五處）**

| 位置 | 有無過濾 | 依據 |
|---|---|---|
| `albums_select` | ✅ | migration `:132-140` |
| `comments_select` | ✅ | `:141-149` |
| `get_family_timeline` | ✅ 四條分支各一份 | `:225`／`:273`／`:324`／`:374` |
| `list_comments`（definer，繞過 policy） | ✅ 兩條游標分支各一份 | `:467-471`／`:489-493` |
| reactions（`get_reaction_counts`／`reactions`） | ❌ 無 | 見 informational-1 |

四條時間軸分支我**實跑驗證過都真的會過濾**（b1 封鎖 b2、b2 建的相簿掛 child 標記）：分支 1／2／3／4 對封鎖者皆 0 列，對未封鎖的 b2 皆 1 列。沒有「漏一處讓被封鎖者仍看得到」的情形（reactions 除外，見下）。

**`unblock_user` 後的可見性**：`private.blocked_pairs()` 是 STABLE，同一敘述內只求值一次、跨敘述會用新快照重算（READ COMMITTED），所以同交易內 `unblock_user` → 下一句查詢立刻看得到解封結果；PostgREST 每支 RPC 各自一個交易，更不會有快取殘留。`100_:299-312` 的冪等斷言走的正是這條路。沒有問題。

**5. `remove_content_as_owner`／`report_content`／`get_family_quota`**

- Owner-only ✅（`:672-682` 只認 `role='owner'`；`100_:439-450` 非 owner 成員被擋 42501）。跨家庭：owner-of-A 移除 B 家內容走的是**同一段** `v_is_owner=false` 判斷（`v_family_id` 由 target 反查，`:661`），與已測的非 owner 案完全同路徑——沒有獨立斷言，但也沒有未覆蓋的分支（見 informational-3）。
- 軟刪欄位 ✅（`100_:411-431` 驗 album／diary／media 的 `deleted_at`）；`deleted_by` 由既有三支 RPC 的 trigger 推導、本檔沒有直接斷言（informational-3）。**額度不會漂**：重複移除同一張 media 不會重複扣 `storage_used_bytes`——`private.media_storage_sync()` 的 UPDATE 分支用 new/old 的 `deleted_at` 相減（`20260822120100_triggers.sql:237-249`），第二次呼叫 delta=0。
- `report_content` 重複檢舉：**冪等**（回傳既有 pending 報告的 id，不新增列、不報錯），沒有新增 LS0xx 碼；API.md `:1171-1175` 口徑一致。
- `get_family_quota` 直接回 `families.storage_used_bytes`／`storage_quota_bytes`；該欄口徑＝**未軟刪** media 的 `byte_size` 總和（`triggers.sql:207-211`「soft delete 會立刻釋放額度」、API.md:296「soft delete（`deleted_at`）立刻釋放額度」）。也就是 owner 移除內容後額度立刻釋放——與 API.md 與 LS002 硬防線讀的是同兩個欄位，三方一致。

**6. 全套件與 gates**

- `supabase db reset` ＋ 完整 `run.sh`（含 LS-143 的 `91_delete_account.sql` 與兩組併發情境、LS-149 的 `100_`）→ 全綠，檔案順序 `00→…→99→100` 正確。
- `scripts/gates/api-contract-check.sh` ✅（RPC 30 支、表 21 張）／`error-codes-check.sh` ✅（三方皆 24 個自訂碼）。
- `pr-body-check.sh --branch feature/LS-149-report-block-rpc --verify` ✅（exit 0；5 條「已修」SHA 反查全中、LS-96 行 0 條）。**注意**：這支必須在 PR 的 worktree 內跑；我第一次在主 checkout（HEAD=main）跑會誤報 5 條 SHA 不在 PR commits 內，是 `merge-base --is-ancestor <sha> HEAD` 對錯了 HEAD，不是 body 有問題。
- **informational 處置齊全**：informational-1 我查證 LS-22 確實有 comment `0b83fd0d`，內容就是「`kind='report'` 只通知 owner、不得廣播全家庭」；informational-2 已執行（#244 先併、本 PR 後 merge）。
- **Scope 乾淨**：R2 delta 只碰 4 個檔案，逐項對得上 R1 的四條 finding（migration 那 10 行是**純註解**，函式本體零改動——我逐行比對過）；merge commit 沒有夾帶任何 development 之外的東西。沒有順手重構、沒有無關改動。

---

### 三、Findings（2 minor、3 informational，皆不擋併）

**minor-1｜`supabase/tests/50_rls_plan_no_percall_subquery.sql:688-689` — 新區塊插入的 `blocked_users` 列沒清掉，滲進同交易後面的量測**

新 DO 區塊插入一列封鎖關係後就結束，整份 `50_` 跑在同一個交易裡，所以**後面所有區塊**（`list_comments` 效能 gate `:845+`、`get_reaction_counts`）現在都在「呼叫者有 1 筆封鎖」的狀態下量測。今天無害（`list_comments` 實測 10–11／25–26 buffers，門檻 60；被封鎖的 `c1000000…001` 不是任何一列的作者），但它**悄悄換掉了既有 gate 量的變體**：本 PR 之前 `list_comments` 那道 gate 量的是 `blocked_pairs()` 回 0 列的形狀，現在是回 1 列的形狀。

失敗情境：日後有人為了加強測試，把這裡的 `blocked_id` 換成真正的作者（很自然的下一步），`list_comments`／reactions 兩段的資料形狀會跟著變、buffers 位移，而紅的檔案與真正的修改點對不起來，排查會被誤導到錯誤的地方。

建議修法：DO 區塊結尾補一句 `delete from public.blocked_users where blocker_id = 'c0000000-…-001' and blocked_id = 'c1000000-…-001';`（一行），讓每個量測區塊彼此獨立。不修就請按 §5-b 記入 LS-96。

**minor-2｜`supabase/tests/100_report_block_rpc.sql:592-596` vs migration `:225`／`:273`／`:324`／`:374` — 四條 `v_has_blocks=true` 分支只有分支 1 被功能測試釘住**

時間軸的封鎖過濾斷言只呼叫 `get_family_timeline(family)`（不篩 child、無游標）＝分支 1。分支 2／3／4 各自帶著一份**複製貼上**的 `NOT EXISTS`（migration 四處各一份），沒有任何斷言碰得到。新增的效能 gate 雖然四條都量，但**測不到「過濾有沒有生效」**——它的 blocked_id 刻意不是作者，而且它斷言的是「回傳列數 > 0」，把 `NOT EXISTS` 整段刪掉那個斷言照樣綠。

失敗情境：日後有人動篩 child 那兩條分支（例如再一次 LS-121 式的 child 標記改動），把 `NOT EXISTS` 刪掉或把 `fc.family_id` 誤寫成 `f.family_id`／漏掉一份，結果是「被封鎖成員的相簿在**寶貝篩選頁**與**第 2 頁以後**重新出現」，而 CI 全綠。這是 UGC 合規面向的功能，靜默失效的代價不低。

**今天的程式碼是對的**——我實跑驗證四條分支都真的過濾（封鎖者 0 列／未封鎖者 1 列），所以這純粹是測試鑑別力的缺口，不是 bug。建議修法：沿用 `100_:575-620` 已經建好的 fixture，再加三個 `count(*)` 斷言（帶 `p_child_id`、帶游標、兩者都帶），約 20 行；不修就記 LS-96。

**informational-1｜reactions 不在封鎖過濾範圍內**

`get_reaction_counts`／`reactions` 沒有疊加 `blocked_pairs()`，所以被封鎖者對「封鎖者看得到的內容」按的愛心仍然計入數字。票文列的是三處（時間軸／留言／相簿），這是 scope 內的取捨、不是遺漏；但 API.md §3 只把 `media_select`／`diaries_select` 寫成刻意排除，**沒有提 reactions**。建議在該段補一行（或留給 LS-23 的 UI 票處理），否則「封鎖後對方內容一律過濾」這句話在 reactions 上是不成立的。

**informational-2｜PR body 對 merge 的敘述已過時**

PR body 第 113／124 行仍寫「本 PR 目前暫不 merge development，等待通知」，但 `e3c29c8` 已經把 development（782bf05）併進來、也解了兩處文字衝突。衝突解法只記在 Linear comment `18e5d5bd`，PR body 讀起來仍是舊狀態——QA 與日後回溯都會先看 PR body。建議併入前補一行（純文字，不影響 gate；`pr-body-check` 目前是綠的）。

**informational-3｜三處小出入**

- `50_…:684` 註解說「借用效能資料集本來就有的 2 千個合成 profile」——該 fixture 建的是 **5** 個合成 profile（`:38-49` `generate_series(1, 5)`），2000 是 `join_requests` 的列數。這份檔案對 fixture 的描述一向精確，這句話會誤導下一個人。
- `100_` 沒有針對 `remove_content_as_owner` 的「跨家庭 owner」與 `deleted_by` 的直接斷言（兩者分別與既有的非 owner 案共用同一段判斷、由既有三支 RPC 的 trigger 與 LS-57 測試涵蓋）——邊際價值低，記錄在案即可。
- `docs/API.md:44`／`:598` 仍寫「全部 12 支 RPC」（實際 30 支）——**既有問題**（`653701e` 就在），非本 PR 造成，依手術式修改原則不要求本票處理。

---

### 四、沒看的範圍（誠實聲明）

- **iOS／Swift**：本 PR 淨變更沒有 Swift 檔；merge 帶進來的 `AppError.swift`／`AppErrorTests.swift` 是 LS-143 的內容，已在 #244 審過，我沒有重審。
- **LS-143 自己的交付物**（`20260903084231_delete_account.sql`、`91_delete_account.sql`、四組併發腳本）：我在完整套件裡跑過它們（全綠），但**沒有重新 review 其內容**，只查了「與本票是否有物件／語意交錯」。
- **正式站 `db push` 沒有實跑**，順序安全性是以本機逐檔套用推論。
- **50k albums 的 `albums_select` plan** 沒有重建資料集重量，沿用 R1 的實測結論（本輪只在小資料集上確認 plan 形狀仍是 hashed SubPlan）。
- **QA 三角色 SQL 實測**（票文驗收第三條）不在本輪範圍。

### 判定

**APPROVE** — R1 的 major-1／minor-1 我都用自己的 mutation 證實真的解掉了（前者：drop trigger 從「全綠」變成「紅在新斷言」；後者：新 gate 抓得到我人工製造的 22338 buffers 退化，而舊 gate 抓不到），minor-2／3 照 R1 建議落地，兩條 informational 的處置都查得到痕跡。merge 三處合併正確、沒有掉任何 LS-143 的東西、兩支 migration 物件零交集。上面 2 條 minor 都是測試鑑別力／測試隔離的缺口（程式碼本身正確，我實跑驗證過），不擋這次併入——請依 handoff 規約逐條處置（已修／記 LS-96／不修＋理由），一條不能省。


### 2026-09-03T11:03:46.843Z · Chih-Lin Yeh · `b1b11cb4`

orchestrator 2026-09-03 19:03：merge-review R2 `9542908d` **APPROVE**（R1 兩項 mutation 證實已解；`blocked_pairs()` 一次物化、1880 buffers 來自 600 候選列逐一求值 actor、門檻 1.33× 餘裕；merge 三處正確）→ PR #248 併入 development **`462da9f`**（status 已貼）。2 minor（`50_…:688` blocked_users 列未清、`100_…:592` 只釘分支 1）入池 LS-96 `08607705`。下一步：等 LS-143 test→main 完成後 `promote.sh development test` → QA（三角色＋封鎖過濾實測）。LS-153 已通知 merge dev。

### 2026-09-03T11:36:28.894Z · Chih-Lin Yeh · `02d1e06e`

orchestrator 2026-09-03 19:35：development `bab1073` CI 四綠 → `promote.sh development test` 成功（test＝`bab1073`，18 commit，同批 LS-150／LS-132）→ QA、派 qa（三角色檢舉／封鎖／移除實測、跨家庭封鎖全域性、unblock 可見性、anon 權限、封鎖路徑 EXPLAIN、API.md 對照）。qa status 待 LS-150 smoke 同 PASS 後由 orchestrator 彙整貼。

### 2026-09-03T11:47:12.902Z · Chih-Lin Yeh · `72417b59`

## QA R1 — test `bab1073` (LS-23 檢舉／封鎖／移除內容 RPC，backend、無 UI)

worktree `.claude/worktrees/qa-test` detach 到 `bab1073dedc78855de045edac9ba33bba9dcc226`（含 LS-149 merge `462da9f`／PR #248 R2 head `7d62b74`，與同 SHA 上的 LS-150、LS-132）。全程經 `scripts/ops/supabase-lock.sh`，跑完已 `supabase db reset` 還原共用容器、lock 已釋放。

**判定：PASS**（7 條逐條列於下，皆有 SQL／輸出佐證）

---

**1. 全套件 `supabase/tests/run.sh` 綠 — PASS**
`supabase db reset`（21 支 migration 依序套用，含 `20260903091313_notification_kind_report.sql`／`20260903091317_report_block_rpc.sql`）＋完整 `run.sh`：
```
✓ 全部 DB 測試通過
```
`100_report_block_rpc.sql` ✓；`50_rls_plan_no_percall_subquery.sql`（含 `v_has_blocks=true` 效能 gate）✓；grep 全檔無 ✗／FAIL／ERROR。

**2. 三角色手動實測（自建家庭 A：owner o／member m1／m2；家庭 B：owner ob／m1／m2 皆為成員）— PASS**
全部跑在一筆交易內（postgres 建 fixture → 各角色 `set_config` 切換 → 結尾 `rollback`），驗後確認 `select count(*) from families where id in (...)` = 0，無殘料。逐項：
- `report_content`：m2 對 m1 的 comment／album／diary／media 各檢舉一筆成功；同人同內容重複檢舉**冪等**（回傳既有 id，不新增列，與 API.md §4 記載一致）。Owner o 收到 4 筆 `notification_events kind='report'`，actor=m2、event_count 未因重複檢舉累加（`notification_events` 本身沒有「收件人」欄位，是資料層而非發送——與 migration 檔頭第 8 節設計裁量一致）。
- `block_user`：m2 封鎖 m1（家庭 A）後，`albums_select`／`comments_select`／`list_comments`／`get_family_timeline` 對 m2 皆看不到 m1 在家庭 A 的內容（實測 0 列）。**對照**：`diaries_select` 直接查詢仍看得到 m1 的日記——這是 migration 註解已載明的刻意範圍排除（`get_family_timeline` 這條主要入口仍正確過濾），非缺陷。
- **單向性**（票文未明講，實測澄清）：m2 封鎖 m1 後，**m1 仍看得到 m2 的內容**（m1 沒有封鎖 m2）——`blocked_users` 是 `blocker_id=我` 的單向名單（API.md §3 `blocked_users` 段），不是互相靜音。
- **跨家庭**：m2 同時是家庭 A／B 成員，封鎖是在家庭 A 底下註冊的（`block_user` 帶 `p_family_id`）。實測 m2 在家庭 B 仍看得到 m1 的相簿（`albums_select`＝1 列、`get_family_timeline(家庭B)`＝1 列）——**封鎖是 per-family，不是全域 pair**（`blocked_users` 主鍵含 `family_id`），這與 API.md §4 `block_user` 段「僅限同一家庭內，不是跨家庭的全域拉黑」的明文設計一致，非缺陷。
- `unblock_user`：解除封鎖後，**同交易內**下一句查詢立刻恢復可見性（`albums_select`／`get_family_timeline` 皆驗證）。
- `remove_content_as_owner`：非 owner（m2）移除他人內容 → 42501；**跨家庭**（家庭 A owner o 移除家庭 B 內容）→ 擋下，SQLSTATE=**42501**（API.md 記載為統一 42501、不區分「不存在」與「不是 owner」，**不是**獨立 LS0xx 碼——與票文原先預期的「跨家庭 LS0xx」用詞不同，但與 API.md §4 及既有 `100_` 測試一致，判定為文件正確、票文用詞不精確）；owner 移除 comment → 軟刪＋`deleted_by=owner`＋相關 pending report 轉 `resolved`；owner 移除 media → 軟刪成功（**無 `deleted_by`**，既有 schema 缺口，migration 已載明沿用不補）。
- `get_family_quota`：實測 owner 移除 media（2048 bytes）前後，`storage_used_bytes` 立即由 2048→0——口徑為**未軟刪** media 的 byte_size 總和，soft delete 立刻釋放額度，與 API.md §3 `media`／§4 一致。

**3. reactions 未套封鎖過濾 — informational（非 FAIL）**
實測：m1 對 m2 的留言按愛心 → m2 封鎖 m1 → `get_reaction_counts` 對該留言仍回傳 `reaction_count=1`（未過濾）。與 migration 設計裁量第 5 點「範圍刻意排除」一致（票文列的是時間軸／留言／相簿三處，不含 reactions）；merge-review R1 informational-1 已指出 API.md §3 沒提這條例外，記錄在案，不算本票 FAIL。

**4. 權限 — PASS**
- `anon` 呼叫五支 RPC（`report_content`／`block_user`／`unblock_user`／`remove_content_as_owner`／`get_family_quota`）→ 皆 **42501**（grant 層擋，函式本體未執行）。
- 未登入 `authenticated`（`request.jwt.claims` 清空、`auth.uid()` 為 NULL）：前四支 RPC → **42501**（函式體內 `v_uid is null` 判斷，與 API.md 一致）；`get_family_quota` → **0 列、非例外**（`security invoker` 依賴 RLS 靜默擋下，符合 API.md「無自訂碼」記載）。
- `notification_events` 對 `authenticated` 無 grant：`select count(*) from notification_events` 直接 **42501**，未受封鎖過濾影響（結構性成立，封鎖過濾只作用在 `albums_select`／`comments_select`／兩支 RPC，碰不到這張表）。

**5. 效能 EXPLAIN（`get_family_timeline`，`v_has_blocks=true` 路徑）— PASS**
全套件跑同一組 `50_rls_plan_no_percall_subquery.sql` 效能資料集（200k feed_items／600 標記列），QA 本輪獨立實跑量到：
```
分支1 buffers=103（hit=99 read=4，門檻≤2500）
分支2 buffers=95（hit=95 read=0，門檻≤2500）
分支3 buffers=1880（hit=1880 read=0，門檻≤2500）
分支4 buffers=992（hit=992 read=0，門檻≤2500）
```
分支 3＝1880 buffers，與 merge-review R1／R2 各自獨立量到的 92-117／1880-1881／990-992 同一量級（誤差 <1%）；四條分支皆在 2500 門檻內、皆為正向回傳（非空探查）。

**6. `docs/API.md` §4 五支 RPC 簽名／錯誤碼與實作一致，gates 綠 — PASS**
逐支核對 `report_content`／`block_user`／`unblock_user`／`remove_content_as_owner`／`get_family_quota` 的簽名、錯誤碼（`42501`／`LS026`／`23514`）、封鎖生效範圍（§3／§4）與本輪實測行為一致。
```
✓ api-contract gate（text 模式／best-effort）通過：RPC 30 支、表 21 張，docs/API.md 與 schema 一致
✓ error-codes gate 通過：docs/API.md §5、LSErrorCode、migrations 三方皆為 24 個自訂碼且一致
```

**7. Lock 釋放、db reset 還原 — PASS**
最終 `supabase db reset` 成功（21 支 migration 依序套用），`supabase-lock.sh --status` 回報 `free`。

---

**回歸冒煙**：本票為 backend-only（無 UI），登入／時間軸／照片上傳／留言四條主流程覆蓋於全套件既有回歸測試（`10_`／`20_`／`85_`～`99_` 等）皆綠，未額外跑 app 層冒煙（同 LS-150 QA 共用同一 worktree，模擬器操作由該票 QA 負責，本票未使用模擬器）。

**RLS 冒煙**：`10_cross_family_isolation.sql`（含本票新增的 `content_reports`／`blocked_users` 兩張表）於全套件內綠。

**產出位置**：手動驗收 SQL 腳本 `/private/tmp/.../scratchpad/LS-149-qa-manual.sql`（scratchpad，非 repo 產物）；完整套件輸出 `/private/tmp/.../scratchpad/LS-149-run-full.log`。模擬器已關：無（本票未使用模擬器）。

**注意**：test 同 SHA `bab1073` 也含 LS-150（UI）；依指示本次 PASS 暫不貼 `qa` commit status，待兩票皆 PASS 後由 orchestrator 彙整貼一次。

### 2026-09-03T11:48:21.607Z · Chih-Lin Yeh · `080d5c21`

orchestrator 2026-09-03 19:48：QA R1 `72417b59` **PASS**（七項全過；澄清：封鎖單向＋per-family、跨家庭不擴散；`remove_content_as_owner` 跨家庭回 `42501` 與 API.md 一致；`get_family_quota` 排除軟刪；reactions 未套封鎖＝informational；EXPLAIN 103/95/1880/992 與 review 一致）。qa status 已貼 test `bab1073`（與 LS-150 合併）→ `promote.sh test main` 等 test push CI（run 33750486901）完成後晉升 → 正式站 `db push`（`091313`＋`091317`）→ 收尾（sweeper 已派）。**產品備註供使用者知悉**：封鎖是「我看不到他」單向且限同家庭，不是雙向全域——若期望雙向或全域，另票。

### 2026-09-03T11:51:49.774Z · Chih-Lin Yeh · `07a93f34`

## dead-code sweep — LS-149（test `bab1073`）

（dead-code-sweeper 產出、orchestrator 代貼，19:51）**結論：1 個低價值候選，其餘無死碼。**

範圍：PR #248 merge commit `462da9f` 共 8 檔（API.md、migration `091313`／`091317`、tests `100_`（新）、`10_`／`50_`／`60_`／`run.sh`）。
- **候選**：`supabase/tests/100_report_block_rpc.sql:219` 宣告 `v_sqlstate text` 未賦值未讀取（例外處理直接 `when sqlstate 'LS026'`），全庫僅此一處；PL/pgSQL 不報 warning。處置：入池 LS-96，下次動同檔順刪一行。
1. migration helper／index／grant：`private.blocked_pairs()`、`feed_item_actor_id()`、`content_reports_reporter_target_pending_key`、五支 RPC 皆在 policy／呼叫端／白名單／測試／API.md 至少各出現一次；`v_has_blocks=false` 分支是刻意效能分流（migration `:151-172`），非殘留。無死碼。
2. 四份複製 `NOT EXISTS`（`:225`/`:273`/`:324`/`:374`）：重複碼非死碼，四分支皆被 `50_` 效能 DO 觸及；測試只釘分支 1 已入池 `08607705`，不重複記。
3. 測試檔無註解掉程式碼／`TODO`；`50_` 新 DO 區塊 13 個變數皆使用。
4. API.md §4 四段／§5 `LS026`／§9 五行與 migration 一一對應；gate 三方對帳 QA 已綠。
5. `notification_kind='report'` 寫入端有測試 1b＋R2 mutation 證實；讀取端 LS-22 Edge Function 未落地＝票文明列範圍外（LS-22 `0b83fd0d`）。informational。

既有死碼觀察：無（API.md「12 支 RPC」文字落差已在 R2 i3）。盲區：動態 SQL、PostgREST 字串呼叫、iOS 端未落地呼叫（本票不碰 iOS）。

### 2026-09-03T11:52:06.873Z · Chih-Lin Yeh · `b1774495`

## retro — LS-149（orchestrator lesson learning，19:51）

**落地**：main **`bab1073`**（19:50）→ 正式站 `db push` `091313_notification_kind_report`＋`091317_report_block_rpc`（`list_migrations` 21 支）→ tag **v0.8.5**（同批 LS-150／LS-132）→ worktree 已清。

**做對的**：(1) R1 REQUEST_CHANGES 兩項（通知 trigger 零測試、封鎖路徑無效能 gate）在 R2 都用 mutation 證實有牙齒（drop trigger → 紅；actor 每列多掃一次 → 22338 buffers 紅）；(2) `blocked_pairs()` 一次物化＋`v_has_blocks` 靜態分流，`false` 分支逐字保留原查詢＝零回歸；(3) 併入順序（LS-143 先、本票 merge dev 解兩處純累加衝突）按 migration 時間戳走，reviewer 核 `git diff 782bf05..e3c29c8` 只含本票 8 檔；(4) QA 自建三角色＋跨家庭案例，抓到「封鎖單向、per-family」這個票文沒寫清楚的語意。

**harness 改善／工具缺口（皆已入池 LS-96）**：`f22f0645` P1 主 checkout 誤寫（本票早期一次，migration 檔留在主 checkout untracked）；`08607705` P3 四分支只釘分支 1＋`blocked_users` 列未清；`c0a8ae7d` P3 sweeper 缺 `save_comment`；本輪 `v_sqlstate`。

**orchestrator 自身**：派工文把 `remove_content_as_owner` 跨家庭寫成「LS0xx」、封鎖寫成「全域 pair」——兩處都與 API.md 不符，QA 以文件為準判 PASS；派工前應開 API.md 核對，不憑印象（與 LS-143 同一教訓，第二次）。

**待使用者**：封鎖語意（單向、限同家庭）是否符合預期；reactions 未套封鎖過濾（scope 內取捨）要不要補。

### 2026-09-04T04:59:14.048Z · Chih-Lin Yeh · `593c957d`

orchestrator 2026-09-04 12:57：使用者問「為什麼需要封鎖？不是取消追蹤或家庭管理員踢掉就好？」——回覆要點記錄於此：(1) App Store 審核指南 1.2（UGC）要求 app 具備檢舉、**封鎖使用者**與及時處理機制，私密家庭 app 有照片／留言 UGC 仍適用，封鎖是送審硬需求；(2) 家庭內沒有「取消追蹤」，一般成員無法踢人（只有 Owner 能），封鎖讓成員不必求助 Owner、也不必退出家庭就能不再看到某人的內容；(3) 語意維持 LS-149 現況：單向、限同家庭、對方不受影響。**reactions 是否套封鎖：維持現狀 (i) 不過濾**——封鎖的目的是「我看不到他」，愛心是匿名計數，不另開票。LS-152 設計把封鎖入口做成內容「⋯」操作表與設定頁的封鎖名單，不做顯眼功能。

