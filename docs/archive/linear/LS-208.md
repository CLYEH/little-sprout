# LS-208 設計 chore：小債清倉 2——末張卡完全隱藏裝飾件 17 筆裁決、`HLXo3` `fZ3KF` 實例年齡片語 NBSP＋`cmp/Card Diary` Multi Caption 補「·」、Auth 連結 in-flight 暗化與空欄位提示 icon 語彙、深色板壓印小字 WCAG 對比量測、Notes 署名計數訂正；收據 `scan_scope: document` 六支 0 為驗收

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:M, lane:design, hold:user |
| Cycle | 2 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M6 合規與帳號 |
| 估點 | — |
| 建立 | 2026-09-05T15:34:42.962Z |
| 開始 | 2026-09-06T08:15:37.761Z |
| 完成 | 2026-09-12T15:17:12.357Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-208/設計-chore小債清倉-2-末張卡完全隱藏裝飾件-17-筆裁決hlxo3-fz3kf-實例年齡片語-nbspcmpcard-diary |
| 關係 | related→LS-147, related→LS-188, related→LS-120, related→LS-24, related→LS-180, related→LS-91, related→LS-18, related→LS-211, related→LS-202, related→LS-152, related→LS-163, related→LS-119, related→LS-171, related→LS-21, related→LS-96, related→LS-177, related→LS-201, related→LS-164, related→LS-22, related→LS-207, related←LS-247, related←LS-237, related←LS-236, related←LS-234, related←LS-227, related←LS-228, related←LS-226, related←LS-218, related←LS-217, related←LS-216, related←LS-193, related←LS-190, related←LS-192 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項（2026-09-05）依 design lane 補位合併：

* `1d0b97c2` MN-C：全稿末張卡裝飾件（角托／Fan Ghost）完全隱藏殘留 **17 筆**（VR 無豁免量法 PARTIAL=0／HIDDEN=17，0 筆文字），位於 Tab Bar 下緣到板底透明區；修需板高 +87pt，[LS-201](https://linear.app/little-sprout-app/issue/LS-201/設計-chore全稿舊債清倉-root-板重疊-17-對重新落位80pt-板距board-clip-document-332-筆26-板與) 明文排除 → 本票裁決：接受為既定視覺（寫進 Notes 與 `references/`）或統一加板高，擇一並附 VR 量測。
* `1d0b97c2` MN-D：`cmp/Card Diary` `x7k2o6` 元件預設已 NBSP，實例覆寫 `fZ3KF`（板 `HLXo3`）仍 U+0020；Multi Caption 缺「·」分隔號。
* `dddda0a3` i-C／i-D（[LS-164](https://linear.app/little-sprout-app/issue/LS-164/taskls-17-ios-帳號密碼登入審核帳號用方案) review）：文字連結 in-flight `.opacity(0.5)` 硬寫、與姊妹鈕暗化語彙不同型；空欄位提示沿用 `$danger` 三角 icon 對「還沒填」略重——稿面無此二態，需設計裁決並補板（`LS-163 / P1` 系列各一態）。
* `0617b9ae` (3)：深色板壓印小字（`$text-secondary` on dark paper）對比偏低，量 WCAG 對比後決定是否調 token。
* `b63bc5fd` N1：`lyu44`／`epDnW` 署名計數（28→24、34→26）訂正。
* **2026-09-06 追加（**[LS-192](https://linear.app/little-sprout-app/issue/LS-192/taskls-152-ios-編輯顯示名稱與頭像02家庭成員管理0303-ax303-ipad03b-移除成員確認03c-轉移-owner) **merge-review R2** `af82ed61` **M-A，orchestrator 裁決** [LS-192](https://linear.app/little-sprout-app/issue/LS-192/taskls-152-ios-編輯顯示名稱與頭像02家庭成員管理0303-ax303-ipad03b-移除成員確認03c-轉移-owner) **comment** `11c8286e`**）**：`03e 需要先轉移家庭管理者身分` 缺「單人家庭」變體——家庭管理者且家庭只有自己一人時無可轉移對象，實作採 03e 版面＋文案「目前家庭只有你一位成員，無法退出家庭。若要離開，請至「帳號」→「刪除帳號」。」、隱藏「前往轉移」只留「返回設定」；稿面需補這一板（含 AX3）。
* **2026-09-06 追加（**[LS-190](https://linear.app/little-sprout-app/issue/LS-190/taskls-23-ios-eula-同意頁首次登入版本更新時零容忍條款摘要08刪除單筆內容確認日記-10留言-10b沿-ls-142) **merge-review R1** `a4ffee31` **m4＋informational）**：`08 EULA 同意頁` 只有「使用條款更新／我們更新了《使用條款》」一種文案，首次登入（從未同意）也顯示「更新」不合語境 → 需「首次登入」文案變體；另 08 板沒畫「不同意」出口（票文範圍 1 要求「不同意 → 留在頁面／登出」，實作用 `cmp/Button Text`「不同意，登出」）→ 稿面補上並定案文案。
* **2026-09-06 追加（**[LS-193](https://linear.app/little-sprout-app/issue/LS-193/taskls-24-ios-刪除帳號流程04a-一般成員04b-唯一-owner-需轉移04d-唯一成員警告04e-最終確認04f) **merge-review R1** `b6b282e5` **M2／M3，orchestrator 裁決** [LS-193](https://linear.app/little-sprout-app/issue/LS-193/taskls-24-ios-刪除帳號流程04a-一般成員04b-唯一-owner-需轉移04d-唯一成員警告04e-最終確認04f) **comment）**：`LS-18` 三岔路 `ForkView` 在停權（`LS052`／`LS054`）與「帳號刪除處理中」（`LS051`）狀態下只有兩顆被擋住的鈕，無登出、無刪除帳號入口——實作補底部文字鈕列（停權：「登出」＋「刪除帳號」；刪除中：「重試刪除」＋「登出」，沿 `cmp/Button Text`）；稿面需補這兩個狀態板（含 AX3）與錯誤文案。

## 範圍

1. 17 筆隱藏裝飾件：逐板列出（沿 VR `LS-201-vr3-probe.py` 量法），二擇一裁決落地；收據 document scope 六支 0、`corner_anchor` 不退步。
2. `HLXo3` 實例與 Multi Caption：改 U+00A0、補「·」（依 [LS-201](https://linear.app/little-sprout-app/issue/LS-201/設計-chore全稿舊債清倉-root-板重疊-17-對重新落位80pt-板距board-clip-document-332-筆26-板與) 案 2 規則：NBSP 綁「·」＋年齡片語）；NBSP gate 觸碰板嚴格必綠。
3. Auth 兩態：`P1` 密碼登入板加「連結進行中（暗化）」與「空欄位提示」兩態，語彙與既有元件一致（不新造 token）；Notes 寫明實作對應（`WelcomeView+PasswordSignIn`／`LabeledTextField`）。
4. 深色壓印對比：量 `$text-secondary` on `$print-paper` 深色的對比值，<4.5:1 則提案 token 調整（附前後截圖），≥4.5 記入 Notes 結案。
5. Notes 計數訂正；收據 `design/evidence/LS-<本票>-r<n>-overflow.json`（`scan_scope: document`）；VR ≥3 輪（前兩輪退修）；使用者核可：僅範圍 3（新增兩態）、範圍 4（若調 token）、範圍 6／7／8（新板／新文案）需核可頁，其餘不需。
6. `LS-152 / 03e` **單人家庭變體板**（＋AX3 一板）：沿 03e 版面與文案規則（見來源），Notes 寫明三路判定（非管理者→03d；管理者有他人→03e；管理者獨自→03e 單人變體）與實作對應 `FamilyStore+Members` 三態函式；板名先 `pen-read.sh` 查現有板名避免碰撞。
7. `LS-152 / 08 EULA 同意頁` **兩項補稿**：(a) 首次登入文案變體（標題／導言不用「更新」語境，零容忍摘要不變）；(b) 「不同意」出口定案——現行實作為文字鈕「不同意，登出」（`cmp/Button Text`，minHeight 48），稿面補上或改成別的形態並在 Notes 定案；同步 AX3 板。
8. `LS-18` **三岔路** `ForkView` **兩個狀態板**：(a) 停權（`LS052`／`LS054`）：既有兩鈕停用態＋錯誤文案＋底部文字鈕列「登出」「刪除帳號」；(b) 帳號刪除處理中（`LS051`）：說明文案＋「重試刪除」「登出」；各附 AX3；Notes 寫明實作對應（`ForkView` footer、`DeleteAccountFlowView` 無家庭模式）。

## 不做

新畫面（範圍 6／7／8 為既有板變體，不算新流程）；[LS-22](https://linear.app/little-sprout-app/issue/LS-22/story留言愛心與彙總推播) 畫面群（[LS-177](https://linear.app/little-sprout-app/issue/LS-177/設計留言愛心互動列與留言-sheetls-22-畫面群推播通知權限提示與設定頁推播通知列-ui-designervisual) 待核可）；`Legal` 槽；角托 ref 判準（[LS-207](https://linear.app/little-sprout-app/issue/LS-207/harnessci-韌性與設計-gate-盲點修補-pr-body-check-linear-反查加重試且-rules) harness）。

## 派工前置

Pen 已於 09-05 22:54 清場、MCP 未重連——**使用者需先** `/mcp` **重連 pencil**（並跑 `LS-207-pen-reset.sh` 讓 Pen 回主 checkout 檔、VR 先對現稿跑 [LS-207](https://linear.app/little-sprout-app/issue/LS-207/harnessci-韌性與設計-gate-盲點修補-pr-body-check-linear-反查加重試且-rules) 六支確認 `ref_hits>0`）；派工時 `pen-open.sh <本票 worktree>` 開票檔，設計期間不切回。

## 驗收

- [ ] 六支收據 document scope flagged 0；變動節點屬性 diff 只含本票類別；NBSP gate 綠
- [ ] VR ≥3 輪 APPROVE；merge-reviewer 通過；核可頁（範圍 3／4／6／7／8）待使用者

## 留言（16）

### 2026-09-06T08:15:41.111Z · Chih-Lin Yeh · `bd7e5c0a`

**orchestrator 09-06 16:1x 派工**：派工前置已達成——使用者 `/mcp` 重連、Pen 文件＝主 checkout、記憶體內容經 `execute` 驗證＝HEAD（LS-201 comment `39311dba`）、舊 backup 已搬離。worktree `LS-208`（`feature/LS-208-design-debt-cleanup-2`，自 development `ba81f8f`）已建，派 ui-designer：第 0 步先對現稿跑六支確認 `corner_anchor.ref_hits>0`，再依範圍 1→8 逐項落地 commit；R1 handoff 後派 VR（≥3 輪）；核可頁限範圍 3／4（若調 token）／6／7／8。

### 2026-09-06T10:21:20.262Z · Chih-Lin Yeh · `a4597e9e`

## visual-reviewer 第 1 輪：ITERATE

（2026-09-06 18:17 CST）

**little-sprout-brand skill**：已載入（`.claude/skills/little-sprout-brand/`，含 `references/slop-forbidden.md`）。

**Pen 路徑**：`bash scripts/ops/pen-status.sh` → `/Users/clyeh/little-sprout/.claude/worktrees/LS-208/design/littlesprout.pen`（pid 72575，MCP 探針 ✓）。收到 18:08 巡檢通知後於 18:09 重新核對，路徑正確；並用 `Get` 重驗身分：**ROOTS=230、LS-208 十一張新板命中 11/11**（development 為 220 且無 `FEuAL`）。18:01–18:04 的六支掃描／tree_hash／截圖同樣落在本票檔（掃描輸出點名了 `LS-163 / 01e-b …連結進行中` 這張只存在於本票的板），**無任何一筆判定需作廢**。新鮮度：Pencil 端 `SUMMARY-HASH total_nodes=9879 tree_hash=245c0476a10c760e` ＝ 磁碟 `python3 scripts/gates/design_tree_hash.py` 的 `245c0476a10c760e` ＝ 收據 `tree_hash`，三方逐字相同。未跑 `--force-reload`／`--kill`，全程無「⚠ Pencil MCP 需重連」。

---

## 一、重掃比對（正典腳本，我自己重跑）

`scripts/design/overflow-scan.js` 全文（第 147–807 行程式碼本體）＋第一行 `SCAN_BOARDS`（同收據 20 板）送進 `mcp__pencil__execute`。一次跑完六支＋雜湊如 LS-171 所述 `InternalError: interrupted`，拆成**同稿態、中間無任何寫入**的連續兩次唯讀 execute（`SCAN_SKIP_HASH` → 六支；`SCAN_HASH_ONLY` → 雜湊；雜湊那次只縮短了 `cornerWarnings`／`compactLines` 內的中文訊息字串，hashOnly 分支不會執行到它們，雜湊路徑本身逐字原樣）。

我的 SUMMARY（六支那次）：

```
SUMMARY total_nodes=9879 scanned_nodes=15951 scan_scope=document sibling_intersection=2071
row_overflow=560 cross_parent_collision=321(bleed-only) text_occlusion=0/0 board_clip=0/0
corner_anchor=6/48/0 document=225/1784/0 ref_hits=746(defs=10) unresolved=3/14
boards=["yec61","HLXo3","qtCd7","kHDk4","N1aeiU","FEuAL","iBYUK","XhwG6","b3nzDp","dxPvP",
"i6uiB4","z042Yg","Oic2v","JzyIv","RC7bN","jftM0","UOWte","VDO0S","L6iIQ0","Z8Q4R"] tree_hash=skipped
```

| 項目 | 收據 | 我的重跑 | |
|---|---|---|---|
| total / scanned nodes | 9879 / 15951 | 9879 / 15951 | ✓ |
| sibling / row / cross | 2071 / 560 / 321 | 2071 / 560 / 321 | ✓ |
| text_occlusion | 0/0 | 0/0 | ✓ |
| board_clip | 0/0 | 0/0 | ✓ |
| corner_anchor（boards） | 6/48/**0** | 6/48/**0** | ✓ |
| corner_anchor（document） | 225/1784/0 | 225/1784/0 | ✓ |
| ref_hits | 746(defs=10) | 746(defs=10) | ✓ 判準活著 |
| unresolved | 3/14 | 3/14 | ✓ 三筆皆 `mount_pair`（Tokens 板 Icon 180/60/40），分類成立 |
| scan_scope／各支 scope | document | document | ✓ |
| boards 涵蓋本輪動過的板 | 20 | 我用 python 對 `origin/development`(689f5ae) 逐 root 比：**新增 11＋刪除 1（rh2Q1）＋變更 9 ＝ 恰好這 20 板**、其餘 **210 個 root byte-identical** | ✓ |
| tree_hash | 245c0476a10c760e | 245c0476a10c760e（Pencil）＝磁碟 python 同值 | ✓ |

三項硬性指標（`corner_anchor.mismatch` / `text_occlusion.flagged` / `board_clip.flagged`）對觸碰板皆為 0，**核銷通過**。

**獨立抽驗（不只轉貼腳本輸出）**
- 範圍 4 對比值：我用 WCAG 2.1 公式自行計算 → `#D3AEB2` on `#E8D9D4` = **1.462**、`#553040` on `#E8D9D4` = **8.129**。與稿面寫的 1.46／8.13 完全吻合。
- 範圍 1 隱藏節點：我自寫 probe（葉節點沿 `clip:true` 祖先鏈相交，可見矩形為空＝完全隱藏）→ 時間軸 5 板 **20 筆**、相簿家族 4 板 11 格 **49 筆**，節點 id 與 Notes 清單逐一吻合。
- 範圍 2/5 codepoint：逐字元核對（見下）。

**Gate 實跑**
- `bash scripts/gates/design-evidence-check.sh design/littlesprout.pen --ticket LS-208 --base origin/development` → exit 0，`✓ design-evidence gate 通過：…（head_sha=b193bf1（本輪最新），total_nodes=9879，六支掃描皆有輸出…、corner_anchor.mismatch=0、boards 覆蓋本 PR 觸碰的板、tree_hash 對應 head_sha 快照、text_occlusion.flagged 為空、board_clip.flagged 為空、scan_scope=document、六支各帶 scope／document_count）`
- `bash scripts/gates/design-notes-check.sh design/littlesprout.pen --base origin/development` → exit 0，`✓ design-notes gate 通過：Notes 板 8 塊、head id 9879、本 PR 範圍曾存在而 head 已無的 id 33、沿革引用 9、缺失 0、署名 NBSP 違規 0（舊債 0）`。沿革 9 筆逐筆核對：`TCrvY`／`cJLcJ`×4／`gMWNP`×2 對 `rh2Q1`／`OwhFv`／`W6lr9R`／`LLD8z`／`nrqqV` 皆在同段給出「原」標記與現行 id，合格。

---

## 二、BLOCKER

### BL-1　root 板重疊 5 對＋板距 <80pt 5 板：直接回退 LS-201，而收據把它分類成無害

`Get(depth 0)` 解析後 bounds，全樹 root 兩兩相交：

| A | B | 交集 |
|---|---|---|
| `jftM0` 實作註記 Handoff Notes | `yec61` LS-21/LS-119 實作註記 | **900 × 634** |
| `yec61` | `EzJCV` LS legal / 文件檢視 (iPhone) | **393 × 678** |
| `b3nzDp` LS-152 實作註記 | `bs8aE` A11y/12e 照片排序 AX3 | **393 × 875** |
| `b3nzDp` | `IH80C` Stress/13 日記詳情 20 張 | 123 × 875 |
| `b3nzDp` | `f1wVw` LS-21/12e 照片排序 | 60 × 875 |

再看最近鄰間距（80pt 規則）：`UOWte` 67、`VDO0S` 67、`L6iIQ0` 67、`Z8Q4R` 67、`N1aeiU` **20**。

成因確定：`yec61`／`jftM0`／`b3nzDp`／`N1aeiU` 都是 `height: undefined`（fit_content）的 Notes 板，本輪各自加了一塊「N LS-208 沿革」後長高；`UOWte`/`L6iIQ0` 852→1060、`VDO0S`/`Z8Q4R` 1900→1950 後吃掉了 80pt 留白。`origin/development` 的 LS-201 r4 收據 293 筆代表清單裡**沒有任何一筆**提到這些 root 板 → LS-201 收工時 root 重疊確實是 0。

最嚴重的不是重疊本身，是**收據怎麼寫它**。`LS-208-r1-overflow.json` 有這一筆：

> `{"node_a": "jftM0", "node_b": "yec61", "classification": "root 層畫面／Notes 板在畫布上緊鄰擺放（僅畫布座標距離判定，非同一畫面內視覺重疊；同類 5 例）"}`

這句話與量測相反。`sibling_intersection` 的判準是 `overlapArea > 4`，只有真的相交才會進 flagged；634pt／678pt／875pt 的重疊不是「緊鄰擺放」。這 5 例被歸進「絕大多數屬品牌既定母題…既有重疊，非本票新增缺陷」的整批免責句裡，於是本票自己造成的回歸就此隱形——這正是「為了歸零而掩蓋」。

**改法**：(a) 把 `yec61`／`jftM0`／`b3nzDp`／`N1aeiU` 沿 y 重新落位到與所有鄰居 ≥80pt（`yec61` 至少下移 714、`b3nzDp` 至少 955，或改換欄）；(b) `UOWte`／`VDO0S`／`L6iIQ0`／`Z8Q4R` 各補回 13pt 以上到 80；(c) r2 收據把這一類獨立成一段、逐筆給真實分類，不得再併進「既有母題重疊」；(d) 既然 fit_content 的 Notes 板每次加沿革都會長高，請在 Notes 記一條「Notes 板加段後必須重跑 root 間距檢查」的收工步驟。

### BL-2　範圍 1 的「另案 49 筆」根因判定與實測相反，且已寫進永久 Notes ＋ LS-96 建議

`yec61/xv1gb` 寫：相簿家族 49 筆「皆被同板同格內較後繪製的不透明『Corner Shape』節點完全蓋住（**非 clip 造成、是 z-order 覆蓋**——與範圍 1 的『末張卡』成因**不同類**）」。

我抽驗四筆，逐層印出可見矩形被誰裁掉：

```
L3zYk/DjW91 Corner TL rect=[15527,2166,26,26]
   clip祖先 T7DfpY Body rect=[15508,847,393,692] → 可見 [26,0]  ← 在此被完全裁掉
R7edNW/mlORb Corner BL rect=[4728,1764,26,26]
   clip祖先 FqE7M Body rect=[4709,847,393,790] → 可見 [26,0]  ← 在此被完全裁掉
eDQhR/UTLcl Fan Ghost rect=[4820,2078,167,59]
   clip祖先 FqE7M Body → 可見 [167,0]  ← 在此被完全裁掉
MYxf1/DjW91 Corner TL（時間軸，範圍 1 本體）
   clip祖先 n7gPAe Body → 可見 [26,0]  ← 在此被完全裁掉
```

四筆都是**被 `Body` 的 `clip:true` 裁掉**，與時間軸末張卡**完全同一 class**。而且我的 probe 本身就是「clip 可見矩形為空」的判準——它能數到這 49 筆，就代表這 49 筆是 clip 造成的；Notes 卻同時說「用同一量測方法」又說「非 clip 造成」，句內自相矛盾。

後果不只是文字錯：這個錯誤根因會讓 LS-96 池項被開成「角托方案改版殘留清查」（大範圍、要動 11 個相簿格），而真正的題目是「Body clip 切掉最後一列」——與範圍 1 同一題，同一個解（板高／clip 邊界）。

**改法**：更正 `xv1gb` 的根因敘述為 clip 裁切、與末張卡同 class；並在同段說明「本票只對時間軸 5 板做裁決、相簿 4 板 11 格 49 筆同 class 但屬他票範圍」的**分割理由**（例如「LS-201 已處理時間軸家族、相簿家族未納入」），而不是用假的「不同類」當理由。給 orchestrator 的 LS-96 池項建議也要一併改寫。

---

## 三、MAJOR

### MJ-1　`HLXo3` 的 Feed 改用「clip:true ＋ 字面 992」，偏離同族 LS-201 既定作法，且讓兩支掃描從此看不見它

同一家族兩張板，結構現在不一樣：

| | `lKoZG`（LS-201 已修，A11y/11 時間軸 AX3） | `HLXo3`（本票新改） |
|---|---|---|
| Body | `fill_container`，**clip=true** | `fill_container`，clip=**false** |
| Feed | h=undefined(fit_content)，clip=false | h=**992**（字面），**clip=true** |
| Tab Bar 淨空 | 具名 `Spacer · Body 收窄補償（Tab Bar 前緣淨空）` h=**88** | **沒有** |

`lyu44` 這段 Notes 是本輪你自己編輯過的，裡面寫著 LS-201 的規則原文：「Feed 補 clip:true 修正 board_clip（**height 維持 fill_container**，淨空由既有具名 Spacer「Body 收窄補償（Tab Bar 前緣淨空）」承載，**未新增字面 height**）」。本票對同一家族的下一張板做了相反的事。

992 是倒推餘數：Tab Bar 頂 1378 − Feed 頂 386 = 992。它沒有主人（品牌十條 #5「不在節奏上的數字都要有主人」），而且 header 一動（AX3 字級、更長的標題）Feed 頂就移，992 不會跟著移。更關鍵的是：Feed 現在 clip=true 且高度固定，內容底 1121 已經被切掉 **129pt**（整條 `Load More` y=1015–1121 完全不可見）；往後任何新增內容都會被靜靜吃掉，而 `board_clip`（只看 root 邊界）與 `text_occlusion`（可見矩形已被 clip 扣掉）**兩支都永遠不會再報**。

**改法**：照 `lKoZG` 做——`aedfE` Body 設 `clip:true`、`GaYB6` Feed 還原成 fit_content 並移除 `height:992`、在 Feed 之後補一顆具名 `Spacer · Body 收窄補償（Tab Bar 前緣淨空）` h=88。此作法在全稿已證明能讓 `text_occlusion` 維持 0（現況全稿 0/0，含 `lKoZG`）。
截圖：`/private/tmp/claude-501/-Users-clyeh-little-sprout/e6972486-fec6-43c0-b8f4-28b43b01a5a0/scratchpad/LS-208/LS-208-vr1-HLXo3.png`、對照 `…/LS-208-vr1-lKoZG-LS201對照.png`

### MJ-2　`iBYUK` 的提示列放在 Footer，違反同族兩個既定作法；文案也換了 register

`iBYUK` 結構：`Top` y=16 h=439（Field Wrap 310，與基準板同）→ **120pt 空白** → `Footer` y=575 h=137，`Reply Row`（`J0GAJ`，circle-alert `$text-primary` ＋「請輸入帳號與密碼。」）在登入鈕正上方。

同族的兩個先例都不是這樣：

- `O1XC2Z`（LS-163 / P1c 帳號密碼登入 · 帳號或密碼錯誤，**同一個畫面的隔壁狀態板**）：`Top` h=464、**`Field Wrap` h=335**（基準 310），錯誤訊息在 Field Wrap 裡貼著欄位；Footer 只有 Button Wrap。
- `hElk0`（Stress / 05 建立家庭 · 空欄位（未填），你在 handoff 裡說沿用它的語彙）：提示就在欄位正下方，文案是「還沒填家庭名稱。在上面打上名字，再按一次。」

你複用了 icon＋色階（這部分對，見「值得保留」），但把**位置**與**語氣**都換掉了。「請輸入帳號與密碼。」只講「怎麼辦」、沒講「發生什麼」、也沒說是哪一欄空著；而長輩要把畫面最底下的一句話連回 440pt 之外的 Email 欄。品牌十條 #8：「語氣先講發生什麼、再講怎麼辦」。

**改法**：把 `J0GAJ` Reply Row 移進 `K06vQ` Field Wrap（放在密碼欄之後，比照 `O1XC2Z` 的 335pt 結構），Footer 還原成只有 Button Wrap；文案改成 `hElk0` 的 register，例如「還沒填 Email 和密碼。在上面兩欄打上資料，再按一次登入。」若堅持留在 Footer，請在 Notes 寫出「為什麼這張與 P1c／hElk0 不同」的理由。
截圖：`…/LS-208-vr1-iBYUK.png`、對照 `…/LS-208-vr1-hElk0-空欄位對照.png`

### MJ-3　範圍 3 兩張新板沒有 AX3，而同族的錯誤態板有

本輪 11 張新 root 中，範圍 6/7/8 每張都配了 AX3（`i6uiB4`／`Oic2v`／`RC7bN`／`VDO0S`／`Z8Q4R`），只有範圍 3 的 `FEuAL`／`iBYUK` 沒有。但同族的 `O1XC2Z`（P1c 錯誤態）**就有** AX3 板 `LkrrS`（852 → 1554）——這個家族的既定作法是：**加了訊息列的狀態板要自己的 AX3**。

而 `iBYUK` 正是本輪唯一新增文字列的板，也是 AX3 風險最高的一張：`$fs-note` 17→40，「請輸入帳號與密碼。」在 345pt 內容寬下會折成兩行、Reply Row 從 25pt 長到 ~110pt，Footer 137 → ~225。這一輪你才剛因為 AX3 折行修過 `Oic2v`／`RC7bN` 的 41pt 溢出——同型風險不該只靠推測。品牌十條 #7：每張重要畫面必附 AX3 壓力板。

**改法**：補 `A11y / P1f 帳號密碼登入 · 空欄位提示 · Dynamic Type AX3`；`FEuAL` 若判定與 `aEb17`（01e AX3）差異只在 fill／label、版面不動，可以不補板，但要在 Notes 明寫這個判定與理由。

### MJ-4　`UOWte`／`L6iIQ0` 用 13pt 說明「為什麼這兩顆不能按」

`q6CXE`「帳號被停權期間，無法建立或加入家庭。」與 `qBdcW`「帳號刪除完成前，無法建立或加入家庭。」都是 `$fs-meta`（實測 token 值 = 13）。

這兩句是全畫面唯一明說「這兩個控制項為什麼失效」的文字——正是品牌十條 #6 點名的「錯在哪／要怎麼改」內容，**一律 ≥17**；13pt 只留給「不讀也能完成任務」的內容。目前停用態的其他訊號很弱（`Card Action` `$accent`→`$surface-2`、`Create Label` `$text-primary`→`$text-secondary`，長輩不會有並排對照），這句話實際上在承擔全部解釋責任。

**改法**：兩處改 `$fs-note`（17）；並把它移到失效控制項的**正上方**（`Card Join Family` 之前）而不是夾在 `Create Family Row` 與按鈕列之間——現在它在版面上同時往上解釋兩個不同的控制項。
截圖：`…/LS-208-vr1-UOWte.png`、`…/LS-208-vr1-L6iIQ0.png`

### MJ-5　Notes 記了一個沒有做的改動（`L6iIQ0` 標題）

`jftM0/nQ0TV` 寫：「② `L6iIQ0`…同版面骨架，**Title 改『帳號刪除處理中』**」。稿面實際：`VeJbg [text] Greeting` 內容仍是「歡迎，淑芬」（與 `UOWte`、基準板 `RxXvq` 相同），全板沒有任何節點內容是「帳號刪除處理中」。

Notes 是這張票最主要的產出物，記著沒發生的事比不記更糟（實作者會照著找）。**改法**：二擇一——把標題真的改掉（但要注意 `UOWte` 也是「歡迎，淑芬」，兩板一致性要一起決定），或把 Notes 這句刪掉／改成「Title 維持『歡迎，淑芬』，狀態差異只由 Notice Row 承載」。

### MJ-6　範圍 4 裁決所倚賴的兩個數字（96／139）無法複算

Tokens 板 `KI61A/vCp8T` 與 `N1aeiU/NYcqj` 寫：「全稿逐節點核對（**96 筆** Imprint Caption／Lab Imprint 實例）」、「它是全稿 **139 處** app 介面次要文字…調整會牽動這 139 處既有正確用法」。

我用四種口徑都對不上：

| 口徑 | Imprint Caption／Lab Imprint | `$text-secondary` |
|---|---|---|
| .pen JSON 未展開 instance | **14** | 514（text 節點） |
| Pencil `resolveInstances:true` | **41** | 683（text）／1189（全節點） |

「不調整 token」是本票範圍 4 的**終局裁決**，而它唯一的量化理由就是那個 139。這個數字必須能被下一個人重算。（對照：同一段的 1.46／8.13 我一算就吻合；範圍 1 的 20／49 我逐筆重算也完全吻合——所以問題不是你不會量，是這兩個數字沒交代口徑。）

**改法**：在 Notes 寫出計數謂詞（展開 instance 與否／算哪些 node type／看 `fill` 還是解析後色值），或直接換成可複算的數字。

---

## 四、MINOR

- **MN-1　20 vs 17 沒有對帳。** `yec61/Djv58` 寫「20 筆…與 LS-201 R3 記錄的 17 筆量級相符（差異屬兩次獨立量測範圍與**捨入取捨**、非結構性出入）」。節點計數沒有捨入。我重算時間軸 5 板 = 5×4 = 20，節點 id 與你的清單一致；票文與 LS-96 `1d0b97c2` 記的是 17（PARTIAL=0／HIDDEN=17／0 筆文字）。**改法**：對 `1d0b97c2` 的 17 筆逐筆比對，指出多出來的 3 筆是什麼（LS-201 當時量的板數不同？Fan Ghost 是否計入？），或明說「LS-201 的 17 是在 LS-201 自己改板高之前／之後量的」。一句話交代清楚即可，但不能用「捨入」。
- **MN-2　`epDnW` 訂正後句子自相矛盾。** 現行文字：「…元件定義本身＋全部 **25** 個既有非空 override 實例（連同…3 張 AX3 卡，共 **24** 個實例＋1 個元件預設＝全稿 **26** 個『·』」。25+3≠24；24+1=25 不等於 26。24 與 26 這兩個數字本身**是對的**（我實測：`wQVzs` override 共 24 個，其中 4 個內容是刻意留白的 `" "`；帶內容 20 個＝單寶貝 15 個各 1 個「·」＋多寶貝 5 個各 2 個＝25，加元件預設 1 個＝**26**），只是中間的推導沒跟著改。**改法**：整句重寫成上面那條可複算的推導。
- **MN-3　`dxPvP` 中段 531pt 無名 Spacer。** `maJ2R [frame] Spacer h=531`，上面只有 105pt 的 Header、下面只有 60pt 的 Footer。來源板 `sF5oA`（03e）本來就有 336pt 空白，但它中段至少有 `Families Card`「陳家　前往轉移 ›」當錨點與下一步入口；03e2 把卡拿掉後，剩下的畫面沒有任何視覺主體，也沒有品牌母題（無沖印品、無卡片），遮住 logo 之後跟任何 SaaS 錯誤頁沒有差別（slop #8「不敢做決定」＋ #2「沒有節奏的空白」）。更實際的問題是：唯一出口「請至『帳號』→『刪除帳號』」只是散文，長輩得自己去翻設定。**改法**：把這句話做成與 03e 同一個 Row 元件（`家` icon ＋「前往帳號 · 刪除帳號」＋ chevron），放在 03e 的 `Card Wrap` 原位——同時填掉版面、給出可點的下一步、維持家族一致；Footer 依 LS-192 裁決仍只留「返回設定」。
  截圖：`…/LS-208-vr1-dxPvP.png`、對照 `…/LS-208-vr1-sF5oA-03e對照.png`
- **MN-4　`JzyIv` 標題把「使用條款」切開。** 「開始使用前，請詳閱使用條款」13 字在 361pt／`$fs-display` 34 下折成「開始使用前，請詳閱使／用條款」。**改法**：縮成能單行或在合理處斷的字串（例如「請先詳閱使用條款」／「開始使用前，請先詳閱條款」），並確認 AX3（`RC7bN`，55pt）下的折行點也不切詞。
- **MN-5　`dxPvP` 的 Notes 文案引號把 Title 與 Subtitle 併成一句。** `KoZ7a` 寫「文案『無法退出家庭。目前家庭只有你一位成員，無法退出家庭。若要離開，請至「帳號」→「刪除帳號」。』」——「無法退出家庭」出現兩次，與 LS-192 `11c8286e` 的原文（只有後半段）不符。稿面本身是對的（Title `Tutlg`＝「無法退出家庭」、Subtitle `SgNBK`＝ LS-192 原文，我逐字比對吻合）。**改法**：Notes 把 Title／Subtitle 分開引，並註明「Title『無法退出家庭』為本票新增、不在 LS-192 指定文案內」。
- **MN-6　收據自稱「SUMMARY 原文」但不是逐字。** `scan_note` 與 handoff 引的 SUMMARY 在 `unresolved=3/14` 之後直接接 `tree_hash=…`，少了腳本實際會印的 ` boards=[…]` 整段（見我上面的重跑輸出）。原文欄位的用途就是讓人逐字比對；縮寫請另起一行標明是摘要。

---

## 五、Informational

- **i-1**　`z042Yg` 852→912、`JzyIv` 940：板高不再等於裝置視窗。>852 的 iPhone 板在本稿是既有慣例（12 系列 906／07 系列 934–1658 等），所以不擋；但 `Action Bar` 在本設計系統的語意是**釘底層**（第五支的 `OVERLAY_RE` 就含它，LS-152 R1 BL-2 正是「釘底動作帶壓住 EULA 法務連結」）。請在 Notes 一句話定義這批 EULA 板是「全捲動內容表示法、Action Bar 畫在內容末端」，否則下一個人會照 912 去排一個釘底的 bar。
- **i-2**　`lyu44` 記的 LS-201 作法是「Feed 補 clip:true」，但稿面實際是 `lKoZG` 的 **Body** clip=true、Feed clip=false。非本票造成，但你這輪剛好在改這段字，順手更正成本較低。
- **i-3**　EULA 零容忍卡四條的項目符號在視覺上偏上、與首行文字未對齊（`z042Yg`／`JzyIv` 皆有，屬既有）。
- **i-4**　`HLXo3` 的多寶貝署名在 AX3 下折成「小安 ／ · 2 歲 3 個月、／ 小明 · 8 個月」，「·」領銜下一行——這與 LS-201 R3 定案（NBSP 綁「·」＋年齡片語）**一致**、也正是 LS-201 核可頁「署名換行『·』領銜下一行前後對照」在等使用者確認的那個行為。本票不需處理，但核可頁請沿用同一組對照，別讓使用者以為是新問題。

---

## 六、值得保留（重做時不要洗掉）

1. **範圍 2/5 的 codepoint 完全正確**，元件定義與實例覆寫逐字一致：`小安`+U+0020+`·`+U+00A0+`2`+U+00A0+`歲`+U+00A0+`3`+U+00A0+`個`+U+2060+`月`+`、`+`小明`+U+0020+`·`+U+00A0+`8`+U+00A0+`個`+U+2060+`月`。完全符合 LS-201 案 2 規則，NBSP gate 觸碰板全綠。
2. **範圍 4 的方向是對的**：`$print-ink-secondary` 是不掛 theme 的紙上墨（品牌十條 #2「紙不會變黑」），`$text-secondary` 是會隨模式翻轉的介面 token，兩者不可混用於紙面——不動 token、改寫 guardrail＋前後 swatch，這個裁決站得住（只差 MJ-6 的數字）。而且你查證了全稿**沒有現存誤用**，這是預防性量測而非修 bug，也誠實標明了。
3. **空欄位提示用 `$text-primary` ＋ circle-alert 而不是 `$danger` 三角**（品牌十條 #8「不用 danger 責備使用者」），並且登入鈕維持 enabled（驗證型 disable=0）。這是對現行實作的正確糾正。
4. **停權／刪除中兩板的暗化語彙**與 `AuthButtons.swift` `isDimmed` 一致（`$accent`／`$on-accent`／`$text-primary` → `$surface-2`／`$text-secondary`），不是硬寫 opacity。對比我實測全部 ≥7:1（`$text-secondary` on `$surface-2` = 7.14／深色 7.03、`$danger` on `$surface` = 10.34／深色 8.40），長輩門檻無虞。
5. **「不同意，登出」用 `cmp/Button Text` 中性色、非 `$danger`**，畫面維持一顆實心主鈕（品牌十條 #1）；零容忍摘要卡內容一字未動。
6. **Notes 的實作對應寫得非常具體**——檔名、函式、建議的布林名（`isEmptyFieldsHint`）、node id、API 錯誤碼（LS051／LS052／LS054）都在。這是本票最有價值的產出，BL-2／MJ-5 只是要求把其中兩處寫對，不是要你寫少。
7. **範圍 1 的節點清單可複算**：我用獨立 probe 逐筆重算，20／49 與你列的 id 完全吻合。量法本身沒有問題（問題在 BL-2 的根因敘述與 MN-1 的對帳）。
8. `rh2Q1` → `Oic2v` 整板重建後的沿革補救（`TCrvY`／`cJLcJ`／`gMWNP`）處理得乾淨，`design-notes-check` 缺失 0。

---

## 七、R2 通過條件

必須全部滿足才進 R3（提醒：R2 依規約仍一律退修，R3 起才可能 APPROVE）：

1. **BL-1**：root 重疊 0 對、所有本票觸碰／新增的板與最近鄰 ≥80pt（含 `N1aeiU` 的 20pt 與四張 67pt）；r2 收據把 root 類單獨成段、逐筆真實分類，不得再併入「既有母題重疊」免責句。
2. **BL-2**：`yec61/xv1gb` 根因改為 clip 裁切、與末張卡同 class，並補上「為什麼相簿家族屬他票」的真實分割理由；給 orchestrator 的 LS-96 池項建議同步改寫。
3. **MJ-1**：`HLXo3` 改回家族既定結構（Body clip＋Feed fit_content＋具名 Spacer 88），移除字面 992；或提出足以推翻 LS-201 的理由並同步改掉 `lyu44` 那條規則（兩張板不能各做各的）。
4. **MJ-2**：`iBYUK` 提示列移進 Field Wrap（比照 `O1XC2Z`），文案改成先講發生什麼、再講怎麼辦、並指出是哪些欄位；或在 Notes 寫出偏離同族的理由。
5. **MJ-3**：補 `A11y / P1f … AX3` 板；`FEuAL` 不補 AX3 的話要在 Notes 寫明判定。
6. **MJ-4**：`q6CXE`／`qBdcW` 改 `$fs-note`(17) 並移到失效控制項正上方。
7. **MJ-5／MN-5**：`nQ0TV` 的 `L6iIQ0` 標題敘述、`KoZ7a` 的 dxPvP 文案引號，兩處與稿面對齊。
8. **MJ-6**：96／139 補上計數謂詞或換成可複算數字。
9. **MN-1／MN-2**：20 vs 17 的差額逐筆對帳；`epDnW` 推導重寫成可複算版本。
10. **MN-3／MN-4**：`dxPvP` 中段補可點的「帳號 → 刪除帳號」Row（沿用 03e 的 Row 元件）；`JzyIv` 標題不切詞（含 AX3）。
11. r2 收據：`scan_scope=document`、六支照舊、三項硬性指標對觸碰板 0、`tree_hash` 對 head 快照、`boards` 涵蓋 r2 動過的**全部**板（改 `HLXo3`／新增 P1f AX3／移動 Notes 板都要列入）；「SUMMARY 原文」欄逐字（含 `boards=[…]`）。
12. 附上 `design-notes-check.sh` 與 `design-evidence-check.sh` 的實跑原文。

**這份稿的靈魂是什麼／缺什麼**：LS-208 不是要畫新畫面，它的產品是**一份可信的紀錄**——「這 17（20）筆為什麼可以接受」「這個 token 為什麼不改」「這兩態的實作要怎麼接」。它的靈魂就是可複算性。在這一點上它有兩張臉：可以被機械複算的部分（codepoint、對比值、隱藏節點清單、24／26）我逐項重算全部吻合，做得比多數設計票紮實；但另一半（49 筆的根因、96／139、20 vs 17、以及被寫成「緊鄰擺放」的 5 對 root 重疊）是**用敘述取代量測**，而且三處都剛好把結論推向「沒問題」。清倉票最不能有的就是這個方向性——把那四處的敘述換成量測，這張票就成立了。

**性質備註**：本輪 BL-1／BL-2／MJ-1／MJ-3／MJ-4／MJ-6／MN-1／MN-2 全屬**可機械複驗項**（座標、clip 幾何、token 值、字級、計數），只有 MJ-2 的文案 register 與 MN-3 的版面主張帶審美成分。


### 2026-09-06T12:02:21.458Z · Chih-Lin Yeh · `3f3d0432`

## visual-reviewer 第 2 輪：ITERATE

（2026-09-06 19:58 CST）

**little-sprout-brand skill**：已載入。
**Pen 路徑**：`pen-status.sh` → `/Users/clyeh/little-sprout/.claude/worktrees/LS-208/design/littlesprout.pen`（pid 72575）。新鮮度三方一致：Pencil `SUMMARY-HASH total_nodes=9899 tree_hash=99ac8dee7e02cc7a` ＝ 磁碟 `design_tree_hash.py` ＝ 收據 `tree_hash`。未跑 `--force-reload`／`--kill`。

**先講結論**：R1 的 12 條裡 **11 條確認達成**，其中 BL-1、BL-2、MJ-1、MJ-2 做得乾淨且可複算。**但不能宣告「R2 條件全數達成、R3 為確認輪」**——條件 7 只完成一半，且本輪新增修改引入 **1 個 blocker、2 個 major**。其中 blocker 正是本輪最該警惕的一種：`VUAqK` 這張「用來證明 AX3 沒問題」的新板，本身在 AX3 下是壞的，而六支掃描結構上看不到它。

---

## 一、重掃比對（我自己跑正典腳本）

`scripts/design/overflow-scan.js` 全文＋收據的 21 板 `SCAN_BOARDS`，依 LS-171 拆成同稿態、中間無寫入的連續兩次唯讀 execute。

我的 SUMMARY（六支那次，逐字）：
```
SUMMARY total_nodes=9899 scanned_nodes=16017 scan_scope=document sibling_intersection=2066 row_overflow=562 cross_parent_collision=321(bleed-only) text_occlusion=0/0 board_clip=0/0 corner_anchor=6/48/0 document=225/1784/0 ref_hits=746(defs=10) unresolved=3/14 boards=["yec61","HLXo3","qtCd7","kHDk4","N1aeiU","FEuAL","iBYUK","VUAqK","XhwG6","b3nzDp","dxPvP","hHrIV","z042Yg","Oic2v","JzyIv","RC7bN","jftM0","UOWte","Deljc","L6iIQ0","I07MZX"] tree_hash=skipped
```

| 項目 | 收據 | 我的重跑 | |
|---|---|---|---|
| total / scanned | 9899 / 16017 | 同 | ✓ |
| sibling / row / cross | 2066 / 562 / 321 | 同 | ✓ |
| text_occlusion / board_clip | 0/0 / 0/0 | 同 | ✓ |
| corner_anchor（boards / document） | 6/48/0 · 225/1784/0 | 同 | ✓ |
| ref_hits / unresolved | 746(defs=10) / 3/14 | 同 | ✓（3 筆皆 Tokens 板 `mount_pair`，同 R1） |
| scan_scope／各支 scope | document | 同 | ✓ |
| tree_hash | 99ac8dee7e02cc7a | Pencil 端同值＝磁碟 python 同值 | ✓ |
| SUMMARY 原文 | 本輪**含 `boards=[…]`，逐字** | — | ✓ MN-6 已修 |

**python root diff（對 `origin/development` 689f5ae）**：ADDED 12 / REMOVED 1（`rh2Q1`）/ CHANGED 9 / **UNCHANGED 210**。ADDED∪CHANGED **恰為收據 21 板，無一遺漏、無一多餘**（我雙向檢查：不在 `boards` 內卻被動到的 root＝無；`boards` 內卻沒被動到的＝無）。

**Gate 我自己實跑**：`design-evidence-check.sh` exit 0（r1／r2 兩張皆過，r2 `head_sha=fdfb5b7（本輪最新）`）；`design-notes-check.sh` exit 0（`Notes 板 8 塊、head id 9899、本 PR 範圍曾存在而 head 已無的 id 142、沿革引用 36、缺失 0、署名 NBSP 違規 0（舊債 0）`）。

**獨立抽驗（不只轉貼腳本輸出）**
- **BL-1 自算**：`Get(depth 0)` 解析後 bounds 全稿 231 個 root 兩兩比對 → **OVERLAP_PAIRS=0**；21 張觸碰板逐一算最近鄰距離 → **<80pt 的板數＝0**。R1 的 5 對（`jftM0×yec61` 634、`yec61×EzJCV` 678、`b3nzDp×`三板 875）全數消失，`sibling_intersection` 由 2071→2066，**差額恰為 5**。
- **MN-2 自算**：`wQVzs` override 24 個、其中刻意留白 4、帶內容 20（單寶貝 15×1＋多寶貝 5×2＝25 個「·」）＋元件定義 1 ＝ **全稿 26** —— 與訂正後 `epDnW` 的算式逐項吻合。
- **範圍 2/5 codepoint（重建後複驗）**：`HLXo3` Feed 整棵重建、`fZ3KF`→`kmbyt` 換 id，覆寫內容 **完整存活**：`小安`+U+0020+`·`+U+00A0+`2`+U+00A0+`歲`+U+00A0+`3`+U+00A0+`個`+U+2060+`月`+`、`+`小明`+U+0020+`·`+U+00A0+`8`+U+00A0+`個`+U+2060+`月`，與元件定義 `x7k2o6` 逐字元相同。

---

## 二、R1 十二條通過條件逐條核銷

| # | 條件 | 判定 | 我的複驗依據 |
|---|---|---|---|
| 1 | BL-1 root 重疊 0、≥80pt、收據獨立分類 | **✓ 達成** | 自算 231 root：重疊 0；21 板最近鄰皆 ≥80；收據 flagged 內無任何 root 層項目 |
| 2 | BL-2 `xv1gb` 根因改 Body-clip＋真實分割理由 | **✓ 達成（做得最好的一條）** | 見下「值得保留」1 |
| 3 | MJ-1 `HLXo3` 回家族結構 | **✓ 結構達成**（但引入新缺陷，見 MJ-B） | Body `fill_container`/clip=true/h=1316、Feed `declH=undefined`/clip=false、具名 Spacer h=88 **在 root 層 d1**——與 `lKoZG` 逐欄位相同 |
| 4 | MJ-2 `iBYUK` 提示入 Field Wrap＋改文案 | **✓ 達成** | `i2SHf` 插槽內 `Reply Line`＋`Help Line`，與 `O1XC2Z` 的 `Error Line`＋`Help Line` 同構；文案已含「發生什麼／哪兩欄／怎麼辦」 |
| 5 | MJ-3 補 AX3／`FEuAL` 記理由 | **✗ 未達成** | `VUAqK` 已建但在 AX3 下破版（**BL-A**）；`FEuAL` 理由成立且我另找到更強支撐（i-B） |
| 6 | MJ-4 `q6CXE`／`qBdcW` 17pt＋移到失效控制項上方 | **✓ 達成** | 兩者皆 `$fs-note`，位置在 `Top` 內（Notice Row 之後、Card 之前）；AX3 `Deljc`／`I07MZX` h=2050，Footer 底 1988 < Home Indicator 2016，無擠壓 |
| 7 | MJ-5（`EUFuw`）／MN-5（`eHZ0H`） | **△ 一半** | `EUFuw` ✓ 已改成事實描述；`eHZ0H` ✗ 仍併引且重複（**MJ-C**） |
| 8 | MJ-6 96／139 補謂詞或刪除 | **✓ 達成** | `vCp8T`／`EEgMu` 兩處數字刪除、改定性描述並註明訂正依據 |
| 9 | MN-1 對帳／MN-2 算式 | **✓ 達成** | MN-1 誠實說明原始腳本已不存在、給出全部 20 個 id 供重跑；MN-2 我複算完全吻合 |
| 10 | MN-3 可點列／MN-4 標題不切詞 | **✓ 達成** | `dxPvP` 用 `cmp/Settings Row` 實例；AX3 `hHrIV` 截圖確認換行正常、掃描無 `Left×Right`；`JzyIv` 標題換行為「開始使用前，請詳閱／使用條款」，「使用條款」完整 |
| 11 | r2 收據各欄位 | **✓ 達成** | 見上表；`boards` 21 板＝root diff 的 ADDED∪CHANGED |
| 12 | 兩支 gate 逐字輸出 | **✓ 達成** | 我自己實跑，exit 0 |

---

## 三、BLOCKER

### BL-A　`VUAqK`（本輪為了滿足 MJ-3 而建的 AX3 板）在 AX3 下破版，而六支掃描結構上看不見

把 `VUAqK` 和家族既有的兩張 AX3 板並排量，問題一眼可見：

| 節點 | `vgvFY`（P1 AX3，既有） | `LkrrS`（P1c AX3，既有） | **`VUAqK`（本輪新建）** |
|---|---|---|---|
| `Field Box` 宣告 height | **140** | **140** | **60** ← 沿用 iPhone 板未改 |
| `Field Value` 實際高（40pt 兩行） | 116 | 116 | 116 |
| Field Value 是否在 Field Box 內 | 11497–11613 ⊂ 11485–11625 ✓ | 12509–12625 ⊂ 12497–12637 ✓ | **15143–15259 vs 框 15171–15231 → 上下各溢出 28pt** ✗ |
| `Toggle Icon`／`Field Help Icon` | **32** | **32** | **18** ← 同樣未改 |

實測後果（絕對座標）：
- Email 欄的 placeholder「yourname@example.com」**跑到自己的輸入框外面**，上緣高出框 28pt、下緣低於框 28pt。
- 密碼欄同樣 28pt 上下溢出；`Toggle Wrap`（「顯示」）15305–15382 也超出其 Field Box 15313–15373 兩端。
- **`密碼` 標籤（15247–15305）與 Email 欄的 `Field Help`（15206–15270）直接重疊 23pt。**

截圖：`/private/tmp/claude-501/-Users-clyeh-little-sprout/e6972486-fec6-43c0-b8f4-28b43b01a5a0/scratchpad/LS-208/LS-208-vr2-VUAqK.png`

**為什麼收據是綠的**——這點比缺陷本身更重要，請寫進 Notes：
- `row_overflow` 只驗**右緣**（x），不驗垂直溢出；
- `board_clip` 只驗板 root 邊界，沒有東西離開 1554 的板；
- `text_occlusion` 只認 `Action Bar|Tab Bar|Capsule|Footer|Toast|Banner`，`Field Box` 不在名單；
- `sibling_intersection` 看的是**同父兄弟**，`Field Value` 是 `Field Box` 的**子節點**，框內溢出天生看不到；
- 密碼標籤 × Email 說明列屬跨 parent，但 `cross_parent_collision` 預設只報 `BLEED_RE`（Corner／Badge／…）候選，Field 類名稱被濾掉。

所以 handoff 的「新增 AX3 板 `VUAqK`…**六支掃描全 0 溢出**」這句話是真的，但它對這張板**不構成任何證據**。MJ-3 的目的是「證明新增的訊息列在 AX3 撐得住」，結果拿來證明的那張板自己撐不住——這正是 R1 說的「用敘述／綠燈取代看一眼」，只是這次換了形式。

**改法**：
1. `VUAqK` 兩個 `Field Box` override 的 `height` 60 → **140**（比照 `vgvFY`／`LkrrS`）；`Toggle Icon`、`Field Help Icon` 的 18 → **32**（同兩板）。
2. 板高要重新推導、不能沿用 1554：`Field Wrap` 現為 790，兩個框各 +80 後約 950；而 `VUAqK` 的 `Field Help` 是 470（比 `LkrrS` 的 412 還多 58，因為多一條 Reply Line）——請照 `LkrrS`（Field Wrap 892 / 板 1554）的比例重算，落地後再跑一次六支＋**目視**。
3. Notes 補一條「AX3 板不是把 iPhone 板換 theme 就好：`Field Box` 高度、icon 尺寸這兩類**不隨 theme 縮放的字面值**必須同步改，而且六支掃描抓不到它」——這條規則值得留給下一張票。

---

## 四、MAJOR

### MJ-A　`Djv58` 把 clip 歸給 `Feed`，但五板實測 clip 在 `Body` 上——與 BL-2 同一種錯，而且是本輪新寫的句子

`Djv58` 訂正後寫：「…20 筆完全落在 **Feed 的 clip:true 邊界**（LS-201 已補）之外」。我對範圍 1 的那 5 板逐一列出誰帶 `clip:true`：

```
yXSht : 板 root ✓ , Body(n7gPAe) ✓ , [Feed dTdm3 clip=false]
a67Na : 板 root ✓ , Body(x5Vmg) ✓ , [Feed aNcaB clip=false]
V4Ktak: 板 root ✓ , Body(Q600X) ✓ , [Feed riVZU clip=false]
lKoZG : 板 root ✓ , Body(YulfE) ✓ , [Feed LJJLP clip=false]
A33la : 板 root ✓ , Body(o73UY) ✓ , [Feed Z83pG clip=false]
```

五板的 `Feed` **一律 clip=false**，裁切的是 `Body`。同一段 Notes 的隔壁節點 `xv1gb` 才剛因為「把成因寫給錯的機制」被列 BL-2 並改正，`Djv58` 這句卻把成因寫給錯的**節點**。而且 R1 的 i-2 已經點過同一件事（`lyu44` 記 LS-201 作法為「Feed 補 clip:true」），本輪未處理，於是這個錯誤現在同時存在於**兩個** Notes 節點。

這不是雞蛋裡挑骨頭：本票的產出就是這份紀錄，下一張票照著「Feed 的 clip」去找，會找到一個 `clip=false` 的節點。

**改法**：`Djv58` 與 `lyu44` 兩處的「Feed 的 clip:true」改為「**Body 的 `clip:true`**（Feed 本身 clip=false，淨空由 root 層具名 Spacer 88 承載）」，並在 `lyu44` 註明這是對 LS-201 原句的訂正。

### MJ-B　`HLXo3` 重建引入 2 筆新 `row_overflow`，而收據只給總數、沒給差額

`row_overflow` 560（r1）→ **562**（r2），新增的正好是一個 R1 完全不存在的類別：

```
2× Stamp Wrap :: Stamp   e.g. FFJTV/xlLIo (+2.3)
```

兩筆都在 `HLXo3`——本輪重建的那張板（`FFJTV/xlLIo` **+2.30**、`ZcRJQ/xlLIo` **+3.45**）。我把全稿 23 個 `Stamp`⊂`Stamp Wrap` 實例全部量過：

- 其餘 21 個一律 **0.14–0.42pt**（旋轉章戳的次像素包絡，低於 0.5 允差，從不被報）
- 只有 `HLXo3` 這兩個是 **2.30 / 3.45**——全稿唯二越過允差的

而且和**來源板**對比，內容物完全一樣、只有容器不一樣：

| | `Stamp`(xlLIo) 寬 | `Ghost`(tBi8u) 寬 | `Date Text` 寬 | **`Stamp Wrap`(OCYqN) 解析寬** |
|---|---|---|---|---|
| `lKoZG` DD1 / DD2 | 334.08 / 335.51 | 331.17 / 333.21 | 314 / 314 | **333.80 / 335.09** |
| `HLXo3` DD1 / DD2 | 334.08 / 335.51 | 331.17 / 333.21 | 314 / 314 | **331.78 / 332.06** |

子節點逐項相同（含 rotation −4／−6、−1／−3），只有 `Stamp Wrap` 在 `HLXo3` 少了約 2–3pt，章戳因此戳出容器。請找出重建時 `Day Group`／`Day Divider` 那一層跑掉的 padding／gap／width，讓 `HLXo3` 的 Day Divider 與 `lKoZG` 逐欄位一致。

**同樣重要的是收據那一面**：handoff 對 `row_overflow` 只寫「23 類語意分組，N 加總=562」。560→562 的差額、以及這 2 筆就落在本輪唯一重建的那張板上，收據和 handoff 都沒有一個字。R1 的 BL-1 是把 5 對 root 重疊寫成「緊鄰擺放」，本輪是把 +2 藏進總數——形式不同，**同一個病**：彙整取代了差額。

**改法**：修好那 2 筆；並在 r3 收據為每支加一行「對上一輪的差額與歸因」（例如 `row_overflow 562→N（−2：HLXo3 Day Divider 對齊 lKoZG）`）。這一行是本票最該留下的機制。

### MJ-C　條件 7 只完成一半，且 `dxPvP` 文案已偏離 LS-192 裁決而未標記

1. **`eHZ0H` 的併引與重複仍在**。現行原文：
   > 文案「無法退出家庭。目前家庭只有你一位成員，無法退出家庭。若要離開，請使用下方的「帳號」設定前往刪除帳號。」

   R1 MN-5 要求把 Title／Subtitle 分開引、並註明 Title 是本票新增。稿面本身是對的（`Tutlg`＝「無法退出家庭」、`SgNBK`＝後半段），但 Notes 仍把兩者黏成一句、「無法退出家庭」出現兩次。

2. **更要緊的是：Subtitle 本身已經被改過，而 Notes 把它當成 LS-192 的原文在引。**
   - LS-192 `11c8286e` 裁決文案：`…若要離開，請至「帳號」→「刪除帳號」。`
   - 現行稿面 `SgNBK`：`…若要離開，請使用下方的「帳號」設定前往刪除帳號。`

   這個改動我**認為是對的**（既然多了可點擊的列，「請至…→…」的自助導航語氣就該換成「用下面那一列」），但它動的是 orchestrator 已裁決的文案，必須被標成本票提案、進使用者核可頁，而不是無聲替換。

**改法**：`eHZ0H` 改成三段分開引——① Title「無法退出家庭」（本票新增，不在 LS-192 指定文案內）；② Subtitle 現行值；③ LS-192 `11c8286e` 原值＋一句「本票因新增可點擊列而調整後半句，列入核可頁待使用者確認」。同時把 `dxPvP` 加進「需核可頁的文案變更」清單。

---

## 五、MINOR

- **MN-A　`iBYUK` 的提示列缺家族的第三個區辨訊號。** `O1XC2Z` 用三重差異把「回話」和「常駐說明」分開：色（`$danger` vs `$text-secondary`）、**字重（600 vs normal）**、icon（circle-alert vs info）。`iBYUK` 保留了色（`$text-primary` vs `$text-secondary`）與 icon，但兩行**都是 normal**——而 `#2B141C` 與 `#553040` 在同底色上差異很細，截圖上兩行讀起來就是同一塊。**改法**：`QMJfn` Reply Text 給 `fontWeight: 600`（就是 `Error Text` 的作法，不引入 `$danger`、不新造 token）。
- **MN-B　節點仍叫 `Footer Note`，但已經搬到 `Top`。** `q6CXE`／`qBdcW`（含 AX3 的 `dFd0Q`／`DRXHy`）位置改對了，名字沒跟著改，實作者照名字會去 Footer 找。**改法**：改名為 `State Note` 之類，並在 `nQ0TV` 同步。

---

## 六、Informational（含一則我自己的更正）

- **i-A（我 R1 的措辭更正）**：R1 MJ-1 我寫「Feed 現在 clip=true 且高度固定，內容底 1121 已經被切掉 129pt（**整條 `Load More` 完全不可見**）」。查證後更正：`Load More`（R1 `PqQEK`／現 `HB3Ju`）**在 `origin/development` 就已經是 `enabled:false`**，並帶著殘留的絕對 `x:0, y:1014.96`——它本來就不顯示，不是被 992 切掉的。被 992 真正切掉的是 `Day Group 2` 尾端 37pt。MJ-1 的主體（字面 992＋clip 位置＋缺具名 Spacer）成立且已修，這一句是我的過度歸因。
  順帶一提：全稿 7 個 `Load More` 只有這一個被停用，且它是唯一一個在 `layout:"vertical"` 的 Feed 裡帶絕對座標的（`lKoZG` 的 `CvUlD` 是啟用、在 flow 內）。屬 development 既有債、不歸本票，但若哪天有人把它打開，它會直接壓在 `Day Group 2` 上 39pt——值得記一筆到 LS-96。
- **i-B　`FEuAL` 免 AX3 的理由比 handoff 論證的更站得住。** 我查了整份稿：`01b`（`pcSdz`，歡迎登入 · 登入中）與 `P1b`（`pl5GE`，帳號密碼登入 · 登入中）**都沒有 AX3 板**，而訊息態 `P1c` 有（`LkrrS`）。也就是說「**in-flight 態不建 AX3、訊息態建**」本來就是這個家族的既有規則，`FEuAL` 只是照做。建議把這句規則直接寫進 `EEgMu`，之後就有可援引的依據，不必每次重新論證。
- **i-C　`z042Yg`／`JzyIv` 的「全捲動內容表示法 vs 釘底 Action Bar」（R1 i-1）本輪未處理。** 不擋，但仍建議在 Notes 一句話定義，否則實作者看 912／940 的板會照著排一個釘底 bar。

---

## 七、值得保留（重做時不要洗掉）

1. **`xv1gb` 的 BL-2 訂正是本票目前最好的一段文字**：把 R1 的錯句原樣引出來、說明「與量測相反」、附四筆抽驗與各自的 Body clip 邊界、再說清楚「我的量測方法本身沒問題（能用 clip 判準量到，就不可能是 z-order），是結論句寫反了」。而且分割理由改成真實的那個（池項授權範圍／發現時機），不再用假的「成因不同類」。這就是清倉票該長的樣子。
2. **`Djv58` 拒絕「捨入」框架**：誠實說明 17 來自一支未進版控、現已不存在的暫存腳本，無法逐筆對帳，並列出全部 20 個 id 讓任何人隨時重跑。承認「量不出來」比湊一個理由好——這個態度請保持（只差 MJ-A 那一個節點名）。
3. **BL-1 修得徹底**：不是把重疊的兩張挪開了事，而是把四張 fit_content Notes 板整批搬到 y>96287 的乾淨區、建立 80pt 間距鏈，並自己發現重建後 `L6iIQ0`／`Deljc` 又生出 20pt 重疊再修掉。我全稿 231 root 複算重疊 0、21 板皆 ≥80。
4. **MJ-1 的結構對齊是逐欄位的**：Body `fill_container`+clip=true+1316、Feed fit_content+clip=false、具名 Spacer 88 放在 **root 層 d1**——與 `lKoZG` 完全一致；而且過程中自己抓到「Spacer 誤放 Body 內 → `text_occlusion` 1 筆真實遮蔽」並用 `Move()` 修正、把過程寫進 `lyu44`。**自己抓到自己的錯並留下記錄**，比一次做對更有價值。
5. **`iBYUK` 的 MJ-2 修法正中要害**：不是把那一列往上搬，而是放進 `Password Field` 的 `i2SHf` 插槽，與 `O1XC2Z` 的 `Error Line`＋`Help Line` 同構；文案「還沒填 Email 和密碼。在上面兩欄打上資料，再按一次登入。」先講發生什麼、再講哪兩欄、再講怎麼辦——品牌十條 #8 的標準寫法。`$text-primary`＋circle-alert 而非 `$danger` 也守住了。
6. **MN-3 沒有硬幹**：第一版手刻 Left／Right 在 AX3 撞了 73pt，改用既有 `cmp/Settings Row` 元件後解決，AX3 板 `hHrIV` 我截圖確認換行正常。「既有元件優於臨場發明」在 AX3 這種變形場景是對的判斷。
7. **範圍 2/5 熬過了整棵 Feed 重建**：`fZ3KF`→`kmbyt` 換 id 後 NBSP／WORD JOINER 逐字元完好。
8. **MJ-4 不只改字級**：17pt 之外還把說明移到失效控制項正上方，AX3 板同步重建且量得出來不擠壓（Footer 底 1988 < Home Indicator 2016）。

---

## 八、R3 通過條件（範圍已很窄）

1. **BL-A**：`VUAqK` 兩個 `Field Box` height 60→140、`Toggle Icon`／`Field Help Icon` 18→32、板高依 `LkrrS` 比例重新推導；落地後**除了六支掃描，附一張 `VUAqK` 截圖**證明兩個輸入框包得住自己的文字、`密碼` 標籤不壓到 Email 說明列。Notes 加「AX3 板的不縮放字面值（框高／icon）必須同步改，且六支掃描抓不到」這條規則。
2. **MJ-A**：`Djv58`＋`lyu44` 的「Feed 的 clip:true」改成 `Body` 的 clip:true（附 Feed clip=false 的事實）。
3. **MJ-B**：`HLXo3` 兩顆 Day Divider 對齊 `lKoZG`（`Stamp Wrap` 解析寬回到 333.8／335.09 量級），`row_overflow` 回到 560；r3 收據每支加一行「對上一輪的差額與歸因」。
4. **MJ-C**：`eHZ0H` 三段分開引（Title 本票新增／現行 Subtitle／LS-192 原值＋調整理由），`dxPvP` 加入需核可頁的文案變更清單。
5. **MN-A／MN-B**：Reply Text `fontWeight: 600`；`Footer Note` 更名並同步 `nQ0TV`。
6. r3 收據照舊（`scan_scope=document`、三項硬指標對觸碰板 0、`tree_hash` 對 head、`boards` 含本輪動過的全部板、SUMMARY 逐字），兩支 gate 逐字輸出。

**性質**：BL-A、MJ-A、MJ-B、MN-B 全屬**可機械複驗項**（座標、宣告值、字面數字、節點名）；MJ-C 是紀錄與核可流程；只有 MN-A 帶一點品味成分。若 R3 把這 6 條收乾淨，我預期 R3 可以 APPROVE。

**這份稿現在缺什麼**：R1 說它的靈魂是「可複算性」，本輪在**紀錄面**幾乎補齊了——`xv1gb`、`Djv58`、`epDnW`、`vCp8T` 四段都從「敘述」變回「量測」，而且會自己承認量不出來。但本輪把重心放在「讓收據綠」的地方又漏了一次：`VUAqK` 是一張為了證明 AX3 沒問題而生的板，交出來時六支全 0、人卻沒看過它長什麼樣。**綠燈是必要條件，不是證據**——尤其是當你要證明的那件事（垂直方向的文字溢出）正好是六支結構上看不見的那一類。R3 只要補上「改完看一眼」這一步，這張票就成立了。


### 2026-09-06T12:40:17.190Z · Chih-Lin Yeh · `0703744e`

## visual-reviewer 第 3 輪：APPROVE（附 1 項合併前必修）

（2026-09-06 20:37 CST）

**little-sprout-brand skill**：已載入。
**Pen 路徑**：`pen-status.sh` → `/Users/clyeh/little-sprout/.claude/worktrees/LS-208/design/littlesprout.pen`（pid 72575）。新鮮度三方一致：Pencil `SUMMARY-HASH total_nodes=9899 tree_hash=95623f2b21914306` ＝ 磁碟 `design_tree_hash.py` ＝ 收據 `tree_hash`。未跑 `--force-reload`／`--kill`，全程無「需重連」。

**判定**：R2 列的 6 條通過條件，**5 條完全成立、第 5 條（MN-A／MN-B）只差一個節點屬性**。所有 blocker／major 等級的項目我都用自己的量測獨立驗過、全部乾淨；剩下的是一個 `fontWeight`。依 LS-152 R4 的附條件核可先例，本輪給 **APPROVE**，並把那一項列為**合併前必修**（單一節點、可機械複驗，不需再開一輪 VR，merge-reviewer 核銷即可）。

---

## 一、六支重掃＋三方雜湊＋root diff（我自己跑）

`scripts/design/overflow-scan.js` 全文＋收據 21 板 `SCAN_BOARDS`，依 LS-171 拆兩次唯讀 execute。第二次（雜湊）我把腳本的雜湊路徑（`canon`／`canonNode`／`utf8Bytes`／`fnv1a64`／`addLimbs`／`hex64` ＋ `includePathGeometry` 走訪）逐字抽出單獨跑——**它算出的值與磁碟 python 實作逐字相同，等於同時證明了轉抄無誤**。

我的 SUMMARY（逐字）：
```
SUMMARY total_nodes=9899 scanned_nodes=16017 scan_scope=document sibling_intersection=2066 row_overflow=560 cross_parent_collision=321(bleed-only) text_occlusion=0/0 board_clip=0/0 corner_anchor=6/48/0 document=225/1784/0 ref_hits=746(defs=10) unresolved=3/14 boards=["yec61","HLXo3","qtCd7","kHDk4","N1aeiU","FEuAL","iBYUK","VUAqK","XhwG6","b3nzDp","dxPvP","hHrIV","z042Yg","Oic2v","JzyIv","RC7bN","jftM0","UOWte","Deljc","L6iIQ0","I07MZX"] tree_hash=skipped
```

| 項目 | 收據 r3 | 我的重跑 | |
|---|---|---|---|
| total / scanned | 9899 / 16017 | 同 | ✓ |
| sibling / row / cross | 2066 / **560** / 321 | 同 | ✓ `row_overflow` 由 R2 的 562 回到 560 |
| text_occlusion / board_clip | 0/0 / 0/0 | 同 | ✓ |
| corner_anchor（boards／document） | 6/48/0 · 225/1784/0 | 同 | ✓ |
| ref_hits / unresolved | 746(defs=10) / 3/14 | 同 | ✓（3 筆皆 Tokens 板 `mount_pair`） |
| tree_hash | 95623f2b21914306 | Pencil 端同值＝磁碟同值 | ✓ |
| `sibling_intersection` 內 **root 層項目** | — | **0**（我另外把 root 層單獨數出來印） | ✓ |

**python root diff（對 `origin/development` 689f5ae）**：ADDED 12 / REMOVED 1（`rh2Q1`）/ CHANGED 9 / **UNCHANGED 210**；我用集合等式直接判定 **`set(ADDED) | set(CHANGED) == 收據 boards(21)` → True**（無多、無漏）。另比 R2→R3：**頂層節點集合完全相同**（231 個，本輪未新增／刪除任何板），只有 13 張既有板內部有變更，與 handoff 的清單一致。

**Gate 我自己實跑（三份收據）**：`design-evidence-check.sh` exit 0，r1／r2／r3 三張全過（r3 `head_sha=be05441（本輪最新）`）；`design-notes-check.sh` exit 0：`Notes 板 8 塊、head id 9899、本 PR 範圍曾存在而 head 已無的 id 144、沿革引用 48、缺失 0、署名 NBSP 違規 0（舊債 0）`。

---

## 二、六條逐一核銷

### 1. BL-A（`VUAqK` AX3 破版）→ **✓ 成立**

我沒有採信收據，也沒有採信 `.claude/evidence/LS-208/r3/vuaqk-aabb-nodes.json`（那個檔只有 38 bytes、內容是 `{"total_nodes": 44, "violations": []}`，是**結論**不是原始資料，本身不可獨立複核）。我自己寫了子對父四邊 AABB 掃描（`resolveInstances:true` 展開、沿 enabled 鏈過濾、TOL=0.5）重跑：

```
AABB子對父 VUAqK  A11y / P1f … AX3      ：live節點=41 違規=0
AABB子對父 iBYUK  LS-163 / P1f 空欄位提示 ：live節點=41 違規=0
AABB子對父 hHrIV  A11y / 03e2 … AX3     ：live節點=30 違規=0
AABB子對父 dxPvP  LS-152 / 03e2         ：live節點=30 違規=0
```
（我數 41 是**活節點**、收據數 44 是全部節點，差額為停用子樹，兩者不衝突。）

R2 我點名的四筆全部消失：Email／密碼兩欄 `Field Value` 已完全落在自己的 `Field Box` 內、`Toggle Wrap` 不再兩端外露、`密碼` 標籤不再壓到 Email 的說明列。`Field Box` 60→**140**、icon 18→**32**，與 `vgvFY`／`LkrrS` 一致。板高 1554→**1584** 後 `text_occlusion` 仍為 0。
截圖：`/private/tmp/claude-501/-Users-clyeh-little-sprout/e6972486-fec6-43c0-b8f4-28b43b01a5a0/scratchpad/LS-208/LS-208-vr3-VUAqK.png`

### 2. MJ-A（Notes 把 clip 歸給 `Feed`）→ **✓ 成立**
`Djv58` 三處、`lyu44` 一處都改成 `Body` 的 `clip:true`，並附「本輪逐板實測 Feed 一律 `clip=undefined`／未設」的事實與訂正標記；`lyu44` 進一步把 LS-201 原句（「Feed 補 clip:true」「height 維持 fill_container」）明確標為誤記並給出正確結構。與我 R2 的實測（五板 Feed 皆 clip=false、Body 皆 clip=true）逐字相符。

### 3. MJ-B（`HLXo3` 重建引入 2 筆 `row_overflow`）→ **✓ 成立，且比我要求的更嚴**
我把全稿 23 顆 `Stamp` ⊄ `Stamp Wrap` 重量一遍：**>TOL(0.5) 的有 0 顆**，最大值 0.42pt。且解析寬度**與來源板逐位小數相同**：

| | `Stamp Wrap` 解析寬 |
|---|---|
| `x4GZ9K/OCYqN`（HLXo3 DD1） | **333.8001** |
| `f9KM0G/OCYqN`（lKoZG DD1） | **333.8001** |
| `PvbTR/OCYqN`（HLXo3 DD2） | **335.0911** |
| `Tuw0p/OCYqN`（lKoZG DD2） | **335.0911** |

`row_overflow` 562→560，`Stamp Wrap :: Stamp` 這一類完全消失。診斷過程（逐欄位比對子節點 → `Delete()`＋`Insert()` 重建排除快取 → 判定為工具端 `fit_content` 解析差異）寫進 `lyu44`，兩個字面 width 的**出處也寫明了**（取自 `lKoZG` 實測值），所以這兩個數字有主人、不是倒推餘數。收據的 `row_overflow.flagged` 也歸因到具體節點 id 而非只給總數——這正是我 R2 要的那一行。

### 4. MJ-C（`eHZ0H` 併引／Subtitle 偏離裁決未標記）→ **✓ 成立**
`eHZ0H` 現在先**逐字引 LS-192 `11c8286e` 原裁決文案**（並點明「原裁決文案本身把『無法退出家庭』重複兩次」），再另段標明 R3 改寫是「**本票提案，不是 LS-192 逐字裁決，待核可（已列入本票核可頁清單，範圍 6）**」。Subtitle（`SgNBK`／AX3 `mPp3X`）逐字一致：`目前家庭只有你一位成員，沒有可轉移的對象。若要離開，請使用下方的「帳號」設定前往刪除帳號。`——去掉與 Title 的重複、補上「為什麼不能轉移」，我認為比原裁決更好，但正確做法就是像現在這樣送人裁。

### 5. MN-A／MN-B → **MN-B ✓；MN-A 差一個節點（見下方「合併前必修」）**
- MN-B ✓：`q6CXE`／`qBdcW`／`dFd0Q`／`DRXHy` 四個節點名皆已改為 `Notice Detail`，且 `nQ0TV`（`mwv65`）有記錄這次改名與理由。
- MN-A △：`iBYUK/QMJfn` 的 `fontWeight` 已是 `600`，截圖上回話列與常駐說明列的區辨很清楚（`LS-208-vr3-iBYUK.png`）。**但 AX3 板 `VUAqK` 的 Reply Text 是自己的節點 `xf0zM`，仍為 `normal`。**

### 6. r3 收據＋兩支 gate → **✓ 成立**（見上表）

### 新增缺陷獵殺（板高 1584 之後）
- 全稿 **231 個 root，重疊 0 對**；21 張觸碰板最近鄰**全部 ≥80pt**；全體最小間距恰為 **80**（`HLXo3→P3DW1`）。`VUAqK` +30pt 沒有吃掉任何 gutter。
- `text_occlusion` / `board_clip` / `corner_anchor.mismatch` 三項硬指標對觸碰板皆 0；我另對 `iBYUK`／`hHrIV`／`dxPvP` 也跑了子對父 AABB，皆 0 違規。
- 本輪未新增／刪除任何頂層板，`qtCd7`／`z042Yg`／`FEuAL` 等未觸碰板內容與 R2 相同。

---

## 三、合併前必修（1 項，不需再開 VR 輪）

**M-1（MN-A 殘留）**：`VUAqK/xf0zM`（AX3 板的 Reply Text）`fontWeight` `normal` → **`600`**，與 `iBYUK/QMJfn` 一致。

- Handoff 寫「`iBYUK`（及其 AX3 板 `VUAqK`，**直接繼承未另覆寫**）的 Reply Text（`QMJfn`）fontWeight normal→600」——這句不成立：`VUAqK` 是從 `iBYUK` 複製而來，帶有自己整棵 `i2SHf` 覆寫子樹（`e8hRO5`／`xf0zM`／`y22bj`／`R7Cvp`），字重沒有跟著改。
- 後果不是零：AX3 下 40pt 四行的回話列與常駐說明列現在**完全同一個外觀**（同字級、同字重，只差 `$text-primary` vs `$text-secondary` 這個很細的色差），區辨反而比 iPhone 版弱——而 AX3 使用者正是最需要區辨的人。截圖 `LS-208-vr3-VUAqK.png` 可見兩段讀起來是同一塊。
- 一個屬性、可機械複驗（`Get("VUAqK", n => n.id==="xf0zM")` 看 `fontWeight`），請在設計 PR 內補上，由 merge-reviewer 核銷。

---

## 四、Informational（不擋合併）

- **i-1　`lyu44` 的「次像素級」用詞與它自己引的數字不符。** 同一段先說 `Stamp Wrap` 在兩板差 ~2pt（331.78／332.06 vs 333.80／335.09），結論卻寫「產生穩定但不一致的**次像素級**結果」。2–3pt 不是次像素（全稿其餘 21 顆的差異 0.14–0.42pt 才是）。工具限制的判定我認同（`Delete()`＋`Insert()` 重建後數值不變，這個實驗設計得好），但**量級描述請改成「pt 級（實測 2–3pt）」**，否則下一個人會以為可以忽略。
- **i-2　BL-A 的證據檔只有結論、沒有原始資料。** `.claude/evidence/LS-208/r3/vuaqk-aabb-nodes.json` 38 bytes、內容是 `{"total_nodes": 44, "violations": []}`。檔名叫 `-nodes.json` 但沒有節點資料，無法被第三方複核（我這次是自己重跑才算數）。往後這類取證檔請存逐節點的四邊量測值。
- **i-3　全體最小板距恰為 80pt、零餘裕。** 四張 fit_content 的 Notes 板每加一段沿革就會長高，下一張票加 Notes 時很可能又壓到。建議把「Notes 板加段後重跑 root 間距檢查」寫進收工步驟（R1 BL-1 已建議過，本輪未落地成文字）。
- **i-4　R1 i-1（EULA 板「全捲動內容表示法 vs 釘底 Action Bar」）三輪未處理。** 不擋，但 `z042Yg` 912／`JzyIv` 940 交給 ios-dev 時，建議 Notes 一句話講清楚 Action Bar 是釘底還是隨內容流。

---

## 五、APPROVE 的依據

**這份設計的三個記憶點（我自己寫得出來的，不是設計者宣稱的）**
1. **一段會自己認錯的紀錄。** `xv1gb`（z-order → Body clip）、`Djv58`（Feed → Body、拒絕「捨入」）、`lyu44`（LS-201 原句誤記）、`vCp8T`（刪掉算不出來的 96／139）——四段都把「敘述」換回「量測」，每次訂正都留下依據的 comment id。這份稿現在任何一句關於幾何的話，都可以被下一個人重跑核對。
2. **`dxPvP` 從散文變成一條路。** 原本用一句「請至『帳號』→『刪除帳號』」叫長輩自己去翻設定；現在是一張複用 `cmp/Settings Row` 的可點卡片列（AX3 下換行正常），而且改動過的文案被誠實標成待核可提案，不是無聲替換。
3. **`iBYUK`／`VUAqK` 這一對，把「即時回話」做成了家族語彙。** 提示放進 `Password Field` 的 `i2SHf` 插槽（與 `O1XC2Z` 的 `Error Line`＋`Help Line` 同構）、用 `$text-primary`＋circle-alert 而不是 `$danger` 三角（品牌十條 #8），登入鈕維持 enabled（驗證型 disable=0），文案先講發生什麼、再講哪兩欄、再講怎麼辦。

**Slop 十條**：無中槍。#1 每畫面一顆實心主鈕（停權態把 `$accent` 降成 `$surface-2` 後畫面零實心，屬「最多一次」的合法情形）；#2／#8 R1 點名的 `dxPvP` 531pt 無名空白已由家族元件填上；#5 未新造任何 token、未擴散強調色；#7 AX3／Stress 壓力板齊備並實測不裁切。
**靈魂測試**：這是一張清倉票，它的產物是紀錄而不是畫面，所以靈魂測試要問的是「這份紀錄可不可信」。三輪下來，我獨立重算過的每一組數字——1.462／8.129、20／49、24 個 override／26 個「·」、333.8001／335.0911、41 節點 0 違規、231 root 0 重疊——**全部吻合**。這一點現在成立。

**三輪對抗迭代已完成**（R1 ITERATE：2 blocker＋6 major；R2 ITERATE：1 blocker＋3 major；R3 APPROVE）。

---

## 六、需使用者核可頁的板 id 清單（**注意：多個 AX3 板 id 在 R2／R3 已變更**）

| 範圍 | 主板 | AX3 板 | 備註 |
|---|---|---|---|
| 3　Auth 兩態 | `FEuAL`（01e-b 連結進行中 · 暗化）、`iBYUK`（P1f 空欄位提示） | **`VUAqK`**（P1f AX3，本票新建） | `FEuAL` 不另建 AX3，理由已記入 `EEgMu`；我另查證家族既有規則支持它（`01b`／`P1b` 兩張 in-flight 板都沒有 AX3，訊息態 `P1c` 才有 `LkrrS`） |
| 4　深色壓印對比 | — | — | **未調 token，依票文不需核可頁**（量測結論記在 Tokens 板 `XhwG6`／`KI61A`） |
| 6　單人家庭變體 | `dxPvP`（03e2） | **`hHrIV`** ⚠ **不是 `i6uiB4`** | `i6uiB4` 已於 R2 刪除 → 曾為 `S9vBx` → 現行 **`hHrIV`**。**本項含文案提案**：Subtitle 偏離 LS-192 `11c8286e` 逐字裁決，需使用者選擇採用改寫版或維持原文 |
| 7　EULA | `JzyIv`（08b 首次登入）、`z042Yg`（08 基準板加「不同意，登出」出口） | `RC7bN`（08b AX3）、**`Oic2v`**（08 AX3，取代已刪除的 `rh2Q1`） | 零容忍摘要卡內容三輪未動；「不同意，登出」用 `cmp/Button Text` 中性色、非 `$danger` |
| 8　三岔路兩狀態 | `UOWte`（04b 帳號已停權）、`L6iIQ0`（04c 帳號刪除處理中） | **`Deljc`**（取代 `VDO0S`）、**`I07MZX`**（取代 `Z8Q4R`） | 停用態用 `$surface-2`／`$text-secondary`（同 `AuthButtons.swift` isDimmed）；說明文字已由 13pt 改 17pt 並移到失效控制項正上方 |

**待使用者裁決的文案項**：`dxPvP`／`hHrIV` 的 Subtitle 改寫（去除 LS-192 原文中重複的「無法退出家庭」、補上「沒有可轉移的對象」）——採用改寫版，或維持 LS-192 逐字原文。

**給 orchestrator 的兩件事**：① 合併前必修 M-1（一個 `fontWeight`）；② `xv1gb` 建議的 LS-96 池項改寫（相簿家族 5 板 11 格 49 筆，與範圍 1 同一 class，非「角托方案改版殘留」）尚待你更正池項文字。


### 2026-09-06T12:55:56.797Z · Chih-Lin Yeh · `e0582dd7`

**orchestrator 09-06 20:57 核可頁**：https://claude.ai/code/artifact/560a2b43-3b8f-41cf-ab1d-141fe658ed9c（12 板：範圍 3 `FEuAL`／`iBYUK`／`VUAqK`、範圍 6 `dxPvP`／`hHrIV`、範圍 7 `JzyIv`／`RC7bN`／`Oic2v`、範圍 8 `UOWte`／`L6iIQ0`／`Deljc`／`I07MZX`）。待使用者三項：整體核可；範圍 6 Subtitle 用 LS-192 逐字原文或本票提案；範圍 7「不同意，登出」形式。設計 PR #349 merge-review R1 進行中（併入不等核可；若使用者選原文，另補一個 text 節點＋r5 收據）。

### 2026-09-06T13:11:00.089Z · Chih-Lin Yeh · `61e654ff`

## merge-reviewer R1（設計 PR）：REQUEST_CHANGES（1 major／2 minor／2 informational）

（2026-09-06 21:08 CST）

**審的 head**：`03469850a69d27f72120b1618f7ced79479dcde1`（PR #349，`feature/LS-208-design-debt-cleanup-2` → `development`）。`gh pr view 349 --json headRefOid` 執行當下同值——審查期間 head 未前進。
**Base**：`origin/development` `e3f566d279098bbf6bcba8fe0394943b08a496cd`。
**Pen**：全程未開 Pencil MCP，`.pen` 一律用 python 讀明文 JSON（依派工指示）。**DB lock**：未持有（本票無 DB 測試）。**模擬器**：未開。

---

### 一、逐項查實（怎麼驗）

- **只動 .pen＋收據**：`git diff --name-status origin/development...0346985` → `M design/littlesprout.pen`＋`A design/evidence/LS-208-r1/r2/r3/r4-overflow.json`，共 5 檔。無 brand skill、無 `references/`、無程式碼檔、無截圖。
- **他票 root byte-identical**：`scratchpad/LS-208-mr/rootdiff.py`（對兩份快照做 root 層 canonical 比對）→ ADDED 12／REMOVED 1（`rh2Q1`）／CHANGED 9／**UNCHANGED 210**；`set(ADDED)|set(CHANGED) == r4 收據 boards(21)` → `True`，雙向皆無多無漏。
- **210 張未觸碰 root 逐字相同**：`scratchpad/LS-208-mr/checks2.py` 改用**保序** `json.dumps`（不 sort_keys）逐張比對 → mismatch **0**；root 相對順序保持不變，12 張新板一律 append 在尾端。
- **token 未動**：同一支 `scratchpad/LS-208-mr/rootdiff.py` 比 `variables`／`themes`／`version`／`fileToken` 四個頂層鍵 → 全部 `True`（範圍 4「不調 token」裁決落實，也代表本 PR 不會影響任何他票板的解析色值）。
- **變動屬性只含本票類別**：`scratchpad/LS-208-mr/attrdiff.py` 逐板列出——`HLXo3` Feed 重建（+13/−12 節點）＋`aedfE` Body `clip:true`；`yec61`/`b3nzDp`/`N1aeiU` 只有 Notes 新段＋`x`/`y` 落位；`jftM0`/`XhwG6` 只有新段（`XhwG6` 屬性變更 0 筆＝Tokens 沒有被就地改）；`kHDk4`/`qtCd7` 各一個 `content`；`z042Yg` 板高 852→912＋新增 `uyQcn`（`cmp/Button Text`，「不同意，登出」）＋`LaIZs`/`WeWvD` 兩個 `fill_container` 寬度修正。無一筆落在本票類別之外。
- **M-1（VR R3 合併前必修）已落地**：`scratchpad/LS-208-mr/find2.py xf0zM QMJfn` → `VUAqK/xf0zM` 與 `iBYUK/QMJfn` 兩顆 Reply Text 欄位逐字相同、`fontWeight` 皆 `"600"`。`git show 37ab798 -- design/littlesprout.pen` 全 diff 只有 2 行（`lyu44` 措辭＋這個 fontWeight），手術式。
- **tree_hash 對 head 快照**：`python3 scripts/gates/design_tree_hash.py design/littlesprout.pen`（worktree LS-208）→ `c7917c6e09662b28` ＝ r4 收據 `tree_hash`；`git diff 37ab798 0346985 -- design/littlesprout.pen` 為空（`0346985` 只加收據 JSON），所以收據 `head_sha=37ab798` 就是 head 的 .pen。
- **三支硬指標對 21 板為 0**：`design/evidence/LS-208-r4-overflow.json` → `corner_anchor.mismatch=0`／`document_mismatch=0`、`text_occlusion.flagged=[]`／`document_flagged=[]`、`board_clip.flagged=[]`／`document_flagged=[]`；另三支 flagged（29／22／8）每筆皆有非空 classification，且**無任何一筆的 node/node_a/node_b 是 root 層 id**（我逐筆對 root id 集合比對）——VR R1 BL-1「root 重疊被歸進既有母題免責句」的病灶確已除。
- **三支 gate 實跑（`bash scripts/gates/design-evidence-check.sh`／`design-notes-check.sh`＋CI，worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-208`，唯讀）**：
  - `bash scripts/gates/design-evidence-check.sh design/littlesprout.pen --ticket LS-208 --base origin/development` → exit 0，四份收據全過，r4 標 `head_sha=37ab798（本輪最新）`、`total_nodes=9899`、`scan_scope=document`、六支各帶 scope／document_count。
  - `bash scripts/gates/design-notes-check.sh design/littlesprout.pen --base origin/development` → exit 0：`Notes 板 8 塊、head id 9899、本 PR 範圍曾存在而 head 已無的 id 144、沿革引用 48、缺失 0、署名 NBSP 違規 0（舊債 0）`。LS-202 的 NBSP gate 內含於這支，觸碰板全綠。
  - `gh run view --job=101489303548`（CI `rules` job）→ 已由 pending 轉 **pass**；`ci`／`ci-ipad`／`db`／`lint` 亦全 pass。
- **NBSP／「·」codepoint**：`scratchpad/LS-208-mr/checks.py` → `qtCd7/x7k2o6` 現值 `5C0F 5B89 0020 00B7 00A0 0032 00A0 6B72 00A0 0033 00A0 500B 2060 6708 3001 5C0F 660E 0020 00B7 00A0 0038 00A0 500B 2060 6708`，即「小安」＋U+0020＋「·」＋U+00A0 綁住年齡片語、月字前 U+2060；base 版完全沒有「·」。符合 LS-201 案 2 規則。
- **root 重疊／80pt（可機械複算的部分）**：`scratchpad/LS-208-mr/overlap.py` → 196 個有數值 bounds 的 root 兩兩相交 **0 對**；21 張觸碰板中可量的 13 張最近鄰**全部 ≥80**，最小值恰 `80.00`（`HLXo3→P3DW1`），與 VR R3 的 Pencil 量測同一個值。
- **PR body 對帳**：`Design:` 段 12 張新增＋9 張改動＝21 個 id 全部存在於 head `.pen`（`design/evidence/LS-208-r4-overflow.json` 的 `boards` 與之逐字相等），`rh2Q1` 確已不在 head；VR 三輪 comment id `a4597e9e`／`3f3d0432`／`0703744e` 皆在 body 列出。

---

### 二、Findings

#### M1（major）`XhwG6/N0AoFY`：VR MJ-6 判為不可複算、R2 宣告「已刪除」的「96」，還活在同一節的標題上

- **位置**：`design/littlesprout.pen` → root `XhwG6`（設計語言 Tokens）本票新增節 `KI61A` → `Mv61K/N0AoFY`。
- **現值**：`✓ After（既有正確用法，全稿 96 處一致）`
- **同一節的 `vCp8T` 自述**：`【R2 訂正，依 VR comment a4597e9e MJ-6】原句「96 筆」在 VR 用四種口徑複算皆對不上（.pen 未展開 instance／Pencil resolveInstances 兩種模式），故刪除此數字只留結論，避免無法複算的量化敘述`。
- **PR body 變更摘要第 4 條**也宣告：「刪除無法逐口徑對上的『96 筆』『139 處』具體數字，改用定性描述」。**我重放後對不上**：`139` 確實只剩「原句…已刪除」的沿革寫法兩處（`vCp8T`／`EEgMu`），但 `96` 還有一處**活的斷言**。
- **具體失敗情境**：下一個人（ios-dev 或下一張清倉票）讀 Tokens 板第 ⑧ 節的 guardrail，會把「全稿 96 處」當成已核對過的事實引用；而三種口徑我都算過，沒有一種是 96——名稱法（`Imprint Caption`＋`Lab Imprint`，未展開 instance）**14**、VR R1 的 `resolveInstances:true` **41**、`fill=="$print-ink-secondary"` 未展開 base **93**／head **95**。同一節裡一句說「這數字不可複算所以刪了」、隔壁一句還在斷言它——本票的產出就是一份可信紀錄，這種自相矛盾正是三輪 VR 在獵的東西，而它剛好躲過了（VR R2 只核了它自己點名的 `vCp8T`／`EEgMu` 兩個節點）。
- **建議修法**（單一 text 節點的 `content`，可機械複驗，不需要再開 VR 輪）：改成不帶數字的定性寫法（例：`✓ After（既有正確用法，全稿 Imprint Caption／Lab Imprint 一致）`），或補上口徑（例：`fill=$print-ink-secondary、未展開 instance 計 95 處（head 0346985）`）。

#### m1（minor）票文範圍 3／8 明文點名的兩個實作對應檔名，Notes 全稿 0 命中

- **`LabeledTextField`**（範圍 3：「Notes 寫明實作對應（`WelcomeView+PasswordSignIn`／`LabeledTextField`）」）：全稿 text 節點 grep `Labeled` → **0 命中**。`WelcomeView+PasswordSignIn.swift`（`N1aeiU/QB86n`）、`PasswordSignInModel`／`PasswordSignInView`（`WmQBj`）都有，只漏了元件本身。
- **具體失敗情境**：我讀了 `/Users/clyeh/little-sprout/LittleSprout/DesignSystem/LabeledTextField.swift`——現行 API 是 `helpText: String?` ＋ `isError: Bool`，**最多只渲染一列** help row，icon 寫死 `exclamationmark.circle.fill`、色寫死 `Color.lsDanger`、字重跟著 `isError` 綁 `.semibold`。稿面 `iBYUK`／`VUAqK`（與既有 `O1XC2Z`）要的是 **Reply Line＋Help Line 兩行同時 enabled**、且提示行是 `$text-primary`＋`circle-alert`（刻意不用 danger）。Notes 只寫「`PasswordSignInView` 依此布林切換 Field Help 插槽內容」，實作者照著做會發現元件根本表達不了兩行，得回頭改 DesignSystem——這正是票文點名這個檔名的原因。
- **`DeleteAccountFlowView`**（範圍 8：「Notes 寫明實作對應（`ForkView` footer、`DeleteAccountFlowView` 無家庭模式）」）：全稿 grep `DeleteAccount` → **0 命中**。`jftM0/mwv65` 寫了 `UOWte` Footer 兩顆鈕「登出」「刪除帳號」，但沒寫「刪除帳號」按下去進哪裡；`EUFuw` 的「實作對應」只涵蓋 `ForkView` 三態導向與 04c「重試刪除」重打 Edge Function。停權態使用者沒有家庭，刪除流程的家庭分支（唯一 owner 需轉移等）不適用——票文要求把這件事寫死，稿面沒有。
- **建議**：`N1aeiU` 範圍 3 段補一句 `LabeledTextField` 需擴充成雙列 help slot；`jftM0` 範圍 8 段補一句 `UOWte` 的「刪除帳號」導向 `DeleteAccountFlowView`（無家庭模式）。

#### m2（minor）範圍 1 的「寫進 Notes 與 `references/`」只做了 Notes

- 票文範圍 1：「接受為既定視覺（**寫進 Notes 與 `references/`**）或統一加板高，擇一並附 VR 量測」。本 PR 選了「接受為既定視覺」，`yec61/Djv58` 寫得很完整（量法、20 筆節點 id、與池項 17 筆的落差成因、三條裁決理由），但 `git diff --name-status` 顯示 `.claude/skills/little-sprout-brand/references/` 一個字都沒改。
- 這不是「設計分支不能改 references/」：`e270fbe`（`docs(brand): LS-201 R3 MN-B 更新 Legal/Status 槽 AX3 數字`）就是上一張設計票在自己的 feature 分支改 `motifs.md` 併進來的。
- **具體失敗情境**：下一張票的 ui-designer 載入 brand skill，在 `references/motifs.md` 找不到「捲動列表末張卡裝飾件被 Body clip 完全裁掉＝既定視覺」這條；只有翻 `yec61` Notes 板才知道。清倉票的裁決沒進到會被自動載入的 skill 檔，下一輪很可能又被當成新缺陷重報一次（本票的 20 筆與 LS-96 `1d0b97c2` 的 17 筆，本身就是這個迴圈的第二次）。
- **建議**：`motifs.md` 補一條（規則＋量法＋指向 `yec61/Djv58` 的節點清單），或由 orchestrator 明示這條括號要求本票不做、記入 LS-96。

#### i-1（informational）R4 handoff 的「fontWeight 不影響座標／尺寸」推論不成立，是收據救回來的

- handoff 寫「本輪只改 `fontWeight` 與 Notes 文字，**不影響任何座標／尺寸**，數字不變即為預期結果、非漏跑」。`normal`→`600` 會改變字面量測寬度，理論上可能改變換行行數與整段高度——這句推論本身站不住。
- 真正讓結論成立的是**收據是改後重跑的**：r4 `tree_hash=c7917c6e09662b28` 我用 `scripts/gates/design_tree_hash.py` 對 head `.pen` 複算一致，代表六支跑的就是含新字重的那棵樹，`board_clip.flagged=0`／`text_occlusion.flagged=0` 是改後的量測值。
- 順帶一提：這次剛好不落在 VR R2 BL-A 指出的結構盲區——`VUAqK` 的 `xf0zM` 在 fit_content 的 `Field Help`（`D8o6M`）裡、不是固定高的 `Field Box`，而 `Body` 是 `fill_container` 掛在 `clip:true` 的板 root 下，真的長高會被 `board_clip` 抓到。往後這類「改字重／字級」的收工句建議直接寫「已重跑、tree_hash 對得上」，不要寫「不影響座標」。

#### i-2（informational）我沒重跑的部分（明講）

- 六支掃描需要 Pen 版面引擎，本次派工明令不開 Pencil MCP，所以我**沒有**獨立重算 `sibling_intersection`／`row_overflow`／`cross_parent_collision` 的數值，也沒重跑 `corner_anchor.ref_hits=746`。這三項採信 VR R3（comment `0703744e`）對 `be05441` 的獨立重跑，加上 r4 收據 tree_hash 對 head 快照複算一致（＝r4 的六支確實跑在 head 這棵樹上）。
- 同理，35 個 `height: undefined`（fit_content）的 root 解析高度沒辦法從 JSON 得出（含四張本輪搬家的 Notes 板與 `XhwG6`／`qtCd7`／`kHDk4`），所以 root 重疊／80pt 我只機械複算了 196 個有數值 bounds 的 root。這 35 張的**垂直**間距採信 VR R3 的 Pencil 量測（231 root 重疊 0、最小間距 80）。可獨立佐證的是：四張 Notes 板搬到 `y=96487` 後是**水平**排列（x=−3367／−2367／−1367，寬 900 → 彼此 100pt），與右邊 `UOWte` 欄（x=−367）也是 100pt，這幾組不依賴解析高度。

---

### 三、做得對、重做時不要洗掉

1. **範圍隔離做得乾淨**：210 張他票 root 保序逐字相同、`variables`／`themes` 未動、新板一律 append、`ADDED∪CHANGED` 恰等於收據 `boards`。設計票最容易出的「順手動到別人的板」在這裡是 0。
2. **R4 是真正的手術式修改**：`.pen` diff 只有 2 行，且兩行都對得上 VR R3 的 M-1 與 i-1。
3. **收據不再拿分類句掩蓋 root 重疊**：三支 flagged 共 59 筆，逐筆都有分類，且無一筆是 root 層——VR R1 BL-1 的病灶被機械地關掉了，不是靠文字改寫。
4. **Notes 的訂正鏈可稽核**：`xv1gb`（z-order → Body clip）、`Djv58`／`lyu44`（Feed → Body）、`vCp8T`／`EEgMu`（刪掉算不出來的數字）、`epDnW`（推導重寫）每一段都引了裁決它的 comment id。M1 之所以是 major 而不是 blocker，正因為這套機制本身是好的——只是漏了一個節點。

---

### 四、Verdict

**REQUEST_CHANGES**（M1 一項 major）。

M1 是單一 text 節點的 `content`，落地後可機械複驗（`Get("XhwG6", n => n.id==="N0AoFY")` 看 `content`），**不需要再開一輪 VR**；m1／m2 建議一併處理（或由 orchestrator 裁定記入 LS-96）。新 head 出來後我對新 head 重審 delta 並重貼 `merge-review` commit status。

依規約已對審的 head `03469850a69d27f72120b1618f7ced79479dcde1` 貼 `merge-review` = `failure`。

---

### 五、`handoff-evidence-check` 實跑（LS-211，貼文前）

`bash scripts/gates/handoff-evidence-check.sh <本 comment 暫存檔>` → **exit 0**，逐字：

```
✓ 第 13 行起的列項有證據
✓ 第 14 行起的列項有證據
✓ 第 15 行起的列項有證據
✓ 第 16 行起的列項有證據
✓ 第 17 行起的列項有證據
✓ 第 18 行起的列項有證據
✓ 第 19 行起的列項有證據
✓ 第 20 行起的列項有證據
✓ 第 21 行起的列項有證據
✓ 第 22 行起的列項有證據
✓ 第 23 行起的列項有證據
✓ 第 24 行起的列項有證據
✓ 第 25 行起的列項有證據
✓ 第 26 行起的列項有證據
✓ 第 27 行起的列項有證據
```

（首跑第 21 行紅：那是「三支 gate 實跑」的父列項，證據在其子列項上。已把 `bash scripts/gates/design-evidence-check.sh`／`design-notes-check.sh` 併進父列項敘述後轉綠——只補了本來就跑過的命令名，未改動任何判定敘述。）


### 2026-09-06T13:17:20.110Z · Chih-Lin Yeh · `6c76595c`

（09-06 21:16）orchestrator 裁定 merge-review R1（61e654ff）：M1／m1／m2 三項全部本輪修——m2 的 `references/` 是票文範圍 1 括號明文要求，屬 scope 內，不記池。已派 ui-designer R2 修正＋r5 收據＋handoff。

VR 不再開輪的理由：三處變更皆為 Notes 文字節點（N0AoFY 改寫、N1aeiU／jftM0 各補一句）與 brand skill markdown，不動任何已核可板（VR R3 `0703744e` APPROVE）的視覺；落地後可機械複驗（`Get("XhwG6", n => n.id==="N0AoFY")` 讀 content、兩支 design gate exit code、r5 三支硬指標）。r5 收據仍為必要（.pen 有變更）。新 head 出來後派 merge-reviewer 對 delta 重審 R2、重貼 commit status。i-1／i-2 處置由 designer handoff 逐條列出。使用者對範圍 6 Subtitle 兩版的裁決尚未回，本輪不碰 dxPvP／hHrIV。

### 2026-09-06T14:02:37.713Z · Chih-Lin Yeh · `d2b78b1e`

## ui-designer R5 handoff：merge-review comment `61e654ff` 1 major／2 minor 逐項修正

（2026-09-06 21:59 CST）

### Ticket
LS-208（設計 chore 小債清倉二），依 PR #349 merge-reviewer R1（comment `61e654ff`，REQUEST_CHANGES：1 major／2 minor／2 informational）修正。

### 已完成
- **M1（major）**：`design/littlesprout.pen` root `XhwG6`（設計語言 Tokens）本票新增節 `KI61A` → `Mv61K/N0AoFY` 的 content，由「✓ After（既有正確用法，全稿 96 處一致）」改為不帶數字的定性寫法「✓ After（既有正確用法，全稿 Imprint Caption／Lab Imprint 一致）」——與同節 `vCp8T` 已刪除 96 這個無法複算的數字一致，不再自相矛盾。
- **m1（minor）(a)**：`N1aeiU/WmQBj`（LS-163 Notes 段②，對應票文範圍 3）段末補一句：「LabeledTextField.swift 現行 API 只有 helpText: String? ＋ isError: Bool 兩個參數，最多渲染一列 help row；稿面 iBYUK／VUAqK（與既有 O1XC2Z）要求 Reply Line＋Help Line 兩行同時 enabled，需先把 LabeledTextField 擴充成雙列 help slot 才能對應。」
- **m1（minor）(b)**：`jftM0/mwv65`（LS-18 Notes 段①，對應票文範圍 8）段末補一句：「UOWte Footer 的『刪除帳號』鈕導向 DeleteAccountFlowView（無家庭模式）——停權中使用者沒有家庭，EUFuw 描述的家庭分支（唯一 owner 需轉移等）不適用於本板。」
- **m2（minor）**：`.claude/skills/little-sprout-brand/references/motifs.md` 「角托三段規則」節新增一條「末張卡角托完全隱藏＝既定視覺，非殘留裝飾（LS-208 範圍 1 裁決）」，含規則、量法（沿用 LS-201 VR R3 comment `1d0b97c2` 定義）、節點清單（`yXSht`/`MYxf1`、`a67Na`/`zfSO1`、`V4Ktak`/`bOKfx`、`lKoZG`/`uvL4p`、`A33la`/`Z9Xuy` 五板末張 Card Diary 及其四顆角托）、指向 `design/littlesprout.pen` Handoff Notes `yec61/Djv58` 的完整推導。格式比照 e270fbe（LS-201 R3 MN-B）慣例。
- PR #349 body 更新：變更摘要第 4 條補 R5 訂正說明、新增「merge-reviewer 修正（R1）」段、驗證方式加入 r5 收據與 `Get(N0AoFY)` 機械複驗、Design 段補 R5 額外變更清單。

### 已驗證（怎麼驗）
- **N0AoFY 讀回**：`Get("XhwG6", n => n.id==="N0AoFY")` → `content = "✓ After（既有正確用法，全稿 Imprint Caption／Lab Imprint 一致）"`，與落地後 commit `8aa7e18` 的 `design/littlesprout.pen` 一致（同一顆 commit 也是 `design/evidence/LS-208-r5-overflow.json` 的 `head_sha`）。
- **兩支 gate（`bash scripts/gates/design-evidence-check.sh`／`bash scripts/gates/design-notes-check.sh`）**：
  - `bash scripts/gates/design-evidence-check.sh design/littlesprout.pen --ticket LS-208 --base origin/development` → **exit 0**，五份收據（r1–r5）皆通過，r5 標「head_sha=8aa7e18（本輪最新）」。
  - `bash scripts/gates/design-notes-check.sh design/littlesprout.pen --base origin/development` → **exit 0**，Notes 板 8 塊、head id 9899、本 PR 範圍曾存在而 head 已無的 id 144、沿革引用 48、缺失 0、署名 NBSP 違規 0（舊債 0）。
- **r5 溢出掃描收據三支硬指標**（`design/evidence/LS-208-r5-overflow.json`）：`corner_anchor` containers=6/points=48/**mismatch=0**（document 225/1784/0，ref_hits=746，unresolved=3/14，皆為 XhwG6 既有 3 個 Icon Mount 對，與 `design/evidence/LS-208-r4-overflow.json` 完全相同）；`text_occlusion.flagged=0`／`board_clip.flagged=0`（document 皆 0）。
- **sibling_intersection／row_overflow／cross_parent_collision 逐板分批獨立重跑**（因 9899 節點級稿件單次 execute 跑 O(n²) 掃描會間歇 `InternalError: interrupted`，改用 8→13 個 board 子集分批跑，逐批加總）：document_count 分別為 **2066／560／321**，與 `design/evidence/LS-208-r4-overflow.json` 的對應 `document_count` 逐一相同；且逐批核對確認 `XhwG6`／`N1aeiU`／`jftM0`（本輪唯一改動的三個節點所在板）在三支掃描中皆貢獻 0 筆——證實本輪純文字內容變更未引入任何新溢出/碰撞。`design/evidence/LS-208-r5-overflow.json` 沿用 r4 的分類清單（因底層原始資料逐位元相同，只是換算方式不同：r4 是完整 JSON 存檔、我方是重新分批驗證同一份資料）。
- **tree_hash 對帳**：`SUMMARY-HASH`（分 9 段跑，因單次 execute 對 9899 節點雜湊也會間歇性 interrupted）加總＝`a306b88ce44f7bf8`；落地後 `python3 scripts/gates/design_tree_hash.py design/littlesprout.pen` 對 head `8aa7e18` 快照重算同值——兩種算法交叉驗證一致。
- **落地**：`bash scripts/ops/pen-land.sh ... --expect-nodes 9899 --after 1788701509 --marker '61e654ff'`（marker 命中 backup 內容且不在落地檔中，覆蓋 mtime 快篩）→ 通過，結構 diff 只有 3 個節點的 `content` 屬性變更（`N0AoFY`／`WmQBj`／`mwv65`），節點總數落地檔 9899→backup 9899 不變，`design-landing-check.sh` 驗證通過。

### 未完成
- **i-1（merge-review informational）**：R4 handoff「fontWeight 不影響座標／尺寸」推論措辭不嚴謹的提醒——**已修**：本輪 handoff（此文件）改用「已重跑、tree_hash 對得上」的寫法描述驗證結論，不再宣稱「不影響座標」。
- **i-2（merge-review informational）**：merge-reviewer 未獨立重跑 sibling_intersection／row_overflow／cross_parent_collision 三支、只採信 VR R3 獨立重跑值——**已修**：本輪（R5）已對這三支做逐板分批的獨立重跑（見上「已驗證」段），document_count 與 r4 收據完全一致，不再只是「採信」，是本輪自己重新算過。

### 風險
- 無新增風險。本輪只改動 3 個既有 text 節點的 `content` 屬性與 1 個 markdown 檔案新增段落，未動任何座標／尺寸／樣式屬性，六支掃描逐一確認 0 新增溢出/碰撞。

### 產出位置
- `.pen` 提交：`8aa7e188cd1f3a6c5439db67d8e8a03436ec42b1`（`fix(design): LS-208 R5 依 merge-review comment 61e654ff 逐項修正`）
- 收據提交：`c1206262402f965472aa4d83f9bbb203e164dc07`（`chore(design): LS-208 R5 溢出掃描收據`）
- PR：https://github.com/CLYEH/little-sprout/pull/349（body 已更新至 R5）
- 收據檔：`design/evidence/LS-208-r5-overflow.json`

### Pen 路徑（LS-91／LS-180）
- 開工核對：`get_app_state()` 回傳 active 文件路徑＝`/Users/clyeh/little-sprout/.claude/worktrees/LS-208/design/littlesprout.pen`，與 `git rev-parse --show-toplevel`（本 worktree）+ `/design/littlesprout.pen` 相符，無需回報路徑錯誤。
- 收工：Pen 仍停在同一票檔路徑（`/Users/clyeh/little-sprout/.claude/worktrees/LS-208/design/littlesprout.pen`），未切檔、未關閉，本輪未呼叫任何 `pen-open.sh`，故無「⚠ Pencil MCP 需重連」訊息需回報。

### 尺寸類（width／height）token 規格值族
- 本輪無新增或修改任何 `width`／`height` 字面值——三處改動皆為 text 節點的 `content` 屬性，母題／版面座標一律未動。無硬寫清單需登記。

### 溢出掃描收據
- 路徑：`design/evidence/LS-208-r5-overflow.json`
- `ticket=LS-208`、`round=5`、`head_sha=8aa7e188cd1f3a6c5439db67d8e8a03436ec42b1`、`total_nodes=9899`、`tree_hash=a306b88ce44f7bf8`、`scan_scope=document`
- 六支：`sibling_intersection`（document_count=2066，flagged 29 類代表，沿用 r4 分類）／`row_overflow`（document_count=560，flagged 22 類代表，沿用 r4 分類）／`cross_parent_collision`（document_count=321，flagged 8 類代表，沿用 r4 分類）／`corner_anchor`（containers=6／points=48／mismatch=0；document 225/1784/0；ref_hits=746；unresolved=3/14，皆 `XhwG6` 既有 Icon Mount 對，`classification: mount_pair`）／`text_occlusion`（flagged=0／document_flagged=0）／`board_clip`（flagged=0／document_flagged=0）。
- SUMMARY 行（分兩趟，9899 節點級單次執行會間歇 interrupted）：
  - 第一趟（雜湊，9 段分批加總）：`SUMMARY-HASH total_nodes=9899 tree_hash=a306b88ce44f7bf8`
  - 第二趟（六支，因同樣間歇 interrupted 改分函式＋分板批次跑：PASS-A sibling_intersection/row_overflow 全量一次成功；PASS-B1 corner_anchor、PASS-B2 text_occlusion/board_clip 全量一次成功；PASS-C cross_parent_collision 拆 8→13 個 board 子集分批加總）：合計 `sibling_intersection=2066 row_overflow=560 cross_parent_collision=321 corner_anchor=6/48/0 document=225/1784/0 ref_hits=746 text_occlusion=0/0 board_clip=0/0`，與 `design/evidence/LS-208-r4-overflow.json` 的對應 `document_count`／計數逐一相同。

### 給 ios-dev 的實作註記
- 本輪未新增畫面／狀態，只是既有 Notes 段落補實作對應說明（見上「已完成」m1(a)(b)），無新增 spacing／字級／色彩變數。

### Skill 影響了哪些取捨
- **frontend-design skill 未載入**：本 session 呼叫 `Skill({skill:"frontend-design:frontend-design"})` 與 `Skill({skill:"frontend-design"})` 皆回傳「Unknown skill」，本環境可用 skill 清單（system-reminder 列出的 available-skills）中不存在此名稱的 skill，非我方跳過。本輪任務範圍僅為文字內容訂正（3 個 text content＋1 個 markdown 段落），未涉及新畫面／版面設計，即使該 skill 可用，實質取捨空間也有限。
- **little-sprout-brand skill 已載入**：載入內容驅動了 m2 的落筆位置與寫法——十條不可協商第 4 條「角托三段規則」是既有規則家族，本輪新增的「末張卡角托完全隱藏＝既定視覺」條目因此被放進 `motifs.md` 的「角托三段規則」章節（緊接在既有角托規則之後、「紙面基底可以借給非照片元件」小節之前），格式比照該檔既有條目（粗體導語＋規則＋量法＋節點清單＋指向 Notes 的完整推導連結），而非另開新章節——這是 skill 內「使用方式與更新」一節「動了…母題規則的設計票，同一 PR 更新本 skill」的直接落實。

### 2026-09-06T14:25:23.988Z · Chih-Lin Yeh · `e1e5157f`

## merge-reviewer R2（設計 PR）：REQUEST_CHANGES（1 major／1 minor／3 informational）

（2026-09-06 22:16 CST）

**審的 head**：`c1206262402f965472aa4d83f9bbb203e164dc07`（PR #349，`feature/LS-208-design-debt-cleanup-2` → `development`）；開審（22:03）與收工（22:22）各查一次 `gh pr view 349 --json headRefOid`，皆同值——審查期間 head 未前進。
**Base**：`origin/development` `e3f566d`，merge-base `ba81f8f`。**Delta**：`git diff 0346985..c120626`（R1 審的 head → 本輪 head），只審 delta＋R1 三項是否落地。
**Pen**：全程未開 Pencil MCP（依派工指示，Pen 停在 LS-208 票檔未動），`.pen` 一律用 python 讀明文 JSON。**DB lock**：未持有（本票無 DB 測試，未跑任何本機容器命令）。**模擬器**：未開，未改 `simctl ui`。

### 一、逐項查實（怎麼驗）

- **delta 只動 3 檔**：`git diff --name-status 0346985..c120626` → `M .claude/skills/little-sprout-brand/references/motifs.md`／`A design/evidence/LS-208-r5-overflow.json`／`M design/littlesprout.pen`；`git diff --stat 8aa7e18 c120626 -- design/littlesprout.pen` 為空，head 的 `.pen` 就是 `8aa7e18` 那份。
- **他票 root 逐字相同（R1 head → 本輪 head）**：`git show 8aa7e18:design/littlesprout.pen` 存成 `scratchpad/LS-208-mr/head2.pen.json`，與 R1 留下的 `scratchpad/LS-208-mr/head.pen.json` 比 → 兩邊皆 231 root，ADDED 0／REMOVED 0／CHANGED 3（`N1aeiU`／`XhwG6`／`jftM0`），其餘 228 張用**保序** `json.dumps`（不 sort_keys）逐字比對 mismatch 0，root 順序完全一致；`version`／`themes`／`variables`／`fileToken` 四個頂層鍵逐字相同（token 未動）。
- **對 base 的全稿保序比對仍成立**：先用 `shasum -a 256` 確認 R1 的 `scratchpad/LS-208-mr/base.pen.json` 與 `git show ba81f8f:design/littlesprout.pen` 同雜湊（`a4fafa88…`），再比 → ADDED 12／REMOVED 1（`rh2Q1`）／CHANGED 9／UNCHANGED 210（保序 mismatch 0），且 `set(ADDED)|set(CHANGED)` 恰等於 `design/evidence/LS-208-r5-overflow.json` 的 `boards`（21 張），三張本輪動到的板都在其中。
- **屬性層 diff：只有 3 個 `content`**：python 逐節點比 `head.pen.json` 與 `head2.pen.json` 的三張板 → 各 ADD=0／REM=0／ATTR-CHG=1，變的分別是 `XhwG6/N0AoFY`、`N1aeiU/WmQBj`、`jftM0/mwv65` 的 `content`；後兩者是 pure append（python `new.startswith(old)` 為 True），三個節點的 `fontSize` 17／`fill` `#2B141C`／`lineHeight` 1.6／`fontWeight` `normal` 等屬性零變更，無任何座標／尺寸屬性被動到。
- **M1 已落地**：`XhwG6/N0AoFY` 現值 `✓ After（既有正確用法，全稿 Imprint Caption／Lab Imprint 一致）`（python 直接讀 `head2.pen.json` 的節點 `content`，不經 Pencil）。
- **「96」活斷言歸零**：對 `scratchpad/LS-208-mr/head2.pen.json` 全稿 3567 個 text 節點 grep `96` → 40 個節點命中，逐筆看前後 60 字上下文：唯一與「Imprint 用量」有關的是 `XhwG6/vCp8T` 的沿革句（`原句「96 筆」…故刪除此數字只留結論`），其餘全是 `LS-96` 票號、`#BC969D` 色碼、`96×96`／固定寬 96 等尺寸、`1608.96` 這類座標。無任何活的「96 處／96 筆」斷言。
- **m1 兩句的原文與位置**：`N1aeiU/WmQBj` 段末新增「【R5 訂正，依 merge-review comment 61e654ff m1】LabeledTextField.swift 現行 API 只有 helpText: String? ＋ isError: Bool 兩個參數，最多渲染一列 help row；…需先把 LabeledTextField 擴充成雙列 help slot 才能對應。」；`jftM0/mwv65` 段末新增「…UOWte Footer 的『刪除帳號』鈕導向 DeleteAccountFlowView（無家庭模式）——停權中使用者沒有家庭，EUFuw 描述的家庭分支（唯一 owner 需轉移等）不適用於本板。」兩句都併進既有 Notes 文字節點，沒有新增節點，字級／顏色 token 因此與同段完全一致。
- **m1(a) 的技術敘述屬實**：讀 `LittleSprout/DesignSystem/LabeledTextField.swift` → 參數確為 `helpText: String?`＋`isError = false`，`body` 內只有一段 `if let helpText { … }` 單列 help row，icon 寫死 `exclamationmark.circle.fill`、色寫死 `Color.lsDanger`。
- **m1(b) 指向的檔案存在**：`git grep -l DeleteAccountFlowView -- '*.swift'` → `LittleSprout/Features/Settings/DeleteAccountFlowView.swift`（LS-188 建的 22 行佔位，LS-24 才實作；註記是給未來實作者的前瞻規格，不是現況描述，這點合票文範圍 8 的要求）。
- **m2 的落點與格式**：`git diff 0346985..c120626 -- .claude/skills/little-sprout-brand/references/motifs.md` 只有 +1 行，插在「角托三段規則」節末（第 35 行），採粗體導語＋規則＋**量法**＋**裁決**＋節點清單＋指向 `yec61/Djv58` 的寫法，與 `git show e270fbe -- …/motifs.md`（LS-201 R3 MN-B）同型；SKILL.md 未動（該檔角托速查沒有本條對應的數字，不像 e270fbe 那次需要同步）。
- **m2 引用的 id 全部活著**：新條列的 16 個 id（5 張板 `yXSht`／`a67Na`／`V4Ktak`／`lKoZG`／`A33la`、5 個 instance `MYxf1`／`zfSO1`／`bOKfx`／`uvL4p`／`Z9Xuy`、4 顆角托 `DjW91`／`JgUK8`／`mlORb`／`atAkU`、`yec61`／`Djv58`）在 `scratchpad/LS-208-mr/head2.pen.json` 建 id 索引後逐一找得到，無死 id。
- **m2 的 clip 敘述屬實**：對 `scratchpad/LS-208-mr/head2.pen.json` 逐板讀 5 張板的 `Body`／`Feed` → `Body`（`n7gPAe`／`x5Vmg`／`Q600X`／`YulfE`／`o73UY`）皆 `clip: true`、`height: fill_container`，`Feed` 皆無 `clip` 鍵——「裁切者是 Body 不是 Feed」這句對。
- **收據 tree_hash／head_sha**：`python3 scripts/gates/design_tree_hash.py design/littlesprout.pen`（worktree 的 .pen＝`8aa7e18` 那份）→ `a306b88ce44f7bf8`，與 `design/evidence/LS-208-r5-overflow.json` 的 `tree_hash` 逐字相同；`head_sha=8aa7e188cd1f3a6c5439db67d8e8a03436ec42b1`＝本 PR 最後一次觸碰 `.pen` 的 commit（`git log --oneline` 顯示其後只有收據 commit `c120626`）；`total_nodes=9899`、`scan_scope=document`。
- **三支硬指標對本票 21 板為 0**：`design/evidence/LS-208-r5-overflow.json` → `corner_anchor.mismatch=0`／`document_mismatch=0`（`containers=6`／`points=48`／`ref_hits=746`／`unresolved` 3 筆皆 `XhwG6` 既有 Icon Mount 對、帶 `classification: mount_pair`）、`text_occlusion.flagged=[]`／`document_flagged=[]`、`board_clip.flagged=[]`／`document_flagged=[]`。
- **r5 與 r4 同型（逐鍵 shape diff）**：python 遞迴列出兩份收據的鍵路徑集合 → 只差兩處：r4 多一個頂層 `scan_note`（r5 沒有）、r5 的 `corner_anchor.unresolved[]` 多一個 `board_name`；六支 scan 皆帶 `scope: document`＋`document_count`，欄位名與 `scripts/design/overflow-scan.js` 一致（收據本來就沒有 `command` 欄，`scripts/gates/design-evidence-check.sh` 檔頭列的必填欄位裡也沒有這一項）。
- **收據內部一致性**：python 讀 `design/evidence/LS-208-r5-overflow.json`，每支 flagged 的 classification 內「同類 N 例」加總＝該支 `document_count`——`sibling_intersection` 29 筆代表加總 2066＝2066、`row_overflow` 22 筆加總 560＝560、`cross_parent_collision` 8 筆加總 321＝321，沒有一筆代表缺計數。
- **flagged 清單與本輪三張板無關**：把 `design/evidence/LS-208-r5-overflow.json` 三支的全部 59 筆代表序列化後 grep `XhwG6`／`N1aeiU`／`jftM0`／`Tokens`／`Handoff` → 0 命中，與「本輪三個節點貢獻 0 筆」的申報不衝突（限制見第三節）。
- **幾何風險自算（不靠 Pen）**：對 `scratchpad/LS-208-mr/head2.pen.json` 讀 root 屬性：三張板都沒有 `height` 鍵（fit_content），文字加長只會往下長；用 root 的 `x`／`y`／`width` 算水平重疊後，`jftM0` 之下最近的重疊 root 是 `EzJCV`（dy=55463），`XhwG6` 之下是 `N1aeiU`／`b3nzDp`（dy=98137），`N1aeiU` 之下沒有任何水平重疊的 root；再以 17pt／`lineHeight` 1.6／內寬 812 估三板文字堆疊高度（jftM0 約 32.8k、N1aeiU 約 9.2k、XhwG6 約 11.6k），追加 2–3 行（約 55–80pt）不可能吃掉 5 萬 pt 以上的餘裕——本輪不可能造成新的 root 重疊或板距退步。
- **兩支 design gate（`bash scripts/gates/design-evidence-check.sh`／`bash scripts/gates/design-notes-check.sh`）實跑（worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-208`，唯讀）**：
  - `bash scripts/gates/design-evidence-check.sh design/littlesprout.pen --ticket LS-208 --base origin/development` → **exit 0**，末行：`✓ design-evidence gate 通過：design/evidence/LS-208-r5-overflow.json（head_sha=8aa7e18（本輪最新），total_nodes=9899，六支掃描皆有輸出…、corner_anchor.mismatch=0、boards 覆蓋本 PR 觸碰的板、tree_hash 對應 head_sha 快照、text_occlusion.flagged 為空、board_clip.flagged 為空、scan_scope=document、六支各帶 scope／document_count）`。
  - `bash scripts/gates/design-notes-check.sh design/littlesprout.pen --base origin/development` → **exit 0**，末行：`✓ design-notes gate 通過：Notes 板 8 塊、head id 9899、本 PR 範圍曾存在而 head 已無的 id 144、沿革引用 48、缺失 0、署名 NBSP 違規 0（舊債 0）`。
- **PR body 已更新且過 gate**：`gh pr view 349 --json body -q .body` 存成 `scratchpad/LS-208-pr-body-r2.md`，`bash scripts/gates/pr-body-check.sh <該檔> --branch feature/LS-208-design-debt-cleanup-2 --verify` → **exit 0**（`✓ PR body 檔頭段含本票票號 LS-208`／`✓ 申報反查：LS-96 行 1 條`；無 `LINEAR_API_KEY` 故 `1d0b97c2` 只驗格式）。內容對帳：變更摘要第 4 條寫了 M1 訂正、第 1 條寫了 motifs.md、第 3／8 條寫了 m1 兩句，另有「merge-reviewer 修正（R1）」整段與 r5 收據列項（含「沿用 r4 分類」的自述）。
- **CI 五支全綠且綁對 SHA**：`gh pr checks 349 --json name,bucket,state` → `ci`／`ci-ipad`／`db`／`lint`／`rules` 皆 `pass/SUCCESS`；`gh run view 34037662425 --json headSha` → `c1206262402f965472aa4d83f9bbb203e164dc07`，跑的就是本輪 head。

### 二、R1 三項落地判定

- **M1（major）→ 已落地、關閉**：`Mv61K/N0AoFY` 的 `content` 改成不帶數字的定性寫法，與同節 `vCp8T` 的沿革句不再自相矛盾；全稿再無「96 處／96 筆」的活斷言。
- **m1（minor）→ 已落地**：兩句都在，且都是併進既有 Notes 文字節點的 pure append（沒有新增節點、沒有動字級／顏色 token）；`LabeledTextField` 只渲染一列 help row、`DeleteAccountFlowView` 檔案存在，兩句敘述對照原始碼屬實。
- **m2（minor）→ 形式落地、內容有誤，判未達成**：motifs.md 確實補了一條、落在「角托三段規則」節、格式比照 `e270fbe`、引用的 id 全部活著——但這條規則把角托掛在錯誤的元件上（M-A），是本項要買的「下一輪不必再量」買不到的那種錯。
- **i-1／i-2**：designer handoff「未完成」欄逐條列了處置。i-1（別再寫「不影響座標」、改寫成「已重跑、tree_hash 對得上」）我認可，本輪 handoff 的措辭確實改了。i-2（三支不再只採信 VR、本輪自己重跑）的重跑方式見第三節裁定。

### 三、r5 收據來源裁定：可接受（informational＋harness 缺口），不列 major

**裁定**：依 CLAUDE.md「整份收據須來自對 head 快照的同一次掃描」與派工給的判準（tree_hash 對得上、各段加總與 r4 逐欄相符且可重現 → 可接受），我判**可接受**。理由四條：

1. `tree_hash` 我用正典 gate 腳本獨立複算一致（`a306b88ce44f7bf8`）——收據對應的是 head `8aa7e18` 這棵樹，不是 r4 那棵（r4 是 `c7917c6e09662b28`）；`head_sha` 也正是最後一次觸碰 `.pen` 的 commit。
2. 六支欄位與 r4 逐欄相符、齊備（`scope`／`document_count`／`scan_scope`），三支硬指標（corner_anchor.mismatch／text_occlusion.flagged／board_clip.flagged，含各自 document 版）皆 0。
3. 三支 O(n²) 的 flagged 清單確實沿用 r4（designer 自述，我也機械確認 canonical-JSON 逐字相同）——但本輪 delta 只有 3 個 text `content`、零座標／尺寸屬性變動，三張板都是 fit_content 且下方餘裕 ≥55,463pt，flagged 集合在幾何上不可能改變。
4. 「分段跑」本身是正典腳本明文支援的模式（`overflow-scan.js` 檔頭：`SCAN_HASH_ONLY`／`SCAN_SKIP_HASH`，「8000 節點級的稿一次 execute 跑完雜湊＋六支會 `InternalError: interrupted`」，LS-171 定調「同一稿態、中間無寫入的連續兩次唯讀 execute，`tree_hash` 抄第一次」）；而收據 JSON 本來就是設計端依 Print 的分類彙整段**手寫**的（同檔頭明文），「手工組裝」在本 repo 不等於造假。

**限制（我沒辦法獨立確認的部分，明講）**：

- 「PASS-C 把 `cross_parent_collision` 拆 8→13 個 board 子集加總」不是正典腳本支援的模式（腳本只有 `SCAN_SCOPE="boards"` 的整體限縮，而 LS-120 R3／R4 的逐板繞過正是 `document_*` 塌縮成 boards 值的事故來源）。分批會漏掉跨批次的板與板之間的碰撞；旁證是 R1 已量過 196 個有數值 bounds 的 root 兩兩相交 0 對、本輪三張板下方餘裕極大——但這是旁證，不是「重跑一次」。
- flagged 清單沿用 r4，若真有「集合變了、總數沒變」的情形，收據看不出來；`tree_hash` 對這一類是盲的（`design-evidence-check.sh` 檔頭自述「不證明各支掃描的數字算對」）。

**harness 缺口（建議記 LS-96）**：正典腳本對 9899 節點級文件的三支 O(n²) 掃描沒有自帶分批＋加總（只有 hash／scans 兩段旗標），設計端只能手工切 board 子集、手工加總，收據因此天然無法宣稱「同一次掃描」。建議 `scripts/design/overflow-scan.js` 增加單支掃描的分批執行與可加總中間結果（含批次清單、跨批次配對如何處理），讓大文件也產得出單一來源的收據；同時考慮讓收據帶「每支掃描結果的雜湊」，讓「沿用舊清單」這件事也變成機械可偵測。

### 四、Findings

#### M-A（major）motifs.md 新增的角托裁決把元件認錯：`cmp/Card Diary` 根本沒有角托，列出的 id 是 `cmp/Card Album` 的實例，而且不是「末張卡」

**位置**：`.claude/skills/little-sprout-brand/references/motifs.md:35`（本 delta 新增的整條）。同一段文字的來源是 `design/littlesprout.pen` 的 `yec61/Djv58`（本 PR 早輪產出、不在本 delta 內——R1 與 VR R3 都沒抓到，我補記）。

**事實（皆機械可驗）**：

- `cmp/Card Diary`（root `qtCd7`）的完整子樹是 `Snippet`／`Photos Row`／`Sign-off Rule`／`Signature`，**沒有任何角托節點**；條文點名的四顆角托 `DjW91`／`JgUK8`／`mlORb`／`atAkU`（`ref → GEBcf`）在 `cmp/Card Album`（root `bhroo`）裡面。
- 條文列的 5 個 instance `MYxf1`／`zfSO1`／`bOKfx`／`uvL4p`／`Z9Xuy` 的 `ref` 全部是 `bhroo`、`name` 全部是 `Card Album 1`。
- 它們也**不是**該板 Feed 的最後一張卡：5 張板最後一個 `Cards` 群的子節點順序都是 `[Card Album 1, Card Diary 2]`（`c2pJ7R`／`rGi9s`／`FIeHM`／`gw8vW`／`P47Wf`，`ref → qtCd7`，皆未停用），Album 在上、Diary 在下。

**具體失敗情境**：下一張設計票的 ui-designer／visual-reviewer 載入 brand skill，讀到「Feed 內最後一張 `cmp/Card Diary` 的四顆角托＝既定視覺、此為終局裁決、之後不需再重新量測討論」，翻開末張卡（Card Diary）卻找不到任何角托——兩條路都壞：(a) 判定角托在某輪被誤刪，於是「補回」四顆角托到 `cmp/Card Diary` 上，正面違反同一份檔案第 4 條「角托三段規則」（角托只給沖印品，Card Diary 是零角托卡）；(b) 判定這條規則對不上現稿，重新量一次——而「不必再量」正是 m2 這條要買到的唯一價值，等於白補。這也是同一個迴圈（LS-96 `1d0b97c2` 的 17 筆→本票重量成 20 筆）的第三次。

**建議修法**：條文與節點清單的 `cmp/Card Diary` 改成 `cmp/Card Album`（`bhroo`），「最後一張」改成「Feed 內最後一張**帶角托的**卡（`cmp/Card Album`；其後還有一張 `cmp/Card Diary`，本身零角托）」。這是 markdown 單行修正，不動 `.pen`、不需要新收據、不需要新 VR 輪。`.pen` `yec61/Djv58` 有同一處錯誤：若一併訂正，是一個 text `content` 改動＋r6 收據；若 orchestrator 裁定只修 skill 檔、Djv58 記入 LS-96 也合理——但兩份不能各說各話。

#### m-A（minor）r5 收據沒有 `scan_note`，「分批跑＋沿用 r4 分類」的來源說明只活在 PR body 與 Linear handoff

**位置**：`design/evidence/LS-208-r5-overflow.json`（頂層缺 `scan_note`；`design/evidence/LS-208-r4-overflow.json` 有）。
**具體失敗情境**：收據是要進版控、日後被稽核的成品。之後有人單看 r5 這份 JSON，會合理假設六支是同一次全稿掃描的輸出；實際上三支 O(n²) 的 flagged 清單來自 r4、計數來自分批加總。r4 自己就用 `scan_note` 記了「三支數字與 R3 相同」的理由，r5 反而把這欄拿掉，可稽核性倒退——而 gate 不驗這欄，純靠人。
**建議**：r5 補 `scan_note`，寫清楚哪幾支全量單跑、哪一支分批加總（`cross_parent_collision` 13 個 board 子集）、flagged 分類沿用 r4 的事實與理由。改這個欄位不影響 `tree_hash`（不在雜湊範圍內），gate 仍綠。

#### i-1（informational）r5 的 `corner_anchor.unresolved[]` 才是正典格式，r4 那份是舊寫法

`scripts/design/overflow-scan.js` 第 379 行的 entry base 是 `{container, container_name, board, board_name, corners}`——`board` 放板 id、`board_name` 放板名。r5 正是這個形狀（`board: "XhwG6"`＋`board_name: "設計語言 v3.3 …"`），r4 把板名塞在 `board`、沒有 `board_name`。兩份都過 gate（這層沒被驗），r5 是往正確方向修，記錄備查、不必回改 r4。

#### i-2（informational）「即『Tab Bar 全字級純 icon』節驗收判準列出的同一批板」——那節列的是 6 板

那節（`motifs.md` 第 93 行起）的逐板數字涵蓋 `yXSht`／`a67Na`／`V4Ktak`／`A33la`／`lKoZG`／`HLXo3` 六板，本條裁決只涵蓋 5 板。差的 `HLXo3` 我查過：LS-120 R6 修 BL-9 時刪掉了它的 Card Album 實例 `CFlP7`，現在 `HLXo3` 底下 `ref=bhroo` 的實例數為 0（兩個 `Cards` 群各只有一張 Card Diary），本來就不可能有這 4 筆——結論沒錯，只是「同一批板」這個借代會讓下一個人來問。修 M-A 時順手加半句（「`HLXo3` 的 Card Album 已於 LS-120 R6 刪除，故不在此列」）即可。

#### i-3（informational）PR body 的自述比收據誠實，這個方向是對的

PR body「驗證方式」段主動寫了「三支 O(n²) 掃描逐板分批獨立重跑，document_count 2066/560/321 與 r4 逐位元相同，沿用 r4 分類」——這種主動揭露正是我判 r5 可接受的關鍵之一（若 body 只寫「六支全跑、0 flagged」，同樣的收據我會判得嚴很多）。m-A 要的只是把這句話搬進收據本身。

### 五、做得對、重做時不要洗掉

1. **手術式到極致**：delta 三個檔、`.pen` 只有 3 個 text `content`、零節點增刪、零座標／尺寸屬性、228 張他票 root 保序逐字相同、四個頂層鍵未動。設計票最容易出的「順手動到別人的板」在這裡是 0。
2. **兩句 m1 都併進既有 Notes 節點**而不是新增節點——字級／顏色 token 一致這件事因此是結構上保證的，不必靠人核對。
3. **motifs.md 的寫法本身是對的**（粗體導語＋規則＋量法＋裁決＋節點清單＋指向 Notes 的完整推導，比照 `e270fbe`），M-A 是內容認錯，不是格式或方向錯——修好之後這條就是這個迴圈的終點。

### 六、我沒看／沒重跑的（明講）

- 六支掃描的數值本身：需要 Pen 版面引擎，本次派工明令不開 Pencil MCP，所以我沒有獨立重算任何一支的數字。我做的是第一節列的間接查核（tree_hash 複算、欄位對帳、內部加總一致、flagged 與本輪三張板無關、幾何餘裕估算）。
- 35 個 `height: undefined`（fit_content）root 的解析高度從 JSON 得不出來，「板距 80pt」這條沿用 R1／VR R3 的結論，本輪只確認 delta 不可能讓它退步。
- 本票 R1–R4 的設計內容（12 張新板的版面、文案、AX3）不在本輪範圍，沿用 VR R3 `0703744e` APPROVE 與 merge-review R1 `61e654ff`。
- 未開模擬器、未跑 `xcodebuild`、未跑任何 `supabase`／`docker` 命令；**DB lock 未持有**（本票無 DB 測試）。

### 七、Verdict

**REQUEST_CHANGES**（M-A 一項 major／m-A 一項 minor／3 informational）。

M1、m1 兩項落地無虞；m2 形式落地但把角托掛在錯誤的元件上，這正是這條規則要防的事，且只需 markdown 單行修正（不動 `.pen`、不需要新收據、不需要新 VR 輪）。r5 收據來源裁定為**可接受**（informational＋harness 缺口），不構成 major。新 head 出來後我對新 head 重審 delta 並重貼 `merge-review` commit status。

依規約已對審的 head `c1206262402f965472aa4d83f9bbb203e164dc07` 貼 `merge-review` = `failure`。

### 八、`handoff-evidence-check` 實跑（LS-211，貼文前）

`bash scripts/gates/handoff-evidence-check.sh <本 comment 暫存檔>` → **exit 0**，逐字：

```
✓ 第 11 行起的列項有證據
✓ 第 12 行起的列項有證據
✓ 第 13 行起的列項有證據
✓ 第 14 行起的列項有證據
✓ 第 15 行起的列項有證據
✓ 第 16 行起的列項有證據
✓ 第 17 行起的列項有證據
✓ 第 18 行起的列項有證據
✓ 第 19 行起的列項有證據
✓ 第 20 行起的列項有證據
✓ 第 21 行起的列項有證據
✓ 第 22 行起的列項有證據
✓ 第 23 行起的列項有證據
✓ 第 24 行起的列項有證據
✓ 第 25 行起的列項有證據
✓ 第 26 行起的列項有證據
✓ 第 27 行起的列項有證據
✓ 第 28 行起的列項有證據
✓ 第 29 行起的列項有證據
✓ 第 30 行起的列項有證據
✓ 第 31 行起的列項有證據
✓ 第 32 行起的列項有證據
✓ 第 33 行起的列項有證據
```

### 2026-09-06T14:27:46.387Z · Chih-Lin Yeh · `0ed81578`

（09-06 22:27）orchestrator 裁定 merge-review R2（e1e5157f）：M-A 兩處都修——motifs.md:35 該條與 `.pen` `yec61/Djv58` 同一處元件認錯（角托屬 `cmp/Card Album` `bhroo`，非 `cmp/Card Diary` `qtCd7`；5 個 instance 皆 Card Album 1、非末張卡），Notes 是稽核鏈的一環，留錯會讓下一張票再量一次，所以 Djv58 用沿革寫法一併訂正；m-A r6 補 `scan_note`。已派 ui-designer R3（要求先用 execute 獨立核對子樹與 instance ref，再改字）→ 新 head 出來後派 merge-reviewer R3 對 delta 重審。VR 仍不開輪（理由同 21:16 裁定：只動 Notes 文字與 markdown）。

r5 收據「分段跑再加總」採 reviewer 裁定：可接受（tree_hash 對 head、三硬指標 0、六支加總逐欄等於 document_count）；harness 缺口記入 LS-96：正典腳本對 9899 節點級文件三支 O(n²) 掃描缺自帶分批＋加總，收據應帶每支掃描結果雜湊。

### 2026-09-06T15:09:06.370Z · Chih-Lin Yeh · `4f9509bf`

## ui-designer R6 handoff：處理 merge-review R2（comment e1e5157f，1 major／1 minor／3 informational）

（2026-09-06 23:05 CST）

**Ticket**：LS-208。**PR**：#349（`feature/LS-208-design-debt-cleanup-2` → `development`）。

### 已完成

1. **M-A（major）motifs.md＋`.pen` 角托元件歸屬訂正**：`.claude/skills/little-sprout-brand/references/motifs.md` 角托三段規則末條把「Feed 內最後一張帶角托的卡」的元件名、角托歸屬、5 個 instance 節點清單、「末張卡」描述全部訂正為 `cmp/Card Album`（原句誤植 `cmp/Card Diary`），並補上 `HLXo3` 半句說明（其 Card Album 已於 LS-120 R6 刪除，故不在此列）。`.pen` `yec61/Djv58` 用沿革句訂正（保留原句，附加「【R3 訂正，依 merge-review e1e5157f M-A】…」），不新增節點、不動字級／顏色 token。
2. **獨立核對**（訂正前，用 execute 自己核對，不照抄 reviewer 結論）：
   - `Get("qtCd7", {depth:3})` 讀回：`qtCd7`（`cmp/Card Diary`）子樹＝`["Snippet(p2odx)","Photos Row(lEgJp)","Sign-off Rule(u59KAY)","Signature(Jqwww)"]`——零角托。
   - `Get("bhroo", {depth:2})` 讀回：`bhroo`（`cmp/Card Album`）子樹含 `Photo Print(r6cYjT)`；走訪確認四顆角托 `DjW91`/`JgUK8`/`mlORb`/`atAkU` 的祖先鏈皆為 `Corner ** → Photo Print(r6cYjT) → bhroo`。
   - 5 個 instance `MYxf1`/`zfSO1`/`bOKfx`/`uvL4p`/`Z9Xuy`：`ref` 皆為 `bhroo`、`name` 皆為 `Card Album 1`（非 `Card Diary`）。
   - 5 個 `Cards` 群（`s3p2iz`/`tPRa8`/`Suokl`/`m5jmX`/`yOd7g`，分屬 `yXSht`/`a67Na`/`V4Ktak`/`lKoZG`/`A33la`）子節點順序皆為 `[Card Album 1(ref=bhroo), Card Diary 2(ref=qtCd7)]`——Card Diary 才是 Feed 真正的最後一張、本身零角托。
   - `HLXo3`（i-2 提到的第 6 板）：`ref=bhroo` 的實例數為 0，證實其 Card Album 已被刪除。
3. **m-A（minor）r6 收據補 `scan_note`**：`design/evidence/LS-208-r6-overflow.json` 頂層新增 `scan_note`，說明本輪分段跑法（詳見下方溢出掃描收據段）。
4. **PR body 更新**：新增「merge-reviewer 修正（R2）」段，逐條列 M-A／m-A／i-1／i-2／i-3 處置；變更摘要第 1 條、Design 段、驗證方式段同步更新；`pr-body-check.sh --verify` exit 0。

### 已驗證（怎麼驗）

- **獨立核對原文**：見上「已完成」第 2 條（execute 讀回的節點結構，逐項列出），訂正結果同步落在 `.claude/skills/little-sprout-brand/references/motifs.md` 該條與 `design/littlesprout.pen`。
- **`Djv58` 讀回 content**：訂正後（同一 execute 內即時讀回＋新 execute 唯讀重讀兩次）皆含「Feed 內最後一張帶角托的卡 Card Album 的四顆角托（【R3 訂正，依 merge-review e1e5157f M-A】原句誤記為「Card Diary」——cmp/Card Diary（qtCd7）子樹為 Snippet／Photos Row／Sign-off Rule／Signature，零角托；四顆角托屬 cmp/Card Album（bhroo→Photo Print→Corner TL/TR/BL/BR）…）」與「節點清單（皆為 Card Album instance，ref→bhroo）：…」兩處訂正，`fontSize`/`fill`/`lineHeight`/`fontWeight` 四個屬性零變更（`Update()` 只改 `content`），文字與 `.claude/skills/little-sprout-brand/references/motifs.md` 訂正後該條同義。
- **兩支 design gate**（worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-208`，`bash scripts/gates/`）：
  - `bash scripts/gates/design-evidence-check.sh design/littlesprout.pen --ticket LS-208 --base origin/development` → **exit 0**，六份收據（r1–r6）皆通過，末行：`✓ design-evidence gate 通過：design/evidence/LS-208-r6-overflow.json（head_sha=ed39d7c（本輪最新），total_nodes=9899，六支掃描皆有輸出…、corner_anchor.mismatch=0、boards 覆蓋本 PR 觸碰的板、tree_hash 對應 head_sha 快照、text_occlusion.flagged 為空、board_clip.flagged 為空、scan_scope=document、六支各帶 scope／document_count）`。
  - `bash scripts/gates/design-notes-check.sh design/littlesprout.pen --base origin/development` → **exit 0**，末行：`✓ design-notes gate 通過：Notes 板 8 塊、head id 9899、本 PR 範圍曾存在而 head 已無的 id 144、沿革引用 48、缺失 0、署名 NBSP 違規 0（舊債 0）`。
- **PR body gate**：`bash scripts/gates/pr-body-check.sh <body 檔> --branch feature/LS-208-design-debt-cleanup-2 --verify` → **exit 0**（`✓ PR body 檔頭段含本票票號 LS-208`／`✓ 第 9 行 LS-96 comment 1d0b97c2 存在`／`✓ 申報反查：LS-96 行 1 條、「已修」行 0 條`）。

### 溢出掃描收據（r6）

`design/evidence/LS-208-r6-overflow.json`：

- **head_sha**＝`ed39d7c2e005c2f264f2013194867a1c9acf3f5a`（本輪唯一觸碰 `.pen` 的 commit——.pen＋motifs.md 訂正那次；收據本身另開兩支 commit：`f359254`（新增收據）＋`b72a0c4`（scan_note 板名訂正 yec61 非 jftM0，不動任何機械驗證欄位））。
- **tree_hash**＝`f958129e31249bd8`——本輪雜湊在 Pencil `SCAN_HASH_ONLY` 拆段多次遇到間歇性 `InternalError: interrupted`（9899 節點／16017 展開節點的稿，與 LS-152/171 記載的已知限制一致）未能跑完，改用 `python3 scripts/gates/design_tree_hash.py design/littlesprout.pen` 對已落地（`pen-land.sh` exit 0 驗證節點數與畫布一致）的 head_sha 快照直接算——與 `design-evidence-check.sh` 用的同一支腳本、同一演算法。
- **六支掃描**：改用 `SCAN_SKIP_HASH=true`（略過雜湊、只跑六支）對同一份落地後、期間零寫入操作（僅 `Get`／`Print` 唯讀呼叫，含多次因間歇中斷而整批回滾的失敗嘗試——`execute` 失敗會整批回滾，回滾即無寫入）的稿態執行，以 `edits` 機制原樣重送多次後完整跑完一次：
  ```
  SUMMARY total_nodes=9899 scanned_nodes=16017 scan_scope=document sibling_intersection=2066 row_overflow=560 cross_parent_collision=321(bleed-only) text_occlusion=0/0 board_clip=0/0 corner_anchor=6/48/0 document=225/1784/0 ref_hits=746(defs=10) unresolved=3/14 boards=["yec61","HLXo3","qtCd7","kHDk4","N1aeiU","FEuAL","iBYUK","VUAqK","XhwG6","b3nzDp","dxPvP","hHrIV","z042Yg","Oic2v","JzyIv","RC7bN","jftM0","UOWte","Deljc","L6iIQ0","I07MZX"] tree_hash=skipped
  ```
  六個數字與 r5 逐位元相同——幾何集合本輪確實未變的直接證據（本輪唯一 `.pen` 變更是 `yec61` 板內 `Djv58` 一個 Notes text 的 `content`，該段落不含角托／印品／覆蓋層元素）。
- **三項硬指標本輪為真實即時重跑結果**（非沿用 r5）：`corner_anchor.mismatch=0`（containers=6／points=48，document=225/1784/0，ref_hits=746，unresolved 3 筆皆 `XhwG6` 既有 `mount_pair`）、`text_occlusion.flagged=0`、`board_clip.flagged=0`，對本票 21 板皆為 0。
- **三支 O(n²) 掃描**（`sibling_intersection`／`row_overflow`／`cross_parent_collision`）document_count（2066/560/321）本輪即時掃描確認與 r5 逐位元相同；flagged 陣列（分類代表與「同類 N 例」計數）沿用 r5 已核可版本（不重寫已核可的分類文字與逐筆 e.g. 範例 id）。
- **`scan_scope`＝`document`**，`boards` 21 板比照 r5，覆蓋本 PR 觸碰的所有板。

### 未完成（reviewer informational 處置）

- **i-1**（`corner_anchor.unresolved[]` 格式，r5 帶 `board_name` 才是正典格式、r4 是舊寫法）：**不修**——reviewer 明確表示「記錄備查、不必回改 r4」，已知悉。
- **i-2**（motifs.md「Tab Bar 全字級純 icon」節列 6 板、本裁決只涵蓋 5 板，差的 `HLXo3` 因 Card Album 已於 LS-120 R6 刪除）：**已修**——修 M-A 時已在 motifs.md 該條順手補上這半句說明。
- **i-3**（PR body 主動揭露「三支 O(n²) 掃描沿用分類」的方向正確）：**已採納**——非需修正項，本輪 r6 延續同一揭露方式（`scan_note`），並更進一步：三支硬指標本輪為真實即時重跑而非沿用。

### Skill 影響了哪些取捨

- **`frontend-design:frontend-design`**：呼叫 Skill 工具回傳 `Unknown skill: frontend-design:frontend-design`——本 session 可用 skill 清單裡沒有這個名稱，判定為**未載入**（找不到該 skill，非略過）。本輪任務性質是文字內容訂正（`.pen` Notes 沿革句＋markdown 條文），不涉及任何視覺排版／色彩／字級決策，即使該 skill 有載入也不會影響本輪任何取捨。
- **`little-sprout-brand`**：已成功載入。本輪唯一與視覺母題相關的取捨是核對「角托三段規則」（skill 條 4）本身的正確性——訂正後的敘述（Card Album 有四顆角托、Card Diary 零角托）與 skill 條 4「角托三段規則」（①四角托＝家人照片收藏品、③零角托＝非收藏品）完全一致，未引入新規則、未變更任何 token。

### 未決事項

- 無新增未決事項；本輪為 merge-review R2 的訂正輪，未涉及任何需要人核可的新設計決策。

### 風險

- 無新增風險。本輪僅 1 個 text `content` 屬性變更（`.pen`）＋ 1 行 motifs.md 修正，零節點增刪、零座標／尺寸屬性變動；六支掃描含三支即時真實重跑確認硬指標為 0。

### 產出位置

- `.claude/skills/little-sprout-brand/references/motifs.md`（角托三段規則末條訂正）
- `design/littlesprout.pen`（`yec61/Djv58` 沿革句訂正）
- `design/evidence/LS-208-r6-overflow.json`（六支掃描收據＋`scan_note`）
- PR #349 body 已更新（`gh pr edit 349`）

### 尺寸類（width／height）token

本輪無新增或修改任何 `width`／`height` 字面值（僅 text `content` 屬性變更），無尺寸類 token 硬寫紀錄需登記。

### Pen 路徑

- 開工核對：`get_app_state()` 回傳 active 文件路徑＝`/Users/clyeh/little-sprout/.claude/worktrees/LS-208/design/littlesprout.pen`，與 `$(git rev-parse --show-toplevel)/design/littlesprout.pen` 一致，符合規約。
- 本輪落地：`bash scripts/ops/pen-land.sh . --expect-nodes 9899 --after "1788705093" --marker "e1e5157f"` → **exit 0**（首次因 backup mtime 落後、`--marker` 未命中被擋，等待約 6 分鐘 autosave 追上後重跑成功；未使用 `--allow-unchanged`）。
- 收工：Pen 仍停在 `/Users/clyeh/little-sprout/.claude/worktrees/LS-208/design/littlesprout.pen`（`get_app_state()` 收工前再次核對），未切檔、未執行任何 `pen-open.sh`，無「⚠ Pencil MCP 需重連」訊息。

### Commits

- `ed39d7c` `fix(design): LS-208 R2 依 merge-review e1e5157f M-A 訂正角托元件歸屬`（motifs.md＋`.pen`）
- `f359254` `chore(design): LS-208 R6 溢出掃描收據`
- `b72a0c4` `fix(design): LS-208 R6 收據 scan_note 板名訂正（yec61 非 jftM0）`

---
`bash scripts/gates/handoff-evidence-check.sh` → exit 0（第 21–26 行列項皆有證據）。

### 2026-09-06T15:22:43.954Z · Chih-Lin Yeh · `efcd4ac4`

## merge-reviewer R3（設計 PR）：APPROVE（0 blocker／0 major／0 minor／3 informational）

（2026-09-06 23:20 CST）

**審的 head**：`b72a0c4fbb9806bde842f4ceab0478ddbd4afaf3`（PR #349，`feature/LS-208-design-debt-cleanup-2` → `development`）；開審（23:10）與收工（23:20）各查一次 `gh pr view 349 --json headRefOid`，皆同值——審查期間 head 未前進。
**Delta**：`git diff c120626..b72a0c4`（R2 審的 head → 本輪 head），三個 commit `ed39d7c`／`f359254`／`b72a0c4`；只審 delta＋R2 兩項（M-A／m-A）是否落地。
**Pen**：全程未開 Pencil MCP（Pen 停在 LS-208 票檔未動），`.pen` 一律用 python 讀明文 JSON（快照取自 `git show <sha>:design/littlesprout.pen`）。**DB lock**：未持有（本票無 DB 測試，未跑任何本機容器命令）。**模擬器**：未開，未跑 `xcodebuild`，未改 `simctl ui`。

### 一、逐項查實（怎麼驗）：delta 範圍

- `git diff --name-status c120626..b72a0c4` → 只有 3 檔：`M .claude/skills/little-sprout-brand/references/motifs.md`（+1/-1 行）／`A design/evidence/LS-208-r6-overflow.json`／`M design/littlesprout.pen`。`git diff --stat` 為 3 files, 123 insertions, 2 deletions。全部落在票文範圍內，無順手改動、無 scope 外檔案。
- `.pen` 三個 commit 中只有 `ed39d7c` 觸碰；`shasum -a 256` 確認 `git show b72a0c4:design/littlesprout.pen`＝`git show ed39d7c:…`＝worktree 現檔（皆 `dae5ce36…`；快照存 `scratchpad/LS-208-mr/head3.pen.json`）。
- `b72a0c4` 只改 `scan_note` 內兩處板名（`git diff --word-diff=plain f359254..b72a0c4` → `jftM0`→`yec61`、並把「jftM0 本身 fit_content」改寫成「yec61 本身 fit_content、Djv58 所在 Handoff Notes 段落…」），`design/evidence/LS-208-r6-overflow.json` 零機械驗證欄位變動——designer 自己抓到並訂正的筆誤。

### 二、M-A 落地判定：**已落地、關閉**（兩處都修，且事實我逐項獨立複驗）

(a) **`motifs.md` 該條**（`git diff c120626..b72a0c4 -- …/motifs.md`，仍是同一條、同一節、粗體導語＋規則＋量法＋裁決＋節點清單的既有格式）。我用 python 對 head 快照（`scratchpad/LS-208-mr/head3.pen.json`，9899 節點建 id／parent 索引）逐項驗，全部與訂正後條文一致：

- `qtCd7`＝`cmp/Card Diary`，子樹 20 節點＝`Snippet(p2odx)`／`Photos Row(lEgJp)`／`Sign-off Rule(u59KAY)`／`Signature(Jqwww)`，**零角托**（子樹內 `ref=GEBcf` 或名稱含 corner 的節點：0 個；`DjW91`/`JgUK8`/`mlORb`/`atAkU` 皆不在其中）。
- `bhroo`＝`cmp/Card Album`，四顆角托的祖先鏈皆為 `Corner TL/TR/BL/BR(DjW91,JgUK8,mlORb,atAkU) → Photo Print(r6cYjT) → cmp/Card Album(bhroo)`，四顆 `ref` 皆 `GEBcf`；`idx['GEBcf'].name`＝`cmp/Photo Corner`（條文「cmp/Photo Corner 系角托」屬實）。
- 5 個 instance `MYxf1`／`zfSO1`／`bOKfx`／`uvL4p`／`Z9Xuy`：`type=ref`、`ref=bhroo`、`name='Card Album 1'`，分屬 `Cards` 群 `s3p2iz`／`tPRa8`／`Suokl`／`m5jmX`／`yOd7g`，對應板 `yXSht`／`a67Na`／`V4Ktak`／`lKoZG`／`A33la`——與條文括號內的板對應逐一相符。
- 五板最後一個 `Cards` 群子節點順序皆為 `[Card Album 1(ref=bhroo), Card Diary 2(ref=qtCd7)]`；`Feed` 子節點順序為 `[…Day Group 1, Day Group 2, Load More]`，Day Group 2 才是最後一組——因此「Card Album 是 Feed 內最後一張**帶角托**的卡、其後的 Card Diary 才是真正末張且零角托」成立。旁證：`cmp/Card Photo`（`umJHD`，Day Group 1 內）確實帶四顆 `ref=GEBcf` 角托（`INXbk`/`O6ami`/`SNbd8`/`P15GB`），所以「最後一張帶角托的卡」這個限定詞是必要且正確的。
- `HLXo3`：`ref=bhroo` 實例數 **0**（兩個 Cards 群各只有一張 Card Diary），i-2 要求的半句「其 Card Album 已於 LS-120 R6 刪除、不在此列」屬實——Notes `yec61/HPvum`／`u7aYzd` 明載 LS-120 R6 修 BL-9 時刪掉 `CFlP7`（Card Album 1）。
- 條文未動的既有敘述我一併復驗：五板 `Body`（`n7gPAe`／`x5Vmg`／`Q600X`／`YulfE`／`o73UY`）皆 `clip:true`＋`height: fill_container`，五個 `Feed`（`dTdm3`／`aNcaB`／`riVZU`／`LJJLP`／`Z83pG`）皆無 `clip` 鍵——「裁切者是 Body 不是 Feed」仍對。

(b) **`.pen` `yec61/Djv58` 沿革句**：用 difflib 對 `head2.pen.json`(c120626) 與 `head3.pen.json`(b72a0c4) 的 `content` 取 opcodes，非 equal 段共 5 段，全部是插入（唯一 replace 是 `'的四顆'`→`'Diary 才是真正最後一張、本身零'`，屬同一句改寫的一部分）：新增「【R3 訂正，依 merge-review e1e5157f M-A】原句誤記為「Card Diary」——cmp/Card Diary（qtCd7）子樹為 Snippet／Photos Row／Sign-off Rule／Signature，零角托；四顆角托屬 cmp/Card Album（bhroo→Photo Print→Corner TL/TR/BL/BR），且非 Feed 最後一張——各板 Cards 群順序皆為 [Card Album, Card Diary]，Card Diary 才是真正最後一張、本身零角托，不受本裁決影響）」與節點清單的「（皆為 Card Album instance，ref→bhroo）」。原句詞組「Card Diary」保留在沿革括號內引用，寫法與同節既有兩處訂正（【R3 訂正，依 VR comment 3f3d0432 MJ-A】／【R2 訂正，依 VR comment a4597e9e MN-1】）同型，並引用 `e1e5157f`。
- **attrdiff（python 逐節點，shallow canonical JSON）**：`yec61` 子樹 288→288 節點，ADD=0／REM=0／ATTR-CHG=1，唯一變動節點是 `Djv58`、唯一變動鍵是 `content`；`fontSize` 17／`fill` `#2B141C`／`lineHeight` 1.6／`fontWeight` normal／`fontFamily` Noto Sans TC／`width` fill_container／`x`／`y`／`height` 全部逐字未變；`yec61` 子節點順序未變。
- **兩份文字互相一致**：訂正後 motifs.md 該條與 `Djv58` 對元件名（`cmp/Card Album`）、角托歸屬（`bhroo→Photo Print`）、instance 清單、「末張卡」描述四項的敘述同義，不再各說各話。

### 三、m-A 落地判定：**已落地**

`design/evidence/LS-208-r6-overflow.json` 頂層新增 `scan_note`（python `keypaths()` 對 r5／r6 遞迴取鍵路徑集合 → r6 只多 `.scan_note`，r5 沒有多任何鍵）。內容如實：明寫①`tree_hash` 改用 `scripts/gates/design_tree_hash.py` 對已落地快照算；②六支改用 `SCAN_SKIP_HASH=true` 對同一稿態跑，並附 SUMMARY 原文；③三支硬指標為本輪即時重跑；④三支 O(n²) 的 flagged 陣列沿用 r5 已核可版本、本輪只重新確認總數。分段與沿用兩件事都寫進收據本身，達成 m-A 要買的「單看 JSON 也知道來源」。

### 四、收據 r6 機械查核

- **tree_hash**：`python3 scripts/gates/design_tree_hash.py design/littlesprout.pen`（worktree 檔＝`ed39d7c` 那份，sha256 已比對）→ `f958129e31249bd8`，與收據 `tree_hash` 逐字相同，也與 designer handoff 申報值相同。
- **head_sha**：收據 `ed39d7c2e005c2f264f2013194867a1c9acf3f5a`；`git log -1 -- design/littlesprout.pen` → 同一 sha，確為本 PR 最後一次觸碰 `.pen` 的 commit。`total_nodes=9899`（＝我建索引時數到的節點數）、`scan_scope=document`。
- **三支硬指標對本票 21 板為 0**：`corner_anchor` `mismatch=0`／`document_mismatch=0`（`containers=6`／`points=48`／`document_containers=225`／`document_points=1784`／`ref_hits=746`／`unresolved` 3 筆皆 `XhwG6` 的 `mount_pair`、帶 `board_name`）、`text_occlusion.flagged=[]`／`document_flagged=[]`、`board_clip.flagged=[]`／`document_flagged=[]`。
- **六支欄位與 r5 同型**：六支名稱、順序、`scope`／`document_count` 欄位齊備且與 r5 一致；逐支 canonical JSON 比對 → **六支全部逐字相同**（含三支 O(n²) 的 flagged 29／22／8 筆）。三支 O(n²) 沿用 r5 的申報屬實，且與 scan_note 自述一致。
- **內部加總一致**：各支 flagged 代表的「同類 N 例」加總＝該支 `document_count`（2066＝2066、560＝560、321＝321，無一筆缺計數）。
- **`boards` 21 板＝本 PR 觸碰的板**：對 merge-base `ba81f8f` 做 root diff（ADDED 12／REMOVED 1 `rh2Q1`／CHANGED 9）→ `set(ADDED)|set(CHANGED)` 與收據 `boards` **完全相等**，無多無少。
- **SUMMARY 原文與收據欄位對得上**：`corner_anchor=6/48/0`／`document=225/1784/0`／`ref_hits=746`／六支數字 2066/560/321 與 0/0，逐項對應收據欄位。

### 五、全稿保序比對（`c120626..ed39d7c`／對 base）

- prev(c120626) 與 head(b72a0c4) 皆 231 root：ADDED 0／REMOVED 0／**CHANGED 1（僅 `yec61`）**；其餘 230 root 用保序 `json.dumps`（不 sort_keys）逐字比對 mismatch **0**，root 順序完全一致（id 序列相等）——他票 root 一個字都沒動。
- `version`／`themes`／`variables`／`fileToken` 四個頂層鍵：prev→head 與 base→head 皆逐字相同（token 未動）。
- 對 base 的全 PR 比對仍成立：ADDED 12／REMOVED 1／CHANGED 9／UNCHANGED 210（保序 mismatch 0）。
- **幾何風險**：本輪唯一變動是 `yec61` Handoff Notes 內一個 text 的 `content`，零節點增刪、零座標／尺寸屬性；`yec61` 為 Notes 板、fit_content，文字加長只往下長。這也解釋了六支數字為何逐位元不變。

### 六、Gate 與 CI 實跑（worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-208`，唯讀）

- `bash scripts/gates/design-evidence-check.sh design/littlesprout.pen --ticket LS-208 --base origin/development` → **exit 0**；末行：`✓ design-evidence gate 通過：design/evidence/LS-208-r6-overflow.json（head_sha=ed39d7c（本輪最新），total_nodes=9899，六支掃描皆有輸出（支數以 scripts/design/overflow-scan.js 檔頭為準）、corner_anchor.mismatch=0、boards 覆蓋本 PR 觸碰的板、tree_hash 對應 head_sha 快照、text_occlusion.flagged 為空、board_clip.flagged 為空、scan_scope=document、六支各帶 scope／document_count）`（r1–r6 六份皆通過）。
- `bash scripts/gates/design-notes-check.sh design/littlesprout.pen --base origin/development` → **exit 0**；末行：`✓ design-notes gate 通過：Notes 板 8 塊、head id 9899、本 PR 範圍曾存在而 head 已無的 id 144、沿革引用 48、缺失 0、署名 NBSP 違規 0（舊債 0）`。
- `bash scripts/gates/pr-body-check.sh scratchpad/LS-208-pr-body-r3.md --branch feature/LS-208-design-debt-cleanup-2 --verify` → **exit 0**；末行：`✓ 申報反查：LS-96 行 1 條、「已修」行 0 條`（另有 `✓ PR body 檔頭段含本票票號 LS-208`、`⚠ 未反查（無 LINEAR_API_KEY）：第 9 行候選 1d0b97c2,e1e5157f,e1e5157f`——本機無 key 故只驗格式，CI 會反查）。
- `gh pr checks 349 --json name,bucket` → `lint`／`ci`／`ci-ipad`／`rules`／`db` 五支皆 `pass`；`gh api …/commits/b72a0c4…/check-runs` 確認五支的 `head_sha` 都是 `b72a0c4`，跑的就是本輪 head。
- PR body 內容對帳：新增「merge-reviewer 修正（R2，1 major／1 minor／3 informational）」整段，M-A／m-A／i-1／i-2／i-3 逐條列處置；變更摘要第 1 條與「Design」段同步更新（`R2 額外變更…：yec61/Djv58`）。

### 七、Findings（3 informational，無 blocker／major／minor）

#### i-1（informational）`scan_note` 稱本輪 `.pen` 變更為「沿革訂正 pure append」，機械上不是純追加

**位置**：`design/evidence/LS-208-r6-overflow.json` 頂層 `scan_note` 第 2 句。
**事實**：difflib 對 `Djv58` 新舊 `content` 取 opcodes → `new.startswith(old)` 為 **False**；訂正文字是插在句中（並把 `'的四顆'` 改寫成 `'Diary 才是真正最後一張、本身零'`），不是接在段末。
**影響**：實質主張（無節點增刪、無座標／尺寸屬性變動）我獨立驗證屬實，幾何論證不受影響；只是日後有人照字面用 `startswith` 複驗會得到 False 而多花一次時間。
**建議**：日後這類中段沿革訂正寫成「中段插入沿革括號、原句詞組保留於括號內引用」即可。**不建議為此再動 `.pen`／再產一輪收據**——改 `scan_note` 要再開一個 commit，成本高於收益。

#### i-2（informational）同一筆訂正的輪次標籤兩份成品不一致（motifs.md 寫 R2、`Djv58` 寫 R3）

**位置**：`motifs.md:35`「**R2 依 merge-review comment e1e5157f M-A 訂正元件歸屬**」vs `.pen` `yec61/Djv58`「【R3 訂正，依 merge-review e1e5157f M-A】」。
**事實**：`Djv58` 同節既有兩處沿革的慣例是「訂正標籤輪次＝來源 review 輪次」（【R3 訂正，依 VR comment 3f3d0432 MJ-A】＝VR R3；【R2 訂正，依 VR comment a4597e9e MN-1】＝VR R2）；`e1e5157f` 是 merge-review **R2**，照慣例本條應標 R2，motifs.md 那半句才是照慣例的寫法。
**影響**：極小——兩處都引同一個 comment id `e1e5157f`，稽核鏈靠 id 不會斷；設計端「R3」讀成「designer 第 3 輪訂正」也講得通。
**建議**：記錄備查、**不修**（再動 `.pen` 就要再產一輪收據）；若日後 LS-207 那類 harness 票要統一沿革標籤語彙，把「輪次＝來源 review 輪次」寫成明文規則即可。

#### i-3（informational）「三支硬指標本輪即時重跑、非沿用 r5」這件事在收據層面仍不可機械驗證

六支 scan 的 canonical JSON 與 r5 逐字相同（硬指標本來就是 0／0／0＋固定的 containers/points/ref_hits/unresolved），因此「重跑後恰好相同」與「照抄」在收據上長得一模一樣；不開 Pencil MCP 就沒有第二來源。這正是我 R2 同僚建議、orchestrator 已記入 LS-96 的 harness 缺口（收據帶「每支掃描結果的雜湊」＋正典腳本自帶分批加總）要解的問題，本輪不重複開票、不因此扣分——designer 在 `scan_note` 主動揭露沿用範圍，已是目前工具能做到的最誠實作法。

### 八、做得對、重做時不要洗掉

1. **M-A 修得徹底**：不只改 markdown，`.pen` 同一處也用沿革句訂正，兩份成品現在同義——稽核鏈上不再有「兩份各說各話」的分岔，下一張票不必再量一次。
2. **獨立核對而非照抄 reviewer 結論**：handoff 列出的 `Get()` 讀回結構（`qtCd7` 四個子節點、四顆角托祖先鏈、5 個 instance 的 `ref`／`name`、Cards 群順序、`HLXo3` 零 Album）我用 python 對 head 快照逐項重放，**五項全部對得上**，沒有一項是誇大或反向。
3. **自己抓到自己的筆誤**（`b72a0c4`：`scan_note` 把板名寫成 `jftM0`，實際是 `yec61`），且只改敘述、不碰任何機械欄位。
4. **手術式**：230 個他票 root 保序逐字不變、四個頂層鍵未動、單一 text `content`、零節點增刪。

### 九、我沒看／沒重跑的（明講）

- 六支掃描的數值本身：需要 Pen 版面引擎，本次派工明令不開 Pencil MCP，我沒有獨立重算任何一支數字。做的是第四節列的間接查核（`design_tree_hash.py` 複算、欄位對帳、內部加總、六支 canonical 逐字比對、`boards`＝觸碰板集合）。
- 「20 顆角托完全落在 `Body` clip 邊界之外」這個**幾何量測結論**：`fit_content` 板的解析高度從 JSON 得不出來，沿用 R1／VR R3 的結論；本輪只確認 delta（單一 Notes 文字）不可能讓它改變，且元件歸屬已訂正。
- 本票 R1–R5 的設計內容（12 張新板的版面、文案、AX3）不在本輪範圍，沿用 VR R3 `0703744e` APPROVE 與 merge-review R1 `61e654ff`／R2 `e1e5157f` 對該部分的結論。
- 未開模擬器、未跑 `xcodebuild`、未跑任何 `supabase`／`docker` 命令；**DB lock 未持有**（本票無 DB 測試）。

### 十、Verdict

**APPROVE**（0 blocker／0 major／0 minor／3 informational）。M-A 兩處都修且事實逐項複驗屬實，m-A 的 `scan_note` 已補且如實；delta 手術式、gate 三支與 CI 五支全綠並綁對 head。三項 informational 皆不建議再動 `.pen`（會多出一輪收據），交由 orchestrator 決定是否記入 LS-96。

依規約對審的 head `b72a0c4fbb9806bde842f4ceab0478ddbd4afaf3` 貼 `merge-review` = `success`。

### 十一、`handoff-evidence-check` 實跑（LS-211，貼文前）

`bash scripts/gates/handoff-evidence-check.sh <本 comment 暫存檔>` → **exit 0**，逐字：

```
✓ 第 11 行起的列項有證據
✓ 第 12 行起的列項有證據
✓ 第 13 行起的列項有證據
```

（首跑 exit 2「找不到『已驗證』段落」＋ exit 1「第 12／13 行缺證據」，故把第一節標題改為「逐項查實（怎麼驗）」並在該兩行補上實際用到的產物路徑 `scratchpad/LS-208-mr/head3.pen.json`／`design/evidence/LS-208-r6-overflow.json`——只補證據路徑，未改任何結論敘述。）

### 2026-09-06T15:24:19.119Z · Chih-Lin Yeh · `f585dd6e`

（09-06 23:22）merge-review R3 APPROVE（efcd4ac4；M-A／m-A 落地皆獨立複驗、tree_hash f958129e 對 head、五支 CI 綠）。PR #349（head b72a0c4）**已可併入，等使用者核可頁三項決定**（560a2b43：整體核可 12 板／範圍 6 Subtitle 兩版／範圍 7 不同意出口）——依 LS-177／201 先例，設計 PR 於核可後才併 development；若選 LS-192 原文 Subtitle，designer 再改一個 text 節點＋r7 收據、merge-reviewer R4 delta。標 `hold:user`。i-1／i-2 記錄備查不修；i-3 即 LS-226 範圍。Pen 依 LS-180 續停本票檔至併入＋清場。

### 2026-09-12T15:01:03.069Z · Chih-Lin Yeh · `91f7ac68`

**使用者核可 09-12 23:00**（orchestrator 代記，決策單 c47ec33a 第 1 題）：1a 整體 12 板**核可**；1b 範圍 6「只有你一位成員」副標採**提案版**；1c 範圍 7 EULA「不同意，登出」**照稿**（文字鈕）。→ 併 PR #349 → 死碼巡檢 → Pen 清場（請使用者 `/mcp` 重連）→ Done → 派下一張設計票。

### 2026-09-12T15:15:03.212Z · Chih-Lin Yeh · `765278a5`

**dead-code-sweeper 09-12 23:14**

## 範圍
`git diff --stat origin/main...origin/development -- design/ docs/`：只有 `design/littlesprout.pen`＋`design/evidence/LS-208-r1~r6-overflow.json` 7 檔；另用 `-- .claude/skills/` 確認 `motifs.md` 1 行新增。`git log origin/main..origin/development -- design/ docs/` 逐一核對 26 個 commit 全部是 `LS-208` 前綴，無夾帶他票異動。本票 In Review、PR #349（head `b72a0c4`）已併 `development`（`0ce9029`），使用者已於 09-12 23:00 核可（comment `91f7ac68`：1a 整體核可／1b 範圍 6 副標採提案版／1c 範圍 7「不同意，登出」照稿）。

## Findings：**未發現本 feature 引入的死碼**

逐項機械複核如下（皆對 `origin/development` 快照，`design/littlesprout.pen` 用 `git show` 讀明文 JSON＋python 解析，未開 Pencil MCP）：

1. **兩支 design gate 在 `.claude/worktrees/LS-208`（PR head，內容與併入後一致）獨立重跑，皆過**：
   - `design-evidence-check.sh --base origin/main` → r1–r6 六份收據全綠（`corner_anchor.mismatch=0`、`text_occlusion.flagged=[]`、`board_clip.flagged=[]`、`scan_scope=document`、r6 `head_sha=ed39d7c` 為本 PR 最後一次觸碰 `.pen` 的 commit）。
   - `design-notes-check.sh --base origin/main` → `Notes 板 8 塊、head id 9899、本 PR 範圍曾存在而 head 已無的 id 144、沿革引用 48、缺失 0、署名 NBSP 違規 0（舊債 0）`——與 R3 handoff 報的數字逐字相同，併入後無退步。
2. **merge-review R2 抓到的 M-A（角托元件歸屬誤植 `cmp/Card Diary`→應為 `cmp/Card Album`）在 head 完全訂正、無殘留**：直接解析 `.pen` 確認 `qtCd7`（`cmp/Card Diary`）子樹 4 個節點（Snippet／Photos Row／Sign-off Rule／Signature）零角托；`bhroo`（`cmp/Card Album`）→`r6cYjT`→`DjW91`/`JgUK8`/`mlORb`/`atAkU` 四顆角托存在；5 個 instance `MYxf1`/`zfSO1`/`bOKfx`/`uvL4p`/`Z9Xuy` 皆 `ref=bhroo`、`name=Card Album 1`；`HLXo3` 內 `bhroo` ref 計數＝0（LS-120 R6 已刪除，與 motifs.md 附註一致）。`motifs.md` 該條文字與 `.pen` `yec61/Djv58` 逐字對應，無兩邊各說各話。
3. **scope 2/5 NBSP／「·」修正無殘留 U+0020**：`cmp/Card Diary` 元件預設 `x7k2o6` 與 `HLXo3` 實例覆寫（原 `fZ3KF`，R2 因 Feed 重建改名 `kmbyt`，Notes 已用沿革句正確標記「已刪除、現行 `kmbyt`」）逐字元核對，兩處 codepoint 序列完全相同：`小安`+U+0020+`·`+U+00A0+`2`+U+00A0+`歲`+U+00A0+`3`+U+00A0+`個`+U+2060+`月`+…，無 U+0020 殘留在「·」與年齡片語之間。
4. **scope 3 Auth 兩態**：`iBYUK` 結構確認提示列已移入 `K06vQ Field Wrap`／`UU6PC Password Field` 插槽內（`i2SHf/xf0zM` Reply Text），`g8jd6 Footer` 只剩 `Button Wrap`——對應 VR R1 MJ-2 与 merge-review 沒有回退。`VUAqK`（AX3）的 `xf0zM` 與 `iBYUK/QMJfn` 皆存在且結構一致（M-1 合併前必修已落地）。
5. **scope 6 單人家庭變體**：`dxPvP` Subtitle 現值＝「目前家庭只有你一位成員，沒有可轉移的對象。若要離開，請使用下方的『帳號』設定前往刪除帳號。」＝使用者核可的**提案版**（非 LS-192 逐字版），與 comment `91f7ac68` 裁決一致。VR R1 MN-3 點名的 531pt 無名 Spacer 已改為 `fill_container`＋新增 `e5MZp Row · 帳號`（`ref=bZhuV`＝既有 `cmp/Settings Row`，非新建孤兒元件）。
6. **scope 8 三岔路兩狀態**：`UOWte`/`L6iIQ0` 的 `q6CXE`/`qBdcW` 說明文字 `fontSize` 已為 `$fs-note`（17），MJ-4 落地。
7. **無新孤兒 component／token**：本票新增的 12 個 root（`FEuAL`/`iBYUK`/`VUAqK`/`dxPvP`/`hHrIV`/`JzyIv`/`RC7bN`/`Oic2v`/`UOWte`/`L6iIQ0`/`Deljc`/`I07MZX`）全部是畫面／AX3 板（`reusable` 皆非 true），不是新元件定義；新增的 `cmp/Button Text`「不同意，登出」實例 `uyQcn` 正確 `ref→qe9yS`（既有元件）。範圍 4 裁決為不調 token，`variables`/`themes` 頂層鍵在 root diff 中確認未變動。
8. **評估「Handoff Notes 板 `EclPC`」**：`EclPC` 是 **LS-177** 的 Handoff Notes 板，不在本票觸碰的 21 個 root 之列（`git diff` 與 root diff 皆確認未變動）；本票實際觸碰的 Notes 板是 `yec61`/`jftM0`/`b3nzDp`/`N1aeiU`（皆存在、沿革標記完整、gate 綠）。若派工指示中的 `EclPC` 是誤植（可能與 LS-177 混淆），提醒 orchestrator 核對；就本票範圍而言這不構成殘留，因為本票根本沒有動過那塊板。

## 既有死碼觀察
無（本票觸碰範圍嚴格限縮在 21 個 root＋8 塊 Notes 板中的 4 塊＋1 個 skill 檔，未見夾帶其他票的舊債）。

## 建議處置
不需開票。若要處理，僅有一項供 orchestrator 知悉但不構成 fix：上方第 8 點的 `EclPC` 疑似誤植，建議下次派工時核對票號對應的 Notes 板 id 來源。

## 搜尋方法與盲區聲明
- 方法：`git show origin/development:design/littlesprout.pen` 取明文 JSON，python 解析（含 `descendants` override 結構）逐節點核對 id／content／ref／fontSize；`design-evidence-check.sh`／`design-notes-check.sh` 在既有 worktree `.claude/worktrees/LS-208`（內容＝PR head＝併入內容）獨立重跑；`git diff`/`git log` 圈定 diff 範圍。
- 盲區（明講）：(1) 未開 Pencil MCP，未做任何幾何／resolveInstances 展開的即時計算——六支溢出掃描的座標數值本身沿用 VR R1–R3、merge-reviewer R1–R3 已完成的多輪獨立重跑結果，本次未重新驗證（那些輪次已用 Pencil 即時複算，比本次純文字讀取更可靠）；(2) 純文字比對抓不到「id 存在但內容語意錯」以外的動態引用問題；(3) 未檢查 Swift 端是否已依 Notes 的實作對應落地（LS-208 為設計專票，實作為後續票範圍，不在本次死碼巡檢內）。

Comment id：（見下方回報）

### 2026-09-12T15:16:56.847Z · Chih-Lin Yeh · `1b274847`

**orchestrator 09-12 23:16 收尾儀式 ②lesson review → Done**
- ①巡檢：sweeper `765278a5` 無殘留（六份收據與 Notes gate 併入後重跑皆綠；R2 M-A 角托元件誤植已訂正；NBSP／「·」codepoint 無殘留；12 新 root 皆畫面板、無新元件定義）。③cleanup：`cleanup-merged.sh --apply LS-208`。④Pen 清場：`pen-open.sh <主 checkout> --kill` ✓（23:03），使用者 23:13 `/mcp` 重連 ✓。
- **教訓 1**：Pen 重開主 checkout 檔時把記憶體中的 LS-208 文件內容寫回主 checkout（`design/littlesprout.pen` 192+/779−，與 dev 版只差 4 行），主 checkout 被弄髒；已 `LS_ALLOW_MAIN_CHECKOUT_WRITE=1 git checkout --` 還原。**規則**：清場後巡檢要看主 checkout `git status`，Pen 寫回的 .pen 一律還原（dev 已有正本）；記入池項。
- **教訓 2**：sweeper 指出派工單寫的 Notes 板 `EclPC` 是 LS-177 的板，本票實際是 `yec61`／`jftM0`／`b3nzDp`／`N1aeiU`——派工前要核對票號↔板 id，不要沿用上一張的 id。
- 使用者裁決：1a 核可、1b 提案版、1c 照稿（`91f7ac68`）。本票 Done；下一張設計票＝App Store 截圖版型（LS-147 前置，cycle 3）。

