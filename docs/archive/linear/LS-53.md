# LS-53 Harness：破壞性偵測器盲區——ALTER POLICY …(false) 與 REVOKE 未納入關鍵字/語意判定

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | — |
| Cycle | — |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-24T15:50:27.536Z |
| 開始 | 2026-08-24T19:59:23.405Z |
| 完成 | 2026-08-24T21:42:46.182Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-53/harness破壞性偵測器盲區-alter-policy-false-與-revoke-未納入關鍵字語意判定 |
| 關係 | related→LS-56, related→LS-48, related→LS-45, related←LS-80, related←LS-90, related←LS-61, related←LS-58, related←LS-63, related←LS-60, related←LS-59, related←LS-52 |

## 描述

來源：PR #60 review F7。`.github/workflows/ci.yml` Migration rules 的破壞性關鍵字清單命中 `DROP POLICY` 但不命中 `ALTER POLICY … USING (false) WITH CHECK (false)` 與 `REVOKE INSERT, UPDATE`——同一個實質變更（既有呼叫端立刻 42501）用 DROP 要人核可、用 ALTER 不用，且 [LS-48](https://linear.app/little-sprout-app/issue/LS-48/taskls-21-後端-diaries-schema-補全時間軸查詢-rpcrls測試) 的 migration 註解已把「ALTER 不觸發偵測器」寫成選型理由（已要求改寫）。這是「想避開 gate 就換寫法」的前例。

## 方向

1. 把 `REVOKE`、`ALTER POLICY … (false)`、`ALTER TABLE … DISABLE ROW LEVEL SECURITY`、`DROP FUNCTION` 等納入偵測清單，或改成「breaking-change」語意分級：破壞資料（需 DESTRUCTIVE-APPROVED）vs 破壞呼叫端（需 PR body 明示 `BREAKING:` 段落＋API.md 同步）。
2. 負向測試比照 [LS-45](https://linear.app/little-sprout-app/issue/LS-45/harness破壞性核可標記改整行錨定子字串比對可被無意提及滿足) `.test.sh` 形式掛 rules job。
3. COLLABORATION §6/§7 同步。走 hotfix-from-main。

## 留言（5）

### 2026-08-24T20:41:56.531Z · Chih-Lin Yeh · `0ce04f30`

## merge-reviewer R1 — PR #78 → ITERATE（orchestrator 代貼＋裁決）

**Blocker（皆為相對舊版逐行 grep 的退化，一字元／兩行註解即繞過）**
- **B1** `migration-breaking-check.sh:103` `while read` 丟棄無結尾換行的末句；`normalize`(:67-75) 吃掉檔尾換行 → migration **最後一句沒分號就不分級**。新 migration 恆為 diff 最後一檔，必中。實測 `drop table public.families`（無分號）→ 無輸出 exit 0；抽 ci.yml step 乾跑 rc=0。→ 修：`read -r stmt || [ -n "$stmt" ]`＋補自測樣本。
- **B2** :68-69 先剝 `/* */` 再剝 `--`，與 Postgres 相反：`-- x /*` … `-- y */` 之間的 DROP＋REVOKE 全被靜默刪除，CI rc=0。→ 修：對調順序＋補自測樣本。

**應修**
- **M1** 命中 DESTRUCTIVE 後 `continue`，`drop function <已上線 RPC>` 只需核可標記、不需 `BREAKING:` 段落也不需動 API.md；與 §6「兩級取聯集」不符。→ **裁決：每句可同時輸出兩級**（拿掉 `continue`），文件不改。
- **M2** `--known-functions` 指向不可讀檔 → grep 失敗當新函式放行（fail open）。→ 參數解析處 `[ -r ] || exit 1`。
- **M3** PR body 回歸表「其餘 NONE」不實：`init_schema`、`harden_default_privileges` 實測皆 BREAKING（後者完全未提）。→ handoff 逐檔更正。

**Informational（handoff 逐條處置）**
I1 `public . fn` 點旁空白逃 B4（修：fn_name 容忍空白）；I2 串接式動態 SQL 逃得掉——檔頭與 §6/§7「execute 動態 SQL 都認得」屬過度宣稱（修文件：僅限單一字面值）；I3 欄名 `type` 的 `alter column type set default` 誤報 DESTRUCTIVE（記已知限制）；I4 :132 `git show` 失敗清單靜默殘缺（修：檢查 pipeline rc）；I5 效能 classify 每句 fork grep（全 repo 6.2s、--base 實務 1–2s，可接受；記後續）；I6 UTF-16 放行（非實際風險，記）；I7 CRLF `\r` 殘留（cosmetic）；I8 §7「沒清單一律視為既有」與 `--base` 實際（視為新）措辭不符（修文件）；I9 §7 ✅ hook＋CI 但 hook 只涵蓋 API.md 半邊（改 ⚠️ 混合或列內註明）；I10 自測盲區與實作盲區重疊——修 B1/B2 必補樣本；I11 push-gate header 順修 CLAUDE.md→COLLABORATION.md（登記、不越界）。

**已驗證通過**：兩支自測 bash 3.2 全綠（51+13）；繞法 (a)–(g) 大多如預期；CI 四情境＋DESTRUCTIVE 情境；push-gate 第 5 步兩方向、缺 ref fail closed；rc 未被吞；merge-tree 對 main 無衝突。

**R2 進場**：B1/B2/M1/M2 落地＋樣本補齊＋I1/I4 修＋I2/I8/I9 文件修正＋M3 表更正；R2 只驗這些＋重跑 (a)–(g) 與 B1/B2 樣本。

### 2026-08-24T21:04:33.798Z · Chih-Lin Yeh · `33c67c5f`

## merge-reviewer R2 — PR #78 @ `21e6262` → APPROVE（orchestrator 代貼＋裁決）

R1 九項逐條親跑全 PASS：B1（末句無分號、無結尾換行、`--base`、跨檔 `from authenticated` 不被蓋）；B2 兩向（`-- a /*`…`-- b */` 中間全抓、`/* -- */ drop` 抓、多行塊註解內不抓）；M1 同句兩級（含 `if exists`、既有函式本體含 drop）；M2 exit 1；I1 四種空白點寫法收斂；I4 移走 base blob 讓 `git show` 真失敗 → rc=1 不靜默；M3 回歸表獨立重跑逐檔吻合；文件措辭全 repo 0 命中舊句；自測 64＋13 綠、自建 16 組繞法無退化；merged tree（含 #81/#79）6 支自測全綠；PR body 無 LS-56 文字、兩支 body gate 皆 rc=1（無標記）。

**Findings（不擋合併，但併入前順手修）**
- **F1 minor（R1 引入）** `:181` `s/^+++ .*/;/p` 把「內容以 `++` 開頭的新增行」誤當檔案邊界：`alter column body set default '\n++ x\n', alter column body type text;` 在 `--base` 模式（CI／push-gate 走這條）0 命中，檔案清單模式與 R1 前都抓得到。→ **修**：邊界錨定改路徑相關（`s|^+++ [^ ]*supabase/migrations/.*|;|p`）或改逐檔取 diff；補樣本。
- **F2 info** 字面值內 `/*` 跨檔吃掉後續語句（a 檔 `comment … is 'see /* details'` 讓 b 檔 `drop table` 消失）→ 與 F1 一併「逐檔 normalize」解掉，或檔頭已知限制補「跨檔放大」。
- **F3 info** `--known-functions <目錄>` 過 `[ -r ]` 後 grep rc=2 → fail open → 改 `[ -f ] && [ -r ]`；補樣本。

**Orchestrator 裁決**：F1/F3 一行級修法，F2 隨 F1 逐檔化順解——併入前修，補三組樣本（64→67），輕量 R3 只驗這三項後併入 main。

**LS-60 執行面**：本 reviewer 仍只有 Bash/Read、system prompt 缺新句 → 定義檔為 session 啟動快取，已記 LS-60。

### 2026-08-24T21:16:20.418Z · Chih-Lin Yeh · `b423265d`

## merge-reviewer R3（輕量核銷）— PR #78 @ `79da483` → APPROVE

範圍：只驗 R2 三項（F1／F2／F3）＋回歸，不重審 R1/R2 已過的面向。R2→R3 diff 只動 `migration-breaking-check.sh`（+60/-23 行）與 `.test.sh`（+24），scope 乾淨、無順手改動。所有證據皆為本 reviewer 在自建臨時 repo 獨立重跑（不依賴作者 `.test.sh`），並以 R2 版腳本（`21e6262`）當對照組確認「修之前真的會漏」。

### 逐項核銷

**F1 PASS — `--base` 改逐檔 diff、只取第一個 `@@` 之後的 `+` 行**
- 指定樣本 `alter table public.t\n  alter column body set default '\n++ x\n', alter column body type text;` 在自建 repo `--base` 模式：R3 → `DESTRUCTIVE  alter table public.t alter column body set default ' ++ x ', alter column body type text`；R2 對照 → **無輸出 rc=0**（漏報如 R2 F1 所述）。
- 「真正檔頭 `+++ b/…` 與內容 `+++ x` 同時存在」：內容行 `+++ b/supabase/migrations/999.sql` 寫在字面值中間、橫跨一個 REVOKE authenticated。R3 完整輸出整句 `BREAKING`，未被切斷；真正的 `+++ b/…` 檔頭因位於首個 `@@` 之前已被排除，未混入語句。（附帶確認：R2 在此樣本剛好也沒被騙——因為內容行在 diff 中呈現為 4 個 `+`，不匹配 `^+++ `；R2 的破口確實只在 `++` 開頭那一種。）

**F2 PASS — 逐檔 normalize，R1 的邊界補 `;` hack 已移除**
- a.sql `comment on table public.t is 'see /* details';` ＋ b.sql `drop table public.users;\n/* cleanup */`：`--base` 與檔案清單兩模式 R3 皆輸出 `DESTRUCTIVE drop table public.users`；R2 兩模式皆 **無輸出**。
- hack 移除確認：腳本內已無 `printf '\n;\n'` 邊界補分號。
- 跨檔黏句樣本仍通過（自建）：A 檔 `revoke insert on public.t from authenticated`（無分號、無結尾換行）＋ B 檔 `revoke select on public.u from public, anon;` → 兩模式皆正確只報 A 為 `BREAKING`，未被 B 的 `from public, anon` 蓋掉。

**F3 PASS — `[ -f ] && [ -r ]`**
- `--known-functions <目錄>` → rc=1 fail closed；R2 對照 `grep: Is a directory` 但 **rc=0 放行**。
- 檔案清單模式輸入為目錄 → rc=1；R2 對照 `cat: Is a directory` 但 **rc=0**。
- 附帶（僅告知，不影響任何呼叫端）：`[ -f ]` 同時排除 `/dev/null` 與 process substitution `<(…)`，兩者現在皆 rc=1。實際呼叫端（`ci.yml:129`、`push-gate.sh:62`）只用 `--base`、不傳 `--known-functions`，`.test.sh` 用一般檔，故無影響。

### 回歸

| 項目 | 結果 |
|---|---|
| 兩支自測 bash 3.2（`3.2.57`） | `migration-breaking-check` rc=0、68 組全 ✓；`breaking-section-check` rc=0、13 組全 ✓；0 個 ✗ |
| `origin/main` 10 檔累積分級 | 獨立重算（逐檔跑、既有函式清單累積 26 個）與 R2 表**逐檔吻合**：init_schema B1／triggers 0／rls_policies 0／harden_default_privileges B1／join_approval B3／fk_reverse_indexes 0／storage_policies 0／invites_write_path **D2+B1**／diaries_write_path_and_timeline B6／albums_comments_owner_scope B2 |
| R1 B1 樣本 | `drop table public.families`（無分號、無結尾換行）→ `DESTRUCTIVE` |
| R1 B2 樣本 | `-- a /*` … `-- b */` 之間 DROP＋REVOKE 兩句全抓；反向 `/* -- */ drop table public.z;` 也抓 |
| 錯誤路徑 | 移走 base blob（`show` 路徑）→ rc=1；移走 head blob（新增的 `diff` 路徑）→ rc=1。兩者皆有明確 `✗` 訊息、不靜默 |
| 真實 repo layout 端到端 | `--shared` clone、在 hotfix branch 上合成 `drop policy … ; alter policy … using (false);` migration → `--base origin/main` 正確輸出 `DESTRUCTIVE`＋`BREAKING`，耗時 0.15s（無效能退化） |
| PR 分支自身 | LS-53 worktree 跑 `--base origin/main` → 0 命中 rc=0（本 PR 不動 migration，與 body 一致） |
| merge-tree | `git merge-tree --write-tree origin/main origin/hotfix/LS-53-breaking-migration-detector` rc=0、`cb2bfc1…`，**無衝突**（main 現為 `a974f26`） |

### Finding（1 項，不擋合併）

**G1 minor（R3 新引入，fail-open 回歸）** `scripts/gates/migration-breaking-check.sh:172、192`
`git diff --name-only` 預設 `core.quotePath=true`，檔名含非 ASCII 位元組時輸出**加引號的 C-escape 形式**；R3 把它當 pathspec 餵回 `git diff … -- "$f"` → 對不上任何路徑 → 空 diff、rc=0 → **該檔整份不分級，靜默放行**。

失敗情境（已重現）：臨時 repo 新增 `supabase/migrations/001_日記.sql`，內容 `drop table public.secret;` ＋ `revoke insert on public.t from authenticated;`
- R3 `--base HEAD~1` → **無輸出 rc=0**
- R2 `--base HEAD~1` → `DESTRUCTIVE` ＋ `BREAKING`（R2 走整目錄單次 diff，檔名無關）

嚴重度為 minor 的理由（比照 R2 對 F1 的定級）：需要一個違反命名慣例、且在 PR 檔案清單肉眼可見的非 ASCII migration 檔名；`supabase migration new` 產不出這種名字。含空白與 `*` 的檔名經實測正常（git 不對這兩者加引號）。另註：**該檔一旦進了 base，之後每次跑都會硬紅**（`git show "$base:<quoted>"` fatal → rc=1）——這條 R2 也一樣，屬既有行為，不是 R3 新增；R3 新增的只有「引入該檔的那個 PR 被靜默跳過」這一格。

建議修法（一個 flag，已驗證有效）：兩處列檔改 `git -c core.quotePath=false ls-tree/diff --name-only …`（實測輸出 `supabase/migrations/001_日記.sql` 未加引號），或改 `--name-only -z` 搭配 `read -r -d ''`。順帶可讓既有的 base 側硬紅一併消失。

### Verdict

**APPROVE** — 無 blocker、無 major。F1／F2／F3 三項皆以 R2 對照組確證修好，回歸全綠、分級表未漂移、merge-tree 無衝突。G1 建議比照 R2 三小項的處理方式併入前順手修（一個 flag）或另開小票登記。

**未看的範圍（明講）**：本輪未重審 R1/R2 已驗過的規則正確性（D1–D4／B1–B6 語意）、`breaking-section-check.sh` 本體、`ci.yml`／`push-gate.sh` 接線與 COLLABORATION 文件措辭——R2→R3 未觸及這些檔（diff 只有兩個檔），僅以「自測全綠＋分級表吻合＋端到端一次」做涵蓋。

**收工證明**：`git -C .claude/worktrees/LS-53 status --porcelain` → 空（HEAD `79da483`）。主 repo 僅 `?? .claude/memory/`（session 開始前既存，非本輪產生）。本輪只讀不改。

### 2026-08-24T21:22:35.080Z · Chih-Lin Yeh · `6eb67d61`

PR #78 已併入 main（R3 APPROVE＋G1 quotePath 一 flag 補修 `8dc8ca9`，diff 只 2 檔 +11/−3，CI 四 job 綠）。back-merge development／test 與 #80（LS-56）同批。Done 條件：back-merge 落地＋收尾 lesson（本票 lesson：gate 自身被打三輪才收斂——末句無分號、註解詞法順序、diff 檔頭錨定、quotePath——每一項都是「規則對、實作邊界錯」，自測樣本從 51 → 69）。

### 2026-08-24T22:11:39.345Z · Chih-Lin Yeh · `0bcc9a25`

## 上線後首例回饋（PR #85 LS-58，R2 reviewer）
gate 對 LS-58 migration 判 7 條 BREAKING、無 DESTRUCTIVE——正確攔到 `ALTER POLICY … (false)` 與 `REVOKE … FROM authenticated`（LS-45 舊 grep 全部漏掉的那類）。**假陽性一例**：`comment on table … is '…ALTER POLICY…'` 的說明文字被算成 BREAKING（設計上不剝字面值以認得 `execute '…'`）。方向 fail-safe，今日無害；若同型態命中 `drop table`／`truncate` 會變假 DESTRUCTIVE（硬擋、需本人核可）。處置：登記已知限制（檔頭已列「字面值不剝」）；若日後假 DESTRUCTIVE 真發生，再評估「只在 `execute` 後的字面值內偵測」的收窄。

