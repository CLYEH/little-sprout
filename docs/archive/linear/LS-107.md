# LS-107 Task：LS-18 iOS（A）——首次進入三岔路＋建立家庭（05）＋邀請家人（07）：Owner 路徑

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | lane:ui |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M1 登入與家庭 |
| 估點 | — |
| 建立 | 2026-08-25T23:31:26.898Z |
| 開始 | 2026-08-25T23:32:42.954Z |
| 完成 | 2026-08-31T05:22:33.255Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-107/taskls-18-iosa-首次進入三岔路建立家庭05邀請家人07owner-路徑 |
| 父票 | LS-18 Story：建立家庭與邀請加入 |
| 關係 | related→LS-91, blocks→LS-108, related→LS-90, related→LS-46, related→LS-33, related→LS-37, related→LS-6, related←LS-190, related←LS-205, related←LS-188, related←LS-197, related←LS-176, related←LS-95, related←LS-113, related←LS-115, related←LS-112, related←LS-103, related←LS-96, related←LS-111, related←LS-110 |

## 描述

## 來源

[LS-18](https://linear.app/little-sprout-app/issue/LS-18/story建立家庭與邀請加入) Story 的 iOS 實作，拆兩段串行（ui lane WIP 1）。設計稿已核可：[LS-46](https://linear.app/little-sprout-app/issue/LS-46/設計定稿合併c-基準使用者指定調整-designlittlesproutpen) 第 11 輪 APPROVE＋使用者 12 項裁決（[LS-18](https://linear.app/little-sprout-app/issue/LS-18/story建立家庭與邀請加入) comment `31d6e4e1`「實作進場條件」）；後端 [LS-33](https://linear.app/little-sprout-app/issue/LS-33/taskls-18-後端-邀請加入申請與審核的-schemarpcrls)／[LS-37](https://linear.app/little-sprout-app/issue/LS-37/db-安全強化invites-insert-policy-收斂-唯一寫入路徑改-create-invite-rpc)／[LS-90](https://linear.app/little-sprout-app/issue/LS-90/taskls-18-後端-邀請碼改-6-碼32-字yuan表30-bitls-89-裁決-acreate-invite驗證正則測試apimd)（6 碼）已上線（`docs/API.md`）。本票＝**Owner 路徑**；[LS-18](https://linear.app/little-sprout-app/issue/LS-18/story建立家庭與邀請加入)（B）＝加入路徑（輸碼／等待核准／審核）。

## Scope

1. **首次進入三岔路**（登入後無家庭）：建立家庭／輸入邀請碼／（deep link 承接由 B 接）——iPhone／iPad 版，整卡可點；依 `design/littlesprout.pen` 對應板（以節點名稱定位，見 [LS-46](https://linear.app/little-sprout-app/issue/LS-46/%E8%A8%AD%E8%A8%88%E5%AE%9A%E7%A8%BF%E5%90%88%E4%BD%B5c-%E5%9F%BA%E6%BA%96%E4%BD%BF%E7%94%A8%E8%80%85%E6%8C%87%E5%AE%9A%E8%AA%BF%E6%95%B4-designlittlesproutpen) comment `ba6dbabb`）。
2. **建立家庭（05）**：名稱輸入、送出中態、Family Caption 用 `$print-ink`；走 INSERT+RETURNING（[LS-6](https://linear.app/little-sprout-app/issue/LS-6/phase-0-2supabase-%E5%B0%88%E6%A1%88schema-migrationrls-policies)）；成功後 root routing 進家庭（`AuthStore`／family store 依既有架構，不可重建 store 而不重建 service——[LS-18](https://linear.app/little-sprout-app/issue/LS-18/story%E5%BB%BA%E7%AB%8B%E5%AE%B6%E5%BA%AD%E8%88%87%E9%82%80%E8%AB%8B%E5%8A%A0%E5%85%A5) comment `1fce1645`）。
3. **邀請家人（07）**：空／載入／已產生三態；6 碼英數 3+3 分組、`$fs-code` 60pt（AX3 檔位 72pt）「唸得出來」（原寫 42pt 為 8 碼時的 AX3 應變值，R1 I1 更正）；角色只給 member／viewer 附白話說明；重新產生需二次確認；「核准必開」為**鎖定狀態列（無開關）**，07a 已關閉態不存在；文案講清楚「名額不退還，需更多請重新產生」（`used_count` 不退還，comment `4f10699c`）；分享（複製碼／系統分享表 `littlesprout://invite/<code>` 連結）。
4. 錯誤歸層依 `docs/API.md` §5（LS016 retryableSystem、LS022 rejected）；`AppError` 既有文法。
5. 不做：輸碼頁（06）、等待頁（06d）、審核清單——B 票。

## 驗收

* **全新帳號無任何邀請碼能自建家庭並邀請他人**（§9-C5）：模擬器走完 登入 → 三岔路 → 建立家庭 → 邀請家人 → 產生碼（截圖每一步，一般＋AX3；iPad 三岔路一張）。
* 產生的碼用本機 SQL 唯讀查 `invites` 對得上（6 碼、期限、次數）。
* 單元測試：family／invite store 的狀態機（送出中／成功／錯誤歸層）≥6 條；既有 157 tests 綠；swiftlint strict 綠；xcodegen 0 diff。
* feature 從 development；ui lane；PR 一支（可分段 commit）。

## 留言（12）

### 2026-08-26T01:01:49.741Z · Chih-Lin Yeh · `ff1a4950`

## merge-review R1 — PR #170 · REQUEST_CHANGES

Head `677e8695dd7047e2ae56a6af54abc11448db0475`。全文見 [PR comment](https://github.com/CLYEH/little-sprout/pull/170#issuecomment-5419113307)。

**2 blocker**
- **F1（race）**：`FamilyStore` 不隨帳號切換重置。A 登出、B 在同一台裝置登入時，`AuthenticatedGate` 的 `.task` 因 `lookupState` 已是 `.success` 而跳過查詢 → B 進到 A 的家庭、設定頁顯示「葉家」，且 `latestInvite` 沒清 → **B 看到 A 的有效邀請碼**。反向（A 無家庭、B 有）則讓 B 落回三岔路再建一個家庭，`family_members` 主鍵 `(family_id, user_id)` 擋不住一人多家。修法：加 `FamilyStore.reset()` 由 `session?.userID` 變化驅動，**不重建 store**（`1fce1645`）。
- **F2（正確性／安全文案）**：「重新產生後，舊的邀請碼就不能再用了」是假的。`create_invite` 只 INSERT，**沒有 revoke_invite**，撤銷唯一路徑是 DELETE（LS-37 註記白紙黑字），而本 PR 從不呼叫。owner 因外流而重新產生時，app 告訴他漏洞堵上了，實際舊碼還能用到 7 天到期或滿 5 次。⚠️ **這是設計稿假設了後端沒有的能力**（`.pen` `NnIbh`／`lkksL` 就是這句），要一併回報設計 owner，修法需你裁決（刪舊碼／改文案／另開 `revoke_invite` 後端票）。

**3 major**：F3 票文明列的「名額不退還」文案未實作、也沒進已知缺口（且與核可稿衝突，該 surface 而非沉默擇一）；F4「還可用 N 次」顯示的是 `max_uses` 且永不更新，重開 app 還會退回空狀態導致累積多支有效碼；F5（**PLAUSIBLE**）`.code` 在 AX3 實得約 92pt 而 `$fs-code` 的 ax3 檔位是 72pt，邀請碼很可能截斷——AX3 未實測，建議用 `xcrun simctl ui content_size` 補圖再判。

**5 minor**：F6 `test_resetCreateFamilyState_...` 名不副實（只測 idle→idle）；F7 in-flight 測試的等待迴圈無上限＋`try?` 吞取消；F8 `refreshMyFamily` 缺 in-flight guard；F9 捲動時內容與返回鍵重疊（根因是既有的 `ScrollableFillView`，本 PR 首次暴露）；F10 複製回饋 Task 未取消。

**informational 11 條**，兩條要點：
- **I1 更正 agent 自列缺口**：邀請碼 60pt 是**對的**——`$fs-code` = 60/72，票文的 42pt 已被 LS-46 R11 取代，該更新的是票文。
- **I2**：`fork` 沖印品池值與 Handoff Notes R4 表格不一致，但我實際解析 frame `RxXvq` 節點 `ppXvK`，**稿面值就是實作採用的值**——是稿面與 Handoff Notes 自相矛盾，不是 code bug，請設計 owner 對帳。
- I4 `ensureProfileExists` 只掛 `createFamily`，LS-108 加入路徑會撞同一個 FK，建議升級成登入流程（另票）；I6 深連結佔位確認無誤吃 URL。

scope 判定通過（`SettingsView` 入口與 `RootView` 改動都在票文內；三個 token 擴充保留預設值）。效能／平行面無 finding。

CI：`ci`／`db`／`lint` 三綠；`rules` 紅＝PR body `Design:` 欄位為空，非程式碼問題（orchestrator 認領）。

未實跑：不 boot 模擬器／不跑 xcodebuild（依派工）；AX3 未實測；DB 未連線查詢（F2／F4 由唯讀分析 migration 得出）；截圖只看 02／04／06。

### 2026-08-26T02:06:35.753Z · Chih-Lin Yeh · `0e7eb5db`

## Orchestrator — 收工接續點（2026-08-26 10:12，使用者額度將盡）

PR #170 head `2990164`（R1 fix 兩支：F1 store reset／F2 DELETE 撤舊碼／F4／F8；F3／F5／F9／F10），CI 四綠，尚未 R2。**下一步**：讀 PR 最後 comment 的處置清單 → 派 merge-reviewer R2（增量 `677e869..2990164`）→ 併 development → 晉升 test → QA（§9-C5 全流程＋AX3）。worktree `.claude/worktrees/LS-107` 保留。

### 2026-08-31T01:10:48.209Z · Chih-Lin Yeh · `9dfd1a9c`

## merge-review R2（增量）— PR #170 · REQUEST_CHANGES

**Head**：`2990164e2029186db1029b1213f5f0c91f60d6dd`（審查範圍 `677e869..2990164`，2 commits、15 檔、+895/-164）
**Verdict：REQUEST_CHANGES**（0 blocker、1 major、5 minor、7 informational）
CI 對帳：`ci`／`db`／`lint`／`rules` 四綠（run 32919470889）。pbxproj 增量只有 3 支新測試檔的三段 entry，無其他專案設定變動。

R1 十條 finding **逐條落實**，修法都走 R1 建議的方向（F1 不重建 store、F5 採 token clamp、F9 只補本畫面不動共用元件），註解把「為什麼」寫在案上。擋下來的不是舊帳，是 **F4 修法自己新開的一個未受保護的共享狀態寫入**——它在一個窄但真實的窗口裡把 F2／F4 要消滅的後果（多支碼同時有效、app 說作廢其實還活著）原樣放回來。

---

## N1 — major · race：`refreshLatestInvite()` 與 `createInvite()` 共寫 `latestInvite`／`createInviteState`，兩者之間沒有任何協調

`LittleSprout/Services/Family/FamilyStore.swift:217-226`
`LittleSprout/Features/Family/InviteFamilyView.swift:70-75`（onAppear 的 unstructured `Task`）
`LittleSprout/Features/Family/InviteFamilyView.swift:35-39`（`phase`）、`:196-204`（`.empty` 的可按 CTA）

`refreshLatestInvite` 不設任何 submitting 狀態，而 `phase` 只認 `createInviteState.isSubmitting`。所以**進場查詢的整段 RTT 內，畫面顯示的是 `.empty`——連同那顆已啟用的「產生邀請碼」**；查詢回來後又無條件 `latestInvite = ...` 覆寫（:220），失敗則直接把 `createInviteState` 蓋成 `.failure`（:223）。三個具體失敗情境：

**A（覆寫剛建立的碼）**：owner 進 07 → onAppear 發查詢（在飛）→ 在 RTT 內按下「產生邀請碼」→ 此刻 `latestInvite` 仍是 nil → `createInvite` 的 :182 撤銷分支被跳過 → DB 多一支 C2（既有的 C1 沒被撤）→ 查詢回來把 `latestInvite` 覆寫成 **C1** → 畫面顯示 C1，C2 隱形、而且此後**永遠撤不掉**（重新產生只刪 `latestInvite.id`）。結果＝R1 F4 要消滅的「多支同時有效、每支各帶 5 個名額」與 R1 F2 要消滅的「app 說舊碼作廢、其實還活著」同時復活。

**B（不需要任何交錯，純序列）**：進場查詢失敗（離線一瞬／PostgREST 5xx）→ `createInviteState = .failure` → 畫面同時是「錯誤列＋空狀態＋可按的產生鈕」，但 app 其實**不知道**這個家庭有沒有碼 → owner 按下去 → 又建一支，舊碼照樣沒撤。同一個檔案裡 `RootView.FamilyLookupFailedView` 已經示範了正確處理（查不到就給重試、不讓使用者在未知狀態上做破壞性動作），07 這裡沒沿用。

**C（in-flight guard 被外部寫入解除）**：查詢慢且最後失敗 → 使用者已按下產生（`createInviteState = .submitting`）→ 查詢的 catch 把狀態蓋成 `.failure` → `:175` 的 `guard !createInviteState.isSubmitting` 形同失效 → 使用者見到錯誤再按一次 → 兩支 `create_invite` RPC 併發 → 兩支碼，`latestInvite` 取決於誰後回。

**建議修法**（三選一或並用，都很小）：
1. 給查詢自己的狀態——票文 Scope 3 本來就要求「空／載入／已產生**三態**」，目前「載入」只綁在建立動作上，查詢期間根本沒有載入態。查詢中不顯示可按的產生鈕，失敗顯示重試（比照 `FamilyLookupFailedView`）。
2. `refreshLatestInvite` 回寫前先驗證結果仍相關：比對 await 前後的 `myFamily?.id`，且 `createInviteState.isSubmitting`／已 `.success` 時丟棄結果、不覆寫、不改 `createInviteState`。（順帶解掉一個更窄的變體：A 的 07 查詢在 B 登入、store 已被 `syncOwner` reset 後才回來，把 A 的碼寫進 B 的 store。）
3. 撤銷改成撤掉該家庭**所有**還有效的碼——`fetchLatestActiveInvite` 本來就撈了 5 筆候選，只丟掉了其餘 4 筆。這才對得起確認對話框那句「舊的邀請碼就不能再用了」，也讓上面幾種殘留自動收斂。

（未實機重現：依派工不 boot 模擬器。但這不是猜測的時序——`refreshLatestInvite` 對兩個共享狀態的無條件寫入是靜態就讀得出來的，情境 B 甚至不需要任何交錯。）

---

## minor

**N2 · 撤銷會 cascade 掉待審申請，UI 沒講，而且新文案還在勸使用者去按** — `SupabaseFamilyAPIClient.swift:167-186`；`supabase/migrations/20260823010000_join_approval.sql:90-91`（`join_requests_invite_same_family_fkey ... on delete cascade`）。後端把這定義成撤銷的預期語意（write-path §3：「撤銷連帶讓該碼底下的待審申請一起消失」），所以不是實作走錯路；問題在**揭露**：確認對話框只說「舊碼不能再用」，而 F3 新增的「名額不退還，需要更多名額請重新產生。」（`InviteFamilyView.swift:236`）等於主動叫 owner 去按那顆會吃掉待審申請的鈕。今天沒有觸發點（申請只能由 LS-108 的加入路徑產生），但 LS-108 一落地，owner 按重新產生就會靜默消滅申請、申請人卡在 06d 等一個永遠不會出現的結果。建議：文案補一句，或撤銷前檢查 pending 申請；**至少要記進 LS-108**。

**N3（PLAUSIBLE）· `.toolbarBackground(.visible)` 的視覺可能與紙感稿面／同 stack 的 05 不一致** — `InviteFamilyView.swift:69`。全 repo 只有這一處用 toolbarBackground，且沒帶顏色＝系統預設材質；對照 r2 的 05 AX3 截圖，同一條導覽堆疊裡 05 是「沒有任何 bar 底、浮動圓形返回鍵」的做法。07 頂端多一條系統色／模糊帶＋分隔線很可能突兀。我沒跑模擬器，只從截圖與程式碼推斷 → **請 QA 用截圖判**；若不符改成帶紙色（`.toolbarBackground(Color.lsBackground, for: .navigationBar)`）。

**N4 · 本次改動製造的孤兒** — `LittleSproutTests/FamilyStoreTests.swift:16-27`：`makeInviteRecord` 在 invite 測試搬去 `FamilyStoreInviteTests`（那邊自己有一份）後全檔 0 呼叫端（grep 命中數＝1＝宣告本身）。SwiftLint 預設規則抓不到未使用的 private method（要 `swiftlint analyze`），所以 lint 綠不代表沒有。刪掉即可。

**N5 · `syncOwner(to: nil)`／`reset()` 在 production 沒有呼叫端，註解卻描述了那條路徑** — `RootView.swift:39-44,66-68`、`FamilyStore.swift:99-118`。`AuthenticatedGate` 只在 `authStore.isAuthenticated()` 為真時存在（`isSessionValid` 要求 `session != nil`），登出時整個被移除、`.task` 只會被**取消**，不會以 nil 重跑一次 → 註解「含登出時變 nil」與 `test_syncOwner_toNil_...` 釘的是一條 production 走不到的路徑（安全性仍成立：B 登入時 id 不同 → reset，F1 的失敗情境確實堵住了）。實際殘留：(a) 登出後 A 的 `myFamily`／`latestInvite` 留在記憶體到下一位登入；(b) 同一位使用者登出再登入不重查（`guard userID != ownerUserID`），沿用上一輪快照——與 R1 前同行為，非退步。建議：`SettingsView.signOut` 成功後呼叫 `familyStore.reset()`（讓那條路徑真的存在、測試才有意義），或把註解與測試改成符合實況。

**N6 · 「未過期」這條語意沒有被任何測試釘住** — `LittleSproutTests/SupabaseFamilyAPIClientInviteTests.swift:77-99`。`test_fetchLatestActiveInvite_*` 只斷言 path／method，沒驗 query；「還有名額」有記憶體端過濾＋專門測試，「未過期」卻**只靠** `.gt("expires_at", now)` 那一行，拿掉它三條測試照樣綠，而後果是過期碼被當成有效碼顯示給 owner。比照同檔 `createInvite` 已經在驗 wire 的作法，加一條 `query.contains("expires_at=gt.")` 斷言即可。

---

## R1 十條逐條核對

| R1 | 處置 | 核對結果 |
|---|---|---|
| F1 blocker 換帳號殘留 | `ownerUserID`／`reset()`／`syncOwner`＋`.task(id:)`＋3 測試 | **已修**，走 R1 指定方向（不重建 store）。殘留見 N5 |
| F2 blocker 舊碼未失效 | 裁決 (a)：`revokeInvite` DELETE、0 列受影響轉 `.rejected`、先撤後建、撤銷失敗不建 | **已修**（`invites_delete` 對 owner 開放、`invites_select` 也開放所以 `.select()` 拿得回列，我核過 policy）。殘留見 N1／N2 |
| F3 major 名額不退還文案 | 07c 補一句 `.note` | **已修**（見 I4 稿面回填） |
| F4 major 還可用 N 次假值 | `usedCount`／`remainingUses`＋`refreshLatestInvite` 進場查 | **已修**，但引入 N1 |
| F5 major(PLAUSIBLE) AX3 截斷 | `maxScaledSize`＝72 clamp＋4 測試 | **已修**（採 R1 提供的第二選項）。07 AX3 截圖仍缺 → 留 QA |
| F6 minor 測試名不副實 | 拆成 clearsFailure／doesNotClearSuccess | **已修**，兩條各自真的會失敗 |
| F7 minor 無界輪詢 | 200 次上限＋`XCTFail` | **已修** |
| F8 minor in-flight guard | `refreshMyFamily` 補 guard＋一條真併發測試 | **已修** |
| F9 minor nav bar 重疊 | 本畫面補 `.toolbarBackground` | **已修**，視覺待驗（N3） |
| F10 minor 複製回饋 Task | 可取消 Task＋`onDisappear` 取消＋`isCancelled` 檢查 | **已修** |

R1 的 informational I1–I11 處置清單我逐條看過，**處置合理、無異議**（I2／I3 屬設計文件對帳、I4／I5 記入 LS-96、I7 另開 LS-111、其餘無需處置）。

## informational（本輪新增，處置：I1–I3 記入 LS-96，I4 併入 LS-111，I5–I7 無需處置）

- **I1**：`fetchLatestActiveInvite` 取 `limit 5` 再記憶體篩名額——某家庭若同時有 >5 支未過期碼且最新 5 支都用罄，會回 nil（誤判成沒有碼）。正常流程（先撤後建）累積不到，只在 N1 競態或人工造資料下成立。
- **I2**：以 `expires_at` 當 `created_at` 的替身排序，前提是「一律固定 7 天」——已在 `InviteRecord` 註解白紙黑字交代；日後開放自訂期限就會壞。
- **I3**：`gt("expires_at", now)` 的 now 取自裝置時鐘，時鐘偏差會讓過期碼看起來有效（反之亦然）。與建立時算 `expiresAt` 是同一個既有假設，未擴大。
- **I4**：F3 新增的文案不在核可稿（`.pen` 07 板無此節點），性質同 rolePicker——建議併入既有設計 chore **LS-111** 一起回填稿面，避免 app 與稿面持續漂移。
- **I5（效能，無 finding）**：`createInvite` 現在是 2（首次）／3（重新產生）個 round trip，彼此資料相依、無可平行化空間，且是使用者手勢觸發的一次性動作。
- **I6（查詢面，無 finding）**：`fetchLatestActiveInvite` 走 `invites_family_idx`；`invites_select` 是 `family_id in (select private.owned_family_ids())`（函式 STABLE、`in (select ...)` ＝ initplan），**沒有** PLAN §5 禁止的 per-row 子查詢退化；無 N+1、無 OFFSET 分頁；`fetchInvite(byCode:)` 走 code 的 unique index。本輪無主執行緒圖片解碼／縮圖相關變更。
- **I7**：「還可用 N 次」是進場快照，owner 停留期間家人送出申請不會更新（`used_count` 在 `request_join` 就加一）。無 realtime，可接受。

## scope

**通過**。增量每一個檔案都對得上 R1 的某一條 finding：Typography（F5，其餘 token 一律 `nil` 保持原行為）、RootView（F1）、FamilyAPIClient／Models／Store／SupabaseClient（F2/F4/F8）、InviteFamilyView（F3/F9/F10）、Preview／Stub（協定變更的必要跟進）、pbxproj（只有 3 支新測試檔）。沒有順手重構、沒有動共用元件（`ScrollableFillView` 明確留原樣）。

## 留給 QA（R1 已留、不因缺席擋 R2）

1. **07 的 AX3 截圖**（本輪未取得，原因是本機容器爭用）——另請用**寬字母碼**實測：72pt 下 `MMMWWW` 這類全寬字母＋`tracking(4)` 仍可能超出可用寬度，clamp 沒有 `minimumScaleFactor` 兜底。
2. **多帳號端到端**：A 登出 → B 登入不得看到 A 的家庭／邀請碼；重新產生後用 SQL 對帳舊碼**真的不在** `invites`。
3. **N3 的 nav bar 視覺**（07 捲動前後各一張，對照 05）。
4. 建議加驗 **N1 情境 A**：進 07 後**立刻**按「產生邀請碼」，事後 SQL 查該家庭 `invites` 應只有一列。

## 我沒有實跑的部分

依派工不 boot 模擬器、不跑 xcodebuild（193 tests 綠採信 CI `ci` job）；未連線本機 DB（RLS／cascade 結論來自唯讀分析 `20260822120200_rls_policies.sql`、`20260823010000_join_approval.sql`、`20260823040000_invites_write_path.sql`、`20260822120000_init_schema.sql`）；AX3 只看了 r2 的 05 兩張截圖（05 版面在 AX3 正常、無截斷），07 的 AX3 未驗；`.pen` 本輪未重新解析（增量沒動設計對照，除 F3 新增文案已由實作者標明稿面無此節點——此點我採信其 R1 已建立的解析結論，未複驗）。


### 2026-08-31T01:46:17.713Z · Chih-Lin Yeh · `b2da0423`

## merge-review R3（增量）— PR #170 · APPROVE

**Head**：`8501a998381a0cd91d95129ba24cef260bc5b3d1`（審查範圍 `2990164..8501a99`，2 commits、10 檔、+491/-41）
**Verdict：APPROVE**（0 blocker、0 major、4 minor、7 informational）
CI 對帳：`ci`／`db`／`lint`／`rules` 四綠（run 33347951415，`headSha` 我核對過＝我審的 SHA）；`ci` job 日誌 `Executed 205 tests, with 0 failures` ＋ XcodeGen 漂移檢查通過，與回報一致。

R2 的 M1 我逐一走過三個情境，**三個都封死了**，而且封的方式跟我在 R2 給的三條建議一致（採 1＋2，未採 3——見 I6）。四支 race 測試不是 happy path：每一支拿掉對應的修法那一行就會紅，我逐條核過（下面列）。

---

## M1 三情境驗證

**情境 A（查詢在飛時按產生 → 多一支撤不掉的孤兒碼）：封死。**
`FamilyStore.swift:197-198` 與 `:250-251` 是**對稱**的雙向 guard，且 `FamilyStore` 是 `@MainActor`、guard 與設 `.submitting` 之間沒有任何 await ⇒ 檢查與寫入在同一個 actor job 內原子完成，不存在 check-then-act 窗口。UI 層 `InviteFamilyView.swift:215-216`（`.checkingExisting` → `EmptyView()`）連按鈕都不渲染。
測試 `FamilyStoreInviteRaceTests.swift:62-105` 用 AsyncStream gate 把查詢卡在飛行中，斷言 `createInvite` 回 nil **且底層 `createInvite` API 呼叫次數＝0**——拿掉 `:198` 那行 guard，createCallCount 會變 1、測試紅。反向測試 `:107-149` 同樣釘住 `lookupCallCount == 0` 並驗最終 `latestInvite` 是 create 寫的新碼。**兩個方向都有測試**，這點做得比我要求的多。

**情境 B（序列：查詢失敗後按產生）：封死（防線在 UI 層）。**
查詢失敗現在寫 `lookupInviteState`（`:269`），`createInviteState` 不再被污染；`InvitePhase` 把它算成 `.lookupFailed`，`actionsSection`（`:217-221`）只渲染「重試」、不渲染「產生邀請碼」，比照 `RootView.FamilyLookupFailedView`——正是我 R2 建議 1 的作法。
`InvitePhaseTests.swift:53-59` 斷言 `phase == .lookupFailed`（**不是 `.empty`**）；因為 `actionsSection` 是對 `InvitePhase` 的 exhaustive switch、且只有 `.empty` 分支渲染產生鈕，這條 case 相等斷言確實是承重的（不是裝飾）。
註：store 層本身仍允許這個呼叫（見 I1），今天只有一個呼叫端所以成立。

**情境 C（in-flight guard 被外部寫入解除）：根因消滅。**
C 的根因是「查詢的 catch 蓋掉 `createInviteState`」——狀態格分開之後這條路徑不存在了。`isResultStillRelevant`（`:278-280`）再檢查一次 `!createInviteState.isSubmitting` 當第二道。`FamilyStoreInviteRaceTests.swift:48-51` 直接斷言查詢失敗後 `createInviteState == .idle`，並在註解裡寫明「這正是情境 C 的根因」——測試釘的是**為什麼**，不只是**是什麼**。

**更窄變體（A 的查詢在 B 登入後才回來）：封死。**
`isResultStillRelevant` 比對 await 前後的 `familyID`；過期結果丟棄時把 `lookupInviteState` 歸零回 `.idle` 而不是留在 `.submitting`（`:259`／`:266`）——這點很重要，留在 `.submitting` 會讓 B **永遠**按不到產生鈕。測試 `:151-196` 兩條斷言都釘住了（`latestInvite` 為 nil ＋ `lookupInviteState == .idle`）。

R2 的 N4（死碼）／N5（signOut 補 `reset()`）／N6（wire 層 `expires_at=gt.` 斷言）三條也都落實，作法與建議一致。

---

## minor

**N1 · `重新產生一組` 在進場查詢飛行期間可按，但按下去是靜默無效** — `InviteFamilyView.swift:335`
`destructiveSection` 的 `.disabled(familyStore.createInviteState.isSubmitting)` 只擋了建立方向，**沒擋新加的 `lookupInviteState.isSubmitting`**。
**失敗情境**：owner 已產生過碼 → 返回設定頁 → 再次進入 07 → `onAppear` 的 `refreshLatestInvite` 開始飛（`latestInvite` 非 nil ⇒ `phase == .generated`，破壞性區照常渲染、按鈕 enabled）→ 使用者立刻按「重新產生一組」→ 確認對話框「重新產生」→ `generate()` → `createInvite` 在 `:198` 的 guard 直接 `return nil` → **對話框關掉、畫面完全沒變、沒有錯誤、沒有 loading**。慢網路下這個窗口是整個 RTT。無資料損害、可重按，但對長輩使用者是「按了沒反應」的無回饋死路（跟本專案 ≥44pt 點擊目標那類可用性硬約束同一個關切面）。
**建議**：`.disabled(familyStore.createInviteState.isSubmitting || familyStore.lookupInviteState.isSubmitting)`（一行）。附帶說明：直接放行其實也是安全的——查詢回來時 `isResultStillRelevant` 會因 `createInviteState.isSubmitting` 而丟棄結果——但用 disable 比放寬 invariant 保守，建議走 disable。

**N2（PLAUSIBLE）· `createInvite` 的 await 後回寫沒有 relevance 檢查，與 `refreshLatestInvite` 不對稱** — `FamilyStore.swift:204-218`（另 `createFamily:161-163` 同類）
這次只給 `refreshLatestInvite` 加了 `isResultStillRelevant`，`createInvite` 的 `latestInvite = ...`／`createInviteState = .success` 仍是無條件回寫。
**失敗情境**（五個條件的合取，所以標 PLAUSIBLE）：A 在 07 按「產生邀請碼」→ 請求掛住（不是失敗，是慢，URLSession 預設 60s 才逾時）→ A 返回設定頁 → 登出（`SettingsView:86` 的 `reset()`）→ B 在同一台裝置登入（`syncOwner` 再 `reset()` 一次並查到 B 的家庭）→ **此時 A 的 `createInvite` 才回來**，無條件寫 `latestInvite = A 的碼`／`createInviteState = .success` → B 進 07，`phase == .generated(A 的碼)`，B 在自己的查詢回來前的那個 RTT 內看得到、複製得到、分享得到 **A 家庭的有效邀請碼**。
自癒：B 自己的 `refreshLatestInvite` 回來就覆寫掉；B 若在窗口內按「重新產生」，`revokeInvite` 會因 RLS 刪 0 列而丟 `.rejected`（`SupabaseFamilyAPIClient.swift:181-183`），不會誤刪 A 的碼——**失敗是安全的**。
**這不是本次增量引入的**（R1／R2 就是這樣寫），N5 的 `reset()` 也沒有實質擴大它（真正起作用的是 B 登入時 `syncOwner` 的 reset，那條路徑本來就在）。但既然 `refreshLatestInvite` 現在有了這道檢查，兩支不對稱本身就是日後會踩的坑。
**建議**：`createInvite` 在 `await` 之後同樣比對 `myFamily?.id == familyID`（兩行，可直接複用 `isResultStillRelevant`）；`createFamily` 比對 `ownerUserID`。→ 記 LS-96。

**N3 · `InvitePhase.allowsGenerate` 沒有任何 production 呼叫端** — `InvitePhase.swift:50`
`git grep allowsGenerate` 命中 12 處：1 處宣告 ＋ 11 處在 `InvitePhaseTests`。`InviteFamilyView` 從頭到尾沒用它——真正的防線是 `actionsSection` 對 `InvitePhase` 的 exhaustive switch。它的文件註解卻寫著「R2 N1 的核心防線」，名實不符。
**後果**：這是本次增量新造的死碼（跟剛刪掉的 N4 同一類），而且會讓下一個讀者以為改 `allowsGenerate` 就能改變按鈕行為（實際不會）。測試面沒有假保證——`test_lookupFailed_..._notEmpty` 的 case 相等斷言才是承重的那條，它仍然有效——但 `test_allowsGenerate_onlyEmpty` 那 5 條斷言確實無法在 `actionsSection` 被改壞時失敗。
**建議**：二選一——(a) 讓 view 真的用它（`.empty` 分支的產生鈕改由 `phase.allowsGenerate` 決定顯示），或 (b) 刪掉 `allowsGenerate` 與 `test_allowsGenerate_onlyEmpty`，把兩條核心測試裡的 `allowsGenerate` 斷言換成 case 相等斷言（那本來就是承重的那句）。我傾向 (b)，比較誠實。

**N4 · 新測試檔又留了一個 orphan** — `LittleSproutTests/InvitePhaseTests.swift:9`
`private let familyID = UUID(...)` 全檔 0 呼叫端（`makeInvite` 用的是 `UUID()`）。跟這次剛刪掉的 `FamilyStoreTests.makeInviteRecord` 完全同一類，SwiftLint 預設規則一樣抓不到（要 `swiftlint analyze`）。刪一行即可。
（順帶：這已經是連續兩輪出現同型死碼——feature 收尾的 dead-code-sweeper 巡檢務必涵蓋 `LittleSproutTests/`，不要只掃 production target。）

---

## informational（建議處置：I1／I2／I5／I7 記 LS-96；I4 併 LS-111；I3／I6 無需處置）

- **I1**（來源 PR #170 I1 · 情境 B 的防線只在 UI 層，`FamilyStore.createInvite` 在 `lookupInviteState == .failure` 時仍會照建、不撤舊碼 · 觸發事故：LS-108 加入路徑或 deep link 新增第二個 `createInvite` 呼叫端時，保護靜默消失 · size S）：建議日後在 store 層補「未確認過既有碼就不准建立」的前置條件（例如要求 `lookupInviteState == .success`）。今天只有一個呼叫端，成立。
- **I2**（來源 PR #170 I2 · `currentError` 優先序 · `InviteFamilyView.swift:36-40`：查詢失敗與建立失敗同時存在時只顯示查詢錯誤 · 觸發事故：`latestInvite` 非 nil 時查詢失敗 → 按重新產生 → revoke 成功但 create 失敗（`latestInvite` 變 nil）→ phase 落回 `.lookupFailed`，畫面顯示「查詢邀請碼時發生問題」但實際是建立失敗；按重試會查到（已被刪的）舊碼不存在 → 落回 `.empty` → 可重新產生，**能恢復但訊息誤導** · size S）。
- **I3**：再次進場時，`onAppear` 的 `resetCreateInviteState()` 只清 create 的失敗殘影，上一輪的 `lookupInviteState == .failure` 要等 `Task` 跑起來才被蓋成 `.submitting` ⇒ 首幀可能閃一下舊的查詢錯誤列。單幀、無害，不建議為此加程式碼。
- **I4**（併入 LS-111）：本輪新增四段核可稿沒有的 user-facing 文案與兩個新狀態殼——「正在確認你的家庭有沒有邀請碼，請稍等一下。」「查詢邀請碼時發生問題，請重試一次。」「正在確認邀請碼…」「重試」（`InviteFamilyView.swift:117-123`／`:136-145`／`:221`）。實作者已在註解說明定位同 `FamilyLookupFailedView`（純技術兜底、不套沖印品母題），我認同這個判斷、不列 finding；但性質同 R2 I4（F3 文案）與 rolePicker，建議一併在 LS-111 回填稿面，避免 app 與 `.pen` 持續漂移。
- **I5**（來源 PR #170 I5 · `refreshMyFamily` 同樣缺 await 後 relevance 檢查（`FamilyStore.swift:141-151`）· 觸發事故：`reset()` 與飛行中的 `refreshMyFamily` 交錯會把舊使用者的 `myFamily` 寫回已歸零的 store；今天不可達（能按到登出鈕就代表 `lookupState == .success`、沒有查詢在飛），且下次登入 `syncOwner` 會再 reset 一次自癒 · size S）。
- **I6**：我 R2 的建議 3（撤銷時刪掉該家庭**所有**還有效的碼）未採用——採 1＋2 已足以封住三情境，這是合理取捨，無異議。殘留：修法之前（含 R1／R2 版本）若已經在真機／模擬器造出孤兒碼，app 端沒有回收路徑。目前尚未上架、DB 可重建，不建議為此加程式碼。
- **I7**：`.checkingExisting` 期間畫面上**一個可按的動作都沒有**（`actionsSection` 是 `EmptyView`）。請求掛住時最壞要等 URLSession 預設 60s 逾時才落到「重試」。這是「不讓使用者在未知狀態上做破壞性動作」的必要代價，但值得 QA 用 Network Link Conditioner 感受一下 60s 空窗是否需要補一顆「取消」。

**效能／平行**：本增量沒有任何 DB／RLS／查詢面變更（`SupabaseFamilyAPIClientInviteTests` 只加了一條 wire 斷言），R2 的 I6 結論不變——無 per-row 子查詢退化、無 N+1、無 OFFSET 分頁。新的互斥 guard 確實把查詢與建立序列化，但兩者資料相依（要先知道有沒有舊碼才能決定要不要撤）、且都是使用者手勢觸發的一次性動作，無可平行化空間，不列 finding。`phase` 是 computed property、每次 body 求值建兩次 enum（`phase` 自己 ＋ `currentError` 內再算一次），成本可忽略。

**scope**：**通過**。10 個檔每一個都對得上 R2 的某一條 finding：FamilyStore／InvitePhase／InviteFamilyView（M1）、SettingsView（N5）、RootView（純註解訂正，N5）、FamilyStoreTests（N4 刪死碼）、SupabaseFamilyAPIClientInviteTests（N6，只加一行斷言）、兩支新測試檔、pbxproj（只有 3 支新檔的 build/ref/group/sources entry，無其他專案設定變動）。沒有順手重構、沒有動共用元件。

## 留給 QA（沿用 R2 清單，新增兩項）

1. R2 的四項全部仍然有效（07 AX3 截圖＋寬字母碼、多帳號端到端、N3 nav bar 視覺、情境 A 的 SQL 對帳）。
2. **新增**：慢網路（Network Link Conditioner）進 07，確認「正在確認邀請碼…」的等待殼版面正常、且**已產生態下**進場時按「重新產生一組」的行為（N1 的死點擊——修了就該有 disable 灰態，沒修就會看到按了沒反應）。
3. **新增**：查詢失敗態（開飛航模式進 07）的版面——錯誤列＋「重試」，且**不得**出現「產生邀請碼」。

## 我沒有實跑的部分

依派工不 boot 模擬器、不跑 xcodebuild（205 tests 綠採信 CI `ci` job 日誌，已核對 `headSha`）；未連線本機 DB（RLS／`revokeInvite` 0 列語意來自唯讀讀取 `SupabaseFamilyAPIClient.swift:173-187` 與 R2 已建立的 policy 分析結論，本輪未複驗 migration）；`.pen` 本輪未解析（I4 的「稿面無此節點」採信實作者標注）；N2 的時序未實機重現（故標 PLAUSIBLE）。


### 2026-08-31T01:48:21.569Z · Chih-Lin Yeh · `3fe35c67`

**R3 APPROVE → 已併 development（2026-08-31）**：PR #170 merge commit `c490731`；merge-review=success 貼於 head `8501a99`（R3 comment `b2da0423`）。→ QA。

QA 清單（R2/R3 累積，qa 派工時逐項）：① 07 邀請頁 AX3 截圖（用 `MMMWWW` 類寬字母碼驗 clamp）；② 多帳號端到端＋SQL 對帳（撤舊碼後 DB 實況）；③ N3 nav bar 視覺（`.toolbarBackground` 是否突兀，不符改 `Color.lsBackground`）；④ 慢網路下 `.checkingExisting` 版面＋N1 死點擊行為記錄；⑤ 飛航模式 `.lookupFailed` 版面不得出現產生鈕。

informational 處置：N1/N2/I1/I2/I5/I7 已入 LS-96（comment `942a860a`）；N3/N4 死碼留 feature 收尾 sweeper（掃描須含 LittleSproutTests/）；I4 已併 LS-111 R4。下一步：development CI 綠後 promote dev→test → 派 qa。worktree LS-107 已移除（§2 併入即清）。

### 2026-08-31T02:33:01.929Z · Chih-Lin Yeh · `a37fb964`

## QA R1 — test tip `c490731`（`c4907312fd32823edf4a09d9f8ba1ae572b75a21`，= origin/test，已核對）

**裁決：FAIL**（核心驗收條件與自動化測試全綠；R2/R3 五項專項中 2 項發現真實視覺缺陷，另 2 項因環境限制未能動態重現）

Pen 路徑核對（LS-91）：`get_app_state` 回傳 active 文件 `/Users/clyeh/little-sprout/.claude/worktrees/LS-111/design/littlesprout.pen`，與本次 QA worktree（`qa-test`，`git rev-parse --show-toplevel` = `.claude/worktrees/qa-test`）不符——依規則本輪**未使用 Pencil MCP** 做任何 .pen 視覺比對，改以票文描述＋既有 r1/r2 輪截圖為基準。已回報 orchestrator。

---

### 票文驗收條件（§驗收）

1. ✓ 全新帳號（email OTP，本機 Mailpit／`demo-otp.sh`）無邀請碼走完 登入→三岔路→建立家庭→邀請家人→產生碼，iPhone 一般＋AX3、iPad 三岔路一張，截圖齊全（見產出位置）。
2. ✓ 產生的碼 SQL 唯讀對帳：`ZT6F63`／`6AE54E`／`4WZ9GB` 三次產生，`role=member`／`max_uses=5`／`expires_at`=+7天 全部對得上畫面顯示；每次重新產生後舊碼列從 `invites` 表**真的消失**（非軟刪）、新碼唯一 active。
3. ✓ 單元測試：family/invite 狀態機相關測試 20+ 條（遠超 ≥6 要求）；`xcodebuild test` 205 tests 全綠；`swiftlint lint --strict` 0 violations；`xcodegen generate` 後 `git status` 0 diff。
4. ✓ 角色選擇（member／viewer 白話說明）、核准必開鎖定列（無開關）、重新產生二次確認對話框、破壞性文案「名額不退還」，均如票文 Scope 描述渲染正確。

### R2/R3 五項專項

① **07 AX3 寬字母碼 clamp — FAIL（新發現）**：用實際產出的寬字母碼 `4WZ9GB` 在 AX3 測試，72pt clamp 確實防止了截斷（六個字元都在畫面上、無溢出／無 cut-off），但 `InviteFamilyView.swift:184-187` 的 `HStack(spacing:) { Text(codeFirstHalf); Text(codeSecondHalf) }` 沒有 `.fixedSize`／`.lineLimit`／`minimumScaleFactor`，导致版面配置在寬字母下把第二組「9GB」從中間斷行成「9G」＋「B」兩行、且與第一組「4WZ」不同高度並排——3+3 分組被破壞，版面明顯錯亂（非「唸得出來」的乾淨排版）。截圖：`06-invite-generated-iphone-ax3-widecode.png`／裁切證據 `06-code-crop4.png`。建議：至少其中一個 `Text` 加 `.fixedSize(horizontal: true, vertical: false)`，或改用單一 `Text` 搭配格式化字串讓換行只發生在分組空格上。
② **多帳號端到端＋SQL 對帳 — PASS**：Owner A 建立家庭「葉家 t  t」＋產生碼 → 登出 → Owner B（全新帳號）登入 → B 落在三岔路、**沒有**看到 A 的家庭或邀請碼（UI 截圖 `08-multiaccount-ownerB-fork-not-ownerA-family.png`）；DB 直查 `auth.users left join family_members` 確認 B 的 `family_id` 為 null，A 的仍是自己的家庭——F1 fix（帳號切換 reset）在真實序列下成立。
③ **N3 nav bar 視覺 — FAIL（判定為突兀）**：07 捲動後頂端出現明顯淺色色帶＋一條分隔線（`06-code-crop`／`N3-navbar-crop.png` 放大裁切可見），對照 05（`02-create-family-crop.png`，同一裁切區域完全無分隔線、純色背景），兩者在同一 navigation stack 中風格不一致，判定突兀。建議依 R2 建議改 `.toolbarBackground(Color.lsBackground, for: .navigationBar)`。
④ **慢網路 `.checkingExisting` ＋ N1 死點擊 — 未能動態重現**：本機 Supabase loopback 延遲 <50ms，`.checkingExisting` 窗口實測抓不到（多次快速重進畫面都已是 `.generated`）；環境無 sudo（無法用 Network Link Conditioner／`pfctl`）也不便中斷本機共用 docker（可能影響 LS-112 等其他 agent）。改以程式碼靜態複核：`InviteFamilyView.swift:335` 的 `.disabled(familyStore.createInviteState.isSubmitting)` 仍未加 `lookupInviteState.isSubmitting` 判斷，與 R3 comment `b2da0423` 描述的 N1 現狀一致（該項已入 LS-96 待辦池，非本輪新 FAIL，記錄即可）。
⑤ **飛航／斷網 `.lookupFailed` 版面 — 未能動態重現，程式碼靜態確認正確**：無法在此模擬器安全模擬斷網（同④原因）。靜態核對 `InviteFamilyView.swift:214-221` 的 `actionsSection` 對 `phase` 做 exhaustive switch，`.lookupFailed` 只渲染「重試」`SecondaryButton`，**不會**出現「產生邀請碼」；`InvitePhaseTests.test_lookupFailed_noLatestInvite_isLookupFailed_notEmpty` 已釘住這條 case 相等斷言（本輪 `xcodebuild test` 綠燈中含此測試）。邏輯正確但依規則未附動態截圖，不記 PASS，列為「需實機或降速工具驗證」。

### 回歸冒煙
✓ 登入（email OTP，兩帳號各一次）；✓ 時間軸載入（Owner A 建立家庭後路由進 Timeline，空狀態正常渲染，多次 tab 切換無 crash）。相簿／留言不在本票 scope，未直接操作，過程中 tab bar／導覽無 crash。

### 需實機驗證
- 推播、Sign in with Apple 完整流程（模擬器不可驗，未記 PASS）。
- 慢網路／斷網動態畫面（④⑤）：需真機或 Network Link Conditioner。

### 風險
- ①（AX3 寬字母碼斷行）是本輪新發現的真實視覺缺陷，會影響長輩使用者唸出邀請碼的可讀性，建議修正後再次 QA。
- ③（N3 nav bar）R2 已標記 PLAUSIBLE，本輪截圖判定證實突兀，建議與①一起修。
- Pen 路徑不符（LS-91）：本輪視覺比對未使用 .pen，僅供 orchestrator 留意 LS-111 worktree 的 pen 編輯器目前是唯一 active 文件。

### 產出位置
- 截圖：`.claude/evidence/LS-107/qa/`（01–08 主要截圖＋06/N3 系列裁切證據）
- 測試日誌：`/private/tmp/claude-501/.../scratchpad/LS-107-qa-test.log`（205 tests, 0 failures）
- SQL 對帳：本機 docker `supabase_db_little-sprout`（唯讀查詢，未 reset）

### 2026-08-31T03:37:43.306Z · Chih-Lin Yeh · `4b6ee413`

## merge-review R1（QA1 修復）— PR #177 · REQUEST_CHANGES

**Head**：`252a55aed422b11bb5f0bb49fada07f035c602af`（審查範圍 `origin/development...252a55a`，2 commits、**1 檔、+4/-4**）
**Verdict：REQUEST_CHANGES**（0 blocker、1 major、1 minor、5 informational）
CI 對帳：`ci`／`db`／`lint`／`rules` 四綠（run 33352799539，check-runs 的 SHA 我核過＝我審的 SHA）；`ci` job 日誌 `Executed 205 tests, with 0 failures`，與 PR body 一致。

fail② 我判**通過**（下面附實測像素）。擋下來的是 fail①：**斷行確實消滅了，但「兩組同步縮放」這個機制不成立**——PR 自己附的 AX3 截圖裡，同一個字母 `W` 在左組與右組是**兩個不同字級**（實測差 6.8%）。

---

## M1 — major · 兩組 `Text` 各自獨立縮放，`.minimumScaleFactor` 不會讓它們同步；PR 附的證據截圖本身就有 6.8% 字級差

`LittleSprout/Features/Family/InviteFamilyView.swift:181-190`

修法把 `.lineLimit(1).minimumScaleFactor(0.5)` 掛在**外層 HStack**上。`lineLimit`／`minimumScaleFactor` 是環境值，會傳到兩個 `Text`——但傳下去的是**同一個下限**，不是同一個**縮放結果**：每個 `Text` 各自拿自己被提議的寬度去算自己的 scale。HStack 的分配是「把剩餘寬度平均提議給彈性最小的子項」，所以兩組拿到**一樣的寬度**、各自縮到剛好填滿 ⇒ **字級與各自的固有寬度成反比**。固有寬度不同 ⇒ 字級不同。`appNumericFont` 只做 `.monospacedDigit()`（只把**數字**等寬），字母 `W/M` 與 `J/B/Z` 的 advance 差很多，所以固有寬度幾乎不會相等。

**實測（我用 PIL 量 `.claude/evidence/LS-107/qa1fix/01-ax3-widecode-fixed.png`，3x）**：

| 量測 | 左組 `WMZ` | 右組 `WGB` |
|---|---|---|
| 同一字母 `W` 的 ink 寬 | 50.67pt | **53.00pt** |
| 同一字母 `W` 的 ink 高 | 39.00pt | **41.67pt** |
| 推回字級（cap 0.705em） | **≈55.3pt** | **≈59.1pt** |
| 整組 ink 寬 | 140.3pt | 141.7pt |
| 整組 ink 寬（換算 em） | 2.537em | 2.397em |

兩組 **rendered 寬度幾乎相同**（140.3／141.7），**字級差 6.8%**，且 ink 的 em 寬比（1.058）與字級比（1.068）在量測誤差內一致——這就是「同寬度提議 ÷ 不同固有寬度」的簽名，不是偶然。版面數字也對得上：card 實測 354pt 寬、`insetCard` 20、`AppSpacing.item` 16 ⇒ 每組分到 (354−40−16)/2 ＝ **149pt**，兩組 ink＋bearing＋tracking 都落在 148–149。

**具體失敗情境（越極端越明顯，字元集 `23456789ABCDEFGHJKLMNPQRSTUVWXYZ`，數字因 `monospacedDigit` 等寬且窄）**：
- 已發生（實測）：`WMZWGB`、AX3、402pt 裝置 → 左 55.3pt／右 59.1pt，兩組不等高並排。QA1 ① 的原始描述就有「與第一組不同高度並排」，這條殘留是同一族的視覺缺陷，只是輕很多。
- 推算（**未實跑，標 PLAUSIBLE 的只是數量級**）：一組寬字母、一組窄字元時差距放大。以 375pt 裝置（SE3／13 mini）AX3 估：可用 271pt、每組 135.5pt；`WWW`（≈221pt@72）→ 縮到 ≈44pt，`222`（≈142pt@72）→ 縮到 ≈69pt ⇒ **同一支碼兩半字級差 1.5×**。這種碼不是造不出來的邊角料（每組 3 個數字的機率各 1.6%，而部分不對稱如 2 數字＋1 窄字母常見得多）。
- 這不是 AX3 專屬：一般字級下我量 `.claude/evidence/LS-107/qa/05-invite-generated-iphone-default-widecode.png`，`4WZ9GB` 剛好用滿（60.0pt、無縮放、餘裕僅約 7pt），比它再寬一點的碼（例如把 `4` 換成 `W`）在**一般字級**也會進入縮放 ⇒ 同樣會不等大。

**建議修法**（任一，都比現在小）：
1. **併成單一 `Text`**（QA `a37fb964` ① 的第二個建議）：`Text("\(first)\u{2002}\(second)")` 之類，一個 `Text` ＝ 一個 scale factor，**結構性保證**同步；`.lineLimit(1)` 保留即可。
2. 或 `ViewThatFits { HStack{…}; VStack{…} }`：放不下時只在**分組邊界**換行，兩組各自吃滿整個 314pt ⇒ 都能維持 72pt、也天然同步（順帶解掉下面的 N1）。
3. 若要維持雙 `Text`＋單行，至少讓兩組共用同一個縮放來源（例如量測後用 `@ScaledMetric`／`GeometryReader` 自己算一個共用 size），不要依賴各自的 `minimumScaleFactor`。

（我認為 1 最小、最誠實；2 最貼稿面字級但改變版面，要設計 owner 點頭。）

---

## minor

**N1 · AX3 的 72pt 檔位實際拿不到，寬碼下有效字級掉到 55pt——比一般字級的 60pt 還小** — `InviteFamilyView.swift:186-190`／`Typography.swift:57-62`

R1 F5 的 clamp 是「AX3 不照系統曲線長到 92pt，改用稿面 `fs-code` 的 AX3 檔位 **72pt**」。加了 `minimumScaleFactor` 之後，寬碼在 402pt 裝置實測落在 **55.3／59.1pt**（見上表），也就是 **AX3 使用者看到的碼比一般字級使用者（60pt）還小**——跟「放大到 72」的用意相反。375pt 裝置估更低（平均碼 ≈50pt、最壞 ≈44pt，未實跑）。

順帶把 `.minimumScaleFactor(0.5)` 這個數字講清楚：**它永遠不會被觸發**。最壞情境（375pt＋AX3＋最寬六碼）需要的 scale ≈0.61，真正的下限是版面幾何、不是這個 0.5；所以它是保險絲不是承重件，PR body 說的「保守下限」是對的，但也代表「縮到 36pt 難以辨讀」這個風險不存在——**我核過了，這條不成立**。真正該擔心的是上面那個 55pt/44pt。

**建議**：若要守住稿面 AX3 字級，走 M1 的建議 2（分組邊界換行）；若接受縮放，請把「AX3 寬碼會縮回一般字級以下」這件事回報設計 owner（LS-111 稿面對帳）並在 `Typography.swift` 的 `maxScaledSize` 註解補一句，別讓下一個人以為 72pt 是保證值。

---

## fail② nav bar — 通過（實測，含 R1 F9 不透明性複驗）

`InviteFamilyView.swift:67-68`

`.toolbarBackground(Color.lsBackground, for: .navigationBar)`（style-only、**沒有** `.visible`）在 SwiftUI 是「指定 bar 背景樣式，可見度仍走 iOS 自動 scroll-edge 行為」。我用像素驗了兩態：

- **未捲動**（`01`／`04-n3-navbar-top-crop.png`）：bar 區域是 (244,223,226)→(239,215,218) 的**漸層**，＝ `appBackground()` 的「窗光」RadialGradient 透過來，**沒有** bar 底、沒有分隔線 ⇒ 與 05 的觀感一致，QA ③ 抱怨的「淺色色帶＋分隔線」消失。
- **已捲動**（`02`／`05-n3-navbar-scrolled-crop.png`）：bar 區域**整片 (237,211,214) ＝ `bg` 資產值 `#EDD3D6`**，且卡片與「閉。」在 bar 下緣被切齊、完全沒有穿透 ⇒ **R1 F9 要解的「內容與返回鍵重疊」仍然成立**。`bg.colorset` 兩個 appearance 的 `alpha` 都是 `1.000`（我讀了 `Assets.xcassets/bg.colorset/Contents.json`），不透明性成立。

orchestrator 問的兩點都是肯定的：背景仍不透明、防重疊仍成立。

## informational（建議處置：I1／I2 記 LS-96；I3 併 LS-111 或 QA 目視認定；I4／I5 無需處置）

- **I1**（size S）：防重疊的保證從「本畫面自己強制 `.visible`」變成「依賴 iOS 對 `ScrollableFillView` 的 scroll-edge 自動偵測」。今天實測有效；但日後 `ScrollableFillView` 若被包進非 ScrollView 的外層容器、或改用其他捲動實作，這層保護會**靜默消失**且沒有任何測試會紅。建議在 LS-96 記一條「nav bar 防重疊無回歸 gate」。
- **I2**（size S）：`.lineLimit`／`.minimumScaleFactor` 是宣告式 modifier，沒有可單測的邏輯分支——PR body 已誠實說明。實際後果是 **AX3 版面完全靠人工截圖把關**，這輪的 6.8% 字級差就是人眼沒抓到、我量像素才看出來的。若要機械化，需要 snapshot test（harness 議題，非本票）。
- **I3**：捲動態的 bar 是**平色** `bg`（#EDD3D6），而它正下方的頁面背景是漸層近亮端（實測左緣 (241,220,223)）⇒ bar 下緣有約 4–9/255（≈3% 亮度）的色階差，而且「窗光」漸層最亮的左上角（gradient center 0.14/0.02 落在 bar 內）在捲動時會被平色蓋掉。**遠比原本的系統材質色帶輕微、且無分隔線**，我不列 finding；但這是主觀視覺，最終由 QA 目視認定。
- **I4**：`tracking(4)` 是絕對點值、不隨縮放等比——縮到 55pt 時字距的相對比例從 5.6%em 變 7.3%em。純細節，不建議為此加程式碼。
- **I5**（**既有、非本 PR 引入，不列 finding**）：AX3 下「還可用 5 次」Pill 被撐成一個近圓形色塊、文字折三行（`01-ax3-widecode-fixed.png`）。QA1 沒判它不合格，也不在本票修法範圍，我只是視覺親核時看到，提供 QA 這輪一併目視。

## 效能／平行／race

本 diff 只有兩個宣告式 view modifier，**沒有**任何並行、狀態、DB／RLS、查詢、圖片解碼面的變更 ⇒ 四維度中的前三項**這輪無可審之處，也沒有找到問題**（不是略過：我確認了 diff 沒碰到任何 async／actor／查詢路徑）。前幾輪的 race 結論（R3 `b2da0423`）不受影響。

## scope

**通過**。1 檔、+4/-4、兩處都對得上 QA1 的兩條 FAIL；`origin/development` 是 `252a55a` 的祖先（線性、無 back-merge）；沒有順手重構、沒動共用元件（`ScrollableFillView`／`Typography` 都沒碰）、沒有測試資料或 migration 進 repo（寬碼 `WMZWGB` 是本機 SQL 造的，diff 裡沒有）。nav bar 那三行舊註解被壓成一行，屬於被改那一行自己的註解，不算越界。

## 我沒有實跑的部分

依 reviewer 角色不 boot 模擬器、不跑 xcodebuild（205 tests 綠採信 CI `ci` job 日誌，已核對 check-runs 的 SHA）；未連線 DB（本輪無 DB 面變更）；`.pen` 未解析（`fs-code` 60／72 檔位採信 `Typography.swift` 註解與 R1 I1 已建立的結論）；375pt 裝置與極端碼（`WWW222` 類）的字級差**是算出來的、沒有實跑**，故標 PLAUSIBLE——M1 被判 major 的依據是**已在 PR 附件實測到的 6.8%**，不是那個推算。


### 2026-08-31T04:27:27.132Z · Chih-Lin Yeh · `faf2e55c`

## merge-review R2（QA1 修復增量）— PR #177 · APPROVE

**Head**：`3918723ad88e0f752ff61beb850ac43476e6f5e5`
**審查範圍**：內容增量 `252a55a..43a8dbd`（1 commit、2 檔、+15/−9）＋ merge commit `3918723` 純度核對。PR 對 `origin/development` 的**總**足跡（三點 diff）＝**2 檔、+17/−11**，沒有夾帶第三個檔。
**Verdict：APPROVE**（0 blocker、0 major、3 minor、4 informational）
CI 對帳：`ci`／`db`／`lint`／`rules` 四綠（run 33356496966，`head_sha` ＝我審的 SHA，四支 check-run 的 `head_sha` 我逐支核過）；`ci` job 日誌 `Executed 205 tests, with 0 failures` ＋ `** TEST SUCCEEDED **`。

**R1 M1（major）已修，而且是結構性修好的，不是碰巧修好的**——我用像素逐字量過三張圖，下面附數字。

---

## M1 複驗 — 通過（實測，非採信）

修法採我 R1 的建議 1：`Text(formattedCode(invite.code))` 單一 `Text`，`formattedCode` 用 `\u{2002}` 接起 3+3。單一 `Text` ＝ 單一 scale factor，**同步是型別層面的保證**，不再依賴兩個 `Text` 各自算 scale。

**實測（PIL，@3x，`.claude/evidence/LS-107/qa1fix-r2/`）**：

| 量測（px @3x） | 左組 `W`／`M`／`Z` | 右組 `W`／`G`／`B` | 字距 |
|---|---|---|---|
| `02-ax3-code-crop-measured.png` | 147／120／87，高皆 113 | **147**／104／90，高皆 113 | 25／30 ‖ **117** ‖ 19／28 |
| `04-default-code-crop-measured.png` | 147／120／87，高皆 113 | **147**／104／90，高皆 113 | 25／30 ‖ **117** ‖ 19／28 |

兩組的 `W` 都是 **147px**（R1 是 152 vs 159，差 6.8%）、六個字母 ink 高全部 **113px**（`G` 117 是圓形 overshoot，正常）。`05-default-wwwmmm-extreme.png` 同樣乾淨：三個 `W` 各 131px、三個 `M` 各 106px，**組內組間零誤差**。M1 封死。

順帶把 R1 N1 提的 `minimumScaleFactor(0.5)` 再確認一次：最極端的 `WWWMMM` 實得 ≈47.3pt，下限是 0.5×72＝36pt，**沒被觸發**——它確實是保險絲不是承重件，跟 R1 的結論一致。

`Typography.swift` 的 N1 註解我讀了，寫的是「72pt 是硬上限、不是保證值」＋指向 LS-111，**準確、沒有美化**。這正是我 R1 要求的處置。

## merge commit `3918723` 純度 — 通過（機械核對）

`git merge-tree 43a8dbd e05cec1` 算出的樹 `8a50285…` **等於** `git rev-parse 3918723^{tree}`⇒ 純三方自動合併，**無任何手解、無夾帶**。兩個 parent 分別是 `43a8dbd`（本分支）與 `e05cec1`（＝ `origin/development` tip）。合進來的 4 檔（`ci.yml` macos-15→26、`.xcode-version` 16.4→26.6、`COLLABORATION.md`、`patrol_linear.py` `lane:ui` 1→2）全部來自 development，非本 PR 產出。

---

## minor

### N1 · `\u{2002}`（0.5em）取代了 `AppSpacing.item`（16pt）當分組間距——吃掉約 14pt 寬度預算，讓寬碼比必要的再小約 5%

`InviteFamilyView.swift:386-388`

修法把分組間距從**設計 token `AppSpacing.item` ＝ 16pt**（原 `HStack(spacing:)`）換成 **EN SPACE ＝ 0.5em**。0.5em 在 60pt 是 **30pt**，加上多出來的一次 `tracking(4)` ⇒ 比原本的 16＋4 寬 **約 14pt**。而這個版面的瓶頸正是寬度（見下），所以這 14pt 直接轉成字級損失。

**實測佐證**：`WMZWGB` 與 `WWWMMM` 兩張圖的 **ink 總跨距都是 304.67pt**（分別 x=139..1052、137..1050）——完全填滿 `354 − 2×20(insetCard) = 314pt` 的可用寬。兩個不同的碼收斂到同一個寬度，就是「被寬度綁死」的簽名。組間 ink 間隙實測 117px＝**39.0pt**，而組內字母間隙只有 6.3–10.0pt ⇒ 分組間距被放大成字母間距的 **4 倍**（稿面的 16pt＋tracking 大約是 2 倍）。

**具體失敗情境（可算，模型與實測自洽）**：字串 7 字元 ⇒ tracking 貢獻固定 28pt，可用給字形的是 314−28＝286pt。
- `WMZWGB`：由 s＝53.4pt 反解總字形寬 G＝5.356em（含 0.5em 空格）。改用 0.25em 分隔（U+2005 FOUR-PER-EM，60pt 時 15pt ≈ 稿面 16pt）⇒ G＝5.106 ⇒ s＝286/5.106＝**56.0pt**（＋4.9%）。
- `WWWMMM`：s＝47.3 ⇒ G＝6.045em；換 0.25em ⇒ **49.4pt**（＋4.4%）。
- 對照修法前（雙 `Text`，每組 149pt）我算 `WMZWGB` 在預設字級是 55.9／58.5pt ⇒ **現在 53.4pt 比修法前「較小的那一組」還小**。其中約 4.9% 是 EN SPACE 造成的、可回收；其餘是同步到寬組的必要代價（我不反對那部分）。

**建議修法**：`\u{2002}` 改 `\u{2005}`（0.25em，最貼 `AppSpacing.item` 16pt）或一般空格 `\u{0020}`（字型必有、`.lineLimit(1)` 下不會斷行）；**並在 `formattedCode` 補一行註解說明為什麼是這個空格**——它現在是承重的排版常數，卻沒有任何一行字解釋它取代了哪個 token。字型是否內含 U+2005 我沒實跑驗證（SF 系統字型一般有，但請實作者換完重截一張圖確認寬度真的變 0.25em，不要假設）。

**為什麼判 minor 不判 major**：碼完整、單行、分組清楚、兩組同級，功能與可讀性都成立；這是 5% 的排版最佳化＋稿面 token 偏離，不是機制失效。

### N2 · `05-default-wwwmmm-extreme.png` 其實是 **AX3** 截圖，檔名寫 default——極端碼**沒有**預設字級的證據

`.claude/evidence/LS-107/qa1fix-r2/05-default-wwwmmm-extreme.png`

**機械證明**：把 `05` 與 `01-ax3-widecode-wmzwgb-synced.png` 在 y=150..1250（狀態列以下、卡片以上：返回鍵＋標題＋三行內文＋「邀請碼」標籤）逐像素比對，**maxdiff ＝ 0、0.000% 差異**；同一區對 `03-default-widecode-wmzwgb.png` 則有 39.3% 像素不同。分帶量測也對得上：`01`／`05` 標題帶高 133px、內文行高 96px；`03` 標題 89px、內文 46px。⇒ `05` 與 `01` 是同一個 dynamic type 檔位（AX3），`03` 才是 default。

**後果**：這輪唯一的「極端碼」證據被標成 default，QA 下一輪會照著讀。結論本身我重新推過**不受影響**（碼被寬度綁死，AX3 與 default 會收斂到同一尺寸——這正是 `01` 與 `03` 的碼帶都是 117px 的原因），但**證據不支持它現在的標題**。建議：重截一張真 default 的 `WWWMMM`，或直接把檔名／說明改正。

### N3 · PR body 仍描述已被取代的 R1 修法，且有一句已被本輪自家證據推翻

PR #177 body「修法」段仍寫「邀請碼 HStack 加 `.lineLimit(1)`」——**HStack 已經不存在了**；「風險」段寫「標準字級下無影響（本來就有餘裕，不會觸發縮放）」，但本輪 `03-default-widecode-wmzwgb.png` 實測預設字級的 `WMZWGB` 就是 **53.4pt（＜60pt，已縮放）**。merge commit 會繼承這段 body，等於把一句已知為假的話寫進版本歷史。建議併入前更新 body（`gh pr edit` 前記得先過 `scripts/gates/pr-body-check.sh`）。

---

## 對 orchestrator 三個提問的正面回答

**Q3：寬碼在預設字級也縮放（≈53.4pt＜60）、AX3 對寬碼零放大——可接受的既有幾何限制，還是要在本 PR 處理？**

**判定：可接受，本 PR 不處理，留 LS-111**（但 N1 的 5% 是本 PR 自己造的、可以順手回收）。

先把事實釘死——**AX3 對寬碼是字面意義的零放大**：`01`(AX3) 與 `03`(default) 的碼帶高度都是 **117px**（比值 1.00），同一張圖裡標題 133/89＝**1.49×**、內文 96/46＝**2.09×**。也就是說整個畫面只有邀請碼這一個元素完全不受 AX3 影響。

判定理由：
1. **綁定約束是卡片寬度，不是 token**。兩個不同的碼 ink 跨距都是 304.67pt＝填滿 314pt。AX3 不會把卡片變寬，所以 60pt 與 72pt 兩個提案值都會被同一道寬度閘門壓成同一個結果。這是幾何，不是 bug。
2. **非本 PR 引入**。修法前 AX3 實測就是 55.3／59.1pt（R1 `4b6ee413` 表格），本來就 ≤ 一般字級的 60pt。本 PR 把它變成單一的 53.4pt，其中可歸咎於本 PR 的只有 N1 那 ~5%。
3. **真要解必須動版面**（`ViewThatFits` 分組邊界換行、縮 tracking、或縮 `insetCard`），三者都改變已核可稿的 07 版面 ⇒ 需設計 owner 點頭。R1 我就寫過建議 2「要設計 owner 點頭」，LS-111 comment `f637dc73` 已掛。
4. `Typography.swift` 這輪補的註解已經誠實揭露「72pt 不是保證值」，下一個讀者不會被誤導——這是我 R1 N1 要的最低限度，已經拿到。

（**PLAUSIBLE，算出來的、沒實跑**：可用字形寬 286pt ÷ 72pt ＝ 3.97em，扣掉 0.5em 空格剩 3.47em ÷ 6 ＝ 平均每字母須 ≤0.578em 才碰得到 72pt；SF Bold 裡 `W`≈0.97em、`M`≈0.87em、`G`≈0.72em 都遠超過，所以**多數 6 碼在 402pt 裝置的 AX3 都拿不到 72pt**，不只是「寬碼」。這個平均值是我從實測 ink＋bearing 反推的粗估，不是量測值，請 LS-111 用真碼取樣驗證再下結論。）

**Q4：`InviteFamilyView.swift` 已頂到 400 行——有沒有為了湊行數砍掉必要註解？**

**沒有系統性砍註解**，但有一處「why」被換掉，我認為可接受，同時要示警一個結構性壓力。

- 行數：`252a55a` 399 → `43a8dbd`／`3918723` **400**。`.swiftlint.yml` 沒有覆寫 `file_length`，用 SwiftLint 預設 `warning: 400`；`--strict` 下**第 401 行就會紅**。lint 綠 ⇒ 現在剛好卡在天花板上，餘裕 0 行。
- 逐行核對本輪 diff：註解只有 **1 換 1**——移除 `// LS-107 QA1 fail①…逐字硬斷行；下一行不斷行且放不下時縮字級。`、新增 `// LS-107 R1 M1（4b6ee413）：雙 Text 各自縮放不同步（實測差 6.8%），改單一 Text。`。**沒有第二處刪減**。
- 代價：`.lineLimit(1).minimumScaleFactor(0.5)` 這兩個 modifier 還在，但它們的來歷（QA1 fail①）現在只剩 git 歷史。折衷是 `Typography.swift` 反而多了 5 行把這段脈絡寫清楚，整體資訊沒有淨損失。**不列 finding。**
- 示警（見 I3）：下一次改這個檔案，作者只有兩個選擇——刪註解或拆檔——而且**沒有任何 gate 會告訴他他正在刪 why 註解**（lint 只數行數）。

**Q1（增量）／Q2（merge 純度）** 見上。

---

## race／效能／平行 — 本輪無可審之處，也沒有找到問題

不是略過：我確認 diff 的兩處都不碰任何 async／actor／`Task`／狀態機／DB／查詢路徑。`formattedCode` 是 `View` 上的純函式（只做字串串接），每次 `.generated` 分支求值多配置一個 String，成本可忽略；`Typography.swift` 只多了註解，`maxScaledSize` 的 body 一個字沒動。R3（`b2da0423`）建立的 race 結論不受影響；R2 I6 的查詢面結論（無 per-row 子查詢退化、無 N+1、無 OFFSET）不受影響。無圖片解碼／縮圖／批次上傳面變更。

## scope — 通過

2 檔、+17/−11（對 development 的總足跡），每一處都對得上 R1 的一條 finding：`InviteFamilyView`（M1）、`Typography`（N1，**純註解**）。沒有順手重構、沒動共用元件（`ScrollableFillView` 沒碰）、沒有測試資料或 migration 進 repo（`WMZWGB`／`WWWMMM` 是本機 SQL 造的，diff 裡沒有）。merge commit 已證明為純自動合併。

## informational（建議處置：I1／I3 記 LS-96；I2 併 LS-111；I4 給 orchestrator）

- **I1**（size S）：`copyCode(invite.code)`（`:254`）與 `inviteURL(invite.code)`（`:242`）用的都是**原始碼字串**，`\u{2002}` 不會外洩到剪貼簿或 deep link——我核過了，沒問題。但這條「顯示用 `formattedCode`、傳輸用 `code`」的分工沒有任何測試釘住；`formattedCode` 是 `View` 的 private func，無 ViewInspector 就測不到。日後若有人把分享文案也改成 `formattedCode`，家人貼上的碼會含一個看不見的 U+2002 而查無此碼、且錯誤訊息完全不會提示。建議在 LS-96 記一條「邀請碼顯示／傳輸分離無回歸 gate」（與 R1 I2 的 snapshot test 缺口同源）。
- **I2**（併 LS-111）：分組間距現在是 0.5em（實測 39.0pt ink 間隙，字母間隙的 4 倍），稿面是 16pt 的 `AppSpacing.item`（約 2 倍）。就算 N1 照建議換成 0.25em，也已經是「跟著字級縮放」而非固定 16pt——性質變了。請一併在 LS-111 對帳，決定 07 分組間距到底該是固定值還是相對值。
- **I3**（size S）：`InviteFamilyView.swift` 卡在 SwiftLint `file_length` 預設 400 的天花板、餘裕 0 行。下一次修改被迫二選一（刪註解／拆檔），而 gate 只數行數、不會分辨刪掉的是不是承重註解。建議 LS-96 記一條（拆檔或明確設定 `file_length` 門檻並在 §7 對照表記錄理由）。
- **I4**（給 orchestrator，非 finding）：**本輪修復沒有 handoff comment**。Linear LS-107 最新 comment 是我的 R1 裁決（`4b6ee413`，03:37），GitHub PR #177 的 issue comments 是**空的**（`gh api .../issues/177/comments` 回傳空陣列）。派工說「修復回報＝Linear 最新 handoff comment」，但那份回報不存在——我這輪是直接讀 commit、程式碼與截圖得出結論的，**沒有採信任何未經核對的回報**（N1／N2 兩條正是這樣挖出來的）。另 CLAUDE.md 要求 handoff 必列「reviewer 全部 informational 的處置」，本輪缺這一條，請補。

## VoiceOver（PLAUSIBLE，未驗）

修法把兩個 `Text`（＝兩個 accessibility element）併成一個，內容含 U+2002。畫面上這個 `Text` 沒有任何 `accessibilityLabel`／`accessibilityValue`（我 grep 過整份 `InviteFamilyView`），所以朗讀完全交給系統對 `"WMZ\u{2002}WGB"` 的預設處理。U+2002 是 whitespace，一般會被當詞界，但我**沒有實跑 VoiceOver**，也不知道它會逐字母拼讀還是當成單字念。對「長輩唸得出來」這條票文目標而言值得 QA 這輪順手開 VoiceOver 聽一次。同檔 `OTPCodeField:42-43` 已經示範了正確作法（`accessibilityLabel` ＋ 把碼用空格 join 當 `accessibilityValue`），可直接沿用。**不列 finding**（未驗證、且非本 PR 引入的新缺口——修法前兩個 `Text` 也沒有 a11y 標註）。

## 留給 QA（本輪新增，接續 R3 清單）

1. **N2 的重截**：真 default 字級的極端碼（`WWWMMM`）一張——現有的 `05` 是 AX3。
2. **AX3 零放大目視確認**：`01`(AX3) 與 `03`(default) 的邀請碼**一樣大**（117px），但同頁其他文字差 1.5–2.1×。這是本輪已知且被接受的取捨（Q3），請確認長輩實際觀感可接受、並回報 LS-111。
3. VoiceOver 唸邀請碼一次（見上）。
4. R3 清單第 2、3 項（慢網路 `.checkingExisting`＋N1 死點擊、飛航 `.lookupFailed`）QA1 兩項都因環境限制未動態重現，仍然掛著。

## 我沒有實跑的部分

依 reviewer 角色不 boot 模擬器、不跑 xcodebuild（205 tests 綠採信 CI `ci` job 日誌，已核對 run 與四支 check-run 的 `head_sha` ＝我審的 SHA）；未連線 DB（本輪無 DB 面變更）；`.pen` 未解析（`fs-code` 60／72 檔位採信 `Typography.swift` 註解與 R1 I1 結論）；VoiceOver 未實跑（故不列 finding）；U+2005 是否存在於該字型未驗證（故 N1 的建議附「換完重截確認」）；Q3 括號內「多數 6 碼在 AX3 都拿不到 72pt」是**推算不是量測**，已標 PLAUSIBLE。所有字級數字均由 ink 高 ÷ 0.705 cap ratio 反推（沿用 R1 同一套方法），量測誤差 ±1px@3x ≈ ±0.5pt。


### 2026-08-31T04:29:36.654Z · Chih-Lin Yeh · `1df1ffa5`

**QA1 fix R2 APPROVE → 已併 development（2026-08-31）**：PR #177 merge `bd196dd`；merge-review=success 貼於 head `3918723`（R2 comment `faf2e55c`）。

**補記 fix 輪 handoff（R2 I4 指出缺漏——fix agent 只回報 orchestrator 未留票面記錄，此為補正）**：M1 改單一 Text＋U+2002 分組（像素實測兩組字級完全一致，AX3 與預設皆 147×113px @3x ≈53.4pt）；N1 於 Typography.swift 補「72pt 是上限非保證值」註解。新發現：極寬碼在預設字級即觸發縮放、AX3 對寬碼零放大（下限由卡片寬決定）——設計對帳已掛 LS-111（comment `f637dc73`）。

R2 minor 處置：N2（證據檔名錯標 default 實為 AX3）orchestrator 已改名；N3（PR body 舊修法描述）orchestrator 已改寫 body；N1／I2（EN SPACE 0.5em vs 稿面 16pt 分組間距）併 LS-111 對帳；I1（顯示／傳輸分離無 gate）／I3（檔案頂 400 行天花板）入 LS-96。

下一步：development CI 綠 → promote dev→test（同批帶 LS-112）→ qa 重驗（fail①② 復驗＋LS-112 驗收一輪雙驗）。fix worktree 已清。

### 2026-08-31T05:10:47.101Z · Chih-Lin Yeh · `1febe644`

## QA2 — test tip `bd196dd`（`bd196ddd7fded60620e2377487350965eafc8a5e`，= origin/test，已核對）

**裁決：PASS**（QA1 兩項 FAIL 復驗皆通過；輕量回歸無異常；已知限制為設計對帳項，非本輪 FAIL）

Pen 路徑核對（LS-91）：`get_app_state` 回傳 active 文件 `/Users/clyeh/little-sprout/.claude/worktrees/LS-111/design/littlesprout.pen`，與本次 QA worktree（`qa-test`，`git rev-parse --show-toplevel` = `.claude/worktrees/qa-test`）不符——本輪**未使用 Pencil MCP** 做任何 .pen 視覺比對；兩項復驗目標（單行 3+3 分組、07/05 nav bar 一致）皆有票文與 fix commit 訊息載明的明確可驗標準，改以模擬器實測＋程式碼核對為準。已回報 orchestrator。

---

### fail① 復驗（AX3＋寬字母碼）— PASS

用比 `WMZWGB` 更極端的碼 `WWWMMM`（本機 SQL 直接 UPDATE 既有邀請碼的 `code` 欄位造出，未過既有 `create_invite` 隨機產生路徑）：

- **一般字級**：`WWW MMM` 單行顯示，兩組字級完全一致（單一 `Text` 保證同步），3+3 分組視覺清楚（EN SPACE 間隔），無溢出、無斷行。截圖 `05-invite-wwwmmm-default.png`。
- **AX3**：同樣單行、無斷行、兩組字級一致，唸得出來。截圖 `06-invite-wwwmmm-ax3.png`。
- 程式碼核對：`InviteFamilyView.swift:183-187` 確認為單一 `Text(formattedCode(invite.code))` ＋ `.lineLimit(1).minimumScaleFactor(0.5)`，`formattedCode` 用 `\u{2002}`（EN SPACE）分組——與 R2 merge-review（`faf2e55c`）核對的修法一致。

### fail② 復驗（07 nav bar 視覺）— PASS

- 07 頂部（未捲動）與捲到底（已捲動）兩態截圖：`07-invite-navbar-top.png`／`08-invite-navbar-scrolled.png`。像素比對 nav bar 區域（y=0-130px）：**99.1% 像素完全一致**（(237,211,214)＝`Color.lsBackground`），僅 0.88% 差異來自返回鍵圖示 anti-aliasing，無色帶、無分隔線。
- 對照screen 05（另建帳號走 04→05 取得）：`09-screen05-createfamily.png` 同區域為 (239-245,215-224,218-227) 的柔和漸層（「窗光」`appBackground()` 效果），肉眼與 07 的頂部／捲動態同樣呈現柔和背景、無突兀色帶或分隔線，兩畫面視覺觀感一致。
- 程式碼核對：`InviteFamilyView.swift:69` 確認為 `.toolbarBackground(Color.lsBackground, for: .navigationBar)`（取代原 `.visible`），對應 fix commit `045153e`。

### QA1 其餘 PASS 項輕量回歸 — PASS

兩組全新帳號（`ls107qa2@example.com`／`ls107qa2b@example.com`，本機 email OTP）各走一次：登入 → 三岔路（04）→ 建立家庭（05）→ 設定 → 邀請家人（07）→ 產生邀請碼 → 角色選擇（一般成員／只能瀏覽，白話說明正確渲染）。兩支碼皆與 DB 對上（`M7Y8FY`／`GYFQMA`，`role=member`／`max_uses=5`／`used_count=0`／`expires_at`=+7 天，唯讀 SQL 核對）。Timeline／相簿／孩子三個 tab 切換無 crash（相簿與孩子為本 feature scope 外的既有空狀態）。未重跑完整 SQL 對帳（撤舊碼／多帳號隔離等），依派工範圍屬 QA1 已驗證項目。

### 設計對帳項（非 FAIL，記錄即可）

`Typography.swift:50-58` 註解已明確記載：AX3 的 72pt 是硬上限、非保證值；寬字母碼（如 `WWWMMM`）在**一般字級**也會因版面寬度觸發縮放（本輪實測與 R2 merge-review 一致）；AX3 對寬碼的實際放大幅度受限於卡片寬度幾何，已交 **LS-111** 對帳，本輪不視為 FAIL。

### 專項④⑤ — 維持「需實機驗證」

慢網路 `.checkingExisting` ／ N1 死點擊、飛航模式 `.lookupFailed` 版面：本機容器延遲 <50ms 無法動態重現，環境無 sudo 無法用降速工具，本輪未重新嘗試（沿用 QA1 註記，程式碼靜態邏輯未變動）。

### 自動化測試

`xcodebuild test`：205 tests，0 failures。`swiftlint lint --strict`：0 violations。`xcodegen generate` 後 `git status` 0 diff。

### 產出位置
- 截圖：`.claude/evidence/LS-107/qa2/`（00–09 主截圖＋像素比對裁切 crop_*.png）
- SQL 對帳：本機 docker `supabase_db_little-sprout`（唯讀查詢＋一次 UPDATE 造測試碼，未 reset）
- 模擬器：`LS-107-qa2-iPhone17Pro`（26E11776-2C5C-43D9-9DF1-5A02CD5EC0F7），已關閉

### 2026-08-31T05:13:10.774Z · Chih-Lin Yeh · `0e325279`

**QA2 PASS → v0.5.0 released（2026-08-31）**：test→main promote `bd196dd`（22 commit），tag v0.5.0；qa=success（comment `1febe644`）。本批無新 migration，不需正式站部署。

**Retro（orchestrator lesson learning review）**：
- **Gate 鏈**：本票走完整最長鏈——design gate（LS-46）→ R1→R3 三輪 merge-review → QA1 FAIL → fix R1→R2 兩輪 → QA2 PASS。QA1 抓到的兩項（AX3 折行、nav bar）都是 reviewer 靜態審不到、只有模擬器實測抓得到的——**QA 模擬器視覺驗收的價值實證**。
- **返工原因分析**：fix R1 的「雙 Text 各自縮放」被像素量測翻案——「看起來同步」不等於「機制上保證同步」；reviewer 用 PIL 量像素的驗法值得成為 UI 票審查慣例。
- **漏接**：fix agent 未留票面 handoff（R2 I4 抓到，orchestrator 已補記）——派工模板的 handoff 要求要包含「save_comment 到票」這步，已在 orchestrator 派工習慣中修正。
- **工具缺口**：AX3 snapshot 測試缺席讓同類問題只能靠 QA 人工（已入池 I2，size:M）。
- 改善項無 High 以上，不另票。sweeper 巡檢中（含 R3 點名的 N3/N4 死碼），CLEAN 後轉 Done。

### 2026-08-31T05:22:30.463Z · Chih-Lin Yeh · `fd90eaa5`

**收尾 sweeper 完成 → Done（2026-08-31）**：批次巡檢（LS-107＋LS-112）跑完。LS-107 findings 處置：F1（`allowsGenerate` 死碼）＋F2（`familyID` 未用）→ 併 LS-108 scope（該票將重構 InviteFamilyView/InvitePhase，comment `ca8d273f`）；F3（`SupabaseFamilyAPIClient` 過時註解）→ LS-96（comment `07d41748`）。無 High 以上另票需求，retro 已記（comment `0e325279`）。收尾儀式齊（QA2 PASS＋sweeper＋retro）。

