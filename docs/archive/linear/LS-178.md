# LS-178 Chore：LS-19 iOS——`CreateChildAvatarFieldUITests` 裸 `isHittable` 斷言 flaky（無 wait／存在性前置）→ 改 `waitForExistence`＋hittable 輪詢，並掃同檔同型斷言

| 欄位 | 值 |
|---|---|
| 狀態 | Canceled（canceled） |
| 優先序 | Medium |
| 標籤 | size:S, lane:ui |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M2 孩子檔案 |
| 估點 | — |
| 建立 | 2026-09-04T20:35:15.565Z |
| 開始 | 2026-09-04T20:35:15.666Z |
| 完成 | 2026-09-04T20:53:27.659Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-178/chorels-19-ios-createchildavatarfielduitests-裸-ishittable-斷言-flaky無 |
| 父票 | LS-19 Story：孩子檔案 CRUD 與年齡標記 |
| 關係 | related→LS-174, related→LS-96, related→LS-144, related→LS-169, related→LS-158 |

## 描述

## 來源

[LS-158](https://linear.app/little-sprout-app/issue/LS-158/harnessqa-端到端驅動不依賴-mobile-mcp-littlesproutuitests-加-qa-e2e) merge-review R1 `be862711`／R2 `69e71c2a`（2026-09-05）兩輪都建議 orchestrator 開 chore：[LS-169](https://linear.app/little-sprout-app/issue/LS-169/taskls-19-ios-寶貝大頭照上傳photospicker方形縮圖storage-family-idavatarschild) 併入的 `LittleSproutUITests/CreateChildAvatarFieldUITests` 對頭像欄位／按鈕直接斷言 `isHittable`，沒有 `waitForExistence(timeout:)` 或 hittable 輪詢前置——模擬器慢一拍就紅，且紅在與變更無關的 PR 上。[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項（2026-09-05 01:18 `5e989fe0` 第二條）原訂「CI 連續紅兩次才升票」；巡檢 04:33 lane:ui 連續空 2 輪、候補全卡設計核可，依 [LS-144](https://linear.app/little-sprout-app/issue/LS-144/harness巡檢加開票責任機械提醒-lane-在飛-0-且無可派候補時動作清單印-開票並列來源story-拆票ls-96-p1-升票) 開票責任提前升票（size S、不需 Design gate）。

## 範圍

1. `CreateChildAvatarFieldUITests`：每個裸 `XCTAssert(...isHittable)` 改為「`waitForExistence(timeout:)` 綠 → 以 `XCTNSPredicateExpectation(format: "isHittable == true")` 或等價輪詢（≤5 秒）→ 斷言」；沿用 `LittleSproutUITests` 既有的等待 helper（先 grep，有就用、沒有才加一支最小 helper 於 `LittleSproutUITests/Support/`）。
2. 掃同檔與 `LittleSproutUITests/QA/`（[LS-158](https://linear.app/little-sprout-app/issue/LS-158/harnessqa-端到端驅動不依賴-mobile-mcp-littlesproutuitests-加-qa-e2e)）之外的 UITests 是否有同型裸 `isHittable`／`exists` 斷言——只列清單進 handoff，**不順修他檔**（scope）。
3. 驗證：該檔測試在專屬模擬器連跑 ≥3 次全綠；mutation：把 timeout 改 0 → 至少一次紅（證明等待真的在做事）。
4. [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `5e989fe0` 第二條回一則銷除註記（附 PR 號）。

## 不做

改產品碼；改 `TapTargetGateTests`；QA e2e 情境測試（[LS-158](https://linear.app/little-sprout-app/issue/LS-158/harnessqa-端到端驅動不依賴-mobile-mcp-littlesproutuitests-加-qa-e2e)）。

## 驗收

- [ ] 該檔連跑 3 次綠＋timeout=0 mutation 紅證據
- [ ] merge-reviewer 通過（scope：只動測試檔＋可能的 helper）
- [ ] [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `5e989fe0` 銷除註記

## 留言（3）

### 2026-09-04T20:35:32.097Z · Chih-Lin Yeh · `c0386ea8`

**2026-09-05 04:36 orchestrator — 派工**

- 巡檢 04:33 lane:ui 連續空 2 輪、候補全卡使用者設計核可 → 依 LS-144 提前升 LS-96 `5e989fe0` 第二條。cycle 1；worktree `.claude/worktrees/LS-178`，分支 `fix/LS-178-avatar-uitest-flaky`（base origin/development `679a417`）。
- 派 ios-dev R1。

### 2026-09-04T20:51:50.301Z · Chih-Lin Yeh · `4322d083`

## handoff — LS-178（ios-dev，2026-09-05）

**Ticket**：LS-178 Chore：`CreateChildAvatarFieldUITests` 裸 `isHittable` 斷言 flaky → 改
`waitForExistence`＋hittable 輪詢，並掃同型斷言只列清單。PR **#286** → development，head
**`7bed50a`**（1 commit，base origin/development `679a417`）。

**重要發現（讀票文＋來源 comment 後確認）**：票文範圍 1 要求的核心修法**在本票開票前就已經
存在**。LS-158 merge-review R1 `be862711`（2026-09-04 21:47）在 §（7）明白建議：「開一張小 chore
對 LS-169 那支測試——把裸 `isHittable` 斷言換成『先 `waitForExistence`，再對 hittable 做
expectation 等待』…不建議把『rerun 就綠』當成處置」。這條建議隨後被記進 LS-96 `5e989fe0`
第二條（2026-09-05 01:18，來源標註「LS-158 merge-review R2 `69e71c2a`」，但翻查 `69e71c2a`
全文本身沒有再覆述這一條——它只是 R1→R2 沿用的既有備註）：「LS-169
`LittleSproutUITests/CreateChildAvatarFieldUITests` 裸 `isHittable` 斷言 flaky（無 wait／存在性
前置）；CI 連續紅兩次即升 chore 票，否則隨下一張碰該檔的 UI 票順修」。而**同一天稍早**
（commit時間戳 2026-09-04 21:53:44 +0800，即 13:53:44 UTC，早於 LS-178 開票時間 20:35:15 UTC
共 6 小時 42 分），LS-174 PR #282 commit `0165e61`（"orchestrator 裁定順手修（同檔域、不另
票）"）已經把該檔改成：`waitForExistence(timeout: 10)` → `NSPredicate("isHittable == true")` +
`expectation(for:evaluatedWith:)` + `wait(for:...)` 輪詢 → 斷言，並額外加了 frame ≥44pt 的幾何
量測——完全符合上述備註「否則隨下一張碰該檔的 UI 票順修」的後半句。LS-178 應是巡檢依 LS-144
開票責任在 LS-174 落地之後才被機械觸發的重複票（未察覺 LS-96 那則的後半句已被滿足）。已在
LS-96 `5e989fe0` 回覆說明（見下）。

**已完成**
1. 確認上述背景（LS-158 comment `be862711`／`69e71c2a`／LS-96 `5e989fe0` 逐條讀過，引據見上）。
2. `LittleSproutUITests/CreateChildAvatarFieldUITests.swift:31`：hittable 輪詢的 `wait(for:
   [hittableExpectation], timeout:)` 從 10 秒收斂到票文明確規定的「≤5 秒」上限——commit
   `7bed50a`。`waitForExistence(timeout: 10)`（第 27 行）維持不變，票文對它沒有秒數限制。
   未加 `LittleSproutUITests/Support/` helper：全檔（含 `QA/`）掃過，這個輪詢模式目前只有這一
   個呼叫點，抽 helper 是單一用途的抽象化（Simplicity First），沿用 LS-174 既有的內嵌寫法。
3. 同型斷言掃描（票文範圍 2，只列清單、不順修）：
   - `LittleSproutUITests/SectionTabBarPushRegressionTests.swift:45` — `XCTAssertTrue(app.buttons["發佈日記"].exists, ...)`
   - `LittleSproutUITests/SectionTabBarPushRegressionTests.swift:69` — `assertTabBarPresent` helper：`let exists = app.buttons.matching(identifier: identifier).firstMatch.exists`
   - `LittleSproutUITests/SectionTabBarPushRegressionTests.swift:78` — `assertTabBarAbsent` helper，同型
   - `LittleSproutUITests/SectionTabBarTests.swift:70` — `XCTAssertTrue(heading.exists, ...)`
   `TapTargetGateTests.swift`／`TapTargetGateSelfTests.swift`／`TapTargetMeasurement.swift`／`QA/`
   掃描結果乾淨，無同型裸 `isHittable`／`exists` 斷言。
4. LS-96 `5e989fe0` 第二條已回銷除註記：comment `82044e49`（附 PR #286＋commit `7bed50a`，並說明
   核心修法早於本票由 LS-174 落地的時序）。

**已驗證（怎麼驗）**
- 專屬模擬器 `LS-178-iPhone17Pro`（`B99F6068-3851-4017-8A4D-03DC6243E2E7`，`detect-simulator.sh`
  依 worktree 目錄名建立）：`xcodebuild build-for-testing` 一次成功，`test-without-building
  -only-testing:LittleSproutUITests/CreateChildAvatarFieldUITests -parallel-testing-enabled NO`
  連跑 **3 次全綠**（`Executed 1 test, with 0 failures`，6.2–7.0 秒／次）。
- Mutation：把 `wait(for: [hittableExpectation], timeout:)` 臨時改 `0`，重新建置後**第一次執行
  即紅**：`error: -[LittleSproutUITests.CreateChildAvatarFieldUITests
  test_avatarField_existsAndIsHittable] : Asynchronous wait failed: Exceeded timeout of 0
  seconds, with unfulfilled expectations: "Expect predicate \`isHittable == 1\` for object
  "新增寶貝照片，目前尚未選擇" Button".`——證明這段輪詢確實在做事、不是裝飾。已用 Edit 還原為
  `timeout: 5`，`git diff` 對照確認只剩 10→5 這一行；重新建置＋連跑 3 次確認回到全綠。
- `swiftlint lint --strict --quiet` 對變更檔：0 違規。
- `git push` 觸發本機 push gate（xcodegen 漂移／API 契約／錯誤碼三方對帳／migration 撞號分級／
  分支票號乾淨／merge 衝突預檢／unit tests）**一次全綠**（`✓ push gate 通過`）；本次 diff 只在
  `LittleSproutUITests/`、未觸及 `Features/` 或 `DesignSystem/`，push gate 本機不跑 UI 測試段
  （`push-gate.sh` 規則），但 CI 的 `ui-test-trigger.sh` 判定路徑集合含 `LittleSproutUITests/`
  前綴會命中，CI 的點擊目標 gate 會實際跑到本檔（不等 CI 結果，依規約 push 後即回報）。
- 本機容器 lock：**未持有**。`TapTargetMeasurement.launch(.createChild)` 是用
  `LS_TAP_TARGET_GATE_SCREEN` 環境變數直接跳進 `CreateChildView` 的量測 harness（見
  `TapTargetMeasurement.swift:11-17`），不需要登入或碰本機 Supabase 容器，本票的驗證段不算
  「模擬器對本機容器」的多步驟人機操作，故未 `--hold`。

**未完成**
- merge-review 尚未進行（PR 剛開）；reviewer informational 處置：無（待 review）。
- 票文範圍 2 的四筆同型斷言清單本身**不修**（scope 明訂「不順修他檔」）；若要修，需求分兩類：
  `SectionTabBarPushRegressionTests.swift:69/78`（helper 內部，兩個呼叫點共用）與其餘兩個獨立
  斷言——都比本票的單一呼叫點更適合抽 helper，留給下一張碰到這些檔案的 UI 票或另立 chore。
- LS-96 未新增其他池項（本票只做既定的第 4 條銷除，未發現新的 informational）。

**風險**
- 診斷面：本票唯一的程式碼變更是 1 行數字（10→5），風險極低；行為上更嚴格（更短的輪詢視窗），
  已用 mutation 驗證過等待邏輯本身有效，5 秒仍遠高於觸發原始 flaky 的「0 等待」情境。
- 流程面：LS-178 是重複票（核心修法已被 LS-174 順手修掉）——已在 LS-96 comment 誠實記錄時序，
  供 orchestrator 判斷是否需要在 harness lesson-learning 補一筆「LS-96 池項的『隨下一張票順修』
  處置沒有機械勾稽，導致後續巡檢仍機械升票」。

**產出位置**
- PR https://github.com/CLYEH/little-sprout/pull/286（head `7bed50a`；base `development`；body
  scratchpad `LS-178-pr-body.md`）。
- worktree `.claude/worktrees/LS-178`，branch `fix/LS-178-avatar-uitest-flaky`。
- 變更檔：`LittleSproutUITests/CreateChildAvatarFieldUITests.swift`（僅 1 行）。
- LS-96 銷除回覆：comment `82044e49`（父 comment `5e989fe0`）。
- 模擬器已關：`B99F6068-3851-4017-8A4D-03DC6243E2E7`（`LS-178-iPhone17Pro`，本票唯一 boot 過的
  一台；已 shutdown 並 delete，依派工單指示）。

### 2026-09-04T20:53:30.844Z · Chih-Lin Yeh · `f2fcc055`

**2026-09-05 04:5x orchestrator — 裁定：重複票，Canceled**

- 實作者 handoff `4322d083` 查證：裸 `isHittable` → `waitForExistence`＋hittable predicate 輪詢已由 LS-174 PR #282 commit `0165e61`（2026-09-04 13:53 UTC）先行落地；本票 20:35 UTC 才開。PR #286 只剩 timeout 10→5 的無需求改動 → 關閉不併、分支與 worktree 已清。
- 根因（orchestrator 自己的錯）：升 LS-96 池項前沒有 grep 該檔現況，且 LS-158 reviewer R2 沿用 R1 建議也未查證。教訓已記 memory：升池項為票前必 `git log -3 -- <檔>`＋grep 現況。
- LS-96 `5e989fe0` 第二條由實作者回覆 `82044e49` 銷除。

