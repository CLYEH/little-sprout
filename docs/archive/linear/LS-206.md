# LS-206 Task：LS-24 後端——家庭成員管理守門：唯一 Owner 且家庭仍有其他成員時不得退出（`family_members` BEFORE DELETE trigger，新錯誤碼 LS0xx，DETAIL 帶需先轉移提示）＋`transfer_ownership(p_family_id, p_to_user_id)` RPC 原子轉移（升對方、降自己同一交易；LS0xx 守門）＋DB 測試＋API.md §3／§4

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:S, lane:backend |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M6 合規與帳號 |
| 估點 | — |
| 建立 | 2026-09-05T13:09:38.097Z |
| 開始 | 2026-09-05T13:10:00.796Z |
| 完成 | 2026-09-05T18:03:00.958Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-206/taskls-24-後端-家庭成員管理守門唯一-owner-且家庭仍有其他成員時不得退出family-members-before |
| 父票 | LS-24 Story：app 內帳號刪除（多 Owner 轉移） |
| 關係 | related→LS-167, related→LS-164, related→LS-143, related→LS-204, blocks→LS-192, related→LS-33, related→LS-144, related→LS-6, related←LS-205, related←LS-96 |

## 描述

## 來源

巡檢 2026-09-05 21:08（[LS-144](https://linear.app/little-sprout-app/issue/LS-144/harness巡檢加開票責任機械提醒-lane-在飛-0-且無可派候補時動作清單印-開票並列來源story-拆票ls-96-p1-升票) 開票責任，backend lane ⚠ 連續空 2 輪）：核對 [LS-192](https://linear.app/little-sprout-app/issue/LS-192/taskls-152-ios-編輯顯示名稱與頭像02家庭成員管理0303-ax303-ipad03b-移除成員確認03c-轉移-owner)（02／03 個人與成員管理、退出家庭 03d／03e）票文所稱「`family_members` 角色與移除／轉移 Owner／退出 RPC（API.md §4）」——**實際不存在**：API.md §3／§4 與 migrations 只有 `family_members_delete` policy（owner 移除任何人；任何人可自行退出）與 `family_members_update`（owner 改 `role`／`can_upload`），`delete_my_account` 的「唯一 owner 且家庭還有其他成員 → LS050」守門只擋刪帳號路徑。因此：(a) 唯一 Owner 可直接 `DELETE family_members` 自行退出，家庭懸空（設計 03e「需先轉移 Owner」目前只有 client 端擋）；(b) 轉移 Owner 是兩次直接 `UPDATE`（升對方、降自己）非原子，[LS-192](https://linear.app/little-sprout-app/issue/LS-192/taskls-152-ios-編輯顯示名稱與頭像02家庭成員管理0303-ax303-ipad03b-移除成員確認03c-轉移-owner) 驗收明列的 race「轉移 Owner 與同時退出」在伺服器端無保證。PLAN §9-A2「唯一 Owner 刪帳號會讓整個家庭懸空，解法是多 Owner＋轉移」的不變量需要伺服器端守門。[LS-192](https://linear.app/little-sprout-app/issue/LS-192/taskls-152-ios-編輯顯示名稱與頭像02家庭成員管理0303-ax303-ipad03b-移除成員確認03c-轉移-owner) **blockedBy 本票。**

## 範圍

1. Migration（append-only）：`family_members` `BEFORE DELETE` trigger——被刪列 `role='owner'` 且該家庭無其他 owner 且仍有其他成員 → `raise exception` 新錯誤碼 **LS0xx**（沿 `delete_account` LS050 語意但獨立碼；`DETAIL` 帶 `{"family_id","family_name"}`）；`finalize_account_deletion`（service_role 路徑，先升最早成員為 owner 再刪）與 `delete_my_account` 情況 2（唯一成員，`DELETE FROM families` cascade）不得被誤擋——用 `pg_trigger_depth()`／session GUC 或明確判斷「家庭無其他成員」放行，寫清楚推演。
2. RPC `public.transfer_ownership(p_family_id uuid, p_to_user_id uuid)`：呼叫者須為該家庭 owner（否則 LS0xx）、對方須為該家庭現任成員（owner／member／viewer 皆可，否則 LS0xx）、對方 ≠ 自己；同一交易內 `UPDATE` 對方 `role='owner'`、自己 `role='member'`（`FOR UPDATE` 鎖兩列，避免與同時退出／另一筆轉移交錯）；回傳新角色對；`security definer set search_path=''`＋`revoke execute from public/anon`＋`grant to authenticated`。**直接** `UPDATE family_members.role` **的既有路徑保留**（[LS-33](https://linear.app/little-sprout-app/issue/LS-33/taskls-18-後端-邀請加入申請與審核的-schemarpcrls)／[LS-6](https://linear.app/little-sprout-app/issue/LS-6/phase-0-2supabase-專案schema-migrationrls-policies) 收斂不動），API.md 標明「轉移請走 RPC，直接改 role 只保留給單向升格」。
3. DB 測試（`supabase/tests/1xx_family_ownership_guard.sql`，lock 內跑）：唯一 owner 有其他成員退出 → LS0xx；唯一 owner 且唯一成員退出 → 允許（家庭隨後可由 delete_my_account 處理）；有兩個 owner 其一退出 → 允許；owner 移除一般成員 → 允許；`transfer_ownership` 正常／非 owner 呼叫／對方非成員／對方＝自己／兩筆併發轉移（第二筆序列化後應 LS0xx 因呼叫者已非 owner）；`delete_my_account` 情況 2 與 `finalize_account_deletion` 既有測試不回歸（`run.sh` 全綠，註明通道）。mutation：拿掉 trigger → 退出案紅；拿掉 `FOR UPDATE` → 併發案紅（或等價證明）。
4. `docs/API.md` §3 `family_members` 列「退出／移除」段補守門與錯誤碼，§4 新增 RPC，§8 錯誤碼表；`api-contract-check.sh` 綠。
5. 併入 main 後正式站 `db push`＋回讀（常設授權）。

## 不做

iOS（[LS-192](https://linear.app/little-sprout-app/issue/LS-192/taskls-152-ios-編輯顯示名稱與頭像02家庭成員管理0303-ax303-ipad03b-移除成員確認03c-轉移-owner)）；刪帳號 RPC 行為變更（[LS-24](https://linear.app/little-sprout-app/issue/LS-24/storyapp-內帳號刪除多-owner-轉移) 既有）；`family_members` policy 收斂成 RPC-only（另議）。

## 驗收

- [ ] DB 測試綠＋兩支 mutation；`run.sh` 全綠（handoff 註明通道：host psql／docker-exec／CI）
- [ ] merge-reviewer 通過（race：轉移 vs 退出、雙 owner 併發降級；scope：不動既有 policy）；QA 兩帳號實測退出守門與轉移；正式站 push 回讀

## 留言（15）

### 2026-09-05T13:10:41.338Z · Chih-Lin Yeh · `c72c06d0`

**Orchestrator（09-05 21:09）**：開票理由——巡檢 backend lane ⚠ 連續空 2 輪；核對 LS-192 票文所稱退出／轉移 RPC 實際不存在（僅 policy DML；唯一 owner 守門只在 `delete_my_account`），伺服器端不變量缺口＝真實前置。LS-192 已設 blockedBy 本票。worktree `.claude/worktrees/LS-206`（`feature/LS-206-family-ownership-guard` 自 development `9a25b76`）已建，ios-dev 已派；要求 handoff 註明 run.sh 通道（LS-204 教訓）。

### 2026-09-05T13:51:16.161Z · Chih-Lin Yeh · `cda95995`

## Handoff — LS-206

**Ticket**：家庭成員管理守門：唯一 Owner 且家庭仍有其他成員時不得退出＋`transfer_ownership` 原子轉移

**已完成**
- `supabase/migrations/20260905132350_family_ownership_guard.sql`：新增 `private.enforce_ownership_transfer_before_leave()`（`family_members` 的 `BEFORE DELETE` row-level trigger）——唯一 owner 且家庭仍有其他成員時退出／被移除 → `LS057`（`DETAIL` 帶 `{"family_id","family_name"}`），比既有 `LS001`（`AFTER STATEMENT` trigger）先觸發、範圍刻意窄於它。
- 新增 `public.transfer_ownership(p_family_id, p_to_user_id)`：同一交易原子轉移 owner 身份，取代非原子的兩次直接 `UPDATE`。呼叫者須為目前 owner（`LS058`）、對方須為目前成員（`LS059`）、對方不能是自己（`LS060`）；兩句各自的 `FOR UPDATE`（`least/greatest` user_id 排序）避免死鎖與 TOCTOU。
- 新測試 `supabase/tests/108_family_ownership_guard.sql`（10 情境）；更新 `30_owner_invariant.sql` 情境 1／6（回 `LS057` 而非 `LS001`）；`60_default_privileges.sql` 白名單登記新 RPC。
- `docs/API.md` §3／§4／§5／§9 同步；`LittleSprout/Errors/AppError.swift`／`AppErrorTests.swift` 補 4 個 `LSErrorCode`（`error-codes-check.sh` 三方對帳硬性要求，本票不做 UI）。

**已驗證（怎麼驗的）**
- DB 測試：docker-exec 管道（`bash scripts/ops/supabase-lock.sh -- bash supabase/tests/run.sh`）全綠，含既有 91/92/101/102/105 等 delete_my_account／finalize_account_deletion／併發回歸不受影響（LS-143 的 `delete_account_race_*.sql` 仍正確拿到 `LS001`）。lock 持有 19 分 37 秒。
- 108 測試檔 10 情境逐一過（唯一 owner 退出＋DETAIL、owner 移除一般成員、雙 owner 其一退出、唯一 owner 兼唯一成員退出的 deviation、transfer_ownership 正常／非 owner／對方非成員／對方＝自己／未登入／序列化第二筆）。
- Mutation 1（拿掉新 trigger）：`disable trigger` 後 `30_owner_invariant.sql` 情境 1 正確轉紅（回舊碼 LS001，不符期望 LS057）；已 `enable` 還原＋重跑全綠。
- Mutation 2（拿掉 `transfer_ownership` 的 `FOR UPDATE`）：手動建無鎖版本函式，用兩個真正並行的 docker exec psql 連線重現「owner 對兩個不同對象幾乎同時各發一次 transfer_ownership」——有鎖版正確序列化（第二筆 LS058、終態僅 1 人被扶正）；無鎖版第二筆用到過期 role 判斷，兩人皆被錯誤扶正成 owner（bug 重現）。已 `supabase db reset` 還原成 migration 原始版本＋重跑全綠。
- api-contract-check／error-codes-check／migration-version-check／migration-immutable-check／migration-breaking-check：皆綠。SwiftLint --strict：綠（AppErrorTests.swift 因新增列舉超出 function_body_length，合併 2 行既有 case 到同一行修正）。
- push gate（含 xcodebuild test 全量單元測試）：`✓ push gate 通過`。本次 push gate 自己 boot 的模擬器 `LS-206-iPhone17Pro`（`7E2EF9A0-EA26-4832-B88E-248C9ED9837A`）由 push-gate.sh 自身 EXIT trap 關閉，已確認 Shutdown。

**未完成 / Deviation（需 merge-reviewer／orchestrator 核對，非遺漏）**
- 票文範圍 3 驗收清單字面「唯一 owner 且唯一成員退出 → 允許」，本 PR **沒有**照字面實作。這個情境（直接對 `family_members` 一句 DELETE，不經過 `delete_my_account()`、不刪 `families` 本身）與 LS-143 三輪 merge-review 才收斂的併發安全網（`delete_account_race_*.sql`：兩位共同 owner 幾乎同時刪帳號，後動者必須被既有 `LS001` 擋下強迫重試）是**同一個資料庫狀態轉換**，trigger 層級無法用呼叫者身份區分。若照票文字面放寬，會讓那組併發安全網的後動者悄悄成功，留下一個 `families` 列還在、但 `family_members` 掛零、永遠不會被任何既有路徑枚舉到的孤兒列——比現況（強迫多一次重試）更糟。**處置：不修，理由如上**——離開唯一成員獨居家的正確路徑本來就是 `delete_my_account()`，不受本票影響、本來就一直可行。完整推演見 migration 檔頭「刻意不做的事」與 `108_family_ownership_guard.sql` 情境 4。若判定票文字面優先，需要另外裁決是否修改 LS-143 那組併發測試（不建議）。
- 正式站 `db push`＋回讀：待併入 main 後由 orchestrator 執行（常設授權）。
- QA 兩帳號實測：待 QA 在 test branch 驗收（本票無 UI，QA 需走 Supabase client 直接呼叫 RPC／SQL）。

**風險**
- 若 deviation 被判不可接受，需另外討論方案（例如另開 `leave_solo_family()` RPC），超出 size:S 範圍，留給後續裁決。
- `transfer_ownership` 的 `RETURNS TABLE` 簽章（4 欄）未經 LS-192 iOS 實作驗證是否符合 client 實際需要的形狀；目前無任何呼叫端，改簽章成本低。

**產出位置**
- PR：https://github.com/CLYEH/little-sprout/pull/323（`feature/LS-206-family-ownership-guard` → `development`），head `96b35d8`。
- migration：`supabase/migrations/20260905132350_family_ownership_guard.sql`
- 測試：`supabase/tests/108_family_ownership_guard.sql`／`30_owner_invariant.sql`／`60_default_privileges.sql`
- 文件：`docs/API.md`
- iOS：`LittleSprout/Errors/AppError.swift`／`LittleSproutTests/AppErrorTests.swift`
- 模擬器已關：`LS-206-iPhone17Pro`（`7E2EF9A0-EA26-4832-B88E-248C9ED9837A`）

### 2026-09-05T13:52:46.748Z · Chih-Lin Yeh · `8777388d`

**Orchestrator（09-05 21:52）**：handoff 收到（PR #323 head `96b35d8`）。申報偏離「唯一 owner 且唯一成員裸 `DELETE` 退出 → 維持 LS001、走 `delete_my_account` cascade」——**初判接受**（避免 0 成員的孤兒 `families` 列、不破 LS-143 `delete_account_race_*` 迴歸網；票文第 3 條該情境改為「拒絕並指向刪帳號路徑」），交 merge-reviewer 複核並確認 API.md §3 說清楚。已派 merge-reviewer R1：trigger／RPC mutation、兩 session 併發實跑（有／無 `FOR UPDATE`）、放行路徑既有測試、scope（`AppError` 只為三方 gate）、cascade 效能；lock 內工作壓最短（LS-164／202 QA 在等）。APPROVE 後 merge → QA 排隊 → 正式站 push。

### 2026-09-05T14:11:47.572Z · Chih-Lin Yeh · `c4be54a2`

## merge-review R1 — LS-206（PR #323）

**審的 head**：`96b35d8e5cecba4ed2e05274b7071f8b45f6093b`（開審時 `gh pr view 323 --json headRefOid` 一致，審完 22:08 再比對仍一致）
**base**：`git merge-base origin/development 96b35d8` = `9a25b76`
**Verdict：REQUEST_CHANGES**（1 major；無 blocker）

---

### 一、申報偏離（orchestrator 初判接受）→ **複核結果：接受，理由成立**

**(a) 既有斷言是否確實要求這條 —— 是，已逐檔查證。**
`supabase/tests/concurrency/delete_account_race_setup.sql:34-47` 建的是「兩位 owner、**沒有**其他成員」的家庭，`delete_account_race_s2.sql:33` 斷言後動者的 `delete_my_account()` 必須拿到 `LS001`。那個被擋下的資料庫狀態，正好就是票文字面要求放行的狀態轉換——「一句 `DELETE family_members` 之後該家庭 `family_members` 掛零、但 `families` 列還在」。要照字面放行，只能放寬 `private.enforce_family_has_owner()`（加一條「家庭已無其他成員就不擋」），那條路會直接讓 `delete_account_race_s2` 的斷言轉紅、並留下沒有任何成員又永遠不會被 `delete_my_account()` 迴圈枚舉到的孤兒 `families` 列。實作者的取捨正確。

補一個結構性論證，讓這個判斷不只依賴上面兩個檔：**新 trigger 只會「多擋」、不可能「少擋」**——它在 `BEFORE DELETE` raise，權威不變量仍是原封不動的 `enforce_family_has_owner()`（`AFTER STATEMENT` + `families` 列鎖）。因此它不可能在 owner 不變量的方向上造成迴歸，唯一的風險是誤擋合法路徑（下面 (b) 實跑）。

**(b) `delete_my_account` 情況 2 與 `finalize_account_deletion` 確實放行 —— 實跑通過。**
`bash scripts/ops/supabase-lock.sh -- bash supabase/tests/run.sh` 全綠（含 91／92／101／102／105 與全部 concurrency 場景）。特別確認 `30_owner_invariant.sql` 情境 5（3 人家庭整個 `delete from families`，走 trigger 的 families-存在性提前放行）與 `102_delete_account_media.sql`／`delete_account_case2_vs_media_*`／`delete_account_vs_finalize_media_*` 皆綠。
另讀碼確認 `finalize_account_deletion`（`20260904080802:127-131`）是「先 `UPDATE` 升最早成員、再 `DELETE` p_user」，新 trigger 在 p_user 那一列會看到已升格的新 owner 而放行。

**(c) API.md §3 對「唯一成員退出」的說明 —— 清楚，指向刪帳號路徑。**
`docs/API.md:240-249` 明寫「仍被既有 `LS001` 擋下……要離開唯一成員的獨居家，正確路徑一律是 `delete_my_account()`（見 §4）」，並附完整理由與 migration 檔頭交叉引用。這一段沒問題。

---

### 二、Findings

#### M1（major）`docs/API.md:1454-1455` — §4 `delete_my_account()` 情況 1 與本 PR 新增的 §3 直接矛盾

§4 情況 1（`LS050` 的補救指示）仍寫：

> 轉移本身走既有路徑——owner 對 `family_members.role` 的直接 `UPDATE`（見 §3 `family_members`），本 RPC 不提供另一支轉移用的 RPC。

本 PR 在 `docs/API.md:250-254` 新增的 §3 卻寫「**任何會把自己從 owner 降級、同時把別人升成 owner 的操作，一律走 `transfer_ownership`**；直接 `UPDATE` 只保留給單向升格」。同一份文件對同一個操作給出相反指示，而 §4 那一段正是使用者碰到 `LS050` 之後最先被導向的地方。

**具體失敗情境**：LS-192（`blockedBy` 本票，就是為了拿這份契約才等）實作「轉移 Owner」畫面時，讀 §4 情況 1 的補救說明，照它寫的「兩次直接 `UPDATE`」實作——於是重新引入本票開票理由裡明列、也是 LS-192 驗收明列的 race（「轉移 Owner 與同時退出」在伺服器端無保證），而 API.md 這份唯一契約來源背書了這個做法。票文範圍 2 要求的「API.md 標明轉移走 RPC」目前只做到 §3，§4 的舊指示沒清掉。

**建議修法**：§4 情況 1 那兩行改成指向 `transfer_ownership`（例如：「轉移請呼叫 `transfer_ownership`（見 §4，LS-206）；直接 `UPDATE role` 只保留給單向升格」）。一句話的事，不動任何 SQL。

#### m1（minor）`supabase/tests/108_family_ownership_guard.sql:320-358` — 沒有任何 committed 測試會因為拿掉 `FOR UPDATE` 而轉紅

情境 10 是「第一筆 `commit` 之後第二筆才開始」的**完全序列化**腳本，**有鎖無鎖都會過**。票文允許「或等價證明」，所以不是 blocker；但目前 `FOR UPDATE` 這個性質只由兩次一次性的手動實跑保護（實作者一次、我一次），repo 裡沒有留下迴歸網——而 `supabase/tests/concurrency/` + `run.sh` 的成對 session driver 正是為這一類 bug 準備的既有治具。

**我自己重跑的兩 session 實測**（docker exec 兩條真並行連線，同一位 owner 幾乎同時對兩個不同對象各發一次轉移；S1 持交易 3 秒，S2 延遲 1.2 秒進場）：

| 版本 | S2 阻塞 | S2 結果 | 終態 owner 數 |
|---|---|---|---|
| 現行（有 `FOR UPDATE`） | 1.801 s | `ERROR: LS058` → `ROLLBACK` | 1（僅第一筆的對象） |
| mutant（拿掉兩句 `for update`，其餘相同） | 1.789 s | 成功，回傳 `from_role=member / to_role=owner` | **2（兩人都被扶正）** |

無鎖版第二筆讀到過期的 `role='owner'` 而通過檢查，`enforce_family_has_owner()` 對「2 位 owner」不會有意見，所以沒有任何既有防線會接住——**`FOR UPDATE` 是真的必要，實作者的說法屬實**。順帶一提，無鎖版 S2 也阻塞了 1.79 s（卡在 `AFTER STATEMENT` trigger 的 `families` 列鎖），所以「有沒有被阻塞」不能當判別式，只有終態 owner 數可以。

**建議**：補一組 `concurrency/transfer_race_{setup,s1,s2,verify}.sql`，verify 斷言 `owner_count = 1` 且被扶正的是第一筆的對象。

#### m2（minor，PLAUSIBLE，未實測重現）`supabase/migrations/20260905132350_family_ownership_guard.sql:156-161` — 鎖序宣稱的範圍過寬，跨路徑死鎖未評估

檔頭寫「任何兩筆併發呼叫（不論呼叫者／對象怎麼交錯）都會依 user_id 遞增序排隊，**不會互為死鎖**」。這句話在 `transfer_ownership` 彼此之間成立，但 `family_members` 上還有另外兩條也取列鎖的路徑：`delete_my_account()`（`20260904212530:949`）與 `finalize_account_deletion()`（`20260904080802:100`），兩者都是 `perform 1 from public.family_members where family_id = X **for update**`——**一句話鎖整個家庭的全部成員列、沒有 `order by`**，實際取鎖順序由計畫決定（小表很可能 seq scan → heap 順序，不是 user_id 順序）。

**具體失敗情境**：owner A 對同一家庭呼叫 `transfer_ownership`（依 user_id 遞增鎖 2 列），成員 B 同時呼叫 `delete_my_account()`（一句鎖全部列、順序不保證）。若兩者取鎖順序相反並交錯，Postgres 偵測到 `40P01` deadlock、砍掉其中一筆。**不會資料損壞、也不是本 PR 新引入的鎖（既有兩條路徑本來就這樣鎖）**，但檔頭那句「不會互為死鎖」目前是過寬的宣稱，之後的人會據此以為這件事已被排除。

**建議**：把該句限定成「兩筆 `transfer_ownership` 之間」，並補一行說明與 `delete_my_account()`／`finalize_account_deletion()` 的全家庭 `for update` 之間存在順序不保證、最壞是 `40P01`、呼叫端重試即可。（不建議在本票改那兩支既有函式的鎖法。）

#### m3（minor，docs）`delete_my_account()` 現在會噴 `LS057`，文件沒有記

**實測**（3 人家庭：owner g0、owner g1、member g2，兩位 owner 幾乎同時呼叫 `delete_my_account()`）：後動者拿到

```
ERROR:  LS057: 你是「…」的唯一 owner，且家庭還有其他成員，須先把 owner 身份轉移給其他成員才能退出或被移除
DETAIL:  {"family_id" : "…", "family_name" : "…"}
CONTEXT:  PL/pgSQL function private.enforce_ownership_transfer_before_leave() line 41 at RAISE
SQL statement "delete from public.family_members where family_id = v_family_id and user_id = v_uid"
```

——本 PR 之前這裡是 `LS001`。終態安全（整筆回滾，家庭仍有 1 owner + 1 member），重試時情況 1 守門會正確回 `LS050`；Swift 端 `LS001`／`LS057` 同為 `.rejected`、`AppError` 沒有逐碼分流，**沒有 UI 迴歸**，訊息其實比 `LS001` 更準。純文件完整性：§4 `delete_my_account()` 的錯誤碼說明與 §5 `LS057` 那一列（`docs/API.md:1730`）都只描述「直接對 `family_members` 的 DELETE」，沒提 `delete_my_account()`／`finalize_account_deletion()` 內部的 DELETE 同樣會觸發。建議 §5 那一列補半句。

#### i1（informational）`…family_ownership_guard.sql:41` — 檔頭推演的條件編號錯位

`finalize_account_deletion()` 那段寫「直接落入**條件 2** 放行」，實際放行它的是條件 3（「排除自己後還有其他 owner」）；條件 2 是 families 存在性檢查。純註解筆誤。

#### i2（informational）批次 `DELETE FROM family_members WHERE family_id = X` 的錯誤碼不確定

`family_members_delete` policy 允許 owner 一句刪光全家。row-level trigger 對 owner 那一列的判斷取決於同一句裡其他列有沒有先被處理掉，因此會不確定地回 `LS057` 或 `LS001`。兩者都是拒絕、無正確性影響，且是 row-level trigger 的固有性質，列出僅供知悉。

#### i3（informational）`LittleSproutTests/AppErrorTests.swift:224` 把既有兩行併成一行

為了讓新增 4 個 case 之後仍在 SwiftLint `function_body_length` 門檻內，屬於「自己造成的 mess 自己收」，範圍最小，可接受。

#### i4（informational）`transfer_ownership` 回傳的是硬編角色而非 `returning` 實際列

因為兩列都已 `FOR UPDATE` 鎖住且證明存在，兩句 `UPDATE` 必然各影響 1 列，硬編值不會說謊。無需修改。

---

### 三、其餘維度

- **Race（trigger）**：新 trigger 的三個 `exists` 都是不加鎖的讀，**這是安全的**——它只加拒絕、不放行，權威序列化仍在原封不動的 `enforce_family_has_owner()`（`families` 列 `FOR NO KEY UPDATE`）。實跑 `owner_guard_*`／`delete_account_race_*`／`delete_account_vs_approve_join_*` 皆綠，`delete_account_race_s2` 仍正確拿到 `LS001`。
- **Race（RPC，我實測的第三個場景）**：`transfer_ownership` vs 對象同時被移除——S1 移除 member U1 並持交易 3 秒，S2 在 1.2 s 時 `transfer_ownership(F, U1)`：S2 正確卡在 `FOR UPDATE` 1.806 s，解鎖後用新查詢讀到對方已不存在，回 `LS059`，終態 owner 數 1。TOCTOU 關閉，符合檔頭推演。
- **Trigger 觸發順序**（實查 `pg_trigger`）：`family_members` 上 BEFORE DELETE 只有 `family_members_not_suspended`（`n` < `o`，先跑）與 `family_members_ownership_transfer_guard`——停權檢查先於 owner 守門，順序正確；`family_members_deletion_guard` 只掛 INSERT/UPDATE OF，不受影響。`transfer_ownership` 的兩句 `UPDATE` 會經過 `not_suspended`（LS052／LS053）與 `deletion_guard`（LS051），停權者／待刪帳號無法被扶正成 owner——縱深防禦自然成立。
- **無 GUC 後門**：trigger 不讀 `private.deletion_bypass_active()`、不看 `pg_trigger_depth()`，全靠「families 還在不在」＋「排除自己後的成員／owner 集合」判斷。符合票文偏好。
- **效能**：`old.role <> 'owner'` 提前 return，非 owner 列 0 次查詢；owner 列最多 3 次，全部命中 PK `(family_id, user_id)` 前綴（`20260822120000_init_schema.sql:74`）。`delete_my_account` 情況 2 的 cascade 大量刪除：`families` 父列先消失，owner 那列只多 1 次 `families` 主鍵查詢，其餘成員列 0 次。**成本可忽略，票文擔心的 per-row count 退化不存在。** RLS policy 未被更動，`50_rls_plan_no_percall_subquery.sql` 綠。
- **權限**：`security definer set search_path = ''`、`revoke ... from public, anon`、`grant to authenticated` 齊備；`60_default_privileges.sql` 白名單只新增一行；`error-codes-check`（34 碼三方一致）、`api-contract-check`（RPC 36 支）皆綠。
- **參數守門順序**無資訊洩漏：LS060（自己）→ LS058（呼叫者非 owner）→ LS059（對方非成員），非 owner 無法用這支 RPC 探測他人是否為某家庭成員。
- **Scope**：7 個檔全部落在票文範圍內。migration append-only（`migration-immutable-check` ✓、`migration-version-check` ✓ 無撞號、時間戳 `20260905132350` 大於既有最大值）；`30_owner_invariant.sql` 只改情境 1／6 的期望碼＋註解（LS001 的 DELETE 路徑覆蓋沒有消失：情境 2 的 UPDATE 降級、108 情境 4、`delete_account_race_s2` 都還在）；`60_default_privileges.sql` 只加一行白名單；`AppError.swift`／`AppErrorTests.swift` 只加 LS057–060 對應（三方 gate 硬性要求，非 UI）。**無 iOS 邏輯變更、無順手重構。**

---

### 四、驗證方式（怎麼驗的）

- **run.sh 通道：docker-exec**（`docker exec supabase_db_little-sprout psql`；本機 host 無 `psql`，run.sh 自動選這條）。`bash scripts/ops/supabase-lock.sh -- supabase db reset` → `… -- bash supabase/tests/run.sh` → `✓ 全部 DB 測試通過`。另 CI `db` job（host psql／`SUPABASE_DB_URL` 通道）在同一個 head 已 pass（3m46s）。
- **Mutation 1（新 trigger）**：`alter table public.family_members disable trigger family_members_ownership_transfer_guard` → `30_owner_invariant.sql` rc=3、`108_family_ownership_guard.sql` rc=3，兩者都退回 `LS001`（不符期望的 `LS057`）；`enable` 還原後 `pg_trigger` 五顆全為 `tgenabled='O'`，兩檔重跑 rc=0。**mutation 有效。**
- **Mutation 2（`FOR UPDATE`）**：見 m1 表格，兩 session 真並行，有鎖／無鎖各跑一次；mutant 函式 `public.ls206_nolock_transfer` 已 `drop`，全部 fixture 已刪除並回讀確認（`leftover_fn` NULL、`leftover_fam` 0）。
- **額外併發**：轉移 vs 對象同時被移除（→ LS059）、3 人家庭兩位 owner 併發 `delete_my_account()`（→ LS057，見 m3）。
- **Gates**：`api-contract-check` ✓、`error-codes-check` ✓、`migration-version-check` ✓、`migration-immutable-check` ✓、`migration-breaking-check --base 9a25b76` 無任何 DESTRUCTIVE／BREAKING 命中。
- **lock**：`cd .claude/worktrees/LS-206 && supabase-lock.sh --hold "LS-206 merge-review R1" --max-minutes 25`，`--release` 印**持有 3 分 33 秒**（LS-164／202 QA 在等，全部實跑壓在一次 hold 內做完）。

### 五、沒看的範圍（誠實聲明）

- 沒有實測重現 m2 的跨路徑死鎖（需要刻意操縱計畫的取鎖順序），因此標 PLAUSIBLE、不列 blocker。
- 沒有走 PostgREST／HTTP 實測 `transfer_ownership` 的 `RETURNS TABLE` 在 client 端的形狀（PR「風險」欄自陳、且目前無呼叫端）——留給 LS-192／QA。
- 正式站 `db push` 回讀不在本次 review 範圍。

---

**修掉 M1（一句話）即可重審；m1／m2／m3／i1 的處置請在下一輪 handoff「未完成」欄逐條交代（修／記入 LS-96／另票／不修＋理由）。**

### 2026-09-05T14:16:06.716Z · Chih-Lin Yeh · `ab17c619`

**Orchestrator（09-05 22:13）**：merge-review R1 `c4be54a2` REQUEST_CHANGES；申報偏離（唯一成員裸退出維持 LS001 → 走 `delete_my_account`）reviewer 複核**接受**，票文第 3 條該情境以此為準。裁定 R2 全收：M1 API.md §4 改指向 `transfer_ownership`；m1 補 `concurrency/transfer_race_*`（拿掉 `for update` 要紅）；m2 死鎖宣稱收窄並寫重試語意；m3 §4／§5 補 LS057；i1 編號；i2～i4 不修＋理由。已續派實作；lock 內只跑一次 run.sh。

### 2026-09-05T14:27:03.790Z · Chih-Lin Yeh · `ee94b6f4`

**Orchestrator（09-05 22:13）**：review R1 `c4be54a2` REQUEST_CHANGES；偏離（唯一成員裸退出維持 LS001）reviewer 複核接受。R2 全收：M1 API.md §4 指向 `transfer_ownership`；m1 補 `concurrency/transfer_race_*`；m2 死鎖宣稱收窄；m3 補 LS057；i1 編號；i2～i4 不修。已續派實作。

### 2026-09-05T14:33:25.946Z · Chih-Lin Yeh · `a7b0be05`

## Handoff — LS-206 R2（回應 merge-review R1 `c4be54a2` REQUEST_CHANGES）

**Ticket**：家庭成員管理守門：唯一 Owner 且家庭仍有其他成員時不得退出＋`transfer_ownership` 原子轉移

**已完成（R2）**
- **M1（major）已修（commit `53aa7a5`）**：§4 `delete_my_account()` 情況 1 原本仍寫「轉移走 owner 直接 `UPDATE role`、本 RPC 不提供轉移 RPC」，與同 PR §3「一律走 `transfer_ownership`」矛盾——改成指向 `transfer_ownership`。
- **m1（minor）已修（commit `746d169`）**：新增 `supabase/tests/concurrency/transfer_race_{setup,s1,s2,verify}.sql`（沿 `delete_account_race_*` 兩 session 形式，掛進 `run.sh` 既有 `race_case`）——同一位 owner 幾乎同時對兩個不同對象各發一次轉移，S2 阻塞 1.82 秒後正確拿到 `LS058`，終態僅 1 位 owner。
- **m2（minor，PLAUSIBLE）已修文件（commit `746d169`）**：migration 檔頭「不會互為死鎖」收窄為僅限兩筆 `transfer_ownership` 之間；補一段與 `delete_my_account()`／`finalize_account_deletion()`（全家庭無序 `for update`）跨路徑最壞情況 `40P01`、client 重試語意的說明，不動既有兩支函式的鎖法。
- **m3（minor，docs）已修（commit `53aa7a5`）**：§4／§5 補上 `delete_my_account()`／`finalize_account_deletion()` 在併發窗口下也可能觸發 `LS057` 的情境。
- **i1（informational）已修（commit `746d169`）**：migration 檔頭「直接落入條件 2 放行」編號錯位訂正為「條件 3」。
- **i2（informational，不修）**：批次 `DELETE FROM family_members WHERE family_id = X` 的錯誤碼（`LS057` 或 `LS001`）不確定，取決於同一句裡其他列有沒有先被處理掉——reviewer 已指出兩者皆是拒絕、無正確性影響，是 row-level trigger 固有性質，僅供知悉，不修。
- **i3（informational，不修）**：`AppErrorTests.swift` 把既有兩行併成一行以符合 `function_body_length` 門檻——reviewer 已認可「自己造成的 mess 自己收，範圍最小，可接受」，不需回復。
- **i4（informational，不修）**：`transfer_ownership` 回傳硬編角色而非 `RETURNING` 實際列——reviewer 已確認兩列皆已 `FOR UPDATE` 鎖住且證明存在，硬編值不會說謊，無需修改。

**已驗證（怎麼驗的）**
- lock 內只跑一次 `run.sh`：docker-exec 管道（`cd .claude/worktrees/LS-206 && bash scripts/ops/supabase-lock.sh --hold "LS-206 R2" --max-minutes 20`）→ `supabase db reset` → `bash supabase/tests/run.sh` → `✓ 全部 DB 測試通過`（含新增的 `transfer_race` 併發情境，S2 正確拿到 `LS058`，終態 owner 1 位）。`--release` 印「持有 2 分 45 秒」。
- `api-contract-check`／`error-codes-check`／`migration-version-check`／`migration-immutable-check`／`migration-breaking-check`：皆綠。
- SwiftLint `--strict`：綠（R2 未改任何 Swift 檔）。
- push gate（含 `xcodebuild test` 全量單元測試，因分支整體仍含 R1 的 Swift 變更而全量跑）：`✓ push gate 通過`；模擬器 `LS-206-iPhone17Pro`（`7E2EF9A0-EA26-4832-B88E-248C9ED9837A`）再次確認 `Shutdown`。

**未完成**
- 與 R1 相同：正式站 `db push`＋回讀待併入 main 後由 orchestrator 執行；QA 兩帳號實測待 test branch 驗收。
- Deviation（票文範圍 3 字面「唯一 owner 且唯一成員退出 → 允許」未照字面實作）：**merge-review R1 已複核接受**，理由不變（與 LS-143 併發安全網共用同一個資料庫狀態轉換，trigger 層級無法區分呼叫者身份），詳見 PR body R1／R2 段與 migration 檔頭「刻意不做的事」。

**風險**：與 R1 handoff 相同（deviation 若被判不可接受需另外裁決；`transfer_ownership` 回傳形狀未經 LS-192 驗證）。

**產出位置**
- PR：https://github.com/CLYEH/little-sprout/pull/323，head `746d169`（R1 `96b35d8`）。
- R2 新增：`supabase/tests/concurrency/transfer_race_{setup,s1,s2,verify}.sql`；`supabase/tests/run.sh`（掛新併發情境）。
- R2 修改：`docs/API.md`（M1／m3）；`supabase/migrations/20260905132350_family_ownership_guard.sql`（m2／i1，皆為註解，無行為變更）。
- 模擬器已關：`LS-206-iPhone17Pro`（`7E2EF9A0-EA26-4832-B88E-248C9ED9837A`）。

### 2026-09-05T14:36:47.902Z · Chih-Lin Yeh · `d4c53821`

**Orchestrator（09-05 22:35）**：R2 handoff 收到（head `746d169`；M1 §4 改指向 `transfer_ownership`、m1 `concurrency/transfer_race_*` 掛進 `race_case`、m2 檔頭收窄、m3 LS057、i1；i2～i4 不修＋理由）。已續派 merge-reviewer R2（併發測試＋去 `for update` mutation 實跑、全文無「直接 UPDATE role」殘句、migration 本體不變）。APPROVE 後 merge → QA 排隊 → 正式站 push。

### 2026-09-05T14:40:01.174Z · Chih-Lin Yeh · `5e31f469`

**Orchestrator（09-05 22:35）**：R2 handoff 收到（head `746d169`；M1 §4 改指向 `transfer_ownership`、m1 `concurrency/transfer_race_*` 掛進 `race_case`、m2 檔頭收窄、m3 LS057、i1；i2～i4 不修＋理由）。已續派 merge-reviewer R2（併發測試＋去 `for update` mutation 實跑、全文無「直接 UPDATE role」殘句、migration 本體不變）。APPROVE 後 merge → QA 排隊 → 正式站 push。

### 2026-09-05T14:55:28.547Z · Chih-Lin Yeh · `863165ca`

## merge-review R2 — LS-206（PR #323）

**審的 head**：`746d1696ea6c1c49702d92aaa024c9a10ddfa865`（開審與收尾兩次比對 `gh pr view 323 --json headRefOid` 皆一致）
**增量**：`96b35d8...746d169`（`53aa7a5` docs／`746d169` tests＋檔頭）
**Verdict：APPROVE**（R1 的 1 major、3 minor、1 informational 全數處理；本輪無新增 blocker／major）

---

### 一、R1 findings 逐項複核

| R1 | 狀態 | 複核 |
|---|---|---|
| **M1**（major，API.md §4／§3 矛盾） | **已修** | `docs/API.md:1454-1458` 改成「轉移請呼叫 `transfer_ownership`（見 §4，LS-206）；直接 `UPDATE role` 只保留給單向升格……不要拿它做轉移用的兩次 `UPDATE`」，與 §3（`:250-254`）語意一致。**全文 grep 複驗**：`轉移` 的全部命中逐條看過，沒有任何殘句還在指示「用直接 `UPDATE role` 做轉移」——`:1574` 是描述哪些 DML 會觸發 `enforce_family_has_owner()` 的既有敘述（陳述事實，非指示），`:228`「Owner 只能改 `role`（升降）」是 policy 能力描述，緊接其後的 `:250-254` 就是限縮條款。M1 關閉。 |
| **m1**（minor，`FOR UPDATE` 無迴歸網） | **已修** | 新增 `concurrency/transfer_race_{setup,s1,s2,verify}.sql`，掛進 `run.sh:509-519` 既有 `race_case`（4 參數簽章正確，S1／S2 的 rc 與 verify 三處都會讓整支 run.sh 非 0 結束）。**我自己實跑 mutation 驗證，見下方三**。斷言比我建議的更強：verify 除了 `owner_count = 1` 還逐人釘死角色（O=member／A=owner／B=member），且 S2 自己也斷言「必須被阻塞 ≥0.5s」＋「必須噴 LS058」。m1 關閉。 |
| **m2**（minor PLAUSIBLE，鎖序宣稱過寬） | **已修（文件）** | migration 檔頭 `:161-186` 把「不會互為死鎖」限縮成「任兩筆 `transfer_ownership` 呼叫彼此之間」，並新增「範圍收斂聲明」整段：明列 `delete_my_account()`／`finalize_account_deletion()` 的單句全家庭無序 `for update`、最壞 `40P01`、非本 PR 新引入、client 重試即可、且刻意不動既有兩支函式的鎖法。與我 R1 的描述一致且更完整。m2 關閉。 |
| **m3**（minor，`delete_my_account` 可噴 LS057 未記） | **已修** | §4 `delete_my_account` 錯誤碼段（`:1540-1549`）與 §5 `LS057` 列（`:1740`）都補上「`delete_my_account()`／`finalize_account_deletion()` 內部的 `DELETE FROM family_members` 在併發窗口下也可能觸發、整筆回滾、重試改拿 `LS050`」。與我 R1 實測到的行為（3 人家庭兩位 owner 併發刪帳號 → 後動者拿 LS057）完全吻合。m3 關閉。 |
| **i1**（檔頭條件編號錯位） | **已修** | `:41-42` 訂正為「條件 3」並註明原誤寫。 |
| **i2／i3／i4**（不修） | **理由核對通過** | i2 批次 DELETE 錯誤碼不確定＝row-level trigger 固有性質、兩者皆拒絕；i3 `AppErrorTests.swift` 併行為 SwiftLint 門檻所需；i4 硬編回傳值因兩列皆已鎖住且證明存在而不會說謊。三條皆與我 R1 的判斷一致，不修正確。 |

---

### 二、Scope（增量）

`git diff --stat 96b35d8...746d169` = 7 檔：`docs/API.md`、migration、4 支新測試、`run.sh`。

- **migration 本體未動**：`git diff` 逐行確認 `20260905132350_family_ownership_guard.sql` 的 31 行變更**全部在 `--` 註解區**（檔頭第 1 節條件編號、第 2 節鎖序段），`create or replace function`／`create trigger`／`revoke`／`grant`／`comment on` 的 SQL 本體一字未改。
- **`migration-immutable-check`**：對 PR 實際 base（`--base origin/development`，即 CI `rules` job 與 push-gate 的用法）**綠**——這支 migration 在 development 尚不存在，屬新增檔，PR 內修訂合法。（附帶說明：若刻意改用上一輪 head `96b35d8` 當 base 它會紅，但那不是任何 gate 的呼叫方式，非問題。）
- **`run.sh` 的更動只有兩處**：新增一個 `race_case` 呼叫；`cc_cleanup` 補上 `de000000-…0001`（家庭）與 `de100000-…0001/2/3`（使用者）三個新 fixture id。既有邏輯零改動。
- **fixture id 無撞號**：`de000000`／`de100000` 前綴 grep 全 repo 只出現在這 4 支新檔＋`run.sh` 的 cleanup。email 用 `ls206-race-*@ls206.test`，與 108 的 `ls206-u*@ls206.test` 不撞；setup 亦自帶前置 `delete`。
- **本輪未動任何 Swift**；全 PR（`9a25b76...746d169`）共 12 檔，全部落在票文範圍。

---

### 三、實跑驗證（本輪自己做的）

**通道：docker-exec**（本機無 host `psql`）。lock 內 `supabase db reset`（rc=0）後，直接以 `race_case` 的同一組四支檔跑三輪：

| 輪次 | setup | S1 rc | S2 rc | verify rc | 結果 |
|---|---|---|---|---|---|
| **baseline**（migration 原樣） | 0 | 0 | **0** | **0** | S2 阻塞 **1.82 s** 後 `LS058=t`；終態 owner 1 位（A），O／B 皆 member |
| **mutant**（`transfer_ownership` 拿掉兩句 `perform … for update`，其餘一字不改） | 0 | 0 | **3** | **3** | S2：`FAIL 併發：… 沒有噴 LS058 …（TOCTOU 沒有被鎖住）`（阻塞 1.78 s、`LS058=f`）；verify：`FAIL 併發：家庭應恰好剩 1 位 owner，實際 2 位` |
| **restored**（重新套回檔案原文） | 0 | 0 | **0** | **0** | 與 baseline 相同，恢復確認 |

**結論：m1 要的迴歸網真的守得住。** mutation 在**兩個獨立斷言點**同時轉紅（S2 的 `LS058` 斷言、verify 的終態 owner 數＋逐人角色），不是只靠單一條件。mutant 函式由 migration 檔實際文本以 `sed` 刪掉那 6 行產生（`diff` 確認除這兩句 `for update` 外零差異），不是另寫一份近似品。

補充一個對測試設計的觀察（**非 finding，供日後參考**）：mutant 的 S2 **也**被阻塞了 1.78 s（卡在 `AFTER STATEMENT` trigger 的 `families` 列鎖，不是 `FOR UPDATE`），所以 S2 裡那條 `elapsed < 0.5` 的斷言**單獨無法**判別 mutation——真正判別的是 `not v_raised` 與 verify 的終態。這一點測試本身寫對了（兩條斷言都在），只是註解容易讓人以為阻塞秒數就是判別式。

**其餘**：
- **CI**（同一 head `746d169`）四個 job 全 pass：`db` 3m45s（host psql／`SUPABASE_DB_URL` 通道跑完整 `run.sh`，含新掛的 `transfer_race` 情境）、`rules` 4m42s（含 `migration-immutable`／`migration-breaking`／`api-contract`／`error-codes` 以正確 base 執行）、`ci` 13m39s、`lint` 15s。
- **本機 gates**：`api-contract-check` ✓（RPC 36 支）、`error-codes-check` ✓（34 碼三方一致）、`migration-version-check` ✓（37 檔無撞號）、`migration-immutable-check --base 9a25b76` ✓、`migration-breaking-check --base 9a25b76` 無任何 DESTRUCTIVE／BREAKING 命中。
- **四維度**：R1 已完整審過 race／效能／平行／scope，本輪增量不含任何 SQL 行為變更（migration 只動註解），故不重複；R1 的結論（trigger 只加拒絕不放行、per-row 成本可忽略、無 RLS policy 變動、`FOR UPDATE` TOCTOU 已關）在此 head 仍成立，並由上表 baseline／restored 兩輪再次驗證。

---

### 四、沒看的範圍（誠實聲明）

- 本輪**沒有**再跑一次完整 `run.sh`（本機）——CI `db` job 在同一 head 已跑完整套並 pass，我把本機 lock 時間讓給排隊中的 QA，只跑了 mutation 必須用到的三輪 race case。
- m2 的跨路徑 `40P01` 仍**未實測重現**（需要刻意操縱查詢計畫的取鎖順序）；R2 已如實記錄於 migration 檔頭，處置為「文件收窄、不動既有鎖法」，同意。
- `transfer_ownership` 經 PostgREST／HTTP 的回傳形狀未驗（無呼叫端），留給 LS-192／QA。
- 正式站 `db push` 回讀不在 review 範圍。

---

### 五、lock

`cd .claude/worktrees/LS-206 && supabase-lock.sh --hold "LS-206 merge-review R2" --max-minutes 20`；`--release` 印**持有 1 分 10 秒**（另在 LS-164 QA 的兩段 hold 後方排隊等待約 13 分鐘，期間未碰容器；第一個等待者逾時後由第二個接手取得）。

**APPROVE。**

### 2026-09-05T15:47:28.520Z · Chih-Lin Yeh · `1bf93380`

## QA 裁決：PASS

驗收基準：`test` branch delta 合併 `da62930`。DB 段與 LS-204 共用同一次 `--hold "QA da62930 backend"`（持有 6 分 28 秒，已釋放）。

### 逐條驗證

- ✓ Migration `20260905132350_family_ownership_guard.sql` 套用乾淨（`supabase db reset` 全部 migrations 依序成功）。
- ✓ `108_family_ownership_guard.sql` 十情境全綠（docker-exec 通道）：情境1 唯一 owner 退出→LS057＋DETAIL 正確；情境2 owner 移除一般成員→允許；情境3 雙 owner 其一退出→允許；情境4（**申報偏離，見下**）唯一 owner 兼唯一成員退出→仍 LS001；情境5 `transfer_ownership` 成功＋DB 實際狀態正確；情境6 非 owner 呼叫→LS058；情境7 對方非成員→LS059；情境8 對方＝自己→LS060；情境9 anon 呼叫→42501；情境10 序列化後第二筆轉移→LS058。
- ✓ `concurrency/transfer_race_*`：兩個並行 `transfer_ownership` 打同一個 owner 對兩個不同對象，S2 被阻塞 1.80 秒後正確拿到 LS058，終態僅 1 位 owner——併發安全網跑於 `run.sh` 併發段，綠。
- ✓ **Mutation 已見於 PR body**：拿掉 trigger → `30_owner_invariant.sql` 情境 1 轉紅（回到舊碼 LS001）；拿掉 `FOR UPDATE` → 手動雙連線重現兩人皆被錯誤扶正成 owner；兩者皆已還原並確認 `run.sh` 恢復全綠。本輪 QA 未重跑 mutation（PR 內已附證據且 108 測試结構未變），改以下方兩／三帳號實測補交叉驗證。
- ✓ **兩／三帳號實測（admin API 建 A/B/C 三個真帳號＋PostgREST 真實 HTTP round trip，非 SQL 測試檔內的 `set_config` 模擬）**：
  1. A（家庭唯一 owner，B 為 member）自行 `DELETE /rest/v1/family_members` 退出 → `400 LS057`，`DETAIL={"family_id":"...","family_name":"LS206 QA 家"}`。
  2. B（非 owner）呼叫 `rpc/transfer_ownership(A)` → `400 LS058`。
  3. A 呼叫 `transfer_ownership(C)`（C 非家庭成員）→ `400 LS059`。
  4. A 呼叫 `transfer_ownership(A)`（自己）→ `400 LS060`。
  5. anon（無 Authorization token）呼叫 `transfer_ownership` → `401 42501 permission denied for function transfer_ownership`。
  6. A 呼叫 `transfer_ownership(B)` → `200`，回傳 `{"from_user_id":A,"from_role":"member","to_user_id":B,"to_role":"owner"}`；DB 實查 A→member、B→owner 正確。
  7. A（現為 member）自行 DELETE 退出 → `204` 成功（不再是唯一 owner，不觸發 LS057）。
  8. owner B 移除一般成員 C（`DELETE family_members`）→ `204` 成功。
  測試資料（A/B/C 三帳號＋家庭）已於驗證後清除。
- ✓ `delete_my_account` 情況 2（唯一成員）與 `finalize_account_deletion` 既有測試不回歸：`run.sh` 全綠含 91/92/101/102/105 等既有測試（併發段的 `delete_account_race_*`／三連線死鎖測試皆通過）。
- ✓ `api-contract-check.sh`：RPC 36 支、表 23 張，一致。
- ✓ `error-codes-check.sh`：34 個自訂碼三方一致（含 LS057-060）。
- ✓ `docs/API.md`：§3 `family_members` 已補 LS057 守門與轉移指引；§4 `transfer_ownership` 完整文件（含併發鎖說明）；§4 `delete_my_account()` 情況 1 已指向 `transfer_ownership`（不再與 §3 矛盾，M1 已修）；§5 錯誤碼表 LS057-060 皆有列；§9 機械對帳清單一致（gate 綠）。

### 已知申報偏離（PASS 不受影響）

PR 自陳的偏離：情境 4「唯一 owner 兼唯一成員直接對 `family_members` 一句 `DELETE` 自行退出」**未**如票文字面放寬為允許，仍被既有 `LS001` 擋下（正確離開路徑維持 `delete_my_account()`）。理由：trigger 層級無法區分呼叫路徑，若放寬會讓 LS-143 兩位共同 owner 併發刪帳號的安全網（`delete_account_race_*.sql`）失效、留下孤兒 `families` 列。merge-review R1 `c4be54a2` 已複核接受此偏離，QA 複核理由成立、不重開爭議。

### 未完成 / 風險

- 正式站 `db push`＋回讀：待併入 main 後由 orchestrator 執行（常設授權），本輪 QA 範圍為 `test` branch 本機驗證。

### 產出位置

- Migration：`supabase/migrations/20260905132350_family_ownership_guard.sql`
- 測試：`supabase/tests/108_family_ownership_guard.sql`；`supabase/tests/concurrency/transfer_race_*.sql`
- 文件：`docs/API.md` §3／§4／§5／§9
- QA 手動驗證：admin API＋PostgREST 真實請求（見上逐條），測試資料已清除，未落地成檔

### 2026-09-05T15:51:30.274Z · Chih-Lin Yeh · `49a3efbe`

**Orchestrator（09-05 23:50）**：delta QA `1bf93380` **PASS**（三帳號真實 HTTP 複核 LS057～060／42501、108＋transfer_race 綠、API.md 一致、偏離成立）。同批 test tip `da62930` 因 LS-167 單項退修 status failure → test→main 等 LS-167 修正併入後重 promote＋delta QA；之後 tag v0.16.0、正式站 `db push` `20260905132350` 回讀 → sweeper → Done → LS-192 解鎖。

### 2026-09-05T17:58:50.070Z · Chih-Lin Yeh · `6a0fc709`

**Orchestrator（09-06 01:58）**：test `127c38c` QA 全 PASS → promote test→main，tag **v0.16.0**；正式站 `supabase db push --linked` 已套用 `20260905132350_family_ownership_guard.sql`（dry-run 只列此一支）；回讀：`public.transfer_ownership` 存在、`family_members` ownership 守門 trigger 1 顆、`schema_migrations` 有該版本。dead-code-sweeper 已派 → Done → `cleanup-merged.sh --apply LS-206` → LS-192 解鎖。

### 2026-09-05T18:02:08.326Z · Chih-Lin Yeh · `ece4ad83`

## dead-code sweep — LS-206

**範圍**：`git diff 9a25b76...da62930 -- supabase docs LittleSprout LittleSproutTests`，限定票文所列檔案：`20260905132350_family_ownership_guard.sql`、`108_family_ownership_guard.sql`、`concurrency/transfer_race_{setup,s1,s2,verify}.sql`、`run.sh` race_case 掛載、`30_owner_invariant.sql`／`60_default_privileges.sql`、`docs/API.md` §3/§4/§5/§8/§9、`AppError.swift`/`AppErrorTests.swift`。diff stat 中另有 UploadQueue 相關 6 個檔案（非本票內容，同批 diff 範圍夾帶其他票變更）——依指示不列入本次巡檢。

### Findings（本 feature 引入）

**未發現需要修的死碼、鷹架殘留或設定殘留。** 逐項核對如下：

1. **Migration 本體**：`private.enforce_ownership_transfer_before_leave()` 與 `public.transfer_ownership()` 皆有掛載點（trigger／RPC grant），檔頭條件編號 1–5 與函式體五段判斷逐條比對一致（i1 的編號錯位已如 R2 所述修正為「條件 3」）。無註解掉的程式碼塊、無 debug print、無 TODO/FIXME 殘留（`grep -nE "TODO|FIXME|XXX|DEBUG"` 對 6 個新檔全數 0 命中）。

2. **`transfer_race_*.sql` 四檔與 `run.sh` 掛載**：`race_case` 呼叫的 4 個檔名與 `race_case()` 函式簽章（`$1=場景名 $2=setup $3=s1 $4=s2 $5=verify`）對應正確。`de000000-…0001`（家庭）／`de100000-…0001~0003`（使用者）三個 fixture id 全部出現在 `run.sh` 的 `cc_cleanup` 清單（`run.sh:551,574-576`），`grep -rn "de000000\|de100000" supabase/` 確認這組前綴只出現在這 4 支新檔＋`run.sh` cleanup，無殘留、無撞號。

3. **`docs/API.md` 全文「轉移」殘句掃描**：`grep -n "轉移\|兩次.*UPDATE" docs/API.md` 逐條看過全部命中——§3（`:250-254`）與 §4（`:1454-1457`，M1 已修）、§4 `transfer_ownership` 說明（`:1644-1662`）三處指示語意一致，均指向「轉移走 RPC、直接 UPDATE 只留單向升格」；`:1574` 是描述既有觸發條件的陳述句（非指示），`:1785/:1800` 是 tier 分類理由陳述。**全文未見殘句仍寫「轉移走兩次直接 UPDATE」**，R2 的修正是完整的。

4. **`30_owner_invariant.sql`／`60_default_privileges.sql`**：前者只改情境 1／6 的期望碼（`LS001`→`LS057`）＋批註，未動其餘情境；後者只加一行 `transfer_ownership(uuid, uuid)` 到 security definer 白名單。皆為單純登記，無殘留。

5. **`AppError.swift`／`AppErrorTests.swift`**：新增 4 個 case（`ownerMustTransferBeforeLeaving`=LS057、`notFamilyOwner`=LS058、`transferTargetNotMember`=LS059、`cannotTransferToSelf`=LS060），tier 分類與列舉測試三處都有列，彼此一致。`grep -rn` 確認**這 4 個 case 目前只在宣告處、tier switch、測試列舉出現，沒有任何呼叫端／catch 分流會用到它們**——這是票文明講的預期狀態（LS-192 尚未落地、本票不做 UI），依指示**只標記、不列為 finding**。

### 觀察（非 finding，供 orchestrator 參考；不影響本票 PASS）

`supabase/tests/108_family_ownership_guard.sql:7` 檔頭寫「本檔另建三個專屬 fixture 家庭（eb 前綴——00_fixtures.sql 與既有測試檔皆未用過這個前綴）」——這句話**不完全準確**：`supabase/tests/concurrency/approve_reject_race_setup.sql`（LS-33，`git show 9a25b76:...` 確認 PR 之前就存在）已經在用 `eb000000-0000-4000-8000-000000000001/2/3` 這三個字面值，只是用途不同（approve_reject_race 拿來當 `auth.users.id`，108 拿來當 `families.id`）。

- **功能面無影響**：`auth.users.id` 與 `families.id` 是兩張不同表各自獨立的主鍵空間，同一個 UUID 字面值同時存在於兩張表不構成衝突；也沒有任何 FK 把 `families.id` 指回 `auth.users.id`。`108_family_ownership_guard.sql` 自己的 `eb100000-…0001~0007`（使用者）與 `approve_reject_race` 的 `eb000000-…0001~0003`（使用者）也不重疊。已檢查 `run.sh` 的 `cc_cleanup` 未列 108 的任何 fixture id——這是預期的，因為 108 不是 concurrency 目錄下的多 session 測試，靠自己檔頭的 `delete ... where id in (...)` 自我清理（每次重跑都先清），不需要掛進 `cc_cleanup`。
- **性質**：純粹是檔頭註解裡「這個前綴沒人用過」的查證不夠廣（只對到 `00_fixtures.sql`，沒查到 `concurrency/` 下既有測試檔），屬於文件準確性小瑕疵，不是鷹架殘留、也不是功能性 bug，不影響任何測試的正確性或可重跑性。
- **建議處置**：不必開票，下次若有人順手改到這個檔頭可以一併訂正措辭（例如把「既有測試檔」改成「既有 `family_members`／`families` 相關測試檔」），或直接不理。

### 既有死碼觀察

本次巡檢範圍內未發現與本票無關的既有死碼（票文明列不動的既有函式如 `enforce_family_has_owner`、`delete_my_account`、`finalize_account_deletion` 均只被閱讀比對、未觸碰，且都仍有清楚呼叫點）。

### 建議處置

- Findings 為空，無需開 fix 票。
- 「觀察」項（eb 前綴檔頭措辭）留給 orchestrator 判斷是否值得記入 LS-96 待辦池；本 sweeper 認為嚴重度不足以單獨開票。
- Worktree `.claude/worktrees/LS-206`：`git status --short` 乾淨，無 dirty 變更。

### 搜尋方法與盲區聲明

- 方法：`git diff 9a25b76...da62930` 逐檔 stat／content 比對；`git show <sha>:<path>` 抽取目標版本全文閱讀；全 repo `grep -rn` 交叉比對 fixture id 前綴（`de000000`／`de100000`／`eb000000`／`eb100000`）、錯誤碼 case 名稱呼叫點、「轉移」全文殘句；`race_case()` 函式簽章與呼叫參數逐一核對。
- 盲區（誠實聲明）：純文字 grep 抓不到動態組字串呼叫 RPC 名稱（例如 client 端用字串插值組 `rpc("transfer_ownership")` 而非常數）——但本票無任何 Swift 呼叫端，此盲區目前不適用。也未執行 `supabase db reset`／實際跑 DB 測試（依指示本機 DB 不需碰），單純以靜態文字比對佐證前面 merge-review R1／R2／QA 已完成的實跑結果，未重覆驗證其正確性本身。

Comment 時間：2026-09-06 02:01 CST。

### 2026-09-05T18:02:56.101Z · Chih-Lin Yeh · `2ffedbe8`

**收尾儀式（09-06 02:0x）**
- **dead-code-sweeper** `ece4ad83`：無死碼；fixture id 皆在 cleanup 清單、API.md 無「兩次直接 UPDATE」殘句、`AppError` 四碼待 LS-192 接（預期）。觀察：108 檔頭「eb 前綴未用過」措辭不精確（`approve_reject_race_setup.sql` 用於 `auth.users.id`，不同主鍵空間無衝突）→ 記入池、下次動 108 順修。
- **Lesson learning review**：(1) 票文誤稱 RPC 已存在（LS-192）——開 ui 票前必對 API.md／migrations grep 一次 RPC 名，寫進開票檢查；(2) 申報偏離（唯一成員裸退出維持 LS001）是正確的，因為它守住 LS-143 的孤兒家庭不變量——票文的驗收條件本身錯了，實作者停下來報而不是照做，這是要的行為；(3) `FOR UPDATE` 必要性靠兩 session 真實併發實證（無鎖 → 2 owner），比推演有力，併發類票一律要求實跑。
- **清理**：`cleanup-merged.sh --apply LS-206` → worktree 已移除。
→ Done（v0.16.0；正式站已推）。LS-192 解鎖。

