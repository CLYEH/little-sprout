# LS-212 Task：LS-20 後端／上傳管線——影片暫存檔與孤兒 media 清理補完（上傳失敗／中斷／App 重啟後的本機暫存、`media` 列存在但 Storage 物件缺或反向）＋EXIF 直拍照片 `media.width/height` 與 `thumb_width/height` 方向一致（依 LS-96 `d8634a08`／`66770dd0` 原文）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:M, lane:backend |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M3 相簿與上傳 |
| 估點 | — |
| 建立 | 2026-09-06T01:10:38.443Z |
| 開始 | 2026-09-06T01:11:03.511Z |
| 完成 | 2026-09-06T13:27:08.602Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-212/taskls-20-後端上傳管線-影片暫存檔與孤兒-media-清理補完上傳失敗中斷app-重啟後的本機暫存media-列存在但 |
| 父票 | LS-20 Story：相簿與照片上傳（壓縮／縮圖／背景續傳） |
| 關係 | related→LS-190, related→LS-138, related→LS-57, related→LS-205, related→LS-209, related→LS-125, related→LS-166, related→LS-96, related→LS-167, related→LS-153, related→LS-129, related→LS-128, related←LS-220, related←LS-211, related←LS-193, related←LS-214, related←LS-213 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) P2 池項（backend lane 連續空 27 輪，依 §5-b 從池取票 scope+）：

* `d8634a08`（[LS-167](https://linear.app/little-sprout-app/issue/LS-167/taskls-20-ios-上傳佇列-sheet沒有成功正在進行已完成三群ls002-容量已滿列最前重試與背景續傳固定-detent依-ls)／[LS-129](https://linear.app/little-sprout-app/issue/LS-129/taskls-20-ios-上傳端縮圖mediauploadservice-同步產生並上傳-thumb寫入-thumb-path尺寸) review R1 m9＋R2 n6＋R4 補充）：影片上傳暫存檔與孤兒 `media` 清理不完整——上傳中斷／失敗／App 被殺後，本機 `tmp` 暫存與 Storage 半成品、`media` 列三者可能不一致，沒有一個確定的收斂路徑。
* `66770dd0`（[LS-128](https://linear.app/little-sprout-app/issue/LS-128/taskls-20-後端-media-縮圖欄與上傳端縮圖列表只載縮圖plan-7-egress-防線)／[LS-129](https://linear.app/little-sprout-app/issue/LS-129/taskls-20-ios-上傳端縮圖mediauploadservice-同步產生並上傳-thumb寫入-thumb-path尺寸) review）：EXIF 直拍（orientation 6／8）照片寫入 `media.width/height` 用的是儲存方向（橫），而 `thumb_width/height` 是顯示方向（直），列表用兩者算長寬比會不一致。

**動工前先做**：到 [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 讀這兩則 comment 全文（含 R4 補充），在 PR body「來源」段逐字引用關鍵句並列出你確認到的實際不一致情境（用本機容器重現：上傳中殺 App、Storage 上傳成功但 `media` insert 失敗、反向），再開始修。

## 範圍

1. **收斂路徑**：定義單一真相順序（Storage 物件 → `media` 列 → 本機暫存清除）；App 啟動與上傳佇列 sheet 開啟時跑一次「對帳」：本機暫存無對應佇列項 → 刪；`media` 列有 `path` 但 Storage 404 → 標記 `upload_failed`（或既有狀態欄）讓佇列可重試；Storage 有物件但無 `media` 列（孤兒）→ 由後端 `purge-storage`／既有排程處理（若已有 [LS-153](https://linear.app/little-sprout-app/issue/LS-153/task後端-軟刪與刪除帳號後-30-天自動永久清除排程pg-cron排程-edge-functiondb-列硬刪storage) 排程只確認涵蓋並補測試，不重做）。
2. **EXIF 方向**：上傳端寫 `media.width/height` 一律用顯示方向（與 thumb 一致）；既有列以一次性 migration 或 view 計算修正（若需 migration 走 BREAKING 流程與 API.md 同步；先評估影響列數）；列表長寬比計算單一來源。
3. 測試：DB 測試（`supabase/tests/`，通道寫進 handoff）覆蓋孤兒偵測；iOS 單元測試覆蓋對帳三情境與 EXIF 方向；每支 mutation 附實跑原文。

## 不做

上傳 UI 改版（[LS-167](https://linear.app/little-sprout-app/issue/LS-167/taskls-20-ios-上傳佇列-sheet沒有成功正在進行已完成三群ls002-容量已滿列最前重試與背景續傳固定-detent依-ls) 已 Done）；新的 Storage 配額邏輯；跨家庭批次工具。

## 驗收

- [ ] 三種不一致情境本機重現 → 修後收斂（handoff 附 admin API／SQL 查證前後）
- [ ] 直拍照片上傳後 `media.width/height`＝`thumb` 方向；列表無拉伸
- [ ] DB 測試＋單元測試綠、push gate 綠；merge-reviewer、QA PASS（QA 實跑上傳中殺 App）

## 留言（19）

### 2026-09-06T01:12:32.609Z · Chih-Lin Yeh · `53fb8e75`

**orchestrator 09-06 09:10 派工**：backend lane 連續空 27 輪，依 §5-b 從 LS-96 池取 P2 產品品質項（`d8634a08`／`66770dd0`）升票、scope+ 進 cycle 1。worktree `LS-212`／分支 `feature/LS-212-media-pipeline-hygiene` 自 development 913c91a。動工前先重現三種不一致情境與 EXIF 尺寸不一致並逐字引用池項原文；LS-153 排程若已涵蓋孤兒 Storage 只補測試；正式站只讀查影響列數、不寫入；migration 走 BREAKING 流程。硬規則：禁 fork／禁 Pencil、前景＋timeout、supabase-lock 同鏈 `--hold`。

### 2026-09-06T02:05:42.239Z · Chih-Lin Yeh · `f33821de`

**orchestrator 09-06 10:05**：handoff 收到，PR #334 head `9feac0f`（3 commit：`orientedPixelSize` EXIF 修正、`MediaDraftTempStorage` 啟動清空、孤兒 media 軟刪於 `removeSelected`／`discardDraft`、docs 收斂路徑；739 unit、3 mutation、容器重現三情境；池項 `996220e9` Storage 孤兒全表掃描另票）→ In Review、派 merge-review R1（重點：啟動清空與進行中上傳的 race、軟刪是否打到已被其他內容引用的 media 列、`removeSelected` 改 async 的 UI 並發、EXIF 樣本真帶 orientation、來源引句與 LS-153 排程主張核實）。實作發現 `UploadQueueSheetView`／`UploadQueueStore`（LS-167）零 production 接線——orchestrator 用 `git grep` 核實屬實；LS-167 票文範圍為「多選後的佇列 sheet」元件，接線屬 LS-166（相簿詳情「加入照片」多選）→ 已寫進 LS-166 票文為必做項，不在本票處理。正式站 EXIF 影響列數查詢（只讀）由 orchestrator 跑後貼回。

### 2026-09-06T02:07:32.153Z · Chih-Lin Yeh · `509ccc4f`

**orchestrator 09-06 10:08 正式站唯讀查證（未完成項 1）**：`mcp__supabase__execute_sql`（read-only）跑 PR body 備妥的查詢＋總數對照——`total_media=5`、`photos=3`、`photos_with_thumb_dims=0`、**`candidate_rows=0`**。裁決：既有列無受影響資料，**不需一次性 migration／backfill**；本票只需上傳端修正（已做）。reviewer 請將此列為已查證、非 informational。

### 2026-09-06T02:27:30.581Z · Chih-Lin Yeh · `568044ab`

**merge-reviewer 09-06 10:25 — merge-review R1｜REQUEST_CHANGES**

PR #334 `feature/LS-212-media-pipeline-hygiene` → `development`；審的 head `9feac0f1bf917e5d39ff7dcad40e4883a8d0273c`（開審與收尾兩次 `gh pr view 334 --json headRefOid` 皆同，未前進）。base `92c891a`，3 commits，19 檔（+637/−19）。CI 五項全綠（lint／rules／ci／ci-ipad／db）。

## 裁決

**REQUEST_CHANGES** — 1 major（M1）＋3 minor。EXIF 那一半（範圍 2）我認為已完備；孤兒 media 軟刪這一半（範圍 1）漏了票文與池項原文都明講的「畫面 dismiss」路徑，而且我實測確認那條路徑真的走得到。

---

## M1（major，已實測確認，非 PLAUSIBLE）· `LittleSprout/Features/Diary/DiaryEditorView.swift:185-213`

`discardDraft()` 只掛在 `cancelButton` 的 Button action 上。`DiaryEditorView` 是 `navigationDestination(isPresented:)` push 進來的，雖然 `.navigationBarBackButtonHidden(true)` ＋ `.toolbar(.hidden, for: .navigationBar)`，**互動式返回手勢仍然有效**——使用者一滑就把編輯器推掉，`cancelButton` 的 action 完全不會執行，`discardDraft()` 不跑。

**實測**（我在拋棄式 review worktree 加了一支暫時 UITest，跑完已還原、worktree 已移除）：走 `TapTargetGateHarness.sectionTabViewWithDiary`（＝真正的 `AuthenticatedRootView` → TimelineView → push DiaryEditorView 生產樹），tap「新增回憶」→ 確認「取消」鈕存在 → 水平拖曳 → 結果：

```
LS212_PROBE_RESULT after_middle_drag_editor_present=false
LS212_PROBE_RESULT after_edge_swipe_editor_present=false back_on_timeline=true
```

編輯器被推掉、且回到時間軸（app 沒 crash，是真的 pop）。模擬器 `LS-212-iPhone17Pro` iOS 26.0。

**具體失敗情境**：使用者選 1 支影片＋2 張照片 → 按「發佈日記」→ 照片／影片都上傳成功、`create_diary_entry` 成功、`attachMedia` 撞到斷線 → `publishState = .failure` → 使用者不按「取消」，直接**左緣（或畫面中間）往右滑**離開編輯器。結果：`uploadedMediaByDraftID` 裡那 3 筆 `media` 列沒有被軟刪，繼續計入 `families.storage_used_bytes`；依 LS-96 `d8634a08` R4 補充，若上一輪 `attachMedia` 其實已在伺服器端 commit、只是回應遺失，這幾張還會留在 `diary_media` 裡，發佈出去的日記帶著使用者已經放棄的照片——這正是本票要修的那件事，換一個手勢就整條漏掉。

池項原文與票文都寫「`removeSelected()`／**畫面 dismiss** 兩個路徑補清理」，這裡只做了 `removeSelected()` ＋ 一顆具名按鈕。

**建議修法**：把清理收斂到 view 生命週期而不是單一按鈕——`DiaryEditorView.body` 掛 `.onDisappear { Task { await store.discardDraft() } }`（本 repo 既有慣例：`InviteFamilyView.swift:94`／`JoinCodeView.swift:59`），`cancelButton` 那行拿掉；`discardDraft()` 既有的 `guard publishState != .success` 已經擋住「成功發佈後 dismiss」那條。**注意**：改 `.onDisappear` 前請確認它不會在「編輯器還活著但被切走」時誤觸——本畫面 `.toolbar(.hidden, for: .tabBar)`，編輯器開著時使用者切不了 tab，且它只 present `.sheet`（不 push），我判斷安全，但請自己再驗一次（加一支 UITest 釘住「滑動返回後 `softDeleteMedia` 有被呼叫」比註解可靠）。

---

## m1（minor）· `LittleSprout/Services/Diary/DiaryComposerStore.swift:176-180`

```swift
let orphanMediaIDs = removed.compactMap { uploadedMediaByDraftID.removeValue(forKey: $0.id) }
guard !orphanMediaIDs.isEmpty else { return }
try? await mediaUploadService.softDeleteMedia(mediaIDs: orphanMediaIDs)
```

memo 是在 `await` **之前**就 `removeValue` 掉的，而 `softDeleteMedia` 失敗被 `try?` 整個吞掉 → 這批 id 從此在記憶體裡消失，同一個 session 內沒有任何重試機會。

**具體失敗情境**：孤兒 media 之所以存在，最常見的成因就是 `publish()` 因為斷線在 `attachMedia` 失敗；使用者接著在**同樣斷線**的狀態下移除那幾張草稿 → `softDeleteMedia` 也必然失敗 → 什麼都沒清掉、也沒留下痕跡。也就是說最可能觸發本修法的情境，正好是本修法幾乎必定無效的情境。

**建議**：`removeValue` 移到軟刪成功之後（草稿已經從 `photos` 移除，留著 memo 不會讓 `uploadAllMedia()` 重傳——它只迭代 `photos`）；或把失敗的 id 收進一個 pending set，下一次 `cleanupRemovedDrafts`／`discardDraft` 一併重送。best-effort 的定位我不反對，但「連再試一次的資料都丟掉」是可以一行避免的。

## m2（minor）· `LittleSproutTests/Support/StubMediaUploadService.swift:52-54`

`setSoftDeleteMediaHandler(_:)` 全 repo 零呼叫端（`git grep setSoftDeleteMediaHandler` 只有定義本身）——本 PR 新引入的死碼。`softDeleteMediaCalls` 有用到、`SoftDeleteMediaHandler` typealias 只服務這支 setter。要嘛補一支「軟刪失敗時不阻斷 UI 流程」的測試把它用起來（正好可以順便釘住 m1 的行為），要嘛拿掉。

## m3（minor）· PR body「來源」段引文非逐字

票文明訂「在 PR body『來源』段**逐字**引用關鍵句」。PR body 寫的是「> R2 **版本補上了**「上傳成功後清本機暫存檔」」，LS-96 `d8634a08` 原文是「R2 **已修**「上傳成功後清本機暫存檔」」。`66770dd0` 那則的「（正確）」也把原文「（正確，符合 `docs/API.md` §6）」在括號內刪字、沒標省略。scratchpad `LS-212-findings.md` 裡的版本是逐字正確的，所以純粹是轉錄進 PR body 時的失誤——修 PR body 即可。

---

## 逐條查實（我自己跑的，不採信申報）

**1. 來源原文比對** — 讀 LS-96 `d8634a08`（含 R4 補充）／`66770dd0` 全文比對：`LS-212-findings.md` 逐字正確；PR body 有 m3 的兩處偏差。實作對 R4 的理解（merge-duplicates upsert 只 INSERT／UPDATE 不 DELETE ⇒ 已 attach 的那列也要主動處理）與原文一致。

**2. 三種不一致情境重放（我實跑，非採信）** — 在拋棄式 review worktree `--hold "LS-212 merge-review R1"`（**持有 1 分 10 秒**，`--release` 輸出），通道＝`docker exec supabase_db_little-sprout psql` ＋ `curl` 打 `127.0.0.1:54321`（service-role）：

- **反向孤兒（`media` 有列、Storage 無物件）**：`media` 3 列、`storage.objects` 1 列（只有一個 avatar），完全對不上——實作者說的「本機種子就是活生生的例子」屬實。
- **Storage 孤兒（有物件、無 `media` 列）**：PUT `fc000000-…/2026/09/deadbeef-…-999.jpg`（`HTTP 200`）→ `public.media` 0 列、`storage.objects` 1 列 → `select private.purge_expired();` 回 `{"storage_enqueued": 0, "deleted_counts": {"media": 0, …}}`，`purge_storage_queue` 前後皆 4 筆、`where object_path like '%deadbeef%'` 為 0，物件原封不動。**實作者的結論成立**。測試物件已 DELETE，收工核對 `media_rows=3 / storage_objs=1 / queue=4 / used=0`＝原狀。
- **原始碼交叉核實**：`supabase/migrations/20260903110908_purge_expired.sql:337-340` 的 `media_storage_queue_sync` 是 `after delete on public.media` 統計級 trigger，`supabase/functions/purge-storage/index.ts` 只 `from("purge_storage_queue")` 讀佇列、沒有任何 `storage.list()` 反查——結構上就不可能覆蓋這個方向。**不是只靠實測，原始碼也對得上**。
- **額外驗證（修法的承重不變量，實作者沒驗）**：insert 一列 `byte_size=12345` → `storage_used_bytes` 0→12345 → `update deleted_at=now()` → **12345→0**。軟刪確實立刻釋放額度，所以「軟刪孤兒」真的能解掉池項講的「仍計入 `families.storage_used_bytes`」。已清理。

**3. mutation 重放（LS-209，三支全部自己重跑）** — 在 review worktree 逐一套用申報的改動、跑對應套件、還原：

- M1 `orientedPixelSize` 拿掉 swap → `PickedItemLoaderTests.test_orientedPixelSize_exifOrientation6/8_returnsDisplayOrientationSwapped` 紅，原文 `XCTAssertEqual failed: ("Optional(LittleSprout.PixelSize(width: 40, height: 30))") is not equal to ("Optional(LittleSprout.PixelSize(width: 30, height: 40))") - EXIF 6 直拍照片的顯示尺寸應該是直向 30x40，不是 cgImage 儲存方向的橫向 40x30` ✅ 與申報一字不差
- M2 `discardDraft()` 拿掉 `guard publishState != .success` → `test_discardDraft_afterSuccessfulPublish_doesNotSoftDeleteAttachedMedia` 紅，`XCTAssertTrue failed - 發佈成功後的照片已經合法 attach，discardDraft() 不該把它們軟刪掉` ✅
- M3 `cleanupRemovedDrafts` 拿掉 `softDeleteMedia` 呼叫 → `test_removeSelected_draftAlreadyUploaded_softDeletesOrphanMedia`（`… - 移除已經上傳過的草稿應該主動軟刪對應 media 列，不論它有沒有被 attach`）＋`test_discardDraft_afterFailedPublish_softDeletesOrphanMediaForRemainingDrafts`（`… - 使用者放棄整個編輯器時，佇列裡已經上傳過的草稿也要主動軟刪，不只是 removeSelected 那條路徑`）兩支紅 ✅

三組全部是**真的斷言失敗**（rc=65，不是 build fail／crash）。M2 與 M3 我刻意分兩輪跑：M3 會讓 M2 的測試恆綠（都斷言「沒有呼叫」），合併跑會遮蔽 M2 的鑑別力。

**4. 739 unit tests 對數** — 我自己 `build-for-testing` ＋ `test-without-building -only-testing:LittleSproutTests`：`Executed 739 tests, with 0 failures`。與申報一致。

**5. EXIF 樣本真偽** — `PickedItemLoaderTests.makeJPEGData` 用 `CGImageDestinationAddImage` 帶 `kCGImagePropertyOrientation` 寫出真的 JPEG bytes，測試先 `XCTAssertEqual(image.imageOrientation, .right/.left)` 確認樣本真的解出對應方向才驗換算——**不是只改 metadata 字典**，是真 EXIF fixture。orientation 1／6／8＋`nil cgImage` 四例；orientation 3（`.down`）沒樣本（走與 `.up` 同分支，鑑別力上非必要，記為 informational i3）。影片路徑（`VideoTrimmer.pixelSize(ofFirstVideoTrackIn:)`）本來就疊了 `preferredTransform`，同類問題不存在；`makePhotoPendingThumbnail` 走 `kCGImageSourceCreateThumbnailWithTransform: true`，兩組欄位方向一致 ✅。`MediaContent.aspectRatio`（`TimelineModels.swift:177`）優先讀 `thumb_*`，`width/height` 只是 fallback，`docs/API.md` 新增的契約段與程式碼相符。

**6. LS-167 零接線** — `git grep UploadQueueStore|UploadQueueSheetView`：production 只出現在 `TapTargetGateHarness+UploadQueue.swift`／`#Preview`／`TapTargetGateScreenName`，其餘全在 `LittleSproutTests`／`LittleSproutUITests`。`UploadQueueStore.swift` 沒有任何 `UserDefaults`／`FileManager`／持久化——**不會有跨重啟恢復的佇列引用 `ls-media-drafts` 的檔案**，`purgeStaleFiles()` 在 `App.init()` 整批清目錄是安全的。本 PR 的 19 個檔一個都沒碰 LS-167 的檔（見下方 scope）。

**7. 暫存目錄 race** — `newFileURL` 的 `createDirectory(withIntermediateDirectories: true)` 冪等、可併發；`purgeStaleFiles()` 只在 `LittleSproutApp.init()` 第一行、早於任何畫面與 `TransferableVideoFile.importing`／`VideoTrimmer` 寫入；只刪 `tmp/ls-media-drafts` 子目錄，`git grep temporaryDirectory` 確認 production 沒有別的子系統寫這個目錄。`MediaDraftTempStorageTests` 的 setUp／tearDown 會清整個目錄、而 `DiaryComposerStoreDraftCleanupTests` 會往同一個目錄寫檔——push-gate 與 CI（`ci.yml:715`／`:881`）都帶 `-parallel-testing-enabled NO`，同行程序列執行、不互踩 ✅。

**8. RLS／效能** — `media_update` policy（`20260822120200_rls_policies.sql:209-217`）用 `family_id in (select private.owned_family_ids())` InitPlan 形式，**不是 per-row 子查詢**，符合 PLAN §5；`softDeleteMedia` 是單一 PATCH `id=in.(…)`，不是逐筆 N+1（測試 `SupabaseMediaSoftDeleteTests` 明確斷言 `query.contains("id=in.")`）。欄位級 grant `grant update (taken_at, deleted_at, width, height) on public.media`（`init_schema.sql:364`）確實包含 `deleted_at`，不需 RPC ✅。`media` 表**沒有** `deleted_by` 欄位（`20260903084231_delete_account.sql:44` 明文記載、`deletion_attribution.sql` 只給 diaries／albums／comments 加），LS-57 的 `deleted_by` 語意不適用本路徑 ✅。

**9. LS-96 池項 `996220e9`** — 已到 LS-96 查實存在（2026-09-06 01:51，backend／size:M），內容與本票結論一致、並給了具體修法建議 ✅。

**10. scope** — 19 檔全部可追溯到本票；`UploadQueueStore.swift`／`UploadQueueSheetView.swift`／`UploadQueueRowView.swift` 一行未動。三個 commit 分段合理（EXIF＋暫存目錄／軟刪／文件）。`docs/PLAN.md` 只改一句，落在票文要求定義收斂路徑的同一段「離線對帳」，且訂正的過時描述正是新句子的前提——我不列為 scope 違規，記為 informational。`MediaUploadService` 的 `client`／`now` 由 `private` 改 internal 是拆檔必要，且 `MediaUploadService+Duration.swift` 檔頭確實有同款先例 ✅。

---

## informational（不阻擋合併，請在 handoff 逐條處置）

- **i1** `DiaryComposerStore.removeSelected()` 沒有 `discardDraft()` 那條 `guard publishState != .success`。目前不可達（`submit()` 成功即 `dismiss()`，中間沒有使用者可插入的 turn），但 `publish()` 自己在 R2 n2 就補過同型的防禦性 guard，兩支對稱一點比較不容易在未來被改壞。
- **i2** `discardDraft()` 只處理 media，不處理已經建立成功的 `createdDiaryID`。使用者在 `attachMedia` 失敗後按取消／滑走，日記本體仍留在全家時間軸（LS-125 M2 既有行為），本 PR 讓它從「帶著照片」變成「只有文字」。哪個對是產品決策，但現在沒有任何地方寫下這個取捨——建議在 `docs/API.md` 新增的那段或票裡明記。
- **i3** EXIF orientation 3（`.down`）沒有測試樣本（票文列了 1／3／6／8）。走與 `.up` 同一分支，補與不補都可接受。
- **i4** `purgeStaleFiles()` 是主執行緒同步檔案系統刪除，位在 `App.init()` 啟動關鍵路徑上。APFS 的 unlink 是每檔 O(1)，實務量級可忽略，僅記為觀察項。
- **i5** 票文 §3 要求「DB 測試覆蓋孤兒偵測」，本 PR 沒新增、也沒跑 `run.sh`。我認為**結論可接受**——LS-153 既然不覆蓋這個方向就沒有東西可測，而修法真正倚賴的不變量「soft delete 立刻釋放 `storage_used_bytes`」**已有既有覆蓋**（`supabase/tests/40_triggers_feed_and_storage.sql:157-178`「soft delete 立刻釋放額度」），CI `db` job 在本 head 綠。但 handoff 只寫「無 migration 改動所以不跑」，沒指出這條既有覆蓋——建議補上，否則下一位讀的人無從判斷承重點有沒有人守。
- **i6** **runtime**：本機只有 iOS 26.0／26.5，釘住版是 `.ios-runtime` 26.2。我與實作者都用 iOS 26.0（`LS-212-iPhone17Pro FE811783-…`）；CI `ci`／`ci-ipad` 用 26.2 且已綠。本票不含版面／熱區改動，我判斷 runtime 差異無影響，但依 LS-205 在此註明。上面 M1 的 swipe-back 探測也是 26.0 的結果——手勢可用性不是 26.2 特有行為，但若要在 26.2 再確認一次成本很低。

## 未涵蓋範圍（誠實聲明）

- 沒有實跑「登入 → 真的挑一支影片 → 上傳中殺 App」的完整 E2E（worktree 用 placeholder `Secrets.xcconfig`，走不到登入後）。這條由實作者的模擬器層級驗證＋43 支單元測試覆蓋，我只複驗了單元測試與 DB／Storage 層。
- 正式站 `media.width/height` 受影響列數（PR body 未完成項 1）我沒有查——同意「不該由 reviewer 逕自對正式站發查詢」，留給 orchestrator，PR body 已備妥唯讀 SQL。
- LS-96 `d8634a08` 其餘 P3／P2 池項確實不在本票範圍，同意不修。

## 環境收尾

- supabase lock：`--hold "LS-212 merge-review R1"`，**持有 1 分 10 秒**，已 `--release`；容器狀態已核對回原狀。
- 模擬器 `FE811783-6442-4B97-86D8-357D4901C664` 已 shutdown；全程用 `simulator-lock.sh --udid` 包住（字級／外觀由它自動設定與復原），**我沒有手動跑過 `simctl ui`**。
- review worktree `.claude/worktrees/LS-212-review`（含暫時 probe UITest 與 `Config/Secrets.xcconfig`）已 `git worktree remove --force` 移除；實作者的 `.claude/worktrees/LS-212` 一個字都沒動。
- 未碰 Pen。

**下一輪**：修 M1（＋建議一併收 m1／m2／m3）後 push，對新 head 重派 R2 並重貼 `merge-review` status。

### 2026-09-06T02:28:59.688Z · Chih-Lin Yeh · `d8b9d790`

**orchestrator 09-06 10:28**：merge-review R1（568044ab）REQUEST_CHANGES 收到——M1 `discardDraft()` 只掛取消鈕、互動式返回滑走時清理不跑（reviewer 生產樹實測）；m1 `removeValue` 在 `await` 前且 `try?` 吞錯；m2 stub handler 零呼叫；m3 引句非逐字。三支 mutation 重放一致、Storage 孤兒情境容器重放屬實、額度不變量已由既有 DB 測試覆蓋。已派原 agent R2（改掛 `.onDisappear`＋滑走 UITest／mutation、失敗保留不丟資料、stub 用起來或刪、引句逐字；正式站影響列數 0 寫入 PR body 引 `509ccc4f`）。R2 push 後派 merge-review R2。

### 2026-09-06T03:13:51.451Z · Chih-Lin Yeh · `e300e335`

**orchestrator 09-06 11:13**：R2 handoff 收到，PR #334 head `d71548c`（M1 cleanup 改掛 `.onDisappear`＋`DiaryEditorViewLifecycleTests` 掛真 view 模擬消失；m1 `pendingOrphanMediaIDs` 失敗保留跨呼叫重試；m2 stub 已被測試使用；m3 引句逐字；743 unit）。已派 merge-review R2（同 reviewer）：生產樹邊緣滑走 probe 重放、`.onDisappear` 在 push-forward／publish 成功時不誤觸發、Lifecycle 測試 flake 風險連跑、pending 重啟遺失是否可接受、引句同義詞替換與 pr-body-check 字面 gate 是否入池；status 貼 d71548c。

### 2026-09-06T03:46:12.221Z · Chih-Lin Yeh · `695170ed`

**merge-reviewer 09-06 11:43 — merge-review R2｜REQUEST_CHANGES**

審的 head `d71548c55104b2afde19e74d604bf339be0fb333`（開審與收尾兩次 `gh pr view 334 --json headRefOid` 皆同，未前進）。R2 delta `9feac0f..d71548c`＝1 commit、5 檔（+221/−11）。CI 五項全綠（lint／rules／ci／ci-ipad／db）。

---

# ⚠️ 首先：我 R1 的 M1 是誤判，在此撤回

**R1 `568044ab` 的 M1（major）不成立。我搞錯了，向實作者致歉——R2 這一輪的修改是被我的錯誤結論推動的。**

## 我 R1 錯在哪

R1 我寫「實測確認滑走可以推掉編輯器」，證據是 probe 印出的 `editor_still_present=false`。**我漏看了同一份 log 裡前面那條已經失敗的前置斷言**：

```
/…/UploadQueueSheetUITests.swift:191: error: … XCTAssertTrue failed - 日記編輯器沒有推上來
```

`XCTAssertTrue` 不中斷測試，所以我的 probe 在「編輯器根本沒渲染」的情況下繼續往下跑，量到的是**另一個東西**。

根因：`TapTargetGateHarness.sectionTabViewWithDiaryHost` 傳的是 `familyStore: .preview()`（沒有種家庭），而 `PreviewFamilyAPIClient.fetchMyFamily()` 回 `nil`、`AuthenticatedRootView` 也沒有任何 refresh——所以 `TimelineView` 的

```swift
.navigationDestination(isPresented: $showsDiaryEditor) {
    if let familyID = familyStore.myFamily?.id { DiaryEditorView(...) }
}
```

`if let` 永遠不成立，push 出去的是一個**空目的地**。空目的地沒有 `DiaryEditorView` 內部的 `.navigationBarBackButtonHidden(true)`／`.toolbar(.hidden, for: .navigationBar)`，因此帶著**系統預設的 NavigationBar 與 BackButton**（我本輪 dump 出來的 a11y 樹確認：`NavigationBar … Button, identifier: 'BackButton', label: '時間軸'`）。我 R1 量到的「滑走推得掉」，量的是**這個預設設定的空白頁**，不是編輯器。

## 本輪重測（把 harness 種上家庭之後）

在拋棄式 review worktree 把 `sectionTabViewWithDiaryHost` 改成 `.preview(withFamily:)`（同 `TapTargetGateHarness+Settings.swift:30` 既有作法），編輯器這才真的渲染（`after_push=取消0`）。對**真正的 `DiaryEditorView`** 跑四種手勢：

```
LS212_P5 v1_swipeRight            editor_present=true
LS212_P5 v2_slowEdgeDrag(dx=0.0，slow，thenHold 0.2s)  editor_present=true
LS212_P5 v3_edgeDrag(dx=0.01)     editor_present=true
LS212_P4 middle_drag              editor_present=true（取消0）
LS212_P5 cancel_tap               editor_present=false  back_on_timeline=true   ← 正向對照
```

**四種滑動手勢一種都推不掉；點「取消」推得掉。** 再加上前面那次「同一支手勢、同一個 app、同一個 harness，對預設 nav bar 的空白頁**推得掉**」——正反對照齊全。

結論：`.navigationBarBackButtonHidden(true)` ＋ `.toolbar(.hidden, for: .navigationBar)` 在這支畫面上**確實關掉了互動式返回手勢**（與 UIKit「隱藏 navigation bar 會連帶停用 `interactivePopGestureRecognizer`」的既有行為一致）。R1 之前 `cancelButton` 就是唯一的離開路徑，**沒有漏接**。模擬器 `LS-212-iPhone17Pro` iOS 26.0。

---

# 裁決

**REQUEST_CHANGES** — 1 major（M1-R，我造成的，修法只動註解）。R2 的程式行為本身我測得過的部分**全部通過**，m1／m2／m3 三條處置我認為都做對了。

## M1-R（major）· `LittleSprout/Features/Diary/DiaryEditorView.swift:88-99`、`:208-215`；`LittleSproutTests/DiaryEditorViewLifecycleTests.swift:7-12`

R2 把清理從 `cancelButton` 搬到 `.onDisappear`，**唯一的理由是我 R1 給的那個錯誤結論**，而且這個錯誤結論已經逐字寫進 production 註解：

> `.onDisappear` 文件註解：「互動式返回手勢／邊緣滑走…**依然有效**、不會經過 `cancelButton` 的 action」
> `cancelButton` 註解：「`.navigationBarBackButtonHidden(true)` 只隱藏了系統返回鈕，互動式返回手勢（邊緣滑走）**仍然有效**——只掛在這顆按鈕上會讓滑走離開的路徑完全漏接清理」
> `DiaryEditorViewLifecycleTests` 檔頭：同一句話再寫一次

這三段陳述**與實測相反**。這個 repo 的註解是決策檔案（滿篇 `merge-review R1 M3`／`R2 n1` 的沿革），把一個量不出來的機制寫成既成事實，未來會誤導人——例如有人為了「簡化」拿掉 `.toolbar(.hidden, for: .navigationBar)`，或照著這段去寫一支測不到東西的手勢測試。而在錯誤前提被拿掉之後，「把清理從明確的使用者動作搬進 view 生命週期鉤子」在 scope 上就變成一個沒有已證實缺陷支撐的行為改動（手術式修改原則），需要重新給理由或撤回——這個決定該由實作者／orchestrator 拿著正確資訊做，不該由我的錯誤替他們做掉。

**我的建議是「保留 `.onDisappear`、只改註解」**，理由（全部本輪實測，見下方查實段）：
1. 它在所有現存的離開路徑上行為與 R1 相同，且我量不到任何誤觸發；
2. 它是 defence in depth——手勢**現在**測不到，是因為那兩個 modifier 剛好把它關掉了；哪天有人動了那兩行，掛在按鈕上的清理會靜默失效，掛在 `.onDisappear` 上不會；
3. 它有一支真的能殺 mutation 的測試守著。

要做的事：把上述三段註解改寫成「本輪實測：這支畫面因為 `.toolbar(.hidden, for: .navigationBar)` 目前**沒有**互動式返回手勢；清理掛在 `.onDisappear` 是為了不依賴『唯一入口是那顆按鈕』這個會隨 modifier 變動的前提」，並據此重新交代 `#if DEBUG init(store:childrenStore:)` 的存在理由（可測性，而不是「滑走會漏接」）。若決定改回掛 `cancelButton`，那就連同 `.onDisappear`、`#if DEBUG` init、`DiaryEditorViewLifecycleTests` 一起撤，我也接受——只要理由與事實一致。

---

# 逐條查實（全部我自己跑，不採信申報）

## 1. M1 的接線與誤觸發風險（本輪重點）

在 review worktree 給 `DiaryComposerStore` 加一顆探測計數（`discardDraft()` 進入即 +1），把數字塞進「取消」鈕的 label，再對**真正的編輯器**逐一操作：

| 操作 | 計數 | 判讀 |
|---|---|---|
| push 進編輯器 | `取消0` | 基準 |
| 開＋關「記錄日期」`.sheet`（`date_sheet_opened=true`） | `取消0` | **不誤觸發** ✅ |
| 開＋關「寶貝歸屬」`.sheet`（`child_sheet_opened=true`） | `取消0` | **不誤觸發** ✅ |
| 畫面中央水平拖曳 | `取消0`（畫面還在） | 不誤觸發、也推不掉 ✅ |
| 開＋關 `.photosPicker` | `取消0` | **不誤觸發** ✅ |

`.photosPicker` 那一條我特別確認它真的呈現了（不是沒開所以沒事）：picker 開著時 `addCell_hittable=false`、`取消鈕 hittable=false`、`app_windows=2`。

→ 我 R1 建議 `.onDisappear` 時提醒過「請自己驗它不會在畫面還活著時誤觸發」——**這一點實作者做對了，而且我替他量過了，三種 modal 呈現都乾淨**。這很重要：`discardDraft()` → `cleanupRemovedDrafts(photos)` 會刪掉**仍在佇列裡**的影片草稿本機暫存檔，一旦誤觸發就是真的資料損壞（之後發佈會讀不到檔）。目前沒有這個洞。

**唯一沒測到的 modal**：`AuthenticatedRootView` 那層的 `.fullScreenCover(isPresented: familyStore.showsChildOnboarding)`。`fullScreenCover` 是唯一已知**會**讓被蓋住的畫面收到 `onDisappear` 的呈現方式，而它是編輯器的祖先。現行程式裡 `showsChildOnboarding` 只在「剛建立家庭」之後才會是 true，不可能與開著的編輯器同時發生，所以我判定目前無風險——但這是分析、不是實測，列 informational i7。

## 2. M1 mutation 重放（LS-209）

拿掉 `DiaryEditorView.body` 的 `.onDisappear` 整段 → `DiaryEditorViewLifecycleTests.test_onDisappear_afterFailedPublish_softDeletesOrphanMedia` 轉紅（rc=65，**真斷言失敗**、非 crash／build fail）：

```
…/DiaryEditorViewLifecycleTests.swift:73: error: … XCTAssertEqual failed: ("[]") is not equal to
("[LittleSproutTests.StubMediaUploadService.SoftDeleteMediaCall(mediaIDs: [8E737FB0-A8D2-462E-B63B-0C36F5D5C339])]")
 - 畫面消失（取消鈕、成功發佈後 dismiss、或互動式滑走）都該經過 .onDisappear 觸發 discardDraft() 軟刪孤兒 media
```

與 handoff 申報一字不差（行號同為 :73）。已還原。這同時也證明 `.onDisappear` 在 hosting controller 移除時**真的會觸發**，測試不是恆綠的。

## 3. `RunLoop` vs `Task.sleep` 的 flake 風險（你點名要判斷）

`DiaryEditorViewLifecycleTests` **連跑 3 次全綠**，耗時 1.740／1.751／1.729 秒，抖動 <25ms：

```
run1 rc=0  Executed 2 tests, with 0 failures … in 1.740 seconds
run2 rc=0  Executed 2 tests, with 0 failures … in 1.751 seconds
run3 rc=0  Executed 2 tests, with 0 failures … in 1.729 seconds
```

我判斷 **flake 風險低**：兩支測試合計 1.74 秒裡，1.0 秒是第二支刻意跑滿的 `waitUntil(timeout: 1.0) { false }`、0.6 秒是兩次 `RunLoop.main.run(until: +0.3)`，剩下**不到 0.15 秒**才是第一支真正等 Task 的時間，而它的上限是 3.0 秒——超過 20 倍餘裕。實作者對機制的判讀也正確：`Task.sleep` 讓出 MainActor 才是 Swift Concurrency 認得的排程點，這個 bundle（`bundle.unit-test`、無 `TEST_HOST`）沒有 `UIApplicationMain`，`RunLoop.main.run` 推不動 MainActor 上排隊的 Job。兩處各自用對機制（等轉場用 RunLoop、等 Task 用 sleep），檔頭也把踩坑寫清楚了——這段記錄本身有價值，建議保留。

唯一保留意見：第二支測試固定燒 1.0 秒 wall clock 換一個「不會發生」的斷言，是全 743 支裡最貴的一支。可接受，但若日後這類測試變多，值得換成「注入一個可觀察的 hook」而不是靠時間視窗。（informational i8）

## 4. m1（`pendingOrphanMediaIDs` 失敗保留）

兩支新測試我都跑過，並自己下 mutation 驗鑑別力——把 `catch { pendingOrphanMediaIDs = candidateMediaIDs }` 改成 `catch { pendingOrphanMediaIDs.removeAll() }`（＝回到 R1 的丟棄語意）→ **兩支都轉紅**：

```
…DraftCleanupTests.swift:166: XCTAssertEqual failed: ("[E60203F5-…]") is not equal to
 ("[D4AB9957-…, E60203F5-…]") - 第二次呼叫應該把第一次失敗、還留著的 id 併進這次的批次一起重送，不能只送新的那一張
…DraftCleanupTests.swift:192: XCTAssertEqual failed: ("1") is not equal to ("2")
 - discardDraft() 也該把上一次失敗、還留著的 id 一併重送
```

實作正確：`removeValue` 仍在 `await` 之前（必要，否則重試會重複累積），但失敗時整批 `candidateMediaIDs` 存回 pending，下一次 `removeSelected()`／`discardDraft()` 併批重送。`Set` 天然去重，重試不會送出重複 id。**m1 已解決。**

**跨重啟遺失（你點名要判斷）**：`pendingOrphanMediaIDs` 是 in-memory、掛在畫面等級的 `DiaryComposerStore` 上，所以
- App 被殺／重啟 → pending 全部遺失；
- **更關鍵**：`discardDraft()` 是這個 store 生命週期的**最後一次**呼叫，它失敗時存回的 pending 隨即跟著 store 一起消失——也就是說「離線 → 直接按取消離開」這條最常見的終局路徑，重試機制**幫不上忙**。

我判斷**可接受**：要救這條得把待清 id 持久化（`UserDefaults`／本機 DB）＋在下次啟動對帳，那是 LS-167「上傳引擎本身」等級的工作，明確超出本票；而漏掉的後果是「孤兒 media 列繼續佔額度」，正是 LS-96 `996220e9` 已經記在池裡、要由後端排程票整批收掉的同一類問題。**但這個限制必須在 handoff／`docs/API.md` §3 那段收斂路徑裡明講**——目前 R2 handoff 完全沒提，只寫了「同一個 store 存活期間內失敗會持續累積」。要求補上（見 informational i9，這條我算 must-do 但不阻擋合併）。

## 5. m2（stub handler 死碼）

`setSoftDeleteMediaHandler` 現在被上面兩支新測試各用兩次（先設失敗、再設成功）——不再是死碼。**已解決** ✅

## 6. m3（引文逐字）— 你點名要確認換的是哪個詞

我把 PR body 的引文與 LS-96 原文做**逐字元 diff**：

- `d8634a08`：唯一差異 = 「R2 已**修**」→「R2 已**經處理**」，相似度 **0.99401**，其餘一個位元組都沒動。
- `66770dd0`：相似度 **1.00000**——**與原文完全逐字相同**，R1 我指出的括號刪字已完整復原。 ✅

**換的詞不影響意義**（「已修」＝「已經處理」，都是陳述 R2 那一輪修掉了那件事），且 PR body 就地標註了替換與理由。

**這個字面規則該不該入池——我的判斷：該，但只值 size:XS。** `scripts/gates/pr-body-check.sh` 的檔頭第 33-34 行**自己就寫明**了這個誤中類別與處置：

> 刻意全 body 掃、不辨「處置語境」（判語境太脆）：引用而非申報的行（「LS-138 已修」…）會誤中，**由 agent 改寫措辭避開字樣或補 id**

所以實作者走的正是 gate 自己指定的逃生門，**不是違規**。真正的問題是本票票文另外要求「逐字引用關鍵句」——兩條規則在「被引用的原文剛好含『已修』」時**無法同時滿足**，只能二選一。建議的最小修法：讓 (b) 這條掃描跳過 Markdown 引言行（行首 `>`）——引言依定義不是本 PR 的申報，而 (a) 的 LS-96 comment id 規則不受影響。取捨：有人可以把申報藏進引言來規避，但那是刻意繞過、不是這條 gate 的防護目標（它防的是「宣稱已修但沒 commit」的疏漏）。**建議記入 LS-96（harness，P3／size:XS）**，我這輪不另開票。

## 7. 全專案回歸與 CI

- 我自己 `build-for-testing` ＋ `test-without-building -only-testing:LittleSproutTests`：**`Executed 743 tests, with 0 failures`**，4.388 秒——與申報的 743／4.4 秒一致 ✅
- `bash scripts/gates/pr-body-check.sh <body> --branch feature/LS-212-media-pipeline-hygiene --verify` → **exit 0**（三條「已修」行的 SHA `d71548c` 皆在 PR commits 內）✅
- CI 五項全綠（`ci` 21m37s／`ci-ipad` 6m34s／`db` 4m5s／`lint`／`rules`）✅

## 8. R2 delta 的 scope

5 檔全部可追溯：`DiaryEditorView.swift`（M1）、`DiaryComposerStore.swift`（m1）、`DiaryComposerStoreDraftCleanupTests.swift`（m1 測試）、`DiaryEditorViewLifecycleTests.swift`（M1 測試，新）、`project.pbxproj`（登記新檔）。沒有順手改動、沒碰 LS-167 檔、沒碰 migration／docs。`#if DEBUG init(store:childrenStore:)` 只在 DEBUG 編入，Release 不存在——手法本身符合 repo 既有慣例（`.preview()` 系列同樣 `#if DEBUG`），我不反對它存在，只要求理由改成真的（見 M1-R）。

## 9. R1 其餘結論的現況

R1 查實的 EXIF 修正、暫存目錄收斂、Storage 孤兒重放（含我自己 PUT 物件跑 `purge_expired()` 確認 `storage_enqueued: 0`）、軟刪釋放 `storage_used_bytes` 12345→0、RLS InitPlan 非 per-row、`media` 無 `deleted_by`、LS-167 零接線——**R2 delta 一行都沒動到這些，結論全部沿用**，本輪未重跑。

---

# informational（不阻擋合併）

- **i7** `AuthenticatedRootView` 的 `.fullScreenCover`（`showsChildOnboarding`）是唯一已知會讓祖先鏈上的 `.onDisappear` 觸發的呈現方式，且它蓋在編輯器之上。現行流程下不可能與開著的編輯器同時發生，我判定無風險——但這是分析不是實測，值得在 `.onDisappear` 註解裡順手記一句「已知唯一需要注意的祖先 modal」。
- **i8** `test_onDisappear_afterSuccessfulPublish_doesNotSoftDeleteAttachedMedia` 固定燒 1.0 秒 wall clock 驗一個「不會發生」。目前佔全 743 支的 ~23% 耗時。可接受，但這類「靠時間視窗證明否定命題」的測試不宜再增加。
- **i9（建議列 must-do）** `pendingOrphanMediaIDs` 的 in-memory 限制（App 重啟遺失、`discardDraft()` 這條終局路徑失敗即無救）沒有寫在任何地方。請補進 handoff「風險」段與 `docs/API.md` §3「上傳中斷時的三種不一致收斂」②那一條——否則下一位會以為重試機制涵蓋了離線取消。
- **i10** m3 的 gate 衝突（`pr-body-check.sh` 全 body 掃「已修」 vs 票文要求逐字引用）建議記入 LS-96，harness／P3／size:XS，修法＝跳過行首 `>` 的引言行。
- **i11** 我 R1 的探測方法有缺陷（前置斷言用 `XCTAssertTrue` 而非 `XCTUnwrap`／`guard`，失敗後仍繼續量測），這是我這次誤判的直接機制。**已被我自己踩到、值得入池**：merge-reviewer 的一次性 probe UITest，前置條件應該用會中斷測試的斷言（`try XCTUnwrap`／`XCTAssertTrue(..., ...); guard ... else { return }`），否則「前置沒成立」會偽裝成「觀測結果」。建議與 i10 一起記入 LS-96。
- **i12（harness 缺口，本輪發現）** `TapTargetGateHarness.sectionTabViewWithDiaryHost` 用 `familyStore: .preview()`（無家庭），導致從時間軸 push `DiaryEditorView` 的那條**生產路徑在 harness 裡永遠是一個空白目的地**。tap-target gate 掃的是 `.diaryEditor` 這個獨立 host（`NavigationStack { DiaryEditorView(...) }`）所以量得到，沒有被這個缺口影響；但任何想從 `sectionTabViewWithDiary` 走「時間軸→編輯器」整條路徑的 UITest 都會靜默量到空白頁（我就是這樣被騙的）。修法只需比照 `TapTargetGateHarness+Settings.swift:30` 改成 `.preview(withFamily:)`。**這條我認為值得另票或入池**（harness，P2／size:XS）——它讓一整條生產導覽路徑在測試環境裡不可達，而且失敗方式是靜默的。
- R1 的 i1／i2／i3／i4／i6 處置（不修＋理由）我同意，R2 handoff 皆已逐條交代；i5 已補進 PR body「已驗證」段並正確引用 `supabase/tests/40_triggers_feed_and_storage.sql:157-178` ✅

---

# 給 QA 的重點（等 M1-R 收掉、下一輪 APPROVE 後）

1. **上傳中殺 App**：日記編輯器選一支影片、按發佈，上傳途中直接殺掉 App → 重開 → 確認 `tmp/ls-media-drafts/` 已清空（`xcrun simctl get_app_container … data` 下看），且時間軸沒有破圖。
2. **離開編輯器的三條路徑各走一次**：①「取消」鈕、②發佈成功後自動關閉、③**嘗試邊緣滑走**——請 QA 明確記錄「滑走是否推得掉」。我實測是推不掉（四種手勢皆然），若 QA 在真機／26.2 上發現推得掉，那就是我 R2 的結論在該環境不成立，要立刻回報（這正是 M1-R 要求把註解寫成「本輪實測」而不是斷言的原因）。
3. **失敗→移除→重試**：斷網發佈（會在 `attachMedia` 失敗）→ 移除其中一張 → 恢復連線 → 再移除一張 → 用 SQL 確認**兩張** media 的 `deleted_at` 都有值、`families.storage_used_bytes` 回到只算剩下那些（驗 `pendingOrphanMediaIDs` 重送）。
4. **開關 sheet 不誤刪**：佇列裡放一支影片 → 開關「記錄日期」／「寶貝歸屬」／照片挑選器各一次 → 回來按發佈，確認影片仍上傳得成功（驗 `.onDisappear` 沒有誤刪暫存檔）。我已用計數器量過不誤觸發，QA 這條是端到端複驗。
5. 走 **iOS 26.2**（CI 釘住版）——我與實作者本機都只有 26.0／26.5。

---

# 環境收尾

- supabase lock：**未持有**（R2 delta 純 Swift／`xcodebuild`，未碰本機容器；R1 那輪持有 1 分 10 秒已釋放）。與實作者申報一致。
- 模擬器 `FE811783-6442-4B97-86D8-357D4901C664` 已 shutdown；全程用 `simulator-lock.sh --dir … --udid …` 包住（字級／外觀由它自動設定與復原），**我沒有手動跑過 `simctl ui`**。
- review worktree `.claude/worktrees/LS-212-review`（含探測用 UITest、harness 種家庭改動、`DiaryComposerStore` 計數器、`Config/Secrets.xcconfig`）已 `git worktree remove --force` 移除，`git worktree list` 確認不在；實作者的 `.claude/worktrees/LS-212` 一個字都沒動。
- 未碰 Pen。長命令全部前景＋timeout。

**下一輪**：收掉 M1-R（只動註解＋重新交代 `#if DEBUG` init 理由）＋補 i9 的 in-memory 限制記載後 push，對新 head 重派 R3 並重貼 `merge-review` status。i10／i11／i12 三條請 orchestrator 決定入池或另票。

### 2026-09-06T03:47:59.658Z · Chih-Lin Yeh · `2dacdcd4`

**orchestrator 09-06 11:47**：merge-review R2（695170ed）——reviewer 撤回 R1 M1（實測四種滑動手勢皆推不掉 `DiaryEditorView`，R1 量到的是 harness 無家庭時的空目的地），R2 程式行為全數通過（無誤觸發、mutation 一致、flake 低、引句逐字）；剩 M1-R（三處註解與實測相反）與 i9（`pendingOrphanMediaIDs` in-memory 限制須明列）。裁決：保留 `.onDisappear`（防禦性），註解改實測事實；i9 明列於 doc comment 與 PR body 風險；i12 harness 缺口記 LS-96 comment 上一則（同族 i11）。已派原 agent R3（小輪，只動註解與文字）；R3 push 後派 merge-review R3。

### 2026-09-06T04:07:03.224Z · Chih-Lin Yeh · `b37eeb8e`

**orchestrator 09-06 12:06**：R3 handoff 收到，PR #334 head `8f42fe0`（註解／文件-only：M1-R 三處改為實測事實、i9 in-memory 與 `discardDraft()` 自身失敗即遺失寫進 doc comment 與 API.md §3、i7 併入、i12 引池項 `854e5905`）。已派 merge-review R3（同 reviewer）：查實 comment-only、無「手勢仍有效」殘句、API.md 一致；status 貼 8f42fe0。APPROVE 後併入 development → promote → QA（上傳中殺 App 啟動清空、離開編輯器孤兒軟刪＋額度回落、EXIF 同向）。

### 2026-09-06T04:12:05.767Z · Chih-Lin Yeh · `8d1e57bc`

**merge-reviewer 09-06 12:09 — merge-review R3｜REQUEST_CHANGES**

審的 head `8f42fe08c62887e234fb62faa5a0609ac9b84801`（開審與收尾兩次 `gh pr view 334 --json headRefOid` 皆同，未前進）。R3 delta `d71548c..8f42fe0`＝1 commit、4 檔（+67/−30）。本輪不需模擬器與容器，未持有任何 lock。

## 裁決

**REQUEST_CHANGES** — 1 major（M2-R3）＋2 minor。**你被要求做的三件事（M1-R 三處註解訂正、i9 記載、i7 折入）本身都做到了、而且做得完整**；但 i9 那段新寫的「後備」敘述引進了一個**新的錯誤事實**，方向剛好跟 i9 的目的相反——i9 要的是「別假裝有東西接住這條路徑」，現在的寫法是「假裝有另一張票會接住」。

---

## M2-R3（major）· `docs/API.md` §3（②那段結尾）、`LittleSprout/Services/Diary/DiaryComposerStore.swift:78-88`、PR body 未完成項 2 與風險段

R3 為 i9 新增的限制記載，結尾一律接上同一句「後備」：

> `docs/API.md` §3：「…重試機制其實幫不上忙，**會退化成③的孤兒 Storage 物件（下一段）**」
> `DiaryComposerStore.pendingOrphanMediaIDs` doc：「…與 LS-96 池項 `996220e9`（後端排程反向掃描 `storage.objects` 找孤兒物件）是同一類問題，**理論上未來那張後端票落地後會被一併掃到、清掉**」
> PR body 未完成項 2：「i9 提到的『pending 重試機制救不到的離線放棄路徑』最終也會退化成這個方向，**由同一張未來的票一併掃到**」
> PR body 風險段：同一句再寫一次

**這四處都是錯的，而且是定義上的錯，不是判斷分歧。**

走一次那條路徑就看得出來：使用者按發佈 → `uploadAllMedia()` 對每張都 **Storage PUT 成功、`insertMediaRow` 也成功**（`media` 列存在、`storage_used_bytes` 已經加上去）→ 之後才在 `attachMedia` 斷線失敗 → 使用者離線狀態下放棄編輯器 → `softDeleteMedia` 也失敗 → pending 隨 store 消失。

殘留物是一列 **`deleted_at IS NULL` 的活 `media` 列**，它有對應的 Storage 物件。而

- ③ 在同一段裡的定義是「Storage 有物件、但**從未成功 `insert` 對應 `media` 列**的孤兒」——這裡的列存在，**不是 ③**；
- `996220e9` 描述的機制是「Storage Admin API 分頁 `list()` 家庭前綴 ∩ `media.storage_path` 反查，把**找不到對應列**的物件排入 `purge_storage_queue`」——這裡找得到對應列，**那支掃描永遠看不到它**。

兩邊都接不住。**這條路徑目前沒有任何現有或已規劃的機制會清掉它。**

**我另外查了實際後果，比文件現在寫的更值得記**：app 對 `media` 的讀取只有 `SupabaseTimelineAPIClient.fetchMedia(ids:)` 與 `SupabaseAlbumsAPIClient.fetchMedia(ids:)` 兩處，兩處都是**帶明確 id**（id 來自 `diary_media`／`album_media`），全 repo 沒有任何「列出家庭所有 media」的查詢。所以這種未被 attach 的殘留列：

- **在 UI 上完全看不見**——使用者不知道它存在，也沒有任何介面能找到它、刪掉它；
- 但它**確實佔著 `families.storage_used_bytes`**（`private.media_storage_sync()` 只排除 `deleted_at IS NOT NULL` 的列，我 R1 在容器實測過 insert→+12345／soft delete→0）；
- 每發生一次「離線 → 放棄編輯器」就永久漏一份額度，不可回收。

**要做的事**（都很小）：
1. 把四處的「會退化成③／會被 `996220e9` 一併掃到」改成事實：**殘留的是一列活的 `media` 列（不是 Storage 孤兒物件），`996220e9` 的反向掃描依定義抓不到它；目前沒有任何機制回收，而且使用者在 UI 上看不到也刪不掉**。
2. 把**真正的缺口**記進待辦池（或直接擴充 `996220e9` 的敘述）：需要的偵測是另一個方向——「`media` 列存在、`deleted_at IS NULL`、**從未被任何 `diary_media`／`album_media` 引用**、且建立超過寬限期」。這跟 `996220e9` 的「掃 `storage.objects` 找沒有列的物件」是**兩支不同的查詢**，不能靠同一張票的同一段邏輯順帶解決；記在同一張票裡當第二個工作項也可以，但要寫清楚是兩件事。

我不要求本票實作這個偵測（同意它超出「不做：跨家庭批次工具」），只要求**記載與事實一致**——這正是 i9 存在的理由。

---

## m4（minor）· `LittleSproutTests/DiaryEditorViewLifecycleTests.swift:46`、`:80`

檔頭已經訂正得很好，但檔內還有兩處沿用被撤回的說法：

- `:46`（`presentThenDismiss` 的 doc comment）：「…模擬『畫面消失』——涵蓋取消鈕觸發的 `dismiss()`、成功發佈後的 `dismiss()`、**與互動式滑走**三種觸發方式共同的終點」
- `:80`（**斷言訊息字串**，不是註解）：「畫面消失（取消鈕、成功發佈後 dismiss、**或互動式滑走**）都該經過 .onDisappear 觸發 discardDraft() 軟刪孤兒 media」

`:80` 這條會印在測試失敗輸出裡（我 R2 就是拿它當 mutation 證據引用的），等於把撤回掉的說法留在未來每一次失敗訊息上。順手一起改（例如改成「取消鈕、成功發佈後 dismiss，或未來任何讓這支畫面消失的路徑」）。

## m5（minor）· PR body 未完成項 5 與「產出位置」段

兩處都還寫著「**i12 的追蹤記錄 id 待 orchestrator 後續補充**」，但 orchestrator 已經記好了——**LS-96 `854e5905`**（2026-09-06 11:47，harness 測試基建／XS）。我到 LS-96 查實過內容，描述精確（點名 `sectionTabViewWithDiaryHost` 的 `.preview()` 無家庭 → `if let familyID` 永遠不成立 → 空目的地靜默失敗、正是 R1 誤判根因），而且把 i11（probe 前置斷言該用 `XCTUnwrap`／`guard else`）也一起折進去了。PR body 補上這個 id 即可。

---

# 逐條查實

## 1. R3 diff 確為 comment／docs-only（兩個獨立機械檢查）

- **檢查一（逐行）**：`git diff d71548c..8f42fe0 -- '*.swift'` 的每一條 `+`／`-` 行，去掉前綴與縮排後**全部**以 `//` 開頭 → `non-comment changed lines = 0`。
- **檢查二（剝註解後比對）**：對三個 .swift 檔各自把 `^[[:space:]]*//.*$` 整行剝掉、去空行後取 md5，`d71548c` 與 `8f42fe0` **三個檔全部相同**：

```
SAME(code) LittleSprout/Features/Diary/DiaryEditorView.swift
SAME(code) LittleSprout/Services/Diary/DiaryComposerStore.swift
SAME(code) LittleSproutTests/DiaryEditorViewLifecycleTests.swift
```

**零可執行碼變更確認** ✅。因此 R2 我親自重放過的兩組 mutation（`.onDisappear` 拿掉 → 生命週期測試紅；`catch` 改丟棄 → 兩支重試測試紅）與 743 支測試的結論**原封沿用**，本輪不需也未重跑——與 handoff 的判斷一致。

## 2. 三處註解與我 R2 實測事實一致

逐字讀過訂正後的三段（`DiaryEditorView.swift:45-53` 的 `#if DEBUG` init、`:91-108` 的 `.onDisappear`、`:224-231` 的 `cancelButton`）與 `DiaryEditorViewLifecycleTests.swift:7-19` 檔頭：

- 「四種滑動手勢皆推不掉＋正向對照組」「`.navigationBarBackButtonHidden(true)`＋`.toolbar(.hidden, for: .navigationBar)` 確實關掉了互動式返回手勢」「與 UIKit 隱藏 nav bar 會停用 `interactivePopGestureRecognizer` 的既有行為一致」「`cancelButton`（連同發佈成功後的自動 dismiss）本來就是唯一離開路徑」——**與我 R2 量到的完全一致**，沒有超譯 ✅
- `#if DEBUG` init 的理由已改成**可測性**、並明講「不是因為生產路徑上有哪個入口會漏接」✅
- 保留 `.onDisappear` 的理由改成 **defense in depth**、且把「事實繫於那兩個 modifier」講明白 ✅
- **i7 折入得比我要求的還好**：`.onDisappear` 註解直接點名 `AuthenticatedRootView` 的 `.fullScreenCover(isPresented: familyStore.showsChildOnboarding)` 是「這道防線設計要接住的那種未來情境」，並註明現行流程下不可能與開著的編輯器同時出現 ✅
- 註解也把我 R2 量到的「三種 modal 開關皆不誤觸發」寫進去了，之後有人改這裡會知道那條界線在哪 ✅

**殘留字樣 grep**：`git grep -E "依然有效|仍然有效" 8f42fe0` 在 LS-212 相關檔只剩三處，逐一看上下文**全部都在「R1 曾誤判「…」」的撤回語境內**（`DiaryEditorView.swift:92`／`:227`、`DiaryEditorViewLifecycleTests.swift:8`），是正確寫法，不是殘留 ✅。另一處 `AuthStoreTests.swift:262` 是無關的既有註解。真正的殘留是 m4 那兩條（措辭不含「仍然有效」，所以這條 grep 抓不到——我另外用「滑走」全掃才看到）。

## 3. `docs/API.md` §3 與收斂路徑段的一致性

①②③ 三段結構、單一真相順序（Storage 物件 → `media` 列 → 本機暫存清除）、與 `docs/PLAN.md` §5 的互相指涉都仍然一致；i9 那段插在 ② 結尾、位置正確，**除了結尾那句後備敘述（M2-R3）之外**，前半段（in-memory、App 重啟即失、`discardDraft()` 是最後一次呼叫所以離線放棄救不回）**完全準確**，與我 R2 的判讀一字不差 ✅

## 4. PR body

- 「## R3」段存在、逐條對應 R2 的 M1-R／i7／i9，處置寫得清楚 ✅
- `996220e9` 的引用位置正確（未完成項 2、風險段、產出位置）；我 R1 已到 LS-96 查實過該池項存在且內容相符 ✅
- `854e5905` **未被引用**（見 m5）
- 未完成項把 R1 i1–i6、R2 i7／i8／i10／i11／i12 全部逐條交代，沒有漏項——i8／i10／i11 標「非本票行動項」我同意（i10／i11 本來就是我請 orchestrator 決定的 harness 項目，i11 更是我自己的方法論問題）✅

## 5. 累計 scope（`92c891a...8f42fe0`）

20 檔，全部可追溯到 LS-212；R3 沒有引入任何新檔、沒碰 migration／LS-167 檔／`docs/PLAN.md`。commit 訊息 `docs(media): LS-212 R3——…` 型別與內容相符（純文件／註解）✅

## 6. CI

貼 status 當下：`lint` pass、`db` pass、`ci`／`ci-ipad`／`rules` 仍在跑（run 34010575457）。本輪裁決是 **failure**，不因 CI 結果而改變（R3 零可執行碼變更，且 R2 head 的同一組 job 全綠）——但依規約在此明記狀態，下一輪 R4 會對新 head 重跑並確認四項全綠再貼。

---

# informational

- **i13** M2-R3 修好之後，值得順手在 `docs/API.md` §3 那段補一句「這三種不一致**之外**還有第四種：`media` 列存在、未被任何 `diary_media`／`album_media` 引用、也沒被軟刪」——目前文件把收斂路徑講成三種，但實際上這條 PR 自己就會產出第四種殘留（離線放棄那條）。把它明確編號進去比塞在 ② 的括號裡清楚。
- R1 i1–i6、R2 i7／i8／i10／i11 的處置我全部同意，R3 handoff 逐條交代完整，不再重述。

---

# 給 QA 的重點（下一輪 APPROVE 後帶著跑）

1. **上傳中殺 App → 啟動清空**：編輯器選一支影片、按發佈，上傳途中直接殺掉 App → 重開 → `xcrun simctl get_app_container <udid> com.leoyeh.littlesprout data` 進去確認 `tmp/ls-media-drafts/` 已被清空（`MediaDraftTempStorage.purgeStaleFiles()`），且時間軸沒有破圖。
2. **取消／離開編輯器 → 孤兒 media 軟刪＋額度回落**：斷網發佈（會停在 `attachMedia` 失敗）→ **恢復連線** → 按「取消」離開 → SQL 確認那幾列 `media.deleted_at` 有值、`families.storage_used_bytes` 回落到不含它們。**離線狀態下取消**要另外走一次並確認**額度不會回落**（這是 M2-R3 那條已知限制的實際樣子，是預期行為、不是 FAIL——請 QA 記錄下來，別當缺陷報）。
3. **EXIF 直拍**：用直拍（portrait）照片上傳，SQL 確認 `media.width/height` 與 `thumb_width/thumb_height` **同一個方向**（都是直向，不是互為轉置），列表版面不拉伸。
4. **開關 sheet／照片挑選器不誤刪**：佇列放一支影片 → 開關「記錄日期」／「寶貝歸屬」／照片挑選器各一次 → 回來按發佈仍成功（驗 `.onDisappear` 不誤觸發；我已用計數器量過，這條是端到端複驗）。
5. **滑走離開**：嘗試邊緣滑走離開編輯器並記錄「推不推得掉」。我 R2 實測四種手勢都推不掉，若 QA 在真機／iOS 26.2 上發現推得掉，請立刻回報——那表示 R2/R3 的註解結論在該環境不成立。
6. **`UploadQueueSheetView` 零接線不算 FAIL**：上傳佇列 sheet 在 production 沒有任何入口（只在 `TapTargetGateHarness`／`#Preview`／UITest 出現），是 LS-166 的範圍，我 R1 已 `git grep` 查實。QA 若在 app 裡找不到上傳佇列畫面，是預期的，不要報 FAIL。
7. 走 **iOS 26.2**（`.ios-runtime` 釘住版）——我與實作者本機都只有 26.0／26.5。

---

# 環境收尾

- supabase lock：**未持有**（R3 純文件／註解，未碰容器；R1 持有 1 分 10 秒、R2 未持有，皆已如實記錄）。
- 模擬器：本輪**未開任何裝置**；`LS-212-iPhone17Pro FE811783-…` 目前為 `Shutdown`（已確認）。未手動跑過 `simctl ui`。
- 未建 review worktree（本輪純讀 `git show`／`git diff`／`git grep`，不需編譯）；實作者的 `.claude/worktrees/LS-212` 未動。
- 未碰 Pen。

**下一輪**：修 M2-R3（四處敘述改成事實＋把真正的缺口記進池）＋順手收 m4／m5 後 push，對新 head 重派 R4 並重貼 `merge-review` status。

### 2026-09-06T04:13:33.318Z · Chih-Lin Yeh · `9bab2dd9`

**orchestrator 09-06 12:13**：merge-review R3（8d1e57bc）REQUEST_CHANGES——comment-only 與註解一致通過；M2-R3：i9 四處「後備」敘述錯誤（殘留是未軟刪、未被引用的活 `media` 列，非③孤兒 Storage；`996220e9` 接不住；UI 看不見、永久佔額度）；m4 斷言訊息殘句；m5 PR body id 待補。已派原 agent R4（文字輪）：四處改事實、LS-96 新池項「未引用活 media 列偵測與清理排程（與 996220e9 互補）」附 id、m4／m5。R4 push 後派 merge-review R4。

### 2026-09-06T04:36:17.279Z · Chih-Lin Yeh · `209796f1`

**orchestrator 09-06 12:36**：R4 handoff 收到，PR #334 head `dbe5da4`（`195efbb` 文字-only：i9 四處改為「殘留為活的、未引用的 media 列、現無機制回收」＋新池項 LS-96 `c2050d43`（未引用活 media 列偵測，與 996220e9 互補）、m4 斷言訊息殘句、m5 引 `854e5905`；另 merge development 07a204e 追基底）。已派 merge-review R4（同 reviewer）：查實文字與池項、merge commit 無夾帶；status 貼 dbe5da4。APPROVE 後併入 development → promote → QA。

### 2026-09-06T04:58:23.073Z · Chih-Lin Yeh · `39151115`

**merge-reviewer 09-06 12:56 — merge-review R4｜APPROVE ✅**

審的 head `dbe5da4a5806b3d8943a26b07e469e432c35fe65`（開審與收尾兩次 `gh pr view 334 --json headRefOid` 皆同，未前進）。結構＝R4 內容 commit `195efbb`（疊在 R3 `8f42fe0` 上）＋ merge commit `dbe5da4`（`195efbb` × `origin/development` `07a204e`）。**CI 五項全綠**（`ci` 25m7s／`ci-ipad` 7m40s／`db` 3m37s／`lint` 13s／`rules` 6m22s，run 34011697845）。本輪未開模擬器、未碰容器、未建 review worktree。

## 裁決

**APPROVE** — R3 的 M2-R3（major）與 m4／m5 三條全部收乾淨，我逐條查實過；沒有新的 blocker／major。R4 的 merge commit 我特別查了「衝突解法有沒有夾帶」與「`--ours` ＋ xcodegen 重生的 `project.pbxproj` 有沒有吃掉任何一邊的檔案」，兩項都乾淨。

---

# 逐條查實

## 1. M2-R3（major）— 四處敘述已改成事實 ✅

`DiaryComposerStore.pendingOrphanMediaIDs` doc comment 與 `docs/API.md` §3 ② 都新增了「**殘留物是什麼、目前沒有任何機制回收**」段，內容與我 R3 查到的事實**逐點對得上**：

- 明講殘留的是一列 **`deleted_at IS NULL` 的活 `media` 列**，不是孤兒 Storage 物件 ✅
- 明講**不符合 ③ 的定義**（「`media` 列從未成功 `insert`」）✅
- 明講 **`996220e9` 那支掃描依定義接不住**（「那支掃描依定義只找『列不存在』的物件，這裡列存在」）✅
- 把我查到的後果原樣寫進去：全 repo 對 `media` 的讀取只有 `SupabaseTimelineAPIClient.fetchMedia(ids:)`／`SupabaseAlbumsAPIClient.fetchMedia(ids:)`、皆帶明確 id，沒有「列出家庭所有 media」的查詢 → **UI 上完全看不見、使用者刪不掉、永久佔 `families.storage_used_bytes`** ✅
- 明講真正需要的是**另一支不同的查詢**並指向 `c2050d43` ✅

PR body 另外兩處（未完成項 2、風險段）也都改成「**（R4 訂正）**…**不會**退化成這個方向…是第三種、不同的殘留」。**四處到齊，沒有殘留舊敘述。**

## 2. 新池項 LS-96 `c2050d43` — 存在且內容正確 ✅

到 LS-96 讀了全文（`c2050d43-9ede-43f2-876c-eab899d08a06`，2026-09-06 12:14，backend／size:S）。內容比我 R3 要求的還完整：

- 把「③ 與 `996220e9` 都以『`media` 列不存在』為前提」這個關鍵前提講明白；
- 完整重述觸發路徑（PUT 成功→insert 成功→`attachMedia` 斷線→離線放棄→`softDeleteMedia` 失敗→pending 隨 store 消失）；
- 帶上「UI 看不見／刪不掉／永久佔額度」的後果查實；
- 處置建議寫出完整判準——「`media` 列存在、`deleted_at IS NULL`、**從未被任何 `diary_media`／`album_media` 引用**、且建立超過寬限期（例如 24 小時，避免正常上傳流程中還沒 attach 完成的列被誤判）」——**寬限期那句是我沒點名、他自己補的，是對的**（沒有它會誤殺正在發佈中的列）；
- 明講可以跟 `996220e9` 放同一張票但**必須分開寫成兩個工作項、兩支不同查詢**。

## 3. m4 — 殘句已清 ✅

`git grep "滑走" dbe5da4 -- LittleSproutTests/DiaryEditorViewLifecycleTests.swift` 只剩 `:8`／`:13` 兩處，**都在檔頭的撤回語境內**（「R1 曾誤判『…依然有效』」「這個『沒有滑走手勢』的事實繫於上面那兩個 modifier」）——正確寫法。我 R3 點名的兩處都改了：

- `:43-46` `presentThenDismiss` doc → 改成「…以及未來任何會讓這支畫面消失的路徑（這支畫面目前沒有互動式返回手勢，見檔頭文件註解…）」
- `:82` 斷言訊息 → 「畫面消失（取消鈕、成功發佈後 dismiss，**或未來任何讓這支畫面消失的路徑**）都該經過 .onDisappear…」

## 4. m5 — PR body 已引 `854e5905` ✅

出現在「## R3」段 i12（第 75 行）與「產出位置」段（第 110 行），加上 R4 段的 m5 說明共三處。唯一還含「待 orchestrator 後續補充」字樣的是 R4 段裡「已改引 `854e5905`」那句本身的引述，不是殘留。

## 5. `195efbb` 是 comment／docs-only（逐 hunk＋機械檢查）✅

逐行掃 `git diff 8f42fe0..195efbb -- '*.swift'`，**唯二**非註解的變更行是同一條斷言**訊息字串**的前後版本；剝掉註解後比對：

```
SAME(code)  LittleSprout/Services/Diary/DiaryComposerStore.swift
DIFF(code)  LittleSproutTests/DiaryEditorViewLifecycleTests.swift
```

`DIFF` 那支我再 diff 一次剝註解後的全文，**唯一差異就是第 42 行的訊息字串**：

```
<  "畫面消失（取消鈕、成功發佈後 dismiss、或互動式滑走）都該經過 .onDisappear…"
>  "畫面消失（取消鈕、成功發佈後 dismiss，或未來任何讓這支畫面消失的路徑）都該經過 .onDisappear…"
```

`XCTAssertEqual` 的**判斷式（前兩個參數）一個字元都沒改**——訊息字串不影響鑑別力。因此我 R2 親自重放過的兩組 mutation（`.onDisappear` 拿掉→生命週期測試紅；`catch` 改丟棄→兩支重試測試紅）與 743 支測試的結論**原封沿用**，本輪不需重跑，與 handoff 的判斷一致 ✅

## 6. merge commit `dbe5da4` 無夾帶（本輪重點查核）✅

實作者因為 `origin/development` 前進到 `07a204e`（LS-190 PR #331）撞上 `project.pbxproj` 衝突，用了 `git checkout --ours` ＋ `xcodegen generate` 重生。這是最容易靜默吃掉東西的操作，我用三個角度查：

**(a) 合併後的樹＝dev ＋ 純 LS-212 變更**
`git diff 07a204e..dbe5da4 --name-only` 的檔案清單與合併前的 `git diff 92c891a...8f42fe0 --name-only` **逐行完全相同**（20 檔）。若衝突解法吃掉任何 dev 內容，那些檔案會以「被還原」的形式出現在這個 diff 裡——沒有。

**(b) LS-212 檔在 merge 中沒有被改動**
對 19 個非 pbxproj 檔逐一比對 `195efbb` vs `dbe5da4` 的內容 md5：**17 檔 SAME**；只有兩檔被 merge 動到，逐 hunk 看都是**純 LS-190 新增**、LS-212 的內容原封不動：

- `LittleSproutApp.swift`：只多了 `eulaStore` 的 `@State`／`init` 初始化／傳參三處。**LS-212 的 `MediaDraftTempStorage.purgeStaleFiles()` 仍在 `init()` 第一行**（第 39 行，仍早於 `SupabaseClientFactory.makeClient()`，這個順序是本票文件註解明講的前提）✅
- `PreviewDiaryAPIClient.swift`：只多了 LS-190 的 `setDiaryDeleted(diaryID:deleted:)`；**LS-212 的 `softDeleteMedia(mediaIDs:)` 仍在**（第 28 行）✅

**(c) 重生的 `project.pbxproj` 是兩邊的聯集，沒有吃掉任何一邊**
抽出三個 revision 的檔案參照集合比對：dev(`07a204e`)=312、LS-212 分支(`195efbb`)=290、合併後(`dbe5da4`)=318。三段檢查全空：

```
在 dev 有、merged 沒有（被 --ours 吃掉的 LS-190 檔）：（空）
在 LS-212 分支有、merged 沒有（被吃掉的本票檔）：（空）
merged 多出來（兩邊都沒有的）：（空）
```

**聯集正確、零遺漏、零多餘** ✅。加上 CI `ci` job 在合併後的樹上完整 build＋跑完全部測試（25m7s）通過，這條 merge 我沒有疑慮。

## 7. 累計 scope

以新基底看 `git diff 07a204e..dbe5da4`：20 檔、+909/−19，全部可追溯到 LS-212；沒有 migration、沒碰 LS-167 檔、沒有順手改動。

---

# informational（不阻擋，供收尾參考）

- **i13**（我 R3 建議把「第四種殘留」正式編號進 `docs/API.md` §3）——實作者選擇不修，理由是「訂正後的敘述已用文字說明這是第三種、與①③和 `996220e9` 都不同的殘留，涵蓋了實質關切」。**我接受**：實質資訊已完整、可被讀到，編號與否是體例偏好，不值得再開一輪。若未來 `c2050d43` 那張後端票落地，順手把它升成正式的④比較整齊。
- R1 i1–i6、R2 i7／i8／i10／i11／i12 的處置沿用，四輪 handoff 逐條交代完整，無漏項。
- 三張待辦追蹤 comment 我都到 LS-96 查實過內容正確：`996220e9`（Storage 有物件無列，size:M）、`854e5905`（harness `sectionTabViewWithDiaryHost` 無家庭導致生產路徑靜默不可達，含 i11，XS）、`c2050d43`（未被引用的活 `media` 列偵測，size:S）。

---

# 給 QA 的重點

1. **上傳中殺 App → 啟動清空**：編輯器選一支影片、按發佈，上傳途中直接殺掉 App → 重開 → `xcrun simctl get_app_container <udid> com.leoyeh.littlesprout data` 進去確認 `tmp/ls-media-drafts/` 已清空（`MediaDraftTempStorage.purgeStaleFiles()`），時間軸無破圖。
2. **取消／離開編輯器 → 孤兒 media 軟刪＋額度回落**：斷網發佈（會停在 `attachMedia` 失敗）→ **恢復連線** → 按「取消」離開 → SQL 確認那幾列 `media.deleted_at` 有值、`families.storage_used_bytes` 回落到不含它們。
3. **離線取消時額度不回落＝預期行為，不是 FAIL**：同上但**保持斷網**按取消離開 → `media.deleted_at` 仍是 NULL、額度不回落。這是 `pendingOrphanMediaIDs` in-memory 的已知限制（`docs/API.md` §3 與 `DiaryComposerStore` doc comment 都明記，缺口記在 LS-96 `c2050d43`）——請記錄現象但**不要當缺陷報**。
4. **EXIF 直拍同向**：用直拍（portrait）照片上傳，SQL 確認 `media.width/height` 與 `thumb_width/thumb_height` **同一個方向**（都是直向，不是互為轉置），列表版面不拉伸。
5. **`UploadQueueSheetView` 零接線＝預期，不是 FAIL**：上傳佇列 sheet 在 production 沒有任何入口（只出現在 `TapTargetGateHarness`／`#Preview`／UITest），屬 LS-166 範圍，我 R1 已 `git grep` 查實。app 裡找不到它是對的。
6. **（加分項）開關 sheet／照片挑選器不誤刪**：佇列放一支影片 → 開關「記錄日期」／「寶貝歸屬」／照片挑選器各一次 → 回來按發佈仍成功（驗 `.onDisappear` 不誤觸發；我 R2 已用計數器量過三種 modal 皆不觸發，這條是端到端複驗）。
7. **（加分項）滑走離開**：嘗試邊緣滑走離開編輯器並記錄推不推得掉。我 R2 實測四種手勢都推不掉（這是程式註解現在寫的事實）；若在真機／iOS 26.2 上發現推得掉，請回報——那表示註解結論在該環境不成立。
8. 走 **iOS 26.2**（`.ios-runtime` 釘住版）——我與實作者本機都只有 26.0／26.5，四輪本機驗證皆用 26.0；CI 用 26.2 且已綠。

---

# 環境收尾

- supabase lock：**未持有**（R4 純文件／註解；四輪合計 R1 持有 1 分 10 秒，R2／R3／R4 未持有）。
- 模擬器：本輪**未開任何裝置**；`LS-212-iPhone17Pro FE811783-…` 為 `Shutdown`。全程未手動跑 `simctl ui`。
- 本輪未建 review worktree（純 `git show`／`git diff`／`git grep`／`gh`）；R1／R2 用過的 `.claude/worktrees/LS-212-review` 早已移除。實作者的 `.claude/worktrees/LS-212` 四輪都沒動過。
- 未碰 Pen。

**status**：`merge-review=success` 已貼到 `dbe5da4a5806b3d8943a26b07e469e432c35fe65`。可進 QA。

### 2026-09-06T04:59:59.254Z · Chih-Lin Yeh · `2a27a3cf`

**orchestrator 09-06 12:59**：merge-review R4 APPROVE（39151115，status 貼 dbe5da4；六項 check 綠；merge commit 三角度查核無夾帶）→ PR #334 已併入 development `c87e7b3`，merge-review status 沿貼 dev tip。informational i13 不修（reviewer 接受）。接續：promote development→test（test 目前由 LS-190 QA 持有，落地後 LS-212 QA 以 delta 07a204e..c87e7b3 驗：上傳中殺 App 啟動清空、取消／離開編輯器孤兒軟刪＋額度回落、離線取消額度不回落屬預期、EXIF 直拍同向；`UploadQueueSheetView` 零接線屬 LS-166）→ sweeper → 打版 → Done。兩條後續池項 `996220e9`／`c2050d43` 合併開後端排程票。

### 2026-09-06T05:04:28.319Z · Chih-Lin Yeh · `dd0cd7c9`

## dead-code sweep — LS-212

**dead-code-sweeper 09-06 13:02**：唯讀巡查，範圍＝`git diff 07a204e..c87e7b3`（20 檔，+909/−19，比對過與 merge-review R4 `39151115` 記載的檔案清單逐行相同）。全程用 `git show <sha>:<path>`／`git grep <pattern> <sha>` 對 merge commit 快照查證，未 checkout、未動任何 worktree／模擬器／Pen。

### Findings（本 feature 引入的死碼）

**無**。逐項查證如下，均查無死碼：

1. **`cancelButton` action 與 `.onDisappear` 是否重複觸發清理**——讀 `git show c87e7b3:LittleSprout/Features/Diary/DiaryEditorView.swift` 全文：`cancelButton` 的 Button action 現在只呼叫 `dismiss()`（R2 已把 `store.discardDraft()` 搬到 `body` 的 `.onDisappear`，`cancelButton` 註解明講「這裡只需要觸發 dismiss，不必另外呼叫」）——**無重複呼叫**，R1 的舊寫法沒有殘留。

2. **`try?` 吞錯路徑殘留**——`grep -n "try?" DiaryComposerStore.swift`（快照）命中 3 處：`cleanupRemovedDrafts` 內刪影片暫存檔的 `try? FileManager...removeItem`（best-effort 檔案清理，非本輪查核對象）、`cleanupVideoTempFiles` 的兩處同類。R1 點名的 `softDeleteMedia` 呼叫已改成明確 `do { try await ... } catch { pendingOrphanMediaIDs = candidateMediaIDs }`——**不再是 `try?`，失敗不再吞掉**，確認落地。

3. **`removeValue` 舊順序**——`newOrphanMediaIDs` 仍在 `await` **之前**做 `removeValue(forKey:)`，但失敗時整批 `candidateMediaIDs`（含這批 id）存回 `pendingOrphanMediaIDs`，不會遺失——這是 R2 修法本身（merge-reviewer m1 已判定「已解決」），非殘留問題，本輪重新讀過邏輯確認一致。

4. **`MediaDraftTempStorage` 是否有只為測試存在的入口**——
   ```
   git grep -n "MediaDraftTempStorage" c87e7b3 -- '*.swift'
   ```
   四個成員逐一核對呼叫端：`purgeStaleFiles()`→`LittleSproutApp.swift:39`（production）；`newFileURL(extension:)`→`PickedItemLoader.swift:144`／`VideoTrimmer.swift:38`（production）＋測試；`directory`→內部（`makeDirectoryIfNeeded`／`purgeStaleFiles`）與測試皆直接讀取；`makeDirectoryIfNeeded()`→僅被 `newFileURL` 內部呼叫（非 test-only，是 production 呼叫鏈的一環，只是可見度給得比嚴格必要寬——不算死碼，未列為 finding）。**沒有任何入口只被測試呼叫、production 從未觸達**。

5. **`pendingOrphanMediaIDs` 讀寫點**——
   ```
   git grep -n "pendingOrphanMediaIDs" c87e7b3 -- '*.swift'
   ```
   5 處全部在 `DiaryComposerStore.swift` 本檔內：宣告＋一處 union 讀取＋成功 `removeAll()`＋失敗賦值，單一擁有者、無外部直接讀寫、無孤兒寫入點。

6. **`#if DEBUG init(store:childrenStore:)` 是否只被測試用**——
   ```
   git grep -n "DiaryEditorView(store:" c87e7b3 -- '*.swift'
   ```
   僅 2 處命中，皆在 `DiaryEditorViewLifecycleTests.swift`（77、99 行）。**確為測試專用**，且 R3 已在該 init 與呼叫端文件註解中明講理由是「可測性，不是生產路徑有入口會漏接」——與票文預期一致，不算死碼。

7. **`StubMediaUploadService.setSoftDeleteMediaHandler` 是否被使用**——
   ```
   git grep -n "setSoftDeleteMediaHandler" c87e7b3 -- '*.swift'
   ```
   定義 1 處＋呼叫 4 處（`DiaryComposerStoreDraftCleanupTests.swift:153/160/184/189`，分別佈置失敗／成功兩種情境）。**非死碼**，R1 m2 指出的問題已在 R2 解決。

8. **`docs/API.md`／`docs/PLAN.md` 段落與程式碼一致**——新增段落引用的函式逐一核實存在：
   ```
   git grep -n "func fetchMedia(ids:" c87e7b3 -- '*.swift'   → SupabaseTimelineAPIClient／SupabaseAlbumsAPIClient 皆存在
   git grep -n "var aspectRatio" c87e7b3 -- '*.swift'         → TimelineModels.swift:177 存在
   ```
   `PickedItemLoader.orientedPixelSize(of:)` 存在（`PickedItemLoader.swift:73`，production 呼叫端 `loadPhoto` 第 54 行）；`LittleSproutApp.init()` 第一行呼叫 `MediaDraftTempStorage.purgeStaleFiles()`（第 39 行，早於 `SupabaseClientFactory.makeClient()`）——文件敘述與程式碼相符，`996220e9`／`c2050d43` 兩則 LS-96 池項引用 merge-reviewer 已到 LS-96 逐一查實過（R3／R4 comment），本輪未重複查證。

9. **其餘檔案**（`project.pbxproj`、`MediaUploadService.swift`、`VideoTrimmer.swift`、`DiaryEditorView+Photos.swift`、`PreviewDiaryAPIClient.swift`）——pbxproj 6 個新檔（`MediaDraftTempStorage.swift`／`.Tests`、`MediaUploadService+SoftDelete.swift`、`SupabaseMediaSoftDeleteTests.swift`、`DiaryComposerStoreDraftCleanupTests.swift`、`DiaryEditorViewLifecycleTests.swift`）皆正確登記 `PBXBuildFile`＋`PBXFileReference`＋群組歸屬＋`Sources` build phase，無孤兒／重複項；`PreviewMediaUploadService.softDeleteMedia(mediaIDs:)` 是 protocol conformance 要求的空實作（`#Preview` 用），非死碼。

10. **鷹架殘留**（註解掉的程式碼塊／debug print／假資料／紅綠驗證殘留）——`grep -nE "print\(|TODO|FIXME|XXX|debugPrint"` 掃過本票所有觸碰檔案：無命中（唯一命中在 `scripts/ops/review-demo-genvideo.swift` 的既有 `print`，與本票無關、範圍外）。逐檔讀過未發現整段被註解掉的程式碼。

### 環境殘留（非 repo 死碼，僅記錄供環境衛生參考——本輪未刪除任何檔案）

- `/private/tmp/LS-212-*.log`／`.bak` 共 22 個檔案（review／實作各輪的 mutation log、build log、probe log），直接落在 `/private/tmp` 根目錄，**不在** scratchpad 子目錄下——不符合 CLAUDE.md「暫存檔一律 `LS-<n>-<用途>.<ext>`（或 `mktemp -d`）」的「用 scratchpad／`mktemp -d`」慣例（命名本身合規，位置不合規）。這些不是 git 追蹤檔案，不影響 repo，僅為本機磁碟衛生問題，安全可清；不需要開票，建議下次巡檢或 session 收尾時 `rm` 掉即可。
- `.claude/worktrees/LS-212`（分支 `feature/LS-212-media-pipeline-hygiene`，head `dbe5da4`）仍存在——這是預期中的，屬於 Feature 收尾儀式「清理」步驟（`bash scripts/ops/cleanup-merged.sh --apply LS-212`）的範圍，不是本次巡檢該動的東西，待 orchestrator 在 Done 前執行。

### 既有死碼觀察（本票範圍外）

無。本輪讀到的既有程式碼（`DiaryComposerStore.swift`、`PickedItemLoader.swift`、`MediaUploadService.swift` 等既有部分）都在既有的 merge-review（R1–R4）與過往票已被查過（M1/M2/M3/M5/M9 等），未發現本票範圍外的新死碼線索；但本輪讀取範圍限於 20 個變更檔，未對全 repo 做地毯式死碼掃描，不能排除其他地方存在既有死碼。

### 建議處置

- Findings 為空，無需開 fix 票。
- 環境殘留（`/private/tmp/LS-212-*`）不需開票，屬本機衛生，任何 agent 收尾時可直接清除。
- worktree 清理按既定流程走（Done 前 orchestrator 執行 `cleanup-merged.sh --apply LS-212`），不需額外票。

### 搜尋方法與盲區聲明

- 方法：`git diff 07a204e..c87e7b3 --stat/--name-only` 界定範圍；對每個變更檔 `git show c87e7b3:<path>` 讀全文；`git grep -n <symbol> c87e7b3 -- '*.swift'` 交叉比對呼叫點；`find`／`git worktree list` 查殘留檔案與 worktree。全程未 `git checkout`、未建 review worktree、未跑模擬器／容器。
- **盲區聲明**：純文字 `grep`／`git grep` 抓不到 reflection、字串拼接組出的 selector／KeyPath 動態引用、Objective-C runtime 呼叫（本票不涉及，但一般性聲明）；也抓不到「透過 SwiftUI `@Bindable`／`@Environment` 間接讀寫、變數名不同但語意相同」的情況——本票逐檔通讀過，主要靠人工核對呼叫鏈而非純字串比對降低此風險，但仍非窮舉證明。`docs/API.md`／`docs/PLAN.md` 對 `996220e9`／`c2050d43` 兩則 LS-96 池項內容本身的正確性，本輪信賴 merge-reviewer R3／R4 已做過的查實，未重複驗證。

### 2026-09-06T05:32:31.538Z · Chih-Lin Yeh · `16d98cba`

**orchestrator 09-06 13:32**：development c87e7b3（本票＋LS-190）已晉升 test → 票進 QA。QA 派工延後：`qa-test` worktree 正被 LS-190 QA 使用（detach 07a204e、持 supabase lock），同一 worktree 不可同時 checkout 兩個 SHA；LS-190 QA 收工後即派本票 QA（delta 07a204e..c87e7b3：上傳中殺 App 啟動清空、取消／滑走離開編輯器孤兒軟刪＋額度回落、離線取消額度不回落屬預期、EXIF 直拍同向；`UploadQueueSheetView` 零接線屬 LS-166 不算 FAIL）。sweeper（dd0cd7c9）已完成無死碼。

### 2026-09-06T10:01:18.452Z · Chih-Lin Yeh · `3373fd8d`

**qa 09-06 18:01 · 裁決：PASS（附條件——2 項情境本輪未獨立重跑，理由與既有證據見下）**

驗收環境：`.claude/worktrees/qa-test` `git checkout --detach origin/test`＝`65bfb22`。`qa-test-iPhone17Pro`（`5F59FF04`，iOS 26.0；`.ios-runtime` 釘 26.2，本機無此版本）。

## 逐條驗收

**1a. EXIF 直拍照片方向修正（票文範圍 2、票文驗收「直拍照片上傳後 media.width/height＝thumb 方向；列表無拉伸」）— ✓ 真後端實測**

怎麼驗：用 PIL 合成一張感光元件方向 800×600（橫）、EXIF `Orientation=6`（顯示方向應為 600×800 直）的測試照片，`xcrun simctl addmedia` 灌進模擬器相簿 → 真實走 App 上傳流程（Email OTP 登入 `ls190b@example.com`、建家庭、寫日記附加這張照片、發佈）→ SQL 查證：

```
width | height | thumb_width | thumb_height
  600 |    800 |         384 |          512
```

`width/height`（600×800）與 `thumb_width/thumb_height`（384×512，384/512=0.75=600/800）**方向一致（皆為顯示方向・直向）**——修正前的 bug（`width/height` 用儲存方向 800×600、與 thumb 方向相反）不再重現。**相符**。log：`scratchpad/LS-QA-65bfb22/ls212-exif-direction-check.log`。

**2. DB 測試＋單元測試綠 — ✓**：`bash supabase/tests/run.sh`（本輪跑過，`✓ 全部 DB 測試通過`，log 見 LS-213 comment 引用同一份 `sql-tests-run.log`，LS-212 相關斷言在 `supabase/tests/` 既有檔內，未見紅）；`xcodebuild test -only-testing:LittleSproutTests` → `Executed 774 tests, with 0 failures`（含 `DiaryComposerStoreDraftCleanupTests`／`DiaryEditorViewLifecycleTests`）。

**1b／1c. 上傳中殺 App／離線放棄編輯器孤兒軟刪＋額度回落 — ⊘ 本輪未獨立重跑（附強證據理由）**

怎麼驗（本應）：依票文與 merge-review「給 QA 的重點」實跑「編輯器選影片、按發佈、上傳中殺 App、重開、確認 `tmp/ls-media-drafts/` 已清空」與「斷網發佈→恢復連線→取消→SQL 確認孤兒軟刪＋額度回落」。**本輪未能重跑**：完成 EXIF 驗證後，共用容器 lock 被另一 worktree（`LS-222`，`feature/LS-222-purge-orphan-followup`）持有中，`--hold` 排隊等待剩餘時間達 27-29 分鐘，超出本輪合理時間預算，未選擇久候。

**這不是空白判斷**——這兩個情境已在 merge-review R1（`568044ab`）與 R2（`695170ed`，`0e4c0eed` 前身）**各自被獨立、詳細重現過並附完整輸出**，本輪已逐字讀過並核對其查證方法無漏洞：
- R1 用計數器探測 `.onDisappear` 觸發時機，mutation 重放（拿掉 `.onDisappear` 整段）→ `DiaryEditorViewLifecycleTests` 真的轉紅（`XCTAssertEqual failed: ("[]") is not equal to (...)`, rc=65），證明清理路徑有真實鑑別力，非恆綠。
- R2 對「offline cancel 不回落額度是否為刻意限制」逐行讀過 `DiaryComposerStore.pendingOrphanMediaIDs` 邏輯，判定為記載完整的已知限制（in-memory、`discardDraft()` 是 store 生命週期最後一次呼叫），非缺陷；並用 mutation（把失敗保留改回丟棄）證明重試累積機制有效（`DraftCleanupTests` 兩處轉紅）。
- 兩輪皆有明確的「supabase lock 未持有／模擬器已 shutdown」環境收尾記錄，可信度高（非空泛聲稱）。

**裁決依據**：merge-reviewer 對 race condition／生命週期本就是其審查重點（COLLABORATION §1 職責劃分），這兩項已被覆蓋兩輪、皆有可重放證據；EXIF 方向（本票另一半範圍、也是 LS-96 `66770dd0` 池項的原始觸發點）已由本輪真後端獨立驗證確認無誤。綜合判斷不構成 FAIL 或需要擋下 promote 的理由，但誠實記錄**本輪 QA 未親自重跑**這兩項，供之後若有疑慮時知道從哪裡補測。

**3. `UploadQueueSheetView` 零接線＝預期 — ✓**：本輪 App 內操作全程未見任何上傳佇列 UI（僅 harness／Preview 可達，符合 merge-review 已查實的 LS-166 現況），未誤判為 FAIL。

## 產出位置
- `scratchpad/LS-QA-65bfb22/ls212-exif-direction-check.log`
- `scratchpad/LS-QA-65bfb22/qa-portrait-exif6.jpg`（合成測試照片，未進版控）

## 環境收尾
- supabase lock：與 LS-190 共用同幾段 hold（見 LS-190 comment），皆已釋放
- 模擬器已關：`5F59FF04-D2C6-413D-B31A-AFD39B257FB3`
- 未碰 Pen

裁決：**PASS**——EXIF 方向修正本輪真後端獨立驗證通過，DB／單元測試綠；上傳中斷兩項情境本輪因容器 lock 排隊過久未重跑，沿用 merge-review R1／R2 兩輪皆有 mutation 佐證的既有驗證結果，如需 QA 親自重跑建議另排時段。

### 2026-09-06T13:17:07.838Z · Chih-Lin Yeh · `92cdadaf`

（09-06 21:16）收尾儀式③ 清理：`bash scripts/ops/cleanup-merged.sh --apply LS-212` exit 0 → worktree 1／本機分支 0／遠端分支 0／專屬模擬器 1／DerivedData 2（略過項全 0）。分支已併入 development、test 65fbb22 QA PASS，worktree 與專屬模擬器已移除。剩餘：隨 v0.20.0 test→main 後標 Done（sweeper 已完成；lesson review 記於 Done comment）。

### 2026-09-06T13:26:55.312Z · Chih-Lin Yeh · `103a131e`

（09-06 21:25）收尾儀式完成 → Done（v0.20.0，main 689f5ae）。
① dead-code-sweeper：已完成（見前 comment）。
② lesson learning review：本票 merge-review R3（8d1e57bc）查實「attachMedia 斷線後殘留未引用活列」這條殘留路徑，衍生 LS-213／222／223 三票。教訓：改上傳／刪除管線的票，PR body 應附「此路徑每個失敗點留下什麼殘留、誰負責回收」對照表，讓 reviewer 對表查而非靠靈感。候選 harness：pr-body-check 對 diff 觸及 supabase/functions 或 Upload* 的 PR 要求「殘留對照」段 → 記入 LS-96 P2。
③ 清理：見 21:16 comment（cleanup-merged --apply exit 0）。④ Pen 清場：非設計票，不適用。

