# LS-226 Harness：`scripts/design/overflow-scan.js` 對萬節點級 .pen 的三支 O(n²) 掃描與 tree-hash 自帶分批＋跨批配對＋加總、收據每支掃描附結果雜湊並由 `design-evidence-check.sh` 驗、`scan_note` 必填（LS-208 r5 分段手寫收據首例，merge-review R2 `e1e5157f`）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:M, lane:harness |
| Cycle | 2 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-06T14:28:41.994Z |
| 開始 | 2026-09-12T02:53:56.520Z |
| 完成 | 2026-09-12T06:27:00.930Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-226/harnessscriptsdesignoverflow-scanjs-對萬節點級-pen-的三支-on-掃描與-tree-hash |
| 關係 | related→LS-215, related→LS-122, related→LS-180, related→LS-230, related→LS-209, related→LS-171, related→LS-168, related→LS-202, related→LS-208, related→LS-96, related←LS-247, related←LS-238, related←LS-234, related←LS-236, related←LS-224, related←LS-166, related←LS-231, related←LS-228, related←LS-220 |

## 描述

## 來源

[LS-208](https://linear.app/little-sprout-app/issue/LS-208/設計-chore小債清倉-2-末張卡完全隱藏裝飾件-17-筆裁決hlxo3-fz3kf-實例年齡片語-nbspcmpcard-diary) 設計 PR #349 merge-review R2（comment `e1e5157f`）與 designer R2 handoff（`d2b78b1e`）：文件 9899 節點時，單次 Pencil `execute` 跑正典腳本的三支 O(n²) 掃描（兄弟交集／跨 parent 碰撞／文字遮蔽）與 tree-hash 間歇 `InternalError: interrupted`。designer 只能手動分段（tree-hash 分 9 段 FNV-1a limb 加總；`cross_parent_collision` 分 13 個 board 批次）再依 Print 彙整段手寫收據 JSON。reviewer 裁定 r5 可接受（tree_hash 對 head、三硬指標 0、六支加總逐欄等於 document_count），但明講兩個盲區：**跨批次配對會漏**（PASS-C 拆 board 子集不是腳本支援的模式）、`tree_hash` **對「沿用舊輪 flagged 清單」是盲的**（r5 三支 O(n²) 的 flagged 與 r4 逐字相同，收據無法證明它是本輪算的）。[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項 `2b5476e1`。

## 範圍

1. `overflow-scan.js`：三支 O(n²) 掃描與 tree-hash 內建分批（依節點索引區間或 board 子集），批次間配對不漏（跨批 pair 由腳本自己補算），最後加總輸出**單一** JSON；分批參數（批大小）有預設、可環境變數覆寫；檔頭說明更新（取代現行 `SCAN_HASH_ONLY`／`SCAN_SKIP_HASH` 的手動流程或與之相容）。
2. 收據 schema：每支掃描附 `result_hash`（該支 `flagged`／`unresolved` canonical JSON 的雜湊）＋頂層 `scan_note` 必填；`design-evidence-check.sh` 新增驗證：(a) 六支 `result_hash` 存在且格式正確；(b) 對 head 快照重算至少一支 O(1)～O(n) 掃描（如 corner_anchor／board_clip）的 `result_hash` 必須相符（fail-closed）；(c) `scan_note` 缺 → 紅。
3. 回歸：對 `design/littlesprout.pen` 現行 head 跑一次完整分批模式，輸出與 [LS-208](https://linear.app/little-sprout-app/issue/LS-208/設計-chore小債清倉-2-末張卡完全隱藏裝飾件-17-筆裁決hlxo3-fz3kf-實例年齡片語-nbspcmpcard-diary) r6 收據六支 document_count 逐欄相符（貼原文）；對一份刻意插入重疊的測試 .pen（`scripts/design/fixtures/` 或既有測試檔）驗跨批 pair 不漏（mutation：把跨批補算拿掉 → 少算，附原文）。
4. `docs/COLLABORATION.md` §7 對照表與 CLAUDE.md 收據句同步（`scan_note`／`result_hash` 為必附項）。

## 不做

Pencil 端 `execute` 逾時本身（第三方）；重跑歷史票收據。

## 驗收

- [ ] 對 9899 節點文件單次命令跑完、無需手動分段；輸出單一 JSON 含六支 `result_hash`＋`scan_note`
- [ ] `design-evidence-check.sh` 對缺 `result_hash`／缺 `scan_note`／雜湊不符三種收據各紅一次（原文）；對 [LS-208](https://linear.app/little-sprout-app/issue/LS-208/設計-chore小債清倉-2-末張卡完全隱藏裝飾件-17-筆裁決hlxo3-fz3kf-實例年齡片語-nbspcmpcard-diary) r6 收據（補雜湊後）綠
- [ ] merge-reviewer 通過（效能：分批後總時間；scope：只動腳本＋gate＋文件）；QA delta（harness 票，以 gate 實跑為證）

## 留言（7）

### 2026-09-12T02:54:00.111Z · Chih-Lin Yeh · `a6214065`

**orchestrator 09-12 10:54**：harness lane 空（LS-228 Done）→ 補位本票。worktree `.claude/worktrees/LS-226`＝`hotfix/LS-226-overflow-scan-batching`（自 main `0f28033`，PR 回 main）。已派 harness 實作 agent：Pencil 停在 LS-208 檔（＝r6 收據目標文件 `b72a0c4`），只讀不切檔；回歸對照 `LS-208-r6-overflow.json`；順手收 COLLABORATION.md 四條池項（`98236da2`／`51bd1635`／`5d6e51b6`／`bab56145`）。→ In Progress。

### 2026-09-12T05:32:08.649Z · Chih-Lin Yeh · `8750b864`

## LS-226 R1 handoff（2026-09-12 13:29 CST）

**Ticket**：LS-226 — 溢出掃描腳本分批模式＋收據 `result_hash`／`scan_note` gate。PR **#358**（`hotfix/LS-226-overflow-scan-batching` → `main`，head `624b11c`，四支 commit：`b3b9891`／`dd42e0f`／`bd9f204`／`624b11c`）。

### 已完成
1. **分批模式＋跨批配對**（`b3b9891`，`scripts/design/overflow-scan.js:31`檔頭 1b、`:199` `DEFAULT_BATCH_SIZE = 20`、`:269` `primaryFilter`、`:753` `defaultBatchSize`（node 環境變數 `SCAN_BATCH_SIZE`）、`:763` `batchRange`、`:1087` Pencil 旗標 `SCAN_BATCH`／`SCAN_BATCH_SIZE`／`SCAN_BATCH_ROOTS`、`:1185` 他批 root `skipChildren`、`:1202` 本批 root scoped 展開走訪、`:1232` `BATCH-JSON`）：一趟 execute 只做一批 root；他批 root 以裸節點入快照，六支只以本批為主節點，root 層 sibling 這類跨批配對由腳本自算。
2. **merge CLI**（`b3b9891`，`overflow-scan.js:789` `mergeBatches`、`:1046` `cli`：`node scripts/design/overflow-scan.js --merge <批檔…> --out <merged.json>`）：驗各批首尾相接蓋滿 `[0, root_count)`／同稿態參數一致，三支 O(n²) 以 class 鍵合併 count、其餘串接／相加，`tree_hash`＝`hash_part` 相加，六支 `result_hash`＝`result_hash_part` 相加＋標頭；最終單一 JSON。
3. **收據形狀＋`result_hash`**（`b3b9891`，`overflow-scan.js:161`／`:166` 檔頭規格、`:924` `IDENTITY`、`:942` `resultHash`、`:946` `withResultHashes`、`:961` `compactScans`、`:1162` 不分批 `RESULT-JSON`、`:1154` `SCAN_SKIP_HASH` 必帶 `SCAN_TREE_HASH`）：三支 O(n²) 只存每類一筆代表（`class`／`count`／`classes`，`document_count` 全量）；六支 `result_hash`＝`scan=`／`scope=`／`tree_hash=` 三行＋in-scope flagged 身分行（corner_anchor 另加 unresolved）FNV-1a 64 加總。
4. **收據 gate**（`dd42e0f`，`scripts/gates/design_tree_hash.py:148` `SCAN_IDENTITY`／`:171` `result_hash`／`:179` `--result-hash` CLI；`scripts/gates/design-evidence-check.sh:275` `RESULT_HASH_MARKER`、`:617` `lacks_result_hash`、`:631` 缺 scan_note、`:641` result_hash 必填、`:652` 重算不符）：(a) 六支 16 碼 hex；(b) corner_anchor／text_occlusion／board_clip 以收據自己的 scan_scope／tree_hash／陣列重算比對，不符即紅（fail-closed）；(c) `scan_note` 必填非空；cutoff 同形、舊收據放行、欄位若在仍驗、PR head 已含標記而最新收據缺欄位紅。
5. **測試＋fixture**（`bd9f204`，`scripts/design/fixtures/cross-batch-snapshot.json`（173 節點／6 root，跨批 root 配對 B1|B2／B2|B3／B4|B5）；`scripts/design/overflow-scan.test.js:908-1096` 九組 LS-226 案；`scripts/gates/design-evidence-check.test.sh:1026-1164` `rh9`／`land9`／`write_receipt9`＋㊼～㊼-l-b 17 條）。
6. **文件**（`624b11c`）：`docs/COLLABORATION.md:262` §7 收據 gate 列 LS-226 段；`CLAUDE.md:12` 收據句；`.claude/agents/ui-designer.md:33`（逾時改分批的步驟）／`:40-42`（模板 `scan_note`／`result_hash`／`class`／`count`）／`:54`（必填說明）；`.claude/agents/visual-reviewer.md:56`（重掃比對加六支 result_hash、逾時改分批）。
7. **LS-96 同檔池項四則**（`624b11c`，一行各一）：`98236da2`（`docs/COLLABORATION.md:225` 不再指涉 scratchpad `LS-224-hibp.sh`，改寫 Management API `PATCH /v1/projects/{ref}/config/auth` body `{"password_hibp_enabled": true}`）、`51bd1635`（`:333` 改「已實測（09-12，`00330bed`）」）、`5d6e51b6`（`:328` R5 插句歸位，「或指令（…）其中一種證據」接回原句）、`bab56145`（`:328` 路徑清單 8→11 項，與 `merge-reviewer.md:25`／`qa.md:43` 同表）。

### 已驗證（怎麼驗）
1. **真實稿回歸（票文第 3 項）**：對 LS-208 head 文件 `/Users/clyeh/little-sprout/.claude/worktrees/LS-208/design/littlesprout.pen`（唯讀 execute，未切檔、未寫入）以 `SCAN_BATCH_ROOTS` 跑 19 段 [0,20) [20,30) [30,35) [35,40) [40,60) [60,80) [80,90) [90,100) [100,110) [110,113) [113,115) [115,120) [120,130) [130,135) [135,145) [145,160) [160,190) [190,210) [210,231)，每批 execute 約 1.5–4 s（TIMING：全稿走訪 0.9–3.0 s＋展開走訪 0.2–1.2 s＋六支 <0.4 s；roots 0–120 為 run-1 完整陣列輸出、120–231 為代表形狀輸出，merge 皆收）；`node scripts/design/overflow-scan.js --merge .claude/evidence/LS-226/r1/batch-*.txt --out .claude/evidence/LS-226/r1/merged.json` exit 0。與 r6 收據 `/Users/clyeh/little-sprout/.claude/worktrees/LS-208/design/evidence/LS-208-r6-overflow.json` 逐欄相同（原文表見 `.claude/evidence/LS-226/r1/regression-vs-r6.md`）：

   | 欄位 | 分批合併 | r6 收據 | 結果 |
   |---|---|---|---|
   | total_nodes | 9899 | 9899 | OK |
   | tree_hash | f958129e31249bd8 | f958129e31249bd8 | OK |
   | sibling_intersection.document_count | 2066 | 2066 | OK |
   | row_overflow.document_count | 560 | 560 | OK |
   | cross_parent_collision.document_count | 321 | 321 | OK |
   | corner_anchor.document_count | 0 | 0 | OK |
   | text_occlusion.document_count | 0 | 0 | OK |
   | board_clip.document_count | 0 | 0 | OK |
   | corner_anchor containers/points/mismatch | 6/48/0 | 6/48/0 | OK |
   | corner_anchor document_containers/points/mismatch | 225/1784/0 | 225/1784/0 | OK |
   | corner_anchor.ref_hits | 746 | 746 | OK |
   | corner_anchor.unresolved | 3（C9eEt, H1HjAP, olUEl）/14 | 3（同）/14 | OK |
   | text_occlusion／board_clip flagged／document | 0/0／0/0 | 0/0／0/0 | OK |
   | scanned_nodes | 16017 | 16017（r6 scan_note） | OK |

   merged SUMMARY 原文：`SUMMARY total_nodes=9899 scanned_nodes=16017 scan_scope=document sibling_intersection=2066 row_overflow=560 cross_parent_collision=321(bleed-only) text_occlusion=0/0 board_clip=0/0 corner_anchor=6/48/0 document=225/1784/0 ref_hits=746(defs=10) unresolved=3/14 boards=[…21 板…] tree_hash=f958129e31249bd8`。逾時（`InternalError: interrupted`）處置實錄：[120,140)→切 [120,130)；[130,160)→[130,140) 再逾時→[130,135)；[190,231)→[190,210)；其餘一次成功（各批 `.claude/evidence/LS-226/r1/batch-*.txt` 含 TIMING 行）。
2. **跨批 fixture＋mutation（票文第 3 項）**：`node scripts/design/overflow-scan.test.js` → `overflow-scan.test.js：全數通過（51 組）`。「LS-226 分批＝不分批」：size 1／2／3／6、Pencil 裸 root 快照、代表形狀各批、scope=boards、不等寬 `{roots}` 分批 merge 與 `compactScans(scanAll(全稿))` 逐位元相同；「LS-226 mutation」：拿掉他批裸 root → sibling 88→85（少 B1|B2／B2|B3／B4|B5 三筆）、sibling result_hash 變、其餘五支不變（輸出附 `.claude/evidence/LS-226/r1/mutation-cross-batch.txt`：`batched + bare foreign roots (正式): sibling=88 … root-pairs=["B1|B2","B2|B3","B4|B5"]`／`batched WITHOUT foreign roots (mutant): sibling=85 … root-pairs=[]`）。
3. **result_hash js／py 同值**：`overflow-scan.test.js` 「LS-226 result_hash」六支 js＝`python3 scripts/gates/design_tree_hash.py --result-hash <receipt> <scan>`；merged.json 三支 py 重算＝JS＝收據值（corner_anchor `5d76c9c4665dded8`、text_occlusion `988da7adbb0604be`、board_clip `dbdca9df7093f63c`）；代表形狀的 sibling py 重算（`eb05a52a421c344b`）≠ 全量（`a2f10c3b7a582a8c`），證明三支 O(n²) 只能同稿態重跑比對。
4. **收據 gate 自測**：`bash scripts/gates/design-evidence-check.test.sh` → `design-evidence-check.test.sh：全數通過`（新增 ㊼～㊼-l-b 17 條：正確綠／boards 綠／缺 result_hash／格式壞／unresolved 事後改過／hash 綁別的 tree_hash／scope 不同／缺或空白 scan_note 紅／cutoff 前舊形狀綠＋放行行、hash 在且錯仍紅／PR head 已含標記紅→補齊綠）。
5. **既有收據不受影響**：以本分支 `scripts/gates/design-evidence-check.sh design/littlesprout.pen --ticket LS-208 --base origin/development` 在 LS-208 worktree 實跑，r1–r6 六份全部 `✓ design-evidence gate 通過`＋「尚無 result_hash……舊收據放行」行。
6. **其他 gate**：`bash scripts/gates/agent-tools-check.sh`（38 條釘句綠）、`bash scripts/gates/brand-skill-check.sh` 綠、`node --check scripts/design/overflow-scan.js`、`bash -n scripts/gates/design-evidence-check.sh`、`python3 -m py_compile scripts/gates/design_tree_hash.py` 綠；push gate 綠（4 commits、與 origin/main 0f28033 可乾淨合併）；`bash scripts/gates/pr-body-check.sh <body> --branch hotfix/LS-226-overflow-scan-batching --verify` exit 0（本機無 LINEAR_API_KEY 反查略過，CI 再驗）。

### 未完成
- reviewer informational 處置：R1 首交，尚無 merge-review 意見（待 #358 審後逐條列「已修／記入 LS-96／另票／不修＋理由」）。
- 不在範圍（票文明列）：Pencil execute 逾時本身、重跑歷史收據。
- CI 結果未等（依派工「push 後即 handoff」）：#358 的 rules job（overflow-scan.test.js／design-evidence-check.test.sh／pr-body-check --verify 反查四個 LS-96 comment id）由 orchestrator 看。

### 風險
- **LS-208 PR #349（in-flight）**：r6 收據無 result_hash／scan_note，現在 cutoff 前放行；若該分支之後把 development（back-merge 本 hotfix 後）併進來，PR head tree 含 `result_hash` 標記，最新收據 r6 會被要求補欄位（同 LS-168 R1 N1 既有規則）——用新腳本對同稿態分批重跑（19 段約 1 分鐘）並照抄 merged 輸出即可。
- 收據形狀變更（三支 O(n²) 只存代表＋`class`／`count`）：gate 本來就不驗這三支 flagged 長度與 document_count 的關係（LS-202）；ui-designer／visual-reviewer 模板已同步，但 in-flight 設計 agent 的 prompt 若沿用舊模板會缺 `result_hash`／`scan_note`（cutoff 前放行，併入新腳本後才要求）。
- 三支 O(n²) 的 result_hash CI 不能重算（收據只存代表）——只證明「同稿態重跑必同值」，複驗留給 visual-reviewer；分批各批同一稿態機械上只由 tree_hash 兜底（批間寫入又改回同一棵樹看不到，同 LS-171 R1 N2）。兩者已記於 COLLABORATION §7 盲區。
- 分批每次 execute 的固定成本是全稿未展開走訪（0.9–3.0 s），逾時仍會間歇發生；處置＝原樣重試一次、再切小 `SCAN_BATCH_ROOTS`（已寫進檔頭 1b 與 ui-designer.md）。

### 產出位置
- PR #358 https://github.com/CLYEH/little-sprout/pull/358（head `624b11c`）；worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-226`。
- 程式：`scripts/design/overflow-scan.js`、`scripts/gates/design_tree_hash.py`、`scripts/gates/design-evidence-check.sh`；測試：`scripts/design/overflow-scan.test.js`、`scripts/gates/design-evidence-check.test.sh`、`scripts/design/fixtures/cross-batch-snapshot.json`；文件：`docs/COLLABORATION.md`、`CLAUDE.md`、`.claude/agents/ui-designer.md`、`.claude/agents/visual-reviewer.md`。
- 證據（gitignored）：`/Users/clyeh/little-sprout/.claude/worktrees/LS-226/.claude/evidence/LS-226/r1/`（`batch-*.txt` 19 段原文、`merged.json`、`merge-stderr.txt`、`regression-vs-r6.md`、`mutation-cross-batch.txt`）；scratchpad `LS-226-pr-body.md`／`LS-226-r1-handoff.md`／`LS-226-batch-equiv.js`／`LS-226-make-fixture.js`。

### 2026-09-12T05:34:11.004Z · Chih-Lin Yeh · `1401cca6`

**orchestrator 09-12 13:3x**：R1 handoff `8750b864`——PR #358 head `624b11c`（4 commit）；對 LS-208 head 文件 19 批 execute＋`--merge`，六支 document_count／tree_hash 與 r6 收據逐欄相符（`.claude/evidence/LS-226/r1/regression-vs-r6.md`）；收據三支 O(n²) 改「每類一代表」形狀；`result_hash` FNV-sum、gate 對三支 O(1)～O(n) 重算比對；自測 42→51＋gate 測試 +17；順手收 COLLABORATION 四條池項。風險：LS-208 r6 收據無 `result_hash`／`scan_note`，cutoff 放行與否需 reviewer 明確結論。→ In Review，已派 merge-reviewer R1。

### 2026-09-12T05:57:38.981Z · Chih-Lin Yeh · `8986ebf8`

## LS-226 merge-review R1 — **APPROVE**（2026-09-12 13:52 CST）

- PR **#358** `hotfix/LS-226-overflow-scan-batching` → `main`；審的 head **`624b11ca1648c697935e71e65f959acd77d67bbc`**（4 commit `b3b9891`／`dd42e0f`／`bd9f204`／`624b11c`）；`git diff main...624b11c` 10 檔 +1068/−118。
- CI 於本 head 五項全綠（rules／db／lint／ci／ci-ipad）；`mergeStateStatus=BLOCKED` 僅缺本裁決。
- Supabase lock：本票不需，**未持有**。模擬器：不適用（無 iOS 變更）。
- Pencil 抽樣重放**未做**：`mcp__pencil__*` 未對本 agent 開放（`get_app_state` 回 `No such tool available`）——改以更強的離線替代證據（見查實 5）。

### 逐條查實（對派工單 1–7）

1. **跨批配對不漏（派工 1）**——成立。讀 `scripts/design/overflow-scan.js` 後確認只有 root 層 sibling 會跨批：`scanCrossParentCollision`／`scanTextOcclusion` 以 board 分群並 `primaryRoots.has(board)` 早退、`scanCornerAnchor` 以容器（root 內）為單位、`scanBoardClip` 以 root 為單位，四者結構上不跨板；`scanSiblingIntersection` 對每個主節點 a 只配對同父且順序在後的 b，故每對恰由「順序較前那側所在的批」算一次，他批 root 以裸節點入快照供配對。**自己的獨立驗證**（不採信實作者測試）：另寫 fuzz `scratchpad/LS-226-fuzz-batch.js`，300 棵隨機樹 × 3 組旗標（預設／crossAll／boards）× 5 種批大小（1／2／3／5／全）＝**4500 組**比對「各批 merge」與「不分批 compactScans」，六支逐位元 mismatch **0**、六支 result_hash 亦相符，過程共觸發 3967 個 root 層（跨批候選）配對。實作者自測 `node scripts/design/overflow-scan.test.js` → `全數通過（51 組）`，我實跑同綠。
2. **mutation 自行重放（LS-209 規約）**——申報屬實。照 `.claude/evidence/LS-226/r1/mutation-cross-batch.txt` 申報的「拿掉他批裸 root」自己重放：不分批 sibling=**88**、正式分批（有裸 root）=**88**（root-pairs `B1|B2`／`B2|B3`／`B4|B5`），mutant（每批只有自己 root）=**85**、root-pairs 空、row 32／cross 6 不變——與申報數字逐字相符，且紅的是預期的少算、不是 crash。另確認 fixture 真的含跨批重疊（`scripts/design/fixtures/cross-batch-snapshot.json`：6 root／173 節點，三對 root 層相鄰板）。
3. **收據形狀變更與 cutoff（派工 2）**——方向 fail-closed 正確，cutoff 界線是**「`scripts/design/overflow-scan.js` 在該收據 `head_sha` 的 tree（輪次最高者另看 PR head 的 tree）裡含字面 `result_hash`」**（`scripts/gates/design-evidence-check.sh` RESULT_HASH_MARKER），與 LS-168／185／202／207 同形、不是日期。實測 `git show main:scripts/design/overflow-scan.js` 該字面出現 **0** 次、新版 22 次，界線乾淨。
   - **(a) #349 現況直接 merge 會不會紅 → 不會紅。** 在 LS-208 worktree 對 `b72a0c4` 以**本分支的新 gate** 實跑 `bash scripts/gates/design-evidence-check.sh design/littlesprout.pen --ticket LS-208 --base origin/development --head-sha b72a0c4…`：r1–r6 六份全部 `✓ design-evidence gate 通過`＋「尚無 result_hash……舊收據放行」行，exit 0。關鍵是 cutoff 讀的是 **PR head commit 的 tree**，不是 CI 的 `refs/pull/N/merge` 工作區——所以 LS-226 back-merge 進 development 這件事本身不會讓 #349 轉紅。
   - **只有在 #349 分支自己把 development 併進來（或 rebase）之後才會紅**：在丟棄式 clone 裡做了忠實模擬（development＋back-merge LS-226 → 再併進 `b72a0c4`），對合成 head 跑同一支 gate → **r6 紅 7 條**（「PR head 的 tree 已含」＋六支 `result_hash` 必填），r1–r5 仍放行綠。
   - **(b) 若使用者選需改字的選項、再出 r7 → 需新格式（且 r6 自動免死）。** 同一個 clone 裡把 `.claude/evidence/LS-226/r1/merged.json` 照抄成 `design/evidence/LS-208-r7-overflow.json`（補 ticket／round／`head_sha=ed39d7c…`／`scan_note`／每筆 classification）並 commit → gate 對 r7 綠（訊息含「六支各帶 result_hash（corner_anchor／text_occlusion／board_clip 重算相符）、scan_note」），**同時 r6 因不再是輪次最高而回到放行綠**。結論：r7 一旦以新格式產出，r6 不必重生；反之若 r7 仍沿用舊腳本（分支未取得新腳本）則機械上仍會放行，這是規約已寫死、gate 不強制的缺口（見 informational i5）。
4. **`result_hash` 規格與 js／py 一致（派工 3）**——canonical 規則寫死在 `scripts/design/overflow-scan.js` 檔頭「result_hash（LS-226…）」段與 `scripts/gates/design_tree_hash.py` 檔頭，兩份逐字同義。**自己實跑 parity**：對 `.claude/evidence/LS-226/r1/merged.json` 六支各跑 `python3 scripts/gates/design_tree_hash.py --result-hash <收據> <支名>`，corner_anchor `5d76c9c4665dded8`／text_occlusion `988da7adbb0604be`／board_clip `dbdca9df7093f63c` 三支 py＝js＝收據值；三支 O(n²) py 重算（代表形狀）≠ 收據值（全量），與檔頭「CI 不能重算、只驗格式」的宣稱一致。**殘餘盲區**：三支 O(n²) 沿用舊輪 flagged 仍完全偵測不到（只驗 16 碼 hex 格式），這一點已明寫在 `docs/COLLABORATION.md` §7 LS-226 段盲區句與腳本檔頭；未被點名的是 `document_flagged`／`document_unresolved` 不在雜湊內（見 i4）。
5. **回歸表與效能（派工 5）**——以離線替代證據查實，強度高於抽樣重放：(i) 我自己重跑 `node scripts/design/overflow-scan.js --merge .claude/evidence/LS-226/r1/batch-*.txt`（19 檔），輸出與實作者 `.claude/evidence/LS-226/r1/merged.json` **逐位元相同**（exit 0）；(ii) 19 批 Pencil `hash_part` 加總得 `tree_hash=f958129e31249bd8`，而 `python3 scripts/gates/design_tree_hash.py` 對 `git show ed39d7c…:design/littlesprout.pen` 這份真實稿算出**同值** `f958129e31249bd8`——獨立 python 全樹實作交叉證明各批確實對同一份真實文件樹、加總法正確，也順帶證明 scoped `Get(rootId)` 的 bounds 與全稿走訪一致；(iii) `design/evidence/LS-208-r6-overflow.json` 六支 `document_count` 2066／560／321／0／0／0、corner 6/48/0、document 225/1784/0、`ref_hits=746`、`unresolved` 3、`total_nodes` 9899 與 merged 逐欄相符（`.claude/evidence/LS-226/r1/regression-vs-r6.md` 原文一致）。效能：各批 `TIMING` 原文顯示固定成本 `roots_hash_walk` 0.918–2.973 s、`snapshot_walk` 0.076–1.239 s、六支合計 <0.5 s，19 批 execute 純運算約 60–75 s（19 段 `roots_hash_walk` 合計 ≈33.8 s 是 O(K·N) 的重複走訪代價，見 i6）；批大小預設 `DEFAULT_BATCH_SIZE=20`、`SCAN_BATCH_SIZE` 於 Pencil 端全域／node 端環境變數覆寫，非法值 throw（`overflow-scan.test.js` 的「LS-226 batchRange」組我實跑綠）。
6. **`SCAN_SKIP_HASH` 改為必帶 `SCAN_TREE_HASH`（派工 4）**——無破壞。`git grep` 全庫：`SCAN_SKIP_HASH` 除 `scripts/design/overflow-scan.js` 與其自測外**零引用**；designer／VR 工作流實際用的是 `SCAN_HASH_ONLY`（`scripts/ops/pen-open.sh:103/241`、`scripts/ops/pen-read.sh:33`），該路徑本次未改語意、`SUMMARY-HASH` 原樣。`.claude/agents/ui-designer.md`（分批步驟＋收據模板＋必填說明）、`.claude/agents/visual-reviewer.md`（重掃比對加六支 result_hash、逾時改分批）、`CLAUDE.md` 收據句、`docs/COLLABORATION.md` §7 皆已同步；兩 agent 無 `tools:` 白名單＝有 Bash／寫檔，`--merge` 路徑可用。
7. **順手池項（派工 6）**——逐條對照 LS-96 原文：`98236da2`(2) COLLABORATION HIBP 行改為 Management API body 敘述 ✓（(1) 的 `supabase/tests/60_default_privileges.sql` 句已不存在該檔，僅留在不可變 migration 註解，無遺留）；`51bd1635` §7「效果」欄改「已實測（09-12，`00330bed`）」✓；`5d6e51b6`(1) R5 插句歸位、「或指令（…）其中一種證據」接回原句 ✓；`bab56145`(1) 路徑清單 8→11 項 ✓ 且與 `scripts/gates/handoff_evidence_check.py:152` `PATH_RE` 實際 11 項相符。**未做的子項見 i7。**
8. **handoff 查實＋scope（派工 7）**——handoff `8750b864` 引用的 24 個行號我用 `git show 624b11c:<檔>` 逐一核對（`overflow-scan.js` 的 31／161／166／199／269／753／763／789／924／942／946／961／1046／1087／1154／1162／1185／1202／1232、`design_tree_hash.py` 148／171／179、`design-evidence-check.sh` 275／617／631／641／652、`docs/COLLABORATION.md` 225／262／328／333、`CLAUDE.md` 12、`.claude/agents/ui-designer.md` 33／40／54、`.claude/agents/visual-reviewer.md` 56）**全部落在申報的位置**，無虛引。Scope：10 檔全在「腳本＋gate＋測試＋文件」範圍內，`git diff --name-status` 無歷史收據重跑、無無關重構；hotfix 自 main `0f28033` 切、PR 回 `main` 正確。另 `git show main:` 對跑確認三支 O(n²) 的 flagged **集合**與舊版完全相同（fixture 三支各「舊有新無 0／新有舊無 0」；真實稿 2066/560/321 與 r6 同），排除了改寫走訪順序造成漏報的風險。
9. **四維度**：race condition 不適用（純 node／Pencil 單執行緒腳本，無並發狀態——全檔無 async／Promise／worker，`git grep` 於 `scripts/design/` 零命中）；平行優化不適用（Pencil 單一 app、每批一次 MCP execute，平行化會讓各批稿態失去同一性）；運算效能以 `.claude/evidence/LS-226/r1/regression-vs-r6.md` 的 TIMING 逐批數字＋我自己的 `scratchpad/LS-226-fuzz-batch.js` 量測為據（`buildIndex` 多出 `pos`／`slot` 兩張 O(n) map，`primaryFilter` 用預算好的 chain、O(1)；`fnv1a64` 改寫見 i1）。

### Findings

**blocker：0。major：0。**

**minor-1（收據新欄位有免費的自洽檢查沒做）**：`scripts/gates/design-evidence-check.sh` 對三支 O(n²) 只驗 `result_hash` 是 16 碼 hex，完全沒驗新欄位之間的關係。實測不變量在真實輸出成立（`.claude/evidence/LS-226/r1/merged.json`：`sum(count)==document_count` 2066／560／321、`classes==len(flagged)` 294／58／183）。**具體失敗情境**：我在丟棄 clone 裡把 r7 的代表從 294/58/183 裁成 29/22/8、`classes` 仍留 294、`result_hash` 照抄全量值 → gate 仍 **綠**。於是同一份稿可以產出兩種都合法、讀者分不出來的收據（照抄全量 vs 裁剪），且手改 `count`／`document_count` 也沒人擋。建議：cutoff 下（欄位存在才驗，同形手續）補 `sum(count) == document_count` 與 `classes == len(flagged)` 兩條，`scripts/gates/design-evidence-check.test.sh` 各補一條負樣本；若「允許裁剪」才是政策，則改為在 `.claude/agents/ui-designer.md` 明寫。

**minor-2（`classification` 逐筆必填 × 新代表形狀的工作量政策未定）**：`design-evidence-check.sh:485-487` 要求三支 O(n²) `flagged` 每一筆都有非空 `classification`，而 `.claude/agents/ui-designer.md` 新增句寫「收據＝merged.json 照抄＋補…每筆代表的 classification」。**具體失敗情境**：LS-208 head 這種稿的 merged.json 有 **535 筆代表**（294+58+183，我實測填滿才綠），而 r6 實務只寫 29/22/8——第一個照字面做的 designer 要手寫 535 條分類（必然 slop），裁剪的人則與「照抄」字面衝突。建議補一句政策：允許只保留本輪要交代的類（其餘略去、`classes`／`document_count` 仍記全量），或反之明令全列。

### Informational（逐條編號，請於下一份 handoff 的「未完成」欄各給處置）

- **i1**：`fnv1a64` 從四 limb＋位元組陣列改寫成 hi/lo 就地編碼（`overflow-scan.js:650`）——這是 tree_hash 的核心原語、影響**所有**收據，屬票文範圍外但為分批效能所必需。我另做 3173 筆 fuzz（ASCII／CJK／2–4 byte／astral 代理對／孤立 surrogate／tab／真實 `canonNode` 行）與未修改的 `scripts/gates/design_tree_hash.py` 交叉比對，mismatch **0**；已足夠，只記錄。
- **i2**：`scanSiblingIntersection` 走訪順序由「逐 parent 雙迴圈」改為「主節點 pre-order」——集合不變（查實 8 已證），但同 count 類的列序與每類代表的 `e.g.` id 可能與舊輪收據不同（fixture 實測第一處差異即為兩個 count=1 類互換），跨輪文字 diff 會有噪音。建議 VR 只比 SUMMARY 數字與 `result_hash`，不比代表 id。
- **i3**：`python3 scripts/gates/design_tree_hash.py --result-hash <收據> sibling_intersection` 對代表形狀會**靜默印出一個無法比對的值**（實測 `eb05a52a421c344b` vs 收據 `a2f10c3b7a582a8c`）。建議 `classes` 欄位存在時往 stderr 印一行「代表形狀，此值不可與收據比對」，免得下一個人把正常現象當造假。
- **i4**：`result_hash` 只涵蓋 in-scope `flagged`／`unresolved`；`document_flagged`／`document_unresolved` 不在雜湊內，沿用舊輪的 `document_*` 陣列仍是盲的。`docs/COLLABORATION.md` §7 盲區句只點名了「三支 O(n²) 不能重算」與「批間同稿態」，建議補上這一項。
- **i5**：cutoff 綁「PR head tree 是否含新腳本」的代價是：分支不更新就永遠適用舊格式（查實 3(b) 實證），而 `CLAUDE.md`／`docs/COLLABORATION.md` 已把 `result_hash`／`scan_note` 寫成必附項——規約嚴於 gate。這是既有 cutoff 手續的固有性質（LS-168 起同形），不要求本票改；但 LS-208 若出 r7，orchestrator 派工時要明示「分支先取得新腳本、用分批模式重跑」。
- **i6**：分批的固定成本是每批都重走一次全稿未展開走訪（`TIMING roots_hash_walk` 0.918–2.973 s × 19 ≈ 33.8 s，佔總運算時間過半），批大小調大可省但逾時風險上升（本次 19 批中有 4 段是逾時後再切小的）。若日後仍嫌慢，可評估「root AABB 與 tree_hash 各自一趟、AABB 那趟全 `skipChildren`」；純記錄，本票不要求。
- **i7**：LS-96 池項只做了各自的 (1)／(2) 子項，**請勿整條劃掉**：`bab56145`(2)「`scripts/gates/handoff-evidence-check.test.sh:666` 註解仍寫『.sh/.md/.yml 已在 R4 移出 PATH_RE』」我實測仍在；`5d6e51b6`(2)（`is_mutation_context` 整行判定，P2 size:S）與 (3)（`xcrun simctl` 誤判建議升獨立票）仍在池。
- **i8**：`mergeBatches` 的 `same("flags")` 只比對布林 `custom_overlay_re`，兩批用**不同** `SCAN_OVERLAY_RE` 字串仍會通過（fail-open）。影響極小（覆寫罕用、且是 designer 自己的 snippet），建議改存 regex 原文再比對，或記入 LS-96。
- **i9**：`scripts/gates/design_tree_hash.py` 的 `_s(None)` 回 `"undefined"`；JSON 明確 `null` 在 js 側會得 `"null"`、py 側得 `"undefined"`，理論上 js／py 不一致。現行身分欄位（node id／axis／overlay）不可能是 null，**PLAUSIBLE 且無實例**，僅記錄。

### Verdict

**APPROVE** — 無 blocker、無 major。跨批配對經 4500 組 fuzz 與 mutation 重放證明不漏；真實稿回歸由「19 批 hash_part 加總 == 獨立 python 全樹 tree_hash」與 r6 逐欄相符雙向鎖住；cutoff 兩個方向（不更新分支→放行、更新分支→r6 紅、出 r7→r6 自動免死）皆已實跑確認。兩條 minor 屬「該補的免費檢查」與「該寫明的政策」，不阻擋合併，請在 back-merge 前後以一支 follow-up 或 LS-96 池項處置。

---

`bash scripts/gates/handoff-evidence-check.sh <本 verdict 暫存檔>` → **exit 0**：

```
✓ 第 10 行起的列項有證據
✓ 第 11 行起的列項有證據
✓ 第 12 行起的列項有證據
✓ 第 16 行起的列項有證據
✓ 第 17 行起的列項有證據
✓ 第 18 行起的列項有證據
✓ 第 19 行起的列項有證據
✓ 第 20 行起的列項有證據
✓ 第 21 行起的列項有證據
```

### 2026-09-12T05:59:36.655Z · Chih-Lin Yeh · `5c318fda`

**orchestrator 09-12 13:58**：merge-review R1 `8986ebf8` **APPROVE**（0 major／2 minor／9 info；4500 組 fuzz 跨批不漏、mutation 88→85 屬實、`fnv1a64` 對 python 參考 mismatch 0、`--merge` 與實作者輸出逐位元相同；**LS-208 結論：(a) #349 現況直接併入不會紅（cutoff 讀 PR head tree，r1–r6 放行綠）；(b) 若再出 r7 必用新格式、r6 自動免死**）。#358 已併 main **`bf6a15a`**（main tip status 54036802596），主 checkout 已 pull。minor-1（O(n²) 新欄位自洽不變量未驗）／minor-2（逐筆 classification 政策與 ui-designer.md 句不一致）→ 池 LS-96 `8647e2d8`。back-merge PR main→development 已開，排在 LS-230 #357 併入之後合併（避免 dev tip 連跳）；之後 sweeper → 收尾儀式 → Done。reviewer 註：merge-reviewer 白名單無 `mcp__pencil__*`，抽樣重放改離線證據。

### 2026-09-12T06:09:38.769Z · Chih-Lin Yeh · `78025a0e`

## dead-code sweep — LS-226

**範圍**：`git log --oneline --grep=LS-226 origin/main` 4 commit `b3b9891`／`dd42e0f`／`bd9f204`／`624b11c`（PR #358，已併 main `bf6a15a`）。逐檔讀 `git show --stat`＋`git diff <commit>^..<commit>` 全文，比對 base `origin/main` 舊版差異；巡了 `scripts/design/overflow-scan.js`（1242 行全讀）、`scripts/gates/design_tree_hash.py`（216 行全讀）、`scripts/gates/design-evidence-check.sh`（LS-226 段 diff）、`scripts/design/overflow-scan.test.js`（新增 226 行）、`scripts/gates/design-evidence-check.test.sh`（新增 151 行）、`scripts/design/fixtures/cross-batch-snapshot.json`、`docs/COLLABORATION.md`／`CLAUDE.md`／`.claude/agents/ui-designer.md`／`.claude/agents/visual-reviewer.md`／`.claude/agents/qa.md` 的 LS-226 相關段落。**主 checkout 唯讀，未動任何檔案。**

### Findings（本 feature 引入，1 項）

**F1（文件殘留，size XS）**：`.claude/agents/ui-designer.md:33` 描述 LS-171 兩趟舊法的句子「第二次加 `SCAN_SKIP_HASH = true` 跑六支（SUMMARY 的 `tree_hash=skipped`）」與現行程式碼行為不符，且未帶 LS-226 新增的 `SCAN_TREE_HASH` 必填規定。

- **原文**（624b11c 併入後仍存在）：`……8000 節點級的稿一次 execute 跑完雜湊＋六支會 InternalError: interrupted（LS-152 VR R3 實測……）：**拆兩次**——第一次 snippet 第一行加 SCAN_HASH_ONLY = true（只印 SUMMARY-HASH total_nodes=… tree_hash=…），第二次加 SCAN_SKIP_HASH = true 跑六支（SUMMARY 的 tree_hash=skipped）；兩次之間不得有任何寫入……`
- **證據**：`grep -n "skipped" scripts/design/overflow-scan.js` → 0 命中（現行腳本不再有任何路徑印出字面 `skipped`）；`git grep -n "tree_hash=skipped"` 全庫只命中這一處程式文件（另兩處在 `design/evidence/LS-194-r1/r2-overflow.json` 是歷史收據原文，不算文件敘述）。現行行為：`SCAN_SKIP_HASH` 沒同時帶合法 `SCAN_TREE_HASH` 會直接 `throw`（`overflow-scan.js:1154`），帶了則 `out.tree_hash = givenHash`（不是字面 `"skipped"`）。
- **矛盾點**：`git diff 624b11c^..624b11c -- .claude/agents/ui-designer.md` 顯示本次改動在**同一段**稍早處插入了正確的新句「LS-171 的 `SCAN_HASH_ONLY`／`SCAN_SKIP_HASH` 兩趟舊法仍可用但第二趟必帶 `SCAN_TREE_HASH = "<第一趟的 tree_hash>"`，**見下**」，但「見下」指向的正是這段未同步更新的舊敘述——同一段文字內部自相矛盾（前面說必帶 `SCAN_TREE_HASH`，後面的操作說明完全沒提、且宣稱的輸出已不存在）。
- **比對**：`docs/COLLABORATION.md` 的 LS-171 段（未被本次改動觸碰）只寫「第二趟 `SCAN_SKIP_HASH` 跑五支」、未宣稱 `tree_hash=skipped` 字面，故無此問題；`visual-reviewer.md:56` 對舊法的描述也只有一句「第二趟必帶 `SCAN_TREE_HASH`，見 ui-designer.md」，把細節指向的正是這處已過時的說明。
- **風險**：純文件（不影響任何 gate/CI），但屬於「見下」forward-reference 自打臉——下一個照 ui-designer.md 操作舊法分段的 agent 可能誤以為不用帶 `SCAN_TREE_HASH`（實際會被腳本 `throw` 擋下，機械上不會真的產出錯誤收據，僅浪費一次 execute）。
- **建議處置**：小幅改字（把該句改成「第二次加 `SCAN_SKIP_HASH = true`＋`SCAN_TREE_HASH = "<第一趟 tree_hash>"` 跑六支（沒帶會 throw；SUMMARY 印的 `tree_hash` 就是抄入的第一趟值）」），或乾脆把整段兩趟舊法從 ui-designer.md 主要操作步驟移除、只保留「LS-226 起改用分批模式，舊法見 `overflow-scan.js` 檔頭『大稿分段跑法（歷史記錄）』」——因為腳本自己的檔頭已經把兩趟舊法降級標成「歷史記錄，LS-226 起一律改用分批模式」，ui-designer.md 卻仍把它當成主要說明详細展開，兩處定位不一致。建議**併入下一張碰 ui-designer.md 的 harness 票順手改**（如 LS-96 池項慣例），不需要單獨開票。

### 未發現的部分（逐項交代排查過程，避免漏報假設）

1. **LS-171/185 舊手動流程殘留**：`SCAN_HASH_ONLY`／`SCAN_SKIP_HASH` 全庫呼叫端／文件引用逐一核對（`git grep -c`）：`SCAN_HASH_ONLY` 在 `scripts/ops/pen-open.sh`（6 處，`--force-reload` 新鮮度比對用）、`scripts/ops/pen-read.sh`（1 處）仍是**活躍且與 LS-226 六支批次掃描無關**的獨立用途（純雜湊新鮮度檢查），不是死碼；`SCAN_SKIP_HASH` 除 `overflow-scan.js` 自身與其自測外零呼叫端，但這是**設計上的 Pencil 全域旗標**（靠 snippet 文字設定，本來就不會出現在其他檔案的「呼叫」語法裡），且與 `SCAN_BATCH`／`SCAN_BATCH_ROOTS` 有明確互斥檢查（`overflow-scan.js:1089`：`batchSpec && (hashOnly || skipHash)` throw），未見死分支——两条路径在 `if (!batchSpec) { … }` 与 `else { … }`（分批）间干净切开，皆可达。
2. **js／py 重複實作是否有一份無呼叫端**：`fnv1a64`／`canon`／`result_hash`／`result_hash_lines` 在 js（`overflow-scan.js`）與 py（`design_tree_hash.py`）各自都有實際呼叫端（js 側被 `scanAll`／`mergeBatches`／Pencil 執行區塊使用；py 側被 `design-evidence-check.sh`（`design_tree_hash.result_hash(...)`，`:358`／`:679`）與其自身 CLI `--result-hash` 使用，且被 `overflow-scan.test.js`「LS-226 result_hash」案 `execFileSync python3 design_tree_hash.py --result-hash` 交叉驗證）——兩份是刻意的跨語言一致性驗證（js 產生、py 重算比對），不是重複遺留，檔頭已註明「必須逐位元同值」。
3. **`scripts/design/fixtures/` 新 fixture**：目錄下只有 `cross-batch-snapshot.json` 一份，確認被 `overflow-scan.test.js:885`（`FIX = JSON.parse(readFileSync(...cross-batch-snapshot.json))`）實際載入使用，貫穿全部 LS-226 分批／merge／result_hash 測試案；無孤兒 fixture。
4. **gate 自測 +17（㊼～㊼-l-b）**：讀完 `design-evidence-check.test.sh` 新增的 `rh9()`／`land9()`／`write_receipt9()` 三個 helper，逐一確認每次呼叫後都緊接 `expect` 斷言（無「產生但沒人驗」的收據檔）；且這些收據檔只存在於測試腳本內部建立的**拋棄式 git sandbox**（`$R`，非本 repo），`find design/evidence -iname "*LS-67*"` 與 `git status --porcelain` 確認正式 repo 沒有殘留。自己實跑 `bash scripts/gates/design-evidence-check.test.sh` 全數通過（含新增 17 條）。
5. **文件一致性（收據句／pen-read 描述）**：`CLAUDE.md:12`、`docs/COLLABORATION.md:262`、`.claude/agents/ui-designer.md:33/40-42/54`、`.claude/agents/visual-reviewer.md:56` 對「六支 `result_hash`＋頂層 `scan_note` 必填、CI 對 corner_anchor／text_occlusion／board_clip 重算比對」的敘述**除 F1 那句歷史殘留外一致**；`.claude/agents/qa.md` 未描述收據 schema（QA 只做 `pen-read.sh` 新鮮度檢查、不寫收據，本來就不需要涵蓋此段，非疏漏）；`qa.md:10` 與 `visual-reviewer.md:11` 的 `pen-read.sh`／`SCAN_HASH_ONLY` freshness-check 描述文字實質相同（此段非 LS-226 觸碰範圍，屬 LS-180 既有內容，僅順帶核對一致性）。
6. **LS-96 四條池項逐條核對**（對照 `mcp__linear__list_comments` LS-96 原文）：
   - `98236da2`：子項 (2) `docs/COLLABORATION.md:225` 已改寫成 Management API `PATCH /v1/projects/{ref}/config/auth` body 敘述，**已入池且已收**；子項 (1)（`supabase/tests/60_default_privileges.sql` 的「16 張表」句）核實**該檔已不含此句**（`grep -n enforce_not_suspended supabase/tests/60_default_privileges.sql` 命中的兩處都與「16 張表」用語無關，該句只殘留在不可變 migration `20260906123430` 註解內，依 PLAN 不可改）——與 624b11c commit message 描述一致，非遺漏。
   - `51bd1635`：`docs/COLLABORATION.md:333` 尾句確認已改為「已實測（09-12，`00330bed`）」，**已入池且已收**。
   - `5d6e51b6`：子項 (1)（COLLABORATION.md:328 R5 插句歸位）核實已接回原句，**已入池且已收**；子項 (2)（`is_mutation_context` 整行判定）與 (3)（`xcrun simctl` 誤判建議升獨立票）**確認仍在池未動**——與 merge-review R1 informational i7 的判定一致，非本次遺漏（票文與 commit message 本來就只認領子項 1）。
   - `bab56145`：子項 (1)（COLLABORATION.md:328 路徑清單 8→11 項）核實與 `merge-reviewer.md:25`／`qa.md:43`／`scripts/gates/handoff_evidence_check.py:152` 的 `PATH_RE` 逐項相符（`.png`／`.log`／`.test.sh`／`scratchpad/`／`evidence/`／`.swift`／`.py`／`.sh`／`.md`／`.yml`／`.json` 11 項），**已入池且已收**；子項 (2)（`handoff-evidence-check.test.sh:667` 過時註解）**確認仍在池未動**，與 merge-review i7 一致。
7. **`design/evidence/` 既有收據**：`git show --stat` 四支 commit 對 `design/evidence/` 路徑零改動；`git status --porcelain` 確認工作區乾淨——未做（也不需要）重跑歷史收據。

### 自測執行紀錄
- `node scripts/design/overflow-scan.js`（node module 自測）：`node scripts/design/overflow-scan.test.js` → `overflow-scan.test.js：全數通過（51 組）`
- `bash scripts/gates/design-evidence-check.test.sh` → `design-evidence-check.test.sh：全數通過`（含 ㊼～㊼-l-b 17 條）
- `python3 -m py_compile scripts/gates/design_tree_hash.py`、`node --check scripts/design/overflow-scan.js`、`bash -n scripts/gates/design-evidence-check.sh` 三支語法檢查全過

### 盲區聲明
- 純文字／grep 交叉比對抓不到：Pencil `execute` 沙盒內的動態行為（本票的 `SCAN_BATCH`／`SCAN_BATCH_ROOTS` 全域旗標靠字面注入 snippet，離線 grep 看不到 Pencil 端真實觸發路徑，只能信賴 node 端自測與 merge-reviewer 已做的 Pencil 端實跑證據）。
- 本次巡檢**只涵蓋 LS-226 四支 commit 的異動範圍**，未對 `overflow-scan.js`／`design-evidence-check.sh` 既有（LS-122/168/185/202/207 等）程式碼做全檔案死碼複查，故「既有死碼觀察」欄從缺——不是「查過確認沒有」，是本票範圍未涵蓋，如需全檔複查請另開票。
- merge-review R1（`8986ebf8`）已提出的 informational i1–i9（`fnv1a64` 改寫、走訪順序變化、py 代表形狀誤讀提示、`document_flagged` 不在雜湊內等）屬效能／規格細節而非死碼，本報告不重複列出，避免與 reviewer 意見重疊誤導 orchestrator 判優先序。

### 建議處置
- F1：**建議併入下一張碰 `.claude/agents/ui-designer.md` 的 harness 票順手改**（純文字、無 gate 風險，比照既有 LS-96 池項慣例）；如 orchestrator 認為值得單獨留痕，也可記入 LS-96 待辦池。
- 其餘：**巡檢 0 新發現**——LS-226 引入的程式碼（分批／merge／result_hash／收據 gate／自測／fixture）與文件同步皆已核實無孤兒符號、無孤兒檔案、無殘留 SQL（本票無 SQL 改動）、無鷹架殘留（debug print／TODO／假資料 一律 0 命中）、無設定殘留（CI workflow 對新測試檔的呼叫皆已接線）。

### 2026-09-12T06:26:57.217Z · Chih-Lin Yeh · `ea90522e`

**orchestrator 09-12 14:26 收尾儀式（§6）**
① dead-code-sweeper `78025a0e`：1 發現（`ui-designer.md:33` 過期 `tree_hash=skipped` 敘述，docs）→ 池 LS-96 `1ae89cbd`；新增 JS／python 函式、fixture、17 個 gate 案皆有呼叫端；四條池項確認已收；51/51＋gate 自測全綠。
② lesson learning：
- harness：Pencil `execute` 對萬節點文件的中斷改由腳本分批＋合併吸收（19 批約 2 分鐘），設計票不再需要手寫收據；`result_hash` 讓「沿用舊輪 flagged」對 O(1)～O(n) 三支不再盲，三支 O(n²) 仍靠格式檢查（reviewer minor-1 不變量 → 池 `8647e2d8`）。
- 流程：收據 schema 變更的 cutoff 以 PR head tree 為準，reviewer 實跑證明 LS-208 #349 現況直接併不紅、r7 才需新格式——已寫進 LS-208 收尾路徑。
- 工具缺口：merge-reviewer 白名單無 `mcp__pencil__*`，抽樣重放只能靠離線證據；本票 reviewer 以 4500 組 fuzz＋python 對照補足，可接受；是否給 reviewer 唯讀 Pencil 工具待觀察（不入池）。
③ 清理：14:10 `cleanup-merged.sh --apply LS-226` ✓（worktree 1／分支 0）。④ Pen：非設計票（Pen 全程停在 LS-208 檔未動）。
main `bf6a15a`（#358）＋back-merge #359 → development `94b4a54` ✓、main ⊂ dev；merge-review R1 APPROVE `8986ebf8`；QA delta＝reviewer fuzz／mutation／`--merge` 位元比對實跑（hotfix 路徑沿 LS-215／228）→ **Done**。

