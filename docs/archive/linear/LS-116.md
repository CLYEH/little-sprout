# LS-116 Harness：patrol_linear.py LANE_LIMITS lane:ui 1→2——LS-67 核可觸發的手動同步（R1 informational 預告；獨立小票不塞 LS-103）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-31T01:51:37.814Z |
| 開始 | 2026-08-31T03:34:09.922Z |
| 完成 | 2026-08-31T03:57:34.314Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-116/harnesspatrol-linearpy-lane-limits-laneui-12-ls-67-核可觸發的手動同步r1 |
| 關係 | related→LS-91, related→LS-81, related→LS-67, related→LS-46, related→LS-96, blocks←LS-103 |

## 描述

來源：使用者 2026-08-31 核可 [LS-67](https://linear.app/little-sprout-app/issue/LS-67/設計多寶貝-profile-三畫面建檔管理切換-ui-designervisual-reviewer-3-輪) 多寶貝設計稿 → 依 §5-b「ui 上限＝已核可設計稿畫面群數」，上限 1→2（[LS-46](https://linear.app/little-sprout-app/issue/LS-46/設計定稿合併c-基準使用者指定調整-designlittlesproutpen)＋[LS-67](https://linear.app/little-sprout-app/issue/LS-67/設計多寶貝-profile-三畫面建檔管理切換-ui-designervisual-reviewer-3-輪)）。`patrol_linear.py` 的 `LANE_LIMITS["lane:ui"]` 硬編為 1（[LS-103](https://linear.app/little-sprout-app/issue/LS-103/harnesspatrolsh-用-linear-api-key-直接打-linear-graphql-把巡檢的-linear) R1 informational 已預告「等 [LS-67](https://linear.app/little-sprout-app/issue/LS-67/設計多寶貝-profile-三畫面建檔管理切換-ui-designervisual-reviewer-3-輪) 核可時手動同步」，記於 [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) comment `67a6bf42`）——現在觸發了，但 patrol 的 lane 補位輸出會少算 ui 一個空位。

orchestrator 原想以 SendMessage 塞進 [LS-103](https://linear.app/little-sprout-app/issue/LS-103/harnesspatrolsh-用-linear-api-key-直接打-linear-graphql-把巡檢的-linear) 在飛 hotfix，該 agent 以「引用無法核實＋hotfix scope 紀律」正確拒絕——本票即正規收口。blockedBy [LS-103](https://linear.app/little-sprout-app/issue/LS-103/harnesspatrolsh-用-linear-api-key-直接打-linear-graphql-把巡檢的-linear)（同檔避免衝突，等 PR #173 併入 main 後從 main 切 hotfix）。

## Scope

1. `scripts/ops/patrol_linear.py` `LANE_LIMITS["lane:ui"]`：1 → 2，旁註記「[LS-46](https://linear.app/little-sprout-app/issue/LS-46/設計定稿合併c-基準使用者指定調整-designlittlesproutpen)＋[LS-67](https://linear.app/little-sprout-app/issue/LS-67/設計多寶貝-profile-三畫面建檔管理切換-ui-designervisual-reviewer-3-輪)（2026-08-31 核可）」。
2. 對應自測 fixture 期望值同步。
3. `docs/COLLABORATION.md` §5-b 表格 ui 列同步（若該表寫死 1）。

## 驗收條件

- [ ] `patrol-linear.test.sh` 全綠（含更新後的 ui 上限 fixture）
- [ ] 真實跑一次 `patrol-linear.sh`：lane 狀態表 ui 列印 2 為上限
- [ ] CI 四 check 綠

## 留言（5）

### 2026-08-31T03:39:51.354Z · Chih-Lin Yeh · `7dcc3383`

## Handoff（ios-dev → orchestrator）

**Ticket**：LS-116 patrol_linear.py `LANE_LIMITS["lane:ui"]` 1→2（LS-67 核可觸發手動同步）

**已完成**
1. `scripts/ops/patrol_linear.py`：`LANE_LIMITS["lane:ui"]` 1→2，旁註記「LS-46＋LS-67（2026-08-31 核可）；上限＝已核可設計稿畫面群數（§5-b）」
2. `docs/COLLABORATION.md` §5-b lane 上限表 ui 列同步：`＝已核可設計稿對應的畫面群數（1 → **2**：LS-46＋LS-67，2026-08-31 核可）`
3. `scripts/ops/patrol-linear.test.sh` 未改動——逐行檢視後確認全檔沒有硬編 ui 上限數值的斷言；⑧ `design_forced_full` 測項讀的是執行當下 `entry["limit"]`（動態算出），只驗 `lane:design` 因 ui 票 In Progress 而被判定 wip==limit，與 ui 自己的上限值無關，因此不需要同步 fixture。

**已驗證（怎麼驗的）**
- `bash scripts/ops/patrol-linear.test.sh` 本機全綠（含 ⑧a/b/c design_forced_full 三組）
- `bash scripts/ops/patrol.test.sh` 本機全綠
- 真實跑一次 `PEN_OPEN_SH=/nonexistent/pen-open.sh bash scripts/ops/patrol-linear.sh`（避開 Pen session），輸出 Lane 補位段：
  ```
  lane:ui        上限2 在飛0  候補：LS-19, LS-109（cycle 外，取第一張需 scope+）  待Spec：無  待結構：無
  ```
  確認上限已印 2。同次真實跑也印出 lane:harness／lane:design 候補與動作清單（含 `save_issue LS-19 cycle=1（scope+）`），此為現況真實 Linear 資料，未執行任何動作（本票 scope 只在驗證上限值，動作交還 orchestrator 巡檢流程處理）
- push gate 全綠（xcodegen 版本比對、unit tests、api-contract、error-codes、migration 版本號、分支票號、與 origin/main 可乾淨合併）

**未完成**：無（票文三項 scope＋驗收條件皆完成；本票無 reviewer informational 待處置——尚未經 merge-reviewer）

**風險**：無破壞性變更，純資料/文件同步；`patrol-linear.test.sh` 與 `patrol_linear.py` 之間仍維持既有「無機械對帳」模式（票文與程式碼註解皆已載明）

**產出位置**
- head SHA：`7aa20a1`（branch `hotfix/LS-116-lane-ui-limit`，from `main` tip `2ca6120`）
- PR：https://github.com/CLYEH/little-sprout/pull/179（hotfix/LS-116-lane-ui-limit → main）
- 模擬器已關：無（本次任務未額外 boot 非 demo 模擬器；目前僅 `demo-iPhone17Pro` Booted，屬豁免）

push 已完成，push gate 已過，不等 CI；後續請派 merge-reviewer。

### 2026-08-31T03:45:36.681Z · Chih-Lin Yeh · `a00063af`

## Merge Review R1 — REQUEST_CHANGES

**審的 head**：`7aa20a124921f8699ba70e476241b47d9bfee058`（PR #179，base `main`，diff base `origin/main` = `2ca6120`）
**diff 範圍**：2 檔 2 行（`scripts/ops/patrol_linear.py` 1 行＋`docs/COLLABORATION.md` 1 行）

### Findings

**F1 — major — `docs/COLLABORATION.md:247`（§7 前饋↔反饋對照表）ui 上限仍寫 1，與本次改動互相矛盾**

該列文字：

> **動作執行仍由 orchestrator**（腳本只讀不寫，不呼叫 Linear mutation）；上限（harness 1／backend 3／design 1／**ui 1**）是腳本頂端與 §5-b 表格各自維護的常數，兩邊沒有機械對帳，改了要手動同步；…

本 PR 改了 `patrol_linear.py`（→2）與 §5-b 表格（→2），但 §7 這句把四個上限**數值再抄了一份**，仍是 `ui 1`。改完後 repo 內同一個數字有三份、其中一份是舊值；而且這句自己宣稱「**兩邊**沒有機械對帳」——實際上是三處，這句話本身也不再準確。

已確認**沒有任何機械 gate 會抓到**：`scripts/gates/` 底下無 lane-limit 對帳腳本，CI `rules` job 只跑分支／commit／secrets 與各 harness 自測；`patrol_linear.py:36` 註解也明講「兩邊沒有機械對帳（巡檢承載）」。也就是說這份漂移只有人工 review 擋得住。

具體失敗情境：§7 是 CLAUDE.md「最高原則」段指定的權威對照表（「對照表見 docs/COLLABORATION.md §7」）。往後 orchestrator／agent 若從 §7 這句讀上限（它正是**專門描述這幾個常數**的那句話），會得到 `ui 1` → ui lane 明明有 2 個位子卻只補 1 張、或反向：下一張「同步上限」的 harness 票以 §7 為準把程式碼改回 1。本票的存在理由就是關掉這類數值漂移（票文：「LS-103 R1 informational 預告的手動同步」），漏一處等於沒關成。

修法：同一檔案、同一次 commit 改 `ui 1` → `ui 2`，並把「兩邊」改成「三處」或改為不重抄數值、只指向 §5-b 表格（後者更好——少一份副本就少一個漂移點）。

### Informational（i1／i2，不影響裁決）

**i1 — `scripts/ops/patrol_linear.py:572`：每 lane 每輪只補一張**
`chosen = candidates_shown[0]`，一輪只產生一個 `save_issue ... state=Ready` 動作。ui 上限變 2 後，wip=0 時要跑兩輪巡檢（間隔約 26 分）才填滿兩個位子。**非本 diff 引入**——`lane:backend` 自 2026-08-25 起上限就是 3，同一行為早已存在，只是上限 1 時看不出來。純記錄，建議入 LS-96 待辦池而非本票處理。

**i2 — PLAUSIBLE — ui 上限 >1 後，ui-vs-ui 的 Pen 競用沒有對應守衛**
`design_forced_full()`（`patrol_linear.py:316`）的 docstring 明講其目的是「否則巡檢可能同時補位派出兩張要動 Pen 的票，撞同一份文件（LS-81／LS-91 同型事故）」，但 `lane_wip():357` 只對 `lane == "lane:design"` 套用 forced-full——**ui 自己對 ui 沒有任何序列化**。上限 1 時「同時最多一張 ui 票」是個附帶不變量，改 2 後消失；§7 Pen 列現在寫的「實務靠 design lane WIP＝1＋『ui 票讀設計稿期間 design 視為滿』序列化」在 ui-vs-ui 這一側不再成立。

**標 PLAUSIBLE 而非 major 的理由**：實際跑 ui 票的是 ios-dev，其定義（`.claude/agents/ios-dev.md:11`）是「依 ui-designer 的 .pen 設計稿實作（orchestrator 會提供設計 handoff 或截圖）」，全檔**沒有** `pen-open.sh`——只有 ui-designer／visual-reviewer／qa 三份定義會開 Pen（grep 已核）。所以兩張 ui 票同時在飛時，兩個 ios-dev 都不會去搶全域單一 Pen 實例，我構造不出可靠的重現路徑。僅在「orchestrator 日後真的讓 ui 票 agent 開 .pen 讀稿」時才會浮現；建議記入 LS-96（與該處已記的 `pen-lock.sh` 同一條）。

### 已驗證（怎麼驗的）

1. **scope（全量）**：`git diff origin/main...7aa20a1` 共 2 檔各 1 行，皆落在票文 scope 1/3；無夾帶重構、無順手改動。scope 面**乾淨**。
2. **「測試無寫死 ui 上限」宣稱 → 屬實**。逐項查核：
   - `patrol-linear.test.sh` 全檔 `lane:ui` 只出現在 435/440 行（⑧ design_forced_full 的 fixture，LS-950 用來觸發條件），主 fixture（②/④）沒有任何 lane:ui 票；
   - ⑧ 的 `assert_design_lane()`（470-500 行）讀的是 `d["lanes"]["lane:design"]` 的 `wip`／`limit`／`chosen`，比較 `wip = limit`（design 的 limit=1 未變），**與 ui 的上限值無關**；
   - ④b 的 lane 表五欄斷言（276-281 行）只針對 `lane:harness`／`lane:backend` 兩列；
   - 全檔 grep「上限」僅一處註解、無數值斷言。
   - **實跑**：在 worktree 內 `bash scripts/ops/patrol-linear.test.sh` → 全綠（含 ⑧a/b/c）。宣稱成立，不需改 fixture。
3. **文件一致性（第 3 點要求）**：repo 全域 grep「畫面群數／核可設計稿／LS-67／harness 1／backend 3」——**寫死 ui 上限數值者共三處**，本 PR 改了兩處，漏第三處 = F1。§4-b cron 模板（135 行）與 §5-b 補位／例外段（174/175 行）只提 `lane:ui` 這個標籤、不含數值，**不需改、也確實沒被改**，正確。CLAUDE.md:23 只指向 §5-b，正確。
4. **前提核實**：LS-67 於 2026-08-31T01:38 轉 Done，§5-b 公式（上限＝已核可設計稿畫面群數）下 LS-46＋LS-67 = 2，值本身正確。
5. **race／效能／平行（程式碼面）**：`LANE_LIMITS` 是 module-level 唯讀常數，無並發、無查詢、無 I/O 影響；除 i1／i2 外無發現。
6. **CI**：`gh pr checks 179` 對 `7aa20a1` 四項全綠（ci 4m15s／db 2m56s／lint 13s／rules 2m17s）。

### 未看的範圍

`patrol.test.sh`（實作回報說也跑過）我沒重跑——本 diff 未觸及 `patrol.sh`，判斷無關。

### Verdict：**REQUEST_CHANGES**（F1 major）

修 F1（單行、同檔）後 R2 重審。注意：新 commit 會讓 head 前移，R2 須對新 SHA 重貼 `merge-review` status。

### 2026-08-31T03:49:12.458Z · Chih-Lin Yeh · `40dcfe31`

## Handoff（ios-dev → orchestrator）R1 續修完成

**Ticket**：LS-116（回應 merge-review R1 comment `a00063af-ae62-4af9-a3b9-97d6300c22a1` 的 F1 major）

**已完成**
- F1（major）：`docs/COLLABORATION.md:247`（§7 對照表）該句不再重抄四個 lane 上限數值，改為指向上方 §5-b 表格；並把「兩邊沒有機械對帳」修正為正確描述——數值副本只有 `patrol_linear.py` 頂端常數與 §5-b 表格兩處，§7 本句只指向、不重抄。單檔單行改動，未動其他內容。

**已驗證（怎麼驗的）**
- `git diff` 確認只改 `docs/COLLABORATION.md` 1 行，未夾帶其他變更
- `bash scripts/ops/patrol-linear.test.sh` 本機重跑：全綠（docs-only，如預期無影響）
- push gate 全綠（xcodegen 版本比對、unit tests、api-contract、error-codes、migration 版本號、分支票號、與 origin/main 可乾淨合併）

**未完成（R1 informational 處置）**
- i1（`patrol_linear.py:572` 每 lane 每輪只補一張，ui 上限 2 需跑兩輪才補滿）：不修，記入 LS-96 待辦池——reviewer 已標明非本 diff 引入（lane:backend 上限 3 時同行為早已存在），純效率項
- i2（PLAUSIBLE：ui 上限 >1 後 ui-vs-ui 的 Pen 競用無守衛）：不修，記入 LS-96 待辦池——reviewer 已標明 PLAUSIBLE 而非 major，理由是 ios-dev 定義全檔無 `pen-open.sh`，目前構造不出可靠重現路徑，僅在 orchestrator 日後真的讓 ui 票 agent 開 .pen 讀稿時才會浮現

**風險**：無，純文件敘述修正，不影響程式行為

**產出位置**
- head SHA：`2555d05`（branch `hotfix/LS-116-lane-ui-limit`，接續前次 `7aa20a1`）
- PR：https://github.com/CLYEH/little-sprout/pull/179（同一 PR，已更新）
- 模擬器已關：無（本次 push gate 用的 `LS-116-iPhone17Pro`（F2BCAA3D）測完已自行 Shutdown；另有 `LS-107-iPhone17Pro` Booted 但非本任務所開，不動；`demo-iPhone17Pro` 屬豁免）

push 已完成，push gate 已過，不等 CI；請派 R2 對新 head `2555d05` 重審重貼 status。

### 2026-08-31T03:56:05.990Z · Chih-Lin Yeh · `5d9bb92f`

## Merge Review R2（增量）— APPROVE

**審的 head**：`2555d056ce6802976c38a629ce5775fe452417b9`
**增量範圍**：`7aa20a1..2555d05` = `docs/COLLABORATION.md` 1 檔 1 行（+1/-1），與回報一致，無夾帶。
**累計範圍**：`origin/main...2555d05` = 2 檔 3 行（`patrol_linear.py` 1／`COLLABORATION.md` 2），全數落在票文 scope 1/3；R1→R2 之間無 scope 蔓延。

### F1（R1 major）→ 已解決，且採用了較好的那個修法

實作選了我建議的第二方案（不重抄數值、只指向 §5-b），比改成 `ui 2` 更好——直接消掉第三個漂移點而非維持它。逐項核實：

1. **§7 行內已無任何上限數值**。`docs/COLLABORATION.md:247` 現為「各 lane 上限數值見上方 §5-b 表格（本句不重抄數值，避免多一份副本、多一個漂移點）」。原句的 `harness 1／backend 3／design 1／ui 1` 四個數值全部移除。
2. **「兩處」描述屬實**（這是 F1 的第二半——原句自稱「兩邊」但實際是三處）。全 repo 核對，帶 lane 上限**數值**者現在恰好兩處：
   - `scripts/ops/patrol_linear.py:37` `LANE_LIMITS = {...}`
   - `docs/COLLABORATION.md:167-171` §5-b 表格

   驗法：`git grep "lane:ui"` 全部 8 個命中逐一判讀（`COLLABORATION.md:135`／`162`／`174`／`175` 只提標籤不含數值；`patrol-linear.test.sh:435,440` 是 fixture 標籤；`patrol_linear.py:326` 是狀態過濾）；再跑 `git grep -E "(lane:)?(harness|backend|design|ui)[^a-zA-Z]{0,4}[0-9]"` 濾掉票號/PR/輪次後只剩 §5-b 表格的 `lane:harness | 1` 與 `lane:design | 1` 兩列。**沒有第三處殘留。**
3. **指向正確**。§5-b 在 158 行、§7 該列在 247 行，「上方」成立；§5-b 表格確實列全五個 lane（harness 1／backend 3／design 1／ui ＝畫面群數 2／product 無上限），指過去讀得到完整資訊，不是斷鏈。
4. **「沒有機械對帳」仍屬實**（不是過時敘述）。`scripts/gates/` 37 支腳本無 lane-limit 對帳；`git grep LANE_LIMITS` 只在 `patrol_linear.py` 內（19/37/230/359/550），無跨檔驗證器。與 `patrol_linear.py:36` 註解「兩邊沒有機械對帳」現在**互相一致**——R1 時那句 py 註解說「兩邊」其實也是對的，錯的是 §7 多出來的第三份；現在兩份文字對齊。
5. **與 CLAUDE.md 的導向一致**。`CLAUDE.md:23` 本來就寫「每 lane WIP 上限與巡檢補位規則見 COLLABORATION §5-b」；§7 改為轉指 §5-b 後，兩條路徑收斂到同一個唯一來源。R1 擔心的「agent 從 §7 讀到舊值 ui 1」的失敗路徑已不存在——§7 現在讀不到任何數值，只能被導去 §5-b。
6. **Markdown 表格未破**。`awk` 核對 245-249 行皆 4 個 pipe、首尾皆 `|`，該列與相鄰列結構一致，渲染不會塌。

### CI

`gh pr checks 179` 對 `2555d05` **四項全綠**：ci 4m50s／db 2m52s／rules 2m19s／lint 13s。`rules` job 含 `patrol-linear.test.sh`，綠即涵蓋「fixture 未因上限改動而失效」——本輪增量為 docs-only，我未在本機重跑自測（CI 已覆蓋，說明如下）。

### 四維度

- **Race condition**：增量為純文件敘述，無程式碼。累計面 `LANE_LIMITS` 是 module-level 唯讀常數（`230`／`359`／`550` 三處只讀），無並發語意變更。無發現。
- **運算效能**：無查詢、無 I/O、無迴圈複雜度變化。無發現。
- **平行優化**：無新增可平行工作。R1 的 i1（每 lane 每輪只補一張，ui 上限 2 需兩輪填滿）已由 orchestrator 入池 LS-96，本輪不重提。
- **Scope**：增量恰為修 F1 所需的最小改動——單檔單行，未順手改鄰近文字、未重排表格、未動 `patrol_linear.py`。**乾淨**。

### R1 informational 追認

i1、i2 皆由 orchestrator 記入 LS-96 待辦池，與我 R1 的建議處置一致（i1 非本 diff 引入、i2 標 PLAUSIBLE 無可靠重現路徑），本輪不再列為 finding。

### 未看的範圍（明講）

- 本機未重跑 `patrol-linear.test.sh`／`patrol.test.sh`：增量是 docs-only，且 CI `rules` job 對 `2555d05` 已綠，判斷重跑無新資訊。
- 未重驗「LS-46＋LS-67 ＝ 2」這個值本身：R1 已核（LS-67 於 2026-08-31T01:38 轉 Done），本輪未觸及該行。

### Findings

**無**。增量無 blocker、無 major、無 minor——F1 修得完整且選了消除副本的方向，找不到新問題。

### Verdict：**APPROVE**

commit status `merge-review` = success 貼於 `2555d056ce6802976c38a629ce5775fe452417b9`。

### 2026-08-31T03:57:31.405Z · Chih-Lin Yeh · `a2da9c98`

**R2 APPROVE → 已併 main（2026-08-31）**：PR #179 merge，main=`0bc784c`；back-merge PR #180 已開（CI 綠後併）。

**Retro（harness 票輕量版）**：本票兩個教訓成對——① 起點：agent 拒絕無法核實的中途指示、改走正規票（scope 紀律成立）；② R1 抓到第三份數值副本漏改——「消滅漂移的票自己漏一份副本」證明 reviewer 建議的方向（消除副本、單一來源指向）優於同步副本；已落地。lane 上限現在只剩 patrol_linear.py 常數＋§5-b 表格兩處、§7 只指向。改善項無 High 以上。→ Done。patrol 下輪起 ui 上限機械輸出＝2。

