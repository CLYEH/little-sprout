# LS-106 Harness：push-gate 對齊 CI——xcodegen 漂移檢查前移＋Xcode／Swift 版本比對（#165 同一 PR 兩次本機綠、CI 紅）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-25T20:14:22.129Z |
| 開始 | 2026-08-25T20:41:02.788Z |
| 完成 | 2026-08-26T01:00:57.534Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-106/harnesspush-gate-對齊-ci-xcodegen-漂移檢查前移xcodeswift-版本比對165-同一-pr-兩次本機綠ci |
| 關係 | related→LS-54, related→LS-96, related→LS-65, related→LS-105, related←LS-139, related←LS-74, related←LS-68, related←LS-115, related←LS-98, related←LS-100 |

## 描述

## 來源

PR #165（[LS-105](https://linear.app/little-sprout-app/issue/LS-105/ax3-下數字鍵盤遮住-otp-畫面的確認登入鈕鍵盤不隨-dynamic-type-縮放-加鍵盤避讓可捲動ls-92-qa1-新發現)）連續兩次本機 push-gate 全綠、CI `ci` job 紅：

1. head `4a3bfa9`：CI「XcodeGen 漂移檢查」11 秒失敗——commit 的 `project.pbxproj` 不是乾淨 `xcodegen generate` 產物（新檔 GUID 不同）。
2. head `8b7a0fa`：CI「Build & Test」編譯錯 exit 65——`KeyboardHeightObserver.swift` 的 `UIScreen.main.bounds` 在 CI 的 Swift 版本被判 main actor-isolated 不可引用；本機 Xcode 較寬鬆。

同類事故（本機 gate 沒攔、CI 才抓）≥2 次 → High（§5-b）。每次多花一輪 CI（~12 分鐘）＋reviewer 等待＋一次 ios-dev 派工。

## 前饋（規約）

* COLLABORATION §4 push-gate 列：加「xcodegen 漂移」與「Xcode 版本比對」兩項；§7 對照表加兩列。

## 反饋（機械）

* `scripts/gates/push-gate.sh` 便宜檢查段（在 xcodebuild 之前；與 [LS-65](https://linear.app/little-sprout-app/issue/LS-65/harnesspush-gate-便宜檢查契約對帳錯誤碼分級票號衝突預檢前移到-xcodebuild-之前) 前移同位置）：
  1. `xcodegen generate` 到暫存目錄後 `diff` 與 commit 的 `LittleSprout.xcodeproj/project.pbxproj`，不同即擋（訊息同 CI）。需本機 xcodegen 版本＝CI（CI `brew install xcodegen` 最新；本機 `xcodegen --version` 不同只警告）。
  2. 讀 `.github/workflows/ci.yml` 的 runner／`xcode-select` 版本（或新增 `.xcode-version` 檔作單一來源，CI 與 push-gate 都讀它），與本機 `xcodebuild -version` 比對；不一致 → 警告並改用 `SWIFT_STRICT_CONCURRENCY=complete` 額外編譯一次（不跑測試，只 build）當替代檢查；一致 → 略過。
* `push-gate.test.sh` 加案例（漂移→擋；版本不一致→警告＋strict build 接線）。

## 驗收

* 對 #165 的歷史 head `4a3bfa9`（漂移）本機跑 push-gate 紅；`8b7a0fa`（MainActor）**在 pin 的 Xcode 16.4 存在時**紅（R1 F2：本專案已 `SWIFT_VERSION = 6.0`，`SWIFT_STRICT_CONCURRENCY=complete` 為 no-op；本機只裝 26.6 時以自測 stub 驗 `DEVELOPER_DIR` 接線）；對 `main` 綠。hotfix 從 main。

## 順手項（來自 [LS-100](https://linear.app/little-sprout-app/issue/LS-100/harness模擬器用完必關-ios-devqa-任務結束-simctl-shutdownpush-gate-測完關專屬機巡檢列出) sweeper，同 PR 一起做）

* `scripts/gates/push-gate.test.sh` stub `xcrun` 的 `STUB_SHUTDOWN_RC` 目前無案例使用——補一組「shutdown 失敗時 `|| true` 不拖垮 gate」案例（或刪旋鈕）。
* `docs/COLLABORATION.md` §4 push-gate 列「關掉本次用到的模擬器」補限定語「（僅限本 worktree 專屬機；共用機／demo-* 不設 trap）」，與 §7 及程式碼一致。

## 留言（4）

### 2026-08-26T00:03:54.752Z · Chih-Lin Yeh · `07146eff`

## merge-review R1 — PR #168 · **REQUEST_CHANGES**

審的 head：`7caa51386f6f760911f881f549d671fecc93e281`（`git diff d8edd66...7caa513`，5 檔 +215/-5）
PR 全文 review：https://github.com/CLYEH/little-sprout/pull/168#issuecomment-5418691873

### 實跑
`push-gate.test.sh` ①～⑫ 全綠（另用 `/bin/bash` 3.2.57 再跑一次 → bash 3.2 相容 OK）、`patrol.test.sh` 全綠、`agent-tools-check.sh` 綠。CI run 32912084627（head 7caa513）`rules`／`db`／`lint`／`ci` **四個全綠**，log 確認 `xcodebuild -version` 印 `Xcode 16.4 / Build 16F6`、SDK `iPhoneSimulator18.5` → `/Applications/Xcode_16.4.app` 在 macos-15 runner 存在，最大風險排除。另查 #165 那次紅的 run（`8b7a0fa`）也是 `Xcode_16.4.app` + `iPhoneSimulator18.5.sdk` → `.xcode-version: 16.4` 忠實對上當時 CI 實際用的版本。
負向控制三組我自己在 scratch 複本重跑確認：拿掉 `|| true` → ⑨ 紅；刪 1a → ⑩ 紅；刪 1b → ⑪⑫ 紅。真 xcodegen 實測鏡射法產出的 `project.pbxproj` 與 repo 版逐位元組相同（不會假紅）。

### Major（2）
* **F1 `push-gate.sh:77`**：1a 只比 `project.pbxproj`，CI 比整個 `.xcodeproj` 目錄。實跑重現——把 `project.yml` 的 `parallelizable: false` 改 `true` 而忘了重跑 xcodegen，`project.pbxproj` **逐位元組相同**（本機綠）但 `xcshareddata/xcschemes/LittleSprout.xcscheme` 變了（CI 紅）。同一類事故原封不動還在，且踩中 LS-54 N8 的保命設定。修法：改以「暫存目錄新產生的那份 `.xcodeproj` 的檔案集合」逐檔比對（該集合天然不含 repo 側的 `swiftpm/**`，不會假紅）。
* **F2 `push-gate.sh:177-183`**：`SWIFT_STRICT_CONCURRENCY=complete` 對本專案是 **no-op**——`project.pbxproj:641,717` 已是 `SWIFT_VERSION = 6.0`（Swift 6 模式本就隱含 complete）；`swiftc` 四組對照（v6／v6+complete／v5／v5+complete）診斷逐字相同。這才是 `8b7a0fa` 本機綠的真正原因（不只是「26.6 較寬鬆」），票文驗收「兩個歷史 head 皆紅」這條路走不到。成本：本機只裝 26.6、pin 16.4 → 不一致分支**每次 push 必然成立**，在整套 test 之後再多編一次 app target，付永久成本換零診斷。修法擇一：(a) 拿掉替代 build 只留警告；(b) pin 的 Xcode 有裝就用 `DEVELOPER_DIR=/Applications/Xcode_<pin>.app/Contents/Developer` 跑 gate 的 xcodebuild（唯一真能讓 `8b7a0fa` 本機轉紅、真滿足驗收的做法），沒裝退回 (a)。

### Minor（4）
F3 替代 build 不需模擬器卻佔 UDID 鎖（共用機退回路徑會擋住另一 worktree 的真 test，`simulator-lock.sh` 預設 timeout 900s）／F4 1a 擋下時把 diff 丟 `/dev/null`、訊息只講 `project.yml`（實跑重現：`LittleSprout/` 下未追蹤 `.swift` → 本機紅 CI 綠；`.DS_Store` 則不會假紅）／F5 `.xcode-version` 缺檔時 1b **無聲**略過，與 CI 硬紅不對稱／F6 pin 的 Xcode 被 runner image 淘汰時 CI 只吐原生錯誤、無指引。

### Informational（6）
I1 1a 註解與 §4／§7 的「暫存目錄深度需與 repo 根一致」說法與實測不符（深度 6 的 `/var/folders/…` 產出仍逐位元組相同；成立的原因是相對路徑＋不解析 symlink），結論對、理由記錯會誤導後人／I2 票文「本機 xcodegen 版本＝CI」半條未做（CI 未鎖版號，已列 §7 盲區），對稱收法是也把 xcodegen 版本釘成檔案 → 建議記入 LS-96／I3 自測 ⑫ 的前置條件是 ⑪ 那句標「還原」其實是「建立」的副作用（fail loud，不是洞，但註解誤導）／I4 1a 鏡射把 repo 根**每個**項目都 symlink 進暫存目錄（含 `.claude/worktrees/*`、根目錄 gitignored secrets 檔），spec 實際只需 4 個，建議縮成 allowlist／I5 `$xg_work` 在 `ln -s` 失敗或 Ctrl-C 時會殘留（只有 symlink，可接受）／I6 **scope 無 finding**：5 檔全在票文範圍、§4 限定語與程式碼一致、⑨ 讓 `STUB_SHUTDOWN_RC` 從死旋鈕變成有斷言的案例、三個 commit 訊息合規。

### 沒審／沒跑
真 push-gate（未 push、未 boot 模擬器）、真 `xcodebuild build`（F2 改以 `swiftc` 直接證明）。

commit status `merge-review` = **failure** 貼在 `7caa513`。

### 2026-08-26T00:47:11.732Z · Chih-Lin Yeh · `e66cfb56`

## merge-review R2 — PR #168：APPROVE

**審的 head**：`870d65eb3f26c49c1383a4a0e64808a848c1d331`（增量 `7caa513..870d65e`）｜全文：https://github.com/CLYEH/little-sprout/pull/168#issuecomment-5418999734

R1 兩個 major 都已修且**獨立重現驗證**：
- **F1**：用真 xcodegen 2.46.0 鏡射實跑——產生側集合＝`project.pbxproj`／`project.xcworkspace/contents.xcworkspacedata`／`xcscheme` 三檔逐位元組相同、`swiftpm/Package.resolved` 正確排除（乾淨樹不假紅）；R1 那個 `parallelizable` 漂移案例現在 exit 1、差異檔列 xcscheme。另用 `git archive 4a3bfa9` 的歷史樹驗到票文驗收條件（1a exit 1，diff 正是 KeyboardHeightObserver GUID）。
- **F2**：`export DEVELOPER_DIR` 在 detect-simulator／simctl／SPM 解析／`xcodebuild test` **全部之前**，simctl 與 xcodebuild 用同一套工具鏈、無錯配；替代 build 已整段移除（F3 自然解）。
- F4／F5／F6／I1／I3／I5 逐條核對通過；F6 的 `ci.yml` step 原碼在本機重跑，pin 不存在時 `::error::`＋exit 1 且在 `xcode-select` 之前。

**突變測試（load-bearing）**：1a 退回只比 pbxproj → ⑪ 紅；拿掉 fail-closed → ⑫ 紅；`export` 拿掉 → ⑬ 接線斷言紅。
**實跑**：`push-gate.test.sh` ①～⑮ 全綠（bash 3.2.57）；`patrol.test.sh`／`agent-tools-check.sh`／`detect-simulator.test.sh` 綠；CI run 32915095163 四 job 全綠（ci job 確認 Xcode 16.4／iPhoneSimulator18.5.sdk）。

**R2 findings（全 minor，不擋併）**
- R2-1 `push-gate.sh:86-92`：只走訪產生側，**repo 側多出來的 `.xcscheme`（GUI 建的 shared scheme／移除 target 後沒 `git rm`）本機綠、CI 紅**——兩邊都實跑重現（xcodegen 會刪掉它不管理的 scheme）。建議只對 `xcshareddata/xcschemes/` 反向比一次。
- R2-2 `push-gate.sh:100-105`：`set -e`＋`pipefail`＋`diff` 回傳 1 → **多檔漂移只印第一個檔的 diff**，105 行的 `exit 1` 在該路徑不可達（擋的行為沒破）。一個 `|| true` 可收。
- R2-3 `push-gate.sh:92`：產生側目錄名硬寫，`cd` 失敗 → 清單空 → **exit 0 fail-open**（改 `project.yml` 的 `name:` 實測重現）。補一行 `[ -d ]`。
- R2-4（PLAUSIBLE）`push-gate.sh:138`：切到 pin 的 Xcode 後 simctl 的 runtime 視野也換——照訊息裝了 16.4 但沒裝 iOS 18.x runtime 時，會紅在無關的「模擬器偵測失敗」。訊息補一句即可。

**informational**：I1 PR body 仍描述 R1 之前的設計（只比 pbxproj／strict-concurrency 替代 build），建議 merge 前更新；I2 ⑩⑪⑫ 的 `rc != 0` 無鑑別力（真正在判的是訊息 grep）；I3 自測 ④ 註解過期；I4 1b 無條件覆寫既有 `DEVELOPER_DIR`、無 opt-out；I5 §4／§7 與實作一致；I6 scope 乾淨（5 檔全在票內）。

**沒跑**：真的 push-gate（沒 push、沒 boot 模擬器）；`8b7a0fa` 那條（本機無 `Xcode_16.4.app`，runtime 只有 iOS 26.0／26.5）。

### 2026-08-26T00:54:04.079Z · Chih-Lin Yeh · `41faf7e4`

## dead-code-sweeper — LS-106 收尾巡檢（PR #168 head `870d65e`，7 commits 逐快照追蹤）

**F-1（新發現，R1／R2 未抓）**：`push-gate.test.sh:119` xcodebuild 假身的 `build)` 分支＋`STUB_BUILD_RC`／`BUILD_LOG`（及 `:112` `STUB_XCODE_BUILD`）——R1 移除「替代 build」功能時未清的 fixture：`push-gate.sh` 已無任何 `xcodebuild build` 呼叫，`build)` 不可達、`STUB_BUILD_RC` 無案例賦值；⑭ 現只斷言 `BUILD_LOG` 為空。處置：**記 LS-96，併入 LS-65 順手刪**（與 R2-1～R2-4 同批）。

**確認無問題**：`SWIFT_STRICT_CONCURRENCY` 只剩兩處歷史註解；`XCODE_APPS_DIR`／`.xcode-version` 讀取點與文件三處一致；1a 排除清單（`.git`／`.xcodeproj`）皆必要；`xg_work` trap 成功路徑有清；其餘 `STUB_*` 皆有案例；ci.yml 只新增一個 step、xcodegen 只裝一次；§4／§7 新增列與 LS-100 列對應不同案例區間、不重複。

已標註不重報：R2 I3（④ 註解過期）、R1 I2／I4、R2-1～R2-4（皆在 LS-96）。

盲區：純靜態＋逐 commit 比對，未重跑自測（R1／R2 已實跑）。

### 2026-08-26T00:54:31.690Z · Chih-Lin Yeh · `27515a06`

## Orchestrator — lesson learning review（LS-106）

**流程回顧**：#165 兩次「本機綠、CI 紅」→ §5-b High 開票 → 派工（05:03 登入逾期中止一次，07:14 接手）→ PR #168 → R1 REQUEST_CHANGES（F1 只比 pbxproj；F2 `SWIFT_STRICT_CONCURRENCY` 對 Swift 6 專案 no-op、且本機 Xcode 26.6 比 CI 16.4 **新**）→ R1 fix（整 .xcodeproj 逐檔比對；`DEVELOPER_DIR` pin 或警告；缺檔 fail-closed；CI 先查 pin 存在）→ R2 APPROVE（真 xcodegen 重現、三組突變紅）→ main → back-merge → sweeper 1 孤兒 fixture。開票到 main 約 4.5 小時（含 2 小時停擺）。

**Harness 改善**
- ✅ 本票落地：push-gate 漂移檢查前移（#165 第一種事故本機可攔）、`.xcode-version` 單一來源＋CI `xcode-select`（CI 版本從此顯式）、pin Xcode 有裝就真對齊。
- ⚠ 未達：票文「`8b7a0fa` 本機紅」——本機無 Xcode 16.4，只能警告。**要真正攔第二種事故需開發機裝 pin 版**（使用者決定，已在對話提出）。
- 記 LS-96（併 LS-65 同批）：R2-1 反向漂移、R2-3 `cd` 失敗 fail-open（**gate 不得 fail-open**，Medium）、R2-2、R2-4、I2／I3／I4、sweeper F-1 孤兒 fixture。
- 教訓：「本機較寬鬆」的直覺方向反了——版本比對機制要對「本機比 CI 新」與「比 CI 舊」兩個方向都有意義；已寫進 §7 盲區。

**設定優化**：`.xcode-version=16.4` 忘記隨 runner 升級同步無提示（§7 記錄）。

**工具缺口**：無新增。

**Done 前置**：R2 ✓、CI ✓、sweeper ✓、retro ✓、main＋back-merge（進行中）→ 完成後 Done。

