# LS-141 Harness：cleanup-merged.sh 對 gitignored 產物（Config/Secrets.xcconfig、supabase/.temp/、supabase/tests/evidence/）不再略過——dirty 判定只看 tracked／untracked-non-ignored

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-03T08:25:42.827Z |
| 開始 | 2026-09-03T09:06:51.239Z |
| 完成 | 2026-09-03T10:26:23.904Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-141/harnesscleanup-mergedsh-對-gitignored |
| 關係 | related→LS-144, related→LS-142, related→LS-121, related→LS-96, related→LS-119, related←LS-180, related←LS-154, related←LS-140 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項 `25a4ff4d`（P1）＋`c7dae0e8`：`cleanup-merged.sh --apply LS-<n>` 對含 gitignored 產物的 worktree 一律「dirty 或有 ignored 殘留，略過」——2026-09-02～03 已重踩 6 次（[LS-121](https://linear.app/little-sprout-app/issue/LS-121/taskls-21-後端-日記相簿多寶貝標記diary-childrenalbum-children-連結表rpc-改陣列時間軸篩選軟刪守門)／128／125／126／129／134／130／135），每張 iOS 票都有 `Config/Secrets.xcconfig`、每張碰 Supabase 的票都有 `supabase/.temp/`／`supabase/tests/evidence/`，等於腳本對真實 worktree 永遠沒用，每次靠 orchestrator 手動 `git worktree remove --force`。收尾儀式的「清理」步驟因此不是機械的。

## 範圍

1. `scripts/ops/cleanup-merged.sh` dirty 判定改為 `git status --porcelain`（不帶 `--ignored`）：tracked 修改或 untracked-non-ignored 檔才算 dirty；ignored 產物一律視為可丟（`git worktree remove --force` 本來就會清）。保留現有白名單機制但改成「額外提醒」而非阻擋（印出被丟掉的 ignored 路徑清單供審計）。
2. 併入 `b410b190` 第 2 項：`--apply LS-<n>` 指名單票時，若該 worktree「尚未開工（與 base 相同）」且 Linear 狀態為 Done／Canceled（無 `LINEAR_API_KEY` 時以 `--force-unstarted` 旗標替代）→ 允許移除。
3. 另查 `aab3d640`：移除前呼叫 `pen-open.sh --status`，若 Pen 開著該 worktree 內的 `.pen` 就拒刪並印處置（[LS-119](https://linear.app/little-sprout-app/issue/LS-119/設計日記編輯器時間軸卡片流ls-21-畫面群-ui-designervisual-reviewer-3-輪) 事故）。
4. 自測 `cleanup-merged.test.sh`（既有或新建）：合成 worktree 含 ignored 檔 → 可清；含 untracked 非 ignored → 略過；Pen 開著 → 拒；尚未開工＋旗標 → 可清。
5. `docs/COLLABORATION.md` §6 收尾儀式清理段落與 §7 一列同步。

## 不做

不改 worktree 建立流程；不動 `pen-open.sh` 本身。

## 驗收

- [ ] hotfix 自 `main`、PR 回 `main`；自測正負樣本全過並掛 CI `rules`
- [ ] 對一個含 `Config/Secrets.xcconfig`＋`supabase/.temp/` 的合成 worktree 實跑 `--apply` 成功移除（PR body 貼輸出）
- [ ] merge-reviewer 通過（mutation：dirty 判定加回 `--ignored` → 「含 ignored 可清」樣本紅）
- [ ] 併 main 後 back-merge `main`→`development`

## 留言（10）

### 2026-09-03T09:26:23.344Z · Chih-Lin Yeh · `ee0141de`

## handoff

**Ticket**：LS-141（hotfix 自 `main`、PR 回 `main`）· PR #247 · head `cc24f5d` · 分支 `hotfix/LS-141-cleanup-ignored` · worktree `.claude/worktrees/LS-141`

### 已完成
1. **dirty 判定改 plain porcelain＋ignored 審計清單** — `158669a` `scripts/ops/cleanup-merged.sh:228-236`（`worktree_status`／`worktree_ignored`／`print_ignored_audit`）；呼叫點 merged 路徑 `:380-381`。`git worktree remove` 對只剩 ignored 檔的 worktree 不需 `--force`（git 2.47 實測），照原樣不帶。
2. **指名 `--apply LS-<n>` 時已收案未開工 worktree 可清** — `158669a` `cleanup-merged.sh:80-81`（參數）、`:116-120`（不帶票號 exit 2）、`:265-290`（`linear_state`：GraphQL `issue(id){state{name type}}`，token 走 curl `-K -` stdin）、`:397-438`（unstarted 分支：Linear completed／canceled 或 `--force-unstarted` → remove＋`branch -D`；否則略過＋三種提示）。
3. **移除前 `pen-open.sh --status`** — `158669a` `cleanup-merged.sh:237-262`（`pen_probe` 只呼叫一次快取、`pwd -P` 正規化；`pen_guard` 拒刪＋處置＋摘要「worktree Pen 開著 n」）。讀不到 → 警告、視為未開、繼續。
4. **自測** — `158669a` `scripts/ops/cleanup-merged.test.sh`：⑪ 反轉（只剩 `Config/Secrets.xcconfig`＋`supabase/.temp/`＋`supabase/tests/evidence/` → 可清＋審計列各路徑；未追蹤非 ignored → 略過、`--force` 不放行）、⑰ Pen 開著（`PEN_BIN` stub）→ 拒／開別路徑 → 清、⑤ 加驗 Pen 讀不到 → 警告後照清、⑱ 未開工（無 key 略過＋提示／`--force-unstarted` 清／stub curl completed 清且 argv 無 token／started 略過／不帶票號 exit 2／dry-run 零副作用／有未追蹤檔旗標不清）。CI `rules` Ops 自測 step 原本就掛 `cleanup-merged.test.sh`，`cc24f5d` `.github/workflows/ci.yml:249-253` 補註解。
5. **docs** — `528c030` `docs/COLLABORATION.md` §6 ③ 清理段落（`:222`）、§7 cleanup-merged 一列（`:298`）、§2 worktree 條 dirty 描述對齊（`:59`，原文與新判定矛盾，最小改動；票文只列 §6／§7，這是額外一處）。

### 已驗證
- `bash scripts/ops/cleanup-merged.test.sh` → 136 ✓、rc=0（①–⑱）。
- Mutation：`worktree_status` 加回 `--ignored=matching` → ⑪ 五條紅（dry-run 列可清／審計標題×2／worktree 已移除／分支已刪）、rc=1；已還原。
- 合成 repo（沿用本 repo 根 `.gitignore`＋`supabase/.gitignore`，真 pen CLI）：已併入 worktree 含 `.claude/evidence/`＋`Config/Secrets.xcconfig`＋`supabase/.temp/`＋`supabase/tests/evidence/` → dry-run 印審計四條、`--apply` 移除 worktree＋分支，exit 0（輸出貼 PR body）。
- 真實 repo：未開工合成 worktree `LS-99141-synth`（含 Secrets.xcconfig＋supabase/.temp/）→ dry-run 略過並提示 `--force-unstarted`；`--apply --force-unstarted LS-99141` 印審計兩條、移除成功、exit 0；Pen 當時開著 LS-142 的 .pen，真 pen-open 呼叫、不阻擋（輸出貼 PR body）。已清乾淨，主 checkout 無殘留。
- `bash scripts/gates/pr-body-check.sh` 通過（LS-96 行 2 條皆帶 id）；push-gate 通過（無 Swift 變更跳過 unit tests）。

### 未完成／informational 處置
- **記入待辦池 LS-96 `113311e1`**：Pen 檢查只看 `--status` 的 active 文件，背景視窗開著同一路徑偵測不到（票文「不動 `pen-open.sh`」）；候選 `pen-open.sh --list`（既有 `list_open_pen_paths` 直接印）＋`pen_guard` 比對全部路徑，size:XS。
- 不修＋理由：`.claude/evidence/<票>/` 隨 worktree 消失——票文方向就是 ignored 一律可丟；緩解＝dry-run 即印審計清單＋§6 明文「看到 `.claude/evidence/` 先確認已貼再 `--apply`」，不再加機械擋（會把票要拿掉的摩擦加回來）。
- 不修＋理由：`--status` 讀不到視為未開（fail-open）——否則 CI 與沒裝 pen 的機器永遠清不了；「Pen 開著但 CLI 未登入」組合會漏擋，§7 盲區已列。
- merge-reviewer 尚未審；reviewer informational 出來後依規則處置。

### 風險
- 方向反轉（ignored 可丟）是刻意的；審計清單是唯一提醒。
- Linear 只在「指名＋尚未開工」時查；一般已併入路徑行為不變。CI 無 `LINEAR_API_KEY`，自測用假 curl。
- pen-open `--status` 每次最多 8s，只在真要移除時呼叫一次。

### 產出位置
- PR #247（`hotfix/LS-141-cleanup-ignored` → `main`，head `cc24f5d`；commits `158669a` 腳本＋自測／`528c030` docs／`cc24f5d` ci 註解）
- 併入後待做：back-merge `main`→`development`；收尾清理 `bash scripts/ops/cleanup-merged.sh --apply LS-141`（本票 worktree 自己就是第一個實戰對象）。

### 2026-09-03T09:32:39.602Z · Chih-Lin Yeh · `557328a4`

orchestrator 2026-09-03 17:33：handoff `ee0141de` 收到——PR **#247** head `cc24f5d`（dirty 判定 plain porcelain＋ignored 審計清單、`--force-unstarted`＋Linear 狀態查詢、Pen guard；自測 136 條、mutation ⑪ 紅；§2 :59／§6 ③／§7 一列；真實 repo 合成 worktree 實跑成功；LS-96 `113311e1`），CI 四綠 → In Review、派 merge-review R1（重點：pen guard fail-open 取捨、舊 git `worktree remove` 不加 `--force` 風險、`.claude/evidence/` 隨 worktree 消失的緩解）。

### 2026-09-03T09:43:24.541Z · Chih-Lin Yeh · `0671fb9e`

## merge-review R1 — PR #247（head `cc24f5d`）

**判定：APPROVE**（0 blocker／0 major；2 minor＋2 informational，皆可依 §5-b 處置後併入）

審的範圍＝`git diff $(git merge-base main cc24f5d)...cc24f5d`：`scripts/ops/cleanup-merged.sh`（+170/-51 區塊）、`cleanup-merged.test.sh`、`docs/COLLABORATION.md`（3 行）、`.github/workflows/ci.yml`（純註解 5 行）。head 對帳：`gh pr view 247 --json headRefOid` ＝ `cc24f5d4cc2991b81a2ea0712141f44f8b6f3087`，與我 diff 的 SHA 一致。

### 逐項查實（皆為我自己實跑／讀碼，不採信 handoff 敘述）

1. **dirty 判定**（`cleanup-merged.sh:228`）確認只剩 plain porcelain。未追蹤非 ignored 仍算 dirty：自測 ⑪ 的 `LS-908-untracked`（`scratch.txt`）實跑略過、`--force` 也不放行（白名單比對走的是同一個 `has_only_whitelisted_residue`，語意未變）。審計清單（`:229-236`）在 dry-run 與 apply 都印——位置在所有 skip return 之後、`act()` 之前，所以 dry-run 印得出、被拒刪的 worktree 不會誤印。
2. **自測 136 條**：`bash scripts/ops/cleanup-merged.test.sh` → 136 ✓、rc=0（16.5s）。
3. **Mutation（票文驗收條件）**：scratchpad 複本把 `worktree_status` 加回 `--ignored=matching` → ⑪ 五條紅、rc=1，紅的正是 PR body 貼的那五條，逐字相同。**另加一組我自己的 mutation**：刪掉兩處 `pen_guard "$w" ...` 呼叫 → ⑤ 一條＋⑰ 五條紅（含 worktree 被誤刪、分支被誤 `-D`）。兩個新機制都真的有牙，不是只驗字串。
4. **`pen_probe` 只呼叫一次**：合成 repo 三個可清 worktree＋計數用 `pen-open.sh` stub，`--apply --all` 實跑 → 移除 3 個、stub 被呼叫 **1** 次。快取有效（`process_wt` 走 process substitution 在主 shell 執行，全域旗標不會被子 shell 吃掉）。
5. **`--status` 輸出格式相容**：`pen-open.sh:186-194` 在 status 模式印「裸路徑」到 stdout、錯誤走 stderr 且 exit 2，`pen_probe` 的 `p=$(... 2>/dev/null) || p=` 接得起來；⑰ 的 stub 印的是真 pen CLI 0.3.4 的 `Currently active canvas editor: \`…\`` 那一行、由 `pen-open.sh` 自己的 parser 解，等於端到端驗過格式，不是繞過 parser 的假綠。`pwd -P` 正規化那段在 `cd` 失敗時保留原字串（fail-open 方向），可接受。
6. **`linear_state` token 路徑**：`printf` 是 bash builtin（不 fork、無 argv 曝光）→ pipe → `curl … -K -`；header 格式與 `patrol_linear.py:110` 完全一致。無 key／缺 curl 或 python3／GraphQL error／identifier 不符 → return 1 → 呼叫端印提示不放行（fail-closed，方向正確）。FILTER 帶引號等字元只會讓 JSON 壞掉 → 一樣 fail-closed，無 shell 注入（`--data "$body"` 有引號）。
7. **`git worktree remove` 不帶 `--force`**：本機 git 2.47.0 實測可清（⑪ 綠）。舊版風險我判定為**無**——`git worktree remove` 的 clean 檢查是 fork 一支 `git status --porcelain`，plain porcelain 本來就不含 ignored，這行為不是 2.47 才有的；且萬一某版本改為拒絕，`act()` 會 fail loud（exit 1），不是靜默誤刪。**不列 finding**。
8. **docs**：§6 ③（`:222`）、§7 一列（`:298`）與腳本逐句對得上（審計清單、`.claude/evidence/` 先貼再清、未開工指名清＋Linear／旗標、Pen 拒刪且不得手動 `--force` 繞過、盲區三條）。§2（`:59`）那一行是 scope 外一處但**合理**：原文寫「dirty worktree（含未追蹤殘留）…一律略過」與新判定直接矛盾，不改就是留下已知錯誤規約；改動只有兩個片語，符合手術式原則。ci.yml 只加註解，`cleanup-merged.test.sh` 本來就在 Ops 自測 run 清單裡（`ci.yml:257`），驗收條件「掛 CI rules」成立。
9. **實跑證據自洽**：PR body 的 mutation 五條與我實測逐字相同；合成／真實 repo 的輸出格式與我讀到的 `print_ignored_audit`／`act` 字串一致。**真實 repo 無殘留**：`git worktree list` 無 `LS-99141-synth`、`git branch --list '*99141*'` 空。
10. **gate**：`pr-body-check.sh --branch hotfix/LS-141-cleanup-ignored` → 通過（LS-96 行 2 條皆帶 comment id）；`gh pr checks 247` → ci／db／lint／rules 四綠；`mergeStateStatus=BLOCKED` 只差本 review 的 required status。
11. **scope**：4 個檔皆可追溯到票文 1–5 項，無順手重構、無無關改動。`patrol.sh:251` 只是印出指令字串、不解析輸出，沒有耦合斷裂。

### Findings

**m1（minor，運算/正確性）`scripts/ops/cleanup-merged.sh:240-250` — Pen 檢查的 fail-open 在「本機有 pen CLI」時把守門整個抵銷**

`--status` 讀不到就一律「視為未開、繼續」。但 `pen-open.sh` 的 status 模式是**單次** `poll_once`＋8 秒看門狗（`pen-open.sh:186-188`，不像 open 模式有 15 秒輪詢預算）——本機負載高、Pen 剛啟動、CLI 一次連線抖動，都會讓這一次嘗試空手而回。

失敗情境：orchestrator 在 LS-142（design 票）併入後跑 `cleanup-merged.sh --apply LS-142`，此時 Pen 正開著 `.claude/worktrees/LS-142/design/littlesprout.pen`；`pen interactive` 這一次被 8 秒看門狗 kill → PEN_ACTIVE 空 → 印個警告就照刪 → LS-119 事故重演（幽靈文件、只能 SIGKILL Pen、所有 pencil MCP 斷線）。

補充分析（縮小嚴重度）：`design/littlesprout.pen` 是**追蹤檔**，所以「Pen 開著且有未落地改動」會先被 plain porcelain 判 dirty 擋下——殘留風險是流程中斷，不是設計資料遺失。因此我判 minor 而非 major。也因為它是追蹤檔、每個 worktree 都有，用「worktree 內有沒有 .pen」當判別式沒有意義。

建議修法（cheap，維持 CI 與沒裝 pen 的機器可清）：判別式改成「這台機器有沒有 pen CLI」——`pen_probe` 讀不到時多問一句 `command -v "${PEN_BIN:-pen}"`：**沒有** pen → 維持現在的 fail-open（CI／別人的機器不受影響）；**有** pen 卻讀不到 → 設個 `PEN_UNKNOWN=1`，讓 `pen_guard` 保守拒刪並印「pen CLI 在 PATH 但 `--status` 讀不到，保守略過；確認 Pen 沒開該 worktree 後重跑」。自測加一格（stub 讓 pen CLI 存在但不回 active 行）即可。

**m2（minor，race condition）`scripts/ops/cleanup-merged.sh:385-394`／`:427-434` — M4-c 刻意縮到「一瞬間」的 TOCTOU 窗被重新拉開，且未開工路徑的重驗漏掉 `has_real_history`／`is_merged_ref`**

兩件事疊在一起：

- (a) `pen_guard`（首次呼叫真的會等 pen CLI 最多 8 秒）被插在「刪除前重驗」與 `act` **之間**（`:391` → `:393` → `:397`）。R2 M4-c 的檔頭註解明講窗口只剩「重驗到 act() 之間這一瞬間」，現在那一瞬間變成最長 8 秒。
- (b) 未開工路徑（`:427-428`）的重驗**只看 dirty**，沒有像 merged 路徑（`:387`）那樣重驗 `is_merged_ref`／`has_real_history`。而 agent 一旦 commit，worktree 就變乾淨，dirty 重驗完全看不見它。

失敗情境（我在 scratchpad **實跑重現**，不是推測）：合成 repo，未開工 worktree `LS-920-race`；把 `pen-open.sh` 換成「呼叫期間落下第一個 commit」的 stub（模擬 pen CLI 那 8 秒內 agent 開工）；跑 `--apply --force-unstarted LS-920` → 輸出「→ 移除 worktree／→ 刪除本機分支」，worktree 目錄與分支雙雙消失，agent 那個未推送的 commit 連 `reflog show LS-920-race` 都查不到（`branch -D` 連 reflog 一起帶走，只剩 `fsck --lost-found` 到下次 gc 為止）。未開工路徑另外還不看 `--min-age`（刻意，已註解），等於這條路沒有任何時間緩衝，唯一防線就是那個沒被重驗的 `has_real_history`。

觸發機率低（要有人在票已 Done／Canceled 或操作者親口 `--force-unstarted` 的當下，正好對同一個 worktree 落下第一個 commit），所以是 minor 不是 major。

建議修法（兩處各一行）：① `pen_probe` 移到重驗**之前**（Pen 狀態本來就不是被競態的對象，先探完再重驗，窗口就回到原本的一瞬間）；② `:428` 的條件補上 `! has_real_history "$b" || ! is_merged_ref "$b" ||`，與 merged 路徑對稱。

### Informational（依 §5-b 處置，我不擋併入）

- **i1 `.claude/evidence/<票>/` 隨 worktree 消失**：我同意 handoff 的「不修」。理由不是「票文這樣寫」而是我查了現況——現有 7 個活 worktree 的 `--ignored=matching` 各只有 0–2 條、且**沒有任何一個**含 `.claude/evidence/`（實際證據都落在主 checkout 的 `.claude/evidence/LS-114|119|120`）。再加一道 `--yes-evidence` 只會把票文要拿掉的摩擦加回來，換到的是目前為零的風險；審計清單＋§6 明文足夠。若日後真的改成 agent 在 worktree 內寫 evidence，再回頭補這道擋。
- **i2 `linear_state` 未快取＋一條提示無自測**：同一個 FILTER 若命中兩個以上未開工 worktree（例如 `LS-141` 與 `LS-141-r2` 並存），會各打一次 curl（每次上限 25s）；另「Linear 查詢失敗」那條 hint 分支（有 key 但回 GraphQL error）自測沒覆蓋。兩者都是效率／覆蓋率層級，非缺陷，建議一併記入 LS-96 待辦池。

### 我沒審的範圍（誠實揭露）

- 沒有對真的 Pen／真的 Linear API 發過任何呼叫（依派工指示不動 Pen）；⑰／⑱ 的正向路徑我只驗到 stub 層，真實 CLI 只驗了輸出格式相容性（見上 5.）。
- 沒有在舊版 git 上實測 `worktree remove`（本機只有 2.47.0）；第 7 點是讀 git 行為推得的結論＋fail-loud 兜底論證。
- 沒有重跑 CI，只讀 `gh pr checks` 結果。


### 2026-09-03T09:44:29.024Z · Chih-Lin Yeh · `a49928e7`

orchestrator 2026-09-03 17:41：merge-review R1 `0671fb9e` **APPROVE**（自測 136＋兩支 mutation 實跑、pen_probe 快取、真實 repo 無殘留），2 minor——minor 2 TOCTOU 由 reviewer 實跑重現「窗口內落 commit 仍被 `branch -D`、未推送 commit 遺失」，屬破壞性工具的資料遺失路徑 → orchestrator 裁定併入前補 R2：`pen_probe` 移到重驗前、未開工重驗補 `has_real_history`／`is_merged_ref`＋注入 commit 的負控；minor 1 Pen fail-open 改「本機有 pen 卻讀不到 → 拒刪」。R2 handoff 後派 merge-review R2（限 delta）。

### 2026-09-03T10:00:25.405Z · Chih-Lin Yeh · `18b7bc5d`

## R2 handoff

**Ticket**：LS-141 · PR #247 · head **`752effe`**（R1 head `cc24f5d` → R2 commits `5a73c1d` 腳本＋自測／`726adae` docs／`752effe` ci 註解）· CI `ci`／`db`／`lint`／`rules` 四綠 · 回應 merge-review R1 `0671fb9e`

### 逐條處置
1. **m2 TOCTOU** — 已修 `5a73c1d` `scripts/ops/cleanup-merged.sh:415-418`（merged 路徑：`pen_guard` 移到 M4-c 重驗之前）／`:462-468`（未開工路徑：`pen_guard` 移到重驗之前，重驗條件補 `has_real_history "$b" || ! is_merged_ref "$b"`，與 merged 路徑對稱）。重驗到 `act` 之間只剩審計那一次快速 `git status`。自測 ⑲ `cleanup-merged.test.sh:593-`：pen stub 以 `PEN_STUB_COMMIT_IN`（`:68`）在 `pen-open.sh --status` 呼叫期間注入 `commit --allow-empty`（reviewer 的重現手法），未開工＋`--force-unstarted`／已併入乾淨兩路徑皆「刪除前重驗發現狀態已變化」拒刪、worktree＋分支仍在、注入的 commit 仍在；含前提斷言（tip 確實前進）。
2. **m1 Pen fail-open** — 已修 `5a73c1d` `cleanup-merged.sh:244-275`：`--status` 讀不到改三分——沒 pen CLI（CI）→ 放行；有 pen CLI 但 Pen 主行程沒在跑（`pgrep -f 'Pen\.app/Contents/MacOS/Pen$'`，同 `pen-open.sh:355`）→ 放行並記一行；有 pen CLI 且 Pen 在跑卻讀不到 → `PEN_UNKNOWN=1`，每個要移除的 worktree 保守拒刪並印處置。**與派工指示的差異（請裁定）**：多了「Pen 沒在跑 → 放行」這一分。理由：`--status` exit 2 涵蓋「app 沒開」，若照字面「有 pen CLI 卻讀不到就拒」，dev 機 Pen 一關就永遠清不了，會逼出習慣性手動 `--force` 繞過（正是本票要拿掉的）；`pgrep` 樣式漂移退回放行，方向同 `pen-open.sh` 預設模式，§7 盲區已列。要改回嚴格版只需刪 `:255-256` 那一個 `elif`。自測 ⑰ `:512-`（Pen 在跑讀不到 → 拒、沒 pen CLI → 清）、⑤ 改驗 Pen 沒在跑 → 清；`pgrep` 用 PATH stub（`:78`），不讓本機真 Pen 決定結果。
3. **i1 `.claude/evidence/` 隨 worktree 消失** — 不修＋理由：同 reviewer 查證（活 worktree 無一含 `.claude/evidence/`，證據都在主 checkout），審計清單＋§6 明文足夠；日後 agent 若改在 worktree 內寫 evidence 再補擋。
4. **i2 `linear_state` 未快取＋查詢失敗提示無自測** — 已修 `5a73c1d` `cleanup-merged.sh:309-316`（`linear_probe` 同票只查一次，直接呼叫不包 `$(...)`）；自測 ⑱ `:579-`：有 key 但 GraphQL error → 「Linear 查詢失敗」提示、不清；同票兩個未開工 → 兩個都清、stub curl 只被呼叫 1 次。
5. docs `726adae` `docs/COLLABORATION.md:222`（§6 ③ 三分判定一句）、`:298`（§7 機制補探 Pen 先於重驗＋未開工重驗、自測清單、盲區三分）；ci 註解 `752effe`。

### 已驗證
- `bash scripts/ops/cleanup-merged.test.sh` → **165 ✓**、rc=0（①–⑲）。
- Mutation（scratchpad 腳本套用→跑→還原，還原後與備份 byte 一致）：
  - **M-a** 未開工 `pen_guard` 移回重驗之後（reviewer R1 重現形狀）→ rc=1，⑲ 五條紅：重驗抓到／摘要重驗生變 1／LS-920-race worktree 仍在（目錄消失）／本機分支仍在（不見了）／注入的 commit 遺失——即 reviewer 描述的資料遺失路徑，被自測釘住。
  - **M-b** 未開工重驗退回只看 dirty（R1 狀態）→ rc=1，同上五條 ⑲ 紅。
  - **M-c** Pen 讀不到一律放行（R1 狀態）→ rc=1，⑰ R2 m1 四條紅（保守略過＋處置／摘要 Pen 開著 1／worktree 仍在／分支仍在）。
- `pr-body-check.sh` 通過（「已修」行 4 條皆帶 SHA、LS-96 行 2 條皆帶 id）；PR body 已增量更新（## R2 段含上述 mutation 輸出）；push-gate 通過；CI 四綠。

### 未完成／informational
- LS-96 池項不變：`113311e1`（Pen 背景視窗偵測不到，候選 `pen-open.sh --list`）。R2 無新增池項。

### 風險
- m1 的「Pen 沒在跑 → 放行」依賴 `pgrep` 樣式（同 `pen-open.sh`），樣式漂移方向是放行——與 R1 相比仍是收緊（R1 是無條件放行）。
- 未開工路徑仍不看 `--min-age`（刻意，tip 是 base 的 commit）；R2 後唯一防線由「重驗含 history／merged」機械化，並有 ⑲ 釘住。

### 產出位置
PR #247 head `752effe`；worktree `.claude/worktrees/LS-141`；待 merge-review R2（限 delta：`git diff cc24f5d..752effe`）。

### 2026-09-03T10:01:09.896Z · Chih-Lin Yeh · `fced8a09`

orchestrator 2026-09-03 18:02：R2 handoff `18b7bc5d` 收到——head `752effe`（pen_guard 移到重驗前＋重驗補 history／merged 判定＋⑲ 注入 commit 負控；Pen fail-open 三分；linear_probe 單查；自測 165、三支 mutation 紅），CI 四綠 → 派 merge-review R2（含裁定 agent 自報偏離：「有 pen CLI 但 Pen 沒在跑 → 放行」）。

### 2026-09-03T10:09:50.369Z · Chih-Lin Yeh · `4b48e29d`

## merge-review R2 — PR #247（head `752effe`）

**判定：APPROVE**（0 blocker／0 major；0 minor；2 informational）
**agent 自報偏離的裁定：可接受，不要求改嚴格版**（理由見 2.）

審的範圍＝**delta** `git diff cc24f5d..752effe`（`cc24f5d` 確認是 `752effe` 的祖先，`..` 與 `...` 同義）：`scripts/ops/cleanup-merged.sh`（+59/-23 區塊）、`cleanup-merged.test.sh`（+103/-9）、`docs/COLLABORATION.md`（2 行）、`.github/workflows/ci.yml`（純註解 4 行）。R1 已審過的部分不重審。
head 對帳：`gh pr view 247 --json headRefOid` ＝ `752effe0e67500b2a8b940546118d60a6b7362da`，與我 diff 的 SHA 一致；worktree HEAD 同值且 `git status --porcelain` 空。

### 逐項查實（皆為我自己讀碼／實跑，不採信 handoff 敘述）

**1. m2 TOCTOU — 已修，且兩條路徑都被自測釘住**

- 位置實查：`pen_guard` 呼叫點現在在 `:418`（merged 路徑，重驗 `:419-428` 之前）與 `:465`（未開工路徑，重驗 `:466-472` 之前）。**重驗到 `act()` 之間只剩 `print_ignored_audit`（`:429`／`:473`）**——`git status --porcelain --ignored=matching`，`matching` 模式對整個被忽略的目錄印目錄本身、不遞迴列檔（PR body 的實跑輸出正是 `!! supabase/.temp/` 這種目錄行），成本與重驗的 plain status 同級。檔頭「一瞬間」的說法成立。
- 未開工路徑重驗條件 `:468` 確認補上 `has_real_history "$b" || ! is_merged_ref "$b" ||`，與 merged 路徑 `:424` 對稱。邏輯我自己推過一遍：合法未開工情形下 `has_real_history` 為 false、`is_merged_ref` 為 true（tip＝base 必是 base 的祖先），不會誤擋；窗口內若落 commit → `has_real_history` 翻 true；若改成 `reset --hard` 到未併入的 commit → `! is_merged_ref` 翻 true。兩種變化都攔得到。
- **M-a 實跑（派工指定）**：scratchpad 複本把未開工路徑的 `pen_guard` 移回重驗之後（單行搬移，diff 只有 465d/472a 兩行）→ 自測 rc=1、⑲ **5 條紅**，逐字就是 PR body 貼的那 5 條，**含「✗ ⑲ 注入的 commit 遺失（LS-920-race）」**。R1 我實跑重現的資料遺失路徑確實被這條自測釘住。
- **M-d（我自己加的 mutation，PR body 沒有）**：把 **merged 路徑** 的 `pen_guard`（`:418`）移回重驗之後 → rc=1、⑲ 4 條紅（重驗沒抓到／worktree 目錄消失／分支被刪／**注入的 commit 遺失（LS-922-race-merged）**）。m2 修法的**兩半都有牙**，不是只有 agent 自己列的那一半。

**2. m1 Pen fail-open 三分 — 已修；偏離裁定「可接受」**

- 三分邏輯實查（`:249-266`）。`pgrep -f 'Pen\.app/Contents/MacOS/Pen$'` 與 `pen-open.sh:355` **逐字元相同**（我對兩檔各自 grep 比對，含跳脫與 `$` 錨點）。「沒有 pen CLI」那一分用 `command -v "${PEN_BIN:-pen}"`，與 `pen-open.sh:162` 自己的存在性檢查（`PEN_BIN=${PEN_BIN:-pen}` → `command -v "$PEN_BIN"`）是同一個判別式——不會出現「pen-open 認為有、cleanup 認為沒有」的分歧。
- **裁定：接受這一分，不要求改回我 R1 建議的嚴格版。** 三個理由：
  (a) 它不是啟發式放寬，而是**正確的前提**——Pen 主行程沒在跑，就不可能有任何文件開著，沒有東西需要保護。嚴格版在這個狀態下拒刪，擋的是不存在的風險。
  (b) 嚴格版在這台機器上的實際後果是「Pen 沒開就永遠清不了」——Pen 只在 design 票期間開著，其餘時間全是關的，等於守門天天誤報。本票的立論（LS-96 `25a4ff4d`：腳本對真實 worktree 永遠沒用 → 6 次手動 `git worktree remove --force`）正是「天天誤報的 gate 會被繞過」。我 R1 的建議在這點上考慮不周，agent 的修正比我原案好。
  (c) 風險方向：`pgrep` 樣式漂移 → 退回放行，但**要兩個獨立故障同時發生**（`--status` 已經讀不到 **且** 樣式漂移）才會回到 R1 的無條件放行；相對 R1 是嚴格收緊，沒有新增風險面。
- **`pgrep` 樣式與 `pen-open.sh:355` 同步的脆弱性**：確實存在（同一個字面值複製在兩個檔案、沒有共用常數、沒有任何 gate 驗兩者一致），但它的最壞後果就是退回 R1 的行為，而 R1 我已判 minor——「minor 的退化路徑」再算一個 minor 會讓本票為了一個兩層故障才會走到的分支多跑一輪，不划算。**列為 informational i3**（見下），修法很便宜。§7 盲區（`:298`）已明文寫「`pgrep` 樣式漂移會退回放行（方向同 `pen-open.sh` 預設模式）」，符合「規則＋盲區都要成文」的要求。
- **M-c 實跑（抽查）**：把 `PEN_UNKNOWN=1` 改成 `=0`（等同 R1 的一律放行）→ rc=1、⑰ **4 條紅**，逐字同 PR body（含 worktree 目錄消失、分支被刪）。守門有牙。

**3. i2 `linear_probe` — 已修**

`:309-316` 實查：`LINEAR_PROBED` 旗標在呼叫 `linear_state` **之前**就設 1（失敗也不重試）；呼叫端 `:449` 直接 `elif linear_probe "$FILTER"; then st=$LINEAR_STATE`，沒有包在 `$(...)` 裡，快取旗標不會被子 shell 吃掉（與 `pen_probe` 同一個寫法，R1 已驗過 `process_wt` 跑在主 shell）。自測 ⑱ 的 curl 呼叫次數斷言（`n_curl -eq 1`）是真的在數 stub log 行數，不是驗字串。

**4. 自測與 mutation 衛生**

- 複本實跑 `cleanup-merged.test.sh` → **165 ✓、rc=0**（R1 是 136，+29 相符）。
- 四支 mutation（M-a／M-b／M-c 為 PR body 宣告，M-d 為我自己加的）全部 rc=1 且紅在該紅的地方；每支跑完都從備份還原，**最後一次還原與 worktree 內原檔 `cmp` byte 一致**。全程在 scratchpad 複本操作，沒有動 worktree 或主 checkout。
- 自測本身的隔離：`mktemp -d`＋`GIT_CONFIG_GLOBAL=/dev/null`＋PATH 前置 stub（`gh`／`curl`／**`pgrep`**）＋`PEN_BIN` stub——`pgrep` 這支 stub 是必要的，否則本機真的開著 Pen 時 ⑤／⑰ 會隨環境變色。這點做對了。
- 測試品質：⑲ 斷言的是「注入的 commit 仍在」而不只是「印了某句話」，⑱ i2 斷言的是 curl 實際呼叫次數——都在驗意圖不是驗字面。

**5. delta 純淨／gate**

- 4 個檔全部可追溯到 R1 findings（m1／m2／i2）＋票文 §5（docs）；`ci.yml` 只有 4 行註解、`docs` 只有 §6 `:222` 與 §7 `:298` 各一行。**無無關重構、無順手改動**。整支 PR（vs merge-base main）也仍是同樣 4 個檔。
- `pr-body-check.sh --branch hotfix/LS-141-cleanup-ignored` → 通過：「LS-96 行 2 條皆帶 comment id、『已修』行 **4 條**皆帶 SHA」。
- `gh pr checks 247` → `ci`／`db`／`lint`／`rules` **四綠**。
- docs 與腳本對得上：§6 `:222` 新增的「Pen 在跑但 `--status` 讀不到也拒（登入 pen CLI 或關掉 Pen 再跑），Pen 沒在跑或本機沒 pen CLI 才放行」＝ `:249-266` 的三分；§7 `:298` 新增「探 Pen 一律放在『刪除前重驗』之前、未開工路徑的重驗含 `has_real_history`／`is_merged_ref`」＝ `:418`／`:465`／`:468`，自測清單與盲區三分也逐句對得上。

### Findings

**無 blocker、無 major、無 minor。** R1 的 m1／m2 我確認已實質關閉（不是敘述上關閉，是 mutation 打得紅）。

### Informational（依 §5-b 處置，我不擋併入；兩條都建議直接記 LS-96 待辦池，不值得另票）

- **i3 `pgrep` 樣式字面值在兩個腳本各寫一份、沒有 gate 驗一致**（`cleanup-merged.sh:256` vs `pen-open.sh:355`）。今天兩邊逐字相同（我比對過），但將來若 Pen 改名／改 bundle 結構而只改了 `pen-open.sh`，`cleanup-merged.sh` 會靜默保留舊樣式並漂到放行側，而且沒有任何測試會紅。票文明文「不動 `pen-open.sh`」，所以最便宜的修法是在 `cleanup-merged.test.sh` 加一條純字串斷言：從兩個檔各 grep 出 `pgrep -f '…'` 的樣式、斷言相等（size:XS，不需要跑 Pen）。
- **i4 摘要標籤把「狀態未知」算進「Pen 開著」**（`:589` 的 `worktree Pen 開著 ${wt_skipped_pen}`）。PEN_UNKNOWN 保守拒刪時共用同一個計數器，只看摘要的人會去找一份其實不存在的開啟中文件（明細行 `:271-273` 講的是實話）。純觀測性字面，改法是拆一個 `wt_skipped_pen_unknown` 或把標籤改成「worktree Pen 開著／狀態未知」。

### 我沒審／沒覆蓋的範圍（誠實揭露）

- **只審 delta**：`cc24f5d` 之前的內容沿用 R1 結論，這一輪沒有重新查證（例如 token 走 `-K -` 的 argv 隱藏、白名單比對、`worktree remove` 不帶 `--force`）。
- 依派工指示**沒有對真的 Pen／真的 Linear API 發過任何呼叫**；m1 三分只驗到 stub 層（`PEN_BIN`＋`pgrep` stub），真實環境下「Pen 在跑但 pen CLI 未登入」這個組合我沒有實地製造過。
- **覆蓋缺口（我判定風險≈0，不列 finding）**：⑰ 的 PEN_UNKNOWN 案例只跑 merged 路徑，未開工路徑的 PEN_UNKNOWN 沒有專屬案例。`pen_guard` 是單一函式、`PEN_UNKNOWN` 分支在函式最前面且與路徑無關，兩條呼叫點的行為必然相同。
- 沒有重跑 CI，只讀 `gh pr checks` 結果；沒有在舊版 git 上實測（本機 2.47.0）。


### 2026-09-03T10:12:08.969Z · Chih-Lin Yeh · `3cba2c64`

orchestrator 2026-09-03 18:11：merge-review R2 `4b48e29d` **APPROVE**（TOCTOU 兩半實跑 M-a／M-d 皆紅；m1 三分裁定可接受）→ PR #247 併入 **main `e5a9c23`**（status 已貼；主 checkout 已 pull）→ back-merge PR **#249** 背景等 CI 併 dev；informational 入池 LS-96 `7aeea175`。派 sweeper → retro → cleanup（用新腳本自己清自己）→ Done。harness lane 接續派 **LS-144**。

### 2026-09-03T10:16:06.388Z · Chih-Lin Yeh · `22bcb8c2`

## dead-code-sweeper 報告（orchestrator 代貼，LS-96 `5cb5ddf3`）

範圍 PR #247（`83be149...752effe`，4 檔），主 checkout main `e5a9c23` 唯讀＋本機重跑自測 165 ✓。**未發現本票引入的死碼**：(1) `pen_probe`／`pen_guard`／`linear_state`／`linear_probe`／`print_ignored_audit` 與三組快取旗標、兩個計數器皆有呼叫端（`:245`／`:269`／`:284`／`:310`／`:235`；merged `:418` 與未開工 `:465` 各一）；舊白名單機制（`has_only_whitelisted_residue` 等）仍在 `:399`／`:424`／`:468` 使用，只是資料來源改為 plain porcelain（票文意圖）；`--ignored=matching` 只剩 `worktree_ignored()` 審計一處。(2) 六個 stub 開關（`PEN_STUB_*`／`GH_STUB_PR_OPEN`／`LINEAR_STUB_TYPE`／`CURL_STUB_LOG`）皆有消費點。(3) `COLLABORATION.md` §2 :59／§6 ③ :222／§7 :298 與腳本一致；§4-b :135 只印指令不需同步。(4) `ci.yml` 純註解 9 行。(5) 無 `LS-141`／`LS-99141-synth` 殘留（worktree／branch／find 皆空）。(6) `.claude/agents/*.md`／`CLAUDE.md` 無「cleanup 會略過 ignored 需手動」過時敘述。盲區：純 grep、未重跑 mutation（採信兩輪 review 實跑）。

### 2026-09-03T10:26:20.150Z · Chih-Lin Yeh · `b7e501f5`

## 收尾（orchestrator 2026-09-03 18:26）

- **sweeper**：`22bcb8c2`，無死碼。
- **Retro**：(1) 六次手動清 worktree 的債一天內還清，且本票 worktree 是新腳本第一個實戰對象（`--apply LS-141` 一次移除、無需 `--force`）。(2) R1 reviewer 實跑重現 TOCTOU（8 秒 Pen 檢查窗口內落 commit 仍被 `branch -D`）——破壞性工具的「檢查→動作」之間任何慢操作都是資料遺失窗口；修法是把慢檢查移到重驗前。規則沉澱：**刪除類腳本的重驗與 act 之間不得有 I/O**。(3) agent 對 R1 建議做了比原案更好的修正（Pen 沒在跑＝放行，否則關掉 Pen 就永遠清不了），reviewer R2 同意——派工要允許 agent 在 handoff 說明偏離。(4) 兩處 `pgrep` 樣式字面值無 gate 對帳（LS-96 `7aeea175`）。(5) 時程：開票 16:25 → 派 17:07 → PR 17:33 → R1 退修 17:41 → R2 APPROVE 18:10 → 併 main 18:11 → back-merge 18:25 → Done。
- **清理**：`cleanup-merged.sh --apply LS-141` 移除 worktree 與本機分支；back-merge PR #249 併 development `905de9c`（status 已貼）。
- 狀態 → **Done**。

