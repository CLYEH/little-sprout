# LS-80 Harness：已併入 base 的 migration 檔不可變 gate——pre-push＋CI 對 base 上既有 `supabase/migrations/*` 的修改／改名／刪除即擋

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-25T05:04:13.577Z |
| 開始 | 2026-08-31T09:12:48.985Z |
| 完成 | 2026-08-31T09:42:33.868Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-80/harness已併入-base-的-migration-檔不可變-gate-pre-pushci-對-base-上既有 |
| 關係 | related→LS-53, related→LS-96, related→LS-76, related→LS-50, related→LS-66, related→LS-70, related→LS-45, related→LS-57, related←LS-108, related←LS-88 |

## 描述

## 來源

[LS-57](https://linear.app/little-sprout-app/issue/LS-57/產品決策owner-軟刪的內容可被作者直接還原diariesalbumscomments-系統性-是否記錄-deleted-by)／[LS-66](https://linear.app/little-sprout-app/issue/LS-66/taskls-47-後端-children-多寶貝-crud-rpc軟刪-30-天可還原角色矩陣測試) 收尾 retro（2026-08-25）。事故：[LS-57](https://linear.app/little-sprout-app/issue/LS-57/產品決策owner-軟刪的內容可被作者直接還原diariesalbumscomments-系統性-是否記錄-deleted-by) agent 在 R2 直接修改 [LS-66](https://linear.app/little-sprout-app/issue/LS-66/taskls-47-後端-children-多寶貝-crud-rpc軟刪-30-天可還原角色矩陣測試) 已併入 main 的 `20260825030000_children_write_path_and_soft_delete.sql`（把 LS040 改 42501），review 才抓到、改為在本票 migration 用 `create or replace` 覆寫。「migration 是歷史紀錄、已併入不可改」只在 COLLABORATION 與 agent 定義有字，沒有 gate（[LS-70](https://linear.app/little-sprout-app/issue/LS-70/harness並行後端票搶同一個本機-supabase-容器db-reset-互踩-隔離或序列化) 的撞號 gate 只比版本號，不管內容）。

## 規則

feature|fix|hotfix 分支相對於 target（development／main）的 merge-base：`supabase/migrations/` 下**在 base 已存在**的檔案不得 modified／renamed／deleted（`git diff --diff-filter=MRD --name-only <merge-base>...HEAD -- supabase/migrations/`）。新增（A）不管。逃生口：commit body 獨佔一行 `MIGRATION-REWRITE-APPROVED: LS-<n>`＋PR body 同段落（僅限尚未部署到正式站的檔——需人判斷，故要標記），與 [LS-45](https://linear.app/little-sprout-app/issue/LS-45/harness破壞性核可標記改整行錨定子字串比對可被無意提及滿足) 整行錨定同寫法。

## Gate

* `scripts/gates/migration-immutable-check.sh --base <ref>`：掛 push-gate（[LS-70](https://linear.app/little-sprout-app/issue/LS-70/harness並行後端票搶同一個本機-supabase-容器db-reset-互踩-隔離或序列化) 的 4b 之後、分級之前）＋CI Migration rules job（對 PR base）。正負自測 `.test.sh`（改既有檔→紅；新增→綠；帶逃生口→綠並印出宣告；改名→紅；bash 3.2 `${name}`）。
* §7 加列；COLLABORATION §6「雲端資料庫只透過 migration 變更」段補一句。

## 驗收

自測全綠；用 [LS-57](https://linear.app/little-sprout-app/issue/LS-57/產品決策owner-軟刪的內容可被作者直接還原diariesalbumscomments-系統性-是否記錄-deleted-by) R2 的實際 diff（改 030000 檔）重演被擋；hotfix 從 main 切。

## 留言（5）

### 2026-08-31T09:26:04.861Z · Chih-Lin Yeh · `2663d330`

## Handoff：LS-80

**Ticket**：LS-80 已併入 base 的 migration 檔不可變 gate

**已完成**
- 新增 `scripts/gates/migration-immutable-check.sh --base <ref> [--head <rev>] [--pr-body <file>]`：對本分支相對 target 的 merge-base，`git diff -M --diff-filter=MRD --name-status` 判定 `supabase/migrations/` 下已存在的檔是否被修改（M）／改名（R，舊路徑消失即算，內容不變也算）／刪除（D）；新增（A，base 端不存在）不受影響。
- 逃生口：commit body 獨佔一行 `MIGRATION-REWRITE-APPROVED: LS-<n>`（行錨定，同 LS-45／LS-50 寫法）；給 `--pr-body` 時同段落也須出現在 PR body，否則擋。
- 自測 `scripts/gates/migration-immutable-check.test.sh`（24 組）。
- 掛 push-gate.sh 第 4c 步（LS-70 撞號之後、分級之前）＋ CI `rules` job 的 Migration rules step（帶 `--pr-body`）＋ Gate 腳本自測 step。
- `docs/COLLABORATION.md`：§6「雲端資料庫只透過 migration 變更」段補一句；§7 對照表新增一列（狀態：⚠️ 混合——形式機械，核可真實性靠人工，同 DESTRUCTIVE-APPROVED 已知限制）。
- 3 支分段 commit：`5d42486`（gate+自測）、`9c7b451`（掛 push-gate/CI）、`92d97e5`（docs）。

**已驗證（怎麼驗的）**
- `bash scripts/gates/migration-immutable-check.test.sh` 全綠（24 組，含：純新增放行；修改／改名／刪除各自被擋；本分支自己新增又自己改不受限；`supabase/tests/` 目錄外不算；逃生口只在 commit body／給了 `--pr-body` 但未同步／兩邊都宣告三種情境；散文提及與粗體包起皆不算數仍擋；**LS-57 R2 實際 diff 形狀重演**——合成 repo 讓 hotfix 從 main 切，直接改已併入 main 的既有 migration `20260825030000_children_write_path_and_soft_delete.sql`（LS040→42501 同型），確認被擋；參數／ref 錯誤 fail closed）。
- `bash scripts/gates/push-gate.test.sh` 全綠（734 行既有自測無回歸——合成 repo 沒有 `supabase/migrations/`，本次新增的呼叫路徑不會被觸發）。
- 本機對真實 repo 跑 `bash scripts/gates/push-gate.sh`：`migration-immutable-check：無既有 migration 被修改／改名／刪除（對 origin/main）` 綠燈；無 Swift 變更，SwiftLint／unit tests 依 LS-76 跳過（CI 仍全跑）。
- PR body 先過 `bash scripts/gates/pr-body-check.sh`，通過後才 `gh pr create`。

**未完成／informational 處置**
- 本票尚未經 merge-reviewer 審查，目前沒有 informational 待處置；PR 待審查後若有 informational finding 會依規約逐條補處置到本欄（已修／記入 LS-96／另票／不修＋理由）。

**風險**
- 逃生口標記真實性（`MIGRATION-REWRITE-APPROVED: LS-<n>` 是否真的對應「尚未部署到正式站」）本 gate 只驗形式、不驗事實，同 `DESTRUCTIVE-APPROVED` 已知限制，靠 merge-reviewer／orchestrator 人工把關。
- CI 尚未跑（push 後立即交 handoff，不等 CI，依規約由 orchestrator 監看）。

**產出位置**
- PR：https://github.com/CLYEH/little-sprout/pull/191（hotfix/LS-80-migration-immutable → main）
- head SHA：`92d97e5`
- 新檔：`scripts/gates/migration-immutable-check.sh`、`scripts/gates/migration-immutable-check.test.sh`
- 改動：`scripts/gates/push-gate.sh`、`.github/workflows/ci.yml`、`docs/COLLABORATION.md`
- worktree：`.claude/worktrees/LS-80`（branch `hotfix/LS-80-migration-immutable`）
- 模擬器已關：無（本票全程未 boot 模擬器——無 Swift 變更，push-gate 依 LS-76 跳過 unit tests 這一段）


### 2026-08-31T09:33:00.161Z · Chih-Lin Yeh · `9953b139`

## merge-review R1 — REQUEST_CHANGES

**PR** #191 · head `92d97e56258c07f8fb08310f36ab7f05430f9116` · base `main` · 我審的即此 SHA。
四維度：race N/A（純 shell gate）／效能 N/A／平行 N/A／scope 乾淨。安全 gate 以「攻擊者繞路」視角逐項打。

### Finding 1 — typechange（T）繞路，gate fail-open【major】
`scripts/gates/migration-immutable-check.sh:71`
判定用 `git diff -M --diff-filter=MRD`。`--diff-filter` 的 **`T`（typechange）沒被收**——把一個既有 migration（regular file `100644`）換成 **symlink（`120000`）或 gitlink/submodule（`160000`）** 在 git 眼中是 T，不是 M/R/D，於是整條 gate 回 exit 0「無違規」。本機 push-gate 與 CI 用同一支腳本、同一組 filter，**兩端都漏**，沒有兜底。

**具體失敗情境（已實跑重現，非 PLAUSIBLE）**：
- 攻擊構造：`rm 20260101000000_init.sql; printf 'drop table foo; -- EVIL' > evil.sql; ln -s evil.sql 20260101000000_init.sql; git add -A; git commit` → `--base development` 回 **exit 0**。已 apply 到正式站的那條 migration，其**有效內容**已被改成 `evil.sql`（`supabase db reset` 讀檔會解到 symlink target），正是本 gate 要防的「同版本號、不同 SQL」drift，而 gate 綠燈。
- gitlink 變體（`update-index --cacheinfo 160000,…`）同樣 exit 0。
- raw 佐證：`:100644 120000 … T supabase/migrations/…`；`--diff-filter=MRD` 空輸出、`--diff-filter=MRDT` 才印 `T`。
- 對照：heavy-modify+rename（內容大改到 -M 配不上對）不會逃——舊路徑消失落在 **D**，被擋（我實跑 A' 確認 exit 1）。唯獨 T 這一類逃。

ticket 驗收明列 symlink 為要打的向量（(b)），此處命中且為 fail-open 方向，屬 gate 核心保證的破口。真實世界誤觸機率低（agent 不會無意 `ln -s` 一個 .sql），但這是本 gate 的對抗性 mandate 所在，且修法只有一行。
**建議修法**：`--diff-filter=MRD` → `--diff-filter=MRDT`；case 加 `T)`（如「類型變更（檔↔symlink／submodule）：${a}」，仍當違規）；`.test.sh` 補一組 typechange 負向樣本（現有 24 組沒覆蓋 T，正是漏網主因）。

### 通過的繞路嘗試（gate 正確）
- **rename 相似度**：大改內容＋改名，-M 配不上對時舊路徑仍以 D 被擋（A' 實跑）。R/D 兩路都收，fail-closed。
- **逃生口濫用**：散文提及／粗體／反引號／前後綴（test ⑪）皆不算；PR-body-only 無 commit body 宣告 → 仍擋（先查 `declared`，空即 exit 1，`--pr-body` 是 AND 非 OR，test ⑨/⑩）。行錨定同 LS-45。
- **fail-closed**：缺/壞 `--base`、`--head`、`--pr-body`、無共同祖先、未知參數、git 失敗 → 一律 exit 2（test ⑬＋推理）。
- **誤擋面乾淨**：純新增（A）綠、非 migration 檔綠、`supabase/tests/` 不算、合法逃生口（commit body＋PR body）綠（test ①⑤⑥⑦⑧⑩＋實跑）。
- **scope**：恰為 5 檔（check.sh＋.test.sh＋push-gate 4c wiring＋ci.yml rules＋COLLABORATION §6/§7），無順手改動。
- **自測真行為**：24 組含 LS-57 R2 實形狀重演（⑫，hotfix 改已併入 main 的 030000 檔被擋）＋負向控制（⑪）；掛 CI `rules` job 自測 step。全綠。

### Informational（入 LS-96 待辦池，不阻併）
- **i1 逃生口 branch-global**：一個 marker 解鎖本分支**全部** migration 改動、不逐檔核可——已於 §7 標 ⚠️ 混合、靠人工把關，設計取捨，記錄備查。
- **i2 本機 stale base**：push-gate 不自動 `git fetch`；若本機 `origin/$BASE` 落後於「引入該檔的 commit」，被改的既有檔會被算成 A 而本機漏擋（我實跑 stale base → exit 0 確認）。CI 對**新鮮** `origin/$BASE` 重驗為權威兜底，且腳本檔頭已註「先 git fetch origin」。屬既知本機限制，與其他 migration gate 一致。

### CI（`92d97e5`）
lint／ci／db／rules 四項全 pass（rules 3m14s，含本 gate 自測與 Migration rules step）。

**Verdict：REQUEST_CHANGES**（Finding 1 = major，typechange fail-open）。修掉 `--diff-filter=MRDT`＋case `T)`＋補 T 自測後可重審。
commit status `merge-review` = failure，綁 `92d97e5`。

### 2026-08-31T09:37:07.881Z · Chih-Lin Yeh · `c2fd712f`

## Handoff：LS-80 R1 修復

**Ticket**：LS-80（回應 R1 merge-review REQUEST_CHANGES，comment `9953b139`）

**已完成**
- Finding 1（major，`migration-immutable-check.sh:71`）：`--diff-filter=MRD` → `--diff-filter=MRDT`，收 typechange（既有 migration 從 regular file 換成 symlink `120000` 或 gitlink/submodule `160000`）。`case` 加 `T)` 分支，訊息印「類型變更（檔↔symlink／submodule）：<path>」，當違規處理。
- 補兩組自測（symlink 攻擊、gitlink 攻擊，用 R1 review 實跑的攻擊構造重現）：既有 migration 被換成 symlink（指向新寫的 `evil.sql`）→ 紅；`git update-index --cacheinfo 160000,...` 換成 gitlink → 紅。自測由 24 組增為 26 組，全綠。
- `docs/COLLABORATION.md` §7 對照表同步更新：規則敘述加 typechange／`MRDT`、自測組數改 26、informational i1（逃生口 branch-global）／i2（本機 stale-base 漏擋、CI 兜底）記錄在該列說明內。
- Commit `49e08f4`：`fix(harness): LS-80 R1 修 typechange 繞路（--diff-filter=MRD→MRDT）`。

**已驗證（怎麼驗的）**
- 修復前先用合成 repo 重現 R1 描述的兩種攻擊構造（symlink／gitlink 換掉既有 migration），確認舊版 `--diff-filter=MRD` 確實 exit 0（漏放）；改用 `--diff-filter=MRDT` 後兩者皆 exit 1（`T\tsupabase/migrations/...`）。
- `bash scripts/gates/migration-immutable-check.test.sh` 全綠（26 組，含新增的 ④b symlink／④c gitlink 兩組）。
- 本機對真實 repo 跑 `bash scripts/gates/push-gate.sh`：全綠，`migration-immutable-check` 步驟正常通過（本分支未動任何 migration）。
- push gate 本身（分支票號、merge 衝突預檢等）通過，已 push（`92d97e5..49e08f4`）。

**未完成／informational 處置**
- i1（逃生口 branch-global，一個 marker 解鎖本分支全部 migration 改動）：不修——設計取捨，已於 §7 該列說明內記錄「⚠️ 混合」的理由，orchestrator 記入 LS-96 待辦池。
- i2（本機 push-gate 不自動 fetch，`origin/$BASE` stale 時可能漏擋）：不修——與其他 migration gate（LS-70／LS-53）一致的既知限制，CI 對新鮮 `origin/$BASE` 重驗為權威兜底，已於 §7 該列說明內記錄，orchestrator 記入 LS-96 待辦池。
- 兩項 reviewer 明確標示「informational，記 LS-96，你不用動」，未額外開票。

**風險**
- 同 R1 前次 handoff：逃生口標記真實性仍靠 merge-reviewer／orchestrator 人工把關，本 gate 只驗形式。
- CI 尚未跑完（push 後立即交 handoff，不等 CI）。

**產出位置**
- PR：https://github.com/CLYEH/little-sprout/pull/191（hotfix/LS-80-migration-immutable → main）
- head SHA：`49e08f4`
- 改動：`scripts/gates/migration-immutable-check.sh`、`scripts/gates/migration-immutable-check.test.sh`、`docs/COLLABORATION.md`
- worktree：`.claude/worktrees/LS-80`
- 模擬器已關：無（本票全程未 boot 模擬器，無 Swift 變更、push-gate 依 LS-76 跳過 unit tests）


### 2026-08-31T09:41:20.207Z · Chih-Lin Yeh · `d71615af`

## merge-review R2 — APPROVE（增量）

**PR** #191 · head `49e08f400e0528201bbd3d9e8c6f2809cdfe5be1` · base `main` · 我審的即此 SHA（headRefOid 一致，未前進）。
增量審 `92d97e5..49e08f4`。R1 Finding 1（major，typechange fail-open）已修，重打攻擊構造確認擋住。

### R1 major 修法確認 — typechange 破口已補
`scripts/gates/migration-immutable-check.sh:75` `--diff-filter=MRD` → `MRDT`；case 加 `T)`（第 92 行，訊息「類型變更（檔↔symlink／submodule）：<path>」當違規）。
獨立重跑 R1 兩組攻擊構造（不靠 vendored 測試、自建合成 repo 對新 head 腳本）：
- **symlink swap**（`rm init.sql; ln -s evil.sql init.sql`）：raw `T\tsupabase/migrations/…`，gate **exit 1**（R1 為 exit 0）✓
- **gitlink swap**（`update-index --cacheinfo 160000,…`）：**exit 1** ✓

### 狀態碼完備性（commit-to-commit `-M` diff 下，MRDT 已完備）
逐碼判定，實跑佐證：
- **A**（新增）：排除正確，新檔本就放行。
- **C**（copy）：僅 `-C`/`--find-copies` 才會出現，本腳本只給 `-M`，故 C 永不出現；且複製既有檔＝原檔未動、副本落 A，非不可變違規（PROBE：cp 既有檔 → gate exit 0，正確）。
- **D/M/R/T**：MRDT 全收 ✓。附帶：mode-only 變更（chmod +x .sql）git 報 **M**，一樣擋（PROBE exit 1）；rename＋轉 symlink 的組合 git 拆成 D+A、落 **D** 擋（PROBE exit 1）。
- **U**（unmerged）：只存在於 conflict 期間的 index，commit..commit diff 不可能產生，排除正確。
- **B**（pairing broken，需 `-B`）／**X**（git 內部錯誤標記）：本情境不會出現。
結論：對「既有 migration 被實質改動」的威脅模型，MRDT 無漏收面。

### 兩組新自測驗到真行為
- ④b：真的 `ln -s` 建 symlink（指向新寫的 `evil.sql`）→ 斷言 exit 1＋訊息含「類型變更」，真行為非 mock。
- ④c：真的 `git update-index --cacheinfo 160000,…` 建 gitlink（submodule 形狀）→ 斷言 exit 1。
自測 24→26 組，本機全綠。

### scope
增量恰為四處：script diff-filter 加 T＋case `T)`＋comment、`.test.sh` 兩組（④b/④c）、COLLABORATION §7 一列（MRD→MRDT／24→26／i1/i2 記入該列）。無順手改動、無越界。

### informational
無新增。R1 的 i1（逃生口 branch-global）／i2（本機 stale-base 漏擋，CI 兜底）已由 orchestrator 入 LS-96，§7 該列已記錄，本輪不重複開票。

### CI（`49e08f4`）
lint／ci／db／rules 四項全 pass（rules 3m7s，含本 gate 自測與 Migration rules step）。

**Verdict：APPROVE**（無 blocker/major；R1 major 已修並經對抗重測，MRDT 完備，scope 乾淨，CI 綠）。
commit status `merge-review` = success，綁 `49e08f4`。

### 2026-08-31T09:42:29.042Z · Chih-Lin Yeh · `4f0a3b23`

**R2 APPROVE → 已併 main（2026-08-31）**：PR #191 merge，main=`f5ee98e`；merge-review=success 貼於 head `49e08f4`（R2 comment `d71615af`）。back-merge PR #192 已開（CI 綠後併）。i1/i2 已入 LS-96。

**Retro（harness 票輕量版）**：LS-57 收尾 retro 開的 gate（防「改已併入 migration」）落地。R1 reviewer 抓到 typechange（MRD 漏 `T`，symlink/gitlink 替換既有 migration 可繞）——「diff-filter 白名單型 gate 要涵蓋所有相關狀態碼」是通用教訓，自測原本 24 組無 T 覆蓋正是漏網主因。純 harness 免 dead-code。→ Done。migration ledger 現在有機械保護:改既有檔（含 typechange）即擋，只放行新增。

