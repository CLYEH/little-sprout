# LS-209 Harness：實作票 agent 防線與 push 韌性——ios-dev tools 白名單移除 `mcp__pencil__*`／禁派 fork＋patrol「Pen 開錯檔（實作票）」⚠、pre-push hook 期間 SSH keepalive（`core.sshCommand`）、CI 加 iPad 機型 job 跑 `*IPadTests`、handoff mutation 必附原文釘句、`agent-tools-check` 提示尾巴、`unused_import` 清理與規則；順修 LS-205 n1～n4／LS-207 N2～N6

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:M, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-05T20:58:43.417Z |
| 開始 | 2026-09-05T21:02:12.707Z |
| 完成 | 2026-09-06T01:33:12.467Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-209/harness實作票-agent-防線與-push-韌性-ios-dev-tools-白名單移除-mcp-pencil-禁派 |
| 關係 | related→LS-87, related→LS-85, related→LS-210, related→LS-197, related→LS-199, related→LS-139, related→LS-190, related→LS-205, related→LS-191, related→LS-188, related→LS-170, related→LS-192, related→LS-207, related→LS-96, related←LS-270, related←LS-271, related←LS-269, related←LS-266, related←LS-263, related←LS-260, related←LS-261, related←LS-259, related←LS-255, related←LS-256, related←LS-254, related←LS-245, related←LS-244, related←LS-242, related←LS-241, related←LS-240, related←LS-236, related←LS-132, related←LS-233, related←LS-232, related←LS-229, related←LS-231, related←LS-203, related←LS-226, related←LS-166, related←LS-225, related←LS-217, related←LS-216, related←LS-223, related←LS-221, related←LS-220, related←LS-219, related←LS-189, related←LS-193, related←LS-215, related←LS-212, related←LS-211 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項（2026-09-06 凌晨）依 harness 優先序合併（[LS-207](https://linear.app/little-sprout-app/issue/LS-207/harnessci-韌性與設計-gate-盲點修補-pr-body-check-linear-反查加重試且-rules) 進 QA 後 lane 空）：

* `cf3707fa`**（XS，二度事故）** [LS-188](https://linear.app/little-sprout-app/issue/LS-188/taskls-152-ios-設定頁-root個人家庭內容與安全法律帳號五區儲存空間頁0909b-已滿依-ls-152)／[LS-192](https://linear.app/little-sprout-app/issue/LS-192/taskls-152-ios-編輯顯示名稱與頭像02家庭成員管理0303-ax303-ipad03b-移除成員確認03c-轉移-owner) 實作票 agent 派 fork，fork 越權編輯他檔（[LS-188](https://linear.app/little-sprout-app/issue/LS-188/taskls-152-ios-設定頁-root個人家庭內容與安全法律帳號五區儲存空間頁0909b-已滿依-ls-152)）或用 Pencil MCP 把 Pen 切到票 worktree（[LS-192](https://linear.app/little-sprout-app/issue/LS-192/taskls-152-ios-編輯顯示名稱與頭像02家庭成員管理0303-ax303-ipad03b-移除成員確認03c-轉移-owner)），皆在 prompt 明說禁止之後發生——規則無 gate。
* `e818c3b3` **(1)（S）** pre-push hook 跑 push gate 8–10 分鐘期間 SSH 閒置，GitHub 斷線（[LS-191](https://linear.app/little-sprout-app/issue/LS-191/taskls-133-ios-法務文件-in-app-檢視-sheet使用條款隱私權政策bundled-markdown-渲染footer) R4 三次 push 才成功）。
* `00b9c9ef`**（S）** `*IPadTests` 以 `XCTSkipUnless(pad)` 在 CI iPhone 永不執行，iPad 分支死碼回歸（[LS-191](https://linear.app/little-sprout-app/issue/LS-191/taskls-133-ios-法務文件-in-app-檢視-sheet使用條款隱私權政策bundled-markdown-渲染footer) F1、[LS-188](https://linear.app/little-sprout-app/issue/LS-188/taskls-152-ios-設定頁-root個人家庭內容與安全法律帳號五區儲存空間頁0909b-已滿依-ls-152) B1 同型）無機械守門。
* `0e49f3e3`**（XS）** handoff「mutation 轉紅」三度失準（crash 被當紅、零鑑別力測試）。
* `c2bb8763`**（trivial）** `agent-tools-check.sh:189` 缺漏訊息固定尾巴（[LS-170](https://linear.app/little-sprout-app/issue/LS-170/harnessios-devreviewer-互動式本機驗證模擬器對本機-supabase-容器必須先-supabase-locksh) 字句）對 25 條規則誤導。
* `7b047d71`**（XS）** 未用 `import os` 約 17 個測試檔、`unused_import` 未開。
* 順修：[LS-205](https://linear.app/little-sprout-app/issue/LS-205/harness模擬器-runtime-與-ci-對齊-detect-simulatorsh-建專屬機時選-ci-同版-runtimeios) n1～n4（`f728958a` 後續）、[LS-207](https://linear.app/little-sprout-app/issue/LS-207/harnessci-韌性與設計-gate-盲點修補-pr-body-check-linear-反查加重試且-rules) N2～N6（`04e3d69c`／`6f0fe361`／`4c54a6d8`／`a0f34998`／`0c938e13`）——各自「一行可修就修，否則列不修理由」。

## 範圍

1. **實作票 agent 防線**：`.claude/agents/ios-dev.md` frontmatter `tools:` 移除 `mcp__pencil__*`（qa／merge-reviewer 保留唯讀 `get_app_state`／`read_skill`，`execute` 只留給 ui-designer／visual-reviewer）；正文硬規則「實作票不得 `pen-open.sh`、不得派 fork／subagent 改檔（平行只用 Explore 唯讀）」；`agent-tools-check.sh` 釘：ios-dev tools 不含 pencil execute、正文含該句；`patrol.sh` Pen 段：Pen 路徑落在 lane 非 design 的票 worktree → `⚠ Pen 開錯檔（實作票 LS-<n>）`＋自測樣本。
2. **push 韌性**：`session-start.sh`／worktree 建立時（`git worktree add` 包裝或 `.githooks` 安裝步驟）設 repo 層 `git config core.sshCommand "ssh -o ServerAliveInterval=30 -o ServerAliveCountMax=20"`（不改使用者全域）；push-gate 檔頭註明；自測驗 config 存在。
3. **CI iPad job**：`ci.yml` 新 job `ci-ipad`（`iPad Air 11-inch (M3)`，讀 `.ios-runtime`）只跑 `-only-testing:LittleSproutUITests/*IPadTests`＋`TapTargetGateTests/testSettingsViewRegular` 類 regular 案（列表由 grep `IPadTests` 自動產生）；required check 納入；push-gate 對 diff 含 `IPadTests`／`Regular` 的變更才跑 iPad（本機無 26.2 fail-open 同 [LS-205](https://linear.app/little-sprout-app/issue/LS-205/harness模擬器-runtime-與-ci-對齊-detect-simulatorsh-建專屬機時選-ci-同版-runtimeios)）。
4. **handoff mutation 原文**：ios-dev 定義「已驗證」欄硬規則（每支 mutation：改了什麼一行 → 哪條測試紅 → 斷言訊息原文；crash／build fail 不算紅）；merge-reviewer 定義「handoff mutation 一律自己重放」；`agent-tools-check.sh` 釘句。
5. **agent-tools-check 提示**：BODY_RULES 加第三欄「提示」（或中性字句），缺漏訊息不再帶 [LS-170](https://linear.app/little-sprout-app/issue/LS-170/harnessios-devreviewer-互動式本機驗證模擬器對本機-supabase-容器必須先-supabase-locksh) 尾巴；自測更新。
6. **unused_import**：全專案移除未用 `import os`／其他未用 import（約 17 檔）；評估 `.swiftlint.yml` 開 `unused_import`（analyzer 規則需 `swiftlint analyze`，CI 成本記入 handoff；成本 >2 分則只清理不開規則並記理由）。
7. 順修清單各自處置並在 handoff 逐條列出。

## 不做

角托 ref 判準現稿實跑（[LS-207](https://linear.app/little-sprout-app/issue/LS-207/harnessci-韌性與設計-gate-盲點修補-pr-body-check-linear-反查加重試且-rules) 範圍 2，另行）；CI runner 換機型以外的 iPad 版面工作。

## 驗收

- [ ] `agent-tools-check.test.sh`／`patrol.test.sh`／`push-gate.test.sh`（若動）綠＋各項 mutation；`agent-tools-check.sh` 對真實 agents 綠
- [ ] CI：本 PR 的 `ci-ipad` job 實跑綠並列出執行的測試；required check 已納入
- [ ] merge-reviewer 通過；hotfix 流程（自 main、PR 回 main、back-merge）

## 留言（12）

### 2026-09-05T22:41:28.668Z · Chih-Lin Yeh · `6b8e7f60`

**orchestrator 09-06 06:41**：handoff 收到，PR #330 head `2540bb4`（→ main，8 commit）→ In Review、派 merge-review R1（重點：`ios-dev` tools 白名單完整性與 Explore 唯讀是否矛盾、patrol 新段對現況 Pen 開在 LS-192 的實測、`ci-ipad` job 是否真列出執行的測試（CI 已顯示 pass，需看 log 非 0 tests）、push-gate iPad UDID 解析修正的 mutation、`7b047d71` 還原後 build 綠、24 個其他未用 import 的池項 id、§7 五列與程式一致）。併主後 orchestrator 待辦：`protection-apply.sh` 納入 `ci-ipad` required、back-merge PR、主 checkout `git pull` 讓新 agent 定義生效（在飛的 LS-190／LS-192 agent 仍用舊定義）。

### 2026-09-05T23:12:20.955Z · Chih-Lin Yeh · `3d16c1d1`

## merge-review R1 — REQUEST_CHANGES（2026-09-06 07:09）

PR #330 `hotfix/LS-209-agent-guard-push-resilience` → `main`；審的 head `2540bb443be92ff11007e855b0e549aae88d0764`（開審與收工兩次 `gh pr view 330 --json headRefOid` 皆同一 SHA，期間未前進）。base `6a5db27`，24 檔 +910/-92，8 個 commit。
CI 五項（`ci`／`ci-ipad`／`db`／`lint`／`rules`）在 run 33996467361 全 pass——但 `ci-ipad` 是「略過」的 9 秒 pass，見 B1。

### Blocker

**B1 `.github/workflows/ci.yml:785-851`（blocker）——`ci-ipad` 的「要跑」路徑從未被執行過，票文驗收第 2 條未達成，且未列為未完成**
- 事實：本 PR 的 `ci-ipad` job 執行 9 秒即結束，log 末行 `→ ci-ipad：略過（同 ci job 點擊目標 gate 判定：本 PR 無 LittleSprout/**/*.swift 或 LittleSproutUITests/** 變更）`（job 101387771072）。全 log 無任何 `Test Case … passed`、無 `以下測試將實跑：`。
- 本機也沒跑到：`push-gate.sh:618-651` 的 best-effort 需要 diff 檔名含 `IPad`／`Regular`（本 PR 的 Swift diff 只有 `LittleSproutTests/FamilyStoreRefreshQuotaRaceTests.swift`，不命中），且本機實測 `xcrun simctl list devices available` 沒有任何名為「iPad Air 11-inch (M3)」的機器（只有 `qa-test-iPadAir11M3`／`qa-LS98-iPadPro13`／`qa-test-iPadPro13`，皆 iOS 26.0）→ 就算觸發也是 ⚠ fail-open。
- 票文驗收：「CI：本 PR 的 `ci-ipad` job **實跑綠並列出執行的測試**；required check 已納入」——未達成；PR body／handoff 的「未完成」只寫了 required check 待套用，沒有揭露這一條（fail loud 違反）。
- 具體失敗情境：併入 main 後 orchestrator 跑 `protection-apply.sh test`／`main`，`ci-ipad` 成為 required check。第一次真正執行會發生在（a）下一張碰 `LittleSprout/**/*.swift` 的 PR，或（b）`promote.sh development→test` 的 push run（push 事件無條件跑）。此時才第一次驗證：`xcodebuild test -scheme LittleSprout`（無 `-project`）在 runner 上可解析、`SettingsViewIPadTests`／`TapTargetGateTests/testSettingsViewRegular` 在 iPad Air 11-inch (M3) + iOS 26.2 真的會綠、UI test runner 能附著。任一項不成立 → 所有 Swift PR 與晉升同時卡死，而且是在「已經是 required check」之後才發現。
- 建議修法（擇一，LS-139 有先例）：① 本 PR 加一處觸發判定集合內的無害改動（例如在 `LittleSproutUITests/SettingsViewIPadTests.swift` 補一行註解），讓本 PR 的 `ci-ipad` 真跑一次並在 PR body 貼出 `Test Case … passed` 清單；② 比照 LS-139 另開一支 `workflow_dispatch` workflow 實跑「要跑」路徑並附 run id。無論哪種，PR body 必須列出實跑的測試名。

### Major

**M1 `.github/workflows/ci.yml:177-215`（major）——`list-ipad-tests.test.sh` 沒掛進 CI `rules` job，但腳本檔頭與 §7 都聲稱已掛**
`rules` job 的自測是逐行明列（無 glob），第 194 行有 `agent-tools-check.test.sh`、196 行有 `push-gate.test.sh`，就是沒有 `list-ipad-tests.test.sh`；而 `scripts/gates/list-ipad-tests.sh:21` 寫「自測：scripts/gates/list-ipad-tests.test.sh（掛 CI rules job）」、COLLABORATION §7 新列也寫「自測 `list-ipad-tests.test.sh`」。失敗情境：日後有人動 `list-ipad-tests.sh` 的 awk 判準或把「清單為空 exit 1」改成 exit 0，CI 全綠；`ci.yml:838` 的 `list=$(…) || {…}` 依賴的正是那個 exit code，退化後 `only_args` 為空、`xcodebuild test` 變成「跑整個 scheme」或「什麼都沒篩」，iPad 守門靜默失效——這正是 00b9c9ef 要防的事。修法：在 `ci.yml:196` 附近加一行 `bash scripts/gates/list-ipad-tests.test.sh || rc=1`。

**M2 `scripts/gates/agent-tools-check.sh:42-51`（major）——新白名單完全沒有被必要工具表釘住，且註解與現況相反**
`RULES` 仍是 `ios-dev|`（必要工具留空），上方 42-44 行註解仍寫「ios-dev 沒有 tools: 行（繼承全部工具），這裡仍列一行（必要工具留空）：確保『無 tools: 行＝放行』這條路徑有樣本覆蓋」——本票已經給 ios-dev 加了顯式 `tools:`，這段註解說明的前提不再成立；實跑輸出也露出 `ios-dev.md：tools: 含必要工具（）` 這個空括號。失敗情境：日後有人編輯那條很長的 `tools:` 行時掉了 `Bash`（跑不了 push-gate／post-status）或 `mcp__linear__get_issue`（讀不了票），`agent-tools-check` 與 CI 全綠，要到下一次派工才炸——這正是本 gate 存在的理由（檔頭第 4-9 行自己寫的）。修法一行：`ios-dev|Bash Read Edit Write Grep Glob Agent mcp__linear__get_issue mcp__linear__list_comments`，並同步改掉 42-44 行註解（「無 tools: 行＝放行」的路徑覆蓋改由自測 fixture 保證，⑮ 已有該樣本）。

**M3 `scripts/ops/promote.sh:32`／`:131`＋`scripts/ops/protection-apply.sh:56`（major）——required check 清單兩處不同步**
`protection-apply.sh` 的 `CHECKS_OK`／`CHECKS_JSON` 已加 `ci-ipad`（test／main 共六個 context），但 `promote.sh:32` 仍是 `REQUIRED_CHECKS="ci db lint rules"`，成功訊息 `:131` 也硬寫「check ci／db／lint／rules 全綠」；`protection-apply.sh:56` 的註解仍寫「test／main：五個 context 齊、四個 Actions check@15368」（現在是六／五）。失敗情境：套用保護後，某個 development tip 的 `ci-ipad` 紅或未完成時，`promote.sh` 前置檢查全過並印「四項全綠」，接著 push 被 GitHub GH006 拒，只剩 `:126` 那句泛用訊息（「分支保護的 required checks 對該 SHA 未滿足？」）讓人自己猜是哪一項。方向是安全的（server-side 仍擋），但把可診斷的紅換成不可診斷的紅。修法：`REQUIRED_CHECKS="ci ci-ipad db lint rules"`＋訊息改成 `${REQUIRED_CHECKS// /／}`，並更新 protection-apply 的註解。

**M4 `.github/workflows/ci.yml:824`／`:831-833`（major）——`ci-ipad` 對 runtime 不一致 fail-closed，與 `ci` job 對同一情境的既有裁決（LS-205 i4）相反**
`ci-ipad` 的 awk 用 `if (os != pin) next` 硬性只收 `.ios-runtime` 釘住版，找不到就 `::error::` + `exit 1`；同一份 ci.yml 的 `ci` job（Build & Test 步驟）對 runtime 漂移**只印 `::warning::` 不擋**，並在註解明寫理由：「讓它變成強制擋反而會把『runner 廠內升版』的排程性事件錯誤地怪到當次 PR 頭上」。`ci-ipad` 的錯誤訊息還寫「比照 LS-205 i4」，但做的是相反的事。失敗情境：GitHub runner image 把 iPad 的 runtime 升到 26.3（目前實測 runner 是 26.2＝pinned，所以今天不會踩），`ci-ipad` 對每一張 Swift PR 與每一次晉升 push 直接紅，而它已是 required check → 整條產線停擺，直到有人改 `.ios-runtime`（改了又牽動本機 detect-simulator 的釘版行為）。建議：兩段式——先找 pinned runtime，找不到就退回「任何 runtime 的 iPad Air 11-inch (M3)」並 `::warning::`（與 `ci` job 一致），真的連機型都不存在才 error；若堅持 fail-closed，至少在 §7 該列寫明這是刻意與 i4 分歧。

### Minor

- **m1 `scripts/ops/protection-apply.sh:88-99`（minor）**：`ci-ipad` 只進 test／main 的 PUT，`development`（MODE=patch，contexts＝before ∪ merge-review）不會納入。結果：iPad 測試紅的 PR 仍可併進 development，要到晉升 test 才擋。其他四項 check 在 development 都是 required（實測 `gh api …/branches/development/protection` 回 ci/lint/rules/db/merge-review），本票讓 `ci-ipad` 成為唯一例外，PR body／§7 未說明是刻意還是遺漏。
- **m2 `scripts/gates/push-gate.sh:645-650`（minor）**：本機 iPad 補跑的 `xcodebuild test` 沒有包進 LS-199 的 `wd_run` 看門狗（同檔 `:578-586` 的 unit tests 有）。UI test 掛住／宿主 crash 是本 repo 有前科的失敗模式（LS-197／LS-199），一旦踩到就是無限卡在 `git push`、沒有摘要也沒有自動釋放鎖。修法：`wd_run` 包一層即可。
- **m3 `scripts/gates/push-gate.sh:632-640`（minor，實測）**：本機段以機型**名稱**字面 `iPad Air 11-inch (M3)` 比對，但本機（也是唯一開發機）的模擬器命名慣例是 `<票號>-<機型>`／`qa-*`（實測三台 iPad 都不叫這個名字），因此這段實務上永遠走 ⚠ fail-open——文件雖誠實寫了 fail-open，但等於這段本機補跑近乎死碼，「best-effort」的實際覆蓋率是 0。
- **m4 `scripts/ops/patrol.sh:407-441`（minor，效能）**：`design_wt=1` 時 `pen-status.sh` 已經回報了 Pen 目前路徑（實測輸出：`Pencil：行程 ✓ … 路徑 …/LS-192/design/littlesprout.pen`），新段又獨立呼叫 `pen-open.sh --status` 走第二次 `pen interactive` IPC（`poll_once` 單次上限 8s）。實測 `patrol.sh --brief` 全程 7.9s（SessionStart hook timeout 30s，尚有餘裕），但每 26 分鐘一輪＋每次 SessionStart 都多付一次可省的 IPC。建議：有 `PENCIL_LINE` 時直接沿用它解析出的路徑。
- **m5（minor，申報品質）**：handoff 的三段式「斷言訊息原文」抄的是測試檔裡**內建負控**的字串（含未展開的 `${got}`／`${rc}`，例如 `✗ ⑮ mutant 應 exit 0 且不印「含被禁工具」（實得 ${got}）`），不是任何一次實跑的輸出。我把 mutation 打在 production 腳本上重放，真正變紅的是別的斷言（見下方重放紀錄）。範圍 4 這條新規則要的就是「可核對的原文」，第一個踩到自己新規則邊界的就是本票；建議日後貼實跑輸出。
- **m6（minor，申報未落地）**：`swiftlint analyze` 額外發現的「24 個非 `import os` 的未用 import」只在 PR body 寫「建議另開票或併入下次動 `.swiftlint.yml` 的 harness 票」，既沒修也沒有 LS-96 comment id。我用 Linear API 撈了 LS-96 全部 338 則 comment，沒有任何一則提到這 24 個（只有原始池項 `7b047d71` 提到 `import os`）。依 handoff 規約應落地成池項 id 或另票。
- **m7 `docs/COLLABORATION.md`（§7 新列一，minor）**：狀態欄寫「機械擋不了『**orchestrator 派的** fork／general-purpose 子 agent 自帶完整工具集』」，但 ios-dev 白名單保留了 `Agent`，**ios-dev 自己派的 fork 一樣繞得過**——LS-188／LS-192 兩起正是 ios-dev 派的 fork。擋不住的邊界描述漏了主要那半，建議照實補。
- **m8 `docs/COLLABORATION.md`（§7 新列四，minor）**：`ci-ipad` 那列狀態欄只寫「required check 待 orchestrator 套用」，沒寫「本票的『要跑』路徑從未實跑過」這個更大的盲區（LS-139 那列就把「本 PR 走略過路徑、要跑路徑由 dispatch run 自證」寫進去了）。跟 B1 一起修。

### Informational

- i1：ios-dev 白名單移除了 `BashOutput`／`KillShell`／`TodoWrite`／`WebFetch`／`WebSearch`／`mcp__supabase__*`（唯讀那組 qa 有）／`mcp__linear__save_comment`。都會顯性失敗不是靜默，但正文仍寫「需要並行才用背景」，而沒有 `BashOutput` 就只能靠重導＋`Read`——正文那句與白名單略有張力，值得順手註明。
- i2：`Agent` 這個工具名與 mobile-mcp 的 32 個工具名沒有任何 gate 能驗（gate 檔頭已誠實聲明此盲區）。交叉比對：ios-dev 的 mobile-mcp 集合與 qa.md 完全相同（程式比對，差集雙向皆空），`Agent` 用語與 COLLABORATION §1「以 Agent 工具的 model 參數覆寫」一致，可接受。
- i3：`patrol.sh --json` 只把 `[Pen] 開錯檔` 進 `flags`，「lane ?（查詢失敗，不擋）」那行在 json 模式看不到（brief／human 有）。

### Mutation 重放（5 支，全部打在 production 腳本上、跑真自測）

| # | 改了什麼一行 | 實際變紅的斷言（原文） |
|---|---|---|
| 1 | `agent-tools-check.sh` `FORBIDDEN_RULES="ios-dev\|mcp__pencil__"` → `FORBIDDEN_RULES=""` | `✗ ⑮ ios-dev tools: 含 mcp__pencil__execute → exit 1（期望 exit 1、輸出含「ios-dev.md：tools: 含被禁工具（字首「mcp__pencil__」）」，實得 0）`（另 ⑮ 其餘 3 條與 ① 同時紅，自測整體 exit 1） |
| 2 | `patrol.sh` awk 拿掉 `LS209-PEN-WRONG-START/END` 整段 | `✗ ㉖(a) human 有「Pen 開錯檔偵測」段並印 ⚠（輸出應含「⚠ Pen 開錯檔（實作票 LS-777）」）`＋㉖(a) brief 兩條、㉖(c) 兩條；且拿掉整段後腳本仍正常結束（`PEN_WRONG_LINE` 宣告放在區塊外，`set -u` 沒炸——這個設計是對的） |
| 3 | `list-ipad-tests.sh` 清單為空的 `exit 1` → `exit 0` | `✗ ⑤ 有檔但無比對 → exit 1（fail loud）（期望 exit 1，實得 0）` |
| 4 | `push-gate.sh` `if [ "$ipad_trigger" -eq 1 ]; then` → `if false; then` | `✗ ㊴(a) diff 含 IPad 檔名卻沒有正確呼叫本機 iPad 測試`＋`✗ ㊴(b) diff 碰 SUT 卻沒有觸發本機 iPad 測試`＋`✗ ㊴(d) 本機無 iPad 模擬器時應 fail-open（實得 exit 0）` |
| 5 | `push-gate.sh` UDID 解析改回 naive 取括號（`sub(/^[^(]*\(/…)`） | `✗ ㊴(a) diff 含 IPad 檔名卻沒有正確呼叫本機 iPad 測試`＋`✗ ㊴(b) …`——確認 handoff 說的「機型名自帶括號會把 M3 當 UDID」這個坑真的被 ㊴ 釘住 |

結論：四支新 gate 的判定邏輯**都是真的載重**，mutation 不是零鑑別力。

### 逐條查實

- **申報 SHA ↔ 檔案**：8 個 commit 逐一 `git show --stat` 對過，與 handoff／PR body 的對應完全一致（`2cf2e14` agents+gate+自測／`b3743bf` patrol 三檔+自測／`207da83` session-start+push-gate 檔頭+自測／`4dabd75` ci.yml+list-ipad-tests+push-gate+protection-apply／`53166da` 兩份 agent 定義+gate 釘句／`1c3274f` 單檔刪一行／`e7f0a94` 順修八檔／`2540bb4` §7 五列）。
- **LS-96 池項 id**：`cf3707fa`／`e818c3b3`／`00b9c9ef`／`0e49f3e3`／`c2bb8763`／`7b047d71` 六則全部在 LS-96 查得到（撈全部 338 則 comment 比對 id 前綴）。新的 24 個未用 import 未入池 → m6。
- **`7b047d71` 的「原判斷幾乎全錯」**：成立。池項點名的 `LittleSproutTests/UploadQueueStoreTests.swift:3` 實際在 `:88` 用了 `OSAllocatedUnfairLock`；我對全 repo 27 個仍有 `import os` 的檔逐檔掃 os 符號（`OSAllocatedUnfairLock`／`os_log`／`Logger(`／`OSLog`…），**零個**是未用的。刪掉的那一個檔由 `ci` job 的 Build & Test 綠背書（該 job 編譯 `LittleSproutTests`）。
- **不修理由**：LS-205 n2（`rt_mismatch_simulators` 命名）——該欄位確實是 `patrol.sh:753` 的 `--json` 輸出欄位，改名是契約破壞，理由成立；n3（㊳ 時序依賴）、n4（CI `::warning::` 分支無法單元測試）、LS-207 N5（`kill -0` 邊界維持 `a0f34998` 裁決）——皆為既有已知盲區，方向安全（fail loud 而非假綠），成立。
- **範圍 2（SSH keepalive）**：實測本機 `.git/config:9` 已有 `sshCommand = ssh -o ServerAliveInterval=30 -o ServerAliveCountMax=20`、`git config --global --get core.sshCommand` 無值、`extensions.worktreeConfig` 未開（所以全 repo 共用一份，worktree 免設）——與宣稱一致；冪等與「不動全域」由 ⑥b 11 條斷言＋mutation 釘住；`push-gate.sh:5-12` 檔頭有說明與手動退路。`session-start.sh` 是 `set -uo pipefail`（無 `-e`），`:81` 的 `[ -n "$ssh_note" ] && …` 回 1 不會中斷腳本，且有 EXIT trap 兜底——確認過沒有 fail-soft 破口。
- **範圍 1 patrol 實測**：在主 checkout 上跑 HEAD 版 `patrol.sh --brief --repo /Users/clyeh/little-sprout`，輸出 `⚠ Pen 開錯檔（實作票 LS-192）` 與 `[Pen] 開錯檔（實作票 LS-192；lane=lane:ui，非 lane:design…）`——偵測正確。註記：handoff／PR body 兩處寫「LS-192 lane:backend」，實查是 **lane:ui**（不影響判定，但申報文字要更正）。lane 查不到的 fail-open 樣本在 `patrol.test.sh` ㉖(c)（`LANE_STUB_RC=3`）有，且 `--lane` 的 exit 3 路徑在 `patrol-linear.test.sh` ⑥c 有。
- **qa／merge-reviewer 的 pencil 唯讀工具**：保留（`qa.md` 仍有 `get_app_state`／`execute`／`read_skill`），`FORBIDDEN_RULES` 只針對 ios-dev，未誤傷。
- **順修與 orchestrator 追加兩條**：`COLLABORATION.md:317` 已改 `if: ${{ !cancelled() }}`，與 `ci.yml:173`／`:279` 實際寫法一致（來源 commit `53e6db3`）✓；`patrol.test.sh` 尾端重複的 LS-903 清理已移除、清理只留在 `:1189-1190` 該測試段落後 ✓。
- **自測實跑（我自己跑，全綠）**：`agent-tools-check.test.sh`（99 組）、`agent-tools-check.sh` 對真實 `.claude/agents/`（6 份、正文 28 條）、`list-ipad-tests.test.sh`、`patrol.test.sh`（303 個 ✓，1m57s）、`patrol-linear.test.sh`、`push-gate.test.sh`、`simulator-lock.test.sh`、`detect-simulator.test.sh`、`supabase-lock.test.sh`、`pr-body-check.test.sh`（91 組）。`list-ipad-tests.sh` 對真 repo 輸出兩筆：`LittleSproutUITests/SettingsViewIPadTests`、`LittleSproutUITests/TapTargetGateTests/testSettingsViewRegular`。
- **四維度**：本票無 Swift 併發／DB 改動（唯一 Swift 變更是刪一行 import）。race／RLS／N+1 無適用面。效能面只有 m4（patrol 多一次 Pen IPC）。scope：所有變更都能對回票文範圍 1-7 或 orchestrator 追加的兩條，**沒有夾帶**。

### 未做／跳過（誠實聲明）

- 沒有實跑 `xcodebuild test` 的 iPad 測試（本機無該機型、且非本次 review 的必要條件）——B1 要的證據該由實作端補。
- 沒有重跑 `swiftlint analyze`（handoff 的 267.71s／25 violations 數字採信、未獨立覆核）。
- 沒有碰 Pen／Pencil MCP；Supabase lock 未持有（本票不涉及本機容器）。
- review worktree 兩個（審查用＋mutation 用）皆以 `git worktree remove --force` 清掉，`git worktree list` 已確認乾淨。

### Verdict：**REQUEST_CHANGES**（1 blocker、4 major）

R2 只需回頭處理 B1／M1／M2／M3／M4＋minor 擇要，其餘已審過的部分（四支 gate 的判定邏輯、SSH keepalive、順修八檔）我這輪重放過、不需要重審。

### 2026-09-05T23:14:43.196Z · Chih-Lin Yeh · `8a9b79f0`

**orchestrator 09-06 07:14 裁決（merge-review R1 3d16c1d1）**：B1 `ci-ipad` 從未實跑——採「自變更觸發」：`ui-test-trigger`／`ci-ipad` 判定集合納入會影響 iPad job 的檔（ci.yml、list-ipad-tests.sh、ui-test-trigger.sh、.ios-runtime、project.yml），本 PR 下一次 push 即真跑，驗收＝job log 逐行列出 `*IPadTests` passed（0 tests／全 skipped 不算）；退路為 LS-139 先例的無害 UITest 改動，不得只靠 workflow_dispatch。M4 改為與 `ci` job 相同的 `::warning::`（LS-205 i4 既有裁決）。M1／M2／M3 照 reviewer。informational：白名單加回 `mcp__linear__save_comment`；`BashOutput`／`KillShell` 不加回、正文改「長命令一律前景＋timeout、不用背景 Bash」；其餘維持移除。m5：handoff mutation 原文一律實跑輸出。已派原 agent R2；R2 push 後派 merge-review R2。

### 2026-09-06T00:10:16.889Z · Chih-Lin Yeh · `7e6b76ed`

**orchestrator 09-06 08:09**：R2 handoff 收到，PR #330 head `f6b8b13`（5 個新 commit）。B1 證據：run 33999986630 的 `ci-ipad` 以「diff 觸及 iPad job 機制本身的檔」觸發，iOS 26.2 iPad Air 11-inch (M3) 上 7 個 Test Case 逐行 passed（`SettingsViewIPadTests` ×6＋`testSettingsViewRegular`）；M1–M4 修、m6 池項 `c123d8dd`、LS-192 lane 更正為 ui。已派 merge-review R2（同 reviewer 續接；重點：觸發判定不會讓每個 PR 都跑 iPad、RULES 表＋自測、M4 warning、mutation 原文為實跑輸出）。

### 2026-09-06T00:24:59.837Z · Chih-Lin Yeh · `719ab233`

## merge-review R2 — APPROVE（2026-09-06 08:23）

審的 head `f6b8b13ea72ddbef90c34c19a44ffb21be860fed`（開審與收工兩次 `gh pr view 330 --json headRefOid` 皆相同）。R2 delta＝`2540bb4..f6b8b13` 4 個 commit／12 檔 +183-59（`7e34e51`／`0cc1879`／`bebf9fb`／`f6b8b13`）。CI run 33999986630 五項全 success（`ci` 5m20s／**`ci-ipad` 8m03s**／`db`／`lint`／`rules`）。R1（`3d16c1d1`）的 1 blocker＋4 major＋8 minor＋3 informational 逐條查實如下，**全部處置到位，無新增 blocker／major**。

### B1（blocker）→ 已解，我自己看 log 核對過

`gh run view 33999986630 --job 101396997377 --log`（job `ci-ipad`，conclusion success）實際輸出：
- 觸發：`→ ui-test-trigger：略過——變更 26 個檔皆不在判定路徑集合…` 之後緊接 `→ ci-ipad：要跑——diff 觸及 iPad job 機制本身的檔「.github/workflows/ci.yml」（…merge-review R1 B1）`
- 機器：`simulator: iPad Air 11-inch (M3) 173E89AD-3DFF-434D-888B-33B580E54CDA iOS 26.2（pinned 26.2）`
- **7 個 Test Case 逐行 passed**：`SettingsViewIPadTests` 六支（`testAccountSectionDeleteRowPushesAndBackReturns` 115.008s／`testContentSafetySectionStorageRowPushesAndBackReturns` 19.598s／`testFamilySectionInviteRowRegressionPushesAndBackReturns` 16.377s／`testLegalSectionSwitchesWithoutPush` 10.099s／`testProfileSectionEntryPushesAndBackReturns` 13.541s／`testSidebarSelectionIsAccessibleAndDistinguishable` 9.149s）＋`TapTargetGateTests.testSettingsViewRegular` 7.332s
- 收尾：`Executed 7 tests, with 0 failures (0 unexpected) in 191.105 seconds`、`Test Suite 'Selected tests' passed`、`** TEST SUCCEEDED **`
→ 不是 0 tests、不是全 skipped，票文驗收第 2 條的前半（實跑綠並列出執行的測試）**已達成**。

**觸發判定不會讓每個 PR 都跑 iPad**——我把 `ci.yml:818-831` 那段原文抽出來，配假 `ui-test-trigger.sh`（回指定 rc）與假 `git`（回指定 diff）實跑七種情境：

| 情境 | 實跑輸出 |
|---|---|
| rc=3、diff＝`docs/x.md`＋`scripts/ops/patrol.sh` | `→ ci-ipad：略過（…也未動 iPad job 機制本身…）`（**未進到測試段**） |
| rc=3、diff 含 `.github/workflows/ci.yml` | `→ ci-ipad：要跑——diff 觸及 iPad job 機制本身的檔「.github/workflows/ci.yml」` → 進測試段 |
| rc=0（碰 `LittleSprout/Features/X.swift`） | 進測試段（沿用原判定） |
| rc=3、diff＝`scripts/gates/list-ipad-tests.sh` | 要跑 |
| rc=3、diff＝`.ios-runtime` | 要跑 |
| rc=3、diff＝`scripts/gates/list-ipad-tests.**test**.sh` | **略過**（正則整行錨定，`.test.sh` 不在集合內——判定夠窄） |
| rc=2（trigger 腳本壞掉） | `::warning::ui-test-trigger.sh exit 2（非 0／3），無法證明可略過，保守照跑` → 要跑 |

判定集合是五個明確檔名的整行錨定正則，純 `docs/`／`supabase/`／`scripts/ops/`／`.claude/agents/` 的 PR 仍然略過——**沒有退化成「每個 PR 都跑」**。成本面：ci.yml 是 harness 票的常客，這類 PR 之後會多付 ~8 分鐘（與 `ci` job 平行、不在關鍵路徑上），我認為對「改到 job 本身就要驗一次」是合理代價。

### M1–M4：逐條查實

- **M1**：`ci.yml:195` 已加 `bash scripts/gates/list-ipad-tests.test.sh || rc=1`；**CI 端證據**——rules job log 有 `✓ list-ipad-tests 自測通過`（2026-09-05T23:58:19Z），不只是加在檔案裡。
- **M2**：`agent-tools-check.sh:51` 改成 `ios-dev|Bash Read Edit Write Grep Glob Agent ${LINEAR3}`，過時註解已刪並改寫成「無 tools: 行＝放行由 ui-designer／visual-reviewer fixture 覆蓋」。自測新增三個樣本，我本輪實跑輸出：`✓ ② M2：ios-dev 有 tools: 行但缺必要工具 → exit 1（regression：曾經規則表無要求，本票起要求）`／`✓ ② M2：ios-dev 缺 mcp__linear__save_comment（i1：白名單加回）→ exit 1`／`✓ ② M2：ios-dev 具備完整必要工具（含三支 Linear）→ 通過`。真實 agents 仍綠（`✓ agent-tools gate 通過（6 份 agent 定義；正文必含字樣 28 條）`），且不再出現 R1 那個空括號訊息。
- **M3**：`promote.sh:34` `REQUIRED_CHECKS="ci ci-ipad db lint rules"`、`:133` 成功訊息改讀 `${REQUIRED_CHECKS// /／}`；`promote.test.sh` 的 `ALL_GREEN` 與 ⑧ fixture 同步補 `ci-ipad`（否則既有全綠樣本會假紅）——`✓ ⑧ 全綠（ci 舊紅新綠）→ exit 0`、`✓ promote 自測通過（17 組樣本）`。`protection-apply.sh:56-63` 註解改「六個 context／五個 Actions check」。
- **M4**：改成兩段式。我把 `ci.yml:837-866` 的 `find_ipad_udid()`＋fallback 原文抽出來配假 `xcrun` 實跑：
  - runner 有 26.2（pinned 命中）→ `simulator: … iOS 26.2（pinned 26.2）`，exit 0，無 warning
  - runner 只有 26.3 → `::warning::CI runner 上找不到 iOS 26.2 的「iPad Air 11-inch (M3)」，改用 iOS 26.3（同一機型的其他 runtime）……（同 ci job LS-205 i4 既有裁決，不 fail-closed）`＋`simulator: … iOS 26.3（pinned 26.2）`，**exit 0 繼續跑**
  - runner 完全沒有該機型 → `::error::找不到任何 runtime 的「iPad Air 11-inch (M3)」模擬器…`，exit 1
  與 `ci` job 的 LS-205 i4 裁決一致，runner image 升版不再變成擋住產線的紅。

### 三支 mutation 重放（皆打在 production 文本上，輸出為實跑原文）

| # | 改了什麼一行 | 實跑結果 |
|---|---|---|
| 1（新觸發判定） | `ci.yml` 判定集合正則 → `^ZZZNOPE$` | 原本「rc=3＋diff 含 ci.yml → 要跑」的樣本翻回 `→ ci-ipad：略過（…也未動 iPad job 機制本身…）`，不再進測試段 → **新判定確實載重** |
| 2（RULES 表） | `agent-tools-check.sh` `ios-dev|Bash Read … ${LINEAR3}` → `ios-dev|`（還原成 R1 的留空） | `✗ ② M2：ios-dev 有 tools: 行但缺必要工具 → exit 1（regression：曾經規則表無要求，本票起要求）（期望 exit 1、輸出含「ios-dev.md：tools: 缺」，實得 0）`＋`✗ ② M2：ios-dev 缺 mcp__linear__save_comment（i1：白名單加回）→ exit 1（期望 exit 1、輸出含「ios-dev.md：tools: 缺 mcp__linear__save_comment」，實得 0）`＋`✗ agent-tools-check 自測失敗`（同輪另有一條 `✗ ① 真 repo 應通過（實得 1）`是我沙箱 cwd 落在主 checkout、其 `ios-dev.md` 仍是舊版所致，**非** mutation 造成，特此聲明） |
| 3（M4 warning 分支） | `found="$(find_ipad_udid "")"` → `found=""`（拿掉退回任一 runtime 的 fallback） | 「runner 只有 26.3」情境從 warning＋exit 0 翻成 `::error::找不到任何 runtime 的…` exit 1 → **warning 分支確實載重、不是死碼** |

### m1–m8 逐條

- **m1**：不加到 development 是**刻意**，理由寫進 `protection-apply.sh:60-63`（development 是快速整合分支，比照 `qa` status 也只在 test→main 才要求；iPad 紅的 PR 可進 development、晉升 test 時才擋），§7 該列也同步寫了。裁決一致、記錄得住，接受。
- **m2**：`push-gate.sh:647-657` 抽成 `run_ipad_best_effort()` 並 `wd_run` 包起來；自測 `✓ ㊴(m2) iPad best-effort 已包進 wd_run 看門狗——「看門狗啟用」印兩次（unit tests＋iPad best-effort 各一次）`。
- **m3**：機型比對改 `/iPad Air 11-inch \(M3\)|iPadAir11M3/`（literal 或 slug 皆命中），自測 `✓ ㊴(e) 本機專屬機叫 qa-test-iPadAir11M3（不叫原生機型名）→ slug 判準命中、正確呼叫本機 iPad 測試`。
- **m4**：`patrol.sh:422-434` 改為優先沿用 `PENCIL_LINE` 已解析的路徑。除了自測 ㉖(g)，我另外對**真實環境**驗了一次：把 `PATROL_PEN_OPEN_SH` 換成會記錄呼叫的假身跑真 repo，輸出仍 `⚠ Pen 開錯檔（實作票 LS-192）`，而 `pen-open.sh` 呼叫次數＝**0**——省下的第二次 IPC 是真的省掉了，且沿用路徑在**真實 pen-status 輸出格式**下解析正確（不是只有 fixture 能跑）。
- **m5**：R2 handoff 的每支 mutation／驗證都改貼實跑輸出，我逐條回跑核對，引用字串與我自己的執行結果一字不差（`② M2` 三條、`⑧ 全綠`、`㊴(e)`、`㊴(m2)`、`㉖(g)` 兩條）。
- **m6**：LS-96 池項 `c123d8dd-a211-4db7-801e-bcfe904b3ab4` 已存在（我用 Linear API 撈 342 則 comment 核對），內容含 24 個檔的 `檔案:行號` 清單與「須逐檔驗證才能刪」的警告；PR body `pr-body-check --verify` 也反查到它（`✓ 第 38 行 LS-96 comment c123d8dd 存在`）。
- **m7**：§7 第 318 行已改成「機械擋不了『**ios-dev 自己派的 fork**／general-purpose 子 agent 自帶完整工具集』（LS-188／LS-192 兩起事故正是 ios-dev 自己派的，不是 orchestrator 派的）」。
- **m8**：§7 第 321 行狀態欄補上「『要跑』路徑本輪已靠自變更觸發真的執行過一次，證據見 R2 handoff 的 CI log」，gate 欄也補了新判定集合、warning 退回、`promote.sh` 同步、`wd_run`、slug 等全部 R2 修正。
- **i1**：白名單加回 `mcp__linear__save_comment`（ios-dev tools 共 33 支）；正文第 22 行同步改成「**不使用背景 Bash**——tools 白名單沒有 `BashOutput`／`KillShell`（LS-209 起移除），背景化後既無法回頭看輸出、也無法中止」，R1 指出的「正文允許背景 vs 白名單缺 BashOutput」矛盾已消除。**i2／i3** 依 handoff 不修，理由成立。

### 其他查實

- **LS-192 lane 更正**：PR body 第 9 行已訂正為 `lane:ui`（原誤記 backend）；我本輪再跑一次真實 patrol，輸出 `[Pen] 開錯檔（實作票 LS-192；lane=lane:ui，非 lane:design…）`，一致。
- **順修兩條仍在**：`COLLABORATION.md` 的 `!cancelled()` 仍在（與 `ci.yml:173`／`:279` 一致）；`patrol.test.sh` 尾端沒有重複的 LS-903 清理。
- **自測本輪重跑（我自己跑，全綠）**：`agent-tools-check.test.sh`（101 組）＋對真實 `.claude/agents/`、`list-ipad-tests.test.sh`、`promote.test.sh`（17 組）、`patrol.test.sh`（305 個 ✓）、`push-gate.test.sh`（含 ㊴(a)–(e)／(m2)）。`detect-simulator`／`simulator-lock`／`supabase-lock`／`patrol-linear`／`pr-body-check` 在 R2 delta 未被更動，沿用 R1 我跑過的綠（CI rules job 本輪亦全綠）。
- **PR body**：`pr-body-check.sh <body> --branch hotfix/LS-209-agent-guard-push-resilience --verify` exit 0（19 條「已修」SHA、4 條 LS-96 反查全過）。
- **scope**：R2 的 12 檔全部對得回 R1 findings 或 orchestrator 裁決（`8a9b79f0`），無夾帶。四維度：無 Swift 併發／DB 面；效能面 m4 已改善。

### 剩餘 informational（不擋合併）

- **I-a（新，minor／PLAUSIBLE）**：`push-gate.sh` 的 slug 比對 `iPadAir11M3` 會連 `qa-*`／`demo-*` 這類持久機一起命中（自測 ㊴(e) 用的正是 `qa-test-iPadAir11M3`）。今天不會踩到——本機那台 qa iPad 是 iOS 26.0、被 `.ios-runtime`＝26.2 的過濾擋掉；但只要哪天有人建了 pinned runtime 上的 `qa-*` iPad，push-gate 就會借用 QA agent 的持久機跑 UI 測試（`simulator-lock` 的鍵是 iPhone 目的地的 lock dir，不與 iPad 互斥），且跑完不會關機。建議日後排除 `qa-*`／`demo-*` 前綴或優先挑 `LS-<n>-*`。
- **I-b**：M4 的 fallback 取 `simctl list` 裡第一台命中的機器（實務上是最舊的 runtime），非「最新可用 runtime」。warning 路徑，影響小。
- **I-c**：`patrol.sh` 沿用 `PENCIL_LINE` 的路徑解析依賴 `… · 路徑 <path> · …` 這個字面格式；`pen-status.sh` 改措辭時會靜默退回舊的 `pen-open.sh --status` 路徑（fail-open、不會誤判），可接受。

### Verdict：**APPROVE**

### orchestrator 併主後待辦

1. `bash scripts/ops/protection-apply.sh test` 與 `bash scripts/ops/protection-apply.sh main`——把 `ci-ipad` 加進 required checks（腳本會回讀 verify；目前實測兩支分支仍只有 ci/db/lint/rules/merge-review）。**做完之前 `promote.sh` 已經會要求 `ci-ipad` 綠**（R2 已同步 `REQUIRED_CHECKS`），這是好事、不衝突。
2. hotfix 流程收尾：`main` → `development` back-merge 一支。
3. 主 checkout `git pull --ff-only origin main`——新的 `ios-dev.md`／`merge-reviewer.md` 定義與 `agent-tools-check.sh` 才會對之後派的 agent 生效（在飛的 LS-190／LS-192 仍用舊定義）。`.claude/settings.json` 未變動，不需要 `/hooks` 重載；`session-start.sh` 的 SSH keepalive 下次 SessionStart 自動生效。
4. 順手處理巡檢一直在叫的 `⚠ Pen 開錯檔（實作票 LS-192）`（Pen 目前仍停在 `.claude/worktrees/LS-192/design/littlesprout.pen`）——這是 R1 就存在的既有殘留，不是本票造成。

### 2026-09-06T00:27:40.483Z · Chih-Lin Yeh · `a9eb6d7e`

**orchestrator 09-06 08:27**：merge-review R2 APPROVE（719ab233，status 貼 f6b8b13；六項 check 全 pass 含 `ci-ipad`）。收尾鏈啟動：merge #330 → main tip 貼 `promote: no content diff` → 主 checkout `git pull`（新 agent 定義生效；在飛的 LS-190／LS-210 agent 仍用舊定義）→ `protection-apply.sh test`／`main` 納入 `ci-ipad` required（development 依 m1 裁決不強制）→ back-merge PR main→development → dev tip 貼 status。之後 dev tip CI 綠再 promote dev→test（test 目前由 LS-192 QA 持有，落地後做 harness delta QA）→ sweeper → Done。informational I-a／I-b／I-c 記入 LS-96（下一則 comment）。

### 2026-09-06T00:45:30.982Z · Chih-Lin Yeh · `7b884787`

**orchestrator 09-06 08:45**：收尾鏈完成——PR #330 併入 main `05b07eb`（merge-review status 沿貼）、主 checkout 已 pull（新 `ios-dev` 定義自下一次派工生效）、`protection-apply.sh test`／`main` 成功（`ci-ipad` 已列 required）、back-merge PR #332 併入 development `913c91a`（status 沿貼）。接續：dev tip CI（含 `ci-ipad`）綠 → promote dev→test → harness delta QA（本票 delta；LS-192 QA 結果另計）→ sweeper → Done＋cleanup。

### 2026-09-06T01:08:11.143Z · Chih-Lin Yeh · `95512b4f`

**orchestrator 09-06 09:07**：development 913c91a（back-merge #332）已晉升 test → 票進 QA，派 harness delta QA（基準 b5f87b9＝LS-192 QA PASS 88fb24bc；驗自測全綠、agent 定義字樣、patrol Pen 段實跑、sshCommand 本機／全域、`ci-ipad` job 與 test／main required contexts、1 個 unused import 移除後 build 綠、delta 無 app／migration／.pen）。PASS 後 promote test→main 打版（與 LS-192 同版）→ sweeper 已由 R1／R2 覆蓋之外另派 → Done＋cleanup。

### 2026-09-06T01:14:00.699Z · Chih-Lin Yeh · `5bd56a44`

## dead-code sweep — LS-209

（2026-09-06 09:12，唯讀巡查，未改動任何檔案；範圍＝`git diff 6a5db27..05b07eb --stat`＝26 檔 +1047/-105）

### Findings（本 feature 引入）

**無死碼。** 逐項核對如下，全部有讀者／有呼叫點／已接進自測或 CI：

1. **`scripts/gates/agent-tools-check.sh`**：`FORBIDDEN_RULES="ios-dev|mcp__pencil__"`（單一規則）在 R1 mutation（清空整條）與自測 ⑮ 都有負控覆蓋（`agent-tools-check.test.sh:284-302`）；三欄 `BODY_RULES` 逐行確認全部 28 條皆為 `<agent>|<字樣>|<提示>` 三欄格式（`awk -F'|' '{print NF}'` 全部輸出 3，無殘留兩欄舊格式）；`RULES` 表 `ios-dev|Bash Read Edit Write Grep Glob Agent ${LINEAR3}` 對照 `ios-dev.md` frontmatter 逐字命中，無舊欄位解析殘留（read_tools() 函式為 R1→R2 抽出的共用解析，`RULES`／`FORBIDDEN_RULES`／`BODY_RULES` 三張表共用同一支，未見各自重寫的舊版）。實跑 `bash scripts/gates/agent-tools-check.sh` 對真實 `.claude/agents/` 輸出 `✓ agent-tools gate 通過（6 份 agent 定義；正文必含字樣 28 條）`；`agent-tools-check.test.sh` 101 組全綠。
2. **`scripts/gates/list-ipad-tests.sh` 與 `ci.yml` `ci-ipad` job**：job 內 `ios_runtime_pin`／`found`／`found_os`／`ipad_udid`／`ipad_os`／`list`／`only_args` 逐一在後續步驟被讀取，無孤兒變數；`list-ipad-tests.test.sh` 已掛進 `ci.yml:197` 的 `rules` job（`grep -n "list-ipad-tests.test.sh" ci.yml` 命中一次，且是本次 PR 新增，非空掛）；實跑 `bash scripts/gates/list-ipad-tests.test.sh` 全綠（含 mutant 驗證）。
3. **`scripts/gates/push-gate.sh` iPad 段**：slug 比對 `iPad Air 11-inch \(M3\)|iPadAir11M3`、UDID 抓標準十六進位格式（R1 mutation 5 釘住的坑，未見退回 naive 取括號的舊寫法殘留）、`run_ipad_best_effort` 已被 `wd_run` 包住（`push-gate.sh:658`）——`grep -n "iPad\|ipad" push-gate.sh` 逐行核對，找不到 R1→R2 改法遺留的舊分支（例如舊版 literal-only 比對、未包 wd_run 的裸呼叫）。`push-gate.test.sh` ㊴(a)-(e)／(m2) 對應每個分支都有正負控，實跑全綠（依 merge-review R2 記錄的輸出核對一致）。
4. **`scripts/ops/patrol.sh` Pen 段＋`patrol-linear.sh --lane`**：`PENCIL_LINE` 沿用邏輯（`patrol.sh:427-435`）與獨立呼叫 `pen-open.sh --status` 的退回路徑並存、非互斥死碼（`pen_path` 兩種來源皆有效使用）；`PEN_WRONG_LINE` 宣告特意放在 `LS209-PEN-WRONG` mutation 標記區塊之外（有明確理由：`set -u` 下游安全）——非孤兒變數。`patrol-linear.sh --lane` 的呼叫端就是 `patrol.sh:446`（生產呼叫，非只有自測餵）；`patrol_linear.py` 的 `lane_of_number()`／`LANE_BY_NUMBER_QUERY`／`--lane` 參數三者串接完整，`print(lane_of_number(...))` 是真實輸出（非殘留 debug print）。`patrol.test.sh` 的 `LANE_STUB_RC` 只在自建的假 `patrol-linear.sh` fixture 內讀取（測試替身模式，非生產程式碼旗標，與其餘既有 `*_STUB_RC` 慣例一致，非「只被自測用的旗標」這種可疑殘留）。
5. **`scripts/ops/session-start.sh` sshCommand 段**：`SSH_KEEPALIVE_CMD`／`cur_ssh_cmd`／`ssh_note` 三者串接完整（冪等檢查→設定→寫入 `ctx`→輸出 JSON），`ssh_note` 在 `:63` 被消費，非孤兒。
6. **`scripts/ops/promote.sh` `REQUIRED_CHECKS`**：`grep -n "REQUIRED_CHECKS"` 命中三處（`:35` 定義、`:87` 迴圈讀取、`:134` 成功訊息），三處皆已同步含 `ci-ipad`，訊息用 `${REQUIRED_CHECKS// /／}` 動態展開、非硬寫。**但**檔頭 `:16` 的敘述性註解「…的 `ci／db／lint／rules`，各取最新一筆」仍是四項字面——**經 `git log -L16,16` 查證，這行文字自 `6515fdc`（LS-85 時代）就存在、本次 LS-209 diff未觸碰這一行**，不算本 feature 引入的殘留，另列「既有觀察」。
7. **`protection-apply.sh` 註解與程式一致**：`:56-66` 的 m1 裁決註解（development 刻意不強制 `ci-ipad`）與 `CHECKS_OK`／`CHECKS_JSON` 程式碼（`:64` `sort == ["ci","ci-ipad","db","lint","merge-review","rules"]`、`:91-100` JSON 含 `ci-ipad`）一致，無矛盾。**但**檔頭 `:4`「仍須四個 check（ci／db／lint／rules）」——`git log -L4,4` 查證這行同樣是 LS-85／87 時代遺留（`0a785ba`／`53af6ac`）、連 LS-87 加的 `merge-review` 都沒算進去，非本次 diff 觸碰，另列既有觀察。
8. **`.claude/agents/ios-dev.md`**：`tools:` 白名單已移除 `BashOutput`／`KillShell`，正文 `:22` 明確解釋「不使用背景 Bash——tools 白名單沒有 BashOutput／KillShell（LS-209 起移除）」——這是刻意的說明句、不是遺留提及，不算誤報；未見其他處提及已移除工具名而未加註記的段落（全文 grep `BashOutput\|KillShell\|背景` 只命中這一處，且該處正是解釋移除原因）。
9. **`docs/COLLABORATION.md` §7 新五列**：逐一核對引用的腳本／欄位皆存在——`ios-dev.md` frontmatter／`FORBIDDEN_RULES`／`BODY_RULES`（列一）、`session-start.sh` sshCommand（列二）、`ci.yml ci-ipad`／`list-ipad-tests.sh`／`protection-apply.sh:56`／`promote.sh REQUIRED_CHECKS`／`push-gate.sh wd_run`（列三）、`ios-dev.md`／`merge-reviewer.md` mutation 三段式規則＋`agent-tools-check.sh` ⑯（列四、五）——全部存在且與程式碼一致，`merge-reviewer.md` diff 核對新增句與 `BODY_RULES:115` 逐字相符。
10. **殘留 `LS-209-*` 暫存檔**：`find . -iname "*LS-209*"` 只命中 `.claude/worktrees/LS-209`（票的 worktree 本身，非暫存檔），scratchpad 之外無殘留暫存檔／腳本。

### 一個邊界個案（非嚴格「死碼」，但巡檢中發現，一併申報）

- `.github/workflows/ci.yml:172` 的 `rules` job 步驟名稱「Gate 腳本自測（destructive-approval-check／…／db-reset-retry）」是一長串括號列舉；本票新增的 `list-ipad-tests.test.sh`（`:197`）**有被執行**（非死碼，自測本身跑得到、也通過），但步驟顯示名稱的括號清單沒有同步加上 `list-ipad-tests`——GitHub Actions UI 上這個步驟名稱會讓人誤以為沒有涵蓋這支自測。不影響功能，只是文件性小落差；`detect-simulator.test.sh` 同樣不在這份列舉裡但那是既有狀態（LS-209 diff 沒有新增那一行，只是緊鄰新增行之後），不算本票新增的落差。建議：orchestrator 判斷併入下次動這個 job 的票順手補，或不修（影響僅可讀性）。

### 既有死碼觀察（本票範圍外，只報不建議刪）

- `scripts/ops/promote.sh:16`：註解「ci／db／lint／rules，各取最新一筆」未列 `ci-ipad`——`git log -L16,16:scripts/ops/promote.sh` 查證此行文字來自 `6515fdc`（LS-85 R1），本次 LS-209 未觸碰。
- `scripts/ops/protection-apply.sh:4`：檔頭「仍須四個 check（ci／db／lint／rules）全綠」——`git log -L4,4` 查證來自 `0a785ba`（LS-87）／`53af6ac`（LS-85），連 LS-87 加的 `merge-review` 都未算入，屬更早的既有落差，LS-209 未觸碰。
- 兩者同性質（敘述性註解未隨後續票號同步檢查數），建議記入 LS-96 待辦池或下次動這兩個檔案的 harness 票順手一併修，不需要單獨開票。

### 建議處置

- 本票（LS-209）：無需開 fix 票，收尾儀式可直接進 cleanup。
- 邊界個案（ci.yml 步驟名稱漏列 `list-ipad-tests`）：留給 orchestrator 判斷是否值得記入 LS-96（影響僅可讀性，不影響機械把關）。
- 既有觀察兩則（`promote.sh:16`／`protection-apply.sh:4` 敘述性註解落後）：建議記入 LS-96 待辦池，供下次動這兩個檔案時順手修。

### `.claude/worktrees/LS-209` 與分支 `hotfix/LS-209-agent-guard-push-resilience`

仍存在（本地＋remote）——依收尾儀式順序，這屬於 sweep 之後的 cleanup 步驟（`cleanup-merged.sh --apply LS-209`），本次巡檢遵照指示未動任何 worktree，僅記錄現況供 orchestrator 執行下一步。

### 搜尋方法

- `git diff 6a5db27..05b07eb --stat` 界定範圍；逐檔 `Read`／`grep -n` 交叉核對規則表、變數宣告與消費點；`git log -L<line>,<line>:<file>` 判斷可疑殘留註解是否為本 feature 引入或既有債務。
- 實跑（唯讀，不動任何檔）：`bash scripts/gates/agent-tools-check.sh`、`agent-tools-check.test.sh`（101 組全綠）、`list-ipad-tests.test.sh`（全綠含 mutant）；其餘（`patrol.test.sh` 305 個、`push-gate.test.sh`、`promote.test.sh` 17 組、`patrol-linear.test.sh`）採信 merge-review R1／R2 已記錄的實跑輸出（`3d16c1d1`／`719ab233`），本輪僅做程式碼交叉比對複核、未重跑全量以節省時間，經抽查（agent-tools-check、list-ipad-tests 全跑）與程式碼閱讀交叉印證一致。

### 盲區聲明

- 純文字 grep／閱讀，抓不到反射、字串拼接、GitHub Actions 表達式間接引用（如 `${{ }}` 內動態組出的檔名）等動態引用模式。
- CI `ci-ipad` job／`protection-apply.sh` 是否已在 GitHub 端真的套用（required checks 清單）未由本次巡檢重新驗證——採信 orchestrator 09-06 08:45 comment 的申報（已套用、`ci-ipad` 已列 required）。
- 未重跑 `patrol.test.sh`（305 組，約 2 分鐘）／`push-gate.test.sh`／`promote.test.sh`／`patrol-linear.test.sh` 全量，僅重跑 `agent-tools-check.test.sh`／`list-ipad-tests.test.sh` 兩支並對其餘採信 merge-review R2 記錄的實跑輸出＋程式碼閱讀交叉核對；若要更高把握需 orchestrator 或下個 agent 全量重跑。
- 不碰模擬器、不碰 Pen、不動任何 worktree（依指示）。

### 2026-09-06T01:20:54.982Z · Chih-Lin Yeh · `60b0a03f`

## Delta QA — PASS（2026-09-06 09:20）

驗的是 `test` tip `913c91af37da0e8e31f17d0de445c6416606cdfa`（＝`origin/test`）。在 `qa-test` worktree `git fetch && git checkout --detach 913c91a` 重驗，未沿用任何 agent 申報。

**Delta 範圍先核對**：`git diff b5f87b9..913c91a --stat` 只有 26 檔（harness／CI／agent 定義／docs＋1 個測試檔），無 `LittleSprout/`（app）、無 `supabase/migrations`、無 `design/*.pen`——app 內容與 `b5f87b9`（LS-192 QA PASS，comment `88fb24bc`）一致，成立。

### 逐條

**1. 自測全綠**（皆在 913c91a 上實跑）：
- `agent-tools-check.test.sh`：101 組樣本，exit 0
- `list-ipad-tests.test.sh`：29 組樣本，exit 0
- `patrol.test.sh`：305 個 ✓，exit 0
- `patrol-linear.test.sh`：106 個 ✓，exit 0
- `push-gate.test.sh`：93 個 ✓，exit 0
- `promote.test.sh`：17 組樣本，exit 0
- `pr-body-check.test.sh`：91 組樣本（100 行 ✓），exit 0
- `agent-tools-check.sh .claude/agents`（真實 agents）：exit 0，`✓ agent-tools gate 通過（6 份 agent 定義；正文必含字樣 28 條）`

**2. 實作票防線** ✓：
- `ios-dev.md` frontmatter `tools:` 逐字檢查：不含任何 `mcp__pencil__*`，含 `mcp__linear__save_comment`。
- 正文含「不得派 fork／subagent 改動任何檔案」（第 13 行）與「不使用背景 Bash——tools 白名單沒有 `BashOutput`／`KillShell`」（第 22 行）。
- `qa.md` 仍保留 `mcp__pencil__get_app_state`／`mcp__pencil__execute`／`mcp__pencil__read_skill`；`merge-reviewer.md` 本來就不含任何 pencil 工具（`b5f87b9` 時已如此，非本票異動，未誤傷）。

**3. patrol Pen 開錯檔** ✓：主 checkout 跑 `bash scripts/ops/patrol.sh --brief`（唯讀），輸出含：
```
⚠ Pen 開錯檔（實作票 LS-192）
[Pen] 開錯檔（實作票 LS-192；lane=lane:ui，非 lane:design——不得在實作票 worktree 開 Pen，見 ios-dev.md 硬規則）
```
與現況（Pen 停在 `.claude/worktrees/LS-192/design/littlesprout.pen`）一致。

**4. sshCommand** ✓：
- `git -C /Users/clyeh/little-sprout config --get core.sshCommand` → `ssh -o ServerAliveInterval=30 -o ServerAliveCountMax=20`
- `git config --global --get core.sshCommand` → 空（未改全域）
- `extensions.worktreeConfig` 未開 → 全 repo 共用同一份 config
- `session-start.sh:39-47` 讀原始碼確認冪等分支：`cur_ssh_cmd=$(git config --get core.sshCommand)`；`if [ "$cur_ssh_cmd" != "$SSH_KEEPALIVE_CMD" ]` 才寫入——目前值已等於目標值，再跑一次會落入「相等即跳過」分支，不重複寫入。`bash -n scripts/ops/session-start.sh` 語法檢查通過。（依指示以靜態讀碼＋語法檢查證明分支，不實際執行以免觸發 cron 提示等副作用）

**5. CI iPad job** ✓：
- `gh run list --branch development` 找到 913c91a 的 run（databaseId 34002101050，event=push，conclusion=success）。
- `ci-ipad` job（id 101402681020）conclusion=success；此 run 是 **push 事件**（back-merge PR #332 併入 development 觸發），依 `ci.yml` 註解「push 事件（promote.sh 晉升）兩條都不看、無條件跑，當晉升前的最後一道伺服器端兜底」——`GITHUB_BASE_REF` 為空，判定區塊整段跳過，直接無條件執行（非 PR 事件才會印「→ ci-ipad：要跑／略過」）。
- 實際輸出：`simulator: iPad Air 11-inch (M3) 0B38A3FD-7EEB-41C1-988C-47D5E50ADC4A iOS 26.2（pinned 26.2）` → `以下測試將實跑：` → `LittleSproutUITests/SettingsViewIPadTests`／`LittleSproutUITests/TapTargetGateTests/testSettingsViewRegular` → 7 個 Test Case 逐行 `passed`（`testAccountSectionDeleteRowPushesAndBackReturns` 42.86s／`testContentSafetySectionStorageRowPushesAndBackReturns` 16.26s／`testFamilySectionInviteRowRegressionPushesAndBackReturns` 14.65s／`testLegalSectionSwitchesWithoutPush` 8.47s／`testProfileSectionEntryPushesAndBackReturns` 12.14s／`testSidebarSelectionIsAccessibleAndDistinguishable` 6.67s／`testSettingsViewRegular` 6.11s）→ `Executed 7 tests, with 0 failures (0 unexpected)` → `** TEST SUCCEEDED **`。
- `gh api repos/CLYEH/little-sprout/branches/test/protection/required_status_checks --jq .contexts` → `["ci","lint","rules","db","merge-review","ci-ipad"]`；`main` 同樣包含 `ci-ipad`。
- `scripts/gates/list-ipad-tests.sh`（真 repo）exit 0，輸出 2 行識別碼（`SettingsViewIPadTests` 整類別＋`TapTargetGateTests/testSettingsViewRegular`）；`grep -c 'func test' LittleSproutUITests/SettingsViewIPadTests.swift` = 6，加上 Regular 那支＝7，與 CI 實跑「Executed 7 tests」吻合。

**6. unused_import** ✓：`git diff b5f87b9..913c91a -- 'LittleSproutTests/**'` 只有 1 檔 1 行——`FamilyStoreRefreshQuotaRaceTests.swift` 移除 `import os`，其餘不變。`xcodebuild build-for-testing -scheme LittleSprout -destination 'generic/platform=iOS Simulator' -project LittleSprout.xcodeproj`（前景，Xcode 26.6）→ 0 個 `error:`、`** TEST BUILD SUCCEEDED **`、exit 0。

**7. 回歸** ✓：`git diff b5f87b9..913c91a --stat` 確認無 `LittleSprout/`、無 `supabase/migrations`、無 `design/*.pen`。`docs/COLLABORATION.md` §7 抽兩列對照程式：
- 「Gate／Ops 腳本自測」步驟宣稱 `if: ${{ !cancelled() }}`（非 `always()`）——`grep -n 'cancelled()\|always()' .github/workflows/ci.yml` 命中 173／282 行皆為 `if: ${{ !cancelled() }}`，一致。
- `ci-ipad` 那列宣稱 test／main required checks 含 `ci-ipad`、development 刻意不加——`protection-apply.sh:56-63` 註解與 `CHECKS_JSON`／`CHECKS_OK` 邏輯（test/main PUT 含 ci-ipad，development 走 patch＝before∪merge-review）與上方第 5 條 `gh api` 實測結果一致。

### 結論

7 條驗收全數 ✓，無 BLOCKED、無 FAIL。**PASS**。

未驗／範圍外聲明：未重跑 `swiftlint analyze`（不在本次 delta 驗收範圍，且該掃描結果已由 R2 merge-review 覆核）；未碰 Pen／Pencil MCP；未持有 supabase-lock（本次驗收不涉及本機容器／模擬器互動，`--status` 確認 `free`）；未 boot 任何模擬器。

### 2026-09-06T01:23:03.901Z · Chih-Lin Yeh · `9a94f8c1`

**orchestrator 09-06 09:22**：delta QA PASS（60b0a03f；qa status 貼 913c91a；七項全 ✓，`ci-ipad` 對 913c91a 實跑 7 案）。sweeper（5bd56a44）無死碼，三條註解殘句轉 LS-211 順修。promote test→main 等 test 分支 push 事件的 `ci` job（run 34003058714）跑完（同 SHA 在 development 的 run 已綠，required check 認 SHA 上最新一筆），綠後 FF＋打版 v0.19.0（與 LS-192 同版）→ lesson comment → cleanup → Done。

### 2026-09-06T01:32:47.733Z · Chih-Lin Yeh · `c50054be`

**orchestrator 09-06 09:32 收尾儀式**
① dead-code-sweeper（5bd56a44）：無死碼；三條註解／顯示名殘句轉 LS-211 順修。
② lesson learning：
- **harness 改善**：本票是第三張「規則有寫、gate 沒釘」事故票（fork／Pencil／背景 push）；落地後同日仍有兩起舊定義 agent 停在等背景通知（LS-210、LS-190 R2）——新定義正文已改「不使用背景 Bash」，若新定義下再發生即升 PreToolUse 擋 ios-dev 的 `run_in_background`（池項待記）。
- **設定優化**：`ci-ipad` 首版「從未實跑」被 reviewer 抓到——新 job 的驗收必須含「本 PR 自己觸發實跑」證據，已寫進 R2 自變更觸發；`protection-apply` required 清單與 `promote.sh` 兩處不同步已改讀同一份變數。
- **工具缺口**：`7b047d71` 池項判斷「26 檔未用 import」幾乎全錯（`OSAllocatedUnfairLock` 需 `import os`）——池項寫入時的 grep 證據不足；LS-211 對 24 檔改逐檔 build 驗；`swiftlint analyze` 267s 不進 CI。
- 成功面：patrol「Pen 開錯檔（實作票）」段第一次實跑即點名現況（LS-192）；SSH keepalive 後本日無再斷線；delta QA 對 required contexts 用 read-only API 核對。
③ `cleanup-merged.sh --apply LS-209`：worktree＋本機分支＋專屬機 LS-209-iPhone17Pro＋DerivedData 已清。
④ 非設計票，Pen 清場不適用。→ Done（v0.19.0）。

