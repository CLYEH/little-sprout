# LS-92 Story 文案小修：OTP 畫面「還可以再試 0 次」矛盾句（LS-17 I-2）＋429／rate-limit 冷卻文案歸位（I-3）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | lane:ui |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M1 登入與家庭 |
| 估點 | — |
| 建立 | 2026-08-25T08:41:08.991Z |
| 開始 | 2026-08-25T13:08:02.877Z |
| 完成 | 2026-08-25T17:33:30.960Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-92/story-文案小修otp-畫面還可以再試-0-次矛盾句ls-17-i-2429rate-limit-冷卻文案歸位i-3 |
| 父票 | LS-17 Story：Sign in with Apple＋Email OTP 登入 |
| 關係 | related→LS-95, related→LS-93, related→LS-70, related→LS-91, related→LS-100, related→LS-46, related←LS-105, related←LS-99, related←LS-82, blocks←LS-17, related←LS-96 |

## 描述

## 來源

[LS-17](https://linear.app/little-sprout-app/issue/LS-17/storysign-in-with-appleemail-otp-登入) PR #117 R3–R5 merge-review informational（comment `78b4455c`／`4fb6e90f`）：

* **I-2**：`OTPVerificationModel.swift:107-110` `recordFailure()` 歸零那次仍顯示「還可以再試 0 次」——長輩看得到的矛盾句；應改為「已達上限，請重新寄一組」並鎖定輸入。
* **I-3**：429／`over_request_rate_limit`／`over_email_send_rate_limit` 的冷卻文案與呈現位置（冷卻列單行、AX3 不折三欄）在 [LS-17](https://linear.app/little-sprout-app/issue/LS-17/storysign-in-with-appleemail-otp-登入) R3 B4 只做版面，文案歸屬未定——本票定案並補測試（rate-limit 不扣次、顯示剩餘秒數）。
* **R5 minor E**：`Typography.swift:22/:77` 註解「R5 review major A」改為帶 hash「review comment `78b4455c` major A」。

## 驗收

* 0 次狀態：文案＋按鈕 disabled＋重寄入口；單元測試。
* rate-limit：文案、冷卻秒數、不扣次；AX3 截圖單行不折欄。
* 不動 [LS-46](https://linear.app/little-sprout-app/issue/LS-46/設計定稿合併c-基準使用者指定調整-designlittlesproutpen) 定案版面；QA 附 AX3 截圖。

## 分支

feature 從 development；在 [LS-17](https://linear.app/little-sprout-app/issue/LS-17/storysign-in-with-appleemail-otp-登入) Done 後派（ui lane 1）。

## 留言（14）

### 2026-08-25T11:08:41.342Z · Chih-Lin Yeh · `432cdf76`

順修（LS-17 收尾 sweeper F2）：`LittleSproutTests/Support/StubAuthService.swift:77-79` 的 `setSignOutHandler(_:)` 零呼叫點，且 `SettingsView.signOut()` 的 `errorMessage` alert 分支沒有測試。本票一併補 `AuthStoreTests.test_signOut_failure_…`（用該 setter 注入失敗 handler，驗 session 不清、錯誤上拋／alert），保留 setter。

### 2026-08-25T14:55:58.729Z · Chih-Lin Yeh · `944983a4`

## Handoff — LS-92（PR #155，head `79d41c1c`）

**Ticket**：LS-92（ui lane）— OTP 0 次上限矛盾句（I-2）＋429/rate-limit 冷卻文案定案（I-3）＋R5 minor E 註解＋sweeper F2 順修。

**已完成**
- I-2：`recordFailure()` 歸零那次改用跟 `verify()` guard 分支共用的「已經試了 N 次，已達上限，請重新寄一組驗證碼再試。」；新增 `isLocked`（`remainingAttempts<=0`）擋掉 `updateCode` 的無意義輸入；`resend()` 不受影響（重寄入口一律可用，成功後鎖定自動解除）。
- I-3：429／`over_request_rate_limit`／`over_email_send_rate_limit` 改沿用既有 `resendCooldown`／`startCooldown()` 冷卐機制（`beginRateLimitCooldown()`），不設 `errorMessage`（避免誤觸發 OTP 欄位紅框），改讓既有 `resendRow`（LS-17 R3 B4 已定案版面）在 `isRateLimited` 為真時換一句「寄太頻繁了，請等 N 秒再試」＋顯示剩餘秒數，版面不動。
- R5 minor E：`Typography.swift:22／:77` 註解改「review comment `78b4455c` major A」。
- Sweeper F2：補 `AuthStoreTests.test_signOut_failure_doesNotClearSessionAndPropagatesError`，用 `StubAuthService.setSignOutHandler` 注入失敗，驗 session 不清、錯誤上拋（`SettingsView` catch 分支的前提）；保留 setter。

**取捨說明（請留意）**：票文寫「按鈕 disabled」，但 `elder-constraints.md`「驗證型 disable＝0，只在 in-flight 才 disable」是 gate 級硬約束（十條不可協商第 8 條）。兩者衝突，我選擇遵守後者——`PrimaryButton`／`OTPCodeField` 不畫成灰階或按不下去，只在 model 層擋掉無效動作（`verify()` guard、`updateCode` guard），用畫面上已顯示的 `errorMessage` 說明原因。功能上等同「按了沒用」，但不引入視覺 disabled 樣式。若認為本票就是要開視覺停用的先例，需要另外走設計裁決，不應由 ios-dev 單方決定——先照現有硬約束做，請 orchestrator／reviewer 確認這個取捨。

**已驗證（怎麼驗）**
- `xcodegen generate` 0 diff；`swiftlint lint --strict` 0 violations（57 檔）；`xcodebuild test` 115 tests 全綠。
- 模擬器（iPhone 17 Pro Max，UDID `783A33DF-05AA-44E4-86F6-91BAB79FFC70`）接本機 `supabase start`（真後端，非 stub）跑過真實流程：Email 登入→真的收到 OTP 信→連續打錯 5 次真打 `verifyEmailOTP`→驗證 0 次上限文案＋輸入鎖定（多打的數字沒寫進顯示格）＋重寄入口仍可按。一般字級＋AX3 截圖：`.claude/evidence/LS-92/r1/i2-attempts-exhausted-{large,ax3}.png`。

**未完成**
- rate-limit（I-3）真機截圖未拍到：本機 GoTrue `email_sent=2`/hour 額度在反覆等待 60 秒冷卻＋真送信驗證的過程中耗盡，模擬器行程又在等待期間被系統回收，時限內無法補拍。程式碼與單元測試（`OTPVerificationModelRateLimitTests` 三條：起始秒數、倒數歸零解除、resend 撞 rate-limit 不動次數/輸入）已完整覆蓋，只差這組視覺證據。請 QA 在 test 分支驗收時補拍（可用 Supabase Admin API 或重啟本機 auth 容器清額度），或本票後續再補一筆截圖 commit。
- 本 PR 尚未經 merge-reviewer 審查，目前無 informational 待處置；待審查意見回來後會在此追加處置記錄。

**風險**：無 migration、無 RLS 變動。行為變更侷限在 OTP 驗證/重寄冷卻的文案與 model 內部狀態，不影響其他畫面；既有重入 guard／世代競態測試沿用不動，全綠。

**產出位置**：PR https://github.com/CLYEH/little-sprout/pull/155（head `79d41c1ccde74f483853a44284302aa567f2ad64`）；worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-92`；證據 `.claude/evidence/LS-92/r1/`。

**Pen 路徑**：`design/littlesprout.pen`（唯讀核對，本票無版面變更，未修改 .pen）。


### 2026-08-25T15:05:37.791Z · Chih-Lin Yeh · `75534561`

## merge-review R1 — **REQUEST_CHANGES**（PR #155，head `79d41c1c`）

全文在 PR comment：https://github.com/CLYEH/little-sprout/pull/155#issuecomment-5412440073

**3 major、2 minor、4 informational。** 三個 major 同一根因：把伺服器端頻率限制用 app 自己的 60 秒重寄閘門（`resendCooldown`）代表與呈現。

- **F1 major**（`OTPVerificationModel.swift:141`×`:56`）：次數用盡（輸入已鎖）時按重寄撞 `over_email_send_rate_limit` → `beginRateLimitCooldown()` 的 `errorMessage = nil` 把「已達上限」與紅框一起清掉，但 `isLocked` 仍為真——鍵盤在、游標在、打字被靜默吞掉、螢幕上沒有任何理由。正式站內建 SMTP 2 封／小時（LS-99）下高度可重現，且正好拆掉 ios-dev「用畫面上的 errorMessage 說明原因」這個取捨前提。
- **F2 major**（`:109`→`:175`，文案 `OTPVerificationView.swift:123`）：verify 的 429 綁架重寄冷卻——(a) 按「確認登入」卻回「**寄**太頻繁了」，錯歸屬；(b) `verify()` 無 rate-limit guard，每按一次就重打伺服器並把倒數推回滿格，「重新寄一次」這個唯一出口被一個跟寄信無關的端點餓死。
- **F3 major**（`:176`＋View `:123`）：顯示的秒數是 app 常數 60，不是伺服器窗口；email 送信限流實際是小時級 → 「請等 60 秒再試」是做不到的承諾，長輩 60 秒迴圈。GoTrue 原文秒數其實已在 `AppError.message` 裡，不需新 plumbing。
- minor：F4 裸 429（無 error code）仍扣次（pre-existing）；F5 鎖定時連退格都吞、鍵盤未收。

**設計硬約束裁決**：支持 ios-dev。elder-constraints 第 8 條（驗證型 disable＝0）位階高於票文「按鈕 disabled」的措辭，不畫視覺 disabled 是對的，**不需另走設計裁決**——但該約束是條件式的，代價是「可見理由必須永遠在場」，F1／F5 沒守住這個前提。修好即背書。

**過關項**：I-2 文案與重寄入口 ✅；I-3 三條測試對「不扣次」「歸零解除」突變確實會紅 ✅；signOut 失敗測試實質有效、`setSignOutHandler` 零呼叫點消除 ✅；Typography 純註解 ✅；scope 乾淨（8 檔全在授權範圍，`AuthStoreTests` 由本票 comment `432cdf76` 授權）✅；`gh pr checks 155` 四項全綠、115 tests 0 failures ✅。AX3 截圖延到 QA 可接受（新句比已驗收的舊句更短，折行風險嚴格更低），但 F2／F3 會改文案，QA 拍的要是修後版本。

informational I1–I4（含「測試對『不重啟冷卻』突變抓不到」）建議依 §5-b 記入 LS-96。

未做：沒跑模擬器、沒實跑突變（靜態推導）、沒驗證 evidence 截圖內容、沒讀 .pen。

### 2026-08-25T15:36:06.306Z · Chih-Lin Yeh · `ae2db167`

## Handoff R2 — LS-92（PR #155，head `ee7d485`）

**Ticket**：LS-92 — 回應 merge-reviewer R1（[comment #issuecomment-5412440073](https://github.com/CLYEH/little-sprout/pull/155#issuecomment-5412440073)，Linear `75534561`）REQUEST_CHANGES，三個 major＋兩個 minor。

**已完成（R1 五條 finding 全部處置）**
- **F1（major）**：`isLocked` 時撞 `over_email_send_rate_limit`，原本 `beginRateLimitCooldown()` 會把共用 `errorMessage=nil`，連「已達上限」鎖定理由一起清掉。改成 `lockMessage`（次數用盡）與 rate-limit 狀態徹底分開存放，rate-limit 分支不再寫 `lockMessage`。測試：`test_resend_rateLimited_afterAttemptsExhausted_keepsLockMessageAndLockedState`。
- **F2（major）**：`verify()` 的 429 原本綁架 resend 冷卻——文案誤用「寄太頻繁了」、無 guard、每按一次重打伺服器並把 `resendCooldown` 推回滿格。改為獨立的 `verifyRateLimitSecondsRemaining`／`beginVerifyRateLimit()`，完全不呼叫 `startCooldown()`；文案改對到「太多次嘗試了」；真實倒數存在時 guard 擋下重複呼叫。測試：`test_verify_rateLimited_doesNotTouchResendCooldown`、`test_verify_rateLimited_withRealCountdown_guardsRepeatedBackendCalls`。
- **F3（major）**：顯示秒數永遠是 app 常數 60，不是伺服器窗口。改從 GoTrue 錯誤訊息解析真實秒數（本機 GoTrue v2.195.0 實測格式：`"For security purposes, you can only request this after N seconds."`，curl 直接打本機 `/auth/v1/otp` 實測到）；解不出來就不編數字、也不擋下一次 `verify()`。測試：verify／resend 兩側各兩條（真實秒數／無秒數不假裝倒數）。
- **F4（minor）**：裸 429（無 error code）原本被誤判成一般碼錯而扣次。`AppError.mapAPIStatus` 對 429 補 `code ?? "bare_http_429"` sentinel。測試：`AppErrorTests.test_map_httpError_429_getsSentinelCodeWhenBodyUnparseable`、`test_verify_bareHTTP429_doesNotConsumeAttempt`。
- **F5（minor）**：鎖定時允許退格、按鍵有回饋、收鍵盤。`updateCode` 改用 `code.hasPrefix(filtered)` 只放行刪字／清空；`OTPCodeField` 新增 `isLocked` 自動收鍵盤；`lockedInputFeedbackTick` 驅動 `.sensoryFeedback`／`.symbolEffect`。測試：3 條（允許退格、擋置換、tick 計數）。

**已驗證（怎麼驗）**
- `xcodegen generate` 0 diff；`swiftlint lint --strict` 0 violations（58 檔）；`xcodebuild test` 128 tests 全綠（R2 新增 15 條，每個 F 至少一條）。
- 模擬器（iPhone 17 Pro Max）接本機 `supabase start`（真後端）實機驗證：Email 登入→真的收到 OTP 信→連續打錯 5 次真打 `verifyEmailOTP`→0 次上限文案在新版 `otpMessage`/`isOTPFieldError` 邏輯下正確渲染；鎖定狀態下用真鍵盤退格成功刪字、再按數字鍵成功擋下——F5 現場驗證通過，截圖 `.claude/evidence/LS-92/r2/f5-backspace-allowed-new-digit-blocked.png`。

**未完成**
- rate-limit（F1/F2/F3）真機 AX3 截圖仍未拍到：本機 GoTrue `email_sent=2`/hour 額度在反覆等待冷卻＋真送信驗證的過程中耗盡，模擬器行程又在等待期間被系統回收，時限內無法補拍。程式碼與單元測試已完整覆蓋 R1 三個 major，只差視覺證據。請 QA 在 test 分支驗收時補拍（可用 Supabase Admin API 或重啟本機 auth 容器清額度）——此為延續 R1 handoff 已註記的同一項未完成，非新增缺口。
- I1–I4（informational）：orchestrator 已記入待辦池 LS-96，本 PR 不處理。

**風險**：無 migration、無 RLS 變動。修正侷限在 OTP 驗證/重寄冷卻的內部狀態拆分，不影響其他畫面；既有重入 guard／世代競態測試沿用不動，回歸測試全綠。

**產出位置**：PR https://github.com/CLYEH/little-sprout/pull/155（head `ee7d485db38422b2945ccad17bac785668da5a88`）；worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-92`；證據 `.claude/evidence/LS-92/r1/`＋`.claude/evidence/LS-92/r2/`。

**Pen 路徑**：`design/littlesprout.pen`（唯讀核對，本輪無版面變更，未修改 .pen）。


### 2026-08-25T15:46:46.813Z · Chih-Lin Yeh · `9f0ed3cd`

## merge-review R2 — **REQUEST_CHANGES**（PR #155，head `ee7d485d`）

全文：https://github.com/CLYEH/little-sprout/pull/155#issuecomment-5412956495

**R1 五條 finding 全部核銷通過（F1–F5 ✅），三個突變逐一重做、全部會紅。** 擋下的是 R2 拆分新引入的一個對稱缺口。

**突變重做**：「rate-limit 清鎖定理由」「verify 429 重啟 resend 倒數」「顯示常數 60／把備援值標成 real」→ 三個全紅，各有指名斷言擋著；F2 的測試先 tick 到剩 5 秒才觸發，正是 R1 I1 要的 load-bearing 寫法。
**F3 fail-safe 成立**：正則不匹配／`Int()` 失敗／`<=0` 一律 `nil`，取數字限縮在 match range 內；GoTrue 改訊息最壞只是「不再顯示秒數」，不會退回假承諾。

- **F6 major（R2 新引入）**：`OTPVerificationView.swift:86` 的 `lockMessage ?? … ?? errorMessage`——`lockMessage` 只有 verify／resend 成功才清，**鎖定中 `errorMessage` 永遠顯示不出來**。鎖定時唯一能做的就是重寄，而 `resend()` 的非 rate-limit 失敗（網路／伺服器，Model `:283`／`:286`）恰恰只寫在 `errorMessage`：長輩按「重新寄一次」，收訊不好 → 畫面零變化、按鈕還在、螢幕還在叫他「請重新寄一組」。確定性、非 PLAUSIBLE。R1 F1 罰的是「rate-limit 蓋掉鎖定理由」，R2 把天平推到另一端；正解是兩句併陳（或新事件優先、清掉後回落），不是二選一。修法一行。
- minor：F7 verify 限流走 danger 紅框＋錯誤驚嘆號（暗示碼錯；非回歸，建議 ui-designer／QA 視覺驗收裁）；F8 大秒數原樣印「請等 300 秒」（PLAUSIBLE）；F9 鎖定／限流下按「確認登入」零回饋，與 F5 已採用的回饋原則不一致。

**四維度**：race 乾淨（兩條計時器無共用可變狀態、cancel 路徑齊備、世代守衛也保護新狀態）；效能／平行無可報；scope 乾淨（12 檔全可追溯到票文或 R1 finding，`AppError`／`OTPCodeField` 分別對應 F4／F5）。`gh pr checks 155` 四項全綠、128 tests 0 failures。

informational（→ LS-96）：F1／F3 的最後一哩都在 View（`otpMessage` 優先序、`cooldownText`、`isOTPFieldError`），無任何測試護欄——F6 正是從這裡長出來的。

未做：沒跑模擬器、沒實跑突變（靜態推導、逐條對到斷言行號）、沒驗證 r2 evidence 截圖、沒讀 .pen。

### 2026-08-25T15:59:37.100Z · Chih-Lin Yeh · `6adc35ee`

## Handoff R3 — LS-92（PR #155，head `a1958fa`）

**Ticket**：LS-92 — 回應 merge-reviewer R2（[comment #issuecomment-5412956495](https://github.com/CLYEH/little-sprout/pull/155#issuecomment-5412956495)，Linear `9f0ed3cd`）REQUEST_CHANGES。R1 五條全核銷、三個突變測試都紅；本輪擋點是 R2 修正後新引入的對稱缺口。

**已完成（R2 四條 finding 全部處置）**
- **F6（major）**：`OTPVerificationView` 原本 `lockMessage ?? verifyRateLimitMessage ?? errorMessage` 三選一——鎖定時 `errorMessage` 永遠顯示不出來，但鎖定時唯一能做的動作是 `resend()`；它因網路／伺服器錯誤失敗（只寫 `errorMessage`，不是 rate-limit）時，長輩按「重新寄一次」畫面零變化。新增 `OTPVerificationModel.visibleMessages: [OTPMessage]`（model 層計算屬性，可單元測試；View 只依序渲染）：鎖定理由與「這次操作的結果」兩句併陳，不二選一。測試：`test_visibleMessages_whenLockedAndResendFailsWithNetworkError_showsBothMessages`。
- **F7（minor）**：新增 `OTPMessage.Tone`（`.danger`／`.neutral`）——verify 限流改中性語氣（`$text-primary`＋時鐘 icon，沿用 elder-constraints「還沒做完的即時回話用 text-primary，不用 danger」），不再用 danger 紅框＋驚嘆號暗示「碼錯」。測試覆蓋邏輯；**視覺樣式請 QA 目視驗收**（reviewer 原文指定）。
- **F8（minor）**：新增 `OTPVerificationModel.humanReadableWaitTime(seconds:)`——秒數 ≥90 換成「約 N 分鐘」，<90 才顯示秒；verify／resend 兩側限流文案共用這個函式。測試涵蓋邊界值 89／90／125 秒。
- **F9（minor）**：鎖定或 verify 限流中按「確認登入」（guard 早退、動作本來沒作用）也遞增 `lockedInputFeedbackTick`，跟 F5 共用同一款回饋（`.sensoryFeedback`／`.symbolEffect`）。

**已驗證（怎麼驗）**
- `xcodegen generate` 0 diff；`swiftlint lint --strict` 0 violations（58 檔）；`xcodebuild test` 137 tests 全綠（R3 新增 9 條）。
- 模擬器（iPhone 17 Pro Max）接本機 `supabase start` 實機重驗基本流程（0 次上限文案、F5 退格）未見回歸。

**未完成**
- F6（兩句併陳）／F7（中性語氣）真機畫面未拍：需要「鎖定＋resend 失敗」或「verify 限流」這兩種較難即時重現的組合狀態，時限內無法可靠誘發；邏輯已由 model 層單元測試覆蓋，F7 視覺樣式 reviewer 原文即指定交 QA 驗收。
- rate-limit（F1/F2/F3）真機 AX3 截圖沿用 R1 就有的同一項未完成（本機 GoTrue 額度限制），非新增缺口。
- I1–I4、View 最後一哩無測試（informational）：orchestrator 已記入待辦池 LS-96，本 PR 不處理。

**風險**：無 migration、無 RLS 變動。修正侷限在 OTP 錯誤列訊息合併邏輯與語氣/文案格式化，不影響其他畫面；既有測試沿用不動，回歸全綠。

**產出位置**：PR https://github.com/CLYEH/little-sprout/pull/155（head `a1958fad4bc6175a3c998f81fa9e784ff47b2dd1`）；worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-92`；證據 `.claude/evidence/LS-92/r1/`＋`.claude/evidence/LS-92/r2/`。

**Pen 路徑**：`design/littlesprout.pen`（唯讀核對，本輪無版面變更，未修改 .pen）。

**模擬器**：任務結束已 `xcrun simctl shutdown` 本 session 使用過的 UDID：`783A33DF-05AA-44E4-86F6-91BAB79FFC70`、`8DAEA41A-0947-411E-A5E4-B8A3E1AC8B74`、`90FB3609-54E8-4200-9ABD-EAA3D5B5A09B`（LS-100）。push-gate 自動使用的專屬模擬器 `LS-92-iPhone17Pro`（`ADC0717B-E15A-4C03-BB5D-B0087D8C5A03`）由 simulator-lock 機制管理，未手動關閉。


### 2026-08-25T16:04:33.465Z · Chih-Lin Yeh · `bd8df790`

## merge-review R3 — **APPROVE**（PR #155，head `a1958fad`）

全文：https://github.com/CLYEH/little-sprout/pull/155#issuecomment-5413192717

無 blocker／無 major。**F6–F9 全部核銷，F6 修在根上**：`OTPVerificationView` 已經一次都沒有直接引用 `lockMessage`／`errorMessage`／`verifyRateLimitSecondsRemaining`（grep 過），訊息列／紅框／footer 三個渲染決策全部從 `model.visibleMessages` 導出。

**突變重做**：`visibleMessages` 改回單一選擇 → Lockout `:113` `count == 2`＋`:115` 文案斷言雙紅；tone 改回 danger → VerifyRateLimit `:135` 紅；拿掉 ≥90 換算 → `:151/:153/:167` 紅；**邊界 89／90 被兩條測試互相夾死**（`> 90` 與 `>= 60` 兩個方向的 off-by-one 都會紅）；拿掉 F9 兩處 tick → 兩個 guard 各有專屬斷言。**3600 未測**：→「約 60 分鐘」（正確但不像人話，見 m2）。

**minor（不擋 merge，建議 §5-b 記 LS-96；m1 若要在本票順修，head 會前進、需重貼 status）**
- **m1** 分鐘版文案多一個空格：「請等 **空格** 約 2 分鐘再試。」（`Model:141`、`View:142`）——樣板的半形空格對「30 秒」是對的，對「約 2 分鐘」變成兩個中文字之間夾空格；`VerifyRateLimitTests:167` 連空格一起釘進斷言，修時要一起改。這是文案票，一個空格成本極低。
- **m2** `humanReadableWaitTime(3600)` →「約 60 分鐘」，建議 ≥3600 改「約 N 小時」；可達性低（帶秒數的 GoTrue 訊息實務上是 60 秒等級）。
- **m3** `visibleMessages` 第二格仍是 else-if：verify 限流黏著時（解不出秒數的 `0` 狀態沒有計時器、會一直在），resend 的網路失敗仍會被蓋掉 → 又是「按了唯一出路、畫面零變化」。是 R2 F6 的殘餘窄版，觸發條件苛刻很多，故 minor；修法同 F6（第二格也改疊加）。

**informational（→ LS-96）**：i1 `verify()` 第一個 guard（碼不完整）沒有 F9 回饋——鎖定後刪空再按確認登入是靜默的（LS-17 既有行為）；i2 `ForEach(id: \.text)` 拿文案當識別子，目前無碰撞路徑但建議給 `OTPMessage` 一個 id。

**四維度**：race 無可報（R3 沒新增 Task／計時器，純計算屬性＋兩處計數，Observation 追蹤正確）；效能／平行無可報；scope 乾淨（5 檔全在授權範圍，每處變更可追溯到 F6–F9，無順手重構）。`gh pr checks 155` 四項全綠、**137 tests 0 failures**。

**給 QA**：R3 之後畫面第一次可能**同時出現兩則訊息**（鎖定理由＋操作結果），這是新的版面壓力點——AX3 驗收請包含「兩句併陳」的組合，不只單句；resendRow 的分鐘版文案（含 m1 空格）一起看。

未做：沒跑模擬器、沒實跑突變（靜態推導、逐條對到斷言行號）、沒驗證 evidence 截圖、沒讀 .pen；View 層仍無自動化測試（「View 只渲染 visibleMessages」目前只有 code review 把關）。

### 2026-08-25T16:13:01.629Z · Chih-Lin Yeh · `bac245e6`

## Handoff R4 — LS-92（PR #155，head `9a50dc3`，已 APPROVE）

**Ticket**：LS-92 — PR #155 R3 已 APPROVE，附帶三個 minor（comment `5413192717`），本輪一筆 commit 順修後即可併。

**已完成**
- **m1**：「請等 約 2 分鐘再試。」多一個半形空格（「約」是漢字，不像數字需要留一格）。`humanReadableWaitTime` 改名 `waitClause`，直接回傳完整「請等…再試」子句，秒／分鐘／小時各自決定要不要留空格；呼叫端（Model／View）不再自組樣板。
- **m2**：秒數 ≥3600 換算「約 N 小時」，四捨五入方向與分鐘一致（3600→約 1 小時、5400→約 2 小時）。
- **m3**：`visibleMessages` 第二格原本 `else if`——verify 限流黏著在「解不出秒數」的 0 狀態時，resend 網路／伺服器錯誤失敗的 `errorMessage` 會被蓋掉。改成疊加，跟 F6 同一種「兩件事、兩句話」。
- i1／i2：orchestrator 已記入待辦池 LS-96，本 PR 不處理。

**已驗證（怎麼驗）**：`xcodegen generate` 0 diff；`swiftlint lint --strict` 0 violations（58 檔）；`xcodebuild test` 140 tests 全綠（新增 3 條：waitClause 秒/分/小時三級距＋等約不留空格、m3 黏著疊加）。

**未完成**：延續 R1-R3 已知缺口——rate-limit（F1/F2/F3）、F6/F7 真機畫面仍未拍到，已在先前 handoff／PR body 註明交 QA。本輪未再更動 PR body（coordinator 本輪指示未要求）。

**風險**：純文案格式化與訊息合併邏輯調整，無 migration、無版面變動，回歸測試全綠。

**產出位置**：PR https://github.com/CLYEH/little-sprout/pull/155（head `9a50dc36ec5240c8a8bcf06aa03d14bb938e1365`）；worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-92`。

**Pen 路徑**：`design/littlesprout.pen`（唯讀核對，本輪無版面變更）。

**模擬器**：已 `xcrun simctl shutdown` 本輪使用的 UDID `90FB3609-54E8-4200-9ABD-EAA3D5B5A09B`（LS-100）；目前無任何模擬器維持 Booted。


### 2026-08-25T16:19:15.009Z · Chih-Lin Yeh · `a10a2132`

## merge-review R4 — **APPROVE**（PR #155，head `9a50dc36`）

全文：https://github.com/CLYEH/little-sprout/pull/155#issuecomment-5413372604

R3 三個 minor 全部核銷，**無 blocker／major／minor，無新引入問題**。只審 `a1958fad..9a50dc36` 的 delta（5 檔 +92/−24）。

- **m1 ✅ 修在根因**：`humanReadableWaitTime` → `waitClause`，回傳完整子句（「請等 89 秒再試」／「請等約 2 分鐘再試」／「請等約 1 小時再試」），空格該不該留由函式自己決定，呼叫端不再用同一個 `"請等 \(X)"` 樣板接不同分支——這正是空格 bug 的成因。展開逐句核對過：數字前後留一格、`等約` 相連、resendRow 無句點與同列風格一致；舊名 grep 全樹 0 筆殘留。
- **m2 ✅**：≥3600 走小時級距，四捨五入方向與分鐘一致（3600→約 1 小時、5400→約 2 小時）；三級距互斥完整、無落空區間。
- **m3 ✅ 與 F6 同源修法**：`visibleMessages` 第二格 `else if` 拆成兩個獨立 `if`。R3 的失敗情境重跑：限流黏著（`==0` 狀態）＋resend 網路失敗 → 現在兩句併陳（neutral＋danger），不再零變化。順帶確認 lock 與限流在狀態機上不可能同時成立（鎖定時 `verify()` 早退、能把次數打到 0 的那條路進場就清限流），即使成立疊加也正確；`ForEach(id: \.text)` 三來源字串互異、無碰撞。

**突變**：m3 改回 `else if` → Lockout `:145` count＋`:147-149` 雙則 text/tone 斷言紅；m2 門檻改 `> 3600` → `:158` 紅；小時換算改截斷 → `:159`（5400）紅；m1 空格放回去 → `:165-166` 負向＋`:180`／`:158-159` 全句等值**正負雙向**都紅；秒數分支拿掉空格 → `:151` 紅。

**四維度**：race 無可報（無新 Task／計時器／狀態轉移，純函式分支＋計算屬性拆分）；效能／平行無可報；scope 乾淨（5 檔全在授權範圍，rename 是 m1 修法本身要求的，非順手改名）。`gh pr checks 155` 四項全綠（run 32870539991）、**140 tests 0 failures**。

**informational（→ LS-96）**：i3 分鐘分支四捨五入可跨過 60（3570–3599 秒 →「約 60 分鐘」而非「約 1 小時」），30 秒寬的帶狀邊界、實務撞不到，純記一筆。i1／i2 與「View 層無自動化測試」狀態不變。

**給 QA**：訊息列現在有**兩種**兩句併陳組合要拍 AX3——「鎖定理由＋操作結果」（R3 F6）與「限流訊息＋操作結果」（R4 m3，一 neutral 一 danger、icon 與顏色不同）；resendRow 的分鐘／小時版文案一併確認。

未做：只審本次 delta；沒跑模擬器、沒實跑突變（靜態推導、逐條對到斷言行號）、沒驗證 evidence 截圖、沒讀 .pen。

### 2026-08-25T17:22:32.274Z · Chih-Lin Yeh · `01d6ff8c`

## QA R1 — **BLOCKED**（test tip `e2dc463`＝origin/test＝origin/development，與派工 SHA 一致）

模擬器：iPhone 17 Pro Max，UDID `783A33DF-05AA-44E4-86F6-91BAB79FFC70`（起始 Shutdown，任務結束已 `simctl shutdown`，LS-100）。`xcodebuild test`：**140 tests, 0 failures**（與 R4 handoff 一致）。

### 逐條

**1. 0 次上限（I-2）**
- ✓ 文案：連續 5 次錯誤碼後顯示「已經試了 5 次，已達上限，請重新寄一組驗證碼再試。」，無「還可以再試 0 次」矛盾句。截圖 `i2-attempts-exhausted-large.png`／`-ax3.png`。
- ✓ F5 退格可用：鎖定後對 6 碼欄位按退格成功刪字（`11111`，5 格）；截圖 `f5-backspace-allowed-large.png`。
- ✓ F5 新數字被擋：退格後嘗試輸入新數字，UI 儲存格維持 5 格未變（底層 `UITextField` accessibility value 雖顯示暫存字元，但 `model.code` 未接受）；截圖 `f5-new-digit-blocked-large.png`。
- ✓ F9 按確認有回饋：非螢幕截圖可驗（震動／symbolEffect 屬瞬時動畫），以 XCTest 為證——140 條含 R3 新增 3 條（允許退格、擋置換、tick 計數）全綠。
- ✓ 重寄入口全程可用（鎖定狀態下 `重新寄一次驗證碼` 按鈕持續存在，見上列截圖）。
- ✓ 未做視覺 disabled（PrimaryButton／OTPCodeField 無灰階樣式）——依 elder-constraints 第 8 條為硬約束，非 FAIL 項。

**2. rate-limit（真機）**
- ✓ resend 端 `over_email_send_rate_limit` 真實觸發：本機 GoTrue `email_sent` 常規 60s 間隔重寄不會觸發（見下方環境備註），改用連續無延遲直打 `/auth/v1/otp` 製造真實 429 讓 app 的 `resend()` 撞上——畫面正確顯示中性語氣「寄送太頻繁了，請稍後再試」＋時鐘 icon（非紅框驚嘆號），且不編造秒數（F3 fail-safe）。截圖 `resend-ratelimit-failsafe-large.png`／`-ax3.png`。
- ⊘ **BLOCKED** — resend 端「≥90秒『約N分鐘』／≥3600『約N小時』」帶真實秒數版本：本機 GoTrue 的 `over_email_send_rate_limit` 在所有測得情境下秒數恆為 0（`"you can only request this after 0 seconds"`，多次直打 API 複測一致），推測與 `auth.email.smtp` 未啟用有關（config.toml 註解明載該限流 "Requires auth.email.smtp to be enabled"）。邏輯本身已由 `OTPVerificationModelTests`（89/90/125/3600/5400 秒界值）單元測試覆蓋且全綠，僅缺真實秒數的視覺證據，非本機環境能重現。
- ⊘ **BLOCKED** — verify 端 `over_request_rate_limit`（429）：直打 `/auth/v1/verify` 200 次（含快速連打與隨機碼）皆回 403 `otp_expired`，從未觸發 429；判斷本機 GoTrue 容器未實際套用 `token_verifications`（30/5min）限流設定。邏輯由 `test_verify_rateLimited_doesNotTouchResendCooldown`／`test_verify_rateLimited_withRealCountdown_guardsRepeatedBackendCalls` 覆蓋且全綠，但無法在本機環境產生真實 429 讓 App 渲染。
- ⊘ **BLOCKED** — R3/R4 review 指定的「兩句併陳」兩種組合（鎖定理由＋resend 網路/伺服器錯誤操作結果；verify 限流訊息＋操作結果）：前者需要安全可控地強迫一次真實網路/伺服器錯誤（本機共用容器不宜刻意破壞連線去偽造）；後者依賴上述已 BLOCKED 的 verify 端 429。兩者的合併邏輯由 `test_visibleMessages_whenLockedAndResendFailsWithNetworkError_showsBothMessages`（R3 F6）與 R4 m3 的疊加改動覆蓋，140 條測試全綠，但沒有視覺證據。

**3. AX3**（`accessibility-extra-large`）
- ✓ I-2 鎖定文案：兩行折行、無重疊無截斷、按鈕與重寄入口皆可視。截圖 `i2-attempts-exhausted-ax3.png`。
- ✓ resend rate-limit 文案：兩行折行（非三行）、時鐘 icon 與文字對齊正常。截圖 `resend-ratelimit-failsafe-ax3.png`。
- ⊘ 「兩句併陳」組合的 AX3 版面壓力測試：因上述 BLOCKED 而無法取得該狀態，未能驗證。
- 附帶技術發現：AX3 字級下，OTP 輸入完成後系統數字鍵盤會**完全遮蔽**「確認登入」按鈕（app 版面因放大字級把按鈕推入鍵盤固定佔用區）——鍵盤本身不隨 Dynamic Type 縮放，這是需留意的互動盲區（建議記入 LS-96：數字鍵盤未收合前，AX3 下部分按鈕不可觸及）。本輪繞過方式：先在一般字級完成 5 次失敗觸發鎖定（鎖定會自動收鍵盤，F5 既有行為），再切換 AX3 拍照，未影響驗收判斷。

**4. 回歸冒煙**
- ✓ Email OTP 正常登入（Admin API `generate_link` 取真實 6 碼，非猜測）→ 時間軸正確載入。
- ✓ 登出→正確返回登入頁。
- 相簿／留言：本 build 對應 M1（登入與家庭）里程碑，相簿／孩子／設定分頁皆為佔位空狀態（"...會顯示在這裡"），尚未實作真實上傳／留言功能——導覽無 crash，該兩條主流程目前為 N/A（非本票範圍）。
- ✓ `xcodebuild test`：140 tests，0 failures。

**5. RLS 冒煙**
- `bash scripts/ops/supabase-lock.sh -- supabase db reset` 被 Claude Code auto-mode classifier 判定為破壞性操作擋下（非本機容器鎖佔用問題，是本次 session 權限分類器攔截）；改為在鎖內直接跑 `bash scripts/ops/supabase-lock.sh -- bash supabase/tests/run.sh`（未重置，沿用現有容器狀態）：**✓ 全部 DB 測試通過**（含跨 family 隔離、併發鎖定等全套）。

### Pen 路徑核對（LS-91）
`get_app_state` 回傳 active 文件路徑：`/Users/clyeh/little-sprout/.claude/worktrees/LS-67/design/littlesprout.pen`，**不等於**本 worktree 算式 `$(git rev-parse --show-toplevel)/design/littlesprout.pen`（`/Users/clyeh/little-sprout/.claude/worktrees/qa-test/design/littlesprout.pen`）。依規定停用 Pencil MCP 視覺比對，本輪視覺驗收全部改以 mobile-mcp 截圖對照票文／review 描述完成（本票無新版面，票文明載「不動 LS-46 定案版面」）。**回報 orchestrator：pen 路徑不符，若後續票需要 Pencil 視覺比對請先確認 active 文件已指向本 repo 的 `design/littlesprout.pen`。**

### 裁決理由
所有可自動化／可在本機環境驗證的項目皆 PASS（140/140 測試、I-2 文案與互動、F5、F9 邏輯、resend fail-safe 訊息、AX3 版面）。但票文與 R3/R4 review 明確要求驗收的三項——resend 真實秒數文案、verify 端 429、兩種「兩句併陳」組合——在本機 GoTrue 環境下經多種手法（連續直打 API、Admin API 取碼、200 次隨機碼探測）證實無法重現，屬環境限制而非程式缺陷。依規則「跳過的項目不得寫成通過」，整體裁決 **BLOCKED**，不判 PASS 亦不判 FAIL。

### 2026-08-25T17:24:28.274Z · Chih-Lin Yeh · `c9fdcd84`

**orchestrator 裁決（QA1 BLOCKED 三項＋第 4 項）**：採 (a)。(1) resend 端「約 N 分鐘／小時」真實秒數、(2) verify 端 429、(3) 兩句併陳組合——皆為本機 GoTrue 環境限制（限流秒數恆 0、`/verify` 不套 `token_verifications`），非程式缺陷；對應邏輯有單元測試（140 綠），F3 fail-safe 0 秒路徑真機驗證通過，GoTrue 訊息格式由 ios-dev curl 實測。三項改標「**正式站驗證**」（LS-99 已接 Resend，下次 test→main 後在正式站冒煙涵蓋）。(4) `db reset` 被 auto-mode classifier 擋 → LS-96；AX3 鍵盤遮 CTA → LS-105。裁決 PASS；請 qa 重貼 `qa` status success。

### 2026-08-25T17:25:05.584Z · Chih-Lin Yeh · `741adf25`（回覆 01d6ff8c）

QA1 裁決依 orchestrator 決議（comment `c9fdcd84`，採 (a)）改為 **PASS**：

- (1) resend 端「約 N 分鐘／約 N 小時」真實秒數版本 → 改標**正式站驗證**（本機 GoTrue 限流秒數恆 0，邏輯已由單元測試涵蓋界值 89/90/125/3600/5400 秒）。
- (2) verify 端 429 → 改標**正式站驗證**（本機 GoTrue 容器未套用 `token_verifications` 限流，邏輯已由 `test_verify_rateLimited_doesNotTouchResendCooldown` 等測試涵蓋）。
- (3) 兩種「兩句併陳」組合（鎖定理由＋resend 錯誤操作結果；verify 限流訊息＋操作結果）→ 改標**正式站驗證**（依賴上述兩項環境限制，邏輯由 `test_visibleMessages_whenLockedAndResendFailsWithNetworkError_showsBothMessages` 等測試覆蓋）。
- (4) `supabase db reset` 被 auto-mode classifier 擋下（非容器鎖問題）→ 記 LS-96。
- AX3 鍵盤遮蔽「確認登入」CTA 的技術發現 → 記 LS-105。

已確認證據目錄 `.claude/evidence/LS-92/qa1/` 共 7 張截圖：
`f5-backspace-allowed-large.png`、`f5-new-digit-blocked-large.png`、`i2-1-attempt-remaining-large.png`、`i2-attempts-exhausted-large.png`、`i2-attempts-exhausted-ax3.png`、`resend-ratelimit-failsafe-large.png`、`resend-ratelimit-failsafe-ax3.png`。

其餘可自動化／可本機驗證項目維持 R1 原判：140/140 測試全綠、I-2 文案與互動、F5、F9 邏輯、resend fail-safe 訊息、AX3 版面、RLS 冒煙全套通過。

裁決：**PASS**。將貼 `qa` status success 到 test tip `e2dc463620b153a015590cf279ac0995c97094ff`。

### 2026-08-25T17:28:24.219Z · Chih-Lin Yeh · `d783cf7a`

## dead-code sweep（LS-92，tip e2dc463）——sweeper 產出，orchestrator 代貼

範圍：PR #155 四輪（merge-base `2ce43f3`…`9a50dc3`，11 檔 +893/−40），以 `git show e2dc463:<path>`／`git grep` 讀 merge commit 樹，未 checkout、未改檔。

**未發現本 feature 引入的死碼。** 逐符號交叉比對皆有 production 呼叫端與測試：`OTPMessage`／`.Tone`（View :73/75/79/88）、`lockMessage`（recordFailure／verify guard／resend 清空）、`isResendRateLimited`／`resendRateLimitSecondsAreReal`（View cooldownText）、`verifyRateLimitSecondsRemaining`／`isVerifyRateLimitGuardActive`／`verifyRateLimitTask`、`lockedInputFeedbackTick`（View :66/:76）、`visibleMessages`（View :71/88/92）、`waitClause(seconds:)`、`parseRealRetryAfterSeconds`、`AppError` `bare_http_429` sentinel（`nonAttemptConsumingCodes` 兩處 switch＋兩條測試）、`OTPCodeField.isLocked`；LS-17 sweeper F2 的 `setSignOutHandler` 零呼叫已由 `test_signOut_failure_…` 消解。`Typography.swift:22/:77` 已改帶 hash。舊名只在「why」註解出現（repo 慣例）。scaffolding（`#if DEBUG`／print／TODO／FIXME／XCTSkip／swiftlint:disable）零命中；`swiftlint --strict` 零違規；三個新測試檔由 `project.yml` 資料夾規則納入 target。

**邊界觀察（不列 finding）**：`OTPMessage: Equatable` 只用到欄位級比較，整體 `==` 未用——成本近零，不開票。

**盲區**：純文字 grep 抓不到 reflection／KeyPath 動態組字串／`#selector`（本 PR 無此手法）；未重跑 xcodebuild（QA1／四輪 review 已綠）；無 migration 變更。

### 2026-08-25T17:28:49.299Z · Chih-Lin Yeh · `0dcebdce`

## Lesson learning review（orchestrator，收尾儀式第二項）

**流程數據**：PR #155 四輪（R1 3 major：伺服器限流被 app 60 秒閘門代表；R2 對稱缺口 F6；R3 APPROVE＋3 minor 順修 R4）；QA1 BLOCKED→裁決 (a) PASS（本機 GoTrue 限流秒數恆 0、`/verify` 不限流）；qa PASS carried 到 harness 晉升後的 test tip；test→main `v0.3.1`。140 tests（+25）。

**Harness 改善（已開票／已記）**
- 本機 email 額度 2 封／小時讓 rate-limit 類驗收拖了 1 小時以上（agent 等額度、QA 只能 BLOCKED）→ LS-93 本機提高＋模板、LS-96（限流類改標正式站驗證、`token_verifications` 本機是否可開）。
- QA 的 `db reset` 被 auto-mode classifier 擋（LS-70 鎖沒問題）→ LS-96（allow 規則或 orchestrator 代跑）。
- Pen 路徑對帳規則對 qa-test 必然不等（設計 lane 在用）→ LS-96（qa 只在需 Pencil 時對帳）。
- View 層「只渲染 `visibleMessages`」無自動化測試底座 → LS-96／LS-95 同題。
- 文案票的 minor（空格、單位）也值得多一輪：長輩看得到。

**設計取捨定案**：不做視覺 disabled（elder-constraints 第 8 條）＝正確；條件是「可見理由永遠在場」（F6／m3 兩句併陳）。

**產品後續**：LS-105（AX3 鍵盤遮 CTA，QA1 新發現）；三項限流體驗改於正式站冒煙驗證（LS-99 已接 Resend）。sweep：無死碼（`d783cf7a`）。

