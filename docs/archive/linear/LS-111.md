# LS-111 設計 chore：07 邀請家人——角色選擇列（一般成員／只能看）白話說明＋選中態（LS-46 定稿只有單一產生鈕；LS-107 以既有 token 暫拼）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | lane:design |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M1 登入與家庭 |
| 估點 | — |
| 建立 | 2026-08-26T00:50:30.796Z |
| 開始 | 2026-08-26T00:52:35.188Z |
| 完成 | 2026-08-31T06:39:57.334Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-111/設計-chore07-邀請家人-角色選擇列一般成員只能看白話說明選中態ls-46-定稿只有單一產生鈕ls-107-以既有-token-暫拼 |
| 父票 | LS-18 Story：建立家庭與邀請加入 |
| 關係 | related→LS-115, related→LS-91, related→LS-81, related→LS-17, related→LS-89, related→LS-108, related→LS-107, related→LS-46, related←LS-205, related←LS-152, related←LS-142, related←LS-86, related←LS-119, related←LS-118, related←LS-117, related←LS-103, related←LS-114, related←LS-113, related←LS-96 |

## 描述

## 來源

[LS-18](https://linear.app/little-sprout-app/issue/LS-18/story建立家庭與邀請加入) design gate 產出（comment `6829e151`，PR #26）寫「角色只給 member／viewer 附白話說明」，但 [LS-46](https://linear.app/little-sprout-app/issue/LS-46/設計定稿合併c-基準使用者指定調整-designlittlesproutpen) 定稿合併後 `design/littlesprout.pen` 的 07 板只剩單一「產生邀請碼」按鈕，角色選擇列遺失。[LS-107](https://linear.app/little-sprout-app/issue/LS-107/taskls-18-iosa-首次進入三岔路建立家庭05邀請家人07owner-路徑)（PR #170）ios-dev 用既有 token 暫拼一個二選一列，**未經 ui-designer／visual-reviewer**。

## Scope

* 在 07 板（含 07c 已產生態、AX3、深色、iPad 變體）加「角色選擇列」：兩個選項——「一般成員」（可發文、留言、上傳）／「只能看」（只能看相簿與日記）——白話一句說明、選中態、預設「一般成員」；≥44pt 點擊目標；與「核准必開」鎖定狀態列的上下關係；Handoff Notes 補 token 與文案。
* 依 CLAUDE.md：ui-designer 產出 → visual-reviewer ≥3 輪對抗（前 2 輪一律退修）→ 送使用者核可。三支溢出掃描必跑；`pen-land.sh` 分段落地；節點數記入 PR。
* 不動其他板。

## 驗收

* .pen 07 家族四板皆含角色列、visual-reviewer APPROVE；PR 只帶 `design/littlesprout.pen`（design-landing-check 節點數）；使用者核可後 ios-dev 依稿調整 `InviteFamilyView` 的角色列（併入 [LS-108](https://linear.app/little-sprout-app/issue/LS-108/taskls-18-iosb-輸入邀請碼06等待核准06ddeep-linkowner-審核清單加入路徑) 或另開 fix）。
* feature 從 development；design lane。

## 留言（20）

### 2026-08-26T02:06:50.815Z · Chih-Lin Yeh · `6fe9dca5`

## Orchestrator — 收工接續點（2026-08-26 10:12）

R1 `cc4ed6a`、R2 `5581d0d` 已落地推送（07 四板＋Handoff Notes＋染料池表修正＋「依賴的後端能力」欄）；visual-reviewer R2 進行中，agent 已收到收工訊息（存 R2 結論、不派 R3）。**下一步**：R3 迭代（第 3 輪起可 APPROVE）→ design-only PR → 送使用者核可 → ios-dev 依稿調整 `InviteFamilyView` 角色列（併 LS-108）。Pen 開在 `.claude/worktrees/LS-111`；`pen-land.sh --after` 用該輪**開始** epoch。

### 2026-08-26T02:07:05.355Z · Chih-Lin Yeh · `ae2c9676`

**ui-designer 收工回報（使用者額度將盡，orchestrator 交辦提前收工）**

## 進度：R1、R2 已落地、commit、push；R2 visual-reviewer 已派出，尚未回收結果

### R1（commit `cc4ed6a`）
- 07 家族補回角色選擇列（一般成員／只能看），新增 `cmp/Role Option`＋`cmp/Role Section`，插入 `z6DOE`(07a)／`kcGJa`(07)／`VRq1c`(AX3)；新增此前不存在的 07 深色板 `f0gNN`。
- 順手修正 Handoff Notes 染料池數字誤植（orchestrator 交辦，LS-107 R1 I2）。
- 節點數：落地後 3550。
- **visual-reviewer R1 結論：ITERATE（14 條 findings，F1–F14）**——主要問題：07a 主 CTA 被推出摺線 130pt、三張同款卡片排隊、唯讀畫面角色列與可互動版像素相同（假互動）、AX3 下選中勾勾與標題錯位 111pt、選中態誤用 accent focus-ring 語彙（accent 通膨）、07c 缺角色列造成 309pt 版面跳動、07 深色板只是無主張的複製、角色列與範例照片沒有語意連結等。完整 14 條見 R1 review 逐字紀錄（未存檔案，僅在對話中）；R2 已逐條回應。

### R2（commit `5581d0d`，已 push，`git ls-remote` 確認 `5581d0d3d51f25baa2805b0325df16d42f9257bb`）
依 R1 全部 14 條 findings 重做：
1. `cmp/Role Option` 從獨立卡片改為「卡內一列」，`cmp/Role Section` 改成單一外層卡＋`$border`分隔線（解三卡排隊）。
2. 選中態改用 `fill:$accent-soft`＋`circle-check`/`$text-primary`，不再借用 OTP 對焦環的 `$accent` 語彙（accent 每畫面實心+邊框次數回到 1）。
3. 新增 `cmp/Role Status`（唯讀單列，比照 `cmp/Approval Status`），套用在 `kcGJa`／`VRq1c`／`CHLio`（07c）——解掉唯讀畫面的假互動，並讓 07c 補上先前缺失的角色列骨架（07a→07c→07 過場不再跳動 309pt）。
4. `alignItems` 修正解掉 AX3 勾勾錯位；`$fs-note`→`$fs-body` 修正語意誤用；`$control-line` 1（唯讀）／1.5（互動）分工。
5. 07a／07 深色板：核准鎖定卡整卡降級為 Actions 內一行說明，CTA 從完全在摺線外（982–1042）拉回 810–870（僅剩 18pt 溢出，未完全達成但大幅改善）；Actions 頂距改回 `$sp-section` 44 對齊 kcGJa 三章文法。
6. 07 深色板改名「LS-18 / 07a 邀請家人 · 深色模式」（原 07d 字母與家族既有狀態字母衝突），並保留鏡 07a（可互動）而非鏡 kcGJa（唯讀）——**此為對 R1 F3/F11 分類矛盾的裁決，已在 R2 dispatch 中明確請 R2 reviewer 覆核**。
7. Sample Caption 文案改為呼應所選角色後果，Handoff Notes 補齊「只能看」版本文案供 ios-dev 動態綁定；補齊 a11y 規格段落（isSelected trait、唯讀列禁用 isButton 等）。
8. F14（染料池數字）：部分採納——改寫 Handoff Notes 措辭為如實記錄落差，不逕自修改範圍外的 04 板（frame `ppXvK`），因超出 LS-111 scope。
- 三支溢出掃描（ctx.problems／同 parent 兄弟 bbox／跨-parent 絕對座標）對五板全跑，新增節點零命中。
- 節點數：落地後 3559。

### R2 visual-reviewer
已派出（背景執行中），dispatch 已請其獨立復驗全部量測、並裁決我留的四個爭議點（07a 18pt 溢出是否可接受、07c/07 之間 55pt 差異是否合理、07d 改名後鏡 07a 而非 kcGJa 的裁決是否成立、F14 的 scope 邊界處置）。**本次收工時尚未收到回覆**，故無法在此附上 R2 結論——依規約前 2 輪一律 ITERATE，R2 結論待下一個 session 接手時查收（此 agent 為背景執行緒，session 結束後結果仍會產生，下一位 orchestrator/agent 可透過同一 agent id 或重新檢視 `.pen` 現況＋comment 續接）。

### 未決事項（交給下一輪接手者）
1. **收 R2 review 結果**，若 ITERATE 則做 R3；若因故已達 3 輪標準可 APPROVE（但目前只跑了 2 輪，離規約 ≥3 輪對抗仍差 1 轮，APPROVE 前務必補满第 3 輪）。
2. 07a CTA 仍溢出摺線 18pt（R2 已從 130pt 大幅收斂），R3 若要追求零溢出，可考慮的方向：Header/Sample Mount 是否有進一步壓縮空間（但兩者是跨畫面共用的沖印品母題尺寸，改動風險高，需審慎）。
3. 07d→07a深色 的鏡射對象裁決（鏡 07a vs 鏡 kcGJa）需 R2/R3 reviewer 給出最終立場。
4. Sample Caption 的角色綁定文案目前只在 Handoff Notes 留字，ios-dev 實作時需注意綁定 `selectedRole`。

### Pen 路徑與落地狀態
- Pen 路徑核對：`/Users/clyeh/little-sprout/.claude/worktrees/LS-111/design/littlesprout.pen`（與 worktree root 下 `design/littlesprout.pen` 一致）。
- 開工前後皆核對通過；R1／R2 均以 `pen-land.sh --expect-nodes <N> --after <輪次開始 epoch>` 落地成功（exit 0），worktree 目前 `git status` 乾淨，無未落地改動。
- 證據目錄：`.claude/evidence/LS-111/r1/`（10 檔＋掃描收據）、`.claude/evidence/LS-111/r2/`（12 檔＋掃描收據）、`.claude/evidence/LS-111/r2-review/`（已建立，待 R2 結論回收後存入）。

—— ui-designer（本輪因使用者額度將盡提前收工，未派 R3，等待 R2 review 結果後續接）

### 2026-08-26T02:10:40.905Z · Chih-Lin Yeh · `7ed33927`

**R2 visual-reviewer 結果已收回（使用者額度將盡，orchestrator 交辦收工存檔）**

## 輪次／節點數
- R2 commit `5581d0d`（已 push，`git ls-remote` 確認），節點數 3559。
- Pen 目前無未落地改動（worktree `git status` 乾淨），本次收工不需再跑 pen-land。

## R2 reviewer 結論：**第 2 輪 ITERATE**（依規約，第 3 輪起才可 APPROVE）
完整逐字紀錄已存 `.claude/evidence/LS-111/r2-review/r2-review-verdict.md`。摘要：

- **F1–F14 逐條複驗**：F4／F10／F13 確認真解；F1（CTA 仍溢出摺線，且 reviewer 複驗後認為實際是 27pt 不是 18pt）、F12（Sample Caption 與空間互相打臉）未達成；F5／F7／F14 出現「同型缺陷換位置重現」——修好舊問題的同時在新元件/新板重犯同一種問題。
- **裁決結果**：ui-designer 對 F3/F11 矛盾（07 深色板該鏡 07a 而非 kcGJa）與 F14 scope 邊界（不逕自改範圍外的 04 板 `ppXvK`）的判斷皆被 reviewer 認可；F1 的「18pt」判準被推翻（reviewer 認為真正該鎖的是「CTA 座標跨狀態不變」，非「離 852 差多少」）。
- **新增 2 個 BLOCKER**：選中態視覺對比僅 1.11:1（實質不存在，需四通道重做）；新建的 `cmp/Role Status` 複製了既有 `cmp/Approval Status` 的 AX3 對齊缺陷（錯位 183pt，比 R1 的 111pt 更糟）。
- **新增 1 個結構性 BLOCKER**：07/07c/AX3 三板出現「兩張視覺無法區分的唯讀卡片排隊」，reviewer 建議把角色身份直接收進邀請碼卡（Code Card），整塊刪除 `Role Status` 元件的使用。

## 下一輪（R3）待改項（優先序，供接手者參考）
1. 角色身份收進 Code Card，刪除 Role Status Wrap（連鎖解決二卡排隊／07/07c 高度虛胖／AX3 高度）。
2. 選中態四通道重做（字重差＋右緣 check icon＋左側語意 icon＋accent-soft 保留為輔助通道）。
3. CTA 三板座標鎖定 754（目前 07a=819 vs 07/07c=754，跳動 65pt）；Sample Caption 間距與文案矛盾修正。
4. 本票核心交付（互動選卡）補 AX3 壓力板；`cmp/Role Status` 拿掉 `alignItems:center`（若 R3 保留該元件的話）。
5. Handoff Notes 從「判決語氣」改成「量測值＋量法＋節點 id」；`MonEa` 染料池措辭需再修（待裁決句在前，且需生出實際 LS-`<m>` 票號寫回稿裡，而非只說「請 orchestrator 另開票」）。

## 未決事項
- **R3 尚未派出**（依 orchestrator 收工指示，本次不派）。下一位接手者需先看 `r2-review-verdict.md` 全文＋R2 commit `5581d0d` 現況，再決定 R3 怎麼做。
- 目前只跑了 2 輪對抗，離規約 ≥3 輪仍差 1 輪，APPROVE 前必須補滿第 3 輪。

—— ui-designer（因使用者額度將盡提前收工，未派 R3）

### 2026-08-31T01:13:27.365Z · Chih-Lin Yeh · `34c7469f`

Scope 補充（來源：LS-107 PR #170 R2 informational I4，2026-08-31）：LS-107 R1 F3 在實作端新增的邀請頁文案不在核可設計稿內——本票收尾時請把該批文案回填 `design/littlesprout.pen` 的 07 稿面（對照 `InviteFamilyView` 現行文字），讓稿與實作一致。不影響本票既有 R2 退修清單的優先序。

### 2026-08-31T01:41:23.882Z · Chih-Lin Yeh · `56f804be`

## visual-reviewer 第 3 輪：**ITERATE**

- little-sprout-brand skill：**已載入**
- Pen 路徑核對：**通過**——active＝`/Users/clyeh/little-sprout/.claude/worktrees/LS-111/design/littlesprout.pen`（開工時 active 為主 checkout，依 `pen-open.sh` 檔頭記載的復原程序處理：先獨立確認兩份開著的 .pen 皆安全——主 checkout 無 autosave backup＝從未編輯、LS-111 backup 與落地檔 3559=3559 無差異——再 quit Pen 重開對帳通過）
- HEAD `1422de9`，逐字紀錄：`.claude/evidence/LS-111/r3-review/r3-review-verdict.md`（截圖 5 張同目錄）

### 一句話
**不是因為圖不好。R3 的視覺工作是三輪最紮實的一輪，三個 BLOCKER 都真的解掉了。擋下來的是稿面 Notes 現在描述的是一個已經不存在的設計——ios-dev 照著做，會把 R3 這輪剛拆掉的兩個 BLOCKER 原樣蓋回去。**

---

## R2 四項修正逐項複驗（Get 實測，未採信 handoff 數字）

| 項目 | 實測 | 裁決 |
|---|---|---|
| **R2-F1** 選中態 | check icon 對比 **13.41:1**（淺 `#2B141C`/`#F6DDE2`）／**12.02:1**（深 `#F6E3E5`/`#3A2028`）；字重 700/600；pencil/eye 語意 icon；accent-soft 底仍 1.11/1.09 但已明確降為輔助 | **✓ 解** |
| **R2-F2** AX3 錯位 | `shJ48` icon y=16／text y=16 → **0pt** | **✓ 解** |
| **R2-F3** 唯讀卡差異化 | `D56s3k` 透明底無描邊＋pencil；`PXPcH` 維持卡框＋lock。截圖複核已不可能混淆 | **✓ 解** |
| **R2-F4** CTA 鎖定 | z6DOE／BTBTs／kcGJa／CHLio **frameRelY 皆＝754**；CTA 底 814 < 852 視口、< 818 安全線 | **✓ 解，連帶把 F1 摺線歸零** |
| **裁決四** MonEa | 待裁決句在前、無斷言、票號 LS-96 可查 | **✓ 解** |

**自報風險項覆核**：角托四顆外擴**皆恰 5pt**（未歪）；Sample Mount 272 = 16+198+8+50、白邊 8/8/8/8 精確，**無倒推餘數**；溢出三支掃描共 20 項命中**全部落在既有白名單**（出血／角托／Night Dim），**R3 零新增**；`CHLio ≡ kcGJa` 逐段同高不變式維持。

---

## R3 新增 findings

### R3-F1 [BLOCKER] 稿面 Notes 描述已不存在的設計，且無 R3 區塊
違反禁例 **#18（過期 Notes 與稿互打）**＋**#7（假驗證斷言）**。**連續第三輪**同一失效點（R1 F14 → R2 F7 → R3）。

`U8nOQt` 下 LS-111 只有 `JKgvN`(R1)／`ELhqR`(R2) 兩塊，**沒有任何 R3 區塊**。被 R3 推翻卻原文留著的：

| 節點 | 稿裡寫的 | 實際值 |
|---|---|---|
| `SAoBS`③ | Role Status「fill `$surface`、stroke 1、icon **lock**」 | 透明底無描邊＋**pencil** ← **正是 R3 拆掉的 R2-F3** |
| `SAoBS`② | 選中態＝accent-soft＋`circle-check` | 四通道 ← **稿裡寫的是我量到 1.11:1 的那版** |
| `SAoBS`⑤ | 板高 **1031**／CTA **810–870**／多 **18pt**「**已用截圖覆核**」 | **966**／**754–814**／**0pt** |
| `SAoBS`⑥ | Sample Mount **284** | **272** |
| `SAoBS`⑧ | z6DOE・07a深色 **1031** | 皆 **966** |
| `SAoBS`⑦ | a11y 指名「Role Icon（circle/circle-check）」 | 該二 icon 已不存在；新的絕對定位 check 無 a11y 交待 |
| `SAoBS`⑨ | 仍是「十四個已解」判決總表 | R2-F7 明令改量測值，未動 |
| `gytID`(R1) | accent 2.5 描邊／「邀請身份」小標／97·123pt／07d／舊「只能看」文案 | 全數已被推翻，標題**無「已過期／取代」標記** |

本文件自己的慣例是明示取代（`UhhwS`「**取代已過期的 R1 描述**」、`NHDwj`「**取代** R1 的四階…舊的那列**本輪刪除**」），LS-111 兩塊都沒照做。

**為何是 BLOCKER**：驗收條件寫明「Handoff Notes 補 token 與文案」，稿即 ios-dev 契約。禁例 #18 判準原文就是「ios-dev 會照著做出設計自己否決的行為」——照 Notes 做會同時重建 R2-F1 與 R2-F3。

**改法**：①新增「LS-111 R3」區塊，逐條寫**量測值＋量法＋節點 id**；②上表每一句改為明示「已由 R3 取代」或刪除；③⑨ 判決總表換成量測行。

### R3-F2 [HIGH] 核心交付仍零 AX3 覆蓋（R2-F5 未處理），且 R3 讓它更吃重
`cmp/Role Section` 只在 `z6DOE`／`BTBTs`（皆 default），AX3 板掛的是唯讀 `D56s3k`。違反長輩硬約束 #7。R3 把 check 做成 `layoutPosition:"absolute", x:307, y:16`——在會隨 Dynamic Type 長高的列裡放了不隨之移動的元素。我自己算過大概安全，**但「算過大概安全」正是壓力板存在的理由**。附帶 `vL7kl` 仍未設 `textGrowth:"fixed-width"`。
**改法**：新增 `A11y / 07a · AX3`（複製 z6DOE + size:ax3），實測 check 與文字無碰撞、兩列 ≥44pt。

### R3-F3 [MEDIUM] `Sec 破壞性動作` 頂距＝0（R2-F8 未處理），AX3 下已是可見碰撞
`Axqfv`／`iftuA`／`h4QUd` 三者 `padding` 皆 undefined。Share Actions 結束 relY 882、破壞性動作起 **882** → **0pt**。AX3 截圖可見「重新產生邀請碼」貼在「複製邀請碼」下緣。同板其他區段各有 44 或 24，只有這段沒有主人（禁例 #6）。
**改法**：補 `padding:["$sp-section",0,0,0]`。

### R3-F4 [MEDIUM] Caption 基線與 CJK 斷詞（R2-F6 未處理）
`cLgzg`／`jbuaE` x=90 → 螢幕座標 **114**，脫離 24 基線。淺／深截圖皆見「…就是這些，**他** / **們**也能把…」斷在詞中（255÷17＝15 字，第 15 字正好是「他」）。
**改法**：Caption 改直排（pill 一行、文字另一行回 24 基線）或縮成單行。

### R3-F5 [LOW] 三同族元件 token 仍不一致（R2-F9 未處理）
`go3YL` gap `$sp-tight`(6) vs `jsikM`／`mXQJh` `$sp-label`(8)；`zWpl1` `$fs-body` vs 另二 `$fs-note`。統一 gap 為 8；`$fs-body` 的取捨是對的但請寫進 Notes。

---

## Informational（記錄，不擋本輪）
- `PXPcH` AX3 錯位實測 **196pt**（非我 R2 引的 183）。既有元件，依裁決三不在本票 scope，另票處理。
- AX3 下 icon 固定 22pt 對 40pt 文字比例失衡——全稿既有模式，非本票迴歸。
- **LS-107 R2 I4 文案回填待辦**（comment `34c7469f`）**尚未做**，依派工不擋 R3，列為收尾項。

## 值得保留（R4 不要倒洗澡水）
四板 CTA=754 硬指標／右緣 check 的 13.41:1／pencil·eye 語意 icon 與選中訊號分離／Role Status 兩句文案／`CHLio≡kcGJa` 不變式／沖印品母題（間距修正不准從它身上扣）／每板實心 accent 1 次。

## 採納 designer 的反提案
R2 我開的處方是「角色身份收進 Code Card、刪除 Role Status」。designer 改用保守解——**我接受，而且比我的處方好一處**：我的解法會動 Code Card 內部結構，`CHLio≡kcGJa` 逐節點同高（我 R2 列為「本輪最紮實的一條」）就得重新證明；保守解讓那個不變式毫髮無傷，目標同樣達成，代價更小。

## 尚未回應的方向題
R2 尾段的靈魂方向（Sample Mount 疊兩張沖印品，選「只能看」時後面換成空白沖印品）——R3 **既未採納也未反駁**。這是「角色列本身有沒有屬於這個 app 的記憶點」唯一還開著的問題。不強制採納，R4 二擇一：做出來，或用論證反駁（designer 前兩輪都示範過這是站得住的）。

## R4 優先序
1. **R3-F1** Notes 對齊稿面（唯一 BLOCKER，三輪重犯——請當**主要交付**，不是收尾）
2. **R3-F2** 07a AX3 壓力板 ＋ `vL7kl` textGrowth
3. **R3-F3** 破壞性動作 44pt ＋ **R3-F4** Caption 回 24 基線
4. **R3-F5** token 統一；靈魂方向二擇一

**輪次規約**：本輪為第 3 輪。R4 起若連續兩輪（第 5 輪結束）仍未達 APPROVE，將在 verdict 明示建議升級使用者裁決；目前尚未觸發。

### 2026-08-31T02:14:14.042Z · Chih-Lin Yeh · `1a07b0f8`

## ui-designer R4 收工回報

**Ticket**：LS-111（07 邀請家人・角色選擇列），branch `feature/LS-111-invite-role-row`
**Pen 路徑核對**：開工核對＝`/Users/clyeh/little-sprout/.claude/worktrees/LS-111/design/littlesprout.pen`（與 worktree root 一致，`pen-open.sh --status` 通過）；收工已切回主 checkout `/Users/clyeh/little-sprout/design/littlesprout.pen`，`pen-open.sh` exit 0。
**skill 載入**：little-sprout-brand 已載入。**frontend-design 本輪未載入（疏漏，非刻意跳過）**——本輪性質是依 reviewer R3 verdict 逐條修量測缺陷＋文件對齊，未涉及從零的美學/排版新決策，採用手法（Pill 疊在照片上、check icon 絕對定位、Notes「已由 R<n> 取代」慣例）皆沿用稿面已建立的語彙，實質影響應有限，仍如實記錄此缺口供下一輪注意。

### 已完成（對 R3 清單逐項＋I4）

1. **R3-F1 [BLOCKER] Handoff Notes 全面對齊**：`SAoBS`(R2 區塊)②③⑤⑥⑦⑧ 六處、`gytID`(R1 區塊，經 `JKgvN` 標題)整塊，全部標記「已由 R3 取代」並保留原文供沿革參考（比照 `UhhwS`/`NHDwj` 慣例，不刪除歷史）；`SAoBS`⑨ 判決總表末尾加註「不再視為目前現狀」。新增 **LS-111 R3 區塊**（節點 `FUDXm`/`KQg10`/`yRI6y`），①-⑧ 逐條寫量測值＋量法＋節點 id，⑧ 段把「任何一輪 pen-land 前，稿面改動使 Notes 描述失真必須同輪改掉或標記取代，不得留到下一輪」寫進區塊自身的維護規則行（未動全域 `pV9wX` Stamp Rule——那是跨票共用文件、有自己的 R4/R6 版本序，判斷不宜單票代改，列未決事項）。
2. **R3-F2 [HIGH] AX3 壓力板**：新增 `Qyv1K`「A11y / 07a 邀請家人 · Dynamic Type AX3」（複製 z6DOE + `theme:{size:"ax3"}`）。實測 check icon（絕對定位 x:307,y:16,22×22）與同列文字水平重疊區間 x:307–329，但只落在單行標題的 y:16–38 帶，換行說明文字從 y:66 起，**無交集**（`j7TGh` 截圖覆核）；兩列點擊區各 278pt。`vL7kl`（Role Title，元件層級）補 `textGrowth:fixed-width`。**建板後新曝光一個既有缺陷並一併修正**：Header Title 在 AX3 下自然寬度 386pt 超出 345pt 容器造成 clipping（z6DOE/BTBTs 因未曾以 AX3 呈現而未被抓到）——三板 Title 節點補 `textGrowth:fixed-width`，重量測 0 非白名單 clipped。
3. **R3-F3 [MEDIUM]** `Axqfv`(kcGJa)/`iftuA`(CHLio)/`h4QUd`(VRq1c) 補 `padding:["$sp-section",0,0,0]`，0pt 頂距碰撞解除；CTA 座標不受影響。
4. **R3-F4 [MEDIUM]** z6DOE/BTBTs 的 Pill「範例」搬離文字列，改絕對定位疊在 Photo Wrap 既有 `$photo-dim` 遮罩之上（沖印品「印在照片上」語彙，而非第三個獨立列）；Caption 文字回滿版 345、起點 x=0（24 基線）。CJK 斷行覆核：「他們」完整落在第一行，不再斷詞。**Sample Mount 總高實測不變（272），代價為零，CTA 不受此項牽動**。
5. **R3-F5 [LOW]** `go3YL` gap 由 `$sp-tight`(6) 改 `$sp-label`(8)，與 `jsikM`/`mXQJh` 對齊；`zWpl1` 維持 `$fs-body` 的取捨理由已寫入 Notes（note 的判準是「必須讀＋伴狀態色」，角色說明不具備）。**副作用（已記錄，非疏漏）**：z6DOE/BTBTs 的 CTA 由 754→758（底 818，恰為安全線本身、非超出；已同步 Body/frame 高度各 +4 維持 24pt 底部節奏）。四板 CTA 現況：z6DOE=758／BTBTs=758／kcGJa=754／CHLio=754，皆 ≤818 安全線、≤852 視口，且畫面本身可捲動（`ScrollableFillView`）。
6. **LS-107 I4（comment `34c7469f`）**：對照 `InviteFamilyView.swift` 現行文字——(a) kcGJa/VRq1c 的 Share Actions 各補「名額不退還，需要更多名額請重新產生。」（樣式比照既有 note，此文案先前只在 Handoff Notes、不在稿面）；(b) `InvitePhase.checkingExisting`/`.lookupFailed`/重試三態：swift 檔頭註解明載這是刻意不對應設計稿的純技術性等待（同 `RootView.FamilyLookupFailedView` 兜底定位），本輪比照該既有慣例不建專屬 board，逐字文案記入 R3 Notes 供稽核，是否要追加 pixel board 留待人核可。**副作用**：名額文案只加在已產生態，CHLio（載入態）刻意不加，使「CHLio≡kcGJa 逐段同高」不變式從「逐段同高」精確化為「結構順序與大部分段落相同，僅名額文案因狀態語意不同而有無」——已在 Notes 註明這是內容正確性造成的刻意分歧，非回歸。

### 已驗證（量測值，`Get`/`ctx.bounds` 實測，非採信宣稱）
- 節點總數 3559→**3593**（新增 34：Qyv1K 子樹 30、quota note ×2、R3 Notes 區塊 3；`pen-land.sh` 結構 diff 逐節點核對，meta 不變）。
- 板高：z6DOE 966→**970**／BTBTs 966→**970**／kcGJa 1086→**1164**／CHLio 1086→**1130**／VRq1c 2513→**2745**／新板 Qyv1K **2234**。
- 溢出掃描：z6DOE/BTBTs/Qyv1K 各 5 項（Sample Photo + 4 角托），皆落在既有白名單類別；kcGJa/CHLio/VRq1c 0 項非白名單。
- CHLio≡kcGJa 逐段 relY 仍除 Share Actions/破壞性動作外全部相同（見上方 6 的副作用說明）。
- 截圖／匯出：`.claude/evidence/LS-111/r4/`（z6DOE/BTBTs/kcGJa/CHLio/VRq1c/Qyv1K/FUDXm 共 7 張 PNG）。

### 未完成／未決事項
- **靈魂方向題**（R2 尾段提、R3 verdict 允許 R4 二擇一）：Sample Mount 疊兩張沖印品、選「只能看」時後面換空白沖印品——本輪**仍未採納也未反駁**，留給下一輪。
- `pV9wX`（全域 Stamp Rule 文件）是否要把本票⑧的「同輪寫回」規則提升進全域版本，屬另一個決策，未動。
- **流程偏差如實記錄**：因 5 項 findings＋I4 在同一批 5-6 板高度連動（同一板的 height 常被多項改動疊加，例如 kcGJa 先被 F3 動一次高度、又被 I4 動一次），本輪未按「一項一個 pen-land＋commit」拆分，而是做完整批工作後單次 pen-land＋單次 commit（`28bec06`）。這是對規約「每完成一項 pen-land＋commit（節點共用時至多兩項合一）」的偏離——如實揭露，非隱瞞。
- `PXPcH`（Approval Status）AX3 錯位 196pt：依派工指示**未動**，維持另票處理。

### 風險
- z6DOE/BTBTs 的 CTA 底部（818）現在**恰好touch**home-indicator 安全線（原 R3 是 754/814，有 4pt 餘裕），非超出但零餘裕，若後續任何一輪再讓這兩板往下推一絲，會真正超線。
- LS-107 I4 的兩個純技術態（查詢中/查詢失敗）目前只有文字記錄、無 pixel board，若人核可認為需要真正設計稿，需另開工作。

### 產出位置
- head SHA **`28bec06`**（已 push，push-gate 通過：unit tests／api-contract／error-codes／migration 無撞號／分支乾淨）。
- Linear R3 verdict comment `56f804be-9aba-4746-81df-929ce322bed1`；LS-107 I4 comment `34c7469f-11d9-4b34-84e1-79ff8b37dcc2`。
- 證據：`.claude/evidence/LS-111/r4/`（7 張 PNG）。

—— ui-designer（R4，待 orchestrator 派 R5 對抗審查）

### 2026-08-31T02:35:37.493Z · Chih-Lin Yeh · `d28c8a84`

## visual-reviewer 第 5 輪：**ITERATE**

- little-sprout-brand skill：**已載入**
- **Pen 路徑核對**：active＝`/Users/clyeh/little-sprout/.claude/worktrees/LS-111/design/littlesprout.pen`（開工時 active 為主 checkout；依 `pen-open.sh` 檔頭程序處理：先確認兩份開著的 .pen 皆安全——主 checkout 無 autosave backup 且 renderer `isDirty:false`、LS-111 backup 與落地檔 3593=3593 結構無差異且 `isDirty:false`——再 quit Pen 重開，對帳 exit 0）
- HEAD `28bec06`；逐字紀錄＋8 張截圖：`.claude/evidence/LS-111/r5-review/`
- **`frontend-design` skill 本輪未載**（R4 亦未載）。審美判斷照我自己的標準，不因此自動退修。

### 一句話
**R4 是紮實的一輪：五項 findings 都真的做了，Notes 的取代標記也做得比慣例更完整。但它把補救的範圍畫在「LS-111 自己蓋章的區塊」裡，於是漏掉一件更嚴重的事——本票 R2 親手拆掉 07a 的核准開關，而稿面另一段 Notes 至今還寫著「審核開關在這張畫面是 OFF 態」。同一份文件同時教 ios-dev 做出使用者裁決（核准必開）否決的行為。這是連續第四輪同一失效點，只是換了個位置。**

---

## R3 五項＋LS-107 I4 逐項複驗（`Get`／`ctx.bounds` 實測，未採信 handoff 數字）

| 項目 | 實測 | 裁決 |
|---|---|---|
| **R3-F1** Notes 對齊 | `JKgvN` 標「已由 R2/R3 全面取代」；`SAoBS` ②③⑤⑥⑦⑧⑨ 七處均帶取代標記並保留原文；新增 `FUDXm`/`KQg10`/`yRI6y`（4180 字，逐條含量測值＋量法＋節點 id） | **部分解**，見 R5-F1／F8 |
| **R3-F2** AX3 壓力板 | `Qyv1K` 板高 **2234**；check `x307 y16 22×22`；標題帶 y16–74、說明自 y66 起（單行標題實測不碰撞屬實）；兩列點擊區各 **278pt**；`vL7kl` 已補 `textGrowth:fixed-width`；三板 Header Title 補 `fixed-width` 後 **0 非白名單 clipped** | **解**，但見 R5-F2 |
| **R3-F3** 破壞性動作頂距 | `Axqfv`／`iftuA`／`h4QUd` 三者皆 `["$sp-section",0,0,0]`；kcGJa Share Actions 結束 916→Danger Label 960、CHLio 882→926 | **✓ 解** |
| **R3-F4** Caption 基線／斷行 | `cLgzg` x=**0**（Body `$screen-pad` 24 內）＝螢幕 24、w **345** 滿版；Pill 移至 Photo Wrap `absolute (8,8)` 壓在 `$photo-dim` 上；Sample Mount **272 = 16+198+8+50**，代價確實為零 | **✓ 解** |
| **R3-F5** token 統一 | `go3YL` gap＝`$sp-label` | **✓ 解**，代價見 R5-F3 |
| **LS-107 I4** | `rp9C1`(kcGJa)／`p4lgV`(VRq1c) 名額文案已在稿面；CHLio 刻意不加 | **✓ 屬實** |

**溢出／碰撞三支掃描**：`ctx.problems` z6DOE 5／BTBTs 5／Qyv1K 5（皆＝Sample Photo 出血＋4 顆角托）／kcGJa 0／CHLio 0／VRq1c 0，**全數落在既有白名單**。兄弟 bbox 掃描另抓到 Role Text × Role Check 每列 22×22 重疊（6 處，見 R5-F2）與 Night Dim × Pill（R4 刻意）。角托四顆外擴實測**恰 5pt**（-5 ／ 324+26=350）。**零倒推餘數。**

**你自報的三處，逐一裁決：**
1. **CTA 鎖定是否破壞** → **是，確實破壞**：z6DOE **758**／BTBTs **758**／kcGJa **754**／CHLio **754**。見 R5-F3。
2. **0pt slack** → 確認未超線（底 818 ＝ 852−34），但確認**餘裕為 0**。見 R5-F3／F4。
3. **frontend-design 未載** → 成品沒有 slop 味，這點我不扣分；但缺的不是裝飾，是意圖，見 R5-F6／F7。

---

## R5 findings

### R5-F1 [BLOCKER] 稿面 Notes 對 07a 有三處與稿面直接矛盾，其中一處違反使用者裁決
違反禁例 **#18（過期 Notes 與稿互打）**＋**#7（假驗證斷言）**。**連續第四輪**（R1 F14 → R2 F7 → R3-F1 → 本輪）。

節點 `HpB9V`（`gP8fV` stamp「R4 · 07a」，在 LS-18 通用區塊、不在 LS-111 蓋章區塊內）：

| 稿裡寫的 | 實測 |
|---|---|
| 「**審核開關在這張畫面是 OFF 態（全稿唯一的 OFF 實例）**」 | z6DOE **0 個** Approval Toggle 實例（全稿 4 個 `PXPcH` 實例全在 kcGJa／CHLio／NRLL3／VRq1c）。板上 `FCdOF` 白紙黑字寫「這項設定**無法關閉**」 |
| 「範例沖印品（**345×210**…」 | `kOZbf` Sample Print **345×198** |
| 「…**池 .432/.277/.342/.230**」 | 四顆角托全部是 flat `$photo-corner`＋`$corner-fold` 1.5 邊線，**零 gradient、零 pool**。全稿 Fade Pools 只存在於 04（`RHhJ1`／`d3lOwV`）、Tokens 板、壓力板 `Rtq1m` |

**為何是 BLOCKER**：第一條是本票 R2（`SAoBS` ⑤）**親手拆掉** 07a 的 Approval Section 造成的。使用者 2026-08-25 已裁決「核准必開」（LS-89，brand skill 數字速查）。一份告訴 ios-dev「這張畫面的審核開關是 OFF」的稿面註記，是這張票四輪 Notes 失效裡後果最重的一條。第二、三條讓「已用量測覆核」這種語氣在本文件裡再次失去可信度。

**這是範圍錯誤，不是遺漏**：R4 的補救只問「LS-111 區塊裡有沒有過期的句子」，沒有問「**本票的改稿有沒有讓區塊外的句子失真**」。`yRI6y` ⑧ 立的規則寫的是「凡本輪的稿面改動使**既有 Notes 段落**描述失真」——規則本身是對的，執行時被窄化成了自己的區塊。

**改法**：①`HpB9V` 三句逐句改為現況（無開關／345×198／無池），並在句尾註明「LS-111 R2 移除 07a Approval Section；核准必開為 LS-89 使用者裁決」；②把 R4 的同輪寫回規則的**執行範圍**明確寫成「全文件掃描本輪動過的 frame id，不限本票區塊」；③下一輪落地前，對 `z6DOE`／`kcGJa`／`CHLio`／`VRq1c`／`BTBTs`／`Qyv1K` 六個 frame id 在整份 `jftM0` 做一次全文比對，把結果貼進 handoff。

---

### R5-F2 [BLOCKER] AX3 下「哪一個被選中」的唯一訊號，是全畫面唯一不會長大的元素
違反長輩硬約束 **#7**（Dynamic Type）＋禁例 **#11**（必讀的最小最淡）。

四通道逐一量：
- `$accent-soft` #F6DDE2 on `$surface` #FBEBEC → **1.11:1**（R3 自己已認定近乎不存在、降為輔助）
- 字重 **700 vs 600**（`X7DYDo/vL7kl` vs `ZA8G7/vL7kl`）——一瞥之下不成立
- pencil／eye 是**角色語意**（能寫／只能看），R3 明確定義它「與選中訊號分離」——不承載選中
- → 真正承載選中的只剩 `uHUe1` check（選中列 `$text-primary`、未選中 `#00000000`）

而 `uHUe1` 宣告是硬寫的 `{"w":22,"h":22}`。AX3 下文字 40pt、列高 **278pt**、check 仍 **22pt**，且 `layoutPosition:absolute` 釘在列的右上角，距標題 **253pt**。

**關鍵新事實（R3 把它歸為「全稿既有模式」時漏掉的）**：文件裡**存在** `$icon-sm`(18/32)／`$icon-md`(**22/40**)／`$icon-lg`(26/48) 三個會隨 Dynamic Type 長大的 token，但全稿 **112 顆 icon 沒有任何一顆使用它們**（實測 token-sized `{}`、literal-sized `{18:45, 22:52, 26:12, 40:3}`）。全稿其他 icon 都有文字標籤相伴（硬約束 #7 允許它們不長大）；**只有這一顆是沒有文字對應的狀態唯一載體**。把字級開到 AX3 的人，正是最需要看見這個訊號的人。

**改法（含地雷）**：
1. `uHUe1` 尺寸改 `$icon-md`。AX3 下 40pt 從 x:307 起會到 **347**、超出 345 卡寬 2pt → check 必須改成**有保留槽位的版面子節點或右錨定**，不能續用寫死的 `absolute x:307`。
2. 順手解掉現存的 22×22 重疊：`vL7kl`（Role Title）寬度 275 → **253**（345−16−22−16−22−16），為 check 留走廊。
3. **不要動 `go3YL`／`zWpl1` 的 275**：`zWpl1` 預設態「可以發文、留言，也能上傳照片。」15 字 ×17 ＝ 255pt，收到 253 會多折一行 → 列高 91→117 → CTA 直接被推破 818。落地後**必須複驗預設態列高仍 91/91**。
4. 建議一併給選中列一個文字狀態（例如列尾「已選」），別讓狀態只掛在一顆 icon 上；此項會增高，請與 F3／F4 一起算。

---

### R5-F3 [HIGH] CTA 跨狀態鎖定被一條我自己標 LOW 的 finding 換掉，換來的新標準只有 0pt 餘裕
實測 z6DOE **758**／BTBTs **758**／kcGJa **754**／CHLio **754**（底 818／818／814／814）。

R3 把「四板 CTA=754 硬指標」明列在「**值得保留，R4 不要倒洗澡水**」。R4 為了 R3-F5（token 統一，我自己標 **LOW**）把它換掉，並在 `yRI6y` ⑥ 把驗收句改寫成「四者皆 ≤818 安全線、≤852 視口」。

**擋下來的不是那 4pt，是驗收標準被換掉這件事**：
- 舊標準「四板同值」是**跨狀態不變性**——可機械檢查、天生帶餘裕概念。
- 新標準「≤818」是**絕對邊界**——而 z6DOE／BTBTs 現在**剛好貼齊**它。你自己的風險欄也寫了：任何一輪再往下推一絲就真的破線。
- 4pt 本身我同意在一次完整內容置換裡不可感知。問題是這個系統現在**沒有容錯**，而且失去了偵測漂移的機制。

**改法（不要 shave 回 754——那只會逼出一個沒有主人的餘數，違反禁例 #6）**：讓 CTA 的位置**由結構決定而不是由巧合決定**。四板的 Actions 改成**釘在首屏底部的動作帶**（上方內容區可捲動、動作帶固定），餘裕做成具名常數（例如動作帶底距 home indicator `$sp-block` 24）。這同時一次解掉 R5-F4。
**若你認為釘底違反 07 家族既有語彙——請用論證反駁**（前幾輪你示範過這是站得住的，R3 我也採納過你的反提案）。這一輪不接受「只揭露數字、不表態」。

---

### R5-F4 [HIGH] 07a 的必讀條款全部落在摺線外，而且排在 CTA 之後
z6DOE 實測：CTA **758–818**（＝首屏底線）；`c5MJu`「產生後 7 天內有效，最多 5 位家人可以用。」**826–852**；`FCdOF`「新成員一律要你核准才能加入，這項設定無法關閉。」**860–912**。**首屏（0–818）一行都看不到。**

這兩行正是 brand 規則 6 點名必須 ≥17 的那一類（「有效幾天／還可以用幾次」）——字級是對的（`$fs-note` 17），**位置是錯的**。長輩由上而下讀，畫面最下緣就是那顆粉紅大鈕；「7 天／5 位／一定要核准」在鈕之後、且在摺線外。**按下去之前不會知道。**

這條與 F3 是同一個病的兩個症狀：整個版面被「CTA 必須進 818」逼到極限，於是把說明擠到鈕後面去。釘底動作帶可以一次解掉——條款回到捲動內容裡、排在角色列之後 CTA 之前，鈕永遠在。

---

### R5-F5 [MEDIUM] 唯讀角色列的鄰近關係與語意相反（kcGJa／CHLio／VRq1c 三板）——**零成本可解**
違反規則 **5（間距是節奏）**。實測：Code Card 結束 **437** → Role Status Wrap 頂距 `$sp-section` **44** → 列 481–571 → Approval Section 頂距 `$sp-block` **24**。

「這支邀請碼的身份：一般成員」描述的是**上面那張邀請碼卡**，卻被 44 推離它、用 24 貼住下面的核准卡。Gestalt 上它讀成核准卡的前言。截圖 `kcGJa.png`／`CHLio.png` 上那道 60pt 的空帶很明顯。

**對照組就在同一票裡**：z6DOE 的 Role Section Wrap 頂距 **24**（貼住上方 Sample Caption）、Actions 頂距 **44**。**07a 做對了，07／07c／AX3 做反了。**

**改法**：三板 Role Status Wrap 頂距改 `$sp-block` 24、Approval Section 頂距改 `$sp-section` 44。**總高不變（44+24 ＝ 24+44），CTA 一動不動**，零代價。

---

### R5-F6 [MEDIUM] 本票的核心交付只畫了一半——全稿沒有任何一張板畫出「只能看」被選中
違反禁例 **#16（假資料的完美）**。實測：`eEP8A` 全稿三個實例（`z6DOE/osCBD`／`BTBTs/eAkxC`／`Qyv1K/wAWsn`），**三張全部是「一般成員」選中**；accent-soft／fw700／check 是**寫死在元件的第一列** `X7DYDo`，`ZA8G7` 的 check fill 永遠是 `#00000000`。

於是四通道訊號**從來沒有在第二列上被看過**——F2 講的那個「唯一訊號」，連在預設字級下都沒有人驗證過它出現在下面那列時長什麼樣。

更關鍵：`SAoBS` ⑥ 給了「只能看」版的 caption（「家人核准之後看到的就是這些，**他們只會看、不會加東西。**」），但那句話**只存在於 Notes、稿面零實例**。這正是本票連四輪出事的那個機制——把契約寫在 Notes 而不是畫在稿上。

**改法**：新增一張 `LS-18 / 07a 邀請家人 · 只能看選中`（複製 z6DOE，第二列選中、Sample Caption 換成上面那句）。這同時是回答 F7 靈魂題**最便宜的版本**：角色選擇終於有了畫得出來的後果。

---

### R5-F7 [MEDIUM] 靈魂方向題連三輪未答；且稿面 Notes 的輪次編號整組錯開一輪
違反禁例 **#18**（Notes 段落標輪次）。

- R2 尾段提出、R3 明令「**R4 二擇一：做出來，或用論證反駁**」。R4 handoff 寫「仍未採納也未反駁」，`yRI6y` 末段也寫「**留給 R4 二擇一**」——**這句話是寫在 R4 的 commit `28bec06` 裡的**。稿面自己說 R4 還沒來。
- 連帶：commit `1422de9`（R3）**沒有任何 Notes 區塊**；`28bec06`（R4）的區塊掛名「**LS-111 R3**」；`SAoBS` ⑤ 用「R3 本輪」指稱 R4 的改動。**拿 git log 對 Notes 區塊會得到錯的答案**，而這份文件存在的唯一理由就是當單一真相來源。

**改法**：`KQg10` 改為「LS-111 R4」、`yRI6y` 內文的「R3 本輪」「留給 R4」同步改；`SAoBS` ⑤ 的「R3 本輪」一併改。R3（`1422de9`）沒有專屬區塊這件事，在 R4 區塊開頭用一句話交代掉。

---

### R5-F8 [LOW] R4 在同一個 commit 裡違反了自己剛寫下的維護規則
`yRI6y` ⑧ 立規：「凡本輪的稿面改動使既有 Notes 段落描述失真，必須在同一輪改掉或明示標記；**沒有同輪寫回視為本輪未完成，不得落地**。」同一 commit：
- R4 的 gap 6→8 讓 `F5dSlm` 列高 **89→91**（實測 `F5dSlm` h=91、`eEP8A` 183＝91+1+91），而 `SAoBS` ① 仍寫「兩列現在等高 **89/89**」，**未標取代**。
- `SAoBS` ③ 的取代標記只涵蓋樣式（透明底／無描邊／pencil），同段「**07c(946)／07(946) body 高度一致**」未被涵蓋——實測 CHLio Body **990**／kcGJa Body **1024**，兩個數字都錯，而且已經不相等（I4 的刻意分歧）。

---

## Informational（不擋本輪，請在 handoff 逐條處置）
- **I1**：`E4DlKu`（CHLio）用 `$fs-meta` **13** 承載「正在產生邀請碼…」載入狀態——禁例 #11 點名的「載入」。緩解：同一句在主鈕以 17pt 重複，資訊未遺失；CHLio 早於本票。**建議記入 LS-96 待辦池**。
- **I2**：全稿 112 顆 icon **零使用** `$icon-sm/md/lg`；Tokens 板宣告的 AX3 icon 放大行為**沒有任何一張板示範**。禁例 #18 的全稿級案例，遠超本票 scope。**建議另記**（F2 只要求修 `uHUe1` 這一顆）。
- **I3**：`PXPcH` AX3 錯位 196pt，依 R3 裁決另票；本輪確認未動、未惡化。
- **I4**：`MonEa` 染料池措辭複驗仍合格（待裁決句在前、LS-96 可查、不逕改 `ppXvK`）。
- **I5**：z6DOE 把 Status Bar＋Nav Back 包在 `Chrome` 框內，kcGJa／CHLio 則是 Body 的兄弟。純結構差異、視覺無影響，但同家族兩種寫法會讓 ios-dev 猶豫。**不修亦可，請明說**。

## 值得保留（R6 不要倒洗澡水）
1. 沖印品母題實測精確：白邊 **8/8/8/8**、四顆角托各外擴**恰 5pt**、Sample Mount **272＝16+198+8+50** — **每個數字都有主人，零倒推餘數**。
2. **「範例」pill 疊到照片上**（Photo Wrap `absolute (8,8)`，壓在 `$photo-dim` 之上）——R4 最好的一個決定：把一個 UI 標籤變成印在相片上的戳記，總高代價實測為 **0**。
3. pencil／eye 語意 icon 與選中訊號分離（R3 的決定，R4 完整保住）。
4. R3-F3 的三板 44pt 章節斷點。
5. Sample Caption 回 24 基線、滿版 345。
6. 每板實心 `$accent` 恰 **1** 次；`$danger` 只用在破壞性文字鈕、不做主鍵；CHLio 用 `$disabled-fg` 是 **in-flight** disable（不是驗證型 disable，規則 8 ✓）。
7. Header Title `textGrowth:fixed-width` 三板補齊——**R4 建 AX3 板時主動抓到一個從未曝光的既有 clipping 並修掉，這是加分項**，正是壓力板存在的理由。
8. `Qyv1K` 本身：兩列點擊區各 278pt、Title 正確折兩行、0 非白名單 clipped。

## R6 優先序
1. **R5-F1** `HpB9V` 三句改回現況＋把同輪寫回規則的**範圍**改成全文件掃描（唯一與使用者裁決衝突的一條，請當主要交付）
2. **R5-F5**（零成本）＋ **R5-F8**＋**R5-F7** 編號（三項都是機械修正，可一批做完）
3. **R5-F2** check 隨 Dynamic Type ＋ `vL7kl` 275→253（**務必複驗預設態列高仍 91/91**）
4. **R5-F6** 「只能看」選中板
5. **R5-F3／F4** 釘底動作帶——**或用論證反駁**（見下方升級建議）

---

## 輪次規約與升級建議

本輪為**第 5 輪**，仍未達 APPROVE。依我 R3 的預告（「連續兩輪、第 5 輪結束仍未 APPROVE 將明示是否建議升級」），本輪明確表態：

**建議升級使用者裁決——但只升級兩個問題，不是整張票。**

理由：F1／F2／F5／F6／F7／F8 都是**具體、可執行、雙方沒有爭議**的缺陷（designer 沒有反對過，只是沒抓到），交給使用者裁決是浪費使用者的時間；這幾項照常在 R6 由 ui-designer 修。真正卡住的只有兩個問題，而且都超出「這張稿好不好」的範圍：

- **Q1（版式慣例，跨畫面）**：07 家族的主鈕要不要改成**釘在首屏底部的動作帶**？現況是 CTA 靠手調座標貼齊 818、餘裕 0，且把必讀條款擠到鈕後面的摺線外（F3＋F4）。釘底能一次解掉，但它會變成全 app 的版式慣例，不該由一張票單方決定。
- **Q2（品牌，解僵局的關鍵）**：**角色選擇列該不該帶沖印品母題**（R2 提的「疊兩張沖印品／選『只能看』時後面換空白沖印品」），還是使用者接受它就是一條安靜的 iOS 清單？
  我的立場要誠實講清楚：遮住 logo，我寫得出這份設計的三個記憶點只有兩個半——①壓著「範例」戳記、四角托、出血的沖印品；②60pt 等寬邀請碼坐在 `$print-paper` 上；③……角色列本身是一條標準 iOS inset grouped list，沒有第三個。而①②是 LS-17／LS-46 留下來的，**不是本票的產出**。
  但反面同樣站得住：這是「長輩看孫子照片」的 app，照片本來就該是主角、角色列本來就該讓位——**安靜是對的，只是安靜不等於通用**。這個取捨（本票的交付物允不允許沒有自己的記憶點）我不認為該由我或 designer 單方決定，四輪下來也證明我們自己收斂不了。
  **若使用者裁決「安靜的清單可以」**，我當場撤回靈魂面的要求，本票的通過標準降為機械正確性（F1／F2／F5／F6／F8），R6 一輪即可收斂。

請 orchestrator 把 Q1／Q2 連同雙方立場整理給使用者；F1／F2／F5／F6／F7／F8 不必等裁決，R6 可直接開工。

### 2026-08-31T03:24:09.283Z · Chih-Lin Yeh · `86a5ffd5`

**使用者裁決（2026-08-31，R5 升級兩題）**：
- **Q1：07 主 CTA 改「釘底動作帶」**（固定畫面底部、不隨捲動）——並確立為**全 app 版式慣例**：之後所有畫面的主動作照此設計。一次解 R5-F3（0 slack 脆弱）＋F4（必讀條款在摺線外／CTA 之後——條款須在閱讀順序上先於 CTA）。
- **Q2：角色選擇列要帶沖印品母題**——不接受「安靜的清單」，reviewer 靈魂面標準維持，設計繼續迭代（預期可能 R7+）。

R6 範圍＝R5 機械六項（F1/F2/F5/F6/F7/F8）＋Q1 結構改版＋Q2 母題設計。

### 2026-08-31T03:39:17.364Z · Chih-Lin Yeh · `5aa95719`

**設計對帳項（來源：LS-107 PR #177 R1 N1，2026-08-31）**：實作端實測——AX3＋寬字母六碼時，邀請碼經 `minimumScaleFactor` 縮放後有效字級約 55pt，**低於一般字級 60pt**；稿面 `$fs-code` 的 AX3 檔位 72pt 在 iPhone 寬度（354pt 內容寬）實際拿不到。07 稿面的 AX3 檔位定義需對帳：接受「AX3 寬碼時碼字級可低於 72（甚至低於 60）」並把實際 clamp 行為寫進 Handoff Notes，或改分組直排等版式讓 72pt 成立。R6/R7 輪處理（與釘底動作帶的新高度預算一起算）。實測數字與截圖見 LS-107 comment `4b6ee413`＋`.claude/evidence/LS-107/qa1fix/`。

### 2026-08-31T04:08:04.713Z · Chih-Lin Yeh · `d5f5c9e6`

## ui-designer R6 收工回報

**Ticket**：LS-111（07 邀請家人・角色選擇列），branch `feature/LS-111-invite-role-row`，worktree `.claude/worktrees/LS-111`
**Pen 路徑核對**：開工核對＝`/Users/clyeh/little-sprout/.claude/worktrees/LS-111/design/littlesprout.pen`（與 `git rev-parse --show-toplevel` 一致，`pen-open.sh --status` 通過）；收工已切回主 checkout `/Users/clyeh/little-sprout/design/littlesprout.pen`，exit 0。
**skill 載入**：`little-sprout-brand`＋`frontend-design` 兩個皆已載入（本輪起連續兩輪漏載記名後補齊）。

### skill 影響的取捨
- **little-sprout-brand**：Q2 的解法直接取自十條規則第 4 條（角托三段規則）——角色列判定為「零角托」類（設定列，非家人照片），因此只借「紙面基底＋白邊」語彙、明文拒絕角托，避免稀釋角托的稀缺性；第 2 條（紙不會變黑）決定了 print-paper 換色後文字必須跟著換成 print-ink／保留 text-primary 的分工（裸紙 vs 疊色）；F5 的 swap 依第 5 條間距節奏（8/16/24/44）鄰近關係。
- **frontend-design**：Q1（釘底動作帶）是本輪的「一個有理由的美學冒險」——沒有照抄任何既有型版，而是在 06 的 flex-spacer 慣例與 10 時間軸的 Tab Bar absolute+clip 慣例之外，為「內容隨狀態變動＋單一貫穿 CTA」這個新前提設計第三種解法（固定 Action Bar，CTA 距底部定值 50pt），並在 Notes／skill 兩處把適用條件寫清楚，避免變成下一個各板各行其是的孤例。

### 已完成（逐項對 Q1/Q2＋F1-F8）

- **Q1（釘底動作帶）**：全 7 板（z6DOE/BTBTs/kcGJa/CHLio/VRq1c/Qyv1K + 新板 K0hWL）一致落地。CTA 移入新增的 Action Bar（1pt hairline + Button Wrap，padding 16/24/16/24），原 Actions/Share Actions 改名 Terms/Secondary Actions，只留條款＋次要按鈕，排在角色列之後、CTA 之前（解 F4）。CTA 到裝置底緣固定 50pt（16+34），與內容量無關（解 F3 的 0 餘裕問題）。慣例定義（適用條件／安全區／與捲動內容關係／與 06 flex-spacer 的差異）已寫入 .pen Notes 新區塊＋`little-sprout-brand` skill `motifs.md`。
- **Q2（印品母題）**：cmp/Role Section 容器改用 $print-paper + $paper-edge 1pt + $paper-shadow；裸紙文字改 $print-ink／$print-ink-secondary，疊 $accent-soft 的選中列維持 $text-primary／$text-secondary（避免深色模式暗上暗）。刻意不加角托，理由與判準已寫入 skill。深色模式效果最明顯（暖米白紙面浮於深色 UI），淺色模式視覺幾乎不變（print-paper 淺值＝surface 淺值）。
- **F1**：`HpB9V` 三處全改（345×198／零染料池／刪除與 LS-89 牴觸的「OFF 態」句）；全文件 jftM0 掃描六個目標 frame id，未見其他矛盾。
- **F2**：`uHUe1` AX3 改 40×40（$icon-md），x 307→289 避免超版；`vL7kl` 275→253（253=345-16-22-16-22-16）解 22×22 結構重疊，AX3 實例另覆寫 235。複驗預設列高 91/91、AX3 列高 278/278 不變。
- **F5**：kcGJa/CHLio/VRq1c 三板 Role Status Wrap／Approval Section 頂距 44/24 對調，零成本（44+24=24+44）。
- **F6**：新板 K0hWL「只能看選中」，Sample Caption 換用 Notes 早已擬好的對應文案。
- **F7/F8**：KQg10/yRI6y「R3 本輪」→「R4」；SAoBS⑤ 同步；SAoBS①③補「已由 R4 取代」現況標記。
- **I5**：不修（Chrome 包框差異純結構、視覺輸出相同、ROI 低），已在 Notes 明說決定與理由。

### 已驗證（量測值）
- 7 板 root height 全部手動校正：z6DOE/BTBTs/K0hWL 995、Qyv1K 2259、kcGJa 1189、CHLio 1155、VRq1c 2770（各較 R5 基準 +25，公式：−CTA−8 +CTA+33 恆為 +25，與內容量無關）。
- CTA 到裝置底緣距離：16（Button Wrap 底 padding）+34（Home Indicator Area）=**50pt，7 板皆同**。
- 條款閱讀順序：z6DOE 內容區依序 Header→Sample Mount→Role Section→Terms（條款）→Action Bar（CTA）；kcGJa 等依序 Header→Code Card→Role Status→Approval→Secondary Actions→破壞性動作→Action Bar（CTA）。
- AX3 列高：Qyv1K 兩列各 278pt（F2 修正後未變）。
- 三支溢出掃描：7 板全數落在既有白名單（Sample Photo 出血 + 4 角托，interactive 三板 + 新板各 5 項；kcGJa/CHLio/VRq1c 0 項）。
- 節點總數：3593→3646（+53：F6 新板 29＋Q1 結構 21＋R6 Notes 區塊 3）。

### 未完成／未決事項
- 無新增未決事項；LS-81（角托 corner-out 5pt 對帳）、PXPcH AX3 錯位 196pt（I3）、pV9wX 全域 Stamp Rule 是否吸收本票維護規則——三項延續前幾輪已知未決，不在本輪處理範圍。

### 風險
- Action Bar 是全新版式慣例，僅在 07 家族實裝一次；下一張用到它的票需要驗證慣例本身是否經得起第二次套用（不同內容長度、不同 CTA 語意）才算真正定案。
- Pencil 的 root/內容區 `height` 屬性是硬寫數字、非 fit_content，已在 skill 記錄陷阱，但下一位設計者若忘記重算仍會被 `clip:true` 靜默吃掉內容——純工具限制，只能靠人工覆核。

### 產出位置
- HEAD `e8b804a`（已 push，push-gate 全通過：unit tests／api-contract／error-codes／migration 無撞號／12 commits）。
- 逐項 commit：f233051(F1)／34ac719(F7+F8)／e46c125(F5)／ad7a923(F2)／ed1bd45(F6)／50b9c82(Q1)／94039fe(Q2)／e8b804a(Notes+skill)。
- 截圖：`.claude/evidence/LS-111/r6/`（7 板 PNG）。
- Pen 路徑：`/Users/clyeh/little-sprout/.claude/worktrees/LS-111/design/littlesprout.pen`（收工已切回主 checkout）。

—— ui-designer（R6，待 orchestrator 派 R7 對抗審查）

### 2026-08-31T04:16:23.126Z · Chih-Lin Yeh · `f637dc73`

**設計對帳項補充（LS-107 PR #177 R2 fix 實測，2026-08-31）**：M1 修復後兩組字級已完全同步（單一 Text），但新數據更值得注意——極寬碼（WMZWGB）在**預設字級**下就已觸發縮放（實測 ~53.4pt），與 AX3 收斂到同一個由版面寬度決定的下限：**AX3 使用者對寬碼完全沒有放大效果**。稿面 `$fs-code` 60/72 兩檔在寬碼情境都拿不到。對帳時請把「碼字級下限由版面寬度決定、寬碼時 60/72 皆不保證」寫進 Notes，或改版式（直排／更窄字距）讓下限抬高。數據：LS-107 comment `4b6ee413` 後續＋`.claude/evidence/LS-107/qa1fix-r2/`。

### 2026-08-31T04:28:51.927Z · Chih-Lin Yeh · `78aebbe6`

## visual-reviewer 第 7 輪：**ITERATE**

- **little-sprout-brand skill：已載入**（含本分支新增的 `motifs.md` 兩節——「紙面基底可以借給非照片元件」「釘底動作帶 Action Bar」——依派工當**待驗證提案**審，結論見 B2／M1／I-d）
- **Pen 路徑核對**：active＝`/Users/clyeh/little-sprout/.claude/worktrees/LS-111/design/littlesprout.pen`（＝`git rev-parse --show-toplevel` + `/design/littlesprout.pen`）。開工時 active 為主 checkout，依 `pen-open.sh` 檔頭程序處理：先確認兩份開著的 .pen 皆安全——LS-111 `pen-land.sh --dry-run` 無未落地變更；主 checkout `git status` 乾淨且 `~/.pencil/backup` 找不到其對應 autosave（＝從未在 Pen 內編輯過）——再 `kill -TERM` Pen 主行程重開，`pen-open.sh --status` 對帳通過。
- HEAD `e8b804a`；**節點實測 3646**（與 R6 handoff 宣稱一致）。**本輪未改動稿面**（`git status` 乾淨）。
- 證據：`.claude/evidence/LS-111/r7-review/`（z6DOE／BTBTs／K0hWL／kcGJa／CHLio／Qyv1K／VRq1c／NRLL3 共 8 張 PNG）。逐字裁決即本 comment（單一真相來源，不另存 md）。

### 一句話

**Q1 的機械面做得乾淨——7 板結構逐字一致、50pt 不變式成立、R5 六項機械修全解，適用／不適用條款寫得比我要求的還負責。擋下來的是三件事：①「零染料池」這句話，是我 R5 判錯的——四張 Sample Print 各有 4 顆池、opacity 就是 `.432/.277/.342/.230`；R6 沒有複驗就照改，於是把一句正確的 Notes 換成一句錯的，同一失效點連第五輪，這次是修它的那一刀造成的。②Q2 在淺色模式視覺增量為零（designer 自承），在深色模式把紙卡切成一半暗一半亮——選中的那一列反而不像紙。③釘底帶自己吃掉 93pt 首屏，兩行必讀條款從「摺線下 8pt」變成「摺線下 33pt」：F4 的閱讀順序那一半解了，摺線那一半退步了。**

---

## 一、Q1 釘底動作帶：7 板逐板實測

| 板 | root h | Chrome | 內容區 | Action Bar | Home Ind. | 合計 | CTA→板底 |
|---|---|---|---|---|---|---|---|
| z6DOE 07a | 995 | 106 | 762 | **93** | 34 | 995 ✓ | **50** |
| BTBTs 07a深 | 995 | 106 | 762 | **93** | 34 | 995 ✓ | **50** |
| K0hWL 只能看選中 | 995 | 106 | 762 | **93** | 34 | 995 ✓ | **50** |
| kcGJa 07 | 1189 | 106 | 956 | **93** | 34 | 1189 ✓ | **50** |
| CHLio 07c | 1155 | 106 | 922 | **93** | 34 | 1155 ✓ | **50** |
| Qyv1K AX3-07a | 2259 | 138 | 1961 | **126** | 34 | 2259 ✓ | **50** |
| VRq1c AX3-07 | 2770 | 138 | 2472 | **126** | 34 | 2770 ✓ | **50** |

- **結構逐字一致**：7 板皆為 `Action Bar`(fill `$surface`, layout vertical, width fill_container) → `Bar Hairline`(rectangle h=1) + `Bar Button Wrap`(padding `["$sp-item","$screen-pad"]`)。**零例外、零魔術數**。
- **50pt 拆解實測**：預設 CTA 在 bar 內佔 17–77（wrap y=1 + pad 16，h=60），bar 93 → 下留 16；AX3 CTA 佔 17–110（h=93），wrap 125 → 下留 16。+ Home Indicator 34 ＝ **50，7 板皆同**。
- **條款閱讀順序**：z6DOE/BTBTs/K0hWL 內容區序＝Header → Sample Mount → Role Section Wrap → **Terms** → (Action Bar)；kcGJa/CHLio/VRq1c＝Header → Code Card → Role Status → Approval → Secondary Actions → 破壞性動作 → (Action Bar)。**條款／次要內容確實全部排在 CTA 之前** ✓
- **與稿面既有慣例的相容性**：我原本要擋「把釘底元件排進 flow 尾端不等於釘底」，實測後**撤回**——`oP9Ey`（A11y/10 時間軸 AX3，板高 1272）的 Tab Bar 就是 `layoutPosition:absolute` 釘在**畫布底**（y=1174＝1272−34−64），不是釘在 852。本文件對「溢出板上的釘底 chrome」既有慣例就是畫布底，R6 與之一致。**此點不成立，不列 finding。**
- **07b（NRLL3）**：實心 `$accent` **0 次**（兩筆核准同權重，符合 brand 規則），慣例適用條件（單一貫穿全狀態主 CTA）本來就不含它 → **不需要另票**。見 I-f。

## 二、R5 機械六項逐項複驗（`Get`／`ctx.bounds` 實測，未採信 handoff）

| 項 | 實測 | 裁決 |
|---|---|---|
| **F1** `HpB9V` | 「345×198」✓（`kOZbf` h=198）；「無核准開關＋LS-89 裁決」✓ 且句中寫了為什麼與票號 | **部分解——新增反向錯誤，見 B1** |
| **F2** check 隨字級 | `wAWsn/uHUe1` **40×40 @ x=289**（289+40=329＝345−16，貼齊右內距）；`vL7kl` AX3 覆寫 **235**、元件層 **253**；AX3 兩列各 **278**、預設 **91/91** | **✓ 解**（新增列內失衡，見 M2） |
| **F5** 間距 swap | kcGJa/CHLio/VRq1c：Role Status Wrap `["$sp-block",0,0,0]`、Approval Section `["$sp-section",0,0,0]`；三板 relY 331/445 一致，下游未動 | **✓ 解，零成本兌現** |
| **F6** 只能看選中板 | `K0hWL` 存在，第二列 check `$text-primary`、第一列 `#00000000`；caption＝「家人核准之後看到的就是這些，**他們只會看、不會加東西。**」 | **✓ 解** |
| **F7** Notes 輪次 | `KQg10`＝「LS-111 **R4** · …」；`yRI6y` 開頭補「R3／commit 1422de9 未留 Notes 區塊」；末段「留給 **R5** 二擇一（R5 已升級為裁決 Q2；R6 依裁決動工）」 | **✓ 解** |
| **F8** 過期數值標記 | `SAoBS` ①③⑤ 三處皆帶取代標記並保留原文 | **✓ 解** |

**溢出掃描**：z6DOE/BTBTs/K0hWL/Qyv1K 各 5 項（Sample Photo 出血＋4 角托），kcGJa/CHLio/VRq1c **0** → 共 20 項，**全數落在既有白名單**。
**節奏**：7 板皆有 ≥1 次 `$sp-section` 44（07a 家族 1／已產生家族 3）；Action Bar 引入的新數字只有 16／24／1，全在節奏上。
**accent 配額**：7 板實心 `$accent` **各 1 次**（CHLio 為 Button Working in-flight，非驗證型 disable）。

---

## 三、R7 findings

### B1 [BLOCKER] `HpB9V` 現在寫「零染料池」，而那張紙上有四顆池——而且是我 R5 判錯、R6 照改造成的
違反禁例 **#18（Notes 與稿互打）**＋**#7（假驗證斷言）**。**同一失效點連續第五輪**（R1 F14 → R2 F7 → R3-F1 → R5-F1 → 本輪），這一次是「修它的那一刀」製造的。

實測（掃全文件 fill 陣列中含 `$mount-pool` 的 radial gradient）：

| 節點 | 板 | 池數 | opacity |
|---|---|---|---|
| `kOZbf` | z6DOE 07a | **4** | .432 / .277 / .342 / .230 |
| `cPl1o` | BTBTs 07a深 | **4** | 同上 |
| `vc4x9` | Qyv1K AX3-07a | **4** | 同上 |
| `b1LeyW` | K0hWL 只能看選中 | **4** | 同上 |
| （kcGJa 全板） | 07 已產生 | **0** | — |

全稿共 **36 張 Print 帶池**（01/01b/01c/02/02b/04/05/05b/08/09/10 家族＋壓測＋Tokens 板皆在列）。

`HpB9V` 現行句：「（345×198，四顆角托，flat corner-fold 描邊、**零染料池**——冷色範例照片，池只在有真實邀請碼的 **04/kcGJa 家族**出現）」——**三處錯**：①這張紙有 4 顆池；②kcGJa 一顆都沒有；③範例照片是室內暖光（阿嬤親孫），不是冷色，而且池在紙層、與照片色溫無關。

**責任要講清楚**：R5 我的量法是去看四顆角托節點（`GEBcf` 實例，確實是 flat `$photo-corner`＋`$corner-fold` 邊線），就下了「零 gradient、零 pool」的結論——池不在角托上，池在**紙的 fill 陣列裡**（`motifs.md`：「池畫在紙層」）。**我量錯了，並且用一個錯的量測要求 designer 刪掉一句正確的話。** 同時 R6 也沒有在動手前自己量一次就照改——這是這條 finding 真正該學到的東西。

**改法**：
1. `HpB9V` 回復為現況並附量法：「…345×198，四顆角托（flat `$photo-corner`＋`$corner-fold` 1.5 邊線），紙層四顆染料池 opacity .432/.277/.342/.230（`kOZbf` fill 陣列第 2–5 項，`Get(id).fill` 可複驗；同值見 `cPl1o`／`vc4x9`／`b1LeyW`）」，並註明「R5 判定『零池』有誤、R6 據以刪除，R7 回復」。
2. 把這條寫進 `yRI6y` ⑧ 維護規則：**凡依 reviewer 的事實主張刪改稿面內容，落地前必須自己量一次並在 Notes 附上量法與節點路徑；「reviewer 說的」不是量測。** 這是這五輪唯一還沒被規則涵蓋的漏洞。
3. 順手複驗：全稿是否還有別的地方沿用了「07a 零池」這個錯誤描述（我掃過 `jftM0` 未見第二處，請你落地時再確認一次並把結果貼進 handoff）。

---

### B2 [BLOCKER] Q2 深色模式：把 theme-aware 的選中底色疊在不掛 theme 的紙上，紙卡被切成一半暗一半亮
違反 brand 十條 **#2（紙不會變黑）**的精神＋禁例 **#4** 的變形。證據：`.claude/evidence/LS-111/r7-review/BTBTs.png`。

實測堆疊：`n6JtDU` fill `$print-paper`（深＝#E8D9D4，**不掛 theme**）→ 選中列 `X7DYDo` fill `$accent-soft`（深＝#3A2028，**掛 theme**）。

| 模式 | 選中列 fill vs 紙面 | 效果 |
|---|---|---|
| 淺 | #F6DDE2 on #FBEBEC ＝ **1.11:1** | 幾乎不存在（R5-F2 已認定） |
| 深 | #3A2028 on #E8D9D4 ＝ **10.80:1**（我用 WCAG 2.1 相對亮度算） | 近黑色塊 |

**同一個狀態、同一個元件，fill 通道在兩個模式差 10 倍。** 深色截圖上的實際結果比數字更糟：選中的「一般成員」那一列因為與頁面底色 `$bg`(#190E12) 太接近，**整列讀起來不是紙、是背景**；卡片剩下「只能看」那一列是暖米白紙 —— **看起來被選中的是沒被選中的那一列**，卡片輪廓也斷了（上方兩個圓角掛在一塊看不見的底色上）。check icon 是唯一的補救，而它只有 22pt。

`motifs.md` 新節那條 carve-out（「疊了 theme-aware 底色的子區塊要維持 `$text-primary`」）**不是規則，是在補救一個自己造成的問題**：把會反轉的底色疊到不會反轉的紙上。它解了文字對比（12.02:1 ✓），但沒解 fill 的極性。

**改法（二選一，請在 Notes 寫明選哪個與為什麼）**：
- **A（保守，成本最低）**：選中列 fill 改成**不掛 theme 的單值**（新增 `$print-select`，兩模式同＝#F6DDE2），選中列文字／icon 回 `$print-ink`／`$print-ink-secondary`。整張卡在兩個模式**完全一致**，`motifs.md` 那條 carve-out 可以整段刪掉，「紙不會變黑」變成無例外的規則。代價：兩模式 fill 通道都只剩 ~1.1:1，選中靠 check＋字重 700/600（R3 四通道裡真正有效的那兩條）——與現況淺色一樣，不變差。
- **B（建議，同時解 H1）**：**讓「紙」本身成為選中訊號**。選中列＝一張 `$print-paper` 紙（`$paper-edge` 1pt＋`$paper-shadow`），未選中列不是紙（透明躺在 `$bg` 上），整組不再是一張大卡。極性兩模式一致（選中＝比較亮的那張紙），淺色靠 1.22:1＋亮邊＋落影浮起（＝全稿卡片既有的浮起量），深色是紙浮在暗底上。這才是「沖印品母題真的進到角色列」有畫面的版本，而且它把 R5-F2 抱怨的「fill 通道實質不存在」一起解掉。**風險**：深色下 #E8D9D4 對 #190E12 很亮，可能搶上方照片——落地後請先出深色截圖自審，若搶戲就把紙面縮到只包住選中列的左側標記帶。

---

### H1 [HIGH] Q2 在淺色模式的視覺增量為零——使用者「不接受安靜的清單」的裁決未達成
`$print-paper` 淺色值＝`$surface` 淺色值（#FBEBEC，tokens.md ① 明列），所以淺色下這次改動的全部可見增量＝1pt `$paper-edge`(#FFF7F8) 描邊＋一道 (0,2)/blur6 的 `$paper-shadow`。R6 handoff 自承「淺色模式下與舊版視覺幾乎一致」。對照 `z6DOE.png`：那仍是一張標準 iOS inset grouped card。

**遮住 logo，我現在寫得出的記憶點還是 2.5 個**：①壓著「範例」戳記、四角托、出血的沖印品；②60pt 等寬邀請碼坐在 `$print-paper` 上；③——角色列仍然沒有。而且①②都是 LS-17／LS-46 的遺產，**不是本票的產出**。這與 R5 的盤點一字不差。

**為什麼借了紙卻沒有畫面**：這份文件裡讓一張紙讀起來像沖印品的東西是——**8pt 白邊、壓印行的小字、角托、染料池**。Q2 只借了紙的 **fill**，而它在淺色下等於 `$surface` ＝ 借了一個看不見的東西。角托不借是對的（判準寫得好，見「值得保留」），但**剩下的三件裡至少要借到一件有幾何的**。

**改法**：走 B2 的**改法 B**（紙成為選中訊號＝借「這是一張可以拿起來的紙」這個幾何事實），一次解 B2＋H1。若你堅持走改法 A，那就必須另外回答：**這條角色列自己的記憶點是什麼**——用一句話寫在 Notes 裡，讓下一輪的我可以拿它來驗收。

---

### H2 [HIGH] 釘底帶自己吃掉 93pt 首屏，兩行必讀條款從「摺線下 8pt」退到「摺線下 33pt」，首屏 0% 可見
違反 brand 規則 **6（「有效幾天／還可以用幾次」一律 ≥17 且要讀得到）**。

首屏內容視窗（07a 家族）＝852 − Chrome 106 − Action Bar **93** − Home Indicator 34 ＝ **619**（body 座標）。實測 body 內容：

| 段 | body relY | 首屏（619）內？ |
|---|---|---|
| Header | 24–129 | ✓ |
| Sample Mount | 129–401 | ✓ |
| Role Section Wrap | 401–**608** | ✓（剛好整張進） |
| Terms（44 頂距） | 608–738 | **摺線落在 44pt 頂距裡** |
| `c5MJu`「產生後 7 天內有效，最多 5 位家人可以用。」 | **652–678** | ✗（摺線下 **33pt**） |
| `FCdOF`「新成員一律要你核准才能加入，這項設定無法關閉。」 | 686–738 | ✗ |

對照 R5：當時摺線 818、`c5MJu` 826–852（摺線下 **8pt**）。**釘底帶把摺線往上推了 93，條款只往上走了 68 → 淨退步 25pt，而且從「探出一點點」變成「完全看不見」。**

使用者 Q1 裁決的括號原文把「**必讀條款在摺線外**／CTA 之後」並列為要解的問題；R6 解了「CTA 之後」，「摺線外」變差了。**這是裁決前提的算術沒有成立**，我必須如實回報，不是重開已決的議題。

**改法（三選一，請明說選哪個）**：
- **(a)** 把 `c5MJu`（單行 26pt）搬進 Action Bar、排在 CTA **之上**（`$fs-note` 17／`$text-secondary`，與 CTA 間距 `$sp-label` 8）→ bar 93→**127**、首屏視窗 **585**；body 少 34（26+8）。此時角色卡底緣 608 > 585，需再收 23pt 才能讓角色卡完整進首屏（例：Sample Print 198→175，或 Header 副標壓成一行）。這也正是慣例文字自己半承諾過的位置（「原本與 CTA 同框的次要文案（到期日／必讀條款）」）。
- **(b)** 維持現狀但**用論證反駁**：例如主張 Header 副標「你核准就能一起看」已在首屏講了核准這件事，而「7 天／5 位」屬於按下去之後才需要知道。我接受有量測支撐的反駁——但若走這條，**必須在 Notes 明記這是對使用者裁決前提的部分不採納**，讓使用者看得到，不要靜默。
- **(c)** 折衷：`FCdOF` 與 Header 副標語意重疊，壓成一行或刪；只留 `c5MJu` 並用 (a) 搬進 bar。

---

### M1 [MEDIUM] Action Bar 的 hairline 用 `$control-line`，全稿其他 18 條 1pt 線全部用 `$border`——而且已經寫進 skill 會擴散
違反 tokens.md ① 的 token 角色定義（`border`＝純裝飾髮絲線與分組線；`control-line`＝**有意義的邊界**：輸入框、外框鈕、焦點框）。

全文件 1pt 橫線掃描（寬 >100）：`$border` **18 條**（04/04-iPad/06d×4/06-kbd/07b/09×3/10b/AX3×3/`cmp/Role Section` 的 `MACIr`），`$text-primary` 2 條（連結底線，正確），**`$control-line` 7 條——全部是本輪新增的 `Bar Hairline`**。

諷刺點：**同一輪的 Q2 才把 `MACIr` 從 `control-line` 改成 `$border`**，理由正是它是分組線。同一個判準在同一個 commit 批次裡用了一次、又反過來違反一次。視覺上也看得出來（`kcGJa.png`）：那條線與旁邊「複製邀請碼」外框鈕同色同權重，畫面上最重的一條線是一條裝飾分隔線。

**改法**：7 個 `Bar Hairline` 的 fill 改 `$border`；**同步改 `motifs.md`「釘底動作帶」那句的「1pt `$control-line` 頂部 hairline」**——它現在是全 app 慣例文字，不改會複製到每一張新畫面。

---

### M2 [MEDIUM] AX3 下同一列裡 check 40pt、語意 icon 22pt——F2 只長大一顆，造成列內失衡
證據：`j7TGh` 截圖。`uHUe1` AX3 40×40 ✓（F2 已解），但同列的 `j6ZNWo`（pencil／eye，R3 定義為「角色語意」通道）仍硬寫 22×22，旁邊是 40pt 的文字。那顆語意 icon 在 AX3 下小到像雜點。

硬約束 #7 沒有被違反（兩顆 icon 都有文字標籤相伴，不是唯一載體），所以不是 BLOCKER；但它是 F2 這一刀**製造出來**的列內不一致，而且修法很便宜。

**改法**：`j6ZNWo` 改 `$icon-md`（預設 22／AX3 40，與 check 同 token）；`vL7kl` 的 AX3 覆寫由 235 → **217**（345−16−40−16−40−16，公式與現行 235 同構）。**複驗**：AX3 兩列列高仍 **278/278**（desc 在 217 寬下仍為 3 行）、預設列高仍 **91/91**、CTA 不動。若複驗發現列高變動就退回、在 Notes 記錄理由。

---

## Informational（不擋本輪，R8 handoff 請逐條處置：已修／記入 LS-96／另票／不修＋理由）

- **I-a｜LS-107 設計對帳項（comment `5aa95719`）R6 未處理**：稿面 `$fs-code` AX3 檔位 72 未動（`VRq1c` Code Row 300／內寬 305），Notes 全文零字提及。依派工不擋本輪，但**必須在 R8 handoff 明白處置**。附一個對處置有用的觀察：稿面那個「300 ≤ 305 所以 72 放得下」是用 **Courier Prime 替身字**量的，實作用系統等寬字，前進寬不同——這正是禁例 #7 的形狀。處置時請把「用哪一種字型量的」寫進 Notes，否則下一輪還會再對帳一次。
- **I-b｜`E4DlKu`（CHLio）「正在產生邀請碼…」仍 `$fs-meta` 13**（R5-I1）：未動、未惡化，同句在主鈕以 17pt 重複，資訊未遺失。**建議記入 LS-96**。
- **I-c｜`PXPcH` AX3 錯位 196pt**（R5-I3）：本輪確認未動、未惡化，維持另票。
- **I-d｜`motifs.md` 新節措辭**：寫「借紙面與**白邊**」，但角色卡實際上**沒有 8pt 白邊**（只有 1pt `$paper-edge`＋落影）。請收斂成「借紙面基底與紙緣亮邊／落影」，避免下一位設計者以為要加 `$print-edge` 8。
- **I-e｜新慣例的波及評估（本票不需要開 retrofit 票）**：我掃了全部 393 寬、非 A11y/Stress 的螢幕板——**27 張有實心 accent，其中 20 張其他板全部剛好 852、內容不溢出**，只有 07 家族 5 張溢出。所以現在沒有任何一張別的畫面需要改版。**建議**：把 `motifs.md` 那節標成「**暫定：n=1，待第二次套用後定案**」，並在下一張「單一 CTA＋內容隨狀態變動」的票（最可能是 08 幫寶貝建檔／09b 加入照片挑選態時）掛上「本慣例第二次套用」的驗收條件——比現在開一張全 app 改版票務實得多。這也回應 R6 自己列的風險。
- **I-f｜07b（NRLL3）**：實心 accent **0 次**（兩筆核准同權重），慣例適用條件不含它，**不需要另票**。但實測它的核准／婉拒鍵在 body **678–1304**、首屏（852−106−34＝712）外——既有問題、與本慣例無關，**建議記入 LS-96**。

---

## 值得保留（R8 不要倒洗澡水）

1. **Action Bar 7 板逐字一致**：`Bar Hairline`(1pt) + `Bar Button Wrap`(`["$sp-item","$screen-pad"]`)，CTA→板底 **50pt 七板皆同**（預設 16+34、AX3 16+34），高度公式 106+762+93+34=995 等五組全部閉合。**零倒推餘數、零魔術數。**
2. **慣例文字裡的「不適用」條款**：明說 06 flex-spacer 家族不改用本慣例、兩套解法各有前提——這是這份文件裡我看過最負責任的一段慣例寫法，請原樣保留（只改 M1 那個 token 字）。
3. **F1 的修法本身**：`HpB9V` 不只改數字，還把「為什麼改」與 LS-89 票號寫進句子；`SAoBS` ①③⑤ 的取代標記保留原文供沿革。這個慣例比文件既有的做法更完整。
4. **F5 零成本 swap** 真的零成本：三板 relY 331/445 一致、下游未動。
5. **K0hWL**：第二列選中＋caption 換成「他們只會看、不會加東西」——本票核心交付終於畫全，而且 caption 與角色選擇的因果關係是這張稿上唯一真的在說話的地方。**這是 R6 最好的一個交付。**
6. **F2 的 x=289**：289+40=329＝345−16，貼齊右內距——公式看得出來。
7. **角托不借給角色列的判準**（「你會再點開看一次的收藏 vs 設定一次就忘記的清單」）：這句判準本身是對的、可執行、值得留在 `motifs.md`。B2 走改法 B 也不牴觸它（借紙不借角托）。
8. 溢出 20 項全白名單；每板實心 accent 恰 1；CHLio 是 in-flight 而非驗證型 disable；節點 3646 與 handoff 宣稱一致。

---

## R8 最短收斂路徑（優先序）

1. **B1** 染料池 Notes 反向錯誤（含把「依 reviewer 主張刪改稿面前必須自己量一次」寫進 `yRI6y` ⑧）——**主要交付**
2. **B2** 深色紙卡（改法 A 或 B；**選 B 同時解 H1**）
3. **H1**（若選改法 A，另需一句話寫出角色列自己的記憶點，供 R8 驗收）
4. **H2** 首屏條款（(a)/(b)/(c) 三選一；選 (b) 必須寫進 Notes 讓使用者看得到）
5. **M1** hairline token（7 個節點＋`motifs.md` 同步）＋ **M2** AX3 icon 對稱（複驗 278/278、91/91）
6. Informational I-a～I-f 逐條處置

## 輪次規約

本輪為**第 7 輪**（≥3 輪門檻早已滿足），仍未達 APPROVE：我寫不出三個記憶點（2.5，且沒有一個是本票產出），禁例 #18／#7／#4 各中一槍。

**本輪不再升級使用者裁決。** R5 升級的兩題使用者已裁決，R6 也照裁決動工了；本輪擋下來的六項**全部是可量測、有具體改法、雙方沒有立場衝突的執行缺陷**（其中 B1 有一半責任在我 R5 的錯誤量測），不是收斂不了的爭點，把它們送去佔用使用者時間是浪費。

**升級條件先講明**：若 R8 之後 **B2＋H1（Q2 的品牌面）仍不成立**，我會在 R8 verdict 明示第二次升級，把「**角色列的沖印品母題只在深色模式成立、淺色維持安靜清單，可不可以接受**」這一題交給使用者定奪——因為那時候爭的就不再是執行，而是這張票的交付物允不允許沒有自己的記憶點。B1／H2／M1／M2 不論如何都照常在 R8 修，不必等任何裁決。


### 2026-08-31T05:02:16.730Z · Chih-Lin Yeh · `e04b0ad0`

## ui-designer R8 收工回報

**Ticket**：LS-111（07 邀請家人・角色選擇列），branch `feature/LS-111-invite-role-row`，worktree `.claude/worktrees/LS-111`
**Pen 路徑核對**：開工核對＝`/Users/clyeh/little-sprout/.claude/worktrees/LS-111/design/littlesprout.pen`（與 `git rev-parse --show-toplevel` 一致，R7 收工狀態沿用，`pen-open.sh` 未被要求重切）；收工已切回主 checkout `/Users/clyeh/little-sprout/design/littlesprout.pen`，`pen-open.sh` exit 0。
**skill 載入**：`little-sprout-brand`＋`frontend-design` 兩個皆已載入。

### skill 影響的取捨
- **little-sprout-brand**：B2/H1 的解法（每一列各自可能是一張紙）直接取自十條規則第 2 條「紙不會變黑」——選中列是真正的紙才用不掛 theme 的 print-ink，未選中列不是紙就必須用 theme-aware 的 text-primary/text-secondary；這條規則同時是我在實作過程中抓到自己一個新引入 bug（未選中列深色模式下 print-ink on $bg-dark 僅 1.1:1）的判準來源。角托三段規則（第 4 條）確認本輪不需要幫角色列加角托（設定列不是收藏品）。H2 的必讀條款位置取捨對照第 6 條「有效幾天／還可以用幾次一律 ≥17 且要讀得到」——這條規則直接排除了 verdict 選項 (b)（反駁保留現狀），因為那等於主張使用者不需要在按鈕前看到這句話，與規則字面衝突。
- **frontend-design**：整輪最大的取捨是 B2+H1 沒有選「改個顏色講究對比就過關」的保守解（verdict 選項 A），而是選了改法 B——讓紙本身變成選中訊號（一張從清單裡浮起的沖印品）。這是把「structure is information」原則落在這張票上：選中不再靠顏色宣告，靠一個真實的物理狀態（紙浮起/沒浮起）宣告，這正是 reviewer 三輪來一直在問的「角色列自己的記憶點」。

### 本輪修了什麼（逐項，5 個 commit，每個都 pen-land 後才 commit）

1. **B1【commit 508540d，主要交付，流程修復】**：R5 誤判「07a 範例沖印品零染料池」、R6 未複驗照改，把 `HpB9V` 一句正確的 Notes 換成錯的——同一失效點連五輪。R8 用 `Get(id).fill` 實測 `kOZbf`/`cPl1o`/`vc4x9`/`b1LeyW` 四節點，確認各有 4 顆 `$mount-pool` gradient（opacity .432/.277/.342/.230），改回正確描述並記錄沿革；同時指出「池只在 kcGJa 家族出現」這句本身也錯（kcGJa 的邀請碼卡 `zfEIM` fill 僅 `$surface`，是全稿唯一沒有池的 Print 類節點）。`yRI6y` ⑧-1 新增維護規則：**依 reviewer verdict 的事實主張刪改稿面前，必須自己 Get 複驗，不能只憑 reviewer 陳述改稿**——這是這張票的結構性止血規則。
2. **M1【commit 0b711a5】**：7 個 `Bar Hairline` fill 由 `$control-line` 改 `$border`，與全稿其他 18 條 1pt 分隔線一致；`motifs.md` 同步改字避免慣例繼續複製錯 token。
3. **B2+H1【commit 8f99730，靈魂面交付】**：改法 B——Role Options 容器不再是一張共用大卡，每一列各自可能是一張紙：選中列＝獨立 `$print-paper`＋`$paper-edge` 1pt＋`$paper-shadow`＋`$radius-lg`（紙上墨 print-ink，不掛 theme）；未選中列＝透明躺在 `$bg` 上（改回 theme-aware 的 text-primary/text-secondary——這是我在驗證截圖時抓到的衍生問題：若未選中列沿用 print-ink，深色模式下對比只剩 1.1:1，因為它根本不是紙，不該套用「紙不會變黑」）。K0hWL 覆寫同步反轉；MACIr 分隔線刪除，列間改 gap 8。四板（z6DOE/BTBTs/Qyv1K/K0hWL）root/Body 高度 +7。複驗：列高 91/91、278/278 不變，三支溢出掃描 20 項全落既有白名單零新增，深色截圖確認選中列是一張浮起的暖米白紙、未選中列文字清楚可讀。
4. **H2【commit 4b7fcbf】**：採 verdict 處置 (c)——把「產生後 7 天內有效，最多 5 位家人可以用。」搬進 Action Bar、疊在 CTA 上方（`$fs-note`/`$text-secondary`，距 CTA `$sp-label` 8），首屏可見率 0%→100%；「新成員一律要你核准」一句與 Header 副標語意重疊，刪除。Terms 容器清空後刪除。Action Bar 高度自動撐開 93→127（AX3 126→314）。**殘餘揭露（未採用反駁式處置，如實記錄）**：首屏視窗因 Action Bar 增高而縮到 585，Role Section Wrap 現底緣 615，超出摺線 30pt——僅「只能看」（非預設選中）列的描述文字尾段落在摺線下，icon/標題/勾選狀態仍在首屏內；核心必讀條款已達成「與 CTA 同框恆常可見」，未強行壓縮 Sample Print 345×198（多處引用的沖印品母題錨定值）或改寫 Header 副標來消除這 30pt——是否要進一步壓縮版面消除殘餘，留待人核可。
5. **M2【commit 9387cc3】**：Qyv1K AX3 覆寫新增 `j6ZNWo`（pencil/eye）40×40，與同列 check 同尺寸；`vL7kl` AX3 寬度 235→217。Pencil `height` 丟棄 `$variable` 的已知限制對 icon width/height 同樣成立（實測驗證），沿用既有解法在實例層級寫死數字。複驗列高 278/278、91/91 不變。
6. **Informational 處置＋補完整輪次 Notes【commit ab87c28】**：見下方逐條回覆。

### 逐條回覆 R7 informational（reviewer 明文要求）

- **I-a（LS-107 `$fs-code` 對帳，comment `5aa95719`／`f637dc73`）**：已處置——寫入 `motifs.md` 邀請碼節③-1，承認「稿面用 Courier Prime 替身字量出 300≤305 放得下，實作用系統等寬字＋`minimumScaleFactor`，AX3 對寬碼完全沒有放大效果，下限由卡片內寬 305pt 決定」，不強行改版式（會牽動 07/07b 既有沖印品版式），如需更高下限需另開一輪版式設計。
- **I-b（`E4DlKu` 13pt 載入文案）**：維持建議記入 LS-96，本輪未動、未惡化。
- **I-c（`PXPcH` AX3 錯位 196pt）**：維持另票，本輪未動、未惡化。
- **I-d（`motifs.md`「白邊」誤稱）**：已修——隨 B2+H1 一併改寫該節，改為「紙面基底與紙緣亮邊／落影」，不再誤導成 8pt 白邊。
- **I-e（Action Bar 慣例波及評估）**：已修——`motifs.md` 該節標題加註「暫定：n=1，待第二次套用後定案」，下一張套用本慣例的票需把「第二次套用」列進驗收條件。
- **I-f（07b 核准鍵在首屏外）**：維持建議記入 LS-96，本輪未觸及 07b（不在 R7 優先序內）。

### Pen-land 結果（5 次，皆 exit 0）
每次落地前幾乎都先遇到一次「backup 落後幾秒」的擋下（autosave 延遲），依固定規則用「插入再刪除一個暫存節點」觸發 autosave 後 20 秒內重跑即通過，未使用 `cp` 繞過、未使用 `--allow-unchanged`。節點總數變化軌跡：3646（R7 基線）→B1 落地 3646（純文字，無節點增減）→M1 落地 3646（純屬性）→B2+H1 落地 3645（刪 MACIr −1）→H2 落地 3637（刪 8 個 Terms/Approval Note 節點）→M2 落地 3637（純屬性）→Notes 區塊落地 3640（新增 stamp+content+wrapper 共 3 個節點）。

### 已驗證
- 全 7 板 Export 存 `.claude/evidence/LS-111/r8/`（z6DOE/BTBTs/kcGJa/CHLio/VRq1c/Qyv1K/K0hWL 共 7 張 PNG）；另有多次 TakeScreenshot 逐段核對（Role Section 淺/深模式、K0hWL 選中態、AX3 icon 對稱）。
- 三支溢出掃描：四板（z6DOE/BTBTs/Qyv1K/K0hWL）各 5 項既有白名單（Sample Photo 出血＋4 角托），kcGJa/CHLio/VRq1c 未觸及、維持 0 項——零新增。
- 對比複驗（WCAG 2.1）：選中列 print-ink on print-paper 沿用 skill 已記錄的 14.92/12.55；未選中列修正後 text-primary on bg 沿用 skill 已記錄的 12.21/15.31（取代了我一度誤植的 print-ink on bg-dark ≈1.1:1 這個未落地的中間狀態）。
- kcGJa/CHLio/VRq1c（read-only Role Status 變體）截圖確認未受本輪任何改動影響。

### 未完成／未決事項
- **H2 殘餘 30pt**：見上方逐項④，需人核可是否要進一步壓縮版面（例如縮小 Sample Print 或改寫 Header 副標）來讓角色列完整回到首屏內，或接受目前「核心必讀條款已解決、僅次要選項的描述文字尾段需一次小捲動」的狀態。
- **I-b／I-c／I-f** 三項待記入 LS-96／另票（本輪未直接寫入 Linear LS-96，依既有慣例僅在 handoff 陳述處置，沿用 R4/R6 慣例）。
- Action Bar 慣例仍是 n=1（本輪未新增第二個套用場景），已在 skill 標「暫定」。

### 風險
- H2 的 30pt 殘餘若使用者認為不可接受，需要另一輪處理，可能牽動 Sample Print 或 Header 副標這兩個目前被多處引用的錨定值，屬於中等範圍的改動。
- B2+H1 的紙面選中機制是全新視覺語彙，目前只在角色列套用過一次；若未來有其他「可選清單」畫面想借用，需要驗證機制本身能否無縫延伸（例如三個以上選項、更長文案）。

### 產出位置
- 5 個 commit（HEAD `ab87c28`，已 push，push-gate 全通過：unit tests／api-contract／error-codes／migration 無撞號／18 commits）：
  - `508540d` B1
  - `0b711a5` M1
  - `8f99730` B2+H1
  - `4b7fcbf` H2
  - `9387cc3` M2
  - `ab87c28` Informational + Notes 區塊
- 證據：`.claude/evidence/LS-111/r8/`（7 張 PNG）。
- Pen 路徑：`/Users/clyeh/little-sprout/.claude/worktrees/LS-111/design/littlesprout.pen`（收工已切回主 checkout `/Users/clyeh/little-sprout/design/littlesprout.pen`，exit 0）。

—— ui-designer（R8，待 orchestrator 派 R9 對抗審查）

### 2026-08-31T05:18:34.902Z · Chih-Lin Yeh · `5cce1b53`

## 第 9 輪：ITERATE（機械項二則，靈魂面已通過）

**little-sprout-brand skill**：已載入（`.claude/skills/little-sprout-brand/`，含 tokens／motifs／slop-forbidden／elder-constraints）。本分支 motifs.md 更新照約定當待驗證提案處理。

**Pen 路徑**：`get_app_state` 回報 active＝`/Users/clyeh/little-sprout/design/littlesprout.pen`（主 checkout，**非**本票 worktree）。`pen-open.sh` 切檔失敗（Pen 主行程對 osascript quit 與 SIGTERM 皆無回應，腳本依規約拒絕 SIGKILL）。改以 `execute` 的 `filePath` 參數直接指向 worktree 檔複驗——**本輪實測 `filePath` 有效**（LS-111 檔 3640 節點 vs 主 checkout 3501，可鑑別），全部審查與量測均在 `/Users/clyeh/little-sprout/.claude/worktrees/LS-111/design/littlesprout.pen`（＝ `git rev-parse --show-toplevel`/design/littlesprout.pen，head `ab87c28`，工作區 clean、與 Pen backup 位元組相同）。詳見末段 harness 情資。

---

## 靈魂：這份設計現在有靈魂了

**Q2 已解，我撤回 R7 預告的二次使用者裁決。** B2+H1 的紙片解是第三次回答裡第一次真的成立的：選中列不再是「疊一層底色」，而是**一張從清單裡被拿起來的沖印紙**——獨立 `$print-paper`、1pt `$paper-edge` 亮邊、`$paper-shadow` (0,2) blur 6 落影、`$radius-lg`，紙上的字用不掛 theme 的 `$print-ink`。深色模式下它從近黑台紙上浮起來，實測 **13.72:1**。這是全 app 第一次讓「紙不會變黑」這條 token 規則去**承載互動狀態**而不只是裝飾照片——規則自己執行自己，跟染料池 `lit()` 同一種美德。

我自己寫得出的三個記憶點（不是設計者宣稱）：

1. **選中＝一張被拿起來的紙**；未選中＝完全透明、直接躺在台紙上的裸字。列間無分隔線，靠紙自己的邊界分界。任何 SaaS radio list 做不出這個。
2. **同一個容器裡兩種墨**：紙上 `$print-ink`（不掛 theme），沒有紙的地方 `$text-primary`（掛 theme）。判準是「這一塊此刻是不是一張真的紙」，且**全稿掃描 themed-text-on-print-paper ＝ 0** 可複驗。
3. **範例沖印品先於選擇**：問你要給什麼權限之前，先給你看一張祖母親吻嬰兒的真照片（345×198、四角托、四顆 `lit()` 算出的池），Caption 把選擇的後果接回照片；選「只能看」時整句改寫。權限被講成「家人會看到什麼」，不是勾一個等級。

靈魂測試 **pass**：首屏是人和照片，UI 讓位。

**但兩個機械項不成立，本輪仍 ITERATE。兩項都很短。**

---

## F1（必修，新回歸）07a 全家族失去 44 章節斷點

`z6DOE` / `BTBTs` / `K0hWL` / `Qyv1K` 四板 **`$sp-section` 出現次數＝0**（掃 gap 與 padding 皆無，含陣列形式）。違反 brand 十條第 5 條「每畫面必須出現一次 44 章節斷點」；07a 不在「碼輸入家族 7 張具名豁免」名單內。

成因是 R8 自己的 H2 修補：`BH3N9` ⑤ 明載 07a 的 44 掛在「Actions 頂距 `$sp-block` 24 改回 `$sp-section` 44，讓 07a 恢復『範例｜設定｜動作』三章文法」，而 `oGvFN` ④「Terms 容器清空後一併刪除」把它一起帶走了——**且 Notes 未揭露這個副作用**。

現況 Body 節奏：Header →0→ Sample Mount →0→ Role Section Wrap，內部只有 24/16/8/24。從上到下沒有任何段落級斷點＝通用 slop #2（間距是節奏，沒有 44 就沒有章節）。對照組 `kcGJa`／`CHLio`／`VRq1c` 三板各有 3 個 44，語彙不一致。

**改法**：`Role Section Wrap` 頂距由 24 改 `$sp-section`（44），恢復「照片是一章／設定是另一章」的斷點。四板同步，root/Body 高度各 +20（906→926、2042→2062）。這是最省的一刀——不動 Sample Print 345×198，不動 Action Bar。

（全稿順帶對帳：45 張 393 寬螢幕板中 29 張有 44。無 44 的 16 張裡，9 張是 06／03 碼輸入家族及其變體〔具名豁免〕，4 張是本票的 07a 家族〔本 finding〕，其餘 3 張 `04 首次進入`／`Stress-05`／`Stress-10` 為既有、不在 LS-111 scope——列為 informational，建議記入 LS-96。）

## F2（必修）`BH3N9` 整塊過期未標記——正是 ⑧ 規則本身

`BH3N9`（R6 區塊）**全文不含任何 "R8" 字樣**，但 R8 已把它描述的東西整個推翻：

- ② 整段仍寫「未選中列（裸紙面）`j6ZNWo`/`vL7kl`/`zWpl1` 元件基底 fill 改 `$print-ink`／`$print-ink-secondary`」「選中列疊 `$accent-soft`」——**這正是 R8 修掉的深色 1.1:1 bug**。ios-dev 照這段做會把剛修好的缺陷原樣裝回去。
- ① 「Bar Hairline（1pt `$control-line` 頂部分隔線）」——R8 已改 `$border`（M1）。
- ① 「原 Actions／Share Actions 改名 Terms／Secondary Actions…留在內容區、排在角色列之後、CTA 之前」——Terms 已刪除（我複驗 `FCdOF`／`MACIr` 皆不存在、07 家族查無 Terms／Actions 節點）。
- ① 高度預算「z6DOE/BTBTs/K0hWL 995；Qyv1K 2259」——實測 **906／2042**。

這是同一失效點的**第六次**重演（R1 F14 → R2 F7 → R3-F1 → R5/R6〔⑧-1〕→ 本輪）。諷刺處在於：R8 在同一支 commit 裡新增了 ⑧-1 止血規則，卻違反了它的母規則 ⑧（「凡本輪稿面改動使既有 Notes 段落描述失真，必須同輪改掉或明示標記『已由 R<n> 取代』，沒有同輪寫回視為本輪未完成，不得落地」）。slop-forbidden #18。

**改法**：`BH3N9` ①②⑤ 三處逐段加「【已由 R8 取代，現況見 `oGvFN`】」，②「未選中列…改 print-ink」那一句因為會誘發已修 bug，要**直接改寫**而不只是標記。

## F3（併 F2 一起做）把淺色的實測數字寫進稿

我逐像素量了 export（非 token 推算）：

| | 選中紙 vs 台紙 | 落影帶最強處 |
|---|---|---|
| 淺色 `z6DOE` | **1.21:1** | 1.40:1 |
| 深色 `BTBTs` | **13.72:1** | 14.03:1 |

紙面訊號在深色比淺色強約 11 倍。**這不是缺陷、我不要求拉高**——`$print-paper` 淺色值 `#FBEBEC` 與 `$surface` 同值、已是系統最亮，台紙 `#EDD3D6` 是中淺粉，兩者之間拿不到 3:1 而不動全域 token；而且「紙躺在紙色台紙上很低調、關燈後才發光」在物理上是對的。**選中態也從不依賴它**：check icon `$print-ink` on `$print-paper` ＝ 14.92:1，加字重 700/600 與語意 icon，WCAG 1.4.11 由勾勾滿足。

但稿面**目前沒有任何地方記錄這個不對稱**——唯一相關的句子是 `BH3N9` ② 那句已過期的「淺色模式下與舊版視覺幾乎一致…幾乎看不見」。依 slop-forbidden #7（宣稱必附量測法與數字），請把上表與「為什麼刻意不拉高」寫進 `oGvFN` ③。

## H2 殘餘 30pt：我判定**可接受**，不送使用者裁決

核心訴求已達成，逐項複驗：`c5MJu`「產生後 7 天內有效，最多 5 位家人可以用。」在 Action Bar 內、`$sp-label` 8 疊在 CTA 之上、閱讀順序先於 CTA、**首屏可見 100%**；50pt 不變式（Button Wrap padding-bottom 16 ＋ Home Indicator 34）成立。我用真 852 視窗合成首屏（`z6DOE-firstscreen-852.png`，紅線＝摺線）覆核無誤。

30pt 殘餘落在「只能看」（非預設選中）列的說明句尾段；該列的 icon、標題、勾選狀態全在首屏內。半截字列是 iOS 正規的「下面還有」暗示，且被截的是非預設選項的描述而非必讀條款。**這項不再是使用者裁決題。**

唯一想要的打磨（不擋核可）：被截的那行字**貼著不透明 Action Bar 的 1pt 硬邊界**，中間 0 間隙，讀起來比較像「字被切壞了」而不是「可以往下捲」。建議 Action Bar 頂緣加一道極短的 `$bg`→透明漸隱（8–12pt），或內容區底部補 8pt padding 讓切點離硬邊界遠一點。

---

## 逐項驗證（實測數字）

| 項 | 判定 | 實測 |
|---|---|---|
| **B1** 染料池 Notes | ✅ | 我自己 `Get(id).fill` 複驗：`kOZbf`/`cPl1o`/`vc4x9`/`b1LeyW` 各 4 顆 `$mount-pool`，opacity **.432/.277/.342/.230**，base `$print-paper`——與 `HpB9V` 現文逐位相符。`zFEIM`→`zfEIM` Code Paper fill 僅 `$surface`，確為全稿唯一無池的 Print 類節點。沿革（R5 誤判→R6 未複驗照改→R7 發現→R8 回復）已記入。 |
| **B1** ⑧-1 維護規則 | ✅ | `yRI6y` ⑧-1 存在，措辭可執行：「凡依 reviewer verdict 的事實主張刪改稿面內容，落地前必須自己用 `Get(id).fill`／`Get(id,{depth})` 等量測一次，並在 Notes 附上量法與節點路徑；『reviewer 說的』本身不是量測」。**我本輪照此規則自行複驗了 B1，未採信 Notes 陳述。** |
| **M1** hairline | ✅ | 7/7 `Bar Hairline`（`pvuLp`/`Rg5Ry`/`g1NI2P`/`bLf7Q`/`T0llPv`/`H0aUo`/`X3s3Rv`）fill＝`$border`、h＝1；全稿 `$control-line` 當裝飾髮絲線者 **0**。motifs.md 同步。 |
| **M2** AX3 語意 icon | ✅ | `Qyv1K` 兩列：Role Icon 與 Role Check 皆 **40×40 @ y=16**，右緣 289+40＝**329**＝345−16 齊邊；Title w＝**217**＝345−16−40−16−40−16。預設板 22×22 @ x307→329、Title 253，同一公式。Icon 落在標題帶（0–58），Desc 自 66 起，無交集。 |
| **B2** 墨/主題紀律 | ✅ | 全稿掃描：themed text on `$print-paper` ＝ **0**；print-ink 節點 24 個全部坐在真紙上（gradient 底層逐一驗為 `["$print-paper", …mount-pool]`，零偽陽性）。`K0hWL` 交換板兩列覆寫對稱正確（選中 print-ink/700、未選中 text-primary/600）。designer 自抓的 bug 修得徹底。 |
| **溢出** | ✅ | 07 家族 partially clipped **20**（4 張照片出血＋16 顆角托），全落既有白名單、零新增；vertical 容器兄弟重疊 **0**。 |
| **I-a** fs-code 對帳 | ✅ | 拖三輪的項已落地：motifs.md 邀請碼 ③-1 寫入 LS-107 實測（寬碼在預設字級即縮到 53–55pt，AX3 與預設收斂到同一個由卡片內寬 305pt 決定的下限，**AX3 對寬碼無放大效果**），並明說是版面寬度的物理下限、不強行改版式。承認現況而非改稿掩蓋——處置正確。 |
| **三份文件一致性** | ⚠️ | motifs.md ↔ 稿面：一致（紙片解、gap 8 無分隔線、`$border` hairline、50pt 不變式、Action Bar「暫定 n=1」註記皆核對無誤；「只有 07 家族 5 張」在排除 A11y 板後正確）。稿面 ↔ .pen Notes：**F2 不一致**。 |

## Informational 處置追認
I-b（`E4DlKu` 13pt 載入文案）、I-c（`PXPcH` AX3 錯位 196pt）、I-f（07b 核准鍵在首屏外）——維持記入 LS-96／另票，本輪未惡化，同意。I-d（motifs.md「白邊」誤稱）已修正為「紙面基底與紙緣亮邊／落影」，正確。I-e（Action Bar 慣例 n=1）已加註。**新增 informational**：`04 首次進入`／`Stress-05`／`Stress-10` 三板無 44 斷點（既有、非本票 scope），建議記入 LS-96。

## 收斂判斷
第 9 輪。靈魂面（Q2）本輪**首次成立並通過**，我撤回 R7 的二次升級預告——**不需要使用者裁決**。剩下 F1／F2／F3 全是機械項且路徑極短（一個 padding token ＋ 一輪 Notes 標記回寫 ＋ 貼上兩個已量好的數字），不構成升級理由。R10 若這三項落地且無新回歸，我預期 APPROVE。

**重做優先序**：① F1 補 44（動 4 板 1 個 padding，含高度重算）→ ② F2 `BH3N9` 標記＋改寫 ②（防 ios-dev 裝回舊 bug，最高風險項）→ ③ F3 補淺/深對比數字 → ④ H2 摺線打磨（選配，不擋核可）。

## 值得保留、重做時不要動
選中紙片的四件套（`$print-paper`＋`$paper-edge` 1pt＋`$paper-shadow` (0,2)/6＋`$radius-lg`）與「兩種墨」判準；列間 gap 8 取代分隔線；`K0hWL` 交換板（本票核心交付，證明兩種選中態都畫得出來）；Sample Print 345×198 與它的四顆池；Caption 綁定選中角色的文案設計；Action Bar 的 50pt 不變式與條款位置；M2 的 40/22 齊邊公式。

---

### harness 情資（給 orchestrator，非設計項）
1. **Pencil MCP `execute` 的 `filePath` 參數現在有效**——LS-81／LS-91 結論「filePath 無效、唯一切檔手段是 `open -a Pen`」已過期。實證：同一組 `Get` 對兩個路徑分別回 3640／3501 節點。`pen-open.sh` 檔頭與 COLLABORATION §4-b 的相關敘述建議複驗後更新；審查／唯讀量測可直接用 `filePath` 指定，不必搶 Pen 的 active 文件（也避開了跨 worktree 搶檔互卡）。
2. **`pen-open.sh` 本輪 fail closed 兩次**：(a) 主 checkout 的 Pen backup 檔不存在（`7f92cc6b…`），`check_root_safe` 把「查無 backup」當成「無法確認安全」而拒絕清場——但那其實是「從未編輯過」的安全訊號，判準可再細分；(b) Pen 主行程對 `osascript quit` 與 `SIGTERM` 皆無反應，腳本依規約不送 SIGKILL，於是無路可走。建議記入 LS-96。
3. 取證：`/Users/clyeh/little-sprout/.claude/worktrees/LS-111/.claude/evidence/LS-111/r9-review/`（8 檔，`.gitignore:21` 已忽略，工作區 clean、未 git add）。

### 2026-08-31T05:38:52.530Z · Chih-Lin Yeh · `c492cc1f`

## ui-designer R10 收工回報

**Ticket**：LS-111（07 邀請家人・角色選擇列），branch `feature/LS-111-invite-role-row`，worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-111`。

**Skill 載入**：`little-sprout-brand`＋`frontend-design` 皆已載入。本輪純機械修正，取捨影響：little-sprout-brand 十條第 5 條（44 章節斷點，F1 直接依它判定違規並修）與第 9 條（漸層只有兩道——H2 打磨時因此**放棄** reviewer 建議的漸層方案，改選 padding 方案，見下）左右了本輪決策；frontend-design 本輪未觸發新的美學取捨（純修復既有設計的過期文件與回歸，非新畫面）。

**Pen 路徑**：開工核對 `get_app_state` 回報 active＝`/Users/clyeh/little-sprout/.claude/worktrees/LS-111/design/littlesprout.pen`，＝ `$(git rev-parse --show-toplevel)/design/littlesprout.pen`（本 worktree），核對通過、未重切。收工前已切回主 checkout（`pen-open.sh` exit 0，見下）。

---

## 逐項回覆 R9 verdict（comment `5cce1b53`）

### F1（必修）——已修，已落地
`Role Section Wrap` 頂距 `$sp-block`(24)→`$sp-section`(44)，`z6DOE`(s0AH3g)/`BTBTs`(fl6ib)/`K0hWL`(XbEv1)/`Qyv1K`(j7TGh) 四板同步；root/Body 高度各 +20：三板 906→926、2042→2062；Body 639/1556→659/1576。複驗：`$sp-section` 出現次數由 0→4（wrapPad 讀回皆為 `["$sp-section",0,0,0]`），溢出掃描仍是既有白名單 20 項（4 板×5：Sample Photo + 4 角托）、零新增。截圖覆核版面無破版。
commit `3db1db9`。

### F2（必修，最高風險）——已修，已落地
`BH3N9`（R6 Notes）①②⑤ 三段加「本節已由 R8 取代，現況見 `oGvFN`」標記；①並列出三處具體過期點（Bar Hairline 現為 `$border`／Terms 容器已刪除／高度預算數字過期，R10 後現況 z6DOE/BTBTs/K0hWL root=926、Qyv1K root=2062）。②的危險句直接改寫：原文「未選中列改 print-ink／選中列疊 accent-soft」與 R8 現況方向相反（R8 修的正是這條深色 1.1:1 bug），改寫為「未選中列 `$text-primary`/`$text-secondary`（theme-aware，躺在 `$bg` 上）；選中列才是獨立 `$print-paper` 紙用 `$print-ink`/`$print-ink-secondary`」，並保留一句說明原提案為何危險（避免未來輪次再次被舊文字誤導）。⑤ 加同款標記指向 `oGvFN` ⑤（M2 AX3 icon 覆寫現況）。複驗：`Get(BH3N9).content` 已不含危險舊句、含修正句；四處標記皆讀回為 true。
commit `fd7b0d6`。

### F3（併 F2 做）——已修，已落地
`oGvFN` ③ 補入 R9 逐像素實測：選中紙 vs 台紙——淺色 `z6DOE` **1.21:1**（落影帶 1.40:1）／深色 `BTBTs` **13.72:1**（落影帶 14.03:1）；並寫入「刻意不拉高淺色值」的理由（`$print-paper` 淺色與 `$surface` 同值已是系統最亮、台紙中淺粉、兩者結構上拿不到 3:1 而不動全域 token；選中態不依賴此對比，check icon 14.92:1＋字重 700/600 語意冗餘已滿足 WCAG 1.4.11）。複驗：`Get(oGvFN).content` 含 "R9 補充實測"、"1.21:1"、"13.72:1"、"14.92:1"，全 true。
與 F2 同 commit `fd7b0d6`。

### H2 打磨（選配，不擋核可）——已做，已落地
`Role Section Wrap` 末端各加 8pt 透明 `Fold Buffer` spacer（4 板），root/Body 高度各再 +8（z6DOE/BTBTs/K0hWL 926→934、Qyv1K 2062→2070）。**選用 reviewer 提供的兩個選項中「內容區補 padding」，不用「漸層」方案**——理由：漸層方案會讓全稿漸層數從 2（窗光＋染料池）變 3，直接牴觸 brand 十條第 9 條「漸層只有兩道，各有物理出處，說不出出處的不畫」；一道純為捲動視覺過渡的 scrim 說不出這兩個既有出處，即使功能上合理也是新增第三道，risk 高於 40pt 這種次要打磨值得冒的程度。padding 方案零風險、零新規則牴觸。
commit `ab4ed89`。

### Informational 追認
I-b／I-c／I-f／新增的三板無 44 斷點（`04 首次進入`／`Stress-05`／`Stress-10`，既有、非本票 scope）——依 R9 判斷維持記入 LS-96 待辦池，本輪未處理（不在本輪授權範圍內，另票或待辦池優先序見 COLLABORATION §5-b）。

---

## 落地與驗證
三輪 `pen-land.sh`（F1／F2+F3／H2）均 **exit 0**：
- F1：`--expect-nodes 3640 --after <t1>`，首次因 autosave 未追上被擋（backup 落後 6 秒），照規約 nudge（無害屬性覆寫觸發新 autosave）後重跑，通過。
- F2+F3：`--expect-nodes 3640 --after <t2>`，同法 nudge 後一次通過。
- H2：`--expect-nodes 3644 --after <t3>`（本輪新增 4 個 Fold Buffer spacer 節點），nudge 後一次通過，結構 diff 印出新增節點與各高度屬性變更，無 meta 變動。

三個 commit 皆通過 commit-gate（design-landing gate＋structure check）。`git push` 通過 push-gate（單元測試＋api-contract＋error-codes＋migration 版本號＋分支票號＋可乾淨合併），已推上 `origin/feature/LS-111-invite-role-row`（HEAD `ab4ed89`）。

**收工程序**：`pen-open.sh "$(git rev-parse --show-toplevel)"` 已切回主 checkout，**exit 0**（`✓ pen-open：Pen 目前文件＝/Users/clyeh/little-sprout/design/littlesprout.pen`）。

## 未決事項
無。R9 verdict 明確表示三項機械修完、無新回歸即預期 APPROVE，且已撤回二次使用者裁決（靈魂面 Q2 已通過）。請 orchestrator 派 R11 複驗；若 APPROVE 可直接送使用者核可，不需再等使用者裁決。

## 風險
F1 的 +20 使 Role Section Wrap 底緣往下推，H2 的 30pt 摺線殘餘因此變成約 50pt（585 視窗 vs 現況約 635 累計底緣）——**判斷仍在可接受範圍**：R9 已確認被截內容僅為「只能看」（非預設）列的描述文字尾段，非必讀條款（必讀條款 `c5MJu` 常駐 Action Bar、首屏可見 100% 不受影響），且本輪 H2 打磨已為此加了緩衝。若 R11 認為這個新數字需要重新裁決，我可以进一步收斂（例如把 F1 的 44 斷點只加在非 Qyv1K 板、或另尋更省版面的三章文法寫法），但目前判斷不需要，因為 R9 verdict 本身已經是基於「30pt 不擋核可」的結構性理由（必讀內容不受影響），不是基於精確像素數字。

### 2026-08-31T05:57:48.932Z · Chih-Lin Yeh · `197a7e20`

## visual-reviewer 第 11 輪：**ITERATE**（單一 finding，最短路徑；三項 substantive landings 全數通過，僅剩本輪 H2 自身引入的一處過期高度值）

**little-sprout-brand skill**：已載入（tokens／motifs／slop-forbidden／elder-constraints 全讀）。
**Pen 路徑核對**：`get_app_state` active＝`/Users/clyeh/little-sprout/.claude/worktrees/LS-111/design/littlesprout.pen`＝`$(git rev-parse --show-toplevel)/design/littlesprout.pen`（本 worktree，head `ab4ed89`）。**通過，未重切、未用 filePath 繞道**——本輪全部量測都在正確的 worktree active 文件上。

---

## 為什麼是 ITERATE 而不是 APPROVE（先講結論）

R9 靈魂面已通過、R10 三項機械（F1/F2/F3）＋H2 我逐項複驗**實質上都對了**。設計在 canvas 上是正確、乾淨、有靈魂的。**唯一未達標的，是本票派工第 2 條的硬判準**：「F2… 高度過期值全部校正到實測。任一句還過期＝ITERATE。」

本輪 H2 的 +8 把 root 從 926/2062 改成 **934/2070**，卻沒有回頭更新引用它的兩處「現況」Notes——這是連六輪失效點（slop-forbidden #18）的**第七次重演，且由本輪自己的 H2 commit（`ab4ed89`）造成**：F2 commit（`fd7b0d6`）才剛寫下「R10 後現況 926/2062」，同輪的 H2 commit 就把它變假、沒同步。母規則 ⑧（「本輪改動使既有 Notes 失真必須同輪改掉」）在被指派來終結 ⑧ 違規的這一輪裡，又被違反了一次。

這不是吹毛求疵：其中一個過期數字（摺線殘餘 30pt）正是要在核可時交給使用者的**取捨數字**，實際已是 ~50pt——記錄少報 20pt。若這輪放行，等於讓「唯一真相來源自稱不可信」的 Notes 過關，把 gate 掏空。**修法只有兩個數字對、一句殘餘重算，路徑極短。**

---

## 逐項驗證（全部實測，Get／Export 複驗，非沿用 Notes）

### F1（必修）——✅ 通過
| 板 | 名稱 | root 實測 | Role Section Wrap padding | `$sp-section` 次數 |
|---|---|---|---|---|
| `z6DOE`(s0AH3g) | 空狀態 | **934** | `["$sp-section",0,0,0]` | 1 |
| `BTBTs`(fl6ib) | 深色 | **934** | `["$sp-section",0,0,0]` | 1 |
| `K0hWL`(XbEv1) | 只能看選中 | **934** | `["$sp-section",0,0,0]` | 1 |
| `Qyv1K`(j7TGh) | AX3 | **2070** | `["$sp-section",0,0,0]` | 1 |

- 頂距四板同步＝`$sp-section`(44)、每板出現 1 次（≥1）✓。高度 934＝906+20(F1)+8(H2)、2070＝2042+20+8，算式閉合 ✓。
- **溢出掃描**：4 板 Export 逐一目視，除既有白名單（照片出血＋角托壓緣）零新增破版；F1/H2 只動 Body 內縱向、root 同步長高，無橫向溢出 ✓。
- **CTA 50pt 不變式未破**：4 板 Bar Button Wrap padding＝`["$sp-item"(16),"$screen-pad"(24)]`、Home Indicator＝34 → CTA 底緣到畫面底 16+34＝50，未動 ✓。
- **designer 自報 F1 風險（+20 把「只能看」列切點推到摺線下 ~50pt）——結構判準確認成立**：4 板必讀條款節點（`c5MJu`/`s5DA9`/`qJZlI`/`u4xy5k`，文字皆「產生後 7 天內有效，最多 5 位家人可以用。」`$fs-note` 17）**全部是 Action Bar 的子節點**（釘底、不隨 Body 長度浮動），首屏 100% 可見與 Body 高度無關。被摺線截掉的只有非預設「只能看」列的描述尾段，icon/標題/勾選狀態仍在首屏。**位移可接受**——與 R9 同一結構論證，不受 30→50 數字變化影響。

### F2（必修，最高風險）——✅ 危險句已終結，但高度值未校正（見 F-A）
- **②危險句：已直接改寫，做對了**。`BH3N9` ② 現在（a）頭部加標記「本節已由 R8 取代，現況見 `oGvFN` ③」；（b）明載正確現況「未選中列直接躺在 `$bg` 上、F5dSlm 用 `$text-primary`/`$text-secondary`（theme-aware）；選中列才是獨立 `$print-paper` 紙用 `$print-ink`/`$print-ink-secondary`」；（c）保留一句把 R6 原提案（未選中 print-ink／選中疊 accent-soft）標為**錯的**、指出深色 1.1:1 成因、「不要依本段原文實作」。ios-dev 照現稿不會把已修 bug 裝回去 ✓。canvas 端我也複驗（BTBTs 截圖：選中＝暖米白紙、未選中＝theme-aware 淺字，與 R9 B2 掃描一致）。
- **①⑤標記：完整指向 `oGvFN`** ✓。① 標記逐點列出三處過期（Bar Hairline 現為 `$border`／Terms 容器已刪／高度數字過期）；⑤ 標記指向 `oGvFN` ⑤。
- **`$control-line`（Bar Hairline）**：① 標記明說「現為 `$border`（非 `$control-line`）」，正文舊字被標記涵蓋 ✓（R9 改法只要求標記）。
- **Terms**：① 標記＋`oGvFN` ④「Terms 容器清空後一併刪除」皆正確 ✓。
- **⚠️ 高度過期值：未校正到實測 → F-A（唯一未達標項）**。

### F3（併 F2）——✅ 通過
`oGvFN` ③ 已含 R9 逐像素實測全套：淺色 `z6DOE` **1.21:1**（落影帶 1.40:1）／深色 `BTBTs` **13.72:1**（落影帶 14.03:1）／check icon `$print-ink` on `$print-paper` **14.92:1**；並完整寫入「刻意不拉高淺色值」的理由（print-paper 淺色＝surface 已系統最亮、台紙中淺粉、結構上拿不到 3:1 而不動全域 token；選中態不依賴此對比，字重 700/600＋語意 icon 冗餘編碼已滿足 WCAG 1.4.11）✓。

### H2（選配）——機械已做，但引入 F-A
- Fold Buffer 8pt 透明 spacer 4 板皆在 Role Section Wrap 末端（`tN6DH`/`X4EuA`/`K0FKHz`/`ffd1e`，h＝8）✓。
- **選 padding 方案不選漸層——正確**：漸層方案會讓全稿漸層數 2→3，牴觸 brand 十條第 9 條，designer 放棄它是對的判斷 ✓。
- **副作用未收尾**：+8 把 root 改成 934/2070、把摺線殘餘從 30pt 推到 ~50pt，兩者都沒回寫 Notes → F-A。

---

## F-A（唯一 finding，必修）`BH3N9` ①／`oGvFN` ④ 高度值仍過期——正是 slop #18 第七次

現稿真實高度（我 Get 實測）：**z6DOE/BTBTs/K0hWL root＝934、Qyv1K root＝2070、Body 667（AX3 以 Get 確認，約 1584）**。但 Notes 兩處「現況」都是舊值，且全稿 Notes **無一處**記載正確的 934/2070：

1. **`BH3N9` ①** 標記寫「R10 後現況 z6DOE/BTBTs/K0hWL root=**926**、Qyv1K root=**2062**」——自稱「R10 後現況」卻是 H2 前的值，差 8。這是本輪 F2 commit 新寫、H2 commit 打臉的**新過期值**。
2. **`oGvFN` ④** 寫「四板 root **906/2042**」（差 28）＋「首屏視窗… 由 619 降到 585，Role Section Wrap 現底緣 615，超出摺線 **30pt**」——這些是 R8-era 值；F1(+20)+H2(+8) 後底緣再下移 28、殘餘實為 ~50pt。而 `BH3N9` ① 把讀者導向 `oGvFN` 當「現況」來源，來源自己卻是舊的。

**改法（二選一，路徑都極短）**：
- (A) 直接改兩處數字：`BH3N9` ① 的 926→**934**、2062→**2070**；`oGvFN` ④ 的 906/2042→**934/2070**、殘餘 30pt→**~50pt**（並把「585／615」重算或改為敘述性「摺線殘餘約 50pt，僅『只能看』非預設列描述尾段落在摺線下，必讀條款在 Action Bar 內首屏 100% 可見」）。
- (B)（更穩，建議）補一行 R10 現況戳記當**單一真相來源**：「R10 後現況（含 F1 +20、H2 +8）：root 934/2070、Body 667/~1584、摺線殘餘約 50pt（同上敘述）」，並把 `BH3N9` ①／`oGvFN` ④ 的舊「現況」數字改指向此行。
- 落地前用 `Get(id).height` 自行複驗（yRI6y ⑧-1 規則），不要再沿用任何 handoff 裡的數字。

---

## 值得保留、重做時絕不要動（防倒洗澡水）
- 選中紙片四件套（`$print-paper`＋`$paper-edge` 1pt＋`$paper-shadow`(0,2)/6＋`$radius-lg`）與「兩種墨」判準——canvas 正確、BTBTs 深色下最驚豔。
- `BH3N9` ② 的危險句改寫（含「R6 原提案是錯的、勿實作」那句）——**這是最高風險項，已做對，別動它**。
- `oGvFN` ③ 的 F3 對比全套數字與「不拉高」理由。
- 列間 gap 8 取代分隔線、`K0hWL` 交換板（Caption 隨角色改寫「只會看、不會加東西」）、Sample Print 345×198 四顆池、Action Bar 50pt 不變式與條款疊 CTA 上（gap $sp-label 8）、M2 40/22 齊邊。
- F1 的 4 板 `$sp-section` 頂距、H2 的 4 顆 Fold Buffer——結構都對，只差把高度數字寫對。

## 給 ui-designer 的重做優先序
只有一件事：**F-A**——把 934/2070／殘餘 ~50pt 校正/戳記到 `BH3N9` ①＋`oGvFN` ④（建議用改法 B 立單一真相來源）。其餘一律不動。這是最後一個數字。

---

## Informational 處置
`04 首次進入`／`Stress-05`／`Stress-10` 三板無 44 斷點（既有、非本票 scope）——維持記入 LS-96，同意 R10 判斷、本輪未惡化。I-b/I-c/I-f 同維持待辦池/另票。

## 若 F-A 修完 → 下一輪預期 APPROVE，屆時交使用者的取捨摘要（先備妥）
1. **摺線殘餘 ~50pt**：非預設「只能看」列的描述尾段落在 852 首屏摺線下；必讀條款（7天/5位）在釘底 Action Bar 內、首屏 100% 可見；icon/標題/勾選在首屏。iOS 正規「下面還有」暗示。若使用者要求進一步壓縮，designer 已列選項（44 只加非 AX3 板／另尋三章文法），但結構論證不需要。
2. **淺色選中紙 vs 台紙對比 1.21:1**：刻意不拉高（print-paper 淺色＝surface 已系統最亮，結構上拿不到 3:1 而不動全域 token）；選中態不依賴此對比，check icon 14.92:1＋字重 700/600＋語意 icon 已滿足 WCAG 1.4.11；深色下同一機制強 11 倍（13.72:1），關燈才發光。

## 產出位置
取證 4 板 Export PNG：`/Users/clyeh/little-sprout/.claude/worktrees/LS-111/.claude/evidence/LS-111/r11-review/`（`z6DOE-empty.png/`、`BTBTs-dark.png/`、`K0hWL-viewonly.png/`、`Qyv1K-ax3.png/` 各含 `<id>.png`；`.gitignore:21` 已忽略、未 git add）。只審未改稿。

### 2026-08-31T06:17:04.442Z · Chih-Lin Yeh · `14834eaf`

## visual-reviewer 第 13 輪：**APPROVE**（收尾輪；F-A 修正落地、過期副本歸零、canvas 未受擾動）

**little-sprout-brand skill**：已載入（tokens／motifs／slop-forbidden／elder-constraints／wordmark 全讀）。
**Pen 路徑**：`get_app_state` active＝主 checkout `/Users/clyeh/little-sprout/design/littlesprout.pen`（非本 worktree），故**全程未動 active 文件、未跑 pen-open**；依派工用 `execute` 的 `filePath` 指向 `/Users/clyeh/little-sprout/.claude/worktrees/LS-111/design/littlesprout.pen` 做所有 Get/Export。**鑑別力已證**：filePath 檔節點數 **3644**（主 checkout 3501），head `937853a`。

---

## 這份設計的靈魂（reviewer 自寫的三個記憶點，canvas 本輪重新目視複驗仍成立）
1. **選中角色列＝一張從清單裡浮起的暖米白沖印紙**（$print-paper＋$paper-edge 1pt＋$paper-shadow＋$radius-lg），深色模式下發光、未選列維持 theme-aware 可讀——「紙不會變黑」規則延伸到照片之外的 UI 元件（BTBTs 截圖最驚豔）。
2. **沖印品母題帶入整個畫面**：範例照片四角托＋白邊＋「範例」壓印 pill 印在 $photo-dim 之上（同「印在照片上」語彙），照片是主角、UI 讓位——長輩看孫子照片的溫度在。
3. **釘底 Action Bar**：必讀條款（7天/5位）疊在 CTA 之上、與 CTA 永遠同框、首屏 100% 可見——「不因驗證未通過而 disable、不把必讀資訊藏在摺線下」在版面留下的痕跡。

---

## 逐項驗證（全部自行 Get/Export 實測，非沿用 Notes）

### F-A（R11 唯一 finding，本輪唯一審查範圍）——✅ 全數落地
**1) `oGvFN` ④ 單一真相來源戳記存在且＝實測**：
| 板 | root 實測 | Body 實測 | 戳記載值 | 一致 |
|---|---|---|---|---|
| z6DOE 空狀態 | **934** | 667 | 934 / 667 | ✓ |
| BTBTs 深色 | **934** | 667 | 934 / 667 | ✓ |
| K0hWL 只能看選中 | **934** | — | 934 | ✓ |
| Qyv1K AX3 | **2070** | 1584 | 2070 / 1584 | ✓ |

root 算式閉合：z6DOE Chrome 106＋Body 667＋Action Bar 127＋Home Ind 34＝934；Qyv1K 138＋1584＋314＋34＝2070（四板直接 children bounds 逐段量測確認）。

**2) 摺線殘餘 58pt——自行複驗＝58pt（與戳記一致）**：Role Section Wrap 於 Body 座標系 y=401、h=242、底緣 643；首屏可見窗＝852−Chrome 106−Action Bar 127−Home Ind 34＝585；殘餘＝643−585＝**58pt**。戳記正確以逐節點量測的 58pt 取代 R11 verdict 的「~50pt」約值，並在同一行註明二者關係——這正是 R11 派工要求「若與 58 不同要指出」的反向落實：**我量到的就是 58，戳記對**。受影響範圍描述亦正確（僅「只能看」非預設列描述尾段落在摺線下；icon／標題／勾選狀態＋必讀條款仍首屏 100% 可見）。

**3) `BH3N9` ① 不再重抄數字、改為指向**：① 現況已刪去 R11 抓到的「R10 後現況 926/2062」那句，改寫為「高度預算數字已過期，現況數字不在此重複抄錄——見 oGvFN ④文末【R10 後現況戳記】單一真相來源，複驗一律以 Get 為準，不要沿用下方 995/2259」。指向正確、且明令不得在本處另抄一份 ✓。

**4) 全稿 Notes 過期現況副本歸零（連七輪失效點確認終結）**：對**整份文件所有 text 節點**（非僅三塊 Notes）掃描 `926/2062/906/2042/995/2259/934/2070/667/1584/58pt/30pt/50pt/619/585/643`——
- **`926`／`2062` 全文件 0 命中**：七輪的失效值已徹底移除 ✓。
- 全稿唯一的「現況」root 高度宣稱只有 `oGvFN` ④ 戳記一處（934/2070）。
- 其餘數字複本全部帶顯式歷史標記：`906/2042`「（R8 當時值，非現況——現況見…戳記）」、`995/2259`「不要沿用下方 995/2259」、`30pt/619`「R8 當時／降到」——皆非現況宣稱，是有標記的沿革記錄，符合 ⑧-2 規則的「引用處寫『見戳記』、不自抄」。
- `fK91A`/`waK4D` 的 `50pt` 命中實為角托染料池橢圓 `668×1150pt` 的字尾，與角色列高度無關，非殘留。

### `yRI6y` ⑧-2 規則——✅ 措辭可執行、命中根因
「任何會被多處 Notes 引用的度量值，只准有一個單一真相來源行（本票起以 `oGvFN`④ 戳記為準），其餘引用處一律寫『見 XX 戳記』而不得自行抄錄數字」——根因分析正確（「根因不是『忘記改』，是『有超過一處要改』」），規則機械可檢、前瞻可執行。這是七輪來第一次針對**根因**（複本存在）而非症狀（某處忘改）立規，是本票最有價值的一條過程資產。

### 回歸抽驗（R9/R11 已過項，確認未被本輪 content 變更波及）——✅
- 節點數 **3644 不變**、canvas 幾何未動（R12 僅改 text 節點內容）。
- z6DOE（淺）＋BTBTs（深）Export＋TakeScreenshot 目視：選中紙四件套、角托＋白邊＋範例 pill、gap 8 取代分隔線、釘底 Action Bar（條款疊 CTA 上 gap 8）、單顆 $accent 實心 CTA、空狀態卡未塌縮——F1/F2/F3/H2 全視覺在位，零破版/塌縮/溢出。
- CTA 50pt 不變式：Action Bar 底緣 900＝Home Indicator 頂（16 padding-bottom＋34＝50）未動 ✓。

---

## 為什麼是 APPROVE
已完成 13 輪（遠逾 ≥3 硬門檻）；三個記憶點我自己寫得出且 canvas 複驗成立；slop 十條無中槍（本輪 content-only、canvas 未動，沿用 R9/R11 全過結論）；靈魂測試 pass；F-A 三項全數落地、七輪失效值 926/2062 全文件歸零。無殘留過期現況副本 → 依派工判準 **APPROVE**。

---

## 交使用者核可的完整取捨摘要（供 orchestrator 轉呈）
1. **摺線殘餘 58pt**：逐節點實測值（Body 座標系 Role Section Wrap 底緣 643 − 首屏可見窗 585）。落在摺線下的**只有非預設「只能看」列描述的尾段**（「只能看見照片和日記，不會變更。」的末句）；icon／標題／勾選狀態在首屏內；**必讀條款（產生後 7 天內有效、最多 5 位家人）在釘底 Action Bar 內、與 CTA 同框、首屏 100% 可見**。iOS 正規「下面還有」暗示；是否進一步壓縮（designer 已列選項：44 只加非 AX3 板／改寫 Header 副標）由使用者定奪，結構論證不需要。
2. **淺色選中紙 vs 台紙對比 1.21:1**：**刻意不拉高**——$print-paper 淺色 #FBEBEC 與 $surface 同值已是系統最亮，台紙中淺粉，結構上拿不到 3:1 而不動全域 token。選中態不依賴此對比：check icon $print-ink on $print-paper＝**14.92:1**＋字重 700/600＋語意 icon 冗餘編碼，已滿足 WCAG 1.4.11。**深色下同一機制強 11 倍（13.72:1）**，關燈才發光。
3. **釘底動作帶已定為全 app 版式慣例**（使用者 R5 Q1 裁決，已寫入 motifs.md；標題註「暫定 n=1，待第二次套用後定案」，下一張套用票需把「第二次套用」列進驗收）。
4. **沖印品母題已帶入角色列**：借「紙不會變黑」＋白邊/陰影語彙，**刻意不用角托**（角托三段規則明文只給家人照片；角色列是設定性選擇列、非收藏品，用角托會稀釋規則稀缺性）。

---

## Informational 處置
- **⑧-2 字面 vs 歷史標記的輕微張力（非阻擋，僅前瞻觀察）**：`BH3N9` ①／`oGvFN` ④ 仍物理保留 995/2259、906/2042 等歷史數字（皆帶「非現況／不要沿用」標記）。這符合 R11 改法(B)「保留舊值改指向」的原核可路徑，也符合 ⑧-2 對「現況引用」的規範（歷史 log 非現況引用）。**不修**：刪除會失去 R6/R8 沿革稽核鏈，且每個複本已被顯式警語包住、混淆風險已控。若日後想進一步瘦身 Notes，屬 hygiene，可記入 LS-96 待辦池，非本票 scope。
- `04 首次進入`／`Stress-05`／`Stress-10` 無 44 斷點（既有、非本票 scope）——維持 LS-96，本輪未惡化。
- I-b（E4DlKu 13pt 載入文案）／I-c（PXPcH AX3 錯位）／I-f（07b 核准鍵首屏外）——維持待辦池/另票，本輪未觸及。

## 值得保留、重做時絕不要動（防倒洗澡水）
選中紙四件套與「兩種墨」判準（深色最驚豔）；`BH3N9` ② 危險句改寫（含「R6 原提案是錯的、勿實作」）；`oGvFN` ③ F3 對比全套與「不拉高」理由；`oGvFN` ④ 單一真相戳記＋`yRI6y` ⑧-2 規則（本票最重要的過程資產）；列間 gap 8、K0hWL 交換板文案、釘底 Action Bar 50pt 不變式＋條款疊 CTA。

## 產出位置
取證 4 板 Export PNG：`/Users/clyeh/little-sprout/.claude/worktrees/LS-111/.claude/evidence/LS-111/r13-review/`（`z6DOE.png`／`BTBTs.png`／`K0hWL.png`／`Qyv1K.png`；`.gitignore` 已忽略、未 git add）。只審未改稿。

**裁決：APPROVE。** 建議 orchestrator 攜上方四點取捨摘要交使用者核可，過 Design gate。

### 2026-08-31T06:18:11.888Z · Chih-Lin Yeh · `fbd29f26`

**R13 APPROVE（2026-08-31）→ 送使用者核可**：visual-reviewer 第 13 輪 APPROVE（comment `14834eaf`），≥3 輪對抗迭代達標、slop 十條無中槍、七輪 Notes 漂移歸零。設計 gate 待使用者核可後過。orchestrator 已將四點取捨摘要呈使用者裁決（釘底慣例／母題／摺線殘餘 58pt／淺色對比 1.21:1）。核可後：design-only PR 併入 → 角色列實作併 LS-108（依 session-resume-0826：ios-dev 依稿調整 InviteFamilyView）。

### 2026-08-31T06:37:40.978Z · Chih-Lin Yeh · `754ab40b`

## merge-review R1 · APPROVE（design-only, scope + 機械正確性）

PR #182 · head `937853ab7b893676a017d959714d63806706ec94` · base `development`

**職責界定**：本輪不重審設計美學（visual-reviewer 已完成 13 輪、R13 APPROVE comment `14834eaf`＋使用者 2026-08-31 核可）。merge gate 對 design-only PR 只審 **scope** ＋機械正確性。

### Scope（主）— PASS
`git diff origin/development...937853a --name-status` 只有兩檔，無任何 Swift／專案檔／gate／migration 夾帶：
- `M design/littlesprout.pen`
- `M .claude/skills/little-sprout-brand/references/motifs.md`

### 機械正確性 — PASS
- **.pen**：`json.load` 解析成功（top-level dict）＝無 `<<<<<<<`／衝突殘留、非亂碼、非損毀（JSON 能 parse 即證明無 raw conflict marker）。內容不解析（visual-reviewer 已 Pencil MCP 深審）。
- **motifs.md**：26 行**純新增、0 刪除**，無移除既有品牌規範。新增內容＝兩節設計慣例（「紙面基底可以借給非照片元件」LS-111 R6/R8、「釘底動作帶 Action Bar」LS-111 R6）＋一則 ③-1 對帳註記。
- **合併狀態**：`mergeStateStatus` = BLOCKED（非 CONFLICTING；BLOCKED 僅因 required check 尚未貼齊，預期內）。
- **CI（對 937853a）**：db pass / lint pass / rules pass / ci pass — 四 job 全綠（此 branch 早於 LS-115，跑舊 workflow macos-15/Xcode 16.4，build 仍綠）。

### Finding（minor，非 blocker）
`.claude/skills/little-sprout-brand/references/motifs.md` ③-1 節 — 該則標註「【LS-107 對帳，2026-08-31】」，記錄的是 LS-107 PR #177 `fs-code` AX3 寬碼實測結論，歸屬另一票（LS-107）。嚴格說是 LS-111 diff 外溢的第三處新增（票文預期只有兩節設計慣例）。**判為 minor 不擋**：屬 design-only 文件、非功能改動，且內容落在同一 07/06 碼家族 `fs-code` 設計域（與本票 07 板工作相鄰），非「無關內容」。建議 orchestrator 收尾時知悉即可，不需回修。

### Verdict: **APPROVE**（無 blocker/major）
head 開審時 `937853a`＝我 `git diff` 的 SHA；貼 status 前將以 `--expect` 再比對當下 headRefOid。
merge-review commit status 隨後貼於 `937853a`。

### 2026-08-31T06:39:54.254Z · Chih-Lin Yeh · `e03a1221`

**design-only PR #182 併入 development `fa0bb7a`（2026-08-31）→ 收尾**：merge-review APPROVE（scope 乾淨，comment `754ab40b`）。設計 gate 全數通過（visual-reviewer R13 APPROVE＋使用者核可＋merge-review scope）。

**Retro（設計票輕量版）**：13 輪對抗迭代——歷來最長。教訓：① **Notes 數值漂移七輪反覆**是最大時間坑，根因＝Handoff Notes 逐處重抄硬編尺寸、無單一真相來源；R12「單一真相來源＋其餘指向」＋`yRI6y` ⑧-1/⑧-2 維護規則治本，效果待後續設計票驗證（機械化候選已記 LS-96）。② reviewer 誤判（R5「零染料池」）被 R6 照抄進稿又傳導兩輪——⑧-1「reviewer 主張非量測、改稿前必自行 Get」是對應反饋。③ 靈魂面僵局（Q2）靠結構解（浮起紙片 vs 全平）而非表面裝飾破解，第 9 輪成立。④ Pen 工具脆弱性反覆卡關→已升 LS-117（High）。reviewer minor（motifs.md ③-1 LS-107 對帳註記）＝design-doc 相鄰域，不修。設計 .pen 隨下次 promote 到 main；角色列 Swift 實作併 LS-108。→ Done。

