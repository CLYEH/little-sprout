# LS-185 Harness：`overflow-scan.js` 第六支 `board_clip`（後代 AABB 超出有 clip 的 root frame 被裁切）＋收據 `scan_scope: boards|document` 欄位＋`cross_parent_collision` 先過濾候選再配對（大稿 Pencil interrupted 緩解）——LS-96 `83392d32` 剩餘半段／`83694378`／`32754383`

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:M, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-05T01:54:55.502Z |
| 開始 | 2026-09-05T01:54:55.602Z |
| 完成 | 2026-09-05T04:01:41.657Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-185/harnessoverflow-scanjs-第六支-board-clip後代-aabb-超出有-clip-的-root-frame |
| 關係 | related→LS-122, related→LS-119, related→LS-177, related→LS-120, related→LS-144, related→LS-96, related→LS-184, related→LS-168, related←LS-238, related←LS-202, related←LS-201, related←LS-198, related←LS-194, related←LS-180, related←LS-186 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項 `83392d32`（P2，[LS-120](https://linear.app/little-sprout-app/issue/LS-120/設計-chorecmptab-bar-ax3-適應單一形態規則) VR R2 `e8dceb23`，「優先於其他掃描類池項」）：四支溢出掃描對「固定覆蓋層蓋住內容」與「內容被推出板外被 clip」無感——前者已由 [LS-168](https://linear.app/little-sprout-app/issue/LS-168/harnessdesign-evidence-gate-補強三項-notes-板節點-id-存在性檢查設計稿五度notes) 第五支 `text_occlusion` 落地（本票核對並在池項回覆銷除該半段），**後者** `board_clip` **仍缺**：[LS-120](https://linear.app/little-sprout-app/issue/LS-120/設計-chorecmptab-bar-ax3-適應單一形態規則) R2 六個 spacer 把 Card Diary／Load More 推出板外被 clip、四支全綠；[LS-177](https://linear.app/little-sprout-app/issue/LS-177/設計留言愛心互動列與留言-sheetls-22-畫面群推播通知權限提示與設定頁推播通知列-ui-designervisual) R2 designer 自報「Header Row 被捲動內容遮」「底部內容撞 tab bar」同型。同池 `83694378`（`4d8ce8dd` 撤回旋轉感知半段後仍有效的另一半）：收據加 `scan_scope: boards|document` 欄位，讓 `design-evidence-check.sh` 與 VR 分得清 `document_*` 數字是全稿還是限縮板（[LS-177](https://linear.app/little-sprout-app/issue/LS-177/設計留言愛心互動列與留言-sheetls-22-畫面群推播通知權限提示與設定頁推播通知列-ui-designervisual) R1／R2 因 9416 節點分段跑、`cross_parent_collision` 限縮 17 板，收據數字語意不明）。同池 `32754383`（改註 `767eb2cb`）：大稿 `execute` 間歇 interrupted，與 snippet O(n²) 配對相關——`cross_parent_collision` 先過濾 `BLEED_RE` 候選再只配對候選×其餘。`89ed8de0` **旋轉盲點已由** `4d8ce8dd` **撤回：本票不得把雙重旋轉寫進正典腳本，**`ctx.bounds` **已是旋轉後包絡框。** 巡檢 09:5x lane:harness 空（[LS-184](https://linear.app/little-sprout-app/issue/LS-184/harnesslock-持有安全補強-supabase-locksh-hold-在主-checkoutgit-dirgit-common) 進 QA）→ 依 [LS-144](https://linear.app/little-sprout-app/issue/LS-144/harness巡檢加開票責任機械提醒-lane-在飛-0-且無可派候補時動作清單印-開票並列來源story-拆票ls-96-p1-升票) 升票。

## 範圍

1. `scripts/design/overflow-scan.js` 第六支 `board_clip`：對每個掃描板（root frame）若 `clip:true`（或 Pencil 等價屬性），列出後代葉節點（文字／icon／image；`resolveInstances`）AABB 超出 root 邊界 ≥ 閾值（沿用既有 1pt 或 `text_occlusion` 的閾值慣例）的節點：`{board, node, overflow_px, side}`；固定覆蓋層（`cmp/Tab Bar`／FAB 等 `OVERLAY_RE`）本身不算；`SCAN_SKIP_HASH`／`SCAN_HASH_ONLY`／`SCAN_BOARDS` 慣例照用；分類欄位允許 `classification:"intentional_bleed"`。
2. 收據 schema：加 `scan_scope`（`boards`＝限縮 `SCAN_BOARDS`／`document`＝全稿）與每支掃描的 `scope`；`board_clip` 鍵與 `flagged` 陣列；`scripts/gates/design-evidence-check.sh`（＋`design_notes_check.py` 若涉及）驗第六支存在、`board_clip.flagged` 限本票板必為 0（同 `text_occlusion` 規則，[LS-168](https://linear.app/little-sprout-app/issue/LS-168/harnessdesign-evidence-gate-補強三項-notes-板節點-id-存在性檢查設計稿五度notes) 慣例）、`scan_scope` 合法值；自測 `design-evidence-check.test.sh` 補正負樣本。
3. `cross_parent_collision` 效能：先以 `BLEED_RE` 過濾候選節點集，只配對候選×其餘（結果集不變——用合成樹證明 old vs new 輸出逐位元相同），檔頭記錄大稿分段跑法（[LS-177](https://linear.app/little-sprout-app/issue/LS-177/設計留言愛心互動列與留言-sheetls-22-畫面群推播通知權限提示與設定頁推播通知列-ui-designervisual) R1 handoff `bcfa06d5` 的 9 段 hash 手法）。
4. 自測：`scripts/design/` 既有 node 測試（若無則建 `overflow-scan.test.js`／`.test.sh`）補：clip 板內容推出板外 → `board_clip` 命中；無 clip 不命中；覆蓋層不算；`scan_scope` 兩值；`cross_parent` 過濾前後等價。**不得對活的 Pen 執行任何動作**（Pencil MCP 斷線中、Pen 開著 [LS-177](https://linear.app/little-sprout-app/issue/LS-177/設計留言愛心互動列與留言-sheetls-22-畫面群推播通知權限提示與設定頁推播通知列-ui-designervisual) 票檔）——全部用合成 JSON 樹離線驗。
5. 文件：`.claude/agents/ui-designer.md`／`visual-reviewer.md` 五支→六支、`SCAN_BOARDS` 與 `scan_scope` 說明；COLLABORATION §7 對照表該列更新；[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `83392d32`（兩半段皆銷）／`83694378`／`32754383` 銷除註記。

## 不做

旋轉感知改寫（已撤回）；Pencil 端 interrupted 根因；對既有已核可設計稿回填掃描（下一張設計票的收據自然帶上）。

## 驗收

- [ ] 合成樹：clip 板內容被推出 → `board_clip` 命中；`design-evidence-check` 對缺第六支／`scan_scope` 非法／`board_clip.flagged` 非空的收據紅（自測）
- [ ] `cross_parent_collision` 過濾前後輸出逐位元相同（合成樹）
- [ ] merge-reviewer 通過；hotfix 流程（自 main、PR 回 main、back-merge）；下一張設計票（[LS-177](https://linear.app/little-sprout-app/issue/LS-177/設計留言愛心互動列與留言-sheetls-22-畫面群推播通知權限提示與設定頁推播通知列-ui-designervisual) R3）收據以六支＋`scan_scope` 出

## 留言（14）

### 2026-09-05T01:55:12.840Z · Chih-Lin Yeh · `f250ee7c`

**2026-09-05 09:56 orchestrator — 派工**

- lane:harness 空（LS-184 進 QA）→ 依 LS-144／§5-b 升掃描類池項（`83392d32` 池內註明優先）；cycle 1；hotfix 流程：worktree `.claude/worktrees/LS-185`，分支 `hotfix/LS-185-overflow-scan-board-clip`（base origin/main `b9470bc`），PR → main，併入後 back-merge。
- 硬限制：Pen 開著 LS-177 票檔、Pencil MCP 斷線——全部離線合成樹驗證，不碰活的 Pen。
- 派 general-purpose R1。

### 2026-09-05T02:25:38.703Z · Chih-Lin Yeh · `387ef973`

# LS-185 Handoff（ios-dev，2026-09-05 10:23 CST）

## Ticket
LS-185 — `overflow-scan.js` 第六支 `board_clip`＋收據 `scan_scope`＋`design-evidence-check.sh` 同步＋`cross_parent_collision` 候選過濾＋自測＋文件＋LS-96 池項銷除。hotfix 自 `main` `b9470bc` 切，PR #298 → `main`（併入後需補 back-merge `main`→`development`）。

## 已完成
1. **第六支 `board_clip`**（`21c7d73`）：`scripts/design/overflow-scan.js:413` `scanBoardClip`——root frame `clip:true` 的板內，可見葉節點（無 live 子節點且 type ∈ text／icon／path／rectangle／ellipse，或快照 `image:true`；`:122` `LEAF_TYPE_RE`、`:125` `hasImageFill`——.pen 實測型別集無 `image`，照片是帶 image fill 的 frame 246／rectangle 24）矩形先 ∩ root **以下**的 `clip:true` 祖先（沿 `text_occlusion` 可見矩形慣例；被中間 clip 整個裁掉＝捲動模擬、不報），伸出 root 任一邊 > TOL 0.5（沿 `row_overflow`）即報 `{board, board_name, node, name, parent, type, side, overflow_px}`，side 取最大邊；`OVERLAY_RE` 命中之節點及其子樹不算（root 自己的名字不比對，`:130` `resolveOverlayRe` 與第五支共用）；`flagged` 限 `boards`、全稿 `document_flagged`；**無旋轉換算**。Pencil 端快照多帶 `image: hasImageFill(n.fill)`（`:643`）；SUMMARY 加 `scan_scope=`／`board_clip=N/M`（`:651`）；`compactLines` 加 `SCAN board_clip` 段。語意寫在檔頭 `:70`。
2. **`scan_scope`**（`21c7d73`＋`7e360bf`）：`:542` `scanAll` 依 `opts.scanScope`（Pencil `SCAN_SCOPE`，`:617`）輸出頂層 `scan_scope` 與每支 `scope`；`boards` 時以 `:450` `restrictToBoards` 把快照限縮到 `SCAN_BOARDS` 子樹再跑六支（不加新 Pencil API、全 `Get` 走訪照跑只是掃前過濾）；非法值／boards 模式無 boards／限縮後為空一律 throw（`7e360bf`）。檔頭 `:84`。
3. **`cross_parent_collision` 候選過濾**（`21c7d73`）：`:219`／`:240`——每板先 O(n) 標記 `BLEED_RE` 候選，候選 i 配對所有 j>i、非候選 i 只配對其後的候選；走訪序仍 (i, j) 字典序、a/b 指派不變 → 輸出逐位元相同；`crossAll` 時人人都是候選＝原全配對。檔頭「大稿分段跑法」段 `:90`（`bcfa06d5` 9 段 FNV 局部和、分函式三趟、`767eb2cb` 間歇中斷原樣重送）。
4. **Gate**（`c47cfaf`）：`scripts/gates/design-evidence-check.sh:235` `SIXTH_MARKER = "scanBoardClip"`、`:241` `has_marker`（泛化 `has_fifth`）、`:322` `sixth = at_sha or (is_latest and at_head)`（與第五支同形，含 fail-closed）、`:460-488` 驗 `scan_scope`（缺→紅 `:472`；非 `boards|document`→紅）、各支 `scope` 若在只接受兩值、`scans.board_clip` 存在（`:482`）且 `flagged` 為空（`:486`，帶 classification 亦紅）、N1 同形（最新收據 head_sha tree 無第六支但 PR head 已含→紅）；舊收據放行印一行 `:508`。**cutoff 決定**：不用時間、用腳本標記（LS-168 慣例）——in-flight LS-177 要等 back-merge 才拿得到新腳本。檔頭 `:47`。
5. **自測**：`scripts/design/overflow-scan.test.js` 22→32 組（`95eb9c2`；`:456` board_clip 七案、`:575` scan_scope、`:593` 等價證明含 `:595` `legacyCrossParent`（照抄 `b9470bc`）與 `:631` `synthBoard` seeded LCG）；`scripts/gates/design-evidence-check.test.sh` ㉞～㊶ 12 樣本（`c47cfaf`；`:663` `land6`、`:683` `write_receipt6`、`:703-783`），40→52 全綠。
6. **文件**（`3f14b31`）：`.claude/agents/ui-designer.md:26-33`（四支→六支、新增第 6 項、六支一律走＋`SCAN_SCOPE` 用法、收據 JSON 加 `scan_scope`／`board_clip`、六個鍵必在）＋`:85`（handoff 欄）；`.claude/agents/visual-reviewer.md:35`（(6) 板裁切、`board_clip.flagged > 0` BLOCKER、`scan_scope` 一致性）＋`:56`（重掃比對）；`docs/COLLABORATION.md:259` 新增第六支列、`:257` 收據列 gate 欄指回；`CLAUDE.md:12` 收據句同步六支＋`scan_scope`（**票文未列 CLAUDE.md**，但該句列舉掃描名與必為 0 欄位，不同步會誤導每個 session——請 reviewer 認可或退回）。
7. **LS-96 銷除註記**：`83392d32` 兩半段 → `180c3118`（overlay 半段核對 `text_occlusion` 確涵蓋「固定覆蓋層蓋住文字」，OE 案即 LS-120 (a) 同型；board_clip 半段本票）；`83694378` → `b5147978`；`32754383` → `f268575c`。皆附 PR #298＋commit SHA。

## 已驗證（怎麼驗）
- `node scripts/design/overflow-scan.test.js` 32 組全綠；`bash scripts/gates/design-evidence-check.test.sh` 52 樣本全綠；`design-notes-check.test.sh` 全綠；`agent-tools-check.sh` 綠（13 條字樣）。
- **Mutation（scratch 副本逐支，皆紅）**——掃描 13 支：M1 root clip 檢查移除→第 24 組、M2 OVERLAY 子樹排除移除→25、M3 中間 clip 祖先不交集→26、M4 TOL→0→23、M5 image 葉節點不算→23、M6 葉型別只剩 text/icon→27、M7 boards 範圍不過濾→28、M8 scan_scope=boards 不限縮→30、M9 每支 scope 標籤移除→30、M10 cross 非候選側從 k=0 配對→31、M11 非候選側整段丟掉→第 5 組（既有 (c) 精確集合）、M12 空限縮不 throw→30、M13 side 取第一個而非最大→27（初版存活，補 `KEBIG` 雙邊 fixture 後殺）。Gate 7 支：G1 接受非空 `board_clip.flagged`→㊲、G2 不驗 `scan_scope` 合法值→㊱㊴-b、G3 不要求 `board_clip`→㉟、G4 不要求 `scan_scope`→㊳、G5 最新收據不看 PR head→㊵、G6 取消舊收據放行→①⑨⑬⑰㉒㉔㉖㉛㉜-b㉝㊴ 11 個既有綠樣本紅、G7 不驗每支 `scope`→㊶。
- **等價證明**：主 fixture（bleed-only 4 筆／crossAll 5 筆）、遮蔽 fixture、裁切 fixture、三 seed 合成板（400 節點、bleed 每 9／5／2 個一個，合計命中 > 50）新舊 `JSON.stringify` 逐位元相同；3000 節點單板 51ms → 6ms（flagged 439，相同）。
- **真實收據不誤紅**：以本 PR 的 gate 對 `origin/feature/LS-177-comments-reactions-design`（`33b43b6`，腳本只含第五支）跑 `design-evidence-check.sh --ticket LS-177 --base origin/development --head-sha <該 ref>`（收據暫以 `git show` 落到 worktree 未追蹤路徑，跑完即刪、worktree 乾淨）→ r1（`fdd5617`）／r2（`5f52b03`）皆綠＋「LS-185 新欄位 board_clip／scan_scope 不要求——舊收據放行」。
- PR body 過 `pr-body-check.sh`；每個 commit 過 commit／push gate；未碰活的 Pen、模擬器、容器；未寫入主 checkout。

## 未完成／不做（含判斷）
- **Pencil 端實跑**：票文明令不得對活的 Pen 執行任何動作——`SCAN_SCOPE`、快照 `image` 欄位、SUMMARY 新欄位在 execute 內只由 node 純函數與原始碼斷言覆蓋；首張真實六支收據由 LS-177 R3 出（票文驗收第三項本就交下一張設計票）。
- **`intentional_bleed` 的位置**：票文範圍 1 寫「分類欄位允許 `intentional_bleed`」、驗收寫「`board_clip.flagged` 非空的收據紅」——兩者只在「in-scope `flagged` 必為空、`intentional_bleed` 是 `document_flagged`（他票板）的固定分類字面」下相容，本票採此解並寫進 gate 檔頭／agent 文件；若 orchestrator 意在「`flagged` 內標 `intentional_bleed` 可放行」，gate `:484-488` 改一處＋㊲ 反向即可。
- **不做 `SCAN_ONLY` 分函式旗標**：票文只要求檔頭記錄分段跑法；旗標會牽動 SUMMARY／compactLines 對缺支的處理，超出範圍。殘餘：分函式仍需手改 snippet，與「不得臨場手寫」有張力——已在 `f268575c` 標 informational，**未入池**，請 orchestrator 決定是否記入 LS-96。
- **icon 被覆蓋層蓋住**：`text_occlusion` 只看 `type:"text"`，池項 `83392d32` 原文寫「文字／icon 葉節點」——overlay 半段以「文字」核銷，icon 殘餘在 `180c3118` 標 informational，**未入池**，請 orchestrator 決定。
- 旋轉感知改寫、Pencil interrupted 根因、既有已核可稿回填：依票文不做。
- reviewer informational：尚無 merge-review（PR 剛開）。

## 風險
- **LS-177 R3 可預期真實命中**：`y7KAW`／`y66AzT` Header Row 移到 `y=−770` 捲離畫面（R2 handoff 自述，不在任何中間 clip 內）→ `board_clip` 會報 top ≈770、gate 紅——方向正確（VR R2 NEW-5 已指出該 hack），designer 改 `enabled:false` 或包 clip 容器即可；請在派 R3 時預告。
- 判斷項：只看 root frame 的 clip（LS-120 若 Feed 自身 clip 就抓不到，靠 VR 截圖）；無 fill 的 rectangle／ellipse 也算可見（誤擋方向）、有 solid fill 的空 frame 不算（漏報方向）；閾值 0.5pt。
- `scan_scope=boards` 不縮短 `Get` 走訪本身的時間（`767eb2cb` 已證走訪非瓶頸）。
- CLAUDE.md 一行改動超出票文列舉範圍（見已完成 6）。

## 產出位置
- PR #298 → `main`：https://github.com/CLYEH/little-sprout/pull/298（head `3f14b31`；commits `21c7d73`／`7e360bf`／`95eb9c2`／`c47cfaf`／`3f14b31`，皆已 push）
- worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-185`，分支 `hotfix/LS-185-overflow-scan-board-clip`（乾淨）
- scratchpad：`LS-185-pr-body.md`、`LS-185-handoff.md`、`LS-185-mut/`（mutation 副本，可刪）
- LS-96 回覆：`180c3118`／`b5147978`／`f268575c`

### 2026-09-05T02:27:00.400Z · Chih-Lin Yeh · `22b558e3`

**2026-09-05 10:3x orchestrator — R1 handoff `387ef973` 收到（PR #298 head `3f14b31`）→ In Review，派 merge-reviewer R1**

- 裁定：(1) `intentional_bleed` 採實作者做法——in-scope `board_clip.flagged` 必為空，該分類字面只用於 `document_flagged`（票文「允許」與驗收「非空紅」衝突，以驗收為準）；(2) CLAUDE.md L12 五→六支同步接受（一行，與 COLLABORATION 對齊）；(3) 兩則 informational 入池 LS-96（`text_occlusion` 不涵蓋 icon 被蓋、分函式無 `SCAN_ONLY` 旗標）。
- LS-177 R3 派工時預告：`y7KAW`／`y66AzT` Header Row（y≈−770 捲離畫面）會被 `board_clip` 報 top 溢出，designer 需改 `enabled:false` 或包 clip 容器（VR R2 NEW-5 同源）。

### 2026-09-05T02:43:38.028Z · Chih-Lin Yeh · `d87d4334`

## merge-review R1 — **APPROVE**（PR #298，head `3f14b31b6457aee07a9de1a01a4d30de00e73e67`，base `b9470bc`；2026-09-05 10:41）

範圍：`git diff b9470bc...3f14b31`，8 檔 +594/−47（scanner 1＋自測 1／gate 1＋自測 1／文件 4）。**無 blocker、無 major**；2 條 minor＋2 條 informational。CI `ci`／`db`／`lint`／`rules` 四項全 pass（run 33938869410）。**未看到任何旋轉換算**（`git diff | grep -i 'rotation|Math.(cos|sin|atan)'` 空、全檔無三角函數）——符合 `4d8ce8dd` 撤回後的紅線。

### 我實跑的驗證（獨立於 handoff）

- **自測**：`node overflow-scan.test.js` **32 組全綠**；`design-evidence-check.test.sh`（真 bash 3.2.57）**52 組全綠**（40→52＝新增 12，與宣稱相符）。
- **我自己的 scanner mutation 12 支（暫存樹，基線先驗 32 綠）**：MB1 拿掉中間 clip 祖先交集／MB2 閾值失效（TOL→−1）／MB3 拿掉 OVERLAY_RE 子樹例外／MB4 拿掉 root `clip:true` 前提／MB5 side 取第一個而非最大／MB6 boards 範圍不過濾／MB8 `scanScope=boards` 不限縮／MB9 空限縮不 throw／MB10 候選側從 k=0 配對／MB11 非候選側整段丟掉／MB12 image 葉節點不算 → **11 支 killed**（分別卡在 (f) 七案、scan_scope 案、(c) 等價案，位置合理）。**MB7 存活**，見 minor-2。
- **gate mutation 7 支（sandbox clone 內，未動真 repo）**：G1 接受非空 `board_clip.flagged`→㊲紅／G2 不驗 `scan_scope` 值→㊱㊴-b／G3 第六支在時不要求 `board_clip`→㉟／G4 不要求 `scan_scope`→㊳／G5 拿掉「最新收據另看 PR head tree」→㊵／G6 不驗每支 `scope`→㊶／G7 cutoff 失效（`sixth=True`）→11 個既有綠樣本紅 —— **7/7 killed**，與 handoff 宣稱一致。
- **等價證明我自己重跑（比 handoff 更大）**：`git show b9470bc:` 的舊 `overflow-scan.js` 對新版，40 顆隨機種子 × 4 板 × 220 節點／板（隨機 clip／enabled／type／image／命中 BLEED_RE 與 OVERLAY_RE 的名稱）× 4 組 opts（預設／`crossAll`／`boards`／自訂 `overlayRe`）→ **五支 flagged 共 800 次比對、`JSON.stringify` 差異 0**（`cross_parent_collision` 累計命中 229,998 筆，不是拿空陣列比空陣列），`scanned_nodes` 亦相同。**document 模式相對舊行為是純增量**（多出 `scan_scope` 與各支 `scope`，其餘逐位元不變）。候選過濾的等價我也從程式碼確認：舊版過濾式 `!all && !BLEED(a) && !BLEED(b) → continue` 的補集恰是「至少一側候選」，新版 `cand×全部 ∪ 非cand×其後的cand` 覆蓋且不重複，(i,j) 升序不變、`a=arr[i]`／`b=arr[j]` 指派不變；`BLEED_RE` 無 `g` 旗標（`.test` 無狀態），把 O(n²) 次呼叫降成 O(n) 次不會因 `lastIndex` 改變結果。
- **in-flight LS-177 不誤紅——我端到端重跑，而非只讀邏輯**：`git clone --shared` 出 sandbox（未寫入任何真 worktree），checkout `origin/feature/LS-177-comments-reactions-design`（`33b43b6`，該 tree 的 `overflow-scan.js` 含 `scanBoardClip` 次數＝**0**），把本 PR 的 `design-evidence-check.sh`＋`design_tree_hash.py` 覆蓋進去後跑 `--ticket LS-177 --base origin/development` → **exit 0**，r1（`fdd5617`）／r2（`5f52b03`，本輪最新、另看 PR head tree）都印「尚無第六支…舊收據放行」。cutoff 用腳本標記而非時間，符合 LS-168 慣例。
- **`scan_scope` fail-loud 邊界（audit 2）**：離線探針——①板名全對→限縮正確；②**一對一錯→不 throw、靜默只掃到對的那個板**（`scanned` 4→2，`boards` 內留下字面 `"Board Bee"`，另一板的真溢出從 `flagged` **與** `document_flagged` 同時消失）；③全錯→throw（`7e360bf` 的修正生效）；④無 boards→throw；⑤`scanScope` 大小寫錯→throw。②的安全網在 gate：`design-evidence-check.sh:385-387` 要求 `corner_anchor.boards` 每個 id 都存在於 `head_sha` 快照頂層，打錯的字面會被紅（`:398-402` 另驗覆蓋本 PR 觸碰的板）——**所以整體仍是 fail-loud，只是攔在 gate 而非 scanner**（見 informational-1）。
- **逐條核實**：handoff 的 13 個行號錨點（`overflow-scan.js:413/122/125/130/450/542`、`design-evidence-check.sh:235/241/322`、兩支自測 `:456/:595/:663/:683`）以 `sed -n` 抽驗**全部命中**；逐 commit 檔案歸屬乾淨（`21c7d73` scanner＋4 行測試 schema 斷言／`7e360bf` 1 行 throw／`95eb9c2` 純測試／`c47cfaf` gate＋自測／`3f14b31` 純文件）——**無夾帶**；`pr-body-check.sh` 綠（LS-96 行 1 條帶 comment id）。LS-96 銷除 `180c3118`（parent `83392d32`）／`b5147978`（`83694378`）／`f268575c`（`32754383`）都在、掛對母 comment、帶 PR＋SHA；**`83392d32` overlay 半段的銷除敘述是誠實的**——它明寫「`text_occlusion` 只看 `type:"text"`，icon 被覆蓋層蓋住仍無感」為殘餘，orchestrator 已把它與 `SCAN_ONLY` 一起入池（`65a0a69b`）。

### findings

**minor-1 · `scripts/design/overflow-scan.js:70` 檔頭「捲動模擬不報」的例子與 LS-177 R3 的實際爆炸半徑**
檔頭寫「被中間 clip 容器整個裁掉的內容是捲動模擬（**LS-177 R2 `y7KAW` Content `clip:true`＋`height:1213`**…），不是被板裁掉、不報」。這句對「Content 底下超出 Content 的部分」成立，但**對這張稿的實際結構不成立**：我離線讀了 LS-177 分支的 .pen（`git show …:design/littlesprout.pen`，未碰活的 Pen）——`Dbc4l` Content 是 `layoutPosition:absolute, y=-671, height:1213, clip:true`，掛在**無 clip** 的 `qXm4l` Body 底下，也就是 **Content 自己就伸到板頂之上約 600pt**；落在 Content 內、板頂之上的葉節點與 Content 求交集後仍在板外 → 依 `:436-439` 一律報 `top`。
- 具體情境：`Section 個人`（4 個葉節點）、`Section 家庭`（3 個，`ref` 未展開、實際更多）在捲離視窗的那一段，會跟 `JMUMY`/`ZqB7P` Header Row 的 `UjPDC`／`p4WBIg`（每板 2 筆，共 4 筆）一起被報 —— **不只 handoff／orchestrator 預告的「Header Row 4 筆」**。
- 影響：`board_clip.flagged` 必為空且不接受白名單，所以 LS-177 R3 的補救不是「Header Row 改 `enabled:false`」就好，而是**兩張板的捲動態都要改成「與板同尺寸的 `clip:true` 視窗 ＋ 內層長欄」**（正是 `ui-designer.md:32` 已經寫對的做法）。
- 建議：檔頭那句補一個限定（「**中間 clip 容器本身也要落在板內**；容器自己伸出板外時，容器內、板外的葉節點仍會報」），並在派 LS-177 R3 時照這個半徑預告。**不是程式錯誤**（行為與票文一致、與 LS-120 的原始事故同類），是正典檔頭的說明失準＋風險低估。
- 精確度說明：葉節點數量是**離線估算（PLAUSIBLE）**——auto-layout 的絕對座標要 Pen 才算得準；但「不只 Header Row」這個方向由 `y=-671` 的宣告幾何＋掃描邏輯直接推得，不需 Pen。

**minor-2 · `scripts/design/overflow-scan.js:419` 葉節點限制讓「有子節點的 image fill frame」整個掃不到（且沒有測試釘住）**
`if ((kids.get(n.id) || []).length) continue;` 之後才看 `LEAF_TYPE_RE || image===true`——由於 `frame` 不在 `LEAF_TYPE_RE`，這行**唯一的實際效果就是排除「帶 image fill 又有子節點」的節點**。
- 具體失敗情境（我實跑探針）：板 800×600 `clip:true`，`Photo`（frame＋image fill）掛在 `y=500 h=260` → 底部溢出 160pt，其唯一子節點 `Caption` 貼在 frame 頂端仍在板內 → **現行實作回 `[]`**（把該行拿掉就回 `PH:bottom:160`）。
- 可及性不是假設：LS-177 分支的 .pen 全稿有 **276 個帶 image fill 的節點，其中 4 個有子節點**（`bjq5n` Photo Wrap／`mBvH4`・`QoRXo`・`OSH9L` Thumb，子節點都是 `Video Badge`）——影片縮圖被推出板外、而角落徽章仍在板內時就漏報。池項 `83392d32` 原文是「任何後代 AABB 超出 root frame」，本票收斂成「葉節點」（票文範圍 1 也這樣寫），所以**行為選擇本身站得住**；問題是它沒有被測住：我的 MB7（拿掉這行）是 12 支 mutation 裡**唯一存活**的一支，32 組全綠。
- 建議（擇一）：(a) 對 `n.image === true` 免除葉節點限制（照片框概念上就是葉子，也把上面 4 個真實節點納回）；或 (b) 保留現行語意但補一組 fixture 釘住「有子節點的 image frame 不報」＋在檔頭「漏報方向」那段補這一句（目前只寫了「有 solid fill 的空 frame 不算」）。

**informational-1 · `overflow-scan.js:450` `resolveBoards` 對打錯的板名回字面、`restrictToBoards` 靜默縮小**：全錯才 throw，部分錯只會少掃一塊而 `document_flagged` 也跟著少（探針②）。gate 端有攔（`:385-387` id 必須存在於 head_sha 頂層），所以不是漏洞；但錯誤訊息會出現在「收據 gate」而不是「掃描當下」，設計端要多跑一輪才知道。候選：`scanAll` 在 `boards` 模式下對解不到 root 的 entry 直接 throw（一行），或至少 Print 一行警告。`resolveBoards` 是既有函式、非本票新開，僅記錄。

**informational-2 · `.github/workflows/ci.yml:195`／`:394` 註解仍寫「四支」**：LS-168 加第五支時就沒同步，本票加第六支同樣沒動。**不是本 PR 造成的退化**（我確認 `b9470bc` 上就已是「四支」），且 CI 行為不受影響——列出來只為避免下一個人以為 gate 只驗四支。

### 裁定與確認

- **`intentional_bleed` 位置**（orchestrator 裁定 1）：實作與裁定一致——`flagged` 必為空、`document_flagged` 才用該字面；gate `:484-488` 帶 classification 亦紅，㊲ 釘住，G1 mutation 殺得掉。
- **CLAUDE.md L12 一行**（裁定 2）：確認只有一句、與 COLLABORATION §7／兩份 agent 定義口徑一致，非夾帶。
- **兩則 informational 入池**（裁定 3）：`65a0a69b` 已存在且兩則都在。

### 沒看／沒跑的（誠實揭露）

- **完全沒有碰 Pen／Pencil MCP／模擬器／容器**：Pencil 端路徑（`:617` `SCAN_SCOPE` 取值、`:643` 快照 `image: hasImageFill(n.fill)`、SUMMARY 新欄位、`Get(resolveInstances)` 展開後的絕對座標）**只由原始碼斷言與純函數自測覆蓋，我無法驗它在真 Pen 上算得對**——第六支對真實稿的第一份數字要等 LS-177 R3。
- minor-1 的葉節點筆數是離線估算（auto-layout 未解），`ref` 未展開，實際只會更多不會更少。
- `design-notes-check.sh`／`design-landing-check.sh` 本 PR 未改、未審；我只確認收據消費者僅 `design-evidence-check.sh`（＋CI 呼叫它）。
- lock：**未持有**（本票純離線 node／bash，不需要本機容器）。

**verdict：APPROVE**（無 blocker／major）。minor-1／minor-2 建議在併入前順手處理（各一到兩行＋一組 fixture）或由 orchestrator 決定入池；併入 `main` 後補 back-merge `main`→`development`（in-flight LS-177 要拿到第六支才有新腳本可跑）。

### 2026-09-05T02:45:10.397Z · Chih-Lin Yeh · `5733e44d`

**2026-09-05 10:5x orchestrator — merge-review R1 `d87d4334` APPROVE 帶 2 minor → 派實作者 R2（併前修）**

- minor-2（image fill＋子節點的 frame 不報、無測試）→ 對 `image===true` 免除葉節點限制＋fixture＋mutation 自證；minor-1（檔頭 `y7KAW` 例子誤導）→ 改寫條件句，並把 LS-177 R3 預告改為「板尺寸 clip 視窗＋內層長欄」。
- informational-1（`resolveBoards` 部分錯板名靜默縮小）／informational-2（`ci.yml:195`／`:394` 註解「四支」過時）→ 記入 LS-96。

### 2026-09-05T02:49:33.026Z · Chih-Lin Yeh · `98349038`

## LS-185 R2 Handoff（ios-dev，2026-09-05 10:48 CST）— merge-review R1 `d87d4334` 2 minor 已修

**新 head**：`73fa7e1`（PR #298，已 push；PR body 已增「R2」段）。

### 已修
- **minor-2（必修）**：`scripts/design/overflow-scan.js:427-428`——可見節點規則改為「`image:true` 的節點不論有無子節點都以自身 AABB 參與；其餘只算無 live 子節點的 text／icon／path／rectangle／ellipse」（原本葉節點限制讓 Photo Wrap／Thumb＋Video Badge 這種帶子標籤的照片框伸出板外時完全不報）。檔頭 `:71-73` 同步。fixture `overflow-scan.test.js:503-509` 板 `KG`：Photo Wrap（image、帶 Caption text＋Video Badge icon 皆在板內）框底伸出 160 → 恰報 `KGWRAP:bottom:160` 一筆、子節點不另報；完全在板內的 Thumb 帶子節點不報；同幾何拿掉 image → 不報（`:563`）。
- **minor-1**：檔頭 (f) 段 `:75-80` 改寫為精確條件——「中間 clip 祖先只把可見區域縮成節點 AABB ∩ 該祖先 AABB，可見區域為空才不報；**若該祖先本身伸出 root，其內、板外的節點仍以 root 裁切判定＝照報**」，並明寫 LS-177 R2 `y7KAW` Content（`y=−671, h=1213, clip:true`，掛在無 clip 的 Body 下）**不是**不報的例子。**對 LS-177 R3 的預告（修正前一則 handoff）**：`y7KAW`／`y66AzT` 需改成「板尺寸的 clip 視窗＋內層長欄」，**不是**只把 Header Row 設 `enabled:false`——Content 自身伸出板頂約 600pt，其內位於板外的列文字全會被 `board_clip` 報。
- informational-1／-2：依 reviewer 指示交 orchestrator 處置，本 PR 不動。

### 已驗證
- `node scripts/design/overflow-scan.test.js` 32→**33** 組全綠；`bash scripts/gates/design-evidence-check.test.sh` 52 全綠（gate 未變）；LS-177 r1（`fdd5617`）／r2（`5f52b03`）收據以本 PR gate 實跑仍綠＋放行行。
- **Mutation（scratch 副本）**：MB7 還原葉節點限制（image 容器帶子節點不參與）→ 第 28 組 `minor-2` 案紅；M5 image 節點完全不算 → 第 23 組紅；MB7b 只讓「有子節點的 image」參與（image 葉節點反而不算）→ 第 23 組紅。三支全殺。
- PR body 過 `pr-body-check.sh`；commit／push gate 綠；未碰活的 Pen。

### 本輪一個流程失誤（已補救，如實記錄）
第一次更新 PR body 時把 `pr-body-check.sh` 接在 `| tail -1` 後、exit code 被管線吃掉——gate 其實紅（informational 行寫了「入池」字樣卻無 comment id），我沒察覺就 `gh pr edit`＋push 了 `73fa7e1`。發現後改寫該行措辭（不含 入池／LS-96／待辦池 字樣，因入池是 orchestrator 的動作）、重跑 gate 以 exit code 判定綠、`gh pr edit`，並 close/reopen PR #298 讓 CI 重讀 body（COLLABORATION §6 做法；head 不變 `73fa7e1`）。教訓：gate 一律以 exit code 分支，不接 `| tail`。

### 產出位置
PR #298 head `73fa7e1`（commits `21c7d73`／`7e360bf`／`95eb9c2`／`c47cfaf`／`3f14b31`／`73fa7e1`）；worktree `/Users/clyeh/little-sprout/.claude/worktrees/LS-185` 乾淨；scratchpad `LS-185-pr-body.md`（含 R2 段）／`LS-185-handoff.md`。

### 2026-09-05T02:50:28.799Z · Chih-Lin Yeh · `89013cf0`

**2026-09-05 11:0x orchestrator — R2 handoff `98349038` 收到（head `73fa7e1`：image 節點免葉節點限制＋fixture＋3 mutation 紅；檔頭條件句改寫；R3 預告修正）→ 派 merge-reviewer R2 審增量。實作者自報 PR body 一次誤推（`| tail -1` 吃掉 gate exit code）已改並 close/reopen 重讀——記入 LS-96 作 pr-body-check 用法教訓。**

### 2026-09-05T02:59:15.436Z · Chih-Lin Yeh · `f3211631`

**R2 補記（PR body 修正，head 不變 `73fa7e1`）**：CI `rules` 在 reopen 後仍紅（run 33940128173）——`pr-body-check --verify` 把 R2 段標題「…`d87d4334` …皆已修」的 SHA 候選抓成 merge-review comment id、反查不在 PR commits。已把該行改成「已修 `73fa7e1`（review comment `d87d4334`）」（SHA 在「已修」後、comment id 放括號），本機 `pr-body-check.sh <body> --branch hotfix/LS-185-overflow-scan-board-clip --verify` 以 exit code 判定 0（「第 24 行『已修』SHA 73fa7e1 在 PR commits 內」），`gh pr edit 298` 後 close/reopen 一次、未加空 commit。**run 33940294837：success——jobs rules／ci／lint／db 全綠**（event=pull_request，head `73fa7e1`）。reviewer R2 可依此 head 審增量。

### 2026-09-05T03:00:19.011Z · Chih-Lin Yeh · `b68fa0bd`

## merge-review R2（增量）— **APPROVE**（PR #298，head `73fa7e10ec3502322a861ecbc99d4bb6680bf6b3`；2026-09-05 10:59）

範圍：`git diff 3f14b31..73fa7e1`＝1 個 commit、**只有 2 檔**（`scripts/design/overflow-scan.js` +13/−7、`overflow-scan.test.js` +21/−2）。gate／文件／`.claude/`／`CLAUDE.md` 在增量中**一行未動**（`git diff --name-only … -- scripts/gates/ docs/ .claude/ CLAUDE.md` 回 0）——所以 R1 那次對 in-flight LS-177 r1／r2 的端到端放行結論原封不動成立（cutoff 標記 `scanBoardClip` 仍在、gate 檔逐位元未變）。**無夾帶**，worktree 乾淨。

**CI（audit 3 的核心）**：head 不變但 PR 曾 close/reopen 兩次；最終 run **33940294837 四項全 pass**（`ci` 4m50s／`db` 3m30s／`lint` 15s／`rules` 3m51s）——我用 `gh run watch --exit-status` 等到結論才貼 status。**前一次 run 33940128173 的 `rules` 是真紅、不是 stale**：見下方 process-1。

### minor-2 已修，我獨立驗過（audit 1）

- **程式**（`overflow-scan.js:427-428`）：`const isLeaf = !(kids…).length; if (!(n.image === true || (isLeaf && LEAF_TYPE_RE.test(n.type||"")))) continue;` —— image 節點不再受葉節點限制，其餘維持原語意。
- **我自己重跑的 mutation（暫存樹，基線先驗 33 綠）**：
  - **MB7 還原葉節點限制**（`!isLeaf || !(image || TYPE)`）→ **killed，恰好卡在第 28 組**（新增的 minor-2 案），與實作者宣稱一致。
  - **M5 image 節點完全不算** → killed（第 23 組）。**MB7b 只讓「有子節點的 image」參與**（image 葉節點反而不算）→ killed（第 23 組）。我另加 **MB7c**（改看 parent 的 image 而非自身）→ killed（第 23 組）；**MB1 中間 clip 祖先交集**回歸 → 仍 killed（第 26 組）。**5/5**。
- **語意探針（直接呼叫 `scanBoardClip`，板高 600）**：① 照片框伸出板底 160、子標籤在板內 → **`PH:bottom:160`**（R1 的漏報案確實關上了）；② 框與子標籤都伸出 → `PH:bottom:160` + `CP:bottom:140` **兩筆**（見 info-2）；③ 完全在板內的 Thumb 帶子節點 → 不報；④ **無 image fill 的純容器 frame 伸出板外、子在板內 → 仍不報**（原語意未被放寬）；⑤ 被中間 clip 祖先整個裁光的 image → 不報；⑥ 名稱命中 OVERLAY_RE 的 image 節點 → 不報。fixture `KG`（`overflow-scan.test.js:503-509`、案 `:563`）的三項斷言與我的探針一致，且它多釘了一條負控（同幾何拿掉 `image` → `flagged` 為空），teeth 足夠。

### minor-1 檔頭改寫精確、且與程式一致（audit 2）

`overflow-scan.js:75-80` 現在寫「中間 clip 祖先只把可見區域縮成『節點 AABB ∩ 該祖先 AABB』，可見區域為空才不報；**若該祖先本身伸出 root，其內、板外的節點仍以 root 裁切判定＝照報**」，並明寫 LS-177 R2 `y7KAW` Content（`y=−671, h=1213, clip:true`，掛在無 clip 的 Body 下）**不是**不報的例子、正確做法是「板尺寸 clip 視窗＋內層長欄」。
我不只讀文字，另用探針⑦驗這句對程式成立：祖先 `Scroll`（`y=−300, h=900, clip:true`）內的 image 節點 `y=−250` → **`P3:top:250`** 照報。條件句與 `:436-441`（`r` 交集 →`r.w>0&&r.h>0` → 對 root 算四邊）逐條對得上。R3 預告已改成「板尺寸 clip 視窗＋內層長欄」，與 R1 minor-1 的裁定一致。

### 自測（audit 3）

`node overflow-scan.test.js` **33 組全綠**（32→33）；`design-evidence-check.test.sh`（bash 3.2.57）**52 組全綠**（gate 未動）。既有 `boards=['KA']` 案的計數同步改成 `document=12`（KG 板恰好多 1 筆），數字自洽。

### findings

**process-1（informational，已於審查中被修復——如實記錄）· PR body 一度真的紅，且是第二次以「非 CI 模式」的呼叫誤判為綠**
我在 10:52 取得的 body 第 24 行是 `## R2（merge-review R1 `d87d4334` APPROVE 帶 2 minor，皆已修）`；用 **CI 的同一組旗標** `pr-body-check.sh --branch <branch> --verify` 在本機重現 → **exit 1**：「第 24 行『已修』的 SHA 不在 PR commits 內（候選：d87d4334）」——`pr-body-check.sh:99-166` 是全 body 逐行掃、不辨語境，該行同時含「已修」與一個 8 位 hex（**我的 Linear comment id**），就被當成申報的 commit SHA。這正是 run 33940128173 `rules` 6 秒就紅的原因（步驟名「PR body 檔頭段含本票票號＋申報可驗證」）。
- R2 handoff 寫「PR body 過 `pr-body-check.sh`」——但那是**不帶 `--branch --verify` 的呼叫**，該模式只驗格式（腳本自己會印「`--verify` 才反查」）。上一輪是 `| tail -1` 吃掉 exit code，這一輪是**旗標與 CI 不同**，兩次都讓 body 帶紅上路。
- 我複查時 body 已被更正為 `## R2：merge-review R1 APPROVE 帶 2 minor，已修 `73fa7e1`（review comment `d87d4334`）`；以同一組 CI 旗標重跑 → **exit 0**（`✓ 第 24 行「已修」SHA 73fa7e1 在 PR commits 內`；候選只要有一個成立即可，`:153-166`），run 33940294837 `rules` 亦綠。**現況無阻擋**。
- 教訓（建議記進 LS-96／§7）：驗 PR body 一律用 CI 的完整形式 `bash scripts/gates/pr-body-check.sh --branch <branch> --verify <file>` 並以 exit code 分支；另可考慮讓「已修」行的 hex 候選排除已知的 Linear comment id 形狀（或要求 SHA 緊跟在「已修」之後），避免引用 review comment id 就誤觸——但那是 gate 改良，非本票。

**info-1（informational）· 三處面向 designer／VR 的定義仍寫「可見葉節點」**：`.claude/agents/ui-designer.md:32`、`docs/COLLABORATION.md:259`、`design-evidence-check.sh:47`／`:482`。嚴格解析（「（無 live 子節點且 type∈…）**或**（帶 image fill）」）與新程式其實相容，所以**不是錯的**；但上位標籤「葉節點」現在會誤導——R1 那個漏報案（Photo Wrap＋Video Badge）正是讀者最可能讀錯的形狀。建議下次動這幾份時各補半句「image 節點不論有無子節點」。本輪未改，不阻擋。

**info-2（informational）· 框與子節點同時伸出板外時會各報一筆**（探針②：`PH:bottom:160` ＋ `CP:bottom:140`）。語意上兩者都真的在板外，不算誤報；但 VR／designer 看到同一個照片框「重複」出現兩筆時要知道這是預期行為。另提醒 LS-177 R3：**帶子節點的滿版照片框從此也會被報**（R1 之前它們完全不參與），修法同樣是包進與板同尺寸的 clip 容器。

### 沒看／沒跑的（誠實揭露）

- 仍**完全沒有碰活的 Pen／Pencil MCP／模擬器／容器**；Pencil 端 `image: hasImageFill(n.fill)` 的真實取值只有原始碼斷言，第六支對真實稿的第一份數字仍要等 LS-177 R3。
- LS-177 r1／r2 收據這一輪**沒有重跑** gate 端到端——依據是「增量未動 gate 任何一行」（`git diff --name-only` 實測 0 檔），R1 的 sandbox 實跑結論直接沿用。
- gate 自測我只驗全綠（52 組），未重跑 R1 的 7 支 gate mutation（gate 未變）。
- lock：**未持有**（純離線 node／bash）。

**verdict：APPROVE**（無 blocker／major；R1 兩條 minor 皆已修且被測住）。併入 `main` 後補 back-merge `main`→`development`。

### 2026-09-05T03:02:10.386Z · Chih-Lin Yeh · `cad3ca0e`

**2026-09-05 11:0x orchestrator — merge-review R2 `b68fa0bd` APPROVE → PR #298 已併入 main `6b2d52a`；back-merge main→development 背景鏈進行（併入後 dev→test → delta QA 兩支自測＋對 LS-177 r1／r2 收據放行 → test→main tag v0.13.2）；票進 QA。R2 informational／process 三則入池 LS-96 `29b413bb`。**

### 2026-09-05T03:29:00.375Z · Chih-Lin Yeh · `723b5d9f`

**2026-09-05 11:28 orchestrator — back-merge PR #299 已併入 development 並晉升 test `103d9d4`（main ⊂ test）；派 delta QA（33＋52 自測、gate 對既有收據不誤紅、離線合成樹四項抽驗、cross_parent 新舊等價）；PASS 後 test→main、tag v0.13.2。LS-177 R3 自此可出六支收據（待使用者 `/mcp` 重連）。**

### 2026-09-05T03:42:00.423Z · Chih-Lin Yeh · `2db41e25`

## QA delta — **PASS**（test head `103d9d403a5791f53db00bfc2f89802f922d9ff4`，2026-09-05 11:41 CST）

worktree `.claude/worktrees/qa-test` detach 到 `103d9d4`（＝`origin/test`）驗收，全程未碰 Pen／容器／模擬器（純 node／bash／git，符合票文硬限制）。

**1. 增量範圍**：`git diff --stat 0bdf667..103d9d4` — 恰 8 檔（`scripts/design/overflow-scan.js`／`.test.js`、`scripts/gates/design-evidence-check.sh`／`.test.sh`、`.claude/agents/ui-designer.md`／`visual-reviewer.md`、`CLAUDE.md`、`docs/COLLABORATION.md`），無例外檔案。✓

**2. 自測**：`node scripts/design/overflow-scan.test.js` **33 組全綠**；`/bin/bash`（3.2.57）`scripts/gates/design-evidence-check.test.sh` **52 組全綠**（`grep -c '^✓'`＝52，exit 0）。✓

**3. gate 對既有收據不誤紅**：
- 端到端重現 merge-reviewer 手法——sandbox `git clone --shared`，checkout `origin/feature/LS-177-comments-reactions-design`（`33b43b6`），覆蓋本 PR 的 `design-evidence-check.sh`，跑 `--ticket LS-177 --base origin/development` → **exit 0**，r1（`fdd5617`）／r2（`5f52b03`）皆印「尚無第六支…LS-185 新欄位不要求——舊收據放行」＋綠（過程中發現 sandbox 繼承的本機 `refs/heads/development` 是 8/25 舊分支、需另外 fetch 真正的 `origin/development` 才能拿到正確 merge-base——已修正，非 LS-185 缺陷）。
- test head 上 `design/evidence/*.json` 現有 36 份收據（LS-119/120/133/142/152/163，共 6 票）：直接把 LS-185 新增的 schema 檢查邏輯（sixth marker cutoff／`scan_scope`／`board_clip` 驗證，逐行核對過與 `git diff` 確認這段是 LS-185 唯一動到的檢查邏輯、與「本 PR 觸碰板」的既有邏輯無關）對每份收據的 `head_sha` 直接跑一遍 → **36/36 通過**（sixth marker 在其歷史 head_sha 一律不存在，正確落入「舊收據放行」分支，無誤紅）。✓

**4. 離線合成樹抽驗**（`require` 純函數，繞開 Pencil）：
- (a) clip 板、帶子節點的 image 節點框底伸出板外 160 → `board_clip` 恰 1 筆 `{node:"PHOTO", side:"bottom", overflow_px:160}`。PASS
- (b) 同節點、板 `clip:false` → 0 筆。PASS
- (c) `scanScope:"boards"` 搭配指向不存在板名的 `boards` → throw（`"限縮範圍是空的…不得默默印全零收據"`）。PASS
- (d) `cross_parent_collision` 新舊等價：直接用 `git show 0bdf667:scripts/design/overflow-scan.js`（真實舊版，非測試檔內的手抄 `legacyCrossParent`）對照新版，**3 個全新 seed**（101/202/303，未出現在 checked-in 測試中）、每個 seed 各測預設與 `crossAll` 兩種 opts，共 6 組 JSON.stringify 逐位元相同（flagged 數 27/217/91/338/15/185）。PASS

**5. 文件**：ui-designer.md（:26 提「六支」＋:32-33 新增第 6 項＋`SCAN_SCOPE` 說明）、visual-reviewer.md（:35 六支敘述含 `board_clip`／:56 重掃比對六支）、COLLABORATION.md §7（新增列 259「收據第六支 `board_clip`」＋257/258 同步引用 LS-185）、CLAUDE.md:12（「六支掃描」＋`scan_scope` 標明 boards｜document）皆確認在場、內容具體對應票文範圍。✓

**6. 回歸 CI**：`gh run list --branch test` 找到 PR #299（即 `103d9d4`）觸發的 run `33941924070`——`rules`／`db`／`lint` 原已綠，`ci` 於本次驗收期間跑完，`gh run watch --exit-status` 確認**四項全 success**，`head_sha` 核對＝`103d9d403a5791f53db00bfc2f89802f922d9ff4`。✓

**未驗證**：無（票文範圍純 harness／離線，六項驗收點全覆蓋；真實 Pen 上 `board_clip`／`scan_scope` 的第一份數字依票文交下一張設計票 LS-177 R3，非本票驗收範圍）。

**風險／備註**：無新增風險；merge-reviewer R1/R2 兩輪 informational 已入池 LS-96，不影響本次裁決。

**產出位置**：worktree `/Users/clyeh/little-sprout/.claude/worktrees/qa-test`（detached `103d9d4`，clean）；驗證腳本暫存於 scratchpad `LS-185-qa-*`（`-probe-abc.js`／`-probe-equiv.js`／`-overflow-scan-OLD.js`／`-check-legacy.py`）。

### 2026-09-05T03:54:56.766Z · Chih-Lin Yeh · `edced2d0`

## dead-code sweep — LS-185

**範圍**：`git diff --stat 0bdf667..103d9d4`（8 檔：`scripts/design/overflow-scan.js`／`.test.js`、`scripts/gates/design-evidence-check.sh`／`.test.sh`、`.claude/agents/ui-designer.md`／`visual-reviewer.md`、`docs/COLLABORATION.md`、`CLAUDE.md`）。已讀票文＋merge-review R1 `d87d4334`／R2 `b68fa0bd`／QA `2db41e25`。主 checkout 唯讀，未跑 pen-*／未用 mcp__pencil__*。

### Findings（本 feature 引入）— **0 件**

逐項核對票文列出的懷疑點，皆未發現殘留：

1. **`cross_parent_collision` 候選過濾**（`overflow-scan.js:223-270`）：舊的內嵌雙迴圈已完全被 `check(i,j)` 閉包＋新的候選/非候選分派迴圈取代，沒有殘留的舊迴圈或死變數；`chain`／`byId`／`coveredByParent` 皆在新結構中被引用。新增的 `module.exports` 項 `pairEntry`（原本是模組內部函式）只為了測試檔 `overflow-scan.test.js:602-624` 的 `legacyCrossParent`（照抄 `git show 0bdf667` 舊實作）等價驗證而導出，該函式在 `(c) 候選過濾等價` 與 `(c) 候選過濾效能` 兩組測試都被呼叫（`overflow-scan.test.js:641/666-671`）——不是孤兒匯出。
   - 驗證指令：`grep -n "pairEntry" scripts/design/overflow-scan.js scripts/design/overflow-scan.test.js` → 3 處定義/呼叫，1 處測試呼叫（`M.pairEntry`），無未引用匯出。
2. **`scanBoardClip`**（`overflow-scan.js:417-453`）：簽名 `(nodes, idx, opts)` 三參數皆有使用路徑（`nodes` 僅在 `idx` 缺省時餵給 `buildIndex`，與其餘五支同慣例）；解構出的 `byId`／`liveNodes`／`kids`／`chain`／`roots` 全部被讀取（`roots` 傳給 `resolveBoards`）；沒有 unreachable 分支——`isLeaf` 判斷、`OVERLAY_RE` 子樹排除、中間 clip 祖先交集、四邊溢出量測，每段都有對應 fixture 命中（`overflow-scan.test.js` 板 `KA`～`KG`，7 組測試逐一覆蓋各分支包含 0 命中的負控案）。
3. **測試檔「舊實作」是否只服務等價測試**：`legacyCrossParent`（`:602-624`）與 `synthBoard`（`:626-638`）只在 `(c) 候選過濾等價`／`(c) 候選過濾效能` 兩個 `ok(...)` 區塊內被呼叫，未被其他測試或正式程式引用——符合票文範圍第 4 項「等價證明」用途，非殘留鷹架。
4. **gate 新舊 schema 分支可達性**（`design-evidence-check.sh:460-488`）：追過 `sixth`／`sixth_at_sha`／`bc`／`scan_scope` 的 if/elif 布林邏輯——`scan_scope is None and not sixth: pass` 之後的 `elif scan_scope is None`（等價於 `sixth and scan_scope is None`）與 `elif scan_scope not in SCAN_SCOPES`（涵蓋「有值就驗」，與 `sixth` 無關）三支互斥且都可達，`board_clip` 的 `bc is None`／`isinstance` 檢查／`flagged` 非空三支同構、皆可達。`has_fifth(rev)` 目前只是 `has_marker(rev, FIFTH_MARKER)` 的一行 wrapper（`:254-255`）——仍在兩處被呼叫（`:312-313`），不是死函式，只是風格上可以直接內聯（不算 dead code，未列 finding）。
5. **測試檔 `mode` case 覆蓋**（`design-evidence-check.test.sh` 的 `write_receipt6`）：`ok / boards / nobc / badscope / bcflag / noscope / legacy5 / badscanscope` 八種全部在 ㉞～㊶ 至少被使用一次（`ok`→㉞／㊵-b，`boards`→㉞-b，`nobc`→㉟，`badscope`→㊱／㊴-b，`bcflag`→㊲，`noscope`→㊳，`legacy5`→㊴／㊵，`badscanscope`→㊶），無孤兒分支。
6. **文件「五支」殘句（本 PR 觸碰的行）**：`ui-designer.md:33/52/88`、`visual-reviewer.md:35/56`、`COLLABORATION.md:257/259`、`CLAUDE.md:12` 這些被本 diff 實際改到的行，全部正確寫「六支」／列出 `board_clip`，內部一致，沒有本 PR 自己造成的新殘句。

**獨立複驗**（不只讀程式，實際重跑）：`node scripts/design/overflow-scan.test.js` → 33 組全綠；`bash scripts/gates/design-evidence-check.test.sh` → 全數通過。與 QA delta `2db41e25` 結論一致。

### 既有死碼觀察（非本 feature 引入，供 orchestrator 參考）

用 `git blame` 確認以下「N支」總數殘句的最後修改時間都早於 LS-185（103d9d4 未觸碰這些行，非本票造成，但因 LS-185 新增第六支而更加過時）：

| 位置 | 內容 | 最後修改 |
|---|---|---|
| `.claude/agents/visual-reviewer.md:11` | 「…截圖與**五支**掃描都做不了」（總數宣稱） | `371dbfcb`（LS-180，2026-09-05 06:17，早於 LS-185） |
| `.claude/agents/visual-reviewer.md:45` | 聚焦輪條款「…要求全樹溢出掃描（上方**四支**，正典腳本）重跑一次」 | `e04312d2`（LS-122 尾聲，2026-09-02，比 LS-168 加第五支還早，已經跳過一次未同步） |
| `docs/COLLABORATION.md:110` | Design gate 檢查項「…FLAGGED 每筆有分類、**五支**掃描皆有輸出且 corner_anchor.mismatch=0…」 | `e91fb7df`（2026-09-04，早於 LS-185） |
| `docs/COLLABORATION.md:307` | 「Pencil MCP 在 session 內斷線不重連，**五支**掃描／截圖全不可用」 | `371dbfcb`（LS-180，同上） |
| `scripts/gates/design-evidence-check.sh:498` | 成功訊息字串 `schema = "…四支掃描皆有輸出…"`（僅收工列印用，不影響紅綠判定） | `fe2b4920`（2026-09-02，早於 LS-168／LS-185） |

另有 `.github/workflows/ci.yml:195`／`:394` 註解仍寫「四支」——這項**已由 merge-review R1 `d87d4334` informational-2 抓到並經 orchestrator 裁定入池 LS-96**（dispatch `5733e44d`），此處僅交叉確認、不重複列 finding。

`docs/API.md`／`docs/store/review-notes.md` 也命中「四支」關鍵字搜尋，但核對後是 OTP／RLS 集合函式的無關語境（假陽性），不是掃描計數殘句。

### 建議處置

- **LS-185 本身：0 finding，無需開票、無需在下一張相關票順手改**——merge-review 兩輪 mutation／equivalence 測試已把票文列舉的每個懷疑點釘死，本次獨立巡檢重跑得到同一結論。
- **既有觀察（5 項「N支」殘句）**：建議 orchestrator 判斷是否併入既有 LS-96 池的 ci.yml 那條（同類問題：文件字面數字跟不上掃描支數增長，`四→五→六` 演進中至少漏改 3 次），開一張小 chore 票一次性地把 repo 內所有「N支」總數宣稱改成「見 `overflow-scan.js` 檔頭」這種指回正典腳本的寫法，避免下一次加第七支時重蹈覆轍；或維持現狀，留待下次真的動到這些行時順手帶過。兩者皆可，非阻塞。

### 搜尋方法與盲區聲明

- 純文字方法：`git diff --stat` 鎖定範圍 → 逐檔 `git diff` 讀增量 → 對懷疑點用 `grep -n`／`git blame`／`git log -S` 交叉核對「這行是不是本 PR 改的」→ 獨立重跑兩支自測腳本驗證行為未壞。
- 盲區：（1）純文字搜尋抓不到 Pencil 端的 reflection／動態欄位存取（如 `n.fill` 的實際 runtime 形狀）——這部分兩輪 merge-review 已誠實揭露「未碰活 Pen」，本次巡檢同樣不碰、不重複驗證執行期行為；（2）`git blame` 只能告訴我「這一行文字最後何時改」，不能證明「這行邏輯上是否仍被需要」——五項既有觀察是純文件字串，人工讀過確認語意過時，不是機械判定；（3）未掃描 `.claude/worktrees/` 下其他票的殘留（不在本票範圍，且那些是獨立 worktree 的活動狀態，不應由本次唯讀巡檢碰觸）。


### 2026-09-05T04:01:32.107Z · Chih-Lin Yeh · `9b828c5c`

**orchestrator 2026-09-05 12:0x 收尾儀式 → Done**

- sweeper `edced2d0`：本票引入 0 件死碼；既有「四支／五支」殘句 5 處（visual-reviewer.md:11／:45、COLLABORATION.md:110／:307、design-evidence-check.sh:498）＋ci.yml:195／:394 → 併入 LS-96 池項一條（見 LS-96 新 comment），下次動到即改成指回 `overflow-scan.js` 檔頭寫法。
- lesson learning：(1) 實作者用 `pr-body-check.sh … | tail -1` 吞掉 exit code、且「已修」行的 comment id 被當 SHA → 兩者已成 LS-186（紅時末行 ✗＋候選只認緊鄰 SHA；reviewer R1 建議改「已修之後同行第一個 hex」，採納）；(2) 收據總數字面散在五處文件，每加一支就過期一次 → 池項統一改指向腳本檔頭，不再寫死數字；(3) CI db job 54322 port flake 兩次 → LS-186 重試腳本。
- cleanup：worktree／本機分支已於 11:07 `cleanup-merged.sh --apply LS-185` 移除；已發布 v0.13.2（`103d9d4`）。

