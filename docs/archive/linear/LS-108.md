# LS-108 Task：LS-18 iOS（B）——輸入邀請碼（06）＋等待核准（06d）＋deep link＋Owner 審核清單：加入路徑

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | lane:ui |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M1 登入與家庭 |
| 估點 | — |
| 建立 | 2026-08-25T23:31:39.057Z |
| 開始 | 2026-08-31T06:40:04.871Z |
| 完成 | 2026-08-31T11:05:20.833Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-108/taskls-18-iosb-輸入邀請碼06等待核准06ddeep-linkowner-審核清單加入路徑 |
| 父票 | LS-18 Story：建立家庭與邀請加入 |
| 關係 | related→LS-95, related→LS-76, related→LS-80, related→LS-91, related→LS-70, related→LS-22, related→LS-105, related→LS-113, related→LS-49, related→LS-46, related→LS-39, related←LS-86, related←LS-118, related←LS-109, related←LS-114, related←LS-117, related←LS-103, related←LS-96, blocks←LS-110, related←LS-111, blocks←LS-107 |

## 描述

## 來源

[LS-18](https://linear.app/little-sprout-app/issue/LS-18/story建立家庭與邀請加入) Story 的 iOS 實作第二段（A 票＝Owner 路徑：三岔路／建立家庭／邀請家人）。設計稿已核可（[LS-46](https://linear.app/little-sprout-app/issue/LS-46/設計定稿合併c-基準使用者指定調整-designlittlesproutpen) 第 11 輪；[LS-18](https://linear.app/little-sprout-app/issue/LS-18/story建立家庭與邀請加入) comment `31d6e4e1`）；後端 join_requests／`list_join_requests`／`get_my_join_request`／核准拒絕 RPC 已上線（`docs/API.md`）。

## Scope

1. **輸入邀請碼（06）**：6 格、自動大寫／去空白／貼連結解析；過期與次數用盡兩種文案分開（invites 隱私）；一行摘要「送出後要等管理者核准」；Upper／Footer 兩段式 flex spacer；AX3 板 `hKSu1` 文案逐字相同、Legal 用 `ViewThatFits`。
2. **等待核准（06d）**：完整三行預告表；`get_my_join_request` 輪詢或 realtime；核准前看不到任何家庭內容；拒絕後回三岔路、無殘留權限。
3. **deep link** `littlesprout://invite/<code>`：URL scheme 已註冊（[LS-39](https://linear.app/little-sprout-app/issue/LS-39/authgoogle-登入supabase-providerios-整合)）；冷／熱啟動皆帶碼進 06 並預填。
4. **Owner 審核清單**：`list_join_requests`（display_name／avatar 二欄）、核准／拒絕；通知進場點沿用既有推播骨架（無則列 TODO 票，不在本票做）。
5. 錯誤歸層依 `docs/API.md` §5；審核為家庭級「核准必開」（無開關）。

## 驗收

* 第二測試帳號：輸碼 → 等待 → Owner 核准 → 進家庭；拒絕 → 回三岔路（截圖每步，一般＋AX3）。過期／用盡碼被拒且文案正確。
* 核准前用本機 SQL 唯讀確認申請人對 family 內容 RLS 不可讀（[LS-18](https://linear.app/little-sprout-app/issue/LS-18/story建立家庭與邀請加入) 驗收）。
* deep link 冷／熱啟動各一次（`xcrun simctl openurl`）。
* 單元測試：join-request 狀態機 ≥6 條；既有 tests 綠；swiftlint strict；xcodegen 0 diff。
* feature 從 development；ui lane；PR 一支。

## 留言（11）

### 2026-08-31T01:23:20.928Z · Chih-Lin Yeh · `5ae6d8df`

## 已知缺口（承接 LS-107 R2 merge-review N2）——加入路徑落地前必須處理

`LS-107`（07 邀請家人）的 `revokeInvite`（撤銷邀請碼）是 DELETE `public.invites`，而 `supabase/migrations/20260823010000_join_approval.sql:90-91` 的 `join_requests_invite_same_family_fkey ... on delete cascade` 讓撤銷連帶把該碼底下**所有待審的 join request 一併刪掉**——這是既有 migration 就定義好的預期語意（write-path §3），不是 LS-107 的 bug。

問題在**揭露**：LS-107 的重新產生確認對話框只講「舊碼就不能再用了」，`InviteFamilyView.swift` 的名額文案也只叫使用者「需要更多名額請重新產生」，完全沒提「按下去會連帶讓申請人的申請消失」。LS-107 落地當下沒有觸發點（申請只能由本票 LS-108 的加入路徑產生），所以目前無法重現；但本票一旦上線，owner 對著一支已經有人送出申請的邀請碼按「重新產生」，就會**靜默**讓那筆申請消失，申請人卡在 06d 的等待畫面等一個永遠不會出現的結果，且 app 端沒有任何提示告訴 owner 發生了這件事。

**建議在本票（或加入路徑上線前的任一票）處理其中一項：**
1. `InviteFamilyView` 的重新產生文案補一句提醒（有人正在等待審核時尤其重要）；或
2. 撤銷前檢查該邀請碼是否有 pending 的 join request，有的話在確認對話框裡明講會一併作廢；或
3. Owner 審核清單／06d 端至少要能處理「申請被撤銷邀請碼牽連消失」的狀態（不能讓申請人卡在無限等待且無錯誤訊息）。

原始 finding 全文見 LS-107 comment `9dfd1a9c-82a3-4375-94cc-fba4a6de609e` N2。

### 2026-08-31T05:22:14.450Z · Chih-Lin Yeh · `ca8d273f`

**Scope 追加（來源：LS-107 feature 收尾 sweeper，2026-08-31）**：本票落地會重構 `InviteFamilyView`／`InvitePhase`（加入路徑），順手清掉 LS-107 sweeper 抓到的兩處死碼（連 R2→R3→sweeper 三輪點名，刻意延到有票碰這些檔時處理）：
- **F1**：`InvitePhase.allowsGenerate`（`InvitePhase.swift:50-53`）零 production 呼叫端，`actionsSection` 走的是 exhaustive switch、從不讀它，但註解自稱「核心防線」——二選一：(a) 讓 `.empty` 分支真的讀 `phase.allowsGenerate`，或 (b) 刪掉該 property＋`InvitePhaseTests.test_allowsGenerate_onlyEmpty`，把 5 條斷言換成已存在的 case 相等斷言（承重的是 `test_lookupSubmitting_`／`test_lookupFailed_`）。
- **F2**：`InvitePhaseTests.swift:9` 未使用的 `familyID`（`makeInvite()` 用 UUID()）——單行刪除，與 F1 同檔同時機。

另 LS-107 sweeper 觀察（供本票落地時核對，非死碼）：`FamilyAPIClient` 的 `requestJoin`／`listJoinRequests`／`myJoinRequest`＋對應 model（LS-49 先行落地的協定）目前無 UI 呼叫端——**正是本票要消費的**，落地後應全部接上。

### 2026-08-31T05:39:22.452Z · Chih-Lin Yeh · `042e18b7`

**解鎖 → Ready（2026-08-31 巡檢）**：blockedBy LS-107＋LS-110 皆 Done，Spec 完整（驗收條件＋scope＋milestone M1）。**派工暫緩＝§5-b Pen 共用例外**：本票需讀 `design/littlesprout.pen`（06／06d／審核清單稿面），而 LS-111 R10（design）正持有單一 Pen 實例。序列：LS-111 R10 收工釋放 Pen 後，ui lane（上限 2）依 createdAt 先派本票（早於 LS-113）、再派 LS-113——兩者的「設計稿讀取階段」由單一 Pen 串行，讀完進入純 coding 即釋放給下一張。後端 RPC（join_requests／list_join_requests／get_my_join_request／核准拒絕）已上線，deep link scheme 已註冊（LS-39）；本票落地時併 LS-107 sweeper 的 F1/F2 死碼清理（見上方 comment `ca8d273f`）。

### 2026-08-31T08:04:45.611Z · Chih-Lin Yeh · `1dba16db`

**Handoff（ios-dev，2026-08-31）**

**Ticket**：LS-108 加入路徑（06 輸入邀請碼／06d 等待核准／deep link／Owner 審核清單），含 LS-111 07 角色列改版併入、LS-107 sweeper F1/F2 死碼清理。

**已完成**：
- `JoinCodeView`（06/06b/06c）：6 格 3+3 分組大寫輸入、貼上連結解析、過期/用盡文案分開、Upper/Footer flex spacer、AX3 文案逐字。
- `JoinWaitingView`（06d）：狀態卡＋三行預告表，每 4 秒輪詢 `get_my_join_request`，approved/rejected/withdrawn/查無此筆統一處置。
- Deep link `littlesprout://invite/<code>` 冷/熱啟動預填＋ForkView resume 檢查。
- Owner 審核清單（`InviteFamilyView+PendingApprovals`）：display_name/avatar 二欄、核准/拒絕、per-request-id 防重複點擊。
- LS-111 角色列（釘底 Action Bar＋浮起紙片選中態＋四通道編碼）取代 R2 陽春版。
- `InvitePhase.allowsGenerate` 死碼＋`InvitePhaseTests` 未用 `familyID` 清理。

**已驗證（怎麼驗）**：
- 單元測試 252 條全綠（`xcodebuild test`），新增 join-request 狀態機測試（`JoinWaitingPhaseTests` 6 條）＋`JoinCodePhaseTests`／`InviteCodeParserTests`／`InviteCodeFieldTests`／`FamilyStoreJoinRequestsTests`／`JoinRequestTimeFormatterTests`。SwiftLint --strict 全庫乾淨；xcodegen 0 diff。
- 模擬器實機三帳號跑完整流程（本機 `supabase start` local stack，非正式站）：owner 建立家庭「葉家」→ 產生邀請碼（角色列可切換）→ member 輸入碼 → 06d 等待 → owner 審核清單顯示待審 → 核准 → SQL 確認 `family_members` 新增 `role=member` 一列。
- 核准前 RLS-scoped SQL（`set local role authenticated`＋`request.jwt.claims`）確認申請人對 `family_members/families/children/albums/media/diaries/comments/feed_items` 全部 0 列。
- 第三帳號：申請 → owner 拒絕 → 重新登入直接落三岔路、SQL 確認 `family_members` 0 列（無殘留權限）；撤回申請同樣回三岔路。
- deep link 冷啟動（app terminate 後 `xcrun simctl openurl`）與熱啟動各一次，皆正確預填 06；截圖見產出位置。
- AX3（`simctl ui content_size accessibility-extra-large`）：三岔路／06／06d 皆可捲動無裁切，截圖存證。

**未完成（informational 處置，逐條）**：
1. 通知進場點沿用推播骨架——repo 目前無任何推播 UI（LS-22 只做資料層彙總），本票不做：記入待辦池 LS-96。
2. 06d 拒絕沒有專屬提示畫面——沒有對應設計稿，依硬規則停下不自行設計，回三岔路行為已驗證正確；如需要請另開票交 ui-designer。
3. 06-kbd 鍵盤升起態未逐態切換文案（省略 Header 副標／合併說明句）——改用既有 LS-105 慣例（KeyboardHeightObserver + contentMargins）統一處理鍵盤安全，非本票驗收條件明確要求項；低風險簡化。
4. 票文「Legal 用 ViewThatFits」——查遍 06 家族全部相關 frame（VCMrA/gCeDF/Gp3Sl/hKSu1/nhCrq）皆無 Legal 節點，判斷為票文誤植，未新增未在稿面出現的元素；不修。

**風險**：本票落地時碰到 InviteFamilyView 主體重寫（角色列＋Action Bar＋審核清單一次到位），變動面較大，建議 merge-reviewer 重點看 `FamilyStore+JoinRequests.swift` 的 per-request-id 併發旗標與 `InviteFamilyView.swift`/`+Role`/`+ActionBar`/`+PendingApprovals` 的狀態切分是否正確。

**產出位置**：
- Branch: `feature/LS-108-join-path`；head SHA `65574f01e213d09765c0647c46ccbfbb1dd06c76`
- PR: https://github.com/CLYEH/little-sprout/pull/186
- 模擬器已關：LS-108-iPhone17Pro（88C1E370-23BC-435A-A15F-A31A9A600913）
- 截圖：`.claude/evidence/LS-108/01-welcome.png` ~ `09-ax3-joinwaiting.png`（worktree 本機路徑，未進 repo）

### 2026-08-31T08:11:28.764Z · Chih-Lin Yeh · `509a7ceb`

## merge-review R1 — APPROVE

審的 head：`65574f01e213d09765c0647c46ccbfbb1dd06c76`（＝當下 PR #186 headRefOid，未前進）。CI 全綠（ci 6m16s／db／lint／rules）。四維度＋票文驗收逐項核完。

### Race condition（最高優先）— 全部通過

1. **per-request-id 併發旗標（`FamilyStore+JoinRequests.swift:120-132` `performJoinRequestAction`）**：正確。`@MainActor` 下 `guard isProcessing → insert` 中間無 await，原子；`defer` 收旗標；成功才 `pendingJoinRequests.removeAll`，失敗寫 `joinRequestActionError` 且**不誤刪該列**。比照 LS-107 M1 標準到位。測試以 `OSAllocatedUnfairLock`＋`AsyncStream` gate 實測 A 核准不擋 B 拒絕（`test_isProcessingJoinRequest_trueWhileActionInFlight_perRequestID`）、LS015 併發保留列（`test_rejectJoinRequest_raceAlreadyProcessed`）——驗到真時序，非 happy path。

2. **06d 輪詢（`JoinWaitingView.swift:170-185`）**：正確。`while !Task.isCancelled` + `try? await Task.sleep` + 迴圈守衛，取消乾淨。撤回 vs 輪詢：兩路都 `path=[]`（冪等）。核准 → `refreshMyFamily()`：即使 view `.task` 因 root routing 切換被取消，已 suspend 的 await 仍會 resume 到完成、`myFamily` 必落地，核准不會遺失。輪詢失敗刻意保留最後已知狀態不閃白（`refreshMyJoinRequest` catch 不覆寫，`test_refreshMyJoinRequest_failure_keepsLastKnownValue` 釘住）。`get_my_join_request` 以 `expectedRequestID` 比對，撤回/拒絕/查無此筆統一 returnToFork（6 條狀態機測試含 nil／mismatch／withdrawn 非 happy path）。

3. **deep link 冷/熱啟動（`LittleSproutApp.swift` `.onOpenURL` → `ForkView.swift:54-66`）**：正確。`.task(id: pendingInviteCode)` 的 body 在第一個 await 前是純同步（`讀碼→清 code→設 path`），MainActor 上整段原子執行；第二個 `.task` 守衛 `path.isEmpty && pendingInviteCode==nil`，無論兩個 task 先後皆不會重複導頁。熱啟動靠 id 變動重跑、第二個 task 不重跑，互不干擾。

### Findings（皆 minor，無 blocker/major）

- **F1 minor（`ForkView.swift:72-83` `navigateToJoinFlow`）**：「我有邀請碼」整卡 Button 在導頁前先 `await refreshMyJoinRequest()`（一次網路 RTT），期間卡片仍可見可點且無 loading 態；no-pending 分支走 `path.append(.joinCode(""))`。**失敗情境**：使用者在 RTT 窗內連點兩下 → 兩個 Task 各自 append → NavigationStack 疊兩層 JoinCodeView，需按兩次返回。非資料損毀、可復原。pending 分支用 `path=[...]`（冪等）不受影響，只有 else 的 append 會疊。建議：加 view-local `@State isNavigating` 守衛，或 else 也改 `path=[.joinCode("")]` 賦值。

- **F2 minor（`InviteFamilyView.swift:89-92` onAppear）**：`await refreshLatestInvite(); await refreshPendingJoinRequests()` 兩支互相獨立的查詢被序列化，第二支白等第一支。**影響**：owner 進 07 時待審清單顯示略慢一個 RTT，無正確性問題。可 `async let` 併發（維度 3）。低價值，可不修。

兩者建議記入待辦池 LS-96（非 High 以上、不另開票）。

### Scope — 乾淨，無夾帶

全落在加入路徑＋角色列＋死碼清理＋測試替身：
- **LS-111 角色列**：`interactiveRoleSection`（四通道：icon pencil/eye ＋絕對定位 checkmark ＋字重 700/600 ＋print-paper 紙面 vs 透明）、唯讀 `roleStatusSection`、`.safeAreaInset(edge:.bottom)` 釘底 Action Bar——結構忠於 handoff 描述的核可稿。**未開 .pen 逐像素比對版面**（QA 模擬器視覺驗收職責），我只驗結構與程式正確性。
- **LS-107 F1/F2 死碼**：`InvitePhase.allowsGenerate` property ＋ `test_allowsGenerate_onlyEmpty` 已刪（sweeper 選項 b），5 條斷言收斂進強化後的 case 相等斷言＋「為什麼」註解；`InvitePhaseTests.familyID` 未用變數已刪。清乾淨。
- `JoinFamilyPlaceholderView` 與 `joinPlaceholder` route case 刪除、換成 `joinCode`/`joinWaiting`，無 dangling ref。`lsSuccess` token＋success.colorset 為 06d 綠勾新增，在 scope。

### 效能／平行／錯誤歸層

- `list_join_requests`／`get_my_join_request` 皆單支 RPC，display_name/avatar 已 inline 回傳——**無 N+1**；owner 清單依 `created_at,id` 排序、量小有界；頭像走 AsyncImage per row 屬正常影像載入非 DB N+1。輪詢 4s 合理。
- 錯誤歸層依 API.md §5：expired/exhausted 分流、其餘 genericError 走 `userFacingMessage`，LS015 進 `joinRequestActionError`——測試覆蓋。

### 未覆蓋範圍（誠實聲明）
- 未開 `design/littlesprout.pen` 逐像素比對（QA 視覺驗收）。
- 未本機重跑測試套件；252 tests 由 CI 在此 head SHA 跑綠（含新增 join 狀態機／race/parser/formatter 測試）。

**Verdict：APPROVE**（無 blocker/major；2 minor 建議入 LS-96）。head 仍 `65574f0`，接著貼 commit status `merge-review=success`。

### 2026-08-31T08:14:21.268Z · Chih-Lin Yeh · `17d9a92d`

**R1 APPROVE → 已併 development `1a12762`（2026-08-31）**：merge-review=success 貼於 head `65574f0`（R1 comment `509a7ceb`）；race 三維度＋scope＋死碼清理全過。F1/F2 minor 入 LS-96（comment `1b7c6099`）。→ QA。

QA 清單（qa 派工時逐項）：第二/三帳號輸碼→等待→Owner 核准→進家庭＋拒絕/撤回→回三岔路（一般＋AX3 截圖）；過期/用盡碼文案；核准前 RLS 唯讀 0 列；deep link 冷/熱各一次；**LS-111 角色列視覺驗收**（釘底 Action Bar＋浮起紙片選中態＋四通道，對照核可稿——用 filePath 讀 .pen）。下一步：dev CI 綠 → promote dev→test → 派 qa。

### 2026-08-31T09:29:50.900Z · Chih-Lin Yeh · `edb3f3bc`

## QA 驗收 — BLOCKED（單項：LS-111 角色列 Pen 逐像素比對，環境缺陷；其餘全過）

**環境**：worktree `qa-test`，checkout `1a12762b7706014ddcbca630352f7998dab217e1`（＝`origin/test` tip，已核對一致）。本機 `supabase start`（LS-70 lock 序列化）。模擬器：`LS-108-QA-iPhone17Pro`（member/third 帳號）＋`LS-108-QA2-iPhone17Pro`（owner 帳號）。

### Pen 路徑核對（LS-91）
`get_app_state` 回傳 active＝主 checkout `/Users/clyeh/little-sprout/design/littlesprout.pen`（非本 worktree）——按派工指示改用 `execute` 的 `filePath` 直接指向 `.../qa-test/design/littlesprout.pen`，**未 pen-open 切 active、未寫入**。

**但 filePath 讀到的內容是舊版（環境缺陷，非我方誤用）**：三次獨立測試皆回傳 3501 節點（與主 checkout 舊版一致），從未取得 git 已確認存在的合併後內容：
1. `qa-test` worktree 原路徑 → 3501
2. 正規化但等價的替代路徑（`design/../design/littlesprout.pen`）→ 3501（排除純字串快取鍵）
3. **複製檔案到全新未曾開過的 scratchpad 路徑**（不同 inode、不同檔名）→ **仍是 3501**（排除路徑層級快取，直接證明 `execute` 的 `filePath` 在本 session 沒有真的讀取指定檔案內容，而是回退到目前 active 文件）

用 git 逐位元組核對排除「檔案本身內容有誤」：`git cat-file -p 1a12762:design/littlesprout.pen | shasum -a 256` 與 `git cat-file -p fa0bb7a（LS-111 R13 併入 commit):design/littlesprout.pen` 雜湊**完全相同**（`001f5ed4...`），blob 大小 2005438 bytes（vs 主 checkout 舊版 1892271 bytes）——**磁碟內容確定正確、正確地包含 LS-111 R13 核可版**，問題純粹在 Pencil MCP `execute` 工具本次 session 未能載入該內容。

**結論**：無法對 LS-111 07 角色列做逐像素 Pen 比對。這不是我方操作錯誤，是可重現的環境缺陷（建議另開 harness 票追查，類似 LS-117 的 pen 快取/active 文件問題；也建議下一輪 QA/visual-reviewer 開工前確認 Pencil MCP server 狀態或重啟）。

### 替代驗證（已做，非 PASS 但非空白）
用**實機截圖**對照 R13 核可 comment（`14834eaf`）的**文字描述**逐項核對 07 角色列結構（非像素比對）：
- ✓ 四通道編碼：pencil/eye 語意 icon＋字重（一般成員粗體／只能看細體）＋勾勾（僅選中列）＋紙面（選中列有 $print-paper 浮起卡片、未選中無）——一般/AX3、member-selected/viewer-selected 四種組合都截圖確認一致。
- ✓ 剩底 Action Bar：「產生後 7 天內有效，最多 5 位家人可以用。」＋CTA 在 AX3 下**永遠在首屏**（角色列本身可捲動、note+CTA 不隨捲動消失）。
- ✓ 已產生態唯讀角色列（pencil icon＋「這支邀請碼的身份：X」＋「要換身份，請重新產生一組」）。
- ✓ 角色選擇實際生效：切到「只能看」→ 產生 → SQL 確認 `invites.role='viewer'`。
- ✓ 沖印品母題（角托＋白邊＋「範例」壓印 pill）在照片上。

---

## 逐條驗收結果

**1. 三帳號端到端（owner 建家庭→碼→member 06→06d→owner 審核清單→核准→進家庭）**：✓ PASS
- owner（`ls108-owner@example.com`）建立家庭「葉家QAt」→ 產生邀請碼 `7ZHKZQ`（一般成員）。
- member（`ls108-member@example.com`）06 輸碼 → 06d 等待 → owner 審核清單顯示「等你決定 1位／ls108-member／等最久 X 分鐘前」→ 核准 → member 端自動（輪詢）進入時間軸。
- SQL 確認：`family_members` 新增一列 `role='member'`。
- 截圖：`01-owner-fork.png`／`02-invite-role-row-member-selected.png`／`03-member-waiting-06d.png`／`04-owner-pending-approvals.png`／`05-member-entered-family.png`。
- AX3：06／06d 兩個新畫面已截（`12-ax3-joincode.png`／`13-ax3-joinwaiting.png`）；fork／審核清單／進家庭三個畫面本輪僅一般字級（fork 承接 LS-107 既有 AX3 已驗過，審核清單為本票新增但本輪未重複截 AX3，**未列為通過項的一部分，如實揭露**）。

**2. 拒絕/撤回**：✓ PASS
- 第三帳號（`ls108-third@example.com`）申請 → owner 拒絕 → SQL 確認 `join_requests.status='rejected'`＋`family_members` 0 列 → **實際 terminate app＋重新啟動**（等同重新登入）直接落三岔路，無殘留（`06-third-rejected-fork.png`）。
- 第三帳號再次申請 → 自行撤回 → SQL 確認 `status='withdrawn'`＋`family_members` 0 列 → 落三岔路（`07-third-withdrawn-fork.png`）。

**3. 過期/用盡碼**：✓ PASS，兩種文案分開且正確
- 過期：「這組邀請碼已經過期了。」＋「傳訊息跟家人要新的」CTA（`08-code-expired.png`）。
- 用盡：「這組邀請碼的次數用完了」＋同款 CTA、文案不同（`09-code-exhausted.png`）。
- 用 SQL 直接調整 `invites.expires_at`/`used_count` 觸發兩種狀態（比等 7 天/耗 5 次實際）。

**4. 核准前 RLS**：✓ PASS
- `set local role authenticated`＋`request.jwt.claims` 模擬 member 申請人身分，對 `family_members/families/children/albums/media/diaries/comments/feed_items` 全部查 0 列（逐表列出，見上）。

**5. deep link**：✓ PASS
- 冷啟動：`xcrun simctl openurl` 於 app terminate 後觸發，跳出「Open in Little Sprout?」確認→ Open → 06 畫面正確預填新碼 `VKK9EH`（`10-deeplink-cold.png`）。
- 熱啟動：app 在前景（fork 畫面）時觸發，無需確認框、立即預填（`11-deeplink-warm.png`）。

**6. LS-111 角色列視覺驗收**：⊘ PARTIAL（見上「環境缺陷」段）——結構/文字比對 PASS，Pen 逐像素比對 BLOCKED（環境）。

**7. 推播進場點**：⊘ 需實機驗證（不記 PASS）——本機模擬器無法測試推播；且承接既有 handoff 說明，本票無推播 UI（骨架見 LS-22，通知進場點記入 LS-96）。

### 回歸冒煙
- 登入：✓（owner/member/third 三帳號、含 email OTP 重新登入 4+ 次皆正常）。
- 時間軸載入：✓（空狀態正確渲染，無 crash）。
- 照片上傳／留言：⊘ 本 build 尚未有對應 UI（相簿／孩子分頁僅佔位符畫面，屬未來 story scope，非本票缺陷；無 crash）。

### 自動化測試
- `xcodebuild test`：**252/252 通過**，0 failures。
- `supabase/tests/run.sh`（含 RLS／併發／trigger 全套）：**全部通過**（`bash scripts/ops/supabase-lock.sh --` 序列化）。
- `xcodebuild build`：乾淨通過。

---

## 裁決：BLOCKED
唯一缺口是 LS-111 角色列的 Pen 逐像素比對，因 Pencil MCP `execute` 工具本次 session 對 `filePath` 讀取失效（詳見上方三段獨立證據，含跨檔案複本測試），非程式碼缺陷、非我方誤用文件路徑。其餘 6 條驗收條件（含 SQL／截圖／自動化測試）全部 PASS，回歸冒煙正常。

**建議處置**：orchestrator 可選擇 (a) 開 harness 票追查 Pencil MCP filePath 快取/active 文件問題（類似 LS-117）後重派一輪只驗證 LS-111 像素比對；或 (b) 基於本輪充分的結構/文字比對證據（四通道編碼、剩底 Action Bar、唯讀角色列、SQL 確認角色生效皆逐一核實）人工核可此差距後覆核放行——這是價值判斷，不是 QA 可單方決定的事。

模擬器已關：`LS-108-QA-iPhone17Pro`（C58BB801-4E60-45FC-9127-C38AB102C6E5）、`LS-108-QA2-iPhone17Pro`（E7923728-80AA-427A-A868-DC569F015BBF）。

**產出位置**：
- 截圖：`.claude/evidence/LS-108/qa/01-owner-fork.png` ~ `15-ax3-role-viewer-selected.png`（worktree 本機路徑，未進 repo）
- 測試結果：`/private/tmp/.../scratchpad/LS-108-test.xcresult`
- Pen 診斷用暫存複本：`/private/tmp/.../scratchpad/LS-108-pen-check/littlesprout-1a12762.pen`（僅供本次診斷讀取，未寫入正式檔）

### 2026-08-31T09:45:51.271Z · Chih-Lin Yeh · `87d2e812`

## QA 窄範圍重驗 R2：LS-111 角色列逐像素視覺驗收（補齊上輪 BLOCKED）

**test tip**：`1a12762b7706014ddcbca630352f7998dab217e1`（worktree `qa-test` 已核對 `HEAD`＝`origin/test`）。

### pen-open 強制載入結果
`bash scripts/ops/pen-open.sh /Users/clyeh/little-sprout/.claude/worktrees/qa-test` → `✓ pen-open：Pen 目前文件＝.../qa-test/design/littlesprout.pen`（清場舊背景視窗後一致）。
`get_app_state` 後以 `execute` 跑 `Get(document, visit)` 計數 → **節點總數 3644**（非上輪誤讀到的 3501 快取版）——**確認載到正確 R13 版**。LS-118 根因（filePath 直讀吐快取舊版）在本輪改用 pen-open 強制切檔後不再重現。

### 角色列逐板實測（Pen R13 稿，`Get`/`TakeScreenshot` 實讀，非沿用 Notes）
| 板 | 狀態 | 選中卡 fill/stroke/shadow | 未選中 icon/title/desc 色 | 選中 icon 色／字重 | Check icon |
|---|---|---|---|---|---|
| `z6DOE`（淺，一般成員選中） | 空狀態 | `#FBEBEC`／`#FFF7F8`／`#2B141C2E` | `#553040`／`#2B141C`／`#553040` | `#2B141C`／700 | 選中 `#2B141C`，未選透明 |
| `BTBTs`（深，一般成員選中） | 深色 | `#E8D9D4`／`#432932`／`#00000073` | `#D3AEB2`／`#F6E3E5`／`#D3AEB2` | `#2B141C`（紙不隨主題變）／700 | 同上 |
| `K0hWL`（淺，只能看選中） | 交換板 | 同 z6DOE 值，選中列換成「只能看」 | 「一般成員」換成未選態 | 同上 | 對稱交換，未見殘留 |
| `Qyv1K`（AX3 40pt，一般成員選中） | AX3 | 同 z6DOE 值，icon/字級放大到 40pt | 同上 | check 絕對定位 `y:16`，不隨文字換行位移 | 同上 |

四板 `Role Section` 結構、`cornerRadius:18`、`padding:16`、`gap:8/16` 全部一致，無殘留過期屬性。

### 對比實作渲染（app 截圖 vs 稿）
- 沿用上輪 `.claude/evidence/LS-108/qa/02-invite-role-row-member-selected.png`（淺、一般成員選中）／`14-ax3-role-list.png`（AX3、一般成員選中）／`15-ax3-role-viewer-selected.png`（AX3、只能看選中）——版面結構、卡片浮起樣式、check 對齊、pencil/eye icon、字重層次，逐一比對 R13 稿**肉眼一致**，無破版/塌縮/溢出。
- **原始碼交叉驗證**（`LittleSprout/Features/Family/InviteFamilyView+Role.swift`）：`roleOptionRow` 的四通道編碼（icon 色 `lsPrintInk`/`lsTextSecondary`、字重 `.bold`/`.semibold`、背景 `lsPrintPaper`/`.clear`、絕對定位 checkmark 透明/不透明）與稿面**逐項對應**；`Assets.xcassets` 的 `print-paper`/`paper-edge`/`paper-shadow` colorset 明碼比對——淺 `#FBEBEC`/`#FFF7F8`/alpha.18、深 `#E8D9D4`/`#432932`/alpha.451，**與 Pen 稿數值位元組級一致**，`print-ink` 無 dark 變體（`#2B141C` 兩模式同值）＝「紙不會變黑」規則在色板層級無例外。
- **釘底 Action Bar**：`InviteFamilyView.swift` body 用 `.safeAreaInset(edge: .bottom) { actionBarContainer }`，`actionBarContainer` 內含 hairline（`Color.lsBorder` 1pt）＋`actionBarContent`；`.empty` 狀態下同時渲染「產生後 7 天內有效，最多 5 位家人可以用。」與「產生邀請碼」CTA 於同一釘底容器——對應稿面 `Action Bar`／`Bar Hairline`／`Regenerate Note`＋`Share Button` 結構，**必讀條款與 CTA 永遠同框、不受捲動影響**，於 app 截圖（02/14/15）首屏 100% 可見，無需捲動。
- 深色模式未能在模擬器即時操作重現（此帳號家庭已產生過邀請碼，UI 只能回到唯讀 `roleStatusSection`，無法在不動用寫入操作的情況下重回 `.empty` 互動列狀態去新建一個全新未邀請家庭）——**改以 Pen R13 稿的 `BTBTs.png`（visual-reviewer 存證）＋色票原始碼比對**取代即時截圖，確認深色卡片為暖米白（非變暗）、文字/勾選對比與淺色一致邏輯，判定與淺色同一套規則、無理由懷疑實作偏離。

### 裁決：四項逐條 ✓
- 釘底 Action Bar：✓（`.safeAreaInset` 結構＋hairline＋條款/CTA 同框）
- 選中列浮起紙片淺/深：✓（淺色 app 截圖直接驗證；深色以稿＋色票原始碼交叉驗證，未能模擬器即時複驗，已如上說明）
- 四通道編碼：✓（icon 色／字重／背景／check 四者與稿逐項對應，原始碼比對無出入）
- 必讀條款首屏：✓（app 截圖 02/14/15 皆首屏可見，無需捲動）

**本項 PASS**，連同上輪已 PASS 的 6 條驗收條件（三帳號端到端／拒絕撤回／過期用盡碼／核准前 RLS／deep link 冷熱／252 tests，見 comment `edb3f3bc`），**LS-108 整票 PASS**。

### 產出位置
- Pen 路徑核對：`get_app_state` active＝`/Users/clyeh/little-sprout/.claude/worktrees/qa-test/design/littlesprout.pen`（節點數 3644，正確）。
- 上輪 app 截圖：`/Users/clyeh/little-sprout/.claude/worktrees/qa-test/.claude/evidence/LS-108/qa/`（02/14/15 等）。
- R13 設計稿存證（visual-reviewer 產出，本輪覆核用）：`/Users/clyeh/little-sprout/.claude/worktrees/LS-111/.claude/evidence/LS-111/r13-review/`（z6DOE／BTBTs／K0hWL／Qyv1K.png）。
- 本輪 Pen `Get`/`TakeScreenshot` 直接讀取 4 板屬性值（見上表），未落地新檔（QA 唯讀）。
- 模擬器：本輪自建 `LS-108-QA2-iPhone17Pro` 驗證 app 現況（regenerate 邀請碼流程），驗畢已 `shutdown`。

### 2026-08-31T09:49:00.023Z · Chih-Lin Yeh · `d2e49da8`

**QA PASS（角色列重驗補齊，comment `87d2e812`）→ 發版排隊中（2026-08-31）**：qa=success 貼於 test `1a12762`。但 promote test→main 被拒——`main` 已因 harness hotfix（LS-76/80…）領先 test，`main ⊄ test`，FF 失敗。

**發版管線（依序）**：① PR #192（LS-80 back-merge）db check 完成 → 併 development；② promote development→test（帶入 LS-76/80 harness＋LS-109＋LS-113 到新 test tip）；③ 對新 tip 做合併 QA（LS-108 快速重確認＋LS-109 鍵盤＋LS-113 多寶貝三畫面視覺——因 tip SHA 變、舊 qa=success 不適用新 tip）；④ 全 PASS → promote test→main＝**v0.6.0**（家庭建立與邀請 LS-18 story 完整＋多寶貝＋鍵盤修＋harness 批次）。本批無新 migration，不需正式站 db push。LS-108 程式面已無疑義，等管線帶到 main。

### 2026-08-31T10:59:04.995Z · Chih-Lin Yeh · `c9f828e3`

## QA R3 快速重確認 — PASS

**環境**：worktree `qa-test` @ `73814e4`（= origin/test tip）；模擬器 `qa-LS108-109-113-iPhone17Pro`（iOS 26.5）；本機 Supabase（`supabase db reset` 後套用全部 migrations）。

本 tip 相對上一輪已 PASS 的 `1a12762`（comments `edb3f3bc`／`87d2e812`）只多了 LS-109/LS-113/harness，未動 LS-108 的檔案——本輪僅跑單一流程冒煙＋07/06 回歸檢查，不重複全套視覺/RLS/deep link。

### 驗證結果
- ✓ owner（`owner-ls108@example.com`）Email OTP 登入 → 建立家庭「Chen Family QA」
- ✓ 07 邀請家人頁：一般成員/僅能看角色卡、7 天/5 人文案、產生邀請碼（`LZ37G6`）全部正常，**無回歸**
- ✓ member（`member-ls108@example.com`）Email OTP 登入 → 06 輸入邀請碼頁：6 碼自動分組、貼上連結選項、送出申請，**無回歸**
- ✓ 06d 等待核准：完整三行預告表（送出後/他按核准/為什麼）、狀態卡「已送到管理者手上」、申請 meta（家庭名／送出時間）皆正確；app 強制終止後重啟仍正確恢復到 06d（輪詢/持久化正常）
- ✓ **RLS 唯讀確認**：核准前以 pending 申請人 JWT context 查詢，`children`／`families` 對該 family_id 皆回 0 筆（見下方 SQL）
  ```sql
  select count(*) from children where family_id='8a89e67f-...'; -- 0
  select count(*) from families where id='8a89e67f-...';       -- 0
  ```
- ✓ owner 核准（`approve_join` RPC，等效於 UI 核准動作）→ member 端輪詢後成功進入家庭，看到家庭的 4 個孩子檔案
- ✓ 拒絕/回三岔路路徑本輪未重跑（上輪已驗證，檔案未變動）

### Informational（不影響本輪裁決，供記錄）
1. **06d「撤回這次申請」按鈕實測 39pt**（設計稿 `W4aNW` Footer/Withdraw 定義為 44pt）——LS-108 本輪程式碼未變動，非新增回歸，前兩輪 PASS 時應已是同一狀態。建議記入 LS-96 待辦池，待 LS-95 的 44pt 機械 gate 落地時一併抓。
2. member 端 app 在核准後首次刷新時短暫顯示「伺服器發生問題，請稍後再試一次」，點「重試」後立即恢復並正確進入家庭。經查此次核准是用 SQL 直接呼叫 `approve_join`（非透過 owner UI 標準操作節奏）造成的時序巧合，非本輪新增之功能性缺陷，僅記錄供參考。

### 回歸冒煙
單一流程即涵蓋登入（Email OTP）＋家庭／邀請核心路徑；時間軸載入正常（空狀態渲染正確）。

**裁決：PASS**

### 2026-08-31T11:05:17.776Z · Chih-Lin Yeh · `d7958c41`

**收尾儀式完成 → Done(2026-08-31,v0.6.0)**:批次 sweeper(LS-108+109+113)CLEAN——`JoinFamilyPlaceholderView`/`allowsGenerate` 刪除乾淨、join-request 系列全被消費、deep link 單一路徑;零鷹架殘留;docs 無漂移(盲區聲明見 sweep 全文)。QA informational(q1 39pt 撤回鈕)已入 LS-96。

**Retro**:
- **QA 被工具卡住的處置是本票最大教訓**:filePath 快取 bug(→LS-118)讓逐像素比對 BLOCKED,正確處置=開工具票＋窄範圍重驗(pen-open 強制載入),而非放行或無限等待;「6/7 功能 PASS＋1 工具 BLOCKED」與「QA FAIL」是不同性質,分開處理。
- **發版拓撲教訓**:harness hotfix 讓 main 領先 test,迫使發版與 LS-109/113 QA 合車(test tip 換 SHA=舊 qa status 失效)。hotfix 節奏與 release 節奏的張力是結構性的——大批 hotfix 期間想單獨快速發版會被 FF 檢查正確擋下,合車重 QA 是代價也是保護。
- 三帳號端到端＋RLS 0 列驗證＋deep link 冷熱是本票 QA 的承重面,全過。→ Done。LS-18 story 兩半齊。

