# LS-181 Harness：`migration-breaking-check` 加 `ALTER TYPE … ADD VALUE` 規則——enum 加值時自動列出消費端（EF 型別守衛／iOS enum／API.md 矩陣）並要求 PR body 逐一確認，防「不認得的 kind 整批靜默丟失」再發生（LS-175 R1-i1）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-04T22:51:21.523Z |
| 開始 | 2026-09-04T22:51:21.635Z |
| 完成 | 2026-09-05T00:46:32.514Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-181/harnessmigration-breaking-check-加-alter-type-add-value-規則-enum |
| 關係 | related→LS-172, related→LS-179, related→LS-158, related→LS-176, related→LS-23, related→LS-144, related→LS-175, related→LS-149, related→LS-96, related→LS-180, related←LS-182, related←LS-183 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項 `a6f28382`（[LS-175](https://linear.app/little-sprout-app/issue/LS-175/taskls-22-後端-media-新增彙總通知事件批次上傳-50-張一則爸爸新增了-50-張照片after-insert) merge-review R1 `b1229dac` i1，2026-09-05）：`scripts/gates/migration-breaking-check.sh` 規則 B1–B6 不含 `ALTER TYPE … ADD VALUE`，enum 加值不會被要求盤點消費端——[LS-149](https://linear.app/little-sprout-app/issue/LS-149/taskls-23-後端-檢舉封鎖owner-移除內容-rpccontent-reportsblocked-users) 加 `report` 後 `push-dispatch` 的 `isNotificationKind` 守衛沒人更新，含 report 事件的已 claim 批次會**靜默丟失**（[LS-175](https://linear.app/little-sprout-app/issue/LS-175/taskls-22-後端-media-新增彙總通知事件批次上傳-50-張一則爸爸新增了-50-張照片after-insert) 才發現並補）。同型風險：`content_target_type`、`notification_kind`、iOS 端 `Codable` enum 對未知值的解碼、API.md 文案矩陣。巡檢 06:5x lane:harness 空（[LS-180](https://linear.app/little-sprout-app/issue/LS-180/harnesspen-opensh-force-reload-以-sigkill-清場會讓-pencil-mcp-斷線且-session) 進 QA）→ 依 [LS-144](https://linear.app/little-sprout-app/issue/LS-144/harness巡檢加開票責任機械提醒-lane-在飛-0-且無可派候補時動作清單印-開票並列來源story-拆票ls-96-p1-升票) 升票。

## 範圍

1. `migration-breaking-check.sh` 新規則 B7：偵測 `alter type <t> add value` → 分類 **BREAKING（enum 加值）**，輸出該 enum 名與新值；並自動 grep 消費端候選：`supabase/functions/**/*.ts` 中出現該 enum 值集合的字面（型別守衛／union）、`LittleSprout/**/*.swift` 中同名 `enum`／`case` 字串、`docs/API.md` 中該 enum 的值表或文案矩陣——列成「消費端清單」印在 gate 輸出。
2. `breaking-section-check.sh`（PR body BREAKING 段）：B7 時要求 body 的 `BREAKING:` 段逐一列出消費端清單每一項的處置（已更新 commit SHA／不需更新＋理由）；缺一項即紅。CI `rules` job 沿用既有接線。
3. 自測 `migration-breaking-check.test.sh`／`breaking-section-check.test.sh` 補案：ADD VALUE 正樣本（列出消費端）、body 少列一項紅、全列綠；用假 EF／Swift／API.md 夾具。
4. 回填：對現有 enum（`notification_kind`／`content_target_type`）跑一次真 repo，確認守衛與矩陣現況一致（[LS-175](https://linear.app/little-sprout-app/issue/LS-175/taskls-22-後端-media-新增彙總通知事件批次上傳-50-張一則爸爸新增了-50-張照片after-insert) R2 已補 `report`），結果寫進 PR body。
5. COLLABORATION §7 對照表一列；[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) `a6f28382` 銷除註記。

## 不做

其他 DDL 類型的消費端盤點；自動修改消費端程式碼。

## 驗收

- [ ] 假 migration `add value 'x'` → gate 印 BREAKING（enum 加值）＋消費端清單；PR body 少列一項 → `breaking-section-check` 紅（自測）
- [ ] 真 repo 對兩個既有 enum 跑過、現況一致
- [ ] merge-reviewer 通過；hotfix 流程（自 main、PR 回 main、back-merge）

## 留言（14）

### 2026-09-04T22:51:36.369Z · Chih-Lin Yeh · `54333506`

**2026-09-05 06:52 orchestrator — 派工**

- lane:harness 空（LS-180 進 QA）→ 依 LS-144 升池項 `a6f28382`；cycle 1；hotfix 流程：worktree `.claude/worktrees/LS-181`，分支 `hotfix/LS-181-breaking-check-enum-add-value`（base origin/main `0c6b58b`），PR → main，併入後 back-merge。
- 派 general-purpose R1。

### 2026-09-04T23:16:50.019Z · Chih-Lin Yeh · `504bc424`

## Ticket
LS-181 R1 handoff（2026-09-05 07:15 CST）— B7 enum ADD VALUE 規則＋消費端清單＋section-check `--findings`。hotfix 自 `main`、PR 回 `main`。

## 已完成（每條附 commit SHA＋檔案:行號）
1. **B7 規則**——`39169d4`，`scripts/gates/migration-breaking-check.sh`：規則表 :49-70、輸出格式 :12-16、參數 `--known-enums`／`--consumer-root` :93-94、`enum_name`／`enum_defs` :181-205、`resolve_consumer_root` :207、`to_pascal` :215、`AWK_CODE`（ts／swift 檔級聚合）:218、`AWK_DOC`（API.md 逐行＋表格區塊）:238、`enum_consumers`（ENUM／CONSUMER 行）:262、`classify` B7 分支 :326-341、`--base` 自 base migrations 取既有值集合 :381。偵測 `alter type <t> add value [if not exists] '<v>'`（大小寫不敏感、跨行、BEFORE 子句、DO 動態 SQL）→ `BREAKING`＋`ENUM\t<schema.name>\t<新值>\t既有值…\t消費端 n 處`＋逐路徑 `CONSUMER\t<enum>\t<path>\t<命中說明＋行號>`；全無命中印「消費端：無（請人工確認）」；`ADD ATTRIBUTE`／`OWNER TO` 不誤判、`RENAME VALUE` 仍走 B5。
2. **接線**——`24bf7e8`：`scripts/gates/breaking-section-check.sh` 加 `--findings FILE`（檔頭 :13-24；路徑抽取 :50、段界 :53、缺項報告 :84）——每個 CONSUMER 路徑須在 `BREAKING:` 段（`BREAKING:` 行起到下一個標題行）同一行寫「已更新 <7–40 hex SHA>」或「不需更新＋理由」，缺一 exit 1 並點名，讀不到 exit 2；`.github/workflows/ci.yml:352-353` 把 `$findings` 落檔 `$RUNNER_TEMP/migration-findings.txt` 傳入；`scripts/gates/push-gate.sh:206` 提醒加印 ENUM／CONSUMER 行。
3. **自測**——`defe87b`：`migration-breaking-check.test.sh:177-265`（B7 18 組：假 EF／Swift／API.md 夾具、跨行、IF NOT EXISTS、大寫＋BEFORE、省略 schema、DO 動態 SQL、無消費端、無 `--known-enums`、既有值 1 個門檻降 1、三個反例、同輸入累加、`--base`、三個 fail closed；69→87）；`breaking-section-check.test.sh:43-87`（`--findings` 16 組：全列綠、括號寫法、CRLF、少列一項紅＋stderr 點名、無處置、無 SHA、6 位 SHA、無理由、只有標點、段外×2、無 BREAKING: 行、無 CONSUMER 行×2、讀不到、未知參數；13→29）。失敗分支皆 `${name}`。
4. **回填**——寫進 PR #291「回填」段：`--base 40dd1cd`（+report）／`--base bc4b592`（+family、+media）各列出 `handler.ts`／`handler.test.ts`／`docs/API.md`／`TimelineModels.swift`；人工核對 `handler.ts:38-50／62-69／142-150／195／208`、`handler.test.ts:162-185`、`docs/API.md:593／2440-2445` 現況與 LS-175 R2 `7065e3f` 一致；`TimelineModels.swift` 是 `FeedKind`（值集合重疊的多報）。
5. **COLLABORATION**——`dd0284a`：`docs/COLLABORATION.md:247` §7 新列「enum 加值必盤點消費端 ↔ B7＋section-check --findings」；`:213` §6 BREAKING 清單補 `ALTER TYPE … ADD VALUE`（超出票文「§7 一列」半句：Rule 6 不留兩份互相矛盾的規則清單）。
6. **LS-96 `a6f28382` 銷除註記**——回覆 comment `83a73237`（附 PR #291＋四個 commit SHA；i2 部分保留）。
7. PR：https://github.com/CLYEH/little-sprout/pull/291（base `main`，head `dd0284a`，4 commits）；body 過 `pr-body-check.sh` 綠後才 `gh pr create`。

## 已驗證（怎麼驗）
- `migration-breaking-check.test.sh` → **87 組全綠**；`breaking-section-check.test.sh` → **29 組全綠**；`agent-tools-check.sh` 綠；`push-gate.test.sh` 綠。
- 真 repo：`--base origin/main`（本分支）無輸出 rc=0；檔案模式全 32 支 migration rc=0、既有 133 行分級不變、只多 3 組 ENUM／CONSUMER；耗時 13.4s→14.0s（baseline 取 `origin/main` 版同機實測），`--base` 約 1s；`ci.yml` `yaml.safe_load` 通過。
- section-check 對真輸出實跑：全列 → `✓ 已逐一交代 4 個消費端路徑`；少列／`不需更新：` 無理由／段外 → exit 1 並點名；`--findings` 讀不到 → exit 2。
- 開發途中抓到並修掉的三個自身缺陷（皆有測試釘住）：`xargs … < /dev/null` 讓 `find` 吃 SIGPIPE 141（改 `xargs -r`）；awk `namehit[f]` 讀取即建元素造成假名稱命中（改 `(f in namehit)`）；`head -12` 在 pipefail 下可能 SIGPIPE（改 awk 截）。
- 每次 push 皆過 push-gate（純 harness 變更，跳過 Swift 步驟）。未等 CI（依派工：push 後立即交 handoff）。

## 未完成（含判斷不做的項目與理由）
- 首次提交，尚無 reviewer informational 待處置。
- **附帶發現（未入池，請 orchestrator 裁定）**：`docs/API.md:1258` `report_content` 的可檢舉 target 列 4 種並寫「同 `content_target_type`」——集合正確（`family` 不可檢舉），但措辭自 `family` 加入後不精確；建議 LS-23 檢舉 UI 票動 API.md 時順手改。本票不動 API.md（無 migration、非 BREAKING）。
- **不做**：其他 DDL 的消費端盤點；自動修改消費端（票文「不做」）。掃描刻意不含 `LittleSproutTests/`（票文限 `LittleSprout/`）、不認 Swift `case .v` switch pattern（編譯器窮舉會擋）、不掃 PL/pgSQL `CASE` 無 `ELSE`（漏報方向，靠 merge-reviewer；已寫進檔頭與 §7 未涵蓋面）。
- LS-96 `a6f28382` 的 i2 半邊（`index.ts` `claimEvents` 行轉換零測試）不在本票範圍，池項保留。

## 風險
- 多報方向：值集合相近的 enum 互相列（`feed_kind`↔`content_target_type`）、API.md 反引號值密集段落——PR body 多寫「不需更新＋理由」即可。
- 漏報方向：Swift snake→camelCase case 名、`||` 拆開的動態 SQL、值含空白的 enum。
- 本 PR 無 migration：CI Migration rules step 的新接線要到下一張加 enum 值的票才首次實戰；自測已覆蓋 `--findings` 正反面。
- 未動任何活的 Pen、模擬器、supabase 容器。

## 產出位置
- `scripts/gates/migration-breaking-check.sh`／`.test.sh`、`scripts/gates/breaking-section-check.sh`／`.test.sh`、`scripts/gates/push-gate.sh`、`.github/workflows/ci.yml`、`docs/COLLABORATION.md`
- PR #291（head `dd0284a`；commits `39169d4`／`24bf7e8`／`defe87b`／`dd0284a`）
- LS-96 `a6f28382` 銷除回覆 `83a73237`
- scratchpad：`LS-181-pr-body.md`、`LS-181-handoff.md`、`LS-181-backfill-40dd1cd.txt`、`LS-181-backfill-bc4b592.txt`
- 模擬器已關：無（純腳本票，未 boot）

### 2026-09-04T23:17:39.993Z · Chih-Lin Yeh · `e0eadb3f`

**2026-09-05 07:2x orchestrator — R1 handoff `504bc424` 收到（PR #291 head `dd0284a`）→ In Review，派 merge-reviewer R1**

- 裁定：`docs/API.md:1258` `report_content` 目標「同 `content_target_type`」用語（`family` 不可檢舉）→ 記入 LS-96 綁 LS-23，本票不動。
- 併入 main 後 back-merge；CI 接線首次實戰在下一張加 enum 值的票。

### 2026-09-04T23:25:49.060Z · Chih-Lin Yeh · `ad5c40eb`

## merge-review R1 — PR #291（head dd0284a）

裁決：**APPROVE**（無 blocker／無 major；**1 條 minor（N1）＋3 條 informational**）。審的 head＝`dd0284ab11663296e3e81423643ac459175ba8d6`，與 `gh pr view 291 --json headRefOid` 當下值相同；base `0c6b58b`，7 檔 +417/−15；`gh pr checks 291` **四項全 pass**（ci 6m22s／db 3m59s／lint 27s／rules 3m40s，run 33928754024——開審時三項在跑，等收工才判讀）。2026-09-05 07:24 CST。純腳本票，未碰 Pen／模擬器／容器。

### （1）B7 規則正確性 — 我用一份涵蓋十種寫法的探針檔實跑

| 輸入 | 結果 |
|---|---|
| `ALTER TYPE public.k1 ADD VALUE 'upper_case';`（全大寫） | ✓ BREAKING＋ENUM，值 `upper_case` |
| `add value if not exists 'ine'` | ✓ 值抽成 **`ine`**（沒把 `if` 當值） |
| 跨四行的 `ALTER TYPE\n public.k3\n ADD VALUE\n 'multiline';` | ✓ 值 `multiline` |
| `ADD VALUE 'before_v' BEFORE 'existing'` | ✓ 值 **`before_v`**（沒誤取 `existing`） |
| `DO $$ BEGIN ALTER TYPE public.k5 ADD VALUE 'dyn'; END $$;` | ✓ 值 `dyn` |
| `ALTER TYPE public.k6 ADD ATTRIBUTE col text;` | ✓ **零輸出**（不誤判） |
| `ALTER TYPE public.k7 OWNER TO postgres;` | ✓ **零輸出** |
| `RENAME VALUE 'a' TO 'b'` / `RENAME TO k9b` | ✓ 仍走既有 B5（BREAKING），**不產生 ENUM 行**（沒被 B7 吃掉） |

**真 repo 回歸**：檔案模式對全部 migration `rc=0`、151 行；把新增的 `ENUM`／`CONSUMER` 行濾掉後與 **main 版腳本的輸出逐字比對**——`diff` 只有三處**純新增**（`102a103`、`132a134,135`），內容正是 repo 內僅有的三句 `add value`（`notification_kind +report`／`content_target_type +family`／`notification_kind +media`）。**既有 133 條分類行一字未動**（133＋3＝136 分類行＋15 條 ENUM/CONSUMER＝151）。`--base origin/main` rc=0 無輸出。

**`--base bc4b592` 回填**與現況一致：`content_target_type +family` 與 `notification_kind +media` 各列 4 個消費端（push-dispatch `handler.ts`／`handler.test.ts`／`TimelineModels.swift`／`docs/API.md`），我逐一對過 ground truth（見下）。

**Mutation（我自己做 3 個）**：`thr` 2→99 → **殺**（三案紅：既有值 1 個降門檻／同輸入累加／`--base`）。另兩個的結論比「殺／不殺」更有價值，見 N1 與 I-1。

### （2）消費端 grep 的過報／漏報 — 我先用手工建 ground truth，再對照 gate 輸出

**你點名的 `TimelineModels.swift` 不會被漏掉，我實測過。** 自造 `ALTER TYPE public.feed_kind ADD VALUE 'moment'` 跑真 repo，輸出：

```
CONSUMER public.feed_kind LittleSprout/Services/Timeline/TimelineModels.swift   名稱＋既有值 3 種，行 3,5,6,16,89,175,215,216,…
CONSUMER public.feed_kind LittleSprout/Services/Timeline/TimelineContentAssembler.swift  名稱＋既有值 1 種，行 15,38,112,113
CONSUMER public.feed_kind supabase/functions/push-dispatch/handler.ts / handler.test.ts / docs/API.md
```

`TimelineModels.swift:5` 正是 `enum FeedKind: String, Decodable, Sendable, Hashable`——它被**名稱命中**（Pascal `FeedKind`＋註解裡的 `feed_kind`）抓到，跟 camelCase 對應無關。

**camelCase 漏報在這個 repo 現況下不成立**：我掃過全部 Swift，`: String` enum **一律寫明 raw value**（`case xxx = "yyy"`，例如 `TapTargetGateScreenName`／`AppError`／`CodingKeys`），**沒有任何一個裸 camelCase 多字 case**——多字值會以引號字面被抓到。真正靠 `case v` 那條分支的只有「無 raw value 的單字 case」形狀（我用合成夾具驗過它有效，見 N1）。

**`index.ts` 沒被列出是正確的，不是漏報**：`isNotificationKind` 的**定義在 `handler.ts:62`**，而 handler.ts 被列出（名稱＋既有值 5 種）——也就是說**當初 LS-149 那次事故，這支 gate 會抓到該改的那個檔**。`index.ts` 只 import／呼叫守衛、不列舉值集合；它含 enum 值字面的三行全是註解裡的同一個 `'report'`（**只有 1 種**，低於門檻 2），依規則不列——需要改的地方不在它身上。

**過報**：`TimelineModels.swift` 會出現在 `content_target_type`／`notification_kind` 的清單（值集合 album/media/diary 重疊）。代價是 PR body 多寫一行「不需更新＋理由」，方向偏安全。**判定：可接受**，且票文檔頭已把這兩類限制寫明。

### （3）`breaking-section-check --findings` — 四種情境我都實跑

| 情境 | 結果 |
|---|---|
| **無 `--findings`**（既有 PR） | exit **0**——與改動前完全相同，**既有 PR 不會誤紅** |
| 有 findings、body 只交代 1／2 個路徑 | exit **1** 並列出缺的兩個路徑（含「同一行寫已更新 `<sha>` 或不需更新：理由、且要在 BREAKING: 行之後、下一個標題之前」的指引） |
| body 兩個路徑都交代（一個 `已更新 a1b2c3d`、一個 `不需更新：理由`） | exit **0** |
| `--findings` 檔讀不到 | exit **2**（fail closed） |

CI 接線也對：`ci.yml` 只有在 `findings` 含 `^BREAKING` 時才落檔並帶 `--findings`，所以**沒有 migration／沒有 BREAKING 的 PR 完全不受影響**；`push-gate.sh` 只印提醒（多印 `ENUM`／`CONSUMER` 行）不擋。自測：`breaking-section-check.test.sh` **29 組**、`migration-breaking-check.test.sh` **87 組**、`push-gate.test.sh` 全綠——**都用 macOS `/bin/bash` 3.2.57 跑過**。

### （4）效能

檔案模式（全部 migration）：舊版 **14s**、新版 **14s**（我各跑一次，秒級解析度看不出差異，與申報的 13.4→14.0s 相符）；`--base origin/main` ≈1s。可接受。

### （5）申報查實／文件／範圍

- **逐 commit 檔案與 handoff 申報完全相符**：`39169d4`（只動 migration-breaking-check.sh）／`24bf7e8`（section-check＋ci.yml＋push-gate）／`defe87b`（兩支自測＋腳本 1 行）／`dd0284a`（只動 COLLABORATION.md）——無夾帶，7 檔全在票文 1–5。
- **LS-96 `a6f28382` 的銷除 `83a73237` 我到 LS-96 抓全部 comment 核對：存在**，內容對應 B7＋`--findings`，帶 commit `39169d4`。
- COLLABORATION **§6:213** 的 BREAKING 清單已含 `ALTER TYPE … ADD VALUE`（含 B7／ENUM／CONSUMER／PR body 要求的完整語意）；**§7:247** 新列的 gate 欄寫明 B7 的比對規則與 `breaking-section-check --findings` 的接線——規則與程式碼一致。
- `pr-body-check.sh --verify` 綠。

### Finding

**N1（minor）Swift「裸 `case v` 宣告行」這條分支是**檔頭明講的功能**，但 87 組自測沒有任何一案釘住它。**
我把 `AWK_CODE` 裡 `else if (lang == "swift" && … /^[[:space:]]*case[[:space:]]+[a-z_]/ …)` 整行刪掉，**87 組全綠**。再用合成夾具驗證它確實會改變行為：一個只有 `case album`／`case media`／`case diary`（**不寫 raw value**）的 Swift 檔——正本輸出 `CONSUMER … Kind.swift 既有值 3 種，行 2,3,4`，拿掉分支後變成「消費端：無」。**所以這不是等價突變，是真的功能缺測**：日後有人重構這段 awk 把它弄掉，測試不會紅，而「沒寫 raw value 的 Swift enum」就會從清單裡消失。
現況風險低（我掃過全 repo，Swift 的 `: String` enum 都寫明 raw value，走的是引號字面那條路），所以列 minor 不擋併入。修法：把我上面那個三行夾具加成一案（`case` 宣告 + 無 raw value + 期望列出該檔）即可。

### Informational

**I-1（別為它「修測試」）`vals=${vals#if not exists }` 是等價寫法，拿掉不影響行為。** 我把它拿掉後 87 組全綠，一度以為是缺測；實跑確認：`IF NOT EXISTS 'ine'` 在有無這行剝除下都抽出 `ine`，因為真正在工作的是後面的引號擷取（`${vals#*\'}` / `${vals%%\'*}`）。**這是等價突變、不是測試漏洞**，寫下來避免有人日後為了「殺掉它」去加一個永遠不會失敗的案例。

**I-2（流程一致性）PR body 沒有申報 LS-96 銷除行。** `pr-body-check` 印「LS-96 行 0 條」——但票文範圍 5 的銷除**實際有做**（`83a73237`，我核對過）。LS-176／LS-180／LS-158 都在 body 寫了一行申報讓 gate 可反查格式與 id，本票沒寫。實質沒少做，只是少了一道可機械反查的痕跡；下次順手補即可。

**I-3（過報的實際樣貌，供 PR body 撰寫者參考）** `TimelineModels.swift` 會同時出現在 `feed_kind`（真消費端）與 `content_target_type`／`notification_kind`（值集合重疊的過報）的清單裡。前者要真的交代處置，後兩者寫「不需更新：iOS 端只解 `feed_kind`，不解這個欄位」即可。這是設計上刻意偏多報的方向，我同意。

### 四維度

race：純分級腳本，無並發；`--findings` 落檔在 `$RUNNER_TEMP`、每次 job 獨立。效能：見（4），新規則的成本落在「有 add value 時才跑消費端 grep」，一般 PR 零成本。平行：無。scope：7 檔全在票文 1–5，四個 commit 各自乾淨；`migration-breaking-check.sh` 對既有 B1–B6 的輸出**逐字未變**（我用 main 版腳本對照證明），是本票最重要的無回歸證據。


### 2026-09-04T23:26:57.479Z · Chih-Lin Yeh · `45c33d53`

**2026-09-05 07:3x orchestrator — merge-review R1 `ad5c40eb` APPROVE 帶 N1 → 派實作者補一案後 reviewer 補 status 再併**

- N1：裸 `case v` Swift 分支缺自測（真缺測）→ 本 PR 補三行夾具案；I-2：PR body 補 LS-96 銷除申報行；I-1／I-3 不動。

### 2026-09-04T23:30:02.639Z · Chih-Lin Yeh · `d44f4033`

## Ticket
LS-181 R2（2026-09-05 07:29 CST）— 回應 merge-review R1 `ad5c40eb`（APPROVE，N1＋I-1～I-3；orchestrator 裁定 `45c33d53`）。PR #291 head **`3b58f93`**（5 commits，base `main` 未前進，仍 `0c6b58b`，不需 merge）。

## 已完成
- **N1（已修 `3b58f93`）**——`scripts/gates/migration-breaking-check.test.sh:247-252`：夾具 `LittleSprout/BareCases.swift`（`enum Feed { case album / case diary }`，無 raw value；名稱 `Feed`≠`FeedKind`，名稱規則不命中）＋既有值 `feed_kind`={album,diary}＋假 migration `alter type public.feed_kind add value 'media'` → 期望 CONSUMER 只列該檔。自測 87→88。
  - **自證（mutation）**：暫時 `sed` 刪掉 `AWK_CODE` 的 `else if (lang == "swift" …)` 行 → 88 組**只有這一案紅**（`期望消費端 LittleSprout/BareCases.swift，實得 NONE，級別 BREAKING+ENUM`，其餘 87 綠）→ `git checkout --` 還原（`git diff --quiet` 確認與 HEAD 相同）→ 88 組全綠。log：scratchpad `LS-181-n1-mutation.log`。
- **I-2（已補）**——PR body 變更摘要第 5 點申報 LS-96 池項 `a6f28382` 銷除回覆 `83a73237`；另加「merge-review R1 → R2 處置」表（N1／I-1／I-2／I-3 四列）。`gh pr view 291 --json body` 取回 live body 只增改自己段落（與本機檔 diff 僅尾端空行），`pr-body-check.sh` 綠（LS-96 行 2 條皆帶 comment id、「已修」行 1 條帶 SHA）後 `gh pr edit`，**在 push `3b58f93` 之前就位**。
- **I-1 不動**（等價寫法，不為殺等價突變加永不失敗案例）；**I-3 不動**（多報方向刻意，處置寫法已在檔頭／§7）。

## 已驗證
- `migration-breaking-check.test.sh` 88/88；mutation 紅→還原→綠（見上）；push-gate 綠（純 harness，跳過 Swift 步驟）。未等 CI。

## 未完成／風險
- 無新增。純腳本票，未碰 Pen／模擬器／容器；base `main` 未前進。

## 產出位置
- PR #291 head `3b58f93`（commits `39169d4`／`24bf7e8`／`defe87b`／`dd0284a`／`3b58f93`）
- scratchpad：`LS-181-pr-body-live.md`（現行 body）、`LS-181-n1-mutation.log`、`LS-181-handoff.md`（已附 R2 段）

### 2026-09-04T23:34:52.023Z · Chih-Lin Yeh · `8c6d756e`

## merge-review R2 — PR #291（head 3b58f93，增量 dd0284a..3b58f93）

裁決：**APPROVE**（無 blocker／無 major／無新 finding）。R1 的 **N1 修對且我複驗過**，I-2 也一併補了；I-1／I-3 本來就不需動作。審的 head＝`3b58f93cb52f2683f1b6e3b008e0e36642d9c8bc`，與 `gh pr view 291 --json headRefOid` 當下值相同；`gh pr checks 291` **四項全 pass**（ci 4m15s／db 3m39s／lint 12s／rules 3m33s，run 33929698411——開審時三項在跑，等收工才判讀）。2026-09-05 07:34 CST。純腳本票，未碰 Pen。

### （1）新案真的釘住那條分支 — **1 紅 87 綠，外科等級的隔離**

我把 `AWK_CODE` 裡的 Swift `case v` 分支整行刪掉再跑全套：

```
✗ B7 Swift 裸 case 宣告行（無 raw value、無名稱命中）靠 case 行分支列出（R1 N1）
   （期望消費端 LittleSprout/BareCases.swift，實得 NONE）
紅案數＝1／綠案數＝87
```

**只有新案紅、其餘 87 組全綠**——這正是一個定點回歸針該有的樣子：不多殺、也沒有靠別的案順便撐住。R1 我報這條缺測時是「刪掉分支 87 組全綠」，現在補上後變成「刪掉分支恰好 1 紅」，缺口封住。

**夾具設計我逐條驗過，成立**：

- `enum Feed`（**不是** `FeedKind`）→ 名稱規則比對的是整字 `FeedKind`／`feed_kind`，`Feed ` 兩者都不中，所以不會被名稱命中「順便」列出——這一點是這個案能真正隔離分支的關鍵。
- **不寫 `: String`**、沒有任何 `"album"` 之類的引號字面 → 引號字面那條路也走不通。
- 兩個裸 case `album`／`diary` ＋ 既有值集合 {album, diary} → 剛好滿足門檻 2，**唯一能命中的路徑就是 `case` 行分支**。
- 註解也把「其餘夾具對 feed_kind 都只命中 ≤1 種既有值、不會被列」寫明，避免別的夾具污染這一案。

基準線：**`/bin/bash` 3.2.57 跑 88 組全綠**。

### （2）PR body

`pr-body-check --verify` 綠：**LS-96 行 2 條**（變更摘要第 5 點的銷除申報帶 comment id **`83a73237`**、可機械反查；R1→R2 處置表再引一次）、**「已修」行 1 條**帶 SHA `3b58f93` 且 gate 確認該 SHA 在本 PR commits 內。R1 的 I-2（少了可反查的痕跡）就此消掉。

### （3）範圍

`origin/main` 仍是 `0c6b58b`、merge-base 未前進；對 main 的差集仍是 **R1 那 7 個檔**（沒有新增檔案、沒有夾帶）；增量本身只有 `migration-breaking-check.test.sh` **7 行**（＋1 行計數字串 87→88）。

### R1 其餘 informational 的狀態

- **I-1**（`vals=${vals#if not exists }` 是等價寫法）——實作者沒有為它加案，**這正是我要的處置**：等價突變不該用永不失敗的案例去「殺」。
- **I-3**（過報樣貌供 PR body 撰寫者參考）——無需動作。

### 四維度（增量）

race／效能／平行：增量只動測試檔，零影響。scope：7 行測試＋PR body，完全對應 R1 的 N1／I-2。


### 2026-09-04T23:36:06.999Z · Chih-Lin Yeh · `d9ce58cd`

**2026-09-05 07:35 orchestrator — merge-review R2 `8c6d756e` APPROVE → PR #291 已併入 main `f304a13`；back-merge main→development 背景進行；票進 QA**

- QA：隨下一批 test 晉升驗兩支自測（88／29）＋真 repo `--base` 綠；CI `--findings` 接線首次實戰待下一張 ADD VALUE 票。
- 併入 dev 後 → dev→test（含 LS-179 QA 中的 test 會再前進一次，PASS 後做 delta）。

### 2026-09-04T23:51:33.201Z · Chih-Lin Yeh · `32b4a53c`

**2026-09-05 07:51 orchestrator — back-merge PR #292 已併入 development `6b32aa7`（main ⊂ dev）；dev CI 綠後晉升 test，隨 LS-179 QA PASS 後做 delta（兩支自測＋真 repo `--base`）再 test→main。**

### 2026-09-05T00:18:50.173Z · Chih-Lin Yeh · `35df65f2`

**2026-09-05 08:18 orchestrator — 已晉升 test `aa199f8`；隨 LS-182 同批 delta QA（兩支自測＋真 repo `--base` 回填核對＋push-gate 自測）。**

### 2026-09-05T00:37:04.848Z · Chih-Lin Yeh · `50e86e1d`

## QA — delta（test `aa199f8`，回歸自 `a0b025c` QA PASS）2026-09-05 08:36 CST

**裁決：PASS**

worktree `qa-test` fetch＋checkout `aa199f8f38518d68e5e4b39bcc23a72b147844a0`（detached，等於 `origin/test`）。純腳本票，未動 Pen／模擬器；本票驗證不需持有 supabase lock（無 DB 互動）。

### 逐條驗收

1. **假 migration ADD VALUE → gate 印 BREAKING＋消費端清單；PR body 少列一項 → breaking-section-check 紅（自測）** — ✓
   - `bash scripts/gates/migration-breaking-check.test.sh` → **88/88 全綠**（含 B7 全部案例：跨行、IF NOT EXISTS、大寫＋BEFORE、省略 schema、DO 動態 SQL、無消費端、無 `--known-enums`、既有值 1 個門檻降 1、三反例、同輸入累加、`--base`、R2 補的 Swift 裸 `case` 分支案、三個 fail closed）。
   - `bash scripts/gates/breaking-section-check.test.sh` → **29/29 全綠**（含少列一項紅＋stderr 點名、全列綠、CRLF、括號寫法、findings 讀不到 exit 2、未知參數 fail closed 等）。

2. **真 repo 對兩個既有 enum 跑過、現況一致** — ✓（含一個需要說明的落差，見下）
   - `bash scripts/gates/migration-breaking-check.sh --base origin/main`：**rc=0**，但**非空輸出**（13 行既有 B1–B6 分類、無 ENUM/CONSUMER 行）。核對原因：`origin/main` 目前在 `f304a13`（LS-181 本身 PR #291 併入點），而 `test` 分支已含 LS-179 的 migration `20260904212530_suspension_and_registrations.sql`（尚未 promote 到 `main`——依 orchestrator comment，promote main 排在本次 delta QA PASS 之後）；grep 過該檔無任何 `add value` 語句，13 行全部是既有 B1–B6 規則（函式/policy 變更），**與 B7／本票增量無關**，是尚未晉升的正常待辦，不是回歸。原始驗收詞句「rc=0 空輸出」的假設（`origin/main`＝`test` 的 migration 基準相同）在目前晉升節奏下不成立，已如實記錄而非強改判定。
   - `--base bc4b592`（既有兩個 enum 加值前的基準）→ 逐字重現 R1／R2 merge-review 的回填結果：
     ```
     ENUM  public.content_target_type  family  既有值 4 個：album,comment,diary,media  消費端 4 處
     CONSUMER public.content_target_type supabase/functions/push-dispatch/handler.test.ts
     CONSUMER public.content_target_type supabase/functions/push-dispatch/handler.ts
     CONSUMER public.content_target_type LittleSprout/Services/Timeline/TimelineModels.swift
     CONSUMER public.content_target_type docs/API.md
     ENUM  public.notification_kind  media  既有值 5 個：album,comment,diary,reaction,report  消費端 4 處
     CONSUMER public.notification_kind supabase/functions/push-dispatch/handler.test.ts
     CONSUMER public.notification_kind supabase/functions/push-dispatch/handler.ts
     CONSUMER public.notification_kind LittleSprout/Services/Timeline/TimelineModels.swift
     CONSUMER public.notification_kind docs/API.md
     ```
   - 核對 `handler.ts` 現況：`isNotificationKind` 已含 `"album" | "media" | "report"`；`isContentTargetType` 已含 `"album" | "media" | "diary" | "comment" | "family"`——兩個守衛都已處理 CONSUMER 清單列出的加值，與 LS-175 R2 落地一致。

3. **merge-reviewer 通過；hotfix 流程** — ✓（既有紀錄）：R1 `ad5c40eb` APPROVE＋N1／I-1~I-3；R2 `8c6d756e` APPROVE（N1 補測、I-2 補 PR body）；PR #291 併入 `main`（`f304a13`），back-merge `main→development` 已完成（`6b32aa7`），現已隨批次晉升 `test`。

### 額外把關
- `bash scripts/gates/push-gate.test.sh` → 全綠（模擬器自測 27 項）。
- `bash scripts/gates/migration-immutable-check.sh --base origin/main --pr-body /dev/null --pr-number 293` → 綠（無既有 migration 被改／刪）；`.test.sh` 自測綠。
- `api-contract-check.sh`／`error-codes-check.sh`／`migration-version-check.sh` 皆綠（自測與真跑）。
- `gh run view` 對 `aa199f8` 的 CI（run 33932507735）：**success**（ci／db／lint／rules 全 pass）。

### 未驗證
無。純腳本票，範圍完全對得到票文 1–5。

證據：本次驗證輸出未落地檔案（純終端機驗證），可重跑指令見上。

### 2026-09-05T00:39:55.469Z · Chih-Lin Yeh · `a2446f78`

**2026-09-05 08:39 orchestrator — delta QA PASS `50e86e1d`（88／29 自測、`--base bc4b592` 回填一致）→ 已到 main `aa199f8`（v0.13.0）；sweeper 批次巡檢中 → retro → Done。**

### 2026-09-05T00:44:56.676Z · Chih-Lin Yeh · `11cf3e94`

## dead-code sweep — LS-181

**2026-09-05 08:43 CST，dead-code-sweeper。範圍：`git diff --stat 34dd954..aa199f8` 中屬本票的檔案——`scripts/gates/migration-breaking-check.sh`／`.test.sh`、`scripts/gates/breaking-section-check.sh`／`.test.sh`、`scripts/gates/push-gate.sh`（4 行）、`.github/workflows/ci.yml`（本票段落）、`docs/COLLABORATION.md`（本票段落）。在主 checkout（唯讀，未改任何檔案）巡查。**

### Findings

未發現本票引入的死碼。逐項證據：

1. **舊 tmp 檔管理殘留檢查**：R1 handoff 提到把既有 `known_file=$(mktemp); trap 'rm -f "$known_file"' EXIT` 改成集中式 `tmpdir=$(mktemp -d); trap 'rm -rf "$tmpdir"' EXIT`。`git diff 34dd954..aa199f8 -- scripts/gates/migration-breaking-check.sh` 確認舊的兩行（獨立 mktemp／trap）已被完整移除、無殘留雙重 trap，`known_file` 在 `--base` 模式下正確改指向 `"$tmpdir/known_functions"`。
2. **函式呼叫完整性**：新增的 `enum_name`／`enum_defs`／`resolve_consumer_root`／`to_pascal`／`enum_consumers`／`AWK_CODE`／`AWK_DOC` 七個新元件逐一確認皆被 `classify()` 內的 B7 分支或彼此呼叫，無孤兒函式；`enum_consumers()` 內 `local ... n=0` 的初始值在函式尾端被 `n=$(grep -c . "$out" || true)` 覆寫，是 bash 慣例的 local 宣告寫法（比照本檔其餘 `local` 變數的既有風格），非死碼。
3. **`index.ts` 舊 import 檢查**：LS-181 未改動 push-dispatch，不適用（該項屬 LS-182，見該票報告）。
4. **`breaking-section-check.sh` 舊行為保留**：既有規則①（`BREAKING:` 行首＋摘要）保持原樣、`--findings` 是新增的可選路徑（未給時 `exit 0` 直接略過新規則②），確認新增程式碼沒有覆蓋或架空舊規則、也沒有殘留任何舊版本的判斷分支。
5. **CI／push-gate 接線一致性**：`ci.yml` 舊版 `bash scripts/gates/breaking-section-check.sh`（無 `--findings`）呼叫已改為新版帶 `--findings "$RUNNER_TEMP/migration-findings.txt"`，無新舊兩套呼叫並存；`push-gate.sh` 的 `grep '^BREAKING'` 改為 `grep -E '^(BREAKING|ENUM|CONSUMER)'`，未留下舊版單一 grep 的死分支。
6. **自測檔**：`migration-breaking-check.test.sh`（69→88 組）／`breaking-section-check.test.sh`（13→29 組）逐案讀過，新增的 18／16 組全部有對應的 `expect_b7`／`expect_f` 呼叫，無定義後未呼叫的夾具或 case；`grep -n "TODO\|FIXME\|XXX"` 全部四個目標檔案 0 命中；`grep` 註解掉的程式碼塊（連續 `#` 前綴且看起來是被停用的可執行行）0 命中。
7. **COLLABORATION.md §6／§7**：新增段落（§6 BREAKING 清單補 `ALTER TYPE … ADD VALUE`、§7 新列一行對照表）與程式碼現況逐句核對一致，無提及已移除或未實作的機制。

### 既有死碼觀察

無。

### 建議處置

無需開票。

### 搜尋方法與盲區聲明

- 方法：以 `git diff 0c6b58b..f304a13`（PR #291 的精確 base/head，排除同批次的 LS-180 混入）隔離出本票對 `ci.yml`／`COLLABORATION.md`／`push-gate.sh` 的實際貢獻；`Read` 全文 `migration-breaking-check.sh` 逐行核對新增的 B7 邏輯與既有 B1–B6 邏輯有無互相覆蓋或殘留；`grep -n` 交叉比對每個新函式/變數的定義與呼叫點；核對三輪 merge-review（`ad5c40eb`／`8c6d756e`）與 QA（`50e86e1d`）記錄的已知限制與本次讀碼結果一致。
- 盲區：純文字掃描抓不到 awk 腳本內動態組出的欄位存取（本檔 `AWK_CODE`／`AWK_DOC` 皆為靜態字串內嵌 awk 原始碼，非動態拼接，風險低）；未實際跑 `.test.sh`（依規約，dead-code 巡檢只讀不執行；QA `50e86e1d` 已記錄 88／29 全綠）；bash 3.2 與較新 bash 版本的行為差異未重新驗證（採信既有 review 記錄「用 macOS `/bin/bash` 3.2.57 跑過」）。
- 範圍排除：`.github/workflows/ci.yml`／`docs/COLLABORATION.md` 在 `34dd954..aa199f8` 全域 diff 中同時被 LS-180（PR #289）與本票（PR #291）觸碰；本報告只涵蓋本票的貢獻部分，LS-180 的部分另計（不在本票報告範圍）。


### 2026-09-05T00:46:29.170Z · Chih-Lin Yeh · `01280546`

**2026-09-05 08:5x orchestrator — 收尾儀式 → Done**

- sweeper `11cf3e94` 0 finding；worktree／分支已清；LS-96 `a6f28382` 第 1 條已銷除。
- **Lesson learning**：(1) 這條 gate 直接對應 LS-149→LS-172 的靜默丟批事故，回填實測證明「當年該改的 `handler.ts` 這支 gate 抓得到」——事故 → gate 的閉環一天內完成。(2) R1 N1「功能有、測試沒釘」是 harness 票最常見缺口，reviewer 用刪分支 mutation 抓到；harness 派工 prompt 應固定要求「每個新分支至少一案 mutation 自證」（ca993eba(2) 的機械化候選）。(3) 過報（值集合重疊）代價一行「不需更新＋理由」，方向偏安全；首次實戰在下一張 ADD VALUE 票，屆時觀察 body 負擔。(4) `docs/API.md:1258` report 目標用語 → LS-96 `a34f39fb` 綁 LS-23。

