# LS-195 Task：LS-23 後端——檢舉事件推播只發給家庭 Owner（`notification_recipients` 對 kind='report' 只取 `family_members.role='owner'`；DB 測試釘住；notification_events 讀取面同步只給 owner）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:S, lane:backend |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M6 合規與帳號 |
| 估點 | — |
| 建立 | 2026-09-05T04:02:45.459Z |
| 開始 | 2026-09-05T04:04:30.060Z |
| 完成 | 2026-09-05T05:10:39.832Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-195/taskls-23-後端-檢舉事件推播只發給家庭-ownernotification-recipients-對-kindreport-只取 |
| 父票 | LS-23 Story：檢舉／封鎖／額度 UI＋EULA |
| 關係 | related→LS-186, related→LS-172, related→LS-175, related→LS-78, related→LS-96, related→LS-189, related←LS-219, related←LS-198 |

## 描述

## 來源

使用者 2026-09-05 11:5x 裁決（原話「檢舉事件的推播通知『只有 Owner』」），銷 [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項 `12e20e0c`。現況：`public.notification_recipients(p_event_ids)`（最新定義在 `supabase/migrations/20260904212530_suspension_and_registrations.sql:811`）對所有 kind 一律廣播給家庭全員（排除 actor／停權／封鎖），`report` 也不例外；`20260903091317_report_block_rpc.sql:736` 檔頭早已標明「只通知家庭 owner」為已知後續工作。§10-B：檢舉內容只有 owner 讀得到（`content_reports_select` policy），推播與 inbox 不得洩漏給一般成員。

## 範圍

1. 新 migration（append-only；正式站已推到 `20260904212530`）：`create or replace function public.notification_recipients(uuid[])` 整支逐字沿用現行定義，只在 where 加 `and (ne.kind <> 'report' or fm.role = 'owner')`；其餘四種 kind 行為不變；revoke 行照舊。
2. `notification_events` 讀取面：檢查 `notification_events` 的 select policy／任何 inbox 用 RPC，`kind='report'` 的列是否對非 owner 可見；若可見，一併收緊為 owner-only（inbox 票 [LS-189](https://linear.app/little-sprout-app/issue/LS-189/taskls-23-ios-內容操作表日記照片留言或長按檢舉封鎖此成員owner-移除內容檢舉原因與已送出封鎖確認封鎖名單解除owner) 依賴此保證）。若已不可見，在 handoff 說明證據（policy 名稱＋測試）。
3. DB 測試：在既有 push-dispatch／notification 測試檔補案——同家庭 owner＋member＋viewer 各有 device token，插一筆 report 事件 → `notification_recipients` 只回 owner 的 token；其餘 kind 仍回全員；actor 為 owner 自己檢舉時回 0 列（沿用 actor 排除）。mutation：拿掉新條件測試必紅。
4. `docs/API.md` §10 收件人規則補 report 一列；`push-dispatch` handler／EF 程式碼不變（純 SQL 面）。
5. 本機 `supabase-lock.sh --hold` 內 `db reset`＋`run.sh` 全綠；`migration-breaking-check.sh` 綠（函式簽章不變）。

## 不做

iOS inbox UI（[LS-189](https://linear.app/little-sprout-app/issue/LS-189/taskls-23-ios-內容操作表日記照片留言或長按檢舉封鎖此成員owner-移除內容檢舉原因與已送出封鎖確認封鎖名單解除owner)）；檢舉表單；EF 部署（正式站 `db push` 由 orchestrator 依 [LS-78](https://linear.app/little-sprout-app/issue/LS-78/harnesstestflight-前收回常設授權-migrate-to-production-改回需使用者允許機械-gate) 授權執行）。

## 驗收

- [ ] DB 測試新案綠＋mutation 紅；`run.sh` 全綠；CI 綠
- [ ] merge-reviewer 通過（scope：只動 where 一條件＋讀取面 policy）；QA 用兩帳號本機驗 report 事件 recipients 只含 owner
- [ ] 併入後正式站 `db push` 並回讀函式定義含新條件

## 留言（6）

### 2026-09-05T04:22:14.635Z · Chih-Lin Yeh · `58dc0578`

Ticket：LS-195

已完成：
- `supabase/migrations/20260905040711_report_recipients_owner_only.sql`：`notification_recipients(uuid[])` 逐字沿用現行定義（`20260904212530` 第 7 段），WHERE 只加 `and (ne.kind <> 'report' or fm.role = 'owner')`；其餘四種 kind 行為不變、函式簽章不變（commit `fa72e71`）。
- `notification_events` 讀取面檢查（票文第 2 項）：該表對 `authenticated` 無任何 RLS policy、無任何 table grant（只 `grant select, update ... to service_role`，`20260825020000_comments_reactions_notifications.sql:621`）；`claim_notification_events`／`notification_recipients` 兩支 RPC 也都只 `revoke ... from public, anon`、無 `authenticated` 的 `grant execute`（per-RPC 顯式 grant 慣例，新函式對 PUBLIC 預設零權限，`20260822120300_harden_default_privileges.sql`）。結論：檢舉事件本來就不會經任何既有讀取面外洩給非 owner，不需額外收緊 policy——已寫進 `docs/API.md` §10（commit `1fcb9da`）。
- `supabase/tests/103_push_dispatch.sql` 補 4d 段（commit `76dacbb`）：家庭 A 新增 viewer 角色成員（晚於既有 1~4c 段加入，不影響既有計數斷言），驗 report 事件只有 owner 收到、owner 自報時 0 列、非 report kind 不受影響。
- `docs/API.md` §10 補 report 收件人規則一列；順手把 LS-175 段落「是否只給 owner 是後續產品決定」改指向本票已定案。

已驗證：
- `supabase-lock.sh --hold "LS-195 db tests" --max-minutes 40`（持有 9 分 46 秒）內：`supabase db reset` 套用新 migration 乾淨完成；`bash supabase/tests/run.sh` 全綠，含新案 `ok 4d：kind=report 只留 family_members.role=owner（LS-195）——member／viewer／actor 本人皆排除，owner 自報時 0 列；非 report kind 不受影響、viewer 仍在全員名單`。
- Mutation 驗證：暫時拿掉 WHERE 新條件 → 重跑 reset+run.sh 立即紅：`ERROR:  FAIL：report 事件的收件人應該恰好 1 筆（爸爸，owner），實際 4`（exit 1）→ diff 比對還原原檔 → 重新 reset+run.sh 恢復 `✓ 全部 DB 測試通過`。
- `bash scripts/gates/migration-breaking-check.sh --base origin/development`：只命中一行 B4（`notification_recipients` CREATE OR REPLACE），已在 PR body 附 `BREAKING:` 段。
- `bash scripts/gates/api-contract-check.sh`：`✓ RPC 34 支、表 23 張，docs/API.md 與 schema 一致`。
- push gate 全綠（migration 版本號無撞號、migration-immutable-check 無既有檔被改、分支票號乾淨、與 origin/development 可乾淨合併）。

未完成／剩餘：目前尚無 reviewer 意見（本票剛送 In Review，merge-reviewer 尚未跑第一輪）——無 informational 待處置。唯一自行發現、不屬本票範圍的既有文件債：`docs/API.md` §10 `notification_recipients` 段原本就漏了「排除停權使用者」（`pr.suspended_at is null`，LS-172 第 1 段既有邏輯）這條 bullet；本票未動這個既有缺口，不修＋理由：與本票變更無關、且是純文件落差不影響行為，留給日後小票補（不佔本票 scope）。

風險與已知問題：
- BREAKING（B4）：唯一呼叫端 `push-dispatch/handler.ts` 不受影響（它不關心收件人怎麼篩出來），docs/API.md 已同 PR 更新。
- 無 DESTRUCTIVE 變更（未 DROP、未動 RLS policy、未動 grant）。

產出位置：PR #301（https://github.com/CLYEH/little-sprout/pull/301）base development；head sha `1fcb9da763478460e6d5526e8030d1f1d5ad8541`；branch `feature/LS-195-report-push-owner-only`。模擬器已關：無（本票未碰模擬器）。

### 2026-09-05T04:32:20.903Z · Chih-Lin Yeh · `945840af`

## merge-review R1 — APPROVE（2026-09-05 12:31）

PR #301 · base `development` · head `1fcb9da763478460e6d5526e8030d1f1d5ad8541`（審查期間 head 未前進）· lock 持有 3 分 38 秒（`LS-195 merge-review R1`，在 `.claude/worktrees/LS-195` 內 hold／release）

### 實跑驗證（不採信自述，全部自己重跑）

1. **函式體逐字比對**：把 `20260904212530_suspension_and_registrations.sql:811-840` 與新 migration 的 `create or replace ... revoke` 段落抽出來做 `diff -u` → **唯一差異就是 +1 行** `and (ne.kind <> 'report' or fm.role = 'owner')`（`supabase/migrations/20260905040711_report_recipients_owner_only.sql:49`），插在 `pr.suspended_at is null` 之後、`not exists (blocked_users …)` 之前。`language sql` / `stable` / `security definer` / `set search_path = ''` / 回傳欄位 / `revoke execute … from public, anon` 全部原樣。`comment on function` 是新增說明，沒有刪掉既有語意。
2. **型別安全**：`notification_kind` 在 `20260903091313` 已 `add value 'report'`，`family_role` enum 含 `'owner'`（`20260822120000_init_schema.sql:22`）。`ne.kind <> 'report'` 的未定型字面量由欄位型別推導，`search_path = ''` 不影響（不需要 schema 限定的型別名）；`ne.kind` NOT NULL、`fm.role` NOT NULL，OR 短路無 NULL 陷阱（真要是 NULL 也是 fail-closed，不會多發）。`supabase db reset` 實際套用成功可證。
3. **DB 測試（自己跑）**：lock 內 `supabase db reset` 套到 `20260905040711` 乾淨完成 → `bash supabase/tests/run.sh` **全綠**（含 `ok 4d`）。
4. **Mutation（自己做，未改任何檔案）**：把 LS-195 之前的函式定義直接灌回本機 DB（`docker exec … psql`，在 lock 內）→ 單跑 `103_push_dispatch.sql` → **紅**：`ERROR: FAIL：report 事件的收件人應該恰好 1 筆（爸爸，owner），實際 4`（4 = 媽媽 2 支＋viewer 1＋爸爸 1，阿嬤是 actor）。再把本票 migration 灌回去 → `103` 與 `60_default_privileges.sql` 皆 exit 0。這一步順帶驗到**正式站的套用路徑**：`CREATE OR REPLACE` 打在既有函式上，ACL 保留（`60_` 仍綠＝`service_role` 有 EXECUTE、`authenticated`／`anon`／PUBLIC 無）。
5. **讀取面（票文第 2 項，自己複核而非採信 handoff）**：對本機 DB 直接查 catalog——
   - `pg_policies where tablename='notification_events'` → **0 列**（RLS enabled + 零 policy＝deny-all）。
   - `role_table_grants where table_name='notification_events'` → 只有 `postgres`（全權）與 `service_role`（SELECT／UPDATE＋預設的 TRUNCATE/REFERENCES/TRIGGER），**沒有 `authenticated`／`anon` 任何一位**。
   - `pg_proc where prosrc like '%notification_events%' and has_function_privilege('authenticated', oid, 'execute')` → **0 列**（沒有任何 authenticated 叫得到的函式讀這張表）。
   - 靜態面補強：repo 內沒有任何 view（`create view` 零命中）、沒有任何 migration 把此表加進 realtime publication；`60_default_privileges.sql:501` 已把 `notification_recipients(uuid[])` 登記在 `v_service_role_rpcs` 白名單（authenticated 必須不可執行），`103_push_dispatch.sql` §6 另有實打的 42501 斷言。
   結論：**「非 owner 讀不到 report 事件」成立**，本票不另收緊 policy 是對的，LS-189 可以依賴這個保證。
6. **測試釘住的三件事**（`supabase/tests/103_push_dispatch.sql:258-328`）：report 只回 owner（member／viewer／actor 本人各有獨立斷言，不是只看總數）、owner 自報回 0 列、非 report kind（album）仍回全員 5 支且 viewer 在內——第三條同時擋住「誤把 owner-only 套到所有 kind」的反向 mutation。viewer 刻意在 4d 才加入家庭，我確認 5/6/7 段沒有任何會被多一名成員影響的斷言（實跑亦綠）。
7. **零收件人不會造成漏送事故**：`supabase/functions/push-dispatch/handler.ts:419` 對 `eventRecipients.length === 0` 是 `continue`（不算失敗），所以 owner 自報／owner 被排除時只是不推播，不會拖垮整批或造成重試 churn——本票收窄不會踩到 LS-96 `841d97da` 那類整批失敗。
8. **Gate／scope**：`migration-breaking-check.sh --base origin/development` 只吐一行 `BREAKING`（B4，CREATE OR REPLACE 既有函式），分類正確、PR body 有 `BREAKING:` 段且說明唯一呼叫端不受影響；PR body 檔頭段落齊全；CI 四項（ci／db／lint／rules）全綠。三個 commit 歸屬乾淨、無夾帶：`fa72e71` 只動 migration、`76dacbb` 只動測試、`1fcb9da` 只動 `docs/API.md`。

### 四維度

- **Race**：純 SQL 面、`stable` 唯讀函式，未新增任何寫入或狀態；`CREATE OR REPLACE` 對進行中的 dispatch 沒有一致性問題（舊呼叫看舊定義直到 commit）。成員角色在事件寫入與 dispatch 之間變動會改變收件人集合，這是既有語意（跟停權／封鎖排除同一形狀），非本票引入。**無 finding**。
- **效能**：新增條件是 `family_members.role` 的等值比較，不是 per-row 子查詢（PLAN §5 紅線未踩）；不改 join 形狀、不新增 N+1、不動分頁。**無 finding**。
- **平行**：handler 的併發送出（`deps.concurrency`）與批次取件路徑一行未動。**無 finding**。
- **Scope**：diff 只有 migration＋測試＋文件三檔 152 行，全部可追溯到票文 1/3/4 項。`docs/API.md` 那句 LS-175「是否只給 owner 是後續產品決定」的改寫我判定**屬必要的文件同步**（不改就會留下一句被本 PR 推翻的錯誤敘述），不列為逾越範圍。**無 finding**。

### Findings

- **N1（minor）** `supabase/functions/push-dispatch/handler.ts:251-255`（本 PR 未動，屬本變更的下游影響）：`case "report"` 的文案是「你的{label}收到一則檢舉」。收窄成 owner-only 之後，**最常見情境（被檢舉的內容不是 owner 自己的）下這句對收件人是錯的**——owner 會收到「你的日記收到一則檢舉」，但那則日記是別的成員的。失敗情境：媽媽發日記 → 阿嬤檢舉 → 只有爸爸（owner）收到推播，內容說「你的日記收到一則檢舉」。票文第 4 項明文「`push-dispatch` handler／EF 程式碼不變」，因此**不要求本 PR 修**；建議記入 LS-96 待辦池或併進 LS-189 的檢舉文案處理（owner 視角應是「家庭中有內容被檢舉」之類的中性句）。
- **N2（informational）** `supabase/migrations/20260905040711_report_recipients_owner_only.sql:6,14`／`docs/API.md:2447`：三處都寫「其餘**四種** kind」但緊接著列了五個值（comment／reaction／diary／album／media）。純註解與文件的計數口誤，零行為影響，建議下次碰到這段時順手改成「五種」（或不寫數字）。
- **N3（informational）** `supabase/migrations/20260905040711_report_recipients_owner_only.sql:49-56` 的條件順序（先 owner-only、再封鎖排除）帶來一個產品層面的取捨：**owner 若封鎖了檢舉人，該則 report 事件的收件人會是 0 列**（唯一候選人被封鎖排除掉），推播完全不發。失敗情境：owner 與成員 A 交惡、owner 封鎖 A → A 檢舉 owner 的日記 → 沒有任何人收到推播。資料不會遺失（`content_reports` 列仍在，`content_reports_select` 讓 owner 讀得到，LS-189 inbox 會顯示），所以不是 blocker；但「檢舉是否該豁免封鎖排除」值得產品決定一次。建議記入 LS-96 池項。
- **N4（informational）** `docs/API.md:2438-2444`：實作者自報的既有文件債（收件人排除清單缺「排除停權使用者」`pr.suspended_at is null`，LS-172 既有邏輯）確認存在且**非本票引入**。同意本票不修；建議處置從「留在 handoff」升級為**記入 LS-96 待辦池**（與 N2 合併成一張極小文件票即可），否則這條容易被遺忘。

### 未審／限制

- 正式站 `db push` 後的回讀驗證不在本輪範圍（併入後由 orchestrator 依 LS-78 執行）。
- 未做多 owner 家庭的情境（本 repo 目前每個家庭一位 owner；多 owner 時語意是「所有 owner 都收到」，符合票文）。
- iOS 端（LS-189 inbox）未涉及。

**Verdict：APPROVE**（無 blocker／major；N1 minor 與 N2–N4 informational 皆不擋合併，處置建議如上，請在 handoff 逐條交代）。

### 2026-09-05T04:34:21.110Z · Chih-Lin Yeh · `b6924fc4`

**orchestrator 2026-09-05 12:5x**：merge-review R1 `945840af` **APPROVE**（status 53588879214 on `1fcb9da`；mutation 灌舊定義紅「實際 4」→ 新定義綠；讀取面 catalog 直查無 authenticated 路徑）。N1 併 LS-189 文案、N2／N4 入池、N3 待使用者產品決定（LS-96 新 comment）。**併入順序**：等 LS-186 back-merge 鏈把 development→test 晉升完（避免 dev tip 交錯讓 promote 的 merge-review status 對不上），再 merge #301 → dev tip 貼 `promote: no content diff（PR #301 head 1fcb9da APPROVE R1）` → promote dev→test → 與 LS-186 合併一輪 delta QA（backend：`run.sh`＋兩帳號 report recipients 實測）→ test→main → 正式站 `db push 20260905040711` 並回讀。

### 2026-09-05T04:59:43.781Z · Chih-Lin Yeh · `e919cab1`

## QA delta R1 — **PASS**（2026-09-05 12:59 CST）

範圍：`test` tip `b1f714f10d51b5be653131e33aba1bbf5c88c341`（PR #301 head `1fcb9da`；merge-review R1 `945840af` APPROVE），涵蓋 **LS-195**（本票）＋**LS-186**（同 delta，摘要見下方連結票的摘要 comment）。worktree `/Users/clyeh/little-sprout/.claude/worktrees/qa-test`，`git rev-parse HEAD` 已核對與 `b1f714f` 相符。

### 零 app 程式碼證據
`git diff --stat 103d9d4..b1f714f -- LittleSprout LittleSproutTests LittleSproutUITests` 輸出為空——本輪 delta 只動 harness／CI／文件／migration／SQL 測試 13 檔（`git diff --stat 103d9d4..b1f714f` 完整列表：`.claude/agents/ios-dev.md`／`.github/workflows/ci.yml`／`CLAUDE.md`／`docs/API.md`／`docs/COLLABORATION.md`／`scripts/ci/db-reset-retry.{sh,test.sh}`／`scripts/gates/agent-tools-check.{sh,test.sh}`／`scripts/gates/pr-body-check.{sh,test.sh}`／`supabase/migrations/20260905040711_report_recipients_owner_only.sql`／`supabase/tests/103_push_dispatch.sql`）。依指示**不跑 xcodebuild、不用模擬器**。

### 逐條驗收（LS-195）

**1. Migration 套用**：lock 內 `supabase db reset` 乾淨套用到 `20260905040711_report_recipients_owner_only.sql`（29 支 migration 全過，無錯誤）。

**2. `bash supabase/tests/run.sh` 全綠**，含新案：
```
NOTICE:  ok 4d：kind=report 只留 family_members.role=owner（LS-195）——member／viewer／actor 本人皆排除，
         owner 自報時 0 列；非 report kind 不受影響、viewer 仍在全員名單
✓ 全部 DB 測試通過
```
（`10_cross_family_isolation.sql` 亦在同一輪 `run.sh` 內 ✓，回歸無破。）

**3. 我自己直查 DB**（不採信 handoff/merge-review 自述，另建一個獨立家庭三角色跑，包在 `BEGIN…ROLLBACK`、跑完驗證零殘留）：
- 建家庭 owner／member／viewer 各一 device token；插 `kind='report'`、actor=member 的事件 → `notification_recipients` **恰 1 列＝owner**，member／viewer 皆不在。
- 同一家庭插 `kind='report'`、actor=owner（自己檢舉）的事件 → **0 列**。
- 對照組 `kind='comment'`、actor=member → **2 列＝owner＋viewer**（member 因為是 actor 被排除，符合既有排除邏輯）。
- 三組數字與訊息：
```
NOTICE:  total rows = 1 (expect 1: owner only)
NOTICE:  owner present = 1 (expect 1)
NOTICE:  member/viewer present = 0 (expect 0)
NOTICE:  total rows = 0 (expect 0)
NOTICE:  total rows = 2 (expect 2: owner + viewer)
NOTICE:  owner present = 1 (expect 1)
NOTICE:  viewer present = 1 (expect 1)
NOTICE:  member(actor) present = 0 (expect 0)
```
- Rollback 後確認 `select count(*) from auth.users where email like 'qadirect-%@ls.test'` = 0，無殘留測試資料。

**4. `authenticated` 角色讀取面**：`set role authenticated; select count(*) from notification_events;` →
```
ERROR:  permission denied for table notification_events
```
另直查 catalog：`pg_policies where tablename='notification_events'` → 0 列（RLS enabled + 零 policy＝deny-all）；`role_table_grants where table_name='notification_events'` → 只有 `postgres`／`service_role`，無 `authenticated`／`anon`。與 merge-review R1 `945840af` 的結論一致：檢舉事件在讀取面本來就出不去，本票不需另收緊 policy。

**5. `docs/API.md` §10** 已看到 report 收件人規則一列（migration 亦有 `comment on function` 說明），與程式行為一致。

### 回歸

- `supabase/tests/10_cross_family_isolation.sql` 含在 `run.sh` 全綠內，✓。
- `bash scripts/gates/migration-breaking-check.sh --base origin/main`：輸出一行 `BREAKING	create or replace function public.notification_recipients(...)`，exit 0（分類正確、非阻擋；與 merge-review R1 的 B4 判定一致，PR body 已附 `BREAKING:` 段）。

### merge-review informational 處置追蹤（`945840af`）
N1（推播文案「你的{label}收到一則檢舉」在 owner-only 後語意不精準）／N2（「其餘四種 kind」應為「五種」的計數口誤）／N3（owner 封鎖檢舉人時 report 事件 0 收件人）／N4（`docs/API.md` 既有文件債：漏「排除停權使用者」）——皆為 informational、reviewer 已裁定不擋合併，orchestrator 記入 LS-96／LS-189 後續處理，QA 未發現新增問題。

### 未跑項目
- 正式站 `db push 20260905040711` 回讀（依 LS-78，票文範圍排除，orchestrator 併入後執行）。
- 多 owner 家庭情境（repo 目前每家一位 owner，非本票範圍）。
- iOS inbox（LS-189，未落地）。

### 產出位置
- worktree：`/Users/clyeh/little-sprout/.claude/worktrees/qa-test`（`git rev-parse HEAD` = `b1f714f10d51b5be653131e33aba1bbf5c88c341`）
- scratchpad：`LS-186-195-qa-run-sh.log`（`run.sh` 全量輸出）／`LS-186-195-qa-direct.sql`／`LS-186-195-qa-authrole.sql`／`LS-186-195-qa-policies.sql`／`LS-186-195-qa-verify-clean.sql`／`LS-186-195-qa-ls185-{red,fixed}-body.md`（LS-186 用）
- 模擬器已關：無（本票零 app 程式碼，未 boot 任何模擬器）
- lock 已釋放：`LS-186-195 delta QA`（持有 3 分 48 秒，正常釋放非到期）
- qa-e2e 證據：未跑（本票純 SQL／harness delta，無 app UI 變更，無多步驟情境可跑）

**LS-186 摘要**：見 https://linear.app/little-sprout-app/issue/LS-186 對應 QA comment（同一輪 delta、同一份裁決 SHA）。

裁決：**PASS**，將以 `post-status.sh` 貼 `qa=success` 到 `b1f714f10d51b5be653131e33aba1bbf5c88c341`。

### 2026-09-05T05:08:36.620Z · Chih-Lin Yeh · `39f01526`

## dead-code sweep — LS-195

範圍：`git diff d22845a..b1f714f10d51b5be653131e33aba1bbf5c88c341`（PR #301 `feature/LS-195-report-push-owner-only` → development，3 commit：`fa72e71`／`76dacbb`／`1fcb9da`）——`supabase/migrations/20260905040711_report_recipients_owner_only.sql`（新檔）、`supabase/tests/103_push_dispatch.sql`（4d 段）、`docs/API.md` §10。唯讀核對，未 checkout／未碰容器與 lock（DB 面驗證依賴既有 QA `e919cab1`／merge-review `945840af` 的實跑記錄）。

### Findings

未發現本 feature 引入的死碼。逐項核對：

1. **函式體是否逐字沿用（無殘留舊版本）**：把新 migration 的 `notification_recipients` 定義與來源 `20260904212530_suspension_and_registrations.sql:811-840` 逐行比對，唯一差異是 WHERE 子句多一行 `and (ne.kind <> 'report' or fm.role = 'owner')`（merge-reviewer 已用 `diff -u` 核對過，我複核一致）；沒有殘留的舊版本函式定義、沒有被取代卻仍留著的分支或註解掉的舊條件。
2. **測試新案（4d 段）是否有跳過／永真**：`supabase/tests/103_push_dispatch.sql` 新增的 4d 段有 7 個獨立 `if v_n <> N then raise exception` 斷言（report／actor=member 恰 1 列＝owner、owner 在其中、member 不在、viewer 不在、actor 本人不在、owner 自報 0 列、對照組 album 仍 5 列且 viewer 在內），每條指向具體數字而非只驗「有無報錯」；commit message 記錄了 mutation 驗證（拿掉新條件重跑 → 4d 段紅在「應為 1 筆，實際 4」），QA（`e919cab1`）與 merge-reviewer（`945840af`）各自獨立重跑過一次得到相同紅字與綠字，非自我宣稱。未發現空洞斷言或被跳過的案例。
3. **grant／revoke 是否留下無效條目**：新 migration 只有 `revoke execute on function public.notification_recipients(uuid[]) from public, anon;`，沒有額外或孤兒的 grant/revoke；比對來源函式同一行寫法，屬既定 pattern（新函式對 PUBLIC 預設零權限，`service_role` 依 `20260822120300_harden_default_privileges.sql` 的既有慣例取得存取），非本票殘留。
4. **「其餘四種 kind」計數口誤**（`supabase/migrations/20260905040711_report_recipients_owner_only.sql:6,14`、`docs/API.md:2447`）：以 `git show <sha>:<path> | grep -n` 確認三處都寫「其餘四種 kind」卻各自列了 5 個值（comment／reaction／diary／album／media）；核對 `notification_kind` enum 全部歷史（`20260825020000` 建立 4 值 + `20260903091313` 加 `report` + `20260904170920` 加 `media`，共 6 值，扣 `report` 本身確實是 5 種）。**此問題已由 merge-review N2（`945840af`）記錄為 informational 並入池，本報告不重複列為新 finding**，僅確認未被遺漏處置。

### 既有觀察（非本 feature 引入的死碼，彙整供 orchestrator 對照——皆已由 reviewer 追蹤，非本次新開）
- merge-review N1（minor）：`supabase/functions/push-dispatch/handler.ts:251-255`（本票未動）的 report 推播文案「你的{label}收到一則檢舉」在 owner-only 收斂後語意不精準（owner 常會收到「別的成員的內容被檢舉」的通知，「你的」不成立）。票文明寫 handler.ts 不動，reviewer 判定不擋合併；建議併入 LS-189 檢舉文案處理或記入 LS-96。
- merge-review N3（informational）：WHERE 子句先套 owner-only 再套封鎖排除，owner 若封鎖了檢舉人，該則 report 事件的收件人會是 0 列（推播完全不發；`content_reports` 資料本身不遺失，owner 仍可透過 LS-189 inbox 讀到）。待使用者對「檢舉是否該豁免封鎖排除」做一次產品決定，已入池。
- merge-review N4（informational）：`docs/API.md` §10 既有文件債——收件人排除清單本來就漏了「排除停權使用者」（`pr.suspended_at is null`，LS-172 既有邏輯）這條 bullet，非本票引入；建議與 N2 合併成一張極小文件票補齊。

### 結論
LS-195 引入的 SQL 遷移與 DB 測試無死碼殘留；「四種／實列 5 值」計數口誤與其餘 informational 已由 reviewer 入池，本報告未重複列為新 finding。

### 搜尋方法與盲區
- 方法：`git show fa72e71`／`76dacbb`／`1fcb9da` 逐檔核對三個 commit 各自的變更範圍；`git diff` 新舊函式定義；`grep -n` 核對 `notification_kind` enum 的全部歷史 `ALTER TYPE ADD VALUE`（確認實際 6 值）；`grep -n` 核對 `notification_events` 的 RLS policy／table grant 與兩支 RPC 的 execute grant（確認 `docs/API.md` 讀取面聲稱屬實）；交叉核對 QA `e919cab1` 與 merge-review `945840af` 的實跑輸出。
- 盲區：本輪未對活資料庫實際執行 `supabase db reset`／`run.sh`（依指示不碰容器／lock），完全依賴 QA／merge-reviewer 已完成的實跑記錄與靜態 diff 交叉核對，未獨立重跑；純文字搜尋抓不到 `push-dispatch/handler.ts` 執行期對收件人集合的實際處理行為（該檔本票未動，其語意風險已由既有 N1 finding 帶出，非本次動態驗證範圍）。

### 2026-09-05T05:10:36.674Z · Chih-Lin Yeh · `6005e5c2`

**orchestrator 2026-09-05 13:1x 收尾儀式 → Done**

- 併入：PR #301 → development `b1f714f`；promote dev→test→main；tag **v0.13.3**（與 LS-186 同版）。delta QA PASS `e919cab1`（三角色 recipients 直查、authenticated 無讀取路徑、run.sh 全綠）。
- **正式站**：`supabase db push` 只推 `20260905040711_report_recipients_owner_only.sql` → Applied；回讀 `pg_get_functiondef(notification_recipients)` 含 `fm.role = 'owner'`＝true、`schema_migrations` 已登錄。
- sweeper `39f01526`：0 件死碼。
- lesson learning：(1) 第一次 `db push --dry-run` 印「up to date」是主 checkout 尚未 pull 到 main tip、migration 檔不在工作樹——部署前先 `git pull` 再 dry-run（已記 notes）；(2) reviewer N3（owner 封鎖檢舉人 → 0 收件人）是規則交互產生的產品問題，已升使用者決定；(3) 推播文案與收件人規則耦合（N1）→ 併進 LS-189 文案一起定。
- cleanup：worktree 12:54 已清；informational N1 併 LS-189、N2／N4 入池 `9f56908b`。

