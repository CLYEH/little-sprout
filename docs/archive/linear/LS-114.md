# LS-114 設計 chore：`cmp/Approval Status`（`PXPcH`）AX3 錯位 196pt——alignItems center 缺陷，與 LS-111 R2-F2 同型（既有元件，R2 裁決三定另票）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Low |
| 標籤 | lane:design |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M1 登入與家庭 |
| 估點 | — |
| 建立 | 2026-08-31T01:43:12.074Z |
| 開始 | 2026-09-01T00:32:12.988Z |
| 完成 | 2026-09-01T00:53:17.319Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-114/設計-chorecmpapproval-statuspxpchax3-錯位-196pt-alignitems-center-缺陷與-ls |
| 關係 | related→LS-119, related→LS-108, related→LS-111, related←LS-154, related←LS-127, related←LS-124, related←LS-122, related←LS-120, related←LS-96 |

## 描述

來源：[LS-111](https://linear.app/little-sprout-app/issue/LS-111/設計-chore07-邀請家人-角色選擇列一般成員只能看白話說明選中態ls-46-定稿只有單一產生鈕ls-107-以既有-token-暫拼) R2 裁決三（既有元件缺陷不在該票 scope，定另票）；R3 審查實測更正錯位量為 **196pt**（R2 引用的 183pt 為舊值）。

## 問題

`cmp/Approval Status`（節點 `PXPcH`）在 AX3 下 icon 與文字錯位 196pt，根因與 [LS-111](https://linear.app/little-sprout-app/issue/LS-111/設計-chore07-邀請家人-角色選擇列一般成員只能看白話說明選中態ls-46-定稿只有單一產生鈕ls-107-以既有-token-暫拼) R2-F2 同型：`alignItems:"center"` 在多行大字下把 icon 拉到垂直中點。[LS-111](https://linear.app/little-sprout-app/issue/LS-111/設計-chore07-邀請家人-角色選擇列一般成員只能看白話說明選中態ls-46-定稿只有單一產生鈕ls-107-以既有-token-暫拼) R3 已修同型的 `cmp/Role Status`（`shJ48`，移除 center 後 0pt），修法可直接沿用。

## 驗收條件

- [ ] `PXPcH` 移除／修正 `alignItems:"center"`，AX3 板 Get 量測 icon 與文字頂對齊誤差 ≤2pt
- [ ] 一般字級版面無迴歸（Get 量測前後一致）
- [ ] 使用該元件的全部稿面溢出掃描零新增
- [ ] visual-reviewer 覆核通過（單項 chore 可一輪）

證據：`.claude/evidence/LS-111/r3-review/` 內截圖；[LS-111](https://linear.app/little-sprout-app/issue/LS-111/設計-chore07-邀請家人-角色選擇列一般成員只能看白話說明選中態ls-46-定稿只有單一產生鈕ls-107-以既有-token-暫拼) comment `56f804be-9aba-4746-81df-929ce322bed1`。

## 留言（5）

### 2026-08-31T08:14:29.383Z · Chih-Lin Yeh · `2148f5b5`

**補位暫緩（2026-08-31 巡檢，orchestrator 判斷）**：patrol 建議本票補位 design lane（cycle 外唯一候補），但 orchestrator 判定**暫不派**——本票 Low、cosmetic（單一 `PXPcH` AX3 對齊、pre-existing、非阻塞），而設計 chore 會佔用單一 Pen＋走 ≥3 輪 visual-review 迴圈（參 LS-111 走了 13 輪）；同時 LS-108 即將進 QA 需讀 .pen 做角色列視覺驗收。以 Low cosmetic 票佔 Pen、擋更高價值的家庭流程 QA 不划算。待家庭流程 QA 收斂、Pen 空出後再派（維持 Backlog，不進 Ready 避免巡檢反覆標「無人接」）。

### 2026-09-01T00:37:49.455Z · Chih-Lin Yeh · `9e78f054`

## 已完成

`PXPcH`（`cmp/Approval Status`）沿用 LS-111 R3 對 `shJ48`/`D56s3k` 的同型修法：`Update(PXPcH, {alignItems:"start"})`——由於 `"start"` 是 schema 預設值，Get 回讀時該屬性直接從 JSON 消失（與 `D56s3k` 現況位元對位一致，非僅視覺等效）。

## 已驗證（Get 實測，前後量測）

**AX3 板（`fY5Qk`，A11y / 07 邀請家人 · Dynamic Type AX3）**
- 修前：icon `sE4ZI` y=212 vs 文字區塊 `mXQJh` y=16 → 誤差 **196pt**（與票文根因描述一致）
- 修後：icon y=16 vs `mXQJh` y=16 → 誤差 **0pt**（≤2pt 過）

**一般字級三個使用點（`nUKrL`／`X1bki`／連帶 `eHOUw`/`cGpi1`/`sc3ds`）**
- 修後：icon y=16，`mXQJh` y=16，誤差 0pt，容器外框高度（h=115／h=90）不變——僅內部 icon 由「跨軸置中」變「頂對齊」，屬 LS-111 R3 已對 `shJ48` 建立且經 R3 核可的同型行為，非新缺陷。screenshot 見 `.claude/evidence/LS-114/r1/`（`fY5Qk.png`／`nUKrL.png`／`X1bki.png`）。

**溢出掃描（六個使用點所屬五張稿面）**
- `kcGJa`／`CHLio`／`NRLL3`／`VRq1c`：0 命中
- `XhwG6`（設計語言 Tokens 稿）：4 命中，皆為「角托三段規則」demo 的四角托（`Corner TL/TR/BL/BR`），與 `Approval Status` 無關、屬既有角托白名單，**零新增**

## 未完成
無（單項 chore，驗收條件四項已逐條驗證，待 visual-reviewer 一輪覆核）

## 風險
無。屬性層級變更，未動節點數／未動子樹結構；已確認與 `D56s3k` 現況一致，行為有 LS-111 R3 先例核可。

## 產出位置
- Pen 路徑：`/Users/clyeh/little-sprout/.claude/worktrees/LS-114/design/littlesprout.pen`（開工核對通過，全程正確；收工已 `pen-open.sh` 切回主 checkout，exit 0）
- pen-land.sh：exit 0，結構 diff：`節點 PXPcH 屬性變更：['alignItems']`；節點總數 3644 → 3644（不變）；meta 不變
- Branch：`feature/LS-114-approval-status-ax3`（已 push）
- Head SHA：`5cc45ae700eacf5e47119013e6f3605b33d38140`
- Evidence：`.claude/evidence/LS-114/r1/`（`fY5Qk.png`／`nUKrL.png`／`X1bki.png`）

## Skill 影響的取捨
- `little-sprout-brand`：確認角托溢出屬既有白名單（十條「不可協商」#4 角托規則）而非新迴歸，避免誤判為新缺陷；本票不涉色彩／字級/母題新決策，主要作用是判讀溢出掃描結果。
- `frontend-design`：本票為既有元件屬性層級修復（單一 alignItems 值），無新版面決策空間，未產生額外取捨；如常規載入。

### 2026-09-01T00:44:09.828Z · Chih-Lin Yeh · `440b46f8`

## visual-reviewer 第 1 輪：**APPROVE**

（票文授權單項 chore 一輪收斂；本輪所有數字由我自行 Get 量測，未沿用 comment `9e78f054` 的申報值。）

- `little-sprout-brand` skill：**已載入**（含 `references/slop-forbidden.md` 20 條）
- Pen 路徑核對：`/Users/clyeh/little-sprout/.claude/worktrees/LS-114/design/littlesprout.pen`（`get_app_state` 回讀＝目標 worktree，非快取；節點數 **3644** 相符，非陳舊的 3501）

### 這份修改的靈魂

這不是「把 icon 挪正 196pt」，是**把一條規則補回它漏掉的那個元件**。`VRq1c`（07 AX3）上 `shJ48`（Role Status）與 `fY5Qk`（Approval Status）是兩列結構完全相同的 icon＋雙行文字列；LS-111 R3 修好了前者、漏了後者，於是同一張畫面上兩列長得一樣的東西對「icon 該站哪裡」給出相反答案——正是 slop 禁例 #18「板與稿互打」的縮小版。這一行的刪除讓兩列重新說同一句話。

### 逐項驗收（四條全過）

**1. `PXPcH` alignItems 已修，AX3 誤差 ≤2pt** ✅
`Get("PXPcH")` 回讀：`alignItems` 屬性**不存在**（＝ schema 預設 `start`），與 `D56s3k`（`cmp/Role Status`）現況同一表示法，逐屬性比對兩者 frame 層一致（`gap`/`padding` 皆 `$sp-item`、icon 22×22、文字 frame `fill_container` vertical）——「位元對位一致」屬實。

| 使用點 | 所在稿面 | icon y | 文字區塊 y | **誤差** |
|---|---|---|---|---|
| `fY5Qk` (AX3) | `VRq1c` | 16 | 16 | **0pt** |
| `nUKrL` | `kcGJa` 07 | 16 | 16 | 0pt |
| `X1bki` | `NRLL3` 07b | 16 | 16 | 0pt |
| `eHOUw` | `CHLio` 07c | 16 | 16 | 0pt |
| `cGpi1` / `sc3ds` | `XhwG6` Tokens | 16 | 16 | 0pt |

修前 196pt 的申報值我用算術獨立覆核：`alignItems:center` 下 icon y ＝ 16＋(414−22)/2 ＝ **212**，212−16 ＝ **196**。與票文與 comment 數字逐位吻合——申報非事後補寫（對照 slop #7「假驗證斷言」，本輪未中槍）。

**2. 一般字級無迴歸** ✅
抽驗 `nUKrL`（h=115）與 `X1bki`（h=90），另外三處一併量測。外框高度不變**可證明而非僅觀察**：容器 `fit_content`，跨軸高 ＝ padding 16＋max(icon 22, 文字塊)＋16；`nUKrL` 16+83+16=115、`X1bki` 16+58+16=90 逐項對上。`alignItems` 只決定跨軸**擺放**、不參與跨軸**定尺**，故 07／07b／07c 三態間不可能因此位移——符合十條 #10「狀態切換不搬動版面」。截圖複驗兩張，icon 與標題首行光學對齊，無碰撞。

**3. 溢出掃描零新增** ✅（用兩支掃描＋截圖，非單靠 `ctx.problems`）
`ctx.problems` 掃描：`kcGJa`／`CHLio`／`NRLL3`／`VRq1c` ＝ **0**；`XhwG6` ＝ 4，全是 `Demo Photo Wrap` 內的 `Corner TL/TR/BL/BR`。
依 slop #7「`ctx.problems` 對兄弟碰撞無感」，我另跑一支**兄弟矩形交集**掃描：前四張 ＝ **0**；`XhwG6` ＝ 25，全部落在 `Demo Print`／`Fade Pools`／`Corner *`／`Print × Mount`——即十條 #4「角托一律壓過紙緣 `corner-out` 5pt」與染料池母題，屬既有白名單。**與 `Approval Status` 零交集，零新增成立。**

**4. 結構 diff 只動一個屬性** ✅（檔案層機械證明，非申報轉述）
`git diff --numstat 5cc45ae^ 5cc45ae` ＝ `0 1 design/littlesprout.pen`——**新增 0 行、刪除 1 行**，該行為 `PXPcH` 屬性區塊內夾在 `"padding": "$sp-item",` 與 `"children": [` 之間的 `"alignItems": "center",`。節點數 3644→3644，meta 未動。這是本票能拿到的最強結構證據：單行刪除在物理上排除了任何附帶變更。

### 對抗性複查（我主動去找、但不成立的疑點）

- **「AX3 下 22pt 的鎖頭配 40pt 的字，是不是這次改壞的？」** 不是。我掃了 `VRq1c` 全部 11 顆 icon，與一般字級板 `kcGJa` **逐顆同尺寸**（狀態列 18／返回 26／膠囊 18／角色列 22／鎖 22／三顆按鈕 22）——該板 icon 一律不隨 Dynamic Type 放大，是板級既有慣例，非本票引入，且與正上方已核可的 `shJ48` 完全一致。
- **靈魂測試／長輩硬約束**：文案 17pt（`$fs-body`＋`$fs-note`）承載「新成員一律要你核准才能進來／為了保護家人隱私…」這種必讀內容，未落入 slop #11「13pt 雜物間」；icon 中性色、零 accent（十條 #1 未通膨）；非沖印品故零角托（#4 正確）；本票未動色彩／字級／母題任一決策。

### 值得保留（重做時勿倒洗澡水）

1. **屬性刪除而非改寫成 `"start"`**——靠 schema 預設表達，使 `PXPcH` 與 `D56s3k` 在 JSON 層真正同形；日後任何「兩列為何不一致」的掃描都會直接對上，而不是要人比對兩個等價字面值。
2. **只改元件、不碰六個實例**——一次修正六處，且不在實例層留下任何覆寫（覆寫會變成下一個 LS-111 式的漏網點）。
3. **未順手「優化」相鄰的間距或 icon 尺寸**——單行 diff 讓這次修改在未來的 blame 裡永遠可讀。

### informational（不阻擋本票）

- **`i1`｜證據落在主 checkout**：comment `9e78f054` 申報的 `.claude/evidence/LS-114/r1/` 實際位於**主 checkout**（`/Users/clyeh/little-sprout/.claude/evidence/LS-114/r1`），worktree 內不存在。兩者皆已被 `.gitignore:21` 忽略、無 repo 污染，且不影響本輪裁決（我已於 `.claude/evidence/LS-114/r1-review/` 自行產出並複驗）。→ 建議記入待辦池 **LS-96**（證據路徑應 worktree 相對）。
- **`i2`｜AX3 板 icon 縮放慣例不一致**：`AX3-10` 曾把 tab bar icon 22→48（Handoff `MM3wn`），而 `VRq1c`（AX3-07）全板 icon 維持預設值。稿是 ios-dev 的契約，SwiftUI 的 SF Symbol 在 Label 內本會隨 Dynamic Type 放大，兩塊 AX3 板給出不同示範會留下解讀空間。**先於本票、與本票無因果**，且涉及非本票 scope 的元件。→ 建議記入待辦池 **LS-96**，不另開票。

### 結論

四條驗收條件全部由我獨立量測通過，變更面積為單一屬性、單行 diff、節點數不變，且修正了一處跨元件的規則漏套。通用 slop 十條與專案版 20 條逐條對照無中槍。**APPROVE**，可開 design-only PR 併入。

證據：`.claude/evidence/LS-114/r1-review/`（`fY5Qk.png`／`nUKrL.png`／`X1bki.png`／`VRq1c.png`）

### 2026-09-01T00:51:55.400Z · Chih-Lin Yeh · `06075307`

## merge-reviewer R1（design-only 輕量 scope 審）：**APPROVE**

審的 head：`5cc45ae700eacf5e47119013e6f3605b33d38140`（PR #194 → `development`）
分工聲明：深度視覺審查已由 visual-reviewer R1 完成（comment `440b46f8`，APPROVE）。本輪**未開 Pen MCP**（LS-119 正在用單一 Pen），僅做 scope＋機械正確性——所有結論來自 git blob 與 CI，不轉述申報值。

### 1. Scope：乾淨（符合手術式修改原則）

```
$ git diff --numstat origin/development...5cc45ae
0	1	design/littlesprout.pen
```
- 檔案數 **1**、新增 **0** 行、刪除 **1** 行；`--name-status` ＝ `M`（無新增／刪除／改名檔）。
- `git log origin/development..5cc45ae` ＝ **單一 commit**，訊息 `design(pen): LS-114 cmp/Approval Status（PXPcH）移除 alignItems:center 修 AX3 錯位 196pt`，第一行格式合規（`<type>(<scope>): LS-<n> <摘要>`）。
- 刪除的那一行我看了 `-U6` 的實際 hunk：`"alignItems": "center",`，夾在 `"padding": "$sp-item",` 與 `"children": [` 之間，該 frame 首個子節點為 icon `sE4ZI`（Lock Icon）——與票文指名的 `PXPcH` 對得上。
- **零 Swift／零 SQL／零 migration／零 script 變更**，無夾帶重構、無順手改動。scope findings：**0**。

### 2. 機械正確性（我獨立跑，未沿用 comment `9e78f054`／`440b46f8` 的數字）

| 檢查 | 方法 | 結果 |
|---|---|---|
| 衝突殘留標記 | `git show 5cc45ae:design/littlesprout.pen \| grep -cE '^(<<<<<<<\|=======\|>>>>>>>)'` | **0** |
| JSON 可解析 | `json.load` 於 head blob | **OK**（top keys：`version`／`children`／`themes`／`variables`／`fileToken`） |
| 節點數不變 | 同一支 walker 跑 base 與 head | **3666 → 3666**（不變） |
| `PXPcH` 屬性 | head blob 解析 | `alignItems` **不存在**（＝ schema 預設 `start`），children ＝ `['sE4ZI','mXQJh']` |
| 姊妹元件同形 | 同上比對 `D56s3k`（`cmp/Role Status`） | `alignItems` 亦 **不存在** → 兩者 JSON 層真正同形，非等價字面值 |
| PR 可併狀態 | `gh pr view 194` | `mergeable: MERGEABLE`、`mergeStateStatus: **BLOCKED**` —— **非 CONFLICTING**；BLOCKED 係 required check `merge-review` 尚未貼（本 comment 後即貼），非衝突 |
| worktree／branch 一致 | `git status --porcelain` 於 `.claude/worktrees/LS-114` | 空（無未提交殘留）；local tip ＝ `origin/feature/...` tip ＝ `5cc45ae` |

**量測方法差異說明（非 finding）**：申報值「節點數 3644」與我的 walker 數出的 3666 不同，係計數規則差異（我計「同時具 `id` 與 `type` 的 dict」；`pen-land.sh` 應另有規則）。本票要證的是 **before == after 不變**，兩種規則各自內部一致且皆成立，故不構成問題——但請勿把 3644 當成可跨工具引用的常數。

### 3. CI：全綠（等到結論才裁決）

`gh pr checks 194`（run `33456088143`，等到 settled，未在 pending 狀態下裁決）：
```
ci     pass  4m37s
db     pass  3m01s
lint   pass  14s
rules  pass  3m18s
```
四支全 pass。（`lint` 的 Node 20 deprecation／Homebrew tap trust 為 runner 環境 annotation，非本 PR 引入，job 結果 pass。）

### 4. 四維度適用性（誠實聲明）

- **Race condition**：N/A——diff 內無 Swift、無並行程式碼、無 Supabase 呼叫。
- **運算效能**：N/A——無 SQL／RLS policy／查詢／圖片路徑變更；刪除一個版面屬性無執行期成本。
- **平行優化**：N/A——無可平行化的工作被序列化之處。
- **Scope**：唯一實質適用維度，如 §1，**通過**。

### 未審範圍（明講）

- **視覺／版面正確性**（AX3 對齊 ≤2pt、一般字級迴歸、溢出掃描）：**我沒審**，未開 Pen、未做 Get 量測、未看截圖。依派工分工，此部分由 visual-reviewer R1（`440b46f8`）背書；我只驗證「該輪所依據的檔案內容與 PR head 是同一份」——`PXPcH` 的 `alignItems` 確已在 `5cc45ae` 消失，與該輪回讀一致。
- **DB 測試**：未實跑 `supabase db reset`／`tests/run.sh`——diff 無 migration，且 CI `db` job 已 pass。

### findings

**0 blocker、0 major、0 minor。** 沒有硬湊——這是單行刪除的 design-only diff，能出問題的面在物理上就很小，而該面（scope、衝突、JSON 完整性、節點數、CI）我逐項驗過都是乾淨的。visual-reviewer 已列的 `i1`（證據落在主 checkout）／`i2`（AX3 板 icon 縮放慣例）我同意皆為 informational、不阻擋本票，處置沿其建議記入待辦池 **LS-96**，本輪不新增。

## Verdict：**APPROVE**（無 blocker／major）

裁決 commit status 已依 §2 綁 `5cc45ae700eacf5e47119013e6f3605b33d38140`（`--expect` 核對當下 headRefOid 相同）。可由 orchestrator 併入 `development`。

### 2026-09-01T00:53:13.386Z · Chih-Lin Yeh · `33cdd22a`

**R1 APPROVE(visual `440b46f8`＋merge-review `06075307`)→ 已併 development `0811eea`(2026-09-01)→ 收尾**。

**Retro(設計 chore 輕量版)**:單項小修一輪收斂——「票文授權單輪」對這種同型已修過的缺陷是對的節奏(對照 LS-111 的 13 輪,分級審查省了大量輪次);修法選「刪屬性回 schema 預設」而非寫死值,讓姊妹元件 JSON 層同形、未來一致性掃描可機械對上,是好慣例。reviewer i2(AX3 板 icon 縮放慣例不一致)已記池、標註 LS-119 大設計輪順帶統一。design-only 免 dead-code。→ Done。**注意:LS-119 分支切在本票併入前,其 .pen 未含本行修正——LS-119 push 時 merge-conflict-check 會驗,屆時併回即可。**

