# LS-203 Task：LS-20 iOS——相簿 tab 列表改讀 `album_summaries` view（`visible_media_count`／`latest_thumb_path`／`latest_storage_path`／`cover_thumb_path`／`cover_storage_path`），拿掉 PostgREST 內嵌 aggregate 與連結列計數口徑；`AlbumsContentAssembler` 退路改用 view 欄位

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:S, lane:ui |
| Cycle | 2 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M3 相簿與上傳 |
| 估點 | — |
| 建立 | 2026-09-05T09:48:52.674Z |
| 開始 | 2026-09-12T06:02:40.177Z |
| 完成 | 2026-09-12T11:25:14.958Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-203/taskls-20-ios-相簿-tab-列表改讀-album-summaries-viewvisible-media |
| 父票 | LS-20 Story：相簿與照片上傳（壓縮／縮圖／背景續傳） |
| 關係 | related→LS-211, related→LS-204, related→LS-205, related→LS-209, related→LS-230, related→LS-165, related→LS-167, related→LS-166, related←LS-146, related←LS-224, related←LS-218, related←LS-217, related←LS-96, blocks←LS-200 |

## 描述

## 來源

[LS-200](https://linear.app/little-sprout-app/issue/LS-200/taskls-20-後端-相簿摘要-view-album-summariessecurity-invokervisible-media)（`album_summaries` security-invoker view）已併 development `40bdeac`（merge-review R2 `0f56dba9`）。[LS-165](https://linear.app/little-sprout-app/issue/LS-165/taskls-20-ios-相簿-tab-首頁相簿卡片列表扇影厚度分級空狀態新增相簿依-ls-142-稿實作含深色ax3ipad-兩欄) R3 暫以 `album_media(count)`＋`latest:album_media(media!inner(...))` 內嵌查詢併入，口徑差異（張數含軟刪連結列）記在 `docs/API.md` 與程式註解，[LS-165](https://linear.app/little-sprout-app/issue/LS-165/taskls-20-ios-相簿-tab-首頁相簿卡片列表扇影厚度分級空狀態新增相簿依-ls-142-稿實作含深色ax3ipad-兩欄) 收尾 `c11a1a0a` 明列本票為後續。

## 範圍

1. `SupabaseAlbumsAPIClient.fetchAlbums`：改 `from("album_summaries")` 讀 view 欄位（keyset 分頁欄位 `created_at`,`id` 不變、`deleted_at is null` 過濾不變）；移除 `album_media(count)` 與 `latest:` 內嵌、`LatestAlbumMediaEntry` 等只為內嵌存在的解碼型別。
2. `AlbumsContentAssembler`：張數＝`visible_media_count`；封面優先序 `cover_thumb_path ?? cover_storage_path ?? latest_thumb_path ?? latest_storage_path ?? 灰底`；`latest_media_id` 保留供詳情頁（[LS-166](https://linear.app/little-sprout-app/issue/LS-166/taskls-20-ios-相簿詳情瀑布流照片牆加入照片入口寶貝標記改名與刪除相簿依-ls-142-稿實作含深色ax3ipad34-張壓測)）。
3. `createAlbum` 的 INSERT 回傳仍走 `albums`（view 不可寫）——插入後以 view 重讀該列或組出 summary（不得讓新建相簿短暫顯示錯誤張數）。
4. 測試：既有 55 個 [LS-165](https://linear.app/little-sprout-app/issue/LS-165/taskls-20-ios-相簿-tab-首頁相簿卡片列表扇影厚度分級空狀態新增相簿依-ls-142-稿實作含深色ax3ipad-兩欄) 單元測試改為 view 形狀的 fixture（含「最新可見無縮圖 → storage_path 退路」「cover 不可見 → latest」「全隱形 → 0／灰底」）；UITests 不變；tap-target gate 不變。
5. `docs/API.md` §4 移除 [LS-165](https://linear.app/little-sprout-app/issue/LS-165/taskls-20-ios-相簿-tab-首頁相簿卡片列表扇影厚度分級空狀態新增相簿依-ls-142-稿實作含深色ax3ipad-兩欄) 兩則口徑差異註記（[LS-200](https://linear.app/little-sprout-app/issue/LS-200/taskls-20-後端-相簿摘要-view-album-summariessecurity-invokervisible-media) R1 刻意保留待本票除役）。

## 不做

相簿詳情（[LS-166](https://linear.app/little-sprout-app/issue/LS-166/taskls-20-ios-相簿詳情瀑布流照片牆加入照片入口寶貝標記改名與刪除相簿依-ls-142-稿實作含深色ax3ipad34-張壓測)）；view 欄位變更（後端）。

## 驗收

- [ ] 單元測試綠、UITests 綠、push gate 綠；本機 Supabase 兩帳號實測：軟刪一張後張數即時正確、封面退路四段各一次
- [ ] merge-reviewer 通過（效能：一趟查詢、payload 只含 summary 欄；scope：不動 [LS-167](https://linear.app/little-sprout-app/issue/LS-167/taskls-20-ios-上傳佇列-sheet沒有成功正在進行已完成三群ls002-容量已滿列最前重試與背景續傳固定-detent依-ls) 檔）；QA PASS

## 留言（14）

### 2026-09-12T06:02:43.662Z · Chih-Lin Yeh · `9da7200c`

**orchestrator 09-12 14:03**：ui lane 空 1（LS-230 併入 dev 轉 QA）→ 補位本票（cycle 2、Ready、blockedBy LS-200 Done；同優先序候補中最早建票）。worktree `.claude/worktrees/LS-203`＝`feature/LS-203-album-summaries-view`（自 development `69e5f20`），派 ios-dev。注意 LS-166 R2 同時在改 `SupabaseAlbumsAPIClient.swift`（改名路徑），本票只動 `fetchAlbums`／`AlbumsContentAssembler`／測試／API.md，push 前若 development 前進先 merge。→ In Progress。

### 2026-09-12T07:16:08.690Z · Chih-Lin Yeh · `af949558`

## Ticket

LS-203：相簿 tab 列表改讀 `album_summaries` view（`visible_media_count`／`latest_thumb_path`／`latest_storage_path`／`cover_thumb_path`／`cover_storage_path`），拿掉 PostgREST 內嵌 aggregate 與連結列計數口徑；`AlbumsContentAssembler` 退路改用 view 欄位。

## 已完成

1. **`SupabaseAlbumsAPIClient.fetchAlbums` 改讀 view**（`b0918a1`，`LittleSprout/Services/Albums/SupabaseAlbumsAPIClient.swift:40-63`）：`from("album_summaries")`，`listSelect` 改成扁平的六個彙總欄＋`id/title/created_at`；keyset 分頁（`created_at`,`id`）與 `deleted_at is null` 過濾不變；移除 `.order(referencedTable: "latest")`／`.limit(referencedTable: "latest")` 這類只服務內嵌形狀的修飾詞。`AlbumListingRow` 改成扁平欄位對映，移除 `LatestAlbumMediaEntry`／`LatestAlbumMedia`／`AlbumMediaCountEntry` 三個只為內嵌解碼存在的型別（`LittleSprout/Services/Albums/AlbumsModels.swift:40-88`）。
2. **`AlbumsContentAssembler` 封面優先序改讀 view 欄位**（同 commit `b0918a1`，`LittleSprout/Services/Albums/AlbumsContentAssembler.swift:36-40`）：`cover_thumb_path ?? cover_storage_path ?? latest_thumb_path ?? latest_storage_path ?? nil`（灰底），不再呼叫 `fetchMedia` 反查 `cover_media_id`——view 已把可見性判準與路徑一次算完，移除對應的平行查詢與 `explicitCoverIds`/`coverMediaTask` 邏輯。`AlbumSummary` 新增 `latestMediaId` 欄位（`AlbumsModels.swift:143`，預設 `nil`，供 LS-166 相簿詳情使用），本票不消費。
3. **`createAlbum` 插入後向 view 重讀**（`58a38f4`，`SupabaseAlbumsAPIClient.swift:120-137`）：`album_summaries` 不可寫，INSERT 仍走 `albums`（只取 `id`），再向 `album_summaries` 依該 id 重讀取得跟 `fetchAlbums` 一致的摘要形狀——剛建立的相簿必定 0 張照片，不會短暫顯示錯誤張數。
4. **測試 fixture 改用扁平形狀**（`c3818b6`）：`AlbumsContentAssemblerTests`／`SupabaseAlbumsAPIClientTests`／`AlbumsStoreTests`／`StubAlbumsAPIClient` 改用 view 六個彙總欄的 fixture（含「最新可見無縮圖→storage_path 退路」「cover 不可見→latest」「全隱形→0／灰底」三案，見票文驗收）；新增 `test_neverCallsFetchMedia` 鎖住「封面優先序不再需要反查 media 表」；新增 `test_createAlbum_insertsIntoAlbumsThenRereadsFromView` 鎖住兩步驟；移除已不成立的 LEFT JOIN `{"media": null}` 迴歸測試（該形狀隨內嵌查詢一併消失）。
5. **`docs/API.md` 除役三則舊筆記**（`76c6781`）：LS-165 R2 留下的「相簿列表依賴 db-aggregates-enabled」「相簿張數口徑：計的是連結列」「封面 fallback 的 latest 內嵌用 media!inner」三則筆記皆已描述完全移除的舊 iOS 程式碼路徑（不只票文明列的兩則，多刪了第三則，見「未完成」段說明），一併移除；`album_summaries` 段落的「待本票除役」forward-reference 改成「LS-203 已落地」的說明。

## 已驗證（怎麼驗的）

- **單元測試**：`xcodebuild test -only-testing:LittleSproutTests`（`LS-203-iPhone17Pro`）**923 個全綠**，含本票 `SupabaseAlbumsAPIClientTests` 13／`AlbumsContentAssemblerTests` 11／`AlbumsStoreTests` 14／`AlbumThicknessTierTests` 8／`AlbumSignatureFormatterTests` 11。對應驗收條件「單元測試綠」。
- **UI 測試**：`xcodebuild test -only-testing:LittleSproutUITests -skip-testing:LittleSproutUITests/QASmokeTests` **125 個全綠**（1 skip，既有非本票測試），含 `AlbumsViewTests` 3／`TapTargetGateTests` 35——票文寫既有 UI 測試與 tap-target gate 不變，本票不動 UI，仍全部重跑複驗綠燈，非只憑代碼推論。
- **SwiftLint**：`swiftlint lint --quiet` 0 warning／0 error，輸出存 `/private/tmp/claude-501/-Users-clyeh-little-sprout/d51d774f-d40e-420c-b428-e43c62514076/scratchpad/LS-203-swiftlint.log`（`EXIT: 0`）。
- **`api-contract-check.sh`**：綠（RPC 40 支、表 25 張，`docs/API.md` 與 schema 一致）。
- **push gate**：`git push` 觸發 `bash scripts/gates/push-gate.sh`（unit tests＋tap-target gate 皆含在內）綠燈通過，已 push；不等 CI。
- **本機 Supabase 兩帳號實測**（票文驗收：「軟刪一張後張數即時正確；封面退路四段各一次（截圖＋SQL／REST 查詢原文）」，`.claude/evidence/LS-203/r1/`）：
  - lock：`cd /Users/clyeh/little-sprout/.claude/worktrees/LS-203 && bash scripts/ops/supabase-lock.sh --hold "LS-203 dev E2E" --max-minutes 20`（同一條命令鏈），**持有 16 分 9 秒**，已 `--release`。
  - `bash scripts/ops/supabase-lock.sh -- supabase db reset` 後，用 GoTrue Admin API（`SERVICE_ROLE_KEY`）建 A（owner／uploader，`ls203-a@ls.test`）／B（member，`ls203-b@ls.test`）兩個真實帳號＋一個家庭（`bash scripts/ops/supabase-lock.sh -- docker exec ... psql` 以 service-role 直接組 5 本相簿 fixture，繞過 `family_members_insert` 的 `WITH CHECK(false)`，同過去 LS-165／LS-200 review 的既有做法）。
  - **封面退路四段，逐段以 B 的真實 JWT 打 `GET /rest/v1/album_summaries`**（`.claude/evidence/LS-203/r1/fallback-before-softdelete.txt`，查詢原文含在檔內）：
    - **A（`cover_thumb_path` 優先）**：`cover_thumb_path` 非 null（`.../albumA-cover-thumb.jpg`），即使 `latest_thumb_path` 另有值（較新一張 `.../albumA-newer-thumb.jpg`）也不採用——見 `.claude/evidence/LS-203/r1/fallback-before-softdelete.txt` 第一筆。
    - **B（`cover_thumb_path` 為 null → 退 `cover_storage_path`）**：`cover_thumb_path: null`、`cover_storage_path: ".../albumB-cover-nothumb.jpg"`——見 `.claude/evidence/LS-203/r1/fallback-before-softdelete.txt` 第二筆。
    - **C（未指定封面 → 退 `latest_thumb_path`）**：`cover_thumb_path`／`cover_storage_path` 皆 null，`latest_thumb_path: ".../albumC-newer-thumb.jpg"`（較新一張）——見 `.claude/evidence/LS-203/r1/fallback-before-softdelete.txt` 第三筆。
    - **D（封面與 `latest_thumb_path` 皆缺 → 退 `latest_storage_path`）**：前三者皆 null，`latest_storage_path: ".../albumD-nothumb.jpg"`——見 `.claude/evidence/LS-203/r1/fallback-before-softdelete.txt` 第四筆。
  - **軟刪即時性**（`.claude/evidence/LS-203/r1/softdelete-realtime.txt`）：A 用真實 JWT 對 Album E 最新一張下 `PATCH /rest/v1/media?id=eq.<id>` 軟刪（`media_update` policy 允許 uploader）；B **同一次後續請求**（非等 purge）立即重查同一本相簿，`visible_media_count` 從 2 即時變 1，`latest_media_id`／`latest_thumb_path`／`latest_storage_path` 正確回退到剩下那張可見照片。
  - **模擬器主流程實測**（DoD #3）：`SIMCTL_CHILD_LS_QA_API_URL`／`SIMCTL_CHILD_LS_QA_ANON_KEY` 指向本機容器＋B 用帳號密碼審核登入方式（`qa.passwordSignIn.*` 既有測試入口）登入 → 導覽到相簿 tab → 5 本相簿標題與張數皆與上方 REST 驗證一致（`.claude/evidence/LS-203/r1/albums-tab-C-B.png`／`albums-tab-A-E.png`：「C-未指定封面退最新縮圖 · 2 張相片」「B-封面無縮圖退原圖 · 1 張相片」「A-封面縮圖優先 · 2 張相片」「E-軟刪即時性 · 1 張相片」——E 的軟刪即時性同時在畫面上覆核）。封面本身因測試路徑非真實 Storage 物件（`createSignedURLs` 簽名失敗），畫面統一顯示灰底佔位圖，四段優先序何者生效無法只憑截圖分辨，以上方 REST 回應為準（見「風險」段）。
- **模擬器已關**：`D240605C-F864-4117-BE1D-D9F930D97095`（`LS-203-iPhone17Pro`，`xcrun simctl shutdown` 已執行，`xcrun simctl list devices` 確認狀態為 Shutdown）。

## 未完成

- **reviewer informational 處置**：本 PR 尚未經 merge-reviewer 審查，暫無待處置項；R1 review 回來後逐條處置（已修／記入 LS-96／另票／不修＋理由），一條不省。
- **自我發現的一項（非 reviewer 提出，主動記錄，非票文列出的「兩則」之一）**：`docs/API.md` 這次多刪了第三則「封面 fallback 的 `latest` 內嵌用 `media!inner`」筆記——票文字面只寫「移除兩則」，但這第三則同樣完整描述已被本票整段移除的舊 embed 查詢（`latest:album_media(media!inner(...))`），留著會與現行程式碼矛盾（描述不存在的查詢），因此一併清掉；已在 commit `76c6781` message 與 PR body 明列，請 merge-reviewer 確認這個判斷是否合理，若認為超出範圍可指出、回退這一小段容易。

## 風險

- **模擬器 runtime 非 pinned 版**：`detect-simulator.sh` 本機找不到釘住的 iOS 26.2，fallback 用 iOS 26.0（CI 為 iOS 26.2；push gate 印「pinned 26.2」警告）。本票未動版面／量測，理論上無影響，仍照規約註明。
- **E2E 截圖的封面圖片本身未渲染**（灰底而非縮圖）：測試 fixture 用假造的 `storage_path`（不對應真實 Storage 物件），`createSignedURLs` 對不存在物件簽名失敗，`AlbumsContentAssembler` 的 `signed[$0]` 查無結果、`cover` 退回 `nil`，四本相簿視覺上一致（皆為灰底 `photo.stack` 佔位圖）。四段優先序的正確性以 REST 回應（`album_summaries` 六欄位原始值，逐字對應 `AlbumsContentAssembler.displayPath(for:)` 讀的欄位）驗證，這是比截圖更貼近程式邏輯的驗證層級；若需要看得到真實縮圖的視覺驗收，需要透過完整上傳流程建立真實 Storage 物件（不在本票範圍，相簿詳情／封面設定 UI 屬 LS-166）。
- `AlbumListingRow` 移除 `coverMediaId` 欄位（原本只服務已刪除的 `fetchMedia` 反查邏輯，現在無任何消費者）——若日後 LS-166 需要知道「這本相簿是否有明確指定封面」（跟「顯示哪張圖」是不同的資訊），需要重新加回這個欄位，屆時另評估。

## 產出位置

- PR：https://github.com/CLYEH/little-sprout/pull/361（`feature/LS-203-album-summaries-view` → `development`）
- Head SHA：`cd9332809fc0a13ac2a745d61248fa53e5d546a3`
- Commits：`b0918a1`（API client＋assembler）／`58a38f4`（createAlbum 重讀）／`c3818b6`（測試 fixture）／`76c6781`（API.md）／`cd93328`（併入 origin/development，無衝突）
- 證據：`.claude/evidence/LS-203/r1/`（`fallback-before-softdelete.txt`／`softdelete-realtime.txt`／`albums-tab-C-B.png`／`albums-tab-A-E.png`）
- 模擬器已關：`D240605C-F864-4117-BE1D-D9F930D97095`

### 2026-09-12T07:17:25.715Z · Chih-Lin Yeh · `044afd0b`

**orchestrator 09-12 15:1x**：R1 handoff `af949558`——PR #361 head `cd93328`：`fetchAlbums` 改讀 `album_summaries` 六欄、`createAlbum` INSERT 後 view 重讀、`AlbumListingRow` 扁平化並移除三個內嵌解碼型別與 `coverMediaId`、assembler 四段退路；API.md 刪三則過期註記（第三則票文未點名，agent 已揭露）；923 單元／125 UI 綠；E2E 兩帳號 5 相簿四段退路＋軟刪即時計數（真 JWT＋REST）。→ In Review，已派 merge-reviewer R1（含與 LS-166 #356 重疊檔的併入順序建議）。

### 2026-09-12T07:55:13.404Z · Chih-Lin Yeh · `677ea237`

## merge-review R1 — LS-203（PR #361 `feature/LS-203-album-summaries-view` → `development`）

**Verdict：REQUEST_CHANGES**（blocker 0／major 1／minor 2／informational 7）

- 審的 head：`cd9332809fc0a13ac2a745d61248fa53e5d546a3`（`gh pr view 361 --json headRefOid` 當下同值）
- base：`origin/development` `94b4a5468ea6d2054d505f0579f6c35d688e6c6f`（即 merge-base；`cd93328` 是乾淨 merge，`git diff-tree --cc cd93328` 無內容）
- 範圍：10 檔 +274／−353（5 支 app 檔、4 支測試檔、`docs/API.md`）
- Supabase lock：在 `.claude/worktrees/LS-203` 內 `--hold LS-203 merge-review R1`，**持有 2 分 9 秒**，已 `--release`
- commit status：`merge-review=failure` 已貼 `cd93328`（status id 54038944322）

### 四維度結論

- **Race condition**：1 major（`createAlbum` 由一次 INSERT+RETURNING 拆成兩個請求後，中間失敗沒有退路／補償，見 major-1）。其餘乾淨：`AlbumsStore` 的 `generation`／`createAlbumGeneration` 守門未動；`AlbumsContentAssembler` 是無狀態 enum；`AlbumListingRow`／`AlbumSummary` 仍是 `Sendable` 值型別，新增欄位不引入共享可變狀態。
- **運算效能**：`fetchAlbums` 確為一趟查詢，`listSelect` 只列 9 欄（`id,title,created_at` ＋六個彙總欄），沒有內嵌 aggregate／embed，也沒有 `latest.order=`／`latest.limit=` 修飾詞；keyset（`created_at`,`id`）與 `deleted_at is null` 述詞逐字未變（`LittleSprout/Services/Albums/SupabaseAlbumsAPIClient.swift:27-51`）。view 端的 plan 由 LS-200 的 `supabase/tests/107_album_summaries.sql` 第 6 段釘住（iOS 實際查詢形狀、無 media／album_media Seq Scan、無非 hashed 逐列 correlated SubPlan），本票未改 SQL，RLS 面無退化。另見 minor-2（`createAlbum` 多一次 RTT）。
- **平行優化**：1 minor（兩支互不相依的批次請求被序列化，見 minor-1）。
- **Scope**：未觸碰 LS-167 上傳佇列檔、未觸碰相簿詳情（LS-166）任何檔；10 個改動檔全部落在票文範圍 1–5。`docs/API.md` 多刪的第三則判定為合理，不要求回退（i3）。

---

### major-1 — `LittleSprout/Services/Albums/SupabaseAlbumsAPIClient.swift:116-131`：INSERT 成功後 view 重讀失敗時沒有退路，整個建立被回報成失敗、相簿卻已存在且無補償

票文 Scope 3 允許「重讀該列**或**組出 summary」，本 PR 選了重讀，但兩個請求之間新開的失敗窗口沒有任何處理：

**具體失敗情境**（可用既有 `TestSupabaseClient` mock 決定性重現：第一個 POST `/rest/v1/albums` 回 201，第二個 GET `/rest/v1/album_summaries` 回 500／連線中斷）：

1. 使用者在「新增相簿」sheet 填標題、選寶貝、按建立。
2. `INSERT into albums` 已 commit（該列真的存在於 DB）。
3. 第二個請求（`from("album_summaries").eq("id", …).single()`）失敗——行動網路在兩個請求之間斷線、PostgREST 5xx，或任何讓 `.single()` 拿不到那一列的情況。
4. `createAlbum` 直接 `throw AppError.map(error)` → `AlbumsStore.createAlbum` 落到最外層 catch → `createAlbumState = .failure`；**不會**呼叫 `setAlbumChildren`、**不會**走既有的 `setAlbumDeleted` 補償、**不會** `refresh`（`LittleSprout/Services/Albums/AlbumsStore.swift` 的 `createAlbum`）。
5. `CreateAlbumView.submit()` 因 `succeeded == false` 不 dismiss，畫面顯示錯誤（`LittleSprout/Features/Albums/CreateAlbumView.swift:181-188`）。使用者以為沒建成 → 重按一次 → 兩本同名相簿，其中一本沒有任何寶貝標記；下拉重新整理後那本「失敗的」相簿會冒出來。

兩點讓這條不是理論值：(a) 同一條呼叫鏈往下 20 行就有既有的補償慣例（`setAlbumChildren` 失敗 → `setAlbumDeleted` 軟刪剛建立的相簿，merge-review R1 M2 留下的設計），這次新開的窗口沒有比照；(b) 改動前是單一請求（INSERT + `select(listSelect).single()`），窗口只在「回應遺失／解碼失敗」時存在，本 PR 把它擴大成「第二個請求任何失敗」。

**建議修法**（擇一，皆 3–5 行）：

- 首選：INSERT 改 `.select("id,title,created_at")`，重讀失敗時 fallback 成本地組出的 `AlbumListingRow(id:title:createdAt:)`（`photoCount` 0、四個路徑欄 nil）。這不是「重複 view 的 coalesce 邏輯」——剛建立的相簿必定沒有 `album_media` 連結列、`cover_media_id` 為 NULL，我在本機實跑確認 view 對新建列回的就是 `visible_media_count=0` 與五個 NULL 路徑欄（見「已驗證」第 5 項）；順帶讓 minor-2 一起消失。
- 次選：維持重讀，但失敗時比照既有慣例 `try? await setAlbumDeleted(albumID:deleted:true)` 再回報失敗，讓「使用者看到的失敗」與「DB 狀態」一致。

不論哪一種，請補一條鎖住此行為的單元測試（第二個請求失敗 → 仍回傳可用的 row，或相簿已被補償軟刪）。

### minor-1 — `LittleSprout/Services/Albums/AlbumsContentAssembler.swift:20-27`：兩支互不相依的批次請求改成序列，每頁多付一趟 RTT

`fetchMedia` 拿掉之後，`signedURLs` 的輸入只由 `rows` 決定（`displayPath(for:)` 純讀欄位），與 `fetchAlbumChildren` 已經完全沒有依賴關係；但程式仍是「先 await 寶貝連結、再 await 簽名」。改動前這兩件事是平行的（`async let childLinksTask` / `async let coverMediaTask`，原註解明寫「平行發出省 RTT——同 `TimelineContentAssembler.fetchDiaryContents` m5 的既有理由」），而當時簽名確實必須等 `fetchMedia`；現在那個理由消失了，平行化反而更容易，卻被一起拿掉。

**具體影響**：`AlbumsStore.refresh` 與每次 `loadMore` 各多一趟序列 RTT（相簿 tab 首屏與每次觸底載入）。**建議**：`async let childLinksTask = apiClient.fetchAlbumChildren(albumIds: ids)`、`async let signedTask = signedURLs(forPaths: …)`，再 `let (childLinks, signed) = try await (childLinksTask, signedTask)`。

### minor-2 — `LittleSprout/Services/Albums/SupabaseAlbumsAPIClient.swift:126-132`：重讀回來的六個彙總欄目前沒有任何消費端

`AlbumsStore.createAlbum` 只用 `created.id`（傳給 `setAlbumChildren`／`setAlbumDeleted`），成功後呼叫 `refresh(familyID:)` 整頁重抓；`CreateAlbumView` 只看 `Bool`。也就是說重讀的 payload（張數與四個路徑欄）從頭到尾沒有被畫出來過，票文「不得讓新建相簿短暫顯示錯誤張數」在現行 store 流程下由 `refresh` 保證，不是由重讀保證。這一趟 RTT 目前是純成本，且正是 major-1 的成因。若採 major-1 的首選修法（本地組出 row、重讀失敗時退回），這條自然消失；若堅持保留重讀，請在程式註解寫清楚它服務的是未來（樂觀插入列表）而非現況。

### informational（逐條編號，處置請列在下一輪 handoff 的「未完成」段）

- **i1**：`AlbumSummary.latestMediaId`（`LittleSprout/Services/Albums/AlbumsModels.swift:139`）本票無消費端，票文 Scope 2 明列保留給 LS-166；轉發已由 `AlbumsContentAssemblerTests` 的 latestMediaId 轉發測試釘住。**收尾 dead-code-sweeper 請勿把它當死碼移除**。
- **i2**：`AlbumsAPIClient.fetchMedia` 在本 PR 併入後，app 端暫時沒有 production 呼叫端（只剩協定＋三個實作＋測試）。LS-166 PR #356 的 `AlbumDetailStore.swift:65` 與 `:105` 會重新用到，**同樣不要移除**。
- **i3**：`docs/API.md` 多刪的第三則（`latest` 內嵌 `media!inner` 的 null 安全）判定**合理、不必回退**：該筆記整段描述的是本票已整段刪除的 embed 查詢，留著會描述不存在的程式碼；沿革也沒斷——新段落逐字列出被除役的三則是哪三則。順帶查過全樹已無其他 PostgREST 內嵌 aggregate 用法，`db-aggregates-enabled` 依賴確實隨本票消失。
- **i4**：E2E 證據的 REST 查詢用 `order=created_at.asc` 且沒帶 `deleted_at=is.null`／keyset `or=(...)`，與 app 實際送出的 query string 不同。目前 app 的 query string 由 `SupabaseAlbumsAPIClientTests` 釘住（SDK 層），SQL 述詞形狀由 `supabase/tests/107_album_summaries.sql` 釘住（DB 層），中間「完整 query string 打 PostgREST 對這支 view」沒有任何機械驗證。本輪我也未能補上（見「未涵蓋」第 2 點），風險評估為低（`deleted_at`／`family_id`／`created_at`／`id` 皆由 `a.*` 展開，我已在 SQL 層逐欄用過）。若要補，建議記入 LS-96 而非另票。
- **i5**：模擬器 runtime 非 pinned（handoff 申報 fallback iOS 26.0、pinned 26.2）。本票不含版面／量測變更、UITests 未改，CI `ci-ipad` 已在 pinned runtime 綠，判定不影響結論；仍照 LS-205 註明。
- **i6**：handoff 的行號引用與 head 實際行號有偏移（`SupabaseAlbumsAPIClient.swift` 申報 40-63／實際 27-51、申報 120-137／實際 104-133；`AlbumsContentAssembler.swift` 申報 36-40／實際 43-45；`AlbumsModels.swift` 申報 143／實際 139）。內容皆屬實，只是行號停在中途版本——下次請在最終 head 重算，或直接引用符號名。
- **i7**：張數口徑改變的 UI 副作用：`AlbumThicknessTier(photoCount:)` 現在吃「可見張數」，含不可見照片的相簿扇影厚度可能比 LS-165 時期低一級。這是本票要的修正，**請在轉 QA 時告知 qa**，避免被當成回歸。

### 與 LS-166 PR #356（head `b44de02`）的重疊與併入順序建議

重疊檔 6 支：`AlbumsAPIClient.swift`／`AlbumsModels.swift`／`SupabaseAlbumsAPIClient.swift`／`PreviewAlbumsAPIClient.swift`／`StubAlbumsAPIClient.swift`／`AlbumsStoreTests.swift`（`AlbumsStore.swift` 只有 #356 改，本票未動）。

- **文字衝突：無**。`git merge-tree --write-tree b44de02 cd93328` exit 0，寫出 tree `4673e492`。兩邊 hunk 位置錯開（#356 動 `SupabaseAlbumsAPIClient.swift:170+`／`214+`、`AlbumsModels.swift:124` 附近新增 `AlbumMediaLinkRow`；本票動 `:8-51`／`:104-133` 與 `AlbumListingRow`／`AlbumSummary`）。
- **語意衝突：1 處，合併後編不過**。#356 新增的 `LittleSprout/TapTargetGateHarness+Albums.swift:234`（新的 stub client）呼叫 `AlbumListingRow(id:title:coverMediaId:createdAt:)`，而本票已移除 `coverMediaId` 參數；該檔本票完全沒碰，所以文字合併乾淨、編譯才爆。#356 diff 裡唯一新增的 `coverMediaId:` 就是這一行（其餘 `coverMediaId:` 出現處都是 development 既有行、本票已改寫，合併時取本票版本，不需人工處理）。
- **`latestMediaId` 定義只有一邊**：本票 `AlbumsModels.swift:33`（row）與 `:139`（summary），#356 沒有同名欄位、也沒動 `AlbumSummary`，不會出現兩份定義。#356 所有 `AlbumSummary(...)` 呼叫端都是具名參數且順序與本票自訂 init 相同（`latestMediaId` 有預設值），可編譯。
- **`fetchMedia` 的去留**：#356 會重新用到（見 i2），本票之後請勿移除。
- **建議順序：先 #361（修完 major-1）再 #356**。#361 是 S 票、改動面小、已到最後一輪；#356 併前 `merge development` 並改那一行（拿掉 `coverMediaId: nil`）＋重跑 build 與 unit／UI 測試即可。反序也可行，成本相同（屆時換 #361 來 merge development 修同一行），但 #356 還在 R2，先併大的會讓 #361 多等一輪。

### 已驗證（怎麼驗）

1. handoff「已完成」1–5 逐條對應 commit 與 head 原始碼：`b0918a1`（`LittleSprout/Services/Albums/SupabaseAlbumsAPIClient.swift`＋`AlbumsContentAssembler.swift`＋`AlbumsModels.swift`＋`AlbumsAPIClient.swift`＋`PreviewAlbumsAPIClient.swift`）／`58a38f4`（`SupabaseAlbumsAPIClient.swift:104-133`）／`c3818b6`（四支測試檔）／`76c6781`（`docs/API.md` −31／+5）——每個 commit 的 `--stat` 檔案清單與申報一字不差。
2. 封面優先序四段各一測試，皆在 `LittleSproutTests/AlbumsContentAssemblerTests.swift`：cover 縮圖優先（同時給 latest 兩欄當誘餌、斷言只簽 cover 縮圖）／cover 縮圖 nil 退 cover 原圖／cover 兩欄 nil 退 latest 縮圖／只剩 latest 原圖／四欄全 nil 不發簽名請求；另有 `test_neverCallsFetchMedia` 鎖住不再反查 media 表。逐一讀過斷言，任何一段順序寫反都會紅（誘餌欄位都填了值）。
3. 移除的 `LatestAlbumMediaEntry`／`LatestAlbumMedia`／`AlbumMediaCountEntry`／`AlbumListingRow.coverMediaId` 無殘留呼叫端：全樹搜尋 0 命中；`LittleSprout/Services/Timeline/TimelineModels.swift:81` 的 `coverMediaId` 是 Timeline 自己的 `AlbumRow`，與本票無關。
4. **本機 Supabase 實跑重放（我自己跑，不採信 handoff）**——`bash scripts/ops/supabase-lock.sh --hold` 內：`supabase db reset` → 灌 `supabase/tests/00_fixtures.sql` → 以 family A 的 viewer（非上傳者）身分、帶 `deleted_at is null` 與 keyset 述詞查 `album_summaries`，得 `visible_media_count=2`、`cover_storage_path` 為封面那張、`latest_media_id` 為較新那張 → 以上傳者身分（走 `media_update` RLS）軟刪較新那張 → 同一個 viewer 立即重查得 `visible_media_count=1`、`latest_*` 正確回退到剩下那張，而同一列的 `album_media` 連結列數仍是 2（證明口徑真的從連結列改成可見張數，不是巧合）。
5. `createAlbum` 兩步驟的 happy path（對應 major-1 的建議修法）：在 `bash scripts/ops/supabase-lock.sh --hold` 的同一個 lock 內以 member 身分 `INSERT into albums`，第二個 statement 依該 id 讀 `album_summaries`（view 定義：`supabase/migrations/20260905074037_album_summaries_view.sql`），得 `visible_media_count=0`、`latest_media_id`／`latest_thumb_path`／`latest_storage_path`／`cover_thumb_path`／`cover_storage_path` 五欄皆 NULL——即「本地組出的 row」與「重讀」對新建相簿完全等價，major-1 的首選修法不會產生口徑漂移。
6. handoff 的 E2E 證據原文查核：`.claude/evidence/LS-203/r1/fallback-before-softdelete.txt` 五本相簿的六個彙總欄逐筆對得上四段退路（A cover 縮圖／B cover 原圖／C latest 縮圖／D latest 原圖）；`.claude/evidence/LS-203/r1/softdelete-realtime.txt` 軟刪後 2→1 且 latest 回退；兩張截圖 `.claude/evidence/LS-203/r1/albums-tab-A-E.png`／`albums-tab-C-B.png` 顯示的張數與上述 REST 回應一致（E 為 1 張）。
7. 與 #356 的合併試算：`git merge-tree --write-tree b44de02 cd93328` exit 0；語意衝突逐行定位在 `LittleSprout/TapTargetGateHarness+Albums.swift:234`（見上一節）。
8. CI（`gh run view 34680114570`）：全綠——`ci` success 34m32s／`ci-ipad` success 9m18s／`db` success 3m38s／`lint` success 18s／`rules` success 7m5s；run 整體 `completed / success`。

### 未涵蓋（誠實揭露）

- 未自行重跑 923 單元／125 UI 測試（`xcodebuild test` 全量）——以 PR run 的 `ci` job 結論為準（已 success），不重複燒機。本輪只靜態讀過本票新增／改寫的測試斷言（見「已驗證」第 2 項）。
- 未在 REST 層重放 app 的完整 query string：取本機 anon key／JWT secret 的命令被權限分類器擋下，改以 SQL 層等價述詞（`family_id` ＋ `deleted_at is null` ＋ keyset ＋ `order by created_at desc, id desc limit 20`）重放，見 i4。
- 未做 mutation 重放：handoff 未申報任何 mutation（LS-209 的重放義務不觸發）。
- 未做版面／tap-target 量測：本票不改 UI，UITests 與 `scripts/gates/tap-target-exemptions.txt` 皆未動。

### 下一輪

修 major-1（並視修法決定 minor-2 是否一併消失）、決定 minor-1 是否採納，informational i1–i7 逐條在 handoff「未完成」段列處置（已修／記入 LS-96／不修＋理由），重新推 head 後我對新 head 重審重貼 `merge-review` status。

### CI 補述（已回填）

PR run `34680114570`（head `cd93328`）：**五個 job 全綠**（`ci` success 34m32s、`ci-ipad`、`db`、`lint`、`rules` 皆 success），run 整體 `completed / success`。本輪 REQUEST_CHANGES 與 CI 無關——major-1 是靜態可證的失敗路徑，現有測試沒有覆蓋到它（這正是要求補一條單元測試的原因）。

### handoff-evidence-check

```
bash scripts/gates/handoff-evidence-check.sh <verdict.md>
✓ 第 72–79 行起的列項皆有證據（8 個列項全過）
gate-exit=0
```

### 2026-09-12T07:58:58.489Z · Chih-Lin Yeh · `4e665ba5`

**orchestrator 09-12 16:0x**：merge-review R1 `677ea237` **REQUEST_CHANGES**（major-1 `createAlbum` INSERT→view 重讀兩段、重讀失敗無補償致重複建相簿；minor-1 assembler 序列 await 多一趟 RTT；minor-2 重讀六欄無消費端；7 info；CI 五綠；lock 2m9s）。reviewer 的併入順序建議「先 #361 再 #356」已過時——#356（LS-166）已於 15:28 併入 development `938e854`，因此本票須 merge development 並修 LS-166 `TapTargetGateHarness+Albums.swift:234` 對 `coverMediaId:` 的語意衝突（文字無衝突、編譯會爆）。已派 ios-dev R2（INSERT `.select` 本地組出列＋移除重讀 RTT、`async let` 還原、合併 dev＋修 harness 呼叫、mutation、E2E 重放）。

### 2026-09-12T08:41:15.804Z · Chih-Lin Yeh · `74f37df6`

## Ticket

LS-203：相簿 tab 列表改讀 `album_summaries` view（R2——修 merge-review R1 REQUEST_CHANGES，comment `677ea237`：1 major／2 minor／7 informational）。

## 已完成

1. **合併 `origin/development`（`938e854`，含 LS-166 #356）＋語意衝突修正**（`405acd8432a6b639aafaf3437b47c2c43cf1482d`）：文字合併無衝突；語意衝突——`LittleSprout/TapTargetGateHarness+Albums.swift:234` 對 `AlbumListingRow(id:title:coverMediaId:createdAt:)` 的呼叫改為新簽名（拿掉 `coverMediaId:`）。`xcodegen generate` 重生後 pbxproj 與合併結果零差異。
2. **major-1 + minor-2**（`81c13d57ec30690641cd96b2e6f30a68a2b55041`）：`createAlbum` 的 INSERT 改 `.select("id,title,created_at")` 直接取回三欄，本地組出等價的 `AlbumListingRow`（`visible_media_count=0`＋五個 NULL 路徑欄），不再向 `album_summaries` 發第二個請求——消除「INSERT 已 commit、重讀失敗就整個 throw」的窗口（`LittleSprout/Services/Albums/SupabaseAlbumsAPIClient.swift:104-133`）；重讀六欄無消費端的 minor-2 隨之消失。`CreatedAlbumIDRow` 改名 `CreatedAlbumRow`（補 `title`／`createdAt`）。
3. **minor-1**（`0acbeb988e518ebb597621ab2fed784fde9c920a`）：`AlbumsContentAssembler.assemble` 的 `fetchAlbumChildren` 與 `signedURLs` 改回 `async let` 平行發出（`LittleSprout/Services/Albums/AlbumsContentAssembler.swift:14-41`），同 `TimelineContentAssembler.fetchDiaryContents` m5 既有寫法。
4. **測試更新**（`c711e3cc1b12b99416595b1059b38648567ba752`）：`test_createAlbum_insertsIntoAlbumsThenRereadsFromView` 換成 `test_createAlbum_insertsWithSelectThenBuildsRowLocally_withoutSecondRequest`（`LittleSproutTests/SupabaseAlbumsAPIClientTests.swift:170-215`），鎖住「INSERT 的 `select=` 只取三欄」＋「全程只有一個 REST 請求」。

## 已驗證（怎麼驗的）

- **驗收條件「單元測試綠」**：`xcodebuild test -only-testing:LittleSproutTests`（`LS-203-iPhone17Pro`）**954/954 全綠**（含合併進來的 LS-166 `AlbumDetailStoreTests`／`AlbumPhotoGridLayoutTests`）。
- **驗收條件「UI 測試綠」**：`xcodebuild test -only-testing:LittleSproutUITests -skip-testing:LittleSproutUITests/QASmokeTests`（同機）**134/134 全綠、1 skip**（既有非本票測試），含 `AlbumsViewTests`／`TapTargetGateTests`（35→含 LS-166 新增項）／`AlbumDetailAX3UITests`／`AlbumDetailViewTests` 全數重跑複驗綠燈，log 存 scratchpad/LS-203-ui-r2.log。全套跑了 1063 秒（超過前景 10 分鐘上限，工具自動移至背景，完成後我立刻核對 log，未輪詢等待）。
- **push gate（tests＋lint）**：`git push origin feature/LS-203-album-summaries-view` 觸發 `push-gate.sh`——api-contract／tap-target-registry／error-codes／migration 撞號／migration-immutable／分支票號乾淨／與 development 可乾淨合併／unit tests 皆綠，`✓ push gate 通過`，已 push（`cd93328..c711e3c`）。
- **SwiftLint**：`swiftlint lint --quiet` exit 0，0 warning／0 error，log 存 scratchpad/LS-203-swiftlint-r2.log。
- **`api-contract-check.sh`**：綠（RPC 40 支、表 25 張）。
- **build**：`xcodebuild build`（同機）exit 0，0 error。
- **mutation（major-1，LS-209）**：改了什麼——暫時把 `createAlbum` 退回 major-1 修法之前的兩步驟設計（INSERT 只取 `id`→向 `album_summaries` 重讀，未落地成 commit，驗完立刻用備份還原，`git status`／`git diff HEAD` 復原後皆為空）。哪條測試紅——`SupabaseAlbumsAPIClientTests.test_createAlbum_insertsWithSelectThenBuildsRowLocally_withoutSecondRequest`。斷言訊息原文：
  ```
  SupabaseAlbumsAPIClientTests.swift:188: error: ... XCTAssertTrue failed - select 應只取三欄，實際 query：select=id
  SupabaseAlbumsAPIClientTests.swift:182: error: ... XCTAssertEqual failed: ("Optional("/rest/v1/album_summaries")") is not equal to ("Optional("/rest/v1/albums")") - 本地組出後不應該再向 album_summaries 發第二個請求（merge-review R1 major-1）
  SupabaseAlbumsAPIClientTests.swift:186: error: ... XCTAssertEqual failed: ("Optional("GET")") is not equal to ("Optional("POST")")
  SupabaseAlbumsAPIClientTests.swift:189: error: ... XCTUnwrap failed: expected non-nil value of type "Data"
  ```
- **驗收條件「本機 Supabase 兩帳號實測：建立相簿（含寶貝標記）→ 列表立即顯示 0 張＋正確標記；軟刪一張後張數即時正確」**：`.claude/evidence/LS-203/r2/r2-e2e.txt`。連線通道：host `curl` 直打 `http://127.0.0.1:54321`（REST／Auth）＋`docker exec supabase_db_little-sprout psql`（service-role 建 fixture，繞過 `family_members_insert` 的 `WITH CHECK(false)`）。lock：`LS-203 dev E2E`，**持有 6 分 41 秒**，已 `--release`。
  - A（owner/uploader）用真實 JWT 對 `POST /rest/v1/albums?select=id,title,created_at` 建相簿（與 `SupabaseAlbumsAPIClient.createAlbum` 目前送出的請求完全同款）→ `set_album_children` 標記寶貝 → B（member，非建立者）立即重查 `album_summaries`：`visible_media_count=0`、`latest_media_id`／`latest_thumb_path`／`latest_storage_path`／`cover_thumb_path`／`cover_storage_path` 五欄皆 `null`（與本地組出的 `AlbumListingRow` 等價）；`album_children` 查得到標記列（原文見 `.claude/evidence/LS-203/r2/r2-e2e.txt`）。
  - attach 2 張 media 後 `visible_media_count=2`；A（uploader）`PATCH /rest/v1/media` 軟刪一張 → B 同一次後續請求立即重查，`visible_media_count` 即時變 1、`latest_media_id` 正確回退；同一列 `album_media` 連結列數仍是 2（原文見 `.claude/evidence/LS-203/r2/r2-e2e.txt`）。
- **模擬器主流程實測（DoD #3）**：本輪 R2 未改動 UI／畫面流程（範圍限資料層與 API client），主流程覆核已含在上方 E2E 的 REST 層直接驗證（`createAlbum` 送出的請求形狀、view 回應）；R1 handoff 的模擬器登入導覽相簿 tab 截圖（`.claude/evidence/LS-203/r1/albums-tab-*.png`）在本輪未重跑，因為畫面與資料流程本身未變，只有 `createAlbum` 內部實作與組裝平行度改變，皆由上方單元測試＋REST E2E 覆蓋。

## 未完成（reviewer informational 逐條處置）

- **i1**（`AlbumSummary.latestMediaId` 本票無消費端）：不修——`AlbumsModels.swift:139` 維持保留供 LS-166 使用，轉發測試持續釘住，本輪未觸及。
- **i2**（`fetchMedia` 本票內暫無 production 呼叫端）：已因合併 development 自然解決——`AlbumsAPIClient.fetchMedia` 現由 LS-166 的 `AlbumDetailStore.swift:65`／`:105` 呼叫。
- **i3**（`docs/API.md` 多刪第三則筆記）：不修——reviewer 已判定合理，不需回退。
- **i4**（`album_summaries` 完整 query string 無機械驗證的縫隙）：記入待辦池 LS-96，comment id `ec45e3b4-2215-41d8-99c8-0be20a1e2c27`。
- **i5**（模擬器 runtime 非 pinned 26.2）：不修＋理由——本機仍無 iOS 26.2 pinned runtime，fallback iOS 26.0；本票不改版面／量測，UITests／tap-target gate 全數重跑複驗綠燈，判定不影響本輪結論。
- **i6**（R1 handoff 行號與 head 有偏移）：本輪處置——PR body／本 handoff 的檔案:行號皆直接對照本輪最終 head 重新核對，非程式改動、無對應 commit。
- **i7**（張數口徑改變使 `AlbumThicknessTier` 扇影可能降級）：不修＋理由——這是本票要的正確行為，R1 handoff 風險段已記載，轉 QA 時會在 Linear 狀態變更 comment 再次提醒 qa 避免誤判回歸。

## 風險

- 沿用 R1：模擬器 runtime 非 pinned 26.2（fallback iOS 26.0，本票不改版面／量測，理論上無影響）；`AlbumThicknessTier` 扇影可能因口徑改變而降級（i7，轉 QA 時會提醒）。
- 合併 development 後多出 LS-166（相簿詳情）約 20 支新檔案的間接風險面——本輪已重跑全量 unit（954）＋UI（134）測試複驗，無回歸；但相簿詳情本身的邏輯正確性屬 LS-166 範疇，不在本票驗證範圍內。

## 產出位置

- PR：https://github.com/CLYEH/little-sprout/pull/361（`feature/LS-203-album-summaries-view` → `development`）
- PR head SHA：`c711e3cc1b12b99416595b1059b38648567ba752`
- 合併（origin/development）commit SHA：`405acd8432a6b639aafaf3437b47c2c43cf1482d`
- R2 Commits：`405acd8`（併入 development＋語意衝突修正）／`81c13d5`（major-1／minor-2）／`0acbeb9`（minor-1）／`c711e3c`（測試更新）
- PR body：已用 `gh pr edit 361` 增量更新（新增「R2 處置表」段，既有段落原樣保留），`pr-body-check.sh --verify` 在本 worktree 內跑過，exit 0。
- LS-96 comment id：`ec45e3b4-2215-41d8-99c8-0be20a1e2c27`
- 證據：`.claude/evidence/LS-203/r2/r2-e2e.txt`
- 模擬器已關：`D240605C-F864-4117-BE1D-D9F930D97095`（`LS-203-iPhone17Pro`）

### 2026-09-12T08:44:13.116Z · Chih-Lin Yeh · `5224f73a`

**orchestrator 09-12 16:4x**：R2 handoff `74f37df6`——head `c711e3c`：合併 development `405acd8`（含 LS-166，修 `TapTargetGateHarness+Albums.swift:234` `coverMediaId:`）、major-1＋minor-2 `81c13d5`（INSERT `.select` 本地組列、無重讀）、minor-1 `0acbeb9`（`async let` 還原）、測試 `c711e3c`；954 單元／134 UI 綠；mutation 紅（原文）；E2E 重放 lock 6m41s（建相簿 0 張＋標記、軟刪即時）；i4 入池 `ec45e3b4`。已派 merge-reviewer R2 delta。

### 2026-09-12T09:16:27.911Z · Chih-Lin Yeh · `87f6b654`

## merge-review R2（delta）— LS-203（PR #361 `feature/LS-203-album-summaries-view` → `development`）

**Verdict：APPROVE**（blocker 0／major 0／minor 1／informational 5）

- 審的 head：`c711e3cc1b12b99416595b1059b38648567ba752`（worktree `.claude/worktrees/LS-203` `git fetch origin` 後 `git rev-parse HEAD` 同值；`gh pr view 361 --json headRefOid` 當下同值）
- base：`origin/development` `938e854c3ab81bcf469613edbc7e12bace1da27e`（＝merge-base，本票已把它併進來）
- R2 delta：`405acd8`（併入 development＋語意衝突一行）／`81c13d5`（major-1＋minor-2）／`0acbeb9`（minor-1）／`c711e3c`（測試）；除 delta 外沿用 R1 `677ea237` 的結論
- Supabase lock：**未持有**（`--hold` 未使用）；本輪唯一的 DB 實跑以 `bash scripts/ops/supabase-lock.sh -- bash -c '<psql>'` 單一命令包裹，全程在一個 `begin; … rollback;` 交易內，**未 `db reset`、未留下任何資料**
- 模擬器：`LS-203-iPhone17Pro`（`D240605C-F864-4117-BE1D-D9F930D97095`）**iOS 26.0**——pinned 26.2 本機未安裝（`xcrun simctl list runtimes` 只有 26.0／26.5），與 handoff i5 申報一致，依 LS-205 註明；本輪只跑單元測試、不含版面／量測，判定不影響結論
- 開跑前 `pgrep -f xcodebuild`：有一支**別票**的在跑行程（pid 67228，`-project .../LS-217/LittleSprout.xcodeproj`，不同專屬機），未干預、未殺；我的重放用自己的 UDID 與獨立 `-derivedDataPath`

### R1 三項指名處置——逐項核實

| R1 項 | 修法 | 我怎麼驗 |
| --- | --- | --- |
| major-1（INSERT→view 重讀兩段無補償） | `81c13d5`：INSERT 改 `.select("id,title,created_at")`，本地組出 `AlbumListingRow`，第二個請求整個消失（`LittleSprout/Services/Albums/SupabaseAlbumsAPIClient.swift:104-133`） | 讀 head 原始碼＋自己重放 mutation（見下）＋自己在 lock 內以 SQL 重放「本地組出 ≡ view 回傳」等價性 |
| minor-2（重讀六欄無消費端） | 隨 major-1 消失——`CreatedAlbumIDRow`→`CreatedAlbumRow`（`:230-243`），回傳值仍只被 `AlbumsStore.createAlbum` 取 `.id` 用，但已不再為此多付一趟 RTT | `git show 81c13d5`；`LittleSprout/Services/Albums/AlbumsStore.swift:128-152` 未動，補償路徑（`setAlbumChildren` 失敗→`setAlbumDeleted`）原樣保留 |
| minor-1（序列 await） | `0acbeb9`：`async let childLinksTask` / `async let signedTask` ＋ `try await (…, …)`（`LittleSprout/Services/Albums/AlbumsContentAssembler.swift:14-41`） | 讀 head 原始碼；與既有先例 `LittleSprout/Services/Timeline/TimelineContentAssembler.swift:117-120` 逐行同形（同一 idiom、同一註解理由），非自創寫法 |

### 四維度（只對 delta）

- **Race condition：乾淨**。(a) `createAlbum` 現在只有一個網路請求，「INSERT 已 commit 卻回報失敗」的窗口縮回改動前的最小值（回應遺失／解碼失敗），且解碼目標從 9 欄的 `AlbumListingRow` 縮成 3 欄的 `CreatedAlbumRow`，解碼失敗面更小；`AlbumsStore.createAlbum` 的 `createAlbumGeneration` 守門與 `setAlbumDeleted` 補償一字未動。(b) `async let` 兩個子任務互不相依、無共享可變狀態（`AlbumsAPIClient` 宣告為 `Sendable`，`SupabaseAlbumsAPIClient` 是持 `SupabaseClient` 的 `final class`，測試用 `StubAlbumsAPIClient` 的狀態全包在 `OSAllocatedUnfairLock` 裡）；錯誤優先序不變（tuple `try await` 由左至右，仍是 `fetchAlbumChildren` 的錯誤先浮出），失敗時另一個子任務在 scope 結束隱式取消並等待，不外洩。專案是 `SWIFT_VERSION: "6.0"`（`project.yml:30`），我的重放編譯對 Albums 三支改動檔**零 concurrency 警告**（build log 內的 Sendable 警告全落在 `EditChildView.swift`／`VideoTrimmer.swift` 等本票未觸碰的既有檔）。
- **運算效能：改善**。建相簿由 2 個 REST 請求降為 1；相簿列表每次 `refresh`／`loadMore` 由 2 段序列降為 1 段平行。SQL／view／query string 一字未改（`fetchAlbums` 的 `listSelect`、keyset、`deleted_at is null` 與 R1 審過的相同），RLS 面無新退化。
- **平行優化：已無殘留序列化**。`AlbumsStore.refresh` 剩下的 `fetchAlbums → assemble` 是真資料相依，不可平行。
- **Scope：乾淨**。`git diff origin/development...c711e3c` 只有 11 檔（5 支 app 檔、4 支測試檔、`LittleSprout/TapTargetGateHarness+Albums.swift` 一行、`docs/API.md`）；`git diff 938e854 405acd8` 證明合併相對 development 只多出這 11 檔——**LS-166 的內容一行未被合併動到**（若被覆蓋會在此 diff 出現刪除）。`LittleSprout.xcodeproj/project.pbxproj` 相對 development **零差異**（xcodegen 重生結果與 development 一致，沒有夾帶 pbxproj 變更）。R2 三個 commit 各只碰 1 支檔（`--stat` 逐一核對），無順手改動。

### minor-1（R2 新增）— 協定契約註解與 `AlbumListingRow` init 註解仍描述已被本輪刪除的行為

`LittleSprout/Services/Albums/AlbumsAPIClient.swift:19-21`（協定檔開頭那張「方法 ↔ RPC／資料表對照（供 `docs/API.md` 對帳）」表）仍寫：

> `createAlbum` → INSERT `public.albums`（…）＋插入後向 `album_summaries` 重讀該列（view 不可寫，見票文 Scope 3）

`81c13d5` 已經把「插入後向 `album_summaries` 重讀該列」整段刪掉，這張表是這個 repo 指定用來跟 `docs/API.md` 對帳的來源，現在描述的是不存在的請求。同源的第二處：`LittleSprout/Services/Albums/AlbumsModels.swift:50` 的 init 註解仍寫「**供測試／`.preview()` 假資料建構**」，但這支 init 自 `81c13d5` 起也是 production 路徑（`SupabaseAlbumsAPIClient.swift:127-129`）唯一組出回傳列的地方，且它的 `photoCount: Int = 0` 與五個 `nil` 預設值**就是**「本地組出 ≡ view 回傳」這件正確性的承載處。

**具體失敗情境**（非當下 bug，是下一位讀者的誤判路徑）：(a) 有人依協定註解做 API.md 對帳／效能盤點，會以為相簿建立仍是兩趟請求，或反過來以為 `docs/API.md` 漏記了一支查詢；(b) 有人依 `AlbumsModels.swift:50`「供測試用」的字面去收緊這支 init（加 `#if DEBUG`、改預設值、或 dead-code 掃描時判定可刪），會直接改掉 production 行為——所幸 `LittleSproutTests/SupabaseAlbumsAPIClientTests.swift:173-215` 對 `photoCount == 0` 與五欄 `nil` 有斷言，會轉紅擋住，所以只列 minor 不列 major。

**建議修法**（兩行註解，不動程式碼）：`AlbumsAPIClient.swift:19-21` 改成「INSERT `public.albums` 並以 `select=id,title,created_at` 取回該列，於 client 端組出 `AlbumListingRow`（view 不可寫；不再重讀，merge-review R1 major-1）」；`AlbumsModels.swift:50` 補一句「亦為 `createAlbum` 本地組出新建列所用——預設值即『剛建立必為 0／NULL』的契約」。可在本票收尾順手改，或列入 handoff「未完成」的處置（不修＋理由亦可接受）——**不阻擋合併**。

### informational（處置請列在下一輪 handoff／收尾 comment 的「未完成」段）

- **i1（R1 i1 續存）**：`AlbumSummary.latestMediaId`（`LittleSprout/Services/Albums/AlbumsModels.swift:155-173`）在 LS-166 併入之後**仍然沒有** production 消費端（全樹 `.latestMediaId` 只命中模型自身與 `AlbumsContentAssembler.swift:38` 的轉發；LS-166 的 `AlbumDetailStore` 走自己的 `fetchAlbumMediaLinks`）。轉發由 `LittleSproutTests/AlbumsContentAssemblerTests.swift:38` 釘住。**收尾 dead-code-sweeper 請勿移除**，理由同 R1。
- **i2（R1 i2 已關閉）**：`fetchMedia` 的消費端已回來——`LittleSprout/Services/Albums/AlbumDetailStore.swift:65` 與 `:105`（我自己 grep 核實），handoff 申報屬實，此項結案。
- **i3**：`async let` 的副作用——`fetchAlbumChildren` 失敗時，簽名請求可能已經發出（多一次無用的 Storage 簽名呼叫）。無正確性影響（回傳值被丟棄、子任務被取消），且這正是既有 `TimelineContentAssembler` 先例的相同取捨，不建議為此加分支。
- **i4**：handoff 的 UI 測試證據來自「工具 timeout 上限截斷後自動背景化」的那次執行（handoff 自己揭露），正是 LS-96 池項 `4de3e796` 描述的縫隙；我本輪**未重跑 UI 測試**（理由見「未涵蓋」）。此項不需處置，僅記錄證據強度。
- **i5**：handoff 行號有一處小偏移——測試申報 `SupabaseAlbumsAPIClientTests.swift:170-215`，實際函式在 `:173`（`:166` 起是它的文件註解）。內容屬實，僅記錄（同 R1 i6 的類型）。

### 已驗證（怎麼驗）

1. **head 與範圍**：`git fetch origin && git rev-parse HEAD` ＝ `c711e3c…`；`git log --oneline cd93328..c711e3c`／`git show --stat 81c13d5 0acbeb9 c711e3c`／`git diff --stat origin/development...c711e3c`／`git diff 938e854 405acd8` 逐一核對，檔案清單與 handoff「產出位置」一字不差。逐檔清單：`LittleSprout/Services/Albums/SupabaseAlbumsAPIClient.swift`／`AlbumsContentAssembler.swift`／`AlbumsModels.swift`／`AlbumsAPIClient.swift`／`LittleSprout/Support/PreviewAlbumsAPIClient.swift`／`LittleSprout/TapTargetGateHarness+Albums.swift`／`LittleSproutTests/SupabaseAlbumsAPIClientTests.swift`／`AlbumsContentAssemblerTests.swift`／`AlbumsStoreTests.swift`／`LittleSproutTests/Support/StubAlbumsAPIClient.swift`／`docs/API.md`。
2. **合併的語意衝突修正**：`git diff 938e854 405acd8 -- LittleSprout/TapTargetGateHarness+Albums.swift` 只有一行（`AlbumListingRow(id:title:coverMediaId:createdAt:)` → 去掉 `coverMediaId:`），與 R1 預告的位置逐字相符；`git diff --stat origin/development...c711e3c -- LittleSprout.xcodeproj/project.pbxproj` 為空。合併後可編譯這件事由我的重放建置（app target 含 `TapTargetGateHarness+Albums.swift`）**實際 build 成功**證明，不是推論。
3. **mutation 重放（LS-209，我自己跑、不採信 handoff）**：`git archive c711e3c | tar -x` 到 session scratchpad 快照（**未改 worktree 任何檔**），`patch -p1 -R` 反向套用 `81c13d5` 對 `SupabaseAlbumsAPIClient.swift` 的 hunk——即 handoff 申報的「退回兩步驟設計」，退回後的 `createAlbum` 逐字等於 R1 head 版本（`.select("id")`＋`from("album_summaries").eq("id",…).single()`）。跑 `xcodebuild test -only-testing:LittleSproutTests/SupabaseAlbumsAPIClientTests`（`LS-203-iPhone17Pro`，獨立 `-derivedDataPath`）：**EXIT=65**，`Executed 15 tests, with 14 failures (1 unexpected)`，**唯一失敗的 test case 是** `test_createAlbum_insertsWithSelectThenBuildsRowLocally_withoutSecondRequest`（其餘 14 支照常 passed）。斷言訊息原文與 handoff 申報**逐字相符**：
   ```
   SupabaseAlbumsAPIClientTests.swift:188: error: … XCTAssertTrue failed - select 應只取三欄，實際 query：select=id
   SupabaseAlbumsAPIClientTests.swift:182: error: … XCTAssertEqual failed: ("Optional("/rest/v1/album_summaries")") is not equal to ("Optional("/rest/v1/albums")") - 本地組出後不應該再向 album_summaries 發第二個請求（merge-review R1 major-1）
   SupabaseAlbumsAPIClientTests.swift:186: error: … XCTAssertEqual failed: ("Optional("GET")") is not equal to ("Optional("POST")")
   SupabaseAlbumsAPIClientTests.swift:189: error: … XCTUnwrap failed: expected non-nil value of type "Data"
   ```
   **紅的是真的斷言失敗、不是 build fail／app crash**：build 階段 exit 0、同批 14 支測試正常通過，失敗訊息全是 XCTAssert*／XCTUnwrap（末尾那條 `XCTestErrorDomain Code=105` 是 mock 對「非預期的第二個請求」回錯誤所致的連帶，仍屬同一支測試）。log：`scratchpad/LS-203-mutation-run.log`。
4. **control（確認紅是 mutation 造成的）**：把快照 `patch -p1` 還原成 head 原樣，同一台機、同一 derivedData 重跑並加審本輪受影響的另兩支測試類——`-only-testing:…/SupabaseAlbumsAPIClientTests -only-testing:…/AlbumsContentAssemblerTests -only-testing:…/AlbumsStoreTests`：**EXIT=0，43/43 綠**。log：`scratchpad/LS-203-control-run.log`。
5. **「本地組出 ≡ view 回傳」我自己在 lock 內重放**（不採信 handoff 的 E2E 檔）：`bash scripts/ops/supabase-lock.sh -- bash -c '<docker exec psql>'`，SQL 在單一交易內：以 postgres 建一個新 `auth.users`＋`families`（trigger 自動補 owner 成員），接著 `set local request.jwt.claims`＋`set local role authenticated` 切成該使用者（`select current_user, auth.uid()` 印出 `authenticated`／該 uid，確認 RLS 身分生效），以**與新版 `createAlbum` 同款的欄位**下 `insert into public.albums … returning id, title, created_at`（RLS `albums_insert` 通過、三欄如實回傳，證明新的 `.select("id,title,created_at")` 在真正的 RLS／grant 下拿得到值），再以同一使用者查 `album_summaries`：`visible_media_count = 0`、`latest_media_id`／`latest_thumb_path`／`latest_storage_path`／`cover_thumb_path`／`cover_storage_path` **五欄全 NULL**——與 `AlbumListingRow(id:title:createdAt:)` 本地組出的值（`photoCount 0` ＋五個 `nil`，`AlbumsModels.swift:50-68`）完全等價。最後 `rollback`，DB 狀態未留痕（未 `db reset`，未影響其他 agent）。SQL：`scratchpad/LS-203-view-equiv.sql`。
6. **handoff E2E 原文查核**：`.claude/evidence/LS-203/r2/r2-e2e.txt` 逐段讀過——建相簿的 REST 回應（`POST /rest/v1/albums?select=id,title,created_at` 回 `id/title/created_at` 三欄）、B（member，非建立者）立即重查 `album_summaries` 得 `visible_media_count:0` ＋五欄 `null`、`album_children` 查得到寶貝標記列、attach 2 張後 `visible_media_count:2`、A（uploader）`PATCH /rest/v1/media` 軟刪一張後 B 同一次後續請求即得 `visible_media_count:1` 且 `latest_media_id` 回退、同一列 `album_media` 連結列數仍是 2。**連線通道 handoff 有寫**（host `curl` 直打 `127.0.0.1:54321` ＋ `docker exec … psql` 建 fixture，符合 LS-204 的要求），且其結論與我第 5 項的獨立重放一致。
7. **informational 處置查核**：i4（R1）確實入池——LS-96 comment `ec45e3b4-2215-41d8-99c8-0be20a1e2c27` 原文對得上（「`album_summaries` 完整 query string … 沒有機械驗證」，估 size S）；i2 的消費端以 `grep -rn "fetchMedia("` 核實；i1 的無消費端與轉發測試以 `grep -rn "\.latestMediaId"` 核實（消費端 `LittleSprout/Services/Albums/AlbumDetailStore.swift:65`／`:105`；轉發測試 `LittleSproutTests/AlbumsContentAssemblerTests.swift:38`）。
8. **CI**：PR run `34683816322`（head `c711e3c`）——`ci` success 32m11s／`ci-ipad` success 7m38s／`rules` success 7m10s／`db` success 3m48s／`lint` success 17s，**五個 job 全綠**，run 整體 completed/success（`gh run view 34683816322`，我在 verdict 貼出前輪詢到 run 收斂才下結論）。`ci` job 跑的是 `-only-testing:LittleSproutTests`（全量單元測試，workflow `.github/workflows/ci.yml` 明列），即 handoff 申報 954 支的 CI 側覆核。

### 未涵蓋（誠實揭露）

- **未自行重跑全量 954 單元／134 UI 測試**：單元以 CI `ci` job 結論為準（success），我只在本機獨立重跑了與 delta 直接相關的三支測試類（43 支，見「已驗證」4）。UI 測試我沒跑——delta 不含任何 UI 程式碼，唯一的 UI 檔改動是 harness 一行 stub 建構子（不在任何 host 的執行路徑上，只影響編譯，而編譯已由我的 build 證明），`ci-ipad` 亦 success。
- **未重放 handoff E2E 的 REST／JWT 全鏈**：改以第 5 項的 SQL 層獨立重放涵蓋同一結論（新建列的 view 回傳等價性、INSERT…RETURNING 三欄在 RLS 下可行），以避免多佔一次本機 stack 的 lock。軟刪即時性這條我沿用 R1 自己在 lock 內跑過的結果（R1 verdict「已驗證」第 4 項）＋本輪 handoff 證據原文，未第三次重放。
- **未做版面／tap-target 量測**：本票不改版面，`scripts/gates/tap-target-exemptions.txt` 與 UITests 在本票 diff 中未變（LS-166 動的那行在 development 側）。
- **未審 LS-166 本身的邏輯**：合併帶進來的相簿詳情屬 PR #356 已審範圍，本輪只驗「合併沒有動到它、且合併後可編譯」。

### 下一步

APPROVE，可併入 `development`。minor-1（兩處註解）與 informational i1（sweeper 勿刪 `latestMediaId`）、i7（R1：厚度分級口徑改變請提醒 qa）請在轉 QA 的 comment／收尾 handoff 列處置。

### handoff-evidence-check

```
bash scripts/gates/handoff-evidence-check.sh <verdict.md>
✓ 第 49 行起的列項有證據
✓ 第 50 行起的列項有證據
✓ 第 51 行起的列項有證據
✓ 第 59 行起的列項有證據
✓ 第 60 行起的列項有證據
✓ 第 61 行起的列項有證據
✓ 第 62 行起的列項有證據
✓ 第 63 行起的列項有證據
gate-exit=0
```

### 2026-09-12T09:18:43.118Z · Chih-Lin Yeh · `bbc025d8`（回覆 87f6b654）

**補述（裁決不變：APPROVE，`merge-review=success` 已貼 `c711e3c`，status id 54040404396）**——貼完 verdict 後順手續查 i4 的證據路徑，兩點更正／補強：

1. **handoff 引用的 `scratchpad/LS-203-ui-r2.log` 不存在**（`find /private/tmp/claude-501 -name "LS-203-ui-r2.log"` 0 命中）。原因未查（可能是重導向沒生效、或事後被清掉），不是我刪的——我只刪過自己建的 `LS-203-dd`／`LS-203-mutation` 兩個目錄。
2. **但 134/134 的申報確實有原始輸出佐證**，只是落在別的路徑：工具自動背景化的 `tasks/b368iyp7e.output`（16:23）尾段有 `Executed 134 tests, with 1 test skipped and 0 failures (0 unexpected) in 1063.108 seconds` ＋ `** TEST SUCCEEDED **`，該檔內 `Test Case … failed` 0 命中。限制：該檔只保留尾段（4.7 KB），看不到 `xcodebuild` 呼叫列，因此**無法從中確認 destination／worktree**（時間戳與內容[含 `UploadQueueSheetUITests` 等合併後才有的案例]與 R2 時序相符）。另：`LS-203-swiftlint-r2.log` 存在但為 0 bytes——這與 `swiftlint lint --quiet` 0 warning 的申報**相符**，不是漏交。

所以 i4 由「只有 handoff 自述」升級為「有原始輸出、路徑記錯」。**處置建議（給 ios-dev／收尾）**：handoff 引用 log 路徑前先 `ls` 確認檔案存在再寫進去；這條與 LS-96 池項 `4de3e796`（工具 timeout 自動背景化的縫隙）同源，若要機械化，可在該池項補一句「handoff 引用的 log 路徑須存在」的檢查點。

### 2026-09-12T09:19:41.607Z · Chih-Lin Yeh · `7ed2a357`

**orchestrator 09-12 17:3x**：merge-review R2 `87f6b654`（＋續查 `bbc025d8`）**APPROVE**（0 major／1 minor／5 info；status success 貼 `c711e3c`；mutation 反套 `81c13d5` 唯一紅的是目標測試、斷言逐字相符；lock 內 SQL 重放「本地組列 ≡ view」等價；scope 11 檔、合併未動 LS-166；CI run 34683816322 五綠）。minor-1（兩行過期註解）＋handoff 引用 log 不存在 → 池 LS-96（17:3x comment）。啟動 `LS-203-merge-follow.sh` 併 #361 → dev → 隨 v0.22.0 批次 promote → QA（相簿列表張數／封面四段退路／新建即時 0 張）。

### 2026-09-12T11:12:05.036Z · Chih-Lin Yeh · `1f8f6736`

# LS-203 QA 裁決（v0.22.0 批次）

worktree `/Users/clyeh/little-sprout/.claude/worktrees/qa-test`，HEAD `07d82bdfdb7d1721f96d7ba189c19f2e82547653`（＝`origin/test`）。

## 判定：PASS

## 已驗證

- **軟刪一張後張數即時正確**：在「B Owned Album」（member `ls166-qa-b@ls.test` 建立）加入 2 張照片（owner `ls166-qa@ls.test` 上傳）；`bash scripts/ops/supabase-lock.sh -- docker exec supabase_db_little-sprout psql ...` 對其中一張 `update media set deleted_at = now()`；以**非上傳者**帳號 B 的真實 RLS 身分（`set local role authenticated` + `request.jwt.claims` 帶 B 的 `auth.uid()`）在同一交易內重查 `select title, visible_media_count, latest_media_id from album_summaries`：`visible_media_count` 從 2 正確變 1、`latest_media_id` 正確回退到剩下那張 `679afc76-a74e-46c1-8941-f88d92756b3c`。上傳者 A 依既有 `media_select` RLS 上傳者例外（`supabase/migrations/20260904080921_media_select_hide_deleted.sql`）仍看到 2 張——已交叉核對該 migration 政策原文，屬既有設計行為非缺陷。
- **封面退路四段**：本輪實跑驗證其中一段——「latest 因軟刪而回退到下一張可見 media」——與上一項同一次查詢一起證實。其餘三段（`cover_thumb_path`／`cover_storage_path`／`latest_storage_path` 各自優先序）本輪未逐一重現，沿用 merge-review R1（`.claude/evidence/LS-203/r1/fallback-before-softdelete.txt`，四段各一 REST 查詢原文）與 R2（reviewer 自行以 SQL 對 `album_summaries` view 定義 `supabase/migrations/20260905074037_album_summaries_view.sql` 做等價性重放＋mutation 反套驗證紅）兩輪獨立證據判定成立。
- **新建相簿即時顯示 0 張＋標記**：建立「LS166 QA Album」勾選寶貝「小豆」，相簿列表卡立即顯示「LS166 QA Album · 0 張相片」＋「小豆 · 11 個月大」（`.claude/evidence/LS-203/qa/new-album-0-photos-with-tag.png`），未見任何短暫錯誤張數，證實 `createAlbum` R2 起單一 INSERT+RETURNING 本地組列的口徑與 view 一致。
- **一趟查詢**：本輪未自行重放網路層 log 檢查，沿用 merge-review R1／R2 對 `SupabaseAlbumsAPIClient.fetchAlbums` query string 的既有查證（`listSelect` 六彙總欄一次拉齊、`.eq`／keyset 分頁一次成型、無內嵌 aggregate）與 CI `ci`／`db` job（test tip `07d82bd` push run `34687685074` 五個 job 全綠，`gh run view 34687685074`）的既有覆核。

## 未完成

- 封面退路其餘三段本輪未逐一互動重現（見上，理由與既有兩輪獨立證據交代）；一趟查詢未自行重放網路層 log（沿用既有查證＋本輪 CI 綠）。

## 風險

低：本輪以非上傳者真實 RLS 身分驗證即時性，結論與既有兩輪 review 一致，無新回歸。

## 產出位置

- 截圖：`.claude/evidence/LS-203/qa/new-album-0-photos-with-tag.png`
- lock：`LS-166 QA 冒煙`（跨票共用同一輪互動 lock），持有 52 分 13 秒，正常釋放非到期
- CI：`gh run view 34687685074`（test tip `07d82bd`，五 job 全綠）

---

`bash scripts/gates/handoff-evidence-check.sh scratchpad/LS-203-qa.md --repo .claude/worktrees/qa-test`（貼文前跑，LS-211）：

```
✓ 第 9 行起的列項有證據
✓ 第 10 行起的列項有證據
✓ 第 11 行起的列項有證據
✓ 第 12 行起的列項有證據
```
（exit 0）

---

**總結**：LS-217／LS-166／LS-203／LS-230 四票在 `test` tip `07d82bd` 皆判 PASS，qa status 即將貼到本 SHA。

### 2026-09-12T11:16:29.597Z · Chih-Lin Yeh · `cb7b6617`

**orchestrator 09-12 19:15**：QA PASS（`1f8f6736`，非上傳者 RLS 視角軟刪後計數 2→1＋封面回退、新相簿立即顯示 0）→ `promote.sh test main` ✓ → main `07d82bd`，tag `v0.22.0`。收尾儀式進行中：①dead-code-sweeper 已派（v0.22.0 四票批次；特別看舊列表查詢路徑是否殘留）②lesson review 待 ③cleanup：`cleanup-merged.sh --apply LS-203` 補跑（worktree 先前已清）。

### 2026-09-12T11:23:37.872Z · Chih-Lin Yeh · `3f474375`

## dead-code sweep — LS-203

**dead-code-sweeper 09-12 19:21**

範圍：v0.22.0 批次（main `07d82bd`）。PR #361（`feature/LS-203-album-summaries-view`，merge `2c01eec`）。`git log --oneline c5f07b9..07d82bd --grep "LS-203"` 共 9 commits（含 R2 修正 `81c13d5`／`0acbeb9`／`c711e3c`）；`gh pr diff 361 --name-only` 取異動檔清單逐一交叉查引用，並對照 `docs/API.md` 是否有懸空引用。

### Findings（2 項，皆為文件註解殘留，不影響行為，可安全訂正）

**1. `LittleSprout/Services/Albums/AlbumsAPIClient.swift:19-21`（協定檔頭「方法 ↔ RPC／資料表對照」表）——描述已被本票自己移除的行為。**

現況原文：

> `createAlbum` → INSERT `public.albums`（…）＋插入後向 `album_summaries` 重讀該列（view 不可寫，見票文 Scope 3）

但 R2 commit `81c13d5` 已把 `createAlbum` 改成 INSERT 時 `.select("id,title,created_at")` 本地組出 `AlbumListingRow`，**不再**對 `album_summaries` 發第二個請求（現況見 `SupabaseAlbumsAPIClient.swift:104-133`，本次已重新讀取確認）。這段註解描述的是 R1 版、已被本票 R2 淘汰的行為，會誤導後續讀者做 API.md 對帳或效能盤點。

證據：merge-review R2（comment `87f6b654`）已明確標記為 **minor-1**，並給出修正建議文字，但判定「不阻擋合併」，PR 隨即直接 APPROVE→併入，未再有一輪讓 ios-dev 順手修正——main 現況仍是舊文字。

建議處置：可安全訂正（純註解，2 行）。reviewer 建議文字：「INSERT `public.albums` 並以 `select=id,title,created_at` 取回該列，於 client 端組出 `AlbumListingRow`（view 不可寫；不再重讀，merge-review R1 major-1）」。

**2. `LittleSprout/Services/Albums/AlbumsModels.swift:50`（`AlbumListingRow` 自訂 init 的文件註解）——描述已過期的「僅測試用」定位。**

現況原文：「供測試／`.preview()` 假資料建構」。但同一支 init 自 R2 `81c13d5` 起也是 **production 路徑**——`SupabaseAlbumsAPIClient.swift:127-129` `createAlbum` 目前唯一組出回傳列的地方，其 `photoCount: Int = 0` 與五個 `nil` 預設值承載著「本地組出 ≡ view 回傳」這個正確性契約（已由 merge-review R2 以 lock 內 SQL 重放驗證等價）。

具體風險（merge-review R2 原文）：若日後有人依字面「只給測試用」去收緊這支 init（例如包進 `#if DEBUG`），會直接改掉 production 行為；所幸現有測試 `SupabaseAlbumsAPIClientTests.swift:173-215` 對 `photoCount==0` 與五欄 `nil` 有斷言會轉紅擋住，故評級只是 minor 不是 major，但仍是誤判風險。

建議處置：訂正註解（補一句「亦為 `createAlbum` 本地組出新建列所用——預設值即『剛建立必為 0／NULL』的契約」，reviewer 已給出建議文字）。

**兩項處置建議**：不阻擋任何 gate、不影響功能，建議記入 LS-96 待辦池，供下一張碰 `AlbumsAPIClient.swift`／`AlbumsModels.swift` 的相簿相關票順手訂正；不建議為此單獨開票。

### 已核實非死碼（避免誤報）

- **`AlbumSummary.latestMediaId`**：本票無 production 消費端，但票文 Scope 2 明列「保留給 LS-166」；本次核實 `grep -rn "\.latestMediaId"` 全樹命中於模型自身、`AlbumsContentAssembler.swift:38`（轉發）與 `AlbumsContentAssemblerTests.swift:38`（測試釘住轉發），非死碼。
- **`AlbumsAPIClient.fetchMedia`**：本票本身在 R1 一度無消費端，但 LS-166 併入後由 `AlbumDetailStore.swift:65`／`:105` 消費，現況非死碼。
- **移除的 `LatestAlbumMediaEntry`／`LatestAlbumMedia`／`AlbumMediaCountEntry`**：全樹 `grep` 0 引用，乾淨移除，無殘留。
- **`docs/API.md` 移除的三則舊口徑筆記**（`db-aggregates-enabled`／連結列計數／`media!inner`）：`grep -rn` 確認 `docs/` 內無其他懸空引用這三則已刪筆記的內容。

### 觀察（既有死碼，非本票引入）

無。

### 搜尋方法與盲區聲明

方法同上：`gh pr diff --name-only` 取異動檔 → 逐一 grep 新符號呼叫端 → 對照四輪 merge-review comment（R1 `677ea237`、R2 `87f6b654`／`bbc025d8`）核對 minor/informational 處置是否已落地。

盲區：reflection／字串拼接不在偵測範圍；未重跑 `bash scripts/ops/supabase-lock.sh` 對 SQL 層做二次查證（信賴 merge-review R2 已在 lock 內完成的等價性重放與 mutation 反套結果）。

### 2026-09-12T11:24:56.424Z · Chih-Lin Yeh · `8698d460`

**orchestrator 09-12 19:24 收尾儀式 ②lesson review → Done**
- ①死碼：sweeper `3f474375` 無死碼；兩處 doc comment 仍描述 R2 前兩段式行為（`AlbumsAPIClient.swift:19-21`／`AlbumsModels.swift:50`，review R2 minor-1 未修即併）→ 裁定記入 LS-96 池（本日池項，ui P3 XS），不另開票。③cleanup：`cleanup-merged.sh --apply LS-203` ✓。
- **教訓**：R2 minor 在 APPROVE 後沒有回到實作者手上就併了——minor 的處置應和 informational 一樣列進 handoff「未完成」段；本票是 orchestrator 直接併 APPROVE 的 PR 而沒核對 minor 清單。**流程修正**：APPROVE 附 minor 時，orchestrator 併 PR 前先看 handoff 是否已處置，否則要求實作者補一筆 commit 或明寫「不修＋理由」。
- **harness**：`handoff-evidence-check.sh` 目前只驗 informational 處置段；minor 是否也納入驗證，記入池項待評估。本票 Done（v0.22.0，main `07d82bd`）。

