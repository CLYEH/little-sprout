# LS-76 Harness：push-gate 對「無 Swift／專案檔變更」的 PR 跳過 xcodebuild test（純文件 harness PR 也吃模擬器 flake）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-25T04:20:01.839Z |
| 開始 | 2026-08-31T08:14:36.399Z |
| 完成 | 2026-08-31T09:00:14.296Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-76/harnesspush-gate-對無-swift專案檔變更的-pr-跳過-xcodebuild-test純文件-harness-pr |
| 關係 | related→LS-49, related→LS-75, related→LS-65, related←LS-238, related←LS-199, related←LS-187, related←LS-95, related←LS-86, related←LS-118, related←LS-108, related←LS-80, related←LS-96, related←LS-39, related←LS-79, related←LS-87, related←LS-83, related←LS-77 |

## 描述

## 來源

[LS-75](https://linear.app/little-sprout-app/issue/LS-75/harnesslane-wip-上限backlogready-機械規則巡檢每輪補位票文-lane-標籤blockedby-關係) PR #113 R1 push：一行文件變更被 push-gate 擋下（`FBSOpenApplicationServiceErrorDomain Code=4` 模擬器啟動 app 失敗，重推即過）。純文件 harness PR 吃整輪 unit test 的 flake。

## 修法

`scripts/gates/push-gate.sh` 第 2 步（xcodebuild test）前判定 `git diff --name-only origin/<target>...HEAD` 是否含 `LittleSprout/`、`LittleSproutTests/`、`project.yml`、`*.xcodeproj`、`Package.resolved`、`.swiftlint.yml`；皆無 → 印「無 Swift 變更，跳過 unit tests（CI 仍跑）」並跳過；SwiftLint 同理。CI `ci` job 不變（仍全跑）。正負自測（合成 repo：只改 docs → 跳過；改 Swift → 跑）。與 [LS-65](https://linear.app/little-sprout-app/issue/LS-65/harnesspush-gate-便宜檢查契約對帳錯誤碼分級票號衝突預檢前移到-xcodebuild-之前)（便宜檢查前移）同票做亦可。

## 驗收

純文件 PR 的 push 在 30 秒內完成；含 Swift 變更的 PR 行為不變；COLLABORATION §4 更新。

## 留言（5）

### 2026-08-31T08:32:57.873Z · Chih-Lin Yeh · `5d42c549`

## Handoff — LS-76

**已完成**：`scripts/gates/push-gate.sh` 新增 0b 節，SwiftLint（步驟 1）與 unit tests（步驟 2）之前判定工作分支相對 target（方向矩陣同第 5／7 步：`hotfix/*`→`origin/main`，其餘→`origin/development`）的 diff 是否含 `LittleSprout/`、`LittleSproutTests/`、`project.yml`、`*.xcodeproj`、`Package.resolved`、`.swiftlint.yml`——皆無即跳過兩步並印訊息（CI 仍跑）；保護分支／detached HEAD／target ref 未 fetch 時維持原行為（安全預設，不新增失敗模式）。CI 的 `ci` job 完全不變。`docs/COLLABORATION.md` §4 已同步更新。

**已驗證（怎麼驗的）**：
- `scripts/gates/push-gate.test.sh` 新增 ⑯～⑳（合成 repo，掛 CI rules job）：⑯只改文件→兩步都跳過且不呼叫模擬器；⑰改 Swift 檔→兩步照跑（真的執行、模擬器有 shutdown）；⑱只改 `.swiftlint.yml`→仍判定有變更不跳過；⑲target ref 未 fetch→不跳過（安全預設）；⑳hotfix/* 方向矩陣正確用 `origin/main`。全數（①～⑳）本機執行通過。
- 負向控制：`git stash` 掉 push-gate.sh 改動後重跑自測，⑯ 正確地紅（證明是有效迴歸測試、非假綠）。
- **自指驗證**：本票 PR 只改 `scripts/gates/`／`docs/`（非 Swift），實際 push 這支分支時新邏輯確實走「跳過」路徑，實測輸出：
  ```
  → push gate：無 Swift 變更，跳過 SwiftLint（CI 仍跑）
  ✓ push gate：無 Swift 變更，跳過 unit tests（CI 仍跑）
  ```
  總耗時 7.18 秒（驗收要求 30 秒內）。
- 本機真 SwiftLint（`swiftlint lint --strict --quiet`）對整個 repo 額外跑一次確認乾淨（exit 0）——本票未動任何 Swift 檔。

**未完成**：無——首次提交，尚未經 merge-reviewer，無 informational finding 待處置。

**風險**：
- 只用路徑字面比對（grep），不判斷檔案內容語意差異——刻意保守（改 `LittleSprout/` 底下任何內容都視為「有變更」而照跑），非漏洞。
- 1a（XcodeGen 漂移檢查）本票未動（票文範圍只列 SwiftLint／unit tests 兩步），純文件 PR 仍會跑；非模擬器步驟、不吃 flake，對 30 秒內完成的驗收無影響（本票實測 7.18 秒已含 1a）。

**產出位置**：
- PR：https://github.com/CLYEH/little-sprout/pull/187（`hotfix/LS-76-pushgate-skip-nonswift` → `main`）
- head SHA：`a49f40ddba507bb26d99ab848f12d612e0264860`
- 檔案：`scripts/gates/push-gate.sh`、`scripts/gates/push-gate.test.sh`、`docs/COLLABORATION.md`
- worktree：`.claude/worktrees/LS-76`
- 模擬器已關：無（本次任務全程未 boot 任何真實模擬器——push 時新邏輯直接跳過 unit tests 分支，自測用的是 stub `xcrun`/`xcodebuild`）


### 2026-08-31T08:41:00.014Z · Chih-Lin Yeh · `1949fd1d`

## merge-review R1 — REQUEST_CHANGES

審的 head：`a49f40ddba507bb26d99ab848f12d612e0264860`（= PR #187 當前 headRefOid，= 我 `git diff origin/main...HEAD` 的 SHA）。

### Scope ✓
只動 `scripts/gates/push-gate.sh`、`scripts/gates/push-gate.test.sh`、`docs/COLLABORATION.md` §4 表格一列——與票文一致，無夾帶、無順手重構。

### 方向矩陣一致性 ✓
0b 節（L38–50）的分支分類與 target 推導與第 5 步（L278–282）、第 6/7 步（L318–322）逐字一致：`main|test|development|DETACHED` 排除、`hotfix/*`→`origin/main`、其餘→`origin/development`。⑳ 明確驗到 hotfix 用 `origin/main`。

### Fail-safe 方向 ✓（每條異常路徑都倒向「跑」）
`skip_swift_steps` 預設 0；只有「非保護分支 + target ref 存在 + 三點 diff 不含任一 allowlist 路徑」才置 1。detached→case 命中 DETACHED→不跳；保護分支→不跳；target 未 fetch→`git rev-parse --verify` 失敗→`if` body 不執行→不跳；命中任一 Swift/專案路徑→不跳。全部 fail-safe（多跑不少跑）。⑲ 驗到未 fetch → 不跳。

### 自測 ⑯–⑳ ✓（獨立實跑，20/20 綠，exit 0）
⑯ 純文件：兩步印跳過訊息 **且** `$SHUTDOWN_LOG` 為空（沒進 xcodebuild 分支）——雙重斷言。⑰ 加 Swift 檔：不跳、真跑 unit tests、模擬器有 shutdown（「Swift PR 行為不變」正向）。⑱ 只改 `.swiftlint.yml`：不跳（regex 命中）。⑲ 未 fetch：不跳。⑳ hotfix→origin/main。負向控制（handoff：stash 掉 fix → ⑯ 轉紅）成立，非假綠。

### CI（`a49f40d`）
`rules`/`lint`/`db` = pass；`ci`（build/test）= **pending**（非紅）。不影響本裁決，但 merge gate 仍需 `ci` 綠。

---

## 🔴 Finding 1（major）— allowlist 漏 `Config/Base.xcconfig`（純 xcconfig PR 會被誤跳，且是本 PR 引入的回歸）
`scripts/gates/push-gate.sh:45` 的 allowlist regex 未含 `.xcconfig`。`Config/Base.xcconfig` 是**現存的 live build-config**：`project.yml:45–47` `configFiles` 對 Debug/Release 都指它，它注入 `SUPABASE_URL`/`SUPABASE_ANON_KEY` 進 build settings/INFOPLIST，且檔頭記載格式寫錯會讓 AppConfig 的 precondition 在啟動時崩潰（LS-49）。

**失敗情境**：一支只改 `Config/Base.xcconfig` 的 PR（例：調 placeholder、或誤踩 `//` 註解截斷）→ 三點 diff 只含 `Config/Base.xcconfig`（xcconfig 內容不寫進 pbxproj，故不觸發 `.xcodeproj`/`project.yml` 命中）→ `skip_swift_steps=1` → 本機 SwiftLint + unit tests 兩步都跳過。這是「該跑卻跳」的 unsafe 方向。

**為何是 major 而非 informational**：這是本 PR **引入的回歸**——改前該 case 會跑完整本機 build/test（正確），改後會跳過（不正確），對這個 case 本機覆蓋率倒退。雖 CI `ci` job 無條件全跑會兜底（不會讓壞碼併入），但本機快速回饋對這類 build-affecting 檔失效，正是本 gate 最該小心的漏判。修法便宜：allowlist 加 `(^|/)Config/.*\.xcconfig$`（或 `\.xcconfig$`），並補一組 xcconfig-only 自測。

## 🟡 Finding 2（minor）— `.xcode-version` 同類漏判
同 L45 allowlist 未含 `.xcode-version`。只改它（bump Xcode pin）→ 誤跳，連帶跳過 1b 工具鏈對齊步。影響較低（1b 本身是對齊 CI 的本機優化；此檔極少單獨變更），建議與 Finding 1 一併加 `(^|/)\.xcode-version$`。

> 已確認**不漏**：SPM 相依宣告在 `project.yml`（命中）、無根 `Package.swift`；`Package.resolved` 位於 `.xcodeproj/**` 雙重命中；`LittleSprout/Info.plist` 由 `LittleSprout/` 前綴命中；`supabase/` migration 不影響 iOS build，且 api-contract/error-codes/migration 三檢查（步驟 3–7）不受 `skip_swift_steps` 管、仍無條件跑。

---

### Verdict：REQUEST_CHANGES
一個 major（Finding 1）。修法極小（allowlist 補 `*.xcconfig` + `.xcode-version` 兩條 pattern，並補測）。其餘四維度（scope／方向矩陣／fail-safe／測試品質）皆過。

commit status `merge-review` = **failure** 綁 `a49f40d`（PR head 未動，貼定）。

### 2026-08-31T08:51:41.140Z · Chih-Lin Yeh · `cb8404ff`

## Handoff — LS-76 R2（回應 R1 REQUEST_CHANGES）

**已完成**：修 R1 `1949fd1d-8ab1-490b-9db5-f91ff79ffe0c` 的 1 major + 1 minor：
- **Finding 1（major）**：allowlist 加 `(^|/)Config/.*\.xcconfig$`——`Config/Base.xcconfig` 是 `project.yml` configFiles 指向的 live build-config（注入 SUPABASE_URL／ANON_KEY，格式錯會啟動崩潰，LS-49），先前只改它的 PR 會被誤判「無變更」而跳過本機 build/test（該跑卻跳的回歸）。
- **Finding 2（minor）**：allowlist 加 `(^|/)\.xcode-version$`——同理避免只改它時連帶誤跳 1b 工具鏈對齊步。
- `docs/COLLABORATION.md` §4 同步補上這兩類檔案的說明。

**已驗證（怎麼驗的）**：
- `push-gate.test.sh` 新增 ⑳（xcconfig-only：不跳過，且真的執行 unit tests、模擬器有 shutdown）、㉑（`.xcode-version`-only：不跳過），並把原 hotfix 案例改編號為 ㉒（純重新編號，內容不變）。全數 ①～㉒ 本機執行通過（`swift.sh` wall time ~64 秒，非卡住——之前一次 60 秒 timeout 誤判為 hang，已用足夠時間重跑確認）。
- 負向控制：`git stash` 掉 push-gate.sh 的 R1 修法、只留測試改動重跑，⑳／㉑ 正確地紅（`只改 Config/Base.xcconfig 卻仍被判定跳過`／`只改 .xcode-version 卻仍被判定跳過`），其餘 ①～⑲、㉒ 不受影響——證明新測試真的鎖住這個回歸，不是假綠。
- 本機真 SwiftLint（`swiftlint lint --strict --quiet`）對整個 repo 再跑一次確認乾淨（exit 0）。
- 自指驗證：本次 R1 修復的 PR diff 仍只有 `scripts/gates/`／`docs/`（未動 `Config/`），push 時新邏輯正確走「跳過」路徑，實測 6.9 秒完成：
  ```
  → push gate：無 Swift 變更，跳過 SwiftLint（CI 仍跑）
  ✓ push gate：無 Swift 變更，跳過 unit tests（CI 仍跑）
  ```

**未完成**：R1 的兩條 finding（1 major + 1 minor）皆已在本次修復——無其餘 informational 待處置。

**風險**：與 R1 首次提交時相同——路徑字面比對（非內容語意）刻意保守；1a（XcodeGen 漂移檢查）本票範圍未變。

**產出位置**：
- PR：https://github.com/CLYEH/little-sprout/pull/187（`hotfix/LS-76-pushgate-skip-nonswift` → `main`，沿用同一支 PR）
- head SHA：`0f53e2bdaaa87ccd94968f2131ab3e3011af84bc`
- 檔案：`scripts/gates/push-gate.sh`、`scripts/gates/push-gate.test.sh`、`docs/COLLABORATION.md`
- worktree：`.claude/worktrees/LS-76`
- 模擬器已關：無（本次任務全程未 boot 任何真實模擬器；當前 Booted 清單上的機器皆屬其他票／demo-* 常駐機，非本次任務所開）


### 2026-08-31T08:58:41.679Z · Chih-Lin Yeh · `ef8e99f5`

## merge-review R2 — APPROVE

審的 head：`0f53e2bdaaa87ccd94968f2131ab3e3011af84bc`（= PR #187 當前 headRefOid，= 我 `git diff a49f40d..0f53e2b` 的 head）。增量審 R1→R2。

### 增量 scope ✓（無夾帶）
`a49f40d..0f53e2b` 只三檔：
- `push-gate.sh`：0b 節 regex 加兩條 alternative `(^|/)Config/.*\.xcconfig$` 與 `(^|/)\.xcode-version$`＋對應解釋註解。
- `push-gate.test.sh`：新增 ⑳（xcconfig-only）㉑（.xcode-version-only）＋既有 ⑳（hotfix 方向矩陣）機械改編號為 ㉒（out20→out22、rc20→rc22、字面 ⑳→㉒，內容逐行未變，diff 已核）＋標頭註解 ⑯～⑲→⑯～㉑。
- `COLLABORATION.md`：§4 Push gate 那一列更新（allowlist 補兩類檔＋來由）。
與任務「兩條 regex＋兩測試＋§4 一列」完全對齊，無無關重構、無順手改動。

### 兩條 regex 正確 ✓（實測逐路徑）
直接對新 regex 跑路徑比對：
- `Config/Base.xcconfig` → MATCH（不跳過）——票文指的 live config，命中 `(^|/)Config/.*\.xcconfig$`。
- `.xcode-version` → MATCH（不跳過）。
- 負向：`docs/COLLABORATION.md`、`scripts/gates/push-gate.sh` → no-match（純文件 PR 仍正確跳過，無誤傷）。
- `Config/Secrets.xcconfig.example` → no-match——正確：它是 `#include?` 的模板（Base.xcconfig 已有安全佔位值，CI／新 clone 不需它），非 live build-input，跳過是對的。

### 清單完備性複掃 ✓（本 repo 完備）
逐一列舉所有 build-input，確認無其他缺口：
- `.xcconfig` 全 repo 僅 `Config/Base.xcconfig`（已納入）＋`Config/Secrets.xcconfig.example`（模板，正確排除）——皆在 `Config/` 下。
- `.entitlements`：不存在，`project.yml` 未引用任何 entitlements。
- `.plist`：僅 `LittleSprout/Info.plist`，由既有 `(^|/)LittleSprout/` 前綴命中（實測 MATCH）。
- `project.yml`／`*.xcodeproj/**`／`.swiftlint.yml`：既有 pattern 命中。
- SPM：相依鎖在 `project.yml`（命中）；無根 `Package.swift`（已確認）；`Package.resolved` 位於 `LittleSprout.xcodeproj/project.xcworkspace/.../swiftpm/Package.resolved`——由 `\.xcodeproj(/|$)` 與 `(^|/)Package\.resolved$` 雙重命中。
→ 對現有 repo 狀態，allowlist 完備。

### 測試驗到真行為 ✓（本機實跑 ①～㉒ 綠、exit 0）
- ⑳ 雙重斷言：out 不含「無 Swift 變更」（不跳過）**且** 含「執行 unit tests」**且** `$SHUTDOWN_LOG` 有 `shutdown <ded_udid>`——證明 xcconfig-only 真的走進 xcodebuild test 分支並收尾關機，不是只印訊息（與 ⑰ Swift-only 同級斷言）。
- ㉑ 斷言不跳過（機制與 ⑳ 同一條 allowlist，⑳ 已驗完整執行路徑，㉑ 鎖 `.xcode-version` 缺口足夠，與 minor 嚴重度相稱）。
- 我獨立實跑 `push-gate.test.sh`：①～㉒ 全綠、exit 0。handoff 的負向控制（stash 掉修法→⑳/㉑ 轉紅）邏輯成立：無兩條 alternative 時 Config/Base.xcconfig 與 .xcode-version 皆不命中→skip_swift_steps=1→印「無 Swift 變更」→斷言 1 紅，真的鎖住回歸。

### CI（`0f53e2b`）
`db`／`lint`／`rules` = pass；`ci`（build/test）= **pending**（非紅，與 R1 同狀態）。不影響本裁決；`ci` 是 GitHub 另一道 required check，仍須自行轉綠才能併。

### Informational（非 finding，記錄用）
`(^|/)Config/.*\.xcconfig$` 要求 `Config/` 前綴——若未來把 xcconfig 放到 `Config/` 以外會被漏判。現無此檔、R1 明文接受此形式、且 CI 無條件全跑兜底，故不列 finding。若要更保守可日後改 `\.xcconfig$`；本票不需動。→ 併入 LS-96 待辦池。

---

### Verdict：APPROVE
四維度（race／效能／平行／scope）皆過，無 blocker/major/minor。R1 的 1 major（Finding 1）＋1 minor（Finding 2）皆已正確修復並以真行為測試鎖住。

commit status `merge-review` = **success** 綁 `0f53e2b`（= 當前 headRefOid，貼定）。

### 2026-08-31T09:00:10.486Z · Chih-Lin Yeh · `3267fea1`

**R2 APPROVE → 已併 main（2026-08-31）**：PR #187 merge，main=`ddf03ab`；merge-review=success 貼於 head `0f53e2b`（R2 comment `ef8e99f5`）。back-merge PR #188 已開（CI 綠後併）。informational（Config/ 前綴）入 LS-96。

**Retro（harness 票輕量版）**：R1 reviewer 抓到 allowlist 漏 `.xcconfig`（Config/Base.xcconfig 是注入 secrets 的 live build-config，改它卻跳過 build 正是本 gate 最該防的漏判）——「跳過類 gate 的清單完備性」需 reviewer 逐一列舉 build-input 覆核，不能只列常見副檔名。純效率 harness 免 dead-code。→ Done。純文件 PR 的本機 push 從整輪 xcodebuild（含模擬器 flake）縮到 ~7s。

