# LS-198 Harness chore：LS-96 池項清倉五件——「收據 N 支」殘句統一指回 `overflow-scan.js` 檔頭（7 處）＋`pr-body-check.sh` 格式模式對純數字 SHA 候選印警告與 deny 訊息補句＋`patrol.sh` du 快取原子寫入／`--json` `sim_linear_note`／`ticket_has_worktree` memo＋`motifs.md` 角托規則對齊稿面＋`docs/API.md` §6／§10 pg_net 範本補 `timeout_milliseconds`

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Medium |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-05T05:41:35.945Z |
| 開始 | 2026-09-05T06:03:25.167Z |
| 完成 | 2026-09-05T09:24:48.143Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-198/harness-chorels-96-池項清倉五件-收據-n-支殘句統一指回-overflow-scanjs-檔頭7-處pr-body |
| 關係 | related→LS-77, related→LS-6, related→LS-185, related→LS-153, related→LS-165, related→LS-122, related→LS-168, related→LS-3, related→LS-195, related→LS-196, related→LS-4, related→LS-171, related→LS-40, related→LS-30, related→LS-186, related→LS-177, related→LS-189, related→LS-187, related→LS-144, related→LS-96, related←LS-204, related←LS-202, related←LS-194 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 待辦池同日累積五組 XS 池項，皆為文件／gate 衛生，單獨不值一票、合起來一次清倉（[LS-144](https://linear.app/little-sprout-app/issue/LS-144/harness巡檢加開票責任機械提醒-lane-在飛-0-且無可派候補時動作清單印-開票並列來源story-拆票ls-96-p1-升票) lane:harness 補位；[LS-187](https://linear.app/little-sprout-app/issue/LS-187/harness巡檢專屬模擬器段改為票已-donecanceled-或-worktree-已不存在的-ls-n-一律-動作清單-cleanup) 進 QA 後 harness lane 空）：

1. `e6112467`：「四支／五支」殘句 7 處（`.claude/agents/visual-reviewer.md:11`／`:45`、`docs/COLLABORATION.md:110`／`:307`、`scripts/gates/design-evidence-check.sh:498`、`.github/workflows/ci.yml:195`／`:394`）→ 改成「六支（以 `scripts/design/overflow-scan.js` 檔頭為準）」或直接指回檔頭，不再寫死數字。
2. `092db41b`（[LS-186](https://linear.app/little-sprout-app/issue/LS-186/harnessgate-精修兩項-pr-body-checksh-verify已修行-sha-候選只認-git-objectlinear) R2 info-1／2）：`pr-body-check.sh` 格式模式（不帶 `--verify`）把純數字 token（CI run id／migration 前綴）當短 SHA → 對純數字候選印一行警告（不紅）；deny 訊息補「檔名／行號裡的數字也算 hex token，把 SHA 放『已修』後第一個」。自測補 2 案。
3. `464baaf6`（[LS-187](https://linear.app/little-sprout-app/issue/LS-187/harness巡檢專屬模擬器段改為票已-donecanceled-或-worktree-已不存在的-ls-n-一律-動作清單-cleanup) R1 info-1～4）：`patrol.sh` du 快取寫入改暫存檔＋`mv`（原子）；`--json` 加 `sim_linear_note`；`ticket_has_worktree` 一輪內 memo（30 台省 ~1.2s）；`[ -d ]`「記錄在、目錄已刪」補自測樣本。
4. `10f3b3b2`（[LS-177](https://linear.app/little-sprout-app/issue/LS-177/設計留言愛心互動列與留言-sheetls-22-畫面群推播通知權限提示與設定頁推播通知列-ui-designervisual) VR R3 MN-2）：`.claude/skills/little-sprout-brand/references/motifs.md` 角托規則②改「對角兩顆為預設；四顆保留給大幅面主視覺」並附節點例（Hero Print `zzXNT`、Empty Print `K1yE6j`、`cmp/Card Diary`、`cmp/Profile Print`）。
5. `f730a9ac`＋`9f56908b` N2／N4：`docs/API.md` §6／§10 兩個 `net.http_post` 範本補 `body := '{}'::jsonb, timeout_milliseconds := 60000`；§10「其餘四種 kind」→五種；§10 收件人規則補「排除停權使用者」bullet。

## 範圍

上列五組，每組一個 commit；相關自測（`pr-body-check.test.sh`、`patrol.test.sh`、`design-evidence-check.test.sh`、`agent-tools-check`）維持綠並補新案；不改任何行為以外的東西（第 2、3 組是行為微調，需自測釘住）。hotfix 流程（自 `main`、PR 回 `main`、back-merge）。

## 不做

新 gate；[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 其他池項；`handler.ts` 推播文案（綁 [LS-189](https://linear.app/little-sprout-app/issue/LS-189/taskls-23-ios-內容操作表日記照片留言或長按檢舉封鎖此成員owner-移除內容檢舉原因與已送出封鎖確認封鎖名單解除owner)）。

## 驗收

- [ ] 五組各附前後 diff 摘要；自測全綠＋第 2／3 組 mutation 各 1 支紅
- [ ] merge-reviewer 通過；[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 五則池項銷除註記

## 留言（9）

### 2026-09-05T06:32:55.234Z · Chih-Lin Yeh · `b2eef6ed`

**Handoff（harness chore，R0 → In Review）**

**Ticket**：LS-198——hotfix 流程：自 `main` `6f4907f` 切 `hotfix/LS-198-pool-cleanup`，PR 回 `main`，併入後 back-merge `main`→`development`。

**已完成（五組各一 commit，diff 摘要）**
1. `145a355` **「四支／五支」殘句指回 `scripts/design/overflow-scan.js` 檔頭**：`.claude/agents/visual-reviewer.md:11`（「截圖與五支掃描都做不了」）／`:45`（「上方四支，正典腳本」）、`docs/COLLABORATION.md:110`（「五支掃描皆有輸出」）／`:307`（「五支掃描／截圖全不可用」）→「六支，以檔頭為準」。`.github/workflows/ci.yml:195`／`:394` 已由 LS-186 `4b4212c` 改成六支（commit 訊息即「ci.yml 兩處註解四支→六支」），本票不動。grep 全 repo 補抓三處寫死現況支數、且都在正典腳本檔頭自己身上：`overflow-scan.js:15`「跑四支掃描」、`:114`「不證明五支數字算對」、`:624` 互斥 throw 訊息「（五支）／不跑五支」。`scripts/gates/design-evidence-check.sh:498` 綠訊息原寫死「四支掃描皆有輸出」→ 依收據實際要求的層數算（`len(required)＋fifth＋sixth`，legacy 兩支走舊字串；第一版 dict `[n]` 對 legacy `n=2` KeyError 讓 ①／⑨／⑰ 紅，自測抓到後改 `.get`）＋「支數以 overflow-scan.js 檔頭為準」；自測 ㉞-c 釘六支收據印「六支」。留著不動：歷史敘事（「LS-122 擴為四支、LS-168 擴為五支」）、相對語（「前四支」）、序數（「第五支」）、LS-122 層的錯誤訊息 `design-evidence-check.sh:366`（自測 ⑪／⑯ 釘）、COLLABORATION §7 沿革列、test fixture 名。
2. `12a0339` **`scripts/gates/pr-body-check.sh`**（LS-186 R2 info `73c10b4e`）：格式模式（不帶 `--verify`）對 (b) 申報行的第一個 hex 候選全為 `[0-9]`（CI run id、migration 檔名前綴 `20260904212530`）者印一行 ⚠「…是純數字——若它是 CI run id／migration 檔名前綴而不是 commit SHA，--verify 會紅」、exit 不變（`--verify` 時不印，`git cat-file` 才是裁判）；deny 訊息兩處（格式模式提示行、`--verify`「不是本 repo 的 commit」）補「檔名／行號裡的數字也算 hex token，把 SHA 放申報字樣後第一個、檔名寫在 SHA 之後」。`pr-body-check.test.sh` ⑧ 三案（88 → 91）。
3. `1ded7e6` **`scripts/ops/patrol.sh`**（LS-187 R1 `05886d9c` info-1～4）：du 快取回寫改 `${du_cache}.tmp.$$`＋`mv -f`（同目錄 rename 原子，失敗靜默並清暫存檔）；`--json` 加 `sim_linear_note`（字串或 `null`，放在 `orphan_simulators` 後）；`ticket_has_worktree` 加一輪內 memo `WT_HAS_MEMO`（每筆「`\n<票號>\t<0|1>`」，換行在前 tab 在後夾住票號，LS-3 不命中 LS-30、LS-40 不命中 LS-4），前置收票號與逐台判定各叫一次時第二次不重掃 `WT_INDEX`。`patrol.test.sh` ㉓ 補：過期重量後快取 inode 必變／不留 `.tmp`／仍一行含路徑、`--json` `sim_linear_note` 兩模式；新 ㉓-b「worktree 記錄仍在 porcelain、目錄已 rm」→ `[ -d ]` 判不在 → 殘機 ⚠（232 → 242）。
4. `295fe4c` **`motifs.md` 角托規則**（LS-177 VR R3 `748595f5` MN-2）：①「四角托＝大幅面主視覺的沖印品」、②「對角兩顆＝其餘沖印品的預設」＋節點例 Hero Print `zzXNT`（200×155）、Empty Print `K1yE6j`（120×120）、`cmp/Card Diary`、`cmp/Profile Print`、App icon；`SKILL.md` 規則 4 摘要句同步（原「兩對角托＝同一張紙縮到 ≤60pt」與稿衝突）。`zzXNT`／`K1yE6j` 在 `origin/feature/LS-177-comments-reactions-design` 的 .pen（main 尚無）；`cmp/Card Diary`（`qtCd7`）／`cmp/Profile Print`（`OePXK`）main 已有。`wordmark-brand.md:44` icon 專屬理由句不動。
5. `4f86318` **`docs/API.md`**（LS-196 R2 `e0015b6c` N1＋LS-195 R1 `945840af` N2／N4）：§6／§10 兩個 `net.http_post` 範本補 `body := '{}'::jsonb, timeout_milliseconds := 60000`＋pg_net 預設 5000 ms 逾時說明（正式站 cron `cac7d8c4` 實際值）；§10「其餘四種 kind」→五種；`notification_recipients` 排除清單加「已被停權的成員」（對照 `20260904212530_suspension_and_registrations.sql:811-838` 的 `pr.suspended_at is null`，只排本人、不另判家庭停權）。

**已驗證（怎麼驗）**
- `bash scripts/gates/pr-body-check.test.sh` → 91 組全綠。**mutation A**（拿掉純數字警告區塊）→ 唯一紅：`✗ ⑧-a 已修 \`20260904212530_x.sql\` \`<sha>\`：格式模式仍綠、但對純數字候選印 ⚠ 警告（期望 exit 0、輸出含「是純數字」，實得 0）`，其餘 90 綠、exit 1。
- `bash scripts/ops/patrol.test.sh` → 242 ✓／0 ✗。**mutation B**（`mv` 改回 `> "$du_cache"` 直接寫）→ 唯一紅：`✗ ㉓ 快取回寫應走暫存檔＋mv（inode 135615586→135615586 未變＝就地改寫，併發讀者可能讀到半行）`，其餘 240 綠、exit 1。**mutation C**（memo 命中後回傳反向，加做）→ 27 紅（㉓ (a) LS-999／LS-30／LS-4 殘機、(b) LS-3、LS-40 反向等）、exit 1。三支 mutation 都在 scratchpad 的 tree 副本上跑，worktree 未動。
- `bash scripts/gates/design-evidence-check.test.sh` → 53 組全數通過（含新 ㉞-c；⑬／㉒／㉔ 的 LS-122 收據仍印「四支」）。
- `bash scripts/gates/agent-tools-check.test.sh` 65 組通過；`brand-skill-check.sh`＋`.test.sh` 綠（24 組）；`node scripts/design/overflow-scan.test.js` 33 組通過（LS-171 原始碼斷言不受 `:624` 改字影響）；`api-contract-check.sh`（text）RPC 34／表 23 一致；`error-codes-check.sh` 28 碼三方一致。
- push gate 全綠（api-contract／tap-target-registry／error-codes／migration 版本與 immutable／分支票號乾淨 5 commits／與 `origin/main` 6f4907f 可乾淨合併；無 Swift 變更跳過 SwiftLint 與 unit tests）。
- PR body：`bash scripts/gates/pr-body-check.sh LS-198-pr-body.md --branch hotfix/LS-198-pool-cleanup --verify` → exit 0（LS-96 行 6 條反查皆存在、申報 SHA 行 0 條）。
- 未碰真實模擬器／Pen／容器／lock／正式站；Linear 只讀、只寫本票。

**未完成**
- reviewer informational 處置規則：merge-reviewer 尚未審（本 comment 是 R0）。R1 起每條 informational 逐條列處置——已修 `<commit SHA>` `<檔案:行號>`／記入待辦池 LS-96 `<comment id>`（先 `save_comment` 取 id）／另票 LS-<m>（限 COLLABORATION §5-b 優先序 High 以上）／不修＋理由——一條不省。
- LS-96 五則池項（`e6112467`／`092db41b`／`464baaf6`／`10f3b3b2`／`f730a9ac`／`9f56908b`）的銷除註記（票文驗收第 2 條）：本票被指示只讀 Linear、只寫 LS-198，留 orchestrator 於 PR 併入後註記。
- `ci.yml:195`／`:394`：票文列為 7 處之二，實際已由 LS-186 `4b4212c` 改好，本票無 diff。
- migration `20260905040711_report_recipients_owner_only.sql:6,14` 的同一句「四種」不動（migration-immutable；票文範圍只列 API.md），如要改需另開 migration。
- 併入後 back-merge `main`→`development`：orchestrator 走 promote／back-merge 流程。

**風險**
- 第 2 組只加輸出（一行 ⚠、deny 半句），exit code 與 `--verify` 判定不變（既有 88 組全綠）。
- 第 3 組：`mv` 失敗退回「這輪沒快取」；memo 只活在單次執行；`--json` 多一欄，既有欄位順序／型別不變（㉑／㉓ jq 斷言全過）。
- 第 1 組 `design-evidence-check.sh` 綠訊息只改 stdout 敘述，errs／exit 不變；CI 無對該字串比對。
- 文件組無程式行為；`zzXNT`／`K1yE6j` 要等 LS-177 設計分支併入 main 才在 main .pen 找得到（`design-notes-check.sh` 只掃 .pen Notes 板）。

**產出位置**
- PR #310 https://github.com/CLYEH/little-sprout/pull/310（base `main`）；head `4f8631849d2f02b4991a3566f6cbd7d36567aa16`。
- Commits：`145a355c308ab7f26dc8db2c9c368fca01b4f999`（G1）／`12a0339d8a8bdcf2b0d68ecb0c62e34895793031`（G2）／`1ded7e6c1de02228d05f11fba3eb485045d854de`（G3）／`295fe4cf7b94bdc96b06d87d55ebc1011c9a4e94`（G4）／`4f8631849d2f02b4991a3566f6cbd7d36567aa16`（G5）。
- 自測與 mutation log：scratchpad `LS-198-patrol-test.log`／`LS-198-dec-test.log`／`LS-198-mutA.log`／`LS-198-mutB.log`／`LS-198-mutC.log`；PR body `LS-198-pr-body.md`。

### 2026-09-05T06:54:36.853Z · Chih-Lin Yeh · `4cf2789e`

## merge-review R1 — **APPROVE**（PR #310，head `4f8631849d2f02b4991a3566f6cbd7d36567aa16`，base `6f4907f`；2026-09-05 14:53）

範圍：12 檔 +106/−25，五組各一 commit。CI 四項全 pass（run 33950046963）。**無 blocker／major**；**2 minor＋3 informational**（minor 都在文件正確性，不阻擋合併，但建議併前順手或明確裁定）。

### 我實跑的驗證

- **自測全綠**：`pr-body-check.test.sh` **91**／`patrol.test.sh` **242**／`design-evidence-check.test.sh` **53**／`agent-tools-check.test.sh` **66**／`brand-skill-check.test.sh` **25**／`node overflow-scan.test.js` **33**，全 exit 0。
- **我自己的 9 支 mutation**（複製樹、基線先驗綠、每支還原後重驗）：
  - **A 拿掉純數字 ⚠ 區塊** → killed（唯一紅 ⑧-a），與實作者宣稱一致。
  - **B du 快取改回就地直寫** → killed（唯一紅：`inode 未變＝就地改寫`）。
  - **D2 memo 分隔少了後面的 tab** → killed（10 紅，含 LS-4／LS-40 互撞）。
  - **E memo 命中回傳反向** → killed（18 紅）。
  - **F `--json sim_linear_note` 永遠 null** → killed（2 紅）。
  - **G `n_zh` 用 `[n]` 而非 `.get`** → killed（①／⑨／⑰ 紅）——這正是 legacy（n=2）KeyError 的重現，證明 `.get` 是必要修正且被測住（審點 3）。
  - **A2（`--verify` 也印警告）／D（memo 少了前導換行）存活** → 見 informational。
- **審點 1（純數字 ⚠ 的取捨）——我實跑三種形狀後裁定「可接受」**：格式模式下 `已修 \`1234567\`` **會**被警告（7 碼全數字的合法短 SHA 誤警機率＝(10/16)^7 ≈ **3.7%**；40 碼幾乎為 0），`已修 \`20260904212530_x.sql\`` 也警告（預期），**exit 全部 0**；`--verify` 一條警告都不印（實測 grep 計數 0）。理由：只加輸出、不改判定，措辭是條件句（「若它是 CI run id／migration 檔名前綴**而不是** commit SHA」），真正的裁判是 CI 一律帶的 `--verify`。**可選收斂**（informational-3）：只在 token 長度 ≥8 才警（run id ~11 碼、migration 前綴 14 碼都涵蓋），就能把 7 碼短 SHA 的誤警清成 0。
- **審點 2（第 3 組）**：
  - `mv` 原子寫入的失敗路徑我逐條讀過 `patrol.sh:577-578`：`printf > "$du_tmp"` 失敗 → `rm -f` 暫存檔（**這輪沒快取**，下輪重量）；`mv -f` 失敗 → 同樣 `rm -f` 暫存檔、**保留舊快取**（舊值可能過時但不是壞檔）——都不會留下半行壞快取 ✓。暫存檔帶 `.$$`，併發 patrol 不互撞 ✓。
  - memo 分隔：`*$'\n'"${1}"$'\t'0*` 的**後方 tab 是關鍵**（我的 D2 突變一拿掉就 10 紅），LS-3／LS-30、LS-4／LS-40 互不命中 ✓。另外我確認 `while … done <<EOF` 在 bash 是**當前 shell**（非管線子 shell），所以改寫成「迴圈內設 `hit`＋迴圈後寫 memo」不會丟值 ✓。
  - `sim_linear_note` 我用假身跑四態實測 `--json`：不帶 `--linear` →「票狀態未查（未帶 --linear）…」；exit 3 →「無 LINEAR_API_KEY…」；exit 1 →「Linear 票狀態查詢失敗（… exit 1）…」；回 Done →「票狀態已查 Linear（LS-165）」且該台 `reason` 轉 `ticket_closed` ✓ 四態值都對。既有欄位順序／型別未變（只在 `orphan_simulators` 後插入）。
- **審點 4（文件對稿面／SQL）**：
  - `docs/API.md` §6／§10 兩個範本補的 `body := '{}'::jsonb, timeout_milliseconds := 60000` 我拉 LS-153 comment `cac7d8c4` 逐字比對，**與正式站 cron job 的 SQL 完全一致**（同樣 `body := '{}'::jsonb, timeout_milliseconds := 60000)`）✓。
  - §10「五種 kind」：清單實列 `comment／reaction／diary／album／media` 五項 ✓。
  - 停權 bullet：`20260904212530_suspension_and_registrations.sql:830` 確為 `and pr.suspended_at is null`，函式內只有**成員本人**停權過濾（另有 `blocked_users` 排除），**沒有**家庭層判定 → 文件「只排除成員本人、不另判家庭停權」與 SQL 一致 ✓。
- **審點 5**：逐 commit 檔案歸屬與五組一一對應、**無夾帶**；worktree 乾淨；PR body `--branch hotfix/LS-198-pool-cleanup --verify` 直接看 exit code → **0**（LS-96 行 6 條反查皆過）；head 未前進（貼 status 前 `--expect` 再比一次）。

### minor-1 · `motifs.md` 的節點例與「全稿慣例」宣稱**與稿面不符**（我用兩份 .pen 離線核對）

新文字說「② 對角兩顆＝其餘沖印品的預設……**全稿慣例**（LS-177 VR R3 MN-2 對帳，**稿為準**回修本檔）：Hero Print `zzXNT`、Empty Print `K1yE6j`、`cmp/Card Diary`、`cmp/Profile Print`、App icon 全用對角兩顆」。我把 `origin/main` 與 `origin/feature/LS-177-comments-reactions-design` 的 .pen 離線解出來逐節點數角托 ref（`Corner TL/BR`／`Mount TL/BR` → `cmp/Photo Corner` `GEBcf`）：

| 節點 | 實測 |
|---|---|
| `zzXNT` Hero Print（200×155） | **2 顆對角** ✓ 與文件相符 |
| `K1yE6j` Empty Print（120×120） | **2 顆對角** ✓ |
| `OePXK` `cmp/Profile Print` | **2 顆對角**（`Mount TL`／`Mount BR`）✓ |
| **`qtCd7` `cmp/Card Diary`** | **0 顆**（main 與 LS-177 分支的整個子樹都沒有任何角托 ref）✗ |

- **(a) `cmp/Card Diary` 這個例子是錯的**：它一顆角托都沒有，屬於③「零角托」那類，不能當②的例子。失敗情境：designer 照這行去「補回對角兩顆」，等於憑空給日記卡加角托；或 VR 拿它當基準判別人違規。
- **(b)「全稿慣例」的方向與稿面相反**：main .pen 上「直接子節點帶角托 ref」的容器分布是 **4 顆 175 個 vs 2 顆 14 個**；4 顆的是 `Print Cell` ×134（相簿／日記詳情的格狀縮圖，高 175–292，**正是「不是大幅面主視覺」的小格子**）、`Print Stage` ×16、`Photo Print` ×10、`Family Print` ×4、`Photo Wrap` ×3、`Email Print` ×2；2 顆的幾乎全是 **App Icon 各尺寸**（`Icon 40/60/180/512`、Light／Dark）＋`cmp/Profile Print` 1 個。LS-177 分支同形（4 顆 175／2 顆 18）。所以「對角兩顆是其餘沖印品的預設、稿為準」這句，對現稿並不成立——現稿的預設其實是四顆，對角兩顆是 icon 與 LS-177 新增的兩張 Print。
- **這不是我來裁定設計規範**：要把預設改成對角兩顆是設計 lane（LS-177 VR R3 MN-2）的決定，我只指出**文件把「將要採用的新規則」寫成了「現稿慣例」**，且引了一個零角托的例子。建議二擇一：(i) 刪掉 `cmp/Card Diary`、把措辭改成「LS-177 起的新預設；既有 `Print Cell` 等 175 處仍是四顆，另票對帳」；或 (ii) 保留規範但補一句「與現稿 175 處四顆不一致，回修由設計票處理」。任一種都只是幾個字。

### minor-2 · 「N 支」清掃漏兩處**現況**支數（handoff 的「grep 全 repo 補抓三處」不完整）

我對全 repo 重跑 grep 後逐條分類，除了 handoff 明列要留的（歷史敘事／相對語／序數／LS-122 層錯誤訊息／§7 沿革列／fixture 名）之外，還有兩句是**描述現況**、現在是錯的：
- `scripts/gates/design-evidence-check.sh:46`：「…不證明**五支**數字算對。」——**同一句**在 `overflow-scan.js:114` 已被本票改成「不證明各支掃描的數字算對」，但 gate 檔頭這份沒跟上；而且這個檔案本票本來就有改（`:498`）。
- `scripts/design/overflow-scan.test.js:2`：「純函數對合成節點樹（絕對座標）驗**四支**掃描的語意」——該檔現在驗六支（33 組含 `board_clip`）。

兩處都只是註解、零行為影響，但票文驗收就是「不再寫死數字」，且 handoff 宣稱「都在正典腳本檔頭自己身上」並不準確。建議順手改（各一個詞）或明確記入池。

### informational

**info-1 · A2 存活**：把純數字警告改成 `--verify` 時也印，91 組全綠——「`--verify` 不印」這條設計（檔頭有寫）沒有樣本釘住。行為現在是對的（我實測 `--verify` 印 0 條），補一組 `hasnt` 即可。

**info-2 · D 存活是實務等價突變**：memo 比對拿掉**前導換行**（只留後方 tab）不會紅。我分析過：票號一律 `LS-<數字>`，較短票號要出現在較長票號記錄裡只可能後接數字（`LS-3` 在 `LS-30` 裡後面是 `0`），永遠碰不到 `\t`，所以前導換行是防禦性、不可達。**不建議**為它補樣本。

**info-3 · 純數字 ⚠ 對 7 碼全數字短 SHA 會誤警**（實測 `1234567` 觸發，約 3.7% 機率）：可加長度 ≥8 的條件收斂，或維持現狀（只是一行提示、exit 不變）。

### 沒看／沒跑的（誠實揭露）

- **未碰 Pen／真實模擬器／容器／正式站**；.pen 一律 `git show` 後離線 python 解析（Pencil 的展開後座標我算不出，本次只數節點與 ref，不涉及版面幾何）。
- `docs/API.md` 我只驗了本票改動的三處（兩個範本、kind 數、停權 bullet）與其對應 SQL／Linear 依據，**沒有**全文複驗 §6／§10 其餘內容。
- 正式站 cron 我沒有連線查，`timeout_milliseconds := 60000` 是比對 LS-153 comment `cac7d8c4` 貼出的 SQL 原文。
- lock：**未持有**。

**verdict：APPROVE**（無 blocker／major）。minor-1／minor-2 建議併前順手（各幾個字）或由 orchestrator 裁定入池；併入 `main` 後補 back-merge `main`→`development`，並補上票文驗收第 2 條的 LS-96 五則銷除註記。

### 2026-09-05T07:00:19.352Z · Chih-Lin Yeh · `1235cd50`

**R2（merge-review R1 `4cf2789e` APPROVE 帶 2 minor）**

- **minor-1 已修 `07abee1234f158b2bc99f35be457491f3468e180`** `.claude/skills/little-sprout-brand/references/motifs.md:27-28`、`SKILL.md:32`——R1 版「對角兩顆＝其餘沖印品的預設、稿為準」與稿相反。改為事實敘述：①「四角托＝這是一張沖印品」＋現稿四顆 175 處維持現狀；②「對角兩顆＝App icon＋LS-177 起新畫的沖印品」（例 Hero Print `zzXNT` 200×155、Empty Print `K1yE6j` 120×120），既有四顆是否統一另票對帳（LS-96 `6d433e65` 舊債票）；`cmp/Card Diary`／`cmp/Profile Print` 兩個例子都拿掉。自己離線複算：`origin/main` 175 個角托容器全部 4 顆（`Print Cell`×134、`Print Stage`×16、`Photo Print`×10、`Family Print`×4、`Photo Wrap`×3、`Email Print`×2），`cmp/Card Diary`（`qtCd7`）與 `cmp/Profile Print`（`OePXK`）子樹皆 0 顆（＝③零角托，不只是 Card Diary）；`origin/development`（LS-177 已併 #309）175 個 4 顆＋4 個 2 顆（Hero Print `zzXNT`／`i71Sl`／`FhEZw`、Empty Print `K1yE6j`）。`wordmark-brand.md:44` icon 專屬理由句不動。
- **minor-2 已修 `04a7af46acea5bebf99c11f76d549e3e35fd1c2d`** `scripts/gates/design-evidence-check.sh:46`「不證明五支數字算對」、`scripts/design/overflow-scan.test.js:2`「驗四支掃描的語意」→ 指回 `overflow-scan.js` 檔頭。再 grep `[二三四五六]支`（排除 design/evidence）：溢出掃描相關只剩沿革／歷史敘事（「LS-122 擴為四支、LS-168 擴為五支、LS-185 擴為六支」、`overflow-scan.js:1`、`design-evidence-check.sh` LS-122 段與 `:366` 該層錯誤訊息）、相對語（「前四支／前五支」）、序數（「第五支／第六支」）與 test fixture 名；其餘「三支／四支」是 RPC／hook／查詢計數，與掃描無關。
- informational 三項（A2 無樣本／D 等價突變／7 碼純數字誤警可收斂 ≥8）：orchestrator 已表示自行入池處置，本票不動。
- 驗證：`design-evidence-check.test.sh` 53 組、`overflow-scan.test.js` 33 組、`brand-skill-check.sh`＋`.test.sh` 24 組皆綠；push gate 綠；PR body 補 Review 段後 `pr-body-check.sh --branch hotfix/LS-198-pool-cleanup --verify` exit 0。
- 產出：PR #310 head `04a7af46acea5bebf99c11f76d549e3e35fd1c2d`（R1 head `4f86318` ＋ `07abee1`／`04a7af4` 兩個 R2 commit）。

### 2026-09-05T07:04:16.512Z · Chih-Lin Yeh · `951e75c2`

## merge-review R2（增量）— **REQUEST_CHANGES（ITERATE）**（PR #310，head `04a7af46acea5bebf99c11f76d549e3e35fd1c2d`；2026-09-05 15:03）

範圍：`git diff 4f86318..04a7af4`＝2 commit／4 檔（+5/−5，全是文件與註解）。**R1 minor-2 已修好；minor-1 修了一半、同時新引入一個可在五秒內證偽的錯誤敘述** → 這輪 ITERATE，修法是一個子句。其餘四個審點我都驗過、全通過。

### major-1（唯一阻擋項）· 新句把 `cmp/Profile Print` 說成零角托，但它現稿是**對角兩顆**

`07abee1` 在 `motifs.md` ② 列尾端新增：

> 「`cmp/Card Diary`／`cmp/Profile Print` 是零角托（③），不是本段的例」

我把 `origin/main` 與 `origin/feature/LS-177-comments-reactions-design` 兩份 .pen 各重解一次、逐層列出角托 ref（`Mount|Corner` 且 `type=ref`）：

| 節點 | main | LS-177 分支 |
|---|---|---|
| `qtCd7` `cmp/Card Diary` | **0 顆** ✓（歸③正確） | 0 顆 |
| **`OePXK` `cmp/Profile Print`** | **2 顆對角**：`/Print/Mount TL`（`R1lbwf`）、`/Print/Mount BR`（`S4O4r8`），皆 ref → `cmp/Photo Corner` `GEBcf` | **2 顆對角**（同上） |

- **它其實是 main 上唯一一個「非 App icon 的對角兩顆」實例**——我 R1 的分布統計裡「2 顆 14 個」中，13 個是 App Icon 各尺寸、剩下那 1 個就是 `cmp/Profile Print`。所以它是②**最好的既有例子**，R1 版的文字把它列在②是**對的**；這輪把它移到③反而寫錯了。
- **具體失敗情境**：designer 讀 ③（「不是沖印品」）去對齊，可能把 `Print/Mount TL`／`Mount BR` 兩顆拿掉（產品視覺退化）；或 VR 在 LS-177 R3／後續設計輪拿這條當基準，把現稿既有的兩顆判成違規。這份檔案正是 ui-designer／visual-reviewer 每輪要讀的正典，錯誤敘述會直接驅動改稿。
- **修法（一句話）**：把該子句改成——「`cmp/Card Diary` 是零角托（③）；`cmp/Profile Print` 現稿就是對角兩顆（`Print/Mount TL`／`Mount BR` → `cmp/Photo Corner`），是②的既有例」。commit message 裡「`cmp/Card Diary`／`cmp/Profile Print` 皆 0 顆故刪例」同樣不準確，R3 handoff 請一併更正措辭。

### 其餘審點：全部通過（R3 只需看這一句）

1. **①列與 SKILL.md 與我 R1 的離線數據完全一致** ✓：①「現稿（main）四顆共 175 處（`Print Cell`×134、`Print Stage`×16、`Photo Print`×10…），維持現狀」——與我實測的 175／134／16／10 逐項相符；②「App icon 實機尺寸（≤60pt）＋LS-177 起新畫的沖印品（`zzXNT` 200×155、`K1yE6j` 120×120）」也與實測相符（兩者都是 2 顆對角）。**「全稿慣例／稿為準」那句已拿掉**，不再宣稱既有稿是兩顆 ✓（R1 minor-1 的主要部分已解決）。`SKILL.md` 規則 4 的摘要句同語、且**沒有**提到 Profile Print，不受 major-1 影響。
2. **`brand-skill-check` 綠** ✓：真 repo gate「skill 本體齊全、ui-designer／visual-reviewer 已接線」＋自測 24 組全綠。
3. **兩處漏網支數的改法與第 1 組一致** ✓：`design-evidence-check.sh:46`→「不證明各支掃描的數字算對（支數以 `scripts/design/overflow-scan.js` 檔頭為準）」、`overflow-scan.test.js:2`→「驗各支掃描的語意（支數以 overflow-scan.js 檔頭為準）」，句式與 G1 相同。我重跑全 repo grep：扣掉沿革敘事（LS-122／LS-168 層）、相對語（`overflow-scan.js:63`「四支的 BLEED_RE」）、序數與 fixture／commit 訊息之後，**沒有殘留描述現況的寫死支數** ✓。
4. **PR body 兩條「已修」以新規則過 `--verify`** ✓：第 18 行 `07abee1234f158b2bc99f35be457491f3468e180`、第 19 行 `04a7af46acea5bebf99c11f76d549e3e35fd1c2d`，都是「已修」之後第一個 hex，`--branch hotfix/LS-198-pool-cleanup --verify` **exit 0**、兩條都反查到。
5. **head 未前進、無夾帶** ✓：`gh pr view 310 --json headRefOid` 與我審的 SHA 相同；增量只有 4 個檔案且與兩個 commit 宣告一一對應；worktree 乾淨。

### informational

**info-4（新增）· 引用的池項主題對不上**：motifs.md／SKILL.md 都把「既有四顆是否統一」指向 LS-96 `6d433e65`。該 comment **確實存在**（2026-09-05 14:1x，LS-177 VR R4 MN-C），但內容是「root 板重疊 17 對／`board_clip` document 332 筆／`text_occlusion` 9 筆」的設計舊債，**沒有提到角托顆數**。指向它當「舊債清倉票」在精神上說得通，但嚴格說是張冠李戴；建議在該池項補一句角托顆數統一，或改引一則新的池項 id。不阻擋。

**R1 的 info-1／2／3 未動**（`--verify` 不印警告無樣本／memo 前導換行為等價突變／純數字 ⚠ 對 7 碼短 SHA 誤警）：本輪未觸及那些程式碼，結論同 R1，處置交 orchestrator。

### 沒看／沒跑的（誠實揭露）

- 增量是純文件／註解，我**沒有**重跑 R1 的 9 支 mutation 與 `pr-body-check`／`patrol`／`design-evidence-check` 三支自測（被測程式碼一行未變，沿用 R1 結論）；只重跑了與本輪相關的 `brand-skill-check`（gate＋自測）。
- CI：`lint` 已 pass，`ci`／`db`／`rules` 在我審查時仍在跑（run 33951298516）——本輪是 failure 裁決，不受 CI 結論影響；R3 貼 success 前我會等四項有結論。
- 未碰 Pen／真實模擬器／容器；.pen 一律 `git show` 後離線 python 解析；**lock 未持有**。

**verdict：REQUEST_CHANGES**（major-1）。改完那一個子句（＋順手更正 commit/handoff 措辭、看要不要處理 info-4）就重派 R3，我只審該句與 `brand-skill-check`。

### 2026-09-05T07:07:48.246Z · Chih-Lin Yeh · `0d477d4d`

**R3（merge-review R2 `951e75c2` REQUEST_CHANGES）**

- **已修 `8578d3468a992ef9fba86c7dbe67961294657a22`** `.claude/skills/little-sprout-brand/references/motifs.md:28`、`SKILL.md:32`。更正我 R2 comment 的錯誤：`cmp/Profile Print` **不是**零角托——R2 我只比對名稱 `Corner TL/TR/BL/BR`，漏掉名為 `Mount TL/BR`、以 ref 指向 `cmp/Photo Corner`（`GEBcf`）的 instance。
- 自己用 python 對 `origin/main` .pen 重數（同時認 name `Corner/Mount xx` 與 ref==`GEBcf`）：`OePXK`（`cmp/Profile Print`）子樹 ref→`GEBcf` **共 2**：`cmp/Profile Print/Print/Mount TL`（`R1lbwf`）、`/Print/Mount BR`（`S4O4r8`）。全稿角托容器：4 顆 175 個、2 顆 14 個（13 個 App icon／Mark 變體＋`lOPO7` Print＝Profile Print 的紙）——與 reviewer 離線數字一致，`cmp/Profile Print` 是 main 上唯一非 App icon 的既有對角兩顆。`origin/development`（LS-177 已併）：2 顆 18 個（＋Hero Print `zzXNT`／`i71Sl`／`FhEZw`、Empty Print `K1yE6j`）。`cmp/Card Diary`（`qtCd7`）子樹 0 顆，維持③。
- 改動：②列補「`cmp/Profile Print`（`OePXK`）現稿就是對角兩顆（`Mount TL`／`BR` → `GEBcf`），是②的既有例」、保留 `zzXNT`／`K1yE6j` 兩例、`cmp/Card Diary` 留在③；`SKILL.md` 規則 4 同步。info-4：「既有四顆是否統一另票對帳」的引用由 `6d433e65`（板重疊／board_clip 舊債）改為 `a442c747`（角托實況）；`6d433e65` 在 skills／docs 已無引用。
- 驗證：`brand-skill-check.sh`＋`.test.sh` 24 組綠；push gate 綠；PR body 補 R2 REQUEST_CHANGES／R3 已修行後 `pr-body-check.sh --branch hotfix/LS-198-pool-cleanup --verify` exit 0。
- 產出：PR #310 head `8578d3468a992ef9fba86c7dbe67961294657a22`。

### 2026-09-05T07:17:15.220Z · Chih-Lin Yeh · `4d674c1f`

## merge-review R3（增量）— **APPROVE**（PR #310，head `8578d3468a992ef9fba86c7dbe67961294657a22`；2026-09-05 15:16）

範圍：`git diff 04a7af4..8578d34`＝**1 commit／2 檔**（`motifs.md:28`、`SKILL.md:32`，各 1 行）。R2 的 major-1 已修正；**無 blocker／major／minor**，R2 的 info-4 也一併解決。CI 四項全 pass（run 33951658567）。

### (1) 措辭與我的離線數據逐項相符

新的 ② 列：「App icon 實機尺寸（≤60pt…）；**`cmp/Profile Print`（`OePXK`）現稿就是對角兩顆**（`/Print/Mount TL` `R1lbwf`／`/Print/Mount BR` `S4O4r8` → `cmp/Photo Corner` `GEBcf`；main 上唯一非 App icon 的既有例）；**LS-177 起新畫的沖印品用對角兩顆**（`zzXNT` 200×155、`K1yE6j` 120×120）。既有四顆（`Print Cell` 等 175 處）維持現狀…」

逐項對我 R1／R2 的實測：
- 節點 id、路徑、元件 id **完全一致**（`OePXK` 子樹 `/Print/Mount TL`＝`R1lbwf`、`/Print/Mount BR`＝`S4O4r8`，兩者都 ref → `GEBcf`）✓
- 「main 上唯一非 App icon 的既有例」✓——我測到的 2 顆容器共 14 個，13 個屬 App Icon 板（Brand 板 10＋設計語言 v3.3 板 3，名稱都是 `Icon *`），剩下 1 個正是 `cmp/Profile Print`。
- 「既有四顆 175 處維持現狀」✓ 與 175／`Print Cell`×134／`Print Stage`×16／`Photo Print`×10 相符。
- **R2 那句錯誤敘述（「`cmp/Card Diary`／`cmp/Profile Print` 是零角托（③）」）已整句移除** ✓；現在文中不再對 `cmp/Card Diary` 作任何角托宣稱（它實測 0 顆，不提就不會錯）。
- `SKILL.md:32` 摘要句同步為「對角兩顆＝App icon（≤60pt）、既有 `cmp/Profile Print`（`OePXK`）、LS-177 起新畫的沖印品」——與 motifs.md 同語、與稿面一致 ✓。

### (2)~(5)

- **`6d433e65` 無殘留**：全 repo（`*.md`／`*.sh`／`*.js`／`*.yml`）grep 0 命中 ✓；改引的 `a442c747` 出現在 `SKILL.md:32`／`motifs.md:28` 兩處。
- **`a442c747` 這次指對了**：我拉 LS-96 核對，該池項（2026-09-05 14:5x，來源就是我的 R1 informational）正文確實寫「`motifs.md` 角托規則實況——現稿四顆 175 處／兩顆 14 處，『對角兩顆為預設』只對 LS-177 起的新沖印品成立；既有四顆是否統一併入 `6d433e65` 舊債票一次決定」——主題對得上，且鏈到真正的舊債票 ✓（R2 info-4 解決）。
- **`brand-skill-check` 綠**：真 repo gate「skill 本體齊全、ui-designer／visual-reviewer 已接線」＋自測 24 組全綠 ✓。
- **PR body**：三條「已修」（`07abee1…`／`04a7af4…`／`8578d34…`，皆全 40 碼且緊跟「已修」）以 `--branch hotfix/LS-198-pool-cleanup --verify` 直接看 exit code → **0**，三條都反查到 ✓。
- **head 未前進、無夾帶**：`gh pr view 310 --json headRefOid` 與我審的 SHA 相同（貼 status 前 `--expect` 再比一次）；增量只有那兩個文件檔、各 1 行；worktree 乾淨 ✓。
- **CI**：`ci` 9m19s／`db` 3m31s／`lint` 17s／`rules` 3m50s **四項全 pass**（run 33951658567），等到結論才貼 status ✓。

### 沒看／沒跑的（誠實揭露）

- 增量是兩行文件，我**沒有**重跑 R1 的 9 支 mutation 與 `pr-body-check`／`patrol`／`design-evidence-check` 三支自測（被測程式碼自 R1 起一行未變，沿用 R1 結論）；本輪只重跑 `brand-skill-check`（gate＋自測）與 PR body gate。
- 角托數據沿用我 R1／R2 對 `origin/main` 與 `origin/feature/LS-177-comments-reactions-design` 兩份 .pen 的離線解析（只數節點與 ref，不涉版面幾何）；本輪未再重解。
- 未碰 Pen／真實模擬器／容器／正式站；**lock 未持有**。

**verdict：APPROVE**（R2 major-1 已修、R2 info-4 一併解決）。併入 `main` 後補 back-merge `main`→`development`，並補上票文驗收第 2 條的 LS-96 五則銷除註記；R1 的 info-1／2／3 已在 `a442c747` 入池，交 orchestrator 依 §5-b 處置。

### 2026-09-05T09:13:02.229Z · Chih-Lin Yeh · `6aeba4f8`

## QA R1 — delta 驗收 `test` tip `588043e`（PASS）

完整報告貼在 LS-165（三票合併 delta QA）：https://linear.app/little-sprout-app/issue/LS-165（comment `c1454bb5`）。

**本票摘要**：五組自測全綠——`pr-body-check.test.sh` 91 組、`patrol.test.sh` 242 ✓／0 ✗、`design-evidence-check.test.sh` 53 組、`brand-skill-check.test.sh`（實際路徑 `scripts/gates/brand-skill-check.test.sh`）24 組；`docs/API.md` §6／§10 兩處 `net.http_post` 範本皆含 `timeout_milliseconds := 60000`（`:1995`／`:2757`）。全數與 PR #310 handoff 一致，無回歸。

未完成：LS-96 五則池項銷除註記——留 orchestrator（票文載明由 orchestrator 於 PR 併入後註記）。

Status：`qa` success 已貼到 `588043e8b47e0ca4bf6871705a97963db12a187b`。

### 2026-09-05T09:23:28.690Z · Chih-Lin Yeh · `2c11b5f1`

## dead-code sweep — LS-198

**範圍**：`git diff 73b6c5ea77f2353970523f3a4843788b6e8d36ba..35756a346ad1ae8c8b878388e0e5c665effd3d39`（LS-198 hotfix commits，五組池項清倉：`145a355`／`12a0339`／`1ded7e6`／`295fe4c`／`4f86318`／`07abee1`／`04a7af4`／`8578d34`）。共 13 檔（`visual-reviewer.md`／`SKILL.md`／`motifs.md`／`docs/API.md`／`docs/COLLABORATION.md`／`overflow-scan.js`／`overflow-scan.test.js`／`design-evidence-check.sh`／`design-evidence-check.test.sh`／`pr-body-check.sh`／`pr-body-check.test.sh`／`patrol.sh`／`patrol.test.sh`）。唯讀 `git show`／`git diff` 讀取，不改檔、不跑 gate。

### Findings

未發現本 feature 引入的死碼。逐項覆核如下：

- **`sim_linear_note` 三態（實為五分支）殘留**：`patrol.sh` 中 `sim_linear_note` 有 5 個賦值點（未帶 `--linear`／找不到 `patrol-linear.sh`／查詢成功／無 `LINEAR_API_KEY`／查詢失敗），全部在 `--brief`（第 662 行 human 輸出）與 `--json`（第 620／625 行）兩處被實際讀取／印出，非未使用變數；`patrol.test.sh` 新增 ㉓-json 兩案（`.sim_linear_note` 分別驗「未帶 --linear」與「已查 Linear（LS-40、LS-3）」文案）機械釘住，無孤兒分支。
- **`WT_HAS_MEMO` memo 變數**：`ticket_has_worktree()` 新增的一輪內 memo（`case "$WT_HAS_MEMO" in *$'\n'"${1}"$'\t'0*|*$'\n'"${1}"$'\t'1*`）有讀（函式開頭 `case` 短路）也有寫（函式尾端 `WT_HAS_MEMO="${WT_HAS_MEMO}"$'\n'"${1}"$'\t'"${hit}"`），`hit` 變數在 `while` 迴圈與尾端皆被讀取，非死變數；`patrol.test.sh` ㉓-b 另建 LS-77（worktree 記錄仍在、目錄已刪）樣本覆蓋「記錄在、目錄已刪」這個先前未覆蓋的分支，非空測試殼。
- **`du_cache` 原子回寫是否留 `.tmp` 殘檔**：`du_tmp="${du_cache}.tmp.$$"` 寫入成功才 `mv -f` 覆蓋、失敗則 `rm -f "$du_tmp"`，兩條路徑都不留殘檔；`patrol.test.sh` ㉓ 新增 inode 換檔斷言（`ino_before`≠`ino_after`）與「不留 .tmp 殘檔」的 `ls "$PATROL_DU_CACHE".tmp.*` 反向斷言，機械驗證非僅口頭宣稱。
- **`pr-body-check.sh` 純數字候選警告分支**：`case "$shas" in *[!0-9]*) ;; *) echo "⚠ ..." ;; esac` 只在 `verify -eq 0`（格式模式，非 `--verify`）且候選字串全為數字時印警告，`pr-body-check.test.sh` ⑧-a／⑧-b 兩案分別驗「純數字印警告」與「非純數字（SHA 在前）不印警告」，both branches covered，非死分支。
- **`design-evidence-check.sh` 支數動態算法**：`n_scans = len(required) + (1 if fifth else 0) + (1 if sixth else 0)`／`n_zh = {4:"四",5:"五",6:"六"}.get(n_scans, str(n_scans))` 取代原本寫死「五支」的字串；`legacy`（兩支）分支仍用獨立的舊 schema 字串、不會呼叫 `n_zh`，不會因未涵蓋 `n_scans=2` 而 KeyError（用 `.get()` 帶 fallback 亦不會拋例外）；`design-evidence-check.test.sh` ㉞-c 新增六支收據案例釘住「六支掃描皆有輸出」字樣，既有 ⑬／㉒／㉔ 四支收據案例仍驗「四支」字樣不受影響（歷史收據 schema 版本共存屬設計本意，非殘留）。
- **`motifs.md`／`SKILL.md` 角托規則②改寫**：R2／R3 兩輪修正（`07abee1`／`295fe4c`／`04a7af4`／`8578d34`）最終落地版本已補齊 `cmp/Profile Print`（`OePXK`）「子樹 ref → `GEBcf` 2 顆」的既有例、`cmp/Card Diary` 仍歸零角托③類、info-4 的「另票對帳」引用已改指向 LS-96 comment `a442c747`（非中途版本 `6d433e65`）——比對最終檔案內容確認無殘留的中間態措辭（如已被取代的「統一另票對帳 LS-96 `6d433e65`」字樣，現行版本已改為 `a442c747`）。
- **`.github/workflows/ci.yml` 是否仍有「四支／五支」殘句**：本次 LS-198 merge 的 diff --stat 未包含 `ci.yml`（未改動），檢查現行內容（195-196、396 行）已是「六支」措辭，經 `git log` 追溯屬先前 LS-185 落地時已同步更新，非本票遺漏；`git grep -n "四支\|五支" .github/workflows/ci.yml` 於 LS-198 merge commit 上零命中，票文原描述的 7 處中，ci.yml 的兩處實際在票分派前已被更早的 commit 修正，不影響本票驗收（其餘 5 處——`visual-reviewer.md` 兩處、`COLLABORATION.md` 兩處、`design-evidence-check.sh` 一處——皆已改寫並機械核對）。
- **其餘「四支／五支」殘留字樣**：全 repo 掃描確認剩餘命中（`overflow-scan.js`／`overflow-scan.test.js`／`design-evidence-check.sh`／`.test.sh`／`docs/API.md`／`ui-designer.md`／SQL migrations／`design/littlesprout.pen` 內文字節點）皆是描述「LS-122 新增第四支」「LS-168 新增第五支」之類的**歷史沿革敘述**，或與本票主題無關的其他語境（如 LS-6 集合函式「四支」、`notify_*` trigger「四支」），非本票範圍內「現況引用支數」的殘句，符合票文「不做：LS-96 其他池項」的邊界。

### 既有死碼觀察

無（本次巡檢未觸及本 feature 以外的既有程式碼路徑；`ci.yml` 兩處係先前票次已修正，非本票新增觀察項）。

### 結論

LS-198 五組池項清倉皆為文件措辭訂正與 gate 腳本行為微調（純數字警告、原子回寫、memo 快取、支數動態算），逐一核對讀寫成對、測試覆蓋兩個分支、無殘留的舊版字串或死變數。

### 搜尋方法與盲區

- 方法：`git show <sha>:<path>`／`git diff` 逐檔核對五組改動；`git grep -n` 全 repo 掃描 `四支`／`五支`／`六支`／`sim_linear_note`／`WT_HAS_MEMO` 等符號，區分「本票範圍內的現況措辭」與「歷史沿革敘述」；交叉核對對應 `.test.sh` 自測案例是否覆蓋新增分支的兩側（正／反）。
- 盲區聲明：純文字掃描無法判斷 bash `case` 語句在所有 shell 實作（如 `dash` vs `bash`）下的邊界行為是否完全一致（本次假設 `.sh` 檔案的 shebang 與既有 CI 執行環境一致，未額外驗證跨 shell 相容性）；未實跑 `pr-body-check.test.sh`／`patrol.test.sh`／`design-evidence-check.test.sh`／`overflow-scan.test.js` 確認目前狀態下自測仍全綠（僅靜態讀取 diff 內容比對邏輯自洽性，未執行）；`design/littlesprout.pen` 內文字節點的沿革敘述屬設計稿內容，非本巡檢員專長範圍，僅標記為「非本票範圍」而未深入判讀是否也該更新。

### 2026-09-05T09:24:40.563Z · Chih-Lin Yeh · `d2eb4c7e`

**orchestrator 2026-09-05 17:2x 收尾儀式 → Done**

- 併入：PR #310 → main `35756a3`（R1 `4cf2789e`／R2 `951e75c2`／R3 `4d674c1f`）；back-merge #311 → development；delta QA PASS（`6aeba4f8`，合併輪 `c1454bb5`：自測 91／242／53／24 全綠、API.md timeout 補齊）。
- sweeper `2c11b5f1`：0 件死碼（`sim_linear_note` 五態、memo、原子 du 快取、純數字警告皆被讀取與測試）。
- lesson learning：(1) 角托計數判準要用 `ref → cmp/Photo Corner`，名稱判準兩輪都判錯（R2 把 Profile Print 寫成零角托）→ 已成 LS-202；(2) 「N 支」寫死數字散在七處，每加一支就過期——文件引用檔頭而非數字（本票落實）；(3) 池項清倉票每組一 commit＋reviewer 逐組 mutation 是可複用的模式。
- cleanup：worktree 15:34 已清；informational 入池 `a442c747`。LS-96 五則池項（`e6112467`／`092db41b`／`464baaf6`／`10f3b3b2`／`f730a9ac`＋`9f56908b`）視為銷除。

