# LS-135 Task：LS-20 iOS——影片時長徽章改讀 media.duration_seconds（上傳端寫入；列表不再對影片簽全尺寸）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | lane:ui |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M3 相簿與上傳 |
| 估點 | — |
| 建立 | 2026-09-02T19:49:12.204Z |
| 開始 | 2026-09-03T05:17:30.804Z |
| 完成 | 2026-09-03T07:18:53.568Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-135/taskls-20-ios-影片時長徽章改讀-mediaduration-seconds上傳端寫入列表不再對影片簽全尺寸 |
| 父票 | LS-20 Story：相簿與照片上傳（壓縮／縮圖／背景續傳） |
| 關係 | related→LS-96, related→LS-126, related→LS-130, related→LS-129, related←LS-140, related←LS-139, blocks←LS-134 |

## 描述

來源：[LS-130](https://linear.app/little-sprout-app/issue/LS-130/taskls-20-ios-列表詳情改讀-thumb-pathnull-退回原圖全尺寸只在放大播放時簽) handoff 取捨（有縮圖影片徽章只剩「影片」）；依賴後端欄位票 [LS-134](https://linear.app/little-sprout-app/issue/LS-134/taskls-20-後端-mediaduration-seconds影片時長欄上傳端寫入列表徽章不再依賴全尺寸簽名)。

## 範圍

1. 上傳端（`MediaUploadService.uploadVideo`，[LS-129](https://linear.app/little-sprout-app/issue/LS-129/taskls-20-ios-上傳端縮圖mediauploadservice-同步產生並上傳-thumb寫入-thumb-path尺寸) 已落地的路徑）：以 `AVAsset` 量測裁切後時長，INSERT 時寫 `duration_seconds`。
2. 列表／詳情（`PhotoCardView`／`MasonryPhotoWallView`）：徽章改讀 `MediaContent.durationSeconds`；NULL（舊列）退回既有 `loadVideoDuration` 行為但**只在** `signedURL` **為原檔時**才嘗試，縮圖列直接顯示「影片」，不重試。
3. 測試：徽章格式 M:SS、NULL 退回、縮圖列零 `AVURLAsset` 呼叫；mutation 清單。

## 驗收

- [ ] 新上傳影片列表徽章顯示「影片 M:SS」且列表零全尺寸簽名（Storage access log 佐證）
- [ ] merge-reviewer 通過；QA 模擬器實測

## 留言（10）

### 2026-09-03T05:18:17.357Z · Chih-Lin Yeh · `363e4043`

orchestrator 2026-09-03 13:18：LS-134 Done（正式站 `duration_seconds` 已套用）→ blockedBy 解除、In Progress（cycle 1）。派 ios-dev：worktree `.claude/worktrees/LS-135`、branch `feature/LS-135-video-duration-badge`（自 development `5e9b01b`，含 LS-130 R4/R5 徽章基礎與 `.clipped()` 修正）。要點：上傳端寫 `duration_seconds`（`max(1, floor)`，量測失敗 NULL）；列表徽章改讀欄位、NULL 只在原檔 URL 才退回 `loadVideoDuration`、縮圖列零 `AVURLAsset`；若時間軸 RPC 未回傳 `duration_seconds` 視為後端缺口另票、不動 SQL；證據含深色截圖＋Storage log 零原檔簽名。

### 2026-09-03T06:14:04.008Z · Chih-Lin Yeh · `834b795a`

## handoff

**Ticket**：LS-135（影片時長徽章改讀 `media.duration_seconds`；依賴 LS-134 已 Done）
**PR**：#241 `feature/LS-135-video-duration-badge` → `development`，head `8a181e0`

### 已完成（每項 SHA＋file:line）

1. **上傳端量測寫入**（commit `33d4c12`）：`LittleSprout/Services/Diary/MediaUploadService.swift`——`uploadVideo`（`:98-152`）在 PUT 開始前用可注入的 `durationLoader`（預設 `AVURLAsset(url:).load(.duration)`，`:45-54`）量測 `fileURL` 時長，`insertMediaRow` 時寫入 `duration_seconds`；`measureDurationSeconds` 抽到新檔 `LittleSprout/Services/Diary/MediaUploadService+Duration.swift`（`file_length` 拆檔）：`max(1, floor(d))`，量測失敗或 `d<=0` 留 `NULL`、不寫 0、不阻斷上傳。`uploadPhoto` 恆傳 `nil`（`:87-89`）。`MediaRowDescriptor`／`MediaInsertPayload` 加 `durationSeconds`／`duration_seconds`。
2. **讀取端徽章改讀查表值**（commit `ce3c4cb`）：
   - `LittleSprout/Services/Timeline/TimelineModels.swift`——`MediaRow`（`:90-118`）與 `MediaContent`（`:130-179`）新增 `durationSeconds: Int?`（`.select()` 選 `*`，隨既有查詢帶回，不需要 RPC／grant 改動）；`needsVideoDurationLookup`（`:176-178`）加上 `durationSeconds == nil` 條件。
   - `LittleSprout/Services/Timeline/TimelineContentAssembler.swift`——四處 `MediaContent(...)` 組裝（`fetchDiaryPhotos`／`fetchDiaryContents`／`fetchAlbumContents`／`fetchMediaContents`）皆帶 `durationSeconds: row.durationSeconds`。
   - `LittleSprout/Services/Timeline/TimelineStore.swift`——新增 `displayDuration(for:)`（`:216-223`）集中「優先讀 `durationSeconds`、`nil` 才退回 `videoDurations` 快取」的判斷；`PhotoCardView.swift`／`DiaryCardView.swift`／`MasonryPhotoWallView.swift` 三個呼叫端改用它，`.task` guard 本身不變（`needsVideoDurationLookup` 已內含新條件）。
   - `TapTargetGateHarness.swift` 縮圖影片樣本刻意維持 `durationSeconds: nil`（過渡期樣本，`DiaryCardVideoBadgeGeometryTests` 斷言恆顯示「影片」，未改動這個共用 fixture）。
3. **docs/API.md 更新**（commit `df6899a`）：「影片時長徽章讀取策略」段的「iOS 消費者現況」標記與判準更新為 LS-130／LS-135。
4. **測試補強＋拆檔**（commit `8a181e0`）：新增 `test_uploadVideo_durationZero_insertsNullDurationSeconds_notZero`（`measureDurationSeconds` 的 `seconds<=0` guard 先前無直接測試）；`SupabaseMediaUploadServiceTests.swift` 拆出 `SupabaseMediaUploadServiceDurationTests.swift`（`type_body_length`）。

### 已驗證（怎麼驗的）

- 476 個 unit tests（463 既有＋13 新增）全綠；`swiftlint lint --strict` 0 violation；`xcodegen generate` 無漂移；push gate 全綠（含 ≥44pt tap-target gate）。
- `LittleSproutUITests/DiaryCardVideoBadgeGeometryTests` 3 支既有幾何／OCR 測試重跑全綠——確認未動到共用 harness 場景的既有斷言。
- **生產路徑真機模擬**（`LS-135-iPhone17Pro`，本機 Supabase）：`simctl recordVideo` 錄真實 5.613 秒影片→`addmedia`→App 內 Email OTP 登入、建家庭、寫日記發佈。DB 核對 `media.duration_seconds=5`（`max(1,floor(5.613))`）。淺色＋深色截圖皆顯示「▶ 影片 0:05」，像素量測 badge 85.3×18.3pt 單行、卡內，深色同 bbox、文字對比 ≈8.76:1。Storage log（`docker logs supabase_storage_little-sprout`）核對：發佈時間窗內僅 `_thumb.jpg` 被簽名（`sign_many`／`get_signed` 各 1 次），對原始 `.mov` **零**簽名操作。測試資料已清（`storage.objects`／`media`／`diaries`／`families`／`auth.users` 皆 count=0）。證據：`scratchpad/LS-135-evidence/`（截圖／`storage-log-summary.txt`／`measurements.txt`）。
- PR body 附完整 mutation 清單（6 條，對應本票每個關鍵判斷）。

### 未完成（informational 處置）

本輪尚未經 merge-review，無既有 reviewer informational 待處置——一條不缺。自行識別、記錄供下一輪判斷：
- **相簿列表畫面**（`AlbumCardView` 尚未實作，LS-126 票文「不做」）：本票的 `needsVideoDurationLookup`／`displayDuration` 規則已在契約層面涵蓋（`fetchAlbumContents` 已組裝 `durationSeconds`），該畫面實作時比照既有 `PhotoCardView` 呼叫方式即可，不需要另外裁決——不修＋理由：非本票範圍，且無待辦動作可開票。
- **既有 video 列的 `duration_seconds` 回填**：LS-134 migration 檔頭已載明「不在本票範圍，需另立 migration」——本票（iOS 端）沒有可做的事，純後端／資料維運範疇，未另開票（尚未達 High 優先序門檻，記入待辦池 LS-96 comment `5dc9eacc-b104-46e1-ad4f-49ba6bc11cd2`）。

### 風險

- 無 migration（`duration_seconds` 由 LS-134 已上線）。
- `MediaContent` 新增必填欄位 `durationSeconds`（無預設值，比照既有 `thumbWidth`／`thumbHeight` 慣例）——14 處呼叫端（生產＋測試＋harness）皆已顯式傳值，編譯期強制。
- `SupabaseMediaUploadService` 新增建構子參數 `durationLoader`（有預設值），既有呼叫端無需修改。
- `uploadVideo` 的時長量測改為序列（PUT 前完成，不與 `uploadOriginalAndThumb` 並行）：本機檔案 metadata 讀取延遲可忽略，換取「PUT 失敗立即 throw、不被 async-let 隱式等待卡住」，見程式碼註解。

### 產出位置

- PR #241：https://github.com/CLYEH/little-sprout/pull/241（head `8a181e0`）
- `gh pr checks 241`：CI 四項（`ci`／`db`／`lint`／`rules`）皆 pending，剛 push 尚未跑完——不等待，交給 orchestrator 監看。
- 證據目錄：`/private/tmp/claude-501/-Users-clyeh-little-sprout/954fc361-48a1-4e75-b3ca-02a3ce7da71b/scratchpad/LS-135-evidence/`
- 模擬器已關：`CCD7C06F-A2EA-435B-8F12-83A0DCF13D5C`（LS-135-iPhone17Pro，本輪唯一使用，已 shutdown；未用 `simctl shutdown all`）。

---
**補記 2026-09-03 14:35**：merge-review R1 `a55e69cf` minor 2 指出上面「記入待辦池」那行原本缺 LS-96 comment id——已補入池（`5dc9eacc-b104-46e1-ad4f-49ba6bc11cd2`，含該項＋minor 1＋informational n1／n2／n4）並回填 id，本段其餘內容不動。


### 2026-09-03T06:15:14.736Z · Chih-Lin Yeh · `b9661d2b`

orchestrator 2026-09-03 14:15：handoff `834b795a` 收到——PR **#241** head `8a181e0`（上傳端 `durationLoader` 量裁切後時長寫 `duration_seconds`；`MediaContent.durationSeconds` DB 優先、`needsVideoDurationLookup` 需 NULL 且原檔；`TimelineStore.displayDuration(for:)`；13 新測試共 476；真影片 5.6s → DB 5、徽章「影片 0:05」淺／深 85.3×18.3pt；Storage log 零原檔簽名）→ In Review、派 merge-review R1（重點：RPC 是否真的回傳 `duration_seconds`、量測失敗不擋上傳、快取退回 race、mutation 實跑、CI「要跑」）。

### 2026-09-03T06:26:10.629Z · Chih-Lin Yeh · `a55e69cf`

## merge-review R1 — PR #241（head `8a181e0`）

**判定：APPROVE**（0 blocker／0 major；2 minor＋4 informational，皆不擋併入）

審查範圍：`git diff 5e9b01b...8a181e0`（17 檔，+532/−44）。審的 head = `8a181e064f6a6f30b04bfe595996e2991242adcf`，與 `gh pr view 241 --json headRefOid` 當下一致。CI 四項全綠（`ci` 10m12s／`db`／`lint`／`rules`），`ci` job log `06:17:33Z` 印「→ ui-test-trigger：要跑——變更含 LittleSprout/Features/Timeline/DiaryCardView.swift」且真跑 `LittleSproutUITests`（06:17:33→06:22 一段，非略過）；`Executed 476 tests, 0 failures`，與 handoff 申報一致。

---

### 逐項查實

**1. 上傳端（`33d4c12`）**

- `LittleSprout/Services/Diary/MediaUploadService.swift:123` 在 PUT 之前 `await Self.measureDurationSeconds(fileURL:loader:)`，結果經 `MediaRowDescriptor.durationSeconds`（`:341`）→ `MediaInsertPayload`（`:359`／`:373`／`:388` CodingKey `duration_seconds`）寫進 INSERT。`uploadPhoto` 恆傳 `nil`（`:88`）。
- 量測失敗**確實不擋上傳**：`MediaUploadService+Duration.swift:21` 是 `try? await loader(...)` 加 `guard`，回傳 `nil` 而不是 throw；`uploadVideo` 的 do/catch 只包 PUT 與 INSERT，量測不在任何 throwing 路徑上。Swift 合成的 `Encodable` 對 Optional 用 `encodeIfPresent`，`nil` 時整個 key 省略（不是送明確 null）→ DB 留 NULL，不會撞 `media_duration_seconds_positive`。實跑佐證見下方測試段。
- **順序（效能維度）**：量測是序列的、在 `uploadOriginalAndThumb`（`:125`）之前，不與 PUT 並行。**這不是缺陷**：量測是一次本機 moov metadata 解析（毫秒級），PUT 是秒級網路作業，而且結果要等 PUT 完成後的 INSERT 才用得到 —— 並行化的收益上限≈0；`:115-122` 給的理由（`async let` 沒有顯式 await 時仍會在 scope 結束前隱式等待，會延後 PUT 失敗的 throw）也成立。另注意 `makeVideoPendingThumbnail`（`:112`／`:302`）已經對同一支檔案開了一個 `AVURLAsset`，技術上可共用省一次載入，但會犧牲 `durationLoader` 的可注入性（測試就得準備真影片檔）——這個取捨我同意，不列 finding。
- `docs/API.md`（`df6899a`）：§3「影片時長（`duration_seconds`，LS-134）」條目（`:269-286`）本來就寫明 `max(1, floor(d))`／失敗留 NULL 不寫 0，與 `MediaUploadService+Duration.swift:22-23` 逐字吻合；本次只改 §6「iOS 消費者現況」段（`:1285-1296`），把 LS-130 標記補成 LS-130／LS-135 並寫出 `needsVideoDurationLookup` 的實際判準（`type == .video && !isThumbnail && durationSeconds == nil`）與「縮圖列一律不查」——與程式碼一致。

**2. 讀取端（`ce3c4cb`）——時間軸真的會拿到 `duration_seconds`：查實通過**

- 時間軸**不經 RPC 取 media**：`get_family_timeline`（`supabase/migrations/20260902011514_diary_album_multi_child_tags.sql:633-643`）的 `returns table` 只有 `kind／ref_id／occurred_at／child_ids`，是純指標；media 走 `SupabaseTimelineAPIClient.swift:82-87` 的 `client.from("media").select()`，supabase-swift 的 `select()` 預設參數即 `"*"`，所以新欄位隨既有批次查詢一起回來，**不需要改 RPC 或 grant**（`20260902195055_media_duration_seconds.sql` 檔頭亦載明 SELECT 是整表 grant、只有 UPDATE 逐欄列舉）。`MediaRow` 的 CodingKeys（`TimelineModels.swift:112-120`）已含 `case durationSeconds = "duration_seconds"`。註解宣稱與事實一致。
- `needsVideoDurationLookup`（`TimelineModels.swift:192-194`）＝ `type == .video && !isThumbnail && durationSeconds == nil`，符合票文「NULL 只在原檔 URL 才退回 `loadVideoDuration`、縮圖列直接『影片』不重試」。三個 `.task` guard（`PhotoCardView.swift:35`／`DiaryCardView.swift:181`／`MasonryPhotoWallView.swift:52`）照抄同一個屬性，沒有分岔。
- `TimelineStore.displayDuration(for:)`（`TimelineStore.swift:233-235`）的 race 檢查：`TimelineStore` 是 `@MainActor @Observable`（`:18-20`），這支是純讀、不寫任何狀態；`loadVideoDuration`（`:215-225`）與 `failedDurations`／`loadingDurations`／`generation` 一行未動，`reset()`（`:183-194`）行為不變。**DB 值優先**代表殘留在 `videoDurations` 的舊 client 量測值不可能蓋掉權威值（順序是 `durationSeconds ?? cache`，不是相反）。額外一個小好處：`??` 右手邊是 autoclosure，有 DB 值時 body 根本不會讀 `videoDurations`，也就不會註冊那份字典的 observation 依賴，少一類無謂重繪。找不到新的競態。

**3. 測試鑑別力——實跑 2 支 mutation（非推理）**

方法：把 worktree `rsync` 一份到 scratchpad（review 全程唯讀，未改 worktree 任何檔案），`-derivedDataPath` 指 scratchpad，跑在本票專屬機 `LS-135-iPhone17Pro`（`CCD7C06F-A2EA-435B-8F12-83A0DCF13D5C`），完成後只關這顆 UDID（未用 `shutdown all`）。基準先跑四個 class 全綠（exit 0）。

- **M1｜`TimelineStore.swift:234` 拿掉 `content.durationSeconds.map(TimeInterval.init) ??`（只讀快取）** → exit 65，`DiaryCardVideoBadgeTests.test_thumbnailVideoWithDurationSeconds_neverAttemptsLoad_badgeShowsDbDurationImmediately` 紅：`DiaryCardVideoBadgeTests.swift:114: XCTAssertEqual failed: ("影片") is not equal to ("影片 0:45")`。＝票文要修的缺陷本身能被測試抓回來。
- **M2｜`MediaUploadService+Duration.swift:23` 拿掉 `max(1, ...)`** → exit 65，`SupabaseMediaUploadServiceDurationTests.test_uploadVideo_durationBelowOneSecond_roundsUpToOne` 紅：`:86: XCTAssertEqual failed: ("Optional(0)") is not equal to ("Optional(1)")`（0.4s → 0）。同一輪 `test_uploadVideo_durationZero_insertsNullDurationSeconds_notZero` **保持綠**，證實 `max(1,...)` 與 `seconds > 0` 兩個 guard 是各自獨立被釘住的（PR body 對這兩條的敘述屬實）。
- 兩支還原後 `TimelineStoreTests` 的三支 `displayDuration` 測試（`TimelineStoreVideoTests.swift:82/98/117`）＋4 支上傳端測試全綠。上傳端測試的斷言寫在 mock handler 內，但 `uploadVideo` 必定打 `/rest/v1/media`、未命中的路徑會 `XCTFail`，不存在「斷言沒跑到卻綠」的漏洞。13 支新測試數目與 476 總數核對無誤。

**4. 證據（`scratchpad/LS-135-evidence/`）**

抽看 `badge-crop-light.png`／`badge-crop-dark.png`：兩張都清楚顯示「▶ 影片 0:05」單行膠囊、未截斷，深色版與淺色版同一 bbox，與 `measurements.txt` 的 85.3×18.3pt／DB `duration_seconds=5`（5.613s → `max(1, floor)` = 5）自洽。`storage-log-summary.txt` 摘錄核對：命中本次上傳物件的操作只有 2 筆 `storage.object.upload`（`.mov` 與 `_thumb.jpg`）＋2 筆 `sign_many`＋**2 筆 `get_signed`，兩筆都是 `_thumb.jpg`，對原始 `.mov` 零筆**。精確一點說：`sign_many` 的 path 清單在 request body、log 行看不到，所以「零全尺寸簽名」是由（a）零筆全尺寸 `get_signed`＝零全尺寸 egress 與（b）程式碼層 `displayPath` 在 `thumb_path != nil` 時只簽縮圖（`TimelineContentAssembler.swift:100/142/169/187`）、全尺寸只在 `signFullSizeURL`（播放當下）現簽共同支撐——結論成立，敘述可以再精準一點。

**5. PR body／CI**：`Design:` 行有（指名 `cmp/Card Photo` 的 `Video Badge`，並說明本票不改稿）、mutation 清單 6 條、風險 5 條（含拆檔理由與 `MediaContent` 新增必填欄位的編譯期強制）。CI 四項見開頭。

**6. Scope**：17 檔全部可追溯到票文。兩個新檔合理——`MediaUploadService+Duration.swift`（`file_length`）與 `SupabaseMediaUploadServiceDurationTests.swift`（`type_body_length`），各有既有前例（`InviteFamilyView+ActionBar.swift`／`SupabaseMediaUploadServiceCleanupTests.swift`）；拆檔 commit `8a181e0` 是純搬移（新檔 +200／`SupabaseMediaUploadServiceTests.swift` −130，該檔對 base 淨 0 差異，逐 commit 核對過）。`TapTargetGateHarness.swift`／`project.pbxproj`／各測試檔的改動都是「`MediaContent` 新增無預設值欄位」的編譯期強制。未動 `Navigation/`、未動 SQL、未夾帶無關重構。

---

### Findings

**minor 1（PLAUSIBLE，未重現）— `LittleSprout/Services/Diary/MediaUploadService+Duration.swift:23`：`Int(seconds.rounded(.down))` 沒有上界**

失敗情境：`loader` 回傳的 `CMTime` 通過 `isValid`／`!isIndefinite`／`isFinite`／`> 0` 四道 guard，但秒數荒謬大——例如壞損或人為構造的 moov 把 duration 寫成 `CMTime(value: Int64.max, timescale: 1)`（≈9.2e18 秒）。`Double → Int` 轉換超出 `Int64` 範圍會直接 trap，上傳在 PUT 之前 crash。較溫和的同型：32-bit 溢位型壞檔（≈4.29e9 秒）不會 crash，但會寫進 `duration_seconds` 且**無 UPDATE grant 不可再改**，徽章從此顯示「影片 71582788:15」。

為何不是 major：App 自己的路徑是 PhotosPicker → `VideoTrimmer` 匯出的暫存檔，正常不會產生這種 CMTime，我也沒有重現（沒有現成的壞檔）。修法一行：guard 補合理上界（例如 `seconds < 24 * 3600 * 30`）或 clamp 後再轉。

**minor 2（流程，不影響程式碼）— handoff `834b795a`「未完成」段第 2 條的待辦池申報沒有 id**

「既有 video 列的 `duration_seconds` 回填……記入待辦池而非另票」——但 LS-96 目前沒有任何 LS-135 相關 comment（我逐則看到 2026-09-03 13:40 `5f80087e` 為止）。CLAUDE.md handoff 規則要求「記入待辦池 LS-96」帶 id，且 LS-96 `224049b7` 已把同型空頭申報記為 **P1「第二次重踩」**。請補一則入池 comment 並回填 id，或改寫成「不修＋理由」（這一條其實比較像後者：iOS 端無事可做，純資料維運）。本輪 R1 之前無 reviewer informational 待處置，這一項是自我識別項，不擋併入。

**informational（不擋，建議記入 LS-96 同批）**

- **n1 註解過期**：`LittleSprout/Support/VideoDurationFormat.swift:3-4`「`media` 表沒有 `duration` 欄位，時長是 `TimelineStore.loadVideoDuration` 向簽名 URL 讀 `AVURLAsset` 得到的」與 `TimelineStore.swift:36-38`（`videoDurations` 屬性註解）同一句，在本票之後都不成立了（查表值才是主來源，client 量測是退回路徑）。API.md 已同步，這兩處沒跟上。
- **n2 缺 JSON 解碼測試**：`MediaRow.durationSeconds` 的 CodingKey 若打錯，Optional 屬性走 `decodeIfPresent` 會**靜默**解成 `nil`、徽章無聲退回「影片」，而 13 支新測試全部在 Swift 端直接建構 `MediaRow`／`MediaContent`，抓不到這型錯誤；目前唯一佐證是 evidence 的生產路徑實跑（DB=5 ↔ 截圖 0:05）。既有 `thumb_path` 同樣沒有解碼測試（既有慣例、非本票引入），所以不算迴歸。
- **n3 PR body 小誤**：mutation 清單第 4 條把 `test_uploadVideo_durationZero_...` 標成 `SupabaseMediaUploadServiceTests`，`8a181e0` 拆檔後實際在 `SupabaseMediaUploadServiceDurationTests`。
- **n4 操作提示**：`-only-testing:LittleSproutTests/TimelineStoreVideoTests` 會靜默跑 0 支（該檔是 `extension TimelineStoreTests`，class 名是 `TimelineStoreTests`）——我第一輪 mutation 就踩到，記下來省下一位的時間。

### 我沒審的範圍

- 未在真機／模擬器重跑 UI 視覺驗收（沿用 ios-dev 證據＋CI 的 `LittleSproutUITests` 全綠）；徽章版面對稿留給 QA。
- 未跑 `supabase db reset`／DB 測試——本票無 migration、無 SQL 變更（`supabase/` 零改動）。
- `AlbumCardView` 尚未實作（LS-126 票文「不做」），只在契約層核對 `fetchAlbumContents` 已帶 `durationSeconds`。


### 2026-09-03T06:27:42.075Z · Chih-Lin Yeh · `42e4c383`

orchestrator 2026-09-03 14:27：merge-review R1 `a55e69cf` **APPROVE**（讀取端資料源查實：時間軸 media 走 `from("media").select()`＝`select *`，`duration_seconds` 隨既有查詢回來、不需後端另票；mutation 兩支實跑翻紅；CI「要跑」476 tests 0 failures）→ PR #241 併入 development **`fbe776e`**（status 已貼）→ 背景等 CI 綠後 `promote.sh development test` → QA（新上傳影片徽章 M:SS、NULL 舊列退回只在原檔、縮圖列零 AVURLAsset、Storage log 零原檔簽名、深色）。minor 1（`Int(rounded)` 無上界 PLAUSIBLE）與 minor 2（handoff「記入待辦池」無 id，第三次）→ 已要求 ios-dev 補入池並回填 id；LS-96 `1acc2ad2`。

### 2026-09-03T06:41:13.090Z · Chih-Lin Yeh · `d398e6f6`

orchestrator 2026-09-03 14:41：CI 綠 → `promote.sh development test` → **test＝`fbe776e`** → QA、派 qa R1（新上傳影片含裁切 → DB 秒數＋徽章 M:SS 淺／深；≥60 秒進位；Storage log 零原檔簽名；NULL 舊列退回只在原檔、縮圖列只顯示「影片」；smoke＋全測試）。PASS → 收尾 → promote main → v0.8.3。

### 2026-09-03T07:14:29.793Z · Chih-Lin Yeh · `43d77ea1`

## QA R1 — PASS

**test HEAD**：`fbe776ecf22cff7ee175fcdd69a64cff07920250`（QA worktree `.claude/worktrees/qa-test` detached checkout，等於 `origin/test`）
**環境**：`qa-test-iPhone17Pro`（`5F59FF04-D2C6-413D-B31A-AFD39B257FB3`），本機 Supabase 已 `supabase-lock.sh -- supabase db reset`（含 `20260902195055_media_duration_seconds.sql`）。

### 逐項驗收

**1. 新上傳影片：App 真實發佈（含 60 秒裁切流程）→ DB → 徽章 M:SS — ✓ PASS**

- `simctl recordVideo` 錄真實桌布影片：短版 6.585s（`AVURLAsset` 量測）、長版 66.79167s。長版 `simctl addmedia` 進相簿，App 內走完整流程：Email OTP 登入（Mailpit 取碼 `373740`）→ 建家庭「LS135QA」→ 寫日記選長影片 → composer 顯示裁切警告「這支影片 1:06，發佈時會保留前 60 秒」→ 發佈。
- DB 直查（SQL 貼值）：
  ```
  id=9a5fe376-8f84-4227-b44a-460badd63d47
  storage_path=.../9a5fe376-8f84-4227-b44a-460badd63d47.mp4
  duration_seconds=60
  ```
  裁切目標剛好 60.0s（`VideoTrimmer` `CMTimeRange(duration: 60)`），伺服端 `max(1,floor(d))` 得 60——落在「≥60 秒案例」的 `1:0x`（x=0）區間，M:SS 進位（分鐘進位）行為以此案驗證：60 秒正確顯示為 `1:00` 而非 `0:60`。
- 徽章文字：**獨立照片卡**（`PhotoCardView`，無日記文字）與**日記卡預覽格**（`DiaryCardView` `previewPhotosRow`）皆顯示「▶ 影片 1:00」。像素量測（screenshot @3x scale，暗色 pill 邊界偵測）：兩處 pill 皆 **83.3×18.0pt**、單行無截斷、在卡片邊界內（卡寬 354pt）。
  - 淺色：`screenshots/timeline-both-badges-light.png`、裁切特寫 `screenshots/badge1-crop.png`（照片卡）／`badge2-crop.png`（日記卡）
  - 深色（`simctl ui ... appearance dark`）：`screenshots/timeline-both-badges-dark.png`、裁切特寫 `screenshots/badge1-crop-dark.png`／`badge2-crop-dark.png`——badge 為固定深底膠囊，深色模式下 bbox 與文字皆與淺色一致。

**2. 列表零全尺寸簽名（Storage log 佐證）— ✓ PASS**

- 時間窗 07:00–07:05 UTC（涵蓋發佈＋時間軸瀏覽＋淺/深色切換多次重繪）：`docker logs supabase_storage_little-sprout` 逐行解析，`sign_many`／`get_signed` 只命中 `_thumb.jpg`（16 筆），對 `9a5fe376...mp4` **零筆**。存檔：`storage-log-window1.log`。
- 正控制組（證明「打開詳情播放才簽原檔」為真，非徽章邏輯誤判為零）：進入日記詳情、雙擊播放影片 → 07:05–07:06 窗口內出現 `sign_many`＋4 筆 `get_signed` 命中 `9a5fe376...mp4`。存檔：`storage-log-play-positive-control.txt`。

**3. NULL 舊列退回 — ✓ PASS（兩案分開建構，同 LS-130 R3 手法：複製既有真實影片位元組＋手動註冊 `storage.objects`＋直接 `insert into media`，觸發 `media_feed_insert` 自動同步 `feed_items`）**

- **Case A**（`thumb_path` NULL、`duration_seconds` NULL）：`displayPath` 退回 `storage_path`（原檔）。App 徽章顯示「影片 1:00」——`TimelineStore.loadVideoDuration` 對原檔簽名量到的 client 端時長（同一份影片位元組，量到的秒數與伺服端一致）。Storage log 可見 `get_signed` 命中 `nullrow-a-video.mp4`（預期內，因為此時原檔本來就是顯示用圖／時長來源）。
- **Case B**（`thumb_path` 非 NULL、`duration_seconds` NULL）：App 徽章只顯示「影片」（無時間），縮圖正常顯示。同一時間窗內對 `nullrow-b-video.mp4` **零筆** `sign_many`／`get_signed`（只有 `nullrow-b-thumb.jpg` 被簽）——確認「縮圖列不重試」。
- 截圖：`screenshots/nullrow-cases-light.png`（原圖）、`screenshots/nullrow-cases-crop.png`（裁切特寫，上：Case B「▶ 影片」、下：Case A「▶ 影片 1:00」）。Storage log：`storage-log-nullrow.txt`。SQL：`nullrow-objects.sql`／`nullrow-media.sql`。

**4. 回歸冒煙 — ✓ PASS**

- 登入（Email OTP）✓；時間軸載入 ✓（多次 relaunch／reload）；照片上傳 ✓（另發一篇純照片日記，成功發佈、卡片正常渲染、無徽章）；詳情開合 ✓（日記詳情頁開啟、雙擊播放影片）；Tab Bar（時間軸／相簿／寶貝／設定）✓ 四頁皆可切換、無 crash。留言功能為既有 placeholder（「留言功能即將推出」），非本票範圍。
- `xcodebuild test`（`qa-test-iPhone17Pro`，`-derivedDataPath` scratchpad）：**`LittleSproutTests.xctest` 476 tests 0 failures ＋ `LittleSproutUITests.xctest` 20 tests 0 failures**，`** TEST SUCCEEDED **`。log：`xcodebuild-test.log`。

### 未驗（超出本輪 dispatch 範圍，不計入裁決）

- RLS 跨 family SQL 冒煙：本輪 dispatch 未列入驗收清單（本票無 migration／RLS 變更），未跑 `supabase/tests/run.sh`。

### 產出位置

證據目錄：`/private/tmp/claude-501/-Users-clyeh-little-sprout/954fc361-48a1-4e75-b3ca-02a3ce7da71b/scratchpad/LS-135-qa-r1/`（screenshots/、videos/、storage-log-*、xcodebuild-test.log、nullrow-*.sql）

模擬器已關：`5F59FF04-D2C6-413D-B31A-AFD39B257FB3`（qa-test-iPhone17Pro）

**裁決：PASS** — 4 項驗收條件（新上傳影片徽章＋DB／零全尺寸簽名／NULL 退回兩案／回歸冒煙）全數通過，證據齊備。

### 2026-09-03T07:15:27.777Z · Chih-Lin Yeh · `bf22cdfb`

orchestrator 2026-09-03 15:15：QA R1 `43d77ea1` **PASS**（真實 60 秒裁切上傳 → DB 60 → 徽章「影片 1:00」淺／深 83.3×18.0pt；列表零原檔簽名、播放才簽；NULL 兩案例退回正確；476＋20 測試綠；qa success 貼 `fbe776e`）→ `promote.sh test main` → **main＝`fbe776e`**、tag **v0.8.3**。派 sweeper → retro → cleanup → Done。

### 2026-09-03T07:18:31.809Z · Chih-Lin Yeh · `29c89d8b`

## dead-code-sweeper 報告（orchestrator 代貼，LS-96 `5cb5ddf3`）

範圍 PR #241（`5e9b01b...8a181e0`，17 檔）。**未發現本票引入的死碼**：(1) `needsVideoDurationLookup`（`TimelineModels.swift:192`）仍是 `PhotoCardView:35`／`DiaryCardView:181`／`MasonryPhotoWallView:52` 三處 `.task` guard，`loadVideoDuration`／`failedDurations` 為 NULL 舊列真實退回路徑（QA Case A 實測）；(2) `durationLoader` 預設實作由 `LittleSproutApp.swift:35` 生產使用，注入點供 4 支測試／harness；(3) `displayDuration(for:)` 已取代三處直接讀 `videoDurations`，生產碼無殘留繞路；(4) `MediaRow.CodingKeys.durationSeconds = "duration_seconds"` 與 migration／API.md §3／§6 一致；(5) 三新檔皆在 pbxproj Sources；`TimelineStoreVideoTests` 是 `TimelineStoreTests` extension → `-only-testing` 命名陷阱（非死碼，LS-96 `5dc9eacc` 第 5 項）；(6) 無 `LS-135-*` 進 repo；(7) 過期註解 `VideoDurationFormat.swift:3-4`／`TimelineStore.swift:36-38`（既有行、本票未觸碰；LS-96 `5dc9eacc` 第 3 項）。鷹架標記零命中；未動 CI／lint／scripts。盲區：純 grep；未重跑 `-only-testing`（採信 QA 476+20 與 mutation 實跑）。

### 2026-09-03T07:18:50.354Z · Chih-Lin Yeh · `b532cb49`

## 收尾（orchestrator 2026-09-03 15:18）

- **sweeper**：`29c89d8b`，無死碼。
- **Retro**：(1) 從 LS-134 Done 派工到 v0.8.3 出貨 2 小時（13:18→15:15），一輪 review APPROVE、一輪 QA PASS——後端契約票先落地、iOS 票只做讀寫接線的拆法，配合 LS-130 已備好的徽章與 `.clipped()` 基礎，是這條鏈最順的一段。(2) 讀取端資料源不經 RPC（`from("media").select()`＝`select *`）——reviewer 查實免掉一張後端票；未來欄位新增票先查 select 形狀再決定是否需要 RPC 改動。(3) handoff「記入待辦池」第三次空話（LS-96 `1acc2ad2`）→ 「handoff 申報可驗證 gate」cycle 2 第一批不可再延。(4) minor 1 `Int(rounded)` 無上界 PLAUSIBLE（LS-96 `5dc9eacc`）；過期註解（`VideoDurationFormat.swift:3-4`／`TimelineStore.swift:36-38`）與 `-only-testing` extension 命名陷阱同池。(5) `cleanup-merged.sh` 因 `Config/Secrets.xcconfig`＋`supabase/.temp/` 略過（第 6 次，`25a4ff4d` P1），手動 `remove --force`。
- **清理**：worktree `.claude/worktrees/LS-135` 已移除；本機分支 `feature/LS-135-video-duration-badge` 已刪；遠端無殘留。
- 狀態 → **Done**（隨 **v0.8.3** 出貨，main `fbe776e`）。

