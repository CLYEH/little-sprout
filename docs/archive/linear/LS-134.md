# LS-134 Task：LS-20 後端——media.duration_seconds（影片時長欄，上傳端寫入；列表徽章不再依賴全尺寸簽名）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | lane:backend |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | M3 相簿與上傳 |
| 估點 | — |
| 建立 | 2026-09-02T19:49:03.640Z |
| 開始 | 2026-09-02T19:49:50.787Z |
| 完成 | 2026-09-03T05:17:15.718Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-134/taskls-20-後端-mediaduration-seconds影片時長欄上傳端寫入列表徽章不再依賴全尺寸簽名 |
| 父票 | LS-20 Story：相簿與照片上傳（壓縮／縮圖／背景續傳） |
| 關係 | related→LS-96, related→LS-125, related→LS-129, related→LS-126, related→LS-128, blocks→LS-135, related→LS-119, related→LS-130 |

## 描述

來源：[LS-130](https://linear.app/little-sprout-app/issue/LS-130/taskls-20-ios-列表詳情改讀-thumb-pathnull-退回原圖全尺寸只在放大播放時簽) handoff（2026-09-03）——列表改讀 `thumb_path` 後，影片卡的「影片 M:SS」徽章因 `AVURLAsset.load(.duration)` 拿到的是縮圖 JPEG 而失敗，只剩「影片」；保留徽章需列表階段簽全尺寸＝繞開 PLAN §7 egress 防線。[LS-119](https://linear.app/little-sprout-app/issue/LS-119/設計日記編輯器時間軸卡片流ls-21-畫面群-ui-designervisual-reviewer-3-輪) 核可稿的影片徽章規格是「影片 M:SS」（固定 12pt），必須恢復。

## 範圍

1. migration（非破壞性）：`public.media` 加 `duration_seconds integer` nullable（CHECK > 0）；RLS 不變；API.md §3／§6 補欄位語意（影片必填，照片 NULL；上傳端以 `AVAsset` 量測，裁切後以裁切後長度為準）。
2. 測試：欄位／CHECK 正負案例；既有 RLS 測試不變；`api-contract-check.sh` 綠。
3. iOS 端（上傳寫入、列表讀取）另票 LS-135。

## 驗收

- [ ] migration 非破壞性、測試綠、API.md 更新
- [ ] merge-reviewer 通過；QA 以 SQL 寫入含 `duration_seconds` 的列並讀回

## 留言（11）

### 2026-09-02T19:50:20.615Z · Chih-Lin Yeh · `41fc794e`

orchestrator 2026-09-03 06:20：backend lane 空 → scope+ 進 cycle 1、派 ios-dev（純後端）。Worktree `.claude/worktrees/LS-134`、branch `feature/LS-134-media-duration`（自 development `dc10347`）；慣例照 LS-128；不加 kind 相依約束（避免既有列違反）；欄位級 UPDATE grant 不含新欄。LS-135 blockedBy 本票。

### 2026-09-02T19:59:41.279Z · Chih-Lin Yeh · `1c84f396`

## ios-dev handoff — LS-134（orchestrator 代貼摘要，2026-09-03 06:45）
PR **#233** → development，head **`7fa495f`**（`eb380cc` migration／`694ed4c` tests／`7fa495f` docs）。
- `20260902195055_media_duration_seconds.sql`：`duration_seconds integer` nullable＋CHECK `media_duration_seconds_positive`（>0）；刻意不加 type 相依約束（既有 video 列為 NULL 會違反）；不加 UPDATE grant（同 storage_path／thumb_*）；RLS／grant／RPC 不變。
- `99_media_duration.sql` 四段：欄位／CHECK 正負／跨家庭 RLS／UPDATE 42501。API.md §3 語意（AVAsset 量測、裁切後、向上取整、照片 NULL）＋§6 徽章策略（NULL 退回「影片」、不得為徽章簽全尺寸）。
**已驗證**：db reset 18 migrations；run.sh 全綠；mutation 實跑（拿掉 CHECK→0／−1 插入成功；補 grant→UPDATE 成功）；api-contract／migration-breaking（零命中）／immutable／push gate 綠。無模擬器。→ In Review，派 merge-review。

### 2026-09-02T20:08:32.155Z · Chih-Lin Yeh · `345014b1`

## merge-review R1 — PR #233（base `dc10347` → head `7fa495f`）

審查範圍：`git diff dc10347...7fa495f` 全部 3 檔（`docs/API.md` +17／`supabase/migrations/20260902195055_media_duration_seconds.sql` +51／`supabase/tests/99_media_duration.sql` +184），零 Swift 變更。CI 四項全綠（ci／db／rules／lint，run 33676525309）。

**Verdict：REQUEST_CHANGES**（1 major，0 blocker）

---

### M1（major）契約：`docs/API.md:272`「整數秒**向上取整**」與本 repo 既有、且被測試釘住的「無條件捨去」慣例正面衝突，PR 全文未提及此衝突

同一個「影片 M:SS」徽章的取整方向，repo 內已有一條被 merge-review 明文裁定並落地的相反規則：

- `LittleSprout/Support/VideoDurationFormat.swift:8-11`：「merge-review R1 i2：無條件捨去到秒，**不四捨五入**——對齊 LS-125 `DiaryDurationFormat.string(from:)` 的既有慣例（**同一支影片在編輯器與時間軸卡片本來應該顯示同一個數字**）」
- `LittleSprout/Support/DiaryDurationFormat.swift:10-11`：同樣無條件捨去
- 兩條都有單元測試釘住：`LittleSproutTests/VideoDurationFormatTests.swift:12-13`（599.6 → `9:59`，不是 `10:00`）、`LittleSproutTests/DiaryDurationFormatTests.swift:18`（32.9 → `0:32`）

**具體失敗情境**：使用者選一支 41.4 秒的影片。
1. 日記編輯器（`LittleSprout/Features/Diary/DiaryEditorView+Photos.swift:100`／`:150`）顯示 `影片 0:41`（`DiaryDurationFormat` 捨去）。
2. LS-135 依本 PR 契約寫入 `ceil(41.4) = 42`。
3. 時間軸卡片徽章讀 `duration_seconds` 交給 `VideoDurationFormat.badgeText`（對整數而言捨去＝原值）→ `影片 0:42`。

同一支影片、相鄰兩個畫面、兩個數字——正是 R1 i2 明文要避免的情況；而且**只要時長不是整數秒就必然發生**（幾乎所有影片）。

**加重情節**：本欄刻意不開 UPDATE 欄位級 grant（`§3` 自己寫了「一旦 `INSERT` 寫入即不可再 `UPDATE`」）。取整方向若之後要改，已寫入的列**沒有合法修正路徑**，需要另一支 migration 開 grant（§3 對 `thumb_path` 已寫過同一個代價，行 250-255）。這不是「之後再說」的等級。

**「避免徽章顯示 `0:00`」的理由成立，但不需要反轉慣例即可滿足**：`max(1, floor(d))` 同時解決 `0:00` 與 `CHECK > 0`，且與 i2 一致。

**建議修法（擇一，並在 §3 明寫選了哪個與理由，CLAUDE.md Rule 6：衝突要挑一個，不要各寫各的）**：
- (a) 推薦：§3 改成「無條件捨去到秒，**下限 1 秒**（不足 1 秒的影片記為 1）」——零 Swift 變更，與 i2、兩支 formatter、兩個測試檔全部一致。
- (b) 若確定要改成向上取整：§3 要明寫「本條**推翻** LS-126 merge-review R1 i2」與理由，並在 LS-135 票面加上「同步改 `VideoDurationFormat`／`DiaryDurationFormat` 與對應兩個測試檔」，否則 LS-135 會落地成 (2)(3) 的不一致。

---

### m1（minor）`docs/API.md:269-278`：§3 沒寫「量測失敗／量到 ≤0 時要寫什麼」——寫 0 會讓整筆上傳在 Storage PUT 之後才失敗

§6（行 1271-1272）提到「上傳端量測失敗的過渡列」是 `NULL`，但 LS-135 實作時讀的是 §3 這段寫入契約，那裡沒有這句。

**具體失敗情境**：資產損毀／`AVAsset` 回 `CMTime` 0（或實作者用 `floor` 而未夾下限）→ `duration_seconds = 0` → `INSERT` 撞 `media_duration_seconds_positive`（23514）。而依 §3「上傳流程順序」（行 256-268），此時原檔與縮圖**都已經 PUT 進 Storage**，整筆上傳失敗並留下兩個孤兒物件要 client 自己清——一個取整細節升級成上傳失敗。

**建議**：§3 補一句「量不到、或量到 ≤ 0 秒時**留 `NULL`**（退回純文字「影片」徽章），**不要寫 0**」。與 M1 的 (a) 修法合併一句即可。

---

### m2（minor）`docs/API.md:67`（§2 總覽表）不可變欄位清單未同步加上 `duration_seconds`

§3 行 277-278 宣告本欄不可變，但 §2「資料表讀寫路徑總覽」`media` 那列的不可變清單仍只到 `…／thumb_path／thumb_width／thumb_height（LS-128）`。本 PR 自稱比照的先例 LS-128（commit `df283e9`）**有**同步更新這格。

**具體失敗情境**：LS-135 實作者先讀 §2 的「給 iOS 呼叫端的心智模型」表，看到 UPDATE 欄位只有四欄、不可變清單沒有 `duration_seconds`，於是設計成「先 `INSERT` media 列、量測完成後再補寫時長」（縮圖曾經被討論過的同一種形狀）→ 執行期吃 42501。

**建議**：§2 行 67 那格補上 `／duration_seconds（LS-134）`。（`docs/PLAN.md:78` 的欄位速覽 LS-128 也沒動，維持不動即可，不是 finding。）

---

### n1（nit）migration 檔頭行 28：`byte_size` 不是「正數」約束

「比照既有 width／height／byte_size 皆為正數的既有慣例」——`init_schema.sql:135` 是 `check (byte_size >= 0)`，允許 0。括號內只引 width/height（`CHECK > 0`）是對的，主句多帶了 `byte_size`。純註解，不影響行為。

### n2（nit）測試列 id 用了 `3b…` 前綴，但列是插進 A 家

`supabase/tests/99_media_duration.sql` 用 `3b000000-…-00d1`~`d5`，而 `00_fixtures.sql:83-91` 的既有分配是 `3a…`＝A 家 media、`3b…`＝B 家 media；這些列的 `family_id` 全是 `fa…`（A 家）。第 3 段斷言「B 家看不到 A 家…」講的卻是一個 `3b…` 的 id，讀起來反向。無 id 碰撞、無功能影響；LS-128 的 `98_media_thumbnails.sql` 用的是 `3a…f1`。

---

## 已查證通過（逐條）

**設計／scope**
- **CHECK 在 NULL 時回 true、既有列必然通過、不需 `NOT VALID`**：`check (duration_seconds is null or duration_seconds > 0)` 對 NULL 列第一個 disjunct 即為 true；`add column` 無 `DEFAULT`，套用當下**每一列**都是 NULL，`ADD CONSTRAINT` 的驗證掃描確定性通過。欄位在同一檔先於約束建立。✅
- **不加 `type='video'` 相依 CHECK 的理由成立**：「`type='video' → duration_seconds not null`」的 CHECK 會對既有列（全 NULL）驗證失敗，migration 直接 23514；`NOT VALID` 只是延後，且不改變「這是上傳端契約而非現在可機械驗證的不變量」的本質。測試第 2 段刻意把「video＋NULL 可插入」釘成正案例，防止日後被誤補，作法正確。✅
- **不加 UPDATE grant 與既有清單一致**：全 repo 對 `public.media` 的 UPDATE grant 只有 `init_schema.sql:364` 的 `(taken_at, deleted_at, width, height)`，無任何 migration 改動它；新欄不會被隱式授權（欄位級 grant 逐欄列舉）。測試第 4 段以 42501 釘住。✅
- **scope 乾淨**：3 檔全在票面範圍（migration／pgTAP／API.md §3§6），零 Swift、零順手重構、零無關格式調整。✅

**契約（另一半）**
- §6 徽章策略 vs LS-130 退回行為：`TimelineStore.loadVideoDuration`（`LittleSprout/Services/Timeline/TimelineStore.swift:169-176`）讀不到時靜默放棄 → `videoDurations[id] == nil` → `VideoDurationFormat.badgeText(duration: nil)` → 「影片」。與 §6「`NULL` 退回純文字「影片」」**一致，不矛盾**。✅
- §3「裁切後長度」與 LS-129 `VideoTrimmer`（60 秒上限）相容性：契約本身可實作（`VideoTrimmer.UploadSource` 目前只回 `pixelSize`，LS-135 需自行重量測輸出檔，屬 LS-135 範圍）。⚠️ 一個 PLAUSIBLE 的邊界：`AVAssetExportSession` 以 `CMTimeRange(duration: 60)` 匯出的實際 asset duration 可能略大於 60（如 60.02，取決於關鍵幀對齊），配上 M1 的向上取整會變成 `61` → 徽章 `1:01`，與 app 對外宣稱的 60 秒上限矛盾。未實跑匯出驗證，標 PLAUSIBLE；採 M1 修法 (a) 後此邊界自動消失（`floor` → 60 → `1:00`）。

**測試鑑別力（按構造核對，未重跑 mutation）**
- 負案例 A/B（0／−1）：其餘欄位皆合法（A 家 owner、前綴正確、width/height > 0、`uploaded_by` 為自己），唯一能擋下的就是 `media_duration_seconds_positive`；拿掉後 `INSERT` 成功 → 走到 `raise exception 'FAIL…'`（P0001，不被 `when check_violation` 接住）→ 向外傳播 → psql `ON_ERROR_STOP` 非 0。**具鑑別力**。✅
- 第 4 段：補開 `grant update (duration_seconds)` 後不再拋 42501 → 同樣落到 `raise exception 'FAIL…'`（即使 RLS 讓它影響 0 列也一樣紅）。**具鑑別力**。✅
- 第 3 段跨家庭 RLS **確實用另一家庭的 JWT**：`b0000000-…-0001`＝`00_fixtures.sql:64` 的 B 家 owner（真實存在的 fixture，不是空身分），且附正向對照（A 家查得到 1 列），對「policy 改成 `using(true)`」有鑑別力。✅
- CI db job 實跑證據：`99_media_duration.sql` 四段 NOTICE 全數輸出（欄位／0 與 −1 被 23514 擋下／隔離 0 列＋1 列／42501），`✓ 99_media_duration.sql`。✅

**機械 gate（本地在 worktree 重跑）**
- migration 時戳 `20260902195055` > development 最後一支 `20260902101842`；`migration-version-check.sh --target origin/development`：綠（本分支引入 1 檔／tree 18 檔）。✅
- `migration-breaking-check.sh --base origin/development`：**零命中**（exit 0、無輸出），D1-D4／B1-B6 皆不觸發，PR body 不需破壞性核可標記。✅
- `migration-immutable-check.sh`：無既有 migration 被改。✅ `api-contract-check.sh`：綠（RPC 24／表 21）。✅
- `origin/development` 仍在 `dc10347`（＝base），無漂移。

**handoff SHA 逐條查實**
- `eb380cc` → 僅 `supabase/migrations/20260902195055_media_duration_seconds.sql` ✅
- `694ed4c` → 僅 `supabase/tests/99_media_duration.sql` ✅
- `7fa495f` → 僅 `docs/API.md` ✅
- 三筆 commit 首行皆符 `<type>(<scope>): LS-134 …`。✅

**race condition／效能／平行**：本票不改任何 policy／函式／索引，RLS plan 不受影響（`50_rls_plan_no_percall_subquery.sql` CI 綠），無 N+1／OFFSET／主執行緒解碼面向可談——**這三個維度找不到問題**，不硬湊。（`ADD CONSTRAINT` 會做一次 ACCESS EXCLUSIVE 下的全表驗證掃描，正式站 `media` 目前資料量下為次秒級，不列為 finding。）

## 未做的事（明講）
- 未在本機重跑 `supabase db reset`／`run.sh`：以本 head 的 CI db job（2m56s，pass，含四段 NOTICE 全文）取代。
- 未重跑 mutation 兩輪（拿掉 CHECK／補 grant）：依派工指示按構造核對，結論如上。
- 未審 LS-135 的 iOS 實作（尚未存在）。

---

修 M1（並順手收 m1／m2）後重推，我對新 head 重審一輪即可；m2 之後的 n1／n2 屬 informational，處置請依 handoff 規約在 LS-135 或收尾時交代。

### 2026-09-02T20:19:42.990Z · Chih-Lin Yeh · `f38eda13`

## merge-review R1 delta — PR #233（`7fa495f..5727b65`）

只審 delta：`git diff 7fa495f..5727b65` ＝ 單一 commit `5727b65`（`docs(api): LS-134 R2 修正 duration_seconds 取整方向與不可變清單`），**唯一檔案 `docs/API.md`**（+16／−7，兩個 hunk：§2 行 67 那格、§3 `duration_seconds` 段），零 SQL／零 Swift。CI 四項對 `5727b65` 全綠（ci 6m59s／db 2m55s／rules 2m48s／lint 14s，run 33677788536）。

**Verdict：REQUEST_CHANGES**（1 major、1 minor）——M1／m1／m2 的**文字本身都改對了**，但改動只落在 `docs/API.md`；同一個 PR 裡的 migration 檔仍有**兩處**寫著「向上取整」，其中一處是會寫進資料庫的 `COMMENT ON COLUMN`。

---

### M2（major）取整方向只改了 API.md，migration 檔仍宣告「向上取整」——且其中一處是 DB 內的欄位註解，**merge 後就改不動了**

`supabase/migrations/20260902195055_media_duration_seconds.sql`（本 PR 新增、本輪未動）：

- **行 26**（檔頭）：`-- 20260822120000_init_schema.sql）。整數秒、裁切後長度向上取整——見 API.md §3`
- **行 46**（`comment on column`，會實際寫進 `pg_description`）：`'影片時長（整數秒，向上取整）。type = ''photo'' 時應為 NULL；…'`

**具體失敗情境**：LS-135 實作者（或 QA）照慣例先看定義這個欄位的 migration，或在 Supabase Studio／`\d+ public.media` 看欄位註解 → 讀到「整數秒，向上取整」→ 寫 `ceil(41.4)=42` → 編輯器 `影片 0:41`、時間軸 `影片 0:42`。這正是 R1 M1 要消除的缺陷，只是換了一份文件來散播；現在 repo 內兩份契約互相矛盾，而 migration 那份離實作者更近。

**為什麼是 major 而不是 nit（時效性）**：
1. `comment on column` 不是原始碼註解，是**資料庫物件的一部分**——merge＋部署後，正式站的 schema 註解會永久宣告與 API.md 相反的規則。
2. 本 PR merge 進 `development` 之後，這支 migration 就受 `scripts/gates/migration-immutable-check.sh` 保護（我在 R1 已實跑過這支 gate），**任何修改都會被擋**；要更正只能再開一支 migration 重下 `comment on column`。現在是一行編輯，merge 後是一張新票＋一支 migration。
3. 這也直接推翻 PR body 對 n1 的處置理由——「留待若日後這支 migration 因其他實質理由需要改動時一併順手修正」：migration 檔案在合併後**不會有「下次觸檔」**（immutable gate 擋著）。本輪就是這支檔案唯一一次可以順手改的機會。

**建議修法**（同一個 commit 三處一起收，成本與現在的 docs commit 相同）：
- 行 25-26 改為與 §3 同語：`整數秒、裁切後長度無條件捨去（下限 1 秒）——見 API.md §3`；順手把 n1 的 `byte_size` 從「皆為正數」那句拿掉（它是 `check (byte_size >= 0)`，允許 0）——**n1 從「下次觸檔順手」變成「就是這一次」**。
- 行 46 的 `comment on column` 首句改為 `'影片時長（整數秒，無條件捨去，下限 1 秒）。…'`。
- 另（nit，同一次編輯順手）：行 26 的交叉引用指向 API.md §3「**影片時長量測與寫入**」，但 API.md 全文沒有這個標題／字串（`git grep` 零命中，`5727b65`）；該段實際的標記是 §3 `media` 的「**影片時長（`duration_seconds`，LS-134）**」條目。這處 R1 我沒查到，是我漏的，一併致歉補上。

（n2 的處置我同意維持不修：測試檔沒有 immutable gate，日後真的可以順手改。）

---

### m3（minor）`docs/API.md:272-273` 的括號註解自我矛盾，容易被讀成 ceil

現行文字：`整數秒，**無條件捨去、下限 1 秒**（不足 1 秒的零頭捨去到 1，不進位、不寫 0）`

「不足 1 秒的零頭**捨去到 1**」字面可解成「把不足 1 秒的零頭進位成 1」——對 41.4 秒就是 42，正好是 R1 M1 判掉的那個行為。緊接的「不進位」救得回來，但這一整段存在的理由就是上一輪一句取整措辭造成的返工，不該再留一句需要靠上下文才能排除誤讀的話。

**建議**：括號改成「**`max(1, floor(d))`**：不足 1 秒的影片記為 1，不進位、不寫 0」。給出算式就沒有解讀空間，LS-135 直接照抄。

---

## 已查證通過（delta 範圍）

- **delta 只有 `docs/API.md`** ✅（`git diff --stat 7fa495f..5727b65`：1 file，16+/7−；未動 migration／測試檔，與 PR body R2 表「未動 migration／測試檔」自述一致）。
- **M1 措辭與裁決一致** ✅：採 R1 建議 (a)——「無條件捨去、下限 1 秒」，並明寫「與 `VideoDurationFormat`／`DiaryDurationFormat` **同源**」、引 R1 i2 原話與「同一支影片在編輯器與時間軸卡片才會顯示同一個數字」的理由，附兩支 Swift 檔路徑。措辭精確處：說的是「沿用同一個**取整方向**」而非「同一個函式」——兩支 formatter 的下限是 0（`max(0, Int(d))`）、DB 契約下限是 1，用「方向」措辭是對的，沒有過度宣稱。✅
- **「下限 1 秒」不與 `CHECK > 0` 矛盾** ✅：`max(1, floor(d)) ≥ 1 > 0`，恆滿足 `media_duration_seconds_positive`。
- **與 LS-135 上傳端契約完備、無空隙** ✅：三條路徑窮盡且互斥——① 量測失敗 → `NULL`；② 量到 `≤ 0` → `NULL`（明文「不得寫 `0`」）；③ 量到 `> 0` → `max(1, floor)` ≥ 1。**沒有任何路徑會產生 `0`**，也就沒有「兩個 Storage 物件已 PUT、`INSERT` 才撞 23514」的孤兒情境（m1 的修正目的達成，且理由與 §3「上傳流程順序」行 256-268、§6「`NULL` 退回純文字『影片』」互相接得上）。
- **m2 已落地** ✅：§2 行 67 `media` 那格不可變清單現為 `…／thumb_path／thumb_width／thumb_height（LS-128）／duration_seconds（LS-134）`，格式與 LS-128 先例（`df283e9`）一致；該 hunk 其餘列皆為 context，未被誤動。
- **§6 徽章策略段落未被本輪改動** ✅（diff 只有兩個 hunk），與 LS-130 `loadVideoDuration` 失敗 → `nil` → 「影片」的退回行為仍然一致。
- **PR body R2 表與 diff 一致** ✅：M1／m1／m2 三列所述改動逐條對得上 `5727b65` 的實際 hunk；n1／n2 標「不修＋理由」且 diff 確實未觸碰 migration／測試檔；分支 4 commits 與 push gate 自述相符。⚠️ 唯一對不上的是 n1 那格的**理由**本身（見 M2 第 3 點：migration 合併後不存在「下次觸檔」）。
- **CI 對 `5727b65` 四項全綠** ✅（docs-only 變更未動 SQL，db job 仍完整跑過 18 支 migration 與 `99_media_duration.sql`）。

### i1（informational，不需改動）
「下限 1 秒」帶來一個有界的殘留差異，我在 R1 建議 (a) 時就內含了它，說清楚以免日後被當成新缺陷：**長度 < 1 秒的影片**在日記編輯器顯示 `影片 0:00`（`DiaryDurationFormat` 下限是 0），時間軸卡片會顯示 `影片 0:01`。這是「不顯示 `0:00`」與「兩處數字全等」二選一的必然取捨，現行選擇（記 1）是兩害相權；若日後真的在意，可改成「< 1 秒 → 留 `NULL`」（徽章退回「影片」）。本輪**無需改動**。

## 未做的事（明講）
- 未重跑 DB 測試／mutation：delta 零 SQL 變更，且 CI db job 對 `5727b65` pass。
- 未重審 R1 已查證通過的 migration／測試內容（CHECK 語意、grant 清單、鑑別力、gate、SHA），僅就 delta 與**跨檔一致性**復查——M2 正是這次跨檔一致性查出來的。

---

修 M2（三處，含順手收 n1 與 §3 交叉引用名稱）＋ m3 後重推，我對新 head 再走一次 delta 復審。commit status `merge-review=failure` 已貼 `5727b65`。

### 2026-09-02T20:31:24.019Z · Chih-Lin Yeh · `e2d97d94`

## merge-review R1 delta 2 — PR #233（`5727b65..11daa5b`）

只審 delta：`git diff 5727b65..11daa5b` ＝ 2 commits（`4be1b43` migration 註解／`11daa5b` API.md 算式化），2 檔：`supabase/migrations/20260902195055_media_duration_seconds.sql`（+11／−6，**只有註解**）與 `docs/API.md`（+2／−2，§3 一句）。CI 四項對 `11daa5b` 全綠（ci 6m0s／db 3m3s／rules 2m49s／lint 22s，run 33679052758）。

**Verdict：APPROVE** — M2／m3／n1 全部收乾淨，無新增問題，無 blocker／major／minor 遺留。

---

## 逐項查證

**① migration 三處文字與 API.md 一致** ✅
三個規範點（算式／NULL 規則／同源依據）三處完全一致，無矛盾；措辭依載體繁簡不同，不是逐字複製（也不需要）：

| 位置 | 算式 | 量測失敗／≤0 | 同源 |
|---|---|---|---|
| `docs/API.md:272-273` | `max(1, floor(d))`（`d` 為量測到的秒數） | 留 `NULL`、不得寫 `0` | `VideoDurationFormat`／`DiaryDurationFormat` |
| migration 行 27-31（檔頭） | `max(1, floor(d))` | 留 NULL、不得寫 0 | 兩支 Swift 檔**完整路徑** |
| migration 行 52-53（`comment on column`） | `max(1, floor(d))` | 留 NULL、不得寫 0 | `VideoDurationFormat`（省略 Diary 版，載體長度考量） |

全樹「向上取整」殘留掃描：`git grep 向上取整 -- docs/ supabase/ LittleSprout/` 於 `11daa5b` **零命中** ✅（R1 delta M2 的兩處已消滅，且沒有第三處）。

**② 交叉引用命中** ✅
migration 檔頭現指向「API.md §3 `media` 表『影片時長（`duration_seconds`，LS-134）』條目」——該字串（含反引號）在 `docs/API.md` 命中 1 次（`git grep -c`，`11daa5b`），即該段落的實際標記。上一輪那個零命中的「影片時長量測與寫入」已不存在。

**③ n1 收乾淨且改對** ✅
檔頭現為「比照既有 width／height 皆為正數的既有慣例（CHECK > 0，20260822120000_init_schema.sql；byte_size 是 CHECK >= 0、允許 0，不適用此例）」——與 `init_schema.sql` 實況一致（`width`／`height` 為 `check (… > 0)`，`byte_size` 為 `check (byte_size >= 0)`）。不只是刪掉錯字，還把「為什麼 byte_size 不適用」記下來，避免日後有人再把三欄混為一談。

**④ 無其他 migration 內容變動（diff 只有註解）** ✅ — 機械核對，不是目視：
把兩版檔案以 `sed 's/--.*//'` 去掉行註解後 diff，**唯一差異是 `comment on column` 的字串字面**（那本來就是本輪要改的目標），兩句 DDL 逐字節相同：
```
alter table public.media add column duration_seconds integer;
alter table public.media add constraint media_duration_seconds_positive
  check (duration_seconds is null or duration_seconds > 0);
```
schema 形狀、約束、grant 一律未動。

**⑤ designer 自陳的 `col_description()` 已獨立複驗** ✅（不只是採信）
對本機共用容器直接查（唯讀，未跑 reset、未經 lock 因為沒有寫入）：
```
docker exec supabase_db_little-sprout psql -At -c
  "select col_description('public.media'::regclass, attnum) …"
→ 影片時長（整數秒，max(1, floor(d))，與 VideoDurationFormat 同源；量測失敗或
  duration <= 0 秒時留 NULL，不得寫 0）。type = 'photo' 時應為 NULL；…
```
兩件事同時被證實：(a) `pg_description` 內確實是新文字，不是只有檔案改了；(b) 字串字面的單引號逃逸正確——`''photo''`／`''video''` 在 DB 內還原成 `'photo'`／`'video'`，`max(1, floor(d))` 的括號逗號未破壞字面。

**⑥ gate 在本 head 重跑** ✅ — immutable 綠（本支 migration 在 `origin/development` 上尚不存在，本輪修改**不觸發** immutable gate；這也正是 R1 delta M2 說的「最後機會」窗口仍開著）、breaking 零命中、version 無撞號（1 檔／tree 18）、api-contract 綠。CI `rules`／`db` job 對同一 head 亦全綠（db 完整跑過 18 支 migration＋`99_media_duration.sql`）。

**⑦ PR body R3 段與 diff 一致** ✅ — M2／n1 掛 `4be1b43`、m3 掛 `11daa5b`，與實際 commit 歸屬相符；n1 那格明確改寫了 R2 的錯誤理由（承認 immutable gate 讓「下次觸檔」不存在），紀錄誠實。

## 未做的事（明講）
- 未重跑 `supabase db reset`／`run.sh`：本輪 SQL 語句零變更（④ 已機械證明），CI db job 對 `11daa5b` pass，且 ⑤ 直接查了 DB 內的實際註解。
- 未重審 R1／R1 delta 已查證通過的內容（CHECK 語意、grant 清單、測試鑑別力、SHA 對應），本輪只審 delta 與跨檔一致性。
- 程序透明：我在 worktree 內短暫 `git checkout 11daa5b` 跑 gate，已 `checkout feature/LS-134-media-duration` 復原，`git status` 乾淨、無任何檔案變更。

## 遺留（不擋合併）
- **n2**（`99_media_duration.sql` 用 `3b…` 前綴插入 A 家的列）維持「不修＋下次觸檔順手」——同意：測試檔不受 immutable gate 約束，「下次」真的存在，與 n1 的情況不同。
- **i1**（<1 秒影片：編輯器 `0:00` vs 卡片 `0:01`）為 `max(1, floor(d))` 的已知有界取捨，已記錄在 R1 delta comment，無需改動。

---

commit status `merge-review=success` 已貼 `11daa5b`。可併。

### 2026-09-02T20:32:19.319Z · Chih-Lin Yeh · `ed53f91b`

orchestrator 2026-09-03 08:30：merge-review R1（`345014b1`）→ delta（`f38eda13`）→ delta 2 **APPROVE**（`e2d97d94`，status success 於 `11daa5b`）→ PR #233 併入 development（merge commit 貼 promote: no content diff）。等 LS-130 併入後 promote → test → QA（SQL 寫入含 `duration_seconds` 的列讀回、CHECK 擋 0／負、UPDATE 42501）→ 正式站 db push（非破壞性）→ LS-135 解鎖。

### 2026-09-02T21:20:10.611Z · Chih-Lin Yeh · `0cf858af`

## dead-code-sweeper（LS-129＋130＋134 同批；orchestrator 代貼，2026-09-03 09:50）
完整報告見 LS-129 同標題 comment。本票：migration／測試無殘留函式／policy／索引；n2 測試 id 前綴（`3b…` 插 A 家）維持「下次觸檔」裁定。→ 巡檢通過。

### 2026-09-02T21:48:07.791Z · Chih-Lin Yeh · `a78536d2`

## QA

裁決：**PASS**

驗收基準：`test` branch，QA worktree `.claude/worktrees/qa-test` detach 到 `origin/test`，HEAD = `b2d4612`（＝指定 SHA）。本機 Supabase `bash scripts/ops/supabase-lock.sh -- supabase db reset`（18 支 migration，含 `20260902195055_media_duration_seconds.sql`；票文寫「19 支」，實際 repo 內只有 18 支——與 merge-review R1 comment `345014b1` 所述「tree 18」一致，非驗收落差，僅票文筆誤）。

### 逐條驗收

1. **`supabase/tests/run.sh` 全綠；`99_media_duration.sql` 四段**：✓ PASS。整套 `run.sh` 通過（含併發／RLS plan 等其餘測試），`99_media_duration.sql` 對應四段（欄位存在＋nullable／CHECK 正負案例／跨家庭 RLS 隔離／UPDATE 42501）NOTICE 全數輸出：
   ```
   ok：duration_seconds 欄位存在、nullable、型別 integer
   ok：影片列帶正整數 duration_seconds 插入成功
   ok：照片列 duration_seconds 留空插入成功
   ok：影片列 duration_seconds 留空（量測失敗的過渡列）插入成功——DB 不強制 video 必填
   ok：duration_seconds = 0 被 CHECK 擋下 (23514)
   ok：duration_seconds < 0 被 CHECK 擋下 (23514)
   ok 隔離：B 家看不到 A 家帶時長的 media 列（0 列），A 家 owner 查得到自己的（1 列）
   ok：duration_seconds 無 UPDATE 權限 (42501)
   ```
   證據：`/private/tmp/claude-501/-Users-clyeh-little-sprout/954fc361-48a1-4e75-b3ca-02a3ce7da71b/scratchpad/LS-129-130-134-qa/run-log.txt`（行 734-748）。

2. **SQL 獨立驗證（不倚賴既有 pgTAP 檔，自己另開一段 rollback 交易驗）**：✓ PASS，四項全部獨立覆核：
   - 寫入 `duration_seconds=41` 的 video 列，讀回 `duration_seconds=41`。
   - `duration_seconds=0`：插入被擋，`SQLSTATE 23514`。
   - `duration_seconds=-1`：插入被擋，`SQLSTATE 23514`。
   - `authenticated` 角色對已存在列 `UPDATE duration_seconds`：被擋，`SQLSTATE 42501`。
   - `col_description('public.media'::regclass, ...)` 直接查詢欄位註解，內容含「`max(1, floor(d))`」，與 migration 檔內 `comment on column` 逐字一致。

3. **`migration-breaking-check.sh --base origin/main`**：✓ PASS，`exit=0`，零命中。

### 補充查證（非票文要求，獨立覆核）

merge-review 過程中反覆修正的取整方向爭議（「向上取整」vs「`max(1, floor(d))`」），我直接讀了併入 `test` 的 migration 檔與 DB 內實際欄位註解，確認最終落地版本一致採用「無條件捨去、下限 1 秒」（`max(1, floor(d))`），與 `VideoDurationFormat`／`DiaryDurationFormat` 既有慣例同源，未見殘留「向上取整」字樣。

### 未驗證範圍

- iOS 端讀寫（上傳寫入、徽章讀取）不在本票範圍（LS-129／LS-135），本票僅後端欄位契約。
- 未跑 `api-contract-check.sh`（票文驗收未列此項；merge-review R1 已跑過並綠，本輪未重跑）。

證據目錄：`/private/tmp/claude-501/-Users-clyeh-little-sprout/954fc361-48a1-4e75-b3ca-02a3ce7da71b/scratchpad/LS-129-130-134-qa/`

### 2026-09-02T21:51:30.279Z · Chih-Lin Yeh · `537f6d3d`

orchestrator 2026-09-03 10:30：QA **PASS**（`a78536d2`：18 migrations、run.sh 全綠、`duration_seconds=41` 讀回、0／−1 → 23514、UPDATE → 42501、`col_description` 顯示 `max(1, floor(d))`、breaking-check 零命中）。但同批 LS-129／130 QA 因 mobile-mcp 工具問題 BLOCKED（qa status failure 於 test `b2d4612`），已派單機重驗；三票同批 promote，本票 Done 與正式站 db push 等重驗結果一起走。

### 2026-09-03T05:15:18.686Z · Chih-Lin Yeh · `d846e46a`

orchestrator 2026-09-03 13:15：qa success 貼 `c5f8ed3`（LS-129／130／134／136 同頭；本票 R2 PASS `a78536d2` 沿用、smoke 帶過）→ promote test→main → **v0.8.1** → 正式站 `supabase db push` 套用 `20260902195055_media_duration_seconds.sql`（Finished；待 `list_migrations` 核對）。收尾：sweeper `b6116ee7`（第一輪）→ retro → cleanup → Done → 解除 LS-135 blockedBy。

### 2026-09-03T05:17:09.120Z · Chih-Lin Yeh · `2b30fad6`

## 收尾（orchestrator 2026-09-03 13:17）

- **sweeper**：`b6116ee7`（第一輪，無阻塞項）。
- **Retro**：(1) 純後端契約票（`media.duration_seconds`，`max(1, floor(d))`、NULL 容錯）驗收只寫後端可自證項，一輪 QA PASS（`a78536d2`）——這個切法沿用。(2) 正式站遷移 `20260902195055_media_duration_seconds` 於 v0.8.1 後 `db push`，`list_migrations` 核對已在（18 支）。(3) 上傳端寫入與列表徽章改讀由 LS-135 接手（blockedBy 本票，Done 後解除）。(4) `cleanup-merged.sh` 因 `supabase/.temp/`＋`supabase/tests/evidence/` ignored 殘留略過（第 4 次，LS-96 `25a4ff4d` P1）。
- **清理**：worktree `.claude/worktrees/LS-134` 手動 `git worktree remove --force`、本機分支 `feature/LS-134-media-duration` 已刪（遠端無）。
- 狀態 → **Done**（隨 v0.8.1 出貨，main `c5f8ed3`）。

