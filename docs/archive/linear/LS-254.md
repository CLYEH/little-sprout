# LS-254 Harness：worker agent 的研究 fork 越權執行——`subagent_type: fork` 繼承父 agent 全文脈絡（含派工單）後平行執行整項任務（LS-234 R7 同一 .pen／branch 雙寫），本 session 第 3 次；PreToolUse hook 對 subagent 內的 `Agent` 呼叫限制 fork／規定研究用 Explore＋agent 定義補規則

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:S, lane:harness |
| Cycle | 2 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-13T06:22:20.721Z |
| 開始 | 2026-09-13T06:23:26.119Z |
| 完成 | 2026-09-13T08:20:12.738Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-254/harnessworker-agent-的研究-fork-越權執行-subagent-type-fork-繼承父-agent |
| 關係 | related→LS-253, related→LS-239, related→LS-209, related→LS-215, related→LS-234, related→LS-238, related→LS-96, related←LS-246, related←LS-256 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 池項（fork 越權，09-13 已記兩次、權重 High）＋第 3 次事故：[LS-234](https://linear.app/little-sprout-app/issue/LS-234/設計app-store-截圖版型-iphone-6913202868與-ipad-1320642752各-5) designer R7（comment `5848df73`）——designer 派 `fork` 做「讀票文與關鍵 comment」的窄研究，fork 繼承整份對話（含 orchestrator 派工單）後把整輪 R7 當成自己的任務平行執行：對同一份 live Pencil 文件寫入、在同一 worktree／branch 提交（`a17ec19`）、寫收據、`gh pr edit` PR body、貼 Linear handoff（`d59e1dc0`）。主 agent 兩次 land 因 autosave 競態失敗後才發現；最終靠主 agent 放棄自己的修正、還原畫布到已提交 head 收斂。早前兩次（見池項）為 read-only 研究 fork 越權改檔。§5-b：同類事故 ≥2 次 → High。

## 範圍

1. **機械 gate**（前饋必有反饋）：PreToolUse hook（`scripts/gates/pretool_engine.py` 既有框架）對**非主 session**（`agent_id` 存在）發出的 `Agent` 呼叫：`subagent_type: fork` 一律擋，訊息指向改用 `Explore`／`general-purpose` 並在 prompt 明寫唯讀；主 session 不受影響（orchestrator 自己的 fork 仍可）。自測：合成 hook 輸入（主 session／subagent × fork／Explore）四格，掛 CI `rules`。
2. **agent 定義**：`ui-designer.md`／`ios-dev.md`／`merge-reviewer.md`／`visual-reviewer.md`／`qa.md` 各加一條「研究用 Explore（唯讀）；禁 fork；任何子 agent 不得寫檔／commit／改 PR／貼 Linear」——沿 [LS-238](https://linear.app/little-sprout-app/issue/LS-238/harness規約文件去敘事化-docscollaborationmd172-處事故教訓rn-fn80-處不再改為已廢止與) 去敘事化格式（規則＋一行理由＋LS 指標）；`agent-tools-check.sh` 釘住字句。
3. **COLLABORATION §1／§7**：對照表補一列（規則／gate／自測）；`dispatch-prompt-rules` 記憶同步由 orchestrator 做。
4. 事故的另一面：Pencil 單一 live 文件被兩個 agent 寫入時無鎖——列為 informational 記池（是否需要 `pen-open.sh` 層的寫入鎖另議，本票不做）。

## 不做

改 Claude Code 的 fork 語意；Pencil 寫入鎖；追溯修正三次事故的產出（皆已收斂）。

## 驗收

- [ ] hook 對 subagent 的 fork 呼叫 exit 2 並印替代方案；主 session fork 不擋；自測四格綠、掛 CI
- [ ] 五份 agent 定義各有該規則且 `agent-tools-check.sh` 驗字樣；§7 對照表有列；merge-reviewer 通過（hotfix 從 main 切、PR 回 main、back-merge）

## 留言（10）

### 2026-09-13T06:24:46.700Z · Chih-Lin Yeh · `47e6e536`

**orchestrator 09-13 14:24**：harness lane 因 LS-238 Done 而空 → 本票提前入 cycle 2、In Progress；worktree `.claude/worktrees/LS-254`（`hotfix/LS-254-fork-guard` 自 main `7ea54c9`）；派 harness-dev：`scripts/hooks/fork-guard.sh`（PreToolUse matcher `Agent`；有 `agent_id` 且 `subagent_type=fork` → exit 2，其餘放行、壞輸入 fail-open）＋五格自測掛 CI `rules`＋四份 agent 定義補「研究用 Explore、禁派 fork」（ios-dev.md 第 14 行已有）＋§7 對照表一列 → PR 回 main。

### 2026-09-13T06:44:10.240Z · Chih-Lin Yeh · `f39b7aad`

**harness-dev 09-13 14:43 R1**

**Ticket**：LS-254（High，size:S，lane:harness）——hotfix 從 `main` `7ea54c9` 切、PR 回 `main`。

**已完成**
1. 範圍 1 機械 gate：`scripts/hooks/fork-guard.sh`＋`fork_guard.py`（PreToolUse matcher `Agent`，`.claude/settings.json` 獨立一條、只加不刪）——hook JSON 頂層 `agent_id` 存在且為非空字串（subagent；身分判準照抄 `large_file_read_guard.py`，`agent_id` 不是 `agent_type`）且 `tool_input.subagent_type == "fork"` → exit 2，stdout deny JSON＋stderr 一行「LS-254：worker agent 禁派 fork（會繼承整份派工單並平行執行）；研究改派 `Explore`（唯讀）或 `general-purpose` 並在 prompt 明寫唯讀」；主 session（`agent_id` 缺席）與非 fork 類型 exit 0；stdin 空／JSON 壞／python3 缺席／引擎異常／`agent_id` 無法採信 → **fail-open** exit 0 並 stderr 註明（settings.json 該條刻意不接 `|| exit 2`）。commit `4d135ee`。
2. 自測 `scripts/hooks/fork-guard.test.sh` 26 組（票文四格＋fail-open 五種＋身分判準補充九組＋mutation 兩組＋settings.json 接線六項）掛 CI `rules` job「Gate 腳本自測」step。同 commit。
3. 範圍 2 agent 定義：`ui-designer.md`／`visual-reviewer.md`／`merge-reviewer.md`／`qa.md` 各加一條「研究用 `Explore`（唯讀）；禁派 fork（LS-254）」（LS-238 格式）；`ios-dev.md` 第 14 行只補 LS-254 指標句。merge-reviewer／qa 的 `tools:` 本無 `Agent`，句子寫成「需要研究／並行一律回報 orchestrator 拆派」（沿 LS-215 對這兩份的既有處理）。commit `3e53c06`。`agent-tools-check.sh` `BODY_RULES` 五條釘「禁派 fork」；自測 ㉒ 十一組、⑤ 條數 42→47。commit `0dda4f6`。
4. 範圍 3 COLLABORATION：§1 加一條 bullet、§7 末尾加一列（規則／gate／自測／已知盲區），其他行不動。commit `bfdc5f7`。
5. PR **#389**（base `main`，head `bfdc5f7`）；body 先過 `pr-body-check.sh LS-254-pr-body.md --branch hotfix/LS-254-fork-guard --verify` exit 0。

**已驗證（怎麼驗）**
- 驗收 1「hook 對 subagent 的 fork 呼叫 exit 2 並印替代方案；主 session fork 不擋；自測四格綠、掛 CI」：`bash scripts/hooks/fork-guard.test.sh` → `✓ fork-guard.sh 自測通過（26 組）`，exit 0；四格＝①a–①d（主×fork 0／主×Explore 0／sub×fork 2／sub×Explore 0），壞 JSON＝②a exit 0＋stderr「fail-open」；CI 接線＝`.github/workflows/ci.yml` rules job 新增 `bash scripts/hooks/fork-guard.test.sh || rc=1`。
- 驗收 2「五份 agent 定義各有該規則且 `agent-tools-check.sh` 驗字樣」：`bash scripts/gates/agent-tools-check.sh` → 五行 `<agent>.md：正文含「禁派 fork」`＋`✓ agent-tools gate 通過（6 份 agent 定義；正文必含字樣 47 條）`，exit 0；`bash scripts/gates/agent-tools-check.test.sh` → `✓ agent-tools-check 自測通過（135 組樣本）`，exit 0（㉒ 含 mutant：拿掉 BODY_RULES 區塊後負樣本變綠）。
- 驗收 2「§7 對照表有列」：`docs/COLLABORATION.md` 第 350 行（新增一列）、§1 第 19 行 bullet；`grep -c LS-254` = 2。
- mutation（fork-guard.test.sh ④）：④a `if identity != "subagent":`→`if False:` → ①a 主 session×fork 翻成 deny，斷言「④a mutant：拿掉身分判斷後，①a（主 session × fork）變成 deny」綠；④b `if ti.get("subagent_type") == "fork":`→`if True:` → ①d sub×Explore 翻成 deny，同型斷言綠。
- 回歸：`large-file-read-guard.test.sh` 64 組／`background-bash-guard.test.sh` 57 組／`pretool.test.sh`／`main-checkout-guard.test.sh`／`brand-skill-check.sh` 全綠；push gate 三次通過（無 Swift 變更，跳過 unit tests）。
- 未驗：CI rules job 實跑（PR 剛開，結果待 checks）；hook 在真 session 的生效需 `/hooks` 重載或重啟（pretool.sh 檔頭既有盲區）——併入 main 後請 orchestrator 重載並跑一次 pipe-test（PR body 驗證段有命令）。

**未完成（informational 處置）**
- 尚無 merge-review 輪次；reviewer 的 informational 出來後逐條處置。
- 票文範圍 4「Pencil 單一 live 文件被兩個 agent 寫入時無寫入鎖」：informational、本票不做——**請 orchestrator 記入常駐追蹤票**（本 handoff 不申報 comment id；是否需要 `pen-open.sh` 層寫入鎖另議）。
- 票文範圍 3「`dispatch-prompt-rules` 記憶同步」：票文明定由 orchestrator 做，本票未動記憶檔。
- 派工單指定 commit 首行 `harness(hooks)`，但 `commit-msg-gate.sh` 只允許 `feat|fix|chore|docs|refactor|test|perf|design`——改用 `chore(hooks)`／`chore(gates)`／`docs(agents)`／`docs(harness)`，不修 gate、不另票（若要開放 `harness` type 屬 harness 待辦，非 High）。
- 派工單「§1 model 政策段若列 agent 可派子 agent 的規則就補一句」：model 政策段沒有這類規則，改在 §1 角色列表末尾加一條 bullet（只加不改）。

**風險**
- **hook fail-open 方向**（刻意、票文明示）：漏放最壞是一次 fork 越權（agent 定義規約＋merge-reviewer scope 兜底）；fail-closed 最壞是任一次解析失敗讓整條產線（含 orchestrator）派不出任何 agent。`agent_id` 欄位若被 CLI 改名 → 全部判成主 session → 整支不擋、不誤擋（與 `large_file_read_guard.py` 極性相反，已寫進檔頭與 §7）。
- 只認 `subagent_type` 字面 `fork`：worker 派 `general-purpose`／具名 agent 並要它寫檔不擋——規約層兜底。
- 無 race／效能／migration／Swift 變更。

**產出位置**
- worktree `.claude/worktrees/LS-254`、分支 `hotfix/LS-254-fork-guard`，commits `4d135ee`→`3e53c06`→`0dda4f6`→`bfdc5f7`（head）。
- PR #389 https://github.com/CLYEH/little-sprout/pull/389（base `main`）。
- 新檔：`scripts/hooks/fork-guard.sh`、`scripts/hooks/fork_guard.py`、`scripts/hooks/fork-guard.test.sh`；改檔：`.claude/settings.json`、`.github/workflows/ci.yml`、`.claude/agents/{ui-designer,visual-reviewer,merge-reviewer,qa,ios-dev}.md`、`scripts/gates/agent-tools-check.sh`、`scripts/gates/agent-tools-check.test.sh`、`docs/COLLABORATION.md`。
- 自測原文：scratchpad `LS-254-fork-guard-test.log`／`LS-254-agent-tools-check.log`／`LS-254-agent-tools-check-test.log`；PR body `LS-254-pr-body.md`。
- 下一步：merge-reviewer 審 #389 → 併 `main` → back-merge `main`→`development` → orchestrator `/hooks` 重載＋pipe-test。

### 2026-09-13T06:45:30.530Z · Chih-Lin Yeh · `6940e4bf`

**orchestrator 09-13 14:44**：harness-dev R1 handoff 收到（`f39b7aad`；PR #389 → main，head `bfdc5f7`；fork-guard 六格自測 26 組綠、agent-tools-check 47 條綠、五份定義釘「禁派 fork」、§1 bullet＋§7 列；偏差：commit 型別改 `chore`／`docs`，settings.json 不接 `|| exit 2` 維持 fail-open）→ In Review、派 merge-review R1（重放六格＋mutation、scope、back-merge 可併性）。併 main 後 orchestrator 需 `/hooks` 重載並跑 pipe-test；範圍 4（Pencil 寫入鎖）記 LS-96。

### 2026-09-13T06:58:34.291Z · Chih-Lin Yeh · `6714b95f`

**merge-reviewer 09-13 14:54 R1 APPROVE**

審的 head：`bfdc5f7be4099de045bea0c98c724fccb40b6acb`（開審 `gh pr view 389 --json headRefOid` ＝同一 SHA）。範圍 `git diff origin/main...bfdc5f7`：13 檔 +417/-10。未持有 supabase lock（本票無 DB／Swift 變更，未跑 DB 測試、未開模擬器）。

**已驗證（怎麼驗）**

1. **驗收 1「subagent 的 fork → exit 2 並印替代方案；主 session 不擋」**——不採信自測，自建合成 stdin JSON（scratchpad `LS-254-c1-main-fork.json` … `LS-254-c6-badjson.json`）直打 `bash scripts/hooks/fork-guard.sh`，六格逐一相符：主×fork `exit 0`／主×Explore `exit 0`／**sub×fork `exit 2`**／sub×Explore `exit 0`／空 stdin `exit 0`／壞 JSON `exit 0`。sub×fork 的 stdout 經 `python3 -c json.load` 確認是合法 JSON（`hookEventName=PreToolUse`、`permissionDecision=deny`），stderr 恰一行且含 `Explore` 替代方案；兩個 fail-open 格 stderr 各一行註明原因（`stdin 是空的`／`hook JSON 無法解析`），allow 格皆無 stdout。
2. **身分判定與 `large_file_read_guard.py` 一致**——`grep -n "agent_id" -A4 scripts/hooks/large_file_read_guard.py` 對照 `scripts/hooks/fork_guard.py` 的 `_identity()`：同樣 `d.get("agent_id", _MISSING)`，欄位缺席＝主 session（不看 `agent_type`）、非空字串＝subagent、型別不對／空字串＝身分不明。第三分支在兩支都落到「放行」，但那是各自 fail-open 方向的結果（LF 擋主 session、本支擋 subagent），不是抄錯。我另跑四組邊角：`agent_id:null`／`"   "`／`tool_input` 非 dict／缺 `tool_name` 皆 `exit 0`，身分不明兩格 stderr 有「無法採信」。
3. **fail-open 極性與 settings.json 接線**——以 `python3` 列出 `.claude/settings.json` 六條 PreToolUse：新條 matcher `Agent`、command `bash "$CLAUDE_PROJECT_DIR/scripts/hooks/fork-guard.sh"`（timeout 10）**不接 `|| exit 2`**，既有五條原樣。此處與其他 hook 不一致是刻意且正確：deny 由腳本自己 `exit 2` 送出（第 1 點實測 exit 2 確實從 wrapper 傳出），`|| exit 2` 只會把「腳本本身跑不起來」轉成 deny，正是 fail-open 要避免的；**不會因此在應擋時放行**。
4. **自測與回歸實跑**：`bash scripts/hooks/fork-guard.test.sh` → `✓ fork-guard.sh 自測通過（26 組）` exit 0；`bash scripts/gates/agent-tools-check.sh` → `✓ agent-tools gate 通過（6 份 agent 定義；正文必含字樣 47 條）` exit 0；`bash scripts/gates/agent-tools-check.test.sh` → `✓ 自測通過（135 組樣本）` exit 0；`bash scripts/hooks/pretool.test.sh`／`large-file-read-guard.test.sh`（64 組）／`background-bash-guard.test.sh`（57 組）／`main-checkout-guard.test.sh` 全 exit 0，未受本 PR 影響。
5. **mutation 重放（不採信申報）**：把 `scripts/hooks/` 三檔＋`.claude/settings.json` 複製成 scratchpad 假 root（`LS-254-mut-root/`，不動受審 worktree），未變異複本先跑綠（26 組）；再把 `scripts/hooks/fork_guard.py` 的 `if ti.get("subagent_type") == "fork":` 改成 `if False:`（永遠放行）→ 自測 **rc=1**、無「自測通過」行，紅的四條原文：`✗ ①c subagent × fork（deny，stdout deny JSON＋stderr 一行替代方案）（期望 exit 2，實得 0…）`、`✗ ③b agent_id＋agent_type 皆有 × fork（deny——正常 subagent 呼叫）（期望 exit 2，實得 0…）`、`✗ ④a mutant：拿掉身分判斷後，①a（主 session × fork）變成 deny…（期望 exit 2，實得 0…）`、`✗ ④b mutation 錨點（subagent_type 比對）不在 fork_guard.py`。紅的是預期的斷言失敗（不是 crash／執行錯誤），與 handoff 申報方向一致。受審 worktree 全程未改（`git status --porcelain` 空、head 仍 `bfdc5f7`）。
6. **CI 接線**：`.github/workflows/ci.yml` 第 226 行 `bash scripts/hooks/fork-guard.test.sh || rc=1`，緊接第 223 行的 `large-file-read-guard.test.sh`，同一個收 `rc` 的 step 內（step 尾 `exit "$rc"`），與既有慣例一致；yml 以 `python3 -c yaml.safe_load` 解析成功（rules job 21 steps）。
7. **Scope**：`git diff origin/main...bfdc5f7 --name-status` 僅 13 檔＝hook 三檔（新增）＋`.claude/settings.json`＋五份 `.claude/agents/*.md`＋`scripts/gates/agent-tools-check.sh` 與其 `.test.sh`＋`ci.yml`＋`docs/COLLABORATION.md`，與票文範圍 1–3 逐項對應，無夾帶重構或順手改動。`docs/COLLABORATION.md` 是 **+2/-0**（§1 一 bullet、§7 一列），其他行零改動；`.claude/agents/ios-dev.md` 是既有 LS-209 那行的**尾端附加**，舊文字完整保留。
8. **規則句不誤導且真的被釘住**：merge-reviewer／qa 的句子寫「本定義 tools 白名單無 `Agent`，需要研究／並行一律回報 orchestrator 拆派」——`grep '^tools:' .claude/agents/merge-reviewer.md` 與 `qa.md` 確實都無 `Agent`，語意與實況相符、不會誤導；`scripts/gates/agent-tools-check.sh` `BODY_RULES` 新增五條釘「禁派 fork」，自測 ㉒ 含「`禁派fork`（無空白）不算 → exit 1」與 mutant「拿掉規則後負樣本變綠」，`grep -c '禁派 fork'` 五份各 1。
9. **Commit 規約**：四支首行逐一餵 `bash scripts/gates/commit-msg-gate.sh <msg 檔>` 全 rc=0（`chore(hooks)`／`docs(agents)`／`chore(gates)`／`docs(harness)`）——handoff 申報的「派工單指定的 `harness(hooks)` 不在白名單、改用 `chore`」屬實且處理正確。
10. **Back-merge 可行**：`git merge-tree --write-tree origin/development bfdc5f7` rc=0、輸出單一 tree `fa51689`，無衝突。附帶事實：`git diff --stat origin/main origin/development -- docs/COLLABORATION.md` 空——development 的 `docs/COLLABORATION.md` 與 main 相同（LS-238 改寫已在 main），§7 表列位置不會撞。
11. **PR body**：`bash scripts/gates/pr-body-check.sh <body> --branch hotfix/LS-254-fork-guard --verify` rc=0（`✓ PR body 檔頭段含本票票號 LS-254`）；「未完成（informational 處置）」欄在（第 68–71 行），列了「尚無 merge-review 輪次」與票文範圍 4 的處置。
12. **四維度**：race condition——`scripts/hooks/fork_guard.py` 無狀態、不寫暫存檔、不碰共享資源，每次呼叫獨立 python3 子行程，無競態面；運算效能——每次 Agent 呼叫多一個 python3 行程（毫秒級，hook timeout 10s）；平行優化——不適用；scope 見第 7 點。

**blocker**：無。　**major**：無。　**minor**：無。

**informational**（四條，皆不影響合併）

- **i1** `.claude/agents/dead-code-sweeper.md` 是六份定義中唯一沒釘「禁派 fork」的（其餘五份 `grep -c` 各 1、它 0）。票文只列五份，**不是 scope 違規**；但它的 `tools:` 同樣無 `Agent`，與 merge-reviewer／qa（同樣無 `Agent` 卻釘了句）處理不一致，前饋層留了一個缺口。機械層不受影響——hook 依 `agent_id` 身分擋，對所有 subagent 一視同仁。建議記 LS-96，下次動 agent 定義時順手補。
- **i2** `scripts/hooks/fork-guard.test.sh` 的 ⑤③–⑤⑥（既有四條 PreToolUse 仍在）整段包在 `if [ -n "$real_jq" ]` 內且**無 else**：jq 缺席時少跑 4 組卻仍印「✓ 自測通過」，組數 26→22 沒有任何提示（fail loud 的反例）。實測本機 `/usr/bin/jq` 存在（macOS 內建）、GitHub macOS runner 亦內建，本 PR 不受影響；且這是 `scripts/hooks/large-file-read-guard.test.sh` 第 395 行的既有慣例（`background-bash-guard.test.sh` 第一段有 else、第二段同樣沒有），照抄慣例合理。若要收斂：else 補印一行「jq 缺席，⑤③–⑤⑥ 略過」。
- **i3** `.github/workflows/ci.yml` 的 step `name:` 字串同時補上了 `large-file-read-guard`（LS-239 當時漏列），嚴格說在 LS-254 範圍外。只是顯示字串、零行為影響，與 `fork-guard` 的新增在同一行，不要求還原。
- **i4** `.claude/agents/ui-designer.md` 的新 bullet 與前一個 bullet 之間空了一行（loose list），`visual-reviewer.md` 則緊貼上一條——純 markdown 風格差異，渲染結果相同。

**沒看的範圍／無法驗證**

- **hook 在真 session 是否生效未驗**（本質限制，非疏漏）：我這個 subagent session 的 hooks 讀自主 checkout 的 `.claude/settings.json`（`main` e3f566d，尚未含本條），無法自我觸發。`/hooks` 重載＋pipe-test 只能由 orchestrator 在併入 main 後做——handoff 與 PR body 都已列此步，請務必補跑。我能提供的最強旁證：第 1 點的合成 payload 實測，以及工具名 `Agent` 確實出現在 `.claude/agents/ios-dev.md` frontmatter `tools:` 並被 `agent-tools-check.sh` 釘住。
- CI `rules` job 的實跑結果未看（只驗 yml 接線與本機逐支綠）。
- 未持有／未取用 supabase lock，未跑 DB 測試、未開模擬器。
- 票文範圍 4（Pencil 寫入鎖）本票明示不做，未審。

**裁決：APPROVE**——無 blocker／major。六格語意逐格實測正確；fail-open 極性刻意、與 `|| exit 2` 的省略互相自洽，且不會在應擋時放行；mutation 重放與申報相符；scope 與票文逐項對應。

---

`bash scripts/gates/handoff-evidence-check.sh LS-254-verdict.md` → rc=0，12 個列項全數「有證據」（第 7–18 行）。

### 2026-09-13T06:59:56.233Z · Chih-Lin Yeh · `c28ec512`

**orchestrator 09-13 14:59**：merge-review R1 **APPROVE**（`6714b95f`，status 貼 `bfdc5f7`；六格 stdin 實測、mutation 紅在預期斷言、scope 13 檔對應、merge-tree 無衝突）→ 啟動併入鏈：merge #389 → main tip status → back-merge PR main→development → dev status。i1（sweeper.md 未釘）／i2／i3／i4 記 LS-96 `a7e9e910`。併 main 後：主 checkout pull、**請使用者 `/hooks` 重載**、pipe-test 一次 → sweeper → lesson → cleanup → Done。

### 2026-09-13T07:09:10.252Z · Chih-Lin Yeh · `fdaa2530`

**orchestrator 09-13 15:09**：#389 已併 main `a013b48`（status 已貼）、back-merge #391 等 CI；`cleanup-merged.sh --apply LS-254`（worktree 1 已移除）；主 checkout pull 到 `a013b48`，已請使用者 `/hooks` 重載。派 sweeper（hook／agent 定義／文件／殘留物）→ lesson review → #391 併入後 Done。

### 2026-09-13T07:12:17.204Z · Chih-Lin Yeh · `7d089135`

## dead-code sweep — LS-254

**dead-code-sweeper 09-13 15:11**

範圲：`git diff 7ea54c9..a013b48`（PR #389 merge commit，main 現況一致，`git log -1` 已核對 HEAD＝`a013b48`）；13 檔 +417/-10，只讀不改。

### hook（fork-guard.sh／fork_guard.py／settings.json／fork-guard.test.sh）— 0 筆

- `fork_guard.py` 只定義兩個函式 `_identity`／`main`，皆有呼叫點（`grep -n "_identity(\|main(" scripts/hooks/fork_guard.py`）；本檔**不 import `pretool_engine`**（與 `large_file_read_guard.py`／`background_bash_guard.py` 不同——那兩支借用它的引號／heredoc 掃描函式，本檔只解析 JSON 頂層欄位，無需重用 tokenizer），未發現「保留給 pretool_engine 卻沒接的 import」。
- `.claude/settings.json` 六條 PreToolUse `matcher`（`python3 -c` 解析）：`Bash|Read|Grep`／`Bash|Write|Edit|MultiEdit|NotebookEdit`／`Bash`／`Bash|Read`／`Agent`／`mcp__linear__save_issue`，`Agent` 只出現一次，無重複註冊。
- `fork-guard.test.sh` 用 `mktemp -d`（`work`／`mut`／`mut2`）＋`trap 'rm -rf "$work"' EXIT`，mutation 段落各自 `rm -rf "$mut"`／`rm -rf "$mut2"`，實跑（`bash scripts/hooks/fork-guard.test.sh` → `✓ fork-guard.sh 自測通過（26 組）`）後 `/private/tmp` 下無殘留子目錄；未發現孤立 fixture 檔。

### agent 定義 — 1 筆待判斷

- **`.claude/agents/ios-dev.md:14`**：本 feature 在既有 LS-209 那行**尾端附加**一句：「**禁派 fork（LS-254）**：fork 繼承整份派工單、會把它當自己的任務平行執行；PreToolUse `fork-guard.sh` 對非主 session 的 `subagent_type: fork` 機械 deny；任何子 agent 不得寫檔／commit／改 PR／貼 Linear。」——同一行前段（LS-209 原文）已經寫了「需要平行處理只能派 `Explore`（唯讀搜尋）；**不得派 fork／subagent 改動任何檔案**——所有程式碼修改必須自己直接做，不假手會寫檔的 subagent」。兩句語意重疊（禁 fork／改派 Explore／子 agent 不得寫檔），是同一規則講兩遍。
  - 證據：`git diff 7ea54c9..a013b48 -- .claude/agents/ios-dev.md` 只有這一行被改；其餘四份定義（`ui-designer.md`／`visual-reviewer.md`／`merge-reviewer.md`／`qa.md`）用 `grep -n "fork\|Explore"` 逐一核對，改動前皆無同類句子，不重疊。
  - 對照 handoff（`f39b7aad`）申報「`ios-dev.md` 第 14 行**只補 LS-254 指標句**」——實際 diff 是完整重述句，不是純索引標記（如「另見 LS-254」），與申報字面不完全一致；merge-review R1（`6714b95f`）第 7 點只核對「舊文字完整保留、是尾端附加」，未指出內容重疊。
  - 不算誤報的理由已排除：`agent-tools-check.sh` 的兩個釘句（`scripts/gates/agent-tools-check.sh:120` 舊句「不得派 fork／subagent 改動任何檔案」LS-209；`:137` 新句「禁派 fork」LS-254）鎖的是**不同字面**，自測 ㉒（`agent-tools-check.test.sh` 新增段）明確驗證兩句缺一不可（「LS-209 舊句在也不救——字樣不同」），所以兩個 gate 釘句本身不是死碼、都在被動用；問題只在 agent 定義**正文內容**語意重複，不影響機械層。
  - 建議處置：不影響功能與 gate（兩個釘句各自必要，不能只留一句），純屬文字精簡機會——**可安全裁剪**成純索引句（例如把新句改成「另見 LS-254：PreToolUse `fork-guard.sh` 機械擋非主 session 的 fork」），但也可能是刻意的 LS-238 事故引用格式（每次事故各自掛 LS 編號句、即便語意重疊，方便未來稽核回溯是哪次事故補的）——**留給 orchestrator 判斷**是否值得為此小改再開一次 agent 定義異動，或併入下次動 `ios-dev.md` 時順手精簡；不建議另開票（size 太小）。

### 文件（COLLABORATION §1／§7）— 0 筆

- §7 新列一行引用 `scripts/hooks/fork-guard.sh`＋`fork_guard.py`（`ls` 確認存在）與自測 `fork-guard.test.sh`（存在，26 組綠）；CI 接線 `.github/workflows/ci.yml:224-226` 確有 `bash scripts/hooks/fork-guard.test.sh || rc=1`。
- §1 新 bullet 標 `（LS-254）`，`grep -c LS-254 docs/COLLABORATION.md` = 2（§1 一次、§7 一次），皆可反查到本票。

### 殘留物 — 0 筆

- `git ls-files | grep -i "LS-254"` 無結果（exit 1）——沒有票號命名的殘留檔留在 repo。
- `.github/workflows/ci.yml` 兩處改動（step name 字串補 `large-file-read-guard／fork-guard`；新增一行 `bash scripts/hooks/fork-guard.test.sh || rc=1`）皆為新增，無註解掉的舊步驟。

### 既有死碼觀察

無——本次審查範圍內（13 個改動檔）未發現本 feature 之外的既有死碼；未擴大掃描到範圍外檔案（依 Rule 3 不動不相干的東西）。

### 建議處置

- ios-dev.md 內容重疊：記入 LS-96 待辦池（純文字精簡，非 High，不夠格另開票）或留給 orchestrator 併入下次動 `ios-dev.md` 的票順手處理。
- 其餘四類：查無 finding，無需處置。

### 搜尋方法與盲區聲明

- 方法：`git diff 7ea54c9..a013b48`（含 `--stat`／`--name-status`）鎖定範圍；`grep -n`／`grep -c` 交叉比對規則字樣與檔案存在性；`python3 -c "ast.parse"` 列舉 `fork_guard.py` 函式定義與呼叫點；`python3 -c "json.load"` 解析 `settings.json` matcher 清單；實跑 `fork-guard.test.sh`／`agent-tools-check.sh`／`agent-tools-check.test.sh` 三支自測確認現況綠。
- 盲區：純文字／AST 靜態掃描抓不到 hook 在真實 Claude Code session 中是否真的被 `/hooks` 重載生效（merge-review R1 已註明這是本質限制，需 orchestrator 併入後實測 pipe-test，非本次死碼掃描範圍）；未對 `.claude/agents/*.md` 全文做語意近似度掃描，只針對本 feature 改動行與其所在同一 bullet 做重疊比對，不排除其他既有規則間也有類似重疊（範圍外，未查）。


### 2026-09-13T07:12:49.125Z · Chih-Lin Yeh · `46a49287`

**orchestrator 09-13 15:12 收尾儀式（①～③；④ 不適用）**

① sweeper `7d089135`：hook／設定接線／CI／殘留物 0 筆；`ios-dev.md:14` 新增「禁派 fork（LS-254）」與同行既有 LS-209 句語意重疊（兩釘句鎖不同字面、皆必要）→ 併入 LS-96 `a7e9e910` i1 同一則（下次動 agent 定義時一併精簡＋補 sweeper.md）。
② lesson review：
- 這張票從第 3 次事故到 hook 併 main 花 2 小時 20 分——「同類事故 ≥2 次 → High → 當輪開票」的機制有效；前兩次只記池是慢了一拍，下次同類 read-only 越權第 2 次就該升票。
- fail-open 極性（settings.json 不接 `|| exit 2`）與 `large-file-read-guard` 一致，reviewer 實測 deny 由腳本自己 exit 2 傳出——這個「哪一層負責 deny」的判準值得寫進 hook 撰寫慣例（記池 P3，與 i2 的 jq else 同票）。
- hook 對主 session 不生效直到 `/hooks` 重載——已請使用者；在那之前靠派工單一句「不要派 fork」兜底。
- 工具缺口：無。
③ cleanup：15:08 `cleanup-merged.sh --apply LS-254`（worktree 1）。
④ Pen：不適用。

→ back-merge #391 併入 development 後 Done。

### 2026-09-13T07:43:43.789Z · Chih-Lin Yeh · `a328d52a`

**orchestrator 09-13 15:43**：併入鏈後半（#391 等 CLEAN）因 main push run `34744124483` `ci` 紅退出——xcresult 為 `testTappingPrivacyRowOpensCorrectLegalDocumentSheet` 同步 flake（同 SHA 的 PR run 全綠；記 LS-96 P2，併 LS-253 同類）→ `gh run rerun --failed` 後重掛 back-merge follow（等 CLEAN → merge #391 → dev tip status）→ Done。

### 2026-09-13T08:20:08.060Z · Chih-Lin Yeh · `f8139f67`

**orchestrator 09-13 16:20**：back-merge #391 併入 development `541d030`（dev tip `merge-review` status 已貼；main ⊆ dev 恢復）。收尾①～③已記（`46a49287`）→ **Done**。fork-guard hook 已在主 checkout；使用者 `/hooks` 重載前，派工單「不要派 fork」句兜底。

