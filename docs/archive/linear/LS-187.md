# LS-187 Harness：巡檢「專屬模擬器」段改為「票已 Done／Canceled 或 worktree 已不存在的 `LS-<n>-*` 一律 ⚠＋動作清單 `→ cleanup-merged.sh --apply LS-<n>`（不再只看 >7 天未用）」＋列 Xcode 預設機台數／磁碟；使用者 2026-09-05 指出 4 台 Done 票殘機巡檢沒抓

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:S, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-05T03:42:35.635Z |
| 開始 | 2026-09-05T04:27:15.428Z |
| 完成 | 2026-09-05T06:34:50.885Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-187/harness巡檢專屬模擬器段改為票已-donecanceled-或-worktree-已不存在的-ls-n-一律-動作清單-cleanup |
| 關係 | related→LS-186, related→LS-40, related→LS-4, related→LS-165, related→LS-96, related→LS-30, related→LS-167, related→LS-3, related→LS-76, related→LS-83, related→LS-176, related→LS-100, related←LS-238, related←LS-205, related←LS-198 |

## 描述

## 來源

使用者 2026-09-05 11:5x：「巡檢程式不是應該要巡檢用不到的模擬器嗎」——當下 `xcrun simctl list` 共 41 台，其中 4 台 `LS-158／169／173／174-*` 是 Done 票殘留（皆 Shutdown），巡檢連續 20+ 輪印「（無 xcrun 或無 >7 天未用的專屬模擬器）」沒抓到。根因：(1) `scripts/ops/patrol.sh` 專屬模擬器段沿 [LS-83](https://linear.app/little-sprout-app/issue/LS-83/harnessdetect-simulator-以機型名選-destination多-worktree-併發-xcodebuild-test) 規則只列「>7 天未用」，票 Done 後 7 天內的殘機視為正常；(2) [LS-176](https://linear.app/little-sprout-app/issue/LS-176/harnesspen-openpen-read-對pen-記得的舊-worktree-路徑已不存在視為可安全捨棄設計票新鮮度保證恢復) 讓 `cleanup-merged.sh --apply LS-<n>` 連刪專屬機，但只在呼叫當下生效——這 4 張票在 [LS-176](https://linear.app/little-sprout-app/issue/LS-176/harnesspen-openpen-read-對pen-記得的舊-worktree-路徑已不存在視為可安全捨棄設計票新鮮度保證恢復) 上線前就清過 worktree，之後沒有任何機制再回頭掃。orchestrator 已手動刪除 4 台（notes 11:4x）。

## 範圍

1. `scripts/ops/patrol.sh` 專屬模擬器段：對每台名稱 `LS-<n>-*` 的模擬器，判定「該票 worktree 不存在（`.claude/worktrees/LS-<n>` 缺）」或「Linear 狀態 Done／Canceled（`--linear` 模式下查；無 API key 時只用 worktree 判定）」→ 印 ⚠ 並在動作清單加 `→ bash scripts/ops/cleanup-merged.sh --apply LS-<n>`（該腳本已含 simctl delete；若 worktree 已不存在而腳本略過附屬資源，改印 `→ xcrun simctl delete <udid>`）；Booted 的仍走 [LS-100](https://linear.app/little-sprout-app/issue/LS-100/harness模擬器用完必關-ios-devqa-任務結束-simctl-shutdownpush-gate-測完關專屬機巡檢列出) 規則不刪；保留 >7 天未用的舊規則作為第二層。`--brief`（SessionStart）只印台數。
2. `cleanup-merged.sh`：`--apply LS-<n>` 在 worktree 已不存在時**仍**執行附屬資源清理（[LS-176](https://linear.app/little-sprout-app/issue/LS-176/harnesspen-openpen-read-對pen-記得的舊-worktree-路徑已不存在視為可安全捨棄設計票新鮮度保證恢復) (e) 段目前以「worktree 不在磁碟」為前提之一——核對它對「worktree 早已清、只剩模擬器」的票是否會跳過；若會，補一條路徑：`--apply LS-<n> --orphans` 或自動判定）。
3. 專屬模擬器段末尾一行「Xcode 預設模擬器 <m> 台（未列入清理；修剪需使用者裁定）」＋ `CoreSimulator/Devices` 體積（沿 [LS-176](https://linear.app/little-sprout-app/issue/LS-176/harnesspen-openpen-read-對pen-記得的舊-worktree-路徑已不存在視為可安全捨棄設計票新鮮度保證恢復) 水位段的 du 快取／看門狗）。
4. 自測：`patrol.test.sh` 補「Done 票殘機 ⚠＋動作行」「worktree 在（In Progress）不 ⚠」「Booted 不刪」三案（stub `xcrun`＋假 worktree）；`cleanup-merged.test.sh` 補孤兒模擬器案。COLLABORATION §4-b 巡檢段一句、§7 對照表該列更新。

## 不做

自動刪除 Xcode 預設機（需使用者裁定）；Booted 機的處置（[LS-100](https://linear.app/little-sprout-app/issue/LS-100/harness模擬器用完必關-ios-devqa-任務結束-simctl-shutdownpush-gate-測完關專屬機巡檢列出) 既有）。

## 驗收

- [ ] stub：一台 `LS-999-iPhone17Pro` Shutdown、無 worktree → 巡檢 ⚠＋動作清單一行；有 worktree → 不 ⚠（自測）
- [ ] 真環境：對現況跑一次巡檢，預設機台數與體積印出、專屬機 0 台
- [ ] merge-reviewer 通過；hotfix 流程（自 main、PR 回 main、back-merge）

## 留言（7）

### 2026-09-05T04:56:34.384Z · Chih-Lin Yeh · `97bad99b`

## Handoff — LS-187（2026-09-05 12:55 CST）

**Ticket**：LS-187（harness，hotfix 流程：自 `main` `9492c84` 切 `hotfix/LS-187-patrol-done-ticket-sims`、PR 回 `main`，併入後需 back-merge `main`→`development`）

**已完成**
1. `scripts/ops/patrol.sh` 專屬模擬器段——**第一層看票**（新）：每台 `LS-<n>-*` 先看該票 worktree 是否還在磁碟（`git worktree list` 無「目錄存在且 basename／分支整字含 `LS-<n>`」者；整字比對同 `cleanup-merged.sh matches_filter`，LS-3 不中 LS-30），或 `--linear` 下 Linear 已 Done／Canceled（只問 worktree 仍在的票、批次一次）→ ⚠＋動作行 `→ bash scripts/ops/cleanup-merged.sh --apply LS-<n>`（human／`--brief` flag／`--json` 欄 `orphan_simulators`）。Booted 的不列刪、只印 ⓘ（LS-100 規則不變）。缺 key（exit 3）／查詢失敗／未帶 `--linear` 都只用 worktree 判定並在段末註明。第二層沿 LS-83（>7 天未用只列不刪）。段末一行「Xcode 預設模擬器 m 台（未列入清理；修剪需使用者裁定）· CoreSimulator/Devices X GB」——Devices 體積走 **du 快取**（`PATROL_DU_CACHE`／`PATROL_DU_CACHE_MIN`＝360 分鐘；human／`--json` 過期才現量一次、看門狗同 LS-176；`--brief` 不量只讀快取；水位段沿用同一值只另量 DerivedData，不重複 du）。`--brief` 表頭「專屬模擬器逾期 n」→「專屬模擬器待清 n（殘機 k）」。
2. `scripts/ops/patrol-linear.sh --closed <n,…>`＋`patrol_linear.py` 新查詢 `CLOSED_BY_NUMBER_QUERY`（`number in […]`＋`state.type in completed／canceled`，一頁 250）：patrol.sh 自己仍不碰 Linear token（§4-b／§5-b 陳述維持為真）；`--closed` 不跑整份報表、**不回頭呼叫 patrol.sh**（呼叫端就是它，會遞迴）；exit 0 查過／3 無 key／2 格式錯／1 查詢失敗；token 仍只走 curl `-K -` stdin。
3. `cleanup-merged.sh`：核對票文範圍 2——(e) 段對「worktree 早已清、連 worktree 記錄都沒有、只剩專屬機／DerivedData」的孤兒**本就會清**（`FILTER_WT_PATHS` 空 → `e_left=0` 路徑），不需 `--orphans` 旗標；只補檔頭說明＋自測 ㉑ 釘住（mutation：改成「本次真的移除過 worktree 才動附屬資源」即紅）。
4. 自測：`patrol.test.sh` ⑭ 補四票 worktree（第一層先看票，沒 worktree 會全被判成殘機、測不到 `-gt 7`）＋㉑ 斷言改看 `derived_data_gb`＋新 ㉓ 35 條（殘機 ⚠＋動作行／worktree 在不 ⚠／Booted 不列刪／LS-30 整字比對／預設機 2 台／du 快取寫入·沿用·brief 不量不寫／`--linear` 假身：回 Done→⚠（worktree 仍在）且 `--closed` 只帶 3 一次、exit 3→只用 worktree、exit 1→註明失敗仍 exit 0）；`patrol-linear.test.sh` ⑥b 8 條（`--closed` fixture：只印 completed／canceled、排序去重 `[3, 7, 90]`、只打一次、token 不進 argv、無 key exit 3、格式錯／空值 exit 2）；`cleanup-merged.test.sh` ㉑ 13 條（孤兒 dry-run 零副作用、apply 真清、鄰近 LS-9400 不動、摘要 1／1）。全檔隔離：假 Devices 目錄、自己的 du 快取檔、`LINEAR_API_KEY` unset。
5. 文件：`docs/COLLABORATION.md` §4-b 工具段一句（殘機規則＋du 快取）／cron 模板旗標例加 `→ cleanup-merged.sh --apply LS-<n>`（LS-187）、§7 LS-83 列改寫（第一層看票＋自測清單）、LS-176 列補「孤兒也照清」。

**已驗證**
- `bash scripts/ops/patrol.test.sh` → 227 ✓ exit 0；`bash scripts/ops/cleanup-merged.test.sh` → 全綠；`bash scripts/ops/patrol-linear.test.sh` → 通過。改動前三支先跑基線皆綠。
- 真環境**唯讀**跑一次 `bash scripts/ops/patrol.sh 40 --linear`（17.6s；當下 10 台：Xcode 預設 2、在飛票 LS-165／167 各 1、demo／qa 6）：段末印「Xcode 預設模擬器 2 台（未列入清理；修剪需使用者裁定）· CoreSimulator/Devices 20 GB（du 剛量）· 票狀態已查 Linear（LS-165、LS-167）」，專屬機殘機 0 台（票文驗收第 2 條）；Linear 半段照常。
- 未碰真實模擬器（無 `simctl delete／create／boot`）；未碰 Pen／容器／lock；Linear 只讀。
- push gate 通過（無 Swift 變更，依 LS-76 跳過 SwiftLint／unit tests；api-contract／tap-target／error-codes／migration 三檢皆綠）；`pr-body-check.sh … --branch hotfix/LS-187-patrol-done-ticket-sims --verify` exit 0。

**未完成／剩餘**
- 待 merge-reviewer 審 PR #304；reviewer 每條 informational 依規則處置（已修附 SHA＋檔案:行號／記入待辦池 LS-96 附 comment id／另票限 §5-b High 以上／不修＋理由），一條不省，回貼本票。
- 併入 `main` 後：back-merge `main`→`development`（hotfix 唯一保留的 back-merge）；票文驗收第 3 條的 hotfix 流程由 orchestrator 收尾。
- 主 checkout 的 `SessionStart`／cron 要吃到新版需 `git pull` main 後才生效（腳本在主 checkout）。

**風險與已知問題**
- 第一層只會多列，不自動刪；刪除仍由人跑 `cleanup-merged.sh --apply`（自帶 dry-run／鎖中不刪／Booted 先 shutdown）。
- Booted 殘機刻意不列刪（LS-100），只印 ⓘ；關掉後下一輪自然落入第一層。
- du 快取檔在 `$TMPDIR`（每使用者一檔）；被清只是下一輪 human 模式重量一次（≤25s 看門狗）；`--brief` 從不量。
- `--closed` 一頁 250 無分頁：一輪 `LS-<n>-*` 票號數遠小於此，超過只會漏標（安全方向）。
- 盲區不變：`LS-<n>-*` 命名的 scratchpad 副本正在跑 xcodebuild 抓不到（LS-176 已記）。

**產出位置**
- PR #304 https://github.com/CLYEH/little-sprout/pull/304（base `main`）
- branch `hotfix/LS-187-patrol-done-ticket-sims`，head `0166055d5c18e0c04671aeb910da6ab50c373717`（4 commits：`d6785e0` patrol-linear --closed／`005bb04` patrol 專屬模擬器段／`5e0ecca` 三支自測／`0166055` 文件）
- worktree `.claude/worktrees/LS-187`

### 2026-09-05T05:18:56.252Z · Chih-Lin Yeh · `05886d9c`

## merge-review R1 — **APPROVE**（PR #304，head `0166055d5c18e0c04671aeb910da6ab50c373717`，base `9492c84`；2026-09-05 13:17）

範圍：8 檔 +360/−34（patrol.sh／patrol-linear.sh／patrol_linear.py／cleanup-merged.sh 檔頭＋三支自測＋COLLABORATION）。CI 四項全 pass（run 33945858510）。**無 blocker／major**；**1 minor（測試鑑別力）＋4 informational**。

### 我實跑驗證的部分

- **三支自測（`/bin/bash` 3.2.57）**：`patrol.test.sh` **227 ✓**（與宣稱一致）、`cleanup-merged.test.sh` **211 ✓**、`patrol-linear.test.sh` **97 ✓**，全 exit 0。
- **我自己的 8 支 mutation**（複製樹，基線先驗 227 綠——複製樹缺 `.claude/settings.json` 會讓 ⑨ 假紅，已補齊後才開跑；每支跑完還原重驗）：
  - M2 Booted 殘機也列刪 → **killed**（㉓(c) 5 條紅，含「不印 simctl delete」「要印 ⓘ」）
  - M3 du 快取過期判定反向（`-lt`→`-ge`）→ **killed**（㉓「下一輪沿用 du 快取」紅）
  - M4 `--closed` exit 3 當成查到了 → **killed**（㉓「無 key → 段末註明只用 worktree 判定」紅）
  - M6 `--brief` 也去 du → **killed**（㉓「--brief 無快取時不該去量 Devices（hook 30s 預算）」紅）
  - M7 `--closed` 不過濾 `state.type` → **killed**（⑥b fixture 比對紅）；M8 不去重不排序 → **killed**（⑥b「body 應帶 [3, 7, 90]」紅）
  - **M1／M5 存活 → 見 minor-1／info-1**（兩者都只是測試沒鑑別力，程式行為我另外證明是對的）
- **`ticket_has_worktree` 整字比對我直接抽出函式做單元探針**（假 WT_INDEX＋真實存在的目錄）：① 只有 `LS-30` worktree 時問 `LS-3` → **判為殘機**（沒有被子字串誤命中）；② 只有 `LS-3` 時問 `LS-30` → 殘機；③ 只有 `LS-174` 時問 `LS-17` → 殘機（檔頭點名的例子）；④ 只有 `LS-3` 時問 `LS-3` → 有 worktree（正對照）；⑤ 記錄在但目錄已不存在 → 殘機（`[ -d "$p" ]` 生效）。**程式在兩個方向都正確。**
- **cleanup-merged 孤兒路徑我自己重現一次**（獨立於 ㉑：臨時 `git init` repo＋手建 `origin/main`／`origin/development`、PATH 前置 stub `xcrun`（只記錄不動真機）、`LS_DERIVED_DATA_ROOT` 假目錄）：`--dry-run LS-9999`（無任何 worktree／分支）→ 摘要「專屬模擬器 1」、xcrun 只被呼叫 `simctl list devices`、**零副作用**；`--apply LS-9999` → **確實呼叫 `simctl delete AAAA-BBBB-9999`**，證實票文範圍 2 的疑慮不成立、不需要 `--orphans` 旗標；`--apply LS-999`（鄰近票號）→ 專屬模擬器 0、沒碰 LS-9999 的資源。（我的假 DerivedData 目錄沒被刪是**預期**：(e) 段依 `info.plist` 的 WorkspacePath 配對，我的假目錄沒有 plist；㉑ 用有 plist 的夾具覆蓋了這條，我看過它的斷言。）
- **`--closed` 契約**：參數驗證雙層（shell `case` 擋非數字／逗號／前後綴逗號＋python `re.fullmatch`）；缺 key → **exit 3 且訊息走 stderr**（patrol.sh 以 `2>/dev/null` 吃掉、stdout 保持乾淨）；不呼叫 patrol.sh（無遞迴）；token 走 `export` 給 python、再由既有 `gql()` 以 `curl -K -` stdin 送出，**argv 不含 token**（與其他模式同一條路徑）；`closed_tickets()` 在伺服器過濾之外再過濾一次 `state.type`（防禦性）。fail-open 方向（缺 key／失敗 → 只用 worktree 判定＋段末註明）合理：這段只列不刪，寧可少標也不要誤導。
- **真實規模效能**（同 repo、快取熱，`--no-pr --no-fetch`）：41 台全預設機 **1.60s**；41 台其中 30 台 `LS-*`（第一層全跑）**2.77s**；今日規模（10 台、2 台 `LS-*`）**1.53s**。→ 第一層成本約 **40ms／台 LS-\***，今天可忽略（見 info-2）。
- **文件**：COLLABORATION §4-b 工具段＋cron 模板動作行、§7 LS-83 列改寫、LS-176 列補「孤兒也照清」——四處與程式行為逐條對得上（包含「LS-3 不中 LS-30」的宣稱，程式確實如此）。
- **逐 commit 歸屬**：`d6785e0` patrol-linear＋py／`005bb04` 只有 patrol.sh／`5e0ecca` 只有三支自測／`0166055` 文件＋cleanup-merged 檔頭 —— **無夾帶**，worktree 乾淨。
- **PR body**：`--branch hotfix/LS-187-patrol-done-ticket-sims --verify` 直接看 exit code → **0**；段落齊（Ticket／變更摘要／驗證方式／風險／截圖），檔頭段第 3 行已寫明「hotfix：併入後 back-merge `main`→`development`」。
- **head 未前進**：貼 status 前以 `--expect $(gh pr view 304 …)` 再比一次。

### minor-1 · ㉓ 的「LS-3 對 LS-30」案**沒有鑑別力**（整字比對退化成子字串不會紅）

我把 `ticket_has_worktree` 的比對改成 `grep -qF "${1}"`（子字串）跑 227 組 → **全綠、0 條紅**。原因：㉓ 的夾具是「裝置 `LS-30-*` ＋ worktree `LS-3`」，這個方向下子字串與整字比對答案相同（「LS-3」不含「LS-30」）；真正能鑑別的方向是**「裝置 `LS-<短>-*` ＋ worktree `LS-<長>`」**（子字串會把 `LS-3` 誤判成有 worktree → 不 ⚠）。
- 失敗情境：日後有人為了效能把那行 grep 換成 `grep -F`／`case` 子字串比對，227 組仍全綠，而巡檢會對「票號是別票前綴」的殘機**靜默漏標**——正是本票要修的病。
- 程式現在是對的（我的單元探針 ①②③ 都正確），所以只是測試缺鑑別力，不是行為錯。
- 建議（一行夾具）：在 ⑭ 的四個 worktree 裡加一個 `LS-40`（或改名），並在 ㉓ 的裝置清單加 `LS-4-iPhone17Pro`，斷言它必須 ⚠。這樣子字串突變必紅，`patrol.test.sh:691-692` 檔頭寫的那條 mutation 敘述才名副其實。

### informational

**info-1 · `[ -d "$p" ]`（worktree 記錄還在但目錄已被 `rm -rf`）沒有樣本**：我把該檢查拿掉 → 227 組全綠（M5 存活）。行為本身正確（探針⑤：記錄在、目錄不在 → 判殘機）。這條在 `cleanup-merged` 之外手動刪 worktree 時才會遇到，價值低，補一組或入池皆可。

**info-2 · 第一層每台 LS 裝置重掃一次 WT_INDEX，且一輪呼叫兩次**：`ticket_has_worktree` 在「收集要問 Linear 的票號」與「主迴圈判定」各呼叫一次，每次對每筆 worktree fork `basename`＋`grep`。實測 30 台 LS-* 時多花 **~1.2s**（40ms／台）；今天 2 台可忽略，但 `--brief`（SessionStart，30s 預算）走同一段。候選：把結果 memo 成 `has_wt_LS-<n>=0/1` 的字串快取（bash 3.2 可行），或先把 WT_INDEX 收斂成「票號集合」一次算完。純效率，非必要。

**info-3 · du 快取寫入不是原子的**：`printf … > "$du_cache"` 直寫；cron（human）與 SessionStart（brief）同時跑時，brief 可能讀到寫一半的行。影響上限是印錯一個 GB 數字（非數字會被 `case … *[!0-9]*` 擋掉並重量），且 repo 內同類寫入（supabase-lock holder）用的是「暫存檔＋mv」。改法一行，價值低。

**info-4 · `--json` 沒有把 `sim_linear_note` 帶出來**：human 模式段末會印「票狀態已查 Linear／無 key 未查／查詢失敗」，但 `--json` 只有 `orphan_simulators`／`default_simulators`，程式讀 JSON 時分不出「這輪有沒有問過 Linear」——同一份 `orphan_simulators` 在缺 key 時只反映 worktree 判定。若之後有工具消費 JSON，建議補一個字串欄位。

### 沒看／沒跑的（誠實揭露）

- **完全沒有碰真實模擬器**：所有 simctl 互動都是 stub 或 `SIMCTL_LIST_JSON` 合成；`cleanup-merged --apply` 只在我自建的臨時 repo＋假 xcrun 上跑過，沒有對真的 CoreSimulator 執行過任何 delete。
- **沒有打真的 Linear API**：`--closed` 只驗到參數驗證、exit code 契約與 fixture 假 curl；`number in […]` 對真 API 的回傳形狀我沿用實作者的真環境紀錄（handoff 說 LS-165／167 查得到），未自行複驗。
- 未碰 Pen／容器；**lock 未持有**。
- `--linear` 的真實串接我只跑到「假身 `PATROL_LINEAR_SH`」層級（自測涵蓋 exit 0／3／1 三路）。

**verdict：APPROVE**（無 blocker／major）。minor-1 建議在併入前補那一組夾具（一行），或由 orchestrator 決定入池；併入 `main` 後補 back-merge `main`→`development`，並提醒主 checkout `git pull` 後 cron／SessionStart 才吃到新版。

### 2026-09-05T05:28:08.801Z · Chih-Lin Yeh · `d6ae5290`

## Handoff R2 — LS-187（2026-09-05 13:27 CST）

**Ticket**：LS-187（merge-review R1 APPROVE comment `05886d9c`，帶 1 minor＋4 info；R2 只動 `scripts/ops/patrol.test.sh`）

**已完成**
- **minor-1 已修 7d9e128b5ace67bb2e87c7c35c7420ce4145be0d scripts/ops/patrol.test.sh:㉓**：原「裝置 LS-30、worktree 只有 LS-3」方向對子字串比對沒鑑別力（"LS-30" in "LS-3 …" 一樣對不上）。補反方向：fixture 加 worktree `LS-40`（`feature/LS-40-sim`；`LS-4` 的 worktree ⑧ 已移除）＋裝置 `LS-4-iPhone17Pro`（Shutdown、剛用過）→ 斷言必 ⚠ 且動作行 `→ bash scripts/ops/cleanup-merged.sh --apply LS-4`（行尾錨定 `LS-4$`，不被 LS-40 滿足）；反向 `LS-40-iPhone17Pro` 有 `LS-40` worktree → 不 ⚠、不進 flag。`--brief` 表頭改「待清 3（殘機 3）」＋帶 LS-4 flag／不帶 LS-40；`--json` orphan 三筆 `["LS-30","LS-4","LS-999"]`、flags 三筆；`--linear` 假身 `--closed` 只帶 worktree 仍在的 `40,3`。
- info-1～info-4：不修（orchestrator 裁定入池，處置見 R1 comment `05886d9c`）。

**已驗證（含突變）**
- 正常：`bash scripts/ops/patrol.test.sh` → 232 ✓ / 0 ✗、exit 0（㉓ 40 條）；`cleanup-merged.test.sh` 全綠；`patrol-linear.test.sh` 通過。
- 突變（`ticket_has_worktree` 的 `grep -qE "(^|[^0-9])${1}([^0-9]|\$)"` → `grep -qF -- "${1}"` 子字串），同一份自測輸出：
  ```
  mutant rc=1 pass=225 fail=6
  ✗ ㉓ 裝置 LS-4 應被判殘機（worktree 只有 LS-40）——整字比對退化成子字串？
  ✗ ㉓ --brief 表頭台數：待清 3（殘機 3：LS-999／LS-30／LS-4）（輸出應含「專屬模擬器待清 3（殘機 3）」）
  ✗ ㉓ --brief 帶 LS-4 殘機 flag（輸出應含「[專屬模擬器 LS-4-iPhone17Pro] 票 LS-4 的 worktree 已不在（殘機）」）
  ✗ ㉓ --json：orphan_simulators 恰三筆（LS-999／LS-30／LS-4 …）（jq 不成立）
  ✗ ㉓ --linear：段末註明已查 Linear（只列 worktree 仍在的 LS-40／LS-3）（輸出應含「票狀態已查 Linear（LS-40、LS-3）」）
  ✗ ㉓ --closed 應只帶 40,3（實得：--closed 4,40,3 --repo …）
  ```
  還原 `patrol.sh`（`git checkout --`，diff 清）後重跑 232 ✓。R1 版同一突變是 227 全綠（reviewer 所指），現在會紅。
- PR body 補 Review 段（「R2 minor-1 已修 <sha> 檔案:行號」同一行）：`pr-body-check.sh <body> --branch hotfix/LS-187-patrol-done-ticket-sims --verify` exit 0（「已修」SHA 反查在 PR commits 內）；`gh pr edit 304` 已套用。push gate 通過（無 Swift 變更）。
- 未碰真實模擬器／Pen／容器／lock。

**未完成／剩餘**
- 待 orchestrator 對 head `7d9e128` 貼 `merge-review` status（R1 status 在 `0166055`）後併入 `main`；併入後 back-merge `main`→`development`。

**產出位置**
- PR #304 https://github.com/CLYEH/little-sprout/pull/304，head `7d9e128b5ace67bb2e87c7c35c7420ce4145be0d`（R2 一個 commit：`7d9e128` test(harness)）
- worktree `.claude/worktrees/LS-187`（clean）

### 2026-09-05T05:39:43.307Z · Chih-Lin Yeh · `1d09f5d0`

## merge-review R2（增量）— **APPROVE**（PR #304，head `7d9e128b5ace67bb2e87c7c35c7420ce4145be0d`；2026-09-05 13:38）

範圍：`git diff 0166055..7d9e128`＝**1 個 commit、1 檔**（`scripts/ops/patrol.test.sh` +18/−7，純測試）。R1 的 minor-1 已修；CI 四項全 pass（run 33947238173）。**無 blocker／major／minor**，1 條 informational（等價突變說明）。

### (1) M1 重做 — **已被殺，紅 6 條**（與實作者自報一致）

我把 `ticket_has_worktree` 的比對改回子字串（`grep -qF "${1}"`）跑 232 組 → **exit 1、6 條紅**：
- `✗ ㉓ 裝置 LS-4 應被判殘機（worktree 只有 LS-40）——整字比對退化成子字串？`
- `✗ ㉓ --brief 表頭台數：待清 3（殘機 3）`／`✗ ㉓ --brief 帶 LS-4 殘機 flag`
- `✗ ㉓ --json：orphan_simulators 恰三筆…flags 三筆`
- `✗ ㉓ --linear：段末註明已查 Linear（LS-40、LS-3）`
- `✗ ㉓ --closed 應只帶 40,3（實得：--closed 4,40,3 …）` ← 這條特別有價值：它直接把「LS-4 被誤判成有 worktree、於是被丟去問 Linear」的副作用也釘住了。

**我另外加做兩支錨點突變**：
- **M1b 只拿掉尾錨**（`(^|[^0-9])LS-n` 保留、去掉 `([^0-9]|$)`）→ **killed，同樣 6 條紅** —— 尾錨（`LS-4` 不得吃到 `LS-40`）確實被新樣本釘住。
- **M1c 只拿掉前錨**（去掉 `(^|[^0-9])`）→ **存活**。這支我判定為**實務上的等價突變**、不是缺口：差異只在「票號前面緊接一個數字」的字串（如 worktree 目錄 `1LS-4`、分支 `feature/2LS-4-x`），而 repo 的命名一律是 `.claude/worktrees/LS-<n>` 與 `(feature|fix|hotfix)/LS-<n>-slug`，`LS-` 前面不可能是數字。列為 informational，不建議為它再加樣本。

### (2) 新樣本的斷言強度 — 行尾錨定與 `--closed` 都對

- 正向：`grep -qE -- '⚠ LS-4-iPhone17Pro（ORPH-4）票 LS-4 的 worktree 已不在（殘機） → bash scripts/ops/cleanup-merged.sh --apply LS-4$'` —— **`$` 行尾錨定**，`--apply LS-40` 這行不可能滿足它。
- 反向：`grep -qE -- '--apply LS-40$'` 必須**不**出現（LS-40 有 worktree）＋`hasnt '⚠ LS-40-iPhone17Pro'` —— 兩個方向都有樣本。
- `--closed`：`grep -qE -- '--closed 40,3( |$)'`，同樣有邊界（`40,30` 之類不會誤過），且**不含 4**（LS-4 沒有 worktree 就不該被問）；`--closed` 只呼叫一次的批次斷言仍在。順序 `40,3` 由裝置清單順序決定（ORPH-4 在 LIVE-40 之前、LIVE-40 在 LIVE-3 之前），穩定不 flaky。
- 段末註記同步改成「票狀態已查 Linear（LS-40、LS-3）」、`--brief` 表頭 3（殘機 3）、`--json` 三筆並斷言 `flags` 三筆 —— 四個輸出面（human／brief／json／flag）都跟著新樣本一起校準，沒有只改一處。

### (3)(4) body／head／夾帶

- PR body 第 18 行 `- R2 minor-1 已修 7d9e128b5ace…` —— **SHA 緊跟在「已修」之後**，我用兩個版本各驗一次、都看 exit code：本分支的 `pr-body-check.sh`（舊規則）**exit 0**；`origin/main` 上已是 LS-186 版（含 `fix_shas`，CI 實際用的就是這份）**也 exit 0**（`✓ 第 18 行「已修」SHA 7d9e128… 在 PR commits 內`）。兩套規則都過，不會在 CI 上翻車。
- head 未前進：`--expect $(gh pr view 304 --json headRefOid)` 與我審的 SHA 相同，貼 status 前再比一次。
- **無夾帶**：增量只有 `scripts/ops/patrol.test.sh` 一檔，`patrol.sh`／`patrol-linear.sh`／`patrol_linear.py`／`cleanup-merged.sh`／文件一行未動（R1 對它們的結論原封成立）；worktree 乾淨。
- 自測：`patrol.test.sh` **232 ✓**（227＋5，恰好對應新增的五條斷言），exit 0；複製樹基線同樣 232 綠後才開始突變。

### informational

**info-1（本輪新增）· M1c「前錨」無樣本＝實務等價突變**，理由如上（`LS-` 之前不可能是數字）。若日後有人把 worktree 命名放寬（例如允許 `2LS-4` 這種前綴），這條就會變成真缺口——留在 comment 供日後查考即可，不必補樣本。

**R1 的 info-1／2／3／4 本輪未動**（`[ -d "$p" ]` 無樣本／第一層每台重掃 WT_INDEX ~40ms／du 快取寫入非原子／`--json` 缺 `sim_linear_note`）：增量沒有碰到那些程式碼，我重跑 M5（拿掉 `-d` 檢查）仍存活，結論與 R1 相同，處置交 orchestrator（入池或不修）。

### 沒看／沒跑的（誠實揭露）

- 增量是純測試檔，我**沒有**重跑 R1 的其餘 mutation（patrol 的 M2／M3／M4／M6、patrol-linear 的 M7／M8）與 `cleanup-merged` 孤兒重現——被測的程式碼一行未變，R1 的結論直接沿用。
- 仍**完全沒有碰真實模擬器／Pen／容器**，未打真 Linear API；**lock 未持有**。

**verdict：APPROVE**（R1 minor-1 已修且被測住）。併入 `main` 後補 back-merge `main`→`development`；主 checkout `git pull` 後 cron／SessionStart 才會吃到新版巡檢。

### 2026-09-05T06:27:35.446Z · Chih-Lin Yeh · `74a407c6`

## QA — delta R1（`test` tip `b86a66c3916a807e2e337b8e87e0b0f220b9abe4`）— **PASS**

環境：`qa-test` worktree `git checkout --detach b86a66c…`，HEAD 已確認＝目標 SHA、working tree clean。全程未碰真實模擬器（無 `simctl create/boot/delete/shutdown`）、未碰 Pen／容器／supabase-lock，Linear 只讀。

### 零 app 碼證據
`git diff --stat 7a7eea9..b86a66c -- LittleSprout LittleSproutTests LittleSproutUITests` → **空輸出、exit 0**。完整 diff 只有 8 檔（`patrol.sh`／`patrol-linear.sh`／`patrol_linear.py`／`cleanup-merged.sh`／三支自測／`COLLABORATION.md`，+371/−34），app／測試目標一行未動 → 免 xcodebuild，符合派工說明。

### A. 自測（三支全綠，第四支不存在）
- `patrol.test.sh` → **232 ✓ / 0 ✗**，exit 0（含 ㉓ 40 條，覆蓋本票全部情境）
- `cleanup-merged.test.sh` → **211 ✓**，exit 0
- `patrol-linear.test.sh` → **97 ✓**，exit 0
- `agent-tools-check.test.sh` → **⊘ 不存在**（repo 內無此檔，`ls scripts/ops/` 確認），非本票範圍，不計入 PASS/FAIL

### B. 真環境唯讀（`bash scripts/ops/patrol.sh 40 --linear`，主 checkout 有 `.env`，於 qa-test worktree 執行；`patrol-linear.sh` 的 `ROOT` 走 `git-common-dir` 一律解到主 checkout，未寫入任何檔案）
- human：段末印
  `Xcode 預設模擬器 2 台（未列入清理；修剪需使用者裁定） · CoreSimulator/Devices 20 GB（du 快取 91m 前） · 票狀態已查 Linear（LS-165、LS-167、LS-197）`
  專屬模擬器殘機 **0 台**（LS-165／167／197 皆有對應 worktree 且在飛，符合現況）
- `--brief` 表頭：`… 專屬模擬器待清 0（殘機 0） …`
- `--json`：含 `orphan_simulators: []`（陣列存在，空）、`default_simulators: 2`

### C. stub 情境（獨立於自測，另用真實 worktree＋合成 `SIMCTL_LIST_JSON` 手動複驗）
合成三台裝置：`LS-999-iPhone17Pro`（Shutdown，無 worktree）／`LS-165-iPhone17Pro`（Shutdown，**真實**已存在 worktree）／`LS-888-iPhone17Pro`（Booted，無 worktree），對真 repo（可看到 LS-165 等真實 worktree）跑 `SIMCTL_LIST_JSON=… patrol.sh 40 --no-pr --no-fetch`：
```
⚠ LS-999-iPhone17Pro（QASTUB-999）票 LS-999 的 worktree 已不在（殘機） → bash scripts/ops/cleanup-merged.sh --apply LS-999
ⓘ LS-888-iPhone17Pro（QASTUB-888）票 LS-888 的 worktree 已不在（殘機），但 Booted 不列刪（LS-100…）
```
`LS-165` 全程未出現在任何 ⚠／`--apply` 行（只出現在正常 worktree 狀態表）——三案（無 worktree ⚠＋動作行／有 worktree 不 ⚠／Booted 只 ⓘ 不列刪）全部吻合票文驗收第一條。

### D. 文件與 `--closed` 契約
- COLLABORATION §4-b 巡檢段（殘機第一層看票／`--linear` 批次查／du 快取／段末 Xcode 預設機台數與 Devices 體積敘述）與 §7 LS-83／LS-176 列，逐句核對與程式實際輸出一致。
- `patrol-linear.sh --closed 999`：
  - **缺 key**：另建隔離 tmp git repo（無 `.env`）＋`env -u LINEAR_API_KEY`，`--repo <tmp>` 跑 → **exit 3**，stderr `--closed 略過（無 LINEAR_API_KEY）…`（未碰主 checkout）。
  - **有 key**：對主 checkout `.env` 實跑 → **exit 0**，stdout 空（LS-999 非已結案票，符合「無者印空」）。另用 PATH 前置 stub `curl`（原樣轉發＋落地 argv／sleep 2s 供 `ps` 取樣）**在請求真的進行中**用 `ps -eww -o command` 現場快照：argv 只有 `-sS --max-time 25 -X POST … -H Content-Type… --data {...} -K -`，**不含 Authorization／token 字串**，金鑰經 `-K -` 走 stdin——與 merge-review 的程式碼審查結論一致，這次是**真跑時的即時證據**而非只靠讀碼。

### 未跑項目
- 真實 `cleanup-merged.sh --apply` 對 stub 裝置未實際執行（票文與 R1 merge-review 已用假 xcrun 驗過刪除路徑，本輪只驗巡檢「列不列、印什麼動作行」的輸出面，未重複驗證刪除本身）。
- 未重跑 R1/R2 merge-review 已覆蓋的 mutation 測試（不在 delta QA 範圍內，兩輪 merge-review 皆 APPROVE 且自測全綠）。

### 產出位置
- 三份自測 log：`/private/tmp/.../scratchpad/LS-187-qa-{patrol,cleanup-merged,patrol-linear}-test.log`
- 真環境三種輸出：`LS-187-qa-patrol-real-{human,brief,json}.log`
- stub 複驗：`LS-187-qa-stub-devices.json`、`LS-187-qa-stub-human.log`
- `--closed` 契約：`LS-187-qa-closed-999-{nokey-isolated,stubcurl}.log`、`LS-187-qa-curl-argv-capture.log`、`LS-187-qa-ps-during-curl.log`

**裁決：PASS**。commit status 貼於 `b86a66c3916a807e2e337b8e87e0b0f220b9abe4`。

### 2026-09-05T06:34:08.190Z · Chih-Lin Yeh · `ca225ee2`

## dead-code sweep — LS-187

**範圍**：`git diff 7a7eea9172f437676571194b093ef992c2f37239..b86a66c3916a807e2e337b8e87e0b0f220b9abe4`，篩出訊息含 LS-187 的 5 個 commit（`d6785e0`／`005bb04`／`5e0ecca`／`0166055`／`7d9e128`）觸碰的 8 檔（`scripts/ops/patrol.sh`、`patrol-linear.sh`、`patrol_linear.py`、`cleanup-merged.sh`、三支自測、`docs/COLLABORATION.md`，+371/−34）。全程用 `git show <sha>:<path>`／`git diff`／grep 讀主 checkout 的 git 物件，未 checkout、未改檔、未碰模擬器／Pen／容器／lock，Linear 只讀。

### Findings

**F1（minor）· `scripts/ops/patrol-linear.sh:37-38` — `--closed` 參數驗證留一個永遠打不到的 case 分支**

```
37:      [ -n "${2:-}" ] || { echo "✗ patrol-linear：--closed 缺值" >&2; exit 2; }
38:      case "$2" in *[!0-9,]*|,*|*,|*,,*|'') echo "✗ patrol-linear：--closed 須為逗號分隔的票號數字（得到「$2」）" >&2; exit 2 ;; esac
```

第 37 行的 `[ -n "${2:-}" ]` 已經把「`$2` 是空字串或未設」擋掉並以「缺值」訊息 `exit 2`；因此第 38 行 case pattern 裡的 `''` 這個 alternative 永遠不可能被比對到（能走到第 38 行時 `$2` 保證非空）。

證據：`scripts/ops/patrol-linear.test.sh` 新增的 ⑥b 案例裡確實有一條 `bash "$plsh" --closed '' --repo "$repo"` 期望 `exit 2`（見同檔），但斷言只驗 `rc -eq 2`、不驗訊息內容——這條測試無論走「缺值」分支還是「須為逗號分隔」分支都會綠燈，蓋不到、也蓋不了第 38 行的 `''` 分支，靜態推導＋測試斷言範圍兩者一致證實它是死分支。

判定：無害（只是重複的防呆訊息永遠不會被印出），不影響任何行為與現有測試綠燈，不阻塞晉升。

建議處置：可安全刪除第 38 行 case pattern 裡的 `|''`（一字元改動），或留著當「防禦性冗餘」不動——兩者皆可，我傾向後者（surgical，改了要重跑三支自測＋merge-review 已 APPROVE 二輪，不值得為一個無害死分支重開審查輪）。**建議記入 LS-96 待辦池**，不必為此開票或卡 promote。

### 其餘檢查面（未發現死碼）

- **第一層／第二層是否重複計算或有無法到達的分支**（`patrol.sh:338-484`）：追蹤了 `sim_ticket` 的三條路徑——`LS-<n>-*`（第一層判定，命中即 `continue`，不落入第二層，`sim_flagged` 不會被兩層都加一次）、`main-*`（`sim_ticket` 留空，直接跳過第一層 if 區塊、只走第二層 `>7` 天判定）、`demo-*|qa-*|其餘`（`continue`／計入 `sim_default`）。三條路徑互斥、無重疊計數，也沒有邏輯上進不去的分支。
- **du 快取變數／函式是否有定義未用**：`DU_CACHE_MIN`、`du_cache`、`devices_du_src`、`devices_kb`、`need_devices_du`、`disk_low` 六個全部逐一 grep 全檔確認都有讀有寫（`patrol.sh:99-103`、`514`、`539-573`），無孤兒變數；新函式 `ticket_has_worktree`／`closed_state_of`（`patrol.sh:355-365`）都在後面的迴圈裡被呼叫到。
- **`patrol-linear.sh --closed` 參數解析其餘分支**：`MODE=closed` 設定後，唯一消費點在 `patrol-linear.sh:79`（`if [ "$MODE" = closed ]`）內部直接 `exit $?`，不會流到後面組 Booted 模擬器段或第 105 行呼叫 python `--mode "$MODE"` 那行（那行只可能收到 human/brief/json，`closed` 分支已提前 exit）——這是正常的提前返回模式，不是死碼。
- **`patrol_linear.py` 新查詢欄位是否全被用到**：`CLOSED_BY_NUMBER_QUERY` 回 `identifier`／`state.name`／`state.type`，`closed_tickets()` 用 `state.type` 過濾、`identifier`＋`state.name` 組成輸出行——三個欄位全用到，無多餘欄位。
- **自測檔內的暫時性 debug 輸出／跳過案／永真斷言**：對三支自測新增區塊做了 grep（`skip|todo|fixme|debug`）與「`✗` 是否配對 `fail=1`」全檔比對，皆無命中——新增的 ㉓（`patrol.test.sh`）、⑥b（`patrol-linear.test.sh`）、㉑（`cleanup-merged.test.sh`）每條失敗分支都正確設 `fail=1`，無跳過案、無永真斷言。
- **`cleanup-merged.sh`**：`git diff` 只有 4 行、全部落在檔頭註解區塊（`scripts/ops/cleanup-merged.sh:51-56`，補「孤兒也照清」一句說明），沒有帶到任何可執行程式碼變更——與票文「只改檔頭說明」的宣稱一致。
- **CI／gate 設定殘留**：`git diff --stat` 對 `.github/` 與 `scripts/gates/` 路徑為空輸出，本 feature 未觸碰任何 CI workflow 或 gate script，無設定殘留可查。
- **未被引用的檔案／資源**：本次 diff 沒有新增檔案，全部是既有 8 檔的修改，無孤兒檔案。
- **SQL 殘留**：本票不涉資料庫，N/A。

### 既有死碼觀察

無。翻查 diff 周邊程式碼（`size_gb_of`、`disk_dedicated` 等既有函式／變數）皆為本 feature 之外原有邏輯且仍被使用中，未發現本 feature 之外新暴露的既有死碼。

### 結論

本 feature 引入的死碼只有 F1 一項，且屬無害的 unreachable 防呆分支，不影響行為、不影響已通過的 232／211／97 條自測與兩輪 merge-review（`05886d9c` R1 APPROVE、`1d09f5d0` R2 APPROVE）與 QA PASS（`74a407c6`）結論。**不建議卡 promote**，建議記入 LS-96 待辦池供日後 batch 清理。

### 搜尋方法與盲區

- 方法：`git log --oneline --grep="LS-187"` 鎖定 5 個 commit；`git diff <base>..<head> -- <8 files>` 逐檔看完整 diff；對 `patrol.sh` 額外用 `git show <head>:<path>` 落地成暫存檔全文讀（`/private/tmp/.../scratchpad/patrol_head.sh`），grep 交叉比對變數/函式定義與呼叫點；對兩支測試檔的新增區塊做 `grep -v fail=1` 與關鍵字掃描。
- 盲區聲明：(1) 純文字＋grep 搜尋抓不到透過 `eval`／間接變數名拼接的動態引用——本次逐一人工讀過所有新變數的定義與唯一消費點，未發現此類拼接，但不能排除更隱蔽的間接引用；(2) 未實際執行任何腳本（含測試腳本）驗證行為，F1 的「不可到達」判定是純靜態 shell 語意推導（`-n` 對空字串必為 false）+ 對應測試斷言範圍的交叉核對，未用 mutation 實跑復現——若要 100% 實證需要跑一次自測並在 case pattern 加 debug echo，本輪未做（唯讀限制）；(3) 未檢查 CI 實際 run 的 workflow 檔（本 feature 未觸碰，判定為 N/A，但未逐行翻 `.github/workflows/*.yml` 內文確認完全沒有間接依賴這 8 檔的路徑字串）。


### 2026-09-05T06:34:47.572Z · Chih-Lin Yeh · `28e06c69`

**orchestrator 2026-09-05 14:4x 收尾儀式 → Done**

- 併入：PR #304 → main `6f4907f`；back-merge #308 → development；promote dev→test→main `b86a66c`；tag **v0.13.5**；主 checkout 已 pull，新巡檢自 14:33 起生效（首輪即印「殘機 0 · 預設機 2 台 · Devices 20 GB · 票狀態已查 Linear」）。delta QA PASS `74a407c6`。
- sweeper `ca225ee2`：1 件 minor（`patrol-linear.sh:37-38` `--closed` 的 `''` case 分支永不到達，前面 guard 已擋）→ 入 LS-96（見新 comment），不重開 review。
- lesson learning：(1) 使用者一句「巡檢不是應該抓用不到的模擬器」揭露 LS-83 規則只看 >7 天、LS-176 只在 cleanup 當下清——巡檢規則要以「票的生命週期」而非「時間」為主軸；(2) reviewer 用「反方向樣本」證明整字比對測試無鑑別力，日後所有字串比對規則的自測都要兩個方向各一案；(3) `--closed` 讓 patrol.sh 仍不碰 token、只借 patrol-linear.sh 一次批次查詢，維持 §4-b「patrol.sh 無 Linear token」為真。
- cleanup：worktree／本機分支 14:02 已清；info-1～4 入池 `464baaf6`（LS-198 正在清）。

