# LS-72 設計 chore：LS-46 R10/R11 informational F6–F8（print-edge token 合併、Tokens 表補對比欄、其餘 12 個沖印品換 token）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | Low |
| 標籤 | lane:design |
| Cycle | 1 |
| Project | Phase 1 — 單一家庭 MVP |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-08-25T03:24:31.618Z |
| 開始 | 2026-08-25T04:37:48.262Z |
| 完成 | 2026-08-25T05:51:38.775Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-72/設計-chorels-46-r10r11-informational-f6-f8print-edge-token-合併tokens |
| 父票 | LS-46 設計定稿合併：C 基準＋使用者指定調整 → design/littlesprout.pen |
| 關係 | related→LS-64, related→LS-62, related→LS-67, related→LS-61, related→LS-68, related→LS-74, related→LS-17, related→LS-26, related→LS-46, related←LS-119, related←LS-96, related←LS-91, related←LS-44, related←LS-30, related←LS-70, related←LS-81, related←LS-75 |

## 描述

## 來源

[LS-46](https://linear.app/little-sprout-app/issue/LS-46/設計定稿合併c-基準使用者指定調整-designlittlesproutpen) R10 核銷審查 informational F6–F8（2026-08-25）；零像素影響，不綁 [LS-46](https://linear.app/little-sprout-app/issue/LS-46/設計定稿合併c-基準使用者指定調整-designlittlesproutpen) PR。

## 項目

* F6：`$print-edge`／`$print-edge-bottom` 兩值相同（8）→ 合併為單一 token 或保留兩個並在 Tokens 板說明「下緣厚度由壓印行掙來」。
* F7：Tokens 板對比表補 `$print-ink`／`$print-ink-secondary` 在 `$print-paper` 上的實測對比欄（14.92／12.55、9.66／8.13）。
* F8：其餘 12 個沖印品（02／05／07 家族等）Print 白邊改用 `$print-edge` token（目前只有歡迎家族八張換完）。

## 規則

ui-designer 一輪＋visual-reviewer 一輪核銷（聚焦，不重開全稿）；每項一 commit；取證 `.claude/evidence/LS-<n>/`。依賴：[LS-46](https://linear.app/little-sprout-app/issue/LS-46/設計定稿合併c-基準使用者指定調整-designlittlesproutpen) 併入 development 後、且與 [LS-67](https://linear.app/little-sprout-app/issue/LS-67/設計多寶貝-profile-三畫面建檔管理切換-ui-designervisual-reviewer-3-輪) 不同時編輯 .pen。

## 追加（[LS-46](https://linear.app/little-sprout-app/issue/LS-46/設計定稿合併c-基準使用者指定調整-designlittlesproutpen) 收尾巡檢，PR #104 review F1/F2）

* F1：`git rm design/mengya-diary-crayon-equal.png`（1.06 MB，R1 證明為拉伸非重繪、全稿零引用；比照 R7 刪 `-dark.png`）；`.pen` 內那段「舊檔保留於 design/ 供對照」文字同步改為「已刪，歷史版本 git 可取回」。
* F2：`design/mengya-diary-crayon-v3-measurements.json` 的 `candidate` 改為 repo 相對路徑 `design/mengya-diary-crayon-v3.png`。

## 留言（5）

### 2026-08-25T05:03:46.663Z · Chih-Lin Yeh · `cbfc2a2e`

## Handoff — LS-72 完成（ui-designer 一輪＋visual-reviewer 一輪，依規則核銷）

**Ticket**：LS-72 — LS-46 R10/R11 informational F1/F2/F6/F7/F8

**已完成**（6 commits，`feature/LS-72-design-chore`，PR [#119](https://github.com/CLYEH/little-sprout/pull/119) → development）

| # | 項目 | commit | 內容 |
|---|---|---|---|
| 1 | F1 | `b2d7035` | `git rm design/mengya-diary-crayon-equal.png`（拉伸偽造舊檔，git 可取回）；.pen `jnJqb` 文字同步改為「已刪，歷史版本 git 可取回」 |
| 2 | F2 | `d8550aa` | `mengya-diary-crayon-v3-measurements.json` 的 `candidate` 從前一 session 的 scratchpad 絕對路徑改為 repo 相對路徑 |
| 3 | F6 | `6b38621` | Tokens 板 Sec④ Spacing Table 新增 `print-edge`／`print-edge-bottom` 兩列說明（保留兩 token 不合併） |
| 4 | F7 | `e391f68` | Tokens 板 Sec① Color Table 補 print-ink／print-ink-secondary 在 `$print-paper` 上的實測對比（14.92/12.55、9.66/8.13） |
| 5 | F8 | `4fbe128` | 12 個沖印品（aw57e/ppXvK/H0KHI/kOZbf/bd0jF/Lm66y/yWH0V/XrvpC/JfQJ3/I6A7U/WF95R/PRq01）白邊 padding 從誤用的 `$sp-label` 改為 `$print-edge`/`$print-edge-bottom` |
| 6 | 修正 | `9eddc49` | 依 visual-reviewer R1 finding：F6 的「合併需動已上線的八張歡迎家族 Print」在同一 PR 內就被 F8 推翻（現有 20 張引用 print-edge），改為不綁定會過期的張數；順手修 F7 兩處折行拆詞 |

**已驗證**
- 每 commit 皆經 LS-26 收工程序落地（get_app_state 確認 active editor → autosave backup mtime 晚於最後 execute → cp 落地 → `design-landing-check.sh --expect-nodes N` 綠燈）
- 全樹掃描：TOTAL_NODES 2676（基線 2654 ＋ F6 新增 22 節點，in-scope）；FLAGGED（partially/fully clipped）50（基線 51，−1，逐一核對後確認 50 個全是既有角托／Hero Photo 刻意出界慣例，無一落在本票觸碰的節點上，−1 差值不可歸因於本票任一項零像素改動）
- F7 四個對比數字獨立以 WCAG 2.1 相對亮度公式複驗：#2B141C on #FBEBEC=14.921、on #E8D9D4=12.553；#553040 on #FBEBEC=9.664、on #E8D9D4=8.130——與 ticket 給定數字一致
- **visual-reviewer 聚焦複核**（1 輪，依 ticket 規則不重開全稿）：F1/F2/F7/F8 APPROVE；F6 首輪 ITERATE（過時的「八張」用詞），已修正並重新落地驗證（截圖見 `.claude/evidence/LS-72/r2/`），未再送二輪複核（ticket 規則為 1+1 輪）
- 截圖取證：`.claude/evidence/LS-72/r1/`（F6/F7 首版＋F8 12 張全覆蓋）、`.claude/evidence/LS-72/r2/`（F6/F7 修正後）
- `git status --porcelain` 全程僅 `design/` 底下變更；push gate（單元測試＋api-contract＋error-codes＋分支票號＋合併檢查）全綠

**未完成／不採納**
- 無。5 項全部完成；visual-reviewer 唯一 finding 已修正並驗證。
- Scope 外提醒（不列 finding，未處理）：reviewer 順手指出 `aw57e` 角托 TL 與 BL/BR 的外擴量（3pt／10pt）跟 Spacing Table 文件所述「corner-out 5pt」不完全一致——這是 LS-46 既有幾何，F8 未觸碰，不在本票 scope，建議另開票或留給下次動到該區域時一併核實。

**風險**：零像素影響（F6/F7 純文件補完，F8 兩 token 現值皆為 8）。唯一實質變更是 F1 刪除一個未被引用的 PNG。

**產出位置**
- PR：https://github.com/CLYEH/little-sprout/pull/119
- Worktree：`.claude/worktrees/LS-72`（分支 `feature/LS-72-design-chore`）
- 證據：`.claude/evidence/LS-72/r1/`、`.claude/evidence/LS-72/r2/`（worktree 相對，未 commit）

### 2026-08-25T05:13:26.491Z · Chih-Lin Yeh · `d5072c66`

## Merge gate review — PR #119（LS-72 設計 chore）

**Verdict：APPROVE**（0 blocker／0 major／4 informational）

Base `origin/development`（2c1fe58）… Head `feature/LS-72-design-chore`（9eddc49）。審查方式：`git diff <base>...<head>` ＋ 對 `.pen` 兩版做 JSON 結構級 diff（parent/id 索引比對），非只讀 textual diff。

---

### 四維結論

| 維度 | 結論 |
|---|---|
| Race condition | **N/A** — diff 內 0 行 Swift／SQL／設定；無 actor、無 Task、無上傳佇列、無 Supabase 寫入。 |
| 運算效能 | **N/A** — 無 RLS policy、無查詢、無分頁、無圖片解碼／壓縮路徑。純設計文件。 |
| 平行優化 | **N/A** — 無可平行化的執行路徑。 |
| Scope | **無 finding**——見下方核對。 |

---

### Scope 核對（結構級，非目測）

diff＝3 檔，全在 `design/`。對 base／head 兩份 `.pen` 建 id 索引逐節點比對，變更集**恰好**是：

- **12 個 `padding` 置換**——changed-id 集合與票文／handoff 宣稱的 12 個 ID **完全相等**（無遺漏、無夾帶）：`aw57e ppXvK H0KHI kOZbf bd0jF Lm66y yWH0V XrvpC JfQJ3 I6A7U WF95R PRq01`
- **3 個 text `content` 變更**——`jnJqb`（F1 註記）、`IuREY`／`xInPT`（F7）
- **+22 節點、−0 節點**（F6 兩列）
- `variables`／`themes`／`version`／`fileToken` **逐鍵相等**，無夾帶 token 改動

沒有無關重構、沒有順手改動。符合手術式修改原則。

### 逐項驗證

**F1**（`design/mengya-diary-crayon-equal.png`）
`git grep` 掃 head 全樹：唯一殘留字串是 `jnJqb` 那句刻意保留的「已刪，歷史版本 git 可取回」歷史註記，**零活引用**。`design/` 底下也無其他素材指向它。

**F2**（`measurements.json`）
全 repo（含 `docs/`、`.claude/`、`.github/`、`*.swift`）搜 `mengya-diary-crayon-v3-measurements` → **零讀取者**。路徑改動不可能破壞任何人。

**F6**（Tokens 板 Spacing Table 兩列）
- 新列欄寬 `186/110/110/408/408`、cell `padding [9,14]`、字色／字重／`$font-num` 與 8 個既有兄弟列**逐屬性一致**
- 斑馬條紋未斷：`gxfJT(#F5E1E3)` → `r6QNL2(#FBEBEC)` → `EWCvp(#F5E1E3)` → `s93dpw(#FBEBEC)`，插入後交替仍正確
- 22 個新 id 與全稿 2676 個既有 id **無碰撞**（全檔 id 唯一性檢查通過）
- AX3 欄填 8 正確——兩 token 皆為無 theme variant 的 `{"type":"number","value":8}`

**F7**（對比欄）
四個數字我用 WCAG 2.1 相對亮度公式獨立複算：
```
#2B141C on #FBEBEC = 14.921   on #E8D9D4 = 12.551
#553040 on #FBEBEC =  9.663   on #E8D9D4 =  8.129
```
與稿上 14.92／12.55／9.66／8.13 **完全吻合**。且確認落在正確欄位——`IuREY`／`xInPT` 的祖先鏈為 `Row 2c/2d < Color Table`，位置是第 5 欄（w=280），表頭正是「實測對比」，非「備註」欄。

**F8**（零像素——已證明，非採信）
```
$print-edge = 8   $print-edge-bottom = 8   $sp-label = 8    （base 與 head 皆同，variables 未改）
```
且 12 張新寫入的 padding 陣列 `["$print-edge","$print-edge","$print-edge-bottom","$print-edge"]` 與 8 張既有沖印品**字面完全相同**，順序語意無自創風險。用量統計交叉驗證：`$print-edge` 24→60（8→20 張 ×3）、`$print-edge-bottom` 8→20、`$sp-label` 141→129（−12，仍有 129 處使用，token 未被孤立）。全 repo 無 Swift／docs 引用這些 token，無下游破壞。

### Landing gate ／ CI

我在本機對 **base ＋ 6 個 commit 的 7 份 `.pen` blob** 逐一跑 `design-landing-check.sh`，全綠，節點數序列：
```
634672d 2654 → b2d7035 2654 → d8550aa 2654 → 6b38621 2676 → e391f68 2676 → 4fbe128 2676 → 9eddc49 2676
```
handoff 的 2654→2676 屬實，+22 恰好發生在 F6 commit。CI 4/4 綠（`ci` job 內含 `.pen 落地結構檢查` step）。6 個 commit message 逐條過 `commit-msg-gate.sh`。PR body 檔頭段含 `LS-72`，`rules` job 綠。

`Design:` 欄位：**本 PR 不適用**——CI 的 Design gate 只在 diff 新增 SwiftUI View struct 時才要求，本 PR 0 行 `.swift`。缺欄位非違規。

### 衝突預檢

`git merge-tree --write-tree origin/development <head>` → 乾淨（rc=0，無衝突標記），`mergeStateStatus: CLEAN`。development 自 merge-base 起的 4 個 commit 全是 promote／back-merge，**tree 淨變更為零**，無漂移。其餘 3 個 open PR（#117 LS-17、#120、#124）**皆不含 `design/` 檔案**，`.pen` 無並行編輯衝突。

---

### Informational（4 項，皆不擋合併）

**I1 — 取證路徑與 handoff 不符**（minor）
handoff 稱證據在「`.claude/evidence/LS-72/r1/`、`r2/`（worktree 相對）」，實際落在 repo root `/Users/clyeh/little-sprout/.claude/evidence/LS-72/`；worktree 的 `.claude/` 底下**沒有** `evidence/`。另有一個 handoff 未提及的 `r1-review/`（visual-reviewer 自己的 9 張）。不影響 PR，但下次 handoff 請寫絕對路徑。

**I2 — 體積：LS-74 的估算高估約 50 倍**（informational，含建議 re-scope）
本 PR 帶進 **5 個**相異 `.pen` blob（非 6——F1／F2 共用同一份，F2 只動 json）：
```
raw 合計          6.98 MB
目前落地（zlib）  0.93 MB
delta-pack 後     0.15 MB   （24 個新物件全部，--window=50）
```
關鍵事實：repo **沒有 `.gitattributes`**，`.pen` 是 UTF-8 純 JSON，git 對它 delta 壓縮效果極好（一次 gc／GitHub 端 repack 後每個快照邊際成本 ≈ 30 KB，不是 1.4 MB）。`design/` 真正的體積負債是 PNG：本次刪掉的 `mengya-diary-crayon-equal.png` 打包後 1.09 MB，且**永遠留在歷史**——F1 縮的是工作區，不是 clone 大小。建議 LS-74 把 scope 從「`.pen` 快照張數」改成「PNG 素材 ＋ 是否該把 `.pen` 標為 binary（現在不標反而是優點，別誤改）」。

**I3 — FLAGGED 51→50 我無法獨立複驗**（PLAUSIBLE，非 blocker）
handoff 的 clipped 節點數 −1「不可歸因」推論需要 Pencil MCP 的 render／measure pass，我只做了靜態分析＋讀已存的截圖，**未重跑全稿掃描**。我沒有理由懷疑（22 個新節點全是 `clip:true` 表格內的 text／frame cell，r2 截圖顯示換行乾淨無裁切），但明確標示：這一條是採信，不是我驗過的。

**I4 — F6 兩列尾註不對稱**（cosmetic，可不修）
9eddc49 把 `print-edge-bottom` 列（`eQ1Ac`）的 `（LS-72 F6）` 尾註移掉了，但 `print-edge` 列（`b7QyOo`）**仍留著**。同表兩相鄰新列一個有票號尾註、一個沒有。r2 截圖看該行不算孤字、可讀性沒問題；且 Color Table 既有列本就帶「（R10）」出處標記，有前例。不值得為它再開一次 `.pen` 落地循環，下次動 Tokens 板時順手抹掉即可。

---

### 已看／未看

**已看**：3 檔完整 diff、`.pen` 全檔結構級比對、7 份 blob 的 landing gate、6 條 commit message gate、WCAG 數字重算、全 repo 引用掃描、merge-tree 衝突預檢、open PR 交集、r2 四張截圖 ＋ r1 f8 抽驗、F8 12 節點的截圖覆蓋率追蹤（6 張直接命中 ID，6 張命中其直屬 `Print Stage` 父框——**12/12 覆蓋成立**，handoff 此處無誇大）。

**未看**：全稿 render／clipping 重掃（見 I3）；`.claude/evidence/LS-72/` 內 29 張截圖只讀了 5 張。

visual-reviewer 的範圍外觀察（`aw57e` 角托 TL 3pt／BL-BR 10pt vs 文件所述 corner-out 5pt）依指示不列為本 PR 缺失——我在 `r1/f8/aw57e.png` 上確認了該不對稱肉眼可見，屬 LS-46 既有幾何，F8 未觸碰其座標，已由 orchestrator 另票。

**可以合併。**

### 2026-08-25T05:14:31.122Z · Chih-Lin Yeh · `744a503b`

## Orchestrator：#119 APPROVE → 併入 development；informational 處置

- **I1**（evidence 路徑寫法）：不修——LS-61 定的固定位置就是主 repo `.claude/evidence/<票號>/`，handoff 用語不準，內容位置正確。
- **I2**（體積估算高估 ~50×，真正負債是 PNG 且永留歷史）：**已修**——LS-74 票文 re-scope（.pen 純 JSON delta-pack 後每版 ~0.03 MB；改針對 `design/*.png` 走 LFS 或縮尺寸，不再考慮 squash 設計 PR）。
- **I3**（FLAGGED 51→50 未重掃）：另票——LS-68 掃描收據 gate 落地後每個設計 PR 必附同 sha 掃描輸出，reviewer 不必再自己重跑。
- **I4**（F6 兩列尾註不對稱）：不修——cosmetic、零像素；下一張動 Tokens 板的設計票（LS-81 或 LS-67）順手對齊。

後續：隨下一批 promote（與 LS-62／LS-64）進 test → main；設計 chore 無 QA 項目（landing gate＋visual-reviewer 已驗），主線併入後 Done。

### 2026-08-25T05:44:40.664Z · Chih-Lin Yeh · `2038c638`

## QA — test branch — 裁決：PASS

環境：worktree `.claude/worktrees/qa-test`（`git fetch origin && git checkout --detach origin/test`），HEAD `01d4e3d`（含 PR #119 `9eddc49`）。本票純設計文件，不涉 DB／模擬器建置。

### 逐條驗收

1. **`design-landing-check.sh design/littlesprout.pen --expect-nodes 2676`** → ✓
   ```
   ✓ design-landing gate 通過：version=2.17，nodes=2676，themes=['mode', 'size']，1463416 bytes，節點數與畫布一致（2676）
   ```
2. **`design/mengya-diary-crayon-equal.png` 不存在（F1）** → ✓（`ls` 回 No such file or directory）
3. **`measurements.json` 的 `candidate` 為 repo 相對路徑（F2）** → ✓（值為 `"design/mengya-diary-crayon-v3.png"`）
4. **Tokens 板兩列（F6）與對比欄（F7）** → ✓（Pencil MCP `get_app_state` 顯示目前 active editor 是 `.claude/worktrees/LS-72/design/littlesprout.pen`，非本次 qa-test worktree 對應檔——判定為佔用，依指示改看既有截圖取證）
   - `.claude/evidence/LS-72/r2/EWCvp.png`、`r6QNL2.png`：`print-edge`／`print-edge-bottom` 兩列並列，數值 8／8，說明文字與 handoff 描述一致。
   - `.claude/evidence/LS-72/r2/MajbP.png`、`i7Fx5.png`：對比欄顯示 `print-ink` 14.92:1／12.55:1、`print-ink-secondary` 9.66:1／8.13:1，與 ticket 給定數字及 merge-review 獨立複算數字（WCAG 2.1 相對亮度公式）逐字吻合。

回歸冒煙：本票零 Swift／SQL 變更（diff 僅 `design/` 3 檔），已隨 LS-62 一併做過 app 啟動＋四分頁導覽冒煙，無 crash（見 LS-62 comment）。

雲端：無 migration，雲端面 N/A。

### 裁決：**PASS** — 4/4 條驗收通過，證據見上。

### 2026-08-25T05:50:11.246Z · Chih-Lin Yeh · `c5606b60`

## 收尾儀式（orchestrator，2026-08-25）

**① dead-code-sweeper（與 LS-62／LS-64 共用）**：零 finding——已刪 PNG 全 repo 唯一殘留是 `jnJqb` 的刻意說明文字；`measurements.json` 路徑相對；`$sp-label` 仍 136 處使用未孤立；F6 尾註不對稱維持 I4 裁定。

**② Lesson learning review**
- **Gate**：landing gate 每 commit 綠（2654→2676）；無漏接。
- **Review**：visual-reviewer 1 輪（F6 說明用了會過期的數字 → 改語意敘述）＋merge-reviewer APPROVE 4 informational。reviewer I2 實測 `.pen` 是純 JSON、git delta-pack 後每版 ~0.03 MB，**推翻 LS-74「逐輪快照是體積負債」的前提** → LS-74 已 re-scope 到 PNG。教訓：**開體積／效能類票前先量**。
- **並行**：QA 想用 Pencil MCP 複驗時被 LS-72 worktree 的 editor 佔用 → 退回看截圖；design／ui 共用 Pen 的規則成立，LS-68 掃描收據 gate 落地後 reviewer／QA 不必再開 Pen。
- **順帶更正一個認知**：sweeper 與 reviewer 都確認 `design/littlesprout.pen` 在磁碟上是明文 UTF-8 JSON（CI 的 `design-landing-check` 本來就在解析它）。「CI 進不了 Pencil」正確的說法是 **CI 沒有 Pen 的版面引擎，做不了溢出掃描／截圖**，不是檔案加密。LS-68 的收據 gate 設計不受影響，票文措辭已需修正（orchestrator 順手改）。

**③ 部署**：純設計檔，N/A。→ 隨 PR #126 併入 main 後 Done；worktree 隨後清。

