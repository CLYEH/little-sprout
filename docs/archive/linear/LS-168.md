# LS-168 Harness：design-evidence gate 補強三項——Notes 板節點 id 存在性檢查（設計稿五度「Notes 落後改稿」）＋收據新鮮度雜湊（拼接收據 gate 盲）＋overflow-scan 第五支「文字遮蔽掃描」（Label × Action Bar／Value × Tab Bar）

| 欄位 | 值 |
|---|---|
| 狀態 | Done（completed） |
| 優先序 | High |
| 標籤 | size:M, lane:harness |
| Cycle | 1 |
| Project | Harness 與協作基建 |
| Milestone | — |
| 估點 | — |
| 建立 | 2026-09-04T05:44:56.398Z |
| 開始 | 2026-09-04T05:44:56.524Z |
| 完成 | 2026-09-04T08:01:50.285Z |
| 建票者 | Chih-Lin Yeh |
| 指派 | — |
| URL | https://linear.app/little-sprout-app/issue/LS-168/harnessdesign-evidence-gate-補強三項-notes-板節點-id-存在性檢查設計稿五度notes |
| 關係 | related→LS-120, related→LS-21, related→LS-159, related→LS-144, related→LS-158, related→LS-122, related→LS-68, related→LS-142, related→LS-67, related→LS-96, related→LS-152, related←LS-238, related←LS-226, related←LS-198, related←LS-185, related←LS-133, related←LS-163, related←LS-171 |

## 描述

## 來源

[LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 三則池項同批升票（harness lane 空、§5-b 優先序 P2）：

* `dbdbbaba`（P2）：設計稿 Notes 板「數字落後最後一次改稿」[LS-142](https://linear.app/little-sprout-app/issue/LS-142/設計相簿頁相簿詳情上傳佇列ls-20-畫面群-ui-designervisual-reviewer-3-輪) 五度復發（R3／R4／R5／R6 後 merge-review 又抓到 9 個數字＋4 個死節點 id）；至少「引用的節點 id 是否存在」可機械驗。
* `6f3b3dd8` 第一項（P2）：`design-evidence-check.sh` 對「收據是修前後拼接、非單一次掃描」完全盲——[LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表) R1 收據 `sibling_intersection` 1792/241 vs 重跑 1791/240，多出的一筆在 head 已不存在，gate 仍綠；[LS-142](https://linear.app/little-sprout-app/issue/LS-142/設計相簿頁相簿詳情上傳佇列ls-20-畫面群-ui-designervisual-reviewer-3-輪) R4 也拆段跑過。
* `6f3b3dd8` 第二項（P3→隨批）：四支正典掃描對「文字被覆蓋層遮蔽」無感（[LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表) R1 BL-2 EULA Action Bar 壓連結、BL-3 膠囊蓋住儲存量；[LS-67](https://linear.app/little-sprout-app/issue/LS-67/設計多寶貝-profile-三畫面建檔管理切換-ui-designervisual-reviewer-3-輪) R1 同 class），VR 每輪自建第五支。

## 範圍

1. **Notes 節點 id 存在性**（新 `scripts/gates/design-notes-check.sh`＋`.py`，或併入 `design-evidence-check.sh`）：對 .pen 內名稱含「實作註記」或「Handoff Notes」的 root frame，抽取其文字節點 `content` 中所有反引號包住、符合 Pencil id 形（5–6 碼英數）的 token，逐一比對 .pen 全樹 id；不存在即紅並列出「板／節點／缺失 id」；**白名單語法**：同句含「已刪除」「原」「當時」「已被…取代」等沿革標記的 id 不算（[LS-142](https://linear.app/little-sprout-app/issue/LS-142/設計相簿頁相簿詳情上傳佇列ls-20-畫面群-ui-designervisual-reviewer-3-輪) R7 慣例），標記字面寫進腳本檔頭。自測：合成 .pen（live id／死 id／沿革標記死 id）三案＋mutation。掛 CI `rules`（.pen 有變更的 PR 才跑，比照 design-evidence-check 的觸發條件）。
2. **收據新鮮度**：`scripts/design/overflow-scan.js` 在 SUMMARY 與收據段多印 `pen_sha256`（掃描當下讀入的 .pen 內容 SHA-256，Pencil `execute` 內以 `Get` 全樹序列化後算——若 execute 環境無法算雜湊，改印 `total_nodes`＋全樹 id 排序後的 FNV／簡單雜湊，檔頭說明）；收據 JSON 加必填欄位 `pen_sha256`（或 `tree_hash`）；`design-evidence-check.sh` 對 `head_sha` 那個 commit 的 .pen 用同一演算法算一次，不符即紅「收據非對 head 單次掃描」。自測：合成 .pen 改一個節點後沿用舊收據 → 紅。**盲區明寫**：只能證明「收據對應這份 .pen」，不能證明四支數字正確（那要 CI 跑 Pencil，本票不做）。
3. **第五支** `text_occlusion`：`overflow-scan.js` 加掃描——同一 root 內任一 text 節點（`type:"text"`）的絕對 AABB 與名稱命中 `OVERLAY_RE`（`Action Bar|Tab Bar|Capsule|Sheet|Scrim|Footer|Toast|Banner`，可用 `SCAN_OVERLAY_RE` 覆寫）且非其祖先的容器交集面積 > 0 → flagged（`node`／`overlay`／`classification`）；SUMMARY 加 `text_occlusion=N`；收據 `scans.text_occlusion.flagged`；gate 驗欄位存在且 **flagged 必為 0**（不接受白名單；VR R1 兩個 BL 都該被這支抓到）。以 [LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表) head `2bd476d` 的 .pen 實跑證明抓到 BL-2／BL-3（handoff 附輸出），以 [LS-142](https://linear.app/little-sprout-app/issue/LS-142/設計相簿頁相簿詳情上傳佇列ls-20-畫面群-ui-designervisual-reviewer-3-輪) head `5a9dc4c` 實跑 0。`scripts/design/overflow-scan.test.js`（若有）補案。
4. `.claude/agents/ui-designer.md`／`visual-reviewer.md`：收據格式加兩個新欄位、第五支語意、Notes id 規約（沿革標記寫法）；`docs/COLLABORATION.md` §7 三列；CI `rules` 掛載（不改「Gate 腳本自測」step 名稱清單——用 run 區塊加行；若名單機制已改自動則沿用）。
5. [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 銷除註記：`dbdbbaba`、`6f3b3dd8`（整則）。

## 不做

CI 上跑 Pencil 重算四支數字；Notes 數字自動重算（`〔量:節點.屬性〕` 標記，留 `dbdbbaba` 第二級另評）；既有票收據回填（新欄位只對本票併入後的新收據要求，gate 對舊收據缺欄位以 head_sha 早於本票併入 commit 為條件放行——寫明）。

## 驗收

- [ ] 三支 gate／掃描自測正負樣本＋mutation 全綠，CI `rules` 綠
- [ ] 對 [LS-152](https://linear.app/little-sprout-app/issue/LS-152/設計設定與成員管理畫面群-刪除帳號流程ls-24封鎖檢舉入口ls-23顯示名稱與頭像退出家庭owner-移除成員刪除單筆內容操作表) `2bd476d` 實跑：Notes id 檢查與 text_occlusion 各抓到 VR R1 對應項；對 [LS-142](https://linear.app/little-sprout-app/issue/LS-142/設計相簿頁相簿詳情上傳佇列ls-20-畫面群-ui-designervisual-reviewer-3-輪) `5a9dc4c` 實跑：Notes id 0 缺失（沿革標記生效）、text_occlusion 0
- [ ] 拼接收據樣本被新鮮度檢查打紅
- [ ] merge-reviewer 通過（誤擋面：既有票收據不被新欄位誤紅）
- [ ] [LS-96](https://linear.app/little-sprout-app/issue/LS-96/harness-待辦池常駐-reviewer-informational盲區補強純效率項先記在這裡滿足-high-以上條件才升獨立票) 兩則銷除註記

## 留言（10）

### 2026-09-04T05:46:07.647Z · Chih-Lin Yeh · `d8c3e805`

orchestrator 2026-09-04 13:46：harness lane 空（LS-159 Done）→ 依 LS-144 開票責任自 LS-96 `dbdbbaba`＋`6f3b3dd8` 開本票（銷除公告 `4f75446f`）；P1 LS-158 留 cycle 2 待 Spec。worktree `hotfix/LS-168-design-evidence-gates` 自 main `9f41909`；派 general-purpose（離線讀 .pen JSON，不開 Pencil）。PR 回 main → back-merge → 生效於 LS-152 R2 之後的收據（R2 收據若已產，VR R2 手動跑第五支）。

### 2026-09-04T06:24:34.311Z · Chih-Lin Yeh · `c1acdcdb`

## handoff — LS-168（2026-09-04 14:23 CST）

**Ticket**：LS-168 Harness：design-evidence gate 補強三項。PR **#269** `hotfix/LS-168-design-evidence-gates` → main，head `e91fb7d`（4 commits，自 `origin/main` 9f41909）。

### 已完成（SHA＋檔案:行號）

1. **Notes 板節點 id 存在性 gate**（池項 `dbdbbaba` 第一級）— `f76ccbb`：`scripts/gates/design-notes-check.sh`（git 端算 head／merge-base／範圍內 .pen commit）＋`design_notes_check.py`（`:35` id 形裸 token 規則、`:40` `HISTORY_MARKERS`、`:104` 判定）＋`design-notes-check.test.sh` 14 案；CI step「.pen Notes 節點 id gate（LS-168）」`.github/workflows/ci.yml:419`＋自測行 `:194`（`e91fb7d`）。判定＝Notes text 內 id 形裸 token ∩「merge-base ∪ 本 PR 範圍內每個觸碰 .pen 的 commit 曾有、head 已無」的 id；**子句級**（。；，切）沿革標記白名單，輸出「板／節點／缺失 id／子句」。
2. **收據新鮮度 `tree_hash`**（池項 `6f3b3dd8` 第一項）— `b2bd4fe` `scripts/design/overflow-scan.js:360-:420`（`canonNode`／`fnv1a64` 16 位元 limb／`treeHash` 逐行相加 mod 2^64，SUMMARY 尾 `tree_hash=` `:517`，`SCAN_HASH_DEBUG` 印單節點行）＋`scripts/gates/design_tree_hash.py`（同規格，含 JS `Number#toString` 數字格式 `:28`）；`e0115d5` `scripts/gates/design-evidence-check.sh:382-:391` 對 `head_sha` 快照重算比對。
3. **第五支 `text_occlusion`**（池項 `6f3b3dd8` 第二項）— `b2bd4fe` `overflow-scan.js:79`（`OVERLAY_RE`＝Action Bar|Tab Bar|Capsule|Footer|Toast|Banner，`SCAN_OVERLAY_RE` 可覆寫）、`:287` `scanTextOcclusion`：text **可見矩形**（AABB ∩ `clip:true` 祖先）× 名稱命中、非祖先、**繪製順序在後**的容器，交集 > 0；範圍同 `corner_anchor`（`flagged` 限 `boards`、`document_flagged` 全稿）；同祖先鏈只報最外層。gate `design-evidence-check.sh:394-:402` 驗 `flagged` 為空（不接受白名單）。
4. agent 定義／docs／CI — `e91fb7d`：`ui-designer.md`（第五支語意、收據格式加 `tree_hash`／`text_occlusion`、整份收據同一次 execute、Notes 沿革寫法子句級規約＋收工自跑 `design-notes-check.sh`）、`visual-reviewer.md`（重掃五支＋`tree_hash` 逐字對帳、Notes 死 id 列入 handoff）、`COLLABORATION.md` §1 Design gate 列＋§7 三列（收據列補 LS-168 段、新增 text_occlusion 列、Notes id 列，含盲區）、`CLAUDE.md` 四支→五支、`ci.yml`（run 區塊加行，step 名稱清單不改）。
5. LS-96 銷除：orchestrator 已貼 `4f75446f`，未再貼。

### 已驗證

- **Notes gate**：自測 14 案全綠＋2 mutation（候選集退化只看 merge-base→⑦紅；白名單退化整句級→④紅）。實跑：LS-142 R6 head `8cd2359`（base 40dd1cd）**缺失 4**＝`Agczg` Q8xZl9／s4VXMV＋`oYEi0`「新 id 為 C0GuD／CVOkb」（＝merge-review c51f982f N2＋MN-N5），`W4X3T`「取代舊 C0GuD」放行；R7 head `5a9dc4c` **缺失 0／沿革 10**；LS-152 `893b642`（.pen 2bd476d）缺失 0（b3nzDp 未引用死 id，候選死 id 只有 JqQZG）；`origin/development` 缺失 0。
- **overflow-scan**：`overflow-scan.test.js` 20 組（＋7）全綠；四份真實 .pen js＝py `tree_hash` 同值：2bd476d `e5b63bc159239c91`、5a9dc4c＝development `3caaac8221da17d8`、8cd2359 `39b3fb5bf2a9f1bd`；FNV-1a 64 對照向量與 13 個數字格式 js＝py。
- **evidence gate**：自測 32 案（25 既有＋㉖～㉛-b）全綠；mutation：拿掉雜湊比對→㉗㉛-b 紅、接受非空 flagged→㉙ 紅、取消舊收據條件→六個既有綠樣本紅。**現行八份收據（LS-142 r1–r7 @4b8e97d、LS-152 r1 @893b642）全綠＋「舊收據放行」行**（放行條件＝`head_sha` commit tree 的 `overflow-scan.js` 不含 `scanTextOcclusion`，不用 cutoff 時間——in-flight 的 LS-152 分支要等併回 development 才拿得到新腳本）。
- **對 .pen 實跑（離線近似，非正典）**：.pen JSON 只存 root／absolute 節點的 x／y，layout 子節點要 Pencil 版面引擎；票文「不需開 Pencil」，故用 scratchpad 近似版面引擎（`LS-168-pen-snapshot.js`，不入 repo）餵純函數：
  - `2bd476d` 以 LS-152 r1 的 34 板為範圍：`text_occlusion` **恰 3 筆＝VR R1 f1cf27d0 的 2 類 3 筆**——`t5wI4` `AlJ6V/n1hzsr Value × ykj25 Tab Bar` 241×22.25、`Rx5mP` `bB1yo/n1hzsr × dXvs2 Tab Bar` 241×22.25、`z042Yg` `PbceX Label × BfVra Action Bar` 306×7.7（VR 實測 241×25／306×5）。
  - `5a9dc4c`／`origin/development` 以 LS-142 r7 的 27 板為範圍：**9 筆，非 0**，全是「Tab Bar 膠囊」class——LS-21 legacy 時間軸 `yXSht`（Name／Sep／Age）、`V4Ktak`／`A33la`（Date Text ×2 各）、`IjWOp` Caption、`Wl90J` Signature Line；加上裁切語意後 LS-142 16 上傳佇列（`rTEGf`／`JnQrT`／`FyV5I`）× Footer 的 6 筆已消失（列在 `clip:true` 的 List `BImYe` 內）。是否為真需 Pencil 正典跑一次確認。

### 未完成／盲區

- **Pencil 端未實跑**（票文明示）：`tree_hash` 同值的前提是 Pencil `Get` 視訪節點與 JSON 節點屬性逐鍵相同（pen-dev 文件「visitor 拿到的是 pure schema data」推定）；首次真實收據若 `tree_hash` 不符 → gate 紅（fail closed），用 `SCAN_HASH_DEBUG = "<id>"` 與 `design_tree_hash.py --dump <id>` 對照單節點行定位。建議 LS-152 R2 收工時 VR 順手比對。
- 5a9dc4c 的 9 筆需 Pencil 確認；若為真，既有板（LS-21／LS-142）在下一次被觸碰時必須整列移出膠囊（規則必然後果，同 `corner_anchor` 對舊債）。**決策點**：是否接受「滾動態文字捲到膠囊底下」為常態而放寬第五支（例如只報部分可見的 straddle）——本票依票文「flagged 必為 0、不接受白名單」未放寬。
- Notes gate 抓不到從未存在過的 id（打字錯）與同一 commit 內建又刪的 id；只驗 id 不驗數字（dbdbbaba 第二級另評）；沿革標記子句級字面比對，子句剛好含「舊」「曾」的活指標會被放行。
- `text_occlusion` 只認名稱允許清單、只看 z-order 不看透明度、非 clip 的兄弟容器背景蓋字抓不到。
- reviewer informational：本票尚無 review；池項處置見 PR body 處置表。

### 偏離票文（已在 PR body 列）

反引號→裸 token＋歷史 id 過濾（實測兩塊目標 Notes 板 0 個反引號）；`OVERLAY_RE` 預設不含 Scrim／Sheet（含進去每張 sheet 板 Status Bar 時間都被報，VR R1 也先扣掉）；`flagged` 限 `boards`；舊收據放行看腳本版本非時間；`CLAUDE.md` 一句同步。

### 風險

誤擋面：舊收據——八份實跑全綠、㉛ 釘住；in-flight LS-152 R2 收據（head_sha 在無新腳本的 commit）照舊 schema 放行。Notes 沿革漏判方向＝誤放行（不誤擋）；誤擋方向只在「本 PR 刪掉的 id 被現在式引用」——正是要抓的。

### 產出位置

PR #269 `https://github.com/CLYEH/little-sprout/pull/269`（head `e91fb7d`）；worktree `.claude/worktrees/LS-168`；PR body `scratchpad/LS-168-pr-body.md`（pr-body-check ✓，反查略過無 LINEAR_API_KEY）；離線近似引擎與四份 .pen 快照在 scratchpad `LS-168-pen-*.js/json`（不入 repo）。CI 未等。

### 2026-09-04T06:59:15.104Z · Chih-Lin Yeh · `11d1894e`

## merge-review R1 — PR #269（head e91fb7d）

裁決：**APPROVE**（無 blocker／無 major；7 條 informational／minor，處置請照 handoff 規約逐條交代）。審查時 head＝`e91fb7dfb8f5dc699188c359d3033073d2364a71`，與 `gh pr view 269 --json headRefOid` 當下值相同；base `9f41909`，diff 13 檔 +1054/−28。CI 四個 job（ci／db／lint／rules）皆 pass。2026-09-04 14:57 CST。

### 我自己重跑了什麼（不是照抄 handoff）

| 項目 | 我的結果 |
|---|---|
| 三支自測 | `design-notes-check.test.sh` 14 案、`overflow-scan.test.js` 20 組、`design-evidence-check.test.sh` 36 行斷言（含 ㉖～㉛-b）全綠 |
| 自測有沒有牙齒（我自己做的 6 個 mutation） | M1 白名單退化成整句級→④紅；M2 候選集只看 merge-base→⑦紅；M3 拿掉「曾存在」過濾→①⑤⑥紅；M4 拿掉 tree_hash 比對→㉗㉛-b 紅；M5 接受非空 flagged→㉙紅；M6 取消舊收據放行→6 個既有綠樣本紅。**全部被殺，測試不是形式** |
| Notes gate 實跑 | LS-142 R6 `8cd2359`（base `40dd1cd`）**缺失恰 4**：`Agczg`／Q8xZl9＋s4VXMV、`oYEi0`／C0GuD＋CVOkb＝c51f982f N2＋MN-N5；R7 `5a9dc4c` 缺失 0／沿革 10；LS-152 `893b642` 缺失 0；`origin/development` 缺失 0 |
| tree_hash js／py | 對四份真實 .pen 自己重算：`2bd476d`＝`893b642`＝`e5b63bc159239c91`、`5a9dc4c`＝development＝`3caaac8221da17d8`、`8cd2359`＝`39b3fb5bf2a9f1bd`，js 與 py 逐字同值；另外做 619 筆 fuzz 交叉（400 條含 astral／控制字元／CJK 的隨機字串＋219 個數字含 1e21／1e-7／denormal 邊界）js＝py，mismatch 0 |
| 既有收據誤擋面 | 用本 head 的 gate 跑 LS-142 r1–r7（head `4b8e97d`）＋LS-152 r1／**r2**（in-flight head `9714b66`）→ **9 份全綠，每份都印「舊收據放行」行**。LS-152 r2 是手跑第五支、有 `scans.text_occlusion` 但無 `tree_hash`，gate 照舊 schema 放行，符合設計 |
| 效能 | 真實 7746 節點 .pen：`treeHashLines` 27ms＋`treeHash` 16ms（1.73M 字元）；合成 7840 節點快照 `scanTextOcclusion` 5ms、五支 `scanAll` 27ms。即使 Pencil sandbox 慢 50×也在 2s 級，**無效能 finding** |
| Notes gate 誤擋面（我加測的） | 掃 145 個觸碰 .pen 的 commit 得歷史 id 宇宙 13,980 個；development 的 4 塊 Notes 板有 638 個 id 形 token（241 活 id／20 歷史死 id／**377 個是英文詞與數字**，如 `Action`／`Banner`／`CLAUDE`／`109pt`／`1F1F1F`）。**這 377 個沒有任何一個在 13,980 個歷史 id 裡**——「∩ 歷史 id」這個設計把誤擋面壓到實測 0 |
| 四維度 | race：無 Swift／無並發寫入，gate 是單進程 CLI，`git show` 子行程互不共享狀態——**無可審之處**；效能：見上；平行：CI step 之間本來就獨立，沒有被不必要序列化的工作——**無可審之處**；scope：見 N0 |

### Finding

**N1（minor，可重現）舊收據放行有繞過窗口** — `scripts/gates/design-evidence-check.sh:268-269`（`fifth` 以 `head_sha` 那個 commit 的 tree 有沒有 `scanTextOcclusion` 判定）。
失敗情境（我在合成 repo 完整重現）：分支已經把含第五支的新腳本併進來（**PR head tree 有** marker），但最後一次觸碰 .pen 的 commit 早於那次併入 → 收據可以完全不填 `tree_hash`／`scans.text_occlusion` 仍然全綠並印「舊收據放行」。這正是 LS-152 R3+ 的形狀（r2 的 `head_sha=4453198` tree 是舊腳本；只要之後幾輪不再動 .pen，新鮮度檢查永遠不會生效）。
不是「沿用舊腳本就永遠放行」——`fifth` 看的是 commit tree、不是 agent 實際跑了哪版腳本，所以分支只要拿到新腳本又動過 .pen 就跑不掉；漏的只有上面這個窗口。
建議：`fifth = (head_sha tree 有) 或 (is_latest 且 PR head tree 有)`。只對最新輪次加嚴是刻意的——最新輪次的 `head_sha` 必須是 last_pen_commit（既有 F2 規則），.pen 內容與工作區相同，拿新腳本重跑一次就填得出對得上的 `tree_hash`；舊輪次的收據產於新腳本存在之前，回填不可能，維持現狀。
附帶：`script.returncode != 0`（淺 clone／物件缺失）也會靜默落到「放行」，建議區分「檔案不存在」與「git 失敗」，後者 fail closed。

**N2（minor，PLAUSIBLE）tree_hash 依賴兩個未文件化的 Pencil visitor 假設，Pencil 端未實跑** — `scripts/design/overflow-scan.js:483-489`。
`Get((n, c) => … canonNode(n, c.parentCtx…, c.index))` 用了 `c.index`；repo 既有腳本與 skills／agent 文件只出現過 `ctx.bounds`／`ctx.parentCtx`／`ctx.skipChildren`／`ctx.problems`，**`index` 一次都沒有**。若 Pencil context 沒有 `index`，每行變成 `<pid>\tundefined\t…`，tree_hash 與 py 永遠不符 → 第一份新收據被打紅，訊息卻指控設計端「收據不是對這份 .pen 單一次掃描」（誤導方向）。`n` 的鍵集若與 JSON 節點不同（多／少任一鍵）結果相同。
我能佐證的一半：8 份既有收據的 `total_nodes`（Pencil 未展開走訪計數）與 JSON 樹節點數一直對得上（我重算 `893b642`＝7746，與收據相符），所以**走訪集合**這一半的假設已被歷史資料驗證；剩下的是 per-node 鍵值與 `index`。我沒有 Pencil，無法重現，故標 PLAUSIBLE、不擋併入。
低成本消除法（建議 orchestrator 在生效前做，30 秒）：請 ui-designer／VR 對 development 快照跑一次 execute，比 SUMMARY 的 `tree_hash` 是否等於 `3caaac8221da17d8`。若不符，改用「同 parent 的到訪序號自行累計」取代 `c.index`（兩端都算得出、不依賴未文件化欄位）。另建議 gate 訊息補一句「若確定是單次掃描，可能是 tree_hash 規格與 Pencil 端不一致，回報 LS-168」。

**N3（minor，實測）沿革標記白名單過寬——25.4% 的 id 引用位置會被自動放行** — `scripts/gates/design_notes_check.py:40-44`。
票文列的是「已刪除／原／當時／已被…取代」；實作另加 `舊`／`曾`／`→`／`重建`／`不存在`／`已改`／`已於`／`取代`。我對 development 的 4 塊 Notes 板量了誤放行面：768 次「活 id 引用」中有 **195（25.4%）**所在子句已經含標記——這些位置的 id 一旦變死，gate 會靜默放行。最大宗是 `→` 60 筆（7.8%，多數是數字轉場如「板高 z6DOE：1134→1031」，與 id 沿革無關）與 `原` 76 筆（9.9%，如「CHLio 原本沒有角色列」）。
方向是誤放行、不誤擋，且 gate 仍抓到 LS-142 R6 的全部 4 筆，所以不擋併入。建議把 `→` 收緊成「箭頭與該 id 緊鄰」（`<id>\s*→` 或 `→\s*<id>` 的一側），或要求標記與 id 距離 ≤ N 字。

**N4（informational）tree_hash 只涵蓋節點樹，`variables`／`themes`／`fileToken` 偵測不到** — `design_tree_hash.py: iter_lines`／`overflow-scan.js: treeHashLines` 只走 `doc.children`。實測 .pen 頂層鍵是 `version`／`children`／`themes`／`variables`／`fileToken`。掃描後只改 design token（`variables`）再落地，tree_hash 不變、gate 綠。Pencil `Get` 只走節點樹，補不進去屬工具限制；建議把檔頭與 §7 的「收據對應這份 .pen」改成「對應這份 .pen 的**節點樹**」，並把 variables／themes 列成明寫盲區。

**N5（informational）Notes gate 涵蓋面：11 個頂層 `type:"note"` 的「設計註記 · …」節點不在掃描範圍** — `design_notes_check.py: NOTES_NAME_RE`（只認「實作註記／Handoff Notes」）＋`text_nodes()`（只收 `type=="text"`）。實測 `893b642` 有 11 個頂層 note 節點，內文全是裸 id 引用（`aODMQ`「原 R2 板（dMUcT）…board 重建為 E4Q1C8」、`ZFqQ5`「（H0KHI 與本壓力板的 yWH0V）…（GgbFw/Family Caption）」）。我用「名稱加『設計註記』＋型別加 `note`」對 LS-142 R6 重跑：**額外命中 0**（那些 id 早在 merge-base 前就死了、不在候選集），現階段無實害。實作與票文字面一致，僅列涵蓋面備忘——一行改動就能收進來。

**N6（informational）收據不驗第五支的 `boards` 範圍** — `design-evidence-check.sh:394-402` 只驗 `scans.text_occlusion.flagged` 為空，不像 `corner_anchor` 那樣要求 `boards` 覆蓋本 PR 觸碰的板；掃描輸出本來就帶 `text_occlusion.boards`，但收據 schema（ui-designer.md）沒收。手動縮小第五支範圍目前沒有機械攔截。建議收據加 `text_occlusion.boards` 並要求與 `corner_anchor.boards` 相同（零額外成本，同一次 execute 就有）。

**N7（informational）gate 執行會在 repo 內留下 `scripts/gates/__pycache__/`** — `design-evidence-check.sh:212-213` 的 `sys.path.insert` ＋ `import design_tree_hash`。LS-168 worktree 現在就有未追蹤的 `scripts/gates/__pycache__/design_tree_hash.cpython-312.pyc`；`.gitignore:48` 只 ignore `design-canvas*/__pycache__/`，所以它會出現在 `git status`、有被 `git add -A` 誤收的風險。建議腳本內設 `PYTHONDONTWRITEBYTECODE=1`（或 `sys.dont_write_bytecode = True`）或補 .gitignore 一行。

**N0（scope，無 finding）** — 13 個檔案全部落在票文 1–4：三支腳本＋兩支 py＋三份自測、ci.yml（自測 run 區塊加一行、新 step，**step 名稱清單確實沒改**）、兩份 agent 定義、§1 Design gate 列＋§7 三列。`CLAUDE.md` 那一句（四支→五支＋`tree_hash`＋Notes gate）我判定**必要**：該行是「每輪收工必附收據」的硬規則正文，留著「四支」會與 §7 和 gate 實際行為衝突，屬手術式同步、非順手改動。`overflow-scan.js` 對既有四支的唯一觸碰是快照多帶一個 `clip` 欄位（第五支所需）與 `scanAll`／SUMMARY／exports 加項，沒有無關重構。

### 第五支對 Tab Bar 膠囊的政策建議（給 orchestrator，不擋併入）

先講事實：`5a9dc4c` 的 9 筆分佈在 5 塊板——`yXSht`（LS-21／11 時間軸 iPhone，Name／Sep／Age）、`V4Ktak`（同 · 深色）、`A33la`（Stress／11 時間軸 · 單一寶貝 filter）、`IjWOp`（A11y／14 相簿 · AX3 40pt，Caption）、`Wl90J`（Stress／14 相簿 · 超長相簿名，Signature Line）。五塊都是 `clip:true` 的整頁 frame ＋ 一個 `Tab Bar` ref 子節點；因為第五支的可見矩形已經 ∩ 過 `clip:true` 祖先（板自己就是），這些文字**在該板渲染出來時真的被膠囊蓋著**，不是被裁掉的區域。這與既有的 LS-120 R7 慣例（「膠囊頂 − 最後一項可見葉節點的底 ∈ 17–67pt」）方向一致：稿面本來就不該讓內容壓在膠囊下。

- **A. 照票文一律 0（現狀）**：利——單一標準、不可白名單，與 `corner_anchor` 一致；VR R1 的 BL-2／BL-3 都抓得到。弊——這 5 塊 legacy／Stress／A11y 板被任何一票觸碰時必須整改；`Stress / 超長相簿名`、`A11y / AX3` 這類「刻意極端」板沒有出口，可能誘發縮 `SCAN_BOARDS`（明文禁止）或為了過 gate 改稿。
- **B. 板名白名單（滾動態／Stress／A11y 前綴豁免）**：省事，但白名單一開就會擴散，違背 LS-122 對 `corner_anchor`「不接受白名單」的既定教訓。**不建議**。
- **C. 稿內顯式標記（例如板上放一個名為 `Scroll State` 的標記節點，或節點名尾綴）**：機械可驗、必須在稿內留痕、reviewer 一眼看得到，比板名白名單難濫用。若日後真的出現「必須呈現內容捲到膠囊下」的板，走這條。
- **D. 幾何放寬（遮蔽面積比 > X% 才報）**：**明確不建議**——VR R1 的真 BL-2 交集只有 306×7.7pt，任何大到足以放過滾動態的門檻都會一併吞掉 BL-2，等於廢掉這支。

我的建議：**維持 A**，把這 5 塊板列為既有債、下一次觸碰時一次清償（與 `corner_anchor` 對舊債的處理一致）；真的出現例外需求再走 C。誤紅他票的風險我機械查證過：`flagged` 只算 `SCAN_BOARDS` 內的板，其餘只進不擋 gate 的 `document_flagged`（`overflow-scan.js:328-343` ＋自測「(e) 範圍」案），所以他票不會被這 9 筆誤紅。

### 我沒看的範圍（明講）

- **Pencil 端一次都沒跑**（票文明示、我也無工具）：`tree_hash` 的 Pencil／JSON 逐位元一致、第五支對真實稿的絕對座標，兩者都只在 node 純函數與 JSON 層驗過。
- **`2bd476d` 的 3 筆與 `5a9dc4c` 的 9 筆我沒有獨立重算幾何**——.pen JSON 只存 root／absolute 節點的 x／y，layout 子節點要 Pencil 版面引擎，我沒有重建近似引擎（重建一份自己的近似引擎再拿它「驗證」對方的近似引擎，證據力等於零）。我做的是結構面複核：那 5 塊板的型別／`clip`／Tab Bar ref 子節點結構與 handoff 的敘述一致，且 .pen schema 沒有 `zIndex` 之類的鍵（實測頂層鍵表），所以第五支「陣列順序＝繪製順序」的 z-order 假設站得住。


### 2026-09-04T07:32:16.511Z · Chih-Lin Yeh · `95919be5`

## handoff — LS-168 R2（merge-review R1 `11d1894e` 修正輪，2026-09-04 15:29 CST）

**Ticket**：LS-168 Harness：design-evidence gate 補強三項。PR **#269** `hotfix/LS-168-design-evidence-gates` → main，head **`5a9dc42`**（R1 head `e91fb7d` 之後 3 支 commit）。CI 四項綠：ci／db／lint／rules（run 33848157037）。

### 已完成（R1 finding 逐條）

| # | 處置 | commit／位置 |
|---|---|---|
| **N1**（minor）舊收據放行可繞過 | **已修** | `57dc007` `scripts/gates/design-evidence-check.sh`：新 `has_fifth(rev)`（`git ls-tree` 區分「檔案不存在→False」與「git 失敗→None」），`fifth = bool(fifth_at_sha) or (is_latest and bool(fifth_at_head))`，`head` 由 bash 多傳一個參數進 python；`None` 進 errs（fail closed，不再靜默放行）；缺欄位且是 PR head 逼出的必填時多印一行「head_sha 落地時正典腳本尚無第五支，但 PR head 的 tree 已含…重跑補進」；放行行對最新輪次加印「PR head 的亦無」。檔頭寫明只對最新輪次加嚴的理由（F2 使 head_sha＝last_pen_commit、內容＝工作區，新腳本重跑即可填；舊輪次回填不可能） |
| **N3**（minor）`→` 子句級放行過寬 | **已修** | `07d58d2` `scripts/gates/design_notes_check.py`：`HISTORY_MARKERS` 移除 `→`，新增 `ARROW_AFTER_RE = r"\s*→"`，判定改 `HISTORY_RE.search(clause) or ARROW_AFTER_RE.match(content, m.end())`——只放行**緊鄰箭頭左側**的死 id（「舊→新」的舊）；右側是現行 id、死了即缺失。docstring 第 5 點、缺失訊息、`design-notes-check.sh` 檔頭與盲區 (3)（補 R1 N3 的 25.4%／60／76 數字）、`ui-designer.md:81`、COLLABORATION §7 Notes 列同步 |
| **N2**（minor，PLAUSIBLE）`c.index`／visitor 鍵集 | **未修，待 VR**（見下） | — |
| **N4**（informational）tree_hash 不含 `variables`／`themes` | **已修** | `5a9dc42`：`design-evidence-check.sh` 檔頭、`overflow-scan.js:70`、`design_tree_hash.py` docstring、COLLABORATION §7 收據列——「收據對應這份 .pen」→「對應這份 .pen 的**節點樹**」，明列頂層 `variables`／`themes`／`fileToken` 不在雜湊內（Pencil `Get` 只走節點樹） |
| **N5**（informational）頂層 `type:"note"` 不在 Notes gate 範圍 | **記入 LS-96 待辦池 `83d24594`** | 池文一行：`notes_boards()` 加收 `type=="note"` 且名稱含「設計註記」、`text_nodes()` 加收 note 的 `content`＋一案自測＋四真實 head 複驗；不在 R2 做：票文範圍明寫 root frame 名稱、reviewer 對 R6 重跑額外命中 0 |
| **N6**（informational）不驗 `text_occlusion.boards` | **記入 LS-96 待辦池 `83d24594`** | 池文一行：收據格式加 `text_occlusion.boards`、gate 在 `fifth` 生效時要求集合＝`corner_anchor.boards`，LS-152 r2（手跑第五支、只有 flagged）在 `fifth` 未生效前不得誤紅；不在 R2 做：改 schema 需同步 ui-designer.md／gate／自測＋in-flight 相容條件 |
| **N7**（informational）`scripts/gates/__pycache__` 殘留 | **已修** | `5a9dc42` `design-evidence-check.sh`：python 呼叫加 `PYTHONDONTWRITEBYTECODE=1`；worktree 既有 `__pycache__`（含 reviewer 留下的 `design_notes_check.cpython-312.pyc`）已移除，R2 全部自測與真實收據重跑後無殘留 |

PR body 已於 **2026-09-04 07:18:42Z（15:18 CST）**更新（`LS-168-pr-body-r2.md`，pr-body-check ✓：LS-96 行 6 條皆帶 comment id、「已修」行 4 條皆帶 SHA），**在最後一支 push（`5a9dc42`，15:18:50 CST）之前**完成，rules job 讀到的是新 body，四項綠、不需 close／reopen。

### 已驗證（怎麼驗）

- **N1 自測**：`design-evidence-check.test.sh` **40 案**全綠（36 既有＋新 ㉜／㉜-b／㉝）。㉜＝reviewer 重現形狀：`land5 … 0`（.pen 落地、tree 無腳本）→ 收據缺兩欄位 → 另一 commit 加含 `scanTextOcclusion` 的腳本（不碰 .pen）→ 期望紅且輸出含「PR head 的 tree 已含」；㉜-b 同 head_sha 用新腳本重跑（`hash_of`）補齊 → 綠；㉝ r5（舊輪次缺欄位）＋併入腳本＋r6 落地五支齊全 → 兩份綠、r5 印放行行。
- **N1 mutation 證據**：把 `fifth = bool(fifth_at_sha) or (is_latest and bool(fifth_at_head))` 改回 `fifth = bool(fifth_at_sha)` → ㉜ **變綠**（輸出「…尚無第五支、PR head 的亦無，LS-168 新欄位 tree_hash／text_occlusion 不要求——舊收據放行」，期望 exit 1 實得 0），㉝ 仍綠（如預期，它釘的是另一面）；改回後全綠。
- **九份真實收據**（reviewer 列的誤擋面）：從 `4b8e97d`／`9714b66` 以 `git archive` 取出收據放進 worktree（未追蹤、驗完刪除），R2 gate 跑 `--ticket LS-142 --base 7f335d8^1 --head-sha 4b8e97d`（merge-base＝`40dd1cd`）→ **r1–r7 七份綠＋七行放行**（r7 最新，印「PR head 的亦無」）；`--ticket LS-152 --base origin/development --head-sha 9714b66` → **r1／r2 兩份綠＋放行**（LS-152 分支 head tree 的 `overflow-scan.js` 不含 `scanTextOcclusion`，實測 0 命中，所以 r2 最新也不受 N1 加嚴影響）。修前同樣九份綠（先跑過一次對照，行為只多了「PR head 的亦無」字樣）。
- **N3 自測**：`design-notes-check.test.sh` **17 案**全綠（14 既有＋⑩ 數字轉場「板高 Xk9f2：1134→1031」死 id 不緊鄰箭頭→紅且不印沿革、⑪「Xk9f2→Zq7Lm」兩側都死→右側 Zq7Lm 紅、左側 Xk9f2 印沿革行）。
- **N3 mutation 證據**：把 `r"→"` 放回 `HISTORY_MARKERS` → ⑩ 與 ⑪ **同時變綠**（期望 exit 1 實得 0）；改回後全綠。
- **N3 真實 head 鑑別力**（修前／修後各跑一次、diff 輸出）：`8cd2359`（base `40dd1cd`）**缺失恰 4**（`Agczg` Q8xZl9／s4VXMV、`oYEi0` C0GuD／CVOkb）、沿革 4，唯一差異是尾行提示文字；`5a9dc4c` **沿革 10／缺失 0，輸出逐字相同**（10 筆沿革命中的是 原／取代舊／已於…刪除重建／當時，沒有一筆靠 `→`）；`893b642`（base origin/development）0；`origin/development`（base origin/main）0。
- **N3 收窄的誤擋面實測**：development 4 塊 Notes 板 216 個 `→`，左側緊鄰 id 形 token 28 個、右側 11 個，**全部是英文詞**（Apple／Google／Header／Filter／Anchor／circle…），無任何真 id；其餘 186 個是「55→60」類數字轉場——所以 `→` 的子句級放行從未保護過任何真 id，收窄零誤擋。
- `overflow-scan.test.js` 20 組全綠（R2 只改檔頭註解）；`bash -n`／`ast.parse`（`-W error`）／`node require` 全過；cumulative diff `e91fb7d..5a9dc42` 恰 9 檔 +121/−19，worktree 乾淨、無 `__pycache__`。
- CI：`gh pr checks 269` 四項 pass（ci 7m21s／db 3m08s／lint 14s／rules 3m25s），head `5a9dc42`。

### 未完成／盲區

- **N2 待 visual-reviewer**（Pen 現被 LS-152 設計票佔用、`pen-open.sh` 會擋，本輪未碰 Pencil）：請 VR 以 Pencil `execute` 對 **同一份 .pen（development 快照）** 跑正典腳本，比對 SUMMARY `tree_hash` 是否＝js／py 的 `3caaac8221da17d8`（`2bd476d`＝`893b642` 應為 `e5b63bc159239c91`）；不符則用 `SCAN_HASH_DEBUG = "<id>"` 與 `design_tree_hash.py --dump <id>` 對照單節點行，若差在 `c.index`，改「同 parent 到訪序號自行累計」。首份真實新收據若 `tree_hash` 不符，gate 是 fail closed、訊息會指向設計端——這是已知風險，見下。
- **N3 取「左側」而非派工文字的「右側」**：orchestrator 指示寫「只放行箭頭右側（或 reviewer 建議的更窄形狀）」，reviewer 建議「`<id>\s*→` 或 `→\s*<id>` 的一側」。我取左側：語意上「A→B」的 A 是被取代者、B 是必須存在的現行 id；自測 ③ 既有的「Xk9f2→Zq7Lm」與 LS-142 R7 慣例同形；真實資料兩側都沒有真 id、選哪側對現況零差異。若要改右側，`ARROW_AFTER_RE.match(content, m.end())` 換成對 `content[:m.start()]` 的 `→\s*$` 一行即可。**請 orchestrator 確認**。
- N1 的「git 失敗 fail closed」分支沒有合成自測：head_sha 已由 rev-list 驗證存在、PR head 已 rev-parse 過，`ls-tree` 對它們失敗只會發生在物件損毀／淺 clone（CI `fetch-depth: 0`），無法乾淨合成；程式碼路徑存在但未被測試釘住。
- N5／N6 只入池未修（池文 `83d24594`）；N4 是文字修正，`variables`／`themes` 仍是 tree_hash 真盲區（工具限制，Pencil `Get` 不走它們）。
- 既有盲區不變：Notes gate 抓不到從未存在過的 id 與同 commit 內建又刪的 id、不驗數字；`text_occlusion` 只認名稱清單、不看透明度；`5a9dc4c` 的 9 筆 Tab Bar 膠囊 class 待 Pencil 正典跑證實＋orchestrator 對 reviewer 政策建議 A 裁定。

### 風險

- **LS-152 併回 development 後**：r2（最新，head_sha `4453198`）會落入 N1 的加嚴——PR head tree 有新腳本 → 必填 `tree_hash`／`text_occlusion`；VR 需對 `4453198` 快照用新腳本重跑補進 r2（或另交 r3，head_sha 同為 last_pen_commit）。若當時 Pencil 端 `tree_hash` 與 py 不符（N2 未消除），LS-152 會被打紅且訊息指控「非單次掃描」——**建議 N2 比對在 LS-152 併 development 之前做**。
- 誤擋面：九份既有收據綠、㉝ 釘住舊輪次不受 PR head 影響；N3 方向是「少放行」，真實資料零命中差異。
- LS-142 / LS-152 收據檔在驗證時曾以未追蹤檔形式短暫放進 worktree，已刪除、未入任何 commit（`git status` 乾淨）。

### 產出位置

- PR #269 `https://github.com/CLYEH/little-sprout/pull/269`，head **`5a9dc42`**（`57dc007` N1 → `07d58d2` N3 → `5a9dc42` N4＋N7），PR body 更新 **2026-09-04 07:18:42Z**（15:18 CST），四項 CI 綠。
- worktree `.claude/worktrees/LS-168`（乾淨）。
- scratchpad：`LS-168-pr-body-r2.md`、`LS-168-r2-handoff.md`、`LS-168-r2-evtest-after.txt`／`-mutN1.txt`、`LS-168-r2-notestest-after.txt`／`-mutN3.txt`、`LS-168-r2-real-{142,152}-{before,after}.txt`、`LS-168-r2-notes-{before,after}-{8cd2359,5a9dc4c,893b642,development}.txt`。
- LS-96 池文 comment `83d24594`（N5／N6）。

### 2026-09-04T07:41:09.351Z · Chih-Lin Yeh · `ba6976b8`

## merge-review R2 — PR #269（head 5a9dc42，增量 e91fb7d..5a9dc42）

裁決：**APPROVE**（無 blocker／無 major）。R1 的 N1／N3／N4／N7 **四條全部修對且我自己複驗過**；N5／N6 入池 `83d24594` 同意；N2 留 VR（Pencil）——處置合理。新增 4 條 informational（R2-I1～I4），皆不需在本票處理。審的 head＝`5a9dc427a26ff08b920b276991a0c06a0e345b3b`，與 `gh pr view 269 --json headRefOid` 當下值相同；增量 9 檔 +121/−19，CI 四項 pass（run 33848157037）。2026-09-04 15:39 CST。只審增量，R1 已審過的部分不重審（`overflow-scan.js` R2 只動註解——我驗過非註解行變更數＝**0**，所以 R1 對第五支演算法／tree_hash 的結論原封不動繼續有效，且 R2 後重算 `5a9dc4c`＝`3caaac8221da17d8`、`893b642`＝`e5b63bc159239c91` js＝py 未變）。

### N1（舊收據放行繞過窗口）— 已關閉，我用 R1 的合成 repo 原地重跑

用的就是 R1 那份 `LS-168-bypass/`（分支已併入含第五支的新腳本、最後一次 .pen commit 早於它、收據不填新欄位）：

| 情境 | R1 gate | R2 gate |
|---|---|---|
| 窗口本體（最新輪次、head_sha tree 無第五支、PR head tree 有） | **綠＋放行行**（漏） | **紅**，第一條就是「…但 PR head 的 tree 已含（新腳本已併入本分支）…用現行腳本對它重跑一次補進」 |
| 用新腳本重跑補齊 `tree_hash`＋`text_occlusion`（可滿足性） | — | **綠**（`tree_hash 對應 head_sha 快照、text_occlusion.flagged 為空`）——證明新要求真的做得到，不是死路 |
| 補了 `tree_hash` 但填舊值（拼接） | — | **紅**「tree_hash 不符」 |
| 舊輪次 r1 缺欄位＋最新 r2 齊全（不回溯） | — | **兩份皆綠**，r1 印放行行、不帶「PR head 的亦無」 |
| 負控：最新 r2 抽掉新欄位（head_sha tree 已含第五支） | — | **紅** |

`has_fifth()` 三態設計正確：`ls-tree` rc≠0 → `None`；輸出為空（檔案不存在）→ `False`；再 `git show` 抓標記。`bool(None)` 落回 False 但 `errs` 已進一條，方向是 fail closed。**我實跑了那條 handoff 自陳「沒有自測釘住」的 git 失敗分支**：把 `ls-tree` 改成帶一個不存在的旗標讓它真的非 0 退出 → 原本綠的兩份收據全部轉紅並印「無法判定…（git ls-tree／show 失敗，淺 clone 或物件缺失）——不能靠猜放行舊收據（fail closed）」。**分支確實通電**，只是沒有 repo 自測釘住（見 R2-I4）。

自測牙齒（我自己做的 mutation）：`fifth = bool(fifth_at_sha) or (is_latest and bool(fifth_at_head))` 退回 `bool(fifth_at_sha)` → **㉜ 立刻變綠**（㉜-b 的斷言也垮）。㉜／㉜-b／㉝ 三案確實各釘一面。

### N3（`→` 收窄）— 語意正確，而且實作者選對了邊

**派工單寫的「右側」是反的，實作者取左側才對。**「A→B」＝A 被 B 取代：B 是現行 id、**必須存在**；A 是被取代者、才該放行。放行右側等於放行「新 id 為 C0GuD」這種現在式活指標——那正是 LS-142 R6 被抓到的 MN-N5 本體。我 R1 的措辭（「`<id>\s*→` 或 `→\s*<id>` 的一側」）兩邊都列、沒有指定，實作者補上了正確的判斷。

我獨立重量了誤擋／誤放行面（origin/development 的 Notes 板，不看 handoff 數字）：

- 箭頭總數 **216**（與 handoff 一致）。
- 箭頭**左側**緊鄰的 id 形 token 14 種、**右側** 7 種，**全部是英文詞**（`Anchor`／`Apple`／`Chrome`／`Filter`／`Google`／`Header`／`lucide`／`circle`…），與現行節點 id 交集 **0**——所以收窄零誤擋，`→` 的子句級放行從來沒保護過任何一個真 id。
- 自動放行率：**25.4%（195/768）→ 17.7%（136/768）**，收窄拿回 59 個位置。

四個真實 head 鑑別力維持：`8cd2359` **缺失恰 4**（同樣是 `Agczg`／Q8xZl9＋s4VXMV、`oYEi0`／C0GuD＋CVOkb）、`5a9dc4c` 0 缺失／10 沿革、`893b642` 0、`origin/development` 0。自測 17 案全綠；mutation：把 `→` 放回 `HISTORY_MARKERS` → ⑩⑪ 同時變綠；拿掉箭頭左側放行 → ③⑪ 變紅。**兩個方向都被釘住**。

邊界形狀我另外探了 5 種（直接餵模組）：`A／B→C`（A 死、無其他標記）→ A 紅、B 放行；`板高 Xk9f2：1134→1031` → 紅（修正目標）；`Xk9f2→Zq7Lm` 左死 → 放行、右死 → 紅；換行後接箭頭 → 放行（跨行，無害）。行為與檔頭一致。

### N4／N7 — 修對

- **N4**：`design-evidence-check.sh` 檔頭、`overflow-scan.js` 檔頭、`design_tree_hash.py` docstring、COLLABORATION §7 收據列四處都改成「這份 .pen 的**節點樹**」並明列 `variables`／`themes`／`fileToken` 不在雜湊內。措辭正確、沒有多改演算法（非註解行變更 0）。
- **N7**：`PYTHONDONTWRITEBYTECODE=1` 加在 python 呼叫上。我**在自己環境沒設任何環境變數**（`env -u`）跑 gate 與 `design_tree_hash.py`，`find scripts -name __pycache__` 兩次都是空——靠腳本自己就守得住。兩個 worktree 現在都無殘留。

### 誤擋面複驗（九份真實收據＋下一個會撞到的真 PR）

- LS-142 r1–r7 @`4b8e97d`：**7/7 綠**；LS-152 r1／r2 @`9714b66`：**2/2 綠**，r2（最新）多印「、PR head 的亦無」——LS-152 分支 head tree 確實還沒有第五支（我實測 `grep -c scanTextOcclusion` ＝0）。
- 我另外跑了 **LS-152 當下 head `92e0669`（R3）**——這是下一個會撞到本 gate 的真 PR：r1 綠、r2 紅，但紅在**既有的 F2 規則**（最新輪次的 head_sha 必須是最後一次 .pen commit），原因是 `design/evidence/LS-152-r3-overflow.json` 目前還是未追蹤檔（`git status` ＝`??`）、不在 diff 候選內。**與 LS-168 無關**；r3 一 commit 就對上，且該分支 head tree 仍無第五支 → 照舊 schema 放行。**LS-168 不會對 LS-152 造成假紅。**

### R2 新增 informational（都不必在本票處理）

**R2-I1（cosmetic）同一原因印三條錯誤** — `design-evidence-check.sh` 新的專屬提示之後，流程仍會落到通用的「tree_hash 必須是 16 碼小寫 hex（收據=None）」與「scans.text_occlusion 缺失」。三條講同一件事，第一條已經給了做法。建議在 `fifth and not fifth_at_sha` 這個分支 append 完就跳過後兩條（或把後兩條收成一條）。無功能影響，看了不會被誤導。

**R2-I2 沿革白名單殘量 17.7%** — `→` 拿掉後，剩下的主要是 `原` 76 筆（「CHLio 原本沒有角色列」）、`舊`／`取代`／`曾` 等子句級字面。票文本來就把「原／當時／已被…取代」列為合法標記，再收窄（例如要求標記與 id 鄰近 N 字）屬判斷題、有誤擋合法沿革敘述的風險。**建議記入 LS-96 池、不在本票做**——本票要修的 `→`（唯一一個「幾乎全是數字轉場」的標記）已經修掉。

**R2-I3（行為變更備忘）`A／B→C` 形狀** — 非緊鄰箭頭的死 id 現在會紅（R1 的子句級 `→` 會放行）。這是收緊、方向正確，但屬行為變更；實測對四個真實 head 零影響（箭頭鄰接的 21 個 token 沒有一個是真 id）。檔頭已寫明「只放行緊鄰箭頭左側」，我認為足夠。

**R2-I4 git 失敗分支無自測** — handoff 自陳、我同意無法乾淨合成（head_sha 與 PR head 都已先被 rev-list／rev-parse 驗過）。我用 mutation 實跑證實它通電（見上）。若日後想釘住，可在自測裡把 `PATH` 前置一支會對 `ls-tree` 回非 0 的假 `git`。優先序低。

### 仍未消除（carry-forward）

**R1-N2（Pencil 端 `c.index`／visitor 鍵集）**依然開著，處置「留給 VR」我同意（本輪 Pen 被 LS-152 佔用）。但**時序上更急了一階**：N1 收緊之後，LS-152 一旦把 development 併回分支（拿到新腳本），它最新輪次的收據就**必須**填 `tree_hash`——那會是第一份由 Pencil 真正產出的 tree_hash。若 `c.index` 假設不成立，LS-152 會被打紅、而且訊息指控「非單次掃描」。**建議：把 VR 的比對排在「LS-168 back-merge 進 development」與「LS-152 併入 development」之間**，比對值 development 快照＝`3caaac8221da17d8`。

### 四維度（增量）

race：無並發、無共享狀態，`has_fifth` 是同步子行程——無可審之處。效能：每份收據多 1 次 `git ls-tree`、最新輪次再多 1 次 `ls-tree`＋`show`；LS-142 七份收據實跑與 R1 無感差異（tree_hash 重算本來就只有 43ms／份）。平行：無。scope：9 檔全部對應 N1／N3／N4／N7，`overflow-scan.js`／`design_tree_hash.py` 只動註解與 docstring（非註解行變更 0），無順手改動、無範圍外檔案。


### 2026-09-04T07:43:38.738Z · Chih-Lin Yeh · `165394c8`

orchestrator 2026-09-04：merge-review R2 `ba6976b8` APPROVE（status `merge-review=success` @ `5a9dc42`）→ PR #269 併入 main（merge commit `df95691`）。back-merge main → development PR #271 已開（`merge-tree` 預檢無衝突，CI 綠即以 merge commit 併入）。收尾：sweeper 進行中；R1-N2（Pencil 端 `tree_hash` 與 js／py 一致性）排在 LS-152 VR R3 用 Pencil 對同一份 .pen 比對，時點在 LS-168 併回 development 之後、LS-152 併 development 之前（reviewer R2 風險項）。

### 2026-09-04T07:48:47.093Z · Chih-Lin Yeh · `91d6c1c7`

## dead-code-sweeper 收尾巡檢（main df95691）

2026-09-04 15:47 CST。範圍：`git diff 9f41909..5a9dc42`（PR #269，13 檔 +1054/−28+121/−19 累計）。方法見文末。**不動任何檔案**，唯讀命令（grep／Read／`git show`／`git diff`）＋在原地跑三支自測驗證仍綠（`PYTHONDONTWRITEBYTECODE=1`，跑後 `find scripts -iname __pycache__` 確認無殘留）。

### Findings（本 feature 引入）

**F1（minor，可機械證明）`scripts/gates/design_notes_check.py:43-46` `HISTORY_MARKERS` 有 2 個項目被同一元組內其他項目結構性吃掉，regex 層面永遠不會是「決定性」的那個 alternative**

```python
HISTORY_MARKERS = (
    r"已刪除", r"刪除重建", r"刪除舊", r"刪 ", r"原(?!因)", r"當時", r"已被", r"取代", r"舊", r"重建", r"曾",
    r"不存在", r"已改", r"已於",
)
```

`刪除重建`（字元「刪除重建」）本身就含子字串「重建」——而「重建」已是同一元組的獨立項；`刪除舊` 同理含子字串「舊」——「舊」也已獨立列在元組裡。因為這是 `"|".join(...)` 的 OR 交替，任何會命中 `刪除重建` 的文字必然也會命中 `重建`，任何會命中 `刪除舊` 的文字必然也會命中 `舊`——這兩項在目前的元組組成下**不可能改變任何一次判定結果**，是可證明的死 alternative（不是「未覆蓋」而是「被同表其他項結構性吸收」）。

證據（實跑）：把這兩項從元組移除、對 12 個含各種標記寫法的樣本字串（含 `③` 自測用的「原…已於R2刪除重建」「取代舊」「刪除重建」「刪除舊版元件」等）逐一比對完整表與移除後的表，**全部 12 例判定結果相同（SAME）**——附我在 sandbox 跑的驗證腳本輸出：
```
原Q8xZl9已於R2刪除重建 True True SAME
Row現為Zq7Lm取代舊Xk9f2 True True SAME
已刪除的節點 True True SAME
刪除舊版元件 True True SAME
刪除重建 True True SAME
當時的id為X True True SAME
已被取代 True True SAME
不存在的id True True SAME
已改版面 True True SAME
曾經是 True True SAME
刪 X True True SAME
```

**注意與 `design-notes-check.sh:14-17` 檔頭註解的關係**：檔頭解釋的是「為什麼 `刪` 沒有做成獨立的寬鬆標記」（避免 LS-152「刪除帳號」流程敘述把同子句活指標整句放行）——這個理由只支持「`刪 `（帶空白）要窄」，並不支持「`刪除重建`／`刪除舊` 這兩個複合詞需要獨立存在」，因為它們要生效的前提（含「重建」或「舊」）本來就已經被同表的獨立項涵蓋。程式碼本身正確、不誤判、不誤放行——這純粹是**多餘但無害**的 dead alternative，兩種處置皆可：
- **刪除**：拿掉 `r"刪除重建", r"刪除舊",` 兩項，行為零變化（含現有 `design-notes-check.test.sh` 案 ③／④／⑧ 在內的全部自測仍綠——已用上面等價性驗證間接證明）。
- **保留＋加註解**：純為人類讀者的自我文件用途（讓「支援哪些常見寫法」一眼看懂），加一行註解說明「刪除重建／刪除舊 技術上已被 重建／舊 涵蓋、僅為可讀性保留」以免下一個讀者誤以為它們在做獨立工作。

建議：不擋 LS-168（已 APPROVE 併 main，行為無誤），**併入下一張觸碰 `design_notes_check.py` 的票順手清或加註解**；若都不順手，記入 LS-96 待辦池。

### 逐項排查但判定「非死碼」的項目（附排除理由，避免誤報）

- **`HISTORY_MARKERS` 其餘 12 項**（`已刪除`／`刪 `／`原(?!因)`／`當時`／`已被`／`取代`／`舊`／`重建`／`曾`／`不存在`／`已改`／`已於`）：兩兩交叉檢查（含正則子字串包含關係），無其餘結構性吸收關係；但**是否在目前真實 `design/littlesprout.pen` 的 Notes 板文字裡曾經被命中**（尤其 `刪 `／`曾`／`已被`／`不存在`／`已改`）我**沒有獨立佐證**——.pen 是加密檔案、工具規約禁止 Read/Grep，只能靠 Pencil MCP 讀，而本次任務要求唯讀不開 Pencil。merge-review R2 只公布了 `原`（76 筆）／`舊`／`取代`／`曾` 的量，未逐一列出其餘標記的命中數，這是**盲區**、非「已排除」。
- **`scripts/design/overflow-scan.js` 匯出符號**（`module.exports` 新增 `OVERLAY_RE`／`scanTextOcclusion`／`canon`／`canonNode`／`fnv1a64`／`hex64`／`addLimbs`／`treeHash`／`treeHashLines`）：逐一比對 `overflow-scan.test.js`，除 `addLimbs` 外全部被直接呼叫測試；`addLimbs` 未被測試直接呼叫，但被 `treeHash`（有測試）與 Pencil execute 分支內部呼叫，非孤兒——判定：**非死碼**，只是沒有獨立單元測試覆蓋這一個內部 helper（風格與其他同表匯出符號一致，屬對稱匯出方便除錯，不算異常）。
- **`scripts/gates/design_tree_hash.py` `--dump` CLI 分支**（`main():132-139`）：`overflow-scan.test.js:416-419` 有實際呼叫 `python3 design_tree_hash.py <path> --dump <id>` 並比對輸出——**非死碼**。
- **`design-evidence-check.sh` 是否留有被 R2 新條件取代的舊分支**：通篇只有一個 `fifth = bool(fifth_at_sha) or (is_latest and bool(fifth_at_head))` 賦值（`:299`），未發現任何殘留的「只看 `fifth_at_sha`」舊寫法或並存分支——R1→R2 是原地修改、非疊加。R2 自己新增的 informational **R2-I1**（`fifth and not fifth_at_sha` 分支之後仍會落到通用檢查、對同一原因印出最多三條錯誤訊息，`:412-434`）我複核仍存在，但這是**已由 merge-reviewer 記錄並裁定「不必在本票處理」的既有 informational**，不是我新發現的死碼，不重複列為 finding。
- **`.github/workflows/ci.yml` 新 step**（`.pen Notes 節點 id gate（LS-168）`）：`$BASE` 沿用 job-level env（`:38`）、`--head-sha` 與既有 `design-evidence-check` step 同一套慣例，未發現指向已改名／不存在檔案的路徑。
- **CLAUDE.md／COLLABORATION.md／ui-designer.md／visual-reviewer.md**：新增段落與既有「四支」敘述並存（`COLLABORATION.md` §7 用「附加段落」方式在既有 LS-122 四支說明後面接上 LS-168 五支/`tree_hash`/Notes 三個新表格列，未見遺留「四支」但語意上應為「五支」的錯誤敘述——`§1 Design gate` 列與 `CLAUDE.md` 硬規則正文均已同步為「五支」；`§7` 表格內部沿用「四支」字樣的地方都是在描述 LS-122 當時的歷史決策段落，語意正確、非誤植）。
- **三支自測腳本**（`design-notes-check.test.sh` 17 案、`design-evidence-check.test.sh` 33 案、`overflow-scan.test.js` 20 組）：逐案讀過，未發現恆真斷言（如比對空字串）或被跳過的案例；我原地重跑三支，**全數通過**，且跑完 `scripts/gates` 下無 `__pycache__` 殘留。

### 建議處置

- F1：**併入下一張觸碰 `design_notes_check.py` 的票順手清（刪 2 行或加註解）**；若近期無票觸碰該檔，記入 LS-96 待辦池（純效率／可讀性項，非功能缺陷，不須升獨立票）。
- 盲區（HISTORY_MARKERS 其餘 12 項在真實 .pen 的命中率）：不建議為此開票——這需要 Pencil 存取，且 merge-review R2 已對最大宗的 `原`／`→` 做過實測；若之後 Notes gate 又復發漏放行，屆時再用同一手法（Pencil 讀 Notes 文字＋離線 regex 統計）查證。

### 既有死碼觀察

無——本次巡檢範圍限定 `9f41909..5a9dc42` 的 13 個變更檔案，未涉及本 PR 未觸碰的既有程式碼，未發現需要另外開票的既有死碼。

### 結論

本 feature（LS-168）引入的程式碼結構乾淨，**僅 1 項極輕微、可證明無害的 dead alternative**（`HISTORY_MARKERS` 內 `刪除重建`／`刪除舊` 被同表 `重建`／`舊` 結構性吸收），無殘留鷹架／debug 輸出/停用旗標、無孤兒匯出符號、無指向不存在檔案的設定、CI／自測接線與文件敘述一致。

### 搜尋方法與盲區聲明

- 方法：`git diff 9f41909..5a9dc42`（stat＋逐檔全文）、`grep -n` 交叉比對函式宣告與呼叫點／`module.exports`／CI step 名稱／文件內腳本路徑引用；三支自測腳本在本機原地重跑（`PYTHONDONTWRITEBYTECODE=1`）驗證仍綠且無 `__pycache__` 殘留；`HISTORY_MARKERS` 冗餘用 python 正則等價性測試獨立驗證（12 樣本，含表內舊有測試字串）。
- 盲區（誠實聲明）：① `.pen` 為加密檔案、工具規約禁止 Read/Grep，本次未開 Pencil，故無法獨立核對 Notes 板真實文字中各標記（尤其 `刪 `／`曾`／`已被`／`不存在`／`已改`）的實際命中情況，只能信賴 merge-review R1/R2 已公布的部分統計（`原`／`→`）與程式邏輯本身的正確性；② 純文字/正則交叉比對抓不到 reflection、動態字串拼接呼叫（本次未見此類寫法，13 個變更檔皆為靜態呼叫）；③ 未驗證 Pencil `execute` 端實際執行結果（`tree_hash`／`text_occlusion` 對真實稿的數字）——這是票文本身已明列、且由 merge-review R1 N2／風險段落追蹤中的既有已知未完成項，非本次死碼巡檢範圍。


### 2026-09-04T07:49:40.659Z · Chih-Lin Yeh · `9e91127d`

orchestrator 2026-09-04 **收尾 lesson learning review**（PR #269 → main `df95691`；back-merge PR #271 CI 中）：

- merge-review R1 `11d1894e` APPROVE（minor N1／N2／N3）→ R2 修 N1／N3／N4／N7、N5／N6 入池 `83d24594` → R2 `ba6976b8` APPROVE。sweeper `91d6c1c7`：1 minor（`design_notes_check.py:43-46` `HISTORY_MARKERS` 內 `刪除重建`／`刪除舊` 被 `重建`／`舊` 結構性吸收）→ LS-96 入池，不另票。
- **未閉合項**：R1-N2（Pencil 端 `tree_hash` 與 js／py 一致）——已交 LS-152 VR R3 三方比對；結果貼本票。若不一致即開 hotfix 修 LS-168。
- Harness 教訓：(1) 兩位 reviewer 都靠**合成 repo 重現**與 **mutation** 才找到 N1 繞過窗口與 N3 誤放行方向——自測「有牙齒」的證明應成為 harness 票 handoff 必附項（本票 R2 起已照做）；(2) 派工單把沿革箭頭放行寫成「右側」是語意錯誤，實作者依測試案例改為左側並說明，reviewer 判定實作者對——派工指令對語意細節要引用既有測試案例而非憑印象。
- 設定優化：無。工具缺口：Pencil 端算法只能由持 Pen 的 agent 驗證（單檔獨佔），harness 票的 Pencil 面驗證要排進 design 票的 VR 輪。
- cleanup：`cleanup-merged.sh --apply LS-168` 移除 worktree／分支。

→ PR #271 併入 development 後改 Done。

### 2026-09-04T07:56:31.429Z · Chih-Lin Yeh · `6383b2fa`

## LS-168 R1-N2 三方 `tree_hash` 一致性複驗 — **不一致，已定位根因（Pencil `Get` 省略 `geometry`）**

visual-reviewer 2026-09-04 15:55，受 orchestrator 指派在 LS-152 R3 審查中順帶做 LS-168 的三方比對。**未改動任何腳本**（只把主 checkout 的兩支複製到 scratchpad 執行，避免 W4 主 checkout 禁寫 hook）。

### 受測對象

- `.pen`：`/Users/clyeh/little-sprout/.claude/worktrees/LS-152/design/littlesprout.pen`（LS-152 HEAD `7197b8d`，.pen 落地 `92e0669`，Pen 正開著同一檔）
- 腳本：主 checkout `df95691` 的 `scripts/gates/design_tree_hash.py` 與 `scripts/design/overflow-scan.js`

### 三方結果

| # | 算法 | `tree_hash` | `total_nodes` |
|---|---|---|---|
| 1 | `design_tree_hash.py <pen>` | `03e7804b035d8e4b` | 8242 |
| 2 | node：`treeHash(treeHashLines(doc))`（同一支 js） | `03e7804b035d8e4b` | 8242（lines=8242） |
| 3 | **Pencil `execute`**（檔頭／§7 規定的 visitor：`canonNode(n, c.parentCtx?c.parentCtx.node.id:null, c.index)`，逐行 FNV-1a 64 相加） | **`84420d7b6419b40e`** | 8242 |

**py 與 node 相同；Pencil 與兩者都不同。** 節點數三方一致（8242），所以不是走訪範圍或 index 問題。

### 根因：Pencil `Get` 把 `geometry` 的路徑資料省略成字面字串 `"..."`

逐 root 分割雜湊比對後，第一個不一致的 root 是 `y6MxMV`（`cmp/Button Apple`），它底下有 `type:"path"` 的 `WiqyL`（Apple Icon）。直接對照該節點：

- **磁碟 JSON／py／node**：`"geometry":"M14.02 10.6c0.02-2.2 1.8-3.26 1.88-3.31-1.02-1.5-2.62-1.7…"`（完整 SVG path，數百字元）
- **Pencil `Get` 回傳**：`String(n.geometry)` 長度 **3**，值就是 `"..."`

其餘鍵完全一致（我對 `WiqyL`／`sD5Ma`／`DhOiK`／`GuOM0`／`mzo0K` 五個節點逐一比對 `Object.keys` 與 `viewBox`／`width`／`height`／`x`／`y`，全部相同；`OePXK`、`t5wI4` 兩個非 path 節點的 canon 行也與 `--dump` 逐字元相同）。

### 決定性證明

把磁碟 JSON 裡**所有帶 `geometry` 鍵的節點**的該值改寫成 `"..."`，其餘一律不動，重算：

```
JSON 原樣                     tree_hash = 03e7804b035d8e4b  nodes=8242
JSON 把 geometry 換成 "..."   tree_hash = 84420d7b6419b40e  nodes=8242  改寫節點數=8
Pencil 實測                   tree_hash = 84420d7b6419b40e
=> geometry 是唯一差異來源？ 是（完全吻合）
```

**8 個節點**（＝Pencil visitor 統計的 `geometry:8`，完全對上）：`WiqyL`（cmp/Button Apple）、`sD5Ma`／`DhOiK`（**`cmp/Photo Corner`**）、`GuOM0`／`aQq5T`／`SieME`／`j7iqM`（cmp/Button Google 的四段 G 弧）、`mzo0K`（Squircle）。

### 影響評估

1. **這不是本稿特例，是全專案性的**：`cmp/Photo Corner`（`GEBcf` → `sD5Ma`／`DhOiK`）是角托元件，本專案幾乎每張稿都用到。任何含 path 節點的 `.pen` 都會命中。
2. **`design-evidence-check.sh` 的 `tree_hash` 比對永遠不會通過**：收據裡的 `tree_hash` 由設計端從 Pencil SUMMARY 抄下（檔頭第 21 行明文「`tree_hash` 抄 SUMMARY 的值」），gate 端用 `design_tree_hash.py` 對 `head_sha` 那份 .pen 重算——兩邊的輸入本質不同，必然不等。這條「收據新鮮度」防線目前是 **fail-closed 但不可通過**，設計端沒有任何合法做法能讓它綠。
3. LS-152 R3 的收據（`design/evidence/LS-152-r3-overflow.json`）目前**沒有** `tree_hash` 也沒有 `text_occlusion`（designer 用的是 LS-168 併入前的四支版本），所以還沒撞到；但一旦 LS-152 依 LS-168 規約重出收據就會撞。

### 建議修法（三選一，我不改腳本，交 LS-168 判斷）

- **(a) 兩邊都排除 `geometry`**：`canonNode` 的 body 過濾從 `k !== "children"` 擴成 `k !== "children" && k !== "geometry"`，py 的 `node_line` 同步。代價是 path 資料改變不會被 tree_hash 察覺（新增盲區，需寫進檔頭「盲區」段）。
- **(b) 只雜湊 `geometry` 的長度或前 N 字元**：兩邊都改成 `geometry: String(v).length`，保留「路徑被換掉會變雜湊」的偵測力，且 Pencil 端拿不到內容也能算出長度——**但要先確認 Pencil 是否連長度都給不了**（我實測 `String(n.geometry).length === 3`，代表 Pencil 端連長度都是省略後的，這條路走不通，除非改用 Pencil 的其他 API 取得真值）。
- **(c) Pencil 端改用 `Get(id, {includePathGeometry:true})`**：`execute.md` 的 `GetOptions` 有 `includePathGeometry` 旗標。**這是我建議優先驗證的方向**——若 visitor 型 `Get(visit, {includePathGeometry:true})` 會回傳完整 geometry，那 (c) 是零盲區的正解，只要在檔尾那次雜湊走訪加上這個選項即可。我沒有實測這個旗標（不改腳本、也不想在 LS-152 的稿上多跑無關實驗），留給 LS-168。

### 附：可重現步驟

```
python3 scripts/gates/design_tree_hash.py <pen>                 # 03e7804b035d8e4b
node -e 'm=require("scripts/design/overflow-scan.js"); …'        # 03e7804b035d8e4b
# Pencil execute：檔尾 hash 走訪片段（canonNode + c.index + fnv1a64 逐行相加）
#   → 84420d7b6419b40e
python3 scripts/gates/design_tree_hash.py <pen> --dump WiqyL     # 看完整 geometry
# Pencil: Get(n => n.id==="WiqyL" && Print(String(n.geometry).length))  → 3
```

`overflow-scan.test.js` 的 js／py 交叉一致案之所以綠，推測是合成節點樹裡沒有 `geometry` 鍵——建議補一個帶 `type:"path"`＋`geometry` 的案例把這個 class 釘住（不然修好也沒有 gate 守著）。

*（LS-152 R3 verdict 已同步註記本項；LS-152 若要滿足 LS-168 的收據規約，需等本票修好，orchestrator 需安排先後順序。）*

### 2026-09-04T08:01:54.533Z · Chih-Lin Yeh · `cd3f0707`

orchestrator 2026-09-04 16:00：back-merge PR #271 併入 development（merge commit `cda2132`，`git diff origin/main origin/development` 為空；head `df95691` 依 §2 貼 `merge-review=success`「promote: no content diff」status `53528337420`）。收尾儀式完成 → **Done**。R1-N2 經 LS-152 VR R3 三方比對證實為 bug（Pencil `Get` 省略 `geometry`，comment `6383b2fa`）→ 以 hotfix **LS-171** 追蹤（Urgent，blocks LS-152），不重開本票。

